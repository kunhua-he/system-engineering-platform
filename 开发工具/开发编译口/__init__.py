# 开发编译口：底座开发循环的一键口（华哥 2026-09-19 裁决「易语言模式」）。
# 一条命令 = 影响面清单 + 生成物自动联动 + 静态规则全查 + 定向测试点名。
# **默认全自动**（华哥 2026-09-22）：按 `开发文档/规范/生成物登记表.json` 自动生成全部
# 派生凭据（摘要/依赖派生/说明书载位/文档账目/元信息头），人只需改代码；
# `--只读` 是并行多写者同改一个仓库时的降级口。
# 退出码是调用方唯一判定依据：0 干净通过，1 失败（有红项），2 未核验（门禁自身无法执行）。
