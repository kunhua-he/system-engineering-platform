"""性能基准包：只用标准库的可重复度量工具。

解决的问题：平台此前**完全没有度量手段**，「每调用一线程开销」「网关空跑多少 QPS」
「SQLite 写多少 TPS」这类问题只能靠感觉争论。本包把争论变成可复现的数字。

覆盖四个场景（见 `基准场景.py`）：
1. 原子能力直调（数据操作支持库.数据交换 的纯计算能力）p50/p99；
2. 网关空跑（进程内起真实 HTTP 网关 + 网关核心 + 后端核心，发最小请求）QPS 与 p50/p99；
3. SQLite 写（经唯一 SQLite 支持库写入**独立临时库**）TPS；
4. 线程包装开销（同线程直调 vs 经超时执行器线程包装的差值，直接回答「每调用一线程」之争）。

口径约束（与本仓铁律一致）：
- **只用标准库**（time / statistics / json / threading / sqlite3 经能力间接使用）；
  不引 pytest-benchmark、不引任何第三方。
- **不为测性能加载模型**：只装配 2 个纯计算支持库包，不导入任何大模型/显存占用路径。
- **不碰正式运行库**：SQLite 场景写独立临时库；后端核心的运行缓存根指向临时目录，
  正式 `工程缓存/运行数据`、`工程缓存/权威状态` 只读不写。
- **不抢制品仓库**：不启动制品、不调 HTML 黑盒验证器，网关用进程内测试服务器。

命令行入口：`运行基准.py`；反向验证：`反向验证.py`；口径与用法：`说明/性能基准说明.md`。
"""
