"""验证门禁四「平台判断越界检测」——只读检查器，把「平台判断只许出现在两个收口层」落成机器判据。

## 为什么需要它（未完成事项 #105）

铁律出处（华哥 2026-09-16 裁决，`AGENTS.md` 跨平台收口条 + `开发文档/项目说明.md` §4）：

> 平台差异只允许在 `公共契约/运行时/平台适配/`（纯平台判定；2026-09-23 已由同名包
> 承担，导入地址 `公共契约.运行时.平台适配` 逐字不变）和
> `公共契约/运行时/进程终止.py`（进程与进程组）两处判断。调用点一律改调收口层，
> **调用点不许自己写平台判断**（禁 `sys.platform` / `os.name` / `是Windows()` /
> `taskkill` / `os.killpg`）；某处必须加平台判断才能改通时，报回来补收口层，不许就地分叉。

**接线前的现场事实**（第一轮《审计_平台判断越界_20260919》§七 #105）：
在三个门禁目录搜 `sys.platform` 与 `os.name` 两种字面量**只有 1 处注释命中** ——
铁律写了、收口层也建了，但没有任何自动防回潮判据。
按哲学 13.1「判据在、门禁不调即纸面规则」，靠人记得 grep 不算强制点。

**为什么复用这里而不是另造第二套扫描器**（哲学 1.2 禁第二实现）：
`开发工具/验证门禁/` 已经是一套「独立只读检查器 + 门禁公共骨架 + 分桶存量基线 + 三拍自证」
的完整形态，且已由 `开发工具/开发编译口/编译口.py` 的 `门禁清单` 接进必经路径
（名字「重复腿与旁路」，角色=阻断）。本检查器**沿用同一套骨架**（`门禁公共.跑标准主流程`
/ `检查结论` / `命中` / `读基线` / `判定` / `写基线段`），只新增判据本体。

## 判据（收口层白名单 + 其余位置判红）

白名单**只有两个收口层**，与铁律原文逐字对应。**形态有两种**（2026-09-23 起）：

| 收口层 | 形态 | 为什么是收口层 |
|---|---|---|
| `公共契约/运行时/平台适配/` | **前缀**（包：14 个职责子文件 + `__init__.py`） | 纯平台判定（谁能依赖它）；原语本体 |
| `公共契约/运行时/进程终止.py` | **精确**（单文件） | 进程与进程组（POSIX/Windows 终止语义差异） |

**为什么 `平台适配` 走前缀而不是精确路径**（2026-09-23 实测）：该文件已按职责拆成
**同名包**（哲学 6.1；华哥 2026-09-23 点名），`公共契约.运行时.平台适配` 这个导入地址
逐字不变，但包内 14 个子文件的相对路径**都不等于**原来那一条精确路径 ⇒ 精确匹配下
**收口层本体反被判「裸读平台标志」**。故白名单改为「精确集合 ∪ 前缀集合」双形态，
前缀只此一条，与铁律原文的两个收口层仍一一对应。
**这不是放宽判据**：`进程终止.py` 仍是精确匹配，前缀也不会覆盖任何非收口层文件
（`公共契约/运行时/` 下没有第二个以 `平台适配/` 起头的路径）。

白名单外任何位置出现下列**取值形态或平台专有用法**即命中（AST 判据，不做行文本正则）：

| 规则号 | 形态 | 说明 |
|---|---|---|
| 一 | 裸读 `sys.platform` / `os.name` / `platform.system()` / `platform.machine()` | 铁律点名禁的四种读法（`platform.*` 两种是第一轮审计 E-1 实测散落的同一类） |
| 二 | **在控制流条件位**调用收口层分支判定原语 `是Windows()` / `是macOS()` / `是Linux()` / `是POSIX()` | 「取值后自行分叉」：即使平台值来自收口层，**分支动作**仍决定行为 → 那是第二份实现（与 `测试中心/运行核心/测试_跨平台收口原语.py` 的「禁止取值后分叉」同口径） |
| 三 | 白名单外**直接用平台专有机制/常量**：调用 `os.fork()` / `os.setsid()`；读 `os.sep` / `os.path.sep`；调 `chmod` / `fchmod` / `lchmod`；字面量写死 `/tmp` | 「平台专有用法」（2026-09-25 补门禁盲区）：与规则一**语义不同** —— 规则一读「我是哪个平台」的**身份值**，规则三用「只有某平台才有的**动作/字面量**」，故另立一条规则而不是 `裸读形态表` 的表扩充（理由与各落点的收口腿见下文） |

**规则二为什么限定「控制流条件位」**（判据精度，2026-09-20 接线时定）：
铁律禁的是**行为分叉**——即平台值**决定走哪条路**。判据若把「任意位置的调用」
都算违规，会把两类**观测位**误判成分叉：

- `if` / `while` / 三元 / `and`·`or` / 推导式条件 **之外的**位置调用它，只用于
  **报告与展示**（例：`开发工具/环境自检/探测_进程组.py` 把 `是POSIX()` 放进详情字典
  `{"是POSIX": 平台适配.是POSIX()}` 供人读；`自检_解释器与平台.py` 把四个判定原语
  放进台账字典）。这类位置**不改变行为路径**，是自检工具「把收口层判定结果摊开给人看」
  的正当用法。
- 把它们判红会逼出两种坏结果：要么给判据开一堆白名单口子（白名单本身就是第二个
  判据事实源），要么把观测改成「从展示值反推」（自证恒真，对账价值归零）。

故判据落点 = **控制流条件位**（`If.test` / `While.test` / `IfExp.test` / `BoolOp.values`
/ 推导式 `ifs` / `Assert.test` 内部的调用）。这是「有没有决定行为」的机器可判形式。

**规则三（平台专有用法）为什么是新的一条规则、而不是 `裸读形态表` 的表扩充**
（2026-09-25「补门禁盲区」现场判定）：

- **判据语义不同**：`裸读形态表` 四条全是「**读平台身份值**」（`sys.platform` / `os.name` /
  `platform.system()` / `platform.machine()` —— 都是「我是哪个平台」）；规则三管的是
  「**用平台专有动作/字面量**」—— `os.fork()` 一个字也没读平台标志，却只在 POSIX 上存在。
  判据落点因此不同：规则一落在「值」，规则三落在「动作 / 字面量」。
- **正确写法不同族**：规则一的改法一律是「改调 `平台适配` 的取值原语」；规则三的改法分属
  **进程 / 路径 / 权限 / 临时目录**四族，各表说明逐条点名收口层**有**的腿，**没有**的腿
  逐字写明「无替代腿，须先补」（不编替代腿）。
- **混表的代价**：既有四条说明逐条点名 `平台适配` 的腿，把平台专有用法塞进同一张表会让
  「说明」口径不一；且「命中该报哪条规则」失去区分度 —— 判据本身也就说不清了。

规则三的四个落点（都只在**白名单外**判）：

| 落点 | 机器形式 | 收口层有没有腿（2026-09-25 现场核实） |
|---|---|---|
| 专有调用 | `ast.Call` 的函数链 = `os.fork` / `os.setsid` | 有「要求 POSIX 能力」门（`平台适配.要求POSIX能力`）与「启动上下文」（`多进程启动上下文`）；**裸 `os.fork()`、「把当前进程自己 setsid」无替代腿，须先补** |
| 专有常量 | `ast.Attribute` 链 = `os.sep` / `os.path.sep` | **无替代原语**；路径边界判定的唯一腿在 `支持库/后端/系统核心支持库/路径安全.校验路径` |
| 权限位 | `ast.Call` 名为 `chmod` / `fchmod` / `lchmod`，**且所在最内层函数体内没有收口层门 `支持chmod()`** | 有门 `平台适配.支持chmod()` 与删除三原语（`清只读后删除树` / `确保可删` / `清除只读属性`）；**「原子写保留原权限位」无替代腿，须先补** |
| 临时目录字面量 | `ast.Constant` 字符串是**绝对路径**且根段 = `tmp`（`写死临时目录根段表`），**且不是裸字符串语句**（排除文档串） | **无替代原语**（`平台适配.平台稳定缓存根()` 是**缓存根**不是**临时目录**，语义不同不可互顶） |

**为什么权限位落点要「函数体内有 `支持chmod()` 就豁免」**：`平台适配.支持chmod()` 是收口层
发给调用点的**门**（样板 = `平台控制面/包仓库/平台客户端制品.py::_加固密钥权限`：`if not
支持chmod(): return` 之后才 `chmod`）—— 判据来自收口层原语、调用点不写平台判断，是收口层
认可的写法。不豁免会把它判成假红（本仓反复栽过「判据太宽＝假红」，教训见
`公共契约/运行时/写入授权.py`）。

**为什么 `/tmp` 落点要排除裸字符串语句**：全仓大量**文档串**引用冻结基线路径
（`/tmp/拆分基线/…`）与说明文字；把文档串判红同样是「判据太宽＝假红」。文档串在 AST 里
恒是「裸表达式语句」的值，故按 `Expr(Constant)` 精确排除，不另开白名单口子。

**规则三只认调用位**：裸引用（`目标 = os.fork`）与能力探测（`hasattr(os, "fork")`）不判
—— 后者正是收口层 `要求POSIX能力` 自己教的写法（「按能力探测而非按名字硬判」）。

扫描面 = `门禁公共.正式层名表`（复用唯一事实源，**不含 `测试中心`**；`测试中心`
因此不在面内 —— 这一点由扫描层名表的来源决定，不由本文件另写一张名单）。

测试侧（`测试中心/**`）**不在扫描面内**：那是模拟另一平台所必需的
（反向验证的目的就是模拟另一个平台，必须直接换掉最底层标志 —— 先例见
`测试中心/运行核心/测试_跨平台收口原语.py` 的桩姿势说明）。

### 与既有检查器的分工（不重复判、不互相顶替）

- `调用腿唯一性检测` 判的是「模块库/技能库/运行核心的**调用腿**唯一」——
  与平台判断无交集；
- `开发工具/复用审计/能力调用图审计` 判「原子旁路/越界导入」——
  平台判断不在它的判据集内；
- `运行核心/依赖防火墙` 只看 **import 方向**：`import platform` / `import sys`
  都是标准库、方向合法 → 实测全绿，**看不见平台取值点散落**。

### 覆盖不了什么（如实声明）

- 判据是**取值形态**级，不是完整数据流级：把平台值经中间变量层层传递后再分叉
  （`标志 = 平台适配.原始平台标志()` 再 `if 标志 == "win32"`）抓不到
  —— 这类形态属语义级判据，本项目暂无；
- 能力探测类写法（`hasattr(os, "killpg")`）**不判**（铁律自己也认这种写法：
  `平台适配.当前平台()` docstring 原文「调用方应按能力探测而非按名字硬判」）；
- **`os.name` 的「探测/自检」用途**：`开发工具/环境自检/` 下的
  `os.name == "nt"` 是**自检项自己的期望值口径**（拿它与收口层判定对账，
  判「平台判定与本机布局是否矛盾」），属**观测**不是**行为分叉**——
  但它读的确实是平台标志，故一并冻结进存量基线（只报不拦），
  不额外开豁免口子（豁免口子本身就是第二个判据事实源）。
- **规则三只认上文四种机器形式**，不是完整数据流级：`分隔符 = os.sep` 经中间变量传递后
  使用、`getattr(os, "fork")()` 这类间接调用，抓不到（与规则一同一边界）；
- **规则三本轮只补「上一轮点名的五类」**，其余平台专有形态仍是盲区（如实声明，不假装
  全覆盖）：`os.killpg` / `os.getpgid` / `os.waitpid`、`taskkill`、`start_new_session=True` /
  `creationflags=`、`os.altsep` / `os.pathsep` / `os.linesep`、`/private/tmp`（macOS 上
  `/tmp` 的等价写法，`开发工具/门面核验.py` 有一处）等；
- **规则三的权限位豁免是「函数体内出现门名」级**，不是数据流级：同一函数体里只要提到
  `支持chmod` 就豁免（哪怕门与 `chmod` 不在同一条执行路径上）—— 这是为「不假红收口层
  认可的样板」付出的精度代价；
- **规则二的既有窄口（本轮未动，超出本次租约）**：上文写「形如 `平台适配.是Windows()` 或
  直接 `是Windows()`」，但实现只认 `节点.func` 是 `ast.Attribute` 的调用 —— `from … import
  是Windows` 后的**裸 `是Windows()`** 实际不被判红（`变异样本表` 里那条裸 `是POSIX()`
  样本靠同表其他样本使第二拍变红）。补它会改变本检查器命中面，须另批处理。
"""

from __future__ import annotations

import ast
import sys
from pathlib import Path

if __name__ == "__main__" or __package__ in (None, ""):
    sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from 公共契约.基础类型.逻辑类型 import 假, 真  # noqa: E402
from 公共契约.运行时.平台适配 import 清只读后删除树  # noqa: E402
from 开发工具.验证门禁.门禁公共 import (  # noqa: E402
    正式层名表,
    新建夹具根,
    检查结论,
    命中,
)

检查器名 = "平台判断越界检测"

规则_裸读平台标志 = "裸读平台标志"
规则_取值后分叉 = "取值后自行分叉"
规则_平台专有用法 = "平台专有用法"

#: 收口层白名单（**精确路径**形态）—— **与铁律原文逐字对应**。
#: 改白名单 = 改铁律，必须同步 `AGENTS.md` 的收口条，不得只改这里。
收口层白名单: frozenset[str] = frozenset({
    "公共契约/运行时/进程终止.py",
})

#: 收口层白名单（**前缀**形态）：`公共契约/运行时/平台适配/` 是 2026-09-23 拆成的
#: **同名包**，包内每个子文件都是收口层本体的一部分，自带平台判定是它的职责。
#: 前缀只此一条（与铁律原文的两个收口层一一对应，不是放宽）。
收口层白名单前缀: tuple[str, ...] = (
    "公共契约/运行时/平台适配/",
)


def _是收口层(相对: str) -> bool:
    """该相对路径是否落在收口层（精确集合 ∪ 前缀集合）—— 白名单的**唯一判定点**。

    收成函数而不是在两处各写一次 `in` / `startswith`：白名单是**判据事实源**，
    两处各判一次就是同一动作两处实现（哲学第 1.2 条）。
    """
    return 相对 in 收口层白名单 or 相对.startswith(收口层白名单前缀)

#: 规则一：裸读平台标志的四条形态（`模块名.属性名`）。
#: 加 `platform.machine()` 的理由：它与 `platform.system()` 是**同一类**（本机平台取值上报），
#: 第一轮审计 E-1 把两者并列点名；#103 已把 `platform.system()` 全部收口，
#: `platform.machine()` 存量冻结进基线（存量只报不拦，新增即红）。
裸读形态表: tuple[tuple[str, str], ...] = (
    ("sys.platform", "裸读 sys.platform（铁律点名，应改调 平台适配.原始平台标志() 或分支原语）"),
    ("os.name", "裸读 os.name（铁律点名，应改调 平台适配.分支原语）"),
    ("platform.system", "裸读 platform.system()（应改调 平台适配.本机系统名()，#103 唯一口径）"),
    ("platform.machine", "裸读 platform.machine()（应改调 平台适配.当前架构()）"),
)

#: 规则二：收口层分支判定原语 —— 出现在白名单外即「取值后自行分叉」。
分支判定原语表: frozenset[str] = frozenset({
    "是Windows", "是macOS", "是Linux", "是POSIX",
})

# ── 规则三：平台专有用法（2026-09-25 补门禁盲区）─────────────────────────────
#
# 为什么是**新的一条规则**而不是 `裸读形态表` 的表扩充：两张表的判据语义不同 ——
# `裸读形态表` 管「**读平台标志**」（`sys.platform` / `os.name` / `platform.system()` /
# `platform.machine()` 都是「我是哪个平台」的**身份取值**，判据落点是「值」）；
# 本节管「**用平台专有机制/常量**」（`os.fork()` / `os.setsid()` / `os.sep` / `chmod`
# 权限位 / 写死 `/tmp` 都是「只有某平台才有的动作或字面量」，判据落点是「动作/字面量」，
# 与「读没读平台标志」无关 —— `os.fork()` 一个字也没读平台标志，却只在 POSIX 上存在）。
# **正确写法也不同**：规则一的改法一律是「改调 `平台适配` 的取值原语」，规则三的改法分属
# 进程/路径/权限/临时目录四族（见各表说明）。混进一张表会让「说明」字段口径不一
# （既有四条说明逐条点名 `平台适配` 的腿），也让「命中该报哪条规则」失去区分度 ——
# 故按既有命名风格另立 `规则_xxx`。
#
# 存量（本规则上线时的现场，逐条冻结进 `存量基线.json`，只报不拦；新增即红）：
#   ① `os.fork()` 2 处、`os.setsid()` 1 处；
#   ② `os.sep` / `os.path.sep` 4 处；
#   ③ `chmod` 权限位 8 处（`平台控制面/包仓库/平台客户端制品.py` 的 3 处已过收口层门
#      `支持chmod()`，按判据不算越界）；
#   ④ 写死 `/tmp` 5 处。

#: 规则三·专有调用表：`模块名.属性名` 形式的**平台专有调用**（Windows 上该属性根本不存在）。
#: 只认**调用位**（`ast.Call`）；裸引用（`目标 = os.fork`）与能力探测（`hasattr(os, "fork")`）
#: **不判** —— 后者正是收口层 `平台适配.要求POSIX能力()` 自己教的写法（「按能力探测而非按名字硬判」）。
专有调用表: tuple[tuple[str, str], ...] = (
    ("os.fork",
     "调用 os.fork()（Windows 上无此属性）：应改调 公共契约.运行时.平台适配.要求POSIX能力"
     "（显式要求 POSIX 专有能力，不满足即抛 平台不支持错误，不让 AttributeError 逸出）；"
     "若只是要「fork 或 spawn 起多进程」，走 平台适配.多进程启动上下文()。"
     "**裸 os.fork() 本体（不 exec 的分叉）无替代腿，须先补**"),
    ("os.setsid",
     "调用 os.setsid()（Windows 上无此属性）：起子进程要独立进程组应走 "
     "平台适配.子进程组启动标志()（POSIX start_new_session / Windows "
     "CREATE_NEW_PROCESS_GROUP）；「本平台有没有独立进程组」的门是 "
     "公共契约.运行时.进程终止.进程组号()。"
     "**「把当前进程自己 setsid」无替代腿，须先补**"),
)

#: 规则三·专有常量表：`模块名.属性名` 形式的**平台专有常量**（取值即平台差异）。
专有常量表: tuple[tuple[str, str], ...] = (
    ("os.sep",
     "读 os.sep（平台路径分隔符常量）：路径**边界判定**应改走本仓唯一腿 "
     "支持库/后端/系统核心支持库/路径安全.校验路径（基线「同职责路径判定」已登记 9 处"
     "同职责实现）；只做路径拼接用 pathlib（Path 运算自带平台语义）。"
     "**平台适配 无 os.sep 替代原语**"),
    ("os.path.sep",
     "读 os.path.sep（平台路径分隔符常量）：与 os.sep 同一处置 —— 路径边界判定走 "
     "支持库/后端/系统核心支持库/路径安全.校验路径，拼接走 pathlib。"
     "**平台适配 无替代原语**"),
)

#: 规则三·权限位调用名表：`chmod` 系调用的函数名
#: （`os.chmod(...)` / `目标.chmod(...)` / 裸 `chmod(...)` 三种形状都算）。
权限位调用名表: frozenset[str] = frozenset({"chmod", "fchmod", "lchmod"})

#: 权限位调用的**收口层门名**：同一函数体内出现它 ⇒ 该 chmod 已「先按收口层判据判、再调」
#: （样板 = `平台控制面/包仓库/平台客户端制品.py::_加固密钥权限` 的 `if not 支持chmod(): return`）
#: ⇒ 那是收口层认可的写法，**不算越界**；不豁免会把样板判成假红。
权限位收口门名 = "支持chmod"

#: 规则三·写死 POSIX 临时目录的**根段名**：**绝对路径**（以 `/` 起头）且第一段等于它即命中。
#: 只认**代码里的字符串字面量**（`ast.Constant`，且不是裸字符串语句）——
#: 文档串/说明串（全仓大量引用冻结基线路径 `/tmp/拆分基线/…`）一律不判，避免「判据太宽＝假红」。
#:
#: **为什么存「段名」而不是存 `/tmp` 字面量**（2026-09-25 现场判定）：本表是**判据数据源**，
#: 不是「调用点写死路径」。表里若存 `/tmp` 字面量，本检查器**自己会被自己的判据命中** ——
#: 而那处命中既不是越界、也不是「存量现场」，只剩「把检查器自己塞进基线」一条路，
#: 那才是真开洞（基线只该装存量）。故存段名、在 `_是写死临时目录()` 里按
#: 「绝对路径 + 第一段相等」比对：判据落在**「绝对路径的根段」这一事实**上，不是规避判据
#: （相邻同名串 `/tmpfoo` 照样不命中）。
写死临时目录根段表: tuple[str, ...] = ("tmp",)

#: 规则三·权限位命中共用的详情（与既有各表「一条一句说明」的风格一致）。
权限位说明 = (
    "调用点自带 POSIX 权限位语义的 chmod/fchmod（Windows 上 os.chmod 只认 "
    "stat.S_IWRITE / stat.S_IREAD 两个位）：应先过收口层门 平台适配.支持chmod() 再调"
    "（样板 = 平台控制面/包仓库/平台客户端制品.py::_加固密钥权限）；"
    "删除场景走 平台适配.清只读后删除树 / 确保可删 / 清除只读属性（全仓唯一实现）。"
    "**「原子写保留原权限位」无替代腿，须先补**"
)

#: 扫描面 = **复用同一条源码边界**（哲学 12.1 唯一事实源），不另列层名表。
#: 本文件最初自写了一张 12 项层名表，被 `同动作双路径检测` 当场判红
#: （「表 扫描层名表 与另一处交叠 7/8 条（同一动作两处实现）」）—— 那正是本条纪律要防的
#: 第二份实现，故改为 import 共享表。来源链：`门禁公共.正式层名表`
#: ← `运行核心/依赖防火墙.py::层名称表`。
扫描层名表: tuple[str, ...] = 正式层名表

#: 变体样本表（`--自证` 第二拍用）：**必须被判红**的形态，逐条对应上面三条规则。
变异样本表: tuple[tuple[str, str, str], ...] = (
    (规则_裸读平台标志, "公共契约/夹具契约/夹具文件.py",
     "import sys\n\n\ndef 夹具():\n    return sys.platform == 'win32'\n"),
    (规则_裸读平台标志, "运行核心/夹具核心.py",
     "import os\n\n\n夹具值 = os.name\n"),
    (规则_裸读平台标志, "开发工具/夹具工具.py",
     "import platform\n\n\n夹具值 = platform.system()\n"),
    (规则_裸读平台标志, "支持库/夹具支持库.py",
     "import platform\n\n\n夹具值 = platform.machine()\n"),
    (规则_取值后分叉, "运行核心/夹具分叉.py",
     "from 公共契约.运行时 import 平台适配\n\n\ndef 夹具():\n"
     "    if 平台适配.是Windows():\n        return 'win'\n    return 'posix'\n"),
    (规则_取值后分叉, "模块库/夹具模块/实现/夹具模块.py",
     "from 公共契约.运行时.平台适配 import 是POSIX\n\n\n夹具值 = 1 if 是POSIX() else 2\n"),
    (规则_取值后分叉, "开发工具/夹具工具.py",
     "from 公共契约.运行时 import 平台适配\n\n\n"
     "def 夹具(项表):\n"
     "    return [项 for 项 in 项表 if 平台适配.是POSIX() and 项]\n"),
    # 规则三（平台专有用法）：五类形态各一条，逐条必须被判红。
    (规则_平台专有用法, "平台控制面/夹具专有.py",
     "import os\n\n\ndef 夹具():\n    return os.fork()\n"),
    (规则_平台专有用法, "运行核心/夹具专有.py",
     "import os\n\n\ndef 夹具():\n    os.setsid()\n"),
    (规则_平台专有用法, "支持库/夹具专有.py",
     "import os\n\n\n夹具值 = os.sep\n"),
    (规则_平台专有用法, "模块库/夹具专有.py",
     "import os\n\n\nos.chmod('/夹具', 0o600)\n"),
    (规则_平台专有用法, "开发工具/夹具专有.py",
     "夹具路径 = '/tmp/夹具.log'\n"),
)

#: 拍一/拍三用的合法样本：**一条都不该报**。
#: ① 收口层本体（白名单内）自带判定 → 不报；
#: ② 调用点只调「取值型」原语、不做分支 → 不报；
#: ③ 能力探测写法（`hasattr`）→ 不报；
#: ④ 收口层本体自带 `chmod`（`平台适配/删除树.py`）→ 白名单内，不报；
#: ⑤ 调用点先过收口层门 `支持chmod()` 再 `chmod`（收口层认可的样板）→ 不报；
#: ⑥ 文档串里提到 `/tmp`（全仓冻结基线路径就长这样）→ 不报。
合法样本表: tuple[tuple[str, str], ...] = (
    ("公共契约/运行时/平台适配/判定.py",
     "import sys\n\n\ndef 是Windows() -> bool:\n    return sys.platform.startswith('win')\n"),
    ("公共契约/运行时/进程终止.py",
     "import os\n\n\ndef 收口判定():\n    return os.name == 'nt'\n"),
    ("运行核心/夹具取样.py",
     "from 公共契约.运行时 import 平台适配\n\n\n"
     "def 夹具台账() -> dict:\n    return {'系统': 平台适配.本机系统名(), '架构': 平台适配.当前架构()}\n"),
    ("支持库/夹具探测.py",
     "from 公共契约.运行时.平台适配 import 平台稳定缓存根\n\n\n"
     "夹具根 = 平台稳定缓存根()\n"),
    # 观测位（详情字典/台账）：平台判定原语出现在**非条件位**，不决定行为路径 → 不报。
    # 这一条是「规则二限定控制流条件位」这条判据精度的边界样本：去掉限定它会变红。
    ("开发工具/夹具观测.py",
     "from 公共契约.运行时 import 平台适配\n\n\n"
     "def 夹具详情() -> dict:\n"
     "    return {'是POSIX': 平台适配.是POSIX(), '平台': 平台适配.当前平台()}\n"),
    # 规则三的边界样本：下面四条**一条都不该报**（去掉对应豁免就会变红）。
    # ④ 收口层本体自带 chmod（`平台适配/删除树.py` 的三个原语是全仓唯一实现）→ 白名单内不报；
    ("公共契约/运行时/平台适配/删除树.py",
     "import os\nimport stat\n\n\ndef 清除只读属性(路径):\n    os.chmod(路径, stat.S_IWRITE)\n"),
    # ⑤ 调用点先过收口层门 `支持chmod()` 再 chmod（收口层认可的样板，#172 口径）→ 不报；
    ("平台控制面/夹具门.py",
     "import os\nfrom 公共契约.运行时.平台适配 import 支持chmod\n\n\n"
     "def 夹具(路径):\n"
     "    if not 支持chmod():\n        return\n    os.chmod(路径, 0o600)\n"),
    # ⑥ 能力探测（收口层自己教的写法：按能力探测而非按名字硬判）→ 不报；
    ("支持库/夹具能力探测.py",
     "import os\n\n\n夹具值 = hasattr(os, 'fork')\n"),
    # ⑦ 文档串里提到 `/tmp`（全仓大量冻结基线路径就长这样）→ 不报。
    ("模块库/夹具文档.py",
     '"""夹具说明：冻结基线在 /tmp/夹具基线/夹具.py。"""\n\n\n夹具值 = 1\n'),
)


def _属性链(节点: ast.AST) -> str:
    """把 `a.b.c` 形式拼成点号串；其余形态返回空串（与收口原语测试同一写法）。"""
    段: list[str] = []
    while isinstance(节点, ast.Attribute):
        段.append(节点.attr)
        节点 = 节点.value
    if isinstance(节点, ast.Name):
        段.append(节点.id)
        return ".".join(reversed(段))
    return ""


def _条件位节点集(树: ast.AST) -> set[int]:
    """收集**控制流条件位**上所有节点的 `id()`（规则二的落点判据）。

    覆盖：`If.test` / `While.test` / `IfExp.test` / `BoolOp.values` / `Assert.test`
    / 推导式的 `ifs` 与 `iter`（`any(是POSIX() for …)` 这类形态的分叉同样在条件位）。

    为什么用 `id()` 集合而不是「逐节点判父」：`ast` 无法从子节点回溯父节点，
    先把条件位的整棵子树 id 收进来，再在遍历时查表 —— 一次表达式遍历，无第二份实现。
    """
    出: set[int] = set()

    def _收(表达式: ast.AST | None) -> None:
        if 表达式 is None:
            return
        for 子 in ast.walk(表达式):
            出.add(id(子))

    for 节点 in ast.walk(树):
        if isinstance(节点, (ast.If, ast.While, ast.IfExp, ast.Assert)):
            _收(节点.test)
        elif isinstance(节点, ast.BoolOp):
            for 值 in 节点.values:
                _收(值)
        elif isinstance(节点, (ast.ListComp, ast.SetComp, ast.GeneratorExp)):
            for 生成 in 节点.generators:
                _收(生成.iter)
                for 条件 in 生成.ifs:
                    _收(条件)
        elif isinstance(节点, ast.DictComp):
            for 生成 in 节点.generators:
                _收(生成.iter)
                for 条件 in 生成.ifs:
                    _收(条件)
    return 出


def _查形态(链: str, 表: tuple[tuple[str, str], ...]) -> str | None:
    """在「(形态, 说明)」表里查属性链/调用链；命中返回说明，未命中返回 `None`。

    收成一处而不是每张表各写一遍循环：表是判据事实源，查询口径必须唯一（哲学 1.2）。
    """
    for 形态, 说明 in 表:
        if 链 == 形态:
            return 说明
    return None


def _函数含门区间(树: ast.AST) -> list[tuple[int, int, bool]]:
    """收集每个函数体的「行区间 + 是否出现收口层门名」—— 权限位判据的豁免落点。

    为什么需要它：`平台适配.支持chmod()` 是收口层发给调用点的**门**
    （样板见 `平台控制面/包仓库/平台客户端制品.py::_加固密钥权限`：`if not 支持chmod(): return`
    之后才 `chmod`）。那种写法是收口层认可的（判据来自收口层原语、调用点不写平台判断），
    **不是越界** —— 不豁免就会把它判成假红（本仓反复栽过「判据太宽＝假红」）。
    """
    出: list[tuple[int, int, bool]] = []
    for 节点 in ast.walk(树):
        if not isinstance(节点, (ast.FunctionDef, ast.AsyncFunctionDef)):
            continue
        含门 = any(
            getattr(子, "id", "") == 权限位收口门名
            or getattr(子, "attr", "") == 权限位收口门名
            for 子 in ast.walk(节点)
            if isinstance(子, (ast.Name, ast.Attribute))
        )
        出.append((节点.lineno, 节点.end_lineno or 节点.lineno, 含门))
    return 出


def _在含门函数内(行号: int, 区间表: list[tuple[int, int, bool]]) -> bool:
    """该行是否落在**最内层**含门函数体内（最内层 = 起始行最大的那个包含区间）。"""
    候选 = [项 for 项 in 区间表 if 项[0] <= 行号 <= 项[1]]
    if not 候选:
        return 假
    return max(候选, key=lambda 项: 项[0])[2]


def _裸字符串语句节点集(树: ast.AST) -> set[int]:
    """收集「裸字符串语句」（`Expr(Constant)`）里那个常量的 `id()`。

    `/tmp` 判据必须排除它们：全仓大量**文档串**引用冻结基线路径（`/tmp/拆分基线/…`）
    与说明文字，把文档串判红就是「判据太宽＝假红」（`公共契约/运行时/写入授权.py` 记过同一教训）。
    文档串在 AST 里恒是「裸表达式语句」的值，故按 `Expr(Constant)` 精确排除，
    不额外开白名单口子。
    """
    出: set[int] = set()
    for 节点 in ast.walk(树):
        if isinstance(节点, ast.Expr) and isinstance(节点.value, ast.Constant):
            出.add(id(节点.value))
    return 出


def _是写死临时目录(文本: str) -> bool:
    """该字符串字面量是否是「写死的 POSIX 临时目录」—— 判据本体，收成一处（唯一实现）。

    口径 = **绝对路径**（以 `/` 起头）**且第一段在 `写死临时目录根段表` 里**。
    按段比对而不是 `startswith` 字面量：既避免判据数据源自身被命中（见该表注释），
    也天然排除相邻同名串（`/tmpfoo` 这种不同目录不会被误判）。
    """
    候选 = 文本.strip()
    if not 候选.startswith("/"):
        return 假
    段 = 候选.split("/")
    return len(段) > 1 and 段[1] in 写死临时目录根段表


def 构建结论(根: Path) -> 检查结论:
    """按三条规则扫正式源码边界，命中白名单外的平台取值形态与平台专有用法。"""
    结论 = 检查结论(名称=检查器名)
    文件表: list[Path] = []
    for 层名 in 扫描层名表:
        层目录 = 根 / 层名
        if not 层目录.is_dir():
            continue
        for 路径 in sorted(层目录.rglob("*.py")):
            if "__pycache__" in 路径.parts:
                continue
            文件表.append(路径)
    结论.扫描面["正式源码 .py"] = len(文件表)
    结论.跳过面["收口层白名单"] = len(收口层白名单) + len(收口层白名单前缀)

    命中表: list[命中] = []
    for 文件 in 文件表:
        相对 = 文件.relative_to(根).as_posix()
        if _是收口层(相对):
            continue  # 收口层本体（含包内各子文件），自带平台判定是它的职责
        源码 = _可解析源码(文件, 结论, 根)
        if 源码 is None:
            continue
        树 = ast.parse(源码)
        条件位 = _条件位节点集(树)
        裸串 = _裸字符串语句节点集(树)
        # 门区间**惰性算**：只有真出现 chmod 系调用时才需要它（绝大多数文件没有），
        # 免得为每个函数各走一遍子树、把 120 秒墙钟预算花在无关文件上（门禁超时＝判红）。
        门区间: list[tuple[int, int, bool]] = []
        for 节点 in ast.walk(树):
            if isinstance(节点, ast.Attribute):
                链 = _属性链(节点)
                说明 = _查形态(链, 裸读形态表)
                if 说明 is not None:
                    命中表.append(命中(
                        规则=规则_裸读平台标志, 文件=相对,
                        行号=节点.lineno, 详情=说明,
                    ))
                    continue
                说明 = _查形态(链, 专有常量表)
                if 说明 is not None:
                    命中表.append(命中(
                        规则=规则_平台专有用法, 文件=相对,
                        行号=节点.lineno, 详情=说明,
                    ))
                continue
            if isinstance(节点, ast.Constant) and isinstance(节点.value, str):
                # 硬编码 `/tmp`：只认代码里的字符串字面量；裸字符串语句（文档串/说明串）
                # 一律排除 —— 全仓大量文档串引用冻结基线路径（`/tmp/拆分基线/…`）。
                if id(节点) not in 裸串 and _是写死临时目录(节点.value):
                    命中表.append(命中(
                        规则=规则_平台专有用法, 文件=相对, 行号=节点.lineno,
                        详情=f"写死 {节点.value.strip()!r}（POSIX 专有临时目录，Windows 无 /tmp）："
                             f"临时目录应走 tempfile.gettempdir() 或 TMPDIR/TEMP 环境变量链；"
                             f"收口层 平台适配.平台稳定缓存根() 是**缓存根**不是**临时目录**，"
                             f"语义不同不可互顶 —— **临时目录无收口原语，须先补**",
                    ))
                continue
            if isinstance(节点, ast.Call):
                函数 = 节点.func
                if isinstance(函数, ast.Name):
                    名 = 函数.id
                    链 = 函数.id
                elif isinstance(函数, ast.Attribute):
                    名 = 函数.attr
                    链 = _属性链(函数)
                else:
                    continue
                说明 = _查形态(链, 专有调用表)
                if 说明 is not None:
                    命中表.append(命中(
                        规则=规则_平台专有用法, 文件=相对,
                        行号=节点.lineno, 详情=说明,
                    ))
                    continue
                # 权限位：白名单外调 chmod 系，**且**所在最内层函数体里没有收口层门
                # `支持chmod()`（有门 = 收口层认可的「先判后调」样板，不算越界）。
                if 名 in 权限位调用名表:
                    if not 门区间:
                        门区间 = _函数含门区间(树)
                    if not _在含门函数内(节点.lineno, 门区间):
                        命中表.append(命中(
                            规则=规则_平台专有用法, 文件=相对,
                            行号=节点.lineno, 详情=权限位说明,
                        ))
                        continue
                if not isinstance(函数, ast.Attribute):
                    continue
                # 只认「收口层取值后分叉」：形如 `平台适配.是Windows()` 或
                # 直接 `是Windows()`（`from … import 是Windows`）。
                if 名 not in 分支判定原语表:
                    continue
                接收者链 = _属性链(节点.func.value)
                if 接收者链 not in ("平台适配", ""):
                    continue
                if id(节点) not in 条件位:
                    continue  # 观测位（详情字典/台账）：不决定行为路径，不是分叉
                命中表.append(命中(
                    规则=规则_取值后分叉, 文件=相对, 行号=节点.lineno,
                    详情=f"条件位调用 平台适配.{名}() —— 调用点自行分叉"
                         f"（平台值来自收口层，但分支动作在调用点 = 第二份实现）；"
                         f"应改调收口层的**取值型**原语（本机系统名()/当前架构()/"
                         f"子进程组启动标志()/进程内存RSS字节()/平台稳定缓存根()），"
                         f"由收口层自己判定",
                ))
    结论.命中列表 = 命中表
    return 结论


def _可解析源码(文件: Path, 结论: 检查结论, 根: Path) -> str | None:
    """读源码；读不成 / 语法错 → 记入「不可解析」（情况未知，不得当无违规，fail-closed）。"""
    try:
        return 文件.read_text(encoding="utf-8")
    except (OSError, UnicodeDecodeError) as 错误:
        结论.不可解析.append(f"{文件.relative_to(根).as_posix()}: {type(错误).__name__}")
        return None
    except Exception as 错误:  # noqa: BLE001 —— 兜底也必须计入未知，不得静默跳过
        结论.不可解析.append(f"{文件.relative_to(根).as_posix()}: {type(错误).__name__}")
        return None


def _自证() -> int:
    """三拍自证：① 合法夹具根 → 绿；② 注入违规样本 → 必红；③ 撤除 → 回绿。"""
    import subprocess

    根 = 新建夹具根("验证门禁四_自证_")
    基线 = 根 / "基线.json"
    for 相对, 内容 in 合法样本表:
        路径 = 根 / 相对
        路径.parent.mkdir(parents=True, exist_ok=True)
        路径.write_text(内容, encoding="utf-8")
    (根 / "基线.json").write_text(
        '{"检查器": {"平台判断越界检测": {}}}', encoding="utf-8"
    )
    # 合法样本落在扫描层名表里，但夹具根里没有 `测试中心` —— 扫描面不为 0 即可
    结果: list[tuple[str, str]] = []

    def _跑() -> int:
        return subprocess.run(
            [sys.executable, str(Path(__file__)), "--根", str(根), "--基线", str(基线)],
            capture_output=True, text=True, timeout=240,
        ).returncode

    第一拍 = _跑()
    结果.append((f"第一拍 合法夹具根（收口层本体 + 取值型原语调用 + 能力探测）", f"退出码 {第一拍}（期望 0=绿）"))
    for _规则, 相对, 内容 in 变异样本表:
        路径 = 根 / 相对
        路径.parent.mkdir(parents=True, exist_ok=True)
        路径.write_text(内容, encoding="utf-8")
    第二拍 = _跑()
    结果.append((f"第二拍 注入 {len(变异样本表)} 份违规样本（三条规则各覆盖）", f"退出码 {第二拍}（期望 1=红）"))
    for _规则, 相对, _内容 in 变异样本表:
        路径 = 根 / 相对
        if 路径.is_file():
            路径.unlink()
    第三拍 = _跑()
    结果.append(("第三拍 撤除违规样本", f"退出码 {第三拍}（期望 0=绿）"))

    print(f"══ {检查器名} 自证（三拍）══")
    for 名, 值 in 结果:
        print(f"  {名}：{值}")
    通过 = 第一拍 == 0 and 第二拍 == 1 and 第三拍 == 0
    print(f"  自证结论：{'通过' if 通过 else '不通过'}")
    if not 通过:
        print(f"  ⚠ 夹具根保留供人工复核：{根}")
        return 1
    # 夹具根可能含只读条目 → 走唯一实现，不留 shutil.rmtree 裸调用
    清只读后删除树(根, 忽略失败=真)
    return 0


def main(argv: list[str] | None = None) -> int:
    实参 = list(sys.argv[1:] if argv is None else argv)
    if "--自证" in 实参:
        return _自证()
    from 开发工具.验证门禁.门禁公共 import 跑标准主流程

    return 跑标准主流程(检查器名, 构建结论, 实参)


if __name__ == "__main__":
    sys.exit(main())
