"""薄壳工具清单：3 个薄壳工具的协议名、中文名、描述、入参与转发目标（唯一元数据来源）。

协议名保持 ASCII（SEP-986：Tool.name 仅允许 [A-Za-z0-9_.-]+），中文名写进 description。
薄壳零业务语义：这里只声明「转发哪个操作、哪个能力」，不写任何业务规则、不读业务数据。
本模块同时是**薄壳工具面指纹的唯一口径**：进程内指纹 / 盘上指纹 / 是否一致 三组键由
`构建工具目录()` 一并返回，调用方据此判断「手里这个壳是不是盘上当前那一版」。
"""

from __future__ import annotations

import hashlib
from typing import Any

import sys as _sys
from pathlib import Path as _Path

_项目根 = _Path(__file__).resolve().parents[2]
if str(_项目根) not in _sys.path:
    _sys.path.insert(0, str(_项目根))

from 公共契约.基础类型.逻辑类型 import 真, 假
from 支持库.适配层.MCP协议提供者 import 构造工具定义


# 唯一转发目标：网关操作名（与 开发文档/网关调用最小示例.md 一致）。
调用能力操作 = "调用能力"
# 查询能力薄壳要求转发的登记能力 id；该能力未注册时薄壳只报错并登记，不自实现搜索。
# 能力 id 是「不带头部包根前缀」的形式（真实注册表口径，已实测：注册表 能力id列表 里是
# `能力目录.搜索能力`；写 `模块库.能力目录.搜索能力` 会被网关判 404 能力未注册）。
搜索能力目标 = "能力目录.搜索能力"

三个工具定义: tuple = (
    构造工具定义(
        "capability_search",
        (
            "查询能力：经唯一网关查平台已注册能力。"
            "**前提**：本工具只服务系统工程平台仓库（传参 项目根 必须是平台绝对路径）；"
            "不是本仓库的会话不要调用，也不要据此载入任何平台上下文。"
            "零命中时自动改用双字滑窗兜底检索（返回值 `是否扩词=true` 标注），"
            "故自然语言描述「我要干什么」即可，不必先猜能力 id。"
            "**★ 3 次探索规则（2026-09-21 华哥定，硬规则）**：任意能力或入口，"
            "**探索 3 次还找不到，就停下来设计「3 步内可达」的路径**，不要继续摸。"
            "找得到、但每次都要猜参数名或记路径，同样按此规则处理 —— 正确做法是把它收成"
            "一条能力或一条直达命令。实测踩过：找一个「契约编译器」摸了 5 次"
            "（搜「编译器」零命中 → 搜「编译」不对 → 列目录 → 列子目录 → 跑崩），"
            "根因就是入口不可直达（脚本缺项目根自举、也没登记成能力），已按此规则修。"
            "**★ 慢调用自报（2026-09-21 华哥定）**：任意能力单次超过 15 秒，网关信封会带"
            "`慢调用提示`（含排查四步），**看到就必须去查卡点，发现一个修一个**，不许傻等；"
            "写文件允许慢，同样会提示但不视为异常。"
            "**★ 一次搜不到不要停**：换同义词/上位词再搜（例：搜「编译口」零命中 → 搜「执行命令」"
            "「进程」「受控执行」），或调 `能力目录.读取能力` 拿该能力的**完整契约与调用示例**；"
            "**靠猜参数名/参数类型会必错**（实测：`执行命令` 的 `命令` 是文本型 shell 串而非数组、"
            "`超时秒` 是双精度数型，猜错即 `参数不合法`）。"
            "**先探索、再执行**：拿到能力 id 只是第一步，执行前必须确认参数名、类型、必填。"
            "**★ 返回体积可控（2026-09-21 华哥裁决「返回的内容需要可控，不然调用几次打爆上下文」）**："
            "`细节级别` 三档 —— 默认 `名称`（只回指针，8 字段）／`名称+说明`（加一段话看懂）／"
            "`完整契约`（含参数与返回，**体积最大，只在确定要用时取**）；"
            "`限制` 默认 10，够用就别加；结果多时用 `游标` 翻页"
            "（把上次返回的 `下一条游标` 原样传回），**不要靠加大 `限制` 一次拿全**。"
            "**★ 不知道某个能力实际怎么传参时，调 `能力目录.常用参数组合`（2026-09-21 华哥要求）**："
            "它按能力 id 返回**历史上最常用的 5 组入参**（含真实样例），契约只声明类型、"
            "这个给你一组能跑通的取值 —— 实测踩过「`超时秒` 传 120 被拒、传 120.0 才过」连错 3 次、"
            "「`项目根` 要在 `参数` 里再给一遍」连错 4 次，都是**不知道别人怎么传**而不是类型错。"
            "`capability_search` 传 `含常用参数=true` 也能让每条命中直接带上这 5 组（默认关，返回体量优先）。"
        ),
        {
            "type": "object",
            "properties": {
                "关键词": {"type": "string", "description": "搜索关键词，例如 读取文件；也可直接用自然语言描述（如「把视频转成音频」），零命中时自动兜底扩词"},
                "限制": {"type": "integer", "description": "每页条数（1~100），默认 10。★ 别靠加大它拿全量，结果多请用 游标 翻页"},
                "细节级别": {"type": "string", "enum": ["名称", "名称+说明", "完整契约"], "description": "返回详略三档，默认「名称」（只回指针，最省上下文）。要一段话看懂用「名称+说明」；只有确定要用某个能力时才用「完整契约」"},
                "含常用参数": {"type": "boolean", "description": "为 true 时每条命中多带 `常用参数`：该能力历史上最常用的 5 组入参（含真实样例）。默认 false —— 会放大返回体量，只在要照抄调用格式时开；单查某个能力用 `能力目录.常用参数组合` 更省"},
                "游标": {"type": "string", "description": "翻页游标：传上一次返回的「下一条游标」取下一页；空=第一页。游标绑定关键词，换关键词必须重搜"},
                "项目根": {"type": "string", "description": "【本工具自己的参数 · 平台身份校验用】系统工程平台仓库绝对路径；非本仓库不要调用。注意：它与能力自身的同名参数是两回事，见 能力自身参数 的说明"},
            },
            "required": ["关键词", "项目根"],
        },
    ),
    构造工具定义(
        "capability_call",
        (
            "调用能力：入参「能力id」+「参数」+「项目根」，经唯一网关转发执行。"
            "**前提**：本工具只服务系统工程平台仓库（传参 项目根 必须是平台绝对路径）；"
            "不是本仓库的会话不要调用。"
            "**参数分两层，别混**："
            "① 顶层 `项目根`＝**本工具自己的**平台身份校验参数（只用于判定「是不是本仓库」）；"
            "② 能力自己的入参一律放进顶层 `参数` 对象里。"
            "`项目根` **已自动补位（2026-09-21）**：能力契约里也声明了 `项目根` 的（如 "
            "`开工编排.开工准备` / `开工即占`）**不必在 `参数` 里手抄第二遍** —— 没给就自动填"
            "顶层那个（此前必须手抄，实测连传 4 次错才开通工）；显式给了就以你给的为准。"
            "**能力id 必须写真实能力 id**（如 `开工编排.开工准备`）；把工具名 `capability_call` "
            "当能力 id 传会回「能力未注册」。"
            "**★ 先探索再执行，禁止靠猜（2026-09-21 华哥裁决，实测踩过）**："
            "调用任何能力前，先用 `capability_search` 搜到它，再确认**参数名 / 参数类型 / 必填**；"
            "一次搜索没拿到用法就**继续往下搜**（换关键词、调 `能力目录.读取能力` 读契约与调用示例），"
            "**不要凭字面猜参数名与类型** —— 实测 `系统核心支持库.进程管理.执行命令` 的 `命令` 是"
            "**文本型**（不是数组）。猜错一次就白付一轮往返。"
            "**★ `执行命令` 不经 shell（实测踩过，别照契约字面理解）**：它按「可执行文件 + 参数」直启，"
            "故 `|` 管道、`>` 重定向、`&&`、以及 `unset`/`export` 这类 shell 内建**一律不支持**"
            "（实测报 `[Errno 2] No such file or directory: 'unset'`）；参数 `命令` 与 `工作目录` 都是文本型。"
            "**★ 一次跑多条命令用 `系统核心支持库.进程管理.执行命令集`（2026-09-21 华哥要求）**："
            "参数 `命令表`=列表型（每条一个命令文本）、`模式`=`串联`（默认，按序）或 `并行`（同时跑）、"
            "`失败即停`（默认真，仅串联生效）—— 一组检查**一次调用跑完**，逐条回带退出码与输出，"
            "不必为每条命令各走一趟（`并行` 实测 4×`sleep 1` 总耗时 1.3 秒）；"
            "要 shell 语义（管道/重定向）仍写 `sh -c \"…\"` 作为**其中一条**命令即可。"
            "**★ 改代码 / 读写文件也用本工具（2026-09-21 华哥裁决「纯用 MCP 开发，包括写代码」）**："
            "平台**已有**这几条原子能力（实测毫秒级），改代码留在平台内即可，不必绕到平台外："
            "读 `文件管理.读取文件`（`文件路径`）；写/新建 `文件管理.写入文件`（`文件路径`+`内容`，自动建父目录）；"
            "**精确替换** `文件系统支持库.文本补丁.应用精确替换`（`文件路径`+`旧文本`+`新文本`，"
            "可选 `根目录` 做路径边界校验、`写入=false` 只预览差异不落盘、`预期文件摘要` 做乐观锁）—— "
            "**`旧文本` 在文件里不唯一时它直接拒绝并回带候选行号（`匹配不唯一`），不会误改**，"
            "这比按行号盲改安全；查旧文本位置用 `文件系统支持库.文本补丁.查找唯一匹配`。"
            "**★ 一次改多个文件（或多处）用 `文件系统支持库.文本补丁.批量应用精确替换`（2026-09-21）**："
            "参数 `编辑列表`=列表型，每条 `{文件路径, 旧文本, 新文本, 起始行?, 结束行?, 预期旧文本摘要?, 预期文件摘要?}`"
            "（字段口径与 `应用精确替换` 逐字一致，最多 64 条）；`根目录` 非空即对每条做边界校验；"
            "`写入`（默认真）、`原子`（默认真）。两个要点：① **`原子=真` 是全或无** —— 先把每条都在内存里试算，"
            "任一条失败就**一条都不写**并回带 `失败清单`（序号+错误码），工作区不会停在「改了一半」；"
            "② **同一文件多条编辑合并为一次读一次写**。返回 `{总数, 成功数, 失败数, 全部成功, 文件数, 已写入文件数, 结果表, 失败清单}` —— "
            "**信封 `成功` 只表示「批次跑完了」，整体是否全绿看 `全部成功`**。"
            "**★ 改完能力让网关重新装配：用本工具的 `操作=热接入`，不要重启网关（2026-09-21）**："
            "改完能力（新增包 / 改实现 / 改契约）必须让网关重新装配才生效，官方口径是"
            "**热接入而不是重启**（决策记录 0008：重启会中断在途调用、打断句柄与租约、牵连依赖 40007 的其他进程）；"
            "`热接入` 与 `健康检查` 都是 `/网关/调用` 上的另一个 `操作`，本工具现在**能直接转发** —— "
            "`capability_call(操作=\"热接入\")` 一次调用即完成增量装配（**不需要 能力id、不接受 参数**），"
            "返回 `{新增包, 变更包, 卸载包, 成功包, 失败包, 已注册能力数}`；"
            "`capability_call(操作=\"健康检查\")` 探活（返回 200=网关活着，无需 curl）。"
            "**只有热接入确实覆盖不到的变更**（网关自身 `运行核心/统一网关/` 的进程内代码）才谈重启，"
            "而那属于「先维护热加载能力」而不是随手 `launchctl kickstart` —— "
            "实测踩过：改完能力跑去重启网关，既慢又要轮询健康，且与 0008 相悖。"
            "**★ 禁止 py 腿（华哥 2026-09-21 裁决「py 调用的腿全砍了」）**："
            "本仓库一切动作（含构建制品、编译口、摘要重算、环境自检）**一律经本工具走 HTTP**，"
            "不得直接 `python3.14 脚本.py` 起第二条腿；"
            "本仓通用执行腿是 `系统核心支持库.进程管理.执行命令`。"
            "在本仓库做任务时，**开工第一步必须调能力 id「开工编排.开工准备」**"
            "（入参 `参数`={项目根: 仓库绝对路径, 任务, 修改路径}），"
            "它返回开工 ID、本次任务的强规范与通用纪律（含哲学纪律）；"
            "按返回值里的 开工ID 提交。规则正文以该返回为唯一送达点，本文不复述。"
            "**★ 返回体积可控（2026-09-21 华哥裁决）**：默认 `返回上限字符`=6000，"
            "超了就**结构化裁剪**（字典丢尾键 / 列表截尾 / 长文本截断）并回带 `值裁剪` 说明"
            "（被丢了哪些键、原有多大、怎么拿全）——**不会静默截断**，也不会把整个大返回灌进上下文。"
            "要精确取字段用 `值字段`（只保留 `值` 的这些顶层键）；确实要全量时把 `返回上限字符` 设为 0。"
            "★ `返回上限字符` 与 `值字段` 是**本工具的入参**（与 `参数` / `能力id` / `项目根` 平级），"
            "**不要写进 `参数` 里** —— 写进去会被当成能力参数忽略，裁剪照旧发生，"
            "现象看着像「值字段不生效」（实测连续踩过 3 次才定位）。"
        ),
        {
            "type": "object",
            "properties": {
                "操作": {"type": "string", "enum": ["调用能力", "热接入", "健康检查"],
                       "description": "转发哪个网关操作，默认「调用能力」。热接入=新增/变更包增量装配免重启（改完能力调它即生效，不需要 能力id、不接受 参数）；健康检查=探活。白名单 fail-closed，表外操作不转发"},
                "能力id": {"type": "string", "description": "真实能力 id，例如 开工编排.开工准备（不是工具名 capability_call）。操作=热接入/健康检查 时不需要"},
                "参数": {"type": "object", "description": "【能力自身的入参】原样转发给网关；能力契约里声明的必填参数（含它自己的 项目根）都在这里给。★ 先经 capability_search / 能力目录.读取能力 确认参数名与类型，不要猜。操作=热接入/健康检查 时必须留空"},
                "返回上限字符": {"type": "integer", "description": "返回 `值` 的字符上限，默认 6000；超了结构化裁剪并回带 `值裁剪` 说明。0 = 不限（慎用，大返回会灌满上下文）"},
                "值字段": {"type": "array", "items": {"type": "string"}, "description": "只保留 `值` 的这些顶层键（精确取字段，最省上下文）。空/不传 = 保留全部"},
                "项目根": {"type": "string", "description": "【本工具自己的参数 · 平台身份校验用】系统工程平台仓库绝对路径；非本仓库不要调用"},
            },
            "required": ["项目根"],
        },
    ),
    构造工具定义(
        "tool_catalog",
        (
            "工具目录：返回薄壳当前暴露的工具清单（协议名/中文名/入参/转发目标）。"
            "只读元数据，不扫描仓库、不转发网关。"
            "**★ 同时回答「我手里这个壳是不是盘上当前那一版」（2026-09-21）**：返回 "
            "`进程内指纹`（本进程启动时已加载的 工具清单.py / 薄壳服务.py 字节 sha256）、"
            "`盘上指纹`（调用时**现读盘**算出的同一对 sha256）、`是否一致`（逻辑型）三组键；"
            "`是否一致=假` 时 `提示` 给出怎么处理、`不一致文件` 点名是哪一份变了。"
            "薄壳是会话启动时拉起的常驻进程，改了工具面它不会自动重载 —— "
            "`是否一致=假` 即说明你手上的工具描述/入参是旧的，**不要照它调**。"
            "薄壳不自重启、也不尝试重启，只如实报「不一致 + 怎么处理」。"
        ),
        {"type": "object", "properties": {}},
    ),
)

中文名到协议名 = {
    "查询能力": "capability_search",
    "调用能力": "capability_call",
    "工具目录": "tool_catalog",
}
协议名到中文名 = {协议名: 中文名 for 中文名, 协议名 in 中文名到协议名.items()}


# ── 薄壳工具面指纹（唯一口径）────────────────────────────────────────────────
# 为什么要有：薄壳是 MCP 客户端在**会话启动时拉起的常驻进程**。改了本文件（工具描述 /
# 入参 / 工具面）之后，运行中的旧壳照旧回旧元数据，调用方**无从知道自己手里的是不是
# 旧壳**。唯一能判的只有「进程里跑的那份代码」与「盘上现在那份代码」的字节比对：
#   进程内指纹 = 本进程导入本模块时（＝薄壳进程启动时）读到的字节 sha256，进程内不再变；
#   盘上指纹   = 调用时**现读盘**算出的同一对 sha256，随时反映最新代码。
薄壳源文件 = ("工具清单.py", "薄壳服务.py")
指纹口径 = "sha256(文件字节) × (工具清单.py, 薄壳服务.py)"
不一致提示 = ("薄壳为会话启动时的旧进程，盘上代码已更新；请重启 MCP 客户端后再调"
              "（薄壳不自重启，也不尝试重启）")


def 源文件路径(文件名: str) -> _Path:
    """薄壳源文件的绝对路径：以本模块所在目录为锚，不猜路径、不扫仓库。"""
    return _Path(__file__).resolve().parent / 文件名


def 算文件指纹(路径: _Path) -> str:
    """文件字节的 sha256 十六进制串；读不到返回空串（空串＝没取到，绝不当成「一致」）。"""
    try:
        return hashlib.sha256(路径.read_bytes()).hexdigest()
    except OSError:
        return ""


def 采集源文件指纹() -> dict[str, str]:
    """采集这一对薄壳源文件的字节指纹 —— **全仓唯一口径**，进程内快照与盘上现读都用它。"""
    return {文件名: 算文件指纹(源文件路径(文件名)) for 文件名 in 薄壳源文件}


#: 进程内指纹兜底值：本模块被导入时（薄壳进程启动时）的快照，进程内恒定不变。
#: 正常路径由入口 薄壳服务.py 在进程启动时显式采集并传入（锚在「本进程启动」那一刻）；
#: 本兜底值保证不经入口的调用方（如直接 import 本模块自测）也拿得到有意义的值。
_导入时指纹: dict[str, str] = 采集源文件指纹()


def 构建指纹段(进程内指纹: dict[str, str] | None = None) -> dict[str, Any]:
    """三组键（进程内指纹 / 盘上指纹 / 是否一致）+ 提示：一眼判断手里这个壳是不是盘上那一版。"""
    进程内 = dict(进程内指纹) if 进程内指纹 else dict(_导入时指纹)
    盘上 = 采集源文件指纹()
    未取到 = sorted(名 for 名, 值 in 盘上.items() if not 值)
    不一致文件 = sorted(名 for 名 in 盘上 if 进程内.get(名) != 盘上.get(名))
    是否一致 = bool(盘上) and not 未取到 and not 不一致文件
    指纹段: dict[str, Any] = {
        "进程内指纹": 进程内,
        "盘上指纹": 盘上,
        "是否一致": 真 if 是否一致 else 假,
        "指纹口径": 指纹口径,
        "判据": ("进程内指纹（进程启动时快照）逐文件等于盘上指纹（调用时现读盘）"
                 "→ 一致；任一不等或读不到 → 不一致"),
        "提示": "" if 是否一致 else 不一致提示,
    }
    if 未取到:
        指纹段["读取失败文件"] = 未取到
    if 不一致文件:
        指纹段["不一致文件"] = 不一致文件
    return 指纹段


def 构建工具目录(进程内指纹: dict[str, str] | None = None,
             进程启动时刻: str = "",
             进程号: int | None = None) -> dict[str, Any]:
    """薄壳自身工具清单（只读元数据，不触碰仓库其它文件）。

    只增不改：老键（成功 / 错误码 / 错误说明 / 薄壳工具数 / 工具 / 传输 / 唯一转发通道）
    语义不变；新增 进程内指纹 / 盘上指纹 / 是否一致 / 提示（+ 指纹口径 / 判据 /
    进程启动时刻 / 进程号），让调用方能判断手里这个壳是不是盘上当前那一版。
    """
    清单 = [
        {
            "协议名": 工具.name,
            "中文名": 协议名到中文名.get(工具.name, 工具.name),
            "入参": 工具.inputSchema,
            "描述": 工具.description,
            "转发目标": {
                "capability_search": f"{调用能力操作} → {搜索能力目标}",
                "capability_call": f"{调用能力操作} → 入参指定能力id",
                "tool_catalog": "不转发（薄壳本地元数据）",
            }.get(工具.name, ""),
        }
        for 工具 in 三个工具定义
    ]
    目录: dict[str, Any] = {
        "成功": True,
        "错误码": "",
        "错误说明": "",
        "薄壳工具数": len(清单),
        "工具": 清单,
        "传输": "stdio（不监听任何端口）",
        "唯一转发通道": "HTTP POST http://127.0.0.1:40007/网关/调用",
    }
    目录.update(构建指纹段(进程内指纹))
    if 进程启动时刻:
        目录["进程启动时刻"] = 进程启动时刻
    if 进程号 is not None:
        目录["进程号"] = 进程号
    return 目录
