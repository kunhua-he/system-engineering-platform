"""薄壳工具清单：3 个薄壳工具的协议名、中文名、描述、入参与转发目标（唯一元数据来源）。

协议名保持 ASCII（SEP-986：Tool.name 仅允许 [A-Za-z0-9_.-]+），中文名写进 description。
薄壳零业务语义：这里只声明「转发哪个操作、哪个能力」，不写任何业务规则、不读业务数据。
"""

from __future__ import annotations

from typing import Any

import sys as _sys
from pathlib import Path as _Path

_项目根 = _Path(__file__).resolve().parents[2]
if str(_项目根) not in _sys.path:
    _sys.path.insert(0, str(_项目根))

from 支持库.适配层.MCP协议提供者 import 构造工具定义


# 唯一转发目标：网关操作名（与 开发文档/网关调用最小示例.md 一致）。
调用能力操作 = "调用能力"
# 查询能力薄壳要求转发的登记能力 id；该能力未注册时薄壳只报错并登记，不自实现搜索。
# 能力 id 是「不带头部包根前缀」的形式（真实注册表口径，已实测：注册表 能力id列表 里是
# `能力目录.搜索能力`；写 `模块库.能力目录.搜索能力` 会被网关判 404 能力未注册）。
搜索能力目标 = "能力目录.搜索能力"

三个工具定义: tuple = (
    构造工具定义(
        "capability_search",
        (
            "查询能力：经唯一网关查平台已注册能力。"
            "**前提**：本工具只服务系统工程平台仓库（传参 项目根 必须是平台绝对路径）；"
            "不是本仓库的会话不要调用，也不要据此载入任何平台上下文。"
            "零命中时自动改用双字滑窗兜底检索（返回值 `是否扩词=true` 标注），"
            "故自然语言描述「我要干什么」即可，不必先猜能力 id。"
            "**一次搜不到不要停**：换同义词/上位词再搜（例：搜「编译口」零命中 → 搜「执行命令」"
            "「进程」「受控执行」），或调 `能力目录.读取能力` 拿该能力的**完整契约与调用示例**；"
            "**靠猜参数名/参数类型会必错**（实测：`执行命令` 的 `命令` 是文本型 shell 串而非数组、"
            "`超时秒` 是双精度数型，猜错即 `参数不合法`）。"
            "**先探索、再执行**：拿到能力 id 只是第一步，执行前必须确认参数名、类型、必填。"
            "**★ 返回体积可控（2026-09-21 华哥裁决「返回的内容需要可控，不然调用几次打爆上下文」）**："
            "`细节级别` 三档 —— 默认 `名称`（只回指针，8 字段）／`名称+说明`（加一段话看懂）／"
            "`完整契约`（含参数与返回，**体积最大，只在确定要用时取**）；"
            "`限制` 默认 10，够用就别加；结果多时用 `游标` 翻页"
            "（把上次返回的 `下一条游标` 原样传回），**不要靠加大 `限制` 一次拿全**。"
        ),
        {
            "type": "object",
            "properties": {
                "关键词": {"type": "string", "description": "搜索关键词，例如 读取文件；也可直接用自然语言描述（如「把视频转成音频」），零命中时自动兜底扩词"},
                "限制": {"type": "integer", "description": "每页条数（1~100），默认 10。★ 别靠加大它拿全量，结果多请用 游标 翻页"},
                "细节级别": {"type": "string", "enum": ["名称", "名称+说明", "完整契约"], "description": "返回详略三档，默认「名称」（只回指针，最省上下文）。要一段话看懂用「名称+说明」；只有确定要用某个能力时才用「完整契约」"},
                "游标": {"type": "string", "description": "翻页游标：传上一次返回的「下一条游标」取下一页；空=第一页。游标绑定关键词，换关键词必须重搜"},
                "项目根": {"type": "string", "description": "【本工具自己的参数 · 平台身份校验用】系统工程平台仓库绝对路径；非本仓库不要调用。注意：它与能力自身的同名参数是两回事，见 能力自身参数 的说明"},
            },
            "required": ["关键词", "项目根"],
        },
    ),
    构造工具定义(
        "capability_call",
        (
            "调用能力：入参「能力id」+「参数」+「项目根」，经唯一网关转发执行。"
            "**前提**：本工具只服务系统工程平台仓库（传参 项目根 必须是平台绝对路径）；"
            "不是本仓库的会话不要调用。"
            "**参数分两层，别混**："
            "① 顶层 `项目根`＝**本工具自己的**平台身份校验参数（只用于判定「是不是本仓库」）；"
            "② 能力自己的入参一律放进顶层 `参数` 对象里。"
            "`项目根` **已自动补位（2026-09-21）**：能力契约里也声明了 `项目根` 的（如 "
            "`开工编排.开工准备` / `开工即占`）**不必在 `参数` 里手抄第二遍** —— 没给就自动填"
            "顶层那个（此前必须手抄，实测连传 4 次错才开通工）；显式给了就以你给的为准。"
            "**能力id 必须写真实能力 id**（如 `开工编排.开工准备`）；把工具名 `capability_call` "
            "当能力 id 传会回「能力未注册」。"
            "**★ 先探索再执行，禁止靠猜（2026-09-21 华哥裁决，实测踩过）**："
            "调用任何能力前，先用 `capability_search` 搜到它，再确认**参数名 / 参数类型 / 必填**；"
            "一次搜索没拿到用法就**继续往下搜**（换关键词、调 `能力目录.读取能力` 读契约与调用示例），"
            "**不要凭字面猜参数名与类型** —— 实测 `系统核心支持库.进程管理.执行命令` 的 `命令` 是"
            "**文本型**（不是数组）。猜错一次就白付一轮往返。"
            "**★ `执行命令` 不经 shell（实测踩过，别照契约字面理解）**：它按「可执行文件 + 参数」直启，"
            "故 `|` 管道、`>` 重定向、`&&`、以及 `unset`/`export` 这类 shell 内建**一律不支持**"
            "（实测报 `[Errno 2] No such file or directory: 'unset'`）；参数 `命令` 与 `工作目录` 都是文本型。"
            "**★ 一次跑多条命令用 `系统核心支持库.进程管理.执行命令集`（2026-09-21 华哥要求）**："
            "参数 `命令表`=列表型（每条一个命令文本）、`模式`=`串联`（默认，按序）或 `并行`（同时跑）、"
            "`失败即停`（默认真，仅串联生效）—— 一组检查**一次调用跑完**，逐条回带退出码与输出，"
            "不必为每条命令各走一趟（`并行` 实测 4×`sleep 1` 总耗时 1.3 秒）；"
            "要 shell 语义（管道/重定向）仍写 `sh -c \"…\"` 作为**其中一条**命令即可。"
            "**★ 改代码 / 读写文件也用本工具（2026-09-21 华哥裁决「纯用 MCP 开发，包括写代码」）**："
            "平台**已有**这四条原子能力（实测毫秒级），改代码留在平台内即可，不必绕到平台外："
            "读 `文件管理.读取文件`（`文件路径`）；写/新建 `文件管理.写入文件`（`文件路径`+`内容`，自动建父目录）；"
            "**精确替换** `文件系统支持库.文本补丁.应用精确替换`（`文件路径`+`旧文本`+`新文本`，"
            "可选 `根目录` 做路径边界校验、`写入=false` 只预览差异不落盘、`预期文件摘要` 做乐观锁）—— "
            "**`旧文本` 在文件里不唯一时它直接拒绝并回带候选行号（`匹配不唯一`），不会误改**，"
            "这比按行号盲改安全；查旧文本位置用 `文件系统支持库.文本补丁.查找唯一匹配`。"
            "**★ 禁止 py 腿（华哥 2026-09-21 裁决「py 调用的腿全砍了」）**："
            "本仓库一切动作（含构建制品、编译口、摘要重算、环境自检）**一律经本工具走 HTTP**，"
            "不得直接 `python3.14 脚本.py` 起第二条腿；"
            "本仓通用执行腿是 `系统核心支持库.进程管理.执行命令`。"
            "在本仓库做任务时，**开工第一步必须调能力 id「开工编排.开工准备」**"
            "（入参 `参数`={项目根: 仓库绝对路径, 任务, 修改路径}），"
            "它返回开工 ID、本次任务的强规范与通用纪律（含哲学纪律）；"
            "按返回值里的 开工ID 提交。规则正文以该返回为唯一送达点，本文不复述。"
            "**★ 返回体积可控（2026-09-21 华哥裁决）**：默认 `返回上限字符`=6000，"
            "超了就**结构化裁剪**（字典丢尾键 / 列表截尾 / 长文本截断）并回带 `值裁剪` 说明"
            "（被丢了哪些键、原有多大、怎么拿全）——**不会静默截断**，也不会把整个大返回灌进上下文。"
            "要精确取字段用 `值字段`（只保留 `值` 的这些顶层键）；确实要全量时把 `返回上限字符` 设为 0。"
        ),
        {
            "type": "object",
            "properties": {
                "能力id": {"type": "string", "description": "真实能力 id，例如 开工编排.开工准备（不是工具名 capability_call）"},
                "参数": {"type": "object", "description": "【能力自身的入参】原样转发给网关；能力契约里声明的必填参数（含它自己的 项目根）都在这里给。★ 先经 capability_search / 能力目录.读取能力 确认参数名与类型，不要猜"},
                "返回上限字符": {"type": "integer", "description": "返回 `值` 的字符上限，默认 6000；超了结构化裁剪并回带 `值裁剪` 说明。0 = 不限（慎用，大返回会灌满上下文）"},
                "值字段": {"type": "array", "items": {"type": "string"}, "description": "只保留 `值` 的这些顶层键（精确取字段，最省上下文）。空/不传 = 保留全部"},
                "项目根": {"type": "string", "description": "【本工具自己的参数 · 平台身份校验用】系统工程平台仓库绝对路径；非本仓库不要调用"},
            },
            "required": ["能力id", "项目根"],
        },
    ),
    构造工具定义(
        "tool_catalog",
        (
            "工具目录：返回薄壳当前暴露的工具清单（协议名/中文名/入参/转发目标）。"
            "只读元数据，不扫描仓库、不转发网关。"
        ),
        {"type": "object", "properties": {}},
    ),
)

中文名到协议名 = {
    "查询能力": "capability_search",
    "调用能力": "capability_call",
    "工具目录": "tool_catalog",
}
协议名到中文名 = {协议名: 中文名 for 中文名, 协议名 in 中文名到协议名.items()}


def 构建工具目录() -> dict[str, Any]:
    """薄壳自身工具清单（只读元数据，不触碰仓库其它文件）。"""
    清单 = [
        {
            "协议名": 工具.name,
            "中文名": 协议名到中文名.get(工具.name, 工具.name),
            "入参": 工具.inputSchema,
            "描述": 工具.description,
            "转发目标": {
                "capability_search": f"{调用能力操作} → {搜索能力目标}",
                "capability_call": f"{调用能力操作} → 入参指定能力id",
                "tool_catalog": "不转发（薄壳本地元数据）",
            }.get(工具.name, ""),
        }
        for 工具 in 三个工具定义
    ]
    return {
        "成功": True,
        "错误码": "",
        "错误说明": "",
        "薄壳工具数": len(清单),
        "工具": 清单,
        "传输": "stdio（不监听任何端口）",
        "唯一转发通道": "HTTP POST http://127.0.0.1:40007/网关/调用",
    }
