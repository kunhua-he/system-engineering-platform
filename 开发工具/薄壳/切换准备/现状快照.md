# 现状快照：本机指向 8766 的客户端配置（切换前，只读抄录）

> 抄录时间：2026-09-15（批次 A2）。**本文件只抄录，不改任何现役配置。**
> 抄录方式：`read_file` 带行号直读 + `grep -n` 全仓定位 + `claude mcp list` 只读健康检查。
> 冻结提交：`8e30554b167f93c9a12826123973bb4ff957d6fc`（2026-09-15 17:26:54 +0800）。

## 一、指向 8766 的配置只有两处（全仓 grep 实测）

```
grep -rn '8766' 项目根 --include=*.py --include=*.sh --include=*.json --include=*.md --include=*.plist
→ 代码/配置类命中仅 .mcp.json、opencode.json（其余命中全部在 开发文档/*.md 的叙述里）
```
- 本机其它客户端配置**都不指向 8766**：`~/.claude.json`、`~/.claude/settings.json`、`~/.codex/config.toml`（指向 8765）、`~/Library/Application Support/Claude/claude_desktop_config.json`（8765）、`~/.cursor/mcp.json`（8765）、`~/.config/opencode/opencode.json`（8765 的 `project_toolkit`）。→ **切换面只有本仓这两个文件。**

## 二、`.mcp.json`（Claude Code 项目级配置）原文

`~/Documents/Agent/PHP/系统工程平台/.mcp.json`：
```json
1|{
2|  "mcpServers": {
3|    "system_engineering_toolkit": {
4|      "type": "http",
5|      "url": "http://127.0.0.1:8766/mcp/",
6|      "headers": {
7|        "Authorization": "Bearer ${MCP_TOOLKIT_TOKEN}"
8|      }
9|    }
10|  }
11|}
```
- 指向 8766 的字段：`mcpServers.system_engineering_toolkit.url` = `http://127.0.0.1:8766/mcp/`（第 5 行）。
- 传输：`"type": "http"`（第 4 行）= Streamable HTTP，**要监听端口**。
- 令牌：`headers.Authorization` = `Bearer ${MCP_TOOLKIT_TOKEN}`（第 7 行）——**用 Claude Code 自己的 `${变量}` 插值**，令牌值不落盘。

## 三、`opencode.json`（opencode 项目级配置）原文

`~/Documents/Agent/PHP/系统工程平台/opencode.json`：
```json
1|{
2|  "$schema": "https://opencode.ai/config.json",
3|  "mcp": {
4|    "system_engineering_toolkit": {
5|      "type": "remote",
6|      "url": "http://127.0.0.1:8766/mcp/",
7|      "enabled": true,
8|      "oauth": false,
9|      "headers": {
10|        "Authorization": "Bearer {env:MCP工具箱_访问令牌}"
11|      }
12|    }
13|  }
14|}
```
- 指向 8766 的字段：`mcp.system_engineering_toolkit.url` = `http://127.0.0.1:8766/mcp/`（第 6 行）。
- 传输：`"type": "remote"`（第 5 行）——opencode 的 remote = HTTP/SSE。
- 令牌：`headers.Authorization` = `Bearer {env:MCP工具箱_访问令牌}`（第 10 行）——**opencode 的 `{env:变量名}` 插值**，令牌不落盘。

## 四、被指向的服务端：8766 现在怎么跑、令牌怎么校验（只读实测）

| 事实 | 实测证据 |
|---|---|
| 8766 正在监听 | `lsof -nP -iTCP -sTCP:LISTEN` → `Python 33333 … TCP 127.0.0.1:8766 (LISTEN)` |
| 启动方式（**回退要用**） | `ps -p 33333 -o command=` → `…/Python -m MCP工具箱.项目服务 --http --端口 8766`，父进程 74830 |
| 启动脚本 | `MCP工具箱/启动HTTP服务.sh`（第 26 行：`exec python3.14 -m MCP工具箱.项目服务 --http --端口 "${SERVICE_PORT}"`） |
| 令牌怎么传 | `MCP工具箱/启动HTTP服务.sh:12-24`：环境变量 `MCP工具箱_访问令牌` 存在则强制校验 `Authorization: Bearer …`；未设置则只靠「回环 + Host/Origin」守卫 |
| 8766 实测令牌状态 | `ps eww -p 33333 \| grep -c 'MCP工具箱_访问令牌'` → **0**（当前 8766 进程**没设**访问令牌，裸跑） |
| launchd | `grep -rl 8766 ~/Library/LaunchAgents/` → **无命中**（8766 不是常驻 plist 拉起的，是手工/脚本起的，D② 下线只能 kill 进程） |

## 五、客户端健康检查的现场原文（`claude mcp list`，只读命令）

```
$ cd ~/Documents/Agent/PHP/系统工程平台 && claude mcp list
Checking MCP server health…

project_toolkit: http://127.0.0.1:8765/mcp/ (HTTP) - ✔ Connected
system_engineering_toolkit: http://127.0.0.1:8766/mcp/ (HTTP) - ⏸ Pending approval (run `claude` to approve)

MCP Config Diagnostics
Location: ~/Documents/Agent/PHP/系统工程平台/.mcp.json
 └ [Warning] [system_engineering_toolkit] mcpServers.system_engineering_toolkit: Missing environment variables: MCP_TOOLKIT_TOKEN
```
- 现役 `.mcp.json` 的 `${MCP_TOOLKIT_TOKEN}` **当前并未设置**（客户端自己报 Missing environment variables），即 8766 侧也确认没开令牌校验——**两侧一致、当前是「回环免凭证」状态**。切薄壳后这一点必须补齐（薄壳强制要求网关凭证，见 `切换步骤.md` 步骤 2）。
- 项目级 `.mcp.json` 的 server 需要**首次批准**才会连（`⏸ Pending approval`），切换后同样要批一次。

`opencode mcp list` 现场原文（只读命令，v1.17.13）：
```
$ cd ~/Documents/Agent/PHP/系统工程平台 && opencode mcp list
┌  MCP Servers
│
●  ✓ project_toolkit connected
│      http://127.0.0.1:8765/mcp/
│
●  ✓ system_engineering_toolkit connected
│      http://127.0.0.1:8766/mcp/
│
└  2 server(s)
```
- opencode 侧 8766 是 **connected**（无令牌校验，与 8766 进程未设 `MCP工具箱_访问令牌` 一致）。
- 切换后这条命令是验收手段之一（D 批步骤 5.3）：期望仍显示 `system_engineering_toolkit connected`，但不再出现 `http://…8766…` 行。

## 六、切换后的对端：40007 唯一网关（薄壳要去的地方）

| 事实 | 实测证据 |
|---|---|
| 40007 正在监听 | `lsof` → `Python 37689 … TCP 127.0.0.1:40007 (LISTEN)` |
| 凭证来源 | `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables.系统库网关凭证`（值已核对存在，本文不复制明文） |
| 薄壳读哪个变量 | `MCP工具箱/薄壳/网关转发.py:17` `凭证环境变量 = "系统库网关凭证"`；`:16` `网关调用地址 = "http://127.0.0.1:40007/网关/调用"` |
| 缺凭证行为 | `网关转发.py:32-38` → 直接返回 `错误码=网关凭证缺失`，**不发起转发**（薄壳 fail-closed，不会静默无凭证调用） |
| 本机 shell 里有没有这个变量 | `printenv 系统库网关凭证` → **未设置**；`grep 系统库网关凭证 ~/.zshrc ~/.zprofile ~/.bash_profile ~/.profile` → **0 命中** → 必须在客户端配置里注入（切换步骤 2） |

## 七、薄壳入口（切换样例指向的实体，现场已存在）

`MCP工具箱/薄壳/`（批次 A 产出，git 未跟踪）：
```
薄壳服务.py          5980 B  ← 入口（stdio）
工具清单.py          3948 B
网关转发.py          3135 B
待补能力清单.py      2026 B
自测_stdio客户端.py  3136 B
```
- `薄壳服务.py:1` 自述「系统工程平台 MCP 薄壳（stdio 传输，不监听任何端口）」。
- `薄壳服务.py:6` 直跑法 `python3.14 MCP工具箱/薄壳/薄壳服务.py`（由 MCP 客户端拉起）。
- `薄壳服务.py:125-134` `主程序()` 走 `stdio_server()` → **无端口**。
- `薄壳服务.py:18-20` 把自己目录插进 `sys.path` 后 `import 工具清单/网关转发/待补能力清单` → **入口用绝对路径即可，不依赖 cwd，也不依赖 PYTHONPATH**。
- `薄壳服务.py:103-106` `list_tools` 恒返回 `三个工具定义` → 工具数恒 3（`工具清单.py:18-59`：`capability_search` / `capability_call` / `tool_catalog`；中文名 `工具清单.py:61-65`）。
