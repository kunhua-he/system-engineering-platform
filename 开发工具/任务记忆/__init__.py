"""任务记忆：给子代理用的**临时记忆包**（写 → 凭句柄读，用完即弃）。

## 这是什么 / 不是什么

| | 项目记忆 | 任务记忆包（本工具） |
|---|---|---|
| 生命周期 | 长期、跨会话、随项目走 | **临时**、随一次派发走、用完即弃 |
| 用途 | 权威事实（业务口径、架构边界） | 本次任务的**已核实事实包**（路径/行号/命令/结论） |
| 注入形态 | 不塞进任务信 | **任务信只写一行取用命令**，正文落盘 |

## 华哥 2026-09-18 裁决（哲学 12.3⑨）

> 「然后注入临时记忆的时候，建议可以选择是项目记忆，还是派发子代理临时记忆。
> 如果是子代理的临时记忆，就返回一句提示词；例如说是：请运行命令：'****'[句柄变量] 读取本次任务记忆。」

**为什么用句柄而不用「把正文复制进任务信」**：正文塞进提示词会随派发次数膨胀、无法回收、
无法复用；句柄形态下同一个记忆包可被多个子代理共用、可在磁盘上留存为**可核查事实**、
事后也能按 `开工ID` 追溯这轮子代理到底拿到了什么上下文。

## 用法

```bash
# 写（主会话）：事实包从 stdin 读，落盘后**打印可直接粘进任务信的那一行**
python3.14 开发工具/任务记忆/任务记忆.py 写 <开工ID> <一句话题目> < 事实包.md

# 读（子代理）：把记忆包原文打到 stdout
python3.14 开发工具/任务记忆/任务记忆.py 读 <开工ID>

# 列 / 清（主会话收尾）
python3.14 开发工具/任务记忆/任务记忆.py 列
python3.14 开发工具/任务记忆/任务记忆.py 清 <开工ID>
```

落点：`工程缓存/任务记忆/<开工ID>.md`（经 `公共契约.运行时.运行缓存.解析运行缓存根`，
**禁止裸拼 `工程缓存`**——制品运行时应落到制品外的稳定缓存根）。
"""
