"""验证场景体检：并行扫描源码与编译制品，一次性报出全部不合格场景（精确到包目录与原因）。

判据**不复制、不猜**：直接调用验证器自己的解析链
（`开发工具.HTML验证.场景加载._解析场景引用` + `_解析多步骤场景`），
验证器将来加任何新规则，本体检器自动跟上，永不漂移。

> 血泪教训（2026-09-17，两次踩坑，别重蹈）：
> ① **判据不能猜**：先前按 `契约版本 == "2.0.0"` 猜，把 133 个合法的「引用文件」全误报成不合格；
>    真判据是 `开发工具/HTML验证/常量.py::场景契约版本 = "验证场景/v1"`。
> ② **只调 `_解析场景引用` 不够**：它只读文件、不校验步骤断言；断言判据在下一层
>    `_解析多步骤场景`（它才调 `验证步骤.从字典` → `_解析断言`）。只调第一层会得到
>    「0 不合格」的假绿，而验证器照样阻断。两层都调才算跑完判据。
> ③ **扫描面为 0 也是假绿**（2026-09-19 修，同族案例 `开发工具/全量重算摘要.py:88-98`）：
>    修前只要「不合格 0 个」就报「全部通过」并退 0 —— 而**一个含场景的包目录都没扫到**时
>    同样是不合格 0 个，于是「体检根本没跑到东西」伪装成「体检通过」。
>    本入口是编译口门禁清单的**阻断项**（`开发工具/开发编译口/编译口.py:47`），
>    一旦 `验证场景引用.json` 被批量改名/搬走、或 `--根`/`--制品` 拼错，
>    编译口会以「退出码 0」整块放行。这正是哲学第 1.4 条「空转即杀」要禁的形态。

**退出码口径（2026-09-19 起，与「空集不是通过」一致）**：
- `0` = **扫描面 > 0 且 0 个不合格**（唯一的「通过」含义）；
- `1` = 有不合格包目录，**或扫描面为 0**（一个含场景包目录都没扫到），**或**显式指定的
  `--制品` 目录不存在。判据什么都没看，一律判红，**不许共用「通过」一词**；
- 扫描面为 0 与「0 个不合格」必须分开报：前者是「没看」，后者才是「看了没事」。
- 「0 个不合格」但「扫描面为 0」时**不再打印「全部通过」**，改为打印判红原因 + 修法。

用法：
    python3.14 开发工具/验证场景体检.py                    # 扫源码侧
    python3.14 开发工具/验证场景体检.py --制品 <制品目录>     # 额外扫制品侧
    python3.14 开发工具/验证场景体检.py --含缓存              # 源码侧也扫 工程缓存/
    python3.14 开发工具/验证场景体检.py --修                  # 批量修两类确定缺陷（只动源码侧）
    python3.14 开发工具/验证场景体检.py --覆盖 <制品目录>      # 覆盖率预检：公开能力取自制品 + 场景取自源码，**不重编译**就能报出还缺谁
"""
from __future__ import annotations
import sys
from pathlib import Path

# 仓库根入 sys.path：本工具既支持 `python3.14 -m 开发工具.验证场景体检`，
# 也支持 `python3.14 开发工具/验证场景体检.py`（直接跑脚本时 sys.path[0] 是 开发工具/，
# 导不到仓库根的 `公共契约` —— 否则报 ModuleNotFoundError，看着像工具坏了）
_仓库根 = str(Path(__file__).resolve().parent.parent)
if _仓库根 not in sys.path:
    sys.path.insert(0, _仓库根)

from 公共契约.基础类型.逻辑类型 import 真, 假

import argparse
import json
import os
import time
from concurrent.futures import ThreadPoolExecutor


def _载入判据(根: Path):
    """载入验证器自己的解析链，保证判据零漂移。"""
    if str(根) not in sys.path:
        sys.path.insert(0, str(根))
    from 开发工具.HTML验证 import 场景加载

    return 场景加载


def _收集包目录(基: Path, 含缓存: bool = False) -> list[Path]:
    """找出所有含 `验证场景引用.json` 的包目录（验证器也是按包目录加载的）。"""
    出: list[Path] = []
    for 底, 目录表, 文件表 in os.walk(基):
        if not 含缓存 and "工程缓存" in Path(底).parts:
            目录表[:] = []
            continue
        if "验证场景引用.json" in 文件表:
            出.append(Path(底))
    return 出


def 查一包(目录: Path, 场景加载) -> tuple[Path, str]:
    """跑完整解析链：引用解析 + 每个场景的步骤/断言解析。"""
    try:
        场景表 = 场景加载._解析场景引用(目录)
    except Exception as 异常:
        return 目录, f"{type(异常).__name__}: {异常}"
    for 原始, 包目录 in 场景表:
        try:
            场景加载._解析多步骤场景(原始, 包目录, "体检")
        except Exception as 异常:
            return 目录, f"{type(异常).__name__}: {异常}"
    return 目录, ""


def 体检(基: Path, 场景加载, 含缓存: bool = False, 并发: int = 32) -> tuple[list[tuple[Path, str]], int]:
    """跑一遍体检，返回（不合格清单, 扫描面大小）。

    **必须同时回传扫描面大小**：只回传「不合格清单」时，0 个包目录与 0 个不合格
    返回值完全相同（都是空列表），调用方无法分开「没看」与「看了没事」——
    这正是 2026-09-19 修掉的假绿成因（哲学 1.4 空集不是通过）。
    """
    包目录表 = _收集包目录(基, 含缓存)
    起 = time.time()
    with ThreadPoolExecutor(并发) as 池:
        结果 = list(池.map(lambda p: 查一包(p, 场景加载), 包目录表))
    坏 = [(d, q) for d, q in 结果 if q]
    print(f"[{基.name}] 扫描 {len(结果)} 个含场景的包目录，耗时 {time.time() - 起:.2f}s，不合格 {len(坏)} 个")
    return 坏, len(结果)


def 批量修(根: Path) -> int:
    """只修**判据明确**的两类确定缺陷，其余一律不碰、只报：
    ① 内联场景缺「清理步骤」→ 补空列表（验证器要求严格四键）；
    ② 负向断言（预期.成功=false）在 `返回断言` 里缺顶层「错误码」，而 `关键值.错误码` 有值
       → 提到顶层（照 `平台控制面/需求登记` 的既有正确写法：顶层与关键值同时存在）。
    """
    改动 = 0
    for 目录 in _收集包目录(根, 含缓存=False):
        引用文件 = 目录 / "验证场景引用.json"
        数据 = json.loads(引用文件.read_text(encoding="utf-8"))
        变了 = False
        for 引用 in 数据.get("验证场景引用", []):
            内联 = 引用.get("场景") if isinstance(引用, dict) else None
            if not isinstance(内联, dict):
                continue
            if "清理步骤" not in 内联:
                内联["清理步骤"] = []
                变了 = True
            for 阶段 in ("前置步骤", "目标步骤", "清理步骤"):
                for 步骤 in 内联.get(阶段) or []:
                    if not isinstance(步骤, dict):
                        continue
                    预期 = 步骤.get("预期")
                    if not isinstance(预期, dict) or 预期.get("成功") is not False:
                        continue
                    断言 = 预期.get("返回断言")
                    if not isinstance(断言, dict) or 断言.get("错误码"):
                        continue
                    候选 = (断言.get("关键值") or {}).get("错误码")
                    if isinstance(候选, str) and 候选:
                        断言["错误码"] = 候选
                        变了 = True
            重排 = {k: 内联[k] for k in ("场景id", "前置步骤", "目标步骤", "清理步骤") if k in 内联}
            for k in list(内联):
                if k not in 重排:
                    重排[k] = 内联[k]
            内联.clear()
            内联.update(重排)
        if 变了:
            引用文件.write_text(json.dumps(数据, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
            改动 += 1
            print(f"  已修: {引用文件.relative_to(根)}")
    return 改动


def 状态码一致性预检(根: Path) -> tuple[bool, str]:
    """期望失败用例的 `状态码` 必须与网关真源映射表逐码一致。**不重编译、不跑黑盒**。

    为什么单独做：验证器与 HTML 黑盒只在**跑制品**时才暴露「状态码 != 预期」，
    于是工作循环退化成「改场景 → 重编译 → 跑黑盒 → 才发现状态码猜错」。
    本条把判据前移到源码侧：错误码确定 → 状态码就确定。

    判据源（不复制、不猜）：`运行核心/统一网关/本地网关.py::公开错误码状态映射`，
    与 `网关核心.公开错误说明表` 逐码一一对应。

    > 血泪教训（2026-09-18，连续 7 轮才归因，违反哲学 3.1/3.3）：
    > 我按「能力级失败经网关包成 200」猜写 `状态码: 200`，实际网关对映射为 400 的错误码
    > **直接返回 400**，于是同一句 `状态码 400 != 预期 200` 报了 7 次。
    > 凡是「错误码已确定」的场景，状态码必须查表，不许猜。

    > 假绿教训（2026-09-19 修，与 `体检` 同源同型）：
    > 修前 0 个期望失败用例时输出「✅ 期望失败用例 0 个，状态码与网关映射表全部一致」并
    > `return 真` —— **判据一条用例都没看**却打出「全部一致」。0 个对象与「全对」是两件事，
    > 不许共用同一句话。现分清：0 个对象时只报「无对象」，绝不说「一致」。
    > 注：「扫描面为 0（含场景包目录一个都没扫到）」的整轮判红由 `主()` 统一裁决
    > （单一所有者，避免两处各判一遍、口径漂移）；本函数只在**有对象**时判状态码对错。
    """
    if str(根) not in sys.path:
        sys.path.insert(0, str(根))
    from 运行核心.统一网关.本地网关 import 公开错误码状态映射

    不符: list[str] = []
    总数 = 0
    包目录数 = 0
    for 目录 in _收集包目录(根, 含缓存=False):
        包目录数 += 1
        # ★ 场景正文在 `验证场景/` 子目录，必须递归扫（用 glob 只扫包根会漏掉全部内联场景 → 假绿）
        for 引用文件 in sorted(目录.rglob("*场景*.json")):
            try:
                数据 = json.loads(引用文件.read_text(encoding="utf-8"))
            except Exception:
                continue
            for 场景 in 数据.get("验证场景", []) if isinstance(数据, dict) else []:
                for 阶段 in ("前置步骤", "目标步骤", "清理步骤"):
                    for 步骤 in 场景.get(阶段, []) or []:
                        预期 = (步骤 or {}).get("预期") or {}
                        if 预期.get("成功") is not False:
                            continue
                        总数 += 1
                        错误码 = (预期.get("返回断言") or {}).get("错误码") or ""
                        真值 = 公开错误码状态映射.get(错误码)
                        写的 = 预期.get("状态码")
                        if 真值 is not None and 写的 != 真值:
                            不符.append(
                                f"{引用文件.name}｜{场景.get('场景id', '')}｜{步骤.get('步骤id', '')}"
                                f"：写 {写的} 真源 {真值}（{错误码}）"
                            )
    if 不符:
        头 = "\n      ".join(不符[:20])
        return 假, f"期望失败用例 {总数} 个，{len(不符)} 个状态码与真源不符：\n      {头}"
    if 包目录数 == 0:
        # 含场景包目录一个都没扫到 ⇒ 本条一条用例都没看 ⇒ 判红（空集不是通过，哲学 1.4）。
        return 假, ("**扫描面为 0**：含 `验证场景引用.json` 的包目录一个都没扫到，"
                    "本条判据什么都没看，不是通过（空集不是通过，哲学 1.4）。")
    if 总数 == 0:
        # 包目录扫到了，但确实没有负向用例：这是「无对象」，不是「全对」——
        # 只报事实，**绝不**打「全部一致」的对勾（0 个对象不能证明任何一致性）。
        return 真, f"扫描面 {包目录数} 个包目录，其中期望失败用例 0 个：本条**无对象可判**，不代表状态码已核验"
    return 真, f"期望失败用例 {总数} 个，状态码与网关映射表全部一致"


def 覆盖预检(根: Path, 制品: Path) -> tuple[bool, str]:
    """用**制品的公开能力** + **源码的场景**跑验证器自己的全集判据，无需重编译。

    为什么单独做：验证器只能对**已编译制品**跑，于是常见工作循环是「改场景 → 重编译（~20 秒）
    → 跑验证器 → 才发现还缺谁」。本函数把「公开能力」与「场景」两个来源拆开取值
    （能力集不受场景改动影响，取自现存制品；场景取自当前源码），一次调用就能拿到
    「还缺哪些能力没有正向目标步骤」的确定答案。

    依赖：制品目录必须已存在（只用它读公开能力，不校验其场景）。
    """
    from 开发工具.HTML验证.制品事实 import _扫描公开能力
    from 开发工具.HTML验证.场景加载 import _解析场景引用, _校验场景全集

    公开能力 = _扫描公开能力(Path(制品))[0]
    场景原始表: list = []
    for 目录 in _收集包目录(根, 含缓存=False):
        场景原始表 += _解析场景引用(目录)
    try:
        _校验场景全集(公开能力, 场景原始表, "预检")
        return 真, f"公开能力 {len(公开能力)} 个全部有正向目标步骤（源码场景 {len(场景原始表)} 条）"
    except Exception as 异常:
        return 假, f"{type(异常).__name__}: {异常}"


def 主() -> int:
    解析 = argparse.ArgumentParser(description="验证场景格式体检（判据直连验证器解析链）")
    解析.add_argument("--根", default=str(Path(__file__).resolve().parent.parent))
    解析.add_argument("--制品", default="", help="额外扫描的编译制品目录（绝对路径或相对根）")
    解析.add_argument("--覆盖", default="", help="覆盖率预检：用该制品（或相对根的路径）的公开能力 + 源码场景跑全集判据，不重编译")
    解析.add_argument("--含缓存", action="store_true", help="源码侧也扫 工程缓存/（默认跳过：缓存里是历史制品副本，不可修）")
    解析.add_argument("--修", action="store_true", help="批量修两类确定缺陷（只动源码侧）")
    参数 = 解析.parse_args()
    根 = Path(参数.根).resolve()
    场景加载 = _载入判据(根)

    if 参数.覆盖:
        制品 = Path(参数.覆盖) if os.path.isabs(参数.覆盖) else 根 / 参数.覆盖
        if not 制品.is_dir():
            print(f"[覆盖预检] 制品目录不存在: {制品}")
            return 1
        print(f"[覆盖预检] 公开能力取自: {制品}")
        通过, 详情 = 覆盖预检(根, 制品)
        print(("  ✅ " if 通过 else "  ❌ ") + 详情)
        if not 通过:
            return 1

    # 状态码一致性：源码侧可判的前置检查（不重编译、不跑黑盒），先跑它避免整轮返工
    码通过, 码详情 = 状态码一致性预检(根)
    print(("[状态码一致性] ✅ " if 码通过 else "[状态码一致性] ❌ ") + 码详情)
    if not 码通过:
        return 1

    if 参数.修:
        print("批量修（只动源码侧，只修判据明确的两类）…")
        数 = 批量修(根)
        print(f"修完 {数} 个文件。\n")

    总坏: list[tuple[Path, str]] = []
    扫描面合计 = 0
    缺目录: list[str] = []
    空扫: list[str] = []
    目标表 = [("源码", 根, 参数.含缓存)]
    if 参数.制品:
        制品基 = Path(参数.制品) if os.path.isabs(参数.制品) else 根 / 参数.制品
        目标表.append(("制品", 制品基, True))
    for 标签, 基, 含缓存 in 目标表:
        if not 基.is_dir():
            # 显式指定却不存在的目录 ⇒ 这份扫描面根本没看 ⇒ 判红。
            # 修前只 print「目录不存在，跳过」然后照旧 return 0：`--根` 拼错、`--制品`
            # 指到已清理的旧制品目录时，入口报「全部通过」——「没看」被说成「通过」。
            缺目录.append(f"{标签}（{基}）")
            print(f"[{标签}] ❌ 目录不存在: {基}")
            continue
        坏, 面 = 体检(基, 场景加载, 含缓存)
        扫描面合计 += 面
        总坏 += 坏
        if 面 == 0:
            # ★ 必须**逐目标**判 0，不能用跨目标累加数掩盖。
            # 累加口径下「源码 136 + 制品 0」会显示「扫描面 136 个」并整轮放行，
            # 而制品那一份**一个包都没看** —— 单目标空转被总量洗白，正是同型假绿
            # （2026-09-19 实测：`--制品 <空目录>` 报「全部通过：扫描面 136 个」、退出码 0）。
            # 正常态下每个目标都有场景：源码侧有 136 个包目录；制品侧按
            # `场景加载._加载场景` 的口径也要能扫出场景（否则验证器自己会抛
            # 「无任何有效验证场景」）。故任一目标为 0 一律判红，不误伤单目标用法。
            空扫.append(f"{标签}（{基}）")
            continue
        for 目录, 问题 in 坏[:40]:
            try:
                显示 = 目录.relative_to(基)
            except ValueError:
                显示 = 目录
            print(f"  ✗ {显示}")
            print(f"      {问题}")
    print()
    if 总坏:
        print(f"合计不合格 {len(总坏)} 个包目录（扫描面 {扫描面合计} 个含场景包目录）。")
    if 缺目录:
        print("❌ 判红：指定的目录不存在，不是「跳过」——" + "、".join(缺目录))
        print("   路径拼错或被搬走时，该份扫描面一个包都没看，空集不是通过（哲学 1.4）。")
        print("   先核对路径（`--根` 应是仓库根、`--制品` 应是现存制品目录），改对再重跑。")
    if 空扫:
        print("❌ 判红：以下目标扫描面为 0 —— 一个含 `验证场景引用.json` 的包目录都没扫到：")
        for 项 in 空扫:
            print(f"     · {项}")
        print("   该目标的判据什么都没看：不是「没有问题」，而是「没检查」"
              "（两件事，不许共用「通过」一词 —— 空集不是通过，哲学 1.4）。")
        print("   排查方向：【源码侧】`--根` 是否指到仓库根；`验证场景引用.json` 是否被改名/搬走；"
              "只在 工程缓存/ 里时需加 `--含缓存`。")
        print("   【制品侧】`--制品` 必须指向**含场景的编译制品**（正常制品含全量 "
              "`验证场景引用.json`）；指向空目录 / 临时目录 / 元数据目录即本项。")
        print("   定位：`rg --files -g 验证场景引用.json <该目录>` 一眼看出有 0 份。")
    if not 总坏 and not 缺目录 and not 空扫 and 扫描面合计 > 0:
        print(f"全部通过：扫描面 {扫描面合计} 个含场景包目录，0 个不合格。")
    return 1 if (总坏 or 缺目录 or 空扫 or 扫描面合计 == 0) else 0


if __name__ == "__main__":
    sys.exit(主())
