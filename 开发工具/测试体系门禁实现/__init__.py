"""测试体系门禁实现包：发现 / 导入探针 / 三项检查 / 报告。

对外只经 `开发工具/测试体系门禁.py` 调用：

- 门禁入口（给发布门禁等调用方）：`运行门禁(根) -> list[dict]`，空列表即通过；
- 完整结果（含跳过项与统计）：`运行检查(根) -> dict`。

内部各模块职责单一：发现只解析路径、可导入性检查只做导入与收集、零测试检查
与跳过检查各自独立判定、报告只做呈现——任一模块都不打印、不退出。
"""
from __future__ import annotations

from . import 发现, 报告, 跳过检查, 零测试检查, 可导入性检查

__all__ = ["发现", "报告", "跳过检查", "零测试检查", "可导入性检查"]
