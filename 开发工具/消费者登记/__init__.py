"""消费者登记：**不保留脚本**，只留口径说明。

## 为什么这里没有脚本

`登记消费者契约落地.py` 是一次性编排工具（现场枚举 → 登记 → 生成基线），原由一路子代理
放在 `工程缓存/`（它自己声明「临时脚本不入正式目录」）。父会话曾把它搬进本目录，
**但搬进来是错的**，理由有三：

1. **它违反平台依赖铁律**：脚本直接 `from 平台控制面.能力目录.实现.能力入口 import ...`
   —— 既跨包导入 `实现/` 目录，又是「开发工具 → 平台控制面」的越层依赖。
   依赖防火墙实测报 3 条违规（1 条跨包导入实现 + 2 条越层依赖）。
2. **它要做的事已经有公开能力**：`平台控制面.能力目录.登记消费者契约`、
   `...判定消费者登记完整性` 都是已注册的公开能力 —— 按铁律「统一调用接口」，
   不该另写一条直连实现的腿。
3. **平台规矩**：一次性脚本不留库（临时编排用 `/tmp` 或工程缓存，完成即弃）。

## 同类工作怎么做（正确姿势）

```bash
# 只报现场事实（不写盘）—— 走公开能力，不直连实现
cd <项目根> && unset PYTHONPATH
python3.14 -c "
import sys; sys.path.insert(0,'.')
from pathlib import Path
from 后端核心.后端核心 import 后端核心
b = 后端核心(); b.启动()
r = b.调用('平台控制面.能力目录.判定消费者登记完整性', {'参考目录': str(Path('.').resolve() / '平台控制面' / '能力目录')})
print(r.成功, r.值)
"
```

## 当前事实（2026-09-18 实测）

判定返回键是 `新增缺登记清单`（**不是** `未登记清单`）、且它是 `list[str]`（元素形如
`能力id <- 消费者包相对路径`），**不是** dict 列表。门禁曾因取错键 + 当 dict 调 `.get()`
而恒绿空转（异常被吞成「零违规」）。
"""
