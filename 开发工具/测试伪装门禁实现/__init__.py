"""测试伪装门禁实现包：发现 / 命名空间 / 桩检测 / 断言检测 / 豁免 / 报告。

对外只经 `开发工具/测试伪装门禁.py` 调用：

- 门禁入口（给发布门禁等调用方）：`运行门禁(根, 强制=False) -> list[dict]`，
  空列表即通过；`强制=False` 是首发「只报不拦」的落地点；
- 完整结果（含基线统计与观察项）：`运行检查(根) -> dict`。

三条硬规则的判据分别落在：

- 规则 1（禁 patch 被测对象自身 / 生产路径）+ 规则 2（一律 autospec）→ `桩检测`
- 规则 3（负面断言强度下限）→ `断言检测`

`命名空间` 只做生产命名空间与 patch 目标的纯解析（含用生产模块 AST 区分
「本体成员」与「依赖边界」），`豁免` 只做清单命中，`报告` 只做呈现。
任一模块都不导入被测模块、不执行用例、不打印、不退出。

所有模块只读：不写文件、不起子进程、不联网、不改仓库（哲学第 23 条）。
"""
from __future__ import annotations

from . import 发现, 命名空间, 断言检测, 报告, 桩检测, 豁免

__all__ = ["发现", "命名空间", "桩检测", "断言检测", "豁免", "报告"]
