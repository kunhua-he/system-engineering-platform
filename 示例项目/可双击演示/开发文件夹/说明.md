# 开发文件夹

这里是 demo 的唯一开发源目录：

- `前端/页面/*.json`：前端 IDE 编辑的页面源文件
- `前端/组件/*.json`：可复用组件模板
- `前端IDE/启动.py`：当前 demo 自己的轻代码前端服务入口，编辑结果直接保存到 `前端/页面/`
- `后端/`：Agent 编写业务后端和模块引用
- `启动前端IDE.command` / `.bat`：双击打开轻代码前端编辑器
- `编译.py`：编译到 `产物文件夹/日期-demo/`

前端不手写 HTML。页面 JSON 经过项目编译器生成产物 HTML；产物目录不依赖开发网关。
