<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 状态描述（支持库.前端.状态描述）

- 类型：支持库
- 版本：1.0.0
- 说明：平台无关窗口状态模型：创建状态、状态流转校验。
- 入口：__init__.py

## 能力
### 创建窗口状态（`状态描述.创建窗口状态`）
- 版本：1.0.0
- 一句话说明：创建平台无关窗口状态字典：窗口id、初始状态与空流转记录
- 所属支持库或模块：支持库.前端.状态描述
- 参数列表：窗口id:文本型, 初始状态:文本型
- 必填参数：窗口id, 初始状态
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：状态描述.创建窗口状态(窗口id=示例窗口, 初始状态=关闭)
- 依赖能力：无
### 状态流转校验（`状态描述.状态流转校验`）
- 版本：1.0.0
- 一句话说明：校验窗口状态流转是否合法（按允许流转表判断）
- 所属支持库或模块：支持库.前端.状态描述
- 参数列表：当前状态:文本型, 目标状态:文本型
- 必填参数：当前状态, 目标状态
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 状态流转不合法, 内部错误
- 调用示例：状态描述.状态流转校验(当前状态=关闭, 目标状态=打开)
- 依赖能力：无
