<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 桌面宿主（支持库.前端.桌面宿主）

- 类型：支持库
- 版本：1.0.0
- 说明：标准库桌面窗口宿主，封装窗口生命周期与事件回调。
- 入口：__init__.py

## 能力
### 启动桌面窗口（`桌面宿主.启动桌面窗口`）
- 版本：1.0.0
- 一句话说明：启动桌面窗口并把用户操作交给业务回调
- 所属支持库或模块：支持库.前端.桌面宿主
- 参数列表：标题:文本型, 初始文本:文本型, 调用函数:句柄型, 受管验证:逻辑型, 状态目录:文本型
- 必填参数：无
- 可选参数：标题, 初始文本, 调用函数, 受管验证, 状态目录
- 返回结构：整数型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 宿主不可用
- 调用示例：桌面宿主.启动桌面窗口()
- 依赖能力：无
