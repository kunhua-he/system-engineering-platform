"""桌面宿主支持库：把窗口生命周期与 tkinter 细节封装在前端支持库内。"""
from 支持库.前端.桌面宿主.实现.桌面宿主 import 启动桌面窗口
from 公共契约.能力契约.契约 import 能力实现
__all__ = ["启动桌面窗口"]

def 注册能力(注册表) -> None:
    注册表.注册(能力实现(能力id="桌面宿主.启动桌面窗口", 包id="支持库.前端.桌面宿主",
                       实现函数=启动桌面窗口, 参数=[{"名称": "标题", "类型": "文本型"},
                       {"名称": "初始文本", "类型": "文本型", "必填": False},
                       {"名称": "调用函数", "类型": "句柄型"}], 返回="整数型",
                       说明="启动桌面窗口并把用户操作交给业务回调"))
