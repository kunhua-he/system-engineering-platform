<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 组件控件（支持库.前端.组件控件）

- 类型：支持库
- 版本：1.0.0
- 说明：声明式前端组件支持库：窗口、页面、组件句柄、属性和事件绑定。
- 入口：__init__.py

## 能力
### 创建窗口（`组件控件.创建窗口`）
- 版本：1.0.0
- 一句话说明：创建窗口句柄。
- 所属支持库或模块：支持库.前端.组件控件
- 参数列表：窗口id:文本型, 标题:文本型, 宽度:整数型, 高度:整数型
- 必填参数：窗口id, 标题
- 可选参数：宽度, 高度
- 返回结构：结果型<窗口句柄型>（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 资源不足
- 调用示例：组件控件.创建窗口()
- 依赖能力：无
### 创建页面（`组件控件.创建页面`）
- 版本：1.0.0
- 一句话说明：在窗口中创建页面句柄。
- 所属支持库或模块：支持库.前端.组件控件
- 参数列表：窗口句柄:整数型, 页面id:文本型, 路由:文本型
- 必填参数：窗口句柄, 页面id
- 可选参数：路由
- 返回结构：结果型<页面句柄型>（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄不合法
- 调用示例：组件控件.创建页面()
- 依赖能力：无
### 创建组件（`组件控件.创建组件`）
- 版本：1.0.0
- 一句话说明：在页面中创建组件句柄。
- 所属支持库或模块：支持库.前端.组件控件
- 参数列表：页面句柄:整数型, 组件id:文本型, 组件类型:文本型, 属性:字典型
- 必填参数：页面句柄, 组件id, 组件类型
- 可选参数：属性
- 返回结构：结果型<组件句柄型>（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄不合法, 属性不支持
- 调用示例：组件控件.创建组件()
- 依赖能力：无
### 设置组件属性（`组件控件.设置组件属性`）
- 版本：1.0.0
- 一句话说明：设置组件属性。
- 所属支持库或模块：支持库.前端.组件控件
- 参数列表：组件句柄:整数型, 属性名:文本型, 属性值:字典型
- 必填参数：组件句柄, 属性名, 属性值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄不合法, 属性不支持
- 调用示例：组件控件.设置组件属性()
- 依赖能力：无
### 绑定组件事件（`组件控件.绑定组件事件`）
- 版本：1.0.0
- 一句话说明：绑定组件事件到公开能力。
- 所属支持库或模块：支持库.前端.组件控件
- 参数列表：组件句柄:整数型, 事件名称:文本型, 能力id:文本型, 参数模板:字典型
- 必填参数：组件句柄, 事件名称, 能力id
- 可选参数：参数模板
- 返回结构：结果型<事件定义型>（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄不合法
- 调用示例：能力.示例()
- 依赖能力：无
### 释放句柄（`组件控件.释放句柄`）
- 版本：1.0.0
- 一句话说明：释放组件、页面或窗口句柄。
- 所属支持库或模块：支持库.前端.组件控件
- 参数列表：句柄id:整数型
- 必填参数：句柄id
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：句柄不存在
- 调用示例：组件控件.释放句柄()
- 依赖能力：无
