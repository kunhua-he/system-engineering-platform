# 窗口描述（支持库.前端.窗口描述）

- 类型：支持库
- 版本：1.0.3
- 说明：平台无关窗口定义描述模型：创建、校验与序列化窗口定义。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 创建窗口定义（`窗口描述.创建窗口定义`）
- 版本：1.0.0
- 一句话说明：创建平台无关窗口定义字典：窗口id、标题、宽度、高度与默认属性
- 所属支持库或模块：支持库.前端.窗口描述
- 参数列表：窗口id:文本型, 标题:文本型, 宽度:整数型, 高度:整数型
- 必填参数：窗口id, 标题, 宽度, 高度
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：窗口描述.创建窗口定义(窗口id=示例窗口, 标题=示例标题, 宽度=400, 高度=300)
- 依赖能力：无
### 校验窗口定义（`窗口描述.校验窗口定义`）
- 版本：1.0.0
- 一句话说明：校验窗口定义字典的结构合法性（必填字段与尺寸类型）
- 所属支持库或模块：支持库.前端.窗口描述
- 参数列表：窗口定义:字典型
- 必填参数：窗口定义
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：窗口描述.校验窗口定义(窗口定义={'窗口id': '示例窗口', '标题': '示例标题', '宽度': 400, '高度': 300, '组件列表': []})
- 依赖能力：无
### 序列化窗口定义（`窗口描述.序列化窗口定义`）
- 版本：1.0.0
- 一句话说明：将窗口定义字典序列化为 JSON 文本
- 所属支持库或模块：支持库.前端.窗口描述
- 参数列表：窗口定义:字典型
- 必填参数：窗口定义
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：窗口描述.序列化窗口定义(窗口定义={'窗口id': '示例窗口', '标题': '示例标题', '宽度': 400, '高度': 300, '组件列表': []})
- 依赖能力：无
### 反序列化窗口定义（`窗口描述.反序列化窗口定义`）
- 版本：1.0.0
- 一句话说明：从 JSON 文本还原窗口定义字典并校验结构合法性
- 所属支持库或模块：支持库.前端.窗口描述
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 反序列化失败, 内部错误
- 调用示例：窗口描述.反序列化窗口定义(文本={"窗口id": "示例窗口", "标题": "示例标题", "宽度": 400, "高度": 300, "组件列表": []})
- 依赖能力：无
