"""支持库.额度控制 包级中文入口。"""

from __future__ import annotations

from 支持库.后端.额度控制.实现.额度控制 import 配置额度
from 支持库.后端.额度控制.实现.额度控制 import 查询额度
from 支持库.后端.额度控制.实现.额度控制 import 检查额度

__all__ = [
    "配置额度",
    "查询额度",
    "检查额度",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("额度控制.配置额度", 配置额度, [{"名称": "额度键", "类型": "文本型", "必填": True, "默认值": None, "说明": "额度键（如 会话id/调用方）"}, {"名称": "额度项", "类型": "文本型", "必填": True, "默认值": None, "说明": "额度项（如 max_tokens/超时秒/次数）"}, {"名称": "额度值", "类型": "整数型", "必填": True, "默认值": None, "说明": "额度值（0=不限制）"}]),
        ("额度控制.查询额度", 查询额度, [{"名称": "额度键", "类型": "文本型", "必填": True, "默认值": None, "说明": "额度键"}]),
        ("额度控制.检查额度", 检查额度, [{"名称": "额度键", "类型": "文本型", "必填": True, "默认值": None, "说明": "额度键"}, {"名称": "额度项", "类型": "文本型", "必填": True, "默认值": None, "说明": "额度项"}, {"名称": "已用值", "类型": "整数型", "必填": True, "默认值": None, "说明": "已用值"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.额度控制",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="额度控制支持库",
            )
        )
