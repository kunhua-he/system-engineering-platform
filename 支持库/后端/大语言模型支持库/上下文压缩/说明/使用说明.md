# 上下文压缩 使用说明

- 包id：支持库.后端.大语言模型支持库.上下文压缩
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 大语言模型支持库.上下文压缩.估算token数 | 估算token数 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 估算文本 token 数 |
| 大语言模型支持库.上下文压缩.估算消息token数 | 估算消息token数 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 估算消息列表 token 数 |
| 大语言模型支持库.上下文压缩.压缩历史 | 压缩历史 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 压缩对话历史（额度可配：压缩阈值/摘要长度上限默认不限制） |

## 大语言模型支持库.上下文压缩.估算token数（估算token数）

**说明**：估算文本 token 数

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.上下文压缩.估算token数(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 大语言模型支持库.上下文压缩.估算消息token数（估算消息token数）

**说明**：估算消息列表 token 数

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 消息列表 | 列表型 | 是 | None | 消息列表 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.上下文压缩.估算消息token数(消息列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 大语言模型支持库.上下文压缩.压缩历史（压缩历史）

**说明**：压缩对话历史（额度可配：压缩阈值/摘要长度上限默认不限制）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 消息列表 | 列表型 | 是 | None | 消息列表 |
| 压缩阈值 | 整数型 | 否 | None | 触发压缩的token阈值（默认8000） |
| 保留最近条数 | 整数型 | 否 | 10 | 保留最近N条完整消息 |
| 摘要长度上限 | 整数型 | 否 | None | 摘要长度上限（默认不限制） |
| 摘要模式 | 文本型 | 否 | 拼接 | 拼接/截断 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.上下文压缩.压缩历史(消息列表=值, 压缩阈值=值, 保留最近条数=值, 摘要长度上限=值, 摘要模式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
