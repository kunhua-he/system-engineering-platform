# 模型连接器（支持库.后端.大语言模型支持库.模型连接器）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.2.9
- 说明：模型连接器：LLM/向量/重排连接器 + 本地模型启动器，各返回六位句柄；其他能力持句柄调用模型。句柄默认 1800 秒无人使用自动释放，可续租/显式释放；系统内存占用超阈值（默认 80%）拒绝新连接（防撑爆内存）。本地（路径/启动器）与云端（url/api_key/模型/上下文长度）统一为连接配置。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 大语言模型支持库.模型连接器.连接LLM | 连接LLM | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}} | 连接大语言模型，返回句柄。本地：部署形态=本地+本地路径/启动器；云端：部署形态=云端+url/api_key/模型/上下文长度。句柄默认 1800 秒无人使用自动释放；系统内存占用超阈值拒绝新连接。 |
| 大语言模型支持库.模型连接器.连接向量模型 | 连接向量模型 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}} | 连接向量（嵌入）模型，返回句柄。 |
| 大语言模型支持库.模型连接器.连接重排模型 | 连接重排模型 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}} | 连接重排模型，返回句柄。 |
| 大语言模型支持库.模型连接器.连接决策模型 | 连接决策模型 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}} | 连接决策模型，返回句柄。决策模型按调用方给的问题定义在固定选项集上做判断，返回选项与标定置信度；本地权重目录须为含 config.json 的 HuggingFace 结构。 |
| 大语言模型支持库.模型连接器.启动本地模型 | 启动本地模型 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '模型类型': '文本型', '模型路径': '文本型', '端口': '整数型', '超时秒': '整数型'}} | 拉起本地模型进程（如 llama-server），启动成功后返回句柄；句柄超时/释放时自动终止进程。内存守卫：系统内存占用超阈值拒绝启动。 |
| 大语言模型支持库.模型连接器.注册本地进程 | 注册本地进程 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '已绑定进程': '逻辑型'}} | 把真实拉起的子进程绑定到句柄（适配层 Provider 调用，内部能力）。 |
| 大语言模型支持库.模型连接器.生成对话 | 生成对话 | 1.0.0 | {'类型': '结果型', '值结构': {'回复': '文本型', '用量': '字典型'}} | 持 LLM 句柄生成对话回复；流式输出默认 false，当前40007网关未装配模型SSE时返回结构化错误。 |
| 大语言模型支持库.模型连接器.生成嵌入 | 生成嵌入 | 1.0.0 | {'类型': '结果型', '值结构': {'向量': '列表型'}} | 持向量句柄生成嵌入向量。 |
| 大语言模型支持库.模型连接器.执行重排 | 执行重排 | 1.0.0 | {'类型': '结果型', '值结构': {'重排结果': '列表型'}} | 持重排句柄重排候选文档。 |
| 大语言模型支持库.模型连接器.执行决策 | 执行决策 | 1.0.0 | {'类型': '结果型', '值结构': {'答案': '字典型'}} | 持决策句柄按问题定义对文本做决策，返回选项/概率/置信度。问题三型：choice（多选一）/ score（分档打分）/ noul（是或否）。 |
| 大语言模型支持库.模型连接器.续租句柄 | 续租句柄 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '已续租': '逻辑型', '超时秒': '整数型'}} | 续租句柄：重置最后活动时间，可选覆盖超时秒。 |
| 大语言模型支持库.模型连接器.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '已释放': '逻辑型'}} | 显式释放句柄（幂等，重复释放不报错）；本地模型句柄释放时终止对应进程。 |
| 大语言模型支持库.模型连接器.查询句柄状态 | 查询句柄状态 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '连接类型': '文本型', '剩余秒': '整数型'}} | 查询句柄状态：有效/已失效、连接类型、模型、剩余可用秒。 |
| 大语言模型支持库.模型连接器.资源快照 | 资源快照 | 1.0.0 | {'类型': '结果型', '值结构': {'系统内存': '字典型', '活跃连接': '字典型', '内存安全阈值': '双精度数型'}} | 返回资源快照：系统内存（总量/可用/占用率）+ 活跃连接分布 + 内存安全阈值。 |
| 大语言模型支持库.模型连接器.加载环境配置 | 加载环境配置 | 1.0.0 | {'类型': '结果型', '值结构': {'已读取键数': '整数型', '配置摘要': '字典型'}} | 从项目环境文件或进程环境变量读取统一模型参数；连接器缺参时自动使用。返回 {已读取键数, 配置摘要}。 |
| 大语言模型支持库.模型连接器.查询环境配置 | 查询环境配置 | 1.0.0 | {'类型': '结果型', '值结构': {'默认向量模型': '文本型', '默认LLM模型': '文本型', '默认重排模型': '文本型'}} | 返回当前统一环境参数（不含密钥明文）。 |
| 大语言模型支持库.模型连接器.循环软护栏 | 循环软护栏 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 检测重复工具调用并注入软提醒（DeepSeek repeat-tool-reminder 模式化），不硬杀不静默，纠错权交给模型 |
| 大语言模型支持库.模型连接器.调度工具调用 | 调度工具调用 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 工具并行调度计划（DeepSeek 模式化：exclusive屏障+有界池+结果按model序） |
| 大语言模型支持库.模型连接器.合成取消结果 | 合成取消结果 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 合成取消/未启动调用的错误结果（错误码 派发前已取消） |
| 大语言模型支持库.模型连接器.重试调度 | 重试调度 | 1.0.0 | {'类型': '结果型', '值结构': {'是否重试': '逻辑型', '下次退避秒': '双精度数型', '原因': '文本型'}} | 按错误码白名单/兜底模式判定是否重试，并给出带抖动的指数退避秒数。模式1=normal：仅错误码白名单命中才重试（白名单为空即不命中）；模式2=always：忽略白名单无条件兜底重试。第 i 次重试退避 = 退避基数秒 × 2^i × (1 + 抖动比例)，单次上限 300 秒；退避计划写入 原因。纯计算只读，不发起模型/网络/工具调用；可选入参 实际错误码：传入时模式1 按「实际错误码 ∈ 白名单」判定是否命中（不传保持旧行为） |
| 大语言模型支持库.模型连接器.组装可用工具清单 | 组装可用工具清单 | 1.0.0 | {'类型': '结果型', '值结构': {'工具清单': '列表型', '数量': '整数型'}} | 按门控模式过滤/排序调用方传入的工具清单：模式1=全量（返回全部）；模式2=显式清单（只返回可用清单命中的工具）。按工具名去重（保序）后升序排序，输出 {工具清单, 数量}。不内嵌任何业务角色判断，清单全部由调用方传入。 |
| 大语言模型支持库.模型连接器.归一化对话响应 | 归一化对话响应 | 1.0.0 | {'类型': '结果型', '值结构': {'内容': '文本型', '思考': '文本型', '工具调用': '列表型', '结束原因': '文本型', '用量': '字典型'}} | 把模型原始响应归一化为 {内容,思考,工具调用,结束原因,用量}；支持 codex_responses/anthropic/ollama/chat_completions 四种协议 |
| 大语言模型支持库.模型连接器.归一化流式分块 | 归一化流式分块 | 1.0.1 | {'类型': '结果型', '值结构': {'类型': '文本型', '内容': '文本型', '工具调用': '列表型', '用量': '字典型'}} | 把模型流式单块归一化为 {类型,内容,工具调用,用量}；结构事件返回 类型=跳过 |
| 大语言模型支持库.模型连接器.探测模型端点 | 探测模型端点 | 1.0.0 | {'类型': '结果型', '值结构': {'可用': '逻辑型', '命中协议': '文本型', '模型': '文本型', '模型清单': '列表型', '响应毫秒': '双精度数型', '逐项结果': '列表型', '诊断': '列表型'}} | 探测一个模型端点是否可用、走哪种协议。目的：调用方只给 url 与 api_key 即可；缺的参数自动补齐——第一步向端点取模型清单（未给模型时，用 GET /models），第二步拿模型试协议（给了协议只打这一个，未给则按 chat_completions→codex_responses→anthropic_messages 依次试，命中即停）。可用性判据是响应体特征字段（choices/output/content+type），不看状态码——宽容服务器对任意路径都回 200。有一个成功即返回成功并列出其余协议的失败原因；三个全失败则如实返回三项失败原因。不猜根路径：url 原样使用，全失败时列出试过的完整路径。 |
| 大语言模型支持库.模型连接器.记录模型调用 | 记录模型调用 | 1.0.0 | {'类型': '结果型', '值结构': {'已记录': '逻辑型', '记录id': '文本型', '累计条数': '整数型'}} | 记录一条模型调用（打分燃料的入口）。★ 本能力不消耗任何 LLM 额度——记录的是「已经发生过的调用」，模型调用由调用方完成后再把结果告诉本能力。存储在本包专用库（运行数据根/大语言模型支持库.模型用量.db）。同 记录id 重复写为更新语义。 |
| 大语言模型支持库.模型连接器.汇总模型表现 | 汇总模型表现 | 1.0.0 | {'类型': '结果型', '值结构': {'汇总': '列表型', '数据条数': '整数型', '说明': '文本型'}} | 把用量记录汇总成可比较的表现指标（纯统计，不消耗任何额度）。按任务类型分组，给出样本数/成功率/平均耗时/平均令牌/平均成本，以及是否样本充足（低于最少样本数不给分，如实标注不编造）。 |
| 大语言模型支持库.模型连接器.选择模型 | 选择模型 | 1.0.0 | {'类型': '结果型', '值结构': {'建议': '列表型', '排除': '列表型', '模式': '文本型', '打分依据': '字典型', '额度说明': '文本型', '诊断': '列表型'}} | 按历史实测表现把候选模型排序，给出建议与理由（只读建议，不自动切换）。第一层硬门槛永远执行（上下文超限排除、候选端点不可达排除）；第二层统计打分仅在启用智能路由时执行（读本地调用记录、四维加权、不消耗额度）。默认保守模式。底座不做隐私/安全判断（哲学 9.3）；参考路径只是用户自带规则的可选输入，不给则完全不涉及。 |

## 大语言模型支持库.模型连接器.连接LLM（连接LLM）

**说明**：连接大语言模型，返回句柄。本地：部署形态=本地+本地路径/启动器；云端：部署形态=云端+url/api_key/模型/上下文长度。句柄默认 1800 秒无人使用自动释放；系统内存占用超阈值拒绝新连接。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型 | 文本型 | 是 | None | 模型名 |
| 提供者 | 文本型 | 否 | 本地 | 提供者标识 |
| 部署形态 | 文本型 | 否 | None | 本地/云端（默认按 url 推断） |
| 本地路径 | 文本型 | 否 | None | 本地模型文件路径 |
| 启动器 | 文本型 | 否 | llama-server | 本地模型启动器 |
| 模型大小字节 | 整数型 | 否 | None | 模型文件大小（内存守卫用） |
| url | 文本型 | 否 | None | 云端端点（如 http://127.0.0.1:8328/hermes-chat/v1） |
| api_key | 文本型 | 否 | None | 云端密钥 |
| 上下文长度 | 整数型 | 否 | None | 上下文窗口 |
| 协议 | 文本型 | 否 | chat_completions | 请求协议：chat_completions、codex_responses 或 anthropic_messages |
| 额外请求头 | 字典型 | 否 | None | 连接级静态请求头，出站时叠加在内置头之上（生成对话的 附加请求头 可覆盖同名键） |
| 超时秒 | 双精度数型 | 否 | 1800 | 无人使用自动释放秒数 |
| 内存估算系数 | 双精度数型 | 否 | None | 覆盖内存守卫的模型占用估算系数（默认 1.15） |
| 内存安全阈值 | 双精度数型 | 否 | None | 覆盖内存守卫阈值（默认 0.80） |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不足 |

**调用示例**：

```python
大语言模型支持库.模型连接器.连接LLM(模型=值, 提供者=值, 部署形态=值, 本地路径=值, 启动器=值, 模型大小字节=值, url=值, api_key=值, 上下文长度=值, 协议=值, 额外请求头=值, 超时秒=值, 内存估算系数=值, 内存安全阈值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.连接向量模型（连接向量模型）

**说明**：连接向量（嵌入）模型，返回句柄。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型 | 文本型 | 是 | None | 模型名 |
| 提供者 | 文本型 | 否 | 本地 | 提供者标识 |
| 部署形态 | 文本型 | 否 | None | 本地/云端（默认按 url 推断） |
| 本地路径 | 文本型 | 否 | None | 本地模型文件路径 |
| 启动器 | 文本型 | 否 | llama-server | 本地模型启动器 |
| 模型大小字节 | 整数型 | 否 | None | 模型文件大小（内存守卫用） |
| url | 文本型 | 否 | None | 云端端点（如 http://127.0.0.1:8328/hermes-chat/v1） |
| api_key | 文本型 | 否 | None | 云端密钥 |
| 超时秒 | 双精度数型 | 否 | 1800 | 无人使用自动释放秒数 |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不足 |

**调用示例**：

```python
大语言模型支持库.模型连接器.连接向量模型(模型=值, 提供者=值, 部署形态=值, 本地路径=值, 启动器=值, 模型大小字节=值, url=值, api_key=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.连接重排模型（连接重排模型）

**说明**：连接重排模型，返回句柄。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型 | 文本型 | 是 | None | 模型名 |
| 提供者 | 文本型 | 否 | 本地 | 提供者标识 |
| 部署形态 | 文本型 | 否 | None | 本地/云端（默认按 url 推断） |
| 本地路径 | 文本型 | 否 | None | 本地模型文件路径 |
| 启动器 | 文本型 | 否 | llama-server | 本地模型启动器 |
| 模型大小字节 | 整数型 | 否 | None | 模型文件大小（内存守卫用） |
| url | 文本型 | 否 | None | 云端端点（如 http://127.0.0.1:8328/hermes-chat/v1） |
| api_key | 文本型 | 否 | None | 云端密钥 |
| 超时秒 | 双精度数型 | 否 | 1800 | 无人使用自动释放秒数 |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不足 |

**调用示例**：

```python
大语言模型支持库.模型连接器.连接重排模型(模型=值, 提供者=值, 部署形态=值, 本地路径=值, 启动器=值, 模型大小字节=值, url=值, api_key=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.连接决策模型（连接决策模型）

**说明**：连接决策模型，返回句柄。决策模型按调用方给的问题定义在固定选项集上做判断，返回选项与标定置信度；本地权重目录须为含 config.json 的 HuggingFace 结构。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型 | 文本型 | 是 | None | 模型名 |
| 提供者 | 文本型 | 否 | 本地 | 提供者标识 |
| 部署形态 | 文本型 | 否 | None | 本地/云端（默认按 url 推断） |
| 本地路径 | 文本型 | 否 | None | 本地模型文件路径 |
| 启动器 | 文本型 | 否 | llama-server | 本地模型启动器 |
| 模型大小字节 | 整数型 | 否 | None | 模型文件大小（内存守卫用） |
| url | 文本型 | 否 | None | 云端端点（如 http://127.0.0.1:8328/hermes-chat/v1） |
| api_key | 文本型 | 否 | None | 云端密钥 |
| 超时秒 | 双精度数型 | 否 | 1800 | 无人使用自动释放秒数 |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不足 |

**调用示例**：

```python
大语言模型支持库.模型连接器.连接决策模型(模型=值, 提供者=值, 部署形态=值, 本地路径=值, 启动器=值, 模型大小字节=值, url=值, api_key=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.启动本地模型（启动本地模型）

**说明**：拉起本地模型进程（如 llama-server），启动成功后返回句柄；句柄超时/释放时自动终止进程。内存守卫：系统内存占用超阈值拒绝启动。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型路径 | 文本型 | 是 | None | 模型文件路径 |
| 模型类型 | 文本型 | 否 | LLM | LLM/向量/重排 |
| 启动器 | 文本型 | 否 | llama-server | 启动器 |
| 端口 | 整数型 | 否 | None | 服务端口 |
| 模型大小字节 | 整数型 | 否 | None | 模型大小（内存守卫用） |
| 超时秒 | 双精度数型 | 否 | 1800 | 无人使用自动释放秒数 |
| 参数 | 字典型 | 否 | None | 可选启动配置：启动参数列表（列表型，追加到 llama-server 命令行，如 ["-c","40960","--cache-ram","0"]）；内存估算系数（数，覆盖内存守卫估算）；协议（LLM 会话协议） |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '模型类型': '文本型', '模型路径': '文本型', '端口': '整数型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不足 |

**调用示例**：

```python
大语言模型支持库.模型连接器.启动本地模型(模型路径=值, 模型类型=值, 启动器=值, 端口=值, 模型大小字节=值, 超时秒=值, 参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.注册本地进程（注册本地进程）

**说明**：把真实拉起的子进程绑定到句柄（适配层 Provider 调用，内部能力）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 启动本地模型 返回） |
| 进程对象 | 空值型 | 否 | None | 子进程对象 |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '已绑定进程': '逻辑型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
大语言模型支持库.模型连接器.注册本地进程(句柄=值, 进程对象=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.生成对话（生成对话）

**说明**：持 LLM 句柄生成对话回复；流式输出默认 false，当前40007网关未装配模型SSE时返回结构化错误。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接LLM 返回） |
| 消息列表 | 列表型 | 是 | None | 有序消息列表，元素形如 {角色, 内容} |
| 系统提示词 | 文本型 | 否 | None | 系统提示词 |
| 流式输出 | 逻辑型 | 否 | False | 是否请求流式输出；40007 的流式请走专用操作 /网关/流式 |
| 温度 | 双精度数型 | 否 | None | 采样温度；不传由上游取默认值 |
| 最大令牌数 | 整数型 | 否 | None | 最大输出令牌数；按协议映射为 max_output_tokens/max_tokens（anthropic 为必填 max_tokens，未指定时用协议默认值） |
| 工具 | 列表型 | 否 | None | 工具定义列表（tools） |
| 响应格式 | 字典型 | 否 | None | 结构化输出格式；映射为 text.format 或 response_format |
| 附加请求头 | 字典型 | 否 | None | 单次调用级请求头，仅本次出站叠加，可覆盖连接级 额外请求头 的同名键 |
| chat_template_kwargs | 字典型 | 否 | None | 传给上游聊天模板的键值，如 {"enable_thinking": false}。开源推理模型（Qwen3.6/gemma-4 实测）默认开思考，token 全用于 reasoning_content、content 返回空串，调用方会误判模型无输出；chat_completions 协议下传本参数即关闭。anthropic 协议不支持，传入报错 |

**返回结构**：{'类型': '结果型', '值结构': {'回复': '文本型', '用量': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 提供者不可用 |
| 模型调用失败 |
| 流式能力未装配 |

**调用示例**：

```python
大语言模型支持库.模型连接器.生成对话(句柄=值, 消息列表=值, 系统提示词=值, 流式输出=值, 温度=值, 最大令牌数=值, 工具=值, 响应格式=值, 附加请求头=值, chat_template_kwargs=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.生成嵌入（生成嵌入）

**说明**：持向量句柄生成嵌入向量。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接向量模型 返回） |
| 文本 | 文本型 | 是 | None | 要嵌入的文本 |

**返回结构**：{'类型': '结果型', '值结构': {'向量': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 提供者不可用 |
| 模型调用失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.生成嵌入(句柄=值, 文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.执行重排（执行重排）

**说明**：持重排句柄重排候选文档。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接重排模型 返回） |
| 查询 | 文本型 | 是 | None | 查询文本 |
| 文档列表 | 列表型 | 是 | None | 候选文档 |

**返回结构**：{'类型': '结果型', '值结构': {'重排结果': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 提供者不可用 |
| 模型调用失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.执行重排(句柄=值, 查询=值, 文档列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.执行决策（执行决策）

**说明**：持决策句柄按问题定义对文本做决策，返回选项/概率/置信度。问题三型：choice（多选一）/ score（分档打分）/ noul（是或否）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接决策模型 返回） |
| 文本 | 文本型 | 是 | None | 待判断的文本（state） |
| 问题 | 字典型 | 是 | None | 问题定义（键为问题名）：{"环节": {"type": "choice", "instructions": "…", "criteria": {"选项A": "…"}}} |

**返回结构**：{'类型': '结果型', '值结构': {'答案': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 提供者不可用 |
| 模型调用失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.执行决策(句柄=值, 文本=值, 问题=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.续租句柄（续租句柄）

**说明**：续租句柄：重置最后活动时间，可选覆盖超时秒。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接LLM / 连接向量模型 / 连接重排模型 / 启动本地模型 返回） |
| 租约秒 | 双精度数型 | 否 | None | 新的超时秒 |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '已续租': '逻辑型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
大语言模型支持库.模型连接器.续租句柄(句柄=值, 租约秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.释放句柄（释放句柄）

**说明**：显式释放句柄（幂等，重复释放不报错）；本地模型句柄释放时终止对应进程。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接LLM / 连接向量模型 / 连接重排模型 / 启动本地模型 返回） |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '已释放': '逻辑型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源释放中 |
| 资源未收敛 |

**调用示例**：

```python
大语言模型支持库.模型连接器.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.查询句柄状态（查询句柄状态）

**说明**：查询句柄状态：有效/已失效、连接类型、模型、剩余可用秒。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 模型连接句柄（由 连接LLM / 连接向量模型 / 连接重排模型 / 启动本地模型 返回） |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '连接类型': '文本型', '剩余秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.模型连接器.查询句柄状态(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.资源快照（资源快照）

**说明**：返回资源快照：系统内存（总量/可用/占用率）+ 活跃连接分布 + 内存安全阈值。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {'系统内存': '字典型', '活跃连接': '字典型', '内存安全阈值': '双精度数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.模型连接器.资源快照()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.加载环境配置（加载环境配置）

**说明**：从项目环境文件或进程环境变量读取统一模型参数；连接器缺参时自动使用。返回 {已读取键数, 配置摘要}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 环境文件路径 | 文本型 | 否 | None | 环境文件路径（不传读 os.environ） |

**返回结构**：{'类型': '结果型', '值结构': {'已读取键数': '整数型', '配置摘要': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.模型连接器.加载环境配置(环境文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.查询环境配置（查询环境配置）

**说明**：返回当前统一环境参数（不含密钥明文）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {'默认向量模型': '文本型', '默认LLM模型': '文本型', '默认重排模型': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.模型连接器.查询环境配置()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.循环软护栏（循环软护栏）

**说明**：检测重复工具调用并注入软提醒（DeepSeek repeat-tool-reminder 模式化），不硬杀不静默，纠错权交给模型

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 历史调用 | 列表型 | 否 | None | 历史工具调用名列表 |
| 工具名 | 文本型 | 否 | None | 目标工具名 |
| 阈值档位 | 列表型 | 否 | None | 提醒阈值 默认[3,5,8] |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 软护栏检查失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.循环软护栏(历史调用=值, 工具名=值, 阈值档位=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.调度工具调用（调度工具调用）

**说明**：工具并行调度计划（DeepSeek 模式化：exclusive屏障+有界池+结果按model序）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 工具调用列表 | 列表型 | 是 | None | 调用列表 |
| 并行上限 | 整数型 | 否 | 10 | 并行上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 调度失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.调度工具调用(工具调用列表=值, 并行上限=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.合成取消结果（合成取消结果）

**说明**：合成取消/未启动调用的错误结果（错误码 派发前已取消）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 调用id | 文本型 | 是 | None | 调用唯一标识（文本），用于把请求与结果对齐 |
| 工具名 | 文本型 | 否 | None | 工具名 |
| 原因 | 文本型 | 否 | None | 原因 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 合成取消结果失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.合成取消结果(调用id=值, 工具名=值, 原因=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.重试调度（重试调度）

**说明**：按错误码白名单/兜底模式判定是否重试，并给出带抖动的指数退避秒数。模式1=normal：仅错误码白名单命中才重试（白名单为空即不命中）；模式2=always：忽略白名单无条件兜底重试。第 i 次重试退避 = 退避基数秒 × 2^i × (1 + 抖动比例)，单次上限 300 秒；退避计划写入 原因。纯计算只读，不发起模型/网络/工具调用；可选入参 实际错误码：传入时模式1 按「实际错误码 ∈ 白名单」判定是否命中（不传保持旧行为）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 重试模式 | 文本型 | 是 | None | 1=normal 白名单/2=always 兜底 |
| 错误码白名单 | 列表型 | 否 | None | 可重试错误码白名单（模式1 生效，空即不命中） |
| 实际错误码 | 文本型 | 否 | None | 本次真实发生的错误码（可选）。传了则模式1 按「实际错误码 ∈ 白名单」判定命中；不传则沿用旧行为（白名单非空即重试） |
| 最大重试次数 | 整数型 | 否 | 3 | 重试次数上限（0-100） |
| 退避基数秒 | 双精度数型 | 否 | 0.5 | 指数退避基数秒（须大于 0） |
| 抖动比例 | 双精度数型 | 否 | 0.1 | 退避上浮抖动比例（0-1，确定性可复现） |

**返回结构**：{'类型': '结果型', '值结构': {'是否重试': '逻辑型', '下次退避秒': '双精度数型', '原因': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 重试调度失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.重试调度(重试模式=值, 错误码白名单=值, 实际错误码=值, 最大重试次数=值, 退避基数秒=值, 抖动比例=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.组装可用工具清单（组装可用工具清单）

**说明**：按门控模式过滤/排序调用方传入的工具清单：模式1=全量（返回全部）；模式2=显式清单（只返回可用清单命中的工具）。按工具名去重（保序）后升序排序，输出 {工具清单, 数量}。不内嵌任何业务角色判断，清单全部由调用方传入。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 门控模式 | 文本型 | 是 | None | 1=全量/2=显式清单 |
| 全部工具清单 | 列表型 | 是 | None | 调用方传入的全量工具清单（文本或含 名称/工具名 的字典） |
| 可用工具清单 | 列表型 | 否 | None | 显式可用工具清单（门控模式=2 生效） |

**返回结构**：{'类型': '结果型', '值结构': {'工具清单': '列表型', '数量': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 组装工具清单失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.组装可用工具清单(门控模式=值, 全部工具清单=值, 可用工具清单=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.归一化对话响应（归一化对话响应）

**说明**：把模型原始响应归一化为 {内容,思考,工具调用,结束原因,用量}；支持 codex_responses/anthropic/ollama/chat_completions 四种协议

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 原始数据 | 字典型 | 是 | None | 模型原始响应 JSON |
| 包含思考 | 逻辑型 | 否 | False | 是否提取思考内容 |

**返回结构**：{'类型': '结果型', '值结构': {'内容': '文本型', '思考': '文本型', '工具调用': '列表型', '结束原因': '文本型', '用量': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 归一化失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.归一化对话响应(原始数据=值, 包含思考=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.归一化流式分块（归一化流式分块）

**说明**：把模型流式单块归一化为 {类型,内容,工具调用,用量}；结构事件返回 类型=跳过

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 分块 | 字典型 | 是 | None | 流式单块 JSON |
| 包含思考 | 逻辑型 | 否 | False | 是否提取思考内容 |

**返回结构**：{'类型': '结果型', '值结构': {'类型': '文本型', '内容': '文本型', '工具调用': '列表型', '用量': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 归一化失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.归一化流式分块(分块=值, 包含思考=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.探测模型端点（探测模型端点）

**说明**：探测一个模型端点是否可用、走哪种协议。目的：调用方只给 url 与 api_key 即可；缺的参数自动补齐——第一步向端点取模型清单（未给模型时，用 GET /models），第二步拿模型试协议（给了协议只打这一个，未给则按 chat_completions→codex_responses→anthropic_messages 依次试，命中即停）。可用性判据是响应体特征字段（choices/output/content+type），不看状态码——宽容服务器对任意路径都回 200。有一个成功即返回成功并列出其余协议的失败原因；三个全失败则如实返回三项失败原因。不猜根路径：url 原样使用，全失败时列出试过的完整路径。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| url | 文本型 | 是 | None | 端点地址，如 http://127.0.0.1:11434/v1（原样使用，不自动改写根路径） |
| api_key | 文本型 | 否 | None | 密钥；缺省用 123 占位（本地服务常不校验） |
| 模型 | 文本型 | 否 | None | 模型名；缺省自动向端点取模型清单并用第一个 |
| 协议 | 文本型 | 否 | None | 取值 chat_completions / codex_responses / anthropic_messages；缺省自动依次试三种、命中即停 |
| 超时秒 | 双精度数型 | 否 | 15 | 单次请求超时秒 |

**返回结构**：{'类型': '结果型', '值结构': {'可用': '逻辑型', '命中协议': '文本型', '模型': '文本型', '模型清单': '列表型', '响应毫秒': '双精度数型', '逐项结果': '列表型', '诊断': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.模型连接器.探测模型端点(url=值, api_key=值, 模型=值, 协议=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.记录模型调用（记录模型调用）

**说明**：记录一条模型调用（打分燃料的入口）。★ 本能力不消耗任何 LLM 额度——记录的是「已经发生过的调用」，模型调用由调用方完成后再把结果告诉本能力。存储在本包专用库（运行数据根/大语言模型支持库.模型用量.db）。同 记录id 重复写为更新语义。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型 | 文本型 | 是 | None | 模型名 |
| 成功 | 逻辑型 | 是 | None | 本次调用是否成功 |
| 任务类型 | 文本型 | 否 | None | 任务类型，如 策划/日常/清洗/看图/生图、不给记为 未分类 |
| 耗时毫秒 | 整数型 | 否 | None | 本次调用耗时毫秒 |
| 输入令牌 | 整数型 | 否 | None | 输入令牌数 |
| 输出令牌 | 整数型 | 否 | None | 输出令牌数 |
| 成本 | 双精度数型 | 否 | None | 本次调用成本 |
| 端点 | 文本型 | 否 | None | 端点地址 |
| 备注 | 文本型 | 否 | None | 备注 |
| 记录id | 文本型 | 否 | None | 记录id；不给自动生成，同 id 重复写为更新语义 |
| 数据库路径 | 文本型 | 否 | None | 选填，默认本包专用库 |

**返回结构**：{'类型': '结果型', '值结构': {'已记录': '逻辑型', '记录id': '文本型', '累计条数': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 写入失败 |
| 读取失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.记录模型调用(模型=值, 成功=值, 任务类型=值, 耗时毫秒=值, 输入令牌=值, 输出令牌=值, 成本=值, 端点=值, 备注=值, 记录id=值, 数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.汇总模型表现（汇总模型表现）

**说明**：把用量记录汇总成可比较的表现指标（纯统计，不消耗任何额度）。按任务类型分组，给出样本数/成功率/平均耗时/平均令牌/平均成本，以及是否样本充足（低于最少样本数不给分，如实标注不编造）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 候选清单 | 列表型 | 是 | None | 候选模型名列表，由调用方给 |
| 任务类型 | 文本型 | 否 | None | 不给则汇总全部记录 |
| 最少样本数 | 整数型 | 否 | 3 | 低于此数标样本不足 |
| 数据库路径 | 文本型 | 否 | None | 选填，默认本包专用库 |

**返回结构**：{'类型': '结果型', '值结构': {'汇总': '列表型', '数据条数': '整数型', '说明': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 读取失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.汇总模型表现(候选清单=值, 任务类型=值, 最少样本数=值, 数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）

## 大语言模型支持库.模型连接器.选择模型（选择模型）

**说明**：按历史实测表现把候选模型排序，给出建议与理由（只读建议，不自动切换）。第一层硬门槛永远执行（上下文超限排除、候选端点不可达排除）；第二层统计打分仅在启用智能路由时执行（读本地调用记录、四维加权、不消耗额度）。默认保守模式。底座不做隐私/安全判断（哲学 9.3）；参考路径只是用户自带规则的可选输入，不给则完全不涉及。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 候选清单 | 列表型 | 是 | None | 每项为字符串或 {模型, 上下文长度, 端点}；由调用方给，底座不存模型台账 |
| 任务描述 | 文本型 | 否 | None | 任务描述 |
| 任务类型 | 文本型 | 否 | None | 任务类型 |
| 上下文长度 | 整数型 | 否 | None | 硬门槛：超过候选支持长度即排除 |
| 启用智能路由 | 逻辑型 | 否 | False | 默认假（保守模式：不读数据、不打分、不耗额度） |
| 参考路径 | 文本型 | 否 | None | 用户自带规则文件绝对路径（数据库/文件/文件夹）。不给=完全不涉及；给了只作排序的可选信号，底座不定义「什么是敏感」 |
| 启用额度标注 | 逻辑型 | 否 | True | 默认真：返回里写明本次是否消耗额度 |
| 权重 | 字典型 | 否 | None | 成功率/效率/性价比/匹配度，默认 0.4/0.2/0.3/0.1 |
| 最少样本数 | 整数型 | 否 | 3 | 低于此数不给分 |
| 数据库路径 | 文本型 | 否 | None | 选填，默认本包专用库 |

**返回结构**：{'类型': '结果型', '值结构': {'建议': '列表型', '排除': '列表型', '模式': '文本型', '打分依据': '字典型', '额度说明': '文本型', '诊断': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 读取失败 |

**调用示例**：

```python
大语言模型支持库.模型连接器.选择模型(候选清单=值, 任务描述=值, 任务类型=值, 上下文长度=值, 启用智能路由=值, 参考路径=值, 启用额度标注=值, 权重=值, 最少样本数=值, 数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）、验证场景/模型用量与路由正向HTTP.json（未登记场景id）
