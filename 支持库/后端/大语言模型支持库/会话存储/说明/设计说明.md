# 会话存储 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.大语言模型支持库.会话存储
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 大语言模型支持库.会话存储.连接会话存储 | 连接会话存储 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 连接会话存储能力，返回六位句柄；句柄无人使用超时自动释放 |
| 大语言模型支持库.会话存储.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放会话存储句柄（幂等） |
| 大语言模型支持库.会话存储.创建会话 | 创建会话 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建会话，返回会话id |
| 大语言模型支持库.会话存储.追加消息 | 追加消息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 追加一条消息（事件追加不可变） |
| 大语言模型支持库.会话存储.读取历史 | 读取历史 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 读取会话历史（可分页） |
| 大语言模型支持库.会话存储.受理输入 | 受理输入 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 受理用户输入，落盘返回输入id+受理序（上下对齐第一步） |
| 大语言模型支持库.会话存储.提升消息 | 提升消息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 受理输入提升为可见消息（写消息表+回填提升序） |
| 大语言模型支持库.会话存储.写入压缩结果 | 写入压缩结果 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 压缩后整体写回（事务，带租约防并发压两次） |
| 大语言模型支持库.会话存储.查询会话 | 查询会话 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询会话元信息+消息数 |
| 大语言模型支持库.会话存储.续约句柄 | 续约句柄 | 1.0.0 | {'类型': '结果型', '值结构': {'句柄': '整数型', '已续约': '逻辑型', '超时秒': '整数型'}} | 续约句柄：重置最后活动时间（续租），可选覆盖超时秒 |
| 大语言模型支持库.会话存储.创建子会话 | 创建子会话 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 在父会话下创建子会话（OpenCode session-tree 模式化，parentID 构建会话树） |
| 大语言模型支持库.会话存储.查询子会话树 | 查询子会话树 | 1.0.0 | {'类型': '结果型', '值结构': {}} | BFS 查询父会话全部子孙会话（限深度） |
| 大语言模型支持库.会话存储.统计子会话树 | 统计子会话树 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 统计父会话子孙会话数 |
| 大语言模型支持库.会话存储.重生成会话 | 重生成会话 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 删除截止序号起的消息（Coze RegenerateMessageID 语义：删旧重跑） |
| 大语言模型支持库.会话存储.清空会话消息 | 清空会话消息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 清空会话全部消息（重新开始语义） |

## 大语言模型支持库.会话存储.连接会话存储（连接会话存储）

**说明**：连接会话存储能力，返回六位句柄；句柄无人使用超时自动释放

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 库路径 | 文本型 | 否 | None | SQLite 路径（缺省用默认） |
| 超时秒 | 整数型 | 否 | 1800 | 句柄超时秒数，不申报默认 30 分钟，可续约 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 连接失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.连接会话存储(库路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.释放句柄（释放句柄）

**说明**：释放会话存储句柄（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.会话存储.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.创建会话（创建会话）

**说明**：创建会话，返回会话id

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 用户id | 文本型 | 是 | None | 归属用户 |
| 模型名 | 文本型 | 否 | None | 模型名 |
| 标题 | 文本型 | 否 | None | 标题 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 创建会话失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.创建会话(句柄=值, 用户id=值, 模型名=值, 标题=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.追加消息（追加消息）

**说明**：追加一条消息（事件追加不可变）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 会话id | 文本型 | 是 | None | 会话id（句柄内资源定位） |
| 角色 | 文本型 | 是 | None | 用户/助手/工具/系统/压缩摘要 |
| 内容 | 字典型 | 是 | None | 消息内容 |
| 来源 | 文本型 | 否 | 用户 | 消息来源 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 追加消息失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.追加消息(句柄=值, 会话id=值, 角色=值, 内容=值, 来源=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.读取历史（读取历史）

**说明**：读取会话历史（可分页）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 会话id | 文本型 | 是 | None | 会话id（句柄内资源定位） |
| 上限 | 整数型 | 否 | None | 读取上限 |
| 偏移 | 整数型 | 否 | None | 偏移 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 读取历史失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.读取历史(句柄=值, 会话id=值, 上限=值, 偏移=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.受理输入（受理输入）

**说明**：受理用户输入，落盘返回输入id+受理序（上下对齐第一步）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 会话id | 文本型 | 是 | None | 会话id（句柄内资源定位） |
| 内容 | 字典型 | 是 | None | 用户输入 |
| 交付模式 | 文本型 | 否 | 正常 | 正常/流式/后台 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 受理输入失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.受理输入(句柄=值, 会话id=值, 内容=值, 交付模式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.提升消息（提升消息）

**说明**：受理输入提升为可见消息（写消息表+回填提升序）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 会话id | 文本型 | 是 | None | 会话id（句柄内资源定位） |
| 输入id | 文本型 | 是 | None | 受理返回的输入id |
| 响应内容 | 字典型 | 否 | None | 助手响应内容 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 提升消息失败 |
| 输入不存在 |

**调用示例**：

```python
大语言模型支持库.会话存储.提升消息(句柄=值, 会话id=值, 输入id=值, 响应内容=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.写入压缩结果（写入压缩结果）

**说明**：压缩后整体写回（事务，带租约防并发压两次）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 会话id | 文本型 | 是 | None | 会话id（句柄内资源定位） |
| 压缩后消息列表 | 列表型 | 是 | None | 压缩后消息列表 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 写入压缩结果失败 |
| 会话不存在 |

**调用示例**：

```python
大语言模型支持库.会话存储.写入压缩结果(句柄=值, 会话id=值, 压缩后消息列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.查询会话（查询会话）

**说明**：查询会话元信息+消息数

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄（由 连接会话存储 返回） |
| 会话id | 文本型 | 是 | None | 会话id（句柄内资源定位） |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 查询会话失败 |
| 会话不存在 |

**调用示例**：

```python
大语言模型支持库.会话存储.查询会话(句柄=值, 会话id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.续约句柄（续约句柄）

**说明**：续约句柄：重置最后活动时间（续租），可选覆盖超时秒

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 会话存储连接句柄 |
| 租约秒 | 整数型 | 否 | None | 续约后超时秒数，缺省只重置倒计时 |

**返回结构**：{'类型': '结果型', '值结构': {'句柄': '整数型', '已续约': '逻辑型', '超时秒': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
大语言模型支持库.会话存储.续约句柄(句柄=值, 租约秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.创建子会话（创建子会话）

**说明**：在父会话下创建子会话（OpenCode session-tree 模式化，parentID 构建会话树）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 连接句柄 |
| 父会话id | 文本型 | 是 | None | 父会话id |
| 用户id | 文本型 | 是 | None | 用户id |
| 模型名 | 文本型 | 否 | None | 模型名 |
| 标题 | 文本型 | 否 | None | 标题 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 父会话不存在 |
| 创建子会话失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.创建子会话(句柄=值, 父会话id=值, 用户id=值, 模型名=值, 标题=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.查询子会话树（查询子会话树）

**说明**：BFS 查询父会话全部子孙会话（限深度）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 连接句柄 |
| 父会话id | 文本型 | 是 | None | 父会话id |
| 深度 | 整数型 | 否 | 10 | 深度上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 查询子会话树失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.查询子会话树(句柄=值, 父会话id=值, 深度=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.统计子会话树（统计子会话树）

**说明**：统计父会话子孙会话数

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 连接句柄 |
| 父会话id | 文本型 | 是 | None | 父会话id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 统计子会话树失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.统计子会话树(句柄=值, 父会话id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.重生成会话（重生成会话）

**说明**：删除截止序号起的消息（Coze RegenerateMessageID 语义：删旧重跑）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 连接句柄 |
| 会话id | 文本型 | 是 | None | 会话id |
| 截止序号 | 整数型 | 是 | None | 删除起点 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 消息不存在 |
| 重生成失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.重生成会话(句柄=值, 会话id=值, 截止序号=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.会话存储.清空会话消息（清空会话消息）

**说明**：清空会话全部消息（重新开始语义）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 连接句柄 |
| 会话id | 文本型 | 是 | None | 会话id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 清空失败 |

**调用示例**：

```python
大语言模型支持库.会话存储.清空会话消息(句柄=值, 会话id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）
