# 会话存储（支持库.后端.大语言模型支持库.会话存储）

- 类型：支持库
- 版本：1.0.3
- 说明：会话/消息/输入三表存储，句柄模式（一次传参=句柄，id=句柄内资源），上下对齐（受理序/提升序分离），压缩写回带租约
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 连接会话存储（`大语言模型支持库.会话存储.连接会话存储`）
- 版本：1.0.0
- 一句话说明：连接会话存储能力，返回六位句柄；句柄无人使用超时自动释放
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：库路径:文本型, 超时秒:整数型
- 必填参数：无
- 可选参数：库路径, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 连接失败
- 调用示例：大语言模型支持库.会话存储.连接会话存储(超时秒=1800)
- 依赖能力：无
### 释放句柄（`大语言模型支持库.会话存储.释放句柄`）
- 版本：1.0.0
- 一句话说明：释放会话存储句柄（幂等）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：大语言模型支持库.会话存储.释放句柄()
- 依赖能力：无
### 创建会话（`大语言模型支持库.会话存储.创建会话`）
- 版本：1.0.0
- 一句话说明：创建会话，返回会话id
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 用户id:文本型, 模型名:文本型, 标题:文本型
- 必填参数：句柄, 用户id
- 可选参数：模型名, 标题
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 创建会话失败
- 调用示例：大语言模型支持库.会话存储.创建会话()
- 依赖能力：无
### 追加消息（`大语言模型支持库.会话存储.追加消息`）
- 版本：1.0.0
- 一句话说明：追加一条消息（事件追加不可变）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型, 角色:文本型, 内容:字典型, 来源:文本型
- 必填参数：句柄, 会话id, 角色, 内容
- 可选参数：来源
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 追加消息失败
- 调用示例：大语言模型支持库.会话存储.追加消息(来源=用户)
- 依赖能力：无
### 读取历史（`大语言模型支持库.会话存储.读取历史`）
- 版本：1.0.0
- 一句话说明：读取会话历史（可分页）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型, 上限:整数型, 偏移:整数型
- 必填参数：句柄, 会话id
- 可选参数：上限, 偏移
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 读取历史失败
- 调用示例：大语言模型支持库.会话存储.读取历史()
- 依赖能力：无
### 受理输入（`大语言模型支持库.会话存储.受理输入`）
- 版本：1.0.0
- 一句话说明：受理用户输入，落盘返回输入id+受理序（上下对齐第一步）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型, 内容:字典型, 交付模式:文本型
- 必填参数：句柄, 会话id, 内容
- 可选参数：交付模式
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 受理输入失败
- 调用示例：大语言模型支持库.会话存储.受理输入(交付模式=正常)
- 依赖能力：无
### 提升消息（`大语言模型支持库.会话存储.提升消息`）
- 版本：1.0.0
- 一句话说明：受理输入提升为可见消息（写消息表+回填提升序）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型, 输入id:文本型, 响应内容:字典型
- 必填参数：句柄, 会话id, 输入id
- 可选参数：响应内容
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 提升消息失败, 输入不存在
- 调用示例：大语言模型支持库.会话存储.提升消息()
- 依赖能力：无
### 写入压缩结果（`大语言模型支持库.会话存储.写入压缩结果`）
- 版本：1.0.0
- 一句话说明：压缩后整体写回（事务，带租约防并发压两次）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型, 压缩后消息列表:列表型
- 必填参数：句柄, 会话id, 压缩后消息列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 写入压缩结果失败, 会话不存在
- 调用示例：大语言模型支持库.会话存储.写入压缩结果()
- 依赖能力：无
### 查询会话（`大语言模型支持库.会话存储.查询会话`）
- 版本：1.0.0
- 一句话说明：查询会话元信息+消息数
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型
- 必填参数：句柄, 会话id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 查询会话失败, 会话不存在
- 调用示例：大语言模型支持库.会话存储.查询会话()
- 依赖能力：无
### 续约句柄（`大语言模型支持库.会话存储.续约句柄`）
- 版本：1.0.0
- 一句话说明：续约句柄：重置最后活动时间（续租），可选覆盖超时秒
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 租约秒:整数型
- 必填参数：句柄
- 可选参数：租约秒
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '已续约': '逻辑型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效
- 调用示例：大语言模型支持库.会话存储.续约句柄()
- 依赖能力：无
### 创建子会话（`大语言模型支持库.会话存储.创建子会话`）
- 版本：1.0.0
- 一句话说明：在父会话下创建子会话（OpenCode session-tree 模式化，parentID 构建会话树）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 父会话id:文本型, 用户id:文本型, 模型名:文本型, 标题:文本型
- 必填参数：句柄, 父会话id, 用户id
- 可选参数：模型名, 标题
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 父会话不存在, 创建子会话失败
- 调用示例：大语言模型支持库.会话存储.创建子会话()
- 依赖能力：无
### 查询子会话树（`大语言模型支持库.会话存储.查询子会话树`）
- 版本：1.0.0
- 一句话说明：BFS 查询父会话全部子孙会话（限深度）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 父会话id:文本型, 深度:整数型
- 必填参数：句柄, 父会话id
- 可选参数：深度
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 查询子会话树失败
- 调用示例：大语言模型支持库.会话存储.查询子会话树(深度=10)
- 依赖能力：无
### 统计子会话树（`大语言模型支持库.会话存储.统计子会话树`）
- 版本：1.0.0
- 一句话说明：统计父会话子孙会话数
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 父会话id:文本型
- 必填参数：句柄, 父会话id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 统计子会话树失败
- 调用示例：大语言模型支持库.会话存储.统计子会话树()
- 依赖能力：无
### 重生成会话（`大语言模型支持库.会话存储.重生成会话`）
- 版本：1.0.0
- 一句话说明：删除截止序号起的消息（Coze RegenerateMessageID 语义：删旧重跑）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型, 截止序号:整数型
- 必填参数：句柄, 会话id, 截止序号
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 消息不存在, 重生成失败
- 调用示例：大语言模型支持库.会话存储.重生成会话()
- 依赖能力：无
### 清空会话消息（`大语言模型支持库.会话存储.清空会话消息`）
- 版本：1.0.0
- 一句话说明：清空会话全部消息（重新开始语义）
- 所属支持库或模块：支持库.后端.大语言模型支持库.会话存储
- 参数列表：句柄:整数型, 会话id:文本型
- 必填参数：句柄, 会话id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 清空失败
- 调用示例：大语言模型支持库.会话存储.清空会话消息()
- 依赖能力：无
