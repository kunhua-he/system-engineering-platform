# 大语言模型支持库（支持库.后端.大语言模型支持库）

- 类型：支持库
- 版本：2.0.2
- 说明：聚合支持库，包含 6 个子域：上下文压缩/会话存储/嵌入缓存/模型连接器/流式推理/重排服务
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 估算token数（`大语言模型支持库.上下文压缩.估算token数`）
- 版本：2.0.2
- 一句话说明：估算文本 token 数
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.上下文压缩.估算token数()
- 依赖能力：无
### 估算消息token数（`大语言模型支持库.上下文压缩.估算消息token数`）
- 版本：2.0.2
- 一句话说明：估算消息列表 token 数
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：消息列表:列表型
- 必填参数：消息列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.上下文压缩.估算消息token数()
- 依赖能力：无
### 压缩历史（`大语言模型支持库.上下文压缩.压缩历史`）
- 版本：2.0.2
- 一句话说明：压缩对话历史（额度可配：压缩阈值/摘要长度上限默认不限制）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：消息列表:列表型, 压缩阈值:整数型, 保留最近条数:整数型, 摘要长度上限:整数型, 摘要模式:文本型
- 必填参数：消息列表
- 可选参数：压缩阈值, 保留最近条数, 摘要长度上限, 摘要模式
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.上下文压缩.压缩历史()
- 依赖能力：无
### 四阶段压缩（`大语言模型支持库.上下文压缩.四阶段压缩`）
- 版本：2.0.2
- 一句话说明：四阶段上下文压缩（无LLM剪枝→定边界→结构化摘要→组装；原子分组不拆散工具调用与结果，严格模式失败即降级为确定性摘要）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：消息列表:列表型, 可用额度token:整数型, 保留最近token:整数型, 降级模式:文本型, 原子分组:逻辑型, 结束标记:文本型
- 必填参数：消息列表, 可用额度token
- 可选参数：保留最近token, 降级模式, 原子分组, 结束标记
- 返回结构：{'类型': '结果型', '值结构': {'摘要文本': '文本型', '保留消息': '列表型', '已压缩条数': '整数型', '降级': '逻辑型', '结束标记': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.上下文压缩.四阶段压缩()
- 依赖能力：无
### 连接会话存储（`大语言模型支持库.会话存储.连接会话存储`）
- 版本：2.0.2
- 一句话说明：连接会话存储能力，返回六位句柄；句柄无人使用超时自动释放
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：库路径:文本型, 超时秒:整数型
- 必填参数：无
- 可选参数：库路径, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.连接会话存储()
- 依赖能力：无
### 释放句柄（`大语言模型支持库.会话存储.释放句柄`）
- 版本：2.0.2
- 一句话说明：释放会话存储句柄（幂等）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.释放句柄()
- 依赖能力：无
### 创建会话（`大语言模型支持库.会话存储.创建会话`）
- 版本：2.0.2
- 一句话说明：创建会话，返回会话id
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 用户id:文本型, 模型名:文本型, 标题:文本型
- 必填参数：句柄, 用户id
- 可选参数：模型名, 标题
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.创建会话()
- 依赖能力：无
### 追加消息（`大语言模型支持库.会话存储.追加消息`）
- 版本：2.0.2
- 一句话说明：追加一条消息（事件追加不可变）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型, 角色:文本型, 内容:字典型, 来源:文本型
- 必填参数：句柄, 会话id, 角色, 内容
- 可选参数：来源
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.追加消息()
- 依赖能力：无
### 读取历史（`大语言模型支持库.会话存储.读取历史`）
- 版本：2.0.2
- 一句话说明：读取会话历史（可分页）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型, 上限:整数型, 偏移:整数型
- 必填参数：句柄, 会话id
- 可选参数：上限, 偏移
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.读取历史()
- 依赖能力：无
### 受理输入（`大语言模型支持库.会话存储.受理输入`）
- 版本：2.0.2
- 一句话说明：受理用户输入，落盘返回输入id+受理序（上下对齐第一步）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型, 内容:字典型, 交付模式:文本型
- 必填参数：句柄, 会话id, 内容
- 可选参数：交付模式
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.受理输入()
- 依赖能力：无
### 提升消息（`大语言模型支持库.会话存储.提升消息`）
- 版本：2.0.2
- 一句话说明：受理输入提升为可见消息（写消息表+回填提升序）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型, 输入id:文本型, 响应内容:字典型
- 必填参数：句柄, 会话id, 输入id
- 可选参数：响应内容
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.提升消息()
- 依赖能力：无
### 写入压缩结果（`大语言模型支持库.会话存储.写入压缩结果`）
- 版本：2.0.2
- 一句话说明：压缩后整体写回（事务，带租约防并发压两次）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型, 压缩后消息列表:列表型
- 必填参数：句柄, 会话id, 压缩后消息列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.写入压缩结果()
- 依赖能力：无
### 查询会话（`大语言模型支持库.会话存储.查询会话`）
- 版本：2.0.2
- 一句话说明：查询会话元信息+消息数
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型
- 必填参数：句柄, 会话id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.查询会话()
- 依赖能力：无
### 续约句柄（`大语言模型支持库.会话存储.续约句柄`）
- 版本：2.0.2
- 一句话说明：续约句柄：重置最后活动时间（续租），可选覆盖超时秒
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 租约秒:整数型
- 必填参数：句柄
- 可选参数：租约秒
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '已续约': '逻辑型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.续约句柄()
- 依赖能力：无
### 创建子会话（`大语言模型支持库.会话存储.创建子会话`）
- 版本：2.0.2
- 一句话说明：在父会话下创建子会话（OpenCode session-tree 模式化，parentID 构建会话树）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 父会话id:文本型, 用户id:文本型, 模型名:文本型, 标题:文本型
- 必填参数：句柄, 父会话id, 用户id
- 可选参数：模型名, 标题
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.创建子会话()
- 依赖能力：无
### 查询子会话树（`大语言模型支持库.会话存储.查询子会话树`）
- 版本：2.0.2
- 一句话说明：BFS 查询父会话全部子孙会话（限深度）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 父会话id:文本型, 深度:整数型
- 必填参数：句柄, 父会话id
- 可选参数：深度
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.查询子会话树()
- 依赖能力：无
### 统计子会话树（`大语言模型支持库.会话存储.统计子会话树`）
- 版本：2.0.2
- 一句话说明：统计父会话子孙会话数
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 父会话id:文本型
- 必填参数：句柄, 父会话id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.统计子会话树()
- 依赖能力：无
### 重生成会话（`大语言模型支持库.会话存储.重生成会话`）
- 版本：2.0.2
- 一句话说明：删除截止序号起的消息（Coze RegenerateMessageID 语义：删旧重跑）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型, 截止序号:整数型
- 必填参数：句柄, 会话id, 截止序号
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.重生成会话()
- 依赖能力：无
### 清空会话消息（`大语言模型支持库.会话存储.清空会话消息`）
- 版本：2.0.2
- 一句话说明：清空会话全部消息（重新开始语义）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 会话id:文本型
- 必填参数：句柄, 会话id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.会话存储.清空会话消息()
- 依赖能力：无
### 查询嵌入（`大语言模型支持库.嵌入缓存.查询嵌入`）
- 版本：2.0.2
- 一句话说明：按 文本+模型名+提供者 查询缓存嵌入向量。命中返回 {命中=true, 向量=[...]}；未命中返回 {命中=false, 向量=null}。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：文本:文本型, 模型名:文本型, 提供者:文本型, 数据库路径:文本型
- 必填参数：文本, 模型名, 提供者
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'命中': '逻辑型', '向量': '列表型或空值'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.嵌入缓存.查询嵌入()
- 依赖能力：无
### 存储嵌入（`大语言模型支持库.嵌入缓存.存储嵌入`）
- 版本：2.0.2
- 一句话说明：存储一条嵌入缓存。同 文本+模型名+提供者 已存在则覆盖（向量更新）。返回 {已存储=true, 缓存键=...}。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：文本:文本型, 向量:列表型, 模型名:文本型, 提供者:文本型, 数据库路径:文本型
- 必填参数：文本, 向量, 模型名, 提供者
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'已存储': '逻辑型', '缓存键': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.嵌入缓存.存储嵌入()
- 依赖能力：无
### 统计缓存（`大语言模型支持库.嵌入缓存.统计缓存`）
- 版本：2.0.2
- 一句话说明：返回缓存条目总数。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：数据库路径:文本型
- 必填参数：无
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'缓存数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.嵌入缓存.统计缓存()
- 依赖能力：无
### 清空缓存（`大语言模型支持库.嵌入缓存.清空缓存`）
- 版本：2.0.2
- 一句话说明：清空全部缓存条目（只删缓存表数据，不动业务数据）。返回 {已清空=true}。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：数据库路径:文本型
- 必填参数：无
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'已清空': '逻辑型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.嵌入缓存.清空缓存()
- 依赖能力：无
### 连接LLM（`大语言模型支持库.模型连接器.连接LLM`）
- 版本：2.0.2
- 一句话说明：连接大语言模型，返回句柄。本地：部署形态=本地+本地路径/启动器；云端：部署形态=云端+url/api_key/模型/上下文长度。句柄默认 1800 秒无人使用自动释放；系统内存占用超阈值拒绝新连接。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：模型:文本型, 提供者:文本型, 部署形态:文本型, 本地路径:文本型, 启动器:文本型, 模型大小字节:整数型, url:文本型, api_key:文本型, 上下文长度:整数型, 协议:文本型, 额外请求头:字典型, 超时秒:整数型
- 必填参数：模型
- 可选参数：提供者, 部署形态, 本地路径, 启动器, 模型大小字节, url, api_key, 上下文长度, 协议, 额外请求头, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.连接LLM()
- 依赖能力：无
### 连接向量模型（`大语言模型支持库.模型连接器.连接向量模型`）
- 版本：2.0.2
- 一句话说明：连接向量（嵌入）模型，返回句柄。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：模型:文本型, 提供者:文本型, 部署形态:文本型, 本地路径:文本型, 启动器:文本型, 模型大小字节:整数型, url:文本型, api_key:文本型, 超时秒:整数型
- 必填参数：模型
- 可选参数：提供者, 部署形态, 本地路径, 启动器, 模型大小字节, url, api_key, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.连接向量模型()
- 依赖能力：无
### 连接重排模型（`大语言模型支持库.模型连接器.连接重排模型`）
- 版本：2.0.2
- 一句话说明：连接重排模型，返回句柄。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：模型:文本型, 提供者:文本型, 部署形态:文本型, 本地路径:文本型, 启动器:文本型, 模型大小字节:整数型, url:文本型, api_key:文本型, 超时秒:整数型
- 必填参数：模型
- 可选参数：提供者, 部署形态, 本地路径, 启动器, 模型大小字节, url, api_key, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '连接类型': '文本型', '模型': '文本型', '部署形态': '文本型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.连接重排模型()
- 依赖能力：无
### 启动本地模型（`大语言模型支持库.模型连接器.启动本地模型`）
- 版本：2.0.2
- 一句话说明：拉起本地模型进程（如 llama-server），启动成功后返回句柄；句柄超时/释放时自动终止进程。内存守卫：系统内存占用超阈值拒绝启动。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：模型路径:文本型, 模型类型:文本型, 启动器:文本型, 端口:整数型, 模型大小字节:整数型, 超时秒:整数型, 参数:字典型
- 必填参数：模型路径
- 可选参数：模型类型, 启动器, 端口, 模型大小字节, 超时秒, 参数
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '模型类型': '文本型', '模型路径': '文本型', '端口': '整数型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.启动本地模型()
- 依赖能力：无
### 注册本地进程（`大语言模型支持库.模型连接器.注册本地进程`）
- 版本：2.0.2
- 一句话说明：把真实拉起的子进程绑定到句柄（适配层 Provider 调用，内部能力）。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 进程对象:空值型
- 必填参数：句柄
- 可选参数：进程对象
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '已绑定进程': '逻辑型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.注册本地进程()
- 依赖能力：无
### 生成对话（`大语言模型支持库.模型连接器.生成对话`）
- 版本：2.0.2
- 一句话说明：持 LLM 句柄生成对话回复；流式输出默认 false，当前40007网关未装配模型SSE时返回结构化错误。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 消息列表:列表型, 系统提示词:文本型, 流式输出:逻辑型, 温度:双精度数型, 最大令牌数:整数型, 工具:列表型, 响应格式:字典型, 附加请求头:字典型, chat_template_kwargs:字典型
- 必填参数：句柄, 消息列表
- 可选参数：系统提示词, 流式输出, 温度, 最大令牌数, 工具, 响应格式, 附加请求头, chat_template_kwargs
- 返回结构：{'类型': '结果型', '值结构': {'回复': '文本型', '用量': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.生成对话()
- 依赖能力：无
### 生成嵌入（`大语言模型支持库.模型连接器.生成嵌入`）
- 版本：2.0.2
- 一句话说明：持向量句柄生成嵌入向量。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 文本:文本型
- 必填参数：句柄, 文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'向量': '列表型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.生成嵌入()
- 依赖能力：无
### 执行重排（`大语言模型支持库.模型连接器.执行重排`）
- 版本：2.0.2
- 一句话说明：持重排句柄重排候选文档。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 查询:文本型, 文档列表:列表型
- 必填参数：句柄, 查询, 文档列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'重排结果': '列表型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.执行重排()
- 依赖能力：无
### 续租句柄（`大语言模型支持库.模型连接器.续租句柄`）
- 版本：2.0.2
- 一句话说明：续租句柄：重置最后活动时间，可选更新超时秒。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型, 租约秒:整数型
- 必填参数：句柄
- 可选参数：租约秒
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '已续租': '逻辑型', '超时秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.续租句柄()
- 依赖能力：无
### 释放句柄（`大语言模型支持库.模型连接器.释放句柄`）
- 版本：2.0.2
- 一句话说明：显式释放句柄（幂等，重复释放不报错）；本地模型句柄释放时终止对应进程。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '已释放': '逻辑型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.释放句柄()
- 依赖能力：无
### 查询句柄状态（`大语言模型支持库.模型连接器.查询句柄状态`）
- 版本：2.0.2
- 一句话说明：查询句柄状态：有效/已失效、连接类型、模型、剩余可用秒。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '连接类型': '文本型', '剩余秒': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.查询句柄状态()
- 依赖能力：无
### 资源快照（`大语言模型支持库.模型连接器.资源快照`）
- 版本：2.0.2
- 一句话说明：返回资源快照：系统内存（总量/可用/占用率）+ 活跃连接分布 + 内存安全阈值。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'系统内存': '字典型', '活跃连接': '字典型', '内存安全阈值': '双精度数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.资源快照()
- 依赖能力：无
### 加载环境配置（`大语言模型支持库.模型连接器.加载环境配置`）
- 版本：2.0.2
- 一句话说明：从项目环境文件或进程环境变量读取统一模型参数；连接器缺参时自动使用。返回 {已读取键数, 配置摘要}。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：环境文件路径:文本型
- 必填参数：无
- 可选参数：环境文件路径
- 返回结构：{'类型': '结果型', '值结构': {'已读取键数': '整数型', '配置摘要': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.加载环境配置()
- 依赖能力：无
### 查询环境配置（`大语言模型支持库.模型连接器.查询环境配置`）
- 版本：2.0.2
- 一句话说明：返回当前统一环境参数（不含密钥明文）。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'默认向量模型': '文本型', '默认LLM模型': '文本型', '默认重排模型': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.查询环境配置()
- 依赖能力：无
### 循环软护栏（`大语言模型支持库.模型连接器.循环软护栏`）
- 版本：2.0.2
- 一句话说明：检测重复工具调用并注入软提醒（DeepSeek repeat-tool-reminder 模式化），不硬杀不静默，纠错权交给模型
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：历史调用:列表型, 工具名:文本型, 阈值档位:列表型
- 必填参数：无
- 可选参数：历史调用, 工具名, 阈值档位
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.循环软护栏()
- 依赖能力：无
### 调度工具调用（`大语言模型支持库.模型连接器.调度工具调用`）
- 版本：2.0.2
- 一句话说明：工具并行调度计划（DeepSeek 模式化：exclusive屏障+有界池+结果按model序）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：工具调用列表:列表型, 并行上限:整数型
- 必填参数：工具调用列表
- 可选参数：并行上限
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.调度工具调用()
- 依赖能力：无
### 合成取消结果（`大语言模型支持库.模型连接器.合成取消结果`）
- 版本：2.0.2
- 一句话说明：合成取消/未启动调用的错误结果（错误码 派发前已取消）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：调用id:文本型, 工具名:文本型, 原因:文本型
- 必填参数：调用id
- 可选参数：工具名, 原因
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.合成取消结果()
- 依赖能力：无
### 重试调度（`大语言模型支持库.模型连接器.重试调度`）
- 版本：2.0.2
- 一句话说明：按错误码白名单/兜底模式判定是否重试，并给出带抖动的指数退避秒数。模式1=normal：仅错误码白名单命中才重试（白名单为空即不命中）；模式2=always：忽略白名单无条件兜底重试。第 i 次重试退避 = 退避基数秒 × 2^i × (1 + 抖动比例)，单次上限 300 秒；退避计划写入 原因。纯计算只读，不发起模型/网络/工具调用；可选入参 实际错误码：传入时模式1 按「实际错误码 ∈ 白名单」判定是否命中（不传保持旧行为）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：重试模式:文本型, 错误码白名单:列表型, 实际错误码:文本型, 最大重试次数:整数型, 退避基数秒:双精度数型, 抖动比例:双精度数型
- 必填参数：重试模式
- 可选参数：错误码白名单, 实际错误码, 最大重试次数, 退避基数秒, 抖动比例
- 返回结构：{'类型': '结果型', '值结构': {'是否重试': '逻辑型', '下次退避秒': '双精度数型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.重试调度()
- 依赖能力：无
### 组装可用工具清单（`大语言模型支持库.模型连接器.组装可用工具清单`）
- 版本：2.0.2
- 一句话说明：按门控模式过滤/排序调用方传入的工具清单：模式1=全量（返回全部）；模式2=显式清单（只返回可用清单命中的工具）。按工具名去重（保序）后升序排序，输出 {工具清单, 数量}。不内嵌任何业务角色判断，清单全部由调用方传入。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：门控模式:文本型, 全部工具清单:列表型, 可用工具清单:列表型
- 必填参数：门控模式, 全部工具清单
- 可选参数：可用工具清单
- 返回结构：{'类型': '结果型', '值结构': {'工具清单': '列表型', '数量': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.组装可用工具清单()
- 依赖能力：无
### 归一化对话响应（`大语言模型支持库.模型连接器.归一化对话响应`）
- 版本：2.0.2
- 一句话说明：把模型原始响应归一化为 {内容,思考,工具调用,结束原因,用量}；支持 codex_responses/anthropic/ollama/chat_completions 四种协议
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：原始数据:字典型, 包含思考:逻辑型
- 必填参数：原始数据
- 可选参数：包含思考
- 返回结构：{'类型': '结果型', '值结构': {'内容': '文本型', '思考': '文本型', '工具调用': '列表型', '结束原因': '文本型', '用量': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.归一化对话响应()
- 依赖能力：无
### 归一化流式分块（`大语言模型支持库.模型连接器.归一化流式分块`）
- 版本：2.0.2
- 一句话说明：把模型流式单块归一化为 {类型,内容,工具调用,用量}；结构事件返回 类型=跳过
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：分块:字典型, 包含思考:逻辑型
- 必填参数：分块
- 可选参数：包含思考
- 返回结构：{'类型': '结果型', '值结构': {'类型': '文本型', '内容': '文本型', '工具调用': '列表型', '用量': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.模型连接器.归一化流式分块()
- 依赖能力：无
### 提交流式请求（`大语言模型支持库.流式推理.提交流式请求`）
- 版本：2.0.2
- 一句话说明：提交流式推理请求，返回流id（SSE 事件流句柄）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：模型连接句柄:整数型, 消息列表:列表型, 系统提示词:文本型
- 必填参数：模型连接句柄, 消息列表
- 可选参数：系统提示词
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.流式推理.提交流式请求()
- 依赖能力：无
### 消费事件（`大语言模型支持库.流式推理.消费事件`）
- 版本：2.0.2
- 一句话说明：消费流式事件（增量/完成/错误）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：流id:文本型, 批次大小:整数型
- 必填参数：流id
- 可选参数：批次大小
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.流式推理.消费事件()
- 依赖能力：无
### 关闭流（`大语言模型支持库.流式推理.关闭流`）
- 版本：2.0.2
- 一句话说明：关闭流式推理（幂等）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：流id:文本型
- 必填参数：流id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.流式推理.关闭流()
- 依赖能力：无
### 格式化事件帧（`大语言模型支持库.流式推理.格式化事件帧`）
- 版本：2.0.2
- 一句话说明：格式化 SSE 事件帧（Coze 事件名协议：ack/增量/完成/错误+[DONE]，含X-Accel-Buffering头建议）
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：事件名:文本型, 数据:字典型, 序号:整数型, 带缓冲头:逻辑型
- 必填参数：事件名
- 可选参数：数据, 序号, 带缓冲头
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.流式推理.格式化事件帧()
- 依赖能力：无
### 重排（`大语言模型支持库.重排服务.重排`）
- 版本：2.0.2
- 一句话说明：对候选文档按与查询的相关性重排，返回 文档+得分+原索引。模型配置 预留模型重排（当前标准库实现）。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：查询:文本型, 文档列表:列表型, 模型配置:字典型, 顶部数量:整数型
- 必填参数：查询, 文档列表
- 可选参数：模型配置, 顶部数量
- 返回结构：{'类型': '结果型', '值结构': {'重排结果': '列表型（文档/得分/原索引）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.重排服务.重排()
- 依赖能力：无
### 检查可用性（`大语言模型支持库.重排服务.检查可用性`）
- 版本：2.0.2
- 一句话说明：返回当前实现可用性（默认标准库始终可用，模型配置仅作声明）。
- 所属支持库或模块：支持库.后端.大语言模型支持库
- 参数列表：模型配置:字典型
- 必填参数：无
- 可选参数：模型配置
- 返回结构：{'类型': '结果型', '值结构': {'可用': '逻辑型', '实现': '文本型', '模型': '文本型', '提供者': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：大语言模型支持库.重排服务.检查可用性()
- 依赖能力：无
