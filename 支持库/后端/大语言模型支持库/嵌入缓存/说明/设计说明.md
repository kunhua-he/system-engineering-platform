# 嵌入缓存 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.大语言模型支持库.嵌入缓存
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 大语言模型支持库.嵌入缓存.查询嵌入 | 查询嵌入 | 1.0.0 | {'类型': '结果型', '值结构': {'命中': '逻辑型', '向量': '列表型或空值'}} | 按 文本+模型名+提供者 查询缓存嵌入向量。命中返回 {命中=true, 向量=[...]}；未命中返回 {命中=false, 向量=null}。 |
| 大语言模型支持库.嵌入缓存.存储嵌入 | 存储嵌入 | 1.0.0 | {'类型': '结果型', '值结构': {'已存储': '逻辑型', '缓存键': '文本型'}} | 存储一条嵌入缓存。同 文本+模型名+提供者 已存在则覆盖（向量更新）。返回 {已存储=true, 缓存键=...}。 |
| 大语言模型支持库.嵌入缓存.统计缓存 | 统计缓存 | 1.0.0 | {'类型': '结果型', '值结构': {'缓存数': '整数型'}} | 返回缓存条目总数。 |
| 大语言模型支持库.嵌入缓存.清空缓存 | 清空缓存 | 1.0.0 | {'类型': '结果型', '值结构': {'已清空': '逻辑型'}} | 清空全部缓存条目（只删缓存表数据，不动业务数据）。返回 {已清空=true}。 |

## 大语言模型支持库.嵌入缓存.查询嵌入（查询嵌入）

**说明**：按 文本+模型名+提供者 查询缓存嵌入向量。命中返回 {命中=true, 向量=[...]}；未命中返回 {命中=false, 向量=null}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 原始文本 |
| 模型名 | 文本型 | 是 | None | 嵌入模型名 |
| 提供者 | 文本型 | 是 | None | 提供者标识 |
| 数据库路径 | 文本型 | 否 | None | 自定义缓存库路径 |

**返回结构**：{'类型': '结果型', '值结构': {'命中': '逻辑型', '向量': '列表型或空值'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 缓存查询失败 |

**调用示例**：

```python
大语言模型支持库.嵌入缓存.查询嵌入(文本=值, 模型名=值, 提供者=值, 数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 大语言模型支持库.嵌入缓存.存储嵌入（存储嵌入）

**说明**：存储一条嵌入缓存。同 文本+模型名+提供者 已存在则覆盖（向量更新）。返回 {已存储=true, 缓存键=...}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 原始文本 |
| 向量 | 列表型 | 是 | None | 嵌入向量 |
| 模型名 | 文本型 | 是 | None | 嵌入模型名 |
| 提供者 | 文本型 | 是 | None | 提供者标识 |
| 数据库路径 | 文本型 | 否 | None | 自定义缓存库路径 |

**返回结构**：{'类型': '结果型', '值结构': {'已存储': '逻辑型', '缓存键': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 缓存写入失败 |

**调用示例**：

```python
大语言模型支持库.嵌入缓存.存储嵌入(文本=值, 向量=值, 模型名=值, 提供者=值, 数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 大语言模型支持库.嵌入缓存.统计缓存（统计缓存）

**说明**：返回缓存条目总数。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数据库路径 | 文本型 | 否 | None | 自定义缓存库路径 |

**返回结构**：{'类型': '结果型', '值结构': {'缓存数': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 缓存查询失败 |

**调用示例**：

```python
大语言模型支持库.嵌入缓存.统计缓存(数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 大语言模型支持库.嵌入缓存.清空缓存（清空缓存）

**说明**：清空全部缓存条目（只删缓存表数据，不动业务数据）。返回 {已清空=true}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数据库路径 | 文本型 | 否 | None | 自定义缓存库路径 |

**返回结构**：{'类型': '结果型', '值结构': {'已清空': '逻辑型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 缓存写入失败 |

**调用示例**：

```python
大语言模型支持库.嵌入缓存.清空缓存(数据库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
