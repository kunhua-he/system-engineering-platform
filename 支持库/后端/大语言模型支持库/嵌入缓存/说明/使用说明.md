<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 嵌入缓存（支持库.后端.大语言模型支持库.嵌入缓存）

- 类型：支持库
- 版本：1.0.0
- 说明：文本嵌入向量缓存：按 文本哈希+模型名+提供者 缓存与读取嵌入向量。只负责缓存读写，不调用嵌入模型；嵌入生成由调用方/提供者完成。存储为 SQLite（默认 工程缓存/大语言模型支持库.嵌入缓存.db）。
- 入口：__init__.py

## 能力
### 查询嵌入（`大语言模型支持库.嵌入缓存.查询嵌入`）
- 版本：1.0.0
- 一句话说明：按 文本+模型名+提供者 查询缓存嵌入向量。命中返回 {命中=true, 向量=[...]}；未命中返回 {命中=false, 向量=null}。
- 所属支持库或模块：支持库.后端.大语言模型支持库.嵌入缓存
- 参数列表：文本:文本型, 模型名:文本型, 提供者:文本型, 数据库路径:文本型
- 必填参数：文本, 模型名, 提供者
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'命中': '逻辑型', '向量': '列表型或空值'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 缓存查询失败
- 调用示例：大语言模型支持库.嵌入缓存.查询嵌入()
- 依赖能力：无
### 存储嵌入（`大语言模型支持库.嵌入缓存.存储嵌入`）
- 版本：1.0.0
- 一句话说明：存储一条嵌入缓存。同 文本+模型名+提供者 已存在则覆盖（向量更新）。返回 {已存储=true, 缓存键=...}。
- 所属支持库或模块：支持库.后端.大语言模型支持库.嵌入缓存
- 参数列表：文本:文本型, 向量:列表型, 模型名:文本型, 提供者:文本型, 数据库路径:文本型
- 必填参数：文本, 向量, 模型名, 提供者
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'已存储': '逻辑型', '缓存键': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 缓存写入失败
- 调用示例：大语言模型支持库.嵌入缓存.存储嵌入()
- 依赖能力：无
### 统计缓存（`大语言模型支持库.嵌入缓存.统计缓存`）
- 版本：1.0.0
- 一句话说明：返回缓存条目总数。
- 所属支持库或模块：支持库.后端.大语言模型支持库.嵌入缓存
- 参数列表：数据库路径:文本型
- 必填参数：无
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'缓存数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：缓存查询失败
- 调用示例：大语言模型支持库.嵌入缓存.统计缓存()
- 依赖能力：无
### 清空缓存（`大语言模型支持库.嵌入缓存.清空缓存`）
- 版本：1.0.0
- 一句话说明：清空全部缓存条目（只删缓存表数据，不动业务数据）。返回 {已清空=true}。
- 所属支持库或模块：支持库.后端.大语言模型支持库.嵌入缓存
- 参数列表：数据库路径:文本型
- 必填参数：无
- 可选参数：数据库路径
- 返回结构：{'类型': '结果型', '值结构': {'已清空': '逻辑型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：缓存写入失败
- 调用示例：大语言模型支持库.嵌入缓存.清空缓存()
- 依赖能力：无
