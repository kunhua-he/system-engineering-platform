# 流式推理（支持库.后端.大语言模型支持库.流式推理）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.2
- 说明：流式推理支持库
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 提交流式请求（`大语言模型支持库.流式推理.提交流式请求`）
- 版本：1.0.0
- 一句话说明：提交流式推理请求，返回流id（SSE 事件流句柄）
- 所属支持库或模块：支持库.后端.大语言模型支持库.流式推理
- 参数列表：模型连接句柄:整数型, 消息列表:列表型, 系统提示词:文本型
- 必填参数：模型连接句柄, 消息列表
- 可选参数：系统提示词
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效
- 调用示例：大语言模型支持库.流式推理.提交流式请求()
- 依赖能力：无
### 消费事件（`大语言模型支持库.流式推理.消费事件`）
- 版本：1.0.0
- 一句话说明：消费流式事件（增量/完成/错误）
- 所属支持库或模块：支持库.后端.大语言模型支持库.流式推理
- 参数列表：流id:文本型, 批次大小:整数型
- 必填参数：流id
- 可选参数：批次大小
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 流不存在
- 调用示例：大语言模型支持库.流式推理.消费事件(批次大小=10)
- 依赖能力：无
### 关闭流（`大语言模型支持库.流式推理.关闭流`）
- 版本：1.0.0
- 一句话说明：关闭流式推理（幂等）
- 所属支持库或模块：支持库.后端.大语言模型支持库.流式推理
- 参数列表：流id:文本型
- 必填参数：流id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：大语言模型支持库.流式推理.关闭流()
- 依赖能力：无
### 格式化事件帧（`大语言模型支持库.流式推理.格式化事件帧`）
- 版本：1.0.0
- 一句话说明：格式化 SSE 事件帧（Coze 事件名协议：ack/增量/完成/错误+[DONE]，含X-Accel-Buffering头建议）
- 所属支持库或模块：支持库.后端.大语言模型支持库.流式推理
- 参数列表：事件名:文本型, 数据:字典型, 序号:整数型, 带缓冲头:逻辑型
- 必填参数：事件名
- 可选参数：数据, 序号, 带缓冲头
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 格式化失败
- 调用示例：大语言模型支持库.流式推理.格式化事件帧(序号=0, 带缓冲头=True)
- 依赖能力：无
