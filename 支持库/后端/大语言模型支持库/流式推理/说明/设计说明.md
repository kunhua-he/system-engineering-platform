# 流式推理 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.大语言模型支持库.流式推理
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 大语言模型支持库.流式推理.提交流式请求 | 提交流式请求 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 提交流式推理请求，返回流id（SSE 事件流句柄） |
| 大语言模型支持库.流式推理.消费事件 | 消费事件 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 消费流式事件（增量/完成/错误） |
| 大语言模型支持库.流式推理.关闭流 | 关闭流 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 关闭流式推理（幂等） |
| 大语言模型支持库.流式推理.格式化事件帧 | 格式化事件帧 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 格式化 SSE 事件帧（Coze 事件名协议：ack/增量/完成/错误+[DONE]，含X-Accel-Buffering头建议） |

## 大语言模型支持库.流式推理.提交流式请求（提交流式请求）

**说明**：提交流式推理请求，返回流id（SSE 事件流句柄）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模型连接句柄 | 整数型 | 是 | None | 模型连接句柄 |
| 消息列表 | 列表型 | 是 | None | 消息列表 |
| 系统提示词 | 文本型 | 否 | None | 系统提示词 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
大语言模型支持库.流式推理.提交流式请求(模型连接句柄=值, 消息列表=值, 系统提示词=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.流式推理.消费事件（消费事件）

**说明**：消费流式事件（增量/完成/错误）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 流id | 文本型 | 是 | None | 流id |
| 批次大小 | 整数型 | 否 | 10 | 批次大小 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 流不存在 |

**调用示例**：

```python
大语言模型支持库.流式推理.消费事件(流id=值, 批次大小=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.流式推理.关闭流（关闭流）

**说明**：关闭流式推理（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 流id | 文本型 | 是 | None | 流id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
大语言模型支持库.流式推理.关闭流(流id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）

## 大语言模型支持库.流式推理.格式化事件帧（格式化事件帧）

**说明**：格式化 SSE 事件帧（Coze 事件名协议：ack/增量/完成/错误+[DONE]，含X-Accel-Buffering头建议）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 事件名 | 文本型 | 是 | None | ack/增量/完成/错误 |
| 数据 | 字典型 | 否 | None | 事件数据 |
| 序号 | 整数型 | 否 | 0 | 事件序号 |
| 带缓冲头 | 逻辑型 | 否 | True | SSE响应头 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 格式化失败 |

**调用示例**：

```python
大语言模型支持库.流式推理.格式化事件帧(事件名=值, 数据=值, 序号=值, 带缓冲头=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（包级场景文件）
