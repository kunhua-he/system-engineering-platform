<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 重排服务（支持库.后端.大语言模型支持库.重排服务）

- 类型：支持库
- 版本：1.0.0
- 说明：候选文档相关性重排。只依赖 Python 标准库；模型重排提供者可插拔。
- 入口：__init__.py

## 能力
### 重排（`大语言模型支持库.重排服务.重排`）
- 版本：1.0.0
- 一句话说明：按查询相关性重排候选文档
- 所属支持库或模块：支持库.后端.大语言模型支持库.重排服务
- 参数列表：查询:文本型, 文档列表:列表型, 模型配置:字典型, 顶部数量:整数型
- 必填参数：查询, 文档列表
- 可选参数：模型配置, 顶部数量
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 重排失败
- 调用示例：大语言模型支持库.重排服务.重排()
- 依赖能力：无
### 检查可用性（`大语言模型支持库.重排服务.检查可用性`）
- 版本：1.0.0
- 一句话说明：返回实现可用性
- 所属支持库或模块：支持库.后端.大语言模型支持库.重排服务
- 参数列表：模型配置:字典型
- 必填参数：无
- 可选参数：模型配置
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：大语言模型支持库.重排服务.检查可用性()
- 依赖能力：无
