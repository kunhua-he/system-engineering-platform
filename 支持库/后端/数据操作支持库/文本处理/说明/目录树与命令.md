# 文本处理 · 目录树与命令

> 包id：`支持库.后端.数据操作支持库.文本处理`；版本：`1.1.2`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 目录树

```text
文本处理/
├── __init__.py
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/文本处理.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
├── 默认配置/默认配置.json
```

## 公开命令

### `文本处理.分割文本`

- 名称：分割文本
- 返回：`结果型`
- 说明：按分隔符分割文本返回片段列表
- 参数：
  - `文本型`；文本型；必填；要分割的文本
  - `分隔符`；文本型；必填；分隔符
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.合并文本`

- 名称：合并文本
- 返回：`结果型`
- 说明：用分隔符连接片段列表
- 参数：
  - `片段列表`；列表型；必填；片段列表
  - `分隔符`；文本型；必填；连接分隔符
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.替换文本`

- 名称：替换文本
- 返回：`结果型`
- 说明：把文本中所有旧文本替换为新文本
- 参数：
  - `文本型`；文本型；必填；源文本
  - `旧文本`；文本型；必填；要替换的旧文本
  - `新文本`；文本型；必填；替换后的新文本
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.查找文本`

- 名称：查找文本
- 返回：`结果型`
- 说明：返回目标在文本中的首个位置（未找到返回 -1）
- 参数：
  - `文本型`；文本型；必填；源文本
  - `目标`；文本型；必填；要查找的目标
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.去空白`

- 名称：去空白
- 返回：`结果型`
- 说明：去除文本首尾空白
- 参数：
  - `文本型`；文本型；必填；源文本
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.转大写`

- 名称：转大写
- 返回：`结果型`
- 说明：把文本转为大写
- 参数：
  - `文本型`；文本型；必填；源文本
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.转小写`

- 名称：转小写
- 返回：`结果型`
- 说明：把文本转为小写
- 参数：
  - `文本型`；文本型；必填；源文本
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.统计长度`

- 名称：统计长度
- 返回：`结果型`
- 说明：统计文本字符长度
- 参数：
  - `文本型`；文本型；必填；源文本
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文本处理.按行分割`

- 名称：按行分割
- 返回：`结果型`
- 说明：按换行符分割文本为行列表
- 参数：
  - `文本型`；文本型；必填；源文本
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
