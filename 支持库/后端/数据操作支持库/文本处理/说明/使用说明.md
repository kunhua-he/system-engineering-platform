# 文本处理 使用说明

- 包id：支持库.后端.数据操作支持库.文本处理
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.文本处理.分割文本 | 分割文本 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '分割后的片段列表'}} | 按分隔符分割文本，返回 {值=片段列表} |
| 数据操作支持库.文本处理.合并文本 | 合并文本 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '合并后的文本'}} | 用分隔符连接片段列表，返回 {值=合并后的文本} |
| 数据操作支持库.文本处理.替换文本 | 替换文本 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '替换后的文本'}} | 把文本中所有旧文本替换为新文本，返回 {值=替换后的文本} |
| 数据操作支持库.文本处理.查找文本 | 查找文本 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '首个位置（未找到为 -1）'}} | 返回目标在文本中的首个位置（未找到返回 -1），返回 {值=位置} |
| 数据操作支持库.文本处理.去空白 | 去空白 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '去空白后的文本'}} | 去除文本首尾空白，返回 {值=去空白后的文本} |
| 数据操作支持库.文本处理.转大写 | 转大写 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '大写文本'}} | 把文本转为大写，返回 {值=大写文本} |
| 数据操作支持库.文本处理.转小写 | 转小写 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '小写文本'}} | 把文本转为小写，返回 {值=小写文本} |
| 数据操作支持库.文本处理.统计长度 | 统计长度 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '字符长度（整数）'}} | 统计文本字符长度，返回 {值=长度} |
| 数据操作支持库.文本处理.按行分割 | 按行分割 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '行列表'}} | 按换行符分割文本为行列表，返回 {值=行列表} |
| 数据操作支持库.文本处理.解析区间 | 解析区间 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '集合模式 {数值列表,数量,是否有效}；范围模式 {起始,结束,是否有效}'}} | 解析 "1-5,7,10" 形式的页码/行号区间文本；集合模式返回去重升序列表，范围模式返回起止 |

## 数据操作支持库.文本处理.分割文本（分割文本）

**说明**：按分隔符分割文本，返回 {值=片段列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 要分割的文本 |
| 分隔符 | 文本型 | 是 | None | 分隔符 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '分割后的片段列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.分割文本(文本=值, 分隔符=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.合并文本（合并文本）

**说明**：用分隔符连接片段列表，返回 {值=合并后的文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 片段列表 | 列表型 | 是 | None | 片段列表 |
| 分隔符 | 文本型 | 是 | None | 连接分隔符 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '合并后的文本'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.合并文本(片段列表=值, 分隔符=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.替换文本（替换文本）

**说明**：把文本中所有旧文本替换为新文本，返回 {值=替换后的文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |
| 旧文本 | 文本型 | 是 | None | 要替换的旧文本（不能为空） |
| 新文本 | 文本型 | 是 | None | 替换后的新文本 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '替换后的文本'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.替换文本(文本=值, 旧文本=值, 新文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.查找文本（查找文本）

**说明**：返回目标在文本中的首个位置（未找到返回 -1），返回 {值=位置}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |
| 目标 | 文本型 | 是 | None | 要查找的目标 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '首个位置（未找到为 -1）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.查找文本(文本=值, 目标=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.去空白（去空白）

**说明**：去除文本首尾空白，返回 {值=去空白后的文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '去空白后的文本'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.去空白(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.转大写（转大写）

**说明**：把文本转为大写，返回 {值=大写文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '大写文本'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.转大写(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.转小写（转小写）

**说明**：把文本转为小写，返回 {值=小写文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '小写文本'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.转小写(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.统计长度（统计长度）

**说明**：统计文本字符长度，返回 {值=长度}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '字符长度（整数）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.统计长度(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.按行分割（按行分割）

**说明**：按换行符分割文本为行列表，返回 {值=行列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 源文本 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '行列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.按行分割(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.文本处理.解析区间（解析区间）

**说明**：解析 "1-5,7,10" 形式的页码/行号区间文本；集合模式返回去重升序列表，范围模式返回起止

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 否 | "" | 如 1-5,7,10 |
| 模式 | 文本型 | 否 | 集合 | 集合 或 范围 |

**返回结构**：集合模式 值={'数值列表': [...], '数量': N, '是否有效': bool}；范围模式 值={'起始': int|None, '结束': int|None, '是否有效': bool}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.文本处理.解析区间(文本="1-5,7,10", 模式="集合")
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：文本处理.解析区间正向
