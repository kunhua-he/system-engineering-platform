"""时间日期支持库包级中文入口。

调用者只能从此入口导入，禁止深入 实现/ 目录。
"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.数据操作支持库.时间日期.实现.时间日期 import 获取当前时间
from 支持库.后端.数据操作支持库.时间日期.实现.时间日期 import 格式化为文本
from 支持库.后端.数据操作支持库.时间日期.实现.时间日期 import 解析文本时间
from 支持库.后端.数据操作支持库.时间日期.实现.时间日期 import 时间戳转换
from 支持库.后端.数据操作支持库.时间日期.实现.时间日期 import 计算间隔
from 支持库.后端.数据操作支持库.时间日期.实现.时间日期 import 注入时钟, 恢复默认时钟

__all__ = [
    "获取当前时间",
    "格式化为文本",
    "解析文本时间",
    "时间戳转换",
    "计算间隔",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现

    for 能力id, 函数, 参数表, 返回类型 in [
        ("数据操作支持库.时间日期.获取当前时间", 获取当前时间, [{"名称": "时区", "类型": "文本型", "必填": 假}], "结果"),
        ("数据操作支持库.时间日期.格式化为文本", 格式化为文本, [{"名称": "时间戳", "类型": "双精度数型", "必填": 真}, {"名称": "格式", "类型": "文本型", "必填": 假}], "结果"),
        ("数据操作支持库.时间日期.解析文本时间", 解析文本时间, [{"名称": "文本", "类型": "文本型", "必填": 真}, {"名称": "格式", "类型": "文本型", "必填": 假}], "结果"),
        ("数据操作支持库.时间日期.时间戳转换", 时间戳转换, [{"名称": "时间戳", "类型": "双精度数型", "必填": 真}], "结果"),
        ("数据操作支持库.时间日期.计算间隔", 计算间隔, [{"名称": "起始时间戳", "类型": "双精度数型", "必填": 真}, {"名称": "结束时间戳", "类型": "双精度数型", "必填": 真}], "结果"),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.数据操作支持库.时间日期",
                实现函数=函数,
                参数=参数表,
                返回=返回类型,
                说明="时间日期原子能力",
            )
        )
