# 时间日期 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.数据操作支持库.时间日期
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.时间日期.获取当前时间 | 获取当前时间 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '当前时间 ISO 格式文本（含 T，秒精度）'}} | 返回当前时间的 ISO 格式文本（默认 Asia/Shanghai 时区），返回 {值=ISO时间文本} |
| 数据操作支持库.时间日期.格式化为文本 | 格式化为文本 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '格式化后的时间文本'}} | 把时间戳按格式化为文本（Asia/Shanghai 时区），返回 {值=格式化文本} |
| 数据操作支持库.时间日期.解析文本时间 | 解析文本时间 | 1.0.0 | {'类型': '结果型', '值结构': {'值': 'Unix 时间戳（浮点数，秒）'}} | 按格式解析时间文本为时间戳（Asia/Shanghai 时区），返回 {值=时间戳} |
| 数据操作支持库.时间日期.时间戳转换 | 时间戳转换 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '时间结构 {年, 月, 日, 时, 分, 秒}'}} | 把时间戳转换为年月日时分秒结构（Asia/Shanghai 时区），返回 {值=时间结构} |
| 数据操作支持库.时间日期.计算间隔 | 计算间隔 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '间隔秒数（浮点数）'}} | 计算两个时间戳的间隔秒数（保留 3 位小数），返回 {值=间隔秒数} |

## 数据操作支持库.时间日期.获取当前时间（获取当前时间）

**说明**：返回当前时间的 ISO 格式文本（默认 Asia/Shanghai 时区），返回 {值=ISO时间文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 时区 | 文本型 | 否 | Asia/Shanghai | IANA 时区名（默认 Asia/Shanghai；未知时区返回 参数不合法） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '当前时间 ISO 格式文本（含 T，秒精度）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.时间日期.获取当前时间(时区=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.时间日期.格式化为文本（格式化为文本）

**说明**：把时间戳按格式化为文本（Asia/Shanghai 时区），返回 {值=格式化文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 时间戳 | 双精度数型 | 是 | None | Unix 时间戳（秒）；非法时间戳返回 参数不合法 |
| 格式 | 文本型 | 否 | %Y-%m-%d %H:%M:%S | strftime 格式（默认 %Y-%m-%d %H:%M:%S） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '格式化后的时间文本'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.时间日期.格式化为文本(时间戳=值, 格式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.时间日期.解析文本时间（解析文本时间）

**说明**：按格式解析时间文本为时间戳（Asia/Shanghai 时区），返回 {值=时间戳}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 时间文本；格式不匹配返回 参数不合法 |
| 格式 | 文本型 | 否 | %Y-%m-%d %H:%M:%S | strptime 格式（默认 %Y-%m-%d %H:%M:%S） |

**返回结构**：{'类型': '结果型', '值结构': {'值': 'Unix 时间戳（浮点数，秒）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.时间日期.解析文本时间(文本=值, 格式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.时间日期.时间戳转换（时间戳转换）

**说明**：把时间戳转换为年月日时分秒结构（Asia/Shanghai 时区），返回 {值=时间结构}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 时间戳 | 双精度数型 | 是 | None | Unix 时间戳（秒）；非法时间戳返回 参数不合法 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '时间结构 {年, 月, 日, 时, 分, 秒}'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.时间日期.时间戳转换(时间戳=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.时间日期.计算间隔（计算间隔）

**说明**：计算两个时间戳的间隔秒数（保留 3 位小数），返回 {值=间隔秒数}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 起始时间戳 | 双精度数型 | 是 | None | 起始时间戳（秒） |
| 结束时间戳 | 双精度数型 | 是 | None | 结束时间戳（秒） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '间隔秒数（浮点数）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.时间日期.计算间隔(起始时间戳=值, 结束时间戳=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
