# 响应塑形 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.数据操作支持库.响应塑形
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.响应塑形.裁剪响应 | 裁剪响应 | 1.0.0 | 结果型 | 按路径/条目数/字节数裁剪结构化响应；信封键、元数据键与数据键均可参数化 |

## 数据操作支持库.响应塑形.裁剪响应（裁剪响应）

**说明**：按路径/条目数/字节数裁剪结构化响应；信封键、元数据键与数据键均可参数化

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 载荷 | 字典型 | 是 | None | 待塑形的响应载荷 |
| 选择路径 | 文本型 | 否 |  | 点分路径，如 data.items.0 |
| 最大条目数 | 整数型 | 否 | None | 每个列表保留的条目上限 |
| 最大字节数 | 整数型 | 否 | None | 序列化后的字节上限 |
| 信封键 | 列表型 | 否 | None | 命中路径时保留的信封字段 |
| 元数据键 | 文本型 | 否 | response_meta | 元数据字段名 |
| 数据键 | 文本型 | 否 | data | 承载数据的字段名 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
数据操作支持库.响应塑形.裁剪响应(载荷=值, 选择路径=值, 最大条目数=值, 最大字节数=值, 信封键=值, 元数据键=值, 数据键=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：未纳入验证场景（暂无验证场景引用）
