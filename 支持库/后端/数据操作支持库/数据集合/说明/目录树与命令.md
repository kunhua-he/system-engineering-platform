# 数据集合 · 目录树与命令

> 包id：`支持库.后端.数据操作支持库.数据集合`；版本：`1.2.4`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。

## 目录树

```text
数据集合/
├── __init__.py
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/数据集合.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
├── 默认配置/默认配置.json
```

## 公开命令

### `数据集合.列表添加`

- 名称：列表添加
- 返回：`结果型`
- 说明：追加条目并返回新列表
- 参数：
  - `列表型`；列表型；必填；源列表
  - `条目`；文本型；必填；要追加的条目
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.列表移除`

- 名称：列表移除
- 返回：`结果型`
- 说明：移除首个匹配条目并返回新列表
- 参数：
  - `列表型`；列表型；必填；源列表
  - `条目`；文本型；必填；要移除的条目
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.列表排序`

- 名称：列表排序
- 返回：`结果型`
- 说明：排序列表并返回新列表
- 参数：
  - `列表型`；列表型；必填；源列表（元素须可比较）
  - `倒序`；逻辑型；可选；默认=False；是否倒序
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.列表查找`

- 名称：列表查找
- 返回：`结果型`
- 说明：返回首个匹配条目索引（未找到返回 -1）
- 参数：
  - `列表型`；列表型；必填；源列表
  - `目标`；文本型；必填；要查找的条目
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.字典设置`

- 名称：字典设置
- 返回：`结果型`
- 说明：设置键值并返回新字典
- 参数：
  - `字典型`；字典型；必填；源字典
  - `键`；文本型；必填；键名
  - `值`；文本型；必填；键值
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.字典获取`

- 名称：字典获取
- 返回：`结果型`
- 说明：获取键值（键不存在返回默认值）
- 参数：
  - `字典型`；字典型；必填；源字典
  - `键`；文本型；必填；键名
  - `默认值`；文本型；可选；键不存在时的返回值
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.集合取并集`

- 名称：集合取并集
- 返回：`结果型`
- 说明：返回两个列表的去重并集
- 参数：
  - `集合1`；列表型；必填；集合1（列表）
  - `集合2`；列表型；必填；集合2（列表）
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.集合取交集`

- 名称：集合取交集
- 返回：`结果型`
- 说明：返回两个列表的去重交集
- 参数：
  - `集合1`；列表型；必填；集合1（列表）
  - `集合2`；列表型；必填；集合2（列表）
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.统计数量`

- 名称：统计数量
- 返回：`结果型`
- 说明：返回列表条目数量
- 参数：
  - `列表型`；列表型；必填；源列表
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.按字段查找`

- 名称：按字段查找
- 返回：`结果型`
- 说明：按字段查找第一个匹配项
- 参数：
  - `列表型`；列表型；必填；字典型项目列表
  - `字段名`；文本型；必填；要匹配的字段名
  - `匹配值`；文本型；必填；期望的字段值
  - `直接匹配`；逻辑型；可选；默认=False；为真时直接比较元素
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.过滤`

- 名称：过滤
- 返回：`结果型`
- 说明：按字段过滤返回新列表
- 参数：
  - `列表型`；列表型；必填；字典型项目列表
  - `字段名`；文本型；必填；要匹配的字段名
  - `等于值`；文本型；必填；期望的字段值
  - `直接匹配`；逻辑型；可选；默认=False；为真时直接比较元素
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.映射转换`

- 名称：映射转换
- 返回：`结果型`
- 说明：按字段映射重命名项目字段并返回新列表
- 参数：
  - `列表型`；列表型；必填；字典型项目列表
  - `字段映射`；字典型；必填；原字段到新字段的映射
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `数据集合.按字段排序`

- 名称：按字段排序
- 返回：`结果型`
- 说明：按字段或元素排序并返回新列表
- 参数：
  - `列表型`；列表型；必填；字典型项目列表
  - `字段名`；文本型；必填；排序字段名
  - `升序`；逻辑型；可选；默认=True；是否升序
  - `直接匹配`；逻辑型；可选；默认=False；为真时按元素本身排序
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
