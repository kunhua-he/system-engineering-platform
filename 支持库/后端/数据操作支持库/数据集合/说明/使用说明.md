# 数据集合 使用说明

- 包id：支持库.后端.数据操作支持库.数据集合
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.数据集合.列表添加 | 列表添加 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '追加后的新列表'}} | 追加条目并返回新列表（原列表不变），返回 {值=新列表} |
| 数据操作支持库.数据集合.列表移除 | 列表移除 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '移除后的新列表'}} | 移除首个匹配条目并返回新列表（原列表不变），返回 {值=新列表} |
| 数据操作支持库.数据集合.列表排序 | 列表排序 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '排序后的新列表'}} | 排序列表并返回新列表，返回 {值=排序后的新列表} |
| 数据操作支持库.数据集合.列表查找 | 列表查找 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '匹配条目索引（未找到为 -1）'}} | 返回首个匹配条目的索引（未找到返回 -1），返回 {值=索引} |
| 数据操作支持库.数据集合.字典设置 | 字典设置 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '设置后的新字典'}} | 设置键值并返回新字典（原字典不变），返回 {值=新字典} |
| 数据操作支持库.数据集合.字典获取 | 字典获取 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '键值或默认值'}} | 获取键值（键不存在返回默认值），返回 {值=键值或默认值} |
| 数据操作支持库.数据集合.集合取并集 | 集合取并集 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '去重并集列表'}} | 返回两个列表的去重并集（排序稳定），返回 {值=并集列表} |
| 数据操作支持库.数据集合.集合取交集 | 集合取交集 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '去重交集列表'}} | 返回两个列表的去重交集（排序稳定），返回 {值=交集列表} |
| 数据操作支持库.数据集合.统计数量 | 统计数量 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '条目数量（整数）'}} | 返回列表条目数量，返回 {值=数量} |
| 数据操作支持库.数据集合.按字段查找 | 按字段查找 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '第一个匹配项（未找到为空）'}} | 按字段查找第一个匹配项（未找到返回空），返回 {值=匹配项或空} |
| 数据操作支持库.数据集合.过滤 | 过滤 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '过滤后的新列表'}} | 按字段过滤返回新列表，返回 {值=过滤后的新列表} |
| 数据操作支持库.数据集合.映射转换 | 映射转换 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '字段重命名后的新列表'}} | 按原字段到新字段映射重命名项目字段并返回新列表，返回 {值=新列表} |
| 数据操作支持库.数据集合.按字段排序 | 按字段排序 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '排序后的新列表'}} | 按字段或元素排序并返回新列表，返回 {值=排序后的新列表} |
| 数据操作支持库.数据集合.列表去重 | 列表去重 |  | {结果列表, 数量} | 列表去重（保持原顺序） |
| 数据操作支持库.数据集合.列表倒序 | 列表倒序 |  | {结果列表} | 列表倒序 |
| 数据操作支持库.数据集合.列表分片 | 列表分片 |  | {结果列表} | 列表分片 |
| 数据操作支持库.数据集合.字典合并 | 字典合并 |  | {结果字典, 键数量} | 合并多个字典（后者覆盖） |
| 数据操作支持库.数据集合.文本截取 | 文本截取 |  | {结果文本} | 文本截取 |
| 数据操作支持库.数据集合.文本去首尾 | 文本去首尾 |  | {结果文本} | 去除文本首尾指定字符 |
| 数据操作支持库.数据集合.文本重复 | 文本重复 |  | {结果文本} | 文本重复 N 次 |

## 数据操作支持库.数据集合.列表添加（列表添加）

**说明**：追加条目并返回新列表（原列表不变），返回 {值=新列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 源列表 |
| 条目 | 文本型 | 是 | None | 要追加的条目（文本/整数/浮点数/布尔/字典/列表等可序列化值） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '追加后的新列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表添加(列表=值, 条目=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.列表移除（列表移除）

**说明**：移除首个匹配条目并返回新列表（原列表不变），返回 {值=新列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 源列表 |
| 条目 | 文本型 | 是 | None | 要移除的条目（不存在时返回原列表） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '移除后的新列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表移除(列表=值, 条目=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.列表排序（列表排序）

**说明**：排序列表并返回新列表，返回 {值=排序后的新列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 源列表（元素须可比较） |
| 倒序 | 逻辑型 | 否 | False | 是否倒序（默认升序） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '排序后的新列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表排序(列表=值, 倒序=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.列表查找（列表查找）

**说明**：返回首个匹配条目的索引（未找到返回 -1），返回 {值=索引}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 源列表 |
| 目标 | 文本型 | 是 | None | 要查找的条目 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '匹配条目索引（未找到为 -1）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表查找(列表=值, 目标=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.字典设置（字典设置）

**说明**：设置键值并返回新字典（原字典不变），返回 {值=新字典}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字典 | 字典型 | 是 | None | 源字典 |
| 键 | 文本型 | 是 | None | 键名 |
| 值 | 文本型 | 是 | None | 键值（文本/整数/浮点数/布尔/字典/列表等可序列化值） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '设置后的新字典'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.字典设置(字典=值, 键=值, 值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.字典获取（字典获取）

**说明**：获取键值（键不存在返回默认值），返回 {值=键值或默认值}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字典 | 字典型 | 是 | None | 源字典 |
| 键 | 文本型 | 是 | None | 键名 |
| 默认值 | 文本型 | 否 | None | 键不存在时的返回值 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '键值或默认值'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.字典获取(字典=值, 键=值, 默认值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.集合取并集（集合取并集）

**说明**：返回两个列表的去重并集（排序稳定），返回 {值=并集列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 集合甲 | 列表型 | 是 | None | 集合甲（列表） |
| 集合乙 | 列表型 | 是 | None | 集合乙（列表） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '去重并集列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.集合取并集(集合甲=值, 集合乙=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.集合取交集（集合取交集）

**说明**：返回两个列表的去重交集（排序稳定），返回 {值=交集列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 集合甲 | 列表型 | 是 | None | 集合甲（列表） |
| 集合乙 | 列表型 | 是 | None | 集合乙（列表） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '去重交集列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.集合取交集(集合甲=值, 集合乙=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.统计数量（统计数量）

**说明**：返回列表条目数量，返回 {值=数量}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 源列表 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '条目数量（整数）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.统计数量(列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.按字段查找（按字段查找）

**说明**：按字段查找第一个匹配项（未找到返回空），返回 {值=匹配项或空}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 字典项目列表 |
| 字段名 | 文本型 | 是 | None | 要匹配的字段名（直接匹配时忽略） |
| 匹配值 | 文本型 | 是 | None | 期望的字段值 |
| 直接匹配 | 逻辑型 | 否 | False | 为真时忽略字段名直接比较元素 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '第一个匹配项（未找到为空）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.按字段查找(列表=值, 字段名=值, 匹配值=值, 直接匹配=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.过滤（过滤）

**说明**：按字段过滤返回新列表，返回 {值=过滤后的新列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 字典项目列表 |
| 字段名 | 文本型 | 是 | None | 要匹配的字段名（直接匹配时忽略） |
| 等于值 | 文本型 | 是 | None | 期望的字段值 |
| 直接匹配 | 逻辑型 | 否 | False | 为真时忽略字段名直接比较元素 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '过滤后的新列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.过滤(列表=值, 字段名=值, 等于值=值, 直接匹配=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.映射转换（映射转换）

**说明**：按原字段到新字段映射重命名项目字段并返回新列表，返回 {值=新列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 字典项目列表 |
| 字段映射 | 字典型 | 是 | None | 原字段到新字段的映射 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '字段重命名后的新列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.映射转换(列表=值, 字段映射=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.按字段排序（按字段排序）

**说明**：按字段或元素排序并返回新列表，返回 {值=排序后的新列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 字典项目列表（直接匹配时为可比较元素列表） |
| 字段名 | 文本型 | 是 | None | 排序字段名（直接匹配时忽略） |
| 升序 | 逻辑型 | 否 | True | 是否升序（默认升序） |
| 直接匹配 | 逻辑型 | 否 | False | 为真时按元素本身排序 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '排序后的新列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据集合.按字段排序(列表=值, 字段名=值, 升序=值, 直接匹配=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.列表去重（列表去重）

**说明**：列表去重（保持原顺序）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 列表 |

**返回结构**：{结果列表, 数量}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表去重(列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.列表倒序（列表倒序）

**说明**：列表倒序

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 列表 |

**返回结构**：{结果列表}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表倒序(列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.列表分片（列表分片）

**说明**：列表分片

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 列表 | 列表型 | 是 | None | 列表 |
| 起始 | 整数型 | 否 | 0 | 起始索引 |
| 结束 | 整数型 | 否 | None | 结束索引 |

**返回结构**：{结果列表}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.列表分片(列表=值, 起始=值, 结束=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.字典合并（字典合并）

**说明**：合并多个字典（后者覆盖）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字典列表 | 列表型 | 是 | None | 字典列表 |

**返回结构**：{结果字典, 键数量}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.字典合并(字典列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.文本截取（文本截取）

**说明**：文本截取

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 |
| 起始 | 整数型 | 否 | 0 | 起始 |
| 长度 | 整数型 | 否 | None | 长度 |

**返回结构**：{结果文本}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.文本截取(文本=值, 起始=值, 长度=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.文本去首尾（文本去首尾）

**说明**：去除文本首尾指定字符

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 |
| 字符集 | 文本型 | 否 | None | 要去的字符（默认空白） |

**返回结构**：{结果文本}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.文本去首尾(文本=值, 字符集=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据集合.文本重复（文本重复）

**说明**：文本重复 N 次

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 |
| 次数 | 整数型 | 是 | None | 重复次数 |

**返回结构**：{结果文本}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |

**调用示例**：

```python
数据操作支持库.数据集合.文本重复(文本=值, 次数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
