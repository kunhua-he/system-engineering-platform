<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 类型转换（支持库.后端.数据操作支持库.类型转换）

- 类型：支持库
- 版本：1.0.0
- 说明：（无）
- 入口：__init__.py

## 能力
### 数据操作支持库.类型转换.文本转整数（`数据操作支持库.类型转换.文本转整数`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：文本:文本型, 进制:整数型
- 必填参数：文本
- 可选参数：进制
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 类型转换失败, 数值溢出
- 调用示例：数据操作支持库.类型转换.文本转整数(进制=10)
- 依赖能力：无
### 数据操作支持库.类型转换.文本转长整数（`数据操作支持库.类型转换.文本转长整数`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：文本:文本型, 进制:整数型
- 必填参数：文本
- 可选参数：进制
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 类型转换失败, 数值溢出
- 调用示例：数据操作支持库.类型转换.文本转长整数(进制=10)
- 依赖能力：无
### 数据操作支持库.类型转换.整数转文本（`数据操作支持库.类型转换.整数转文本`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：整数:整数型
- 必填参数：整数
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.类型转换.整数转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.长整数转文本（`数据操作支持库.类型转换.长整数转文本`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：长整数:长整数型
- 必填参数：长整数
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.类型转换.长整数转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转单精度数（`数据操作支持库.类型转换.文本转单精度数`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 类型转换失败, 数值溢出
- 调用示例：数据操作支持库.类型转换.文本转单精度数()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转双精度数（`数据操作支持库.类型转换.文本转双精度数`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 类型转换失败
- 调用示例：数据操作支持库.类型转换.文本转双精度数()
- 依赖能力：无
### 数据操作支持库.类型转换.数值转文本（`数据操作支持库.类型转换.数值转文本`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：数值:双精度数型, 小数位数:整数型
- 必填参数：数值
- 可选参数：小数位数
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 类型转换失败
- 调用示例：数据操作支持库.类型转换.数值转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.逻辑转文本（`数据操作支持库.类型转换.逻辑转文本`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：逻辑值:逻辑型
- 必填参数：逻辑值
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.类型转换.逻辑转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转逻辑（`数据操作支持库.类型转换.文本转逻辑`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 类型转换失败
- 调用示例：数据操作支持库.类型转换.文本转逻辑()
- 依赖能力：无
### 数据操作支持库.类型转换.校验类型（`数据操作支持库.类型转换.校验类型`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库.类型转换
- 参数列表：值:字典型, 目标类型:文本型
- 必填参数：值, 目标类型
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.类型转换.校验类型()
- 依赖能力：无
