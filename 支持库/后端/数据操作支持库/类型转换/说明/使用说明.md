# 类型转换 使用说明

- 包id：支持库.后端.数据操作支持库.类型转换
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.类型转换.文本转整数 | 文本转整数 | 1.0.0 | {'类型': '结果型', '值结构': '整数型'} | 数据操作支持库.类型转换.文本转整数 确定性原子转换 |
| 数据操作支持库.类型转换.文本转长整数 | 文本转长整数 | 1.0.0 | {'类型': '结果型', '值结构': '长整数型'} | 数据操作支持库.类型转换.文本转长整数 确定性原子转换 |
| 数据操作支持库.类型转换.整数转文本 | 整数转文本 | 1.0.0 | {'类型': '结果型', '值结构': '文本型'} | 数据操作支持库.类型转换.整数转文本 确定性原子转换 |
| 数据操作支持库.类型转换.长整数转文本 | 长整数转文本 | 1.0.0 | {'类型': '结果型', '值结构': '文本型'} | 数据操作支持库.类型转换.长整数转文本 确定性原子转换 |
| 数据操作支持库.类型转换.文本转单精度数 | 文本转单精度数 | 1.0.0 | {'类型': '结果型', '值结构': '单精度数型'} | 数据操作支持库.类型转换.文本转单精度数 确定性原子转换 |
| 数据操作支持库.类型转换.文本转双精度数 | 文本转双精度数 | 1.0.0 | {'类型': '结果型', '值结构': '双精度数型'} | 数据操作支持库.类型转换.文本转双精度数 确定性原子转换 |
| 数据操作支持库.类型转换.数值转文本 | 数值转文本 | 1.0.0 | {'类型': '结果型', '值结构': '文本型'} | 数据操作支持库.类型转换.数值转文本 确定性原子转换 |
| 数据操作支持库.类型转换.逻辑转文本 | 逻辑转文本 | 1.0.0 | {'类型': '结果型', '值结构': '文本型'} | 数据操作支持库.类型转换.逻辑转文本 确定性原子转换 |
| 数据操作支持库.类型转换.文本转逻辑 | 文本转逻辑 | 1.0.0 | {'类型': '结果型', '值结构': '逻辑型'} | 数据操作支持库.类型转换.文本转逻辑 确定性原子转换 |
| 数据操作支持库.类型转换.校验类型 | 校验类型 | 1.0.0 | {'类型': '结果型', '值结构': '逻辑型'} | 数据操作支持库.类型转换.校验类型 确定性原子转换 |

## 数据操作支持库.类型转换.文本转整数（文本转整数）

**说明**：数据操作支持库.类型转换.文本转整数 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 参数 |
| 进制 | 整数型 | 否 | 10 | 进制 参数 |

**返回结构**：{'类型': '结果型', '值结构': '整数型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 类型转换失败 |
| 数值溢出 |

**调用示例**：

```python
数据操作支持库.类型转换.文本转整数(文本=值, 进制=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.文本转长整数（文本转长整数）

**说明**：数据操作支持库.类型转换.文本转长整数 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 参数 |
| 进制 | 整数型 | 否 | 10 | 进制 参数 |

**返回结构**：{'类型': '结果型', '值结构': '长整数型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 类型转换失败 |
| 数值溢出 |

**调用示例**：

```python
数据操作支持库.类型转换.文本转长整数(文本=值, 进制=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.整数转文本（整数转文本）

**说明**：数据操作支持库.类型转换.整数转文本 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 整数 | 整数型 | 是 | None | 整数 参数 |

**返回结构**：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.类型转换.整数转文本(整数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.长整数转文本（长整数转文本）

**说明**：数据操作支持库.类型转换.长整数转文本 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 长整数 | 长整数型 | 是 | None | 长整数 参数 |

**返回结构**：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.类型转换.长整数转文本(长整数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.文本转单精度数（文本转单精度数）

**说明**：数据操作支持库.类型转换.文本转单精度数 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 参数 |

**返回结构**：{'类型': '结果型', '值结构': '单精度数型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 类型转换失败 |
| 数值溢出 |

**调用示例**：

```python
数据操作支持库.类型转换.文本转单精度数(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.文本转双精度数（文本转双精度数）

**说明**：数据操作支持库.类型转换.文本转双精度数 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 参数 |

**返回结构**：{'类型': '结果型', '值结构': '双精度数型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 类型转换失败 |

**调用示例**：

```python
数据操作支持库.类型转换.文本转双精度数(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.数值转文本（数值转文本）

**说明**：数据操作支持库.类型转换.数值转文本 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值 | 双精度数型 | 是 | None | 数值 参数 |
| 小数位数 | 整数型 | 否 | None | 小数位数 参数 |

**返回结构**：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 类型转换失败 |

**调用示例**：

```python
数据操作支持库.类型转换.数值转文本(数值=值, 小数位数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.逻辑转文本（逻辑转文本）

**说明**：数据操作支持库.类型转换.逻辑转文本 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 逻辑值 | 逻辑型 | 是 | None | 逻辑值 参数 |

**返回结构**：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.类型转换.逻辑转文本(逻辑值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.文本转逻辑（文本转逻辑）

**说明**：数据操作支持库.类型转换.文本转逻辑 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 文本 参数 |

**返回结构**：{'类型': '结果型', '值结构': '逻辑型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 类型转换失败 |

**调用示例**：

```python
数据操作支持库.类型转换.文本转逻辑(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.类型转换.校验类型（校验类型）

**说明**：数据操作支持库.类型转换.校验类型 确定性原子转换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 值 | 字典型 | 是 | None | 值 参数 |
| 目标类型 | 文本型 | 是 | None | 目标类型 参数 |

**返回结构**：{'类型': '结果型', '值结构': '逻辑型'}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.类型转换.校验类型(值=值, 目标类型=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
