# 度量 使用说明

- 包id：支持库.后端.数据操作支持库.度量
- 版本：1.3.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.度量.规范化计时条目 | 规范化计时条目 | 1.0.0 | {'类型': '结果型', '值结构': {'条目': {'名称': '条目名（缺失时用 计时_序号 兜底）', '状态': '状态文本；缺失时按成功键映射 pass/fail，否则 unknown', '耗时秒': '耗时秒数（保留 3 位小数）或空', '附带键': '命中的附带字段（command/level/source 等）'}}} | 规范化单条计时数据；名称/耗时/状态/附带键别名可参数化 |
| 数据操作支持库.度量.汇总计时条目 | 汇总计时条目 | 1.0.0 | {'类型': '结果型', '值结构': {'数量': '条目总数', '已计时数': '可读到耗时的条目数', '总耗时秒': '耗时合计（保留 3 位小数；空列表为整数 0）'}} | 汇总已规范化的计时条目：数量/已计时数/总耗时秒 |
| 数据操作支持库.度量.解析计时数据 | 解析计时数据 | 1.0.0 | {'类型': '结果型', '值结构': {'条目列表': '规范化后的条目列表（非法条目跳过）', '摘要': '{数量, 已计时数, 总耗时秒}', '警告列表': '解析问题警告文本（记警告不抛异常）'}} | 解析计时数据 JSON 并规范化汇总；任何解析问题记警告不抛异常 |
| 数据操作支持库.度量.判定耗时超标 | 判定耗时超标 | 1.0.0 | {'类型': '结果型', '值结构': {'超标': '是否超过阈值（逻辑型）', '实测秒': '本次实测耗时（双精度数型，3 位小数）', '阈值秒': '本次使用的阈值（双精度数型）', '倍率': '实测/阈值；未超标时为 0.0', '名称': '回显的判定对象名', '加速方向': '超标时为加速方式表全量（八项，每项含 序/方式/适用/本项目实测）；未超标时空列表', '说明': '中文判定说明（含倍数与下一步动作）'}} | 判定一次工具调用的实测耗时是否超过阈值（默认 5 秒），超了就回带该走哪些加速方向。实现哲学 11.2（2026-09-19 华哥裁决）「工具调用如果超过 5 秒的，就要考虑是哪里可以优化了」—— 把该裁决做成可被门禁/工具链自动调用的原子件，不靠人记得住。加速方向 取自 加速方式表（八项：进目录即剪枝/多线程并发/批量替代逐个/缓存复用/降数据量/改架构/换实现算法/异步后台化），按适用面从宽到窄给，不替调用方拍板。 |
| 数据操作支持库.度量.判定可否复用 | 判定可否复用 | 1.0.0 | {'类型': '结果型', '值结构': {'可复用': '能否直接复用已验证结果（逻辑型）', '分数': '历史成功率（双精度数型，4 位小数）', '判定': '可复用/抽样执行/必须执行（文本型）', '样本数': '历史样本条数（整数型）', '成功数': '历史成功条数（整数型）', '指纹一致': '当前指纹是否在历史记录中命中（逻辑型）', '依据': '「为什么可跳」的证据句，直接写进门禁输出（文本型）', '说明': '中文判定说明（含下一步动作）'}} | 按历史成功率给检查项打分，判定能否直接复用已验证结果（跳过执行）；判定必须带「为什么可跳」的证据（成功率+指纹一致），不许静默跳过（哲学 11.2 打分机制，2026-09-19 华哥裁决） |

## 数据操作支持库.度量.规范化计时条目（规范化计时条目）

**说明**：规范化单条计时数据；名称/耗时/状态/附带键别名可参数化

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 条目 | 字典型 | 是 | None | 原始计时条目 |
| 索引 | 整数型 | 否 | 0 | 用于名称兜底 |
| 名称键别名 | 列表型 | 否 | None | 默认 name/tool/target/command |
| 耗时键别名 | 列表型 | 否 | None | 默认 duration_seconds/耗时/seconds |
| 状态键别名 | 列表型 | 否 | None | 默认 状态/status |
| 成功键 | 文本型 | 否 | success | 布尔成功标志键名 |
| 附带键别名 | 列表型 | 否 | None | 默认 command/level/source |

**返回结构**：{'类型': '结果型', '值结构': {'条目': {'名称': '条目名（缺失时用 计时_序号 兜底）', '状态': '状态文本；缺失时按成功键映射 pass/fail，否则 unknown', '耗时秒': '耗时秒数（保留 3 位小数）或空', '附带键': '命中的附带字段（command/level/source 等）'}}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 条目格式错误 |

**调用示例**：

```python
数据操作支持库.度量.规范化计时条目(条目=值, 索引=值, 名称键别名=值, 耗时键别名=值, 状态键别名=值, 成功键=值, 附带键别名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数据操作支持库.度量.汇总计时条目（汇总计时条目）

**说明**：汇总已规范化的计时条目：数量/已计时数/总耗时秒

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 条目列表 | 列表型 | 是 | None | 已规范化的条目列表 |
| 耗时键别名 | 列表型 | 否 | None | 兼容 duration_seconds 等调用方命名 |

**返回结构**：{'类型': '结果型', '值结构': {'数量': '条目总数', '已计时数': '可读到耗时的条目数', '总耗时秒': '耗时合计（保留 3 位小数；空列表为整数 0）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.度量.汇总计时条目(条目列表=值, 耗时键别名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数据操作支持库.度量.解析计时数据（解析计时数据）

**说明**：解析计时数据 JSON 并规范化汇总；任何解析问题记警告不抛异常

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 原始文本 | 文本型 | 否 |  | 计时数据 JSON 文本 |
| 容器键别名 | 列表型 | 否 | None | 默认 items/timings/测试 |
| 名称键别名 | 列表型 | 否 | None | 透传给规范化 |
| 耗时键别名 | 列表型 | 否 | None | 透传给规范化 |

**返回结构**：{'类型': '结果型', '值结构': {'条目列表': '规范化后的条目列表（非法条目跳过）', '摘要': '{数量, 已计时数, 总耗时秒}', '警告列表': '解析问题警告文本（记警告不抛异常）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.度量.解析计时数据(原始文本=值, 容器键别名=值, 名称键别名=值, 耗时键别名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数据操作支持库.度量.判定耗时超标（判定耗时超标）

**说明**：判定一次工具调用的实测耗时是否超过阈值（默认 5 秒），超了就回带该走哪些加速方向。实现哲学 11.2（2026-09-19 华哥裁决）「工具调用如果超过 5 秒的，就要考虑是哪里可以优化了」—— 把该裁决做成可被门禁/工具链自动调用的原子件，不靠人记得住。加速方向 取自 加速方式表（八项：进目录即剪枝/多线程并发/批量替代逐个/缓存复用/降数据量/改架构/换实现算法/异步后台化），按适用面从宽到窄给，不替调用方拍板。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 实测秒 | 双精度数型 | 是 | None | 本次工具调用的实测耗时（秒） |
| 阈值秒 | 双精度数型 | 否 | None | 判定阈值；不传用默认 5.0；必须是正数 |
| 名称 | 文本型 | 否 |  | 被判定对象的名字（工具/命令/能力 id），仅回显 |

**返回结构**：{'类型': '结果型', '值结构': {'超标': '是否超过阈值（逻辑型）', '实测秒': '本次实测耗时（双精度数型，3 位小数）', '阈值秒': '本次使用的阈值（双精度数型）', '倍率': '实测/阈值；未超标时为 0.0', '名称': '回显的判定对象名', '加速方向': '超标时为加速方式表全量（八项，每项含 序/方式/适用/本项目实测）；未超标时空列表', '说明': '中文判定说明（含倍数与下一步动作）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.度量.判定耗时超标(实测秒=值, 阈值秒=值, 名称=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数据操作支持库.度量.判定可否复用（判定可否复用）

**说明**：按历史成功率给检查项打分，判定能否直接复用已验证结果（跳过执行）；判定必须带「为什么可跳」的证据（成功率+指纹一致），不许静默跳过（哲学 11.2 打分机制，2026-09-19 华哥裁决）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 历史记录 | 列表型 | 否 | None | 历史执行记录列表（或 JSON 字符串），每项含 成功（逻辑型/文字）与 指纹（文本型）；不可解析按空列表处理 |
| 当前指纹 | 文本型 | 否 |  | 当前输入/制品指纹；与历史记录不一致则缓存失效、必须真跑 |
| 可复用成功率 | 双精度数型 | 否 | None | 可复用的成功率下限，不传用默认 0.98；须 0~1 |
| 最少样本 | 整数型 | 否 | None | 判定所需最少历史样本数，不传用默认 3；不足即判必须执行 |

**返回结构**：{'类型': '结果型', '值结构': {'可复用': '能否直接复用已验证结果（逻辑型）', '分数': '历史成功率（双精度数型，4 位小数）', '判定': '可复用/抽样执行/必须执行（文本型）', '样本数': '历史样本条数（整数型）', '成功数': '历史成功条数（整数型）', '指纹一致': '当前指纹是否在历史记录中命中（逻辑型）', '依据': '「为什么可跳」的证据句，直接写进门禁输出（文本型）', '说明': '中文判定说明（含下一步动作）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.度量.判定可否复用(历史记录=值, 当前指纹=值, 可复用成功率=值, 最少样本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）
