# 数据操作支持库（支持库.后端.数据操作支持库）

- 类型：支持库
- 版本：2.0.2
- 说明：聚合支持库，包含 7 个子域：响应塑形/度量/数据交换/数据集合/文本处理/时间日期/类型转换
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 裁剪响应（`数据操作支持库.响应塑形.裁剪响应`）
- 版本：2.0.2
- 一句话说明：按路径/条目数/字节数裁剪结构化响应；信封键、元数据键与数据键均可参数化
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：载荷:字典型, 选择路径:文本型, 最大条目数:整数型, 最大字节数:整数型, 信封键:列表型, 元数据键:文本型, 数据键:文本型
- 必填参数：载荷
- 可选参数：选择路径, 最大条目数, 最大字节数, 信封键, 元数据键, 数据键
- 返回结构：{'类型': '结果型', '值结构': {'塑形结果': '塑形后的响应载荷', '已塑形': '是否执行了塑形（无任何限制参数时为假）', '已截断': '是否发生条目截断或字节截断', '选择路径': '命中的规范选择路径（未命中为空）', '省略计数': '{路径: 被省略条目数}', '警告列表': '路径解析失败或最小响应仍超限的警告文本列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.响应塑形.裁剪响应()
- 依赖能力：无
### 规范化计时条目（`数据操作支持库.度量.规范化计时条目`）
- 版本：2.0.2
- 一句话说明：规范化单条计时数据；名称/耗时/状态/附带键别名可参数化
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：条目:字典型, 索引:整数型, 名称键别名:列表型, 耗时键别名:列表型, 状态键别名:列表型, 成功键:文本型, 附带键别名:列表型
- 必填参数：条目
- 可选参数：索引, 名称键别名, 耗时键别名, 状态键别名, 成功键, 附带键别名
- 返回结构：{'类型': '结果型', '值结构': {'条目': {'名称': '条目名（缺失时用 计时_序号 兜底）', '状态': '状态文本；缺失时按成功键映射 pass/fail，否则 unknown', '耗时秒': '耗时秒数（保留 3 位小数）或空', '附带键': '命中的附带字段（command/level/source 等）'}}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.度量.规范化计时条目()
- 依赖能力：无
### 汇总计时条目（`数据操作支持库.度量.汇总计时条目`）
- 版本：2.0.2
- 一句话说明：汇总已规范化的计时条目：数量/已计时数/总耗时秒
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：条目列表:列表型, 耗时键别名:列表型
- 必填参数：条目列表
- 可选参数：耗时键别名
- 返回结构：{'类型': '结果型', '值结构': {'数量': '条目总数', '已计时数': '可读到耗时的条目数', '总耗时秒': '耗时合计（保留 3 位小数；空列表为整数 0）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.度量.汇总计时条目()
- 依赖能力：无
### 解析计时数据（`数据操作支持库.度量.解析计时数据`）
- 版本：2.0.2
- 一句话说明：解析计时数据 JSON 并规范化汇总；任何解析问题记警告不抛异常
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：原始文本:文本型, 容器键别名:列表型, 名称键别名:列表型, 耗时键别名:列表型
- 必填参数：无
- 可选参数：原始文本, 容器键别名, 名称键别名, 耗时键别名
- 返回结构：{'类型': '结果型', '值结构': {'条目列表': '规范化后的条目列表（非法条目跳过）', '摘要': '{数量, 已计时数, 总耗时秒}', '警告列表': '解析问题警告文本（记警告不抛异常）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.度量.解析计时数据()
- 依赖能力：无
### 序列化JSON（`数据操作支持库.数据交换.序列化JSON`）
- 版本：2.0.2
- 一句话说明：把字典或列表数据序列化为 JSON 文本（ensure_ascii=False，缩进 2），返回 {值=JSON文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数据:字典型
- 必填参数：数据
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': 'JSON 文本字符串（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.序列化JSON()
- 依赖能力：无
### 反序列化JSON（`数据操作支持库.数据交换.反序列化JSON`）
- 版本：2.0.2
- 一句话说明：把 JSON 文本解析为字典或列表，返回 {值=解析后的数据}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '解析后的字典或列表（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.反序列化JSON()
- 依赖能力：无
### 序列化CSV（`数据操作支持库.数据交换.序列化CSV`）
- 版本：2.0.2
- 一句话说明：把行列表序列化为 CSV 文本，返回 {值=CSV文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：行列表:列表型
- 必填参数：行列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': 'CSV 文本字符串（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.序列化CSV()
- 依赖能力：无
### 反序列化CSV（`数据操作支持库.数据交换.反序列化CSV`）
- 版本：2.0.2
- 一句话说明：把 CSV 文本解析为行列表（可选分隔符与严格校验），返回 {值=行列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 分隔符:文本型, 严格模式:逻辑型
- 必填参数：文本
- 可选参数：分隔符, 严格模式
- 返回结构：{'类型': '结果型', '值结构': {'值': '行列表（成功时，每行为字符串单元格列表）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.反序列化CSV()
- 依赖能力：无
### 深合并（`数据操作支持库.数据交换.深合并`）
- 版本：2.0.2
- 一句话说明：递归合并两个字典：同键且两边都是字典时递归合并，否则用覆盖值替换；不修改入参，返回 {合并结果,键数}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：基础:字典型, 覆盖:字典型
- 必填参数：基础, 覆盖
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'合并结果': '递归合并后的新字典', '键数': '顶层键数量'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.深合并()
- 依赖能力：无
### 解析YAML（`数据操作支持库.数据交换.解析YAML`）
- 版本：2.0.2
- 一句话说明：把 YAML 文本解析为数据（经适配层 YAML提供者），返回 {值=解析后的数据}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '解析后的数据（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.解析YAML()
- 依赖能力：无
### 序列化YAML（`数据操作支持库.数据交换.序列化YAML`）
- 版本：2.0.2
- 一句话说明：把数据序列化为 YAML 文本（经适配层 YAML提供者），返回 {值=YAML文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数据:JSON值型
- 必填参数：无
- 可选参数：数据
- 返回结构：{'类型': '结果型', '值结构': {'值': 'YAML 文本字符串（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.序列化YAML()
- 依赖能力：无
### 提取带前缀JSON（`数据操作支持库.数据交换.提取带前缀JSON`）
- 版本：2.0.2
- 一句话说明：从文本中提取带指定前缀的 JSON 行（默认从末尾找）；剥前缀后必须是 JSON 对象，返回 {找到,数据,原因}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 前缀:文本型, 从末尾查找:逻辑型
- 必填参数：文本, 前缀
- 可选参数：从末尾查找
- 返回结构：{'类型': '结果型', '值结构': {'找到': '是否命中（布尔）', '数据': '命中的 JSON 对象或 null', '原因': '未命中原因（仅未命中时出现）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.提取带前缀JSON()
- 依赖能力：无
### 压缩描述字段（`数据操作支持库.数据交换.压缩描述字段`）
- 版本：2.0.2
- 一句话说明：递归折叠指定字段的字符串值（折叠空白），只动该字段不改结构与其它键，返回 {压缩结果,字段名}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数据:字典型, 字段名:文本型
- 必填参数：数据
- 可选参数：字段名
- 返回结构：{'类型': '结果型', '值结构': {'压缩结果': '折叠后的新结构', '字段名': '实际处理的字段名'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.压缩描述字段()
- 依赖能力：无
### 列表添加（`数据操作支持库.数据集合.列表添加`）
- 版本：2.0.2
- 一句话说明：追加条目并返回新列表（原列表不变），返回 {值=新列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 条目:JSON值型
- 必填参数：列表, 条目
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '追加后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表添加()
- 依赖能力：无
### 列表移除（`数据操作支持库.数据集合.列表移除`）
- 版本：2.0.2
- 一句话说明：移除首个匹配条目并返回新列表（原列表不变），返回 {值=新列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 条目:JSON值型
- 必填参数：列表, 条目
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '移除后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表移除()
- 依赖能力：无
### 列表排序（`数据操作支持库.数据集合.列表排序`）
- 版本：2.0.2
- 一句话说明：排序列表并返回新列表，返回 {值=排序后的新列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 倒序:逻辑型
- 必填参数：列表
- 可选参数：倒序
- 返回结构：{'类型': '结果型', '值结构': {'值': '排序后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表排序()
- 依赖能力：无
### 列表查找（`数据操作支持库.数据集合.列表查找`）
- 版本：2.0.2
- 一句话说明：返回首个匹配条目的索引（未找到返回 -1），返回 {值=索引}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 目标:JSON值型
- 必填参数：列表, 目标
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '匹配条目索引（未找到为 -1）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表查找()
- 依赖能力：无
### 字典设置（`数据操作支持库.数据集合.字典设置`）
- 版本：2.0.2
- 一句话说明：设置键值并返回新字典（原字典不变），返回 {值=新字典}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：字典:字典型, 键:文本型, 值:JSON值型
- 必填参数：字典, 键, 值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '设置后的新字典'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.字典设置()
- 依赖能力：无
### 字典获取（`数据操作支持库.数据集合.字典获取`）
- 版本：2.0.2
- 一句话说明：获取键值（键不存在返回默认值），返回 {值=键值或默认值}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：字典:字典型, 键:文本型, 默认值:JSON值型
- 必填参数：字典, 键
- 可选参数：默认值
- 返回结构：{'类型': '结果型', '值结构': {'值': '键值或默认值'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.字典获取()
- 依赖能力：无
### 集合取并集（`数据操作支持库.数据集合.集合取并集`）
- 版本：2.0.2
- 一句话说明：返回两个列表的去重并集（排序稳定），返回 {值=并集列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：集合1:列表型, 集合2:列表型
- 必填参数：集合1, 集合2
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '去重并集列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.集合取并集()
- 依赖能力：无
### 集合取交集（`数据操作支持库.数据集合.集合取交集`）
- 版本：2.0.2
- 一句话说明：返回两个列表的去重交集（排序稳定），返回 {值=交集列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：集合1:列表型, 集合2:列表型
- 必填参数：集合1, 集合2
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '去重交集列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.集合取交集()
- 依赖能力：无
### 统计数量（`数据操作支持库.数据集合.统计数量`）
- 版本：2.0.2
- 一句话说明：返回列表条目数量，返回 {值=数量}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型
- 必填参数：列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '条目数量（整数）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.统计数量()
- 依赖能力：无
### 按字段查找（`数据操作支持库.数据集合.按字段查找`）
- 版本：2.0.2
- 一句话说明：按字段查找第一个匹配项（未找到返回空），返回 {值=匹配项或空}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段名:文本型, 匹配值:JSON值型, 直接匹配:逻辑型
- 必填参数：列表, 字段名, 匹配值
- 可选参数：直接匹配
- 返回结构：{'类型': '结果型', '值结构': {'值': '第一个匹配项（未找到为空）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.按字段查找()
- 依赖能力：无
### 过滤（`数据操作支持库.数据集合.过滤`）
- 版本：2.0.2
- 一句话说明：按字段过滤返回新列表，返回 {值=过滤后的新列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段名:文本型, 等于值:JSON值型, 直接匹配:逻辑型
- 必填参数：列表, 字段名, 等于值
- 可选参数：直接匹配
- 返回结构：{'类型': '结果型', '值结构': {'值': '过滤后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.过滤()
- 依赖能力：无
### 映射转换（`数据操作支持库.数据集合.映射转换`）
- 版本：2.0.2
- 一句话说明：按原字段到新字段映射重命名项目字段并返回新列表，返回 {值=新列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段映射:字典型
- 必填参数：列表, 字段映射
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '字段重命名后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.映射转换()
- 依赖能力：无
### 按字段排序（`数据操作支持库.数据集合.按字段排序`）
- 版本：2.0.2
- 一句话说明：按字段或元素排序并返回新列表，返回 {值=排序后的新列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段名:文本型, 升序:逻辑型, 直接匹配:逻辑型
- 必填参数：列表, 字段名
- 可选参数：升序, 直接匹配
- 返回结构：{'类型': '结果型', '值结构': {'值': '排序后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.按字段排序()
- 依赖能力：无
### 列表去重（`数据操作支持库.数据集合.列表去重`）
- 版本：2.0.2
- 一句话说明：列表去重（保持原顺序）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型
- 必填参数：列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'结果列表': '去重后的新列表（保持首次出现顺序）', '数量': '去重后条目数'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表去重()
- 依赖能力：无
### 列表倒序（`数据操作支持库.数据集合.列表倒序`）
- 版本：2.0.2
- 一句话说明：列表倒序
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型
- 必填参数：列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'结果列表': '倒序后的新列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表倒序()
- 依赖能力：无
### 列表分片（`数据操作支持库.数据集合.列表分片`）
- 版本：2.0.2
- 一句话说明：列表分片
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 起始:整数型, 结束:整数型
- 必填参数：列表
- 可选参数：起始, 结束
- 返回结构：{'类型': '结果型', '值结构': {'结果列表': '分片后的新列表（按 Python 切片口径）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表分片()
- 依赖能力：无
### 字典合并（`数据操作支持库.数据集合.字典合并`）
- 版本：2.0.2
- 一句话说明：合并多个字典（后者覆盖）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：字典列表:列表型
- 必填参数：字典列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'结果字典': '合并后的新字典（后者覆盖前者）', '键数量': '合并后键数量'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.字典合并()
- 依赖能力：无
### 文本截取（`数据操作支持库.数据集合.文本截取`）
- 版本：2.0.2
- 一句话说明：文本截取
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 起始:整数型, 长度:整数型
- 必填参数：文本
- 可选参数：起始, 长度
- 返回结构：{'类型': '结果型', '值结构': {'结果文本': '截取后的文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.文本截取()
- 依赖能力：无
### 文本去首尾（`数据操作支持库.数据集合.文本去首尾`）
- 版本：2.0.2
- 一句话说明：去除文本首尾指定字符
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 字符集:文本型
- 必填参数：文本
- 可选参数：字符集
- 返回结构：{'类型': '结果型', '值结构': {'结果文本': '去首尾后的文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.文本去首尾()
- 依赖能力：无
### 文本重复（`数据操作支持库.数据集合.文本重复`）
- 版本：2.0.2
- 一句话说明：文本重复 N 次
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 次数:整数型
- 必填参数：文本, 次数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'结果文本': '重复后的文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.文本重复()
- 依赖能力：无
### 展平字典（`数据操作支持库.数据集合.展平字典`）
- 版本：2.0.2
- 一句话说明：把嵌套字典展平为单层；键用分隔符连接，可选同时写入末级键名
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数据:字典型, 分隔符:文本型, 保留扁平键:逻辑型, 最大深度:整数型
- 必填参数：数据
- 可选参数：分隔符, 保留扁平键, 最大深度
- 返回结构：{'类型': '结果型', '值结构': {'展平结果': '展平后的单层字典', '键数': '展平后的键数量'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.展平字典()
- 依赖能力：无
### 展平结构条目（`数据操作支持库.数据集合.展平结构条目`）
- 版本：2.0.2
- 一句话说明：把任意嵌套结构展平为「路径 + 值」的有序条目列表；含列表索引、深度上限与条目上限
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数据:JSON值型, 分隔符:文本型, 列表索引格式:文本型, 最大深度:整数型, 最大条目数:整数型, 超深标记:文本型, 根路径:文本型
- 必填参数：无
- 可选参数：数据, 分隔符, 列表索引格式, 最大深度, 最大条目数, 超深标记, 根路径
- 返回结构：{'类型': '结果型', '值结构': {'条目列表': '[{路径, 值, 超深}] 有序条目', '总条目数': '全部条目数（含被截断部分）', '已截断': '是否发生条目截断'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.展平结构条目()
- 依赖能力：无
### 规范化条目指纹（`数据操作支持库.数据集合.规范化条目指纹`）
- 版本：2.0.2
- 一句话说明：把条目集合归一化为可比较的指纹文本；列表与字典两种形态统一
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：条目列表:JSON值型, 字段顺序:列表型, 条目分隔符:文本型, 字段分隔符:文本型, 缺失文本:文本型, 排序:逻辑型
- 必填参数：条目列表, 字段顺序
- 可选参数：条目分隔符, 字段分隔符, 缺失文本, 排序
- 返回结构：{'类型': '结果型', '值结构': {'指纹': '归一化并排序后的指纹文本', '条目数': '参与指纹的条目数'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.规范化条目指纹()
- 依赖能力：无
### 分割文本（`数据操作支持库.文本处理.分割文本`）
- 版本：2.0.2
- 一句话说明：按分隔符分割文本，返回 {值=片段列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 分隔符:文本型
- 必填参数：文本, 分隔符
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '分割后的片段列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.分割文本()
- 依赖能力：无
### 合并文本（`数据操作支持库.文本处理.合并文本`）
- 版本：2.0.2
- 一句话说明：用分隔符连接片段列表，返回 {值=合并后的文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：片段列表:列表型, 分隔符:文本型
- 必填参数：片段列表, 分隔符
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '合并后的文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.合并文本()
- 依赖能力：无
### 替换文本（`数据操作支持库.文本处理.替换文本`）
- 版本：2.0.2
- 一句话说明：把文本中所有旧文本替换为新文本，返回 {值=替换后的文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 旧文本:文本型, 新文本:文本型
- 必填参数：文本, 旧文本, 新文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '替换后的文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.替换文本()
- 依赖能力：无
### 查找文本（`数据操作支持库.文本处理.查找文本`）
- 版本：2.0.2
- 一句话说明：返回目标在文本中的首个位置（未找到返回 -1），返回 {值=位置}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 目标:文本型
- 必填参数：文本, 目标
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '首个位置（未找到为 -1）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.查找文本()
- 依赖能力：无
### 去空白（`数据操作支持库.文本处理.去空白`）
- 版本：2.0.2
- 一句话说明：去除文本首尾空白，返回 {值=去空白后的文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '去空白后的文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.去空白()
- 依赖能力：无
### 转大写（`数据操作支持库.文本处理.转大写`）
- 版本：2.0.2
- 一句话说明：把文本转为大写，返回 {值=大写文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '大写文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.转大写()
- 依赖能力：无
### 转小写（`数据操作支持库.文本处理.转小写`）
- 版本：2.0.2
- 一句话说明：把文本转为小写，返回 {值=小写文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '小写文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.转小写()
- 依赖能力：无
### 统计长度（`数据操作支持库.文本处理.统计长度`）
- 版本：2.0.2
- 一句话说明：统计文本字符长度，返回 {值=长度}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '字符长度（整数）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.统计长度()
- 依赖能力：无
### 按行分割（`数据操作支持库.文本处理.按行分割`）
- 版本：2.0.2
- 一句话说明：按换行符分割文本为行列表，返回 {值=行列表}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '行列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.按行分割()
- 依赖能力：无
### 解析区间（`数据操作支持库.文本处理.解析区间`）
- 版本：2.0.2
- 一句话说明：解析 "1-5,7,10" 形式的页码/行号区间文本；集合模式返回去重升序列表，范围模式返回起止
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 模式:文本型
- 必填参数：无
- 可选参数：文本, 模式
- 返回结构：{'类型': '结果型', '值结构': {'数值列表': '集合模式的去重升序数值列表', '数量': '集合模式的数值个数', '是否有效': '集合模式：解析出数值或原文为空为真；范围模式：成功解析为真', '起始': '范围模式的起始值（非法或空原文为空）', '结束': '范围模式的结束值（非法或空原文为空）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.解析区间()
- 依赖能力：无
### 获取当前时间（`数据操作支持库.时间日期.获取当前时间`）
- 版本：2.0.2
- 一句话说明：返回当前时间的 ISO 格式文本（默认 Asia/Shanghai 时区），返回 {值=ISO时间文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：时区:文本型
- 必填参数：无
- 可选参数：时区
- 返回结构：{'类型': '结果型', '值结构': {'值': '当前时间 ISO 格式文本（含 T，秒精度）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.获取当前时间()
- 依赖能力：无
### 格式化为文本（`数据操作支持库.时间日期.格式化为文本`）
- 版本：2.0.2
- 一句话说明：把时间戳按格式化为文本（Asia/Shanghai 时区），返回 {值=格式化文本}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：时间戳:双精度数型, 格式:文本型
- 必填参数：时间戳
- 可选参数：格式
- 返回结构：{'类型': '结果型', '值结构': {'值': '格式化后的时间文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.格式化为文本()
- 依赖能力：无
### 解析文本时间（`数据操作支持库.时间日期.解析文本时间`）
- 版本：2.0.2
- 一句话说明：按格式解析时间文本为时间戳（Asia/Shanghai 时区），返回 {值=时间戳}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 格式:文本型
- 必填参数：文本
- 可选参数：格式
- 返回结构：{'类型': '结果型', '值结构': {'值': 'Unix 时间戳（浮点数，秒）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.解析文本时间()
- 依赖能力：无
### 时间戳转换（`数据操作支持库.时间日期.时间戳转换`）
- 版本：2.0.2
- 一句话说明：把时间戳转换为年月日时分秒结构（Asia/Shanghai 时区），返回 {值=时间结构}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：时间戳:双精度数型
- 必填参数：时间戳
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '时间结构 {年, 月, 日, 时, 分, 秒}'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.时间戳转换()
- 依赖能力：无
### 计算间隔（`数据操作支持库.时间日期.计算间隔`）
- 版本：2.0.2
- 一句话说明：计算两个时间戳的间隔秒数（保留 3 位小数），返回 {值=间隔秒数}
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：起始时间戳:双精度数型, 结束时间戳:双精度数型
- 必填参数：起始时间戳, 结束时间戳
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '间隔秒数（浮点数）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.计算间隔()
- 依赖能力：无
### 文本转整数（`数据操作支持库.类型转换.文本转整数`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.文本转整数 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 进制:整数型
- 必填参数：文本
- 可选参数：进制
- 返回结构：{'类型': '结果型', '值结构': '整数型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转整数()
- 依赖能力：无
### 文本转长整数（`数据操作支持库.类型转换.文本转长整数`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.文本转长整数 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 进制:整数型
- 必填参数：文本
- 可选参数：进制
- 返回结构：{'类型': '结果型', '值结构': '长整数型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转长整数()
- 依赖能力：无
### 整数转文本（`数据操作支持库.类型转换.整数转文本`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.整数转文本 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：整数:整数型
- 必填参数：整数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.整数转文本()
- 依赖能力：无
### 长整数转文本（`数据操作支持库.类型转换.长整数转文本`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.长整数转文本 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：长整数:长整数型
- 必填参数：长整数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.长整数转文本()
- 依赖能力：无
### 文本转单精度数（`数据操作支持库.类型转换.文本转单精度数`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.文本转单精度数 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '单精度数型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转单精度数()
- 依赖能力：无
### 文本转双精度数（`数据操作支持库.类型转换.文本转双精度数`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.文本转双精度数 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '双精度数型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转双精度数()
- 依赖能力：无
### 数值转文本（`数据操作支持库.类型转换.数值转文本`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.数值转文本 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数值:双精度数型, 小数位数:整数型
- 必填参数：数值
- 可选参数：小数位数
- 返回结构：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.数值转文本()
- 依赖能力：无
### 逻辑转文本（`数据操作支持库.类型转换.逻辑转文本`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.逻辑转文本 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：逻辑值:逻辑型
- 必填参数：逻辑值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '文本型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.逻辑转文本()
- 依赖能力：无
### 文本转逻辑（`数据操作支持库.类型转换.文本转逻辑`）
- 版本：2.0.2
- 一句话说明：数据操作支持库.类型转换.文本转逻辑 确定性原子转换
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '逻辑型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转逻辑()
- 依赖能力：无
### 校验类型（`数据操作支持库.类型转换.校验类型`）
- 版本：2.0.2
- 一句话说明：按公共契约校验值是否属于目标正式类型：数值四型/逻辑型/句柄型委托公共契约，其余正式类型缺公共校验层即明确拒绝
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：值:JSON值型, 目标类型:文本型
- 必填参数：值, 目标类型
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': '逻辑型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.校验类型()
- 依赖能力：无
