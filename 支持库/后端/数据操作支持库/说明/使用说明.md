# 数据操作支持库（支持库.后端.数据操作支持库）

- 类型：支持库
- 版本：2.0.1
- 说明：聚合支持库，包含 5 个子域：数据交换/数据集合/文本处理/时间日期/类型转换
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 数据操作支持库.类型转换.文本转整数（`数据操作支持库.类型转换.文本转整数`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:, 进制:
- 必填参数：文本, 进制
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转整数()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转长整数（`数据操作支持库.类型转换.文本转长整数`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:, 进制:
- 必填参数：文本, 进制
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转长整数()
- 依赖能力：无
### 数据操作支持库.类型转换.整数转文本（`数据操作支持库.类型转换.整数转文本`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：整数:
- 必填参数：整数
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.整数转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.长整数转文本（`数据操作支持库.类型转换.长整数转文本`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：长整数:
- 必填参数：长整数
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.长整数转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转单精度数（`数据操作支持库.类型转换.文本转单精度数`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:
- 必填参数：文本
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转单精度数()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转双精度数（`数据操作支持库.类型转换.文本转双精度数`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:
- 必填参数：文本
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转双精度数()
- 依赖能力：无
### 数据操作支持库.类型转换.数值转文本（`数据操作支持库.类型转换.数值转文本`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数值:, 小数位数:
- 必填参数：数值, 小数位数
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.数值转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.逻辑转文本（`数据操作支持库.类型转换.逻辑转文本`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：逻辑值:
- 必填参数：逻辑值
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.逻辑转文本()
- 依赖能力：无
### 数据操作支持库.类型转换.文本转逻辑（`数据操作支持库.类型转换.文本转逻辑`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:
- 必填参数：文本
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.文本转逻辑()
- 依赖能力：无
### 数据操作支持库.类型转换.校验类型（`数据操作支持库.类型转换.校验类型`）
- 版本：2.0.1
- 一句话说明：（无）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：值:, 目标类型:
- 必填参数：值, 目标类型
- 可选参数：无
- 返回结构：无（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.类型转换.校验类型()
- 依赖能力：无
### 序列化JSON（`数据操作支持库.数据交换.序列化JSON`）
- 版本：2.0.1
- 一句话说明：把字典或列表数据序列化为 JSON 文本
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：数据:字典型
- 必填参数：数据
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.序列化JSON()
- 依赖能力：无
### 反序列化JSON（`数据操作支持库.数据交换.反序列化JSON`）
- 版本：2.0.1
- 一句话说明：把 JSON 文本解析为字典或列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.反序列化JSON()
- 依赖能力：无
### 序列化CSV（`数据操作支持库.数据交换.序列化CSV`）
- 版本：2.0.1
- 一句话说明：把行列表序列化为 CSV 文本
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：行列表:列表型
- 必填参数：行列表
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.序列化CSV()
- 依赖能力：无
### 反序列化CSV（`数据操作支持库.数据交换.反序列化CSV`）
- 版本：2.0.1
- 一句话说明：把 CSV 文本解析为行列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据交换.反序列化CSV()
- 依赖能力：无
### 获取当前时间（`数据操作支持库.时间日期.获取当前时间`）
- 版本：2.0.1
- 一句话说明：返回当前时间的 ISO 格式文本
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：时区:文本型
- 必填参数：无
- 可选参数：时区
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.获取当前时间()
- 依赖能力：无
### 格式化为文本（`数据操作支持库.时间日期.格式化为文本`）
- 版本：2.0.1
- 一句话说明：把时间戳按格式化为文本
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：时间戳:双精度数型, 格式:文本型
- 必填参数：时间戳
- 可选参数：格式
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.格式化为文本()
- 依赖能力：无
### 解析文本时间（`数据操作支持库.时间日期.解析文本时间`）
- 版本：2.0.1
- 一句话说明：按格式解析时间文本为时间戳
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 格式:文本型
- 必填参数：文本
- 可选参数：格式
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.解析文本时间()
- 依赖能力：无
### 时间戳转换（`数据操作支持库.时间日期.时间戳转换`）
- 版本：2.0.1
- 一句话说明：把时间戳转换为年月日时分秒结构
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：时间戳:双精度数型
- 必填参数：时间戳
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.时间戳转换()
- 依赖能力：无
### 计算间隔（`数据操作支持库.时间日期.计算间隔`）
- 版本：2.0.1
- 一句话说明：计算两个时间戳的间隔秒数
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：起始时间戳:双精度数型, 结束时间戳:双精度数型
- 必填参数：起始时间戳, 结束时间戳
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.时间日期.计算间隔()
- 依赖能力：无
### 列表添加（`数据操作支持库.数据集合.列表添加`）
- 版本：2.0.1
- 一句话说明：追加条目并返回新列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 条目:文本型
- 必填参数：列表, 条目
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表添加()
- 依赖能力：无
### 列表移除（`数据操作支持库.数据集合.列表移除`）
- 版本：2.0.1
- 一句话说明：移除首个匹配条目并返回新列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 条目:文本型
- 必填参数：列表, 条目
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表移除()
- 依赖能力：无
### 列表排序（`数据操作支持库.数据集合.列表排序`）
- 版本：2.0.1
- 一句话说明：排序列表并返回新列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 倒序:逻辑型
- 必填参数：列表
- 可选参数：倒序
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表排序()
- 依赖能力：无
### 列表查找（`数据操作支持库.数据集合.列表查找`）
- 版本：2.0.1
- 一句话说明：返回首个匹配条目索引（未找到返回 -1）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 目标:文本型
- 必填参数：列表, 目标
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表查找()
- 依赖能力：无
### 字典设置（`数据操作支持库.数据集合.字典设置`）
- 版本：2.0.1
- 一句话说明：设置键值并返回新字典
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：字典:字典型, 键:文本型, 值:文本型
- 必填参数：字典, 键, 值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.字典设置()
- 依赖能力：无
### 字典获取（`数据操作支持库.数据集合.字典获取`）
- 版本：2.0.1
- 一句话说明：获取键值（键不存在返回默认值）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：字典:字典型, 键:文本型, 默认值:文本型
- 必填参数：字典, 键
- 可选参数：默认值
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.字典获取()
- 依赖能力：无
### 集合取并集（`数据操作支持库.数据集合.集合取并集`）
- 版本：2.0.1
- 一句话说明：返回两个列表的去重并集
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：集合1:列表型, 集合2:列表型
- 必填参数：集合1, 集合2
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.集合取并集()
- 依赖能力：无
### 集合取交集（`数据操作支持库.数据集合.集合取交集`）
- 版本：2.0.1
- 一句话说明：返回两个列表的去重交集
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：集合1:列表型, 集合2:列表型
- 必填参数：集合1, 集合2
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.集合取交集()
- 依赖能力：无
### 统计数量（`数据操作支持库.数据集合.统计数量`）
- 版本：2.0.1
- 一句话说明：返回列表条目数量
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型
- 必填参数：列表
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.统计数量()
- 依赖能力：无
### 按字段查找（`数据操作支持库.数据集合.按字段查找`）
- 版本：2.0.1
- 一句话说明：按字段查找第一个匹配项
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段名:文本型, 匹配值:文本型, 直接匹配:逻辑型
- 必填参数：列表, 字段名, 匹配值
- 可选参数：直接匹配
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.按字段查找()
- 依赖能力：无
### 过滤（`数据操作支持库.数据集合.过滤`）
- 版本：2.0.1
- 一句话说明：按字段过滤返回新列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段名:文本型, 等于值:文本型, 直接匹配:逻辑型
- 必填参数：列表, 字段名, 等于值
- 可选参数：直接匹配
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.过滤()
- 依赖能力：无
### 映射转换（`数据操作支持库.数据集合.映射转换`）
- 版本：2.0.1
- 一句话说明：按字段映射重命名项目字段并返回新列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段映射:字典型
- 必填参数：列表, 字段映射
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.映射转换()
- 依赖能力：无
### 按字段排序（`数据操作支持库.数据集合.按字段排序`）
- 版本：2.0.1
- 一句话说明：按字段或元素排序并返回新列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 字段名:文本型, 升序:逻辑型, 直接匹配:逻辑型
- 必填参数：列表, 字段名
- 可选参数：升序, 直接匹配
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.按字段排序()
- 依赖能力：无
### 列表去重（`数据操作支持库.数据集合.列表去重`）
- 版本：2.0.1
- 一句话说明：列表去重（保持原顺序）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型
- 必填参数：列表
- 可选参数：无
- 返回结构：{结果列表, 数量}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表去重()
- 依赖能力：无
### 列表倒序（`数据操作支持库.数据集合.列表倒序`）
- 版本：2.0.1
- 一句话说明：列表倒序
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型
- 必填参数：列表
- 可选参数：无
- 返回结构：{结果列表}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表倒序()
- 依赖能力：无
### 列表分片（`数据操作支持库.数据集合.列表分片`）
- 版本：2.0.1
- 一句话说明：列表分片
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：列表:列表型, 起始:整数型, 结束:整数型
- 必填参数：列表
- 可选参数：起始, 结束
- 返回结构：{结果列表}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.列表分片()
- 依赖能力：无
### 字典合并（`数据操作支持库.数据集合.字典合并`）
- 版本：2.0.1
- 一句话说明：合并多个字典（后者覆盖）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：字典列表:列表型
- 必填参数：字典列表
- 可选参数：无
- 返回结构：{结果字典, 键数量}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.字典合并()
- 依赖能力：无
### 文本截取（`数据操作支持库.数据集合.文本截取`）
- 版本：2.0.1
- 一句话说明：文本截取
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 起始:整数型, 长度:整数型
- 必填参数：文本
- 可选参数：起始, 长度
- 返回结构：{结果文本}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.文本截取()
- 依赖能力：无
### 文本去首尾（`数据操作支持库.数据集合.文本去首尾`）
- 版本：2.0.1
- 一句话说明：去除文本首尾指定字符
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 字符集:文本型
- 必填参数：文本
- 可选参数：字符集
- 返回结构：{结果文本}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.文本去首尾()
- 依赖能力：无
### 文本重复（`数据操作支持库.数据集合.文本重复`）
- 版本：2.0.1
- 一句话说明：文本重复 N 次
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 次数:整数型
- 必填参数：文本, 次数
- 可选参数：无
- 返回结构：{结果文本}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.数据集合.文本重复()
- 依赖能力：无
### 分割文本（`数据操作支持库.文本处理.分割文本`）
- 版本：2.0.1
- 一句话说明：按分隔符分割文本返回片段列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 分隔符:文本型
- 必填参数：文本, 分隔符
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.分割文本()
- 依赖能力：无
### 合并文本（`数据操作支持库.文本处理.合并文本`）
- 版本：2.0.1
- 一句话说明：用分隔符连接片段列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：片段列表:列表型, 分隔符:文本型
- 必填参数：片段列表, 分隔符
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.合并文本()
- 依赖能力：无
### 替换文本（`数据操作支持库.文本处理.替换文本`）
- 版本：2.0.1
- 一句话说明：把文本中所有旧文本替换为新文本
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 旧文本:文本型, 新文本:文本型
- 必填参数：文本, 旧文本, 新文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.替换文本()
- 依赖能力：无
### 查找文本（`数据操作支持库.文本处理.查找文本`）
- 版本：2.0.1
- 一句话说明：返回目标在文本中的首个位置（未找到返回 -1）
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型, 目标:文本型
- 必填参数：文本, 目标
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.查找文本()
- 依赖能力：无
### 去空白（`数据操作支持库.文本处理.去空白`）
- 版本：2.0.1
- 一句话说明：去除文本首尾空白
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.去空白()
- 依赖能力：无
### 转大写（`数据操作支持库.文本处理.转大写`）
- 版本：2.0.1
- 一句话说明：把文本转为大写
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.转大写()
- 依赖能力：无
### 转小写（`数据操作支持库.文本处理.转小写`）
- 版本：2.0.1
- 一句话说明：把文本转为小写
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.转小写()
- 依赖能力：无
### 统计长度（`数据操作支持库.文本处理.统计长度`）
- 版本：2.0.1
- 一句话说明：统计文本字符长度
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.统计长度()
- 依赖能力：无
### 按行分割（`数据操作支持库.文本处理.按行分割`）
- 版本：2.0.1
- 一句话说明：按换行符分割文本为行列表
- 所属支持库或模块：支持库.后端.数据操作支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：数据操作支持库.文本处理.按行分割()
- 依赖能力：无
