# 数据交换 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.数据操作支持库.数据交换
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据操作支持库.数据交换.序列化JSON | 序列化JSON | 1.0.0 | {'类型': '结果型', '值结构': {'值': 'JSON 文本字符串（成功时）'}} | 把字典或列表数据序列化为 JSON 文本（ensure_ascii=False，缩进 2），返回 {值=JSON文本} |
| 数据操作支持库.数据交换.反序列化JSON | 反序列化JSON | 1.0.0 | {'类型': '结果型', '值结构': {'值': '解析后的字典或列表（成功时）'}} | 把 JSON 文本解析为字典或列表，返回 {值=解析后的数据} |
| 数据操作支持库.数据交换.序列化CSV | 序列化CSV | 1.0.0 | {'类型': '结果型', '值结构': {'值': 'CSV 文本字符串（成功时）'}} | 把行列表序列化为 CSV 文本，返回 {值=CSV文本} |
| 数据操作支持库.数据交换.反序列化CSV | 反序列化CSV | 1.0.0 | {'类型': '结果型', '值结构': {'值': '行列表（成功时，每行为字符串单元格列表）'}} | 把 CSV 文本解析为行列表，返回 {值=行列表} |

## 数据操作支持库.数据交换.序列化JSON（序列化JSON）

**说明**：把字典或列表数据序列化为 JSON 文本（ensure_ascii=False，缩进 2），返回 {值=JSON文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数据 | 字典型 | 是 | None | 要序列化的数据（字典或列表，须可 JSON 序列化；不可序列化返回 序列化失败） |

**返回结构**：{'类型': '结果型', '值结构': {'值': 'JSON 文本字符串（成功时）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 序列化失败 |

**调用示例**：

```python
数据操作支持库.数据交换.序列化JSON(数据=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.反序列化JSON（反序列化JSON）

**说明**：把 JSON 文本解析为字典或列表，返回 {值=解析后的数据}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | JSON 文本；非法 JSON 返回 反序列化失败 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '解析后的字典或列表（成功时）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 反序列化失败 |

**调用示例**：

```python
数据操作支持库.数据交换.反序列化JSON(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.序列化CSV（序列化CSV）

**说明**：把行列表序列化为 CSV 文本，返回 {值=CSV文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 行列表 | 列表型 | 是 | None | 行列表（每行为单元格列表）；非法结构返回 序列化失败 |

**返回结构**：{'类型': '结果型', '值结构': {'值': 'CSV 文本字符串（成功时）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 序列化失败 |

**调用示例**：

```python
数据操作支持库.数据交换.序列化CSV(行列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.反序列化CSV（反序列化CSV）

**说明**：把 CSV 文本解析为行列表，返回 {值=行列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | CSV 文本；非法 CSV 返回 反序列化失败 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '行列表（成功时，每行为字符串单元格列表）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 反序列化失败 |

**调用示例**：

```python
数据操作支持库.数据交换.反序列化CSV(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.深合并（深合并）

**说明**：递归合并两个字典：同键且两边都是字典时递归合并，否则用覆盖值替换；不修改入参，返回 {合并结果,键数}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 基础 | 字典型 | 是 | None | 基础字典 |
| 覆盖 | 字典型 | 是 | None | 覆盖字典 |

**返回结构**：{"类型": "结果型", "值结构": {"合并结果": "递归合并后的新字典", "键数": "顶层键数量"}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据交换.深合并(基础=值、覆盖=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.解析YAML（解析YAML）

**说明**：把 YAML 文本解析为数据（经适配层 YAML提供者），返回 {值=解析后的数据}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 |  | YAML 文本 |

**返回结构**：{"类型": "结果型", "值结构": {"值": "解析后的数据（成功时）"}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 解析失败 |

**调用示例**：

```python
数据操作支持库.数据交换.解析YAML(文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.序列化YAML（序列化YAML）

**说明**：把数据序列化为 YAML 文本（经适配层 YAML提供者），返回 {值=YAML文本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数据 | JSON值型 | 是 |  | 要序列化的数据 |

**返回结构**：{"类型": "结果型", "值结构": {"值": "YAML 文本字符串（成功时）"}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 序列化失败 |

**调用示例**：

```python
数据操作支持库.数据交换.序列化YAML(数据=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.提取带前缀JSON（提取带前缀JSON）

**说明**：从文本中提取带指定前缀的 JSON 行（默认从末尾找）；剥前缀后必须是 JSON 对象，返回 {找到,数据,原因}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文本 | 文本型 | 是 | None | 被检索的多行文本 |
| 前缀 | 文本型 | 是 | None | 行前缀，如 @@结果@@ |
| 从末尾查找 | 逻辑型 | 否 | True | 默认 真 |

**返回结构**：{"类型": "结果型", "值结构": {"找到": "是否命中（布尔）", "数据": "命中的 JSON 对象或 null", "原因": "未命中原因（仅未命中时出现）"}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据交换.提取带前缀JSON(文本=值、前缀=值、从末尾查找=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据操作支持库.数据交换.压缩描述字段（压缩描述字段）

**说明**：递归折叠指定字段的字符串值（折叠空白），只动该字段不改结构与其它键，返回 {压缩结果,字段名}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数据 | 字典型 | 是 | None | 任意 JSON 结构 |
| 字段名 | 文本型 | 否 | description | 要折叠的字段名 |

**返回结构**：{"类型": "结果型", "值结构": {"压缩结果": "折叠后的新结构", "字段名": "实际处理的字段名"}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据操作支持库.数据交换.压缩描述字段(数据=值、字段名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
