# 数据交换（支持库.后端.数据操作支持库.数据交换）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.2.3
- 说明：原子数据交换能力：JSON 与 CSV 的序列化/反序列化。只依赖 Python 标准库，无第三方依赖。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 序列化JSON（`数据操作支持库.数据交换.序列化JSON`）
- 版本：1.0.0
- 一句话说明：把字典或列表数据序列化为 JSON 文本（ensure_ascii=False，缩进 2），返回 {值=JSON文本}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：数据:字典型
- 必填参数：数据
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': 'JSON 文本字符串（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 序列化失败
- 调用示例：数据操作支持库.数据交换.序列化JSON()
- 依赖能力：无
### 反序列化JSON（`数据操作支持库.数据交换.反序列化JSON`）
- 版本：1.0.0
- 一句话说明：把 JSON 文本解析为字典或列表，返回 {值=解析后的数据}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '解析后的字典或列表（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 反序列化失败
- 调用示例：数据操作支持库.数据交换.反序列化JSON()
- 依赖能力：无
### 序列化CSV（`数据操作支持库.数据交换.序列化CSV`）
- 版本：1.0.0
- 一句话说明：把行列表序列化为 CSV 文本，返回 {值=CSV文本}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：行列表:列表型
- 必填参数：行列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': 'CSV 文本字符串（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 序列化失败
- 调用示例：数据操作支持库.数据交换.序列化CSV()
- 依赖能力：无
### 反序列化CSV（`数据操作支持库.数据交换.反序列化CSV`）
- 版本：1.1.0
- 一句话说明：把 CSV 文本解析为行列表（可选分隔符与严格校验），返回 {值=行列表}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：文本:文本型, 分隔符:文本型, 严格模式:逻辑型
- 必填参数：文本
- 可选参数：分隔符, 严格模式
- 返回结构：{'类型': '结果型', '值结构': {'值': '行列表（成功时，每行为字符串单元格列表）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 反序列化失败
- 调用示例：数据操作支持库.数据交换.反序列化CSV(文本=a,b
1,2, 分隔符=,, 严格模式=True)
- 依赖能力：无
### 深合并（`数据操作支持库.数据交换.深合并`）
- 版本：1.0.0
- 一句话说明：递归合并两个字典：同键且两边都是字典时递归合并，否则用覆盖值替换；不修改入参，返回 {合并结果,键数}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：基础:字典型, 覆盖:字典型
- 必填参数：基础, 覆盖
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'合并结果': '递归合并后的新字典', '键数': '顶层键数量'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.数据交换.深合并()
- 依赖能力：无
### 解析YAML（`数据操作支持库.数据交换.解析YAML`）
- 版本：1.0.0
- 一句话说明：把 YAML 文本解析为数据（经适配层 YAML提供者），返回 {值=解析后的数据}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '解析后的数据（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 解析失败
- 调用示例：数据操作支持库.数据交换.解析YAML()
- 依赖能力：无
### 序列化YAML（`数据操作支持库.数据交换.序列化YAML`）
- 版本：1.0.0
- 一句话说明：把数据序列化为 YAML 文本（经适配层 YAML提供者），返回 {值=YAML文本}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：数据:JSON值型
- 必填参数：无
- 可选参数：数据
- 返回结构：{'类型': '结果型', '值结构': {'值': 'YAML 文本字符串（成功时）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 序列化失败
- 调用示例：数据操作支持库.数据交换.序列化YAML()
- 依赖能力：无
### 提取带前缀JSON（`数据操作支持库.数据交换.提取带前缀JSON`）
- 版本：1.0.0
- 一句话说明：从文本中提取带指定前缀的 JSON 行（默认从末尾找）；剥前缀后必须是 JSON 对象，返回 {找到,数据,原因}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：文本:文本型, 前缀:文本型, 从末尾查找:逻辑型
- 必填参数：文本, 前缀
- 可选参数：从末尾查找
- 返回结构：{'类型': '结果型', '值结构': {'找到': '是否命中（布尔）', '数据': '命中的 JSON 对象或 null', '原因': '未命中原因（仅未命中时出现）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.数据交换.提取带前缀JSON(从末尾查找=True)
- 依赖能力：无
### 压缩描述字段（`数据操作支持库.数据交换.压缩描述字段`）
- 版本：1.0.0
- 一句话说明：递归折叠指定字段的字符串值（折叠空白），只动该字段不改结构与其它键，返回 {压缩结果,字段名}
- 所属支持库或模块：支持库.后端.数据操作支持库.数据交换
- 参数列表：数据:字典型, 字段名:文本型
- 必填参数：数据
- 可选参数：字段名
- 返回结构：{'类型': '结果型', '值结构': {'压缩结果': '折叠后的新结构', '字段名': '实际处理的字段名'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据操作支持库.数据交换.压缩描述字段(字段名=description)
- 依赖能力：无
