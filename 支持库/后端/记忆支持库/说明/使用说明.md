# 记忆支持库（支持库.后端.记忆支持库）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.3
- 说明：项目记忆读写/搜索/管理（独立，链接SQLite原子能力；向量生成上游调用模型连接器）
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `大语言模型支持库.模型连接器.连接向量模型` 版本约束 `>=1.0.0`
- 能力 `大语言模型支持库.模型连接器.生成嵌入` 版本约束 `>=1.0.0`
- 能力 `大语言模型支持库.模型连接器.释放句柄` 版本约束 `>=1.0.0`

## 能力
### 写入记忆（`记忆支持库.写入记忆`）
- 版本：1.0.0
- 一句话说明：写入一条记忆（自动生成向量）；可选按项目归属
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：名称:文本型, 正文:文本型, 记忆类型:文本型, 深度:文本型, 标签:列表型, 智能体:文本型, 状态:文本型, 标识:文本型, 项目:文本型
- 必填参数：名称, 正文
- 可选参数：记忆类型, 深度, 标签, 智能体, 状态, 标识, 项目
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 存储失败
- 调用示例：记忆支持库.写入记忆(记忆类型=参考, 深度=standard, 状态=已保存)
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 追加记忆（`记忆支持库.追加记忆`）
- 版本：1.0.0
- 一句话说明：追加更新已有记忆（找不到则新建）；可选在指定项目内追加
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：标题:文本型, 细节:文本型, 记忆类型:文本型, 项目:文本型
- 必填参数：标题, 细节
- 可选参数：记忆类型, 项目
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 存储失败
- 调用示例：记忆支持库.追加记忆(记忆类型=参考)
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 完成记忆（`记忆支持库.完成记忆`）
- 版本：1.0.0
- 一句话说明：标记记忆完成
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：标题:文本型
- 必填参数：标题
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 存储失败
- 调用示例：记忆支持库.完成记忆()
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 删除记忆（`记忆支持库.删除记忆`）
- 版本：1.0.0
- 一句话说明：删除记忆（含向量）
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：标识:文本型
- 必填参数：标识
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 存储失败
- 调用示例：记忆支持库.删除记忆()
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 搜索记忆（`记忆支持库.搜索记忆`）
- 版本：1.0.0
- 一句话说明：语义搜索记忆（向量+关键词兜底）；可选按项目过滤；仅关键词=true 时完全不调用大模型与向量
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：查询:文本型, 返回条数:整数型, 项目:文本型, 仅关键词:逻辑型
- 必填参数：查询
- 可选参数：返回条数, 项目, 仅关键词
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 查询失败
- 调用示例：记忆支持库.搜索记忆(返回条数=5)
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 最近记忆（`记忆支持库.最近记忆`）
- 版本：1.0.0
- 一句话说明：最近记忆列表；可选按项目过滤
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：数量:整数型, 项目:文本型
- 必填参数：无
- 可选参数：数量, 项目
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 查询失败
- 调用示例：记忆支持库.最近记忆(数量=10)
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 列出记忆（`记忆支持库.列出记忆`）
- 版本：1.0.0
- 一句话说明：列出全部记忆；可选按项目过滤
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：项目:文本型
- 必填参数：无
- 可选参数：项目
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 查询失败
- 调用示例：记忆支持库.列出记忆()
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 生成标识（`记忆支持库.生成标识`）
- 版本：1.0.0
- 一句话说明：由标题生成记忆标识
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：标题:文本型
- 必填参数：标题
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：记忆支持库.生成标识()
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
### 解析配置回退链（`记忆支持库.解析配置回退链`）
- 版本：1.0.0
- 一句话说明：按 精确→泛化→全局 三级回退解析配置（TencentDB mps 模式化落地），返回匹配目标与回退路径
- 所属支持库或模块：支持库.后端.记忆支持库
- 参数列表：目标类型:文本型, 目标标识:文本型, 层级:文本型, 配置表:列表型
- 必填参数：目标类型, 目标标识, 配置表
- 可选参数：层级
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 解析失败
- 调用示例：记忆支持库.解析配置回退链()
- 依赖能力：大语言模型支持库.模型连接器.连接向量模型, 大语言模型支持库.模型连接器.生成嵌入, 大语言模型支持库.模型连接器.释放句柄
