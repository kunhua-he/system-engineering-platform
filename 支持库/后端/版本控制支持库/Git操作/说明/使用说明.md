# Git操作 使用说明

- 包id：支持库.后端.版本控制支持库.Git操作
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 版本控制支持库.Git操作.检查提供者 | 检查提供者 | 1.0.0 | 结果型 | git --version 真实探针：返回 {git: 可用, 版本}；缺失/超时 → 提供者不可用 |
| 版本控制支持库.Git操作.创建工作区 | 创建工作区 | 1.0.0 | 结果型 | git worktree add [-b 分支名] 路径 [起始点]：新建独立工作区，返回 {路径, 分支} |
| 版本控制支持库.Git操作.查询工作区 | 查询工作区 | 1.0.0 | 结果型 | git worktree list --porcelain：返回 {工作区列表: [{路径, 分支, 提交}]} |
| 版本控制支持库.Git操作.关闭工作区 | 关闭工作区 | 1.0.0 | 结果型 | git worktree remove 非强制关闭：先查未提交修改，存在 → 未提交修改 拒绝 |
| 版本控制支持库.Git操作.提交 | 提交 | 1.0.0 | 结果型 | git add 指定路径 + git commit 最小原子提交；路径越界 → 路径越界 |
| 版本控制支持库.Git操作.回滚 | 回滚 | 1.0.0 | 结果型 | git revert --no-edit 撤销指定提交；冲突自动 --abort 恢复干净状态 |
| 版本控制支持库.Git操作.挑拣合入 | 挑拣合入 | 1.0.0 | 结果型 | git cherry-pick 挑拣指定提交到当前分支；冲突自动 --abort 恢复干净状态 |
| 版本控制支持库.Git操作.当前状态 | 当前状态 | 1.0.0 | 结果型 | status/log 摘要：{分支, 未提交修改, 最近提交} |
| 版本控制支持库.Git操作.获取当前提交哈希 | 获取当前提交哈希 | 1.0.0 | 结果型 | git rev-parse HEAD + rev-parse --abbrev-ref HEAD：返回 {提交哈希, 分支}；正常分支返回分支名，detached HEAD 时分支为 HEAD；非仓库目录 → 命令失败 |

## 版本控制支持库.Git操作.检查提供者（检查提供者）

**说明**：git --version 真实探针：返回 {git: 可用, 版本}；缺失/超时 → 提供者不可用

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 15 | 版本探针超时秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 提供者不可用 |

**调用示例**：

```python
版本控制支持库.Git操作.检查提供者(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.创建工作区（创建工作区）

**说明**：git worktree add [-b 分支名] 路径 [起始点]：新建独立工作区，返回 {路径, 分支}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 新路径 | 文本型 | 是 | None | 新工作区绝对路径（白名单：禁空格/换行/;|&`$） |
| 分支名 | 文本型 | 否 | None | 可选新分支名（白名单） |
| 起始点 | 文本型 | 否 | None | 可选提交哈希或分支名 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.创建工作区(仓库路径=值, 新路径=值, 分支名=值, 起始点=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.查询工作区（查询工作区）

**说明**：git worktree list --porcelain：返回 {工作区列表: [{路径, 分支, 提交}]}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.查询工作区(仓库路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.关闭工作区（关闭工作区）

**说明**：git worktree remove 非强制关闭：先查未提交修改，存在 → 未提交修改 拒绝

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 目标路径 | 文本型 | 是 | None | 待关闭工作区绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 未提交修改 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.关闭工作区(仓库路径=值, 目标路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.提交（提交）

**说明**：git add 指定路径 + git commit 最小原子提交；路径越界 → 路径越界

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库（或工作区）目录绝对路径 |
| 路径列表 | 列表型 | 是 | None | 待提交路径绝对路径列表（白名单+越界校验） |
| 消息 | 文本型 | 是 | None | 单行提交消息（禁换行/控制字符） |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 路径越界 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.提交(仓库路径=值, 路径列表=值, 消息=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.回滚（回滚）

**说明**：git revert --no-edit 撤销指定提交；冲突自动 --abort 恢复干净状态

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库（或工作区）目录绝对路径 |
| 提交哈希 | 文本型 | 是 | None | 7-40 位十六进制提交哈希 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 冲突 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.回滚(仓库路径=值, 提交哈希=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.挑拣合入（挑拣合入）

**说明**：git cherry-pick 挑拣指定提交到当前分支；冲突自动 --abort 恢复干净状态

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库（或工作区）目录绝对路径 |
| 提交哈希 | 文本型 | 是 | None | 7-40 位十六进制提交哈希 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 冲突 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.挑拣合入(仓库路径=值, 提交哈希=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.当前状态（当前状态）

**说明**：status/log 摘要：{分支, 未提交修改, 最近提交}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库（或工作区）目录绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 仓库不存在 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
版本控制支持库.Git操作.当前状态(仓库路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.获取当前提交哈希（获取当前提交哈希）

**说明**：git rev-parse HEAD + rev-parse --abbrev-ref HEAD：返回 {提交哈希, 分支}；正常分支返回分支名，detached HEAD 时分支为 HEAD；非仓库目录 → 命令失败

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 命令失败 |
| 提供者不可用 |
| 超时 |
| 取消 |

**调用示例**：

```python
版本控制支持库.Git操作.获取当前提交哈希(仓库路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 版本控制支持库.Git操作.校验分支名（校验分支名）

**说明**：校验分支名是否安全（git check-ref-format 常用子集）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 分支名 | 文本型 | 是 | None | 待校验的分支名 |

**返回结构**：（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
版本控制支持库.Git操作.校验分支名(分支名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 版本控制支持库.Git操作.建议分支名（建议分支名）

**说明**：按前缀与时间戳生成建议分支名（前缀非法时回退默认）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 前缀 | 文本型 | 否 | None | 分支前缀 |
| 时间戳 | 文本型 | 否 | None | 显式时间戳 |

**返回结构**：（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
版本控制支持库.Git操作.建议分支名(前缀=值, 时间戳=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）
