# 并发控制支持库 使用说明

- 包id：支持库.后端.并发控制支持库
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 并发控制支持库.创建互斥锁 | 创建互斥锁 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建互斥锁，返回句柄 |
| 并发控制支持库.创建信号量 | 创建信号量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建信号量（计数），返回句柄 |
| 并发控制支持库.创建线程池 | 创建线程池 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建线程池，返回句柄 |
| 并发控制支持库.获取锁 | 获取锁 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 获取互斥锁 |
| 并发控制支持库.释放锁 | 释放锁 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放互斥锁 |
| 并发控制支持库.获取信号量 | 获取信号量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 获取信号量（计数减1） |
| 并发控制支持库.释放信号量 | 释放信号量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放信号量（计数加1） |
| 并发控制支持库.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放并发资源句柄（幂等，线程池关闭） |

## 并发控制支持库.创建互斥锁（创建互斥锁）

**说明**：创建互斥锁，返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 整数型 | 否 | None | 句柄超时，默认30分钟 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
并发控制支持库.创建互斥锁(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.创建信号量（创建信号量）

**说明**：创建信号量（计数），返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 初始值 | 整数型 | 否 | 1 | 信号量初始计数 |
| 超时秒 | 整数型 | 否 | None | 句柄超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
并发控制支持库.创建信号量(初始值=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.创建线程池（创建线程池）

**说明**：创建线程池，返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 最大线程数 | 整数型 | 否 | 4 | 最大线程数 |
| 超时秒 | 整数型 | 否 | None | 句柄超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建失败 |

**调用示例**：

```python
并发控制支持库.创建线程池(最大线程数=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.获取锁（获取锁）

**说明**：获取互斥锁

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 互斥锁句柄 |
| 超时秒 | 双精度数型 | 否 | None | 等待超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.获取锁(句柄=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.释放锁（释放锁）

**说明**：释放互斥锁

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 互斥锁句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.释放锁(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.获取信号量（获取信号量）

**说明**：获取信号量（计数减1）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 信号量句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.获取信号量(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.释放信号量（释放信号量）

**说明**：释放信号量（计数加1）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 信号量句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.释放信号量(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 并发控制支持库.释放句柄（释放句柄）

**说明**：释放并发资源句柄（幂等，线程池关闭）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 并发资源句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
并发控制支持库.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
