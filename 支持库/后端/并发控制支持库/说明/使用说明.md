# 并发控制支持库 使用说明

- 包id：支持库.后端.并发控制支持库
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 并发控制支持库.lane完成 | lane完成 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 上报完成/失败，失败附退避秒 |
| 并发控制支持库.lane排入 | lane排入 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 排入任务；队列满直接拒绝 |
| 并发控制支持库.lane查询 | lane查询 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询 lane 状态（空=全部） |
| 并发控制支持库.lane领取 | lane领取 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 微任务泵领取可执行槽位 |
| 并发控制支持库.主体串行完成 | 主体串行完成 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 主体桶任务完成上报 |
| 并发控制支持库.主体串行排入 | 主体串行排入 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 主体桶内排入任务（同主体排队串行） |
| 并发控制支持库.主体串行查询 | 主体串行查询 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询主体桶状态（空=全部） |
| 并发控制支持库.主体串行领取 | 主体串行领取 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 从主体桶领取（并发上限1=同主体同时最多1执行） |
| 并发控制支持库.创建lane | 创建lane调度器 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建 lane 调度器（OpenClaw Swarm 模式化：limit+active+queue），同名复用 |
| 并发控制支持库.创建事件总线 | 创建事件总线 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建事件总线（通知/决策双语义，订阅者队列有界） |
| 并发控制支持库.创建互斥锁 | 创建互斥锁 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建互斥锁，返回句柄 |
| 并发控制支持库.创建信号量 | 创建信号量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建信号量（计数），返回句柄 |
| 并发控制支持库.创建容量闸门 | 创建容量闸门 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建容量闸门，上限>0，同名复用（Hermes async_delegation 同锁容量检查模式化） |
| 并发控制支持库.创建并发组 | 创建并发组 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建并发组（一组任务并发跑；失败即停/收口不打断双语义） |
| 并发控制支持库.创建流管道 | 创建流管道 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建流管道（生产者写入/消费者读取，有界队列） |
| 并发控制支持库.创建线程池 | 创建线程池 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建线程池，返回句柄 |
| 并发控制支持库.占用容量 | 占用容量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 同锁检查+占用；超限直接拒绝不排队，可等待重试 |
| 并发控制支持库.发布决策 | 发布决策 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 发布决策类事件（waterfall：按序询问，显式 继续 才问下一个） |
| 并发控制支持库.发布通知 | 发布通知 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 发布通知类事件（广播给所有通知订阅者） |
| 并发控制支持库.取消订阅 | 取消订阅 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 取消订阅（按序号移除） |
| 并发控制支持库.查询事件总线 | 查询事件总线 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询事件总线状态（句柄空=列全部） |
| 并发控制支持库.查询容量闸门 | 查询容量闸门 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询闸门状态（空=全部） |
| 并发控制支持库.查询并发组 | 查询并发组 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询并发组状态（句柄空=列全部） |
| 并发控制支持库.查询流管道 | 查询流管道 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询流管道状态（句柄空=列全部） |
| 并发控制支持库.登记主体桶 | 登记主体桶 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 登记主体串行桶（OpenClaw ALS 模式化：同主体串行，内部 lane 并发上限1） |
| 并发控制支持库.管道写入 | 管道写入 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 向流管道写入一条事件（队列满返回 队列已满） |
| 并发控制支持库.管道读取 | 管道读取 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 从流管道读取事件（FIFO 出队） |
| 并发控制支持库.组内完成 | 组内完成 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 上报任务完成（执行→结束；返回组累计统计） |
| 并发控制支持库.组内提交 | 组内提交 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 向并发组提交任务（排队；任务名重复返回 已存在） |
| 并发控制支持库.组内等待 | 组内等待 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 等待并发组全部结束（轮询；超时返回仍在执行快照） |
| 并发控制支持库.组内领取 | 组内领取 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 从并发组领取可执行任务（受限并发上限；失败即停模式组已失败则拒绝） |
| 并发控制支持库.获取信号量 | 获取信号量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 获取信号量（计数减1） |
| 并发控制支持库.获取锁 | 获取锁 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 获取互斥锁 |
| 并发控制支持库.订阅事件 | 订阅事件 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 订阅事件（模式=通知广播 或 决策按序询问） |
| 并发控制支持库.释放信号量 | 释放信号量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放信号量（计数加1） |
| 并发控制支持库.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放并发资源句柄（幂等，线程池关闭） |
| 并发控制支持库.释放容量 | 释放容量 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放闸门容量（幂等） |
| 并发控制支持库.释放锁 | 释放锁 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放互斥锁 |
| 并发控制支持库.结构化任务结果 | 结构化任务结果 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '终态': '文本型', '值': 'JSON值型', '错误说明': '文本型'}} | 异步任务结果收口：严格/收口双模式，把成功值/取消/异常统一收敛为结构化终态（成功/取消/异常），不向调用方抛异常 |

## 并发控制支持库.lane完成（lane完成）

**说明**：上报完成/失败，失败附退避秒

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | lane 名称 |
| 数量 | 整数型 | 是 | None | 完成数 |
| 失败数 | 整数型 | 否 | 0 | 失败数 |
| 退避秒 | 数值型 | 否 | 0 | 退避秒 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| lane不存在 |
| 完成上报失败 |

**调用示例**：

```python
并发控制支持库.lane完成(名称=值, 数量=值, 失败数=值, 退避秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.lane排入（lane排入）

**说明**：排入任务；队列满直接拒绝

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | lane 名称 |
| 任务数 | 整数型 | 否 | 1 | 任务数 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| lane不存在 |
| 排入失败 |

**调用示例**：

```python
并发控制支持库.lane排入(名称=值, 任务数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.lane查询（lane查询）

**说明**：查询 lane 状态（空=全部）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 否 | None | lane 名称 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| lane不存在 |
| 查询失败 |

**调用示例**：

```python
并发控制支持库.lane查询(名称=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.lane领取（lane领取）

**说明**：微任务泵领取可执行槽位

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | lane 名称 |
| 数量 | 整数型 | 否 | 1 | 领取上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| lane不存在 |
| 领取失败 |

**调用示例**：

```python
并发控制支持库.lane领取(名称=值, 数量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.主体串行完成（主体串行完成）

**说明**：主体桶任务完成上报

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 主体id | 文本型 | 是 | None | 主体id |
| 数量 | 整数型 | 否 | 1 | 完成数 |
| 失败数 | 整数型 | 否 | 0 | 失败数 |
| 退避秒 | 数值型 | 否 | 0 | 退避秒 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 主体未登记 |
| 完成上报失败 |

**调用示例**：

```python
并发控制支持库.主体串行完成(主体id=值, 数量=值, 失败数=值, 退避秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.主体串行排入（主体串行排入）

**说明**：主体桶内排入任务（同主体排队串行）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 主体id | 文本型 | 是 | None | 主体id |
| 任务数 | 整数型 | 否 | 1 | 任务数 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 主体未登记 |
| 排入失败 |

**调用示例**：

```python
并发控制支持库.主体串行排入(主体id=值, 任务数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.主体串行查询（主体串行查询）

**说明**：查询主体桶状态（空=全部）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 主体id | 文本型 | 否 | None | 主体id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 主体未登记 |
| 查询失败 |

**调用示例**：

```python
并发控制支持库.主体串行查询(主体id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.主体串行领取（主体串行领取）

**说明**：从主体桶领取（并发上限1=同主体同时最多1执行）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 主体id | 文本型 | 是 | None | 主体id |
| 数量 | 整数型 | 否 | 1 | 领取数量 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 主体未登记 |
| 领取失败 |

**调用示例**：

```python
并发控制支持库.主体串行领取(主体id=值, 数量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建lane（创建lane调度器）

**说明**：创建 lane 调度器（OpenClaw Swarm 模式化：limit+active+queue），同名复用

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | lane 名称 |
| 并发上限 | 整数型 | 否 | 8 | 并发上限 |
| 队列上限 | 整数型 | 否 | 50 | 队列上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建失败 |

**调用示例**：

```python
并发控制支持库.创建lane(名称=值, 并发上限=值, 队列上限=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建事件总线（创建事件总线）

**说明**：创建事件总线（通知/决策双语义，订阅者队列有界）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | 事件总线名称 |
| 队列上限 | 整数型 | 否 | 1000 | 订阅者队列上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建事件总线失败 |

**调用示例**：

```python
并发控制支持库.创建事件总线(名称=值, 队列上限=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建互斥锁（创建互斥锁）

**说明**：创建互斥锁，返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 整数型 | 否 | None | 句柄超时，默认30分钟 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
并发控制支持库.创建互斥锁(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建信号量（创建信号量）

**说明**：创建信号量（计数），返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 初始值 | 整数型 | 否 | 1 | 信号量初始计数 |
| 超时秒 | 整数型 | 否 | None | 句柄超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
并发控制支持库.创建信号量(初始值=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建容量闸门（创建容量闸门）

**说明**：创建容量闸门，上限>0，同名复用（Hermes async_delegation 同锁容量检查模式化）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 闸门名 | 文本型 | 是 | None | 容量闸门名称 |
| 上限 | 整数型 | 是 | None | 容量上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建失败 |

**调用示例**：

```python
并发控制支持库.创建容量闸门(闸门名=值, 上限=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建并发组（创建并发组）

**说明**：创建并发组（一组任务并发跑；失败即停/收口不打断双语义）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | 并发组名称 |
| 并发上限 | 整数型 | 否 | 8 | 并发执行上限 |
| 失败模式 | 文本型 | 否 | 收口不打断 | 失败即停 或 收口不打断 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建并发组失败 |

**调用示例**：

```python
并发控制支持库.创建并发组(名称=值, 并发上限=值, 失败模式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建流管道（创建流管道）

**说明**：创建流管道（生产者写入/消费者读取，有界队列）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 名称 | 文本型 | 是 | None | 流管道名称 |
| 容量上限 | 整数型 | 否 | 1000 | 队列容量上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建流管道失败 |

**调用示例**：

```python
并发控制支持库.创建流管道(名称=值, 容量上限=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.创建线程池（创建线程池）

**说明**：创建线程池，返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 最大线程数 | 整数型 | 否 | 4 | 最大线程数 |
| 超时秒 | 整数型 | 否 | None | 句柄超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建失败 |

**调用示例**：

```python
并发控制支持库.创建线程池(最大线程数=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.占用容量（占用容量）

**说明**：同锁检查+占用；超限直接拒绝不排队，可等待重试

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 闸门名 | 文本型 | 是 | None | 容量闸门名称 |
| 槽位数 | 整数型 | 否 | 1 | 占用槽位数 |
| 等待秒 | 数值型 | 否 | 0 | 超限等待秒数 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 闸门不存在 |
| 占用失败 |

**调用示例**：

```python
并发控制支持库.占用容量(闸门名=值, 槽位数=值, 等待秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.发布决策（发布决策）

**说明**：发布决策类事件（waterfall：按序询问，显式 继续 才问下一个）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 事件总线句柄 |
| 事件名 | 文本型 | 是 | None | 事件名 |
| 负载 | 字典型 | 否 | None | 负载数据 |
| 继续 | 逻辑型 | 否 | False | 是否继续询问下一订阅者 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 发布决策失败 |

**调用示例**：

```python
并发控制支持库.发布决策(句柄=值, 事件名=值, 负载=值, 继续=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.发布通知（发布通知）

**说明**：发布通知类事件（广播给所有通知订阅者）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 事件总线句柄 |
| 事件名 | 文本型 | 是 | None | 事件名 |
| 负载 | 字典型 | 否 | None | 负载数据 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 发布通知失败 |

**调用示例**：

```python
并发控制支持库.发布通知(句柄=值, 事件名=值, 负载=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.取消订阅（取消订阅）

**说明**：取消订阅（按序号移除）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 事件总线句柄 |
| 序号 | 整数型 | 是 | None | 订阅序号 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 订阅不存在 |
| 取消订阅失败 |

**调用示例**：

```python
并发控制支持库.取消订阅(句柄=值, 序号=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.查询事件总线（查询事件总线）

**说明**：查询事件总线状态（句柄空=列全部）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 否 | None | 句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 查询失败 |

**调用示例**：

```python
并发控制支持库.查询事件总线(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.查询容量闸门（查询容量闸门）

**说明**：查询闸门状态（空=全部）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 闸门名 | 文本型 | 否 | None | 闸门名 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 闸门不存在 |
| 查询失败 |

**调用示例**：

```python
并发控制支持库.查询容量闸门(闸门名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.查询并发组（查询并发组）

**说明**：查询并发组状态（句柄空=列全部）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 否 | None | 句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 查询失败 |

**调用示例**：

```python
并发控制支持库.查询并发组(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.查询流管道（查询流管道）

**说明**：查询流管道状态（句柄空=列全部）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 否 | None | 句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 查询失败 |

**调用示例**：

```python
并发控制支持库.查询流管道(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.登记主体桶（登记主体桶）

**说明**：登记主体串行桶（OpenClaw ALS 模式化：同主体串行，内部 lane 并发上限1）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 主体id | 文本型 | 是 | None | 主体id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 登记失败 |

**调用示例**：

```python
并发控制支持库.登记主体桶(主体id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.管道写入（管道写入）

**说明**：向流管道写入一条事件（队列满返回 队列已满）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 流管道句柄 |
| 事件名 | 文本型 | 是 | None | 事件名 |
| 负载 | 字典型 | 否 | None | 负载数据 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 队列已满 |
| 管道写入失败 |

**调用示例**：

```python
并发控制支持库.管道写入(句柄=值, 事件名=值, 负载=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.管道读取（管道读取）

**说明**：从流管道读取事件（FIFO 出队）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 流管道句柄 |
| 数量 | 整数型 | 否 | 1 | 读取条数上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 管道读取失败 |

**调用示例**：

```python
并发控制支持库.管道读取(句柄=值, 数量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.组内完成（组内完成）

**说明**：上报任务完成（执行→结束；返回组累计统计）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 并发组句柄 |
| 任务名 | 文本型 | 是 | None | 任务名 |
| 成功 | 逻辑型 | 是 | None | 是否成功 |
| 结果值 | 字典型 | 否 | None | 结果 |
| 错误 | 文本型 | 否 | None | 错误说明 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务不存在 |
| 组内完成失败 |

**调用示例**：

```python
并发控制支持库.组内完成(句柄=值, 任务名=值, 成功=值, 结果值=值, 错误=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.组内提交（组内提交）

**说明**：向并发组提交任务（排队；任务名重复返回 已存在）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 并发组句柄 |
| 任务名 | 文本型 | 是 | None | 任务名 |
| 负载 | 字典型 | 否 | None | 任务负载 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务已存在 |
| 组内提交失败 |

**调用示例**：

```python
并发控制支持库.组内提交(句柄=值, 任务名=值, 负载=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.组内等待（组内等待）

**说明**：等待并发组全部结束（轮询；超时返回仍在执行快照）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 并发组句柄 |
| 超时秒 | 双精度数型 | 否 | 10 | 等待超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 组内等待失败 |

**调用示例**：

```python
并发控制支持库.组内等待(句柄=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.组内领取（组内领取）

**说明**：从并发组领取可执行任务（受限并发上限；失败即停模式组已失败则拒绝）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 并发组句柄 |
| 数量 | 整数型 | 否 | None | 领取数量 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 组内领取失败 |

**调用示例**：

```python
并发控制支持库.组内领取(句柄=值, 数量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.获取信号量（获取信号量）

**说明**：获取信号量（计数减1）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 信号量句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.获取信号量(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.获取锁（获取锁）

**说明**：获取互斥锁

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 互斥锁句柄 |
| 超时秒 | 双精度数型 | 否 | None | 等待超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.获取锁(句柄=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.订阅事件（订阅事件）

**说明**：订阅事件（模式=通知广播 或 决策按序询问）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 事件总线句柄 |
| 事件名 | 文本型 | 是 | None | 事件名 |
| 模式 | 文本型 | 否 | 通知 | 通知或决策 |
| 订阅者名 | 文本型 | 否 | None | 订阅者名称 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 队列已满 |
| 订阅失败 |

**调用示例**：

```python
并发控制支持库.订阅事件(句柄=值, 事件名=值, 模式=值, 订阅者名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.释放信号量（释放信号量）

**说明**：释放信号量（计数加1）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 信号量句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.释放信号量(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.释放句柄（释放句柄）

**说明**：释放并发资源句柄（幂等，线程池关闭）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 并发资源句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
并发控制支持库.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.释放容量（释放容量）

**说明**：释放闸门容量（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 闸门名 | 文本型 | 是 | None | 容量闸门名称 |
| 槽位数 | 整数型 | 否 | 1 | 释放槽位数 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 释放失败 |

**调用示例**：

```python
并发控制支持库.释放容量(闸门名=值, 槽位数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.释放锁（释放锁）

**说明**：释放互斥锁

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 整数型 | 是 | None | 互斥锁句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
并发控制支持库.释放锁(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 并发控制支持库.结构化任务结果（结构化任务结果）

**说明**：异步任务结果收口：严格/收口双模式，把成功值/取消/异常统一收敛为结构化终态（成功/取消/异常），不向调用方抛异常

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 结果模式 | 文本型 | 是 | None | 1=严格（参数明显非法返回 参数不合法）/2=收口（一律返回结构化终态） |
| 任务标识 | 文本型 | 否 | None | 任务标识，用于错误说明定位 |
| 结果值 | JSON值型 | 否 | None | 任务产出的值（空值视为未产出） |
| 异常说明 | 文本型 | 否 | None | 异常说明（非空即终态=异常、成功=假） |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '终态': '文本型', '值': 'JSON值型', '错误说明': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 收口失败 |

**调用示例**：

```python
并发控制支持库.结构化任务结果(结果模式=值, 任务标识=值, 结果值=值, 异常说明=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）
