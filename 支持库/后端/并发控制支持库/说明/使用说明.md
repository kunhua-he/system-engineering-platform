# 并发控制支持库（支持库.后端.并发控制支持库）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.4
- 说明：多线程/互斥锁/信号量/线程池（参考易语言多线程支持库，句柄模式）
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### lane完成（`并发控制支持库.lane完成`）
- 版本：1.0.0
- 一句话说明：上报完成/失败，失败附退避秒
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 数量:整数型, 失败数:整数型, 退避秒:双精度数型
- 必填参数：名称, 数量
- 可选参数：失败数, 退避秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, lane不存在, 完成上报失败
- 调用示例：并发控制支持库.lane完成(失败数=0, 退避秒=0.0)
- 依赖能力：无
### lane排入（`并发控制支持库.lane排入`）
- 版本：1.0.0
- 一句话说明：排入任务；队列满直接拒绝
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 任务数:整数型
- 必填参数：名称
- 可选参数：任务数
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, lane不存在, 排入失败
- 调用示例：并发控制支持库.lane排入(任务数=1)
- 依赖能力：无
### lane查询（`并发控制支持库.lane查询`）
- 版本：1.0.0
- 一句话说明：查询 lane 状态（空=全部）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型
- 必填参数：无
- 可选参数：名称
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, lane不存在, 查询失败
- 调用示例：并发控制支持库.lane查询()
- 依赖能力：无
### lane领取（`并发控制支持库.lane领取`）
- 版本：1.0.0
- 一句话说明：微任务泵领取可执行槽位
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 数量:整数型
- 必填参数：名称
- 可选参数：数量
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, lane不存在, 领取失败
- 调用示例：并发控制支持库.lane领取(数量=1)
- 依赖能力：无
### 主体串行完成（`并发控制支持库.主体串行完成`）
- 版本：1.0.0
- 一句话说明：主体桶任务完成上报
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：主体id:文本型, 数量:整数型, 失败数:整数型, 退避秒:双精度数型
- 必填参数：主体id
- 可选参数：数量, 失败数, 退避秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 主体未登记, 完成上报失败
- 调用示例：并发控制支持库.主体串行完成(数量=1, 失败数=0, 退避秒=0.0)
- 依赖能力：无
### 主体串行排入（`并发控制支持库.主体串行排入`）
- 版本：1.0.0
- 一句话说明：主体桶内排入任务（同主体排队串行）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：主体id:文本型, 任务数:整数型
- 必填参数：主体id
- 可选参数：任务数
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 主体未登记, 排入失败
- 调用示例：并发控制支持库.主体串行排入(任务数=1)
- 依赖能力：无
### 主体串行查询（`并发控制支持库.主体串行查询`）
- 版本：1.0.0
- 一句话说明：查询主体桶状态（空=全部）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：主体id:文本型
- 必填参数：无
- 可选参数：主体id
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 主体未登记, 查询失败
- 调用示例：并发控制支持库.主体串行查询()
- 依赖能力：无
### 主体串行领取（`并发控制支持库.主体串行领取`）
- 版本：1.0.0
- 一句话说明：从主体桶领取（并发上限1=同主体同时最多1执行）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：主体id:文本型, 数量:整数型
- 必填参数：主体id
- 可选参数：数量
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 主体未登记, 领取失败
- 调用示例：并发控制支持库.主体串行领取(数量=1)
- 依赖能力：无
### 创建lane调度器（`并发控制支持库.创建lane`）
- 版本：1.0.0
- 一句话说明：创建 lane 调度器（OpenClaw Swarm 模式化：limit+active+queue），同名复用
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 并发上限:整数型, 队列上限:整数型
- 必填参数：名称
- 可选参数：并发上限, 队列上限
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建失败
- 调用示例：并发控制支持库.创建lane(并发上限=8, 队列上限=50)
- 依赖能力：无
### 创建事件总线（`并发控制支持库.创建事件总线`）
- 版本：1.0.0
- 一句话说明：创建事件总线（通知/决策双语义，订阅者队列有界）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 队列上限:整数型
- 必填参数：名称
- 可选参数：队列上限
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建事件总线失败
- 调用示例：并发控制支持库.创建事件总线(队列上限=1000)
- 依赖能力：无
### 创建互斥锁（`并发控制支持库.创建互斥锁`）
- 版本：1.0.0
- 一句话说明：创建互斥锁，返回句柄
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：并发控制支持库.创建互斥锁()
- 依赖能力：无
### 创建信号量（`并发控制支持库.创建信号量`）
- 版本：1.0.0
- 一句话说明：创建信号量（计数），返回句柄
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：初始值:整数型
- 必填参数：无
- 可选参数：初始值
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：并发控制支持库.创建信号量(初始值=1)
- 依赖能力：无
### 创建容量闸门（`并发控制支持库.创建容量闸门`）
- 版本：1.0.0
- 一句话说明：创建容量闸门，上限>0，同名复用（Hermes async_delegation 同锁容量检查模式化）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：闸门名:文本型, 上限:整数型
- 必填参数：闸门名, 上限
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建失败
- 调用示例：并发控制支持库.创建容量闸门()
- 依赖能力：无
### 创建并发组（`并发控制支持库.创建并发组`）
- 版本：1.0.0
- 一句话说明：创建并发组（一组任务并发跑；失败即停/收口不打断双语义）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 并发上限:整数型, 失败模式:文本型
- 必填参数：名称
- 可选参数：并发上限, 失败模式
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建并发组失败
- 调用示例：并发控制支持库.创建并发组(并发上限=8, 失败模式=收口不打断)
- 依赖能力：无
### 创建流管道（`并发控制支持库.创建流管道`）
- 版本：1.0.0
- 一句话说明：创建流管道（生产者写入/消费者读取，有界队列）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：名称:文本型, 容量上限:整数型
- 必填参数：名称
- 可选参数：容量上限
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建流管道失败
- 调用示例：并发控制支持库.创建流管道(容量上限=1000)
- 依赖能力：无
### 创建线程池（`并发控制支持库.创建线程池`）
- 版本：1.0.0
- 一句话说明：创建线程池，返回句柄
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：最大线程数:整数型
- 必填参数：无
- 可选参数：最大线程数
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建失败
- 调用示例：并发控制支持库.创建线程池(最大线程数=4)
- 依赖能力：无
### 占用容量（`并发控制支持库.占用容量`）
- 版本：1.0.0
- 一句话说明：同锁检查+占用；超限直接拒绝不排队，可等待重试
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：闸门名:文本型, 槽位数:整数型, 等待秒:双精度数型
- 必填参数：闸门名
- 可选参数：槽位数, 等待秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 闸门不存在, 占用失败
- 调用示例：并发控制支持库.占用容量(槽位数=1, 等待秒=0.0)
- 依赖能力：无
### 发布决策（`并发控制支持库.发布决策`）
- 版本：1.0.0
- 一句话说明：发布决策类事件（waterfall：按序询问，显式 继续 才问下一个）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 事件名:文本型, 负载:字典型, 继续:逻辑型
- 必填参数：句柄, 事件名
- 可选参数：负载, 继续
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 发布决策失败, 句柄失效
- 调用示例：并发控制支持库.发布决策(继续=False)
- 依赖能力：无
### 发布通知（`并发控制支持库.发布通知`）
- 版本：1.0.0
- 一句话说明：发布通知类事件（广播给所有通知订阅者）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 事件名:文本型, 负载:字典型
- 必填参数：句柄, 事件名
- 可选参数：负载
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 发布通知失败, 句柄失效
- 调用示例：并发控制支持库.发布通知()
- 依赖能力：无
### 取消订阅（`并发控制支持库.取消订阅`）
- 版本：1.0.0
- 一句话说明：取消订阅（按序号移除）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 序号:整数型
- 必填参数：句柄, 序号
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：订阅不存在, 取消订阅失败, 句柄失效
- 调用示例：并发控制支持库.取消订阅()
- 依赖能力：无
### 查询事件总线（`并发控制支持库.查询事件总线`）
- 版本：1.0.0
- 一句话说明：查询事件总线状态（句柄空=列全部）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型
- 必填参数：无
- 可选参数：句柄
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：查询失败, 句柄失效
- 调用示例：并发控制支持库.查询事件总线()
- 依赖能力：无
### 查询容量闸门（`并发控制支持库.查询容量闸门`）
- 版本：1.0.0
- 一句话说明：查询闸门状态（空=全部）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：闸门名:文本型
- 必填参数：无
- 可选参数：闸门名
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 闸门不存在, 查询失败
- 调用示例：并发控制支持库.查询容量闸门()
- 依赖能力：无
### 查询并发组（`并发控制支持库.查询并发组`）
- 版本：1.0.0
- 一句话说明：查询并发组状态（句柄空=列全部）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型
- 必填参数：无
- 可选参数：句柄
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：查询失败, 句柄失效
- 调用示例：并发控制支持库.查询并发组()
- 依赖能力：无
### 查询流管道（`并发控制支持库.查询流管道`）
- 版本：1.0.0
- 一句话说明：查询流管道状态（句柄空=列全部）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型
- 必填参数：无
- 可选参数：句柄
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：查询失败, 句柄失效
- 调用示例：并发控制支持库.查询流管道()
- 依赖能力：无
### 登记主体桶（`并发控制支持库.登记主体桶`）
- 版本：1.0.0
- 一句话说明：登记主体串行桶（OpenClaw ALS 模式化：同主体串行，内部 lane 并发上限1）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：主体id:文本型
- 必填参数：主体id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 登记失败
- 调用示例：并发控制支持库.登记主体桶()
- 依赖能力：无
### 管道写入（`并发控制支持库.管道写入`）
- 版本：1.0.0
- 一句话说明：向流管道写入一条事件（队列满返回 队列已满）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 事件名:文本型, 负载:字典型
- 必填参数：句柄, 事件名
- 可选参数：负载
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 队列已满, 管道写入失败, 句柄失效
- 调用示例：并发控制支持库.管道写入()
- 依赖能力：无
### 管道读取（`并发控制支持库.管道读取`）
- 版本：1.0.0
- 一句话说明：从流管道读取事件（FIFO 出队）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 数量:整数型
- 必填参数：句柄
- 可选参数：数量
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 管道读取失败, 句柄失效
- 调用示例：并发控制支持库.管道读取(数量=1)
- 依赖能力：无
### 组内完成（`并发控制支持库.组内完成`）
- 版本：1.0.0
- 一句话说明：上报任务完成（执行→结束；返回组累计统计）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 任务名:文本型, 成功:逻辑型, 结果值:字典型, 错误:文本型
- 必填参数：句柄, 任务名, 成功
- 可选参数：结果值, 错误
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务不存在, 组内完成失败, 句柄失效
- 调用示例：并发控制支持库.组内完成()
- 依赖能力：无
### 组内提交（`并发控制支持库.组内提交`）
- 版本：1.0.0
- 一句话说明：向并发组提交任务（排队；任务名重复返回 已存在）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 任务名:文本型, 负载:字典型
- 必填参数：句柄, 任务名
- 可选参数：负载
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务已存在, 组内提交失败, 句柄失效
- 调用示例：并发控制支持库.组内提交()
- 依赖能力：无
### 组内等待（`并发控制支持库.组内等待`）
- 版本：1.0.0
- 一句话说明：等待并发组全部结束（轮询；超时返回仍在执行快照）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 超时秒:双精度数型
- 必填参数：句柄
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 组内等待失败, 句柄失效
- 调用示例：并发控制支持库.组内等待(超时秒=10.0)
- 依赖能力：无
### 组内领取（`并发控制支持库.组内领取`）
- 版本：1.0.0
- 一句话说明：从并发组领取可执行任务（受限并发上限；失败即停模式组已失败则拒绝）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 数量:整数型
- 必填参数：句柄
- 可选参数：数量
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 组内领取失败, 句柄失效
- 调用示例：并发控制支持库.组内领取()
- 依赖能力：无
### 获取信号量（`并发控制支持库.获取信号量`）
- 版本：1.0.0
- 一句话说明：获取信号量（计数减1）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 超时秒:双精度数型
- 必填参数：句柄
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 获取信号量失败
- 调用示例：并发控制支持库.获取信号量()
- 依赖能力：无
### 获取锁（`并发控制支持库.获取锁`）
- 版本：1.0.0
- 一句话说明：获取互斥锁
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 超时秒:双精度数型
- 必填参数：句柄
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 获取锁失败
- 调用示例：并发控制支持库.获取锁()
- 依赖能力：无
### 订阅事件（`并发控制支持库.订阅事件`）
- 版本：1.0.0
- 一句话说明：订阅事件（模式=通知广播 或 决策按序询问）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型, 事件名:文本型, 模式:文本型, 订阅者名:文本型
- 必填参数：句柄, 事件名
- 可选参数：模式, 订阅者名
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 队列已满, 订阅失败, 句柄失效
- 调用示例：并发控制支持库.订阅事件(模式=通知)
- 依赖能力：无
### 释放信号量（`并发控制支持库.释放信号量`）
- 版本：1.0.0
- 一句话说明：释放信号量（计数加1）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效
- 调用示例：并发控制支持库.释放信号量()
- 依赖能力：无
### 释放句柄（`并发控制支持库.释放句柄`）
- 版本：1.0.0
- 一句话说明：释放并发资源句柄（幂等，线程池关闭）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '已释放': '逻辑型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 资源释放中, 资源未收敛
- 调用示例：并发控制支持库.释放句柄()
- 依赖能力：无
### 释放容量（`并发控制支持库.释放容量`）
- 版本：1.0.0
- 一句话说明：释放闸门容量（幂等）
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：闸门名:文本型, 槽位数:整数型
- 必填参数：闸门名
- 可选参数：槽位数
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 释放失败
- 调用示例：并发控制支持库.释放容量(槽位数=1)
- 依赖能力：无
### 释放锁（`并发控制支持库.释放锁`）
- 版本：1.0.0
- 一句话说明：释放互斥锁
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 释放锁失败
- 调用示例：并发控制支持库.释放锁()
- 依赖能力：无
### 结构化任务结果（`并发控制支持库.结构化任务结果`）
- 版本：1.0.0
- 一句话说明：异步任务结果收口：严格/收口双模式，把成功值/取消/异常统一收敛为结构化终态（成功/取消/异常），不向调用方抛异常
- 所属支持库或模块：支持库.后端.并发控制支持库
- 参数列表：结果模式:文本型, 任务标识:文本型, 结果值:JSON值型, 异常说明:文本型
- 必填参数：结果模式
- 可选参数：任务标识, 结果值, 异常说明
- 返回结构：{'类型': '结果型', '值结构': {'成功': '逻辑型', '终态': '文本型', '值': 'JSON值型', '错误说明': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 收口失败
- 调用示例：并发控制支持库.结构化任务结果()
- 依赖能力：无
