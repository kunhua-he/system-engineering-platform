<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 命令安全（支持库.后端.系统核心支持库.命令安全）

- 类型：支持库
- 版本：1.0.0
- 说明：命令安全支持库
- 入口：__init__.py

## 能力
### 检测危险命令（`系统核心支持库.命令安全.检测危险命令`）
- 版本：1.0.0
- 一句话说明：检测命令是否命中危险模式，返回 {危险, 原因}；只判定不执行
- 所属支持库或模块：支持库.后端.系统核心支持库.命令安全
- 参数列表：命令:文本型, 最大长度:整数型
- 必填参数：命令
- 可选参数：最大长度
- 返回结构：{'类型': '结果型', '值结构': {'危险': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.命令安全.检测危险命令(命令=ls -la)
- 依赖能力：无
