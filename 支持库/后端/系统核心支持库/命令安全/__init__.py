"""支持库.命令安全 包级中文入口。"""

from __future__ import annotations

from 支持库.后端.系统核心支持库.命令安全.实现.命令安全 import 检测危险命令

__all__ = [
    "检测危险命令",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("系统核心支持库.命令安全.检测危险命令", 检测危险命令, [{"名称": "命令", "类型": "文本型", "必填": True, "默认值": None, "说明": "待检测的命令文本（终端执行前）"}, {"名称": "最大长度", "类型": "整数型", "必填": False, "默认值": None, "说明": "命令长度上限，默认 65536"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.系统核心支持库.命令安全",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="命令安全支持库",
            )
        )
