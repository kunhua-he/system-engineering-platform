"""删除守卫支持库包级中文入口。

调用者只能从此入口导入，禁止深入 实现/ 目录。
"""

from __future__ import annotations

from 支持库.后端.系统核心支持库.删除守卫.实现.删除守卫 import 检查删除
from 支持库.后端.系统核心支持库.删除守卫.实现.删除守卫 import 检查清空

__all__ = [
    "检查删除",
    "检查清空",
]


from 支持库.后端.系统核心支持库.删除守卫.实现.删除守卫 import 检查删除
from 支持库.后端.系统核心支持库.删除守卫.实现.删除守卫 import 检查清空


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现

    for 能力id, 函数, 参数表 in [
        ("系统核心支持库.删除守卫.检查删除", 检查删除, [{"名称": "目标表", "类型": "文本型", "必填": True, "默认值": None, "说明": "要删除的目标表名"}, {"名称": "删除比例", "类型": "双精度数型", "必填": False, "默认值": None, "说明": "待删行占比（0~1），超上限拒绝"}, {"名称": "过滤条件", "类型": "文本型", "必填": False, "默认值": None, "说明": "删除过滤条件（空则拒绝）"}, {"名称": "比例上限", "类型": "双精度数型", "必填": False, "默认值": 0.8, "说明": "删除比例上限（默认 0.8）"}, {"名称": "保护表", "类型": "列表型", "必填": False, "默认值": None, "说明": "自定义保护表列表"}]),
        ("系统核心支持库.删除守卫.检查清空", 检查清空, [{"名称": "目标表", "类型": "文本型", "必填": True, "默认值": None, "说明": "要清空的表名"}, {"名称": "保护表", "类型": "列表型", "必填": False, "默认值": None, "说明": "自定义保护表列表"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.系统核心支持库.删除守卫",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="检查清空整表操作：受保护表拒绝。返回 {通过, 原因}。",
            )
        )
