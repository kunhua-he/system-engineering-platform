# 删除守卫 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.删除守卫
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.删除守卫.检查删除 | 检查删除 | 1.0.0 | {'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}} | 检查一次删除操作是否安全：受保护表拒绝、无条件删除拒绝、删除比例超上限拒绝。返回 {通过, 原因}。 |
| 系统核心支持库.删除守卫.检查清空 | 检查清空 | 1.0.0 | {'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}} | 检查清空整表操作：受保护表拒绝。返回 {通过, 原因}。 |

## 系统核心支持库.删除守卫.检查删除（检查删除）

**说明**：检查一次删除操作是否安全：受保护表拒绝、无条件删除拒绝、删除比例超上限拒绝。返回 {通过, 原因}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标表 | 文本型 | 是 | None | 要删除的目标表名 |
| 删除比例 | 双精度数型 | 否 | None | 待删行占比（0~1），超上限拒绝 |
| 过滤条件 | 文本型 | 否 | None | 删除过滤条件（空则拒绝） |
| 比例上限 | 双精度数型 | 否 | 0.8 | 删除比例上限（默认 0.8） |
| 保护表 | 列表型 | 否 | None | 自定义保护表列表 |

**返回结构**：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.删除守卫.检查删除(目标表=值, 删除比例=值, 过滤条件=值, 比例上限=值, 保护表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.删除守卫.检查清空（检查清空）

**说明**：检查清空整表操作：受保护表拒绝。返回 {通过, 原因}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标表 | 文本型 | 是 | None | 要清空的表名 |
| 保护表 | 列表型 | 否 | None | 自定义保护表列表 |

**返回结构**：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.删除守卫.检查清空(目标表=值, 保护表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
