<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 删除守卫（支持库.后端.系统核心支持库.删除守卫）

- 类型：支持库
- 版本：1.0.0
- 说明：危险删除三重保护。只依赖 Python 标准库。
- 入口：__init__.py

## 能力
### 检查删除（`系统核心支持库.删除守卫.检查删除`）
- 版本：1.0.0
- 一句话说明：检查删除是否安全
- 所属支持库或模块：支持库.后端.系统核心支持库.删除守卫
- 参数列表：目标表:文本型, 删除比例:双精度数型, 过滤条件:文本型, 比例上限:双精度数型, 保护表:列表型
- 必填参数：目标表
- 可选参数：删除比例, 过滤条件, 比例上限, 保护表
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.删除守卫.检查删除(比例上限=0.8)
- 依赖能力：无
### 检查清空（`系统核心支持库.删除守卫.检查清空`）
- 版本：1.0.0
- 一句话说明：检查清空是否安全
- 所属支持库或模块：支持库.后端.系统核心支持库.删除守卫
- 参数列表：目标表:文本型, 保护表:列表型
- 必填参数：目标表
- 可选参数：保护表
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.删除守卫.检查清空()
- 依赖能力：无
