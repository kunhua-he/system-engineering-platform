# 删除守卫（支持库.后端.系统核心支持库.删除守卫）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.2
- 说明：危险删除三重保护（借鉴 TencentDB-Agent-Memory）：删除比例上限/空过滤条件拒绝/保护资产表。只做检查判定，不执行删除。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查删除（`系统核心支持库.删除守卫.检查删除`）
- 版本：1.0.0
- 一句话说明：检查一次删除操作是否安全：受保护表拒绝、无条件删除拒绝、删除比例超上限拒绝。返回 {通过, 原因}。
- 所属支持库或模块：支持库.后端.系统核心支持库.删除守卫
- 参数列表：目标表:文本型, 删除比例:双精度数型, 过滤条件:文本型, 比例上限:双精度数型, 保护表:列表型
- 必填参数：目标表
- 可选参数：删除比例, 过滤条件, 比例上限, 保护表
- 返回结构：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.删除守卫.检查删除(比例上限=0.8)
- 依赖能力：无
### 检查清空（`系统核心支持库.删除守卫.检查清空`）
- 版本：1.0.0
- 一句话说明：检查清空整表操作：受保护表拒绝。返回 {通过, 原因}。
- 所属支持库或模块：支持库.后端.系统核心支持库.删除守卫
- 参数列表：目标表:文本型, 保护表:列表型
- 必填参数：目标表
- 可选参数：保护表
- 返回结构：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.删除守卫.检查清空()
- 依赖能力：无
