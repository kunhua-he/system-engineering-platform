# 工具执行 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.工具执行
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.工具执行.受理工具调用 | 受理工具调用 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 受理工具调用，返回执行id（两段式受理） |
| 系统核心支持库.工具执行.登记执行结果 | 登记执行结果 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 登记执行结果（成功或失败，失败带错误标记） |
| 系统核心支持库.工具执行.查询执行结果 | 查询执行结果 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询执行结果 |
| 系统核心支持库.工具执行.清理执行记录 | 清理执行记录 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 清理执行记录（幂等） |
| 系统核心支持库.工具执行.登记投递 | 登记投递 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 登记投递（Hermes claim 模式化：SQLite落库重启不丢，0限制原文存取） |
| 系统核心支持库.工具执行.认领投递 | 认领投递 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 原子认领最早待认领投递 |
| 系统核心支持库.工具执行.完成投递 | 完成投递 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 投递完成登记（成功/失败） |
| 系统核心支持库.工具执行.补投超时 | 补投超时 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 已认领超时未完成→改回待认领（重启补投语义） |

## 系统核心支持库.工具执行.受理工具调用（受理工具调用）

**说明**：受理工具调用，返回执行id（两段式受理）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 工具名 | 文本型 | 是 | None | 工具名 |
| 参数 | 字典型 | 否 | None | 工具参数 |
| 超时秒 | 整数型 | 否 | 30 | 超时秒 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.工具执行.受理工具调用(工具名=值, 参数=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.登记执行结果（登记执行结果）

**说明**：登记执行结果（成功或失败，失败带错误标记）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 执行id | 文本型 | 是 | None | 执行id |
| 成功 | 逻辑型 | 是 | None | 是否成功 |
| 结果值 | 字典型 | 否 | None | 结果 |
| 错误 | 文本型 | 否 | None | 错误说明 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 执行不存在 |

**调用示例**：

```python
系统核心支持库.工具执行.登记执行结果(执行id=值, 成功=值, 结果值=值, 错误=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.查询执行结果（查询执行结果）

**说明**：查询执行结果

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 执行id | 文本型 | 是 | None | 执行id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 执行不存在 |

**调用示例**：

```python
系统核心支持库.工具执行.查询执行结果(执行id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.清理执行记录（清理执行记录）

**说明**：清理执行记录（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 执行id | 文本型 | 是 | None | 执行id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.工具执行.清理执行记录(执行id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.登记投递（登记投递）

**说明**：登记投递（Hermes claim 模式化：SQLite落库重启不丢，0限制原文存取）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 队列名 | 文本型 | 是 | None | 队列名 |
| 负载 | 字典型 | 是 | None | 负载 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 登记投递失败 |

**调用示例**：

```python
系统核心支持库.工具执行.登记投递(队列名=值, 负载=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.认领投递（认领投递）

**说明**：原子认领最早待认领投递

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 队列名 | 文本型 | 是 | None | 队列名 |
| 认领者 | 文本型 | 否 | None | 认领者 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 认领投递失败 |

**调用示例**：

```python
系统核心支持库.工具执行.认领投递(队列名=值, 认领者=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.完成投递（完成投递）

**说明**：投递完成登记（成功/失败）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 投递id | 文本型 | 是 | None | 投递id |
| 成功 | 逻辑型 | 是 | None | 是否成功 |
| 结果值 | 字典型 | 否 | None | 结果 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 投递不存在 |
| 完成投递失败 |

**调用示例**：

```python
系统核心支持库.工具执行.完成投递(投递id=值, 成功=值, 结果值=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）

## 系统核心支持库.工具执行.补投超时（补投超时）

**说明**：已认领超时未完成→改回待认领（重启补投语义）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 队列名 | 文本型 | 是 | None | 队列名 |
| 超时秒 | 整数型 | 否 | 60 | 超时阈值 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 补投超时失败 |

**调用示例**：

```python
系统核心支持库.工具执行.补投超时(队列名=值, 超时秒=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：工具执行.工具调用两段式闭环（正向）、工具执行.认领投递闭环（正向）
