# 工具执行 使用说明

- 包id：支持库.后端.系统核心支持库.工具执行
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.工具执行.受理工具调用 | 受理工具调用 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 受理工具调用，返回执行id（两段式受理） |
| 系统核心支持库.工具执行.登记执行结果 | 登记执行结果 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 登记执行结果（成功或失败，失败带错误标记） |
| 系统核心支持库.工具执行.查询执行结果 | 查询执行结果 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询执行结果 |
| 系统核心支持库.工具执行.清理执行记录 | 清理执行记录 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 清理执行记录（幂等） |

## 系统核心支持库.工具执行.受理工具调用（受理工具调用）

**说明**：受理工具调用，返回执行id（两段式受理）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 工具名 | 文本型 | 是 | None | 工具名 |
| 参数 | 字典型 | 否 | None | 工具参数 |
| 超时秒 | 整数型 | 否 | 30 | 超时秒 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.工具执行.受理工具调用(工具名=值, 参数=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.工具执行.登记执行结果（登记执行结果）

**说明**：登记执行结果（成功或失败，失败带错误标记）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 执行id | 文本型 | 是 | None | 执行id |
| 成功 | 逻辑型 | 是 | None | 是否成功 |
| 结果值 | 字典型 | 否 | None | 结果 |
| 错误 | 文本型 | 否 | None | 错误说明 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 执行不存在 |

**调用示例**：

```python
系统核心支持库.工具执行.登记执行结果(执行id=值, 成功=值, 结果值=值, 错误=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.工具执行.查询执行结果（查询执行结果）

**说明**：查询执行结果

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 执行id | 文本型 | 是 | None | 执行id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 执行不存在 |

**调用示例**：

```python
系统核心支持库.工具执行.查询执行结果(执行id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.工具执行.清理执行记录（清理执行记录）

**说明**：清理执行记录（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 执行id | 文本型 | 是 | None | 执行id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.工具执行.清理执行记录(执行id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
