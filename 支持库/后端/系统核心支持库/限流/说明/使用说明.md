# 限流（支持库.后端.系统核心支持库.限流）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.3
- 说明：限流支持库
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 令牌桶判定（`系统核心支持库.限流.令牌桶判定`）
- 版本：1.0.0
- 一句话说明：令牌桶惰性补充与取用判定，返回 {放行, 当前令牌数, 上次补充时间}；状态由调用方持有
- 所属支持库或模块：支持库.后端.系统核心支持库.限流
- 参数列表：桶容量:整数型, 每秒补充:双精度数型, 当前令牌数:双精度数型, 上次补充时间:双精度数型, 现在:双精度数型, 取用数量:整数型
- 必填参数：桶容量, 每秒补充, 当前令牌数, 上次补充时间
- 可选参数：现在, 取用数量
- 返回结构：{'类型': '结果型', '值结构': {'放行': '逻辑型', '当前令牌数': '双精度数型', '上次补充时间': '双精度数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.限流.令牌桶判定(桶容量=1, 每秒补充=1.0, 当前令牌数=1.0)
- 依赖能力：无
### 生成请求编号（`系统核心支持库.限流.生成请求编号`）
- 版本：1.0.0
- 一句话说明：生成唯一请求编号（十六进制片段），返回 {请求编号}
- 所属支持库或模块：支持库.后端.系统核心支持库.限流
- 参数列表：长度:整数型
- 必填参数：无
- 可选参数：长度
- 返回结构：{'类型': '结果型', '值结构': {'请求编号': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.限流.生成请求编号(长度=8)
- 依赖能力：无
### 登录限速判定（`系统核心支持库.限流.登录限速判定`）
- 版本：1.0.0
- 一句话说明：登录失败限速判定，返回 {锁定, 原因, 剩余可尝试次数}
- 所属支持库或模块：支持库.后端.系统核心支持库.限流
- 参数列表：当前失败次数:整数型, 最大失败次数:整数型, 锁定剩余秒:双精度数型
- 必填参数：当前失败次数, 最大失败次数
- 可选参数：锁定剩余秒
- 返回结构：{'类型': '结果型', '值结构': {'锁定': '逻辑型', '原因': '文本型', '剩余可尝试次数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.限流.登录限速判定(当前失败次数=1, 最大失败次数=1, 锁定剩余秒=0.0)
- 依赖能力：无
