# 进程管理 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.进程管理
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.进程管理.启动进程 | 启动进程 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 启动外部进程，返回句柄 |
| 系统核心支持库.进程管理.终止进程 | 终止进程 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 终止进程 |
| 系统核心支持库.进程管理.查询进程状态 | 查询进程状态 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询进程运行状态 |
| 系统核心支持库.进程管理.等待进程结束 | 等待进程结束 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 等待进程结束并获取输出 |
| 系统核心支持库.进程管理.执行命令 | 执行命令 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 执行 shell 命令并等待 |
| 系统核心支持库.进程管理.检查命令可用 | 检查命令可用 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 检查命令是否可用（which） |
| 系统核心支持库.进程管理.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放进程句柄（幂等，强制杀残留） |
| 系统核心支持库.进程管理.沙箱执行命令 | 沙箱执行命令 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 在 macOS sandbox-exec 内核沙箱内执行 shell 命令：系统只读、仅工作目录可读写；无沙箱平台 fail-closed |

## 系统核心支持库.进程管理.启动进程（启动进程）

**说明**：启动外部进程，返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | 可执行文件路径 |
| 参数 | 列表型 | 否 | None | 参数列表 |
| 工作目录 | 文本型 | 否 | None | 工作目录 |
| 环境变量 | 映射型 | 否 | None | 额外环境变量 |
| 超时秒 | 整数型 | 否 | None | 句柄超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 启动失败 |

**调用示例**：

```python
系统核心支持库.进程管理.启动进程(命令=值, 参数=值, 工作目录=值, 环境变量=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.终止进程（终止进程）

**说明**：终止进程

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |
| 强制 | 逻辑型 | 否 | False | true 强制 kill |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
系统核心支持库.进程管理.终止进程(句柄=值, 强制=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.查询进程状态（查询进程状态）

**说明**：查询进程运行状态

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
系统核心支持库.进程管理.查询进程状态(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.等待进程结束（等待进程结束）

**说明**：等待进程结束并获取输出

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |
| 超时秒 | 双精度数型 | 否 | None | 等待超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 超时 |

**调用示例**：

```python
系统核心支持库.进程管理.等待进程结束(句柄=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.执行命令（执行命令）

**说明**：执行 shell 命令并等待

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | shell 命令 |
| 超时秒 | 双精度数型 | 否 | 60 | 超时 |
| 工作目录 | 文本型 | 否 | None | 工作目录 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超时 |
| 执行失败 |

**调用示例**：

```python
系统核心支持库.进程管理.执行命令(命令=值, 超时秒=值, 工作目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.检查命令可用（检查命令可用）

**说明**：检查命令是否可用（which）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | 命令名 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.进程管理.检查命令可用(命令=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.释放句柄（释放句柄）

**说明**：释放进程句柄（幂等，强制杀残留）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.进程管理.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.沙箱执行命令（沙箱执行命令）

**说明**：在 macOS sandbox-exec 内核沙箱内执行 shell 命令：系统只读、仅工作目录可读写；无沙箱平台 fail-closed

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | shell 命令 |
| 工作目录 | 文本型 | 是 | None | 沙箱唯一可读写目录 |
| 超时秒 | 双精度数型 | 否 | 60 | 超时秒；超时用 killpg 回收整棵进程树 |
| 输出上限字节 | 整数型 | 否 | 1048576 | stdout/stderr 各自截断上限 |
| 环境变量 | 字典型 | 否 | None | 白名单内可覆盖的额外环境变量 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 沙箱不可用 |
| 超时 |
| 执行失败 |

**调用示例**：

```python
系统核心支持库.进程管理.沙箱执行命令(命令=值, 工作目录=值, 超时秒=值, 输出上限字节=值, 环境变量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：进程管理.命令与沙箱正向（验证场景.json）

---

## 安全阻断决策记录（批次1-1 · S-01 / S-12 / S-06）

> 本节是本次安全修复的口径权威：为什么这样改、边界在哪、逆向验证怎么复现。改动随包版本 1.1.3 生效。

### S-01 · 沙箱 profile 注入（SBPL 注入）—— 已修

- **结构化拼装**：`实现/进程管理.py::_sbpl字面量` 是 profile 内唯一转义点
  （`\` → `\\`、`"` → `\"`、换行/回车 → 转义序列）；`_构建沙箱配置` 只用「已转义字面量」
  拼 `(subpath …)` / `(literal …)` 子句再逐行 join，函数体内不再把原始路径插值进 profile 文本。
- **入口拒绝（fail-closed）**：唯一校验点 `沙箱执行命令` 在 `resolve()` 之后拒绝含
  `"` `\` `\n` `\r` `)` 的工作目录，返回 `参数不合法`。
  代价：合法但含 ASCII `)` 的目录名也被拒（换取 profile 结构不可能被路径改写）。
- **边界**：`启动进程` / `执行命令` 不经沙箱，不受本条约束。

### S-12 · profile 语法错仍报成功 —— 已修

- **早失败**：非法字符在构造 profile 之前就被拒，不再让 `sandbox-exec` 退出 65 后由调用方猜。
- **编译预检**：`_预检沙箱配置` 在跑真实命令前先用 `/usr/bin/true` 编译一次 profile；
  编译失败 → `执行失败`（fail-closed），绝不让「根本没执行」被报成 `成功=True`。
- **口径不变**：命令自身非零退出码仍以 `值.成功执行=False` + `值.退出码` 如实表达，
  `结果.成功` 只表示「能力执行完成」。

### S-06 · 检测危险命令未接线 —— 选择「接入执行路径」

- **选择接线、不删除**。理由：实现落在 `命令安全` 包（不在本包属主域），删除要跨包动实现与
  能力注册；而该检测是平台自述的高风险前置策略，接线才兑现设计意图。
- **接法**：经唯一能力调用服务取 `系统核心支持库.命令安全.检测危险命令`
  （`公共契约.能力契约.调用器`）——不在支持库之间直接 import、不导入任何 `实现/` 目录，
  不引入新的依赖方向违规。能力级依赖登记在该包 `包声明.json` 的 `依赖`
  （`{"能力": "系统核心支持库.命令安全.检测危险命令", "版本": ">=1.0.0"}`）。
  **注意**：重新编译本包时必须把该依赖一并传给 `编译能力定义`，否则会被 `依赖: []` 覆盖。
- **接线点**：`执行命令`（参数校验后、起进程前）、`沙箱执行命令`（工作目录校验后、构造 profile 前）；
  命中返回 `危险命令`。
- **不接线**：`启动进程`——argv 直启、不经 shell，没有「命令文本」可判，按落点建议不默认拦截。
- **降级口径**：检测器不可用时留痕放行（模块级 `危险命令检测问题`），不回退成拦截。
  理由：检测是纵向加固层，边界是内核沙箱与进程组回收；未经加载器装配的直连场景
  （单测/脚本/工具链）取不到调用器，若这里 fail-closed 会打断既有正常调用。
- **网关映射待登记（跨域）**：`危险命令` 尚未出现在网关 `公开错误说明表` /
  `公开错误码状态映射`（与 `目录不存在` / `沙箱不可用` / `执行失败` 同属能力级表外码），
  经网关调用当前落 HTTP 500。待网关域登记 `危险命令 → 403` 时，**同一提交**把
  `验证场景.json` 中「危险命令前置拦截」步骤的预期状态码从 500 改为 403。
