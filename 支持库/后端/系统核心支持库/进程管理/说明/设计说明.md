# 进程管理 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.进程管理
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.进程管理.启动进程 | 启动进程 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 启动外部进程，返回句柄 |
| 系统核心支持库.进程管理.终止进程 | 终止进程 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 终止进程 |
| 系统核心支持库.进程管理.查询进程状态 | 查询进程状态 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询进程运行状态 |
| 系统核心支持库.进程管理.等待进程结束 | 等待进程结束 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 等待进程结束并获取输出 |
| 系统核心支持库.进程管理.执行命令 | 执行命令 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 执行 shell 命令并等待 |
| 系统核心支持库.进程管理.检查命令可用 | 检查命令可用 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 检查命令是否可用（which） |
| 系统核心支持库.进程管理.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放进程句柄（幂等，强制杀残留） |
| 系统核心支持库.进程管理.沙箱执行命令 | 沙箱执行命令 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 在 macOS sandbox-exec 内核沙箱内执行 shell 命令：系统只读、仅工作目录可读写；无沙箱平台 fail-closed |

## 系统核心支持库.进程管理.启动进程（启动进程）

**说明**：启动外部进程，返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | 可执行文件路径 |
| 参数 | 列表型 | 否 | None | 参数列表 |
| 工作目录 | 文本型 | 否 | None | 工作目录 |
| 环境变量 | 映射型 | 否 | None | 额外环境变量 |
| 超时秒 | 整数型 | 否 | None | 句柄超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 启动失败 |

**调用示例**：

```python
系统核心支持库.进程管理.启动进程(命令=值, 参数=值, 工作目录=值, 环境变量=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.终止进程（终止进程）

**说明**：终止进程

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |
| 强制 | 逻辑型 | 否 | False | true 强制 kill |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
系统核心支持库.进程管理.终止进程(句柄=值, 强制=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.查询进程状态（查询进程状态）

**说明**：查询进程运行状态

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |

**调用示例**：

```python
系统核心支持库.进程管理.查询进程状态(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.等待进程结束（等待进程结束）

**说明**：等待进程结束并获取输出

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |
| 超时秒 | 双精度数型 | 否 | None | 等待超时 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 超时 |

**调用示例**：

```python
系统核心支持库.进程管理.等待进程结束(句柄=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.执行命令（执行命令）

**说明**：执行 shell 命令并等待

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | shell 命令 |
| 超时秒 | 双精度数型 | 否 | 60 | 超时 |
| 工作目录 | 文本型 | 否 | None | 工作目录 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超时 |
| 执行失败 |

**调用示例**：

```python
系统核心支持库.进程管理.执行命令(命令=值, 超时秒=值, 工作目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.检查命令可用（检查命令可用）

**说明**：检查命令是否可用（which）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | 命令名 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.进程管理.检查命令可用(命令=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.释放句柄（释放句柄）

**说明**：释放进程句柄（幂等，强制杀残留）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 进程句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.进程管理.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.进程管理.沙箱执行命令（沙箱执行命令）

**说明**：在 macOS sandbox-exec 内核沙箱内执行 shell 命令：系统只读、仅工作目录可读写；无沙箱平台 fail-closed

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 命令 | 文本型 | 是 | None | shell 命令 |
| 工作目录 | 文本型 | 是 | None | 沙箱唯一可读写目录 |
| 超时秒 | 双精度数型 | 否 | 60 | 超时秒；超时用 killpg 回收整棵进程树 |
| 输出上限字节 | 整数型 | 否 | 1048576 | stdout/stderr 各自截断上限 |
| 环境变量 | 字典型 | 否 | None | 白名单内可覆盖的额外环境变量 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 沙箱不可用 |
| 超时 |
| 执行失败 |

**调用示例**：

```python
系统核心支持库.进程管理.沙箱执行命令(命令=值, 工作目录=值, 超时秒=值, 输出上限字节=值, 环境变量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：进程管理.命令与沙箱正向（验证场景.json）
