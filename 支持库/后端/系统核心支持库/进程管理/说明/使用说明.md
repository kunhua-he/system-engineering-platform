# 进程管理（支持库.后端.系统核心支持库.进程管理）

- 类型：支持库
- 版本：1.1.6
- 说明：进程启动/终止/查询/等待/执行命令/检查命令
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 启动进程（`系统核心支持库.进程管理.启动进程`）
- 版本：1.0.0
- 一句话说明：启动外部进程，返回句柄（argv 直启，不经 shell，不内建危险命令检测）
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：命令:文本型, 参数:列表型, 工作目录:文本型, 环境变量:字典型, 超时秒:整数型, 就绪地址:文本型, 就绪超时秒:双精度数型
- 必填参数：命令
- 可选参数：参数, 工作目录, 环境变量, 超时秒, 就绪地址, 就绪超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 启动失败, 启动超时
- 调用示例：系统核心支持库.进程管理.启动进程()
- 依赖能力：无
### 终止进程（`系统核心支持库.进程管理.终止进程`）
- 版本：1.0.0
- 一句话说明：终止进程
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：句柄:整数型, 强制:逻辑型
- 必填参数：句柄
- 可选参数：强制
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 终止失败
- 调用示例：系统核心支持库.进程管理.终止进程(强制=False)
- 依赖能力：无
### 查询进程状态（`系统核心支持库.进程管理.查询进程状态`）
- 版本：1.0.0
- 一句话说明：查询进程运行状态
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效
- 调用示例：系统核心支持库.进程管理.查询进程状态()
- 依赖能力：无
### 等待进程结束（`系统核心支持库.进程管理.等待进程结束`）
- 版本：1.0.0
- 一句话说明：等待进程结束并获取输出
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：句柄:整数型, 超时秒:双精度数型
- 必填参数：句柄
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 超时, 超出限制, 等待失败
- 调用示例：系统核心支持库.进程管理.等待进程结束()
- 依赖能力：无
### 执行命令（`系统核心支持库.进程管理.执行命令`）
- 版本：1.0.0
- 一句话说明：执行单条命令并等待；按「可执行文件 + 参数」直启，**不经 shell**，故管道/重定向/&&/shell 内建均不支持（要 shell 语义请写 sh -c "…"）；执行前经 命令安全.检测危险命令 前置拦截
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：命令:文本型, 超时秒:双精度数型, 工作目录:文本型
- 必填参数：命令
- 可选参数：超时秒, 工作目录
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超时, 执行失败, 超出限制, 危险命令
- 调用示例：系统核心支持库.进程管理.执行命令(超时秒=60.0)
- 依赖能力：无
### 执行命令集（`系统核心支持库.进程管理.执行命令集`）
- 版本：1.0.0
- 一句话说明：一次执行多条命令（串联=按序、并行=同时跑），逐条回带退出码与输出。逐条复用 执行命令，故危险命令拦截/argv 拆分/输出有界/进程组整组回收同一实现；有界：最多 64 条、并行最多 8 路
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：命令表:列表型, 模式:文本型, 超时秒:双精度数型, 工作目录:文本型, 失败即停:逻辑型
- 必填参数：命令表
- 可选参数：模式, 超时秒, 工作目录, 失败即停
- 返回结构：{'类型': '结果型', '值结构': {'模式': '文本型，本次实际使用的模式', '结果表': '列表型，每项含 序号/命令/成功/耗时毫秒/退出码/标准输出/错误输出；被跳过项带 已跳过=真。**成功 = 退出码为 0**；命令没跑起来（超时/危险命令/执行失败）时 成功=假 且无退出码，改带 错误码/错误说明', '总数': '整数型，入参命令表条数', '已执行数': '整数型', '成功数': '整数型（退出码为 0 的条数）', '失败数': '整数型', '已跳过数': '整数型（串联+失败即停时后面未跑的条数）', '全部成功': '逻辑型，仅当每条都跑且退出码都是 0 才为真', '总耗时毫秒': '双精度数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.进程管理.执行命令集(模式="串联", 超时秒=60.0, 失败即停=true)
- 依赖能力：无
### 检查命令可用（`系统核心支持库.进程管理.检查命令可用`）
- 版本：1.0.0
- 一句话说明：检查命令是否可用（which）
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：命令:文本型
- 必填参数：命令
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 检查失败
- 调用示例：系统核心支持库.进程管理.检查命令可用()
- 依赖能力：无
### 释放句柄（`系统核心支持库.进程管理.释放句柄`）
- 版本：1.0.0
- 一句话说明：释放进程句柄（幂等，强制杀残留）
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.进程管理.释放句柄()
- 依赖能力：无
### 沙箱执行命令（`系统核心支持库.进程管理.沙箱执行命令`）
- 版本：1.0.0
- 一句话说明：在 macOS sandbox-exec 内核沙箱内执行 shell 命令：系统只读、仅工作目录可读写；工作目录含引号/反斜杠/换行/右括号即拒；profile 编译预检失败 fail-closed；命中危险命令前置拦截；无沙箱平台 fail-closed
- 所属支持库或模块：支持库.后端.系统核心支持库.进程管理
- 参数列表：命令:文本型, 工作目录:文本型, 超时秒:双精度数型, 输出上限字节:整数型, 环境变量:字典型
- 必填参数：命令, 工作目录
- 可选参数：超时秒, 输出上限字节, 环境变量
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在, 沙箱不可用, 超时, 执行失败, 危险命令
- 调用示例：系统核心支持库.进程管理.沙箱执行命令(超时秒=60.0, 输出上限字节=1048576)
- 依赖能力：无
