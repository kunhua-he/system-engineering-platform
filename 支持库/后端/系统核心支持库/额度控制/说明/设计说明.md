# 额度控制 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.后端.系统核心支持库.额度控制
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.额度控制.配置额度 | 配置额度 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 按调用方/动作配置额度（默认不限制，配置后生效） |
| 系统核心支持库.额度控制.查询额度 | 查询额度 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询当前额度配置 |
| 系统核心支持库.额度控制.检查额度 | 检查额度 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 检查额度是否超限（0=不限制永远通过） |

## 系统核心支持库.额度控制.配置额度（配置额度）

**说明**：按调用方/动作配置额度（默认不限制，配置后生效）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 额度键 | 文本型 | 是 | None | 额度键（如 会话id/调用方） |
| 额度项 | 文本型 | 是 | None | 额度项（如 max_tokens/超时秒/次数） |
| 额度值 | 整数型 | 是 | None | 额度值（0=不限制） |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.额度控制.配置额度(额度键=值, 额度项=值, 额度值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.额度控制.查询额度（查询额度）

**说明**：查询当前额度配置

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 额度键 | 文本型 | 是 | None | 额度键 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.额度控制.查询额度(额度键=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.额度控制.检查额度（检查额度）

**说明**：检查额度是否超限（0=不限制永远通过）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 额度键 | 文本型 | 是 | None | 额度键 |
| 额度项 | 文本型 | 是 | None | 额度项 |
| 已用值 | 整数型 | 是 | None | 已用值 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.额度控制.检查额度(额度键=值, 额度项=值, 已用值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
