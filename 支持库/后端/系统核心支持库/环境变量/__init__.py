"""环境变量 子域入口（聚合库，由系统核心支持库 聚合入口统一注册）。"""
from __future__ import annotations
from 支持库.后端.系统核心支持库.环境变量.实现.环境变量 import 获取环境变量, 设置环境变量, 删除环境变量, 列出环境变量
from 支持库.后端.系统核心支持库.环境变量.实现.环境变量 import 读取环境文件
__all__ = ["获取环境变量", "设置环境变量", "删除环境变量", "列出环境变量", "读取环境文件"]
def 注册能力(注册表) -> None:
    from 公共契约.能力契约.契约 import 能力实现
    注册表.注册(能力实现(能力id="系统核心支持库.环境变量.获取环境变量", 包id="支持库.后端.系统核心支持库.环境变量", 实现函数=获取环境变量, 参数=[{"名称": "名称", "类型": "文本型", "必填": True, "说明": "环境变量名"}, {"名称": "包含敏感值", "类型": "逻辑型", "必填": False, "说明": "是否回显敏感键明文（默认 否：敏感键的值掩码为 ***）"}], 返回="结果", 说明="获取环境变量值；敏感键（键名含 KEY/TOKEN/SECRET/PASSWORD/凭据/密钥 等）的值默认掩码为 ***，明文需传 包含敏感值=True"))
    注册表.注册(能力实现(能力id="系统核心支持库.环境变量.设置环境变量", 包id="支持库.后端.系统核心支持库.环境变量", 实现函数=设置环境变量, 参数=[{"名称": "名称", "类型": "文本型", "必填": True, "说明": "环境变量名"}, {"名称": "值", "类型": "文本型", "必填": True, "说明": "值"}], 返回="结果", 说明="设置环境变量"))
    注册表.注册(能力实现(能力id="系统核心支持库.环境变量.删除环境变量", 包id="支持库.后端.系统核心支持库.环境变量", 实现函数=删除环境变量, 参数=[{"名称": "名称", "类型": "文本型", "必填": True, "说明": "环境变量名"}], 返回="结果", 说明="删除环境变量"))
    注册表.注册(能力实现(能力id="系统核心支持库.环境变量.列出环境变量", 包id="支持库.后端.系统核心支持库.环境变量", 实现函数=列出环境变量, 参数=[{"名称": "前缀", "类型": "文本型", "必填": False, "说明": "过滤前缀"}, {"名称": "包含敏感值", "类型": "逻辑型", "必填": False, "说明": "是否回显敏感键明文（默认 否：敏感键的值掩码为 ***）"}], 返回="结果", 说明="列出环境变量（可过滤前缀）；敏感键（键名含 KEY/TOKEN/SECRET/PASSWORD/凭据/密钥 等）的值默认掩码为 ***，明文需传 包含敏感值=True"))
    注册表.注册(能力实现(能力id="系统核心支持库.环境变量.读取环境文件", 包id="支持库.后端.系统核心支持库.环境变量", 实现函数=读取环境文件, 参数=[{"名称": "文件路径", "类型": "文本型", "必填": True, "说明": ".env 文件绝对路径"}, {"名称": "写入进程", "类型": "逻辑型", "必填": False, "说明": "是否同时写入当前进程环境（默认否）"}, {"名称": "覆盖已存在", "类型": "逻辑型", "必填": False, "说明": "写入进程时是否覆盖已有键（默认否）"}, {"名称": "包含敏感值", "类型": "逻辑型", "必填": False, "说明": "是否回显敏感键明文（默认 否：敏感键的值掩码为 ***；只影响回显，不影响写入进程）"}], 返回="结果", 说明="读取 .env 文件为键值字典；跳过空行与 # 注释，值剥离成对引号；敏感键的值默认掩码为 ***"))
