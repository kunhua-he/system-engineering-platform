<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 环境变量（支持库.后端.系统核心支持库.环境变量）

- 类型：支持库
- 版本：1.1.0
- 说明：环境变量读/写/删/列
- 入口：__init__.py

## 能力
### 获取环境变量（`系统核心支持库.环境变量.获取环境变量`）
- 版本：1.0.0
- 一句话说明：获取环境变量值
- 所属支持库或模块：支持库.后端.系统核心支持库.环境变量
- 参数列表：名称:文本型
- 必填参数：名称
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.环境变量.获取环境变量()
- 依赖能力：无
### 设置环境变量（`系统核心支持库.环境变量.设置环境变量`）
- 版本：1.0.0
- 一句话说明：设置环境变量（进程内）
- 所属支持库或模块：支持库.后端.系统核心支持库.环境变量
- 参数列表：名称:文本型, 值:文本型
- 必填参数：名称, 值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.环境变量.设置环境变量()
- 依赖能力：无
### 删除环境变量（`系统核心支持库.环境变量.删除环境变量`）
- 版本：1.0.0
- 一句话说明：删除环境变量
- 所属支持库或模块：支持库.后端.系统核心支持库.环境变量
- 参数列表：名称:文本型
- 必填参数：名称
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.环境变量.删除环境变量()
- 依赖能力：无
### 列出环境变量（`系统核心支持库.环境变量.列出环境变量`）
- 版本：1.0.0
- 一句话说明：列出环境变量（可过滤前缀）
- 所属支持库或模块：支持库.后端.系统核心支持库.环境变量
- 参数列表：前缀:文本型
- 必填参数：无
- 可选参数：前缀
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.列出环境变量()
- 依赖能力：无
### 读取环境文件（`系统核心支持库.环境变量.读取环境文件`）
- 版本：1.0.0
- 一句话说明：读取 .env 文件为键值字典；跳过空行与 # 注释，值剥离成对引号；可选写入当前进程
- 所属支持库或模块：支持库.后端.系统核心支持库.环境变量
- 参数列表：文件路径:文本型, 写入进程:逻辑型, 覆盖已存在:逻辑型
- 必填参数：文件路径
- 可选参数：写入进程, 覆盖已存在
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 读取失败
- 调用示例：系统核心支持库.环境变量.读取环境文件(写入进程=False, 覆盖已存在=False)
- 依赖能力：无
