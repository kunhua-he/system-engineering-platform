# 系统核心支持库（支持库.后端.系统核心支持库）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：2.0.2
- 说明：聚合支持库，包含 14 个子域：删除守卫/协议白名单/命令安全/工具执行/幂等消息/权限审批/检查点恢复/环境变量/系统信息/资源管理/路径安全/进程管理/限流/额度控制
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查删除（`系统核心支持库.删除守卫.检查删除`）
- 版本：2.0.2
- 一句话说明：检查一次删除操作是否安全：受保护表拒绝、无条件删除拒绝、删除比例超上限拒绝。返回 {通过, 原因}。
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标表:文本型, 删除比例:双精度数型, 过滤条件:文本型, 比例上限:双精度数型, 保护表:列表型
- 必填参数：目标表
- 可选参数：删除比例, 过滤条件, 比例上限, 保护表
- 返回结构：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.删除守卫.检查删除()
- 依赖能力：无
### 检查清空（`系统核心支持库.删除守卫.检查清空`）
- 版本：2.0.2
- 一句话说明：检查清空整表操作：受保护表拒绝。返回 {通过, 原因}。
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标表:文本型, 保护表:列表型
- 必填参数：目标表
- 可选参数：保护表
- 返回结构：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.删除守卫.检查清空()
- 依赖能力：无
### 校验端点（`系统核心支持库.协议白名单.校验端点`）
- 版本：2.0.2
- 一句话说明：校验 api类型 是否在白名单内，openai-compatible 必须有 base_url 且协议为 http/https。返回 {通过, 原因}。
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：api类型:文本型, base_url:文本型
- 必填参数：api类型
- 可选参数：base_url
- 返回结构：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.协议白名单.校验端点()
- 依赖能力：无
### 查询白名单（`系统核心支持库.协议白名单.查询白名单`）
- 版本：2.0.2
- 一句话说明：返回当前白名单列表。
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'白名单': '列表型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.协议白名单.查询白名单()
- 依赖能力：无
### 检测危险命令（`系统核心支持库.命令安全.检测危险命令`）
- 版本：2.0.2
- 一句话说明：检测命令是否命中危险模式，返回 {危险, 原因}；只判定不执行
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型, 最大长度:整数型
- 必填参数：命令
- 可选参数：最大长度
- 返回结构：{'类型': '结果型', '值结构': {'危险': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.命令安全.检测危险命令()
- 依赖能力：无
### 受理工具调用（`系统核心支持库.工具执行.受理工具调用`）
- 版本：2.0.2
- 一句话说明：受理工具调用，返回执行id（两段式受理）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：工具名:文本型, 参数:字典型, 超时秒:整数型
- 必填参数：工具名
- 可选参数：参数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.受理工具调用()
- 依赖能力：无
### 登记执行结果（`系统核心支持库.工具执行.登记执行结果`）
- 版本：2.0.2
- 一句话说明：登记执行结果（成功或失败，失败带错误标记）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：执行id:文本型, 成功:逻辑型, 结果值:字典型, 错误:文本型
- 必填参数：执行id, 成功
- 可选参数：结果值, 错误
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.登记执行结果()
- 依赖能力：无
### 查询执行结果（`系统核心支持库.工具执行.查询执行结果`）
- 版本：2.0.2
- 一句话说明：查询执行结果
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：执行id:文本型
- 必填参数：执行id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.查询执行结果()
- 依赖能力：无
### 清理执行记录（`系统核心支持库.工具执行.清理执行记录`）
- 版本：2.0.2
- 一句话说明：清理执行记录（幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：执行id:文本型
- 必填参数：执行id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.清理执行记录()
- 依赖能力：无
### 登记投递（`系统核心支持库.工具执行.登记投递`）
- 版本：2.0.2
- 一句话说明：登记投递（Hermes claim 模式化：SQLite落库重启不丢，0限制原文存取）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：队列名:文本型, 负载:字典型, 库路径:文本型
- 必填参数：队列名, 负载
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.登记投递()
- 依赖能力：无
### 认领投递（`系统核心支持库.工具执行.认领投递`）
- 版本：2.0.2
- 一句话说明：原子认领最早待认领投递
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：队列名:文本型, 认领者:文本型, 库路径:文本型
- 必填参数：队列名
- 可选参数：认领者, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.认领投递()
- 依赖能力：无
### 完成投递（`系统核心支持库.工具执行.完成投递`）
- 版本：2.0.2
- 一句话说明：投递完成登记（成功/失败）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：投递id:文本型, 成功:逻辑型, 结果值:字典型, 库路径:文本型
- 必填参数：投递id, 成功
- 可选参数：结果值, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.完成投递()
- 依赖能力：无
### 补投超时（`系统核心支持库.工具执行.补投超时`）
- 版本：2.0.2
- 一句话说明：已认领超时未完成→改回待认领（重启补投语义）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：队列名:文本型, 超时秒:整数型, 库路径:文本型
- 必填参数：队列名
- 可选参数：超时秒, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.补投超时()
- 依赖能力：无
### 生成id（`系统核心支持库.幂等消息.生成id`）
- 版本：2.0.2
- 一句话说明：生成幂等 id = sha256(会话id|内容) 截断 24 位，可带前缀。同一 会话id+内容 永远同一 id。
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 内容:文本型, 前缀:文本型
- 必填参数：会话id, 内容
- 可选参数：前缀
- 返回结构：{'类型': '结果型', '值结构': {'幂等id': '文本型', '算法': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.幂等消息.生成id()
- 依赖能力：无
### 校验id（`系统核心支持库.幂等消息.校验id`）
- 版本：2.0.2
- 一句话说明：校验幂等 id 格式（24位十六进制 或 前缀-24位）。
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：幂等id:文本型
- 必填参数：幂等id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'有效': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.幂等消息.校验id()
- 依赖能力：无
### 校验动作（`系统核心支持库.权限审批.校验动作`）
- 版本：2.0.2
- 一句话说明：校验动作是否放行（允许/拒绝/询问）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：动作类型:文本型, 目标:文本型
- 必填参数：动作类型
- 可选参数：目标
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.权限审批.校验动作()
- 依赖能力：无
### 登记规则（`系统核心支持库.权限审批.登记规则`）
- 版本：2.0.2
- 一句话说明：登记/更新动作规则策略
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：动作类型:文本型, 策略:文本型
- 必填参数：动作类型, 策略
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.权限审批.登记规则()
- 依赖能力：无
### 查询规则表（`系统核心支持库.权限审批.查询规则表`）
- 版本：2.0.2
- 一句话说明：返回当前全部动作规则
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.权限审批.查询规则表()
- 依赖能力：无
### 保存检查点（`系统核心支持库.检查点恢复.保存检查点`）
- 版本：2.0.2
- 一句话说明：保存会话检查点（SQLite，返回检查点id）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 状态快照:字典型, 版本:文本型, 库路径:文本型, 中断原因:文本型
- 必填参数：会话id, 状态快照
- 可选参数：版本, 库路径, 中断原因
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.保存检查点()
- 依赖能力：无
### 恢复检查点（`系统核心支持库.检查点恢复.恢复检查点`）
- 版本：2.0.2
- 一句话说明：恢复会话最新检查点
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 库路径:文本型
- 必填参数：会话id
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.恢复检查点()
- 依赖能力：无
### 登记中断（`系统核心支持库.检查点恢复.登记中断`）
- 版本：2.0.2
- 一句话说明：登记会话中断（Coze InterruptID 语义：保存检查点+中断原因，返回中断id）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 状态快照:字典型, 中断原因:文本型, 版本:文本型, 库路径:文本型
- 必填参数：会话id, 状态快照, 中断原因
- 可选参数：版本, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.登记中断()
- 依赖能力：无
### 续跑检查点（`系统核心支持库.检查点恢复.续跑检查点`）
- 版本：2.0.2
- 一句话说明：带中断id续跑：校验中断→恢复→标记已续跑（幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 中断id:文本型, 库路径:文本型
- 必填参数：会话id, 中断id
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.续跑检查点()
- 依赖能力：无
### 查询中断（`系统核心支持库.检查点恢复.查询中断`）
- 版本：2.0.2
- 一句话说明：查询会话未续跑的中断列表
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 库路径:文本型
- 必填参数：会话id
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.查询中断()
- 依赖能力：无
### 创建任务（`系统核心支持库.检查点恢复.创建任务`）
- 版本：2.0.2
- 一句话说明：创建任务（OpenClaw 任务注册表模式化：状态=排队，SQLite落库重启不丢）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：任务名:文本型, 归属人:文本型, 库路径:文本型
- 必填参数：任务名
- 可选参数：归属人, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.创建任务()
- 依赖能力：无
### 领取任务（`系统核心支持库.检查点恢复.领取任务`）
- 版本：2.0.2
- 一句话说明：领取任务（排队→运行中，仅排队态可领幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：任务id:文本型, 执行者:文本型, 库路径:文本型
- 必填参数：任务id
- 可选参数：执行者, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.领取任务()
- 依赖能力：无
### 完成任务（`系统核心支持库.检查点恢复.完成任务`）
- 版本：2.0.2
- 一句话说明：完成任务（运行中→已完成/已失败，终态不可再改）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：任务id:文本型, 成功:逻辑型, 结果值:字典型, 库路径:文本型
- 必填参数：任务id, 成功
- 可选参数：结果值, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.完成任务()
- 依赖能力：无
### 查询任务（`系统核心支持库.检查点恢复.查询任务`）
- 版本：2.0.2
- 一句话说明：查询任务状态（按任务id 或 归属人列表）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：任务id:文本型, 归属人:文本型, 库路径:文本型
- 必填参数：无
- 可选参数：任务id, 归属人, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.查询任务()
- 依赖能力：无
### 续跑任务（`系统核心支持库.检查点恢复.续跑任务`）
- 版本：2.0.2
- 一句话说明：续跑任务（已失败/已完成→排队，DeepSeek cold resume 语义）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：任务id:文本型, 执行者:文本型, 库路径:文本型
- 必填参数：任务id
- 可选参数：执行者, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.续跑任务()
- 依赖能力：无
### 超时重排队（`系统核心支持库.检查点恢复.超时重排队`）
- 版本：2.0.2
- 一句话说明：运行中超时→排队（worker 崩溃恢复语义）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：任务id:文本型, 超时秒:整数型, 库路径:文本型
- 必填参数：任务id
- 可选参数：超时秒, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.超时重排队()
- 依赖能力：无
### 获取环境变量（`系统核心支持库.环境变量.获取环境变量`）
- 版本：2.0.2
- 一句话说明：获取环境变量值；敏感键（键名含 KEY/TOKEN/SECRET/PASSWORD/凭据/密钥 等）的值默认掩码为 ***，明文需传 包含敏感值=True
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：名称:文本型, 包含敏感值:逻辑型
- 必填参数：名称
- 可选参数：包含敏感值
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.获取环境变量()
- 依赖能力：无
### 设置环境变量（`系统核心支持库.环境变量.设置环境变量`）
- 版本：2.0.2
- 一句话说明：设置环境变量（进程内）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：名称:文本型, 值:文本型
- 必填参数：名称, 值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.设置环境变量()
- 依赖能力：无
### 删除环境变量（`系统核心支持库.环境变量.删除环境变量`）
- 版本：2.0.2
- 一句话说明：删除环境变量
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：名称:文本型
- 必填参数：名称
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.删除环境变量()
- 依赖能力：无
### 列出环境变量（`系统核心支持库.环境变量.列出环境变量`）
- 版本：2.0.2
- 一句话说明：列出环境变量（可过滤前缀）；敏感键（键名含 KEY/TOKEN/SECRET/PASSWORD/凭据/密钥 等）的值默认掩码为 ***，明文需传 包含敏感值=True
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：前缀:文本型, 包含敏感值:逻辑型
- 必填参数：无
- 可选参数：前缀, 包含敏感值
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.列出环境变量()
- 依赖能力：无
### 读取环境文件（`系统核心支持库.环境变量.读取环境文件`）
- 版本：2.0.2
- 一句话说明：读取 .env 文件为键值字典；跳过空行与 # 注释，值剥离成对引号；可选写入当前进程（写进程用真实值）；敏感键（键名含 KEY/TOKEN/SECRET/PASSWORD/凭据/密钥 等）的值默认掩码为 ***，明文需传 包含敏感值=True
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：文件路径:文本型, 写入进程:逻辑型, 覆盖已存在:逻辑型, 包含敏感值:逻辑型
- 必填参数：文件路径
- 可选参数：写入进程, 覆盖已存在, 包含敏感值
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.读取环境文件()
- 依赖能力：无
### 获取操作系统信息（`系统核心支持库.系统信息.获取操作系统信息`）
- 版本：2.0.2
- 一句话说明：操作系统/版本/架构/主机名
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取操作系统信息()
- 依赖能力：无
### 获取CPU信息（`系统核心支持库.系统信息.获取CPU信息`）
- 版本：2.0.2
- 一句话说明：CPU 核数/负载
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取CPU信息()
- 依赖能力：无
### 获取内存信息（`系统核心支持库.系统信息.获取内存信息`）
- 版本：2.0.2
- 一句话说明：内存总量（字节）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'总量字节': '整数型，内存总量（字节）', '可用字节': '整数型，可用内存（字节）；macOS 需经 vm_stat 计算，暂为空值', '已用字节': '整数型，已用内存（字节）；同上', '说明': '文本型，口径说明'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取内存信息()
- 依赖能力：无
### 获取磁盘信息（`系统核心支持库.系统信息.获取磁盘信息`）
- 版本：2.0.2
- 一句话说明：磁盘用量（字节）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：路径:文本型
- 必填参数：无
- 可选参数：路径
- 返回结构：{'类型': '结果型', '值结构': {'路径': '文本型，磁盘路径', '总量字节': '整数型，磁盘总量（字节）', '可用字节': '整数型，可用空间（字节）', '已用字节': '整数型，已用空间（字节）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取磁盘信息()
- 依赖能力：无
### 获取进程列表（`系统核心支持库.系统信息.获取进程列表`）
- 版本：2.0.2
- 一句话说明：进程列表（按CPU排序）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：排序:文本型
- 必填参数：无
- 可选参数：排序
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取进程列表()
- 依赖能力：无
### 创建内容摘要（`系统核心支持库.资源管理.创建内容摘要`）
- 版本：2.0.2
- 一句话说明：创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本（平台既有契约：文件管理/自修复工具直接消费摘要文本）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：文件路径:文本型, 算法:文本型
- 必填参数：文件路径
- 可选参数：算法
- 返回结构：{'类型': '结果型', '值结构': {'值': '内容摘要文本（hex 字符串）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建内容摘要()
- 依赖能力：无
### 创建不可变快照（`系统核心支持库.资源管理.创建不可变快照`）
- 版本：2.0.2
- 一句话说明：创建不可变快照（复制 + 快照摘要 + 只读标记）；成功返回快照摘要路径（平台既有契约）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：来源目录:文本型, 快照目录:文本型
- 必填参数：来源目录, 快照目录
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '快照摘要文件路径'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建不可变快照()
- 依赖能力：无
### 创建唯一运行目录（`系统核心支持库.资源管理.创建唯一运行目录`）
- 版本：2.0.2
- 一句话说明：创建唯一运行目录（临时目录语义，调用方负责释放）；返回目录路径（平台既有契约：运行核心/自修复工具直接消费路径）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：基础目录:文本型, 前缀:文本型
- 必填参数：基础目录
- 可选参数：前缀
- 返回结构：{'类型': '结果型', '值结构': {'值': '唯一运行目录路径'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建唯一运行目录()
- 依赖能力：无
### 原子写入（`系统核心支持库.资源管理.原子写入`）
- 版本：2.0.2
- 一句话说明：原子写入：临时文件 + fsync + rename；默认保留目标文件原权限位，写入后同步父目录
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 内容:文本型, 内容字节:字节集型, 保留权限:逻辑型
- 必填参数：目标路径
- 可选参数：内容, 内容字节, 保留权限
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（真值）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.原子写入()
- 依赖能力：无
### 原子替换（`系统核心支持库.资源管理.原子替换`）
- 版本：2.0.2
- 一句话说明：原子替换文件内容（可选版本比较），返回 (成功, 消息)（平台既有契约）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 新内容:文本型, 期望版本:文本型
- 必填参数：目标路径, 新内容
- 可选参数：期望版本
- 返回结构：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.原子替换()
- 依赖能力：无
### 比较并交换（`系统核心支持库.资源管理.比较并交换`）
- 版本：2.0.2
- 一句话说明：比较并交换（CAS）：读取 → 比较 → 原子写回；并发冲突返回 版本冲突，返回 (成功, 消息)（平台既有契约）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 期望值:文本型, 新值:文本型
- 必填参数：目标路径, 期望值, 新值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.比较并交换()
- 依赖能力：无
### 资源短锁（`系统核心支持库.资源管理.资源短锁`）
- 版本：2.0.2
- 一句话说明：原子获取并释放资源级跨进程短锁，返回可传输执行结果
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：锁目录:文本型, 资源id:文本型, 持有者:文本型
- 必填参数：锁目录, 资源id
- 可选参数：持有者
- 返回结构：{'类型': '结果型', '值结构': {'已获取': '逻辑型', '已释放': '逻辑型', '持有者': '文本型', '锁路径': '文本型', '说明': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.资源短锁()
- 依赖能力：无
### 安全释放（`系统核心支持库.资源管理.安全释放`）
- 版本：2.0.2
- 一句话说明：安全释放资源（文件删除/目录递归删除，不存在视为幂等成功），返回 (成功, 消息)（平台既有契约：文档解析模块直接消费）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.安全释放()
- 依赖能力：无
### 创建受管状态（`系统核心支持库.资源管理.创建受管状态`）
- 版本：2.0.2
- 一句话说明：创建受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：资源id:文本型, 初始状态:字典型
- 必填参数：资源id, 初始状态
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建受管状态()
- 依赖能力：无
### 读取受管状态（`系统核心支持库.资源管理.读取受管状态`）
- 版本：2.0.2
- 一句话说明：读取受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.读取受管状态()
- 依赖能力：无
### 更新受管状态（`系统核心支持库.资源管理.更新受管状态`）
- 版本：2.0.2
- 一句话说明：更新受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型, 新状态:字典型, 期望版本:文本型
- 必填参数：句柄, 新状态
- 可选参数：期望版本
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.更新受管状态()
- 依赖能力：无
### 释放受管状态（`系统核心支持库.资源管理.释放受管状态`）
- 版本：2.0.2
- 一句话说明：释放受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.释放受管状态()
- 依赖能力：无
### 创建审计账本（`系统核心支持库.资源管理.创建审计账本`）
- 版本：2.0.2
- 一句话说明：创建审计账本（OpenClaw 模式化：默认开/上限10万/保留30天，0加密）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：账本名:文本型, 上限:整数型, 保留天:整数型
- 必填参数：账本名
- 可选参数：上限, 保留天
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建审计账本()
- 依赖能力：无
### 记审计事件（`系统核心支持库.资源管理.记审计事件`）
- 版本：2.0.2
- 一句话说明：记录审计事件（底座0限制，完整记录传入字段；脱敏由业务端自理）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：账本名:文本型, 事件类型:文本型, 主体:文本型, 对象:文本型, 结果码:文本型, 耗时毫秒:整数型, 通道:文本型, 方向:文本型, 元数据:字典型
- 必填参数：账本名, 事件类型
- 可选参数：主体, 对象, 结果码, 耗时毫秒, 通道, 方向, 元数据
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.记审计事件()
- 依赖能力：无
### 查审计事件（`系统核心支持库.资源管理.查审计事件`）
- 版本：2.0.2
- 一句话说明：按条件查询审计事件（原文匹配）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：账本名:文本型, 事件类型:文本型, 主体:文本型, 对象:文本型, 条数:整数型
- 必填参数：账本名
- 可选参数：事件类型, 主体, 对象, 条数
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.查审计事件()
- 依赖能力：无
### 清空审计账本（`系统核心支持库.资源管理.清空审计账本`）
- 版本：2.0.2
- 一句话说明：清空审计账本（谨慎操作）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：账本名:文本型
- 必填参数：账本名
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.清空审计账本()
- 依赖能力：无
### 登记配置快照（`系统核心支持库.资源管理.登记配置快照`）
- 版本：2.0.2
- 一句话说明：登记配置当前快照指纹（改前基线，OpenClaw config-journal 模式化）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：配置名:文本型, 配置:字典型, 操作人:文本型
- 必填参数：配置名, 配置
- 可选参数：操作人
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.登记配置快照()
- 依赖能力：无
### 对比配置指纹（`系统核心支持库.资源管理.对比配置指纹`）
- 版本：2.0.2
- 一句话说明：对比新配置与基线；变更记 journal（改前/新指纹/操作人）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：配置名:文本型, 新配置:字典型, 操作人:文本型
- 必填参数：配置名, 新配置
- 可选参数：操作人
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.对比配置指纹()
- 依赖能力：无
### 查配置指纹（`系统核心支持库.资源管理.查配置指纹`）
- 版本：2.0.2
- 一句话说明：查询配置指纹与变更记录（回答'什么时候被谁改的'）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：配置名:文本型
- 必填参数：配置名
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.查配置指纹()
- 依赖能力：无
### 登记回滚步骤（`系统核心支持库.资源管理.登记回滚步骤`）
- 版本：2.0.2
- 一句话说明：登记可回滚步骤（DeepSeek 逆序回滚模式化：建链任一步失败按逆序释放）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：事务id:文本型, 步骤名:文本型, 回滚动作:字典型
- 必填参数：事务id, 步骤名, 回滚动作
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.登记回滚步骤()
- 依赖能力：无
### 执行逆序回滚（`系统核心支持库.资源管理.执行逆序回滚`）
- 版本：2.0.2
- 一句话说明：按登记逆序执行全部回滚（0限制：底座只编排，动作由调用者回滚器执行）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：事务id:文本型, 回滚器:字典型
- 必填参数：事务id
- 可选参数：回滚器
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.执行逆序回滚()
- 依赖能力：无
### 查询回滚事务（`系统核心支持库.资源管理.查询回滚事务`）
- 版本：2.0.2
- 一句话说明：查询事务回滚状态
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：事务id:文本型
- 必填参数：事务id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.查询回滚事务()
- 依赖能力：无
### 校验路径（`系统核心支持库.路径安全.校验路径`）
- 版本：2.0.2
- 一句话说明：校验相对路径是否安全落在根目录内（防穿越/符号链接逃逸）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：根目录:文本型, 相对路径:文本型
- 必填参数：根目录, 相对路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.路径安全.校验路径()
- 依赖能力：无
### 校验文件名（`系统核心支持库.路径安全.校验文件名`）
- 版本：2.0.2
- 一句话说明：校验文件名安全（非空/无分隔符/非隐藏）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：文件名:文本型
- 必填参数：文件名
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.路径安全.校验文件名()
- 依赖能力：无
### 显示相对路径（`系统核心支持库.路径安全.显示相对路径`）
- 版本：2.0.2
- 一句话说明：把绝对路径显示为相对基准目录的字符串；不在任一基准下则原样返回
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 基准目录:文本型, 备选基准:文本型
- 必填参数：目标路径
- 可选参数：基准目录, 备选基准
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.路径安全.显示相对路径()
- 依赖能力：无
### 启动进程（`系统核心支持库.进程管理.启动进程`）
- 版本：2.0.2
- 一句话说明：启动外部进程，返回句柄（argv 直启，不经 shell，不内建危险命令检测）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型, 参数:列表型, 工作目录:文本型, 环境变量:字典型, 超时秒:整数型, 就绪地址:文本型, 就绪超时秒:双精度数型
- 必填参数：命令
- 可选参数：参数, 工作目录, 环境变量, 超时秒, 就绪地址, 就绪超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.启动进程()
- 依赖能力：无
### 终止进程（`系统核心支持库.进程管理.终止进程`）
- 版本：2.0.2
- 一句话说明：终止进程
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型, 强制:逻辑型
- 必填参数：句柄
- 可选参数：强制
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.终止进程()
- 依赖能力：无
### 查询进程状态（`系统核心支持库.进程管理.查询进程状态`）
- 版本：2.0.2
- 一句话说明：查询进程运行状态
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.查询进程状态()
- 依赖能力：无
### 等待进程结束（`系统核心支持库.进程管理.等待进程结束`）
- 版本：2.0.2
- 一句话说明：等待进程结束并获取输出
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型, 超时秒:双精度数型
- 必填参数：句柄
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.等待进程结束()
- 依赖能力：无
### 执行命令（`系统核心支持库.进程管理.执行命令`）
- 版本：2.0.2
- 一句话说明：执行 shell 命令并等待；执行前经 命令安全.检测危险命令 前置拦截
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型, 超时秒:双精度数型, 工作目录:文本型
- 必填参数：命令
- 可选参数：超时秒, 工作目录
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.执行命令()
- 依赖能力：无
### 检查命令可用（`系统核心支持库.进程管理.检查命令可用`）
- 版本：2.0.2
- 一句话说明：检查命令是否可用（which）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型
- 必填参数：命令
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.检查命令可用()
- 依赖能力：无
### 释放句柄（`系统核心支持库.进程管理.释放句柄`）
- 版本：2.0.2
- 一句话说明：释放进程句柄（幂等，强制杀残留）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.释放句柄()
- 依赖能力：无
### 沙箱执行命令（`系统核心支持库.进程管理.沙箱执行命令`）
- 版本：2.0.2
- 一句话说明：在 macOS sandbox-exec 内核沙箱内执行 shell 命令：系统只读、仅工作目录可读写；工作目录含引号/反斜杠/换行/右括号即拒；profile 编译预检失败 fail-closed；命中危险命令前置拦截；无沙箱平台 fail-closed
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型, 工作目录:文本型, 超时秒:双精度数型, 输出上限字节:整数型, 环境变量:字典型
- 必填参数：命令, 工作目录
- 可选参数：超时秒, 输出上限字节, 环境变量
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.沙箱执行命令()
- 依赖能力：无
### 令牌桶判定（`系统核心支持库.限流.令牌桶判定`）
- 版本：2.0.2
- 一句话说明：令牌桶惰性补充与取用判定，返回 {放行, 当前令牌数, 上次补充时间}；状态由调用方持有
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：桶容量:整数型, 每秒补充:双精度数型, 当前令牌数:双精度数型, 上次补充时间:双精度数型, 现在:双精度数型, 取用数量:整数型
- 必填参数：桶容量, 每秒补充, 当前令牌数, 上次补充时间
- 可选参数：现在, 取用数量
- 返回结构：{'类型': '结果型', '值结构': {'放行': '逻辑型', '当前令牌数': '双精度数型', '上次补充时间': '双精度数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.限流.令牌桶判定()
- 依赖能力：无
### 生成请求编号（`系统核心支持库.限流.生成请求编号`）
- 版本：2.0.2
- 一句话说明：生成唯一请求编号（十六进制片段），返回 {请求编号}
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：长度:整数型
- 必填参数：无
- 可选参数：长度
- 返回结构：{'类型': '结果型', '值结构': {'请求编号': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.限流.生成请求编号()
- 依赖能力：无
### 登录限速判定（`系统核心支持库.限流.登录限速判定`）
- 版本：2.0.2
- 一句话说明：登录失败限速判定，返回 {锁定, 原因, 剩余可尝试次数}
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：当前失败次数:整数型, 最大失败次数:整数型, 锁定剩余秒:双精度数型
- 必填参数：当前失败次数, 最大失败次数
- 可选参数：锁定剩余秒
- 返回结构：{'类型': '结果型', '值结构': {'锁定': '逻辑型', '原因': '文本型', '剩余可尝试次数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.限流.登录限速判定()
- 依赖能力：无
### 配置额度（`系统核心支持库.额度控制.配置额度`）
- 版本：2.0.2
- 一句话说明：按调用方/动作配置额度（默认不限制，配置后生效）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：额度键:文本型, 额度项:文本型, 额度值:整数型
- 必填参数：额度键, 额度项, 额度值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.额度控制.配置额度()
- 依赖能力：无
### 查询额度（`系统核心支持库.额度控制.查询额度`）
- 版本：2.0.2
- 一句话说明：查询当前额度配置
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：额度键:文本型
- 必填参数：额度键
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.额度控制.查询额度()
- 依赖能力：无
### 检查额度（`系统核心支持库.额度控制.检查额度`）
- 版本：2.0.2
- 一句话说明：检查额度是否超限（0=不限制永远通过）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：额度键:文本型, 额度项:文本型, 已用值:整数型
- 必填参数：额度键, 额度项, 已用值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.额度控制.检查额度()
- 依赖能力：无
