# 系统核心支持库（支持库.后端.系统核心支持库）

- 类型：支持库
- 版本：2.0.1
- 说明：聚合支持库，包含 10 个子域：会话存储/删除守卫/协议白名单/工具执行/幂等消息/权限审批/检查点恢复/资源管理/路径安全/额度控制
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查删除（`系统核心支持库.删除守卫.检查删除`）
- 版本：2.0.1
- 一句话说明：检查删除是否安全
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标表:文本型, 删除比例:双精度数型, 过滤条件:文本型, 比例上限:双精度数型, 保护表:列表型
- 必填参数：目标表
- 可选参数：删除比例, 过滤条件, 比例上限, 保护表
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.删除守卫.检查删除()
- 依赖能力：无
### 检查清空（`系统核心支持库.删除守卫.检查清空`）
- 版本：2.0.1
- 一句话说明：检查清空是否安全
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标表:文本型, 保护表:列表型
- 必填参数：目标表
- 可选参数：保护表
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.删除守卫.检查清空()
- 依赖能力：无
### 校验端点（`系统核心支持库.协议白名单.校验端点`）
- 版本：2.0.1
- 一句话说明：校验端点协议是否在白名单内
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：api类型:文本型, base_url:文本型
- 必填参数：api类型
- 可选参数：base_url
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.协议白名单.校验端点()
- 依赖能力：无
### 查询白名单（`系统核心支持库.协议白名单.查询白名单`）
- 版本：2.0.1
- 一句话说明：返回白名单列表
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.协议白名单.查询白名单()
- 依赖能力：无
### 受理工具调用（`系统核心支持库.工具执行.受理工具调用`）
- 版本：2.0.1
- 一句话说明：受理工具调用，返回执行id（两段式受理）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：工具名:文本型, 参数:字典型, 超时秒:整数型
- 必填参数：工具名
- 可选参数：参数, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.受理工具调用()
- 依赖能力：无
### 登记执行结果（`系统核心支持库.工具执行.登记执行结果`）
- 版本：2.0.1
- 一句话说明：登记执行结果（成功或失败，失败带错误标记）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：执行id:文本型, 成功:逻辑型, 结果值:字典型, 错误:文本型
- 必填参数：执行id, 成功
- 可选参数：结果值, 错误
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.登记执行结果()
- 依赖能力：无
### 查询执行结果（`系统核心支持库.工具执行.查询执行结果`）
- 版本：2.0.1
- 一句话说明：查询执行结果
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：执行id:文本型
- 必填参数：执行id
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.查询执行结果()
- 依赖能力：无
### 清理执行记录（`系统核心支持库.工具执行.清理执行记录`）
- 版本：2.0.1
- 一句话说明：清理执行记录（幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：执行id:文本型
- 必填参数：执行id
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.工具执行.清理执行记录()
- 依赖能力：无
### 生成id（`系统核心支持库.幂等消息.生成id`）
- 版本：2.0.1
- 一句话说明：生成幂等 id
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 内容:文本型, 前缀:文本型
- 必填参数：会话id, 内容
- 可选参数：前缀
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.幂等消息.生成id()
- 依赖能力：无
### 校验id（`系统核心支持库.幂等消息.校验id`）
- 版本：2.0.1
- 一句话说明：校验幂等 id 格式
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：幂等id:文本型
- 必填参数：幂等id
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.幂等消息.校验id()
- 依赖能力：无
### 校验动作（`系统核心支持库.权限审批.校验动作`）
- 版本：2.0.1
- 一句话说明：校验动作是否放行（允许/拒绝/询问）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：动作类型:文本型, 目标:文本型
- 必填参数：动作类型
- 可选参数：目标
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.权限审批.校验动作()
- 依赖能力：无
### 登记规则（`系统核心支持库.权限审批.登记规则`）
- 版本：2.0.1
- 一句话说明：登记/更新动作规则策略
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：动作类型:文本型, 策略:文本型
- 必填参数：动作类型, 策略
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.权限审批.登记规则()
- 依赖能力：无
### 查询规则表（`系统核心支持库.权限审批.查询规则表`）
- 版本：2.0.1
- 一句话说明：返回当前全部动作规则
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.权限审批.查询规则表()
- 依赖能力：无
### 保存检查点（`系统核心支持库.检查点恢复.保存检查点`）
- 版本：2.0.1
- 一句话说明：保存会话检查点（SQLite，返回检查点id）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型, 状态快照:字典型, 版本:文本型
- 必填参数：会话id, 状态快照
- 可选参数：版本
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.保存检查点()
- 依赖能力：无
### 恢复检查点（`系统核心支持库.检查点恢复.恢复检查点`）
- 版本：2.0.1
- 一句话说明：恢复会话最新检查点
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：会话id:文本型
- 必填参数：会话id
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.检查点恢复.恢复检查点()
- 依赖能力：无
### 获取环境变量（`系统核心支持库.环境变量.获取环境变量`）
- 版本：2.0.1
- 一句话说明：获取环境变量值
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：名称:文本型
- 必填参数：名称
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.获取环境变量()
- 依赖能力：无
### 设置环境变量（`系统核心支持库.环境变量.设置环境变量`）
- 版本：2.0.1
- 一句话说明：设置环境变量（进程内）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：名称:文本型, 值:文本型
- 必填参数：名称, 值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.设置环境变量()
- 依赖能力：无
### 删除环境变量（`系统核心支持库.环境变量.删除环境变量`）
- 版本：2.0.1
- 一句话说明：删除环境变量
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：名称:文本型
- 必填参数：名称
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.删除环境变量()
- 依赖能力：无
### 列出环境变量（`系统核心支持库.环境变量.列出环境变量`）
- 版本：2.0.1
- 一句话说明：列出环境变量（可过滤前缀）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：前缀:文本型
- 必填参数：无
- 可选参数：前缀
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.环境变量.列出环境变量()
- 依赖能力：无
### 获取操作系统信息（`系统核心支持库.系统信息.获取操作系统信息`）
- 版本：2.0.1
- 一句话说明：操作系统/版本/架构/主机名
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取操作系统信息()
- 依赖能力：无
### 获取CPU信息（`系统核心支持库.系统信息.获取CPU信息`）
- 版本：2.0.1
- 一句话说明：CPU 核数/负载
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取CPU信息()
- 依赖能力：无
### 获取内存信息（`系统核心支持库.系统信息.获取内存信息`）
- 版本：2.0.1
- 一句话说明：内存总量（字节）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取内存信息()
- 依赖能力：无
### 获取磁盘信息（`系统核心支持库.系统信息.获取磁盘信息`）
- 版本：2.0.1
- 一句话说明：磁盘用量（字节）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：路径:文本型
- 必填参数：无
- 可选参数：路径
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取磁盘信息()
- 依赖能力：无
### 获取进程列表（`系统核心支持库.系统信息.获取进程列表`）
- 版本：2.0.1
- 一句话说明：进程列表（按CPU排序）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：排序:文本型
- 必填参数：无
- 可选参数：排序
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.系统信息.获取进程列表()
- 依赖能力：无
### 创建内容摘要（`系统核心支持库.资源管理.创建内容摘要`）
- 版本：2.0.1
- 一句话说明：创建文件内容摘要（sha256，分块读取）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：文件路径:文本型, 算法:文本型
- 必填参数：文件路径
- 可选参数：算法
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建内容摘要()
- 依赖能力：无
### 创建不可变快照（`系统核心支持库.资源管理.创建不可变快照`）
- 版本：2.0.1
- 一句话说明：创建不可变快照并生成快照摘要
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：来源目录:文本型, 快照目录:文本型
- 必填参数：来源目录, 快照目录
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建不可变快照()
- 依赖能力：无
### 创建唯一运行目录（`系统核心支持库.资源管理.创建唯一运行目录`）
- 版本：2.0.1
- 一句话说明：创建唯一运行目录（临时目录语义）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：基础目录:文本型, 前缀:文本型
- 必填参数：基础目录
- 可选参数：前缀
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.创建唯一运行目录()
- 依赖能力：无
### 原子写入（`系统核心支持库.资源管理.原子写入`）
- 版本：2.0.1
- 一句话说明：原子写入文件（临时文件+fsync+rename）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 内容:文本型
- 必填参数：目标路径, 内容
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.原子写入()
- 依赖能力：无
### 原子替换（`系统核心支持库.资源管理.原子替换`）
- 版本：2.0.1
- 一句话说明：原子替换文件内容（可选版本比较）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 新内容:文本型, 期望版本:文本型
- 必填参数：目标路径, 新内容
- 可选参数：期望版本
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.原子替换()
- 依赖能力：无
### 比较并交换（`系统核心支持库.资源管理.比较并交换`）
- 版本：2.0.1
- 一句话说明：CAS 提交（并发冲突返回版本冲突）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：目标路径:文本型, 期望值:文本型, 新值:文本型
- 必填参数：目标路径, 期望值, 新值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.比较并交换()
- 依赖能力：无
### 资源短锁（`系统核心支持库.资源管理.资源短锁`）
- 版本：2.0.1
- 一句话说明：资源级跨进程短锁（mkdir 原子性）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：锁目录:文本型, 资源id:文本型, 持有者:文本型
- 必填参数：锁目录, 资源id
- 可选参数：持有者
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.资源短锁()
- 依赖能力：无
### 安全释放资源（`系统核心支持库.资源管理.安全释放`）
- 版本：2.0.1
- 一句话说明：安全释放资源（幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.资源管理.安全释放()
- 依赖能力：无
### 校验路径（`系统核心支持库.路径安全.校验路径`）
- 版本：2.0.1
- 一句话说明：校验相对路径是否安全落在根目录内（防穿越/符号链接逃逸）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：根目录:文本型, 相对路径:文本型
- 必填参数：根目录, 相对路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.路径安全.校验路径()
- 依赖能力：无
### 校验文件名（`系统核心支持库.路径安全.校验文件名`）
- 版本：2.0.1
- 一句话说明：校验文件名安全（非空/无分隔符/非隐藏）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：文件名:文本型
- 必填参数：文件名
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.路径安全.校验文件名()
- 依赖能力：无
### 启动进程（`系统核心支持库.进程管理.启动进程`）
- 版本：2.0.1
- 一句话说明：启动外部进程，返回句柄
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型, 参数:列表型, 工作目录:文本型, 环境变量:字典型, 超时秒:整数型
- 必填参数：命令
- 可选参数：参数, 工作目录, 环境变量, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.启动进程()
- 依赖能力：无
### 终止进程（`系统核心支持库.进程管理.终止进程`）
- 版本：2.0.1
- 一句话说明：终止进程
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:文本型, 强制:逻辑型
- 必填参数：句柄
- 可选参数：强制
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.终止进程()
- 依赖能力：无
### 查询进程状态（`系统核心支持库.进程管理.查询进程状态`）
- 版本：2.0.1
- 一句话说明：查询进程运行状态
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:文本型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.查询进程状态()
- 依赖能力：无
### 等待进程结束（`系统核心支持库.进程管理.等待进程结束`）
- 版本：2.0.1
- 一句话说明：等待进程结束并获取输出
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:文本型, 超时秒:双精度数型
- 必填参数：句柄
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.等待进程结束()
- 依赖能力：无
### 执行命令（`系统核心支持库.进程管理.执行命令`）
- 版本：2.0.1
- 一句话说明：执行 shell 命令并等待
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型, 超时秒:双精度数型, 工作目录:文本型
- 必填参数：命令
- 可选参数：超时秒, 工作目录
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.执行命令()
- 依赖能力：无
### 检查命令可用（`系统核心支持库.进程管理.检查命令可用`）
- 版本：2.0.1
- 一句话说明：检查命令是否可用（which）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：命令:文本型
- 必填参数：命令
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.检查命令可用()
- 依赖能力：无
### 释放句柄（`系统核心支持库.进程管理.释放句柄`）
- 版本：2.0.1
- 一句话说明：释放进程句柄（幂等，强制杀残留）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：句柄:文本型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.进程管理.释放句柄()
- 依赖能力：无
### 配置额度（`系统核心支持库.额度控制.配置额度`）
- 版本：2.0.1
- 一句话说明：按调用方/动作配置额度（默认不限制，配置后生效）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：额度键:文本型, 额度项:文本型, 额度值:整数型
- 必填参数：额度键, 额度项, 额度值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.额度控制.配置额度()
- 依赖能力：无
### 查询额度（`系统核心支持库.额度控制.查询额度`）
- 版本：2.0.1
- 一句话说明：查询当前额度配置
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：额度键:文本型
- 必填参数：额度键
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.额度控制.查询额度()
- 依赖能力：无
### 检查额度（`系统核心支持库.额度控制.检查额度`）
- 版本：2.0.1
- 一句话说明：检查额度是否超限（0=不限制永远通过）
- 所属支持库或模块：支持库.后端.系统核心支持库
- 参数列表：额度键:文本型, 额度项:文本型, 已用值:整数型
- 必填参数：额度键, 额度项, 已用值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：系统核心支持库.额度控制.检查额度()
- 依赖能力：无
