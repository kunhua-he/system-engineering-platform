# 系统信息 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.系统信息
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.系统信息.获取操作系统信息 | 获取操作系统信息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 操作系统/版本/架构/主机名 |
| 系统核心支持库.系统信息.获取CPU信息 | 获取CPU信息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | CPU 核数/负载 |
| 系统核心支持库.系统信息.获取内存信息 | 获取内存信息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 内存总量（字节） |
| 系统核心支持库.系统信息.获取磁盘信息 | 获取磁盘信息 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 磁盘用量（字节） |
| 系统核心支持库.系统信息.获取进程列表 | 获取进程列表 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 进程列表（按CPU排序） |

## 系统核心支持库.系统信息.获取操作系统信息（获取操作系统信息）

**说明**：操作系统/版本/架构/主机名

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 无 |

**调用示例**：

```python
系统核心支持库.系统信息.获取操作系统信息()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.系统信息.获取CPU信息（获取CPU信息）

**说明**：CPU 核数/负载

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 无 |

**调用示例**：

```python
系统核心支持库.系统信息.获取CPU信息()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.系统信息.获取内存信息（获取内存信息）

**说明**：内存总量（字节）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 无 |

**调用示例**：

```python
系统核心支持库.系统信息.获取内存信息()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.系统信息.获取磁盘信息（获取磁盘信息）

**说明**：磁盘用量（字节）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径 | 文本型 | 否 | / | 磁盘路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 路径不合法 |

**调用示例**：

```python
系统核心支持库.系统信息.获取磁盘信息(路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.系统信息.获取进程列表（获取进程列表）

**说明**：进程列表（按CPU排序）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 排序 | 文本型 | 否 | cpu | 排序方式 cpu/mem |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 执行失败 |

**调用示例**：

```python
系统核心支持库.系统信息.获取进程列表(排序=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
