<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 协议白名单（支持库.后端.系统核心支持库.协议白名单）

- 类型：支持库
- 版本：1.0.0
- 说明：LLM API 端点协议白名单校验。只依赖 Python 标准库。
- 入口：__init__.py

## 能力
### 校验端点（`系统核心支持库.协议白名单.校验端点`）
- 版本：1.0.0
- 一句话说明：校验端点协议是否在白名单内
- 所属支持库或模块：支持库.后端.系统核心支持库.协议白名单
- 参数列表：api类型:文本型, base_url:文本型
- 必填参数：api类型
- 可选参数：base_url
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.协议白名单.校验端点()
- 依赖能力：无
### 查询白名单（`系统核心支持库.协议白名单.查询白名单`）
- 版本：1.0.0
- 一句话说明：返回白名单列表
- 所属支持库或模块：支持库.后端.系统核心支持库.协议白名单
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：系统核心支持库.协议白名单.查询白名单()
- 依赖能力：无
