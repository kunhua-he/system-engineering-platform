# 协议白名单 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.协议白名单
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.协议白名单.校验端点 | 校验端点 | 1.0.0 | {'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}} | 校验 api类型 是否在白名单内，openai-compatible 必须有 base_url 且协议为 http/https。返回 {通过, 原因}。 |
| 系统核心支持库.协议白名单.查询白名单 | 查询白名单 | 1.0.0 | {'类型': '结果型', '值结构': {'白名单': '列表型'}} | 返回当前白名单列表。 |

## 系统核心支持库.协议白名单.校验端点（校验端点）

**说明**：校验 api类型 是否在白名单内，openai-compatible 必须有 base_url 且协议为 http/https。返回 {通过, 原因}。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| api类型 | 文本型 | 是 | None | 协议类型（openai/anthropic/openai-compatible） |
| base_url | 文本型 | 否 | None | 端点地址（openai-compatible 必填） |

**返回结构**：{'类型': '结果型', '值结构': {'通过': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.协议白名单.校验端点(api类型=值, base_url=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.协议白名单.查询白名单（查询白名单）

**说明**：返回当前白名单列表。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {'白名单': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
系统核心支持库.协议白名单.查询白名单()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
