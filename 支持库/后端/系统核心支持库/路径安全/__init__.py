"""支持库.路径安全 包级中文入口。"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 校验路径
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 校验相对路径文本
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 校验文件名
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 显示相对路径
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 判据码_绝对写法
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 判据码_家目录写法
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 判据码_段跳转
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 判据码_当前目录
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 判据码_点段
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 判据码集合
from 支持库.后端.系统核心支持库.路径安全.实现.路径安全 import 像路径写法文本

__all__ = [
    "校验路径",
    "校验相对路径文本",
    "校验文件名",
    "显示相对路径",
    "判据码_绝对写法",
    "判据码_家目录写法",
    "判据码_段跳转",
    "判据码_当前目录",
    "判据码_点段",
    "判据码集合",
    "像路径写法文本",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。

    ★ 2026-09-23 撤注册（越界修复）：`校验相对路径文本` **不再作为能力注册**。
    它是 `校验路径` 的**纯判定节点**（`校验路径` = 本节点 + 根内绝对化 + realpath 逃逸判），
    按哲学 7.1/7.5 的定义「内部能力＝公开能力的某一个节点」属**内部节点**，不是公开能力。
    T3 收口（提交 `7dd1f57b`）当时只加了注册、没同步 `能力定义.json` ⇒
    `装配后检查` 报「已注册实现但未在声明中列出」⇒ **整包被跳过** ⇒
    `路径安全` 全部 4 条能力（含原本可用的 `校验路径` / `校验文件名` / `显示相对路径`）
    一起从注册表消失（源码侧装配面实测 749，撤注册后回 752，与活网关 `/健康` 一致）。
    三处调用方（`平台控制面/包仓库/路径安全.py`、`平台控制面/能力目录/租约路径.py`、
    `开发工具/HTML验证/路径安全.py`）**全部是进程内直接 import**，不经网关，
    故撤注册零影响；函数仍在本模块导出（见 `__all__`）。
    """
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("系统核心支持库.路径安全.校验路径", 校验路径, [{"名称": "根目录", "类型": "文本型", "必填": 真, "默认值": None, "说明": "受控根目录"}, {"名称": "相对路径", "类型": "文本型", "必填": 真, "默认值": None, "说明": "要校验的相对路径"}]),
        ("系统核心支持库.路径安全.校验文件名", 校验文件名, [{"名称": "文件名", "类型": "文本型", "必填": 真, "默认值": None, "说明": "文件名"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.系统核心支持库.路径安全",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="路径安全支持库",
            )
        )
    注册表.注册(能力实现(能力id="系统核心支持库.路径安全.显示相对路径", 包id="支持库.后端.系统核心支持库.路径安全", 实现函数=显示相对路径, 参数=[{"名称": "目标路径", "类型": "文本型", "必填": 真, "说明": "要展示的绝对路径"}, {"名称": "基准目录", "类型": "文本型", "必填": 假, "说明": "首选基准目录"}, {"名称": "备选基准", "类型": "文本型", "必填": 假, "说明": "次选基准目录"}], 返回="结果", 说明="把绝对路径显示为相对基准目录的字符串；不在则原样返回"))
