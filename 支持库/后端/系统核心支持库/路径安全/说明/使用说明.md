# 路径安全 使用说明

- 包id：支持库.后端.系统核心支持库.路径安全
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.路径安全.校验路径 | 校验路径 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 校验相对路径是否安全落在根目录内（防穿越/符号链接逃逸） |
| 系统核心支持库.路径安全.校验文件名 | 校验文件名 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 校验文件名安全（非空/无分隔符/非隐藏） |

## 系统核心支持库.路径安全.校验路径（校验路径）

**说明**：校验相对路径是否安全落在根目录内（防穿越/符号链接逃逸）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 根目录 | 文本型 | 是 | None | 受控根目录 |
| 相对路径 | 文本型 | 是 | None | 要校验的相对路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 路径解析失败 |

**调用示例**：

```python
系统核心支持库.路径安全.校验路径(根目录=值, 相对路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.路径安全.校验文件名（校验文件名）

**说明**：校验文件名安全（非空/无分隔符/非隐藏）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件名 | 文本型 | 是 | None | 文件名 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
系统核心支持库.路径安全.校验文件名(文件名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
