# 资源管理 · 目录树与命令

> 包id：`支持库.后端.系统核心支持库.资源管理`；版本：`1.1.2`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。

## 目录树

```text
资源管理/
├── __init__.py
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/资源管理.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 说明/资源管理说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
├── 默认配置/默认配置.json
```

## 公开命令

### `资源管理.创建内容摘要`

- 名称：创建内容摘要
- 返回：`结果型`
- 说明：创建文件内容摘要（sha256，分块读取）
- 参数：
  - `文件路径`；文本型；必填；文件路径
  - `算法`；文本型；可选；默认=sha256；摘要算法
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.创建不可变快照`

- 名称：创建不可变快照
- 返回：`结果型`
- 说明：创建不可变快照并生成快照摘要
- 参数：
  - `来源目录`；文本型；必填；来源目录
  - `快照目录`；文本型；必填；快照目标目录
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.创建唯一运行目录`

- 名称：创建唯一运行目录
- 返回：`结果型`
- 说明：创建唯一运行目录（临时目录语义）
- 参数：
  - `基础目录`；文本型；必填；基础目录
  - `前缀`；文本型；可选；默认=运行；目录名前缀
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.原子写入`

- 名称：原子写入
- 返回：`结果型`
- 说明：原子写入文件（临时文件+fsync+rename）
- 参数：
  - `目标路径`；文本型；必填；目标文件路径
  - `内容`；文本型；必填；要写入的内容
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.原子替换`

- 名称：原子替换
- 返回：`结果型`
- 说明：原子替换文件内容（可选版本比较）
- 参数：
  - `目标路径`；文本型；必填；目标文件路径
  - `新内容`；文本型；必填；新的文件内容
  - `期望版本`；文本型；可选；默认=；期望版本号
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.比较并交换`

- 名称：比较并交换
- 返回：`结果型`
- 说明：CAS 提交（并发冲突返回版本冲突）
- 参数：
  - `目标路径`；文本型；必填；目标文件路径
  - `期望值`；文本型；必填；期望的当前值
  - `新值`；文本型；必填；要写入的新值
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.资源短锁`

- 名称：资源短锁
- 返回：`结果型`
- 说明：资源级跨进程短锁（mkdir 原子性）
- 参数：
  - `锁目录`；文本型；必填；锁目录
  - `资源id`；文本型；必填；资源id
  - `持有者`；文本型；可选；默认=；持有者标识
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `资源管理.安全释放`

- 名称：安全释放资源
- 返回：`结果型`
- 说明：安全释放资源（幂等）
- 参数：
  - `路径`；文本型；必填；要释放的路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
