# 资源管理支持库
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


原子资源操作支持库，提供：内容摘要、不可变快照、唯一运行目录、原子写入、原子替换、比较并交换、资源级跨进程短锁、安全释放。

使用方式（经能力注册表调用）：

```python
from 公共契约.能力契约.契约 import 能力注册表
# 装配后：
注册表.获取("资源管理.创建内容摘要").调用(文件路径="...")
注册表.获取("资源管理.比较并交换").调用(目标路径="...", 期望值=1, 新值=2)
```

并发语义：比较并交换 在值不匹配时返回 版本冲突，禁止覆盖他人提交；原子写入/替换 使用 临时文件+fsync+rename 保证原子性。
