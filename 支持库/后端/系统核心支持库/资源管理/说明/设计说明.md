# 资源管理 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.后端.系统核心支持库.资源管理
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.资源管理.创建内容摘要 | 创建内容摘要 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '内容摘要文本（hex 字符串）'}} | 创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本（平台既有契约：文件管理/自修复工具直接消费摘要文本） |
| 系统核心支持库.资源管理.创建不可变快照 | 创建不可变快照 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '快照摘要文件路径'}} | 创建不可变快照（复制 + 快照摘要 + 只读标记）；成功返回快照摘要路径（平台既有契约） |
| 系统核心支持库.资源管理.创建唯一运行目录 | 创建唯一运行目录 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '唯一运行目录路径'}} | 创建唯一运行目录（临时目录语义，调用方负责释放）；返回目录路径（平台既有契约：运行核心/自修复工具直接消费路径） |
| 系统核心支持库.资源管理.原子写入 | 原子写入 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '成功标志（真值）'}} | 原子写入文件（临时文件 + fsync + rename）；成功返回真值（平台既有契约：运行核心直接消费） |
| 系统核心支持库.资源管理.原子替换 | 原子替换 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}} | 原子替换文件内容（可选版本比较），返回 (成功, 消息)（平台既有契约） |
| 系统核心支持库.资源管理.比较并交换 | 比较并交换 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}} | 比较并交换（CAS）：读取 → 比较 → 原子写回；并发冲突返回 版本冲突，返回 (成功, 消息)（平台既有契约） |
| 系统核心支持库.资源管理.资源短锁 | 资源短锁 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '资源短锁实例（获取/释放/持有者是谁）'}} | 资源级跨进程短锁（mkdir 原子性 + 持有者信息）；构造返回锁实例，经 获取/释放 方法使用（平台既有契约：运行核心直接消费） |
| 系统核心支持库.资源管理.安全释放 | 安全释放 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}} | 安全释放资源（文件删除/目录递归删除，不存在视为幂等成功），返回 (成功, 消息)（平台既有契约：文档解析模块直接消费） |
| 系统核心支持库.资源管理.创建受管状态 | 创建受管状态 | 1.0.0 | 结果型 | 创建受管状态并返回资源句柄 |
| 系统核心支持库.资源管理.读取受管状态 | 读取受管状态 | 1.0.0 | 结果型 | 按句柄读取受管状态 |
| 系统核心支持库.资源管理.更新受管状态 | 更新受管状态 | 1.0.0 | 结果型 | 按句柄更新受管状态并校验版本 |
| 系统核心支持库.资源管理.释放受管状态 | 释放受管状态 | 1.0.0 | 结果型 | 释放受管状态句柄 |
| 系统核心支持库.资源管理.创建审计账本 | 创建审计账本 | 1.0.0 | 结果型 | 创建审计账本（OpenClaw 模式化：默认开/上限10万/保留30天，0加密） |
| 系统核心支持库.资源管理.记审计事件 | 记审计事件 | 1.0.0 | 结果型 | 记录审计事件（底座0限制，完整记录传入字段；脱敏由业务端自理） |
| 系统核心支持库.资源管理.查审计事件 | 查审计事件 | 1.0.0 | 结果型 | 按条件查询审计事件（原文匹配） |
| 系统核心支持库.资源管理.清空审计账本 | 清空审计账本 | 1.0.0 | 结果型 | 清空审计账本（谨慎操作） |
| 系统核心支持库.资源管理.登记配置快照 | 登记配置快照 | 1.0.0 | 结果型 | 登记配置当前快照指纹（改前基线，OpenClaw config-journal 模式化） |
| 系统核心支持库.资源管理.对比配置指纹 | 对比配置指纹 | 1.0.0 | 结果型 | 对比新配置与基线；变更记 journal（改前/新指纹/操作人） |
| 系统核心支持库.资源管理.查配置指纹 | 查配置指纹 | 1.0.0 | 结果型 | 查询配置指纹与变更记录（回答'什么时候被谁改的'） |
| 系统核心支持库.资源管理.登记回滚步骤 | 登记回滚步骤 | 1.0.0 | 结果型 | 登记可回滚步骤（DeepSeek 逆序回滚模式化：建链任一步失败按逆序释放） |
| 系统核心支持库.资源管理.执行逆序回滚 | 执行逆序回滚 | 1.0.0 | 结果型 | 按登记逆序执行全部回滚（0限制：底座只编排，动作由调用者回滚器执行） |
| 系统核心支持库.资源管理.查询回滚事务 | 查询回滚事务 | 1.0.0 | 结果型 | 查询事务回滚状态 |

## 系统核心支持库.资源管理.创建内容摘要（创建内容摘要）

**说明**：创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本（平台既有契约：文件管理/自修复工具直接消费摘要文本）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文件路径（文件不存在抛出 FileNotFoundError，由调用方捕获转换为统一失败） |
| 算法 | 文本型 | 否 | sha256 | 摘要算法（默认 sha256） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '内容摘要文本（hex 字符串）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 摘要失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建内容摘要(文件路径=值, 算法=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.创建不可变快照（创建不可变快照）

**说明**：创建不可变快照（复制 + 快照摘要 + 只读标记）；成功返回快照摘要路径（平台既有契约）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 来源目录 | 文本型 | 是 | None | 来源目录（不存在抛出 FileNotFoundError，由调用方捕获） |
| 快照目录 | 文本型 | 是 | None | 快照目标目录（自动创建） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '快照摘要文件路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 快照失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建不可变快照(来源目录=值, 快照目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.创建唯一运行目录（创建唯一运行目录）

**说明**：创建唯一运行目录（临时目录语义，调用方负责释放）；返回目录路径（平台既有契约：运行核心/自修复工具直接消费路径）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 基础目录 | 文本型 | 是 | None | 基础目录（自动创建） |
| 前缀 | 文本型 | 否 | 运行 | 目录名前缀（默认 运行） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '唯一运行目录路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录创建失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建唯一运行目录(基础目录=值, 前缀=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.原子写入（原子写入）

**说明**：原子写入文件（临时文件 + fsync + rename）；成功返回真值（平台既有契约：运行核心直接消费）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标路径 | 文本型 | 是 | None | 目标文件路径（自动创建父目录） |
| 内容 | 文本型 | 是 | None | 要写入的内容（文本或字节） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（真值）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 写入失败 |

**调用示例**：

```python
系统核心支持库.资源管理.原子写入(目标路径=值, 内容=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.原子替换（原子替换）

**说明**：原子替换文件内容（可选版本比较），返回 (成功, 消息)（平台既有契约）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标路径 | 文本型 | 是 | None | 目标文件路径 |
| 新内容 | 文本型 | 是 | None | 新的文件内容（文本或字节） |
| 期望版本 | 文本型 | 否 |  | 期望版本号（空串跳过版本比较；不匹配返回 版本冲突） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 版本冲突 |
| 写入失败 |

**调用示例**：

```python
系统核心支持库.资源管理.原子替换(目标路径=值, 新内容=值, 期望版本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.比较并交换（比较并交换）

**说明**：比较并交换（CAS）：读取 → 比较 → 原子写回；并发冲突返回 版本冲突，返回 (成功, 消息)（平台既有契约）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标路径 | 文本型 | 是 | None | 目标文件路径（不存在返回 资源不存在） |
| 期望值 | 文本型 | 是 | None | 期望的当前值（不匹配返回 版本冲突） |
| 新值 | 文本型 | 是 | None | 要写入的新值 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不存在 |
| 版本冲突 |
| 写入失败 |

**调用示例**：

```python
系统核心支持库.资源管理.比较并交换(目标路径=值, 期望值=值, 新值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.资源短锁（资源短锁）

**说明**：资源级跨进程短锁（mkdir 原子性 + 持有者信息）；构造返回锁实例，经 获取/释放 方法使用（平台既有契约：运行核心直接消费）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 锁目录 | 文本型 | 是 | None | 锁目录（自动创建） |
| 资源id | 文本型 | 是 | None | 资源id（锁名一部分） |
| 持有者 | 文本型 | 否 |  | 持有者标识（写入锁内持有者信息） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '资源短锁实例（获取/释放/持有者是谁）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源被占用 |
| 超时 |

**调用示例**：

```python
系统核心支持库.资源管理.资源短锁(锁目录=值, 资源id=值, 持有者=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.安全释放（安全释放）

**说明**：安全释放资源（文件删除/目录递归删除，不存在视为幂等成功），返回 (成功, 消息)（平台既有契约：文档解析模块直接消费）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径 | 文本型 | 是 | None | 要释放的文件或目录路径 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 释放失败 |

**调用示例**：

```python
系统核心支持库.资源管理.安全释放(路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.创建审计账本（创建审计账本）

**说明**：创建审计账本（OpenClaw 模式化：默认开/上限10万/保留30天，0加密）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 账本名 | 文本型 | 是 | None | 账本名 |
| 上限 | 整数型 | 否 | 100000 | 事件上限 |
| 保留天 | 整数型 | 否 | 30 | 保留天数 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建审计账本(账本名=值, 上限=值, 保留天=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.记审计事件（记审计事件）

**说明**：记录审计事件（底座0限制，完整记录传入字段；脱敏由业务端自理）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 账本名 | 文本型 | 是 | None | 账本名 |
| 事件类型 | 文本型 | 是 | None | 事件类型 |
| 主体 | 文本型 | 否 | None | 主体（原文记录） |
| 对象 | 文本型 | 否 | None | 对象 |
| 结果码 | 文本型 | 否 | OK | 结果码 |
| 耗时毫秒 | 整数型 | 否 | None | 耗时 |
| 通道 | 文本型 | 否 | None | 通道 |
| 方向 | 文本型 | 否 | None | 方向 |
| 元数据 | 字典型 | 否 | None | 完整元数据 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 记录失败 |

**调用示例**：

```python
系统核心支持库.资源管理.记审计事件(账本名=值, 事件类型=值, 主体=值, 对象=值, 结果码=值, 耗时毫秒=值, 通道=值, 方向=值, 元数据=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.查审计事件（查审计事件）

**说明**：按条件查询审计事件（原文匹配）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 账本名 | 文本型 | 是 | None | 账本名 |
| 事件类型 | 文本型 | 否 | None | 事件类型 |
| 主体 | 文本型 | 否 | None | 主体 |
| 对象 | 文本型 | 否 | None | 对象 |
| 条数 | 整数型 | 否 | 50 | 条数 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 账本不存在 |
| 查询失败 |

**调用示例**：

```python
系统核心支持库.资源管理.查审计事件(账本名=值, 事件类型=值, 主体=值, 对象=值, 条数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.清空审计账本（清空审计账本）

**说明**：清空审计账本（谨慎操作）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 账本名 | 文本型 | 是 | None | 账本名 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 账本不存在 |
| 清空失败 |

**调用示例**：

```python
系统核心支持库.资源管理.清空审计账本(账本名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.登记配置快照（登记配置快照）

**说明**：登记配置当前快照指纹（改前基线，OpenClaw config-journal 模式化）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 配置名 | 文本型 | 是 | None | 配置名 |
| 配置 | 字典型 | 是 | None | 配置内容 |
| 操作人 | 文本型 | 否 | None | 操作人 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 登记失败 |

**调用示例**：

```python
系统核心支持库.资源管理.登记配置快照(配置名=值, 配置=值, 操作人=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.对比配置指纹（对比配置指纹）

**说明**：对比新配置与基线；变更记 journal（改前/新指纹/操作人）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 配置名 | 文本型 | 是 | None | 配置名 |
| 新配置 | 字典型 | 是 | None | 新配置 |
| 操作人 | 文本型 | 否 | None | 操作人 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 对比失败 |

**调用示例**：

```python
系统核心支持库.资源管理.对比配置指纹(配置名=值, 新配置=值, 操作人=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.查配置指纹（查配置指纹）

**说明**：查询配置指纹与变更记录（回答'什么时候被谁改的'）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 配置名 | 文本型 | 是 | None | 配置名 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 配置不存在 |
| 查询失败 |

**调用示例**：

```python
系统核心支持库.资源管理.查配置指纹(配置名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.登记回滚步骤（登记回滚步骤）

**说明**：登记可回滚步骤（DeepSeek 逆序回滚模式化：建链任一步失败按逆序释放）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 事务id | 文本型 | 是 | None | 事务id |
| 步骤名 | 文本型 | 是 | None | 步骤名 |
| 回滚动作 | 字典型 | 是 | None | 回滚动作 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 登记失败 |

**调用示例**：

```python
系统核心支持库.资源管理.登记回滚步骤(事务id=值, 步骤名=值, 回滚动作=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.执行逆序回滚（执行逆序回滚）

**说明**：按登记逆序执行全部回滚（0限制：底座只编排，动作由调用者回滚器执行）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 事务id | 文本型 | 是 | None | 事务id |
| 回滚器 | 字典型 | 否 | None | 回滚器 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 事务不存在 |
| 回滚失败 |

**调用示例**：

```python
系统核心支持库.资源管理.执行逆序回滚(事务id=值, 回滚器=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）

## 系统核心支持库.资源管理.查询回滚事务（查询回滚事务）

**说明**：查询事务回滚状态

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 事务id | 文本型 | 是 | None | 事务id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 事务不存在 |
| 查询失败 |

**调用示例**：

```python
系统核心支持库.资源管理.查询回滚事务(事务id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：资源管理.审计账本正向 / 资源管理.配置指纹正向 / 资源管理.逆序回滚正向（验证场景.json）
