# 资源管理 使用说明

- 包id：支持库.后端.系统核心支持库.资源管理
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.资源管理.创建内容摘要 | 创建内容摘要 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '内容摘要文本（hex 字符串）'}} | 创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本（平台既有契约：文件管理/自修复工具直接消费摘要文本） |
| 系统核心支持库.资源管理.创建不可变快照 | 创建不可变快照 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '快照摘要文件路径'}} | 创建不可变快照（复制 + 快照摘要 + 只读标记）；成功返回快照摘要路径（平台既有契约） |
| 系统核心支持库.资源管理.创建唯一运行目录 | 创建唯一运行目录 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '唯一运行目录路径'}} | 创建唯一运行目录（临时目录语义，调用方负责释放）；返回目录路径（平台既有契约：运行核心/自修复工具直接消费路径） |
| 系统核心支持库.资源管理.原子写入 | 原子写入 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '成功标志（真值）'}} | 原子写入文件（临时文件 + fsync + rename）；成功返回真值（平台既有契约：运行核心直接消费） |
| 系统核心支持库.资源管理.原子替换 | 原子替换 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}} | 原子替换文件内容（可选版本比较），返回 (成功, 消息)（平台既有契约） |
| 系统核心支持库.资源管理.比较并交换 | 比较并交换 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}} | 比较并交换（CAS）：读取 → 比较 → 原子写回；并发冲突返回 版本冲突，返回 (成功, 消息)（平台既有契约） |
| 系统核心支持库.资源管理.资源短锁 | 资源短锁 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '资源短锁实例（获取/释放/持有者是谁）'}} | 资源级跨进程短锁（mkdir 原子性 + 持有者信息）；构造返回锁实例，经 获取/释放 方法使用（平台既有契约：运行核心直接消费） |
| 系统核心支持库.资源管理.安全释放 | 安全释放 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}} | 安全释放资源（文件删除/目录递归删除，不存在视为幂等成功），返回 (成功, 消息)（平台既有契约：文档解析模块直接消费） |
| 系统核心支持库.资源管理.创建受管状态 | 创建受管状态 | 1.0.0 | 结果型 | 创建受管状态并返回资源句柄 |
| 系统核心支持库.资源管理.读取受管状态 | 读取受管状态 | 1.0.0 | 结果型 | 按句柄读取受管状态 |
| 系统核心支持库.资源管理.更新受管状态 | 更新受管状态 | 1.0.0 | 结果型 | 按句柄更新受管状态并校验版本 |
| 系统核心支持库.资源管理.释放受管状态 | 释放受管状态 | 1.0.0 | 结果型 | 释放受管状态句柄 |

## 系统核心支持库.资源管理.创建内容摘要（创建内容摘要）

**说明**：创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本（平台既有契约：文件管理/自修复工具直接消费摘要文本）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文件路径（文件不存在抛出 FileNotFoundError，由调用方捕获转换为统一失败） |
| 算法 | 文本型 | 否 | sha256 | 摘要算法（默认 sha256） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '内容摘要文本（hex 字符串）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 摘要失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建内容摘要(文件路径=值, 算法=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.创建不可变快照（创建不可变快照）

**说明**：创建不可变快照（复制 + 快照摘要 + 只读标记）；成功返回快照摘要路径（平台既有契约）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 来源目录 | 文本型 | 是 | None | 来源目录（不存在抛出 FileNotFoundError，由调用方捕获） |
| 快照目录 | 文本型 | 是 | None | 快照目标目录（自动创建） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '快照摘要文件路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 快照失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建不可变快照(来源目录=值, 快照目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.创建唯一运行目录（创建唯一运行目录）

**说明**：创建唯一运行目录（临时目录语义，调用方负责释放）；返回目录路径（平台既有契约：运行核心/自修复工具直接消费路径）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 基础目录 | 文本型 | 是 | None | 基础目录（自动创建） |
| 前缀 | 文本型 | 否 | 运行 | 目录名前缀（默认 运行） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '唯一运行目录路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录创建失败 |

**调用示例**：

```python
系统核心支持库.资源管理.创建唯一运行目录(基础目录=值, 前缀=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.原子写入（原子写入）

**说明**：原子写入文件（临时文件 + fsync + rename）；成功返回真值（平台既有契约：运行核心直接消费）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标路径 | 文本型 | 是 | None | 目标文件路径（自动创建父目录） |
| 内容 | 文本型 | 是 | None | 要写入的内容（文本或字节） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（真值）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 写入失败 |

**调用示例**：

```python
系统核心支持库.资源管理.原子写入(目标路径=值, 内容=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.原子替换（原子替换）

**说明**：原子替换文件内容（可选版本比较），返回 (成功, 消息)（平台既有契约）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标路径 | 文本型 | 是 | None | 目标文件路径 |
| 新内容 | 文本型 | 是 | None | 新的文件内容（文本或字节） |
| 期望版本 | 文本型 | 否 |  | 期望版本号（空串跳过版本比较；不匹配返回 版本冲突） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 版本冲突 |
| 写入失败 |

**调用示例**：

```python
系统核心支持库.资源管理.原子替换(目标路径=值, 新内容=值, 期望版本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.比较并交换（比较并交换）

**说明**：比较并交换（CAS）：读取 → 比较 → 原子写回；并发冲突返回 版本冲突，返回 (成功, 消息)（平台既有契约）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标路径 | 文本型 | 是 | None | 目标文件路径（不存在返回 资源不存在） |
| 期望值 | 文本型 | 是 | None | 期望的当前值（不匹配返回 版本冲突） |
| 新值 | 文本型 | 是 | None | 要写入的新值 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不存在 |
| 版本冲突 |
| 写入失败 |

**调用示例**：

```python
系统核心支持库.资源管理.比较并交换(目标路径=值, 期望值=值, 新值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.资源短锁（资源短锁）

**说明**：资源级跨进程短锁（mkdir 原子性 + 持有者信息）；构造返回锁实例，经 获取/释放 方法使用（平台既有契约：运行核心直接消费）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 锁目录 | 文本型 | 是 | None | 锁目录（自动创建） |
| 资源id | 文本型 | 是 | None | 资源id（锁名一部分） |
| 持有者 | 文本型 | 否 |  | 持有者标识（写入锁内持有者信息） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '资源短锁实例（获取/释放/持有者是谁）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源被占用 |
| 超时 |

**调用示例**：

```python
系统核心支持库.资源管理.资源短锁(锁目录=值, 资源id=值, 持有者=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.资源管理.安全释放（安全释放）

**说明**：安全释放资源（文件删除/目录递归删除，不存在视为幂等成功），返回 (成功, 消息)（平台既有契约：文档解析模块直接消费）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径 | 文本型 | 是 | None | 要释放的文件或目录路径 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 释放失败 |

**调用示例**：

```python
系统核心支持库.资源管理.安全释放(路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
