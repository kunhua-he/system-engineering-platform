<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 资源管理（支持库.后端.系统核心支持库.资源管理）

- 类型：支持库
- 版本：1.1.0
- 说明：原子资源操作：内容摘要、不可变快照、唯一运行目录、原子写入、原子替换、比较并交换、资源级跨进程短锁、安全释放。只依赖 Python 标准库。
- 入口：__init__.py

## 能力
### 创建内容摘要（`系统核心支持库.资源管理.创建内容摘要`）
- 版本：1.0.0
- 一句话说明：创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本（平台既有契约：文件管理/自修复工具直接消费摘要文本）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：文件路径:文本型, 算法:文本型
- 必填参数：文件路径
- 可选参数：算法
- 返回结构：{'类型': '结果型', '值结构': {'值': '内容摘要文本（hex 字符串）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 摘要失败
- 调用示例：系统核心支持库.资源管理.创建内容摘要(算法=sha256)
- 依赖能力：无
### 创建不可变快照（`系统核心支持库.资源管理.创建不可变快照`）
- 版本：1.0.0
- 一句话说明：创建不可变快照（复制 + 快照摘要 + 只读标记）；成功返回快照摘要路径（平台既有契约）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：来源目录:文本型, 快照目录:文本型
- 必填参数：来源目录, 快照目录
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '快照摘要文件路径'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 快照失败
- 调用示例：系统核心支持库.资源管理.创建不可变快照()
- 依赖能力：无
### 创建唯一运行目录（`系统核心支持库.资源管理.创建唯一运行目录`）
- 版本：1.0.0
- 一句话说明：创建唯一运行目录（临时目录语义，调用方负责释放）；返回目录路径（平台既有契约：运行核心/自修复工具直接消费路径）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：基础目录:文本型, 前缀:文本型
- 必填参数：基础目录
- 可选参数：前缀
- 返回结构：{'类型': '结果型', '值结构': {'值': '唯一运行目录路径'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录创建失败
- 调用示例：系统核心支持库.资源管理.创建唯一运行目录(前缀=运行)
- 依赖能力：无
### 原子写入（`系统核心支持库.资源管理.原子写入`）
- 版本：1.1.0
- 一句话说明：原子写入：临时文件 + fsync + rename；默认保留目标文件原权限位，写入后同步父目录
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：目标路径:文本型, 内容:文本型, 内容字节:字节集型, 保留权限:逻辑型
- 必填参数：目标路径
- 可选参数：内容, 内容字节, 保留权限
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（真值）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 写入失败
- 调用示例：系统核心支持库.资源管理.原子写入()
- 依赖能力：无
### 原子替换（`系统核心支持库.资源管理.原子替换`）
- 版本：1.0.0
- 一句话说明：原子替换文件内容（可选版本比较），返回 (成功, 消息)（平台既有契约）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：目标路径:文本型, 新内容:文本型, 期望版本:文本型
- 必填参数：目标路径, 新内容
- 可选参数：期望版本
- 返回结构：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 版本冲突, 写入失败
- 调用示例：系统核心支持库.资源管理.原子替换(期望版本=)
- 依赖能力：无
### 比较并交换（`系统核心支持库.资源管理.比较并交换`）
- 版本：1.0.0
- 一句话说明：比较并交换（CAS）：读取 → 比较 → 原子写回；并发冲突返回 版本冲突，返回 (成功, 消息)（平台既有契约）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：目标路径:文本型, 期望值:文本型, 新值:文本型
- 必填参数：目标路径, 期望值, 新值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 资源不存在, 版本冲突, 写入失败
- 调用示例：系统核心支持库.资源管理.比较并交换()
- 依赖能力：无
### 资源短锁（`系统核心支持库.资源管理.资源短锁`）
- 版本：1.0.0
- 一句话说明：原子获取并释放资源级跨进程短锁，返回可传输执行结果
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：锁目录:文本型, 资源id:文本型, 持有者:文本型
- 必填参数：锁目录, 资源id
- 可选参数：持有者
- 返回结构：{'类型': '结果型', '值结构': {'已获取': '逻辑型', '已释放': '逻辑型', '持有者': '文本型', '锁路径': '文本型', '说明': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 资源被占用, 超时
- 调用示例：系统核心支持库.资源管理.资源短锁(持有者=)
- 依赖能力：无
### 安全释放（`系统核心支持库.资源管理.安全释放`）
- 版本：1.0.0
- 一句话说明：安全释放资源（文件删除/目录递归删除，不存在视为幂等成功），返回 (成功, 消息)（平台既有契约：文档解析模块直接消费）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '(成功标志, 消息) 二元组'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 释放失败
- 调用示例：系统核心支持库.资源管理.安全释放()
- 依赖能力：无
### 创建受管状态（`系统核心支持库.资源管理.创建受管状态`）
- 版本：1.0.0
- 一句话说明：创建受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：资源id:文本型, 初始状态:字典型
- 必填参数：资源id, 初始状态
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 句柄已过期, 版本冲突, 资源操作失败
- 调用示例：系统核心支持库.资源管理.创建受管状态()
- 依赖能力：无
### 读取受管状态（`系统核心支持库.资源管理.读取受管状态`）
- 版本：1.0.0
- 一句话说明：读取受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 句柄已过期, 版本冲突, 资源操作失败
- 调用示例：系统核心支持库.资源管理.读取受管状态()
- 依赖能力：无
### 更新受管状态（`系统核心支持库.资源管理.更新受管状态`）
- 版本：1.0.0
- 一句话说明：更新受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：句柄:整数型, 新状态:字典型, 期望版本:文本型
- 必填参数：句柄, 新状态
- 可选参数：期望版本
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 句柄已过期, 版本冲突, 资源操作失败
- 调用示例：系统核心支持库.资源管理.更新受管状态(期望版本=)
- 依赖能力：无
### 释放受管状态（`系统核心支持库.资源管理.释放受管状态`）
- 版本：1.0.0
- 一句话说明：释放受管状态（统一调用能力接口）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '受管状态信封'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 句柄已过期, 版本冲突, 资源操作失败
- 调用示例：系统核心支持库.资源管理.释放受管状态()
- 依赖能力：无
### 创建审计账本（`系统核心支持库.资源管理.创建审计账本`）
- 版本：1.0.0
- 一句话说明：创建审计账本（OpenClaw 模式化：默认开/上限10万/保留30天，0加密）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：账本名:文本型, 上限:整数型, 保留天:整数型
- 必填参数：账本名
- 可选参数：上限, 保留天
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建失败
- 调用示例：系统核心支持库.资源管理.创建审计账本(上限=100000, 保留天=30)
- 依赖能力：无
### 记审计事件（`系统核心支持库.资源管理.记审计事件`）
- 版本：1.0.0
- 一句话说明：记录审计事件（底座0限制，完整记录传入字段；脱敏由业务端自理）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：账本名:文本型, 事件类型:文本型, 主体:文本型, 对象:文本型, 结果码:文本型, 耗时毫秒:整数型, 通道:文本型, 方向:文本型, 元数据:字典型
- 必填参数：账本名, 事件类型
- 可选参数：主体, 对象, 结果码, 耗时毫秒, 通道, 方向, 元数据
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 记录失败
- 调用示例：系统核心支持库.资源管理.记审计事件(结果码=OK)
- 依赖能力：无
### 查审计事件（`系统核心支持库.资源管理.查审计事件`）
- 版本：1.0.0
- 一句话说明：按条件查询审计事件（原文匹配）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：账本名:文本型, 事件类型:文本型, 主体:文本型, 对象:文本型, 条数:整数型
- 必填参数：账本名
- 可选参数：事件类型, 主体, 对象, 条数
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 账本不存在, 查询失败
- 调用示例：系统核心支持库.资源管理.查审计事件(条数=50)
- 依赖能力：无
### 清空审计账本（`系统核心支持库.资源管理.清空审计账本`）
- 版本：1.0.0
- 一句话说明：清空审计账本（谨慎操作）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：账本名:文本型
- 必填参数：账本名
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 账本不存在, 清空失败
- 调用示例：系统核心支持库.资源管理.清空审计账本()
- 依赖能力：无
### 登记配置快照（`系统核心支持库.资源管理.登记配置快照`）
- 版本：1.0.0
- 一句话说明：登记配置当前快照指纹（改前基线，OpenClaw config-journal 模式化）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：配置名:文本型, 配置:字典型, 操作人:文本型
- 必填参数：配置名, 配置
- 可选参数：操作人
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 登记失败
- 调用示例：系统核心支持库.资源管理.登记配置快照()
- 依赖能力：无
### 对比配置指纹（`系统核心支持库.资源管理.对比配置指纹`）
- 版本：1.0.0
- 一句话说明：对比新配置与基线；变更记 journal（改前/新指纹/操作人）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：配置名:文本型, 新配置:字典型, 操作人:文本型
- 必填参数：配置名, 新配置
- 可选参数：操作人
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 对比失败
- 调用示例：系统核心支持库.资源管理.对比配置指纹()
- 依赖能力：无
### 查配置指纹（`系统核心支持库.资源管理.查配置指纹`）
- 版本：1.0.0
- 一句话说明：查询配置指纹与变更记录（回答'什么时候被谁改的'）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：配置名:文本型
- 必填参数：配置名
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 配置不存在, 查询失败
- 调用示例：系统核心支持库.资源管理.查配置指纹()
- 依赖能力：无
### 登记回滚步骤（`系统核心支持库.资源管理.登记回滚步骤`）
- 版本：1.0.0
- 一句话说明：登记可回滚步骤（DeepSeek 逆序回滚模式化：建链任一步失败按逆序释放）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：事务id:文本型, 步骤名:文本型, 回滚动作:字典型
- 必填参数：事务id, 步骤名, 回滚动作
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 登记失败
- 调用示例：系统核心支持库.资源管理.登记回滚步骤()
- 依赖能力：无
### 执行逆序回滚（`系统核心支持库.资源管理.执行逆序回滚`）
- 版本：1.0.0
- 一句话说明：按登记逆序执行全部回滚（0限制：底座只编排，动作由调用者回滚器执行）
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：事务id:文本型, 回滚器:字典型
- 必填参数：事务id
- 可选参数：回滚器
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 事务不存在, 回滚失败
- 调用示例：系统核心支持库.资源管理.执行逆序回滚()
- 依赖能力：无
### 查询回滚事务（`系统核心支持库.资源管理.查询回滚事务`）
- 版本：1.0.0
- 一句话说明：查询事务回滚状态
- 所属支持库或模块：支持库.后端.系统核心支持库.资源管理
- 参数列表：事务id:文本型
- 必填参数：事务id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 事务不存在, 查询失败
- 调用示例：系统核心支持库.资源管理.查询回滚事务()
- 依赖能力：无
