"""支持库.权限审批 包级中文入口。"""

from __future__ import annotations

from 支持库.后端.系统核心支持库.权限审批.实现.权限审批 import 校验动作
from 支持库.后端.系统核心支持库.权限审批.实现.权限审批 import 登记规则
from 支持库.后端.系统核心支持库.权限审批.实现.权限审批 import 查询规则表

__all__ = [
    "校验动作",
    "登记规则",
    "查询规则表",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("系统核心支持库.权限审批.校验动作", 校验动作, [{"名称": "动作类型", "类型": "文本型", "必填": True, "默认值": None, "说明": "动作类型"}, {"名称": "目标", "类型": "文本型", "必填": False, "默认值": None, "说明": "动作目标"}]),
        ("系统核心支持库.权限审批.登记规则", 登记规则, [{"名称": "动作类型", "类型": "文本型", "必填": True, "默认值": None, "说明": "动作类型"}, {"名称": "策略", "类型": "文本型", "必填": True, "默认值": None, "说明": "允许/拒绝/询问"}]),
        ("系统核心支持库.权限审批.查询规则表", 查询规则表, []),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.系统核心支持库.权限审批",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="权限审批支持库",
            )
        )
