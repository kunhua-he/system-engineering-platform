# 权限审批（支持库.后端.系统核心支持库.权限审批）

- 类型：支持库
- 版本：1.0.1
- 说明：权限审批支持库
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 校验动作（`系统核心支持库.权限审批.校验动作`）
- 版本：1.0.0
- 一句话说明：校验动作是否放行（允许/拒绝/询问）
- 所属支持库或模块：支持库.后端.系统核心支持库.权限审批
- 参数列表：动作类型:文本型, 目标:文本型
- 必填参数：动作类型
- 可选参数：目标
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 未知动作
- 调用示例：系统核心支持库.权限审批.校验动作()
- 依赖能力：无
### 登记规则（`系统核心支持库.权限审批.登记规则`）
- 版本：1.0.0
- 一句话说明：登记/更新动作规则策略
- 所属支持库或模块：支持库.后端.系统核心支持库.权限审批
- 参数列表：动作类型:文本型, 策略:文本型
- 必填参数：动作类型, 策略
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.权限审批.登记规则()
- 依赖能力：无
### 查询规则表（`系统核心支持库.权限审批.查询规则表`）
- 版本：1.0.0
- 一句话说明：返回当前全部动作规则
- 所属支持库或模块：支持库.后端.系统核心支持库.权限审批
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 内部错误
- 调用示例：系统核心支持库.权限审批.查询规则表()
- 依赖能力：无
