<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 幂等消息（支持库.后端.系统核心支持库.幂等消息）

- 类型：支持库
- 版本：1.0.0
- 说明：会话消息幂等 id 生成。只依赖 Python 标准库。
- 入口：__init__.py

## 能力
### 生成id（`系统核心支持库.幂等消息.生成id`）
- 版本：1.0.0
- 一句话说明：生成幂等 id
- 所属支持库或模块：支持库.后端.系统核心支持库.幂等消息
- 参数列表：会话id:文本型, 内容:文本型, 前缀:文本型
- 必填参数：会话id, 内容
- 可选参数：前缀
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.幂等消息.生成id()
- 依赖能力：无
### 校验id（`系统核心支持库.幂等消息.校验id`）
- 版本：1.0.0
- 一句话说明：校验幂等 id 格式
- 所属支持库或模块：支持库.后端.系统核心支持库.幂等消息
- 参数列表：幂等id:文本型
- 必填参数：幂等id
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.幂等消息.校验id()
- 依赖能力：无
