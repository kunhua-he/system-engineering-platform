# 幂等消息（支持库.后端.系统核心支持库.幂等消息）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.2
- 说明：会话消息幂等 id 生成（借鉴 OpenCode SessionInput.admit）：同一 会话id+内容 永远同一 id，配合唯一约束实现重复提交返回同一记录。只生成不落库。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 生成id（`系统核心支持库.幂等消息.生成id`）
- 版本：1.0.0
- 一句话说明：生成幂等 id = sha256(会话id|内容) 截断 24 位，可带前缀。同一 会话id+内容 永远同一 id。
- 所属支持库或模块：支持库.后端.系统核心支持库.幂等消息
- 参数列表：会话id:文本型, 内容:文本型, 前缀:文本型
- 必填参数：会话id, 内容
- 可选参数：前缀
- 返回结构：{'类型': '结果型', '值结构': {'幂等id': '文本型', '算法': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.幂等消息.生成id()
- 依赖能力：无
### 校验id（`系统核心支持库.幂等消息.校验id`）
- 版本：1.0.0
- 一句话说明：校验幂等 id 格式（24位十六进制 或 前缀-24位）。
- 所属支持库或模块：支持库.后端.系统核心支持库.幂等消息
- 参数列表：幂等id:文本型
- 必填参数：幂等id
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'有效': '逻辑型', '原因': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：系统核心支持库.幂等消息.校验id()
- 依赖能力：无
