"""支持库.检查点恢复 包级中文入口。"""

from __future__ import annotations

from 支持库.后端.系统核心支持库.检查点恢复.实现.检查点恢复 import 保存检查点
from 支持库.后端.系统核心支持库.检查点恢复.实现.检查点恢复 import 恢复检查点

__all__ = [
    "保存检查点",
    "恢复检查点",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("系统核心支持库.检查点恢复.保存检查点", 保存检查点, [{"名称": "会话id", "类型": "文本型", "必填": True, "默认值": None, "说明": "会话id"}, {"名称": "状态快照", "类型": "字典型", "必填": True, "默认值": None, "说明": "状态快照"}, {"名称": "版本", "类型": "文本型", "必填": False, "默认值": "1.0.0", "说明": "版本"}]),
        ("系统核心支持库.检查点恢复.恢复检查点", 恢复检查点, [{"名称": "会话id", "类型": "文本型", "必填": True, "默认值": None, "说明": "会话id"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.系统核心支持库.检查点恢复",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="检查点恢复支持库",
            )
        )
