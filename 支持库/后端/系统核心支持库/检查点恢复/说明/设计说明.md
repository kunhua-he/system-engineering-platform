# 检查点恢复 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.系统核心支持库.检查点恢复
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.检查点恢复.保存检查点 | 保存检查点 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 保存会话检查点（SQLite，返回检查点id） |
| 系统核心支持库.检查点恢复.恢复检查点 | 恢复检查点 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 恢复会话最新检查点 |
| 系统核心支持库.检查点恢复.登记中断 | 登记中断 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 登记会话中断（Coze InterruptID 语义：保存检查点+中断原因，返回中断id） |
| 系统核心支持库.检查点恢复.续跑检查点 | 续跑检查点 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 带中断id续跑：校验中断→恢复→标记已续跑（幂等） |
| 系统核心支持库.检查点恢复.查询中断 | 查询中断 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询会话未续跑的中断列表 |
| 系统核心支持库.检查点恢复.创建任务 | 创建任务 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建任务（OpenClaw 任务注册表模式化：状态=排队，SQLite落库重启不丢） |
| 系统核心支持库.检查点恢复.领取任务 | 领取任务 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 领取任务（排队→运行中，仅排队态可领幂等） |
| 系统核心支持库.检查点恢复.完成任务 | 完成任务 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 完成任务（运行中→已完成/已失败，终态不可再改） |
| 系统核心支持库.检查点恢复.查询任务 | 查询任务 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 查询任务状态（按任务id 或 归属人列表） |
| 系统核心支持库.检查点恢复.续跑任务 | 续跑任务 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 续跑任务（已失败/已完成→排队，DeepSeek cold resume 语义） |
| 系统核心支持库.检查点恢复.超时重排队 | 超时重排队 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 运行中超时→排队（worker 崩溃恢复语义） |

## 系统核心支持库.检查点恢复.保存检查点（保存检查点）

**说明**：保存会话检查点（SQLite，返回检查点id）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |
| 状态快照 | 字典型 | 是 | None | 状态快照 |
| 版本 | 文本型 | 否 | 1.0.0 | 版本 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 保存检查点失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.保存检查点(会话id=值, 状态快照=值, 版本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.恢复检查点（恢复检查点）

**说明**：恢复会话最新检查点

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 恢复检查点失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.恢复检查点(会话id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.登记中断（登记中断）

**说明**：登记会话中断（Coze InterruptID 语义：保存检查点+中断原因，返回中断id）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |
| 状态快照 | 字典型 | 是 | None | 中断时状态 |
| 中断原因 | 文本型 | 是 | None | 中断原因 |
| 版本 | 文本型 | 否 | 1.0.0 | 版本 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 保存检查点失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.登记中断(会话id=值, 状态快照=值, 中断原因=值, 版本=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.续跑检查点（续跑检查点）

**说明**：带中断id续跑：校验中断→恢复→标记已续跑（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |
| 中断id | 文本型 | 是 | None | 中断id |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 中断不存在或已续跑 |
| 续跑失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.续跑检查点(会话id=值, 中断id=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.查询中断（查询中断）

**说明**：查询会话未续跑的中断列表

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 查询中断失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.查询中断(会话id=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.创建任务（创建任务）

**说明**：创建任务（OpenClaw 任务注册表模式化：状态=排队，SQLite落库重启不丢）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 任务名 | 文本型 | 是 | None | 任务名 |
| 归属人 | 文本型 | 否 | None | 归属人 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 创建任务失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.创建任务(任务名=值, 归属人=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.领取任务（领取任务）

**说明**：领取任务（排队→运行中，仅排队态可领幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 任务id | 文本型 | 是 | None | 任务id |
| 执行者 | 文本型 | 否 | None | 执行者 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务不存在 |
| 领取任务失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.领取任务(任务id=值, 执行者=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.完成任务（完成任务）

**说明**：完成任务（运行中→已完成/已失败，终态不可再改）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 任务id | 文本型 | 是 | None | 任务id |
| 成功 | 逻辑型 | 是 | None | 是否成功 |
| 结果值 | 字典型 | 否 | None | 结果 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务不存在 |
| 完成任务失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.完成任务(任务id=值, 成功=值, 结果值=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.查询任务（查询任务）

**说明**：查询任务状态（按任务id 或 归属人列表）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 任务id | 文本型 | 否 | None | 任务id |
| 归属人 | 文本型 | 否 | None | 归属人 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务不存在 |
| 查询任务失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.查询任务(任务id=值, 归属人=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.续跑任务（续跑任务）

**说明**：续跑任务（已失败/已完成→排队，DeepSeek cold resume 语义）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 任务id | 文本型 | 是 | None | 任务id |
| 执行者 | 文本型 | 否 | None | 执行者 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务不存在 |
| 续跑任务失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.续跑任务(任务id=值, 执行者=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）

## 系统核心支持库.检查点恢复.超时重排队（超时重排队）

**说明**：运行中超时→排队（worker 崩溃恢复语义）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 任务id | 文本型 | 是 | None | 任务id |
| 超时秒 | 整数型 | 否 | 60 | 超时阈值 |
| 库路径 | 文本型 | 否 | None | 可选库路径 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 任务不存在 |
| 超时重排队失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.超时重排队(任务id=值, 超时秒=值, 库路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：检查点恢复.检查点与中断闭环（正向）、检查点恢复.任务状态机闭环（正向）
