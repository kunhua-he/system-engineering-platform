# 检查点恢复 使用说明

- 包id：支持库.后端.系统核心支持库.检查点恢复
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 系统核心支持库.检查点恢复.保存检查点 | 保存检查点 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 保存会话检查点（SQLite，返回检查点id） |
| 系统核心支持库.检查点恢复.恢复检查点 | 恢复检查点 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 恢复会话最新检查点 |

## 系统核心支持库.检查点恢复.保存检查点（保存检查点）

**说明**：保存会话检查点（SQLite，返回检查点id）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |
| 状态快照 | 字典型 | 是 | None | 状态快照 |
| 版本 | 文本型 | 否 | 1.0.0 | 版本 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 保存检查点失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.保存检查点(会话id=值, 状态快照=值, 版本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 系统核心支持库.检查点恢复.恢复检查点（恢复检查点）

**说明**：恢复会话最新检查点

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 会话id | 文本型 | 是 | None | 会话id |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 恢复检查点失败 |

**调用示例**：

```python
系统核心支持库.检查点恢复.恢复检查点(会话id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
