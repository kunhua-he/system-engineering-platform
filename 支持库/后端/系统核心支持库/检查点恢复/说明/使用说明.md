# 检查点恢复（支持库.后端.系统核心支持库.检查点恢复）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.5
- 说明：检查点恢复支持库
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 保存检查点（`系统核心支持库.检查点恢复.保存检查点`）
- 版本：1.0.0
- 一句话说明：保存会话检查点（SQLite，返回检查点id）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：会话id:文本型, 状态快照:字典型, 版本:文本型, 库路径:文本型, 中断原因:文本型
- 必填参数：会话id, 状态快照
- 可选参数：版本, 库路径, 中断原因
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 保存检查点失败
- 调用示例：系统核心支持库.检查点恢复.保存检查点(版本=1.0.0)
- 依赖能力：无
### 恢复检查点（`系统核心支持库.检查点恢复.恢复检查点`）
- 版本：1.0.0
- 一句话说明：恢复会话最新检查点
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：会话id:文本型, 库路径:文本型
- 必填参数：会话id
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 恢复检查点失败
- 调用示例：系统核心支持库.检查点恢复.恢复检查点()
- 依赖能力：无
### 登记中断（`系统核心支持库.检查点恢复.登记中断`）
- 版本：1.0.0
- 一句话说明：登记会话中断（Coze InterruptID 语义：保存检查点+中断原因，返回中断id）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：会话id:文本型, 状态快照:字典型, 中断原因:文本型, 版本:文本型, 库路径:文本型, 开工ID:文本型
- 必填参数：会话id, 状态快照, 中断原因
- 可选参数：版本, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 保存检查点失败
- 调用示例：系统核心支持库.检查点恢复.登记中断(版本=1.0.0)
- 依赖能力：无
### 续跑检查点（`系统核心支持库.检查点恢复.续跑检查点`）
- 版本：1.0.0
- 一句话说明：带中断id续跑：校验中断→恢复→标记已续跑（幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：会话id:文本型, 中断id:文本型, 库路径:文本型
- 必填参数：会话id, 中断id
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 中断不存在或已续跑, 续跑失败, 中断不存在
- 调用示例：系统核心支持库.检查点恢复.续跑检查点()
- 依赖能力：无
### 查询中断（`系统核心支持库.检查点恢复.查询中断`）
- 版本：1.0.0
- 一句话说明：查询会话未续跑的中断列表
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：会话id:文本型, 库路径:文本型
- 必填参数：会话id
- 可选参数：库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 查询中断失败
- 调用示例：系统核心支持库.检查点恢复.查询中断()
- 依赖能力：无
### 创建任务（`系统核心支持库.检查点恢复.创建任务`）
- 版本：1.0.0
- 一句话说明：创建任务（OpenClaw 任务注册表模式化：状态=排队，SQLite落库重启不丢）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：任务名:文本型, 归属人:文本型, 库路径:文本型, 开工ID:文本型
- 必填参数：任务名
- 可选参数：归属人, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 创建任务失败
- 调用示例：系统核心支持库.检查点恢复.创建任务()
- 依赖能力：无
### 领取任务（`系统核心支持库.检查点恢复.领取任务`）
- 版本：1.0.0
- 一句话说明：领取任务（排队→运行中，仅排队态可领幂等）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：任务id:文本型, 执行者:文本型, 库路径:文本型, 开工ID:文本型
- 必填参数：任务id
- 可选参数：执行者, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务不存在, 领取任务失败
- 调用示例：系统核心支持库.检查点恢复.领取任务()
- 依赖能力：无
### 完成任务（`系统核心支持库.检查点恢复.完成任务`）
- 版本：1.0.0
- 一句话说明：完成任务（运行中→已完成/已失败，终态不可再改）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：任务id:文本型, 成功:逻辑型, 结果值:字典型, 库路径:文本型, 开工ID:文本型
- 必填参数：任务id, 成功
- 可选参数：结果值, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务不存在, 完成任务失败
- 调用示例：系统核心支持库.检查点恢复.完成任务()
- 依赖能力：无
### 查询任务（`系统核心支持库.检查点恢复.查询任务`）
- 版本：1.0.0
- 一句话说明：查询任务状态（按任务id 或 归属人列表）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：任务id:文本型, 归属人:文本型, 库路径:文本型
- 必填参数：无
- 可选参数：任务id, 归属人, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务不存在, 查询任务失败
- 调用示例：系统核心支持库.检查点恢复.查询任务()
- 依赖能力：无
### 续跑任务（`系统核心支持库.检查点恢复.续跑任务`）
- 版本：1.0.0
- 一句话说明：续跑任务（已失败/已完成→排队，DeepSeek cold resume 语义）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：任务id:文本型, 执行者:文本型, 库路径:文本型
- 必填参数：任务id
- 可选参数：执行者, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务不存在, 续跑任务失败
- 调用示例：系统核心支持库.检查点恢复.续跑任务()
- 依赖能力：无
### 超时重排队（`系统核心支持库.检查点恢复.超时重排队`）
- 版本：1.0.0
- 一句话说明：运行中超时→排队（worker 崩溃恢复语义）
- 所属支持库或模块：支持库.后端.系统核心支持库.检查点恢复
- 参数列表：任务id:文本型, 超时秒:双精度数型, 库路径:文本型
- 必填参数：任务id
- 可选参数：超时秒, 库路径
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 任务不存在, 超时重排队失败
- 调用示例：系统核心支持库.检查点恢复.超时重排队(超时秒=60)
- 依赖能力：无
