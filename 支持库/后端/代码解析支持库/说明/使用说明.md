# 代码解析支持库（支持库.后端.代码解析支持库）

- 类型：支持库
- 版本：1.1.2
- 说明：代码解析原子能力：解析代码字节/解析代码文件/按规则切块/解码文本，供代码解析器调用。只依赖 Python 标准库。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 解析代码字节（`代码解析支持库.解析代码字节`）
- 版本：1.0.0
- 一句话说明：解析代码字节为块列表
- 所属支持库或模块：支持库.后端.代码解析支持库
- 参数列表：原始字节:字节集型, 文件id:整数型, 文件格式:文本型, 规则:字典型, 规则文件路径:文本型, 支持扩展名:列表型
- 必填参数：原始字节, 文件id, 文件格式
- 可选参数：规则, 规则文件路径, 支持扩展名
- 返回结构：{'类型': '结果型', '值结构': {'值': '块列表字典'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：代码解析失败, 解析异常
- 调用示例：代码解析支持库.解析代码字节()
- 依赖能力：无
### 解析代码文件（`代码解析支持库.解析代码文件`）
- 版本：1.0.0
- 一句话说明：解析代码文件为块列表
- 所属支持库或模块：支持库.后端.代码解析支持库
- 参数列表：文件id:整数型, 路径:文本型, 扩展名:文本型, 规则文件路径:文本型, 支持扩展名:列表型
- 必填参数：文件id, 路径, 扩展名, 规则文件路径
- 可选参数：支持扩展名
- 返回结构：{'类型': '结果型', '值结构': {'值': '块列表字典'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：代码解析失败, 解析异常
- 调用示例：代码解析支持库.解析代码文件()
- 依赖能力：无
### 按规则切块（`代码解析支持库.按规则切块`）
- 版本：1.0.0
- 一句话说明：按规则切分块
- 所属支持库或模块：支持库.后端.代码解析支持库
- 参数列表：内容:文本型, 文件id:整数型, 文件格式:文本型, 规则:字典型
- 必填参数：内容, 文件id, 文件格式, 规则
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '块列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：代码解析失败, 解析异常
- 调用示例：代码解析支持库.按规则切块()
- 依赖能力：无
### 解码文本（`代码解析支持库.解码文本`）
- 版本：1.0.0
- 一句话说明：解码字节为文本
- 所属支持库或模块：支持库.后端.代码解析支持库
- 参数列表：原始字节:字节集型
- 必填参数：原始字节
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': {'文本': '文本', '编码': '编码名'}}}（统一结果：成功/值/错误码/错误说明）
- 错误码：代码解析失败, 解析异常
- 调用示例：代码解析支持库.解码文本()
- 依赖能力：无
### 加载切块规则（`代码解析支持库.加载切块规则`）
- 版本：1.0.0
- 一句话说明：加载切块规则字典
- 所属支持库或模块：支持库.后端.代码解析支持库
- 参数列表：规则文件路径:文本型, 强制重载:逻辑型
- 必填参数：规则文件路径
- 可选参数：强制重载
- 返回结构：{'类型': '结果型', '值结构': {'值': '规则字典'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：代码解析失败, 解析异常
- 调用示例：代码解析支持库.加载切块规则()
- 依赖能力：无
### 扫描类属性（`代码解析支持库.扫描类属性`）
- 版本：1.0.0
- 一句话说明：AST 扫描类属性字符串赋值，产出去重值清单（如 __tablename__）
- 所属支持库或模块：支持库.后端.代码解析支持库
- 参数列表：目录:文本型, 属性名:文本型, 递归:逻辑型, 排除关键字:列表型, 最大文件数:整数型
- 必填参数：目录, 属性名
- 可选参数：递归, 排除关键字, 最大文件数
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在
- 调用示例：代码解析支持库.扫描类属性(递归=True, 最大文件数=5000)
- 依赖能力：无
