# 代码解析支持库 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


代码解析原子能力：解析代码字节/解析代码文件/按规则切块/解码文本/加载切块规则。

调用方式（40007 网关）：

```text
POST 40007 /网关/调用
{能力id: "代码解析支持库.解析代码字节", 参数: {原始字节, 文件id, 文件格式}}
```

只依赖 Python 标准库。

## 能力清单

| 能力id | 名称 | 说明 |
| --- | --- | --- |
| 代码解析支持库.解析代码字节 | 解析代码字节 | 解析代码字节为块列表 |
| 代码解析支持库.解析代码文件 | 解析代码文件 | 解析代码文件为块列表 |
| 代码解析支持库.按规则切块 | 按规则切块 | 按规则切分块 |
| 代码解析支持库.解码文本 | 解码文本 | 解码字节为文本 |
| 代码解析支持库.加载切块规则 | 加载切块规则 | 加载切块规则字典 |
| 代码解析支持库.扫描类属性 | 扫描类属性 | AST 扫描类属性字符串赋值，产出去重值清单（如 __tablename__） |
