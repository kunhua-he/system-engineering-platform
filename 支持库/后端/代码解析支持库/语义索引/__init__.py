"""语义索引包级中文入口。

调用者只能经 40007 网关调用能力，禁止直接 import 实现目录。
"""
from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.代码解析支持库.语义索引.实现.语义索引 import (
    建代码索引, 查代码块,
)

__all__ = ["建代码索引", "查代码块"]

包id = "支持库.后端.代码解析支持库.语义索引"


def 注册能力(注册表) -> None:
    """由支持库加载器调用。

    参数名序必须与 能力定义.json 逐字逐序一致（网关漂移检测会比对）；本入口不再手工
    重建参数表——参数整条取自 能力契约/参数契约.json（见下块）。
    """
    from 公共契约.能力契约.契约 import 能力实现

# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 参数契约镜像（**函数内字面量**；本块由「丢参」缺陷修复注入，勿删）
    #
    # 缺陷溯源（2026-09-18 注入；2026-09-22 按 #204 铺开到剩余旧入口）：本入口原先手工
    # 重建注册参数声明，`默认值`/`说明` 全靠手抄，抄漏即静默丢参——网关按**入口注册表**
    # 过滤入参（`运行核心/统一网关/协议/类型规格.py` 的必填判据是 `项.get("必填") is 真`），
    # 入口表漏一个参数，新参数就传了没反应（#197 实测：`文件系统支持库.文件操作.目录树`
    # 漏了 `忽略模式`，两次验收都拿到旧行为）。修法：参数声明从 `能力契约/参数契约.json`
    # **整条透传**（`参数=_参数契约表[能力id]`），入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：
    #  ① `_参数契约表` 是字面量、键是常量，`开发工具/契约编译/漂移检测` 的 AST 读取器
    #     （`_静态字面量` 的 `ast.Subscript` 分支）能解析出**整条参数含 `必填`/`默认值`**；
    #     放在函数内是因为该读取器只收 `注册能力` 函数体内的赋值（`_扫描注册语句`）。
    #  ② 表内布尔位写**中文 `真`／`假`**（正式口径，真源 `公共契约/基础类型/逻辑类型.py`）；
    #     本文件顶部已 import 真/假，漂移检测读取器（`_导入常量绑定`）能静态解析出参数表。
    # 为什么按下标取而不是包一层 `_参数声明(能力id)`：包一层会让 AST 一律判「不可静态
    # 判定」，等于拿门禁覆盖率换干净（不可接受）。契约名序/返回口径核验由装配期唯一闸门承担
    # （`运行核心/加载器/生命周期管理/管理器.装配系统` → 内核 `能力注册表.校验声明一致`），本入口不再另立第二份。
    _参数契约表 = {
        "代码解析支持库.语义索引.建代码索引": [
            {"名称": "索引根", "类型": "文本型", "必填": 真, "默认值": None, "说明": "要索引的代码目录"},
            {"名称": "库文件", "类型": "文本型", "必填": 假, "默认值": "", "说明": "SQLite 落点；空 = 工程缓存/语义索引/<目录名>.db"},
            {"名称": "嵌入模型名", "类型": "文本型", "必填": 假, "默认值": "bge-m3", "说明": "嵌入模型名"},
            {"名称": "排除目录", "类型": "列表型", "必填": 假, "默认值": [], "说明": "空 = 平台默认排除"},
            {"名称": "批大小", "类型": "整数型", "必填": 假, "默认值": 32, "说明": "每批嵌入条数"},
            {"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 60.0, "说明": "子进程超时"},
        ],
        "代码解析支持库.语义索引.查代码块": [
            {"名称": "索引根", "类型": "文本型", "必填": 真, "默认值": None, "说明": "已建索引的根（用于定位默认库文件）"},
            {"名称": "查询", "类型": "文本型", "必填": 真, "默认值": None, "说明": "自然语言或符号名"},
            {"名称": "库文件", "类型": "文本型", "必填": 假, "默认值": "", "说明": "空 = 用默认库文件"},
            {"名称": "顶部数量", "类型": "整数型", "必填": 假, "默认值": 10, "说明": "返回条数"},
            {"名称": "文件模式", "类型": "文本型", "必填": 假, "默认值": "", "说明": "可选 glob 过滤"},
            {"名称": "最小分数", "类型": "双精度数型", "必填": 假, "默认值": 0.0, "说明": "低于该分数的丢弃"},
            {"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 60.0, "说明": "子进程超时"},
        ],
    }
# ===== 参数声明透传 结束 =====
    契约表 = [
        ("代码解析支持库.语义索引.建代码索引", 建代码索引,
         "把代码目录切成块 → 生成嵌入向量 → 落 SQLite（块文本 + 向量 + 文件路径 + 起止行）。切块复用 代码解析支持库.按规则切块，嵌入复用 大语言模型支持库.模型连接器 与 嵌入缓存（命中缓存不重算）。单文件失败隔离进 跳过清单。"),
        ("代码解析支持库.语义索引.查代码块", 查代码块,
         "向量召回 topN 后做关键词/符号精排（召回宽、精排严），返回带真实 文件路径 与 起始行/结束行 的候选列表。语义索引只做召回补充，最终位置必须回 读取文件 按行区间取真实内容，不以向量相似度当事实。"),
    ]
    for 能力id, 函数, 说明 in 契约表:
        注册表.注册(能力实现(
            能力id=能力id, 包id=包id, 实现函数=函数,
            参数=_参数契约表[能力id], 返回="结果型", 说明=说明))
