{
  "搜索词": [
    "切块",
    "语义索引"
  ],
  "候选能力id": [
    "代码解析支持库.按规则切块",
    "代码解析支持库.解析代码文件",
    "代码解析支持库.加载切块规则",
    "大语言模型支持库.模型连接器.生成嵌入",
    "大语言模型支持库.嵌入缓存.查询嵌入",
    "大语言模型支持库.嵌入缓存.存储嵌入",
    "项目文档支持库.切块"
  ],
  "决策": [
    "切块：**复用** `代码解析支持库.按规则切块` —— 本包不再写第二套切块（搜索词「切块」实测 7 条命中，其中 3 条是本包上游的切块件）",
    "嵌入：**复用** `大语言模型支持库.模型连接器` 的 `连接向量模型`/`生成嵌入`，缓存**复用** `大语言模型支持库.嵌入缓存` —— 本包不自己调模型、不建第二套缓存表（命中缓存不重算）",
    "`项目文档支持库.切块` 不适用：它按 Markdown 结构切**文档**（标题/段落/表格），本包切**代码**目录，面向对象不同",
    "**新建**：`建代码索引` / `查代码块` 两个能力 —— 检索面此前只有文档侧（`项目文档支持库`）与符号侧（`代码解析支持库.代码地图`，要精确名字才查得到），缺「自然语言 → 代码位置」这一层；搜索词「语义索引」「代码块」实测候选里均无承载，故新建成立",
    "句柄生命周期（2026-09-22 改）：查询侧改为**长驻复用**（模块级缓存），不再每次「取句柄 → 用 → 释放」—— 实测每次建拆多付约 6 秒；释放时同步清缓存，避免把死句柄还回去"
  ],
  "第三方处理": "无第三方依赖：无需 依赖锁.json（仓内铁口径：依赖锁存在 ⟺ 有第三方依赖）",
  "包id": "支持库.后端.代码解析支持库.语义索引",
  "名称": "语义索引",
  "说明": "本文件是设计期决策记录，不参与运行时；改动后需重算 完整性摘要.json"
}
