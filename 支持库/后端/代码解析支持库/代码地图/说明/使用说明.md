# 代码地图（支持库.后端.代码解析支持库.代码地图）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.2
- 说明：代码地图原子能力：只读直读代码地图 SQLite（nodes/edges 表），按模式检索符号节点、查节点出边入边。只产出结构化事实，不渲染文本、不读源码、不做相关性排名。只依赖 Python 标准库（sqlite3），直读外部 CLI 产出的索引文件，不写库、不在索引目录建 side 文件。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 查询节点（`代码解析支持库.代码地图.查询节点`）
- 版本：1.0.0
- 一句话说明：直读代码地图 SQLite 的 nodes 表检索符号节点：模式参数化四类匹配（名称子串/名称精确/限定名子串/路径子串/全文），节点种类、文件路径、语言收窄。命中为空是成功结果（节点列表 空、命中数 0），不是错误；不做相关性排名与文本渲染
- 所属支持库或模块：支持库.后端.代码解析支持库.代码地图
- 参数列表：代码地图路径:文本型, 关键词:文本型, 模式:文本型, 节点种类:列表型, 文件路径:文本型, 语言:文本型, 最大条数:整数型
- 必填参数：代码地图路径, 关键词
- 可选参数：模式, 节点种类, 文件路径, 语言, 最大条数
- 返回结构：{"类型": "结果型", "值结构": {"节点列表": "有序：[{节点id,种类,名称,限定名,文件路径,语言,起始行,结束行,是否导出,签名}]；按 文件路径、起始行、名称 稳定排序", "命中数": "整数型：WHERE 命中总数（不随 最大条数 变化）", "已截断": "逻辑型：命中数 大于 实际返回条数 时为 true", "模式": "文本型：本次生效的模式", "代码地图路径": "文本型：被读取的代码地图绝对路径"}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 查询失败
- 调用示例：代码解析支持库.代码地图.查询节点（经 40007 网关 POST /网关/调用，参数：代码地图路径=<项目根>/.codegraph/codegraph.db, 关键词=网关核心, 模式=名称精确）
- 依赖能力：无
### 查询关系（`代码解析支持库.代码地图.查询关系`）
- 版本：1.0.0
- 一句话说明：直读代码地图 SQLite 的 edges 表查某节点的关系事实：方向参数化出边/入边/双向，关系种类收窄。起始节点按 节点id 精确命中，或按 节点名称（可加 文件路径）唯一命中；不判调用链相关性、不拼源码、不评爆炸半径
- 所属支持库或模块：支持库.后端.代码解析支持库.代码地图
- 参数列表：代码地图路径:文本型, 节点id:文本型, 节点名称:文本型, 文件路径:文本型, 方向:文本型, 关系种类:列表型, 最大条数:整数型
- 必填参数：代码地图路径
- 可选参数：节点id, 节点名称, 文件路径, 方向, 关系种类, 最大条数
- 返回结构：{"类型": "结果型", "值结构": {"起始节点": "{节点id,种类,名称,限定名,文件路径,语言,起始行,结束行,是否导出,签名}", "关系列表": "有序：[{方向,关系种类,行号,对方节点id,对方种类,对方名称,对方文件路径,对方起始行}]；按 对方文件路径、对方起始行、对方名称 稳定排序", "命中数": "整数型：关系命中总数（不随 最大条数 变化）", "已截断": "逻辑型：命中数 大于 实际返回条数 时为 true", "方向": "文本型：本次生效的方向", "代码地图路径": "文本型：被读取的代码地图绝对路径"}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 查询失败
- 调用示例：代码解析支持库.代码地图.查询关系（经 40007 网关 POST /网关/调用，参数：代码地图路径=<项目根>/.codegraph/codegraph.db, 节点名称=网关核心, 方向=入边）
- 依赖能力：无
### 查询状态（`代码解析支持库.代码地图.查询状态`）
- 版本：1.0.0
- 一句话说明：只读报告代码地图索引的**真实可用性**与**覆盖率**：直读 SQLite 报节点数/边数/已索引文件数，传 项目根 时再算「仓库 .py 总数 vs 已索引数」的覆盖率与未索引数，据此判 可用/需同步。替代原先「.codegraph 目录存在」的弱判据；**不跑外部 CLI**（codegraph status 实测 45 秒超时），秒级返回
- 所属支持库或模块：支持库.后端.代码解析支持库.代码地图
- 参数列表：代码地图路径:文本型, 项目根:文本型
- 必填参数：代码地图路径
- 可选参数：项目根
- 返回结构：{"类型": "结果型", "值结构": {"可用": "逻辑型：状态为 可用 时为 true；需同步/不存在/缺表/打不开 均为 false", "状态": "文本型：可用 / 需同步 / 不存在 / 缺表 / 打不开", "详情": "文本型：人读原因（含节点数、边数、未索引数、覆盖率）", "节点数": "整数型：nodes 表行数", "边数": "整数型：edges 表行数", "已索引文件数": "整数型：nodes 表里 distinct file_path 数", "覆盖率": "字典型：{仓库py文件数, 已索引py文件数, 未索引py文件数, 覆盖率}；未传 项目根 或扫描失败时为 {} 或只含 说明"}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：代码解析支持库.代码地图.查询状态（经 40007 网关 POST /网关/调用，参数：代码地图路径=<项目根>/.codegraph/codegraph.db, 项目根=<项目根>）
- 依赖能力：无
