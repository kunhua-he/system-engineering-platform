# 文件系统支持库

提供原子文件系统能力，全部通过包级中文入口调用：

```python
from 支持库.后端.文件系统 import 读取文件, 写入文件, 判断存在, 列出目录, 删除文件, 复制文件, 移动文件, 获取大小, 获取修改时间
```

- 文本读写自动创建父目录。
- 删除不存在的文件视为成功（幂等）。
- 失败统一返回 `结果(成功=False, 错误=错误结构)`，不抛裸异常。
