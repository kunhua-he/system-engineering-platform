"""Tesseract 提供者包级中文入口（外部命令 tesseract 受管执行）。

调用者只从此入口导入，禁止深入 实现/ 目录。
公开能力（分组 OCR识别）：
- 识别图片 → {文本} 或 {词列表}（词级数据，路径/字节二选一）
- 语言包列表 → {语言列表, 数据目录, 数量}
- 版本探针 → {tesseract, 版本, 满足最低版本}
命令路径配置覆盖体系：函数参数 命令路径 > 环境变量 Tesseract提供者_命令路径
> 默认 PATH 探测；显式路径不存在 → 工具缺失，不可执行 → 命令失败，
版本低于最低版本 → 版本不兼容。
所有 tesseract 调用一律走 受管进程（独立进程组 + 超时/取消/上限/零残留）。
"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.OCR识别支持库.OCR识别.实现.提供者 import 识别图片
from 支持库.后端.OCR识别支持库.OCR识别.实现.提供者 import 语言包列表
from 支持库.后端.OCR识别支持库.OCR识别.实现.提供者 import 版本探针

__all__ = [
    "识别图片",
    "语言包列表",
    "版本探针",
    "注册能力",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用，向能力注册表注册本库能力。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("OCR识别支持库.OCR识别.识别图片", 识别图片, [{"名称": "图片路径", "类型": "文本型", "必填": 假, "说明": "图片文件绝对路径（与图片字节二选一）"}, {"名称": "图片字节", "类型": "字节集型", "必填": 假, "说明": "图片二进制内容（与图片路径二选一，经临时目录落盘后识别，即时清理）"}, {"名称": "语言", "类型": "文本型", "必填": 假, "默认值": "eng", "说明": "tesseract 语言码，如 eng/chi_sim；缺失时返回 语言包缺失"}, {"名称": "词级数据", "类型": "逻辑型", "必填": 假, "默认值": 假, "说明": "True 返回词级数据 {词列表}（TSV 解析），False 返回 {文本}"}, {"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 60, "说明": "外部进程超时（超过强杀进程组返回 超时）"}, {"名称": "取消事件", "类型": "资源引用型", "必填": 假, "说明": "threading.Event；被设置则终止进程组返回 取消"}, {"名称": "输出上限字节", "类型": "双精度数型", "必填": 假, "默认值": 33554432, "说明": "stdout 输出上限，超过返回 超出限制"}, {"名称": "命令路径", "类型": "文本型", "必填": 假, "说明": "tesseract 可执行文件路径；优先于环境变量 Tesseract提供者_命令路径，其次默认 PATH 探测。显式路径不存在 → 工具缺失；不可执行 → 命令失败。错误消息与日志不泄露路径细节"}]),
        ("OCR识别支持库.OCR识别.语言包列表", 语言包列表, [{"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 10, "说明": "外部进程超时"}, {"名称": "命令路径", "类型": "文本型", "必填": 假, "说明": "tesseract 可执行文件路径；优先于环境变量 Tesseract提供者_命令路径，其次默认 PATH 探测。显式路径不存在 → 工具缺失；不可执行 → 命令失败。错误消息与日志不泄露路径细节"}]),
        ("OCR识别支持库.OCR识别.版本探针", 版本探针, [{"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 10, "说明": "外部进程超时"}, {"名称": "命令路径", "类型": "文本型", "必填": 假, "说明": "tesseract 可执行文件路径；优先于环境变量 Tesseract提供者_命令路径，其次默认 PATH 探测。显式路径不存在 → 工具缺失；不可执行 → 命令失败。错误消息与日志不泄露路径细节"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.OCR识别支持库.OCR识别",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="探测 tesseract 版本与可用性：返回 {tesseract, 版本, 满足最低版本}；版本低于最低版本时返回 版本不兼容",
            )
        )
