# 网络请求（支持库.后端.网络通信支持库.请求）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：2.0.4
- 说明：网络请求原子能力：HTTP 客户端适配所有主流场景（方法/请求头/代理/SSL/重定向/Cookie/文件上传/文件下载），SSRF 防护
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `网络通信支持库.出站安全.校验出站URL` 版本约束 `>=1.0.0`
## 能力
### 发送请求（`网络通信支持库.请求.发送请求`）
- 版本：2.0.0
- 一句话说明：发送 HTTP 请求，适配主流场景：GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS、JSON/表单/原始字节、自定义请求头/Cookie、代理、SSL验证开关、重定向控制、超时/SSRF防护/响应上限
- 所属支持库或模块：支持库.后端.网络通信支持库.请求
- 参数列表：地址:文本型, 方法:文本型, 请求头:字典型, 请求体:JSON值型, 超时秒:双精度数型, 最大字节数:整数型, 允许回环:逻辑型, 代理:文本型, SSL验证:逻辑型, 跟随重定向:逻辑型, Cookie:文本型
- 必填参数：地址
- 可选参数：方法, 请求头, 请求体, 超时秒, 最大字节数, 允许回环, 代理, SSL验证, 跟随重定向, Cookie
- 返回结构：{'类型': '结果型', '值结构': {'状态码': '整数型，HTTP 响应状态码', '响应头': '字典型，响应头键值对（同名头以列表回带）', '响应文本': '文本型，响应正文（按响应编码解码后的文本）', '错误信息': '文本型，成功恒为空串（非空说明仅出现在失败详情的回带里）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 响应超限, 网络超时, 网络失败, HTTP错误
- 调用示例：网络通信支持库.请求.发送请求(方法=GET, 超时秒=10.0, 最大字节数=5242880, 允许回环=False)
- 依赖能力：网络通信支持库.出站安全.校验出站URL
### 上传文件（`网络通信支持库.请求.上传文件`）
- 版本：2.0.0
- 一句话说明：multipart/form-data 上传本地文件
- 所属支持库或模块：支持库.后端.网络通信支持库.请求
- 参数列表：地址:文本型, 文件路径:文本型, 字段名:文本型, 额外字段:字典型, 超时秒:双精度数型, 允许回环:逻辑型, 代理:文本型
- 必填参数：地址, 文件路径
- 可选参数：字段名, 额外字段, 超时秒, 允许回环, 代理
- 返回结构：{'类型': '结果型', '值结构': {'状态码': '整数型，HTTP 响应状态码', '响应头': '字典型，响应头键值对（同名头以列表回带）', '响应文本': '文本型，响应正文（按响应编码解码后的文本）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超出限制, 上传失败, 响应超限, 网络超时, 网络失败, HTTP错误
- 调用示例：网络通信支持库.请求.上传文件(字段名=file, 超时秒=10.0, 允许回环=False)
- 依赖能力：网络通信支持库.出站安全.校验出站URL
### 下载文件（`网络通信支持库.请求.下载文件`）
- 版本：2.0.0
- 一句话说明：从 URL 流式下载文件到本地（适配大文件）
- 所属支持库或模块：支持库.后端.网络通信支持库.请求
- 参数列表：地址:文本型, 保存路径:文本型, 请求头:字典型, 超时秒:双精度数型, 允许回环:逻辑型, 代理:文本型, SSL验证:逻辑型
- 必填参数：地址, 保存路径
- 可选参数：请求头, 超时秒, 允许回环, 代理, SSL验证
- 返回结构：{'类型': '结果型', '值结构': {'状态码': '整数型，HTTP 响应状态码', '文件路径': '文本型，落盘文件的绝对路径', '字节数': '整数型，写入落盘文件的字节数'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 响应超限, 下载失败, 网络超时
- 调用示例：网络通信支持库.请求.下载文件(超时秒=10.0, 允许回环=False)
- 依赖能力：网络通信支持库.出站安全.校验出站URL
### 构建查询串（`网络通信支持库.请求.构建查询串`）
- 版本：2.0.0
- 一句话说明：把参数映射编码为 URL 查询串
- 所属支持库或模块：支持库.后端.网络通信支持库.请求
- 参数列表：参数:字典型
- 必填参数：参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'查询串': '文本型，urlencode 后的查询串；参数为空时为空串'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：网络通信支持库.请求.构建查询串()
- 依赖能力：网络通信支持库.出站安全.校验出站URL
