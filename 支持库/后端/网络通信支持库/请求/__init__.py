"""网络请求支持库包级中文入口（外部协议边界）。

调用者只从此入口导入，禁止深入 实现/ 目录。
安全默认：SSRF 防护（拒绝回环/内网/私有地址）、响应大小上限、凭证不泄漏。
"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 公共契约.能力契约.契约 import 能力实现
from 支持库.后端.网络通信支持库.请求.实现.网络请求 import 发送请求
from 支持库.后端.网络通信支持库.请求.实现.网络请求 import 上传文件
from 支持库.后端.网络通信支持库.请求.实现.网络请求 import 下载文件
from 支持库.后端.网络通信支持库.请求.实现.网络请求 import 构建查询串

__all__ = [
    "发送请求",
    "上传文件",
    "下载文件",
    "构建查询串",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 缺陷溯源：本入口原先手工重建注册参数声明（`参数=参数表` 四元组里的手写字面量），
    # 只写 名称/类型/必填，`默认值` 整批缺声明（2026-09-24 实测本包 26 条、全仓 93 条）。
    # 后果：网关按入口契约表判参时拿不到默认值 —— 「≥10 秒须走异步腿」的守卫**不传参即被绕过**
    # （发送请求/上传文件/下载文件 契约 超时秒 默认 10，实测不传参 成功=true）。
    # 手工表一旦漏一个参数，新参数还会被网关**静默丢弃**（同族现场见未完成事项 #197）。
    # 修法：参数声明从 `能力契约/参数契约.json` **整条透传**，入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：漂移检测的 AST 读取器
    # （`_静态字面量` 的 `ast.Subscript` 分支）只收 `注册能力` 函数体内的赋值，
    # 包一层 `_参数声明(能力id)` 会让 AST 一律判「不可静态判定」，等于拿门禁覆盖率换干净。
    # 表内布尔位写中文 真/假（真源 公共契约/基础类型/逻辑类型.py）。
    # 契约名序/返回口径核验由装配期唯一闸门承担（`运行核心/加载器/生命周期管理/管理器.装配系统`
    # → 内核 `能力注册表.校验声明一致`），本入口不再另立第二份。
    _参数契约表 = {
        "网络通信支持库.请求.发送请求": [
            {"名称": "地址", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "方法", "类型": "文本型", "必填": 假, "默认值": "GET"},
            {"名称": "请求头", "类型": "字典型", "必填": 假, "默认值": None},
            {"名称": "请求体", "类型": "JSON值型", "必填": 假, "默认值": None},
            {"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 10},
            {"名称": "最大字节数", "类型": "整数型", "必填": 假, "默认值": 5242880},
            {"名称": "允许回环", "类型": "逻辑型", "必填": 假, "默认值": 假},
            {"名称": "代理", "类型": "文本型", "必填": 假, "默认值": None},
            {"名称": "SSL验证", "类型": "逻辑型", "必填": 假, "默认值": None},
            {"名称": "跟随重定向", "类型": "逻辑型", "必填": 假, "默认值": None},
            {"名称": "Cookie", "类型": "文本型", "必填": 假, "默认值": None},
        ],
        "网络通信支持库.请求.上传文件": [
            {"名称": "地址", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "文件路径", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "字段名", "类型": "文本型", "必填": 假, "默认值": "file"},
            {"名称": "额外字段", "类型": "字典型", "必填": 假, "默认值": None},
            {"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 10},
            {"名称": "允许回环", "类型": "逻辑型", "必填": 假, "默认值": 假},
            {"名称": "代理", "类型": "文本型", "必填": 假, "默认值": None},
        ],
        "网络通信支持库.请求.下载文件": [
            {"名称": "地址", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "保存路径", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "请求头", "类型": "字典型", "必填": 假, "默认值": None},
            {"名称": "超时秒", "类型": "双精度数型", "必填": 假, "默认值": 10},
            {"名称": "允许回环", "类型": "逻辑型", "必填": 假, "默认值": 假},
            {"名称": "代理", "类型": "文本型", "必填": 假, "默认值": None},
            {"名称": "SSL验证", "类型": "逻辑型", "必填": 假, "默认值": None},
        ],
        "网络通信支持库.请求.构建查询串": [
            {"名称": "参数", "类型": "字典型", "必填": 真, "默认值": None},
        ],
    }
# ===== 参数声明透传 结束 =====
    for 能力id, 函数, 返回类型 in [
        ("网络通信支持库.请求.发送请求", 发送请求, "结果型"),
        ("网络通信支持库.请求.上传文件", 上传文件, "结果型"),
        ("网络通信支持库.请求.下载文件", 下载文件, "结果型"),
        ("网络通信支持库.请求.构建查询串", 构建查询串, "结果型"),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.网络通信支持库.请求",
                实现函数=函数,
                参数=_参数契约表[能力id],
                返回=返回类型,
                说明="HTTP 客户端原子能力（外部协议边界）",
            )
        )
