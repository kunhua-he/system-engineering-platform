"""支持库.出站安全 包级中文入口。"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.网络通信支持库.出站安全.实现.出站安全 import 校验出站URL

__all__ = [
    "校验出站URL",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("网络通信支持库.出站安全.校验出站URL", 校验出站URL, [{"名称": "地址", "类型": "文本型", "必填": 真, "默认值": None, "说明": "待校验的出站 URL（http/https）"}, {"名称": "解析DNS", "类型": "逻辑型", "必填": 假, "默认值": 真, "说明": "是否对主机名做 DNS 解析后逐地址判定"}, {"名称": "DNS超时秒", "类型": "双精度数型", "必填": 假, "默认值": 10.0, "说明": "DNS 解析超时"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.网络通信支持库.出站安全",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="出站安全支持库",
            )
        )
