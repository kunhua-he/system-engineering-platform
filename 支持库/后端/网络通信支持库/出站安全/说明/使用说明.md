# 出站安全（支持库.后端.网络通信支持库.出站安全）

- 类型：支持库
- 版本：1.0.1
- 说明：出站安全支持库
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 校验出站URL（`网络通信支持库.出站安全.校验出站URL`）
- 版本：1.0.0
- 一句话说明：出站 URL 的 SSRF 防护：拒绝私有/内网/回环/保留/云元数据地址，可选 DNS 解析后逐地址判定
- 所属支持库或模块：支持库.后端.网络通信支持库.出站安全
- 参数列表：地址:文本型, 解析DNS:逻辑型, DNS超时秒:双精度数型
- 必填参数：地址
- 可选参数：解析DNS, DNS超时秒
- 返回结构：{'类型': '结果型', '值结构': {'允许': '逻辑型', '原因': '文本型', '地址': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：网络通信支持库.出站安全.校验出站URL(地址=https://example.com/a, 解析DNS=True)
- 依赖能力：无
