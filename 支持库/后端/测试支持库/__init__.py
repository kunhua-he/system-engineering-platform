"""测试支持库 包级中文入口。

三个原子能力：验证计划（`测试支持库.验证计划`）、验证命令白名单
（`测试支持库.验证命令白名单`）、验证结果判定（`测试支持库.验证结果判定`）。
"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.测试支持库.实现.测试支持库 import (
    验证计划,
    验证命令白名单,
    验证结果判定,
)

__all__ = ["验证计划", "验证命令白名单", "验证结果判定", "注册能力"]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 缺陷溯源：本入口原先手工重建注册参数声明（`参数=...` 手写字面量），只写 名称/类型/必填，
    # `默认值` 整批缺声明（2026-09-24 实测：本包 14 条契约参数中 9 条漏声明，
    # 全仓 93 条）。后果：网关按入口契约表判参时拿不到默认值 —— 「≥10 秒须走异步腿」的守卫
    # 对 超时秒≥10 的能力**不传参即被绕过**（实测 执行命令/沙箱执行命令/发送请求 等 9 条）；
    # 其余能力的默认值同样在网关侧不可见。
    # 手工表一旦漏一个参数，新参数还会被网关**静默丢弃**（同族现场见未完成事项 #197）。
    # 修法：参数声明从 `能力契约/参数契约.json` **整条透传**，入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：漂移检测的 AST 读取器
    # （`_静态字面量` 的 `ast.Subscript` 分支）只收 `注册能力` 函数体内的赋值，
    # 包一层 `_参数声明(能力id)` 会让 AST 一律判「不可静态判定」，等于拿门禁覆盖率换干净。
    # 表内布尔位写中文 真/假（真源 公共契约/基础类型/逻辑类型.py）。
    # 契约名序/返回口径核验由装配期唯一闸门承担（`运行核心/加载器/生命周期管理/管理器.装配系统`
    # → 内核 `能力注册表.校验声明一致`），本入口不再另立第二份。
    _参数契约表 = {
        "测试支持库.验证计划": [
            {"名称": "目标路径", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "关键词", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "级别", "类型": "文本型", "必填": 假, "默认值": "工作包"},
            {"名称": "仓库根目录", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "制品目录", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "最大条目数", "类型": "整数型", "必填": 假, "默认值": 50},
        ],
        "测试支持库.验证命令白名单": [
            {"名称": "命令", "类型": "列表型", "必填": 真, "默认值": None},
            {"名称": "仓库根目录", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "校验存在", "类型": "逻辑型", "必填": 假, "默认值": 真},
        ],
        "测试支持库.验证结果判定": [
            {"名称": "退出码", "类型": "整数型", "必填": 真, "默认值": None},
            {"名称": "标准输出", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "标准错误", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "命令", "类型": "列表型", "必填": 假, "默认值": None},
            {"名称": "跳过列表", "类型": "列表型", "必填": 假, "默认值": None},
        ],
    }
# ===== 参数声明透传 结束 =====

    注册表.注册(能力实现(
        能力id="测试支持库.验证计划",
        包id="支持库.后端.测试支持库",
        实现函数=验证计划,
        参数=_参数契约表["测试支持库.验证计划"],
        返回="结果",
        说明="只读计算验证计划：该跑哪些精确 unittest 模块入口与命令；绝不执行",
    ))
    注册表.注册(能力实现(
        能力id="测试支持库.验证命令白名单",
        包id="支持库.后端.测试支持库",
        实现函数=验证命令白名单,
        参数=_参数契约表["测试支持库.验证命令白名单"],
        返回="结果",
        说明="白名单准入判定：精确 unittest 模块入口 / HTML 黑盒验证器 / 唯一发布入口，其余一律拒绝",
    ))
    注册表.注册(能力实现(
        能力id="测试支持库.验证结果判定",
        包id="支持库.后端.测试支持库",
        实现函数=验证结果判定,
        参数=_参数契约表["测试支持库.验证结果判定"],
        返回="结果",
        说明="真实执行输出 → 通过/阻断；零测试成功、导入失败当跳过、未解释跳过、缓存假绿一律阻断",
    ))
