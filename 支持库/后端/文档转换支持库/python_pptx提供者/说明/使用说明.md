# python_pptx提供者（支持库.后端.文档转换支持库.python_pptx提供者）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.3
- 说明：python-pptx 独立提供者能力定义（唯一可编辑事实源）。能力列表每条与 能力定义编译器 的单能力定义结构一致：能力id/版本/中文名称/说明/参数/返回/错误码/行为/提供者。派生物（包声明/参数契约/注册入口/验证场景引用/完整性摘要）与本文件保持一致，改本文件后须同步重算。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 解析演示文稿（`内部.演示文稿.解析`）
- 版本：1.0.0
- 一句话说明：解析 pptx 为平台通用文档字典（幻灯片块{类型/文本/来源位置.幻灯片}、备注入附加字段、图像入资源列表），值结构兼容 通用文档.转字典()
- 所属支持库或模块：支持库.后端.文档转换支持库.python_pptx提供者
- 参数列表：文件路径:文本型, 格式:文本型, 最大幻灯片数:整数型, 最大字节数:整数型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：格式, 最大幻灯片数, 最大字节数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'值': '通用文档'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：文件不存在, 参数不合法, 提供者不可用, 文件损坏, 超出限制
- 调用示例：内部.演示文稿.解析(格式=, 最大幻灯片数=200, 最大字节数=209715200, 超时秒=60.0)
- 依赖能力：无
### 生成演示文稿（`内部.演示文稿.生成`）
- 版本：1.0.0
- 一句话说明：按 内容参数（幻灯片列表：标题/要点/备注）生成 pptx，返回 结果[生成产物字典]（字节b64/媒体类型/摘要/格式/字节数/诊断）
- 所属支持库或模块：支持库.后端.文档转换支持库.python_pptx提供者
- 参数列表：内容参数:字典型
- 必填参数：内容参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '生成产物'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 生成失败
- 调用示例：内部.演示文稿.生成()
- 依赖能力：无
