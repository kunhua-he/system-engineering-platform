"""PDF 文本表格：唯一实现在 支持库/适配层/pdfplumber提供者（D-2 收口，本包不放第二份）。

本文件原与 `支持库/适配层/pdfplumber提供者/实现/PDF文本表格.py` **逐字节相同**
（结构债 D-2 第 ① 组，sha256 9651df34…）。同一份逻辑只能有一个实现，故本文件
改为**转调**：让 `支持库.后端.文档转换支持库.PDF文本表格.实现.PDF文本表格`
与适配层腿那唯一实现成为**同一个模块对象**（`sys.modules[__name__] = 唯一实现`）。

为什么不直接 `import ...实现.PDF文本表格`：跨包导入 `实现/` 被
`运行核心/依赖防火墙.py` 强制拒绝（判据「跨包禁止导入 实现/ 目录」）；而适配层腿
的公开入口 `__init__.py` 已经是合规的同层导入，且它会正常加载自己的 `实现/` 子模块，
故这里先导公开入口、再把两个模块名指向同一对象（兜底路径按文件路径显式载入，
文件缺失时明确报错、不静默降级）。同一模块对象、不产生第二份实现是平台既有做法，
见 `平台控制面/授权/__init__.py`。
"""

from __future__ import annotations

import sys

import 支持库.适配层.pdfplumber提供者  # noqa: F401 —— 公开入口（同层，合规）

from 公共契约.运行时.导入前缀 import 载入唯一实现, 取根前缀, 取系统根

唯一实现名 = 取根前缀(__name__) + "支持库.适配层.pdfplumber提供者.实现.PDF文本表格"
系统根 = 取系统根(__file__)

载入唯一实现(唯一实现名, 系统根 / "支持库" / "适配层" / "pdfplumber提供者" / "实现" / "PDF文本表格.py")

sys.modules[__name__] = sys.modules[唯一实现名]
