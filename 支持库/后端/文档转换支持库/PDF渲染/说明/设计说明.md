# PyMuPDF提供者 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.后端.文档转换支持库.PDF渲染
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 文档转换支持库.PDF渲染.检测加密页数 | 检测加密页数 | 1.0.0 | 结果型 | fitz 打开 PDF：返回 {已加密: bool, 页数: int}；疑似加密 → 文件加密 |
| 文档转换支持库.PDF渲染.渲染整页 | 渲染整页 | 1.0.0 | 结果型 | fitz 整页渲染为图像字节 base64 |
| 文档转换支持库.PDF渲染.提取图像 | 提取图像 | 1.0.0 | 结果型 | 提取页面内嵌图像：返回 [{xref, 字节b64, 尺寸}] |
| 文档转换支持库.PDF渲染.校验PDF | 校验PDF | 1.0.0 | 结果型 | fitz 重新打开 PDF 验证：返回 {页数: int} |

## 文档转换支持库.PDF渲染.检测加密页数（检测加密页数）

**说明**：fitz 打开 PDF：返回 {已加密: bool, 页数: int}；疑似加密 → 文件加密

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | PDF 文件绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 参数不合法 |
| 提供者不可用 |
| 文件损坏 |
| 文件加密 |
| 超时 |
| 提供者崩溃 |

**调用示例**：

```python
文档转换支持库.PDF渲染.检测加密页数(文件路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文档转换支持库.PDF渲染.渲染整页（渲染整页）

**说明**：fitz 整页渲染为图像字节 base64

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | PDF 文件绝对路径 |
| 页序号 | 整数型 | 是 | None | 从 1 开始的页序号 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 参数不合法 |
| 提供者不可用 |
| 文件损坏 |
| 超出限制 |
| 超时 |
| 提供者崩溃 |

**调用示例**：

```python
文档转换支持库.PDF渲染.渲染整页(文件路径=值, 页序号=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文档转换支持库.PDF渲染.提取图像（提取图像）

**说明**：提取页面内嵌图像：返回 [{xref, 字节b64, 尺寸}]

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | PDF 文件绝对路径 |
| 页序号 | 整数型 | 是 | None | 从 1 开始的页序号 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 参数不合法 |
| 提供者不可用 |
| 文件损坏 |
| 超出限制 |
| 超时 |
| 提供者崩溃 |

**调用示例**：

```python
文档转换支持库.PDF渲染.提取图像(文件路径=值, 页序号=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文档转换支持库.PDF渲染.校验PDF（校验PDF）

**说明**：fitz 重新打开 PDF 验证：返回 {页数: int}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | PDF 字节内容 |
| 超时秒 | 双精度数型 | 否 | 30 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |

**调用示例**：

```python
文档转换支持库.PDF渲染.校验PDF(字节=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
