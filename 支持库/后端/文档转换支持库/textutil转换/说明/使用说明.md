# textutil提供者（支持库.后端.文档转换支持库.textutil转换）

- 类型：支持库
- 版本：1.0.3
- 说明：textutil 系统能力独立提供者：文本格式转换（txt/html/rtf/doc/docx 互转、提取文本）。macOS 系统能力，受控子进程调用。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查提供者（`文档转换支持库.textutil转换.检查提供者`）
- 版本：1.0.0
- 一句话说明：检查 textutil 是否可用（真实独立进程探针 textutil -help + macOS 版本）；缺失 → 提供者不可用
- 所属支持库或模块：支持库.后端.文档转换支持库.textutil转换
- 参数列表：超时秒:双精度数型
- 必填参数：无
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：提供者不可用
- 调用示例：文档转换支持库.textutil转换.检查提供者(超时秒=30)
- 依赖能力：无
### 转换文本文件（`文档转换支持库.textutil转换.转换文本文件`）
- 版本：1.0.0
- 一句话说明：textutil 转换：输入文件 → 目标格式（txt/html/rtf/doc/docx 互转）；返回 文本内容
- 所属支持库或模块：支持库.后端.文档转换支持库.textutil转换
- 参数列表：输入路径:文本型, 目标格式:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 输出目录:文本型
- 必填参数：输入路径, 目标格式
- 可选参数：超时秒, 最大输出字节, 输出目录
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 转换失败, 超时, 超出限制
- 调用示例：文档转换支持库.textutil转换.转换文本文件(输入路径=/tmp/示例文档.rtf, 目标格式=txt, 超时秒=60, 最大输出字节=209715200)
- 依赖能力：无
