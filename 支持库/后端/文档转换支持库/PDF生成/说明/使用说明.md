# reportlab提供者（支持库.后端.文档转换支持库.PDF生成）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.2
- 说明：reportlab PDF 生成提供者：第三方库隔离到一次性子进程执行（主进程不加载 reportlab），渲染唯一实现在 支持库.适配层.reportlab提供者，本包只做隔离边界与结果归一；中文字体优先系统字体回退 CID。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 生成PDF（`文档转换支持库.PDF生成.生成PDF`）
- 版本：1.0.0
- 一句话说明：按 内容参数（标题/段落列表/表格列表）生成 PDF，返回 结果[生成产物字典]（字节b64/媒体类型/摘要/字节数/诊断）
- 所属支持库或模块：支持库.后端.文档转换支持库.PDF生成
- 参数列表：内容参数:字典型
- 必填参数：内容参数
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 生成失败
- 调用示例：文档转换支持库.PDF生成.生成PDF(内容参数={'标题': '示例报告', '段落列表': ['第一段正文'], '表格列表': [{'表头': ['姓名', '数量'], '行': [['张三', '1']]}]})
- 依赖能力：无
