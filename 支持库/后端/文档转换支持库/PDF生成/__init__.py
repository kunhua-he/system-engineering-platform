"""reportlab 提供者包级中文入口（reportlab 隔离在一次性子进程中执行）。

调用者只从此入口导入，禁止深入 实现/ 目录。
公开能力：文档转换支持库.PDF生成.生成PDF（标题/段落列表/表格列表 → 生成产物字典）。
主进程**不 import reportlab**：由 实现/子进程入口.py 在独立子进程内经
支持库.适配层.reportlab提供者 公开入口调用（唯一实现，本包不再放第二份渲染代码）；
中文字体优先系统字体，回退内置 CID 字体。
"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.文档转换支持库.PDF生成.实现.生成PDF import 生成PDF

__all__ = ["生成PDF", "注册能力"]


def 注册能力(注册表) -> None:
    """由支持库加载器调用，向能力注册表注册本库能力。"""
    from 公共契约.能力契约.契约 import 能力实现

    注册表.注册(
        能力实现(
            能力id="文档转换支持库.PDF生成.生成PDF",
            包id="支持库.后端.文档转换支持库.PDF生成",
            实现函数=生成PDF,
            参数=[{"名称": "内容参数", "类型": "字典型", "必填": 真, "默认值": None}],
            返回="结果型",
            说明="按 内容参数（标题/段落列表/表格列表）生成 PDF，返回 结果[生成产物字典]（字节b64/媒体类型/摘要/字节数/诊断）",
        )
    )
