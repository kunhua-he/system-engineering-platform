# FFmpeg提供者（支持库.后端.媒体处理支持库.FFmpeg媒体）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.1.4
- 说明：FFmpeg 外部命令独立提供者：ffprobe 媒体探测（时长/流/格式）、ffmpeg 转码/音轨抽取/抽帧。独立进程组+超时+取消+输出上限截断+临时文件零残留；缺少外部命令返回 提供者不可用（未配置），不伪装成功。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查提供者（`媒体处理支持库.FFmpeg媒体.检查提供者`）
- 版本：1.0.1
- 一句话说明：检查 ffmpeg/ffprobe 是否可用（真实版本探针），返回版本；缺失 → 提供者不可用
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：超时秒:双精度数型
- 必填参数：无
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法、提供者不可用
- 调用示例：媒体处理支持库.FFmpeg媒体.检查提供者(超时秒=30.0)
- 依赖能力：无
### 探测媒体（`媒体处理支持库.FFmpeg媒体.探测媒体`）
- 版本：1.0.0
- 一句话说明：ffprobe 探测媒体：返回 {时长秒, 流列表, 格式, 大小字节, 比特率}；损坏 → 损坏媒体
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：文件路径:文本型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超时, 取消, 进程崩溃
- 调用示例：媒体处理支持库.FFmpeg媒体.探测媒体(文件路径=/tmp/示例媒体.mp4, 超时秒=60.0)
- 依赖能力：无
### 提取音频（`媒体处理支持库.FFmpeg媒体.提取音频`）
- 版本：1.0.0
- 一句话说明：从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac）：返回 {字节b64, 格式, 字节数}；无音轨 → 无音轨；超长 → 超长媒体
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：文件路径:文本型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 无音轨, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理支持库.FFmpeg媒体.提取音频(文件路径=/tmp/示例媒体.mp4, 输出格式=wav, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：无
### 转码（`媒体处理支持库.FFmpeg媒体.转码`）
- 版本：1.0.0
- 一句话说明：ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：文件路径:文本型, 输出格式:文本型, 编码选项:字典型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型, 返回字节:逻辑型
- 必填参数：文件路径
- 可选参数：输出格式, 编码选项, 输出路径, 超时秒, 最大输出字节, 最大时长秒, 返回字节
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理支持库.FFmpeg媒体.转码(文件路径=/tmp/示例媒体.mp4, 输出格式=mp4, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：无
### 抽取帧（`媒体处理支持库.FFmpeg媒体.抽取帧`）
- 版本：1.0.0
- 一句话说明：ffmpeg 抽帧（jpg/png，时间点秒）：返回 {字节b64, 格式, 宽度, 高度}；宽度/高度为输出帧经 ffprobe 受控探测链得到的真实尺寸（禁止固定值）
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：文件路径:文本型, 时间点秒:双精度数型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径, 时间点秒
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理支持库.FFmpeg媒体.抽取帧(文件路径=/tmp/示例媒体.mp4, 时间点秒=1.0, 输出格式=jpg, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：无
### 截取音频（`媒体处理支持库.FFmpeg媒体.截取音频`）
- 版本：1.1.0
- 一句话说明：ffmpeg 按时间区间截取音频（复核区间用）：-ss 前置快速定位 + -t 限长；返回 {字节b64, 格式, 字节数, 输出路径}；无音轨 → 无音轨；区间不合法 → 参数不合法
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：文件路径:文本型, 开始秒:双精度数型, 结束秒:双精度数型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径, 开始秒, 结束秒
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 无音轨, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理支持库.FFmpeg媒体.截取音频(文件路径=/abs/直播.mp4, 开始秒=600.0, 结束秒=780.0, 输出格式=mp3, 输出路径=/abs/缓存/区间_0001.mp3, 超时秒=120.0)
- 依赖能力：无
### 拼接媒体（`媒体处理支持库.FFmpeg媒体.拼接媒体`）
- 版本：1.0.0
- 一句话说明：按顺序无损拼接多个同源分片成一个文件：返回 {格式, 字节数, 输出路径}（可选 字节b64）；实现为 ffmpeg concat demuxer + -c copy，不重编码 → 秒级完成、零画质损失、不吃 GPU；分片编码参数不一致时直接失败（进程崩溃），不产出看似成功的坏文件。典型用法：直播录制分片（同一档位）拼成整场，供逐字稿精校按整场处理
- 所属支持库或模块：支持库.后端.媒体处理支持库.FFmpeg媒体
- 参数列表：文件列表:列表型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 返回字节:逻辑型
- 必填参数：文件列表
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 返回字节
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理支持库.FFmpeg媒体.拼接媒体(输出格式=mp4, 超时秒=120, 最大输出字节=209715200, 返回字节=True)
- 依赖能力：无
