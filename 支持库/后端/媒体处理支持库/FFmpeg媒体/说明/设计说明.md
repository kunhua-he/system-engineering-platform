# FFmpeg提供者 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.后端.媒体处理支持库.FFmpeg媒体
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 媒体处理支持库.FFmpeg媒体.检查提供者 | 检查提供者 | 1.0.0 | 结果型 | 检查 ffmpeg/ffprobe 是否可用（真实版本探针），返回版本；缺失 → 提供者不可用 |
| 媒体处理支持库.FFmpeg媒体.探测媒体 | 探测媒体 | 1.0.0 | 结果型 | ffprobe 探测媒体：返回 {时长秒, 流列表, 格式, 大小字节, 比特率}；损坏 → 损坏媒体 |
| 媒体处理支持库.FFmpeg媒体.提取音频 | 提取音频 | 1.0.0 | 结果型 | 从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac）：返回 {字节b64, 格式, 字节数}；无音轨 → 无音轨；超长 → 超长媒体 |
| 媒体处理支持库.FFmpeg媒体.转码 | 转码 | 1.0.0 | 结果型 | ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编 |
| 媒体处理支持库.FFmpeg媒体.抽取帧 | 抽取帧 | 1.0.0 | 结果型 | ffmpeg 抽帧（jpg/png，时间点秒）：返回 {字节b64, 格式, 宽度, 高度}；宽度/高度为输出帧经 ffprobe 受控探测链得到的真实尺寸（禁止固定值） |
| 媒体处理支持库.FFmpeg媒体.截取音频 | 截取音频 | 1.1.0 | 结果型 | ffmpeg 按时间区间截取音频（复核区间用）：-ss 前置快速定位 + -t 限长；返回 {字节b64, 格式, 字节数, 输出路径}；无音轨 → 无音轨；区间不合法 → 参数不合法 |

## 媒体处理支持库.FFmpeg媒体.检查提供者（检查提供者）

**说明**：检查 ffmpeg/ffprobe 是否可用（真实版本探针），返回版本；缺失 → 提供者不可用

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 30 | 版本探针超时秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 提供者不可用 |

**调用示例**：

```python
媒体处理支持库.FFmpeg媒体.检查提供者(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体处理支持库.FFmpeg媒体.探测媒体（探测媒体）

**说明**：ffprobe 探测媒体：返回 {时长秒, 流列表, 格式, 大小字节, 比特率}；损坏 → 损坏媒体

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 探测超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |

**调用示例**：

```python
媒体处理支持库.FFmpeg媒体.探测媒体(文件路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体处理支持库.FFmpeg媒体.提取音频（提取音频）

**说明**：从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac）：返回 {字节b64, 格式, 字节数}；无音轨 → 无音轨；超长 → 超长媒体

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件绝对路径 |
| 输出格式 | 文本型 | 否 | wav | wav/mp3/aac/m4a/ogg/flac |
| 输出路径 | 文本型 | 否 | None | 可选输出文件绝对路径（默认临时文件自动清理） |
| 超时秒 | 双精度数型 | 否 | 60 | 提取超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
| 返回字节 | 逻辑型 | 否 | True | 是否随返回值带回 base64 字节；已落盘的大文件建议否（避免 4/3 倍放大） |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 无音轨 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |

**调用示例**：

```python
媒体处理支持库.FFmpeg媒体.提取音频(文件路径=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体处理支持库.FFmpeg媒体.转码（转码）

**说明**：ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件绝对路径 |
| 输出格式 | 文本型 | 否 | mp4 | mp4/mkv/webm/mov/avi |
| 编码选项 | 字典型 | 否 | None | 白名单键值：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；硬编压缩推荐：视频编码器=hevc_videotoolbox、视频码率=900k、码率上限=1100k、缓冲=2200k、帧率=15、视频标签=hvc1、音频编码器=copy |
| 输出路径 | 文本型 | 否 | None | 可选输出文件绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 转码超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
| 返回字节 | 逻辑型 | 否 | True | 是否随返回值带回 base64 字节；已落盘的大文件建议否（避免 4/3 倍放大） |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |

**调用示例**：

```python
媒体处理支持库.FFmpeg媒体.转码(文件路径=值, 输出格式=值, 编码选项=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体处理支持库.FFmpeg媒体.抽取帧（抽取帧）

**说明**：ffmpeg 抽帧（jpg/png，时间点秒）：返回 {字节b64, 格式, 宽度, 高度}；宽度/高度为输出帧经 ffprobe 受控探测链得到的真实尺寸（禁止固定值）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件绝对路径 |
| 时间点秒 | 双精度数型 | 是 | None | 抽帧时间点（≥0） |
| 输出格式 | 文本型 | 否 | jpg | jpg/png |
| 输出路径 | 文本型 | 否 | None | 可选输出文件绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 抽帧超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
| 返回字节 | 逻辑型 | 否 | True | 是否随返回值带回 base64 字节；已落盘的大文件建议否（避免 4/3 倍放大） |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |

**调用示例**：

```python
媒体处理支持库.FFmpeg媒体.抽取帧(文件路径=值, 时间点秒=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体处理支持库.FFmpeg媒体.截取音频（截取音频）

**说明**：ffmpeg 按时间区间截取音频（复核区间用）：-ss 前置快速定位 + -t 限长；返回 {字节b64, 格式, 字节数, 输出路径}；无音轨 → 无音轨；区间不合法 → 参数不合法

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件绝对路径 |
| 开始秒 | 双精度数型 | 是 | None | 截取起始秒（≥0） |
| 结束秒 | 双精度数型 | 是 | None | 截取结束秒（必须大于 开始秒） |
| 输出格式 | 文本型 | 否 | wav | wav/mp3/aac/m4a/ogg/flac |
| 输出路径 | 文本型 | 否 | None | 可选输出文件绝对路径（默认临时文件自动清理） |
| 超时秒 | 双精度数型 | 否 | 60 | 截取超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
| 返回字节 | 逻辑型 | 否 | True | 是否随返回值带回 base64 字节；已落盘的大文件建议否（避免 4/3 倍放大） |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 无音轨 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |

**调用示例**：

```python
媒体处理支持库.FFmpeg媒体.截取音频(文件路径=值, 开始秒=值, 结束秒=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：FFmpeg媒体.本机受管真实媒体（真实媒体链）

## 并发容量与吞吐基线（2026-09-16 本机实测）

> **实测容量特性**，用于估算批量任务的并发上限与完成时间；换机器需重新实测。

| 项 | 实测值 | 说明 |
|---|---|---|
| **并发上限（VideoToolbox 会话数）** | **20 路** | 23 路（含 2 个孤儿）→ 380+ 次失败；**32 路 → 399+ 次失败**；均为 `ffmpeg 执行失败：外部命令异常退出（退出码 187）: return code -22 (Invalid argument)`。**关键：32 路时 CPU 仅 23%、`GPU Device Utilization` 仅 54% 仍报错** → 限制来自**硬件编码器的并发会话数量**（约 20 个），**不是算力过载**。CPU/GPU 有空闲 ≠ 能提并发 |
| 20 路吞吐 | **141.4 GB/h**（26.4 分钟采样，按源字节计） | 存档档参数：`hevc_videotoolbox` + 目标码率 = 源×0.35 夹 [500k, 900k] + `-r 15` + `-c:a copy` |
| 单路 CPU 占用 | 约 43%（20 路合计约 853%） | 硬编主要吃媒体引擎，CPU 占用不高 |
| 压缩率 | 63%–90%（源码率越高省得越多） | 源 1–2 Mbps 省 60%+；源 6–8 Mbps 省 85%+ |
| 并发边际收益 | 6→14 路仅快 27% | 说明 14 路时媒体引擎已接近饱和 |

**实践结论**：

1. 批量转码并发定 **20 路**；多任务共享该额度时用 16+4 之类的划分，**总和不要超过 20**——超过的部分会被网关限流拒绝（默认单维度并发 20），或因 **VideoToolbox 会话数超限**触发 `-22` 失败。**不要用 CPU/GPU 占用率判断"还有余量"**：实测 32 路时 CPU 23%、GPU 54% 仍在报错，因为硬编走专用媒体引擎、不走 CPU，空闲的 CPU 核帮不上硬编。（若要用起空闲 CPU，只能另配软编（libx265）并行，但软编仅 1–2x 实时、远慢于硬编 5–6x，且码率控制口径不同，收益有限。）
2. 估算完成时间用 **141.4 GB/h**。
3. **网关重启会在途 ffmpeg 变孤儿**（PPID=1 被 launchd 收养）继续占用媒体引擎，使实际并发超过配置值并触发 `-22`；发现 `ps` 里 ffmpeg 数多于配置时，先查孤儿（`ps ax -o pid,ppid,command | grep ffmpeg | awk '$2==1'`）。按状态机登记归属回收的修法见 `开发文档/未完成事项.md`。
