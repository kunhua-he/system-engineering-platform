# 文档生成 · 目录树与命令

> 包id：`支持库.后端.办公文档支持库.文档生成`；版本：`1.0.3`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 目录树

```text
文档生成/
├── __init__.py
├── 依赖契约/依赖契约.json
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/文档生成.py
├── 实现/签名校验.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
├── 默认配置/默认配置.json
```

## 公开命令

### `文档生成.生成DOCX`

- 名称：生成DOCX
- 返回：`结果型`
- 说明：按内容块列表（标题/段落/表格/分页）生成 DOCX，返回 结果[生成产物]
- 参数：
  - `参数`；任意
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文档生成.生成XLSX`

- 名称：生成XLSX
- 返回：`结果型`
- 说明：按工作表列表（表名/列/行）生成 XLSX，返回 结果[生成产物]
- 参数：
  - `参数`；任意
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文档生成.生成PPTX`

- 名称：生成PPTX
- 返回：`结果型`
- 说明：按幻灯片列表（标题/要点/备注）生成 PPTX，返回 结果[生成产物]
- 参数：
  - `参数`；任意
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文档生成.生成PDF`

- 名称：生成PDF
- 返回：`结果型`
- 说明：按内容块列表生成 PDF（中文 STSong/PingFang 注册），返回 结果[生成产物]
- 参数：
  - `参数`；任意
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文档生成.校验签名`

- 名称：校验签名
- 返回：`结果型`
- 说明：校验生成产物签名：OOXML 有效 ZIP 且含必要成员；PDF 头尾完整且可重新打开
- 参数：
  - `格式`；文本型
  - `字节集型`；任意
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
