# 文档生成 使用说明

- 包id：支持库.后端.办公文档支持库.文档生成
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 办公文档支持库.文档生成.生成DOCX | 生成DOCX | 1.0.0 | {'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}} | 按内容块列表生成 DOCX，返回 结果[生成产物]（经受管提供者 python_docx提供者 生成） |
| 办公文档支持库.文档生成.生成XLSX | 生成XLSX | 1.0.0 | {'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}} | 按工作表列表生成 XLSX，返回 结果[生成产物]（经受管提供者 openpyxl提供者 生成） |
| 办公文档支持库.文档生成.生成PPTX | 生成PPTX | 1.0.0 | {'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}} | 按幻灯片列表生成 PPTX，返回 结果[生成产物]（经受管提供者 python_pptx提供者 生成） |
| 办公文档支持库.文档生成.生成PDF | 生成PDF | 1.0.0 | {'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}} | 按内容块列表生成 PDF，返回 结果[生成产物]（经受管提供者 reportlab提供者 生成） |
| 办公文档支持库.文档生成.校验签名 | 校验签名 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '诊断文本列表（成功）'}} | 校验生成产物签名：OOXML 检查有效 ZIP 与必要成员；PDF 检查头尾并经受管提供者 PDF隔离提供者.校验PDF 重开 |
| 办公文档支持库.文档生成.合并文本文件 | 合并文本文件 |  | {输出路径, 合并文件数, 总字节数} | 合并多个文本文件到输出文件 |
| 办公文档支持库.文档生成.文档统计 | 文档统计 |  | {路径, 行数, 字数, 字符数, 字节数} | 统计文档行数/字数/字符数/字节数 |

## 办公文档支持库.文档生成.生成DOCX（生成DOCX）

**说明**：按内容块列表生成 DOCX，返回 结果[生成产物]（经受管提供者 python_docx提供者 生成）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 参数 | 字典型 | 是 | None | 生成参数：内容块列表（标题/段落/表格/分页），为空返回 参数不合法 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 生成失败 |

**调用示例**：

```python
办公文档支持库.文档生成.生成DOCX(参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档生成.生成XLSX（生成XLSX）

**说明**：按工作表列表生成 XLSX，返回 结果[生成产物]（经受管提供者 openpyxl提供者 生成）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 参数 | 字典型 | 是 | None | 生成参数：工作表列表（表名/列/行），为空返回 参数不合法 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 生成失败 |

**调用示例**：

```python
办公文档支持库.文档生成.生成XLSX(参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档生成.生成PPTX（生成PPTX）

**说明**：按幻灯片列表生成 PPTX，返回 结果[生成产物]（经受管提供者 python_pptx提供者 生成）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 参数 | 字典型 | 是 | None | 生成参数：幻灯片列表（标题/要点/备注），为空返回 参数不合法 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 生成失败 |

**调用示例**：

```python
办公文档支持库.文档生成.生成PPTX(参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档生成.生成PDF（生成PDF）

**说明**：按内容块列表生成 PDF，返回 结果[生成产物]（经受管提供者 reportlab提供者 生成）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 参数 | 字典型 | 是 | None | 生成参数：内容块列表（标题/段落/表格/分页），为空返回 参数不合法 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 生成失败 |

**调用示例**：

```python
办公文档支持库.文档生成.生成PDF(参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档生成.校验签名（校验签名）

**说明**：校验生成产物签名：OOXML 检查有效 ZIP 与必要成员；PDF 检查头尾并经受管提供者 PDF隔离提供者.校验PDF 重开

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 格式 | 文本型 | 是 | None | 产物格式（docx/xlsx/pptx/pdf；其他格式返回 参数不合法） |
| 字节 | 字节集型 | 是 | None | 产物字节内容（非二进制返回 参数不合法） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '诊断文本列表（成功）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 生成失败 |

**调用示例**：

```python
办公文档支持库.文档生成.校验签名(格式=值, 字节=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档生成.合并文本文件（合并文本文件）

**说明**：合并多个文本文件到输出文件

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件列表 | 列表型 | 是 | None | 文本文件路径列表 |
| 输出路径 | 文本型 | 是 | None | 输出文件路径 |
| 分隔符 | 文本型 | 否 | \n | 文件间分隔符 |

**返回结构**：{输出路径, 合并文件数, 总字节数}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |
| , |
| 合 |
| 并 |
| 失 |
| 败 |

**调用示例**：

```python
办公文档支持库.文档生成.合并文本文件(文件列表=值, 输出路径=值, 分隔符=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档生成.文档统计（文档统计）

**说明**：统计文档行数/字数/字符数/字节数

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文档路径 |

**返回结构**：{路径, 行数, 字数, 字符数, 字节数}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参 |
| 数 |
| 不 |
| 合 |
| 法 |
| , |
| 文 |
| 件 |
| 不 |
| 存 |
| 在 |
| , |
| 统 |
| 计 |
| 失 |
| 败 |

**调用示例**：

```python
办公文档支持库.文档生成.文档统计(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
