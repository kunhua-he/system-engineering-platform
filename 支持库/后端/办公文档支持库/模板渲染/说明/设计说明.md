# 模板渲染 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.办公文档支持库.模板渲染
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 办公文档支持库.模板渲染.渲染模板 | 渲染模板 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 按变量字典渲染模板占位符（{{变量名}}） |
| 办公文档支持库.模板渲染.提取变量名 | 提取变量名 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 提取模板中的全部变量名 |

## 办公文档支持库.模板渲染.渲染模板（渲染模板）

**说明**：按变量字典渲染模板占位符（{{变量名}}）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模板 | 文本型 | 是 | None | 模板文本 |
| 变量 | 字典型 | 否 | None | 变量表 |
| 缺失行为 | 文本型 | 否 | 保留原文 | 缺失变量处理 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 变量缺失 |

**调用示例**：

```python
办公文档支持库.模板渲染.渲染模板(模板=值, 变量=值, 缺失行为=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.模板渲染.提取变量名（提取变量名）

**说明**：提取模板中的全部变量名

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 模板 | 文本型 | 是 | None | 模板文本 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
办公文档支持库.模板渲染.提取变量名(模板=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
