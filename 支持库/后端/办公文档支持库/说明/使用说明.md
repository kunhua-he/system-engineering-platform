# 办公文档支持库（支持库.后端.办公文档支持库）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：2.0.2
- 说明：聚合支持库，包含 8 个子域：PDF文档/文字文档/文档生成/模板渲染/演示文稿/表格公式/表格文档/轻量文本解析
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 解析PDF（`办公文档支持库.PDF文档.解析PDF`）
- 版本：2.0.2
- 一句话说明：解析 PDF 为平台通用文档（文本/表格/图像，经 PDF隔离提供者 隔离子进程执行），返回 结果[通用文档]
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文件路径:文本型, 最大页数:整数型, 最大字节数:整数型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：最大页数, 最大字节数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'值': '通用文档（块列表/资源列表/元数据）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.PDF文档.解析PDF()
- 依赖能力：无
### 解析文字文档（`办公文档支持库.文字文档.解析文字文档`）
- 版本：2.0.2
- 一句话说明：解析 DOC/DOCX 文字文档为平台通用文档（段落/表格/图像资源）；DOCX 原生经 python_docx提供者 解析，DOC 先经 LibreOffice 转换再解析
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文件路径:文本型, 格式:文本型, 最大字节数:整数型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：格式, 最大字节数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'值': '通用文档（块列表/资源列表/元数据）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文字文档.解析文字文档()
- 依赖能力：无
### 生成DOCX（`办公文档支持库.文档生成.生成DOCX`）
- 版本：2.0.2
- 一句话说明：按内容块列表生成 DOCX，返回 结果[生成产物]（经受管提供者 python_docx提供者 生成）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：参数:字典型
- 必填参数：参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.生成DOCX()
- 依赖能力：无
### 生成XLSX（`办公文档支持库.文档生成.生成XLSX`）
- 版本：2.0.2
- 一句话说明：按工作表列表生成 XLSX，返回 结果[生成产物]（经受管提供者 openpyxl提供者 生成）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：参数:字典型
- 必填参数：参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.生成XLSX()
- 依赖能力：无
### 生成PPTX（`办公文档支持库.文档生成.生成PPTX`）
- 版本：2.0.2
- 一句话说明：按幻灯片列表生成 PPTX，返回 结果[生成产物]（经受管提供者 python_pptx提供者 生成）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：参数:字典型
- 必填参数：参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.生成PPTX()
- 依赖能力：无
### 生成PDF（`办公文档支持库.文档生成.生成PDF`）
- 版本：2.0.2
- 一句话说明：按内容块列表生成 PDF，返回 结果[生成产物]（经受管提供者 reportlab提供者 生成）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：参数:字典型
- 必填参数：参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '生成产物（格式/字节/媒体类型/摘要/诊断/附加）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.生成PDF()
- 依赖能力：无
### 校验签名（`办公文档支持库.文档生成.校验签名`）
- 版本：2.0.2
- 一句话说明：校验生成产物签名：OOXML 检查有效 ZIP 与必要成员；PDF 检查头尾并经受管提供者 PDF隔离提供者.校验PDF 重开
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：格式:文本型, 字节:字节集型
- 必填参数：格式, 字节
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '诊断文本列表（成功）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.校验签名()
- 依赖能力：无
### 合并文本文件（`办公文档支持库.文档生成.合并文本文件`）
- 版本：2.0.2
- 一句话说明：合并多个文本文件到输出文件
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文件列表:列表型, 输出路径:文本型, 分隔符:文本型
- 必填参数：文件列表, 输出路径
- 可选参数：分隔符
- 返回结构：{'类型': '结果型', '值结构': {'输出路径': '文本型', '合并文件数': '整数型', '总字节数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.合并文本文件()
- 依赖能力：无
### 文档统计（`办公文档支持库.文档生成.文档统计`）
- 版本：2.0.2
- 一句话说明：统计文档行数/字数/字符数/字节数
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'路径': '文本型', '行数': '整数型', '字数': '整数型', '字符数': '整数型', '字节数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.文档生成.文档统计()
- 依赖能力：无
### 渲染模板（`办公文档支持库.模板渲染.渲染模板`）
- 版本：2.0.2
- 一句话说明：按变量字典渲染模板占位符（{{变量名}}）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：模板:文本型, 变量:字典型, 缺失行为:文本型
- 必填参数：模板
- 可选参数：变量, 缺失行为
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.模板渲染.渲染模板()
- 依赖能力：无
### 提取变量名（`办公文档支持库.模板渲染.提取变量名`）
- 版本：2.0.2
- 一句话说明：提取模板中的全部变量名
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：模板:文本型
- 必填参数：模板
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.模板渲染.提取变量名()
- 依赖能力：无
### 解析演示文稿（`办公文档支持库.演示文稿.解析演示文稿`）
- 版本：2.0.2
- 一句话说明：解析 PPT/PPTX 演示文稿为平台通用文档（幻灯片文本/备注/图像资源）；PPTX 原生经 python_pptx提供者 解析，PPT 先经 LibreOffice 转换再解析
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文件路径:文本型, 格式:文本型, 最大幻灯片数:整数型, 最大字节数:整数型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：格式, 最大幻灯片数, 最大字节数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'值': '通用文档（块列表/资源列表/提供者版本/原始文件摘要）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.演示文稿.解析演示文稿()
- 依赖能力：无
### 求值公式（`办公文档支持库.表格公式.求值公式`）
- 版本：2.0.2
- 一句话说明：对 Excel 公式求值：非 = 开头原样返回；支持 SUM/AVERAGE/COUNT/MAX/MIN 与四则运算，AST 白名单求值禁用 eval；错误值沿用 Excel 语义（#VALUE!/#DIV/0!/#NAME?）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：表达式:文本型, 单元格字典:字典型
- 必填参数：表达式, 单元格字典
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'结果文本': '求值结果文本；错误值沿用 Excel 语义（#VALUE!/#DIV/0!/#NAME?）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.求值公式()
- 依赖能力：无
### 解析单元格地址（`办公文档支持库.表格公式.解析单元格地址`）
- 版本：2.0.2
- 一句话说明：解析 A1 样式地址为行列：行 1 起、列 0 起；非法地址返回 0/0（与旧实现一致）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：地址:文本型
- 必填参数：地址
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'行': '行号（1 起）', '列': '列号（0 起）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.解析单元格地址()
- 依赖能力：无
### 格式化单元格地址（`办公文档支持库.表格公式.格式化单元格地址`）
- 版本：2.0.2
- 一句话说明：把（行 1 起、列 0 起）格式化为 A1 样式地址
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：行:整数型, 列:整数型
- 必填参数：行, 列
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'地址': 'A1 样式地址文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.格式化单元格地址()
- 依赖能力：无
### 格式化列字母（`办公文档支持库.表格公式.格式化列字母`）
- 版本：2.0.2
- 一句话说明：把列序号（0 起）格式化为列字母：0 → A、26 → AA
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：列:整数型
- 必填参数：列
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'列字母': '列字母文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.格式化列字母()
- 依赖能力：无
### 列字母转索引（`办公文档支持库.表格公式.列字母转索引`）
- 版本：2.0.2
- 一句话说明：把列字母转为列序号（0 起）：A → 0、AA → 26
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：列字母:文本型
- 必填参数：列字母
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'索引': '列号（0 起）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.列字母转索引()
- 依赖能力：无
### 解析单元格范围（`办公文档支持库.表格公式.解析单元格范围`）
- 版本：2.0.2
- 一句话说明：解析 'A1:B3' 为四角行列；非两段写法按单地址处理（左上右下相同）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：范围:文本型
- 必填参数：范围
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'左上行': '左上角行号（1 起）', '左上列': '左上角列号（0 起）', '右下行': '右下角行号（1 起）', '右下列': '右下角列号（0 起）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.解析单元格范围()
- 依赖能力：无
### 展开单元格范围（`办公文档支持库.表格公式.展开单元格范围`）
- 版本：2.0.2
- 一句话说明：把 'A1:C3' 展开为范围内全部单元格地址（先行后列）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：范围:文本型
- 必填参数：范围
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'地址列表': '范围内全部单元格地址列表（先行后列）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.展开单元格范围()
- 依赖能力：无
### 批量格式化单元格地址（`办公文档支持库.表格公式.批量格式化单元格地址`）
- 版本：2.0.2
- 一句话说明：把 (行,列) 列表批量格式化为 A1 样式地址，返回与输入同序同长的地址列表；逐项复用 格式化单元格地址，非法项语义与单次调用完全一致，供调用方把循环内逐格转换外提为一次调用
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：行列列表:列表型
- 必填参数：行列列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'地址列表': '与输入同序同长的 A1 样式地址列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.批量格式化单元格地址()
- 依赖能力：无
### 批量解析单元格地址（`办公文档支持库.表格公式.批量解析单元格地址`）
- 版本：2.0.2
- 一句话说明：把 A1 地址列表批量解析为行列列表，返回与输入同序同长的结果列表；逐项复用 解析单元格地址，非法地址按原函数语义产出 0/0（与单次调用一致）
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：地址列表:列表型
- 必填参数：地址列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'解析列表': '与输入同序同长的行列字典列表，每项含 行 与 列'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格公式.批量解析单元格地址()
- 依赖能力：无
### 解析表格文档（`办公文档支持库.表格文档.解析表格文档`）
- 版本：2.0.2
- 一句话说明：解析 XLS/XLSX 表格文档为平台通用文档（工作表/单元格，公式值策略）；XLSX 原生经 openpyxl提供者 解析，XLS 先经 LibreOffice 转换再解析
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文件路径:文本型, 格式:文本型, 最大工作表数:整数型, 最大行数:整数型, 最大字节数:整数型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：格式, 最大工作表数, 最大行数, 最大字节数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'值': '通用文档（块列表/提供者版本/原始文件摘要）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.表格文档.解析表格文档()
- 依赖能力：无
### 解析Markdown块（`办公文档支持库.轻量文本解析.解析Markdown块`）
- 版本：2.0.2
- 一句话说明：把 Markdown 文本切成中间块列表：语法集「完整」识别 8 类块（标题/段落/代码/表格/列表项/引用/分隔线/图像），语法集「简化」只识别 3 类块（标题/段落/代码）。行号自 1 起；空文本按语法集产出占位或不产出
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文本:文本型, 语法集:文本型
- 必填参数：文本
- 可选参数：语法集
- 返回结构：{'类型': '结果型', '值结构': {'块列表': '中间块列表：[{类型,文本,行起,行止,属性}]，属性含 章节 及 level/language/url/empty'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.轻量文本解析.解析Markdown块()
- 依赖能力：无
### 解析纯文本块（`办公文档支持库.轻量文本解析.解析纯文本块`）
- 版本：2.0.2
- 一句话说明：把纯文本按空行切成段落中间块列表；行号自 1 起，连续非空行合成一段
- 所属支持库或模块：支持库.后端.办公文档支持库
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'块列表': '段落中间块列表：[{类型,文本,行起,行止,属性}]'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：办公文档支持库.轻量文本解析.解析纯文本块()
- 依赖能力：无
