# 演示文稿（支持库.后端.办公文档支持库.演示文稿）

- 类型：支持库
- 版本：1.0.4
- 说明：演示文稿（ppt/pptx）解析原子能力：pptx 用 python-pptx 原生解析，ppt 旧格式经 支持库.后端.办公文档支持库.文档转换 的 LibreOffice 链转为 pptx 再解析；返回 结果[通用文档]，含幻灯片文本块、备注与图像资源。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `文档转换支持库.LibreOffice转换.转换办公文件` 版本约束 `>=1.0.0`

## 能力
### 解析演示文稿（`办公文档支持库.演示文稿.解析演示文稿`）
- 版本：1.0.0
- 一句话说明：解析 PPT/PPTX 演示文稿为平台通用文档（幻灯片文本/备注/图像资源）；PPTX 原生经 python_pptx提供者 解析，PPT 先经 LibreOffice 转换再解析
- 所属支持库或模块：支持库.后端.办公文档支持库.演示文稿
- 参数列表：文件路径:文本型, 格式:文本型, 最大幻灯片数:整数型, 最大字节数:整数型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：格式, 最大幻灯片数, 最大字节数, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'值': '通用文档（块列表/资源列表/提供者版本/原始文件摘要）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 超出限制, 文件损坏, 提供者不可用, 转换失败, 超时
- 调用示例：办公文档支持库.演示文稿.解析演示文稿(格式=, 最大幻灯片数=200, 最大字节数=209715200, 超时秒=60)
- 依赖能力：文档转换支持库.LibreOffice转换.转换办公文件

## 可配置项
### 默认最大幻灯片数（类型: 整数型）
- 必填：False
- 允许项目覆盖：True
- 允许运行时覆盖：True
- 敏感：False
- 默认值：200
- 说明：幻灯片数上限
- 不合法时错误码：超出限制
### 默认最大字节数（类型: 整数型）
- 必填：False
- 允许项目覆盖：True
- 允许运行时覆盖：True
- 敏感：False
- 默认值：209715200
- 说明：文件大小/解压总量上限
- 不合法时错误码：超出限制
### 默认超时秒（类型: 整数型）
- 必填：False
- 允许项目覆盖：True
- 允许运行时覆盖：True
- 敏感：False
- 默认值：60
- 说明：ppt 转换链执行超时
- 不合法时错误码：超时
