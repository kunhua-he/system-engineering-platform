# 文档转换 使用说明

- 包id：支持库.后端.办公文档支持库.文档转换
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 办公文档支持库.文档转换.检查提供者 | 检查提供者 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '可用性字典（textutil/libreoffice: 可用|不可用）'}} | 检查 textutil / LibreOffice 转换提供者是否可用，返回可用性字典 |
| 办公文档支持库.文档转换.转换文本文件 | 转换文本文件 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '转换结果（文本/格式/提供者）'}} | 用 textutil 转换文档（doc/rtf/html 等）为文本或 HTML（经受管提供者 textutil提供者 执行） |
| 办公文档支持库.文档转换.转换办公文件 | 转换办公文件 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '转换结果（路径/格式/提供者）'}} | 用 LibreOffice 转换 Office 文档（doc/xls/ppt 等）为目标格式，返回目标文件路径（经受管提供者 LibreOffice提供者 执行） |

## 办公文档支持库.文档转换.检查提供者（检查提供者）

**说明**：检查 textutil / LibreOffice 转换提供者是否可用，返回可用性字典

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 提供者名 | 文本型 | 是 | None | 提供者名（全部/textutil/libreoffice；其他值返回 参数不合法） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '可用性字典（textutil/libreoffice: 可用|不可用）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
办公文档支持库.文档转换.检查提供者(提供者名=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档转换.转换文本文件（转换文本文件）

**说明**：用 textutil 转换文档（doc/rtf/html 等）为文本或 HTML（经受管提供者 textutil提供者 执行）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源文档路径（不存在返回 文件不存在） |
| 目标格式 | 文本型 | 否 | txt | 目标格式（txt/html/rtf；其他值返回 参数不合法） |
| 输出目录 | 文本型 | 否 | None | 输出目录（缺省由提供者隔离临时目录管理） |
| 超时秒 | 双精度数型 | 否 | 60 | 转换软超时（秒），超时返回 超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限（字节），超限返回 超出限制 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '转换结果（文本/格式/提供者）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 转换失败 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
办公文档支持库.文档转换.转换文本文件(源路径=值, 目标格式=值, 输出目录=值, 超时秒=值, 最大输出字节=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 办公文档支持库.文档转换.转换办公文件（转换办公文件）

**说明**：用 LibreOffice 转换 Office 文档（doc/xls/ppt 等）为目标格式，返回目标文件路径（经受管提供者 LibreOffice提供者 执行）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源文档路径（不存在返回 文件不存在） |
| 目标格式 | 文本型 | 是 | None | 目标格式（txt/html/rtf/pdf/docx/pptx/xlsx/odt/ods/odp/csv/png/jpg；其他值返回 参数不合法） |
| 输出目录 | 文本型 | 否 | None | 输出目录（缺省为源文件所在目录） |
| 超时秒 | 双精度数型 | 否 | 60 | 转换软超时（秒），超时返回 超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限（字节），超限返回 超出限制 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '转换结果（路径/格式/提供者）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 转换失败 |
| 超时 |
| 超出限制 |

**调用示例**：

```python
办公文档支持库.文档转换.转换办公文件(源路径=值, 目标格式=值, 输出目录=值, 超时秒=值, 最大输出字节=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
