# psycopg提供者（支持库.后端.数据库连接支持库.psycopg数据库）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.3
- 说明：PostgreSQL 唯一同步连接池支持库：持数据库句柄借还池连接，调用方不接触连接对象，不在调用方建池。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 连接数据库（`数据库连接支持库.psycopg数据库.连接数据库`）
- 版本：1.0.0
- 一句话说明：创建唯一同步 PostgreSQL 连接池并返回数据库句柄；调用方只持句柄，不接触连接对象。
- 所属支持库或模块：支持库.后端.数据库连接支持库.psycopg数据库
- 参数列表：连接串:文本型, 连接池大小:整数型, 超时秒:双精度数型
- 必填参数：连接串
- 可选参数：连接池大小, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 超时, 连接失败, 查询失败
- 调用示例：数据库连接支持库.psycopg数据库.连接数据库(连接池大小=4, 超时秒=10.0)
- 依赖能力：无
### 查询数据库（`数据库连接支持库.psycopg数据库.查询数据库`）
- 版本：1.0.0
- 一句话说明：持数据库句柄执行查询并自动借还池连接；查询失败连接只丢弃不回池。
- 所属支持库或模块：支持库.后端.数据库连接支持库.psycopg数据库
- 参数列表：数据库句柄:整数型, SQL:文本型, 参数:列表型, 超时秒:双精度数型
- 必填参数：数据库句柄, SQL
- 可选参数：参数, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 超时, 连接失败, 查询失败
- 调用示例：数据库连接支持库.psycopg数据库.查询数据库(参数=[], 超时秒=30.0)
- 依赖能力：无
### 事务执行数据库（`数据库连接支持库.psycopg数据库.事务执行数据库`）
- 版本：1.0.0
- 一句话说明：持数据库句柄按顺序执行 SQL 列表，全部成功提交，任一失败整体回滚。
- 所属支持库或模块：支持库.后端.数据库连接支持库.psycopg数据库
- 参数列表：数据库句柄:整数型, SQL列表:列表型, 超时秒:双精度数型
- 必填参数：数据库句柄, SQL列表
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 超时, 连接失败, 查询失败
- 调用示例：数据库连接支持库.psycopg数据库.事务执行数据库(超时秒=30.0)
- 依赖能力：无
### 连接池状态（`数据库连接支持库.psycopg数据库.连接池状态`）
- 版本：1.0.0
- 一句话说明：返回句柄对应连接池的有界状态，不暴露连接对象。
- 所属支持库或模块：支持库.后端.数据库连接支持库.psycopg数据库
- 参数列表：数据库句柄:整数型
- 必填参数：数据库句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效
- 调用示例：数据库连接支持库.psycopg数据库.连接池状态()
- 依赖能力：无
### 关闭数据库连接（`数据库连接支持库.psycopg数据库.关闭数据库连接`）
- 版本：1.0.0
- 一句话说明：关闭句柄对应连接池并使句柄失效；重复关闭保持幂等成功。
- 所属支持库或模块：支持库.后端.数据库连接支持库.psycopg数据库
- 参数列表：数据库句柄:整数型
- 必填参数：数据库句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据库连接支持库.psycopg数据库.关闭数据库连接()
- 依赖能力：无
