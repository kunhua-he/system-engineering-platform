# psycopg数据库 使用说明

- 包id：支持库.后端.数据库连接支持库.psycopg数据库
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据库连接支持库.psycopg数据库.连接 | 连接 | 1.0.0 | 结果型 | pg8000 连接测试：成功返回 {已连接: true} |
| 数据库连接支持库.psycopg数据库.查询 | 查询 | 1.0.0 | 结果型 | 查询：返回 {行列表: [{列名: 值}...]}；注意 pg8000 参数化值返回字符串 |
| 数据库连接支持库.psycopg数据库.事务执行 | 事务执行 | 1.0.0 | 结果型 | 事务执行：全部成功提交；任一失败回滚 |
| 数据库连接支持库.psycopg数据库.关闭 | 关闭 | 1.0.0 | 结果型 | 关闭：本提供者每次调用自包含连接，关闭为空操作 |

## 数据库连接支持库.psycopg数据库.连接（连接）

**说明**：pg8000 连接测试：成功返回 {已连接: true}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 连接串 | 文本型 | 是 | postgresql://localhost:5432/postgres | postgresql:// 格式连接串 |
| 超时秒 | 双精度数型 | 否 | 10 | 连接超时秒数 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 超时 |
| 连接失败 |

**调用示例**：

```python
数据库连接支持库.psycopg数据库.连接(连接串=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据库连接支持库.psycopg数据库.查询（查询）

**说明**：查询：返回 {行列表: [{列名: 值}...]}；注意 pg8000 参数化值返回字符串

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 连接串 | 文本型 | 是 | postgresql://localhost:5432/postgres | postgresql:// 格式连接串 |
| SQL | 文本型 | 是 | SELECT 1 | SQL 语句 |
| 参数 | 列表型 | 否 | [] | 参数化值列表 |
| 超时秒 | 双精度数型 | 否 | 30 | 查询超时秒数 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 超时 |
| 连接失败 |
| 查询失败 |

**调用示例**：

```python
数据库连接支持库.psycopg数据库.查询(连接串=值, SQL=值, 参数=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据库连接支持库.psycopg数据库.事务执行（事务执行）

**说明**：事务执行：全部成功提交；任一失败回滚

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 连接串 | 文本型 | 是 | postgresql://localhost:5432/postgres | postgresql:// 格式连接串 |
| SQL列表 | 列表型 | 是 | ['SELECT 1'] | SQL 语句列表 |
| 超时秒 | 双精度数型 | 否 | 30 | 事务超时秒数 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 超时 |
| 连接失败 |
| 查询失败 |

**调用示例**：

```python
数据库连接支持库.psycopg数据库.事务执行(连接串=值, SQL列表=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据库连接支持库.psycopg数据库.关闭（关闭）

**说明**：关闭：本提供者每次调用自包含连接，关闭为空操作

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 连接串 | 文本型 | 是 | postgresql://localhost:5432/postgres | postgresql:// 格式连接串 |
| 超时秒 | 双精度数型 | 否 | 5 | 超时秒数 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |

**调用示例**：

```python
数据库连接支持库.psycopg数据库.关闭(连接串=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
