# SQLite数据库支持库 · 目录树与命令

> 包id：`支持库.后端.数据库连接支持库.SQLite数据库`；版本：`1.0.7`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。

```text
SQLite数据库/
├── __init__.py
├── 实现/提供者.py
├── 包声明.json
├── 能力契约/参数契约.json
├── 依赖契约/依赖契约.json
├── 权限契约/权限契约.json
├── 说明/使用说明.md
└── 验证场景引用.json
```

调用只能经过：

```python
from 支持库.后端.数据库连接支持库.SQLite数据库 import 查询
结果 = 查询("/tmp/数据.db", "SELECT 1")
```
