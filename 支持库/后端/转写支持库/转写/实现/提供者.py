"""跨平台语音转写独立提供者：唯一实现在 支持库/适配层/MLXWhisper提供者（D-3 收口，本包不放第二份）。

本文件原与 `支持库/适配层/MLXWhisper提供者/实现/提供者.py` **逻辑同源**，差异只有三处：
① 取根方式（后端腿**不再按层数**、改按锚目录判据取根，见 `公共契约/运行时/导入前缀`；适配层腿仍是 `parents[4]`）
② 头部注释里指向的既有测试文件名（`测试_MLXWhisper提供者.py` ↔ `测试_*.py`）；
③ `转写音频文件` 的 `附加术语`/`返回分段` 两个参数 —— 该差异**已按迁移清单 §10.3.2
先补进适配层腿**（适配层包 1.0.3 → 1.0.4、能力 `转写.转写音频文件` 1.0.0 → 1.1.0
并已真跑验证），两腿公开名与签名现已逐项同一。

同一份逻辑只能有一个实现，故本文件改为**转调**：让
`支持库.后端.转写支持库.转写.实现.提供者` 与适配层腿那唯一实现成为
**同一个模块对象**（`sys.modules[__name__] = 唯一实现`）。本包 `__init__.py` 照旧从
本路径导入 `检查转写可用性 / 获取模型版本 / 转写音频文件` —— **对外 import 路径零改动**。

为什么不直接 `import ...实现.提供者`：跨包导入 `实现/` 被
`运行核心/依赖防火墙.py` 强制拒绝（判据「跨包禁止导入 实现/ 目录」）；而适配层腿
的公开入口 `__init__.py` 已经是合规的同层导入，且它会正常加载自己的 `实现/` 子模块，
故这里先导公开入口、再把两个模块名指向同一对象（兜底路径按文件路径显式载入，
文件缺失时明确报错、不静默降级）。同一模块对象、不产生第二份实现是平台既有做法，
见 `平台控制面/授权/__init__.py`。
"""

from __future__ import annotations

import sys

import 支持库.适配层.MLXWhisper提供者  # noqa: F401 —— 公开入口（同层，合规）

from 公共契约.运行时.导入前缀 import 载入唯一实现, 取根前缀, 取系统根

唯一实现名 = 取根前缀(__name__) + "支持库.适配层.MLXWhisper提供者.实现.提供者"
系统根 = 取系统根(__file__)

载入唯一实现(唯一实现名, 系统根 / "支持库" / "适配层" / "MLXWhisper提供者" / "实现" / "提供者.py")

sys.modules[__name__] = sys.modules[唯一实现名]
