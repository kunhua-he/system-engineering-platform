# 转写 使用说明

- 包id：支持库.后端.转写支持库.转写
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 转写支持库.转写.检查可用性 | 检查可用性 | 1.0.0 | 结果型 | 检查 MLX Whisper 转写可用性：未配置模型 → 未配置模型；模型目录缺失 → 模型缺失；配置有效 → 子进程探针返回 {可用, 模型名, 模型版本} |
| 转写支持库.转写.转写音频文件 | 转写音频文件 | 1.0.0 | 结果型 | 转写音频文件为文本：受管子进程执行，覆盖 超时/取消/进程组回收/输出上限；未配置模型 → 未配置模型（如实返回，禁止模拟成功） |
| 转写支持库.转写.获取模型版本 | 获取模型版本 | 1.0.0 | 结果型 | 获取 MLX Whisper 模型与库版本：未配置模型 → 未配置模型；返回 {模型名, 模型版本} |

## 转写支持库.转写.检查可用性（检查可用性）

**说明**：检查 MLX Whisper 转写可用性：未配置模型 → 未配置模型；模型目录缺失 → 模型缺失；配置有效 → 子进程探针返回 {可用, 模型名, 模型版本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 300 | 子进程超时 |
| 配置 | 字典型 | 否 | None | 配置对象（模型路径/模型名）；缺省读环境变量 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 未配置模型 |
| 模型缺失 |
| 提供者不可用 |
| 超时 |
| 进程崩溃 |
| 参数不合法 |

**调用示例**：

```python
转写支持库.转写.检查可用性(超时秒=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 转写支持库.转写.转写音频文件（转写音频文件）

**说明**：转写音频文件为文本：受管子进程执行，覆盖 超时/取消/进程组回收/输出上限；未配置模型 → 未配置模型（如实返回，禁止模拟成功）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 音频文件绝对路径 |
| 超时秒 | 双精度数型 | 否 | 300 | 子进程超时 |
| 配置 | 字典型 | 否 | None | 配置对象（模型路径/模型名）；缺省读环境变量 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 未配置模型 |
| 模型缺失 |
| 转写失败 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
| 提供者不可用 |

**调用示例**：

```python
转写支持库.转写.转写音频文件(文件路径=值, 超时秒=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 转写支持库.转写.获取模型版本（获取模型版本）

**说明**：获取 MLX Whisper 模型与库版本：未配置模型 → 未配置模型；返回 {模型名, 模型版本}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 300 | 子进程超时 |
| 配置 | 字典型 | 否 | None | 配置对象（模型路径/模型名）；缺省读环境变量 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 未配置模型 |
| 模型缺失 |
| 提供者不可用 |
| 超时 |
| 进程崩溃 |
| 参数不合法 |

**调用示例**：

```python
转写支持库.转写.获取模型版本(超时秒=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
