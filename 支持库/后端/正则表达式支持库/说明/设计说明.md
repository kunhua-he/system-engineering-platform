# 正则表达式支持库 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.后端.正则表达式支持库
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 正则表达式支持库.校验 | 校验 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 正则合法性校验 |
| 正则表达式支持库.匹配 | 匹配 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 正则匹配返回所有命中 |
| 正则表达式支持库.搜索 | 搜索 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 正则搜索返回首个命中 |
| 正则表达式支持库.替换 | 替换 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 正则替换 |
| 正则表达式支持库.分割 | 分割 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 按正则分割 |

## 正则表达式支持库.校验（校验）

**说明**：正则合法性校验

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 表达式 | 文本型 | 是 | None | 正则表达式 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
正则表达式支持库.校验(表达式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 正则表达式支持库.匹配（匹配）

**说明**：正则匹配返回所有命中

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 表达式 | 文本型 | 是 | None | 正则表达式 |
| 文本 | 文本型 | 是 | None | 目标文本 |
| 忽略大小写 | 逻辑型 | 否 | False | 是否忽略大小写 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 表达式不合法 |

**调用示例**：

```python
正则表达式支持库.匹配(表达式=值, 文本=值, 忽略大小写=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 正则表达式支持库.搜索（搜索）

**说明**：正则搜索返回首个命中

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 表达式 | 文本型 | 是 | None | 正则表达式 |
| 文本 | 文本型 | 是 | None | 目标文本 |
| 忽略大小写 | 逻辑型 | 否 | False | 是否忽略大小写 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 表达式不合法 |

**调用示例**：

```python
正则表达式支持库.搜索(表达式=值, 文本=值, 忽略大小写=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 正则表达式支持库.替换（替换）

**说明**：正则替换

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 表达式 | 文本型 | 是 | None | 正则表达式 |
| 文本 | 文本型 | 是 | None | 目标文本 |
| 替换为 | 文本型 | 是 | None | 替换内容 |
| 忽略大小写 | 逻辑型 | 否 | False | 是否忽略大小写 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 表达式不合法 |

**调用示例**：

```python
正则表达式支持库.替换(表达式=值, 文本=值, 替换为=值, 忽略大小写=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 正则表达式支持库.分割（分割）

**说明**：按正则分割

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 表达式 | 文本型 | 是 | None | 正则表达式 |
| 文本 | 文本型 | 是 | None | 目标文本 |
| 忽略大小写 | 逻辑型 | 否 | False | 是否忽略大小写 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 表达式不合法 |

**调用示例**：

```python
正则表达式支持库.分割(表达式=值, 文本=值, 忽略大小写=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
