<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 正则表达式支持库（支持库.后端.正则表达式支持库）

- 类型：支持库
- 版本：1.0.0
- 说明：正则匹配/搜索/替换/分割/校验（参考易语言正则表达式支持库）
- 入口：__init__.py

## 能力
### 校验（`正则表达式支持库.校验`）
- 版本：1.0.0
- 一句话说明：正则合法性校验
- 所属支持库或模块：支持库.后端.正则表达式支持库
- 参数列表：表达式:文本型
- 必填参数：表达式
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：正则表达式支持库.校验()
- 依赖能力：无
### 匹配（`正则表达式支持库.匹配`）
- 版本：1.0.0
- 一句话说明：正则匹配返回所有命中
- 所属支持库或模块：支持库.后端.正则表达式支持库
- 参数列表：表达式:文本型, 文本:文本型, 忽略大小写:逻辑型
- 必填参数：表达式, 文本
- 可选参数：忽略大小写
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 表达式不合法
- 调用示例：正则表达式支持库.匹配(忽略大小写=False)
- 依赖能力：无
### 搜索（`正则表达式支持库.搜索`）
- 版本：1.0.0
- 一句话说明：正则搜索返回首个命中
- 所属支持库或模块：支持库.后端.正则表达式支持库
- 参数列表：表达式:文本型, 文本:文本型, 忽略大小写:逻辑型
- 必填参数：表达式, 文本
- 可选参数：忽略大小写
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 表达式不合法
- 调用示例：正则表达式支持库.搜索(忽略大小写=False)
- 依赖能力：无
### 替换（`正则表达式支持库.替换`）
- 版本：1.0.0
- 一句话说明：正则替换
- 所属支持库或模块：支持库.后端.正则表达式支持库
- 参数列表：表达式:文本型, 文本:文本型, 替换为:文本型, 忽略大小写:逻辑型
- 必填参数：表达式, 文本, 替换为
- 可选参数：忽略大小写
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 表达式不合法
- 调用示例：正则表达式支持库.替换(忽略大小写=False)
- 依赖能力：无
### 分割（`正则表达式支持库.分割`）
- 版本：1.0.0
- 一句话说明：按正则分割
- 所属支持库或模块：支持库.后端.正则表达式支持库
- 参数列表：表达式:文本型, 文本:文本型, 忽略大小写:逻辑型
- 必填参数：表达式, 文本
- 可选参数：忽略大小写
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 表达式不合法
- 调用示例：正则表达式支持库.分割(忽略大小写=False)
- 依赖能力：无
