# 浏览器自动化支持库 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


## 定位

提供受管浏览器会话的最小原子能力。浏览器第三方程序只属于受管 Provider，不进入主进程；会话、Profile 快照、CDP、端口、临时目录和子进程由底座句柄/租约/资源账本治理。

## 公开能力

- `浏览器自动化.创建会话`
- `浏览器自动化.导航页面`
- `浏览器自动化.读取页面`
- `浏览器自动化.页面操作`
- `浏览器自动化.获取截图`
- `浏览器自动化.关闭会话`

所有有状态能力第一参数必须是句柄。页面操作只允许白名单动作，不开放任意主机代码执行。

## 当前状态

B1 已冻结契约和失败场景；B2 已完成 Provider 实现与真实 HTTP 闭环，Provider 未装配时必须返回“提供者不可用”，不得伪造成功。
