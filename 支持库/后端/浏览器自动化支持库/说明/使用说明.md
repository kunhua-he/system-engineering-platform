# 浏览器自动化支持库（支持库.后端.浏览器自动化支持库）

- 类型：支持库
- 版本：1.3.2
- 说明：受管浏览器会话原子能力；Provider独立进程运行，句柄和资源由底座治理。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 创建会话（`浏览器自动化.创建会话`）
- 版本：1.1.0
- 一句话说明：创建浏览器会话并返回句柄；支持指定视口宽高（0 表示用浏览器默认）
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：会话模式:文本型, 授权标识:文本型, 超时秒:整数型, 视口宽:整数型, 视口高:整数型
- 必填参数：无
- 可选参数：会话模式, 授权标识, 超时秒, 视口宽, 视口高
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 启动失败, 超时, 创建失败
- 调用示例：浏览器自动化.创建会话(会话模式=临时, 授权标识=, 超时秒=30)
- 依赖能力：无
### 导航页面（`浏览器自动化.导航页面`）
- 版本：1.0.0
- 一句话说明：在受管会话中打开HTTP或HTTPS页面
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：句柄:整数型, 地址:文本型, 超时秒:整数型
- 必填参数：句柄, 地址
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {'地址': '文本型', '标题': '文本型', '状态': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 地址不允许, 导航失败, 超时
- 调用示例：浏览器自动化.导航页面(超时秒=30)
- 依赖能力：无
### 读取页面（`浏览器自动化.读取页面`）
- 版本：1.0.0
- 一句话说明：读取页面内容：内容类型支持 文本 / HTML / 链接（链接返回可见链接清单）
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：句柄:整数型, 内容类型:文本型, 最大长度:整数型
- 必填参数：句柄
- 可选参数：内容类型, 最大长度
- 返回结构：{'类型': '结果型', '值结构': {'地址': '文本型', '标题': '文本型', '内容': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 读取失败, 输出超限
- 调用示例：浏览器自动化.读取页面(内容类型=文本, 最大长度=20000)
- 依赖能力：无
### 页面操作（`浏览器自动化.页面操作`）
- 版本：1.0.0
- 一句话说明：执行白名单页面操作，不开放任意主机代码执行
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：句柄:整数型, 操作:文本型, 选择器:文本型, 文本:文本型, 超时秒:整数型
- 必填参数：句柄, 操作
- 可选参数：选择器, 文本, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'状态': '文本型', '地址': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 操作不允许, 句柄无效, 操作失败, 超时
- 调用示例：浏览器自动化.页面操作(选择器=, 文本=, 超时秒=30)
- 依赖能力：无
### 获取截图（`浏览器自动化.获取截图`）
- 版本：1.0.0
- 一句话说明：获取当前页面有界截图并登记为资源
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：句柄:整数型, 格式:文本型, 最大字节数:整数型
- 必填参数：句柄
- 可选参数：格式, 最大字节数
- 返回结构：{'类型': '结果型', '值结构': {'资源句柄': '整数型', '格式': '文本型', '字节数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 截图失败, 输出超限
- 调用示例：浏览器自动化.获取截图(格式=png, 最大字节数=5242880)
- 依赖能力：无
### 关闭会话（`浏览器自动化.关闭会话`）
- 版本：1.0.0
- 一句话说明：请求关闭浏览器会话并核对进程、端口和临时目录回收；重复关闭幂等
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型', '已释放': '逻辑型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 资源未收敛
- 调用示例：浏览器自动化.关闭会话()
- 依赖能力：无
### 读取截图（`浏览器自动化.读取截图`）
- 版本：1.0.0
- 一句话说明：读取截图资源内容（截图句柄由本支持库创建，故由本支持库解析）；二进制经 base64 返回
- 所属支持库或模块：支持库.后端.浏览器自动化支持库
- 参数列表：资源句柄:整数型, 最大字节数:整数型
- 必填参数：资源句柄
- 可选参数：最大字节数
- 返回结构：{'类型': '结果型', '值结构': {'内容base64': '截图字节的 base64 文本（ascii）', '字节数': '截图字节数（整数）', '路径': '截图临时文件路径'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 资源不存在, 读取失败, 输出超限
- 调用示例：浏览器自动化.读取截图(最大字节数=20971520)
- 依赖能力：无
