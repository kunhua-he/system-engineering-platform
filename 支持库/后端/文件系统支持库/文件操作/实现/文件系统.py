"""文件系统原子能力实现（不对外暴露，只经包级中文入口调用）。

全部公开能力返回统一结果（成功/值/错误/错误码）；参数缺失或类型非法
返回 参数不合法，文件系统异常转换为稳定错误码，不吞异常、不以成功形状
伪装失败。文件读取一律显式关闭句柄（with/close 闭合）。
判断存在 为平台既有契约：直接返回布尔（统一验证器直接消费），
非法参数按不存在处理。
"""

from __future__ import annotations

import contextlib
import os
import shutil
import threading
from pathlib import Path
from typing import Any, Callable

from 公共契约.基础类型.结果类型 import 结果
from 公共契约.错误结构 import 错误结构
from 支持库.后端.文件系统支持库.文件操作.实现.危险路径 import (
    拦截危险路径,
    放行标注,
    汇总放行标注,
)
from 公共契约.基础类型.逻辑类型 import 真, 假
from 公共契约.运行时.平台适配 import 清只读后删除树, 移动并可删, MCP身份准入


默认最大字符 = 2000


#: 本模块**每一个会改变磁盘状态**的腿都先做这一道身份对账（2026-09-23 华哥裁决
#: 「改代码也加上全局限制，也要用 mcp…否则全部拒绝落盘」）。
#:
#: 判据只有一份（`平台适配.MCP身份准入`，它内部转 `对账MCP身份` 取拒绝文本），
#: 本函数只把「不过」翻成 `结果.失败("权限不足", …)` —— 与 `写入文件` 的处置逐字同源。
#:
#: ★ 为什么必须补齐（2026-09-23 现场复核的同类缺口）：上一轮只挂了 `写入文件` +
#: 文本补丁两条腿，而**同一个模块里的** `删除文件` / `复制文件` / `移动文件` /
#: `创建目录` / `清理全部临时资源` 都没挂 —— 同一道口的第二扇门：
#: 终端腿想落盘，换一个能力调就绕过去了。本仓纪律「发现一个问题先排查同类」，
#: 故一次补齐（`文件系统补充.py` 的四个同类腿亦同批挂上）。
def _身份拒绝() -> 结果 | None:
    """身份对账不过是返回失败结果；通过返回 None（调用方继续原逻辑）。"""
    通过, 说明 = MCP身份准入()
    if 通过:
        return None
    return 结果.失败("权限不足", 说明, 来源="文件系统")


def _登记md写入凭据(路径: Path, 内容: str) -> None:
    """`.md` 落盘后登记一条写入凭据；**任何异常都不抛**（凭据是旁路，反噬写入即事故）。

    华哥 2026-09-23：「人工也要使用 mcp 工具才行。不能直接手写？否则编译的时候，直接报错。」
    本函数是「经工具写」这一事实的**留痕点**之一（另一处在 `开发工具.MD文档生成` 的 `--写盘`）。
    为什么只对 `.md` 留痕：判据（编译口「拒绝手写md」）只管 md，
    对每个文件都写凭据等于给判据喂它不看的噪声、还平白多一次 IO。
    """
    if 路径.suffix.lower() != ".md":
        return
    try:
        from 公共契约.诊断 import 写入流水
        项目根 = _推断项目根(路径)
        if 项目根 is None:
            return
        写入流水.记一次写入(项目根, 路径.resolve().relative_to(项目根).as_posix(),
                        内容, 工具="文件系统支持库.文件操作.写入文件")
    except Exception:
        return


def _推断项目根(路径: Path) -> Path | None:
    """从被写文件的绝对路径往上找仓库根（含 `AGENTS.md` 且含 `开发文档/` 的那一层）。

    为什么不用环境变量/固定层级：写入腿既可能被源码树里的调用方用（`__file__` 在仓库内），
    也可能被制品里的调用方用（`__file__` 在制品内，往上找不到源码树）——
    沿目标路径本身往上找是**唯一与调用方无关**的定位法。找不到就返回 None（不登记，不猜）。
    """
    当前 = 路径.resolve().parent
    for _ in range(12):
        if (当前 / "AGENTS.md").is_file() and (当前 / "开发文档").is_dir():
            return 当前
        if 当前.parent == 当前:
            break
        当前 = 当前.parent
    return None


def _精炼行(原文行: list) -> tuple[list, list]:
    """结构性精炼：折叠连续空行、去行尾空白、去首尾空行。

    返回（精炼后的行、行映射）；行映射[k] = 精炼后第 k 行在原文行里的下标，
    调用方据此把「已回到第几行」换算回原文行号，续取才接得上。

    **只做结构性处理，不做语义删减**：注释、代码、正文一个字都不删 ——
    「哪句是废话」是语义判断，底座不做（删错即丢信息，且不可逆）。
    """
    精炼: list = []
    映射: list = []
    上一个空 = False
    for 下标, 行 in enumerate(原文行):
        是空 = not 行.strip()
        if 是空:
            if 上一个空:
                continue
            精炼.append("\n")
        else:
            精炼.append(行.rstrip() + "\n" if 行.endswith("\n") else 行.rstrip())
        映射.append(下标)
        上一个空 = 是空
    while 精炼 and 精炼[0] == "\n":
        精炼.pop(0)
        映射.pop(0)
    while 精炼 and 精炼[-1] == "\n":
        精炼.pop()
        映射.pop()
    return 精炼, 映射


def 读取文件(文件路径: str = None, 编码: str = "utf-8",
             起始行: int = 0, 结束行: int = 0,
             最大字符: int = 默认最大字符, 精简: bool = 真,
             允许大文件: bool = 假) -> 结果:
    """按文本方式读取文件内容；默认精炼限长，显式分段则不限量。

    ``起始行`` / ``结束行`` 是 1 起的行号、闭区间；两者都为 0（默认）时取全文，
    与改前逐字一致。只传 起始行 取到文件末；只传 结束行 从首行起。区间超出文件
    实际范围时按存在的行返回（不报错），空区间返回空串。

    **默认路径（2026-09-22 华哥定）**：未显式分段时，先做**结构性精炼**
    （折叠连续空行 / 去行尾空白 / 去首尾空行，``精简=假`` 可关），再按 ``最大字符``
    （默认 2000）限长；超出则回**前 2000 字符 + 一行续取提示**（带续取的原文起始行），
    **不是失败、也不静默** —— 先看核心，要更多再按偏移取。

    **分段读取不限量**：显式传 ``起始行`` / ``结束行`` 即视为调用方自己控量，
    不做精炼、不限长（偏移值查询可以无限）；``允许大文件=真`` 或 ``最大字符=0``
    同样放行全量（用户绝对自主权不被剥夺）。

    为什么要它（#197）：全文读取在大文件上会打爆调用方上下文，而「读取文件头部字节」
    只给字节、拿不到指定行；中间段（例如某能力的契约定义）此前只能整份读进来。
    """
    if 文件路径 is None or not isinstance(文件路径, str) or not 文件路径.strip():
        return 结果.失败("参数不合法", "文件路径必须为非空文本", 来源="文件系统")
    if not isinstance(编码, str):
        return 结果.失败("参数不合法", "编码必须是文本", 来源="文件系统")
    for 名称, 值 in (("起始行", 起始行), ("结束行", 结束行), ("最大字符", 最大字符)):
        if not isinstance(值, int) or isinstance(值, bool) or 值 < 0:
            return 结果.失败("参数不合法", f"{名称} 必须是非负整数（0 = 不限）", 来源="文件系统")
    for 名称, 值 in (("精简", 精简), ("允许大文件", 允许大文件)):
        if not isinstance(值, bool):
            return 结果.失败("参数不合法", f"{名称} 必须是逻辑型（真/假）", 来源="文件系统")
    if 起始行 and 结束行 and 起始行 > 结束行:
        return 结果.失败("参数不合法", f"起始行({起始行}) 不能大于 结束行({结束行})", 来源="文件系统")
    路径 = Path(文件路径)
    if not 路径.is_file():
        return 结果.失败("文件不存在", f"文件不存在: {文件路径}", 来源="文件系统")
    try:
        文本 = 路径.read_text(encoding=编码)
    except LookupError as 错误:
        return 结果.失败("参数不合法", f"未知编码: {错误}", 来源="文件系统")
    except OSError as 错误:
        return 结果.失败("文件读取失败", str(错误), 来源="文件系统")
    行表 = 文本.splitlines(keepends=True)
    起 = (起始行 - 1) if 起始行 else 0
    止 = 结束行 if 结束行 else len(行表)
    原文行 = 行表[起:止]
    分段读 = bool(起始行 or 结束行)
    if 允许大文件 or 最大字符 == 0 or 分段读:
        return 结果.成功结果("".join(原文行))
    精炼行, 行映射 = _精炼行(原文行) if 精简 else (list(原文行), list(range(len(原文行))))
    已回: list = []
    累计 = 0
    停在哪 = len(精炼行)
    for 序号, 行 in enumerate(精炼行):
        if 累计 + len(行) > 最大字符:
            停在哪 = 序号
            break
        已回.append(行)
        累计 += len(行)
    if 停在哪 >= len(精炼行):
        return 结果.成功结果("".join(已回))
    续行 = 起 + 行映射[停在哪] + 1
    return 结果.成功结果(
        "".join(已回)
        + f"\n…[已精炼限长：全文 {len(行表)} 行 / {len(文本)} 字符，本次回 {累计} 字符；"
          f"续取：起始行={续行}（显式分段读取不限量）]"
    )


def 写入文件(文件路径: str = None, 内容: str = None, 编码: str = "utf-8",
             允许危险路径: bool = 假) -> 结果:
    """按文本方式写入文件（自动创建父目录）。

    危险路径护栏（相对安全底线，见 实现/危险路径.py 判据全文）：默认拒绝命中
    危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；
    调用方传 允许危险路径=真 可显式放行并如实标注（用户绝对自主权不被剥夺）。

    **写入凭据（2026-09-23 华哥裁决「人工也要使用 mcp 工具才行，不能直接手写」）**：
    写的是 `.md` 时，落盘后按内容指纹登记一条写入凭据（`公共契约.诊断.写入流水`）——
    编译口的「拒绝手写md」判据据此区分「经工具写」与「有人直接改」。登记失败**不反噬写入**
    （文件已落盘；凭据缺失由判据那边如实判红，不在写入路径上制造二次失败）。

    **原子写（2026-09-23，为「全项目只读」铺路）**：走 `_原子写`（临时文件 + `os.replace`），
    不再 `path.write_text`。关键实测：文件置 `0444` 后 `write_text` **被 OS 拒**（这正是
    「agent 自带工具直接拒绝落盘」的机器根据），而 `os.replace` **照样成功**（原子换名只靠
    **目录**写权限）。故各写入腿改原子写之后，「源码面全只读 + 只有 MCP 腿能写」才成立；
    不改的腿会被只读锁拦死（含本腿自己）。

    **身份对账（2026-09-23 华哥裁决「没对上账直接拒绝落盘」）**：本函数是**第一判据**就调
    `公共契约.运行时.平台适配.MCP身份准入()` —— 对账不过即 `结果.失败("权限不足", …)`
    **不落盘、不抛异常**，错误说明原样取 `对账MCP身份()` 的文本（含可照抄的替代命令），
    不在本文件另写一份（同一条腿两处实现即缺陷）。判据的三种走法：

    · 网关进程内调用 / 编译口子进程（继承 `系统库网关凭证`）⇒ 放行；
    · 终端直跑调本能力 ⇒ **拒绝**（这正是「终端禁止运行本项目代码」的落点）；
    · 修 MCP 自身（白名单 `系统平台_修MCP自身` 非空）⇒ 放行（华哥「修 mcp 可以用终端」）。

    诚实标注：本判据只挡「经本能力落盘」，**挡不住** `echo >` / `sed -i` / agent 自带
    编辑工具这类不经 Python 的写法 —— 那是下一层（整仓只读）的事，不在本轮。
    """
    通过, 说明 = MCP身份准入()
    if not 通过:
        return 结果.失败("权限不足", 说明, 来源="文件系统")
    if 文件路径 is None or not isinstance(文件路径, str) or not 文件路径.strip():
        return 结果.失败("参数不合法", "文件路径必须为非空文本", 来源="文件系统")
    if 内容 is None or not isinstance(内容, str):
        return 结果.失败("参数不合法", "内容必须为文本", 来源="文件系统")
    if not isinstance(编码, str):
        return 结果.失败("参数不合法", "编码必须是文本", 来源="文件系统")
    拦截 = 拦截危险路径(文件路径, 允许危险路径, "写入文件")
    if 拦截 is not None:
        return 拦截
    路径 = Path(文件路径)
    try:
        _原子写(路径, 内容, 编码)
        _登记md写入凭据(路径, 内容)
        return 结果.成功结果(真)
    except LookupError as 错误:
        return 结果.失败("参数不合法", f"未知编码: {错误}", 来源="文件系统")
    except OSError as 错误:
        return 结果.失败("文件不可写", str(错误), 来源="文件系统")


def _原子写(目标: Path, 内容: str, 编码: str = "utf-8") -> None:
    """原子写：同目录唯一临时名 + `fsync` + `os.replace`，**并保留目标原有权限位**。

    为什么必须保留权限位：临时文件默认 `0o600`，直接 `replace` 会让目标权限变成 0600
    （本仓 `文本补丁` 2026-09-21 实测过这个坑）—— 在「全项目只读」场景下更严重：
    一次写入会把只读的 `0444` 静默降成 `0600`，锁就自己开了。

    **内核只读锁的解锁窗口（2026-09-23 华哥裁决「纯粹电脑硬件权限管控」）**：
    整仓受管文件可能置了 macOS 不可变标志（`chflags uchg`，见
    `公共契约/运行时/仓库只读锁.py`）—— 那时 `os.replace` 会被内核以
    `Operation not permitted` 拒绝（实测）。故整个落盘动作包在 `临时解锁()` 里：

        解锁（目标文件 + 其父目录）→ 临时件 → replace → **按原状态重锁**

    为什么父目录也要解锁：`os.replace` 是「在父目录里换名」、新建文件是「在父目录里
    加条目」，父目录锁着时两者都被拒。重锁用「进入时读到的状态」，故本来没锁的文件
    写完之后**不会被凭空锁上**（锁的范围只由 `上锁全仓()` 决定，不由谁碰巧写过它决定）。
    """
    import os
    import tempfile
    from 公共契约.运行时.仓库只读锁 import 临时解锁
    权限 = None
    try:
        if 目标.is_file():
            权限 = 目标.stat().st_mode & 0o777
    except OSError:
        权限 = None
    with 临时解锁(目标):
        # ★ 建父目录必须也在窗口内：目标在**新建的多层目录**里时，`mkdir(parents=True)`
        #   是在「已锁的祖父目录」里加条目 —— 放在窗口外会在这一步就被内核拒
        #   （实测 2026-09-23：向已锁树写 `已锁目录/新目录/新文件.md` 报
        #   `Operation not permitted`）。`临时解锁()` 已按「走到第一个已存在祖先」解锁。
        目标.parent.mkdir(parents=True, exist_ok=True)
        句柄 = tempfile.NamedTemporaryFile(
            "w", encoding=编码, dir=str(目标.parent) if 目标.parent.is_dir() else None,
            delete=False, prefix=".__写入_", suffix=".tmp")
        try:
            句柄.write(内容)
            句柄.flush()
            os.fsync(句柄.fileno())
        finally:
            句柄.close()
        try:
            if 权限 is not None:
                os.chmod(句柄.name, 权限)
            os.replace(句柄.name, 目标)
        except OSError:
            try:
                os.unlink(句柄.name)
            except OSError:
                pass
            raise


def 判断存在(文件路径: str = None) -> bool:
    """判断路径是否存在（平台既有契约：直接返回布尔；非法参数视为不存在）。"""
    if not isinstance(文件路径, str) or not 文件路径.strip():
        return 假
    return Path(文件路径).exists()


def 列出目录(目录路径: str = None) -> 结果:
    """列出目录下条目名称（排序稳定）。"""
    if 目录路径 is None or not isinstance(目录路径, str) or not 目录路径.strip():
        return 结果.失败("参数不合法", "目录路径必须为非空文本", 来源="文件系统")
    路径 = Path(目录路径)
    if not 路径.is_dir():
        return 结果.失败("目录不存在", f"目录不存在: {目录路径}", 来源="文件系统")
    try:
        return 结果.成功结果(sorted(条目.name for 条目 in 路径.iterdir()))
    except OSError as 错误:
        return 结果.失败("目录读取失败", str(错误), 来源="文件系统")


def 删除文件(文件路径: str = None, 允许危险路径: bool = 假) -> 结果:
    """删除文件或空目录（不存在视为幂等成功）。

    危险路径护栏：默认拒绝命中危险路径判据的目标；允许危险路径=真 显式放行并如实标注。
    """
    拒绝 = _身份拒绝()
    if 拒绝 is not None:
        return 拒绝
    if 文件路径 is None or not isinstance(文件路径, str) or not 文件路径.strip():
        return 结果.失败("参数不合法", "文件路径必须为非空文本", 来源="文件系统")
    拦截 = 拦截危险路径(文件路径, 允许危险路径, "删除文件")
    if 拦截 is not None:
        return 拦截
    路径 = Path(文件路径)
    if not 路径.exists():
        return 结果.成功结果(真)
    try:
        from 公共契约.运行时.仓库只读锁 import 临时解锁树
        # 删除 = 在**父目录**里删条目 ⇒ 父目录锁着时被内核拒（实测）；目标自身锁着时
        # `unlink` 同样被拒。故整条删除动作包在解锁窗口里，窗口结束按原状态重锁。
        with 临时解锁树(路径):
            if 路径.is_dir():
                路径.rmdir()
            else:
                路径.unlink()
        return 结果.成功结果(真)
    except OSError as 错误:
        return 结果.失败("文件删除失败", str(错误), 来源="文件系统")


def _窗口落盘(目标: Path, 写动作: Callable[[], None],
             窗口表: tuple[Path, ...]) -> None:
    """在 `临时解锁树` 窗口里执行 `写动作`，**成功与失败两条路都把落点对齐到上下文锁态**。

    为什么要有这一个函数（哲学 1.2「同一件事只有一处实现」）：`复制文件` / `移动文件`
    是**同一族**腿（骨架都是「在解锁窗口里新建或覆盖一个落点，出来把落点对齐到
    它所在上下文的锁态」）—— 骨架与对齐必须**两腿共用一份**，各写一遍就会像本轮之前
    那样「成功路径对齐了、失败路径漏了」（同一根因只修一处）。

    **失败路径为什么也必须对齐（本轮修的洞 ⑤，2026-09-23）**：`shutil.copy2` / 移动腿
    在窗口内**写半截后抛错**（ENOSPC / EINTR）时，落点**已经存在**，但它**进入窗口时
    不存在** ⇒ 不在 `临时解锁树` 的「原状态」表里 ⇒ 窗口 `finally` 只重锁「进入时确实
    锁着的」条目，半截落点不在其中 ⇒ **锁树里留下一个未锁的半截文件**
    （可在其中凭空新建/改写内容，`查询()` 的未锁数由 0 变 1）。
    故对齐动作从「成功路径上的一行」提成「`try/finally` 里的一条腿」：`finally` 覆盖
    成功、`OSError`、以及 `KeyboardInterrupt` 这类非 `OSError` 的中断 —— **只有一处实现**，
    不靠在 `except` 里再补一遍（那是同一件事的第二套实现）。

    **为什么对齐仍在解锁窗口之外调**：窗口内父目录是**临时解锁态**，那时读上下文锁态
    得到的是「未锁」，对齐会得出错误答案（既有实测：窗口内对齐后副本仍 `flags` 为空）。
    `finally` 里窗口已经退出（`with` 的 `__exit__` 在栈展开中先跑），故读到的是真锁态。

    **为什么用 `ExitStack` 而不是把窗口开在调用方**：移动腿要同时开**源与目标**两个窗口
    （源处删 + 目标处写），复制腿只开目标一个；窗口表由调用方给，`ExitStack` 让
    「一个到两个窗口」共用同一段代码，两腿的 `with` 嵌套不必各写一份。

    分工：窗口的开启、`mkdir(parents=True)` 与失败路径的对齐都归本函数（两腿共同的骨架），
    调用方只描述「窗口开在哪、真正动磁盘的那一句是什么」。
    """
    from 公共契约.运行时.仓库只读锁 import 临时解锁树, 对齐目标锁态
    try:
        with contextlib.ExitStack() as 栈:
            for 项 in 窗口表:
                栈.enter_context(临时解锁树(项))
            目标.parent.mkdir(parents=True, exist_ok=True)
            写动作()
    finally:
        对齐目标锁态(目标)


def 复制文件(源路径: str = None, 目标路径: str = None,
             允许危险路径: bool = 假) -> 结果:
    """复制文件到目标路径（自动创建父目录）。

    危险路径护栏只判**目标路径**（源路径只读，不构成系统性破坏）；
    允许危险路径=真 显式放行并如实标注。
    """
    拒绝 = _身份拒绝()
    if 拒绝 is not None:
        return 拒绝
    if 源路径 is None or not isinstance(源路径, str) or not 源路径.strip():
        return 结果.失败("参数不合法", "源路径必须为非空文本", 来源="文件系统")
    if 目标路径 is None or not isinstance(目标路径, str) or not 目标路径.strip():
        return 结果.失败("参数不合法", "目标路径必须为非空文本", 来源="文件系统")
    源 = Path(源路径)
    if not 源.is_file():
        return 结果.失败("文件不存在", f"源文件不存在: {源路径}", 来源="文件系统")
    拦截 = 拦截危险路径(目标路径, 允许危险路径, "复制文件目标路径")
    if 拦截 is not None:
        return 拦截
    try:
        目标 = Path(目标路径)
        # 复制要动**父目录**（建目录 + 加条目）与目标自身，故整条包在解锁窗口里。
        # ★ 副本的锁态按**目标父目录**对齐，不按源文件（否则「把仓库外的文件搬进来」
        #   会在锁树里留下一个未锁的新文件 —— 洞 ④，2026-09-23 实测）。
        #   窗口位置、窗口外的对齐、以及**失败路径同样要对齐**（洞 ⑤）都归 `_窗口落盘`：
        #   那是复制腿与移动腿共用的唯一实现，不在本函数里另写一遍。
        _窗口落盘(目标, lambda: shutil.copy2(源, 目标), (目标,))
        return 结果.成功结果(真)
    except OSError as 错误:
        return 结果.失败("文件复制失败", str(错误), 来源="文件系统")


def 移动文件(源路径: str = None, 目标路径: str = None,
             允许危险路径: bool = 假) -> 结果:
    """移动文件或目录到目标路径（自动创建父目录）。

    危险路径护栏同时判**源路径与目标路径**（移动=源处删除+目标处写入，两端都可能
    造成系统性破坏）；允许危险路径=真 显式放行并如实标注。
    """
    拒绝 = _身份拒绝()
    if 拒绝 is not None:
        return 拒绝
    if 源路径 is None or not isinstance(源路径, str) or not 源路径.strip():
        return 结果.失败("参数不合法", "源路径必须为非空文本", 来源="文件系统")
    if 目标路径 is None or not isinstance(目标路径, str) or not 目标路径.strip():
        return 结果.失败("参数不合法", "目标路径必须为非空文本", 来源="文件系统")
    源 = Path(源路径)
    if not 源.exists():
        return 结果.失败("文件不存在", f"源文件不存在: {源路径}", 来源="文件系统")
    拦截 = 拦截危险路径(源路径, 允许危险路径, "移动文件源路径") \
        or 拦截危险路径(目标路径, 允许危险路径, "移动文件目标路径")
    if 拦截 is not None:
        return 拦截
    try:
        目标 = Path(目标路径)
        # 移动 = 源处删 + 目标处写，**两端的父目录都要动**，故把源与目标一并纳入解锁窗口。
        # ★ 搬进来的条目按**目标父目录**对齐锁态（同 复制文件，洞 ④）；窗口位置、
        #   窗口外的对齐、以及**失败路径同样要对齐**（洞 ⑤）都归 `_窗口落盘`（两腿共用）。
        _窗口落盘(目标, lambda: 移动并可删(源, 目标), (源, 目标))
        移动标注 = 汇总放行标注([源路径, 目标路径])
        if 移动标注:
            return 结果.成功结果({"成功": 真, "危险路径放行": 移动标注,
                                 "路径": 目标路径})
        return 结果.成功结果(真)
    except OSError as 错误:
        return 结果.失败("文件移动失败", str(错误), 来源="文件系统")


def 获取大小(文件路径: str = None) -> 结果:
    """获取文件大小（字节）。"""
    if 文件路径 is None or not isinstance(文件路径, str) or not 文件路径.strip():
        return 结果.失败("参数不合法", "文件路径必须为非空文本", 来源="文件系统")
    路径 = Path(文件路径)
    if not 路径.is_file():
        return 结果.失败("文件不存在", f"文件不存在: {文件路径}", 来源="文件系统")
    try:
        return 结果.成功结果(路径.stat().st_size)
    except OSError as 错误:
        return 结果.失败("文件大小读取失败", str(错误), 来源="文件系统")


def 获取修改时间(文件路径: str = None) -> 结果:
    """获取文件修改时间戳（秒）。"""
    if 文件路径 is None or not isinstance(文件路径, str) or not 文件路径.strip():
        return 结果.失败("参数不合法", "文件路径必须为非空文本", 来源="文件系统")
    路径 = Path(文件路径)
    if not 路径.exists():
        return 结果.失败("文件不存在", f"文件不存在: {文件路径}", 来源="文件系统")
    try:
        return 结果.成功结果(路径.stat().st_mtime)
    except OSError as 错误:
        return 结果.失败("修改时间读取失败", str(错误), 来源="文件系统")


def 创建目录(目录路径: str = None, 递归: bool = 真,
             允许危险路径: bool = 假) -> 结果:
    """创建目录（递归创建父目录；已存在视为幂等成功）。

    危险路径护栏：默认拒绝命中危险路径判据的目标；允许危险路径=真 显式放行并如实标注。
    """
    拒绝 = _身份拒绝()
    if 拒绝 is not None:
        return 拒绝
    if 目录路径 is None or not isinstance(目录路径, str) or not 目录路径.strip():
        return 结果.失败("参数不合法", "目录路径必须为非空文本", 来源="文件系统")
    拦截 = 拦截危险路径(目录路径, 允许危险路径, "创建目录")
    if 拦截 is not None:
        return 拦截
    路径 = Path(目录路径)
    if 路径.is_dir():
        return 结果.成功结果(真)
    try:
        from 公共契约.运行时.仓库只读锁 import 临时解锁树, 对齐目标锁态
        # 建目录 = 在父目录里加条目 ⇒ 父目录锁着时被内核拒（实测）。
        with 临时解锁树(路径):
            if 递归:
                路径.mkdir(parents=True, exist_ok=True)
            else:
                路径.mkdir(exist_ok=True)
        # ★ 新建的目录按**父目录**对齐锁态（洞 ④ 的同族，窗口外调）：在已锁树里建目录后
        #   若不锁上，这个目录就是「锁树里的一块未锁区域」——里面还能随意新建。
        对齐目标锁态(路径)
        return 结果.成功结果(真)
    except OSError as 错误:
        return 结果.失败("目录创建失败", str(错误), 来源="文件系统")


def 读取二进制文件(受控根目录: str = None, 相对路径: str = None,
                    最大字节数: int = 0) -> 结果:
    """在受控根目录内按相对路径读取二进制文件（路径边界校验 + 大小上限）。

    相对路径不得越出受控根目录（防路径逃逸）；最大字节数>0 时超限拒绝
    （防一次性无界读入内存）。供文件资产主链路经项目适配层调用。
    """
    if 受控根目录 is None or not isinstance(受控根目录, str) or not 受控根目录.strip():
        return 结果.失败("参数不合法", "受控根目录必须为非空文本", 来源="文件系统")
    if 相对路径 is None or not isinstance(相对路径, str):
        return 结果.失败("参数不合法", "相对路径必须为文本", 来源="文件系统")
    根目录 = Path(受控根目录).resolve()
    if not 根目录.is_dir():
        return 结果.失败("目录不存在", f"受控根目录不存在: {受控根目录}", 来源="文件系统")
    目标 = (根目录 / 相对路径).resolve()
    try:
        if os.path.commonpath([str(根目录), str(目标)]) != str(根目录):
            return 结果.失败("路径越界", f"路径越出受控根目录: {相对路径}", 来源="文件系统")
    except ValueError:
        return 结果.失败("路径越界", f"路径越出受控根目录: {相对路径}", 来源="文件系统")
    if not 目标.is_file():
        return 结果.失败("文件不存在", f"文件不存在: {相对路径}", 来源="文件系统")
    try:
        文件大小 = 目标.stat().st_size
        if 最大字节数 and 文件大小 > 最大字节数:
            return 结果.失败("文件超限", f"文件 {文件大小} 字节超过上限 {最大字节数}", 来源="文件系统")
        return 结果.成功结果(目标.read_bytes())
    except OSError as 错误:
        return 结果.失败("文件读取失败", str(错误), 来源="文件系统")


def 读取文件头部字节(受控根目录: str = None, 相对路径: str = None,
                     字节数: int = 4096) -> 结果:
    """在受控根目录内按相对路径只读文件头部 N 字节（不加载全文件）。

    受控根 + 相对路径 双重路径边界校验（防路径逃逸）；只读头部字节数，
    大文件也不会整体载入内存。供文件头嗅探/预览等只读场景调用。
    """
    if 受控根目录 is None or not isinstance(受控根目录, str) or not 受控根目录.strip():
        return 结果.失败("参数不合法", "受控根目录必须为非空文本", 来源="文件系统")
    if 相对路径 is None or not isinstance(相对路径, str):
        return 结果.失败("参数不合法", "相对路径必须为文本", 来源="文件系统")
    根目录 = Path(受控根目录).resolve()
    if not 根目录.is_dir():
        return 结果.失败("目录不存在", f"受控根目录不存在: {受控根目录}", 来源="文件系统")
    目标 = (根目录 / 相对路径).resolve()
    try:
        if os.path.commonpath([str(根目录), str(目标)]) != str(根目录):
            return 结果.失败("路径越界", f"路径越出受控根目录: {相对路径}", 来源="文件系统")
    except ValueError:
        return 结果.失败("路径越界", f"路径越出受控根目录: {相对路径}", 来源="文件系统")
    if not 目标.is_file():
        return 结果.失败("文件不存在", f"文件不存在: {相对路径}", 来源="文件系统")
    try:
        with open(目标, "rb") as 文件流:
            return 结果.成功结果(文件流.read(max(int(字节数), 0)))
    except OSError as 错误:
        return 结果.失败("文件读取失败", str(错误), 来源="文件系统")


_临时资源登记表: set[str] = set()
_临时资源锁 = threading.Lock()


def 登记临时资源(路径: str = None) -> 结果:
    """登记一个临时资源（文件或目录），由 清理全部临时资源 统一释放。"""
    if not isinstance(路径, str) or not 路径.strip():
        return 结果.失败("参数不合法", "路径必须为非空文本", 来源="文件系统")
    with _临时资源锁:
        _临时资源登记表.add(路径)
        return 结果.成功结果(len(_临时资源登记表))


def 清理全部临时资源(路径前缀: str = "", 允许危险路径: bool = 假) -> 结果:
    """清理已登记临时资源（文件删除/目录递归删除，不存在视为幂等成功）。

    路径前缀 非空时只清理以此前缀开头的登记资源；无匹配登记资源时返回
    资源不存在（避免静默什么都不做）。登记表变更全部在锁内完成。

    危险路径护栏：清理=删除动作，逐条登记资源默认拒绝命中危险路径判据的目标
    （护栏在任何删除发生之前完成判定，命中即整批不改动）；允许危险路径=真 显式放行并如实标注。
    """
    if 路径前缀 is None or not isinstance(路径前缀, str):
        return 结果.失败("参数不合法", "路径前缀必须为文本", 来源="文件系统")
    拒绝 = _身份拒绝()
    if 拒绝 is not None:
        return 拒绝
    with _临时资源锁:
        # 前缀匹配必须按**路径段边界**（缺陷 #151，2026-09-20）：
        # 裸 `startswith` 是字符串级匹配，前缀 `工程缓存/tmp/abc` 会连
        # `工程缓存/tmp/abcxxx`（相邻但不同的登记项）一起匹配并 `rmtree` —— 误删。
        # 加 `os.sep` 边界后只匹配「就是它本身」或「它在某段之下」。
        待清理 = _临时资源登记表 if not 路径前缀 else {
            路径 for 路径 in _临时资源登记表
            if 路径 == 路径前缀 or 路径.startswith(路径前缀 + os.sep)
        }
        if 路径前缀 and not 待清理:
            return 结果.失败("资源不存在", f"无匹配前缀 {路径前缀} 的登记资源", 来源="文件系统")
        for 路径 in sorted(待清理):
            拦截 = 拦截危险路径(路径, 允许危险路径, "清理全部临时资源")
            if 拦截 is not None:
                return 拦截
        清理失败表 = []
        清理数量 = 0
        # ★ 清理腿也要开解锁窗口（2026-09-23 复核补）：登记资源可能落在**已锁的**
        #   事实源目录里，不开窗口时 `unlink`/`清只读后删除树` 必被内核拒、全部进
        #   `清理失败表`（fail-closed、如实报错，不留洞，但这条腿在锁态下等于不可用）。
        from 公共契约.运行时.仓库只读锁 import 临时解锁
        with 临时解锁(*[Path(项) for 项 in 待清理]):
            for 路径 in list(待清理):
                try:
                    目标 = Path(路径)
                    if 目标.is_dir():
                        清只读后删除树(目标)
                    else:
                        目标.unlink(missing_ok=True)
                    清理数量 += 1
                    _临时资源登记表.discard(路径)
                except OSError as 错误:
                    清理失败表.append(f"{路径}: {错误}")
        清理标注 = 汇总放行标注(sorted(待清理))
    if 清理失败表:
        return 结果.失败("清理失败", "；".join(清理失败表), 来源="文件系统")
    if 清理标注:
        return 结果.成功结果({"清理数量": 清理数量, "危险路径放行": 清理标注})
    return 结果.成功结果(清理数量)
