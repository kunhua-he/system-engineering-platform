# 文件操作 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 包id：支持库.后端.文件系统支持库.文件操作
- 版本：2.0.1
- 说明：原子文件系统能力：读取/写入/判断存在/列出目录/删除/复制/移动/大小/修改时间/创建目录/受控二进制读取/头部读取/临时资源登记清理。只依赖 Python 标准库。写入/追加/删除/移动/复制/建目录/解压落盘/清理登记资源 带危险路径护栏：默认拒绝命中危险路径判据的目标，可用 允许危险路径=真 显式放行并如实标注。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

- **危险路径护栏口径（哲学第 9 条 5 项「相对安全」）**：9 个写类能力（写入/删除/复制/移动/创建目录/清理临时资源/追加写入/压缩/解压）带 `允许危险路径:逻辑型`，默认拒绝命中危险路径判据的目标、可显式放行；**这是相对安全，不是绝对安全边界**——同机同用户下持有文件权限者仍可直接操作，护栏只用于防误操作与系统性破坏；完整判据见同目录 `设计说明.md`。

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 文件系统支持库.文件操作.读取文件 | 读取文件 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '单文件腿（不传 路径清单）= 文本型，文件文本内容；批量腿（传 路径清单）= 字典型 {文件清单, 文件数, 成功数, 失败数}，文件清单 每项 {文件, 内容, 错误码?, 错误说明?}（错误码/错误说明 只在该项失败时出现）'}} | 按文本方式读取文件内容，返回 {值=文本内容}；可只取一段行区间（起始行/结束行，1 起闭区间，默认 0=不限）。默认路径（未显式分段）先做结构性精炼（折叠连续空行/去行尾空白/去首尾空行，精简=假 可关），再按 最大字符（默认 2000）限长；超出回前 2000 字符并附一行续取提示（带续取的原文起始行），不失败、不静默。显式传 起始行/结束行 即视为调用方自行控量：不精炼、不限长（偏移值查询可以无限）；允许大文件=真 或 最大字符=0 同样放行全量；传 路径清单 即批量取证：一次调用读多文件多区间（区间清单 一一对应、可空），值形状 {文件清单, 文件数, 成功数, 失败数}（每项 {文件, 内容, 错误码?, 错误说明?}），不传 路径清单 时行为与改前逐字节一致 |
| 文件系统支持库.文件操作.写入文件 | 写入文件 | 1.2.0 | {'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}} | 按文本方式写入文件（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.判断存在 | 判断存在 | 1.0.0 | {'类型': '逻辑型', '值结构': {'存在': '路径是否存在'}} | 判断路径是否存在并直接返回布尔（平台既有契约：统一验证器直接消费布尔；非法参数视为不存在） |
| 文件系统支持库.文件操作.列出目录 | 列出目录 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '条目名称列表'}} | 列出目录下条目名称（排序稳定），返回 {值=名称列表} |
| 文件系统支持库.文件操作.删除文件 | 删除文件 | 1.2.0 | {'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}} | 删除文件或空目录（不存在视为幂等成功），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.删除目录 | 删除目录 | 1.1.0 | {'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）', '删除文件数': '递归删掉的文件数（整数）', '删除目录数': '递归删掉的目录数（整数，含目标目录自身）'}} | 递归删除目录及其全部内容（不存在视为幂等成功），返回 {值=true, 删除文件数, 删除目录数}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.复制文件 | 复制文件 | 1.2.0 | {'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}} | 复制文件到目标路径（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.移动文件 | 移动文件 | 1.2.0 | {'类型': '结果型', '值结构': {'值': '成功标志（布尔）；调用方显式放行危险路径时为字典型 {成功, 危险路径放行, 路径} 标注'}} | 移动文件或目录到目标路径（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.获取大小 | 获取大小 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '文件大小（整数，字节）'}} | 获取文件大小（字节），返回 {值=大小} |
| 文件系统支持库.文件操作.获取修改时间 | 获取修改时间 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '修改时间戳（浮点数，秒）'}} | 获取文件修改时间戳（秒），返回 {值=时间戳} |
| 文件系统支持库.文件操作.创建目录 | 创建目录 | 1.2.0 | {'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}} | 创建目录（已存在视为幂等成功），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.读取二进制文件 | 读取二进制文件 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '文件字节内容'}} | 在受控根目录内按相对路径读取二进制文件（路径边界校验 + 大小上限），返回 {值=字节} |
| 文件系统支持库.文件操作.读取文件头部字节 | 读取文件头部字节 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '文件头部字节'}} | 在受控根目录内按相对路径只读文件头部 N 字节（不加载全文件），返回 {值=头部字节} |
| 文件系统支持库.文件操作.登记临时资源 | 登记临时资源 | 1.1.0 | {'类型': '结果型', '值结构': {'值': '当前登记数量（整数）'}} | 登记一个临时资源（文件或目录），由 清理全部临时资源 统一释放，返回 {值=当前登记数量} |
| 文件系统支持库.文件操作.清理全部临时资源 | 清理全部临时资源 | 1.2.0 | {'类型': '结果型', '值结构': {'值': '清理数量（整数）；调用方显式放行危险路径时为字典型 {清理数量, 危险路径放行} 标注'}} | 清理已登记临时资源（文件删除/目录递归删除，不存在视为幂等成功），返回 {值=清理数量}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.搜索文件 | 搜索文件 | 1.0.0 | {'类型': '结果型', '值结构': {'文件数': '匹配文件数量（整数）', '文件列表': '匹配文件绝对路径列表（排序稳定）'}} | 按通配符搜索文件（可递归） |
| 文件系统支持库.文件操作.追加写入 | 追加写入 | 1.2.0 | {'类型': '结果型', '值结构': {'路径': '目标文件路径', '追加字节数': '实写字节数（utf-8 编码后的长度，整数）——名实一致，不再记字符数', '追加字符数': '写入文本的 str 字符个数（整数）；与 追加字节数 区分，中文等多字节字符时两者不同', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}} | 追加文本到文件末尾；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.压缩文件 | 压缩文件 | 1.2.0 | {'类型': '结果型', '值结构': {'目标路径': 'zip 目标文件路径', '条目数': '写入归档的文件条数（整数）', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}} | 压缩文件/目录为 zip；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.解压文件 | 解压文件 | 1.2.0 | {'类型': '结果型', '值结构': {'目标目录': '解压目标目录', '条目数': '解出条目数（整数）', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}} | 解压 zip 到目录（防 zip 炸弹 + 防路径穿越；体积/条目数/压缩比三道上限）；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行 |
| 文件系统支持库.文件操作.获取文件权限 | 获取文件权限 | 1.0.0 | {'类型': '结果型', '值结构': {'路径': '目标路径', '权限': '八进制权限文本（如 0o644）', '可读': '当前进程是否可读（逻辑型）', '可写': '当前进程是否可写（逻辑型）', '可执行': '当前进程是否可执行（逻辑型）'}} | 获取文件权限（读/写/执行） |
| 文件系统支持库.文件操作.查找祖先目录 | 查找祖先目录 | 1.0.0 | {'类型': '结果型', '值结构': {'根目录': '命中的祖先目录绝对路径；未命中返回起点', '命中': '是否找到标记（逻辑型）', '层级': '向上查找的层数（整数）', '命中路径': '命中的标记路径；未命中为空文本', '起点': '解析后的起点绝对路径'}} | 从起点向上查找包含指定标记（子目录或文件）的最近祖先目录；找不到返回起点 |
| 文件系统支持库.文件操作.目录树 | 目录树 | 1.0.0 | {'类型': '结果型', '值结构': {'树文本': '缩进树形文本（目录名后带 /），一次调用拿整棵树', '节点数': '实际收录节点数（整数，不含树根自身）', '目录数': '实际收录目录数（整数）', '文件数': '实际收录文件数（整数）', '已截断': '是否因 条数上限 截断（逻辑型）', '截断处': '截断发生的相对路径（相对树根，/ 分隔）；未截断为空文本', '估算token': '整数型，整棵树文本的估算 token 数（口径：字符数 / 3，与 代码解析支持库.仓库地图 同一把尺子）—— 让调用方知道该不该继续下钻，省一次试探。注意网关另有 `返回上限字符`（纯字符），两者不是同一把尺子，别混用', '估算口径': '文本型，估算token 的口径说明（照抄，避免调用方猜）'}} | 一次调用返回多层目录树（深度/名称过滤/条数预算）：返回 {树文本, 节点数, 目录数, 文件数, 已截断, 截断处}；纯只读，条数超限如实截断并回报 |

## 文件系统支持库.文件操作.读取文件（读取文件）

**说明**：按文本方式读取文件内容，返回 {值=文本内容}；可只取一段行区间（起始行/结束行，1 起闭区间，默认 0=不限）。默认路径（未显式分段）先做结构性精炼（折叠连续空行/去行尾空白/去首尾空行，精简=假 可关），再按 最大字符（默认 2000）限长；超出回前 2000 字符并附一行续取提示（带续取的原文起始行），不失败、不静默。显式传 起始行/结束行 即视为调用方自行控量：不精炼、不限长（偏移值查询可以无限）；允许大文件=真 或 最大字符=0 同样放行全量；传 路径清单 即批量取证：一次调用读多文件多区间（区间清单 一一对应、可空），值形状 {文件清单, 文件数, 成功数, 失败数}（每项 {文件, 内容, 错误码?, 错误说明?}），不传 路径清单 时行为与改前逐字节一致

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 否 | None | 要读取的文件路径（项目根相对路径（posix、不带 ..），绝对路径兼容）；不传 路径清单 时必填（文件不存在返回 文件不存在） |
| 编码 | 文本型 | 否 | utf-8 | 文本编码（默认 utf-8） |
| 起始行 | 整数型 | 否 | 0 | 起始行号（1 起，闭区间）；0 = 从首行起。用于只取文件的一段行区间，避免全文读入打爆调用方上下文 |
| 结束行 | 整数型 | 否 | 0 | 结束行号（1 起，闭区间）；0 = 取到文件末。区间超出文件范围时按实际存在的行返回，空区间返回空串 |
| 最大字符 | 整数型 | 否 | 2000 | 默认路径下本次返回的字符上限（0 = 不限）；超出回前 N 字符并附续取提示。显式分段读取不受它约束 |
| 精简 | 逻辑型 | 否 | True | 默认路径下是否做结构性精炼（折叠连续空行/去行尾空白/去首尾空行）；只做结构性处理，不删注释与正文。显式分段读取不精炼 |
| 允许大文件 | 逻辑型 | 否 | False | 真 = 跳过精炼与限长，放行全量（调用方自担上下文风险）；默认 假 |
| 路径清单 | 列表型 | 否 | None | 批量取证：要读的文件清单（每项写 项目根相对路径（posix、不带 ..），绝对路径兼容）。传了它即走批量腿、一次调用回全部文件；不传则走单文件腿（与改前逐字节一致） |
| 区间清单 | 列表型 | 否 | None | 批量取证：与 路径清单 一一对应的行区间清单（必须等长），每项写 [起始行, 结束行] 或 {"起始行": 起, "结束行": 止}，写 空 表示该项走默认路径（精炼 + 最大字符 限长）。只在传 路径清单 时有意义 |
| 项目根 | 文本型 | 否 | None | 批量取证里 路径清单 相对项的解析基准（与 开工即占 的 项目根 同义）；留空落 环境变量 系统平台_项目根 → 进程 cwd。只在传 路径清单 时有意义 |

**返回结构**：{'类型': '结果型', '值结构': {'值': '单文件腿（不传 路径清单）= 文本型，文件文本内容；批量腿（传 路径清单）= 字典型 {文件清单, 文件数, 成功数, 失败数}，文件清单 每项 {文件, 内容, 错误码?, 错误说明?}（错误码/错误说明 只在该项失败时出现）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 文件读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.读取文件(文件路径=值, 编码=值, 起始行=值, 结束行=值)
文件系统支持库.文件操作.读取文件(文件路径=值, 编码=值, 起始行=值, 结束行=值, 最大字符=值, 精简=值, 允许大文件=值)
文件系统支持库.文件操作.读取文件(文件路径=值, 编码=值, 起始行=值, 结束行=值, 最大字符=值, 精简=值, 允许大文件=值, 路径清单=值, 区间清单=值, 项目根=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.写入文件（写入文件）

**说明**：按文本方式写入文件（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 目标文件路径 |
| 内容 | 文本型 | 是 | None | 要写入的文本内容 |
| 编码 | 文本型 | 否 | utf-8 | 文本编码（默认 utf-8；未知编码返回 参数不合法） |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不可写 |
| 危险路径 |
| 越界 |
| 权限不足 |

**调用示例**：

```python
文件系统支持库.文件操作.写入文件(文件路径=值, 内容=值, 编码=值, 允许危险路径=值)
文件系统支持库.文件操作.写入文件(文件路径=值, 内容=值, 编码=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.判断存在（判断存在）

**说明**：判断路径是否存在并直接返回布尔（平台既有契约：统一验证器直接消费布尔；非法参数视为不存在）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 要判断的路径（非法参数按不存在处理） |

**返回结构**：{'类型': '逻辑型', '值结构': {'存在': '路径是否存在'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
文件系统支持库.文件操作.判断存在(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.列出目录（列出目录）

**说明**：列出目录下条目名称（排序稳定），返回 {值=名称列表}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目录路径 | 文本型 | 是 | None | 要列出的目录路径（目录不存在返回 目录不存在） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '条目名称列表'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 目录读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.列出目录(目录路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.删除文件（删除文件）

**说明**：删除文件或空目录（不存在视为幂等成功），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 要删除的文件或空目录路径 |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件删除失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.删除文件(文件路径=值, 允许危险路径=值)
文件系统支持库.文件操作.删除文件(文件路径=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.删除目录（删除目录）

**说明**：递归删除目录及其全部内容（不存在视为幂等成功），返回 {值=true, 删除文件数, 删除目录数}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目录路径 | 文本型 | 是 | None | 要递归删除的目录路径（不存在视为幂等成功） |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）', '删除文件数': '递归删掉的文件数（整数）', '删除目录数': '递归删掉的目录数（整数，含目标目录自身）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录删除失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.删除目录(目录路径=值, 允许危险路径=值)
文件系统支持库.文件操作.删除目录(目录路径=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.复制文件（复制文件）

**说明**：复制文件到目标路径（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源文件路径（不存在返回 文件不存在） |
| 目标路径 | 文本型 | 是 | None | 目标文件路径 |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 文件复制失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.复制文件(源路径=值, 目标路径=值, 允许危险路径=值)
文件系统支持库.文件操作.复制文件(源路径=值, 目标路径=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.移动文件（移动文件）

**说明**：移动文件或目录到目标路径（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源路径（不存在返回 文件不存在） |
| 目标路径 | 文本型 | 是 | None | 目标文件或目录的路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（布尔）；调用方显式放行危险路径时为字典型 {成功, 危险路径放行, 路径} 标注'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 文件移动失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.移动文件(源路径=值, 目标路径=值, 允许危险路径=值)
文件系统支持库.文件操作.移动文件(源路径=值, 目标路径=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.获取大小（获取大小）

**说明**：获取文件大小（字节），返回 {值=大小}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文件路径（不存在返回 文件不存在） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '文件大小（整数，字节）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 文件大小读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.获取大小(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.获取修改时间（获取修改时间）

**说明**：获取文件修改时间戳（秒），返回 {值=时间戳}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文件路径（不存在返回 文件不存在） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '修改时间戳（浮点数，秒）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 修改时间读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.获取修改时间(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.创建目录（创建目录）

**说明**：创建目录（已存在视为幂等成功），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目录路径 | 文本型 | 是 | None | 要创建的目录路径 |
| 递归 | 逻辑型 | 否 | True | 是否递归创建父目录（默认递归） |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录创建失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.创建目录(目录路径=值, 递归=值, 允许危险路径=值)
文件系统支持库.文件操作.创建目录(目录路径=值, 递归=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.读取二进制文件（读取二进制文件）

**说明**：在受控根目录内按相对路径读取二进制文件（路径边界校验 + 大小上限），返回 {值=字节}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 受控根目录 | 文本型 | 是 | None | 受控根目录（不存在返回 目录不存在） |
| 相对路径 | 文本型 | 是 | None | 根目录内相对路径（越界返回 路径越界） |
| 最大字节数 | 整数型 | 否 | 0 | 大小上限（0 表示不限制；超出返回 文件超限） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '文件字节内容'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 路径越界 |
| 文件不存在 |
| 文件超限 |
| 文件读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.读取二进制文件(受控根目录=值, 相对路径=值, 最大字节数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.读取文件头部字节（读取文件头部字节）

**说明**：在受控根目录内按相对路径只读文件头部 N 字节（不加载全文件），返回 {值=头部字节}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 受控根目录 | 文本型 | 是 | None | 受控根目录（不存在返回 目录不存在） |
| 相对路径 | 文本型 | 是 | None | 根目录内相对路径（越界返回 路径越界） |
| 字节数 | 整数型 | 否 | 4096 | 要读取的头部字节数（默认 4096） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '文件头部字节'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 路径越界 |
| 文件不存在 |
| 文件读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.读取文件头部字节(受控根目录=值, 相对路径=值, 字节数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.登记临时资源（登记临时资源）

**说明**：登记一个临时资源（文件或目录），由 清理全部临时资源 统一释放，返回 {值=当前登记数量}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径 | 文本型 | 是 | None | 要登记的临时资源路径（非空文本） |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '当前登记数量（整数）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.登记临时资源(路径=值)
文件系统支持库.文件操作.登记临时资源(路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.清理全部临时资源（清理全部临时资源）

**说明**：清理已登记临时资源（文件删除/目录递归删除，不存在视为幂等成功），返回 {值=清理数量}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径前缀 | 文本型 | 否 |  | 只清理路径以此前缀开头的登记资源（空串=清理全部；无匹配返回 资源不存在） |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '清理数量（整数）；调用方显式放行危险路径时为字典型 {清理数量, 危险路径放行} 标注'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 资源不存在 |
| 清理失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.清理全部临时资源(路径前缀=值, 允许危险路径=值)
文件系统支持库.文件操作.清理全部临时资源(路径前缀=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.搜索文件（搜索文件）

**说明**：按通配符搜索文件（可递归）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目录 | 文本型 | 是 | None | 搜索目录 |
| 通配符 | 文本型 | 是 | None | 通配符如 *.md |
| 递归 | 逻辑型 | 否 | False | 是否递归子目录 |

**返回结构**：{'类型': '结果型', '值结构': {'文件数': '匹配文件数量（整数）', '文件列表': '匹配文件绝对路径列表（排序稳定）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.搜索文件(目录=值, 通配符=值, 递归=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.追加写入（追加写入）

**说明**：追加文本到文件末尾；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径 | 文本型 | 是 | None | 文件路径 |
| 内容 | 文本型 | 是 | None | 追加内容 |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'路径': '目标文件路径', '追加字节数': '实写字节数（utf-8 编码后的长度，整数）——名实一致，不再记字符数', '追加字符数': '写入文本的 str 字符个数（整数）；与 追加字节数 区分，中文等多字节字符时两者不同', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 写入失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.追加写入(路径=值, 内容=值, 允许危险路径=值)
文件系统支持库.文件操作.追加写入(路径=值, 内容=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.压缩文件（压缩文件）

**说明**：压缩文件/目录为 zip；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源文件/目录 |
| 目标路径 | 文本型 | 是 | None | zip 目标路径 |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'目标路径': 'zip 目标文件路径', '条目数': '写入归档的文件条数（整数）', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 源不存在 |
| 压缩失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.压缩文件(源路径=值, 目标路径=值, 允许危险路径=值)
文件系统支持库.文件操作.压缩文件(源路径=值, 目标路径=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.解压文件（解压文件）

**说明**：解压 zip 到目录（防 zip 炸弹 + 防路径穿越；体积/条目数/压缩比三道上限）；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | zip 文件路径 |
| 目标目录 | 文本型 | 是 | None | 解压目标目录 |
| 最大字节数 | 整数型 | 否 | None | 解压后总字节上限，默认 1 GiB（1024*1024*1024）；传 0/负数/非数值 → 参数不合法 |
| 最大条目数 | 整数型 | 否 | None | 归档条目数上限，默认 10000；传 0/负数/非数值 → 参数不合法 |
| 最大压缩比 | 双精度数型 | 否 | None | 解压总字节 ÷ 压缩包磁盘字节 的上限，默认 100（防 zip 炸弹）；传 0/负数/非数值 → 参数不合法 |
| 允许危险路径 | 逻辑型 | 否 | False | 危险路径护栏（相对安全底线）：默认拒绝命中危险路径判据的目标（系统目录 / 用户凭据与 shell 启动配置 / 指向它们的软链接父路径）；传 真 显式放行并在返回结果里如实标注 危险路径放行 |
| 开工ID | 文本型 | 是 | None | 本次改动的开工ID（`开工编排.开工即占` 返回的凭证）。写入腿据它核对「这条路径的活跃写租约是不是你认领的」：缺失、或与租约所有者不符即拒绝（错误码 越界） |

**返回结构**：{'类型': '结果型', '值结构': {'目标目录': '解压目标目录', '条目数': '解出条目数（整数）', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 源不存在 |
| 超过解压上限 |
| 超过条目上限 |
| 超过压缩比上限 |
| 路径越界 |
| 解压失败 |
| 危险路径 |
| 越界 |

**调用示例**：

```python
文件系统支持库.文件操作.解压文件(源路径=值, 目标目录=值, 最大字节数=值, 最大条目数=值, 最大压缩比=值, 允许危险路径=值)
文件系统支持库.文件操作.解压文件(源路径=值, 目标目录=值, 最大字节数=值, 最大条目数=值, 最大压缩比=值, 允许危险路径=值, 开工ID=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.获取文件权限（获取文件权限）

**说明**：获取文件权限（读/写/执行）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 路径 | 文本型 | 是 | None | 文件路径 |

**返回结构**：{'类型': '结果型', '值结构': {'路径': '目标路径', '权限': '八进制权限文本（如 0o644）', '可读': '当前进程是否可读（逻辑型）', '可写': '当前进程是否可写（逻辑型）', '可执行': '当前进程是否可执行（逻辑型）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 路径不存在 |
| 读取失败 |

**调用示例**：

```python
文件系统支持库.文件操作.获取文件权限(路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.查找祖先目录（查找祖先目录）

**说明**：从起点向上查找包含指定标记（子目录或文件）的最近祖先目录；找不到返回起点

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 起点 | 文本型 | 是 | None | 文件或目录路径 |
| 标记 | 文本型 | 是 | None | 子目录名或文件名，如 .git |
| 匹配起点名 | 逻辑型 | 否 | True | 起点自身名等于标记时返回其父 |
| 最大层数 | 整数型 | 否 | 64 | 向上查找层数上限 |

**返回结构**：{'类型': '结果型', '值结构': {'根目录': '命中的祖先目录绝对路径；未命中返回起点', '命中': '是否找到标记（逻辑型）', '层级': '向上查找的层数（整数）', '命中路径': '命中的标记路径；未命中为空文本', '起点': '解析后的起点绝对路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 路径无效 |

**调用示例**：

```python
文件系统支持库.文件操作.查找祖先目录(起点=值, 标记=值, 匹配起点名=值, 最大层数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）

## 文件系统支持库.文件操作.目录树（目录树）

**说明**：一次调用返回多层目录树（深度/名称过滤/条数预算）：返回 {树文本, 节点数, 目录数, 文件数, 已截断, 截断处}；纯只读，条数超限如实截断并回报

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目录路径 | 文本型 | 是 | None | 树根目录路径（不存在或不是目录返回 参数不合法）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 最大深度 | 整数型 | 否 | 3 | 递归深度；1 = 只列树根的直接子项，0 = 不限深度 |
| 包含文件 | 逻辑型 | 否 | True | 假 = 只返回目录骨架（文件数为 0） |
| 名称模式 | 文本型 | 否 |  | 可选 glob（如 *.py），只作用于文件条目，目录骨架始终保留；空 = 不过滤 |
| 排除目录 | 列表型 | 否 | [] | 排除的目录名（整表替换，不叠加）；空 = 用平台默认排除（.git、__pycache__、工程缓存、node_modules、.venv、dist、build） |
| 条数上限 | 整数型 | 否 | 500 | 节点数上限（不含树根自身）；超限如实截断并在 已截断/截断处 回报，不静默 |
| 含大小 | 逻辑型 | 否 | False | 真 = 文件条目附带字节数（形如 `名称 (123 字节)`） |
| 忽略模式 | 列表型 | 否 | None | 忽略型 glob 列表（2026-09-21 补，只增的可选参数）：命中的条目（目录连子树）不进树；不传 = 行为与改前逐字一致。与 名称模式 方向相反（那个是含型白名单，同 tree -P；这个是忽略型，同 .gitignore 直觉），两者可同时用（先忽略再白名单）。整段相对路径与名称都参与匹配，故 `工程缓存/**` 与 `*.log` 两种写法都能用 |

**返回结构**：{'类型': '结果型', '值结构': {'树文本': '缩进树形文本（目录名后带 /），一次调用拿整棵树', '节点数': '实际收录节点数（整数，不含树根自身）', '目录数': '实际收录目录数（整数）', '文件数': '实际收录文件数（整数）', '已截断': '是否因 条数上限 截断（逻辑型）', '截断处': '截断发生的相对路径（相对树根，/ 分隔）；未截断为空文本', '估算token': '整数型，整棵树文本的估算 token 数（口径：字符数 / 3，与 代码解析支持库.仓库地图 同一把尺子）—— 让调用方知道该不该继续下钻，省一次试探。注意网关另有 `返回上限字符`（纯字符），两者不是同一把尺子，别混用', '估算口径': '文本型，估算token 的口径说明（照抄，避免调用方猜）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
文件系统支持库.文件操作.目录树(目录路径=值, 最大深度=值, 包含文件=值, 名称模式=值, 排除目录=值, 条数上限=值, 含大小=值, 忽略模式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）
