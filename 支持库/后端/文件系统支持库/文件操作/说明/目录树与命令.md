# 文件系统 · 目录树与命令

> 包id：`支持库.后端.文件系统支持库.文件操作`；版本：`1.1.8`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。

## 目录树

```text
文件系统/
├── __init__.py
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/文件系统.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
├── 默认配置/默认配置.json
```

## 公开命令

### `文件系统.读取文件`

- 名称：读取文件
- 返回：`结果型`
- 说明：按文本方式读取文件内容
- 参数：
  - `文件路径`；文本型；必填；要读取的文件路径
  - `编码`；文本型；可选；默认=utf-8；文本型编码
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.写入文件`

- 名称：写入文件
- 返回：`结果型`
- 说明：按文本方式写入文件（自动创建父目录）
- 参数：
  - `文件路径`；文本型；必填；目标文件路径
  - `内容`；文本型；必填；要写入的文本内容
  - `编码`；文本型；可选；默认=utf-8；文本型编码
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.判断存在`

- 名称：判断存在
- 返回：`逻辑型`
- 说明：判断路径是否存在并直接返回布尔（平台既有契约）
- 参数：
  - `文件路径`；文本型；必填；要判断的路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.列出目录`

- 名称：列出目录
- 返回：`结果型`
- 说明：列出目录下条目名称（排序稳定）
- 参数：
  - `目录路径`；文本型；必填；要列出的目录路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.删除文件`

- 名称：删除文件
- 返回：`结果型`
- 说明：删除文件或空目录（不存在视为幂等成功）
- 参数：
  - `文件路径`；文本型；必填；要删除的文件或空目录路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.复制文件`

- 名称：复制文件
- 返回：`结果型`
- 说明：复制文件到目标路径（自动创建父目录）
- 参数：
  - `源路径`；文本型；必填；源文件路径
  - `目标路径`；文本型；必填；目标文件路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.移动文件`

- 名称：移动文件
- 返回：`结果型`
- 说明：移动文件或目录到目标路径（自动创建父目录）
- 参数：
  - `源路径`；文本型；必填；源路径
  - `目标路径`；文本型；必填；目标路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.获取大小`

- 名称：获取大小
- 返回：`结果型`
- 说明：获取文件大小（字节）
- 参数：
  - `文件路径`；文本型；必填；文件路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.获取修改时间`

- 名称：获取修改时间
- 返回：`结果型`
- 说明：获取文件修改时间戳（秒）
- 参数：
  - `文件路径`；文本型；必填；文件路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.创建目录`

- 名称：创建目录
- 返回：`结果型`
- 说明：创建目录（已存在视为幂等成功）
- 参数：
  - `目录路径`；文本型；必填；要创建的目录路径
  - `递归`；逻辑型；可选；默认=True；是否递归创建父目录
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.读取二进制文件`

- 名称：读取二进制文件
- 返回：`结果型`
- 说明：在受控根目录内按相对路径读取二进制文件（路径边界校验 + 大小上限）
- 参数：
  - `受控根目录`；文本型；必填；受控根目录
  - `相对路径`；文本型；必填；根目录内相对路径
  - `最大字节数`；整数型；可选；默认=0；大小上限（0 不限制）
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.读取文件头部字节`

- 名称：读取文件头部字节
- 返回：`结果型`
- 说明：在受控根目录内按相对路径只读文件头部 N 字节
- 参数：
  - `受控根目录`；文本型；必填；受控根目录
  - `相对路径`；文本型；必填；根目录内相对路径
  - `字节数`；整数型；可选；默认=4096；要读取的头部字节数
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.登记临时资源`

- 名称：登记临时资源
- 返回：`结果型`
- 说明：登记一个临时资源（文件或目录）
- 参数：
  - `路径`；文本型；必填；要登记的临时资源路径
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件系统.清理全部临时资源`

- 名称：清理全部临时资源
- 返回：`结果型`
- 说明：清理已登记临时资源（不存在视为幂等成功）
- 参数：
  - `路径前缀`；文本型；可选；默认=；只清理此前缀开头的登记资源（空串=全部）
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
