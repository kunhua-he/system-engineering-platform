# 文件系统支持库（支持库.后端.文件系统支持库）

- 类型：支持库
- 版本：2.0.2
- 说明：聚合支持库，包含 3 个子域：内容检索/文件操作/文本补丁
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 正则搜索（`文件系统支持库.内容检索.正则搜索`）
- 版本：2.0.2
- 一句话说明：按正则搜索目录内文件内容（优先 rg，回退 grep）；返回 {引擎, 搜索根, 匹配列表, 匹配数, 已达上限, 命令}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：根目录:文本型, 模式:文本型, 路径:文本型, 文件模式:文本型, 最大匹配数:整数型, 上下文:整数型, 忽略大小写:逻辑型, 排除列表:列表型
- 必填参数：根目录, 模式
- 可选参数：路径, 文件模式, 最大匹配数, 上下文, 忽略大小写, 排除列表
- 返回结构：{'类型': '结果型', '值结构': {'引擎': '实际使用的检索引擎（rg 或 grep）', '搜索根': '相对根目录的搜索起点（根目录本身为 .）', '匹配列表': '列表，每项 {文件, 行号, 文本}', '匹配数': '匹配条数（整数）', '已达上限': '匹配数是否达到上限（逻辑型）', '命令': '实际执行的命令列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.内容检索.正则搜索()
- 依赖能力：无
### 读取文件（`文件系统支持库.文件操作.读取文件`）
- 版本：2.0.2
- 一句话说明：按文本方式读取文件内容，返回 {值=文本内容}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型, 编码:文本型
- 必填参数：文件路径
- 可选参数：编码
- 返回结构：{'类型': '结果型', '值结构': {'值': '文件文本内容'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.读取文件()
- 依赖能力：无
### 写入文件（`文件系统支持库.文件操作.写入文件`）
- 版本：2.0.2
- 一句话说明：按文本方式写入文件（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型, 内容:文本型, 编码:文本型, 允许危险路径:逻辑型
- 必填参数：文件路径, 内容
- 可选参数：编码, 允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.写入文件()
- 依赖能力：无
### 判断存在（`文件系统支持库.文件操作.判断存在`）
- 版本：2.0.2
- 一句话说明：判断路径是否存在并直接返回布尔（平台既有契约：统一验证器直接消费布尔；非法参数视为不存在）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：{'类型': '逻辑型', '值结构': {'存在': '路径是否存在'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.判断存在()
- 依赖能力：无
### 列出目录（`文件系统支持库.文件操作.列出目录`）
- 版本：2.0.2
- 一句话说明：列出目录下条目名称（排序稳定），返回 {值=名称列表}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：目录路径:文本型
- 必填参数：目录路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '条目名称列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.列出目录()
- 依赖能力：无
### 删除文件（`文件系统支持库.文件操作.删除文件`）
- 版本：2.0.2
- 一句话说明：删除文件或空目录（不存在视为幂等成功），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型, 允许危险路径:逻辑型
- 必填参数：文件路径
- 可选参数：允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.删除文件()
- 依赖能力：无
### 复制文件（`文件系统支持库.文件操作.复制文件`）
- 版本：2.0.2
- 一句话说明：复制文件到目标路径（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标路径:文本型, 允许危险路径:逻辑型
- 必填参数：源路径, 目标路径
- 可选参数：允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.复制文件()
- 依赖能力：无
### 移动文件（`文件系统支持库.文件操作.移动文件`）
- 版本：2.0.2
- 一句话说明：移动文件或目录到目标路径（自动创建父目录），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标路径:文本型, 允许危险路径:逻辑型
- 必填参数：源路径, 目标路径
- 可选参数：允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（布尔）；调用方显式放行危险路径时为字典型 {成功, 危险路径放行, 路径} 标注'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.移动文件()
- 依赖能力：无
### 获取大小（`文件系统支持库.文件操作.获取大小`）
- 版本：2.0.2
- 一句话说明：获取文件大小（字节），返回 {值=大小}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '文件大小（整数，字节）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.获取大小()
- 依赖能力：无
### 获取修改时间（`文件系统支持库.文件操作.获取修改时间`）
- 版本：2.0.2
- 一句话说明：获取文件修改时间戳（秒），返回 {值=时间戳}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '修改时间戳（浮点数，秒）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.获取修改时间()
- 依赖能力：无
### 创建目录（`文件系统支持库.文件操作.创建目录`）
- 版本：2.0.2
- 一句话说明：创建目录（已存在视为幂等成功），返回 {值=true}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：目录路径:文本型, 递归:逻辑型, 允许危险路径:逻辑型
- 必填参数：目录路径
- 可选参数：递归, 允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'值': '成功标志（布尔：真=已完成）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.创建目录()
- 依赖能力：无
### 读取二进制文件（`文件系统支持库.文件操作.读取二进制文件`）
- 版本：2.0.2
- 一句话说明：在受控根目录内按相对路径读取二进制文件（路径边界校验 + 大小上限），返回 {值=字节}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：受控根目录:文本型, 相对路径:文本型, 最大字节数:整数型
- 必填参数：受控根目录, 相对路径
- 可选参数：最大字节数
- 返回结构：{'类型': '结果型', '值结构': {'值': '文件字节内容'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.读取二进制文件()
- 依赖能力：无
### 读取文件头部字节（`文件系统支持库.文件操作.读取文件头部字节`）
- 版本：2.0.2
- 一句话说明：在受控根目录内按相对路径只读文件头部 N 字节（不加载全文件），返回 {值=头部字节}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：受控根目录:文本型, 相对路径:文本型, 字节数:整数型
- 必填参数：受控根目录, 相对路径
- 可选参数：字节数
- 返回结构：{'类型': '结果型', '值结构': {'值': '文件头部字节'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.读取文件头部字节()
- 依赖能力：无
### 登记临时资源（`文件系统支持库.文件操作.登记临时资源`）
- 版本：2.0.2
- 一句话说明：登记一个临时资源（文件或目录），由 清理全部临时资源 统一释放，返回 {值=当前登记数量}
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '当前登记数量（整数）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.登记临时资源()
- 依赖能力：无
### 清理全部临时资源（`文件系统支持库.文件操作.清理全部临时资源`）
- 版本：2.0.2
- 一句话说明：清理已登记临时资源（文件删除/目录递归删除，不存在视为幂等成功），返回 {值=清理数量}；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径前缀:文本型, 允许危险路径:逻辑型
- 必填参数：无
- 可选参数：路径前缀, 允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'值': '清理数量（整数）；调用方显式放行危险路径时为字典型 {清理数量, 危险路径放行} 标注'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.清理全部临时资源()
- 依赖能力：无
### 搜索文件（`文件系统支持库.文件操作.搜索文件`）
- 版本：2.0.2
- 一句话说明：按通配符搜索文件（可递归）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：目录:文本型, 通配符:文本型, 递归:逻辑型
- 必填参数：目录, 通配符
- 可选参数：递归
- 返回结构：{'类型': '结果型', '值结构': {'文件数': '匹配文件数量（整数）', '文件列表': '匹配文件绝对路径列表（排序稳定）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.搜索文件()
- 依赖能力：无
### 追加写入（`文件系统支持库.文件操作.追加写入`）
- 版本：2.0.2
- 一句话说明：追加文本到文件末尾；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径:文本型, 内容:文本型, 允许危险路径:逻辑型
- 必填参数：路径, 内容
- 可选参数：允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'路径': '目标文件路径', '追加字节数': '实写字节数（utf-8 编码后的长度，整数）——名实一致，不再记字符数', '追加字符数': '写入文本的 str 字符个数（整数）；与 追加字节数 区分，中文等多字节字符时两者不同', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.追加写入()
- 依赖能力：无
### 压缩文件（`文件系统支持库.文件操作.压缩文件`）
- 版本：2.0.2
- 一句话说明：压缩文件/目录为 zip；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标路径:文本型, 允许危险路径:逻辑型
- 必填参数：源路径, 目标路径
- 可选参数：允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'目标路径': 'zip 目标文件路径', '条目数': '写入归档的文件条数（整数）', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.压缩文件()
- 依赖能力：无
### 解压文件（`文件系统支持库.文件操作.解压文件`）
- 版本：2.0.2
- 一句话说明：解压 zip 到目录（防 zip 炸弹 + 防路径穿越；体积/条目数/压缩比三道上限）；默认拒绝危险路径（危险路径护栏），可用 允许危险路径=真 显式放行
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标目录:文本型, 最大字节数:整数型, 最大条目数:整数型, 最大压缩比:双精度数型, 允许危险路径:逻辑型
- 必填参数：源路径, 目标目录
- 可选参数：最大字节数, 最大条目数, 最大压缩比, 允许危险路径
- 返回结构：{'类型': '结果型', '值结构': {'目标目录': '解压目标目录', '条目数': '解出条目数（整数）', '危险路径放行': '仅调用方显式放行危险路径时出现的标注文案'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.解压文件()
- 依赖能力：无
### 获取文件权限（`文件系统支持库.文件操作.获取文件权限`）
- 版本：2.0.2
- 一句话说明：获取文件权限（读/写/执行）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'路径': '目标路径', '权限': '八进制权限文本（如 0o644）', '可读': '当前进程是否可读（逻辑型）', '可写': '当前进程是否可写（逻辑型）', '可执行': '当前进程是否可执行（逻辑型）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.获取文件权限()
- 依赖能力：无
### 查找祖先目录（`文件系统支持库.文件操作.查找祖先目录`）
- 版本：2.0.2
- 一句话说明：从起点向上查找包含指定标记（子目录或文件）的最近祖先目录；找不到返回起点
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：起点:文本型, 标记:文本型, 匹配起点名:逻辑型, 最大层数:整数型
- 必填参数：起点, 标记
- 可选参数：匹配起点名, 最大层数
- 返回结构：{'类型': '结果型', '值结构': {'根目录': '命中的祖先目录绝对路径；未命中返回起点', '命中': '是否找到标记（逻辑型）', '层级': '向上查找的层数（整数）', '命中路径': '命中的标记路径；未命中为空文本', '起点': '解析后的起点绝对路径'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.查找祖先目录()
- 依赖能力：无
### 查找唯一匹配（`文件系统支持库.文本补丁.查找唯一匹配`）
- 版本：2.0.2
- 一句话说明：在文本中定位旧文本的唯一位置；未找到/不唯一/摘要不符均失败
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文本:文本型, 旧文本:文本型, 起始行:整数型, 结束行:整数型, 预期旧文本摘要:文本型
- 必填参数：文本, 旧文本
- 可选参数：起始行, 结束行, 预期旧文本摘要
- 返回结构：{'类型': '结果型', '值结构': {'起始偏移': '命中起始字符偏移（整数）', '结束偏移': '命中结束字符偏移（整数）', '起始行': '命中起始行号（1 起）', '结束行': '命中结束行号（含）', '旧文本摘要': '旧文本 SHA256 十六进制摘要', '匹配数': '命中数（成功恒为 1）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文本补丁.查找唯一匹配()
- 依赖能力：无
### 应用精确替换（`文件系统支持库.文本补丁.应用精确替换`）
- 版本：2.0.2
- 一句话说明：唯一匹配校验通过后替换，可选原子写入，并返回统一差异预览；声明根目录内路径边界校验（越界返回 路径越界）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型, 旧文本:文本型, 新文本:文本型, 根目录:文本型, 起始行:整数型, 结束行:整数型, 预期旧文本摘要:文本型, 预期文件摘要:文本型, 写入:逻辑型
- 必填参数：文件路径, 旧文本, 新文本
- 可选参数：根目录, 起始行, 结束行, 预期旧文本摘要, 预期文件摘要, 写入
- 返回结构：{'类型': '结果型', '值结构': {'文件路径': '目标文件绝对路径', '相对路径': '相对 根目录 的路径（缺省为文件名）', '已写入': '是否落盘（逻辑型；写入=假 时为假）', '起始偏移': '命中起始字符偏移（整数）', '结束偏移': '命中结束字符偏移（整数）', '起始行': '命中起始行号', '结束行': '命中结束行号', '旧文本摘要': '旧文本 SHA256', '旧文件摘要': '替换前整文件 SHA256', '新文件摘要': '替换后整文件 SHA256', '差异': 'unified_diff 文本', '差异行数': '差异行数（整数）', '新增行数': '新增行数（整数）', '删除行数': '删除行数（整数）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文本补丁.应用精确替换()
- 依赖能力：无
### 解析代码补丁（`文件系统支持库.文本补丁.解析代码补丁`）
- 版本：2.0.2
- 一句话说明：解析 Codex 风格补丁并计算各文件新内容（不落盘）；支持 更新/创建/删除/改名
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：根目录:文本型, 补丁文本:文本型
- 必填参数：根目录, 补丁文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'操作列表': '列表，每项含 操作/相对路径/文件路径/已变更/旧文件摘要/新文件摘要/新内容', '操作数': '补丁操作数（整数）', '变更数': '实际变更的操作数（整数）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文本补丁.解析代码补丁()
- 依赖能力：无
