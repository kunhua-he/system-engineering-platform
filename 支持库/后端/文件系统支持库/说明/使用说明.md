# 文件系统支持库（支持库.后端.文件系统支持库）

- 类型：支持库
- 版本：2.0.2
- 说明：聚合支持库，包含 1 个子域：文件操作
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 读取文件（`文件系统支持库.文件操作.读取文件`）
- 版本：2.0.1
- 一句话说明：按文本方式读取文件内容
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型, 编码:文本型
- 必填参数：文件路径
- 可选参数：编码
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.读取文件()
- 依赖能力：无
### 写入文件（`文件系统支持库.文件操作.写入文件`）
- 版本：2.0.1
- 一句话说明：按文本方式写入文件（自动创建父目录）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型, 内容:文本型, 编码:文本型
- 必填参数：文件路径, 内容
- 可选参数：编码
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.写入文件()
- 依赖能力：无
### 判断存在（`文件系统支持库.文件操作.判断存在`）
- 版本：2.0.1
- 一句话说明：判断路径是否存在并返回逻辑型
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：逻辑型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.判断存在()
- 依赖能力：无
### 列出目录（`文件系统支持库.文件操作.列出目录`）
- 版本：2.0.1
- 一句话说明：列出目录下条目名称（排序稳定）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：目录路径:文本型
- 必填参数：目录路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.列出目录()
- 依赖能力：无
### 删除文件（`文件系统支持库.文件操作.删除文件`）
- 版本：2.0.1
- 一句话说明：删除文件或空目录（不存在视为幂等成功）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.删除文件()
- 依赖能力：无
### 复制文件（`文件系统支持库.文件操作.复制文件`）
- 版本：2.0.1
- 一句话说明：复制文件到目标路径（自动创建父目录）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标路径:文本型
- 必填参数：源路径, 目标路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.复制文件()
- 依赖能力：无
### 移动文件（`文件系统支持库.文件操作.移动文件`）
- 版本：2.0.1
- 一句话说明：移动文件或目录到目标路径（自动创建父目录）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标路径:文本型
- 必填参数：源路径, 目标路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.移动文件()
- 依赖能力：无
### 获取大小（`文件系统支持库.文件操作.获取大小`）
- 版本：2.0.1
- 一句话说明：获取文件大小（字节）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.获取大小()
- 依赖能力：无
### 获取修改时间（`文件系统支持库.文件操作.获取修改时间`）
- 版本：2.0.1
- 一句话说明：获取文件修改时间戳（秒）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：文件路径:文本型
- 必填参数：文件路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.获取修改时间()
- 依赖能力：无
### 创建目录（`文件系统支持库.文件操作.创建目录`）
- 版本：2.0.1
- 一句话说明：创建目录（已存在视为幂等成功）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：目录路径:文本型, 递归:逻辑型
- 必填参数：目录路径
- 可选参数：递归
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.创建目录()
- 依赖能力：无
### 读取二进制文件（`文件系统支持库.文件操作.读取二进制文件`）
- 版本：2.0.1
- 一句话说明：在受控根目录内按相对路径读取二进制文件（路径边界校验 + 大小上限）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：受控根目录:文本型, 相对路径:文本型, 最大字节数:整数型
- 必填参数：受控根目录, 相对路径
- 可选参数：最大字节数
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.读取二进制文件()
- 依赖能力：无
### 读取文件头部字节（`文件系统支持库.文件操作.读取文件头部字节`）
- 版本：2.0.1
- 一句话说明：在受控根目录内按相对路径只读文件头部 N 字节
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：受控根目录:文本型, 相对路径:文本型, 字节数:整数型
- 必填参数：受控根目录, 相对路径
- 可选参数：字节数
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.读取文件头部字节()
- 依赖能力：无
### 登记临时资源（`文件系统支持库.文件操作.登记临时资源`）
- 版本：2.0.1
- 一句话说明：登记一个临时资源（文件或目录）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.登记临时资源()
- 依赖能力：无
### 清理全部临时资源（`文件系统支持库.文件操作.清理全部临时资源`）
- 版本：2.0.1
- 一句话说明：清理已登记临时资源（不存在视为幂等成功）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径前缀:文本型
- 必填参数：无
- 可选参数：路径前缀
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.清理全部临时资源()
- 依赖能力：无
### 搜索文件（`文件系统支持库.文件操作.搜索文件`）
- 版本：2.0.1
- 一句话说明：按通配符搜索文件（可递归）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：目录:文本型, 通配符:文本型, 递归:逻辑型
- 必填参数：目录, 通配符
- 可选参数：递归
- 返回结构：{文件数, 文件列表}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.搜索文件()
- 依赖能力：无
### 追加写入（`文件系统支持库.文件操作.追加写入`）
- 版本：2.0.1
- 一句话说明：追加文本到文件末尾
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径:文本型, 内容:文本型
- 必填参数：路径, 内容
- 可选参数：无
- 返回结构：{路径, 追加字节数}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.追加写入()
- 依赖能力：无
### 压缩文件（`文件系统支持库.文件操作.压缩文件`）
- 版本：2.0.1
- 一句话说明：压缩文件/目录为 zip
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标路径:文本型
- 必填参数：源路径, 目标路径
- 可选参数：无
- 返回结构：{目标路径, 条目数}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.压缩文件()
- 依赖能力：无
### 解压文件（`文件系统支持库.文件操作.解压文件`）
- 版本：2.0.1
- 一句话说明：解压 zip 到目录（防路径穿越）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：源路径:文本型, 目标目录:文本型
- 必填参数：源路径, 目标目录
- 可选参数：无
- 返回结构：{目标目录, 条目数}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.解压文件()
- 依赖能力：无
### 获取文件权限（`文件系统支持库.文件操作.获取文件权限`）
- 版本：2.0.1
- 一句话说明：获取文件权限（读/写/执行）
- 所属支持库或模块：支持库.后端.文件系统支持库
- 参数列表：路径:文本型
- 必填参数：路径
- 可选参数：无
- 返回结构：{路径, 权限, 可读, 可写, 可执行}（统一结果：成功/值/错误码/错误说明）
- 错误码：无
- 调用示例：文件系统支持库.文件操作.获取文件权限()
- 依赖能力：无
