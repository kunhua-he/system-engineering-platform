# 内容检索 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.后端.文件系统支持库.内容检索
- 版本：1.1.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 文件系统支持库.内容检索.正则搜索 | 正则搜索 | 1.0.0 |  | 按正则搜索目录内文件内容（优先 rg，回退 grep）；返回 {引擎, 搜索根, 匹配列表, 匹配数, 已达上限, 命令} |

## 文件系统支持库.内容检索.正则搜索（正则搜索）

**说明**：按正则搜索目录内文件内容（优先 rg，回退 grep）；返回 {引擎, 搜索根, 匹配列表, 匹配数, 已达上限, 命令}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 根目录 | 文本型 | 是 | None | 搜索根目录（绝对路径） |
| 模式 | 文本型 | 是 | None | 正则表达式 |
| 路径 | 文本型 | 否 |  | 相对根目录的子路径 |
| 文件模式 | 文本型 | 否 |  | 文件名通配 |
| 最大匹配数 | 整数型 | 否 | 30 | 上限（硬上限 200） |
| 上下文 | 整数型 | 否 | 0 | 上下文行数 0-3 |
| 忽略大小写 | 逻辑型 | 否 | False | 默认 假 |
| 排除列表 | 列表型 | 否 | None | 排除项 |

**返回结构**：（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
文件系统支持库.内容检索.正则搜索(根目录=值, 模式=值, 路径=值, 文件模式=值, 最大匹配数=值, 上下文=值, 忽略大小写=值, 排除列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：未纳入验证场景（暂无验证场景引用）
