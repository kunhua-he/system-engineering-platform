# 内容检索 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 包id：支持库.后端.文件系统支持库.内容检索
- 版本：1.1.3
- 说明：按正则表达式搜索目录内文件内容（优先 rg，回退 grep）
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 正则搜索（`文件系统支持库.内容检索.正则搜索`）
- 版本：1.0.0
- 一句话说明：按正则搜索目录内文件内容（优先 rg，回退 grep）；返回 {引擎, 搜索根, 匹配列表, 匹配数, 已达上限, 命令}
- 所属支持库或模块：支持库.后端.文件系统支持库.内容检索
- 参数列表：根目录:文本型, 模式:文本型, 路径:文本型, 文件模式:文本型, 最大匹配数:整数型, 上下文:整数型, 忽略大小写:逻辑型, 排除列表:列表型, 紧凑返回:逻辑型
- 必填参数：根目录, 模式
- 可选参数：路径, 文件模式, 最大匹配数, 上下文, 忽略大小写, 排除列表, 紧凑返回
- 返回结构：{'类型': '结果型', '值结构': {'引擎': '实际使用的检索引擎（rg 或 grep）', '搜索根': '相对根目录的搜索起点（根目录本身为 .）', '匹配列表': '列表，每项 {文件, 行号, 文本}', '匹配数': '匹配条数（整数）', '已达上限': '匹配数是否达到上限（逻辑型）', '命令': '实际执行的命令列表'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在, 超时, 执行失败, 检索失败
- 调用示例：文件系统支持库.内容检索.正则搜索(路径=, 文件模式=, 最大匹配数=30, 上下文=0, 忽略大小写=False, 紧凑返回=False)
- 依赖能力：无
## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 文件系统支持库.内容检索.正则搜索 | 正则搜索 | 1.0.0 | {'类型': '结果型', '值结构': {'引擎': '实际使用的检索引擎（rg 或 grep）', '搜索根': '相对根目录的搜索起点（根目录本身为 .）', '匹配列表': '列表，每项 {文件, 行号, 文本, 是否命中}（逻辑型；假=该行是 上下文 带出的上下文行）', '匹配数': '命中条数（整数，不含上下文行）', '上下文数': '本次生效的上下文行数（整数 0-3）', '已达上限': '匹配数是否达到上限（逻辑型）', '命中行': '紧凑档（紧凑返回=真）专用：列表，每条 `文件:行号:文本`（命中行）或 `文件-行号-文本`（上下文行，与 rg -C 同约定）；紧凑档下 匹配列表 与 命令 都不出现', '命令': '默认档（紧凑返回=假）专用：实际执行的命令列表；紧凑档下不出现'}} | 按正则搜索目录内文件内容（优先 rg，回退 grep）；返回 {引擎, 搜索根, 匹配列表, 匹配数, 上下文数, 已达上限, 命令}；上下文 大于 0 时命中行前后各 上下文 行一并回在 匹配列表（是否命中=假）；紧凑返回=真 时返回 {引擎, 搜索根, 命中行, 匹配数, 上下文数, 已达上限}（去掉 命令 回显、把匹配列表压成 命中行），默认 假 与改前逐字节一致 |

## 文件系统支持库.内容检索.正则搜索（正则搜索）

**说明**：按正则搜索目录内文件内容（优先 rg，回退 grep）；返回 {引擎, 搜索根, 匹配列表, 匹配数, 上下文数, 已达上限, 命令}；上下文 大于 0 时命中行前后各 上下文 行一并回在 匹配列表（是否命中=假）；紧凑返回=真 时返回 {引擎, 搜索根, 命中行, 匹配数, 上下文数, 已达上限}（去掉 命令 回显、把匹配列表压成 命中行），默认 假 与改前逐字节一致

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 根目录 | 文本型 | 是 | None | 搜索根目录（绝对路径） |
| 模式 | 文本型 | 是 | None | 正则表达式 |
| 路径 | 文本型 | 否 |  | 相对根目录的子路径 |
| 文件模式 | 文本型 | 否 |  | 文件名通配 |
| 最大匹配数 | 整数型 | 否 | 30 | 上限（硬上限 200） |
| 上下文 | 整数型 | 否 | 0 | 上下文行数 0-3；大于 0 时命中行前后各 N 行一并回在 匹配列表，以 是否命中=假 标记，且不占 最大匹配数 额度 |
| 忽略大小写 | 逻辑型 | 否 | False | 默认 假 |
| 排除列表 | 列表型 | 否 | None | 排除项；不传用默认噪声目录表 |
| 紧凑返回 | 逻辑型 | 否 | False | 真 = 紧凑档：去掉 命令 回显（实测占 27%）并把 匹配列表 压成 命中行（每条 文件:行号:文本，上下文行用 文件-行号-文本），命中信息一个不少（实测 16 命中：2,635 → 1,312 字符，降 50.2%）；默认 假 = 与改前逐字节一致 |

**返回结构**：{'类型': '结果型', '值结构': {'引擎': '实际使用的检索引擎（rg 或 grep）', '搜索根': '相对根目录的搜索起点（根目录本身为 .）', '匹配列表': '列表，每项 {文件, 行号, 文本, 是否命中}（逻辑型；假=该行是 上下文 带出的上下文行）', '匹配数': '命中条数（整数，不含上下文行）', '上下文数': '本次生效的上下文行数（整数 0-3）', '已达上限': '匹配数是否达到上限（逻辑型）', '命中行': '紧凑档（紧凑返回=真）专用：列表，每条 `文件:行号:文本`（命中行）或 `文件-行号-文本`（上下文行，与 rg -C 同约定）；紧凑档下 匹配列表 与 命令 都不出现', '命令': '默认档（紧凑返回=假）专用：实际执行的命令列表；紧凑档下不出现'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 超时 |
| 执行失败 |
| 检索失败 |

**调用示例**：

```python
文件系统支持库.内容检索.正则搜索(根目录=值, 模式=值, 路径=值, 文件模式=值, 最大匹配数=值, 上下文=值, 忽略大小写=值, 排除列表=值)
文件系统支持库.内容检索.正则搜索(根目录=值, 模式=值, 路径=值, 文件模式=值, 最大匹配数=值, 上下文=值, 忽略大小写=值, 排除列表=值, 紧凑返回=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景.json（未登记场景id）
