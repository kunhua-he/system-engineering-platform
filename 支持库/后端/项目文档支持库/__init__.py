"""支持库.后端.项目文档支持库 包级中文入口。

调用者只从此入口导入，禁止深入 实现/ 目录。
公开能力：切块 / 校验规范 / 建索引库 / 写入索引 / 查询索引 / 登记项目 / 查询项目 / 解析清单 / 改清单条目 / 校验清单 /
扫描文档集合 / 校验集合判据（集合级跨文档判据）/ 建全仓索引（一趟索引入库）
"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.项目文档支持库.实现.切块 import 切块
from 支持库.后端.项目文档支持库.实现.规范校验 import 校验规范
from 支持库.后端.项目文档支持库.实现.索引库 import 建索引库, 写入索引, 查询索引
from 支持库.后端.项目文档支持库.实现.未命中词 import 登记未命中词, 列出未命中词
from 支持库.后端.项目文档支持库.实现.项目登记 import 登记项目, 查询项目
from 支持库.后端.项目文档支持库.实现.清单 import 解析清单
from 支持库.后端.项目文档支持库.实现.清单改 import 改清单条目
from 支持库.后端.项目文档支持库.实现.清单校验 import 校验清单
from 支持库.后端.项目文档支持库.实现.集合扫描 import 扫描文档集合
from 支持库.后端.项目文档支持库.实现.集合校验 import 校验集合判据
from 支持库.后端.项目文档支持库.实现.全仓索引 import 建全仓索引
from 支持库.后端.项目文档支持库.实现.坑条目写入 import 建坑索引, 记踩坑
from 支持库.后端.项目文档支持库.实现.开工上下文 import 查开工上下文
from 支持库.后端.项目文档支持库.实现.哲学条款 import 查哲学条款

__all__ = [
    "切块",
    "校验规范",
    "建索引库",
    "写入索引",
    "查询索引",
    "登记项目",
    "查询项目",
    "解析清单",
    "改清单条目",
    "校验清单",
    "扫描文档集合",
    "校验集合判据",
    "建全仓索引",
    "建坑索引",
    "记踩坑",
    "查开工上下文",
    "查哲学条款",
    "登记未命中词",
    "列出未命中词",
    "注册能力",
]


def _文本(名称: str, 必填: bool, 说明: str, 默认=None) -> dict:
    return {"名称": 名称, "类型": "文本型", "必填": 必填, "默认值": 默认, "说明": 说明}


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现

    for 能力id, 函数, 参数表, 说明 in [
        ("项目文档支持库.切块", 切块, [
            _文本("正文", True, "文档正文文本（非空）"),
        ], "把项目文档正文按 Markdown 结构切成有序块（标题/段落/代码/表格/列表/引用），每块附起始行与结束行（行号自 1 起）；代码块内的井号不算标题。"),
        ("项目文档支持库.校验规范", 校验规范, [
            _文本("正文", True, "待校验的文档正文"),
            _文本("判据文件", True, "规范判据 JSON 文件绝对路径"),
            {"名称": "是否权威文档", "类型": "逻辑型", "必填": 假, "默认值": 假,
             "说明": "真=按权威文档口径校验（含元信息头要求）"},
        ], "按规范判据文件逐条校验文档正文，返回违规范条目清单（判据id/严重级/说明/行号）；无判据文件时明确失败，不假装通过。"),
        ("项目文档支持库.建索引库", 建索引库, [
            _文本("库文件", False, "库文件绝对路径；留空取运行数据根/项目文档库.db"),
        ], "初始化项目文档索引库（项目登记表/文档索引表/块索引表及高频索引），重复调用保持幂等。"),
        ("项目文档支持库.写入索引", 写入索引, [
            _文本("项目名", True, "项目名（项目登记表主键）"),
            _文本("文档相对路径", True, "相对项目文档根的路径"),
            {"名称": "块列表", "类型": "列表型", "必填": 真, "默认值": None,
             "说明": "切块结果列表（含 起始行/结束行/块类型/块文本）"},
            {"名称": "文件修改时间", "类型": "双精度数型", "必填": 假, "默认值": 0,
             "说明": "源文件修改时间戳（秒）"},
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "写入或更新一个文档的索引（文档级 + 块级），同项目同路径采用覆盖更新；库表不存在时自动初始化。"),
        ("项目文档支持库.查询索引", 查询索引, [
            _文本("项目名", True, "项目名"),
            _文本("关键词", False, "查询关键词（空白分隔多词，全命中优先）；与 搜索词 至少给一个"),
            {"名称": "返回条数", "类型": "整数型", "必填": 假, "默认值": 20, "说明": "返回上限"},
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
            _文本("细节级别", False, "标题/简介(默认)/内容/完整 四档；默认档与改前返回逐字段兼容", "简介"),
            {"名称": "搜索词", "类型": "列表型", "必填": 假, "默认值": None,
             "说明": "候选词组列表，如 [[\"开通\",\"流程\"],[\"签约\",\"步骤\"]]；各组独立检索打分，只返回评分最高的一组。传了它 关键词 只原样带回不参与检索"},
            {"名称": "截断上限", "类型": "整数型", "必填": 假, "默认值": 0,
             "说明": "内容/完整档下块文本最大字符数；0 或负 = 不截断"},
        ], "按项目名与关键词查询块索引（大小写不敏感子串匹配）。支持四档细节级别（标题/简介/内容/完整，默认档与改前逐字段兼容）、多组搜索词择优（各组独立打分取最高）、以及零命中组经「未命中词」表自动扩词（只读，只对零命中的那一组生效）。"),
        ("项目文档支持库.登记项目", 登记项目, [
            _文本("项目名", True, "项目名"),
            _文本("项目根目录", True, "项目绝对路径"),
            _文本("文档根相对路径", False, "相对项目根的文档根", "开发文档"),
            _文本("规范文件", False, "规范文件相对项目根的路径", ""),
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "把一个项目写进登记表（项目名/项目根目录/文档根相对路径/规范文件），同项目名覆盖更新（幂等）；不校验目录是否真实存在（那是模块层的边界校验）。"),
        ("项目文档支持库.查询项目", 查询项目, [
            _文本("项目名", False, "留空列出全部", ""),
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "查询已登记项目：给项目名单查，留空列出全部（按登记时间）。"),
        ("项目文档支持库.解析清单", 解析清单, [
            _文本("正文", True, "清单正文文本（非空）"),
        ], "把 markdown 表格清单解析成结构化条目（表头/条目列表/条目数，每条目含行号/列值/原始行）；识别表头行与分隔行，非表格文本如实报告为 0 条，不假装解析成功。"),
        ("项目文档支持库.改清单条目", 改清单条目, [
            _文本("正文", True, "清单正文文本（非空）"),
            _文本("操作", True, "增 / 删 / 改"),
            _文本("定位", True, "删/改＝目标编号；增＝某编号之后（与插入位置二选一）", ""),
            _文本("条目", False, "增/改的新条目：竖线分隔文本或列值列表，须自带编号"),
            _文本("插入位置", False, "增条目位置：「尾部」或某编号（插在该编号之后）", "尾部"),
        ], "对 markdown 表格清单增/删/改一条条目并返回新正文（纯文本变换，不留状态）；不自动排序、不自动生成编号，编号必须由调用方给，重复或缺编号明确失败。"),
        ("项目文档支持库.扫描文档集合", 扫描文档集合, [
            _文本("根目录", True, "扫描根目录绝对路径"),
            _文本("文件名模式", False, "文件名 glob 模式", "*.md"),
            {"名称": "豁免目录", "类型": "列表型", "必填": 假, "默认值": None,
             "说明": "豁免片段列表；可写相对仓库根的全路径（自动剥去与扫描根同名的首段）"},
            {"名称": "是否读正文", "类型": "逻辑型", "必填": 假, "默认值": 假,
             "说明": "真=返回正文（判据需要时用）"},
            {"名称": "最大文档数", "类型": "整数型", "必填": 假, "默认值": 5000,
             "说明": "超出则明确失败，不静默截断"},
        ], "按根目录与文件名模式扫出一批文档（只读、稳定排序）：返回相对路径/行数/字节数/修改时间，可选读正文；豁免目录按路径片段逐段比较（防子串误伤）；命中数超出上限时明确失败不静默截断。"),
        ("项目文档支持库.校验集合判据", 校验集合判据, [
            _文本("根目录", True, "扫描根目录绝对路径"),
            _文本("判据文件", True, "集合判据 JSON 绝对路径"),
            _文本("文件名模式", False, "留空取判据文件里的声明", ""),
            {"名称": "豁免目录", "类型": "列表型", "必填": 假, "默认值": None,
             "说明": "留空取判据文件里的声明；传了则覆盖"},
        ], "按集合判据文件校验一批文档的跨文档关系（全仓唯一/引用可达/跨文档配对/状态枚举），返回违规条目（判据id/严重级/说明/文件/行号）；判据来自数据文件，规范改一句只改数据；判据文件不可读时明确失败，不假装通过。"),
        ("项目文档支持库.建全仓索引", 建全仓索引, [
            _文本("项目名", True, "项目唯一标识，与 查询索引 的项目名一致"),
            _文本("根目录", True, "扫描根目录绝对路径"),
            _文本("文件名模式", False, "文件名 glob 模式", "*.md"),
            {"名称": "豁免目录", "类型": "列表型", "必填": 假, "默认值": None,
             "说明": "豁免片段列表，如 [\"工程缓存\",\".git\"]"},
            {"名称": "是否含包能力", "类型": "逻辑型", "必填": 假, "默认值": 真,
             "说明": "真=把正式三层各包的 能力定义.json 渲染成能力清单一并入索引"},
            _文本("库文件", False, "索引库绝对路径；留空取默认"),
        ], "一趟索引：扫一批文档 + 正式三层各包的能力清单，逐份切块并写入索引库（只索引、不改文档）。供「动手前先搜全仓有没有既有承载」用；建好后由 查询索引 按关键词检索。单份失败逐份记入 跳过清单，不静默、不整体失败、不做语义判断（判断归调用方）。"),
        ("项目文档支持库.建坑索引", 建坑索引, [
            _文本("项目名", True, "项目名（坑条目归属的项目）"),
            _文本("条目文件", True, "踩坑条目 JSON 绝对路径"),
            {"名称": "是否重建", "类型": "逻辑型", "必填": 假, "默认值": 假,
             "说明": "真=先清空该项目旧条目再灌（喂料刷新用）"},
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "把「踩坑条目」数据文件灌进坑条目表（喂料端，批量）：条目是数据、改一条只改数据；五键齐全才收（场景id/症状/根因/判据/原文位置），缺键或状态非法时明确失败不静默跳过。"),
        ("项目文档支持库.记踩坑", 记踩坑, [
            _文本("项目名", True, "项目名"),
            _文本("场景id", True, "在做什么时踩的坑（如「写验证场景」）"),
            _文本("症状", True, "看到的报错原文或现象"),
            _文本("根因", True, "真因（不猜、按现场取证写）"),
            _文本("判据", True, "一条可执行命令或可查的真源位置"),
            _文本("原文位置", True, "细节所在文件#锚点（索引跳转，不搬正文）"),
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "把刚踩的一个坑结构化落盘（状态机入口，单条）：五键齐全才收，新条目落在「草稿」态，被检索命中后自动推进状态。"),
        ("项目文档支持库.查开工上下文", 查开工上下文, [
            _文本("项目名", True, "项目名"),
            _文本("本次任务", False, "我正要做什么（自然语言；留空则按状态与命中次数给基线排序）", ""),
            {"名称": "返回条数", "类型": "整数型", "必填": 假, "默认值": 5, "说明": "返回上限（建议 3-5 条）"},
            _文本("只列状态", False, "留空=可检索三态（草稿/已用/已固化）；可传如「已固化」单查", ""),
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "按「我正要做什么」检索历史踩坑条目（**不全量载入**）：返回相关坑的症状/根因/判据/原文位置，已固化项附「已由判据强制，不必读原文」提示；命中即推进状态机，已失效条目不返回但如实上报条数。"),
        ("项目文档支持库.查哲学条款", 查哲学条款, [
            _文本("本次任务", False, "我正要做什么（自然语言；留空＝列该主条全部条款）", ""),
            _文本("主条号", False, "只要某一主条（1-14）；留空＝全 14 条范围检索", ""),
            {"名称": "返回条数", "类型": "整数型", "必填": 假, "默认值": 5, "说明": "返回上限（建议 3-5 条）"},
        ], "按「我正要做什么」检索设计哲学条款（**不载入 1065 行全文、不 rg 全文搜**）：中文双字滑窗纯字符串匹配，返回命中的几条第 1 句结论 + 判据 + 原文位置，细节按原文位置跳转；未命中如实返回空，不捏造条款。"),
        ("项目文档支持库.校验清单", 校验清单, [
            _文本("正文", True, "清单正文文本（非空）"),
            _文本("必填列", False, "必填列名列表或逗号分隔列名"),
            _文本("编号列", False, "编号所在列名；表头无此列时退化为第一列并报「编号列缺失」", "编号"),
            _文本("编号模式", False, "可选编号正则（如 ^\\d{4}$）；留空只判编号空值与唯一性"),
        ], "校验 markdown 表格清单：编号唯一性、编号格式（模式由调用方给）、必填列非空、表格结构完整（缺列）；非表格文本如实报「非表格正文」并判不通过。"),
        ("项目文档支持库.登记未命中词", 登记未命中词, [
            _文本("项目名", True, "项目名"),
            _文本("原始词", True, "搜不到的那个词"),
            _文本("建议词", True, "该改用哪个词才能搜到（必填：只登记「没搜到」无法用于扩词）"),
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "登记「搜不到的词 → 该搜什么」的实测映射（UPSERT，幂等）。供 查询索引 在零命中时自动扩词；建议词必填且不得等于原始词。"),
        ("项目文档支持库.列出未命中词", 列出未命中词, [
            _文本("项目名", True, "项目名"),
            _文本("库文件", False, "库文件绝对路径；留空取默认"),
        ], "列出该项目的未命中词映射（按命中次数降序、原始词升序稳定排序），供 Agent 一次补齐建议词，不必逐词重新摸索。"),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.项目文档支持库",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明=说明,
            )
        )
