"""支持库.流式推理 包级中文入口。"""

from __future__ import annotations

from 支持库.后端.流式推理.实现.流式推理 import 提交流式请求
from 支持库.后端.流式推理.实现.流式推理 import 消费事件
from 支持库.后端.流式推理.实现.流式推理 import 关闭流

__all__ = [
    "提交流式请求",
    "消费事件",
    "关闭流",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("流式推理.提交流式请求", 提交流式请求, [{"名称": "模型连接句柄", "类型": "文本型", "必填": True, "默认值": None, "说明": "模型连接句柄"}, {"名称": "消息列表", "类型": "列表型", "必填": True, "默认值": None, "说明": "消息列表"}, {"名称": "系统提示词", "类型": "文本型", "必填": False, "默认值": None, "说明": "系统提示词"}]),
        ("流式推理.消费事件", 消费事件, [{"名称": "流id", "类型": "文本型", "必填": True, "默认值": None, "说明": "流id"}, {"名称": "批次大小", "类型": "整数型", "必填": False, "默认值": 10, "说明": "批次大小"}]),
        ("流式推理.关闭流", 关闭流, [{"名称": "流id", "类型": "文本型", "必填": True, "默认值": None, "说明": "流id"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.流式推理",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="流式推理支持库",
            )
        )
