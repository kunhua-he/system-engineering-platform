# Pillow提供者（支持库.后端.图像处理支持库.图像解码）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.3
- 说明：Pillow（PIL，C 原生扩展）独立提供者：解码图像（格式/尺寸/模式）/像素统计/生成占位图（纯色/渐变/文本）/生成缩略图/图像EXIF转置/透明背景合成/计算感知哈希/缩放图像/重编码图像。原生扩展强制独立子进程，主进程零加载 PIL。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 解码图像（`图像处理支持库.图像解码.解码图像`）
- 版本：1.0.0
- 一句话说明：Pillow 打开并完整解码图像字节：返回 {格式, 宽度, 高度, 模式}；无法识别 → 格式未知，数据损坏 → 文件损坏，像素超限 → 超大
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 超时秒:双精度数型
- 必填参数：字节
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.解码图像(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==)
- 依赖能力：无
### 像素统计（`图像处理支持库.图像解码.像素统计`）
- 版本：1.0.0
- 一句话说明：统计图像像素：返回 {宽度, 高度, 像素数, 平均颜色{红,绿,蓝}}；像素超限 → 超大
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 超时秒:双精度数型
- 必填参数：字节
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.像素统计(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==)
- 依赖能力：无
### 生成占位图（`图像处理支持库.图像解码.生成占位图`）
- 版本：1.0.0
- 一句话说明：生成占位图（纯色/渐变/文本）PNG 字节 base64：返回 {图像b64, 格式, 宽度, 高度}
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：宽度:整数型, 高度:整数型, 占位类型:文本型, 背景颜色:文本型, 前景颜色:文本型, 文本:文本型, 超时秒:双精度数型
- 必填参数：宽度, 高度, 占位类型
- 可选参数：背景颜色, 前景颜色, 文本, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.生成占位图(宽度=64, 高度=32, 占位类型=纯色, 背景颜色=#CCCCCC, 前景颜色=#333333, 文本=, 超时秒=60.0)
- 依赖能力：无
### 生成缩略图（`图像处理支持库.图像解码.生成缩略图`）
- 版本：1.0.0
- 一句话说明：按最大边长生成等比例缩略图（只缩不放大）：返回 {图像b64, 格式, 宽度, 高度}；无法识别 → 格式未知，数据损坏 → 文件损坏，像素超限 → 超大
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 最大边长:整数型, 超时秒:双精度数型
- 必填参数：字节, 最大边长
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.生成缩略图(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==, 最大边长=32, 超时秒=60.0)
- 依赖能力：无
### 图像EXIF转置（`图像处理支持库.图像解码.图像EXIF转置`）
- 版本：1.0.0
- 一句话说明：按 EXIF orientation 转置图像方向：返回 {图像b64, 格式, 宽度, 高度}；无 EXIF 方向时原样返回
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 超时秒:双精度数型
- 必填参数：字节
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.图像EXIF转置(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==, 超时秒=60.0)
- 依赖能力：无
### 透明背景合成（`图像处理支持库.图像解码.透明背景合成`）
- 版本：1.0.0
- 一句话说明：RGBA/LA/P 透明图像与背景色合成（输出 RGB PNG）：返回 {图像b64, 格式, 宽度, 高度}
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 背景颜色:文本型, 超时秒:双精度数型
- 必填参数：字节, 背景颜色
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.透明背景合成(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==, 背景颜色=#FFFFFF, 超时秒=60.0)
- 依赖能力：无
### 计算感知哈希（`图像处理支持库.图像解码.计算感知哈希`）
- 版本：1.0.0
- 一句话说明：计算感知哈希（aHash/dHash/pHash）：返回 {哈希, 哈希类型}；确定性：同输入同哈希；差异性：不同图像哈希不同
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 哈希类型:文本型, 超时秒:双精度数型
- 必填参数：字节, 哈希类型
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.计算感知哈希(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==, 哈希类型=aHash, 超时秒=60.0)
- 依赖能力：无
### 缩放图像（`图像处理支持库.图像解码.缩放图像`）
- 版本：1.0.0
- 一句话说明：精确缩放图像（宽度/高度至少一个，缺省一侧按纵横比推算）：返回 {图像b64, 格式, 宽度, 高度}
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 宽度:整数型, 高度:整数型, 超时秒:双精度数型
- 必填参数：字节
- 可选参数：宽度, 高度, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.缩放图像(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==, 宽度=64, 超时秒=60.0)
- 依赖能力：无
### 重编码图像（`图像处理支持库.图像解码.重编码图像`）
- 版本：1.0.0
- 一句话说明：重编码图像为 JPEG/PNG/WebP（JPEG/WebP 质量 1-100，PNG 无损）：返回 {图像b64, 格式, 宽度, 高度}
- 所属支持库或模块：支持库.后端.图像处理支持库.图像解码
- 参数列表：字节:字节集型, 格式:文本型, 质量:整数型, 超时秒:双精度数型
- 必填参数：字节
- 可选参数：格式, 质量, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 超大, 格式未知, 文件损坏, 超时, 提供者崩溃, 提供者不可用
- 调用示例：图像处理支持库.图像解码.重编码图像(字节=iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==, 格式=PNG, 质量=90, 超时秒=60.0)
- 依赖能力：无
