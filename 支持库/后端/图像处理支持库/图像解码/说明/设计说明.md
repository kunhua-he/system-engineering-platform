# Pillow提供者 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.图像处理支持库.图像解码
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 图像处理支持库.图像解码.解码图像 | 解码图像 | 1.0.0 | 结果型 | Pillow 打开并完整解码图像字节：返回 {格式, 宽度, 高度, 模式}；无法识别 → 格式未知，数据损坏 → 文件损坏，像素超限 → 超大 |
| 图像处理支持库.图像解码.像素统计 | 像素统计 | 1.0.0 | 结果型 | 统计图像像素：返回 {宽度, 高度, 像素数, 平均颜色{红,绿,蓝}}；像素超限 → 超大 |
| 图像处理支持库.图像解码.生成占位图 | 生成占位图 | 1.0.0 | 结果型 | 生成占位图（纯色/渐变/文本）PNG 字节 base64：返回 {图像b64, 格式, 宽度, 高度} |
| 图像处理支持库.图像解码.生成缩略图 | 生成缩略图 | 1.0.0 | 结果型 | 按最大边长生成等比例缩略图（只缩不放大）：返回 {图像b64, 格式, 宽度, 高度}；无法识别 → 格式未知，数据损坏 → 文件损坏，像素超限 → 超大 |
| 图像处理支持库.图像解码.图像EXIF转置 | 图像EXIF转置 | 1.0.0 | 结果型 | 按 EXIF orientation 转置图像方向：返回 {图像b64, 格式, 宽度, 高度}；无 EXIF 方向时原样返回 |
| 图像处理支持库.图像解码.透明背景合成 | 透明背景合成 | 1.0.0 | 结果型 | RGBA/LA/P 透明图像与背景色合成（输出 RGB PNG）：返回 {图像b64, 格式, 宽度, 高度} |
| 图像处理支持库.图像解码.计算感知哈希 | 计算感知哈希 | 1.0.0 | 结果型 | 计算感知哈希（aHash/dHash/pHash）：返回 {哈希, 哈希类型}；确定性：同输入同哈希；差异性：不同图像哈希不同 |
| 图像处理支持库.图像解码.缩放图像 | 缩放图像 | 1.0.0 | 结果型 | 精确缩放图像（宽度/高度至少一个，缺省一侧按纵横比推算）：返回 {图像b64, 格式, 宽度, 高度} |
| 图像处理支持库.图像解码.重编码图像 | 重编码图像 | 1.0.0 | 结果型 | 重编码图像为 JPEG/PNG/WebP（JPEG/WebP 质量 1-100，PNG 无损）：返回 {图像b64, 格式, 宽度, 高度} |

## 图像处理支持库.图像解码.解码图像（解码图像）

**说明**：Pillow 打开并完整解码图像字节：返回 {格式, 宽度, 高度, 模式}；无法识别 → 格式未知，数据损坏 → 文件损坏，像素超限 → 超大

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.解码图像(字节=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.像素统计（像素统计）

**说明**：统计图像像素：返回 {宽度, 高度, 像素数, 平均颜色{红,绿,蓝}}；像素超限 → 超大

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.像素统计(字节=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.生成占位图（生成占位图）

**说明**：生成占位图（纯色/渐变/文本）PNG 字节 base64：返回 {图像b64, 格式, 宽度, 高度}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 宽度 | 整数型 | 是 | None | 占位图宽度（像素） |
| 高度 | 整数型 | 是 | None | 占位图高度（像素） |
| 占位类型 | 文本型 | 是 | None | 纯色/渐变/文本 |
| 背景颜色 | 文本型 | 否 | #CCCCCC | #RRGGBB 背景色 |
| 前景颜色 | 文本型 | 否 | #333333 | #RRGGBB 前景色（文本/渐变起点色） |
| 文本 | 文本型 | 否 |  | 占位文本，缺省用 宽x高 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.生成占位图(宽度=值, 高度=值, 占位类型=值, 背景颜色=值, 前景颜色=值, 文本=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.生成缩略图（生成缩略图）

**说明**：按最大边长生成等比例缩略图（只缩不放大）：返回 {图像b64, 格式, 宽度, 高度}；无法识别 → 格式未知，数据损坏 → 文件损坏，像素超限 → 超大

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 最大边长 | 整数型 | 是 | None | 缩略图最大边长（像素），保持纵横比，超限才缩 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时，上限 60 秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.生成缩略图(字节=值, 最大边长=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.图像EXIF转置（图像EXIF转置）

**说明**：按 EXIF orientation 转置图像方向：返回 {图像b64, 格式, 宽度, 高度}；无 EXIF 方向时原样返回

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时，上限 60 秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.图像EXIF转置(字节=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.透明背景合成（透明背景合成）

**说明**：RGBA/LA/P 透明图像与背景色合成（输出 RGB PNG）：返回 {图像b64, 格式, 宽度, 高度}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 背景颜色 | 文本型 | 是 | None | #RRGGBB 背景色 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时，上限 60 秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.透明背景合成(字节=值, 背景颜色=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.计算感知哈希（计算感知哈希）

**说明**：计算感知哈希（aHash/dHash/pHash）：返回 {哈希, 哈希类型}；确定性：同输入同哈希；差异性：不同图像哈希不同

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 哈希类型 | 文本型 | 是 | None | aHash/dHash/pHash |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时，上限 60 秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.计算感知哈希(字节=值, 哈希类型=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.缩放图像（缩放图像）

**说明**：精确缩放图像（宽度/高度至少一个，缺省一侧按纵横比推算）：返回 {图像b64, 格式, 宽度, 高度}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 宽度 | 整数型 | 否 | None | 目标宽度（像素），缺省按纵横比推算 |
| 高度 | 整数型 | 否 | None | 目标高度（像素），缺省按纵横比推算 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时，上限 60 秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.缩放图像(字节=值, 宽度=值, 高度=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 图像处理支持库.图像解码.重编码图像（重编码图像）

**说明**：重编码图像为 JPEG/PNG/WebP（JPEG/WebP 质量 1-100，PNG 无损）：返回 {图像b64, 格式, 宽度, 高度}

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 字节 | 字节集型 | 是 | None | 图像字节内容（PNG/JPEG 等） |
| 格式 | 文本型 | 否 | PNG | JPEG/PNG/WebP |
| 质量 | 整数型 | 否 | 90 | JPEG/WebP 质量 1-100 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时，上限 60 秒 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 超大 |
| 格式未知 |
| 文件损坏 |
| 超时 |
| 提供者崩溃 |
| 提供者不可用 |

**调用示例**：

```python
图像处理支持库.图像解码.重编码图像(字节=值, 格式=值, 质量=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
