# 数值计算支持库（支持库.后端.数值计算支持库）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.1.2
- 说明：常用数值计算：四舍五入/取整/绝对值/最大/最小/随机/求和/平均（参考易语言数值计算支持库）
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 四舍五入（`数值计算支持库.四舍五入`）
- 版本：1.0.0
- 一句话说明：四舍五入到指定小数位
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值:双精度数型, 小数位:整数型
- 必填参数：数值
- 可选参数：小数位
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.四舍五入(小数位=0)
- 依赖能力：无
### 取整（`数值计算支持库.取整`）
- 版本：1.0.0
- 一句话说明：取整（向下/向上/四舍五入）
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值:双精度数型, 方式:文本型
- 必填参数：数值
- 可选参数：方式
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.取整(方式=四舍五入)
- 依赖能力：无
### 绝对值（`数值计算支持库.绝对值`）
- 版本：1.0.0
- 一句话说明：绝对值
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值:双精度数型
- 必填参数：数值
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.绝对值()
- 依赖能力：无
### 最大值（`数值计算支持库.最大值`）
- 版本：1.0.0
- 一句话说明：列表最大值
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值列表:列表型
- 必填参数：数值列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.最大值()
- 依赖能力：无
### 最小值（`数值计算支持库.最小值`）
- 版本：1.0.0
- 一句话说明：列表最小值
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值列表:列表型
- 必填参数：数值列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.最小值()
- 依赖能力：无
### 随机数（`数值计算支持库.随机数`）
- 版本：1.0.0
- 一句话说明：随机整数（可指定范围）
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：下限:整数型, 上限:整数型
- 必填参数：无
- 可选参数：下限, 上限
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.随机数(下限=0, 上限=100)
- 依赖能力：无
### 求和（`数值计算支持库.求和`）
- 版本：1.0.0
- 一句话说明：列表求和
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值列表:列表型
- 必填参数：数值列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.求和()
- 依赖能力：无
### 平均值（`数值计算支持库.平均值`）
- 版本：1.0.0
- 一句话说明：列表平均值
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值列表:列表型
- 必填参数：数值列表
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 计算失败
- 调用示例：数值计算支持库.平均值()
- 依赖能力：无
### 分位数（`数值计算支持库.分位数`）
- 版本：1.0.0
- 一句话说明：线性插值百分位数（保证单调 P50≤P95≤P99≤max）；返回 分位数值/样本数/最小/中位/最大
- 所属支持库或模块：支持库.后端.数值计算支持库
- 参数列表：数值列表:列表型, 百分位:双精度数型, 小数位:整数型
- 必填参数：数值列表, 百分位
- 可选参数：小数位
- 返回结构：{'类型': '结果型', '值结构': {'分位数值': '双精度数型（空列表为 None）', '样本数': '整数型', '百分位': '双精度数型', '最小值': '双精度数型', '中位数': '双精度数型', '最大值': '双精度数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数值计算支持库.分位数(小数位=3)
- 依赖能力：无
