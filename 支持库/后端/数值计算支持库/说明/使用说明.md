# 数值计算支持库 使用说明

- 包id：支持库.后端.数值计算支持库
- 版本：1.1.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数值计算支持库.四舍五入 | 四舍五入 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 四舍五入到指定小数位 |
| 数值计算支持库.取整 | 取整 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 取整（向下/向上/四舍五入） |
| 数值计算支持库.绝对值 | 绝对值 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 绝对值 |
| 数值计算支持库.最大值 | 最大值 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 列表最大值 |
| 数值计算支持库.最小值 | 最小值 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 列表最小值 |
| 数值计算支持库.随机数 | 随机数 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 随机整数（可指定范围） |
| 数值计算支持库.求和 | 求和 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 列表求和 |
| 数值计算支持库.平均值 | 平均值 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 列表平均值 |
| 数值计算支持库.分位数 | 分位数 | 1.0.0 |  | 线性插值百分位数（保证单调 P50≤P95≤P99≤max）；返回 分位数值/样本数/最小/中位/最大 |

## 数值计算支持库.四舍五入（四舍五入）

**说明**：四舍五入到指定小数位

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值 | 双精度数型 | 是 | None | 要处理的数值 |
| 小数位 | 整数型 | 否 | 0 | 保留小数位 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.四舍五入(数值=值, 小数位=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.取整（取整）

**说明**：取整（向下/向上/四舍五入）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值 | 双精度数型 | 是 | None | 要处理的数值 |
| 方式 | 文本型 | 否 | 四舍五入 | 向下/向上/四舍五入 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.取整(数值=值, 方式=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.绝对值（绝对值）

**说明**：绝对值

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值 | 双精度数型 | 是 | None | 要处理的数值 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.绝对值(数值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.最大值（最大值）

**说明**：列表最大值

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值列表 | 列表型 | 是 | None | 数值列表 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.最大值(数值列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.最小值（最小值）

**说明**：列表最小值

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值列表 | 列表型 | 是 | None | 数值列表 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.最小值(数值列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.随机数（随机数）

**说明**：随机整数（可指定范围）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 下限 | 整数型 | 否 | 0 | 随机下限 |
| 上限 | 整数型 | 否 | 100 | 随机上限 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.随机数(下限=值, 上限=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.求和（求和）

**说明**：列表求和

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值列表 | 列表型 | 是 | None | 数值列表 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.求和(数值列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.平均值（平均值）

**说明**：列表平均值

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值列表 | 列表型 | 是 | None | 数值列表 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 计算失败 |

**调用示例**：

```python
数值计算支持库.平均值(数值列表=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）

## 数值计算支持库.分位数（分位数）

**说明**：线性插值百分位数（保证单调 P50≤P95≤P99≤max）；返回 分位数值/样本数/最小/中位/最大

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 数值列表 | 列表型 | 是 | None | 数值列表 |
| 百分位 | 双精度数型 | 是 | None | 0~1，如 0.95 |
| 小数位 | 整数型 | 否 | 3 | 结果保留小数位 |

**返回结构**：（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
数值计算支持库.分位数(数值列表=值, 百分位=值, 小数位=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：None（None）
