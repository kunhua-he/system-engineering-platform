"""支持库.数值计算支持库 包级中文入口。"""

from __future__ import annotations

from 公共契约.基础类型.逻辑类型 import 真, 假

from 支持库.后端.数值计算支持库.实现.数值计算 import 四舍五入
from 支持库.后端.数值计算支持库.实现.数值计算 import 取整
from 支持库.后端.数值计算支持库.实现.数值计算 import 绝对值
from 支持库.后端.数值计算支持库.实现.数值计算 import 最大值
from 支持库.后端.数值计算支持库.实现.数值计算 import 最小值
from 支持库.后端.数值计算支持库.实现.数值计算 import 随机数
from 支持库.后端.数值计算支持库.实现.数值计算 import 求和
from 支持库.后端.数值计算支持库.实现.数值计算 import 平均值
from 支持库.后端.数值计算支持库.实现.数值计算 import 分位数

__all__ = [
    "四舍五入",
    "取整",
    "绝对值",
    "最大值",
    "最小值",
    "随机数",
    "求和",
    "平均值",
    "分位数",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 缺陷溯源：本入口原先手工重建注册参数声明（`参数=...` 手写字面量），只写 名称/类型/必填，
    # `默认值` 整批缺声明（2026-09-24 实测：本包 14 条契约参数中 1 条漏声明，
    # 全仓 93 条）。后果：网关按入口契约表判参时拿不到默认值 —— 「≥10 秒须走异步腿」的守卫
    # 对 超时秒≥10 的能力**不传参即被绕过**（实测 执行命令/沙箱执行命令/发送请求 等 9 条）；
    # 其余能力的默认值同样在网关侧不可见。
    # 手工表一旦漏一个参数，新参数还会被网关**静默丢弃**（同族现场见未完成事项 #197）。
    # 修法：参数声明从 `能力契约/参数契约.json` **整条透传**，入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：漂移检测的 AST 读取器
    # （`_静态字面量` 的 `ast.Subscript` 分支）只收 `注册能力` 函数体内的赋值，
    # 包一层 `_参数声明(能力id)` 会让 AST 一律判「不可静态判定」，等于拿门禁覆盖率换干净。
    # 表内布尔位写中文 真/假（真源 公共契约/基础类型/逻辑类型.py）。
    # 契约名序/返回口径核验由装配期唯一闸门承担（`运行核心/加载器/生命周期管理/管理器.装配系统`
    # → 内核 `能力注册表.校验声明一致`），本入口不再另立第二份。
    _参数契约表 = {
        "数值计算支持库.四舍五入": [
            {"名称": "数值", "类型": "双精度数型", "必填": 真, "默认值": None},
            {"名称": "小数位", "类型": "整数型", "必填": 假, "默认值": 0},
        ],
        "数值计算支持库.取整": [
            {"名称": "数值", "类型": "双精度数型", "必填": 真, "默认值": None},
            {"名称": "方式", "类型": "文本型", "必填": 假, "默认值": "四舍五入"},
        ],
        "数值计算支持库.绝对值": [
            {"名称": "数值", "类型": "双精度数型", "必填": 真, "默认值": None},
        ],
        "数值计算支持库.最大值": [
            {"名称": "数值列表", "类型": "列表型", "必填": 真, "默认值": None},
        ],
        "数值计算支持库.最小值": [
            {"名称": "数值列表", "类型": "列表型", "必填": 真, "默认值": None},
        ],
        "数值计算支持库.随机数": [
            {"名称": "下限", "类型": "整数型", "必填": 假, "默认值": 0},
            {"名称": "上限", "类型": "整数型", "必填": 假, "默认值": 100},
        ],
        "数值计算支持库.求和": [
            {"名称": "数值列表", "类型": "列表型", "必填": 真, "默认值": None},
        ],
        "数值计算支持库.平均值": [
            {"名称": "数值列表", "类型": "列表型", "必填": 真, "默认值": None},
        ],
        "数值计算支持库.分位数": [
            {"名称": "数值列表", "类型": "列表型", "必填": 真, "默认值": None},
            {"名称": "百分位", "类型": "双精度数型", "必填": 真, "默认值": None},
            {"名称": "小数位", "类型": "整数型", "必填": 假, "默认值": 3},
        ],
    }
# ===== 参数声明透传 结束 =====
    for 能力id, 函数 in [
        ("数值计算支持库.四舍五入", 四舍五入),
        ("数值计算支持库.取整", 取整),
        ("数值计算支持库.绝对值", 绝对值),
        ("数值计算支持库.最大值", 最大值),
        ("数值计算支持库.最小值", 最小值),
        ("数值计算支持库.随机数", 随机数),
        ("数值计算支持库.求和", 求和),
        ("数值计算支持库.平均值", 平均值),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.数值计算支持库",
                实现函数=函数,
                参数=_参数契约表[能力id],
                返回="结果",
                说明="常用数值计算：四舍五入/取整/绝对值/最大/最小/随机/求和/平均（参考易语言数值计算支持库）",
            )
        )
    注册表.注册(能力实现(能力id="数值计算支持库.分位数", 包id="支持库.后端.数值计算支持库", 实现函数=分位数, 参数=_参数契约表["数值计算支持库.分位数"], 返回="结果", 说明="线性插值百分位数（保证单调）"))
