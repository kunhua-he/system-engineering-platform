# XML解析支持库 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：支持库.后端.XML解析支持库
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| XML解析支持库.解析XML | 解析XML | 1.0.0 | {'类型': '结果型', '值结构': {}} | XML 文本 → 结构 |
| XML解析支持库.生成XML | 生成XML | 1.0.0 | {'类型': '结果型', '值结构': {}} | 结构 → XML 文本 |
| XML解析支持库.查询节点 | 查询节点 | 1.0.0 | {'类型': '结果型', '值结构': {}} | XPath 查询节点 |

## XML解析支持库.解析XML（解析XML）

**说明**：XML 文本 → 结构

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| XML文本 | 文本型 | 是 | None | XML 文本 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 解析失败 |

**调用示例**：

```python
XML解析支持库.解析XML(XML文本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## XML解析支持库.生成XML（生成XML）

**说明**：结构 → XML 文本

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 根节点名 | 文本型 | 是 | None | 根节点名称 |
| 子节点 | 列表型 | 否 | None | 子节点列表 [{名称, 文本, 属性}] |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
XML解析支持库.生成XML(根节点名=值, 子节点=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## XML解析支持库.查询节点（查询节点）

**说明**：XPath 查询节点

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| XML文本 | 文本型 | 是 | None | XML 文本 |
| XPath | 文本型 | 是 | None | XPath 表达式 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 解析失败 |
| 节点不存在 |

**调用示例**：

```python
XML解析支持库.查询节点(XML文本=值, XPath=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
