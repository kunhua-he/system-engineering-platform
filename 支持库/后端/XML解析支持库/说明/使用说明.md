# XML解析支持库（支持库.后端.XML解析支持库）

- 类型：支持库
- 版本：1.0.2
- 说明：XML 解析/生成/查询节点（参考易语言 XML解析支持库）
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 解析XML（`XML解析支持库.解析XML`）
- 版本：1.0.0
- 一句话说明：XML 文本 → 结构
- 所属支持库或模块：支持库.后端.XML解析支持库
- 参数列表：XML文本:文本型
- 必填参数：XML文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 解析失败
- 调用示例：XML解析支持库.解析XML()
- 依赖能力：无
### 生成XML（`XML解析支持库.生成XML`）
- 版本：1.0.0
- 一句话说明：结构 → XML 文本
- 所属支持库或模块：支持库.后端.XML解析支持库
- 参数列表：根节点名:文本型, 子节点:列表型
- 必填参数：根节点名
- 可选参数：子节点
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 生成失败
- 调用示例：XML解析支持库.生成XML()
- 依赖能力：无
### 查询节点（`XML解析支持库.查询节点`）
- 版本：1.0.0
- 一句话说明：XPath 查询节点
- 所属支持库或模块：支持库.后端.XML解析支持库
- 参数列表：XML文本:文本型, XPath:文本型
- 必填参数：XML文本, XPath
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 解析失败, 节点不存在
- 调用示例：XML解析支持库.查询节点()
- 依赖能力：无
