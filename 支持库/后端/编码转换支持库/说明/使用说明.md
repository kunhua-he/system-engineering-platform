# 编码转换支持库（支持库.后端.编码转换支持库）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.3
- 说明：字符编码转换：UTF-8/GBK/Unicode 互转、检测编码（参考易语言汉字编码转换支持库）
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 文本转字节（`编码转换支持库.文本转字节`）
- 版本：1.0.0
- 一句话说明：文本 → 字节序列（指定编码）
- 所属支持库或模块：支持库.后端.编码转换支持库
- 参数列表：文本:文本型, 编码:文本型
- 必填参数：文本
- 可选参数：编码
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 编码失败
- 调用示例：编码转换支持库.文本转字节(编码=utf-8)
- 依赖能力：无
### 字节转文本（`编码转换支持库.字节转文本`）
- 版本：1.0.0
- 一句话说明：字节序列 → 文本
- 所属支持库或模块：支持库.后端.编码转换支持库
- 参数列表：字节b64:文本型, 编码:文本型
- 必填参数：字节b64
- 可选参数：编码
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 解码失败
- 调用示例：编码转换支持库.字节转文本(编码=utf-8)
- 依赖能力：无
### 检测编码（`编码转换支持库.检测编码`）
- 版本：1.0.0
- 一句话说明：检测字节序列编码（启发式）
- 所属支持库或模块：支持库.后端.编码转换支持库
- 参数列表：字节b64:文本型
- 必填参数：字节b64
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：编码转换支持库.检测编码()
- 依赖能力：无
### 转码（`编码转换支持库.转码`）
- 版本：1.0.0
- 一句话说明：字节从源编码转为目标编码
- 所属支持库或模块：支持库.后端.编码转换支持库
- 参数列表：字节b64:文本型, 源编码:文本型, 目标编码:文本型
- 必填参数：字节b64, 源编码, 目标编码
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 转码失败
- 调用示例：编码转换支持库.转码()
- 依赖能力：无
