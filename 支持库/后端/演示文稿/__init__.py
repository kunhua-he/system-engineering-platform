"""演示文稿支持库包级中文入口（经调用器调用受管提供者能力）。

调用者只从此入口导入，禁止深入 实现/ 目录。
安全默认：OOXML 不可信 ZIP 检查、损坏/伪装拒绝、超限拒绝、缺提供者不伪装成功。
"""

from __future__ import annotations

from 公共契约.能力契约.契约 import 能力实现
from 支持库.后端.演示文稿.实现.演示文稿 import 解析演示文稿

__all__ = [
    "解析演示文稿",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用，向能力注册表注册本库能力。"""
    注册表.注册(
        能力实现(
            能力id="演示文稿.解析演示文稿",
            包id="支持库.后端.演示文稿",
            实现函数=解析演示文稿,
            参数=[
                {"名称": "文件路径", "类型": "文本", "必填": True},
                {"名称": "格式", "类型": "文本", "必填": False},
                {"名称": "最大幻灯片数", "类型": "整数", "必填": False},
                {"名称": "最大字节数", "类型": "整数", "必填": False},
                {"名称": "超时秒", "类型": "数字", "必填": False},
            ],
            返回="结果",
            说明="解析 PPT/PPTX 为平台通用文档（幻灯片文本/备注/图像资源，经受管提供者解析）",
        )
    )
