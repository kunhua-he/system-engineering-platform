"""支持库.数据结构支持库 包级中文入口。"""

from __future__ import annotations

from 支持库.后端.数据结构支持库.实现.数据结构 import 创建栈
from 支持库.后端.数据结构支持库.实现.数据结构 import 创建队列
from 支持库.后端.数据结构支持库.实现.数据结构 import 压栈
from 支持库.后端.数据结构支持库.实现.数据结构 import 出栈
from 支持库.后端.数据结构支持库.实现.数据结构 import 入队
from 支持库.后端.数据结构支持库.实现.数据结构 import 出队
from 支持库.后端.数据结构支持库.实现.数据结构 import 释放句柄

__all__ = [
    "创建栈",
    "创建队列",
    "压栈",
    "出栈",
    "入队",
    "出队",
    "释放句柄",
]


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现
    for 能力id, 函数, 参数表 in [
        ("数据结构支持库.创建栈", 创建栈, [{"名称": "超时秒", "类型": "整数型", "必填": False, "默认值": None, "说明": "句柄超时，默认30分钟"}]),
        ("数据结构支持库.创建队列", 创建队列, [{"名称": "超时秒", "类型": "整数型", "必填": False, "默认值": None, "说明": "句柄超时，默认30分钟"}]),
        ("数据结构支持库.压栈", 压栈, [{"名称": "句柄", "类型": "整数型", "必填": True, "默认值": None, "说明": "栈句柄"}, {"名称": "值", "类型": "任意型", "必填": True, "默认值": None, "说明": "入栈值"}]),
        ("数据结构支持库.出栈", 出栈, [{"名称": "句柄", "类型": "整数型", "必填": True, "默认值": None, "说明": "栈句柄"}]),
        ("数据结构支持库.入队", 入队, [{"名称": "句柄", "类型": "整数型", "必填": True, "默认值": None, "说明": "队列句柄"}, {"名称": "值", "类型": "任意型", "必填": True, "默认值": None, "说明": "入队值"}]),
        ("数据结构支持库.出队", 出队, [{"名称": "句柄", "类型": "整数型", "必填": True, "默认值": None, "说明": "队列句柄"}]),
        ("数据结构支持库.释放句柄", 释放句柄, [{"名称": "句柄", "类型": "整数型", "必填": True, "默认值": None, "说明": "结构句柄"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.后端.数据结构支持库",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="栈/队列数据结构（参考易语言数据结构支持库，句柄模式）",
            )
        )
