<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 数据结构支持库（支持库.后端.数据结构支持库）

- 类型：支持库
- 版本：1.0.0
- 说明：栈/队列数据结构（参考易语言数据结构支持库，句柄模式）
- 入口：__init__.py

## 能力
### 创建栈（`数据结构支持库.创建栈`）
- 版本：1.0.0
- 一句话说明：创建栈（后进先出），返回句柄
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：超时秒:整数型
- 必填参数：无
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据结构支持库.创建栈()
- 依赖能力：无
### 创建队列（`数据结构支持库.创建队列`）
- 版本：1.0.0
- 一句话说明：创建队列（先进先出），返回句柄
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：超时秒:整数型
- 必填参数：无
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据结构支持库.创建队列()
- 依赖能力：无
### 压栈（`数据结构支持库.压栈`）
- 版本：1.0.0
- 一句话说明：入栈
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：句柄:整数型, 值:JSON值型
- 必填参数：句柄, 值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 栈为空
- 调用示例：数据结构支持库.压栈()
- 依赖能力：无
### 出栈（`数据结构支持库.出栈`）
- 版本：1.0.0
- 一句话说明：出栈
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 栈为空
- 调用示例：数据结构支持库.出栈()
- 依赖能力：无
### 入队（`数据结构支持库.入队`）
- 版本：1.0.0
- 一句话说明：入队
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：句柄:整数型, 值:JSON值型
- 必填参数：句柄, 值
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 队列为空
- 调用示例：数据结构支持库.入队()
- 依赖能力：无
### 出队（`数据结构支持库.出队`）
- 版本：1.0.0
- 一句话说明：出队
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄失效, 队列为空
- 调用示例：数据结构支持库.出队()
- 依赖能力：无
### 释放句柄（`数据结构支持库.释放句柄`）
- 版本：1.0.0
- 一句话说明：释放结构句柄（幂等）
- 所属支持库或模块：支持库.后端.数据结构支持库
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：数据结构支持库.释放句柄()
- 依赖能力：无
