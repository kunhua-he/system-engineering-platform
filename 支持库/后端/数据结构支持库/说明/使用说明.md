# 数据结构支持库 使用说明

- 包id：支持库.后端.数据结构支持库
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 数据结构支持库.创建栈 | 创建栈 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建栈（后进先出），返回句柄 |
| 数据结构支持库.创建队列 | 创建队列 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 创建队列（先进先出），返回句柄 |
| 数据结构支持库.压栈 | 压栈 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 入栈 |
| 数据结构支持库.出栈 | 出栈 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 出栈 |
| 数据结构支持库.入队 | 入队 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 入队 |
| 数据结构支持库.出队 | 出队 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 出队 |
| 数据结构支持库.释放句柄 | 释放句柄 | 1.0.0 | {'类型': '结果型', '值结构': {}} | 释放结构句柄（幂等） |

## 数据结构支持库.创建栈（创建栈）

**说明**：创建栈（后进先出），返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 整数型 | 否 | None | 句柄超时，默认30分钟 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据结构支持库.创建栈(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据结构支持库.创建队列（创建队列）

**说明**：创建队列（先进先出），返回句柄

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 整数型 | 否 | None | 句柄超时，默认30分钟 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据结构支持库.创建队列(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据结构支持库.压栈（压栈）

**说明**：入栈

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 栈句柄 |
| 值 | 任意型 | 是 | None | 入栈值 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 栈为空 |

**调用示例**：

```python
数据结构支持库.压栈(句柄=值, 值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据结构支持库.出栈（出栈）

**说明**：出栈

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 栈句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 栈为空 |

**调用示例**：

```python
数据结构支持库.出栈(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据结构支持库.入队（入队）

**说明**：入队

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 队列句柄 |
| 值 | 任意型 | 是 | None | 入队值 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 队列为空 |

**调用示例**：

```python
数据结构支持库.入队(句柄=值, 值=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据结构支持库.出队（出队）

**说明**：出队

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 队列句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 句柄失效 |
| 队列为空 |

**调用示例**：

```python
数据结构支持库.出队(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 数据结构支持库.释放句柄（释放句柄）

**说明**：释放结构句柄（幂等）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 句柄 | 文本型 | 是 | None | 结构句柄 |

**返回结构**：{'类型': '结果型', '值结构': {}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
数据结构支持库.释放句柄(句柄=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
