# 个人蒸馏支持库（支持库.后端.个人蒸馏支持库）

- 类型：支持库
- 版本：1.0.1
- 说明：个人蒸馏网关支持库：查询上下文与事实投影格式化。外部服务地址可用环境变量 V3个人蒸馏地址 覆盖（默认 http://127.0.0.1:8898）。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 查询上下文（`个人蒸馏支持库.查询上下文`）
- 版本：1.0.0
- 一句话说明：查询个人蒸馏上下文
- 所属支持库或模块：支持库.后端.个人蒸馏支持库
- 参数列表：主体标识:文本型, 岗位标识:文本型, 场景:文本型, as_of:文本型, known_at:文本型
- 必填参数：主体标识, 岗位标识, as_of, known_at
- 可选参数：场景
- 返回结构：{'类型': '结果型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：个人蒸馏不可用, 查询异常
- 调用示例：个人蒸馏支持库.查询上下文()
- 依赖能力：无
### 格式化可注入上下文（`个人蒸馏支持库.格式化可注入上下文`）
- 版本：1.0.0
- 一句话说明：格式化投影为注入文本
- 所属支持库或模块：支持库.后端.个人蒸馏支持库
- 参数列表：上下文:字典型
- 必填参数：上下文
- 可选参数：无
- 返回结构：{'类型': '结果型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：格式化异常
- 调用示例：个人蒸馏支持库.格式化可注入上下文()
- 依赖能力：无
### 构建上下文请求参数（`个人蒸馏支持库.构建上下文请求参数`）
- 版本：1.0.0
- 一句话说明：构建上下文请求参数
- 所属支持库或模块：支持库.后端.个人蒸馏支持库
- 参数列表：主体标识:文本型, 岗位标识:文本型, 场景:文本型, as_of:文本型, known_at:文本型
- 必填参数：主体标识, 岗位标识, as_of, known_at
- 可选参数：场景
- 返回结构：{'类型': '结果型'}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 构建异常
- 调用示例：个人蒸馏支持库.构建上下文请求参数()
- 依赖能力：无
