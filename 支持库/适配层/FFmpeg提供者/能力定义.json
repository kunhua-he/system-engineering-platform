{
 "包id": "支持库.适配层.FFmpeg提供者",
 "版本": "2.0.0",
 "说明": "FFmpeg 外部命令独立提供者：ffprobe 媒体探测（时长/流/格式）、ffmpeg 转码/音轨抽取/抽帧。独立进程组+超时+取消+输出上限截断+临时文件零残留；缺少外部命令返回 提供者不可用（未配置），不伪装成功。 ★ 批R·R-28（2026-09-24，华哥裁决①「删适配层孪生能力面、保留后端腿 id」）：本包**能力面已删**（能力列表 为空）—— 上面那句是**历史沿革描述**，不再是本包的能力声明；同名能力面收归后端腿 `支持库/后端/媒体处理支持库/FFmpeg媒体`（对外 id 不变，生产调用点零改动），本包只剩实现（`实现/` 下），由后端腿经 `载入唯一实现` 直接载入。",
 "能力列表": []
}
