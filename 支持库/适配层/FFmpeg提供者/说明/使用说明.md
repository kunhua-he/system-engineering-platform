# FFmpeg提供者 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 包id：支持库.适配层.FFmpeg提供者
- 版本：2.0.0
- 说明：FFmpeg 外部命令独立提供者：ffprobe 媒体探测（时长/流/格式）、ffmpeg 转码/音轨抽取/抽帧。独立进程组+超时+取消+输出上限截断+临时文件零残留；缺少外部命令返回 提供者不可用（未配置），不伪装成功。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查提供者（`FFmpeg媒体.检查提供者`）
- 版本：1.0.1
- 一句话说明：检查 ffmpeg/ffprobe 是否可用（真实版本探针），返回版本；缺失 → 提供者不可用
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：超时秒:双精度数型
- 必填参数：无
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用
- 调用示例：FFmpeg媒体.检查提供者(超时秒=30.0)
- 依赖能力：无
### 探测媒体（`FFmpeg媒体.探测媒体`）
- 版本：1.0.0
- 一句话说明：ffprobe 探测媒体：返回 {时长秒, 流列表, 格式, 大小字节, 比特率}；损坏 → 损坏媒体
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：文件路径:文本型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超时, 取消, 进程崩溃
- 调用示例：FFmpeg媒体.探测媒体(文件路径=/tmp/示例媒体.mp4, 超时秒=60.0)
- 依赖能力：无
### 提取音频（`FFmpeg媒体.提取音频`）
- 版本：1.0.0
- 一句话说明：从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac）：返回 {字节b64, 格式, 字节数}；无音轨 → 无音轨；超长 → 超长媒体
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：文件路径:文本型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 无音轨, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：FFmpeg媒体.提取音频(文件路径=/tmp/示例媒体.mp4, 输出格式=wav, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：无
### 转码（`FFmpeg媒体.转码`）
- 版本：1.0.0
- 一句话说明：ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：文件路径:文本型, 输出格式:文本型, 编码选项:字典型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型, 返回字节:逻辑型
- 必填参数：文件路径
- 可选参数：输出格式, 编码选项, 输出路径, 超时秒, 最大输出字节, 最大时长秒, 返回字节
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：FFmpeg媒体.转码(文件路径=/tmp/示例媒体.mp4, 输出格式=mp4, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：无
### 抽取帧（`FFmpeg媒体.抽取帧`）
- 版本：1.0.0
- 一句话说明：ffmpeg 抽帧（jpg/png，时间点秒）：返回 {字节b64, 格式, 宽度, 高度}；宽度/高度为输出帧经 ffprobe 受控探测链得到的真实尺寸（禁止固定值）
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：文件路径:文本型, 时间点秒:双精度数型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径, 时间点秒
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：FFmpeg媒体.抽取帧(文件路径=/tmp/示例媒体.mp4, 时间点秒=1.0, 输出格式=jpg, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：无
### 截取音频（`FFmpeg媒体.截取音频`）
- 版本：1.1.0
- 一句话说明：ffmpeg 按时间区间截取音频（复核区间用）：-ss 前置快速定位 + -t 限长；返回 {字节b64, 格式, 字节数, 输出路径}；无音轨 → 无音轨；区间不合法 → 参数不合法
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：文件路径:文本型, 开始秒:双精度数型, 结束秒:双精度数型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径, 开始秒, 结束秒
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 无音轨, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：FFmpeg媒体.截取音频(输出格式=wav, 超时秒=60, 最大输出字节=209715200, 最大时长秒=7200)
- 依赖能力：无
### 拼接媒体（`FFmpeg媒体.拼接媒体`）
- 版本：1.0.0
- 一句话说明：按顺序无损拼接多个同源分片成一个文件：返回 {格式, 字节数, 输出路径}（可选 字节b64）；实现为 ffmpeg concat demuxer + -c copy，不重编码 → 秒级完成、零画质损失、不吃 GPU；分片编码参数不一致时直接失败（进程崩溃），不产出看似成功的坏文件。典型用法：直播录制分片（同一档位）拼成整场，供逐字稿精校按整场处理
- 所属支持库或模块：支持库.适配层.FFmpeg提供者
- 参数列表：文件列表:列表型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 返回字节:逻辑型
- 必填参数：文件列表
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 返回字节
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：FFmpeg媒体.拼接媒体(输出格式=mp4, 超时秒=120, 最大输出字节=209715200, 返回字节=True)
- 依赖能力：无
## 能力清单
| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| FFmpeg媒体.检查提供者 | 检查提供者 | 1.0.1 | 结果型 | 检查 ffmpeg/ffprobe 是否可用（真实版本探针），返回版本；缺失 → 提供者不可用 |
| FFmpeg媒体.探测媒体 | 探测媒体 | 1.0.0 | 结果型 | ffprobe 探测媒体：返回 {时长秒, 流列表, 格式, 大小字节, 比特率}；损坏 → 损坏媒体 |
| FFmpeg媒体.提取音频 | 提取音频 | 1.0.0 | 结果型 | 从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac）：返回 {字节b64, 格式, 字节数}；无音轨 → 无音轨；超长 → 超长媒体 |
| FFmpeg媒体.转码 | 转码 | 1.0.0 | 结果型 | ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编 |
| FFmpeg媒体.抽取帧 | 抽取帧 | 1.0.0 | 结果型 | ffmpeg 抽帧（jpg/png，时间点秒）：返回 {字节b64, 格式, 宽度, 高度}；宽度/高度为输出帧经 ffprobe 受控探测链得到的真实尺寸（禁止固定值） |
| FFmpeg媒体.截取音频 | 截取音频 | 1.1.0 | 结果型 | ffmpeg 按时间区间截取音频（复核区间用）：-ss 前置快速定位 + -t 限长；返回 {字节b64, 格式, 字节数, 输出路径}；无音轨 → 无音轨；区间不合法 → 参数不合法 |
| FFmpeg媒体.拼接媒体 | 拼接媒体 | 1.0.0 | 结果型 | 按顺序无损拼接多个同源分片成一个文件：返回 {格式, 字节数, 输出路径}（可选 字节b64）；实现为 ffmpeg concat demuxer + -c copy，不重编码 → 秒级完成、零画质损失、不吃 GPU；分片编码参数不一致时直接失败（进程崩溃），不产出看似成功的坏文件。典型用法：直播录制分片（同一档位）拼成整场，供逐字稿精校按整场处理 |
## FFmpeg媒体.检查提供者（检查提供者）
**说明**：检查 ffmpeg/ffprobe 是否可用（真实版本探针），返回版本；缺失 → 提供者不可用
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 30 | 版本探针超时秒 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
**调用示例**：
```python
FFmpeg媒体.检查提供者(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
## FFmpeg媒体.探测媒体（探测媒体）
**说明**：ffprobe 探测媒体：返回 {时长秒, 流列表, 格式, 大小字节, 比特率}；损坏 → 损坏媒体
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 超时秒 | 双精度数型 | 否 | 60 | 探测超时 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
**调用示例**：
```python
FFmpeg媒体.探测媒体(文件路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
## FFmpeg媒体.提取音频（提取音频）
**说明**：从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac）：返回 {字节b64, 格式, 字节数}；无音轨 → 无音轨；超长 → 超长媒体
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 输出格式 | 文本型 | 否 | wav | wav/mp3/aac/m4a/ogg/flac |
| 输出路径 | 文本型 | 否 | None | 可选输出文件路径（默认临时文件自动清理）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 超时秒 | 双精度数型 | 否 | 60 | 提取超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 无音轨 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
**调用示例**：
```python
FFmpeg媒体.提取音频(文件路径=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
## FFmpeg媒体.转码（转码）
**说明**：ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 输出格式 | 文本型 | 否 | mp4 | mp4/mkv/webm/mov/avi |
| 编码选项 | 字典型 | 否 | None | 白名单键值：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；硬编压缩推荐组合：视频编码器=hevc_videotoolbox、视频码率=900k、码率上限=1100k、缓冲=2200k、帧率=15、视频标签=hvc1、音频编码器=copy |
| 输出路径 | 文本型 | 否 | None | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 超时秒 | 双精度数型 | 否 | 60 | 转码超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
| 返回字节 | 逻辑型 | 否 | True | 是否在返回值里带回 base64 字节；已落盘的大文件建议否，避免 4/3 倍放大与内存占用 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
**调用示例**：
```python
FFmpeg媒体.转码(文件路径=值, 输出格式=值, 编码选项=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值, 返回字节=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
## FFmpeg媒体.抽取帧（抽取帧）
**说明**：ffmpeg 抽帧（jpg/png，时间点秒）：返回 {字节b64, 格式, 宽度, 高度}；宽度/高度为输出帧经 ffprobe 受控探测链得到的真实尺寸（禁止固定值）
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 时间点秒 | 双精度数型 | 是 | None | 抽帧时间点（≥0） |
| 输出格式 | 文本型 | 否 | jpg | jpg/png |
| 输出路径 | 文本型 | 否 | None | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 超时秒 | 双精度数型 | 否 | 60 | 抽帧超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 媒体时长上限，超过 → 超长媒体 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
**调用示例**：
```python
FFmpeg媒体.抽取帧(文件路径=值, 时间点秒=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
## FFmpeg媒体.截取音频（截取音频）
**说明**：ffmpeg 按时间区间截取音频（复核区间用）：-ss 前置快速定位 + -t 限长；返回 {字节b64, 格式, 字节数, 输出路径}；无音轨 → 无音轨；区间不合法 → 参数不合法
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 源媒体路径（视频或音频）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 开始秒 | 双精度数型 | 是 | None | 区间开始秒 |
| 结束秒 | 双精度数型 | 是 | None | 区间结束秒（必须大于开始秒） |
| 输出格式 | 文本型 | 否 | wav | wav/mp3/aac/m4a/ogg/flac |
| 输出路径 | 文本型 | 否 | None | 输出文件路径；缺省用临时文件并自动清理 |
| 超时秒 | 双精度数型 | 否 | 60 | 子进程超时 |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出上限 |
| 最大时长秒 | 双精度数型 | 否 | 7200 | 源媒体时长上限 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 损坏媒体 |
| 无音轨 |
| 超长媒体 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
**调用示例**：
```python
FFmpeg媒体.截取音频(文件路径=值, 开始秒=值, 结束秒=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 最大时长秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
## FFmpeg媒体.拼接媒体（拼接媒体）
**说明**：按顺序无损拼接多个同源分片成一个文件：返回 {格式, 字节数, 输出路径}（可选 字节b64）；实现为 ffmpeg concat demuxer + -c copy，不重编码 → 秒级完成、零画质损失、不吃 GPU；分片编码参数不一致时直接失败（进程崩溃），不产出看似成功的坏文件。典型用法：直播录制分片（同一档位）拼成整场，供逐字稿精校按整场处理
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件列表 | 列表型 | 是 | None | 待拼接分片绝对路径列表，顺序即拼接顺序，至少 2 项 |
| 输出格式 | 文本型 | 否 | mp4 | mp4/mkv/webm/mov/avi |
| 输出路径 | 文本型 | 否 | None | 可选输出文件路径；省略则写临时文件并自动清理（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 超时秒 | 双精度数型 | 否 | 120 | 拼接超时（流复制很快，120 秒足够整场） |
| 最大输出字节 | 整数型 | 否 | 209715200 | 输出大小上限 |
| 返回字节 | 逻辑型 | 否 | True | 是否在返回值里带回 base64 字节；已落盘的大文件建议否 |
**返回结构**：结果型（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
**调用示例**：
```python
FFmpeg媒体.拼接媒体(文件列表=值, 输出格式=值, 输出路径=值, 超时秒=值, 最大输出字节=值, 返回字节=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：FFmpeg媒体.真实小媒体正向HTTP链（本包 验证场景引用.json，8 步）
