# 代码地图提供者 使用说明
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.适配层.代码地图提供者
- 版本：1.0.1

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 代码地图提供者.检查提供者 | 检查提供者 | 1.0.0 | 结果型 | 检查 codegraph 外部命令是否可用（真实版本探针）；缺失 → 提供者不可用 |
| 代码地图提供者.查询项目地图状态 | 查询项目地图状态 | 1.0.0 | 结果型 | 只读查询某项目的代码地图索引状态：是否存在、文件/节点/边数量、索引时间、待同步变更数（不做任何写入） |
| 代码地图提供者.重建项目地图索引 | 重建项目地图索引 | 1.0.0 | 结果型 | 对指定项目重建/同步代码地图索引（传项目根目录即可）：先探针提供者，再执行索引，返回同步前后的节点/文件数量与耗时，供调用方核实是否真的刷新 |

## 代码地图提供者.检查提供者（检查提供者）

**说明**：检查 codegraph 外部命令是否可用（真实版本探针）；缺失 → 提供者不可用

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 30 | 探针超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 提供者不可用 |
| 超时 |

**调用示例**：

```python
代码地图提供者.检查提供者(超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：代码地图提供者.真实索引保鲜正向HTTP链（本包 验证场景引用.json，5 步）

## 代码地图提供者.查询项目地图状态（查询项目地图状态）

**说明**：只读查询某项目的代码地图索引状态：是否存在、文件/节点/边数量、索引时间、待同步变更数（不做任何写入）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目根目录 | 文本型 | 是 | None | 待查项目的绝对路径 |
| 超时秒 | 双精度数型 | 否 | 120 | 超时 |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 超时 |
| 执行失败 |

**调用示例**：

```python
代码地图提供者.查询项目地图状态(项目根目录=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：代码地图提供者.真实索引保鲜正向HTTP链（本包 验证场景引用.json，5 步）

## 代码地图提供者.重建项目地图索引（重建项目地图索引）

**说明**：对指定项目重建/同步代码地图索引（传项目根目录即可）：先探针提供者，再执行索引，返回同步前后的节点/文件数量与耗时，供调用方核实是否真的刷新

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目根目录 | 文本型 | 是 | None | 待建索引的项目绝对路径（支持任意项目，不写死本仓） |
| 重建方式 | 文本型 | 否 | 同步 | 同步（增量，快）/ 全量（重建，慢） |
| 超时秒 | 双精度数型 | 否 | 1800 | 超时（全量重建可能数分钟） |

**返回结构**：结果型（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 超时 |
| 执行失败 |

**调用示例**：

```python
代码地图提供者.重建项目地图索引(项目根目录=值, 重建方式=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：代码地图提供者.真实索引保鲜正向HTTP链（本包 验证场景引用.json，5 步）
