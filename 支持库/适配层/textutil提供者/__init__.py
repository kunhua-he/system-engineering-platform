"""textutil 提供者包级中文入口（macOS 系统工具 textutil 受管边界）。

调用者只从此入口导入，禁止深入 实现/ 目录。
公开能力（分组 textutil转换）：
- 检查提供者 → 版本探针；缺失 → 提供者不可用
- 转换文本文件 → txt/html/rtf/doc/docx 互转，返回 文本内容
系统工具经独立进程组受管执行：超时 killpg、输出上限、临时目录零残留。
"""

from __future__ import annotations

from 支持库.适配层.textutil提供者.实现.文本转换 import 检查提供者
from 支持库.适配层.textutil提供者.实现.文本转换 import 转换文本文件

__all__ = ["检查提供者", "转换文本文件", "注册能力"]


def 注册能力(注册表) -> None:
    """由支持库加载器调用，向能力注册表注册本库能力（分组 textutil转换）。"""
    from 公共契约.能力契约.契约 import 能力实现

    for 能力id, 函数, 参数名, 说明 in [
        ("textutil转换.检查提供者", 检查提供者,
         [{"名称": "超时秒", "类型": "数字", "必填": False, "默认值": 30}],
         "检查 textutil 是否可用（真实独立进程探针 textutil -help + macOS 版本）；缺失 → 提供者不可用"),
        ("textutil转换.转换文本文件", 转换文本文件,
         [{"名称": "输入路径", "类型": "文本", "必填": True, "默认值": None},
          {"名称": "目标格式", "类型": "文本", "必填": True, "默认值": None},
          {"名称": "超时秒", "类型": "数字", "必填": False, "默认值": 60},
          {"名称": "最大输出字节", "类型": "整数", "必填": False, "默认值": 209715200},
          {"名称": "输出目录", "类型": "文本", "必填": False, "默认值": None}],
         "textutil 转换：输入文件 → 目标格式（txt/html/rtf/doc/docx 互转）；返回 文本内容"),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="支持库.适配层.textutil提供者",
                实现函数=函数,
                参数=参数名,
                返回="结果",
                说明=说明,
            )
        )
