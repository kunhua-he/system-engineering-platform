{
  "搜索词": [
    "模型服务",
    "嵌入",
    "重排",
    "决策",
    "HuggingFace",
    "本地模型"
  ],
  "候选能力id": [
    "模型连接器.连接模型",
    "模型连接器.调用模型",
    "模型服务.检查提供者"
  ],
  "决策": {
    "复用": [],
    "升级": [],
    "新建": [
      "模型服务.检查提供者 —— ★ 本次新建的根因（#96）：`支持库/适配层/模型服务.py` 此前是**根下平铺内部件**（直连 torch/transformers/fastapi/uvicorn、无 依赖锁.json、不进依赖生命周期审计），第三方依赖只登记在 内部件依赖声明.json。包化为 模型服务提供者 后，本能力是该提供者的可用性边界：定位运行栈四项 + 读发行包版本，缺件 → 提供者不可用（只定位不导入，不在主进程加载原生扩展）。",
      "不新建 嵌入/重排/决策 的公开能力：它们是**服务进程的 HTTP 面**（`/v1/embeddings`、`/v1/rerank`、`/v1/decision`），由 `模型连接器` 经 HTTP 调用，不是能力调用入口的入参形态（调用方无法经唯一入口把 `模型路径` 传进来，见 #179 相对安全声明）。"
    ],
    "废弃": []
  },
  "第三方处理": "第三方依赖 4 项：torch 2.13.0（PyPI，BSD-3-Clause）、transformers 5.8.1（PyPI，Apache-2.0）、fastapi 0.136.3（PyPI，MIT）、uvicorn 0.46.0（PyPI，BSD-3-Clause），全部落 依赖锁.json。**如实申报为 4 个不同发行包**（都是直接 import，不合并归属、不伪装单一发行包）——后果是依赖生命周期审计的「混装」判据会对本提供者判红，与既有 MLXWhisper提供者 / 转写 同形；是否拆分提供者由所有者裁决，本包不伪造归属、不放宽判据。原登记在 支持库/适配层/内部件依赖声明.json 的「模型服务」条目在包化后删除。",
  "包id": "支持库.适配层.模型服务提供者",
  "名称": "模型服务提供者",
  "说明": "本文件是设计期决策记录，不参与运行时；改动后需重算 完整性摘要.json"
}
