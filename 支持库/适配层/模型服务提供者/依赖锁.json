{
  "包": [
    {
      "名称": "torch",
      "版本": "2.13.0",
      "模块名": "torch",
      "发行包": "torch",
      "来源": "PyPI",
      "说明": "模型运行时（张量/设备/推理）；自带原生扩展，只在服务子进程内导入，底座主进程不加载"
    },
    {
      "名称": "transformers",
      "版本": "5.8.1",
      "模块名": "transformers",
      "发行包": "transformers",
      "来源": "PyPI",
      "说明": "权重与分词器加载（AutoTokenizer/AutoModel/AutoModelForCausalLM）"
    },
    {
      "名称": "fastapi",
      "版本": "0.136.3",
      "模块名": "fastapi",
      "发行包": "fastapi",
      "来源": "PyPI",
      "说明": "HTTP 面（/v1/models、/v1/embeddings、/v1/rerank、/v1/decision）"
    },
    {
      "名称": "uvicorn",
      "版本": "0.46.0",
      "模块名": "uvicorn",
      "发行包": "uvicorn",
      "来源": "PyPI",
      "说明": "ASGI 服务器（默认只绑回环 127.0.0.1，本服务无鉴权）"
    }
  ],
  "提供者id": "支持库.适配层.模型服务提供者",
  "直接依赖": [
    {
      "名称": "torch",
      "版本": "2.13.0"
    },
    {
      "名称": "transformers",
      "版本": "5.8.1"
    },
    {
      "名称": "fastapi",
      "版本": "0.136.3"
    },
    {
      "名称": "uvicorn",
      "版本": "0.46.0"
    }
  ],
  "依赖闭包": [
    {
      "名称": "torch",
      "版本": "2.13.0",
      "来源": "PyPI",
      "许可证": "BSD-3-Clause",
      "说明": "模型运行时；传递依赖（sympy/networkx/filelock/typing-extensions 等）由 torch 自身约束，本锁只固定直接依赖的精确版本"
    },
    {
      "名称": "transformers",
      "版本": "5.8.1",
      "来源": "PyPI",
      "许可证": "Apache-2.0",
      "说明": "权重/分词器加载；传递依赖（tokenizers/safetensors/huggingface-hub 等）由 transformers 自身约束"
    },
    {
      "名称": "fastapi",
      "版本": "0.136.3",
      "来源": "PyPI",
      "许可证": "MIT",
      "说明": "HTTP 面；传递依赖（starlette/pydantic 等）由 fastapi 自身约束"
    },
    {
      "名称": "uvicorn",
      "版本": "0.46.0",
      "来源": "PyPI",
      "许可证": "BSD-3-Clause",
      "说明": "ASGI 服务器；传递依赖（click/h11 等）由 uvicorn 自身约束"
    }
  ],
  "环境": {
    "Python": "3.14.4",
    "操作系统": "macOS",
    "CPU": "arm64"
  },
  "生成时间": "",
  "说明": "精确锁定，禁止范围版本。★ 如实申报：本提供者声明 **4 个不同发行包**（torch/transformers/fastapi/uvicorn）—— 它们都是 实现/模型服务.py 的**直接 import**，不是传递依赖，故不得合并成一个发行包归属、也不得只锁一项而把其余塞进 依赖闭包（那是失真）。后果：依赖生命周期审计的「混装」判据（审计核心.py:322 `检查依赖锁`，口径「一个提供者只锁一个第三方」）会对本提供者判红，与既有的 MLXWhisper提供者（mlx-whisper + faster-whisper）、转写（faster-whisper + torch）同形。本锁不为此伪造归属、不放宽判据；是否拆分提供者由所有者裁决。另：缺件时 检查提供者 返回 提供者不可用（fail-closed），服务进程由 模型连接器 的本地启动链按路径拉起。"
}
