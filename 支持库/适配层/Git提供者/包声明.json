{
 "包id": "支持库.适配层.Git提供者",
 "名称": "Git提供者",
 "类型": "支持库",
 "依赖类别": "环境依赖",
 "版本": "2.0.0",
 "说明": "Git 外部命令独立提供者：worktree 创建/查询/关闭（可强制）、提交（指定路径或全量 -A）、回滚、挑拣合入、切换分支、合并分支、当前状态、获取当前提交哈希。分支名/路径/提交哈希结构化参数白名单（拒绝空格/换行/;|&`$ 等元字符），变更类能力（提交/切换分支/合并分支）另过仓库根白名单（越界 → 路径越界），全部命令以 subprocess 参数列表受管执行（禁 shell 拼接）。 ★ 批R·R-28（2026-09-24，华哥裁决①「删适配层孪生能力面、保留后端腿 id」）：本包**能力面已删**（能力列表 为空）—— 上面那句是**历史沿革描述**，不再是本包的能力声明；同名能力面收归后端腿 `支持库/后端/版本控制支持库/Git操作`（对外 id 不变，生产调用点零改动），本包只剩实现（`实现/` 下），由后端腿经 `载入唯一实现` 直接载入。",
 "入口": "__init__.py",
 "依赖": [],
 "能力": []
}