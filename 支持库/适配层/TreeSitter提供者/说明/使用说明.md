# TreeSitter提供者 使用说明
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 包id：支持库.适配层.TreeSitter提供者
- 版本：1.0.1

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 适配层.TreeSitter提供者.解析语法树 | 解析语法树 | 1.0.0 | {'类型': '结果型', '值结构': {'语法树': '中性语法树根节点：{种类,起始行,结束行,起始字节,结束字节,字段:{中文字段名:子节点下标},子节点:[…]}；种类为中文名（导入语句/导出语句/函数声明/函数表达式/箭头函数/词法声明/变量声明符/类声明/方法定义/调用表达式/字符串字面量/模板字符串/标识符/成员访问），未登记的一律 其他；字段名同为中文（来源/名称/值/正文/调用目标/实参/对象/属性），未登记的不导出'}} | 把 TypeScript/TSX 源码解析为底座中性语法树根节点：每个节点带中文种类、行号、utf-8 字节区间、中文字段表与有序子节点表；语法错误不抛异常（第三方错误恢复，错误区照常翻出）。第三方不可用报 提供者不可用，由调用方决定是否降级。 |
| 适配层.TreeSitter提供者.检查可用性 | 检查可用性 | 1.0.0 | {'类型': '结果型', '值结构': {'可用': '逻辑型，依赖是否可导入且解析器可构造', '版本': '文本型，tree-sitter 版本（不可用时为空）', '说明': '文本型，判定说明'}} | 探针：报告 tree-sitter / tree-sitter-typescript 是否可导入、版本与解析器是否可构造，供健康检查与依赖审计使用；不可用时如实报告并给出原因。 |

## 适配层.TreeSitter提供者.解析语法树（解析语法树）

**说明**：把 TypeScript/TSX 源码解析为底座中性语法树根节点：每个节点带中文种类、行号、utf-8 字节区间、中文字段表与有序子节点表；语法错误不抛异常（第三方错误恢复，错误区照常翻出）。第三方不可用报 提供者不可用，由调用方决定是否降级。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 代码文本 | 文本型 | 是 | None | 待解析的源码文本（utf-8） |
| 语言 | 文本型 | 否 | typescript | 语法语言：typescript / tsx（同一发行包内，不额外引入第三方） |

**返回结构**：{'类型': '结果型', '值结构': {'语法树': '中性语法树根节点：{种类,起始行,结束行,起始字节,结束字节,字段:{中文字段名:子节点下标},子节点:[…]}；种类为中文名（导入语句/导出语句/函数声明/函数表达式/箭头函数/词法声明/变量声明符/类声明/方法定义/调用表达式/字符串字面量/模板字符串/标识符/成员访问），未登记的一律 其他；字段名同为中文（来源/名称/值/正文/调用目标/实参/对象/属性），未登记的不导出'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 解析失败 |

**调用示例**：

```python
适配层.TreeSitter提供者.解析语法树(代码文本=值, 语言=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：适配层.TreeSitter提供者.中性语法树与探针（本包 验证场景引用.json，3 步：解析语法树 / 可用性探针 / 语言非法）

## 适配层.TreeSitter提供者.检查可用性（检查可用性）

**说明**：探针：报告 tree-sitter / tree-sitter-typescript 是否可导入、版本与解析器是否可构造，供健康检查与依赖审计使用；不可用时如实报告并给出原因。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| （无参数） | | | | |

**返回结构**：{'类型': '结果型', '值结构': {'可用': '逻辑型，依赖是否可导入且解析器可构造', '版本': '文本型，tree-sitter 版本（不可用时为空）', '说明': '文本型，判定说明'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 依赖不可用 |

**调用示例**：

```python
适配层.TreeSitter提供者.检查可用性()
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：适配层.TreeSitter提供者.中性语法树与探针（本包 验证场景引用.json，3 步：解析语法树 / 可用性探针 / 语言非法）
