# 目录树

> 包id：`支持库.适配层.TreeSitter提供者`；版本：`1.0.1`；类型：`支持库`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。
<!-- 机器管理｜类型：包级说明-目录树与命令｜生成器：开发工具.MD文档生成 -->


```text
支持库/适配层/TreeSitter提供者/
├── 包声明.json
├── __init__.py
├── 依赖锁.json
├── 复用决策.json
├── 生命周期契约.json
├── 能力定义.json
├── 能力契约/参数契约.json
├── 能力数据/{Agent查询数据,能力搜索数据}.json
├── 实现/提供者.py
├── 权限契约/权限契约.json
├── 配置契约/配置契约.json
├── 资源预算.json
├── 说明/{使用说明,设计说明,目录树与命令}.md
└── 验证场景引用.json
```

## 命令

```bash
# 语法检查（单文件）
cd 项目根 && unset PYTHONPATH && python3.14 -m py_compile 支持库/适配层/TreeSitter提供者/实现/提供者.py

# 依赖直连审计（本提供者内 tree_sitter 导入合法；其它目录出现即违规）
python3.14 -c "from 运行核心.依赖防火墙 import 审计依赖; r=审计依赖(); print([f'{v.文件}:{v.行号}' for v in r.违规列表 if 'tree_sitter' in (v.目标 or '')])"

# 消费方行为回归（语法索引）
python3.14 -m unittest 测试中心.支持库.核心.测试_语法索引
```
