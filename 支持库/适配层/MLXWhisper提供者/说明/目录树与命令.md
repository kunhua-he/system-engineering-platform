# MLXWhisper提供者 · 目录树与命令

> 包id：`支持库.适配层.MLXWhisper提供者`；版本：`1.0.0`；类型：`支持库`

## 目录树

```text
MLXWhisper提供者/
├── __init__.py
├── 依赖契约/依赖契约.json
├── 依赖锁.json
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/子进程入口.py
├── 实现/子进程解析.py
├── 实现/提供者.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力定义.json
├── 能力数据/Agent查询数据.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
```

## 公开命令

### `转写.检查可用性`

- 名称：检查可用性
- 返回：`结果型`
- 说明：检查 MLX Whisper 转写可用性：未配置模型 → 未配置模型；模型目录缺失 → 模型缺失；配置有效 → 子进程探针返回 {可用, 模型名, 模型版本}
- 参数：
  - `超时秒`；双精度数型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `转写.转写音频文件`

- 名称：转写音频文件
- 返回：`结果型`
- 说明：转写音频文件为文本：受管子进程执行，覆盖 超时/取消/进程组回收/输出上限；未配置模型 → 未配置模型（如实返回，禁止模拟成功）
- 参数：
  - `文件路径`；文本型
  - `超时秒`；双精度数型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `转写.获取模型版本`

- 名称：获取模型版本
- 返回：`结果型`
- 说明：获取 MLX Whisper 模型与库版本：未配置模型 → 未配置模型；返回 {模型名, 模型版本}
- 参数：
  - `超时秒`；双精度数型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
