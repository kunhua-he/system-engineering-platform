# 依赖分两段（第三方翻译层 / 环境依赖层）

> **勿手改**：本文件由生成器派生，禁止手改（哲学 7.9 生成物三律）。
> 派生入口：`系统工程平台_依赖派生/生成依赖分两段.py --生成`（生成器版本 1.0.0）
> 契约指纹：`公共契约/包声明/声明.py` sha256 `834157e87d5ce4bc82960e0f2ed216f01439580038fe44654051d4cbf8cd8225`
> 输入指纹：`4dc78c2030999fc1227eb729b4dcf676de25fa46f6b8be316756ddd5b5dad371`
> 口径来源：决策记录 `0021`、`开发文档/项目说明.md` 第 2 条 2.2/2.3。
> 唯一登记：`支持库/适配层/依赖登记.json`（本文件由同一份数据派生，不是第二事实源）。

## 一、第三方翻译层（要 `import` 的包）

| 件 | 件类别 | 路径 | 版本 | 依赖（名称@版本·来源·类别） | 现场依据 |
|---|---|---|---|---|---|
| MCP协议提供者 | 正式包 | `支持库/适配层/MCP协议提供者` | 1.28.1 | mcp@1.28.1·未标注·第三方库 | 依赖锁.json 包[] 1 项：mcp=第三方库<br>AST import：mcp |
| MLXWhisper提供者 | 正式包 | `支持库/适配层/MLXWhisper提供者` | 0.4.3 | mlx-whisper@0.4.3·未标注·第三方库 | 依赖锁.json 包[] 1 项：mlx-whisper=第三方库<br>AST import：mlx_whisper<br>AST 子进程调用 1 处：subprocess.Popen<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| PDF隔离提供者 | 正式包 | `支持库/适配层/PDF隔离提供者` | 0.11.10 | pdfplumber@0.11.10·未标注·第三方库<br>PyMuPDF@1.28.0·未标注·第三方库<br>Poppler@26.04.0·外部应用·环境依赖 | 依赖锁.json 包[] 3 项：pdfplumber=第三方库、PyMuPDF=第三方库、Poppler=环境依赖<br>AST import：fitz、pdfplumber<br>AST 子进程调用 1 处：subprocess.Popen<br>**备注**：同时具备代码级与运行级两面：pdfplumber(第三方库)、PyMuPDF(第三方库)、Poppler(环境依赖)；现场自述「模拟提供者」：未接真实外部依赖 |
| Pillow提供者 | 正式包 | `支持库/适配层/Pillow提供者` | 12.2.0 | Pillow@12.2.0·未标注·第三方库 | 依赖锁.json 包[] 1 项：Pillow=第三方库<br>AST import：PIL<br>AST 子进程调用 1 处：subprocess.Popen<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| PyMuPDF提供者 | 正式包 | `支持库/适配层/PyMuPDF提供者` | 1.28.0 | PyMuPDF@1.28.0·未标注·第三方库 | 依赖锁.json 包[] 1 项：PyMuPDF=第三方库<br>AST import：fitz<br>AST 子进程调用 1 处：subprocess.Popen<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| TreeSitter提供者 | 正式包 | `支持库/适配层/TreeSitter提供者` | 0.26.0 | tree-sitter@0.26.0·未标注·第三方库<br>tree-sitter-typescript@0.23.2·未标注·第三方库 | 依赖锁.json 包[] 2 项：tree-sitter=第三方库、tree-sitter-typescript=第三方库<br>AST import：tree_sitter、tree_sitter_typescript |
| YAML提供者 | 正式包 | `支持库/适配层/YAML提供者` | 6.0.3 | PyYAML@6.0.3·未标注·第三方库 | 依赖锁.json 包[] 1 项：PyYAML=第三方库<br>AST import：yaml |
| pdfplumber提供者 | 正式包 | `支持库/适配层/pdfplumber提供者` | 0.11.10 | pdfplumber@0.11.10·未标注·第三方库 | 依赖锁.json 包[] 1 项：pdfplumber=第三方库<br>AST import：pdfplumber |
| psycopg提供者 | 正式包 | `支持库/适配层/psycopg提供者` | 3.3.4 | psycopg@3.3.4·未标注·第三方库 | 依赖锁.json 包[] 1 项：psycopg=第三方库<br>AST import：psycopg |
| reportlab提供者 | 正式包 | `支持库/适配层/reportlab提供者` | 5.0.0 | reportlab@5.0.0·未标注·第三方库 | 依赖锁.json 包[] 1 项：reportlab=第三方库<br>AST import：reportlab |
| 密码签名提供者 | 正式包 | `支持库/适配层/密码签名提供者` | 47.0.0 | cryptography@47.0.0·未标注·第三方库 | 依赖锁.json 包[] 1 项：cryptography=第三方库<br>AST import：cryptography<br>AST 子进程调用 1 处：subprocess.Popen<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| 模型服务 | 内部件 | `支持库/适配层/模型服务.py` | — | — | 无 依赖锁.json<br>AST import：fastapi、torch、transformers、uvicorn |
| 系统探针 | 内部件 | `支持库/适配层/系统探针.py` | — | — | 无 依赖锁.json<br>AST import：psutil<br>AST 子进程调用 1 处：subprocess.Popen<br>源码命令令牌：soffice、textutil<br>**备注**：同时具备代码级与运行级两面：第三方 import（psutil） + 子进程调用 1 处／外部命令查找 0 处 |

共 13 件。

## 二、环境依赖层（独立进程/服务/动态库/外部接口）

| 件 | 件类别 | 路径 | 版本 | 依赖（名称@版本·来源·类别） | 现场依据 |
|---|---|---|---|---|---|
| FFmpeg提供者 | 正式包 | `支持库/适配层/FFmpeg提供者` | 8.1 | ffmpeg@8.1·外部应用·环境依赖<br>ffprobe@8.1·外部应用·环境依赖 | 依赖锁.json 包[] 2 项：ffmpeg=环境依赖、ffprobe=环境依赖<br>AST 子进程调用 1 处：subprocess.Popen<br>源码命令令牌：ffmpeg、ffprobe |
| Git提供者 | 正式包 | `支持库/适配层/Git提供者` | 2.50.1 | git@2.50.1·外部应用·环境依赖 | 依赖锁.json 包[] 1 项：git=环境依赖<br>AST 子进程调用 2 处：subprocess.Popen、subprocess.run |
| LibreOffice提供者 | 正式包 | `支持库/适配层/LibreOffice提供者` | 26.2.2.2 | LibreOffice soffice@26.2.2.2·外部应用·环境依赖 | 依赖锁.json 包[] 1 项：LibreOffice soffice=环境依赖<br>AST 子进程调用 1 处：subprocess.Popen<br>源码命令令牌：libreoffice、soffice |
| Tesseract提供者 | 正式包 | `支持库/适配层/Tesseract提供者` | 5.5.2 | tesseract@5.5.2·外部应用·环境依赖 | 依赖锁.json 包[] 1 项：tesseract=环境依赖<br>AST 子进程调用 1 处：subprocess.Popen<br>源码命令令牌：tesseract |
| textutil提供者 | 正式包 | `支持库/适配层/textutil提供者` | 26.5.2 | textutil@26.5.2·系统工具·环境依赖 | 依赖锁.json 包[] 1 项：textutil=环境依赖<br>AST 子进程调用 1 处：subprocess.Popen<br>源码命令令牌：textutil |
| 密钥提供者 | 内部件 | `支持库/适配层/密钥提供者` | — | — | 无 依赖锁.json<br>AST 子进程调用 1 处：subprocess.run<br>shutil.which：security<br>源码命令令牌：security |
| 本地进程适配器 | 内部件 | `支持库/适配层/本地进程适配器` | — | — | 无 依赖锁.json<br>AST 子进程调用 1 处：subprocess.Popen<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| 浏览器自动化提供者 | 正式包 | `支持库/适配层/浏览器自动化提供者` | 0.1.13 | browser-use@0.1.13·外部应用·环境依赖 | 依赖锁.json 包[] 1 项：browser-use=环境依赖<br>AST 子进程调用 3 处：subprocess.Popen、subprocess.check_output<br>源码命令令牌：agent-browser、browser-use |

共 8 件。

### 2.1 附注：无外部依赖（标准库/仓内装配件）

这些内部件既非第三方库也非环境依赖；按现场真实性质单列，不塞进上面两段。

| 件 | 件类别 | 路径 | 版本 | 依赖（名称@版本·来源·类别） | 现场依据 |
|---|---|---|---|---|---|
| HTTP服务适配器 | 内部件 | `支持库/适配层/HTTP服务适配器` | — | — | 无 依赖锁.json<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| MySQL提供者 | 内部件 | `支持库/适配层/MySQL提供者` | — | — | 无 依赖锁.json<br>**备注**：现场自述「占位」：未接真实外部依赖 |
| __init__ | 内部件 | `支持库/适配层/__init__.py` | — | — | 无 依赖锁.json |
| 动态库适配器 | 内部件 | `支持库/适配层/动态库适配器` | — | — | 无 依赖锁.json<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| 提供者注册表 | 内部件 | `支持库/适配层/提供者注册表` | — | — | 无 依赖锁.json<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| 提供者配置 | 内部件 | `支持库/适配层/提供者配置` | — | — | 无 依赖锁.json |
| 数据库适配器 | 内部件 | `支持库/适配层/数据库适配器` | — | — | 无 依赖锁.json<br>**备注**：现场自述「模拟提供者」：未接真实外部依赖 |
| 模型HTTP提供者 | 内部件 | `支持库/适配层/模型HTTP提供者.py` | — | — | 无 依赖锁.json |
| 脱敏模式 | 内部件 | `支持库/适配层/脱敏模式.py` | — | — | 无 依赖锁.json |
| 适配契约 | 内部件 | `支持库/适配层/适配契约.py` | — | — | 无 依赖锁.json |
| 配置契约 | 内部件 | `支持库/适配层/配置契约` | — | — | 无 依赖锁.json |
| 配置安全 | 内部件 | `支持库/适配层/配置安全` | — | — | 无 依赖锁.json |
| 配置读取 | 内部件 | `支持库/适配层/配置读取` | — | — | 无 依赖锁.json |

## 三、环境依赖的六要素声明（版本/入口/端口/健康探针/启停/降级）

缺项一律如实标注，由后续批次按条目补齐（决策记录 `0021` §未做）——本轮不凭空写。

| 件 | 版本 | 入口 | 端口 | 健康探针 | 启停语义 | 降级语义 | 备注 |
|---|---|---|---|---|---|---|---|
| FFmpeg提供者 | 8.1 | __init__.py | 无 | FFmpeg媒体.检查提供者 | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 缺失/失败 → 提供者不可用（fail-closed，不伪装成功） | — |
| Git提供者 | 2.50.1 | __init__.py | 无 | Git操作.检查提供者 | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 缺失/失败 → 提供者不可用（fail-closed，不伪装成功） | — |
| LibreOffice提供者 | 26.2.2.2 | __init__.py | 无 | LibreOffice转换.检查提供者 | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 缺失/失败 → 提供者不可用（fail-closed，不伪装成功） | — |
| Tesseract提供者 | 5.5.2 | __init__.py | 无 | OCR识别.版本探针 | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 缺失/失败 → 提供者不可用（fail-closed，不伪装成功） | — |
| textutil提供者 | 26.5.2 | __init__.py | 无 | textutil转换.检查提供者 | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 缺失/失败 → 提供者不可用（fail-closed，不伪装成功） | — |
| 密钥提供者 | — | __init__.py | 无 | 未派生（需补） | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 未声明 | — |
| 本地进程适配器 | — | __init__.py | 无 | 未派生（需补） | 无常驻进程：一次调用一次子进程，用完即退（无需启停命令） | 未声明 | 现场自述「模拟提供者」：未接真实外部依赖 |
| 浏览器自动化提供者 | 0.1.13 | __init__.py | 无 | 浏览器自动化.检查可用 | 有常驻/守护进程（现场出现 --session/守护 调用，必须显式启停，缺失时按提供者不可用） | 缺失/失败 → 提供者不可用（fail-closed，不伪装成功） | — |

## 四、待复核（现场读不到即如实登记，未编造）

| 件 | 待复核项 |
|---|---|
| Pillow提供者 | 健康探针：包声明能力列表内未派生出探针能力（现场无 检查/探针 能力 id） |
| PyMuPDF提供者 | 健康探针：包声明能力列表内未派生出探针能力（现场无 检查/探针 能力 id） |
| pdfplumber提供者 | 健康探针：包声明能力列表内未派生出探针能力（现场无 检查/探针 能力 id） |
| reportlab提供者 | 健康探针：包声明能力列表内未派生出探针能力（现场无 检查/探针 能力 id） |
| 密码签名提供者 | 健康探针：包声明能力列表内未派生出探针能力（现场无 检查/探针 能力 id） |
| 密钥提供者 | 版本：依赖锁.json 内根项未标注 版本，版本未登记（不编造）；内部件无 依赖锁.json（按决策记录 0033：有外部依赖应建锁），版本与探针口径待主会话收口；降级语义：实现内未出现「提供者不可用」，降级口径未声明 |
| 本地进程适配器 | 版本：依赖锁.json 内根项未标注 版本，版本未登记（不编造）；内部件无 依赖锁.json（按决策记录 0033：有外部依赖应建锁），版本与探针口径待主会话收口；降级语义：实现内未出现「提供者不可用」，降级口径未声明 |
| 模型服务 | 版本：依赖锁.json 内根项未标注 版本，版本未登记（不编造）；内部件无 依赖锁.json（按决策记录 0033：有外部依赖应建锁），版本与探针口径待主会话收口 |
| 系统探针 | 版本：依赖锁.json 内根项未标注 版本，版本未登记（不编造）；内部件无 依赖锁.json（按决策记录 0033：有外部依赖应建锁），版本与探针口径待主会话收口 |

## 五、判定依据汇总

| 规则 | 口径 |
|---|---|
| 逐项类别 | 锁项 `来源` ∈ {外部应用, 系统工具} ⇒ 环境依赖；否则必须以 AST 现场 import 同名模块证实 ⇒ 第三方库；两者都不成立 ⇒ 判红（无法现场证实，不许猜） |
| 件级类别 | 任一锁项为 第三方库 ⇒ 第三方库；否则有 依赖锁.json ⇒ 环境依赖；无锁则按现场：有第三方 import ⇒ 第三方库，仅有子进程/可执行调用 ⇒ 环境依赖，都没有 ⇒ 无外部依赖 |
| 版本 | 只从各件 依赖锁.json 现场读；读不到写 null 并在 需复核 登记，不编造 |
| 端口 | 扫描 listen/bind/serve 监听调用；全适配层均为按需一次性子进程 ⇒ 无监听端口 |
| 健康探针 | 由各包 包声明.json 的能力列表中派生（能力 id 含 检查提供者/检查可用/检查可用性/版本探针） |
| 启停语义 | 现场出现 --session/close/守护 调用 ⇒ 有常驻进程；否则一次性子进程，无需启停命令 |
| 降级语义 | 实现源码出现「提供者不可用」⇒ 缺失/失败即 fail-closed；未出现则如实标需复核 |
