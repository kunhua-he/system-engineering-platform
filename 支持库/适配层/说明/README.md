# 适配层根目录导航（只作目录导航，不作规则源）

> **定位声明**：本文件只是**目录导航索引**——回答「根下这一项是干什么的、谁在用」。
> 它**不是规则源**，不定义任何豁免或边界。规则源只有一个：
> `AGENTS.md` 的《每个支持库必备》下的「系统适配器豁免」段 + `开发文档/项目说明.md`。
> 两者与本文件不一致时，以那两份为准。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 一、根下两类内容

`支持库/适配层/` 根下混放两类内容（历史遗留形态，2026-09-17 收口时逐项登记）：

1. **正式包**：带 `包声明.json` 的 `*提供者/` 目录，属第三方翻译层的正式支持库，
   不受「系统适配器豁免」，按 `AGENTS.md`《每个支持库必备》交齐要件。
2. **内部适配器**：不带 `包声明.json` 的目录与根级散装 `.py`，属 `AGENTS.md`
   「系统适配器豁免」所指的**不对外提供能力契约**的内部件；`扫描正式包` 以
   `包声明.json` 为标记，不收录它们。

本文件只登记第 2 类。第 1 类的权威说明在各包 `说明/使用说明.md` 与 `说明/设计说明.md`。

> A/B 形态的可复核判据与逐目录依据见本文**第四节**；提供者注册机制两套并存的登记与对齐判据见**第五节**。

## 二、豁免口径下的内部件清单（2026-09-17 实测）

引用计数口径：`rg` 全仓模块路径级引用（排除 `工程缓存/`、`__pycache__/`、
`开发文档/参考资料/`、`开发文档/归档/`），只数 `.py` 命中。

### 2.1 无 `包声明.json` 的目录（12 个）

| 目录 | 职责 | 文件 | 代码引用 | 进编译器固定复制表 |
|---|---|---|---|---|
| `提供者注册表/` | 外部提供者的注册、唯一选择与路由；`标准提供者.py` 注册四类外部适配器 | 2 `.py` | 5 处（`运行核心/运行环境管理器/提供者生命周期.py`、`测试中心/运行核心/测试_提供者生命周期.py`、`适配层/__init__.py`） | 是 |
| `配置契约/` | 提供者配置需求声明与校验（同一适配能力只能选一个提供者） | 1 `.py` | 2 处（`提供者注册表/` 内部两文件） | 是 |
| `提供者配置读取/` | 提供者层：读外部提供者配置 JSON 并按能力 id 取配置段（**原名 `配置读取/`**，2026-09-18 配置双入口收口改名，只统一命名不合并） | 2 `.py` | **0 处**（见 2.4 待决项） | 否 |
| `配置安全/` | 配置密钥引用边界：环境变量引用与运行时解析 | 1 `.py` | **0 处导入**（与 `开发文档/未完成事项.md` 的「未接线」记账一致） | 否 |
| `提供者配置/` | 提供者配置数据 `提供者配置.json` | 1 `.json` | 经 `提供者注册表/` 与 `提供者配置读取/` 读取 | 否 |
| `密钥提供者/` | 真实密钥提供者：环境变量与 macOS 钥匙串（密钥值只在内存） | 1 `.py` | 经 `适配层/__init__.py::创建密钥提供者` **函数体内延迟导入**（刻意不包级 import，理由见该函数 docstring） | 否（刻意排除） |
| `数据库适配器/` | 数据库外部适配契约 + 模拟提供者（不连真实数据库） | 1 `.py` | 1 处（`适配层/__init__.py`） | 是 |
| `HTTP服务适配器/` | HTTP 服务外部适配契约 + 模拟提供者（不发真实网络请求） | 1 `.py` | 1 处（`适配层/__init__.py`） | 是 |
| `本地进程适配器/` | 本地进程外部适配契约 + 模拟提供者（不启动真实进程） | 1 `.py` | 6 处（`适配层/__init__.py`、`提供者注册表/标准提供者.py`、`测试中心/支持库/测试_进程文件补齐.py`、`测试中心/模块库/测试_自修复工具.py`） | 是 |
| `动态库适配器/` | 动态库外部适配契约 + 模拟提供者（不加载真实动态库） | 1 `.py` | 1 处（`适配层/__init__.py`） | 是 |
| `MySQL提供者/` | **占位**：未实现、未注册、不可调用，只冻结未来目录树与公开命令 | 1 `.md` | 仅文档（`开发文档/未完成事项.md`、`开发文档/方案/底座升级方案.md`） | 否 |
| `说明/` | 本目录导航与形态判据文档（`README.md`、`依赖分两段.md`）；**2026-09-19 新增，此前清单为 11 个** | 0 `.py`、2 `.md` | 仅文档 | 否 |

### 2.2 根级散装 `.py`（7 个）

| 文件 | 职责 | 代码引用 | 进编译器固定复制表 |
|---|---|---|---|
| `__init__.py` | `支持库.适配层` 包级公开入口（公开 16 个符号） | 编译器固定复制 + 全仓 `from 支持库.适配层 import …` | 是 |
| `适配契约.py` | 外部适配器统一接口（检查可用/获取版本/建连或启动/最小操作/关闭或释放/统一结果/资源状态） | 5 处（4 个 `*适配器/` + `__init__.py`） | 是 |
| `系统探针.py` | 系统工具健康探针（独立子进程）+ 系统内存汉化原子能力 | 20 处（`启动监督器/健康监督.py`、`运行核心/环境指纹/环境指纹.py`、`大语言模型支持库/模型连接器`、`文档转换支持库` 等 + 4 个测试） | 是 |
| `脱敏模式.py` | 通用敏感信息正则（脱敏工具与密钥引用共用） | 3 处（`运行核心/运行诊断/运行事件/脱敏工具.py`、`配置安全/密钥引用.py`）+ 1 测试 | 是 |
| `模型HTTP提供者.py` | LLM/向量/重排的本地与云端统一 HTTP 协议提供者 | 12 处（`大语言模型支持库/模型连接器/实现/模型连接器.py` 与 `实现/模型连接基元.py`（2026-09-19 拆分后 HTTP 兼容调用器的新家）、`大语言模型支持库/__init__.py`、5 个测试） | 否 |
| `模型服务.py` | 底座内部 HuggingFace 权重加载器脚本（GGUF 以外的模型源） | 1 处：`大语言模型支持库/模型连接器/实现/本地启动准备与守卫.py`（2026-09-19 拆分后 `_构建本地启动命令` 的新家；以路径拼接方式引用，非 import） | 否 |
| ~~`密码适配.py`~~ | **已删除**（2026-09-17，`4d4a12d5`）：直连 cryptography 的历史兼容文件，正式代码早已迁到 `密码签名提供者` | 0 处（删除前已核） | — |

### 2.3 已消除的同名歧义

`模型服务/`（0 文件空目录）曾与 `模型服务.py` **同名并存**，构成一处目录/模块同名歧义。
2026-09-17 处置：**删除空目录 `模型服务/`**，唯一实现保留为 `模型服务.py`。
判据：`模型服务.py` 被 `模型连接器.py:638` 真实引用；`模型服务/` 无文件、无任何引用。
现状：`支持库/适配层/` 根下**不存在**名为 `模型服务` 的目录。

### 2.4 原待决项处置结果（2026-09-17 已结）

1. **`密码适配.py` → 已删除**（提交 `4d4a12d5`）：0 代码引用、自述已废弃；同批改正了两处域外叙述——
   `运行核心/依赖防火墙.py` 的豁免理由注释（例子换成仍真实存在的 `模型HTTP提供者.py`）与
   `密码签名提供者/说明/设计说明.md` 的两处过期描述。验收：`适配层` 16 个公开入口逐个导入 `OK`、
   `sys.modules` 无 cryptography、`支持库.适配层.密码适配` 已 `ModuleNotFoundError`。
2. **`配置读取/` → 保留**：它被 `AGENTS.md`「系统适配器豁免」清单**显式列名**，属**被承认的内部适配器**，
   不是误建物；**零引用本身不构成删除理由**（豁免清单就是它的存在依据）。
   判据（供后续同类判断）：**先分「合规豁免件」与「无据误建物」——前者零引用也留，后者零引用清。**
   **2026-09-18 追加（二次审计 §7 横切发现）**：该目录已改名为 `提供者配置读取/`。二次审计把它记为
   「与 `项目适配层/配置适配/` 并存的配置双入口、疑似同一件事两套实现」；**经读两边实现 + 分层口径裁定为
   合法门面层 / 提供者层分层，不合并，只统一命名** —— 定性依据、改名影响面与反向验证见
   `开发文档/分析/配置双入口收口_20260918.md`。

## 三、与编译器的关系

`开发工具/项目编译/项目编译器.py` 在编译制品时**只复制固定表**，不遍历根下全部内容：

- `运行时适配文件`（第 28 行）= `__init__.py`、`脱敏模式.py`、`系统探针.py`、`适配契约.py`
- 固定复制目录（第 172、636 行）= `提供者注册表`、`密码签名提供者`、`数据库适配器`、
  `HTTP服务适配器`、`本地进程适配器`、`动态库适配器`、`配置契约`

因此 `密钥提供者/`、`提供者配置读取/`、`配置安全/`、`提供者配置/`、`MySQL提供者/`、
`模型服务.py`、`模型HTTP提供者.py`、`密码适配.py` **不进闭包式制品**。
`密钥提供者/` 不在表内是有意为之（见 `__init__.py::创建密钥提供者` docstring）。

**给后续改动者**：把根级散装 `.py` 移进子目录会**改变依赖防火墙的豁免判定**
（`运行核心/依赖防火墙.py:451-468`：豁免只覆盖「根下直接文件」与「`*提供者/` 子目录」，
不覆盖其他子目录）——搬动前必须先确认该文件不 import 第三方，否则会新增违规。

## 四、A/B 形态判据（2026-09-21 #91 登记，只读复核用）

本节把根下目录分成 **A 形态（正式包）** 与 **B 形态（非正式包／内部适配器）**，判据全部取自**现场文件事实**，
可逐条复核。本节**不新增豁免、不定义边界** —— 豁免名录与边界仍以 `AGENTS.md`「系统适配器豁免」为唯一规则源
（现场行号 `AGENTS.md:442-447`；`开发文档/未完成事项.md` #91 里写的 `AGENTS.md:327-329` 是过期行号，此处更正）。

### 4.1 判据（三条，按顺序判）

| 序 | 判据 | 现场取证（在 `支持库/适配层/` 下执行） | A 形态 | B 形态 |
|---|---|---|---|---|
| 1 | 目录内有 `包声明.json` | `for d in */; do test -f "${d}包声明.json" && echo A ${d%/} || echo B ${d%/}; done` | 有 | 无 |
| 2 | 目录内有 `能力定义.json`（对外能力契约） | `find . -maxdepth 2 -name 能力定义.json`（数返回行数） | 有 | 无 |
| 3 | 是否进网关注册表 | `capability_search(关键词=<能力名>)`（命中里应出现 `<包id>.<能力名>`） | 进 | 不进 |

现场复算（2026-09-21）：目录 `ls -1d */` 计 **32** 行（含 `__pycache__/`，真实目录 **31** 个）；
`包声明.json` = **19**；`能力定义.json` = **19**（与包声明同数，无「有声明无定义」的中间态）。
即 **A 形态 19 个 / B 形态 12 个**。`开发文档/未完成事项.md` #91 原记「18 有包声明」是 2026-09-19 快照，
差量 1 个来自 `说明/`（该目录 2026-09-19 16:31 落盘），现场真值是 19。

### 4.2 A 形态 19 个（有 `包声明.json`，按 `AGENTS.md`《每个支持库必备》交齐要件）

`FFmpeg提供者`、`Git提供者`、`LibreOffice提供者`、`MCP协议提供者`、`MLXWhisper提供者`、`PDF隔离提供者`、
`Pillow提供者`、`PyMuPDF提供者`、`Swift编译器提供者`、`Tesseract提供者`、`TreeSitter提供者`、`YAML提供者`、
`pdfplumber提供者`、`psycopg提供者`、`reportlab提供者`、`textutil提供者`、`代码地图提供者`、`密码签名提供者`、
`浏览器自动化提供者`（共 19 个）。判据：`find . -maxdepth 2 -name 包声明.json` 逐条命中。

### 4.3 B 形态 12 个（无 `包声明.json`，逐项带现场依据）

| 目录 | 依据（现场文件事实） | 在 `AGENTS.md:442-447` 豁免名录 |
|---|---|---|
| `HTTP服务适配器/` | 无 `包声明.json`；`__init__.py` + `http服务适配器.py`；自述「模拟提供者（不发真实网络请求）」 | 是 |
| `MySQL提供者/` | 无 `包声明.json`；目录下**只有 `说明/`**（0 `.py`、无实现） | **否**（不在名录；本文 2.1 已记「占位：未实现、未注册、不可调用」） |
| `动态库适配器/` | 无 `包声明.json`；`__init__.py` + `动态库适配器.py` | 是 |
| `密钥提供者/` | 无 `包声明.json`；`__init__.py` + `密钥提供者.py`（环境变量 + macOS 钥匙串） | 是（名录显式列名，但与同段「`*提供者` 包不受此豁免」表面冲突，见 4.4） |
| `提供者注册表/` | 无 `包声明.json`、无 `能力定义.json`、无 `能力契约/`；`__init__.py` + `提供者注册表.py` + `标准提供者.py` | 是 |
| `提供者配置/` | 无 `包声明.json`；只有 `提供者配置.json`（数据目录，0 `.py`） | 是 |
| `提供者配置读取/` | 无 `包声明.json`；`__init__.py` + `提供者配置读取.py` + `配置读取工具.py` + `说明/` | 是 |
| `数据库适配器/` | 无 `包声明.json`；`__init__.py` + `数据库适配器.py`（模拟提供者，不连真实数据库） | 是 |
| `本地进程适配器/` | 无 `包声明.json`；`__init__.py` + `本地进程适配器.py`（模拟提供者，不启动真实进程） | 是 |
| `说明/` | 无 `包声明.json`；只有 `README.md` + `依赖分两段.md`（0 `.py`） | **否**（文档目录，不出能力、不被代码 import，无需豁免） |
| `配置契约/` | 无 `包声明.json`；`__init__.py` + `提供者配置契约.py` | 是 |
| `配置安全/` | 无 `包声明.json`；**无 `__init__.py`**，只有 `密钥引用.py` | 是 |

### 4.4 登记在案的张力（如实记录，不自行处置）

1. `密钥提供者/`：既在豁免名录内，又落在同段「`*提供者` 包与对外发布支持库不受此豁免」的范围内 —— 两句口径互相矛盾，现场形态是 B（无 `包声明.json`）。处置口径未定，登记备查。
2. `说明/`、`MySQL提供者/`：现场是 B 形态，但**不在**豁免名录内。前者是文档目录、后者是占位目录，都不出能力，故不构成门禁缺口；但名录与现场形态并非一一对应。
3. 门禁侧名录 `开发工具/公开调用完整性门禁_包发现.py:54-58` 的 `系统适配器名表`（11 项）与 `AGENTS.md` 名录逐项同源，**不含** `说明`、`MySQL提供者`；该文件自述「改本名录必须同批改 AGENTS.md」。

### 4.5 禁止事项（#91 明文）

不给这 12 个目录补 `包声明.json`／`能力定义.json`／`能力契约/`（#91 期望「**不补能力定义**」）。
补了即落 A 形态，`扫描正式包` 会收录、豁免失效，须按九要素交齐或先裁决 —— 豁免不得自证。

## 五、提供者注册机制两套并存登记与对齐判据（2026-09-21 #90 登记，只读复核用）

### 5.1 并存事实（两套各自独立，都不删不改）

| 项 | 甲：适配层内部件 | 乙：平台控制面正式包 |
|---|---|---|
| 落点 | `支持库/适配层/提供者注册表/` | `平台控制面/提供者/注册表/` |
| 形态（判据见第四节） | B 形态：无 `包声明.json`、无 `能力定义.json`、无 `能力契约/` | A 形态：有 `包声明.json`、`能力定义.json`、`能力契约/参数契约.json`、`实现/`、`说明/` |
| 对外能力契约 | **不出**（0 条） | **4 条**：`平台控制面.提供者.注册表.登记提供者` / `.调用提供者` / `.查询提供者` / `.卸载提供者`（`能力定义.json:7` / `:82` / `:156` / `:214`） |
| 符号面（Python） | `提供者声明`、`提供者注册表`、`提供者路由信息`、`注册全部标准提供者`（`__init__.py:3-6`） | 经 `平台控制面/统一入口/核心.py:114` 实例化：`注册` / `已注册` / `调用` / `预算` |
| 进网关注册表 | 否 | 是（`capability_search(关键词="登记提供者")` 命中 `平台控制面.提供者.注册表.登记提供者` 与 `.调用提供者`） |
| 现场消费者（代码） | `运行核心/运行环境管理器/提供者生命周期.py:100,194`（只读事实点，调 `登记路由`）、`测试中心/运行核心/测试_提供者生命周期.py:34`、`支持库/适配层/__init__.py:37-38` | `平台控制面/统一入口/核心.py:114,123,435,438`、`开发工具/统一能力入口/视图/普通用户视图.py:144`（`已注册` / `预算`） |
| 进编译器固定复制表 | 是（`开发工具/项目编译/项目编译器.py:173,635`） | 是（正式包，随装配发现） |

### 5.2 「零连接」复核结论（2026-09-21 实测）

- 甲乙**互不引用**（判据：`rg -n "平台控制面.提供者.注册表" 支持库/适配层/` 命中 **0**；`rg -n "适配层.提供者注册表" 平台控制面/` 命中 **0**）→ 「两套之间零连接」**仍成立**。
- 但**甲并非死件**：它被 `运行核心/运行环境管理器/提供者生命周期.py` 当「只读事实点」用（`登记路由`）。准确表述是「甲有真实消费者，但甲与乙之间零连接」，不是「两套都零引用」—— 固化此口径以免后续误读。
- 结论：**维持现状、不合并**（与 #90 期望一致）；本节只做登记与判据，不动任何实现代码。

### 5.3 只读对齐判据（可复核；任一条不成立即判「连接已建立／冲突」，须重开裁决，不得静默改）

1. **能力 id 集合交集为空**：甲不出能力契约 → 甲的能力 id 集合恒为 **空集**；乙为 4 条。判据：`find 支持库/适配层/提供者注册表 -name 能力定义.json -o -name 参数契约.json -o -name 包声明.json` 必须 **0 命中**（实测 0）；若命中即判冲突（甲已擅自出契约）。
2. **甲不进网关注册表**：`capability_search` 命中里**不得**出现 `支持库.适配层.提供者注册表.*` 形式的能力 id，只允许 `平台控制面.提供者.注册表.*`。
3. **双向引用恒零**：5.2 的两条 `rg` 必须都为 0 命中；任一侧出现引用即判「两套已连接」。
4. **甲的同层消费者白名单**：只允许 `运行核心/运行环境管理器/提供者生命周期.py`、`测试中心/运行核心/测试_提供者生命周期.py`、`支持库/适配层/__init__.py`（外加甲包内部自引用）；出现新的**跨层**消费者（尤其 `平台控制面/`）即判越界，须先裁决再动。
5. **同名不同物不得互指**：`支持库.适配层.提供者注册表` 是 Python 模块路径（类名 `提供者注册表`），`平台控制面.提供者.注册表` 是能力 id 前缀；两者不得在文档/契约里互相写成对方的 id。
6. **形态不得漂移**：甲一旦补出 `包声明.json`，即落 A 形态，`扫描正式包` 会收录、`AGENTS.md` 豁免失效 → 届时按 A 形态交齐九要素或先裁决。

