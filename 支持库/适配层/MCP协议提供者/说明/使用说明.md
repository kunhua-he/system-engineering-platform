# MCP协议提供者（支持库.适配层.MCP协议提供者）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.0.0
- 说明：第三方 mcp SDK（模型上下文协议）边界的翻译层：把薄壳用到的服务构造、初始化选项、工具定义、文本内容与标准输入输出传输收进适配层，并提供依赖可用性探针。调用者只走包级中文入口。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检查可用性（`适配层.MCP协议提供者.检查可用性`）
- 版本：1.0.0
- 一句话说明：探针：报告 mcp SDK 依赖是否可导入及其版本，供健康检查与依赖审计使用。
- 所属支持库或模块：支持库.适配层.MCP协议提供者
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'可用': '逻辑型，依赖是否可导入', '版本': '文本型，依赖版本（不可用时为空）', '说明': '文本型，判定说明'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：依赖不可用
- 调用示例：适配层.MCP协议提供者.检查可用性()
- 依赖能力：无
