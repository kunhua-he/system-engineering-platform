<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# psycopg提供者（支持库.适配层.psycopg提供者）

- 类型：支持库
- 版本：1.0.0
- 说明：第三方边界：唯一直接接触 psycopg(psycopg3) 的地方，把驱动英文 API 翻译成中文原语供支持库调用；对外 PostgreSQL 能力归 支持库.后端.数据库连接支持库.psycopg数据库。
- 入口：__init__.py

## 能力
### 检查可用性（`支持库.适配层.psycopg提供者.检查可用性`）
- 版本：1.0.0
- 一句话说明：驱动健康探针：返回 psycopg 是否可用与本机驱动版本；不连数据库、无副作用。
- 所属支持库或模块：支持库.适配层.psycopg提供者
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：提供者不可用
- 调用示例：支持库.适配层.psycopg提供者.检查可用性()
- 依赖能力：无
