"""系统探针对外入口（根下平铺件：自举 + 等价再导出腿）。

**#96 包化后**：真实实现已搬到 `支持库/适配层/系统探针提供者/实现/系统探针.py`，
该提供者有 `包声明.json` / `依赖锁.json` / `生命周期契约.json`，进依赖生命周期审计。

本件**保留**，形态与理由：

- 既有 5 个运行时调用方（启动监督器/健康监督、运行核心/环境指纹、
  运行核心/运行环境管理器、文档转换支持库/LibreOffice转换、适配层/textutil提供者、
  适配层/LibreOffice提供者、大语言模型支持库/模型连接器、适配层/硬件画像探针）
  与 4 份测试按 `支持库.适配层.系统探针` 导入 —— 路径、函数签名、字段名**逐字不变**；
- `支持库/适配层/` 根下平铺 `.py` 与包并存是**既有合法形态**（自举 + 等价再导出，
  同 `模型HTTP提供者.py` / `脱敏模式.py` / `硬件画像探针.py`）；
- 本件**只做转发，不复制第二套实现**（哲学：不保留旧腿、结果唯一即收口）。

`mock.patch("支持库.适配层.系统探针.检查系统工具")` 仍落在本模块属性上，不受影响
（见 `开发工具/测试伪装门禁实现/命名空间.py`）。
"""

from __future__ import annotations

from 支持库.适配层.系统探针提供者 import (
    探针结果,
    检查系统工具,
    系统内存快照,
    读取系统内存,
    终止探针子进程,
)

__all__ = [
    "探针结果",
    "检查系统工具",
    "系统内存快照",
    "读取系统内存",
    "终止探针子进程",
]
