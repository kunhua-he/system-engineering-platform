# YAML提供者（支持库.适配层.YAML提供者）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.1.0
- 说明：第三方 PyYAML 边界的翻译层：把 YAML 文本解析为数据、把数据序列化为 YAML 文本，并提供可用性探针。调用者只走包级入口。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 解析YAML（`适配层.YAML提供者.解析YAML`）
- 版本：1.0.0
- 一句话说明：把 YAML 文本解析为数据（字典型/列表型/标量）；空文本或非文本报 参数不合法，文本不是合法 YAML 报 解析失败，PyYAML 未安装报 依赖不可用。
- 所属支持库或模块：支持库.适配层.YAML提供者
- 参数列表：文本:文本型
- 必填参数：文本
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': '解析后的数据（字典型/列表型/标量）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 解析失败, 依赖不可用
- 调用示例：适配层.YAML提供者.解析YAML()
- 依赖能力：无
### 序列化YAML（`适配层.YAML提供者.序列化YAML`）
- 版本：1.0.0
- 一句话说明：把数据（字典型/列表型/标量）序列化为 YAML 文本；不可序列化对象报 序列化失败，PyYAML 未安装报 依赖不可用。
- 所属支持库或模块：支持库.适配层.YAML提供者
- 参数列表：数据:JSON值型
- 必填参数：数据
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'值': 'YAML 文本'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 序列化失败, 依赖不可用
- 调用示例：适配层.YAML提供者.序列化YAML()
- 依赖能力：无
### 检查可用性（`适配层.YAML提供者.检查可用性`）
- 版本：1.0.0
- 一句话说明：探针：报告 PyYAML 依赖是否可导入及其版本，供健康检查与依赖审计使用；依赖不可导入时报 依赖不可用（可重试）。
- 所属支持库或模块：支持库.适配层.YAML提供者
- 参数列表：无
- 必填参数：无
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'可用': '逻辑型，依赖是否可导入', '版本': '文本型，依赖版本（不可用时为空）', '说明': '文本型，判定说明'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：依赖不可用
- 调用示例：适配层.YAML提供者.检查可用性()
- 依赖能力：无
