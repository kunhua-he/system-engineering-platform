{
  "包id": "支持库.适配层.Tesseract提供者",
  "版本": "2.0.0",
  "说明": "Tesseract OCR 外部命令独立提供者：图片 OCR 识别（文本/词级数据）、语言包列表、版本探针。命令路径纳入平台配置覆盖体系：显式配置优先（参数 命令路径 > 环境变量 Tesseract提供者_命令路径），默认可探测（which tesseract）；显式路径不存在 → 工具缺失，不可执行 → 命令失败，版本低于最低版本 → 版本不兼容。外部命令受管执行（独立进程组/超时/取消/输出上限/零残留），工具缺失如实返回 工具缺失。 ★ 批R·R-28（2026-09-24，华哥裁决①「删适配层孪生能力面、保留后端腿 id」）：本包**能力面已删**（能力列表 为空）—— 上面那句是**历史沿革描述**，不再是本包的能力声明；同名能力面收归后端腿 `支持库/后端/OCR识别支持库/OCR识别`（对外 id 不变，生产调用点零改动），本包只剩实现（`实现/` 下），由后端腿经 `载入唯一实现` 直接载入。",
  "能力列表": []
}
