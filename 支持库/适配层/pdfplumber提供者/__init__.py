# 本文件由契约编译器自动生成，禁止手工修改
"""中文公开入口（由能力定义编译器生成）。"""

from __future__ import annotations

from 公共契约.能力契约.契约 import 能力实现

from 支持库.适配层.pdfplumber提供者.实现.PDF文本表格 import 解析PDF
from 支持库.适配层.pdfplumber提供者.实现.PDF文本表格 import 提取表格

__all__ = ["解析PDF", "提取表格", "注册能力"]


def 注册能力(注册表) -> None:
    """由支持库加载器调用，向能力注册表注册本库能力。"""
    注册表.注册(
        能力实现(
            能力id="PDF文本表格.解析PDF",
            包id="支持库.适配层.pdfplumber提供者",
            实现函数=解析PDF,
            参数=["文件路径", "最大页数", "最大字节数", "超时秒"],
            返回="结果",
            说明="pdfplumber 解析 PDF：文本块/表格块 → 通用文档字典；加密检测/页数大小上限",
        )
    )
    注册表.注册(
        能力实现(
            能力id="PDF文本表格.提取表格",
            包id="支持库.适配层.pdfplumber提供者",
            实现函数=提取表格,
            参数=["文件路径", "页序号"],
            返回="结果",
            说明="提取指定页表格数据（二维数组）",
        )
    )
