# PyMuPDF提供者（支持库.适配层.PyMuPDF提供者）

- 类型：支持库
- 版本：1.0.3
- 说明：PyMuPDF（fitz，SWIG 原生扩展）独立提供者：PDF 加密检测/页数/整页渲染/图像提取/校验。原生扩展强制独立子进程，主进程零加载 fitz。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 检测加密页数（`PDF渲染.检测加密页数`）
- 版本：1.0.0
- 一句话说明：fitz 打开 PDF：返回 {已加密: bool, 页数: int}；疑似加密 → 文件加密
- 所属支持库或模块：支持库.适配层.PyMuPDF提供者
- 参数列表：文件路径:文本型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：文件不存在, 参数不合法, 提供者不可用, 文件损坏, 文件加密, 超时, 提供者崩溃
- 调用示例：PDF渲染.检测加密页数(文件路径=支持库/适配层/PyMuPDF提供者/示例/示例.pdf, 超时秒=60.0)
- 依赖能力：无
### 渲染整页（`PDF渲染.渲染整页`）
- 版本：1.0.0
- 一句话说明：fitz 整页渲染为图像字节 base64
- 所属支持库或模块：支持库.适配层.PyMuPDF提供者
- 参数列表：文件路径:文本型, 页序号:整数型, 超时秒:双精度数型
- 必填参数：文件路径, 页序号
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：文件不存在, 参数不合法, 提供者不可用, 文件损坏, 超出限制, 超时, 提供者崩溃
- 调用示例：PDF渲染.渲染整页(文件路径=支持库/适配层/PyMuPDF提供者/示例/示例.pdf, 页序号=1, 超时秒=60.0)
- 依赖能力：无
### 提取图像（`PDF渲染.提取图像`）
- 版本：1.0.0
- 一句话说明：提取页面内嵌图像：返回 [{xref, 字节b64, 尺寸}]
- 所属支持库或模块：支持库.适配层.PyMuPDF提供者
- 参数列表：文件路径:文本型, 页序号:整数型, 超时秒:双精度数型
- 必填参数：文件路径, 页序号
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：文件不存在, 参数不合法, 提供者不可用, 文件损坏, 超出限制, 超时, 提供者崩溃
- 调用示例：PDF渲染.提取图像(文件路径=支持库/适配层/PyMuPDF提供者/示例/示例.pdf, 页序号=1, 超时秒=60.0)
- 依赖能力：无
### 校验PDF（`PDF渲染.校验PDF`）
- 版本：1.0.0
- 一句话说明：fitz 重新打开 PDF 验证：返回 {页数: int}
- 所属支持库或模块：支持库.适配层.PyMuPDF提供者
- 参数列表：字节:字节集型, 超时秒:双精度数型
- 必填参数：字节
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 文件损坏, 超时, 提供者崩溃
- 调用示例：PDF渲染.校验PDF(字节=<示例PDF的base64文本（见 示例/示例.pdf）>, 超时秒=30.0)
- 依赖能力：无
