# 本文件由契约编译器自动生成，禁止手工修改
"""中文公开入口（由能力定义编译器生成）。"""

from __future__ import annotations

from 公共契约.能力契约.契约 import 能力实现

from 支持库.适配层.python_docx提供者.实现.文字文档 import 解析文字文档
from 支持库.适配层.python_docx提供者.实现.文字文档 import 生成文字文档

__all__ = ["解析文字文档", "生成文字文档", "注册能力"]


def 注册能力(注册表) -> None:
    """由支持库加载器调用，向能力注册表注册本库能力。"""
    注册表.注册(
        能力实现(
            能力id="文字文档.解析文字文档",
            包id="支持库.适配层.python_docx提供者",
            实现函数=解析文字文档,
            参数=["文件路径", "格式", "最大字节数", "超时秒"],
            返回="结果",
            说明="解析 DOCX 为通用文档字典：段落块/表格块/图像资源；OOXML 按不可信 ZIP 校验",
        )
    )
    注册表.注册(
        能力实现(
            能力id="文字文档.生成文字文档",
            包id="支持库.适配层.python_docx提供者",
            实现函数=生成文字文档,
            参数=["内容参数"],
            返回="结果",
            说明="按 内容参数（内容块列表：标题/段落）生成 DOCX，返回 结果[生成产物字典]（字节b64/媒体类型/摘要）",
        )
    )
