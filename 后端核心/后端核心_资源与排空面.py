"""资源与排空面：受管资源句柄的三个转发口（状态/续租/关闭）、自动排空装配与终止器登记（混入类）。

**为什么独立成文件**：这一簇是后端核心宿主的**资源治理出口** —— 网关的
`资源状态` / `资源续租` / `资源关闭` 三条能力（`运行核心/统一网关/网关核心.py`）
全部只走本类的三个转发口，加上自动有状态排空的装配（`启用自动排空`）与真实资源
终止函数登记（`注册排空终止器`）。宿主主文件只保留装配、注册、调用与生命周期。

**对外零变化（2026-09-19 拆分）**：本文件成员逐字取自冻结基线
`/tmp/拆分基线/后端核心.py`（812 行，sha256 前16 = 7fa787df454402a3，对应 HEAD
干净工作区），成员名、签名、默认值、函数体一个不改；`后端核心.后端核心.后端核心`
经 MRO 原样继承这些名字。

**宿主契约**（类注解，真源＝`后端核心.py` `后端核心.__init__`）：
`资源句柄服务` / `排空`。`排空` 留 `Any`：`排空管理器` 在 `启用自动排空` 内
**惰性导入**（原导入时机不变，避免模块级新增依赖边）。

**导入方向**：本文件只被 `后端核心.py` 模块级导入，**不得反向导入** `后端核心.py`。
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Any


class 资源与排空面:
    """受管资源转发与自动排空装配簇。

    宿主契约（类注解，真源＝`后端核心.后端核心.__init__`）：
    `资源句柄服务` / `排空`。`排空` 为 `Any`——其真类型 `排空管理器` 由
    `启用自动排空` 惰性导入，保持拆分前导入时机。
    """

    资源句柄服务: Any
    排空: Any

    def 资源状态(self, 句柄: int, *, 项目id: str = "", 所有者: str = "") -> dict | None:
        return self.资源句柄服务.状态(句柄, 项目id=项目id, 所有者=所有者)

    def 资源续租(self, 句柄: int, *, 租约秒: float = 300,
                 项目id: str = "", 所有者: str = "") -> dict:
        return self.资源句柄服务.续租(句柄, 租约秒=租约秒, 项目id=项目id, 所有者=所有者)

    def 资源关闭(self, 句柄: int, *, 项目id: str = "", 所有者: str = "") -> dict:
        return self.资源句柄服务.关闭(句柄, 项目id=项目id, 所有者=所有者)

    def 启用自动排空(self, 排空超时秒: float = 3.0) -> None:
        """启用自动有状态排空：调用前后自动计数，异常路径也减。

        同时把排空管理器接入真实超时执行器（P0 B-02）：**执行线程自己持
        「活动任务」计数** —— 超时后杀不掉的执行单元计数不会减，排空时如实报
        「活动任务 N」。不注册强制终止器：Python 无法强杀线程，注册一个杀不掉
        的终止器等于谎报已回收。
        """
        from 运行核心.资源协调.有状态排空.排空管理 import 排空管理器
        from 运行核心.能力调用.超时执行 import 执行器
        self.排空 = 排空管理器(排空超时秒=排空超时秒)
        执行器.接排空管理器(self.排空)

    def 能力执行残留状态(self) -> dict[str, Any]:
        """真实超时执行器的残留账本快照（超时后还有多少执行单元在跑）。"""
        from 运行核心.能力调用.超时执行 import 残留账本快照
        return 残留账本快照()

    def 注册排空终止器(self, 名称: str, 终止函数: Callable[[], Any]) -> None:
        """登记由后端核心统一触发的真实资源终止函数。"""
        if self.排空 is None:
            self.启用自动排空()
        self.排空.注册强制终止器(名称, 终止函数)

