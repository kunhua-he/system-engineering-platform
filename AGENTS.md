# 系统工程平台 · 项目硬规则

> 更新：2026-09-22｜口径：实测｜唯一事实源：`开发文档/项目说明.md`（不一致时以它为准）
<!-- 机器管理｜类型：权威文档｜生成器：开发工具.MD文档生成 -->

## 开工（本仓库一切改动的唯一入口）

1. **不连 MCP 不开工**：没有开工 ID 的改动不合规。
2. 调 `capability_call`，能力 id = `开工编排.开工准备`，传 `项目根` = 本仓库绝对路径、`任务` = 要做什么、`修改路径` = 要改的文件。返回的 `开工ID` 就是本次标识。
   **要动文件必须走互斥门**：`修改路径` 填全并改用 `开工编排.开工即占`（原子认领，任一条被占即整体失败）；收工调 `开工编排.收工释放`。
3. 要能力用 `capability_search` 搜、`capability_call` 调；**不许在仓库里另起第二套实现**。
4. 收工按 `开工ID` 提交，**只用精确路径**（禁 `git add -A`）。
5. 本协议只在本仓库目录内生效：不在本仓库动手时，不要连本仓库 MCP、不要载入平台上下文。

## 三条铁律（全文见细则，本文只给结论）

- **单腿铁律**：装配 / 验证 / 发布 / 运行只走「编译制品 + HTML/HTTP 传参」，**无豁免**。
- **不保留旧腿**：旧实现被取代直接删，不留兼容入口 / 兼容壳 / 旧名别名；同一件事两套实现即缺陷。
- **改完第一动作**：跑 `python3.14 -m 开发工具.开发编译口.编译口`（默认档 = 读 git 变更集），绿了再继续。

## 细则去哪查（核心在本文，细则全文在 `开发文档/规范/项目细则.md`）

用 `项目文档支持库.查询索引(项目名=系统工程平台, 关键词=<下表词>)` —— 返回 文件路径 / 行区间 / 片段，**不必整份读文档**。

| 要做什么 | 关键词 |
|---|---|
| 派子代理 / 写任务信 / 继承协议 | `子代理 任务信 继承协议` |
| 连 MCP / 代码地图 / 薄壳 / 能力入口 | `MCP 代码地图 薄壳` |
| 单腿铁律全文 / 验收 / 验证分级 | `单腿 制品 验收` |
| 命令环境 / 提交纪律 / 工具姿势（开工默认只给前两类全文） | 传 `开工编排.开工准备` 的 `细节级别=完整` |
| 写文档 / 归档 / 文档分类 | `文档 归档 规则` |
| 文件放哪 / 目录约定 | `目录约定` |
| 分层归属 / 谁依赖谁 | `唯一分层方案` |
| 新建支持库 / 模块必备件 | `每个支持库必备` |
