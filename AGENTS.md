# 系统工程平台 · 项目硬规则

## 项目 MCP 与代码地图

- 本仓库使用独立 MCP：system_engineering_toolkit（Streamable HTTP，配置见根目录 opencode.json，remote http://127.0.0.1:8766/mcp/）。
- 平台 MCP 为单一对外网关，无角色门面，工具直通网关；华世王镞_v3 侧通过聚合 MCP 接入本平台（se_ 前缀路由，只负责路由，不改变工具面）。（聚合方案落地中）
- 新会话开工先调用 project_context，读取项目身份、代码地图状态、证据可信度和最近 1 至 3 次成功验证。
- MCP 客户端连接失败时，允许使用 Python 标准库、curl 或受控 HTML 后端代理直连同一 `http://127.0.0.1:8766/mcp/`；这是传输降级，不是新建第二套 MCP。降级客户端必须完成 `initialize`，声明 `Accept: application/json, text/event-stream`，保存并回传 `mcp-session-id`，随后按 JSON-RPC 调用同一工具名。
- 降级直连只能替代客户端适配，不能绕过 `project_context`、`mcp_feedback`、`verify_and_record`、路径边界和验证命令白名单。浏览器 HTML 不得直接保存凭据或伪造反馈；应由本地 Python/HTTP 代理代发请求。
- 理解或定位代码先调用 codegraph_explore；.codegraph/ 是本仓库独立代码地图，不与任何业务项目共用。
- 运行时验证账本只写入本仓库 开发文档/项目证据/ 的机器文件；当前版本功能和用法只写入 `开发文档/项目说明.md` 及包级说明，不再新增项目记忆、阶段报告或临时审计 Markdown。
- 证据可信度由代码地图、当前 Git/工作区指纹和真实成功验证计算，不允许 Agent 主观自评。

## 单一网关与子代理继承协议

本平台只保留一个权威 MCP 服务 system_engineering_toolkit（HTTP 8766），
作为单一对外网关；不设角色门面，工具直通网关，不得为支持库、模块、核心
分别复制一套会产生分叉状态的 MCP 实例。

任务信必须声明目标项目路径与允许修改目录。权限与写入边界由任务信声明的
允许修改目录约束，发布门禁双重把关，提示词不是唯一安全边界。

### 子代理继承协议（任务信必带）

主代理派发子代理或孙代理时，必须在提示词中原样写入：

1. 目标项目绝对路径和专属 MCP 实例；
2. 允许修改目录；
3. 第一条调用必须是 project_context，并核对返回的项目名称和根目录；
4. 随后调用 codegraph_explore，读取证据可信度和最近 1 至 3 次成功验证；
5. 使用完 MCP 后调用 mcp_feedback，明确填写总结、不满意、多余、缺失和升级建议；无问题也填写“无”；
6. 反馈后调用 verify_and_record，只有退出码为 0 的验证才能记为成功；
7. 不得使用其他仓库的 MCP、记忆、代码地图或验证证据；
8. 子代理继续派发孙代理时必须继续传递本协议；
9. 回传必须包含 MCP 实例、项目根、代码地图查询、MCP反馈、修改文件、验证命令、退出码和剩余风险。

缺少以上开工证据、根目录不一致、MCP 不可用或代码指纹失配时，立即阻断，不得绕过 MCP 修改。

### MCP 连接降级

`MCP工具箱/项目服务.py` 是唯一权威实现。服务未监听时先运行
`./MCP工具箱/启动HTTP服务.sh 8766`；若 Agent 仍无法注入 MCP 工具，可按
`开发文档/项目说明.md` 的 MCP 段落使用 Python/curl 完成同一 Streamable HTTP 会话。
直连成功只证明工具服务可达，仍须按本文件顺序完成开工、反馈和验证。

### 发信与收信反馈闭环

- 发出的每封任务信必须原样包含：开工调用、允许修改目录、子代理继承、mcp_feedback 五字段、反馈后验证、回信携带开工id。
- 收到交付时，维护者不能只读回信；必须调用 feedback_review(work_id=回信开工id) 读取原始反馈和升级候选。
- 每条升级候选必须分类为“本次立即修复、进入平台升级池、无需处理”，并写明理由；影响当前正确性、权限、数据或资源释放的问题必须立即修。
- 没有开工id、没有原始反馈、反馈与任务不匹配或验证发生在反馈之前，均视为交付链不完整，不得验收完成。

### 验证分级与串并行节奏

任务信必须选择且只选择一个验证级别：

| 级别 | 执行者 | 命令与边界 |
|---|---|---|
| 工作包 | 子代理/开发者 | 只跑明确测试文件；多文件无共享资源时使用 --并行数 |
| 合并波次 | 波次协调者 | 只跑受影响的 --阶段，保持固定阶段顺序 |
| 阶段收口 | 主协调者 | 全部工作包合并后只跑一次常规全量 |
| 正式发布 | 发布者 | 只跑一次 --范围 全部 和发布门禁 |

工作包命令示例：

python3.14 测试中心/运行测试.py --测试文件 测试中心/模块库/测试_甲.py 测试中心/模块库/测试_乙.py --并行数 8

合并波次命令示例：

python3.14 测试中心/运行测试.py --阶段 静态契约 --阶段 组件合规

并行按资源键判定，不按机器性能猜测：使用独立临时目录、纯计算和只读契约的测试可并行；同一固定端口、测试数据库、发布指针、外部应用、共享目录或强杀目标必须串行。子代理不得自行跑全量，已有全量运行时禁止重复启动。

慢速场景作为运行环境证据保留，但不再无条件重跑。源码、依赖锁、Python、操作系统、CPU架构和关键外部程序文件指纹一致，且成功证据未超过7天时直接复用。只有环境或相关实现变化、证据过期、故障修复回归时才运行：

python3.14 测试中心/运行测试.py --范围 慢速 --强制慢速

## 定位

本仓库是与任何业务项目完全平级、完全独立的系统级工程底座（唯一事实源：
`开发文档/项目说明.md`，本仓库内部权威冻结文档；本文件
只允许与其一致，不一致时以冻结文档为准并在同一任务内修复本文件）。

分层：公共契约 -> 支持库（原子能力/第三方边界）-> 模块库（组合流程）
-> 项目适配层（绑定版本/配置/权限/路径/密钥引用与项目别名）-> 正式代码
-> 运行核心（装载/调度/资源/调用/治理）-> 平台控制面（目录/策略/制品/发布/证据）。

## 唯一分层方案

```text
正式代码
  -> 项目适配层、统一网关或统一能力服务
  -> 模块公开能力（模块只保存能力 id/契约版本，不静态导入支持库/提供者）
  -> 运行核心注入的唯一能力调用器 -> 能力注册表解析支持库能力
  -> 锁定提供者与受管执行单元
  -> 数据库、服务器、DLL、操作系统、第三方库或外部协议
```

职责固定：

- 支持库只提供原子能力、参数/返回/错误转换和资源释放；一个第三方发行包
  只能归属一个独立支持库和一个受管提供者。
- 模块只组合公开能力完成流程，不直接连接第三方库；即使只需一个原子能力
  也必须使用薄模块门面。
- 项目适配层只绑定模块版本、配置、权限、路径、密钥引用和项目中文别名；
  不得调用支持库、提供者或实现源码。
- 正式代码只处理统一结果（成功/值/错误码/错误说明/可重试/详细信息），
  不得导入第三方、支持库、提供者或任何实现源码。
  示例项目与测试代码作为调用方样板，允许经包级公开入口调用（与调用者条款一致）。
- 运行核心只有一个权威能力注册表、一个提供者路由和一条调用证据链；
  公开能力重复注册失败，只允许全摘要一致的幂等重放。
- 回滚只切换不可变旧制品和激活指针，禁止恢复源码 v0 分支或翻转废弃标记。
- 禁止通过 sys.path、包 __path__、动态导入或全局单例拼接两个源码树。
- 第三方强制英文只留在支持库适配层；正式业务语义一律中文。

统一公开结果固定包含：成功、值、错误码、错误说明。底层异常不得直接泄漏到正式代码。

## 验收

主验收命令（唯一标准入口）：

```bash
python3.14 测试中心/运行测试.py            # 常规
python3.14 测试中心/运行测试.py --范围 慢速  # 慢速（强杀/压力/真实外部提供者）
python3.14 测试中心/运行测试.py --范围 全部  # 顺序执行全部
python3.14 开发工具/发布门禁/运行发布门禁.py   # 正式发布唯一判定
python3.14 开发工具/能力搜索/能力搜索器.py --能力 读取文件
python3.14 示例项目/适配层示例/运行入口/运行.py
```

门禁：实际加载全部测试；零测试、导入失败、未解释跳过、债务通过、缓存假绿
全部必须失败；发布只有“通过/阻断”两种结果，其余均非零退出码。

> 废弃说明：`python3.14 -m unittest discover -s 测试中心 -p '测试_*.py'`
> 因 unittest 的 VALID_MODULE_NAME 只认 ASCII 标识符（中文测试文件名
> 被跳过），实测返回 NO TESTS RAN——存在"零测试成功"漏洞，已废弃为主
> 验收命令，禁止再以此作为验收依据。

## 铁律

- 全中文命名：变量、函数、类、文件、目录、注释一律中文；约定保留的 id
  统一写作 id；只有语言关键字、框架标准属性、第三方、操作系统和外部协议
  强制英文时才保留英文。
- 只允许 Python 标准库；第三方依赖必须封装在 `支持库/适配层/` 对应提供者。
- 严禁导入、引用、读取任何业务项目（含华世王镞_v3）的代码、数据库、配置、
  测试、运行数据。
- 支持库只提供原子能力；模块只组合支持库；加载器只负责发现、解析、装载
  与生命周期；核心只负责装载、调度、资源、调用和运行治理。
- 跨包调用只能经过公开契约与包级中文入口，禁止深入实现目录。
- 调用者只能 `from 支持库.后端.文件系统 import 读取文件`，
  禁止 `from 支持库.后端.文件系统.实现.文件系统 import 读取文件`。
- 测试统一放在 `测试中心/`，测试代码使用标准库 unittest，不依赖 pytest。
- 工程缓存可删除、可重新生成，不作为正式源代码；测试产物和快照只放工程缓存。
- 原生扩展、动态库和不可控全局状态第三方强制独立进程，主进程不得导入。
- 所有线程、进程、队列、缓存、日志、临时文件、句柄和终态记录必须有界。
- 新能力必须先有确认需求、能力搜索、复用决策、占用租约、验收契约、
  装配计划和工作包；缺少任一项不得开工。
- 发布前必须通过依赖防火墙（平台与各接入项目）并成为构建、签名、安装和
  激活事务的必经步骤；独立脚本未被发布入口调用等同门禁不存在。

## 目录约定

```text
公共契约/   包声明、能力契约、基础类型、错误结构、生命周期、版本规则
平台控制面/ 需求登记、能力目录、包仓库、策略中心、发布管理、证据账本（唯一写权威）
启动监督器/ 核心启动、资源限制、健康检查、版本切换与核心回退
运行核心/   加载器、任务调度、资源协调、能力调用、统一网关、运行诊断、
            依赖防火墙、运行环境管理器、环境指纹
前端核心/   窗口、页面、组件、状态、事件、路由和渲染宿主
后端核心/   后端执行单元、请求上下文、能力执行和宿主级生命周期
支持库/     后端原子支持库、前端描述模型、适配层（第三方/系统/外部协议边界）
模块库/     只组合支持库完成功能的样板模块
项目适配层/ 项目声明、配置合并、依赖锁定、模块绑定、运行入口
开发工具/   统一能力入口、能力搜索、说明书生成、契约编译、组件规范、
            组件合规、复用审计、发布门禁、统一验证
测试中心/   独立测试环境（unittest），唯一测试代码位置
示例项目/   最小运行样板
客户端/     平台客户端制品构建脚本
MCP工具箱/  平台 MCP HTTP 服务工具箱（启动脚本、验证门禁等）
开发文档/   当前版本说明、架构/能力契约与源码参考研究；机器验证账本只保留 JSON/JSONL
工程缓存/   生成物（可删除）
```

能力扩充按“运行域 / 能力领域 / 组件包”分类。物理目录不作为公开调用契约。

## 每个支持库必备

- `包声明.json`：包id、类型、版本、依赖、能力声明
- `能力契约/`：能力参数与返回声明
- `实现/`：原子能力实现（不对外暴露）
- `说明/`：使用说明
- `验证场景引用.json`：验证场景引用
- `完整性摘要.json`：文件清单与哈希
- `__init__.py`：包级中文入口

> 系统适配器豁免：`支持库/适配层/` 下不对外提供能力契约的内部适配器（配置读取、
> 配置契约、提供者注册表、提供者配置、配置安全、HTTP服务适配器、数据库适配器、
> 本地进程适配器、动态库适配器、密钥提供者、系统探针 等）不要求 `包声明.json`/`完整性摘要.json`/
> `能力契约/`/`验证场景引用.json`；`扫描正式包` 以 `包声明.json` 为标记，不收录此类
> 适配器。`*提供者` 包与对外发布支持库不受此豁免。

## 验证

```bash
python3.14 -m py_compile $(find . -type f -name '*.py' -not -path './工程缓存/*')
python3.14 测试中心/运行测试.py
```

禁止：零测试仍然成功、导入失败当跳过、未解释跳过、资源未清理、
依赖循环被忽略、多提供者冲突被忽略、只验证导入不验证真实返回值。
