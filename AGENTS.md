# 系统工程平台 · 项目硬规则

> 更新：2026-09-24｜口径：实测｜唯一事实源：`开发文档/项目说明.md`（不一致时以它为准）
<!-- 机器管理｜类型：权威文档｜生成器：开发工具.MD文档生成 -->

## 开工（本仓库一切改动的唯一入口）

1. **不连 MCP 不开工**：没有开工 ID 的改动不合规。
2. 调 `capability_call`，能力 id = `开工编排.开工准备`，传 `项目根` = 本仓库绝对路径、`任务` = 要做什么、`修改路径` = 要改的文件。返回的 `开工ID` 就是本次标识。
   **要动文件必须走互斥门**：`修改路径` 填全并改用 `开工编排.开工即占`（原子认领，任一条被占即整体失败）；收工调 `开工编排.收工释放`。
3. 要能力用 `capability_search` 搜、`capability_call` 调；**不许在仓库里另起第二套实现**。
4. 收工按 `开工ID` 提交，**只用精确路径**（提交纪律全文见 `开发文档/规范/多会话并行开发规约.md` §九；禁 `git add -A`）。
5. 本协议只在本仓库目录内生效：不在本仓库动手时，不要连本仓库 MCP、不要载入平台上下文。
6. **一次取全**：`开工准备` 多传 `资料级别='指针'`（默认预算 8000 字符）即一次拿到六项开工资料（开工ID+租约 / 涉及路径占用状态 / 契约与判据指针 / 规范片段 / 能力映射 / 纪律段）；超预算自动降级成「文件:行区间」指针（零信息损失）。要纪律全文再传 `细节级别='完整'`。
7. **硬纪律**：凡可经 MCP 拿到的信息**不得用终端取**（终端只留三类：明令只读取证脚本 / git 只读状态 / MCP 确实不覆盖的检查）；确实拿不到就在回传里**如实回报缺口**，不许退回终端硬凑（全文＝`通用纪律.工具姿势` 第 31 条）。

## 三条铁律（全文见细则，本文只给结论）

- **单腿铁律**（全文：`开发文档/规范/项目细则.md`「单腿铁律：只走 HTML / 制品一条腿」）：装配 / 验证 / 发布 / 运行只走「编译制品 + HTML/HTTP 传参」，**无豁免**。
- **不保留旧腿**：旧实现被取代直接删，不留兼容入口 / 兼容壳 / 旧名别名；同一件事两套实现即缺陷。
- **改完第一动作**：调 MCP `自修复工具.静态编译`（`类型=1`、`模块ID`=你改的库/模块；**能力 id 必须带包前缀**，短名会被网关判 `能力未注册`）。**测试只允许用 MCP 调 `自修复工具.静态编译` / `自修复工具.动态编译` 其中一个**，其余测试入口一律算旁路。

## 编译与测试：整个项目只有 2 个公开能力（哲学 7.1 / 7.5）

| 能力 | 一次调用做完什么 | 传参 | 返回 |
|---|---|---|---|
| **静态编译** | 不带实际数据：代码合规 + 契约 + 对应生成物（含制品生成、全部派生件、静态门禁）+ 全仓按库/模块分片并行 | `类型`（`0`=全仓 / `1`=指定库或模块）、`模块ID`（路径，`类型=0` 时可空） | 成功数量 / 失败数量 + 逐条明细（模块ID 或 库ID、失败原因、**缺乏什么导致无法自动自愈**） |
| **动态编译** | 静态编译的全部内容 **+ 实际测试脚本实测**（测试必须超小型） | 同上 | 同上 |

- **其余一切编译/测试入口都是旁路**：发现旁路或散装节点 ⇒ **撤掉它的 HTML 能力、转成内部能力**（公开能力＝成熟稳定的；内部能力＝公开能力的某一个节点）。收口要**两面一起撤**：`包声明.json` 的 `内部层=真` **且** 页面不再引用该 `能力id`。
- 判据：任一编译/测试动作若不在这两个能力内、或不经 MCP 调用，即旁路。

## 细则去哪查（核心在本文，细则全文在 `开发文档/规范/项目细则.md`）

用 `项目文档支持库.查询索引(项目名=系统工程平台, 关键词=<下表词>)` —— 返回 文件路径 / 行区间 / 片段，**不必整份读文档**。

| 要做什么 | 关键词 |
|---|---|
| 派子代理 / 写任务信 / 继承协议 | `子代理 任务信 继承协议` |
| 连 MCP / 代码地图 / 薄壳 / 能力入口 | `MCP 代码地图 薄壳` |
| 单腿铁律全文 / 验收 / 验证分级 | `单腿 制品 验收` |
| **编译只有 2 个公开能力（静态/动态）/ 旁路转内部 / 传参标准** | `哲学 7.1 静态编译 动态编译 旁路 内部能力` |
| 命令环境 / 提交纪律 / 工具姿势（开工默认只给前两类全文） | 传 `开工编排.开工准备` 的 `细节级别=完整` |
| 写文档 / 归档 / 文档分类 | `文档 归档 规则` |
| 文件放哪 / 目录约定 | `目录约定` |
| 分层归属 / 谁依赖谁 | `唯一分层方案` |
| 新建支持库 / 模块必备件 | `每个支持库必备` |
