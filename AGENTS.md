# 系统工程平台 · 项目硬规则

> 最后更新：2026-09-20
> 口径：实测（数字随建包与提交逐日变化，引用前必须重跑取证命令）
> 维护者：项目硬规则（根规则）
<!-- 机器管理｜类型：权威文档｜生成器：开发工具.MD文档生成 -->


## 强制开工协议（2026-09-19 华哥裁决，四条不可跳过）

> 本协议**只在本仓库目录内载入**；不在本仓库动手时不要调用本仓库 MCP，也不要据此载入任何平台上下文。

1. **不连 MCP 不开工**：本仓库的一切改动，必须先经 MCP `system_engineering_toolkit`（或等价地直调唯一网关 40007）走一遍开工，**没有开工 ID 的改动不合规**。
2. **第一步取开工 ID**：调 `capability_call` 能力 id = `开工编排.开工准备`，传参 `项目根` = 本仓库绝对路径（`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`）、`任务` = 本次要做的事、`修改路径` = 要改的文件（可空）。**返回里的 `开工ID` 就是本次开工的唯一标识**（不传则当场生成），后续写任务记忆包、提交 GitHub 都用它。
   **要动文件就必须走互斥门**：把 `修改路径` 填全（可写 `{"路径": …, "指纹": …}`，指纹用 `sha256:<hex>` 或裸 hex 由入口自动归一），改用能力 id = `开工编排.开工即占`（同样返回 `开工ID`）——它**原子认领全部路径，任一条被别人占着就整体不开工并回报占用者**；收工调 `开工编排.收工释放` 释放。**不填 `修改路径` 就没有互斥保护**（`开工准备` 只查不占，这是分工不是缺陷）。
3. **要能力自己搜，不要猜、不要重复造**：用 `capability_search` 搜关键词，拿到能力 id 后用 `capability_call` 调用。找不到能力再考虑补底座，**不许在仓库里另起第二套实现**。
4. **收工按开工 ID 提交**：结束前把改动按 `开工ID` 提交（提交信息带上它），并做收尾（释放租约、登记未完成事项）。

**薄壳只服务本仓库**：`项目根` 必须是**真实存在且含 `AGENTS.md` + `开发文档/项目说明.md` 的目录**，否则调用一律被拒（`非本平台项目根`）且**不返回任何平台规则**——这是刻意的最小 token 设计：非本项目的会话不会被灌入平台上下文。

```text
开工（复制即用）：
  tool = capability_call
  项目根 = /Users/hekunhua/Documents/Agent/PHP/系统工程平台
  能力id = 开工编排.开工准备
  参数   = {"任务": "<本次要做什么>", "修改路径": ["<要改的文件，可空>"]}
  → 返回值里的 开工ID 就是本次开工标识
收工：
  git commit -m "<开工ID> <做了什么>"  -- <精确路径>
```

## 项目 MCP 与代码地图

- 本仓库使用独立 MCP：system_engineering_toolkit（**stdio 薄壳，无 HTTP 端口**；入口 `开发工具/薄壳/薄壳服务.py`，仅 3 个工具；配置见根目录 `opencode.json`、`.mcp.json`）。原 Streamable HTTP `http://127.0.0.1:8766/mcp/` 已随 `MCP工具箱/` 于 2026-09-15 清场下线，照抄连不上。
- 平台 MCP 为单一对外网关，无角色门面，工具直通网关；外部项目只能通过各自声明的公开契约和授权边界消费本平台能力，本仓库不维护外部项目聚合路由或业务接入实现。
- 新会话开工先读 `开发文档/项目说明.md`（唯一事实源）与 `开发文档/未完成事项.md`（唯一债务清单）；证据可信度以 `.codegraph/` 代码地图、当前 Git/工作区指纹和真实成功验证现场计算，不允许主观自评。
- **开工先查历史的坑（不要全量读文档）**：撞报错、写新代码、改验证场景之前，先调一次
  `项目文档支持库.查开工上下文(项目名="系统工程平台", 本次任务="<我正要做什么>")`（经 40007 `/网关/调用`），
  返回 3–5 条相关坑（症状 + 判据 + 原文位置），**不载入 126KB 项目说明与 1065 行哲学**。
  撞报错时把**报错原文**直接当 `本次任务` 再查一次；踩到新坑时用 `项目文档支持库.记踩坑(...)` 落盘。
  详见 `支持库/后端/项目文档支持库/说明/使用说明.md` 的「踩坑条目检索（开工上下文）」节。
- 薄壳只暴露 3 个工具（`capability_search` / `capability_call` / `tool_catalog`，实测见 `开发工具/薄壳/工具清单.py`）；**能力执行一律走 40007 统一网关**（`GET http://127.0.0.1:40007/健康` 可探活）。40006（只读能力发现网关）当前**未启用**（无监听；`launchctl` 的 `com.huashi.gateway-40006` 退出码 2 且 plist 仍指已删路径），现役脚本名为 `开发工具/能力网关/启动能力网关.sh`。MCP 不作为只读审计前置条件，已编译制品可直接通过 HTML 黑盒验证。
- stdio 薄壳的协议握手为 MCP 标准 `initialize` → `tools/list` → `tools/call`（**无 HTTP 会话层，不存在 `mcp-session-id`**）；不得绕过路径边界与验证命令白名单。
- 代码地图 `/.codegraph/` 是本仓库独立代码地图，不与任何业务项目共用；查询走网关能力 `代码解析支持库.代码地图.查询节点` / `查询关系`（参数 `代码地图路径=.codegraph/codegraph.db`，**只读打开**，实测可用）。
- 运行时验证账本只写入本仓库 开发文档/项目证据/ 的机器文件；当前版本功能和用法只写入 `开发文档/项目说明.md` 及包级说明，不再新增项目记忆、阶段报告或临时审计 Markdown。
- 证据可信度由代码地图、当前 Git/工作区指纹和真实成功验证计算，不允许 Agent 主观自评。

## 单一网关与子代理继承协议

本平台只保留一个权威 MCP 服务 system_engineering_toolkit（**stdio 薄壳，3 个工具，无 HTTP 端口**；
原 HTTP 8766 已随 `MCP工具箱/` 于 2026-09-15 清场下线），
作为单一对外网关；不设角色门面，工具直通网关，不得为支持库、模块、核心
分别复制一套会产生分叉状态的 MCP 实例。

任务信必须声明目标项目路径与允许修改目录。权限与写入边界由任务信声明的
允许修改目录约束，发布门禁双重把关，提示词不是唯一安全边界。

### 子代理继承协议（任务信必带）

主代理派发子代理或孙代理时，**必须先经生成器**（不要手写 context —— 巡检实测 19/19 路「工具姿势/提交纪律/命令环境」整批丢失）：

```bash
python3.14 开发工具/任务记忆/派活任务信.py --生成派发 <标题> <任务正文文件> <允许修改路径1> [路径2 …]
```

它自动拼固定条款 + 本任务强规范、自检缺项，输出【goal】【context】两段直接抄进 `delegate_task`。**并行改公共文件前先认领**：`capability_call(能力id='开工编排.开工即占', 参数={'项目根':…,'任务':…,'修改路径':[…],'所有者':…})`，收工 `开工编排.收工释放`。

此外，任务信还必须写入：

条目 1：目标项目绝对路径，以及允许修改目录（目录外一律不碰）；
条目 2：**开工第一步调 `开工编排.开工准备`**（经 40007 网关）—— 它一次返回开工上下文、本次任务的强规范（`任务类型` / `强规范`）**以及无条件返回的通用纪律**（`通用纪律` 键：命令环境 / 工具姿势 / 提交纪律 / 改完必做 / 判据 五段，**与任务类型无关，必然返回**）；拿到后用 `git rev-parse --short HEAD` 核对项目根一致（根目录不一致即停手报告）。**不许只读文档不开工**：纪律以本入口返回的为准，**派活方不必再手抄条款**（实测手抄必漏：37 路 kickoff 里纪律标志全 0 命中）；
条目 3：**提交纪律**（也见 条目 2 返回的 `通用纪律.提交纪律`）：遵守 `开发文档/规范/多会话并行开发规约.md` 的写者降级规程，**只 `git add` 自己改的文件，禁止 `git add -A` / `git add .`**；**`git commit` 也必须带精确路径 pathspec**（索引是全局共享的：只 add 自己文件却跑不带 pathspec 的 commit，会把别人已暂存的文件一起提交 —— 实测已复发两次）；提交后 `git show --stat HEAD` 核对文件数，自述「已提交」必须附提交哈希；
条目 4：需要找代码/能力时，用 `capability_search` 查能力、`capability_call` 执行（经 40007 网关），或读目标包 `说明/设计说明.md`；
条目 5：不得使用其他仓库的 MCP、记忆、代码地图或验证证据；
条目 6：子代理继续派发孙代理时必须继续传递本协议（递归继承，直到最末层）；
条目 7：改过包必须跑 `python3.14 -m 开发工具.开发编译口.编译口 --变更`（并行期用 `--文件 <自己改的文件>`；内含全量重算摘要闭合与静态规则全查，**实测约 28 秒**（2026-09-20 现场计时：单遍 27.5 秒；大头是参数可用性 8.1s + 重复腿与旁路 5.8s + 测试体系门禁 5.8s，其余各项均 <2s）；**每条重命令只跑一遍，输出重定向到文件再从文件取**，不要为看 tail 跑第二遍；编译口不可用时退回 `python3.14 -m 开发工具.全量重算摘要`，口径是全仓「不闭合 0/N」）；
条目 8：每处修复必须配反向验证（故意弄坏一次、确认真的变红）；没有反向验证的修复不算修好；
条目 9：回传必须包含项目根、修改文件清单、验证命令与真实退出码、剩余风险；**产物必须现场验收，不采信自述**。
条目 10：**工具姿势**（见 `通用纪律.工具姿势`）—— **扫描文件（找文件/搜内容/查引用/数行数）仅使用 `rg`**（例 `rg -n '关键词' --glob '*.py' .`；实测 0.07 秒扫完全仓）；**找超长文件与拆分候选仅使用现成清单** `开发文档/分析/超长文件拆分候选_*.md`；**查符号定义/引用/影响面仅使用代码地图**；**扫描统计类动作仅使用 `开发工具/` 下现成工具**（50+ 个）；**读文件仅使用 `read_file`**；**找能力仅使用 `capability_search`**；成组动作落一个脚本跑一次；**命令必须带超时**（`timeout` 已装于 `/opt/homebrew/bin`）。

缺少允许修改目录声明、根目录不一致或产物未经验收时，不得计为完成。

### Agent 接入面（2026-09-15 D 清场后：3 工具薄壳，无端口）

平台对 Agent 的 MCP 接入面收敛为**一个 stdio 薄壳**：`开发工具/薄壳/薄壳服务.py`，只暴露 **3 个工具**：

| 协议名 | 作用 |
|---|---|
| `tool_catalog` | 本地元数据：薄壳工具清单与入参（不转发） |
| `capability_search` | 经唯一网关查已注册能力（转发到 `能力目录.搜索能力`） |
| `capability_call` | 经唯一网关执行任意已注册能力（`POST http://127.0.0.1:40007/网关/调用`，操作=调用能力） |

- **无 HTTP 端口、无访问令牌层**：原 8766 HTTP MCP 服务（50 工具）与 `MCP工具箱/` 整目录已于 2026-09-15 删除；8766 已下线，`.mcp.json` 与 `opencode.json` 已切到 stdio 薄壳（备份 `/tmp/8766切换回退_20260915/`；配置级回退 `git restore --source=8e30554b -- .mcp.json opencode.json`）。
- **凭证**：薄壳只认环境变量 `系统库网关凭证`（`开发工具/薄壳/网关转发.py`），缺失即返回 `网关凭证缺失` 且**不转发**（fail-closed）。客户端经 `MCP_GATEWAY_TOKEN` 插值注入（`~/.zshrc` 里从 40007 plist 取值，不写明文）。bash 不能 `export` 中文变量名，故引用名用 ASCII。**网关直连时凭证走标准头 `Authorization: Bearer <令牌>`（纯 ASCII）；中文只允许出现在 JSON 正文里，绝不放进 HTTP 头**（HTTP 头只能是 ASCII，中文头必然被拒）。
- **薄壳不是第二条执行腿**：只做协议翻译（stdio ↔ 唯一网关 HTTP），不实现业务逻辑、不读源码树内部。
- **自测**：`env "系统库网关凭证=…" python3.14 -B 开发工具/薄壳/自测_stdio客户端.py` → 期望 `tools/list = 3` 且三条调用 `成功=True`。
- **只读能力发现网关（40006）**：脚本 `开发工具/能力网关/启动能力网关.sh 40006`（只读 `/能力/搜索`、`/能力/契约/{能力id}`；执行仍统一走 40007 `POST /网关/调用`）。**当前未启用**：40006 无监听，`launchctl` 的 `com.huashi.gateway-40006` 退出码 2 且 plist 仍指已删路径；脚本历史上也从未跑通（原用中文变量名，bash 直接报 `command not found`，现已改为 ASCII 变量名）。

服务未注入时可直接对编译制品跑 HTML 黑盒；HTML 结果只证明真实 HTTP 功能，不替代治理账本、反馈和正式发布证据。

### 发信与收信反馈闭环

- 发出的每封任务信必须原样包含：目标项目路径与允许修改目录、子代理继承协议、验证级别、回传要素（修改文件/验证命令/退出码/剩余风险）。
- 收到交付时，维护者不能只读回信；必须**现场验收产物**（读文件、跑真实请求），核对回信与实物一致。
- 与平台能力有关的改进建议，经网关能力 `平台控制面.能力反馈` 提交（**不再有 `mcp_feedback` / `feedback_review` 这类 MCP 工具**，它们随 8766 管理端于 2026-09-15 删除）。
- 每条建议必须分类为“本次立即修复、进入平台升级池、无需处理”，并写明理由；影响当前正确性、权限、数据或资源释放的问题必须立即修。
- 没有允许修改目录声明、没有产物、产物与任务不匹配或验证发生在产物之前，均视为交付链不完整，不得验收完成。

### 验证分级与串并行节奏

任务信必须选择且只选择一个验证级别：

| 级别 | 执行者 | 命令与边界 |
|---|---|---|
| 工作包 | 子代理/开发者 | 只跑明确的 unittest 模块入口；多文件无共享资源时按资源键并行 |
| 合并波次 | 波次协调者 | 只跑受影响的 unittest 模块入口，保持固定阶段顺序 |
| 阶段收口 | 主协调者 | 全部工作包合并后只跑一次正式 HTML 黑盒 |
| 正式发布 | 发布者 | HTML 黑盒验证器（编译产物真实 HTTP）+ 发布门禁，只跑一次 |

工作包命令示例（开发期辅助，编译器禁跑全量）：

python3.14 -m unittest 测试中心.第一批维修.测试_HTML验证器

合并波次命令示例（开发期辅助）：

python3.14 -m unittest 测试中心.第一批维修.测试_HTML验证器 测试中心.第一批维修.测试_客户端构建安全

并行按资源键判定，不按机器性能猜测：使用独立临时目录、纯计算和只读契约的测试可并行；同一固定端口、测试数据库、发布指针、外部应用、共享目录或强杀目标必须串行。子代理不得自行跑全量，已有全量运行时禁止重复启动。

HTML 黑盒验证（华哥裁决 2026-08-29，正式发布主判定）：

python3.14 -m 开发工具.HTML验证.验证器 --制品 <编译产物目录> --并发 32

- 编译器绝对禁止跑全量：只编译小单元 + 检查合规，不代替功能验证。
- HTML 可以跑全量：对编译产物发真实 GET/POST，多线程并发，只看状态码 + 返回值。
- 后端怎么实现，HTML 不需要知道：只认编译产物的真实 HTTP 返回。
- 正式发布 = 编译产物能访问、能拿到真实数据 = 通过；不再跑慢速审计。
- unittest 模块入口只作开发期定向回归；发布门禁只认正式激活制品的真实 HTML 黑盒结果。

## 定位

本仓库是与任何业务项目完全平级、完全独立的系统级工程底座（唯一事实源：
`开发文档/项目说明.md`，本仓库内部权威冻结文档；本文件
只允许与其一致，不一致时以冻结文档为准并在同一任务内修复本文件）。

分层：公共契约 -> 支持库（原子能力/第三方边界）-> 模块库（组合流程）
-> 项目适配层（绑定版本/配置/权限/路径/密钥引用与项目别名）-> 正式代码
-> 运行核心（装载/调度/资源/调用/治理）-> 平台控制面（目录/策略/制品/发布/证据）。

## 唯一分层方案

```text
正式代码
  -> 项目适配层、统一网关或统一能力服务
  -> 模块公开能力（模块只保存能力 id/契约版本，不静态导入支持库/提供者）
  -> 运行核心注入的唯一能力调用器 -> 能力注册表解析支持库能力
  -> 锁定提供者与受管执行单元
  -> 数据库、服务器、DLL、操作系统、第三方库或外部协议
```

职责固定：

- 支持库只提供原子能力、参数/返回/错误转换和资源释放；一个第三方发行包
  只能归属一个独立支持库和一个受管提供者。
- 模块只组合公开能力完成流程，不直接连接第三方库；即使只需一个原子能力
  也必须使用薄模块门面。
- 项目适配层只绑定模块版本、配置、权限、路径、密钥引用和项目中文别名；
  不得调用支持库、提供者或实现源码。
- 正式代码只处理统一结果（成功/值/错误码/错误说明/可重试/详细信息），
  不得导入第三方、支持库、提供者或任何实现源码。
  示例项目与测试代码作为调用方样板，允许经包级公开入口调用（与调用者条款一致）。
- 运行核心只有一个权威能力注册表、一个提供者路由和一条调用证据链；
  公开能力重复注册失败，只允许参数形状一致的幂等重放。
- 回滚只切换不可变旧制品和激活指针，禁止恢复源码 v0 分支或翻转废弃标记。
- 禁止通过 sys.path、包 __path__、动态导入或全局单例拼接两个源码树。
- 第三方强制英文只留在支持库适配层；正式业务语义一律中文。

统一公开结果固定包含：成功、值、错误码、错误说明。底层异常不得直接泄漏到正式代码。

## 单腿铁律：只走 HTML / 制品一条腿（华哥 2026-09-21 裁决）

**这条是硬门禁，不是偏好。** 全底座（装配、验证、发布、运行）**只保留一条腿**：
编译制品 + HTML/HTTP 传参。**没有例外。**

华哥原话：

> 「把另外一条腿砍掉，只保留 html 的方式，后续只保留这个，这个是要在 AGENT.md 上面写死。
> 整个底座除了必要，否则只能用 html 方式装配或者是验证等。那就只需要维护这一套 html 的规则就行。」

### 1. 唯一允许的链路

```text
源码 → 编译器/契约编译器 → 编译制品 → HTTP(HTML 传参) 装配/验证/发布/运行
```

**一切验证、装配、发布动作都必须对「编译制品」发真实 HTTP 请求，不准对源码树直接下结论。**

| 动作 | 唯一合法形态 | 禁止形态 |
|---|---|---|
| 装配 | 从**制品**装载 | 从源码树 `sys.path` 直接装配 |
| 验证 | `python3.14 -m 开发工具.HTML验证.验证器 --制品 <制品目录> --并发 <N>` | 对源码树跑 py 脚本自证 |
| 发布 | 发布门禁只认**激活制品的真实 HTML 黑盒结果** | 拿 unittest 或源码快照当发布依据 |
| 运行（常驻网关） | **跑激活制品** | 跑源码树 |

### 2. 为什么必须写死（真实事故，非理论）

2026-09-21 实测事故链：

1. `支持库/适配层/Swift编译器提供者/依赖锁.json` 的 `环境` 三字段是空串，且**已提交进主线**（`9b61bda7`）；
2. **制品里根本没有这个提供者**（`find 工程缓存/制品仓库 -path "*Swift编译器提供者*"` 零命中）；
3. 所以 HTML 验证验的是**干净制品**，永远全绿；
4. 而 40007 常驻网关**从源码树启动**（`WorkingDirectory` = 仓库根，入口 `运行核心/启动运行核心网关.py`）；
5. ⇒ 源码树那条腿带着坏锁 → 装配被阻断 → **网关直接崩掉**。

**判据**：这条腿的问题在制品上永远复现不了，**HTML 验证「通过」时，真正跑着的那份代码从没被验过**。
两腿并行 = 验证覆盖不到运行实体 = 假绿。

### 3. 无豁免：一切动作只走 HTML 腿（华哥 2026-09-21 二次裁决）

**豁免已删除。** 原先列的三条豁免（编译阶段本身 / 静态规则检查 / 开发期本地命令）**全部作废**，
不再承认任何「按设计如此」的例外。判据只有一条：**这个动作用 HTTP 调得到能力吗？**

| 动作 | 唯一合法形态 |
|---|---|
| 构建制品 | **HTTP 调** `平台控制面.包仓库.构建制品`，不得跑 `客户端/构建平台客户端.py` |
| 入库 / 签名 / 安装 / 校验 | **HTTP 调**（`入库客户端制品` / `签名制品` / `安装平台客户端制品` / `校验平台客户端制品`） |
| 装配 / 验证 / 发布 / 运行 | 对**激活制品**发 HTTP；跑源码树一律禁止 |
| 编译口 / 全量重算摘要 / 环境自检 | **HTTP 调** `系统核心支持库.进程管理.执行命令`（传 shell 串 + 工作目录 + 超时秒） |

**为什么删（现场实测，不是理论）**：`客户端/构建平台客户端.py` 这条 py 腿**自己就是第二条腿** ——
它绕过能力入口直调 `入库`/`安装`，而 40007 常驻网关又跑源码树，
于是「构建出来的东西」和「跑着的东西」各自一套路径。保留豁免 = 保留这个分叉。

**★ 禁一切 py 腿（华哥 2026-09-21 裁决「py 调用的腿，全砍了」）**：
不得直接 `python3.14 <脚本>.py` 起第二条腿。本仓通用执行腿是
`系统核心支持库.进程管理.执行命令`——**参数先探再填，不要猜**：
`命令` 是**文本型 shell 串**（不是数组）、`工作目录` 文本型、`超时秒` **双精度数型**
（传整数即 `参数不合法`，实测踩过）。用法先搜 `capability_search`，
一次没拿到就换词继续搜或调 `能力目录.读取能力` 读契约，**不要凭字面猜参数名与类型**。

**实测（2026-09-21，经 40007 HTTP）**：环境自检退出码 0 ／ 全量重算摘要退出码 0（全部闭合）
／ 编译口退出码 1（判红项为存量，非执行失败）—— 三者**均可经 HTTP 执行**，
原先「开发期本地命令按设计如此」的说法是**探索不足**，不是真缺口。

### 4. 落地要求

- 40007 常驻网关必须跑**激活制品**（`工程缓存/制品仓库/平台客户端环境/平台客户端/` 固定路径），
  plist 三处同改：`WorkingDirectory` → 制品环境目录、`ProgramArguments[1]` → 制品内 `运行核心/启动运行核心网关.py`、`PYTHONPATH` 保持 `.`。
- **构建/入库/安装一律走 HTTP 能力**：`平台控制面.包仓库.构建制品` → `入库客户端制品` →
  `签名制品` → `安装平台客户端制品` → `校验平台客户端制品`（五步各有对外能力，经 40007 调用）。
  **不得**再跑 `python3.14 客户端/构建平台客户端.py`（py 腿，见 §3 无豁免口径）。
- **HTML 并发**：`--并发` 可拉到 **200–400**（2026-09-21 上限从 64 放开到 512，网关侧同步放开）。
  唯一正式判定口径仍是**多线程 HTML 黑盒对制品发真实 GET/POST**。
- **禁止保留第二条腿**：不得为「源码树也能跑」保留兼容路径、不得两套装配共存；
  旧腿被取代直接删（哲学第 1 条「不保留旧腿」）。
- **构建链本身也必须走 HTTP**：`客户端/构建平台客户端.py` 是 py 腿，
  唯一合法形态是 HTTP 调 `平台控制面.包仓库.构建制品` 及其后续四步（入库/签名/安装/校验）。
  该脚本在单腿铁律下的去留见唯一债务清单。

## 验收

**开发循环第一动作（华哥 2026-09-19 裁决「易语言模式」）**：改完任何文件，第一动作是开发编译口——
一条命令完成 影响面清单 + 生成物核对 + 静态规则全查 + 定向测试点名（**实测约 28 秒**，2026-09-20 现场计时；大头是参数可用性 8.1s + 重复腿与旁路 5.8s + 测试体系门禁 5.8s），绿了再继续；
unittest 只跑编译口点名的受影响模块，HTML 黑盒只在发布时跑一次：

```bash
python3.14 -m 开发工具.开发编译口.编译口 --变更     # 默认读 git 变更集；并行期用 --文件 显式点名自己改的文件
python3.14 -m 开发工具.开发编译口.编译口 --自证     # 改过检查器/判据后必跑：编译口自身三拍（绿/红/恢复）
```

- 编译口默认**只读**（不写生成物）；要它自动重算生成物（摘要/父包级联）加 `--联动`。
- 五态结论：干净通过 / 债务冻结 / 只报告 / 未核验 / 失败；退出码 0/1/2 唯一判定；
  门禁超时或崩溃一律按「未核验」处理，**解析不出来不许静默跳过（防假绿）**。
- 红项区分「本次变更引入」与「存量冻结」；存量冻结债以登记基线为准，新增红永远阻断。
- 方案全文见 `开发文档/规范/批量开发流程与分级验证.md`；第一轮审计的全部发现与逐条证真结论见 `开发文档/分析/审计发现汇总_20260919.md`。

正式验收入口（唯一标准链路）：

```bash
python3.14 -m unittest 测试中心.<受影响测试模块>  # 开发期定向回归
python3.14 -m 开发工具.HTML验证.验证器 --制品 <编译产物目录> --并发 32  # 正式真实HTTP
python3.14 开发工具/发布门禁/运行发布门禁.py   # 正式发布唯一判定
# 步骤 0：本机能不能跑 —— 以常驻网关的真实 HTTP 返回为准（不看任何 py 自检脚本的快照）
TOKEN="$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)"
curl -sS http://127.0.0.1:40007/健康 -H "Authorization: Bearer $TOKEN"
#   ⇒ {"状态":"健康","能力数":720}：能力数 > 0 ⇒ 本机可跑；连不上或能力数 0 ⇒ 不可跑
# 批量能力调用（真实验收；浏览器页面见 开发文档/规范/开发支持库.md 的「验证腿」）
python3.14 -m 开发工具.全量重算摘要 [--只报]   # 改过包后必跑
python3.14 -m 开发工具.能力搜索.能力搜索器 --能力 读取文件
# 示例运行需注入网关凭证（否则报「网关凭证缺失」；bash 不能 export 中文变量名，故用 env 前缀）
env 系统库网关凭证="$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)" python3.14 示例项目/适配层示例/运行入口/运行.py
```

门禁：实际加载全部测试；零测试、导入失败、未解释跳过、债务通过、缓存假绿
全部必须失败；发布只有“通过/阻断”两种结果，其余均非零退出码。

> 不使用 unittest discover 或不存在的聚合测试脚本；中文测试文件必须通过明确的
> `python3.14 -m unittest 测试中心.<模块路径>` 入口执行，零测试、导入失败和未解释跳过均阻断。

## 铁律

- 全中文命名：变量、函数、类、文件、目录、注释一律中文；约定保留的 id
  统一写作 id；只有语言关键字、框架标准属性、第三方、操作系统和外部协议
  强制英文时才保留英文。
- **对外错误码与说明必须中文**（决策记录 `0003`）：不得返回 `NOT_REGISTERED` / `CONSUMER_REQUIRED` /
  `CAPABILITY_REQUIRED` / `INTERNAL_ERROR` / `RESTART_LIMIT` / `IMPORT_VIOLATION` 这类英文枚举。
  新能力声明错误码时一律用中文；发现存量英文码按同类批量收口。
- **文档与汇报也用中文（华哥 2026-09-10 重申）**：开发文档、决策记录、提交说明和
  给华哥的汇报一律用中文，只保留 ID、PPT、OCR、LLM 这类人人认识的缩写；
  新目录与新文档用中文命名（决策记录、归档、参考资料），技术动作也用中文表述
  （说“移动文件”而不是“git mv”）。
- 只允许 Python 标准库；第三方依赖必须封装在 `支持库/适配层/` 对应提供者。
- 严禁导入、引用、读取任何业务项目的代码、数据库、配置、
  测试、运行数据。
- 支持库只提供原子能力；模块只组合支持库；加载器只负责发现、解析、装载
  与生命周期；核心只负责装载、调度、资源、调用和运行治理。
- 跨包调用只能经过公开契约与包级中文入口，禁止深入实现目录。
- **传参类型铁律（华哥裁决 2026-08-26）：一次传参必须用句柄（指向能力进程），
  id 是句柄内的资源定位只作二次传参；禁止不持句柄直接传 id 绕过能力进程，
  违反返回 `句柄失效`/`参数不合法`。**
- 调用者只能 `from 支持库.后端.文件系统 import 读取文件`，
  禁止 `from 支持库.后端.文件系统.实现.文件系统 import 读取文件`。
- 测试统一放在 `测试中心/`，测试代码使用标准库 unittest，不依赖 pytest。
- 工程缓存可删除、可重新生成，不作为正式源代码；测试产物和快照只放工程缓存。
- 原生扩展、动态库和不可控全局状态第三方强制独立进程，主进程不得导入。
- 所有线程、进程、队列、缓存、日志、临时文件、句柄和终态记录必须有界。
- 新能力必须先有确认需求、能力搜索、复用决策、占用租约、验收契约、
  装配计划和工作包；缺少任一项不得开工。
- 发布前必须通过依赖防火墙（平台与各接入项目）并成为构建、签名、安装和
  激活事务的必经步骤；独立脚本未被发布入口调用等同门禁不存在。
- **跨平台收口（华哥 2026-09-16 裁决：一次性收口，不补双份代码）**：平台差异只允许在
  `公共契约/运行时/平台适配.py`（纯平台判定；谁都能依赖）和 `公共契约/运行时/进程终止.py`
  （进程与进程组）两处判断。调用点一律改调收口层，**调用点不许自己写平台判断**（禁
  `sys.platform` / `os.name` / `是Windows()` / `taskkill` / `os.killpg`）；某处必须加平台判断才能
  改通时，报回来补收口层，不许就地分叉。Windows 特别警示：`os.kill(pid, 0)` 不是探活，
  而是 `TerminateProcess`（会把被查进程**真杀掉**），存活探测一律经 `进程终止.进程存活`。
- **改子包必须级联重算聚合父包摘要**：任何改动后用 `python3.14 -m 开发工具.全量重算摘要 [--只报]`
  自检，口径是**全仓「不闭合 0/N」**，不是只看自己改过的包（全仓有 10 个聚合父包）。
  漏算父包会让摘要不闭合，是提交后最常见的返工点。
- **开发循环第一动作（华哥 2026-09-19 裁决「易语言模式」）**：改完任何文件先跑
  `python3.14 -m 开发工具.开发编译口.编译口 --变更`，绿了再做别的；**不许拿跑一堆 unittest 替代编译口**
  （测试只跑它点名的受影响模块）。改检查器/门禁判据时必须跑 `--自证` 三拍，并给判据配「变异样本」
  （故意弄坏必红）；解析不出来或门禁崩溃一律按「未核验」处理，**不许静默跳过（防假绿）**。
- **提交只用精确路径**：本仓常有多会话/多子代理并行改动，`git add -A` 会把别人的半成品带进
  提交；一律 `git add <精确路径>`。
- **每处修复必须配反向验证**：修完验证器、门禁、检查器后，都要故意弄坏一次、确认它真的变红
  （不许拿「执行异常」当「检出成功」；宽 `except` 会把内部故障伪装成检出成功）。没有反向验证的
  修复，等于没证明修的是缺陷而不是假绿。
- **判据变更必须同批 grep 全仓同类断言**：改了判据（例：空白声明一律删、类型名收敛为 16 个正式名）
  就要同批清理依赖旧判据的测试与验证场景。只改生产侧会让旧断言变成长期「测试红」，而测试红无人
  处理（测试门禁未接线），最后变成一堆没人认领的假缺陷。
- **多会话并行写者降级**：多个会话或子代理同时改一个仓库时，按
  `开发文档/规范/多会话并行开发规约.md` 的写者降级规程执行（文件级独占、只 add 自己改的文件、
  被他人改动覆盖时以现场回读为准）。
- **不许用「假平台判定」跑破坏性自检**：改平台判定后真跑装配，会按错误平台删掉本机
  `工程缓存/提供者运行环境/` 下的有效环境。模拟平台的测试不得触达真实写/删路径；
  平台判定与实际解释器布局矛盾时应 fail-closed 拒绝执行（`开发工具/环境自检.py` 已按此加闸）。
- **规范不得指向已删除的东西**：新增或修改规范、任务信、文档时，必须把其中的入口、命令、
  工具在**当轮实测一遍**。指向已清场接入面（8766 管理端那套 `project_context` /
  `codegraph_explore` / `mcp_feedback` / `verify_and_record`）会让整份规范不可执行，
  进而长期无人遵循——这是 2026-09-16 文档收口的直接教训。

## 目录约定

```text
公共契约/   包声明、能力契约、基础类型、错误结构、生命周期、版本规则
平台控制面/ 需求登记、能力目录、包仓库、策略中心、发布管理、证据账本（唯一写权威）
启动监督器/ 核心启动、资源限制、健康检查、版本切换与核心回退
运行核心/   加载器、任务调度、资源协调、能力调用、统一网关、运行诊断、
            依赖防火墙、运行环境管理器、环境指纹
前端核心/   窗口、页面、组件、状态、事件、路由和渲染宿主
后端核心/   后端执行单元、请求上下文、能力执行和宿主级生命周期
支持库/     后端原子支持库、前端描述模型、适配层（第三方/系统/外部协议边界）
模块库/     只组合支持库完成功能的样板模块
项目适配层/ 项目声明、配置合并、依赖锁定、模块绑定、运行入口
开发工具/   统一能力入口、能力搜索、说明书生成、契约编译、组件规范、
            组件合规、复用审计、发布门禁、统一验证
测试中心/   独立测试环境（unittest），唯一测试代码位置
示例项目/   最小运行样板
客户端/     平台客户端制品构建脚本
开发工具/薄壳/    平台 MCP stdio 薄壳（3 工具，无端口；agent 接入面）
开发工具/开发编译口/  开发循环一键口（影响面+生成物核对+静态规则全查+测试点名；华哥 2026-09-19「易语言模式」）
开发工具/能力网关/  只读能力发现网关（40006，当前未启用；执行仍走 40007）
开发文档/   当前版本说明、架构/能力契约、决策记录与参考资料（外部源码研究）；
            规范只在 开发文档/规范/，备查方案只在 开发文档/方案/；
            机器验证账本只保留 JSON/JSONL
工程缓存/   生成物（可删除）
```

能力扩充按“运行域 / 能力领域 / 组件包”分类。物理目录不作为公开调用契约。

## 文档与归档规则

- 每个项目只保留一套权威文档（**7 份**）：`开发文档/项目说明.md`（唯一事实源，含**设计哲学第 1–14 条**）、`开发文档/未完成事项.md`（唯一债务清单）、`开发文档/当前流程图.md`（文字细则）、`开发文档/架构总览.md`（图版架构）、`开发文档/主开发文档.md`（**入口索引 + 文档地图**）、`开发文档/哲学总账.md`（强制情况登记）、`开发文档/目标定义_什么是OK.md`（目标判据）；根目录 `README.md` 为对外定位的唯一权威处。
- **全项目 md 只走一个统一生成器（华哥 2026-09-20 定）**：`开发工具.MD文档生成`（一个入口，传参选类型：
  `--类型` 或 `--序号`，`--校验` / `--写盘` / `--新建`）。**一类文档一个格式**，格式定义是数据
  （`开发文档/规范/文档类型判据.json`，取自 `--列出` 实测）；**新增一类文档 = 改该判据文件一处**，
  不加代码文件。**后面不许直接手写 md**：生成区只有生成器能写（人改即 `--校验` 判红、`--写盘` 刷掉），
  人工区逐行保留 + 零丢失自检，边界切不出即 fail-closed。权威文档/规范的「最后更新」**从 git 派生**
  （内容没变日期不动，避免假新鲜与每天假红）。**全部编辑收口到 `开发工具/`（SDK 式单一腿）**，
  出现第二套 md 生成实现即缺陷（哲学 1.2）。详见 `开发文档/规范/机器化与统一生成器.md`。
- **规范收口在固定位置**：全局规范只在 `开发文档/规范/`（多会话并行开发规约 / 批量开发流程与分级验证 / 网关调用最小示例 / 新建支持库与模块指南 / 新建技能包指南 / 通用能力反馈接口与平台自治治理 / 资源回收确认机制 / 轻代码前端与独立编译契约 / 能力目录说明书 / **Markdown 文档体例规范** / 运行槽与环境变量 / 场景覆盖率口径 / 说明书权威分工与维护规约 / 方案文档生命周期规范 / **机器化与统一生成器**）；备查方案只在 `开发文档/方案/`；**包级规范在各包 `说明/使用说明.md` + `说明/设计说明.md`，不得在别处复述包内规则**。新会话找规范先看 `开发文档/主开发文档.md` 的「文档地图」（A 必读 / B 规范 / C 备查 / D 记录 / E 代码入口）。
- **Markdown 一律按 `开发文档/规范/Markdown 文档体例规范.md` 写**（章节层级、代码块围栏、元信息头、包级三件套、引用写法、编号、归档、检查方式）。该规范单点管全仓文档体例，别处不得复述。
- 临时工作台、审计报告、方案稿在结论并入权威文档后立即删除，不与权威文档并列；历史由提交记录保留；已修复事项从债务清单直接删除，不留流水账。
- 需要记录“为什么这么定”（含被否决的做法）时写进 `开发文档/决策记录/`，编号四位递增且不复用；被推翻的决策不删除，只把状态改为“已变更”或“已废弃”并写明替代它的编号。
- **归档只保留一个顶层 `开发文档/归档/`，按 `年月_主题` 命名**；归档件**不作为派活依据**，其路径**不得出现在权威文档正文的链接里**（正文需要指涉历史时写「见 Git 提交」）。口径边界：**成品、制品、安装包、构建产物不进本地归档**（直接上传 GitHub，本机旧的直接删，见哲学 1.7）；**文档历史**（历轮取证、旧快照、已完结审计报告）进归档目录。两类对象边界见 `开发文档/规范/Markdown 文档体例规范.md` 第八节。
- 权威文档里的数字必须标清“实测”还是“历史快照”并给出取证命令，不能把旧快照写成持续事实；文档不反向修改代码事实，代码和真实请求验证优先于旧文档描述。**标法固定**：按 `Markdown 文档体例规范.md` 第三节的三行元信息头（最后更新 / 口径 / 维护者），不散写在正文。
- 开工阅读顺序：`AGENTS.md` → `开发文档/主开发文档.md`「文档地图」B 栏（规范）→ `开发文档/项目说明.md` → `开发文档/未完成事项.md` → `开发文档/当前流程图.md` → 目标包 `说明/` → 需要时读 `开发文档/决策记录/`；`开发文档/参考资料/外部源码研究/` 只作选型参考，不作为实现依据。

## 每个支持库必备

- `包声明.json`：包id、类型、版本、依赖、能力声明
- `能力契约/`：能力参数与返回声明
- `实现/`：原子能力实现（不对外暴露）
- `说明/`：使用说明
- `验证场景引用.json`：验证场景引用
- `完整性摘要.json`：文件清单与哈希
- `__init__.py`：包级中文入口

> 系统适配器豁免：`支持库/适配层/` 下不对外提供能力契约的内部适配器（提供者配置读取、
> 配置契约、提供者注册表、提供者配置、配置安全、HTTP服务适配器、数据库适配器、
> 本地进程适配器、动态库适配器、密钥提供者、系统探针.py 等）不要求 `包声明.json`/`完整性摘要.json`/
> `能力契约/`/`验证场景引用.json`；`扫描正式包` 以 `包声明.json` 为标记，不收录此类
> 适配器。`*提供者` 包与对外发布支持库不受此豁免。

## 验证

```bash
python3.14 -m py_compile $(find . -type f -name '*.py' -not -path './工程缓存/*')
python3.14 -m unittest 测试中心.<受影响测试模块>
```

禁止：零测试仍然成功、导入失败当跳过、未解释跳过、资源未清理、
依赖循环被忽略、多提供者冲突被忽略、只验证导入不验证真实返回值。
