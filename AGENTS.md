# 系统工程平台 · 项目硬规则

> 最后更新：2026-09-22
> 口径：实测（数字随建包与提交逐日变化，引用前必须重跑取证命令）
> 维护者：项目硬规则（根规则）
<!-- 机器管理｜类型：权威文档｜生成器：开发工具.MD文档生成 -->



## 细则去哪查（本文已按「核心 + 细则」分册）

**本文只放每次开工都要遵守的核心规则。**以下细则已**原文**搬到 `开发文档/规范/项目细则.md`
（已入项目文档索引），需要时查它，不要靠猜：

| 要做什么 | 查询关键词 |
|---|---|
| 派子代理 / 写任务信 / 继承协议 | `子代理 任务信 继承协议` |
| 连 MCP / 代码地图 / 薄壳 / 能力入口 | `MCP 代码地图 薄壳` |
| 写文档 / 归档 / 文档分类 | `文档 归档 规则` |
| 文件放哪 / 目录约定 | `目录约定` |
| 分层归属 / 谁依赖谁 | `唯一分层方案` |
| 新建支持库 / 模块必备件 | `每个支持库必备` |

查询方式：`项目文档支持库.查询索引(项目名="系统工程平台", 关键词="<上表关键词>")`
（返回 文件路径/起始行/结束行/片段，**不必整份读文档**）。

**为什么分册**：本文原 23,311 字符，而 Agent 侧注入有截断（实测 Codex 只注入 16.3K，
后 2/3 规则——含《铁律》《文档与归档规则》——**看不见**）。分册后核心全部落在注入窗口内，
细则按需查，**内容一字未丢**。

## 强制开工协议（2026-09-19 华哥裁决，四条不可跳过）

> 本协议**只在本仓库目录内载入**；不在本仓库动手时不要调用本仓库 MCP，也不要据此载入任何平台上下文。

1. **不连 MCP 不开工**：本仓库的一切改动，必须先经 MCP `system_engineering_toolkit`（或等价地直调唯一网关 40007）走一遍开工，**没有开工 ID 的改动不合规**。
2. **第一步取开工 ID**：调 `capability_call` 能力 id = `开工编排.开工准备`，传参 `项目根` = 本仓库绝对路径（`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`）、`任务` = 本次要做的事、`修改路径` = 要改的文件（可空）。**返回里的 `开工ID` 就是本次开工的唯一标识**（不传则当场生成），后续写任务记忆包、提交 GitHub 都用它。
   **要动文件就必须走互斥门**：把 `修改路径` 填全（可写 `{"路径": …, "指纹": …}`，指纹用 `sha256:<hex>` 或裸 hex 由入口自动归一），改用能力 id = `开工编排.开工即占`（同样返回 `开工ID`）——它**原子认领全部路径，任一条被别人占着就整体不开工并回报占用者**；收工调 `开工编排.收工释放` 释放。**不填 `修改路径` 就没有互斥保护**（`开工准备` 只查不占，这是分工不是缺陷）。
3. **要能力自己搜，不要猜、不要重复造**：用 `capability_search` 搜关键词，拿到能力 id 后用 `capability_call` 调用。找不到能力再考虑补底座，**不许在仓库里另起第二套实现**。
4. **收工按开工 ID 提交**：结束前把改动按 `开工ID` 提交（提交信息带上它），并做收尾（释放租约、登记未完成事项）。

**薄壳只服务本仓库**：`项目根` 必须是**真实存在且含 `AGENTS.md` + `开发文档/项目说明.md` 的目录**，否则调用一律被拒（`非本平台项目根`）且**不返回任何平台规则**——这是刻意的最小 token 设计：非本项目的会话不会被灌入平台上下文。

```text
开工（复制即用）：
  tool = capability_call
  项目根 = /Users/hekunhua/Documents/Agent/PHP/系统工程平台
  能力id = 开工编排.开工准备
  参数   = {"任务": "<本次要做什么>", "修改路径": ["<要改的文件，可空>"]}
  → 返回值里的 开工ID 就是本次开工标识
收工：
  git commit -m "<开工ID> <做了什么>"  -- <精确路径>
```

## 定位

本仓库是与任何业务项目完全平级、完全独立的系统级工程底座（唯一事实源：
`开发文档/项目说明.md`，本仓库内部权威冻结文档；本文件
只允许与其一致，不一致时以冻结文档为准并在同一任务内修复本文件）。

分层：公共契约 -> 支持库（原子能力/第三方边界）-> 模块库（组合流程）
-> 项目适配层（绑定版本/配置/权限/路径/密钥引用与项目别名）-> 正式代码
-> 运行核心（装载/调度/资源/调用/治理）-> 平台控制面（目录/策略/制品/发布/证据）。

## 单腿铁律：只走 HTML / 制品一条腿（华哥 2026-09-21 裁决）

**这条是硬门禁，不是偏好。** 全底座（装配、验证、发布、运行）**只保留一条腿**：
编译制品 + HTML/HTTP 传参。**没有例外。**

华哥原话：

> 「把另外一条腿砍掉，只保留 html 的方式，后续只保留这个，这个是要在 AGENT.md 上面写死。
> 整个底座除了必要，否则只能用 html 方式装配或者是验证等。那就只需要维护这一套 html 的规则就行。」

### 1. 唯一允许的链路

```text
源码 → 编译器/契约编译器 → 编译制品 → HTTP(HTML 传参) 装配/验证/发布/运行
```

**一切验证、装配、发布动作都必须对「编译制品」发真实 HTTP 请求，不准对源码树直接下结论。**

| 动作 | 唯一合法形态 | 禁止形态 |
|---|---|---|
| 装配 | 从**制品**装载 | 从源码树 `sys.path` 直接装配 |
| 验证 | `python3.14 -m 开发工具.HTML验证.验证器 --制品 <制品目录> --并发 <N>` | 对源码树跑 py 脚本自证 |
| 发布 | 发布门禁只认**激活制品的真实 HTML 黑盒结果** | 拿 unittest 或源码快照当发布依据 |
| 运行（常驻网关） | **跑激活制品** | 跑源码树 |

### 2. 为什么必须写死（真实事故，非理论）

2026-09-21 实测事故链：

1. `支持库/适配层/Swift编译器提供者/依赖锁.json` 的 `环境` 三字段是空串，且**已提交进主线**（`9b61bda7`）；
2. **制品里根本没有这个提供者**（`find 工程缓存/制品仓库 -path "*Swift编译器提供者*"` 零命中）；
3. 所以 HTML 验证验的是**干净制品**，永远全绿；
4. 而 40007 常驻网关**从源码树启动**（`WorkingDirectory` = 仓库根，入口 `运行核心/启动运行核心网关.py`）；
5. ⇒ 源码树那条腿带着坏锁 → 装配被阻断 → **网关直接崩掉**。

**判据**：这条腿的问题在制品上永远复现不了，**HTML 验证「通过」时，真正跑着的那份代码从没被验过**。
两腿并行 = 验证覆盖不到运行实体 = 假绿。

### 3. 无豁免：一切动作只走 HTML 腿（华哥 2026-09-21 二次裁决）

**豁免已删除。** 原先列的三条豁免（编译阶段本身 / 静态规则检查 / 开发期本地命令）**全部作废**，
不再承认任何「按设计如此」的例外。判据只有一条：**这个动作用 HTTP 调得到能力吗？**

| 动作 | 唯一合法形态 |
|---|---|
| 构建平台客户端制品 | **HTTP 调** `自修复工具.构建平台客户端制品`（2026-09-21 #213 收口），不得跑 `客户端/构建平台客户端.py` |
| 构建通用包制品 | **HTTP 调** `平台控制面.包仓库.构建制品`（**注意**：它要求调用方传入全量 `文件表`，产出内容寻址制品记录，**不读源码树、不产出 `平台客户端-<摘要16>`**） |
| 入库 / 签名 / 安装 / 校验 | **HTTP 调**（`入库客户端制品` / `签名制品` / `安装平台客户端制品` / `校验平台客户端制品`） |
| 装配 / 验证 / 发布 / 运行 | 对**激活制品**发 HTTP；跑源码树一律禁止 |
| 编译口 / 全量重算摘要 / 环境自检 | **HTTP 调** `系统核心支持库.进程管理.执行命令`（传 shell 串 + 工作目录 + 超时秒） |

**为什么删（现场实测，不是理论）**：`客户端/构建平台客户端.py` 这条 py 腿**自己就是第二条腿** ——
它绕过能力入口直调 `入库`/`安装`，而 40007 常驻网关又跑源码树，
于是「构建出来的东西」和「跑着的东西」各自一套路径。保留豁免 = 保留这个分叉。

**★ 禁一切 py 腿（华哥 2026-09-21 裁决「py 调用的腿，全砍了」）**：
不得直接 `python3.14 <脚本>.py` 起第二条腿。本仓通用执行腿是
`系统核心支持库.进程管理.执行命令`——**参数先探再填，不要猜**：
`命令` 是**文本型 shell 串**（不是数组）、`工作目录` 文本型、`超时秒` **双精度数型**
（传整数即 `参数不合法`，实测踩过）。用法先搜 `capability_search`，
一次没拿到就换词继续搜或调 `能力目录.读取能力` 读契约，**不要凭字面猜参数名与类型**。

**实测（2026-09-21，经 40007 HTTP）**：环境自检退出码 0 ／ 全量重算摘要退出码 0（全部闭合）
／ 编译口退出码 1（判红项为存量，非执行失败）—— 三者**均可经 HTTP 执行**，
原先「开发期本地命令按设计如此」的说法是**探索不足**，不是真缺口。

### 4. 落地要求

- 40007 常驻网关必须跑**激活制品**（`工程缓存/制品仓库/平台客户端环境/平台客户端/` 固定路径），
  plist 三处同改：`WorkingDirectory` → 制品环境目录、`ProgramArguments[1]` → 制品内**双层**路径
  `平台客户端/运行核心/启动运行核心网关.py`、`PYTHONPATH` 保持 `.`。
  **★ 改完 plist 文件 ≠ 切换生效（2026-09-21 实测，假绿级陷阱）**：`launchctl kickstart` 用的是
  launchd **已加载的作业定义**、**不重读 plist** —— 实测改完 plist 再重启，报「就绪/健康/能力数/
  装配告警」全绿，而 `ps` 的 argv 与 `lsof` 的 cwd **仍是源码树**（差一步就误报切换成功）。
  必须 `launchctl bootout gui/<uid>/com.huashi.gateway-40007` + `launchctl bootstrap gui/<uid>
  ~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 重载；**验收判据必须是
  `ps -o command=`（argv 见制品内双层路径）与 `lsof -a -p <pid> -d cwd -Fn`（cwd = 制品环境目录）**，
  只看健康检查不算验过。另：`plutil -replace ProgramArguments.1` 是**插入**不是替换（数组会变三项、
  旧值被挤到 index 2），须再 `plutil -remove ProgramArguments.2` 清理。
  **切换后构建能力须显式给 `源码根`**：`自修复工具.构建平台客户端制品` 的 `系统根` 由实现模块位置
  推出，网关跑制品时那就是制品根，而 `客户端/` 不在制品的 `顶层包表` 里 ⇒ 不传 `源码根` 会找不到
  构建脚本本体（该参数即为此而加，见债务 #215）。
- **plist 第四处必改：`系统底座_工程缓存根` 必须显式指到源码树的 `工程缓存`**（2026-09-21 实测，
  静默分叉级陷阱）：`公共契约/运行时/运行缓存.解析运行缓存根` 的判据是「环境显式覆盖 → 制品稳定根 →
  源码工程缓存」，而制品根名是 `平台客户端` ⇒ **不设该变量时整个平台的运行态（`制品仓库`／`运行数据`）
  被解析到 `~/Library/Caches/系统工程平台/运行缓存/`**，而构建/安装链（构建脚本以 `工作目录=源码根` 起）
  写的是**源码树的 `工程缓存`** ⇒ **同一条链落在两个仓**。实测症状（切换后当场撞到）：
  `平台控制面.包仓库.校验平台客户端稳定路径` 回 `有效=false 缺少激活指针 当前.json（从未安装或指针丢失）`、
  `诊断平台客户端激活指针` 的回收报「根不存在跳过：`~/Library/Caches/…/平台客户端制品`」—— 而网关的
  「健康／能力数 746／装配告警空」**全绿**，即**健康检查完全不覆盖这个分叉**（见债务 #219）。
  修法：`plutil -insert EnvironmentVariables.系统底座_工程缓存根 -string <源码树>/工程缓存`，
  再 `bootout` + `bootstrap`（与上面同一套重载要求）。**验收判据（已机器化，别再用肉眼）**：
  `python3.14 开发工具/能力网关/重启网关.py --运行态同根` —— **退出码即结论**（0=同根），
  一次问三件事：网关侧 `制品根目录` 是否 = 源码树 `工程缓存/制品仓库`、`校验平台客户端稳定路径`
  是否 `有效=true` 且激活路径同树、**以及 plist 这一处是否设对**。第三问防的正是「现在对、
  下次重启又分叉」：`launchctl kickstart` 用已加载的作业定义、**不重读 plist**。
  `--探活` 与发布门禁项「网关运行态与构建链同根」走的是**同一条判据**（实现只有一处：
  `开发工具/能力网关/重启网关.py`）。
- **构建/入库/安装一律走 HTTP 能力**：`自修复工具.构建平台客户端制品` → `入库客户端制品` →
  `签名制品` → `安装平台客户端制品` → `校验平台客户端制品`（五步各有对外能力，经 40007 调用）。
  **不得**再跑 `python3.14 客户端/构建平台客户端.py`（py 腿，见 §3 无豁免口径）。
- **HTML 并发**：`--并发` 可拉到 **200–400**（2026-09-21 上限从 64 放开到 512，网关侧同步放开）。
  唯一正式判定口径仍是**多线程 HTML 黑盒对制品发真实 GET/POST**。
- **禁止保留第二条腿**：不得为「源码树也能跑」保留兼容路径、不得两套装配共存；
  旧腿被取代直接删（哲学第 1 条「不保留旧腿」）。
- **构建链本身也必须走 HTTP**：`客户端/构建平台客户端.py` 是 py 腿，
  唯一合法形态是 HTTP 调 `自修复工具.构建平台客户端制品` 及其后续四步（入库/签名/安装/校验）。
  **该脚本保留为判据本体、由能力封装**（与 `开发工具/开发编译口/编译口.py` 同范式：脚本持判据，
  能力只做结论归并与结构化，不新建第二套判据）；**直接敲脚本 = 第二条腿**。

## 验收

**开发循环第一动作（华哥 2026-09-19 裁决「易语言模式」）**：改完任何文件，第一动作是开发编译口——
一条命令完成 影响面清单 + **生成物自动联动** + 静态规则全查 + 定向测试点名 + **复跑确认**
（**实测约 15 秒**，2026-09-22 现场计时；18 项静态门禁 8 路并行），绿了再继续；
unittest 只跑编译口点名的受影响模块，HTML 黑盒只在发布时跑一次：

```bash
python3.14 -m 开发工具.开发编译口.编译口            # ★ 默认档：不给任何范围参数 = 读 git 变更集 + 全自动写盘
python3.14 -m 开发工具.开发编译口.编译口 --库 <包id>  # 传库的 ID 即可（按该包+反向依赖闭包自动补全；不依赖 git 变更集）
python3.14 -m 开发工具.开发编译口.编译口 --自证     # 改过检查器/判据后必跑：编译口自身三拍（绿/红/恢复）
```

- **默认全自动（华哥 2026-09-22 裁决）**：编译口默认**写盘**，按数据侧登记表
  `开发文档/规范/生成物登记表.json` 自动生成**全部派生凭据**（完整性摘要与父包级联 /
  依赖分两段 / 包级说明使用说明与目录树 / 债务清单账目 / 权威文档与规范元信息头 /
  人工类说明书载位），并由步骤4 拿步骤3 的**真实门禁退出码**做复跑确认。
  **人只需改代码**，不需要记得加开关（「要人记得」的开关本身就是漂移源）。
  并行多写者同改一个仓库时用 `--只读` 降级（**只核验不写盘**，但仍按登记表逐条核验现场）。
- **传参越少越好（华哥 2026-09-22 裁决）**：「使用命令，传参是越少越好……路径类的，本质上内部开发
  的时候，默认地址就是根目录，地址就可以省下了。传参要编译什么，就使用 库的 ID」。故：
  **不给范围参数 = 读 git 变更集**（最常用的一档不该要人记得加开关；`--变更` 保留为等价同义）；
  工作目录固定 = 仓库根，`--文件` 的路径一律**相对根目录**写，不写绝对路径。
- **按包的派生凭据拆 N 节点并行（同一条裁决）**：「这样子一下子也能索引到对应的根目录，然后快速的
  多线程去验证是否有 bug。拆成 N 多个节点，然后复用我们的多线程库。快速的去解决掉。」故
  `按影响面包` 的凭据（契约版本自增 / 能力定义编译）**一个包一个节点、8 路并行**，并行由平台
  `系统核心支持库.进程管理.执行命令集` 提供（**不自己起第二套线程池**），逐节点结果进
  `生成节点`/`核验节点`，判红时**能点出是哪个包**红的。
- **契约版本号默认自增 1（华哥 2026-09-22 裁决）**：「版本号到这个，一般是默认值，默认值是自增 1」。
  契约面变化而版本没跟上时，`契约版本自增` 这条凭据自动递增（默认**补丁位 +1**；判据给出
  `必须升主版本` 时升主版本），**不再手改版本号**；判据说不要求递增（纯内部优化 / 仅文档性字段
  变化，如 `参数.说明`、`返回.值结构`）就**不动**。
- 派生凭据的**清单与命令是数据**（`开发文档/规范/生成物登记表.json`）：新增一类派生凭据
  = 改该文件一处，不加代码文件（与 `文档类型判据.json` 同形制）。`类别=登记`（基线冻结）
  与 `类别=人工` 的条目执行器**不跑、只报** —— 自动跑登记类等于门禁自废（会把本次新增
  写进基线、抹平「只增不改」的方向）。
- 五态结论：干净通过 / 债务冻结 / 只报告 / 未核验 / 失败；退出码 0/1/2 唯一判定；
  门禁超时或崩溃一律按「未核验」处理，**解析不出来不许静默跳过（防假绿）**。
- 红项区分「本次变更引入」与「存量冻结」；存量冻结债以登记基线为准，新增红永远阻断。
- 方案全文见 `开发文档/规范/批量开发流程与分级验证.md`；第一轮审计的全部发现与逐条证真结论见 `开发文档/分析/审计发现汇总_20260919.md`。

正式验收入口（唯一标准链路）：

```bash
python3.14 -m unittest 测试中心.<受影响测试模块>  # 开发期定向回归
python3.14 -m 开发工具.HTML验证.验证器 --制品 <编译产物目录> --并发 32  # 正式真实HTTP
python3.14 开发工具/发布门禁/运行发布门禁.py   # 正式发布唯一判定
# 步骤 0：本机能不能跑 —— 以常驻网关的真实 HTTP 返回为准（不看任何 py 自检脚本的快照）
TOKEN="$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)"
curl -sS http://127.0.0.1:40007/健康 -H "Authorization: Bearer $TOKEN"
#   ⇒ {"状态":"健康","能力数":720}：能力数 > 0 ⇒ 本机可跑；连不上或能力数 0 ⇒ 不可跑
# 批量能力调用（真实验收；浏览器页面见 开发文档/规范/开发支持库.md 的「验证腿」）
python3.14 -m 开发工具.全量重算摘要 [--只报]   # 改过包后必跑
python3.14 -m 开发工具.能力搜索.能力搜索器 --能力 读取文件
# 示例运行需注入网关凭证（否则报「网关凭证缺失」；bash 不能 export 中文变量名，故用 env 前缀）
env 系统库网关凭证="$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)" python3.14 示例项目/适配层示例/运行入口/运行.py
```

门禁：实际加载全部测试；零测试、导入失败、未解释跳过、债务通过、缓存假绿
全部必须失败；发布只有“通过/阻断”两种结果，其余均非零退出码。

> 不使用 unittest discover 或不存在的聚合测试脚本；中文测试文件必须通过明确的
> `python3.14 -m unittest 测试中心.<模块路径>` 入口执行，零测试、导入失败和未解释跳过均阻断。

## 铁律

- 全中文命名：变量、函数、类、文件、目录、注释一律中文；约定保留的 id
  统一写作 id；只有语言关键字、框架标准属性、第三方、操作系统和外部协议
  强制英文时才保留英文。
- **对外错误码与说明必须中文**（决策记录 `0003`）：不得返回 `NOT_REGISTERED` / `CONSUMER_REQUIRED` /
  `CAPABILITY_REQUIRED` / `INTERNAL_ERROR` / `RESTART_LIMIT` / `IMPORT_VIOLATION` 这类英文枚举。
  新能力声明错误码时一律用中文；发现存量英文码按同类批量收口。
- **文档与汇报也用中文（华哥 2026-09-10 重申）**：开发文档、决策记录、提交说明和
  给华哥的汇报一律用中文，只保留 ID、PPT、OCR、LLM 这类人人认识的缩写；
  新目录与新文档用中文命名（决策记录、归档、参考资料），技术动作也用中文表述
  （说“移动文件”而不是“git mv”）。
- 只允许 Python 标准库；第三方依赖必须封装在 `支持库/适配层/` 对应提供者。
- 严禁导入、引用、读取任何业务项目的代码、数据库、配置、
  测试、运行数据。
- 支持库只提供原子能力；模块只组合支持库；加载器只负责发现、解析、装载
  与生命周期；核心只负责装载、调度、资源、调用和运行治理。
- 跨包调用只能经过公开契约与包级中文入口，禁止深入实现目录。
- **传参类型铁律（华哥裁决 2026-08-26）：一次传参必须用句柄（指向能力进程），
  id 是句柄内的资源定位只作二次传参；禁止不持句柄直接传 id 绕过能力进程，
  违反返回 `句柄失效`/`参数不合法`。**
- 调用者只能 `from 支持库.后端.文件系统 import 读取文件`，
  禁止 `from 支持库.后端.文件系统.实现.文件系统 import 读取文件`。
- 测试统一放在 `测试中心/`，测试代码使用标准库 unittest，不依赖 pytest。
- 工程缓存可删除、可重新生成，不作为正式源代码；测试产物和快照只放工程缓存。
- 原生扩展、动态库和不可控全局状态第三方强制独立进程，主进程不得导入。
- 所有线程、进程、队列、缓存、日志、临时文件、句柄和终态记录必须有界。
- 新能力必须先有确认需求、能力搜索、复用决策、占用租约、验收契约、
  装配计划和工作包；缺少任一项不得开工。
- 发布前必须通过依赖防火墙（平台与各接入项目）并成为构建、签名、安装和
  激活事务的必经步骤；独立脚本未被发布入口调用等同门禁不存在。
- **跨平台收口（华哥 2026-09-16 裁决：一次性收口，不补双份代码）**：平台差异只允许在
  `公共契约/运行时/平台适配.py`（纯平台判定；谁都能依赖）和 `公共契约/运行时/进程终止.py`
  （进程与进程组）两处判断。调用点一律改调收口层，**调用点不许自己写平台判断**（禁
  `sys.platform` / `os.name` / `是Windows()` / `taskkill` / `os.killpg`）；某处必须加平台判断才能
  改通时，报回来补收口层，不许就地分叉。Windows 特别警示：`os.kill(pid, 0)` 不是探活，
  而是 `TerminateProcess`（会把被查进程**真杀掉**），存活探测一律经 `进程终止.进程存活`。
- **改子包必须级联重算聚合父包摘要**：任何改动后用 `python3.14 -m 开发工具.全量重算摘要 [--只报]`
  自检，口径是**全仓「不闭合 0/N」**，不是只看自己改过的包（全仓有 10 个聚合父包）。
  漏算父包会让摘要不闭合，是提交后最常见的返工点。**这条现在由编译口默认自动做**
  （生成物登记表的「摘要闭合」条，写盘口径含级联），见下条。
- **开发循环第一动作（华哥 2026-09-19 裁决「易语言模式」）**：改完任何文件先跑
  `python3.14 -m 开发工具.开发编译口.编译口`（**默认档**；收窄用 `--库 <包id>`），绿了再做别的；
  **不许拿跑一堆 unittest 替代编译口**（测试只跑它点名的受影响模块）。改检查器/门禁判据时必须跑
  `--自证` 三拍，并给判据配「变异样本」（故意弄坏必红）；解析不出来或门禁崩溃一律按「未核验」处理，
  **不许静默跳过（防假绿）**。
- **平台固定需求的派生凭据一律全自动（华哥 2026-09-22 裁决）**：「平台底层固定需求的东西，
  编译的同时全部自动生成凭据，而不是反复的去修」「尽量不需要人工维护吧，毕竟人工维护最容易
  出现的问题就是会漂移」⇒ 编译口**默认写盘**，按数据侧登记表
  `开发文档/规范/生成物登记表.json` 生成全部派生凭据，并用真实门禁退出码复跑确认。
  **人只需改代码**；新增一类派生凭据 = 改登记表一处（不加代码文件）。
  条目分成三类且执行器只跑第一类：`派生`（可自动重算，自动生成）/ `登记`（基线冻结，
  自动执行等于门禁自废 ⇒ 只报不跑）/ `人工`（正文是人的判断 ⇒ 只报不跑）。
- **提交只用精确路径**：本仓常有多会话/多子代理并行改动，`git add -A` 会把别人的半成品带进
  提交；一律 `git add <精确路径>`。
- **每处修复必须配反向验证**：修完验证器、门禁、检查器后，都要故意弄坏一次、确认它真的变红
  （不许拿「执行异常」当「检出成功」；宽 `except` 会把内部故障伪装成检出成功）。没有反向验证的
  修复，等于没证明修的是缺陷而不是假绿。
- **判据变更必须同批 grep 全仓同类断言**：改了判据（例：空白声明一律删、类型名收敛为 16 个正式名）
  就要同批清理依赖旧判据的测试与验证场景。只改生产侧会让旧断言变成长期「测试红」，而测试红无人
  处理（测试门禁未接线），最后变成一堆没人认领的假缺陷。
- **多会话并行写者降级**：多个会话或子代理同时改一个仓库时，按
  `开发文档/规范/多会话并行开发规约.md` 的写者降级规程执行（文件级独占、只 add 自己改的文件、
  被他人改动覆盖时以现场回读为准）。
- **不许用「假平台判定」跑破坏性自检**：改平台判定后真跑装配，会按错误平台删掉本机
  `工程缓存/提供者运行环境/` 下的有效环境。模拟平台的测试不得触达真实写/删路径；
  平台判定与实际解释器布局矛盾时应 fail-closed 拒绝执行（`开发工具/环境自检.py` 已按此加闸）。
- **规范不得指向已删除的东西**：新增或修改规范、任务信、文档时，必须把其中的入口、命令、
  工具在**当轮实测一遍**。指向已清场接入面（8766 管理端那套 `project_context` /
  `codegraph_explore` / `mcp_feedback` / `verify_and_record`）会让整份规范不可执行，
  进而长期无人遵循——这是 2026-09-16 文档收口的直接教训。

## 验证

```bash
python3.14 -m py_compile $(find . -type f -name '*.py' -not -path './工程缓存/*')
python3.14 -m unittest 测试中心.<受影响测试模块>
```

禁止：零测试仍然成功、导入失败当跳过、未解释跳过、资源未清理、
依赖循环被忽略、多提供者冲突被忽略、只验证导入不验证真实返回值。
