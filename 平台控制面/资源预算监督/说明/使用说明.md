# 资源预算监督 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 包id：平台控制面.资源预算监督
- 版本：1.0.0

> 本文是本包的**调用权威**：能力、参数、返回结构、错误码与调用示例。
> 设计与边界（为什么这样切、管什么、不管什么、失败如何表达）以同目录 `设计说明.md` 为准。

## 包说明

平台控制面资源预算监督面：把 资源监督器 的预算声明判定语义切成可经网关调用的只读/判定原子能力（必需项清单、预算完整性、阈值有效性）。必需项与预算判定唯一实现在 平台控制面/资源监督.py，本包只做边界校验与结果归一；写类动作（注册执行单元/提交任务/排空/优雅停止）与运行中采样依赖进程内实例状态，不在本轮能力面。

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 平台控制面.资源预算监督.查询必需预算项 | 查询必需预算项 | 1.0.0 | 结果型（必需预算项/数量/说明） | 查询资源监督器要求的全部必需预算项（只读）。 |
| 平台控制面.资源预算监督.校验预算声明 | 校验预算声明 | 1.0.0 | 结果型（有效/说明/缺失项） | 校验资源预算声明是否含资源监督器要求的全部必需项（判定，返回缺失项清单）。 |
| 平台控制面.资源预算监督.校验预算阈值 | 校验预算阈值 | 1.0.0 | 结果型（有效/说明/非法项） | 校验预算项取值本身是否有效（线程/并发/队列/句柄/重启次数为正整数、超时为正数、内存与临时空间可解析量纲），逐项给非法原因。 |

> 调用方只经**唯一能力调用入口**按能力 id 调用，不得 import 实现，也不得深入 `实现/` 目录。

## 平台控制面.资源预算监督.查询必需预算项（查询必需预算项）

**说明**：查询资源监督器要求的全部必需预算项（只读）。

**参数表**：

本能力**无参数**，不接受任何入参（传任何参数都判 `参数不合法`）。

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 必需预算项 | 列表型，必需预算项名列表 |
| 数量 | 整数型，必需项个数 |
| 说明 | 文本型，必需项口径来源 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
{"必需预算项": ["内存上限", "线程上限", "子进程上限", "并发调用上限", "队列长度", "文件句柄上限",
                 "临时空间上限", "单次调用超时", "每分钟重启次数", "空闲回收时间"],
 "数量": 10, "说明": "必需项口径唯一来源：平台控制面/资源监督.py 的 资源监督器.必需预算键"}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.资源预算监督.查询必需预算项", {})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.资源预算监督.校验预算声明（校验预算声明）

**说明**：校验资源预算声明是否含资源监督器要求的全部必需项（判定，返回缺失项清单）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 资源预算 | 字典型 | 是 | None | 资源预算声明字典 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 有效 | 逻辑型，true=必需项完整 |
| 说明 | 文本型，校验结论原文 |
| 缺失项 | 列表型，缺失的必需预算项名 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
{"有效": false, "说明": "资源预算缺少必需项: ['内存上限', '线程上限', ... ]",
 "缺失项": ["内存上限", "线程上限", "子进程上限", "并发调用上限", "队列长度", "文件句柄上限",
          "临时空间上限", "单次调用超时", "每分钟重启次数", "空闲回收时间"]}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.资源预算监督.校验预算声明", {"资源预算": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.资源预算监督.校验预算阈值（校验预算阈值）

**说明**：校验预算项取值本身是否有效（线程/并发/队列/句柄/重启次数为正整数、超时为正数、内存与临时空间可解析量纲），逐项给非法原因。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 资源预算 | 字典型 | 是 | None | 资源预算声明字典 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 有效 | 逻辑型，true=全部阈值合法 |
| 说明 | 文本型，校验结论原文 |
| 非法项 | 列表型，每项含 项 与 原因 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
{"有效": true, "说明": "预算阈值全部合法", "非法项": []}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.资源预算监督.校验预算阈值", {"资源预算": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---
