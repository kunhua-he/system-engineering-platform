# 策略闸门 使用说明
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 包id：平台控制面.策略闸门
- 版本：1.0.0

> 本文是本包的**调用权威**：能力、参数、返回结构、错误码与调用示例。
> 设计与边界（为什么这样切、管什么、不管什么、失败如何表达）以同目录 `设计说明.md` 为准。

> **拒绝码口径（决策 0003「对外错误只走中文」，B-16 收口）**：判定器拒绝时返回的 `错误码`
> 一律中文短词，逐字取自 `平台控制面/策略中心.py` 的真实返回：
> `待裁决` / `复用被拒` / `依赖缺失` / `未签名` / `签名失效` / `发布者不受信` /
> `需求未确认` / `版本已存在` / `预算不完整`；允许时为空串。
> 早期英文码已全部作废，本包任何文件都不再保留英文码；`能力定义.json`（唯一事实源）与
> 全部编译派生物（`能力数据/*.json`、`能力契约/参数契约.json`、`包声明.json`、
> `完整性摘要.json`）口径已由官方编译器对齐一致。

## 包说明

平台控制面策略闸门：把 策略中心 的只读判定语义（复用/依赖/签名/发布/资源五类）切成可经网关调用的原子能力，供 创建组件/提交候选包/签名与发布 三条主链做进程外策略自查。判定语义唯一实现在 平台控制面/策略中心.py，本包只做边界校验与结果归一，不复制判定逻辑；判定不向证据账本写证据（证据写入属写类动作，不在本轮只读/判定能力面）。

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 平台控制面.策略闸门.查询策略类型 | 查询策略类型 | 1.0.0 | 结果型（策略类型表/数量/判定键表/证据写入） | 查询策略中心支持的策略类型与各类型判定键（只读，不落证据）。 |
| 平台控制面.策略闸门.判定复用策略 | 判定复用策略 | 1.0.0 | 结果型（策略类型/允许/理由/错误码） | 复用策略判定（只读）：按能力id判定是否允许新增/复用/并存，拒绝时给稳定错误码。 |
| 平台控制面.策略闸门.判定依赖策略 | 判定依赖策略 | 1.0.0 | 结果型（策略类型/允许/理由/错误码） | 依赖策略判定（只读）：声明依赖能力是否全部已登记，缺失即拒绝并列缺失项。 |
| 平台控制面.策略闸门.判定签名策略 | 判定签名策略 | 1.0.0 | 结果型（策略类型/允许/理由/错误码） | 签名策略判定（只读）：未签名/签名失效/发布者不受信一律拒绝，并给稳定错误码。 |
| 平台控制面.策略闸门.判定发布策略 | 判定发布策略 | 1.0.0 | 结果型（策略类型/允许/理由/错误码） | 发布策略判定（只读）：未确认需求、候选版本已发布即拒绝，并给稳定错误码。 |
| 平台控制面.策略闸门.判定资源策略 | 判定资源策略 | 1.0.0 | 结果型（策略类型/允许/理由/错误码） | 资源策略判定（纯计算）：资源预算缺 策略中心 要求的必需项即拒绝。 |

> 调用方只经**唯一能力调用入口**按能力 id 调用，不得 import 实现，也不得深入 `实现/` 目录。

## 平台控制面.策略闸门.查询策略类型（查询策略类型）

**说明**：查询策略中心支持的策略类型与各类型判定键（只读，不落证据）。

**参数表**：

本能力**无参数**，不接受任何入参（传任何参数都判 `参数不合法`）。

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 策略类型表 | 列表型，支持的策略类型名（复用/依赖/签名/发布/资源） |
| 数量 | 整数型，策略类型个数 |
| 判定键表 | 字典型，策略类型 → 该类型需要的判定键列表 |
| 证据写入 | 文本型，明确说明判定不落证据账本 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
{"策略类型表": ["复用", "依赖", "签名", "发布", "资源"], "数量": 5,
 "判定键表": {"复用": ["能力id"], "依赖": ["包id", "依赖"], "签名": ["包id", "已签名", "签名失效", "签名者"],
            "发布": ["包id", "需求id", "候选版本"], "资源": ["包id", "资源预算"]},
 "证据写入": "判定类能力不写结论证据（证据写入属写类动作，不在本轮能力面）"}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.策略闸门.查询策略类型", {})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.策略闸门.判定复用策略（判定复用策略）

**说明**：复用策略判定（只读）：按能力id判定是否允许新增/复用/并存，拒绝时给稳定错误码。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 能力id | 文本型 | 是 | None | 待判定的能力id |
| 存储目录 | 文本型 | 否 | 工程缓存/平台控制面 | 平台权威状态库所在目录；留空取默认目录 |
| 项目id | 文本型 | 否 |  | 调用方项目标识（随存储实例登记，用于故障定位） |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 策略类型 | 文本型，固定为 复用 |
| 允许 | 逻辑型，true=允许，false=拒绝（拒绝也在 成功=True 的信封里，按 允许 判定） |
| 理由 | 文本型，判定理由原文 |
| 错误码 | 文本型，拒绝时的稳定策略错误码（待裁决/复用被拒）；允许时为空串 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
（不存在的能力 id）{"策略类型": "复用", "允许": true, "理由": "能力不存在，允许新增", "错误码": ""}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 存储目录不可用 |
| 查询记录失败 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.策略闸门.判定复用策略", {"能力id": …, "存储目录": …, "项目id": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.策略闸门.判定依赖策略（判定依赖策略）

**说明**：依赖策略判定（只读）：声明依赖能力是否全部已登记，缺失即拒绝并列缺失项。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 包id | 文本型 | 是 | None | 被判定包id（策略主题） |
| 依赖 | 列表型 | 是 | None | 依赖声明列表，每项含 能力id |
| 存储目录 | 文本型 | 否 | 工程缓存/平台控制面 | 平台权威状态库所在目录；留空取默认目录 |
| 项目id | 文本型 | 否 |  | 调用方项目标识 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 策略类型 | 文本型，固定为 依赖 |
| 允许 | 逻辑型，true=允许，false=拒绝 |
| 理由 | 文本型，判定理由原文（含未登记依赖集合） |
| 错误码 | 文本型，拒绝时的稳定错误码（依赖缺失）；允许时为空串 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
（本条未单独实测，验证场景已覆盖）
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 存储目录不可用 |
| 查询记录失败 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.策略闸门.判定依赖策略", {"包id": …, "依赖": …, "存储目录": …, "项目id": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.策略闸门.判定签名策略（判定签名策略）

**说明**：签名策略判定（只读）：未签名/签名失效/发布者不受信一律拒绝，并给稳定错误码。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 包id | 文本型 | 是 | None | 被判定包id（策略主题） |
| 已签名 | 逻辑型 | 是 | None | 包是否已签名 |
| 签名失效 | 逻辑型 | 否 | False | 签名是否失效（内容/权限/预算变化） |
| 签名者 | 文本型 | 否 |  | 签名发布者标识 |
| 存储目录 | 文本型 | 否 | 工程缓存/平台控制面 | 平台权威状态库所在目录；留空取默认目录 |
| 项目id | 文本型 | 否 |  | 调用方项目标识 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 策略类型 | 文本型，固定为 签名 |
| 允许 | 逻辑型，true=允许，false=拒绝 |
| 理由 | 文本型，判定理由原文 |
| 错误码 | 文本型，拒绝时的稳定错误码（未签名/签名失效/发布者不受信） |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
（本条未单独实测，验证场景已覆盖）
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 存储目录不可用 |
| 查询记录失败 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.策略闸门.判定签名策略", {"包id": …, "已签名": …, "签名失效": …, "签名者": …, "存储目录": …, "项目id": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.策略闸门.判定发布策略（判定发布策略）

**说明**：发布策略判定（只读）：未确认需求、候选版本已发布即拒绝，并给稳定错误码。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 包id | 文本型 | 是 | None | 被判定包id（策略主题） |
| 候选版本 | 文本型 | 否 |  | 待发布候选版本 |
| 需求id | 文本型 | 否 |  | 关联需求id；留空表示不校验需求 |
| 存储目录 | 文本型 | 否 | 工程缓存/平台控制面 | 平台权威状态库所在目录；留空取默认目录 |
| 项目id | 文本型 | 否 |  | 调用方项目标识 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 策略类型 | 文本型，固定为 发布 |
| 允许 | 逻辑型，true=允许，false=拒绝 |
| 理由 | 文本型，判定理由原文 |
| 错误码 | 文本型，拒绝时的稳定错误码（需求未确认/版本已存在） |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
（本条未单独实测，验证场景已覆盖）
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 存储目录不可用 |
| 查询记录失败 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.策略闸门.判定发布策略", {"包id": …, "候选版本": …, "需求id": …, "存储目录": …, "项目id": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---

## 平台控制面.策略闸门.判定资源策略（判定资源策略）

**说明**：资源策略判定（纯计算）：资源预算缺 策略中心 要求的必需项即拒绝。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 包id | 文本型 | 是 | None | 被判定包id（策略主题） |
| 资源预算 | 字典型 | 是 | None | 资源预算声明字典 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 策略类型 | 文本型，固定为 资源 |
| 允许 | 逻辑型，true=允许，false=拒绝 |
| 理由 | 文本型，判定理由原文（含缺失项） |
| 错误码 | 文本型，拒绝时的稳定错误码（预算不完整） |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 638）：

```json
（空 资源预算）{"策略类型": "资源", "允许": false, "理由": "资源预算缺少必需项: [...]", "错误码": "预算不完整"}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**（进程内装配后）：

```python
后端.调用("平台控制面.策略闸门.判定资源策略", {"包id": …, "资源预算": …})
```

**验证状态**：见同包 `验证场景引用.json`。

---
