# 包仓库 使用说明
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 包id：平台控制面.包仓库
- 版本：1.1.1

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 平台控制面.包仓库.校验平台客户端制品 | 校验平台客户端制品 | 1.0.0 | {'类型': '结果型', '值结构': {'通过': '逻辑型，true = 签名有效、磁盘内容一致且信任元数据目标校验通过', '消息': '文本型，校验结论说明（不通过时逐字沿用实现层原文）'}} | 校验平台客户端制品：真实重读磁盘逐一比对文件摘要，并校验 Ed25519 签名与信任元数据目标，篡改或半成品一律判不通过。 |
| 平台控制面.包仓库.校验平台客户端稳定路径 | 校验平台客户端稳定路径 | 1.0.0 | {'类型': '结果型', '值结构': {'有效': '逻辑型，true = 稳定路径可安全读取', '消息': '文本型，有效性说明（无效时逐字沿用实现层原文）', '激活路径': '文本型，有效时的安装目录绝对路径；无效时为空串'}} | 校验平台客户端稳定路径：激活指针可读 → 已安装目录存在 → 目录摘要与指针一致 → 指针指向的制品目录存在且摘要一致，任一不符即判无效并给出可诊断原因。 |
| 平台控制面.包仓库.安装候选包 | 安装候选包 | 1.0.0 | {'类型': '结果型', '值结构': {'安装路径': '文本型，安装后的版本目录绝对路径', '包id': '文本型，安装的包id', '版本': '文本型，安装的包版本', '完整性摘要': '文本型，安装目录的内容摘要（同一包同一版本的唯一摘要）'}} | 安装候选包到不可变版本目录：校验包声明与包结构，同包同版本禁止覆盖，经临时目录加原子替换落盘并生成安装记录，失败清理临时目录。 |
| 平台控制面.包仓库.校验已安装包完整性 | 校验已安装包完整性 | 1.0.0 | {'类型': '结果型', '值结构': {'校验成功': '逻辑型，true = 版本已安装且内容摘要与期望摘要一致', '证据': '文本型，实际摘要，或摘要不一致/未安装的原因原文'}} | 校验已安装版本的完整性摘要：重算安装目录内容摘要并与期望摘要逐字比对，摘要漂移或版本未安装即判校验失败并给出证据原文。 |
| 平台控制面.包仓库.校验可重复构建 | 校验可重复构建 | 1.0.0 | {'类型': '结果型', '值结构': {'双工作区一致': '逻辑型，true = 冻结输入在两个全新工作区的构建摘要完全一致', '基线摘要': '字典型，冻结输入的正式文件摘要表（相对路径 → sha256）', '变化摘要': '字典型，变化输入的正式文件摘要表（相对路径 → sha256）', '消息': '文本型，双工作区一致性说明（不一致时含首个差异文件）'}} | 校验可重复构建：同一冻结输入在两个全新工作区构建的文件摘要必须完全一致，同时给出变化输入的构建摘要，供调用方判定“输入变化摘要必须变化”。 |
| 平台控制面.包仓库.校验可信仓库元数据 | 校验可信仓库元数据 | 1.0.0 | {'类型': '结果型', '值结构': {'根信任通过': '逻辑型，true = 根信任元数据验签与新鲜度校验通过', '目标通过': '逻辑型，true = 目标元数据验签且制品内容摘要与登记一致', '快照通过': '逻辑型，true = 快照元数据验签且目标表与磁盘目标一致', '替换攻击阻断': '逻辑型，true = 被替换制品摘要的目标元数据确实校验失败', '原因': '文本型，未通过项的原因汇总；全部通过且反向阻断时为替换攻击的阻断原因'}} | 校验可信仓库元数据：真实初始化根信任、发布目标制品、生成仓库快照并逐类做 Ed25519 验签，再对被替换的目标摘要做一次反向校验，证明替换攻击确实被阻断。 |
| 平台控制面.包仓库.校验包生命周期 | 校验包生命周期 | 1.0.0 | {'类型': '结果型', '值结构': {'问题列表': '列表型，状态机不符合契约的问题描述；空列表表示全部判定通过'}} | 校验包生命周期状态机：真实执行七步合法流转、一次非法流转、一次重复操作与故障回滚，返回全部不符合契约的问题清单。 |
| 平台控制面.包仓库.登记发布者 | 登记发布者 | 1.0.0 | {'类型': '结果型', '值结构': {'发布者': '文本型，登记的发布者身份', '状态': '文本型，重读信任记录的状态（有效表示登记成功）', '过期时间': '文本型，重读信任记录的过期时间戳', '公钥指纹': '文本型，公钥 PEM 的 sha256 前 16 位（与 支持库.适配层 公钥指纹同一算法）'}} | 登记发布者：把发布者身份与公钥写入信任目录（复用 签名能力.登记发布者），并重读信任记录回报状态与过期时间；私钥一律不得登记为公钥。 |
| 平台控制面.包仓库.撤销发布者 | 撤销发布者 | 1.0.0 | {'类型': '结果型', '值结构': {'已撤销': '逻辑型，true = 本次把有效信任记录置为已撤销', '状态': '文本型，撤销后重读的信任记录状态', '消息': '文本型，撤销结论说明（含重读状态）'}} | 撤销发布者：把信任目录中该发布者的有效记录置为已撤销（复用 签名能力.撤销发布者）；未登记过的发布者判 发布者不存在，已撤销的返回 已撤销=假 的幂等结论。 |
| 平台控制面.包仓库.构建制品 | 构建制品 | 1.0.0 | {'类型': '结果型', '值结构': {'制品摘要': '文本型，制品的内容寻址摘要（sha256 前 32 位，也是制品目录名）', '包id': '文本型，构建的包id', '版本': '文本型，构建的版本', '消息': '文本型，实现层构建结论原文'}} | 构建制品：冻结输入经路径安全校验、临时目录构建、内容寻址摘要与原子发布落成制品（复用 服务.包仓库.构建制品），返回内容寻址制品摘要。 |
| 平台控制面.包仓库.签名制品 | 签名制品 | 1.0.0 | {'类型': '结果型', '值结构': {'制品摘要': '文本型，已签名制品的摘要', '发布者': '文本型，签名者身份', '状态': '文本型，重读制品记录的状态（已签名表示成功）', '消息': '文本型，实现层签名结论原文'}} | 签名制品：用发布者身份对已构建制品做 Ed25519 签名（复用 签名能力.签名制品），签名正文覆盖包id/版本/发布者/文件摘要/物料清单/来源证据；发布者不在信任目录或已撤销即拒绝。 |
| 平台控制面.包仓库.校验签名 | 校验签名 | 1.0.0 | {'类型': '结果型', '值结构': {'签名有效': '逻辑型，true = 签名验签通过且磁盘文件摘要与签名正文一致', '消息': '文本型，校验结论说明（不通过时逐字沿用实现层原文）'}} | 校验签名：Ed25519 验签 + 重读制品目录逐一重算文件摘要（复用 签名能力.校验签名），已撤销发布者的签名一并判无效；未签名/被篡改的制品一律判 签名有效=假。 |
| 平台控制面.包仓库.安装制品 | 安装制品 | 1.0.0 | {'类型': '结果型', '值结构': {'安装路径': '文本型，安装到达的目标目录', '消息': '文本型，实现层安装结论原文'}} | 安装制品：安装前后双重校验（签名 + 磁盘摘要）后经临时目录与原子替换落盘（复用 安装能力.安装制品）；未签名、被篡改或半成品一律在复制前被拒。 |
| 平台控制面.包仓库.查询制品布局 | 查询制品布局 | 1.0.0 | {'类型': '结果型', '值结构': {'制品根目录': '文本型，生效的制品内容寻址根目录绝对路径', '客户端制品目录': '文本型，制品根目录下的 平台客户端制品 子目录（实现层派生口径）', '制品路径': '文本型，制品根目录/制品摘要；未给 制品摘要 时为空串', '根目录存在': '逻辑型，true = 制品根目录当前已存在（本能力不创建目录）'}} | 查询制品布局（只读）：把实现层的制品目录口径暴露成能力——返回制品根目录、客户端制品目录与指定摘要对应的内容寻址路径，调用方不再直取实现属性拼字符串。 |
| 平台控制面.包仓库.诊断平台客户端激活指针 | 诊断平台客户端激活指针 | 1.0.0 | {'类型': '结果型', '值结构': {'陈旧': '逻辑型，true = 源制品目录与已安装副本双边都对不上指针（真陈旧，需重置或重装）', '可继续': '逻辑型，true = 可以继续安装（单边不符可继续，安装会修复源目录）', '原因': '文本型，诊断说明（逐字沿用实现层原文）', '陈旧级别': '文本型，取值：无 / 源目录被写脏（已安装副本正常） / 已安装副本被改（源制品正常） / 指针摘要字段不合法 / 指向制品未入库（摘要缺失） / 指向制品目录不存在 / 源与已安装摘要均不符 / 指针文件不可读', '源符合': '逻辑型，true = 源制品目录实算摘要与指针一致', '已安装符合': '逻辑型，true = 已安装副本实算摘要与指针一致', '摘要16': '文本型，指针里的 摘要sha256（制品身份摘要）', '制品名': '文本型，指针里的 制品目录（平台客户端-<摘要16>）', '制品摘要': '文本型，指针里的 制品摘要（内容寻址摘要）', '必经路径': '字典型，收尾报告（制品根指针 schema 归一与过期制品回收的结论）'}} | 诊断平台客户端激活指针（D-17 ① 的可诊断出口）：按源制品目录与已安装副本**双边**判定陈旧，返回陈旧级别、原因与双边符合性；单边不符只记级别且判可继续（安装时会重建源目录修复）。 |
| 平台控制面.包仓库.重建平台客户端激活指针 | 重建平台客户端激活指针 | 1.0.0 | {'类型': '结果型', '值结构': {'已重建': '逻辑型，true = 指针已按已安装副本重建', '消息': '文本型，重建结论说明（含原摘要 → 新摘要，逐字沿用实现层原文）', '原摘要': '文本型，改写前指针里的 摘要sha256；无指针时为空串', '新摘要': '文本型，已安装副本的实算身份摘要（改写后指针里的 摘要sha256）', '已安装路径': '文本型，已安装副本 <环境目录>/平台客户端 的绝对路径'}} | 重建平台客户端激活指针（D-17 ① 的修复出口）：按**已安装副本**的实算摘要改写 环境目录/当前.json，使稳定路径立刻恢复可读；不重复安装、不碰制品仓库。 |
| 平台控制面.包仓库.重置平台客户端激活指针 | 重置平台客户端激活指针 | 1.0.0 | {'类型': '结果型', '值结构': {'已重置': '逻辑型，true = 本次执行了重置（指针已备份或本就是未安装态）', '消息': '文本型，重置结论说明（含备份路径与发布管理侧清空结论，逐字沿用实现层原文）', '备份路径': '文本型，指针备份文件绝对路径；无 当前.json 可备份时为空串', '发布指针已清空': '逻辑型，true = 发布管理侧确有激活记录且本次已清空（版本+1、栅栏令牌+1）'}} | 重置平台客户端激活指针（D-17 ① 的修复出口）：把 环境目录/当前.json 改名备份为 当前.json.陈旧指针备份_<时间戳>（不删除），并同批清空发布管理侧激活指针（版本+1、栅栏令牌+1），回到「从未安装」状态。 |
| 平台控制面.包仓库.读取或生成客户端签名密钥 | 读取或生成客户端签名密钥 | 1.0.0 | {'类型': '结果型', '值结构': {'私钥PEM': '文本型，Ed25519 私钥 PEM（复用或本次生成；只经参数与返回进出，不写入状态库、不进日志）', '公钥PEM': '文本型，Ed25519 公钥 PEM（SubjectPublicKeyInfo）', '已复用': '逻辑型，true = 本次原样复用了磁盘上已有的密钥对（未生成新钥）', '密钥目录': '文本型，生效的密钥目录绝对路径'}} | 读取或生成客户端签名密钥（**落盘 + 复用**，非纯计算）：已有 发布者私钥.pem / 发布者公钥.pem 即原样复用（保证公钥与既有信任记录一致），缺失才生成并落盘并加固权限（目录 700、私钥 600、公钥 644）。 |
| 平台控制面.包仓库.入库客户端制品 | 入库客户端制品 | 1.0.0 | {'类型': '结果型', '值结构': {'制品摘要': '文本型，制品的内容寻址摘要（sha256 前 32 位，也是内容寻址目录名）', '制品名': '文本型，入库的制品目录名（平台客户端-<摘要16>）', '消息': '文本型，实现层入库结论原文（含内容寻址+签名+信任元数据）'}} | 入库客户端制品：把已构建的客户端制品 平台客户端-<摘要16> 登记到正式包仓库体系——内容寻址登记 → 生成元数据落盘 → Ed25519 签名 → 磁盘信任元数据（根信任/目标/快照）→ 入库自检五步编排，一条能力给全结论。 |
| 平台控制面.包仓库.安装平台客户端制品 | 安装平台客户端制品 | 1.0.0 | {'类型': '结果型', '值结构': {'安装路径': '文本型，安装到达的稳定路径 <环境目录>/平台客户端', '制品名': '文本型，本次安装的制品目录名（传入或实现层派生）', '复用': '逻辑型，true = 已安装同摘要且指针一致，本次为幂等复用（未重复复制）', '消息': '文本型，实现层安装结论原文'}} | 安装平台客户端制品到环境：固定装到 <环境目录>/平台客户端，经临时目录 + 逐文件 fsync + os.replace 原子替换，再做发布管理 CAS 激活指针并同步 当前.json。与 安装制品 的差别：安装制品按调用方给的 目标目录 复制、不做发布管理激活、不写 当前.json。 |

## 平台控制面.包仓库.校验平台客户端制品（校验平台客户端制品）

**说明**：校验平台客户端制品：真实重读磁盘逐一比对文件摘要，并校验 Ed25519 签名与信任元数据目标，篡改或半成品一律判不通过。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 制品摘要 | 文本型 | 是 | None | 待校验制品的标识摘要（入库时生成），必填；空值或非法摘要判 参数不合法 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录，客户端制品目录由它派生出“平台客户端制品”子目录 |
| 环境目录 | 文本型 | 否 |  | 平台客户端安装环境目录（外部调用方稳定路径所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据（根信任/目标/快照）所在目录；留空取实现层默认目录 |

**返回结构**：{'类型': '结果型', '值结构': {'通过': '逻辑型，true = 签名有效、磁盘内容一致且信任元数据目标校验通过', '消息': '文本型，校验结论说明（不通过时逐字沿用实现层原文）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验平台客户端制品(制品摘要=值, 状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.校验平台客户端稳定路径（校验平台客户端稳定路径）

**说明**：校验平台客户端稳定路径：激活指针可读 → 已安装目录存在 → 目录摘要与指针一致 → 指针指向的制品目录存在且摘要一致，任一不符即判无效并给出可诊断原因。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录；留空取实现层默认目录 |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录 |
| 环境目录 | 文本型 | 否 |  | 安装环境目录（当前.json 与 平台客户端 目录所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据目录；留空取实现层默认目录 |
| 客户端制品目录 | 文本型 | 否 |  | 客户端制品目录；留空时由 制品根目录 派生（制品根目录/平台客户端制品），两者都留空取实现层默认目录 |

**返回结构**：{'类型': '结果型', '值结构': {'有效': '逻辑型，true = 稳定路径可安全读取', '消息': '文本型，有效性说明（无效时逐字沿用实现层原文）', '激活路径': '文本型，有效时的安装目录绝对路径；无效时为空串'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验平台客户端稳定路径(状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值, 客户端制品目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.安装候选包（安装候选包）

**说明**：安装候选包到不可变版本目录：校验包声明与包结构，同包同版本禁止覆盖，经临时目录加原子替换落盘并生成安装记录，失败清理临时目录。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库根目录 | 文本型 | 是 | None | 不可变版本仓库根目录（版本/ 与 安装记录/ 由实现层自动创建），必填 |
| 包目录 | 文本型 | 是 | None | 候选包源码目录（须含 包声明.json/能力契约/实现），必填 |
| 开工ID | 文本型 | 否 |  | 本次改动的开工凭证（`开工编排.开工即占` 返回）。落点由调用方入参决定，按 fail-closed 声明；本能力只做形态归一、不自行判拒（判据唯一在 `公共契约/运行时/写入授权.py::校验写入授权`） |

**返回结构**：{'类型': '结果型', '值结构': {'安装路径': '文本型，安装后的版本目录绝对路径', '包id': '文本型，安装的包id', '版本': '文本型，安装的包版本', '完整性摘要': '文本型，安装目录的内容摘要（同一包同一版本的唯一摘要）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 候选包安装失败 |

**调用示例**：

```python
平台控制面.包仓库.安装候选包(仓库根目录=值, 包目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.校验已安装包完整性（校验已安装包完整性）

**说明**：校验已安装版本的完整性摘要：重算安装目录内容摘要并与期望摘要逐字比对，摘要漂移或版本未安装即判校验失败并给出证据原文。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库根目录 | 文本型 | 是 | None | 不可变版本仓库根目录，必填 |
| 包id | 文本型 | 是 | None | 已安装包的包id，必填 |
| 版本 | 文本型 | 是 | None | 已安装包的版本号，必填 |
| 期望摘要 | 文本型 | 否 |  | 期望的内容摘要；留空只重算不比对（用于取当前摘要），非空时必须逐字一致 |

**返回结构**：{'类型': '结果型', '值结构': {'校验成功': '逻辑型，true = 版本已安装且内容摘要与期望摘要一致', '证据': '文本型，实际摘要，或摘要不一致/未安装的原因原文'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验已安装包完整性(仓库根目录=值, 包id=值, 版本=值, 期望摘要=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.校验可重复构建（校验可重复构建）

**说明**：校验可重复构建：同一冻结输入在两个全新工作区构建的文件摘要必须完全一致，同时给出变化输入的构建摘要，供调用方判定“输入变化摘要必须变化”。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 冻结输入 | 字典型 | 是 | None | 冻结输入：必须含非空 文件表（相对路径 → 文件内容），可选 构建时刻/主机名；内容里的 {时间戳}/{工作区路径}/{主机名} 占位符按冻结值确定性展开 |
| 变化输入 | 字典型 | 是 | None | 与冻结输入做对照的变化输入，结构与冻结输入相同；其构建摘要用于判定摘要确实随输入变化 |
| 工作区根目录 | 文本型 | 否 |  | 变化输入构建所用工作区根目录（不存在即创建）；留空时用一次性临时工作区，调用结束即清理 |

**返回结构**：{'类型': '结果型', '值结构': {'双工作区一致': '逻辑型，true = 冻结输入在两个全新工作区的构建摘要完全一致', '基线摘要': '字典型，冻结输入的正式文件摘要表（相对路径 → sha256）', '变化摘要': '字典型，变化输入的正式文件摘要表（相对路径 → sha256）', '消息': '文本型，双工作区一致性说明（不一致时含首个差异文件）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验可重复构建(冻结输入=值, 变化输入=值, 工作区根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.校验可信仓库元数据（校验可信仓库元数据）

**说明**：校验可信仓库元数据：真实初始化根信任、发布目标制品、生成仓库快照并逐类做 Ed25519 验签，再对被替换的目标摘要做一次反向校验，证明替换攻击确实被阻断。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 存储目录 | 文本型 | 否 |  | 可信仓库元数据存储目录（根信任/目标/快照与离线私钥所在）；留空取平台运行态默认目录（工程缓存/制品仓库/可信仓库校验探测） |

| 存储目录 | 文本型 | 否 |  | 可信仓库元数据存储目录（根信任/目标/快照与离线私钥所在）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/可信仓库校验探测`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'根信任通过': '逻辑型，true = 根信任元数据验签与新鲜度校验通过', '目标通过': '逻辑型，true = 目标元数据验签且制品内容摘要与登记一致', '快照通过': '逻辑型，true = 快照元数据验签且目标表与磁盘目标一致', '替换攻击阻断': '逻辑型，true = 被替换制品摘要的目标元数据确实校验失败', '原因': '文本型，未通过项的原因汇总；全部通过且反向阻断时为替换攻击的阻断原因'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验可信仓库元数据(存储目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.校验包生命周期（校验包生命周期）

**说明**：校验包生命周期状态机：真实执行七步合法流转、一次非法流转、一次重复操作与故障回滚，返回全部不符合契约的问题清单。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 包id | 文本型 | 是 | None | 被校验状态机的包id（仅用于状态记录身份，不读取磁盘），必填 |
| 版本 | 文本型 | 否 | 1.0.0 | 被校验状态机的包版本；留空取 1.0.0 |

**返回结构**：{'类型': '结果型', '值结构': {'问题列表': '列表型，状态机不符合契约的问题描述；空列表表示全部判定通过'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验包生命周期(包id=值, 版本=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.登记发布者（登记发布者）

**说明**：登记发布者：把发布者身份与公钥写入信任目录（复用 签名能力.登记发布者），并重读信任记录回报状态与过期时间；私钥一律不得登记为公钥。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 发布者 | 文本型 | 是 | None | 发布者身份标识（信任记录主键），必填；空值判 参数不合法 |
| 公钥PEM | 文本型 | 是 | None | 发布者 Ed25519 公钥 PEM（SubjectPublicKeyInfo 公钥块），必填；传入私钥或非公钥块判 参数不合法 |
| 有效期秒 | 双精度数型 | 否 | 31536000.0 | 信任有效期（秒）；留空取 31536000（365 天），非正数判 参数不合法 |
| 维护者 | 文本型 | 否 |  | 执行登记的维护者身份，写入证据记录（调用者字段） |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录（工程缓存/制品仓库） |

| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'发布者': '文本型，登记的发布者身份', '状态': '文本型，重读信任记录的状态（有效表示登记成功）', '过期时间': '文本型，重读信任记录的过期时间戳', '公钥指纹': '文本型，公钥 PEM 的 sha256 前 16 位（与 支持库.适配层 公钥指纹同一算法）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 发布者登记未落盘 |

**调用示例**：

```python
平台控制面.包仓库.登记发布者(发布者=值, 公钥PEM=值, 有效期秒=值, 维护者=值, 状态目录=值, 制品根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.撤销发布者（撤销发布者）

**说明**：撤销发布者：把信任目录中该发布者的有效记录置为已撤销（复用 签名能力.撤销发布者）；未登记过的发布者判 发布者不存在，已撤销的返回 已撤销=假 的幂等结论。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 发布者 | 文本型 | 是 | None | 待撤销的发布者身份，必填；空值判 参数不合法 |
| 维护者 | 文本型 | 否 |  | 执行撤销的维护者身份 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录（工程缓存/制品仓库） |

| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'已撤销': '逻辑型，true = 本次把有效信任记录置为已撤销', '状态': '文本型，撤销后重读的信任记录状态', '消息': '文本型，撤销结论说明（含重读状态）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 发布者不存在 |

**调用示例**：

```python
平台控制面.包仓库.撤销发布者(发布者=值, 维护者=值, 状态目录=值, 制品根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.构建制品（构建制品）

**说明**：构建制品：冻结输入经路径安全校验、临时目录构建、内容寻址摘要与原子发布落成制品（复用 服务.包仓库.构建制品），返回内容寻址制品摘要。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 包id | 文本型 | 是 | None | 制品所属包id，必填；空值判 参数不合法 |
| 版本 | 文本型 | 是 | None | 制品版本（同包同版本只绑定一个内容摘要），必填；空值判 参数不合法 |
| 文件表 | 字典型 | 是 | None | 正式文件表（相对路径 → 文件内容），必须非空且键值皆为文本；二进制文件按 hexfile: 前缀十六进制入库，必填 |
| 构建输入 | 字典型 | 是 | None | 构建来源证据（源提交/构建命令等），必须以对象给出，必填 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录（工程缓存/制品仓库） |

| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'制品摘要': '文本型，制品的内容寻址摘要（sha256 前 32 位，也是制品目录名）', '包id': '文本型，构建的包id', '版本': '文本型，构建的版本', '消息': '文本型，实现层构建结论原文'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 制品构建失败 |

**调用示例**：

```python
平台控制面.包仓库.构建制品(包id=值, 版本=值, 文件表=值, 构建输入=值, 状态目录=值, 制品根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.签名制品（签名制品）

**说明**：签名制品：用发布者身份对已构建制品做 Ed25519 签名（复用 签名能力.签名制品），签名正文覆盖包id/版本/发布者/文件摘要/物料清单/来源证据；发布者不在信任目录或已撤销即拒绝。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 制品摘要 | 文本型 | 是 | None | 待签名制品的内容寻址摘要，必填；空值判 参数不合法 |
| 私钥PEM | 文本型 | 是 | None | 发布者 Ed25519 私钥 PEM，必填；只经参数进出，不落盘、不进返回与证据；非私钥块判 参数不合法 |
| 发布者 | 文本型 | 是 | None | 签名者身份，必须在信任目录且未撤销、未过期，必填 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录（工程缓存/制品仓库） |

| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'制品摘要': '文本型，已签名制品的摘要', '发布者': '文本型，签名者身份', '状态': '文本型，重读制品记录的状态（已签名表示成功）', '消息': '文本型，实现层签名结论原文'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 制品签名失败 |

**调用示例**：

```python
平台控制面.包仓库.签名制品(制品摘要=值, 私钥PEM=值, 发布者=值, 状态目录=值, 制品根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.校验签名（校验签名）

**说明**：校验签名：Ed25519 验签 + 重读制品目录逐一重算文件摘要（复用 签名能力.校验签名），已撤销发布者的签名一并判无效；未签名/被篡改的制品一律判 签名有效=假。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 制品摘要 | 文本型 | 是 | None | 待校验制品的内容寻址摘要，必填；空值判 参数不合法 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录（工程缓存/制品仓库） |

| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'签名有效': '逻辑型，true = 签名验签通过且磁盘文件摘要与签名正文一致', '消息': '文本型，校验结论说明（不通过时逐字沿用实现层原文）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.校验签名(制品摘要=值, 状态目录=值, 制品根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.安装制品（安装制品）

**说明**：安装制品：安装前后双重校验（签名 + 磁盘摘要）后经临时目录与原子替换落盘（复用 安装能力.安装制品）；未签名、被篡改或半成品一律在复制前被拒。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 制品摘要 | 文本型 | 是 | None | 待安装制品的内容寻址摘要，必填；空值判 参数不合法 |
| 目标目录 | 文本型 | 是 | None | 安装目标目录（安装前后均校验，已存在时由实现层原子替换覆盖），必填；空值判 参数不合法 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空取实现层默认目录（工程缓存/制品仓库/平台客户端状态） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认目录（工程缓存/制品仓库） |

| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`；制品/受管缓存态落制品外） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外） |
**返回结构**：{'类型': '结果型', '值结构': {'安装路径': '文本型，安装到达的目标目录', '消息': '文本型，实现层安装结论原文'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 制品安装失败 |

**调用示例**：

```python
平台控制面.包仓库.安装制品(制品摘要=值, 目标目录=值, 状态目录=值, 制品根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.查询制品布局（查询制品布局）

**说明**：查询制品布局（只读）：把实现层的制品目录口径暴露成能力——返回制品根目录、客户端制品目录与指定摘要对应的内容寻址路径，调用方不再直取实现属性拼字符串。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空取实现层默认根目录（工程缓存/制品仓库，绝对路径，不受调用方工作目录影响） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`；制品/受管缓存态落制品外）；两种布局下都是绝对路径，不受调用方工作目录影响 |
| 制品摘要 | 文本型 | 否 |  | 制品摘要（sha256 前 32 位）；给出时返回 制品根目录/制品摘要 的内容寻址路径，长度非 32 位判 参数不合法 |

**返回结构**：{'类型': '结果型', '值结构': {'制品根目录': '文本型，生效的制品内容寻址根目录绝对路径', '客户端制品目录': '文本型，制品根目录下的 平台客户端制品 子目录（实现层派生口径）', '制品路径': '文本型，制品根目录/制品摘要；未给 制品摘要 时为空串', '根目录存在': '逻辑型，true = 制品根目录当前已存在（本能力不创建目录）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.查询制品布局(制品根目录=值, 制品摘要=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.诊断平台客户端激活指针（诊断平台客户端激活指针）

**说明**：诊断平台客户端激活指针（D-17 ① 的可诊断出口）：按源制品目录与已安装副本**双边**判定陈旧，返回陈旧级别、原因与双边符合性；单边不符只记级别且判可继续（安装时会重建源目录修复）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任/发布/激活指针事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`）；链内其余三个目录口径与之同根，客户端制品目录由它派生（制品根目录/平台客户端制品） |
| 环境目录 | 文本型 | 否 |  | 平台客户端安装环境目录（已安装副本 `<环境目录>/平台客户端` 与激活指针 `<环境目录>/当前.json` 所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据（根信任/目标/快照）目录；留空取实现层默认目录 |
| 自动收尾 | 逻辑型 | 否 | True | 真（缺省）= 走实现层必经路径收尾（归一到制品根 当前.json 的唯一 schema + 按上限回收过期制品）；假 = 纯只读结论，不写任何文件（隔离断言用） |

**返回结构**：{'类型': '结果型', '值结构': {'陈旧': '逻辑型，true = 源制品目录与已安装副本双边都对不上指针（真陈旧，需重置或重装）', '可继续': '逻辑型，true = 可以继续安装（单边不符可继续，安装会修复源目录）', '原因': '文本型，诊断说明（逐字沿用实现层原文）', '陈旧级别': '文本型，取值：无 / 源目录被写脏（已安装副本正常） / 已安装副本被改（源制品正常） / 指针摘要字段不合法 / 指向制品未入库（摘要缺失） / 指向制品目录不存在 / 源与已安装摘要均不符 / 指针文件不可读', '源符合': '逻辑型，true = 源制品目录实算摘要与指针一致', '已安装符合': '逻辑型，true = 已安装副本实算摘要与指针一致', '摘要16': '文本型，指针里的 摘要sha256（制品身份摘要）', '制品名': '文本型，指针里的 制品目录（平台客户端-<摘要16>）', '制品摘要': '文本型，指针里的 制品摘要（内容寻址摘要）', '必经路径': '字典型，收尾报告（制品根指针 schema 归一与过期制品回收的结论）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.包仓库.诊断平台客户端激活指针(状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值, 自动收尾=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.重建平台客户端激活指针（重建平台客户端激活指针）

**说明**：重建平台客户端激活指针（D-17 ① 的修复出口）：按**已安装副本**的实算摘要改写 环境目录/当前.json，使稳定路径立刻恢复可读；不重复安装、不碰制品仓库。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任/发布/激活指针事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`）；链内其余三个目录口径与之同根，客户端制品目录由它派生（制品根目录/平台客户端制品） |
| 环境目录 | 文本型 | 否 |  | 平台客户端安装环境目录（已安装副本 `<环境目录>/平台客户端` 与激活指针 `<环境目录>/当前.json` 所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据（根信任/目标/快照）目录；留空取实现层默认目录 |

**返回结构**：{'类型': '结果型', '值结构': {'已重建': '逻辑型，true = 指针已按已安装副本重建', '消息': '文本型，重建结论说明（含原摘要 → 新摘要，逐字沿用实现层原文）', '原摘要': '文本型，改写前指针里的 摘要sha256；无指针时为空串', '新摘要': '文本型，已安装副本的实算身份摘要（改写后指针里的 摘要sha256）', '已安装路径': '文本型，已安装副本 <环境目录>/平台客户端 的绝对路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目标不存在 |
| 文件读取失败 |

**调用示例**：

```python
平台控制面.包仓库.重建平台客户端激活指针(状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.重置平台客户端激活指针（重置平台客户端激活指针）

**说明**：重置平台客户端激活指针（D-17 ① 的修复出口）：把 环境目录/当前.json 改名备份为 当前.json.陈旧指针备份_<时间戳>（不删除），并同批清空发布管理侧激活指针（版本+1、栅栏令牌+1），回到「从未安装」状态。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任/发布/激活指针事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`）；链内其余三个目录口径与之同根，客户端制品目录由它派生（制品根目录/平台客户端制品） |
| 环境目录 | 文本型 | 否 |  | 平台客户端安装环境目录（已安装副本 `<环境目录>/平台客户端` 与激活指针 `<环境目录>/当前.json` 所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据（根信任/目标/快照）目录；留空取实现层默认目录 |

**返回结构**：{'类型': '结果型', '值结构': {'已重置': '逻辑型，true = 本次执行了重置（指针已备份或本就是未安装态）', '消息': '文本型，重置结论说明（含备份路径与发布管理侧清空结论，逐字沿用实现层原文）', '备份路径': '文本型，指针备份文件绝对路径；无 当前.json 可备份时为空串', '发布指针已清空': '逻辑型，true = 发布管理侧确有激活记录且本次已清空（版本+1、栅栏令牌+1）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 写入失败 |

**调用示例**：

```python
平台控制面.包仓库.重置平台客户端激活指针(状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.读取或生成客户端签名密钥（读取或生成客户端签名密钥）

**说明**：读取或生成客户端签名密钥（**落盘 + 复用**，非纯计算）：已有 发布者私钥.pem / 发布者公钥.pem 即原样复用（保证公钥与既有信任记录一致），缺失才生成并落盘并加固权限（目录 700、私钥 600、公钥 644）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 密钥目录 | 文本型 | 否 |  | 客户端签名密钥目录；留空时经唯一解析器取运行态根下的 `平台客户端密钥`（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端密钥`） |

**返回结构**：{'类型': '结果型', '值结构': {'私钥PEM': '文本型，Ed25519 私钥 PEM（复用或本次生成；只经参数与返回进出，不写入状态库、不进日志）', '公钥PEM': '文本型，Ed25519 公钥 PEM（SubjectPublicKeyInfo）', '已复用': '逻辑型，true = 本次原样复用了磁盘上已有的密钥对（未生成新钥）', '密钥目录': '文本型，生效的密钥目录绝对路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 写入失败 |

**调用示例**：

```python
平台控制面.包仓库.读取或生成客户端签名密钥(密钥目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.入库客户端制品（入库客户端制品）

**说明**：入库客户端制品：把已构建的客户端制品 平台客户端-<摘要16> 登记到正式包仓库体系——内容寻址登记 → 生成元数据落盘 → Ed25519 签名 → 磁盘信任元数据（根信任/目标/快照）→ 入库自检五步编排，一条能力给全结论。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 制品目录 | 文本型 | 是 | None | 已构建的客户端制品目录（须为 平台客户端-<摘要16> 形态，且目录名摘要与实算摘要一致），必填；不存在判 目标不存在 |
| 构建输入 | 字典型 | 是 | None | 构建来源证据（源提交/构建命令等），必须是非空对象，必填；空值判 参数不合法 |
| 私钥PEM | 文本型 | 是 | None | 发布者 Ed25519 私钥 PEM（只经参数进出，不落盘、不进返回与证据），必填；非私钥块判 参数不合法 |
| 公钥PEM | 文本型 | 是 | None | 发布者 Ed25519 公钥 PEM；发布者首次登记时写入信任目录，与既有信任记录不一致则判 验证失败，必填 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任/发布/激活指针事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`）；链内其余三个目录口径与之同根，客户端制品目录由它派生（制品根目录/平台客户端制品） |
| 环境目录 | 文本型 | 否 |  | 平台客户端安装环境目录（已安装副本 `<环境目录>/平台客户端` 与激活指针 `<环境目录>/当前.json` 所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据（根信任/目标/快照）目录；留空取实现层默认目录 |

**返回结构**：{'类型': '结果型', '值结构': {'制品摘要': '文本型，制品的内容寻址摘要（sha256 前 32 位，也是内容寻址目录名）', '制品名': '文本型，入库的制品目录名（平台客户端-<摘要16>）', '消息': '文本型，实现层入库结论原文（含内容寻址+签名+信任元数据）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目标不存在 |
| 验证失败 |

**调用示例**：

```python
平台控制面.包仓库.入库客户端制品(制品目录=值, 构建输入=值, 私钥PEM=值, 公钥PEM=值, 状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）

## 平台控制面.包仓库.安装平台客户端制品（安装平台客户端制品）

**说明**：安装平台客户端制品到环境：固定装到 <环境目录>/平台客户端，经临时目录 + 逐文件 fsync + os.replace 原子替换，再做发布管理 CAS 激活指针并同步 当前.json。与 安装制品 的差别：安装制品按调用方给的 目标目录 复制、不做发布管理激活、不写 当前.json。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 开工ID | 文本型 | 否 | None | 本次开工凭证（`开工编排.开工即占` 返回）；本能力把 开工ID 透传给底下唯一写入腿，受管路径写入须被一条 所有者==该开工ID 的活跃写租约覆盖，判据唯一在 `公共契约/运行时/写入授权.校验写入授权`（本层不自行判拒） |
| 制品摘要 | 文本型 | 是 | None | 已入库制品的内容寻址摘要（sha256 前 32 位），必填；长度非 32 位判 参数不合法，未入库判 制品安装失败 |
| 制品名 | 文本型 | 否 |  | 安装后的制品目录名（平台客户端-<摘要16>）；留空由实现层按制品记录的版本字段派生 |
| 状态目录 | 文本型 | 否 |  | 平台状态库所在目录（制品/信任/发布/激活指针事实的权威存储）；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库/平台客户端状态`） |
| 制品根目录 | 文本型 | 否 |  | 制品内容寻址根目录；留空时经唯一解析器取运行态根（源码态回落 `<系统根>/工程缓存/制品仓库`）；链内其余三个目录口径与之同根，客户端制品目录由它派生（制品根目录/平台客户端制品） |
| 环境目录 | 文本型 | 否 |  | 平台客户端安装环境目录（已安装副本 `<环境目录>/平台客户端` 与激活指针 `<环境目录>/当前.json` 所在）；留空取实现层默认目录 |
| 信任目录 | 文本型 | 否 |  | 可信仓库元数据（根信任/目标/快照）目录；留空取实现层默认目录 |

**返回结构**：{'类型': '结果型', '值结构': {'安装路径': '文本型，安装到达的稳定路径 <环境目录>/平台客户端', '制品名': '文本型，本次安装的制品目录名（传入或实现层派生）', '复用': '逻辑型，true = 已安装同摘要且指针一致，本次为幂等复用（未重复复制）', '消息': '文本型，实现层安装结论原文'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 制品安装失败 |

**调用示例**：

```python
平台控制面.包仓库.安装平台客户端制品(制品摘要=值, 制品名=值, 状态目录=值, 制品根目录=值, 环境目录=值, 信任目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：包仓库.制品与稳定路径校验（本包 验证场景引用.json，2 步）、包仓库.候选包安装与完整性校验（本包 验证场景引用.json，2 步）、包仓库.可重复构建校验（本包 验证场景引用.json，1 步）、包仓库.可信仓库元数据与生命周期校验（本包 验证场景引用.json，2 步）、包仓库.制品生产链能力面（本包 验证场景引用.json，7 步）、包仓库.制品生产主链正向成功（本包 验证场景引用.json，6 步）、包仓库.客户端制品链正向（本包 验证场景引用.json，5 步）
