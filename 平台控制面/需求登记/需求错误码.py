"""需求登记包的稳定错误码映射：中文域错误码 → 平台稳定码（唯一映射表）。

为什么单独一份（第 1 条 3 项「结果唯一即收口」）：
需求闸门的稳定码此前只散落在调用方（`平台控制面/统一入口.py` 的 4 处、
`开发工具/契约编译/消费者契约.py` 的错误码表），能力面看不到它们、能力 id 里也含不了
「需求」。包化后把两份口径的**对应关系**收在本表：本包能力一律返回中文域错误码
（与 `平台控制面` 其余包同词表），调用方按本表映射为稳定码即可，不另立第二套判定。

口径（2026-09-17 B-16 收口最后一批：**拆开原先并成一条的「不存在/未确认」**）：
- `需求id不能为空` → `REQUIREMENT_REQUIRED`（空需求id一律阻断，与统一入口 4 处同码）；
- `需求不存在` → `REQUIREMENT_NOT_FOUND`（目标需求快照**查不到**；此前误并入
  `REQUIREMENT_UNCONFIRMED`，与 `统一入口.py::_装配计划` 的真实返回「需求不存在」不符，本批拆开）；
- `需求未确认` → `REQUIREMENT_UNCONFIRMED`（需求**存在但未确认**，一律阻断开发与发布
  放行；与 `统一入口.py::_装配计划`/`::_创建组件`/`::_提交候选包` 的真实返回「需求未确认」逐字对齐）。
两条中文码与 `统一入口.py` 的实际返回逐字相同，不得再用旧文案「需求不存在或未确认」
把两者并成一条。

**读点（B-17 收口「死表」）**：本表由 `实现/能力入口.py` 的 `_稳定码` / `_失败`
实际读取，失败结果的 `详情.稳定错误码` 一律由本表生成（函数内不得再写稳定码字面量）。
修前 `实现/能力入口.py:28` 只是 import 了本表、全文件 0 处使用，稳定码全写死在分支里，
实测改本表行为逐字不变 —— 即本表当时是摆设；现改为唯一读点，改本表必然改变返回值。

其余条目逐字保持收敛前行为，不改。
"""

from __future__ import annotations

稳定错误码映射: dict[str, str] = {
    "": "",
    "参数不合法": "INVALID_PARAMETER",
    "需求id不能为空": "REQUIREMENT_REQUIRED",
    "需求不存在": "REQUIREMENT_NOT_FOUND",
    "需求未确认": "REQUIREMENT_UNCONFIRMED",
    "需求登记失败": "REQUIREMENT_REGISTER_FAILED",
    "需求查询失败": "REQUIREMENT_QUERY_FAILED",
    "装配计划生成失败": "ASSEMBLY_PLAN_FAILED",
    "工作包登记失败": "WORK_PACKAGE_REGISTER_FAILED",
}

__all__ = ["稳定错误码映射"]
