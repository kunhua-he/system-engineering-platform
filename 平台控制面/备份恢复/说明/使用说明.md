# 备份恢复 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 包id：平台控制面.备份恢复
- 版本：1.1.0

> 本文是本包的**调用权威**：能力、参数、返回结构、错误码与调用示例。
> 设计与边界（为什么这样切、管什么、不管什么、失败如何表达）以同目录 `设计说明.md` 为准。

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 平台控制面.备份恢复.声明备份契约 | 声明备份契约 | 1.0.0 | {'类型': '结果型', '值结构': {'契约表': '字典型，类名 → {恢复点, 恢复时限秒, 保留周期, 恢复顺序, 校验方法名}', '类数': '整数型，契约覆盖的数据类数（三类）'}} | 声明三类权威数据的备份契约（只读，不触碰磁盘）：每类给出恢复点、恢复时限秒、保留周期与恢复顺序，校验方法以方法名（校验方法名）对外，保证返回值可 JSON 化。 |
| 平台控制面.备份恢复.执行备份 | 执行备份 | 1.0.0 | {'类型': '结果型', '值结构': {'清单': '字典型，备份清单：快照目录、备份时间、契约（不含校验方法）与三类文件条目（文件/大小/摘要，缺失类带 缺失 标记）'}} | 真实备份三类权威数据到时间戳快照子目录并落 备份清单.json：WAL 先合并、逐类真实复制与摘要计算；数据源缺失的类在清单里如实标记缺失（不伪造、不补默认值），该缺失会被 校验备份 判失败。 |
| 平台控制面.备份恢复.校验备份 | 校验备份 | 1.0.0 | {'类型': '结果型', '值结构': {'通过': '逻辑型，true = 三类数据校验全部通过', '原因': '文本型，通过时的结论；不通过时为逐类失败原因汇总（逐字沿用实现层原文）'}} | 校验备份：定位清单 → 逐类内容摘要核对 → 逐类真实校验方法（sqlite integrity_check / 证据哈希重放 / 制品逐文件 sha256）。通过=False 是校验结论（正常答复），备份目录内无任何备份则失败码为 备份不存在。 |
| 平台控制面.备份恢复.恢复备份 | 恢复备份 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型，true = 三类数据全部恢复且逐类校验通过；**空表（没有可核验对象）时为 false**（空集 ≠ 通过，哲学 1.4），此时属未核验态而非失败', '状态': '文本型，恢复结论三态：通过 / 未核验 / 失败。未核验 = 结果表为空（没有可核验对象），既不是通过也不是失败', '是否未核验': '逻辑型，true = 本次恢复没有可核验对象（结果条数 0），结论为未核验', '结果条数': '整数型，实际参与判定的逐类结果条数；0 即未核验', '状态说明': '文本型，未核验时的口径说明（空表 = 没有可核验对象，属未核验，不是失败也不是通过）；其余态为空串', '恢复顺序': '列表型，实际执行的恢复类名顺序（按契约 恢复顺序 升序）', '结果': '列表型，每类一项 {类, 成功, 原因}', '目标目录': '文本型，实际恢复到的绝对路径', '回退警告': '文本型，非空 = 检测到版本回退（旧快照将覆盖更新的状态）'}} | 按契约恢复顺序把三类数据恢复到目标目录，恢复后逐类真实校验；目标目录现有权威状态比本备份更新时带回退警告（旧快照重放必须被调用方看到）。备份目录内无任何备份则失败码为 备份不存在。 |
| 平台控制面.备份恢复.查询备份源根 | 查询备份源根 | 1.0.0 | {'类型': '结果型', '值结构': {'类根表': '列表型，每项为 {类, 根目录, 存在, 来自}：类=权威状态/证据账本/包仓库；根目录=该类生效的源目录绝对路径；存在=该目录当前是否存在（不创建）；来自=声明该根的落点或「调用方显式传入」', '类数': '整数型，回报的数据类数（三类）'}} | 查询备份源根（只读自省，不建目录、不写盘）：逐类回报生效的源根目录、该目录当前是否存在、以及该根由谁声明。用途：调用方与门禁不必猜「三类数据住在哪」——三类不共用一个根（状态/证据在平台状态的默认存储目录，包仓库在包仓库包的制品仓库缓存根），把口径做成可查、可测、可回归的能力，根漂移当场暴露。 |

## 平台控制面.备份恢复.声明备份契约（声明备份契约）

**说明**：声明三类权威数据的备份契约（只读，不触碰磁盘）：每类给出恢复点、恢复时限秒、保留周期与恢复顺序，校验方法以方法名（校验方法名）对外，保证返回值可 JSON 化。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 覆盖表 | 字典型 | 否 | None | 契约盖写对象：可给 恢复点/恢复时限秒/保留周期，也可按类名给一个对象做该类盖写；不是对象判 参数不合法 |
| 存储根目录 | 文本型 | 否 |  | 备份源数据根目录（权威状态.db / 制品 所在）；留空即逐类回落各自的权威宿主（声明契约不读写该目录，仅作契约身份） |

**返回结构**：{'类型': '结果型', '值结构': {'契约表': '字典型，类名 → {恢复点, 恢复时限秒, 保留周期, 恢复顺序, 校验方法名}', '类数': '整数型，契约覆盖的数据类数（三类）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.备份恢复.声明备份契约(覆盖表=值, 存储根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：备份恢复.契约声明（本包 验证场景引用.json，1 步）、备份恢复.备份与校验（本包 验证场景引用.json，2 步）、备份恢复.按序恢复（本包 验证场景引用.json，1 步）、备份恢复.查询源根（本包 验证场景引用.json，1 步）

## 平台控制面.备份恢复.执行备份（执行备份）

**说明**：真实备份三类权威数据到时间戳快照子目录并落 备份清单.json：WAL 先合并、逐类真实复制与摘要计算；数据源缺失的类在清单里如实标记缺失（不伪造、不补默认值），该缺失会被 校验备份 判失败。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 备份目录 | 文本型 | 是 | None | 备份落点根目录（其下新建 快照_<时间戳>/），必填；空值判 参数不合法 |
| 存储根目录 | 文本型 | 否 |  | 备份源数据根目录（权威状态.db / 制品 所在）；留空即逐类回落各自的权威宿主 |

**返回结构**：{'类型': '结果型', '值结构': {'清单': '字典型，备份清单：快照目录、备份时间、契约（不含校验方法）与三类文件条目（文件/大小/摘要，缺失类带 缺失 标记）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.备份恢复.执行备份(备份目录=值, 存储根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：备份恢复.契约声明（本包 验证场景引用.json，1 步）、备份恢复.备份与校验（本包 验证场景引用.json，2 步）、备份恢复.按序恢复（本包 验证场景引用.json，1 步）、备份恢复.查询源根（本包 验证场景引用.json，1 步）

## 平台控制面.备份恢复.校验备份（校验备份）

**说明**：校验备份：定位清单 → 逐类内容摘要核对 → 逐类真实校验方法（sqlite integrity_check / 证据哈希重放 / 制品逐文件 sha256）。通过=False 是校验结论（正常答复），备份目录内无任何备份则失败码为 备份不存在。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 备份目录 | 文本型 | 是 | None | 备份目录（含 备份清单.json，或其下有时间戳快照子目录），必填；空值判 参数不合法 |
| 存储根目录 | 文本型 | 否 |  | 备份源数据根目录（权威状态.db / 制品 所在）；留空即逐类回落各自的权威宿主（校验以清单里的快照目录为准） |

**返回结构**：{'类型': '结果型', '值结构': {'通过': '逻辑型，true = 三类数据校验全部通过', '原因': '文本型，通过时的结论；不通过时为逐类失败原因汇总（逐字沿用实现层原文）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 备份不存在 |

**调用示例**：

```python
平台控制面.备份恢复.校验备份(备份目录=值, 存储根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：备份恢复.契约声明（本包 验证场景引用.json，1 步）、备份恢复.备份与校验（本包 验证场景引用.json，2 步）、备份恢复.按序恢复（本包 验证场景引用.json，1 步）、备份恢复.查询源根（本包 验证场景引用.json，1 步）

## 平台控制面.备份恢复.恢复备份（恢复备份）

**说明**：按契约恢复顺序把三类数据恢复到目标目录，恢复后逐类真实校验；目标目录现有权威状态比本备份更新时带回退警告（旧快照重放必须被调用方看到）。备份目录内无任何备份则失败码为 备份不存在。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 备份目录 | 文本型 | 是 | None | 备份目录（含 备份清单.json，或其下有时间戳快照子目录），必填；空值判 参数不合法 |
| 目标目录 | 文本型 | 是 | None | 恢复目标目录（不存在即创建），必填；空值判 参数不合法 |
| 存储根目录 | 文本型 | 否 |  | 备份源数据根目录（权威状态.db / 制品 所在）；留空即逐类回落各自的权威宿主（恢复以清单里的快照目录为准） |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型，true = 三类数据全部恢复且逐类校验通过；**空表（没有可核验对象）时为 false**（空集 ≠ 通过，哲学 1.4），此时属未核验态而非失败', '状态': '文本型，恢复结论三态：通过 / 未核验 / 失败。未核验 = 结果表为空（没有可核验对象），既不是通过也不是失败', '是否未核验': '逻辑型，true = 本次恢复没有可核验对象（结果条数 0），结论为未核验', '结果条数': '整数型，实际参与判定的逐类结果条数；0 即未核验', '状态说明': '文本型，未核验时的口径说明（空表 = 没有可核验对象，属未核验，不是失败也不是通过）；其余态为空串', '恢复顺序': '列表型，实际执行的恢复类名顺序（按契约 恢复顺序 升序）', '结果': '列表型，每类一项 {类, 成功, 原因}', '目标目录': '文本型，实际恢复到的绝对路径', '回退警告': '文本型，非空 = 检测到版本回退（旧快照将覆盖更新的状态）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 备份不存在 |

**调用示例**：

```python
平台控制面.备份恢复.恢复备份(备份目录=值, 目标目录=值, 存储根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：备份恢复.契约声明（本包 验证场景引用.json，1 步）、备份恢复.备份与校验（本包 验证场景引用.json，2 步）、备份恢复.按序恢复（本包 验证场景引用.json，1 步）、备份恢复.查询源根（本包 验证场景引用.json，1 步）

## 平台控制面.备份恢复.查询备份源根（查询备份源根）

**说明**：查询备份源根（只读自省，不建目录、不写盘）：逐类回报生效的源根目录、该目录当前是否存在、以及该根由谁声明。用途：调用方与门禁不必猜「三类数据住在哪」——三类不共用一个根（状态/证据在平台状态的默认存储目录，包仓库在包仓库包的制品仓库缓存根），把口径做成可查、可测、可回归的能力，根漂移当场暴露。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 存储根目录 | 文本型 | 否 |  | 显式传入时该根为三类共用根（夹具/演练/测试构造自足数据根用）；留空即逐类回报各自的权威宿主 |

**返回结构**：{'类型': '结果型', '值结构': {'类根表': '列表型，每项为 {类, 根目录, 存在, 来自}：类=权威状态/证据账本/包仓库；根目录=该类生效的源目录绝对路径；存在=该目录当前是否存在（不创建）；来自=声明该根的落点或「调用方显式传入」', '类数': '整数型，回报的数据类数（三类）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
平台控制面.备份恢复.查询备份源根(存储根目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：备份恢复.契约声明（本包 验证场景引用.json，1 步）、备份恢复.备份与校验（本包 验证场景引用.json，2 步）、备份恢复.按序恢复（本包 验证场景引用.json，1 步）、备份恢复.查询源根（本包 验证场景引用.json，1 步）
