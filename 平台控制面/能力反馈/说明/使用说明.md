# 能力反馈 使用说明
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 包id：平台控制面.能力反馈
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 平台控制面.能力反馈.登记能力反馈 | 登记能力反馈 | 1.0.0 | {'类型': '结果型', '值结构': {'反馈id': '文本型，本能力生成的反馈编号', '状态': '文本型，登记后的反馈状态（已登记 或 命中的既有状态）', '是否重复': '逻辑型，true = 同一去重键已登记，未新增行'}} | 登记一条能力反馈（运行时诊断）：校验必填字段、契约版本格式、能力存在与请求摘要大小，按去重键保证同一反馈只落一条事实，并生成内容摘要用于幂等判据。 |
| 平台控制面.能力反馈.查询能力反馈 | 查询能力反馈 | 1.0.0 | {'类型': '结果型', '值结构': {'记录表': '列表型，公开字段的反馈记录数组（不含去重键与内容摘要）', '数量': '整数型，本次返回的记录条数'}} | 查询能力反馈记录（只读）：按反馈id/状态/能力id 过滤，返回公开字段记录表与数量，去重键与内容摘要不外泄。 |
| 平台控制面.能力反馈.迁移能力反馈状态 | 迁移能力反馈状态 | 1.0.0 | {'类型': '结果型', '值结构': {'反馈id': '文本型，被迁移的反馈编号', '状态': '文本型，迁移后的新状态', '处理者': '文本型，本次状态迁移的处理者'}} | 迁移一条能力反馈的状态（8 态状态机）：按当前状态校验目标状态是否允许，已修复必须带验证证据与发布制品，成功后追加状态证据；并发写用状态版本条件更新拦截冲突。 |

## 平台控制面.能力反馈.登记能力反馈（登记能力反馈）

**说明**：登记一条能力反馈（运行时诊断）：校验必填字段、契约版本格式、能力存在与请求摘要大小，按去重键保证同一反馈只落一条事实，并生成内容摘要用于幂等判据。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 请求 | 字典型 | 是 | None | 反馈请求对象：来源系统/来源版本/请求id/能力id/契约版本/错误码/错误说明 必填，HTTP状态码、优先级、请求摘要、响应摘要、复现标识 可选 |
| 存储目录 | 文本型 | 否 |  | 反馈状态库所在目录；留空取平台默认目录（工程缓存/平台控制面） |

**返回结构**：{'类型': '结果型', '值结构': {'反馈id': '文本型，本能力生成的反馈编号', '状态': '文本型，登记后的反馈状态（已登记 或 命中的既有状态）', '是否重复': '逻辑型，true = 同一去重键已登记，未新增行'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 能力不存在 |
| 幂等键冲突 |
| 反馈写入失败 |
| 能力校验不可用 |

**调用示例**：

```python
平台控制面.能力反馈.登记能力反馈(请求=值, 存储目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：能力反馈.登记与查询闭环（本包 验证场景引用.json，2 步）、能力反馈.状态迁移按状态机校验（本包 验证场景引用.json，1 步）、能力反馈.状态迁移正向成功（本包 验证场景引用.json，1 步）

## 平台控制面.能力反馈.查询能力反馈（查询能力反馈）

**说明**：查询能力反馈记录（只读）：按反馈id/状态/能力id 过滤，返回公开字段记录表与数量，去重键与内容摘要不外泄。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 反馈id | 文本型 | 否 |  | 反馈编号过滤条件；留空表示不限 |
| 状态 | 文本型 | 否 |  | 状态过滤条件（已登记/已确认/修复中/待验证/已修复/无法复现/重复反馈/已关闭）；留空表示不限 |
| 能力id | 文本型 | 否 |  | 能力id 过滤条件；留空表示不限 |
| 限制 | 整数型 | 否 | 50 | 返回条数上限，1 到 100 的整数 |
| 存储目录 | 文本型 | 否 |  | 反馈状态库所在目录；留空取平台默认目录（工程缓存/平台控制面） |

**返回结构**：{'类型': '结果型', '值结构': {'记录表': '列表型，公开字段的反馈记录数组（不含去重键与内容摘要）', '数量': '整数型，本次返回的记录条数'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 反馈查询失败 |

**调用示例**：

```python
平台控制面.能力反馈.查询能力反馈(反馈id=值, 状态=值, 能力id=值, 限制=值, 存储目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：能力反馈.登记与查询闭环（本包 验证场景引用.json，2 步）、能力反馈.状态迁移按状态机校验（本包 验证场景引用.json，1 步）、能力反馈.状态迁移正向成功（本包 验证场景引用.json，1 步）

## 平台控制面.能力反馈.迁移能力反馈状态（迁移能力反馈状态）

**说明**：迁移一条能力反馈的状态（8 态状态机）：按当前状态校验目标状态是否允许，已修复必须带验证证据与发布制品，成功后追加状态证据；并发写用状态版本条件更新拦截冲突。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 反馈id | 文本型 | 是 | None | 待迁移的反馈编号 |
| 请求 | 字典型 | 是 | None | 状态请求对象：状态/处理者/原因 必填，关联提交、验证证据、发布制品 可选；未知字段一律拒绝 |
| 存储目录 | 文本型 | 否 |  | 反馈状态库所在目录；留空取平台默认目录（工程缓存/平台控制面） |

**返回结构**：{'类型': '结果型', '值结构': {'反馈id': '文本型，被迁移的反馈编号', '状态': '文本型，迁移后的新状态', '处理者': '文本型，本次状态迁移的处理者'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 反馈不存在 |
| 状态迁移不允许 |
| 状态版本冲突 |
| 已修复必须提供验证证据和发布制品 |

**调用示例**：

```python
平台控制面.能力反馈.迁移能力反馈状态(反馈id=值, 请求=值, 存储目录=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：能力反馈.登记与查询闭环（本包 验证场景引用.json，2 步）、能力反馈.状态迁移按状态机校验（本包 验证场景引用.json，1 步）、能力反馈.状态迁移正向成功（本包 验证场景引用.json，1 步）
