"""平台控制面反馈公开服务（正式包包级中文入口）。

两类反馈、一份机制、同一事实源（`权威状态.db`）：

- `能力反馈服务`：运行时诊断反馈（能力id + 契约版本 + 错误码 + 8 态状态机 + `能力反馈` 专表）。
- `开发反馈服务`：开发过程反馈（开工id + 五字段 + 反馈门禁 + 升级候选 → 需求表升级池），
  事实落 `证据` 账本（类型 `开发反馈`），零 schema 变更。
- `反馈语义.py`：两类反馈共用的机制（文本/脱敏/摘要/内容摘要）——第 1 条 3 项「结果唯一即收口」。
- `开工id校验.py`：开工id 有效性判定（格式 + 运行库协作状态域登记，fail-closed），
  补掉旧 `mcp_feedback` 的直接用原始 work_id 缺口。

正式包边界（本目录是 `平台控制面` 正式根下的治理能力面）：
- 调用方（运行核心/统一网关、模块、薄壳）**只经唯一能力调用入口**按能力 id 调用，
  不得 import 实现，也不得深入 `实现/` 目录；
- 本入口的 `注册能力(注册表)` 由加载器装配时调用，注册三条运行时诊断反馈能力：
  `登记能力反馈` / `查询能力反馈` / `迁移能力反馈状态`（机器可读定义见同目录
  `能力定义.json`，实现见 `实现/能力入口.py`）。
"""

from 公共契约.基础类型.逻辑类型 import 真, 假
from 平台控制面.能力反馈.反馈服务 import 能力反馈服务
from 平台控制面.能力反馈.开发反馈服务 import (
    开发反馈服务,
    旧工具映射,
    稳定错误码映射,
    能力表,
    证据类型_升级候选,
    证据类型_开发反馈,
    调用能力,
    默认回退文件,
)
from 平台控制面.能力反馈.开工id校验 import (
    协作状态登记源,
    开工id校验器,
    归一化,
    格式合法,
    默认查询运行态,
    默认校验器,
)
from 平台控制面.能力反馈.实现.能力入口 import (
    登记能力反馈,
    查询能力反馈,
    迁移能力反馈状态,
    设置能力存在判定,
    默认存储目录,
)

__all__ = [
    "协作状态登记源",
    "开发反馈服务",
    "开工id校验器",
    "归一化",
    "旧工具映射",
    "格式合法",
    "稳定错误码映射",
    "能力反馈服务",
    "能力表",
    "证据类型_升级候选",
    "证据类型_开发反馈",
    "调用能力",
    "默认回退文件",
    "默认查询运行态",
    "默认校验器",
    "登记能力反馈",
    "查询能力反馈",
    "迁移能力反馈状态",
    "设置能力存在判定",
    "默认存储目录",
    "注册能力",
]

包id = "平台控制面.能力反馈"



def _校验参数名序(参数契约表: dict[str, list[dict]],
                  注册参数名序: dict[str, list[str]]) -> None:
    """装配期闸门：注册参数名序必须逐字等于契约名序，否则本包装配失败。

    为什么要有它：注册处一旦漏一个参数（或顺序写反），网关的必填校验就与契约分叉
    ——那正是本包 2026-09-18 被修掉的缺陷形态。宁可装配期就 raise，也不静默漏参。
    """
    for 能力id, 契约参数 in 参数契约表.items():
        注册名序 = 注册参数名序.get(能力id)
        if 注册名序 is None:
            raise ValueError(f"能力 {能力id} 有契约但注册处未声明参数名序")
        契约名序 = [参数.get("名称") for 参数 in 契约参数]
        if 契约名序 != list(注册名序):
            raise ValueError(
                f"能力 {能力id} 注册参数名序 {list(注册名序)} 与契约参数名序 {契约名序} "
                "不一致：参数事实源只有 能力契约/参数契约.json 一处（哲学 12.1），"
                "入口不得另立一份")

def 注册能力(注册表) -> None:
    """由加载器调用，向能力注册表注册本治理面的公开能力。

    注册参数与 `能力定义.json` 逐条对应（参数名与顺序一致）；
    能力实现只做边界校验与结果归一，反馈语义仍在同目录反馈服务里。
    """
# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 参数契约镜像（**函数内字面量**；本块由「丢参」缺陷修复注入，勿删）
    #
    # 缺陷溯源（2026-09-18）：本入口原先手工重建注册参数声明（只写 名称/类型），把
    # `必填` 丢掉；而网关唯一校验点 `运行核心/统一网关/类型规格.py` 的判据是
    # `项.get("必填") is 真` → `None is 真` = 假 → 必填校验静默失效：少传参数不再被
    # 网关拦下，直接落进实现体抛 TypeError，用户拿到 HTTP 500 而不是干净的 400
    # 「缺少必填参数 X」。修法：参数声明从 `能力契约/参数契约.json` **整条透传**
    # （`参数=_参数契约表[能力id]`），入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：
    #  ① `_参数契约表` 是字面量、键是常量，`开发工具/契约编译/漂移检测` 的 AST 读取器
    #     （`_静态字面量` 的 `ast.Subscript` 分支）能解析出**整条参数含 `必填`** ——
    #     改前那处「参数口径未解析」不因本次修复恶化，反而把读不到的 `必填` 读到了。
    #     放在函数内是因为该读取器只收 `注册能力` 函数体内的赋值（`_扫描注册语句`）。
    #  ② 「裸布尔」防回潮门禁**已把 `必填`/`默认值` 计入违规**（本文件已迁为 真/假），改回裸 True/False 即判红。
    # 表内布尔位写**中文 `真`／`假`**（正式口径，真源 `公共契约/基础类型/逻辑类型.py`）；
    # 本文件顶部已 import 真/假，漂移检测读取器（`_导入常量绑定`）能静态解析出参数表，
    # （契约写了的键才留，没写的不补），不给漂移检测造出「注册声明了契约没声明的默认值」。
    # 为什么按下标取而不是包一层 `_参数声明(能力id)`：包一层会让 AST 一律判「不可静态
    # 判定」，等于拿门禁覆盖率换干净（不可接受）。名序护盾由 `_注册参数名序` +
    # `_校验参数名序()` 承担，装配期逐条对契约核名序，不一致即 raise。
    _参数契约表 = {
        "平台控制面.能力反馈.查询能力反馈": [
            {"名称": "反馈id", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "状态", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "能力id", "类型": "文本型", "必填": 假, "默认值": ""},
            {"名称": "限制", "类型": "整数型", "必填": 假, "默认值": 50},
            {"名称": "存储目录", "类型": "文本型", "必填": 假, "默认值": ""},
        ],
        "平台控制面.能力反馈.登记能力反馈": [
            {"名称": "请求", "类型": "字典型", "必填": 真, "默认值": None},
            {"名称": "存储目录", "类型": "文本型", "必填": 假, "默认值": ""},
        ],
        "平台控制面.能力反馈.迁移能力反馈状态": [
            {"名称": "反馈id", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "请求", "类型": "字典型", "必填": 真, "默认值": None},
            {"名称": "存储目录", "类型": "文本型", "必填": 假, "默认值": ""},
        ],
    }
    _注册参数名序 = {
        "平台控制面.能力反馈.查询能力反馈": ["反馈id", "状态", "能力id", "限制", "存储目录"],
        "平台控制面.能力反馈.登记能力反馈": ["请求", "存储目录"],
        "平台控制面.能力反馈.迁移能力反馈状态": ["反馈id", "请求", "存储目录"],
    }
    _校验参数名序(_参数契约表, _注册参数名序)
# ===== 参数声明透传 结束 =====
    from 公共契约.能力契约.契约 import 能力实现

    注册表.注册(能力实现(
        能力id="平台控制面.能力反馈.登记能力反馈",
        包id=包id,
        实现函数=登记能力反馈,
        参数=_参数契约表["平台控制面.能力反馈.登记能力反馈"],
        返回="结果",
        说明="登记一条能力反馈（运行时诊断）：校验必填字段与能力存在，按去重键幂等落一条事实。",
        版本="1.0.0",
        提供者id=包id,
        提供者版本="1.0.0",
    ))
    注册表.注册(能力实现(
        能力id="平台控制面.能力反馈.查询能力反馈",
        包id=包id,
        实现函数=查询能力反馈,
        参数=_参数契约表["平台控制面.能力反馈.查询能力反馈"],
        返回="结果",
        说明="查询能力反馈记录（只读）：按反馈id/状态/能力id 过滤，返回公开字段记录表。",
        版本="1.0.0",
        提供者id=包id,
        提供者版本="1.0.0",
    ))
    注册表.注册(能力实现(
        能力id="平台控制面.能力反馈.迁移能力反馈状态",
        包id=包id,
        实现函数=迁移能力反馈状态,
        参数=_参数契约表["平台控制面.能力反馈.迁移能力反馈状态"],
        返回="结果",
        说明="迁移一条能力反馈的状态（8 态状态机）：校验迁移合法性并追加状态证据。",
        版本="1.0.0",
        提供者id=包id,
        提供者版本="1.0.0",
    ))
