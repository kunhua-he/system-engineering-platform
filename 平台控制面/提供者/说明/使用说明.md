# 提供者 使用说明

- 包id：平台控制面.提供者
- 版本：1.1.0

> 本文是本包的**调用权威**：能力、参数、返回结构、错误码与调用示例。
> 设计与边界（为什么这样切、管什么、不管什么、失败如何表达）以同目录 `设计说明.md` 为准。

## 包说明

平台控制面·提供者面：真实系统能力提供（资源采样 / 整机内存水位 / 治理面依赖审计 / 控制面只读指针）。对外只保留 采样资源 / 采样整机内存 / 读取整机内存水位 / 整机内存水位超限 / 水位采样定时器 / 取消登记快照 / 审计平台控制面 / 只读激活指针 八条能力；动态库调用与提供者注册表由子包 动态库提供者 / 注册表 各自声明，本包不重复登记。

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 平台控制面.提供者.采样资源 | 采样资源 | 1.0.0 | 结果型（内存峰值MB/文件句柄数/临时空间字节/线程峰值/并发峰值/队列峰值/采样时间） | 真实采样当前进程资源并合并资源监督执行单元峰值；**采样失败如实标注，不伪装数值**。 |
| 平台控制面.提供者.审计平台控制面 | 审计平台控制面 | 1.0.0 | 结果型（审计文件数/违规数/违规清单/无违规） | 治理面专属依赖防火墙静态审计：动态导入/反射导入/深入实现目录/直连外部服务四类规则，逐文件 AST 判定。 |
| 平台控制面.提供者.只读激活指针 | 只读激活指针 | 1.0.0 | 结果型（指针、是否命中） | 控制面不可用时的调用面只读视图：只读打开权威状态库取激活指针，**未命中如实返回 `是否命中=false`**。 |
| 平台控制面.提供者.采样整机内存 | 采样整机内存 | 1.0.0 | 结果型（物理字节/已用字节/可用字节/使用率） | 读本机（整机）物理内存占用：**立即采一次并刷新水位缓存**；单一职责、不遍历目录、不读句柄，纯系统调用。高频监控请用 读取整机内存水位。 |
| 平台控制面.提供者.读取整机内存水位 | 读取整机内存水位 | 1.0.0 | 结果型（支持/物理字节/已用字节/可用字节/占用率/快照年龄秒/快照陈旧/采样次数/采样失败次数/采样周期秒/快照陈旧阈值秒/依据/原因） | 读整机内存水位快照（定时器 30 秒采一次，**本能力只读缓存、零系统调用**）；资源闸门与监控器的唯一读入口。 |
| 平台控制面.提供者.整机内存水位超限 | 整机内存水位超限 | 1.0.0 | 结果型（超限/说明/阈值/占用率/物理字节/已用字节/可用字节/快照年龄秒/采样周期秒） | 整机内存水位判定（读缓存快照，不采样）：**80% 水位闸门的公开判据入口**，与能力调用准入用同一份判定，让「闸门为什么拦我」可以被直接问出来。 |
| 平台控制面.提供者.水位采样定时器 | 水位采样定时器 | 1.0.0 | 结果型（动作/本次启动/本次停止/周期秒/采样次数/采样失败次数/快照年龄秒/快照陈旧/支持） | 整机内存水位采样定时器的启动/停止/查状态（有界：全局只允许一条采样线程）。采样是低频事实、判据是高频读，本能力让 HTML 监控面板能直接问到并控制采样器。 |
| 平台控制面.提供者.取消登记快照 | 取消登记快照 | 1.0.0 | 结果型（在册条数/在册上限/在册/留痕条数/留痕） | 调用取消登记快照：在册调用与取消留痕的有界只读视图（**断连即回收**的可观测面）。验收「客户端断开后子进程被整组回收」时据此核对留痕条数与原因，而不是只看一句成功。 |

## 平台控制面.提供者.采样资源（采样资源）

**说明**：真实采样当前进程资源（内存峰值/文件句柄/临时空间）并合并资源监督执行单元峰值；采样失败如实标注，不伪装数值。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 临时目录 | 文本型 | 否 |  | 临时空间采样根目录；留空取系统临时目录，给出但不可读即明确失败 |

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 内存峰值MB | 双精度数型，getrusage 真实峰值（macOS 字节/Linux KB 已换算） |
| 文件句柄数 | 整数型，当前进程真实句柄数（/proc/self/fd 或 /dev/fd） |
| 临时空间字节 | 整数型，采样根目录内文件字节合计 |
| 线程峰值 | 整数型，资源监督执行单元线程峰值 |
| 并发峰值 | 整数型，资源监督执行单元并发峰值 |
| 队列峰值 | 整数型，资源监督执行单元队列峰值 |
| 采样时间 | 文本型，本地时区时间戳 |

**实测真实返回**（2026-09-16 进程内装配真调，装配能力数 645）：

```json
{"内存峰值MB": 145.41, "文件句柄数": 10, "临时空间字节": 510696,
 "线程峰值": 0, "并发峰值": 0, "队列峰值": 0, "采样时间": "2026-09-16 23:58:30"}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 采样失败 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.采样资源", {"临时目录": ""})
```

## 平台控制面.提供者.审计平台控制面（审计平台控制面）

**说明**：治理面专属依赖防火墙静态审计：动态导入/反射导入/深入实现目录/直连外部服务四类规则，逐文件 AST 判定并返回违规清单。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目标目录 | 文本型 | 否 |  | 被审计目录；留空取平台控制面目录（本面默认治理对象），给出但不可读即明确失败 |

**返回结构**：结果型

| 值字段 | 说明 |
| --- | --- |
| 审计文件数 | 整数型，本次真实解析的 .py 文件数 |
| 违规数 | 整数型，违规条数 |
| 违规清单 | 列表型，每条形如 [来源层→目标] 规则 @ 文件:行号 |
| 无违规 | 逻辑型，true = 违规数为 0 |

**实测真实返回**：

```json
{"审计文件数": 86, "违规数": 58,
 "违规清单": ["[平台控制面→平台控制面.包仓库.实现.能力入口] 跨包深入实现目录 @ 平台控制面/包仓库/__init__.py:27"],
 "无违规": false}
```

> **口径提示（勿误读）**：违规数非 0 **不等于**平台控制面坏了。第三类规则「任何导入不得深入实现目录」
> 与正式包的标准入口写法（入口 `__init__.py` 依赖同包 `实现/`）在当前口径下相互冲突，兄弟面
> （`平台状态`/`发布管理`/`授权`/`备份恢复`）同样命中——这是**规则与入口规范之间的口径问题**，
> 详见同目录 `设计说明.md`「已知边界」。发布前请以发布门禁的现场输出为准，本能力只给原始清单。

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不可用 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.审计平台控制面", {})
```

## 平台控制面.提供者.只读激活指针（只读激活指针）

**说明**：控制面不可用时的调用面只读视图：只读打开权威状态库取激活指针（不启动统一能力服务、不执行结构迁移、未命中如实返回）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 存储目录 | 文本型 | 否 |  | 平台权威状态库所在目录；留空取平台默认目录（工程缓存/平台控制面），相对路径按平台根解析 |
| 指针id | 文本型 | 是 | None | 激活指针id（发布指针主键） |

**返回结构**：结果型

| 值字段 | 说明 |
| --- | --- |
| 指针 | 字典型或空值型，命中时为 指针id/目标/版本/栅栏令牌/状态，未命中为 null |
| 是否命中 | 逻辑型，true = 该激活指针存在 |

**实测真实返回**（查询一个未登记的指针id）：

```json
{"指针": null, "是否命中": false}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 存储目录不可用 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.只读激活指针", {"存储目录": "", "指针id": "平台客户端"})
```

## 平台控制面.提供者.采样整机内存（采样整机内存）

**说明**：读本机（整机）物理内存占用：物理总量/已用/可用/使用率。**立即采一次并刷新水位缓存**；单一职责、不遍历目录、不读句柄，实测 14.2 微秒（纯系统调用，无子进程）。高频监控请用 读取整机内存水位（读定时器快照，零系统调用）。

**参数表**：无（无入参）。

**返回结构**：结果型（统一结果：成功/值/错误码/错误说明/可重试/详细信息）

| 值字段 | 说明 |
| --- | --- |
| 物理字节 | 整数型，物理内存总量 |
| 已用字节 | 整数型，物理总量减可用 |
| 可用字节 | 整数型，free+inactive+purgeable（与 macOS memory_pressure 的 available 同口径） |
| 使用率 | 双精度数型，0 到 1 的已用占比 |

**实测真实返回**（2026-09-21 经 MCP 直调唯一网关，耗时 0.98 毫秒）：

```json
{"物理字节": 103079215104, "已用字节": 30388355072, "可用字节": 72690860032, "使用率": 0.2948}
```

**错误码表**：

| 错误码 |
| --- |
| 内存读数不可用 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.采样整机内存", {})
```

## 平台控制面.提供者.读取整机内存水位（读取整机内存水位）

**说明**：读整机内存水位快照（定时器 30 秒采一次，本能力只读缓存、零系统调用）：物理/已用/可用字节、占用率、快照年龄秒、快照陈旧、采样次数与失败次数。资源闸门与监控器的唯一读入口。

**参数表**：无（无入参）。

**返回结构**：结果型

| 值字段 | 说明 |
| --- | --- |
| 支持 | 逻辑型，快照是否可用 |
| 物理字节 | 整数型，物理内存总量 |
| 已用字节 | 整数型，物理总量减可用 |
| 可用字节 | 整数型，free+inactive+purgeable |
| 占用率 | 双精度数型，0 到 1 的已用占比 |
| 快照年龄秒 | 双精度数型，快照距现在多少秒（单调时钟） |
| 快照陈旧 | 逻辑型，年龄是否超过 90 秒（3 个采样周期） |
| 采样次数 | 整数型，定时器累计采样次数 |
| 采样失败次数 | 整数型，累计失败次数 |
| 采样周期秒 | 双精度数型，定时器周期（当前 30 秒） |
| 快照陈旧阈值秒 | 双精度数型，陈旧判据（当前 90 秒） |
| 依据 | 文本型，采样依据（macOS 系统调用名 / Linux 伪文件） |
| 原因 | 文本型，采样不可用时的原因；可用时为空 |

**实测真实返回**（2026-09-21 经 MCP 直调唯一网关，耗时 1.55 毫秒）：

```json
{"支持": true, "物理字节": 103079215104, "已用字节": 32201752576, "可用字节": 70877462528,
 "占用率": 0.3124, "快照年龄秒": 0.0, "快照陈旧": false, "采样次数": 2, "采样失败次数": 0,
 "采样周期秒": 30.0, "快照陈旧阈值秒": 90.0,
 "依据": "sysctlbyname hw.memsize + host_statistics64（free+inactive+purgeable；speculative 已含于 free）", "原因": ""}
```

**错误码表**：

| 错误码 |
| --- |
| 内存读数不可用 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.读取整机内存水位", {})
```

## 平台控制面.提供者.整机内存水位超限（整机内存水位超限）

**说明**：整机内存水位判定（读缓存快照，不采样）：返回 超限/说明/阈值/占用率 与快照事实。这是 80% 水位闸门的公开判据入口，与能力调用准入用同一份判定，让「闸门为什么拦我」可以被直接问出来。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 阈值 | 双精度数型 | 否 |  | 判定阈值（0 到 1）；留空取平台默认 0.8 |

**返回结构**：结果型

| 值字段 | 说明 |
| --- | --- |
| 超限 | 逻辑型，占用率是否超过阈值 |
| 说明 | 文本型，带实测占用率/阈值/快照年龄的中文说明 |
| 阈值 | 双精度数型，本次判定实际用的阈值 |
| 占用率 | 双精度数型，0 到 1 的已用占比 |
| 物理字节 | 整数型，物理内存总量 |
| 已用字节 | 整数型，物理总量减可用 |
| 可用字节 | 整数型，物理总量减已用 |
| 快照年龄秒 | 双精度数型，快照距现在多少秒 |
| 采样周期秒 | 双精度数型，定时器周期 |

**实测真实返回**（2026-09-21 经 MCP 直调唯一网关，阈值 0.8，耗时 0.71 毫秒）：

```json
{"超限": false, "说明": "整机内存水位正常（29.5% ≤ 80%，快照 0.02 秒前）", "阈值": 0.8,
 "占用率": 0.2948, "物理字节": 103079215104, "已用字节": 30388355072,
 "可用字节": 72690860032, "快照年龄秒": 0.02, "采样周期秒": 30.0}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 内存读数不可用 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.整机内存水位超限", {"阈值": 0.8})
```

## 平台控制面.提供者.水位采样定时器（水位采样定时器）

**说明**：整机内存水位采样定时器的启动/停止/查状态（有界：全局只允许一条采样线程）。采样是低频事实、判据是高频读，本能力让 HTML 监控面板能直接问到并控制采样器。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 动作 | 文本型 | 否 |  | 启动 / 停止 / 状态；留空取状态（并自动启动定时器） |
| 周期秒 | 双精度数型 | 否 |  | 采样周期（秒）；留空取平台默认 30 |

**返回结构**：结果型

| 值字段 | 说明 |
| --- | --- |
| 动作 | 文本型，本次实际执行的动作 |
| 本次启动 | 逻辑型，本次是否真的起了一条采样线程（幂等：已在跑返回 false） |
| 本次停止 | 逻辑型，本次是否真的停掉了采样线程 |
| 周期秒 | 双精度数型，本次生效的周期 |
| 采样次数 | 整数型，累计采样次数 |
| 采样失败次数 | 整数型，累计失败次数 |
| 快照年龄秒 | 双精度数型，快照距现在多少秒 |
| 快照陈旧 | 逻辑型，年龄是否超过陈旧阈值 |
| 支持 | 逻辑型，快照是否可用 |

**实测真实返回**（2026-09-21 经 MCP 直调唯一网关，动作=状态，耗时 0.62 毫秒）：

```json
{"动作": "状态", "本次启动": false, "本次停止": false, "周期秒": 30.0, "采样次数": 8,
 "采样失败次数": 0, "快照年龄秒": 0.032, "快照陈旧": false, "支持": true}
```

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.水位采样定时器", {"动作": "状态", "周期秒": 30.0})
```

## 平台控制面.提供者.取消登记快照（取消登记快照）

**说明**：调用取消登记快照：在册调用与取消留痕的有界只读视图（断连即回收的可观测面）。验收「客户端断开后子进程被整组回收」时据此核对留痕条数与原因，而不是只看一句成功。

**参数表**：无（无入参）。

**返回结构**：结果型

| 值字段 | 说明 |
| --- | --- |
| 在册条数 | 整数型，当前还在执行的已登记调用数 |
| 在册上限 | 整数型，在册表上限（512） |
| 在册 | 列表型，每项 {请求id, 已取消, 原因, 取消时刻} |
| 留痕条数 | 整数型，历史取消留痕条数 |
| 留痕 | 列表型，每项 {时间, 类型, 请求id, 说明}（有界 200 条，最新在后） |

**实测真实返回**（2026-09-21 经 MCP 直调唯一网关，无在册调用，耗时 0.59 毫秒）：

```json
{"在册条数": 0, "在册上限": 512, "在册": [], "留痕条数": 0, "留痕": []}
```

**错误码表**：

| 错误码 |
| --- |
| 内部错误 |

**调用示例**：

```python
后端.调用("平台控制面.提供者.取消登记快照", {})
```
