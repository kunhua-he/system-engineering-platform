"""动态库真实提供者公开入口（正式包包级中文入口）。

`动态库实现.py` 用 ctypes 真实加载系统动态库并调用**已登记签名**的最小接口；
本入口把它包成三条公开能力：`调用动态库` / `可用库列表` / `宿主可用性`。

契约口径（与实现逐字一致）：
- 库不存在、宿主不支持（缺 ctypes）、加载失败 → `宿主不可用`，禁止伪造成功；
- 未登记签名的函数拒绝调用（`签名未登记`），绝不猜测签名导致崩溃；
- 只依赖 Python 标准库（ctypes）。

正式包边界：调用方只经唯一能力调用入口按能力 id 调用，不得深入 `实现/` 目录。
"""

from 公共契约.基础类型.逻辑类型 import 真, 假
from 平台控制面.提供者.动态库提供者.动态库实现 import (
    候选名表,
    加载库,
    可用库列表,
    宿主可用性,
    宿主可用状态,
    宿主不可用状态,
    已加载缓存,
    归一化库名,
    查找库路径,
    签名表,
    类型映射表,
    转换参数,
    调用,
    来源名称,
)
from 平台控制面.提供者.动态库提供者.实现.能力入口 import (
    可用库列表 as 能力可用库列表,
    宿主可用性 as 能力宿主可用性,
    调用动态库,
)

__all__ = [
    "调用",
    "加载库",
    "查找库路径",
    "转换参数",
    "归一化库名",
    "可用库列表",
    "宿主可用性",
    "宿主可用状态",
    "宿主不可用状态",
    "签名表",
    "候选名表",
    "已加载缓存",
    "类型映射表",
    "来源名称",
    "调用动态库",
    "能力可用库列表",
    "能力宿主可用性",
    "注册能力",
]

包id = "平台控制面.提供者.动态库提供者"

def 注册能力(注册表) -> None:
    """由加载器调用，向能力注册表注册本提供者的公开能力。"""
# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 参数契约镜像（**函数内字面量**；本块由「丢参」缺陷修复注入，勿删）
    #
    # 缺陷溯源（2026-09-18）：本入口原先手工重建注册参数声明（只写 名称/类型），把
    # `必填` 丢掉；而网关唯一校验点 `运行核心/统一网关/协议/类型规格.py` 的判据是
    # `项.get("必填") is 真` → `None is 真` = 假 → 必填校验静默失效：少传参数不再被
    # 网关拦下，直接落进实现体抛 TypeError，用户拿到 HTTP 500 而不是干净的 400
    # 「缺少必填参数 X」。修法：参数声明从 `能力契约/参数契约.json` **整条透传**
    # （`参数=_参数契约表[能力id]`），入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：
    #  ① `_参数契约表` 是字面量、键是常量，`开发工具/契约编译/漂移检测` 的 AST 读取器
    #     （`_静态字面量` 的 `ast.Subscript` 分支）能解析出**整条参数含 `必填`** ——
    #     改前那处「参数口径未解析」不因本次修复恶化，反而把读不到的 `必填` 读到了。
    #     放在函数内是因为该读取器只收 `注册能力` 函数体内的赋值（`_扫描注册语句`）。
    #  ② 「裸布尔」防回潮门禁**已把 `必填`/`默认值` 计入违规**（本文件已迁为 真/假），改回裸 True/False 即判红。
    # 表内布尔位写**中文 `真`／`假`**（正式口径，真源 `公共契约/基础类型/逻辑类型.py`）；
    # 本文件顶部已 import 真/假，漂移检测读取器（`_导入常量绑定`）能静态解析出参数表，
    # （契约写了的键才留，没写的不补），不给漂移检测造出「注册声明了契约没声明的默认值」。
    # 为什么按下标取而不是包一层 `_参数声明(能力id)`：包一层会让 AST 一律判「不可静态
    # 判定」，等于拿门禁覆盖率换干净（不可接受）。契约名序/返回口径核验由装配期唯一闸门承担
    # （`运行核心/加载器/生命周期管理/管理器.装配系统` → 内核 `能力注册表.校验声明一致`），本入口不再另立第二份。
    _参数契约表 = {
        "平台控制面.提供者.动态库提供者.可用库列表": [
        ],
        "平台控制面.提供者.动态库提供者.宿主可用性": [
        ],
        "平台控制面.提供者.动态库提供者.调用动态库": [
            {"名称": "库名", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "函数名", "类型": "文本型", "必填": 真, "默认值": None},
            {"名称": "参数表", "类型": "列表型", "必填": 假, "默认值": None},
        ],
    }
# ===== 参数声明透传 结束 =====
    from 公共契约.能力契约.契约 import 能力实现

    注册表.注册(能力实现(
        能力id="平台控制面.提供者.动态库提供者.调用动态库",
        包id=包id,
        实现函数=调用动态库,
        参数=_参数契约表["平台控制面.提供者.动态库提供者.调用动态库"],
        返回="结果",
        说明="真实加载动态库并调用已登记签名的函数；未登记签名或宿主不支持一律明确失败。",
        版本="1.0.0",
        提供者id=包id,
        提供者版本="1.0.0",
    ))
    注册表.注册(能力实现(
        能力id="平台控制面.提供者.动态库提供者.可用库列表",
        包id=包id,
        实现函数=能力可用库列表,
        参数=_参数契约表["平台控制面.提供者.动态库提供者.可用库列表"],
        返回="结果",
        说明="真实探测已知系统库（sqlite3/c/system），返回库名、真实路径与可用状态。",
        版本="1.0.0",
        提供者id=包id,
        提供者版本="1.0.0",
    ))
    注册表.注册(能力实现(
        能力id="平台控制面.提供者.动态库提供者.宿主可用性",
        包id=包id,
        实现函数=能力宿主可用性,
        参数=_参数契约表["平台控制面.提供者.动态库提供者.宿主可用性"],
        返回="结果",
        说明="探测当前宿主能否真实提供动态库调用；不可用时状态为 宿主不可用。",
        版本="1.0.0",
        提供者id=包id,
        提供者版本="1.0.0",
    ))
