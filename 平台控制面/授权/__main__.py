"""`python3.14 -m 平台控制面.授权` 的入口：只读打印授权口径模型。

与同包 `__init__.py` 的分工：`__init__.py` 是能力面（装配期注册 + 公开名再导出），
本文件只做「命令行入口」这一件事，且**不复制任何授权判定逻辑**、不写任何授权事实
——数据全部来自唯一实现点 `平台控制面/授权/核心.py` 的角色表/等级表/操作最小角色/
可授予角色表/稳定错误码。授权动作（注册/授予/撤销/切换/校验）一律仍走唯一能力入口
按能力 id 调用，本入口不提供第二条授权路径。
"""
from __future__ import annotations

from 平台控制面.授权.核心 import (
    可授予角色表,
    操作最小角色,
    稳定错误码,
    角色等级,
    角色表,
)


def 打印授权模型() -> int:
    """只读打印授权口径模型；返回进程退出码（恒 0：本入口不改授权事实）。"""
    print(f"角色表：{角色表}")
    print(f"可授予角色表：{可授予角色表}")
    print(f"角色等级：{角色等级}")
    print(f"操作最小角色：{操作最小角色}")
    print(f"稳定错误码：{稳定错误码}")
    return 0


if __name__ == "__main__":
    raise SystemExit(打印授权模型())
