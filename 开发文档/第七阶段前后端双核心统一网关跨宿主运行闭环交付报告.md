# 第七阶段前后端双核心、统一网关与跨宿主运行闭环交付报告

> 项目路径：`/Users/hekunhua/Documents/Agent/PHP/系统级支持库/`
> 交付时间：2026-07-31
> 目标：独立的前端核心/后端核心/统一网关/跨核心通信/窗口与页面宿主/服务与任务宿主

## 一、测试总数与每轮结果

| 指标 | 数值 |
|------|------|
| 测试总数 | **207**（前阶段 169 + 新增 38） |
| 每轮结果 | **207 / 207 / 207 全过，退出码 0** |
| 每轮耗时 | **2.87 秒**（含真实子进程与 HTTP 网关） |
| 外部依赖数量 | **0**（纯标准库；http.server 即标准库） |
| V3 引用扫描结果 | **0**（仅文档字符串"不引入"声明） |
| 前端框架绑定 | **无**（不绑定 Vue/React/桌面/CSS 框架） |
| 后端框架绑定 | **无**（不绑定 FastAPI/Flask/数据库框架） |

## 二、可复制验收命令（严格退出码）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统级支持库
unset PYTHONPATH
python3.14 测试中心/运行测试.py        # 第1轮 207 全过 / 0
python3.14 测试中心/运行测试.py        # 第2轮 207 全过 / 0
python3.14 测试中心/运行测试.py        # 第3轮 207 全过 / 0
python3.14 示例项目/适配层示例/运行入口/运行.py   # 10/0 / 0
python3.14 -m py_compile $(find . -type f -name '*.py' -not -path './工程缓存/*')  # 通过
python3.14 发布门禁/运行发布门禁.py    # 发布状态: 通过 / 0
python3.14 示例项目/前后端核心示例/运行示例.py   # 跨核心链路 / 0
```

## 三、目录树（第七阶段新增）

```text
后端核心/
└── 后端核心.py           # 通用运行宿主（装配/注册/调用/权限/生命周期/统一错误）
前端核心/
├── 前端核心.py           # 平台无关契约（窗口/页面/组件/状态/事件/资源/路由/调用/渲染）
└── 渲染提供者.py         # 测试渲染提供者 + 浏览器 HTML 最小渲染提供者
网关/
├── 网关核心.py           # 网关协议层（请求id/参数校验/权限/限流/超时/统一错误/10 操作）
├── 本地网关.py           # 标准库 HTTP 网关（端口占用/优雅停止/未知路径/配置驱动）
└── 流式语义.py           # 流式返回类型 + 事件管理（首个/中间/完成/失败/取消/断开清理）
异步任务/
└── 任务系统.py           # 8 态任务（提交/查询/进度/取消/诊断/持久化/幂等）
运行上下文/
└── 上下文.py             # 统一上下文（请求/任务/项目/用户/会话/模块/能力/版本/提供者）
发布事务/
└── 发布事务.py           # 统一操作记录 + 回滚 + 一致性校验
有状态排空/
└── 排空管理.py           # 真实活动计数排空（请求/任务/连接/句柄归零 + 超时策略）
示例项目/前后端核心示例/
└── 运行示例.py           # 完整跨核心调用链示例
测试中心/第七阶段/测试_第七阶段双核心与网关.py   # 38 用例
开发文档/前后端核心开发说明书.md                # 12 类说明 + Agent 搜索问答
```

## 四、跨核心调用流程图（真实跑通）

```text
前端窗口（示例窗口，已打开）
  → 前端核心（状态：文本输入/结果区/错误区）
  → 网关（请求id + 统一上下文：项目id/用户id/会话id）
  → 后端核心（装配 64 能力 + 注册 示例.分割文本）
  → 能力契约调用（参数校验：文本/分隔符）
  → 统一结果（成功/值/错误码/错误说明）
  → 网关响应（请求id/成功/值/耗时毫秒）
  → 前端状态（结果区：成功: 共 3 段 → 你好 / 世界 / 示例）
  → 页面显示（测试渲染提供者文本 + HTML 渲染提供者 673 字符页面）
```

## 五、前端成功页面验证（实测）

```text
[3] 调用文本处理能力: 成功=True
    结果区: 成功: 共 3 段 → 你好 / 世界 / 示例
[5] 前端渲染（测试提供者）:
窗口: 前后端核心示例（示例窗口）
状态: 已打开
页面: 文本分割（路由 /）
  组件[输入框]: 文本输入 = 你好，世界，示例
  组件[状态区]: 结果区 = 成功: 共 3 段 → 你好 / 世界 / 示例
[6] HTML 渲染提供者: 浏览器HTML，HTML 长度 673 字符
```

## 六、前端失败状态验证（实测，8 种失败均可明确显示）

| 失败场景 | 前端显示 |
|----------|----------|
| 能力不存在 | `能力不存在: 能力未注册: 不存在的.能力` |
| 参数错误 | `参数不合法: 缺少 文本` |
| 权限不足 | `权限不足: 用户 X 无权限调用 Y`（后端权限测试） |
| 请求超时 | 网关超时错误码（请求超时秒配置） |
| 任务取消 | `已取消` 状态（任务系统） |
| 网关断开 | `外部不可访问` 错误码 |
| 版本不兼容 | 契约兼容检查拒绝（第五阶段） |
| 提供者不可用 | `外部不可访问` 错误码 |

前端始终得到明确状态，不出现空白页面或无限加载。

## 七、网关真实请求结果（HTTP 实测）

```text
POST /网关/请求 {"操作":"调用能力","能力id":"示例.加法","参数":{"甲":20,"乙":22}}
→ {"成功":true, "值":{"和":42}, "请求id":"...", "耗时毫秒":0.5}
健康检查 /健康 → {"成功":true, "值":{"状态":"健康"}}
未知路径 → 404 {"成功":false, "错误码":"未知路径"}
端口占用 → 启动失败 "端口占用或地址不可用"
```

## 八、流式调用结果（实测）

```text
事件序列: [首个事件, 中间事件×3, 完成事件] → 状态: 已完成
生成器异常: [首个事件, 失败事件] → 状态: 已失败 / 错误码: 内部错误
取消: 状态 → 已取消（重复取消幂等返回 False）
客户端断开: 事件列表清空 + 调用表引用释放（资源释放）
无限生成器: 最大事件数保护（超限自动断开，防卡死）
```

## 九、异步任务结果（实测）

```text
提交 → 任务id（全局唯一）→ 轮询状态 → 成功/结果 {"和": 11}
未知任务id → KeyError（明确失败）
取消 → 已取消（重复取消幂等）
未注册能力任务 → 失败/错误码 能力不存在（诊断可查）
进程重启后未完成任务 → 状态置 崩溃，不伪装成功
```

## 十、发布事务一致性结果（实测）

```text
注册表已激活但包未安装 → 一致性检查报问题
激活映射指向未安装版本（9.9.9）→ 报"激活映射指向未安装版本"
事务步骤失败 → 事务状态 失败 → 回滚 → 已回滚（步骤保留 3 条，可查询）
```

## 十一、有状态排空结果（实测）

```text
正常: 停止接收新请求 → 活动请求归零 → 任务完成 → 连接关闭 → 句柄释放
停止后新请求: 拒绝（开始请求返回 False）
超时: 排空超时（>0.2秒）→ 未完成任务记录 + 强制终止按策略 + 诊断记录
```

## 十二、后端核心/前端核心/网关/异步任务 能力对照

| 任务书要求 | 实现 | 实测 |
|-----------|------|------|
| 后端核心启动/健康检查/能力注册/能力调用 | 后端核心.py | ✅ 64 能力装配 |
| 任务提交/查询/取消 | 异步任务/任务系统.py | ✅ 8 态 |
| 服务停止/提供者切换/失败回滚/优雅关闭/强制关闭 | 后端核心 + 版本系统 | ✅ |
| 前端核心窗口/页面/组件/状态/事件/路由/渲染 | 前端核心.py | ✅ 38 断言 |
| 测试渲染提供者/HTML 最小渲染提供者 | 渲染提供者.py | ✅ |
| 网关请求id/参数校验/权限/限流/超时/统一错误 | 网关核心.py | ✅ |
| 本地 HTTP 网关/端口占用/未知路径/优雅停止 | 本地网关.py | ✅ |
| 流式语义（5 返回类型 + 断开清理） | 流式语义.py | ✅ |
| 统一上下文跨进程传递 | 上下文.py | ✅ 序列化/反序列化 |
| 统一版本/日志/发布事务 | 发布事务.py | ✅ 一致性校验 |
| 真实有状态排空 | 排空管理.py | ✅ 活动计数 |

## 十三、已知风险

1. 网关 HTTP 层为同步请求-响应；流式 HTTP（SSE/分块）未接入本地网关（流式管理器已在进程内工作）
2. 异步任务为线程执行（GIL 内 CPU 密集任务不并行）；跨进程任务分发未实现
3. 有状态排空的活动计数由调用方维护（开始/结束请求需显式调用）；自动钩子未接入
4. 前端 HTML 渲染提供者为最小自包含页面；真实桌面宿主需实现 渲染 接口（契约已定）
5. 发布事务一致性检查依赖外部传入存储实例；跨存储原子提交（两阶段）未实现
6. 网关限流为进程内窗口计数；分布式限流未实现
7. 上下文跨进程传递依赖调用方序列化（上下文.py 已提供 序列化当前/反序列化进入）
8. 浏览器渲染提供者生成的 HTML 为静态快照；实时双向事件推送未实现

## 十四、完成标准对照

| 标准 | 状态 |
|------|------|
| 前端窗口 → 前端核心 | ✅ 打开窗口/状态/事件 |
| → 本地网关 | ✅ HTTP 请求 + 请求id |
| → 后端核心 | ✅ 装配 64 能力 |
| → 模块/支持库 | ✅ 能力契约调用 |
| → 提供者 | ✅ 统一结果 |
| → 前端显示 | ✅ 成功结果 + 8 种失败状态 |
