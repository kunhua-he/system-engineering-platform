# 第八阶段生产化运行时与验收交付报告

> 项目路径：`/Users/hekunhua/Documents/Agent/PHP/系统级支持库/`
> 状态：功能维修完成，严格验收通过

## 一、验收结果

| 验收项 | 结果 |
|---|---|
| 统一测试第 1 轮 | 259 tests，通过，退出码 0，4.84 秒 |
| 统一测试第 2 轮 | 259 tests，通过，退出码 0，4.91 秒 |
| 统一测试第 3 轮 | 259 tests，通过，退出码 0，4.89 秒 |
| 第八阶段新增测试 | 48 tests，通过，约 1.36 秒 |
| 适配层示例 | 10/0，通过，退出码 0 |
| 前后端核心示例 | 真实 HTTP 网关链路通过，退出码 0 |
| 真实浏览器交互示例 | 普通 HTTP + 流式 HTTP 通过，退出码 0 |
| 全量语法编译 | 通过 |
| 发布门禁 | 30 项通过，退出码 0 |
| 严格 ResourceWarning 检查 | 发布门禁通过，无未关闭子进程警告 |
| 外部依赖 | 0，核心使用 Python 标准库 |
| V3 引用 | 0 |

## 二、本次维修内容

1. 发布门禁不再对依赖锁、完整性摘要和跳过项恒定返回成功。
2. 未指定候选包时，发布门禁自动遍历 13 个正式支持库和模块包。
3. 依赖锁两次生成分别取证，不再读取同一个最终文件进行自比较。
4. 完整性摘要改为 13 个包、83 个正式文件的逐文件 SHA-256 清单。
5. 增加锁文件、包文件和完整性摘要的反向篡改阻断验证。
6. 中文凭证改为 UTF-8 字节级恒定时间比较。
7. 门禁热切换临时进程按路由表与版本表并集清理。
8. 独立提供者停止后关闭标准输入、输出和错误管道。
9. 旧测试改为验证“跳过测试必须阻断发布”，不再要求跳过后伪造通过。
10. 修复发布门禁浏览器渲染请求的错误导入路径。
11. 修复网关限流门禁的并发释放与频率阈值验证顺序。
12. 流式通道完成、失败、取消、超时和断开均保持唯一终态并释放引用。

## 三、第八阶段能力

- 浏览器交互提供者：真实 `fetch` 调用网关，包含加载、成功、错误、取消、重试和断开状态。
- HTTP 流式：首个、中间、完成、失败、取消事件，客户端断开清理。
- 独立任务进程：真实子进程、JSON 协议、取消、超时、崩溃和持久化。
- 自动排空：请求、任务、连接、句柄计数自动回收，异常路径不泄漏。
- 发布事务恢复：准备、提交、完成和崩溃恢复。
- 网关安全：本机监听、凭证引用、请求大小、路径、权限、脱敏和审计。
- 五维限流：请求、任务、流式、用户频率、能力频率。
- 反向门禁：篡改锁文件、包文件和摘要均被阻断。

## 四、真实示例链路

```text
浏览器页面
  -> 前端核心
  -> 本地 HTTP 网关
  -> 后端核心
  -> 模块公开能力
  -> 支持库公开能力
  -> 提供者进程
  -> 统一结果
  -> 前端状态和页面显示
```

真实浏览器示例输出包含：普通 HTTP 能力调用成功、首个到完成事件流、页面状态“等待 → 加载中 → 成功”，以及失败、取消和断开状态。

## 五、已知限制

1. 全量 259 个测试包含历史真实进程和 HTTP 回归，单轮约 4.9 秒，未达到早期全量 2 秒目标；第八阶段新增测试单独约 1.36 秒。后续应优化测试装配缓存和进程复用，但不得削弱真实链路。
2. 数据库、动态库和 HTTP 外部提供者仍需由具体项目提供真实外部适配实现；系统级核心不直接连接业务资源。
3. 分布式限流、跨机器任务调度和系统级密钥服务不在本阶段范围。
4. 浏览器交互已验证真实 HTTP 和流式协议，但桌面原生渲染宿主仍需独立提供者实现。

## 六、可复制验收命令

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统级支持库
python3.14 测试中心/运行测试.py
python3.14 测试中心/运行测试.py
python3.14 测试中心/运行测试.py
python3.14 示例项目/适配层示例/运行入口/运行.py
python3.14 示例项目/前后端核心示例/运行示例.py
python3.14 示例项目/真实浏览器交互示例/运行示例.py
python3.14 -m py_compile $(find . -type f -name '*.py' -not -path './工程缓存/*')
python3.14 -W error::ResourceWarning 发布门禁/运行发布门禁.py
```
