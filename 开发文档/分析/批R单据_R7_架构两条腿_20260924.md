<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R · R-7 · 架构级两条腿（T2／T4／T6／T7／T9）

> 日期：2026-09-24 ｜ 开工ID：`开工-20260924-110433-6549`（T2/T4/T6文案/T7/T9）＋`开工-20260924-110929-e4e0`（T6 实现腿补占）
> 租约id清单：`a364296eb46544fe` `7578a7cc485e498c` `19bee4c5f6534ed4` `100ff593412c40a0` `5c02c8896f7b48cc` `0da49d6ed6284e39` `6511bce1ec864787`
> 上游：`开发文档/分析/审计_两条腿与冗余_20260923.md`（T2／T4／T6／T7／T9）＋`开发文档/方案/维修方案_两条腿与写入越界_20260923.md`＋`开发文档/决策记录/0061_待裁八项按设计哲学裁决.md`
> 裁决原话：华哥「如果是两条腿的就合并成一条」「发现问题就直接全做了」
> 口径：本件是**分析类**件；条目销账后按分析类口径删除。读数均为本批现场实测（行号会漂，引用前重跑命令）

---

## 零、结论（直达核心）

| 条 | 定性 | 动作 | 状态 |
|---|---|---|---|
| **T2／T4** | 同一路由（`/能力/搜索`、`/能力/契约/{能力id}`）两个 HTTP 服务各实现一遍；40006 停用腿未删 | 删掉 40006 那条腿的**整条腿**（`能力网关.py`＋`启动能力网关.sh`）；保留 `重启网关.py`（它服务 40007，是另一条腿） | **已做**（3 处依赖面越写集，见 §五 U1–U5） |
| **T6** | 同名参数 `环境变量` 两条腿语义相反（`启动进程`＝整体替换 ⇒ 静默丢 PATH） | 统一为**合并**：实现 1 处＋参数说明 2 处（两条腿同口径句） | **已做**（活体三拍实测，见 §二） |
| **T7** | 异步形态两条腿：能力自带 `异步` 参数被网关「≥10 秒走异步腿」先判 ⇒ 永远取不到 | 删 `异步` 参数＋其专属返回键 4 个＋说明引用；契约版本 `1.1.0`→`2.0.0`（删参数＝契约面变化，按 `决策记录 0020` 必须递增） | **已做**（派生件待重生成，见 U6） |
| **T9** | 两个公开编译能力没有 HTML 能力入口 | 按**既有机制**（轻代码页面 JSON＋`页面模型.校验页面` 判据）补 1 页绑定两条能力 | **已做**（落点＝全仓唯一在仓页面工程，见 U10 须裁） |

**改了/删了哪些路径（源码树）**

| 动作 | 路径 |
|---|---|
| 删 | `开发工具/能力网关/能力网关.py`（sha256 `a18c81a4…`）、`开发工具/能力网关/启动能力网关.sh`（sha256 `82d9f326…`） |
| 改 | `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py`（`启动进程` 环境变量改合并） |
| 改 | `支持库/后端/系统核心支持库/进程管理/能力定义.json`（两条腿 `环境变量` 说明同口径） |
| 改 | `模块库/自修复工具/能力定义.json`（删 `异步` 参数／返回键／说明引用＋版本 2.0.0） |
| 新增 | `示例项目/可双击演示/开发文件夹/前端/页面/编译入口.json`（T9 的 HTML 能力入口） |
| 新增 | 本件 |

---

## 一、T2／T4 · 40006 停用腿删除

### 1.1 定性（现场复核）

- 同一路由两处实现：`开发工具/能力网关/能力网关.py:87`（`/能力/搜索`）、`:89`（`/能力/契约/`）↔ `运行核心/统一网关/传输/网关边界面.py:353`、`:398`（本批复核实测行号一致）。
- 40006 那条腿**停用**：`lsof` 无监听、`launchctl` 无该标签、launchd plist 早已改名 `.已停用_目标文件已删除`。

### 1.2 零消费实测（改前）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
lsof -nP -iTCP:40006 -sTCP:LISTEN || echo '(无监听)'
ls ~/Library/LaunchAgents/ | grep 40006
grep -rn '40006' 客户端/ 运维脚本/ 启动监督器/ opencode.json zcode.json
curl -s -m 3 -o /dev/null -w '40006 状态码=%{http_code} 退出码=%{exitcode}\n' http://127.0.0.1:40006/能力/搜索
```

真实输出：

```
(无监听)
com.huashi.gateway-40006.plist.已停用_目标文件已删除
(客户端/运维脚本/启动监督器/配置 零命中)
40006 状态码=000 退出码=7      # 连接失败
```

**活腿同路由实测（证明删掉的是重复腿，不是唯一能力面）**：

```bash
curl -s -m 8 -H 'X-System-Credential: html-blackbox-verifier' \
  -w '\n状态码=%{http_code}\n' \
  'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E6%90%9C%E7%B4%A2?%E5%85%B3%E9%94%AE%E8%AF%8D=%E8%AF%BB%E5%8F%96%E6%96%87%E4%BB%B6&%E9%99%90%E5%88%B6=1'
```

```
{"请求id": "a9bb040d78a24be3", "操作": "能力搜索", "成功": true, "值": [{…}], …}
状态码=200
```

无凭证时同路由回 `401 权限不足`（fail-closed），带凭证回 `200 成功=true 操作=能力搜索` ⇒ 40007 活腿**确实承载**同一路由。

### 1.3 动作

- 删 `开发工具/能力网关/能力网关.py`、`开发工具/能力网关/启动能力网关.sh`（经 MCP `文件管理.删除文件`，绝对路径；两文件都**只**是那条腿，故整文件删）。
- **保留** `开发工具/能力网关/重启网关.py`：它服务 **40007**（`网关地址 = http://127.0.0.1:40007`、`服务标签 = com.huashi.gateway-40007`），是活腿的重启工具，被 `开发工具/发布门禁/运行发布门禁_制品验证.py:39` 与 `模块库/自修复工具/实现/开发编译口自检.py` 真实消费 —— **不是**停用腿，不在删除范围。
- 未删 `开发工具/能力网关/__pycache__/能力网关.cpython-314.pyc`：无源码的 `__pycache__` 缓存**不可导入**（Python 只在源文件存在时使用它），且删除被写腿按「无活跃写租约」拒绝（`越界`）—— 无害残留，随下次编译/清缓存消失。

### 1.4 改后读数

```bash
ls -l 开发工具/能力网关/
```
```
-rw-------  1 hekunhua  staff  27754 Sep 24 03:55 重启网关.py
```

```bash
git status --short 开发工具/能力网关/
```
```
 D 开发工具/能力网关/启动能力网关.sh
 D 开发工具/能力网关/能力网关.py
```

同一路由的**实现处**（非测试、非白名单）全仓只剩活腿一处：

```
运行核心/统一网关/传输/网关边界面.py:353  elif 路径 == "/能力/搜索":
运行核心/统一网关/传输/网关边界面.py:398  elif 路径.startswith("/能力/契约/"):
```

（`运行核心/统一网关/安全/安全边界.py:28,97` 与 `运行核心/统一网关/本地网关.py:618` 是**允许路径表**白名单，不是第二套实现。）

**验收对照**：`lsof 40006 无监听` ✓ ｜ `同一路由全仓只一处实现` ✓ ｜ `启动能力网关.sh 一并处置` ✓

---

## 二、T6 · 同名参数 `环境变量` 统一为合并

### 2.1 定性（现场复核，含「另一条腿到底是谁」的订正）

审计与债务清单把第二条腿记作 `执行命令`；现场核对**是 `沙箱执行命令`**：`支持库/后端/系统核心支持库/进程管理/能力定义.json` 里 `环境变量` 只有两处 —— `启动进程`（改前＝整体替换）与 `沙箱执行命令`（＝白名单内合并）。`执行命令` 根本没有这个参数。

### 2.2 改前活体实测（源码树直跑，只读探针）

> 说明：40007 网关跑的是**制品树**（`工程缓存/制品仓库/平台客户端环境/平台客户端/平台客户端`），源码改动要重编译才在 MCP 上生效；故本条的活体证据在源码树直跑（下同）。

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && python3.14 - <<'EOF'
import sys; sys.path.insert(0, '.')
from 支持库.后端.系统核心支持库.进程管理 import 启动进程, 等待进程结束, 释放句柄
r = 启动进程(命令="env", 环境变量={"X": "1"})
句柄 = r.值["句柄"]
w = 等待进程结束(句柄=句柄, 超时秒=15)
输出 = (w.值 or {}).get("标准输出", "")
print("子进程环境（逐行）:", [行 for 行 in 输出.splitlines() if 行.strip()])
print("能看到 PATH 吗:", "PATH=" in 输出, "| 能看到 X 吗:", "X=1" in 输出)
释放句柄(句柄=句柄)
r2 = 启动进程(命令="python3.14 --version", 环境变量={"X": "1"})
print("启动 成功=", r2.成功, "错误码=", getattr(r2, "错误码", ""), "说明=", getattr(r2, "错误说明", ""))
EOF
```

真实输出（改前）：

```
子进程环境（逐行）: ['X=1']
能看到 PATH 吗: False | 能看到 X 吗: True
启动 成功= False 错误码= 启动失败 说明= [Errno 2] No such file or directory: 'python3.14'
```

⇒ 传一个环境变量就**静默丢掉 PATH**；`python3.14` 直接找不到（看着像解释器没装）。

### 2.3 动作

| 落点 | 改前 | 改后 |
|---|---|---|
| `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:201-207`（docstring） | 「语义与 `执行命令` 的同名参数**相反**…整体替换、不合并…**待裁**」 | 「★ **合并**（决策记录 0061 第③项）：在当前进程环境之上逐键覆盖…调用方不再需要自己带上 PATH」 |
| 同文件 `:221`（实现） | `subprocess.Popen(cmd, cwd=工作目录, env=环境变量, …)` | `合并环境 = {**os.environ, **环境变量} if 环境变量 else None` ＋ `env=合并环境` |
| `进程管理/能力定义.json` `启动进程.环境变量.说明` | 「★ 整体替换、不合并…退出码 127…对照：沙箱执行命令 是白名单内追加（合并）」 | 「★ 合并：在既有环境之上逐键覆盖（传参覆盖同名键、其余继承）—— 既有环境＝当前进程环境（os.environ）…」 |
| 同文件 `沙箱执行命令.环境变量.说明` | 「白名单内可覆盖…；与 启动进程 的同名参数（整体替换）语义相反」 | 「★ 合并：在既有环境之上逐键覆盖（传参覆盖同名键、其余继承）—— 既有环境＝沙箱安全环境（白名单）…；与 启动进程 的同名参数同一口径（都是合并）」 |

两条腿的 `说明` 现以**同一句口径句**开头（「★ 合并：在既有环境之上逐键覆盖（传参覆盖同名键、其余继承）」），差异只在「既有环境是哪一份」。

### 2.4 改后活体实测（三拍）

同一探针（改后）：

```
== 探针1：启动进程(命令='env', 环境变量={'X':'1'})
子进程退出码= 0
环境变量条数= 59 | 能看到 PATH 吗: True | 能看到 X 吗: True
PATH 前 60 字符: PATH=/Users/hekunhua/Library/FlyEnv/alias:/Users/hekunhua/Library/

== 探针2：启动进程(命令='python3.14 --version', 环境变量={'X':'1'})
启动 成功= True 值= {'句柄': 730246, 'PID': 85612, '命令': 'python3.14 --version'}
退出码= 0 标准输出= Python 3.14.4

== 探针3（反向）：启动进程(命令='env')（留空 环境变量）
环境变量条数= 58 | 含 PATH: True | 含 X: False
```

⇒ 传 `{"X": "1"}`：子进程**看到 PATH 且 X=1**；留空：行为与改前逐字一致（原样继承）。

`python3.14 -c 'import ast; ast.parse(...)'` AST 解析通过；文件 JSON 合法（`json.loads` 通过）。

---

## 三、T7 · 删掉 `运行开发编译口` 的 `异步` 参数

### 3.1 定性（复核）

网关侧「`超时秒` ≥10 必须走异步腿」的判据在**边界先判**（`运行核心/统一网关/核心处理/网关参数校验面.py::同步腿超时上限秒 = 10.0`），而该能力 `超时秒` 默认 600 ⇒ `异步=真` 从调用方视角**永远取不到**（空转即杀，哲学 1.4）。

### 3.2 改前／改后

| 项 | 改前 | 改后 |
|---|---|---|
| 参数 | `['变更文件','库列表','全仓','自证','只读','上线','超时秒','源码根','异步']` | `['变更文件','库列表','全仓','自证','只读','上线','超时秒','源码根']` |
| 能力 `说明` | 「…；**异步=真 时改用平台既有异步腿**（启动进程/…）起编译并立刻回句柄，绕开网关单次转发 120 秒上限」 | 「…；**长任务一律走平台任务腿（任务提交/任务查询）**，本能力不自带异步形态（原 异步 参数已按 2026-09-23 裁决删除，依据 决策记录 0061）」 |
| 返回 `值结构` | 含 `异步`／`句柄`／`PID`／`下一步`（四键均注明「仅 异步=真」） | 四键删除（参数没了 ⇒ 这四个键永不出现，留着就是空转） |
| 契约版本 | `1.1.0` | `2.0.0`（删参数＝**契约面**变化 ⇒ 按 `决策记录 0020`／哲学 5.3 必须递增；`返回.值结构` 是纯文档字段，不参与该判据） |

写入回执（MCP `文件系统支持库.文本补丁.应用精确替换`，逐次带差异）：4 次写入，文件摘要链 `a0f18406…`→`707c33bc…`→`ddde1248…`→`deacbc52…`→`8c604886…`；`git diff --stat`＝`1 file changed, 3 insertions(+), 14 deletions(-)`。

改后复核：`json.loads` 通过；全文仅剩 1 处 `异步` 命中（就是新写的「本能力不自带异步形态」那句）。

**写集边界（华哥本批硬约束）**：只动 `模块库/自修复工具/能力定义.json`，**未碰** `运行核心/统一网关/**`。

---

## 四、T9 · 给两个公开编译能力补 HTML 能力入口

### 4.1 既有机制与判据（`capability_search` 定位，未新造第二套）

- **HTML 暴露面判据**（`开发文档/项目说明.md` 哲学 7.1 第④段原文）：HTML 暴露面＝**页面引用该 `能力id`**，判据在 `开发工具/轻代码前端编辑器/页面模型.py::校验页面`（`校验页面` 逐组件校验 `属性.能力id` 必须落在**可绑定集**里）。
- **可绑定集**＝`开发工具/项目编译/正式包索引.py::构建索引()['能力所有者']`。本批实测：可绑定集 **752** 条，`自修复工具.静态编译`／`自修复工具.动态编译` **都在里面**（`True`/`True`）⇒ 缺的不是可绑定集，**是页面**。
- 页面形状由 `支持库/前端/组件控件/组件目录/组件目录.json` 定（组件类型／默认属性／事件），页面 JSON 只存实例与绑定。

### 4.2 改前读数

```
全仓轻代码页面 JSON 数: 2
引用两条公开编译能力的页面: （空）
其余页面（引用数=0）: ['示例项目/可双击演示/开发文件夹/前端/页面/主页.json']
```

⇒ 与审计判据一致：**没有任何页面引用**这两个能力（它们是「公开能力」却在单腿铁律下不可达）。

### 4.3 动作

新增 `示例项目/可双击演示/开发文件夹/前端/页面/编译入口.json`（页面id `编译入口`、路由 `/编译入口`、标题「底座编译入口：静态编译 / 动态编译」）：

- `模块ID输入`（输入框，绑定 `文本`，默认值 `模块库/自修复工具`）
- `静态编译按钮`（按钮，`属性.能力id = 自修复工具.静态编译`，`参数模板 = {类型: 1, 模块ID: $文本}`，事件 `点击`）
- `动态编译按钮`（按钮，`属性.能力id = 自修复工具.动态编译`，同上）
- `结果区`（状态区，绑定 `结果`）

落点理由：全仓**只有这一个在仓页面工程**（`示例项目/可双击演示/开发文件夹/前端/页面/`，其 `项目声明.json` 声明 `前端入口 = 前端/页面/主页.json`，`开发工具/项目编译/项目编译器.py:703` 按 `前端/页面/*.json` 全量收集页面并生成路由表）—— 补入口只能在既有页面工程里补，不自造第二套入口机制。

### 4.4 改后读数与校验

```
全仓轻代码页面 JSON 数: 2
引用两条公开编译能力的页面: {'示例项目/可双击演示/开发文件夹/前端/页面/编译入口.json': ['自修复工具.动态编译', '自修复工具.静态编译']}
```

校验（机制自带校验器，只读）：

```
✓ 示例项目/可双击演示/开发文件夹/前端/页面/主页.json      （对照：既有页面）
✓ 示例项目/可双击演示/开发文件夹/前端/页面/编译入口.json  （新增页面）
```

**旁证（顺带实测到的一条口径差，本批不动）**：MCP 能力 `支持库.前端.前端契约校验.校验页面定义` 对同一页面回 **19 处「不在前端核心 页面定义 口径内（拒绝自创字段）」**（它按 `前端核心.页面定义` 的窄字段集判，连 `工程id`／`修订号`／`对象id` 都算自创）—— 既有页面 `主页.json` 同形同判。即：**轻代码页面 JSON** 的判据是 `页面模型.校验页面`，`校验页面定义` 是 `前端核心` 窄口径的另一面。本批按既有机制只认前者，未改任何一面。

---

## 五、未做 / 须裁决（逐条带证据与可复制命令）

| # | 项 | 为什么没做 | 证据／处置 |
|---|---|---|---|
| **U1** | `测试中心/开发工具/测试_能力网关.py`（整文件 8 用例）测的就是已删腿 | **越写集**（写集只含 `开发工具/能力网关/**`） | 实测：`python3.14 -c "import importlib; importlib.import_module('测试中心.开发工具.测试_能力网关')"` ⇒ `ModuleNotFoundError: No module named '开发工具.能力网关.能力网关'`。处置：整文件删（8866 时代先例）或改指 40007 活腿（其 8 条断言在活腿上逐条仍成立，路由实测 200） |
| **U2** | `测试中心/底座升级/测试_批次三假绿修复.py:33-38` `test_能力网关拒绝4780` 直接 `from 开发工具.能力网关.能力网关 import 启动网关` | **越写集** | 处置：改为判 `公共契约/运行时/端口策略.校验应用监听端口`（保留端口拒绝的**判据本体**，本来就是它）或活腿的启动入口 |
| **U3** | `公共契约/HTTP路由契约.json` `公开入口` 2 条（`/能力/搜索`、`/能力/契约/{能力id}`）的 `实现入口`＝`开发工具.能力网关.能力网关`、`验证场景`＝`测试中心/开发工具/测试_能力网关.py` | **越写集** | 处置：`实现入口` 改 `运行核心.统一网关.本地网关`（活腿实测 200，见 §1.2）、`验证场景` 换活腿场景。★ 本批全仓扫描确认**没有任何机器判据校验这两个字段指向的文件是否存在**（仅 `测试中心/第一批维修/测试_网关安全与有界并发.py:468` 读 `唯一执行入口` 段），故不会因本次删除而新增红 |
| **U4** | `开发工具/发布门禁/防回潮存量基线.json:29` 键 `开发工具/能力网关/能力网关.py: 9` | 基线件**不在写集** | 文件已删 ⇒ 该键自然「基线内有、本次未出现＝收敛」（判据自述只判「不得比基线更差」），不判红；建议随下次基线维护删键 |
| **U5** | 4 份文档仍自述 40006 腿「未启用／要用再起」 | **越写集** | `开发文档/规范/项目细则.md:22,:96,:215`、`开发文档/规范/批量开发流程与分级验证.md:32`、`开发文档/规范/网关调用最小示例.md:226-228`、`开发文档/当前流程图.md:130,:152,:383`。处置：改成「40006 腿已删（2026-09-24 批R·R-7），能力发现唯一腿＝stdio 薄壳 `capability_search`」 |
| **U6** | **派生件未重生成**（本批硬约束「不跑编译」） | 华哥本批明确不跑编译 | 待重生成：`支持库/后端/系统核心支持库/进程管理/能力契约/参数契约.json`（仍写旧「整体替换」说明，实测 `rg 整体替换` 命中）、`模块库/自修复工具/能力数据/能力搜索数据.json`（仍写旧「异步=真」说明）、两包的 `说明/使用说明.md`／`包声明.json`／`完整性摘要.json`。处置：主会话走 `capability_call(能力id='自修复工具.静态编译', 参数={'类型':1,'模块ID':'支持库/后端/系统核心支持库/进程管理'})` 与 `{'类型':1,'模块ID':'模块库/自修复工具'}` |
| **U7** | T7 实现侧残留：`模块库/自修复工具/实现/开发编译口自检.py` 仍有 `异步` 形参与 `异步 is 真` 分支（能力面已无该参数 ⇒ 死分支）；`开发工具/开发编译口/生成物联动.py:115-119` 注释「`启动进程`／`沙箱执行命令` 的 `环境变量` 入参是**整体替换**」对 `启动进程` 已不成立 | **越写集**（写集只含 `模块库/自修复工具/能力定义.json`） | 处置：实现侧删死分支；注释改为「`启动进程`＝当前进程环境上合并、`沙箱执行命令`＝白名单上合并」（该注释是「MCP 识别符号靠继承传导」的脆弱点登记，口径改后脆弱点只剩「显式传 `环境变量` 覆盖掉识别符号」这一半） |
| **U8** | 制品树仍是旧构建 | 本批不跑上线 | `工程缓存/制品仓库/平台客户端环境/平台客户端/平台客户端/开发工具/能力网关/能力网关.py` 仍在（10:24 构建）⇒ 制品里的 40006 腿随下次上线链重建自然消失；**注意**：MCP 网关跑的正是这棵树，故 T6/T7 的源码改动要**重编译＋重启**才在 MCP 上生效（热接入不重载已导入模块） |
| **U9** | 同批写入者租约重叠（**已按纪律处理，留痕**） | — | `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py` 2026-09-24 10:54:24–11:09:24 由 `R6-进程回收` 持有（租约 `aa18e8723a324be6`，到期未续，含 0 条留言）。本批 11:04 首次 `开工即占` **被整体拒绝**（未部分认领）⇒ 先认领其余 6 条；11:09:29 该租约到期后再认领成功（`开工-20260924-110929-e4e0`）。认领时现场指纹 `13d1d359…`＝R6 认领时登记的指纹 ⇒ **期间无人写入**，无内容冲突。若 R6 仍在飞：带 `预期文件摘要` 的补丁会被判「内容已变更」、不带摘要的精确替换只动它自己那段（两改并存）；**唯一风险是整文件回写**（用陈旧副本覆盖本批改动）⇒ 建议主会话在 R6 收工后复核 `进程管理.py` 里 `合并环境 = {**os.environ, **环境变量}` 那行是否仍在 |
| **U10** | **须裁决**：T9 页面落点 | — | 落在 `示例项目/可双击演示/开发文件夹/前端/页面/`（全仓唯一在仓页面工程）。若华哥要求这两个公开编译能力挂到**平台自身的维护者页面**（当前仓内不存在这样的页面工程），须另立工程 —— 本批不新造，故只登记 |

---

## 六、附：本批全部读数汇总（可复制）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台

# T2/T4
ls -l 开发工具/能力网关/                 # 只剩 重启网关.py
git status --short 开发工具/能力网关/    # D 启动能力网关.sh / D 能力网关.py
lsof -nP -iTCP:40006 -sTCP:LISTEN        # 空

# T6
python3.14 - <<'EOF'
import sys; sys.path.insert(0, '.')
from 支持库.后端.系统核心支持库.进程管理 import 启动进程, 等待进程结束, 释放句柄
r = 启动进程(命令="env", 环境变量={"X": "1"})
w = 等待进程结束(句柄=r.值["句柄"], 超时秒=15)
输出 = (w.值 or {}).get("标准输出", "")
print("含 PATH:", any(x.startswith("PATH=") for x in 输出.splitlines()), "| 含 X:", "X=1" in 输出)
释放句柄(句柄=r.值["句柄"])
EOF

# T7
python3.14 -c "import json;d=json.load(open('模块库/自修复工具/能力定义.json',encoding='utf-8'));print([(c['能力id'],c['版本'],[x['名称'] for x in c['参数']]) for c in d['能力列表'] if '编译' in c['能力id']])"

# T9
ls 示例项目/可双击演示/开发文件夹/前端/页面/
```

本批**未跑**任何编译/测试/门禁（华哥本批硬约束：不跑编译、不 git 提交）；上述读数均为只读探针与 MCP 只读/写腿回执。
