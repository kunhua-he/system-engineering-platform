# 系统工程平台 · 未完成事项落点审计（2026-09-22）

> 开工ID：`开工-20260922-231817-c0bd`｜口径：实测（逐条回当前工作树读真代码）
> 目的：给未完成事项清单的 **26 项**逐项标出「改哪里、判据在哪、能不能直接派活」，供后续派活与排期使用。
> 本件只作**落点事实基线**，不替代 `开发文档/未完成事项.md`（唯一债务清单）。

---

## 一、账目复核（机器口径，可重跑）

| 项 | 实测 | 命令 |
|---|---|---|
| 清单与正文一致 | 是（未漂移） | `python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验` → 退 0 |
| §四 新增债务 | **26 项** | `grep -c '^| — |' 开发文档/未完成事项.md` → 27（26 为 §四，1 为 §三 过时指针行） |
| 待修 / 大项 / 待裁决 | 0 / 0 / 0 | 同上 |
| §7.8 A 批行 | 已标「已落地」残留 | 清单 §7.8 |

**结论：真实待办 = 26 项，全部集中在 §四「新增债务」；其余 1 行是过时引用（详见 §六）。**

---

## 二、落点总表（26 项）

> 「落点」= 真源文件与行号（当前工作树实测）；「判据」= 门禁/生成器里现存的检查点；「派活性」= 可直接派活 / 需先定口径 / 需重建制品。

| # | 事项（简） | 落点（真源） | 判据位置 | 派活性 |
|---|---|---|---|---|
| 1 | 注入面收窄只做了三个大头 | `模块库/开工编排/实现/开工编排.py:567,568,600`（工作区快照/最近成功验证/缺失原子能力）、`模块库/开工编排/实现/返回体量分档.py:162,170` | 无（无体积上限判据） | 可直接派 |
| 2 | 薄壳 `值裁剪` 小上限下丢光整个值 | `开发工具/薄壳/薄壳服务.py:192 _兜底裁剪()`（:212/:228 子裁剪、:315 调用点） | 同文件 :315 | 可直接派 |
| 3 | `通用纪律` 被取两次（白读一次盘） | `模块库/开工编排/实现/开工编排.py:440` 与 `:511`（后者覆盖前者） | 无（缺「纯函数同函数体不得取两次」静态判据） | 可直接派 |
| 4 | `tool_catalog` 曾注入无关动态段（已修留档） | `开发工具/薄壳/工具清单.py`（工具面组装） | 防回潮判据：工具描述不得含 `{{常用传参}}` 字面量 | 已修，只留防回潮判据 |
| 5 | 发布门禁存量红 ②⑦⑧ | ② `开发工具/测试伪装门禁.py:81 存量违约上限=271`；⑦⑧ 见 `开发文档/分析/门禁存量红收口_第二批_20260922.md:97` | 同文件 :86 判定式 `计数 <= 存量违约上限` | ②可直接派；⑦⑧需先出失败清单 |
| 6 | unittest 存量红 11 条 | `测试中心/开发工具/测试_注册口径漂移.py`（8）+ `测试中心/开发工具/测试_退出码诚实口径.py`（3）；数据源 `开发工具/契约编译/注册口径存量基线.json`（09-19，已过期 33 项） | 测试自身断言 | 可直接派 |
| 7 | 参数口径真分歧 3 项 | `开发工具/参数口径审计.py:235`（基线路径）+ `开发工具/参数口径基线.json`；分歧源＝各包 `能力定义.json` 的 `返回`/`超时秒`/`输出上限字节` | 同文件 :197 `--写基线`（只增不改，新增即红） | 档 1 可直接派（按包分批） |
| 8 | EC-4 英文错误码债族缺 2 夹具 | `测试中心/平台控制面/测试_工作包11_消费者契约注册表.py:26,55,81,82,86`、`测试中心/平台控制面/测试_工作包02_调用Agent视图.py:47,52,89` | `开发工具/发布门禁/运行发布门禁_防回潮公共.py`（白名单） | 可直接派 |
| 9 | `返回.值结构` 只有类型没说明（497 条 / 43 包） | 全仓 107 份 `能力定义.json` 的 `返回.值结构`；热点 `支持库/后端/大语言模型支持库/模型连接器/能力定义.json`（27 处） | `开发工具/契约编译/参数可用性体检.py`（查参数说明，**不覆盖返回值说明**） | 需先加判据再分批补 |
| 10 | MCP 写文件 `根目录` 只做越界校验 | `支持库/后端/文件系统支持库/文本补丁/能力定义.json:223`（批量说明）、`:96`（单文件说明） | 无（契约未声明解析基准） | 可直接派（加解析基准语义） |
| 11 | 两处存量测试红（契约编译搜索） | `测试中心/开发工具/测试_契约编译搜索.py`；真源 `支持库/后端/图像处理支持库/图像解码/包声明.json:203 内部层=true`、`模块库/能力目录/实现/能力索引.py:159,164,167,188` | 测试断言 `聚合契约解析` 字符串 | 需先定案（补腿或改断言） |
| 12 | `运行开发编译口` 的 `源码根` 要手给 | `模块库/自修复工具/实现/开发编译口自检.py:32 系统根=parents[3]`、`:95 _源码根()`、`:116-120` 报错处；同构见 `模块库/自修复工具/实现/客户端构建.py:106`、`模块库/自修复工具/实现/效率巡检.py:65` | 同文件 :132 参数门 | 可直接派（平台自定位源码树） |
| 13 | 热接入对制品命名空间装配失败 | `运维脚本/热接入.py`（入口）、`运行核心/统一网关/核心处理/网关执行面.py`（装配）、`后端核心/后端核心.py` | 无（按源码包名装配） | 可直接派（按制品包名口径） |
| 14 | `读取文件` 默认行为变更影响面未核 | `支持库/后端/文件系统支持库/文件操作/实现/文件系统.py:29 默认最大字符=2000`、`:63-66 签名`、`:111-129 分支`；门面 `模块库/文件管理/能力定义.json:7` | 无（默认值变更无影响面判据） | 可直接派（核调用方） |
| 15 | HTML 黑盒 `--并发 200` 是假红发生器 | `开发工具/HTML验证/常量.py:8 并发上限=512`、`:9 默认并发`；资源独占 `开发工具/HTML验证/端口池.py _场景资源键`、`开发工具/HTML验证/场景执行器.py:130-143,161` | 同上 :161 `max_workers` | 可直接派（写死正式口径 32） |
| 16 | 两处手写 markdown 解析未收口 | `开发工具/公开调用完整性门禁_说明书解析.py:43 标题行=re.compile(r"^(#{2,4})\s+(.*)$")`、`支持库/后端/项目文档支持库/实现/切块.py:14 标题模式=re.compile(r"^(#{1,6})\s")` | `开发工具/验证门禁/同动作双路径检测.py` + `开发工具/验证门禁/存量基线.json`（存量 14 条） | ①高风险②连带契约版本，需分批 |
| 17 | 两份混血说明书合不上 ⇒ 刷不动 | `开发工具/MD文档生成/说明书/合并重生成.py:147 判定血统()`、`:629-637 超配对拒写`、`:731-741 血统改判`；两包 `支持库/后端/测试支持库/说明/使用说明.md`、`模块库/组件生成/说明/使用说明.md` | 同文件 :635 fail-closed | 可直接派（修小节序列对齐） |
| 18 | 生成器「拒写」退出码仍 0（假绿） | `开发工具/MD文档生成/说明书/合并重生成.py:75 退出码口径`；消费方 `开发文档/规范/生成物登记表.json:64-69,123-124` | 登记表把「退 0」当已生成 | 可直接派（三类分开计数） |
| 19 | 补账表行落在现行空行之后拆表 | `开发工具/MD文档生成/说明书/合并重生成.py:502 冲()`、`:568 空行一律取现行`、`:581-605 冲() 调用点`、`:619-622 自检` | 同文件 :333（#188 同域） | 可直接派 |
| 20 | 白名单「生成物类·含人工改写行」整份禁覆盖 | `开发文档/项目证据/说明书白名单.json:19,165,179,186,200,245,318` | 白名单处置字段 | 可直接派（重裁两条为行级合并） |
| 21 | `开工上下文.py` 摘要不闭合 | `支持库/后端/项目文档支持库/实现/开工上下文.py`、`支持库/后端/项目文档支持库/完整性摘要.json:43` | `开发工具/全量重算摘要.py` | 可直接派（跑重算） |
| 22 | `执行命令` 契约自相矛盾 | `支持库/后端/系统核心支持库/进程管理/能力定义.json:263`（能力说明「不经 shell」）vs `:270`（参数说明「shell 命令」）；同类 `:512` vs `:519` | 无（契约内部一致性无判据） | 可直接派（二选一收口） |
| 23 | 公开调用完整性门禁不比「默认值」 | `开发工具/公开调用完整性门禁_说明书解析.py:424-434`（只比名称/类型/必填） | 同文件六环判定 | 可直接派（补默认值一列） |
| 24 | `任务类型清单[*].判据` 里仍有裸命令 | `开发文档/规范/任务类型规范.json:118`（建包判据第 0 步 `curl` 直连 40007）、`:253`（拆分大文件判据 `py_compile`/`-c import`）；同源夹具 `模块库/开工编排/验证夹具/任务类型规范.json` | `通用纪律`（只扫工具姿势，不扫任务类型判据） | 需先定口径 |
| 25 | `批量应用精确替换` 的 `写入`/`原子` 语义反直觉 | `支持库/后端/文件系统支持库/文本补丁/能力定义.json:240 写入（默认值 true）`、`:247 原子`、`:290 补偿动作` | 无 | 可直接派（改默认值或加干跑） |
| 26 | `搜索能力(完整契约)` 一条命中≈7 千字符、七段同义重复 | `模块库/能力目录/实现/能力索引.py:516-544`（参数/参数类型/参数列表/参数名/必填/必填参数/可选参数 同义七份）；精简字段表 `模块库/能力目录/实现/能力目录.py:54,65` | 无 | **优先派（省 50–60% 上下文）** |

---

## 三、分块小结与建议批次

### 批 1（收益最大，建议先做）
- **#26 搜索能力去重** —— 落点单点（`能力索引.py:516-544`），改完立即降上下文压力；
- **#1 注入面收窄剩余三块** —— 落点集中在 `开工编排.py:567/568/600`；
- **#3 通用纪律取两次** —— 一行删除，白读一次盘。

### 批 2（假绿/假红治理，必须成组）
- #18 生成器拒写退出码 + #17 混血说明书 + #19 拆表 + #20 白名单：**同一链的四个环**（改能力 → 生成器零提案 → 编译口绿 → 门禁红），单修任一环都还会再红；
- #23 说明书默认值环 + #22 契约内部矛盾：同属「契约自洽」；
- #15 并发口径写死 32（#5⑦ 的失败数才能比）。

### 批 3（契约面统一，按包分批）
- #7 参数口径 3 项真分歧（档 1 统一到多数侧）、#9 `返回.值结构` 补说明（**先加判据**，否则补完再漂）、#10 根目录解析基准、#25 写入默认值。

### 批 4（腿与制品）
- #13 热接入认制品命名空间、#12 源码根自定位 —— 两者都是「网关跑制品时的腿」；
- #14 读取文件影响面（122 个文件命中，逐类核）。

### 批 5（测试与门禁存量）
- #5② 测试桩 18 项（已定位 6 文件）、#6 unittest 11 条、#8 EC-4 夹具、#11 契约编译搜索两红。

---

## 四、需先定口径 / 需裁决（不直接派）

| 项 | 卡点 |
|---|---|
| #24 | 「判据里的验证命令算不算裸命令」未定；含转义引号，锚点难写 |
| #11 | 补回「聚合契约解析」唯一腿 vs 改断言到实际入口名，二选一 |
| #9 | 先加「值＝纯类型名即判红」判据，还是先补 497 条 |
| #16 | `公开调用完整性门禁_说明书解析.py` 是**阻断门禁解析核心**，收口风险高；切块属支持库，连带契约版本自增 |
| #5⑦⑧ | 需先出完整失败清单（36 项 / 16 组），再拆号 |

---

## 五、需重建制品 / 重启才转绿

- #5 里 `第三方权限网络文件进程声明`（`Swift编译器提供者` 缺 `生命周期契约.json`）已新建该件，但判据读**激活制品**（`开发工具/发布门禁/运行发布门禁_制品验证.py:354 校验第三方访问声明`）⇒ 须重建制品 + 重启网关；
- #12/#13 修完后同样需经「编译口 → 构建制品 → 热接入」链路验证（#13 未修前只能重启）。

---

## 六、本轮发现的清单自身问题（不在 26 项内）

| 问题 | 现场 | 建议 |
|---|---|---|
| §三「待裁决」表引 `§四 #60`，而 §四 已改「表内无编号、以项计」 | `开发文档/未完成事项.md:83` | 删掉该指针行或改为按事项标题引用（按「已修/已废条目直接删」口径） |
| §7.8 A 批行已标「已落地」但仍占 1 行 | `未完成事项.md:211` | 按「已处置批次的行直接删」口径删行 |
| 「新增债务 26 项」与 `grep -c` 得 27 的差异 | 见 §一 | 机器生成口径已自洽（27 = 26 + 1 指针行），无需改；但清单头部可加一行说明 |

---

## 七、本轮新发现（不在原清单，取证时现场碰到）

| 现象 | 现场证据 | 为什么是缺陷 |
|---|---|---|
| **`文件管理.写入文件` 用相对路径会静默写进激活制品树，并回 `成功=true`** | 2026-09-22 23:36 实测：`文件管理.写入文件(文件路径="开发文档/分析/未完成事项落点审计_20260922.md")` 回 `值: true`；但源码树 `开发文档/分析/` 下**不存在该件**，实体落在 `工程缓存/制品仓库/平台客户端环境/平台客户端/开发文档/分析/未完成事项落点审计_20260922.md`（即制品环境目录下） | 与 #10 同根（`根目录` 只做越界校验、不做解析基准），但**后果更重**：写错树却报成功 = 典型假绿；且往制品树里写文件会污染制品字节，可能连带 `运行发布门禁_制品验证.py` 的「制品不变」判据 |

---

## 八、可复现命令（供核验）

```
python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验
python3.14 -m 开发工具.参数口径审计
python3.14 -m 开发工具.全量重算摘要 --只报
python3.14 开发工具/验证门禁/同动作双路径检测.py
```

> 取证时点：2026-09-22 23:17–23:40（开工ID `开工-20260922-231817-c0bd`）
> 本件所有行号均为**工作树实测**，非清单原样拄录；引用前请重跑对应命令（清单口径：引用前必须重跑取证）。
