# 系统工程平台 · 维修路线（第二轮：证真 + 落点 + 排序）

> 最后更新：2026-09-23 ｜ 开工ID：开工-20260922-235521-5d8a ｜ 口径：现场重跑，命令见各条
> 状态：生效（过程件，不是事实源）
> 语料快照：2026-09-23（分支 `主干`，本仓 `/Users/hekunhua/Documents/Agent/PHP/系统工程平台`，HEAD `1a17ffba`）
> 对应债务：`开发文档/未完成事项.md` §四「新增债务」（第一轮找全的 16 条，证据与数字以那份为准，本件不另立）
> 上游：第一轮找全已提交 `cd2061d8`；本件是设计哲学第 12 条**第二轮**的交付物（证真 + 精确定位修复落点 + 排序），不改任何代码
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


## 一、排序原则（不是「性价比」，是依赖顺序 + 稳定性优先）

按哲学第 1 条「先稳定性后高效性」与第 12 条：**先修「让缺陷能被看见」的东西，再修缺陷本身**。理由很硬：
本轮所有读数都来自**落后 HEAD 13 个提交**的激活制品，制品不换，后面的维修连「改完有没有变好」都验不了。

因此路线分四批：**批 0 让读数可信 → 批 1 阻断级 → 批 2 存量与口径 → 批 3 待裁决**。

## 二、批 0：让读数可信（必须先做）

| 序 | 事项 | 落点 | 验收判据 |
|---|---|---|---|
| 0-1 | 重建制品 + 重启网关（激活制品落后 HEAD 13 提交） | 上线链一条调用：`自修复工具.构建平台客户端制品` → 入库 → 签名 → 安装 → 重启网关（`launchctl kickstart -k gui/$(id -u)/com.huashi.gateway-40007`） | `git rev-list --count <制品来源.提交>..HEAD` = 0；且重取 §四 所有制品侧读数 |
| 0-2 | 制品来源记「含未提交变更」要定案（制品内容不对应任何提交） | 客户端构建侧的制品来源写入处（`客户端/构建平台客户端.py`） | 干净工作区构建出的 `制品来源.json` 不得出现「含未提交变更」；脏工作区构建须 fail-closed 或由判据判红 |
| 0-3 | 给「制品落后提交数」加判据（现在靠人记得） | 发布门禁的制品验证项（`开发工具/发布门禁/运行发布门禁_制品验证.py`） | 落后超阈值即判红，不再由「中间态（不用修）」这种措辞盖过 |

## 三、批 1：阻断级（修法已明确，可直接派活）

| 序 | 事项 | 落点（文件:行） | 验收判据 |
|---|---|---|---|
| 1-1 | `是否阻断` 同名不同义：能力读数回「必须被阻断」，门禁实际基线内不拦 | 契约文案 `平台控制面/能力目录/包声明.json:328`；门禁判据 `平台控制面/能力目录/消费者登记判定.py:374 判定登记完整性带基线`；基线 `平台控制面/能力目录/消费者关系基线.json` | 基线内缺登记回「不阻断」、基线外回「阻断」；能力键与门禁键改名或文案对齐 |
| 1-2 | `依赖生命周期审计` 的 `审计全部` 无任何入口（自述「发布门禁候选」但没进门禁） | 自述 `开发工具/依赖生命周期审计/运行依赖生命周期审计.py:1`；发布门禁只用了一半 `开发工具/发布门禁/运行发布门禁_制品验证.py:374`（只 import `扫描提供者目录`） | 造一条「缺停止入口」的提供者，接入口必须判红 |
| 1-3 | 22 条依赖生命周期违规逐条定性（真缺陷补停止入口或改文案；设计取舍登记豁免） | 13 个提供者各自的 `生命周期契约.json` 与 `实现/`（清单见 §四 对应条） | 定性后 `运行依赖生命周期审计.py` 退出码 0，或存量走基线且只减不增 |
| 1-4 | `测试写入边界门禁` 判据一几乎全盲（1123/1125 写动作「解析不出而放行」） | `开发工具/测试写入边界门禁.py`（判据一解析器；报数行 `:804`） | 先量清「解析不出」成因分布再扩解析器或登记显式豁免；反向样本（故意写仓库内相对路径）必须被抓到 |
| 1-5 | 租约「现场指纹」腿没有 `项目根` 参数 ⇒ 现场指纹恒「不存在」、「内容是否变更」恒真 | `平台控制面/能力目录/实现/文件租约维护.py:154`、`:295-333`、`:397`；`平台控制面/能力目录/实现/文件租约申请.py:164`；根因在 `项目根()` 的解析基准 | 源码树相对路径经网关调用必须读出真指纹；读不到指纹必须回「未核验」而非「已变更」 |

## 四、批 2：存量与口径（不阻断，但都在腐蚀判据分辨力）

| 序 | 事项 | 落点（文件:行） | 验收判据 |
|---|---|---|---|
| 2-1 | `测试写入边界门禁` 存量基线该降没降（门禁自己提示要下调） | `开发工具/测试写入边界门禁存量基线.json`，条目 `测试中心/支持库/测试_pdfplumber提供者.py::Testpdfplumber提供者.test_超时值合法边界真解析成功` | 基线桶下调后门禁回「已收敛」；并给「基线比现场多」加自动提醒 |
| 2-2 | `同动作双路径检测` 14 条存量里 8 条从未登记 | 3 条自建契约读取腿：`开发工具/发布门禁/运行发布门禁_制品验证.py:111`、`开发工具/契约编译/口径比对.py:404`、`开发工具/契约编译/漂移检测.py:436`；5 处两处配置表：`支持库/适配层/配置安全/密钥引用.py:16`、`运行核心/运行诊断/运行事件/脱敏工具.py:14`、`项目适配层/配置适配/配置校验.py:14`、`平台控制面/统一入口/核心.py:56`、`开发工具/契约编译/消费者契约.py:17` | 逐条收口到唯一腿，或进 `开发工具/验证门禁/存量基线.json` 并写清合法两份的理由 |
| 2-3 | `常用传参` 给假线索（已删参数 `联动`、工具级入参 `项目根`） | `开发工具/薄壳/工具清单.py:138-180`（取数在 `账.查常用参数`） | 按当前契约参数表过滤；给一个含已删参数的旧账本，`常用传参` 必须不再回它 |
| 2-4 | 装配口径与搜索面差额（成因已定案但数字过期、无判据） | `模块库/能力目录/实现/能力索引.py:56 定位数据根` docstring | 更新拆解数字；给「装配口径 − 搜索面 = 适配层有意排除数」加判据 |
| 2-5 | `检索准度基准` 文本层首条准确与召回率偏低 | 文本层腿：`支持库/后端/文件系统支持库/内容检索` 的正则搜索 | 先扩样本再定案；该基准要么接进门禁、要么明确是人工基准 |
| 2-6 | `md查重` 只报红未登记 | 门禁入口 `开发工具/开发编译口/编译口.py:135`（`开发工具.MD文档生成 --查重`） | 核该文件是真重复还是判据假红，定案后登记结论 |
| 2-7 | `项目文档.人工改文档` 契约自相矛盾（配置 30 秒 vs 说明 300 秒） | `模块库/项目文档/能力定义.json` 的 配置契约 与 说明 | 两处对齐；同批 grep 依赖旧文案的测试与验证场景 |
| 2-8 | 全仓文档索引有 5 份过期（审计靠索引定位会读到旧块） | `项目文档支持库.建全仓索引`（可挂进编译口派生链） | 重建后 `查询索引` 回「新鲜」；或把新鲜度接成检索前置提示 |
| 2-9 | 制品仓库积压 35 份 `当前.json.统一schema前_*` 迁移残留 | `工程缓存/制品仓库/平台客户端制品`；回收口 `开发工具/工程缓存回收.py` 报「无需清理」 | 定保留策略或让回收口认这类命名；删前先查引用面 |
| 2-10 | `效率巡检` 无子代理日志时整口回「未核验」不可用 | `模块库/自修复工具` 的 效率巡检 | 写清适用范围，或改跑静态效率面并如实分栏 |
| 2-11 | `项目文档.写文档` 的默认判据路径在本仓恒失败：推导按 `文档根/规范/Markdown 文档体例判据.json`，而本仓登记 `文档根相对路径="."`、判据真身在 `开发文档/规范/` | 判据推导处 `模块库/项目文档`；登记记录见 `项目文档支持库.查询项目(系统工程平台)`；本轮实测报 `/…/系统工程平台/./规范/Markdown 文档体例判据.json` 不存在 | 推导须能落到 `开发文档/规范/`（或把判据路径登记进项目档案）；显式传 `判据文件` 时必须能落盘（本轮已用显式路径落盘成功） |

## 五、批 3：待裁决（只有一件）

| 序 | 待裁事项 | 选项 |
|---|---|---|
| 3-1 | `跨平台部署验证` 全量验收口需「华哥明确授权」，且它会 `重建依赖锁 --写入 --全部`（改 36 份依赖锁） | ① 本次审计授权跑全量（须在干净工作区做，并确认 36 份锁变更可接受）；② 不跑，全量端到端留作覆盖缺口。无论哪种，都该把「全量验收 = 授权门」登记成显式待裁项，而不是只写在脚本 docstring 里 |

## 六、第二轮证真状态（诚实分栏）

- **已证真并当场改写 3 条**（原判有错，已改回 §四）：`判定消费者登记完整性`（原判「未接线」错，实为同名不同义且已接线）、`依赖与生命周期审计`（补准：脚本自述「发布门禁候选」、发布门禁只用其一半）、`装配口径与搜索面差额`（补准：成因已在 docstring 定案、但数字过期且无判据）。
- **已证真并定位到文件:行**：1-1、1-2、1-4、1-5、2-1、2-2、2-3、2-4、2-6。
- **待证真**（落点已知但前提未逐条复核）：1-3 的 22 条定性、2-5、2-7、2-8、2-9、2-10、批 0 三项。
- **未纳入本轮**：HTML 黑盒与场景红那批存量红 —— 按既有结论须在制品重建（批 0-1）后才能分出真缺陷与滞后噪声。

## 七、下一步（第三轮）

第三轮才是「把全部问题一次性修完」，且按哲学第 12.2 条**一次性全派**：批 0 是批 1 与批 2 的前置（不改制品，维修结果无法验收）；批 1 内部 5 条互不依赖可并行；批 2 十一条彼此独立可并行。
派活时任务信必须带开工ID 与本件给出的 `文件:行` 落点，不得只写条款号。
