# 审计发现汇总（2026-09-19 第一轮 · 含逐条现场证真）

> 最后更新：2026-09-20
> 口径：实测（每条附现场命令与退出码；**行号会漂，引用前按符号名重新定位**）
> 维护者：审计发现唯一台账
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


本文是 **2026-09-19 第一轮审计（8 份报告）的唯一继承件**。原 8 份报告已按「同类蒸馏成 1 个」
删除（Git 历史保留）。**逐条定性**由三路只读证真（开工 ID `开工-20260920-003000-z1aa` /
`-z1bb` / `-z1cc`）现场重核：每条都打开了报告写的 `文件:行号`，行号漂移处按符号名重定位。

**定性四档**：`已修`（主现象消除）· `部分成立`（部分消除或有残留）· `仍成立`（未动）·
`已消失/未发现`（报告当年成立，现已不存在）。

**★ 本文只作取证台账，不是待办源**：本文保留**现场命令与输出原文**（复议时有据可查）。
全部待办**已逐条并入** `开发文档/未完成事项.md` 第七部分（#64–#105），
**另有一份「不是缺陷（设计取舍）」防误修清单**在该清单 7.7 节。**不要再从本文派活**。

**真债务去向**：仍成立且属真缺陷的，已登记进 `开发文档/未完成事项.md`；**不是缺陷的设计取舍**
留在本文第四节，防后人误修。

---

## 一、空转与死代码（报告原：`审计_空转与死代码_20260919.md`）

| 条目 | 定性 | 关键现场证据 |
|---|---|---|
| B-1 非正式类型名判据面塌缩 | **已修**（残 2 处注释/死分支） | `防回潮公共.py:63` 指针已改真源 `协议/类型规格.py`；`:156` 读到空集改 `raise RuntimeError`（判据不得以空集充当「无违规」） |
| B-2 摘要闭合门禁两分支都 return 0 | **已修** | `--只报` 现场退出码 =1（原恒 0）；三支分开于 `全量重算摘要.py:84-100` |
| B-3a 验证场景体检空扫描面仍报通过 | **已修** | 空根 → `❌ 扫描面为 0`、退出码 =1；`验证场景体检.py:329` |
| B-3b 测试伪装门禁 0 文件仍报通过 | **仍成立** | 空测试中心 → `测试文件 0 个` 仍 `通过`、退出码 =0。落点 `测试伪装门禁.py:336` + `运行发布门禁_执行项分组_制品与流程.py:349` ⇒ **已登记 #61** |
| I-1 编译口「静态规则全查」未纳入五类判据 | **仍成立** | 现场清单 6 条，`测试体系门禁`/`测试伪装`/`反向破坏`/`防回潮` 关键词命中 **0/0/0/0**；落点 `编译口.py:47` ⇒ **已登记 #62** |
| I-2「零调用点顶层函数」清单 | **部分成立**（**总数不是 33**） | 三口径复算 32/38/46；逐条差异已查清：**31 存活 / 2 已接线 / 1 新增**。报告口径本身不可复现（「全仓 1005 个 .py」与现场 1140/906 都不符） |
| I-3 `统一网关/协议表.py` 死件 | **已修** | `ls` → No such file；零 .py 引用；已建反向测试 `测试_网关协议表唯一性.py`（首条判据＝副本不得复活） |
| I-4 热切换 `记录灰度观测` 全仓 0 调用方 | **仍成立** | 3 处全在同文件（2 注释 + 1 定义）；落点 `热切换.py:264` |
| I-5a `开发效率巡检.main()` 恒返回 0 | **部分成立** | 已加零交付 `return 1`；三处阈值判定（`:342`/`:357`/`:394`）仍只 append 不进退出码 |
| I-5b `提供者环境回收.回收()` 恒返回 0 | **仍成立** | `:117 return 0`；`跳过` 只打印 |
| I-5c `安装或卸载队列常驻.卸载()` 丢弃返回码后报成功 | **仍成立** | `:115` 丢弃 `执行()` 返回码、`:119` 报「已卸载」、`:120 return 0`（同文件 `安装()` 已示范正确姿势） |
| S-1 哲学总账缺「反向试验」栏 + 无「规则自检」强制点 | **仍成立** | `反向试验` 在总账中计数 **0**；`开发工具/` 全域「规则自检」零命中 |
| S-2 编译口「变更集无 .py → 跳过」被归入通过桶 | **仍成立** | `编译口.py:337-339` 直接置退出码 0，结论行无「未核验」字样 |
| S-3 死代码归因与处置需进第二轮 | **仍成立** | 无「零调用条目逐条归因 + 最小改法 + 验证命令」的处置清单 |

---

## 二、文档事实源分叉（报告原：`审计_文档事实源分叉_20260919.md`）

| 条目 | 定性 | 关键现场证据 |
|---|---|---|
| P0-1 哲学条数「1–13」vs「1–14」 | **仍成立**（3 处已修 / 2 处未修） | `AGENTS.md:308` 仍写 1–13；`主开发文档.md:18` 仍写「1–13 / 子项 ≤56」；`哲学总账.md:8`、`项目说明.md` ×3 已修 |
| P0-2 子项数/上限多方不一致 | **部分成立** | 差集只剩 `11.2`（项目说明有、总账无）；`项目说明.md:109` 写「59 子项」＝其自身声明命令输出为 **60**；`主开发文档.md:18` 上限 ≤56 仍过期（总账已改 ≤60） |
| P0-3 已删旧账仍活在 `目标定义_什么是OK.md` | **已修** | 「49 条款 / 139 包」现场零命中；已改「本文件不复述数字」 |
| P0-4 `方案/二次审计落地编排_20260918.md` 仍自称「唯一编排入口」 | **仍成立** | 该文件 `:6` 与 `执行编排_唯一入口_20260918.md:7` 同时自称唯一；`主开发文档.md:71` 亦用此措辞 |
| P0-5 `AGENTS.md` 把一次性审计过程件写成硬规则 | **仍成立** | `AGENTS.md:193`「方案全文见 `开发文档/分析/深度审计_底座升级清单_20260919.md`」—— **该文件本次已随之收敛，此引用必须一并改指** |
| P1-1 手写包数/能力数/说明书分母过期 | **仍成立**（差距扩大） | 现场：包声明 **142**、使用说明 **143**、包级 142（生成物类 115／人工类 27）、能力定义 **136**；`项目说明.md:42/43/49` 三行数字与取证命令均不符 |
| P1-2 生成物 `规范/能力目录说明书.md` 带过期数字且缺输入指纹 | **仍成立** | `:5` 写「共 138 份包声明」（现场全仓 142）；输入指纹三项零命中 |
| P1-3 「5 根」vs「6 根」 | **仍成立** | `公共契约/正式根.py:30` 已 6 根；`启动监督器/健康监督/说明/设计说明.md:56-59` 仍写 5 根且引用失效行号（写 :23，真实在 :30） |
| P1-4 `规范/场景覆盖率口径.md` 自称权威处但不在任何清单 | **仍成立** | `:5` 自称权威处；`:106` 分母仍写 681（现场 689）；`AGENTS.md` 规范清单与 `主开发文档` B 栏**均未列它** |
| P1-5 `未完成事项.md` §8.7 重号 + 顺序倒置 | **已修**（残留 1 处悬挂引用） | 该文件已整体重构（§8.x 零命中）；`主开发文档.md:74` 仍指「§8.7」→ **悬挂** |
| P1-6 `未完成事项.md` 表内条目号重号 | **部分成立** | 现场重号：`#7`×2、`#56`×2、`#60`×2（`#23`/`#1` 已消失）。⚠ 注意形态不同：`#56`/`#60` 是「一节摘要行 + 一节明细行」同条目，`#7` 是**两件不相干的事共号** —— 处置不要一刀切 |
| P2-1 决策 `0013` 未按规则标「已变更」 | **仍成立** | `0013:3` 仍写「现行（已落地）」而现场是 14 主条；另 `0021:4`/`0048:6` 状态值越出五态 |
| P2-2 `临时文档/` 未清空 | **已修** | 只剩 `README.md`（临时稿结论已并入债务清单） |
| P2-3 `交付记录/` 于「不再新增」后新增且带过期数 | **仍成立** | `20260918_夜间推进简报_给华哥.md` 仍在、`681` 仍出现 3 次；`主开发文档.md:79` 仍写「只读不改、不再新增」 |
| P2-4 `主开发文档.md` 文档地图漏列 `项目证据/` | **仍成立** | 文档地图 D 栏 4 行无 `项目证据/`；但 `AGENTS.md:43` 承认它是运行时验证账本落点 |
| P2-5 权威文档元信息头「最后更新」落后 | **部分成立** | `主开发文档.md:3` 写 09-17 而内容 09-20 仍在改；`当前流程图.md:3` 写 09-17、mtime 09-18 |
| 契约版本第二处硬写 | **未发现** | `契约版本 = "2.0.0"` 唯一常量；136 份参数契约全为 2.0.0 |
| 决策记录重号/跳号复用 | **未发现** | `0001–0055`（缺 `0010` 为合法跳号）无重复 |

---

## 三、重复腿与旁路 / 能力唯一性 / 依赖分层 / 平台越界（4 份报告合并）

### 3.1 重复腿与旁路

| 条目 | 定性 | 关键现场证据 |
|---|---|---|
| B-1 `全自动精校.py` 直连文件读写 | **已修** | `运行能力调用图审计.py` → 违规数 **0**；已改走 `文件系统支持库`/`数据操作支持库` |
| I-1 模块库跨边界调用两条腿 | **已修**（二合一） | `def 设置HTTP连接器` 在 `模块库/` 命中 **0**；48 个文件改走唯一调用器 |
| I-2 网关协议表两份同名副本 | **已修**（含连带假绿） | `协议表.py` 已删；`_历史短名源` 已改真源；`_防回潮历史短名集` 由 0 → **18**（原恒空＝门禁假绿） |
| I-3 同名平铺 `.py` + 同名包目录并存 | **部分成立**（3 处 → **5 处**，性质已变） | 5 处：`平台控制面/授权`、`平台控制面/统一入口`、`开发工具/环境自检`、`运行核心/能力调用/超时执行`、`启动监督器/健康监督`。手法已换为「真实现唯一落在包内 `核心.py`、平铺件降为再导出腿」；**残留死件** `启动监督器/健康监督/实现/兼容平铺模块.py`（仅被 2 处注释提及） |
| I-4 技能脚本直连库绕过网关 | **已修** | `技能库绕网关检测.py` → 直连脚本数 **0**；报告要求的可调能力白名单已补 |
| I-5 `查看契约` 第二套实现 | **仍成立** | `开发入口.py:50` 本地 `rglob` 分支一行未动；已被 `同动作双路径检测.py` 登记为存量 |
| I-6 模块把 `设置HTTP连接器` 作为公开第二入口导出 | **已修** | `模块库/`（排除 `实现/`）命中 0，只剩 docstring 文字 |
| I-7 网关自建 sqlite 幂等库 | **仍成立**（位置搬家） | 真实现现在 `运行核心/统一网关/运行态/幂等存储.py:54`；`同层走能力样板`（`任务系统.py`/`灰度指标.py`）仍在 ⇒ 收口有先例可循 |
| S-1 统一能力入口视图直读私有属性 | **部分成立** | 真可疑仍只有 2 处（`维护者视图.py:80`、`组件开发Agent视图.py:22`），与报告一致 |
| S-2 4 份手写 HTTP 客户端 | **仍成立** | `网关转发.py:45`、`热接入回归.py:36-41`、`批量精校队列.py`（6 处）、`基准环境.py:69` |
| S-3 死 import sqlite3 | **部分成立** | `备份契约.py:9` 仍成立；`工具执行.py` 已真用；**新增** `网关核心.py:33`（零使用） |
| S-4 能力搜索器死常量 | **仍成立** | `能力搜索器.py:31` 仅定义行 |

### 3.2 能力唯一性降重

| 条目 | 定性 | 关键现场证据 |
|---|---|---|
| B-1 适配层腿与后端腿两套实现 | **部分成立 → 大幅收口** | 现场成对 **49 对 / 11 对包**（报告 43 对为旧读数）。已收口：Tesseract / textutil / pdfplumber / reportlab / **转写（报告写「未动」，实已收口）**；部分：Pillow / PyMuPDF / PDF隔离 / FFmpeg；**判定不迁移**：LibreOffice（10.7 三条硬证据）；**真未动**：Git提供者 |
| B-2 调用面仍指向将被降级那条腿 | **仍成立，且是设计** | 实现文件内后端腿 id 字符串 50 条／14 文件；但迁移清单 10.3.3 已改判为「实现载体搬家、对外 id 不变」⇒ **这 50 条按设计不需要改**（报告定性应以 10.3.3 为准） |
| I-1 `模块库/*` 逐能力 1:1 纯透传门面（参数面被裁） | **仍成立** | `文件管理.读取文件` 契约只有 `文件路径`，底座有 `文件路径`+`编码`；`写入文件` 缺 `编码`/`允许危险路径`；`删除文件` 缺 `允许危险路径` |
| I-2 提供者注册机制两套并存 | **仍成立** | `支持库/适配层/提供者注册表/` 无包声明、无能力定义；`平台控制面/提供者/注册表/` 与它**零连接** |
| I-3 适配层根下 12 个「非正式包」目录 | **仍成立** | 18 有包声明 / 12 无；`AGENTS.md:327-329` 已明文豁免「不对外提供能力契约的内部适配器」 |
| I-4 `流式创建摘要` 与 `创建内容摘要` 返回类型不一致 | **仍成立** | 底座 `资源管理.py:97` 仍 `-> str` 且抛 `FileNotFoundError`；模块层仍 try/except 包装 |
| S-1 `类型转换` 10 项说明同一模板 | **仍成立**（建议级，报告结论「仅登记不改」） | 无动作 |
| S-2 `续约句柄`/`续租句柄` 参数相同返回差一字 | **仍成立** | 6 套 `释放句柄` 并存 |
| S-3 `数据集合` 内含 3 条文本类能力 | **仍成立** | `文本截取`/`文本去首尾` 仍在 |

### 3.3 依赖分层归属

| 条目 | 定性 | 关键现场证据 |
|---|---|---|
| S0-1 PostgreSQL 未登记为环境依赖 | **已修** | 新增手写源 `支持库/适配层/服务型环境依赖.json`（端口依据 `lsof -nP -iTCP:5432` 实测）。注：报告建议的落点（依赖锁/生命周期契约）未采纳，**另建唯一手写源** |
| S0-2 密钥提供者被依赖生命周期审计「非标准」跳过 | **仍成立** | 审计输出 `跳过: 密钥提供者: 无 包声明.json` |
| S0-3 模型服务.py / 系统探针.py 直连第三方无锁 | **部分成立** | 仍是根下平铺 .py 未包化；**但**依赖登记已补声明 ⇒「该建锁没锁」的判据缺口仍在、登记面已补齐 |
| S1-2 依赖分两段声明件未被任何门禁调用 | **仍成立**（程度减轻） | 发布门禁零命中；`生成依赖分两段.py --校验` 真退出码 1（4 项红 → 2 项） |
| S2-1 三项环境依赖不在 `环境自检` 外部命令表 | **仍成立** | 表仍 8 项，无 `security`/`node`/`codegraph`/`browser-use` |
| S2-2 密钥提供者/本地进程适配器 六要素缺 3 项 | **已修** | 版本/健康探针/降级语义三项全补齐 |
| S2-2b 代码地图提供者 缺生命周期契约 | **已修** | `生命周期契约.json` 已存在 |
| S2-3 依赖登记覆盖落后现场 | **已修** | 登记 36 / 现场 37（唯一差 `说明/` 目录，本应在册外） |
| S2-4 生成器仓内仓外双份 | **已修** | 仓外副本已删；登记已指仓内 |
| S3-1 `豁免正向场景` 判据形态不足以守住边界 | **仍成立**（零实例） | 能力定义含 `外部依赖` = **0/7374**；判据未加「两档拒本地」 |
| 第 3 项 第三方 import 分布 | **仍成立**（分布一致 + 4 项边角新增） | 总数 117：`reportlab 34`/`openpyxl 12`/`cryptography 11`/`pptx 10`/`docx 10`/`mcp 9`/`fitz 4`；新增 `faster_whisper 1`（转写双后端）、`技能底座能力 2`（合规） |
| 第 6 项 依赖锁覆盖率 | **仍成立** | `依赖锁.json` **33** 份，跳过 3 |

### 3.4 平台判断越界

**结论：收口层四原语全部落地（E-2/E-3/E-4/E-5），报告主体已修。**

| 条目 | 定性 |
|---|---|
| B1-1 任务进程多进程启动分叉 / B1-2 命令文本拆分分叉 / B1-3 RSS 取法分叉 / B2-1 第三平台判定层 / B3-1 裸读 `sys.platform` | **全部已修**（`平台适配.py` 新增 `多进程启动上下文()`/`拆分命令文本()`/`进程内存RSS字节()`/`平台稳定缓存根()` 四原语并入 `__all__`） |
| D-1/D-2 注释与实现冲突 | **已修** |
| D-3 三处 docstring 仍写 `start_new_session / killpg` | **部分成立（1/3）** | `技能库/后端/技能库/实现/技能库.py:15` 与 `Git操作/实现/受管执行.py:4` 仍成立 |
| E-1 本机系统名/架构取值上报唯一口径缺位 | **仍成立** | `平台适配.py` 无 `本机系统名()`；7 处 `platform.system()` 取值 + `强制校验.py:64-72` 的 `_归一化系统名` 粘合补丁仍在 |
| E-6 平台专有能力可用性统一表达 | **仍成立**（报告自标「可选，当前 fail-closed 行为正确」） |
| §六.5 本铁律无门禁覆盖 | **仍成立** | `rg "sys\.platform|os\.name"` 在三门禁目录只有 1 处注释命中 ⇒ 无自动防回潮判据 |

---

## 四、不是缺陷（设计取舍）· 防误修清单

> 这些**不是**待办。后人看到现象时不要「顺手修」——每条都经现场核实为有意设计。

1. **「声明 699 / 注册 468」能力数差**：网关 468 = 「支持库+模块库」且「非适配层、非内部层」的公开包，口径自洽，**不是缺陷**。
2. **调用面 50 条后端腿 id 字符串**：按迁移清单 10.3.3「实现载体搬家、对外 id 不变」属设计，**不需要改**。
3. **适配层根下 12 个无包声明目录**：`AGENTS.md:327-329` 明文豁免「不对外提供能力契约的内部适配器」。
4. **LibreOffice 对不迁移**：10.7 三条硬证据（零字节产物判定 / 失败重试 / `--norestore` 均适配层更弱）。
5. **`类型转换` 10 项说明同模板**：报告结论即「仅登记不改」。
6. **本机系统名双口径**：`_归一化系统名` 是历史锁兼容补丁，**在有正式原语之前不要直接删**（先补 `本机系统名()`）。
7. **同名平铺件本身**：5 处并存是「拆包期自举壳」的当前形态，**真实现已唯一在包内 `核心.py`**；只有那 1 个 `兼容平铺模块.py` 是死件。

---

## 五、Windows 兼容阻碍点（原报告未逐条证真，以下为报告原结论，未被推翻）

**收口层已建成（41/46 个 `Popen` 调用点已接上），生产代码 0 处 `start_new_session`。
换到 Windows 起不来的原因不是收口层没做完，而是三处独立硬阻断：**

| 级别 | 条数 | 内容 |
|---|---:|---|
| 阻断启动 | 6 | 五个入口的 `平台适配.脚本入口准入()`；33/33 份 `依赖锁.json` 全写死 `macOS/arm64`（`强制校验.py:240-274` 规则 5） |
| 阻断某能力 | 14 | 进程枚举/OCR 转换/浏览器自动化/密钥/资源采样/任务调度等在该平台不可用或恒失败 |
| 仅告警 | 5 | 读数缺失、编码或路径语义不一致 |

---

## 六、仓库洁净度（原报告未逐条证真）

**一句话**：本仓无「病入膏肓」问题（无制品/构建产物进仓、无第二套归档顶层、无 `工程缓存/` 误入库、
无大文件残留）；17 个顶层目录 13 个合规。问题集中在**三类「纸面已收口、现场还没清」的尾巴**。

- **P0（2）**：① `DGX-Spark…素材/` 下 4 个一次性脚本（含写死网关凭证取法与 `POST 40007` 调用体的 `.sh`）；② `开发工具/性能基准/结果/` 两个 JSON 入库（违反该包说明第 4 行「新结果默认不要入库」）。
- **P1（6）**：临时文档 1 份过程稿 · 三份已取代方案仍留全仓批次表 · 两份自称唯一编排入口 · 技能库 SKILL.md 指已删物 · 限流边界说明指已删底稿目录。
- **P2（5）**：`/tmp` 已删路径引用 · 附表内容重叠 · 五项方案缺五态声明 · 十六份方案未登记进文档地图 · 目录约定漂移 7 处。
- **不是缺陷（9 条）**：见原报告第五节。

---

## 七、缺陷热点巡诊（原报告未逐条证真）

| 结论 | 内容 |
|---|---|
| 1 | `运行发布门禁.py` 是**唯一超一级热点**（修复 32 次、3336 行、热度比第二名高 8.9 倍）⇒ 第一优先拆，拆完单独跑回归 |
| 2 | 本轮 15 路拆完后，「高频且 >800 行且未派发」= **0**（选点正确） |
| 3 | 真正「天生易错区」是**测试文件和小文件**（修复率 ≥0.7 的是判据反复调，**该做契约固化而不是拆分**） |
| 4 | 根因分布（637 次归类）见原报告 |

---

## 八、平台判断越界·深度审计与底座升级清单

**结论**：机器门禁全绿，无阻断级问题；警告级 3 条、观察级 5 条、扩容建议 11 条。
（`AGENTS.md:193` 曾把本文件写成硬规则「方案全文见」—— 该引用已随本次收敛一并作废，
后续以 `开发文档/规范/批量开发流程与分级验证.md` 为准。）

---

## 九、本汇总的取证方式

- 三路只读证真：`开工-20260920-003000-z1aa`（空转与死代码）· `-z1bb`（文档事实源分叉）·
  `-z1cc`（重复腿/能力唯一性/依赖分层/平台越界）。三路均**未改任何仓库文件**，
  临时脚本只落 `/private/tmp/系统平台临时脚本_20260918/`。
- 报告基线 `836e9066`，证真基线 `cf2e5a57`（相差约 60 个提交）—— 这是多条
  「已修/形态变了」的直接原因。
- **行号会漂**：本文所有落点以**符号名**为准，行号仅作线索。
