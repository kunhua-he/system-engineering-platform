# 系统工程平台 · 活跃租约锁洞与提交门禁实测（2026-09-24）

> 开工ID：`开工-20260924-013434-677b`
> 口径：**分析件**（人工类）；本文只记本轮实测因果，不新增设计结论
> 上游：`开发文档/分析/审计落点_写入授权越界_20260924.md`（D3）＋本批 `开发文档/分析/网关fd耗尽收口与提交_20260924.md`（E3）
<!-- 机器管理｜类型：分析｜生成器：人工 -->

---

## 一、实测序列（一条链，每一步都有读数）

| 步 | 动作 | `仓库锁 --查看` 读数 |
|---|---|---|
| 1 | 提交前 | 受管 5687｜已锁 5675｜**未锁 12** |
| 2 | `仓库锁.py --补齐` | 新锁 12 项 ⇒ **未锁 0**（提交通过，`7570337d`） |
| 3 | `开工即占` 认领 `开发文档/分析/网关fd耗尽收口与提交_20260924.md` | — |
| 4 | 经 MCP `写入文件` 写该件 | 受管 5686｜**未锁 2** |
| 5 | `仓库锁.py --补齐` | 新锁 2 项 ⇒ **未锁 0**（提交通过，`80eb7715`） |
| 6 | 再次 `开工即占` 认领另一条路径 | — |
| 7 | `开工编排.收工释放`（释放 `114103d196dd42a5`） | 受管 5688｜**未锁 0**（**没跑 `--补齐`，自己回到 0**） |

## 二、因果结论（本件唯一的增量）

**「未锁 > 0」的触发点是「存在活跃写租约」，不是「写腿写坏了」。**

- `申请文件租约` 认领成功那一刻就 `解锁供写入(目标 + 其直接父目录)`；
- 回锁（`回锁`）**只在租约终结时**发生 —— 两条终结路径：`开工编排.收工释放`、或下一次认领时的懒回收（`回收过期文件租约`）；
- ⇒ **活跃租约存续期间，那两条路径（目标＋父目录）一直是未锁态**，与写没写过无关（第 4 步“写了一次”只是让它更显眼）；
- ⇒ 第 7 步是关键对照：**同一个仓、同一条腿，只靠 `收工释放` 就从 2 回到 0，没跑 `--补齐`**。

**推论（对批F 的定位）**：批F 的 `写腿收口锁态()` 修的是「**窗口退出后仍未锁**」那一档（写腿自己造成的缺口），
**管不到「认领时解开的」那一档** —— 后者本来就该由租约终结收口。两件事不冲突，但**只有两件都成立时锁才是完整的**。

## 三、直接后果（对「子代理 mcp 经常失败」的一条补因）

**只要并行维修期存在活跃租约，`pre-commit` 就必然拒提交**（判据就是「未锁 > 0」），
而 `git commit` 是每个子代理收工的最后一步 ⇒ 在批量作业里表现为「子代理跑到最后一步莫名失败」。

**已存在的正确处置**（不是新机制，只需照用）：

1. 提交前先 `开工编排.收工释放` 释放自己的租约（**首选**，会把目标＋父目录一并回锁）；
2. 若仍有缺口（别人留下的），跑 `python3.14 运维脚本/仓库锁.py --补齐`（只补「该锁却没锁」）；
3. **两条路都能用，但不要用 `git commit --no-verify`**（钩子拦不住它，见钩子自述「可达上限①」）。

## 四、本件边界

- 本文**未**改任何代码；只记实测与因果。
- 第 4 步的 `未锁 2` 与第 1 步的 `未锁 12` **未逐条复原归属**（与 D3 旧账同一处境）；本件的结论只靠第 7 步那条**因果对照**，不靠归属推断。
- 本件是**分析类**件，条目销账后按分析类口径删除。
