<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R单据 · R-2 解释器口径收口（U-5）

**开工ID**：`开工-20260924-105431-45d5`　**日期**：2026-09-24　**仓库**：`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`
**裁决原话**：「如果其他 python 也能支持的，也行。这个不强制也可以，只是 3.14 是开发过程使用的，可能更稳定。」
**落成口径**：不强制 3.14 —— 3.14 是**优先项**、不是**强制项**；凡写死 `python3.14` 作可执行或解释器常量的落点，一律归队唯一节点并允许回落。
**本批纪律**：不跑编译、不 git 提交；未改本表以外的文件。

## 一、唯一节点与口径

| 项 | 结论 | 依据 |
|---|---|---|
| 权威节点 | `sys.executable` | 批Q 清单 U-5「4 种写法 → `sys.executable`」；全仓既有权威（P-12 已按此清 1 类） |
| 3.14 的地位 | 优先项（不是强制项） | 华哥 2026-09-24 裁决 |
| 进程内落点 | 直接归队 `sys.executable`（本进程解释器天然可回落） | 不新写选择逻辑 |
| 只能写名字的落点 | 按「优先 3.14、取不到回落 `python3`」 | 白名单**禁绝对路径**，`sys.executable` 进不去；见 §2 第 18–22 行 |

## 二、逐条落点表

类 1 = 可执行路径／解释器常量（真起进程或准入白名单）；类 2 = 文档示例／口径文本；类 3 = 测试期望值／判据。

### 2.1 类 1 · 已修（22 条）

| # | 文件:行（改后） | 改前 | 改后 |
|---|---|---|---|
| 1 | `开发工具/开发编译口/编译口.py:992` | `_跑(["python3.14", "-m", "py_compile", *目标], 超时秒=120)` | `_跑([解释器, …])` |
| 2 | `开发工具/开发编译口/编译口.py:1011` | 同形（全仓超时秒） | `解释器` |
| 3 | `开发工具/开发编译口/编译口.py:1118` | `"python3.14" -m 开发工具.能力id冻结基线门禁` | `解释器` |
| 4 | `开发工具/开发编译口/编译口.py:1125` | `"python3.14" -m py_compile <坏样本>` | `解释器` |
| 5 | `开发工具/开发编译口/编译口.py:1131` | `"python3.14" -m py_compile <编译口本体>` | `解释器` |
| 6 | `开发工具/开发编译口/编译口.py:1553` | `"python3.14" -m 开发工具.全量重算摘要 --只报` | `解释器` |
| 7 | `开发工具/开发编译口/编译口.py:1608` | `["python3.14", "-m", 门禁清单[序号]["模块"], …]` | `解释器` |
| 8 | `开发工具/开发编译口/编译口.py:1734` | `["python3.14", "-m", "unittest"], 点名, …` | `解释器` |
| 9 | `开发工具/开发编译口/编译口.py:58` | （无此常量） | 新增 `解释器 = sys.executable` + 4 行理由注释 |
| 10 | `开发工具/开发编译口/生成物联动.py:204/215/228` | `_命令` 原样返回 argv（登记表里的 `python3.14` 直跑） | 新增 `_是解释器名` / `_归一出解释器`：argv[0] 是裸解释器名 ⇒ 换 `sys.executable` |
| 11 | `平台控制面/备份恢复/恢复编排器.py:116` | `解释器 = "python3.14" if shutil.which("python3.14") else sys.executable` | `subprocess.run([sys.executable, …])`（删掉 which 分支） |
| 12 | `模块库/项目文档/实现/文档生成器.py:57` | `首选解释器 = "python3.14"` / `兜底解释器 = "python3"` | `解释器 = sys.executable` |
| 13 | `模块库/项目文档/实现/文档生成器.py:38` | （无 sys） | `import sys` |
| 14 | `模块库/项目文档/实现/文档生成器.py:122-136` | 「首选找不到才用兜底重试」分支 | 删除重试分支（第二套选择逻辑） |
| 15 | `开发工具/发布门禁/发布治理.py:181` | `命令列表 = ["python3.14", str(发布门禁脚本)]` | `[sys.executable, …]` |
| 16 | `开发工具/发布门禁/发布治理.py:18` | （无 sys） | `import sys` |
| 17 | `开发工具/能力网关/启动能力网关.sh:20-28` | `exec python3.14 -m 开发工具.能力网关.能力网关 …` | `command -v` 回落链（`python3.14` → `python3`）+ 都取不到则报错退 1 |
| 18 | `支持库/后端/测试支持库/实现/测试支持库.py:31` | `允许可执行 = ("python3.14",)` | `("python3.14", "python3")`（**不强制**） |
| 19 | `支持库/后端/测试支持库/实现/测试支持库.py:269` | `_拒绝("开发期回归只允许 python3.14 -m <精确模块>")` | 由 `允许可执行` 拼出实际白名单 |
| 20 | `支持库/后端/测试支持库/实现/测试支持库.py:321` | `if list(命令) == list(唯一发布命令)` | `if 命令[1:] == 唯一发布命令[1:]`（按**入口**识别） |
| 21 | `技能库/技能/验证编排/scripts/验证编排.py:248` | `list(命令) == list(唯一发布命令)` | `list(命令)[1:] == list(唯一发布命令)[1:]` |
| 22 | `开发工具/发布门禁/发布治理.py:252-253` | `条目.get("命令") != list(唯一发布命令)` | 加类型守卫 + 按**入口**比对 |

**三处 `唯一发布命令` 字面逐字保持不动**（`测试支持库` :37 / `验证编排` :44 / `发布治理` :51）——它们是**规范写法**（展示用，仍写优先项 3.14），识别改按入口，故三处不再需要同步改，跨模块契约不漂移。

### 2.2 类 2 · 口径文本已同步（7 条）

| # | 文件:行 | 改前 | 改后 |
|---|---|---|---|
| 23 | `支持库/后端/测试支持库/说明/使用说明.md:62` | 「可执行程序只允许 `python3.14`」 | 「只允许 `python3.14` 或回落的 `python3`（**优先 3.14、允许回落**…）」 |
| 24 | `支持库/后端/测试支持库/说明/设计说明.md:80` | 「同源（本包完全对齐，未放宽）」 | 「同源（…唯一放宽：解释器不强制 3.14…）」 |
| 25 | `支持库/后端/测试支持库/说明/设计说明.md:86` | 「**不放宽**任何一条拒绝规则」 | 「…（**唯一例外**：解释器白名单放宽 + 唯一发布命令改按入口识别）」 |
| 26 | `开发文档/规范/任务类型规范.json:28` | 「全线 python3.14（本机 python3 是 3.9.6，不是本项目解释器）」 | 「解释器优先 python3.14、允许回落（…）」 |
| 27 | `模块库/开工编排/验证夹具/任务类型规范.json:28` | 同上（与真源逐字相同） | 同上（保持与真源逐字相同） |
| 28 | `开发工具/git钩子/pre-commit:114` | warn「找不到 python3.14（本钩子放行…）」 | warn「找不到可用的 python 解释器（本钩子放行…）」 |
| 29 | `开发工具/任务记忆/派活任务信.py:55` | 「自检标志串：`python3.14` / `不许 sleep` / `走平台异步腿`。」 | 追加「（`python3.14` 是**优先项**不是**强制项**…标志串只判「命令环境段在位」）」 |

真源核实：`任务类型规范.json` 的**真源是 `开发文档/规范/任务类型规范.json`**（`开发工具/任务记忆/派活任务信.py:28` 与 `模块库/开工编排/实现/任务类型规范.py` 都读它）；`模块库/开工编排/验证夹具/任务类型规范.json` 是**验证夹具**，与真源 `diff` 为空 ⇒ 两条必须同改，本批已同改。

### 2.3 类 3 · 测试期望值／判据（**未改**，已核兼容）

| 落点 | 类 | 不改的理由 |
|---|---|---|
| `测试中心/支持库/测试_测试支持库.py` 23 处 | 3 | 期望值仍成立：验证计划仍以 3.14 为优先项输出；拒绝样例（绝对路径 / 逃逸 / shell 元字符 / 多参）一条没放宽 |
| `支持库/后端/测试支持库/验证场景.json` 16 处 | 3 | 同上（场景期望值 = 优先项形态） |
| `测试中心/第一批维修/测试_统一编译器.py` 3、`测试中心/模块库/测试_自修复工具.py` 2、`测试中心/开发工具/测试_项目编译器.py` 1 | 3 | 测试夹具里的固定命令样例，与本次收口面无关 |
| `开发文档/项目证据/底座升级/B0-浏览器自动化基线.json:60` | 3 | 历史证据快照（存档，非判据） |
| `开发工具/任务记忆/派活任务信.py:410/435` 标志串 `"python3.14"` | 3 | 存在性锚点；更新后的真源文本仍含该串 ⇒ 判据照旧通过 |
| `开发工具/契约编译/消费者契约.py:179` | 3 | 它是**统一入口**白名单的样例，本就含 `python3`（不强制） |
| `平台控制面/统一入口/核心.py:63` | 3 | 本就不强制（`{"python3.14", "python3", "unittest", "pytest", "true"}`） |

### 2.4 未改 · 边界（见 §5）

`开发文档/规范/生成物登记表.json` 24 处 argv[0]、`.mcp.json` / `opencode.json` / `zcode.json` 3 处接入面启动配置、`开发工具/环境自检/自检_解释器与平台.py`。

## 三、`rg` 前后计数

判据 A（**执行位**：argv[0]／解释器常量／exec）：
`rg -n '(\[|,)[[:space:]]*"python3\.14"|= "python3\.14"|"python3\.14" if|exec python3\.14' --glob '*.py' --glob '*.json' --glob '*.sh' -g '!工程缓存/**'`

| | 改前（HEAD） | 改后（工作区） | 差 |
|---|---|---|---|
| 全仓执行位 | **73** | **62** | −11 |
| ├ `开发工具/开发编译口/编译口.py` | 8 | **0** | −8 |
| ├ `开发工具/能力网关/启动能力网关.sh` | 1 | **0** | −1 |
| ├ `开发工具/发布门禁/发布治理.py` | 1 | **0** | −1 |
| ├ `模块库/项目文档/实现/文档生成器.py` | 1 | **0** | −1 |
| ├ `平台控制面/备份恢复/恢复编排器.py` | 1 | 1 | 0（改后那 1 处是**本批新增注释**里引用的改前写法，非可执行位） |
| ├ `开发文档/规范/生成物登记表.json` | 24 | 24 | 0（**数据侧**，运行时经 `_命令` 归一为 `sys.executable`） |
| └ 其余（测试中心 29 / 派活任务信 2 / 消费者契约 1 / B0 基线 1 / 验证编排 1 / 测试支持库 3） | 36 | 36 | 0（类 3 或规范写法，见 §2.3） |

判据 B（全仓 `.py/.json/.sh` 的 `python3.14` 行数，排除 `工程缓存/`）：**559 → 559**。该判据含大量文档示例，且本批新增注释/口径文本显式写出「优先 3.14」，字样回填与删除量相抵，**不单独作缺陷判据**（改前基线同法测得 559）。
判据 C（题面给的口径 `rg -n "python3(\.14)?"`，含裸 `python3`）：**619 → 622** —— 同上，增量全部来自本批新增的「允许回落 `python3`」口径文本与注释，不是新增写死点。

## 四、回落实测读数（真调用）

**① `测试支持库.验证命令白名单`（源码树真调）**

| 命令 | 改前 | 改后（实测） |
|---|---|---|
| `["python3.14","-m","测试中心.支持库.测试_测试支持库"]` | 允许=真 形态=unittest | 允许=真 形态=unittest |
| `["python3","-m","测试中心.支持库.测试_测试支持库"]` | 允许=假「可执行程序不在白名单: 'python3'」 | **允许=真 形态=unittest** |
| `["python3.14","开发工具/发布门禁/运行发布门禁.py"]` | 允许=真 形态=正式发布 | 允许=真 形态=正式发布 |
| `["python3","开发工具/发布门禁/运行发布门禁.py"]` | 允许=假 | **允许=真 形态=正式发布** |
| `["python3.14","/tmp/随手脚本.py"]` | 允许=假「禁止绝对路径」 | 允许=假「禁止绝对路径」（**拒绝面未放宽**） |

**② `发布治理._正式证据问题`（入口比对）**

- 回落解释器证据（`命令=["python3", "开发工具/发布门禁/运行发布门禁.py"]`）⇒ 不再报「正式发布证据必须绑定唯一发布命令」（推进到后续检查「正式发布证据场景覆盖缺失」）
- 换入口证据（`命令=["python3", "开发工具/随便跑.py"]`）⇒ **仍报**「正式发布证据必须绑定唯一发布命令」（判据没被放宽成不判）

**③ `生成物联动._命令` 登记表 argv[0] 归一**（经 MCP `系统核心支持库.进程管理.执行命令` 真跑）

```
登记表原文 : ['python3.14', '-m', '开发工具.能力id冻结基线门禁']
归一后     : ['/opt/homebrew/opt/python@3.14/bin/python3.14', '-m', '开发工具.能力id冻结基线门禁']
非解释器条目: ['git', 'status']（原样，不动别的 argv 位）
```

**④ 「取不到 3.14 时仍能跑」真跑**（经 MCP 异步腿；干净 `PATH=/usr/bin:/bin`）

```
干净 PATH 下 which python3.14 = None          ← 取不到 3.14
干净 PATH 下 which python3    = /usr/bin/python3
真跑（归一后命令）退出码 = 0
输出尾部：能力 id 冻结门禁通过：当前集 ⊇ 基线集，无 id 消失（只增不改成立）
```

⇒ **回落生效**：本机取不到 3.14 的 PATH 下，归一后的派生凭据命令照跑且退出码 0。

## 五、残余与边界

1. **制品未重建（本批最大残余）**：MCP 网关（`http://127.0.0.1:40007`，进程号 52871）跑的是 `工程缓存/制品仓库/平台客户端环境/平台客户端/平台客户端` 这棵**制品树**，与源码树已不一致（实测 `diff` 制品 ≠ 源码）。因此**经网关 `capability_call` 读到的仍是旧制品行为**（实测：`["python3", …]` 仍被拒）。本批纪律不跑编译 ⇒ 未重建制品；§4 的回落实测全部在**源码树**取证（带 `系统库网关凭证`，即 `脚本入口准入` 的白名单入口）。**制品重建后本批才在网关侧生效**。
2. `开发文档/规范/生成物登记表.json` 的 24 处 argv[0] 仍是字面 `python3.14` —— 数据侧写不出 `sys.executable`，靠 `生成物联动._命令` 运行时归一兜住（§4 ③④ 已实测）。若日后登记表要支持别的解释器名，只需 `_是解释器名` 判据已按 `python` / `python3*` 前缀覆盖。
3. `.mcp.json` / `opencode.json` / `zcode.json` 三处**接入面启动配置未改**：它们是**客户端**自己的启动命令，平台侧无法代它回落；且直接改成 `python3` 会落到本机 3.9.6（跑不动本项目），「回落」在那里反而有害。⇒ 保持写死，登记为接入面事项。
4. `开发工具/环境自检/自检_解释器与平台.py` 的「不是 3.14 即不支持」**未改**：`期望Python主次版本 = (3, 14)` 是**依赖锁『环境.Python』**口径（`强制校验` 规则 5 只比主次版本），属**环境指纹面**、不属解释器选择面；本批裁决不覆盖它。
5. `支持库/后端/测试支持库/实现/测试支持库.py` 的 `允许可执行` 放宽，是 `设计说明.md` 原「未放宽任何一条拒绝规则」的**唯一例外**，已在文档同步标注（§2.2 第 24/25 条）。
6. `生成物联动._归一出解释器` 只在 `_命令` 出口生效；`跑分片` 仍把 argv 拼成**命令字符串**再交 `执行命令集` ⇒ 若 `sys.executable` 路径含空白（Windows 常见），该拼接会拆错。本机（macOS，无空白）实测无碍；属**既有拼接脆弱性**，不在本批范围，登记待办。
7. 本批**未跑编译、未 git 提交**：`完整性摘要.json` 等派生件与契约漂移基线需在收口时由编译口／发布门禁重算（本批落点均为实现与文档，**未新增公开能力、未改能力 id**，`能力id冻结基线` 实测仍通过）。

## 六、收工信息

- 开工ID：`开工-20260924-105431-45d5`（先占 13 条，后补 `启动能力网关.sh` 与 `派活任务信.py` 各 1 条）
- 改动文件 14 个：`开发工具/开发编译口/编译口.py`、`开发工具/开发编译口/生成物联动.py`、`平台控制面/备份恢复/恢复编排器.py`、`模块库/项目文档/实现/文档生成器.py`、`支持库/后端/测试支持库/实现/测试支持库.py`、`技能库/技能/验证编排/scripts/验证编排.py`、`开发工具/发布门禁/发布治理.py`、`开发工具/能力网关/启动能力网关.sh`、`支持库/后端/测试支持库/说明/使用说明.md`、`支持库/后端/测试支持库/说明/设计说明.md`、`开发文档/规范/任务类型规范.json`、`模块库/开工编排/验证夹具/任务类型规范.json`、`开发工具/git钩子/pre-commit`、`开发工具/任务记忆/派活任务信.py`（+ 本单据）
- 语法自检：7 个 `.py` 全部 `py_compile` 通过；2 个 `.json` 解析通过；2 个 `.sh` `bash -n` 通过。
