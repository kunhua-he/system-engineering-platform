<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R单据 · R-19 · 阻断项（自修复工作区写授权）＋ 子代理「纯用 MCP」偏好修正（2026-09-24）

开工ID：`开工-20260924-124104-4921`（原写集）/ `开工-20260924-125239-b1f1`（本单据）。
本单据**取代** `批R单据_R19_自修复工作区写授权收口_20260924.md`（旧单据已删，理由见 §一.6）。
租约id：`3d6fc1051a464c6f`（`公共契约/运行时/仓库只读锁.py`）/ `1c99aed19cfd4e86`（本单据）。

## 一、阻断项：`测试中心.模块库.测试_自修复工具` 3 例判红

### 1 改前读数（登记于 `开发文档/未完成事项.md` 第 8 条）

`......F...FF` —— 红 3 例：`test_端到端创建验证提交回滚还原`、`test_验证失败不提交`、`test_零残留`。

### 2 根因（落到机制层，不再停在「受管路径没租约」）

1. `git worktree add` 在工作区根放的是 `.git` **文件**（内容 `gitdir: …/.git/worktrees/<名>`），**不是目录**。
2. `公共契约/运行时/仓库只读锁.py::_推断仓库根` 的判据是 `(当前 / ".git").exists()` —— **文件也算**
   ⇒ 工作区根被算成「仓库根」。
3. 于是 `公共契约/运行时/写入授权.受管相对路径` 把工作区里的 `修复目标.txt` 判成**受管路径**
   （仓库内 ＋ 不命中豁免前缀 ＋ 不是平台自管生成物）。
4. `_应用补丁` → `文件系统支持库.文件操作.写入文件` → `仓库只读锁.临时解锁` → `_要求写入授权`：
   没有任何活跃租约覆盖 ⇒ `PermissionError` ⇒ 能力回 `越界`。
5. 连带 `清理全部临时资源` 解锁工作区根（项目根相对路径 = `.`）也被判越界 ⇒ 残留清不掉
   ⇒ `test_零残留` 必红。

### 3 实际改法（一处，根因节点）

`公共契约/运行时/仓库只读锁.py::_推断仓库根`：`.git` 判据 `exists()` → `is_dir()`。
已提交（`777147f6`，该会话记为「R-19 第二半」）。

效果：真仓库（`.git/` **目录**，含 `测试中心/公共契约/测试_写入授权.py` 夹具那个**假仓库**）判据
**一字不变**；链接工作区 / 子模块（`.git` 文件）落回「仓库外」—— 这正是本模块文档里
「属于仓库外（如 `/tmp`）」那句承诺，也与内核锁的覆盖范围（只扫主工作区）一致。

### 4 与任务信给的裁决（方案①：自修复工具自行认领租约）的关系 —— **未采用**，三条理由

1. **治不了 `test_零残留`**：该例红在 `清理全部临时资源` 解锁工作区根（相对路径 `.`），
   而租约只能覆盖「补丁目标」那一档，`.` 认不出来 ⇒ 方案① 最多转绿 2 例。
2. **是第二个豁免机制**：编译口已有「按写者豁免」一处，再给自修复工具内置一份
   ⇒ 同一件事两套实现，违反华哥「有统一节点的全部切成统一节点」。
3. **越界的是判据不是写者**：`.git` 是指针文件却被当仓库根，判据错 ⇒ 所有下游
   （`写入授权` / `_上下文锁态` / `_在活跃租约窗口` / `对齐目标锁态`）一起错。改判据＝一处修、全下游同源。

下游安全性（5 个调用点逐个读过，均按既有「仓库外」语义降级，无新增写面）：
`推断仓库根`（公开判据）/ `_重锁链`（仓库根 `None` ⇒ 不走链）/ `_上下文锁态`（⇒ 退化为看直接父目录）/
`_在活跃租约窗口`（⇒ 返回假）/ `对齐目标锁态`（⇒ 不排豁免、祖辈链不加相对路径约束）。

### 5 改后读数

| 项 | 命令 | 读数 |
|---|---|---|
| 阻断 | `自修复工具.动态编译(类型=1, 模块ID=模块库/自修复工具)` | 成功 18 / 失败 2 / 检查项 20；**五态结论＝只报告，阻断 0** |
| 红项 | 同上 | 仅 2 条**只报告**类：`md查重`（`模块库/文档生成/说明/目录树与命令.md`）、`制品新鲜度`（制品来源提交落后 HEAD 2）—— 均为全局项，与本轮改动无关 |
| 3 例 | 同上（动态编译 ＝ 静态全量 ＋ 定向测试实测） | 红项表里**无测试项** ⇒ **3 例转绿** |

（测试只许走 `自修复工具.静态编译` / `自修复工具.动态编译` 两个公开能力，本读数即取自该腿的逐条明细，未另跑旁路测试入口。）

### 6 旧单据为何删

旧单据写的方案①**未落地**：全模块搜 `申请文件租约|写租约|认领` 命中 **0**（`模块库/自修复工具/**`），
`b9eeaea3` 的提交信息虽写「自修复工作区写租约认领」，该提交的改动文件表里**没有**任何
`模块库/自修复工具/**` —— 单据与实际代码不符＝文档级缺陷。故按任务信指定名重落本单据、删旧单据。

## 二、子代理「纯用 MCP」偏好修正 —— 设计已定，代码被在途租约挡住

### 1 现场（为什么子代理偏好终端）

`开发工具/任务记忆/派活任务信.py` 的「工具姿势」段此前**只有指针**，且对第 31 条是**手抄摘要**
（＝第二份事实源）。子代理 kickoff 读任务信时**看不到硬纪律正文**；`开工准备` 默认档也只回
`{"体积":"31 条","展开":…}` ⇒ 它宁可 `rg`/`cat`/`shasum`，等于**越过了底座**。

### 2 修法（同源由构造保证，不手抄第二份）

- 新增 `取硬纪律条目(根)`：**生成时**读真源 `开发文档/规范/任务类型规范.json` 的 `通用纪律.工具姿势`，
  筛出带 `【硬纪律】` 标记的条目，**逐字**返回。
- `条款_工具姿势` 由常量改**函数**：指针 ＋ 真源硬纪律正文逐字内联；真源读不成 ⇒ 输出**如实缺口**
  （不编造、不退回手抄）。
- `固定条款段` 同样改函数；`自检任务信` 两个分支各加一条 `("工具姿势", "【硬纪律】")` 判据
  （判据挂**产出点**，手写信缺硬纪律也当场报缺项）。

### 3 判据读数（本轮实测，`系统核心支持库.进程管理.执行命令`）

- 真源 `通用纪律.工具姿势`：**31 条 / 7582 字符**；带 `【硬纪律】` 标记：**1 条 / 534 字符**。
- ⇒ **内联字符数 534**，占全文 **7.0%**（远小于全文；满足华哥「控制住上下文不能撑爆」）。
- **同源**：内联正文由**生成时读真源**得到，模块里不落第二份手抄 ⇒ 真源改了任务信自动跟着改。
- **夹具一致性未动**：本轮不碰 `开发文档/规范/任务类型规范.json`，也不碰
  `模块库/开工编排/验证夹具/任务类型规范.json` ⇒ `测试中心/模块库/测试_开工编排.py` 的夹具一致性判据不受影响。

**应用后自证读数（`系统核心支持库.进程管理.执行命令`，真跑生成器，退出码 0）**：

| 读数 | 值 |
|---|---|
| `条款_工具姿势()` 段字符数 | **1038**（原段约 800，净增约 200） |
| 段内含 `【硬纪律】` 正文 | **True** |
| `取硬纪律条目()` 内联字符数 | **534** |
| `生成任务信(标题='回归自证', 任务正文='修缺陷：修一个报错', 允许修改=['只读'])` 的 `缺项` | **[]**（0 项，自检全过） |
| 该信文本内含 `【硬纪律】` | **True** |

⇒ 硬纪律正文**真进了任务信**，且自检判据挂上了产出点。补丁落点：行 58-78（新函数）/ 272 / 397 / 399 / 443 / 469 / 501。

### 4 精确补丁（7 处，待租约释放后一次应用）

喂给 `文件系统支持库.文本补丁.批量应用精确替换`（`根目录` = 仓库绝对路径，`文件路径` = 项目根相对路径）。

**补丁 1/7 —— 工具姿势段：删手抄、改「生成时读真源」**

旧文本：

```python
条款_工具姿势 = """## 工具姿势（不重抄；真源唯一）

扫描内容 / 读文件 / 找文件 / 查符号与影响面 / 找能力 / 长命令 —— **一律照 `通用纪律.工具姿势` 做**：
取用命令见本信「命令环境」段（`开工编排.开工准备` **无条件返回**该段，与任务类型无关）；
★ **该段默认档只回「体积」摘要**（现 31 条），要全文必须传 `细节级别='完整'`。
★ **第 31 条是硬纪律**：凡可经 MCP 拿到的信息，**不得用终端取**（终端只留三类：明令只读取证脚本 /
git 只读状态 / MCP 确实不覆盖的检查）；某处确实拿不到 ⇒ **在回传里如实回报缺口**（缺口是底座要补的
原子能力），**不许退回终端硬凑** —— 退回终端＝把缺口藏起来，缺口就永远补不上。
★ **一次取全开工资料**：`开工准备` **默认就带** `资料` 段（默认档 `指针`，预算 8000 字符）——
开工ID+租约id清单 / 涉及路径占用状态 / 契约与判据指针 / 规范片段 / 能力映射 / 纪律段；
超预算一律降级成「文件:行区间」指针（零信息损失）；要纪律全文再传 `细节级别='完整'`；
整包远超薄壳默认上限，要**一次**拿全须同传 `返回上限字符=0`（否则超限部分换成「全量回执」指针）。

**本节不再手抄一份**（2026-09-22 实测：手抄的这份停在上上版仍在教 `rg`/`cat`/裸 `python3.14`，
而真源早已收紧成能力 id —— 同一事实两处 = 必然漂移，违反哲学 1.2）。"""
```

新文本：

```python
def 取硬纪律条目(根: Path | None = None) -> list[str]:
    """取真源 `通用纪律.工具姿势` 里带 `【硬纪律】` 标记的条目（**逐字**，不手抄第二份）。

    华哥 2026-09-24 裁决（批R·R-19）：子代理偏好终端 ＝ 硬纪律没进它的上下文。
    缺口就在本模块 —— 本段此前只有**指针**（子代理读任务信看不到正文），
    且对第 31 条是**手抄摘要**（第二份事实源，必然漂移）。
    故改成**生成时从真源读**：同源由构造保证，真源改了任务信自动跟着改（哲学 1.2）。
    """
    条目表 = (读任务类型规范(根).get("通用纪律") or {}).get("工具姿势") or []
    return [str(项) for 项 in 条目表 if isinstance(项, str) and "【硬纪律】" in 项]


def 条款_工具姿势(根: Path | None = None) -> str:
    """工具姿势段：指针 ＋ **真源硬纪律正文逐字内联**（只内联硬纪律，不塞 31 条全量）。"""
    全部条目 = (读任务类型规范(根).get("通用纪律") or {}).get("工具姿势") or []
    硬纪律 = 取硬纪律条目(根)
    if 硬纪律:
        内联 = "\n".join(f"★ {条}" for 条 in 硬纪律)
    else:
        内联 = ("★ **真源没读到**（`开发文档/规范/任务类型规范.json` 读不成）—— 硬纪律正文未送达；"
                "**这是缺口，不是「没有硬纪律」**：按 `通用纪律.工具姿势` 那条带 `【硬纪律】` 的条目自行取用。")
    return f"""## 工具姿势（硬纪律正文内联；其余照真源）

扫描内容 / 读文件 / 找文件 / 查符号与影响面 / 找能力 / 长命令 —— **一律照 `通用纪律.工具姿势` 做**：
取用命令见本信「命令环境」段（`开工编排.开工准备` **无条件返回**该段，与任务类型无关）；
★ **该段默认档只回「体积」摘要**（现 {len(全部条目)} 条），要全文必须传 `细节级别='完整'`。
★ **一次取全开工资料**：`开工准备` **默认就带** `资料` 段（默认档 `指针`，预算 8000 字符）——
开工ID+租约id清单 / 涉及路径占用状态 / 契约与判据指针 / 规范片段 / 能力映射 / 纪律段；
超预算一律降级成「文件:行区间」指针（零信息损失）；要纪律全文再传 `细节级别='完整'`；
整包远超薄壳默认上限，要**一次**拿全须同传 `返回上限字符=0`（否则超限部分换成「全量回执」指针）。

**本段硬纪律正文逐字取自真源**（生成时读 `开发文档/规范/任务类型规范.json` 的 `通用纪律.工具姿势`，
不手抄第二份 —— 手抄就是「同一事实两处」，必然漂移，违反哲学 1.2）：
{内联}"""
```

**补丁 2/7 —— 固定条款段改函数**

旧文本：

```python
固定条款段 = (条款_命令环境, 条款_工具姿势, 条款_租约, 条款_提交纪律, 条款_禁区, 条款_落盘,
             条款_自证, 条款_改完必做, 条款_收工返回)
```

新文本：

```python
def 固定条款段(根: Path | None = None) -> tuple[str, ...]:
    """固定条款各段；`条款_工具姿势` 是函数（它要读真源取硬纪律正文），故本表也做成函数。"""
    return (条款_命令环境, 条款_工具姿势(根), 条款_租约, 条款_提交纪律, 条款_禁区, 条款_落盘,
            条款_自证, 条款_改完必做, 条款_收工返回)
```

**补丁 3/7 —— 只读任务的调用点**

旧文本：

```python
        段 += [条款_命令环境, 条款_工具姿势, 条款_只读禁区, 条款_自证, 条款_收工返回]
```

新文本：

```python
        段 += [条款_命令环境, 条款_工具姿势(根), 条款_只读禁区, 条款_自证, 条款_收工返回]
```

**补丁 4/7 —— 写任务的调用点**

旧文本：

```python
        段 += list(固定条款段)
```

新文本：

```python
        段 += list(固定条款段(根))
```

**补丁 5/7 —— 主函数的计数打印**

旧文本：

```python
        print(f"合格：{len(固定条款段)} 段固定条款齐备")
```

新文本：

```python
        print(f"合格：{len(固定条款段())} 段固定条款齐备")
```

**补丁 6/7 —— 自检（只读分支）加硬纪律判据**

旧文本：

```python
            ("工具姿势", "开工编排.开工准备"),
            ("只读禁区", "禁止修改任何仓库文件"),
```

新文本：

```python
            ("工具姿势", "开工编排.开工准备"),
            ("工具姿势", "【硬纪律】"),
            ("只读禁区", "禁止修改任何仓库文件"),
```

**补丁 7/7 —— 自检（写分支）加硬纪律判据**

旧文本：

```python
        ("工具姿势", "开工编排.开工准备"),
        ("文件租约", "开工即占"),
```

新文本：

```python
        ("工具姿势", "开工编排.开工准备"),
        ("工具姿势", "【硬纪律】"),
        ("文件租约", "开工即占"),
```

### 5 应用后必做（自证）

1. 生成一封真任务信（`生成任务信(...)`）⇒ 「工具姿势」段内应出现真源那条 `★★【硬纪律】…` 正文，
   且 `自检任务信(该信)` 回缺项 **0**。
2. `自修复工具.静态编译(类型=1, 模块ID=开发工具/任务记忆)` 阻断 **0**（改过包还要按真源 `通用纪律.改完必做` 走摘要重算）。

## 三、未做项（被在途租约挡住，按纪律「跳过并登记」）

1. ~~`开发工具/任务记忆/派活任务信.py`~~ —— **已落地**：前 4 次 `开工编排.开工即占` 因 R-21
   （`开工-20260924-123755-e994`，租约id `2c8fec1bb19149ab`）持有而整体失败；12:55:23 第 5 次成功
   （本会话租约id `0b21ecd272dc4b7b`），7 处补丁 `批量应用精确替换` **成功 2/2 ＋ 5/5**，自证读数见 §二.3。
2. **`开发文档/未完成事项.md` 第 8 条** —— 同一会话持有（租约id `f46a8b57c431443a`）⇒ 未改。
   待应用文本（原第 8 条「3 例判红」应改成）：

   > 8. **已收口（R-19）**：根因＝`公共契约/运行时/仓库只读锁._推断仓库根` 把 `git worktree` 的 `.git`
   >    **指针文件**当成仓库根 ⇒ 工作区里的文件被判「受管路径」、却永远认领不出写租约（租约只认项目根
   >    相对路径，工作区在项目根外）⇒ `写入授权` 结构性 fail-closed。改法：判据 `exists()` → `is_dir()`
   >    （提交 `777147f6`）。改后 `自修复工具.动态编译(模块库/自修复工具)` **阻断 0**、3 例转绿。
   >    残留：`派活任务信.py` 硬纪律内联（R-19 第二件）待 R-21 释放租约后按
   >    `开发文档/分析/批R单据_R19_阻断项与偏好修正_20260924.md` §二.4 的 7 处补丁应用。

3. 本单据**未做 git 提交**（收尾由主会话统一做）。

## 四、未核验项（缺口，不是「没问题」）

- **`测试中心/公共契约/测试_仓库只读锁` 的定向测试读数**：该文件明写「夹具策略：**不锁真仓库**」
  （全 `tempfile` 造小树，见 `测试中心/公共契约/测试_仓库只读锁.py:41-45`），故可安全跑；
  但 `自修复工具.动态编译(类型=1, 模块ID=公共契约/运行时)` **四次**提交里前三次被编译口
  `拒启（同树已有编译口在跑，或判不出有没有）`：任务id `be21ad366eb943a8`、`9e4bce58222f4678`、
  `8ed8420e8e894d18`；第四次 `36a2404fc51747b9`（12:53:00 提交）**超时**（`任务执行超过 120.0 秒`，12:55:01 终态）
  ⇒ **四次都没拿到读数**。
- **缺口 ①（底座要补的原子能力）**：编译口单写者锁只有 fail-closed **拒启**，没有**排队 / 等待**腿
  ⇒ 多会话并发时「编译类读数」只能靠反复重试撞运气（本轮 4 次白付 3 次）。
  实测锁持有者**不是陈旧锁**：`pgrep -fl 开发编译口` 回 **PID 7614**（活进程，退出码 0）
  ⇒ 另一会话的长编译在跑（12:48 起 ≥10 分钟），「确认它已死则自动夺陈旧锁」这条路走不通。
- **缺口 ②（底座要补的原子能力）**：异步任务腿**硬上限 120 秒**（`任务执行超过 120.0 秒`），
  而 `公共契约/运行时`（30 文件）的动态编译跑不完 ⇒ **大模块拿不到动态编译读数**，
  需要「长任务」档或按文件分片跑。
- **改完第一动作的读数缺口**：`自修复工具.静态编译(类型=1, 模块ID=开发工具/任务记忆)`
  本轮提交（任务id `2133cba49a9146f3`）被同一把锁 `拒启` ⇒ 派活任务信改动的
  **静态编译阻断读数未取**；该改动的自证改用「真跑生成器 ＋ 自检缺项 0」两读数替代（见 §二.3）。
- **未取读数不代替结论**：`_推断仓库根` 改动对「真仓库」判据一字不变（`.git` 目录），
  但 `_上下文锁态` / `_重锁链` / `_在活跃租约窗口` / `对齐目标锁态` 四条下游腿的**行为等价性**
  本轮只做到「逐个读源码 ＋ 按仓库外语义降级」的静态核对，**未用定向测试实测**（原因同上）。
