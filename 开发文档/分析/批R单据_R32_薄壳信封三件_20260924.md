<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R单据 · R-32：薄壳「信封」三件（路径基准统一 ＋ 列表项结构回带 ＋ 看键档）

- 开工ID：`开工-20260924-164022-44f2`（17 条路径）／`开工-20260924-164355-e0f1`（2 份 使用说明.md）；两次失败的 `开工即占`（`开工-20260924-163911-3f7c`／`开工-20260924-164215-5dca`）未产生租约。
- 基线提交：`aaa2f26243177159657e586495b3dc27fb21fa27`（本批改动**未 git 提交**，收尾由主会话统一做）。
- 一句话：**第一件、第二件已在源码层完成并各自取得改前/改后读数；第三件（看键档）两条路径双双被 R-33 占用，未落一行，改为「具体改法」登记。**

## 〇、完成状态

| 件 | 源码层 | MCP 面判据 |
|---|---|---|
| 一 相对路径默认基准统一 | 完成（已落盘） | 待「重建制品 ＋ 重启网关」后复验 |
| 二 `参数不合法` 回带列表项结构 | 完成（已落盘） | 同上 |
| 三 看键档 | **未做（阻断登记）** | — |

**为什么三件的 MCP 面判据都不能在本批给出**：网关跑的是 **13:46 的制品装配**。平台门禁自己把这条写成了红项（原文）：`制品落后 HEAD 1 个提交（阈值 0，制品来源提交 81cc618d1cf9e3916e1636f5b2de315f7ccd8769）—— 网关跑的是旧装配，须重建制品 + 重启网关 ⇒ 网关跑的是旧装配：源码已改而运行态未改，须「重建制品 + 重启网关」后再复验读数`。现场实证两条：① `工程缓存/制品仓库/平台客户端环境/平台客户端/平台客户端/公共契约/运行时/平台适配/路径解析.py` 仍 **48 行**、无 `本次调用基准变量`；② 同树的 `支持库/后端/文件系统支持库/内容检索/能力定义.json` 仍 `"版本": "1.1.3"`（源码已是 1.1.4）。故本批的 live MCP 读数只能给「改前」，改后读数走**源码树直调**（cwd 刻意设成制品内层根，用来证明起作用的是基准而不是 cwd）。

## 一、相对路径默认基准统一（完成）

### 改了哪些文件

| 文件 | 改法 |
|---|---|
| `公共契约/运行时/平台适配/路径解析.py` | 新增 `本次调用基准变量`（`contextvars.ContextVar`，多请求并发不互踩）＋ `设置本次调用基准()`（收 `str`/`Path`；其余类型一律**清空**，不猜语义不抛异常）；`解析路径` 生效根由**三级扩为四级**：显式 `根` → **本次调用基准** → 环境变量 `系统平台_项目根` → 进程 cwd。**不设第 2 级时逐字退回改前** |
| `公共契约/运行时/平台适配/__init__.py` | 导出 `设置本次调用基准`（含 `__all__`） |
| `运行核心/统一网关/核心处理/网关执行面.py` | `执行面._执行` 入口第一行：`设置本次调用基准(_生效解析根(请求.参数.get("项目根")))` —— 派发前**唯一**握着顶层 `项目根` 的时刻，有根设根、无根/空串**置空**（不残留上一次） |
| `支持库/后端/文件系统支持库/内容检索/能力定义.json` | `1.1.3 → 1.1.4`：`根目录` 必填 `true → false`、默认值 `null → ""`、说明与 `空值` 口径同步（省略/空串 ＝ 调用方 `项目根`） |
| `支持库/后端/文件系统支持库/内容检索/实现/内容检索.py` | 形参 `根目录: str \| None = None → 根目录: str = ""`（与契约对齐，消「契约漂移」阻断）；`根 = Path(解析路径(根目录文本 or ".")).resolve()` —— 空串用 `"."` 表达「基准本身」，**不自写基准** |
| 内容检索 派生件（`__init__.py` 注册镜像／`能力契约/参数契约.json`／`包声明.json`／`完整性摘要.json`） | 同批刷（走编译/生成腿，非手改） |
| `支持库/后端/文件系统支持库/文本补丁/能力定义.json` | `1.4.0 → 1.4.1`（本件属第二件：补 `编辑列表.项结构`）＋ 派生件同批刷 |

**★ 本批自查修掉一个自伤（值得单列）**：`网关执行面.py` 初版写成 `self._生效解析根(...)`，而 `_生效解析根` 是 `运行核心/统一网关/核心处理/网关参数校验面.py` 的**模块级函数**（`执行面` 类只有 `_执行`，宿主不会把它变成方法）⇒ 一旦随制品上线就是 `AttributeError`（整条网关调用面挂）。已改为**模块级函数调用 ＋ 显式 import**；复验：`import 运行核心.统一网关.核心处理.网关执行面` 成功（无循环导入）、`python3.14 -c ast.parse` 语法 OK。

### 改前 / 改后读数

判据 1 —— `批量应用精确替换` **不传 `根目录`**（`编辑列表=[{文件路径:'公共契约/运行时/平台适配/路径解析.py', 旧文本:'import contextvars', 新文本:'import contextvars'}]`、`写入=假`、`原子=真`）：

- **改前（live MCP，原文）**：`错误码=批量未应用`；`失败清单[0].文件路径=/Users/hekunhua/Documents/Agent/PHP/系统工程平台/工程缓存/制品仓库/平台客户端环境/平台客户端/公共契约/运行时/平台适配/路径解析.py`；`错误说明=文件不存在: <同上>`。
- **改后（源码树直调，基准＝调用方项目根，cwd＝制品内层根）**：`成功=True 错误码=空 全部成功 写入=假`（键：`总数/成功数/失败数/全部成功/原子/写入/已写入/文件数`）。
- 同一形态的另一条（`文件路径='开发文档/项目说明.md'`）改前同样 `文件不存在` 落制品镜像根。

判据 2 —— `正则搜索` **不传 `根目录`**（`路径='开发文档/分析'`）：

- **改前（live MCP，原文）**：`参数不合法：能力 文件系统支持库.内容检索.正则搜索 缺少必填参数 根目录（本能力参数（*=必填）：根目录*、模式*、路径、文件模式、最大匹配数、上下文、忽略大小写、排除列表、紧凑返回）`。
- **改后（源码树直调）**：`成功=True 搜索根=开发文档/分析 匹配数=5`。
- 对照（不给基准时仍是改前形态，证明「不设即退回」）：`错误码=目录不存在 错误说明=搜索路径不存在: <制品内层根>/开发文档/分析`。

判据 3 —— **已传 `根目录` 的调用行为逐字节不变**（同输入、只切基准两态）：`无基准: 成功=True 匹配数=5`｜`有基准: 成功=True 匹配数=5`｜`值 逐字节一致=True`。

判据 4 —— **基准不残留**（网关长活进程的关键判据）：`设置本次调用基准(源码根)` 后 `解析路径('开发文档', None)` ＝ 源码根；`设置本次调用基准(None)`（或空串）后 ＝ 制品内层根/开发文档（退回 cwd，不是上一个值）。

## 二、`参数不合法` 回带列表型参数的项结构（完成）

### 改了哪些文件

| 文件 | 改法 |
|---|---|
| `运行核心/统一网关/协议/类型规格.py` | 新增 `_项结构清单()`（渲染成一行 `编辑列表[文件路径*(文本型)、…]`）＋ `_加项结构尾注()`；接在 `校验能力参数` 的**三处「调用方自己传错」**返回上（必填缺失 2 处 ＋ 类型不符 1 处）。契约自身缺陷两类（非正式类型名／未知类型）**不加**尾注 |
| `支持库/后端/文件系统支持库/文本补丁/能力定义.json` ＋ `能力契约/参数契约.json` | `编辑列表` 补 `项结构`：`文件路径*`／`旧文本*`／`新文本*`（文本型）、`起始行`／`结束行`（整数型）、`预期旧文本摘要`／`预期文件摘要`（文本型） |
| `公共契约/基础类型/字段名册.py` | 新增 `项结构` 字段口径：列表型参数的逐项结构，每项 `{名称, 类型, 必填}` |

### 改前 / 改后读数（同输入：`参数={"补丁清单": []}`，即**故意传错参数名**）

- **改前（live MCP，原文）**：`参数不合法：能力 文件系统支持库.文本补丁.批量应用精确替换 缺少必填参数 编辑列表（本能力参数（*=必填）：编辑列表*、根目录、写入、原子）`
- **改后（源码树直调，声明取自 `能力契约/参数契约.json`，原文）**：`参数不合法：能力 文件系统支持库.文本补丁.批量应用精确替换 缺少必填参数 编辑列表（本能力参数（*=必填）：编辑列表*、根目录、写入、原子）；列表型参数的项结构：编辑列表[文件路径*(文本型)、旧文本*(文本型)、新文本*(文本型)、起始行(整数型)、结束行(整数型)、预期旧文本摘要(文本型)、预期文件摘要(文本型)]`
- **只增字段**：改后 ＝ 改前原文 ＋ 尾注（前缀逐字相同），错误码仍 `参数不合法`，既有措辞一字未改。
- **类型不符同样带尾注**：`… 的参数 编辑列表 必须是 列表型；列表型参数的项结构：编辑列表[…]`。
- **正确调用零差异**：`校验能力参数(…, 合法入参)` 返回 `''`（空串）。
- **契约没声明 `项结构` 的能力不编造**（`正则搜索` 契约无 `项结构`）：`参数不合法：能力 文件系统支持库.内容检索.正则搜索 缺少必填参数 模式（本能力参数（*=必填）：根目录、模式*、路径、文件模式、最大匹配数、上下文、忽略大小写、排除列表、紧凑返回）` —— **无尾注**（不按 `说明` 散文反推，避免给假结构）。

## 三、看键档（未做 · 阻断登记 ＋ 具体改法）

### 为什么不能在本批做（两条平台读数，不是我的判断）

- `开发工具/薄壳/薄壳服务.py`：活跃租约属 `开工-20260924-164041-49c6`（**租约id=`43cf8682f530491e`**，占用开始 `16:40:42`、过期 `16:55:42`，任务「批R·R-33 收口：查文件 范围 改为可选（默认项目根）+ 补测 + 未完成事项登记节 + 单据读数刷新」）⇒ 我 `16:42:15` 的 `开工即占` **整体失败**：`错误码=文件已被占用`、`冲突路径清单 ['开发工具/薄壳/薄壳服务.py']`。
- `开发工具/薄壳/工具清单.py`：活跃租约属 `开工-20260924-161652-30c0`（**租约id=`aafb272fba0f430a`**，过期 `17:38:01`）。
- **为什么不能只落一半**：`看键` 是 **MCP 入参**，客户端按 `工具清单.组装工具面()` 的 `inputSchema` 校验（顶层 `additionalProperties: false`，由 mcp SDK `jsonschema.validate` 执行）—— **不在 schema 里声明的键当场被拒**。只改 `薄壳服务.py` 等于死代码（且不可验）。

### 具体改法（两个文件各一处，照抄即可；先 `开工即占` 这两条路径）

1. `开发工具/薄壳/工具清单.py`
   - **行 116**（`"值字段": {…}` 之后、`"项目根": {…}` 之前）插入：
     `"看键": {"type": "boolean", "description": ("真 = 只回结构（顶层键 ＋ 每键类型 ＋ 容器条数），**不回任何值**、不落盘、不触发回执；默认假 ⇒ 与改前逐字节一致")},`
   - **行 88** 的描述句尾部追加：`看键=真 只回结构不回值；`
2. `开发工具/薄壳/薄壳服务.py`
   - 在 **行 703**（`def _裁剪值(`）之前插入 `_看键(值)`：字典型 → `{"类型":"对象","键数":N,"键":{<键名>: <递归结构>}}`；列表型 → `{"类型":"数组","条数":N,"元素":<首项结构，空数组 None>}`；标量 → `{"类型":"文本"/"整数"/"小数"/"布尔"/"空"}`（**不回任何值**）。
   - **行 1234** 之前（网关操作分支：健康检查／热接入／**任务查询／任务提交**／任务取消）与 **行 1290** 之前（调用能力分支）各插同一段：
     `if 参数.get("看键") is True:` / `    数据["值"] = _看键(数据.get("值"))` / `    return 数据`
   - 边界（写进 docstring）：它是**信封**投影（只对返回体做形状归纳），**不许去问能力「你的结构是什么」**（那就成了业务 ＋ 第二套事实源）；**不落盘、不触发回执**（因此不产生 `回执` 指针）。
   - **异步腿的判断（本批结论）**：**同批覆盖**。`任务提交`／`任务查询` 走的是同一个「网关操作」分支（行 1234），一次 `任务查询` 本就是一次同步请求 ⇒ 看键天然生效；**唯一不覆盖**的是 `操作=速办`（行 1426 `_速办` 自己组合结果、只转发 ≤3 个短参数），建议明确「要看结构走 调用能力／任务查询」，**不把 看键 铺进速办**（速办的价值就是短话，多一个开关就背离它）。
   - 落完必跑：`静态编译 类型=1 模块ID=开发工具/薄壳`（编译口把 `开发工具/薄壳/` 的定向测试点名为 `测试中心.开发工具.测试_薄壳返回可控`，该测试同批也被 R-33 持有）。

## 四、静态编译读数（全走异步腿：任务提交 → 任务查询）

| 任务id | 类型／范围 | 创建→完成 | 成功/失败/检查项 | 五态 | 红项 |
|---|---|---|---|---|---|
| `2857f61885854092` | `0` 全仓 | 16:38:59→16:39:21 | 18 / 2 / 20 | 失败 | 公开调用完整性（阻断）、制品新鲜度（只报告） |
| `a2d1632285f44d4c` | `1` 运行核心/统一网关 | 16:40:41→16:41:02 | 18 / 2 / 20 | 失败 | 同上两条（受影响包数 0） |
| `6f8d6df290c84a74` | `1` 支持库/后端/文件系统支持库/内容检索 | 16:41:32→16:41:54 | 18 / 2 / 20 | 失败 | 同上两条 |
| `62dca84215b94643` | `1` 支持库/后端/文件系统支持库/文本补丁 | 16:41:40→16:42:17 | 18 / 2 / 20 | 失败 | 同上两条 |
| `77745a11bbbb4ca1` | `1` 支持库/后端/文件系统支持库（**修完 使用说明 载位后复跑**） | 16:44:37→16:44:59 | 18 / 2 / 20 | 失败 | 同上两条 |

逐条明细原文（5 次读数一字不差）：

- `[阻断] 公开调用完整性 退出码=1 耗时=2.33s：  [可收敛] 支持库/后端/OCR识别支持库/OCR识别|内部层-可绑定集仍含: 当前 0 < 基线 3（应下调基线）` —— 点名的包是 `OCR识别支持库`，**不是本批路径**。
- `[只报告] 制品新鲜度 退出码=1 耗时=4.03s：{… "落后提交数": 1, "落后阈值": 0, "来源绑定通过": false … "说明": "制品落后 HEAD 1 个提交（阈值 0，制品来源提交 81cc618d…）—— 网关跑的是旧装配，须重建制品 + 重启网关 ⇒ 网关跑的是旧装配：源码已改而运行态未改，须「重建制品 + 重启网关」后再复验读数" …}`。

`结论原文`（5 次同文，照录）：`五态结论：失败 —— 阻断类红项：公开调用完整性（退出码 1）、包级说明-使用说明（生成退出码 2）、说明书载位同步（人工类）（生成退出码 2）。按红项修完再跑一遍。`

## 五、本批自己发现并修掉的一条红因：`说明/使用说明.md` 载位漂移

- **现场**：`能力定义.json` 版本升了（`1.1.3→1.1.4`、`1.4.0→1.4.1`），而两份 `说明/使用说明.md` 的 `- 版本：` 载位行停在旧值 —— 它是 `生成物登记表.json` 里 `包级说明-使用说明` 登记的派生件，也正是 `公开调用完整性` 门禁的一半。
- **修法（只走登记表那条腿，不手改）**：`python3.14 -m 开发工具.MD文档生成 --类型 包级说明-使用说明 --写盘 --文件 <包>/说明/使用说明.md`（内容检索、文本补丁各一次）。
- **过程读数**：第一次把两条命令串跑，`文本补丁` 一份被**写入授权**判 `越界`（原文）：`越界：支持库/后端/文件系统支持库/文本补丁/说明/使用说明.md 没有任何**活跃写租约**覆盖（写入腿只判「是不是经 MCP」，不判路径授权；本判据补的就是这一半）` ⇒ 补 `开工即占`（`开工-20260924-164355-e0f1`）后分别重跑，两份都写盘成功（`退出码=0`）。
- **写盘读数**：内容检索 `现行 69 行 / 账 50 行 / 合并 69 行；人工 22 保、2 刷、丢账 0、补账 2；人工行零丢失=True`；文本补丁 `现行 96 行 / 账 54 行 / 合并 96 行；人工 38 保、1 刷、丢账 1、补账 1；人工行零丢失=True`；两文件 diff 各 **2 行**（合计 4 insertions / 4 deletions）。
- **改后核验读数（核验命令原文，两份都）**：`扫描 1 份说明书；有漂移 0 份；人工行零丢失未过 0 份；未核验（判不出）0 份；白名单合法跳过 0 份 ⇒ 全部已是固定点（零漂移）`。

## 六、未做项与须确认项

1. **第三件（看键档）未落一行** —— 阻断在 R-33 的两条租约（`43cf8682f530491e`／`aafb272fba0f430a`）。具体改法与行号见第三节；释放后按登记落盘并跑 `静态编译 类型=1 模块ID=开发工具/薄壳`。
2. **三件的 MCP 面判据全部待复验** —— 须先「重建制品 ＋ 重启网关」（平台门禁红项原文见第〇节）。本批只交付：live 改前读数 ＋ 源码级改后读数 ＋ 接线复现。
3. **`包级说明-使用说明（生成退出码 2）`／`说明书载位同步（人工类）（生成退出码 2）` 两条红项，在修完本批漂移后仍然存在**，且与本批两个包的 `使用说明.md` 内容无关（同一批核验命令回「零漂移」）。**已定位到具体成因（原文）**：直接跑登记表声明的核验命令 `python3.14 -m 开发工具.MD文档生成 --类型 包级说明-使用说明 --校验` ⇒ `退出码=2`，输出 `⇒ 未核验（判不出，非零退出）：… ? 支持库/适配层/FFmpeg提供者/说明/使用说明.md: 账行数 10 远小于现行 297 行：疑似血统判错（判为 G2）；请核对生成器或手工指定血统（另一支 G1 的账 8 行、合不上现行的小节顺序/份数，改判也不成立，故拒写）`。即：**这两条红项来自 `支持库/适配层/FFmpeg提供者` 的说明书血统判错**（别批在改的包），本批未碰 `生成物联动.py`（不在本批写集内）。**须确认**：由 FFmpeg提供者 那批（或主会话）处理。
4. **`公开调用完整性` 阻断红项**（`OCR识别支持库|内部层-可绑定集 当前 0 < 基线 3（应下调基线）`）属别批，本批只如实照录。
5. **本批写集与租约**：`开工-20260924-164022-44f2` 17 条 ＋ `开工-20260924-164355-e0f1` 2 条；收工已按 `租约id清单` 释放（读数见回传）。
6. **未 git 提交**（按任务信：收尾由主会话统一做，只用精确路径 `git add`）。
