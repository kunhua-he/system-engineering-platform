<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R · R-3 · U-7 提供者可用性探针合并（20 处逐处对比 + 唯一节点收口）

- 开工ID：`开工-20260924-105443-8c45`（租约 `67f0d97dd9d3432f` `badd860b71a04a70` `30d35595593f47da` `fe3cfd906c8d4d38` `e2e498c3e8c44310` `dd74c9f2ca7343fb`）
- 日期：2026-09-24｜本文件由本次开工独占
- 唯一节点（收口目标）：`支持库/适配层/系统探针提供者/实现/系统探针.py::检查系统工具`（经 `支持库.适配层.系统探针` 再导出）
- 上游：`开发文档/分析/批Q清单_20260924.md` 第三节 U-7（原判「须裁」）
- 裁决原话：「这个你肉眼对比。如果是类型相似的，合并成 1 类原子能力就行。」

## 结论（直达核心）

**20 处逐处对比完成；本次实合 2 处（FFmpeg提供者、Git提供者），另 3 处（LibreOffice提供者、文档转换支持库.LibreOffice转换、textutil提供者）在本次开工前已落在同一节点上。**

- **合并前 → 合并后**：探针实现点 **20 处 → 20 处**（能力面不变：每个提供者仍须保留自己的 `检查提供者`／`检查可用性` 公开能力）；其中「手写外部命令子进程探针腿」**4 条 → 2 条**。
- 甲档（外部命令版本探针）8 处中 **5 处**落在唯一节点上（3 处既有 + 2 处本次），余 2 处按正当差异保留并逐条写明判据。
- 改动 3 个源文件 + 1 个测试文件；**未改任何公开能力契约**（两个被改能力的 `参数`／`返回`／`错误码` 逐字不变）。
- **未做项 1 条**：两个被改包的 `完整性摘要.json` 已判红，按「派生件不许手改」未由本次重算，须主会话走编译口（见 §五）。

## 一、20 处逐处对比表

普查命令（只读，排除 `工程缓存`）：

```
rg -n "def 检查提供者|def 检查可用性" --glob '*.py' -g '!工程缓存/**'
```

### 甲档 · 外部命令独立子进程版本探针（8 处）

| # | 落点（文件:行）| 能力 id | 被探命令 | 什么算可用 | 返回结构 | 谁在消费 |
|---|---|---|---|---|---|---|
| 1 | `支持库/适配层/FFmpeg提供者/实现/探测.py:52` | `媒体处理支持库.FFmpeg媒体.检查提供者` | `ffmpeg -version` ＋ `ffprobe -version` | 两工具都定位到 **且** 两次探针都退出码 0 | 结果型；`{ffmpeg:"可用", ffprobe:"可用", 版本:{ffmpeg, ffprobe}}` | `模块库/媒体处理/实现/媒体处理.py:89`（转调）、`模块库/直播逐字稿/实现/直播逐字稿.py:80`（转调）、`测试中心/支持库/测试_FFmpeg提供者.py:166` |
| 2 | `支持库/适配层/Git提供者/实现/Git提供者.py:30` | `版本控制支持库.Git操作.检查提供者` | `git --version` | 能跑起来 **且** 退出码 0 | 结果型；`{git:"可用", 版本}` | `模块库/自修复工具/实现/自修复工具.py:157`（转调）、`测试中心/支持库/测试_Git提供者.py:85` |
| 3 | `支持库/适配层/Swift编译器提供者/实现/Swift工具.py:91` | `Swift编译器提供者.检查提供者` | `swiftc --version` ＋ `codesign --help` | 两工具都定位到 **且**（退出码 0 **或** 退出码非 0 但有输出）| 结果型；`{提供者可用, 工具版本:{swiftc, codesign}, swiftc 路径, codesign 路径}` | `测试中心/支持库/测试_SwiftCompiler提供者.py` |
| 4 | `支持库/适配层/代码地图提供者/实现/提供者.py:87` | `代码地图提供者.检查提供者` | 委托子进程探 `codegraph --version` | 透传子进程结论 | 结果型；透传 `{可执行路径, 版本, 退出码}` | `测试中心/支持库/测试_代码地图提供者.py:18` |
| 5 | `支持库/适配层/代码地图提供者/实现/子进程入口.py:96` | （同上，真探针体）| `codegraph --version` | 在 PATH＋`~/.npm-global/bin`＋`/usr/local/bin`＋`/opt/homebrew/bin` 内定位到可执行 | 子进程 JSON；`{成功, 值:{可执行路径, 版本, 退出码}}` | 同上 |
| 6 | `支持库/适配层/LibreOffice提供者/实现/文档转换.py:57` | `LibreOffice提供者.检查提供者` | `soffice --version` | `检查系统工具` 成功 | 结果型；`{LibreOffice:"可用", 版本, 退出码, 标准错误摘要}` | 依赖生命周期审计、健康监督能力面、`测试中心/支持库/测试_文档转换提供者.py` |
| 7 | `支持库/后端/文档转换支持库/LibreOffice转换/实现/文档转换.py:78` | `文档转换支持库.LibreOffice转换.检查提供者` | `soffice --version` | `检查系统工具` 成功 | 结果型；同 #6 | 同上 |
| 8 | `支持库/适配层/textutil提供者/实现/文本转换.py:43` | `textutil提供者.检查提供者` | `textutil -help`（版本取 macOS 版本）| `检查系统工具` 成功 | 结果型；`{textutil:"可用", 版本, 退出码, 标准错误摘要}` | `运行核心/环境指纹`、健康监督、`测试中心/支持库/测试_文档转换提供者.py` |

### 乙档 · 第三方 Python 依赖在位探针（7 处，不产子进程）

| # | 落点（文件:行）| 能力 id | 被探对象 | 什么算可用 | 返回结构 | 谁在消费 |
|---|---|---|---|---|---|---|
| 9 | `支持库/适配层/模型服务提供者/实现/提供者.py:42` | `模型服务.检查提供者` | torch／transformers／fastapi／uvicorn | 四项都能 `find_spec` 定位（**故意不导入**，原生扩展不进主进程）| 结果型；`{可用, 探针, 依赖[{名称,发行包,版本}], 缺失, 说明}` | 无仓内调用方（测试／依赖审计读能力面）|
| 10 | `支持库/适配层/系统探针提供者/实现/提供者.py:36` | `系统探针.检查提供者` | psutil | **真读一次** `psutil.virtual_memory()` 成功 | 结果型；`{可用, 探针, 版本, 内存总量字节, 内存可用字节, 内存占用率, 说明}` | 依赖生命周期审计、健康监督能力面 |
| 11 | `支持库/适配层/YAML提供者/实现/提供者.py:62` | `适配层.YAML提供者.检查可用性` | PyYAML | 模块级容忍式 `import yaml` 成功 | 裸字典；`{可用, 版本, 说明}` | 依赖审计、健康检查 |
| 12 | `支持库/适配层/psycopg提供者/实现/提供者.py:158` | `适配层.psycopg提供者.检查可用性` | psycopg 驱动 | 驱动可导入（**不连数据库**）| 结果型；`{驱动可用, 驱动版本:{psycopg}}` | `测试中心/支持库/测试_psycopg提供者.py` |
| 13 | `支持库/适配层/MCP协议提供者/实现/协议.py:53` | `适配层.MCP协议提供者.检查可用性` | mcp SDK | SDK 可导入 | 裸字典；`{可用, 版本, 说明}` | 依赖审计、健康检查 |
| 14 | `支持库/适配层/TreeSitter提供者/实现/提供者.py:155` | `适配层.TreeSitter提供者.检查可用性` | tree-sitter ＋ tree-sitter-typescript | 可导入 **且** 能真构造 `typescript` 解析器 | 裸字典；`{可用, 版本, 说明}` | 依赖审计、健康检查 |
| 15 | `支持库/适配层/MLXWhisper提供者/实现/子进程解析.py:116` | `转写支持库.转写.检查可用性` | mlx_whisper／faster_whisper ＋ 模型配置 | 库在位 **且** 模型路径／模型名配置有效（**不加载权重**）| 子进程 JSON；`{值:{可用, 模型名, 模型版本}}` | `模块库/媒体转写/实现/媒体转写.py:79`（转调）、`模块库/直播逐字稿/实现/直播逐字稿.py:79`（转调） |

### 丙档 · 模块层转调／链路汇总（4 处，自身不探）

| # | 落点（文件:行）| 能力 id | 被探对象 | 什么算可用 | 返回结构 | 谁在消费 |
|---|---|---|---|---|---|---|
| 16 | `模块库/媒体处理/实现/媒体处理.py:82` | `媒体处理.检查提供者` | 转调 `媒体处理支持库.FFmpeg媒体.检查提供者` | 转调结果成功 | 结果型；透传 #1 的值 | 模块门面（对外 HTTP）|
| 17 | `模块库/知识检索/实现/知识检索.py:86` | `知识检索.检查可用性` | 转调 `嵌入缓存.统计缓存` ＋ `重排服务.检查可用性` | 两个依赖探针都成功 | 结果型；`{可用, 缺失支持库, 知识库}` | 模块门面（对外 HTTP）|
| 18 | `模块库/媒体转写/实现/媒体转写.py:70` | `媒体转写.检查可用性` | 转调 `转写支持库.转写.检查可用性` | 转调结果成功 | 结果型；透传 #15 | 模块门面（对外 HTTP）|
| 19 | `模块库/直播逐字稿/实现/直播逐字稿.py:69` | `直播逐字稿.检查可用性` | 转调 `转写支持库.转写.检查可用性` ＋ `媒体处理支持库.FFmpeg媒体.检查提供者` | 两者都成功 | 结果型；`{可用, ffmpeg可用, 转写可用, 转写详情}` | 模块门面（对外 HTTP）|

### 丁档 · 无探针（常量声明，1 处）

| # | 落点（文件:行）| 能力 id | 被探对象 | 什么算可用 | 返回结构 | 谁在消费 |
|---|---|---|---|---|---|---|
| 20 | `支持库/后端/大语言模型支持库/重排服务/实现/重排服务.py:78` | `大语言模型支持库.重排服务.检查可用性` | **无**（标准库词法重排，恒可用）| 恒真 | 结果型；`{可用, 实现, 模型, 提供者}` | `模块库/知识检索/实现/知识检索.py:86`（转调）|

## 二、分档与合并结论

**合 5 处（甲档）落在唯一节点 `系统探针.检查系统工具` 上**：

- 本次实合 **2 处**：#1 FFmpeg提供者、#2 Git提供者。
- 开工前既有 **3 处**：#6 LibreOffice提供者、#7 文档转换支持库.LibreOffice转换、#8 textutil提供者（它们本就 `from 支持库.适配层.系统探针 import 检查系统工具`）。

**保留 15 处，逐条判据：**

| 处 | 判据（为什么不能合）|
|---|---|
| #3 Swift编译器提供者 | **失败语义相反**。`codesign` 实测**四个参数全退出码 2**（`--help`／`-h`／`--version`／`-v` 逐个实测），故本处判据是「退出码非 0 **但有真实输出** ⇒ 工具在位」；而 `检查系统工具` 的契约是「退出码非零 ⇒ 失败（错误码 退出码非零）」。合并即让 codesign 恒报不可用（真回归），且节点契约被 健康监督／环境指纹 共用，不能为一家改语义。|
| #4/#5 代码地图提供者 | **被探命令的运行环境不同**。`codegraph` 是 `#!/usr/bin/env node` 脚本，子进程入口必须 `env=_环境()` 把兜底目录前置进 PATH 才能连带找到 node；`检查系统工具` **无 env 注入面**（只接受 名称／命令列表／超时秒／版本参数）。另有定位面差异：本处含 `~/.npm-global/bin`＋`/usr/local/bin`＋`/opt/homebrew/bin` 三步兜底查找。|
| #9～#15 乙档 7 处 | **类型不同**：探的是**进程内第三方依赖**（import／`find_spec`／发行包元数据／真构造解析器），不产子进程；且仓内**无现成权威节点**（`检查系统工具` 只管外部可执行；`运行核心/环境指纹._包版本` 是私有件、不属支持库层）。按「先找现成节点，别新建机制」，新建一个依赖探针节点即新建机制。另：7 处判据各不相同（不导入／真读一次／真构造解析器／查模型目录），返回结构已由**各自包的 `能力定义.json` 逐包公开声明**（dict 型与结果型并存），合并即改 7 份公开契约。|
| #16～#19 丙档 4 处 | **形态不同**：自身不探任何东西，只按网关调支持库能力（#17／#19 还是**多依赖汇总**）。本身即「转调」形态，无重复实现可删。|
| #20 重排服务 | **无探针可合**：返回常量成功，不探任何对象。|

## 三、本次改动（3 源 + 1 测试）

| 文件 | 改了什么 |
|---|---|
| `支持库/适配层/Git提供者/实现/Git提供者.py` | `检查提供者` 由自持 `subprocess.run(["git","--version"])` 改为转调 `检查系统工具("git",["git"],超时秒=…)`；失败一律映射公开错误码 `提供者不可用`（内部信封码 工具缺失／探针超时／退出码非零 不越网关）；删掉 `import subprocess`（本文件已无第二处用法）|
| `支持库/适配层/FFmpeg提供者/实现/探测.py` | `检查提供者` 由自持 `执行受管命令([路径,"-version"])` 改为对 ffmpeg／ffprobe 各调一次 `检查系统工具(…, 版本参数="-version")`；值结构 `{ffmpeg, ffprobe, 版本}` 逐字保留；保留「未配置」失败文案与 `min(超时秒, 5.0)` 上限；`执行受管命令` 仍被 `探测元数据` 使用，导入保留 |
| `测试中心/支持库/测试_Git提供者.py` | `test_检查提供者` 的版本断言随口径统一由 `assertIn("git version", 版本)` 改为 `assertRegex(r"^\d+\.\d+", 版本)` |
| `开发文档/分析/批R单据_R3_提供者探针_20260924.md` | 本单据（新建）|

**合并没有动任何公开契约**：`媒体处理支持库.FFmpeg媒体.检查提供者`（参数 `超时秒` 双精度数型 默认 30；错误码 `参数不合法`／`提供者不可用`；返回 结果型）与 `版本控制支持库.Git操作.检查提供者`（参数 `超时秒` 默认 15；错误码 `提供者不可用`；返回 结果型）的 `能力定义.json` 逐字未变。
**版本字段口径变化（有意）**：由 `git --version`／`ffmpeg -version` 的**整行 banner** 改为**提取出的版本号**（如 `2.54.0`／`8.1`），与 `环境指纹`／`健康监督` 及同族 `Tesseract提供者.版本探针` 的口径一致。

## 四、回归读数（合并前后同一输入结论一致）

可复制命令（读数，非测试套件；旧实现取自 `git show HEAD:<路径>` 真跑，非复述）：

```
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
python3 - <<'PY'
import subprocess, sys, types, json
sys.path.insert(0, '.')
def 载入旧件(路径, 名):
    src = subprocess.run(['git','show',f'HEAD:{路径}'],capture_output=True,text=True,check=True).stdout
    m = types.ModuleType(名); exec(compile(src, 路径, 'exec'), m.__dict__); return m
旧Git, 旧FF = 载入旧件('支持库/适配层/Git提供者/实现/Git提供者.py','旧G'), 载入旧件('支持库/适配层/FFmpeg提供者/实现/探测.py','旧F')
import 支持库.适配层.Git提供者 as 新Git包, 支持库.适配层.FFmpeg提供者.实现.探测 as 新FF
for 名, 旧, 新 in (('Git', 旧Git.检查提供者, 新Git包.检查提供者), ('FFmpeg', 旧FF.检查提供者, 新FF.检查提供者)):
    o, n = 旧(), 新()
    print(名, '旧', o.成功, repr(o.错误码), json.dumps(o.值, ensure_ascii=False))
    print(名, '新', n.成功, repr(n.错误码), json.dumps(n.值, ensure_ascii=False))
    print(名, '结论一致 =', (o.成功, o.错误码) == (n.成功, n.错误码))
PY
```

实测输出（2026-09-24，原样粘贴）：

```
[Git提供者.检查提供者]
  旧 成功=True 错误码='' 值={"git": "可用", "版本": "git version 2.54.0 (Apple Git-157)"}
  新 成功=True 错误码='' 值={"git": "可用", "版本": "2.54.0"}
  结论一致(成功/错误码) = True
[FFmpeg提供者.检查提供者]
  旧 成功=True 错误码='' 值={"ffmpeg": "可用", "ffprobe": "可用", "版本": {"ffmpeg": "ffmpeg version 8.1 Copyright (c) 2000-2026 the FFmpeg developers", "ffprobe": "ffprobe version 8.1 Copyright (c) 2007-2026 the FFmpeg developers"}}
  新 成功=True 错误码='' 值={"ffmpeg": "可用", "ffprobe": "可用", "版本": {"ffmpeg": "8.1", "ffprobe": "8.1"}}
  结论一致(成功/错误码) = True

输入2：工具缺失（查找命令→None）
  旧 成功=False 错误码='提供者不可用' 说明='ffmpeg/ffprobe 未找到（未配置提供者）'
  新 成功=False 错误码='提供者不可用' 说明='ffmpeg/ffprobe 未找到（未配置提供者）'
  结论一致 = True

输入3：探针失败（subprocess.Popen 全抛 OSError）
  旧 成功=False 错误码='提供者不可用' 说明='无法运行 git --version' 可重试=True
  新 成功=False 错误码='提供者不可用' 说明='git 探针失败（工具缺失）: 无法启动 git: 模拟 git 缺失' 可重试=True
  结论一致 = True

测试断言可满足性：结果.值["git"]='可用' → True；assertRegex(r"^\d+\.\d+", '2.54.0') = True
```

三档输入（真实环境／工具缺失／探针失败）的**成功标志与公开错误码逐条一致**；仅 `版本` 字段按 §三 的有意口径变化。

`检查系统工具` 侧同输入读数（同一节点、逐工具）：

```
git 成功=True 退出码=0 版本='2.54.0'
ffmpeg 成功=True 退出码=0 版本='8.1'
swiftc 成功=True 退出码=0 版本='6.4'
codesign 成功=False 错误码='退出码非零' 退出码=2 版本=''   ← #3 保留的实测依据
codegraph 成功=True 退出码=0 版本='1.5.0'
```

## 五、残余与边界（如实留档）

1. **未做项 · 派生件未重算（1 条）**：两个被改包的 `完整性摘要.json` 现判红，问题均只有我改的那一个文件：
   - `支持库/适配层/Git提供者`：`["文件摘要不一致: 实现/Git提供者.py"]`
   - `支持库/适配层/FFmpeg提供者`：`["文件摘要不一致: 实现/探测.py"]`

   取证命令（MCP 只读）：`组件规范支持库.校验完整性摘要(包目录=<上述包绝对路径>)`。
   按「派生件不许手改（由编译口重算）」**未由本次写入**；请主会话收口时跑：
   `自修复工具.静态编译(类型=1, 模块ID=支持库.适配层.Git提供者)` 与 `…模块ID=支持库.适配层.FFmpeg提供者`。
   说明：`开发工具/全量重算摘要.py` 的写盘腿在终端被 `脚本入口准入` 拦下（实测 `权限不足: 拒绝执行：本项目的代码只能经 MCP 运行`），故终端腿不可用。
2. **未跑编译、未 git 提交**（本批纪律）；测试套件未由本次运行，回归读数只覆盖探针函数真调用，端到端回归请走 `自修复工具.动态编译`。
3. **集外同病（1 处，未动）**：`支持库/适配层/Tesseract提供者/实现/提供者.py:92` 的 `版本探针` 是同一类「手写子进程版本探针」，但命名不落 `检查提供者`／`检查可用性`，**不在本次 20 处口径内**，按精确范围未动。它另有「最低版本比对 → 版本不兼容」语义，若后续要合需单独裁。
4. **集外同族（2 处，未动）**：`PDF隔离提供者.检查提供者`（实现名 `检查提供者版本`）与 `密码签名提供者.检查提供者` 探的是**隔离子进程内**的第三方版本，命名不在本次口径内。
5. **未验证项**：`codesign` 的「退出码 2 但有输出」在本机实测成立；其他 macOS 版本／Xcode 版本未测，判据以本机实测为准。`codegraph` 的 PATH 兜底必要性在本机未复现（本机 node 在 `~/.local/bin`，已在 PATH 上），保留判据取自该文件的落盘注释与既有代码，**未做反向验证**。
6. **并发提示**：本次 `--只报` 两跑之间另出现 2 项外部漂移（`模块库/项目文档`、`平台控制面/备份恢复`），属其他会话的在途改动，本次未触碰。
