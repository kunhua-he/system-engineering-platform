# 系统工程平台 · 运行核心调研（树形图 ＋ 问题表）

> 状态：起草
> 最后更新：2026-09-25
> 口径：**子代理只读调研 ＋ 主会话 MCP 侦察**；节点清单取自 `文件管理.列出文件` 现场，行号取自各路子代理实测（**未逐条复核**，复核状态见 §八）
> 维护者：运行核心调研
> 范围：`运行核心/`、`后端核心/`、`公共契约/`（含 `运行时/`）、`启动监督器/`、`前端核心/`；不含 `支持库/`、`模块库/` 内部实现（只取它们与核心的接缝）
> 本轮**只调研不修**
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

---

## 一、核心总树（全量节点）

```
系统工程平台（核心面 · 全量节点）
│
├─ 运行核心/                                     运行态与执行核心
│  ├─ 启动运行核心网关.py                        唯一启动入口（:15 环境准入 fail-closed／:75 并发参数／:117 建宿主／:127 任务系统／:136 起网关／:172 资源账本循环／:329 停机编排）
│  ├─ 权威状态.py                                ＋5 面：_事务锁面／_句柄租约面／_可靠性面／_资源与进程面／_连接迁移面（:69 直连 sqlite）
│  ├─ sqlite错误码判据.py／时钟校准.py／进程身份.py／资源id编码.py／依赖防火墙.py／工程缓存/
│  ├─ 环境指纹/环境指纹.py                       :105 无生产调用方
│  ├─ 加载器/                                    装配链
│  │  ├─ 包发现/发现器.py                        :64 发现全部／:50 扫描目录（:103 枚举正式根失败只留痕）
│  │  ├─ 包安装/入口路径.py:29／支持库安装.py:46／模块安装.py:44
│  │  ├─ 依赖解析/解析器.py:55（Kahn 拓扑）／装配锁.py:20（weakref 表）:56 建锁
│  │  ├─ 生命周期管理/管理器.py                  :193 装配系统（:206→:232→:262→:337→:349→:426→:521）
│  │  ├─ 版本系统/ ／提供者选择/选择器.py:57
│  │  └─ 提供者隔离/独立进程_账本.py             :89 第三套系统根推导／:112 直连 sqlite／:74 世代 id 单例
│  ├─ 统一网关/                                  唯一对外执行入口
│  │  ├─ 本地网关.py                             :588 构造／:662 启动／:379 网关调用路由／:695 优雅停止
│  │  ├─ 网关核心.py                             :65 MRO 组装（幂等面→参数校验面→执行面→请求处理面）
│  │  ├─ 停机编排.py                             :115 四段停机／:509 信号
│  │  ├─ 核心处理/网关处理面.py:35／网关执行面.py:39／网关参数校验面.py:75／网关幂等面.py:45:80
│  │  ├─ 传输/网关边界面.py:261 do_POST／:288 do_GET／流式HTTP.py:408 自带安全面:546／反馈路由面.py:97／流式路由面.py
│  │  ├─ 协议/网关信封.py:55／类型规格.py:192（唯一类型校验）／参数别名表.py:21／操作协议表.py:42／错误说明表.py:32／幂等协议常量.py／调用点缺参判定.py
│  │  ├─ 安全/安全边界.py／有界服务器.py:38（继承有界HTTP）／限流器.py:179（:239 死分支）
│  │  └─ 运行态/幂等存储.py:54（直连 sqlite）
│  ├─ 能力调用/                                  唯一执行权威
│  │  ├─ 唯一能力调用.py                         :80 服务／:165 调用能力／:248 校验／:394 任务通道
│  │  ├─ 唯一能力调用_指纹与绑定面.py／_装配面.py:110（CAS）／_诊断面.py:44／_调用参数.py:76／_调用记录.py／_调用错误码.py:56
│  │  ├─ HTTP连接器.py:73:246（第二条 HTTP 腿）／进程内连接器.py:31
│  │  ├─ 超时执行.py ＋ 超时执行/准入闸门.py／基础.py／形态判定.py:73 执行通道:152 端点推断／执行器_核心.py:138:198／执行器_结果面.py:18／_观测面／_账本面／_闸门面／残留账本.py
│  │  ├─ 控制调用/控制面.py／证据链.py
│  │  └─ 运行上下文/上下文.py
│  ├─ 任务调度/                                  任务路
│  │  ├─ 任务接入.py／任务系统.py／任务工作器.py
│  │  └─ 任务进程.py ＋ _任务归还面／_关闭队列面／_回收落盘面／_对象.py:183／_监视轮询面
│  ├─ 运行状态监督/实现/只读状态库.py:43         自建 sqlite（判据基线内 5 条）
│  ├─ 资源协调/服务.py／句柄服务.py:399／有状态排空/
│  ├─ 运行诊断/运行事件/／诊断中心/{诊断中心.py,失败记录.py,诊断复现.py}／安全审计/
│  └─ 自适应选型/启动接线.py／档位表.py／三层合并.py／并发自适应.py:151:195／容量基线消费.py（无生产调用方）
│
├─ 后端核心/                                     宿主与装配
│  ├─ 后端核心.py                                :88 __init__／:115 默认系统根 parents[1]／:126 装配模板缓存（无键）／:139 deepcopy 死分支／:145 装配告警走 stderr／:158 启动／:551 热接入覆写全局模板／:606 调用／:664 健康检查（不含跳过包）／:709 状态快照（无出口）
│  └─ 后端核心_包指纹面.py:113:123:128／_能力目录面.py／_资源与排空面.py
│
├─ 公共契约/                                     唯一底层（层，允许依赖=空）
│  ├─ 正式根.py                                  :30 正式根名表／:56 源码层名表／:215 是保留目录／:278 是聚合视图包／:291 是装配包（零调用）
│  ├─ 句柄体系.py                                :1 句柄唯一状态机（:45 回收证据表）
│  ├─ HTTP路由契约.json
│  ├─ 基础类型/                                  类型唯一真源
│  │  ├─ 类型表.py:7／结果类型.py:29:57:65／错误结构/错误结构.py:16:38
│  │  ├─ 字段名册.py:50／:289 取路径写法口径／:353 是路径族字段／:406 是敏感键名
│  │  └─ 二进制类型.py／数值类型.py／文本类型.py／集合类型.py／逻辑类型.py／空值类型.py／时间类型.py／数据类.py／文档结构.py／资源引用类型.py／JSON类型.py／历史短名.py／类型目录.md
│  ├─ 能力契约/契约.py:124:174／调用器.py:143:168（唯一调用腿）／数组文本.py:90:138／常用意图.py／契约指纹.py
│  ├─ 诊断/写入流水.py:91:232／调用账本.py:207:222（直连 sqlite）／忽略记录.py:31
│  ├─ 运行时/                                    共性节点集中地
│  │  ├─ 写入授权.py:392:296:147／仓库只读锁.py:154:242:972／有界IO.py:21:52:58:160／有界HTTP.py:25
│  │  ├─ 子进程协议.py:136:187／进程终止.py:199:393:554／取消登记.py:88:122
│  │  ├─ JSON解码.py:21／数据库URI.py:55:79／端口策略.py:12／资源水位.py:155:186／资源键表.py:43
│  │  ├─ 运行缓存.py:35:65（:25 空壳）／执行耗时账本.py:74:88／导入前缀.py:40:66:80
│  │  └─ 平台适配/判定.py:118／路径解析.py:76／资源口径.py:62／进程分叉.py／解释器.py／硬件画像.py:196／系统位置.py／删除树.py／准入.py／身份对账.py／采样基元.py／非阻塞读.py／要求POSIX.py／转写后端选择.py／__init__.py:53（再导出层）
│  └─ 错误结构/ 生命周期/ 版本规则/ 包声明/
│
├─ 启动监督器/                                   治理侧
│  ├─ 健康监督.py:23（平铺再导出腿，被包目录遮蔽、无调用方）
│  ├─ 健康监督/实现/提供者.py
│  └─ 容量基线.py:176（超限处理/熔断/解除熔断 无生产调用方）
│
└─ 前端核心/（宿主侧，非运行主链）
   ├─ 前端核心.py:82-150／渲染提供者.py:50-68（默认 8899）／浏览器交互.py:36-40（默认 8899）
```

---

## 二、启动链（节点树 · 父→子）

```
启动运行核心网关.py:15 环境准入（平台适配.校验支持范围，fail-closed）
└─ :19 非 macOS/arm64 → 中文报错 + sys.exit(1)
:75 _按硬件画像定并发参数 → 自适应选型/并发自适应.py:151 → 硬件画像探针:157 → :195 记录 jsonl
:117 后端核心() → 后端核心/后端核心.py:88 __init__（:115 默认系统根 parents[1]）
:118 后端核心.启动()
└─ 后端核心.py:158 → :117 装配()
   ├─ :126 _装配锁（模块级 :53）→ :127 _装配模板 命中判定（模块级 :49）
   ├─ 未命中 → 加载器/生命周期管理/管理器.py:193 装配系统()
   │  ├─ :206 发现全部 → 包发现/发现器.py:64（:96-112 三根 + 正式根.py:125）
   │  ├─ :218 选择提供者 → 提供者选择/选择器.py:57
   │  ├─ :232 解析依赖 → 依赖解析/解析器.py:55
   │  ├─ :262 单包预检 → 包安装/入口路径.py:29 + 运行环境管理器/环境管理器.py:153
   │  ├─ :285 聚合契约漂移校验 → :309 装配锁（依赖解析/装配锁.py:56）
   │  ├─ :337 绑定 → 能力调用/唯一能力调用_装配面.py:110（CAS）
   │  ├─ :349 提供者环境 → 环境管理器.py:481→:466→:305→:680/:387；强制校验.py:148 终检
   │  ├─ :426 批次装配 → 包安装/支持库安装.py:46 / 模块安装.py:44
   │  └─ :521 记录装配状态
   ├─ 命中 → 后端核心.py:139 注册表 = deepcopy(_装配模板)
   └─ :145 装配告警(stderr) → :146 包指纹表 → 后端核心_包指纹面.py:123 → :170 启用校验
:127 构造任务系统 → 任务调度/任务接入.py
:136 本地网关服务器(网关核心(...), 端口, 127.0.0.1)
└─ 本地网关.py:588 → :614 安全配置 → :646 流式管理器
:159 网关.启动() → 本地网关.py:662 → :670 凭证加载 → :675 有界线程HTTP服务器 → :682 serve_forever
:172 资源账本维护循环 → 资源协调/句柄服务.py:399
:329 停机编排 → 统一网关/停机编排.py:115 → :350 装信号（:513）
:357 while True sleep（常驻空转，信号内联走四段）
```

---

## 三、一次调用的链路（节点树 · 父→子）

```
入口层
├─ A. HTTP 入口 40007
│  安全/有界HTTP.py:25 ── 安全/有界服务器.py:38（:51 构造／:60 写限流响应）
│  └ 本地网关.py:662 → :675 建服务器 → :711 处理类 → :714 setup（套接字超时=请求超时秒）
│     ├ 传输/网关边界面.py:261 do_POST → :192 校验边界 → :209 读请求体 → :252 解码JSON值
│     │   ├ /网关/调用 → 本地网关.py:379 普通路由面._处理网关请求
│     │   ├ /网关/热接入 → 本地网关.py:555
│     │   ├ /平台/能力反馈* → 传输/反馈路由面.py:97／:57 取反馈调用器（→调用器.py:168）
│     │   └ /网关/流式[*/取消] → 传输/流式HTTP.py:546（自带处理器:455）
│     └ 网关边界面.py:288 do_GET → /健康:299 /存活:312 /就绪:321 /能力/搜索:353 /能力/目录:373 /能力/契约:398
├─ B. MCP 入口（进程外薄壳）
│  薄壳服务.py:1169 _调用能力 → :1152 _展开数组 → 公共契约/能力契约/数组文本.py:90 → :138 _强转
│  └ 开发工具/薄壳/网关转发.py:120 → :69 发送 → POST 40007/网关/调用  ★与 A 同腿，汇于 本地网关.py:379
└─ C. 进程内入口（不经网关）
   能力调用/HTTP连接器.py:73 → :246（客户端另一条 HTTP 腿）
   能力调用/进程内连接器.py:31 → 调用器.py:168 → 唯一能力调用服务

网关核心（网关核心.py:65；MRO：幂等记忆面→参数校验面→执行面→请求处理面）
└ 核心处理/网关处理面.py:35 处理
  ├ :53 允许操作表判 → :141 权限通过 → 安全/限流器.py:179
  ├ 核心处理/网关幂等面.py:45 → :80 等锁 wait → 网关执行面.py:39 _执行
  ├ :73-92 异常→错误码收口  ├ :115 审计记录  └ :138 参数账记一次调用
执行面（核心处理/网关执行面.py:39）
  ├ :53 设基准 → :54 参数错误（网关参数校验面.py:139）
  ├ :137 调用能力 → 协议/参数别名表.py:21 → 网关参数校验面.py:198 → 协议/类型规格.py:192（唯一校验点）
  │     → :259 过滤能力参数 → :92 归一化路径族 → :281 被忽略参数 → :41 最接近契约参数 → :207 同步腿拒绝说明
  │     → 后端核心.调用 → 唯一能力调用.py:165 调用能力
  └ :219/263/281 任务提交/查询/取消 → 任务调度
唯一能力调用服务（唯一能力调用.py:165）
  ├ :248 校验能力参数 → :264 展开参数 → :273 执行实现
  │   超时执行/形态判定.py:185 判定执行形态 → :73 执行通道 → 直调/任务(:394)/线程(执行器_核心.py:198)
  │   执行器_核心.py:138 准入（内存水位→全局→每能力）→ 执行器_结果面.py:18 限流失败
  ├ :308 异常收口 → _调用错误码.py:72 公开错误码（:56 表）
  └ _诊断面.py:44 超时并记录／:86 失败并记录
信封与错误码资产
  ├ 协议/网关信封.py:55（:80 转字典）／协议/错误说明表.py:32／本地网关.py:84 公开错误码状态映射／错误结构.py:38／子进程协议.py:67（:136/187 主循环）
```

---

## 四、共性节点唯一性判定（12 类）

| 共性节点 | 判定 | 唯一腿（实测） | 关键缺口 |
|---|---|---|---|
| 1 路径边界 | 基线内不唯一 9 处 | `系统核心支持库/路径安全/实现/路径安全.py:89/140` | 6 处口径分歧；相对路径按「能力自身树」解析 |
| 2 写授权·租约 | 判据唯一，3 处路径判定自建 | `公共契约/运行时/写入授权.py:392` | 无专属静态判据；租约「现场指纹」腿缺 `项目根` ⇒ 恒报已变更 |
| 3 仓库只读锁 | **已唯一** | `运行时/仓库只读锁.py:154/242/972` | 释放回锁晚于认领解锁 |
| 4 状态库/sqlite | **不唯一 49 处** | `数据库连接支持库/SQLite数据库/实现/提供者.py:76/92` | 基线内 5 处，**基线外 ≥15 处**；判据只盖运行核心+技能库 |
| 5 账本与流水 | **4 套独立实现** | 写入流水.py:91／调用账本.py:96／执行耗时账本.py:88／唯一能力调用_诊断面 | 无判据 |
| 6 进程分叉与终止 | 基本唯一 | 进程终止.py:839／平台适配/资源口径.py:62 | 2 处裸调（资源硬限制.py:201、重启网关.py:444） |
| 7 超时 | **≥12 套来源** | **无唯一腿** | 10/9/120/600/1800/30/300… 互不引用，无判据 |
| 8 JSON | 通用序列化无腿 | JSON解码.py:21（仅字节集冻结） | 网关拒非有限数、其余裸 `json.loads`，无判据 |
| 9 HTTP 出站 | **20+ 处自建** | 网络通信支持库/请求/实现/网络请求.py:100 | 判据只盖适配层 |
| 10 日志/诊断 | 不唯一（print 直写 16 处） | 诊断/忽略记录.py:31 | 无判据 |
| 11 临时目录（生产侧） | **119 处 `tempfile.*`** | 系统核心支持库/资源管理/实现/资源管理.py:179 | 判据仅测试侧 |
| 12 能力调用 | **已唯一**（三腿各一） | 契约/调用器.py:168／技能库.py／HTTP连接器.py | 判据齐 |

**直接回答「全兼容吗」：不完全。** 兜住的只有 #3、#6 主体、#12；**超时、JSON、HTTP 出站、日志、临时目录（生产侧）5 类在判据基线之外、当前无任何判据兜着**。

---

## 五、公共契约底层原语（唯一实现清单）

| 原语 | 唯一实现 |
|---|---|
| 类型 | `基础类型/类型表.py:7` |
| 参数·结果 | `基础类型/结果类型.py:29` ＋ `能力契约/契约.py:174` |
| 错误码 | `基础类型/错误结构/错误结构.py:38` |
| 字段名册 | `基础类型/字段名册.py:50` |
| 路径口径 | `运行时/平台适配/路径解析.py:76` ＋ `字段名册.py:289` |
| JSON | `运行时/JSON解码.py:21` |
| 有界IO | `运行时/有界IO.py:21/160` |
| 有界HTTP | `运行时/有界HTTP.py:25` |
| 子进程协议 | `运行时/子进程协议.py:136` |
| 进程终止 | `运行时/进程终止.py:199` |
| 写授权 | `运行时/写入授权.py:392` |
| 只读锁 | `运行时/仓库只读锁.py:154/242` |
| 运行缓存 | `运行时/运行缓存.py:35/65` |
| 数据库URI | `运行时/数据库URI.py:55` |
| 端口 | `运行时/端口策略.py:12` |
| 资源水位／键表 | `运行时/资源水位.py:186`／`资源键表.py:43` |
| 取消登记 | `运行时/取消登记.py:88` |
| 导入前缀（系统根） | `运行时/导入前缀.py:40/66/80` |
| 句柄 | `公共契约/句柄体系.py:1` |

**分层单向性（丙路结论）**：支持库/模块库→上层 import **0 处**；仅 3 处经登记的跨层边（`组件规范.py:104` 懒载开发工具、`写入授权.py:267/386` 懒载平台控制面），形状一致 ⇒ **分层是单向的**。

---

## 六、问题表（全量 · 按面分组；每问只动一个节点）

### 6.1 启动·装配·宿主（甲路）

| 问题 | 归属节点 | 修改节点 |
|---|---|---|
| 装配模板缓存无键，多根/多版本共用，不隔离 | 后端核心.py:126-142 | 后端核心.py:126 |
| 热接入把全局 `_装配模板` 覆写成自己注册表，多实例互污 | 后端核心.py:551 | 后端核心.py:551 |
| 装配跳过包只打 stderr，`/健康` 不含，运行期不可见 | 后端核心.py:664-674 | 后端核心.py:664 |
| `状态快照`（跳过包唯一出口）无网关操作暴露＝死出口 | 后端核心.py:709 | 后端核心.py:709 |
| `deepcopy` 后判 `None` 是死分支，永不触发 | 后端核心.py:139-141 | 后端核心.py:139 |
| 跳过任意多包仍判装配成功，无比例判据 | 管理器.py:497 | 管理器.py:497 |
| 包指纹表初始化失败静默 return，基线空→漂移校验形同虚设 | 包指纹面.py:128-129 | 包指纹面.py:128 |
| 指纹校验默认关（环境变量），正式部署不设即无拦截 | 包指纹面.py:113-121 | 包指纹面.py:113 |
| 装配状态表用 weakref：注册表被 GC 即丢幂等基线 | 装配锁.py:20 | 装配锁.py:20 |
| `是装配包` 定义但零调用；`枚举包目录` 未调 `是非生产声明` | 正式根.py:291 | 正式根.py:316 |
| 支持库安装/模块安装 各写一份同构 `加载入口模块` | 模块安装.py:22 | 模块安装.py:22 |
| 模块连接器注入失败仅 print，包半可用仍算装配成功 | 模块安装.py:61-65 | 模块安装.py:61 |
| `确保环境` 直接入口不取串行锁（仅并行包装取锁） | 环境管理器.py:305 | 环境管理器.py:305 |
| 环境校验缓存键仅 size+mtime_ns，可假命中 | 环境管理器.py:562 | 环境管理器.py:562 |
| 系统根推导多套：后端核心用 parents[1] | 后端核心.py:115 | 后端核心.py:115 |
| `提供者生命周期._定位系统根` 自带祖先循环（第二套） | 提供者生命周期.py:115 | 提供者生命周期.py:115 |
| `独立进程_账本._定位系统根` 自带祖先循环（第三套） | 独立进程_账本.py:89 | 独立进程_账本.py:89 |
| 世代 id 为进程级单例，同进程多网关共用一个 | 独立进程_账本.py:74 | 独立进程_账本.py:74 |
| 启动清扫为进程级一次性，多实例只扫一次 | 提供者生命周期.py:52 | 提供者生命周期.py:52 |
| `提供者生命周期管理器` 无生产调用方 | 提供者生命周期.py:90 | 提供者生命周期.py:90 |
| `环境指纹` 模块无生产调用方，判据空转 | 环境指纹/环境指纹.py:105 | 环境指纹.py:105 |
| 外部应用探针清单与 `健康监督.默认探针清单` 两套 | 环境指纹.py:70 | 环境指纹.py:70 |
| `强制校验` 规则 5 自采 python/os/arch，与指纹两套采样 | 强制校验.py:271 | 强制校验.py:271 |
| `容量基线.超限处理/熔断/解除熔断` 无生产调用方 | 容量基线.py:176 | 容量基线.py:176 |
| `容量基线消费` 无生产调用方 | 容量基线消费.py:1 | 容量基线消费.py:1 |
| `健康监督.py` 平铺再导出腿，被包目录遮蔽、无调用方 | 健康监督.py:23 | 健康监督.py:23 |
| `已配置退出超时` 用子串探 plist，注释里出现即判真 | 停机编排.py:104 | 停机编排.py:112 |
| 优雅停止 join timeout=1800，与停机 20s 量纲不一 | 本地网关.py:695 | 本地网关.py:695 |
| 信号归属进程级单例，同进程第二个网关装不上信号 | 停机编排.py:509 | 停机编排.py:509 |
| `发现全部` 枚举正式根失败只留痕，扫描面静默缩小 | 发现器.py:103-109 | 发现器.py:103 |

### 6.2 调用·网关·信封（乙路）

| 问题 | 归属节点 | 修改节点 |
|---|---|---|
| 类型强转只在 MCP 位置数组腿；对象表腿只校验不强转 | 数组文本.py:138 | 类型规格.py:192 |
| 未知参数：网关边界剔除、进程内腿拒绝（两层无机器判据） | 契约.py:167 | 契约.py:167 |
| 信封构造点 ≥10 处各拼 dict | 网关边界面.py:183 | 网关信封.py:55 |
| 「内部错误」文案无「怎么修」 | 网关处理面.py:91 | 错误说明表.py:65 |
| 「操作不存在」文案无修法（不说去查允许操作表） | 网关处理面.py:74 | 错误说明表.py:34 |
| 401「权限不足」不说凭证怎么拿（变量名/头名） | 网关边界面.py:205 | 安全边界.py:191 |
| 超时值来源 ≥8 处、默认 10/1800/86400/30/5 并存 | 网关信封.py:35 | 本地网关.py:604 |
| HTTP连接器 默认 10 秒 vs 网关 1800 秒 | HTTP连接器.py:50 | HTTP连接器.py:101 |
| 进程内连接器 默认 10 秒，同一病 | 进程内连接器.py:28 | 进程内连接器.py:60 |
| 同步腿守卫阈值 10 与网关超时 1800 无关联判据 | 网关参数校验面.py:75 | 网关参数校验面.py:75 |
| 幂等等锁 wait 用信封 1800 当上限（锁等待与执行混用） | 网关幂等面.py:80 | 网关幂等面.py:80 |
| 流式腿自建 400 状态码映射，不复用公开映射 | 流式HTTP.py:599 | 本地网关.py:84 |
| 反馈路由硬编码状态码映射，第二套 | 反馈路由面.py:106 | 本地网关.py:84 |
| `_设置失败` 与 `_设置后端字典结果` 两条失败收口并存 | 网关处理面.py:158 | 网关处理面.py:152 |
| 异常→错误码两张表靠人同步 | 网关处理面.py:73 | 唯一能力调用_调用错误码.py:56 |
| 请求体读取 10s 与套接字 1800s 反复 settimeout | 网关边界面.py:232 | 网关边界面.py:243 |
| 流式服务器自带安全配置/凭证/审计，第二套安全面 | 流式HTTP.py:408 | 流式HTTP.py:388 |
| 流式请求体上限 1MB 与网关 64MB 不同源 | 流式HTTP.py:27 | 流式HTTP.py:27 |
| 有界服务器 限流重试秒=1 就地复制 | 有界服务器.py:49 | 有界服务器.py:49 |
| 反馈路由 手拼信封，不复用 网关响应 | 反馈路由面.py:76 | 网关信封.py:55 |
| 有界服务器 手写 429 报文，绕过 `_写JSON` | 有界服务器.py:60 | 有界服务器.py:60 |
| 流式 `_写JSON` 第二套 JSON 写回 | 流式HTTP.py:505 | 流式HTTP.py:505 |
| 6 个 GET 路由各自手拼网关请求（字段复制 6 份） | 网关边界面.py:299 | 网关边界面.py:299 |
| 流式腿自实现 `_读取`，不复用 `_读请求体` | 流式HTTP.py:461 | 流式HTTP.py:461 |
| 路径族绝对化只在网关腿；进程内腿无归一 | 网关参数校验面.py:92 | 网关参数校验面.py:92 |
| 断连取消登记只在 HTTP 腿；进程内腿无 | 本地网关.py:498 | 本地网关.py:498 |
| 子进程协议信封形状与网关信封不同，无对齐判据 | 子进程协议.py:67 | 子进程协议.py:67 |
| 限流器.进入任务/进入流式 全仓零调用（死分支） | 限流器.py:239 | 限流器.py:239 |
| `_参数摘要` 零调用实现 | 唯一能力调用_调用参数.py:76 | 唯一能力调用_调用参数.py:76 |
| 能力权限表因默认范围含「管理员」实际不生效 | 操作协议表.py:42 | 安全边界.py:33 |
| 执行形态靠端点关键词推断，误判即资源风险 | 形态判定.py:152 | 形态判定.py:152 |

### 6.3 公共契约·唯一底层（丙路）

| 问题 | 归属节点 | 修改节点 |
|---|---|---|
| 第二套有界线程HTTP（自建信号量） | 平台控制面/提供者/HTTP提供者.py:83 | 改继承 有界HTTP.py:25 |
| 第三套有界HTTP（函数内类） | 开发工具/HTML验证/验证服务.py:115 | 改导入 有界HTTP.py:25 |
| 第四套有界HTTP（模板内类） | 开发工具/项目编译/项目编译器.py:542 | 模板改导入 有界HTTP.py:25 |
| 第二套有界子进程IO | 平台控制面/提供者/资源硬限制.py:94 | 转调 有界IO.py:160 |
| 第三套有界读 | 平台控制面/提供者/HTTP提供者.py:193 | 转调 有界IO.py:21 |
| 第四套有界读 | 平台控制面/统一入口/核心.py:67 | 转调 有界IO.py:21 |
| 手拼 `file:?mode=ro`（静默截断风险） | 模型连接器/实现/模型路由.py:95 | 转调 数据库URI.py:55 |
| 第二套结果类（字段不同） | 发布门禁/发布治理.py:70 | 改用 结果类型.py:29 |
| 直连 sqlite ×11（记忆:114／图库:68／索引库:45／会话存储:145／嵌入缓存:70／模型用量:79／工具执行:156／库连接:91／幂等存储:54／权威状态_连接迁移面:69／独立进程_账本:112） | 各支持库与运行核心 | 转调 数据库连接支持库 |
| 直连 sqlite（公共契约够不到支持库） | 诊断/调用账本.py:222／运行时/执行耗时账本.py:78 | 下沉底层 SQLite 原语 |
| 绕写腿落盘 write_text ×7（浏览器宿主:66／技能库:272,947／依赖锁定:164／项目声明:126／环境指纹:160／包仓库服务:151／核心快照:60） | 各层 | 转调 文件系统支持库.写入文件 |
| `parents[2]` 自推系统根（脆） | 执行耗时账本.py:74 | 改用 导入前缀.py:66 |
| 空壳转调函数（仅可读性） | 运行缓存.py:25 | 并入 平台适配.平台稳定缓存根 |
| 逐包手写 base64 ×3（PDF隔离:111／PyMuPDF子进程解析:147／编码转换:57） | 适配层/支持库 | 转调 二进制类型.py |
| 进程提供者自读管道（无界 os.read） | 提供者/进程提供者.py:43,66 | 转调 有界IO.py:21 |
| 拆包后命名空间补名（再导出层） | 平台适配/__init__.py:53 | 调用点直连子模块 |

### 6.4 共性节点唯一性（丁路）

| 问题 | 归属节点 | 修改节点 |
|---|---|---|
| 路径边界 9 处自建（基线内），6 处口径分歧 | 路径边界 | 路径安全.py:89 |
| 解析基准不统一：相对路径按能力自身树解析 | 路径边界 | 平台适配/路径解析.py |
| 租约「现场指纹」腿无 `项目根` ⇒ 恒报已变更 | 写授权/租约 | 平台控制面.查询文件租约 |
| 释放回锁晚于认领解锁 ⇒ 认领即解锁被盖 | 内核锁 | 仓库只读锁.py:805 |
| 账本自建 sqlite：执行耗时账本:78,131／调用账本:222 | 状态库 | 执行耗时账本.py:78 |
| 支持库自建 sqlite ≥11 处 | 状态库 | 各支持库（转能力腿） |
| 平台控制面自建 sqlite 4 处 | 状态库 | 备份恢复/恢复执行.py:47 |
| 4 套账本各自建库/文件，无统一账本节点 | 账本 | 公共契约/诊断（择一） |
| `os.fork` 裸调 | 进程 | 资源硬限制.py:201 |
| `start_new_session=True` 裸写 | 进程 | 重启网关.py:444 |
| 超时 ≥12 套默认值互不引用 | 超时 | 公共契约/运行时（建唯一口径） |
| 同步腿 10.0 与薄壳 9.0/120.0 三套并存 | 超时 | 薄壳服务.py:437 |
| JSON 无非有限数统一守卫 | JSON | JSON解码.py（补通用腿） |
| 出站 HTTP 自建 20+ 处 | HTTP | 网络请求.py:100 |
| 运行核心自建 HTTP 客户端（HTTP连接器:221,261／远程镜像:292,298） | HTTP | HTTP连接器.py:221 |
| 支持库自建 HTTP（召回:61／模型连接基元:184／模型HTTP提供者:119／个人蒸馏:63） | HTTP | 模型HTTP提供者.py:119 |
| 日志无唯一腿：print 直写 stdout（密码签名子进程入口:44／技能模板生成器:31／PDF渲染子进程入口:100） | 诊断 | 各子进程入口 |
| 临时目录 119 处 `tempfile.*`（生产侧） | 临时目录 | 资源管理.py:179 |
| 适配层临时目录自建（FFmpeg:115／拼接:61／LibreOffice:128,230／Tesseract临时文件:20） | 临时目录 | Tesseract/临时文件.py:20 |
| 技能库宿主自建 http.client（判据已豁免） | 能力调用 | 技能库.py:149 |

### 6.5 运行态·任务·状态·资源（戊路）

```
启动运行核心网关.py:57  并发取值接线（自适应选型唯一生产消费点）
:127 构造任务系统（唯一任务腿入口）／:136 网关核心／:172 资源账本维护循环接线（每 300s）
└ 资源协调/句柄服务.py:399 维护循环 → :430 清扫一次 → 权威状态_资源与进程面.py:227／_句柄租约面.py:287／_资源与进程面.py:312
任务调度/任务接入.py:52 构造任务系统 → :59 注册全部能力
└ 任务调度/任务系统.py:133 任务系统（门面·第二份状态）
   ├ :147 进程池／:286 提交／:397 查询（纯只读）／:432 取消
   ├ :313 _同步循环（终态回写唯一收敛点）→ :364 闸门只认「本会话活动」→ :255 保存已加锁→运行库
   ├ :205 _读旧账本（旧腿残留）／:168 运行库＝解析运行数据根/底座运行.db（域=任务，声明唯一落点）
   └ 任务进程.py:147 任务进程池
      ├ :214 提交 → :350 _登记任务表（唯一写入口）→ :364 _驱逐历史任务（有界 16）
      ├ :444 查询（含磁盘快照回退 :457-466）／:488 取消／:193 _准备执行器（spawn 分支）
      ├ 任务进程_监视轮询面.py:66 监视循环（落盘失败吞掉 :84-85）→ :89 判定轮询 → :140 轮询
      ├ 任务进程_回收落盘面.py:63 收敛／:123 落盘波次／:151 落盘（JSON 快照）
      ├ 任务进程_关闭队列面.py:101 排空／:159 停止
      └ 任务进程_对象.py:107 独立任务（:193 本会话活动 闸门源）
资源协调/服务.py:56 资源协调器 → :66 自建第三份权威状态／:212 CAS 提交第二套闭环
运行状态监督/实现/提供者.py:37 默认存储目录＝<缓存根>/平台控制面 → 只读状态库.py:43 只读连接（第二条只读 sqlite 口径）
权威状态.py:195 唯一状态机（5 面：连接迁移/句柄租约/事务锁/资源与进程/可靠性）；:136 锁所有权判据；:88-97 主码常量（与 sqlite错误码判据.py:18-20 逐字重复）
公共契约/运行时/：运行缓存.py:65／数据库URI.py:55（唯一收口）／取消登记.py:88（真用：本地网关.py:74）／资源水位.py:186（真用：执行器_核心.py:162）／资源键表.py:43／执行耗时账本.py:88（唯一调用方＝生成物联动.py:163，开关默认关）
时钟校准.py:116／进程身份.py:69:106（均真用）
运行核心/工程缓存/运行数据/网关幂等.sqlite3（错根残留；因 网关幂等面.py:189 parents[2]=运行核心）
```

| # | 问题 | 归属节点 | 修改节点 |
|---|---|---|---|
| 1 | 终态被陈旧磁盘快照回滚，查询只读不回写 | 任务系统.py:374 | 任务进程.py:444（去快照回退） |
| 2 | 池落盘失败被吞，终态永不到盘 | 监视轮询面.py:84-85 | 回收落盘面.py:123（fail-closed） |
| 3 | 同一任务事实两处存：底座运行.db 与 JSON 快照 | 任务进程.py:457 | 回收落盘面.py:151 |
| 4 | 权威状态库两个文件，监督默认只读平台控制面那份 | 提供者.py:37 | 提供者.py:37 |
| 5 | sqlite 主码常量两处逐字重复 | 权威状态.py:88-97 | 权威状态.py:88-97（改导入） |
| 6 | 资源协调器零生产调用＋第三份库＋第二套 CAS | 服务.py:56/66/212 | 服务.py:212 |
| 7 | 自建第二条只读 sqlite 连接口径 | 只读状态库.py:43 | 只读状态库.py:43 |
| 8 | 启动接线/容量基线消费无生产调用＝熔断未接线 | 容量基线消费.py:41/57 | 容量基线消费.py:41 |
| 9 | 执行耗时账本运行核心零调用、默认关＝空转 | 执行耗时账本.py:88 | 执行耗时账本.py:88 |
| 10 | 旧账本读取腿仍在 | 任务系统.py:205 | 任务系统.py:205 |
| 11 | spawn 分支在唯一支持平台 macOS 恒不生效 | 任务进程.py:193 | 任务进程.py:193 |
| 12 | 网关运行数据根错根，产出 运行核心/工程缓存 残留 | 运行核心/工程缓存/运行数据/ | 网关幂等面.py:189 |
| 13 | 门面/池双层状态话语权分叉（0.5s 同步＋查询刷新两处改同一状态） | 任务系统.py:313 | 任务系统.py:313 |

**判定（戊路）**：不干净。运行态不是「一个源」而是「一个声明源＋至少两份影子」：任务域声明唯一落点＝`底座运行.db`，实际同事实还活在门面内存、池内存、池 JSON 快照；权威状态域同一 schema 的 `权威状态.db` 有**三个文件**，只读监督只覆盖第二个。

**「能力任务终态看不到」的机制（已在码上定位）**：门面查询只读（任务系统.py:397）→ 独立对象来自 `进程池.查询` 的快照回退（任务进程.py:457）→ 闸门「本会话活动」恒真（对象.py:193；本会话登记集永不清理 任务进程.py:182/311）⇒ 陈旧快照把门面终态回滚成非终态；而 `_同步循环` 写终态后该任务已退出同步射程（:340-344），运行库仍留终态 ⇒ 正是「运行库有失败、轮询 100 秒看不到」。

**减法候选（戊路）**：删池磁盘快照回退（任务进程.py:457）／落盘失败 fail-closed（回收落盘面.py:123）／主码常量合一（删 权威状态.py:88-97）／运行状态监督默认目录改指运行核心权威状态库（提供者.py:37）／网关运行数据根错根收口（网关幂等面.py:189）／删资源协调器（服务.py）／自适应选型未接线件接线或删／删旧账本读取腿（任务系统.py:205）／删 spawn 分支（任务进程.py:193）。

---

## 七、减法候选（先稳定性后高效性）

**稳定性优先（口径漂移直接致误判）**
1. 错误码口径合一：删 `网关处理面.py:73-92` 异常分支与 `_调用错误码.py:56` 重复表；`反馈路由面.py:106`、`流式HTTP.py:599` 改用 `本地网关.py:84` 公开映射。
2. 装配模板缓存按 `系统根目录` 加键（`后端核心.py:126`），消除多根/多版本互污。
3. 系统根推导收敛到唯一节点 `导入前缀.取系统根`（`后端核心.py:115`／`提供者生命周期.py:115`／`独立进程_账本.py:89`）。
4. 超时同源：建 `公共契约/运行时/超时口径.py`，连接器默认 10 秒、同步腿阈值、幂等锁等待解耦（**先登记不改数值**）。
5. 装配跳过包暴露到 `/健康`（`后端核心.py:664`），`状态快照` 给出口或删除。
6. 删死分支/空转：`限流器.py:239`、`_调用参数.py:76`、`健康监督.py:23`、`是装配包`（或让它被真调）、`容量基线消费.py`、`提供者生命周期管理器`（接线或删）。
7. 信封构造收口：`_反馈响应:76`、流式 `_写JSON:505`、有界 `_写限流响应:60` → 统一经 `网关信封.py:55`。

**高效性（可合/可删，收益次之）**
8. 4 套账本 → 1；JSON 补通用腿；11 处直连 sqlite → 唯一 SQLite 能力腿（公共契约 2 处需先下沉原语）。
9. 3 套有界HTTP／3 套有界IO／7 处 write_text／3 处 base64 手写 → 各自唯一腿。
10. 采样/探针清单合一（`环境指纹.py:70` ＋ `健康监督.默认探针清单` ＋ `强制校验.py:271`）。
11. 适配层/支持库临时根统一走 `资源管理.创建唯一运行目录`。

---

## 八、待核与已知漂移

1. **行号复核状态**：本文件行号全部来自 4 路子代理只读实测（甲/乙/丙/丁），**主会话未逐条复核**；改动前必须回现场核（口径：`git show HEAD:<文件>` 或现场 Read，别读中间态）。
2. **架构总览漂移**（`开发文档/架构总览.md`）：① 图 1/图 2 仍写「薄壳 stdio · 3 工具（tool_catalog/capability_search/capability_call）」，实际已是 **4 条腿**（速办族与 `操作` 已下线）；② 图注仍引 `运行核心/统一网关/网关核心.py:1150`，该文件现仅 105 行、职责已拆到 `核心处理/`、`传输/`、`协议/`、`安全/`、`运行态/`。
3. **5 路已齐**：甲（启动装配）／乙（调用网关）／丙（公共契约）／丁（共性节点）／戊（运行态）均已实测回传；行号**未逐条复核**，改动前回现场核。
4. **本轮只调研不修**：上述问题表**一条都未修**；要修时按「每问只动一个节点」逐条开工。
