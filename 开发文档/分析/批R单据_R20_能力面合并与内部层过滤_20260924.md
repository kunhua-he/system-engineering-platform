<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-20 单据：能力面合并（T1）与内部层生成侧过滤（T8）—— **T8 已落地；T1 按权威清单登记（本批未合并）**

> **阅读顺序**：§一～§七 = 上一线程的**零产出现状登记**（原文保留，它证明 R-20 当时确实一行没动）；**§八 = 本线程（2026-09-24 12:53 起，开工ID `开工-20260924-125339-55da`）的实做**。要看结论直接跳 §八。

> 开工ID `开工-20260924-124720-22e5`｜租约id `aad96f1b53a4416c`｜取证时间 2026-09-24 12:46–12:48｜HEAD `141fab83`
>
> **先说结论，别误读：本线程（源会话 `01a0d1b2-8690-79e0-87e7-9fbf3860ea93` 派下来的这一支）从未承接 R-20，盘上 R-20 零产出是真的。**
> 不存在「写在别处又被回退」——本线程全部写集只有 R-17 单据（`批R单据_R17_默认档送达_20260924.md`），与 T1/T8 零交集。
> 本单据是**主会话要求补的现状登记**，不是完成记录；T1、T8 两项的活**一行都没动**。

## 一、取证：R-20 零产出（可复现）

| 核验点 | 实测 | 判据 |
| --- | --- | --- |
| 本单据是否存在 | 开工时 `路径校验.通过清单` 回 `存在=false / 待建=true` | 本文件即当时待建的那个 |
| T1 五组包是否被改 | `git status --porcelain` 无 `支持库/适配层/{Git,FFmpeg,Pillow,PyMuPDF,Tesseract}提供者/**` 任何条目，也无对应后端成对包条目 | 零改动 |
| T8 生成侧是否被改 | `git status` 无 `开发工具/契约编译/**`、无 `开发工具/公开调用完整性门禁_检查与判定.py` 条目 | 零改动 |
| 本线程写集 | `git status` 里本线程产生的唯一条目 = ` M 开发文档/分析/批R单据_R17_默认档送达_20260924.md` | 与 T1/T8 零交集 |
| 曾否落过 R-20 又回退 | `开发文档/分析/` 目录列表里从未出现过 `*R20*` / `*R-20*` 文件名 | 无回退痕迹 |

> 同期在写的其他会话（从 `git status` 与租约清单读得，仅作背景，不属本线程）：`开工-20260924-123755-e994`（R-21 开工资料默认送达与返回体量噪声收敛，15 文件写集）、`批R单据_R19_自修复工作区写授权收口` 与 `批R单据_R22_零调用能力清点` 两个未跟踪单据、以及 `模块库/自修复工具/**` + `测试中心/模块库/测试_自修复工具.py` 的写者。⇒ 现场 ≥4 个写者，**编译类任务按派活纪律必须串行**。

## 二、T1｜同名能力 id 面合并：改前条数 → 改后条数

**改前 = 改后（一条都没改）**：

| 组 | 同名能力 id 数（改前） | 改后 | 目标 |
| --- | --- | --- | --- |
| Git | 14 / 14 | **14 / 14（未动）** | 0 |
| FFmpeg | 7 / 7 | **7 / 7（未动）** | 0 |
| Pillow | 9 / 9 | **9 / 9（未动）** | 0 |
| PyMuPDF | 4 / 4 | **4 / 4（未动）** | 0 |
| Tesseract | 3 / 3 | **3 / 3（未动）** | 0 |

- **读数口径**：上表数字**引自权威清单**（`开发文档/未完成事项.md:693`，审计 T1 行），**本线程未重测**（重测需逐包对账 `包声明.json`，属主会话明令「别再展开调查」的范围）。因本线程对这两侧包**零改动**，故「改后」在逻辑上必然等于「改前」；这一条由 §一 的 `git status` 取证支撑。
- **卡在哪一步**：**没开工，所以没卡在具体某行**。本线程收到的任务信是 R-17（注入面默认档送达 ＋ 存量漂移收口），从未收到 R-20 任务信，故未认领 `支持库/适配层/*提供者` 与后端成对包的 `包声明.json`/`能力定义.json`，也未执行 `批量应用精确替换` —— **不存在某个文件某一行的错误码/错误说明可报**（没有失败，是没做）。

## 三、T8｜内部层生成侧过滤：报告 69 条 → 拦截 0 条

| 读数 | 值 | 依据 |
| --- | --- | --- |
| 内部层能力报告数 | **69 条**（16 个内部层包） | `开发工具/公开调用完整性门禁_检查与判定.py:258` 自述原文：「16 个内部层包 / 69 条能力全部仍落在「页面可绑定能力集」内」；`未完成事项.md:694` 同口径 |
| 生成侧拦截数 | **0 条** | 生成侧过滤**未落地**：`开发工具/契约编译/**` 全目录搜 `内部层` = **0 命中** ⇒ 生成器根本不认这个字段 |
| 判据侧闸 | **已在**（批R·R-9 落地） | 同文件 `:250-276`「内部层收口第二面闸」、`收集内部层能力`@283、`检查内部层可绑定`@387；缺口类型常量 `内部层-页面仍引用`@274 / `内部层-可绑定集仍含`@275 |

⇒ T8 的真实形状是**「判据有了、生成没改」**：门禁能判红，但生成「页面/可绑定集」的那条腿**未按 `内部层=真` 过滤**（`:268` 原文：「它未过滤 `内部层` ⇒ 内部层能力**仍可被绑**」），故 69 条仍是存量（只报不拦）。本线程对生成侧**零改动**，拦截数保持 0。

## 四、未做项 + 原因（按「文件 + 行区间 + 具体改法 + 为什么本批做不了」）

### 未做 1｜T1 五组同名 id 收面（能力面合并）

- **方案落点（权威）**：`开发文档/方案/适配层唯一化迁移清单_20260917.md` —— 对齐规则 `:27-32`、逐对判定要点 `:33-53`、词边界口径 `:54-61`；迁移面 `表 2-A 真调用点 :78-142` / `表 2-B 声明与契约 :143-181` / `表 2-C 仅文案 :182-196`；后端成对包处置分档 `A 档降门面 8 对 :214-220` / `B 档 :221-227` / `C 档 :228-232` / `D 档目录删除 :233-238`；逐对清单 `附表 A :399+`（`FFmpeg提供者 ↔ FFmpeg媒体 :404`、`Git提供者 ↔ Git操作 :420`、`LibreOffice提供者 ↔ LibreOffice转换 :443` …）。
- **两侧包（实测存在）**：`支持库/适配层/` 下确有 `Git提供者`／`FFmpeg提供者`／`Pillow提供者`／`PyMuPDF提供者`／`Tesseract提供者` 五个目录（`列出目录` 读数）。
- **具体改法**：按附表 A 逐对把能力 id 收到适配层腿；后端成对包按 §3 分档处置（A 档降门面、D 档删目录）；同步改 `表 2-B` 列出的声明/契约（`包声明.json`、`能力定义.json`、`验证场景引用.json`）、`表 2-C` 文案、以及 `§4.8 测试断言`。
- **为什么本批做不了**：① 本线程**未承接 R-20**（任务信是 R-17）；② 这是**契约变更**（改能力 id + 删/降包）⇒ 必须走 `契约编译` 重算派生件 + 按哲学 5.3 判版本递增 + 跑 `静态编译`/`动态编译`，而现场 ≥4 个活跃写者（§一），派活纪律要求**编译类任务串行**；③ 未认领任何 `支持库/适配层/*` 或后端成对包的租约 ⇒ 按纪律**不得动手**。

### 未做 2｜T8 内部层生成侧过滤

- **判据落点（已在）**：`开发工具/公开调用完整性门禁_检查与判定.py:250-276`（常量与说明）、`:283 收集内部层能力`、`:387 检查内部层可绑定`。
- **缺的落点（待改）**：生成「页面/可绑定集」的那条腿 —— 实测 `开发工具/契约编译/**` 搜 `内部层` **0 命中**，即生成器侧**无任何** `内部层` 判断。具体改法：在生成可绑定集/页面能力面时按 `包声明.json` 的 `内部层=真` 过滤（口径唯一源 = `公共契约/包声明/声明.解析包声明`，`内部层=bool(数据.get(...))`，见 `:286`），使 16 桶 / 69 条自然退出可绑定集。
- **为什么本批做不了**：① 同上，本线程未承接；② 该项在 `未完成事项.md:694` 的处置列写的是「**待裁**」（存量基线只报不拦、现场 0 条页面真引用）—— 在未拿到裁决前动手会与存量基线口径冲突；③ 生成侧属 `开发工具/契约编译`，与 §一 列出的活跃写者同轮编译窗口冲突。

## 五、编译读数（如实：无）

| 项 | 读数 |
| --- | --- |
| `自修复工具.静态编译` | **未跑** —— 本线程任务信（R-17）明写「**不跑编译、不 git 提交**（留给收尾链）」 |
| `自修复工具.动态编译` | **未跑**，同上 |
| 成功/失败/阻断 | **无读数**（不编造；本线程对 T1/T8 零改动，也无可编译的变更集） |
| `pytest` | **未跑**（纪律：测试只允许经 MCP `静态编译`/`动态编译`） |
| git 提交 | **未提交**（本单据按任务信留给收尾链） |

## 六、本批写集

| 文件 | 动作 |
| --- | --- |
| `开发文档/分析/批R单据_R20_能力面合并与内部层过滤_20260924.md` | 新建（本单据，现状登记） |

## 七、需要主会话裁决 / 派活的两条

1. **R-20 要不要真做**：若要，请按 §四 的落点单派任务信（T1 与 T8 分开，T8 需先给「生成侧过滤」的裁决）；两条都属契约变更 ⇒ **编译串行**，请避开 R-19/R-21/R-22 的编译窗口。
2. **R-20 与既有批次的归属**：本线程只登记现状，未认领任何 T1/T8 路径；若 R-20 已由别的会话承接，请以那个会话的单据为准，本单据仅作「零产出」的取证留痕。

---

## 八、本线程实做（2026-09-24 12:53 起 · 开工ID `开工-20260924-125339-55da`）

> 口径：全部读数均为**本批现场实测**（`能力定义.json` 逐条计数 / `包声明.json` 逐条搜 `内部层` / 调用点逐条取行号），命令与判据在每小节内标明。

### 8.1 T8｜内部层生成侧过滤：**已落地**

**落点（生成侧唯一腿）**：`开发工具/项目编译/正式包索引.py::构建索引` —— 它就是「页面可绑定集」的**生成点**（`公开调用完整性门禁` R-9 注释 `:266` 原文指认：可绑定集＝本函数返回的 `能力所有者` 键集，而 `轻代码前端编辑器/页面模型._读取能力目录()` 读的正是同一份）。

**改法（3 文件 10 处精确替换，全成）**

| 文件 | 位置 | 动作 |
| --- | --- | --- |
| `开发工具/项目编译/正式包索引.py` | `:84` 区 | 新增 `页面可绑定能力: dict[str, str] = {}`（与另两表同函数内派生，**不自持第二份判据**） |
| 同上 | `:88` 区 | 包循环首行加 `是内部层 = bool(声明.get("内部层", 假))`（口径唯一源＝`公共契约/包声明/声明.解析包声明`，与门禁 `收集内部层能力` 同读法） |
| 同上 | `:125` 区 | `能力所有者[能力id] = …` 之后加 `if not 是内部层: 页面可绑定能力[能力id] = …` |
| 同上 | `:143` 区 | 索引返回值新增 `"页面可绑定能力"` |
| `开发工具/轻代码前端编辑器/页面模型.py` | `:37-39` | `_读取能力目录()` 改读 `["页面可绑定能力"]`（**页面可绑定集的定义处**） |
| `开发工具/公开调用完整性门禁_检查与判定.py` | `:368`／`:384`／`:266`／`:274` | `页面可绑定能力集()` 取键改 `["页面可绑定能力"]`；三处注释同步改口（含「本桶应收敛为 0」） |

**为什么「另立一个键」而不是直接过滤 `能力所有者`（关键，别当绕路）**

`能力所有者` 是**全量 owner**，有 5 个消费方，其中 4 个**必须**看得见内部层能力：

| 消费方 | 用途 | 若过滤 `能力所有者` 的后果 |
| --- | --- | --- |
| `开发工具/项目编译/项目编译器.py:595` | `能力归属`：`能力id` → `包id`，供依赖闭包 | 内部层能力解析不到 ⇒ `找不到能力所属包` |
| `开发工具/HTML验证/制品事实.py:72` | `owner包id表` → 制品 `包目录表` | 内部层包被挤出制品 ⇒ 违反「装配可用、网关 0 暴露」 |
| `开发工具/快速编译/运行快速编译.py:105` | `_全局能力所有者()`：校验声明能力存在 | 内部层包自校验失败 |
| `测试中心/第一批维修/测试_能力唯一事实源.py:116/:145` | 索引能力集 / owner 断言 | **该文件属 R-23 写集，本批不许碰** |
| `开发工具/轻代码前端编辑器/页面模型.py:39` | **页面可绑定集**（本 T8 的对象） | 正是要过滤的那一个 |

⇒ 过滤 `能力所有者` ＝ 为一个**可见性**诉求去改**调用通道与依赖闭包**（改错面）。本批形态：**同一份索引内派生一个可见性子集，两个「页面侧」读取腿同源改指它** —— 生成点仍在 `构建索引` 一处，**没有新增判据、没有第二份表**。

**前后读数（硬判据 3）**

| 读数 | 改前 | 改后 |
| --- | --- | --- |
| `内部层=真` 桶数 / 能力数 | **16 桶 / 69 条** | **16 桶 / 69 条**（标记不变：本批不增不删 `内部层`） |
| 落在「页面可绑定集」内的内部层能力 | **69 条** | **0 条（69 条被拦）** |
| `内部层-可绑定集仍含` 桶 | 16 桶 / 69 条（存量基线，只报不拦） | 应收敛为 0（桶自然消失） |
| `内部层-页面仍引用` 桶 | 0（现场 0 条真引用） | 0（**不许弄出红**：过滤不会新造页面引用） |
| 调用通道 / 依赖闭包 / 制品装配 | — | **零差异**（`能力所有者`/`依赖能力所有者` 一字未改） |

**收口两面一致（硬判据 4）**：`包声明.json` 的 `内部层=真` 侧 = **16 包**（逐条见 §8.2）；页面引用侧 = **0 条** ⇒ 两侧一致（「标了内部层却仍被页面引用」= 0）。

### 8.2 16 桶逐条（`内部层=真`；本批只读不改）

| # | 包（项目根相对） | `内部层` 行 |
| --- | --- | --- |
| 1 | `支持库/后端/文档转换支持库/textutil转换/包声明.json` | `:51` |
| 2 | `支持库/后端/文档转换支持库/python_docx提供者/包声明.json` | `:47` |
| 3 | `支持库/后端/文档转换支持库/PDF隔离提供者/包声明.json` | `:87` |
| 4 | `支持库/后端/版本控制支持库/Git操作/包声明.json` | `:302` |
| 5 | `支持库/后端/文档转换支持库/PDF文本表格/包声明.json` | `:51` |
| 6 | `支持库/后端/媒体处理支持库/FFmpeg媒体/包声明.json` | `:219` |
| 7 | `支持库/后端/文档转换支持库/PDF生成/包声明.json` | `:23` |
| 8 | `支持库/后端/文档转换支持库/python_pptx提供者/包声明.json` | `:61` |
| 9 | `支持库/后端/文档转换支持库/LibreOffice转换/包声明.json` | `:51` |
| 10 | `支持库/后端/文档转换支持库/openpyxl提供者/包声明.json` | `:51` |
| 11 | `支持库/后端/文档转换支持库/PDF渲染/包声明.json` | `:83` |
| 12 | `支持库/后端/图像处理支持库/图像解码/包声明.json` | `:203` |
| 13 | `支持库/后端/OCR识别支持库/OCR识别/包声明.json` | `:83` |
| 14 | `支持库/后端/转写支持库/转写/包声明.json` | `:71` |
| 15 | `支持库/后端/数据库连接支持库/psycopg数据库/包声明.json` | `:99` |
| 16 | `支持库/后端/数据库连接支持库/SQLite数据库/包声明.json` | `:223` |

**适配层侧 `内部层` 命中 = 0 条**（`支持库/适配层` 全目录搜 `"内部层"` 零命中）⇒ 「内部层」只标在后端成对包上，与 T1 的方向互为佐证。

### 8.3 T1｜5 组同名能力 id：**改前 = 改后（本批未合并）**

| 组 | 后端腿（含 `内部层=真`） | 后端条数 | 适配层腿 | 适配层条数 | 改前 | 改后 |
| --- | --- | --- | --- | --- | --- | --- |
| Git | `支持库/后端/版本控制支持库/Git操作`（1.3.0） | **14** | `支持库/适配层/Git提供者`（1.2.1） | **14** | 14/14 | **14/14（未合并）** |
| FFmpeg | `支持库/后端/媒体处理支持库/FFmpeg媒体` | **7** | `支持库/适配层/FFmpeg提供者` | **7** | 7/7 | **7/7（未合并）** |
| Pillow | `支持库/后端/图像处理支持库/图像解码` | **9** | `支持库/适配层/Pillow提供者` | **9** | 9/9 | **9/9（未合并）** |
| PyMuPDF | `支持库/后端/文档转换支持库/PDF渲染` | **4** | `支持库/适配层/PyMuPDF提供者` | **4** | 4/4 | **4/4（未合并）** |
| Tesseract | `支持库/后端/OCR识别支持库/OCR识别` | **3** | `支持库/适配层/Tesseract提供者` | **3** | 3/3 | **3/3（未合并）** |

（逐条实测：两份 `能力定义.json` 搜 `"能力id"`，Git 14/14、FFmpeg 7/7、Pillow 9/9、PyMuPDF 4/4、Tesseract 3/3，与任务信数字逐字相符。）

**方向（权威原文，不是本线程的取舍）**：`开发文档/方案/适配层唯一化迁移清单_20260917.md` **表 2-B「现 id → 目标 id」逐行写的是「去首段」**：
`版本控制支持库.Git操作.当前状态` → `Git操作.当前状态`；`图像处理支持库.图像解码.像素统计` → `图像解码.像素统计`；`OCR识别支持库.OCR识别.识别图片` → `OCR识别.识别图片`。
⇒ 保留**适配层腿**的 id；**后端腿降门面**（保留目录与实现门面、去掉能力面）；调用点切到适配层 id。

**逐组阻断（为什么本批不合并）**

| 组 | 必改的调用点/声明（表 2-A/2-B 口径 ＋ 本批实测） | 本批能不能动 | 阻断 |
| --- | --- | --- | --- |
| Git | `模块库/开工编排/实现/开工编排.py:40,41`；`模块库/开工编排/{依赖契约/依赖契约.json, 包声明.json, 复用决策.json}`；`模块库/自修复工具/实现/自修复工具.py`（8 个 id，`:140/:157/:169/:181/:239/:246/:248/:261`）；`模块库/自修复工具/{包声明.json, 复用决策.json}` | **不能** | 两个目录**都在本批禁碰清单**（R-21 改开工编排、R-19 改自修复工具）⇒ 切了 id 就把它俩打红 |
| FFmpeg | `模块库/媒体处理/{依赖契约/依赖契约.json, 包声明.json, 复用决策.json}`；`模块库/直播逐字稿/{依赖契约/依赖契约.json, 包声明.json, 复用决策.json, 实现/分片转写.py:15, 实现/复核调度.py:18}` | 能 | 无（受共同阻断） |
| Pillow | `模块库/图像处理/包声明.json:14,18,22,26,30,34,38,42,46` | 能 | 无（受共同阻断） |
| PyMuPDF | `模块库/文档解析/包声明.json:10,26`、`模块库/文档解析/复用决策.json:4,8` | 能 | 无（受共同阻断） |
| Tesseract | `模块库/OCR/{依赖契约/依赖契约.json:4,8,12, 包声明.json:10,14,18, 复用决策.json:10,11,12}` | 能 | 无（受共同阻断） |

**共同阻断（三条各自独立成立）**

1. **契约面变更 ⇒ 必须编译串行且必须拿到「阻断 0」自证**：删后端腿能力面＝改 `能力定义.json`／`包声明.json`（`能力` 列表）+ 派生件重算 + 版本递增。本批现场 ≥4 个写者，本线程 **5 次 `静态编译` 全部拒启（`未核验`）**（见 §8.4）⇒ 在这种窗口里做契约面变更**违反纪律**。
2. **「删能力面」的连带面是整批量**（表 2-C 实测：定义与说明书 243 处 / 测试引用 26 处 / 开发文档 26 处 / 实现代码 78 处），且 `验证场景.json`／`验证场景引用.json`／生命周期契约是**无生成器的手写件**，不一起改即悬空引用。
3. **Git 组无解**，而**只做其余 4 组会造成半迁移**（4 组一条腿 + Git 两条腿；两条腿并存期间 `能力搜索` 对 Git 仍回两条）—— 比不做更差。按任务信「先做能做的，剩下的登记，别硬来」，本批选择**整组登记、不半做**。

**本批对 T1 的唯一实体动作 = 只读取证**（`git status` 零改动 + 两份 `能力定义.json` 逐条计数 + 调用点现场行号）；**未改任何 T1 路径**。

### 8.4 静态编译读数（硬判据 5）

| 次序 | 任务id | 模块ID | 结论 |
| --- | --- | --- | --- |
| 1 | `8294b6fb94a9493b` | `开发工具/项目编译` | `未核验`（拒启） |
| 2 | `d56545091d464295` | `开发工具/项目编译` | `未核验`（拒启） |
| 3 | `c010f93b4c18484e` | `开发工具/项目编译` | `未核验`（拒启） |
| 4 | `6241247061e94d96` | `开发工具/轻代码前端编辑器` | `未核验`（拒启） |

**结论原文（四次逐字相同）**：「五态结论：未核验 —— 拒启（fail-closed）：同树已有编译口在跑，或判不出有没有（不许两个写者同改派生件）。等它跑完再重跑；确认它已死则重跑即自动夺陈旧锁。」
⇒ **本批拿不到「阻断 0」读数**（同树有别的会话的编译口长期占用）。按派活纪律「编译类任务一律串行」+ 任务信「撞上拒启就隔一会儿重试」，本线程共重试 4 次后**如实登记为未拿到读数**，不编造、不改道走终端。**待收尾链在无并发写者的窗口复跑**（`类型=1`，`模块ID` 分别取 `开发工具/项目编译`、`开发工具/轻代码前端编辑器`、`开发工具`）。

### 8.5 本批写集与未做项

| 文件 | 动作 |
| --- | --- |
| `开发工具/项目编译/正式包索引.py` | 改（T8：新增 `页面可绑定能力` 派生键） |
| `开发工具/轻代码前端编辑器/页面模型.py` | 改（T8：`_读取能力目录` 改指新键） |
| `开发工具/公开调用完整性门禁_检查与判定.py` | 改（T8：`页面可绑定能力集` 改指新键 + 三处注释改口） |
| `开发文档/分析/批R单据_R20_能力面合并与内部层过滤_20260924.md` | 续写（本件） |
| `开发文档/未完成事项.md` | 改（第 1／2 条状态更新） |

**未做（逐条如实）**

1. **T1 五组能力面合并**：未做（§8.3 给方向、逐组阻断、必改清单）。
2. **`静态编译` 阻断 0 自证**：未拿到（§8.4，拒启）。
3. **未新增、未删除任何 `内部层` 标记**；未动任何 `支持库/适配层/**` 与后端成对包。
4. **未补 `验证场景.json`**：T8 是生成侧过滤，无新能力 ⇒ 无新场景（如收尾链要求，可补一条「内部层能力不出现在可绑定集」的断言）。
5. **未 git 提交**（主会话统一收口）。

---

## 九、收口线程实做（2026-09-24 13:12 起 · 开工ID `开工-20260924-131201-d2b5` · 取证时 HEAD `06386bc3`）

> 本线程只做三件：① T8 现场取证（改前/改后计数）；② 补跑 `静态编译` 拿「阻断 0」读数；③ T1 归属登记。**T8 的代码一行未改**（§8.1 已落地，并随 `06386bc3` 入库）。

### 9.1 T8 改前/改后计数（硬判据 3）

**只读取证**：直读 `正式包索引.构建索引(仓库根)` 两个键的条数（只读，未写盘）。

| 读数 | 值 |
| --- | --- |
| `能力所有者` 条数（全量 owner，改前＝改后） | **752** |
| `页面可绑定能力` 条数（改后新增键） | **683** |
| 被生成侧过滤条数（＝内部层包所拥有的能力） | **69** |
| 绑定集 ∩ 被过滤项 | **空**（`False`）⇒ 生成侧**真拦**，不是只报 |
| 过滤样例（前 5） | `OCR识别支持库.OCR识别.版本探针`／`…识别图片`／`…语言包列表`／`内部.文字文档.生成`／`内部.文字文档.解析` |

⇒ **报告 69 条（只报）→ 拦截 69 条（生成侧真过滤，可绑定集内 0 条）**；与权威清单「16 桶 / 69 条」逐字相符。

### 9.2 静态编译读数（硬判据 5，走异步腿）

> 口径变化：同步腿（`操作=调用能力`）已被网关按「超时秒=600 ≥ 10」拒启并回 `须走异步腿` ⇒ 本轮一律 `任务提交` ＋ `任务查询`（§8.4 那 4 次「拒启」属同步腿时代的形状，现已不存在）。

| 次序 | 任务id | 模块ID | 五态 | 成功/失败 | 阻断 | 只报红 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `9b45968f1cb14f37` | `开发工具/项目编译` | 失败 | 18 / 2 | 1 条（首跑，复跑即消失，见下） | 制品新鲜度 |
| 1′ | `62394151d1be4294`（**复跑**） | `开发工具/项目编译` | **只报告** | 19 / 1 | **0 条** ✅ | 制品新鲜度 |
| 2 | `e66c4faa18614077` | `开发工具/轻代码前端编辑器` | **只报告** | 19 / 1 | **0 条** ✅ | 制品新鲜度 |
| 3 | `a3b5e3bfcbc7438d` | `开发工具` | 未核验 | 0 / 1 | — | — |

- **次序 1′ 与次序 2 都拿到「阻断 0」**（两个被改模块各一条）：`结论原文`＝「五态结论：只报告 —— 无阻断红项，但只报告类有判红：制品新鲜度（退出码 1）」；退出码 **0**。
- **次序 1 首跑的那条阻断已随复跑消失（如实留痕）**：首跑（13:12:18）检查项「重复腿与旁路 → 同动作两条路」判红，`结论原文`逐字：「`支持库/后端/文件系统支持库/内容检索/实现/内容检索.py:29`: [同职责路径判定] 同职责路径边界判定在 10 处各有一份实现（唯一腿 = `支持库/后端/系统核心支持库/路径安全.校验路径`）｜结论：红 —— 基线外新增 1 条」。归因＝`内容检索` 模块**在途未提交改动**（该模块 8 个文件现场 ` M`，会话 `开工-20260924-130647-37d6` 租约活跃），与 T8 的 3 处改动**零交集**；**复跑（13:14:30）该阻断已消失**（同一工作区、HEAD 未变、`内容检索` 仍 8 个文件在途）⇒ 是那个会话在两次编译之间把重复实现自收了，本批无需处置。
- **次序 3 是口径缺口（已登记）**：`开发工具` **不是合法库/模块 id**（`结论原文`：「点名的库 id 不存在（fail-closed）。核对 `包声明.json` 的 包id 字段后重跑」）⇒ `开发工具/公开调用完整性门禁_检查与判定.py` 直挂 `开发工具/` 根、自己没有 `包声明.json`，**无法按模块名点名编译**；它的合规性只能由改它的那两个模块（`开发工具/项目编译`／`开发工具/轻代码前端编辑器`）的读数覆盖。
- **两处「只报红」同源、与本批无关**：制品新鲜度 —— 制品来源提交 `777147f6` ≠ 取证时 HEAD `06386bc3`，落后 5 个提交（`06386bc3` 系别的会话在本批编译前约 1 分钟入库）⇒ 须「重建制品 ＋ 重启网关」（收尾链动作）。

### 9.3 硬判据 4：收口两面一致

| 面 | 读数 |
| --- | --- |
| `包声明.json` 的 `内部层` 标记 | **本批零新增、零删除**（16 桶保持原样；`git status` 无任何 `支持库/适配层/**` 与后端成对包条目） |
| 页面引用该 `能力id` | **现场 0 条**（未变） |
| 生成侧 | **已过滤**：可绑定集内 0 条（§9.1）⇒ 两面一致，且**未弄出红** —— 次序 1／2 各 20 项检查里，`公开调用完整性门禁` 的 `内部层-可绑定集仍含` 桶均未判红 |

### 9.4 T1 归属登记（本批不重复动手）

T1 五组同名能力 id 的合并**已由另一会话承接**：租约所有者 `开工-20260924-130452-b2c2`，任务原文含「R-20续：T1 能力面收口…（有同名孪生的适配层是第二条腿）」，其活跃租约覆盖 `支持库/适配层/{Git,Pillow,PyMuPDF,Tesseract}提供者/{包声明,能力定义}.json`（另有 `开工-20260924-130835-356c` 持 `Tesseract提供者/{复用决策,能力定义}.json`）。⇒ 本线程**不碰 T1 任何路径**，避免两个写者同改一套包。**改前条数仍以任务信为准**（Git 14/14、FFmpeg 7/7、Pillow 9/9、PyMuPDF 4/4、Tesseract 3/3）；**改后条数请以 `b2c2` 会话的单据为准**（本线程不重复测量、不重复动手）。

### 9.5 本线程写集与未做项

| 文件 | 动作 |
| --- | --- |
| `开发文档/分析/批R单据_R20_能力面合并与内部层过滤_20260924.md` | 续写（本 §九） |
| `开发文档/未完成事项.md` | 改（剩余项 #1／#2 两行状态与下一步） |

**未做（逐条如实）**：T1 合并（归 `b2c2` 会话）；`开发工具` 点名（库 id 不存在，口径缺口已登记）；制品重建 ＋ 网关重启（收尾链动作，制品落后 HEAD）；git 提交（主会话统一收口）。
