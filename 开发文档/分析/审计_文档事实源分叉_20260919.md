# 审计：文档事实源分叉（第一轮 · 只读）

> 最后更新：2026-09-19
> 口径：**实测**（全部条目在 HEAD `836e9066` 现场重跑，命令与输出摘要随条附上；未跑测试、未做任何仓库写入）
> 维护者：小马仔（受派：文档事实源分叉审计第一轮）
> 项目根：`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`　分支：主干　**HEAD：`836e9066`**
> 工作树：`git status --porcelain` → **5 项未跟踪**（本轮 5 路并行审计各自的报告，均落在 `开发文档/分析/`；全部为**新增未提交**，无任何已跟踪文件被改动）

**审计范围**：权威文档 7 份 + 根 `README.md` / `AGENTS.md`；`开发文档/规范/`（19 项）、`开发文档/方案/`（39 份）、`开发文档/分析/`（审计开始时 28 份已跟踪件）、`开发文档/决策记录/`（47 份 + 模板）、`开发文档/交付记录/`、`开发文档/归档/`、`开发文档/项目证据/`、`开发文档/临时文档/`。
**判据来源**：`AGENTS.md:277`（权威 7 份）、`开发文档/项目说明.md` 第 4.2/4.5、5.1、5.3、7.9、12.4 条、`开发文档/规范/方案文档生命周期规范.md`、`开发文档/规范/Markdown 文档体例规范.md` 第七节。

---

## 一、结论摘要（分级）

| 级别 | 条数 | 含义 |
|---|---|---|
| **P0 真分叉** | 5 | 同一事实在权威文档之间取值相反，或被推翻的旧账仍存活在权威文档里，会直接误导新会话 |
| **P1 真分叉** | 6 | 过期数字 / 生成物滞后 / 章节与条目重号 / 过程件越界被当事实源 |
| **P2 待收口** | 5 | 体例、登记面或状态值缺口，不误导结论但违反自身规则 |
| **本类未发现** | 2 | 契约版本第二处硬写、决策记录重号/跳号复用（均实测干净） |
| **不是缺陷** | 8 | 合法的过程件/备查件/归档件与设计取舍 |
| **合计逐条明细** | **18** | 二、逐条明细共 18 条（5 + 6 + 5 + 2） |

**一句话结论**：**「多份文档并行自称权威」这个 2026-09-18 那次事故本身没有复发**（当前 `开发文档/方案/` 的「全仓批次编排」实际只有 1 份，其余 38 份都只写「本件事怎么批」）——但**同类风险的三个新形态**已经长出来了：
① **同一份权威文档内部自相矛盾**（`项目说明.md` 同时写「第 1–14 条」与「这 13 条」）；
② **被明文删除的旧账在另一份权威文档里存活**（哲学总账已删的「49 条款 / 8-18-24-3 四态表」，仍完整活在 `目标定义_什么是OK.md`）；
③ **过程件越界进权威正文**（`AGENTS.md` 把一次性审计过程件写成「方案全文见」）。

另有 **1 份生成物带过期数字且缺输入指纹**（`规范/能力目录说明书.md` 写「138 份包声明」，现场 141）、**1 份过程件仍自称「唯一编排入口」**（`方案/二次审计落地编排_20260918.md`，与全仓唯一入口并存）。

---

## 二、逐条明细

### 【P0-1】哲学条数「第 1–13 条」与「第 1–14 条」在权威文档间取值相反，且 `项目说明.md` 一份文件内部就自相矛盾

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `AGENTS.md:277` | 「每个项目只保留一套权威文档（**7 份**）：`开发文档/项目说明.md`（唯一事实源，含**设计哲学第 1–13 条**）」 |
| `开发文档/主开发文档.md:18` | 「**唯一权威事实源**：**设计哲学第 1–13 条（唯一编号，主条 ≤14 / 子项 ≤56）**」 |
| `开发文档/哲学总账.md:8` | 「条款原文的唯一真源是 `开发文档/项目说明.md` 的《设计哲学》段（**第 1–13 条**）」 |
| `开发文档/项目说明.md:9` | 「全库**只有一套条款编号**，就是本文**《设计哲学》第 1–14 条**」 |
| `开发文档/项目说明.md:103` | 「## 设计哲学（**第 1–14 条**，唯一编号）」 |
| `开发文档/项目说明.md:13` | 「引用平台规则一律写「第 N 条」，**N 只指本文这 13 条**」 |

**冲突点**：**四份文件分成两派**——AGENTS / 主开发文档 / 哲学总账说「1–13 条」，项目说明正文（章节标题 + 编号体系声明）说「1–14 条」。更严重的是 `项目说明.md` **自己第 9 行写 1–14、第 13 行写 13**，同一份文件两句互斥。第 14 条（14.1–14.4「归因与校验的位置」）**已真实存在**（`项目说明.md:354-370`，子项 14.1/14.2/14.3/14.4 全部在位，且被决策 `0035`、`0039`、`0040` 引用），故「13 条」是**过期口径**。

**现场命令 + 输出摘要**

```bash
timeout 60 rg -n "1–13 条|1-13 条|1–14 条|13 条|14 条" AGENTS.md 开发文档/项目说明.md 开发文档/主开发文档.md 开发文档/哲学总账.md
```

→ 命中 6 处，如上表逐条对应（三处「1–13」+ 两处「1–14」+ 一处「这 13 条」）。

```bash
timeout 60 rg -n "^1[0-4]\.[0-9]+ \*\*" 开发文档/项目说明.md | wc -l
```

→ `4`（14.1/14.2/14.3/14.4 真实存在，第 14 条不是纸面）。

**建议归口**：以 `项目说明.md` 正文为准（「第 1–14 条」），**同批改三处引用**：`AGENTS.md:277`、`主开发文档.md:18`、`哲学总账.md:8` 一律改为「第 1–14 条」；`项目说明.md:13` 的「这 13 条」改为「这 14 条」。四项必须同批落，否则改完仍是三派。

**影响面**：`AGENTS.md` 是每次开工第一份读的文件，`主开发文档.md` 是入口索引，`哲学总账.md` 是强制情况登记——三处同时给错条数上界，新会话按「13」去核对会认为第 14 条不存在，**直接丢失 14.1–14.4 四条规则**（含「连续两次失败必须归因补底层」这条华哥亲自追问过的规则）。

---

### 【P0-2】哲学子项数三方不一致：项目说明 58 / 哲学总账 55 / 上限 60 vs 56；哲学总账实测漏登记 4 条

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/项目说明.md:109` | 「2026-09-18 收口后实测：**14 主条 / 58 子项**（上限 主条 ≤14、**子项 ≤60**；2026-09-18 因新增 12.3⑧⑨ **上调子项上限 56→60**）」 |
| `开发文档/哲学总账.md:1` | 「# 哲学总账（**14 主条 / 55 子项** → 判据 → 强制点 → 状态）」 |
| `开发文档/哲学总账.md:111-112` | 「子项 **55** 条（2026-09-18 新增 14.1 / 14.2…）」「上限：主条 ≤14、**子项 ≤56**（12.4② 同日修订；2026-09-18 用满未破）」 |
| `开发文档/主开发文档.md:18` | 「（唯一编号，主条 ≤14 / **子项 ≤56**）」 |

**冲突点**：**子项实数两说**（58 vs 55）、**子项上限两说**（60 vs 56）。按 `项目说明.md:339`（12.4②）原文，上限**已由 56 上调为 60 并写明理由**，故「≤56」是**被取代的旧口径**，却在 `哲学总账.md:112` 和 `主开发文档.md:18` 两处存活。子项数 55 是**漏登记**（不是另一种切法，见下）。

**现场命令 + 输出摘要**

```bash
timeout 60 rg -o "^[0-9]+\.[0-9]+ \*\*" 开发文档/项目说明.md | sort -u | wc -l
# → 58

timeout 60 rg -o "^[0-9]+\.[0-9]+ " 开发文档/哲学总账.md | sort -u | wc -l
# → 55

# 差集
timeout 20 comm -23 <(sort -u /tmp/项目说明子项.txt) <(sort -u /tmp/总账子项.txt)
# → 4.5  13.3  14.3  14.4     ← 项目说明有、总账没有
timeout 20 comm -13 <(sort -u /tmp/项目说明子项.txt) <(sort -u /tmp/总账子项.txt)
# → （空）                     ← 总账没有多登记的
```

**哲学总账漏登记的正是 4 条**：`4.5`（方案文档本身也归口）、`13.3`（归因四环链）、`14.3`（反复犯错要补底层）、`14.4`（文案给人看、错误码给机器判）——且 `哲学总账` 自己的抬头就写「本文件**只登记强制情况**」，漏登记等于**这 4 条规则在强制面上不存在**。

**建议归口**：① 上限口径统一到 `项目说明.md` 的「子项 ≤60」（`哲学总账.md:112`、`主开发文档.md:18` 同批改）；② 哲学总账补登 4.5 / 13.3 / 14.3 / 14.4 四行，抬头计数改为「14 主条 / 58 子项」；③ 条数**只在一处声明**（体例规范第七节已明文），`哲学总账.md:111` 的本地计数行应改为「见 `项目说明.md`」不写绝对值。

**影响面**：哲学总账是「一条规则一行账」的强制情况唯一登记处；漏 4 条 = 这 4 条在门禁接线盘点上不会被看见（撞 13.1「判据在、门禁不调即纸面规则」）。

---

### 【P0-3】被明文删除的旧账「49 条款 / 8-18-24-3 四态表」仍完整存活在权威文档 `目标定义_什么是OK.md`

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/哲学总账.md:116-118` | 「旧表已被本行取代（原文 **49 条款 = 8 机器 / 18 半强制 / 24 人工 / 3 纸面**）——**该表有算术缺陷**：分项和 8+18+24+3 = **53 ≠ 合计 49**，且逐条总账与速览两处对不上，故按「有缺陷的旧账不留」**直接删除，不保留**」 |
| `开发文档/目标定义_什么是OK.md:37` | 「现状　**49 条款：8 条机器强制 / 18 条半强制 / 24 条人工 / 3 条纸面**」 |
| `开发文档/目标定义_什么是OK.md:67` | 「① 规则可强制　**纸面 3 条 + 半强制 18 条**　纸面 0；半强制全接线　**21 处**」 |
| `开发文档/目标定义_什么是OK.md:38` | 「目标　纸面规则 = 0」 |

**冲突点**：`哲学总账.md` 已按「有算术缺陷的旧账不留」**整表删除**并留痕说明；但**同一张表在 `目标定义_什么是OK.md`（7 份权威之一、且是「唯一目标判据」）里原样存活**，且带着那个已知的算术缺陷（8+18+24+3=53≠49）。结果是：**同一份被判定作废的数据，一份权威文档说「已删不留」，另一份权威文档拿它当「现状」**。这精确命中哲学 4.2（落痕按类归口，不新开事实源）与 12.4②（同一条规则不得在多处各自表述）。

另：`目标定义.md:69` 的「③ 生成物不漂　手工联动 6 份生成物　一条命令 + diff=0　**139 包**」也是过期数（现场 141 包，见 P1-3）。

**现场命令 + 输出摘要**

```bash
timeout 60 rg -n "49 条款|49 条|半强制 18|纸面 3" --glob '!开发文档/归档/**' --glob '!开发文档/参考资料/**' -g '*.md'
```

→ `开发文档/哲学总账.md:116`（作为「已删」的说明引用）、`开发文档/目标定义_什么是OK.md:37`、`开发文档/目标定义_什么是OK.md:67`（**作为当前现状**）、`开发文档/分析/深度审计_底座升级清单_20260919.md:168/229/239`（过程件引用）、`方案/项目总纲重写方案_20260917.md:91/93`（过程件引用）。

**建议归口**：`目标定义_什么是OK.md:37` 与 `:67` 的「49 条款 / 8-18-24-3 / 纸面 3 / 半强制 18」**删除绝对值**，改为「见 `哲学总账.md` 四态重测绘结论」——哲学总账 `:114` 已写明「四态统计**待批 1 接线后一次性重测绘**」，目标定义应引用该口径，不自行写数。

**影响面**：`目标定义_什么是OK.md` 是「OK 阶段四判据」的唯一判据。判据①「规则可强制」的**现状基线**是错的（且数学不闭合），用它算差距（「21 处」）会得出错误的完成度。

---

### 【P0-4】`方案/二次审计落地编排_20260918.md` 仍自称「唯一编排入口」，与全仓唯一编排入口并存（撞 4.5）

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/方案/二次审计落地编排_20260918.md:6` | 「> 状态：生效（**唯一编排入口**，本批次的派活依据）」 |
| `开发文档/方案/执行编排_唯一入口_20260918.md:7` | 「**覆盖关系**：本文是**全仓唯一编排入口**。子方案只写「本件事怎么落」，**不得再写批次顺序**」 |
| `开发文档/规范/方案文档生命周期规范.md:57` | 「**全仓任何时刻，只允许一份「编排入口」文档**」 |
| `开发文档/规范/方案文档生命周期规范.md:73` | 互踩形态「自称唯一」判据：「正文出现「唯一权威 / 唯一主线 / **编排入口** / 唯一入口」的文档 **> 1 份**」→ 处置「保留真正的唯一者，其余改措辞并指向它」 |
| `开发文档/主开发文档.md:71` | 「`方案/二次审计落地编排_20260918.md` \| **二次审计唯一编排入口**：逐条证真结果…」 |
| `开发文档/方案/底座升级方案.md:346` | 「├─ 开发文档/方案/执行编排_唯一入口_20260918.md   ← 现行编排入口（本文已取代）」 |

**冲突点**：`执行编排_唯一入口_20260918.md:7` 声明自己是「**全仓**唯一编排入口」；`二次审计落地编排` 声明自己是「**本批次**唯一编排入口」——按 `方案文档生命周期规范.md:63` 「**子方案不得自建编排**：子方案只写「本件事怎么落」，不写「全仓批次顺序」」，二者**用同一顶帽子**（唯一编排入口）互相踩。`主开发文档.md:71` 更是把它单列进「C+ 本批次编排」栏并**照抄「唯一编排入口」措辞**，等于在地图层面承认两套。

**该问题已被自查发现但未修复**：`开发文档/分析/审计报告_全仓现状_20260918.md:211-212` 原文：

> 「4 份不合规：…`二次审计落地编排_20260918.md`（状态自称「唯一编排入口」）」
> 「编排入口唯一性 \| `执行编排_唯一入口_20260918.md` 是唯一 H1 级编排 \| **但 `二次审计落地编排` 正文自称「唯一编排入口」→ 两份并存，违反规范 §三**」

**现场命令 + 输出摘要**

```bash
timeout 60 rg -n "唯一编排入口|本批唯一编排" -g '*.md' --glob '!开发文档/参考资料/**'
# → 开发文档/方案/执行编排_唯一入口_20260918.md:7      本文是**全仓唯一编排入口**
#   开发文档/方案/二次审计落地编排_20260918.md:6      生效（**唯一编排入口**，本批次的派活依据）
#   开发文档/未完成事项.md:66                         （**唯一编排入口**）  → 指执行编排
#   开发文档/未完成事项.md:181                        （本批唯一编排入口）  → 指二次审计落地编排
#   开发文档/主开发文档.md:45 / :71                   两份都标「唯一编排入口」
#   开发文档/决策记录/0036:48                         全仓唯一编排入口 → 指执行编排
#   开发文档/决策记录/0037:45                         成为**全仓唯一编排入口** → 指执行编排
```

**同一事实源内部两处指代相反**：`未完成事项.md:66` 与 `:181` **都在同一份权威文档里**，一个说执行编排是唯一入口、一个说二次审计落地编排是本批唯一入口——**加上 §8.7 重号三次（见 P1-6），这两行指代哪一份完全靠猜**。

**建议归口**：`二次审计落地编排_20260918.md:6` 的状态行改为「> 状态：**已完结（2026-09-18）** —— 过程件，不作为派活依据；结论已并入 `未完成事项.md` §8.7 与决策 `0041`–`0046`」，并把「唯一编排入口」措辞删掉（改为「见 `执行编排_唯一入口_20260918.md`」）。`主开发文档.md:71` 的「二次审计唯一编排入口」改为「二次审计过程件（已完结）」。**注意 `主开发文档.md:73-74` 已经写对了**（「该编排是**过程件**…编排本身不作为事实源」）——只是 `:71` 的表行与之矛盾，两行同批统一即可。

**影响面**：派活时按「唯一编排入口」找活干的新会话有 50% 概率读错文件（读到已全部提交完的二次审计编排，误以为还有 21 条没做）。

---

### 【P0-5】`AGENTS.md` 把「一次性审计过程件」写成硬规则的「方案全文见」（撞 4.2 / 4.5）

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `AGENTS.md:162` | 「- 方案全文见 `开发文档/分析/深度审计_底座升级清单_20260919.md` 第八、九节。」 |
| `AGENTS.md:277` | 权威文档只 7 份；`开发文档/分析/` **不在** 7 份之内 |
| `开发文档/方案/方案文档生命周期规范.md:27` | 「**方案文档是「过程件」，不是「事实源」。**」 |
| `开发文档/规范/方案文档生命周期规范.md:87-90`（4.5） | 「**子方案只写「本件事怎么落」**；**待办状态以 `未完成事项.md` 为唯一权威**」 |

**冲突点**：`AGENTS.md:162` 位于「## 验收 → 开发循环第一动作（华哥 2026-09-19 裁决「易语言模式」）」段末，是**硬规则的落点指引**。但它指向的 `开发文档/分析/深度审计_底座升级清单_20260919.md` 是 **2026-09-19 的一次性只读审计过程件**（该文件 `:9` 自述「四项机器门禁真跑 + 全仓 AST 级导入分类（**一次性只读脚本**，放 `/tmp` 未进仓库）」），且该文件内含 **18 处「待办/待修/施工项/必须」字样**（现场 `rg -c` 实测）。按 4.2，落痕应归口权威文档；按 4.5，**子方案/过程件不得作为待办的唯一落点**。这里是**反向越界**：不是过程件抢权威，而是**权威硬规则把过程件当成了自己的规格正文**。

**现场命令 + 输出摘要**

```bash
timeout 60 rg -n "开发文档/分析/" AGENTS.md 开发文档/项目说明.md 开发文档/未完成事项.md 开发文档/主开发文档.md \
  开发文档/哲学总账.md 开发文档/目标定义_什么是OK.md 开发文档/当前流程图.md 开发文档/架构总览.md README.md
# → AGENTS.md:162                       方案全文见 开发文档/分析/深度审计_底座升级清单_20260919.md 第八、九节
#   开发文档/未完成事项.md:71            立项依据：开发文档/分析/DGX-Spark…（合规：立项依据引用）
#   开发文档/项目说明.md:205             超线文件进 开发文档/分析/超长文件拆分候选_20260918.md（合规：清单类）

timeout 30 rg -c "待办|待修|施工|必须" 开发文档/分析/深度审计_底座升级清单_20260919.md
# → 18
```

**对比**：另两处对 `分析/` 的引用**是合规的**——`未完成事项.md:71` 是「立项依据」（引用来源，不复制结论）、`项目说明.md:205` 是「超线文件进该清单」（清单本身不是事实源，只是候选表）。**只有 `AGENTS.md:162` 越界**：它把过程件的「第八、九节」当成开发编译口这条硬规则的**正文**。

**建议归口**：`AGENTS.md:162` 改为「- 开发编译口的机制说明与验收口径见 `开发文档/规范/批量开发流程与分级验证.md`（若尚未收录，先补进规范，再删本行对过程件的指向）；过程件 `开发文档/分析/深度审计_底座升级清单_20260919.md` 只作历史，不作为派活依据。」**不要在 `AGENTS.md` 里保留对 `分析/` 文件的指向**。

**影响面**：`AGENTS.md` 是「项目硬规则」唯一载体。它对 `分析/` 的指向会被后续会话当成先例（「既然 AGENTS 能引分析件，我也能」），是**过程件越界的传播源**。

---

### 【P1-1】`目标定义_什么是OK.md` / `项目说明.md` 手写包数、能力数、说明书分母全部过期（撞 7.9「派生优先于手写」）

**涉及文件与行号**

| 文件:行号 | 文档写的值 | 现场实测值 |
|---|---|---|
| `开发文档/项目说明.md:43` | 「源头能力数　`能力定义.json` **136 份**（聚合父包无此文件：**142 − 136 = 6** 个聚合父包）」 | 五根 `能力定义.json` = **135**；全仓 = **136** |
| `开发文档/项目说明.md:49` | 「包声明 `包声明.json` **141 份** ｜ 全仓 `使用说明.md` **142 份** ｜ 包级 `说明/使用说明.md` **141 份**（生成物类 116 / **人工类 25**）」 | 全仓 **142 / 143 / 142**（生成物类 116 / **人工类 26**） |
| `开发文档/目标定义_什么是OK.md:69` | 「③ 生成物不漂 … **139 包**」 | 全仓 **142** 包 |

**冲突点**：`项目说明.md:43` 的算式**自己不闭合**——它写「142 − 136 = 6」，但 142 是**含非正式根的包声明数**，136 是**含非正式根的能力定义数**；而该行给出的取证命令是 `find 支持库 模块库 技能库 平台控制面 运行核心 -name 能力定义.json | wc -l`，该命令实测返回 **135**（不是 136）。⇒ **命令与声称值不符**。`:49` 同理：它自己的取证命令把 `归档`、`参考资料` 一起排除、**包含** `启动监督器`，实测输出是 `包声明 142 | 使用说明 143 | 包级 142（生成物类 116 / 人工类 26）`，与它声称的 `141 | 142 | 141（…25）` **全部对不上**。

**现场命令 + 输出摘要**

```bash
# 项目说明:43 的取证命令（原文照抄，未改）
timeout 60 find 支持库 模块库 技能库 平台控制面 运行核心 -name '能力定义.json' | wc -l
# → 135          （文档写 136）

# 五根包声明
timeout 60 find 支持库 模块库 技能库 平台控制面 运行核心 -name '包声明.json' | wc -l
# → 141

# 项目说明:49 的取证命令（原文照抄，未改）
timeout 120 python3.14 -c "
from pathlib import Path
根=Path('.').resolve(); 排除={'工程缓存','.git','__pycache__','归档','参考资料'}
包声明=[p for p in 根.rglob('包声明.json') if not (排除 & set(p.parts))]
说明=[p for p in 根.rglob('使用说明.md') if not (排除 & set(p.parts))]
包级=[p for p in 说明 if p.parent.name=='说明']
生成物类=sum(1 for p in 包级 if '调用口径以本文为准' in p.read_text(encoding='utf-8'))
print('包声明',len(包声明),'| 使用说明',len(说明),'| 包级',len(包级),f'（生成物类 {生成物类} / 人工类 {len(包级)-生成物类}）')
"
# → 包声明 142 | 使用说明 143 | 包级 142（生成物类 116 / 人工类 26）
#    文档写: 包声明 141 | 使用说明 142 | 包级 141（生成物类 116 / 人工类 25）

# 五根外还有哪份包声明
timeout 120 python3.14 -c "
from pathlib import Path
根=Path('.').resolve(); 排除={'工程缓存','.git','__pycache__','归档','参考资料'}
全部={p for p in 根.rglob('包声明.json') if not (排除 & set(p.parts))}
五根=set()
for r in ('支持库','模块库','技能库','平台控制面','运行核心'):
    for p in (根/r).rglob('包声明.json'):
        if not (排除 & set(p.parts)): 五根.add(p)
for p in sorted(全部-五根): print('五根外:',p.relative_to(根))
"
# → 全仓 142 五根 141
#    五根外: 启动监督器/健康监督/包声明.json
```

**另一个真分叉**：`项目说明.md:49` 写「人工类 **25**」，而 `开发文档/项目证据/说明书白名单.json` 的「计数」段写「人工类 **26**」、「人工类细分_人工独创 4 + _人工独创工具级 1 + _G2生成器血统 21」；`规范/说明书权威分工与维护规约.md:111` 已明文声明这是**两套切法**（「与本文 7.1 的生成物类 116／人工类 25 属**不同切法**，引用时必须写明用的是哪一套口径」）。⇒ **规约已知道这是口径分歧并写了「必须写明口径」，但 `项目说明.md:49`（唯一事实源）没有写**，读者无法判断该用哪个数。

**建议归口**：按 7.9「派生优先于手写」——这三行**全部改为「跑命令取真值」，正文不写绝对值**（`README.md:5` 已经这么做了：「只写不变式与取证命令，不写会漂的具体数字」）。这是本项目**已经采纳的口径**，`项目说明.md` 与 `目标定义.md` 只是没跟上。
另：`项目说明.md:49` 必须补一句「人工类口径两套（包级 25 含/不含工具级）见 `说明书白名单.json`」，或直接删掉绝对值只留命令。

**影响面**：`项目说明.md:43/49` 是「唯一权威口径表」，被全仓当引用源（`规范/说明书权威分工与维护规约.md`、`方案/适配层唯一化迁移清单` 等都在引）。数字过期会让所有的「分母」推算连锁出错——例如 `场景覆盖率口径.md` 分母 135 就是从这里推出来的（见 P1-5）。

---

### 【P1-2】生成物 `规范/能力目录说明书.md` 带过期数字且缺 7.9 要求的输入指纹

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/规范/能力目录说明书.md:1` | `<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->` |
| `开发文档/规范/能力目录说明书.md:5` | 「共 **138** 份包声明。」 |
| `开发文档/规范/说明书权威分工与维护规约.md:264` | 「\| 3 \| 包声明份数 \| `开发文档/规范/能力目录说明书.md` 首行写「共 **138** 份包声明」 \| **141** \| ⬜ **待处理**：该份是生成物，重跑生成器刷新，**不许手改** \|」 |
| `开发文档/项目说明.md:233-234`（哲学 7.9） | 「① 生成物带**输入指纹**（源内容摘要 + 生成器版本 + 契约版本），**写进生成物自身**」 |

**冲突点**：**生成物带过期数字**（138 vs 现场 141 五根 / 142 全仓）。已由 `说明书权威分工与维护规约.md:264` **登记为待处理**（不是没人知道），但**尚未执行**——这是「债已被记录、生成物与源仍在分叉」的状态，不是新发现，但必须在事实源分叉清单里占一行（因为该表头写「共 138 份包声明」，读者会当成当前事实）。
**另：7.9①「输入指纹」没有落地**——该文件首行只有生成器名与来源描述，**没有源内容摘要、没有生成器版本、没有契约版本**。现场扫描全仓 `.md`：**只有这一份带头部生成标记**；`支持库/**/说明/使用说明.md`（116 份生成物类）**全都没有机器可识别标记**（实测 `head -3` 无 `<!-- 生成物 -->`）。

**现场命令 + 输出摘要**

```bash
timeout 30 rg -c "^\| [^|]+ \| [^|]+ \| (支持库|模块库|技能库) \|" 开发文档/规范/能力目录说明书.md
# → 120（表体行）

timeout 30 head -5 开发文档/规范/能力目录说明书.md
# → <!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->
#    （空行） # 系统能力目录（空行） 共 138 份包声明。

timeout 120 rg -l "请勿手改" 开发文档/规范/开发文档/分析/ 开发文档/方案/ 2>/dev/null
# → 开发文档/规范/能力目录说明书.md、开发文档/规范/说明书权威分工与维护规约.md
#   （后者是「提到」该标记，不是自身是生成物）

timeout 20 head -3 支持库/前端/浏览器宿主/说明/使用说明.md
# → # 浏览器宿主（支持库.前端.浏览器宿主）
#    （空行） - 类型：支持库
#    ← 无任何「生成物/勿手改/输入指纹」标记（7.9② 未落地）

timeout 120 rg -l "调用口径以本文为准" --glob '**/说明/使用说明.md' --glob '!工程缓存/**' | wc -l
# → 116（生成物类说明书，全部无机器可识别标记）
```

**建议归口**：① 重跑 `开发工具/说明书生成/说明书生成器.py::生成目录说明书` 刷新（规约 `:264` 已给处置）；② 给生成器补 7.9① 的输入指纹头 + 7.9② 的机器可识别标记，**同批刷 116 份 `使用说明.md`**（这是门禁线，不是本轮范围）。

**影响面**：`能力目录说明书.md` 被 `AGENTS.md:278` 列为 B 栏规范之一——读者会把它当「当前能力目录的权威份数」。

---

### 【P1-3】正式根数「5 根」与「6 根」：包级设计说明未跟 `正式根.py` 的扩充

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `公共契约/正式根.py:27` | `正式根名表: tuple[str, ...] = ("支持库", "模块库", "技能库", "平台控制面", "运行核心", "启动监督器")` ← **6 根** |
| `开发文档/架构总览.md:41` | 「`运行核心/加载器` · 按 `公共契约/正式根.py` **六根发现装配**<br/>支持库 / 模块库 / 技能库 / 平台控制面 / 运行核心 / 启动监督器」 ← **6 根（已同步）** |
| `启动监督器/健康监督/说明/设计说明.md:56-59` | 「`启动监督器/` **不在** `公共契约/正式根.py` 的 `正式根名表`（**当前 5 根**：支持库/模块库/技能库/平台控制面/运行核心），因此本包**不参与装配发现**（与债务 C-5 `项目适配层` 同类）。要让它进装配，需要把 `启动监督器` 加进 `公共契约/正式根.py:23 正式根名表`」 ← **5 根（已过期）** |
| `开发文档/方案/适配层唯一化迁移清单_20260917.md:243` | 「`启动监督器` 2（**唯一在五根之外的包**）」 ← 表述仍按「五根之外」 |

**冲突点**：`正式根.py:27` **已经包含 `启动监督器`**（第 6 项），`架构总览.md:41` 也已是「六根」；但 `启动监督器/健康监督/说明/设计说明.md:56`（包级规范，`主开发文档.md:40` 明文「改哪个包就读那个包的说明」）**仍说不在名表、当前 5 根、本包不参与装配发现**。⇒ **包级说明与代码事实相反**。另 `正式根.py` 自身 `:23` 行号也已漂移（现为 `:27`），`设计说明.md:59` 指的行号失效。

**现场命令 + 输出摘要**

```bash
timeout 20 awk 'NR>=24 && NR<=31 {print NR": "$0}' 公共契约/正式根.py
# → 26: # 唯一清单：顶层正式包根（顺序＝发现/统计顺序）
#    27: 正式根名表: tuple[str, ...] = ("支持库", "模块库", "技能库", "平台控制面", "运行核心", "启动监督器")

timeout 20 awk 'NR>=56 && NR<=59 {print NR": "$0}' 启动监督器/健康监督/说明/设计说明.md
# → 56: `启动监督器/` **不在** `公共契约/正式根.py` 的 `正式根名表`（当前 5 根：
#    57: 支持库/模块库/技能库/平台控制面/运行核心），因此本包**不参与装配发现**
#    58: （与债务 C-5 `项目适配层` 同类）。要让它进装配，需要把 `启动监督器` 加进
#    59: `公共契约/正式根.py:23 正式根名表`——该文件属公共契约域，本包不越域改。

timeout 60 rg -n "5 根|五根|六根" -g '*.md' -g '*.py'
# → 启动监督器/健康监督/说明/设计说明.md:56  （5 根，过期）
#   开发文档/架构总览.md:41                 （六根，已同步）
#   开发文档/方案/适配层唯一化迁移清单_20260917.md:243（"唯一在五根之外的包"，过期表述）
#   开发文档/归档/…/取证与历史.md:234        （归档，合法）
```

**建议归口**：`启动监督器/健康监督/说明/设计说明.md` 的「已知边界」段整段重写（本包**实际已参与装配发现**，现场 `find 启动监督器 -name 包声明.json` → 1，且 `未完成事项.md`/`项目说明.md` 的包数口径已含它）。`正式根.py:23` 的行号引用改为「见 `公共契约/正式根.py::正式根名表`」（不写行号，防漂移）。

**影响面**：包级设计说明是「改这个包先读」的规范，说错「本包不参与装配」会让人误判改动能否生效。

---

### 【P1-4】`规范/场景覆盖率口径.md` 自称「权威处＝本文件」但不在任何权威清单里，且分母已过期

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/规范/场景覆盖率口径.md:5` | 「> 维护者：验证资产治理（**权威处＝本文件**；本文件只定口径，不改源码、不接线门禁）」 |
| `开发文档/规范/场景覆盖率口径.md:106-109` | 「分母（正式公开能力全集）= **681** … 分子 … = 681 … 场景覆盖率 = 100.0000%」 |
| `AGENTS.md:278` | 「**规范收口在固定位置**：全局规范只在 `开发文档/规范/`（多会话并行开发规约 / 批量开发流程与分级验证 / 网关调用最小示例 / 新建支持库与模块指南 / 新建技能包指南 / 通用能力反馈接口与平台自治治理 / 资源回收确认机制 / 轻代码前端与独立编译契约 / 能力目录说明书 / **Markdown 文档体例规范**）」← **不含 `场景覆盖率口径.md`** |
| `开发文档/主开发文档.md`（B 栏 13 项） | **不含 `场景覆盖率口径.md`** |
| `开发文档/项目说明.md:44` | 「正式索引 \| 支持库 + 模块库 + 技能库合计 **689 条能力**」 |

**冲突点**：① **该文件的「权威处」自称没有被任何权威清单承认**——`AGENTS.md:278` 的规范白名单（10 项）与 `主开发文档.md` B 栏（13 项）都**没有列它**，但它在 `开发文档/规范/` 下、被 2 个文件引用（`审计报告_全仓现状_20260918.md:123` 指出它「写能力 **681**、分母 135」与当前值不符）；② `:106` 分母 **681** 与 `项目说明.md:44`「合计 **689 条能力**」冲突。
**是否越界**：该文件的「权威处＝本文件」是**限定在「场景覆盖率这一个指标的分子分母口径」范围内**（同段限定「只定口径，不改源码、不接线门禁」），**不等于**全局事实源——**尺度上不构成「并列自称权威」**。但它**没被登记进规范清单**，属「该列未列」（撞 §三「规范只在 `开发文档/规范/`」的登记面）。

**现场命令 + 输出摘要**

```bash
timeout 20 sed -n '1,8p' 开发文档/规范/场景覆盖率口径.md
# → # 场景覆盖率口径
#    > 最后更新：2026-09-18
#    > 口径：实测（随建包与提交逐分钟变化，引用前必须重跑「六、取证命令」）
#    > 维护者：验证资产治理（权威处＝本文件；本文件只定口径，不改源码、不接线门禁）

timeout 60 rg -n "场景覆盖率口径" --glob '!工程缓存/**'
# → 开发文档/规范/场景覆盖率口径.md:1（自身）
#   开发文档/分析/审计报告_全仓现状_20260918.md:123（指出其数过期）

timeout 60 rg -n "场景覆盖率口径" AGENTS.md 开发文档/主开发文档.md 开发文档/项目说明.md 开发文档/目标定义_什么是OK.md
# → （零命中）
```

**建议归口**：① 在 `AGENTS.md:278` 的规范清单与 `主开发文档.md` B 栏**补登 `场景覆盖率口径.md`**（它是真规范、有取证命令、被审计引用，属漏列）；② `:106` 的 681 改为「跑第六节取证命令取真值」不写绝对值；③ 「权威处＝本文件」措辞改为「本指标的分子分母口径以本文件为准（不构成全局事实源）」——**去掉可能被误读为并列权威的表述**。

**影响面**：场景覆盖率是验证资产的覆盖率判据；分母 681 vs 689 差 8 条能力，会让「覆盖率 100%」这个结论失真。

---

### 【P1-5】`未完成事项.md` 三处 §8.7 重号 + §8.6 排在 §8.7 之后（编号顺序倒置）

**涉及文件与行号**

| 文件:行号 | 章节标题 |
|---|---|
| `开发文档/未完成事项.md:178` | `### 8.7 二次审计（2026-09-18 外部独立审计）落地` |
| `开发文档/未完成事项.md:240` | `### 8.6 新增债务（全量精校现场实测）` ← **顺序倒置：8.6 排在 8.7 之后** |
| `开发文档/未完成事项.md:253` | `### 8.7 模块库入口注册丢参「必填」→ 8 包 38 条能力必填校验静默失效（2026-09-18 已修）` |
| `开发文档/未完成事项.md:314` | `### 8.7 #71 / #72 收口（2026-09-18 夜间，第二路接手，提交 `f59e2266`）` |
| `开发文档/未完成事项.md:23` | 「**∴ 真正待处置 = §8.3(6) + §8.4(3) + §8.6(1) + §8.7(21) = 31 条**」 |
| `开发文档/未完成事项.md:24` | 「★ **§8.7 是 2026-09-18 二次审计新增**，其中 41–49 正在并发维修、57–61 只落决策不动代码」 |
| `开发文档/主开发文档.md:74` | 「证真结论已被 `未完成事项.md` **§8.7** 与决策 `0041`–`0046` 吸收」 |

**冲突点**：**`§8.7` 被三个完全不同的章节同时占用**（二次审计落地 / 必填丢参已修 / #71#72 收口）。抬头 `:23` 的「§8.7(21)」与 `:24` 的「§8.7 是二次审计新增」指的是 `:178` 那份，但**读者按编号找会命中三处**（`:253`、`:314` 那两份都不是 21 条）。`主开发文档.md:74` 引「§8.7」同样有歧义。撞 `Markdown 文档体例规范.md` 第七节「编号不复用 / 不重号」与 12.4④（决策记录编号纪律的同类）。另 **§8.6 位于 §8.7 之后**（`:240` 在 `:178` 之后），编号顺序与文档顺序相反。

**现场命令 + 输出摘要**

```bash
timeout 30 rg -n "^### 8\.[0-9]" 开发文档/未完成事项.md
# → 50:### 8.1   54:### 8.2   90:### 8.3   114:### 8.3.1   130:### 8.3.2
#   148:### 8.4   158:### 8.5   178:### 8.7   240:### 8.6   253:### 8.7   314:### 8.7
```

**建议归口**：按「编号不复用」纪律——**保留 `:178` 为 §8.7**；`:253` 与 `:314` 两份是**已修完的收口记录**，按 12.4⑤「条目修好就直接删除（历史由 git 保留），不留已修表格」**整段删除**（两段标题本身就带「已修」「收口」字样）。删完后 `:240` 的 §8.6 自然回到 §8.5 之后，顺序倒置同时消解。若华哥要求留痕，则改为无编号的一行「见提交 `f59e2266`」，不给 §8.x 号。

**影响面**：债务清单是「唯一债务清单」；重号让「§8.7 有 21 条」这个说法在搜索层面不可验证。

---

### 【P1-6】`未完成事项.md` 表内条目号重号：#23 / #7 / #1 各出现两次，且外部文件已按重号引用

**涉及文件与行号**

| 文件:行号 | 条目 |
|---|---|
| `开发文档/未完成事项.md:62` | `\| 23 \| 卡点 D：样本＝接口义务 vs 放未来 \| **做掉**（按哲学 12.5，不推迟） \|`（**§8.2.1**） |
| `开发文档/未完成事项.md:95` | `\| 23 \| **说明书存量治理施工表**（剩 **G3/G4/G5/G9** 四格） \| 中。… \|`（**§8.3**） |
| `开发文档/未完成事项.md:157` | `\| 7 \| D-6 同名文件+同名包合并 … \|`（**§8.4**） |
| `开发文档/未完成事项.md:175` | `\| 7 \| **「能力面 830」口径不可用** \| … \|`（**§8.5**） |
| `开发文档/未完成事项.md:154` | `\| 1 \| **D-1 适配层唯一化** \| … \|`（**§8.4**） |
| `开发文档/未完成事项.md:244` | `\| 1 \| **网关重启遗留模型孤儿 → wired 显存泄漏** \| … \|`（**§8.6**） |

**冲突点**：`#23 / #7 / #1` 各被两个不同章节同时使用。**后果已经外溢**——外部文件按「第 23 项」引用时无法确定指哪一条：

- `开发文档/方案/说明书存量治理施工表_20260918.md:1` 标题：「# 说明书存量治理施工表（**第 23 项**）」→ 指 `:95`
- `开发文档/交付记录/20260918_夜间推进简报_给华哥.md:81`：「**#23 说明书治理** …」→ 指 `:95`
- `开发文档/分析/深度审计_底座升级清单_20260919.md:119`：「…需逐行确认（**#23**）」→ 指 `:95`

—— 这 3 处都指 `:95`，**运气好**；但 `:62` 的 #23 也在同一份文件里，**没有任何机制防止后续会话引用到错的那条**。`#7` 同理：`未完成事项.md` 引用「§8.5 第 7 项」与「§8.4 第 7 项」会分别命中两条。

**现场命令 + 输出摘要**

```bash
timeout 60 rg -o "^\| *[0-9]+ \|" 开发文档/未完成事项.md | sort | uniq -c | sort -rn | head
# →  2 | 57 |   2 | 56 |   2 | 55 |   2 | 54 |   2 | 53 |   2 | 52 |   2 | 51 |
#    2 | 50 |   2 | 23 |   2 | 7  |   2 | 1  |   1 | 73 | …

timeout 60 rg -n "^\| 23 \|" 开发文档/未完成事项.md
# → 62:| 23 | 卡点 D：样本＝接口义务 vs 放未来 …
#    95:| 23 | **说明书存量治理施工表**（剩 **G3/G4/G5/G9** 四格）…
```

**说明（不是缺陷的部分）**：50–61 的双份是**同一编号在不同章节（§8.2.1 与 §8.7）分别登记同一件事的两面**（如 `:79` 编号 50 是探针、`未完成事项.md` §8.7 里 41–61 是审计条目），该文件已在开头说明「§8.2.1 另有 5 条（编号 50–54）」——**属于约定，但同样有歧义风险**。真正需要处置的是 **`#23` / `#7` / `#1`**：它们**指向完全不同的两件事**（不是同一件事的两面）。

**建议归口**：① `:62` 的 #23 所在 §8.2.1 是「新增待裁决」临时段，其 50–54 与 §8.7 的 41–61 已重叠 —— 按 12.4⑤ 收口：**已裁决完的 §8.2.1 段落整段删除**（`:60-63` 三条 20/22/23 已全部裁完，文档自己写「三条已于 2026-09-18 全体裁完」）；② `:157` 的 #7 与 `:175` 的 #7 分属 §8.4/§8.5，**§8.4 的编号空间与 §8.5 独立**已由文档声明（`：150`「本节编号为 1/3/7，缺 2、4–6」）—— 属约定，但**引用时必须带 `§8.4 第 7 项` / `§8.5 第 7 项`**，正文里凡出现裸「第 N 项」一律补节号。

**影响面**：债务清单条目号是派活时的唯一定位手段（12.1④「条目的落点必须现场写明」）。重号会让派活派错条目——`未完成事项.md:114-129`（§8.3.1）自己就记着一次「清单是待办线索，不是事实」的真实浪费。

---

### 【P2-1】`决策记录/0013` 未按规则标「已变更」（描述的状态已被 0030 与第 14 条取代）

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/决策记录/0013_设计哲学收敛为12主条.md:1` | 「# 0013 设计哲学收敛为 **12 主条**（近似条款合并）」 |
| `开发文档/决策记录/0013_设计哲学收敛为12主条.md:3` | 「> 状态：**现行（已落地）**」 |
| `开发文档/决策记录/0030_设计哲学收口_废止1.5_替换9.4_吸收外部借鉴.md:6` | 「## 一、改了什么（**12 主条 / 49 子项 → 13 主条 / 53 子项**）」 |
| `AGENTS.md:281` | 「被推翻的决策不删除，只把状态改为「**已变更**」或「**已废弃**」并写明替代它的编号」 |
| `开发文档/项目说明.md:339`（12.4④） | 「状态变化**原地更新**（改「状态：已变更 / 已废弃」并写明替代编号），**不追加流水段**」 |

**冲突点**：`0013` 的状态是「**现行（已落地）**」，但它描述的「12 主条」已先后被 `0030`（升为 13 主条）与后续第 14 条取代 —— **现行是 14 主条**。按 AGENTS:281 / 12.4④，它应标「已变更 → 见 `0030`」。**这是「被推翻的决策未标已变更」的实例**。

**现场命令 + 输出摘要**

```bash
timeout 20 head -3 开发文档/决策记录/0013_设计哲学收敛为12主条.md
# → # 0013 设计哲学收敛为 12 主条（近似条款合并）
#    （空行）> 状态：现行（已落地）

timeout 30 rg -n "12 主条 / 49 子项|12 主条" 开发文档/决策记录/0030_*.md
# → 6:## 一、改了什么（12 主条 / 49 子项 → **13 主条 / 53 子项**）
```

**建议归口**：`0013:3` 状态改为「> 状态：**已变更（2026-09-17）→ 见 `0030_设计哲学收口_废止1.5_替换9.4_吸收外部借鉴.md`（12 主条 → 13 主条，后又增第 14 条）**」，正文不动（按 12.4④ 不追加流水段）。

**影响面**：`0013` 是「为什么是 12 条」的取舍记录。状态写「现行」会让按编号回溯的人认为 12 是当前口径（与 P0-1 的「1–13/1–14」混淆叠加）。

---

### 【P2-2】`开发文档/临时文档/` 未按 4.2 / 决策 `0004` 清空（结论已并入权威）

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/临时文档/README.md:3` | 「> **这个目录通常应该是空的。**」 |
| `开发文档/临时文档/README.md:9-11` | 「**规矩**：结论一旦并入 `项目说明.md` / `未完成事项.md` / `规范/` / `决策记录/`，**原稿立即删除**，**不在本目录留过程稿**（2026-09-17 已按此清空过一次，19 份全部收口后删除）」 |
| `AGENTS.md:280` | 「临时工作台、审计报告、方案稿在结论并入权威文档后**立即删除**，不与权威文档并列」 |
| `开发文档/分析/审计报告_全仓现状_20260918.md:284` | 「顺手清过程件——`分析/`16 份 ref=0、**`临时文档/1 份`**、…」 ← 该审计已点名这份 |

**冲突点**：`开发文档/临时文档/01_补关键值断言_无法定值与剩余缺口_20260918.md`（10,327 字节，2026-09-18 08:05）**仍在**，其结论已并入 `未完成事项.md:16`（「`13 C3 关键值口径`（真源 `正向断言有实断言()` 重算，零断言 0 成立）」）。按 README 自己的规矩应立即删除。**这不是新发现**——`分析/审计报告_全仓现状_20260918.md:284` 已点名，但未执行。

**现场命令 + 输出摘要**

```bash
timeout 30 ls -la 开发文档/临时文档/
# → 01_补关键值断言_无法定值与剩余缺口_20260918.md   10327  Sep 18 08:05
#    README.md                                       787  Sep 17 21:16

timeout 60 rg -n "关键值|C3" 开发文档/未完成事项.md
# → 16:>   `13 C3 关键值口径`（真源 `正向断言有实断言()` 重算，**零断言 0 成立**；…）  ← 结论已在权威

timeout 60 git log -1 --format="%h|%ad|%s" --date=format:"%m-%d" -- 开发文档/临时文档/01_补关键值断言_无法定值与剩余缺口_20260918.md
# → e75f42ae|09-18|第 13 项（C3 关键值断言）：两路并行补完 55 处，经 HTML 黑盒验收通过
```

**建议归口**：删除该文件（结论已在 `未完成事项.md:16` + `项目说明.md` 第 9 条 9.4.1；历史由 git 保留）。若其中有 `项目说明.md` 未覆盖的「无法定值清单」，先把它并入 `未完成事项.md` §8.5（该节正是「已知覆盖缺口」）。

**影响面**：`临时文档/` 按定义不应与权威并列。留着它 = 有一份「过程稿」躺在权威目录树里，读者可能把它当第二份清单。

---

### 【P2-3】`交付记录/20260918_夜间推进简报_给华哥.md` 于「不再新增」规则生效后新增，且带过期数字

**涉及文件与行号**

| 文件:行号 | 原文引用 / 事实 |
|---|---|
| `开发文档/主开发文档.md:79` | 「\| [交付记录/](./交付记录/) \| 历史留档：…**只读不改、不再新增**（新交付由 Git 提交说明承载，见 `AGENTS.md`「不再新增阶段报告」） \|」 |
| `AGENTS.md:20` | 「…**不再新增项目记忆、阶段报告或临时审计 Markdown**。」 |
| `开发文档/交付记录/20260918_夜间推进简报_给华哥.md:96` | 「退出码 0 ｜ 通过: 目标能力 **681**，步骤 1042；清理失败 0，资源残留 0」 |
| `开发文档/项目说明.md:44` | 「正式索引 … 合计 **689 条能力**」 |

**冲突点**：**规则时间线**——`主开发文档.md:79` 的「不再新增」措辞由提交 `aeea721a`（**2026-09-17 22:00**）写入；而 `交付记录/20260918_夜间推进简报_给华哥.md` 由 `d4af0bb7`（**2026-09-18 05:35**）新增，之后又被 `788a63a7`/`a6ffa8e2`/`969b8336`/`d8195211`/`518572b7` **多次更新**（最后 09-18 17:00）。⇒ **规则生效后仍在新增 1 个目录级文件 + 6 次更新**。且该简报写死「目标能力 681」，与 `项目说明.md:44` 的 689 冲突。

**现场命令 + 输出摘要**

```bash
timeout 60 git log -1 --format="%h|%ad|%s" --date=format:"%Y-%m-%d %H:%M" aeea721a
# → aeea721a|2026-09-17 22:00|MD 文档收敛：权威收口 + 目录树干净（…）

timeout 60 git log --diff-filter=A --format="%h %ad %s" -- 开发文档/交付记录/20260918_夜间推进简报_给华哥.md
# → d4af0bb7 Fri Sep 18 05:35:26 2026 +0800 新增：2026-09-18 夜间推进简报（交付记录，供华哥与审计者复核）

timeout 60 git log --format="%h|%ad|%s" --date=format:"%m-%d" --since="2026-09-18" -- 开发文档/交付记录/
# → 518572b7|09-18|文档收口… / 788a63a7|09-18|简报更新… / a6ffa8e2|09-18|… / 969b8336|09-18|… / d8195211|09-18|…

timeout 30 rg -n "681|689" 开发文档/交付记录/20260918_夜间推进简报_给华哥.md
# → 96:退出码 0 ｜ 通过: 目标能力 681 …    129:…公开能力 681 个…    281:# ② 覆盖率（应报「公开能力 681 个…」）
```

**建议归口**：① 该简报按「只读不改、不再新增」**移入 `开发文档/归档/202609_夜间推进简报/`**（归档是文档历史的合法去处，`AGENTS.md:282`）；② 若华哥认为它该留，则 `主开发文档.md:79` 的「不再新增」措辞需补一个例外（**但不能两边都保留现状**）；③ 681 按 `项目说明.md:44` 口径改「见当次取证命令」，不写死。

**影响面**：`交付记录/` 是「只看不改」的留档区。规则与事实相反时，下个会话会以为「还能往这里写」，重演过程件堆积。

---

### 【P2-4】`主开发文档.md` 文档地图漏列 `项目证据/`（机器账本，是事实源）与 `临时文档/`、`分析/`

**涉及文件与行号**

| 文件:行号 | 原文引用 |
|---|---|
| `开发文档/主开发文档.md:76-82`（D 栏） | 只列 `交付记录/`、`决策记录/`、`归档/`、`参考资料/外部源码研究/` |
| `AGENTS.md:20` | 「运行时验证账本只写入本仓库 **`开发文档/项目证据/`** 的机器文件」 ← 该项目证据是「运行时验证账本」，是机器事实源 |
| `开发文档/主开发文档.md:99` | 「文档收口规则（2026-09-10）：临时工作台、审计报告、节点实施记录在结论并入…后立即删除，**不与权威文档并列**」 |

**冲突点**：`开发文档/项目证据/` 含 `能力id冻结基线.json`、`说明书白名单.json`、`发布门禁143项基线_20260918.json`、`验证历史.jsonl`、`MCP使用反馈.jsonl` —— 这些**是判据的机器事实源**（`规范/说明书权威分工与维护规约.md:82`：「白名单是唯一机器清单」；`未完成事项.md` 多处引冻结基线）。**文档地图 D 栏没有它**，新会话按地图找规范会漏掉这类机器判据。同栏也未列 `临时文档/`（应删）与 `分析/`（过程件），但地图 `:99` 的处理规则已覆盖这两者——**唯独 `项目证据/` 既不在 D 栏、也无归属说明**。

**现场命令 + 输出摘要**

```bash
timeout 30 rg -n "项目证据" 开发文档/主开发文档.md AGENTS.md
# → AGENTS.md:20（唯一一处：运行时验证账本只写入 开发文档/项目证据/）
#    开发文档/主开发文档.md：（零命中）← 文档地图未列

timeout 30 ls 开发文档/项目证据/
# → MCP使用反馈.jsonl  发布门禁143项基线_20260918.json  底座升级
#    能力id冻结基线.json  能力id冻结基线_交付说明.md  说明书白名单.json  验证历史.jsonl
```

**建议归口**：在 `主开发文档.md` D 栏补一行「\| [项目证据/](./项目证据/) \| **机器判据账本**（白名单 / 冻结基线 / 发布门禁基线 / 验证历史）：只由能力写入，不手改；正文引用处见 `AGENTS.md` \|」，并说明 `分析/` 与 `临时文档/` 按 `:99` 规则处置（**在地图上明确它们不在权威面**，避免读者以为「没列=不存在」）。

**影响面**：地图是「新会话照这张表找规范」的唯一入口。漏列机器账本 = 有人会去手改 JSON（撞 7.9 生成物三律）。

---

### 【P2-5】`项目说明.md` 与 `目标定义_什么是OK.md` 元信息头「最后更新」落后于实际改动时间

**涉及文件与行号**

| 文件 | 元信息头写的「最后更新」 | 文件 mtime | 最后提交 |
|---|---|---|---|
| `开发文档/项目说明.md:3` | 2026-09-17 | 2026-09-18 21:18 | `8e983c54` 09-18 21:18 |
| `开发文档/哲学总账.md:3` | 2026-09-17 | 2026-09-18 16:12 | （有 09-18 内容：`:110-112` 写 2026-09-18 新增第 14 条） |
| `开发文档/当前流程图.md:3` | 2026-09-17 | 2026-09-18 19:47 | — |
| `开发文档/主开发文档.md:3` | 2026-09-17 | 2026-09-18 18:17 | `8e134ebc` 09-18 18:18 |
| `开发文档/架构总览.md:3` | 2026-09-17 | 2026-09-17 23:03 | —（一致） |
| `开发文档/目标定义_什么是OK.md:3` | 2026-09-17 | 2026-09-17 23:03 | —（一致） |

**冲突点**：`AGENTS.md:283` 要求「按 `Markdown 文档体例规范.md` 第三节的三行元信息头（最后更新 / 口径 / 维护者）」标注；`项目说明.md` 正文里含 19 处 `2026-09-18` 字样（现场 `rg -c` 实测）却写「最后更新：2026-09-17」。⇒ **元信息头与实际内容不符**（撞体例规范第三节 + AGENTS:283「不能把旧快照写成持续事实」）。

**现场命令 + 输出摘要**

```bash
timeout 30 rg -n "^> 最后更新" 开发文档/项目说明.md 开发文档/哲学总账.md 开发文档/当前流程图.md 开发文档/主开发文档.md
# → 项目说明.md:3  最后更新：2026-09-17
#   哲学总账.md:3  最后更新：2026-09-17
#   当前流程图.md:3 最后更新：2026-09-17
#   主开发文档.md:3 最后更新：2026-09-17

timeout 30 rg -c "2026-09-18" 开发文档/项目说明.md
# → 19

timeout 30 stat -f "%Sm  %N" -t "%Y-%m-%d %H:%M" 开发文档/项目说明.md 开发文档/当前流程图.md 开发文档/主开发文档.md
# → 2026-09-18 21:18  开发文档/项目说明.md
#    2026-09-18 19:47  开发文档/当前流程图.md
#    2026-09-18 18:17  开发文档/主开发文档.md
```

**建议归口**：四份文件的「最后更新」同批改为实际日期（2026-09-18）。

**影响面**：元信息头是读者判断「这份数字是不是当前」的第一依据；写旧日期会让读者主动降权（也可能反过来，把 09-18 的改动当成 09-17 的旧结论）。

---

### 【本类未发现】契约版本第二处硬写（撞 5.1）

**审计动作**：全仓搜「硬写契约版本号」的第二处。

```bash
# 1. 契约版本常量赋值处
timeout 180 rg -n "契约版本\s*=\s*[\"']" -g '*.py' --glob '!工程缓存/**' --glob '!.git/**'
# → 开发工具/HTML验证/常量.py:7   场景契约版本 = "验证场景/v1"   ← 另一根轴（场景契约，非平台契约版本）
#   公共契约/版本规则/契约版本.py:15  契约版本 = "2.0.0"        ← 唯一事实源（5.1 指定）
#   （其余命中全是 docstring 里的说明文字，非赋值）

# 2. 生成器是否从事实源注入（而不是各写一份）
timeout 30 rg -n "取契约版本|from 公共契约.版本规则.契约版本" 开发工具/契约编译/能力定义编译器.py
# → :31  from 公共契约.版本规则.契约版本 import 契约版本
#    :266 当前契约版本 = 契约版本  # 契约版本唯一（哲学第 5 条 1 项）：本文件不再另立 1.0.0
#    :323 版本 = 契约版本

timeout 30 rg -n "契约版本" 项目适配层/依赖锁定/依赖锁定.py
# → :16  from 公共契约.版本规则.契约版本 import 契约版本 as 契约版本常量
#    :25  # 契约版本不写字面量：见文件顶部 …
#    :136 "契约版本": 契约版本常量,

# 3. 派生物里的值是否全等于事实源
timeout 120 rg -o '"契约版本": "[^"]*"' --glob '**/能力契约/参数契约.json' | sed 's/.*: //' | sort | uniq -c
# → 136 "2.0.0"        ← 全部与事实源一致，无第二值

# 4. 手写 md 里的字面量
timeout 60 rg -n "契约版本\s*[=:：]\s*2\.0\.0|当前版本": "2\.0\.0" -g '*.md'
# → 开发文档/规范/网关调用最小示例.md:116    "当前版本": "2.0.0",   ← 示例返回体
```

**结论**：**本类未发现「第二处硬写契约版本」的真分叉**。① 唯一的常量定义就是 `公共契约/版本规则/契约版本.py:15`；② 两个相关生成器（能力定义编译器、依赖锁定）**都从事实源 import**，且源码里留有「不写字面量」的注释（`依赖锁定.py:25`）；③ 136 份派生物 `参数契约.json` 的 `契约版本` **全部等于 `2.0.0`**，无第二个值。④ `开发工具/HTML验证/常量.py:7` 的 `场景契约版本 = "验证场景/v1"` 是**另一根轴**（场景格式版本，`哲学总账.md:313` 已登记为独立判据），不属平台契约版本，合法。
**唯一可提的一点（P2 级，不是分叉）**：`开发文档/规范/网关调用最小示例.md:116` 的示例返回体里硬写 `"当前版本": "2.0.0"`——该文件是**文档示例**，不是事实源，但它**没有标注「示例值」**，读者可能当成当前值。建议改为 `"当前版本": "<见 公共契约/版本规则/契约版本.py>"` 或加一行「示例值，随契约版本变化」。

**另：`验证场景引用.json` 的 `契约版本` 有两个值（135 份 `验证场景/v1` + 1 份 `2.0.0`）**

```bash
timeout 60 rg -o '"契约版本": "[^"]*"' --glob '**/验证场景引用.json' | sed 's/.*: //' | sort | uniq -c
# → 1 "1.0.0"   1 "2.0.0"   135 "验证场景/v1"
```

`平台控制面/能力反馈/验证场景引用.json:2` 是 `验证场景/v1`（正确），`:18` 与 `:120` 的两个值是该场景**请求参数里的 `契约版本` 字段**（`:18` = `"1.0.0"`、`:120` = `"2.0.0"`）—— 那是**能力反馈请求的入参**，不是文件自身的契约版本。**合法**，不构成第二事实源。（`:18` 的 `1.0.0` 是同一个场景里两个步骤的入参值不同，属作者手写的场景数据。）

---

### 【本类未发现】决策记录重号 / 跳号复用

**审计动作**：全量核对 47 份编号文件 + 模板。

```bash
timeout 30 ls 开发文档/决策记录/ | sort
# → 0001–0009（缺 0010）、0011–0048、模板.md   共 47 份 + 模板

timeout 60 git log --oneline --all --diff-filter=A -- '开发文档/决策记录/0010*'
# → （空 = 全历史从未存在过）

timeout 60 rg -n "\b0010\b" -g '*.md' --glob '!工程缓存/**' --glob '!开发文档/参考资料/**'
# → 开发文档/规范/Markdown 文档体例规范.md:160  「实测缺口：决策记录 0010 全历史不存在…按本条属**合法跳号**，不需要补」
#   开发文档/分析/深度审计_底座升级清单_20260919.md:83  「0010 为合法跳号…；0022 撞号事故…已在提交 12954720 收口…无需处理」
#   开发文档/归档/…（归档，合法）

timeout 60 rg -n "编号说明" 开发文档/决策记录/*.md
# → 0021_依赖分两段…:3  「本记录原写 0009，与既有 0009_技能库落地.md 同号；按 12.4④ 由后到者顺延为 0021」
#   0023_连接器超时回落.md:3  「本记录原写 0015，与既有 0015_限流器边界如实收口.md 同号；…顺延为 0023」
#   0048_裁决窗口…:2  「本记录原写 0022（0022_裁决窗口复用.md）…编号…顺延为 0048，原 0022 文件按「编号不复用」删除」
```

**结论**：**本类未发现重号**。① 目录内编号 `0001`–`0048`（缺 `0010`）**无重复**；② `0010` 是**合法跳号**（`git log --all --diff-filter=A` 全历史确无此文件，体例规范第七节明文允许「缺号即从未存在过」）；③ 历史上的三组同号（`0008`/`0009`/`0015`）与 0022 撞号事故**均已按 12.4④ 顺延并对留痕**（`0021`/`0023`/`0048` 头部都有「编号说明」行），原重复文件已删除。⇒ **这一条做得干净，值得作为「已收口的样例」记录。**

**唯一遗留（属 P2-1 项）**：`0013` 的**状态值**未随其描述被取代而更新（见 P2-1）。此外 `0021` 的状态「**声明层方案未落地**」与 `0048`「**已实施并真实验收通过**」**都不在模板允许的五个取值内**（`决策记录/模板.md:5`：「状态取值：草稿 / 评审中 / 现行 / 已变更 / 已废弃」）—— 属状态值越界，非编号问题。

---

## 三、「不是缺陷（设计取舍）」清单

以下 8 项经现场核对**属合法的过程件 / 备查件 / 归档件或设计取舍**，不计入分叉缺陷。**唯一要求：核对它们有没有被正文当事实源引用**（已在每条注明核对结果）。

| # | 对象 | 为什么不是缺陷 | 是否被正文当事实源引用 |
|---|---|---|---|
| 1 | `开发文档/分析/` 全部过程件（审计开始时 28 份已跟踪件 + 同批 5 份未跟踪审计报告） | 4.2 与 `方案文档生命周期规范.md:27` 明文：「方案文档是过程件、不是事实源」。现场实测 **27/28 份已跟踪件 ref=0**（未被任何正文引用） | **1 份越界**：`AGENTS.md:162` 引 `深度审计_底座升级清单_20260919.md`（列 P0-5）。另 2 处引用合规：`未完成事项.md:71`（立项依据）、`项目说明.md:205`（拆分候选清单） |
| 2 | `开发文档/归档/` 4 个主题目录 | `AGENTS.md:282`：归档按「年月_主题」组织，合法留档；归档件不作为派活依据 | **未发现**被权威正文链接（`主开发文档.md:81` 只列目录名，未链具体文件） |
| 3 | `开发文档/交付记录/` 9 个历史目录（2026-09-11 至 09-16） | D 栏定义为「历史留档、只读不改」 | **未发现**被权威正文当事实源引用（各目录最后提交均在 `aeea721a` 2026-09-17 之前，符合「不再新增」） |
| 4 | `开发文档/参考资料/外部源码研究/` | AGENTS:284 明文「只作选型参考，不作为实现依据」 | **未发现**被权威正文当作实现依据 |
| 5 | 决策记录 `0010` 缺号 | 体例规范第七节明文「允许跳号，缺号即从未存在过」；`git log --all --diff-filter=A` 全历史确认不存在 | 不适用（缺号本身） |
| 6 | 历史三组重号 `0008`/`0009`/`0015` + `0022` 撞号 | 均已按 12.4④ 顺延为 `0021`/`0023`/`0048` 并留「编号说明」行，原重复文件已删除 | 已收口（`0048` 自述「内容已完整并入本文，含现象/根因/验收」） |
| 7 | 136 份 `能力契约/参数契约.json` 里的 `"契约版本": "2.0.0"` | 生成物，生成器从 `公共契约/版本规则/契约版本.py` 注入（`能力定义编译器.py:266/323`），实测值与事实源全等 | 不适用（派生物，非手写事实源） |
| 8 | 135 份 `验证场景引用.json` 里的 `"契约版本": "验证场景/v1"` | **由能力作者手写**，但其真源是 `开发工具/HTML验证/常量.py:7::场景契约版本`，`新建支持库与模块指南.md:262` 明文规定 | 合规；`说明书权威分工与维护规约.md` 已登记「编译器明确不生成（会覆盖合法文件）」的口径 |

**补充说明（设计取舍，非缺陷）**：`开发文档/方案/` 39 份里 **19 份已列文档地图、20 份未列**——经逐份核对，未列的 20 份中 `任务类型规范强制注入方案`/`模型探针与动态路由方案` 已标「已完结」、`发布门禁拆分方案`/`说明书存量治理施工表`/`测试中心归口方案` 标「生效」、`智能路由与模型打分方案`/`开工句柄文件乐观锁方案`/`文件改动流水与登记不冻结方案`/`稳定槽切换执行方案` 标「起草」、`底座升级方案`/`底座大版本升级方案`/`底座扩容总纲` 标「已取代」。**「未列进地图」不等于「不是方案」**——地图 `:42` 的 C 栏定义是「备查（要用时查）」，不是穷举清单。但其中 **4 份缺状态声明**（`拆上帝文件执行方案`、`端到端背压修复方案`、`检索能力升级方案`、`稳定槽切换执行方案` 的状态行不在五态内/缺失）已由 `分析/审计报告_全仓现状_20260918.md:211` 登记，属方案自身合规问题（**撞「方案文档头部必须声明状态」**），建议单列一轮处理，本轮不重复计。

---

## 四、建议归口表

> 覆盖「二、逐条明细」全部 18 条：16 条需动作（P0×5 + P1×6 + P2×5）+ 2 条「本类未发现」（无需动作，列出行仅为闭合审计范围）。

| 编号 | 问题一句话 | 建议动作 | 归口到哪份权威文档 | 优先级 |
|---|---|---|---|---|
| P0-1 | 「第 1–13 条」与「第 1–14 条」四文件两派、`项目说明.md` 内部矛盾 | 四处同批改「1–14 条」 | `AGENTS.md:277`、`主开发文档.md:18`、`哲学总账.md:8`、`项目说明.md:13` | **P0** |
| P0-2 | 子项 58/55、上限 60/56 三方不一致；总账漏登记 4.5/13.3/14.3/14.4 | 统一上限为 60；总账补登 4 行；条数只在一处声明 | `哲学总账.md:1/111/112`、`主开发文档.md:18` | **P0** |
| P0-3 | 已删的「49 条款四态表」仍在 `目标定义.md` 当现状用 | 删绝对值，改引 `哲学总账.md` 重测绘口径 | `目标定义_什么是OK.md:37/67` | **P0** |
| P0-4 | `二次审计落地编排` 仍自称「唯一编排入口」 | 状态改「已完结 · 过程件」；删「唯一编排入口」措辞 | `方案/二次审计落地编排_20260918.md:6`、`主开发文档.md:71` | **P0** |
| P0-5 | `AGENTS.md` 把一次性审计过程件写成硬规则「方案全文见」 | 改为指向 `规范/`；删对 `分析/` 的指向 | `AGENTS.md:162` | **P0** |
| P1-1 | 包数/能力数/说明书分母过期；「人工类 25 vs 26」无口径说明 | 按 README 口径只留取证命令不写死数字；补口径说明 | `项目说明.md:43/49`、`目标定义_什么是OK.md:69` | **P1** |
| P1-2 | 生成物 `能力目录说明书.md` 写「138 份包声明」；7.9① 输入指纹未落地 | 重跑生成器；补输入指纹头（门禁线） | `规范/能力目录说明书.md:1/5` | **P1** |
| P1-3 | 包级设计说明说「正式根当前 5 根、本包不参与装配」 | 整段重写；行号引用改「见 `正式根.py::正式根名表`」 | `启动监督器/健康监督/说明/设计说明.md:56-59` | **P1** |
| P1-4 | `场景覆盖率口径.md` 自称权威处但不在任何规范清单；分母 681 过期 | 补登规范清单；分母改「跑命令取真值」；措辞降级 | `AGENTS.md:278`、`主开发文档.md` B 栏、`规范/场景覆盖率口径.md:5/106` | **P1** |
| P1-5 | `未完成事项.md` 三处 §8.7 重号 + §8.6 顺序倒置 | §8.7 只留二次审计那份；另两份按 12.4⑤ 删 | `未完成事项.md:253`、`:314` | **P1** |
| P1-6 | 条目号 #23/#7/#1 各重号两次，外部文件已按号引用 | §8.2.1 已裁完段落整段删；裸「第 N 项」补节号 | `未完成事项.md:60-63`、正文引用处 | **P1** |
| P2-1 | 决策 `0013` 未标「已变更」 | 状态改「已变更 → 见 `0030`」；`0021`/`0048` 状态值归五态 | `决策记录/0013:3`、`0021:4`、`0048:6` | **P2** |
| P2-2 | `临时文档/01_…md` 未按 4.2 / 决策 `0004` 清空 | 删除（结论已在 `未完成事项.md:16`）；有未覆盖内容先并入 §8.5 | 删除 `开发文档/临时文档/01_*.md` | **P2** |
| P2-3 | `交付记录/20260918_…简报` 在「不再新增」生效后新增且带过期数 | 移入 `归档/202609_夜间推进简报/`；或修 D 栏措辞 | `开发文档/交付记录/20260918_*.md`、`主开发文档.md:79` | **P2** |
| P2-4 | 文档地图漏列 `项目证据/`（机器账本） | D 栏补一行；说明 `分析/`、`临时文档/` 不在权威面 | `主开发文档.md:76-82` | **P2** |
| P2-5 | 四份权威文档「最后更新」写 09-17、内容含 09-18 | 元信息头同批改 2026-09-18 | `项目说明.md:3`、`哲学总账.md:3`、`当前流程图.md:3`、`主开发文档.md:3` | **P2** |
| — | 契约版本第二处硬写 | **本类未发现**；`网关调用最小示例.md:116` 建议加「示例值」标注 | `规范/网关调用最小示例.md:116`（可选） | — |
| — | 决策记录重号/跳号复用 | **本类未发现**（`0010` 合法跳号；历史重号已收口留痕） | 无需动作 | — |

---

## 五、审计局限

1. **只读边界**：本轮**未修改任何仓库文件**（唯一写入 = 本报告）；未跑测试、未跑门禁、未重启服务、未 `git add/commit`。**只读自证**：`git status --porcelain` 在审计过程中与收尾时**都只有 5 项 `??` 未跟踪文件**（本轮 5 路并行审计各自的报告，全部为新增），**无任何已跟踪文件出现在 `M`/`D`/`A` 列** ⇒ 未触碰既有文件。收尾命令：

```bash
timeout 60 git status --porcelain
# → ?? 开发文档/分析/审计_仓库洁净度_20260919.md
#    ?? 开发文档/分析/审计_依赖分层归属_20260919.md
#    ?? 开发文档/分析/审计_平台判断越界_20260919.md
#    ?? 开发文档/分析/审计_文档事实源分叉_20260919.md   ← 本报告
#    ?? 开发文档/分析/审计_能力唯一性降重_20260919.md
```
2. **数字口径**：报告中所有「现场实测值」均在 HEAD `836e9066` 同刻取得，且附可复跑命令。按 `项目说明.md:43` 的口径纪律，这些数字**随并发建包逐分钟变化**——引用本报告数字前须重跑命令。
   - 本轮实测：五根 `包声明.json` **141**、五根 `能力定义.json` **135**；全仓（排除 `工程缓存/.git/__pycache__/归档/参考资料`）`包声明.json` **142** / `能力定义.json` **136** / `使用说明.md` **143**；`md` 全仓 **701** 份（含 `开发文档/参考资料/**`）。
3. **未覆盖的审计面**：
   - **未逐份读 39 份方案的正文**——只核了状态声明行与批次表存在性。「同数不同值」类只抽查了能力数/包数/说明书分母三类高频指标，未做全量 39×N 两两比对。
   - **未核对 `开发文档/基础契约/`（无此目录）以外的代码侧契约**——第 5.1 条只说「契约版本」，未扩展到「能力契约指纹算法是否只有一套」（5.3 的第二半），那需要跑代码比对，超出只读文档审计范围。
   - **未评价 `开发文档/规范/` 下 6 个 `.json` 判据文件的内容一致性**（`Markdown 文档体例判据.json`、`任务类型规范.json`、`踩坑条目.json`、`哲学…`），只核了它们在文档地图/AGENTS 清单里的登记情况。
   - **过程件的「存废」未做价值判断**——`分析/` 28 份是留是删属华哥裁决范围，本报告只判「有没有越界成为事实源」。
   - **未核 `开发文档/项目证据/` 内 JSON/JSONL 的数据正确性**（只核了它们被谁引用、是否在地图上）。
4. **判据自身的局限**：本轮的「自称唯一」扫描采用 `方案文档生命周期规范.md:108-120` 的**只扫 H1 标题**口径（该规范记录了首版判据 `grep -l '唯一入口'` 报 22 份假阳性的教训）。H1 级命中只有 `执行编排_唯一入口_20260918.md`（合规）与 `规范/资源回收确认机制.md`（`# 资源回收确认机制（唯一权威 · 2026-08-26 华哥口径）`——该文件是资源生命周期归属的规范，其「唯一权威」是**在「句柄体系内置职责」这一主题内**的限定表述，且 `主开发文档.md:36` 已把它标为「资源生命周期归属（句柄体系内置职责，唯一权威）」，**不构成与 7 份权威并列**，故未列为缺陷）。但 **P0-4 的「唯一编排入口」不是 H1 而是状态行**——说明该判据**只扫 H1 会漏掉状态行里的自称**，这是判据本身的覆盖缺口（撞 12.5「判据自己也要有不应被触发的样本」），建议下一轮把判据扩到「状态行 + H1」。
5. **同一时刻的并行写入**：审计期间 `开发文档/分析/` 另有 4 份同批审计件落盘（`审计_仓库洁净度_20260919.md`、`审计_依赖分层归属_20260919.md`、`审计_平台判断越界_20260919.md`、`审计_能力唯一性降重_20260919.md`，mtime 03:41–03:46），**全部为未跟踪（`??`）、未被 git 提交、也未被任何正文引用**（现场 `rg` 零命中）。本报告只把 `深度审计_底座升级清单_20260919.md` 纳入越界核对（因 `AGENTS.md:162` 引它，且它**已被提交**），上述 4 份新件未展开——它们与本报告同属「本轮过程件」，按 4.2 应在结论并入权威后一并删除。
