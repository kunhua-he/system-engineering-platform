# 批P·P-6 + P-7 回传（维修子代理 Q-c）

> 状态：**已完成**（6/6 条收敛 + P-6 复证）｜生成：2026-09-24 09:50
> 开工ID：`开工-20260924-094428-1577`（主，7 条租约）+ `开工-20260924-094708-9690`（追加，1 条租约）
> 上游：`开发文档/分析/批P清单_20260924.md` 的 **P-6 / P-7** 两段；裁决 **裁-5**（P-6 收敛计算收成唯一节点，取兄弟正确形状）
> 华哥口径：①「全部切成统一节点」②「哪里有报错就按这个方式，有权威节点就统一到它」
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

---

## 一、P-6 唯一节点（Q-a 落盘，本件**复证**）

**节点**：`开发工具/存量基线消费.py::分桶与收敛(条目列表, 上限表, 分桶函数)`

为什么单独一件、不落 `开发工具/验证门禁/门禁公共.py`：后者模块级自举要求「祖先目录里同时有 `模块库` 与 `测试中心`」，一旦被 `开发工具/公开调用完整性门禁` 引入，**制品内的该门禁就会脱离源码树即崩**（实测 `StopIteration`）。本件刻意**零自举、零仓内依赖**（只用标准库），源码树与制品内行为一致。

**两条腿的调用点（各自只提供桶键构造，判定口径不再自持第二份）**：

| 腿 | 调用点 | 桶键 |
|---|---|---|
| `开发工具/公开调用完整性门禁_检查与判定.py` | `:41` import；`:247` `return 分桶与收敛(违规, 基线, lambda 条: 分桶(条, 根))` | `包相对路径\|缺口类型` |
| `开发工具/契约编译/漂移检测.py` | `:184` import；`:245` `return 分桶与收敛(条目列表, 上限表, lambda 条: f"{条['包']}\|{条['缺口类型']}")` | `包\|缺口类型` |

### P-6 双证读数（本件现场复跑，非转述）

**单元复现**（基线 `{A\|X:3, B\|Y:2}`、现场 `[]`）—— 旧版取 `git show HEAD:开发工具/公开调用完整性门禁_检查与判定.py` 落 `/tmp` 后与新工作树并排跑同一输入：

| | 收敛桶数 | 明细 |
|---|---|---|
| 修前（HEAD） | **0**（错，应为 2） | `[]` |
| 修后（工作树） | **2** ✔ | `[{桶: A\|X, 当前: 0, 基线: 3}, {桶: B\|Y, 当前: 0, 基线: 2}]` |
| 唯一节点直调 | **2** ✔ | 同上 |

**真仓读数**：

| | 现场原始违规 | 基线 | 收敛桶数 |
|---|---|---|---|
| 修前（HEAD） | 0 条 | 7 桶 / 20 条 | **0**（错，应为 7） |
| 修后（工作树） | 0 条 | 7 桶 / 20 条 | **7** ✔（逐桶 `当前 0 / 基线 N`） |

复跑命令（只读）：`python3.14 /tmp/p6_验证.py`（脚本在 /tmp，不落仓）。

---

## 二、P-7 六条逐条收敛

★ 铁律执行情况：**3 条走门禁自己的出口重建，3 条无出口（如实写明）**。

| # | 载体 | 改前 | 改后 | **用的是哪个出口** |
|---|---|---|---|---|
| 1 | `开发工具/发布门禁/运行发布门禁_防回潮公共.py:275` 裸布尔标量 | 374 | **335** | **无出口** —— 见下 §三 ① |
| 2 | `开发文档/项目证据/能力id冻结基线.json:6` `能力数` | 751 | **752** | `python3.14 开发工具/能力id冻结基线门禁.py --冻结`（经 MCP `执行命令` 异步腿，任务 `4a16b2a130254031`） |
| 3 | `开发工具/参数口径基线.json:178` `能力数` | 675 | **690** | `python3.14 -m 开发工具.参数口径审计 --写基线`（任务 `b376a09cdf9d4a0c`） |
| 4 | `开发文档/项目证据/第三方导入分布基线.json:6` `文件数` | 1212 | **1282** | `python3.14 -m 开发工具.第三方导入分布基线门禁 --冻结`（任务 `ebba9c2ff7724188`） |
| 5 | `开发工具/公开调用完整性门禁存量基线.json:6` `条目` | 7 桶 20 条 | **`{}`** | **无出口** —— 见下 §三 ② |
| 6 | `开发工具/契约编译/参数可用性体检.py:261` 路径口径 `存量基线` | 103 | **0** | **无出口** —— 见下 §三 ③ |

### 与批P 表格的**唯一**偏差（如实上报，非盲抄）

批P 表里第 4 条写「应收敛到 **1281**」，出口重建实测 **1282**。差 **+1** = `开发工具/存量基线消费.py`（P-6 的唯一节点，Q-a 2026-09-24 09:27 落盘，**在批P 审计取样之后**；`开发工具` 在 `正式根名表` 内 ⇒ 计入扫描面）。**取出口读数 1282**（出口是现场权威，不按旧快照回退）。

### R2 出口重建的**附带吸收**（必须留痕，不许当没发生）

`--冻结` 把 `开发文档/项目证据/能力id冻结基线.json` 按现场整体重写：除 `能力数 751→752` 与补入 `自修复工具.仓库锁`（指纹 `324c57660a18bde2`）外，还**重写了 199 条已存 id 的 `内容指纹`**（`106 insertions / 101 deletions`）。

为什么这是出口的**规定行为**、不是洗白：该基线自己的口径写明「**契约变更是合法行为，但必须留痕** —— 已存在 id 的**内容指纹**变化 / 归属包变化属 `留痕`（允许但报到），只有 **id 消失**才判红」（`开发工具/能力id冻结基线门禁.py:20`、`:82`、`:239-240`）。指纹来源是 owner 包 `能力定义.json`（`:112` `计算内容指纹`），而 2026-09-23 之后批C（路径口径 40 包 `说明` 重写）与批P·P-3（编译器批量回填 64 处）**本来就在改这些文件** ⇒ 199 条指纹变化与并行改动面吻合。

**复核**：重建后非冻结模式复跑（经 MCP 异步腿，任务 `82608486d3844e6e`）⇒ `退出码 0`、`当前集 ⊇ 基线集，无 id 消失（只增不改成立）`、留痕 0、新增 0 —— 即**没有 id 被删/改名**，「只增不改」成立。

### 逐条复跑判据（复核脚本 `/tmp/p7_复核.py`，只读）

```
R1 标量 = 335 | 分桶条目数 = 335 | 分桶文件数 = 77 | 两处相等 = True
R2 能力数 = 752 | 条目数 = 752 | 仓库锁 = 324c57660a18bde2 | 消失 = [] | 新增 = ['自修复工具.仓库锁']
R3 能力数 = 690
R4 文件数 = 1282
R5 条目 = {} | 真仓 新增 0 / 存量 0 / 收敛 0
R6 路径口径存量基线 = 0（现场冲突 0 条，实测）
```

R1 判据**自己的输出**（经 MCP 异步腿，任务 `d9fb0931a6e04286`）：

```
扫描文件 1003；本次计数 341 条｜存量基线 335 条（态=债务冻结；是否阻断=真）
｜按文件分桶判定；**新增 6 条**：删除树.py:143、文件租约维护.py:140/:141、查重.py:87 …
```

---

## 三、三条「无出口」的实情（为什么不走出口）

① **R1 裸布尔标量**：`运行发布门禁.py` 的 CLI 只有 `--制品/--包目录/--跳过测试/--禁止真实进程/--测试并行数/--测试真跑`，**没有任何写基线入口**；标量是 `运行发布门禁_防回潮公共.py` 里的**模块级常量**，无生成器。它对应的分桶基线 `防回潮存量基线.json` 已由批O·O-10（`开工-20260924-065922-a059`）走扫描逻辑重建为 335，**本标量只欠一次同步**（两处注释自认「是同一个量，必须相等」）。⇒ 只能 MCP 文本补丁同步。

② **R5 `公开调用完整性门禁存量基线.json`**：`公开调用完整性门禁.py` 只接**位置参数 根**（`:666`），全簇落盘腿只有 `读取存量基线` / `默认存量基线路径`（**零写者**）；文件自己的 `口径` 字段亦自认「生成脚本 = 本次交付的 **/tmp 脚本** + 本文件条目」⇒ **该基线从来就没有入库出口**。⇒ 只能 MCP 文本补丁同步。

③ **R6 `路径口径存量基线`**：`参数可用性体检.py` 的 `主函数()`（`:327`）**不接 argv**，只打印三类计数与退出码；常量是模块级赋值。⇒ 只能 MCP 文本补丁同步。

> 三条「无出口」不是本次偷懒，是**机制缺口**：这三处存量基线都只能靠人改数字，即「回填没有出口腿」。建议后续按哲学 13.3 四环链给它们各补一个同名重建入口（或收口到 `开发工具/存量基线消费` 同一套），**本件不越权新造**。

---

## 四、改动清单（文件 + 行号 + 改前/改后）

| 文件 | 行号 | 改前 → 改后 |
|---|---|---|
| `开发工具/发布门禁/运行发布门禁_防回潮公共.py` | `:261-263` | 注释「现场实测 **374** 条 / 375→374 同步下调」→「现场实测 **335** 条 / 并记 2026-09-24 批O·O-10 重建：条目数 374→335、文件数 91→77」 |
| 同上 | `:275` | `"裸布尔": {"态": "债务冻结", "存量基线": 374, "阻断": 真},` → `... 335 ...` |
| `开发文档/项目证据/能力id冻结基线.json` | `:6` | `"能力数": 751` → `752`（出口重建；条目 751 → 752） |
| `开发工具/参数口径基线.json` | `:178` | `"能力数": 675` → `690`（出口重建） |
| `开发文档/项目证据/第三方导入分布基线.json` | `:6` | `"文件数": 1212` → `1282`（出口重建） |
| `开发工具/公开调用完整性门禁存量基线.json` | `:4-6` | `基线时点 2026-09-17` / 旧 `口径` / `条目{7 桶 20 条}` → `2026-09-24（本次清空）` / 口径补「P-7 清空 + 无出口」/ `"条目": {}` |
| `开发工具/契约编译/参数可用性体检.py` | `:253-261` | 注释「命中 **103** 条 / **40** 个包」→「命中 **0** 条（原 103 已全部收敛）+ 无出口说明」；`路径口径存量基线 = 103` → `= 0` |

### 连带改动（P-7 第 5 条的必需收口，**不许留红**）

| 文件 | 行号 | 改前 → 改后 |
|---|---|---|
| `测试中心/开发工具/测试_公开调用完整性门禁.py` | `:561-579` | `assertGreater(len(存量), 0, "真仓库应有存量（E-2/E-3 上线时点的分治基线）")` → 改为锁「**双空 + 条目真空**」：`新增 == []`、`存量 == []`、`收敛 == []`、`json…get("条目") == {}` |

**为什么必须改**：原断言锁的是「E-2/E-3 **上线时点**有存量」这个**历史时点**，不是判据口径。基线合法清零后 `存量` 必然为 0 ⇒ 该断言会把「已收敛」**误判成回归**。改后的「条目真空」一条才是对「基线被偷偷回填」有分辨力的断言（空基线下 `存量/新增` 恒空，锁不住回填）。

---

## 五、没做的部分与原因

1. **裸布尔判据现场仍是红的**：R1 只把标量校正到**真实存量 335**，**不洗白**那 6 条基线外新增（`删除树.py` 1、`文件租约维护.py` 2、`查重.py` 1、`制品新鲜度门禁.py` 2，均 2026-09-23/24 新提交带入）—— 批O 已明文「刻意不吸收，吸收即等于洗白」。修这 6 条不在 P-6/P-7 范围。
2. **未编译、未跑测试、未提交 git**（按派活纪律）。测试改动只做**逻辑等价复跑**（`/tmp/p7_复核.py` 实跑 `运行门禁并分诊(仓库根)` + 读基线「条目」，四条断言全真）。
3. **`运行发布门禁.py` epilog 过期文案未改**（`:778` 写「裸布尔=债务冻结（存量 **1605** 条、新增即红）」）：该文件**不在我的写集**（未认领）。
4. **`开发文档/未完成事项.md:268` 条目未销账**：其「① 存量 103 条收敛」已完成、②「收敛后必须下调基线」本件已做；该文件**已被并行会话占用**（工作树脏），不越域改。
5. **`存量基线消费.py` 无专属测试**：P-6 的判据目前只有间接覆盖（两腿各自的测试）。本件未新造测试（不在写集）。
6. **同块内另一处过期快照未改**：`运行发布门禁_防回潮公共.py:265` 写「扫描面 = 正式源码目录名表 12 层 共 **718** 份 .py」，而判据现场报 **1003** 份（该行带 2026-09-18 日期，属历史快照陈述，本次未动）。

---

## 六、租约回执

- **开工ID**：`开工-20260924-094428-1577`（7 条：R1-R6 六个载体 + 本 md）、`开工-20260924-094708-9690`（1 条：测试文件）
- **释放数**：8 条（`收工释放` 按 `租约id清单` 传值，不是 `开工ID`）
- 收工按**精确路径**提交，禁 `git add -A`（提交纪律见 `开发文档/规范/多会话并行开发规约.md` §九）。
