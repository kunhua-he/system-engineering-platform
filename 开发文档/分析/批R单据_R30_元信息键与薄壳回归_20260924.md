<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R·R-30 单据：`元信息键` 扩表 ＋ `_裁剪值` 回归测试 ＋ `测试_薄壳返回可控` 归因

- 时间：2026-09-24
- 开工ID：`开工-20260924-154603-26d8`（代码腿）／续租 `开工-20260924-160356-9ef3`（落盘腿）
- 事实源：`开发文档/未完成事项.md`「批R·R-27 登记」＋ `开发文档/分析/批R单据_R27_终端根因与越界收口_20260924.md`
- 写集：`开发工具/薄壳/薄壳服务.py`、`测试中心/开发工具/测试_薄壳返回可控.py`、本单据、`开发文档/未完成事项.md`

## 一、`元信息键` 扩表（R-27 债务 #2）

### 1.1 判据与唯一节点

判据（R-27 立，本批沿用**同一判据**）：**丢了它，正文仍能推出同一事实** —— 键值要么是调用方自己传的入参回显，要么是实现里的常量回显。
唯一节点：`开发工具/薄壳/薄壳服务.py::元信息键`（frozenset）。**各包不得自建同名表**（同一件事两套实现即缺陷）；逐键依据写在节点旁，不另存一份。

### 1.2 扩表前后读数

| 项 | 改前 | 改后 |
|---|---|---|
| 键数 | **3** | **19** |
| 键名 | `命令` `引擎` `搜索根` | 上列 3 键 ＋ `项目根` `关键词` `游标` `限制` `原子` `写入` `动作` `允许扩展名列表` `阈值秒` `小数位` `方式` `百分位` `心跳超时秒` `阈值档位` `并行上限` `总调用数` |

代码改动面（`git diff` 去掉纯注释行后）：**只有 1 处 hunk** —— `元信息键 = frozenset({...})` 由 3 键扩到 19 键；`+30/-4` 行里其余全是注释。

### 1.3 扫描口径（扩表的取证范围）

扫全部 `能力定义.json` 的 `返回.值结构` 顶层键：**142** 个定义文件 ／ **585** 个带值结构的能力 ／ **933** 个去重顶层键。

### 1.4 逐键依据（每键一句：为什么丢了它正文仍能推出同一事实）

| 键 | 命中能力数 | 依据 |
|---|---|---|
| `命令` | 11 | 本次 argv 的回显（`正则搜索` 的 rg argv／编译口命令行／生成器命令行／`启动进程` 的「命令＋参数」）—— 全由入参推出 |
| `引擎` | 1 | 实现常量回显（恒为 `rg`） |
| `搜索根` | 1 | 入参 `根目录` 的复述（该能力根目录本身为 `.`） |
| `项目根` | 10 | 入参 `项目根` 的复述（`自修复工具.*` 5 个腿 ＋ `项目文档.*` 4 个腿） |
| `关键词` | 4 | 入参原值（`能力目录.搜索能力`／`项目文档.查文档`／`查记忆`） |
| `游标` `限制` | 各 1 | `能力目录.搜索能力` 的入参原值（`限制` 越界按契约收敛，规则写死 ⇒ 可推） |
| `原子` `写入` | 各 1 | `文本补丁.批量应用精确替换` 的入参回显（契约原文即「回显入参…」） |
| `动作` | 2 | `自修复工具.仓库锁`／`提供者.水位采样定时器` 的入参回显 |
| `允许扩展名列表` | 1 | `文件选择描述.创建文件选择描述` 的入参副本（契约原文即「入参副本」） |
| `阈值秒` `小数位` `方式` `百分位` `心跳超时秒` `阈值档位` `并行上限` `总调用数` | 各 1 | 「本次使用的 X」一族：回显调用方传的阈值／精度／档位／上限（不传取契约默认 ⇒ 可推） |

现场读数（本批收尾时的 **MCP 真调用**）：一次 `正则搜索` 超预算回包里 `丢弃说明＝已省略元信息键：引擎、搜索根、命令` —— 扩表后的节点在真链路上生效。

### 1.5 边界（**不许**纳的键）—— 反向读数

933 个顶层键里，承载事实的高频键**一条未纳**：`值`(112) `状态`(58) `说明`(51) `句柄`(38) `数量`(27) `原因`(25) `成功`(24) `消息`(19) `名称`(17) `退出码`(15) `版本`(14) `通过`(13) `会话id`(13) `格式`(13) …… 任务点名的 `匹配列表`／`标准输出`／`退出码` 均未纳。

### 1.6 已知例外（如实登记，不静默）

键名集是**全局**的，与「按能力逐条判据」有结构性冲突。两处例外只在**超预算**时才会被丢：

1. `浏览器自动化.检查可用` 的 `命令` 是「本机发现的 browser-use 启动命令」＝**事实**，非回显；
2. `能力目录.搜索能力` 的 `项目根` 是「本次检索所读那棵树的根」＝**事实**（契约原文已点明「不是调用方的 `项目根`」），其来源由同级 `项目根来源` 兜底。

根治方向：键集由**能力契约**供（唯一节点仍在薄壳）。已登记为 R-30 新债务（见 `开发文档/未完成事项.md`「批R·R-30 登记」）。

## 二、`_裁剪值` 回归测试（R-27 债务 #3）

- 用例名：`测试中心.开发工具.测试_薄壳返回可控.裁剪测试.test_超预算先丢元信息留正文`
- 判据：造「正文大、元信息长」的字典，超预算裁剪后 **正文键在、元信息键不在**。两条腿各钉一次 —— 腿①主路径靠 `_省元信息`；腿②降级路径（落盘不可用）靠 `_正文优先` 的键序。
- 空断言护栏：用例内先断言「元信息键表不得为空」。

### 2.1 读数（前后对照）

| 阶段 | 用例数 | F | E |
|---|---|---|---|
| 加之前（**HEAD 版测试文件** × 当前源码树） | 89 | 3 | 6 |
| 加之后（工作树） | **90** | **2** | **0** |
| 反向试验：把 `_正文优先` 摘成 no-op（只跑该用例） | 1 | **1** | 0 |

### 2.2 反向试验原文（证明它不是空断言）

```
FAIL: test_超预算先丢元信息留正文 (...裁剪测试.test_超预算先丢元信息留正文)
AssertionError: '匹配列表' not found in {'允许扩展名列表': '长…', '关键词': '长…', '写入': '长…',
 '动作': '长…', '原子': '长…', '命令': '长…', '小数位': '长…', '并行上限': '长…',
 '引擎': '长…', '心跳超时秒': '长…'} : 降级路径同样必须先留正文（`_正文优先` 的键序）
【基线】用例数 1 | F 0 | E 0      【反向：_正文优先 摘成 no-op】用例数 1 | F 1 | E 0
```

反向时降级路径先留的是元信息键、正文 `匹配列表` 被整段丢掉 —— 正是 R-27 修掉的那个症状，说明本用例钉住的是真实行为。

### 2.3 既有断言

**0 条既有断言被删成空**。3 处停在旧口径的断言/桩改成了现行口径并**加**了钉点（净增断言），逐条见 §三 3.1。

## 三、`测试_薄壳返回可控` 的 2 个 error 归因

### 3.1 定性：**测试夹具过时**（断言过时 ＋ 桩签名过时），非薄壳缺陷、非环境时序

取证方法：把 **HEAD 版**测试文件（`git show HEAD:…`）在**当前源码树**上跑一遍（不往仓库写任何文件）⇒ **89 用例 / F3 / E6**，逐条报错如下：

| # | 用例 | 报错 | 根因（现口径） |
|---|---|---|---|
| 1 | `裁剪测试.test_字段投影优先于裁剪` | `TypeError: 'NoneType' object is not subscriptable` | 旧断言取 `说明["保留字段"]`；R-21 裁决已删 `保留字段`（入参键名回声），R-27 起「投影成功即回 `None`」 |
| 2 | `裁剪测试.test_字段环事实挂在指针旁不丢` | 同上 | 同上 |
| 3 | `全量回执库测试.test_回执落的是未投影原值` | `KeyError: '保留字段'` | 同上 |
| 4 | `裁剪测试.test_落盘不可用时不静默丢内容` | `TypeError: <lambda>() takes 1 positional argument but 2 were given` | 桩 `lambda _值: None` 没跟上 `_落全量回执` 的**新签名**（R-27 加了 `耗时毫秒` 形参） |
| 5 | `深裁反向验证.test_降级路径下退回旧口径承诺判据变红` | 同上 | 同上 |
| 6 | `兜底深缩反向验证.test_降级路径下退回旧口径唯一大键压不下去` | 同上 | 同上 |
| 7 | `裁剪测试.test_指针回执比硬裁便宜得多` | `5486 not less than or equal to 1200` | R-27 起回包**自带缩影**（`值` 键）⇒「整包 ≤ 1200」不再成立；判据改量**指针区**（除 `值` 外的键）—— 那才是「省上下文」的手段本身 |

R-25 当时读到的 **2 E** 是本族的早期读数（当时只有 #1/#2 两条触发；R-27 之后 #3～#6 才被同一族根因带出来）。

### 3.2 `Received request before initialization was complete` 归因：**设计时序**，非缺陷

- 机制（`开发工具/薄壳/薄壳服务.py:1326-1334` 已写明）：换壳把 SDK 会话里那份「已握手」状态一起换掉，而握手只能由客户端发起、客户端不会重发 ⇒ 新壳停在 `NotInitialized`，此后每次调用都被 SDK 判 `RuntimeError: Received request before initialization was complete`，再被会话循环接住。薄壳不持有任何会话态（每次调用独立转发给网关）⇒「无握手也服务」语义正确。
- 该路径已有专门用例钉住：`自换新壳测试.test_换壳后不重新握手也能服务`（本批该组 11 例全绿）。
- 读数：本次整模块运行该告警出现 **1** 次（来自 `自换新壳测试` 的真 stdio 端到端腿），**不产生任何 E/F**。

### 3.3 改前 → 改后

| 阶段 | 读数 |
|---|---|
| R-25 实测（13:01，全仓动态编译轮） | `..........E...E..............` ＋ `Received request before initialization was complete` |
| 改前实测（本批：HEAD 版测试文件 × 当前源码树） | 89 用例 / F 3 / E 6 |
| 改后（工作树） | **90 用例 / F 2 / E 0** |

## 四、未归因残量：2 F（**已定性，登记不修**）

两条红都在 `注入面预算测试`，与 R-30 的三件事**无关**：

- 读数：`工具面 2790 > 预算 2000`，明细 `{'capability_search': 738, 'capability_call': 1903, 'tool_catalog': 149}`；`最坏 2834 = 静态 2624 + 2×105`。
- 静态组成：`capability_search` desc 139 ＋ schema 544；`capability_call` desc 1027 ＋ schema 781；`tool_catalog` desc 90 ＋ schema 67 ⇒ **静态 2624**。
- 与 R-30 无关的证据：① `开发工具/薄壳/工具清单.py` 本批**零改动**；② R-30 对 `薄壳服务.py` 的非注释改动**只有 `元信息键` 一行**（去注释后 1 处 hunk）；③ 静态部分是**账本无关**的 —— 三个工具定义各含 1 个 8 字符占位 `{{常用传参}}` 且已扣除，动态段实测只 +166（`capability_search` 63／`capability_call` 103／`tool_catalog` 0，上限 105）。
- 定性：**真缺陷** —— 注入面静态文案＋schema 超了华哥 2026-09-22「所有注入提示词最高 2000 字符」的裁决；落点 `开发工具/薄壳/工具清单.py`（R-27 项四刚写进去的两层语义口径表）。
- 处置：不在本批写集内（R-30 写集＝`薄壳服务.py`／测试文件／本单据／`未完成事项.md`）⇒ 按纪律「缩小写集或跳过并登记」**登记为新债务**；未改断言、未调预算（不许为绿而删断言）。

## 五、硬判据读数汇总

| # | 判据 | 读数 |
|---|---|---|
| 1 | `元信息键` 扩表前后键数变化 ＋ 每键一句依据 | **3 → 19**；19 键逐条依据见 §1.4（扫描口径 142 文件／585 能力／933 键），边界反向读数见 §1.5，2 条例外见 §1.6 |
| 2 | 回归用例：加之前 → 加之后全绿，且反向试验判红 | 加之前 **89 用例 / F3 / E6** → 加之后 **90 用例 / F2 / E0**（新增用例本身绿）；反向（`_正文优先` 摘成 no-op）⇒ 该用例 **F1 / E0** |
| 3 | `测试_薄壳返回可控` 改前 → 改后读数 | 改前（HEAD 版测试文件 × 当前源码）**89 用例 / F3 / E6**（R-25 当时读数 2 E，同族）→ 改后 **90 用例 / F2 / E0**；剩余 2 F 已定性（§四） |
| 4 | `自修复工具.静态编译`（异步腿） | 任务id `77927ec49fed4b6c`（`类型=1`、`模块ID=开发工具/薄壳`）：**成功 17 ／ 失败 3 ／ 检查项 20**；2 条阻断**均在并发会话在途面上**（① `设置本次调用基准` 在途导入失败＝R-32；② `OCR识别支持库` 内部层基线漂移＝R-28 基线在途），1 条只报告（制品新鲜度）；**本批改动面无一条红** —— 详见 §六 |

## 六、静态编译（异步腿）

- 任务提交：`自修复工具.静态编译`，`类型=1`、`模块ID=开发工具/薄壳` ⇒ 任务id `77927ec49fed4b6c`。
- 读数（完成于 16:10:40）：**成功 17 ／ 失败 3 ／ 检查项 20**，退出码 2、五态结论 `未核验`。逐条明细：
  - **阻断①「防回潮·反向破坏」**：`ImportError: cannot import name '设置本次调用基准' from 公共契约.运行时.平台适配` —— 该名**在途新增**（`公共契约/运行时/平台适配/路径解析.py:56`、`公共契约/运行时/平台适配/__init__.py:179`，源码注释自称「批R R-32」），且这两个文件在 `git status` 里处于**未提交在途态** ⇒ **并发会话在途改动**，非 R-30。
  - **阻断②「公开调用完整性」**：`[可收敛] 支持库/后端/OCR识别支持库/OCR识别|内部层-可绑定集仍含: 当前 0 < 基线 3（应下调基线）` —— 基线漂移；现场 `开发文档/项目证据/能力id冻结基线.json` 与门禁 `开发工具/公开调用完整性门禁_检查与判定.py` 均为未提交在途态（R-28「T1 能力面合并」同族）⇒ 非 R-30。
  - **只报告「制品新鲜度」**：制品落后 HEAD 1 个提交（`81cc618d` vs `aaa2f262`）—— 与 R-27 遗留项同族，须「重建制品＋重启网关」后复验。
- **本批改动面本身无红**：三条失败**无一指向** `开发工具/薄壳/薄壳服务.py` 或 `测试中心/开发工具/测试_薄壳返回可控.py`。
- 对照：据提交 `75545738` 的提交信息，R-27 收尾（13:44）读数为全仓 **20/0** 干净通过 ⇒ 本轮这 2 条阻断是 13:44 之后**他人**在途改动带出来的，不是本批。
- 结论：**本分片未达「阻断 0」**，但阻断项全部落在并发会话的在途面上；本批改动面自身零红。要拿到干净的「阻断 0」，须等 R-28／R-32 提交后复跑本分片。

### 6.1 对照分片（`测试中心/开发工具`，任务id `cbab074d73a14e30`，完成于 16:21:06）

同一轮里再跑本批**另一个**改动面所在的分片，用来判「红是不是本批／本分片造成的」：

| 分片 | 成功／失败／检查项 | 阻断项 | 只报告项 |
|---|---|---|---|
| `开发工具/薄壳`（本批主改动面） | 17／3／20 | ① 防回潮·反向破坏＝`设置本次调用基准` 在途导入失败 ② 公开调用完整性＝`OCR识别支持库` 内部层基线漂移 | 制品新鲜度 |
| `测试中心/开发工具`（本批测试文件所在面） | 17／3／20 | ① 契约漂移＝`文件系统支持库.内容检索.正则搜索` 的 `根目录` 注册默认值 `None` ≠ 契约 `''` ② **公开调用完整性＝同一条 `OCR识别支持库` 内部层基线漂移** | 制品新鲜度 |

读数含义：

- **「公开调用完整性／OCR 基线漂移」在两个分片同时出现** ⇒ 与分片无关、与 R-30 无关，是**全仓基线在途**（`开发文档/项目证据/能力id冻结基线.json` 未提交）；
- 另一条阻断各自不同（薄壳面＝`公共契约.运行时.平台适配` 的 R-32 在途新增名；测试面＝`内容检索` 的契约默认值在途改动）⇒ 都是**并发会话在途面**；
- 两条明细**无一指向** `开发工具/薄壳/薄壳服务.py`、`测试中心/开发工具/测试_薄壳返回可控.py`、`开发工具/薄壳/工具清单.py`；
- 制品新鲜度（只报告）两分片一致 ⇒ 与 R-27 遗留项同族，须「重建制品＋重启网关」后复验。

## 七、改了哪些文件 / 未做项

**改了的文件**

| 文件 | 改动 |
|---|---|
| `开发工具/薄壳/薄壳服务.py` | `元信息键` 3 → 19 键（唯一节点）＋ 逐键依据/已知例外注释；**非注释改动只有 1 处** |
| `测试中心/开发工具/测试_薄壳返回可控.py` | 新增 `test_超预算先丢元信息留正文`；3 处旧口径断言/桩改成现行口径（净增断言，未删断言） |
| `开发文档/分析/批R单据_R30_元信息键与薄壳回归_20260924.md` | 本单据 |
| `开发文档/未完成事项.md` | R-27 债务 #2/#3 标已收口；新增 R-30 债务（见该文件「批R·R-30 登记」节） |

**未做项**

1. **2 F（注入面超预算）未修**：落点在 `工具清单.py`（R-27 口径表），不在本批写集；已登记。缺的是「收哪一段静态文案」的口径裁决。
2. **`元信息键` 键集来源未根治**：仍是薄壳里的全局名集（2 条已知例外见 §1.6）；根治＝键集由能力契约供，属跨包改造。
3. **制品未重建**：R-27 遗留的「制品新鲜度」只报告项与本批同族（源码改了、制品未重建）。
4. **未 git 提交**：按纪律收尾由主会话统一做。
