<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批Q · Q-h · U-2 路径族参数 `说明` 文案收敛（独立复核 + 证据单据）

- 开工ID：`开工-20260924-103414-ca94`（租约 `27f2fa3d8bf04e2c`）
- 日期：2026-09-24｜本文件由本次开工独占
- 口径唯一腿：`公共契约/基础类型/字段名册.py` 的 `路径写法口径_普通` / `路径写法口径_基准` / `取路径写法口径` / `基准路径字段名`
- 判据接线：`开发工具/契约编译/参数可用性体检.py::检查路径口径`（判据 6，静态编译「参数可用性」门禁）

## 结论（直达核心）

**本项在本次开工前已由 批G 实质落地，本次 Q-h 没有产生任何源改动。**
落地提交：`081194c3`（开工 `开工-20260924-021527-acc8`，提交信息自述「L1 契约参数说明 103→0」）。

- 落地范围：**40 个包的 `能力定义.json`，103 条普通路径族参数的 `说明`**（`git show 081194c3 -- '*能力定义.json'` 实测 `-` 说明行 103 条 / `+` 说明行 103 条 / 文件 40 个，与派单数字逐一对上）。
- 本次实测读数：**改前 103 条 / 40 包 → 改后 0 条 / 0 包**（命令与原文见 §二）。
- 因此本次工作 = **独立复核 + 逐条明细与残余边界落盘**；硬去再改一遍源只会制造漂移，故未改。

## 一、措辞唯一来源（真源原文，本单据不另造）

`公共契约/基础类型/字段名册.py` 原文：

- `路径写法口径_普通 = "写项目根相对路径（posix、不带 ..）；绝对路径兼容"`
- `路径写法口径_基准 = "写绝对路径（它本身就是解析基准）"`
- 裁决句：「**普通路径族入参写「项目根相对路径」（posix，不带 `..`）；基准类入参写「绝对路径」。**」绝对路径在普通路径族入参上**永久兼容**，但规范写法只有一个。

⇒ 103 条改后文案用的固定句式即真源 `路径写法口径_普通` 原文外加「口径：」前缀，逐字对齐，未造第二套措辞：

```
（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容）
```

## 二、探针命令与真实读数

判据 6 的判定式（`检查路径口径`，实现原文口径）：

```
口径 = 取路径写法口径(名称)          # 非路径族 → 空串，跳过
if not 口径 or 名称 in 基准路径字段名: 跳过   # 基准类不判（写绝对是对的）
说明 = 参数.说明
if "绝对路径" in 说明 and "相对" not in 说明: 判红
```

命令（只读，**未跑编译**）：

```
python3 开发工具/契约编译/参数可用性体检.py
```

本次实测读数（原样粘贴，2026-09-24）：

```
参数可用性体检：扫描参数 2272 个，违规 0 条；存量基线 0
返回字段形状：裸返回 + 空值结构 + 裸值结构 149 条；存量基线 149
路径传参口径：与唯一口径冲突 0 条；存量基线 0
⇒ 三类均未超存量基线（存量冻结，只减不增）
```

判据 6 一行读数：**`路径传参口径：与唯一口径冲突 0 条；存量基线 0`**。
扫描面：142 个 `能力契约/参数契约.json`（排除 `工程缓存`，`体检()` 实现原文）。

基线的三段沿革（`git log -S` 实测，可复现）：

| 时点 | 提交 | `路径口径存量基线` | 现场读数 |
|---|---|---|---|
| 判据 6 接线（批C） | `e89a57b6` | **103**（冻结存量） | 103 |
| 源文案收敛（批G） | `081194c3` | 103（未动） | **0**（源已改，派生件由编译口重算） |
| 基线收敛（批Q·U-1） | `f48ef31c` | **103 → 0** | 0 |

⇒ 派单里说的「存量基线已冻结 103 条」= 批C 的冻结值；103 条的**源文案**已由批G 收敛，基线由批Q·U-1 同步下调到 0（只减不增）。

源/派生两面对账（本次实测）：`能力定义.json` 命中固定句式 **103 条 / 40 文件**，`能力契约/参数契约.json` 命中同一句式 **103 条 / 40 文件**，逐字一致 ⇒ 派生件不陈旧，判据读的派生面与源面同结论。

## 三、103 条明细（源文件 + 能力id + 参数名 + 改前/改后原文）

改前原文取 `081194c3^`（收敛前）同名参数，改后原文取当前盘上源文件，一一对应，无一条缺前。

| # | 源文件 | 能力id | 参数名 | 改前 `说明` | 改后 `说明` |
|---|---|---|---|---|---|
| 1 | `支持库/后端/OCR识别支持库/OCR识别/能力定义.json` | `OCR识别支持库.OCR识别.识别图片` | `图片路径` | 图片文件绝对路径（与图片字节二选一） | 图片文件路径（与图片字节二选一）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 2 | `支持库/后端/代码解析支持库/仓库地图/能力定义.json` | `代码解析支持库.仓库地图.建符号图` | `库文件` | SQLite 落点绝对路径；空 = 默认落在 <平台根>/工程缓存/仓库地图/<仓名>.db | SQLite 落点路径；空 = 默认落在 <平台根>/工程缓存/仓库地图/<仓名>.db（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 3 | `支持库/后端/代码解析支持库/仓库地图/能力定义.json` | `代码解析支持库.仓库地图.取仓库地图` | `库文件` | 仓库地图 SQLite 绝对路径；空 = 用默认库文件 <平台根>/工程缓存/仓库地图/<仓名>.db | 仓库地图 SQLite 路径；空 = 用默认库文件 <平台根>/工程缓存/仓库地图/<仓名>.db（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 4 | `支持库/后端/代码解析支持库/代码地图/能力定义.json` | `代码解析支持库.代码地图.查询节点` | `代码地图路径` | 代码地图 SQLite 文件绝对路径（如 <项目根>/.codegraph/codegraph.db） | 代码地图 SQLite 文件路径（如 <项目根>/.codegraph/codegraph.db）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 5 | `支持库/后端/代码解析支持库/代码地图/能力定义.json` | `代码解析支持库.代码地图.查询关系` | `代码地图路径` | 代码地图 SQLite 文件绝对路径（如 <项目根>/.codegraph/codegraph.db） | 代码地图 SQLite 文件路径（如 <项目根>/.codegraph/codegraph.db）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 6 | `支持库/后端/代码解析支持库/代码地图/能力定义.json` | `代码解析支持库.代码地图.查询状态` | `代码地图路径` | 代码地图 SQLite 文件绝对路径（如 <项目根>/.codegraph/codegraph.db） | 代码地图 SQLite 文件路径（如 <项目根>/.codegraph/codegraph.db）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 7 | `支持库/后端/代码解析支持库/能力定义.json` | `代码解析支持库.解析代码文件` | `路径` | 源码文件绝对路径 | 源码文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 8 | `支持库/后端/代码解析支持库/能力定义.json` | `代码解析支持库.加载切块规则` | `规则文件路径` | 切块规则 JSON 文件的绝对路径 | 切块规则 JSON 文件的路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 9 | `支持库/后端/大语言模型支持库/模型连接器/能力定义.json` | `大语言模型支持库.模型连接器.选择模型` | `参考路径` | 用户自带规则文件绝对路径（数据库/文件/文件夹）。不给=完全不涉及；给了只作排序的可选信号，底座不定义「什么是敏感」 | 用户自带规则文件路径（数据库/文件/文件夹）。不给=完全不涉及；给了只作排序的可选信号，底座不定义「什么是敏感」（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 10 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.探测媒体` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 11 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.提取音频` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 12 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.提取音频` | `输出路径` | 可选输出文件绝对路径（默认临时文件自动清理） | 可选输出文件路径（默认临时文件自动清理）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 13 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.转码` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 14 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.转码` | `输出路径` | 可选输出文件绝对路径 | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 15 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.抽取帧` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 16 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.抽取帧` | `输出路径` | 可选输出文件绝对路径 | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 17 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.截取音频` | `文件路径` | 源媒体绝对路径（视频或音频） | 源媒体路径（视频或音频）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 18 | `支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` | `媒体处理支持库.FFmpeg媒体.拼接媒体` | `输出路径` | 可选输出文件绝对路径；省略则写临时文件并自动清理 | 可选输出文件路径；省略则写临时文件并自动清理（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 19 | `支持库/后端/文件系统支持库/文件操作/能力定义.json` | `文件系统支持库.文件操作.移动文件` | `目标路径` | 目标文件或目录的绝对路径 | 目标文件或目录的路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 20 | `支持库/后端/文件系统支持库/文件操作/能力定义.json` | `文件系统支持库.文件操作.目录树` | `目录路径` | 树根目录绝对路径（不存在或不是目录返回 参数不合法） | 树根目录路径（不存在或不是目录返回 参数不合法）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 21 | `支持库/后端/文件系统支持库/文本补丁/能力定义.json` | `文件系统支持库.文本补丁.应用精确替换` | `文件路径` | 目标文件绝对路径 | 目标文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 22 | `支持库/后端/文档转换支持库/LibreOffice转换/能力定义.json` | `文档转换支持库.LibreOffice转换.转换办公文件` | `输入路径` | 输入文件绝对路径 | 输入文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 23 | `支持库/后端/文档转换支持库/PDF文本表格/能力定义.json` | `文档转换支持库.PDF文本表格.解析PDF` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 24 | `支持库/后端/文档转换支持库/PDF文本表格/能力定义.json` | `文档转换支持库.PDF文本表格.提取表格` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 25 | `支持库/后端/文档转换支持库/PDF渲染/能力定义.json` | `文档转换支持库.PDF渲染.检测加密页数` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 26 | `支持库/后端/文档转换支持库/PDF渲染/能力定义.json` | `文档转换支持库.PDF渲染.渲染整页` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 27 | `支持库/后端/文档转换支持库/PDF渲染/能力定义.json` | `文档转换支持库.PDF渲染.提取图像` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 28 | `支持库/后端/文档转换支持库/PDF隔离提供者/能力定义.json` | `文档转换支持库.PDF隔离提供者.解析PDF` | `文件路径` | 待解析文件绝对路径 | 待解析文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 29 | `支持库/后端/文档转换支持库/openpyxl提供者/能力定义.json` | `内部.表格文档.解析` | `文件路径` | 待解析 xlsx 文件绝对路径 | 待解析 xlsx 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 30 | `支持库/后端/文档转换支持库/python_docx提供者/能力定义.json` | `内部.文字文档.解析` | `文件路径` | 待解析 docx 文件绝对路径 | 待解析 docx 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 31 | `支持库/后端/文档转换支持库/python_pptx提供者/能力定义.json` | `内部.演示文稿.解析` | `文件路径` | 待解析 pptx 文件绝对路径 | 待解析 pptx 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 32 | `支持库/后端/文档转换支持库/textutil转换/能力定义.json` | `文档转换支持库.textutil转换.转换文本文件` | `输入路径` | 输入文件绝对路径 | 输入文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 33 | `支持库/后端/检索支持库/统一检索/能力定义.json` | `检索支持库.统一检索.统一检索` | `代码地图路径` | 代码地图 SQLite 绝对路径；不传用 项目根/.codegraph/codegraph.db | 代码地图 SQLite 路径；不传用 项目根/.codegraph/codegraph.db（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 34 | `支持库/后端/版本控制支持库/Git操作/能力定义.json` | `版本控制支持库.Git操作.创建工作区` | `新路径` | 新工作区绝对路径（白名单：禁空格/换行/;|&`$） | 新工作区路径（白名单：禁空格/换行/;|&`$）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 35 | `支持库/后端/版本控制支持库/Git操作/能力定义.json` | `版本控制支持库.Git操作.关闭工作区` | `目标路径` | 待关闭工作区绝对路径 | 待关闭工作区路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 36 | `支持库/后端/系统核心支持库/环境变量/能力定义.json` | `系统核心支持库.环境变量.读取环境文件` | `文件路径` | .env 文件绝对路径 | .env 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 37 | `支持库/后端/系统核心支持库/资源管理/能力定义.json` | `系统核心支持库.资源管理.原子写入` | `目标路径` | 目标文件绝对路径；父目录不存在时自动创建 | 目标文件路径；父目录不存在时自动创建（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 38 | `支持库/后端/系统核心支持库/路径安全/能力定义.json` | `系统核心支持库.路径安全.显示相对路径` | `目标路径` | 要展示的绝对路径 | 要展示的路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 39 | `支持库/后端/网络通信支持库/请求/能力定义.json` | `网络通信支持库.请求.上传文件` | `文件路径` | 本地文件绝对路径 | 本地文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 40 | `支持库/后端/转写支持库/转写/能力定义.json` | `转写支持库.转写.转写音频文件` | `文件路径` | 音频文件绝对路径 | 音频文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 41 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.建索引库` | `库文件` | 库文件绝对路径；留空取运行数据根/项目文档库.db | 库文件路径；留空取运行数据根/项目文档库.db（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 42 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.写入索引` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 43 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.查询索引` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 44 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.登记项目` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 45 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.查询项目` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 46 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.建坑索引` | `条目文件` | 踩坑条目 JSON 绝对路径 | 踩坑条目 JSON 路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 47 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.建坑索引` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 48 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.记踩坑` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 49 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.查开工上下文` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 50 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.登记未命中词` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 51 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.列出未命中词` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 52 | `支持库/后端/项目文档支持库/能力定义.json` | `项目文档支持库.建全仓索引` | `库文件` | 索引库绝对路径；留空取默认 | 索引库路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 53 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.探测媒体` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 54 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.提取音频` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 55 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.提取音频` | `输出路径` | 可选输出文件绝对路径（默认临时文件自动清理） | 可选输出文件路径（默认临时文件自动清理）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 56 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.转码` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 57 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.转码` | `输出路径` | 可选输出文件绝对路径 | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 58 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.抽取帧` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 59 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.抽取帧` | `输出路径` | 可选输出文件绝对路径 | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 60 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.截取音频` | `文件路径` | 源媒体绝对路径（视频或音频） | 源媒体路径（视频或音频）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 61 | `支持库/适配层/FFmpeg提供者/能力定义.json` | `FFmpeg媒体.拼接媒体` | `输出路径` | 可选输出文件绝对路径；省略则写临时文件并自动清理 | 可选输出文件路径；省略则写临时文件并自动清理（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 62 | `支持库/适配层/Git提供者/能力定义.json` | `Git操作.创建工作区` | `新路径` | 新工作区绝对路径（白名单：禁空格/换行/;|&`$） | 新工作区路径（白名单：禁空格/换行/;|&`$）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 63 | `支持库/适配层/Git提供者/能力定义.json` | `Git操作.关闭工作区` | `目标路径` | 待关闭工作区绝对路径 | 待关闭工作区路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 64 | `支持库/适配层/LibreOffice提供者/能力定义.json` | `LibreOffice转换.转换办公文件` | `输入路径` | 输入文件绝对路径 | 输入文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 65 | `支持库/适配层/MLXWhisper提供者/能力定义.json` | `转写.转写音频文件` | `文件路径` | 音频文件绝对路径 | 音频文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 66 | `支持库/适配层/PDF隔离提供者/能力定义.json` | `PDF隔离提供者.解析PDF` | `文件路径` | 待解析文件绝对路径 | 待解析文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 67 | `支持库/适配层/PyMuPDF提供者/能力定义.json` | `PDF渲染.检测加密页数` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 68 | `支持库/适配层/PyMuPDF提供者/能力定义.json` | `PDF渲染.渲染整页` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 69 | `支持库/适配层/PyMuPDF提供者/能力定义.json` | `PDF渲染.提取图像` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 70 | `支持库/适配层/Swift编译器提供者/能力定义.json` | `Swift编译器提供者.编译源代码` | `输出路径` | 可执行文件绝对路径 | 可执行文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 71 | `支持库/适配层/Swift编译器提供者/能力定义.json` | `Swift编译器提供者.签名制品` | `应用路径` | .app 目录绝对路径 | .app 目录路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 72 | `支持库/适配层/Swift编译器提供者/能力定义.json` | `Swift编译器提供者.校验签名` | `应用路径` | .app 目录绝对路径 | .app 目录路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 73 | `支持库/适配层/Tesseract提供者/能力定义.json` | `OCR识别.识别图片` | `图片路径` | 图片文件绝对路径（与图片字节二选一） | 图片文件路径（与图片字节二选一）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 74 | `支持库/适配层/pdfplumber提供者/能力定义.json` | `PDF文本表格.解析PDF` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 75 | `支持库/适配层/pdfplumber提供者/能力定义.json` | `PDF文本表格.提取表格` | `文件路径` | PDF 文件绝对路径 | PDF 文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 76 | `支持库/适配层/textutil提供者/能力定义.json` | `textutil转换.转换文本文件` | `输入路径` | 输入文件绝对路径 | 输入文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 77 | `模块库/媒体处理/能力定义.json` | `媒体处理.探测媒体` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 78 | `模块库/媒体处理/能力定义.json` | `媒体处理.提取音频` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 79 | `模块库/媒体处理/能力定义.json` | `媒体处理.提取音频` | `输出路径` | 可选输出文件绝对路径（缺省临时文件由支持库自动清理） | 可选输出文件路径（缺省临时文件由支持库自动清理）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 80 | `模块库/媒体处理/能力定义.json` | `媒体处理.转码` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 81 | `模块库/媒体处理/能力定义.json` | `媒体处理.转码` | `输出路径` | 可选输出文件绝对路径（缺省临时文件由支持库自动清理） | 可选输出文件路径（缺省临时文件由支持库自动清理）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 82 | `模块库/媒体处理/能力定义.json` | `媒体处理.抽取帧` | `文件路径` | 媒体文件绝对路径 | 媒体文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 83 | `模块库/媒体处理/能力定义.json` | `媒体处理.抽取帧` | `输出路径` | 可选输出文件绝对路径（缺省临时文件由支持库自动清理） | 可选输出文件路径（缺省临时文件由支持库自动清理）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 84 | `模块库/媒体处理/能力定义.json` | `媒体处理.拼接媒体` | `输出路径` | 可选输出文件绝对路径 | 可选输出文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 85 | `模块库/媒体转写/能力定义.json` | `媒体转写.转写视频文件` | `文件路径` | 视频文件绝对路径 | 视频文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 86 | `模块库/媒体转写/能力定义.json` | `媒体转写.转写音频文件` | `文件路径` | 音频文件绝对路径 | 音频文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 87 | `模块库/文件管理/能力定义.json` | `文件管理.读取文件` | `文件路径` | 待读取文件绝对路径 | 待读取文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 88 | `模块库/文件管理/能力定义.json` | `文件管理.写入文件` | `文件路径` | 目标文件绝对路径 | 目标文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 89 | `模块库/文件管理/能力定义.json` | `文件管理.复制文件` | `源路径` | 源文件绝对路径 | 源文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 90 | `模块库/文件管理/能力定义.json` | `文件管理.复制文件` | `目标路径` | 目标文件绝对路径 | 目标文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 91 | `模块库/文件管理/能力定义.json` | `文件管理.移动文件` | `源路径` | 源文件绝对路径 | 源文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 92 | `模块库/文件管理/能力定义.json` | `文件管理.移动文件` | `目标路径` | 目标文件绝对路径 | 目标文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 93 | `模块库/文件管理/能力定义.json` | `文件管理.删除文件` | `文件路径` | 待删除文件绝对路径 | 待删除文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 94 | `模块库/文件管理/能力定义.json` | `文件管理.列出文件` | `目录路径` | 待列出目录绝对路径 | 待列出目录路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 95 | `模块库/文件管理/能力定义.json` | `文件管理.流式创建摘要` | `文件路径` | 待摘要文件绝对路径 | 待摘要文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 96 | `模块库/文档解析/能力定义.json` | `文档解析.解析文档` | `文件路径` | 待解析文件绝对路径 | 待解析文件路径（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 97 | `模块库/直播逐字稿/能力定义.json` | `直播逐字稿.转写媒体文件` | `文件路径` | 媒体文件绝对路径（视频或音频） | 媒体文件路径（视频或音频）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 98 | `模块库/直播逐字稿/能力定义.json` | `直播逐字稿.全自动精校` | `源文件路径` | 视频或音频绝对路径（自动判别，无需用户分类） | 视频或音频路径（自动判别，无需用户分类）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 99 | `模块库/直播逐字稿/能力定义.json` | `直播逐字稿.全自动精校` | `导出路径` | 精校逐字稿输出绝对路径（用户唯一要看的文件） | 精校逐字稿输出路径（用户唯一要看的文件）（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 100 | `模块库/直播逐字稿/能力定义.json` | `直播逐字稿.全自动精校` | `缓存目录` | 中间产物绝对路径（分片/疑难/复核/裁决/质检），可断点续跑 | 中间产物路径（分片/疑难/复核/裁决/质检），可断点续跑（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 101 | `模块库/项目文档/能力定义.json` | `项目文档.登记项目` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 102 | `模块库/项目文档/能力定义.json` | `项目文档.写文档` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 103 | `模块库/项目文档/能力定义.json` | `项目文档.查文档` | `库文件` | 库文件绝对路径；留空取默认 | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |

共 **103 条 / 40 个源文件**；改前原文由 `git show 081194c3^:<源文件>` 取同名参数，无一条缺前。

## 四、残余与边界（如实留档，不硬凑）

- **残余命中：0 条 / 0 包。** 判据 6 无残留，故无「保留理由」需要解释。
- **为什么新文案必须同时出现「相对」**：判定式是 `"绝对路径" in 说明 and "相对" not in 说明` ⇒ 只把「绝对路径」改成「项目根相对路径」而不保留「绝对路径兼容」，同样过门禁；但真源 `路径写法口径_普通` 原文本身就同时含两者（规范写法 + 兼容性承诺），故 103 条统一带全句，未做「只求过门禁」的删减。
- **判据 6 判不到的边界（本次实测，如实申报，未改判据、未改文案）**：
  1. **基准类 110 条**：`名称 in 基准路径字段名` 显式跳过——它们写「绝对路径」正是对的（相对写法没有别的基准，会基准漂移）。
  2. **沉默类 227 条**：普通路径族里 `说明` 既不含「相对」也不含「绝对路径」（如 `临时目录` 的「临时空间采样目录；留空取系统临时目录」）。判定式只抓「提绝对不提相对」，这类判不出。本次实测其中含「绝对路径」者 **0 条** ⇒ 不是判据漏报，是判据设计面之外的一类（口径已由唯一腿集中承载，靠参数说明逐条复述不是本判据的目标）。
  3. **非参数位文案**：判据 6 只读 `能力[].参数[].说明`；包级/能力级 `说明` 里的同类措辞（例：`模块库/直播逐字稿/能力定义.json` 包说明仍写「传视频或音频绝对路径」）不在判红面。
- **本次未触及**：`检查路径口径` 实现、`路径口径存量基线 = 0`、任何 `能力定义.json` / `参数契约.json` 均**未改**。本开工只新增本单据一个文件；`git status --porcelain` 另有 `开发文档/未完成事项.md`、`开发文档/项目证据/MD写入流水.jsonl`、`技能库/技能/能力上架自检/*`（3 个）等脏文件，其 mtime（10:34:40–10:35:07）早于本单据（10:35:23）且属并行会话，**非本开工所改**。

## 五、未做与边界声明

- 未跑编译（任务纪律：主会话统一收口）；仅跑了只读判据脚本 `参数可用性体检.py`。
- 未 git 提交、未建分支（主会话统一收口）。
- 未改任何源文件（**改无可改**：103 条已于批G 收敛，基线已于批Q·U-1 收敛）。
- 未改他人文件；本单据独占，未动 `批Q单据_Qa_存量基线收敛_20260924.md`（U-1）等兄弟单据。
- 复用而非另写：明细与残余统计一律 import `公共契约.基础类型.字段名册` 的 `是路径族字段` / `基准路径字段名` / `取路径写法口径` 取口径，**未另写路径参数名单**；判据读数一律来自 `参数可用性体检.py` 本体，未另写扫描脚本。
