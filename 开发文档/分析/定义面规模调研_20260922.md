# 定义面规模调研 · 2026-09-22

> 起因：华哥 2026-09-22「所有的变量，函数名，类似于字段这种，**定义好先，再去操作**，
> 不然现在左右乱飞。你可以使用代码地图，试试看，有多少个变量等，**不改代码，就只调研**」。
> 本件是**纯调研**：只量化现状，不提修法、不改任何生产代码。
> 取证工具：`.codegraph/codegraph.db`（只读）+ AST 扫字典键字面量（脱机脚本 `/tmp`）。
> 开工 ID：`开工-20260922-155137-42ef`（同批）
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

## 一、定义面规模（代码地图口径，实测）

代码地图状态实测：**24472 节点 / 53964 边 / 1229 文件，覆盖率 1.0**（`可用=true`）。

| 种类 | 定义处数 | 不同名字数 | 平均同名 |
|---|---|---|---|
| import | 7695 | — | — |
| method | 5518 | 4172 | 1.3 |
| function | 5054 | 4002 | 1.3 |
| **variable** | **3882** | **2621** | 1.5 |
| file | 1229 | — | — |
| class | 1092 | 1015 | 1.1 |
| constant | 2 | 2 | 1.0 |

**变量 3882 处 / 函数 5054 处 / 方法 5518 处 / 类 1092 处** —— 这就是华哥问的「有多少个变量等」。

## 二、「左右乱飞」的量化：跨文件重名

### 变量名跨文件重名 Top 10

| 名字 | 处数 | 跨文件数 | 是否正常 |
|---|---|---|---|
| `__all__` | 255 | 255 | 正常（Python 约定）|
| `系统根` | 244 | 224 | **可疑：同一概念应有唯一来源** |
| `来源` | 56 | 56 | 可疑 |
| `包id` | 33 | 33 | 正常（约定保留 id）|
| `_规格` | 31 | 31 | 可疑 |
| `_模块` | 30 | 30 | 可疑 |
| `唯一实现名` | 30 | 30 | 可疑 |
| `唯一实现文件` | 30 | 30 | 可疑 |
| `默认超时秒` | 30 | 30 | **可疑：默认值应只声明一处**|
| `仓库根` | 22 | 22 | 可疑 |

### 函数名跨文件重名 Top 10

| 名字 | 处数 | 跨文件数 | 判读 |
|---|---|---|---|
| `__init__` | 197 | 167 | 正常 |
| `注册能力` | 151 | 151 | 正常（平台装配约定）|
| `setUp` / `tearDown` | 205/116 | 139/89 | 正常（unittest）|
| `_失败` | 69 | 69 | **可疑：每个包各写一份助手** |
| `_校验参数名序` | 51 | 51 | **可疑：同上，51 份拷贝** |
| `转字典` | 59 | 47 | 可疑 |
| `调用能力` | 26 | 24 | 可疑 |
| `主函数` / `主程序` | 24/15 | 24/15 | 正常（脚本入口）|
| `_成功` | 22 | 22 | 可疑 |

## 三、字段面（代码地图**不覆盖**，本次补测）

华哥特别提到「类似于字段这种」——代码地图 `nodes` 表只收 variable/function/class，
**不含字典键**，而「字段」正是返回值信封与参数对象的主体。故另用 AST 扫全仓字典键字面量：

| 指标 | 实测 |
|---|---|
| 字典键出现总处数 | **31462** |
| 不同键名个数 | **5016** |
| 只出现在**单个文件**的键名 | **3234 个（64%）** |

**64% 的字段名只在一个文件里出现过** —— 这就是「定义好先，再去操作」最直接的缺口：
它们不是「定义」，而是各自在用的字面量。

### 按跨文件数分层

| 档 | 键名个数 |
|---|---|
| 跨 ≥100 文件 | 12 |
| 跨 30–99 文件 | 25 |
| 跨 10–29 文件 | 111 |
| 跨 3–9 文件 | 715 |
| 跨 2 文件 | 919 |
| 单文件内 | 3234 |

### 信封字段（统一结果里的已知键，实测处数）

| 字段 | 处数 | 跨文件数 |
|---|---|---|
| 成功 | 535 | 118 |
| 错误码 | 565 | 155 |
| 错误说明 | 373 | 102 |
| 状态 | 249 | 101 |
| 值 | 208 | 80 |
| 说明 | 1318 | 174 |
| 原因 | 169 | 56 |
| 句柄 | 128 | 45 |
| 请求id | 67 | 34 |
| 详情 | 77 | 25 |
| 可重试 | 42 | 22 |
| 耗时毫秒 | 36 | 23 |

### 疑似同义异名（人工可裁决的候选，本次只列不判）

| 族 | 候选 | 实测 |
|---|---|---|
| 路径 | 路径 / 文件路径 / 目标目录 / 根目录 / 项目根 / 工作目录 / 输出路径 / 源路径 | 200/116/8/14/49/9/16/2 处 |
| 错误 | 错误 / 错误码 / 错误说明 / 原因 / 失败原因 / 问题 | 43/565/373/169/15/13 处 |
| 尺寸 | 大小 / 字节数 / 长度 / 数量 / 条数 | 9/26/1/50/8 处 |
| 时间 | 时间 / 时间戳 / 耗时秒 / 耗时毫秒 / 开始时间 / 结束时间 | 79/3/29/36/7/1 处 |
| 成败 | 成功 / 通过 / 有效 / 可用 / 结果 / 值 | 535/72/11/27/44/208 处 |

### 含英文字母的字段名（全中文命名铁律面）

**808 个键名含英文字母（共 3201 处）**。Top：`能力id` 414、`包id` 186、`content` 72、
`请求id` 67、`role` 62、`type` 52、`项目id` 51、`需求id` 46、`任务id` 42、`Content-Type` 35、`开工id` 34。

其中 `id` 类（能力id/包id/请求id/项目id/需求id/任务id/开工id）是**铁律明文允许**的保留写法；
`content` / `role` / `type` / `Content-Type` / `url` 等属**协议强制英文**（HTTP 头、LLM 消息角色），
也是允许的。**本次未发现明确违规项**，故不列缺陷，只作规模记录。

## 四、结论（只陈述事实，不提修法）

1. **定义面规模**：变量 3882 / 函数 5054 / 方法 5518 / 类 1092（代码地图口径）；
   字段（字典键）出现 31462 处、**5016 个不同名字**（AST 口径）。
2. **「乱飞」的主要形态有两个**：
   - **跨文件重名**：`系统根` 跨 224 个文件各写一遍（244 处）、`默认超时秒` 跨 30 个文件（30 处）、
     `_失败`/`_校验参数名序` 各跨 69/51 个文件（每个包一份拷贝）；
   - **单例字段**：**64% 的字段名（3234 个）只在一个文件里出现过** —— 它们从未被当作「定义」沉淀下来。
3. **本件不改任何代码**（华哥明示「不改代码，就只调研」）；
   结论供后续裁决「哪些该收成公共定义、哪些是合理局部量」。

## 五、取证脚本与口径（可复现）

- 代码地图口径：`SELECT kind, COUNT(*) FROM nodes GROUP BY kind`（只读开库）+ 跨文件重名 `GROUP BY name HAVING COUNT(DISTINCT file_path) > 1`；
- 字段面口径：AST 扫 `ast.Dict` 的 `str` 常量键，扫描面 = 15 个顶层源码目录，实测扫 1228 个 .py、解析失败 0；
- **两个口径不可互相替代**：代码地图不含字典键；AST 不含类型标注与跨文件引用关系。
