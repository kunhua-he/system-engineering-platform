# 批O 清单（第二轮：证真 + 精确定位修复落点 + 排序）

> 状态：待执行 ｜ 生成：2026-09-24 06:59 ｜ 开工ID：`开工-20260924-065922-a059`
> 上游：`开发文档/未完成事项.md` §7.11（批N 登记 N-2…N-8）
> 口径：哲学 12.1（三轮）／12.2（一次性盘清、一次性全派）／12.3（单任务 ≤10 分钟）／14.1（归因三分）
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

## 一、本批的前置（已完成，带读数）

| 项 | 读数 |
|---|---|
| 制品重建 + 安装 | 制品摘要 `4658509d5495a20e`；可重复构建校验通过；稳定路径可读 |
| 网关重启 | PID 7634 → **71719**；就绪 2.88s；能力数 752；装配告警 **0**；**运行态同根 = 真** |
| 全仓静态编译（类型=0） | 成功 **19** / 失败 **1**；五态结论 = **只报告，无阻断红项** |
| `制品新鲜度` | **已转绿**（退出码 0）—— 上线链生效，N-1/N-7 的修复**已在运行态** |
| 唯一只报告红 | `md查重`（退出码 1） |

## 二、证真结论（`git show HEAD:<文件>`，按 12.1 ④）

**当场删掉（前提已不成立）**：

| 原条目 | 证真读数 | 处置 |
|---|---|---|
| N-2 之「`收工释放` 漏转发 `项目根`」 | HEAD `模块库/开工编排/实现/开工编排.py:966-969` **已经转发**，且注释逐字写着「★ 转发解锁腿的解析基准（不转发 = 解锁指纹按 cwd 解析 = 本缺陷）」 | **删**。残留只有「两处都空 ⇒ 不转发」，而 `_解析基准根` `:224-226` 的 docstring 明写这是**刻意设计**（「不转发该参数，让下游按自己那条解析腿走」，不是转发空串冒充根）⇒ 该残留**不是缺陷**，一并删 |
| N-5 之「`任务进程.py:424` `查询` 带写副作用」 | HEAD 该文件 `:439` 起的 `查询` docstring 明写「**只读入口不带写副作用**（批K）：旧实现在重建分支里调 `_登记任务表`…」⇒ **批K 已修** | **删**。子代理给的行号（`:424`/`:435`）是**过时行号**，实际指向 `_终止未登记工作器` 的 docstring |

**前提成立（进入第三轮）**：下表。

## 三、第三轮派工表（一次性全派）

> 并行划分依据：**写集互不相交**即可并行；同一文件一律串行（平台租约也会挡）。

| # | 交付物（一个可独立验收物） | 落点（`文件:行号`） | 最小改法 | 归因 | 写集 | 并行 |
|---|---|---|---|---|---|---|
| O-1 | 租约腿根解析收口 | `平台控制面/能力目录/文件租约存储.py:242`（下游 `:243`/`:262`）+ 同族 `实现/文件租约申请.py:131,:213`、`实现/文件改动登记.py:87`、`实现/文件租约维护.py:166,:296,:449,:503` | 留空即 `解析路径(…, None)` ⇒ env→cwd（网关 cwd = 制品根）⇒ **登记错那棵树的内容指纹**。改：这些入口一律要求显式根，取不到即 `参数不合法`（fail-closed），不自落 cwd | ②架构错位 | `平台控制面/能力目录/**` | 独立 |
| O-2 | 模型连接器根解析收口 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型供应链校验.py:114` | 同 O-1（自述「生效根 = 环境变量 → 进程 cwd」） | ②架构错位 | 该文件 | 独立 |
| O-3 | 文件系统补充根解析收口 | `支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py:233` | 同 O-1 | ②架构错位 | 该文件 | 独立 |
| O-4 | 自修复工具自锚定收口 | `模块库/自修复工具/实现/客户端构建.py:147`（`_源码根` 第③档）、`开发编译口自检.py`、`编译公共.py:271` | 第③档「本模块所在树」是**第二条腿**（权威节点 = `公共契约/运行时/导入前缀.取系统根`，批N 已在 `仓库锁.py`/`能力目录.py` 收口）⇒ 一律转调 `取系统根(__file__)` | ②架构错位 | `模块库/自修复工具/**` | 独立 |
| O-5 | 摘要序列化收成唯一腿 | `开发工具/重建依赖锁.py:313`（`新文本 = json.dumps(数据, ensure_ascii=False, indent=2)`）、`开发工具/MD文档生成/说明书/完整性摘要生成器.py:48/:53`、`支持库/后端/组件规范支持库/实现/组件规范.py:103-104` | 三处自写序列化公式（字节今日相同，**无尾换行**一处）⇒ 一律转调 `开发工具/全量重算摘要.py` 的 `完整性摘要文本`（被声明的唯一权威）。**判据**：改后 `python3.14 -m 开发工具.全量重算摘要 --只报` 仍「全部闭合」 | ②架构错位 | 上述 3 文件 | 独立 |
| O-6 | 写实体后同轮刷摘要 | `开发工具/依赖派生/生成依赖分两段.py:870-875`、`开发工具/MD文档生成/说明书/载位同步.py:245`、`开发工具/契约编译/版本自增.py:117-125`、`支持库/后端/组件规范支持库/实现/支持库模板生成器.py:620`（写前多算一次，删 `:620` 留 `:629`） | 写实体后**同轮**重算该包摘要（复用 `完整性摘要文本`） | ②架构错位 | 上述 4 文件 | 独立 |
| O-7 | `任务系统.查询` 去写副作用 | `运行核心/任务调度/任务系统.py:311`（终态分支 `:324-325` 调 `_保存已加锁()` + 写 `上次同步状态表`） | 只读入口不得落库；终态回写改由**收敛点**（`_收敛已加锁` 一族）负责，查询只读。**判据**：`测试中心/运行核心/测试_任务系统*.py` 既有用例不回归 | ①实现错 | `运行核心/任务调度/任务系统.py` | 串行（与 O-8 同族） |
| O-8 | SQLite 读路径去建表 DDL | `支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py:419`（`_创建运行库表(连接对象, 补齐列=False)`）+ `:105` 经 `_打开` 建目录切 WAL | 读路径只读：建表移到**写入/初始化腿**；`_打开` 的目录/WAL 副作用同理。**判据**：`测试中心/支持库/测试_SQLite数据库*.py` 不回归 | ①实现错 | 该文件 | 独立 |
| O-9 | 存量基线下调 | `开发工具/测试写入边界门禁存量基线.json`（2 桶该降：键差 8 / 处差 10）、`开发工具/验证门禁/存量基线.json`（1 键）、`开发工具/契约编译/全面漂移存量基线.json`（40 桶全收敛 = 整体空转）、`开发工具/发布门禁/防回潮存量基线.json`（裸布尔虚高 33） | **跑门禁自己的 `--写基线`，不手改基线文件**。另：`能力id冻结基线` 该升未升（现场 +1 id）须重建 | ①实现错 | 4 个基线 json | 独立 |
| O-10 | 死条目清理 | `开发工具/发布门禁/运行发布门禁_执行项分组_制品与流程.py:328-331`（`旧过渡目录表` **24 名现场全不存在 ⇒ 判据恒真、无鉴别力**）、`开发工具/组件合规/模块合规.py:43`（`第三方根模块` 7 名零 import，且与 `第三方导入分布基线.json` **双向脱节**）、`开发工具/验证门禁/同动作双路径检测.py:434`（`校验路径在仓库内` 已成死条目）、`开发工具/依赖生命周期审计/审计核心.py:526`（`无跨调用状态` 死条目） | 该删就删；`模块合规` 第三方根模块表**改由 `第三方导入分布基线.json` 单腿供数** | ①实现错 | 上述 4 文件 | 独立 |
| O-11 | `任务查询` 顶层状态不再误导 | `运行核心/任务调度/任务系统.py`（顶层 `状态` 字段）+ `模块库/自修复工具/实现/编译公共.py:271` | 顶层 `状态` 只表示「命令跑完了」；**拒启/未核验不得回「成功」**——改由 `结果.五态结论` 派生，或改名 `进程状态` 并必带 `五态结论`+`退出码`。**判据**：造一条被单写者守卫拒启的编译任务，读回执须一眼看出「没跑成」 | ①实现错 | 同 O-7 文件 | **与 O-7 串行**（同文件） |
| O-12 | 测试夹具跟上写入授权判据 | `测试中心/公共契约/测试_仓库只读锁.py`（12 failures + 4 errors，HEAD 基线对照同为 12/4） | 夹具改走 `平台控制面.能力目录.申请文件租约` **真认领**（而非直调 `锁.解锁供写入()` 造窗口） | ①实现错 | 该测试文件 | 独立 |
| O-13 | 参数名册统一（**须裁决**） | 名册唯一节点 = `公共契约/基础类型/字段名册.py`（`路径族字段名` 75 / `基准路径字段名` / `同义异名表` / `判违规异名表`，均已存在）；未统一 8 簇：仓库根族 8 名一义（64 处）、**状态库族**（`存储目录` 68 处 / 17 说明 / **5 类落点**）、边界根族 2 名、SQLite 库文件族 3 名、输出字节上限族（名册与契约**类型互相矛盾**）、写目标文件族 5 名、路径清单族 6 名、文件模式族 3 名 | 改名 = **对外契约面变更**，须先出设计。**裁决依据见第四节** | ③设计缺陷 | 142 份 `能力定义.json` | **本批不派**，等裁决 |

## 四、须裁决项（按哲学 14.1 / 第 5 条先自裁，不足才上呈）

**O-13 参数名册统一**：哲学第 5 条 5.3 明写「**版本递增的判据 = 对外契约指纹变化**」，且 `开发工具/契约编译/对外契约变更判定.py` 是**已存在的唯一判据**⇒ 改名后版本自增**有机器腿兜住**，不构成阻断。但 5.3 同时把该判据标为「★★ 半强制（最要紧的一条）—— 发布门禁零命中」。
⇒ **本会话自裁结论**：**先做「零风险的一半」**——把 8 簇的**规范名定进名册**（`路径族字段名` 是冻结集合，只增不改不删；新增 `同义异名表` 条目是**纯登记**，不动任何契约），**不做** 142 份契约的批量改名。改名本身拆成独立批次、单独出设计。
⇒ 若华哥认为该口径不对，明天再裁。

## 五、验收口径（第三轮收口时跑，中途不跑）

1. `自修复工具.静态编译{类型:1}` 逐个受影响包 ⇒ **无阻断红项**；
2. `自修复工具.静态编译{类型:0}` ⇒ 与本节基线（成功 19 / 失败 1）**对账零新增**；
3. `python3.14 -m 开发工具.全量重算摘要 --只报` ⇒ 「全部闭合」；
4. 受影响包的定向单测（`python3.14 -m unittest 测试中心.<…>`）⇒ 通过；
5. `测试中心/公共契约/测试_仓库只读锁.py` 的 12/4 **必须归零**（O-12 的验收物）。

## 六、本节边界（如实留档）

- 证真只覆盖了 N-2 与 N-5 的两条**已不成立**条目 + N-3/N-4/N-8 的存在性；**未**逐条证真 O-1…O-12 的每一个行号（行号来自子代理只读审计，可能随提交漂移）。**子代理到场后仍须现场复核行号**（12.3 ⑤：复核是对照一两条命令，不是从零探索）。
- `--全仓` 口径下**按影响面包派生凭据（契约版本自增／能力定义编译）覆盖为零（未核验）**—— 本轮实测新发现，登记待下批。
