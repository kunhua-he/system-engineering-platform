# 开工 ID：路由簇（TZ-20260918-02）

> 最后更新：2026-09-18
> 口径：实测（开工前现场回读，命令随文给出）
> 维护者：小马仔（路由簇）

## 一、开工 ID 与用途

**开工 ID = `路由簇`**（英文代号 `TZ2`，全称「智能路由与模型打分」簇）。
本 ID 用于本会话所有改动、提交信息、文档标注的唯一标识，避免与其他并发会话混淆。

前置簇：`探针簇`（TZ-20260918-01，已落地提交 `2953118d`）。

## 二、文件域声明（文件级独占）

**只允许改以下文件**（其余一律不碰）：

```text
支持库/后端/大语言模型支持库/模型连接器/实现/模型用量.py        （新建：记录 + 汇总）
支持库/后端/大语言模型支持库/模型连接器/实现/模型路由.py        （新建：选择模型）
支持库/后端/大语言模型支持库/模型连接器/__init__.py
支持库/后端/大语言模型支持库/模型连接器/能力定义.json
支持库/后端/大语言模型支持库/模型连接器/包声明.json
支持库/后端/大语言模型支持库/模型连接器/能力契约/参数契约.json
支持库/后端/大语言模型支持库/模型连接器/能力数据/能力搜索数据.json
支持库/后端/大语言模型支持库/模型连接器/权限契约/权限契约.json
支持库/后端/大语言模型支持库/模型连接器/说明/使用说明.md
支持库/后端/大语言模型支持库/模型连接器/说明/设计说明.md
支持库/后端/大语言模型支持库/模型连接器/完整性摘要.json        （生成物）
支持库/后端/大语言模型支持库/完整性摘要.json                    （生成物）
测试中心/支持库/测试_模型连接器路由.py                          （新建）
开发文档/方案/智能路由与模型打分方案_20260918.md
开发文档/未完成事项.md
```

**★ 明确不碰**：`模型连接器/实现/模型连接器.py`（他人在途）、`运行态表定义`
（属 `数据库连接支持库`，跨包改动）。用量数据走**本包自己的专用库**（照 `会话存储`/
`嵌入缓存` 先例），因此**不需要动别人包**。

## 三、已核实事实包（现场取证，勿重新探索）

| 事实 | 取证 |
|---|---|
| ★ **打分燃料为空**：`会话存储.db` 会话表 0 行、消息表 0 行、成本统计非空 0 行 | `sqlite3` 只读查询 |
| 六个运行态域（`任务/作业/协作状态/能力占用/灰度观测/灰度状态/会话`）**无「模型用量」域** | 读 `提供者.py:178 运行库表定义` |
| 全底座搜「调用记录/用量统计/评分」→ **0 条能力命中** | 扫全部 `能力定义.json` |
| 本包同父下**两个专用库先例**（会话存储/嵌入缓存）都**直接 `import sqlite3`** 连自己的库，不跨包依赖 | `grep -n "import sqlite3"` 两个实现文件 |
| 专用库路径先例：`解析运行数据根(Path(__file__).resolve().parents[5])` + `库名.db` | `会话存储.py:30`、`运行缓存.py:65` |
| 支持库间**正式跨包通道**是能力调用器（按能力id），但**本包先例不跨包** | `公共契约/能力契约/调用器.py` |
| `归一化对话响应` 已能提用量（`prompt_tokens`/`completion_tokens`、`input_tokens`/`output_tokens` 两套命名） | `响应归一化.py:26` |
| 探针能力已可用（前置簇交付）：`探测模型端点`，15/15 单测 + 13/13 真实 HTTP | 提交 `2953118d` |
| 底座 JSON **格式不统一**（能力定义/参数契约 indent=2；能力搜索/权限契约/包声明 indent=1） | 前置簇实测；写回必须用原格式 |
| 并发期**禁止全仓级动作**；提交必须带 pathspec | `规范/多会话并行开发规约.md:320` |
| 改源码后网关**有模块缓存**，必须调 `热接入` 才生效（非重启） | 前置簇实测 |

## 四、可直接复制执行的验收命令

```bash
cd "/Users/hekunhua/Documents/Agent/PHP/系统工程平台"
unset PYTHONPATH

# 1) 语法
python3.14 -m py_compile 支持库/后端/大语言模型支持库/模型连接器/实现/模型用量.py \
                          支持库/后端/大语言模型支持库/模型连接器/实现/模型路由.py

# 2) 定向单测
python3.14 -m unittest 测试中心.支持库.测试_模型连接器路由

# 3) 包级摘要（禁止全仓重算）
python3.14 -m 开发工具.全量重算摘要 --只报   # 只读核对口径；写盘走包级能力

# 4) 真实 HTTP（经 40007 唯一网关）
#    凭证现场取：plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - \
#      ~/Library/LaunchAgents/com.huashi.gateway-40007.plist
```

## 五、边界与禁做

- **禁止** `git add -A` / 全量重算摘要 / 全量编译。
- **禁止**动 `模型连接器.py`（他人在途）与任何其他包。
- **禁止**在底座里定义「敏感/隐私」规则（哲学 9.3；华哥：限制越多兼容性越差）。
- **默认零额度**：只有显式打开 LLM 辅助才可能耗额度，且必须在返回里写明。
- 凭证不得写入任何文档。
