# 批Q 单据 · Q-g：P-7 六条存量基线「独立复核」

> 对应批P **P-7**（六条存量基线该升／该降）。
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
> 子代理：`子代理-Qg`｜时刻：2026-09-24 09:52–10:00｜**未提交 git**（纪律：主会话收口统一提交）。
> 开工ID：`开工-20260924-095258-86dc`（1 条租约：本文件）。
> 上游：`开发文档/分析/批P清单_20260924.md` P-7 段 ＋ `开发文档/分析/批Q清单_20260924.md` 一／二／五节。
> **同批已交付**：`开发文档/分析/批P_P-6P-7_Q-c回传_20260924.md`（Q-c，2026-09-24 09:48）。**本件是独立复核，不是重复施工**：六条改动已由 Q-c 落地在工作树（未提交），我做的是逐条**独立复跑 + 出口探针 + 反向验证**。
> 本件是**证据单据**（命令 + 真实输出），不是第二份口径源；口径以代码与 `开发文档/规范/项目细则.md` 为准。

---

## 〇、结论先行

1. **P-7 六条已由 Q-c 全部落地**（工作树未提交）。我逐条**独立复跑**，六条读数**全部与「应收敛到」一致**；唯一偏差是第 4 条 —— 批P 表写 **1281**，**出口真值 1282**（差额已定位到具体文件，见 §二）。
2. **「3 条走出口 ／ 3 条无出口」的判定成立** —— 我用独立探针复核过，不是采信自述（见 §三）。
3. **本轮零改动**：只读 + 只落本 md（写集 1 条）。
4. **4 处残留 + 1 条机制观察**（见 §五），全部在 P-7 写集**之外**，按纪律报主会话、**未越域**。

---

## 一、六条逐条（现值 → 我的独立复跑读数 → 出口）

| # | 载体 | 批P 现值 | 工作树现值 | 我的独立复跑读数 | 出口 |
|---|---|---|---|---|---|
| 1 | `开发工具/发布门禁/运行发布门禁_防回潮公共.py:275` 裸布尔标量 | 374 | **335** | 标量 335 ＝ `开发工具/发布门禁/防回潮存量基线.json` 项.裸布尔.条目数 335、文件数 77，两处相等=True | **无出口**（§三①） |
| 2 | `开发文档/项目证据/能力id冻结基线.json` `能力数` | 751 | **752** | 门禁 CLI：`能力 id 冻结门禁通过：当前集 ⊇ 基线集，无 id 消失`；直算：条目 752 ／ 现场 752 ／ 消失 [] ／ 新增 [] ／ 指纹变化 0；`自修复工具.仓库锁` 指纹 `324c57660a18bde2`（归属 `模块库.自修复工具`）；网关 `健康检查` 能力数 **752**（第三源） | `--冻结` |
| 3 | `开发工具/参数口径基线.json` `能力数` | 675 | **690** | `python3.14 -m 开发工具.参数口径审计`：`能力 690 个`、`真分歧 1 → 1`、`✓ 无新增真分歧`，退出码 0 | `--写基线` |
| 4 | `开发文档/项目证据/第三方导入分布基线.json` `文件数` | 1212 | **1282**（批P 写 1281） | 门禁 CLI：`扫描 1282 个 .py；第三方模块 22 个（基线 22 个）；import 共 111 处`、`门禁通过` | `--冻结` |
| 5 | `开发工具/公开调用完整性门禁存量基线.json` `条目` | 7 桶 20 条 | **`{}`** | 门禁 CLI：`通过…无新增违规`；直算：现场原始违规 **0 条**、新增 0 ／ 存量 0 ／ 收敛 0 | **无出口**（§三②） |
| 6 | `开发工具/契约编译/参数可用性体检.py:261` `路径口径存量基线` | 103 | **0** | `python3.14 开发工具/契约编译/参数可用性体检.py`：`路径传参口径：与唯一口径冲突 0 条；存量基线 0`，退出码 0 | **无出口**（§三③） |

---

## 二、批P 表的偏差（如实报，1 处）

**第 4 条「应收敛到 1281」偏低 1，现场真值 1282。** 差额**已定位到具体文件**：`开发工具/存量基线消费.py`（P-6 的唯一节点，Q-a 2026-09-24 09:27 落盘，**晚于批P 采样时点**；`开发工具/` 在扫描面内）。判据（内存剔它，**不落仓**）：剔掉该文件后扫描面恰为 **1281 个 .py**，逐字命中批P 的数。⇒ **取出口读数 1282**（出口是现场权威，不按旧快照回退）。

其余五条的「现值 ／ 应收敛到」与我的复跑**逐条一致，无偏差**。

---

## 三、出口探针（「无出口」是实测，不是自述）

① **R1 裸布尔标量**：`rg -n "add_argument|ArgumentParser" 开发工具/发布门禁/运行发布门禁.py` ⇒ 只有 `--制品 ／ --包目录 ／ --跳过测试 ／ --禁止真实进程 ／ --测试并行数 ／ --测试真跑`（`:780-790`），**零写基线入口**；标量是 `运行发布门禁_防回潮公共.py` 的**模块级常量**，无生成器。
② **R5 存量基线**：`rg -n "add_argument|ArgumentParser|写入文件|write_text" 开发工具/公开调用完整性门禁.py 开发工具/公开调用完整性门禁_检查与判定.py` ⇒ **零命中**（既无 CLI 写口，也无落盘腿）。
③ **R6 路径口径存量基线**：`开发工具/契约编译/参数可用性体检.py:329 def 主函数() -> int:` **不接 argv**，`:364` 的 `__main__` 直接调它；常量在 `:261` 模块级赋值。

---

## 四、反向验证（5 条，均为现场真输出）

| # | 手法 | 弄坏 | 还原 |
|---|---|---|---|
| A | R2，`--基线` 指向 /tmp 副本（往基线**加**一个不存在的能力 id） | 违规 **1** 项（`伪造.不存在的能力`） | 违规 0 |
| B | R4，/tmp 副本 | ① `文件数` 改 99999 → **仍绿**（证实批P 表「非判据字段」）；② 删模块 `PIL` → 违规 1 项（基线外新增） | 违规 0 |
| C | R6，内存 monkeypatch `路径口径存量基线 = -1` | 退出码 **1** | 退出码 **0** |
| D | R1，把 `判据._读分桶基线` 打成恒 None（模拟分桶基线不可读） | 标量 335 → 仍红（总数判定 341 > 335）；**标量改 999 → 转绿** | 复原后红 |
| E | R5，把清空前那份 7 桶（键取自现文件 `理由` 段）喂回 `应用存量基线` | 现场原始 0 条 → **收敛 7 桶**（逐桶 当前 0 < 基线 3） | 条目 `{}` → 收敛 0 |

**A 的第一次试验是错误设计，如实留痕**：我先把 `自修复工具.仓库锁` 从 /tmp 基线里**删掉**，结果**不判红** —— 因为 R2 的判据是「当前集 ⊇ 基线集」，**缩基线是合法方向**。改成**加**一个伪造 id 才判红。

---

## 五、新发现（全部在 P-7 写集之外，报主会话，未越域）

**F1（机制 · 须裁）标量与分桶条目数是同一事实的两处。** `运行发布门禁_防回潮公共.py:262` 的注释自认「本标量与该桶 `条目数` 是同一个量，两处必须相等」，但**只有注释在保证相等**。§四 D 已证：在「分桶基线不可读」的回落路径上，标量是**真判据输入**，改错即**漏判**（341 条违规被判绿）。建议二选一 —— ① 回落值改成从分桶基线派生；② 补一条「标量 == 分桶条目数」的一致性判据。**属 fail-closed 口径（与裁-1 同域），不自行改。**

**F2（过期文案）** `开发工具/发布门禁/运行发布门禁.py:778` epilog 仍写「裸布尔=债务冻结（存量 **1605** 条、新增即红）」，与现值 335 差 1270。该文件不在我的写集。

**F3（过期文案）** `开发工具/发布门禁/运行发布门禁_防回潮公共.py:265` 写「扫描面…共 **718** 份 .py」，判据现场报 **1003** 份（该行自带 2026-09-18 日期，属历史快照陈述）。

**F4（出口的留痕缺口）`--冻结` 会静默吸收「内容指纹变化」留痕。** 拿 `git show HEAD:开发文档/项目证据/能力id冻结基线.json`（751 条、基线时点 2026-09-23）跑门禁 ⇒ **违规 0 ／ 留痕 99（内容指纹变化）／ 新增 1**；`--冻结` 重建后这 99 条留痕归 0，**留痕只剩基线文件自身的 git diff**。根因已查清、**不是错**：该基线冻于 2026-09-23 21:26（提交 `f9992120`），其后 2026-09-24 的批G/H/M/N/O 改动了一批能力定义 ⇒ 99 条能力的内容指纹漂移（工作树里 `能力定义.json` **0 个脏文件**，漂移来自已提交的批次）。建议「显式重建基线」时把留痕单独落一份证据（哲学 13.3 第 ④ 环）。

**F5（未销账）** `开发文档/未完成事项.md:268` 的三步待办里 ①②（存量 103→0、基线同步下调到 0）**已完成**，③（收口轮 `静态编译` 类型=0 复核）未做；`:464-465` 的 P-6／P-7 仍标待修。该文件工作树被并行会话占用 ⇒ **未越域改**。

---

## 六、没做的部分与原因

1. **未改任何文件**（含 F1–F5）：全部越出 P-7 写集，或属须裁项。
2. **未跑编译 ／ 测试 ／ 发布门禁整轮**（按派活纪律，归主会话）。R5 那处**测试连带改动**（`测试中心/开发工具/测试_公开调用完整性门禁.py` 的 `test_真仓库基线覆盖全部存量`）只做**逻辑等价复跑**：`json` 已在该测试文件 `:17` 导入；四条断言（新增空 ／ 存量空 ／ 收敛空 ／ 基线条目 `{}`）逐条现场为真。
3. **R1 判据现场仍红**（**不属 P-7**）：计数 **341** ／ 基线 **335** ／ **新增 6 条** —— `公共契约/运行时/平台适配/删除树.py:143`、`平台控制面/能力目录/实现/文件租约维护.py:140`、`:141`、`开发工具/MD文档生成/查重.py:87`、`开发工具/制品新鲜度门禁.py:103`、`:108`。属**真实新增**（批O 明文「刻意不吸收，吸收即等于洗白」）。
4. **未提交 git**：六条载体 + 测试文件 + 新增 `开发工具/存量基线消费.py` 仍在工作树。

---

## 七、租约回执

- **开工ID**：`开工-20260924-095258-86dc`
- **租约id清单**：`f80f28dcba1c4203`（本 md，1 条）
- **释放数**：1（收工释放）
- 只读取证脚本落 `/tmp`（`qg_r2r5.py` ／ `qg_r1r5raw.py` ／ `qg_反向验证.py` ／ `qg_反向验证2.py` ／ `qg_1281.py` ／ `qg_留痕.py`），不落仓。
