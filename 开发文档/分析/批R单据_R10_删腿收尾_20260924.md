<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R · R-10 · 删腿后的收尾（R-7 的 U1／U2／U3／U5／U7 ＋ U4 收敛读数）

> 日期：2026-09-24 ｜ 开工ID：`开工-20260924-111830-79a3`
> 租约id清单：`abe6057bae014762` `1733f86a3f304e9e` `f30f49f0b32243d7` `edb4f90abb724a68` `0c29247cc5bf4eb0` `90791790688b43b3` `5b2bd86196114878` `7847511afe4644e9` `b756cf4917494ba2` `53803f157b914451` `0e95c71f1d2c46ab` `ecf76fc2bab048f3` `cd705afeb28d4395` `f4661985117b449c`
> 上游：`开发文档/分析/批R单据_R7_架构两条腿_20260924.md`（§五 U1–U7）＋`开发文档/方案/维修方案_两条腿与写入越界_20260923.md`
> 口径：本件是**分析类**件；条目销账后按分析类口径删除。读数均为本批现场实测（行号会漂，引用前重跑命令）
> 本批**未跑**任何编译/门禁/测试、**未 git 提交**（华哥本批硬约束）

---

## 零、结论（直达核心）

| 条 | 定性 | 动作 | 状态 |
|---|---|---|---|
| **U1** | `测试中心/开发工具/测试_能力网关.py`（整文件 8 用例）测的就是已删 40006 腿 | 整文件删（8 条断言逐条打的是**已删腿的处理器类** `能力网关请求处理器`，**不覆盖活腿** ⇒ 不许改指活腿再假装通过） | **已做** |
| **U2** | `测试中心/底座升级/测试_批次三假绿修复.py:33-38` 的 `test_能力网关拒绝4780` 直接 import 已删腿 | 删该用例；同文件另 3 例测**活腿**（`本地网关拒绝4780`／`流式网关拒绝4780`／`门禁超时…`）全部保留 | **已做** |
| **U3** | `公共契约/HTTP路由契约.json` 两条公开入口的 `实现入口`／`验证场景` 仍写已删腿 | 改活腿真值：`实现入口`＝`运行核心.统一网关.本地网关`、`验证场景`＝`测试中心/运行核心/测试_渐进能力目录.py`；附**活体调用**证据（40007 实测 200／401／404） | **已做** |
| **U5** | 活文档仍自述 40006「未启用／要用再起」 | 按 `rg -n "40006"` 找全 ⇒ 改 **7 份活文档**为「已删除」（R-7 只列了 4 份，漏 `项目说明.md`／`多会话并行开发规约.md`／`未完成事项.md`） | **已做** |
| **U7** | `能力定义.json` 删 `异步` 后的**实现侧死分支** ＋ `生成物联动.py` 陈旧注释 | `开发编译口自检.py` 删 `_启动编译` 整函数＋`异步` 形参/文档段/分支；`生成物联动.py` 注释改为现状口径 | **已做** |
| **U4** | `防回潮` 存量基线因删腿**自然收敛** | 该门禁**无 `--写基线` 出口**（批Q/批P 实测；CLI 只有 `--制品/--包目录/--跳过测试/--禁止真实进程/--测试并行数/--测试真跑`），且收敛方向本就不判红 ⇒ **不动基线数值**，只报读数（见 §五） | **已做（只读判定，零写入）** |

**改了/删了哪些路径（源码树）**

| 动作 | 路径 |
|---|---|
| 删 | `测试中心/开发工具/测试_能力网关.py` |
| 改 | `测试中心/底座升级/测试_批次三假绿修复.py` |
| 改 | `公共契约/HTTP路由契约.json` |
| 改 | `模块库/自修复工具/实现/开发编译口自检.py` |
| 改 | `开发工具/开发编译口/生成物联动.py` |
| 改 | `开发工具/开发入口.py` |
| 改 | `开发文档/规范/项目细则.md`、`开发文档/规范/批量开发流程与分级验证.md`、`开发文档/规范/网关调用最小示例.md`、`开发文档/规范/多会话并行开发规约.md` |
| 改 | `开发文档/当前流程图.md`、`开发文档/项目说明.md`、`开发文档/未完成事项.md` |
| 新增 | 本件 |

---

## 一、U1／U2 · 两个测试指向已删腿

### 1.1 定性（现场复核）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
rg -n "from 开发工具.能力网关.能力网关|测试_能力网关" --glob '!开发文档/**' --glob '!*.jsonl'
```

改前真实输出（两处）：

```
测试中心/开发工具/测试_能力网关.py:22:from 开发工具.能力网关.能力网关 import 能力网关请求处理器, 启动网关
测试中心/底座升级/测试_批次三假绿修复.py:34:        from 开发工具.能力网关.能力网关 import 启动网关
```

`测试中心/开发工具/测试_能力网关.py` 的 8 条用例全部经 `ThreadingHTTPServer(("127.0.0.1", 0), 能力网关请求处理器)` 起**已删腿自己的处理器类**，与 40007 活腿无关（活腿的处理器是 `运行核心/统一网关/传输/网关边界面.py`）⇒ 按「不保留旧腿」**整文件删**。

### 1.2 动作

| 落点 | 改前 | 改后 |
|---|---|---|
| `测试中心/开发工具/测试_能力网关.py` | 整文件 139 行（8 用例，全部 import 已删腿） | **文件删除**（经 MCP `文件管理.删除文件`） |
| `测试中心/底座升级/测试_批次三假绿修复.py:33-38` | `def test_能力网关拒绝4780(self)`（6 行，直接 import 已删腿的 `启动网关`） | 该用例删除；其余 4 例保留（`test_4780是应用保留端口`／`test_本地网关拒绝4780`／`test_流式网关拒绝4780`／`test_门禁超时返回失败并不留下子进程`） |
| `开发工具/开发入口.py:78-79` | 注释以已删测试为证据：`# \`测试中心/开发工具/测试_能力网关.py::test_查看不存在的能力返回错误\` 判红。` | 改为事实陈述（不再引用已删文件）：「2026-09-23 实测（当时承载该路由的 40006 腿已于 2026-09-24 批R·R-7 删除；本翻译按契约对任何调用方仍一律生效）」 |

### 1.3 改后读数（可复制）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
ls 测试中心/开发工具/测试_能力网关.py            # 期望：No such file or directory
rg -n "def test" 测试中心/底座升级/测试_批次三假绿修复.py
rg -n "开发工具.能力网关.能力网关" --glob '!开发文档/**' --glob '!*.jsonl'   # 期望：0 命中
python3.14 -c "import importlib; importlib.import_module('测试中心.底座升级.测试_批次三假绿修复'); print('导入通过')"
```

真实输出：

```
ls: 测试中心/开发工具/测试_能力网关.py: No such file or directory
10:    def test_4780是应用保留端口(self):
18:    def test_本地网关拒绝4780(self):
26:    def test_流式网关拒绝4780(self):
33:    def test_门禁超时返回失败并不留下子进程(self):
（rg 已删腿 import：0 命中）
导入通过
```

⇒ 改前那两条 `ModuleNotFoundError: No module named '开发工具.能力网关.能力网关'` 的入口**整条消失**（不是改指活腿）。

---

## 二、U3 · `HTTP路由契约.json` 两条公开入口改活腿真值

### 2.1 改前／改后

| 路径 | 字段 | 改前 | 改后 |
|---|---|---|---|
| `/能力/搜索` | `实现入口` | `开发工具.能力网关.能力网关` | `运行核心.统一网关.本地网关` |
| `/能力/搜索` | `验证场景` | `测试中心/开发工具/测试_能力网关.py` | `测试中心/运行核心/测试_渐进能力目录.py` |
| `/能力/契约/{能力id}` | `实现入口` | `开发工具.能力网关.能力网关` | `运行核心.统一网关.本地网关` |
| `/能力/契约/{能力id}` | `验证场景` | `测试中心/开发工具/测试_能力网关.py` | `测试中心/运行核心/测试_渐进能力目录.py` |

- `实现入口` 取 `运行核心.统一网关.本地网关`：与同文件另两条 HTTP 入口（`唯一执行入口`、`/健康`）**同口径** —— HTTP 边界归 `本地网关`，路由分支在 `运行核心/统一网关/传输/网关边界面.py:353`（`/能力/搜索`）与 `:398`（`/能力/契约/`）。
- `验证场景` 取 `测试中心/运行核心/测试_渐进能力目录.py`：它是**活腿**的真实 HTTP 测试（自己起 `本地网关服务器`，跑 `能力搜索`/`能力详情` 两个操作）。
- **覆盖缺口如实登记**：`GET` 壳本身（查询参数解析、状态码映射）**没有专测**（原专测随 40006 腿删除；新写测试属本批写集外且「不跑编译」无法验证）⇒ 已登记为待补项（见 §七）。

### 2.2 改后能被**活体调用**验证（40007 实测，本批现场）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
# ① 无凭证：fail-closed
curl -s -m 8 -o /dev/null -w '状态码=%{http_code}\n' \
  'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E6%90%9C%E7%B4%A2?%E5%85%B3%E9%94%AE%E8%AF%8D=%E8%AF%BB%E5%8F%96%E6%96%87%E4%BB%B6&%E9%99%90%E5%88%B6=1'
# ② 带凭证：同一路由 200
curl -s -m 8 -H 'X-System-Credential: html-blackbox-verifier' -o /tmp/r10_2.json -w '状态码=%{http_code}\n' \
  'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E6%90%9C%E7%B4%A2?%E5%85%B3%E9%94%AE%E8%AF%8D=%E8%AF%BB%E5%8F%96%E6%96%87%E4%BB%B6&%E9%99%90%E5%88%B6=1'
python3.14 -c "import json;d=json.load(open('/tmp/r10_2.json'));print('操作=',d['操作'],'成功=',d['成功'],'值条数=',len(d['值']))"
# ③ 契约路由（带凭证）
curl -s -m 8 -H 'X-System-Credential: html-blackbox-verifier' -o /tmp/r10_3.json -w '状态码=%{http_code}\n' \
  'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E5%A5%91%E7%BA%A6/%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F%E6%94%AF%E6%8C%81%E5%BA%93.%E6%96%87%E4%BB%B6%E6%93%8D%E4%BD%9C.%E8%AF%BB%E5%8F%96%E6%96%87%E4%BB%B6'
# ④ 不存在的能力：404 + 能力不存在（不是 500）
curl -s -m 8 -H 'X-System-Credential: html-blackbox-verifier' -o /tmp/r10_4.json -w '状态码=%{http_code}\n' \
  'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E5%A5%91%E7%BA%A6/%E4%B8%8D%E5%AD%98%E5%9C%A8.%E8%83%BD%E5%8A%9B'
# ⑤ 已删腿：连接失败 + 无监听
curl -s -m 3 -o /dev/null -w '40006 状态码=%{http_code} 退出码=%{exitcode}\n' http://127.0.0.1:40006/能力/搜索
lsof -nP -iTCP:40006 -sTCP:LISTEN || echo '(40006 无监听)'
```

真实输出：

```
① 状态码=401   {"操作": "HTTP边界", "成功": false, "错误码": "权限不足", "错误说明": "访问凭证缺失或无效"}
② 状态码=200   操作= 能力搜索 成功= True 值条数= 1
③ 状态码=200   操作= 能力详情 成功= True 能力id= 文件系统支持库.文件操作.读取文件
④ 状态码=404   {"操作": "能力详情", "成功": false, "错误码": "能力不存在", "错误说明": "请求的能力不存在"}
⑤ 40006 状态码=000 退出码=7 ；(40006 无监听)
```

⇒ 改后两条公开入口**确实由 40007 活腿承载**（200），无凭证 fail-closed（401），不存在的能力按契约回 404（不是 500）。

---

## 三、U5 · 自述 40006 的活文档（找全 7 份，不只 4 份）

### 3.1 找全（改前读数）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
rg -l "40006" | sort
rg "40006" --stats | grep -E "matches|matched lines|files contained"
```

改前（R-7 删腿后、本批动手前）：**87 处 / 52 行 / 18 文件**；其中**活文档 7 份**自述 40006（R-7 §五 U5 只列了 4 份）：

| # | 文件 | 改前自述 | 改后 |
|---|---|---|---|
| 1 | `开发文档/规范/项目细则.md`（:22／:96／:215） | 「40006 当前**未启用**…现役脚本名为 `启动能力网关.sh`」（脚本已删） | 「已于 2026-09-24 批R·R-7 **整条腿删除**…同一路由只剩 40007 一处实现」 |
| 2 | `开发文档/规范/批量开发流程与分级验证.md`（:32） | 「40006 **当前未启用**（无监听，launchd 退出码 2）」 | 「**已于 2026-09-24 批R·R-7 整条腿删除**（源码两文件已删）」 |
| 3 | `开发文档/规范/网关调用最小示例.md`（:226-228） | 「只读能力发现网关（40006，**可选**）…要用再起」 | 「只读能力发现网关（40006）**已删除**…只剩 40007 一处实现」 |
| 4 | `开发文档/当前流程图.md`（:130／:152／:383） | 「40006 只读网关当前**未启用**（均无监听）」 | 「已于 2026-09-24 批R·R-7 整条腿删除（源码两文件已删、无监听）」 |
| 5 | `开发文档/项目说明.md`（:49／:553） | 行标题「只读能力发现网关（**未启用**）」＋取证命令 `ls 开发工具/能力网关/`（实测 `启动能力网关.sh`、`能力网关.py`） | 行标题改「（**已删除**）」＋取证命令改 `ls 开发工具/能力网关/`（只剩 `重启网关.py`）／`rg -n '40006' 开发工具/能力网关/`（0 命中） |
| 6 | `开发文档/规范/多会话并行开发规约.md`（:19／§0.4／:148／:319） | §0.4 标题「当前**未启用**」＋给出 `./开发工具/能力网关/启动能力网关.sh [端口] [地址]` 启动法 | §0.4 标题改「**已于 2026-09-24 删除**」，删启动法，加「**不要再按旧版找脚本或起 40006**」；:148／:319 两处资源表改「已删除」 |
| 7 | `开发文档/未完成事项.md`（:233） | §四债务行标题「40006 停用腿未删」 | 行标题加「**→ ★已做（2026-09-24 批R·R-7 删腿；批R·R-10 收尾引用面）**」，处置列补**验收读数**（三判据 ✓）＋指向本件 |

`开发文档/未完成事项.md` 是**生成器件**（账目块「机器生成，禁止手改」）：本次只改**人工区**那一行（人工区＝账目输入源），账目块**未动** ⇒ `python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验` 仍回「生成区与正文实数一致」（实测退出码 0），条数不变。

### 3.2 改后读数

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
rg "40006" --stats | grep -E "matches|matched lines|files contained"
rg -c "40006" | sort
# 关键判据：还自述「40006 可用／未启用」的处数 = 0（活文档 7 份现在一律写「已删除」）
rg -n "40006.*(未启用|可选|要用再起|现役脚本)" --glob '!开发文档/归档/**' --glob '!开发文档/分析/**' --glob '!开发文档/方案/**' --glob '!*.jsonl'
```

真实输出：

```
改前：87 matches ｜ 52 matched lines ｜ 18 files contained matches
改后：85 matches ｜ 53 matched lines ｜ 19 files contained matches
（分文件：开发工具/开发入口.py 1；开发文档/当前流程图.md 3；开发文档/未完成事项.md 1；
 开发文档/规范/多会话并行开发规约.md 6；开发文档/规范/批量开发流程与分级验证.md 1；
 开发文档/规范/网关调用最小示例.md 2；开发文档/规范/项目细则.md 3；开发文档/项目说明.md 2；
 其余为 归档／分析／方案／交付记录／项目证据 历史件）
关键判据（还自述 40006 可用/未启用）：**0 处现状自述**（唯一命中是 `开发文档/未完成事项.md:233`
 里**引述改前原文**的那一句「自述 40006「**当前未启用**，执行仍走 40007」」—— 属历史取证，
 不是现状自述；除它以外 0 命中）
```

**两处计数上升要如实说明（不是回潮）**：

- **文件 +1**＝`开发工具/开发入口.py` 新增 1 处**事实性说明**（原注释引用已删测试，现改述「当时承载该路由的 40006 腿已于 2026-09-24 删除」）—— 是**追述删除**，不是把 40006 说成可用。
- **行 +1**＝`开发文档/规范/网关调用最小示例.md` 把一段拆成两段（多 1 行）；`开发文档/规范/多会话并行开发规约.md` §0.4 反而由 4 行收成 3 行（-1 行）。
- **处数 -2**（87→85）：删掉的是「现役脚本／要用再起」这类**误导性表述**。
- **本件自身的足迹（如实计入）**：本件（记录删腿这件事）另含 **36 处 / 30 行** `40006` ⇒ 「全仓含本件」读数 = **121 处 / 83 行 / 20 文件**；剔除本件后即上表 **85 处 / 53 行 / 19 文件**（可复跑：`rg -c "40006" 开发文档/分析/批R单据_R10_删腿收尾_20260924.md` → 30）。

---

## 四、U7 · T7 实现侧死分支与陈旧注释

### 4.1 `模块库/自修复工具/实现/开发编译口自检.py`（只为 `异步` 存在的分支）

| 落点 | 改前 | 改后 |
|---|---|---|
| `:422-453` | 整个 `def _启动编译(命令, 根, 超时秒) -> 结果:`（32 行，只为 `异步=真` 档存在） | **整函数删除** |
| `:456-458` 形参 | `… 超时秒: float = 600.0, 源码根: str = "", 异步: bool = 假) -> 结果:` | `… 超时秒: float = 600.0, 源码根: str = "") -> 结果:` |
| `:469-478` 文档段 | 「**异步=真**（2026-09-23 新增）：…异步档才能一发起编译…」＋「异步=真 时返回 {异步, 句柄, PID, 命令, 项目根, 下一步}」 | 收成一句同口径说明：「**长任务不自带异步形态**：一律走平台任务腿（任务提交/任务查询），与 `能力定义.json` 的 `运行开发编译口.说明` 同口径（原 `异步` 参数已按 2026-09-23 裁决删除）」 |
| `:490-492` 分支 | `if 异步 is 真:` ＋注释 ＋ `return _启动编译(命令, 根, float(超时秒))` | **删除**（`命令 = _组命令(...)` 之后直接进同步档 `调用 = _调用支持库("系统核心支持库.进程管理.执行命令", …)`） |

**零消费核对（改前实测）**：全仓 `_启动编译` 只有 3 处命中（定义 1 ＋ 文档引用 1 ＋ 分支调用 1，全在本文件）⇒ 删函数不留悬空引用；全仓无任何测试断言 `运行开发编译口` 的 `异步` 参数（`rg -n "异步" 测试中心/` 命中项全是别的主题：同步腿异步强制／进程管理异步腿／薄壳工具面）。

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
rg -n "_启动编译|异步" 模块库/自修复工具/实现/开发编译口自检.py   # 期望：0 命中
python3.14 -c "import ast; ast.parse(open('模块库/自修复工具/实现/开发编译口自检.py',encoding='utf-8').read()); print('AST 通过')"
```

真实输出：

```
（rg：0 命中）
AST 通过
```

### 4.2 `开发工具/开发编译口/生成物联动.py:115-119` 陈旧注释

| 项 | 改前 | 改后 |
|---|---|---|
| 口径 | 「`启动进程` / `沙箱执行命令` 的 `环境变量` 入参是**整体替换**语义（实测 `Popen(env=…)` 不合并），谁显式传了它谁就把符号丢了」 | 「两条腿的 `环境变量` 入参**都是合并**（`启动进程`＝在当前进程环境上逐键覆盖；`沙箱执行命令`＝在沙箱白名单上合并；口径 2026-09-24 批R·R-7 统一）」 |
| 残余脆弱点 | 「将来新增调用点若传 `环境变量`，**必须显式带上识别符号**」 | 「显式传 `环境变量` 不再整体丢掉继承来的识别符号，**残余窗口只剩「调用点显式传同名键把它覆盖掉」这一种**…必须保证识别符号**不被覆盖**」 |

（该注释是「MCP 识别符号靠继承传导」的脆弱点登记；口径改后脆弱点**只剩一半**，如注释所述。）

---

## 五、U4 · `防回潮` 存量基线的自然收敛读数

### 5.1 门禁自己的出口是什么（先查清，不手改数值）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
rg -n "add_argument|ArgumentParser" 开发工具/发布门禁/运行发布门禁.py   # 只有 --制品/--包目录/--跳过测试/--禁止真实进程/--测试并行数/--测试真跑
rg -n "防回潮存量基线" --glob '!开发文档/**' -l
```

⇒ 该门禁**没有 `--写基线` 出口**（标量是 `运行发布门禁_防回潮公共.py` 的模块级常量；分桶基线 `防回潮存量基线.json` 由**判据自己的扫描逻辑**按文件分组生成）。本项自己写明的规则是「存量下降时应下调基线」，但**下调动作没有机器出口**；而判据对「基线内有、本次未出现」**一律不判红**（收敛是正向信号）⇒ 本批**不动该 json 的任何一个数字**，只报读数。

### 5.2 收敛前后条数（本批现场实测，只读探针）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
python3.14 - <<'EOF'
import sys; sys.path.insert(0, '.')
from 开发工具.发布门禁 import 运行发布门禁_防回潮判据 as 判据
from 开发工具.发布门禁.运行发布门禁_防回潮公共 import 防回潮存量基线路径, _读分桶基线
from 开发工具.发布门禁.运行发布门禁_底座 import 系统根
捕获 = {}
原 = 判据._按文件分组
def 包装(命中, 显示根):
    捕获['命中'] = list(命中); 捕获['分组'] = 原(命中, 显示根); return 捕获['分组']
判据._按文件分组 = 包装
通过, 详情 = 判据.校验裸布尔防回潮(系统根)
分桶 = _读分桶基线(防回潮存量基线路径, "裸布尔") or {}
分组 = 捕获['分组']
收敛键 = sorted(k for k in 分桶 if k not in 分组)
print('通过=', 通过, '| 现场计数=', len(捕获['命中']))
print('基线键数=', len(分桶), '基线条目合计=', sum(分桶.values()))
print('整体收敛键=', 收敛键, '收敛条数=', sum(分桶[k] for k in 收敛键))
print('基线外新出现文件=', sorted(k for k in 分组 if k not in 分桶))
EOF
```

真实输出：

```
通过= False | 现场计数= 334
基线键数= 77 基线条目合计= 335
整体收敛键= ['开发工具/能力网关/能力网关.py'] 收敛条数= 9
基线外新出现文件= ['公共契约/运行时/平台适配/删除树.py', '平台控制面/能力目录/实现/文件租约维护.py',
 '开发工具/MD文档生成/查重.py', '开发工具/制品新鲜度门禁.py', '开发工具/开发编译口/生成物联动.py',
 '支持库/适配层/reportlab提供者/实现/子进程入口.py']
（判据详情原文：扫描文件 1004；本次计数 334 条｜存量基线 335 条｜按文件分桶判定；**新增 9 条**）
```

**收敛前后条数**：

| 项 | 收敛前（基线） | 收敛后（现场） | 差 |
|---|---|---|---|
| 裸布尔桶 · 键数 | 77 | 76（77 − 1 个整体收敛键） | **−1 键** |
| 裸布尔桶 · 条数 | 335 | 334 | **−1 条** |
| 其中「基线内已收敛」 | — | **1 键 / 9 条**（`开发工具/能力网关/能力网关.py`，删腿直接贡献） | **−9 条** |
| 其中「文件内部分收敛」 | — | 1 键（`支持库/适配层/Git提供者/实现/Git提供者.py` 2 → 1，与删腿无关） | −1 条 |
| 基线外新出现 | — | 6 文件 / 9 条（`删除树.py` 1、`文件租约维护.py` 2、`查重.py` 1、`制品新鲜度门禁.py` 2、`生成物联动.py` 1、`reportlab提供者/子进程入口.py` 2） | +9 条 |

**判红归因（关键）**：本次 `通过=False` **不是删腿造成的** —— 删腿方向是**收敛**（−9 条）；判红来自 9 条**基线外新出现**（基线自己注明「刻意不吸收 2026-09-23/24 新增提交带入的条目，保留分辨力、待修」，与 R-10 写集零交集）。⇒ 按「不许手改基线数值」：**基线文件本次零写入**（`git status` 该文件无本批改动）。

---

## 六、本批写入面与自检

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
git status --short -- 公共契约/HTTP路由契约.json 测试中心/底座升级/测试_批次三假绿修复.py \
  测试中心/开发工具/测试_能力网关.py 模块库/自修复工具/实现/开发编译口自检.py \
  开发工具/开发编译口/生成物联动.py 开发工具/开发入口.py 开发文档/规范/项目细则.md \
  开发文档/规范/批量开发流程与分级验证.md 开发文档/规范/网关调用最小示例.md \
  开发文档/当前流程图.md 开发文档/项目说明.md 开发文档/规范/多会话并行开发规约.md 开发文档/未完成事项.md
python3.14 -c "import json; json.load(open('公共契约/HTTP路由契约.json',encoding='utf-8')); print('JSON 合法')"
```

真实输出（13 条＝12 改 ＋ 1 删）：

```
 M 公共契约/HTTP路由契约.json
 M 开发工具/开发入口.py
 M 开发工具/开发编译口/生成物联动.py
 M 开发文档/当前流程图.md
 M 开发文档/未完成事项.md
 M 开发文档/规范/多会话并行开发规约.md
 M 开发文档/规范/批量开发流程与分级验证.md
 M 开发文档/规范/网关调用最小示例.md
 M 开发文档/规范/项目细则.md
 M 开发文档/项目说明.md
 M 模块库/自修复工具/实现/开发编译口自检.py
 M 测试中心/底座升级/测试_批次三假绿修复.py
 D 测试中心/开发工具/测试_能力网关.py
JSON 合法
```

`开发文档/当前流程图.md:152` 的 ASCII 框**改前就已不齐**（框线 64 列、内行 66–71 列，历史遗留）；本次替换**逐字符等宽**（「均无监听」→「均已删腿」，4 个全角字→4 个全角字），未引入新的错位，也未顺手改无关格式。

---

## 七、未做项与登记（如实边界）

| # | 项 | 为什么没做 | 建议出口 |
|---|---|---|---|
| 1 | **派生件未重生成**（R-7 U6 的余项） | 华哥本批硬约束「不跑编译」 | `capability_call(能力id='自修复工具.静态编译', 参数={'类型':1,'模块ID':'模块库/自修复工具'})`；受影响的派生件：`模块库/自修复工具/能力契约/参数契约.json`、`能力数据/能力搜索数据.json`、`能力数据/Agent查询数据.json`、`说明/使用说明.md`、`包声明.json`、`__init__.py`、`完整性摘要.json`（T7 删 `异步` 的派生面） |
| 2 | **`GET /能力/搜索`／`/能力/契约/{能力id}` 的 GET 壳无专测** | 原专测随 40006 腿删除；新写测试属写集外，且「不跑编译」无法验证 | 在 `测试中心/运行核心/` 补一例活腿 GET 路由测试（参数解析 + 200/401/404 状态码），再把它写进 `HTTP路由契约.json` 的 `验证场景` |
| 3 | **防回潮基线未下调** | 该门禁**无 `--写基线` 出口**（见 §五 5.1） | 随下次基线维护轮（重建 `裸布尔` 桶，顺带处置 9 条基线外新增）；本批只报读数 |
| 4 | **`开发工具/开发入口.py` 的 `搜索能力`/`查看契约` 可能已无活调用方** | 全仓 `rg` 未见非测试 import（原唯一调用方是已删的 40006 腿）；但它是 `测试中心/开发工具/测试_契约编译搜索.py` 的被测对象 ⇒ 删模块属**越本单写集** | **待核**：确认无活调用方后按「不保留旧腿」整函数/整模块处置（须另一单，含测试改写） |
| 5 | **制品树仍是旧构建**（R-7 U8） | 本批不跑上线 | `工程缓存/制品仓库/平台客户端环境/平台客户端/平台客户端/开发工具/能力网关/能力网关.py` 仍在 ⇒ 随下次上线链重建自然消失 |
| 6 | **孤儿 `.pyc` 缓存** | 无源码的 `__pycache__` 不可导入（Python 只在源文件存在时使用它），且路径在写集外 | `测试中心/开发工具/__pycache__/测试_能力网关.cpython-314.pyc`、`开发工具/能力网关/__pycache__/能力网关.cpython-314.pyc` —— 随下次编译/清缓存消失 |
| 7 | **归档／分析／方案／交付记录／项目证据里的 40006** | 它们是**历史留痕**（记录当时状态，不是现状自述） | 不动。清单：`开发文档/归档/**`（2 份）、`开发文档/分析/审计_两条腿与冗余_20260923.md` 等 5 份分析件、`开发文档/方案/**`（3 份）、`开发文档/交付记录/20260915_收口轮/交付报告.md`、`开发文档/项目证据/MCP使用反馈.jsonl` |
| 8 | **`README.md` 里的 `POST /能力/搜索` 示例** | 与本单 U3 无关（那是「直接打网关」的另一句示例，未指向已删腿） | 随 README 口径轮复核 |

---

## 八、附：本批全部读数汇总（可复制）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台

# U1/U2
ls 测试中心/开发工具/测试_能力网关.py                      # No such file or directory
rg -n "开发工具.能力网关.能力网关" --glob '!开发文档/**' --glob '!*.jsonl'   # 0 命中
rg -n "def test" 测试中心/底座升级/测试_批次三假绿修复.py    # 4 例，全为活腿

# U3（活体）
curl -s -m 8 -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E6%90%9C%E7%B4%A2'                 # 401
curl -s -m 8 -H 'X-System-Credential: html-blackbox-verifier' -o /dev/null -w '%{http_code}\n' \
  'http://127.0.0.1:40007/%E8%83%BD%E5%8A%9B/%E6%90%9C%E7%B4%A2?%E5%85%B3%E9%94%AE%E8%AF%8D=%E8%AF%BB%E5%8F%96%E6%96%87%E4%BB%B6'   # 200

# U5
rg "40006" --stats | grep -E "matches|matched lines|files contained"
rg -n "40006.*(未启用|可选|要用再起|现役脚本)" --glob '!开发文档/归档/**' --glob '!开发文档/分析/**' --glob '!开发文档/方案/**' --glob '!*.jsonl'
# 唯一命中 = 未完成事项.md:233 引述改前原文那一句（历史取证）；除它以外 0 命中

# U7
rg -n "_启动编译|异步" 模块库/自修复工具/实现/开发编译口自检.py   # 0 命中
python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验            # 生成区与正文实数一致

# U4（只读判定）
见 §五 5.2 的探针脚本；基线文件本批零写入。
```

本批**未跑**任何编译/门禁/测试、**未 git 提交**；上述读数均为只读探针、活体 HTTP 调用与 MCP 写腿回执。
