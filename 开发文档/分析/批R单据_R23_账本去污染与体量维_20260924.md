<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-23 单据：账本去污染 ＋ 返回体量维 ＋ 常用意图扩映射

> 开工ID `开工-20260924-124626-2185`｜租约 6 条（`公共契约/能力契约/常用意图.py`、`公共契约/诊断/调用账本.py`、`运行核心/运行诊断/安全审计/安全审计.py`、`运行核心/统一网关/核心处理/网关处理面.py`、`测试中心/第一批维修/测试_能力唯一事实源.py`、本单据）
> 提交 `b9eeaea3`（26 路径 → 24 文件）｜制品 `188db769d66119dc`（已安装激活）｜验收时 HEAD `777147f6`
> 口径：本件读数均为**现场实测**；「改前」＝网关跑 `23ec6a2c` 制品时的读数，「改后」＝重启后跑 `188db769d66119dc` 的读数

## 一、改前/改后总览

| # | 项 | 改前 | 改后 | 判据 |
|---|---|---|---|---|
| 1 | `常用参数组合(办公文档支持库.文档生成.文档统计)` 第 1 组 | `__事实源探针__:逻辑型`（32 次），真实用法 2 次被压到第 2/3 位；组合数 3 | `文件路径:文本型`（2 次）；哨兵**已消失**；组合数 **2** | 华哥「参数名一定要对齐」＋ R-22 头号缺陷 |
| 2 | 审计账本 `返回字符数` | 字段**不存在**（体量维无落点） | `安全审计.jsonl` 尾三行（2127–2129）均含 `返回字符数` | R-22 第 2 条 |
| 3 | 0 跳注入面「常见意图→首选能力」 | 4 条（读文件/内容检索/跑命令/找能力） | **8 条**（＋看Git状态/看当前提交/切分支或建工作区/看系统信息） | R-22 第 3 条 |
| 4 | `开工准备` 默认档 | 无 `资料` 键（须显式传 `资料级别`） | **默认就带** `资料`（指针档 1,312 字符 / 预算 8,000；整包 9,632 字符） | 华哥「1 次运行拿到开工所有资料」 |

## 二、账本去污染（主项）

**根因（写侧，不是读侧）**：`测试中心/第一批维修/测试_能力唯一事实源.py` 起**进程内测试网关**（`创建测试服务器` ＋ `网关核心(后端)`），而该网关的调用参数账走**生产运行数据根** ⇒ 逐能力探针的 `{"__事实源探针__": true}` 被逐条记进生产账。R-22 实测：**752 条 / 23,914 次 = 账本总量 51.8%**。

**修法（单节点：写口与读口同一处）** —— 判据只在 `公共契约/诊断/调用账本.py` 定义一次：

| 落点 | 判据 | 行为 |
|---|---|---|
| `是保留键名(键)` | 键名形如 `__xxx__`（平台/测试保留命名空间，不是业务参数） | **唯一判据**，写口与读口共用 |
| 写口 `记一次调用` | `含保留键名(参数)` | 该次**不入账**（返回 假，不抛） |
| 读口 `查常用参数` | `形状含保留键名(行.入参形状)` | 该形状**不计入**（`组合数` 与 `常用参数组合` 同口径）；**存量行不删** |
| 读窗口 | `读行上限 = max(200, 上限*20)` | 先过滤再取前 N —— 否则前 N 名被哨兵占满时会误报「一组都没有」 |

**测试侧（保住测试意图，不删测试）**：`测试_能力唯一事实源.py` 的 `setUpClass`/`tearDownClass` 把账改道到临时文件 —— 用**唯一节点已支持**的环境变量 `系统底座_调用参数账`（常量 `参数账环境变量`），只隔离这一本账，其余运行数据（索引/制品/审计）路径一个字不动。**理由**：不改探针（它要探每个能力是否可调），只让「测试产出不写生产账」；不新起第二套隔离机制。

**存量 23,914 条处置结论：留（不清库）＋ 读侧过滤**。理由：① 这是**审计账**，删历史＝破坏可追溯；② 读侧过滤后对任何结论都无影响（已实测：第 1 组不再是哨兵）；③ 写侧已封住新增。⇒ 「清」是破坏性动作、「标记」要加列改表结构，两者收益为零。

## 三、返回体量维

**落点＝既有唯一审计节点**（`运行核心/运行诊断/安全审计`，不新起第二个审计文件）：

- `安全审计.记录()` 新增形参 `返回字符数: int = 0`，落进同一条记录（**只记字符数，一个字正文都不进账本** —— 否则审计文件自己就爆、只读证据里躺业务数据）。
- 唯一产生点在网关：`网关处理面` 在 `响应.耗时毫秒` 旁算一次 `返回字符数 = len(json.dumps(响应.值, ensure_ascii=False, default=str))`（与薄壳 `返回上限字符` 同源口径；算不出来按 0，绝不反噬主调用），随同一条 `审计.记录(...)` 写入。
- 读口：新增 `安全审计.返回体量报告(最大记录数/上限/大返回字符数)`，与 `慢调用报告` **同源、同形制**（只派生、不另存第二份，哲学 1.2）——回 `阈值字符数 / 读取记录数 / 大返回条数 / 按能力聚合 / 最大明细`。

## 四、常用意图扩映射

只改**唯一事实源** `公共契约/能力契约/常用意图.py`（`工具清单.py:90` 的描述与 `开工准备.资料.能力映射` 都从它渲染，禁手抄第二份）：

| 意图 | 首选能力 | 限定 |
|---|---|---|
| 看Git状态 | `版本控制支持库.Git操作.当前状态` | 别在终端跑 git status/log：回 {分支, 未提交修改, 最近提交} |
| 看当前提交 | `版本控制支持库.Git操作.获取当前提交哈希` | 别在终端跑 git rev-parse；同包 提交/推送/回滚 都在能力面 |
| 切分支或建工作区 | `版本控制支持库.Git操作.切换分支` | 别在终端跑 git worktree：同包 创建工作区/查询工作区/合并分支 |
| 看系统信息 | `系统核心支持库.系统信息.获取CPU信息` | 别在终端跑 top/df：同包 获取内存信息/获取操作系统信息/获取磁盘信息/获取进程列表 |

**实测**：`开工准备` 的 `资料.能力映射` 回 **8 条**（原文即上表）。

## 五、验证与门禁读数

- `自修复工具.静态编译`（`类型=0` 全仓）**两次**：均 **成功 18 / 失败 2 / 检查项 20 / 五态=只报告 / 退出码 0**，**`失败门禁=[]`（无阻断）**。两条只报红＝`md查重`（`模块库/文档生成/说明/目录树与命令.md`，旧账）＋`制品新鲜度`（当时制品落后 HEAD，重建后消）。
- `自修复工具.仓库锁`：提交被钩子拦（真缺口 1 条＝`.`）⇒ `动作=补齐`（**新锁 1 / 跳过开窗 34 / 锁掉开窗 0**）⇒ 提交通过。
- 制品：`自修复工具.构建平台客户端制品(安装=真)` ⇒ `188db769d66119dc`，写盘后自校验通过、可重复构建校验通过、已入库安装、稳定路径可读。
- 网关重启：`开发工具/能力网关/重启网关.py`（经 MCP `执行命令`）；**生效证据**＝改后 `常用参数组合` 第 1 组变真实用法（旧制品下不可能）。

## 六、未做项与须确认项

1. **`开工准备.资料.租约查询问题` 非空**（实测：`租约腿查不出来：（不是「没人占」，别当放行）`）。未归因到具体行、**未修** —— 它影响「涉及路径占用状态」这一项的可信度（现为 fail-closed，不当放行，方向安全）。
2. **体量维没有能力面读口**：`返回体量报告` 与既有 `慢调用报告` 一样是**进程内方法**（平台今天就没给 `慢调用报告` 开能力面），故「返回体量 top N」现在**只能由平台内部调用**，agent 侧取不到。要开能力面＝新增公开能力（须走包声明/能力定义/镜像/编译），本批未做。
3. **`常用传参` 仍混入薄壳级 `项目根`**（`未完成事项` 老账：`常用传参` 与 `能力详情` 参数表不一致）。本批的保留键名规则**不覆盖**它（`项目根` 不是保留键名）——仍须按该条既定修法「读口按当前契约参数名过滤」收口。
4. **0 跳工具描述的改动要等 MCP 重连才可见**：薄壳进程在会话启动时就载入了旧描述，本会话看到的 `capability_call` 描述仍是 4 条映射；`资料.能力映射`（网关侧）已是 8 条。
5. **未跑定向测试**：本批未调 `动态编译`，故 R-19 的阻断项只有 `静态编译` 级证据（契约/合规/摘要全绿），**没有测试级证据**。
6. **`未完成事项.md` 未更新**：该文件在验收期被 `开工-20260924-123755-e994`（R-21 会话）持活跃租约（`开工即占` 被拒，冲突路径 1 条），本批**不抢租约**，故本次结论只落本单据 ＋ 审计总账第十节。
7. **同工作区仍有并发写者**：`开发文档/未完成事项.md`（12:51:13）与 `模块库/开工编排/**`＋`开发工具/薄壳/薄壳服务.py`（12:49:52 起 13 文件）属 R-21 会话在写，本批**未提交**它们（只提交自己的 5 个文件 `777147f6`）。
