# 依赖分层与归属审计（第一轮 · 只读）

> 最后更新：2026-09-19
> 口径：实测（数字随提交逐日变化，引用前须重跑本文档内取证命令）
> 维护者：依赖分层归属审计（第一轮，只读）

**项目根**：`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`　**分支**：主干　**HEAD**：`836e9066`　**工作树**：`git status --porcelain` → 0 项未提交

**命令环境**：`export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH`；解释器 `/opt/homebrew/opt/python@3.14/bin/python3.14`（3.14.4）

**纪律**：全程只读——未改任何仓库文件（本报告为唯一新增件）、未跑测试、未重启服务、未装依赖、未 `git add/commit`。

---

## 一、结论摘要（分级）

> 分级口径：**S0** = 违反《项目说明》第 2 条硬条款且当前无任何机器判据兜住；**S1** = 违反条款但已有机器判据存在、只是未接线/漏覆盖；**S2** = 声明不完整、现场可证但不影响本地运行；**S3** = 判据缺口（当前零实例，未来可被绕过）。

| 编号 | 级别 | 一句话 | 条款 | 已有机器覆盖 |
|---|---|---|---|---|
| **S0-1** | S0 | `psycopg提供者` 连的是 PostgreSQL 服务（独立进程·默认 `127.0.0.1:5432`），但全仓**没有任何一处**把 PostgreSQL 登记为环境依赖项；端口写死在实现代码里，不是声明 | 2.2②、2.3 | ❌ 无任何判据扫「服务型环境依赖」 |
| **S0-2** | S0 | `密钥提供者`（macOS 钥匙串 `security`）虽被依赖登记标为环境依赖，却被依赖生命周期审计**判为「非标准提供者」跳过**——真实外部命令依赖 0 机器覆盖 | 2.2、2.3 | ⚠️ 有判据，但覆盖判据把它排除了 |
| **S0-3** | S0 | `支持库/适配层/模型服务.py`、`系统探针.py` 直接 `import` 第三方（fastapi/torch/transformers/uvicorn、psutil）却**无依赖锁**、无包声明、不进任何审计 | 2.2①、2.3 | ⚠️ 依赖防火墙放行（根下适配文件豁免），无「该建锁」判据 |
| **S1-1** | S1 | `模块库/直播逐字稿/实现/全自动精校.py` 自己 `open()` 读文件（原子旁路），同文件 `_读已有裁决` 判「重复能力」——模块侧做了「文件句柄」这类底层机制 | 2.1 | ✅ **已接入**发布门禁且基线 0 → 当前会判红 |
| **S1-2** | S1 | `依赖登记.json` / `说明/依赖分两段.md`（2.3 的六要素声明本体）**未被任何门禁调用**；实测 `--校验` 已 4 项红、无人看见 | 2.3 | ❌ 判据在、门禁不调（13.1 纸面规则） |
| **S2-1** | S2 | `代码地图提供者`（codegraph 1.5.0 + Node.js 24.15.0）与 `浏览器自动化提供者`（browser-use 0.1.13）不在 `环境自检` 外部命令表 → 换机器时「node 不在 PATH」这类坑**探不到** | 2.2 健康探针 | ⚠️ 锁里写清了说明，环境自检探不到 |
| **S2-2** | S2 | 环境依赖层三项缺声明：`密钥提供者` 缺 版本/健康探针/降级语义；`本地进程适配器` 缺同样三项；`代码地图提供者` 缺 `生命周期契约.json` | 2.3 六要素 | ⚠️ 依赖登记如实标了「需复核」，但无人收口 |
| **S2-3** | S2 | 依赖登记覆盖落后现场 3 项（`代码地图提供者`、`提供者配置读取`、`硬件画像探针.py` 现场有、登记无；`配置读取` 登记有、现场已改名） | 2.3 | ⚠️ `--校验` 能报，未接线 |
| **S2-4** | S2 | `生成依赖分两段.py` **仓内仓外各一份、字节完全相同**，而登记文件里的 `生成器` 字段仍指向**仓外**路径 | 2.4① 不许新开第二条腿 | ❌ 无判据 |
| **S3-1** | S3 | `外部依赖.豁免正向场景` 判据只要求「`豁免正向场景 is True` **且** 有 `地址`」——无法区分「底座之外的上游服务」与「本地服务」，正是 2.2 边界要防的形态。当前 **136 份能力定义.json 全 0 命中**，本类未发现实际误用 | 2.2 边界 | ⚠️ 判据形态不足以守住边界 |

**一句话**：第三方 import 的物理归属**干净**（全部落在 `支持库/适配层/` 与 `支持库/后端/` 的提供者，模块库与核心层 0 处直连）；缺口全部集中在**「环境依赖」这一半**——服务类依赖（PostgreSQL）完全没登记，非包类外部命令（security/node/codegraph/browser-use）进不了环境自检，内部件（模型服务/系统探针）有第三方 import 却没有依赖锁。

---

## 二、已有机器覆盖矩阵

> **先盘已有再报缺口**。下表全部为现场实跑或实读判据源得到，不是文档转述。

| # | 机器件 | 判什么 | 接没接进必经路径 | 本轮实测 |
|---|---|---|---|---|
| 1 | `运行核心/依赖防火墙.py`（789 行） | 层向（`允许依赖表` + `文件级豁免表`）、跨包 `实现/` 导入、支持库反向导入、动态导入绕过、**非白名单层直连第三方发行包**（`:608`）、正式代码直达提供者 | ✅ **已接**：`开发工具/发布门禁/运行发布门禁.py:2357-2363` 门禁项「依赖防火墙」 | `审计依赖()` → 审计文件数 **769**、违规 **0**、成功 **True**；`检查依赖口径()` 无问题；`检查豁免表()` 无问题 |
| 2 | `开发工具/复用审计/能力调用图审计.py`（320 行） | 模块库：原子旁路（文件/网络/数据库/进程/临时目录/动态导入）、`模块库网络进程表`、越界导入、**第三方直连**（`第三方前缀表`，与依赖防火墙共用同一 `set` 对象 `:30`）、四者漂移、重复能力 | ✅ **已接**：`运行发布门禁.py:1699-1758` `校验能力调用图防回潮`，门禁项「防回潮·能力调用图」，配置「干净通过 / 基线 0」 | `运行能力调用图审计.py` → 退出码 **1**，审计文件数 **62**，违规 **2**（详见 S1-1） |
| 3 | `开发工具/依赖生命周期审计/`（审计核心 484 行 + 入口 + 反验） | 第三方支持库提供者：依赖锁独立版本声明 / 混装 / 完整性摘要重算比对 / 健康探针 / **停止入口真实存在** / **释放策略文案 vs 实现** | ⚠️ **半接**：`运行发布门禁.py:801-805` 只 import 它的 `扫描提供者目录` 用于「第三方权限声明」项；**入口本身（`运行依赖生命周期审计.py`）未被任何门禁调用** | `运行依赖生命周期审计.py` → 退出码 **1**，标准提供者 **33**、跳过 3；违规 1（见 S2-2/S2-4） |
| 4 | `开发工具/依赖派生/生成依赖分两段.py`（646 行） | 由 AST 现场派生：各正式包 `包声明.json.依赖类别`、`支持库/适配层/依赖登记.json`、`支持库/适配层/说明/依赖分两段.md`（2.3 六要素声明的唯一生成器） | ❌ **未接**：`rg -ln "依赖分两段\|依赖登记" 开发工具 运行核心 平台控制面` → 命中仅 `开发工具/依赖派生/*` 自身 | `--校验` → 退出码 **1**，**4 项红**（见 S1-2/S2-3） |
| 5 | `开发工具/重建依赖锁.py`（486 行） | 依赖锁 `环境` 字段（Python/操作系统/CPU）按当前平台重建；空锁拒绝盖章；定点刷新摘要 | ⚠️ 被 `开发工具/环境自检.py` 第 3 项调用（干跑）；未发布门禁 | `环境自检` 第 3 项 → 「**33/33 份依赖锁与当前环境一致**（需重建 0 份）」 |
| 6 | `开发工具/包体检.py`（293 行） | 九要素目录/必备文件、`__init__.py` 注册能力、复用决策、权限契约、验证场景、骨架残留、摘要一致；**依赖锁委托唯一判据** `公共契约/包声明/声明.检查依赖锁内容`（`:49-69`） | ❌ 手工入口，未发布门禁 | 本轮未跑（口径见第六节局限） |
| 7 | `开发工具/环境自检.py`（1121 行） | 解释器/平台架构/依赖锁一致性/收口层 10 项+2 项真跑/POSIX 专有参数/中文环境变量名/装配冒烟/**外部命令可用性**（`:67-76`，8 项） | ❌ 手工入口（AGENTS.md 列为「步骤 0」） | 退出码 **0**：通过 **21**、警告 **0**、不支持 **0**；第 8 项装配 2.0s、能力数 **699**、跳过包 **0**；第 9 项「**8/8 项可用**」 |
| 8 | `运行发布门禁.py:781-895` `校验第三方访问声明` | 第三方提供者的权限/网络/文件/进程声明与真实依赖一致；**只扫适配层**（`适配层根 = 制品层根/支持库/适配层`） | ✅ 已接，门禁项「第三方权限网络文件进程声明」 | 判据源码实读；明确自报「适配层外另有 N 份依赖锁**不在本项声明核验范围**」（`:888-894`） |

**被机器管住的类别**（结论）：① 层与层之间的方向；② 模块库对第三方的直连与原子旁路；③ 提供者的依赖锁混装 / 摘要漂移 / 健康探针存在性 / 停止入口真假 / 释放文案名不副实；④ 依赖锁的平台指纹。**共 4 类。**

**没被任何机器管住的类别**：① 服务型环境依赖（PostgreSQL 这类「独立进程」）的登记；② 该有锁却没锁的内部件；③ `依赖登记.json` 与实际现场的一致性；④ 环境自检的外部命令表与依赖登记的环境依赖清单是否闭合。**共 4 类。**

---

## 三、逐条明细

### 第 1 项｜支持库是否只提供「最小原子能力」（2.1）

**本类未发现「把连接池/句柄/重试下放到模块或业务侧」的实例。** 现场取证：

- 支持库侧持有这些机制是合规的：`支持库/后端` 下「重试」197 处、「连接池」31 处、「句柄超时」14 处。
- 模块侧对「重试」的全部出现都是**消费结果信封的 `可重试` 字段**，不是自己实现重试：
  `模块库/图像处理/实现/图像处理.py:37-59`（`可重试=(失败错误码 == "提供者不可用")`，四处）。
- 模块侧的「句柄」全部是**持有受管句柄并转交给能力**（2.1 允许的用法）：
  `模块库/简单窗口/实现/简单窗口.py:45-116`（`_读取状态(句柄)` → 调 `系统核心支持库.资源管理.读取受管状态`）、
  `模块库/网页浏览/实现/网页浏览.py:49-62`（句柄由能力返回、原样回传，重试语义来自能力）。
- `模块库/开工编排/实现/开工编排.py:316` 的「文件租约」是**经能力面读取的文件租约实况**，不是模块自建租约——这条正是任务信提示的、上一轮被误报为「缺失件」的已存在机制，本轮确认**不是缺陷**。

**唯一真实命中见下方 S1-1。**

#### S1-1｜模块自己 `open()` 读文件（原子旁路）+ 同文件重复能力

- **文件:行号**：`模块库/直播逐字稿/实现/全自动精校.py:137`（`with open(os.path.join(裁决目录, 名), encoding="utf-8") as f:`）；同判据在同文件 `:122` `_读已有裁决` 报「重复能力」，`:206`、`:368` 另有 `Path(...)` 展开。
- **类别**：分层越界（模块做了「文件句柄」这一底层机制）。
- **现场证据**：
  ```
  $ python3.14 开发工具/复用审计/运行能力调用图审计.py
  模块库/直播逐字稿/实现/全自动精校.py:137:原子旁路-文件读写
  模块库/直播逐字稿/实现/全自动精校.py:122:重复能力
  审计文件数: 62；违规数: 2
  退出码=1
  ```
- **违规条款**：2.1（连接池/句柄/重试这类底层机制全部归支持库，不下放到模块或业务）；`AGENTS.md`「模块只组合公开能力完成流程，不直接连接第三方库」。
- **影响面**：单点。`模块库/直播逐字稿` 的裁决复用路径绕过支持库的文件能力，导致 `08_裁决/窗口_*.json` 的读取没有走资源释放与权限声明链；同一文件存在两处同判定。
- **最小改法落点**：把 `:122-144` 的目录列举与文件读取改经能力调用器调 `文件系统支持库.内容检索 / 文件操作` 的公开能力；`:137` 的 `open()` 随之消失。
- **是否已被门禁覆盖**：✅ **已被覆盖且当前会判红**。判据 `能力调用图审计.py:296-320` 已由 `运行发布门禁.py:1699-1758` 接线为强制项「防回潮·能力调用图」，配置「干净通过 / 存量基线 0 / 阻断=真」；`开发工具/发布门禁/防回潮存量基线.json` 的「能力调用图」条目为 **空表 `{}`**，即任何一条都算「基线外新增」→ 直接判红。**这一条是本轮唯一的「已机器强制、当前确实红」的依赖分层违规**。

---

### 第 2 项｜模块是否只组合公开能力（有没有模块直接连第三方）

**本类未发现。** 全仓 AST 扫描（1005 个 `.py`，排除 `.git/__pycache__/工程缓存`）的第三方顶层 import 分布（下表第 3 项）显示：

- `模块库/**` 第三方 import **0 处**。
- `前端核心/**`、`后端核心/**`、`运行核心/**`、`平台控制面/**`、`项目适配层/**`、`技能库/**` 第三方 import **0 处**。

机器双保险：`运行核心/依赖防火墙.py:608-652`（第三方直连判据，非白名单层一律报红）＋ `开发工具/复用审计/能力调用图审计.py:118`（`第三方直连/格式解析原子实现`，模块库更严）。两份判据共用同一个 `第三方前缀表` 对象（`能力调用图审计.py:30`），不是两把尺子。本轮实测：两处违规均为 0（依赖防火墙 769 文件 0 违规；能力调用图仅 2 条且都不是第三方直连）。

**唯一合法开洞点**（不是缺陷，见第五节）：`依赖防火墙.py:230-233` `服务宿主SDK白名单` —— `开发工具/薄壳/` 直连 `mcp` / `starlette` / `uvicorn`。

---

### 第 3 项｜第三方 import 的出现位置分布（全量）

**取证方法**：AST 解析全仓 1005 个 `.py`，取 `ast.Import` / `ast.ImportFrom`（`level==0`）第一段，排除 `sys.stdlib_module_names` 与平台自有顶层名（现场从根目录推导，含 `平台客户端`/`系统工程平台`）。

**核心结论：第三方 import 全部落在「适配层提供者 / 后端聚合包提供者 / 测试中心」，模块库与全部核心层 0 处。**

| 第三方模块 | 出现次数 | 文件数 | 出现目录 |
|---|---|---|---|
| `reportlab` | 34 | 8 | `支持库/适配层/reportlab提供者`、`测试中心/*`（6 文件） |
| `openpyxl` | 12 | 6 | `支持库/后端/文档转换支持库/openpyxl提供者`、`测试中心/*`（5 文件） |
| `cryptography` | 11 | 1 | `支持库/适配层/密码签名提供者` |
| `docx` | 10 | 6 | `支持库/后端/文档转换支持库/python_docx提供者`、`测试中心/*`（5 文件） |
| `pptx` | 10 | 5 | `支持库/后端/文档转换支持库/python_pptx提供者`、`测试中心/*`（4 文件） |
| `mcp` | 9 | 1 | `支持库/适配层/MCP协议提供者/实现/协议.py:21-29` |
| `fitz` | 4 | 3 | `支持库/适配层/PDF隔离提供者`、`支持库/适配层/PyMuPDF提供者`、`测试中心/支持库/测试_文档生成.py:109,263` |
| `PIL` | 3 | 3 | `支持库/后端/图像处理支持库/图像解码`、`支持库/适配层/Pillow提供者`、`测试中心/支持库/测试_Tesseract提供者.py` |
| `mlx_whisper` | 2 | 2 | `支持库/后端/转写支持库/转写`、`支持库/适配层/MLXWhisper提供者` |
| `pdfplumber` | 2 | 2 | `支持库/适配层/PDF隔离提供者`、`支持库/适配层/pdfplumber提供者` |
| `tree_sitter` / `tree_sitter_typescript` | 2 / 1 | 1 | `支持库/适配层/TreeSitter提供者/实现/提供者.py:77,78,142` |
| `torch` / `fastapi` / `transformers` / `uvicorn` | 2/1/1/1 | 1 | `支持库/适配层/模型服务.py:9-13` ⚠️ 见 S0-3 |
| `psutil` | 1 | 1 | `支持库/适配层/系统探针.py:33` ⚠️ 见 S0-3 |
| `psycopg` | 1 | 1 | `支持库/适配层/psycopg提供者/实现/提供者.py:26` |
| `yaml` | 1 | 1 | `支持库/适配层/YAML提供者/实现/提供者.py:11` |

**归处判定**：上述全部条目落在两类合规位置——① `支持库/适配层/<提供者>/`（翻译层，2.1/2.3 指定落点）；② `支持库/后端/<支持库>/<提供者>/`（门面→提供者同一支持库内部，不算跨模块）；③ `测试中心/`（测试构造夹具，`依赖防火墙.py:460` 与 `能力调用图审计.py:280` 的 `排除片段表` 都显式把 `测试中心` 剪出产品源码）。**无散落。**

> 说明：`测试中心` 的 47 处第三方 import 是测试自建 PDF/表格/图片夹具，属允许；测试代码不在产品源码边界（`运行发布门禁.py:137-140` `正式源码目录名表` 不含 `测试中心`）。

---

### 第 4 项｜环境依赖是否每一项都有完整声明（版本/入口/端口/健康探针/启停/降级语义）

#### S0-1｜PostgreSQL 服务没有被登记为环境依赖（本项最重）

- **文件:行号**：
  - `支持库/适配层/psycopg提供者/实现/提供者.py:48-49`：`return {"host": unquote(解析.hostname) if 解析.hostname else "127.0.0.1", "port": 解析.port or 5432, ...}` —— **服务地址与端口写死在实现代码里**。
  - `支持库/适配层/psycopg提供者/说明/设计说明.md:33`：`连接串 = "postgresql://主机:5432/数据库"`
  - `支持库/适配层/psycopg提供者/依赖锁.json` 的 `包` 只有 `psycopg`（PyPI 包），**没有 PostgreSQL 服务项**。
- **类别**：环境依赖缺失（运行级依赖未登记）。
- **现场证据**：
  ```
  $ python3.14 -c "import json;d=json.load(open('支持库/适配层/依赖登记.json',encoding='utf-8'));print([j['名称'] for j in d['条目'] if j['依赖类别']=='环境依赖'])"
  ['FFmpeg提供者', 'Git提供者', 'LibreOffice提供者', 'Tesseract提供者', 'textutil提供者',
   '密钥提供者', '本地进程适配器', '浏览器自动化提供者']
  ```
  → **PostgreSQL 不在其中**；`支持库/适配层/说明/依赖分两段.md` 的环境依赖层表格同样没有它。
- **违规条款**：2.2②（环境依赖＝运行级依赖，独立进程/服务/动态库/外部接口，**必须有声明**：版本、入口、端口、健康探针、启停、降级语义）、2.3（环境依赖段的每一项都必须带声明）。
- **影响面**：`psycopg提供者` 是**唯一**把外部形态翻成数据库连接的提供者；实测缺 6 项里的 **5 项**——版本（没写支持哪个 PostgreSQL 大版本）、入口（无服务入口）、端口（只在实现里默认 `5432`，不是声明）、启停（无）、降级语义（说明文档 `:58` 有文字、实现有「提供者不可用」，但**未进依赖登记**）。换机器时「本机有没有 PostgreSQL、什么版本」无任何机器可读处。
- **最小改法落点**：给 `支持库/适配层/psycopg提供者` 增环境依赖声明段（可选做法：在 `依赖锁.json` 增 `环境依赖` 数组，或在 `生命周期契约.json` 增 `环境依赖` 块），内容至少为 `{名称: PostgreSQL, 版本: <实测大版本>, 入口: <服务/容器>, 端口: 5432, 健康探针: <可执行的探活>, 启停: <命令>, 降级语义: 缺服务→提供者不可用}`，并让 `依赖派生/生成依赖分两段.py` 把它派生出清单。**不要**用 `外部依赖.豁免正向场景`（该字段按 2.2 边界不适用于本地依赖）。
- **是否已被门禁覆盖**：❌ **无任何判据**。`运行发布门禁.py:781-895` 只扫适配层的 `依赖锁.json`（第三方包条目），不扫服务型依赖；`依赖生命周期审计/审计核心.py:156-201` 只认「有 生命周期契约.json + 第三方依赖证据」的提供者，psycopg提供者虽在范围内，但它的证据是 **psycopg 这个包**，判据不会问「psycopg 连的那个服务登记了吗」。

#### S2-1｜三项真实环境依赖不在 `环境自检` 外部命令表

- **文件:行号**：`开发工具/环境自检.py:67-76` `外部命令表`（8 项：`ffmpeg` / `ffprobe` / `tesseract` / `soffice|libreoffice` / `textutil` / `git` / `ps` / `pgrep`）。
- **类别**：环境依赖声明不完整（换机器可见性缺口）。
- **现场证据**：
  ```
  $ python3.14 -m 开发工具.环境自检
  [9] 外部命令可用性 【通过】 8/8 项可用：ffmpeg、ffprobe、tesseract、soffice 或 libreoffice、textutil、git、ps、pgrep

  # 而对同一份代码做关键词检索：
  环境自检 是否探 security: False
  环境自检 是否探 node: False
  环境自检 是否探 codegraph: False
  环境自检 是否探 browser-use: False
  ```
  未纳入的三项+一个命令：`密钥提供者`（`security`，`依赖登记` 标环境依赖）、`代码地图提供者`（`codegraph` + `node`，`依赖锁.json` 来源=外部应用）、`浏览器自动化提供者`（`browser-use`，`依赖锁.json` 来源=外部应用；另有辅助依赖 `agent-browser`）。
- **违规条款**：2.2（环境依赖必须有健康探针）、2.2「不允许连不上就豁免」。
- **影响面**：`代码地图提供者/依赖锁.json` 的说明已经写死了实测坑——「★ 实测在 `/usr/local/bin/node`，不在 `/opt/homebrew/bin`，PATH 漏了它会报 `env: node: No such file or directory`」——但 `环境自检` 探不到 node，换机器的人会先看到能力调用报错、才反推 PATH，而不是开工第一步就看见。同类：钥匙串不可用、browser-use 未装，都只能等调用时报「提供者不可用」。
- **最小改法落点**：`开发工具/环境自检.py` 的 `外部命令表` 增 `security`（macOS 钥匙串）、`node`、`codegraph`、`browser-use`；或更彻底——把该表改为**从 `支持库/适配层/依赖登记.json` 的「环境依赖」条目现场派生**（唯一事实源，避免两张表各自漂移）。
- **是否已被门禁覆盖**：⚠️ 判据存在（`环境自检` 第 9 项），但**核验面不全 = 8/8 通过是局部真话**。这一项的「通过」不构成「本机环境依赖齐了」的证据。

#### S2-2｜环境依赖六要素逐项缺失明细

来源：`支持库/适配层/依赖登记.json`（生成物，生成器 `开发工具/依赖派生/生成依赖分两段.py`）＋ 现场实读。

| 件 | 类别 | 版本 | 入口 | 端口 | 健康探针 | 启停 | 降级语义 | 缺哪几项 |
|---|---|---|---|---|---|---|---|---|
| FFmpeg提供者 | 环境依赖 | 8.1 | `__init__.py` | 无（一次性子进程） | `FFmpeg媒体.检查提供者` | 无常驻 | fail-closed | — |
| Git提供者 | 环境依赖 | 2.50.1 | `__init__.py` | 无 | `Git操作.检查提供者` | 无常驻 | fail-closed | — |
| LibreOffice提供者 | 环境依赖 | 26.2.2.2 | `__init__.py` | 无 | `LibreOffice转换.检查提供者` | 无常驻 | fail-closed | — |
| Tesseract提供者 | 环境依赖 | 5.5.2 | `__init__.py` | 无 | `OCR识别.版本探针` | 无常驻 | fail-closed | — |
| textutil提供者 | 环境依赖 | 26.5.2 | `__init__.py` | 无 | `textutil转换.检查提供者` | 无常驻 | fail-closed | — |
| 浏览器自动化提供者 | 环境依赖 | 0.1.13 | `__init__.py` | 无 | `浏览器自动化.检查可用` | **有常驻/守护进程**（须显式启停） | fail-closed | — |
| **密钥提供者** | 环境依赖 | **无** | `__init__.py` | 无 | **无** | 无常驻 | **无** | **版本、健康探针、降级语义（3 项）** |
| **本地进程适配器** | 环境依赖 | **无** | `__init__.py` | 无 | **无** | 无常驻 | **无** | **版本、健康探针、降级语义（3 项）** |

- **违规条款**：2.3（环境依赖段每一项都必须带声明）。
- **现场证据**：`依赖登记.json` 内两件的 `需复核` 原文——
  `"版本：依赖锁.json 内根项未标注 版本，版本未登记（不编造）"`、
  `"内部件无 依赖锁.json（按决策记录 0033：有外部依赖应建锁），版本与探针口径待主会话收口"`、
  `"降级语义：实现内未出现「提供者不可用」，降级口径未声明"`。
- **影响面**：`密钥提供者` 读的是 macOS 钥匙串（`密钥提供者.py:123` `shutil.which("security")`，`:150-153` `subprocess.run(...)`）；它没有健康探针，意味着**钥匙串不可用时要到业务调用才暴露**，且失败码是 `钥匙串不可用`（`密钥提供者.py:23`）——实现里已有明确降级语义，只是**没被登记**。
- **最小改法落点**：给两件补 依赖锁.json（`security` 版本的来源=系统工具）并在 `能力定义`/`生命周期契约` 里补探针；把现有 `密钥提供者.py:23` 的错误码写进登记的降级语义字段。
- **是否已被门禁覆盖**：⚠️ 生成器**如实标了「需复核」而不是编造**（这是对的），但 `--校验` 只在手工入口跑，且「需复核」是「记账」不是「判红」。

#### S2-2b｜`代码地图提供者` 缺 `生命周期契约.json`

- **文件:行号**：`支持库/适配层/代码地图提供者/`（有 依赖锁/包声明/能力定义/完整性摘要/资源预算，**无 生命周期契约.json**）。
- **现场证据**：18 个适配层正式包逐件核验 → 仅 `代码地图提供者` 缺该件（其余 17 个齐）。
- **违规条款**：2.3（环境依赖段的每一项都必须带声明）＋ `AGENTS.md`「每个支持库必备」。
- **影响面**：`生命周期契约.json` 是「健康探针 / 停止入口 / 释放策略」的唯一落点；缺它 → `依赖生命周期审计/审计核心.py:276-306` `检查健康探针` 只能退化到读 `能力定义.json`（它恰好有 `代码地图提供者.检查提供者`，所以侥幸没红），但**释放策略无处可核对**。
- **最小改法落点**：补 `生命周期契约.json`（提供者id、健康探针、资源模型、释放策略、停止入口），并重算摘要。
- **是否已被门禁覆盖**：⚠️ `依赖生命周期审计` 有判据（缺健康探针/缺停止入口），但**未接入发布门禁**；当前侥幸通过。

#### S0-3｜内部件直接 import 第三方却无依赖锁、无包声明、不进任何审计

- **文件:行号**：
  - `支持库/适配层/模型服务.py:9-13`（`import torch` / `uvicorn` / `fastapi` / `transformers`）
  - `支持库/适配层/系统探针.py:33`（`import psutil`）
- **类别**：第三方库依赖缺声明（2.2①）。
- **现场证据**：`依赖登记.json` 对应条目 `依赖锁: "无 依赖锁.json"`、`需复核: ["内部件无 依赖锁.json（按决策记录 0033：有外部依赖应建锁），版本与探针口径待主会话收口", ...]`；现场实读：两处 import 属实，两处均无 `依赖锁.json`。
- **违规条款**：2.2①（第三方库＝代码级依赖，落在适配层提供者）、2.3；决策记录 `0033`「有外部依赖应建锁」。
- **影响面**：
  - `模型服务.py` 是 `fastapi+uvicorn+torch+transformers` 四条重依赖，**没有任何版本的独立声明**——换机器装什么版本全靠猜，且与「精确锁定，禁止范围版本」的全仓锁口径不一致。
  - 两者都没有 `包声明.json`，因此**不在** `扫描提供者目录`（`审计核心.py:170-176`）范围内 → 拿不到完整性摘要、健康探针、停止入口、释放策略四项机器把关。
- **最小改法落点**：二选一——① 建 `模型服务提供者/`、`系统探针提供者/` 正式包（包声明 + 依赖锁 + 能力定义 + 生命周期契约 + 摘要），把根下 `.py` 收进去；② 若确认二者是「只为底座内部翻译、不对外提供能力契约」的系统适配器，则在 `AGENTS.md` 的适配器豁免清单里**显式登记**它们（当前清单 `AGENTS.md:296-300` 列了 `系统探针.py` 但**没列 `模型服务.py`**），并把该豁免与 2.2① 的关系写成可判定的判据。
- **是否已被门禁覆盖**：⚠️ **依赖防火墙放行它们是刻意设计**（`依赖防火墙.py:619-627`：根下直接文件即 AGENTS.md 所指的内部适配器豁免，「判它们违规等于要求翻译层不许碰被翻译的库」）。**但没有任何判据要求它们建锁**——`包体检.py:49-69` 只判「锁存在但空」为红、缺失即放行（因为「无第三方依赖时删锁」），它**不会**因为「该有锁而没锁」判红。

---

### 第 5 项｜`外部依赖.豁免正向场景` 是否被误用在本地第三方库/环境依赖上

**本类未发现实际误用（零实例）。但判据形态不足以守住 2.2 边界。**（S3-1）

- **文件:行号**：
  - 判据读取处：`开发工具/HTML验证/场景加载.py:82-103`（`_声明豁免正向场景的能力`），核心行 `:103`：`if isinstance(声明, dict) and 声明.get("豁免正向场景") is True and 声明.get("地址"):`
  - 口径规范：`开发文档/规范/场景覆盖率口径.md:63`（字段形态）、`:163`（「实测当前 0 条」）、`:192`（禁止与基线混用）
  - 边界条款：`开发文档/项目说明.md:147-148`、`开发文档/决策记录/0021_依赖两段_第三方翻译层与环境依赖层.md:65-69`
- **现场证据**：
  ```
  $ rg -n "豁免正向场景" -g '!.git' .
  → 命中 5 个文件，全部为「判据实现 + 规范文本 + 决策记录」；
    没有任何一份 能力定义.json 命中。

  $ rg -n '"外部依赖"' -g '!.git' .
  → 命中仅 2 处：开发工具/HTML验证/场景加载.py:102（读取），
    开发文档/规范/场景覆盖率口径.md:63（字段样例）。

  $ 全仓 136 份 能力定义.json 扫描："外部依赖" 出现 0 份
  ```
  → `豁免正向场景` **当前零启用**，本轮**不存在误用实例**。
- **违规条款**：2.2 边界（该字段只适用于「底座之外的上游服务不可用」，不适用于本地第三方库/环境依赖）。
- **影响面（判据层面的缺口，不是当前缺陷）**：判据把「有 `地址` 字段」当作「上游服务」的等价证据。`地址` 是自由字符串——把 `postgresql://127.0.0.1:5432/...`（本机服务）、或某本地库的安装路径写进去，`场景加载.py:103` 会**照样放行**。没有任何检查能区分「底座之外的上游」与「本地」。一旦有人拿它给 `psycopg提供者` 这类本地依赖开洞，就会静默扣减场景覆盖率分母（`场景覆盖率口径.md:275`）而无人察觉。
- **最小改法落点**：`场景加载.py:103` 的判据加两档——① 地址必须能判定为「非本地」（拒绝 `127.0.0.1` / `localhost` / `0.0.0.0` / 私网段）；② 地址指向的依赖若同时出现在 `支持库/适配层/依赖登记.json` 或某提供者的 `依赖锁.json` 里，**判红**（「本地已具备的依赖不许豁免」）。两档都不需要新数据源，用现场已有的登记与锁即可。
- **是否已被门禁覆盖**：⚠️ 判据存在（`场景加载._声明豁免正向场景的能力` 被 `开发工具/HTML验证` 的覆盖率计算消费），但**不判「该不该豁免」**，只判「有没有声明」。

---

### 第 6 项｜依赖锁覆盖率与「锁内容 vs 环境自检口径」一致性

#### 6.1 依赖锁清单（实测 33 份，`环境` 字段全部 `{"Python":"3.14.4","操作系统":"macOS","CPU":"arm64"}`）

| 分布 | 份数 | 明细 |
|---|---|---|
| `支持库/适配层/*` | 18 | 18 个正式包**一一对应**，含 `代码地图提供者`（`codegraph 1.5.0` + `node 24.15.0`，来源=外部应用） |
| `支持库/后端/*` | 15 | `OCR识别`(tesseract)、`图像解码`(Pillow)、`FFmpeg媒体`(ffmpeg/ffprobe)、`psycopg数据库`(psycopg)、`LibreOffice转换`、`PDF文本表格`(pdfplumber)、`PDF渲染`(PyMuPDF)、`PDF生成`(reportlab)、`PDF隔离提供者`(pdfplumber/PyMuPDF/Poppler)、`openpyxl提供者`、`python_docx提供者`、`python_pptx提供者`、`textutil转换`、`Git操作`(git)、`转写`(mlx-whisper) |
| **合计** | **33** | 适配层 18 + 后端 15 |

**覆盖率结论**：
- 适配层 18 个正式包 **100% 有锁** ✅（含本轮最容易被漏的 `代码地图提供者`）。
- 「纯标准库包不建锁是允许的」这条被正确地实现为「**有第三方 import 或无锁件才需要锁**」——实测纯标准库包不建锁的例子：`支持库/后端/数据库连接支持库/SQLite数据库`（`依赖生命周期审计` 输出「有 生命周期契约.json 但**无第三方依赖证据**……非第三方支持库提供者，跳过」）✅ 口径正确。
- **未建锁但确有外部依赖的 4 件**（全部是适配层「内部件」，无包声明）：
  | 件 | 外部依赖证据 | 应有锁 |
  |---|---|---|
  | `支持库/适配层/模型服务.py` | `import fastapi/uvicorn/torch/transformers` | 第三方库 |
  | `支持库/适配层/系统探针.py` | `import psutil` + `subprocess.Popen` | 第三方库（+环境） |
  | `支持库/适配层/密钥提供者/` | `shutil.which("security")` + `subprocess.run` | 环境依赖 |
  | `支持库/适配层/本地进程适配器/` | `subprocess.Popen` | 环境依赖 |
  → 撞 2.2①/2.3 ＋ 决策记录 `0033`。**归入 S0-3 / S2-2。**

#### 6.2 锁内容 vs 环境自检口径

**口径一致（实测）**：
```
$ python3.14 -m 开发工具.环境自检
[3] 依赖锁一致性 【通过】 33/33 份依赖锁与当前环境一致（干跑：需重建 0 份）；
    锁内环境 {"CPU": "arm64", "Python": "3.14.4", "操作系统": "macOS"}
```
一致性由 `开发工具/重建依赖锁.py:74-96` `采样环境()` 与 `运行核心/环境指纹.计算环境指纹` 单一来源保证；`环境自检.py:232-285` 直接调用它的干跑口径（不写盘），**不是第二套采样**。三者（重建依赖锁 / 环境自检 / `运行核心.运行环境管理器.强制校验` 规则 5）对同三元组同一尺子。

**两处口径覆盖的差异（不是冲突，是覆盖缺口）**：
- `环境自检` 第 3 项只回答「**已有锁的环境字段**对不对」，不会发现「**该有锁而没有**」。
- `重建依赖锁 --干跑` 只在有锁的 33 份上迭代（`枚举依赖锁()`），同样不发现缺失。
- `依赖生命周期审计` 的 `检查依赖锁` 只对**标准提供者**（33 个）要锁；`模型服务.py` / `系统探针.py` / `密钥提供者` / `本地进程适配器` 不在其中（无包声明或判为「非标准」）。
- ⇒ **「该建锁而没建锁」在全仓无判据**。这是 6.1 四件缺锁能长期存活的直接原因。

**额外发现：`代码地图提供者` 混装判定**（2.3 的「一第三方一支持库一提供者」口径）
```
$ python3.14 开发工具/依赖生命周期审计/运行依赖生命周期审计.py
标准提供者 33 个（跳过 3 个非标准目录）
  跳过: MySQL提供者: 无 包声明.json，非标准第三方支持库提供者，跳过
  跳过: 密钥提供者: 无 包声明.json，非标准第三方支持库提供者，跳过
  跳过: 后端/数据库连接支持库/SQLite数据库: 有 生命周期契约.json 但无第三方依赖证据…，跳过
违规清单（1 个提供者，1 条）：
[代码地图提供者]
    - 混装: 一个提供者声明多个第三方 ['Node.js', 'codegraph（npm 全局包）']
退出码=1
```
判断：`codegraph`（npm 全局包）与 `node`（它的运行时）确实是**两个发行包**，判据 `审计核心.py:244-253` 按 `发行包` 字段取值 → 报混装。这是**判据与现实的张力**（见第五节），不是代码缺陷——`codegraph` 没有 `node` 就跑不起来，锁里已用 `依赖闭包` 的 `说明` 写清「node 是 codegraph 的运行时」。建议在 `依赖锁.json` 里用既有的 `发行包` 字段把 `node` 归到 `codegraph` 的闭包而不是同级 `包` 数组，使判据与语义一致。

---

### 第 7 项｜翻译层是否「不照搬全量」（过度封装）

**本类未发现。** 现场逐件取证（20 个 `*提供者` 目录 + 10 个内部件，全量 AST/文本扫描）：

- **无 `import *`**、**无 `__getattr__` 透传**、**无 `globals().update` / `locals().update`** —— 全部 0 命中。没有任何提供者把第三方的命名空间整片暴露出去。
- **能力面都是「我们真正用得上的」小面**（`包声明.json.能力` 条数）：

  | 提供者 | 能力数 | 被翻译的第三方 |
  |---|---|---|
  | FFmpeg提供者 | 5 | ffmpeg / ffprobe |
  | Git提供者 | 9 | git |
  | LibreOffice提供者 | 2 | soffice |
  | MCP协议提供者 | 1 | mcp |
  | MLXWhisper提供者 | 3 | mlx-whisper |
  | PDF隔离提供者 | 3 | pdfplumber / PyMuPDF / Poppler |
  | Pillow提供者 | 9 | Pillow |
  | PyMuPDF提供者 | 4 | PyMuPDF |
  | Tesseract提供者 | 3 | tesseract |
  | TreeSitter提供者 | 2 | tree-sitter / tree-sitter-typescript |
  | YAML提供者 | 3 | PyYAML |
  | pdfplumber提供者 | 2 | pdfplumber |
  | psycopg提供者 | **1** | psycopg |
  | reportlab提供者 | **1** | reportlab |
  | textutil提供者 | 2 | textutil |
  | 代码地图提供者 | 3 | codegraph / node |
  | 密码签名提供者 | 7 | cryptography |
  | 浏览器自动化提供者 | **1** | browser-use |
  | 合计 | **62** | — |

  对照：`Pillow` 公开 API 数百、`cryptography` 数百、`reportlab` 数百，翻译层只暴露 1–9 条 —— 符合 2.1「翻译层只翻译我们真正用得上的原子能力」＋ 2.4②「翻译层不照搬全量」。`psycopg提供者` 只有 1 条能力（执行查询）而 psycopg 本身有连接池、异步、COPY、通知等一大片 —— **没有照搬**。

- **重心只在边界**（2.4③）：内部形态统一走 `公共契约.基础类型.结果类型`（如 `支持库/后端/网络通信支持库/请求/实现/网络请求.py:40`），实现层看不到第三方的原生异常/对象形态。

---

### 附：跨项结构发现

#### S1-2｜`依赖分两段` 的声明件未被任何门禁调用（2.3 属纸面）

- **文件:行号**：生成器 `开发工具/依赖派生/生成依赖分两段.py`（646 行）；产物 `支持库/适配层/依赖登记.json`（1110 行）、`支持库/适配层/说明/依赖分两段.md`。
- **现场证据**：
  ```
  $ rg -ln "依赖分两段|依赖登记" 开发工具 运行核心 平台控制面
  开发工具/依赖派生/生成依赖分两段.py
  开发工具/依赖派生/__init__.py
  （仅自身，无任何门禁/审计引用）

  $ python3.14 开发工具/依赖派生/生成依赖分两段.py --校验
  ✗ 校验未通过（4 项）：
    - 代码地图提供者：包声明 依赖类别=None，现场派生应为 '环境依赖'
    - 依赖登记.json 类别不一致：代码地图提供者（登记=None 派生='环境依赖'）、
      提供者配置读取（登记=None 派生='无外部依赖'）、硬件画像探针（登记=None 派生='无外部依赖'）
    - 依赖登记.json 输入指纹过期（源已变，未重跑生成器）
    - 说明/依赖分两段.md 与派生结果不一致（未重跑生成器）
  退出码=1
  ```
- **违规条款**：2.3；哲学 13.1（判据在、门禁不调即纸面规则）。
- **影响面**：2.3 的「环境依赖段每一项都必须带声明」**唯一**的落地载体就是这份登记 + 清单，而它已经与现场漂移（3 件未登记、1 件改名、1 个包声明 `依赖类别` 缺失），且**没有任何必经路径会看到这个红**。
- **最小改法落点**：把 `生成依赖分两段.py --校验` 接进 `运行发布门禁.py` 的强制项（形制照 `校验能力调用图防回潮`：只消费既有判据、不另立第二份实现）；同时把 `依赖登记.json` 的「输入指纹过期」作为阻断（现在是自报项）。
- **是否已被门禁覆盖**：❌ 未接。

#### S2-4｜`生成依赖分两段.py` 仓内仓外双份，登记仍指向仓外

- **现场证据**：
  ```
  $ shasum -a 256 开发工具/依赖派生/生成依赖分两段.py ../系统工程平台_依赖派生/生成依赖分两段.py
  2106c55159f50108fedca0c58b2b3cc7b5200b54d6d74b05ebb6fc56bc640ed7  开发工具/依赖派生/生成依赖分两段.py
  2106c55159f50108fedca0c58b2b3cc7b5200b54d6d74b05ebb6fc56bc640ed7  ../系统工程平台_依赖派生/生成依赖分两段.py
  ```
  `依赖登记.json:2` 的 `"生成器"` 字段值 = `"系统工程平台_依赖派生/生成依赖分两段.py"`（**仓外路径**）；而生成器自己的 docstring `:8-11` 写着「生成器住在仓外……仓内任何新文件（含 `开发工具/`）都不在允许面内」——但现在仓内**已经有了一份完全相同的副本**。
- **违规条款**：2.4①（不许新开第二条腿：每条额外的「腿」都是一次翻译分叉）；1.2/1.4（空转即杀 · 结果唯一）。
- **影响面**：两处同哈希副本会随下一次修改分叉；登记里的 `生成器` 指向仓外，`--校验` 的「输入指纹」与生成器来源不在同一棵树内，追溯链断在仓外（AGENTS.md 铁律「规范不得指向已删除的东西」的同类）。
- **最小改法落点**：删仓外副本（`../系统工程平台_依赖派生/`，含 `交接说明_给主会话.md`），把 `依赖登记.json` 的 `生成器` 字段改为仓内相对路径 `开发工具/依赖派生/生成依赖分两段.py`，重跑 `--生成` 刷新输入指纹。
- **是否已被门禁覆盖**：❌ 无判据。

#### S2-3b｜`依赖登记` 覆盖落后现场（登记件集合 ≠ 现场件集合）

```
登记条目数: 34   现场件数: 36
现场有、登记无: ['代码地图提供者', '提供者配置读取', '硬件画像探针']
登记有、现场无: ['配置读取']
【登记 统计块】{'件数': 34, '正式包': 17, '内部件': 17, '第三方库': 13, '环境依赖': 8, '无外部依赖': 13}
```
- 「配置读取」在登记里、现场已改名为 `提供者配置读取`（目录实读：`支持库/适配层/提供者配置读取/`，含 `__init__.py` / `提供者配置读取.py` / `配置读取工具.py` / `说明/设计说明.md`）。
- `硬件画像探针.py`（361 行，适配层新件）与 `代码地图提供者`（18 个正式包之一，有锁有摘要）**完全不在登记里**。
- 撞 2.3（件级归属必须可列举）。**最小改法**：重跑 `生成依赖分两段.py --生成`（前提是按 S0-3 先把 `模型服务.py`/`系统探针.py` 的处置定了，否则生成器会在 `报警` 阶段拒绝生成）。

---

## 四、「不是缺陷（设计取舍）」清单

> 这一节记录**看着像违规、实则已裁定的设计**，防止下一轮把它们又报成缺口（本项目已发生过一次）。

| # | 现场 | 为什么不是缺陷 | 依据 |
|---|---|---|---|
| 1 | `开发工具/薄壳/` 直连 `mcp` / `starlette` / `uvicorn` | 薄壳是**平台自身的服务宿主**（对外暴露 MCP tool/list + ASGI），不是能力消费者。把宿主封进 `*提供者/` 会让「声称原子的提供者持有宿主任期」，反而破坏单一职责。已开唯一显式白名单，**只限这三个包前缀、只限这一个层** | `运行核心/依赖防火墙.py:217-233`；`AGENTS.md:50-64` |
| 2 | `支持库/适配层/` 根下平铺的 `.py`（`模型服务.py` / `系统探针.py` / `动态库适配器` / `脱敏模式.py` / `适配契约.py` …）直连第三方或起子进程 | `AGENTS.md` 明确豁免「不对外提供能力契约的内部适配器」。判它们违规 = 要求翻译层不许碰被翻译的库，只会把适配代码塞进 `*提供者/` 冒充提供者，让「一第三方一支持库一提供者」失真的**反向**后果 | `运行核心/依赖防火墙.py:619-627`；`AGENTS.md:296-300` |（注：但由此**衍生**的「该建锁没建锁」是真缺口，见 S0-3——豁免的是**结构要件**，不是依赖声明）
| 3 | 同一条链路的「后端腿 + 适配层腿」两套包（`支持库/后端/文档转换支持库/PDF文本表格` 与 `支持库/适配层/pdfplumber提供者` 等 12 对） | 已有明确裁决与迁移清单：**适配层为唯一实现；后端腿降为门面或删除**。`PDF文本表格`、`PDF生成` 已完成收口（`实现/PDF文本表格.py` 39 行 `sys.modules[__name__] = 唯一实现`；`PDF生成/实现/子进程入口.py:27` 子进程转调），其余 10 对按批次迁移中 | `开发文档/方案/适配层唯一化迁移清单_20260917.md:5-21, 48-50` |
| 4 | `开发工具/复用审计/提供者直连规则.py` 的「解析失败恒判阻断」 | 明确的口径统一：源码无法 AST 解析 = 直连情况**未知**，不等于「没有直连」；`解析失败` 是保留模式，**不可登记、不可豁免** | `开发工具/复用审计/提供者直连规则.py:13-16, 55-56, 143-151` |
| 5 | `测试中心/**` 47 处第三方 import | 测试自建 PDF/表格/图片夹具；`测试中心` 不在 `运行发布门禁.py:137-140` 的正式源码目录名表内，且被两份审计的 `排除片段表` 显式剪枝 | `运行核心/依赖防火墙.py:460`；`能力调用图审计.py:280` |
| 6 | `模块库/网页浏览`、`图像处理` 里出现「重试」「句柄」字样 | 都是**消费**能力返回的信封字段（`可重试`）或**持有**能力返回的受管句柄再原样回传，不是模块自建机制 | `模块库/图像处理/实现/图像处理.py:37-59`；`模块库/网页浏览/实现/网页浏览.py:49-62` |
| 7 | `模块库/开工编排/实现/开工编排.py:316` 的「文件租约实况」 | 是**经能力面读取**的租约状态，机制本体在支持库侧。这正是上一轮被误报为「缺失件」的已存在机制 | 现场实读 |
| 8 | `支持库/后端/数据库连接支持库/SQLite数据库` 不建依赖锁 | 纯标准库实现（只 `import sqlite3`，`包声明.依赖=[]`）→ 「无第三方依赖的正确表达就是**不建锁**」，缺失即正确 | `开发工具/包体检.py:49-69`；`开发工具/依赖生命周期审计/审计核心.py:156-199` |
| 9 | `代码地图提供者` 被依赖生命周期审计报「混装」 | `codegraph`（npm 全局包）与 `node`（它的运行时）确实是两个发行包；锁里已用 `依赖闭包.说明` 写清「node 是 codegraph 的运行时」。属**判据与现实的张力**，建议用 `发行包` 字段归一，不是代码缺陷 | `支持库/适配层/代码地图提供者/依赖锁.json` |

---

## 五、审计局限

1. **未跑全量测试 / 未跑发布门禁**（任务纪律：只读、不跑测试、不重启服务）。因此「发布门禁当前是红是绿」为**推断**而非实测：由判据源码 + 基线配置 + 判据模块单独实跑推出（`能力调用图` 基线空表 + 2 条违规 ⇒ 会红）。
2. **`开发工具/包体检.py` 未跑**。它是只读的，但会经 `开发工具.HTML验证.场景加载._解析场景引用` 触达场景树；为严守「不确定就不跑」纪律本轮未执行，因此「九要素齐全性」只对 18 个适配层正式包做了**手工等价核对**（依赖锁/生命周期契约/能力定义/完整性摘要/资源预算五件），未做全仓 105 个包。
3. **`环境自检` 第 8 项会真跑装配**（官方入口，已按任务书允许执行）。它写入了 `工程缓存/`（`.gitignore` 已排除的生成物），`git status --porcelain` 仍为 0 项 —— 未污染工作树。快照：装配 2.0 秒、能力数 699、跳过包 0、无新增监听端口异常。
4. **第三方 import 分布由 AST 静态扫描得到**（1005 个 `.py`），不含动态 `importlib` 拼接的模块名（`依赖防火墙.py:402-425` 会解析有界前缀，本轮的统计脚本未做同样解析）。因此「分布」可能少计个别受控动态导入；但动态导入本身由 `依赖防火墙` 的「动态导入绕过依赖审计」判据独立把关（本轮 0 违规）。
5. **「环境依赖完整声明」的判定依赖 `支持库/适配层/依赖登记.json`（生成物）**。该文件已实测漂移（S2-3b），因此本文档中「某件已声明 X」的结论均**同时做了现场实读复核**（读包声明/锁/实现源码），未单采信登记。
6. **`豁免正向场景` 的判定只到「判据形态」层面**。要证明它无法被误用，需要构造负例夹具（往某能力定义.json 写 `外部依赖:{豁免正向场景:true, 地址:"127.0.0.1:5432"}` 再看覆盖率是否扣减）——本轮为只读，**未构造任何夹具**。
7. **未验证制品态**（`平台客户端/` 编译副本）。`依赖防火墙.py:109-139` 与 `发布门禁.py:762-778` 都已为制品态的双根/前缀剥离做过修根，但本轮只在源码态实测。
8. **`依赖登记.json` 的「六要素」是生成物**。它的「端口：无」结论来自生成器的规则（`生成依赖分两段.py:286` `端口依据 = "全件 AST 无 listen/bind/serve 监听调用…"`）—— 这条规则**只扫模块自身是否监听**，**不会发现「本模块要连的远端服务在监听」**（正是 S0-1 的 PostgreSQL 落在这个盲区里的原因）。这是规则级局限，不是数据错误。

---

## 六、取证命令索引（复跑用）

```bash
export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH
cd "/Users/hekunhua/Documents/Agent/PHP/系统工程平台"

# 仓库状态
git rev-parse --short HEAD && git status --porcelain | wc -l

# 已有机器件（只读）
timeout 300 python3.14 -c "from 运行核心.依赖防火墙 import 审计依赖, 审计结果转清单, 检查依赖口径, 检查豁免表; r=审计依赖(); print(r.审计文件数, len(r.违规列表), r.成功); print(审计结果转清单(r)[:20]); print(检查依赖口径() or '无'); print(检查豁免表() or '无')"
timeout 180 python3.14 开发工具/复用审计/运行能力调用图审计.py          # 模块库原子旁路/第三方直连
timeout 120 python3.14 开发工具/依赖生命周期审计/运行依赖生命周期审计.py  # 提供者锁/摘要/探针/释放
timeout 120 python3.14 开发工具/依赖派生/生成依赖分两段.py --校验        # 依赖类别/登记/清单
timeout 180 python3.14 -m 开发工具.环境自检 --json                      # 本机环境 + 依赖锁指纹
timeout 120 python3.14 -m 开发工具.重建依赖锁 --干跑                    # 环境指纹漂移（不写盘）

# 第三方 import 分布（AST，全仓）
python3.14 - <<'PY'
import os,ast,sys
from pathlib import Path
根=Path("."); 标准=set(sys.stdlib_module_names)
平台={p.name for p in 根.iterdir() if p.is_dir()}|{p.stem for p in 根.glob("*.py")}|{"平台客户端","系统工程平台"}
跳={"__pycache__","工程缓存",".git",".pytest_cache",".ruff_cache"}
for d,子,fs in os.walk(根):
    p=Path(d); 子[:]=[x for x in 子 if x not in 跳]
    for f in fs:
        if not f.endswith(".py"): continue
        try: t=ast.parse((p/f).read_text(encoding="utf-8"))
        except Exception: continue
        for n in ast.walk(t):
            if isinstance(n,ast.Import): ns=[a.name.split(".")[0] for a in n.names]
            elif isinstance(n,ast.ImportFrom): ns=[(n.module or "").split(".")[0]] if not n.level else []
            else: continue
            for x in ns:
                if x and x not in 标准 and x not in 平台:
                    print(f"{(p/f).relative_to(根)}:{n.lineno} {x}")
PY

# 豁免字段误用面（零实例取证）
rg -n "豁免正向场景" -g '!.git' .
rg -n '"外部依赖"' -g '!.git' .
python3.14 -c "import glob,json;print(sum(1 for p in glob.glob('**/能力定义.json',recursive=True) if '\"外部依赖\"' in open(p,encoding='utf-8').read()),'/ 总',len(glob.glob('**/能力定义.json',recursive=True)))"

# 双份生成器
shasum -a 256 开发工具/依赖派生/生成依赖分两段.py ../系统工程平台_依赖派生/生成依赖分两段.py
```
