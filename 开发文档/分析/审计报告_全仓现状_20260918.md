# 全仓现状审计报告（2026-09-18 夜）

> 最后更新：2026-09-18 19:32
> 口径：实测（每条结论附取证命令；数字随并发建包逐分钟变化，引用前必须重跑）
> 维护者：主会话审计（只读）

## 0. 快照与读法

**审计快照**：`git rev-parse HEAD` = `7abdd12bbd92c8489859f86648a75fd67fa36c1b`（19:31:39）。
取证期间 HEAD **连续推进 3 次**（`76ec667e` → `43c7ce07` → `7abdd12b`），工作区另有 10 个已改 + 5 个未跟踪文件，
说明**本批 A–H 多路并发维修仍在写盘**。

因此本报告分两类结论，**必须区分读**：

- **【主干既存】**——在 HEAD 上可复现，与工作区脏改无关，是真正待处置的缺陷。
- **【并发在途/已闭合】**——审计期间被并发代理改掉，只作过程留痕，**不作为派活依据**。

**本条纪律是本次审计最硬的一条**：本仓同时有多会话写盘，任何未标快照的读数都会在一分钟后失效。

---

## 1. 门禁与测试红灯

### 1.1 【主干既存】能力 id 冻结基线口径与实际装配不一致（唯一确定性红灯）

```
$ python3.14 -m unittest 测试中心.开发工具.测试_能力id冻结基线门禁
Ran 13 tests in 1.446s
FAILED (failures=1)
AssertionError: 基线 id 集与装配口径不一致：装配多
  ['代码地图提供者.查询项目地图状态', '代码地图提供者.检查提供者', '代码地图提供者.重建项目地图索引']；基线多 []
退出码=1
```

精确差集（现场复算）：

```
$ python3.14 -c "import json; b=set(json.load(open('开发文档/项目证据/能力id冻结基线.json'))['条目']);
  from 开发工具.项目编译.正式包索引 import 构建索引; o=set(构建索引('.')['能力所有者']);
  print('基线',len(b),'装配',len(o)); print('装配多:',sorted(o-b)); print('基线多:',sorted(b-o))"
基线 686 装配 689
装配多: ['代码地图提供者.查询项目地图状态', '代码地图提供者.检查提供者', '代码地图提供者.重建项目地图索引']
基线多: []
```

**根因**（`git log` 实测）：基线三次重建 `fcc46e4a`=**681** → `ebfb910b`=**683** → `8f2e2293`=**686**（当前）。
此后提交 `878182ca` 新增 3 条 `代码地图提供者.*` 能力，**基线未同步重建**。

**为什么门禁本体却报「通过」**（同一快照实测）：

```
$ python3.14 -m 开发工具.能力id冻结基线门禁
[新增] 能力 id 新增 3 条（「只增不改」的合法方向，只报不拦）
能力 id 冻结门禁通过：当前集 ⊇ 基线集，无 id 消失（只增不改成立）   退出码=0
```

⇒ **门禁判据是「当前 ⊇ 基线」（只增不改），测试判据是「两者相等」**。
两个口径对同一对象给出相反结论，**且测试红无人认领**——这正是 `AGENTS.md`
「判据变更必须同批 grep 全仓同类断言」要防的形态。要么测试改为「只增不改」口径，
要么基线随新增能力同步重建（后者更符合 `未完成事项.md:12` 「按门禁口径重建基线」的既有做法）。
**处置前必须先裁定口径归属，不要两头各改一半。**

### 1.2 【并发在途·已闭合】组件规范支持库三处红灯

审计前半段实测到三处同时指向 `支持库/后端/组件规范支持库` 的红灯，**均属主干既存**（该包工作区当时干净）：

| 红灯 | 现场证据 |
|---|---|
| 摘要不闭合 | `python3.14 -m 开发工具.全量重算摘要 --只报` → `[待重算] 支持库/后端/组件规范支持库: ['文件摘要不一致: 实现/支持库模板生成器.py']`；另磁盘 28 文件 vs 登记 27（`实现/待作者补清单.py` 未登记） |
| 说明书能力级版本不一致 | 门禁报 `能力id=组件规范支持库.生成说明书 详情=1.1.0 ← 1.2.0` |
| 说明书缺参数 | 门禁报 `详情=允许重建骨架` |

**真因**：提交 `878182ca` 把 `能力定义.json` 的 `生成说明书` 由 1.1.0 升 1.2.0 并新增参数
`允许重建骨架`，**但未同步说明书、也未重算摘要**（摘要 mtime 18:55:27 早于源文件 18:56:35/18:56:45）。

**当前状态（19:31 复测，已闭合）**：

```
$ python3.14 -m 开发工具.全量重算摘要 --只报        → 全部闭合：无可重算项  退出码=0
$ python3.14 -m 开发工具.公开调用完整性门禁        → 门禁通过：无新增违规  退出码=0
```

⇒ 保留此节是为了记录**「改能力定义三件套不同步」是复发型缺陷**（该包 1 小时内二次复发），
不是当前债务。

### 1.3 其余门禁现状（同快照，全绿）

| 检查 | 结果 |
|---|---|
| `python3.14 -m 开发工具.环境自检` | **21 项全通过**，能力数 689、跳过包 0；退出码 0 |
| `公开调用完整性门禁` | 通过（存量基线 20 项只报不拦）；退出码 0 |
| `全量重算摘要 --只报` | 全部闭合；退出码 0 |
| `能力id冻结基线门禁` | 通过（口径见 §1.1 争议）；退出码 0 |
| 在飞新增文件 `py_compile` | 全部通过 |

---

## 2. 文档漂移：权威文档数字与代码事实不一致

权威文档 `开发文档/项目说明.md` 抬头「最后更新：2026-09-17」，其能力/包口径在 09-18
被并发建包整体改写，**成片漂移**。逐条现场复算：

| 出处 | 文档陈述 | 现场实测 | 判定 |
|---|---|---|---|
| `项目说明.md:42` | 源头包数 **140** | `find 支持库 模块库 技能库 平台控制面 运行核心 -name 包声明.json \| wc -l` = **141** | 不一致 |
| `项目说明.md:43` | 能力定义 **134** 份 | 同口径 `-name 能力定义.json` = **135** | 不一致（分母 140→141 与分子 134→135 同步漂移，差值 6 不变） |
| `项目说明.md:45` | 装配 **648**、跳过 0 | `正式包索引.构建索引('.')['能力所有者']` = **689**；`环境自检` 装配 = **689** | 不一致 |
| `项目说明.md:44` | 正式索引 114+18+1=**133 包** / 能力 **639** | 实测 支持库 **117** + 模块库 **19** + 技能库 **1** = **137 包** / 能力 **689** / 冲突 0 | 不一致 |
| `项目说明.md:54` | 内容寻址目录 **5 份**；激活指针版本 **105** | `ls -d 工程缓存/制品仓库/平台客户端制品/平台客户端-*` = **1**；`当前.json` 版本 **168** | 不一致 |
| `项目说明.md:1035` + `AGENTS.md:205` | 「全仓有 **10 个**聚合父包」 | 按「含子包」口径 = **8**；按「无 `能力定义.json`」口径 = **6** | 不一致 |

**其中两条是「同一文件自相矛盾」，比对外漂移更硬**：

1. **聚合父包数**：`项目说明.md:43` 说 6 个，`:1035` 说 10 个。现场两个口径都有事实支撑
   （6 = 无 `能力定义.json` 的聚合库；8 = 含子包目录的聚合包），**10 是丢失口径的历史值**。
   文档没写清「哪个口径数哪个」，读者无法自证。
2. **哲学主条数**：`项目说明.md:9`/`:106` 写「第 **1–13** 条」，但现场 `grep -c '^### 第'` = **14**
   （第 14 条于 2026-09-18 新增）。`AGENTS.md:256` 同写 1–13。而 `哲学总账.md:1`、
   `主开发文档.md:18` 已改 14——**四份权威文档对同一事实给出两个数**。
   另 `AGENTS.md:14` 写「1065 行哲学」，实测 `项目说明.md` = **1070 行**、`哲学总账.md` = **529 行**，
   「哲学」到底指哪一份也不清。

**规范侧同类漂移**：`开发文档/规范/场景覆盖率口径.md:106-109` 写能力 **681**、分母 135，
现场为 **689 / 136**；`:98` 写步骤总数 1039、正向 782，现场为 **1067 / 797**。

**处置口径建议**（供裁决，不在本次改动范围）：权威文档的数字类陈述应**只保留「口径 + 取证命令」，
不保留绝对值**，或统一加「快照时点」列。当前写法把 09-17 的绝对值与 09-18 的绝对值混排，
下一轮建包必然再次全部失效。

---

## 3. 结构债现场核实

### 3.1 【主干既存】`支持库/核心/` 空壳仍在（债务 #39①）

```
$ ls -la 支持库/核心/
-rw-r--r--  1 hekunhua  staff  40 Aug 22 21:46 __init__.py
drwxr-xr-x  3 hekunhua  staff  96 Sep 18 17:55 __pycache__
$ git ls-files 支持库/核心/
支持库/核心/__init__.py
```

`__init__.py` 内容仅一行 docstring `"""系统核心支持库能力域。"""`，**已被 git 跟踪**。
它是全仓唯一「既无实质实现、又不承载任何子包」的包目录。债务清单 #39① 现场成立。

### 3.2 【主干既存】死路径 `支持库/核心/类型转换`（债务 #39②）

`开发文档/方案/底座升级方案.md` 内：

```
$ grep -n "支持库/核心/类型转换" 开发文档/方案/底座升级方案.md | wc -l   → 4
$ grep -c "支持库/核心" 开发文档/方案/底座升级方案.md                    → 6
```

命中行 `:421`、`:424`、`:501`、`:570`（精确路径 4 处），另有 `:378`、`:398` 为裸前缀。
真位置实存：`支持库/后端/数据操作支持库/类型转换`。**条目写「6 处」是按裸前缀计、
文字描述却是「`支持库/核心/类型转换` 死路径」，计数口径与文字不严。**

### 3.3 【主干既存】21 个空目录（未被 git 跟踪，属本地残留）

```
$ python3.14 -c "…rglob 找空目录（排除 工程缓存/.git/__pycache__/归档/参考资料）…"
空目录数: 21
```

分布：`测试中心/` 12 个阶段目录（第三～十一、十三阶段、结构迁移、权威状态、资源并发）+
`测试中心/慢速层/` 3 个 + `测试中心/辅助/` 3 个 + `示例项目/可双击演示/产物文件夹` +
`支持库/后端/办公文档支持库/文档转换/实现`。
另 7 个孤儿 `.pyc`（同名 `.py` 已不存在）。

### 3.4 【设计上合法，但通用判据会误报】6 个聚合父包「缺件」

按「有 `包声明.json` 即正式包」扫描，124 个正式包中 6 个缺 `能力契约/`、`实现/`、`验证场景引用.json`：

`支持库/后端/{办公文档, 大语言模型, 数据操作, 文件系统, 系统核心, 网络通信}支持库`

**现场核实这 6 个确实无 `能力定义.json`**，与 `规范/说明书权威分工与维护规约.md:80` 所称
「6 大聚合父包」一致——**它们是聚合容器，缺件是设计结果不是缺陷**。保留此节是提醒：
任何新增的「六件齐全」判据都必须显式豁免这 6 个，否则会长期报假缺陷。

### 3.5 上帝文件（债务 #55）

全仓 982 个 `.py`，**12 个超过 1000 行**：

| 行数 | 路径 |
|---:|---|
| 3268 | `开发工具/发布门禁/运行发布门禁.py` |
| 2210 | `开发工具/契约编译/漂移检测.py` |
| 1573 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` |
| 1544 | `开发工具/公开调用完整性门禁.py` |
| 1371 | `运行核心/权威状态.py` |
| 1336 | `运行核心/统一网关/网关核心.py` |
| 1215 | `运行核心/统一网关/本地网关.py` |
| 1121 | `开发工具/环境自检.py` |
| 1090 | `运行核心/任务调度/任务进程.py` |
| 1061 | `运行核心/能力调用/超时执行.py` |
| 1037 | `运行核心/加载器/提供者隔离/独立进程.py` |
| 1016 | `支持库/后端/并发控制支持库/实现/并发控制.py` |

（`开发工具/组件合规/合规测试包.py` 999 行，临界未入选。）

---

## 4. 过程件堆积与文档生命周期

按 `规范/方案文档生命周期规范.md`（五态：起草/生效/已完结/已取代/已废弃）核查：

| 位置 | 现状 | 判定 |
|---|---|---|
| `开发文档/方案/` | 32 份，28 份合规五态 | 4 份不合规：`拆上帝文件执行方案_20260918.md`（无状态）、`端到端背压修复方案_20260918.md`（无状态，19:28 新落盘）、`稳定槽切换执行方案_20260918.md`（状态值「⏳ 待执行」不在五态内）、`二次审计落地编排_20260918.md`（状态自称「唯一编排入口」） |
| 编排入口唯一性 | `执行编排_唯一入口_20260918.md` 是唯一 H1 级编排 | **但 `二次审计落地编排` 正文自称「唯一编排入口」→ 两份并存，违反规范 §三** |
| `开发文档/分析/` | 17 份 | **17 份无一份使用规范五态声明**；其中 **16 份 ref=0**（无任何文档引用），标题多为「复算/盘点/回传/归因」，按 `AGENTS.md:259`「结论并入权威文档后立即删除」属**形态上应删** |
| `开发文档/临时文档/` | 2 份 | `01_补关键值断言_无法定值与剩余缺口_20260918.md` 主题 = C3 关键值口径，而 `未完成事项.md:13` 已宣告该项「无欠账」→ **结论已并入，未删** |
| 死路径总量 | `开发文档/` 正文（排除归档/参考资料）**284 条唯一死路径、463 处命中** | 集中于 `方案/`（406 处），多为「计划中迁移映射表」（如 `测试中心归口方案` 里改名前路径）与历史交付记录，**非全部是缺陷**；但 `方案/底座升级方案.md` 的 4 处属真死路径（§3.2） |

**唯一死 Markdown 链接**：`方案/项目文档治理方案_20260917.md:125 -> Y`（相对目录解析为 `开发文档/方案/Y`）。

---

## 5. 债务清单自洽性抽查

抬头汇总（`未完成事项.md:8-19`）**算术自洽**：`§8.3(6) + §8.4(3) + §8.6(1) + §8.7(21) = 31`，
逐节表行数复核一致。但**三条已登记结论与现场不符**：

| 条目 | 清单陈述 | 现场实测 | 判定 |
|---|---|---|---|
| `:12` #40 | 「已按门禁口径重建基线 **681→683**，**该项测试 OK**」 | 基线现为 **686**；测试 **FAILED**（§1.1） | **「测试 OK」已不成立**；683 是中间态 |
| `:13` #13 | C3 关键值：包目录 **135**｜正向步骤 **963** | 用同一加载链复算：包目录 **136**、正向目标步骤 **797**（零断言 0 仍成立） | 「零断言 0」成立，两个数字复算不出 |
| `:70` #36 | `客户端/构建平台客户端.py:738/739/744/747` 直调 | 现场命中行 = **832/843/871**；文件 **876 行**（旁注写 868） | 结论成立，**行号已漂移** |
| `:78` #39② | 死路径「**6 处**」 | 精确路径 **4 处**，裸前缀 6 处（§3.2） | 计数口径不严 |
| `:60` D-9 | 「README 实测命中『技能库/运维脚本』**13 处**」 | `grep -c '技能库/运维脚本' README.md` = **0**（README 已于 `7ebece38` 重写） | **已不成立** |

另 `§8.4` 编号为 `1,3,7`（缺 2、4–6），但**未像 §8.3/§8.5 那样加「编号不复用」说明**。

**决策记录编号**：45 份编号文件（`0001`–`0046`）+ `模板.md`。**无重号**；缺 `0010`，
经 `规范/Markdown 文档体例规范.md:138` 认定为「合法跳号，不需要补」。
状态声明格式不统一：18 份用 `> 状态：`，27 份用正文 `- 状态：`；**`0045_零配置自适应与安全红线.md` 全文无状态声明**。

---

## 6. 并发在途实况（不作为派活依据）

审计期间工作区持续变动，供协调者掌握写入面：

```
$ git status --porcelain   （15 项）
 M 公共契约/能力契约/契约.py
 M 支持库/后端/大语言模型支持库/完整性摘要.json
 M 支持库/后端/大语言模型支持库/模型连接器/__init__.py
 M 支持库/后端/大语言模型支持库/模型连接器/完整性摘要.json
 M 支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py
 M 支持库/后端/转写支持库/转写/完整性摘要.json
 M 支持库/后端/转写支持库/转写/实现/提供者.py
 M 支持库/适配层/MLXWhisper提供者/完整性摘要.json
 M 支持库/适配层/MLXWhisper提供者/实现/提供者.py
 M 运行核心/权威状态.py
 ?? 开发文档/分析/二次审计D_状态层硬化_回传_20260918.md
 ?? 开发文档/分析/二次审计E_回传_20260918.md
 ?? 支持库/后端/大语言模型支持库/模型连接器/实现/模型供应链校验.py
 ?? 支持库/后端/大语言模型支持库/模型连接器/数据/
 ?? 运行核心/时钟校准.py
```

对应债务清单 §8.7 的 **B9（模型二进制供应链，54）**、**B6（时钟校准，51）**、
**H 簇（零配置自适应 49，已见 `运行核心/自适应选型/`、`支持库/适配层/硬件画像探针.py`）**
等多路正在落地。审计首轮观察到的 `组件规范支持库` 三处红灯即在此期间被修掉。

---

## 7. 结论与建议处置顺序

**本次审计的确定性结论**：主干上有 **1 处测试红灯**（§1.1，能力 id 基线口径冲突）
与 **成片文档漂移**（§2），其余红灯均在审计期间被并发维修闭合。
结构债 `#39` 两条、上帝文件 12 个、21 个空目录、284 条死路径**现场均成立**。

**建议顺序**（只作建议，口径需裁决）：

1. **先裁 §1.1 的口径归属**——门禁「只增不改」vs 测试「必须相等」二者只能留一个，
   否则每次新增能力必然产生一条无人认领的红。裁定后同批修测试或重建基线。
2. **再收 §2 文档漂移**——`项目说明.md` 与 `AGENTS.md` 的数字类陈述改为「口径 + 取证命令 + 快照时点」，
   消除同文件自相矛盾（聚合父包 6 vs 10、哲学 1–13 vs 14）。这是**下一次建包必然复发的债**，
   只刷数字治标。
3. **顺手清过程件**——`分析/`16 份 ref=0、`临时文档/1 份`、`方案/` 4 份缺状态声明、
   两份编排入口并存；21 个空目录与 7 个孤儿 `.pyc` 可直接删（均未被 git 跟踪）。
4. **`支持库/核心/` 空壳与 4 处死路径**（#39）属小改，可并入任一波次顺手做。
5. **上帝文件拆分（#55）** 仍应**最后单独一波**——它要动 A–F 全部文件，与当前并发面正面冲突。

**本报告性质**：只读审计过程件，落在 `开发文档/分析/`。
按 `AGENTS.md`「结论并入权威文档后立即删除」，**§1.1、§2、§3 的结论应并入
`未完成事项.md` 与 `项目说明.md` 后删除本文件**，不长期并列于权威文档。
