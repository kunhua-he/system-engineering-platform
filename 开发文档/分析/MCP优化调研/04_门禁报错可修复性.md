# 静态检查/门禁报错「可直接修」的设计 —— B2 审计调研

- **最后更新**：2026-09-21
- **口径**：只读审计调研。所有结论以 GitHub 真实开源仓库的源码 / 规范文本 / README 为准，仓库均浅克隆到本机后逐条取证；star 数为 2026-09-21 通过 GitHub 仓库页实时读取的近似值。
- **维护者**：华哥 Agent（B2 路）
- **状态**：起草

## 一、本路要回答的问题

本项目的 MCP 调用链是：Hermes 薄壳（`mcp__system_engineering_toolkit__*`）→ 40007 唯一网关 → 能力目录 → 具体能力实现。本路负责一个节点：

- **B2（返回体层·门禁报红只给一行原文）**：门禁判红时返回的是「一行原文」，**不给落点（file:line:col）、不给修法（可复制的命令 / 可应用的替换）**，人只能拿着这行字再去全仓找、再猜怎么改，于是又变成多轮往返。

元判据（华哥原话）：

> 「你访问 3 次，和你一次访问。**本质上一次访问注入上下文再多，也没 3 次访问那么贵**。这个是实打实的问题。」

即 **往返次数 × 每轮全上下文注入 = 真成本**。任何只把返回变小、没减少往返的方案都不算解决问题。对本路的具体含义是：**一条红项如果一次没给全「哪一行 + 怎么改」，就注定要被再问一轮**——所以「报错可修复性」就是「往返次数」问题，不是排版问题。

## 二、调研方法与取证清单

（待填）

## 三、逐仓库审计

（待填）

## 四、硬问题回答

（待填）

## 五、照搬进本项目的落地代价

（待填）

## 六、剩余风险与未做项

（待填）
