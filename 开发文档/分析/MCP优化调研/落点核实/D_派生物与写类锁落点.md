# D 派生物与写类锁落点核实（D1 / D2 / zcode L7 / zcode L8）

<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

- 核实时点：2026-09-21 CST｜只读取证（未修改任何代码/配置）
- 取证方式：现场读源文件 + 现场跑能力（只在 `~/.hermes/cache/scratch` 造临时文件，仓库零改动）
- 解释器：`/opt/homebrew/bin/python3.14`（跑前 `unset PYTHONPATH`）
- 仓库根：`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`
- 口径：本文所有「现状」均带 `文件:行` 与原文片段；凡与审计报告表述不符的现场事实，集中在每节末尾「与任务描述不符」小节并汇总到 §五。

---

## 一、D1 编译器只产 6 份派生物，「6」写死在编译器里

### 现状（文件:行 + 原文）

**落点**：`开发工具/契约编译/能力定义编译器.py`

6 份派生物全部在 `编译能力定义` 一个函数体内**顺序语句**产出（`534-593`），逐条如下：

| # | 派生物（相对包目录） | 生成器函数 | 生成器定义行 | 写盘调用行 |
|---|---|---|---|---|
| 1 | `能力契约/参数契约.json` | `生成能力契约` | `296-350` | `559` |
| 2 | `包声明.json` | `生成包声明` | `364-390` | `577` |
| 3 | `__init__.py` | `生成注册入口` | `410-466` | `580` |
| 4 | `能力数据/能力搜索数据.json` | `生成搜索数据` | `469-484` | `584` |
| 5 | `能力数据/Agent查询数据.json` | `生成Agent数据` | `487-503` | `585` |
| 6 | `完整性摘要.json` | `生成完整性摘要`（委托 `支持库.后端.组件规范支持库`） | `515-525` | `592` |

原文（`534-593`，节选每份的写盘行，序号即上表）：

```
534|def 编译能力定义(定义文件: Path, 包目录: Path, 包id: str, 包名称: str,
...
550|    # 1. 能力契约（S0 唯一聚合格式；生成物经唯一聚合契约解析器自检）
559|    _写产物(契约路径, 契约文本, 结果)
564|    # 2. 包声明能力清单（保留既有额外顶层字段，如 句柄超时秒）
577|    _写产物(声明路径, 声明文本, 结果)
578|    # 3. 注册入口
580|    _写产物(入口路径, 生成注册入口(定义, 包id, 实现模块), 结果)
581|    # 4. 搜索数据 + Agent 数据
584|    _写产物(数据目录 / "能力搜索数据.json", 生成搜索数据(定义), 结果)
585|    _写产物(数据目录 / "Agent查询数据.json", 生成Agent数据(定义, 包id), 结果)
586|    # 5. 验证场景引用：编译器不再生成（v1 契约由能力作者手写维护），
591|    # 6. 完整性摘要
592|    _写产物(包目录 / "完整性摘要.json", 生成完整性摘要(定义, 包id, "实现", 包目录), 结果)
```

**串行还是并行**：**串行**。是单函数体内 6 段顺序语句，没有任何循环、没有清单常量、没有并行。`_写产物`（`596-613`）只管写一份，不知道一共有几份；`编译结果.产物列表`（`74`、`613`）是运行期「写到哪算哪」的收集，**不是可配置的注册表**。

**有没有可替代写死的注册表结构**：**没有**。全仓找不到「派生物清单」的单一常量，同一份清单被复制了 4 处：

- `能力定义编译器.py:8-13`（docstring 列举，5 行文字 ≈ 6 个文件）
- `开发工具/契约编译/生成物一致性检查.py:17-27`（docstring 里的比对口径表格）
- `开发工具/契约编译/生成物一致性检查.py:301-304`（import 6 个生成器函数）
- `开发工具/契约编译/漂移判定.py:439-442`（import 6 个生成器函数）

原文（`生成物一致性检查.py:300-308`）：

```
300|    try:
301|        from 开发工具.契约编译.能力定义编译器 import (
302|            生成Agent数据, 生成包声明, 生成能力契约, 生成注册入口, 生成搜索数据,
303|            校验能力定义, 从包推导编译参数, 读取能力定义,
304|        )
305|        from 开发工具.契约编译.聚合契约解析 import 读取原始
```

原文（`漂移判定.py:439-442`）：

```
439|    from 开发工具.契约编译.能力定义编译器 import (
440|        生成Agent数据, 生成包声明, 生成能力契约, 生成注册入口,
441|        生成搜索数据, 读取能力定义, 校验能力定义,
442|    )
```

**第 7 个生成器存在但不产**（`506-514`，`586-590` 的条件写盘形同虚设）：

```
506|def 生成验证场景引用(定义: dict[str, Any], 包id: str) -> str | None:
...
513|    """
514|    return None
```

**「拒绝覆盖」规则挂在写入口上**（`596-613`），这也说明写入口不是纯写：

```
602|    if 路径.is_file():
607|        if 路径.suffix in (".py", ".md") and 生成标记 not in 既有:
608|            if 路径.name == "__init__.py" and "def 注册能力" in 既有:
609|                return  # 手写入口含注册能力 → 合法，保留
610|            结果.问题列表.append(f"生成文件被手工修改: {路径.name}（缺少生成标记）")
611|            return
612|    路径.write_text(内容, encoding="utf-8")
```

### 改造点（精确到文件:行 + 改什么）

1. `开发工具/契约编译/能力定义编译器.py`：在 `生成标记 = ...`（`47`）之后新增模块级常量 **`派生物注册表`**，每项形如
   `{"名称": "参数契约.json", "相对路径": "能力契约/参数契约.json", "生成器": 生成能力契约, "依据源": "能力定义.json（+既有契约）", "时机": "前置", "读既有": True}`；
   6 份按上表填入，第 7 项 `验证场景引用.json` 显式登记 `"生成器": None, "不产原因": "v1 契约由作者手写"`（把「有意不产」从注释变成数据）。
2. `能力定义编译器.py:558-592`：把 6 段顺序语句换成「遍历 `派生物注册表`」的一次循环；`时机` 分两档（`前置`＝需要既有文件的非破坏性生成：参数契约/包声明；`后置`＝依赖包内全部文件的 完整性摘要）。
3. `能力定义编译器.py:596-613`：`_写产物` 增加 `注册项` 参数（至少带 `名称`），让 `产物列表` 回带注册表名称，取代「按 `路径.name` 反推类型」。
4. `生成物一致性检查.py:322-361` 与 `漂移判定.py:470-490`：比对循环改为读同一 `派生物注册表` 取「相对路径 + 生成器 + 依据源」，不再各自 import 6 个函数名；`生成物一致性检查.py:17-27` 的 docstring 表格改为指向注册表。
5. `能力定义编译器.py:506-514`：`生成验证场景引用` 保留（单一「不生成」落点），但其存在性由注册表第 7 项表达。

### 代价

- 改 **3 个文件**：`能力定义编译器.py`（新增注册表约 40 行；`编译能力定义` 由 60 行缩到约 25 行）、`生成物一致性检查.py`（约 45 行改循环）、`漂移判定.py`（约 20 行改循环）。
- 合计约 **120-130 行**（净增约 30-40 行）。

### 风险

- **顺序不能自由重排**：`完整性摘要.json` 把包内全部文件的 sha256 入账（`515-525` 委托唯一生成器），必须**最后**写；注册表若只用列表顺序表达，重排会静默产出错误摘要 → 循环里需对 `后置` 档做顺序断言。
- **`读既有` 语义不能丢**：参数契约（`552-558`）与包声明（`567-576`）是**非破坏性**生成，要读既有文件才不丢手写键；注册表漏掉这一位会退化成覆盖手写内容。
- **有测试盯着清单**：`测试中心/开发工具/测试_能力定义编译器.py`、`测试_漂移门禁.py`、`测试_编译非破坏.py` 断言生成物形状，改结构前先跑它们。
- `生成物一致性检查.py` 有只读护栏（`63-97`），改它时任何新增写路径会被当场拦下——这是设计，不是 bug。

### 与任务描述不符（D1）

- **编译器里没有「6」这个字面量**：`能力定义编译器.py` 全文无数字 6 的常量，硬编码形态是 `558-592` 的 **6 段顺序语句**；字面「6 类」出现在 **`生成物一致性检查.py:4`**（`契约编译器的产物有 6 类、说明书有 2 支生成器、依赖锁有 1 个正式入口`）。
- **实为「6 份实产 + 1 份显式不产」**：第 7 个生成器 `生成验证场景引用`（`506-514`）恒返回 `None`，所以「只产 6 份」的准确表述是 6 份落盘、1 份有意不产。
- **既有文档口径更大**：`开发文档/分析/生成物地图_20260918.md:13` 写「改一条源文件后，**最多有 7 份生成物要跟着变**（4 件编译器 JSON + `__init__.py` + `完整性摘要.json` + `说明/使用说明.md`）」——含说明书那一份。同一文档 `211-215` 还更正过「6 份生成物」口径：其中 `参数契约.json`/`包声明.json` 被编译器当**输入**读、`验证场景引用.json` 编译器已拒绝生成，算成「要跟着变的生成物」会把同步点虚高一倍。D1 的「6 份派生物」应据此标注口径边界。

---

## 二、D2 派生物互锁（说明书死结）

### 现状（文件:行 + 原文）

**落点**：`开发工具/契约编译/生成物一致性检查.py`（说明书三档判定在 `238-285`）

三档判定按顺序落，白名单是**第三档**：

```
262|        if G1账号 is not None and 现说明 == G1账号:
265|                            "一致", "与 G1 生成器逐字相同"))
266|        elif G2账号 is not None and 现说明 == G2账号:
270|                            "一致", "与 G2 生成器逐字相同"))
271|        elif 白名单条 is not None:
272|            结果.append(检查项("说明/使用说明.md", f"{相对包}/说明/使用说明.md",
273|                            f"（人工类·{白名单条.get('血统细分', '未细分')}）", "（人工）",
274|                            "手工维护",
275|                            f"白名单类别 {白名单条.get('类别')}：生成器不得覆盖"))
```

```
276|        elif G2账号 is not None:
277|            加, 减 = 差异行(现说明, G2账号)
278|            结果.append(检查项("说明/使用说明.md", f"{相对包}/说明/使用说明.md",
279|                            "组件规范支持库/实现/说明书生成器（G2）", "见生成器",
280|                            "不一致（形态漂移）",
281|                            f"G2 生成器多 {加} 行／现行多 {减} 行。★ 形态漂移≠缺陷："
282|                            f"该生成器拒绝覆盖已有文件，批量重生成会抹掉人工内容（见说明书白名单铁律）"))
```

白名单事实源（`171-176`）：

```
171|def 说明书白名单() -> dict[str, dict]:
173|    数据, 原因 = 读JSON(仓库根 / "开发文档" / "项目证据" / "说明书白名单.json")
176|    return {条["路径"]: 条 for 条 in 数据.get("白名单", []) if isinstance(条, dict) and 条.get("路径")}
```

**逐字比对绕开写入口**：比对对象是**直接调生成器函数得到的返回值**，不是「跑一次编译再读盘」：

```
340|    # 3.1 能力契约/参数契约.json —— 非破坏性口径：必须把既有契约传进生成器
342|    期望契约 = json.loads(生成能力契约(定义, 读取原始(契约路径) if 契约路径.is_file() else None))
349|    期望声明 = json.loads(生成包声明(定义, 编译参数["包id"], 编译参数["包名称"],
350|                              编译参数["包类型"], 编译参数["依赖"]))
357|    比JSON("能力数据/能力搜索数据.json", "能力数据/能力搜索数据.json",
358|         json.loads(生成搜索数据(定义)), "能力定义编译器.生成搜索数据", "能力定义.json")
371|            期望入口 = 生成注册入口(定义, 编译参数["包id"], 编译参数["实现模块"])
395|        期望摘要 = 生成完整性摘要(包目录, 包id=编译参数["包id"], 版本=str(定义.get("版本") or "1.0.0"))
```

比对函数本体（`322-338`）与 `__init__.py` 的**全文逐字**比对（`363-388`）：

```
331|        if 实际 == 期望对象:
332|            结果.append(检查项(生成物名, f"{相对包}/{相对文件}", 生成器名, 依据源, "一致", ""))
363|    # 3.4 __init__.py —— 只对带生成标记的入口判红；手写入口含 注册能力 是合法形态
370|        if 契约编译器生成标记 in 入口文本:
372|            if 入口文本 == 期望入口:
```

**检查器本身没有写能力**（`63-97`）：

```
80|    for 名 in ("write_text", "write_bytes", "mkdir", "replace", "unlink", "rmdir", "rename", "touch"):
83|            setattr(Path, 名, _拒绝(名))
88|        if isinstance(模式, str) and any(标志 in 模式 for 标志 in ("w", "a", "x", "+")):
91|                f"只读保证：生成物一致性检查不得写盘（已拦下 open({文件}, {模式!r})）。")
```

**生成器侧确实拒绝覆盖**（`支持库/后端/组件规范支持库/实现/说明书生成器.py`）：

```
179|    """校验并返回输出目标路径：默认拒绝覆盖；**骨架态**可显式重建；拒绝路径逃逸。
210|            raise FileExistsError(f"拒绝覆盖已有说明书: {目标路径}")
```

**死结机制（现场可判）**：

1. 想让说明书「重新生成到与 G2 逐字相同」——G2 生成器**拒绝覆盖已有文件**（`说明书生成器.py:210`），这条路物理上不通；G2 的逐字相同只可能来自「首次生成时恰好写对了」。
2. 想让「检查器顺手修一下」——检查器被只读护栏焊死（`63-97`），它连 `mkdir` 都拦，物理上没有写能力。
3. 于是白名单（`271-275`）成了唯一出口，且它的命中条件**排在两档逐字相同之后**：只要某次恰好与 G1/G2 逐字相同，人工类包就落回「一致」，白名单登记形同虚设。
4. `276-282` 又把「形态漂移」明确判为「**≠缺陷**」——于是它既不判红、又不能自动收口，只能人工逐份维护。`生成物地图_20260918.md:15` 记的稳定存量正是这一档：**14 条，全部是 `说明/使用说明.md` 形态漂移**。

### 改造点（精确到文件:行 + 改什么）

1. `生成物一致性检查.py:262-275`：把白名单分支**前移到三档之首**——白名单命中即判「手工维护（不可重算）」，不再要求先与 G1/G2 逐字不同（修掉「恰好逐字相同就落回一致」）。
2. `生成物一致性检查.py:276-282`：给「不一致（形态漂移）」补**机器可判字段** `漂移归属`（`生成器落后` / `人工已登记` / `未登记`），把注释里那句「形态漂移≠缺陷」变成数据；`未登记` 直接判红（逼登记进白名单）。判据可直接复用同文件 `差异行`（`158-168`）的行数。
3. `支持库/后端/组件规范支持库/实现/说明书生成器.py:179-210`：把「骨架态可显式重建」的开关显式暴露成参数（如 `覆盖=真` / `重建=真`），并把默认仍是拒绝覆盖；只有显式传参才允许覆盖。
4. `生成物一致性检查.py:340-361`：比对不再自行 import 6 个生成器拼装，改为经 `能力定义编译器` 的**只算不写**入口（配合 §一 改造点 2 的 `派生物注册表`），让它知道写入口的「拒绝覆盖」规则（`596-611`），消除第二份算式。

### 代价

- 改 **3 个文件**：`生成物一致性检查.py` 约 20-30 行、`说明书生成器.py` 约 5-10 行、`开发文档/项目证据/说明书白名单.json` 结构加一个字段（数据侧逐条补，条数按现状 8 份生成物类量级）。
- 合计约 **40-60 行** + 白名单数据逐条补字段。

### 风险

- **白名单前移会改口径**：原本判「一致」的人工类包会改判「手工维护」，`汇总()`（`426-435`）的「不一致总数」与退出码（`538`）随之变化。当前 `42` 行注明「本入口**不**接入任何门禁」，所以暂不炸门禁，但接线时口径必须先定。
- **给生成器开「覆盖」开关等于给批量重生成开闸**：必须限定为显式传参、且只对骨架态生效，否则正是白名单铁律要防的「抹掉人工内容」。
- **形态漂移转成可判字段会一次性产生一批红项**：历史 14 条要么补登记、要么改生成器侧输入源（`开发文档/规范/说明书权威分工与维护规约.md:267` 已记「另有 6 份生成物类已漂移未登记」待处理）。
- 说明书比对依赖 `开发文档/项目证据/说明书白名单.json` 存在且结构不变（`171-176` 读不到就退化成空表 → 所有人工类包都会落进 `276-282` 形态漂移档）。

### 与任务描述不符（D2）

- **「逐字比对约 300-308 行」不准确**：`300-308` 实际是 **import 块**（`301-304` 导入 6 个生成器函数 + 异常兜底 `306-308`）。真正的逐字/逐键比对落在 **`322-338`（`比JSON`）** 与 **`340-361`（6 份的调用）**，`__init__.py` 的**全文逐字**比对另在 **`363-388`**。
- 「绕开写入口」这一判断**成立**：比对直接调生成器函数返回值，完全不经过 `编译能力定义` / `_写产物`（`534-613`），因此对写入口的「拒绝覆盖」规则（`607-611`）一无所知。
- 白名单/形态漂移/「该生成器拒绝覆盖已有文件」三段行号与描述一致（`271-275`、`276-282`，后者即原文 `281-282`）。

---

## 三、zcode L7 写类乐观锁进必经路径

### 现状（文件:行 + 原文）

**落点 A（认领侧）**：`平台控制面/能力目录/实现/文件租约申请.py`

认领时**已经**现场算内容指纹并登记：

```
164|    # 现场指纹一律在**进独占区之前**算（对仓库文件的 IO 不该占着存储锁）；
166|    指纹表: list[str] = []
167|    for 相对路径, _声明 in 路径清单:
168|        指纹, 指纹问题 = 计算内容指纹(根, 相对路径)
170|            return 失败结果("内容指纹不可用", 指纹问题)
171|        指纹表.append(指纹)
212|        租约清单 = [
213|            存储.新租约(路径, 持有人, 承载任务, 秒, 文本(存储标签), 时刻,
214|                        内容指纹=指纹表[序号], 已声明指纹=声明)
215|            for 序号, 路径, 声明 in 可认领
216|        ]
```

登记字段与对外读数（`平台控制面/能力目录/文件租约存储.py`）：

```
249|        return {
250|            "租约id": uuid.uuid4().hex[:16], "键": 文件键(路径), "路径": 路径,
...
255|            "内容指纹": str(内容指纹 or ""), "已声明指纹": str(已声明指纹 or ""),
272|        对外["指纹一致"] = 指纹是否一致(记录.get("已声明指纹"), 记录.get("内容指纹"))
```

**但校验指纹缺省是「假」（不是必经路径）**：

```
120|def 申请文件租约(修改路径: Any = None, 所有者: Any = "", 任务: Any = "",
121|                 持有秒: Any = None, 存储标签: Any = "", 存储目录: Any = "",
122|                 校验指纹: Any = 假, 冲突策略: Any = "") -> Any:
156|    要校验指纹 = 校验指纹 is 真
197|        if 要校验指纹:
204|            if 不符:
206|                return 失败结果("内容已变更", (
```

**落点 B（编排侧）**：`模块库/开工编排/实现/开工编排.py` 的 `开工即占`（`604-678`）

传参只在显式 `真` 时才带（`653-654`），返回值**只取 `租约id清单`，把申请结果里的 `租约清单`（含 `内容指纹`）丢掉了**：

```
653|    if 校验指纹 is 真:
654|        申请参数["校验指纹"] = 真
655|    申请 = _调用(能力_申请文件租约, 申请参数)
664|    租约id清单 = list((申请.值 or {}).get("租约id清单") or [])
665|    return 结果.成功结果({
666|        "已开工": 真,
...
669|        "租约": {
670|            "状态": "已认领",
671|            "租约id清单": 租约id清单,
672|            "申请清单": 路径表,
673|            "认领数": int((申请.值 or {}).get("认领数") or len(租约id清单)),
674|            "说明": ("收工时按 租约id清单 调 平台控制面.能力目录.释放文件租约 释放；"
675|                    "提交前先调 查询文件租约(含现场指纹=真) 读「内容是否变更」做核对"),
```

契约也把返回口径写死成不含摘要（`模块库/开工编排/能力定义.json:189`）：

```
189|          "租约": "字典型，含 状态/租约id清单/申请清单/认领数/说明",
```

**落点 C（写回侧）**：`支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py`

写回**没有**自动校验摘要，`预期文件摘要` 缺省空串即跳过：

```
219|def 应用精确替换(
...
227|    预期文件摘要: str = "",
271|    旧文件摘要 = 计算文本摘要(原文本)
272|    预期文件 = (预期文件摘要 or "").strip().lower()
273|    if 预期文件 and 预期文件 != 旧文件摘要:
275|            "文件摘要不符",
```

而且本模块**明确不参与租约**：

```
11|本模块只做「定位与替换」，不参与业务账本、租约、权限或工作区治理。
```

现场核对调用方：`rg` 全仓，`应用精确替换` / `批量应用精确替换` 的**调用方只有** `测试中心/支持库/测试_文本补丁.py`，其余命中都是 `开发工具/薄壳/工具清单.py:109,113` 的说明文字与网关错误码表——**没有任何「先查租约/核对摘要再写」的编排**。

**现在只能靠调用方手调**（`平台控制面/能力目录/实现/文件租约维护.py`）：

```
295|def 查询文件租约(修改路径: Any = None, 存储目录: Any = "", 含现场指纹: Any = 假,
299|    **含现场指纹=真 时**：每条记录补一组**此刻现场算得**的读数（`现场指纹` /
300|    `现场指纹一致` / `内容是否变更`）
328|    if 含现场指纹 is 真:
331|            现场, _问题 = 计算内容指纹(根, str(对外.get("路径") or ""))
332|            对外["现场指纹"] = 现场
333|            对外["现场指纹一致"] = 指纹是否一致(对外.get("内容指纹"), 现场)
334|            对外["内容是否变更"] = not 指纹是否一致(对外.get("已声明指纹"), 现场)
```

### 改造点（精确到文件:行 + 改什么）

1. `模块库/开工编排/实现/开工编排.py:664-678`：把 `申请.值["租约清单"]` 原样回带（至少 `路径`/`租约id`/`内容指纹`/`已声明指纹`），在 `租约` 下新增 `认领摘要` 字段；同步改 `模块库/开工编排/能力定义.json:189` 的 `返回.值结构.租约` 描述。**约 6 行。**
2. `模块库/开工编排/实现/开工编排.py:653-654`：把「显式传 `真` 才校验」改为「条目带了声明指纹即自动校验」（`校验指纹 is None` 时按 `_提取路径认知`（`681-700`）是否产出过 `{"路径","指纹"}` 判定），让乐观锁进必经路径而不是可选开关；同步改 `能力定义.json:176-181` 的 `校验指纹` 参数说明。**约 3 行 + 契约 1 处。**
3. `支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py`：给 `应用精确替换`（`219-331`）与 `批量应用精确替换`（`368-559`）各加「写前自动核对」——新增可选参数 `租约存储目录`，写前按文件调 `查询文件租约(含现场指纹=真)`，`内容是否变更=真` 即失败 `内容已变更`；批量按已分好的 `分组序`（`448`）**每文件一次查询**，不逐条查。**约 30-40 行。**

### 代价

- 改 **2-3 个文件**：`开工编排.py` 约 10 行、`文本补丁.py` 约 40 行、`能力定义.json`（两包）2 处契约字段。
- 合计约 **40-60 行**。

### 风险

- **依赖方向可能反转**：`文本补丁.py` 是文件系统支持库的原子能力，docstring（`11`）明确「不参与…租约…」；把租约拉进写路径会引入 `平台控制面` 依赖。必须先定「支持库 → 平台控制面」是否合法，否则撞依赖防火墙（`生成物一致性检查.py:256-257` 记的正是这类真违规的判法）。
- **缺省行为一改会打到既有调用方**：`校验指纹` 自动生效后，凡「认领时未带指纹、之后再改」的会话会突然失败 → 建议分两步：本批只加**返回值**（改造点 1），缺省变更下一批做，且先全仓盘点调用方。
- **租约有默认持有期**：`文件租约存储.py:49` `默认持有秒 = 300.0`、`最长持有秒 = 86400.0`；写前核对可能碰到已过期租约（`是活跃` 已按「状态活跃 **且** 未过期」判，`82-103`）→ 需明确「过期租约算不算不符」。
- `查询文件租约` 全量会打爆上下文（`311-314` 记实测 412 条 / 250697 字符），写前核对必须带 `修改路径` 与 `摘要=真`。

### 与任务描述不符（L7）

- **租约认领侧「有回带」**：`申请文件租约` 的成功返回 `租约清单` 经 `文件租约存储.对外记录`（`259-273`）**原样带出 `内容指纹` / `已声明指纹` / `指纹一致`**。丢摘要的是**编排层** `开工即占`（`开工编排.py:664-678` 只取 `租约id清单`）与契约描述（`能力定义.json:189`）。
- 所以缺口的准确表述是三层，而不是「租约层没回带」：① **编排层丢弃**已算好的摘要；② **缺省不校验**（`文件租约申请.py:122` `校验指纹=假`、`开工编排.py:653` 只在显式 `真` 时传）；③ **写路径零接线**（`文本补丁.py:11` 明确声明不参与租约，全仓无「先查租约再写」的编排）。

---

## 四、zcode L8 写类原子写

### 现状（文件:行 + 原文）——**与任务描述不符，先纠正**

**落点 A**：`支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py` 的 `应用精确替换`（`294-314`）

**它已经是 tempfile + os.replace，不是直接 open/write**：

```
294|    已写入 = False
295|    if 写入:
296|        目录 = 目标.parent
297|        try:
298|            句柄 = tempfile.NamedTemporaryFile(
299|                "w", encoding="utf-8", dir=str(目录), delete=False,
300|                prefix=".__补丁_", suffix=".tmp")
301|            try:
302|                句柄.write(新文本内容)
303|                句柄.flush()
304|                os.fsync(句柄.fileno())
305|            finally:
306|                句柄.close()
307|            os.replace(句柄.name, 目标)
308|            已写入 = True
```

**落点 B**：同文件 `批量应用精确替换`（`510-539`），**每文件一次** tempfile + fsync + os.replace：

```
510|    # ── 阶段二：逐文件一次原子写（每文件只写一次） ──────────────────────────────
519|            try:
520|                句柄 = tempfile.NamedTemporaryFile(
521|                    "w", encoding="utf-8", dir=str(目标.parent), delete=False,
522|                    prefix=".__补丁_", suffix=".tmp")
...
525|                os.fsync(句柄.fileno())
527|                os.replace(句柄.name, 目标)
528|                已写入文件数 += 1
```

**真正的缺口：权限位不恢复**。`tempfile.NamedTemporaryFile` 建的临时件是 **0600**，`os.replace` 换名后目标文件权限被临时件覆盖，原权限丢失；两处都没有 `os.chmod` / `shutil.copymode`（`rg` 全文：`文本补丁.py` 里 `chmod`/`copymode`/`权限` 命中 0 处，只有 `11`、`602` 两行注释声明「权限治理由调用方负责」）。

**现场实测（本机 `/opt/homebrew/bin/python3.14`，临时文件在 `~/.hermes/cache/scratch`，仓库零改动）**：

```
单文件 应用精确替换：
  调用前权限: 0o644
  调用成功: True
  已写入: True
  调用后权限: 0o600          ← 权限被改成临时件的 0600
  NamedTemporaryFile 默认权限: 0o600

批量 批量应用精确替换（0o644 / 0o640 两文件）：
  调用前: 0o644 0o640
  成功: True 全部成功: True 已写入文件数: 2
  调用后: 0o600 0o600        ← 两个文件权限都被改成 0600
```

**对照：平台已有唯一原子写底座，文本补丁没走它**（`平台控制面/能力目录/单文件互斥存储.py:243-273`）：

```
243|    def 原子写文本(self, 正文: str, 权限: int = 0o600) -> None:
255|            描述符 = os.open(临时, os.O_WRONLY | os.O_CREAT | os.O_EXCL, 权限)
260|            os.fsync(描述符)
263|            os.replace(临时, self.存储文件)
273|        同步目录(self.存储文件.parent)
```

调用方 `文件租约存储.py:217` 显式传了权限：

```
217|            self.原子写文本(正文, 权限=0o644)
```

`单文件互斥存储.py` 还多做一步 `同步目录`（`60-73`），文本补丁两处**都没做目录 fsync**。

**另外两条更差的写路径**（同为文件系统支持库，同样不恢复权限）：

- `支持库/后端/文件系统支持库/文件操作/实现/文件系统.py:84-87`（`写入文件`）：`路径.parent.mkdir(...)` + `路径.write_text(内容, encoding=编码)` —— **直接写、非原子、无权限恢复**。
- `支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py:124-127`（`追加写入`）：`with open(路径, "ab") as f: f.write(字节缓冲)` —— **直接追加**。

### 改造点（精确到文件:行 + 改什么）

1. `文本补丁.py:295-314`：落盘前 `原权限 = os.stat(目标).st_mode & 0o777`（目标必存在，`263-264` 已判 `is_file`），`os.replace` 后 `os.chmod(目标, 原权限)`；并照 `单文件互斥存储.py:60-73` 补一次目录 fsync。**约 8-12 行。**
2. `文本补丁.py:512-539`：同上（每文件写前 stat 一次、写后 chmod）。**约 6-10 行。**
3. `文件系统.py:84-91`（`写入文件`）与 `文件系统补充.py:124-127`（`追加写入`）：二选一——要么也改成 tempfile + os.replace + 权限恢复，要么在能力 `说明` 里显式标注「非原子写、不参与写类锁场景」，把「谁知道它不原子」变成可判字段。**约 20-30 行（若都改）。**

### 代价

- **最小修**：只补 `文本补丁.py` 两处 → **1 个文件、约 15-22 行**。
- **完整修**（含 `文件系统.py` / `文件系统补充.py` 两条写路径）→ **3 个文件、约 35-52 行**。

### 风险

- **目录 fsync 会让写变慢**：批量上限 `批量编辑上限条数 = 64`（`334`），每文件多一次 fsync 会放大耗时；建议只在 `原子=真` 时做。
- **权限恢复有前提**：`应用精确替换` 的 `os.replace` 只在目标已存在时执行（`263-264` 先判 `is_file`），可安全取原权限；但 `批量应用精确替换` 的写盘循环（`513-527`）里**没有重新判存在性**，若两条编辑之间文件被外部删除，`os.stat` 会抛 `OSError` → 需 try/except 并按「新文件用默认权限」处理。
- **改 `写入文件` 会改语义**：原子写会消灭「写一半可见」的中间态，可能有测试或调用方依赖旧行为；改前先跑 `测试中心/支持库/测试_文本补丁.py` 与文件系统相关测试。
- **不要顺手把 `文本补丁` 改成走 `单文件互斥存储.原子写文本`**：那是平台控制面的互斥存储底座，引入它会带来跨层依赖（同 §三 风险 1）。

### 与任务描述不符（L8）

- **「直接 open/write（没有 tempfile + os.replace）」在 `文本补丁` 上不成立**：`应用精确替换`（`298-307`）与 `批量应用精确替换`（`520-527`）**已经是** 临时文件 + `flush` + `fsync` + `os.replace`，失败还删临时件（`309-314`、`529-539`）。契约里也写明了：`能力定义.json:184` `"副作用": "写入=真 时以临时文件 + os.replace 原子替换目标文件；写入=假 只预览"`。
- **真实缺口是权限位**：实测 0644/0640 → **0600**（临时件默认权限覆盖目标），两处都没有 `chmod`/`copymode`。
- **「直接 open/write」这个描述另有落点**：是 `文件系统.py:86` 的 `写入文件`（`路径.write_text(...)`）与 `文件系统补充.py:126` 的 `追加写入`（`open(路径, "ab")`），不在 `文本补丁`。

---

## 五、与任务描述不符的现场事实（汇总）

1. **L8 主判据错位**：`应用精确替换` / `批量应用精确替换` **不是**直接 open/write，已是 tempfile + `os.replace` 原子写；真缺口是**权限位不恢复**（实测 0644/0640 → 0600），且缺目录 fsync。「直接 open/write」的真身在 `文件系统.py:86`（`写入文件`）与 `文件系统补充.py:126`（`追加写入`）。
2. **D1「6 写死在编译器里」措辞不精确**：`能力定义编译器.py` 里没有数字 6 的常量，硬编码是 `558-592` 的 6 段顺序语句；字面「6 类」在 `生成物一致性检查.py:4`；另有第 7 个生成器 `生成验证场景引用`（`506-514`）**恒返回 None（有意不产）**，准确口径是「6 份实产 + 1 份显式不产」。既有文档口径更大：`生成物地图_20260918.md:13` 写「最多 7 份」（含说明书），`211-215` 还更正过「6 份」里混进了被当**输入**读的 `参数契约.json`/`包声明.json`。
3. **D2 行号偏移**：任务描述说的「逐字比对约 300-308 行」实际是 **import 块**（`301-304` + 异常兜底 `306-308`）；逐键/逐字比对在 `322-338`、`340-361`，`__init__.py` 全文逐字比对在 `363-388`。三段互锁（白名单 `271-275`、形态漂移 `276-282`、「该生成器拒绝覆盖已有文件」原文 `281-282`）与描述一致。
4. **L7 缺口层次与描述不同**：租约认领侧**已经**登记并回带内容摘要（`文件租约申请.py:166-171`、`212-216`；`文件租约存储.py:255`、`272`）；丢摘要的是编排层 `开工即占`（`开工编排.py:664-678` 只取 `租约id清单`，契约 `能力定义.json:189` 同步写死）。缺口是「编排层丢弃 + 缺省不校验（`文件租约申请.py:122` `校验指纹=假`）+ 写路径与租约零接线（`文本补丁.py:11`）」。

---

## 六、取证命令（可复跑，全部只读）

```bash
cd "/Users/hekunhua/Documents/Agent/PHP/系统工程平台"
# D1：6 份派生物的写盘行
rg -n "_写产物\(" 开发工具/契约编译/能力定义编译器.py
# D1/D2：同一份清单的 4 处复制
rg -n "生成能力契约|生成搜索数据|生成Agent数据|生成注册入口|生成包声明" --glob '*.py' -l
# D2：三段互锁
sed -n '262,285p' 开发工具/契约编译/生成物一致性检查.py
# L7：写路径有没有接线租约
rg -n "文本补丁.应用精确替换|文本补丁.批量应用精确替换" --glob '*.py'
# L8：文本补丁里有没有权限治理
rg -n "租约|chmod|权限|copymode|同步目录|fsync" 支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py
```

现场跑权限实测（仓库零改动，临时件落在 scratch）：

```bash
cd ~/.hermes/cache/scratch && unset PYTHONPATH
# 单文件：0644 → 调用 应用精确替换 → 读回权限
# 批量：0644/0640 → 调用 批量应用精确替换 → 读回权限
# 两次实测结果均为 0o600
```
