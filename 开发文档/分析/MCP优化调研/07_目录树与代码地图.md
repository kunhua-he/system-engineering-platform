# 07 目录树与代码地图：给 LLM 看仓库结构的业界成熟做法（E1 审计）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 最后更新：2026-09-21
- 口径：本机 clone 真实源码（`/Users/hekunhua/.hermes/cache/scratch/开源调研/07/<仓库名>/`，`--depth 1`）逐文件核对；star 数为 shields.io 取值，**会变**，取证命令见文末附录。
- 状态：已完成
- 维护者：MCP审计调研-E1（子代理）

---

## 一、结论摘要（先给答案）

1. **aider 的 repo map 不是「目录树」，是「按 PageRank 排序的符号摘要」**：用 tree-sitter 的 tags 查询抽 `def`/`ref`，建 `networkx.MultiDiGraph`，跑 `nx.pagerank(weight="weight", personalization=...)`，再把节点排名按出边权重摊回「(文件, 符号)」，最后用**二分查找**逼近 token 预算（`get_ranked_tags_map_uncached`）。它给我们的启示是**「排序 + 预算 + 二分」三件套**，而不是「树」本身。
2. **我们要的「一棵带忽略规则和深度限制的树」在业界是成熟且极小的实现**：`modelcontextprotocol/servers` 的 filesystem server 里 `directory_tree` 工具就是一个 `readdir(withFileTypes)` 递归 + `minimatch` 排除，源码不到 80 行，返回 `{name, type, children[]}` JSON。**这是最值得直接照搬的一件**。
3. **12.4 秒的 `find` 与 0.07 秒的 `rg` 的差距，本质是「有没有忽略规则 + 有没有单进程遍历」**：aider/repomix/gitingest 三家都各自维护了一份「默认忽略清单」（`.git`、`node_modules`、`__pycache__`、锁文件……），并在遍历时套用；repomix 还额外用 globby 走 git 感知排序。**我们的 `文件管理.列出文件` 目前只有单层、无忽略规则、无深度限制**（见第二节原文）。
4. **token 预算不能靠"估"，要靠真实 tokenizer**：gitingest 用 `tiktoken.get_encoding("o200k_base")`，aider 用模型自带 `token_count`（>200 字符时采样外推），repomix 走 `gpt-tokenizer`。三家的做法都指向：**返回体里必须带一个「这个结构大概多少 token」的数字**，LLM 才知道该不该继续下钻。
5. **可直接当依赖用的候选**（许可证/体积/Python 兼容，详见第五节）：
   - `pathspec`（gitignore 语义，纯 Python，MIT）——**最推荐**；
   - `tiktoken`（已在本机 Python 生态内，gitingest 在用）；
   - tree-sitter 系（`tree-sitter` + `tree-sitter-language-pack`）——**重量级，仅当要做符号级地图时才引入**；
   - ctags（GPL-2.0，二进制）——**许可证与部署形态都要掂量**。

---

## 二、本项目 E1 现状（真源原文，非记忆）

节点 E1 的原话是「只有单层『列出目录』，没有『树』；实测 `find` 一棵树 12.4 秒，`rg` 只要 0.07 秒」。

真源 `模块库/文件管理/能力定义.json`（第 313–364 行，`文件管理.列出文件`）：

- 能力id：`文件管理.列出文件`，版本 `1.0.0`，中文名「列出文件」
- 说明原文：**「列出目录下条目名称（排序），返回 结果[名称列表]」**
- 参数：**只有 1 个** —— `目录路径`（文本型，必填，说明「待列出目录绝对路径」）
- 返回：`结果型`，值结构 `{成功: 逻辑型, 值: 列表型}`
- 错误码：`目录不存在` / `目录读取失败` / `提供者不可用`

**契约层面的三个缺口（对着上面原文逐条比）**：

| 缺口 | 现状 | 业界对应做法 |
|---|---|---|
| 无深度 | 参数里没有任何深度字段 | `directory_tree` 递归；repomix/gitingest 全树 + 忽略 |
| 无忽略规则 | 参数里没有任何排除字段 | `directory_tree.excludePatterns`；repomix `defaultIgnoreList`；gitingest `DEFAULT_IGNORE_PATTERNS` |
| 无类型/无 token 估算 | 返回 `值` 是裸 `列表型`（只有名字） | `directory_tree` 返回 `{name,type,children}`；gitingest 摘要里带 `Estimated tokens` |

⇒ **E1 不是"慢"这一个问题，是"契约里根本没有树这个概念"**。慢只是没有忽略规则后的必然结果（12.4 秒的 `find` 是把 `.git`/`工程缓存` 一起走了一遍）。

---

## 三、逐个仓库审计

### 3.1 `Aider-AI/aider` —— repo map（本方向最对口）

- 地址：https://github.com/Aider-AI/aider
- star：约 49k（取证：`curl -s https://img.shields.io/github/stars/Aider-AI/aider.json`）
- 许可证：Apache-2.0
- 关键文件：`aider/repomap.py`（867 行）、`aider/queries/tree-sitter-language-pack/<语言>-tags.scm`、`aider/special.py`、`aider/models.py::get_repo_map_tokens`
- 本机源码路径：`/Users/hekunhua/.hermes/cache/scratch/开源调研/07/aider/`

#### （1）符号抽取：tree-sitter tags 查询，def/ref 两类

`get_tags_raw(fname, rel_fname)`（第 279–363 行）流程：

1. `lang = filename_to_lang(fname)` → `get_language(lang)` / `get_parser(lang)`（来自 `tree_sitter_languages` 或 `tree-sitter-language-pack`，见文件头 `USING_TSL_PACK`）；
2. 读 `aider/queries/<pack>/<lang>-tags.scm`，`Query(language, query_scm)` 跑 `parser.parse(...)` 的根节点；
3. 捕获名按前缀分类：`name.definition.*` → `kind="def"`，`name.reference.*` → `kind="ref"`，其余丢弃；
4. 产出 `Tag(rel_fname, fname, name, kind, line)`。

查询文件本身极小，例如 `python-tags.scm` 全文：

```scheme
(module (expression_statement (assignment left: (identifier) @name.definition.constant) @definition.constant))
(class_definition name: (identifier) @name.definition.class) @definition.class
(function_definition name: (identifier) @name.definition.function) @definition.function
(call function: [ (identifier) @name.reference.call
                 (attribute attribute: (identifier) @name.reference.call) ]) @reference.call
```

**兜底机制（值得抄）**：若某语言只给出 `def` 没有 `ref`（源码注释明说 cpp 就是这种），就用 Pygments 的 `Token.Name` 词法 token 全部当 `ref` 补回去（第 347–363 行）。

#### （2）建图与排序：PageRank + 加权边

`get_ranked_tags(...)`（第 365–574 行）：

- 数据：`defines: {符号 → {文件}}`、`references: {符号 → [文件]}`；只保留**同时被定义又被引用**的符号（`idents = set(defines) & set(references)`，第 468 行）。
- 图：`G = nx.MultiDiGraph()`，边方向是 **referencer → definer**（第 514 行 `G.add_edge(referencer, definer, weight=use_mul * num_refs, ident=ident)`）。
- **权重乘子（`mul`，第 487–514 行）——这是"排序"的全部秘密**：

| 条件 | 乘子 |
|---|---|
| 符号被用户**在聊天里提到**（`mentioned_idents`） | × 10 |
| 符号是 snake/kebab/camel 命名**且长度 ≥ 8** | × 10 |
| 符号以 `_` 开头（私有） | × 0.1 |
| 该符号**被 >5 个文件定义**（低频价值） | × 0.1 |
| 引用方文件**在 chat 里**（当前正在改的文件） | 额外 × 50 |
| 引用次数 | 取 `math.sqrt(num_refs)` 做压缩，防止高频低价值提及压过一切 |

- 无引用的定义加自环 `weight=0.1`（注释说明是为了兼容 tree-sitter 0.23.2 的 ruby 解析差异）。
- **个人化向量**：`personalize = 100 / len(fnames)`；文件在 chat 里 +personalize，文件在 `mentioned_fnames` 里取 `max`，**路径分量或文件名命中 mentioned_idents 也 +personalize 一次**（第 424–445 行）；然后 `nx.pagerank(G, weight="weight", personalization=..., dangling=...)`（第 520–525 行）。
- **摊回定义**：把每个源节点的 rank 按出边权重比例分摊到目标 `(文件, 符号)` 上累加（第 533–545 行），排序得到 `ranked_definitions`；**chat 里已有的文件会被 `continue` 跳过**（第 556–557 行，因为它们整份已在上下文里，不需要摘要）。

#### （3）token 预算：二分查找逼近 + 15% 容差

- **预算从哪来**（`aider/models.py::get_repo_map_tokens`，第 782–789 行）：`map_tokens = max_input_tokens / 8`，再 `min(…, 4096)`、`max(…, 1024)`；用户可用 `--map-tokens` 覆盖（`main.py` 第 964–967 行）。
- **没有 chat 文件时放大视野**（`get_repo_map`，第 120–132 行）：`target = min(max_map_tokens * map_mul_no_files, max_context_window - 4096)`，其中 `map_mul_no_files` **默认 8**（构造函数默认值，第 56 行）⇒ 空聊天时会尝试给到接近上下文上限的树。
- **二分查找**（`get_ranked_tags_map_uncached`，第 629–706 行）：

```python
middle = min(int(max_map_tokens // 25), num_tags)   # 初值：按 token 预算/25 估"每个符号约 25 token"
while lower_bound <= upper_bound:
    tree = self.to_tree(ranked_tags[:middle], chat_rel_fnames)
    num_tokens = self.token_count(tree)
    pct_err = abs(num_tokens - max_map_tokens) / max_map_tokens
    ok_err = 0.15                                     # 误差 <15% 就收工
    if (num_tokens <= max_map_tokens and num_tokens > best_tree_tokens) or pct_err < ok_err:
        best_tree, best_tree_tokens = tree, num_tokens
        if pct_err < ok_err: break
    if num_tokens < max_map_tokens: lower_bound = middle + 1
    else:                           upper_bound = middle - 1
    middle = int((lower_bound + upper_bound) // 2)
```

即：**在"取前 N 个符号"上二分**，每轮真的渲染一次再数 token，允许 15% 超/欠。这是"预算内选节点"的完整机制。

- **token 计数本身也做了省算**（`token_count`，第 89–101 行）：< 200 字符直接交给模型 tokenizer；否则抽 100 行样本，按 `sample_tokens / len(sample_text) * len_text` 外推。
- **渲染**（`to_tree`，第 748–784 行）：按 `sorted(tags)` 分组，同文件输出 `文件名:\n<TreeContext 摘要>`，每行**截断到 100 字符**（防 minified js 撑爆）。
- **缓存**：`.aider.tags.cache.v{CACHE_VERSION}` 目录（第 43 行，`CACHE_VERSION` 当前 3/4 分语言包），`refresh` 支持 `auto|always|files|manual`（第 57、592–608 行）。
- **特殊文件优先**：`filter_important_files(other_rel_fnames)`（`aider/special.py`）结果被 **prepend 到 ranked_tags 最前面**（第 656–662 行），保证 `README`/构建文件这类"没符号但重要"的文件先入选。

**⇒ 对我们的直接可搬项**：不是整个 PageRank（我们暂时没有"改哪些文件"的 chat 上下文），而是
① 先给「重要文件」白名单置顶（README/说明/能力定义.json）；
② 按"名字长度 ≥ 8 / 被引用次数 / 目录深度"给一个可解释的权重；
③ 用**二分 + 真 token 计数**逼近预算，而不是拍一个 `max_files`。

### 3.2 `tree-sitter/tree-sitter` —— 符号解析底座

- 地址：https://github.com/tree-sitter/tree-sitter
- star：约 27k；许可证：MIT；本机体积 7.1M（`--depth 1`）
- 机制：C 核心 + 各语言独立 grammar 仓库，运行时用 S-expression 查询（`.scm`）在语法树上做模式匹配，产出带位置信息的捕获。
- 在本方向里的位置：**aider 与 repomix 都用它，但用法完全不同**——
  - aider 用它做 `def`/`ref` 抽取 → 建引用图 → PageRank（见 3.1）；
  - repomix 用它做**签名压缩**（见 3.3）。
- 对我们（Python 3.14 后端）的代价：`tree-sitter` 有官方 Python binding（`pip install tree-sitter`），但**每种语言要单独装 grammar 包**（aider 干脆依赖 `tree-sitter-language-pack` 打包合集）。装 3–5 种语言的体积与编译成本，远超"给 LLM 一棵树"的需求。

**判断：本阶段不该引入 tree-sitter**。我们的需求是「目录树 + 忽略规则 + token 预算」，符号级地图是**下一级需求**（对应 D1「改 1 个能力要同步 N 个文件」那种"谁引用了谁"的问题），届时再引入更划算。

### 3.3 `yamadashy/repomix` —— 仓库打包（tree + 忽略 + token 三件套齐备）

- 地址：https://github.com/yamadashy/repomix
- star：约 28k；许可证：MIT；体积 16M
- 本机源码：`/Users/hekunhua/.hermes/cache/scratch/开源调研/07/repomix/src/core/`

关键结构（真实目录）：

```
src/core/file/         fileCollect.ts / filePathSort.ts / fileTreeGenerate.ts / fileProcessContent.ts / fileSearch.ts ...
src/core/output/outputStyles/   xmlStyle.ts / markdownStyle.ts / plainStyle.ts
src/core/tokenCount/   buildTokenCountStructure.ts / types.ts
src/core/treeSitter/   parseFile.ts / languageConfig.ts / parseStrategies/{Python,TypeScript,Go,Vue,Css,Default}ParseStrategy.ts / queries/query*.ts
src/core/security/     （secretlint 密钥扫描）
src/core/metrics/      （每个文件的字符/token 统计）
src/config/defaultIgnore.ts   defaultIgnoreList
```

- **默认忽略清单**（`src/config/defaultIgnore.ts`，实测原文分节）：版本控制 `.git/** .hg/** .hgignore .svn/**`；依赖目录 `**/node_modules/** **/bower_components/** **/jspm_packages/** vendor/** **/.bundle/** **/.gradle/** target/**`；日志 `logs/** **/*.log **/npm-debug.log*`；运行时数据 `pids/** *.pid`；覆盖率 `coverage/** .nyc_output/**`；构建中间物 `.grunt/**` ……（按用途分节注释，可读性高）。
- **排序是显式实现的**（`src/core/file/filePathSort.ts`，`sortPaths`）：按 `/` 切段逐段比较，**同一层里目录排在文件前面**（`if (!isLastA && isLastB) return -1; // Directory`），段相等时按段数（路径长度）排。注释还专门解释了 Windows `path.sep` 归一化问题 —— 说明这是被真实 bug 打磨过的实现。
- **输出格式**：XML（默认）/ Markdown / JSON / Plain 四种；XML 头部是 `<file_summary>`（含 `<purpose>` 一段说明"这是给 AI 的仓库打包"）+ `<directory_structure>` + 逐文件 `<file path="...">`（`src/core/output/outputStyles/xmlStyle.ts`）。
- **token 统计**：`tokenCount/` + `metrics/`，per-file 与 overall 两级。
- **签名压缩**：`treeSitter/parseFile.ts` + 各 `ParseStrategy` + `queries/query<Lang>.ts`，用于 `--compress` 把函数体压成签名（这是"在预算里塞更多文件"的正统做法）。

**对我们的可搬项**：`filePathSort.sortPaths` 的排序语义（目录优先、逐段比较、同级按名字）与 `defaultIgnoreList` 的**分节写法**，可以直接翻译成中文默认清单。

### 3.4 `cyclotruc/gitingest` —— 「一次给全」的摘要式打包

- 地址：https://github.com/cyclotruc/gitingest
- star：约 16k；许可证：MIT；体积 1.2M（**很轻**）
- 本机源码：`/Users/hekunhua/.hermes/cache/scratch/开源调研/07/gitingest/src/gitingest/`

关键文件与行数（`wc -l` 实测）：`ingestion.py` 325、`output_formatter.py` 210、`query_parser.py` 190、`utils/ignore_patterns.py` 240、`utils/git_utils.py` 531、`clone.py` 176、`entrypoint.py` 352。

- **输出三段式**（`output_formatter.py::format_node`，第 27–62 行）：`summary` + `tree` + `content`；其中 `tree = "Directory structure:\n" + _create_tree_structure(...)`，summary 末尾追加 **`Estimated tokens: {token_estimate}`**。
- **token 估算用真 tokenizer**（第 181–210 行）：`tiktoken.get_encoding("o200k_base")`（注释标明对应 gpt-4o / gpt-4o-mini），失败降级为 warning 不报错（"网络下载不到就跳过估算"）。
- **树渲染**（`_create_tree_structure`，第 129–178 行）：递归 children，用 `is_last` 决定 `└──` / `├──` 前缀 —— 与我们在终端看到的 `tree` 命令同款。
- **默认忽略清单**（`utils/ignore_patterns.py::DEFAULT_IGNORE_PATTERNS`，第 7 行起，实测原文按语言分组）：Python 组 `*.pyc *.pyo *.pyd __pycache__ .pytest_cache .coverage .tox .nox .mypy_cache .ruff_cache .hypothesis poetry.lock Pipfile.lock`；Node 组 `node_modules bower_components package-lock.json yarn.lock .npm .yarn .pnpm-store bun.lock bun.lockb`；Java 组 `*.class *.jar *.war *.ear *.nar .gradle/ build/ .settings/ .classpath *.gradle .project`；C/ObjC 组 `*.o *.obj *.dll *.dylib *.exe` ……（**这份清单几乎可以逐条抄**，中文项目再补 `工程缓存/`、`*.tmp`）。
- 查询解析支持 `--include/--exclude` 通配、子路径、分支、最大文件大小（`query_parser.py` 190 行）。

**对我们的可搬项**：**「summary + tree + token 估算」三段式**——这正是 E1 需要的返回体形状；`DEFAULT_IGNORE_PATTERNS` 直接翻译成中文默认忽略表。

### 3.5 `sourcegraph/scip` —— 代码智能索引格式（符号级，远期）

- 地址：https://github.com/sourcegraph/scip
- star：约 810（**小众但权威**，它是 Sourcegraph 的新一代索引格式）；许可证：Apache-2.0；体积 3.9M
- 关键文件：`scip.proto`（**唯一事实源**）、`docs/scip.md`、`docs/DESIGN.md`、`docs/CLI.md`、`cmd/`（CLI 实现）、`bindings/`（各语言绑定）
- `scip.proto` 顶层消息（实测 `rg "^message |^enum "` 输出）：`Index` / `Metadata` / `ToolInfo` / `Document` / `Symbol` / `Package` / `Descriptor` / `Signature` / `SymbolInformation` / `Relationship` / `Occurrence` / `Diagnostic` / `SingleLineRange` / `MultiLineRange`；枚举 `ProtocolVersion` / `TextEncoding` / `PositionEncoding` / `SymbolRole` / `SyntaxKind` / `Severity` / `DiagnosticTag` / `Language`。
- 机制要点：符号用**结构化字符串** `scheme manager package version descriptor+` 表达（`Symbol` + `Package` + `Descriptor`）；`Occurrence` 记录每个符号出现的位置与 `SymbolRole`（定义/引用/实现/…）；`Relationship` 表达 is_implementation / is_reference 等关系；`Document` 承载一个文件的所有 occurrence。
- **与我们的关系**：这是"跨仓库、可增量、可持久化的符号索引"，比 aider 的临时 repo map 重得多。**本项目已有 `.codegraph/codegraph.db`（83MB）在做同类事**（任务信工具姿势里明确写了"查符号/调用链先查代码地图"）⇒ 我们**不缺符号级索引**，缺的是**索引之上给 LLM 的"树/地图"视图**。SCIP 的 `SymbolRole`/`Relationship` 字段设计可作我们代码地图后续扩展的参考，但**本轮不需要引入**。

### 3.6 `universal-ctags/ctags` —— 最快的符号表产出器

- 地址：https://github.com/universal-ctags/ctags
- star：约 7.3k；许可证：**GPL-2.0**（⚠ 与我们的分发形态需评估）；体积 43M
- 关键文件：`docs/man/ctags.1.rst`（手册真源）、`docs/`（`extending.rst` / `developers.rst` / `output-format.rst` 等）
- 机制与可用的输出格式（`docs/man/ctags.1.rst` 实测原文）：

  - `--output-format=(u-ctags|e-ctags|etags|xref|json)`（第 309 行）；
  - `json` 格式**需要编译时开启 jansson**（第 317–319 行原文："`json` format is available only if…"），另有独立手册页 `ctags-json-output(5)`；
  - `-x` / `--output-format=xref` 输出**制表符分隔、人可读的交叉引用表**（第 332 行）；
  - `--list-output-formats` / `--list-languages` 可枚举能力（第 1319 行）；
  - 字段控制 `--fields`、`--extras`、`--kinds-<LANG>`，JSON 下字段类型由 `--fields-<LANG>` 控制（第 1140 行提到 JSON type）。

- **对我们的价值**：如果要"一次拿到全仓符号表"，`ctags -f - --output-format=json --fields=+n -R <目录>` 是**单进程 C 实现，比 Python 遍历快一个量级**；但 ① GPL-2.0；② 需额外安装二进制；③ 对我们的"目录树"需求**过重**。**建议：仅在"符号级地图"立项时评估，不进本轮。**

### 3.7 `microsoft/lsif-node` —— LSIF（语言服务器索引格式）的参考实现

- 地址：https://github.com/microsoft/lsif-node
- star：约 199；许可证：MIT；体积 8.6M
- 结构（实测 `ls -1`）：`protocol/`（LSIF 协议定义）、`lsif/`、`tsc/`、`tsc-tests/`、`language-service/`、`sqlite/`、`tooling/`、`samples/`
- 机制：把 TypeScript 语言服务的语义结果（定义/引用/悬停）导出为 LSIF 图（顶点+边），落地为文件或 SQLite，供代码托管平台做"跳转/查找引用"。
- **行业现状**：Sourcegraph 已用 SCIP 取代 LSIF（LSIF 的主要维护动力已转向 SCIP）⇒ **LSIF 是"上一代"，我们不该以它为目标格式**；它的价值是**证伪了一条路**：为"给 LLM 看结构"而自建一套完整索引格式，代价极高（这个仓库 8.6M 才勉强覆盖 TS 一种语言）。

### 3.8 `modelcontextprotocol/servers` 的 filesystem server —— 现成的「目录树」MCP 工具

- 地址：https://github.com/modelcontextprotocol/servers（`src/filesystem/`）
- star：约 91k（整仓）；许可证：GitHub 未识别出单一 SPDX（仓库内各 server 分目录各自声明，`filesystem` 目录为 MIT）
- 本机源码：`/Users/hekunhua/.hermes/cache/scratch/开源调研/07/servers/src/filesystem/index.ts`

`directory_tree` 工具的**真实契约原文**（第 547–561 行）：

```
title: "Directory Tree"
description: "Get a recursive tree view of files and directories as a JSON structure.
  Each entry includes 'name', 'type' (file/directory), and 'children' for directories.
  Files have no children array, while directories always have a children array (which may be empty).
  The output is formatted with 2-space indentation for readability. Only works within allowed directories."
inputSchema: { path: string, excludePatterns: string[] (optional, default []) }
outputSchema: { content: string }
annotations: { readOnlyHint: true, openWorldHint: false }
```

实现（第 570–587 行）：`fs.readdir(validPath, { withFileTypes: true })` 递归，对每个条目算 `relativePath`，用 `minimatch(relativePath, pattern, { dot: true })` 匹配；**非通配模式会三种写法都试一遍**：精确/`**/pattern`/`**/pattern/**` —— 也就是「写 `node_modules` 就能同时命中该目录本身与其下全部内容」。返回 `TreeEntry {name, type, children?}`，`JSON.stringify(tree, null, 2)`。

**这是与 E1 逐字对应的现成实现**：递归 + 排除模式 + 类型标注 + JSON 结构 + `readOnlyHint` 注解（只读语义）。

### 3.9 `sourcegraph/sourcegraph` —— 未能取到（如实标注未完成）

shields.io 对 `sourcegraph/sourcegraph` 返回 `repo not found`，本机 `--depth 1` clone 亦未产出可用工作副本（该仓库体量极大）。**本报告不含该仓的一手结论**，代码智能部分以 3.5（`sourcegraph/scip`）与 3.7（`microsoft/lsif-node`）为准。**这一项如实标注为未完成，不用记忆补写。**

**补充一条比 clone 更重要的现场事实**：继续深挖本项目时发现，**平台里已经有一份近期（2026-09-21 提交 `65808c3f`）落地的同类实现** ——`文件系统支持库.文件操作.目录树` 与 `代码解析支持库.仓库地图`（PageRank + token 二分）。审计时不查自己仓库是最好的返工制造机，详见第四、五节。

## 四、E1 的硬问题正面回答

### 4.1 问题一：aider 怎么在 token 预算内选节点？

完整机制（源码见 3.1，全部可复现）：

1. **排序输入**：tree-sitter tags 查询抽出所有 `def`/`ref`，只保留「既被定义又被引用」的符号 ⇒ `(文件, 符号)` 候选集。
2. **图与权重**：`MultiDiGraph`，边 `referencer → definer`，权重 = `乘子 × sqrt(引用次数)`；乘子表见 3.1（被提及 ×10、命名规范且 ≥8 字符 ×10、私有 ×0.1、被 >5 文件定义 ×0.1、引用方在 chat 里 ×50）。
3. **个性化 PageRank**：`personalize = 100 / 文件数`，chat 里/被提到的文件与"路径分量或文件名命中被提到的标识符"都加 personalization ⇒ `nx.pagerank(weight="weight", personalization=..., dangling=...)`。
4. **摊回**：把每个源节点的 rank 按出边权重比例分摊到目标 `(文件, 符号)` 并累加排序；chat 内文件跳过（整份已在上下文）。
5. **预算选择（关键）**：**在"取排名前 N 个符号"的 N 上二分**，每轮真的渲染一次并数 token，`pct_err < 0.15` 即收工；初值 `min(max_map_tokens // 25, 符号总数)`（隐含假设"每符号约 25 token"）。**粒度是符号，不是截断字符串**。
6. **预算从哪来**：`max_input_tokens / 8`，夹在 `[1024, 4096]`；聊天为空时乘 `map_mul_no_files=8` 并夹到 `上下文窗口 - 4096`。
7. **重要文件置顶**：`aider/special.py::filter_important_files` 的结果无论排名如何都排在最前。

⇒ 一句话：**PageRank 定次序，二分定长度，tokenizer 定真假**。

### 4.2 问题二：我们只需要「一棵带忽略规则和深度限制的树」，最小实现是什么？

**最小实现 = 递归 scandir（单进程、不 fork）+ 一份默认忽略目录名集合 + 深度上限 + 条数上限 + 三段式返回**。业界可直接抄的参考：`mcp__…` 的 `directory_tree`（递归 + `excludePatterns` minimatch）与其 `{name, type, children[]}` 结构。

**本机实测（2026-09-21，同一仓库 `系统工程平台`，11G / 含 `工程缓存`）**：

| 做法 | 结果 | 耗时 |
|---|---|---|
| `find . -print`（全仓，含 `工程缓存`/`.git`） | 487291 个路径 | **12.196 秒** |
| `find . -not -path "*/工程缓存/*" -not -path "*/.git/*"` | 7260 个路径 | **12.179 秒**（**没有变快**） |
| `rg --files`（遵守 .gitignore） | 3896 个文件 | **0.04 秒** |
| Python `os.walk` + 目录名忽略集 | 3916 个文件 | **0.037 秒** |
| Python 线程池并行 scandir + 忽略集 | 3916 个文件、深度 11 | **0.097 秒** |
| **`文件系统支持库.文件操作.目录树`（平台已有能力，深度=2 / 条数上限=200）** | 节点数 200、目录 120、文件 80、已截断=true、截断处 `示例项目/示例项目.py` | **网关耗时毫秒 3.7** |

**结论（这是本节最有价值的一条实测）**：`find` 那 12.4 秒**根本不是"遍历慢"，是"遍历了不该遍历的东西"** —— 加 `-not -path` 排除后仍然 12.18 秒（几乎零变化），说明开销全在**进程对 487291 个 inode 的 `lstat` 系统调用**上；一旦换到**单进程 `scandir` + 先按目录名剪枝**，同一棵树是 **毫秒级**（`os.walk` 0.037 秒 / 平台能力 3.7 毫秒）。⇒ **E1 的"12.4 秒"不是需要新算法，是需要"不 fork、不 lstat 全量、先剪枝"这一个姿势**。

### 4.3 问题三：有没有现成项目可以直接当依赖用？

**先说结论：对本项目当前状态，最优解不是引依赖，而是复用平台自己已有的 `目录树`。** 但若确需引依赖，逐项清白如下（许可证 / 体积 / Python 兼容，均为本机实测或仓库原文）：

| 候选 | 许可证 | 体积（`--depth 1`） | Python 就绪度（本机实测） | 判断 |
|---|---|---|---|---|
| **`pathspec`**（gitignore 语义） | MIT | 纯 Python 包 | **本机未安装** | 若要"真读 `.gitignore`"（不只是目录名黑名单），这是唯一正统选择；纯 Python、无编译、体积小 —— **推荐作为首选** |
| `tiktoken` | MIT | 轮子 | **本机已装（`find_spec` 为真）** | 做真 token 估算的唯一低成本选择；gitingest 用的就是 `o200k_base` |
| `networkx` | BSD-3 | — | **本机已装** | 建图/PageRank；平台已自研退化实现（见 4.4），**不建议再引** |
| `pygments` | BSD-2 | — | **本机已装** | 只做"没 ref 时的词法兜底"（aider 的兜底腿），价值低 |
| `tree_sitter` | MIT | 7.1M（核心） | **本机已装** | ⚠ 核心装了不等于语言能用：**每种语言要另装 grammar 包**（aider 靠 `tree-sitter-language-pack` 合集）；平台已把它收在 `支持库/适配层/TreeSitter提供者`，**不要再引入第二条腿**（违反单一实现口径） |
| `universal-ctags` | **GPL-2.0** | 43M | 非 Python 包（可执行文件） | ⚠ GPL 与分发形态要评估；且需额外安装二进制 ⇒ **不进本轮** |
| `repomix` | MIT | 16M | **Node.js/TypeScript** | 只能参考机制（`sortPaths`、`defaultIgnoreList`、XML/签名压缩），**不能当 Python 依赖** |
| `gitingest` | MIT | 1.2M | Python 包（**体积最小**） | 若要"一次给全整仓"，它是最省事的；`DEFAULT_IGNORE_PATTERNS` 与三段式输出可直接抄 |
| `lsif-node` | MIT | 8.6M | Node.js | 为目标格式而引它不值得（8.6M 才覆盖 TS 一种语言，且已被 SCIP 取代）⇒ **不引** |
| `scip` / `scip.proto` | Apache-2.0 | 3.9M | Go 为主 | 它是**格式定义**，可作字段设计参考；平台已有代码地图，⇒ **不引** |

## 五、与本项目节点的对应（E1 的缺口矩阵）

**关键事实：平台已经在 2026-09-21（提交 `65808c3f`）落地了本节最需要的两件东西**（审计发现的现场事实，非任务信所述）：

### （1）`文件系统支持库.文件操作.目录树`（已有，去重缺口 A）

- 契约原文（`支持库/后端/文件系统支持库/文件操作/能力定义.json` 第 1336–1404 行）：参数 `目录路径`(必填) / `最大深度`(默认 3，0=不限) / `包含文件`(默认真) / `名称模式`(glob，只作用于文件) / `排除目录`(默认为空 → 用平台默认排除 `.git、__pycache__、工程缓存、node_modules、.venv、dist、build`) / `条数上限`(默认 500) / `含大小`(默认假)；返回 `{树文本, 节点数, 目录数, 文件数, 已截断, 截断处}`。
- 实现（`实现/目录树.py`，132 行）：`os.scandir` + `entry.is_dir(follow_symlinks=False)`（软链当文件，避免环）+ 同层按名称升序 + 超限如实截断回报；**纯标准库、纯只读**。
- 实测：网关耗时 **3.7 毫秒**（深度 2 / 上限 200）。

### （2）`代码解析支持库.仓库地图.建符号图` + `取仓库地图`（已有，去重缺口 B）

- 包说明原文：「从代码仓抽定义/引用建加权有向图落 SQLite（nodes/edges 与 代码地图 同构），并按 **Personalized PageRank + token 预算二分**渲染『文件 + 符号签名』紧凑文本地图」。
- `取仓库地图`（能力定义第 52–71 行）：参数 `仓库根`(必填) / `锚点`(空=均匀分布) / `token预算`(默认 4096，1~200000) / `库文件`；返回 `{地图文本, 收录文件数, 收录符号数, 预算, 已用预算, 是否截断}`。
- **token 估算口径写明是「字符数/3 向上取整」（中文场景保守，不用 4 字符/token）**，见 `实现/地图渲染.py::估算token` —— 即 **平台已有 `大语言模型支持库.上下文压缩.估算token数`，但没有用 tiktoken 的真 tokenizer**。
- `实现/页排名.py` 里明确写着「退化实现与 networkx 同口径（衰减系数 0.85、权重归一、悬挂节点质量均摊）」⇒ 平台**自研了 PageRank 退化实现**，没有引 networkx。
- `实现/地图渲染.py` 注释记录了真实踩坑：「按文件二分时，只要排名第一的文件放不下就整张地图空掉（**实测 200 token 下收录文件数=0**）；按符号二分能得到最大可容纳前缀」——**这正是 aider 做法的价值所在，且平台已独立踩过同一个坑并解决**。
- 实测：`取仓库地图` 调用返回 **HTTP 404 `文件不存在`**（`工程缓存/仓库地图/系统工程平台.db` 未生成）⇒ **能力在、图未建**；要用先调 `建符号图`。
- `建符号图` 的忽略口径与 `目录树` 一致（`.git、__pycache__、工程缓存、node_modules、.venv、dist、build`），但**参数只有目录名黑名单，没有 glob/`.gitignore` 语义**。

### 缺口矩阵（对着上面原文逐条比）

| 业界做法 | 平台现状 | 缺口级别 |
|---|---|---|
| 多层树 + 深度限制 | ✅ `目录树.最大深度`（默认 3，0=不限） | **无缺口** |
| 忽略规则 | ⚠ 只有**目录名精确匹配**的默认 7 项 + 调用方整表替换；**既不读 `.gitignore`，也不支持 glob**（`repomix.defaultIgnoreList` 87 条、`gitingest.DEFAULT_IGNORE_PATTERNS` 按语言分组） | **真缺口（小）** |
| 排序 | ⚠ 仅「同层按名称升序」；缺 repomix 的"**同层目录优先于文件**"语义（`filePathSort.sortPaths`） | **真缺口（极小）** |
| 节点类型/结构 | ⚠ `目录树` 返回的是**树文本**（缩进文本），不是 `directory_tree` 的 `{name,type,children}` JSON | 可选 |
| token 预算 | ⚠ 树本身**没有 token 估算**；仓库地图有但口径是"字符数/3"**估算**，不是真 tokenizer | **真缺口（中）** |
| 符号级地图 | ⚠ 能力已有、**图未建**（实测 404） | **落地缺口（不是开发缺口）** |
| 重要文件置顶 | ❌ 无 `filter_important_files` 等价物（README/说明/能力定义.json 不会自动排前） | 真缺口（小） |

## 六、照搬过来的落地代价（改哪 / 改多大 / 风险）

**前提：本轮为只读审计，以下均为评估，未做任何改动。**

### 方案甲：最小改动 —— 复用 `目录树`，不改代码（成本≈0）

- 做什么：E1 的"发现层往返次数"问题，**直接用 `文件系统支持库.文件操作.目录树` 一次调用拿整棵树**（实测 3.7 毫秒，替代 12.4 秒的 `find`）。
- 改哪：**不改代码**，只改调用姿势与薄壳工具描述（把 `目录树` 放进常用能力清单）。
- 风险：极低。唯一风险是**调用方不知道它存在**（本轮实测：`能力搜索` 关键词「目录树」命中数=1，能搜到 ⇒ 风险已缓解）。

### 方案乙：补三个小缺口（改动小、无契约破坏）

1. **同层目录优先**：改 `实现/目录树.py` 的 `条目表.sort(...)` 排序键为 `(不是目录, 名称)`。
   - 代价：1 行；风险：**排序稳定**行为字段声明了 `排序稳定: true`，顺序变化的**输出字节会变**，任何对树文本做逐字快照的测试/场景需同步（**这是唯一需要留意的联动**）。
2. **忽略规则升级为 glob + 可选读 `.gitignore`**：在 `排除目录`（目录名）之外加一个 `忽略模式`（glob，如 `*.log` / `工程缓存/**`）。
   - 代价：新增 1 个**可选参数**（契约只增不改，合规）+ 实现里加 `fnmatch.fnmatch` 匹配；纯标准库，不引 `pathspec`。
   - 若要"真读 `.gitignore`"：需引 `pathspec`（MIT、纯 Python，本机未装）⇒ **建议先不做**，先用 glob 覆盖 90% 场景。
3. **返回体加 `估算token` 字段**：复用平台既有 `大语言模型支持库.上下文压缩.估算token数` 的口径（字符/3）或在其上加 tokenizer 腿。
   - 代价：返回**只增字段**（契约合规），实现里 1 次 `len(树文本)` 运算；风险极低。

### 方案丙：接上已有但未建图的符号地图（成本低但收益大）

- 做什么：对仓库跑一次 `代码解析支持库.仓库地图.建符号图`，之后 `取仓库地图` 就能按 PageRank + token 二分拿"文件 + 符号签名"地图（实测当前 404 = 未建图）。
- 代价：一次建图调用（写 SQLite 到 `工程缓存/仓库地图/`）；后续每次取地图是只读查询。
- 风险：① `建符号图` 是**写盘副作用**（新建/整表重建 SQLite），需确认 `工程缓存/` 的清理策略不会误删；② 地图的 token 口径是估算（字符/3），**与薄壳 `返回上限字符=6000` 的裁剪口径不是同一把尺子**，可能出现"地图说 4096 token，网关按字符裁掉一半" ⇒ **两把尺子要对齐**（这是本轮最值得提醒的隐性风险）。

### 不建议做（明确排除）

- ❌ 引 `tree-sitter` 做符号抽取：平台已在 `支持库/适配层/TreeSitter提供者` 收口，**再引第二条腿违反单一实现口径**；且符号级需求已被 `仓库地图` 覆盖。
- ❌ 引 `networkx`：`实现/页排名.py` 已自研同口径退化实现（注释明写 alpha=0.85、权重归一、悬挂质量均摊），**引库会造成第二实现**。
- ❌ 引 `ctags`：GPL-2.0 + 额外二进制 + 需求被覆盖。
- ❌ 引 `lsif-node` / 自建 LSIF/SCIP 索引：平台已有 `.codegraph/codegraph.db`（83MB）与 `仓库地图`，**重复造索引格式是纯浪费**。

## 七、元判据对照（往返次数 × 每轮上下文注入）

按华哥尺子逐条判：

| 方案 | 往返次数 | 单次返回体 | 判定 |
|---|---|---|---|
| 现状：`find` 走 `执行命令`，目录多时要多次 + 缓过 15 秒前台上限 | 多次 | 小但不完整 | **最差**：往返多且单次还超时 |
| 现状：`文件管理.列出文件`（单层、无忽略、无深度） | **N 次**（每层一次） | 每次都很小 | **坏在往返**：拿一棵 3 层树要 ≥3 次往返，每次都付全上下文注入 |
| 方案甲：`目录树` 一次调用 | **1 次** | 较大（有 `条数上限` 兜底） | **正解**：往返从 N → 1，返回体虽大但一次装完 |
| 方案丙：`取仓库地图`（token 预算 4096） | **1 次** | 有预算上限 | **正解**：带预算的一次性地图 |
| 反例：把 `directory_tree` 返回拆成"目录一层一个工具" | 更多 | 更小 | **没解决问题**，只是把返回变小 |

⇒ **E1 的修法必须落在"一次调用给全"上**；任何"把树拆小、多调几次"的方案按元判据一律不合格。

## 八、附录：取证命令（数字会变，命令不变）

```bash
# star 数 / 许可证（shields.io，本机需走 ClashX 代理）
curl -s "https://img.shields.io/github/stars/Aider-AI/aider.json"
curl -s "https://img.shields.io/github/license/Aider-AI/aider.json"

# 仓库拉取（--depth 1，落 /Users/hekunhua/.hermes/cache/scratch/开源调研/07/<仓库名>/）
export https_proxy=http://127.0.0.1:4780 http_proxy=http://127.0.0.1:4780
/Library/Developer/CommandLineTools/usr/bin/git clone --depth 1 https://github.com/<仓库全名>.git <目录>

# aider：排序与预算机制（三处）
rg -n "def get_ranked_tags|add_edge|pagerank" <aider>/aider/repomap.py
rg -n "def get_ranked_tags_map_uncached" -A78 <aider>/aider/repomap.py
rg -n "def get_repo_map_tokens" -A9 <aider>/aider/models.py

# repomix：排序与默认忽略
rg -n -A30 "export const sortPaths" <repomix>/src/core/file/filePathSort.ts
rg -n -A40 "defaultIgnoreList" <repomix>/src/config/defaultIgnore.ts

# gitingest：默认忽略与 token 估算
rg -n "DEFAULT_IGNORE_PATTERNS" -A100 <gitingest>/src/gitingest/utils/ignore_patterns.py
rg -n "o200k_base|Estimated tokens" <gitingest>/src/gitingest/output_formatter.py

# MCP filesystem：目录树工具契约与实现
rg -n -A15 '"directory_tree"' <servers>/src/filesystem/index.ts

# 本项目：目录树契约 / 仓库地图契约
rg -n -A70 '"能力id": "文件系统支持库.文件操作.目录树"' 支持库/后端/文件系统支持库/文件操作/能力定义.json
rg -n -A21 '"能力id": "代码解析支持库.仓库地图.取仓库地图"' 支持库/后端/代码解析支持库/仓库地图/能力定义.json

# 本项目：12.4 秒 find 与毫秒级替代（复现第四节表格）
time find . -print | wc -l
time rg --files | wc -l
```

---

**一句话总结**：E1 的要害不是"没有树"，而是"只有单层 + 没有剪枝 + 走了会 fork 全量 `lstat` 的路"；**平台里已经有一份毫秒级的 `目录树` 和一份 PageRank + token 二分的地图实现**，本轮真正该做的三件小事是：**排序把目录提前、忽略规则升到 glob、返回体补一个 token 数（并与网关裁剪口径对齐）**。
