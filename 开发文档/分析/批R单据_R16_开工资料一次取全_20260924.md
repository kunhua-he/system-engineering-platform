<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-16 单据：开工资料一次取全（`开工编排.开工准备` 新增 `资料` 段）

> 取数时点：2026-09-24 11:45–12:05（Asia/Shanghai）｜开工ID：`开工-20260924-114521-3265`
> 纪律：一切经 MCP（`capability_search` → `capability_call`）；**未跑编译、未 git 提交**；只改本单据「变更清单」列出的文件。
> 华哥目标原话：「我希望的就是纯用 mcp，然后尽量都功能后面变成，1 次运行，能获取开工所有的资料。这是我的目标，但是控制住上下文不能撑爆」。

## 一、参数与返回设计

### 1.1 只增两个参数（哲学第 5 条 2 项：协议只增不删不改名）

| 参数 | 类型 | 默认 | 取值 | 说明 |
|---|---|---|---|---|
| `资料级别` | 文本型 | `指针` | `指针` / `摘要` / `完整` | 指针＝只给指针与计数；摘要＝指针 ＋ 预算内片段正文；完整＝不投影（把现有的都给全） |
| `资料预算` | 整数型 | `8000` | ≥0（`0` = 不限） | 本次新增 `资料` 段的字符上限 |

- 非法取值一律 `参数不合法`（实测三条：`资料级别='xx'`、`资料预算=-1`、`资料预算='8'`）。
- **不传这两个参数 ⇒ 返回体里 `资料`/`资料用量` 两键一个都不出现**（硬判据三要求与改前逐字节一致）。
- 档位常量与预算/降级阶梯是**进程内纯函数**，单独成件：`模块库/开工编排/实现/开工资料.py`（与既有 `返回体量分档.py` 同构）。
- `资料预算` 只约束 `资料` 段；改前既有各段仍由 `细节级别` 管（否则就会破坏判据三）。

### 1.2 返回新增 `资料` 段（六项）

`资料` 段键名（9 键，三档相同）：

`开工ID` / `租约id清单` / `涉及路径` / `租约查询问题` / `契约与判据指针` / `规范片段` / `规范片段问题` / `能力映射` / `纪律段`

六项与事实源（**只聚合与投影，不产生新事实**）：

| # | 项 | 键 | 事实源（转调，不重写） |
|---|---|---|---|
| ① | 开工ID ＋ 租约id清单 | `开工ID` / `租约id清单` | 本次 `开工ID` ＋ `平台控制面.能力目录.查询文件租约` 活跃记录 |
| ② | 涉及路径 | `涉及路径` | `路径校验.通过清单` ＋ 活跃租约 → 逐条 `占用状态`（`活跃租约`/`真缺口`/`未占`/`未查询`） |
| ③ | 契约与判据指针 | `契约与判据指针` | 逐包读 `能力定义.json` / `能力契约/参数契约.json` / `开发文档/项目证据/能力id冻结基线.json` 的**原文行区间** |
| ④ | 规范片段 | `规范片段` | `项目文档支持库.查询索引`（按 `任务` 关键词） |
| ⑤ | 能力映射 | `能力映射` | `公共契约/能力契约/常用意图.py`（**唯一事实源**，与薄壳工具描述同源） |
| ⑥ | 纪律段 | `纪律段` | 顶层 `通用纪律` 同名三段（命令环境/提交纪律/工具姿势），**只投影、不重写** |

设计要点：

- **占用状态 fail-closed**：租约腿查不出来 ⇒ `未查询`（**不回落成 `未占`**），并在 `租约查询问题` 里如实写原因。
- **指针一律 `文件:行区间`**：`_按行读` 显式 `允许大文件=真 / 精简=假` 读原文，避免精炼改行号 ⇒ 发出「看着能用、指不到原文」的假指针。
- **⑤ 不手抄第二份**：映射收到两边都能依赖的最底层 `公共契约/能力契约/常用意图.py`；薄壳 `开发工具/薄壳/工具清单.py` 里 `★ 常见意图→首选能力` 那句改由 `渲染映射文本()` 生成（模块 import 薄壳是逆向依赖，故不收在薄壳里）。

### 1.3 硬预算与降级阶梯（超预算一律指针）

超预算按**固定顺序**降级，每一级都保留指针：

1. `规范片段正文→指针`
2. `纪律段正文→指针`（正文本来就在同一次调用的顶层 `通用纪律` 里）
3. `涉及路径明细→指针`（去 `占用者`/`租约id`/`存在`）
4. `契约逐能力明细→指针`（留包级三条指针）

降到底仍超 ⇒ `资料用量.是否超预算=真` ＋ `超预算原因`（**不静默**）。
`资料级别=完整` 或 `资料预算=0` ⇒ 不投影（`预算管辖` 如实标注）。

## 二、六项键名与条数（读数）

取数：`修改路径` = 本单据写集 5 条路径；租约响应取**经 MCP 真调**回来的那一份（见 §四·须确认项 4）。下表是**声明面（`模块库/开工编排/能力定义.json`）改完之后**重取的一套读数（③ 的行区间随之右移）。

| 档 | 资料段字符数 | 总返回字符数 | 是否超预算 | 降级项 |
|---|---|---|---|---|
| `指针`（默认） | **3,361** | 12,616 | 假 | — |
| `摘要` | 4,482 | 13,737 | 假 | — |
| `完整` | 166,181 | 175,432 | 假（不受预算约束） | — |

六项条数（三档）：

| # | 项 | 指针 | 摘要 | 完整 |
|---|---|---|---|---|
| ① | 开工ID ＋ 租约id清单 | `开工-20260924-R16-真读数` ＋ **5** 条 | 同 | 同 |
| ② | 涉及路径 | **5** 条（5 条全 `活跃租约` ＋ 租约id） | 同 | 同 |
| ③ | 契约与判据指针 | **4** 包（`模块库.开工编排` 4 条能力 ＋ 3 个非正式包各 1 条文件指针） | 同 | 同 |
| ④ | 规范片段 | **5** | **3** | **20** |
| ⑤ | 能力映射 | **4** | 4 | 4 |
| ⑥ | 纪律段 | **3** 段 | 3 | 3 |

③ 指针样例（能力 `开工编排.开工准备`）：
`能力定义=模块库/开工编排/能力定义.json:7-134`｜`参数契约=模块库/开工编排/能力契约/参数契约.json:5-89`｜`门禁基线=开发文档/项目证据/能力id冻结基线.json:1808-1812`

④ 指针样例：`开发文档/未完成事项.md:112-213`（指针档只给指针 ＋ 「传 资料级别=摘要 或 完整 取片段正文」）。
⑤ 四条：读文件=`文件系统支持库.文件操作.读取文件`／内容检索=`文件系统支持库.内容检索.正则搜索`／跑命令=`系统核心支持库.进程管理.执行命令`／找能力=`能力目录.搜索能力`。
⑥ 三段体积：命令环境 3 条／提交纪律 5 条／工具姿势 1 项。

## 三、四条硬判据（逐条读数）

### 判据一：一次调用拿齐六项 —— **达成**

一次 `开工准备(资料级别=指针)` 回 `资料`（9 键）＋ `资料用量`（8 键），六项条数见 §二；新增顶层键只有 `资料`/`资料用量` 两个。

### 判据二：`资料级别=指针` 总返回 ≤ 预算，且超预算项必有指针 —— **达成**

- 资料段 **3,361** ≤ 预算 **8,000** ⇒ 超预算 假、降级项 空。（本节反向那三条用的是 **2 条路径的确定性桩租约**，基数较小：桩档 2,551。）
- ④ 每条都有 `文件:行区间`：真；③ 每条三腿都有 `文件:行区间`：真。
- **指针能照取回原文**（反向核）：把 12 条指针逐条 `读取文件(起始行/结束行)` 读回来，**12/12 文本里含该能力 id**。
- 反向（弄坏必红）：
  - `资料预算=1` ⇒ 资料段 2,140、**超预算=真**、降级项 `[涉及路径明细→指针, 契约逐能力明细→指针]`（降到最低仍超 ⇒ 如实报，不静默）。
  - 弄坏样本（把 `_规范片段` 换成「指针档也塞 2 万字符正文」）＋ `资料预算=3000` ⇒ 资料段 **1,852**、超预算 假、降级项 `[规范片段正文→指针]`；还原后回 2,551 ⇒ 降级闸确实兜住了。
  - `资料预算=0` ⇒ 2,551、不受约束（`预算管辖` 如实标注）。

### 判据三：不传新参数 ⇒ 与改前**逐字节一致** —— **达成（两条腿都给了证据）**

- **(a) 同刻对照（主证据，可复现）**：把 `git show HEAD:模块库/开工编排/实现/开工编排.py` 在本进程 `exec` 成独立模块当「改前」，**同一时刻、同一工作区**跑两遍对比 ⇒ 三档（摘要默认／完整／索引）**顶层键集相同、新增顶层键 0、差异处数 0、严格逐字节一致=真**。同一时刻取数 ⇒ 连 `代码地图`/`工作区快照` 这类现场读数都一致，比较不含噪声。
- **(b) 与 R16 动手前抓的基线对照**：顶层键集相同、新增顶层键 0；5 处差异**全部是现场读数**，不是代码行为 —— `代码地图.覆盖率.仓库py文件数 1296→1298`（本次新增 2 个 `.py`：`开工资料.py`、`常用意图.py`）、`未索引py文件数 58→60`、`覆盖率 0.9552→0.9538`、`工作区快照.未提交修改数 160→169`、`工作区快照.体积 4195→4201`；**归一（排除 代码地图/工作区快照）后逐字节一致=真**。
- 结论：新增两个参数与新增 `资料` 段**不改变不传参时的任何返回**，老调用方零影响。

### 判据四：`资料级别=完整` 与「分别调用各节点拼起来」逐字节等价 —— **达成**

| 项 | 等价 | 条数 |
|---|---|---|
| ④ `规范片段` == `项目文档支持库.查询索引(细节级别=完整, 返回条数=20).命中列表` | 真 | 20 |
| ⑤ `能力映射` == `公共契约.能力契约.常用意图.映射表()` | 真 | 4 |
| ⑥ `纪律段` == 顶层 `通用纪律` 同名三段 | 真 | 3 |
| ① `租约id清单` == `查询文件租约` 活跃记录 id | 真 | 5 |
| ② `涉及路径` == `通过清单 ＋ 活跃租约` 投影 | 真 | 5 |

## 四、边界与须确认项

### 边界（已守）

- **不做第二份事实源**：六项事实源仍是各自节点；本件只决定「摆多少、超预算先砍谁」，且每级降级都留 `文件:行区间` 指针。
- **不塞大正文**：指针档 3,361 字符；`完整` 档 166,181 是**显式要的**，默认不出现。
- 只增参数、只增返回键；不动既有键语义。

### 须确认项

1. **★ 默认档与「一次调用拿齐」的张力**：`资料级别` 的**默认值是 `指针`**，但为了让判据三（不传参逐字节一致）成立，`资料` 段**只在显式传 `资料级别` 或 `资料预算` 时出现**。⇒ 「纯用 MCP 一次拿齐」要调用方**显式**写 `资料级别=指针`。若华哥要「不传也带」，判据三必然破（返回体多两键）—— 请裁决取哪一条。
2. **★ MCP HTTP 链路当前拿不到新参数（本批不跑编译的必然结果，实测）**：
   - 探针（2026-09-24 11:57，真 MCP 调用）：`开工准备(资料级别='指针', 资料预算=8000)` ⇒ 网关回 `被忽略参数：[{资料级别：该能力契约里没有这个参数，已被网关剔除}, {资料预算：同上}]`。
   - 根因：① 40007 网关进程（PID 52871）`cwd = 工程缓存/制品仓库/平台客户端环境/平台客户端`，跑的是 **2026-09-24 10:24 构建的激活制品**；② 网关按**已注册的 `能力实现.参数`** 校验参数（`运行核心/统一网关/核心处理/网关参数校验面.py:313`），而那份 `能力实现` 由 `能力定义编译器` 从 `模块库/开工编排/能力定义.json` 生成到 `模块库/开工编排/__init__.py` —— 是**派生件**，本批不许手改。
   - ⇒ 收口动作：`自修复工具.静态编译(类型=1, 模块ID=模块库/开工编排)` 重算派生件（`__init__.py` ＋ `能力契约/参数契约.json`）＋ 重建制品后，**复测一次**这两条读数才算 live 收口。本批已把**声明面**改到位（`能力定义.json` 参数表 ＋ `值结构` ＋ 版本 1.4.0→1.5.0），编译口照它重算即可。
3. **`资料预算` 的管辖面**：只约束新增 `资料` 段（改前既有各段由 `细节级别` 管）。若华哥要「整个返回值 ≤ 预算」，那是另一条（会与判据三冲突），须裁决。
4. **源码树进程内腿缺 `平台控制面` 包**（**既有状态，非本批引入**）：`获取能力调用器()` 在源码树进程内注册表里查不到 `平台控制面.能力目录.查询文件租约`（回 `能力未注册`），故 `开工准备` 的租约腿在**进程内**恒为 `未查询`（fail-closed，如实报，不冒充「没人占」）。经网关（MCP）该能力正常（本次真调回 5 条活跃租约）。§二 的 ② 就是把**经 MCP 真调**回来的那份响应喂给投影腿得到的真实占用读数。
5. **`规范片段` 的项目名口径**：取 `项目根目录名`（`系统工程平台`），与 `项目文档支持库.查询索引` 的注册名一致；换目录名会取不到 ⇒ 如实回 `规范片段问题`（**不回落成「没有规范」**）。
6. **③ 门禁基线只对已在 `能力id冻结基线.json` 登记的能力给**；未登记的只有 能力定义/参数契约 两条腿（样例里 3 个非正式包则只给文件指针）。

## 五、变更清单

| 文件 | 动作 | 说明 |
|---|---|---|
| `模块库/开工编排/实现/开工资料.py` | 新增 | 档位收敛 ＋ 预算投影 ＋ 降级阶梯（进程内纯函数，不对外暴露） |
| `模块库/开工编排/实现/开工编排.py` | 改 | 只增 `资料级别`/`资料预算` 两个形参；新增 `_按行读`/`_首现行号`/`_块行区间`/`_所属包目录`/`_包契约指针`/`_契约与判据指针`/`_规范片段`/`_纪律段`/`_组装资料`；`开工准备` 尾部只在显式传参时挂 `资料`/`资料用量` |
| `公共契约/能力契约/常用意图.py` | 新增 | 「常见意图→首选能力」**唯一事实源** ＋ `渲染映射文本()` |
| `开发工具/薄壳/工具清单.py` | 改 | `★ 常见意图→首选能力` 那句改由 `渲染映射文本()` 生成（不再手写字面量） |
| `模块库/开工编排/能力定义.json` | 改 | 声明面：`开工准备` 版本 1.4.0→1.5.0；参数增 `资料级别`/`资料预算`；`值结构` 增 `资料`/`资料用量`；`说明` 追加资料段一句 |
| `开发文档/分析/批R单据_R16_开工资料一次取全_20260924.md` | 新增 | 本单据 |

**未动**：`模块库/开工编排/__init__.py`、`模块库/开工编排/能力契约/参数契约.json`、`完整性摘要.json` —— 三者都是派生件，由编译口重算。

## 六、复现命令

证据脚本（本批落盘在 `/tmp/r16/`，均为源码树直调，不改仓）：

```bash
python3.14 /tmp/r16/收口终验.py      # 判据三(a) 同刻对照 ＋ 判据一/二/四 ＋ 反向验证
python3.14 /tmp/r16/判据三.py        # 判据三(b) 与 R16 动手前基线对照
python3.14 /tmp/r16/真租约读数.py    # 真租约响应喂投影腿的三档读数
```

MCP 链路探针（真调用）：

```json
{"操作":"调用能力","能力id":"开工编排.开工准备",
 "参数":{"任务":"R16 MCP 链路探针","项目根":"<本仓库绝对路径>","修改路径":["模块库/开工编排/实现/开工编排.py"],
        "资料级别":"指针","资料预算":8000}}
→ 回 被忽略参数：[{资料级别,…},{资料预算,…}]（见 §四·须确认项 2）
```
