<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R单据 · R-14 注入面收口（让子代理偏好纯 MCP）

- 开工ID：`开工-20260924-114139-07a5`｜日期：2026-09-24｜口径：实测（全部经 MCP）
- 范围：有界维修 —— 只做「注入面定位 + 真源补纪律 + 布尔省略口径修正」，**不跑编译、不 git 提交**
- 华哥原话（本批由来）：「子代理偏好终端，怎么样让他的偏好修正为纯用 mcp？这个本质上是属于底层缺失吧。」
- 实测底数（R-13 读数）：11 路子代理 `exec_command` 1356 次 vs `capability_call` 144 次（其中 rg 565、cat/sed/head 297）⇒ **参数面不缺（3/3 覆盖），缺的是「想不到」**

## 一、完成 / 未完成

| 项 | 状态 | 落点 |
|---|---|---|
| ① 注入面全部真源定位（不许猜） | **完成** | 本文 §二：5 处面，逐段 `文件:行` |
| ② 真源补「工具姿势」硬纪律（单一节点） | **完成** | `开发文档/规范/任务类型规范.json:63`（第 31 条）+ 夹具同批同步（sha256 逐字一致） |
| ③ R-12 布尔 `假` 省略口径修正 | **完成** | `开发工具/薄壳/薄壳服务.py:420-445`；live 前后读数见 §四 |
| ④ 单据落盘 | **完成** | 本文件 |

未做：默认档（摘要）仍不送达 `工具姿势` 正文（见 §五 残余 1）；未跑编译；未提交。

## 二、注入面清单（子代理实际读到的每一段文本）

### 面 A · 任务信正文（子代理 kickoff 直接读；`开发工具/任务记忆/派活任务信.py`）

| 段 | 来源文件:行 | 性质 |
|---|---|---|
| 命令环境 | `开发工具/任务记忆/派活任务信.py:48-56` | **指针**（取用命令 = 调 `开工编排.开工准备` 的 `细节级别=完整`） |
| **工具姿势** | `开发工具/任务记忆/派活任务信.py:58-65` | **指针**（本节不重抄；只给「照 `通用纪律.工具姿势` 做」+ 取用命令） |
| 提交纪律 | `开发工具/任务记忆/派活任务信.py:67-73` | 指针 |
| 禁区 | `开发工具/任务记忆/派活任务信.py:75-95` | 全文（真源即本模块，无第二处） |
| 落盘与回传 | `开发工具/任务记忆/派活任务信.py:96-105` | 全文 |
| 自证与如实 | `开发工具/任务记忆/派活任务信.py:106-111` | 全文 |
| 禁区（只读任务版） | `开发工具/任务记忆/派活任务信.py:112-120` | 全文 |
| 文件租约 | `开发工具/任务记忆/派活任务信.py:121-163` | 全文 |
| 收工与返回 | `开发工具/任务记忆/派活任务信.py:165-232` | 全文 |
| 改完必做 | `开发工具/任务记忆/派活任务信.py:234-242` | 指针 |
| 组装点 | `开发工具/任务记忆/派活任务信.py:244`（固定条款段）、`:338`（「本次任务固定纪律」汇总行）、`:365-369`（只读分支） | — |
| 自检判据 | `开发工具/任务记忆/派活任务信.py:401-455` | 标志串（不是纪律正文） |

### 面 B · `开工编排.开工准备` 无条件返回的 `通用纪律`（**真源唯一节点**）

真源文件：`开发文档/规范/任务类型规范.json`

| 段 | 真源 文件:行 |
|---|---|
| 哲学纪律 | `开发文档/规范/任务类型规范.json:8-26` |
| 命令环境 | `开发文档/规范/任务类型规范.json:27-31` |
| **工具姿势** | `开发文档/规范/任务类型规范.json:32-63` —— **本次新增硬纪律条 = `:63`** |
| 提交纪律 | `开发文档/规范/任务类型规范.json:65-70` |
| 改完必做 | `开发文档/规范/任务类型规范.json:71-79` |
| 判据 | `开发文档/规范/任务类型规范.json:80` |
| 效率达标线 | `开发文档/规范/任务类型规范.json:81-88` |
| 派活纪律 | `开发文档/规范/任务类型规范.json:89-96` |
| 通用纪律版本 | `开发文档/规范/任务类型规范.json:257`（1.14.0 → **1.15.0**） |

载体代码（谁把它送到子代理）：
- `模块库/开工编排/实现/任务类型规范.py:139-163` —— `取通用纪律()`；键 `说明` 不返回（省 token）
- `模块库/开工编排/实现/开工编排.py:519,713-718` —— **无条件**随开工上下文返回 `通用纪律` / `通用纪律问题` / `通用纪律提示`
- `模块库/开工编排/实现/返回体量分档.py:45,102` —— 档位投影（索引 / 摘要 / **完整**）；`摘要保留全文段 = (命令环境, 提交纪律, 判据)` ⇒ **`工具姿势` 不在默认档**
- 副本（测试强制与真源逐字一致）：`模块库/开工编排/验证夹具/任务类型规范.json`

### 面 C · MCP 工具描述（每轮都在上下文里，0 跳）

- `开发工具/薄壳/工具清单.py:52-79` —— `capability_search` 描述（含「找能力=本工具；看契约=调用能力 操作『能力详情』」）
- `开发工具/薄壳/工具清单.py:81-111` —— `capability_call` 描述；`:89-91` = **R-13 补的「常见意图→首选能力」**（读文件 / 内容检索 / 跑命令 / 找能力）
- `开发工具/薄壳/工具清单.py:113-125` —— `tool_catalog` 描述
- 指纹面：`开发工具/薄壳/工具清单.py:197`（工具面指纹重算记录）

### 面 D · 项目根 `AGENTS.md`（每会话注入）

- `AGENTS.md:41` —— 「命令环境 / 提交纪律 / 工具姿势（开工默认只给前两类全文）| 传 `开工编排.开工准备` 的 `细节级别=完整`」
  （这一行本身就是「默认档只给前两类全文」的机器可见陈述 —— 与面 B 的 `摘要保留全文段` 互证。）

### 面 E · 度量与门禁（不注入，决定「有没有送达」是否报红）

- `开发工具/开发效率巡检.py:49` —— 查纪律送达（判据是四个标志串）
- `测试中心/模块库/测试_开工编排.py:577` 起 —— `通用纪律规范与夹具` 三条判据：源头↔夹具逐字一致 / 工具姿势不得直接写外平台工具名 / 不得写裸命令

**跳数结论（子代理实际读到的路径）**：面 C **0 跳**（每轮必见，但只有映射表、无判据）；面 A **1 跳**（任务信点名「照 `通用纪律.工具姿势` 做」+ 取用命令）；面 B **2 跳**（真源 → `开工准备` → 任务信），且**默认档拿不到正文，必须显式 `细节级别=完整`**。

## 三、补的纪律正文（真源唯一节点，落点 `开发文档/规范/任务类型规范.json:63`）

原文（`通用纪律.工具姿势` 第 31 条）：

> ★★【硬纪律】凡可经 MCP 拿到的信息，不得用终端取（2026-09-24 批R·R-14；判据＝「这条信息有没有 MCP 能力能拿到」）—— 读文件 → `文件系统支持库.文件操作.读取文件`；按内容检索 → `文件系统支持库.内容检索.正则搜索`；找能力 → `能力目录.搜索能力`；看契约 → 能力详情（`capability_call(操作='能力详情', 能力id='<能力id>')`）；列目录 / 找文件 → `文件系统支持库.文件操作.列出目录` / `文件系统支持库.文件操作.搜索文件`；查符号与影响面 → `代码解析支持库.代码地图.查询节点` / `查询关系`；跑命令 → `系统核心支持库.进程管理.执行命令` / `执行命令集`。**终端（驱动方 shell 类工具）只保留三类**：① 跑本仓明令禁止走 MCP 的只读取证脚本；② git 只读状态（status / log / show / diff 这类不改仓库的）；③ MCP 确实不覆盖的检查。**三类之外一律走 MCP**；某处确实拿不到 ⇒ **在回传里如实回报缺口**（缺口是底座要补的原子能力），**不许退回终端硬凑** —— 退回终端＝把缺口藏起来，缺口就永远补不上。

同批同步（不是第二份，是同一字节的副本 —— 测试 `test_验证夹具与源头逐字一致` 强制）：
- `模块库/开工编排/验证夹具/任务类型规范.json`
- 两文件 sha256（经 MCP `系统核心支持库.资源管理.创建内容摘要`）＝ `f34dabeb58f261d32229dcfadaccdb91f4e64cc3908c2274e36f3dc82c4a2f32`（**一致**）
- `通用纪律版本` 1.14.0 → 1.15.0（`开发文档/规范/任务类型规范.json:257`）

live 读数（改后，经 MCP）：
- `开工编排.开工准备(细节级别=索引)` → `通用纪律.工具姿势.体积` = **31 条**（改前 30 条）
- `通用纪律问题` = 空串 ⇒ JSON 可读、真源未被改坏

## 四、R-12 口径修正：布尔 `假` 不再当空值

落点：`开发工具/薄壳/薄壳服务.py`
- `:417-431` —— 口径注释：`false` 从空值清单移出；新增 ★★ 段写明「`假` 是**值**不是空值」
- `:434-445` —— `是空值()`：删掉 `项 is 假`；改后 = `项 is None or 项 == "" or 项 == [] or 项 == {}`
- 唯一出口不变：`:1205` `数据 = 去空值(数据)`（薄壳交给 agent 的唯一一层）
- `空值豁免键 = frozenset({"成功"})`（`:431`）保留不动

live 读数（**同一次调用，改前 / 改后**）：

| 探针 | 改前（11:41） | 改后（11:43） |
|---|---|---|
| `能力目录.搜索能力(关键词='文件系统支持库.文件操作.读取文件', 细节级别='名称', 限制=2)` | `是否扩词` / `低置信` / `含常用参数` / `精确命中升档` **四个布尔键全缺**（`false` 被当空值省掉）；`能力列表[*]` 的 `内部层` / `适配层` 同样缺键 | 四个布尔键**全部保留**：`是否扩词=false`、`低置信=false`、`含常用参数=false`、`精确命中升档=false`；`内部层=false` / `适配层=false` **保留** |
| 同一次调用的空值键 | `兜底说明`("") / `兜底判据`({}) / `游标`("") 已省 | **仍然省**（空值键 0） |
| `开工编排.开工准备(细节级别='索引', 值字段=[通用纪律问题, 强规范是否截断, 代码地图])` | `代码地图.可用`(false) **缺键**、`强规范是否截断`(false) **缺键** | `代码地图.可用=false`、`强规范是否截断=false` **保留**；`通用纪律问题`("") **仍省** |
| `平台控制面.能力目录.查询文件租约(只要活跃=真)` | 记录里 `指纹一致`(false) 与 `已声明指纹`("") **都缺键** —— 正是华哥点名的误读源 | `指纹一致=false` **保留**；`已声明指纹`("") 仍省 |

**口径结论：同一次调用里「空值键 N→0、布尔 `假` 保留」。** `指纹一致=false`（「核验过、且不一致」）不再与「没核验」（缺键）在 agent 眼里混成一个样子。

## 五、残余（未做项 / 建议下一步）

1. **默认档（摘要）不送达 `工具姿势` 正文** —— 本次最关键的残余：纪律已进真源，但 `模块库/开工编排/实现/返回体量分档.py:102` 的 `摘要保留全文段 = (命令环境, 提交纪律, 判据)` **不含 `工具姿势`**，所以不传 `细节级别` 的调用只拿到 `{"体积":"31 条","展开":"传 细节级别=完整 取原文"}`（已实测）。子代理若照默认档走，仍看不到这条硬纪律。
   - 建议（二选一，均为**指针 / 标记**级改动，不产生第二份正文）：
     - **A（0 跳，最省）**：在 `开发工具/薄壳/工具清单.py:89-91` 的 `capability_call` 描述里加一行指针 —— 「★ 凡可经 MCP 拿到的信息不得用终端取（判据与三类终端例外见 `开工编排.开工准备` 完整档 的 `通用纪律.工具姿势`）」。工具描述每轮都在上下文里，0 跳。
     - **B（1 跳）**：`返回体量分档.py` 的 `_摘要纪律()` 对 `工具姿势` 增加「带 `【硬纪律】` 标记的条目保留全文、其余仍收成指针」；代价是默认档 `通用纪律` 增加约 500 字符（现约 1,614 字符，闸 2,000）。
   - 两条都要改档位 / 描述面，属另一批，**本轮未做**。
2. **未跑编译**（本批明令）：`自修复工具.静态编译` 未调；`通用纪律规范与夹具` 的逐字一致判据未跑测试 —— 改用 MCP `创建内容摘要` 比对 sha256 取证（见 §三）。
3. **未 git 提交**（本批明令）：改动仍在工作区；4 条租约待 `开工编排.收工释放`。
4. **`开发工具/任务记忆/派活任务信.py:58-65` 指针文案略旧**：它写「该段默认档只回『体积』摘要」—— 本轮未改档位，该句仍成立；若采纳 §五-1 的 A / B 方案，必须同批改它，否则同一事实两处。
5. **`模块库/开工编排/说明/使用说明.md` 仍写「通用纪律 五段」**（生成物，未刷）：真源现为 8 段（哲学纪律 / 命令环境 / 工具姿势 / 提交纪律 / 改完必做 / 判据 / 效率达标线 / 派活纪律），该说明是契约生成物，随下次契约编译刷新。
6. **取证姿势自纠（本条即新纪律的自我适用样本）**：本轮算两个 JSON 的 sha256 时我先用了 `系统核心支持库.进程管理.执行命令` 跑 `shasum`，随后发现平台**已有** `系统核心支持库.资源管理.创建内容摘要` ⇒ 已改用能力腿复算（结论一致）。按新硬纪律，下一次应**先搜能力再决定**，不该先回终端。
7. **驱动方侧不在本仓可控面**：R-13 诊断的「发现性缺口」里，子代理默认工具集的**终端排在前面**属驱动方工具面；本仓只能把「判据 + 首选能力映射」送到 0 / 1 跳，改不了驱动方的工具排序。若实测仍不转向，下一批应查驱动方侧的工具描述注入面。
