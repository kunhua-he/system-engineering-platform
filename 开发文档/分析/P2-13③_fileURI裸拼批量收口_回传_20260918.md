# file: URI 未编码裸拼「全仓批量收口」回传（2026-09-18）

**任务**：把剩下的 file: URI 未编码裸拼全仓收口到唯一构造口径（同类一次修全，不留第二次）。
**提交**：`9fa5d5c0`（收口①，11 文件）+ `93f59d78`（收口② 摘要，5 文件）。
**结论**：全仓真需修 **13 处**（原列表 9 处 + 我重扫补出 4 处），全部收口到唯一口径；
另有 16 处「看起来像」经逐处判定**不需修**（理由见判定表）。依赖防火墙零违规（759 文件 / 0 违规）。

---

## 一、全仓扫描结果（不是只信传入列表，自己重扫）

扫描口径（`--include='*.py'`，排除 `__pycache__` / `工程缓存` / `开发文档`）：
`grep -rn 'file:.*mode=ro'`、`grep -rn '"file:|\'file:'`、`grep -rn 'uri=True'`、
`grep -rn 'file://'` 四种形态各扫一遍。

**传入列表只报了 6 处，主会话说 9 处，实测 13 处** —— 补出 4 处：

| # | 位置 | 传入列表是否含 |
| --- | --- | --- |
| 1 | `平台控制面/提供者/控制面对账.py:28` | ✅ |
| 2 | `平台控制面/备份恢复/校验函数.py:26` | ✅ |
| 3 | `平台控制面/备份恢复/备份执行.py:37` | ✅ |
| 4 | `开发工具/备份恢复演练.py:65` | ✅ |
| 5 | `开发工具/备份恢复演练核对.py:28` | ✅ |
| 6 | `开发工具/备份恢复演练核对.py:116` | ✅ |
| 7 | `测试中心/支持库/测试_运行数据库.py:179` | ❌ **扫漏（我自己重扫补出）** |
| 8 | `支持库/后端/代码解析支持库/代码地图/实现/只读连接.py:24` | ❌ **扫漏** |
| 9 | `支持库/适配层/代码地图提供者/实现/子进程入口.py:57` | ❌ **扫漏** |
| 10-11 | `运行核心/运行状态监督/实现/只读状态库.py:42`、`运行核心/权威状态.py:1609` | ❌ 口径**已建但未覆盖** |
| 12-13 | `运行核心/权威状态.py` 的 `只读库URI` / `连接真实库路径` 定义处 | ❌ 口径本身住的层不对 |

后 6 处不是「另一个缺陷」，是**同一个「同类收口不彻底」**：口径建好了却没统一，
仓里同时存在 **3 份等价实现**（`只读库URI` / `只读连接` 的 as_uri 拼串 / `只读URI` 的
as_uri 拼串）+ 6 处裸拼。本次一次修全。

## 二、逐处判定表（真需修 / 不需修 + 理由）

### 真需修 13 处

| # | 位置 | 判定 | 理由 |
| --- | --- | --- | --- |
| 1 | `平台控制面/提供者/控制面对账.py:28` | **真需修** | 路径 = `存储目录`（能力入参，调用方传），含 `#`/`?` 时 URI 截断 → 连空库 → `激活指针` 表不存在 → 被上层包成 503（该文件注释里已记过同类误判） |
| 2 | `平台控制面/备份恢复/校验函数.py:26` | **真需修** | 路径含备份项文件名（磁盘实际名）；截断后连新建空库 → `integrity_check` 恒 `ok` → **真损坏被报成「完整性检查通过」**（反验已实测） |
| 3 | `平台控制面/备份恢复/备份执行.py:37` | **真需修** | 源库路径来自入参；截断后 `VACUUM INTO` 从**错库**取快照 → 备份内容错，且全程不报错 |
| 4 | `开发工具/备份恢复演练.py:65` | **真需修** | 演练副本路径由受管临时根拼出，但**受管临时根可配置**（`--临时根` 类入参）；且它调的就是上面 2/3 的同一语义 |
| 5 | `开发工具/备份恢复演练核对.py:28` | **真需修** | 同上，核对连错库 = 把「恢复了 A 库」核对成「B 库一致」 |
| 6 | `开发工具/备份恢复演练核对.py:116` | **真需修** | `生产根` 来自配置/入参，`VACUUM INTO` 从错库取生产快照 |
| 7 | `测试中心/支持库/测试_运行数据库.py:179` | **真需修** | 读的是 `工程缓存/运行数据/底座运行.db`（**仓库内路径，随项目根走**）——项目放在含 `#`/`?` 的目录下就截断成空库 → 巡检恒绿（防复发用例自己失效） |
| 8 | `支持库/后端/代码解析支持库/代码地图/实现/只读连接.py:24` | **真需修（第二份实现）** | 它自己写了 `as_uri()+"?mode=ro&immutable=1"`；参数文本、编码方式独立演进 = 两把尺子 |
| 9 | `支持库/适配层/代码地图提供者/实现/子进程入口.py:57` | **真需修（第三份实现）** | 同上一模一样的第三份；且 `项目根目录` 是**能力入参**，用户可传含 `#`/`?` 的路径 |
| 10 | `运行核心/运行状态监督/实现/只读状态库.py:42` | **真需修（第二份实现）** | 与 #8/#9 等价的第四份 as_uri 拼串，`存储目录` 同样是入参 |
| 11 | `运行核心/权威状态.py:1609` | **真需修（口径本身住错层）** | 定义住在最高层，导致 #1-#10 的低层调用方够不到、只能各写一份——根因就在这 |
| 12-13 | `运行核心/权威状态.py` 的 `只读库URI` / `连接真实库路径` 定义 | **真需修（下移）** | 见第三节分层依据 |

### 不需修 16 处（逐处理由）

| 位置 | 判定 | 理由（可核验） |
| --- | --- | --- |
| `测试中心/运行核心/测试_环境镜像.py:355,392,469` | 不需修 | 路径源 = `self.临时 = Path(tempfile.mkdtemp())`（第 65/277 行），**mkdtemp 随机段字符集仅 `[A-Za-z0-9_]`**（实测样例 `tmptgi1pygk`），不含 `#`/`?` → 不可能截断 |
| `测试中心/运行核心/测试_镜像下载.py:386` | 不需修 | 同上，`self.临时` 同源 |
| `测试中心/运行核心/测试_镜像发布.py:270` | 不需修 | 同上，`镜像仓库` 建在 `self.临时` 下 |
| `测试中心/支持库/测试_文字文档.py:61,103` | 不需修 | 路径源 = `mkdtemp(prefix='私有档案_', dir=输出目录.parent)`（第 55 行），`输出目录 = self.临时目录/"转doc"`（第 240 行），全在 mkdtemp 树内 → 字符集安全 |
| `测试中心/支持库/测试_演示文稿.py:61,103` | 不需修 | 同上，与文字文档测试同一夹具形状 |
| `测试中心/第一批维修/测试_LibreOffice受管池.py:120` | 不需修 | 只做 `startswith("file://")` 断言，**不构造 URI、不连库**（纯读） |
| `支持库/后端/办公文档支持库/演示文稿/实现/演示文稿.py:78` | 不需修 | 读 pptx 内部 XML 的 `TargetMode="External"` 关系属性，是**别人文件里的字符串**，不是我们构造的 URI |
| `支持库/后端/文档转换支持库/python_pptx提供者/实现/演示文稿.py:49` | 不需修 | 同上 |
| `开发工具/项目编译/项目编译器.py:484`、`运行核心/运行环境管理器/远程镜像.py:286`、`支持库/后端/文档转换支持库/LibreOffice转换/实现/文档转换.py:10` | 不需修 | 均出现在**注释/文档字符串**里（说明 `file://` 来源语义），无 URI 构造 |
| `开发文档/决策记录/0015…:79`、`开发文档/分析/*.md` | 不需修 | 开发文档里的历史命令记录，不是运行代码 |

## 三、收口位置的分层依据（依赖防火墙真实输出，不是猜）

`python3.14` 导入 `运行核心/依赖防火墙.py` 读 `允许依赖表`，真实输出：

```
公共契约   -> []
其他       -> ['前端核心','后端核心','平台控制面','开发工具','技能库','支持库','模块库','运行核心','项目适配层']
前端核心   -> ['公共契约','运行核心']
后端核心   -> ['公共契约','运行核心']
平台控制面 -> ['公共契约','支持库','运行核心']
开发工具   -> ['公共契约','前端核心','后端核心','支持库','模块库','运行核心','项目适配层']
技能库     -> ['公共契约']
支持库     -> ['公共契约']          ← 关键
模块库     -> ['公共契约','支持库']
运行核心   -> ['公共契约','支持库']
项目适配层 -> ['公共契约','运行核心']
```

**判定**：`平台控制面 → 运行核心` **允许**（所以任务书里问的那一条是「能」）；
但**真正的约束在别处**：`支持库 -> ["公共契约"]` ⇒ **支持库层不得依赖 运行核心**。
本次调用方横跨八个层（公共契约/支持库/模块库/技能库/运行核心/平台控制面/项目适配层/开发工具），
其中 `支持库/后端/代码解析支持库`（#8）和 `模块库` 按上表都够不到 运行核心。

⇒ **不能原地复用**，必须下移；下移目标按「谁的允许集能覆盖上述全部层」反推：
`公共契约` 是**唯一**对上述每一层都在允许集内的层（`公共契约 -> []` 无出边，
不会制造环）。因此下沉到 `公共契约/运行时/数据库URI.py` —— 与同层 `平台适配.py`/`有界IO.py`
同性质（都是「跨层共用的底层原语」，它们也都住在这一层）。

**下移代价实测**（导入 `运行核心.权威状态`）：拖入 **86 个模块、27.6 ms**，
且连带 `运行核心.进程身份`——低层调用方为拼一个字符串背这个依赖不划算。
下移后各层只背标准库 `pathlib`。

**为什么连 `连接真实库路径` 一并下移**：它与 `只读库URI` 是**一对验真原语**
（前者构造「我打算读哪个库」，后者回答「我实际连到了哪个库」）。空库 `integrity_check`
恒返 `ok`，只要前者永远发现不了 URI 被截断连错库。低层调用方要自证「没连错库」必须两个都有。

**为什么必须新建而不是复用现成件**（已按边界要求先找过）：
- `公共契约/运行时/` 下**无任何** sqlite/URI 相关件（`grep 'as_uri\|sqlite3\|mode=ro\|URI' 公共契约/` 零命中）；
- `运行核心/运行状态监督/实现/只读状态库.py:只读连接()` 在 `实现/` 下——依赖防火墙
  **强制拒绝导入任何 `实现/` 目录**，且它不是公开能力；参数写死 `mode=ro`、不带 `timeout`，
  覆盖不了本次要收的两种参数形态。故不复用、新建并在文件头写明理由。

**零违规证据**：`审计依赖()` → `成功: True 文件数: 759 违规数: 0`；
`检查依赖口径()` → `[]`；`检查头注释与表同源()` → `[]`。
（基线 757 文件 0 违规 → 收口后 759 文件 0 违规，净增 2 = 我新增的 `数据库URI.py`
+ 并行子代理在途的 `资源键表.py`。）

**唯一出处实测**：

```
只读库URI 同一对象: True | 连接真实库路径 同一对象: True
出处模块: 公共契约.运行时.数据库URI
```

## 四、定向验真（3 个特殊字符路径 × 5 个改动点）

路径：A 含 `?`（`问?甲`）／B 含 `#`（`井#乙`）／C 含中文与空格（`中文 丙 目录`）。

```
[绿] 只读库URI 连对库 | A/B/C           目标=权威状态.db 实连=权威状态.db
[绿] 写探针必须被拒 | A/B/C             attempt to write a readonly database
[绿] 校验权威状态 检出损坏 | A/B/C      返回=(False, 'sqlite 打开或检查失败: file is not a database')
[绿] ① 控制面对账.只读激活指针 | A/B/C  命中={'指针id':'p1','目标':'t1',…}
[绿] ② 备份执行.一致性快照 | A/B/C      快照内容=('真数据-不许被覆盖',)
[绿] ③ 演练核对._查询 | A/B/C           查询=[('t1',)]
[绿] ④ 校验函数.校验权威状态(好库) | A/B/C  返回=(True, '完整性检查通过')
[绿] ⑤ 校验函数.校验权威状态(坏库) | A/B/C  返回=(False, 'sqlite 打开或检查失败: file is not a database')

合计 24 项，红 0 项
```

## 五、反向验证（退回裸拼 → 必红；还原 → 必绿 + sha 一致）

挑 `平台控制面/备份恢复/校验函数.py` 退回首轮那行裸拼。红的口径不是「随便报个错」，
而是复现真实危害：目标库放含 `?` 的目录且**真损坏**，裸拼在 `?` 处截断成前缀名并
**现场新建空库**，其 `integrity_check` 恒 `ok`：

```
① 收口版（当前 HEAD）→ 必须绿
   前提-库真损坏=True
   结论-校验权威状态 判通过=False 说明=sqlite 打开或检查失败: database disk image is malformed
   现场是否新建了截断前缀库 问: False
   VERDICT=GREEN

② 退回裸拼（sha 9b8a59c2…，与原 sha 不同）
   前提-库真损坏=True
   结论-校验权威状态 判通过=True 说明=完整性检查通过     ← ★ 真损坏被报成「通过」
   现场是否新建了截断前缀库 问: True                    ← ★ 现场真的新建了空库
   VERDICT=RED

③ 还原 → 必须绿 + sha 一致
   还原后 sha256: fc7bdb93a7a1846e4dc7c39d79f2b6412d4d402de0b47c6f54f2a11402288c78
   sha 一致: True
   VERDICT=GREEN
```

（还原用 `git checkout 9fa5d5c0 -- <文件>` 兜底；脚本带 `finally` 强制还原。）

## 六、回归

| 项 | 结果 |
| --- | --- |
| `py_compile` 全部 11 个改动文件 | 全 OK |
| `测试中心.平台控制面.测试_工作包09_备份契约`（备份/校验/恢复真实契约） | 7/7 OK |
| `测试中心.平台控制面.测试_补修阻断` | 21/21 OK |
| `测试中心.平台控制面.测试_平台控制面` | 13/13 OK |
| `测试中心.发布门禁.测试_权威状态门禁` | 1/1 OK |
| `测试中心.支持库.测试_运行数据库` | 10/10 OK |
| `测试中心.支持库.测试_代码地图提供者` | **3/4 FAILED，但与本次改动无关**（见下） |
| `开发工具/备份恢复演练.py`（端到端真实演练） | 四类数据逐类核对**全绿**，RTO 0.0015s，RPO 0.4231s，结论「一致」 |
| `python3.14 -m 开发工具.全量重算摘要 --只报` | 改前 5 项漂移（全在我改的包）→ 重算后「全部闭合：无可重算项」 |
| `python3.14 -m 开发工具.公开调用完整性门禁` | 通过，rc=0，无新增违规（存量基线 20 项「只减不增」未变） |

**`测试_代码地图提供者` 3 项失败的归因（查清，非本次引入）**：
报错是子进程 `支持库/适配层/代码地图提供者/实现/子进程入口.py` **第 20 行
`ModuleNotFoundError: No module named '公共契约'`**，而 `git show 9fa5d5c0~1:该文件` 的第 20 行
字面就是同一句导入。**因果实测**：把我加的第 21 行导入删掉、把 #9 退回裸拼版，
同一测试**仍然 3 项失败**（失败与我的行无关）。根因是该测试用 `sys.path.insert('支持库/适配层')`
自行组路径，子进程入口按 `__file__` 推导系统根时得 `/…/支持库/适配层`，读不到顶层包
——**修前修后、裸拼收口都失败**，属该测试夹具自身的既有问题，不在本次任务面内（未动它）。

## 七、未完成 / 存疑项

1. **`测试_代码地图提供者` 3 项失败未修**：属该测试路径夹具自身的既有缺陷（上文已给因果证据），
   不在「file: URI 收口」任务面内，我没有扩大改动面去动它。建议单开一条。
2. **`公共契约/运行时/数据库URI.py` 未跑 `公开调用完整性门禁` 的「六环」登记**：该门禁扫的是
   正式公开包（有 `包声明.json`/`能力定义.json` 的包），`公共契约/运行时/*.py` 是**根下直接文件**
   形态（与 `有界IO.py`/`平台适配.py` 一致），不在其扫描面内；门禁实测 rc=0 无新增违规。
   是否需要为它补 `说明书/能力定义` 请华哥裁——现形态与同层既有件一致。
3. **未加「存储路径禁含 `#`/`?`」的启动自检**：与 P2-13③ 前轮的判断一致——URI 编码修好后
   `#`/`?` 已能安全通过，再加禁字符自检属新增行为、与「编码已解决」重复。
4. **未推送远程**（按边界）。
5. **并行子代理冲突实测**：`运行核心/权威状态.py` 在我改完后被另一在途子代理**整文件重写并
   还原了那两处定义**（实测 mtime 20:26:19、`git diff` 197 行）。我已重做并**立刻提交**
   使其落史（`9fa5d5c0`），当前 HEAD 上同一性实测为 True。若后续有人再动该文件，
   请以 `9fa5d5c0` 的导入行为准。

## 附：临时脚本清单（全在 `/private/tmp/系统平台临时脚本_20260918/`，未留仓库）

| 脚本 | 用途 |
| --- | --- |
| `跑防火墙.py` | 打印 `允许依赖表` 真实允许集 |
| `基线审计.py` | `审计依赖()` 文件数/违规数 |
| `定向验真.py` | 三路径 × 五改动点 24 项 |
| `反向验证.py` | 裸拼红／收口绿／还原 sha 一致 |
| `定向重算摘要.py` | 只重算本次改动包的摘要 |
