# 测试中心归口盘点（阶段目录按能力域归口 · 引用面硬数据）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- **开工ID**：20260918-测试中心归口盘点
- **执行人**：小马仔
- **任务来源**：`开发文档/未完成事项.md` §8.4 第 8 项「`测试中心/` 30 个阶段目录按能力域归口」
- **依据条款**：1.1（干净底座优先·底座唯一）、12.4①（分层归位）、12.4⑤（归档件不作派活依据）
- **纪律**：全程**只读** —— 未搬任何目录、未删任何目录、未改任何文件；脚本落 `/tmp`；`工程缓存/` 一处未动。本文件是唯一新增物。
- **处置结论**：**不执行搬运**。引用面是大面（源码/配置侧 **23 处 / 12 文件**），且盘点中发现既有方案自身存在**判据自相矛盾**（见 §5.1）。按任务口径「引用面大 → 落盘点文档 + 两方案供裁决」执行。
- **口径**：`python3.14`（3.14.4），跑前 `unset PYTHONPATH`

## 0. 快照锚点（复算必须对齐这一行）

| 项 | 值 |
|---|---|
| 仓库根 | `/Users/hekunhua/Documents/Agent/PHP/系统工程平台` |
| 快照时间 | **2026-09-18 04:22:03 CST** |
| HEAD | `96a09976` |
| 全仓未提交项 | **367**（并行多路维修在途，工作树是中间态） |
| 其中 `测试中心/` 未提交项 | **80**（4 未追踪 + 76 已改） |
| 测试资产清单 md5 前 16 位 | `3f7482964b4b1542`（对 `测试中心/**/测试_*.py` 排序后 md5） |
| 扫描文件数 | **3340** 个 `.py`/`.md`/`.json`（排除 `工程缓存/`、`.git/`、`__pycache__/`） |

> ⚠ 快照期间有并行子代理在改 `测试中心/` 下文件（未提交项 80）。**要对齐本文数字，先核对上面的 md5 与 HEAD**。
>
> ⚠ **快照后漂移留痕**：本文落盘后 1 分钟内，并行会话又把 `测试中心/` 测试资产从 **202 → 203**（新增 `测试中心/支持库/测试_降级记录表有界收口.py`、`测试中心/运行核心/测试_冷启动准入后端平台原语.py` 两个未追踪文件，另两个前次未追踪文件已由该会话提交），md5 随之为 `64e7e86ba26d1820`。
> **本文全部结构类结论（顶层目录 28、空壳 19、引用面 23 处 / 12 文件）不受影响** —— 新增的两个文件都在**既有能力域目录**内，不改目录结构与引用面。但**再次印证 §5.2 的「并行在途」判断：`测试中心/` 此刻是活的，不适合做目录搬迁。**

---

## 一、现状盘点（实测）

### 1.1 一句话结论

> `测试中心/` 顶层 **28 个目录** = **9 个能力域命名** + **10 个纯阶段名（全是空壳，零文件）** + **9 个批次/旧层/其他命名**（其中 5 个是空壳）。
> 有真实内容的只有 **13 个**，空壳 **15 个**。
> **任务标题说的「30 个阶段目录」在现场是 28 个顶层目录**；其中真正带「阶段」字样的只有 **10 个顶层 + `慢速层/` 下 3 个子目录 = 13 个**，且**全部为空壳**。
> **真正的「阶段目录 vs 能力域目录并存」问题不在顶层阶段名上，而在 `第一批维修/`（28 文件）、`加载器/`（5）、`发布门禁/`（4）、`底座升级/`（1）这四个「批次/旧层」命名的**有内容**目录上。**

### 1.2 28 个顶层目录逐个清点（实测）

| 顶层目录 | 文件数 | `.py` 数 | 命名类别 | 子目录 |
|---|---|---|---|---|
| `公共契约/` | 7 | 7 | **能力域** | — |
| `启动监督器/` | 2 | 2 | **能力域** | — |
| `平台控制面/` | 19 | 19 | **能力域** | — |
| `开发工具/` | 26 | 26 | **能力域** | — |
| `支持库/` | 52 | 52 | **能力域** | `核心/` |
| `模块库/` | 28 | 28 | **能力域** | — |
| `示例项目/` | 2 | 2 | **能力域** | — |
| `运行核心/` | 33 | 33 | **能力域** | — |
| `项目适配/` | 2 | 2 | **能力域** | — |
| `第一批维修/` | **28** | 28 | 批次命名（**有内容**） | — |
| `加载器/` | **5** | 5 | 旧层残影（**有内容**） | — |
| `发布门禁/` | **4** | 4 | 旧层残影（**有内容**） | — |
| `底座升级/` | **1** | 1 | 批次命名（**有内容**） | — |
| `慢速层/` | 0 | 0 | 批次命名（空壳） | `第十三阶段`、`第十四阶段`、`第十八阶段`（全空） |
| `辅助/` | 0 | 0 | 批次命名（空壳） | `可复现构建审计`、`灾难恢复审计`、`统一体验面审计`（全空） |
| `权威状态/` | 0 | 0 | 批次命名（空壳） | — |
| `结构迁移/` | 0 | 0 | 批次命名（空壳） | — |
| `资源并发/` | 0 | 0 | 批次命名（空壳） | — |
| `第三阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第四阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第五阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第六阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第七阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第八阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第九阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第十阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第十一阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| `第十三阶段/` | 0 | 0 | **阶段名（空壳）** | — |
| **合计 28** | | | 能力域 9 / 阶段名 10 / 批次旧层 9 | |

**汇总**：能力域命名 **9**、纯阶段名 **10**、批次/旧层/其他 **9**；有内容 **13**、零文件 **15**。

### 1.3 「阶段目录」与「能力域目录」并存的现场真相

| 问题形态 | 现场事实 | 判定 |
|---|---|---|
| 顶层阶段目录（`第三阶段/` … `第十三阶段/`） | **10 个，全部零文件、git 未追踪、全树零引用** | **空壳遗留**，不是「待归口的测试资产」 |
| `慢速层/` 下阶段子目录 | **3 个，同样空** | 同上 |
| 「阶段命名但有内容」的目录 | **0 个** | 顶层没有出现「阶段命名 + 有内容」的并存 |
| 但**批次/旧层命名 + 有内容**确实与能力域并存 | `第一批维修/`(28)、`加载器/`(5)、`发布门禁/`(4)、`底座升级/`(1) = **38 个文件 / 4 个目录** | **这才是本项真正的归口对象** |

**空壳由来（实测，不是推断）**：`git log --name-only -- 测试中心/第三阶段` 命中提交 `125fc5ed`（2026-08-30「chore: 删除历史阶段慢速审计与测试侧生产依赖」）。该提交删除了 `测试中心/第三阶段/测试_第三阶段验证场景.py` 等 **60+ 个阶段测试文件**，**但空目录没被删除**（git 不追踪空目录，故删除文件后目录留在了工作树）。同批还删了 `慢速层/*`、`权威状态/*`、`结构迁移/*`、`资源并发/*`、`辅助/*` 下全部文件。
> 这 19 个空目录的 mtime 全部是 **2026-09-15 13:05**，与此刻无关，不是并行会话的在途新建。

### 1.4 数量总表

| 口径 | 实测值 | 取证命令 |
|---|---|---|
| 顶层目录数（除 `__pycache__`） | **28** | `find 测试中心 -maxdepth 1 -type d -not -name __pycache__ \| wc -l` |
| 有内容顶层目录 | **13** | 逐目录 `find <dir> -type f -not -path '*__pycache__*'` |
| 零文件顶层目录（空壳） | **15** | 同上 |
| 全树零文件目录（含嵌套） | **19** | `find 测试中心 -type d -empty` |
| `测试中心/` 内 `.py` 总数 | **209** | `find 测试中心 -name '*.py' -not -path '*__pycache__*' \| wc -l` |
| 其中测试资产 `测试_*.py` | **202** | `find 测试中心 -name '测试_*.py' -not -path '*__pycache__*' \| wc -l` |
| 其中非 `测试_` 的 `.py` | **7** | 6 个 `__init__.py` + `运行核心/冷启动脚本.py` |
| `测试_工作包NN_` 形式文件 | **11 个，全在 `测试中心/平台控制面/`** | `find 测试中心 -name '测试_工作包*.py'` |

> **任务标题「30 个阶段目录」的口径出处说明**：现场没有 30 个阶段目录。与「30」最接近的可核实现象有两处，都不等于「阶段目录」：① 顶层目录 28 个；② `测试_工作包01…11` 这 11 个文件**已经**在能力域目录 `平台控制面/` 里（**不是**待归口的阶段目录）。**本项应按「4 个批次/旧层命名的有内容目录 + 15 个空壳目录」这一实测口径处置**，详见 §5.3。

---

## 二、引用面全量硬数据（本项裁决的核心依据）

### 2.1 分桶总表（实测）

扫描面：全仓 `3340` 个 `.py`/`.md`/`.json`（排除 `工程缓存/`、`.git/`、`__pycache__/`）。
判定口径：把既有方案 `开发文档/方案/测试中心归口方案_20260918.md` 的 204 行映射表解析成「旧路径 → 新路径」，**逐条比对**引用命中的是不是一个会改名的测试模块。

**该方案实测：204 个 `.py` 中，57 个真的改路径，147 个原位不动。**

| 桶 | 点号引用条数 | 斜杠引用条数 | 合计 | 文件数 | 后果 |
|---|---|---|---|---|---|
| **A 源码/配置/规约** | 6 | 17 | **23** | **12** | **真会断 / 真会过期，必须同批改写** |
| B `测试中心/` 自身 | 0 | 3 | 3 | 3 | docstring 注释，同批改 |
| C 活文档（方案/交付记录/决策记录/分析） | 13 | 83 | 96 | 17 | 历史事实，按 12.4⑤ **一律不改**；仅牵涉派活的需加迁移注记 |
| D 归档件 | 0 | 5 | 5 | 2 | 已声明不作派活依据，不改 |
| E 归口方案自身 | 4 | 14 | 18 | 1 | 方案文档自述 |
| **合计** | **23** | **122** | **145** | **35** | |

### 2.2 【A 桶】真会断/真会过期的引用 —— 23 处 / 12 文件（逐条）

| # | 文件 | 处数 | 引用内容 | 归口后 |
|---|---|---|---|---|
| 1 | `AGENTS.md` | **3** | `:83` `测试中心.第一批维修.测试_HTML验证器`；`:87` 同一模块 + `测试中心.第一批维修.测试_客户端构建安全` | 失效 → `测试中心.开发工具.测试_HTML验证器`、`测试中心.平台控制面.测试_客户端构建安全` |
| 2 | `公共契约/HTTP路由契约.json` | **6** | `:93/:103` `测试中心/第一批维修/测试_节点M2_40007流式网关接线.py`（「验证场景」字段值 ×2）；`:111/:121/:133/:141` `测试中心/第一批维修/测试_能力反馈接口.py`（×4） | 失效 → `测试中心/运行核心/…`、`测试中心/平台控制面/…` |
| 3 | `公共契约/能力契约/契约指纹.py` | 1 | `:10` `测试中心/开发工具/测试_依赖防火墙口径.py` | 失效 → `测试中心/运行核心/…` |
| 4 | `平台控制面/提供者/进程提供者.py` | 2 | `:155/:190` docstring `测试中心/第一批维修/测试_Provider进程.py::test_P0_14` | 失效 → `测试中心/平台控制面/…` |
| 5 | `开发工具/测试体系门禁.py` | 1 | `:206` `测试中心.开发工具.测试_核心快照模板`（存量真跑红基线说明里的模块名） | 失效 → `测试中心.支持库/…` |
| 6 | `开发工具/统一能力入口/视图/错误码与建议口径说明.md` | 2 | `:81` 斜杠路径、`:88` 点号模块名，均指 `测试_视图错误码键一致` | 失效 → `测试中心/开发工具/…` |
| 7 | `支持库/适配层/TreeSitter提供者/实现/提供者.py` | 1 | `:183` docstring `测试中心/支持库/核心/测试_语法索引.py` | 失效 → `测试中心/支持库/测试_语法索引.py` |
| 8 | `支持库/适配层/TreeSitter提供者/生命周期契约.json` | 1 | `:17` 同上 | 失效（**改此 JSON 会触发摘要重算**） |
| 9 | `支持库/适配层/TreeSitter提供者/说明/目录树与命令.md` | 1 | `:34` `测试中心.支持库.核心.测试_语法索引` | 失效 |
| 10 | `运行核心/依赖防火墙.py` | 2 | `:194/:744` `测试中心/开发工具/测试_依赖防火墙口径.py` | 失效 → `测试中心/运行核心/…` |
| 11 | `运行核心/加载器/提供者隔离/独立进程.py` | 2 | `:401/:437` docstring `测试中心/第一批维修/测试_Provider进程.py::test_P0_14` | 失效 → `测试中心/平台控制面/…` |
| 12 | `运行核心/运行诊断/诊断中心/诊断中心.py` | 1 | `:58` `验证场景表` 字典值 `"依赖循环拒绝": "测试中心/加载器/测试_加载器.py"` | 失效 → `测试中心/运行核心/…` |
| — | **合计** | **23** | | |

### 2.3 【A 桶】零引用的部分 —— 15 个空壳目录

**实测：15 个空壳目录被「源码/配置/规约」引用的条数 = `0`。**

```
测试中心/慢速层/   测试中心/辅助/      测试中心/权威状态/   测试中心/结构迁移/
测试中心/资源并发/ 测试中心/第三阶段/  测试中心/第四阶段/   测试中心/第五阶段/
测试中心/第六阶段/ 测试中心/第七阶段/  测试中心/第八阶段/   测试中心/第九阶段/
测试中心/第十阶段/ 测试中心/第十一阶段/ 测试中心/第十三阶段/
```

取证（四条独立证据，全部实测）：
1. **源码/配置零引用**：`0` 处（上表；判定正则 `测试中心[/\.]<目录名>[/\.\s"'\`\)\]\},;:]`）。
2. **无 import 语句**：`grep -rn "^ *(from|import) *测试中心\.(慢速层|辅助|权威状态|结构迁移|资源并发|第N阶段)" --include=*.py` → **空**。
3. **git 未追踪**：`git ls-tree -r --name-only HEAD 测试中心/ | grep -E '第三阶段|慢速层|权威状态|结构迁移|资源并发'` → **0 条**（git 从不追踪空目录）。
4. **无门禁断言目录数**：`grep -rn "顶层目录数|目录数.*28|== 28" --include=*.py --include=*.json` → **0 条**。`开发工具/测试体系门禁.py` 的发现口径是 `rglob("测试_*.py")`（`开发工具/测试体系门禁实现/发现.py:68`），**只认文件、不数目录**。

> 唯一的引用在 `开发文档/`：`归档/202608_第十四阶段/工作包清单.json` 4 处（归档件，12.4⑤ 不作派活依据）+ `方案/测试中心归口方案_20260918.md` 17 处（方案文档自述）。

### 2.4 门禁与目录约定的实际依赖（实测）

| 依赖方 | 依赖形态 | 是否依赖域名/目录数 |
|---|---|---|
| `开发工具/测试体系门禁.py` | `发现.发现测试文件(根)` → `测试中心.rglob("测试_*.py")` | **只依赖 `测试中心/` 存在 + 文件名 `测试_*.py`**；域名随 `rglob` 任意变化，不受影响 |
| `开发工具/测试伪装门禁实现/发现.py` | 同口径 `rglob` | 同上 |
| `开发工具/测试伪装门禁实现/命名空间.py` | `测试自身根 = ("测试中心",)`（顶层段常量） | 只依赖顶层名 `测试中心` |
| `开发工具/组件合规/模块合规.py:34` | `系统根 = next(祖先 for … if (祖先/"模块库").is_dir() and (祖先/"测试中心").is_dir())` | 只依赖顶层名 `测试中心` |
| `开发工具/发布门禁/运行发布门禁.py:930` | `_视图登记源 = ("测试中心", "平台控制面", "测试_视图错误码键一致.py")` | **依赖三段路径全等** → 见 §5.1 |
| `AGENTS.md:236` | `测试中心/   独立测试环境（unittest），唯一测试代码位置` | 只依赖顶层名 |
| `测试中心/__init__.py` | `系统根目录 = 测试中心目录.parent` 自举 | 只依赖自身位置 |

**结论**：门禁体系**不**对「阶段目录」或任何子域名做约定；既有方案里的「9 个能力域」是方案的**自定目标**，不是任何门禁的既有约束。

---

## 三、可选方案（两案，各自代价）

> 两案的**共同前置**（无论选哪案都必须先做，否则 `git mv` 会因「路径不在索引里」失败）：
> ```bash
> cd ~/Documents/Agent/PHP/系统工程平台
> export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH
> git add 测试中心            # 80 项在途/未追踪先入索引；不提交
> ```

### 方案 A：执行搬运（全量归口）

**做什么**：按 `开发文档/方案/测试中心归口方案_20260918.md` 的 204 行映射表执行 —— 57 个测试模块改模块名、4 个批次/旧层目录消解、`支持库/核心/` 上移、18 个空壳目录删除、`__pycache__` 全清。

**硬代价（实测）**：

| 代价项 | 实测值 |
|---|---|
| 改名测试模块 | **57 个**（204 中 147 个原位不动） |
| 必改的源码/配置/规约引用 | **23 处 / 12 文件**（§2.2 全表） |
| 触发摘要重算的包 | **≥1**（`支持库/适配层/TreeSitter提供者/生命周期契约.json` 改了就要跑 `开发工具/全量重算摘要.py`） |
| 门禁影响 | `公共契约/HTTP路由契约.json`「验证场景」字段 ×6 —— 该文件被 `测试中心/第一批维修/测试_网关安全与有界并发.py:468` 直读；改错即门禁红 |
| 与在途改动冲突 | `测试中心/` 现 **80 项未提交**，其中 `加载器/`(4)、`发布门禁/`(1) 恰是**待搬目录** → 与并行会话**直接冲突** |
| 前置未决 | §5.1 的判据自相矛盾**未裁定**；本方法动 `测试_视图错误码键一致.py`，而方案正文自述「本就不动它」 |
| 回滚 | 方案 §5.3 已备 L1/L2/L3 三档 |
| 规模 | **中**（映射表已逐行给出，机械执行 + 12 文件改写 + 定向冒烟） |

### 方案 B：不搬（只删空壳 + 把 4 个批次目录的归口另立台账）

**做什么**：两段，**可分开执行**：

- **B1（零引用面，可立即做）**：删除 15 个空壳顶层目录 + `慢速层/`、`辅助/` 下的 6 个空壳子目录。顶层 **28 → 13**。
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  # 先看，后删（--dry-run 式：先列）
  find 测试中心 -type d -empty | sort          # 实测 19 条
  # 确认 19 条全部在 §2.3 清单内后：
  find 测试中心 -type d -empty -delete
  ```
  **硬依据**：源码/配置引用 `0` 处；无 import；git 从不追踪；无门禁断言目录数（§2.3 四条证据）。
  **代价**：**近零**。唯一副作用是把方案 §1.3「空壳 → 直接删」这一步提前落地（与既有方案同向，不冲突）。

- **B2（引用面大，另立台账）**：`第一批维修/`(28)、`加载器/`(5)、`发布门禁/`(4)、`底座升级/`(1) 共 **38 个文件 / 4 个目录**的归口**不在本次执行**，改为在 `未完成事项.md` 里单独立项（连同 §5.1 待裁点一起）。理由：这 38 个文件牵动 §2.2 的 **23 处 / 12 文件**引用 + 80 项在途改动，属「大重构」，按 1.1「能解决就修到底、解决不了就重构」应单独立项排期。

**硬代价**：0 处引用改写；0 个模块改名；0 个摘要重算；0 与在途改动冲突。
**遗留**：`测试中心/` 顶层仍有 13 个目录，其中 4 个（`第一批维修`、`加载器`、`发布门禁`、`底座升级`）仍是批次/旧层命名 → **「测试入口可由受影响包推导」这个目标只达成一半**。

### 两案对照

| 维度 | 方案 A 全量搬 | 方案 B 只删空壳 |
|---|---|---|
| 改动测试模块名 | 57 个 | **0** |
| 改源码/配置引用 | **23 处 / 12 文件** | **0** |
| 摘要素重算 | ≥1 包 | 0 |
| 与 80 项在途改动冲突 | **是**（`加载器/`、`发布门禁/` 待搬） | 否 |
| 前置未决 | §5.1 必须先裁 | 无 |
| 顶层目录 28 → | 10 | 13 |
| 「测试入口可推导」 | **完全达成** | 达成一半 |
| 风险 | 中（12 文件必须同批改对） | **近零** |

### 小马仔建议

**先执行 B1，B2 与方案 A 一起交华哥排期**。理由三条（全部有硬数据）：
1. 任务标题里的「**阶段目录**」在现场**全部是空壳**（10 顶层 + 3 嵌套，零文件、零引用），B1 正好把这一半清干净，且**代价近零**；
2. 真正贵的是「批次/旧层命名的**有内容**目录」（38 文件），它牵动 **23 处 / 12 文件**引用 —— 引用面**大**，按任务口径**应落文档不硬搬**；
3. 现场有 **80 项 `测试中心/` 未提交改动**，且待搬目录里的 `加载器/`(4)、`发布门禁/`(1) **正在被并行会话改** —— 此刻硬搬 = 与在途任务对撞（8.4 第 53 项已就此类「不并行动」立过判据）。

---

## 四、验证方式

### 4.1 若执行 B1（删空壳）后

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
find 测试中心 -maxdepth 1 -type d -not -name __pycache__ | wc -l      # 期望 13（原 28）
find 测试中心 -type d -empty | wc -l                                   # 期望 0（原 19）
find 测试中心 -name '测试_*.py' -not -path '*__pycache__*' | wc -l     # 期望 202（不变）
python3.14 -m 开发工具.测试体系门禁 --只报告                            # 期望：测试文件 202，与删前同值
# 定向冒烟（证明删目录没伤到任何测试）
python3.14 -m unittest 测试中心.平台控制面.测试_工作包11_消费者契约注册表
python3.14 -m unittest 测试中心.支持库.测试_模型协议别名 测试中心.模块库.测试_测试资源
```

### 4.2 本文数字的复现

盘点脚本（只读，可直接重跑）落在 `/tmp`：

| 脚本 | 作用 |
|---|---|
| `/tmp/归口盘点_硬数字.py` | §1.4 数量总表 + §2.1 分桶总表 |
| `/tmp/归口终稿_数字.py` | §2.2 A 桶 23 处逐条 + §2.3 空壳零引用核验 |
| `/tmp/解析归口映射.py` | 解析方案映射表 → 204 条 / 57 改名 / 147 不动 |
| `/tmp/判定_断引用.py` | 按映射逐条判定引用是否断 |
| `/tmp/测试中心归口_全量引用面.json` | 全量引用面中间产物 |

复现前置：HEAD `96a09976`、测试资产 md5 前 16 位 `3f7482964b4b1542`、扫描面 3340 文件。

---

## 五、风险清单与待裁点

### 5.1 【高·新发现】既有归口方案自身存在判据自相矛盾 —— 必须先裁

**实测两处互斥表述**，在**同一份文档**里：

| 位置 | 表述 |
|---|---|
| `开发文档/方案/测试中心归口方案_20260918.md:381`（映射表） | `平台控制面/测试_视图错误码键一致.py` → **`开发工具/测试_视图错误码键一致.py`**（**要搬**） |
| 同文件 `:721`（风险清单 §6.2） | 「`测试中心/平台控制面/测试_视图错误码键一致.py` … ✅ **域名与文件名都不变，零改动**」（**不搬**） |
| 同文件 `:725` | 「这 8 个域名与 `测试_视图错误码键一致.py` 这个文件名**此后不许再改**」 |
| 同文件 `:747` | 「**本方案（按域归位）本就不动它，故迁移可安全进行**」 |

**为什么这是硬伤（生产侧实测）**：

```
开发工具/发布门禁/运行发布门禁.py:930   _视图登记源 = ("测试中心", "平台控制面", "测试_视图错误码键一致.py")
开发工具/发布门禁/运行发布门禁.py:1268  值 = _读模块字面量(扫描根.joinpath(*_视图登记源), 变量)
```

发布门禁**按三段路径硬读**这个文件取「向后兼容英文键」登记真源。**若按映射表把它搬到 `开发工具/`，`运行发布门禁.py:930` 就会读不到文件 → 登记真源缺失 → 门禁红**（而方案 §6.2 声称「零改动」，正是没意识到自己的映射表把它搬走了）。

> **待裁**：`测试_视图错误码键一致.py` 到底搬不搬？搬，则必须**同批**改 `运行发布门禁.py:930/1268` 并说明「生产门禁依赖测试路径」的取舍；不搬，则映射表 §381 行要改。
> **另有一条独立在途红**（与本项无关，勿归因给归口）：`python3.14 -m unittest 测试中心.第一批维修.测试_发布门禁` 实测 **Ran 12 tests → 1 FAIL**（`test_删除测试中心后生产门禁仍可导入并执行制品选择`，`:213` 断言发布门禁源码不得出现「测试中心」字样）。直接执行 `开发工具/发布门禁/运行发布门禁.py:930` 现含 `测试中心`；方案 §6.3 已查出 HEAD 上该断言原本是绿的，红由并行会话的未提交改动引入 —— **归因属并行会话裁决，不是本项**。

### 5.2 【中】与并行在途改动对撞

`测试中心/` 现 **80 项未提交**。待搬目录中 `加载器/`(4 文件)、`发布门禁/`(1 文件) **正在被改**：

```
 M 测试中心/加载器/测试_加载器.py
 M 测试中心/加载器/测试_装配单包隔离.py
 M 测试中心/加载器/测试_装配环境缓存.py
 M 测试中心/加载器/测试_装配锁与幂等.py
 M 测试中心/发布门禁/测试_发布门禁收敛.py
```

此刻对其执行 `git mv`/删除，会把并行会话的中间态一起卷进迁移。**这正是本次不硬搬的直接理由之一。**

### 5.3 【中】任务口径与现场不一致 —— 需回填

任务标题写「**30 个阶段目录**」，现场实测：顶层目录 **28** 个，其中阶段命名 **10** 个且**全部为空壳**，真正有内容的批次/旧层目录 **4** 个。
**建议**：§8.4 第 8 项改述为「`测试中心/` 批次/旧层命名目录按能力域归口（4 目录 / 38 文件）+ 清理 15 个空壳目录」，避免下一轮照「30 个阶段目录」派活时**找不到对象**（同 8.3.1 派活前复核纪律）。

### 5.4 【低】`工程缓存/` 不参与

`工程缓存/` git 未追踪、是内容寻址的不可变制品快照（含旧路径字符串）。按 1.7 与既有方案 §6.6，**一处不动**（本项全程未触碰）。

### 5.5 【低】`开发文档/` 历史件不改

C 桶 96 处 / D 桶 5 处按 12.4⑤「历史由 git 保留」**一律不改**。唯 `方案/裸布尔迁移分批方案_20260917.md`（42 处，是本仓最大引用面）**若将来执行搬运，需追加迁移注记**，否则下一批照它派活会定位不到文件。

---

## 六、交付物与状态

| 项 | 状态 |
|---|---|
| 只读盘点 | ✅ 完成（28 目录逐个清点、引用面全量扫描 3340 文件） |
| 引用面硬数据 | ✅ 完成（A 桶 23 处 / 12 文件逐条；分桶总表 145 条 / 35 文件） |
| 两方案与代价 | ✅ 完成（§三 对照表） |
| 新发现的判据矛盾 | ✅ 完成（§5.1，附生产侧取证行号） |
| **目录搬运** | ❌ **未执行**（引用面大 + 在途冲突 + 前置待裁 → 按任务口径落文档） |
| **空壳目录删除** | ❌ **未执行**（同上；命令已备于 §三 B1，零引用面硬数据已备于 §2.3） |
| 本文件 | `开发文档/分析/测试中心归口盘点_20260918.md`（唯一新增物） |
