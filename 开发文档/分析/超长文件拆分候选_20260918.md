# 超长文件拆分候选（2026-09-18）

**性质**：**候选清单，不是门禁**。口径见 `项目说明.md` 哲学 6.1（华哥 2026-09-18 建议：超 1000 行「拆小一点，降低到 800 左右」＝建议）。
**排序依据**：哲学 6.1 的两条判据 —— **改动频率 × 读取成本**。提交次数高＝每次读全量上下文都亏；行数高＝读取成本高。**两者都高才是第一优先**。

## 一、第一优先：高频 × 超长（提交 ≥ 20 次且 > 800 行）

| 行数 | 提交次数 | 文件 | 拆分方向（待裁决，本表只登记） |
|---:|---:|---|---|
| 2626 | 49 | `开发工具/发布门禁/运行发布门禁.py` | 按检查项簇拆：防回潮四项 / 能力面门禁 / 编译与摘要 / 接线主流程；主文件只留调度 |
| 1667 | 35 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` | 按「连接 / 流式 / 工具调用 / 重试」拆 |
| 1240 | 35 | `运行核心/统一网关/网关核心.py` | 🟡 **部分已拆（2026-09-18）**：1240 → **1074 行**，拆出 `协议表.py`(467) / `类型规格.py`(201)；剩余簇耦合过深（拆分路判断「硬拆会引入循环依赖」），**待后续评估** |
| 1156 | 30 | `运行核心/统一网关/本地网关.py` | 按「转发 / 流式 / 审计」拆 |
| 963 | 20 | `平台控制面/包仓库/平台客户端制品.py` | ✅ **已拆（2026-09-18）**：963 → **520 行**，拆出 `制品布局.py`(167) / `激活指针面.py`(287) / `环境回收面.py`(196)；测试 18/18 OK |

## 二、第二优先：中频 × 超长（提交 10–19 次且 > 800 行）

| 行数 | 提交次数 | 文件 |
|---:|---:|---|
| 1533 | 17 | `开发工具/公开调用完整性门禁.py` |
| 1259 | 16 | `运行核心/权威状态.py` |
| 2150 | 11 | `开发工具/契约编译/漂移检测.py` |
| 1016 | 10 | `支持库/后端/并发控制支持库/实现/并发控制.py` |
| 998 | 19 | `开发工具/组件合规/合规测试包.py` |
| 930 | 13 | `运行核心/任务调度/任务进程.py` |
| 874 | 14 | `支持库/后端/系统核心支持库/资源管理/实现/资源管理.py` |
| 868 | 18 | `客户端/构建平台客户端.py` |
| 811 | 18 | `后端核心/后端核心.py` |

## 三、低频观察（提交 ≤ 7 次，按 6.1「低频文件到 800 行也无所谓」不拆）

| 行数 | 提交次数 | 文件 |
|---:|---:|---|
| 1120 | 1 | `开发工具/环境自检.py` |
| 845 | 7 | `技能库/后端/技能库/实现/技能库.py` |
| 835 | 5 | `公共契约/运行时/进程终止.py` |

## 复现命令

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
# 行数（排工程缓存与归档）
find . -name '*.py' -not -path './工程缓存/*' -not -path './归档/*' -not -path '*/__pycache__/*' \
  -exec wc -l {} + | sort -rn | awk '$1>800'
# 提交次数
git log --oneline --follow -- <文件> | wc -l
```
