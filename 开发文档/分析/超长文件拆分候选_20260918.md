# 超长文件拆分候选（2026-09-18）
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


**性质**：**候选清单，不是门禁**。口径见 `项目说明.md` 哲学 6.1（华哥 2026-09-18 建议：超 1000 行「拆小一点，降低到 800 左右」＝建议）。
**排序依据**：哲学 6.1 的两条判据 —— **改动频率 × 读取成本**。提交次数高＝每次读全量上下文都亏；行数高＝读取成本高。**两者都高才是第一优先**。

## 一、第一优先：高频 × 超长（提交 ≥ 20 次且 > 800 行）

| 行数 | 提交次数 | 文件 | 拆分方向（待裁决，本表只登记） |
|---:|---:|---|---|
| 2626 | 49 | `开发工具/发布门禁/运行发布门禁.py` | 按检查项簇拆：防回潮四项 / 能力面门禁 / 编译与摘要 / 接线主流程；主文件只留调度 |
| 1667 | 35 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` | 🟢 **已拆完（2026-09-19）**：1591 → **744 行**（≤800 达标），本轮再拆出四段到同目录：`实现/模型连接基元.py`(225) / `实现/环境配置.py`(127) / `实现/本地启动准备与守卫.py`(383) / `实现/模型工具调度.py`(356)。**已逐条遵循 2026-09-18 的两条实测结论**：①「本地模型启动器」的**进程与句柄段**（`_启动本地模型`/`启动本地模型`/`_终止本地进程`/`注册本地进程`）仍不拆 —— 它引用文件内 `_内存守卫`/`_包申报超时`/`_句柄键`/`_回收过期句柄`/`释放句柄` 等（本轮把其中「启动准备层」下移到新模块并按名导入，栈仍单向向下，未制造双向依赖）；②「句柄与连接管理」段仍不拆（公共底座，`连接表`/`调用函数表`/`锁`/`句柄系统` 是各段共用账本）。**对外零变化已验**：与冻结基线（`/tmp/拆分基线/模型连接器.py`，sha256 前16=88844a886830e4ed，HEAD=1dc0f0f6）并排比对 **105 个成员名 / 61 个签名 / 20 个模块级常量值 / 44 项行为探针 全零差异**。**逐字节搬运**：852 行搬出仅 3 行属白名单改写（`环境配置 = 配置` → 原对象就地更新以保别名活性；`_等待本地健康` 两行裸布尔 → `真`/`假`，防回潮分桶基线把新文件判新增，改后本包累计 8→6）。`_HTTP调用模型` 的 `NameError`（引用了从未导入的 `_响应归一化体`）**原样保留**，未顺手修 —— 修它会同时改变 3 个测试的判定。**连带改动**：`测试中心/运行核心/测试_硬件画像与档位.py`（现值对账的源码路径随之改到 `实现/模型连接基元.py`）、`支持库/适配层/说明/README.md`（两行引用计数与路径）、`开发工具/发布门禁/防回潮存量基线.json`（本文件 8→6）。 |
| 1338 | 40 | `运行核心/统一网关/网关核心.py` | ✅ **已按职责拆到位（2026-09-19）**：1338 → 1053 → 942 → 784 → **99 行**（宿主只剩装配骨架）。**两轮**：① 判据源先迁 + 搬 `公开错误说明表`（405 行/348 码 → `错误说明表.py`），3 门禁源 + 1 测试同批改指新落点；② 按华哥新裁决「能拆多细就多细，按职责拆」把宿主按职责拆穿，拆出 **8 个职责文件**：`调用点缺参判定.py`(41) / `参数别名表.py`(33) / `操作协议表.py`(25) / `幂等协议常量.py`(23) / `网关幂等面.py`(169) / `网关参数校验面.py`(126) / `网关执行面.py`(279) / `网关处理面.py`(157)；连同先前拆出的 `类型规格.py`(201) / `幂等存储.py`(114) / `网关信封.py`(78)，宿主仅留：四职责混入类装配 + `__init__`（唯一状态归属）+ 两个装配 setter + 全部再导出。**对外零变化已验**：拆前 21 个类内方法全在位、公开名只少 0 项、`公开错误说明表 is 错误说明表.公开错误说明表` 等对象同一性全 True、21 个同包模块全部导入 OK、35 个导入方冒烟通过。**定点测试 6 组全绿**（网关兼容性 11/11、类型收口 3/3、任务接入 8/8、资源句柄 8/8、安全与有界并发 18/18、协议表唯一性 6/6）。★ 拆出的 8 个新文件建了「跨文件依赖」段（原是同一模块内的兄弟名）；`测试_网关协议表唯一性` 的 `表正本表` 从「网关核心.py」改为四张表各自的落点。 |
| 1156 | 30 | `运行核心/统一网关/本地网关.py` | 按「转发 / 流式 / 审计」拆 |
| 963 | 20 | `平台控制面/包仓库/平台客户端制品.py` | ✅ **已拆（2026-09-18）**：963 → **520 行**，拆出 `制品布局.py`(167) / `激活指针面.py`(287) / `环境回收面.py`(196)；测试 18/18 OK |

## 二、第二优先：中频 × 超长（提交 10–19 次且 > 800 行）

| 行数 | 提交次数 | 文件 |
|---:|---:|---|
| 1613 | 15 | `公共契约/运行时/平台适配.py` | ✅ **已拆（2026-09-23，华哥点名「1562 行，拆分一下，之前有拆分的哲学的」）**：1613 → 同名**包** `公共契约/运行时/平台适配/`。**不留平铺薄入口**（与 `开发工具/环境自检.py` 的先例**不同**）：实测 `编译口._落到真模块` 的解析顺序是「先 `.py` 本身，再退到 `包.__init__`」，保留平铺件会让 import 边落在平铺件上、包 `__init__` 收不到边，被 `未接线非包py` 判成「无覆盖」；导入地址 `公共契约.运行时/平台适配` **逐字不变**，90 处调用点零改动。按职责拆成 **14 个子文件**（判定 / 采样基元 / 硬件画像 / 准入 / 身份对账 / 要求POSIX / 解释器 / 资源口径 / 非阻塞读 / 删除树 / **转写后端选择** / 进程分叉 / 路径解析 / 系统位置）+ `__init__.py` 只做再导出（并补齐 stdlib 属性面 `platform`/`os`/`sys`…，`测试_转写跨平台后端.py` 的 15 处 `mock.patch.object(平台适配.platform, …)` 依赖它）。**子模块名避开与导出函数同名**：原定 `转写后端.py` 与它导出的 `转写后端()` 同名，实测同名会让「摘掉回导」**不抛 `ImportError`**（子模块顶替了函数位）⇒ 改名 `转写后端选择.py`。零对外变化验收 = 与拆前冻结基线（**57 成员名 / 36 模块常量值 / 46 行为探针 / `__all__` 63 项顺序**）逐项零差异 + 149 个名字逐名 `is` 同一性核过 + 摘回导必判红（公开名与私有名各一次，反向）。 |
| 1533 | 17 | `开发工具/公开调用完整性门禁.py` |
| 1259 | 16 | `运行核心/权威状态.py` |
| 2150 | 11 | `开发工具/契约编译/漂移检测.py` |
| 1016 | 10 | `支持库/后端/并发控制支持库/实现/并发控制.py` |
| 998 | 19 | `开发工具/组件合规/合规测试包.py` |
| 930 | 13 | `运行核心/任务调度/任务进程.py` |
| 874 | 14 | `支持库/后端/系统核心支持库/资源管理/实现/资源管理.py` |
| 868 | 18 | `客户端/构建平台客户端.py` |
| 811 | 18 | `后端核心/后端核心.py` |

## 三、低频观察（提交 ≤ 7 次，按 6.1「低频文件到 800 行也无所谓」不拆）

| 行数 | 提交次数 | 文件 |
|---:|---:|---|
| 1120 | 1 | `开发工具/环境自检.py` |
| 845 | 7 | `技能库/后端/技能库/实现/技能库.py` |
| 835 | 5 | `公共契约/运行时/进程终止.py` |

## 复现命令

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
# 行数（排工程缓存与归档）
find . -name '*.py' -not -path './工程缓存/*' -not -path './归档/*' -not -path '*/__pycache__/*' \
  -exec wc -l {} + | sort -rn | awk '$1>800'
# 提交次数
git log --oneline --follow -- <文件> | wc -l
```
