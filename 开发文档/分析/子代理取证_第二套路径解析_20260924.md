# 子代理取证 · 「第二套路径解析」落点清单（2026-09-24）

> 来源：**只读**子代理 Gauss（`01a0cf0b-4d21-7861-88aa-aaa8fdcfedd9`），任务信要求「只读、不占租约、不写文件、不跑编译」
> 口径：**主会话未逐条复跑** —— 本件是子代理回执的**原样留档**，派活前必须按 `未完成事项.md` §一.1 现场复核
> 用途：`开发文档/分析/审计落点_单一节点收口_20260924.md` §三 S2 的逐条落点
> 唯一腿：`公共契约/运行时/平台适配/路径解析.py::解析路径`（相对按生效根拼绝对）＋ `公共契约/基础类型/字段名册.py::是路径族字段`
> 唯一消费点：`运行核心/统一网关/核心处理/网关参数校验面.py::_归一化路径族参数`
<!-- 机器管理｜类型：分析｜生成器：人工 -->

---

## 一、计数（子代理实测）

| 类 | 形状 | 命中 | 涉及文件 |
|---|---|---|---|
| 1 | 自带 `is_absolute()` 分支拼根（自己在做相对→绝对拼接） | **21** | 17 |
| 2 | 自己 `parents[N]` / `__file__.parents` 推根**并拿它当相对路径基准** | **9** | 9 |
| 3 | 自己 `os.path.join(根, 相对入参)` / `Path(根) / 相对入参` | **8** | 8 |
| 4 | 自建第二份「路径族参数名名单」 | **0** | 0（源码面） |

合计源码面 **38 处 / 30 个文件**（`开发工具/派活前核验.py`、`开发工具/MD文档生成/人工编辑.py`、`开发工具/快速编译/单文件自查.py`、`开发工具/契约编译/生成物一致性检查.py`、`开发工具/契约编译/对外契约变更判定.py`、`平台控制面/提供者/实现/能力入口.py`、`运行核心/运行状态监督/实现/提供者.py` 同时命中类 1 与类 2）。

**扫描规模**：`rg --files -g '*.py'` 排除 `工程缓存/`、`.git/`、`**/__pycache__/`、`开发文档/归档/`、`开发文档/参考资料/`、`产物/`、`生成物/`、`**/node_modules/` ⇒ **1291** 个 `.py`。

---

## 二、类 1 · 自带 `is_absolute()` 分支拼根（21 处）

```
开发工具/派活前核验.py:396         if not 目标.is_absolute():
开发工具/派活前核验.py:397             目标 = 系统根 / 参.多条
开发工具/派活前核验.py:404         if not 目标.is_absolute():
开发工具/派活前核验.py:405             目标 = 系统根 / 参.查重
开发工具/派活前核验.py:412         if not 目标.is_absolute():
开发工具/派活前核验.py:413             目标 = 系统根 / 参.落点
开发工具/快速编译/单文件自查.py:41  路径 = 系统根 / 相对路径 if not Path(相对路径).is_absolute() else Path(相对路径)
开发工具/验证场景体检.py:261        制品 = Path(参数.覆盖) if os.path.isabs(参数.覆盖) else 根 / 参数.覆盖
开发工具/验证场景体检.py:288        制品基 = Path(参数.制品) if os.path.isabs(参数.制品) else 根 / 参数.制品
开发工具/契约编译/对外契约变更判定.py:590  包目录 = Path(包目录) if Path(包目录).is_absolute() else 项目根 / 包目录
开发工具/契约编译/生成物一致性检查.py:484  路径 = (仓库根 / 规范化) if not Path(规范化).is_absolute() else Path(规范化)
开发工具/项目编译/项目编译器.py:334  if not 目标.is_absolute():
开发工具/项目编译/项目编译器.py:335      目标 = 项目 / 目标
开发工具/MD文档生成/人工编辑.py:107   绝对 = (候选 if 候选.is_absolute() else 项目根 / 候选).resolve()
开发工具/MD文档生成/人工编辑.py:174   草稿路径 = Path(草稿) if Path(草稿).is_absolute() else 项目根 / 草稿
开发工具/MD文档生成/说明书/类型入口.py:33  路径 = (项目根 / 单文件) if not Path(单文件).is_absolute() else Path(单文件)
开发工具/MD文档生成/说明书/合并重生成.py:935  包目录 = (系统根 / 原文) if not Path(原文).is_absolute() else Path(原文)
技能库/技能/交付收尾/scripts/交付收尾.py:110  return 路径 if 路径.is_absolute() else (项目根 / 路径)
技能库/技能/验证编排/scripts/验证编排.py:197  return 路径 if 路径.is_absolute() else (项目根 / 路径)
平台控制面/提供者/实现/能力入口.py:46  return 路径 if 路径.is_absolute() else (系统根目录 / 路径)
运行核心/运行状态监督/实现/提供者.py:50  return (路径 if 路径.is_absolute() else Path(平台系统根) / 路径).resolve()
支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py:799  绝对 = 候选 if 候选.is_absolute() else (根 / 候选)
支持库/后端/文件系统支持库/内容检索/实现/内容检索.py:58  相对 = str(路径.resolve().relative_to(根目录)) if 路径.is_absolute() \
支持库/后端/文件系统支持库/内容检索/实现/内容检索.py:59      else str((根目录 / 路径).resolve().relative_to(根目录))
支持库/后端/大语言模型支持库/模型连接器/实现/模型供应链校验.py:111  return 目标.resolve() if 目标.is_absolute() else (Path.cwd() / 目标).resolve()
sitecustomize.py:122              if not 候选.is_absolute():
sitecustomize.py:123                  候选 = Path.cwd() / 候选
```

**已排除**（子代理口径，供复核）：`测试中心/**` 里 `is_absolute()` 命中全是 `assertTrue(...)` 断言（不拼根）；`公共契约/运行时/平台适配/路径解析.py` 本体、`运行核心/统一网关/核心处理/网关参数校验面.py::_归一化路径族参数` 本体、`公共契约/基础类型/字段名册.py` 本体；`支持库/适配层/Swift编译器提供者`、`支持库/适配层/Tesseract提供者` 里的外部 CLI 参数拼装（外部契约，非平台入参口径）；只判绝对性、不拼根的纯拒绝判据 10 处。

---

## 三、类 2 · `parents[N]` 推根后做基准（9 处）

```
开发工具/派活前核验.py:27                    系统根 = Path(__file__).resolve().parents[1]
  → :397 / :405 / :413 / :425                目标 = 系统根 / 参.*
开发工具/快速编译/单文件自查.py:32            系统根 = Path(__file__).resolve().parents[2]
  → :41                                      路径 = 系统根 / 相对路径 …
开发工具/契约编译/对外契约变更判定.py:139      默认项目根 = Path(__file__).resolve().parents[2]
  → :590                                     包目录 = … else 项目根 / 包目录
开发工具/契约编译/生成物一致性检查.py:99       仓库根 = Path(__file__).resolve().parents[2]
  → :484                                     路径 = (仓库根 / 规范化) …
开发工具/MD文档生成/人工编辑.py:163-164       项目根 = Path(__file__).resolve() … while 项目根.name and not (项目根 / "开发文档").is_dir()
  → :107 / :174                              绝对 = … else 项目根 / 候选
平台控制面/提供者/实现/能力入口.py:32-33       本包目录 = …parents[1]；系统根目录 = 本包目录.parents[1]
  → :46                                      return 路径 if 路径.is_absolute() else (系统根目录 / 路径)
平台控制面/能力目录/实现/能力入口.py:29        _包内系统根 = Path(__file__).resolve().parents[3]
  → :185                                      根 = _文本(平台根) or str(_包内系统根)
运行核心/依赖防火墙.py:76                    系统根 = Path(__file__).resolve().parents[1]
  → 运行核心/运行状态监督/实现/提供者.py:24    from 运行核心.依赖防火墙 import 系统根 as 平台系统根
  → 运行核心/运行状态监督/实现/提供者.py:50    return (路径 if 路径.is_absolute() else Path(平台系统根) / 路径).resolve()
开发工具/依赖生命周期审计/运行依赖生命周期审计.py:20  默认系统根 = 本文件目录.parents[1]
  → :44                                      系统根 = 系统根 or 默认系统根（用作扫描根，非相对→绝对拼；子代理标注「备核」）
```

---

## 四、类 3 · 自己 `os.path.join(根, 相对入参)` / `Path(根) / 相对入参`（8 处）

```
支持库/后端/系统核心支持库/路径安全/实现/路径安全.py:80   绝对路径 = os.path.normpath(os.path.join(根, 归一))
     （归一 = 入参 `相对路径`；根 = 入参 `根目录`，:78 用 os.path.abspath 绝对化）
开发工具/HTML验证/路径安全.py:29                          路径 = (根 / 相对).resolve()
     （相对 = 页面/场景请求里的路径文本，:27 先拒绝对路径）
模块库/自修复工具/实现/自修复工具.py:51-52                 根 = Path(工作区路径).resolve()；目标 = (根 / 相对路径).resolve()
技能库/后端/技能库/实现/技能库.py:701 / :711               根 = Path(技能根目录 or "").resolve()；候选 = (根 / 标识).resolve()
     （标识 = 入参 `技能标识或路径`；相对根按 cwd 解析，未走唯一腿）
模块库/项目文档/实现/项目边界.py:62 / :68                  字符串形态拼根；组合路径(档案.项目根目录, 文档根) 用作入参路径基准
支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py:256  遍历基 = os.path.abspath(源路径)（隐式按 cwd 绝对化，同族）
运行核心/加载器/包安装/入口路径.py:44                      入口路径 = (包根 / 相对入口).resolve()
     （边界：`相对入口` 来自 包声明.入口，属声明驱动，非能力入参）
模块库/开工编排/实现/开工编排.py:273                       目标 = (项目根解析 / 归一).resolve()
     （边界：基准根经 `_解析基准根` → `解析路径`（唯一腿），见 :236；归一 由本模块自算）
```

---

## 五、类 4 · 第二份「路径族参数名名单」

**源码面零命中。** 子代理用的检索式：

```
rg -n '(路径名|路径字段|路径参数|路径键|路径族)[A-Za-z_\u4e00-\u9fa5]*\s*(:.*)?=\s*[\(\{\[]' --glob '*.py' -g '!工程缓存/**' …
rg -n "路径族" --glob '*.py' … -l
```

命中只有**唯一腿本体**与**测试侧**（测试侧名单用于断言「名单里必须有这些名字」，不参与运行时判定）：

```
公共契约/基础类型/字段名册.py:223                  路径族字段名 = frozenset({     ← 唯一腿本体（排除项）
测试中心/公共契约/测试_路径解析.py:52 / :58 / :75   F3路径族名 / 补全路径族名 / 故意不入册名
测试中心/运行核心/测试_网关路径解析.py:57 / :60      补全路径族名 / 故意不入册名
```

⇒ **「哪些名字算路径族」这条腿已经是单一节点**（本批新加的 `基准路径字段名` 是它的子集，不是第二份名单）。

---

## 六、子代理自报的口径边界（主会话复核时优先看这几条）

- **生效根取值自建**（不走 `解析路径` 三级兜底）：`技能库/技能/验证编排/scripts/验证编排.py:219`、`技能库/技能/交付收尾/scripts/交付收尾.py:373`、`模块库/开工编排/实现/开工编排.py:383`。
- **自制「绝对性」判据**（判据不是拼根，但是第二份口径）：`平台控制面/能力目录/租约路径.py:54-56::_是绝对路径`、`模块库/项目文档/实现/项目边界.py:117`、`平台控制面/包仓库/路径安全.py:67`、`支持库/后端/组件规范支持库/实现/核心快照模板.py:51`。
- **推根但本模块未自行拼路径**（根来源同属 `parents[N]`，拼路径在别处）：`平台控制面/提供者/实现/能力入口.py:31`、`平台控制面/快照校验/实现/提供者.py:26-27`、`平台控制面/统一入口/实现/能力入口.py:29`、`平台控制面/平台状态/实现/能力入口.py:30`、`平台控制面/能力反馈/实现/能力入口.py:29`、`平台控制面/提供者/注册表/实现/能力入口.py:24`、`平台控制面/策略闸门/实现/提供者.py:28`、`平台控制面/能力目录/实现/租约边界.py:26/30`。
- **非入参驱动的 `根 / 相对`**（相对值来自声明/档案/git 输出）：`运行核心/加载器/包安装/入口路径.py:44`、`支持库/后端/大语言模型支持库/模型连接器/实现/模型供应链校验.py:193`、`开发工具/项目编译/工作区指纹.py:120`、`技能库/后端/技能库/实现/技能库.py:544/852`。

---

## 七、本件的边界

- 全部条目由**只读子代理**给出，主会话**未逐条复跑**；派活前按 `未完成事项.md` §一.1 现场复核（行号会漂）。
- 本件**不新增**结论，只留档取证；收口后的销账以 `审计落点_单一节点收口_20260924.md` §三 S2 为准。
