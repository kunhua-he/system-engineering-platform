<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-12 单据：账本可观测性 + 返回体积收口

> 取数时点：2026-09-24 11:20–11:40（Asia/Shanghai）｜开工ID：`开工-20260924-112025-6e75`
> 纪律：一切经 MCP；**未跑编译、未 git 提交**；只改本单据「变更清单」列出的文件。

## 一、读数复核（A 返回过大 / B 耗时没落库）

### 复现命令

回执库是「美化 JSON 多行」，**不是 JSONL 逐行** ⇒ 必须流式 `raw_decode`：

```python
import json, io, collections
s = io.open('工程缓存/运行数据/MCP全量回执/20260924_全量回执.jsonl', encoding='utf-8').read()
d = json.JSONDecoder(); i = n = 0; 全 = collections.Counter(); 字典 = collections.Counter()
while i < len(s):
    while i < len(s) and s[i] in ' \n\r\t': i += 1
    if i >= len(s): break
    o, end = d.raw_decode(s, i); i = end; n += 1
    v = o.get('值'); cid = v.get('能力id') if isinstance(v, dict) else None
    if isinstance(cid, str) and cid.startswith('自修复工具.'):
        全[cid] += 1
        if isinstance(v.get('结果'), dict): 字典[cid] += 1
print('总回执', n)
for k, c in 全.most_common(6): print(k, '全部', c, '/ 结果字典', 字典[k])
```

### A 返回过大 —— 复核：**成立**

| 项 | 本单据实测（口径：`值` 的 JSON 字符数） |
|---|---|
| 回执总条数 | 478（取数时点） |
| `记录表` 类 | 36 条 / 合计 13,828,260 / **单次最大 779,707** |
| `自修复工具.静态编译` | 53 次真跑 / 合计 3,548,930 / 单次最大 88,617 / 均值 66,960 |
| `自修复工具.动态编译` | 17 次真跑 / 合计 1,072,073 / 单次最大 75,822 / 均值 63,063 |
| `自修复工具.运行开发编译口` | 3 次 / 合计 206,074 / 单次最大 68,732 |

主会话给的 55 / 19 / 4 次与 3,568,406 / 1,082,998 / 211,319 字符**口径一致**（那三个数按「回执记录整体」计，并把 2/2/1 条「能力详情」回执也算了进去）；本表改按「`值` 部分 + 只算真跑」计，故略小。结论不变：**`标准输出` 一项就占静态编译单次的 87%**（单次 61,412 里 53,540）。

### B 耗时没落库 —— 复核：**三条成立，但结论须更正**

成立的三条：`调用参数账.sqlite3` 表结构无耗时列；`底座运行.db.执行耗时` 当日只有 1 行（来源=自检）；回执库 `grep -c "Wall time"` = 0。

**须更正的一条**：耗时**不是「库答不了」** —— `工程缓存/运行数据/审计/安全审计.jsonl` 逐条已带 `耗时毫秒` 与 `能力id`（实测末条：`{"能力id": "开工编排.收工释放", "耗时毫秒": 256.9, ...}`），且 `运行核心/运行诊断/安全审计/安全审计.py::慢调用报告()` 已能从该账本派生「哪些能力慢」。产点唯一：`运行核心/统一网关/核心处理/网关处理面.py` 的 `finally`（`响应.耗时毫秒 = (time.monotonic() - 开始) * 1000`）。

⇒ 更正后的缺口是：**回执条目自身不带耗时**，跨库/跨进程对照时无法自证「这一条回执花了多久」。件1 按这个口径补，**不新建第二套计时**。

### C 空值键（追加要求）

当日 478 条回执原文合计 21,204,154 字符 → 去空值后 19,949,343（**降 5.92%**）；单例见 §五。（独立复核 11:32 / 487 条：22,611,782 → 21,231,709，**降 6.10%**、省 1,380,073 字符 —— 口径同 §八。）

## 二、件1 回执补耗时

- **落点**：`开发工具/薄壳/薄壳服务.py` —— `_落全量回执(值, 耗时毫秒)`；来源是新增的 `_信封耗时毫秒(结果)` 读 `结果["信封"]["耗时毫秒"]`（网关**只产生一次**的那个值）。
- **改法**：**只增字段** —— 回执记录由 `{"摘要","时刻","值"}` 变 `{"摘要","耗时毫秒","时刻","值"}`；`摘要` 的哈希输入仍是「时刻+值」（摘要是内容去重键，把耗时并进去会让同一内容重复落盘）；耗时取不到时**该键不出现**，记录与改前逐字相同。
- **读数（一条真调用）**：
  - 调用：`平台控制面.能力目录.查询文件租约(修改路径=["开发工具/薄壳/薄壳服务.py"], 返回上限字符=1)`
  - 回执 `工程缓存/运行数据/MCP全量回执/20260924_全量回执.jsonl` 第 574660 行起：`{"摘要": "7d5ed56968…", "耗时毫秒": 14.36, "时刻": "2026-09-24T11:22:41", "值": {…}}`
  - `grep -c '"耗时毫秒"' 工程缓存/运行数据/MCP全量回执/20260924_全量回执.jsonl` → 改前 **0** → 改后 **>0**（独立复核 11:32：**11** 条）
- **口径说明**：落的是**网关侧执行耗时**（能力本身多慢），不是客户端那句 `Wall time`（含传输与客户端开销）—— 两者不是同一读数；本批取前者，因为它与审计账本同源、可对账。

## 三、件2 `记录表` 默认摘要优先

- **落点**：`平台控制面/能力目录/实现/文件租约维护.py::查询文件租约`（`记录表` 的唯一产出点）。
- **改法（只增参数）**：新增 `带明细`（逻辑型，默认 `假`）与 `样例条数`（整数型，默认 `3`）。
  - `带明细=假`（默认）⇒ `记录表` 只回前 `样例条数` 条样例，另带 `记录表条数`（总数）与 `取全量`（取数指引）；`数量`/`活跃数`/`未核验数` 三个计数**一律是全体读数**（`数量` 仍是总数，故可用它判样例有没有被截断）。
  - `带明细=真` ⇒ 回全表，**连新键都不加**，返回体与改前逐字节一致。
  - `样例条数` 非法（非整数 / 负数）⇒ `参数不合法`，不静默回落默认值。
- **内部调用方已同步**：`模块库/开工编排/实现/开工编排.py` 的撞车判定显式传 `带明细=真`（撞车清单少一条就是漏判占用 = fail-open）。
- **前后读数（离线重放当日 36 条真实回执）**：

| | 合计字符 | 单次最大 | 均值 |
|---|---|---|---|
| 改前 | 13,828,260 | 779,707 | 384,118 |
| 默认（`带明细=假`） | 56,017 | 2,451 | 1,556 |
| **降幅** | **99.59%** | 99.69% | — |

判据 ≥90% ⇒ **达成**；`带明细=真` 与改前逐字节等价 ⇒ **达成**。

## 四、件3 两个编译能力加 `明细级别`

- **落点**：`模块库/自修复工具/实现/编译公共.py` 新增 `归一明细级别` + `投影明细级别`（**唯一一条腿**，静态/动态共用）；`静态编译.py` / `动态编译.py` 各只加一处「归一 + 投影」。
- **改法（只增参数）**：`明细级别`（文本型，取值 `计数` / `失败项`（**默认**）/ `全量`）。
  - `计数` = 只有计数与结论状态（成功/失败数量、检查项数、受影响包数、五态结论、退出码、命令、项目根）；
  - `失败项`（默认）= 计数 ＋ 失败明细（逐条明细只留非通过项、失败门禁、未核验项、只报红项、缺失项表、结论原文）；
  - `全量` = 原样返回（实现上直接 `return 归并`，不拷贝）⇒ **逐字节等价是构造性成立的**；
  - 两档都不回 `标准输出` / `标准错误` / `检查项表` / `定向测试点名`；非法值判 `参数不合法`。
- **前后读数（离线重放当日真实编译回执）**：

| 能力 | 次数 | 改前合计 | 默认(`失败项`) | 降幅 | `计数` | 降幅 | `全量` 逐字节等价 |
|---|---|---|---|---|---|---|---|
| `自修复工具.静态编译` | 53 | 3,548,930 | 85,334 | **97.6%** | 17,543 | 99.5% | 是 |
| `自修复工具.动态编译` | 17 | 1,072,073 | 26,919 | **97.5%** | 6,110 | 99.4% | 是 |
| （旁证）`运行开发编译口` | 3 | 206,074 | 2,262 | 98.9% | 520 | 99.7% | 是 |

判据 ≥80% ⇒ **达成**；`全量` 与改前逐字节等价 ⇒ **达成**。

⚠ **默认档变化属口径变更**：不传时由「全量」变「失败项」（返回体小约 97%）⇒ **须华哥确认**；要老行为显式传 `明细级别=全量`。

## 五、空值不显示（动态省略，华哥 2026-09-24 追加）

- **落点**：`开发工具/薄壳/薄壳服务.py` —— 唯一出口 `调用工具()` 的 `json.dumps(数据, ...)`（三个工具都走这一行）；新增 `空值豁免键` / `是空值` / `去空值`。
- **口径**：`null` / `""` / `[]` / `{}` / `假` ⇒ 空，递归省略；**`0` / `0.0` 不算空**（计数 0 是读数：「0 条失败」与「没这个字段」是两件事）；**唯一豁免 `成功`**（`假` 的成功就是失败信号本身，省掉它 agent 就分不清「成功」与「没说」）。`假` 用 `is` 判而非 `==`（Python 里 `0 == False` 为真，用等号会把计数 0 当空值删掉）。
- **不许踩的坑（已按此实现）**：只在**这一层**做一次递归省略，**内部 `值` 契约的键集一个字都不动** —— 回执库里落的仍是网关回包的**原值**（`_落全量回执` 在 `_裁剪值` 内、位于本层之前），平台内下游代码读的是那条腿，不经本层。
- **读数（同一调用）**：

| | 空值键个数 | `记录表[0]` 键数 | 非空叶子 |
|---|---|---|---|
| 内部 `值`（回执原文） | **6**（存储标签/已声明指纹/留言/释放原因/释放时间/指纹一致） | 19 | 16 |
| agent 面向（去空值后） | **0** | 13 | 16 |

- **断言（下游 `值` 契约键集不变）**：同一次调用的回执原文（= 网关回包原值）**仍含**那 6 个空值键与全部 19 个键，只有 agent 面向的载荷被省略 ⇒ 省略只发生在序列化出口，内部契约键集不变。
- **非空字段逐字节等价**：`非空叶子 原=16 / 代理=16 / 逐字节等价=True`。
- **live 读数（真调用，2026-09-24 11:32）**：薄壳跑的是**源码树**（件1 的 `耗时毫秒` 已在同一进程落盘 ⇒ 本件同样已生效于 MCP 链路）。调用 `平台控制面.能力目录.查询文件租约(修改路径=["开发工具/薄壳/薄壳服务.py"], 含现场指纹=真)`：agent 收到的 `记录表[0]` 是 **18 个键**，比回执原文的 19 键少的 4 个正是空值键（`存储标签` / `已声明指纹` / `留言` / `指纹一致`）；`未核验数: 0` **保留**（0 是读数，不是空值）。同一次调用的回执原文（`20260924_全量回执.jsonl` 第 627844 行起）里那 4 个键**仍在** ⇒ 下游 `值` 契约键集不变，省略只发生在 agent 面向那一层。
- **全量重放复核（487 条回执）**：空值键 **108,741 → 1**；删减性（非空字段一个不少、标量逐字节等价）**0 条失败**；唯一残键是 `结果.结果表[0].成功=false` —— 正是 `空值豁免键` 的设计内例外（失败信号本身）。

## 六、变更清单

| 文件 | 改动 |
|---|---|
| `开发工具/薄壳/薄壳服务.py` | `_信封耗时毫秒`；`_落全量回执(值, 耗时毫秒)`；`_裁剪值(…, 耗时毫秒)`；两处调用点传耗时；`空值豁免键`/`是空值`/`去空值` + `调用工具` 出口去空值 |
| `平台控制面/能力目录/实现/文件租约维护.py` | `查询文件租约` 增 `带明细`/`样例条数` + `_解析样例条数` + 默认摘要档（`记录表条数`/`取全量`） |
| `平台控制面/能力目录/__init__.py` | 参数表 + 注册名序各增两个新参数 |
| `平台控制面/能力目录/能力契约/参数契约.json` | 两个新参数 + 值结构文档 + 版本 `1.1.0→1.2.0` |
| `平台控制面/能力目录/能力定义.json` | 同上（可编辑事实源） |
| `模块库/自修复工具/实现/编译公共.py` | 明细级别常量表 + `归一明细级别` + `投影明细级别` |
| `模块库/自修复工具/实现/静态编译.py`、`动态编译.py` | 增 `明细级别` 参数、归一、投影 |
| `模块库/自修复工具/__init__.py` | 参数表注册 `明细级别` |
| `模块库/自修复工具/能力契约/参数契约.json`、`能力定义.json` | 两个能力各增 `明细级别` + 版本 `1.0.0→1.1.0` |
| `模块库/开工编排/实现/开工编排.py` | 撞车判定显式 `带明细=真` |
| 本文件 | 交付单据 |

**契约四腿一致性自检**（契约 / 能力定义 / 入口签名 / 注册名序）：

- `平台控制面.能力目录.查询文件租约`：四处名序均 `[修改路径, 存储目录, 只要活跃, 摘要, 含现场指纹, 带明细, 样例条数, 项目根]`
- `自修复工具.静态编译` / `动态编译`：三处均 `[类型, 模块ID, 超时秒, 源码根, 明细级别]`
- 改动过的 8 个 `.py` 均 `ast.parse` 通过；4 个契约/定义 JSON 均 `json.load` 通过（**这是解析检查，不是测试**；测试按纪律只走 MCP 的静态/动态编译）。

## 七、残余与须确认项

**须华哥确认（2 项）**

1. **件3 默认档变更**：`静态编译`/`动态编译` 不传 `明细级别` 时由「全量」改为「失败项」。
2. **空值省略把 `假` 也省掉**：按口径 `假` 属空值，故 `指纹一致=false` / `能否自动自愈=false` 这类判定位会被省略（读法变成「缺键 = 假」）；`成功` 已豁免。若希望**布尔判定位一律保留**，请裁决 —— 我改成只省 `null`/`""`/`[]`/`{}`。

**残余（未做 / 待收口）**

1. **件2/件3 缺 live 读数**：网关跑的是 `工程缓存/制品仓库/平台客户端环境/平台客户端/平台客户端`（2026-09-24 10:24 构建的**激活制品**），源码改动**未生效于网关**（实测 11:32 那次 `查询文件租约` 仍回全表、无 `记录表条数`/`取全量`，即老行为）；按纪律不跑编译 ⇒ 本单据件2/件3 读数是**离线重放当日真实回执**（可复现），须**重建制品后复测一次**才算 live 收口。**件1/件4 不在此列**：薄壳跑源码树，两项均已 live 生效（见 §二 / §五）。
2. **生成物未重跑**：`能力数据/能力搜索数据.json`、`能力数据/Agent查询数据.json`、`完整性摘要.json`、`包声明.json`、`说明/使用说明.md` 仍是旧版（按纪律不跑编译），下一轮编译由生成器收口；`能力id冻结基线` 的内容指纹会变（该门禁按设计只留痕、不拦）。
3. **`自修复工具.运行开发编译口` 未加 `明细级别`**：本批只点了两个公开编译能力；它当日 3 次 / 206,074 字符，同病（按哲学 7.1 它是内部腿，公开面已由静态/动态收口）。是否收口请一并裁决。
4. **`调用参数账.sqlite3` 未加耗时列**：因为耗时已在权威审计账本逐条可查（见 §一 B 更正）；若要「按能力聚合的耗时账」，那是另一件事（口径需先定）。
5. **未知消费者**：仓库外/其它会话若直读 `值["记录表"]` 全表，或依赖 `值` 里的空值键存在，需按新口径调整（`记录表` 加 `带明细=真`；空值键改用 `.get`）。
6. **件2 契约 `数量` 的说明文案**已澄清为「总条数」（纯文档性变化；`对外契约变更判定` 明确「只改说明文案不要求递增」）。参数新增已按规则递增能力版本。

## 八、独立复核（本单据落盘后重放，2026-09-24 11:32）

取数：`工程缓存/运行数据/MCP全量回执/20260924_全量回执.jsonl`（**487 条**，比 §一 取数时点多 9 条）＋ 直接 import 改动后的 `模块库/自修复工具/实现/编译公共.py`、`开发工具/薄壳/薄壳服务.py` 重放（不跑编译、不起服务）。

| 项 | 判据 | 复核读数 | 结论 |
|---|---|---|---|
| 件1 耗时落盘 | 回执里能查到该字段 | `grep -c '"耗时毫秒"'` = **11**（改前 0）；如 `开工编排.开工即占` 11:32:30 那条回执带 `"耗时毫秒"` | 达成 |
| 件2 `记录表` 摘要优先 | 默认降 ≥90% ＋ `带明细=真` 逐字节等价 | 39 条全匹配：15,254,279 → 61,195（**99.60%**）；35 条含 `数量`：15,251,523 → 59,851（**99.61%**）；单次最大 779,707 → 2,451；`带明细=真` 不投影 ⇒ 逐字节等价 | 达成 |
| 件3 编译 `明细级别` | 默认降 ≥80% ＋ `全量` 逐字节等价 | 静态编译 53 条：3,548,930 → 85,334（**97.6%**）/ `计数` 17,543（99.5%）；动态编译 17 条：1,072,073 → 26,919（**97.5%**）；`运行开发编译口` 3 条：206,074 → 2,262（98.9%）；`投影明细级别(…, "全量") is 原` 全 True | 达成 |
| 件4 空值省略 | 空值键 N→0；非空字段逐字节等价；契约键集不变 | 487 条：空值键 **108,741 → 1**（唯一残键 = 豁免键 `成功=false`）；删减性 0 条失败；live 真调用见 §五 | 达成 |

**口径对齐（重要）**：件3 的 `值` 是**任务腿记录**，编译能力真正的返回值在 `值.结果` 里 —— §一 与 §四 的字符数**都按 `值.结果` 计**，故两处数字可逐位对齐（`值` 整条口径会多出任务腿固定开销，静态编译为 3,561,450）。

复现脚本（关键片段；`sys.path` 指向仓库根）：

```python
import json, io, sys, importlib.util
sys.path.insert(0, '.')
from 模块库.自修复工具.实现.编译公共 import 投影明细级别
spec = importlib.util.spec_from_file_location('薄壳服务', '开发工具/薄壳/薄壳服务.py')
薄壳 = importlib.util.module_from_spec(spec); spec.loader.exec_module(薄壳)  # 只取 去空值/是空值，不起服务
s = io.open('工程缓存/运行数据/MCP全量回执/20260924_全量回执.jsonl', encoding='utf-8').read()
d = json.JSONDecoder(); i = 0; recs = []
while i < len(s):                                   # 美化 JSON 多行 ⇒ 流式 raw_decode
    while i < len(s) and s[i] in ' \n\r\t': i += 1
    if i >= len(s): break
    o, end = d.raw_decode(s, i); i = end; recs.append(o)
C = lambda v: len(json.dumps(v, ensure_ascii=False))
组 = [r['值'] for r in recs if isinstance(r.get('值'), dict)
      and r['值'].get('能力id') == '自修复工具.静态编译'
      and isinstance(r['值'].get('结果'), dict)]
print(len(组), sum(C(v['结果']) for v in 组),
      sum(C(投影明细级别(v['结果'], '失败项')) for v in 组))   # → 53 3548930 85334
print(sum(C(薄壳.去空值(r['值'])) for r in recs),
      sum(1 for r in recs for _ in [1] if 薄壳.是空值(r['值'])))  # 空值键口径见 §八 表
```

**本次复核未覆盖**：live 端的件2/件3（须重建制品）、生成物重跑、`运行开发编译口` 是否加 `明细级别` —— 见 §七。
