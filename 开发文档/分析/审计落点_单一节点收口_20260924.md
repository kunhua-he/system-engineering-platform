# 审计落点 · 单一节点收口（2026-09-24 批）

> 开工ID：`开工-20260924-001244-7265`（批A）／`开工-20260924-001444-7424`（批A续）／批B 开工ID 见同批 `未完成事项.md` 登记
> 口径：**本件是落点对照**（发现 → 落点 → 登记 → 状态），不新增结论；结论以现场实测与权威文档为准
> 上游：`开发文档/分析/审计落点_两条腿与冗余_20260923.md`（两条腿 · 冗余 · 写入越界总账）
> 触发：华哥「你统一一下，到底是相对的，还是绝对的。**全部传参都固定**」＋「只要有报错……看看有没有权威节点，保持统一的。有统一节点的，全部切成统一节点」
<!-- 机器管理｜类型：分析｜生成器：人工 -->

---

## 一、本批做了什么（4 修 + 1 机制，全部带实测判据）

| # | 事项 | 权威节点（唯一腿） | 实测结果 |
|---|---|---|---|
| A1 | **`公开调用完整性门禁` 4 项新增违规清零**：`说明书-缺参数`（能力目录/回收过期文件租约 缺 `项目根`）、`说明书-未含能力名`＋`说明书-缺能力段`（自修复工具/仓库锁）、`验证场景-未覆盖能力`（同上） | 说明书唯一腿 = `项目文档.人工改文档`（`人工类` 说明书不得由生成器覆盖）；场景唯一腿 = `模块库/自修复工具/验证场景/仓库锁.json` | 门禁 `通过`（退出码 0）；新增 4 → **0** |
| A2 | **存量 17 条 → 0**：`平台控制面/授权` 9 条 + `平台控制面/统一入口` 2 条 `说明书-缺返回载位`（清单表返回列缺信封类型名，改法 `{…}` → `结果型（值结构：{…}）`）；`支持库/前端/前端契约校验` 包级版本载位 1 + 缺能力段 4（清单表换门禁体例）；`支持库/适配层/浏览器自动化提供者` `公开调用-声明未导出` 1 | 同上 + 包入口导出面（`__all__` 必须列真实模块级函数，与 `textutil提供者` 同形） | 存量 17 → 8 → **0**，门禁 `通过` |
| A3 | **`开发工具/验证门禁/存量基线.json` 收敛**（4 条已修 + `平台判断越界检测` 段清空） | 门禁自己的 `--写基线`（不手改基线文件） | `运行验证门禁` 结论 `绿`；`同动作两条路` 存量 20 → 16 |
| B1 | **路径传参口径收成唯一腿**：`公共契约/基础类型/字段名册.py` 新增 `基准路径字段名`（21 名）+ `路径写法口径_普通` / `路径写法口径_基准` + `取路径写法口径(名称)` | 本文件即唯一腿（与 `是路径族字段` / `路径族字段名` 同形制） | 三个消费点全部只读它，**本批没有第二份名单** |
| B2 | **机器判据接进唯一体检节点**：`开发工具/契约编译/参数可用性体检.py` 判据 6「普通路径族参数的 `说明` 只提绝对不提相对即判红」（进 `静态编译` 的「参数可用性」门禁） | 同 B1（判据里**零路径参数名字面量**） | 实测命中 **103 条 / 40 包**，基线冻结；门禁 `三类均未超存量基线`、退出码 0 |
| B3 | **agent 面向口径一处写死**：`开发工具/薄壳/工具清单.py` 的 `capability_call` 描述 + `开发文档/规范/项目细则.md` 新增「路径传参口径（唯一）」一节 | 描述与细则都**引用** B1 的口径，不另写一套 | 见 §二 口径正文 |

---

## 二、路径传参口径（唯一，全文一句）

> **普通路径参数一律写「项目根相对路径」（posix、不带 `..`）；只有 `项目根` 这类基准参数写绝对路径。绝对路径在普通路径参数上永久兼容。**

- **为什么是「相对优先」而不是「绝对优先」**：`修改路径`（列表型）与全部租约/登记/证据面**本来就是项目根相对路径**；相对路径可移植（换机器/换用户不失效）、可对账、不把本机路径写进证据。选绝对优先会同时改掉这四处既有口径，且与 `开工编排.开工即占`「显式拒绝绝对路径」正面冲突。
- **为什么基准类必须绝对**：`项目根`/`仓库根`/`源码根`/`受控根目录`… **本身就是解析基准**，写相对会落到环境变量 `系统平台_项目根` 或 cwd —— 那是「基准漂移」，不是「路径写法」。
- **行为层不改**：华哥 2026-09-22 裁决「绝对/相对都要能用」仍然成立（`_归一化路径族参数` 只做「相对→按生效根拼绝对」，绝对原样）。本批统一的是**规范写法**（agent 只记一句），不是收紧兼容性。

---

## 三、新发现（已登记 `开发文档/未完成事项.md` §四）

| 编号 | 发现 | 落点 | 状态 |
|---|---|---|---|
| S1 | **路径族参数 `说明` 与平台口径冲突 103 条 / 40 包**（说明写「绝对路径」、平台两可） | 源 = 各包 `能力定义.json` 的参数 `说明`；派生件 = `能力契约/参数契约.json` | 判据 6 已接线（基线 103 冻结）；收敛批次待派活 |
| S2 | **「第二套路径解析」38 处 / 30 文件**（自带 `is_absolute()` 分支拼根 21 处；`parents[N]` 推根后做基准 9 处；自己 `os.path.join(根, 入参)` 8 处） | 逐条清单见 `开发文档/分析/子代理取证_第二套路径解析_20260924.md`（待落盘；本批由只读子代理 Gauss 实测给出） | 待收口到 `公共契约/运行时/平台适配/路径解析.py::解析路径` |
| S3 | **`能力详情` 输出超阈值即被卸载成回执指针**，调用方必须二次 `读取文件` 才能拿到参数名/类型 —— 是「子代理搜一次拿不到参数、于是二跳」的隐形放大器（与 §一.3 `缺少必填参数` TOP 同源） | 薄壳 `capability_call` 的 `操作=能力详情` 分支 + 回执卸载阈值 | 待定性（是阈值问题还是该默认分档） |
| S4 | **`--写基线` 的收敛没有「扫描面不得缩小」护栏**：基线条目「基线有、本次未命中」即被删除；若某检查器的扫描面因改动缩小，会**静默放开**（读数表现为「绿」） | `开发工具/验证门禁/门禁公共.py::判定`（`可收敛`）+ `--写基线` | 本批实测：`平台判断越界检测` 段被清空 —— 逐条核为**合法收敛**（该 5 条已在 `346d777e` 修掉），但缺护栏本身要登记 |

---

## 四、本件的边界（如实留档）

- **未跑**发布门禁；本批只跑了 `公开调用完整性门禁`、`运行验证门禁`、`参数可用性体检` 与 `静态编译`（见提交信息）。
- **未做** S1/S2/S3/S4 的修复本身 —— 只做了「判据接线 + 口径收成唯一腿 + 登记」。
- S2 的 38 处清单由**只读子代理**给出（Gauss，`01a0cf0b-4d21-7861-88aa-aaa8fdcfedd9`），主会话**未逐条复跑**；派活前须按 `未完成事项.md` §一.1 现场复核。
- 本件是**分析类**件，条目销账后按分析类口径删除。
