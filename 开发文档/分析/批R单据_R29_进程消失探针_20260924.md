<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# R-29 判据1 探针：进程消失（写窗口无主）

本文件由**另一个进程**（pid 6937，`R-29 活体探针` 租约的所有者）持租约，
用于复现「会话异常结束 ⇒ 写窗口还开着」的真场景：

- 该进程活着时：回收腿 **不许** 动这条窗口（回收数 0）—— 判据 2；
- 该进程被杀之后：回收腿凭「所有者进程已消失」把它收回并回锁 —— 判据 1。

★ 本行由 MCP 写入腿在**租约活跃期**写入：证明该路径此刻真的可写（判据 2 实测）。
