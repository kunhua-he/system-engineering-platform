<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-17 单据：注入面默认档送达 ＋ 存量漂移收口

> 取数时点：2026-09-24 12:06–12:12（Asia/Shanghai）｜开工ID：`开工-20260924-120658-7321`（写集 8 条）／`开工-20260924-120815-b4cc`（AGENTS.md）
> 纪律：一切经 MCP（`capability_search` → `capability_call`）；**未跑编译、未 git 提交**（留给收尾链）。租约已全部 `收工释放`（复测 活跃数 0）。

## 一、背景（前序实测，本批不重复调查）

- `R-14` 已把「工具姿势」第 31 条硬纪律写进真源（`通用纪律版本` 1.15.0），但**默认档（不传 `细节级别`）不送达该段正文** —— 只回 `{"体积":"31 条","展开":…}`。子代理 kickoff 走的就是默认档 ⇒ 硬纪律实际没进上下文。
- `R-16` 已给 `开工准备` 加 `资料级别`／`资料预算`，`资料级别=指针` 一次回六项；但**只在显式传参时才有 `资料` 段**（为保「不传参逐字节一致」判据）⇒ 必须在 0 跳注入面告诉 agent「要传这个参数」。

## 二、裁决：方案 B 否决，只走方案 A

| 方案 | 落点 | 实测读数 | 裁决 |
|---|---|---|---|
| B：把 `工具姿势` 放进 `摘要保留全文段` | `模块库/开工编排/实现/返回体量分档.py:102` | `工具姿势` 序列化 **7,706 字符**（`开发文档/规范/任务类型规范.json` 现场量），远超 `块上限字符` 闸 **2,000**；摘要档 `通用纪律` 现值约 1,614 字符 | **否决**（必破闸，且破的是「所有注入提示词最高 2000 字符」既有裁决） |
| A：`capability_call` 工具描述加 0 跳指针 | `开发工具/薄壳/工具清单.py` | 描述文本**每轮都在上下文**（注入面 C），不占返回预算 | **采纳** |

「默认档送达」的解法因此不是把正文塞进返回，而是**把「去哪儿拿」写进 0 跳面**。

## 三、改了哪些文件（7 个文件 / 10 条编辑，`批量应用精确替换` 结果：总数 10、成功数 10、失败数 0、原子=真、已写入文件数 7）

| # | 文件 | 改动 | 新文件 sha256 前 12 |
|---|---|---|---|
| 1 | `开发工具/薄壳/工具清单.py:94-100` | `capability_call` 描述新增两段：**硬纪律**（凡可经 MCP 拿到的信息不得用终端取；拿不到就如实回报缺口）＋ **开工一次取全**（`开工准备` 传 `资料级别='指针'`，超预算降级成「文件:行区间」指针） | `61d696c9d341` |
| 2 | `开发工具/任务记忆/派活任务信.py:62-68` | `条款_工具姿势` 指针刷新：点名「第 31 条是硬纪律」＋「一次取全开工资料（`资料级别='指针'`）」 | `c1aeb19369fd` |
| 3-4 | `AGENTS.md:3`、`:17-18` | 更新日期 2026-09-22→09-24；`## 开工` 新增第 6／7 条（一次取全／硬纪律） | `b7ae95f2c849` |
| 5 | `支持库/适配层/textutil提供者/实现/文本转换.py:95-96` | 终止回调由直调 `进程终止.强制结束子进程` 改为走**唯一入口** `进程终止.结束并留痕(进程, 位置="textutil提供者.转换文本文件", …)` | 见 `结果表` |
| 6-7 | `支持库/适配层/textutil提供者/生命周期契约.json:17-18` | 释放策略／停止入口去掉已删的 `_终止进程组`，改述为 `进程终止.结束并留痕`（进程组终止→宽限→强杀→整组复查） | — |
| 8-9 | `支持库/后端/文档转换支持库/textutil转换/生命周期契约.json:17-18` | 同上（去掉「薄委托 强制结束子进程」表述） | — |
| 10 | `支持库/后端/文档转换支持库/textutil转换/实现/文本转换.py:4` | docstring 删「与一处内部薄委托（`_终止进程组`）」 | `0d2b2cbdca70` |

**写集内认领但未改**：`模块库/开工编排/说明/使用说明.md` —— 文件头是 `<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->`，属**生成物**，手改必被编译口覆盖。其「五段／版本 1.4.0」是 R-16 改了 `能力定义.json` 后**未重跑编译**的陈旧读数，收尾链跑 `静态编译` 即自愈。**不手改生成物**（哲学第 1 条）。

## 四、硬判据读数

1. **默认档送达**：`capability_call` 描述里现在含 `★【硬纪律】凡可经 MCP 拿到的信息不得用终端取…` 与 `★ 开工一次取全：…'资料级别':'指针'…`（差异见 `批量应用精确替换` 结果表 序号 1）。
2. **不破 2000 闸**：本批**不触碰任何返回体内容**（只改工具描述文本与契约散文）⇒ 摘要档各块字符数与改前逐字节一致；`工具姿势` 仍以 `{"体积":"31 条","展开":…}` 形态出现，正文改从 0 跳指针取。
3. **零信息损失**：新增指针全部带「能力 id + 参数」或「文件:行区间」，照它可取回原文。

## 五、未做项与须确认项

- **未跑编译**：`AGENTS.md`／`使用说明.md` 两份生成物要等收尾链 `自修复工具.静态编译(类型=0)` 重算；`文本转换.py` 的改动在编译口之前对 MCP HTTP 链路**不生效**（网关跑激活制品）。
- **未 git 提交**（纪律）：收尾链统一提交。
- **须确认 1**：`位置` 串取短名 `textutil提供者.转换文本文件`（对齐 LibreOffice 腿既有写法 `LibreOffice转换.转换办公文件`），是否要改全包 id 待裁。
- **须确认 2**：`AGENTS.md` 全仓搜「写 `AGENTS.md` 的腿」**0 命中**（无生成器），但文件头带「生成器：开发工具.MD文档生成」⇒ 两处口径不一致；本批按**手改**处置并登记。
- **存量残余（写集外，未处置）**：其它腿是否仍有直调 `强制结束子进程` 的 `终止回调` —— 本批只收 textutil（R-11 登记范围）。

## 六、收尾补记（2026-09-24 12:26–12:36，开工ID `开工-20260924-122609-f69a`／`开工-20260924-122722-8279`）

### 6.1 自修复工具注册镜像漂移（阻断红，本批新收）

收尾链跑 `自修复工具.静态编译(类型=1, 模块ID=模块库/自修复工具)`，首读 **契约漂移 阻断**：
`注册参数名序 ['变更文件','库列表','全仓','自证','只读','上线','超时秒','源码根','异步'] ≠ 契约参数名序 [...,'源码根']`。

- **根因**：`决策记录 0061` 删 `运行开发编译口.异步` 时，`能力定义.json`（真源）与实现都改了，**入口注册镜像 `模块库/自修复工具/__init__.py:96` 漏改**。该镜像**无生成器**（手抄件）—— 依据 `开发文档/分析/批P-P3_注册默认值_20260924.md`「权威节点 A：ln（45 包已接入；**无生成器**）」；R-15 加 `路径清单` 时也是手改同一形状（`文件操作/__init__.py:85-87` 现场可证）。
- **收口**：`批量应用精确替换` 删该行（−1 行 / 文件 sha256 `dc6eebb7ef15…` → `8a11fdf0e7a9…`）；复跑 `静态编译` ⇒ **契约漂移 通过**，成功 18 / 失败 2（两只报告项：md查重、制品新鲜度）。
- **同类复核**：`测试中心/模块库/测试_自修复工具.py` 搜 `异步` **0 命中**；`能力定义.json` 仅 1 处、`参数契约.json` 仅 2 处，全是说明散文里的「原 `异步` 参数已删」，非参数声明。

### 6.2 「未知字段一律忽略」措辞按裁决收口（华哥 2026-09-24 裁决：参数名必须对齐，未对齐即剔除）

| 文件:行 | 改后（行为逐字不变，只改口径措辞） |
|---|---|
| `运行核心/统一网关/核心处理/网关参数校验面.py:260` | `按能力契约剔除未对齐参数名（口径：参数名必须与契约逐字对齐，未对齐即剔除、不转给实现；回带 被忽略参数 ＋ 最接近的契约参数）` |
| `运行核心/统一网关/本地网关.py:395` | `兼容口径：未对齐的顶层字段**直接剔除**，不再 400` |

复跑 `静态编译(类型=1, 模块ID=运行核心/统一网关)` ⇒ **阻断 0**，成功 18 / 失败 2（同两只报告项）。

### 6.3 默认档送达实测读数（R-17 自检）

- **落盘面**：`开发工具/薄壳/工具清单.py:94-100` 现场读到两段指针 ——
  `★【硬纪律】凡可经 MCP 拿到的信息不得用终端取（终端只留三类：明令只读取证脚本 / git 只读状态 / MCP 确实不覆盖的检查）；拿不到就如实回报缺口…（全文＝开工编排.开工准备 的 通用纪律.工具姿势 第 31 条）`
  ＋ `★ 开工一次取全：给 开工编排.开工准备 传 参数={'项目根':…,'任务':…,'资料级别':'指针'}，一次回六项开工资料…`。
- **默认档实测**（本会话现场调 `开工编排.开工准备`，**不传** `细节级别`／`资料级别`）：返回键 `通用纪律` → `工具姿势` = `{"体积":"31 条","展开":"传 细节级别=完整 取原文"}` —— **正文不进返回**（不破 2000 闸，与 6.4 裁决一致）；同时 `资料` 键**不存在**（只在显式传 `资料级别` 时才有），`通用纪律提示` 仍在。⇒ 「送达」由 0 跳工具描述承担，描述每轮都在 agent 上下文。
- **生效边界（如实登记）**：工具描述在 MCP `initialize` 时下发 ⇒ 长连接会话看到的仍是改前描述，**下一次薄壳重连后**才生效。

### 6.4 方案 B 复核（为什么默认档不能塞正文）

`工具姿势` 序列化 **7,706 字符** ≫ `块上限字符` 闸 **2,000**（摘要档 `通用纪律` 现值约 1,614）⇒ 塞进返回必破闸，维持 6.1 之前的裁决：**只走 0 跳指针**。

## 七、独立复核读数（2026-09-24 12:40–12:45，开工ID `开工-20260924-124343-1434`）

### 7.1 四项逐条核验（判据 → 实测）

| 项 | 判据 | 实测读数 | 结论 |
| --- | --- | --- | --- |
| 1 默认档送达 | A：0 跳描述指针 | live 薄壳 `capability_call` 描述含 `★【硬纪律】凡可经 MCP 拿到的信息不得用终端取…` 与 `★ 开工一次取全…` | 已达（每轮都在 agent 上下文） |
| 1 默认档送达 | B：`摘要保留全文段` 保留 `【硬纪律】` 条目 | `工具姿势` 序列化 7,706 字符 ≫ 闸 2,000（见 §6.4） | 否决，只走 A |
| 2 指针文案刷新 | 指向新增的第 31 条 | `开发工具/任务记忆/派活任务信.py:63-69` 现写「★ **第 31 条是硬纪律**…」 | 已达 |
| 2 指针文案刷新 | `使用说明.md`「五段」对齐 | `模块库/开工编排/说明/使用说明.md:12,35` 仍写「命令环境 / 工具姿势 / 提交纪律 / 改完必做 / 判据 **五段**」 | **未达**，见 §7.2 |
| 3 textutil 存量漂移 | 不留已删旧名 | `模块库/**/*.py` 与 `**/*.json` 搜 `_终止进程组` = **0 命中**；`模块库/**/*.py` 搜 `强制结束子进程` = **0 命中** | 已达 |
| 3 textutil 存量漂移 | 适配层走唯一节点 `结束并留痕` | `支持库/适配层/PyMuPDF提供者/实现/提供者.py:54` 的 `终止回调` 已调 `进程终止.结束并留痕` | 已达 |
| 4 自检 | 不传 `细节级别`／`资料级别` 实调 | 见 §7.1.1 | 已取读数 |

#### 7.1.1 默认档实调读数（`开工编排.开工准备`，不传 `细节级别`／`资料级别`）

- 返回体：**8,004 字符**。
- `通用纪律.工具姿势` = `{"体积":"31 条","展开":"传 细节级别=完整 取原文"}` ⇒ **正文不进返回**（与 §6.4 裁决一致）。
- `资料` / `资料用量` / `细节级别` 三键**都不存在**：`值字段` 实探回 `值裁剪.字段不存在 = ["资料","资料用量","细节级别"]`。
- 显式传 `资料级别='指针'` 时回 `被忽略参数[0] = {参数名: 资料级别, 原因: 该能力契约里没有这个参数，已被网关剔除, 最接近的契约参数: [验证级别, 细节级别]}` —— 见 §7.3。

### 7.2 残留漂移一：`通用纪律` 段名文案写少了 3 段（真源在 `能力定义.json`）

- 现值（真源 `模块库/开工编排/能力定义.json:10,96`）：「命令环境 / 工具姿势 / 提交纪律 / 改完必做 / 判据 **五段**」；同一句的说明段另写「…**四类** 与任务类型无关的固定纪律」。
- 实际段名（`模块库/开工编排/实现/返回体量分档.py::纪律段落名表` ＋ `判据`）= **哲学纪律 / 命令环境 / 工具姿势 / 提交纪律 / 改完必做 / 效率达标线 / 派活纪律 / 判据 = 8 段**（`开工准备` 的 `通用纪律提示` 也按这 7＋1 列）。
- 少列的 3 段含 **`哲学纪律`** —— 即「干净底座优先 / 不保留旧腿 / 一次性盘清、一次性全派…」那 12 条，是华哥反复引用的那一组。文案写「四类」会让读者以为通用纪律里没有它。
- 9 处落点（同一句话）：真源 `能力定义.json:10,96`；派生件 `能力契约/参数契约.json:7,93`、`能力数据/能力搜索数据.json:5,38`、`能力数据/Agent查询数据.json:36`、`包声明.json:71,78`。
- `模块库/开工编排/说明/使用说明.md:12,35` 是**它的生成物**（文件头 `生成器：开发工具.MD文档生成`）⇒ **手改使用说明.md 无效**（下次编译被覆盖），须改真源后由编译口重算。

### 7.3 残留漂移二：0 跳注入面已承诺「资料段默认就带」，live 网关却给不出

- 0 跳注入面（live 薄壳 `capability_call` 描述）已写：「**资料段默认就带**，不必再传 `资料级别`」。
- live 网关实测两拍：① 默认档**无** `资料` 键；② 显式传 `资料级别='指针'` 被当**未知参数剔除**（`该能力契约里没有这个参数`）。
- 而源码树 `模块库/开工编排/能力契约/参数契约.json:59,66` 已有 `资料级别`／`资料预算`，`能力数据/能力搜索数据.json:5` 已写明「R-21 改为默认送达」。
- ⇒ 判据：**网关跑的是早于 R-16 的那一版契约**，源码树改动未部署。今天照 0 跳指针做的 agent **拿不到**六项开工资料。收口动作（重建制品 ＋ 重启网关）属 R-21 收尾链，本会话不代做。

### 7.4 撞车实况（为什么这两条只登记、不动手）

`开工编排.开工即占` 实测**拒绝**认领：`开发文档/未完成事项.md` 有活跃租约，属 `开工-20260924-123755-e994`（租约id `f46a8b57c431443a`，任务「**R-21 开工资料默认送达与返回体量噪声收敛**」，占用 12:37:56 / 过期 12:52:56）。同一会话的未提交写集（15 个文件）含 `AGENTS.md`、`开发工具/薄壳/工具清单.py`、`开发工具/任务记忆/派活任务信.py`、`模块库/开工编排/{能力定义.json, 能力契约/参数契约.json, 实现/开工编排.py, 实现/开工资料.py, …}`。

⇒ §7.2 的真源（`能力定义.json`）与 §7.3 的部署**都落在 R-21 写集内**，按「批量认领必须整体成功、不许静默跳过」的纪律**不代做**，只登记读数与落点；R-17 本批自身写集（`开发文档/分析/批R单据_R17_默认档送达_20260924.md`，租约id `cfd43ca0f44f4868`）与它**零重叠**。

### 7.5 「全量回执」成本读数（供 R-21「返回体量噪声收敛」参考）

本轮复核里 **6 次**因返回值超阈值被换成「全量回执」指针，各多付一轮 `读取文件(回执 jsonl 行区间)`：

| 调用 | 原字符数 |
| --- | --- |
| `读取文件 模块库/开工编排/说明/使用说明.md:1-45` | 4,034 |
| `正则搜索 五段\|四类 @模块库/开工编排` | 2,596 |
| `正则搜索 未完成事项.md 标题` | 6,846 |
| `正则搜索 _终止进程组\|结束并留痕 @全仓 *.py` | 5,107 |
| `开工准备` 索引档 | 2,091 |
| `能力详情 批量应用精确替换` | 2,300 |

阈值 ≈ 薄壳默认 `返回上限字符` 2,000：≤2,000 当场内联（对照：`正则搜索 _终止进程组 @全仓 *.json` 0 命中 → 内联）、>2,000 走回执。⇒ 读数建议：把「回执阈值」与「默认上限」解耦（或让 ≤8,000 内联），否则每次稍大的读取/检索都变成 2 次往返。

### 7.6 本批写集

| 文件 | 动作 |
| --- | --- |
| `开发文档/分析/批R单据_R17_默认档送达_20260924.md` | 追加本节（§七） |

本节只增不改：§一–§六 原文一字未动。未跑编译、未 git 提交（留给收尾链）。

