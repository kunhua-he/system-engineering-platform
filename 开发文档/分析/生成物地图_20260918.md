# 生成物地图（逐包取证）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 取证时点：2026-09-18 04:20 CST｜主干分支｜取证时 HEAD = `6b70e0b1`
  （并行会话在 04:22:35 又提交了 `1ef8f262`，把本批新增的 `生成物一致性检查.py` 一并带入库；
  该文件仍与工作区**同一 blob** `d04a56ad`，内容未变）
- 取证方式：**逐包现场重算比对**，命令见 §六；不引用任何二手统计
- 对应方案：`开发文档/方案/项目总纲重写方案_20260917.md` §五「生成物自动化」
- 只读校验入口：`开发工具/契约编译/生成物一致性检查.py`（不写盘、不自动修）

> **一句话结论**：改一条源文件后，**最多有 7 份生成物要跟着变**（4 件编译器 JSON + `__init__.py` + `完整性摘要.json` + `说明/使用说明.md`）。
> 全仓 141 包共 **1741 条生成物条目**：**822 条可机器重算且已逐条核验**。
> **稳定存量漂移 = 14 条**（全部是 `说明/使用说明.md` 形态漂移）；另有 1 条 `完整性摘要.json` 写包途中漂移在取证时被当场捕获、随后由写者自己闭合（§五有对账）。
> **漂移没有 834 处**——方案 §5.1 的「6 份 × 139 包」是**同步点**口径，
> 它把 3 份只作**输入**的源文件（参数契约 / 包声明 / 验证场景引用）也算进了「生成物」，见 §四「口径更正」。

---

## 一、生成物地图（每个正式包一套）

| # | 生成物 | 由哪个生成器产出 | 依据哪些源文件 | 谁在校验它 | 能否机器重算 | 现存漂移数 |
|---|---|---|---|---|---|---|
| 1 | `能力契约/参数契约.json` | `开发工具/契约编译/能力定义编译器.py::生成能力契约` | `能力定义.json`（非破坏性：还读既有契约以保留手写 `中文名称`/`名称`/自定义键/`调用示例`） | `测试_漂移门禁`、`运行发布门禁`（注册口径）、门禁「全面漂移」 | **能** | **0 / 135** |
| 2 | `包声明.json` | `能力定义编译器.py::生成包声明` | `能力定义.json`（+ 既有声明的额外顶层字段原样保留，如 `句柄超时秒`） | 同上 + `公开调用完整性门禁`（六环字段级对账） | **能** | **0 / 135** |
| 3 | `能力数据/能力搜索数据.json` | `能力定义编译器.py::生成搜索数据` | `能力定义.json` | 仅 `测试_能力定义编译器` 断言 | **能** | **0 / 135** |
| 4 | `能力数据/Agent查询数据.json` | `能力定义编译器.py::生成Agent数据` | `能力定义.json` + 包id | 仅 `测试_能力定义编译器` 断言 | **能** | **0 / 135** |
| 5 | `__init__.py` | `能力定义编译器.py::生成注册入口` | `能力定义.json` + 包id + 实现模块 | `测试_漂移门禁`（生成标记）、`组件合规` | **能**（仅 3 份带生成标记；132 份为编译器承认的手写入口） | **0 / 135** |
| 6 | `完整性摘要.json` | `支持库/后端/组件规范支持库/实现/完整性摘要.py::生成完整性摘要`（**唯一生成器**，被能力定义编译器委托） | 包内**全部文件**的 sha256 + `能力定义.json`（派生 `能力数`/`能力清单`/`能力定义摘要`） | `组件合规.完整性摘要`、`发布门禁.摘要一致`、`检测完整性摘要格式` | **能** | **0 / 135**（04:20 曾捕获 1 条写包途中漂移） |
| 7 | `说明/使用说明.md` | **无单一权威**：G1=`开发工具/说明书生成/说明书生成器.py`；G2=`组件规范支持库/实现/说明书生成器.py`；人工类见白名单 | G1：`包声明.json`+`参数契约.json`；G2：`能力定义.json`+`参数契约.json`+`能力搜索数据.json`+`验证场景引用.json` | `公开调用完整性门禁`（E-3 字段级对账）、`漂移检测.检测说明书一致` | **部分能**（见 §三） | **14 / 141**（形态漂移） |
| 8 | `依赖锁.json` | `开发工具/重建依赖锁.py`（唯一正式入口；替代此前只能手改的 32 份锁） | 包自身依赖集（原样保留）+ **当前平台指纹**（`运行核心/环境指纹` 真实采样） | `公共契约/包声明/声明.检查依赖锁内容`、装配期 `生命周期管理/管理器` 强制校验 | **能**（只刷 环境/生成时间，幂等） | **0 / 32** |
| 9 | `权限契约/权限契约.json` | **无** | **无**（模板生成器 `支持库/后端/组件规范支持库/实现/{模块,支持库}模板生成器.py` 只产骨架，不随源刷新） | `组件合规._场景权限`、`发布门禁`、`对外契约变更判定`（读 `允许用户`） | **不能** | 135 份人工维护 |
| 10 | `配置契约/配置契约.json` | **无** | **无**（同上，模板产 `{"默认超时秒": 30}` 或 `{"配置需求": []}`） | `组件合规._场景配置` | **不能** | 135 份人工维护 |
| 11 | `依赖契约/依赖契约.json` | **无** | **无** | `组件合规._场景依赖` | **不能** | 21 份人工维护 |
| 12 | `资源预算.json` | **无** | **无**（模板产保守默认值 `默认资源预算()`，一次性的，不是实测） | `组件合规`（设计期产物不是运行时生效值） | **不能** | 133 份人工维护 |
| 13 | `复用决策.json` | **无** | **无**（设计期决策记录） | `组件合规` | **不能** | 134 份人工维护 |
| 14 | `生命周期契约.json` | **无** | **无**（只对第三方提供者要求） | `依赖生命周期审计`（健康探针/释放策略/停止入口）、`发布门禁` | **不能** | 47 份人工维护 |
| 15 | `默认配置/默认配置.json` | **无** | **无** | **全仓无校验者**（`开发入口.py` 只在建目录时列出该名） | **不能** | 12 份人工维护 |
| 16 | `验证场景引用.json` | `能力定义编译器.py::生成验证场景引用` **恒返回 None**（2026-09-18 起显式不生成） | v1 契约由**能力作者手写**（编译器生成会把合法文件覆盖成非法旧格式） | `验证场景体检`、`HTML验证/场景加载`、`公开调用完整性门禁` | **不能**（编译器已明确不产） | 135 份人工维护 |
| 17 | `说明/目录树与命令.md` | **无**（规范 `Markdown 文档体例规范.md` 要求条件必备） | **无** | **全仓无校验者** | **不能** | 66 份人工维护 |
| 18 | `说明/设计说明.md` | **模板生成器产七节骨架**，正文由作者手写（`新建支持库与模块指南.md` 步骤 1.5） | **无** | **无**（指南明写「模板给的是骨架，待填写必须真填」） | **不能** | 141 份人工维护 |

**「改一条源文件 → 要动几份生成物」（现场实测，不是估算）**

| 改的是 | 必须跟着变的生成物 | 份数 |
|---|---|---|
| `能力定义.json` 的一条能力 | 参数契约 + 包声明 + 能力搜索数据 + Agent查询数据 + `__init__.py` + 完整性摘要 | **6** |
| 包内任意 `.py` / 任意文件 | 完整性摘要 | **1**（+ 聚合父包的摘要） |
| `包声明.json` 的说明/版本 | 完整性摘要 +（G1 说明书需重生成）| **2** |
| `能力契约/参数契约.json` / `能力搜索数据.json` / `验证场景引用.json` | G2 说明书需重生成 | **1** |
| 平台换代（换 OS/CPU/Python）| 依赖锁（32 份） | **1 类** |

> 6 份 ≠ 方案 §5.1 的 6 份：那份里的 `注册入口` 与这里的 `__init__.py` 是同一件；
> 但方案把 `参数契约`、`包声明` 与它们的**来源**混为一谈了 —— 见 §四。

---

## 二、逐类现场取证

### 2.1 编译器派生物（第 1-5 项，135 包 × 4 件 JSON）

取证命令（写入 `/tmp`，只报不改，口径与唯一编译器一致）：

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH
python3.14 - <<'PY'
import json, sys; sys.path.insert(0,'.')
from pathlib import Path
from 开发工具.契约编译.能力定义编译器 import 生成能力契约, 生成包声明, 生成搜索数据, 生成Agent数据, 读取能力定义, 从包推导编译参数
from 开发工具.契约编译.聚合契约解析 import 读取原始
for 包 in sorted(Path('.').glob('支持库/**/能力定义.json')) + sorted(Path('.').glob('模块库/**/能力定义.json')):
    p = 包.parent
    定义 = 读取能力定义(包)
    参数, 问题 = 从包推导编译参数(p)
    if 问题: print('推导失败', p, 问题); continue
    cf = p/'能力契约'/'参数契约.json'
    assert json.loads(生成能力契约(定义, 读取原始(cf) if cf.is_file() else None)) == 读取原始(cf), f'参数契约漂移 {p}'
    ......
PY
```

实测结果：**135 个能力包，4 类 JSON 派生物全仓零漂移**；`__init__.py` 有生成标记的 3 份也与重生成**逐字相同**。

### 2.2 完整性摘要（第 6 项）—— **1 处真漂移**

```bash
python3.14 开发工具/全量重算摘要.py --只报
# → 共 1 项
#   [待重算] 支持库/适配层/pdfplumber提供者: ['文件摘要不一致: 实现/PDF文本表格.py']
```

复核（同一判据、同一包）：

```bash
python3.14 开发工具/契约编译/生成物一致性检查.py 支持库/适配层/pdfplumber提供者
# → ✗ 完整性摘要.json ← 组件规范支持库.生成完整性摘要
#      与按当前源重算不同，差异键 ['文件清单']（重算 20 条清单，仅 实现/PDF文本表格.py 哈希不同）
```

**注意时点**：`实现/PDF文本表格.py` 落盘 2026-09-18 04:18:24、`完整性摘要.json` 落盘 04:18:55 —— 是**正在跑的其他并行任务**改到一半的中间态（任务约束明写「在跑的其他任务正在改包」）。本批**不重算、不修**，如实登记。

### 2.3 说明书（第 7 项）—— **14 处形态漂移，0 处真缺陷**

`说明/使用说明.md` 全仓 141 份，逐份现场跑两支生成器：

| 血统 | 份数 | 判定 |
|---|---|---|
| G1 生成器逐字相同 | **96** | 可机器重算 |
| G2 生成器逐字相同 | **2**（`模块库/能力目录`、`模块库/项目文档`） | 可机器重算 |
| 白名单人工类（生成器不得覆盖） | **29** | 机器不可重算（合法） |
| **形态漂移**（与 G2 现产出不同、且**不在白名单**） | **14** | 见下 |

14 项清单（与 G2 生成器实跑的整行集合差）：

| 包 | 差额 | 主因（现场复核） |
|---|---|---|
| 技能库/后端/技能库 | G2 多 149／现行多 51 | 说明文字比 `能力定义.json` 的「一句话说明」更长（人工留了长描述） |
| 支持库/前端/桌面宿主 | 多 39／多 18 | 同上（`启动桌面窗口并把用户操作交给业务回调` vs 定义里的短句） |
| 支持库/前端/浏览器宿主 | 多 42／多 18 | 同上 |
| 支持库/前端/组件控件 | 多 194／多 73 | 同上，且**现行说明书把句柄类参数标成 `整数型`，定义/契约里是 `文本型`**（实质口径冲突，需裁决） |
| 支持库/后端/并发控制支持库 | 多 1212／多 438 | 同上；现行 `句柄:整数型` vs 定义 `文本型` |
| 支持库/后端/系统核心支持库/进程管理 | 多 265／多 97 | 现行写「依赖能力：无」，而包声明的 `依赖` 非空 → **G1 比 G2 更贴近事实** |
| 支持库/后端/网络通信支持库/请求 | 多 153／多 51 | 同上 |
| 支持库/后端/大语言模型支持库/模型连接器 | 多 682／多 241 | 版本差（现行 1.2.5 / 生成侧 1.2.6）+ 参数类型差 |
| 支持库/后端/办公文档支持库 | G2 多 7／现行多 151（G1 多 237／现行多 116） | **聚合父包**：说明称 6 个子域，磁盘上已有 8 个 → 现行版本落后 |
| 支持库/后端/大语言模型支持库 | 多 7／多 404 | 聚合父包：称 5 个子域，实为 6 个（漏 `会话存储`） |
| 支持库/后端/数据操作支持库 | 多 7／多 536 | 聚合父包：称 5 个子域，实为 7 个（漏 `响应塑形`/`度量`） |
| 支持库/后端/文件系统支持库 | 多 7／多 217 | 聚合父包：称 1 个子域，实为 3 个（漏 `内容检索`/`文本补丁`） |
| 支持库/后端/系统核心支持库 | 多 7／多 492 | 聚合父包：说明子域数与子包数不符 |
| 支持库/后端/网络通信支持库 | 多 7／多 74 | 聚合父包：同上 |

**为什么判成「形态漂移」而不是「缺陷」**：这 14 份全部含签名句「调用口径以本文为准」，即**生成物类形态**；但它们**不在**白名单 34 条里。白名单铁律「禁止对全部说明书批量重生成」的三条理由（人工类 26 份会被整文件覆盖、B 混血 6 份手写区会被刷掉、生成物类 116 份里另有 12 份含生成器不产的人工行）在本次实测里**全部复现**：白名单覆盖 34 项，逐份重算后仍有 **108 份**含签名句的说明书不在白名单（含这 14 份）。**这 14 份就是白名单的漏登面**——登记缺口，不是代码缺陷，处置是「补登记 + 逐份裁决」，不在本批范围。

### 2.4 依赖锁（第 8 项）—— 0 处漂移

```bash
python3.14 -m 开发工具.重建依赖锁 --json | head -12
# → "目标锁数": 32, "需重建": 0, "其中环境漂移": 0, "拒绝": 0
```

32 份锁的环境块（Python 3.14.4 / macOS / arm64）与当前平台**逐值一致**，幂等口径成立（换平台才重建）。

### 2.5 编译器已明确「不生成」的生成物（第 16 项）

`能力定义编译器.生成验证场景引用` 现**恒返回 None**，docstring 写明：旧实现输出的 `{场景id,目标,范围}` 三键格式缺 `契约版本`、不是合法 v1 引用条目，**生成即把合法文件覆盖成非法文件并毁掉作者手写的富场景**。故 135 份 `验证场景引用.json` 全部由作者手写，机器不可重算。

### 2.6 谁在校验它：映射表

```bash
grep -rn "完整性摘要\|参数契约\|能力搜索数据\|使用说明\|验证场景引用\|权限契约\|配置契约\|依赖契约\|生命周期契约" \
  --include=*.py 开发工具 运行核心 公共契约 | grep -v __pycache__ | head -40
```

| 生成物 | 校验者（现场取证） |
|---|---|
| 参数契约 / 包声明 / 能力搜索数据 / Agent查询数据 / `__init__.py` | `测试中心/开发工具/测试_漂移门禁.py`（门禁②：全仓 `检测能力定义漂移` 必须为 0）；`测试_能力定义编译器.py` |
| 参数契约（注册口径）| `发布门禁/运行发布门禁.py:1840`（注册口径新增判据）；`漂移检测.检测注册口径漂移` |
| 参数契约 / 实现 / 说明书（三方）| `发布门禁:1870`（「全面漂移」强制项 = `全面漂移检测门禁`）；存量基线 `契约编译/全面漂移存量基线.json` |
| 完整性摘要 | `组件合规/合规测试包.py:_场景完整性摘要`；`发布门禁` 摘要一致；`漂移检测.检测完整性摘要格式` |
| 说明书 | `公开调用完整性门禁.py`（E-3 字段级对账、`说明书-使用说明.md缺失`）；`漂移检测.检测说明书一致` |
| 依赖锁 | `公共契约/包声明/声明.检查依赖锁内容`（**唯一判据**）；`运行核心/加载器/生命周期管理/管理器.py`（装配期强制）；`重建依赖锁.依赖锁判据()` |
| 权限契约 | `组件合规._场景权限`；`发布门禁:804`；`对外契约变更判定` |
| 配置契约 / 依赖契约 | `组件合规._场景配置` / `._场景依赖` |
| 生命周期契约 | `依赖生命周期审计/审计核心.py`（健康探针/释放策略/停止入口）；`发布门禁:834` |
| 资源预算 / 复用决策 | `组件合规`（S0 缺项阻断清单，只查在不在） |
| 默认配置 / 目录树与命令 | **无任何校验者** |
| 设计说明 | **无任何校验者**（只有指南要求「必须真填」） |

### 2.7 **「伪生成物」核实（方案 §5.3 点名要查）**：**结论 = 0 份**

判定口径（可复现）：对每个带生成标记（`本文件由契约编译器自动生成，禁止手工修改`）的文件，**用当前源重算并要求逐字相同**。带标记却与重算不同的，才是「该自动却手写」。

```bash
grep -rl "本文件由契约编译器自动生成，禁止手工修改" --include='__init__.py' 支持库 模块库 技能库 平台控制面 启动监督器 运行核心 | wc -l
# → 3
python3.14 开发工具/契约编译/生成物一致性检查.py --全部 --json    # 逐条看 __init__.py 的状态
# → __init__.py：一致 3／手工维护 132／不一致 0
```

**真正的「伪生成物」不是「该自动却手写的」，而是反向的「长得像生成物、实际是人工维护、却没人登记」**：

1. **14 份说明书形态漂移且不在白名单**（§2.3）—— 含签名句（生成物形态），但内容与两支生成器都不一致；一旦有人按「生成物类可重生成」处理，会整文件覆盖。
2. **132 份 `__init__.py` 无生成标记**，靠「含 `def 注册能力`」被编译器**放行**（`_写产物` 里的特判）。这不是漂移，但**没有人校验这 132 份的注册口径是否与 `<包id>.实现.<模块>` 相符**——`测试_漂移门禁` 只查「生成标记或注册能力」，不查内容。
3. **`默认配置/默认配置.json`（12 份）与 `说明/设计说明.md`（141 份）全仓零校验者**——写了没人看，是「声明了字段却没人校验」的同一类病（B-14 那条病根的兄弟）。

---

## 三、只读校验入口

**`开发工具/契约编译/生成物一致性检查.py`**（本批新增，唯一交付的代码文件）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH
python3.14 开发工具/契约编译/生成物一致性检查.py 模块库/开工编排 支持库/后端/组件规范支持库 模块库/文件管理
python3.14 开发工具/契约编译/生成物一致性检查.py --全部
python3.14 开发工具/契约编译/生成物一致性检查.py --全部 --json
```

- **只读保证（代码级强制，不是约定）**：运行期把 `pathlib.Path` 的 `write_text/write_bytes/mkdir/replace/unlink/rmdir/rename/touch` 与 `builtins.open` 的写模式**打桩拦截**，任何生成器试图写盘当场抛错并计入报告；另设 `sys.dont_write_bytecode = True`。**它没有写的能力**，不可能被当成重算器误用。
- **退出码**：`0` 全一致｜`1` 存在真实不一致｜`2` 用法错误/包未命中。
- **判据不另立算式**：每一类都调用该生成物自己的权威生成器（见 §一表），只做「重算结果 vs 磁盘」比对。
- **说明书是四档**：`G1逐字相同` / `G2逐字相同` / `形态漂移` / `白名单人工类` —— 不用一个「不一致」把「人工维护」和「真落后」混为一谈。
- **未接门禁**（任务边界）：本批只交付入口与数据，接线是下一步。

---

## 四、口径更正：方案 §5.1 的「139 包 ≈ 834 个同步点」

方案 §5.1 写「改 1 文件 → 手工同步 6 份生成物（139 包 ≈ 834 个同步点）」。现场取证后**需要更正两处**：

1. **包数 139 → 141**（`find . -name 包声明.json` 排除 `工程缓存` 得 141；`发现全部()` 只收 135，差的 6 个是聚合父包）。
   注意：`find` **不排除 `工程缓存` 时会得 5174** —— 该目录下有 5033 份制品仓库里的包声明副本，取证必须排除。
2. **「6 份生成物」里有 3 份其实是源文件**：`参数契约.json`、`包声明.json`、`验证场景引用.json` —— 前两者是 135 份**逐包存在**、且被 `能力定义编译器` 当作**输入**读（非破坏性口径要读既有契约/既有声明才不丢手写内容），第三者编译器**已明确拒绝生成**。把它们算成「要跟着变的生成物」会把同步点虚高一倍。
3. **「漂移 85 次/历史」无法用当前工具复现**：`git log` 里没有可机读的漂移台账；本批给的是**当前真实快照数**（稳定存量 14 条 + 1 条被当场捕获的写包途中漂移），历史数不在本批范围，不予背书。

---

## 五、当前真实漂移数（全仓 141 包，2026-09-18 04:20 CST，提交 `6b70e0b1`）

取证命令：

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH
python3.14 开发工具/契约编译/生成物一致性检查.py --全部 --json > /tmp/生成物一致性_全仓.json
python3.14 开发工具/全量重算摘要.py --只报          # 交叉复核完整性摘要
python3.14 -m 开发工具.重建依赖锁 --json            # 交叉复核依赖锁
```

| 状态 | 条目数 | 说明 |
|---|---|---|
| `一致` | **807** | 与按当前源重算逐字相同 |
| `不一致` | **1** | `支持库/适配层/pdfplumber提供者/完整性摘要.json` |
| `不一致（形态漂移）` | **14** | `说明/使用说明.md`，全部未登记进白名单 |
| `缺失` | **0** | — |
| `手工维护`（合法） | **161** | 132 份 `__init__.py`（编译器承认的手写入口）+ 29 份白名单人工类说明书 |
| `无法机器重算` | **752** | 8 类无生成器物 |
| `跳过` | **6** | 6 个聚合父包（无能力定义，不产派生物） |
| **合计** | **1741** | 141 个正式包 |

> **那 1 条 `不一致` 是并行会话的中间态，已自行闭合 —— 复跑对账（04:23:34）**
> - 04:20:09 全仓跑：`支持库/适配层/pdfplumber提供者/完整性摘要.json` 与重算不同（差异键 `文件清单`），
>   同一时点 `python3.14 开发工具/全量重算摘要.py --只报` **独立复现**同一条：`文件摘要不一致: 实现/PDF文本表格.py`。
> - 04:23:34 复跑：`全量重算摘要.py --只报` → **全部闭合**；该文件与摘要登记的 sha256 **同为 `c2616b9c6fe956bedbe28d5f11e905c791b0fc078de81266f7705ca48b2ea525`**；
>   全仓检查器读数为 `一致 808／手工维护 161／形态漂移 14／不一致 0`，退出码仍为 1（14 条形态漂移）。
> - 结论：**它是一条真实被捕获的「写包途中」漂移**，正是本入口要抓的场景；不是稳定存量。
>   本批未触碰该包（不重算、不修）。

**分生成物类型的真实漂移数**

| 生成物类型 | 漂移数 / 总数 | 严重度 |
|---|---|---|
| `说明/使用说明.md`（形态漂移） | **14 / 141** | **稳定存量**：登记缺口为主，含 2 处**实质口径冲突**（`组件控件`/`并发控制支持库` 的 `句柄` 参数：现行说明书 `整数型` vs 契约/定义 `文本型`） |
| `完整性摘要.json` | **0 / 135**（04:23:34 读数） | 04:20 曾捕获 1 条写包途中漂移，已被写者自己闭合，见上方对账 |
| `能力契约/参数契约.json` | **0 / 135** | — |
| `包声明.json` | **0 / 135** | — |
| `能力数据/能力搜索数据.json` | **0 / 135** | — |
| `能力数据/Agent查询数据.json` | **0 / 135** | — |
| `__init__.py` | **0 / 135** | — |
| `依赖锁.json` | **0 / 32** | — |
| 其余 8 类（权限/配置/依赖契约、资源预算、复用决策、生命周期契约、默认配置、验证场景引用） | **不可判**（752 条） | 全仓无生成器，只能登记不能重算 |
| `说明/目录树与命令.md`、`说明/设计说明.md` | **不可判** | 全仓零校验者 |

### 与既有门禁现场跑数的**分歧**（必须点明，不许两套数字并存）

| 门禁 | 现场跑数 | 与地图的分歧原因 |
|---|---|---|
| `检测能力定义漂移`（`测试_漂移门禁` 在跑） | **6 包报红**：全是 `存在包声明但无能力定义（需迁移到唯一事实源）` | 命中的就是 **6 个聚合父包**。`编译包()` 已明确认定「聚合父包不编译是**正确结果**」，但这个检测器**没同步该口径** → 6 条**假红**。本批不改检测器，如实登记。 |
| `检测完整性摘要格式` | 0 条 | 与地图一致 |
| `重建依赖锁 --json` | 需重建 0 / 32 | 与地图一致 |
| `全量重算摘要 --只报` | 待重算 1 项 | 与地图一致（同一包） |
| `错误码文案派生 校验` | 1 项违规：`错误码-派生规范读不到说明表源`（`运行核心/统一网关/网关核心.py`） | **判据过期**：`公开错误说明表` 已迁到 `运行核心/统一网关/协议表.py`，`网关核心.py` 只剩「原样再导出」（`from 运行核心.统一网关.协议表 import ... 公开错误说明表`），而 `开发工具/错误码文案派生.py:62` 的 `说明表源` 仍指向 `网关核心.py` → AST 读不到模块级字面量赋值。属「生成物搬迁后判据没跟」的同类漂移（与生成物地图同病，本批只报不动）。 |

---

## 六、取证命令清单（可复现）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH   # /usr/bin/git 是 Xcode shim
unset PYTHONPATH                                                 # 必须；python3 是 3.9.6 会假绿

# ① 包数与生成物份数
find . -name '包声明.json' -not -path './.git/*' -not -path './工程缓存/*' | wc -l   # 141
find ./工程缓存 -name '包声明.json' | wc -l                                          # 5033（制品仓库副本，必须排除）

# ② 全仓生成物一致性（本批唯一代码交付物）
python3.14 开发工具/契约编译/生成物一致性检查.py --全部 --json | python3.14 -m json.tool | head -40

# ③ 与既有通道交叉复核（全部只读）
python3.14 开发工具/全量重算摘要.py --只报
python3.14 -m 开发工具.重建依赖锁 --json | head -12
python3.14 开发工具/错误码文案派生.py 校验

# ④ 只读性自证：跑全仓前后 git 工作区必须无差异
git status --porcelain | sort > /tmp/跑前.txt
python3.14 开发工具/契约编译/生成物一致性检查.py --全部 > /dev/null
git status --porcelain | sort > /tmp/跑后.txt
diff /tmp/跑前.txt /tmp/跑后.txt && echo "无差异 = 检查器确实只读"
```

---

## 七、给下一步（批 4 的后续，不在本批范围）

1. **接线门禁（C 路线）**：把 `生成物一致性检查.py` 的「参数契约/包声明/能力搜索数据/Agent查询数据/`__init__.py`/完整性摘要」6 类接成发布门禁的**强制项**（存量冻结 + 新增即红，与「全面漂移」「注册口径」同形），**排除**说明书 4 档里的 `手工维护`/`形态漂移`。
2. **先修 3 处判据自身**（不然接线后立刻假红）：
   - `检测能力定义漂移` 的「存在包声明但无能力定义」要对 6 个聚合父包放行（口径已有 `编译包()` 的实现）；
   - `错误码文案派生.py` 的 `说明表源` 改指 `协议表.py`；
   - 白名单补登 14 份形态漂移说明书（或裁决其去留）。
3. **裁决 2 处实质口径冲突**：`支持库/前端/组件控件`、`支持库/后端/并发控制支持库` 的 `句柄` 参数 —— 契约/定义写 `文本型`，现行说明书写 `整数型`，二者必有一错。
4. **6 个聚合父包的说明书要跟子域**：`办公文档/大语言模型/数据操作/文件系统/系统核心/网络通信` 的说明词条数与磁盘子包数普遍不符（差 2~3 个子域），且它们**不被 `发现全部()` 收**（只返回 135），是「生成器发现范围」的洞。
5. **8 类无生成器物要不要建生成器**（752 条）：其中 `权限契约`（135 份）与 `配置契约`（135 份）有结构、有意义、有校验者，是收益最高的两个候选；`默认配置`、`设计说明`、`目录树与命令` 连校验者都没有，先裁决去留再谈自动化。
