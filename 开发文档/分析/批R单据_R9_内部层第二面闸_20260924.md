<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R单据 · R-9：内部层收口「第二面」加机器闸（审计 T8）

- 开工ID：`开工-20260924-111036-45b3`（5 条租约，见 §七）
- 项目根：`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`
- 上游：`开发文档/分析/审计_两条腿与冗余_20260923.md` **§T8**（判据原文：`内部层=真` 既不是「网关 0 暴露」的判据，也不是「页面不可绑定」的判据 —— 收口靠人；待裁＝给第二面补一条机器判据）
- 平台口径：`开发文档/项目说明.md` **§237（哲学 7.1）**「收口面有两处，必须一起撤」—— ① 网关暴露面＝`公共契约/包声明/声明.py` 的 `内部层=真`；② HTML 暴露面＝页面不再引用该 `能力id`
- 一句话结论：**第二面已有机器闸**。判据挂在**既有门禁**（`开发工具/公开调用完整性门禁`，未新造骨架）上，分两载体各出各的缺口类型：**页面真引用 ⇒ 判红**、**可绑定集仍含 ⇒ 走该门禁既有存量基线（只报不拦、只减不增）**。现场存量 **16 桶 / 69 条**（全为「可绑定集仍含」），「页面仍引用」现场 **0 条**；反向样本三拍 = **判红 → 转绿 → 复红**。

---

## 一、权威节点（先找，不新造第二套）

`capability_search` 检索「公开调用完整性门禁 / 门禁 骨架 存量基线 / 内部层」，实测结论：本仓**门禁骨架只有一套** —— `开发工具/公开调用完整性门禁.py`（入口，只接位置参数 `根`）+ 三个兄弟件 `_包发现.py` / `_检查与判定.py` / `_说明书解析.py`，存量出口为 `开发工具/公开调用完整性门禁存量基线.json`（键＝`包相对路径|缺口类型`，语义＝**只减不增**，由 `开发工具/存量基线消费.py::分桶与收敛` 统一算）。

故本批**只往这一套里加一个判据**：

| 环节 | 落点 |
|---|---|
| 判据本体 | `开发工具/公开调用完整性门禁_检查与判定.py::检查内部层可绑定`（`:387`） |
| 接线 | `开发工具/公开调用完整性门禁.py:518` `违规.extend(检查内部层可绑定(根))`（在 `检查门禁原始项` 内，与其它判据同批收集） |
| 存量 | 复用该门禁既有桶键 `分桶(条, 根)` 与既有基线文件，**不自持第二份** |
| 反向样本 | `测试中心/开发工具/测试_公开调用完整性门禁.py::Test内部层第二面闸`（`:656`） |

---

## 二、载体实测（先量，不猜）

### 2.1 「页面」的真实载体

- 目录约定：`<任意层>/前端/页面/*.json`（与 `开发工具/项目编译/项目编译器` 同一约定）。
- 现场实测命中（`find` 实跑）：`示例项目/可双击演示/开发文件夹/前端/页面/` 下三个文件 ——
  - `主页.json`（**设计态页面**，`组件列表[1].属性.能力id = 数据操作支持库.文本处理.统计长度`）
  - `编译入口.json`（**设计态页面**，`能力id = 自修复工具.静态编译 / 自修复工具.动态编译`）
  - `编译制品.json`（**编译产物**，非设计态）
- **承载能力引用的字段只有两处**：`组件列表[].属性.能力id` 与 `组件列表[].事件[].能力id`。校验点 = `开发工具/轻代码前端编辑器/页面模型.py::校验页面`；收集点 = 同包 `编译页面.编译` 的 `能力集合`。
- 扫描面刻意排除 `编译制品.json` / `编辑记录.json`（编译产物与编辑留痕）—— 把编译输出当页面，就会拿**制品里的旧引用**判红，那是把产物当契约。

### 2.2 「可绑定集」的真实载体

- 载体 = `开发工具/项目编译/正式包索引.py::构建索引(系统根)["能力所有者"]` 的**键集**（构建点 `:84` / `:125` / `:143`；门面 `:158 能力所有者表`）。
- **它就是页面绑定读的那一份**：`开发工具/轻代码前端编辑器/页面模型.py:37-39` `_读取能力目录()` ⇒ `set(构建索引(根目录)["能力所有者"])`；`:80` `能力目录 = _读取能力目录()` 用它校验页面引用。
- **实测缺陷面**：`构建索引` 的 `能力所有者` 构建通篇无 `内部层` 判断（审计 T8 已量：`rg -c '内部层'` = 0）⇒ 16 个 `内部层=真` 的包 / 69 条能力**仍落在页面可绑定集内**，页面**可以**绑，只差人不去绑。

---

## 三、判据设计

对每个 `包声明.json` 里 `内部层=真` 的 `能力id`（唯一字段口径源：`公共契约/包声明/声明.py:123 内部层=bool(数据.get("内部层", 假))`），分两载体各出一类缺口：

| 缺口类型 | 判据 | 处置 |
|---|---|---|
| `内部层-页面仍引用` | 该 `能力id` 仍出现在仓内页面 JSON 的 `属性.能力id` / `事件[].能力id` | **判红**（新增即红，退出码 1） |
| `内部层-可绑定集仍含` | 该 `能力id` 仍在 `构建索引.能力所有者` 键集内 | **存量**（按本门禁既有体例进基线：只报不拦、只减不增） |
| `内部层-可绑定集判据不可用:<诊断>` | 有内部层能力、但可绑定集读不成（索引构建抛错 / `能力所有者` 非字典 / 为空） | **判红**（fail-closed，不静默当「可绑定集为空」放过） |

两条边界（刻意设计，防恒红/恒绿）：

1. **一个 `内部层=真` 的包都没有 ⇒ 本闸零条目**（空夹具根、未启用内部层的仓库走这条）—— 空集不是通过也不是缺陷，是「这份切片里没有要被判的东西」。
2. **聚合视图父包也参与扫描**（包发现走唯一入口 `找全部包目录`，E-2 起含全部正式根）—— 视图父包声明了能力就是真实 owner，不能靠「视图」身份把第二面整体移出扫描。
3. **真仓库剪枝、夹具不剪**（`_页面扫描剪枝`）：真仓库页面扫描进目录即剪 `工程缓存` 等生成式目录；测试夹具的受管临时根恰在 `<仓库>/工程缓存/测试临时/` 下，若一律剪枝，新判据在夹具上永远测不出来（平台纪律：新判据必须能在夹具上测出来）。变的只是代价开关，**什么算页面两档逐字一致**。

---

## 四、存量面（现场读数）

现场跑 `检查门禁原始项(仓库根)`，按该门禁自己的 `分桶` 统计：

- `内部层-页面仍引用`：**0 条**
- `内部层-可绑定集仍含`：**69 条 / 16 桶**

| 桶（包相对路径｜缺口类型） | 条数 |
|---|---|
| `支持库/后端/OCR识别支持库/OCR识别｜内部层-可绑定集仍含` | 3 |
| `支持库/后端/图像处理支持库/图像解码｜内部层-可绑定集仍含` | 9 |
| `支持库/后端/媒体处理支持库/FFmpeg媒体｜内部层-可绑定集仍含` | 7 |
| `支持库/后端/数据库连接支持库/SQLite数据库｜内部层-可绑定集仍含` | 8 |
| `支持库/后端/数据库连接支持库/psycopg数据库｜内部层-可绑定集仍含` | 5 |
| `支持库/后端/文档转换支持库/LibreOffice转换｜内部层-可绑定集仍含` | 2 |
| `支持库/后端/文档转换支持库/PDF文本表格｜内部层-可绑定集仍含` | 2 |
| `支持库/后端/文档转换支持库/PDF渲染｜内部层-可绑定集仍含` | 4 |
| `支持库/后端/文档转换支持库/PDF生成｜内部层-可绑定集仍含` | 1 |
| `支持库/后端/文档转换支持库/PDF隔离提供者｜内部层-可绑定集仍含` | 3 |
| `支持库/后端/文档转换支持库/openpyxl提供者｜内部层-可绑定集仍含` | 2 |
| `支持库/后端/文档转换支持库/python_docx提供者｜内部层-可绑定集仍含` | 2 |
| `支持库/后端/文档转换支持库/python_pptx提供者｜内部层-可绑定集仍含` | 2 |
| `支持库/后端/文档转换支持库/textutil转换｜内部层-可绑定集仍含` | 2 |
| `支持库/后端/版本控制支持库/Git操作｜内部层-可绑定集仍含` | 14 |
| `支持库/后端/转写支持库/转写｜内部层-可绑定集仍含` | 3 |

**出口纪律**：本判据**没有 `--写基线` / `--冻结` 出口**（`开发工具/公开调用完整性门禁.py:672` 只接位置参数 `根`，落盘腿只有 `读取存量基线` / `默认存量基线路径`）⇒ 基线条目只能走该门禁**既有的**同步腿（MCP 文本补丁同步），**未手改任何数值**：桶键由判据自己的 `分桶` 现场生成。

**逐桶对账（现场复算）**：`基线["条目"] == 现场内部层桶` ⇒ **真**；`桶内计数超出` ⇒ **空**；`基线多报` ⇒ **空**；合计 **69 条 / 16 桶**。

---

## 五、反向样本三拍读数（实测，非转述）

夹具：`工程缓存/测试临时/<临时根>/` 下造 `支持库/后端/示例包`（`内部层=true` + 一条能力 `示例.测试能力`，六环齐备）与 `示例项目/演示/前端/页面/主页.json`。
**拍 0 的夹具基线由判据自己的 `分桶` 生成**（未手写数值）。

| 拍 | 动作 | 新增 | 存量 | 收敛 | 退出码 | 新增缺口类型 |
|---|---|---|---|---|---|---|
| 拍 0 | 未引用（基线刚冻结） | 0 | 1 | 0 | **0** | — |
| 拍 1 | 造「`内部层=真` 且被页面引用」 | **1** | 1 | 0 | **1** | `内部层-页面仍引用` |
| 拍 2 | 撤掉页面引用（`能力id` 置空串） | 0 | 1 | 0 | **0** | — |
| 拍 3 | 恢复页面引用 | **1** | 1 | 0 | **1** | `内部层-页面仍引用` |

夹具基线原文：`{"支持库/后端/示例包|内部层-可绑定集仍含": 1}` —— 载体②（可绑定集）在夹具下同样命中（那是平台现状的真实存量，不是夹具缺陷），故读数只反映载体①（页面引用）。

**单元测试**（持久化的反向样本，同口径）：`测试中心/开发工具/测试_公开调用完整性门禁.py::Test内部层第二面闸` **4 条全绿** ——

- `test_内部层能力在可绑定集内出存量条目`（载体②出条目）
- `test_非内部层包不进本闸`（撤 `内部层` ⇒ 零条目，证明不是恒红）
- `test_反向样本三拍_判红转绿复红`（三拍断言）
- `test_页面生成物不被当页面`（`编译制品.json` 不计引用）

**真仓库对账**：`运行门禁并分诊(仓库根)` ⇒ `新增 = 2 条 / 存量 = 69 条 / 收敛 = 0`。其中**新增的 2 条与本闸无关**：`说明书-多参数` + `说明书-能力级版本不一致`，均出自 `模块库/自修复工具/说明/使用说明.md`，由**并行会话在改的** `模块库/自修复工具/能力定义.json`（工作区已 `M`）引入；本闸现场 `内部层-` 新增 **0 条**。

---

## 六、未做项（如实）

1. **生成侧过滤未做**（`构建索引.能力所有者` 过滤 `内部层=真`）—— 那是审计 T8 的**待裁项**（改了会动 `正式包索引` 与所有消费者），本批范围只到「加闸 + 存量登记」。落地后上述 16 桶应自然收敛，判据会自报 `[可收敛]`。
2. **审计 T8 的另一条未做**：`模块库/自修复工具/包声明.json` 无 `内部层` 键（第一面也没落，`模块库/自修复工具/实现/静态编译.py:5` 自述 `运行开发编译口`「仍是内部腿」）。该包**不在本批写集**，且其 `能力定义.json` 正被并行会话占用 ⇒ 不碰。
3. **`模块库/能力目录/实现/能力索引.py:175` 注释过期**（写「16 包 68 条」，实测 69 条）—— 不在写集，未改。
4. **未跑编译、未 git 提交**（纪律要求）。本件的全部读数来自 `检查门禁原始项` / `运行门禁并分诊` 现场复跑与单元测试直跑；静态编译 / 动态编译由收口方统一跑。
5. 真仓库 `运行门禁`（裸跑、无基线）当前**非零退出**，但红的是并行会话那 2 条 `说明书-*`，非本闸；本闸在真仓库上 `内部层-` 新增为 0。

---

## 七、写集与租约（开工-20260924-111036-45b3）

| # | 路径 | 租约id | 改动 |
|---|---|---|---|
| 1 | `开发工具/公开调用完整性门禁.py` | `ff360ac13e7f475e` | +6 行：import 5 名 + `违规.extend(检查内部层可绑定(根))`（`:518`） |
| 2 | `开发工具/公开调用完整性门禁_检查与判定.py` | `9d9337cc471d4522` | +185 行：常量 `:274-275`、`收集内部层能力 :283`、`_页面扫描剪枝 :313`、`页面引用能力id集 :324`、`页面可绑定能力集 :367`、`检查内部层可绑定 :387` |
| 3 | `开发工具/公开调用完整性门禁存量基线.json` | `038a3bf285cf4686` | 条目 0 → 16 桶 / 69 条（既有落盘腿同步） |
| 4 | `测试中心/开发工具/测试_公开调用完整性门禁.py` | `f1b995efa54f47a9` | +新类 `Test内部层第二面闸`（`:656`，4 条）；`test_真仓库基线覆盖全部存量`（`:562`）断言改为「逐桶等于现场内部层桶 + 只许装内部层桶」 |
| 5 | `开发文档/分析/批R单据_R9_内部层第二面闸_20260924.md` | `8858c02918db4069` | 本件 |

---

## 附录：复跑命令

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台

# 判据本体 + 存量读数
python3.14 -c "import sys;sys.path.insert(0,'.');from 开发工具.公开调用完整性门禁 import 运行门禁并分诊,检查门禁原始项,分桶;from pathlib import Path;根=Path('.').resolve();n,c,h=运行门禁并分诊(根);print('新增',len(n),'存量',len(c),'收敛',len(h));b={};
[ b.__setitem__(分桶(t,根), b.get(分桶(t,根),0)+1) for t in 检查门禁原始项(根) if str(t['缺口类型']).startswith('内部层-')];print(len(b),'桶',sum(b.values()),'条')"

# 反向样本（持久化单元测试）
python3.14 -m pytest "测试中心/开发工具/测试_公开调用完整性门禁.py::Test内部层第二面闸" -q
```
