<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R · R-13 · 「子代理绕过 MCP 跑终端」的根因收口（提示词注入面）

> 日期：2026-09-24 ｜ 开工ID：`开工-20260924-112735-68f7`
> 租约id清单：`81e82a64617a4f78`（开发工具/薄壳/工具清单.py） `a977ebd11ae34cfc`（本件）
> 上游：`开发文档/分析/终端命令与能力缺口调研_20260923.md`（§3.1「有对等能力但没用」）＋`开发文档/未完成事项.md`（#219 工具面落后于薄壳）
> 口径：本件是**分析类**件；读数均为本批现场实测（行号/账本值会漂，引用前重跑 §五 命令）
> 本批**未跑**任何编译/门禁/测试、**未 git 提交**（华哥本批硬约束）

---

## 零、结论（直达核心）

1. **前提更正（华哥 2026-09-24 指正）**：「MCP 没能力／没人用」**不成立** —— 账本口径 `正则搜索` 全史 745 次、`读取文件` 1,082 次，752 条能力**全部有调用记录、0 条从未进账**。真问题是**用得不顺手 / 想不到**。
2. **主修项＝提示词注入面**：`工具清单.py` 的三个工具描述此前**零处**点名「读文件 / 内容检索 / 跑命令」该用哪个能力 ⇒ 结构性的「想不到」。本批按既有**动态段占位符机制**补「常见意图→首选能力」映射（4 条），**不另造描述源**。
3. **附带修掉一处存量红**：注入面预算 `静态 1,903 + 2×110 = 2,123 > 2000`（实测组装工具面 2,106）—— 不修就塞不进第 2 条的新文案。已按同一口径重算并收紧文案：`1,784 + 2×105 = 1,994 ≤ 2000`（实测 1,990）。
4. **对照实验达标**：同一取证任务，MCP 路线 **2 次 / 2,992 字符** ≤ 终端观察形态 **5 次 / 3,244 字符**。
5. **零新增能力、零新增参数** —— 「多文件同一区间」由既有 `正则搜索(上下文=N)` 覆盖，不构成新节点缺口；`读取文件` 单文件单区间属低频形状，登记残余不新建。

**改了哪些文件**

| 动作 | 路径 |
|---|---|
| 改 | `开发工具/薄壳/工具清单.py`（18 处精确替换：三处描述 + 九处 schema 文案 + 预算注释与动态段上限） |
| 新增 | 本件 |

---

## 一、诊断：为什么「想不到用 MCP」（可复现读数）

### 1.1 数据源与口径

- 本批现场读数来源：① MCP 实测（`tool_catalog` / `能力详情` / 真实能力调用）；② 账本 `工程缓存/运行数据/调用参数账.sqlite3`（口径：全时段精确计数）；③ 终端 `rg -n` / `sed -n` / `wc -m`。
- 字符口径统一为**字符数**：终端用 `wc -m`，MCP 取网关自报 `原字符数`。
- **不是**回忆，也**不是** `MCP全量回执` 库（那只存「超过返回上限」的大返回，小返回不进去 —— 华哥 2026-09-24 指正的口径错误即出在这里）。

### 1.2 三个真实 rg 用法 × `文件系统支持库.内容检索.正则搜索` 参数面对照

能力参数面（`能力详情` 实测）：`根目录` / `模式` / `路径` / `文件模式` / `最大匹配数`（硬上限 200）/ `上下文`（0–3）/ `忽略大小写` / `排除列表`。

| # | 终端 rg 用法 | 对应 MCP 传参 | 一次能否拿到同样东西 | 差在哪 |
|---|---|---|---|---|
| 1 | `rg -n "模式" 路径 --glob '*.py' \| head -40` | `模式` + `路径` + `文件模式='*.py'` + `最大匹配数=40` | **能** | 无。`最大匹配数` 硬上限 200（`head -40` 之外的任意大值要分页或收紧 `文件模式`） |
| 2 | `rg -n -C2 "模式" 路径` | 同上 + `上下文=2` | **能** | `上下文` 只到 3（rg 可任意）；>3 行上下文拿不到 |
| 3 | `rg -n "模式" 子目录/`（按目录递归） | 同上 + `路径='运行核心'` | **能** | 无（实测 `搜索根=运行核心`、`匹配数=3`、`上下文数=2`，与终端一致） |

**⇒ 参数面不缺（3/3 覆盖），本批不新增参数。** 缺的是下面 1.3 / 1.4 两条。

实测读数（同一份全仓 `_归一化路径族参数`，16 命中）：

| 形态 | 返回字符数 | 说明 |
|---|---|---|
| 终端 `rg -n`（无上下文） | 1,223 | 16 行 `文件:行号:文本` |
| MCP `正则搜索(上下文=0)` | 2,635 | 其中 `命令` 回显 **714**（27%），匹配列表 1,836（≈115 字符/命中；终端 76/命中） |
| 终端 `rg -n -C2` | 4,981 | 命中 + 前后各 2 行 |
| MCP `正则搜索(上下文=2)` | 8,719 | 同内容 ⇒ **1.75×** |

**差在哪（只增参数的落点）**：结构化返回把每条命中／每个上下文行都摊成完整对象（逐行重复 `文件` 绝对路径与 `文件/行号/文本/是否命中` 四个键名），另有 `命令` 回显 714 字符。**建议（写集外，须华哥确认）**：给 `正则搜索` 只增一个可选参数（如 `紧凑返回=真`，默认关、老键语义不变），把 16 命中压回 ≈1,400 字符量级。

### 1.3 `文件系统支持库.文件操作.读取文件`：一次只能一个文件一个区间（实测）

传 `文件路径="运行核心/统一网关/核心处理/网关参数校验面.py\n公共契约/基础类型/字段名册.py"`（换行拼接两个路径），现场实测：

```
成功=false   错误码=文件不存在
错误说明=文件不存在: /Users/hekunhua/Documents/Agent/PHP/系统工程平台/运行核心/统一网关/核心处理/网关参数校验面.py\n公共契约/基础类型/字段名册.py
```

⇒ 能力把整串当一个路径 ⇒ **多文件 / 多区间必须 N 次调用**。参数面只有单值 `文件路径` + 单区间 `起始行`/`结束行`（另 `最大字符` 默认 2000、`精简` 默认 真；显式分段则不限长不精炼）。

**是否要新增「批量取证」节点？判据：已有节点能否覆盖 ⇒ 能，故不新增。** 「一次调用拿多文件多区间的相关行」正是 `正则搜索(上下文=N)` 的形状（实测一次回 16 命中 + 前后各 2 行，跨 6 个文件）；`目录树` 覆盖「看结构」；只有「多文件**整段正文**」才需 N 次 `读取文件`，属低频形状。⇒ 本批**不新增公开能力**。

### 1.4 发现性缺口（本批主修项）

改前 `tool_catalog` 实测：`capability_call` 描述全文＝ POST 形状 + 返回体量 + 路径口径 + 异步腿，**零处**出现 `正则搜索` / `读取文件` / 检索类字样；`capability_search` 描述除 `能力目录.搜索能力` 外不点名任何能力。⇒ Agent 每次只读到那一段，**「有对等能力但不知道」**（与 2026-09-23 调研件 §3.1 同一结论，本批把它落成施工）。

### 1.5 存量红：注入面预算已超

`测试中心/开发工具/测试_薄壳返回可控.注入面预算测试` 的判据是 `三个描述 + 三个 schema ≤ 2000`，并要求 `静态 + 2×动态段上限 ≤ 2000`（构造保证）。改前现场实测：

```
静态 = 1903   动态段字符上限 = 110   最坏 = 2123   ≤2000: False
实测组装工具面 = 2106                          ≤2000: False
```

⇒ **两条断言都是红的**（2026-09-23 补「路径口径 / 异步腿」文案时未按该文件内注释重算上限）。不修则 §1.4 的文案塞不进去，故本批一并收回到预算内。

## 二、收口（改了什么）

### 2.1 提示词注入面（主修项）—— 只改 `开发工具/薄壳/工具清单.py`

走既有 `{{常用传参}}` 占位符机制，未新增描述源。新增文案（`capability_call` 描述内）：

```
★ 常见意图→首选能力：读文件=`文件系统支持库.文件操作.读取文件`；
内容检索=`文件系统支持库.内容检索.正则搜索`（≈一条 rg：文件模式/上下文/最大匹配数）；
跑命令=`系统核心支持库.进程管理.执行命令`；找能力=`能力目录.搜索能力`。
```

`capability_search` 描述补 `★ 找能力=本工具；看契约=调用能力 操作「能力详情」。`

**能力id 真源核对（硬约束：描述里的 id 必须真实存在）** —— 本批现场用 `capability_search` 逐个核在册：

| 描述里出现的能力id | 现场核对 |
|---|---|
| `文件系统支持库.文件操作.读取文件` | 在册（`能力详情` 取到完整契约） |
| `文件系统支持库.内容检索.正则搜索` | 在册（`能力详情` 取到完整契约，包版本 1.1.2） |
| `系统核心支持库.进程管理.执行命令` | 在册（`capability_search` 命中） |
| `能力目录.搜索能力` | 在册（与薄壳 `搜索能力目标` 常量同源） |

⇒ 4/4 命中。**未加自动断言**（加断言要动 `测试中心/…`，在本单写集外）⇒ 见 §四 R13-5。

### 2.2 注入预算重算（把存量红收回绿）

同一口径（该文件内注释即口径）：`静态 + 2×动态段上限 ≤ 2000`。

| 项 | 改前 | 改后 |
|---|---|---|
| 三个模板（去占位符） | 注释 462 ／ 实测 582 | **555** |
| 三个 schema | 注释 1,288 ／ 实测 1,321 | **1,229** |
| 静态合计 | 1,903 | **1,784** |
| `动态段字符上限` | 110 | **105**（=(2000-1784)/2=108，取 105 留 6 字符余量） |
| 最坏（构造保证） | 2,123 ❌ | **1,994** ✅ |
| 实测组装工具面 | 2,106 ❌ | **1,990** ✅ |

腾出的空间来自**收紧冗余文案**（不动判据钉住的要件）：三处 schema 文案压短（-92）、目录模板去「元数据」、调用模板去掉 JSON 样例里两个可选键与重复复述、路径口径去举例与「永久兼容」句、异步腿细节改为指向 `通用纪律`。**判据要件全部保留**：描述仍含「默认 2000 字符」、仍含「异步腿 / 任务提交 / 任务查询」、schema 仍含「默认 2000；」、`细节级别` enum 未动、三个占位符未动。

### 2.3 未做（守边界）

- **未新增能力、未新增参数**（§1.3 判据：已有节点可覆盖）。
- **未改 `薄壳服务.py`**（无需要）。
- **未跑编译/门禁/测试、未 git 提交**（华哥本批硬约束）。

## 三、对照实验（硬判据）

取证任务：**「找出全仓 `_归一化路径族参数` 的调用点并读上下文」**。

### 路线① 终端

| 步 | 命令 | 字符数 |
|---|---|---|
| 1 | `rg -n "_归一化路径族参数" --glob '*.py' . \| head -40` | 1,223 |
| 2 | `sed -n '85,100p' 运行核心/统一网关/核心处理/网关参数校验面.py` | 496 |
| 3 | `sed -n '260,275p' 同上` | 564 |
| 4 | `sed -n '183,195p' 公共契约/基础类型/字段名册.py` | 480 |
| 5 | `sed -n '245,258p' 测试中心/运行核心/测试_网关路径解析.py` | 481 |
| — | **合计（观察形态）** | **5 次 / 3,244** |
| — | 最小形态（`rg -n -C2` 一次到位） | **1 次 / 4,981** |

### 路线② MCP

| 步 | 能力与传参 | 字符数 |
|---|---|---|
| 1 | `文件系统支持库.内容检索.正则搜索(模式=_归一化路径族参数, 文件模式='*.py', 最大匹配数=50)` | 2,635（16 命中） |
| 2 | `文件系统支持库.文件操作.读取文件(文件路径=运行核心/统一网关/核心处理/网关参数校验面.py, 起始行=92, 结束行=100)` | 357 |
| — | **合计** | **2 次 / 2,992** |
| — | （对照）单次带上下文 `正则搜索(上下文=2)` | 1 次 / 8,719 |

### 读数结论

- **vs 观察形态（5 次 / 3,244）**：MCP **2 次 / 2,992** ⇒ 次数更少、体积更小 ⇒ **达标**。
- **vs 最小形态（1 次 / 4,981）**：MCP 次数 +1、体积 −40% ⇒ 属「相当或更少」一侧。
- **差在哪（逐条）**：① 返回面结构性膨胀 1.75×（§1.2，主因是逐行重复键名 + `命令` 回显 714）⇒ **只增一个「紧凑返回」可选参数**即可追平，属 `内容检索` 包、本单写集外，**须华哥确认**；② `读取文件` 无批量 ⇒ 已判**不必新增**（`正则搜索(上下文=N)` 覆盖多文件多区间）。
- **不可优化项（边界）**：MCP 往返成本（华哥实测口径约 6–7 秒/次）不由本单改变，故本单只给**次数**与**体积**两组读数，不把往返耗时算作路线优劣。

## 四、残余与须确认项

| # | 事项 | 现状 | 处置 |
|---|---|---|---|
| R13-1 | `正则搜索` 返回面结构性膨胀 1.75×（`命令` 回显 714 字符 + 逐行重复键名） | 只增参数的落点已定位 | **须华哥确认**后另开单：给 `内容检索.正则搜索` 增可选「紧凑返回」参数（默认关）。写集外 |
| R13-2 | **工具面文案的验收口径**：客户端 schema 是连接时刻快照 | 本批服务端已生效（`tool_catalog` 实测 `是否一致=真`、`本进程已换壳=真`、`工具清单.py` 指纹 `a0b394a5…`）；但**本会话**工具面仍是旧文案 | 验收必须在**新开会话**里看新文案（未完成事项 #219 同一根因） |
| R13-3 | **派生件未重建**：全仓 4 份制品副本（`工程缓存/制品仓库/…/开发工具/薄壳/工具清单.py`）仍是旧文案 | 本批不跑编译 | 随 `自修复工具.静态编译` 时自然收敛 |
| R13-4 | `注入面预算测试` 本批未跑 | 纪律：不跑编译/测试 | 复核用 `自修复工具.静态编译(类型=1, 模块ID=开发工具/薄壳)`；本件已用同一判据算式现场量到 `1,994 ≤ 2000` |
| R13-5 | 描述里的能力id **未加自动断言** | 本批只做了现场核对（4/4 在册） | 建议在 `测试中心/开发工具/测试_薄壳返回可控.py` 增一条「描述中出现的能力id 必须能在册」的断言（防手抄漂移；该文件注释已记过一次「描述写 6000、实现 2000」的事故）。写集外 |
| R13-6 | 终端用途其余两类：复合命令 394 次（29%，`cd && git …`）、内联 python 86 次（6%） | 不在本单范围 | 复合命令：`执行命令(经shell=真)` / `Git操作.*` 可覆盖（属 §1.4 同类「想不到」）；内联 python：2026-09-23 调研件 §3.2 真缺口 2（无「执行代码」能力），仍待裁决 |

## 五、复现命令（引用前重跑）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台/开发工具/薄壳
python3.14 - <<'PY'
import json,sys
sys.path.insert(0,'/Users/hekunhua/Documents/Agent/PHP/系统工程平台')
from 开发工具.薄壳 import 工具清单 as 清单
静态=sum(len(t.description or '')-len(清单.动态段占位)+len(json.dumps(t.inputSchema,ensure_ascii=False)) for t in 清单.三个工具定义)
print(静态, 清单.动态段字符上限, 静态+2*清单.动态段字符上限)
PY

# 对照实验：终端侧
rg -n "_归一化路径族参数" --glob '*.py' . | head -40 | wc -m
rg -n -C2 "_归一化路径族参数" --glob '*.py' . | wc -m
```

MCP 侧（每次单独调，读 `原字符数`）：`文件系统支持库.内容检索.正则搜索(根目录=<绝对>, 模式=_归一化路径族参数, 文件模式='*.py', 最大匹配数=50, 上下文=0|2)`。

---

## 六、一句话交接

「子代理绕过 MCP」的真身不是「没能力」，是**工具面没告诉它该用哪个**（§1.4）；本批按既有动态段机制把「常见意图→首选能力」写进 `工具清单.py`，并顺手把超预算的注入面（2,123/2,000）收回 1,994；参数面经 3/3 对照确认**不缺**，故**零新增能力、零新增参数**。
