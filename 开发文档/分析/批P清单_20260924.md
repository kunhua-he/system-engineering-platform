# 批P 清单（第一轮：找全问题 · 4 路只读子代理 + 主会话活体实证）

> 状态：待执行 ｜ 生成：2026-09-24 09:00 ｜ 开工ID：`开工-20260924-091605-fe7f`
> 上游：`开发文档/未完成事项.md` §7.12（批O 登记 O-10…O-18）
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
> 口径：哲学 12.1（三轮：找全 → 证真+落点+排序 → 一次性全修）／12.2（一次性盘清、一次性全派）／12.3（单任务 ≤10 分钟）／14.1（归因三分）
> 说明：本件是**第一轮产物**，只负责「找全问题 + 留下可复跑的判据」；维修落在批Q 清单。

---

## 一、本批前置（已完成，带读数）

| 项 | 读数 |
|---|---|
| 制品重建 | `自修复工具.构建平台客户端制品(安装=真)` ⇒ 制品摘要 **7157cdb95cd5e9b4**、可重复构建校验**通过**、入库+激活 |
| 制品新鲜度 | **已转绿**（落后提交 0，来源提交 = HEAD `0d5be150`）—— 批O 的 O-18「活体未生效」**已消除** |
| 网关重启 | PID **71719 → 82820**、就绪 3.05s、能力数 752、装配告警 **0**、**运行态同根=真** |

---

## 二、P 系列（新发现，本批 4 路只读 + 主会话活体）

### P-1 ★ 阻断级 · 真缺口 29 条（主会话活体实证）

| 项 | 内容 |
|---|---|
| **现象** | `自修复工具.仓库锁 动作=查看` 实测 **未锁 29 / 缺口 29 / 开窗 0** —— 批O 收尾自报「缺口 0」 |
| **实测序列** | ① 我认领 `开发文档/分析/批P清单_20260924.md`（触发懒回收）；② `查看` ⇒ 缺口 **29**；③ `补齐` ⇒ `新锁数 29`、`跳过开窗数 0`、`锁掉开窗数 0`；④ 复 `查看` 应回 0 |
| **命中清单** | 8 个**目录**（`开发工具`、`开发工具/MD文档生成/说明书`、`开发工具/依赖派生`、`开发工具/依赖生命周期审计`、`开发工具/发布门禁`、`开发工具/契约编译`、`开发工具/组件合规`、`开发工具/验证门禁`）+ 21 个文件，**全部落在批O 改过的路径族** |
| **因果（租约账实证）** | 那批路径的租约 `所有者=主会话-批O收口2`、`状态=已过期`、`释放原因=自动回收：过期时间 2026-09-24 08:51:04 已到；心跳停摆超过 900.0 秒未续租`、`释放时间=2026-09-24 08:55:02` |
| **机制** | 认领时 `解锁供写入(目标 + 父链)` 开窗 → 租约到期（08:51:04）→ **回锁只在「下一次认领触发懒回收」时才发生**（08:55:02）⇒ 这 4 分钟窗口里 `查询()` 把「锁开着、租约已过期未回收」算成**真缺口** ⇒ `pre-commit` 按判据一**拒提交** |
| **定性** | 这是 O-17「口径冲突」的**活体第三态**（前两态只有夹具不可达）：既不是「别人正在用的窗口」（N-7 挡的那种），也不是「写腿中途被杀」（真缺口），而是**第三类：已过期、待回收的窗口** |
| **落点** | `公共契约/运行时/仓库只读锁.py` `查询()` / `活跃租约开窗集()`（`:919`，判据用 `是活跃`＝状态活跃**且未过期**）；`平台控制面/能力目录/实现/文件租约维护.py` `回收过期文件租约`（懒触发） |
| **修法（候选，须选一）** | ① 开窗判据从 `是活跃` 改为「盘上活跃」（未回收即算开窗）—— 与 `回收过期文件租约` 的入口门同口径；② 把懒回收挂到 `查询()` 腿上（查询即顺手回收）。**两个都要防「降级成永久开窗」**：① 的前提是回收腿真的会跑 |
| **判据** | `capability_call(能力id="自修复工具.仓库锁", 参数={"动作":"查看","项目根":"<abs>"})` ⇒ 期望 `缺口数 0`（在无活跃租约时） |

### P-2 ★ 反向条款级 · 参数名写错仍被静默剔除（Ohm 实证）

| 项 | 内容 |
|---|---|
| **判定** | **纸面（未落地）**。批H 裁-2 已裁 fail-closed，但源码仍是「算出差集直接丢弃」 |
| **活体读数** | `capability_call(能力id="文件管理.读取文件", 参数={"文件路径":…, "路径":"x", "限定行数":1})` ⇒ **成功=true**，回带 `被忽略参数: [{"参数名":"路径","最接近的契约参数":["文件路径"]}, …]` |
| **落点（3 处）** | ① 主：`运行核心/统一网关/协议/类型规格.py:162` `_ = sorted(set(参数) - 参数名)`；② 例外表：`运行核心/统一网关/核心处理/网关参数校验面.py:52` `横切参数名 = frozenset({"项目根"})` 必须同批传入，否则薄壳自动补位的 `项目根` 会把每个未声明它的能力判红；③ 两腿：`网关执行面.py:135`（同步）/ `:222`（任务提交） |
| **唯一节点要求** | 相似度判据**不得新写第二套**：`近似名阈值 = 0.34` + `difflib.get_close_matches` 现住 `网关参数校验面.py:35`，应抽成唯一节点后两处共调 |
| **须裁决** | 与 `类型规格.py:158-160` 逐字登记的「未知参数一律忽略」（哲学第 5 条 2 项）**正面冲突** ⇒ 这不是纯 bug 修复，是**口径反转** |

### P-3 ★ 真 bug · 注册字面量丢 `默认值` ⇒ 异步腿守卫对 64 条能力失效（Ohm 实证）

| 项 | 内容 |
|---|---|
| **现象** | 契约声明了非空默认值、而注册字面量缺 `默认值` 键 —— **64 条** |
| **分布** | `数据操作支持库/数据集合` 13、`测试支持库` 9、`系统核心支持库/进程管理` 5、`环境变量` 5、`文本补丁` 5、`内容检索` 5、`表格文档` 5、`演示文稿` 4、`PDF文档` 3 … |
| **行为后果（不是文档问题）** | `_同步腿拒绝说明` 的生效值口径 ②「没传、但契约声明了默认值 ⇒ 用默认值」**读的正是注册表 `.参数`** ⇒ 这 64 条里凡 `超时秒 ≥ 10` 的能力（`解析PDF`/`解析表格文档`/`执行命令集`/`沙箱执行命令`）在活体上**绕过同步腿守卫** ⇒ 「须走异步腿」这条裁决对它们**等于没落地** |
| **活体佐证** | `执行命令` 显式传 `超时秒=600` 被拒（`须走异步腿`）；**不传**时却成功 —— 因 `进程管理/__init__.py:13` 注册字面量写 `{'名称':'超时秒','类型':'双精度数型','必填':假,'说明':'超时'}`，**无 `默认值`** |
| **根因** | 93 个「生成形态」`__init__.py` + 35 个「手写形态」并存；`开发工具/契约编译/能力定义编译器.py:430-441` **有**生成 `默认值` 的能力（注释明写「参数表必须带全四要素」）⇒ 标准修法是**重跑编译器**，不是 64 处手补（避免又造第二套回填脚本） |
| **须裁决** | 是否授权「重跑能力定义编译器批量回填 64 处」 |

### P-4 ★ 真 bug · 聚合 `包声明.json` 与子包 `参数契约.json` 类型漂移（12 条）

同一参数名、同一库、**两种类型口径**：`系统核心支持库.进程管理.启动进程.超时秒`（聚合=整数型 vs 子包=双精度数型）、`沙箱执行命令.输出上限字节`、`大语言模型支持库.模型连接器.连接LLM.超时秒` … 共 12 条。活体后果：`执行命令.超时秒` 传整数即 `参数不合法`，而同一库的 `启动进程.超时秒` 在聚合声明里是整数型。

### P-5 ★ 阈值三处不一致（应收口为一处）

| 处 | 值 | 落点 |
|---|---|---|
| 代码（判据） | **10**（含 10 拒） | `运行核心/统一网关/核心处理/网关参数校验面.py:63` `同步腿超时上限秒 = 10.0` |
| 工具面提示词 | **10** | `开发工具/薄壳/工具清单.py:94` |
| 通用纪律 | **15**（两处） | `任务类型规范.json` 工具姿势 `[26]`、`[45][46]` |

**修法**：按「代码是唯一事实」统一为 **10**，纪律那两处改成引用代码节点，不另写数值。

### P-6 ★ 真 bug · 存量基线收敛计算漏桶 ⇒ 「该降未降」永远发现不了（Singer 实证）

| 项 | 内容 |
|---|---|
| **落点** | `开发工具/公开调用完整性门禁_检查与判定.py:242` `for 键, 条们 in 桶.items():` —— **只遍历「现场出现的桶」** |
| **后果** | 基线里**有、现场一条都没有**的桶，循环根本不进 ⇒ 永不进收敛 ⇒ **收敛提示根本不响** |
| **复现（双证）** | 单元：基线 `{A|X:3, B|Y:2}`、现场 `[]` ⇒ 返回 `收敛=0`（应为 2）。真仓：原始违规 0 条、基线 7 桶 20 条 ⇒ `收敛=0`（应为 7 桶） |
| **兄弟实现是对的** | `开发工具/契约编译/漂移检测.py:243` 用 `for 键 in sorted(set(桶) \| set(上限表))` ⇒ 同一输入返回 `收敛=2` |
| **定性** | 同一职责（存量基线消费）**两份实现分叉、一对一错** —— 哲学 1.2「同一件事两套实现即缺陷」的活标本 |
| **修法** | 收敛计算收成**唯一节点**（取正确那份的形状），两条腿共调；或直接把错的那处改成 `set(桶) \| set(上限表)` 并抽公共函数 |

### P-7 · 6 条存量基线该升/该降（Singer 实测，含应收敛值）

| 载体 | 现在 | **应收敛到** | 依据 |
|---|---|---|---|
| `开发工具/发布门禁/运行发布门禁_防回潮公共.py:274` 裸布尔标量 | 374 | **335** | 自身注释「与该桶 `条目数` 是同量，必须相等」；分桶=335 |
| `开发文档/项目证据/能力id冻结基线.json:6` `能力数` | 751 | **752** + 补条目 `自修复工具.仓库锁`（指纹 `324c57660a18bde2`） | 现场 752，消失 0（=O-16） |
| `开发工具/参数口径基线.json:178` `能力数` | 675 | **690** | 现场扫全仓=690 |
| `开发文档/项目证据/第三方导入分布基线.json:6` `文件数` | 1212 | **1281** | 现场 1281 个 `.py`（非判据字段） |
| `开发工具/公开调用完整性门禁存量基线.json:6` `条目` | 7 桶 20 条 | **`{}`** | 现场原始违规 0 条 |
| `开发工具/契约编译/参数可用性体检.py:259` 路径口径 `存量基线` | 103 | **0** | 现场冲突 0（批C 已清零源，未同步下调基线） |

### P-8 · `全枚举门禁.py` 未接统一骨架（第 5 条腿基线机制缺位）

`开发工具/验证门禁/全枚举门禁.py` **不消费** `验证门禁/存量基线.json`（该文件只有 4 段，无本门禁段）、**不走** `门禁公共.跑标准主流程` / `写基线段`，自建 `主函数` 直接 `return 1 if 结论.判定红 else 0`；其余四个检查器全部 `跑标准主流程`。现场命中 0 故不红，但基线机制在这条腿上缺位。

### P-9 · 生成侧把机器绝对路径写进产物（Pascal 全扫，O-10 扩面）

| 文件:行 | 生成产物 | 写进什么 |
|---|---|---|
| `支持库/后端/组件规范支持库/实现/支持库模板生成器.py:324` | 新建支持库 `实现/提供者.py` | `系统根 = "@@系统根@@"` ⇒ 渲染成**生成期源码树绝对路径** |
| 同上 `:527` | 新建支持库**测试骨架** | `for 目录 in ("@@库父目录@@", "@@系统根@@")` 两处都是当次机器绝对路径 |
| 同上 `:599`/`:615` | （渲染实参） | `系统根=系统根` / `库父目录=输出目录.parent` —— 上面两行的注入点 |
| `…/实现/模块模板生成器.py:374` | 新建模块测试骨架 | `系统根 = Path(__file__).resolve().parents[2]` —— **第二套根判据**（层数写法） |
| `…/实现/待作者补清单.py:110`/`:120` | `<包目录>/待作者补.md` | `python3.14 开发工具/包体检.py {绝对包目录}` —— 随包入制品**且计入指纹** ⇒ 换机即指纹漂移 |

### P-10 ★ 底座缺口 · `显示相对路径` 零消费方（Pascal 的核心判断）

全平台只有两个相关权威节点：
- 取根：`公共契约/运行时/导入前缀.取系统根`（**有大量消费方**）
- **相对化**：`系统核心支持库.路径安全.显示相对路径`（实现 `路径安全.py:181`）—— 全仓 `rg` 实测**零消费方**

⇒ 「写进产物前先相对化」**没有任何强制点**，生成侧 5 个落点 + 存量 8 份产物都从这个缺口漏出去。**这是 P-9 那一整根轴的根因**。

### P-11 · 存量产物已进制品树（8 份，非 .py 逐字节原样保留）

| 文件:行 | 内容 |
|---|---|
| `技能库/技能/能力上架自检/验证声明.json:7,20,45,58` | 用例 `包目录` 写死机器绝对路径（`参数` 与 `预期` 两处强耦合，全仓无 runner） |
| `支持库/适配层/Swift编译器提供者/复用决策.json:68` | `"根目录"` 绝对路径（属断言/决策面） |
| `支持库/适配层/浏览器自动化提供者/验证场景引用.json:18` | 断言期望值 `/Users/…/.local/bin/uvx`（=O-11），来源是 `Path.home()` 推导 ⇒ 换用户名即不等 |
| `开发工具/性能基准/结果/基准结果-20260918-182155.json` | 19 处运行数据根绝对路径 —— **属实测证据，不改**（`.gitignore` 明写「仓内只保留一份对照锚点」） |
| `开发工具/消费者登记/__init__.py:21`、`开发工具/验证门禁/接线说明.md:11`、`运行核心/加载器/版本系统/说明/灰度口径.md:112`、`开发工具/git钩子/pre-commit:86` | docstring / 文档里的 `cd` 示例 |

**制品保留结论（实测）**：非 `.py` 文件**逐字节原样保留**（4 份源 vs 制品 sha256 逐一相等）；唯一重写器 `客户端/导入重写器.py` 是 `ast.NodeTransformer`，**只改 `.py` 的 `import`**，对 `.json/.md` 无能为力。制品内 `/Users/` 命中 23 行 / 8 文件。

### P-12 ★ 真 bug · 硬编码解释器无回落（Schrodinger 判为「本批唯一真机器绑定的可执行缺陷」）

`模块库/自修复工具/实现/效率巡检.py:42`、`开发编译口自检.py:41`、`客户端构建.py:43` 三处 `解释器 = "/opt/homebrew/bin/python3.14"` **作为 `argv[0]` 直用、无回落**；而平台已有唯一节点 `公共契约/运行时/平台适配/解释器.py` + `提供者隔离/独立进程_启动调用面.py::_确定解释器`；对照 `pre-commit:110` 有 `command -v` 回落 ⇒ **两条腿**。

### P-13 ★ 真 bug · 技能侧路径口径与已裁口径冲突

`技能库/技能/能力上架自检/SKILL.md:9` 写「绝对路径优先；相对路径按当前工作目录解析」，而 `公共契约/基础类型/字段名册.py:256` 已裁「基准路径字段名 唯一口径」。**实测传相对返回相对**（未按生效根归一）⇒ 契约漂移（落点在 `能力上架自检.py:148`）。

### P-14 · 其他登记（低优先 / 须标注）

- **孤儿基线 3 个**（`rg` 零消费方）：`发布门禁143项基线_20260918.json`、`底座升级/B0-浏览器自动化基线.json`、`性能基准/结果/基准结果-20260918-182155.json` ⇒ 建议标注「存档，非判据」。
- **`说明书白名单.json:24` 计数段未被消费**：`白名单合计`=42 / `人工类`=133，明细实测 149 / 130 ⇒ 口径漂移（非判据）。
- **`技能库/技能/验证编排/验证声明.json`**：`~/…` 家目录写法（非本机绝对路径，可移植），但与「基准字段=绝对路径」口径不一致 ⇒ 观察项。
- **测试中心 7 文件写死机器绝对路径**（`测试_模型连接器探针.py:20` 等）—— 属 `.py` 轴，制品树无 `测试中心`，不落制品。
- **`全仓 parents[N]` 共 408 处**（测试中心 242 / 开发工具 75 / 支持库 30 …）：`测试_导入前缀` 的 population **只覆盖后端源码的自举段、不覆盖 `测试中心/`** ⇒ 分叉可长期存活。

---

## 三、O 系列承接（批O 未修 9 条的现状）

| # | 批O 原判 | 本批核实 |
|---|---|---|
| O-10 | 待修 | ✅ 仍成立，**已扩面为 P-9**（多出 `:527` 测试骨架 + `模块模板生成器.py:374` + `待作者补清单.py`） |
| O-11 | 须裁决 | ✅ 仍成立，**已并入 P-11**（实测属断言期望值，且 `单步场景.py:88` 允许字段集**不含 `$动态`** ⇒ 现无合法改法） |
| O-12 | 须裁决（`组件规范.py:119` 越层） | 未纳入本批（依赖防火墙豁免表改动，单独裁） |
| O-15 | 对齐 374 | ✅ **已并入 P-7**（应收敛到 335） |
| O-16 | 走 `--冻结` | ✅ **已并入 P-7**（应收敛到 752） |
| O-17 | 须裁决（16 条结构性不可达） | ✅ 升级为 **P-1**（活体第三态实证）+ 仍缺「当前写者是谁」节点 |
| O-18 | 制品未重建 | ✅ **已消除**（本批已重建+重启，制品新鲜度转绿） |

---

## 四、须裁决清单（按审计哲学自审：哲学是否够裁）

| # | 事项 | 哲学能否自裁 | 结论 |
|---|---|---|---|
| 裁-1 | P-2 fail-closed 是否推翻「未知字段一律忽略」 | **不能** —— 反向条款是逐字落盘的哲学第 5 条 2 项 | **留给华哥** |
| 裁-2 | P-3 是否授权「重跑能力定义编译器批量回填 64 处」 | **能** —— 哲学 1.2「不保留旧腿 / 同一件事两套实现即缺陷」+ 7.9「能不能派生」 | **自裁：授权重跑编译器**（不手补 64 处） |
| 裁-3 | P-1 开窗判据改成「盘上活跃」还是「查询即懒回收」 | **能** —— 哲学 1.2 判据一处 + 8.3「收成一处」 | **自裁：取「查询即懒回收」**（判据本体不动，把已有的回收腿挂到查询点上；改判据风险更高，会让过期窗口永不回收） |
| 裁-4 | P-5 阈值统一为 10 | **能** —— 哲学 5.1「契约唯一」+ 代码是唯一事实 | **自裁：统一为 10** |
| 裁-5 | P-6 收敛计算收成唯一节点 | **能** —— 哲学 1.2 | **自裁：收成唯一节点，取兄弟正确形状** |
| 裁-6 | 11 存量产物（P-11）是删断言还是改写法 | **不能** —— 涉及松开断言/扩允许表，属口径变更 | **留给华哥**（但 P-11 第一条实测「现无合法改法」） |
| 裁-7 | P-10 是否新建「写进产物前相对化」强制点 | **能** —— 哲学 13.3 四环链（缺 ④门禁 即纸面） | **自裁：接线，不新建能力** |

---

## 五、本件边界（如实留档）

- 4 路只读子代理**均未改任何文件、未写租约、未编译、未提交**（`git status --porcelain` 前后为空）。
- P-1 的 29 条**未逐条复原归属**（与 D3 旧账同处境），因果结论靠「租约账记录 + 所有者 + 过期/回收时间戳」三证，不靠推断。
- P-9/P-10/P-11 的行号来自子代理**工作树实测**（工作树 == HEAD `0d5be150`，0 行改动）。
- **未跑**：完整发布门禁整轮（111–132 秒）；`测试体系门禁 --真跑`（非只读档位）。
- **未定性**：`核心快照模板.py:111` 把绝对快照目录写进 `激活指针.json`（落 `工程缓存/`，灰区）；`技能库/…/验证声明.json` 的 `/tmp/验证编排验收/` 是否算本机专属。
- 本件是**分析类**件，条目销账后按分析类口径删除。
