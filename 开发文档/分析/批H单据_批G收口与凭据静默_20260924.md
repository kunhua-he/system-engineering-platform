# 批H 单据 · 批G 收口验收 + 批H 清单（2026-09-24）

> 开工ID：`开工-20260924-014152-2f92`（批G 派活）｜本件登记：`开工-20260924-021527-acc8`
> 口径：本件是**派活单据**；结论均带实测命令或现场输出，不新增未取证结论
> 上游：`开发文档/分析/批G单据_路径腿唯一化_20260924.md`｜债务登记：`开发文档/未完成事项.md` §四
<!-- 机器管理｜类型：分析｜生成器：人工 -->

---

## 一、批G 验收（实测）

| 层 | 目标 | 实测结果 |
|---|---|---|
| L1 契约参数说明 | 103 → 0 | 40 包定向 `静态编译` 跑 `能力定义编译` **40 节点 红 0**（派生件已重生成）；G-1 改 76 条 / G-2 改 27 条 |
| L2 非派生文案 | 9 → 0 | 4 文件 9 处（`README.md` 5 / `多会话并行开发规约.md` 1 / `开发支持库_细则.md` 1 / `新建技能包指南.md` 2）；基准类逐字未动 |
| L3 自写拼根 | 38 → 0 | G-3 改 15 处 / G-4 改 13 文件（含 5 处同族补收）；残留项逐条带边界理由 |
| L4 自制绝对性判据 | 8 处 | **本批不修**（等裁决，见 §二 裁-1）→ 转批H 第 15 条 |
| L5 自制生效根 | 3 → 0 | G-4 收口；`技能库` 两处够不到唯一腿 → 批H 第 9 条 |

**编译口实测（唯一编译入口，均经 MCP）**

- `静态编译 类型=0`（全仓）：`成功 17 / 失败 2`；红 = `拒绝手写md`（假红，已收口）+ `md查重`（只报告）
- `静态编译 类型=1`（40 个包 id）：`成功 17 / 失败 2`；**步骤1 受影响包 40**、`契约版本自增` 40 节点红 0、`能力定义编译` 40 节点红 0、`点名包内 .py 语法编译` 216 个通过、**步骤4 复跑确认 转绿 10 条 / 仍红 0**
- 收口后 `python3.14 -m 开发工具.MD文档生成 --拒绝手写校验` → **退出码 0**（改动 md 9 份 / 凭据 9 命中）

**派活回执**：G-1（34 包 76 条）/ G-2（6 包 27 条 + 9 处）/ G-3（14 文件，11 改 3 边界）/ G-4（13 文件）；四路租约均已 `收工释放`，未 `git add`、未提交。

---

## 二、两条裁决（按设计哲学自裁，报华哥知悉）

**裁-1：绝对性判据的唯一节点不是 `解析路径`，而是 `校验相对路径文本`。**

- 唯一**判据**节点 = `支持库/后端/系统核心支持库/路径安全/实现/路径安全.py::校验相对路径文本`（自述「全平台路径边界判定的唯一节点」，已判 ①URL 解码 ②反斜杠 ③盘符 ④`~` ⑤POSIX 绝对 ⑥`..` ⑦整串 `.` ⑧段内 `.`）。
- `公共契约/运行时/平台适配/路径解析.py::解析路径` 是**映射**节点（相对→绝对），不是判据节点 ⇒ 两者职责不同，**不构成两条腿**（哲学 8.2 唯一性判定）。
- 故 8 处自写绝对性判据**一律删除并转调 `校验相对路径文本`**（其中 `平台控制面/包仓库/路径安全.py:67` 紧邻 `:69` 已在转调 ⇒ 纯残留重复）。
- **不采用**「把 `解析路径` 改宽认盘符」：那会把它从映射节点改成判据节点，等于新开一条腿，且让「映射」与「拒绝」两种语义挤进同一个函数。

**裁-2：L4 圈外约 10 处的入批判据 = 「是否回答同一个问题」。**

- 回答「这条路径是不是合法的项目根内相对路径」→ **入批**，转调唯一判据节点；
- 回答别的语义（基准类必须是绝对、`is_relative_to` 越界判、`sys.path` 引导）→ **不入批**，但按哲学 1.8「例外必须自证」逐条登记理由。

---

## 三、批H 派活切分（不相交写集）

| 路 | 写集 | 活 |
|---|---|---|
| H-1 | `平台控制面/能力目录/租约路径.py`、`平台控制面/包仓库/路径安全.py`、`支持库/后端/组件规范支持库/实现/核心快照模板.py` | 删自写绝对性判据、转调 `校验相对路径文本`（L4 前三处） |
| H-2 | `支持库/后端/系统核心支持库/路径安全/实现/路径安全.py`、`开发工具/HTML验证/路径安全.py`、`运行核心/依赖防火墙.py`、`模块库/开工编排/实现/开工编排.py`、`模块库/项目文档/实现/项目边界.py` | L4 后五处（含 `开工编排` 同一函数两条判据的自相矛盾收口） |
| H-3 | `支持库/后端/文件系统支持库/文件操作/实现/文件系统.py`、`支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py` | 凭据登记失败留痕（不许静默）+ `相对路径` 口径唯一化 + 批量相对路径解析不一致 |
| H-4 | `平台控制面/能力目录/实现/文件租约申请.py`、`运行核心/加载器/包安装/入口路径.py` | `锁问题` 回带进 `开工即占` 回执；`定位系统根` 第二份实现收口（先定 fail-closed 口径） |

**完成判据**：`静态编译 类型=0` 无新增阻断红；L4 八处代码级 `is_absolute()` / `isabs` 命中 0（合法例外逐条带理由）；`拒绝手写校验` 退出码 0。

---

## 四、批H 待办清单

逐条见 `开发文档/未完成事项.md` §四（本件落盘同批登记 15 条）。

---

## 五、本件边界

- 未跑 `动态编译`；L3「行为是否真在生产路径上被走到」仍未实测（与取证件同一标注）。
- 本件是**分析/单据类**件，条目销账后按分析类口径删除。
