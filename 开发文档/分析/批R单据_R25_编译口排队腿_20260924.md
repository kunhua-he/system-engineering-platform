<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-25 单据：编译口有界排队腿 ＋ 异步腿不再被「连接寿命」压死

> 开工ID `开工-20260924-130139-bfb8`（写集 3 条：编译口 / 网关转发 / 本单据）＋ `开工-20260924-130337-e2e5`（用例 1 条）
> 取证时间 2026-09-24 13:00–13:12｜HEAD `268e2565`（取证期间被**并发会话**推进过一次：12:59 时 HEAD＝`7f464a98`）
> 未跑 git 提交（收尾由主会话统一做）；**未做项见 §六**

## 一、根因：两件其实是一件事

| # | 现象 | 根因节点（逐级） | 现场证据 |
|---|---|---|---|
| 1 | 编译口「同树已有写者 ⇒ **拒启**」，活被弹回调用方 | `开发工具/开发编译口/编译口.py` 的 `起单写者守卫`（改前 :1427 附近）：`try: 句柄 = 占单写者锁(根)` / `except (锁被占, 锁不可判定): … return None, 真` —— **只有 fail-closed 拒启，没有排队腿** | R-19 实测：**4 次拒启 ＋ 1 次 120 秒超时，两个读数根本没取到** |
| 2 | 异步任务跑到 **120.0 秒被硬杀** | `开发工具/薄壳/网关转发.py`：`默认超时秒 = 120.0`（薄壳自身连接寿命）→ `转发()` 无条件 `载荷.setdefault("调用方超时秒", 默认超时秒)` → `运行核心/统一网关/本地网关.py:464` `超时秒 = min(超时秒, float(调用方超时原值))` → `运行核心/统一网关/核心处理/网关执行面.py` 的 `任务提交` 分支把它当 `任务系统.提交(超时秒=请求.超时秒)` 传下去 → `运行核心/任务调度/任务进程.py:305` `任务对象.截止时刻 = monotonic() + 超时秒` → `运行核心/任务调度/任务进程_监视轮询面.py:126-127` 回「任务执行超过 120.0 秒」 | **本轮实测**（§三 判据 1） |

**为什么必须同批改**：排队腿把「拒启」换成「**等**」，而「等」要吃任务预算。外层 120 秒不动，就会**等到一半被掐死** —— 本轮现场正是这样被掐死的（§三 判据 1 的副产物）。**单改任一条都会让另一条变成新瓶颈。**

## 二、改了哪些文件（3 代码 ＋ 1 用例 ＋ 本单据）

### 2.1 `开发工具/开发编译口/编译口.py`
- 新增常量：`排队上限环境变量="编译口排队上限秒"`（:1186）、`排队上限秒=900.0`、`排队轮询秒=2.0`、`不可判定容忍环境变量="编译口不可判定容忍秒"`／`不可判定容忍秒=6.0`（:1197）
- 新增函数：`_排队上限()`（:1355）、`_不可判定容忍()`（:1372）、`_排队占锁(根, 上限秒, 打印)`（:1388）
- `起单写者守卫`（:1427）：拒启路径改成「**排队等 → 等到上限才拒启**」，调用点 :1444-1445；拒启说明由「同树已有编译口在跑（PID …）」升级为**带已等多久 ＋ 建议**
- `锁陈旧秒=3600` 一字未改（陈旧锁仍可夺、仍如实报）

### 2.2 `开发工具/薄壳/网关转发.py`
- 新增 `同步腿操作 = frozenset({"调用能力"})`（含 6 行理由注释，与 `默认超时秒` 注释块同处）
- `转发()`：`调用方超时秒` **只对同步腿注入**（`if str(载荷.get("操作") or "调用能力") in 同步腿操作:`）
- `默认超时秒=120.0` 仍是 urlopen 超时（薄壳自身寿命，**未动**）

### 2.3 `测试中心/开发工具/测试编译口单写者守卫.py`
- `import threading`；新增 `_钉住环境键(键, 值)`（钉法同 `_钉住非嵌套`，记原值、还原）
- `活锁必须拒启`：子进程环境钉 `编译口排队上限秒="0"`（否则会等满缺省 900 秒 ⇒ 用例挂死）
- `判不出来一律拒启._拒启取证`：钉 `编译口不可判定容忍秒="0"`（否则每条坏锁用例白等 6 秒）
- **新增类 `活锁排队等到就放行`（2 例）**：① 持锁者释放后排队者拿到锁（断言真等过 ≥1000ms、输出含「排队等待中」）② 等到上限才拒启且说明含持锁者 PID／启动时刻／已排队等待／建议，且**排队者没动别人的锁**（锁态一致）

## 三、四条判据读数

### 判据 1（真并发：一个持锁、另一个调编译 ⇒ 第二个**等到**并成功）—— ✅ **现场真并发**（非构造，强于构造）

取证链（全部经 MCP，无终端）：
- 13:05:32 提交 `自修复工具.动态编译(类型=1, 模块ID=开发工具/开发编译口/编译口.py, 源码根=本仓)` → 任务id `28f1f06d253f488d`
- 13:06:11 读 `工程缓存/编译口单写者锁.json`：持锁者是**别的会话**（PID 24207，命令行 `--不跑点名 --文件 支持库/适配层/Tesseract提供者/…`）⇒ 我的编译口**在排队**
- 13:06:35 再读同一把锁：持锁者变成 **PID 21968，命令行 `--文件 开发工具/开发编译口/编译口.py`**（＝我本次的编译口）
⇒ **我的编译口等了约 62 秒后拿到锁并开跑**。**改前这一步就是「拒启」**（旧代码 `except 锁被占: return None, 真`），读数根本拿不到。

### 判据 1 副产物（也是判据 2 的**真实现场**）：外层 120 秒把等待中的任务掐死
- 13:07:33 任务查询：`状态=超时`、`错误说明="任务执行超过 120.0 秒"`、创建 13:05:32 / 完成 13:07:33 —— **正好 120.0 秒**
⇒ 与 R-19 那条「1 次 120 秒超时」**逐字同形**，根因即 §一 第 2 行那条链。**这就是「两条必须同批」的实证。**

### 判据 2（超时路径：等到上限 ⇒ 拒启且说明含持锁者身份）—— ⚠ **用例已落地，读数待复测**
- 用例 `活锁排队等到就放行.test_等到上限才拒启且说明含持锁者` 已断言四件：`PID {本进程}`／`启动于 `／`已排队等待`／`建议`，并断言 `≥600ms`（真等到上限）且 `<30s`（不挂死）
- **读数未取到**：见 §六.1（复测被外层 120 秒上限与并发写者卡住）
- 说明文案（代码逐字，供人核对）：`{原锁被占说明}；已排队等待 {已等:.0f} 秒（上限 {上限:.0f} 秒）仍未拿到锁。建议：① 等它跑完再重跑；② 确认它已死则重跑即自动夺陈旧锁；③ 要等更久就把环境变量 编译口排队上限秒 调大（当前上限 {上限:.0f} 秒）`

### 判据 3（不引入泄漏）—— ⚠ **部分读数**
- **无孤儿进程**：等待期间排队者**不持有任何句柄**（`_排队占锁` 只在循环里调 `占单写者锁` 与 `time.sleep`，失败即不建锁），故被外层掐断也留不下子进程
- **锁态一致**：掐断现场读锁文件仍是我那把（PID 21968，**已死**）⇒ 留一把**陈旧锁**，下一写者按「原 PID 已不存在」**自动夺并如实报**（这是既有设计，非本轮引入）
- **未取的读数**：`进程列表` 能力**没有 `筛选` 参数**（调用被网关剔除，回全量 910 条、值被裁剪成只剩 `进程数`）⇒ 「无孤儿编译口进程」这一条**未拿到硬读数**，已登记（§六.3）

### 判据 4（不传新参数时行为与改前一致）—— ✅ 有例外，且例外是**有意**的
- 不传新参数、不设环境变量时：`占单写者锁`／`释放单写者锁`／`锁陈旧秒`／夺陈旧锁判定／`--只读`／嵌套短路／退出码 2 的拒启形状**一字未改**
- **有意变更两条**（都是本任务的目的）：① 锁被**活写者**占 ⇒ 由「立刻拒启」变「排队等（缺省 900 秒）」，到上限才拒启；② `锁不可判定` 由「立刻拒启」变「容忍 6 秒重试再拒启」——后者收的是**既有竞态**：`占单写者锁` 走 `O_CREAT|O_EXCL` **先建空文件再写内容**，并发第二者在这个微窗口读到空文件会被判 `锁不可判定` 而**假拒启**

## 四、异步上限取值与依据

- **不删超时**（华哥口径「资源无限、只要回收干净」不等于「无限等」——无限等是泄漏温床）
- 排队上限缺省 **900 秒**：依据 `R-22` 实测 `自修复工具.静态编译` **max 90,220ms** ＋ 编译口注释自述「单轮实测 2~4 分钟」⇒ 900 秒覆盖「前面排 2~3 轮」
- **比它更紧的外层边界**：`自修复工具.静态编译` 契约 `超时秒` 缺省 **600**（`能力详情` 读得）、资源预算 `单次调用超时 1800`；两者取小即真实等待上限
- 可覆盖：`编译口排队上限秒`（传 `0`＝不排队＝旧行为，门禁与既有用例靠它钉住）
- **异步腿侧**：`调用方超时秒` 不再注入 ⇒ 任务预算回到能力契约自己的 `超时秒`（静态编译 600 秒），**有界但够用**

## 五、编译读数（本轮已取到的）

| 轮次 | 命令 | 读数 |
|---|---|---|
| 1 | `动态编译(类型=1, 模块ID=测试中心/开发工具/测试编译口单写者守卫.py)`（**未传 源码根** ⇒ 跑的是**制品里的旧编译口**） | 成功 **19** / 失败 **1** / 检查项 20，五态＝**只报告**，退出码 0；唯一失败项＝`制品新鲜度`（只报告：制品来源提交 `777147f6` ≠ HEAD，落后 4）⇒ **无阻断** |
| 2 | `动态编译(…, 模块ID=开发工具/开发编译口/编译口.py, 源码根=本仓)` | **超时**（120.0 秒，见 §三 判据 1 副产物）—— 排队 62 秒 ＋ 编译未跑完 |
| 3 | `动态编译(…, 模块ID=测试中心/开发工具/测试编译口单写者守卫.py, 源码根=本仓, 明细级别=全量)` | 任务id `cc47c17f9eb84c15`，提交于 13:08:01 —— **读数见本单据落盘后的补记** |

## 六、未做项（逐条带原因与出口）

1. **测试读数未取全（判据 2/3 的硬读数）**：三条路都被堵 —— ① 编译口单写者锁被**并发会话**持着（PID 24207 在编 `支持库/适配层/Tesseract提供者/**`，即另一会话正在推 T1）；② 排队会吃掉外层预算；③ 外层预算是**薄壳进程里的旧代码**注入的 120 秒（本会话启动时载入）⇒ **本会话内改不掉**。
   **出口**：本批改动落盘 ＋ 重建制品 ＋ 重启网关 ＋ **MCP 重连（薄壳换新代码）**后，重跑 `动态编译(类型=1, 模块ID=测试中心/开发工具/测试编译口单写者守卫.py, 源码根=<仓库根>)`，看 定向测试 一栏。
2. **`开发文档/未完成事项.md` 第 5/8 条未更新**：该文件被 `开工-20260924-125339-55da`（R-20）持租约至 `13:08:39`，本会话 3 次 `开工即占` 全部**整体失败**（批量认领不许静默跳过）。替换文本见 §七，租约一放即可一次替换。
3. **新登记的底座缺陷（本批发现，未修）**：
   - `开工编排.开工即占` **失败信封**也带全量 `开工上下文`（实测 8,555–9,391 字符），且**内容在 `详情` 里而不在 `值` 里** ⇒ 薄壳的 `值字段` 投影**对它完全无效**（回「值字段未生效：值不是对象（NoneType）」）。成功信封同样问题：一次 12,493 字符、`租约id清单` 被字典丢尾键丢掉 ⇒ 调用方还得去翻 `MCP全量回执` 才拿得到租约 id（本轮实际发生过）。
   - `系统核心支持库.系统信息.获取进程列表` **没有 `筛选` 参数**（`筛选` 被网关剔除）⇒ 想查「有没有孤儿进程」只能拿全量 910 条、值再被裁成 `进程数` ⇒ 「回收干净」这条**判据在能力面上不可达**（本轮判据 3 因此只拿到部分读数）。
   - 租约查询的 `内容指纹` 里 `已声明指纹` 为空时仍回 `指纹一致=false`（本轮两条实测：编译口 `sha256:6a2566b8…`、网关转发 `sha256:2639d262…`，均 `已声明指纹=""`、`指纹一致=false`）—— 语义上应是「**未声明 ⇒ 未核验**」，回 `false` 会被读成「内容变了」。
4. **T1（能力面两条腿）本轮未动**：现场另有会话在做（锁文件实证：`--文件 支持库/适配层/Tesseract提供者/…`），本会话**不抢**。方向与逐组落点见 `开发文档/分析/批R单据_R20_能力面合并与内部层过滤_20260924.md` §8.3 与 `开发文档/方案/适配层唯一化迁移清单_20260917.md`。
5. **未跑 git 提交**（按纪律，收尾由主会话统一做）。

## 六之二、收尾补记（2026-09-24 13:14–13:20，全部经 MCP 取证）

> 开工ID `开工-20260924-131944-1176`。本节取代 §三 里标「⚠ 待复测」的两条读数，并**修正 §六.1 的出口**。

### 判据 2／3 硬读数（已取到）
- 入口：`自修复工具.运行开发编译口(变更文件=[开发工具/开发编译口/编译口.py, 开发工具/薄壳/网关转发.py, 测试中心/开发工具/测试编译口单写者守卫.py], 源码根=本仓)` → 任务id `e6f16649604a4365`，13:16:05 → 13:17:52。
- **`测试中心.开发工具.测试编译口单写者守卫 退出码=0 耗时=5.33s：.................`（17 例全绿）** —— 含本批新增的 `活锁排队等到就放行` 2 例（① 持锁者释放后排队者拿到锁、真等过 ≥1000ms；② 等到上限才拒启且说明含持锁者 PID／启动时刻／已排队等待／建议，且排队者没动别人的锁）。**判据 2、判据 3 由这两例的断言给出。**
- 同轮 12 个定向测试模块并行（8 路，墙钟 82.06s）：红 3、未跑 0。

### 判据 1／4 补读数
- 判据 1（真并发等锁）：见 §三 现场读数（等到约 62 秒后拿到锁）。
- 判据 4：锁空时 `动态编译(类型=1, 模块ID=测试中心/开发工具/测试编译口单写者守卫.py, 源码根=本仓, 明细级别=全量)` 任务 `bbe1006c601c4624` **24 秒**跑完（13:14:14→13:14:38，未排队）⇒ 无锁时不引入额外延迟。

### 编译读数（本轮已取到的）
- `动态编译(…, 源码根=本仓, 明细级别=全量)`：**成功 19／失败 1／检查项 20**，`失败门禁=[]`、`未核验项=[]`，唯一只报红＝`制品新鲜度`，**五态＝只报告，退出码 0（阻断 0）**。
- `制品新鲜度` 原文要点：`制品来源提交 777147f695093e19a4dda09a4273215ed90a61bf`、`落后提交数 5`、`说明：网关跑的是旧装配，须重建制品 + 重启网关` ⇒ **本批改动在运行态仍是旧的**（只有传 `源码根=本仓` 时跑的才是新代码）。
- `运行开发编译口(变更文件=本批 3 文件)` 五态＝**失败**（红 3，见下），**本批用例本身全绿**。

### 出口修正（§六.1 原文是错的）
- 原写「重跑 `动态编译(类型=1, 模块ID=测试中心/开发工具/测试编译口单写者守卫.py)`，看 定向测试 一栏」——**错**。实测：`模块ID` 给**用例文件自己**时 `定向测试点名=[]`（步骤5 原话：「受影响包未匹配到任何测试模块 —— 这是『没有点名对象』，不是『测试通过』」）。
- 正确入口：**`变更文件` 给被测源文件**（`自修复工具.运行开发编译口` 的 `变更文件`），步骤5 才按「反向 import 图闭包 ∩ 测试模块」把覆盖它的用例点起来。
- 新登记缺陷：两个公开编译能力（静态／动态）都**不暴露「点名」参数**，而编译口 CLI 有 `--不点名`（＝本来有点名开关）⇒ 想让某条用例真跑，只能靠「变更文件 → 测试覆盖」自动命中。

### 三个红模块（待归因，逐条给归因方法）
| 模块 | 读数 | 归因 |
|---|---|---|
| `测试中心.开发工具.测试_注册口径漂移` | `E.EFEEEEEEE.E.FFFF...F...`，2.18s | **已登记存量红**（`开发文档/未完成事项.md:100`：unittest 存量红 9 条／35 用例，经 HEAD 干净工作树对比证真，非任何一次改动引入）；但本轮回的 E／F 数多于登记的 6 条 ⇒ 登记数偏小 |
| `测试中心.开发工具.测试_编译口未核验与门禁清单` | 退出码 1，**耗时 82.03s**（同轮最长） | **待归因**。已排除「排队腿把嵌套子编译口挂住」：该模块 `跑编译口()` 默认注入 `编译口嵌套标记`（源码 :63-71），嵌套层跳过单写者守卫、不排队 |
| `测试中心.开发工具.测试_薄壳返回可控` | `..........E...E..............` ＋ `WARNING:root:Failed to validate request: Received request before initialization was complete` | **待归因**，症状与 `未完成事项.md:92` 登记的「换壳后新壳停在 NotInitialized」同族；本批改了 `开发工具/薄壳/网关转发.py` ⇒ 薄壳指纹变、需换新壳，须专门复验 |
- 归因方法：① 拿 `06386bc3` 之前的提交建独立工作区，跑同样三模块对照；② 或单跑该模块取其详细 FAIL 文本（编译口的定向测试**只回点号、不回明细** —— 这本身也是取证缺口）。

## 七、`未完成事项.md` 待替换文本（租约释放后一次应用）

**第 5 条**（两处片段替换）：
- `设计如此（不许两个写者同改派生件）` → `**已改**（2026-09-24 批R·R-25）：单写者锁保留，**被占改成有界排队等**（缺省 900 秒，环境变量 `编译口排队上限秒` 可覆盖），等到上限才拒启`
- `派活纪律：编译类任务**一律串行**，不许并发派` → `排队腿落地后编译类任务**不必再串行派**（并行会排队，不会互相拒启）；但**外层调用方超时仍是更紧的边界**（薄壳旧代码注入 120 秒 ⇒ 排队会吃掉预算，实测被掐死一次）`

**第 8 条**（整行替换为）：`| 8 | ~~**定向测试红（新，阻断上线）**~~ | **已收口**（2026-09-24，R-19 第二半 `777147f6`）：根因是判据错——`公共契约/运行时/仓库只读锁.py::_推断仓库根` 用 `exists()` 判 `.git`，而 worktree 根上的 `.git` 是**指针文件** ⇒ 工作区根被当仓库根 ⇒ 受管路径永远认不出写租约（结构性 fail-closed）。改 `exists()` → `is_dir()` 后 `动态编译(类型=1, 模块ID=模块库/自修复工具)` 回 **成功 18／失败 2／五态＝只报告，阻断 0**（3 例转绿） | — |`
