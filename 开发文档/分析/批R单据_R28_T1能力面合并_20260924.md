<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R · R-28：T1 能力面两条腿 —— 按裁决①删「适配层孪生能力面」

> 收口线程 开工ID `开工-20260924-160509-9a1c`（2026-09-24 16:04 起）｜编辑由 `开工-20260924-154841-a2ba` 于 15:48 落地（其 38 条租约 16:12:47 自动回收）｜复核事实源 `开工-20260924-143411-1295`（R-20 单据 §十）
> 事实源：`开发文档/未完成事项.md`「批R 收尾补记 / 剩余项 #1」＋ `开发文档/分析/批R单据_R20_能力面合并与内部层过滤_20260924.md`（§十 复核）
> 裁决：华哥「如果是两条腿的就合并成一条」⇒ 主会话拍板 **方案①**：删「适配层孪生能力面」、保留后端腿 id。

## 一、结论（先说）

- **T1 已按方案①落地**：5 个适配层提供者的孪生能力面（**37 条 id**）已删，5 包自此「**有实现、无能力面**」；对外 id 不变、**生产调用点零改动**。
- **硬判据 1~4 全部达成**（§三）；**硬判据 5（阻断 0）未达成**，但**三条红项经逐条取证均非本批造成**（§五）。
- 本批另修两处**收口必需**：`公开调用完整性门禁` 六环判据新增「两侧同时显式空」放行（否则 5 包必判 `声明-包声明.json未列能力`）＋ `能力id冻结基线` 显式重建（37 条合法下线留痕）。

## 二、改了哪些文件（本批写集）

| 文件 | 动作 | 说明 |
|---|---|---|
| `支持库/适配层/{Git,FFmpeg,Pillow,PyMuPDF,Tesseract}提供者/能力定义.json` | 改 | `能力列表` → `[]`；`说明` 留 R-28 沿革（历史描述，不再是能力声明） |
| 同上 `包声明.json` | 改 | `能力` → `[]`（**两侧同时显式空**是合法形态的判据） |
| 同上 `__init__.py` | 改 | 注册镜像：公开能力 **0 条** |
| 同上 `完整性摘要.json` | 改 | 派生镜像，同批刷 |
| `支持库/适配层/{Git,Tesseract}提供者/能力契约/参数契约.json`、`能力数据/*.json` | 改 | 派生镜像 → `能力契约: []` |
| `开发工具/契约编译/能力定义编译器.py` | 改 | :249/:268 新增「有实现、无能力面」合法形态（两侧同时空才放行；单侧清空照旧判红） |
| `开发工具/公开调用完整性门禁_检查与判定.py` | 改（**本线程**） | 新增 `_两侧显式零能力` ＋ :95 判据改 `elif not 能力列表 and not _两侧显式零能力(...)` |
| `模块库/能力目录/实现/能力索引.py` | 改（**本线程**） | :198-207 口径复核：752→715／62→25／74→37 |
| `开发文档/项目证据/能力id冻结基线.json` | 改（**本线程**） | `--冻结` 重建：752 → **715**，37 条合法下线留痕 |
| `开发文档/未完成事项.md` | 改（**本线程**） | 剩余项 #1 收口 |
| `开发文档/分析/批R单据_R28_T1能力面合并_20260924.md` | 新增 | 本单据 |

**明确未碰**：`模块库/开工编排/**`、`模块库/自修复工具/**`（硬判据 4）；`支持库/适配层/*/实现/**`（**保留实现**是方案①的既定代价，不是缺陷）。

## 三、硬判据读数

### 判据 1｜5 组同名能力 id：两条 → 一条（逐组给条数）

| 组 | 适配层腿（包） | 改前 | **改后** | 后端腿（保留，唯一节点） | 改前 | **改后** |
|---|---|---|---|---|---|---|
| Git | `支持库/适配层/Git提供者` | 14 | **0** | `支持库/后端/版本控制支持库/Git操作` | 14 | **14** |
| FFmpeg | `支持库/适配层/FFmpeg提供者` | 7 | **0** | `支持库/后端/媒体处理支持库/FFmpeg媒体` | 7 | **7** |
| Pillow | `支持库/适配层/Pillow提供者` | 9 | **0** | `支持库/后端/图像处理支持库/图像解码` | 9 | **9** |
| PyMuPDF | `支持库/适配层/PyMuPDF提供者` | 4 | **0** | `支持库/后端/文档转换支持库/PDF渲染` | 4 | **4** |
| Tesseract | `支持库/适配层/Tesseract提供者` | 3 | **0** | `支持库/后端/OCR识别支持库/OCR识别` | 3 | **3** |
| **合计** | — | **37** | **0** | — | **37** | **37** |

- 口径：读 `能力定义.json` 的 `能力列表[].能力id` 计数（本线程实读，改后逐包 0 条）。改前值逐字引自 R-20 单据 §10.1（`开工-20260924-143411-1295` 现场实测，两腿完全镜像）。
- **不留旧腿**：5 包 `能力定义.json` 的能力声明已删；**未**留别名 / 兼容壳 / 转发腿（`能力搜索` 实测见判据 2）。

### 判据 2｜`能力搜索` 同一动作只回一条

实调 `能力搜索(关键词="Git操作.当前状态")`：唯一同名命中 ＝ **`版本控制支持库.Git操作.当前状态`**（`包id` ＝ `支持库.后端.版本控制支持库.Git操作`、`内部层=true`、`适配层=false`）；**适配层那条 0 命中**（其余命中为 `自修复工具.*` 的双字滑窗兜底，非孪生）。

### 判据 3｜装配口径：752 → **715**（被排除孪生 62 → **25**）

| 读数 | 改前 | **改后** | 取证方式 |
|---|---|---|---|
| 装配口径（注册表去重后能力 id 数） | **752** | **715** | `能力id冻结基线门禁 --冻结` 直读（口径＝装配口径），冻结文件 `能力数: 715` |
| 发现面 | 690 | **690** | `能力搜索` 兜底判据「全表条数」实测 |
| 被排除孪生 | **62** | **25** | 差额 ＝ 装配口径 − 发现面 |
| 适配层侧登记条数 | 74 | **37** | `能力id冻结基线.json` 逐包计数（FFmpeg 7／Git 14／Tesseract 3／PyMuPDF 4／Pillow 9 已消失） |

**结论**：62 条里**含这 5 组的 37 条** ⇒ 改后自然收敛为 **25**（余下 25 条属其他适配层包，本批未动）；`能力索引.py` 自述口径已同批改成 `715 = 690 + 25`。

### 判据 4｜生产调用点零改动

`git diff --stat -- 模块库/开工编排 模块库/自修复工具 支持库/适配层` 实测：**38 files changed, 400 insertions(+), 8211 deletions(-)**，逐行**全部落在 `支持库/适配层/**`**；`模块库/开工编排` 与 `模块库/自修复工具` **一行未出**（该命令对这两处的输出为空）⇒ 生产调用点零改动（`开工编排.py:40-41` 等全部照旧）。`git 当前状态` 的 `未提交修改` 清单同样不含这两处。

**派生镜像同批已刷**（`git diff --stat` 逐包可见）：5 包的 `能力契约/参数契约.json`、`能力数据/{Agent查询数据,能力搜索数据}.json`、`完整性摘要.json`、`__init__.py` 均随本批刷新（例：`Git提供者/能力契约/参数契约.json` 与 `FFmpeg提供者/能力契约/参数契约.json` 实读均为 `{"契约版本": "2.0.0", "能力契约": []}`），无「改前只改 `能力定义.json`、派生件留旧」的漂移。

## 四、静态编译读数（异步腿）

> 口径：同步腿被网关按「超时秒 ≥ 10」拒启 ⇒ 一律 `任务提交` ＋ `任务查询`。

| 轮次 | 任务id | 模块ID | 五态 | 成功/失败 | 阻断 |
|---|---|---|---|---|---|
| 改前基线 | `174a30596097409d` | `支持库/适配层/Git提供者` | 未核验 | 16 / 4 | **3**（公开调用完整性、能力id冻结基线、防回潮·反向破坏） |
| 改前基线 | `83559eba212a49f6` | `支持库/适配层/FFmpeg提供者` | 未核验 | 17 / 3 | **2**（公开调用完整性、防回潮·反向破坏） |
| **改后** | `706b5da4b781456f` | `支持库/适配层/Git提供者` | 失败 | 17 / 3 | **2**（契约漂移、公开调用完整性）——落点均为 `支持库.后端.文件系统支持库.内容检索`，非本批 |
| **改后** | `472ecac1c52d4626` | `支持库/适配层/FFmpeg提供者` | 失败 | 17 / 3 | **2**（同上，落点同一外部项） |
| **改后** | `bd430bfb9c1349d3` | `支持库/适配层/Pillow提供者` | 失败 | 17 / 3 | **2**（同上） |
| **改后** | `396e201bcc58407f` | `支持库/适配层/PyMuPDF提供者` | 失败 | 17 / 3 | **2**（同上） |
| **改后** | `c471633972b14a07` | `支持库/适配层/Tesseract提供者` | 失败 | 17 / 3 | **2**（同上） |
| **改后·全仓** | `316667a32ff340b1` | `类型=0 全仓` | 失败 | 17 / 3 | **2** —— 逐条明细的 `模块ID或库ID` 直接点名 **`文件系统支持库.内容检索.正则搜索`**（外部），**本批 5 包零红项** |

**五包读数完全同构**：17/3、阻断 2（均为同一外部项）、只报告 1（制品新鲜度）—— R-28 未在任何一条里留下红项。

**本批红项的消除对照**：

- `能力id冻结基线` 37 项违规 → **已消**（`--冻结` 重建 715 条，合法下线留痕）；
- `公开调用完整性` 5 项 `声明-包声明.json未列能力`（FFmpeg/Git/Pillow/PyMuPDF/Tesseract 各 1）→ **已消**（六环判据同口径放行）；
- `防回潮·反向破坏` 的 `ImportError: 设置本次调用基准` → 改后复跑**未再现**（源树确有该符号：`公共契约/运行时/平台适配/路径解析.py:56` ＋ `__init__.py:179`；先前一次为**制品落后 1 提交**的陈旧装配效应，属环境类、非本批）。

**改后残留（全部非本批，逐条给证据）**：

1. **`契约漂移`（阻断）**：`[判红] 支持库/后端/文件系统支持库/内容检索: 文件系统支持库.内容检索.正则搜索: 注册参数 根目录 默认值 None ≠ 契约默认值 ''` —— 落点在 `支持库/后端/文件系统支持库/内容检索`，另一会话正在改其 `使用说明.md`/契约（`能力id冻结基线` 的 `[留痕] 内容指纹变化` 同批点名该能力）。
2. **`公开调用完整性`（阻断）**：剩 2 项新增违规，**全在 `支持库.后端.文件系统支持库.内容检索`**（`说明书-包级版本不一致 1.1.3 ← 1.1.4`、`说明书-必填不一致 正则搜索 根目录 True ← False`）；本批 5 包的 5 项已归零。
3. **`制品新鲜度`（只报告）**：制品来源提交 `81cc618d` ≠ HEAD `aaa2f262`，落后 1 提交 ⇒ 收尾链动作「重建制品 ＋ 重启网关」。

**生成类（结论原文点名）**：`包级说明-使用说明（生成退出码 2）`、`说明书载位同步（人工类）（生成退出码 2）` —— 与上述 `内容检索` 说明书在途改动同源；`支持库/适配层/Git提供者` 的逐条明细**未出现**任何说明书红项。

## 五、未做项（如实）

| # | 项 | 为什么没做 |
|---|---|---|
| 1 | **`阻断 0`（硬判据 5）** | 未达成。残留 2 条阻断的落点全在 `支持库.后端.文件系统支持库.内容检索`（**另一会话在途**），本批无权改；按「不修无关 bug」如实登记 |
| 2 | **类型=0 全仓** | 见 §六「后续」：本单据落盘后补跑 |
| 3 | `公开调用完整性门禁存量基线.json` 16 桶下调 | **未做**。门禁自述「只判『基线外新增』与『桶内计数超出』，低于基线只打 `[可收敛]` 提示」；本次判红的实际原因是第 2 条那 2 项新增违规。16 桶现值全 0（其中 **37/69 属本批**、**32/69 属 R-20 生成侧过滤的既有收敛**）——下调属 **R-20 的债务记账**，跨批，故只登记不动 |
| 4 | git 提交 / 制品重建 / 网关重启 | 任务信：留给收尾链（主会话） |

## 六、须确认项

1. **`内容检索` 的两条阻断**（契约漂移 ＋ 说明书 2 项）请主会话指到其所属会话收口；本批只做取证与登记。
2. **`公开调用完整性门禁存量基线` 的 16 桶**是否本批一并下调为 0（含 R-20 的 11 桶）——涉及跨批记账，请主会话明示。
3. **五包「有实现、无能力面」的连带面**：R-20 单据 §10.3（3）登记的 39 处测试硬 import／`开发工具/环境自检.py:67-72` 文案／手写 `验证场景*.json` 是否已随之收敛，本批**未逐条复核**（本批点名编译的 5 个包均未报出这些红项，但全仓面未单独扫）。
4. **租约交接留痕**：`开工-20260924-154841-a2ba` 的 38 条租约于 16:12:47 自动回收、`开工-20260924-160123-89b1`（同一 R-28 交付流）持 `未完成事项.md` 至 16:16:24 —— 两条均为本任务流的前序会话（心跳分别停在 15:57:43／16:01:24），本线程按「继承并接管」处理。

## 七、复现命令

```
python3.14 开发工具/能力id冻结基线门禁.py            # 应通过（当前集 ⊇ 基线集）
python3.14 开发工具/公开调用完整性门禁.py            # 本批 5 包应 0 违规
```

MCP：`能力目录.搜索能力(关键词="Git操作.当前状态")` ⇒ 只回后端腿一条；`自修复工具.静态编译(类型=1, 模块ID=支持库/适配层/<包>)`。
