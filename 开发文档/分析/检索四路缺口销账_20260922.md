# 检索四路缺口（G1–G5）销账 · 2026-09-22

> 原条目：`开发文档/未完成事项.md` **#208**（2026-09-21 立项，华哥「搜索以准确为主 + 完完全全用 MCP」）。
> 本件是**销账证据**：五路逐条现场核实 + 真调用证据。销账后 #208 行从待办清单移除，结论留本件。
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


## 一、结论

**G1–G5 全部已落地**，不是「待做」。立项时的证据（「搜『目录树』零命中」「决策层验证场景未覆盖」）
到 2026-09-22 已不成立 —— 只有 G5 的验证场景是真缺，本轮补上。

| # | 缺口 | 现状 | 真调证据 |
|---|---|---|---|
| G1 | 目录树（一次拿多层） | ✅ `文件系统支持库.文件操作.目录树` 已实现 | 见 §2.1 |
| G2 | 代码语义索引 | ✅ `代码解析支持库.语义索引.建代码索引` / `查代码块` 已实现 | 见 §2.2 |
| G3 | 仓库地图（排序 + 预算） | ✅ `代码解析支持库.仓库地图.建符号图` 已实现 | 见 §2.3 |
| G4 | 统一检索入口 | ✅ `检索支持库.统一检索.统一检索` 已实现 | 见 §2.4 |
| G5 | 决策层真跑通 | ✅ 本轮补验证场景 + 补 2 条权限声明（`a5e71019`） | 见 §2.5 |

## 二、逐条真调证据

### 2.1 G1 目录树

经 40007 网关调 `文件系统支持库.文件操作.目录树`（目录路径=`模块库`、最大深度=2、条数上限=60）：

```
成功 = true ｜ 耗时 2.4 毫秒
节点数 60 ｜ 目录数 33 ｜ 文件数 27
已截断 = true ｜ 截断处 = "媒体转写/能力数据"
估算token = 310 ｜ 估算口径 = "字符数 / 3（与 代码解析支持库.仓库地图 同一把尺子）"
```

**比方案多出的东西**（方案只要 7 参数 / 6 返回键）：参数多 `忽略模式`；返回多 `估算token` + `估算口径`。
**关键**：`已截断=真` + `截断处` 如实回报，不静默截断。

### 2.2 G2 代码语义索引

经网关调 `代码解析支持库.语义索引.查代码块`（索引根=`支持库/后端/大语言模型支持库/模型连接器`、查询=`续租句柄`、顶部数量=3）：

```
成功 = true ｜ 召回数 260 ｜ 命中数 3 ｜ 耗时 7381.69 毫秒
[1] 实现/模型连接器.py:802-812  分数 0.850（向量 0.786 + 关键词 1.0）
[2] __init__.py:6-103          分数 0.665
[3] __init__.py:106-210        分数 0.646
```

**第 1 条正是目标函数**（`def 续租句柄` 在 `:803`）⇒ 召回 → 精排链路有效。

### 2.3 G3 仓库地图

能力 `代码解析支持库.仓库地图.建符号图` 已注册；索引库实存：
`工程缓存/语义索引/仓库地图.db` **2.97 MB**（说明真建过图，不是空壳）。

### 2.4 G4 统一检索入口

能力 `检索支持库.统一检索.统一检索` 已注册（RRF 倒数排名融合多路召回）；
索引库实存：`工程缓存/语义索引/统一检索.db` **401 KB**。

### 2.5 G5 决策层

**本轮真缺的一项**：`模型连接器/验证场景.json` 里 **0 处**提到 `连接决策模型` / `执行决策`
⇒ 接入有了、没有可引用的成功验证。

本轮补（提交 `a5e71019`）：
1. 验证场景加 2 步：`连接决策模型`（连夹具 HTTP 模型）+ `执行决策多选一`（choice 型问题）
2. 定向体检抓出 **2 个能力无权限声明** ⇒ `权限契约.json` 补 2 条

定向验证（**不跑全仓**，按分级验证纪律）：
```
包体检 <模型连接器>          → ✅ 无缺项，0.108 秒
验证场景体检 --根 <模型连接器> → ✅ 全部通过，0.00 秒
全量重算摘要                 → ✅ 全部闭合，0.87 秒
```

## 三、遗留（观察项，不占编号）

| 现象 | 数字 | 为什么值得记 |
|---|---|---|
| G2 单次查询耗时 | **7.4 秒**（召回 260 条） | 每次查询都要现算**查询嵌入**；若要进交互路径（Agent 每轮搜代码），必须先进 `嵌入缓存` 或预生成查询向量，否则单次 7 秒会成为新黑洞 |

## 四、方法论教训（本轮实证）

1. **立项时的「零命中」到销账时可能已不成立** —— G1 的 `目录树` 早就在，只是立项那天的搜索没命中。
   销账必须先 `capability_search` 核能力面，**不能照抄立项证据**。
2. **定向验证比全仓快 100 倍**：`包体检 <包>` 0.11 秒 vs 全仓编译口 12 秒。
   小改动跑定向、一批收口才跑全仓（`开发文档/规范/批量开发流程与分级验证.md` §二）。
