# 端到端背压核对（B8）

> 任务：二次审计 J / B8「端到端背压是否协同需核对」（`开发文档/未完成事项.md:165` 第 53 项 ⏳ 未派）。
> 性质：**只读审计**。本次任务唯一写入物就是本文件（任务记忆包 `工程缓存/任务记忆/` 只读，未改动）。
> **快照口径（唯一、可复现）：`git rev-parse HEAD` = `4bc164eff045433c07f6df899879233e318cc695`**
> 行号**一律按该提交取**（`git show <提交>:<路径>`），**不按工作区**——原因是本仓同时有别的会话在改
> `超时执行.py`/`唯一能力调用.py`/`任务进程.py`/`独立进程.py`/`提供者生命周期.py`/`权威状态.py` 等文件，
> 工作区存在未提交改动，读工作区会让行号随别人的编辑漂移、不可复现。
> 复算（198 个锚点，缺失 0 才通过）：
> `unset PYTHONPATH && python3.14 /private/tmp/系统平台临时脚本_20260918/b8_verify_v2.py`
> 落 `开发文档/分析/端到端背压核对_20260918.md`；复算脚本在 `/private/tmp/系统平台临时脚本_20260918/`。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 0. 口径说明

只回答四个问题：**有上限吗 / 满了给什么错误码（不是文案） / 调用方能不能判别可重试 / 有没有退避提示**，
再加**层与层不协同**。每条结论带 `文件:行`；查不到实现写「未见实现」，不推测。

「退避提示」判据：链路上是否有 `Retry-After`（或等价字段，如把「建议下次重试秒数」写进结果详情）
**随失败一起回传**。结论：**生产代码 0 处产生、0 处解析 `Retry-After`**
（`运行核心`/`公共契约`/`支持库`/`后端核心`/`平台控制面`/`模块库` 全目录 grep 无命中；
唯一提及出现在 `开发文档/参考资料/外部源码研究/` 的**外部参考**架构笔记里，不是本仓实现）。

「可重试」的载体在本仓是存在的：`公共契约/基础类型/结果类型.py:40` `def 可重试(self) -> bool:`；
消费侧的读法也真实存在：`平台控制面/提供者/HTTP提供者.py:155` `if 结果["成功"] or not 结果["可重试"]:`。
问题不在「有没有这个字段」，而在**网关这一步把它丢了**（见 N2）。

---

## 1. 层结论矩阵

| # | 层 | ① 有容量上限吗（怎么定） | ② 满了给什么错误码（能否判别可重试） | ③ 退避提示 | ④ 层间协同 |
|---|---|---|---|---|---|
| 1 | **入口**（有界线程 HTTP 边界） | 有。工作线程信号量 = `并发上限`（**配置**，默认 64，硬区间 1..256；生产装配 256）；backlog `min(128, 并发上限)` | **有**：`限流` → HTTP **429** + 结构化信封。调用方可从 429 判别，但**信封无 `可重试` 字段** | **未见实现**（429 响应头固定 4 行，无 `Retry-After`） | ⚠ 与网关层是**两套独立并发闸门**，都回 `429/限流`，调用方分不清哪层满 |
| 2 | **网关**（五维限流 + 幂等） | 有。固定窗口频率 + 并发，**全是构造常量默认值**：全局并发 50、单维并发 40、窗口 10s、项目 500/用户 100/能力 200/任务 100/提供者 300 | **有**：`限流` → 429。可重试语义只在**人类文案**「请稍后重试」；网关响应结构**无 `可重试` 字段** | **未见实现**（无秒数、无头） | ⚠ `进入任务`/`进入流式` 两闸门**全仓无调用方**（配置了不生效）；⚠ 幂等「进行中」表无上限 |
| 3 | **能力调用**（唯一能力调用 + 执行线程准入） | 有。全局执行线程 256、**每能力** 64、每能力闸门登记 512 —— **模块常量**（依据：取自网关工作线程的可配置上界/默认值） | **有且完整**：统一结果 `限流` + **`可重试=真`** + `详情["准入门"]`（全局/每能力、上限、当前占用）。**全链路唯一同时给码、给可重试、给明细的一层** | **未见实现**（只有「不排队」语义） | ✅ 与网关 429 同码；⚠ 满载**直接拒绝**，与任务层**排队长等**取向相反 |
| 4 | **任务调度**（任务进程池） | 有。活动进程 `最大活动数=4`、排队槽 `最大排队数=16`、`提交截止秒=30.0` —— **构造默认值，生产装配未覆盖** | **有，但码被吞**：层内抛 `资源繁忙错误`；`资源繁忙` **未登记**两表 → 网关无对应分支 → **`内部错误` / HTTP 500** | **未见实现** | ❌ **最严重**：任务层明确判「忙/可重试」，到网关被伪装成「服务端故障」；❌ 已入队任务**不受提交截止约束**，可无限等槽 |
| 5 | **提供者**（独立进程池 / 生命周期 / 进程提供者） | 有。`提供者进程池` 池大小 2（上限 16）、资源键表 4096；`提供者生命周期` 最大进程数 8；`进程提供者` 池大小 1、资源键表 4096 | **有且带可重试**：`资源繁忙` / `Provider资源键表已满` / **`可重试=真`**；生命周期另有 `已取消`/`超时`/`滞留响应超预算`（后者带可重试） | **未见实现** | ❌ **两条并行实现均无生产调用方**；❌ `最大进程数=8` 只在**启动路径**生效，调用路径无提供者级准入 |
| 6 | **外部模型**（模型 HTTP 出站） | 有（非并发上限）：单次 HTTP 超时（默认 600/1800，配置可覆盖）、响应体 4MB、内存安全阈值 80%（连接阶段拒新模型）；**无每模型并发闸门** | **有**：上游 429 → `请求限流`；5xx → `模型调用失败`/`上游错误`；**可重试判定两处相反**（429 在提供者层判真、在连接器层判假） | ❌ **未解析上游 `Retry-After`**；本仓**有现成退避算法**（指数退避 0.5s 基数、上限 300s）但**无生产调用方** | ❌ 最需要退避的 429 路径：无退避提示 + 可重试判定不一致 |

---

## 2. 逐层证据（全部行号 = 提交 `4bc164ef`）

### 2.1 入口层（HTTP 边界）

**上限（怎么定）**
- `运行核心/统一网关/本地网关.py:262` `最大工作线程: int = 64`（构造默认）；`:264` `1 <= 最大工作线程 <= 256` 硬区间；`:265` 越界抛 `ValueError`。
- `运行核心/统一网关/本地网关.py:267` `self.request_queue_size = min(128, 最大工作线程)`（监听 backlog）。
- `运行核心/统一网关/本地网关.py:268` `self._工作线程信号量 = threading.BoundedSemaphore(最大工作线程)`。
- 生产值：`运行核心/启动运行核心网关.py:75` `"并发上限": 256` → `运行核心/统一网关/本地网关.py:354` `并发上限`（默认 64；`:356` 校验 1..256）→ `:423` `最大工作线程=self.并发上限`。
- 流式路径共用同一预算：`运行核心/统一网关/本地网关.py:395` `HTTP流式管理器(最大并发通道数=self.并发上限)`。

**满了给什么码**
- `运行核心/统一网关/本地网关.py:275` `process_request` → `:276` `if not self._工作线程信号量.acquire(blocking=False):` → `:278` `self.拒绝请求数 += 1` → `:279` `_写限流响应(request)`。
- `运行核心/统一网关/本地网关.py:301-306` 写 429 + 网关响应信封；`:304` `"错误码": "限流", "错误说明": "网关工作线程已达上限"`；`:308` `HTTP/1.1 429 Too Many Requests`。
- 码表：`运行核心/统一网关/本地网关.py:48` `"限流": 429`。边界拒绝走同一信封：`:558` `def _拒绝(self, 状态码: int, 错误码: str, …)`。
- 流式路径满载同为 429：`运行核心/统一网关/流式HTTP.py:245-246` → `运行核心/统一网关/本地网关.py:984` `self._拒绝(429, "限流", str(错误), "流式生成对话")`。
- **可重试判别**：可按 429 + `错误码=限流` 判别；**信封无 `可重试` 字段**（`:301-306` 的字段清单即全部）。
- 约束：未登记错误码回落 500 —— `运行核心/统一网关/本地网关.py:1115` `状态码 = 公开错误码状态映射.get(响应.错误码, 500)`。
- 请求体上限：`运行核心/统一网关/本地网关.py:364` `请求大小上限`（默认 1MiB；生产装配加大）；超限时 `:1016` `状态码 = 413 if "大小上限" in 错误说明 else 400`。

**退避提示**：**未见实现**。`运行核心/统一网关/本地网关.py:307-313` 响应头固定 `Content-Type`/`Content-Length`/`Cache-Control`/`Connection`。

**不协同**
- 入口工作线程闸门（默认 64 / 生产 256）与网关层五维闸门（全局 50、单维 40）是**两套独立计数器**，都回 `限流/429`；调用方无法从响应分辨是哪一层满。
- 超时错层：`运行核心/统一网关/本地网关.py:352` `请求超时秒` 默认 1800（生产 `运行核心/启动运行核心网关.py:66` 显式 1800）；socket 超时 `:471`、`:634`；请求体读取另有更短上限（`:353`）。
- 同域第二份有界实现行为不同：`公共契约/运行时/有界HTTP.py:16` `最大线程数: int = 32`、`:22` `self.shutdown_request(请求)`（**满载不回任何响应**）。`运行核心/统一网关/本地网关.py:244` 自述「本类是本仓网关域唯一的有界线程 HTTP 实现」并点明那份**满载行为不同**；域外调用方（`开发工具/轻代码前端编辑器/启动编辑器.py:27`、`开发工具/能力网关/能力网关.py:42`、`支持库/前端/浏览器宿主/实现/浏览器宿主.py:13`）用的是后一份。有界实现的头部在 `运行核心/统一网关/本地网关.py:235`。

### 2.2 网关层（五维限流 + 幂等）

**上限（怎么定）** —— `运行核心/统一网关/限流器.py`，**全是构造默认值：无配置文件、无环境变量入口**：
- `:72` `最大并发请求: int = 50`；`:73` `最大任务数: int = 20`；`:74` `最大流式连接: int = 10`。
- `:83` `单维度并发: int = 40`（注释记 2026-09-16 从 20 放宽到 40，理由是「本机资源远未饱和」—— 即**上限不是容量实测值**）；`:84` `窗口秒: float = 10.0`（固定窗口）。
- 频率上限表 `:103-109`：项目 500 / 用户 100 / 能力 200 / 任务 100 / 提供者 300。
- 生效面自述（边界）：`:6`「**不跨重启保留、不跨进程/不跨实例共享**」；`:13`/`:65`「单进程网关 + 本机私有部署」。多实例需外部共享限流，本平台当前不提供。

**满了给什么码**
- 唯一比对入口 `运行核心/统一网关/限流器.py:173` `进入请求`；比对点 `:191`（全局并发）、`:198`（单维并发）、`:203`（窗口频率）；拒绝原因字符串 `:192`、`:200`、`:205`；错误码 `:29` `错误码_限流 = "限流"`；留痕 `:164` `def _记录拒绝(`。
- 网关落码：`运行核心/统一网关/网关核心.py:1027` `通过, _ = self.限流器.进入请求(` → `:1032` `被限流 = 真` → `:1033` `self._设置失败(响应, "限流")`；文案 `:96`；HTTP 映射 `运行核心/统一网关/本地网关.py:48`；同码文案另见 `运行核心/统一网关/协议表.py:96`。
- **可重试判别**：❌ 网关响应结构**无 `可重试`** —— `运行核心/统一网关/网关核心.py:525`（`class 网关响应`）字段区到 `:538` 末行「版本差异」，`转字典` `:540-552`。全集为 `请求id/操作/成功/值/错误码/错误说明/句柄/耗时毫秒/请求版本/当前版本/版本差异`。
- 下游也不读：`运行核心/能力调用/HTTP连接器.py:157-165` 的返回字典只取 `成功/值/错误码/错误说明/句柄/请求id/耗时毫秒`（`":164"` 即 `"耗时毫秒"` 那一行）；全文件 `可重试` grep 命中 0。

**退避提示**：**未见实现**（无字段、无头、无秒数）。

**不协同**
- ❌ **两个闸门无调用方（配置了不生效）**：`运行核心/统一网关/限流器.py:233` `进入任务`（自述 `:238`「全仓目前没有调用方（网关的任务入口由 `进入请求` 覆盖）」）、`:253` `进入流式`（自述「全仓目前没有调用方」）。全仓 grep 只命中定义与文档字符串。
- 因此 `最大任务数=20`（`:73`）、`最大流式连接=10`（`:74`）两个上限**从不参与判定**：任务维实际由 `请求并发/单维度并发` 生效；流式维实际由 `流式HTTP.py` 的信号量生效。
- ❌ **幂等「进行中」表无上限**：`运行核心/统一网关/网关核心.py:567` 两表无界声明；插入 `:747-756`；只有**已完成**表被裁剪（`:772`、`:780` `while len(self._幂等表) > 500:`）；`_幂等进行中` 仅在收尾 `pop`（`:771`、`:779`）时减。键是调用方自报的 `请求id`（长度上限 64）。
- 兜底会把「忙」吞成「故障」：`运行核心/统一网关/网关核心.py:1052-1053` `except (ConnectionError, TimeoutError): → 提供者不可用`；`:1054-1055` `except Exception → 内部错误`。任何以其它异常类型表达「满」的下层（见 2.4）都会落到 500。
- 幂等等待用调用方时限兜底：`运行核心/统一网关/网关核心.py:763-764`（同一幂等键等待超时 → `超时`），这是本层唯一带「等多久」语义的分支。
- 失败结果类型不合法时另报 `:928` `self._设置失败(响应, 结果对象.错误码 or "内部错误")`。

### 2.3 能力调用层（唯一能力调用 + 执行线程准入）

**上限（怎么定）** —— `运行核心/能力调用/超时执行.py`，**模块级常量，单一来源**（可经构造参数覆盖，测试用）：
- `:110` `默认全局执行线程上限 = 256`、`:111` `默认每能力执行线程上限 = 64`、`:119` `每能力闸门数上限 = 512`。
- 取值依据写在 `:100` 起注释块（「全局上限 = 256：执行线程只可能由网关工作线程创建…」）：全局 256 = 网关工作线程的**可配置上界**；每能力 64 = 网关工作线程的**默认值**。即上限**按入口配置定标**，不是硬件实测。
- 准入实现 `:448` `def _准入(self, 能力id: str)`：先占全局、再占每能力；满载 `等待秒=0` ⇒ **直接拒绝、不排队**（模块说明 `:51`）。
- 调用点 `:529` `闸门名表, 拒绝详情, 拒绝说明 = self._准入(能力id)`；单例 `:717` `执行器 = 超时执行器()`。
- 闸门表有界：超 `每能力闸门数上限` 的新能力 id 走共享兜底闸门（`:119` + 模块说明）。

**满了给什么码**（本链路最完整的一层）
- 满载说明 `:467` `说明 = (f"能力执行线程已达全局上限 {self.全局执行线程上限}"`。
- 拒绝结果 `:501` `"限流", 说明, 来源="超时执行器.线程准入", 可重试=真` + `详情={准入门/能力id/包id/请求id/全局执行线程上限/每能力执行线程上限}`。
- 原样收口到网关（模块说明）→ `运行核心/统一网关/本地网关.py:48` 映射 429。
- 超时口径：`运行核心/能力调用/唯一能力调用.py:249` / `:303`（「给了有效时限就走真实超时执行」）→ `:301` `def _执行实现(` → `:313` `执行器.执行(`；超时失败 `:356` `失败("超时", 说明, 来源="唯一能力调用服务", …, 可重试=真, 详情=详情)`。
- `可重试` 载体：`公共契约/基础类型/结果类型.py:40`；消费侧读法范例：`平台控制面/提供者/HTTP提供者.py:155`。

**退避提示**：**未见实现**（有「不排队」，无「多久以后再来」）。

**不协同**
- ⚠ **取向与任务层相反**：本层「满载直接拒绝、不排队」（`:51`）vs 任务层「排队长等」（`运行核心/任务调度/任务进程.py:255`、`:278-279`）。同一次业务走同步调用还是任务提交，背压行为不同，**对外无统一约定**。
- ⚠ 本层上限（256/64）与入口上限（默认 64 / 生产 256）**同源但两个独立计数器**：靠「入口可配置上界 256 = 执行线程全局上限 256」维持不互相触发（`:100` 起注释块的论证），该不变式目前由两侧各自校验维持（`运行核心/统一网关/本地网关.py:264`、`:356`），**无单一门禁固定**。

### 2.4 任务调度层（任务进程池）

**上限（怎么定）** —— `运行核心/任务调度/任务进程.py`，**构造默认值，生产装配未覆盖**：
- `:159` `最大活动数: int = 4`、`:160` `最大排队数: int = 16`、`:160` `提交截止秒: float = 30.0`。
- 生产装配链：`运行核心/启动运行核心网关.py:53` `任务系统实例 = 构造任务系统(后端)` → `运行核心/任务调度/任务接入.py:52-54` `实例 = 任务系统(存储目录=存储目录 or 默认任务存储目录())`（**不传上限**）→ `运行核心/任务调度/任务系统.py:62` 默认 `最大活动数: int = 4, 最大排队数: int = 16,`。
- 即：本机任务并发**硬顶 4**、排队**硬顶 16**、入队前最多等 **30 秒**，全是代码默认值，不是配置项。

**满了给什么码**
- `运行核心/任务调度/任务进程.py:59` `class 资源繁忙错误(Exception):`（`:63` 默认消息「任务进程池资源繁忙，拒绝提交新任务」）。
- 排队满 → `:263` `if len(self.等待队列) < self.最大排队数:` 为假 → `:266` `raise 资源繁忙错误(...)`。
- 队列满且未入队、等槽超截止 → `:273` `raise 资源繁忙错误("提交任务[..]等待槽位超过 30.0 秒，进程池资源繁忙（提交超时）")`。
- 停止态 → `:261`/`:281` `raise RuntimeError("任务进程池已停止，拒绝提交新任务")`。
- ❌ **错误码被判成服务端故障**：
  - `资源繁忙` **不在** `运行核心/统一网关/网关核心.py` 的 `公开错误说明表`（`:84` 起），也**不在** `运行核心/统一网关/本地网关.py` 的 `公开错误码状态映射`（`:37` 起）—— 两文件 `"资源繁忙"` grep 命中 **0**。
  - 网关调用点 `运行核心/统一网关/网关核心.py:956` `任务对象 = self.任务系统.提交(` **无 try/except**；异常一路上抛到 `处理()` 兜底 `:1054-1055` → `内部错误` → HTTP 500（`:1115` 回落）。
  - 门面也是裸调用：`运行核心/任务调度/任务系统.py:211-213` `def 提交(` → `:213` `独立对象 = self.进程池.提交(`，不转码。
  - 结论：**任务层明确表达「忙（可重试）」，调用方收到「内部错误 / 500」**。

**退避提示**：**未见实现**。

**不协同**
- ❌ **已入队任务无总时长上限**：`:255`（注释「不受截止约束（有界队列语义：排队任务不得丢失）」）→ `:278`（注释「已入队：带心跳等待槽位」）→ `:279` `self.提交条件.wait(timeout=0.1)`；循环只在拿到槽位或 `已停止` 时退出（`:261`/`:281`）。**只有入队前的等待受 30 秒截止约束**（`:276` `self.提交条件.wait(timeout=剩余截止)`）。
  对端：入口 HTTP 请求超时 1800 秒（`运行核心/统一网关/本地网关.py:352`、生产 `运行核心/启动运行核心网关.py:66`）。含义：**调用方早已超时放弃，任务仍可能在队列里等到槽位并被真正执行**（静默延迟执行，对外无任何「排队时长上限」）。
- ⚠ 排队水位不可观测：`:902` `def 活动进程数` 只给活动数，`等待队列` 长度无对外快照字段。
- ✅ 有界队列的「入队即占槽、上抛必归还」不变式实现良好（`:232` 说明、`:284`/`:340` 归还调用、`:352` `def _退出排队`），与背压协同无关，是**做对了**的部分。

### 2.5 提供者层（独立进程池 / 生命周期 / 进程提供者）

**上限（怎么定）**
- `运行核心/加载器/提供者隔离/独立进程.py:901` `池大小: int = 2, 最大资源键数: int = 4096`；`:68` `最大允许池大小 = 16`（构造校验）。
- `运行核心/运行环境管理器/提供者生命周期.py:46` `默认最大进程数 = 8`，`:94` `最大进程数: int = 默认最大进程数`；`:48` `默认最大重启次数 = 3`；`:49` `取消等待上限秒 = 2.0`；`:98` `调用超时秒: float = 3.0`。
- `平台控制面/提供者/进程提供者.py:271`（池大小 默认 1、最大资源键数 默认 4096）。

**满了给什么码**
- 资源键表满 → `运行核心/加载器/提供者隔离/独立进程.py:935` `_分配成员` → `:939` 判定 → 调用点 `:952-953` `进程调用结果(假, 错误码="资源繁忙", 错误说明="Provider资源键表已满", 可重试=真)`。
- 同语义另一份：`平台控制面/提供者/进程提供者.py:342` `{"成功": 假, "错误码": "资源繁忙", "错误说明": "Provider资源键表已满", "可重试": 真}`；另有 `:334` `已关闭`、`:336` `未启动`。
- 生命周期调用路径失败码：`:432`/`:462`/`:466` `错误码="已取消"`；`:470` `错误码="超时",`；`:440` `错误码="滞留响应超预算",`（带 `可重试=真`）。
- 启动路径进程数上限：`:318` `def 启动提供者(self, 提供者id: str) -> tuple[bool, str]:` → `:331`（TOCTOU 复查点 `:361`）`return 假, f"运行中进程数已达上限（{self._最大进程数}），拒绝启动 {提供者id}"` —— **返回 (bool, str)，不是错误码**。
- 可重试载体：`运行核心/加载器/提供者隔离/独立进程.py:353` `可重试: bool = 假`（`进程调用结果` 字段）。

**退避提示**：**未见实现**。

**不协同**
- ❌ **两条并行实现均不在产线**：`运行核心/加载器/提供者隔离/独立进程.py:898` `class 提供者进程池:` 全仓只命中定义（无构造、无调用）；`平台控制面/提供者/进程提供者.py:267` `本地进程提供者` 只被测试调用（`测试中心/第一批维修/测试_Provider进程.py:19`）。生产实际入口是 `运行核心/运行环境管理器/提供者生命周期.py:318` + 调用路径（内部直接建 `独立进程`）。
- ❌ **提供者级「并发满」只在启动路径判**：`:331`/`:361` 的 `最大进程数` 校验在 `启动提供者`；调用路径不做提供者级并发准入，只有 2 秒取消等待（`:49`）与 3 秒调用超时（`:98`）。即「8 个进程」是**启动名额**，不是「同时在飞的最大调用数」。
- ⚠ 该层 `资源繁忙` 同样未登记两表（`运行核心/统一网关/网关核心.py`/`本地网关.py` grep 0），目前靠「无调用方」侥幸未暴露；一旦接通即复现 N1。

### 2.6 外部模型层（模型 HTTP 出站）

**上限（怎么定）**
- 单次 HTTP 超时：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:35` `默认超时秒 = 1800`（`_HTTP请求超时秒`：配置 → 环境变量 → 默认）；流式路径 `支持库/适配层/模型HTTP提供者.py:533` 默认 120。
- 响应体上限：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:356` `_失败("超出限制", f"{连接类型} HTTP响应超过4MB上限")`。
- 并发型上限：**无每模型并发闸门**；只有内存门槛 `:36` `内存安全阈值 = 0.80` → `:138` `return _失败("资源预算未验证"`、`:145` `return _失败("资源不足",`（**连接阶段**拒新模型，不是调用阶段限流）。
- 工具调用并行上限 `:1180` `调度默认并行上限 = 10`（产出调度计划，不是背压闸门）。

**满了给什么码（含可重试不一致）**
- 提供者路径：`支持库/适配层/模型HTTP提供者.py:151-152` `if 状态码 == 429: return _失败("请求限流", …, 可重试=True)`；`:148` 不可达 → `提供者不可用/可重试=True`；`:150` 408/504 → `超时/可重试=True`；5xx → `模型调用失败/可重试=状态码>=500`。
- 连接器路径：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:379` `429: "请求限流"` → `:382` `return _失败(错误码, f"模型 HTTP 返回 {错误.code}", 可重试=错误.code >= 500)`。
  ❌ **同一事实、相反结论**：上游 429 时，`模型HTTP提供者.py:152` 判 **可重试=True**；`模型连接器.py:382` 判 **可重试=假**（429 < 500）。
- 流式：`支持库/适配层/模型HTTP提供者.py:265` `def _流式错误(错误码, 错误说明, *, 可重试=False…`；`:605-606` 上游错误 `可重试=响应.status >= 500`；`:618` `可重试=异常.code >= 500`。
- 流式事件透传：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:938` `_流式错误事件(…, 可重试)` → `:948` `def 流式生成对话(` → `:993` `from 支持库.适配层 import 模型HTTP提供者 as 提供者`。

**退避提示**
- ❌ **未解析上游 `Retry-After`**：`支持库/适配层/模型HTTP提供者.py:122`、`:596` 只有**请求头构造**（`headers=请求头`）；无响应头读取（异常分支 `:132` 只取状态码）。
- ❌ **有退避算法但无生产调用方**：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1250` `默认最大重试次数 = 3`、`:1251` `默认退避基数秒 = 0.5`、`:1254` `退避最大秒 = 300.0`、`:1270` `def 重试调度(*,`（返回 `{是否重试, 下次退避秒, 原因}`）；已登记为能力 `支持库/后端/大语言模型支持库/模型连接器/__init__.py:109`（`:114` `退避基数秒` 默认 0.5）。全仓生产调用方 grep 命中 **0**。

**不协同**
- ❌ 上游 429 是最需要「可重试 + 退避」的场景，却同时具备三处缺陷：可重试判定相反（N4）、无 `Retry-After` 解析、退避算法无人调用（N3）。
- ⚠ 消费侧被迫用**字符串关键词**判可重试：`模块库/直播逐字稿/实现/精校裁决.py:194` `("503", "502", "429", "timed out", "超时", "限流", "过载", "繁忙")`，并自带重试循环（`:236` `重试上限 = max(0, int(重试次数 or 0))`）——业务层绕过统一可重试语义自造一套。
- ⚠ 流式通道**削平源事件的 `可重试`**：`运行核心/统一网关/流式HTTP.py:294-298` 只取 `错误码`（`:296`）与 `错误说明` 组装失败事件（源事件来自 `模型HTTP提供者.py:265` / `模型连接器.py:938`，本来带 `可重试`）。事件本身会经 `_终止`（`:122`）→ `迭代事件`（`:198`）下发，**不是静默丢弃**，但字段被削掉。
- ⚠ 流式满载 429 来源不同：`运行核心/统一网关/流式HTTP.py:245-246`（通道信号量满）→ `运行核心/统一网关/本地网关.py:984`；相关上限 `:33` `最大并发通道上限 = 256`、`:28` `最大事件数上限 = 10000`、`:30` `累计payload上限字节 = 4 * 1024 * 1024`；事件队列**有字节预算**（`:79` + `:101`），但**无条数上限**；异常兜底 `:308` `通道.失败("提供者崩溃", …)`。

---

## 3. 不协同清单（按严重度）

| # | 不协同 | 现象 | 证据（提交 4bc164ef） |
|---|---|---|---|
| **N1** | **任务层「忙」被伪装成「服务端故障」** | 任务进程池判 `资源繁忙`（明确资源问题）→ 网关无对应分支、码未登记两表 → `内部错误` / **HTTP 500**。调用方按「服务端故障」排查，实际只需退避重试 | `运行核心/任务调度/任务进程.py:59,266,273`；`运行核心/任务调度/任务系统.py:213`；`运行核心/统一网关/网关核心.py:956,1054-1055`；`运行核心/统一网关/本地网关.py:1115` |
| **N2** | **网关对外丢掉「可重试」** | 可重试只写在人类文案（「请稍后重试」），响应结构无 `可重试` 字段；下游连接器也不读 → 全链路只能靠 HTTP 状态码猜 | `运行核心/统一网关/网关核心.py:96,525,538,540-552`；`运行核心/能力调用/HTTP连接器.py:157-165`（grep `可重试` = 0） |
| **N3** | **全链路无退避提示** | 生产代码 0 处产生/解析 `Retry-After`；唯一有退避算法的 `重试调度` 无生产调用方 | 全仓 grep 无命中；`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1250-1270`；`支持库/适配层/模型HTTP提供者.py:122,596` |
| **N4** | **上游 429 可重试判定两处相反** | 同一 HTTP 事实：提供者层判 `可重试=True`，连接器层判 `可重试=假` | `支持库/适配层/模型HTTP提供者.py:151-152` vs `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:379,382` |
| **N5** | **任务队列对已入队任务无时长上限（无限排队）** | 入队后不受 30 秒截止约束，0.1s 心跳无限等槽；对端 HTTP 1800 秒早超时 → 调用方放弃后任务可能仍被真正执行 | `运行核心/任务调度/任务进程.py:255,276,278-279`；`运行核心/统一网关/本地网关.py:352`；`运行核心/启动运行核心网关.py:66` |
| **N6** | **同一「满」有两种闸门、一个错误码** | 入口工作线程闸门与网关五维闸门是两套独立计数器，都回 `限流/429`，调用方无法分辨哪层满 | `运行核心/统一网关/本地网关.py:268,276,279,304,354` vs `运行核心/统一网关/限流器.py:191,198,203`；`运行核心/统一网关/网关核心.py:1033` |
| **N7** | **两个已配置的限流维度不生效** | `最大任务数=20`、`最大流式连接=10` 对应的 `进入任务`/`进入流式` 全仓无调用方（代码自述） | `运行核心/统一网关/限流器.py:73,74,233,238,253` |
| **N8** | **幂等「进行中」表无界** | 已完成表有 500 条上限，进行中表无上限；键是调用方自报的 ≤64 字符 `请求id` | `运行核心/统一网关/网关核心.py:567,747-756,771,779` vs `:772,780` |
| **N9** | **提供者层两条实现都不在产线** | `独立进程.提供者进程池` 无任何调用；`平台控制面/提供者/进程提供者` 只被测试调用 → 该层「满了怎么答」在生产链路上未被使用 | `运行核心/加载器/提供者隔离/独立进程.py:898,952-953`；`平台控制面/提供者/进程提供者.py:267,342`；`测试中心/第一批维修/测试_Provider进程.py:19` |
| **N10** | **提供者级并发只在启动路径判** | `最大进程数=8` 是启动名额，不是同时在飞的调用数上限；调用路径无提供者级准入 | `运行核心/运行环境管理器/提供者生命周期.py:46,94,318,331,361` |
| **N11** | **流式通道削平 `可重试`** | 源事件（提供者/连接器）带 `可重试`，网关组装失败事件时只留 `错误码`/`错误说明` | `运行核心/统一网关/流式HTTP.py:294-298`；`支持库/适配层/模型HTTP提供者.py:265`；`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:938` |
| **N12** | **同域两份有界 HTTP 实现行为不一致** | 本域满载回结构化 429；`公共契约` 那份直接关连接（**不回任何响应**），域外编辑器/能力网关/浏览器宿主用的是后一份 | `运行核心/统一网关/本地网关.py:235,244`；`公共契约/运行时/有界HTTP.py:16,22`；调用方 `开发工具/轻代码前端编辑器/启动编辑器.py:27`、`开发工具/能力网关/能力网关.py:42`、`支持库/前端/浏览器宿主/实现/浏览器宿主.py:13` |
| **N13** | **同步调用「拒绝」 vs 任务「排队」无统一约定** | 能力调用层满载直接拒绝不排队；任务层排队长等。同一业务两条路径两种背压行为 | `运行核心/能力调用/超时执行.py:51`；`运行核心/任务调度/任务进程.py:255,278-279` |

补充（同属协同、非背压）：启动路径的 `最大进程数` 拒绝返回 `(bool, str)` 而非错误码（`运行核心/运行环境管理器/提供者生命周期.py:331`），与「失败必带错误码」口径不同形。

---

## 4. 建议修法落点（精确到 文件:行，按优先级）

**P0 —— 把「忙」从「故障」里救出来（N1）**
1. `运行核心/统一网关/网关核心.py:956`：`任务系统.提交` 外包 `try/except 资源繁忙错误`，转成 `self._设置失败(响应, "资源繁忙")`，不要透传到 `:1054-1055` 兜底。
2. 同提交补两处登记：`运行核心/统一网关/网关核心.py:84` 起 `公开错误说明表` 增 `"资源繁忙"`（`_设置失败` 在 `:584` 用它取文案）；`运行核心/统一网关/本地网关.py:37` 起 `公开错误码状态映射` 增 `"资源繁忙": 503`（资源未就绪可稍后重试，**不要落 500**）。落地后须过 `开发工具/公开调用完整性门禁`。
3. `运行核心/任务调度/任务系统.py:213`：`进程池.提交` 同步转码，避免「异常只在网关层解一次」。

**P0 —— 端到端「可重试」显式化（N2 / N11）**
4. `运行核心/统一网关/网关核心.py:525`（字段区至 `:538`）与 `:540`（`转字典`）增 `可重试: bool`（协议只增不删，旧调用方不受影响）；`_设置失败`（`:584`）按码给默认值：`限流`/`资源繁忙`/`提供者不可用`/`外部不可访问`/`超时` → 真。
5. `运行核心/统一网关/本地网关.py:301-306`（线程满载信封）、`:558` `_拒绝`（边界拒绝信封）、`:984`（流式 429）同步补 `可重试`。
6. `运行核心/统一网关/流式HTTP.py:294-298`：组装失败事件时把源事件的 `可重试`（及已有 `状态码`）一并带上，而不是只取两键。

**P1 —— 退避提示（N3）**
7. `运行核心/统一网关/本地网关.py:307-313` 的 429 响应头加 `Retry-After`：限流层取窗口剩余（源 `运行核心/统一网关/限流器.py:84` `窗口秒`）；线程/执行线程层给固定建议值，常量放 `运行核心/能力调用/超时执行.py:110-111` 旁，保持单一来源。
8. 统一结果侧给等价字段：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1270` 的 `重试调度` 已算 `下次退避秒`，把它接进出站失败详情；消费方从 `模块库/直播逐字稿/实现/精校裁决.py:194` 的关键词白名单改为读该字段。

**P1 —— 统一上游 429 判定（N4）**
9. `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:382`：把 429 归入可重试，与 `支持库/适配层/模型HTTP提供者.py:152` 对齐；更好的做法是两处共用同一「状态码 → 错误码 + 可重试」映射函数，消除同表两写。
10. `支持库/适配层/模型HTTP提供者.py:132` 一带读响应头 `Retry-After`，作为第 8 条的输入。

**P1 —— 任务排队有界（N5）**
11. `运行核心/任务调度/任务进程.py:255,278-279`：给「已入队」阶段也设**总排队截止**（复用 `:160` `提交截止秒` 或新增独立参数），到点走 `:266`/`:273` 同一错误码；并把「排队超时」写进对外可读状态。
12. `运行核心/任务调度/任务系统.py:62` 的上限改为**可从装配注入**（`运行核心/任务调度/任务接入.py:54` 与 `运行核心/启动运行核心网关.py:53` 传值），避免「4 个进程」这种硬默认值藏在代码里。

**P2 —— 闸门与可观测一致性（N6 / N7 / N8 / N13）**
13. N6：给两个来源的 429 在 `错误说明`/`详情` 里带**层级标识**（「网关工作线程饱和」 vs 「网关限流：<维度>」）；已有可用信息见 `运行核心/统一网关/限流器.py:192,200,205` 与 `运行核心/统一网关/本地网关.py:304`。
14. N7：要么在 `运行核心/统一网关/网关核心.py` 的任务提交/流式分支接入 `进入任务`/`进入流式`（`运行核心/统一网关/限流器.py:233,253`），要么删掉这两个上限与参数，不要留「配置了不生效」。
15. N8：`运行核心/统一网关/网关核心.py:567` 的 `_幂等进行中` 加与 `:772,780` 同口径的上限与淘汰策略；或对 `请求id` 引入更强来源约束（当前入口只校验长度）。
16. N13：把「拒绝还是排队」上升为协议约定写进 `运行核心/统一网关/协议表.py`（现有 `"限流"` 条目在 `:96`），让同步调用与任务提交对同一资源压力给出可预期行为。

**P2 —— 提供者层收口（N9 / N10）**
17. N9：`运行核心/加载器/提供者隔离/独立进程.py:898` 的 `提供者进程池` 与 `平台控制面/提供者/进程提供者.py:267` 二选一（「同一件事两套实现即缺陷」），删掉的那份连测试一并收。
18. N10：把提供者并发准入从 `运行核心/运行环境管理器/提供者生命周期.py:331`（启动路径）延伸到调用路径，或明确写清「最大进程数不是并发上限」这一边界。

**已有可用件（修法直接复用，不必新造）**
- `可重试` 载体：`公共契约/基础类型/结果类型.py:40`。
- 满载拒绝完整范本（码 + 可重试 + 详情三件套）：`运行核心/能力调用/超时执行.py:501`。
- 退避算法：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1250-1270`。
- 有界队列「入队即占槽、上抛必归还」范本：`运行核心/任务调度/任务进程.py:232,352`。
- 限流拒绝留痕（拒绝记录 + 维度键）：`运行核心/统一网关/限流器.py:164`。
- 幂等键等待有界（唯一带「等多久」语义的分支）：`运行核心/统一网关/网关核心.py:763-764`。

---

## 5. 复算与证据完整性

- 全部 `文件:行` 由锚点脚本在**提交快照** `4bc164eff045433c07f6df899879233e318cc695` 上逐条复算：
  `unset PYTHONPATH && python3.14 /private/tmp/系统平台临时脚本_20260918/b8_verify_v2.py`
  → `锚点 198 个，缺失 0 个`（口径是 `git show <提交>:<路径>`，**不读工作区**）；明细落同目录 `b8_行号.json`。
- 报告自检脚本对正文里每条 `文件:行` 断言做越界检查（含简写文件名解析）：结果 **PASS**。
  命令：`unset PYTHONPATH && python3.14 /private/tmp/系统平台临时脚本_20260918/b8_check_report.py 开发文档/分析/端到端背压核对_20260918.md`
- 关键行人工抽检脚本（`b8_spotcheck.py`）逐条核对过正文与代码文本是否一致。
- 「未见实现」的判据都是**全仓 grep 0 命中**（已排除 `工程缓存/` 制品与 `__pycache__`）：
  `Retry-After`/`retry_after`（生产代码）、`进入任务`/`进入流式` 的调用方、`提供者进程池` 的调用方、
  `重试调度` 的生产调用方。
- 未做的事（如实声明）：**未跑运行时压测、未构造真实满载场景**验证「满载时调用方实际看到什么」。
  本报告是**静态证据核对**；动态复核可配合 `测试中心/运行核心/测试_超时执行线程准入.py:12`、
  `测试中心/第一批维修/测试_网关安全与有界并发.py:173`、`:193`。
- 快照会移动：核查期间 `HEAD` 在一天内变了多次（`8f2e2293 → 02a8b8d5 → c21f5b86 → 091174c5 →
  84b1b4cc → ff061387 → 55e6eb7c → 3e77becd → be2de855 → 4bc164ef`）。**引用本报告行号前，先跑一次
  `b8_verify_v2.py`（带上你实际的提交号）确认**；该脚本对每个锚点做存在性校验，漂移会以 `MISS` 直接报出。
- 附带发现（供主会话留意，不在 B8 范围）：工作区里 `运行核心/能力调用/超时执行.py` 与
  `运行核心/能力调用/唯一能力调用.py` 存在**未提交改动**（同一批锚点的提交行号 110/111/119/501/717 对应
  工作区 352/353/361/747/1022）。本报告一律以提交口径为准。
