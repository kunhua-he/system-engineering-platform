<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 审计：底座两条腿与冗余（批R 总账）

> 最后更新：2026-09-24 12:25 ｜ 口径：实测（每条都给现场取证命令或单据指针）
> 本件是**单入口审计落点**：汇总批R 全部发现与收口状态；明细以 17 份 `批R单据_R*.md` 为准，本件不另立结论。
> 配套：待办归 `开发文档/未完成事项.md`；上一版对照 `开发文档/分析/审计落点_两条腿与冗余_20260923.md`（2026-09-23 那轮）。

## 一、怎么用

- **两条腿**＝同一件事有两套实现（装配/验证/发布/运行任一面），违反单腿铁律。
- **冗余**＝同一个判定/映射/基线被多份表各写一份，必然漂移。
- **越界**＝写入腿不判「这条路径有没有被授权」，本质是代码 bug，不是纪律问题。
- 收口方向一律：**收成一个唯一节点，旧腿直接删**（不保留旧名别名/兼容壳）。

## 二、两条腿（架构级）

| 编号 | 发现 | 落点 | 收口 | 状态 |
|---|---|---|---|---|
| T2 | 能力搜索/契约查看两条 HTTP 腿（40006 只读网关 ↔ 40007 统一网关） | `开发工具/能力网关/能力网关.py` ↔ `运行核心/统一网关/传输/网关边界面.py` | 删 40006 整条腿，只留 40007 一处实现 | **已收口**（R-7，源码两文件已删） |
| T4 | 能力发现双腿：stdio 薄壳 vs 40006 只读网关 | 同上 ＋ `启动能力网关.sh` | 同上；能力发现现役只走 stdio 薄壳 `capability_search` | **已收口**（R-7） |
| T1 | 适配层提供者包与后端成对包各注册同一批能力 id | 例：`支持库/适配层/Git提供者` ↔ `支持库/后端/版本控制支持库/Git操作` 各 14 条、后缀同名 14/14 | 后端腿降垫片（能力面收口，同一动作 `能力搜索` 只回一条） | 部分（实现已一条，能力面待收） |
| — | 进程回收流程 4 处各写一份「终止→宽限→强杀→复查」 | `句柄体系:341`、`浏览器自动化:272`、`模型连接器:625`、`本地启动准备与守卫:581/:708` | 全收 `公共契约/运行时/进程终止.py` 的 `结束并留痕`（唯一入口） | **已收口**（R-6 + R-11：自写升级顺序 5 → 0，薄委托 15 → 0，`结束并留痕(` 调用点 4 → 49） |
| — | textutil 腿终止回调直调 `强制结束子进程`，绕过唯一入口 | `支持库/适配层/textutil提供者/实现/文本转换.py:95` | 改走 `结束并留痕`；两份 `生命周期契约.json` 与后端腿 docstring 同批去 `_终止进程组` | **已收口**（R-17） |
| — | 编译/测试入口旁路（散装节点） | 见 R-9（内部层第二面闸） | 旁路转内部能力，两面一起撤 | 部分（生成侧过滤待落地） |

## 三、过度冗余（同一判定多份表）

| 编号 | 冗余面 | 收成唯一节点 | 状态 |
|---|---|---|---|
| U-1 | 存量基线收敛计算多份 | `开发工具/存量基线消费.py`（`分桶与收敛`） | **已收口**（批Q） |
| U-2 | 路径传参口径说明 103 条 | `公共契约/基础类型/字段名册.py::取路径写法口径` | **已收口**（Q-h） |
| U-3 | 第二套路径解析 38 处 | 全平台唯一解析腿 `解析路径` | **已收口**（Q-h2 / R-15 顺带 1 处） |
| U-4 | 生成期绝对路径注入 5 处 | 锚目录判据（不再写死源码树路径 / 不再 `parents[N]` 推根） | **已收口**（Q-f） |
| U-5 | 解释器选择 4 种写法 | `sys.executable` 归队（`python3.14` 为优先项、非强制项） | **已收口**（批P） |
| U-6 | 32 份转调垫片内联加载块 | 统一加载腿 | **已收口**（R-5） |
| U-7 | 提供者可用性探针 20 处 | 提供者探针唯一节点 | **已收口**（R-3） |
| U-8 | 判据多份表（类型短名映射 3 / 正式类型表判定 5 / 说明书对账 2 / 场景 `$动态` 2 / 包必备件 3 / 存量基线骨架 5） | 各自门禁出口重建 | 部分（Q-i） |

## 四、越界（写入授权）

| 编号 | 发现 | 落点 | 状态 |
|---|---|---|---|
| X1 | 写入腿只判「是不是经 MCP」，不判路径有没有被授权 | `文件操作/实现/文件系统.py` 写入分支 | **已收口**（批1 层1：`公共契约/运行时/写入授权.py` 落地） |
| X2 | 文本补丁腿显式声明不管租约（声明与实现两套口径） | `文本补丁/实现/文本补丁.py:11` | **已收口**（声明同批改口） |
| X3 | 文档写入腿同样无授权判据 | `开发工具/MD文档生成/人工编辑.py` | **已收口** |
| X4 | 内核只读锁对写入腿不构成授权门 | `公共契约/运行时/仓库只读锁.py` | **已收口**（批1 层3：`临时解锁` 纳入同一判据） |
| X5 | 租约认领是自证的，无人校验授权范围 | `文件租约申请.py` | **已收口**（批1 层2：派活登记 `{派活标识, 允许修改目录, 只读, 过期时刻}`） |
| — | 整仓内核锁从「人工开关」升级为**租约驱动**（开工即占 ⇒ 解锁；收工/过期 ⇒ 回锁）＋ 解锁只认网关凭证 | `仓库只读锁.py` / `文件租约申请.py` / `文件租约维护.py` / 新增 `模块库/自修复工具/实现/仓库锁.py` | **已收口**（`f9992120`，zcode 会话承接任务） |

## 五、批R 单据索引（17 份，R-1..R-17）

| 单据 | 主题 |
|---|---|
| `批R单据_R1_网关协议面_20260924.md` | 网关协议面 |
| `批R单据_R2_解释器口径_20260924.md` | 解释器口径 |
| `批R单据_R3_提供者探针_20260924.md` | 提供者探针（U-7） |
| `批R单据_R4_冗余与存量产物_20260924.md` | 冗余与存量产物 |
| `批R单据_R5_垫片收口_20260924.md` | 转调垫片收口（U-6） |
| `批R单据_R6_进程回收_20260924.md` | 进程回收 |
| `批R单据_R7_架构两条腿_20260924.md` | 架构两条腿（T2/T4，删 40006 腿） |
| `批R单据_R8_架构级设计方案_20260924.md` | 架构级设计方案 |
| `批R单据_R9_内部层第二面闸_20260924.md` | 内部层第二面闸（旁路转内部） |
| `批R单据_R10_删腿收尾_20260924.md` | 删腿收尾 |
| `批R单据_R11_进程回收收尾_20260924.md` | 进程回收收尾（零孤儿实证） |
| `批R单据_R12_账本与返回体积_20260924.md` | 账本与返回体积 |
| `批R单据_R13_终端绕过根因_20260924.md` | 终端绕过根因（注入面缺口） |
| `批R单据_R14_注入面收口_20260924.md` | 注入面收口（工具姿势第 31 条硬纪律） |
| `批R单据_R15_批量取证_20260924.md` | 批量取证（读取文件批量腿 / 正则搜索紧凑档） |
| `批R单据_R16_开工资料一次取全_20260924.md` | 开工资料一次取全（`资料级别`/`资料预算`） |
| `批R单据_R17_默认档送达_20260924.md` | 默认档送达（0 跳工具描述指针）＋ textutil 漂移 |

## 六、R-18：全仓静态编译红收口（本批，单据并入本件）

收尾链第一次 `静态编译`（`类型=0`）读数为 **成功 15 / 失败 5**，3 条判红逐条归因后收口：

| # | 判红 | 根因（实测） | 收口 |
|---|---|---|---|
| 1 | 契约漂移 `文件系统支持库.内容检索.正则搜索` / `.文件操作.读取文件` | R-15 只改了 `能力定义.json`（声明面），派生件 `能力契约/参数契约.json` 未重算 | 走 `静态编译`（它内含 `开发工具.开发编译口.编译口`）重算派生件 |
| 2 | 公开调用完整性 **11 项新增违规**（全是 `说明书-缺参数` / `说明书-必填不一致` / `说明书-能力级版本不一致`） | 4 份说明书登记为「**人工类**／生成器不得覆盖」⇒ 两条生成腿都到不了，载位只能手改；且门禁读的是**顶部散文载位块**（`- 参数列表：`/`- 可选参数：`），不是下面的 markdown 参数表 | **手改 4 份说明书 10 处**：`内容检索`（`- 参数列表`/`- 可选参数`/`- 调用示例` 补 `紧凑返回`）、`文件操作`（`文件路径` 必填 是→否 ＋ 补 `路径清单`/`区间清单`/`项目根` 三行）、`开工编排`（版本 1.4.0→1.5.0 ＋ 补 `资料级别`/`资料预算`）、`能力目录`（版本 1.1.0→1.2.0 ＋ 补 `带明细`/`样例条数` ＋ 散文「三个现场读数」→「四个」）。复跑 ⇒ **门禁通过** |
| 3 | 规范漂移 4 份规范 6 条（点名已删的 `开发工具/能力网关/能力网关.py`） | 豁免词表 `开发工具/规范漂移体检.py:77` 有 `已删除`、**没有 `已删`** 词形，而 R-7 删腿后的文案写的是「两文件**均已删**」⇒ 假红 | 词表 `已删除` → `已删(?:除)?`。复跑 ⇒ **漂移 0 条** |

**方法论（本批两次踩坑，供后续复用）**：

1. `静态编译` 是**单写者**（同树已有编译口在跑 ⇒ 拒启 fail-closed）。**不许并发派**：4 路并发里 3 路直接被拒启（返回 `未核验`）。
2. 门禁读的载位**不是**你能看到的那张表 —— `公开调用完整性门禁` 逐能力读**段内散文载位**（`- 参数列表：`），下面的 markdown 参数表只当备选。改说明书前先跑 `_解析说明书` + `_段内载位` 看它到底读了哪几行。

## 七、剩余未收口（不进本批写集，归 `未完成事项.md`）

- **T1 能力面收口**：适配层提供者包与后端成对包同名能力 id（Git 14/14、FFmpeg 7/7、Pillow 9/9、PyMuPDF 4/4、Tesseract 3/3）—— 实现已一条，能力面仍两条。
- **内部层生成侧过滤（审计 T8）**：`正式包索引.构建索引` 的 `能力所有者` 未过滤 `内部层` ⇒ 16 桶 / 69 条内部层能力仍落在页面可绑定集内（现为存量基线，只报不拦）。
- **`参数名写错静默剔除` 与「逐字登记的未知字段一律忽略」的冲突**：网关现已回带 `被忽略参数` ＋ `最接近的契约参数`（本批实测：传 `路径清单` 给 `查询文件租约` 即被报出），但「是否直接删除」仍需一次口径裁决。
- **`系统平台_修MCP自身=1` 的收紧边界**：只在「解锁」腿收紧，写文件腿仍放行（有意保留修 MCP 后路）。

## 八、本批验收读数

- `静态编译`（`类型=0`）：见收尾链末次读数（本件更新于 12:25，编译任务 `23e4c53729e64cfe`）。
- `公开调用完整性门禁`：**通过**（六环一致 + 说明书字段一致 + 错误码登记环一致，无新增违规）。
- `规范漂移体检`：**漂移 0 条**（扫 28 份规则文件）。
- `契约漂移`：随 `静态编译` 派生件重算后复测。
- 租约：全部 `收工释放`，活跃数 0。

## 九、R-17 收尾链补记（2026-09-24 12:26–12:36）

- **新收阻断红 1 条**：`模块库/自修复工具` 注册镜像漏改已删参数 `异步`（`__init__.py:96`）⇒ `静态编译` 契约漂移**阻断**；删该行后复跑 **阻断 0**。同类复核：`能力定义.json`／`参数契约.json` 里残留的「异步」全是说明散文，非参数声明。
- **措辞裁决落地 1 条**（华哥 2026-09-24）：「未知字段一律忽略」→「**未对齐参数名一律剔除**」（`运行核心/统一网关/核心处理/网关参数校验面.py:260` ＋ `运行核心/统一网关/本地网关.py:395`），行为逐字不变、只改登记措辞。
- **制品与运行态已同根**：制品 `23d9c42552f35e5d` 构建＋安装成功（来源提交 `23ec6a2c`＝HEAD，来源绑定通过）；网关重启 PID 52871→5464、就绪 3.55s、健康检查 200、能力数 752、装配告警 0、`运行态同根` 真。
- **实测生效证据**：`读取文件` 批量腿（`路径清单`＋`区间清单`）**一次调用回 2 文件切片** ⇒ R-15 生效（重启前该参数在网关侧不存在）。
- **新红 1 条（阻断上线，已登记）**：`测试中心.模块库.测试_自修复工具` 3 例判红 —— 根因＝`写入授权` 判据要求受管路径持活跃写租约，而 `自修复工具.创建修复工作区` 直写 `修复目标.txt` 不持租约；`运行开发编译口 上线=真` 按「红的不上线」跳过上线（制品与重启已另腿完成）。归 `未完成事项.md` 第 8 条。
- 提交 `23ec6a2c`（17 文件）已推送 `origin/主干`；租约全部 `收工释放`。

## 十、R-23 补记：账本去污染 ＋ 返回体量维 ＋ 常用意图扩映射（2026-09-24 12:46–12:55）

> 开工ID `开工-20260924-124626-2185`｜提交 `b9eeaea3`＋`777147f6`｜制品 `188db769d66119dc`｜单据：`批R单据_R23_账本去污染与体量维_20260924.md`

**三条「有能力却零调用／有数据却无落点」的底层缺陷，本批各收一条：**

| 缺陷（R-22 清点） | 根因落点 | 修法（单节点） | 改前 → 改后实测 |
|---|---|---|---|
| 账本被测试探针污染：形状 `__事实源探针__:逻辑型` **752 条 / 23,914 次 = 账本 51.8%**，把 710/796 个能力的「首选常用参数」顶成哨兵 | **写侧**：`测试_能力唯一事实源.py` 起的进程内测试网关把探针记进**生产**参数账 | `公共契约/诊断/调用账本.py` 加**保留键名**判据（`__xxx__` 是平台保留命名空间、不是业务参数）：写口不入账 ＋ 读口不计入（存量不删，审计原样保留）；测试侧用**既有** `系统底座_调用参数账` 把账改道到临时文件 | `常用参数组合(办公文档支持库.文档生成.文档统计)` 第 1 组：`__事实源探针__:逻辑型`(32 次) → **`文件路径:文本型`**(2 次)；组合数 3 → 2 |
| 体量维**无权威落点**（F1 第二步未做，审计里只有耗时、没有返回字符数） | `运行核心/运行诊断/安全审计` 记录里缺字段 | `安全审计.记录()` 加 `返回字符数`（**只记字符数、不记正文**），唯一产生点在 `网关处理面`（与薄壳 `返回上限字符` 同源口径）；读口 `返回体量报告()` 与 `慢调用报告()` 同源同形制 | 字段不存在 → `安全审计.jsonl` 尾三行均含 `返回字符数` |
| 14 条「不知道有」零调用能力（Git 11 ＋ 系统信息 3）：agent 高频在终端跑 `git status/rev-parse/worktree`、`top/df` | 0 跳注入面的唯一事实源 `公共契约/能力契约/常用意图.py` **只有 4 条映射** | 只改这一个文件（`工具清单.py` 描述与 `开工准备.资料` 都从它渲染，禁手抄第二份），补 4 条意图 | `开工准备.资料.能力映射` 4 条 → **8 条**（含「别在终端跑 git status/log」等限定） |

**同一件事的两面**：账本污染同时是「测试不写生产运行数据」这条通用纪律的缺口 —— 故**没有**在调用点打补丁，而是：判据收在唯一节点（`调用账本`），隔离用唯一节点已支持的既有环境变量（`运行缓存`/`调用账本` 的 `系统底座_*`），**零新增机制**。

**本批门禁**：全仓 `静态编译` 两次均 **成功 18 / 失败 2 / `失败门禁=[]`（无阻断）**；两条只报红＝`md查重`（旧账）＋`制品新鲜度`（重建后消）。制品 `188db769d66119dc` 已安装激活、网关已重启、`常用参数组合` 改后读数即生效证据。

**本批新登记（详见 R-23 单据 §六）**：① `开工准备.资料.租约查询问题` 非空（`租约腿查不出来`，未归因）；② 体量维无能力面读口（`返回体量报告` 与 `慢调用报告` 同为进程内方法）；③ `常用传参` 仍混薄壳级 `项目根`（老账，保留键名规则不覆盖）；④ 0 跳描述要等 MCP 重连才可见；⑤ 未跑定向测试（`测试_自修复工具` 3 例无测试级证据）；⑥ `未完成事项.md` 在验收期被 R-21 会话持租约，**未抢**，本批结论只落本件 ＋ R-23 单据。

**R-19 第二半（同批收口，`777147f6`）**：`公共契约/运行时/仓库只读锁._推断仓库根` 判据由 `.git` **存在**改为 `.git` 是**目录** —— `git worktree` 根的 `.git` 是指针**文件**，原判据把隔离工作区算成「仓库根」⇒ 工作区里写盘要一条**永远认领不出来**的写租约 ⇒ `写入授权` 结构性 fail-closed（`测试_自修复工具` 3 例假红根因）。改后真仓库（含测试夹具假仓库）判据一字不变，链接工作区/子模块落回「仓库外」，与本模块文档承诺一致。

## 十一、主会话收口补记（2026-09-24 12:36–13:00）

### 1. 薄壳「超限只回指针」→「指针 ＋ 裁剪缩影」（本轮实测生效）

- **落点**：`开发工具/薄壳/薄壳服务.py::_裁剪值` 的 `if 指针:` 分支。该文件回执库注释的**第 ③ 条本来就写着**「裁剪仍然照做并如实标注『这是回执的缩影，不是全量』」，但**实现里没做** —— 只回指针、不回缩影（注释与实现两套口径）。
- **后果（现场根因）**：任何返回值超 `返回上限字符` 的调用，调用方拿到指针后**必须再发一次取回请求**才知道够不够。这就是华哥点名的「访问了一次，然后读取了一下发现不够，再访问几次」，也是子代理改道终端的诱因之一（薄壳默认闸 2,000 字符 vs `开工准备` 默认档实测 7,432 字符）。
- **修法**：超限且落盘成功时，按 `上限字符 - 指针开销 - 说明预算` 给**裁剪缩影**留预算（`_缩到限内` 先缩长文本叶子、**容器与键序一字不动**，缩不动再 `_兜底裁剪`），回 `{**指针, 值: 缩影, 这是缩影, 裁剪后字符数, 截断说明?, 丢弃说明?, 未能压到限内?}`；`说明键优先级` 增 `这是缩影`（紧随 `已裁剪`，永不被丢）。
- **live 读数**（本轮一次真实调用，`返回上限字符=600`）：`值={释放数: 1}` ＋ `这是缩影="本次回的是裁剪结果，不是全量；全量见 回执 + 取回"` ＋ `裁剪后字符数=10` ＋ `截断说明` ＋ 回执指针 ⇒ **一次调用即可判断够不够，全量仍由指针指得住**。
- **边界**：`未能压到限内` 仍如实回带；指针仍是全量的唯一入口；缩影**不是**全量（键名直接写明，不靠调用方悟）。

### 2. `开工准备.资料` 默认送达已 live

- 实测（`开工即占` 返回里的 `开工上下文.资料用量`）：`资料级别=指针 / 资料预算=8000 / 资料段字符数=1897 / 总返回字符数=9174`；`能力映射` 已由 R-23 扩到 **8 条**（含「别在终端跑 git status/log」等限定）。
- 结论：华哥「1 次运行拿到开工所有资料」**已在网关侧成立** —— 默认档即带 `资料`，不必再传 `资料级别`（R-16 引入 → R-21 改默认送达 → R-23 扩映射）。

### 3. R-20 未执行 → 已重派（派活纪律教训）

- 现象：先前派给 R-17 那一路的 R-20 任务信**没有被处理**（该线程收到的仍是 R-17 信），盘上 T1/T8 **零产出**；已按那份「现状登记」单据（含 T1/T8 权威落点）**重派**。
- 教训（写进派活纪律）：**复用旧子代理线程派新单不可靠**；且交付物必须是**盘上可读的单据** —— 只靠子代理回传文字不行（主会话读不到部分线程的 turn 内容）。

### 4. 本轮收尾时仍在跑

`R-19`（自修复工作区写授权第二半，已随 `777147f6` 收口）、`R-20`（重派：T1 能力面合并 ＋ T8 生成侧过滤）、`R-21`（单据落盘）、`Q-k`（3 条只报告判红）、`R-22`（零调用清点，**只读**，已交付）。

## 十二、主会话裁决与收尾（2026-09-24 13:40｜本节经平台写入能力落盘）

> 本节的每一条都是**主会话按华哥口径拍的板**（「如果是两条腿的就合并成一条」「有统一节点的全部切成统一节点」「不保留旧腿」「静默一律改报错」），
> 明细在对应的 `批R单据_R*.md`，本件只记裁决与读数。

### 1. 裁决：`R-19` 偏离我给的方案① —— **我认它的根因，方案①作废**

- 我原裁：`自修复工具` 自行认领租约（对齐编译口「按写者豁免」形状）。
- `R-19` 实测推翻：真根因在**判据** —— `公共契约/运行时/仓库只读锁._推断仓库根` 用 `.git` **存在**判仓库根，而 `git worktree` 根的 `.git` 是**指针文件** ⇒ 隔离工作区被当成仓库根 ⇒ 写盘要一条**永远认领不出来**的租约 ⇒ 结构性 fail-closed。
- **裁决**：改判据（`.exists()` → `.is_dir()`）**优于**我的方案①，因为①是**第二个豁免机制**（编译口已有一处），违背「有统一节点就切统一节点」；且①治不了 `test_零残留`。
- 读数：`测试_自修复工具` `......F...FF`（3 红）→ `动态编译` **阻断 0**；已随 `777147f6` 入库。

### 2. 裁决：`T1` 能力面两条腿 —— **不重复动手**

- 另一会话（`开工-20260924-130452-b2c2`）已持租约承接 T1（适配层 vs 后端成对包同名 id：Git 14/14、FFmpeg 7/7、Pillow 9/9、PyMuPDF 4/4、Tesseract 3/3）。
- **裁决**：本批**不碰** T1 任何路径（两个写者同改一套包必冲突）。T1 以该会话单据为准。

### 3. 裁决：`T8` 内部层生成侧 —— **由「只报」改「拦截」（已收）**

- 读数：`能力所有者` **752** ／ `页面可绑定能力` **683** ／ **被过滤 69 条**，绑定集 ∩ 被过滤项 = **空**；与权威清单「16 桶 / 69 条」逐字相符，页面真引用现场 0 条 ⇒ 两面一致。

### 4. 裁决：`md查重` 退出码结构上不可达 —— **修射程，不改角色**

- 事实：判据把「同一生成器模板产生的同名产物」当成重复（`目录树与命令.md` × 同模板那份，相似 0.88，文件内重复行 **0**）；全仓只要任意一对 ≥0.40 就 `return 1` ⇒ **退出码 0 结构上不可达**。
- **裁决**：把「同模板同名产物 / 模板语汇相似 / 同包同源」列为**正当原子差异**（同 `U-7` 那种正当原子差异），在**判据自己的权威节点**上豁免。
- 读数：`退出码 1` → **`退出码 0`**（578 份参与、正当原子差异退出比对 866 对）；**不放宽阈值、不动生成物与生成器、不靠登记清单**；真重复（异名异包、逐字相同）仍判红（`1.0 / 0.8819 / 0.8152` 三对）。

### 5. 裁决：编译口「被锁就拒启」—— **改有界排队**

- 事实：`R-19`/`R-23` 因单写者锁被活进程持有，**6 次拒启 ＋ 1 次 120 秒超时**，读数根本取不到；且父进程被超时杀掉后**子进程变孤儿继续占锁**（主会话实测 PID 76141，PPID=1，跑了 9 分 30 秒）。
- **裁决**：华哥口径「资源无限、只要回收干净」⇒ 单写者锁**保留**（防写冲突），但**「被占就拒启」改有界排队**。
- 读数：缺省排队上限 **900 秒**（依据 `R-22` 实测静态编译 max 90,220ms ＋ 单轮 2~4 分钟）；真并发等到 **62 秒**拿到锁（改前＝拒启）；超时说明含 `PID／启动于／已等待 N 秒／建议①②③`；排队期不建句柄（无泄漏）；传 `0`＝旧行为。已随 `06386bc3` 入库。
- **主会话当场处置**：孤儿锁 PID 76141 已按进程组回收，并复测 `静态编译` 异步腿**阻断 0**（13:03）。

### 6. 裁决：`执行命令` 无外壳 = **子代理偏好终端的机械根因**

- 事实：`命令` 声明文本型却按 argv 拆、不过 shell ⇒ `&&`／`|`／`2>&1`／`unset` **全静默失效**（`ls … 2>&1 | head -20` ⇒ `ls: |: No such file or directory`，还回带**未过滤的完整 /tmp 列表 24,244 字符**）。通用纪律明令 `unset PYTHONPATH`，该能力做不到 ⇒ 与「工具姿势第 31 条：凡可经 MCP 拿到的信息不得用终端取」**互相打死**。
- **裁决**：静默失效**一律改明确报错**（华哥口径：未对齐就报，不许静默），同时给**出口** `经shell=真`。
- 读数：改后同命令 ⇒ `参数不合法`，点名 `>、|` 并指路 `经shell=真`；`经shell=真` ⇒ `退出码 0`、正好 3 行。

### 7. 裁决：`正则搜索` 超预算**丢正文留元信息** —— **改正文优先**

- 事实：裁剪按「丢尾键」走 ⇒ 最长的 `命令`（rg argv 约 800 字符）**留下**、`匹配列表` **整段消失**（`2262 → 30`，两次检索白跑）。
- 归因更正：落点在**薄壳**（`_兜底裁剪`）**不在** `内容检索.py`。
- 读数：同一输入/上限 2000，`7273 → 缩影 24 字符`（改前，只剩 `引擎`/`搜索根`）→ `4974 → 缩影 {匹配列表: 18 条} 1840 字符`，`丢弃说明＝已省略元信息键：引擎、搜索根、命令`。唯一节点 `元信息键` 已写死，后续按同判据扩表，不许各包自建。

### 8. 裁决：传参口径「相对还是绝对」—— **写死，减少猜测**

- `返回上限字符` ＝ **信封上限**（不解除能力自身截断）；`回执触发字符数 = 默认上限×4 = 8000` 与默认档**解耦**；指针 525→435 字符、缩影预算 1232→1399。
- 路径口径唯一：**普通路径参数一律写项目根相对路径**；只有 `项目根` 这类**基准**参数写绝对。

### 9. 主会话自纠：整仓内核锁误操作 —— **已回退，且比操作前更干净**

- 我误跑了不带 `--补齐` 的上锁 ⇒ 锁掉 **23 条别的会话的活跃租约开窗**。
- 处置：MCP `自修复工具.仓库锁(动作=下锁)` → 再 `运维脚本/仓库锁.py --补齐`（只补真缺口、跳过开窗）。
- 复核：`已锁 5707｜未锁 32（活跃租约开窗 32｜真缺口 0）` ⇒ **别的会话写窗口全部保住，真缺口 0**（操作前是「真缺口 1」）。

### 10. 剩余项（**不在本批**，已在 `未完成事项.md` 登记）

- `T1` 能力面两条腿（另一会话在飞）；`OCR识别支持库` 公开调用完整性（他会话在飞）。
- 长期只报红：`制品新鲜度`（出口＝重建制品＋重启网关，发布动作）。
- 三个红模块待归因：`测试_注册口径漂移`（存量）、`测试_编译口未核验与门禁清单`、`测试_薄壳返回可控`（与薄壳换壳缺陷同族）。
- **已裁决：不成立** —— `文本补丁.批量应用精确替换` **持** `写入授权` 闸（`R-27` 现场核对：持租约写 ⇒ 通过；不持租约写 ⇒ `越界`）。`R-24` 终稿那条「无闸」是误判 —— 它那 29 条编辑**全部持有对应文件租约**，所以闸是放行的。`未完成事项.md` 的 R-27 节已同口径复核更正。

## 十三、收尾验收读数（2026-09-24 13:45，主会话实调）

> 顺序：提交 `9bceab23`（已推 `origin/主干`）→ 重建制品 `78ea93ab8139abf3`（安装=真）→ 重启网关 `com.huashi.gateway-40007`（PID 74399 → 44096，就绪 4 秒）→ 逐项实调。

| # | 验收项 | 读数 | 判定 |
|---|---|---|---|
| 1 | **全仓 `静态编译`** | 成功 **20**／失败 **0**／检查项 20／五态结论 **干净通过**／退出码 0（改前 18/2 只报告） | ✅ **首次干净通过** |
| 2 | `开工准备` **默认档一次取全** | 不传 `资料级别` 即回 `资料` 段：`资料级别=指针`／`资料预算=8000`／**资料段 1,488 字符**／`是否超预算=false`／总返回 9,565 | ✅ 华哥「1 次运行拿全开工资料」成立 |
| 3 | `租约查询问题` **误报** | 不传 `修改路径` ⇒ 该键**不存在**（改前回「租约腿查不出来」） | ✅ 误报已消 |
| 4 | 0 跳「常见意图→首选能力」 | **8 条**，含「看Git状态 → `版本控制支持库.Git操作.当前状态`：**别在终端跑 git status/log**」 | ✅ 终端偏好被正面对冲 |
| 5 | `执行命令` **shell 语义** | `ls /tmp 2>&1 | head -3` ＋ `经shell=真` ⇒ 退出码 0、标准输出**正好 3 行**（改前：`ls: |: No such file or directory` ＋ 未过滤的完整 /tmp 列表 24,244 字符） | ✅ **终端绕过的机械根因已修** |
| 6 | `正则搜索` **明确报错** | 传文件路径当目录 ⇒ `目录不存在` ＋ 回带**解析后的绝对路径**（改前：`匹配列表` 静默消失、0 命中） | ✅ 静默已改报错 |
| 7 | `读取文件` **批量腿** | 一次调用回 **2 文件切片**（`路径清单` ＋ `区间清单`） | ✅ 批量取证真生效 |
| 8 | 制品新鲜度 | 全仓编译里**不再出现**（改前恒为唯一只报红） | ✅ 转绿 |

**结论**：华哥要的「纯用 MCP」有了机械抓手（第 4／5 条）＋「1 次运行拿全开工资料」已在默认档成立（第 2 条）＋上下文不撑爆（资料段 1,488 字符，超预算降级阶梯仍在）。

**仍未收**（都不在本轮写集，已在 `未完成事项.md` 登记）：`T1` 能力面两条腿（他会话在飞）／`OCR识别支持库` 公开调用完整性（他会话在飞）／三个红模块待归因（`测试_注册口径漂移`、`测试_编译口未核验与门禁清单`、`测试_薄壳返回可控`）／`元信息键` 扩表与 `_裁剪值` 回归测试（`R-27` 债务 #2／#3）。
