# 批L 清单 · 同类问题两轮扫描（类一：只读入口带写副作用｜类二：容器级事实存成对象字段）

> 开工ID：`开工-20260924-043407-4bfb`（修正版重写：`开工-20260924-044939-20ee`）
> 口径：**只读扫描**（未改任何实现）；每条带现场行号与可复制命令
> 上游：`开发文档/分析/批K单据_任务终态与同类问题_20260924.md` §六（类一/类二的判据来源）
> 扫描范围：全仓 `*.py`，排除 `工程缓存/`、`**/__pycache__/**`、`开发文档/归档/**`、`测试中心/**`
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


---

## 类一 · 只读入口带写副作用

**判据**：函数名/语义是**只读**（查询/读取/获取/检查/查看/取），函数体内却调用了**写入口**（登记/写入/落盘/保存/驱逐/创建/删除）。

### 首例（已在批K 证真）

| 文件:行 | 只读入口 | 写副作用 | 后果 |
|---|---|---|---|
| `运行核心/任务调度/任务进程.py:424-438` | `任务进程池.查询`（磁盘快照重建，本是只读回退） | `:435` 调 `_登记任务表`（唯一写入口）⇒ 触发 `_驱逐历史任务`（`:349-370`） | 查询挤掉本会话活动条目 ⇒ 门面终态永久回写不出去（详见批K 单据） |

### 第一轮找全（AST 扫描，非抽样）

扫描器：`/tmp/扫_只读入口带写副作用.py`（只读，遍历全仓 AST，取「函数名以 查询/读取/获取/检查/查看/取/读/列出/列举/探测/检视 开头」且「体内存在调用名含 登记/写入/落盘/保存/驱逐/删除/移除/清空/提交/标记/安装/卸载/迁移/搬迁/刷新/更新/创建/销毁/释放/关闭/回收/淘汰/裁剪」」）

复跑命令：

```bash
python3 /tmp/扫_只读入口带写副作用.py
```

结果：**24 处候选**。逐条人判后分三类：

#### 真命中 / 待证真（3 条）

| # | 文件:行 | 只读入口 | 写副作用 | 现场读数 | 严重度判断 |
|---|---|---|---|---|---|
| L1-1 | `支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py:399` | `查询运行态`（docstring：按域**查询**运行态） | `:417` `_创建运行库表(连接对象, 补齐列=False)` —— **纯查询入口在连接上跑建表 DDL** | 入口打开连接后第一件事就是 `_创建运行库表` | 与首例同形：查询带写副作用。**待证真**：库为只读/无写权限时，查询是否失败；以及是否与 `_已建表` 守卫（类二 2-5）叠加成「每次查询一次 DDL 写事务」 |
| L1-2 | `运行核心/任务调度/任务系统.py:311` | `任务系统.查询`（门面查询） | `:311-312` `if 任务对象.状态 in _终态: self._保存已加锁()` —— **查询触发门面落盘** | 与批K 现场同行（`底座运行.db::任务` 该行 `状态=成功`、`完成时间=2026-09-24 04:04:52`、`结果=None`） | 可能是「状态落了、结果没落」的现场通道。**待证真**：`_保存已加锁` 落盘的是 `任务对象.转字典()` 还是部分字段 |
| L1-3 | `支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py:340` `读取历史` / `:492` `查询会话` | 两个只读入口 | 调 `_更新活动`（写「活动」时间/状态） | 未展开（本轮只到 AST 命中） | **待证真**：只读接口在存储上写「最后活动」是否必要；若存储只读或加锁，读接口是否被写路径拖失败 |

#### 排除（命名假阳，非写副作用）

`公共契约/能力契约/调用器.py:168 获取能力调用器→标记装配中`（惰性装配，设计如此）、`公共契约/运行时/取消登记.py:127 取消→_登记留痕`（取消本身是写动作）、`运行核心/任务调度/任务系统.py:335 取消`、`运行核心/任务调度/任务进程.py:483 取消`（同上）、`支持库/后端/系统核心支持库/资源管理/实现/资源管理.py:417 获取→_回收残留`（抢锁时回收崩溃残留，`资源短锁` docstring 已定三级判定，设计如此）、`支持库/后端/浏览器自动化支持库/实现/浏览器自动化.py:111 获取截图→创建句柄/登记资源`（截图本身即资源，必须登记）、`支持库/后端/代码解析支持库/` 下 `仓库地图/取仓库地图.py:32`、`代码地图/查询关系.py:32`、`查询状态.py:38`、`查询节点.py:29`（`关闭代码地图`＝释放自己打开的连接，非业务写）、`开发工具/开发编译口/生成物联动.py:176`、`公共契约/诊断/写入流水.py:275,311`、`平台控制面/包仓库/版本仓库.py:105`、`平台控制面/提供者/版本兼容.py:70`、`平台控制面/需求登记/实现/能力入口.py:161`、`开发工具/公开调用完整性门禁.py:474,597`、`开发工具/测试体系门禁实现/可导入性检查.py:57`、`运行核心/运行状态监督/实现/提供者.py:67`（以上均为「调用名里含写词但实为读/判据/构造」）。


---

## 类二 · 容器级事实被存成对象字段

**判据**（两条同时满足）：① 字段语义是**容器级**（本会话/本进程/本轮/首次/已初始化/已告警/已迁移…），不是对象自身内容；② 该字段所属的类**有重建路径**（从磁盘/快照/账本重建，`__init__` 不再赋该值）。

### 第一轮找全（口径 + 命中 5 条）

扫描口径（4 条只读命令）：

1. 命名面：`rg -o --no-filename "self\.[^\s=(){}\[\],:'\"#]+" -g '*.py' -g '!工程缓存/**' -g '!**/__pycache__/**' -g '!开发文档/归档/**' -g '!测试中心/**' | sed 's/^self\.//' | sort -u` → **2253 个实例字段名**，再按容器语义正则过滤
2. 赋值面：`rg -n "self\.[^\s=]*\s*=\s*[^=]"` + 容器语义过滤 → 仅 3 处
3. 重建原语面：`rg -n "for 键, ?值 in .*\.items\(\):\s*$|setattr\(|vars\(|__dict__|__new__\(|object\.__setattr__|def 从[^ (]*\(|def 恢复[^ (]*\(|def 反序列化|__setstate__|__getstate__|__reduce__"`
4. 重建点全集：`rg -n "def 从[^ (]*\(|def 恢复[^ (]*\(|def 反序列化"` → 逐条读被重建类的字段定义

#### 真命中（两条判据都满足）5 条 / 7 个字段

| # | 文件:行 | 类.字段 | 容器级语义 | 重建路径（文件:行） | 失真后果 |
|---|---|---|---|---|---|
| **2-1 ★** | `运行核心/任务调度/任务进程_对象.py:180` | `独立任务.本会话活动` | 本会话是否真起过工作进程 | `任务进程.py:430`（`json.loads` 快照）+ `:432-434`（`for 键,值 in 数据.items(): setattr`）；`转字典()`（`任务进程_对象.py:196-206`）**不含** | **可达 · 现场已发生**：重建对象恒为假 ⇒ `任务系统.py:279` 回写闸门永久关闭 |
| 2-2 | `运行核心/任务调度/任务进程_对象.py:171` | `独立任务.结果已收取` | 每任务最多收取一次结果（B-07 单读者仲裁） | 同上；`转字典()` 不含 | 同形但**不可达**：读者仅 `任务进程_监视轮询面.py:115`（走 `活动任务表` 活对象）；重建对象不入该表，且 `接收连接`（`:165`）同样不入快照 |
| 2-3 | `运行核心/任务调度/任务进程_对象.py:178` | `独立任务.回收者` | 哪个线程持回收权（并发闸） | 同上；`转字典()` 不含 | 同形但**不可达**：读者仅 `监视轮询面.py:99`、`回收落盘面.py:84,96,100`（都走 `活动任务表`） |
| 2-4 | `运行核心/任务调度/任务进程_对象.py:167-169` | `独立任务.待发布状态` / `待发布错误码` / `待发布错误说明` | 终态待发布意图（跨轮次重试意图） | 同上；`转字典()` 不含 | 同形但**不可达**：读者仅 `监视轮询面.py:102-105`，写者 `回收落盘面.py:87-89,114-116`（同表） |
| **2-5 ★** | `公共契约/诊断/调用账本.py:177` | `调用参数账._已建表` | 本进程已在本库建过表（DDL 幂等守卫） | **反向前提**：类**每次调用都新建实例** —— `公共契约/诊断/调用账本.py:293,299`、`模块库/能力目录/实现/能力目录.py:416,596` 均为 `调用参数账()` 现建现用 | **可达 · 守卫被绕过**：`记一次调用` 自称在**热路径**，却每次跑 `库.execute(建表语句)`+`库.execute(索引语句)`+`库.commit()`（`调用账本.py:182-188`）＝每次调用一次无谓写事务 |

**本类最重要的一条判据（区分 ★ 与非 ★）**：形状相同、后果天差地别，差别**不在字段，在读者射程**。
2-1 的读者（`任务系统._应用独立状态已加锁`，`:271-288`）由 `_同步循环`（`:241-269`）驱动，遍历**门面全表**（现场 249 条，含池外历史）⇒ 必然拿到重建对象。2-2/2-3/2-4 的读者都只遍历 `活动任务表`（池内活对象）⇒ 重建对象永远进不去 ⇒ **偶然安全**。⇒ 只按「字段形状」立判据会同时给出 3 个假红；判据必须带上**读者射程**。

#### 边界（只满足判据 ①，该类无重建路径 ⇒ 不算命中）

离变成缺陷只差一次「从磁盘/账本重建」的重构，登记备查：

`运行核心/统一网关/停机编排.py:152`（`已请求`，仅 `启动运行核心网关.py:329` 建一次）、`运行核心/统一网关/安全/安全边界.py:46`（`已加载`）、`运行核心/任务调度/任务进程.py:175`（`已停止`，仅 `任务系统.py:72` 建一次）、`运行核心/时钟校准.py:246`（`_已处理序号`）、`运行核心/运行环境管理器/提供者生命周期.py:113`（`_已处理唤醒序号`）、`运行核心/统一网关/传输/流式HTTP.py:101`（`_回调已执行`）、`技能库/后端/技能库/实现/技能库.py:358`（`_已核超限`）、`支持库/后端/系统核心支持库/资源管理/实现/资源管理.py:359`（`已持有`）、`平台控制面/提供者/进程提供者.py:45`（`退出已记录pid`，已是集合形状）、`开发工具/薄壳/薄壳服务.py:237`（`_进程内指纹`）、`开发工具/性能基准/基准环境.py:105`、`开发工具/统一验证/验证工作区.py:31`、`开发工具/性能基准/反向验证.py:217`。

#### 已是正确形状（供对照 —— 批K K1 该照抄的就是这个）

| 文件:行 | 事实 | 形状 |
|---|---|---|
| `运行核心/能力调用/唯一能力调用_装配面.py:43-44` | 「本进程是否曾装配过任务系统」 | **模块级** `_任务系统曾装配标记 = 假`；`接入任务系统()` 内 `global` 写，`_任务系统曾装配()` 内加锁读。事实归容器（模块），对象只是视图 ⇒ 任何对象重建都天然带对的话语权 |
| `运行核心/任务调度/任务进程.py:103-107` | fd 预算读法 | 文件内**唯一读法**，口径一处（不许调用方另算） |

#### 明确排除（不凑数）

- 类**有重建路径但字段是自身内容**（判据 ① 不成立）：`任务`（`任务系统.py:33-47`，重建点 `:161`）、`运行上下文`（`上下文.py:20-35`，重建点 `:81`）、`句柄`（`句柄体系.py:123-137`，重建点 `:175`）、`失败记录`（`失败记录.py:37-51`，重建点 `:93`）、`安装记录`（`版本仓库.py:116`）、`发布事务`/`事务步骤`（`发布事务.py:72`）、`操作记录`（`事务恢复.py:68`）、`版本包`（`版本注册表.py:87`）、`项目声明`（`项目声明.py:86`）、`包声明`（`声明.py:85`）—— 字段全是自身内容。
- `灰度指标.恢复()`（`灰度指标.py:138`）从运行库重建 `状态表`：字段 `状态表/搬迁观测数/写入失败计数/同步错误` 均为自身内容。
- `权威状态` 借壳 `object.__new__`（`运行核心/运行状态监督/实现/只读状态库.py:48`）：该实例**只借类属性**（校验规则表/迁移序列表），不持实例状态，形状正确。
- 测试夹具 `测试中心/**`、缓存 `工程缓存/`、`__pycache__/`、`开发文档/归档/` 整目录排除；散文提及（如 `技能库.py:13`、`任务进程.py:7`）非字段，不计。


---

## 第三类 · 内核只读锁与租约的接线缺口（本批现场实测，新增）

**本类不是扫出来的，是**写盘被内核拒绝**时现场证真的**（2026-09-24 04:45–04:55 主会话）。

### 现场事实（逐条可复跑）

| # | 事实 | 取证命令 |
|---|---|---|
| 1 | 我的租约 `开工-20260924-043407-4bfb` **状态=活跃**、覆盖 `开发文档/分析/批K单据…md` 与 `…批L清单_20260924.md` | 读 `工程缓存/平台控制面/文件租约.json` 里 `状态=='活跃'` 的记录 |
| 2 | 但 `开发文档`、`开发文档/分析` **都是 `uchg`**，两个租约路径也**都是 `uchg`** | `ls -ldO 开发文档 开发文档/分析` |
| 3 | 同一时刻，子代理 5 条租约路径（`运行核心/任务调度/{任务系统,任务进程,任务进程_对象}.py`、`测试中心/运行核心/测试_*.py`）**未锁** | `python3.14 运维脚本/仓库锁.py --查看` → 「未锁 7」 |
| 4 | 平台自己的权威 md 写腿 `项目文档.写文档` 因此被内核拒：`PermissionError: [Errno 1] Operation not permitted: '…/开发文档/分析/批L清单_20260924.md'` | `capability_call(能力id="项目文档.写文档", …)` |
| 5 | 走正规「下锁」能力后同一次写盘**立刻成功** | `capability_call(能力id="自修复工具.仓库锁", 参数={"动作":"下锁"})` → 解锁 5688 项、失败表 `[]` |
| 6 | **回锁后**（`动作=上锁`，新锁 5696 项）子代理那 5 条**活跃租约**路径**当场变 `uchg`** | `ls -lO 运行核心/任务调度/任务进程_对象.py 测试中心/运行核心/测试_任务进程池有界.py` |
| 7 | 权威能力 `项目文档.写文档` 写出的 md **没有写入凭据**：`开发文档/项目证据/MD写入流水.jsonl` 里**无** `批L清单` 任何一条，而 `pre-commit` 判据二当场判它「手写」 | `rg -n "批L清单" 开发文档/项目证据/MD写入流水.jsonl`（0 命中）；`python3.14 -m 开发工具.MD文档生成 --拒绝手写校验` → `[手写·新建且无写入凭据] 开发文档/分析/批L清单_20260924.md` |
| 8 | 而 `工程缓存/运行数据/写入凭据登记失败.jsonl` **不存在** ⇒ 不是「登记失败」，是**根本没调登记** | `ls 工程缓存/运行数据/写入凭据登记失败.jsonl` |

### 机械根因（三条，各自独立）

| # | 落点 | 缺陷 | 后果 |
|---|---|---|---|
| **3-1 ★** | `公共契约/运行时/仓库只读锁.py:880-886` | `重锁一遍(仓库根)` 的**函数体就是 `return 上锁全仓(仓库根)`**，而 `上锁全仓`（`:826-850`）对 `_受管条目(根)` **逐条无条件上锁**、**完全不读文件租约账**。于是「缺口」与「活跃租约的合法开窗」在它眼里是同一件事 | **收紧动作无声吊销活跃会话的写窗口**：现场事实 6 就是再证（子代理租约仍活跃、路径已被锁死）。`文件租约维护.py:243` 的注释已点名这个形状：「盘上还写着活跃、路径却已锁死」 |
| **3-2 ★** | `模块库/项目文档/实现/写文档.py:9,127-128` → `支持库/后端/系统核心支持库/资源管理/实现/资源管理.py:186` `原子写入` | 权威 md 能力的落盘腿走 `系统核心支持库.资源管理.原子写入`，而**该腿全文没有 `临时解锁` / `写腿收口锁态`**（实测 `rg` 0 命中）。对照：另一条落盘腿 `支持库/后端/文件系统支持库/文件操作/实现/文件系统.py:267,275` **两样都接了** | **同一件事两条落盘腿，一条全、一条缺**：① 整仓锁在位时该腿被内核拒（现场事实 4）；② 该腿也不调 `写入流水.登记md写入凭据` ⇒ **权威能力写的文件被自己的 `pre-commit` 判据判成「手写」**（现场事实 7、8） |
| **3-3 ★** | `公共契约/运行时/仓库只读锁.py` 的 `解锁供写入`（`平台控制面/能力目录/实现/文件租约申请.py:139-173` 接线） | 认领即解锁**确实生效**（现场：认领后 `开发文档/分析` 与两个 md 立刻变未锁）；但它只解「认领路径 + 直接父链」，**不解「为了新建而需要的祖父链」以外的东西** —— 这不是缺陷，是**射程事实**，登记在此以免下一个人误判 | 供对照：本次 `开工编排.开工即占` 后 `开发文档/分析` 未锁、`开发文档` 仍锁（不影响在 `分析` 内建文件） |

### 与类二/批K 的关系

3-1 与类二 2-1 是**同一个形状**：**「容器级事实（谁的窗口开着）被存成一次性状态（盘上的 flags），而回锁动作只认 flags、不认账」**。修法方向一致：**判据归账（租约账），动作只执行** —— `重锁一遍` 必须先从租约账取「活跃租约路径及其父链」的豁免集，再上锁。

3-2 与类一 是同一条铁律的两个落点：**同一件事只许一条腿**。现在 md 落盘有两条腿（`资源管理.原子写入` / `文件系统支持库.文件操作.写入文件`），凭据与开窗只接在后者上。

### 修法与验收（待批M 展开，本件只定方向）

| # | 修法方向 | 验收（须带反向样本） |
|---|---|---|
| 3-1 | `重锁一遍` 加**租约豁免**：先读 `工程缓存/平台控制面/文件租约.json` 的活跃记录，取其路径 + 待建父链，上锁时跳过。若 `上锁全仓` 同时要服务「整仓收紧」语义，则**拆成两个公开动作**（`全仓收紧` 不豁免 / `补齐缺口` 豁免），不许一个动作两种语义 | 活跃租约下跑 `补齐` ⇒ 该路径仍可写；把豁免关掉 ⇒ 立刻变红 |
| 3-2 | md 落盘**收成一条腿**：让 `项目文档.写文档` 改走 `文件系统支持库.文件操作.写入文件`（它已有开窗 + 凭据登记），或把开窗与凭据登记下沉到 `资源管理.原子写入` 成为唯一落盘原语。**不许两条腿各自补一半** | 整仓上锁、无租约时走 MCP 写文件应回「写入未授权」而不是内核 `Operation not permitted`；有活跃租约时应成功、写后 flags 复原、且 `MD写入流水.jsonl` 有对应凭据 |


---

## 待证真清单（下一批的输入，不在本件下结论）

| # | 待证真 | 判据/命令 |
|---|---|---|
| V1 | `查询运行态` 在只读库/无写权限时是否失败；`_创建运行库表` 是否每次都跑 DDL | `sed -n '399,425p' 支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py` |
| V2 | `任务系统.查询` 的 `_保存已加锁` 落哪些字段；为何现场 `状态/完成时间` 已落而 `结果` 为空 | `sed -n '300,340p' 运行核心/任务调度/任务系统.py` |
| V3 | `会话存储.读取历史/查询会话` 的 `_更新活动` 写什么、是否可省 | `sed -n '340,360p;492,505p' 支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py` |
| V4 | `调用参数账._已建表` 热路径 DDL 的实际开销 | 量 `记一次调用` 单次耗时 |
| V5 | 3-1 的现场触发者是谁（`补齐` 还是某条租约释放联动） | 本轮 `rg "\"动作\": \"(补齐|上锁)\"" 工程缓存/运行数据/MCP全量回执/20260924_全量回执.jsonl` **0 命中** ⇒ 触发者可能是终端腿 `运维脚本/仓库锁.py --补齐`，需按 mtime 追 |
| V6 | `开发文档/未完成事项.md` 在 04:42–04:47 被**同内容重复登记 5 次**（sha 全同），写者是谁 | `rg -c "未完成事项" 开发文档/项目证据/MD写入流水.jsonl`；对照同一时段的调用账本 |


---

## 收口去向

- 类一 L1-1/L1-2/L1-3 与 V1–V6 → 进 `开发文档/未完成事项.md` §四 登记（**待复核后**，避免把待证真当已定案）
- 类二 2-1 已由批K K1/K2 覆盖（修法见批K 单据 §三）；2-5 单独成条（热路径 DDL）
- 第三类 3-1/3-2 → **本批现场已证真**，进 §四 登记并立批M 单据（修法方向见上表）；3-3 只作射程登记
