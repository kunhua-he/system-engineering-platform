# 制品重建窗口与 N7 销账 · 2026-09-22

> 原条目：`开发文档/未完成事项.md` **§二 #56**（40007 从「跑源码树」切到「跑激活制品」）
> 与 **§二「制品重建窗口一次收口（4 项）」**。
> 本件是**销账证据**：逐项现场取证。销账后两行从待办清单移除，结论留本件。
> 开工 ID：`开工-20260922-031847-b2a5`（首轮）/ `开工-20260922-033745-d5f0`（制品后核验轮）。
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


## 一、结论

| 项 | 结论 | 关键证据 |
|---|---|---|
| #56 40007 跑激活制品 | ✅ 成立 | 网关 PID 50841，`lstart=Tue Sep 22 03:35:23`，`cwd=工程缓存/制品仓库/平台客户端环境/平台客户端`；稳定路径 `制品摘要.json` 的 `摘要sha256=946adda28668a6e6`（由提交 `71f9adb0` 构建）；`健康检查` 回 `能力数 749` |
| 窗口① F1 经网关验 `开工编排.开工准备` | ✅ 已验 | 经网关回带 `代码地图.判据=真实状态判据（代码解析支持库.代码地图.查询状态…）`，且 `缺失原子能力` 不再列 `代码地图.查询状态`（旧制品里它是弱判据 + 缺失清单条目） |
| 窗口② E1 `备份恢复演练` 进发布门禁 | ✅ 已接 | 提交 `71f9adb0`（真跑组第 17 项）；实测 `--规模 5`：RTO 4.173s（契约 60s）/RPO 5.1951s/三类全绿/退出码 0 |
| 窗口③ 新能力 `代码解析支持库.代码地图.查询状态` 进制品 | ✅ 已验 | 经网关真调（入参 `代码地图路径`）回 24075 节点／53031 边／1211 文件／覆盖率 0.987；装配能力数 748 → **749** |
| 窗口④ 句柄复用加速实测（经网关连调 3 次） | ✅ 已验 | `查代码块` 三次耗时 **2378.14ms → 84.09ms → 89.78ms**（第 2 次比第 1 次快 **96.5%**，判据要求 ≥50%） |
| N7 纯 MCP 源码树 Git 提交不可能 | ✅ 已修 | 提交 `e5f57cca`；修复前 `Git操作.提交` 回 HTTP 400 `路径越界`，`详情.白名单=[制品根, 系统临时目录]` |

## 二、逐项取证

### 2.1 N7：仓库根白名单按位置推根（根因）

- **真源**：`支持库/适配层/Git提供者/实现/仓库根白名单.py::底座仓库根()` 原实现 `Path(__file__).resolve().parents[4]`。
- **现场**：网关 40007 的 launchd `WorkingDirectory` 指向制品稳定路径，那份拷贝的 `parents[4]` = **制品根**
  ⇒ 白名单 = [制品根, 系统临时目录]，**源码树不在其中** ⇒ 对源码树的任何提交都被判 `路径越界`。
- **实测（修复前）**：`版本控制支持库.Git操作.提交`（仓库路径=源码树，路径列表=[开发文档/未完成事项.md]）
  → HTTP 400，`错误码=路径越界`，`详情.白名单=["/…/平台客户端环境/平台客户端/平台客户端", "/private/var/folders/…/T"]`。
- **修法**：改为**向上找最近一个含 `.git` 的祖先目录**（worktree 里 `.git` 是文件，故目录/文件都认）。
  制品拷贝位于源码仓 `工程缓存/` 内、自身无 `.git` ⇒ 命中**源码仓根**，源码树与制品两种跑法拿到同一个仓库根。
  安全边界不变（仍只放行「跑着的这份代码所在的那个仓」+ `工作区允许提交根` 显式加白 + 系统临时目录）。
- **定向验证**：`全量重算摘要` 全部闭合；依赖派生重跑（依赖分两段 39 件）；`编译口 --变更` 无阻断红项；
  `测试中心.支持库.测试_Git提供者` Ran 34 tests OK。
- **端到端**：制品重建 + 网关重启后，`Git操作.提交` 不再回 `路径越界`（已越过白名单校验、进到参数/路径层）；
  本轮三个提交全经 MCP 完成。

### 2.2 制品重建与激活

- 重建命令：能力 `自修复工具.构建平台客户端制品`（`安装=true`，`超时秒=1800`），耗时 32.7 秒。
- 结果：`制品摘要=946adda28668a6e6`（旧为 `06456822d525a368` 版本 189）；
  `写盘后自校验通过=true`、`可重复构建校验通过=true`、`入库消息=已入库（内容寻址+签名+信任元数据）`、
  `稳定路径校验=稳定路径可读`；`改写导入数 3425`、`重算包摘要数 148`。
- 重建前先跑 `编译口 --全仓`：五态 = 只报告（**无阻断红项**）。

### 2.3 网关重启与装配确认

- `launchctl kickstart -k gui/501/com.huashi.gateway-40007`（03:35:23）。
- 重启后：`健康检查` 回 `{状态: 健康, 能力数: 749}`；`/健康` 无凭证 401（与规范一致：无凭证 401、带凭证 200）。
- 为什么必须重启而不是 `热接入`：本批含 `运行核心/统一网关/**` 的**进程内代码**（批C 的 C1/C3/C4/C9/C11/C17），
  热接入覆盖不到；重启前试过 `热接入` 回 `变更包 0`（它比对的是自己那份拷贝）。

### 2.4 句柄复用加速（窗口④）

经网关连调 `代码解析支持库.语义索引.查代码块`（查询=续租句柄，索引根=模型连接器，顶部数量=3）：

| 次序 | 网关回带耗时 |
|---|---|
| 第 1 次 | 2378.14 ms |
| 第 2 次 | 84.09 ms |
| 第 3 次 | 89.78 ms |

判据「第 2 次比第 1 次快 ≥50%」→ 实测 **96.5%**，远超阈值；召回数 260、命中 3、首条命中目标函数（`模型连接器.py:802-812`）。
修复前基线为单次 **6.5–7.4 秒**（每次建拆句柄）。

### 2.5 为什么本轮能收口（此前卡住的真因）

窗口此前一直排不上，是因为三件事同源：**源码树 ≠ 制品**（`编译口 --变更` 报「变更文件 619 / 受影响包 147」）。
本轮按「一次重建 + 一次重启」把源码侧改动整体带进激活制品，随后逐项经网关真调取证 —— 
不再用进程内裸调代替（进程内裸调会被 `调用器.py:203` 挡回 `[E未装配] 能力调用器未装配`）。

## 三、本轮提交

| 提交 | 内容 |
|---|---|
| `e5f57cca` | 仓库根白名单改为向上定位真实 Git 仓库根（N7） |
| `3a69ec33` | 补录前序会话漏提交的债务清单登记（3 项 → 4 项） |
| `71f9adb0` | E1：备份恢复演练接进 `运行发布门禁` 执行项（真跑组第 17 项） |
