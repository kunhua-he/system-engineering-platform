# 开工 ID：探针簇（TZ-20260918-01）

> 最后更新：2026-09-18
> 口径：实测（开工前现场回读，命令随文给出）
> 维护者：小马仔（探针簇）

## 一、开工 ID 与用途

**开工 ID = `探针簇`**（英文代号 `TZ`，全称「模型探针」簇）。
本 ID 用于：本会话内所有改动、提交信息、文档标注的**唯一标识**，
便于与其他并发会话（当日至少 3 路在途）区分归属、避免抢文件。

**提交信息格式**（本簇一律照此）：

```text
探针簇：<一句话做了什么>

<要点，含实测数据>
```

## 二、允许修改的文件清单（★ 文件级独占，清单外一律不碰）

开工前已按 `开发文档/规范/多会话并行开发规约.md` 第九节现场回读，确认以下文件**当前无人占用**：

```bash
git status --porcelain 支持库/后端/大语言模型支持库/    # 实测：空（该包工作树干净）
git log --oneline --since="5 minutes ago"              # 实测：无本簇相关在途提交
```

| # | 文件 | 动作 |
|---|---|---|
| 1 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` | 新增探针实现 |
| 2 | `支持库/后端/大语言模型支持库/模型连接器/能力定义.json` | 加能力条目（唯一可编辑事实源） |
| 3 | `支持库/后端/大语言模型支持库/模型连接器/包声明.json` | 版本 +1、能力清单同步 |
| 4 | `支持库/后端/大语言模型支持库/模型连接器/能力契约/参数契约.json` | 参数/返回/错误码/行为 |
| 5 | `支持库/后端/大语言模型支持库/模型连接器/能力数据/能力搜索数据.json` | 参数名与契约对称 |
| 6 | `支持库/后端/大语言模型支持库/模型连接器/权限契约/权限契约.json` | 新能力加 `"允许用户": ["*"]` |
| 7 | `支持库/后端/大语言模型支持库/模型连接器/说明/使用说明.md` | 新能力段（H2 格式 + 错误码表） |
| 8 | `支持库/后端/大语言模型支持库/模型连接器/说明/设计说明.md` | 设计口径 |
| 9 | `支持库/后端/大语言模型支持库/模型连接器/验证场景.json` | 正向验证场景 |
| 10 | `支持库/后端/大语言模型支持库/模型连接器/完整性摘要.json` | 包级重算（不手写） |
| 11 | `支持库/后端/大语言模型支持库/模型连接器/__init__.py` | 包级中文入口注册 |
| 12 | `支持库/后端/大语言模型支持库/完整性摘要.json` | 聚合父包级联重算（只重算本父包） |
| 13 | `支持库/后端/大语言模型支持库/模型连接器/复用决策.json` | 补搜索词与候选 |
| 14 | `测试中心/支持库/测试_模型连接器探针.py` | 定向测试（新建） |

## 三、★ 并发纪律（照 `多会话并行开发规约.md` 第九节，逐条遵守）

| 纪律 | 本簇执行方式 |
|---|---|
| **只 add/commit 自己的路径** | 一律 `git add <精确路径>`；**禁止 `git add -A` / `git add .`** |
| **commit 必须带 pathspec** | `git commit -m "…" -- <路径1> <路径2>`，提交后 `git show --stat HEAD` 核文件数 |
| **禁止全仓级动作** | **不跑** `python3.14 -m 开发工具.全量重算摘要`（会写多个包）；<br>改用**包级** `组件规范支持库.生成完整性摘要`（只传本包目录） |
| **只做包级编译** | 统一编译器 `--文件` / `--能力` 小单元编译，不跑全量 |
| **受保护文件先请示** | `AGENTS.md` 等一律不碰 |
| **临时脚本不落库** | 一律放 `/tmp/`，不放仓库内 |
| **改前先回读、改后自查** | 每步 `read` 确认当前真内容；改完 `git diff` 自查 |

## 四、本次任务边界

**做什么**（P0，已获华哥授权）：

```text
新增原子能力：大语言模型支持库.模型连接器.探测模型端点
目的：用户只给 url + key 就能探通；缺的参数自动补
流程：第一步取模型（GET）→ 第二步试协议（命中即停）→ 全失败报三个原因
```

**不做什么**（理由见方案 §四）：

- 扫端口式「自动发现」（已废弃方向）
- 跨机算力池 / 多矩阵
- 隐私路由（归业务端）
- 动态路由 P1（`按任务选模型`）——**本轮不碰**，待华哥单独拍板

## 五、验收判据

| 项 | 命令/方式 |
|---|---|
| 语法 | `python3.14 -m py_compile <改动的 .py>` |
| 定向测试 | `python3.14 -m unittest 测试中心.支持库.测试_模型连接器探针` |
| 包级摘要 | `组件规范支持库.生成完整性摘要`（只传本包目录）→ 写回 `完整性摘要.json` |
| 包级编译 | `开发工具.项目编译.项目编译器`（`--文件` 小单元） |
| **真实 HTTP** | 经 40007 网关调 `探测模型端点`，对**真实端点**验四场景（见方案 §6.1） |
| **反向验证** | 故意给错 key / 错端口 / 非模型服务 → 必须报出**正确的失败类型** |

## 六、开工前现场回读记录

```text
时间：2026-09-18 21:1x
git status --porcelain 支持库/后端/大语言模型支持库/  → 空（无占用）
git log --oneline --since="5 minutes ago"            → 无本簇相关在途提交
最近一次动过本包的是 f5eaccfa（21:05，另一会话：连接LLM 守卫参数可调）—— 已提交落定，
不与本簇改动区域重叠（它改 连接LLM 参数签名，本簇新增独立能力）。
包版本：模型连接器 1.2.6（21 个能力）；聚合父包 大语言模型支持库 2.0.2（50 个能力）。
```

> 依据：华哥 2026-09-18 21:1x 授权「你自己列一个开工 ID，然后你就可以开始做了。
> 不要跟其他的 agent 撞车抢同一个文件就行」。
> 方案：`开发文档/方案/模型探针与动态路由方案_20260918.md`（状态：起草）。
