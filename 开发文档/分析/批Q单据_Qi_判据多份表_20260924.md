<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批Q · Q-i · U-8 单据：同一判据多份表 → 各收成 1 份

- 开工ID：`开工-20260924-103610-9d13`（租约 4 条，已全部释放）
- 日期：2026-09-24｜任务源：`开发文档/分析/审计_两条腿与冗余_20260923.md` §R4、`开发文档/分析/审计落点_单一节点收口_20260924.md`
- 本次范围：**只做判据收口**；未跑编译、未 git 提交、未动本批以外的文件
- 本单据独占，与 `批Q单据_Qh_路径说明文案_20260924.md` 等无重叠

## 一、结论先行

| 表 | 改前份数 | 改后份数 | 结论 |
|---|---|---|---|
| ① 说明书对账判据 | 2 | **1** | ✅ 已收口：粗的那条改成转调唯一真源（严的那条） |
| ② 有界 HTTP | 2 | **1** | ✅ 已收口：差异逐项参数化后只留下层一处实现 |
| ③ 类型短名映射 | 3 | **3（未收口）** | ⛔ 按纪律**停下如实报告**：`公共契约/基础类型` 里**没有现成权威短名表**，纪律明令不得自造第四份 |

另：审计 §R4 对②的行号已漂移（详见 §六.3）。

---

## 二、表① 说明书对账判据：2 份 → 1 份（已收口）

### 2.1 改前 2 份的真实差异（逐条读现场）

| 份 | 落点（改前） | 判据 |
|---|---|---|
| **粗** | `开发工具/契约编译/漂移判定.py:371::检测说明书一致` | **全文子串**：`契约["版本"] in 内容`、每个参数名 `in 内容`、`错误码[:3]` 逐个 `in 内容` |
| **严** | `开发工具/公开调用完整性门禁_说明书解析.py:457::检查说明书字段` | **结构化载位逐能力对账**：包级/能力级版本载位、参数载位（名称/类型/必填/默认值）、多参数、错误码三档载体、返回载位/返回类型 |

粗的那条会漏的四种形态（现场读严判据正文得出，均已被严判据覆盖、粗判据看不见）：

1. 参数写错**类型**；2. 参数写错**必填**；3. 参数写错**默认值**（`#126` 新增列）；4. **返回类型**写错；5. 错误码只写了**第 4 个之后**（粗判据只取前 3 个）。

⇒ **不是正当分工**，是同一判据的两份实现（哲学 1.3），且强度不对称。

### 2.2 唯一真源落点

- **判据真源**：`开发工具/公开调用完整性门禁_说明书解析.py:457::检查说明书字段`（未改动一个字，仍按结构化载位逐能力对账）
- **适配落点**：`开发工具/契约编译/漂移判定.py:371::检测说明书一致` —— 现**不持有任何判据**，只做三件事：① 按真源的唯一约定 `说明书相对路径`（`说明/使用说明.md`，落点 `公开调用完整性门禁_说明书解析.py:33`）反推包目录；② 读包声明；③ 把包级违规清单收敛成**本能力**那一条。
- **不并包级违规**（`能力id == "*"`）：那是包级根因，按仓规「同一根因只计一条」由门禁的包级环负责，不向 642 条能力放大。
- **不可判不静默**：包声明缺失/不可读时回 `说明书不可判（…）`，不当作通过。

### 2.3 改前 → 改后对照

| 项 | 改前 | 改后 |
|---|---|---|
| 判据实现处 | 2（漂移判定 + 说明书解析） | **1**（说明书解析） |
| 漂移判定里的判据代码 | 8 行子串比对 | 0 行判据（纯适配） |
| `说明书缺失` 分支 | `f"说明书缺失: {文件}"` | **逐字不变** |
| 全仓结论（125 包/642 能力） | `缺口计数={"错误码漂移":1}`、判红 1 | **完全相同**（见 §五.1） |
| 合成盲区样例 | `None`（漏） | 出条目（抓到） |

---

## 三、表② 有界 HTTP：2 份 → 1 份（已收口）

### 3.1 改前 2 份的真实差异（逐条读现场）

| 份 | 落点（改前） |
|---|---|
| 下层 | `公共契约/运行时/有界HTTP.py:10::有界线程HTTP服务器` |
| 网关域 | `运行核心/统一网关/安全/有界服务器.py:32::有界线程HTTP服务器`（审计写的 `本地网关.py:235` 是**漂移行号**，见 §六.3） |

**四类差异 —— 全是行为，不是冗余，故逐字保留为参数/显式分支：**

| # | 差异 | 下层（域外） | 网关域 | 收口后载体 |
|---|---|---|---|---|
| 1 | **满载出口** | 只 `shutdown_request`，**不回任何响应** | 写 `429 + 网关响应信封 JSON` 再关连接 | 下层新增 `满载响应回调`（缺省 `None` ＝原行为）；网关域注入 `_写限流响应` |
| 2 | **预算参数名与默认** | `最大线程数`（默认 32） | `最大工作线程`（1..256，默认 64） | 下层保 `最大线程数`；网关域保 `最大工作线程` 校验后转发 |
| 3 | **请求队列上限** | 不动 `request_queue_size`（=5） | `min(128, 最大工作线程)` | 下层新增 `请求队列上限`（缺省 `None` ＝不动） |
| 4 | **可观测** | 无计数、无诊断 | `活动工作线程数`/`拒绝请求数` + 连接诊断 | **下沉下层一处**（域外只增属性、行为不变）；网关域仅覆写 `handle_error` 抑制堆栈 |

**收口前实际是 4 份代码**：下层 1 份 + 网关域 1 份，另有两处同判据副本（见 §六.2）。

### 3.2 唯一真源落点

- **通用部分唯一实现**：`公共契约/运行时/有界HTTP.py:25::有界线程HTTP服务器`
  - `:35::__init__`（信号量预算 + `请求队列上限` + `满载响应回调` + 计数）
  - `:49::process_request`（满载出口 → 回调 → `shutdown_request`；预算归还在 `finally`，线程起不来也归还）
  - `:78::记录连接诊断` / `:85::连接诊断快照`
- **网关域差异注入**：`运行核心/统一网关/安全/有界服务器.py:38::有界线程HTTP服务器(_下层有界线程HTTP服务器)`
  - 覆写面实测只剩 `['_写限流响应', 'handle_error', '限流建议重试秒']`（`__init__` 只做校验 + 转发）
  - `:51::__init__` 保 `最大工作线程`（1..256，默认 64）与 `min(128, 最大工作线程)` 字面量
  - `_写限流响应` **逐字保留**（含 `Retry-After`、`Cache-Control: no-store`、`Connection: close`）
- **分层守住**：满载响应体（网关协议）留在网关域；`公共契约` 不认识网关信封，不反向依赖运行核心。
- **调用点零改动**：`本地网关.py` 与 `流式HTTP.py` 的再导出实测仍是同一对象；`测试中心/运行核心/测试_优雅停机.py:73`（`最大工作线程=8`）与 `测试中心/运行核心/测试_硬件画像与档位.py:354`（读 `min` 第 0 个字面量 = 128）继续成立。

---

## 四、表③ 类型短名映射：3 份（**未收口**）

### 4.1 三份的条目差集（现场实测，非估算）

| 份 | 落点 | 键数 |
|---|---|---|
| A | `运行核心/统一网关/协议/类型规格.py:48::类型短名映射` | 18 |
| B | `开发工具/契约编译/能力定义编译器.py:71::易语言类型别名` | 10 |
| C | `开发工具/快速编译/运行快速编译.py:24::旧类型` | 11 |

- **同键不同值（真分叉）：0 处** —— 三份没有对同一个短名给出两个不同正式名。
- 现有分叉形态是**覆盖宽度不同**：
  - A 有而 B 无：`逻辑、浮点数、字节、映射、日期时间、资源引用、句柄、结果`（8 键）
  - A 有而 C 无：`逻辑、长整数、单精度数、双精度数、空值、映射、日期时间、资源引用、句柄`（9 键）
  - C 有而 A 无：`数字、空`（2 键）；B 有而 A 无：无
- **并集 = 20 键**（A 18 ∪ {`数字`→双精度数型、`空`→空值型}）
- 后果：同一个 `能力定义.json` 里写 `数字`/`空`，经 C 编译认得、经 A 的网关边界不归一（`类型规格.py:179` 的 `类型短名映射.get(类型, 类型)` 原样透传）⇒ 静默分叉已成立。

### 4.2 为什么停下（判据）

纪律原文：**「收口到 `公共契约/基础类型` 里已有的类型节点（先读该目录，找现成的权威表；找不到现成权威表就停下如实报告，不要自己造第四份）」**。

现场读完 `公共契约/基础类型/` 全部 17 个条目：

- `类型表.py` 只有 **`正式类型表`（16 项正式名，无短名侧）**；
- `类型目录.md` 只列 16 项正式名 + 「历史短名只能在迁移解析器内部识别」的**文字口径**；
- `__init__.py` 导出的是 16 型**判定函数**与字段名册，**无短名→正式名的映射节点**。

⇒ **无现成权威表** ⇒ 按纪律停下，不自造第四份。

### 4.3 建议的落点（需华哥裁决，本批未动）

仓内**已有设计**指向同一处，不是新机制：

- `开发文档/方案/裸布尔迁移分批方案_20260917.md:217`：「在 `公共契约/基础类型/` 新增 `历史短名.py`：一份 `历史短名到正式名`（`类型短名映射` 18 键 ∪ `易语言类型别名` 10 键 ∪ `运行快速编译.旧类型` 11 键，取并集后逐条确认无冲突）」
- `开发文档/方案/类型体系收口盘点_20260917.md:183`（TY-4）：归一表 16 张 → 2 张，落点 `公共契约/基础类型/{历史短名.py, 正式名到注解.py}`
- 按 §4.1 实测：并集 20 键、冲突 0 ⇒ 该设计**可直接落地**，无需再裁决条目
- 落地后 A/B/C 三处统一转调，`运行发布门禁_防回潮判据.py:170`（现「现场读 `网关核心.py::类型短名映射`」）同步改读真源
- 本条属「数据模型/口径」改动，按华哥既定偏好**先出设计再动手**，故本批只报不做

---

## 五、回归证据（可复制命令与真实读数）

### 5.1 表①：同一输入（全仓 125 包 / 642 能力）改前改后**同一结论**

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && python3.14 -c "
import sys, json
from pathlib import Path
sys.path.insert(0, '.')
from 开发工具.契约编译.漂移检测 import 全面漂移扫描
统计 = 全面漂移扫描(Path('.').resolve())
print('包数', 统计['包数'], '能力数', 统计['能力数'])
print('缺口计数', json.dumps(统计['缺口计数'], ensure_ascii=False))
print('判红', len(统计['问题列表']))
"
```

| 读数 | 改前 | 改后 |
|---|---|---|
| 包数 / 能力数 | 125 / 642 | **125 / 642** |
| 缺口计数 | `{"错误码漂移": 1}` | **`{"错误码漂移": 1}`** |
| 判红（基线外新增） | 1 | **1** |
| 判红内容 | `支持库/后端/文件系统支持库/文件操作` 的 `文件系统支持库.文件操作.写入文件 错误码漂移` | **同一包、同一能力、同一缺口类型** |
| 说明书类缺口 | 0 | **0** |

**结论：同一输入同一结论，收口零新增红。**（那条 `错误码漂移` 是本次之前就有的存量红，与本批无关，未动。）

**收口安全性前置实测**（严判据全仓独立跑，改前就量过）：

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && python3.14 -c "
import sys, json
from pathlib import Path
sys.path.insert(0, '.')
from 开发工具.公开调用完整性门禁_说明书解析 import 检查说明书字段
from 开发工具.契约编译.漂移检测 import _枚举正式包目录与契约
总 = 0; 包数 = 0
for 包目录, _ in _枚举正式包目录与契约(Path('.').resolve(), ('支持库','模块库','技能库')):
    包数 += 1
    声明 = json.loads((包目录/'包声明.json').read_text(encoding='utf-8'))
    总 += len(检查说明书字段(包目录, 声明))
print('包数', 包数, '严判据违规总数', 总)
"
```

实测读数：**`包数 125 严判据违规总数 0`**（125 包**全有** `包声明.json`，`缺包声明 0`）
⇒ 把粗的换成严的**不会把任何包判红**。

### 5.2 表①：粗判据的盲区确实被补上（合成输入，粗/转调同跑）

用 `git show HEAD:开发工具/契约编译/漂移判定.py` 取**改前**的 `检测说明书一致`，与改后函数对同一合成包同跑：

| 合成说明书正文 | 粗判据（改前） | 转调后 |
|---|---|---|
| `版本 1.0.0 参数甲 参数不合法`（有全部子串、**零结构化载位**） | `None`（**漏**） | `说明书与入口不一致: 说明书-缺能力段 测试.合成包.演示（本能力共 1 条）` |
| `（空白说明书）` | `说明书与契约不一致: 缺少版本 1.0.0` | `说明书与入口不一致: 说明书-缺能力段 测试.合成包.演示（本能力共 1 条）` |
| 文件不存在 | `说明书缺失: /tmp/不存在.md` | **逐字相同** |

### 5.3 表②：满载行为与可观测逐字保留

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && python3.14 - <<'PY'
import sys, socket, threading, time, json
from pathlib import Path
from http.server import BaseHTTPRequestHandler
sys.path.insert(0, str(Path('.').resolve()))
from 运行核心.统一网关.安全.有界服务器 import 有界线程HTTP服务器 as 网关服务器
from 公共契约.运行时.有界HTTP import 有界线程HTTP服务器 as 下层服务器
进入 = threading.Event(); 放行 = threading.Event()
class 处理器(BaseHTTPRequestHandler):
    def do_GET(self):
        进入.set(); 放行.wait(10)
        self.send_response(200); self.send_header('Content-Length','2'); self.end_headers(); self.wfile.write(b'ok')
    def log_message(self, *a): return
def 原始请求(端口):
    连接 = socket.create_connection(('127.0.0.1', 端口), timeout=3)
    连接.sendall(b'GET / HTTP/1.1\r\nHost: 127.0.0.1\r\nConnection: close\r\n\r\n')
    数据 = b''
    try:
        while True:
            块 = 连接.recv(4096)
            if not 块: break
            数据 += 块
    except (socket.timeout, ConnectionResetError, OSError): pass
    连接.close(); return 数据
def 探测(类, 关键字):
    进入.clear(); 放行.clear()
    服务 = 类(('127.0.0.1', 0), 处理器, **关键字)
    端口 = 服务.server_address[1]
    threading.Thread(target=服务.serve_forever, daemon=True).start()
    线程 = threading.Thread(target=lambda: 原始请求(端口)); 线程.start()
    assert 进入.wait(5)
    原始 = 原始请求(端口); 满载后活动 = 服务.活动工作线程数
    放行.set(); 线程.join(5)
    截止 = time.monotonic() + 2
    while 服务.活动工作线程数 and time.monotonic() < 截止: time.sleep(0.01)
    服务.shutdown(); 服务.socket.close()
    return 服务, 原始, 满载后活动
服务, 原始, 满载后活动 = 探测(网关服务器, {'最大工作线程': 1})
数据 = json.loads(原始.split(b'\r\n\r\n', 1)[1].decode('utf-8'))
print('C1 状态行', 原始.split(b'\r\n')[0].decode('latin-1'))
print('C1 Retry-After=1', b'Retry-After: 1' in 原始)
print('C1 信封8键齐', {'请求id','操作','成功','值','错误码','错误说明','句柄','耗时毫秒'} <= set(数据))
print('C1 错误码/成功', 数据['错误码'], 数据['成功'])
print('C1 满载后活动/拒绝', 满载后活动, 服务.拒绝请求数, '排空后活动', 服务.活动工作线程数)
服务2, 原始2, 满载后活动2 = 探测(下层服务器, {'最大线程数': 1})
print('C2 域外满载回字节', len(原始2), '（期望 0）')
print('C2 域外队列上限', 服务2.request_queue_size, '（期望 5）')
print('C2 域外满载后活动/拒绝', 满载后活动2, 服务2.拒绝请求数, '排空后活动', 服务2.活动工作线程数)
for 值 in (0, 257, True, 64):
    try:
        服务3 = 网关服务器(('127.0.0.1', 0), 处理器, 最大工作线程=值)
        print('C3 最大工作线程', 值, '→ 接受（队列', 服务3.request_queue_size, '）'); 服务3.socket.close()
    except ValueError as 错误:
        print('C3 最大工作线程', 值, '→ ValueError:', 错误)
PY
```

实测读数（原文）：

```
C1 状态行          : HTTP/1.1 429 Too Many Requests
C1 Retry-After=1   : True
C1 信封 8 键齐全   : True
C1 错误码/成功     : 限流 / False
C1 满载后活动/拒绝 : 1 / 1 | 排空后活动: 0
C1 类名/队列/默认线程: 有界线程HTTP服务器 / 1 / 1
C2 满载回字节      : 0 （期望 0）
C2 满载后活动/拒绝 : 1 / 1 | 排空后活动: 0
C2 队列上限        : 5 （期望 5 = 不动 request_queue_size）
C3 最大工作线程=0   → ValueError: 最大工作线程必须是 1 到 256 之间的整数
C3 最大工作线程=257 → ValueError: 最大工作线程必须是 1 到 256 之间的整数
C3 最大工作线程=True → ValueError: 最大工作线程必须是 1 到 256 之间的整数
C3 最大工作线程=64  → 接受（队列 64）
```

**结论：满载 429 信封 8 键 + `Retry-After: 1` 逐字保留；域外「只关连接、不回响应」逐字保留；计数与排空语义逐字保留；构造签名与参数校验逐字保留。**

### 5.4 表②：接线与继承关系

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && python3.14 -c "
import sys, inspect
from pathlib import Path
sys.path.insert(0, str(Path('.').resolve()))
from 运行核心.统一网关.安全.有界服务器 import 有界线程HTTP服务器 as 网关
from 公共契约.运行时.有界HTTP import 有界线程HTTP服务器 as 下层
from 运行核心.统一网关 import 本地网关
from 运行核心.统一网关.传输 import 流式HTTP
print('继承', issubclass(网关, 下层), '| 基类模块', 网关.__bases__[0].__module__)
print('本地网关再导出同一对象', 本地网关.有界线程HTTP服务器 is 网关)
print('流式HTTP 再导出同一对象', 流式HTTP.有界线程HTTP服务器 is 网关)
print('网关构造签名', inspect.signature(网关.__init__))
print('网关覆写面', sorted(k for k in 网关.__dict__ if not k.startswith('__')))
"
```

实测读数：

```
继承 True | 基类模块 公共契约.运行时.有界HTTP
本地网关再导出同一对象 : True
流式HTTP 再导出        : True
网关构造签名 : (self, 地址, 处理器类, *, 最大工作线程: 'int' = 64) -> 'None'
网关覆写面   : ['_写限流响应', 'handle_error', '限流建议重试秒']
```

### 5.5 语法与导入面

- `python3.14 -m py_compile` 三个文件 → OK
- 四个受影响调用点导入实测 OK：`开发工具.能力网关.能力网关`、`开发工具.轻代码前端编辑器.启动编辑器`、`支持库.前端.浏览器宿主.实现.浏览器宿主`、`运行核心.启动运行核心网关`
- `git diff --stat`（本次三个文件）：`公共契约/运行时/有界HTTP.py` +68/-… 、`开发工具/契约编译/漂移判定.py` +48/-… 、`运行核心/统一网关/安全/有界服务器.py` **112 行变动、净减**（第二份实现下线）

---

## 六、未收口项与原因

### 6.1 类型短名映射 3 份 —— **未收口**

- **原因**：`公共契约/基础类型/` 无现成权威短名表（§4.2 现场判据）；纪律明令「找不到现成权威表就停下如实报告，不要自己造第四份」。
- **已量清的落地条件**：并集 20 键、同键冲突 0 处（§4.1）；仓内已有设计落点 `公共契约/基础类型/历史短名.py`（§4.3）。
- **建议**：华哥裁决后，一次落地「新建真源 → A/B/C 三处转调 → `运行发布门禁_防回潮判据.py:170` 改读真源」。

### 6.2 有界 HTTP 的第 3、4 处副本 —— **未收口**

| 处 | 落点 | 性质判据 | 建议 |
|---|---|---|---|
| 第 3 处 | `开发工具/HTML验证/验证服务.py:115`（`服务模式` 内的**局部类**） | **真代码**，硬编码 `BoundedSemaphore(32)` + `shutdown_request` ⇒ **与下层那份行为一致**，是纯冗余、可安全转调 | 可收口：改 import `公共契约.运行时.有界HTTP`，`最大线程数=32` 显式传（行为零变化） |
| 第 4 处 | `开发工具/项目编译/项目编译器.py:542` | **生成器模板里的产物源码**（在 `f'''…'''` 内），模板自述「独立 HTML 启动器；**不依赖开发网关**」，产物是独立项目启动器 | **属正当原子差异**（生成物要能脱离开发网关独立跑）。但注：该模板已 `import {导入前缀}公共契约.运行时.运行缓存` ⇒ 产物里 `公共契约` **可用**，是否一并收口需裁决；本批**不动**（改生成器会连带制品摘要与产物一致性） |

- 本批范围是审计点名的「2 份」，以上两处是本次现场**新发现**，故只报不改，避免越界。

### 6.3 审计 §R4 的行号已漂移（现场事实，非本次引入）

- `运行核心/统一网关/本地网关.py:235` 现场是**错误码→状态码映射表**的一段，**不是**有界 HTTP 实现。
- 有界 HTTP 实现已于 **2026-09-19** 按「被双方依赖的符号下沉」拆到 `运行核心/统一网关/安全/有界服务器.py`（收口前 `:32`，收口后 `:38`）。
- ⇒ 后续引用请以 `运行核心/统一网关/安全/有界服务器.py` 为准。

### 6.4 顺带发现（本批已随重写下线，供备查）

- `运行核心/统一网关/安全/有界服务器.py` 原有 `from 公共契约.运行时.端口策略 import 校验应用监听端口` 是**死导入**（本文件从未使用；全仓唯一使用者是 `本地网关.py:667`）。本次重写该文件时随之下线，**无行为影响**。

### 6.5 全仓扫描的 1 条存量红（**与本批无关，未动**）

- `支持库/后端/文件系统支持库/文件操作` 的 `文件系统支持库.文件操作.写入文件`：`错误码漂移: 实现引用 权限不足 但契约未声明（文件系统.py）`。
- 改前改后读数**完全相同**（§5.1），属本批之前的存量，按「不修无关 bug」未动。

---

## 七、本次落盘文件

| 文件 | 性质 |
|---|---|
| `开发工具/契约编译/漂移判定.py` | 改：`检测说明书一致` 转调唯一真源 |
| `公共契约/运行时/有界HTTP.py` | 改：通用部分唯一实现（+`满载响应回调`/`请求队列上限`/计数与诊断） |
| `运行核心/统一网关/安全/有界服务器.py` | 改：第二份实现下线，改继承 + 差异注入 |
| `开发文档/分析/批Q单据_Qi_判据多份表_20260924.md` | 新增：本单据 |

**未做**（按任务纪律）：未跑 `静态编译`/`动态编译`、未 `git add`/`git commit`、未改本批以外任何文件。
