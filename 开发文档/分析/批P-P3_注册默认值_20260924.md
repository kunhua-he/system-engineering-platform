# 批P · P-3 现状量清与维修方案（Q-e 回传）

> 状态：已量清 · **未改任何源文件**（按任务书第 2 步：手写形态需逐处改 ⇒ 先停下报告）
> 生成：2026-09-24 09:47 ｜ 开工ID：`开工-20260924-094620-9cb9` ｜ 所有者：子代理-Qe
> 落地：2026-09-24 10:20 由子代理 **Q-e2** 按**路线甲**执行（20 包接入既有唯一节点「参数声明透传」`_参数契约表`），
>   收口读数与活体判据见 `开发文档/分析/批Q单据_Q-e2_注册默认值接入_20260924.md`
> 上游：`开发文档/分析/批P清单_20260924.md` P-3 段 ／ `开发文档/分析/批Q清单_20260924.md` 一、二节
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

---

## 一、现状量清：命中 **93 条**（批P 记 64，实测 93）

判据（只读，复用既有唯一比对腿 `开发工具/契约编译/口径比对.py::比对注册口径明细`）：逐包跑
`读取契约口径(<包>/能力契约/参数契约.json)` ↔ `读取注册口径(<包>/__init__.py)`，取
`结果.存量漏声明列表`（= 契约声明了非空 `默认值`、注册字面量缺 `默认值` 键）。

同一条腿的权威读数（`python3.14 -m 开发工具.契约编译.漂移检测 --报告`）：

```
只报 存量漏声明 93 条（默认值 93／必填 0）；判红 0 条
```

⇒ 批P 的 64 与现场的 93 是同一条腿的两个数：**93 才是实测值**，且与
`开发工具/契约编译/注册口径存量基线.json` 的 `条目数.默认值漏声明 = 93` 一致（过期项 0）。

**分布（20 包 / 93 条）**：

| 条数 | 包 |
|---|---|
| 20 | `支持库/后端/数据操作支持库/数据集合` |
| 9 | `支持库/后端/测试支持库` ／ `支持库/后端/网络通信支持库/请求` |
| 5 | `办公文档支持库/表格文档` ／ `文件系统支持库/内容检索` ／ `文件系统支持库/文本补丁` ／ `系统核心支持库/环境变量` ／ `系统核心支持库/进程管理` |
| 4 | `办公文档支持库/演示文稿` ／ `数据操作支持库/数据交换` |
| 3 | `办公文档支持库/PDF文档` ／ `办公文档支持库/文字文档` ／ `数据操作支持库/响应塑形` ／ `数据操作支持库/时间日期` ／ `文档转换支持库/openpyxl提供者` |
| 2 | `支持库/后端/代码解析支持库` ／ `数据操作支持库/文本处理` |
| 1 | `数值计算支持库` ／ `网络通信支持库/网页解析` ／ `适配层/TreeSitter提供者` |

## 二、生成形态 / 手写形态：**93 : 0，全部手写形态**

两条独立读数（逐包）：

| 判据 | 读数 |
|---|---|
| `__init__.py` 含生成标记 `本文件由契约编译器自动生成` | **全仓只有 4 个**（`模块库/开工编排`、`模块库/组件生成`、`模块库/项目文档`、`支持库/后端/代码解析支持库/仓库地图`）；93 条所在的 20 包**一个都没有** |
| `__init__.py` 含 `def 注册能力` | **20/20 全含** |

⇒ **93 条 = 0 条生成形态 + 93 条手写形态**。批P「93 个生成形态 `__init__.py` + 35 个手写形态并存」这个前提与实测不符。

（形态不止「手写/生成」二分，手写侧还分三类：`进程管理` 是逐条 `注册表.注册(能力实现(...))`；`数据集合` 是表驱动
`for 能力id, 函数, 参数表, 返回类型 in [...]`；`代码解析支持库` 另有 5 个 `_包装*` 闭包。）

## 三、★ 决定性读数：**重跑能力定义编译器对 93 条零生效**

三条证据（源码 + 生成物门禁 + 真跑回执）：

1. **源码就是「不覆盖」**：`开发工具/契约编译/能力定义编译器.py::_写产物`（约 614-630 行）
   ```python
   if 路径.suffix in (".py", ".md") and 生成标记 not in 既有:
       if 路径.name == "__init__.py" and "def 注册能力" in 既有:
           return  # 手写入口含注册能力 → 合法，保留
   ```
2. **生成物一致性检查也承认它合法**：`开发工具/契约编译/生成物一致性检查.py`（约 388-392 行）对这类入口判
   「手工维护（合法）」——不判红，故门禁不会逼它归队。
3. **真跑**：`自修复工具.静态编译(类型=1, 模块ID=支持库/后端/系统核心支持库/进程管理)`
   ⇒ 任务 `24d861c47abd4738`，**退出码 0**，步骤2 明细「[生成] 能力定义编译 退出码=0」，
   而 `进程管理/__init__.py` 的 md5 **改前 = 改后 = `9e5b808cd4dc9b688e5d58dd9968c064`**，`默认值` 命中数仍为 3。

⇒ **批P 裁-2「授权重跑能力定义编译器批量回填 64 处」的前提不成立**：编译器按设计跳过这 20 个手写入口，跑 N 次也不会补上一条 `默认值`。

## 四、行为层活体读数（修前，真实调用）

| 调用 | 结果 |
|---|---|
| `系统核心支持库.进程管理.执行命令 {命令:"echo 活体探针"}`（**不传 超时秒**） | **成功=true**（守卫被绕过） |
| 同上 + `超时秒=600` | **失败，错误码 `须走异步腿`** |
| `系统核心支持库.进程管理.沙箱执行命令 {命令, 工作目录:"/tmp"}`（不传 超时秒） | **成功=true**（守卫被绕过） |

**受影响的 9 条能力**（93 条里契约 `超时秒` 默认值 ≥ 同步腿阈值 `网关参数校验面.同步腿超时上限秒 = 10.0`）：

```
办公文档支持库.PDF文档.解析PDF         默认值=60
办公文档支持库.文字文档.解析文字文档    默认值=60
办公文档支持库.演示文稿.解析演示文稿    默认值=60
办公文档支持库.表格文档.解析表格文档    默认值=60
系统核心支持库.进程管理.执行命令        默认值=60
系统核心支持库.进程管理.沙箱执行命令    默认值=60
网络通信支持库.请求.发送请求            默认值=10
网络通信支持库.请求.上传文件            默认值=10
网络通信支持库.请求.下载文件            默认值=10
```

⇒ P-5 裁决的「≥10 秒须走异步腿」对这 9 条**等于没落地**（不传参即绕过）。

## 五、唯一节点与三条候选路线

### 已有权威节点 A：「参数声明透传」（**45 包已接入；无生成器**）

形态：`注册能力` 函数体内写 `_参数契约表 = {能力id: [整条参数…]}` 字面量，注册行改为
`参数=_参数契约表[能力id]`；原手写 `参数=[…]` 字面量整体撤除。

- 已接入 **45 包**（`rg -l "参数声明透传（契约整条镜像" -g '**/__init__.py'` = 45）；93 条所在 20 包**全部未接入**。
- 由 `f59e2266`（2026-09-18「修 26 包入口注册丢参必填」）一次性**手工**注入，**全仓没有注入器/生成器**（该串只在 `__init__.py` 命中）。
- 漂移检测的 AST 读取器能解析该字面量（`_静态字面量` 的 `ast.Subscript` 分支），故可被门禁对账。

### 已有权威节点 B：编译器生成入口（4 包）——**不可用于这 20 包**

实测 `生成注册入口` 对 `进程管理` 的输出 vs 现状：

| 项 | 现行手写入口 | 编译器生成入口 |
|---|---|---|
| 行数 | 16 | 151 |
| 参数级 `"说明"` 次数 | **29** | **0** |
| `_包装*` 闭包 | 本包无（`代码解析支持库` 有 5 个） | 不支持 |

⇒ 生成形态会**丢掉全部参数级说明**，与「减少 agent 猜测」的底座目标反向。**不推荐**。

### 候选（请裁）

| 路线 | 内容 | 代价 | 风险 |
|---|---|---|---|
| **甲** | 20 包手工接入「参数声明透传」（照 45 包既有形状） | 20 文件 | 低（形状已被 45 包验证） |
| **乙** | 新增一个**注入节点**（挂 `开发工具/契约编译/`，作为编译腿子步骤），机械刷新 148 个手写入口的 `_参数契约表` | 1 处新节点 + 148 文件刷新 | 中（新增机制须裁；但一次收口「45 已接入 + 20 未接入」） |
| **丙** | 让编译器接管这 20 包（删手写、改生成形态） | 20 文件重写 | **高**（丢参数级说明；`_包装` 闭包不可表达） |

> 判据：华哥 G1/G2「有统一节点就接入、不要手写第二份」——路线甲用的是既有节点；路线乙是把它补成「有生成器」的节点。

## 六、第二条腿核查：**现场没有第二处在补默认值**

`rg -n '默认值.*=.*契约|setdefault.*默认值|参数\["默认值"\]\s*=' --glob '*.py'`（全仓）只命中三处，均非回填腿：

- `开发工具/契约编译/口径读取.py:323`（读注册字面量时**搬运已有键**，不补）
- `测试中心/开发工具/测试_对外契约变更判定.py:396`（测试夹具）
- `测试中心/开发工具/测试_注册口径漂移.py:495`（测试夹具）

⇒ 接入路线甲/乙不会撞车。

## 七、未做的部分与原因

- **未改任何源文件**（20 包入口一字未动）：按任务书第 2 步「手写形态需逐处手改 ⇒ 先停下报给我」。
- **未手补 93 处 `默认值`**：华哥口径「不许手补」；手补正是造第二套回填腿。
- **未提交 git**（禁）。
- **未跑**：发布门禁整轮；本批只跑了 `静态编译(类型=1)` 一个包（作「重跑编译器」的取证）。
- 顺带取证：`静态编译` 自身 `超时秒` 契约默认值 600 ≥ 10，同步腿被拒 ⇒ 本任务的编译腿**必须走异步腿**（`任务提交` → `任务查询`）。

## 八、给主会话的裁决请求

1. **P-3 修法路线**（甲／乙／丙）——尤其「是否授权新增注入节点（乙）」。
2. **是否先修行为后果最重的一半**：9 条 `超时秒 ≥ 10` 的能力可先单独走路线甲（改动面小、可立即活体验证守卫转红），其余 84 条随后。
3. **批P 的两处口径订正**：命中数 64 → **93**；「93 个生成形态 `__init__.py`」→ 实测**全仓仅 4 个**生成形态入口。
