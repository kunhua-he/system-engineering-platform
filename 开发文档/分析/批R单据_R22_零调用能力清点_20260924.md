<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R单据 · R-22 零调用能力清点与发现性归因

- 日期：2026-09-24
- 开工ID：`开工-20260924-124156-9e7d`（只认领本单据路径，1 条租约）
- 批次性质：**只读**（不改代码／契约／派生件；未跑编译、未 git 提交）
- 取数口径：一切经 MCP；MCP 不覆盖处（读 jsonl 明细）走 `系统核心支持库.进程管理.执行命令`，逐条标注

## 〇、一句话结论

平台**不缺调用数据**，缺的是**「谁在调」的来源维度**：账本 46,142 次里 **23,914 次（51.8%）是自动探针**，导致 **710 / 796 个能力的「常用参数」首选被污染成哨兵**；扣掉噪声后**真正零业务调用的只有 37 条（4.9%）**，其中 14 条正好是「agent 习惯用终端代替」的意图（Git 11 + 系统信息 3）。

## 一、账本取数口径（三维：次数／耗时／体量）

| 维 | 权威落点 | MCP 取数口径 | 状态 |
|---|---|---|---|
| 次数 | `公共契约/诊断/调用账本.py` → `<运行数据根>/调用参数账.sqlite3`（主键 `(能力id, 入参形状)`，网关每调一次自增） | `能力目录.常用参数组合`（读口）；全量读一次 108,003 字符 → 回执指针 | ✅ 有 |
| 耗时 | `工程缓存/运行数据/审计/安全审计.jsonl`（+`.1`/`.2` 轮转），每条含 `能力id`/`耗时毫秒`/`操作`/`请求id` | **无能力面**：`搜索能力(关键词=慢调用)` 0 命中、`安全审计` 包 0 条能力 ⇒ 本批走命令腿读 jsonl | ⚠️ 有数据、无取数面 |
| 体量 | **无权威落点**（`开发文档/方案/MCP与能力网关升级总方案_20260921.md:315-316` 的 F1 第二步未做：`安全审计.jsonl` 字段全集无返回字符数） | 代理读数 = `工程缓存/运行数据/MCP全量回执/<日期>_全量回执.jsonl`（超限返回原文堆） | ❌ 缺 |

实测：`调用参数账.sqlite3` 991,232 字节、12:37 仍在写；`安全审计.jsonl` 839,922 字节（12:37 在写）＋ `.1` 8.4MB ＋ `.2` 8.4MB。

## 二、清点读数（注册面／发现面／调用面）

- **注册全集 752**（制品树 148 份 `包声明.json` 的 `能力` 数组去重）；其中 `内部层=真` **69 条**（与已知 T8 的 69 一致）
- **发现面 690**（`能力目录.搜索能力` 返回的 `兜底判据.全表条数`）⇒ 差额 62 = 适配层／内部层被排除
- **账本内出现过 796 个 能力id**（>752：含 `测试.HTTP能力` 1551 次、`示例.包.示例能力` 551、`示例.包.读文件` 505、`文件管理.读取文件` 214 等测试／示例／历史 id）
- 账本总量 **46,142 次**；形状行数 **2,355**
- 分布：N=796，MIN=1，P25=34，**中位 35**，P75=37，MAX=3,554（`数据操作支持库.数据交换.反序列化JSON`）

### 2.1 污染（本批最重要发现）

- 形状 `__事实源探针__:逻辑型`：**752 条**（每个注册能力恰好 1 条）、**23,914 次** = 账本总量 **51.8%**
- 来源：`测试中心/第一批维修/测试_能力唯一事实源.py:67` 传 `{'__事实源探针__': True}`
- 后果（live 复现）：**710 / 796** 个能力的「常用参数」**首选**是哨兵。实测 `能力目录.常用参数组合(能力id='办公文档支持库.文档生成.文档统计', 上限=3)` → 第 1 组 `入参形状=__事实源探针__:逻辑型`、`调用次数=32`；真实用法（`文件路径:文本型`，2 次）被压在下面 ⇒ agent 若「照抄常用传参」会拿到探针哨兵
- 分布指纹：中位 35 ≈ 探针 32 + 真实 ~3；**34~37 的窄带就是探针扫全表的证据**

### 2.2 零调用（扣掉探针后）

注册 752 里 **37 条**一次业务调用都没有（**4.9%**）。按桶：

| 桶 | 条数 | 桶 | 条数 |
|---|---|---|---|
| Git操作 | 11 | 数据结构支持库 | 2 |
| 平台控制面 | 10 | 适配层 | 2 |
| 系统核心支持库 | 5 | 启动监督器 | 1 |
| 支持库 | 2 | 大语言模型支持库 | 1 |
| 并发控制支持库 | 1 | 模型服务 | 1 |
| 系统探针 | 1 | | |

## 三、慢 top5（`安全审计.jsonl` 25,665 条 / 790 能力）

| 排名 | 能力id | 次数 | 均(ms) | p95(ms) | max(ms) |
|---|---|---|---|---|---|
| 1 | `自修复工具.静态编译` | 246 | 1946.6 | 19,470.0 | **90,220.3** |
| 2 | `自修复工具.运行开发编译口` | 49 | 2683.4 | 10,046.0 | 10,059.7 |
| 3 | `自修复工具.构建平台客户端制品` | 54 | 2912.5 | 10,045.4 | 24,932.3 |
| 4 | `版本控制支持库.Git操作.推送` | 42 | 533.6 | 4,691.9 | 6,374.9 |
| 5 | `媒体转写.检查可用性` | 21 | 485.7 | 2,867.9 | 3,776.7 |

值得单列的**交互级慢**（直接决定 agent 每轮等待）：

- `能力目录.搜索能力` n=369、均 444.6ms、**max 22,433.9ms**（搜一次能力最慢 22.4 秒）
- `开工编排.开工即占` n=340、均 1381.8ms、**max 59,328.2ms**
- `系统核心支持库.进程管理.执行命令` n=301、均 787.7ms、**max 60,090.3ms**
- `自修复工具.仓库锁` n=49、**max 56,128.8ms**

## 四、体量 top（代理读数：超限回执条数）

`MCP全量回执/20260924_全量回执.jsonl` 实测 **28,028,654 字节 / 989 条**（保留 7 天，过期 2026-10-01）：

| 排名 | 能力id | 超限条数 | 判断 |
|---|---|---|---|
| 1 | `自修复工具.静态编译` | 67 | 本来该大（逐条明细） |
| 2 | `自修复工具.动态编译` | 22 | 本来该大 |
| 3 | `文件系统支持库.文件操作.写入文件` | 14 | **废话多**（回显而非结论） |
| 4 | `开工编排.开工即占` | 13 | **废话多**（回执／清单可投影） |
| 5 | `文件系统支持库.文本补丁.应用精确替换` / `批量应用精确替换` | 11 / 11 | **废话多** |

## 五、发现性归因（37 条逐条判）

**0 跳注入面现状**：`开发工具/薄壳/工具清单.py` 的 `capability_call` 描述里那句映射，来源唯一事实源 `公共契约/能力契约/常用意图.py::常见意图首选能力` —— **只有 4 条**：读文件／内容检索／跑命令／找能力。

### 5.1 「不知道有」类（**底层缺陷，要修**）—— 14 条

- **Git 11 条**：`Git操作.切换分支`、`创建工作区`、`合并分支`、`回滚`、`建议分支名`、`当前状态`、`挑拣合入`、`查询工作区`、`校验分支名`、`检查提供者`、`获取当前提交哈希`
  - 证据：同包 `Git操作.提交` 217 次、`推送` 42 次 ⇒ 能力**能调得动、被调过**；这 11 条零业务调用，而 agent 高频在终端跑 `git status`／`git rev-parse`／`git worktree`
  - **`Git操作.当前状态`（≈ status/log 摘要）零调用**是这一类最干净的例子
- **`系统核心支持库.系统信息.获取CPU信息` / `获取内存信息` / `获取操作系统信息` 3 条**：包声明**无 `内部层` 键**（实测 `支持库/后端/系统核心支持库/系统信息/包声明.json` 0 命中）⇒ 属公开能力却零调用；agent 用终端 `uname`／`sysctl` 代替

### 5.2 「不需要」类（正当）—— 18 条

- 探针／健康类 5：`支持库.适配层.psycopg提供者.检查可用性`、`适配层.MCP协议提供者.检查可用性`、`适配层.TreeSitter提供者.检查可用性`、`模型服务.检查提供者`、`系统探针.检查提供者`（由健康监督编排，不该 agent 手调）
- `启动监督器.健康监督.查询健康检查项`、`大语言模型支持库.模型连接器.资源快照`
- 底座原语 3：`并发控制支持库.创建互斥锁`、`数据结构支持库.创建栈`、`数据结构支持库.创建队列`（给别的能力用；**公开面过宽的候选**）
- `平台控制面.*` 只读自省 8：`授权.查询角色模型`、`策略闸门.查询策略类型`、`统一入口.查询稳定操作表`、`资源预算监督.查询必需预算项`、`提供者.动态库提供者.可用库列表`、`…宿主可用性`、`系统核心支持库.协议白名单.查询白名单`、`系统核心支持库.权限审批.查询规则表`

### 5.3 「取不到」类（体验缺陷）—— 5 条

- `平台控制面.包仓库.安装平台客户端制品`、`包仓库.校验可信仓库元数据`、`发布管理.对账发布状态`、`发布管理.恢复未完成发布` —— 发布运维腿，需前置资料（激活指针／仓库元数据）才能调；本会话实际走的是 `构建平台客户端制品`（54 次）
- `支持库.前端.前端契约校验.查询前端契约清单` —— 要「知道有这张清单」才会去查

## 六、修法清单（只写方案，未实施）

1. **账本加来源维度（治本）**：`公共契约/诊断/调用账本.py` 建表语句加列 `来源`（`人调`/`验证`/`探针`），写方 `运行核心/统一网关` 按探针哨兵参数或请求来源判定；读口 `模块库/能力目录/实现/能力目录.py` 的常用参数腿加 `WHERE 来源 <> '验证'`。**最小改动（治标）**：读口加 `入参形状 NOT LIKE '%__事实源探针__%'`。预期效果：710 条被污染的「常用参数首选」回到真实用法
2. **让探针别走网关**：`测试中心/第一批维修/测试_能力唯一事实源.py:67` 改进程内调用（若其目的只是校验注册面），或落独立库。预期效果：账本总量立刻掉 51.8%
3. **补 0 跳映射**（唯一事实源 `公共契约/能力契约/常用意图.py`，**只增不改**）：加 `git 状态=版本控制支持库.Git操作.当前状态`、`系统信息=系统核心支持库.系统信息.获取操作系统信息`、`跑测试=自修复工具.静态编译`。预期效果：14 条「不知道有」从零调用变有首选
4. **暴露耗时／体量两维**：给 `运行核心/运行诊断/安全审计` 加一条公开能力（如 `慢调用报告`，参数 `上限`/`时间窗`），判据复用既有 `慢调用报告()`（含 p50/p95/p99）；体量维按 `MCP与能力网关升级总方案_20260921.md:315` 的 F1 第二步，在 `安全审计.jsonl` 记录里加 `返回字符数`/`是否截断`。预期效果：慢／体量 top-N 一次调用取到
5. **内部层双声明收口（与已知 T1 同族）**：`支持库/适配层/Git提供者/包声明.json` 与 `支持库/后端/版本控制支持库/Git操作/包声明.json` 声明**同名能力 id 两条腿**，且 `内部层` 一条真（后端包 `包声明.json:302`）一条缺（适配层包）⇒ 发现面过滤必然漏。按「不保留旧腿」删一条

## 七、缺口与未做项

- **体量维无权威落点** ⇒ 本批只能给「超限回执条数」代理读数，不是真实返回体量分布
- **慢调用报告无 MCP 能力面** ⇒ 本批走命令腿读 jsonl（属硬纪律允许的「MCP 确实不覆盖的检查」）
- 注册面 752 为按 148 份 `包声明.json` 去重自算；发现面 690 取自 `搜索能力` 的 `全表条数`；**差额 62 的逐条拆解未做**
- 37 条的 `内部层` 标志**未逐条取**（我的读法被「适配层同名包」覆盖 ⇒ 全部读成 None）；只对 `Git操作.当前状态` 经 `搜索能力` 实测到 `内部层=真`
- §六 的 5 条修法**待登记到 `开发文档/未完成事项.md`**
- **未跑编译、未 git 提交**（本批明令）
- 命令腿限制记录：`执行命令` 同步腿要求 `超时秒 < 10`；命令串走 shlex 无 shell（`|` 与换行都不可用）；`读取文件` 的 `返回上限字符` 小于内容体量时只回**回执指针**（要全量传 `0`）
