# 审计落点总账（两条腿 · 冗余 · 写入越界）

> 最后更新：2026-09-23 ｜ 开工ID：开工-20260923-223210-ac2d
> 口径：**只读审计的落点对照**（发现 → 落点 → 债务登记 → 批次 → 状态），本件不新增结论，结论以审计件与现场为准
> 维护者：主会话 ｜ 审计件：`开发文档/分析/审计_两条腿与冗余_20260923.md`（开工-20260923-220111-6ef6）
> 维修方案：`开发文档/方案/维修方案_两条腿与写入越界_20260923.md` ｜ 债务清单：`开发文档/未完成事项.md` §四
> 说明：本件是**分析类**件，不是权威文档；条目销账后按分析类口径删除
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


---

## 一、怎么用这张表

- **落点**＝改哪里（文件:行，行号以 2026-09-23 现场为准，引用前重跑取证命令）。
- **登记**＝已写入 `开发文档/未完成事项.md` §四（表内无编号，以「项」计）。
- **批次**＝维修方案里的批 0/1/2/3/4/5。
- **状态**：`待派活` / `待裁` / `待核` / `已登记未修` / `未登记`。

---

## 二、阻断级：写入越界（本次新发现，本质是代码 bug）

| 编号 | 发现 | 落点 | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| X1 | 写入腿只判「是不是经 MCP」，**不判路径有没有被授权** | `支持库/后端/文件系统支持库/文件操作/实现/文件系统.py:191` | 已登记 | 批 1 层 1 | 待派活 |
| X2 | 文本补丁腿**显式声明**不管租约（声明与实现两套口径） | `支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py:11` | 已登记 | 批 1 层 1 | 待派活 |
| X3 | 文档写入腿同样无授权判据 | `开发工具/MD文档生成/人工编辑.py`（`租约\|开工\|授权\|允许修改` 零命中） | 已登记 | 批 1 层 1 | 待派活 |
| X4 | 内核只读锁对写入腿**不构成授权门** | `公共契约/运行时/仓库只读锁.py` `_要求网关凭证` docstring（`临时解锁()`「不受本门槛约束」） | 已登记 | 批 1 层 3 | 待派活 |
| X5 | 租约认领是**自证**的，无人校验授权范围 | `平台控制面/能力目录/实现/文件租约申请.py:176` | 已登记 | 批 1 层 1 | 待派活 |
| X6 | 派活任务信的「允许修改目录」是**纯提示词** | `开发文档/规范/项目细则.md`（无判据/无强制点/无反向试验） | 已登记 | 批 1 层 2 | 待派活 |
| X7 | 平台**无法区分主代理与子代理**（同一薄壳、同一网关凭证） | 薄壳进程 + `系统库网关凭证` | 已登记 | 批 1 层 2（需先补底座） | 待派活 |
| X8 | 授权判据本体**尚未存在**（层 1 未落地） | `公共契约/运行时/写入授权.py` —— 实测该文件**不存在**，全仓 `校验写入授权` 零命中 | 已登记 | 批 1 层 1 | 待派活 |

> **现场触发**：本次 4 路**只读**子代理全部越界（3 路落盘、3 路占租约、1 个后台 `静态编译` 任务），租约已由主代理经 `收工释放` 统一释放并逐条留痕。

---

## 三、两条腿（T 系列）

| 编号 | 发现 | 落点 | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| T1 | 适配层提供者包与后端成对包**各注册同一批能力 id** | `支持库/适配层/Git提供者/能力定义.json` ↔ `支持库/后端/版本控制支持库/Git操作/能力定义.json`（14/14）；同形 FFmpeg 7/7、Pillow 9/9、PyMuPDF 4/4、Tesseract 3/3 | 已登记 | 批 2 | 待派活 |
| T2 | 能力搜索 / 契约查看**两条 HTTP 腿** | `开发工具/能力网关/能力网关.py:87,89` ↔ `运行核心/统一网关/传输/网关边界面.py:353,398` | 已登记 | 批 2 | 待派活 |
| T3 | **路径安全 3 份实现**，均未转调唯一腿 | 3 份各自实现（未完成事项已登记「5 份实现、实测 6 处分歧」） | 已登记（存量） | 批 2 | 待核（6 处分歧是否仍在） |
| T4 | 能力发现双腿：stdio 薄壳 vs 40006 只读网关 | `开发工具/能力网关/能力网关.py:138` + `启动能力网关.sh`；`lsof -nP -iTCP:40006` 实测空 | 已登记 | 批 2 | 待派活 |
| T5 | 「跑一条命令」≥5 条腿并存 | `执行命令` / `执行命令集` / `沙箱执行命令` / `启动进程`+句柄四件套 / 文件面 | 已登记 | 批 5 待裁 | 待裁 |
| T6 | 同名参数 `环境变量` 在两条腿**语义相反** | `支持库/后端/系统核心支持库/进程管理/能力定义.json:34` 与 `:580` | 已登记 | 批 2 / 批 5 待裁 | 待裁 |
| T7 | 异步形态两条腿：能力自带 `异步=真` vs 平台任务腿 | `模块库/自修复工具/能力定义.json:332`（`异步` 定义）/ `:362`（说明）/ `:321`（超时秒默认 600） | 已登记 | 批 5 待裁 | 待裁 |

---

## 四、冻结存量 28 条（本仓自带检查器，全部「只报不拦」）

| 检查器 | 命中 | 落点 | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| `同动作双路径检测` | **10** | `开发工具/验证门禁/同动作双路径检测.py` 报出的 10 条（含 `平台控制面/统一入口/核心.py:56` ↔ `开发工具/契约编译/消费者契约.py:17` 稳定操作表；`平台控制面/备份恢复/备份契约.py:36` ↔ `备份执行.py:18` 三类数据表；`支持库/后端/项目文档支持库/实现/切块.py:14` 手写 markdown 正则） | 已登记 | 批 3 | 待派活 |
| `调用腿唯一性检测` | **5** | `运行核心/` 5 处自建 `sqlite3.connect` | 已登记 | 批 3 | 待派活 |
| `平台判断越界检测` | **13** | 裸读 `os.name`/`sys.platform` 6 条；调用点自行分叉 3 条等 | 已登记 | 批 3 | 待派活 |
| `技能库绕网关检测` | **0** | — | — | — | 绿 |
| **基线键盲区**（判据自身缺陷） | — | `开发工具/验证门禁/门禁公共.py:12`：基线键 = `规则｜文件相对路径`（**不含行号**）⇒ 同文件同规则下新增违规被静默算作存量（13 条命中只对应 6 个键） | 已登记 | 批 0 | 待派活 |

---

## 五、过度冗余（R 系列 + 判据两处实现）

| 编号 | 发现 | 落点 | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| R1 | **32 份转调垫片各自复制内联加载块**（25 份 13 行逐字相同、另 3 份 17 行；`importlib` 兜底块复制 25 遍） | `公共契约/运行时/导入前缀.py`（已收两段推导，兜底块未收） | 已登记 | 批 3 | 待派活 |
| R2 | 可用性探针各写一份（`检查提供者` 11 处、`检查可用性` 9 处） | 各提供者包 | 已登记 | 批 5 待裁 | 待裁 |
| R3 | 进程组回收 / 子进程主循环各写一份 | 子代理取证，需逐份定位 | 已登记 | 批 3 | 待派活 |
| R4 | 提供者直连两表 | `平台控制面/提供者/防火墙审计_规则.py:18` ↔ `开发工具/复用审计/提供者直连规则.py:22` | 已登记 | 批 5 待裁 | 待核 |
| R5 | 文档写入腿手写 markdown 正则 | `支持库/后端/项目文档支持库/实现/切块.py:14` ↔ 唯一腿 `办公文档支持库.轻量文本解析.解析Markdown块` | 已登记 | 批 3 | 待派活 |
| P1 | 类型短名映射 **3 份**，条目互不相同 | `运行核心/统一网关/协议/类型规格.py:49`、`开发工具/契约编译/能力定义编译器.py:72`、`开发工具/快速编译/运行快速编译.py:24` | 已登记 | 批 3 | 待派活 |
| P2 | 「正式类型表」判定 **5 处**各自判，容忍度不一致 | `聚合契约解析.py:169`、`口径比对.py:125`、`运行发布门禁_防回潮判据.py:166`、`公开调用完整性门禁_说明书解析.py:260`、`类型规格.py:182`（唯一真源 `公共契约/基础类型/类型表.py`） | 已登记 | 批 3 | 待派活 |
| P3 | 说明书对账判据 2 份（粗的那条会漏） | `开发工具/契约编译/漂移判定.py:370` ↔ `开发工具/公开调用完整性门禁_说明书解析.py:457` | 已登记 | 批 3 | 待派活 |
| P4 | 场景 `$动态` 允许字段集 2 份（第二份自称真源是前者却判自己那份） | `开发工具/HTML验证/路径安全.py:63` ↔ `开发工具/包体检.py:24` | 已登记 | 批 3 | 待派活 |
| P5 | 包必备件清单 **3 份** / 存量基线骨架 **5 份** | 子代理取证，需逐份定位 | 已登记 | 批 3 | 待核 |
| P6 | 父聚合包 `包声明.json` **重复 244 条能力声明** | 142 份 `能力定义.json` / 752 条唯一 id；148 份 `包声明.json` / 996 条声明 | 已登记（设计内双份，有测试钉着） | — | 不是缺陷（父份是手抄复制品，与 T1 漂移同源） |

---

## 六、文档层（D 系列）

| 编号 | 发现 | 落点 | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| D1 | **「三条铁律」同名不同义**（两套完全不同的东西，都在权威面上） | `README.md:120` ↔ `AGENTS.md:15` | 已登记 | 批 4 | 待派活 |
| D2 | 单腿铁律 **4 处各自表述**；`git add -A` 禁用 ≥8 处复述 | `开发文档/规范/项目细则.md`（唯一全文 115 行/4961 字符）、`AGENTS.md`、`README.md`、`开发文档/项目说明.md` | 已登记 | 批 4 | 待派活 |
| D3 | 应删未删 / 悬空引用 / 归档链接 | `开发文档/分析/`、`开发文档/方案/` 存量稿；`开发文档/主开发文档.md` 指向 `归档/`；`项目说明.md`/`README.md`/`决策记录/0019_*` 指向已归档的 `H/H2 段` | 已登记 | 批 4 | 待核（**必须逐份定性**） |
| D4 | `哲学总账.md` 7.1 / 7.5 **口径反转**（文档说「尚未建成/纸面/待接线」，现场实现+登记+冻结基线三处俱在） | `开发文档/哲学总账.md` 7.1 / 7.5 | 已登记 | 批 5 待裁 | 待裁 |
| D5 | 编译/测试入口**多于哲学 7.1 的两个公开能力** | 公开面 `自修复工具.静态编译` / `动态编译`（共用 `编译公共.py`）；269 个测试文件中 **268 个自带 `__main__`** 可直跑 | 已登记 | 批 5 待裁 | 待裁 |

---

## 七、读数与口径（批 0 用）

| 编号 | 事项 | 读数/落点 | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| M1 | **静态编译复跑**（承接 zcode 会话第 10 项）— **已复跑 2026-09-23 22:35（走任务腿）** | MCP `静态编译` `类型=0` ⇒ `成功 17 / 失败 2`：① `公开调用完整性`（阻断，退出码 1）＝**存量** `平台控制面/授权|说明书-缺返回载位 × 9`；② `md查重`（**只报告**，不阻断）命中 `支持库/后端/办公文档支持库/文字文档/说明/设计说明.md`。其余 12 项全通过（含 `重复腿与旁路` 9.53s 通过、`债务清单` 通过、`拒绝手写md` 通过） | 已登记（已回填结果） | 批 0 | **已复跑**；余 9 条存量 + 1 条 md查重 待定性 |
| M5 | **订正：「任务腿 MCP 面不可达」已失效** | 实测 2026-09-23：`开发工具/薄壳/薄壳服务.py:371` 的 `薄壳允许操作` **已含** `任务提交/任务查询/任务取消`，且本批经 MCP 任务腿**跑通了全仓编译**（`任务提交` 拿 `任务id` → `任务查询` 取结果）⇒ `未完成事项.md` §四「任务腿」那条的 ① 项**已失效**，只剩 ② 提示词（「>15 秒走任务腿」）与 ③ 输出 256 KiB 上限两项 | 已登记（已回填订正） | 批 0 | 待销账（① 项） |
| M2 | 代码地图覆盖率 | `开工准备` 回带：覆盖率 **0.9612**，50 个 .py 未进索引（1238/1288） | 已登记 | 批 0 | 待派活 |
| M3 | 激活制品落后提交 | 上线链：重建 + 重启后 `制品来源.提交..HEAD` = 0 | 已登记 | 批 0 | 待派活 |
| M4 | 本件审计的边界 | 未跑任何编译/测试/门禁（除标注「子代理经 MCP 实测」者）⇒ **没有发布门禁级结论**；未扫 `工程缓存/`、`.git/`、`__pycache__/`、`开发文档/归档/`、`开发文档/参考资料/` | — | — | 如实留档 |

---

## 八、已核对、判定「不是重复」的（防误修清单）

| 件 | 判据 | 结论 |
|---|---|---|
| `能力定义.json` 能力 id | 142 份 / 752 条 / **唯一 752（0 重复）** | 无重复 id |
| `说明书生成器.py` ×2 | 已写明消歧（输入/输出形态/调用方/能力面全不同） | 不是重复 |
| `验证报告.py` ×2 | 两个不同 dataclass（`验证报告` / `验证结果`） | 不是重复 |
| `文档生成.py` ×2 | `模块库/*` 只持能力 id 经唯一调用器转调 | 正常分层 |
| `能力入口.py` / `核心.py` 同名后缀 | 「一包一 `__init__.py` 注册能力」约定产物 | 不是重复 |
| 父聚合包 244 条重复 id | 设计允许，`测试中心/第一批维修/测试_HTML验证器.py` 有断言钉着 | 设计内双份（但与 T1 漂移同源） |
| 16 个 `内部层=真` 包 / 69 条能力 | 页面侧引用实测 0 命中 | 「两面一起撤」的页面面已干净 |
| `支持库/适配层/` 内部适配器 | 系统适配器豁免 | 已声明豁免，不算两条腿 |

---

## 九、本件的边界

- 本件**不新增**审计结论；每条结论的判据以审计件与现场为准。
- 「已登记」指已写入 `开发文档/未完成事项.md` §四，**不等于已修**。
- 行号以 2026-09-23 现场为准；引用前重跑取证命令（`开发文档/项目说明.md` 口径：会变的数字不得写死在权威文档正文）。

---

## 十、批M 落点（2026-09-24，承接批L 三条 ＋ zcode 留棒三条）

> 方案：`开发文档/方案/维修方案_批M_写入腿与开窗豁免_20260924.md` ｜ 开工ID：`开工-20260924-045552-25eb`
> 上游：`开发文档/分析/批L清单_20260924.md` ＋ zcode 会话 `sess_bb791744-4cc8-4738-9941-30db53100547` 的留棒表
> 批K 已落盘并推送：`95336c83`（任务终态不回写收口：K1 会话登记集归池 ＋ K2 门面同步射程 ＋ K3 手抄复制转唯一节点）

| 编号 | 发现 | 落点（文件:行，引用前重跑取证） | 登记 | 批次 | 状态 |
|---|---|---|---|---|---|
| M-1 | **`重锁一遍()` 无声吊销活跃会话写窗口**（＝zcode 留棒 3-1） | `公共契约/运行时/仓库只读锁.py` `重锁一遍()`（约 :880，函数体 = `return 上锁全仓()`）；`上锁全仓()`（约 :826）逐条无条件上锁、不读租约账 | 已登记（§四 7.10） | 批M-2 | 待派活 |
| M-2 | **md 落盘两条腿；权威能力走素腿**（＝zcode 留棒 3-2） | 素腿 `支持库/后端/系统核心支持库/资源管理/实现/资源管理.py:186`；成熟腿 `支持库/后端/文件系统支持库/文件操作/实现/文件系统.py`（`写入文件` 四件齐）；受害者 `模块库/项目文档/实现/写文档.py:9` | 已登记（§四 7.10） | 批M-1 | 待派活 |
| M-3 | **`Git操作.提交.路径列表` 口径仍是绝对**（传参两条腿的最后一段） | `支持库/适配层/Git提供者/能力定义.json` 该参数 `说明`；`通用纪律.工具姿势` [28]；对侧 `开工编排.开工即占.修改路径` = 仓库相对 | 已登记（§四 7.10） | 批M-3 | 待派活 |
| M-4 | **zcode 会话第 10 项未收**（重建制品 ＋ 重启网关 ＋ MCP 验收新能力 ＋ 静态编译复跑） | `制品来源.提交..HEAD`；全仓 `静态编译` `类型=0`；`自修复工具.仓库锁` 四档 | 已登记（§四 7.10） | 批M-4 | 待执行 |
| M-5 | **子代理 MCP 精度四类根因**（能力 id 短名／参数名静默剔除／`值字段` 深裁丢证据／长命令没走异步腿） | ① 已修（回执自带 `候选能力id`）；② `参数名不在契约即拒`（H5-b 已裁决 fail-closed，待落地）；③ `开发工具/薄壳/薄壳服务.py` `_兜底裁剪`；④ `通用纪律.工具姿势` [25] | 已登记（§四 7.10） | 批M-5 | ① 已修；②③ 待派活 |

**开窗豁免的三个入口（同一份判据，禁止各写一套）**：

| 入口 | 落点 | 现状 |
|---|---|---|
| `pre-commit` 钩子 | `开发工具/git钩子/pre-commit` `# >>> 锁核验开始` 段 | `未锁数 > 0` ⇒ 提交被拒（批L 亲历） |
| `--查看` | `运维脚本/仓库锁.py:54 _查看()`（退出码判据约 :72） | 对开窗报退出码 1 |
| `--补齐` | `运维脚本/仓库锁.py:101 _补齐()`（退出码判据约 :129） | **会把别人正在用的窗口一把锁上**（批L 当场踩过） |

**本批派活**（写集不重叠）：M-a = `支持库/后端/系统核心支持库/**`（M-2）；M-b = `公共契约/运行时/仓库只读锁.py` ＋ `运维脚本/仓库锁.py` ＋ `开发工具/git钩子/pre-commit`（M-1）；M-c = `支持库/适配层/Git提供者/能力定义.json` ＋ `开发工具/薄壳/工具清单.py`（M-3，**等 M-a/M-b 收工后**才动薄壳）。

**本批边界**：本件只记落点与状态，**不新增审计结论**；行号以 2026-09-24 现场为准。
