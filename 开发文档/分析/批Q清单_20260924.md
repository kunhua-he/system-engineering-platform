# 批Q 清单（维修方案 · 按华哥「统一节点」口径收口）

> 状态：执行中 ｜ 生成：2026-09-24 09:30 ｜ 开工ID：`开工-20260924-091605-fe7f`
> 上游：`开发文档/分析/批P清单_20260924.md`（P-1..P-14）＋ 华哥 2026-09-24 会话裁决口径
> 口径：哲学 1.2（同一件事两套实现即缺陷）／7.5（编译只有 2 个公开能力）／12.1（三轮）／13.3（四环链）
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

---

## 一、华哥本轮口径（凡与批P 冲突，以此为准）

| # | 原话口径 | 落到本批的判据 |
|---|---|---|
| **G1** | 「全部切成统一节点…而不是单独的自己组件一个机制或者内置自己手写代码」 | 已有权威节点/机制的一律**接入**；再写第二份实现 = 缺陷 |
| **G2** | 「只要有报错…看看有没有权威节点，有统一节点的全部切成统一节点」 | 排障第一动作 = **找权威节点**，不是就地打补丁 |
| **G3** | 「资源是无限的，只要注意回收不要导致资源泄露」 | 机制必须**可回收**；把「过期未回收窗口」永久降级成「永久开窗」= 制造泄漏 |
| **G4** | 「到底是相对的还是绝对的，全部传参都固定，减少 agent 的猜测」 | 普通路径参数 = **项目根相对路径**（posix、不带 `..`），绝对兼容；基准参数（`项目根`）写绝对 |
| **G5** | 「一定要把底座做得十分干净」 | 收口后**不留兼容壳、不留旧名别名** |
| **G6** | 「单个节点，不要手写第二份」 | 生成侧不得把机器绝对路径写进产物；写盘一律经写入腿 |

---

## 二、批P 14 条的处置道

### A 道 · 已自裁，直接派活

| 项 | 内容 | 派给 | 状态 |
|---|---|---|---|
| P-1 | **阻断级** · 过期未回收窗口被算成真缺口 ⇒ pre-commit 拒提交 | Q-a | **执行中** |
| P-3 | 64 条注册字面量丢 `默认值`（异步腿守卫对它们失效） | Q-e | 待派 |
| P-4 | 聚合 `包声明.json` 与子包 `参数契约.json` 类型漂移 12 条 | Q-b | **执行中** |
| P-5 | 异步腿阈值三处不一致（10／10／15） | Q-b | **执行中** |
| P-6 | 收敛计算只遍历现场桶 ⇒ 「该降未降」永远发现不了 | Q-c | **执行中** |
| P-7 | 6 条存量基线该升该降 | Q-c | **执行中** |
| P-8 | 全枚举门禁未接统一骨架（第 5 条腿基线机制缺位） | 主会话 | **已修已提交** `befb758e` |
| P-9 | 生成侧把机器绝对路径写进产物（5 个落点） | Q-f | 待派 |
| P-10 | `显示相对路径` 零消费方（P-9 的根因：无强制点） | Q-f | 待派 |
| P-11 | 存量产物已进制品树（8 份，非 `.py` 逐字节保留） | Q-g | 待派（含须裁项） |
| P-12 | 硬编码解释器无回落 3 处 | 子代理＋主会话 | **已修已提交** `befb758e` |
| P-13 | 技能侧路径口径与已裁口径冲突 | Q-d | 待派 |
| P-14 | 孤儿基线 3 个／计数段未消费／家目录写法等观察项 | Q-g | 待派 |

### B 道 · 须华哥裁决（不派）

| 项 | 冲突点 | 为什么哲学不能自裁 |
|---|---|---|
| 裁-1（P-2） | fail-closed 是否推翻「未知字段一律忽略」 | 反向条款是**逐字落盘的哲学第 5 条 2 项** |
| 裁-6（P-11） | 存量产物是删断言还是改写法 | 涉及松开断言／扩允许表 = 口径变更 |

---

## 三、统一节点收口（G1／G2 的直接产物，独立于批P）

> 不是「新问题」，是**同一类病反复被不同人发现** —— 同一职责两份以上实现。

| 编号 | 收口对象 | 现状 | 权威节点（收口目标） | 状态 |
|---|---|---|---|---|
| U-1 | 存量基线收敛计算 | 两份分叉，一对一错 | 取兄弟正确形状抽唯一函数 | 随 Q-c |
| U-2 | 路径传参口径存量 103 条／40 包 | 三套口径并存 | `公共契约/基础类型/字段名册.取路径写法口径`（唯一腿已建） | 待派 Q-h |
| U-3 | 第二套路径解析 38 处／30 文件 | 唯一腿已存在却未被接入 | `公共契约/运行时/平台适配/路径解析.解析路径` | 待派 Q-h |
| U-4 | 生成期绝对路径注入（5 落点） | 无强制点 | 待接线（P-10 的相对化强制点） | 随 Q-f |
| U-5 | 解释器选择 | 4 种写法 | `sys.executable` | P-12 已清 1 类；余 4 类**须裁决**（`测试支持库` 强制 `python3.14` 是有意发布纪律） |
| U-6 | 32 份转调垫片内联加载块 | 25 份 13 行逐字相同 | `公共契约/运行时/导入前缀.py` 一处 helper | 待派 Q-i |
| U-7 | 提供者可用性探针 | `检查提供者` 11 处 ＋ `检查可用性` 9 处 | 待裁（被探命令不同可能属正当原子差异） | 须裁 |
| U-8 | 判据多份表 | 类型短名映射 3／正式类型表判定 5／说明书对账 2／场景 `$动态` 2／包必备件 3／存量基线骨架 5 | 各收成 1 份，统一转调唯一真源 | 待派 Q-i |

---

## 四、本批完成判据（逐条可跑）

1. **P-1**：`自修复工具.仓库锁 动作=查看` 在无活跃租约时 `缺口数=0`；且**写腿被杀留下的真缺口仍判红**（反向样本必须有）。
2. **P-4／P-5**：`执行命令.超时秒` 传整数不再 `参数不合法`；阈值三处同值（代码是唯一事实 ⇒ 10）。
3. **P-6**：单元 `基线{A|X:3, B|Y:2} + 现场[]` ⇒ `收敛=2`；真仓 ⇒ 收敛 7 桶。
4. **P-7**：六条基线**走各自门禁出口重建**后，对应门禁全绿（不许手改数值）。
5. **收口门禁**：`自修复工具.静态编译`（`类型=0`）⇒ 失败门禁空，或失败项全部有存量基线解释。
6. **绝对底线**：同一职责全仓只剩一处实现（`rg` 可复核）。

---

## 五、纪律（每个子代理都必须遵守）

- 一切经 MCP；改文件用 `文件系统支持库.文本补丁.应用精确替换`（参数名 `旧文本`／`新文本`）。
- 开工 `开工编排.开工即占`（`修改路径` 填全）；收工 `开工编排.收工释放`（★ 参数名 `租约id清单`，传**租约id值**不是 `开工ID`）。
- 子代理**不改别人文件、不编译、不提交**；主会话统一收口。
- 派生件不许手改（`能力契约/参数契约.json` 由编译口重算）。
- 基线只减不增；能走门禁出口重建的不许手改数值。

---

## 六、本件边界（如实留档）

- 批P 的 P-1 因果靠「租约账记录 ＋ 所有者 ＋ 过期／回收时间戳」三证，29 条**未逐条复原归属**。
- 本件是**分析类**件，条目销账后按分析类口径删除。
- **未跑**：完整发布门禁整轮（111–132 秒）；`测试体系门禁 --真跑`（非只读档位）。
- 制品态三份 `模块库/自修复工具/实现/*.py` 仍是旧写法（`解释器 = '/opt/homebrew/bin/python3.14'`），**待下次制品重建消除**。
