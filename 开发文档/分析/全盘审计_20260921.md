# 全盘审计（MCP 与能力网关 · 债务清单 · 方案落地）· 2026-09-21

> 最后更新：2026-09-21
> 口径：**只读盘点**。所有数字为现场跑命令取证；所有判定带 `文件:行` 或命令原文。
> 开工 ID：`开工-20260921-222911-7c83`
> 维护者：全盘审计（主代理机器判据 + 8 路只读子代理分片核实）
> 状态：起草（机器判据部分已完成；条目核实部分待 8 路回传回填）
<!-- 机器管理｜类型：分析｜生成器：人工维护 -->

---

## 零、一句话结论

**「还有多久」的答案是：真实待办只有 21 条，串行约 52 小时、8 路并行约 7 小时；但真正的瓶颈不是条数，是「账目失真 + 一次访问拿不全 + 三个裁决」。**

- **账目失真**：债务清单机器校验报「一致」，但**它只数行、不读状态**。8 路现场核实后，**69 条里 48 条已修/已消解/不是缺陷**（只需销账），**真待办只有 21 条**（未修 11 + 部分修 10）。
  ⇒ 「69 条待办」是**虚高数字**，直接拿它估工期会高估 **3 倍以上**。
- **一次访问拿不全**：本轮审计**自己踩了 B1 六次**（第 4–6 次）——`返回上限字符` 写错层被静默忽略、单字段超上限就整段丢（投影 + 提高上限**都救不了**）。这不是偶发，是**结构性**的：只要薄壳按「丢整键」处置，任何大输出都必须靠外部摘要器绕开。
  ⇒ 这正是华哥「一次访问 vs 三次访问」成本问题的**结构性来源**。
- **三个裁决决定工期差 8 小时**：**#187**（74 份设计说明，450 分钟 vs 30 分钟，**单项差 6.5–7 小时**）、**#60**（修探针 vs 声明降级）、**#85**（手写 HTTP 客户端收口到哪个统一件）。

**门禁面好消息**：编译口 **13 个阻断项全部退出码 0**，**没有阻断级债务**；剩余都是「存量红 + 只报不拦」。

---

## 一、机器硬判据（本轮现场取证，全部可复现）

### 1.1 债务清单（权威入口）

**取证**：`python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验`（工作目录＝仓库根）

| 项 | 值 |
|---|---|
| 机器校验结论 | **生成区与正文实数一致（机器生成，未漂移）** |
| 文件 | `开发文档/未完成事项.md`，306 行 / 108,668 字节 |

**清单自报账目**：

| 分区 | 条数 | 编号 |
|---|---|---|
| 一、待修（可直接派活） | **0** | — |
| 二、待执行的大项 | **0** | — |
| 三、待裁决 | **0** | — |
| 四、新增债务 | **27** | #60 / #181–#219 等 |
| 七、7.1–7.6 第一轮审计 | **33** | #67–#106 |
| 七、7.8 20260918 批审计 | **1 项**（无编号） | — |
| 七、7.9.1 跨平台无关真缺陷 | **4** | #156 / #162 / #163 / #172 |
| 七、7.9.2 跨平台补充 | **5** | #173–#177 |
| **合计（计入待办）** | **69** | — |
| **全文件编号条目** | **72** | 69 + 3（五段覆盖缺口，不计待办） |

**不计入待办的三类**：五、已知覆盖缺口 3 条（#4 / #6 / #7，**不是缺陷**）；7.7 + 7.9.5「不是缺陷」**12 条**（防误修清单）；7.9.4 与既有审计重复 **6 条**。

### 1.2 ★ 清单自身失真（本轮最重要的发现）

机器校验说「一致」，**但它只数行、不读状态**。逐条读 §四 正文可见：**至少 15 条已写「已修／已关闭」，却仍列在待办账目里**。

| 编号 | 正文自述状态 |
|---|---|
| #181 | 已修（并留作判据存在性证明） |
| #184 | ② 已修；① 属逐批推进 |
| #185 | 已修（238 处全清零） |
| #186 | 已修（含三拍反向验证） |
| #188 | 已修（提交 `91084bcc` + 修正 `3f707ff9`） |
| #189 | 已修（含反向验证与全仓干跑） |
| #192 | **已收口**（真根因与假设不同） |
| #195 | **本条已关闭**（四条修复全部落地并验收） |
| #201 | **本条已关闭**（⇒ 原文） |
| #202 | 已修 |
| #214 | 已修（含 23 tests OK） |
| #216 | ①② 都已修（含 74 tests OK） |
| #217 | 已修（含 24 tests OK） |
| #218 | 已修（含 16 tests + 221 tests OK） |
| #219 | 已修（含 12 tests + 现场反向验证） |

⇒ **「69 条待办」不能直接当工作量基线用**。华哥口径是「已修条目直接删」，这 15 条是**该删未删**（历史账没清）。

### 1.3 编译口（`--变更 --联动`）

**取证**：`python3.14 -m 开发工具.开发编译口.编译口 --变更 --联动`（经摘要器，因原文 76,921 字符）

| 项 | 值 |
|---|---|
| 退出码 | **2** |
| 五态结论 | **未核验**（fail-closed）—— 原因：**变更集里无 .py** ⇒ **语法编译这一项一次都没跑**，不得读成「通过」 |
| 总耗时 | 12.7 秒（基线 12.2 秒，正常） |

**阻断项（13 项）——全部退出码 0**：

| 阻断项 | 结论 |
|---|---|
| 摘要闭合 | 已在步骤 2 核对，跳过（避免同轮重复执行） |
| 契约漂移 | 包数 **140**；契约能力 **746**；已解析注册 **724**（未解析注册 22 / 返回口径未解析 0 / 参数口径未解析 **57**） |
| 公开调用完整性 | **无新增违规**；存量红：`平台控制面/授权｜说明书-缺返回载位 × 9` |
| 能力id冻结基线 | 通过（新增 33 条合法）；★ **1 条内容指纹变化须重建基线**：`系统核心支持库.资源管理.创建内容摘要`（`378ff95bd8687fcc → 2c0ac7f72132ee12`） |
| 验证场景体检 | 全部通过：140 个含场景包目录，0 不合格；期望失败用例 89 个状态码全一致 |
| 重复腿与旁路 | 绿（**只报不拦**）：同动作两条路存量 14 / 调用腿唯一性 5 / 技能库绕网关 0 / 平台判断越界 13 / 系统根全枚举 0 |
| 测试体系门禁 | 通过（限**快速级**）：240 测试文件全可导入、0 零测试、0 未解释跳过；★ **④ 真跑级未执行 —— 本通过不含任何用例执行结论** |
| 防回潮·反向破坏 | 13/13 通过；恒真检查 True（判据非恒真） |
| 参数可用性 | 扫描 **2247** 个参数，**违规 0**；存量基线 0 |
| 规范漂移 | 扫描 26 份规则文件，**漂移 0** |
| 债务清单 | 一致 |
| 目录树与命令 | 一致 |
| 拒绝手写md | 通过（本轮改动 md 0 份） |

**只报告类判红（2 项）**：

| 项 | 内容 |
|---|---|
| 测试伪装门禁（退出码 1） | **14 条**：规则 3「负面断言强度不足」8 条、规则 2「裸桩缺 `autospec=True`/`spec=`」4 条、规则 1「patch 生产路径」1 条、观察「断言全为弱断言」1 条 |
| md查重（退出码 1） | 有重复 md（原文被摘要器截断，仅见 `支持库/后端/媒体处理支持库/FFmpeg媒体/说明/使用说明.md`） |

### 1.4 手写 md 归一进度（#184 ①）

**取证**：`python3.14 -m 开发工具.MD文档生成 --待归一清单`

| 项 | 值 |
|---|---|
| 无机器印记的 tracked md | **366 份**（清单里写的 700 份已过时） |
| 耗时 | 240 毫秒 |

**按顶层目录分布**：

| 目录 | 份数 |
|---|---|
| 支持库 | **256** |
| 模块库 | 47 |
| 平台控制面 | 25 |
| 开发文档 | 17 |
| 技能库 | 9 |
| 开发工具 | 5 |
| 运行核心 | 4 |
| 公共契约 | 1 |
| 启动监督器 | 1 |
| 项目适配层 | 1 |

⇒ **大头在 `支持库/`（256/366 = 70%）**，且绝大多数形态是 `<包>/说明/使用说明.md` 与 `<包>/说明/设计说明.md`。

### 1.5 网关与工作区状态

**取证**：`开工编排.开工准备` + 能力搜索

| 项 | 值 |
|---|---|
| 网关健康 | 200 |
| 能力数（装配口径） | **746**（不是清单里的 745；另一个 agent 已增） |
| 装配告警 | **0** |
| 工作区 | **干净**（未提交修改数 0） |
| 当前 HEAD | `37957f10`（#219 防回潮） |
| 分支 | 主干 |
| 发现面能力总数 | **674**（`能力目录.搜索能力` 实测） |
| 契约能力（口径 140 包） | **746** |
| 证据可信度 | 50 / 部分可信（依据：项目规则 20 + 代码地图 30 + 指纹匹配 50；**指纹原子能力缺口 ⇒ 匹配数恒 0**） |

**★ 能力数三套口径**（引用必须注明）：**746** 契约条数 / **674** 发现面 / **724** 已解析注册。

### 1.6 缺失原子能力（`开工准备` 自报，4 项）

| 能力 | 说明 |
|---|---|
| `发布治理.当前工作区指纹` | 内容级工作区指纹（`git status --porcelain` + 每文件 sha256）。**缺它 ⇒ 工作区指纹为空 ⇒「当前代码指纹命中的成功证据数」恒为 0** |
| `验证证据.读取成功记录` | 读 `开发文档/项目证据/验证历史.jsonl` 按口径筛 |
| `代码地图.查询状态` | 跑 `codegraph status` 返回 `{可用, 状态, 详情}`；现只能给「`.codegraph` 目录是否存在」弱判据 |
| `开工编排.验证计划` | 按修改路径推导受影响测试目录与定向验证命令 |

---

## 二、★ 本轮审计自身踩到的缺陷（活证据，非引用）

### 2.1 B1「返回体超限被丢」——本轮复现 **2 次**（累计第 4、5 次）

| 次序 | 现场 | 结果 |
|---|---|---|
| 第 4 次 | 把 `返回上限字符` **写进了 `参数` 里**（按 #199 定性，它应与 `参数` **平级**） | 参数被**静默忽略** → 生效上限仍 6000 → `结果表` **整段丢弃**，只回 96 字符摘要 |
| 第 5 次 | 把 `返回上限字符=30000` 放**顶层**（生效）+ `值字段=["结果表"]` 投影 | 单字段 **77,777 字符 > 30,000** ⇒ **整段丢弃**，原话「每个字段单独都超过上限，已全部丢弃」 |
| 第 6 次 | 顶层 `返回上限字符` **忘了传**（跑 3 条 rg 的 `执行命令集`） | 8,616 > 6,000 ⇒ **整段丢弃**，只剩 93 字符摘要 |

**结论（这是方案 §八 8.1 第 2 条的加强证据）**：
- 「投影 + 提高上限」**都救不了**「单字段超上限」这条路径 —— 因为处置是**丢整键**，不是缩。
- 本轮被迫写了 **2 个外部摘要器**（`~/.hermes/cache/scratch/审计_编译口摘要.py`、`审计_待归一摘要.py`）才能拿到编译口与待归一清单的结论。
- **只要 B1 不修，任何「一次访问拿全」都是空话** —— 这正是华哥说的「一次访问 vs 三次访问」成本问题的**结构性来源**。

### 2.2 裁1「未知参数被静默忽略且不回带清单」——现场复现

**现场**：`能力目录.搜索能力(查询="开工准备")` → `查询` **被静默忽略**，返回体里 `关键词=""`、回了 5 条**无关的 OCR 结果**，**没有任何「未知参数」提示**。改传正确参数名 `关键词` 才拿到 2 条正确结果。

**这正是方案 §九 要求修的那条**：`当前流程图.md:63/119` 写「立即拒绝」、`网关调用最小示例.md:129-138` 写「一律忽略」，方案选「**忽略 + 回带清单**」—— 现状是**忽略但零回带**，调用方只能靠猜。

### 2.3 `执行命令` 的 cwd 是**制品根**（设计，但踩坑）

`pwd` 实测 = `工程缓存/制品仓库/平台客户端环境/平台客户端`。**源码树必须用绝对路径或显式 `工作目录` 参数**。本轮第一次跑 `rg 开发文档/...` 报 `No such file or directory`，就是踩了这个。

⇒ 与 #217 同源：「我该操作哪棵树」在**每一处**都要显式化（#217 只修了 `模块库/自修复工具/实现/` 的 3 个能力级入口，**`执行命令` 自身的默认 cwd 未在 #217 范围内**）。

### 2.4 主代理独立复核 #218（不依赖子代理结论）

**起因**：路 4 报出「文本子串 `实现/` 目录下仍有 **28** 处 `parents[`」，而 #218 自述「`parents[N]` 真推导在 `支持库/后端` 下**归零**」—— 两者必须对齐。

**取证**：自写 AST 检测器（`~/.hermes/cache/scratch/审计_垫片parents真推导.py`）扫 `支持库/**/实现/*.py`。

| 项 | 值 |
|---|---|
| 含 `parents[` **文本**的 py 文件 | **28** |
| 其中含**真推导**（AST 表达式本身）的 | **9** |
| 分布 | **适配层腿 7 处** + **后端腿 2 处** |

**逐条甄别（读现场原文 + 检测器口径）**：

| 处 | 判定 | 依据 |
|---|---|---|
| 适配层腿 7 处（`Git提供者/仓库根白名单.py:36`、`MLXWhisper/PDF隔离/Pillow/PyMuPDF/reportlab/代码地图` 各 `子进程入口.py`） | **合法** | #218 明写「适配层腿按层数取根**不动**，本轮不在范围内」；且检测器 `_后端源码()` = `支持库/后端/**/*.py`，**适配层不在扫描面** |
| `大语言模型支持库/模型连接器/实现/本地启动准备与守卫.py:482` `parents[2] / "包声明.json"` | **有意豁免** | 检测器 `测试_导入前缀.py:178-179` 原文：`if "包声明" in ast.unparse(节点.value) or 名称.endswith("包目录"): continue  # 包内相对文件：不是「取系统根」，不判` |
| `图像处理支持库/图像解码/实现/子进程入口.py:49` `parents[4] / "工程缓存" / "制品仓库" / "平台客户端环境"` | **有意保留** | 该文件 docstring `:4` 原文：「适配层腿仍是 `parents[4]`，**两腿实测各自都指向项目根，不是错误**；见迁移清单 §10.8」；`:5` 又写该处属「部署自举（激活指针解析 + 注入）—— 这一处是后端腿侧的必要能力，**必须逐字保留**」 |

**结论：`#218` 不是假绿** —— 30 份垫片 **30/30 都已改用 `取根前缀`**（路 4 实测「未用取根前缀: 0」），检测器口径内确实归零，2 处残留**均有明写依据**。

**★ 但暴露一个判据覆盖面缺口（建议登记为低优先观察项）**：

`测试中心/开发工具/测试_导入前缀.py:163-182` 的 `test_无按层数取系统根的赋值` **只遍历 `ast.Assign` / `ast.AnnAssign`** ⇒ **非赋值形态的 `parents[N]` 扫不到**（函数调用实参、`return`、比较式、f-string 等）。`图像解码/子进程入口.py:49` 正是「内联在 `候选表.append(...)` 里」的形态 —— 它没被判红，**不是因为合规，是因为判据看不见**。

⇒ 若将来有人把取根写成 `foo(Path(__file__).resolve().parents[4])`，**判据静默放过**。修法很小（把遍历条件从「赋值语句」放宽到「任意表达式」，再排除字符串 `Constant` 与「包声明」两类豁免），但**不改就等于留了一个可绕过的口子**。

---

## 三、条目核实（8 路只读子代理分片）

**8 路已派出**（`deleg_e494af81`），产出落 `工程缓存/审计中间产物_20260921/`：

| 路 | 范围 | 条数 |
|---|---|---|
| 路 1 | §四 #60 / #182 / #183 / #184 / #185 | 5 |
| 路 2 | §四 #186 / #187 / #188 / #189 / #193 / #200 | 6 |
| 路 3 | §四 #196 / #199 / #201 / #202 / #204 / #208 / #210 / #212 | 8 |
| 路 4 | §四 #214 / #216 / #217 / #218 / #219（**独立复核另一个 agent 的修复**） | 5 |
| 路 5 | §七 7.1–7.3（#67–#85） | 13 |
| 路 6 | §七 7.4–7.6（#89–#106） | 18 |
| 路 7 | §七 7.8 + 7.9.1 + 7.9.2 | 1 项 + 9 条 |
| 路 8 | 方案 §二 A–F 全层 + §三 zcode L1–L11 落点 | 全 |

### 3.1 总账（**69 条编号条目 + 7.8 那 1 项，全部现场核实完毕**）

| 判定 | 条数 | 含义 |
|---|---|---|
| **已修 / 已消解 / 不是缺陷** | **48** | 现场已成立，**只需销账** |
| **未修** | **11** | 真待办 |
| **部分修** | **10** | 真待办（剩一部分） |
| 7.8（表内无编号） | 1 项 | 部分证真（14 份件全在，10 份结论未登记） |
| **合计** | **69 + 1** | — |

⇒ **「69 条待办」的真实构成是「48 条该销账 + 21 条真待办」**。这是本次审计最重要的一个数字纠正。

**派活覆盖自检**：8 路覆盖 §四 24 条 + §七 41 条 = 65 条；**#181 / #192 / #195 三条主代理自己补核**（均判已修）⇒ 69 条全覆盖，**无漏派**。

### 3.2 已修 / 已消解 / 不是缺陷 —— **48 条**（只需销账）

| 分区 | 编号 | 条数 |
|---|---|---|
| §四 | #182 / #183 / #185 / #188 / #189 / #196 / #199(不是缺陷) / #200(已消解) / #201 / #202 / #214 / #216 / #217 / #218 / #219 / #181 / #192 / #195 | **18** |
| §七 7.1–7.3 | #68 / #69 / #73 / #74 / #77 / #78 / #79 / #81 | **8** |
| §七 7.4–7.6 | #89 / #90 / #91 / #92 / #94 / #95 / #98 / #99 / #101 / #102 / #103 / #104(不是缺陷) / #105 | **13** |
| §七 7.9.1 + 7.9.2 | #156 / #162 / #163 / #172 / #173 / #174 / #175 / #176 / #177 | **9** |

**其中 4 条属「另一个 agent 刚修、本轮独立复核为真已修」**（不信自述，逐条现场验证）：

| 编号 | 独立复核结论 |
|---|---|
| **#214** | `环境回收面._保留判据` 四路并集、`回收过期制品` 与 `回收报告` 共用同一判据；`测试_平台客户端制品接入` **23 tests OK** |
| **#216** | 30 份垫片 **30/30** 已改 `取根前缀`；`测试_导入前缀` **16 tests OK** |
| **#217** | `运行开发编译口` / `效率巡检` 均已获显式 `源码根` + fail-closed；`测试_自修复工具` **24 tests OK** |
| **#218** | `公共契约/运行时/导入前缀.py` 已建（导出 `根锚目录表`/`取根前缀`/`取系统根`），45 份引用；检测器口径内归零（详见 §2.4 我的独立复核） |
| **#219** | plist 第四处 `系统底座_工程缓存根` 确在；三问全绿；`测试_网关运行态同根` **12 tests OK** |

**★ 两条特别值得记的「假绿已被消除」**：

- **#176**（HTML 验证器端口正则取首匹配）—— 子代理**真跑复现**：同一文本喂修前/修后两个实现 → **修前 40007（假绿现场）/ 修后 45080（真监听端口）**。这是本轮唯一一条用「抽函数真跑 + 并列跑旧实现当反向样本」证明的。
- **#182**（`检查错误码登记` 早退短路）—— `测试_公开调用完整性门禁` 由 **3 红 → 49 tests OK**。

### 3.3 ★ 真待办之一：**未修 11 条**

| 编号 | 现状（现场证据） | 剩余 | 阻塞 |
|---|---|---|---|
| **#60** | ★**根因本次新定位，与原文判断不同**：`重排服务` **已存在且可用**（实测 `大语言模型支持库.重排服务.检查可用性` → `{可用:true}`）。真病根是**探针传错参数名** —— `模块库/知识检索/实现/知识检索.py:88` 用 `{文本,模型名,提供者}` 探 `重排服务.重排`，而该能力必填是 `查询`+`文档列表` ⇒ **探针恒 400 ⇒ 恒判缺失** | **20** | ⚠️ **待裁决**：子代理建议走「**修探针**」（1 文件）而非「声明降级」（2-3 文件） |
| **#187** | 74 份 `设计说明.md` 装的是使用说明内容（`git grep` 复现 74 份，六特征交集 73 份）；`合并重生成.py:91` 写死 `使用说明.md` ⇒ **无生成器可刷** | **450** | ⚠️ **待裁决**：裁决「不该有调用口径段」→ 450 分钟；裁决「该有」→ **30–60 分钟**（改首部声明文案） |
| **#204** | ★**实测 5 个包，不是原文的 15**：`支持库/后端/文件系统支持库/文件操作`、`检索支持库/统一检索`、`代码解析支持库/代码地图`、`语义索引`、`语法索引` | **150** | 无（须先跑 `漂移检测` 确认当前一致） |
| **#210** | `合并重生成.py:181 def 字段键(行)` **签名只吃单行**、无法携带 `###` 小节作用域 ⇒ 同键跨小节误配根因仍在 | **60** | 无（须先做全仓改前/改后 `--校验` 对比） |
| **#93** | `续约句柄` vs `续租句柄` 参数面完全相同、返回键差一字；**`释放句柄` 已由 6 套增为 7 套** | **90** | 无（派生成本约为改码 10 倍，见 #212） |
| **#97** | `依赖分两段` 仍未被发布门禁调用（`rg` 零命中）；`--校验` **真退出码 1**，不一致项由 2 **增至 5** | **60** | 无 |
| **#100** | 第三方 import 分布基线**不存在**（只存在于债务清单叙述里，非机器判据） | **60** | 无 |
| **#76** | `规范/能力目录说明书.md:5` 写「共 140 份包声明」，现场 `扫描正式包=146`；**输入指纹三项零命中** | **75** | 无（刷新 15 + 指纹立项 60） |
| **#85** | 4 份手写 HTTP 客户端**全在**。★**口径问题**：清单给的收口目标 `薄壳/网关转发.py:转发()` **本身就是这 4 份之一**，「现成件」前提在现场不成立 | **120** | ⚠️ 需先定统一件（候选：`支持库.后端.网络通信支持库.请求.发送请求`） |
| **#80** | `交付记录/` 规则写「不再新增」，而 `20260918_夜间推进简报_给华哥.md` 于规则生效后新增且含 `681` 三处 —— **现状＝两边都保留，即清单明确禁止的状态** | **15** | 无 |
| **#82** | `主开发文档.md:3` 与 `当前流程图.md:3` 头部均写 09-20，真实改动为 **09-21 17:42 / 18:58** | **5** | 无 |

### 3.4 ★ 真待办之二：**部分修 10 条** + 7.8

| 编号 | 已做的部分 | 还缺什么 | 剩余 |
|---|---|---|---|
| **#208** | 一期目录树 ✓、二期仓库地图 ✓、三期语义索引 ✓ | **四期统一检索**（`模块库/统一检索` 目录不存在，既有的是另一条腿）+ 五期决策层验证场景 | **240** |
| **#212** | ④ 编译口瘦身 ✓（实测 **12.24/12.09 秒**，基线成立）、③ 说明书一条命令刷新 ✓ | ① 派活经生成器（`效率巡检.py:50` 原文「生成器已建好，但派活时仍手写 context」）+ ② sleep 清零/长命令后台化 | **120** |
| **#193** | 点名两份已零漂移（`同键配对位` 修法在位） | 全仓仍 `扫描 107 / 有漂移 5 / 未过 1`（唯一未过 = `代码解析支持库/仓库地图/说明/使用说明.md`，超配对 42 行）；**小节作用域**未做 | **135** |
| **#96** | 依赖登记已补声明、审计已单列 | 2 个目录未包化（`模型服务.py`/`系统探针.py` 仍平铺）+ **无 `依赖锁.json`** | **120** |
| **#84** | 已登记进存量基线（`调用腿唯一性检测`） | 代码仍手写 `sqlite3.connect`，未改走能力腿（清单要求**先核可行性再改**） | **90** |
| **#106** | 污染目录**已不存在**、夹具已改 `tempfile` | **判据未入库**（`测试体系门禁实现/` 三项检查均不覆盖「测试写仓库正式根」） | **45** |
| **#186** | 「多一个」已消（`渲染表格.py` 已删，`git` 留痕 `bd164cf5`） | 「少一个」**按现行口径刻意不判**（唯一事实源=完整性摘要，刻意不补条） | **75** 或**改判不是缺陷=0** |
| **#67** | 反向试验栏已补（总账 `:545`） | 「规则自检」**门禁项未建**（`开发工具/` 零命中，总账 `:557/:578` 自己承认） | **60** |
| **#184** | ② 已改（但数字又失真：现 **11–12 秒**，文档写 28 秒）；① 700 → **366 份** | ① 剩 366 份（**大项，不按分钟估算**）；② 改 2 处文案 | **10** + 大项 |
| **#75** | 点名三行已改「跑命令取真值」 | 同表 `:46/:47` 仍手写 `689`（真值 **750**） | **10** |
| **7.8** | 14 份件**全部真存在**（0 份已删）；本轮顺手证真 4 份（2 份结论已消解） | 剩 10 份逐条证真（实测约 6 分钟/份） | **55** |

### 3.5 方案 A–F 全层 + zcode L1–L11 落地状态（路 8）

**已落地 11 项**：

| 项 | 落地证据（提交） |
|---|---|
| **A2c** 薄壳够到结构化契约面 | `薄壳服务.py:99` 允许操作已含 `能力详情`；`工具清单.py:87` enum 同步（`0d58a74c`） |
| **B1** 返回体深裁 | 四种处置重构为「先缩字符串叶子、**容器永不丢**，缩不动才丢键」；`未能压到限内` 如实回带（`0d58a74c`+`2f4127cc`） |
| **C2-①** 宽限秒透传 | `独立进程_账本.py:212-213` 新增 `宽限秒` 形参 + `:233-236` 透传（默认 1.0 未动，缺省走旧路径）（`6050446f`） |
| **E1** 目录树 token 预算 | `目录树.py:171-172` 估算口径=字符数/3（与仓库地图**同尺**）（`89549f30`） |
| **E2** 精确命中升档 | `能力目录.py:469-473` 已落地 —— ★**方案 §七 的「无 `总数==1` 升档分支」已不成立**（`89549f30`/`f84575cb`） |
| **F1** 耗时榜第一步 | `安全审计.py:209 _分位`（最近秩）+ `:279-282 p50/p95`（`89549f30`） |
| **L1** 描述瘦身 | 三工具说明合计 **1819 字符**（⚠️ 方案记录改后是 1312 ⇒ **瘦身被部分回吐**） |
| **L9** `additionalProperties` | 三工具顶层 schema 全补 `false`（`6050446f`） |
| **L10** `isError` 语义 | `协议.py:89 构造工具结果(文本, *, 失败=False)` + `薄壳服务.py:504-505` 按 `成功` 置位；★**「isError 无人验证」已补**（`自测_stdio客户端.py:83,93` 现读 `isError`）（`6050446f`） |
| **L7 第一层** | `开工编排.py:664-683` 已扩含 `内容指纹`（`8b382a65`） |
| **L8** 原子写权限位 | `文本补丁.py:253 _原子写保留权限` + `:233 _同步目录`，两处调用点（`8b382a65`） |

**未落地 11 项**：

| 项 | 现状 | 我的估时 |
|---|---|---|
| **A1** 薄壳批入参 | 三落点全在，无列表型入参、无 `可否并行` 字段 | 240（含 745 能力逐条标可否并行） |
| **A2** 错误体四段式 | `工具清单.py:90` 的 `参数` **仍无 `properties`**；A2c 只在 description 加了「不要猜」 | 90 |
| **A2b** 枚举可选值 | 139 契约字段集 **100% = 5 键**；编译器 5 键白名单；**全仓 `可选值` 0 命中** | 300（含门禁重算） |
| **B2 第一层** | 编译口 `_尾部` 已修；**门禁自己 `问题表[:40]` 未修**；对象化六件套一条未做 | 60 |
| **C1 第 4 判据** | 三件套全在；**类身份漂移判据 0 命中** | 90（含先实测 `gc.get_referrers` 耗时） |
| **C2-②** 就绪探针 | 阶段表在；「就绪探针」0 命中 | 120 |
| **C2-③** 网关 blue-green | 方案选「登记不做」 | 0 |
| **D1** 生成物头统一 | 方案批 5 不做，另立方案 | 0 |
| **D2** `verify-generated` 三行语义 | 白名单/拒写行号与方案一致 | 90 |
| **F2** 分类维度 | 调用账本仍 5 列无耗时列；分类维度未落地 | 60 |
| **L11** 慢调用提示 | 与 F1 同一件事（前置已落地） | 0 |

### 3.6 子代理顺手发现的新问题（**本轮新增，尚未登记**）

| # | 新问题 | 建议 |
|---|---|---|
| **N1** | `测试_导入前缀.py:163-182` 的判据**只遍历赋值语句** ⇒ 非赋值形态的 `parents[N]` 扫不到（**我独立复核确认**，详见 §2.4） | 登记为低优先观察项 |
| **N2** | `公开调用完整性门禁存量基线.json` 有 **2 个已归零却未下调的桶**（`模型连接器|说明书-包级版本不一致:1`、`模块库/测试资源|说明书-缺能力段:2`），且 `_检查与判定.py:243-249` 的收敛判定**只遍历「现场出现过的桶」** ⇒ 现场为 0 的桶不会打印 `[可收敛]`，**可静默吸收新违规** | 建议并入下一轮（约 25 分钟） |
| **N3** | `开发工具/依赖生命周期审计` 对 `Git操作`/`FFmpeg媒体`/`textutil转换` 报「释放策略不符：文案声称回收进程组但实现中无 `killpg`/`start_new_session` 证据」——**这些实现恰是已按铁律收口到 `公共契约/运行时/进程终止.py` 的形态** ⇒ **审计侧误报** | ⚠️ **不要回退实现去满足字面量**，应改判据 |
| **N4** | `测试体系门禁` 的 **④ 真跑级从未执行** ⇒ 所有「测试通过」不含用例执行结论（编译口原文自陈） | 收口前至少跑一次真跑级 |
| **N5** | `能力id冻结基线` 有 **1 条内容指纹变化**（`系统核心支持库.资源管理.创建内容摘要`） | 先复核该契约变更是否合法，再重建基线 |
| **N6** | 适配层腿仍有 **12 处 `parents[4]`** 取根（#218 口径外既有写法） | 建议后续统一到 `取系统根`（属潜在同类风险） |
| **N7** ★★ | **切制品后源码树「提交/合并/切换分支」全被白名单拒 —— 纯 MCP 提交当前不可能**。现场：`版本控制支持库.Git操作.提交(仓库路径=<源码树根>)` → **HTTP 400 `路径越界`**，详情 `白名单 = [<制品根>/平台客户端/平台客户端, <TMPDIR>]`。**根因（读源码确认）**：`支持库/适配层/Git提供者/实现/仓库根白名单.py:51` 的 `候选表 = [底座仓库根()] + 环境变量 工作区允许提交根 + tempdir`，而 `底座仓库根()` 是**从 `__file__` 推的** ⇒ 制品模式下推成**制品根**；`plutil` 实测 plist 的 `EnvironmentVariables` **8 项里没有 `工作区允许提交根`** ⇒ 白名单只剩制品根 + TMPDIR。**同族**：#217/#218/#219 都是「我该操作哪棵树靠隐式推断」，**#217 只修了 `模块库/自修复工具/实现/` 的 3 个能力级入口，`Git操作` 这条链没在范围内** | 建议照 #217 同一解法：给变更类 Git 操作加**显式 `仓库根`**（留空回落本树、fail-closed 校验），或把 `底座仓库根()` 改成前缀感知。**⚠️ 本轮未修**（属新任务，且改 Git 契约要连带重编译 + 派生同步） |
| **N8** | `支持库/后端/项目文档支持库/坑库.json` **实为 SQLite 数据库**（文件名叫 `.json`，内容是 SQLite），其副产物 `坑库.json-shm` / `坑库.json-wal` **未跟踪且未被 `.gitignore` 覆盖** —— `git status --short` 实测持续出现 2 个 `??`。⇒ ① 文件扩展名与实际格式不符（与 #199 同类：**名字骗人**）；② 运行态副产物污染仓库工作区 | 建议：① 改名（如 `坑库.sqlite3`）或至少改扩展名；② 把 `*.json-shm`/`*.json-wal` 补进 `.gitignore`。**⚠️ 本轮未修** |

---

## 四、**还有多久才能完美修好**

### 4.1 直接回答

| 口径 | 时长 | 说明 |
|---|---|---|
| **纯工程量（串行）** | **≈ 52 小时** | 债务清单 21 条真待办 ≈ 35 小时 + 方案未落地 8 项 ≈ 17 小时 |
| **8 路并行** | **≈ 7 小时** | 本轮 8 路 **484 秒**核实完 65 条，证明并行度可用；修复类受文件租约约束，按 4–8 路 |
| **裁决往「省」的方向走** | **省 ≈ 8 小时** | 仅 #187 一项就省 6.5–7 小时（450 → 30~60 分钟） |
| **销账（前提动作）** | **20 分钟** | 48 条已修条目从清单删掉，让后续估算不再虚高 |
| **手写 md 归一 366 份** | **不按分钟算** | 大项、可批量、不阻塞功能 |

**⇒ 一句话：不卡裁决、8 路并行，大约 1 个工作日（7 小时）能到「干净位置」；串行做约 52 小时。**

### 4.2 工作量分解（分钟）

| 来源 | 条数/项数 | 分钟 | 备注 |
|---|---|---|---|
| 债务清单 · 未修 | 11 | **1105** | 含 #187 一项 450（**裁决项**） |
| 债务清单 · 部分修 | 10 | **905** | 含 #208 一项 240 |
| 债务清单 · 7.8 | 1 项 | **55** | 剩 10 份件逐条证真 |
| 顺手项（N2 收敛判据） | 1 | **25** | 可并入下一轮 |
| **债务清单小计** | | **2090** | ≈ 34.8 小时 |
| 方案未落地（A1/A2/A2b/B2/C1/C2-②/D2/F2） | 8 | **1050** | 我估；A2b 独占 300（含门禁重算窗口） |
| **合计** | | **3140** | ≈ **52.3 小时** |

### 4.3 关键路径与顺序（按「先稳定后高效」）

| 步 | 动作 | 时长 | 为什么这个顺序 |
|---|---|---|---|
| **0** | **销账** —— 删掉 48 条已修条目 | 20 分钟 | **前提**：不销账，任何「还剩 N 条」都是错的 |
| **1** | **无阻塞项批量修**（#82 / #80 / #75 / #186 / #67 / #106 / #184② / #204 / #210 / #93 / #97 / #100 / #76） | ≈ 700 分钟 | 全是「改文档 / 补判据 / 加形参」级别，**只增不改、可单点回退** |
| **2** | **方案 P0 三条**（A2 错误体四段式 / A2b 枚举 / B2 第一层） | ≈ 450 分钟 | A2b 要改 139 份源头 `能力定义.json` + 门禁重算，**必须一次性在门禁窗口内做完** |
| **3** | **方案结构性四条**（A1 并发 / C1 第 4 判据 / C2-② 就绪探针 / D2 / F2） | ≈ 600 分钟 | A1 需先改 fail-closed（745 能力逐条标 `可否并行`），**不改完不许开并发** |
| **4** | **需裁决项**（#187 / #60 / #85） | 视裁决 | 见 §五 |
| **5** | **手写 md 归一 366 份** | 大项 | 按类 `--类型 <类型> --写盘 --补头` 批量，可与上面并行 |
| **6** | **收口验证** | ≈ 60 分钟 | 跑一次 `测试体系门禁 --级别 真跑级`（N4）+ 重建 `能力id冻结基线`（N5）+ 编译口全绿 |

### 4.4 三个「让工期翻倍」的隐性风险

1. **#187 是裁决项不是工程量** —— 74 份 `设计说明.md` 若判定「不该有调用口径段」，要按 `设计说明模板` 重建**人工区**，清单已定「**不可机器硬刷**」⇒ **450 分钟且无法并行**。裁决往另一个方向走则只 30–60 分钟。**这一项单独决定工期差 6.5–7 小时。**
2. **#212 的「派生同步成本是改码的十倍」仍在** —— ① 派活仍手写 context（`效率巡检.py:50` 原文自陈）、② sleep/长命令后台化未做 ⇒ **每修一条都要付派生同步的税**。这两项不做，上面所有估时都要乘系数。
3. **`测试体系门禁` 的 ④ 真跑级从未执行** —— 意味着**当前所有「测试通过」都不含用例执行结论**。收口前必须跑一次，否则「修好了」没有证据。

### 4.5 已经不需要做的（本轮确认，防止后人重复劳动）

- **#199 / #104** —— 明确「不是缺陷」，防误修；
- **方案 C2-③（网关 blue-green）/ D1（生成物头统一）** —— 方案已裁「本轮不做」，**不要再立项**；
- **N3（依赖生命周期审计误报）** —— **不要回退实现去满足字面量**，应改判据；
- **适配层腿 12 处 `parents[4]`** —— 是**另一条腿的既有口径**，不在 #218 范围内。

---

## 五、待华哥裁决

### 5.1 **只有 3 条真正需要你定**（其余我按设计哲学自裁）

| # | 待裁 | 两个方向 | 我的建议 |
|---|---|---|---|
| **裁A** | **#187** 74 份 `设计说明.md` 该不该有「调用口径段」 | ① 不该有 → 按 `设计说明模板` 重建人工区，**450 分钟且不可机器硬刷**；② 该有 → 只改首部声明文案，**30–60 分钟** | 建议**②**：这 74 份正文是**人按设计说明的名义写的内容**，机器重写会丢事实；而「首部声明与正文不符」本身是**文案问题**。**这一条决定工期差 6.5–7 小时** |
| **裁B** | **#60** 走「修探针」还是「声明降级」 | ① 修探针（1 文件，20 分钟）；② 声明降级（2–3 文件，25 分钟） | 建议**①修探针** —— 子代理已实测 `重排服务` **本就可用**，是探针传错参数名（用 `{文本,模型名,提供者}` 探必填 `查询`+`文档列表`）导致**恒判缺失**。声明降级是**把 bug 说成设计** |
| **裁C** | **#85** 4 份手写 HTTP 客户端收口到哪个统一件 | 清单原给的 `薄壳/网关转发.py:转发()` **本身就是这 4 份之一** ⇒ 前提不成立 | 建议收口到 **`支持库.后端.网络通信支持库.请求.发送请求`**（平台侧现成件），而不是让薄壳那份兼任「统一件」 |

### 5.2 **不需要你定，我直接做**（按「已修条目关闭即删」口径）

| # | 动作 | 时长 |
|---|---|---|
| 1 | **销账**：把 **48 条**已修/已消解/不是缺陷条目从债务清单删掉 | 20 分钟 |
| 2 | 修 `AGENTS.md:76/:315` 的耗时数字（28 秒 → 实测 **11–12 秒**，注明 8 路并行） | 10 分钟 |
| 3 | 修 `项目说明.md:46/:47` 的 `689`（真值 **750**） | 10 分钟 |
| 4 | 修 `主开发文档.md:3` / `当前流程图.md:3` 的「最后更新」 | 5 分钟 |
| 5 | `#80` `交付记录/` 的「两边都保留」状态收口（移归档 或 补例外） | 15 分钟 |
| 6 | **N2** 下调 2 个归零桶 + 收敛判据覆盖「现场为 0 的桶」 | 25 分钟 |

### 5.3 **收口前必须验的三件事**（不算裁决，算验收前提）

| # | 事项 | 为什么必须 |
|---|---|---|
| 1 | 跑一次 `测试体系门禁 --级别 真跑级` | **④ 真跑级从未执行** ⇒ 当前所有「测试通过」**不含用例执行结论** |
| 2 | 复核 `资源管理.创建内容摘要` 的契约变更是否合法，再重建 `能力id冻结基线` | 有 **1 条内容指纹变化**（`378ff95bd8687fcc → 2c0ac7f72132ee12`） |
| 3 | 编译口 `--全仓` 口径跑一遍 | 本轮 `--变更` 因**工作区干净**⇒变更集无 `.py`⇒**语法编译这一项一次都没跑**，五态结论是「未核验」 |
