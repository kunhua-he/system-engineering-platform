# 审计 · 能力唯一性降重（第一轮）

> 状态：**已完结**（2026-09-19；只读审计，未改任何仓库文件）
> 审计对象：系统工程平台（仓库根 `/Users/hekunhua/Documents/Agent/PHP/系统工程平台`）
> 固定版本：`836e90665b2112db23eb1b2024163952b0dd39a8`（短 `836e9066`，分支「主干」）
> 工作树：干净（`git status --porcelain | wc -l` → `0`），故本报告全部行号对应当前 HEAD 现场，无需四态对账
> 命令环境：`export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH`；全仓搜索一律 `rg`；所有命令带 `timeout`
> 纪律：只读 —— 未改文件、未提交、未跑测试、未重启服务；中间产物全部落 `/tmp/audit_cap/`

---

## 零、方法与取证口径

按任务书要求，先取「真源」把**全部已注册能力 id** 列出来，不靠肉眼抽样。

### 0.1 三条口径实测（同一时点读数）

| 口径 | 命令 | 实测 |
|---|---|---|
| 网关检索面（「能力目录.搜索能力」分页全量拉取） | `curl -sS -m 30 -X POST http://127.0.0.1:40007/网关/调用 -H "Authorization: Bearer $TK" -H "Content-Type: application/json" -d '{"操作":"调用能力","能力id":"能力目录.搜索能力","参数":{"关键词":"","限制":200}}'`（游标翻页 5 轮） | **468**（分页 100+100+100+100+68，`是否截断` 末轮为 `false`） |
| 磁盘声明能力（扫描 `能力定义.json` 的 `能力列表`） | `os.walk` 六个正式根，排除 `工程缓存/`、`运行数据/`、`__pycache__/` | **699**（136 个包） |
| 差额归因 | 按「根 / 内部层 / 适配层」分类 | 其他根 102（平台控制面·运行核心·技能库·启动监督器）＋ 内部层 68 ＋ 适配层 61 = **231**；`公开口径 − 网关 = 0`、`网关 − 公开口径 = 0` |

**结论**：网关 468 = 「支持库+模块库」且「非适配层、非内部层」的公开包，口径自洽。**「声明 699 / 注册 468」不是缺陷**（见第五节第 4 条）。

### 0.2 唯一性判定采用的四级框架（`开发文档/项目说明.md:249-259`）

```
候选机制
  ├─ ① 同名同类型            → ✅ 合并增强（收口，不建第二套）
  ├─ ② 底子不同但格式一样     → 🔀 加「模式」变量
  ├─ ③ 底子有同类但缺原子点   → 🔧 增强现有支持库（补能力，不建新库）
  └─ ④ 真正确认真空缺        → 🆕 才允许立项新建
```

### 0.3 本路审计的哲学条款（先读原文再判）

| 条 | 原文位置 | 判据摘要 |
|---|---|---|
| 1.2 | `项目说明.md:120` | 不保留旧腿；同一件事存在两套以上实现即视为缺陷；边界口径④**「能力级同 id 多版本并存」＝内部第二执行腿，禁止** |
| 1.3 | `项目说明.md:127` | 同一目的两套实现＝本质重复；保留最全面的一个 + 归因原子能力，其余蒸馏 |
| 8.1 | `项目说明.md:243` | 能力唯一权威，杜绝旁路 |
| 8.2 | `项目说明.md:244` | 发现第二套实现（无论来源）即收口删除 |
| 8.3 | `项目说明.md:246` | 原子能力精而不杂：**能参数化成一个能力的绝不分家** |
| 5.1 | `项目说明.md:190` | 契约唯一，恒等于 `公共契约/版本规则/契约版本.py`（实测 `契约版本 = "2.0.0"`） |
| 7.9 | `项目说明.md:237` | 派生优先于手写；同一件事不得同时存在「生成」与「手写」两种表达 |
| 2.1 | `项目说明.md:144` | 分层归属：支持库＝最小原子能力；模块＝**组装**原子能力解决一类问题 |
| 2.4 | `项目说明.md:152-164` | 翻译器原则：外部形态差异只在**边界**吃干净；不许新开第二条腿 |
| 内部实现不得复制 | `项目说明.md:484` | 「同一职责的内部实现也不得复制：相同能力必须合并为一个实现……第三方能力只能经唯一Provider适配。**当前代码中仍存在的历史包装或重复执行面，只能视为待收口债务**，不得继续新增或作为新的正式入口。」 |

---

## 一、结论摘要（按严重度分级）

### 阻断级（2 项）

| # | 条目 | 条款 | 现状态 |
|---|---|---|---|
| **B-1** | `支持库/适配层/*提供者`（18 包 61 能力）与 `支持库/后端/*` 成对包（12 对／**43 对同能力面能力 id**）**两套实现并存**；其中 **2 组 `.py` 至今逐字节相同**、**9 对后端腿仍持自有实现** | 1.2①（内部第二执行腿禁止）／1.3／8.2／2.4① | **未收口**（已登记债务 D-1，方向已裁，执行未完成） |
| **B-2** | 上述 12 对里，**调用方消费的是「后端腿 id」，而既定方向是「适配层为唯一实现」** —— 57 条真调用点仍在调用那条**将被降级**的腿 | 1.3（结果唯一即收口）／8.2 | **未收口**（`开发文档/未完成事项.md:154` 登记为「大」） |

> 说明：B-1/B-2 是**同一件事的两面**（实现重复面 + 调用面）。二者均已是**已登记、已裁方向的存量债务**，本路审计的增量价值在于：**给出「哪些已收口、哪些没动」的现时刻度**，避免照抄 2026-09-17 清单的漂移数字。

### 重要级（4 项）

| # | 条目 | 条款 | 现状态 |
|---|---|---|---|
| **I-1** | `模块库/*` 存在**逐能力 1:1 纯透传门面**：`模块库/文件管理` 9 能力全部只是转发同名支持库能力，且**参数面被裁**（丢了 `编码`／`允许危险路径`） | 1.3／8.3（同目的两套 id） | 未裁决 |
| **I-2** | **提供者注册机制两套并存**：`支持库.适配层.提供者注册表`（进程内 Python 类，非能力）与 `平台控制面.提供者.注册表`（4 条公开能力）——两套 id、两套写入点、无对齐判据 | 8.1／8.2／7.9 | 未裁决 |
| **I-3** | `支持库/适配层/` 根下 **12 个无 `包声明.json`、零能力定义** 的目录（数据库适配器／动态库适配器／本地进程适配器／HTTP服务适配器／密钥提供者／提供者注册表／配置契约／配置安全／提供者配置／提供者配置读取／MySQL提供者／说明），与已能力化的 18 个 `*提供者` 包形成**两套登记形态** | 8.1（旁路）／2.4① | 未裁决 |
| **I-4** | `模块库/文件管理.流式创建摘要` 与 `系统核心支持库.资源管理.创建内容摘要`：**同一目的、参数面被裁、契约返回类型还不一致**（前者 `结果`／后者裸文本），造成「模块层被迫手动 try/except 包装」 | 1.3／8.3 | 未裁决（平台自有记录已提过） |

### 建议级（3 项）

| # | 条目 | 条款 | 现状态 |
|---|---|---|---|
| **S-1** | `数据操作支持库.类型转换` 10 项，**说明文字全部套同一模板**（「<能力id> 确定性原子转换」，其余 9 项与首项相似度 0.83–0.98） | 8.3（能参数化的绝不分家） | 建议保留（见第五节第 5 条的反向理由），仅登记 |
| **S-2** | `大语言模型支持库.会话存储.续约句柄` 与 `大语言模型支持库.模型连接器.续租句柄`：**参数完全相同**（`句柄`/`租约秒`），返回仅 `已续约`/`已续租` **一字之差** | 8.3 + 四级② | 建议归四级②，暂不修 |
| **S-3** | `数据操作支持库.数据集合` 内含 4 条**文本类能力**（`文本截取`／`文本重复`／`文本去首尾`），与同库 `文本处理` 包（10 条文本能力）目的重叠 | 8.3（归属精而不杂）／2.1 | 建议归档案，暂不修 |

### 明确「本类未发现」的两类

| 审计类 | 结论 |
|---|---|
| **2. 同 id 多版本并存**（撞 1.2④，硬禁止） | **本类未发现**。实测：`能力定义.json` 全部 699 条能力 id 的版本号集合**恒为单元素**（多版本并存数 = **0**）；`运行核心/加载器/版本系统/版本注册表.py:1` 的「同一包多个版本并存」是**包级**版本注册表，与「能力级同 id 多版本」不是同一件事，且当前默认存储目录 `/tmp/系统级支持库_版本` **不存在**（注册表空表） |
| **4. 「底子不同但格式一样」却各自建包、没加模式变量** | **未发现典型形态**。全仓归一化说明文本相似度 ≥0.80 的不同 id 对共 57 对，逐对读实现后**无一对**是「同一目的、两种底层、各自建包」——命中的都是「同一族原子操作的不同参数位」（类型转换／文本处理／文件操作），属第五节的合法设计取舍。真正的跨包重复全部落在 **B-1**（适配层/后端腿），其形态是「同一目的一致、底子也是同一个第三方」，属**四级①**而非② |

---

## 二、逐条明细

### B-1｜适配层腿与后端腿：12 对包／43 对同能力面 id 两套实现并存

**候选能力 id 清单（43 对，完整枚举）**

| # | 后端腿 id（`支持库/后端/...`） | 适配层腿 id（`支持库/适配层/...`） | 两腿 `.py` 关系（本路实测） |
|---|---|---|---|
| 1–9 | `图像处理支持库.图像解码.{解码图像,像素统计,生成占位图,生成缩略图,图像EXIF转置,透明背景合成,计算感知哈希,缩放图像,重编码图像}` | `图像解码.{同名 9 条}` | `实现/子进程解析.py` **★逐字节相同**（`79e5973623e0`，360 行）；`实现/提供者.py` 相似 99.0%；`实现/子进程入口.py` 相似 97.4% |
| 10–12 | `OCR识别支持库.OCR识别.{识别图片,语言包列表,版本探针}` | `OCR识别.{同名 3 条}` | `实现/临时文件.py` **★逐字节相同**（`3090094b949b`，38 行）；`实现/提供者.py` 相似 99.5%；`实现/受管进程.py` 相似 81.7% |
| 13–17 | `媒体处理支持库.FFmpeg媒体.{检查提供者,探测媒体,提取音频,转码,抽取帧}` | `FFmpeg媒体.{同名 5 条}` | `实现/探测.py` 相似 96.6%；`实现/处理.py` 相似 82.1%；`实现/进程管理.py` 相似 86.0% |
| 18–26 | `版本控制支持库.Git操作.{检查提供者,创建工作区,查询工作区,关闭工作区,提交,回滚,挑拣合入,当前状态,获取当前提交哈希}` | `Git操作.{同名 9 条}` | `实现/白名单.py` 相似 96.7%；`实现/Git提供者.py` 相似 96.1%；`实现/受管执行.py` 相似 84.0%；`实现/提交回滚.py` 相似 77.8% |
| 27–28 | `文档转换支持库.LibreOffice转换.{检查提供者,转换办公文件}` | `LibreOffice转换.{同名 2 条}` | `实现/文档转换.py` 相似 25.8%（两腿都直调 `soffice`） |
| 29–30 | `文档转换支持库.textutil转换.{检查提供者,转换文本文件}` | `textutil转换.{同名 2 条}` | `实现/文本转换.py` 相似 96.8% |
| 31 | `文档转换支持库.PDF生成.生成PDF` | `PDF生成.生成PDF` | **已收口**：后端 `实现/生成PDF.py` 相似 16%（转调），`实现/子进程入口.py:27` `from 支持库.适配层.reportlab提供者 import 生成PDF as _唯一实现生成PDF` |
| 32–35 | `文档转换支持库.PDF渲染.{检测加密页数,渲染整页,提取图像,校验PDF}` | `PDF渲染.{同名 4 条}` | **已收口**：后端 `实现/子进程解析.py` 40 行（`sys.modules[__name__] = 唯一实现`） |
| 36–38 | `文档转换支持库.PDF隔离提供者.{解析PDF,校验PDF,检查提供者}` | `PDF隔离提供者.{同名 3 条}` | **已收口**：后端 `实现/子进程解析.py` 40 行（同手法） |
| 39–40 | `文档转换支持库.PDF文本表格.{解析PDF,提取表格}` | `PDF文本表格.{同名 2 条}` | **已收口**：后端 `实现/PDF文本表格.py` **39 行**（同手法） |
| 41–43 | `转写支持库.转写.{检查可用性,转写音频文件,获取模型版本}` | `转写.{同名 3 条}` | `实现/提供者.py` 相似 96.5%；`实现/子进程解析.py` 相似 81.8%；`实现/子进程入口.py` 相似 96.0% |

**判定条款**：1.2①（内部第二执行腿禁止）＋ 1.3（结果唯一即收口）＋ 8.2（发现第二套实现即收口删除）＋ 2.4①（不许新开第二条腿）

**现场证据**

命令 1 —— 成对同能力面 id 对数（规范化后按「禁止跨包导入 `实现/`」的既有口径重建配对）：
```
$ python3 -c "扫描六个正式根的 能力定义.json，对每条适配层 id 找 endswith('.'+id) 的后端 id"
「适配层短式 id」↔「后端点分包id式」成对同能力面：43 对
```

命令 2 —— 逐字节相同 `.py`（sha256）：
```
$ python3 -c "os.walk 六个正式根，过滤 '__pycache__'/'夹具'/'测试'，对每个 .py 取 sha256 分组"
=== 完全字节相同的 .py 实现文件（≥2 份） ===
[79e5973623e0] 2 份  (16279B)
    支持库/适配层/Pillow提供者/实现/子进程解析.py
    支持库/后端/图像处理支持库/图像解码/实现/子进程解析.py
[3090094b949b] 2 份  (1312B)
    支持库/适配层/Tesseract提供者/实现/临时文件.py
    支持库/后端/OCR识别支持库/OCR识别/实现/临时文件.py
[6462fc97f6a7] 8 份  (25B)   ← 空壳 __init__.py，非业务代码，不计
[e3b0c44298fc] 5 份  (0B)    ← 同上
同内容文件组数：4   （剔除空壳后业务代码重复组 = 2）
```

命令 3 —— 近似重复（相似度 ≥0.85，长度 ≥800B，排除夹具/测试）：
```
38 对
1.0000  支持库/适配层/Pillow提供者/实现/子进程解析.py  ↔  支持库/后端/图像处理支持库/图像解码/实现/子进程解析.py
0.9970  支持库/适配层/PDF隔离提供者/实现/子进程入口.py  ↔  支持库/后端/文档转换支持库/PDF隔离提供者/实现/子进程入口.py
0.9955  支持库/适配层/Tesseract提供者/实现/提供者.py  ↔  支持库/后端/OCR识别支持库/OCR识别/实现/提供者.py
0.9938  支持库/适配层/PyMuPDF提供者/实现/子进程入口.py  ↔  ...
0.9904  支持库/适配层/Pillow提供者/实现/提供者.py  ↔  支持库/后端/图像处理支持库/图像解码/实现/提供者.py
... （其余 33 对见上表）
（其中 3 对属前端描述包 `__init__.py` 与办公文档子包 `__init__.py` 的注册样板重复，见第五节）
```

命令 4 —— 后端腿是否已转调（`适配层` 引用计数，`0` = 未收口）：
```
$ rg -c '适配层' <后端腿>/实现/ | wc -l
    0  支持库/后端/图像处理支持库/图像解码        ← 未收口
    0  支持库/后端/媒体处理支持库/FFmpeg媒体      ← 未收口
    0  支持库/后端/版本控制支持库/Git操作         ← 未收口
    0  支持库/后端/OCR识别支持库/OCR识别          ← 未收口
    0  支持库/后端/转写支持库/转写                ← 未收口
    1  支持库/后端/文档转换支持库/LibreOffice转换
    1  支持库/后端/文档转换支持库/textutil转换
    1  支持库/后端/浏览器自动化支持库
    2  支持库/后端/文档转换支持库/PDF生成         ← 已收口
    1  支持库/后端/文档转换支持库/PDF渲染         ← 已收口
    1  支持库/后端/文档转换支持库/PDF文本表格     ← 已收口
    1  支持库/后端/文档转换支持库/PDF隔离提供者   ← 已收口
```
已收口的 4 组现场（可当做法样板）：
- `支持库/后端/文档转换支持库/PDF文本表格/实现/PDF文本表格.py` —— **39 行**，末行 `sys.modules[__name__] = sys.modules[唯一实现名]`
- `支持库/后端/文档转换支持库/PDF渲染/实现/子进程解析.py:1-40` —— 头注释写明「本文件原与 `支持库/适配层/PyMuPDF提供者/实现/子进程解析.py` **逐字节相同**（结构债 D-2 第 ⑥ 组，sha256 f26cb539…）」
- `支持库/后端/文档转换支持库/PDF隔离提供者/实现/子进程解析.py:1-40` —— 同上（D-2 第 ② 组，sha256 f77d070a…）
- `支持库/后端/文档转换支持库/PDF生成/实现/子进程入口.py:27` —— `from 支持库.适配层.reportlab提供者 import 生成PDF as _唯一实现生成PDF  # noqa: E402`

还是重复的 2 组现场（**没有**任何「唯一实现」声明，两份都是完整业务实现）：
- `支持库/后端/图像处理支持库/图像解码/实现/子进程解析.py:1-20` 与 `支持库/适配层/Pillow提供者/实现/子进程解析.py:1-20` —— docstring、常量（`禁用库环境变量名 = "Pillow提供者_禁用库"`、`输入字节上限 = 64 * 1024 * 1024`、`单图最大像素 = 40_000_000`、`统计最大像素 = 25_000_000`）**逐字节相同**，两文件均含 `_PIL模块 = None  # (Image, ImageDraw, ImageFont, ImageOps, ImageStat)`
- `支持库/后端/OCR识别支持库/OCR识别/实现/临时文件.py` 与 `支持库/适配层/Tesseract提供者/实现/临时文件.py` —— sha256 `3090094b949b`／38 行，全同

**影响面**

- 两套实现各自有独立的 `能力定义.json`／`参数契约.json`／`使用说明.md`／`完整性摘要.json`／`版本`（例：Pillow 腿与图像解码腿同为 `1.0.3`，但 Git 腿 `1.0.3` vs Git操作腿 `1.2.0`）——**改一边不会动另一边**，是漂移的结构性来源。
- 43 对 id 中**每一对都在网关里各有一条独立能力记录**，调用方可任选其一 → 同一目的两个权威结果。
- 与哲学 1.2④不同，这不是「同 id 多版本」，而是**「同能力面、不同 id、同 id 多实现」** —— 按 8.2「发现第二套实现（无论来源）即收口删除」，**属硬违规**。
- 影响 12 个包 / 43 对能力 / 约 121 个 `.py` 文件（两腿合计）。

**四级框架归类**：**① 同名（同能力面）同类型 → 合并增强（收口，不建第二套）**

**是否建议修**：**是**（阻断级）。但**方向已裁、不新增裁决**：
- 方向口径：`开发文档/方案/适配层唯一化迁移清单_20260917.md:5`「**适配层为唯一实现；后端成对包降为门面或删除**」；依据 `开发文档/未完成事项.md:154`（D-1）
- 结构债编号：D-2（`开发文档/方案/结构债处置方案_20260916.md:43` 列 6 组逐字节相同；`:242`「并入 D-1（其批 4）」）

**最小改法落点**（沿用平台既有两手法，**不新开第三条路**）

```
纯 Python 库、无原生扩展（如 pdfplumber 腿）：
  后端腿 /实现/<文件>.py 末尾
      sys.modules[__name__] = sys.modules[唯一实现名]
  样板：支持库/后端/文档转换支持库/PDF文本表格/实现/PDF文本表格.py:39

需子进程隔离的库（Pillow / fitz / tesseract / reportlab / mlx_whisper）：
  后端腿 /实现/子进程入口.py 内
      from 支持库.适配层.<提供者> import <能力> as _唯一实现…
  样板：支持库/后端/文档转换支持库/PDF生成/实现/子进程入口.py:27
```

**未收口批次（本路实测，按「最后一次已知状态」标注）**

| 批 | 对象 | 状态 |
|---|---|---|
| 已完成 | PDF文本表格（D-2①）、PDF隔离提供者（D-2②）、PDF渲染（D-2⑥）、PDF生成（reportlab） | **已收口** |
| 未动 | Pillow/图像解码（D-2④ 逐字节相同）、Tesseract/OCR识别（D-2⑤ 逐字节相同） | **仍是两份完整实现** |
| 未动 | FFmpeg媒体、Git操作、转写、LibreOffice转换、textutil转换（＋ 浏览器自动化：结构已是「适配层承载实现＋后端承载能力面」，见第五节） | **未收口** |

---

### B-2｜调用面仍指向「将被降级的那条腿」：57 条真调用点

**判定的哲学条款**：1.3（结果唯一即收口）／8.2（杜绝旁路）

**现场证据**

命令（任务书指定的「真源」检索，非肉眼抽样）：
```
$ curl … {"能力id":"能力目录.搜索能力","参数":{"关键词":"缩放图像","限制":50}}
总数=9
    图像处理.分析图像文件 | 模块库.图像处理
    图像处理.缩放图像     | 模块库.图像处理
    …（9 条全在 模块库.图像处理）

$ curl … {"关键词":"生成PDF"}   → 总数=2：办公文档支持库.文档生成.生成PDF / 文档生成.生成文档
$ curl … {"关键词":"解析PDF"}   → 总数=2：办公文档支持库.PDF文档.解析PDF / 文档解析.解析文档
$ curl … {"关键词":"挑拣合入"}  → 总数=4：自修复工具.{创建修复工作区,回滚修复,获取当前提交哈希,验证修复}
```

关键点：**适配层腿的 id（如 `图像解码.缩放图像`、`Git操作.挑拣合入`）在「能力目录.搜索能力」里查不到** —— 因为 `模块库/能力目录/实现/能力索引.py:22` 有

```python
排除包前缀 = ("支持库.适配层.",)
```

即**适配层腿对检索面不可见**。而调用方（模块库与办公文档支持库）**实际调用的正是后端腿 id**：

```
$ rg -no '"图像处理支持库\.图像解码\.[^"]*"' 模块库/图像处理/实现/图像处理.py
85:  "文件系统支持库.文件操作.读取二进制文件"
90:  "图像处理支持库.图像解码.解码图像"
93:  "图像处理支持库.图像解码.像素统计"
106: "图像处理支持库.图像解码.生成占位图"
117: "图像处理支持库.图像解码.解码图像"
125: "图像处理支持库.图像解码.生成缩略图"
134: "图像处理支持库.图像解码.图像EXIF转置"
142: "图像处理支持库.图像解码.透明背景合成"
151: "图像处理支持库.图像解码.计算感知哈希"
169: "图像处理支持库.图像解码.缩放图像"
178: "图像处理支持库.图像解码.重编码图像"

$ rg -no '"版本控制支持库\.Git操作\.[^"]*"' 模块库/自修复工具/实现/自修复工具.py
147:"版本控制支持库.Git操作.关闭工作区"     164:"版本控制支持库.Git操作.检查提供者"
176:"版本控制支持库.Git操作.创建工作区"     188:"版本控制支持库.Git操作.提交"
246:"版本控制支持库.Git操作.当前状态"       253:"版本控制支持库.Git操作.回滚"
255:"版本控制支持库.Git操作.挑拣合入"       268:"版本控制支持库.Git操作.获取当前提交哈希"
```

**影响面**：`开发文档/方案/适配层唯一化迁移清单_20260917.md:66` 实测「真调用点 **57 条 / 17 个实现文件**；消费方 **14 个包**（模块库 9 + 办公文档支持库 5 子包）；去重 50 个文件（实现 19 / 契约声明 18 / 包声明 13）」

**四级框架归类**：①（合并增强）—— 与 B-1 同一收口动作的调用侧

**是否建议修**：**是**（阻断级），但**必须在 B-1 收口之后**（先把唯一实现定在适配层腿，再切调用点；反序会让调用点指向一条尚未承载实现的腿）

**最小改法落点**
```
调用点（19 个实现文件）：
  "支持库名.包名.能力名"  →  "包名.能力名"        例：图像处理支持库.图像解码.缩放图像 → 图像解码.缩放图像
声明面（18 契约 + 13 包声明）：
  同批切 id；改后重算完整性摘要（开发工具.全量重算摘要）
已有唯一在产消费点（未切）：
  支持库/后端/办公文档支持库/文档生成/实现/签名校验.py:17
      校验PDF能力id = "PDF隔离提供者.校验PDF"      ← 已是适配层短式 id，属「已切」
  支持库/后端/办公文档支持库/文档生成/依赖契约/依赖契约.json:3
      {"能力": "PDF隔离提供者.校验PDF", "版本": ">=1.0.0"}
  支持库/适配层/__init__.py:33-38                       ← 适配层自身入口
  运行核心/运行环境管理器/提供者生命周期.py:194         ← 适配层包公开入口导入
```

---

### I-1｜`模块库/*` 逐能力 1:1 纯透传门面：同目的两套 id、且参数面被裁

**判定的哲学条款**：1.3（同一目的两套实现）／8.3；**反向依据** 2.1（模块＝组装原子能力解决一类问题）

**候选能力 id 清单（`模块库/文件管理` 9 条全量 1:1 转发）**

| 模块 id | 转发的支持库 id | 参数面差异 |
|---|---|---|
| `文件管理.读取文件` | `文件系统支持库.文件操作.读取文件` | 丢 `编码`（模块硬写 `utf-8`） |
| `文件管理.写入文件` | `文件系统支持库.文件操作.写入文件` | 丢 `编码`、丢 `允许危险路径` |
| `文件管理.复制文件` | `文件系统支持库.文件操作.复制文件` | 同名同参 |
| `文件管理.移动文件` | `文件系统支持库.文件操作.移动文件` | 同名同参 |
| `文件管理.删除文件` | `文件系统支持库.文件操作.删除文件` | 丢 `允许危险路径` |
| `文件管理.列出文件` | `文件系统支持库.文件操作.列出目录` | **改名**（`列出文件` ↔ `列出目录`） |
| `文件管理.读取二进制文件` | `文件系统支持库.文件操作.读取二进制文件` | 同名同参 |
| `文件管理.读取文件头部字节` | `文件系统支持库.文件操作.读取文件头部字节` | 同名同参 |
| `文件管理.流式创建摘要` | `系统核心支持库.资源管理.创建内容摘要` | **跨库转发**，丢 `算法`（见 I-4） |

**现场证据**

```
$ sed -n '44,105p' 模块库/文件管理/实现/文件管理.py
def 读取文件(文件路径: str) -> 结果:
    """读取文本文件（默认 utf-8），返回 结果[文本]。"""
    return _调用支持库("文件系统支持库.文件操作.读取文件", {"文件路径": 文件路径, "编码": "utf-8"})

def 写入文件(文件路径: str, 内容: str) -> 结果:
    """写入文本文件（默认 utf-8），返回 结果。"""
    return _调用支持库("文件系统支持库.文件操作.写入文件", {"文件路径": 文件路径, "内容": 内容, "编码": "utf-8"})

def 复制文件(源路径: str, 目标路径: str) -> 结果:
    return _调用支持库("文件系统支持库.文件操作.复制文件", {"源路径": 源路径, "目标路径": 目标路径})
… （9 个函数，函数体全部只有一行 return _调用支持库(...)）
```
```
$ python3 -c "统计实现体的调用点数"
模块库/文件管理/实现/文件管理.py  行数=101  调用点=10
模块库/OCR/实现/OCR.py            行数=142  调用点=5
模块库/媒体转写/实现/媒体转写.py  行数=123  调用点=5
模块库/媒体处理/实现/媒体处理.py  行数=163  调用点=7
模块库/图像处理/实现/图像处理.py  行数=179  调用点=12
```

参数面差异（`能力定义.json` 对照）：
```
文件系统支持库.文件操作.读取文件 -> [('文件路径','文本型',True), ('编码','文本型',False)]
文件管理.读取文件             -> [('文件路径','文本型',True)]
文件系统支持库.文件操作.写入文件 -> [('文件路径',True), ('内容',True), ('编码',False), ('允许危险路径',False)]
文件管理.写入文件             -> [('文件路径',True), ('内容',True)]
文件系统支持库.文件操作.删除文件 -> [('文件路径',True), ('允许危险路径',False)]
文件管理.删除文件             -> [('文件路径',True)]
```

**反证（为什么不能一律判缺陷）**：模块库中**确有真正在组装的包**，例如
- `模块库/图像处理.识别图像格式` → 实现体内 `return _调用支持库("图像处理支持库.图像解码.解码图像", …)`，**把「解码结果」重组为「格式+宽高+模式」**（`模块库/图像处理/实现/图像处理.py:112-117`）—— 这是组装，不是透传
- `模块库/文档解析` → 按格式分派到 4 个不同支持库能力（`办公文档支持库.文字文档/表格文档/演示文稿` + `文档转换支持库.PDF文本表格`）、含临时目录申请与释放（`模块库/文档解析/实现/文档解析.py:26-36, 90-98, 154-160`）
- `模块库/项目文档` → 有独立参数/边界模块（`模块库/项目文档/实现/调用.py:23`、`登记项目.py:17`、`项目边界.py`）
- `模块库/简单窗口` → 组合 4 条能力（`窗口描述.创建窗口定义`／`窗口描述.校验窗口定义`／`状态描述.创建窗口状态`／`状态描述.状态流转校验`），有状态机

**结论**：`模块库/文件管理`（9/9 透传）、`模块库/OCR`（3 条 → 3 条）、`模块库/媒体转写`（4 条）、`模块库/媒体处理`（6 条）、`模块库/图像处理`（9 条中 8 条）**是纯透传门面**，属「同一目的两套 id」；而 `文档解析`／`项目文档`／`简单窗口` 属合法组装（见第五节）。

**影响面**：5 个模块包 / 约 30 条模块能力与支持库能力 1:1 同名（末段名全同的跨包组共 **55 组**）；此外**参数面被裁**意味着调用方走模块 id 就**拿不到** `编码`／`允许危险路径` —— 这是「门面比底座弱」，不是「门面比底座强」。

**四级框架归类**：**①（同能力面同类型）→ 合并增强**；但**分层合法性需先裁决**（四级③「增强现有支持库」不适用，因为底座能力已存在，缺的不是原子点）

**是否建议修**：**建议先裁决再修**（重要级）。理由：哲学 2.1 明文「模块＝组装原子能力解决一类问题」，纯透传门面**不满足「组装」**，但**删除它们会破坏已发布的模块层契约与验证场景**（各模块均有 `验证场景/正向HTTP场景.json`）。按哲学 1.2②边界口径，兼容旧调用方**只走「到期版本＋使用计数＋删除条件」三件**，本次审计不具备这三件的现场数据。

**最小改法落点**（按最小侵入排列）

| 选项 | 落点 | 代价 |
|---|---|---|
| A（推荐，先做） | 把参数面**补齐**到与支持库一致（`模块库/文件管理/能力契约/参数契约.json` + `能力定义.json` + `实现/文件管理.py` 3 处），让门面不再比底座弱 | 无破坏性；不动 id |
| B（后续） | 在三个文件写「到期版本／使用计数／删除条件」，纳入 1.2② 三件套 | 需先采集使用计数 |
| C（不推荐当场做） | 直接删纯透传模块能力 | 破契约、毁验证场景 |

---

### I-2｜提供者注册机制两套并存

**判定的哲学条款**：8.1（能力唯一权威，杜绝旁路）／8.2（发现第二套实现即收口删除）／7.9（不得同时存在两种表达）

**候选能力/机制清单**

| # | 标识 | 形态 | 位置 |
|---|---|---|---|
| 1 | `支持库.适配层.提供者注册表`（`提供者声明`／`提供者注册表`／`注册全部标准提供者`） | **进程内 Python 类与函数**，不注册为能力 | `支持库/适配层/提供者注册表/提供者注册表.py`（10480B）、`标准提供者.py`（3808B）、`__init__.py`（371B） |
| 2 | `平台控制面.提供者.注册表.{登记提供者,调用提供者,查询提供者,卸载提供者}` | **网关可调能力**（4 条） | `平台控制面/提供者/注册表/能力定义.json`、`注册表实现.py`、`实现/能力入口.py` |

**现场证据**

```
$ sed -n '1,15p' 支持库/适配层/提供者注册表/提供者注册表.py
"""提供者注册表：外部提供者注册、唯一选择与路由权威化。
规则：同一适配能力只能选择一个提供者；提供者必须声明名称、版本、能力和配置需求…
路由权威化（第三十阶段C3）：提供者注册表、实际进程与依赖锁必须对齐。
登记路由 把能力绑定到 提供者名称+提供者目录+依赖锁路径+运行方式；
同一能力已登记不同目录必须失败（禁止目录有记录、运行时却选另一实现）；
…本注册表只承载路由事实点。
"""

$ python3 -c "读 平台控制面/提供者/注册表/包声明.json 的说明"
提供者注册表：注册 → 授权 → 资源监督 → 真实执行 → 证据。对外只暴露
登记提供者/调用提供者/查询提供者/卸载提供者 四条能力，提供者以「对目标能力的委托」
表达，真实执行经资源监督有界执行单元。

$ rg -n '适配层.提供者注册表|提供者路由信息' --glob '!工程缓存/**' .
./运行核心/运行环境管理器/提供者生命周期.py:194:  from 支持库.适配层.提供者注册表 import 提供者路由信息
./支持库/适配层/__init__.py:37:                 from 支持库.适配层.提供者注册表.提供者注册表 import 提供者声明, 提供者注册表
./支持库/适配层/__init__.py:38:                 from 支持库.适配层.提供者注册表.标准提供者 import 注册全部标准提供者

$ rg -n '适配层|提供者注册表' 平台控制面/提供者/注册表/*.py 平台控制面/提供者/注册表/实现/*.py
（无任何 支持库.适配层 引用 —— 两套注册表之间零连接）
```

**第三套（同族）**：`平台控制面/提供者` 包另有 3 条能力（`采样资源`／`审计平台控制面`／`只读激活指针`），以及 `平台控制面/提供者/动态库提供者` 3 条（`调用动态库`／`可用库列表`／`宿主可用性`）—— 三者都叫「提供者 ×××」，但各自的「提供者」语义不同。

**影响面**：`支持库/适配层/提供者注册表` 被 `运行核心/运行环境管理器/提供者生命周期.py:194`（生命周期权威）**与**测试中心引用；`平台控制面/提供者/注册表` 由已编译制品与网关公开。两套写入点、两套 `登记`／`查询` 语义、**无任何对齐判据** —— 同一「提供者路由事实」存在两个权威。

**四级框架归类**：**①（同名同类型：都是「提供者注册表」）→ 合并增强**；若判定二者语义确实不同（进程内路由事实点 vs 能力化委托执行），则落 **②（底子不同但格式一样）→ 加模式变量**，但**必须先有判据把二者区分开**（当前无）。

**是否建议修**：**建议登记＋先补判据，暂不合并**（重要级）。理由：两者解决的问题确有重叠但不完全等同；哲学 12.2③「维修期发现的非当前问题不打断当前轮——先落盘，按同类批量扫描后随下一批统一收口」。

**最小改法落点**
```
① 先在 开发文档/未完成事项.md 登记「提供者注册双写点」，并写明对齐判据缺口；
② 补一条只读判据能力：比对「支持库.适配层.提供者注册表 的登记路由」
   与「平台控制面.提供者.注册表 的已登记提供者能力id」，
   不一致即报（落 平台控制面/能力目录 或 平台控制面/包仓库 —— 与既有门禁同域）；
③ 判据绿后再裁合并或加模式变量。
```

---

### I-3｜`支持库/适配层/` 根下 12 个「非正式包」目录：与 18 个已能力化 `*提供者` 包形成两套登记形态

**判定的哲学条款**：8.1（旁路）／2.4①（不许新开第二条腿）／10.4（全套齐活才算补完）

**清单（实测：适配层 `支持库/适配层/` 下 30 个目录）**

| 形态 | 数量 | 举例 |
|---|---|---|
| **有 `包声明.json`、有能力定义**（18 个 `*提供者` 包，61 条能力） | 18 | `Pillow提供者`／`Git提供者`／`FFmpeg提供者`／`密码签名提供者` … |
| **无 `包声明.json`、无 `能力定义.json`、零公开 id**（12 个目录） | 12 | 见下表 |

```
$ python3 -c "遍历 支持库/适配层/*/，判 包声明.json 是否存在"
适配层无包声明目录数: 12
   HTTP服务适配器        能力定义.json=False
   MySQL提供者          能力定义.json=False   ← 目录下只有 说明/
   动态库适配器           能力定义.json=False
   密钥提供者            能力定义.json=False
   提供者注册表           能力定义.json=False
   提供者配置            能力定义.json=False   ← 目录下只有 提供者配置.json
   提供者配置读取          能力定义.json=False
   数据库适配器           能力定义.json=False
   本地进程适配器          能力定义.json=False
   说明                能力定义.json=False   ← 文档目录
   配置契约             能力定义.json=False
   配置安全             能力定义.json=False

$ ls -1 支持库/适配层/数据库适配器
__init__.py  161B
数据库适配器.py  2347B
```

**现场引用（证明它们不是死代码，是真实第二形态）**
```
$ rg -n 'import.*提供者注册表|from .*提供者注册表' --glob '!工程缓存/**' -g '!支持库/适配层/提供者注册表/**' .
./运行核心/运行环境管理器/提供者生命周期.py:194:  from 支持库.适配层.提供者注册表 import 提供者路由信息
./支持库/适配层/__init__.py:37,38
./测试中心/运行核心/测试_提供者生命周期.py:34

$ rg -n '数据库适配器|动态库适配器|本地进程适配器|HTTP服务适配器' --glob '!工程缓存/**' .
支持库/适配层/提供者注册表/标准提供者.py:13-16   ← 四个适配器被登记为「标准提供者」
支持库/适配层/__init__.py:33-36                   ← 适配层包级公开面
测试中心/支持库/测试_进程文件补齐.py:23
测试中心/模块库/测试_自修复工具.py:47,86
```

**影响面**：`支持库/适配层/__init__.py:32-38` 把这 6 个（`适配契约`／4 个适配器／提供者注册表／标准提供者）**作为适配层包级公开面导出**，而它们**不在网关注册表中、不在「能力目录.搜索能力」检索面内、也不受任何能力契约约束**。同一个「适配层」下并存：

```
形态 A（10 个模块库/支持库能力面）：经 网关 /能力契约/ 契约/验证场景 全套 —— 可发现、可校验
形态 B（12 个目录）：      裸 .py，仅 Python import 可达       —— 不可发现、不可校验
```

**四级框架归类**：**①（同名同类型：都在适配层干「翻译第三方」）→ 合并增强**；对确实无法能力化者（环境依赖层）落 **③（增强现有支持库）**

**是否建议修**：**建议登记，暂不修**（重要级）。理由：`AGENTS.md:296-300` 明确豁免「不对外提供能力契约的内部适配器」，且 `开发文档/分析/审计_依赖分层归属_20260919.md:404` 已就同一批文件给出**反向结论**：「判它们违规 = 要求翻译层不许碰被翻译的库，只会把适配代码塞进 `*提供者/` 冒充提供者，让『一第三方一支持库一提供者』失真的**反向**后果」。故**必须与依赖归属审计的结论合并处理，不得单独下裁决**。

**最小改法落点**
```
① 与 开发文档/分析/审计_依赖分层归属_20260919.md 的 S0-3（该建锁没建锁）合并；
② 在 支持库/适配层/说明/README.md 明确写清「形态 A / 形态 B 的分界判据」
   （可能力化 vs 环境依赖层），使两形态不再是「无判据的并存」；
③ 不为这 12 个目录补 能力定义.json（那会把环境依赖层硬造成能力，违背 2.1）。
```

---

### I-4｜`模块库/文件管理.流式创建摘要` 与 `系统核心支持库.资源管理.创建内容摘要`：同目的、参数被裁、返回类型不一致

**判定的哲学条款**：1.3（结果唯一即收口）／8.3；**衍生依据** 2.4③（翻译的重心只在边界）

**现场证据**
```
$ python3 -c "对照两能力的参数与返回"
文件管理.流式创建摘要      v1.0.0  参数 [('文件路径','文本型',True)]
    返回 {'类型':'结果型','值结构':{'成功':'逻辑型','值':'文本型'}}
    说明 大文件分块流式创建内容摘要（sha256），返回 结果[摘要文本]

系统核心支持库.资源管理.创建内容摘要 v1.0.0 参数 [('文件路径','文本型',True), ('算法','文本型',False,'sha256')]
    返回 {'类型':'结果型','值结构':{'值':'内容摘要文本（hex 字符串）'}}
    说明 创建文件内容摘要（sha256，分块读取不加载全文件）；成功返回摘要文本
         （平台既有契约：文件管理/自修复工具直接消费摘要文本）

$ sed -n '91,101p' 模块库/文件管理/实现/文件管理.py
def 流式创建摘要(文件路径: str) -> 结果:
    """大文件分块流式创建内容摘要（sha256），失败返回统一错误结构。

    系统核心支持库.资源管理.创建内容摘要 成功返回摘要文本（非 结果），此处包装为 结果。
    """
    try:
        return _调用支持库("系统核心支持库.资源管理.创建内容摘要", {"文件路径": 文件路径}, 失败错误码="摘要失败")
    except FileNotFoundError as 错误:
        return 结果.失败("文件不存在", str(错误), 来源="文件管理")
    except Exception as 错误:
        return 结果.失败("摘要失败", f"系统核心支持库.资源管理.创建内容摘要 调用失败: {错误}", 来源="文件管理")
```

**平台自有记录**：`开发文档/项目证据/MCP使用反馈.jsonl:62`（第二十五阶段工作包）原始反馈原文：

> 「资源管理.创建内容摘要 契约声明返回"结果"，实际实现返回字符串并对缺失文件抛 FileNotFoundError，**模块层被迫自行 try/except 包装，契约与实现不一致增加调用方负担**」
> 升级建议：「建议资源管理.创建内容摘要 与 文件系统 各原子能力**统一为 结果 返回结构并注册稳定错误码，模块层即可纯透传**，消除手动异常包装。」

**影响面**：2 个包（`模块库/文件管理`、`支持库/后端/系统核心支持库/资源管理`）＋ 全部 `创建内容摘要` 消费方（`自修复工具` 亦消费，见 `自修复工具/实现/自修复工具.py:225`）。这是**「底座契约与实现分叉」被模块层用 try/except 掩盖**，属哲学 3.2「不兜底、不降级掩盖」的反面。

**四级框架归类**：**②（底子不同但格式一样 → 加模式变量）／③（底子有同类但缺原子点 → 增强现有支持库）**。精确说：缺的不是新能力，是**底座返回结构统一** —— 属 ③。

**是否建议修**：**建议修**（重要级），但**修底座不修模块**（哲学 7.7「能力不足就补底座，不许收窄约束」）。

**最小改法落点**
```
① 落点：支持库/后端/系统核心支持库/资源管理/实现/资源管理.py（让 创建内容摘要 返回 结果）；
② 同批改：能力定义.json + 能力契约/参数契约.json + 说明/使用说明.md（契约三处齐活，哲学 10.4）；
③ 契约面变化（返回类型）→ 按哲学 5.3 必须递增包版本（当前 1.1.2 → 1.2.0）；
④ 旧调用方（文件管理.流式创建摘要 的 try/except）在底座修好后**删除包装、改纯透传**；
⑤ 不得反向做（把模块层的 结果 包装当"契约"补进底座 —— 那是把兜底固化成契约）。
```

---

### S-1｜`数据操作支持库.类型转换` 10 项：说明文字全部套同一模板

**判定的哲学条款**：8.3（能参数化成一个能力的绝不分家）

**候选能力 id 清单（10 条）**
```
数据操作支持库.类型转换.文本转整数     数据操作支持库.类型转换.文本转长整数
数据操作支持库.类型转换.整数转文本     数据操作支持库.类型转换.长整数转文本
数据操作支持库.类型转换.文本转单精度数  数据操作支持库.类型转换.文本转双精度数
数据操作支持库.类型转换.数值转文本     数据操作支持库.类型转换.逻辑转文本
数据操作支持库.类型转换.文本转逻辑     数据操作支持库.类型转换.校验类型
```

**现场证据**
```
$ python3 -c "归一化说明文本后两两比对（difflib.SequenceMatcher）"
0.980  数据操作支持库.类型转换.文本转整数  ||  数据操作支持库.类型转换.文本转长整数
0.980  数据操作支持库.类型转换.整数转文本  ||  数据操作支持库.类型转换.长整数转文本
0.963  数据操作支持库.类型转换.文本转单精度数  ||  数据操作支持库.类型转换.文本转双精度数
0.920  数据操作支持库.类型转换.文本转整数  ||  数据操作支持库.类型转换.文本转逻辑
…（10 条中 9 条互相相似度 ≥0.83，说明文字全是「<能力id> 确定性原子转换」同一模板）
```

**影响面**：仅本包内 10 条 id；零跨包重复。

**四级框架归类**：**①（内部同名族）／②（加模式变量：方向 + 目标类型）**

**是否建议修**：**建议保留、仅登记**（建议级）。**反向理由**（必须写进报告，否则会被误当成 8.3 违规批量合并）：
- `参数契约` 用的是**正式类型体系**（`公共契约/基础类型/`），`文本转整数` 与 `文本转双精度数` 的**类型转换语义、溢出边界、错误码集合都不同**；按 8.3「每个原子能力必须定义清楚（输入/输出/存储格式/错误码）」，它们是**不同的原子能力**，不是同一能力的参数化。
- 若按 8.3 硬合成一个「类型转换(值, 目标类型)」能力，会把**类型检查从编译/契约期推迟到运行期**，与 7.9／5.3 的契约面要求相反（`校验类型` 恰是为此存在的反例）。

**最小改法落点**：无（不改）。仅在 `开发文档/未完成事项.md` 留一条「已审、判为设计取舍，勿按 8.3 合并」的防重复探索记录（与哲学 3.3「一次命中」同源）。

---

### S-2｜`会话存储.续约句柄` 与 `模型连接器.续租句柄`：参数完全相同，返回差一字

**判定的哲学条款**：8.3；四级 **②**（底子不同但格式一样 → 加模式变量）

**现场证据**
```
大语言模型支持库.会话存储.续约句柄    v1.0.0
    参数 [('句柄','整数型',True), ('租约秒','整数型',False)]
    返回 {'句柄':'整数型','已续约':'逻辑型','超时秒':'整数型'}
    说明 续约句柄：重置最后活动时间（续租），可选覆盖超时秒

大语言模型支持库.模型连接器.续租句柄  v1.0.0
    参数 [('句柄','整数型',True), ('租约秒','整数型',False)]
    返回 {'句柄':'整数型','已续租':'逻辑型','超时秒':'整数型'}
    说明 续租句柄：重置最后活动时间，可选更新超时秒。

$ rg -n 'def 续约句柄|def 续租句柄' 支持库/后端/大语言模型支持库/*/实现/*.py
支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1181:def 续租句柄(句柄: int | None = None, 租约秒: int = None) -> 结果:
支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py:258:def 续约句柄(句柄: int = None, 租约秒: int = None) -> 结果:
```

**影响面**：2 条能力、2 个包；均属 `大语言模型支持库` 域内。相似度 **0.800**（全仓 ≥0.80 的不同 id 对里，只有这一对是「参数面完全相同」的真参数化候选）。

**四级框架归类**：**②（底子不同：会话库句柄 vs 连接池句柄；格式一样：参数/返回同形）→ 加模式变量**

**是否建议修**：**建议保留、登记为四级②候选**（建议级）。理由：
- 两者**句柄体系不同**（`会话存储` 的句柄指向 SQLite 会话记录；`模型连接器` 的句柄指向进程内连接池），底子确实不同 → 不能按 ① 直接合并。
- 按四级②应加「模式」变量（如 `连接类型=1 会话 / 2 模型`），但**当前句柄本身是不透明整数**，`会话存储.释放句柄`／`模型连接器.释放句柄`／`数据结构支持库.释放句柄`／`并发控制支持库.释放句柄`／`组件控件.释放句柄`／`系统核心支持库.进程管理.释放句柄` **6 套 `释放句柄` 同名并存**（同样的问题在释放侧更明显）→ 合并需先统一句柄类型体系（已登记债务 E-8 类型体系收口，`未完成事项.md:155`），**不得先合并能力**。

**最小改法落点**
```
不作代码改动；在 开发文档/未完成事项.md 登记：
  「续约句柄/续租句柄 + 6 套 释放句柄 属四级②候选；
   前置依赖 = E-8 类型体系收口（未完成事项.md:155）；
   E-8 完成前不得合并。」
```

---

### S-3｜`数据集合` 内含文本类能力，与同库 `文本处理` 包目的重叠

**判定的哲学条款**：8.3（每个原子能力精而不杂）／2.1（归属固定）

**现场证据**
```
$ python3 -c "列 数据操作支持库.数据集合 的 23 条能力"
数据操作支持库.数据集合.文本截取    [('文本','文本型',True), ('起始','整数型',False), ('长度','整数型',False)]  说明「文本截取」
数据操作支持库.数据集合.文本去首尾  [('文本','文本型',True), ('字符集','文本型',False)]                    说明「去除文本首尾指定字符」
数据操作支持库.数据集合.文本重复    [('文本','文本型',True), ('次数','整数型',True)]                      说明「文本重复 N 次」
数据操作支持库.数据集合.列表分片    [('列表','列表型',True), ('起始','整数型',False), ('结束','整数型',False)] 说明「列表分片」

$ python3 -c "列 数据操作支持库.文本处理 的 10 条能力"
数据操作支持库.文本处理.分割文本 / 合并文本 / 替换文本 / 查找文本 / 去空白 / 转大写 / 转小写 / 统计长度 / 按行分割 / 解析区间
```

**影响面**：`数据操作支持库.数据集合`（23 条）中的 3 条文本能力；相似度对照见下：
```
0.855  数据操作支持库.数据集合.文本去首尾  ||  数据操作支持库.文本处理.去空白   ← 目的重叠（都是裁剪首尾）
```

**四级框架归类**：**①（同目的，同为「文本操作」）→ 合并增强／归位**

**是否建议修**：**建议保留、登记归位候选**（建议级）。`文本去首尾` 带 `字符集` 参数（可裁任意字符），`去空白` 固定裁空白 —— 语义不同，**不构成「同一目的两套实现」**；真正的问题是**归属**（文本操作住在 `数据集合` 包里，撞 2.1「分层与归属固定」）。

**最小改法落点**
```
① 不作能力合并（会破契约）；
② 在 支持库/后端/数据操作支持库/数据集合/说明/设计说明.md 写明
   「本包为何含 3 条文本能力」的归属理由；写不出来才按 2.1 迁 文本处理；
③ 迁包 = 改 id（`数据集合.文本截取` → `文本处理.文本截取`）× 3 + 契约/声明/说明/摘要同批 + 版本递增，
   代价 > 收益，除非后续出现真实调用方混淆。
```

---

## 三、被显式判为「非缺陷」的候选（本路的反向核对）

以下均在首轮机械扫描中**命中过「疑似重复」**，逐对**读完实现**后判定**不是缺陷**。此节是审计可信度的另一半：避免把合法的分层/边界当成重复批量合并。

| # | 对象 | 为什么不是缺陷 | 依据 |
|---|---|---|---|
| 1 | `支持库/适配层/*提供者` 作为**翻译层**（18 包 61 能力） | 哲学 2.4「底座的一切外部接触面都必须经过翻译层」「翻译层用成熟第三方实现，不自己造」。适配层的**存在本身**是设计，不是重复；**缺陷是它和后端腿并存两套实现**（即 B-1），不是它有实现 | `项目说明.md:152-164`；`架构总览.md:50,92`（「第三方只在适配层」） |
| 2 | `模块库/文档解析`／`模块库/项目文档`／`模块库/简单窗口` | 真正在组装：格式分派+临时目录+释放（文档解析）、独立边界模块（项目文档）、4 能力组合+状态机（简单窗口） | 各包 `实现/*.py`；`项目说明.md:144` |
| 3 | `模块库/图像处理.识别图像格式` | 基于 `图像解码.解码图像` **重组成不同返回结构**（解码结果 → 格式+宽高+模式） | `模块库/图像处理/实现/图像处理.py:112-117` |
| 4 | **声明 699 / 网关 468 的差额 231** | 差额 = 其他根 102 + 内部层 68 + 适配层 61。`能力索引.py:22` `排除包前缀 = ("支持库.适配层.",)` ＋ `:95-100` `是否公开包`（内部层不暴露）是**既有边界判据**；实测 `公开口径 − 网关 = 0` | `模块库/能力目录/实现/能力索引.py:22,95-100`；`项目说明.md:48`（明写「非适配层公开包口径（**不是缺陷**）」） |
| 5 | `数据操作支持库.类型转换` 10 项 | 契约用正式类型体系，转换语义/边界/错误码各不相同；硬合并会把类型检查推迟到运行期 | 见 S-1 的反向理由；`项目说明.md:246` |
| 6 | 同名「检查可用性」**10 处**、同名「检查提供者」**12 处** | 全部同名不同对象：`转写.检查可用性`（MLX Whisper）／`适配层.psycopg提供者.检查可用性`（驱动）／`YAML提供者`／`MCP协议提供者`／`TreeSitter提供者`／`重排服务`／`知识检索`／`媒体转写`／`直播逐字稿`；「检查提供者」分别探 ffmpeg/soffice/git/textutil/codegraph/pdfplumber+tesseract。属 8.3 的「原子能力」正当存在，**不是同目的** | 各包 `能力定义.json` |
| 7 | 同名「释放句柄」**6 处** | 与 S-2 同源：句柄体系不同（进程/会话/连接/结构/并发/组件）。属 E-8 类型体系收口范围，**当下不判重复** | 见 S-2 |
| 8 | `平台控制面/能力目录`（14 能力）vs `模块库/能力目录`（2 能力） | **同名不同目的**：前者是消费者契约注册表/契约漂移判定/发布门禁/文件租约；后者是能力检索（`搜索能力`/`读取能力`）。两包 `包id` 不同（`平台控制面.*` vs `模块库.*`）、能力面零交集 | 两包 `能力定义.json`；`模块库/能力目录/包声明.json:6` |
| 9 | 前端 5 个描述包 / 办公文档 4 子包 `__init__.py` 相似度 0.85–0.93 | 是**同族注册样板**（`能力实现(...)` 注册元组的固定形状），非业务实现；`_校验参数名序` 等闸门是同一份装配期判据的**有意复用** | `支持库/前端/状态描述/__init__.py:24-30`；`支持库/后端/办公文档支持库/文字文档/__init__.py:9-25` |
| 10 | D-2 中**已收口的 4 组**逐字节相同 `.py` | 后端腿已改为 40 行转调门面（`sys.modules[__name__] = 唯一实现`）或子进程转调，**不再持第二份实现** —— 这是**正确解**，不是缺陷 | `支持库/后端/文档转换支持库/PDF渲染/实现/子进程解析.py:1-40`；`PDF文本表格/实现/PDF文本表格.py:39`；`PDF生成/实现/子进程入口.py:27` |
| 11 | `支持库/适配层/` 根下 12 个无包声明目录 | `AGENTS.md:296-300` 明确豁免「不对外提供能力契约的内部适配器」；`开发文档/分析/审计_依赖分层归属_20260919.md:404` 已给出**反向**理由（强行能力化会让「一第三方一支持库一提供者」失真）。本路**不单独下判**，登记为 I-3 待合并 | `AGENTS.md:296-300`；依赖分层归属审计第 2 条 |
| 12 | `版本注册表`（包级多版本并存） | 哲学 1.2④禁的是**「能力级同 id 多版本」**；包级多版本有 5.3② 明文授权（「旧版本按 版本注册表 引用保护保留、无引用才允许清理」），且当前 `/tmp/系统级支持库_版本` 目录不存在 = 空表 | `项目说明.md:125,194`；`运行核心/加载器/版本系统/版本注册表.py:1-3` |
| 13 | `模块库/文件管理.流式创建摘要` 的 try/except | **不是**「第二套实现」，而是**对底座契约分叉的兜底**。它该消失（I-4），但消失方式是**修底座返回结构**，不是删模块能力 | `模块库/文件管理/实现/文件管理.py:91-101`；`MCP使用反馈.jsonl:62` |

---

## 四、审计局限（本路没做到 / 不能下判的）

1. **未跑装配口径的官方读数**。任务书引用的官方口径「装配后 664」（`未完成事项.md:175`）本路**未复现**：`装配系统(...)` 直调在 180 秒 `timeout` 内未返回（进程内直调底座属于第二执行腿，任务书亦要求本路只读源码、不走 import）。替代做法是**用网关检索面 468 + 磁盘声明 699 两条口径交叉核对**，两者各自自洽且差额归因干净（231 = 其他根 102 + 内部层 68 + 适配层 61）。**若需 664 口径，须另跑官方命令；本报告的 468/699 与之口径不同，不可混用。**
2. **未取「使用计数」**。I-1（模块透传门面）与 B-2（调用点切换）都要落 1.2② 的「到期版本＋使用计数＋删除条件」三件，本路**只读、无运行态统计**，故**只给最小改法落点、不给删除条件**。
3. **B-1 的「最后一次已知状态」可能已漂移**。2026-09-17 清单（`适配层唯一化迁移清单_20260917.md:12`）快照时点 HEAD 为 `5686479a`、脏树 29 项；本路 HEAD 为 `836e9066`、脏树 0 项。本报告 B-1 的「已收口 4 组 / 未收口 9 对」是**本路现场实测**，与那份清单的「第 9、10 对已完成 / 其余 10 对未迁移」**不完全一致**（本路实测已有 4 组收口）。引用一律以本报告现场读数为准。
4. **相似度阈值是启发式**。全仓近似重复用 `difflib.SequenceMatcher.ratio() ≥ 0.85`（过滤长度差 >15%），可能漏掉重写式重复（同一逻辑换写法），也必然包含样板重复（已放第三节第 9 条）。**它不是判据，只是候选生成器**；每条结论都回到实现读完才判。
5. **未审计「生成物 vs 手写」的 7.9 面**。生成物三律（输入指纹/勿手改/全量重算无差异）与「同一件事不得同时存在生成与手写」属另一路审计范围（`开发文档/分析/生成物地图_20260918.md` 已在），本路**未展开**，故第四节不出现 7.9 的独立条目。
6. **未审计契约版本面**。5.1（契约唯一）本路只核对了事实源存在与取值（`公共契约/版本规则/契约版本.py` → `契约版本 = "2.0.0"`），**未逐包比对**是否存在第二处契约版本声明。
7. **`能力数据/能力搜索数据.json` 未纳入比对**。该文件是检索辅助数据，可能与 `能力定义.json` 分叉；本路只以 `能力定义.json` + `包声明.json` 为真源，未做三处交叉。
8. **未判 I-2 / I-3 的最终形态**。两条都**必须**与 `开发文档/分析/审计_依赖分层归属_20260919.md` 合并处置（多路并行，本路只写本文件），故本报告只给「登记 + 补判据」的最小动作，**不给合并方案**。

---

## 五、一句话收口

**能力唯一性的真正主战场只有一处**：`支持库/适配层/*提供者` 与 `支持库/后端/*` 的 **12 对包 / 43 对同能力面 id**（B-1 + B-2），它同时撞 1.2①／1.3／8.2／2.4①，且**方向已裁、只差执行** —— 本路实测已有 **4 组收口、9 对未动**，另有 **2 组业务代码至今逐字节相同**。
其余问题（I-1 ~ I-4、S-1 ~ S-3）**均为可参数化/可归位/需先裁决的二级问题**，且每条都有反向理由说明「为什么不能机械合并」。
**「同 id 多版本并存」本类未发现**（699 条能力 id 版本集合恒单元素）。
