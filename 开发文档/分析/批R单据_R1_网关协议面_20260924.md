<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R · R-1 网关协议面收口（子代理回传）

> 状态：**已落地**（源码树）｜开工ID `开工-20260924-105544-fb2c`（追加租约 `开工-20260924-105658-0c62`）｜所有者：子代理-R1
> 裁决：华哥 2026-09-24「参数名一定要对齐，如果未对齐，直接给删除掉，这个修改一下这个信息。」（**口径定死，不再选**）
> 生成：2026-09-24｜上游：`开发文档/分析/批P清单_20260924.md` P-2 段

## 零、口径（一句话）

**参数名必须对齐；未对齐即剔除（回带 `被忽略参数`），不判红、不失败。**

- 批H 裁-2 的 fail-closed（未对齐即报错）方向**已被本裁决推翻**，未落地、也不再落地；
- 代码行为本来就是「剔除」，本次**改的是说法 + 三处表的收口**；唯一一处真行为变化见 §一-6（`数字` / `空` 也归一）；
- 旧登记「未知字段一律忽略」在代码注释与两处权威正文里逐字改成「未对齐即剔除」；
- `哲学第 5 条 2 项` 这个条号是**引证错误**（5.2 原文讲「版本不对齐默认不失败」），仓内已两处纠正（`公共契约/能力契约/契约.py:107`、`开发文档/方案/MCP接入面瘦身与网关协议对齐方案_20260921.md:52`）⇒ 本次把引用该条号的注释一并改为指向规范正文，不再复述错条号。

## 一、落点逐条（改前 → 改后）

### 1. 主节点：`运行核心/统一网关/协议/类型规格.py`（`校验能力参数` 内）

| | 内容 |
|---|---|
| 改前 | `参数名 = {...}` 集合推导 + `_ = sorted(set(参数) - 参数名)`（原 :162）——**算出差集直接丢弃**；上方 3 行注释逐字登记「未知参数一律忽略（哲学第 5 条 2 项）」 |
| 改后 | **删掉** `参数名` 定义与那行死代码（同一判据的第二套差集，哲学 1.2），注释改为「参数名必须对齐；未对齐即剔除（回带 `被忽略参数`）」并指向规范真源 |
| 为何删而非留 | 剔除与回带已在 `网关参数校验面` 落地（`_过滤能力参数` / `_被忽略参数`）；本函数是**唯一校验点**，只判必填与类型，不该再算一份差集 |

### 2. 例外表：`运行核心/统一网关/核心处理/网关参数校验面.py` `横切参数名`

- `横切参数名 = frozenset({"项目根"})` **保留不动**（本批未删）。理由：`开发工具/薄壳/薄壳服务.py` 对每次「调用能力」自动补位顶层 `项目根`，不排除则每个未声明它的能力都回带一条**平台自己注入**的噪声；
- 反证用例仍在：`测试中心/运行核心/测试_网关兼容性.py` 的 `test_横切参数项目根不回带` 与反向样本 `test_掐掉横切排除后项目根必被报出`；
- 该表注释里引的「网关对未知参数一律忽略」措辞 → 改为「网关对未对齐参数一律剔除、不判红」。

### 3. 两腿：`运行核心/统一网关/核心处理/网关执行面.py` `:135`（同步）/ `:222`（任务提交）

- 现场核对：两条腿**逐字同口径**——`已归一参数 = _应用参数别名(...)` → `_能力参数错误(...)` → `响应.被忽略参数 = self._被忽略参数(...)`，**未发现分叉**；
- 故判据本身未动，本批只动了它的导入面（见落点 6：`类型短名映射` 收口后该名不存在，留着即 ImportError）；
- 两腿共调的唯一节点：`参数校验面._被忽略参数` → `最接近的契约参数`。

### 4. 唯一节点：`近似名阈值 = 0.34` + `difflib.get_close_matches`

| | 内容 |
|---|---|
| 改前 | 阈值（:38）与 `difflib.get_close_matches(键, 参数名, n=3, cutoff=近似名阈值)`（:300）内联在 `_被忽略参数` 里 |
| 改后 | 抽成模块级函数 `最接近的契约参数(键, 参数名, 取数=3)`（阈值仍是它的模块常量 `近似名阈值`），`_被忽略参数` 转调它；**两条腿经同一方法共调该节点** |
| 判据 | 全仓 `difflib.get_close_matches` 调用点 = **1**（唯一节点内，`rg` 取证见 §二-4）；未新写第二套相似度实现 |

### 5. 口径文本同步（代码注释 4 处 + 权威正文 2 处）

- 代码注释：`类型规格.py`（见落点 1）、`网关参数校验面.py:147`、`网关参数校验面.py:271`（`_被忽略参数` docstring）；
- 相邻口径面同批改：`公共契约/能力契约/契约.py:103`「网关边界对未知字段是**忽略**」→「对未对齐参数是**剔除**（不判红、不失败）」；`运行核心/统一网关/传输/流式路由面.py:70/:105` 注释 →「未对齐字段一律剔除（本流式入口不回带 `被忽略参数`）」；
- **权威正文（先定位真源，未猜）**：`项目文档支持库.查询索引(项目名=系统工程平台, 关键词="未知字段一律忽略 被忽略参数")` 的命中列表里，口径正文只有两处，均已改：
  1. `开发文档/规范/网关调用最小示例.md:129`（「调用方只需记住的四条兼容口径」规则 1）：标题从「多传字段不会失败」改为「**参数名必须对齐；未对齐即剔除**」，正文「未知字段一律忽略」→「未对齐字段一律剔除（**不判红、不失败**）」，小标题「但「忽略」不等于「无声」」→「但「剔除」不等于「无声」」；
  2. `开发文档/当前流程图.md:119`（传参铁律「多余参数分两层」）：网关边界从「按协议兼容口径**忽略**未知字段」→「对**未对齐参数一律剔除**（不判红、不失败）」。

### 6. 同批：类型短名映射 3 份 → 1 份（华哥「类型相似的合并成 1 类」）

**新真源**：`公共契约/基础类型/历史短名.py::历史短名到正式名` = **20 键**（三份并集，同键冲突 0 处；值全部落在 `类型表.py::正式类型表` 16 项内）。

| 份 | 原落点 | 原键数 | 改后 |
|---|---|---|---|
| A | `运行核心/统一网关/协议/类型规格.py::类型短名映射` | 18 | 删表，转调真源 |
| B | `开发工具/契约编译/能力定义编译器.py::易语言类型别名` | 10 | 删表，转调真源 |
| C | `开发工具/快速编译/运行快速编译.py::旧类型` | 11 | 删表，转调真源 |

**连带（不连则断，已同批处置）**：

- `开发工具/契约编译/漂移判定.py:242`：原 `from 开发工具.契约编译.能力定义编译器 import 易语言类型别名`（自述「唯一源、取不到就响亮失败」）→ 改指新真源（否则 import 必失败）；
- `开发工具/发布门禁/运行发布门禁_防回潮公共.py`：`_历史短名源` / `_历史短名变量` 由 `运行核心/统一网关/协议/类型规格.py::类型短名映射` 改指 `公共契约/基础类型/历史短名.py::历史短名到正式名`（不改则该门禁**读不到表 → 空转判红**，正是它自己注释里写的那种假绿/假红）；
- `运行核心/统一网关/网关核心.py` 与 4 个核心处理模块（参数校验面/执行面/幂等面/处理面）的 `from ...类型规格 import 类型短名映射` → 从导入列表移除（收口后该名不存在）。

**唯一一处真行为变化（如实登记）**：网关边界与能力定义编译器现在也认 `数字` / `空`（原先只有 `运行快速编译` 认）。存量扫描：`运行快速编译` 判红集新增的 10 个键（`逻辑/映射/日期时间/资源引用/句柄/结果/空值/长整数/单精度数/双精度数`）在 `能力定义.json / 包声明.json / 参数契约.json` 里 **0 命中** ⇒ **不产生存量红**。

## 二、回归证据（真实读数）

### 2.1 未知参数：剔除、不判红、照旧回带

```
capability_call(能力id="文件系统支持库.文件操作.读取文件",
                参数={"文件路径":"AGENTS.md","路径":"x","限定行数":1})
```

读数（原文）：

```
成功 = true
被忽略参数 = [
  {"参数名": "路径",     "原因": "该能力契约里没有这个参数，已被网关剔除", "最接近的契约参数": ["文件路径"]},
  {"参数名": "限定行数", "原因": "该能力契约里没有这个参数，已被网关剔除", "最接近的契约参数": []}
]
```

⇒ 口径成立：**剔除 + 回带，不失败**；`路径` 被点向真名 `文件路径`（唯一节点生效），`限定行数` 无相近名留空（阈值不瞎点）。

### 2.2 合法参数不得被误剔

```
capability_call(能力id="文件系统支持库.文件操作.读取文件",
                参数={"文件路径":"AGENTS.md","起始行":1,"结束行":1,"最大字符":80})
```

读数：`成功 = true`，`值 = "# 系统工程平台 · 项目硬规则\n"`，**`被忽略参数` 未出现**（空列表不回带）。

### 2.3 类型短名：三处收口后同一输入同一结论

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && python3.14 -c "
import sys, pathlib
sys.path.insert(0, '.')
from 公共契约.基础类型.历史短名 import 历史短名到正式名
from 公共契约.基础类型.类型表 import 正式类型表
from 运行核心.统一网关.协议 import 类型规格
from 开发工具.契约编译 import 能力定义编译器 as 编译器
from 开发工具.快速编译 import 运行快速编译 as 快速编译
from 开发工具.发布门禁 import 运行发布门禁_防回潮公共 as 防回潮
from 开发工具.契约编译 import 漂移判定
print('真源键数 =', len(历史短名到正式名))
print('值全在正式表内 =', set(历史短名到正式名.values()) <= set(正式类型表))
print('网关边界转调同一对象 =', 类型规格.历史短名到正式名 is 历史短名到正式名)
print('能力定义编译器转调同一对象 =', 编译器.历史短名到正式名 is 历史短名到正式名)
print('运行快速编译转调同一对象 =', 快速编译.历史短名到正式名 is 历史短名到正式名)
print('防回潮门禁 AST 读到键数 =', len(防回潮._防回潮历史短名集(pathlib.Path('.'))))
for 名 in ['文本','布尔','数字','空','浮点数','结果','空值','句柄','日期时间','逻辑']:
    print(名, '→ 网关边界', 类型规格.历史短名到正式名.get(名, 名), '| 编译器', 编译器.历史短名到正式名.get(名, 名), '| 快速编译判红', 名 in 快速编译.历史短名到正式名)
print('漂移判定._注解可接受(文本) =', 漂移判定._注解可接受('文本', {'文本型'}))
"
```

读数（原文回显，节选）：

```
真源键数 = 20
值全在正式表内 = True
网关边界转调同一对象 = True
能力定义编译器转调同一对象 = True
运行快速编译转调同一对象 = True
防回潮门禁 AST 读到键数 = 20
文本 → 网关边界 文本型     | 编译器 文本型     | 快速编译判红 True
布尔 → 网关边界 逻辑型     | 编译器 逻辑型     | 快速编译判红 True
数字 → 网关边界 双精度数型 | 编译器 双精度数型 | 快速编译判红 True
空   → 网关边界 空值型     | 编译器 空值型     | 快速编译判红 True
浮点数 → 网关边界 双精度数型 | 编译器 双精度数型 | 快速编译判红 True
结果 → 网关边界 结果型     | 编译器 结果型     | 快速编译判红 True
空值 → 网关边界 空值型     | 编译器 空值型     | 快速编译判红 True
句柄 → 网关边界 句柄型     | 编译器 句柄型     | 快速编译判红 True
日期时间 → 网关边界 日期时间型 | 编译器 日期时间型 | 快速编译判红 True
逻辑 → 网关边界 逻辑型     | 编译器 逻辑型     | 快速编译判红 True
漂移判定._注解可接受(文本) = True
```

⇒ 三处**同一输入同一结论**（同一对象、同一映射、判红集合同一）。

### 2.4 导入面与门禁单点

```bash
python3.14 -c "import importlib; [importlib.import_module(m) for m in ('运行核心.统一网关.网关核心','运行核心.统一网关.核心处理.网关执行面','运行核心.统一网关.核心处理.网关参数校验面','运行核心.统一网关.核心处理.网关幂等面','运行核心.统一网关.核心处理.网关处理面','运行核心.统一网关.传输.流式路由面','开发工具.发布门禁.运行发布门禁_防回潮判据','开发工具.发布门禁.运行发布门禁_防回潮公共','开发工具.快速编译.运行快速编译','开发工具.契约编译.能力定义编译器','开发工具.契约编译.漂移判定','公共契约.能力契约.契约','公共契约.基础类型.历史短名')]"
```

- 读数：**导入成功 13 / 13**；
- `运行发布门禁_防回潮判据.校验非正式类型名防回潮()` ⇒ `True｜正式类型名真源 公共契约/基础类型/类型表.py 共 16 项；历史短名 20 项；契约文件 429 份；判定位 12090；纯结构说明跳过 27；本次计数 0 条｜存量基线 0 条`（**读新真源后仍干净通过，0 存量红**）；
- 唯一节点单点实跑：`最接近的契约参数('路径', ['文件路径','修改路径','项目根'])` ⇒ `['文件路径','修改路径']`；`最接近的契约参数('未来字段', ['输入文本'])` ⇒ `[]`；
- `rg -n 'get_close_matches' --glob '!工程缓存'` ⇒ 全仓 **1** 处调用（唯一节点内）+ 1 处 `import difflib`。

## 三、残余与边界（未做 / 不能做）

1. **活体读数的边界（重要）**：§2.1 / §2.2 是经**运行中的网关**（PID 52871）拿到的，该进程 `cwd = 工程缓存/制品仓库/平台客户端环境/平台客户端`，即**跑的是制品、不是本仓源码树**（实测：制品内 `运行核心/统一网关/协议/类型规格.py` 不含新文本，`grep 历史短名到正式名` = 0）。⇒ 这两条读数证明「未对齐即剔除、不判红」在**运行面**成立，**不能**当作「本次源码改动已活体生效」的证据；源码改动的活体生效要主会话收口（静态/动态编译 + 发布或热接入）后才有。
2. **未跑编译、未提交**：按任务书纪律，`静态编译` / `动态编译` 与 git 提交留给主会话；本次只做了**只读导入核对**与**门禁单点实跑**（不是测试入口，未走旁路）。
3. **反向样本锚点未动**：`测试中心/运行核心/测试_网关兼容性.py` 的 3 个反向样本分别锚 `近似名阈值 = 0.34`、`_被忽略参数` 的列表推导原文、` and 键 not in 横切参数名`——三处文本本次**逐字保留**（唯一节点只把 difflib 调用抽成函数，阈值仍是模块常量），故该测试文件**一行未改**。
4. **`漂移判定.py` 是脏基线**：开工前 `git status` 已显示它在改（他人会话），我只加了 3 处（真源 import / 说明措辞 / 失败文案），该文件其余 hunk **不是本次产物**。
5. **未销账**：`开发文档/未完成事项.md` 的 `P-2`（本批正题）与 `U-8③ 类型短名映射 3 份` 仍挂着；按「条目修好就直接删除」应由主会话收口时一并销账（本次**未动该文件**，避免与主会话并发冲突）。
6. **本批范围外的同类项（仍在账）**：`U-8` 其余各项（正式类型表判定 5 处 / 说明书对账 2 份 / 场景 `$动态` 2 份 / 包必备件 3 份 / 存量基线骨架 5 份）未动；`子代理MCP失败归因` N1（能力 id 近似提示）走的是检索腿，与本次相似度节点**不是同一判据**，未动。
7. **类型短名表只减不增**：`历史短名到正式名` 新增键等于放宽历史包袱，须先有裁决（已写进新文件 docstring）。

## 四、改动文件清单（16 个，全路径）

```text
公共契约/基础类型/历史短名.py                                    （新建，唯一真源）
公共契约/能力契约/契约.py                                        （口径措辞）
运行核心/统一网关/协议/类型规格.py                               （落点 1 / 5 / 6）
运行核心/统一网关/核心处理/网关参数校验面.py                     （落点 2 / 4 / 5）
运行核心/统一网关/核心处理/网关执行面.py                         （落点 3 / 6 导入面）
运行核心/统一网关/核心处理/网关幂等面.py                         （落点 6 导入面）
运行核心/统一网关/核心处理/网关处理面.py                         （落点 6 导入面）
运行核心/统一网关/网关核心.py                                    （落点 6 导入面）
运行核心/统一网关/传输/流式路由面.py                             （落点 5 口径措辞）
开发工具/契约编译/能力定义编译器.py                               （落点 6）
开发工具/契约编译/漂移判定.py                                    （落点 6 连带）
开发工具/快速编译/运行快速编译.py                                 （落点 6）
开发工具/发布门禁/运行发布门禁_防回潮公共.py                      （落点 6 连带）
开发工具/发布门禁/运行发布门禁_防回潮判据.py                      （落点 6 连带，仅注释指针）
开发文档/规范/网关调用最小示例.md                                 （落点 5 权威正文）
开发文档/当前流程图.md                                            （落点 5 权威正文）
```
