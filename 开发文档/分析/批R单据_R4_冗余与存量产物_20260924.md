<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->
# 批R · R-4 冗余与存量产物清理（R5 僵尸目录 + P-14 观察项 + P-11 存量产物）

- 开工ID：`开工-20260924-105516-48ee`｜日期：2026-09-24｜本文件由本次开工独占
- 租约id清单（8 条）：`9b240485d0bc4e3d`、`2c92be8502ab4440`、`a224b8565df64d42`、`c0f7030a724144be`、`fdc74cf915fd493f`、`914f4607d00d42c6`、`4cb894db6c174fdc`、`f40a00963e3343e3`
- 华哥裁决原话：「如果是用不上的就删了，如果是两条腿的就合并成一条，具体你来裁决。」
- 本批纪律：**不跑编译、不 git 提交**；改文件走 `文件系统支持库.文本补丁.批量应用精确替换`，删目录走 `文件系统支持库.文件操作.删除目录`
- 上游：`开发文档/分析/批P清单_20260924.md`（P-11 / P-14）＋ `开发文档/分析/批Q清单_20260924.md`

## 〇、结论一张表

| 块 | 项 | 定性 | 动作 |
|---|---|---|---|
| ① | `开发工具/说明书生成/`（R5 僵尸目录） | **用不上 ⇒ 删** | 删目录（实测 5 文件 / 2 目录） |
| ②-1 | `发布门禁143项基线_20260918.json` | 真零消费方 ⇒ **保留 ＋ 标注「存档，非判据」** | 包成 `{说明, 基线}` |
| ②-2 | `底座升级/B0-浏览器自动化基线.json` | 真零消费方 ⇒ **保留 ＋ 标注「存档，非判据」** | 加首键 `说明` |
| ②-3 | `性能基准/结果/基准结果-20260918-182155.json` | **批P 误判为孤儿**：有活消费方，且属实测证据 | **不改** |
| ②-4 | `说明书白名单.json` 的 `计数` 段 | 零消费方 ＋ 可导出事实重复 ＋ 逐版漂移 ⇒ **删段** | 删 `计数` 段 |
| ②-5 | `说明书权威分工与维护规约.md:112-113` 注脚 | ②-4 的**悬空注脚**（同批收口） | 删 2 行 |
| ③-1 | `技能库/技能/能力上架自检/验证声明.json` | 保留（六件套必备件；绝对写法是 P-13 的下游） | **不改**，交 Q-d |
| ③-2 | `支持库/适配层/Swift编译器提供者/复用决策.json:68` | 保留（`根目录` ∈ 基准路径字段名 ⇒ 写绝对即规范） | **不改** |
| ③-3 | `支持库/适配层/浏览器自动化提供者/验证场景引用.json:18` | 保留（真值断言）⇒ **须裁决** | **不改** |
| ③-4 | `开发工具/性能基准/结果/基准结果-20260918-182155.json` | 同 ②-3 | **不改** |
| ③-5 | 3 处 `cd` 示例 | 机器绝对路径 ⇒ **改写成 `<项目根>`** | 3 处改写 |
| ③-6 | `开发工具/git钩子/pre-commit:86` | **批P 误判为 cd 示例**：实为事故取证原文引语 | **不改** |

## 一、R5 僵尸目录：`开发工具/说明书生成/` ⇒ 删

**定性：用不上 ⇒ 删。** 判据三条，全部实测：

1. **真实现已迁走**：该目录下 `find -name '*.py' | wc -l` = **0**；真实现住 `开发工具/MD文档生成/说明书/`（`说明书生成器.py`/`完整性摘要生成器.py`/`运行生成.py`/`合并重生成.py`/`类型入口.py`/`载位同步.py`/`__init__.py`）。
2. **零消费方**：`rg "说明书生成"`（排除 `工程缓存`/`.git`/`__pycache__`）全仓命中里，**没有一处**以 `开发工具.说明书生成` / `开发工具/说明书生成` 作为读取点 —— 命中全是 ① `开发工具/MD文档生成/说明书/`、② `说明书生成器` 这个**类名/生成器名**、③ `开发文档/方案|归档` 里的**历史记述**（如 `说明书存量治理施工表_20260918.md:56` 的旧 import 示例、`人工编辑.py:12` 记「一次目录改名 `开发工具/说明书生成/` → `开发工具/MD文档生成/说明书/`」）。
3. **进不了版本库**：`git ls-files 开发工具/说明书生成/` 为空（未跟踪），且 `__pycache__/` 在 `.gitignore:2`。残留物只有 5 个 `*.cpython-314.pyc`（源文件已不存在的陈旧字节码）。

**改前 / 改后读数**

```
改前：ls -la 开发工具/说明书生成/  ⇒ 只有 __pycache__/（0 个 .py）
       find … -type f           ⇒ 5 个 .pyc：说明书生成器 / 完整性摘要生成器 / 运行生成 / 合并重生成 / __init__
改后：删除目录 返回 {值:true, 删除文件数:5, 删除目录数:2}
       ls 开发工具/说明书生成/  ⇒ No such file or directory（test -d ⇒ 已删除）
       ls 开发工具/MD文档生成/说明书/ ⇒ 7 个 .py 原样在位（真实现未受影响）
```

## 二、P-14 观察项

### 2.1 孤儿基线 3 个 —— 复核结果与批P 不一致

复核命令（**只读**）：

```
rg -n "发布门禁143项基线"   --glob '!工程缓存/**' --glob '!.git/**' --glob '!**/__pycache__/**' .
rg -n "B0-浏览器自动化基线"   （同上）
rg -n "基准结果-20260918-182155" （同上）
```

| 文件（真实路径） | 复核结论 | 处置 |
|---|---|---|
| `开发文档/项目证据/发布门禁143项基线_20260918.json` | **真零消费方**：命中只剩本批上游单据 `批P清单:138` 与历史取证单 `20260918批证真_20260922.md:59`（都在 `开发文档/分析/`，非读取点） | 保留 ＋ 标注存档 |
| `开发文档/项目证据/底座升级/B0-浏览器自动化基线.json` | **真零消费方**：命中只有 `批P清单:138` 一处 | 保留 ＋ 标注存档 |
| `开发工具/性能基准/结果/基准结果-20260918-182155.json` | **不是孤儿**：`开发工具/性能基准/说明/性能基准说明.md:8` 明写「**仓内只保留一份对照锚点**：`结果/基准结果-20260918-182155.json`」，`:32` 写「其余不入库」，`:117` 给 `--对比 开发工具/性能基准/结果/基准结果-20260918-182155.json` 的调用示例 ⇒ **有指定用途的活锚点** | **不改**（并更正批P 的「孤儿」判断） |

**②-1 改前 / 改后读数**

```
改前：顶层类型 = list，84 项（通过 76 / 不通过 8）；文件无「说明」字段可挂（裸数组）
改后：顶层类型 = dict，键 [说明, 基线]；基线仍 84 项（数值一字未动）
```

**②-2 改前 / 改后读数**

```
改前：顶层键 [任务, 开工id, 项目, 项目根目录, …]，无「说明」
改后：顶层键 [说明, 任务, 开工id, 项目, …]（只增首键，原键与值一字未动）
```

> **形状说明（②-1）**：原文件是裸数组，JSON 数组挂不了字段。为让「存档，非判据」随文件走，把外层包成 `{说明, 基线}`。**这一条属形状变更**，判据是「该文件全仓零消费方（见上表）⇒ 无读取方会因此改变行为」。若华哥要求保持裸数组，等价选项只有「删文件」或「改名加前缀」，见 §四 裁-C。

### 2.2 `说明书白名单.json` 的 `计数` 段 ⇒ 删段（一份事实源）

**定性：删段。** 判据五条：

1. **零机器消费方**（实测）：全仓读该文件的只有 4 处，且**都只读 `白名单` 数组**，无一处读 `计数`：
   - `开发工具/发布门禁/运行发布门禁_防回潮判据.py:585`（`校验说明书白名单联动`，只取 `["白名单"]` 的 `路径`）
   - `开发工具/契约编译/生成物一致性检查.py:177`（`说明书白名单()`，只取 `["白名单"]`）
   - `开发工具/MD文档生成/说明书/载位同步.py:280`（只读 `类别`/`处置`）
   - `测试中心/开发工具/测试_合并重生成反向验证.py:290,351`（只取 `["白名单"]`）
2. **是可导出事实的重复登记**（＝第二事实源）：`计数.白名单合计` 恒等于 `白名单` 数组长度（逐版取证见下表）；`计数.人工类` 是数组里 `类别==人工类` 的条数。
3. **每一版都在漂移**（`git show <提交>:开发文档/项目证据/说明书白名单.json` 逐版实测）：

```
提交        数组条数  计数.白名单合计  计数.人工类  明细.人工类
9d9b6cdc      34          34            —            —
5ee70bba      34          34            26           25
f1f650a5      41          41            26           25
c406ab9c      42          42            26           25
252c1048      42          42            26           25
e3eee120      42          42            26           25
ec661880      42          42            26           24
605d55d4     149          42 ← 未同步   133          131
4debf04b     149          42            133          130
HEAD(改前)   149          42            133          130
```

   `批B`（`605d55d4`「说明书白名单补登 107 份形态漂移（42 → 149 条）」）把数组 42→149，`计数.白名单合计` 留在 42；`计数.人工类` 从 `5ee70bba` 起就与明细差 1，此后**从未对齐**。
4. **没有生成器**：全仓无任何写该文件的代码（`rg` 无写入点）⇒ `计数` 全靠手改 ⇒ 必然继续漂移。「由明细算」需要新建一条生成器写盘 = 再起第二套实现，且绕开单腿写入腿，**不可取**。
5. **该段自述的真源不在本文件**：`唯一真源` 字段写「`开发文档/规范/新建支持库与模块指南.md` 第九节」，而该节 §九 开头明写「**这一节是包级 `说明/使用说明.md` 的唯一规范处，别处不得复述**」⇒ `计数` 段里的口径正文（`G1口径说明`/`G3G4G5G9收口`/`R6判据同源` 等）本身即「别处复述」。

**改前 / 改后读数**

```
改前：1211 行 / 106063 字节；顶层键 7 个 = [账本, 口径日期, 唯一真源, 血统判据, 计数, 铁律, 白名单, G1复核结论]
      计数.白名单合计 = 42   vs  明细实测 149
      计数.人工类     = 133  vs  明细实测 130
改后：1186 行 / 101898 字节（Δ −25 行 / −4165 字节）；JSON 有效
      顶层键 = [账本, 口径日期, 唯一真源, 血统判据, 铁律, 白名单, G1复核结论]
      白名单 条数 = 149（未动）｜铁律 条数 = 5（未动）｜血统判据 5 键（未动）
```

> **被删内容可原样取回**（未在本文复述 4 KB 正文，避免再造第二份事实源）：
> `git show HEAD:开发文档/项目证据/说明书白名单.json | sed -n '12,36p'`

### 2.3 连带收口：`说明书权威分工与维护规约.md` 的悬空注脚

`开发文档/规范/说明书权威分工与维护规约.md:113` 原文：

> 该文件新版 `计数` 段还新增「生成物类含人工改写行 18」「人工类 26」等细分口径——**与本文 7.1 的生成物类 116／人工类 25 属不同切法**，引用时必须写明用的是哪一套口径（血统判据口径见第二节）。

该注脚**整条只为警告 `计数` 段那套「第二口径」而存在**；`计数` 段一删，它即成悬空引用。⇒ 同批删掉（`应用精确替换` 返回 `删除行数: 2`）。该文件同处的另一条注脚「条数会变（实测）…引用前必须重读该文件取实时值」**保留** —— 它说的是 `白名单` 数组本身，仍然成立。

> 该文件属 `机器管理｜类型：规范｜生成器：开发工具.MD文档生成`，本次**追加认领**了它的租约后才动（见「租约」行）；写入经 `批量/应用精确替换` 唯一落盘点，已按口径登记 md 写入凭据（`开发文档/项目证据/MD写入流水.jsonl` 自动追加，非手工编辑）。

## 三、P-11 存量产物（8 份）逐条定性

| # | 文件:行 | 改前内容 | 定性 | 判据 |
|---|---|---|---|---|
| 1 | `技能库/技能/能力上架自检/验证声明.json:7,20,45,58` | `包目录` = `/Users/hekunhua/Documents/Agent/PHP/系统工程平台/技能库/技能/能力上架自检/验证夹具/齐备包`（`参数` 与 `预期` 各 2 处） | **保留**（跨批依赖） | ① 它是技能包**六件套必备件**（`技能库/后端/技能库/实现/技能库.py:55` 的 `技能包必需文件`），删不得；② 脚本 `scripts/能力上架自检.py:148` 是 `Path(包目录).expanduser()` —— **按进程 cwd 解析相对写法**，`:160` 又把 `包目录` 原样回显进 `预期`（`预期.包目录 == 参数.包目录` 恒等）。这正是 **P-13**（技能侧路径口径与已裁口径冲突，派给 Q-d）的落点；③ 该技能的 `SKILL.md`/`契约.json`/`scripts/能力上架自检.py` **本批正在被并行会话修改**，且新契约文案已写「技能脚本当前按进程 cwd 解析相对写法 ⇒ **传绝对路径最稳**」——此时改成相对，等于与在改的契约对撞、且用例会变成 cwd 依赖的假绿 |
| 2 | `支持库/适配层/Swift编译器提供者/复用决策.json:68` | `"根目录": "/Users/hekunhua/Documents/Agent/PHP/系统工程平台"` | **保留** | ① `根目录` **在** `公共契约/基础类型/字段名册.py:275` 的 `基准路径字段名` 集合里 ⇒ 按已裁口径「基准类入参写**绝对路径**」，此处写绝对**正是规范写法**；② 它是 `重建索引的正确调用` 的调用示例，该文件自述「本文件是**设计期决策记录，不参与运行时**」；③ 非生成侧注入（P-9 那根轴管的是生成器渲染，不管手写决策记录） |
| 3 | `支持库/适配层/浏览器自动化提供者/验证场景引用.json:18` | 断言期望值 `"命令": ["/Users/hekunhua/.local/bin/uvx", "browser-use"]` | **保留 ⇒ 须裁决**（裁-A） | 这是 `浏览器自动化.检查可用` 的**真实返回断言**（`Path.home()` 推导的真值），换用户名即不等；要可移植必须新增 `$动态` 类型，而 `开发工具/HTML验证/路径安全.py:121-128` 的 `允许字段` 只有 6 类（制品根/受管临时目录/受管临时路径/夹具文件复制/步骤返回/环境变量）⇒ **扩允许表 = 口径变更**，按本批约束「不要硬改」，单列须裁决 |
| 4 | `开发工具/性能基准/结果/基准结果-20260918-182155.json` | 19 处运行数据根绝对路径 | **保留** | 属**实测证据**（同一结论见批P 表内「不改」），且是性能基准说明指定的唯一对照锚点（见 §2.1） |
| 5 | `开发工具/消费者登记/__init__.py:21` | `cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH` | **改写** | 文档示例 ⇒ 去机器绝对路径 |
| 6 | `开发工具/验证门禁/接线说明.md:11` | `cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台` | **改写** | 同上 |
| 7 | `运行核心/加载器/版本系统/说明/灰度口径.md:112` | `cd "/Users/hekunhua/Documents/Agent/PHP/系统工程平台"` | **改写** | 同上（该处小节标题本就写「可复跑核对（**在项目根执行**）」） |
| 8 | `开发工具/git钩子/pre-commit:86` | `#   执行：`line 40: 根=/Users/…: No such file or directory`，紧接着 `set -u` 下` | **保留** | **批P 把它归入「cd 示例」是误判**：该行是 **2026-09-23 实测事故的逐字引语**（原文是 bash 3.2 把 `/Users/…` 吞进变量名的报错行），且已是 `…` 省略形态、**不含完整机器绝对路径**。改写引语＝篡改取证原文。该文件里唯一另一处相关写法是 `:21` 的 `<仓库根>`（已是占位写法） |

**③-5 改写口径（3 处统一）**

`cd` 的目标是「项目根」，而**基准路径字段名**的正规名就是 `项目根` ⇒ 统一写成占位形式 `cd <项目根>`。这与本仓既有写法同族，未造第二套措辞：`sitecustomize.py:36-45` 的 `cd <仓库根>`、`开发工具/测试体系门禁实现/导入探针.py:14` 的 `--根 <仓库根>`、`开发工具/契约编译/聚合契约解析.py:99` 与 `开发文档/规范/任务类型规范.json:38` 的 `<项目根>`。命令本体（`python3.14 开发工具/…`）本就是**项目根相对路径**，一字未动。

```
改前 → 改后
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH   →   cd <项目根> && unset PYTHONPATH
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台                        →   cd <项目根>
cd "/Users/hekunhua/Documents/Agent/PHP/系统工程平台"                      →   cd <项目根>

复测：rg -n "/Users/hekunhua" 这三个文件 ⇒ 零命中
      其余 3 份（③-1/2/3）机器绝对路径**原样保留**（见上表判据）
```

## 四、须裁决（本批**未动**，给判据）

| 编号 | 冲突点 | 为什么不能自裁 | 可选修法 |
|---|---|---|---|
| **裁-A** | `浏览器自动化提供者/验证场景引用.json:18` 的断言值写死本机 `uvx` 绝对路径，换机即判红 | 要放宽就得给 `开发工具/HTML验证/路径安全.py:121-128` 的 `允许字段` **加一类 `$动态`**（如「本机命令路径」）；`$动态` 是**场景 schema 的允许表**，扩表 = 口径变更（同批约束明写「不要硬改」） | ① 扩 `$动态` 允许表（口径变更，须批）；② 断言降到只校 `命令[1] == "browser-use"` 与 `命令` 长度（**松开断言**，同样须裁）；③ 维持现状（本机真值，制品换机时该断言本就该红） |
| **裁-B** | `技能库/技能/能力上架自检/验证声明.json` 的 `包目录` 绝对 vs 相对 | 它是 **P-13** 的下游：脚本按 cwd 解析（`scripts/能力上架自检.py:148`），且该技能正被并行会话改（新契约文案「传绝对路径最稳」）⇒ 本批改会与在改的契约对撞 | 等 Q-d 把脚本改成按生效根归一后，**同批**把 `参数`/`预期` 两处一起改项目根相对（两处强耦合，必须同改） |
| **裁-C** | `发布门禁143项基线_20260918.json` 的形状（裸数组 → `{说明, 基线}`） | 数组挂不了标注字段；改成对象是**形状变更**（虽然实测零消费方） | ① 接受本批的包壳写法；② 删文件（用不上 ⇒ 删）；③ 改名加 `存档_` 前缀（改路径，需另批） |

## 五、本批落地清单（8 条路径）

```
删除：开发工具/说明书生成/                      （5 文件 / 2 目录；含 __pycache__，git 未跟踪且已 gitignore）
改写：开发文档/项目证据/说明书白名单.json        （−25 行：删「计数」段）
改写：开发文档/项目证据/发布门禁143项基线_20260918.json（+说明，包成 {说明, 基线}）
改写：开发文档/项目证据/底座升级/B0-浏览器自动化基线.json（+首键「说明」）
改写：开发工具/消费者登记/__init__.py            （cd 示例 → <项目根>）
改写：开发工具/验证门禁/接线说明.md              （cd 示例 → <项目根>）
改写：运行核心/加载器/版本系统/说明/灰度口径.md  （cd 示例 → <项目根>）
改写：开发文档/规范/说明书权威分工与维护规约.md  （−2 行：删指向已删「计数」段的悬空注脚）
新增：开发文档/分析/批R单据_R4_冗余与存量产物_20260924.md（本文件）
```

`git diff --stat`（本批路径，**未提交**）：

```
 开发工具/消费者登记/__init__.py                    |  2 +-
 开发工具/验证门禁/接线说明.md                      |  2 +-
 开发文档/规范/说明书权威分工与维护规约.md          |  2 --
 开发文档/项目证据/发布门禁143项基线_20260918.json  |  7 ++++--
 开发文档/项目证据/底座升级/B0-浏览器自动化基线.json |  1 +
 开发文档/项目证据/说明书白名单.json                | 25 ---------------------
 运行核心/加载器/版本系统/说明/灰度口径.md          |  2 +-
 7 files changed, 9 insertions(+), 32 deletions(-)
```

> `开发文档/项目证据/MD写入流水.jsonl` 同时有增量 —— 那是**写入腿按口径自动登记的 md 写入凭据**（本批 3 个 `.md` 落盘各一条），不是手工编辑；该文件在本次开工前已由并行会话修改。

## 六、本批**未**做的事（诚实交代）

- **没跑编译**（静态/动态编译都没跑）、**没 git 提交** —— 按本批纪律。
- 没动任何**未认领**的路径：并行会话正在改的 `技能库/技能/能力上架自检/{SKILL.md,契约.json,scripts/能力上架自检.py}`、`测试中心/公共契约/测试_仓库只读锁.py`（其 `:456` 的 docstring 里提到 `开发工具/说明书生成/`，属**历史举例**，未改）。
- ②-1 的形状变更（裁-C）与 ③-1/③-3 的保留（裁-A/裁-B）都是**有判据的保留/最小改动**，不是漏做。
