# 20260918 批审计件只读证真 · 总表与逐条结论

> 证真方法：照 `开发文档/未完成事项.md` §一.1 与 §七 开头口径 —— 一律读提交版本核验条目前提（`git show HEAD:<文件>`，不读工作树）、
> 每条结论带现场证据（文件:行 + 真实命令输出）、旧快照行号会漂故以真代码内容定性、分「仍成立 / 已消解 / 不是缺陷」三类。
> **核验基准：HEAD = 925578278be8ad9b0de2b3b0e156daae614543d6（2026-09-22）。**
> 核验期间有并行会话在推进 #184（约 300 份 `说明/*.md` 被加机器印记）与 #67（新增 `开发工具/规则自检.py`），
> 该等改动非本任务所为，本任务全程只读（未改项目内任何文件；临时产物在 `/tmp` 与 scratch 目录）。
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->


## 一、总表（逐件三类 + 仍成立分档）

| 批次 | 审计件 | 仍成立·真缺陷 | 仍成立·数字过期 | 仍成立·建议未落地 | 仍成立·断言为真非缺陷 | 已消解 | 不是缺陷 | 合计 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 0 | 批A · 深度审计_底座升级清单_20260919.md | 13 | 2 | 9 | 16 | 8 | 7 | 55 |
| 1 | 批B · 生成物地图_20260918.md + 说明书混血包行级对账_20260918.md | 2 | 11 | 1 | 5 | 15 | 1 | 35 |
| 2 | 批C · 端到端背压核对_20260918.md + 配置双入口收口_20260918.md | 42 | 0 | 1 | 21 | 6 | 1 | 71 |
| 3 | 批D · 测试中心归口盘点_20260918.md + 超长文件拆分候选_20260918.md + 缺陷热点巡诊_结论_20260919.md | 6 | 1 | 3 | 3 | 18 | 4 | 35 |
| 4 | 批E · 能力面与拆分清单_20260918.md + 备份恢复演练实测_20260918.md | 1 | 12 | 0 | 0 | 6 | 2 | 21 |
| 5 | 批F · 子代理探索耗时归因 + 开工上下文检索_归因与方案 + 新建支持库回填链路归因 + DGX-Spark本地算力视频（四件） | 1 | 0 | 0 | 0 | 17 | 8 | 26 |
| **合计** | 14 份件 | **65** | **26** | **14** | **45** | **70** | **23** | **243** |

> **仍成立 总数 = 150**（真缺陷 65 + 数字过期 26 + 建议未落地 14 + 断言为真非缺陷 45）。
> 「断言为真非缺陷」一档是**防误修**用：报告该条断言经复核确实为真，但它描述的是正确现状/可用件/已落地动作，不构成待办。

## 二、逐条结论（全量）

### 0 · 批A · 深度审计_底座升级清单_20260919.md

#### 【仍成立·真缺陷】13 条

**1. 警告1 债族清单漏登记两个测试夹具 + 验收判据永不真零**

- 现场证据：`rg -n 'CAPABILITY_NOT_FOUND|CALL_TIMEOUT|CALL_FAILED' 测试中心/平台控制面/测试_工作包11_消费者契约注册表.py` → :26 `["CAPABILITY_NOT_FOUND","CALL_TIMEOUT"]`、:55 断言、:81/:82/:86 CALL_TIMEOUT/CALL_FAILED；`rg -n 'CAPABILITY_NOT_FOUND' 测试中心/平台控制面/测试_工作包02_调用Agent视图.py` → :6/:125（注释）。`git show 92557827:开发文档/方案/类型体系收口盘点_20260917.md | grep -n EC-4` → :170 清单为『开发工具/统一能力入口/视图/*.py（4 文件）、平台控制面/需求登记/需求错误码.py』= 5 文件，未列两个夹具。全仓判据 `rg -n 'CALL_FAILED|…|REQUIREMENT_UNCONFIRMED' -g '*.py' -g '!工程缓存/**'` 仍命中视图别名表 + `开发工具/发布门禁/运行发布门禁_防回潮公共.py:81-82` 白名单 + 两夹具。
- 期望修法：期望修法（可直接粘进债务清单）：① EC-4 收口波次文件清单由「5」补为「5+2」——`测试中心/平台控制面/测试_工作包11_消费者契约注册表.py`、`测试中心/平台控制面/测试_工作包02_调用Agent视图.py`（`签名校验.py` 已自行消解，不再列）；② 两夹具英文样例改中文；③ 验收判据显式写「除视图别名表与 `运行发布门禁_防回潮公共.py` 白名单外命中 0」，否则判据永远假红。

**2. 警告2 测试中心 22 文件深入实现导入（49 处），豁免口径待裁决**

- 现场证据：`rg -l "^from 支持库\.[^ ]*\.实现|^import 支持库\.[^ ]*\.实现" 测试中心/ | wc -l` → **26**（报告 22）；同式 `| wc -l` 行数 → **37**（报告 49）。`grep -n 调用方样板 /tmp/AGENTS.md` → :193『示例项目与测试代码作为调用方样板，允许经包级公开入口调用（与调用者条款一致）』；`git show 92557827:开发文档/规范/新建支持库与模块指南.md | grep -n 提供者级单测` → 无命中。
- 期望修法：灰色地带原样存在。期望修法（二选一，裁决前不动代码）：① 在 `开发文档/规范/新建支持库与模块指南.md` 明示「提供者级单测允许 import 同包实现，跨包仍禁止」，并把文件数基线改为现场值 26 文件/37 处；② 迁到公开入口，判据改为 0。

**3. 警告3① AGENTS.md 目录约定未列 技能库/**

- 现场证据：`grep -c "技能库" /tmp/AGENTS.md`（HEAD 版本）→ **0**；`grep -c "技能库" /tmp/项目说明.md` → **10**；`ls -d */` → 17 个顶层目录含 `技能库/`。
- 期望修法：期望修法：AGENTS.md「目录约定」段补 `技能库/`（这正是 AGENTS.md 自己『与项目说明不一致即同任务修复』条款）。判据：`rg -c "技能库" AGENTS.md` > 0。

**4. 警告3② 运维脚本/ 全仓文档 0 提及**

- 现场证据：`grep -c "运维脚本" /tmp/AGENTS.md` → **0**；`grep -c "运维脚本" /tmp/项目说明.md` → **0**；`ls -d */` 含 `运维脚本/`。
- 期望修法：期望修法：`运维脚本/` 要么在 AGENTS.md 目录约定登记，要么并入 `开发工具/`。判据：`ls -d */` 每一项都能在 AGENTS.md 目录约定或其明示豁免里找到。

**5. 观察2 6 个聚合支持库豁免未写进「每个支持库必备」**

- 现场证据：`rg -l "聚合支持库" 支持库/后端/*/包声明.json` → 系统核心/大语言模型/数据操作/文件系统/网络通信/办公文档 共 6 个；`ls 支持库/后端/系统核心支持库/` 无 `能力契约/`、无 `验证场景引用.json`（六个包同形）；`grep -n 聚合 /tmp/AGENTS.md` → 仅 :389 摘要级联，无豁免句；「每个支持库必备」段列 包声明.json/能力契约//实现//说明//验证场景引用.json/完整性摘要.json/__init__.py。
- 期望修法：期望修法：在 AGENTS.md「每个支持库必备」段补一句「聚合父包豁免 `能力契约/` 与 `验证场景引用.json`（子域持有）」，与适配器豁免同款。

**6. 观察4 项目证据目录里有 1 份 md**

- 现场证据：`ls -la 开发文档/项目证据/` → `能力id冻结基线_交付说明.md` 5312 B 仍在（另含 MCP使用反馈.jsonl / 发布门禁143项基线_20260918.json / 能力id冻结基线.json / 说明书白名单.json / 验证历史.jsonl / 底座升级/）。
- 期望修法：期望修法：移 `开发文档/交付记录/`，或按「结论并入即删」处理。

**7. 扩容6 说明书行级合并工具前置（施工表 G3/G4/G5/G9 四格行级合并）**

- 现场证据：`ls 开发工具/说明书生成/` → 只剩 `__pycache__`（报告路径已失效）；`find . -name '合并重生成.py' -not -path './工程缓存/*'` → `开发工具/MD文档生成/说明书/合并重生成.py`；`git show 92557827:开发文档/未完成事项.md | grep -n 193\|210` → #193『行级配对缺「小节作用域」…**当前处置**：本轮**未改生成器**』、#210『同键配对缺「小节作用域」，历史态说明书残余超配对…**待修**』。
- 期望修法：工具已存在（含逐行配对 + 超配对 fail-closed 拒写），但行级缺陷仍在债务清单（#193/#210 待修），『新包默认走生成物类骨架 / 存量四格用工具吃掉』未见强制。期望修法：先修 #193/#210 的小节作用域配对，再把「新包默认生成物类骨架」写进新建指南判据；报告引用的 `开发工具/说明书生成/` 路径需改为 `开发工具/MD文档生成/说明书/`。

**8. §七 该补充的 6 件·② 门禁三拍自证账本**

- 现场证据：`ls -la 开发文档/项目证据/验证历史.jsonl` → 228400 B 存在；读方 `rg -l "验证历史" -g '*.py' -g '!工程缓存/**'` → `开发工具/发布门禁/发布治理.py`、`模块库/开工编排/实现/开工编排.py`、`模块库/能力目录/实现/能力目录.py`、`模块库/能力目录/实现/能力索引.py`、`技能库/技能/交付收尾/scripts/交付收尾.py`。
- 期望修法：『已有读方』成立；仍缺「三拍入账 + 新门禁必带」判据。期望修法：给 `验证历史.jsonl` 补「三拍入账」写入腿与新门禁准入判据——**勿另建第二本账**。

**9. §七 该补充的 6 件·④ 注册快照**

- 现场证据：`grep -n "\-\-冻结" 开发工具/能力id冻结基线门禁.py` → `:44 --冻结 显式重建/冻结基线`、`:6 口径（唯一权威 = 装配口径）`；全仓 `rg -l "注册快照"` → 0 命中。
- 期望修法：『部分有：`能力id冻结基线门禁 --冻结` 即装配口径导出，缺参数形状字段 + 漂移检测不读快照』现场成立。期望修法同 扩容3。

**10. §七 该补充的 6 件·⑥ 容量/性能回归基线**

- 现场证据：`ls 开发工具/性能基准/` → `运行基准.py` 16816 B、`结果对比.py` 4877 B、`基准场景.py`、`基准口径.py`、`反向验证.py`、`基准环境.py`；`grep -rn "阈值|准入" 运行基准.py 结果对比.py` → **0 命中**。
- 期望修法：『部分有：运行基准 --对比 + 结果对比.py 现成，缺准入阈值判定 + 常态化接线』现场成立。期望修法：补准入阈值判据并接进收口/发布门。

**11. §九 9.1 待第二批：派活任务信自动携带编译口命令 + 开工准备「改完必做」同步**

- 现场证据：`grep -n 开发编译口 开发工具/任务记忆/派活任务信.py` → `:191 | 改过 开发工具/、运行核心/、公共契约/ | python3.14 -B -m 开发工具.开发编译口.编译口 --文件 <你改的全部文件> |`（命令表静态注入，半条已落地）；`grep -c 开发编译口 模块库/开工编排/实现/开工编排.py` → **0**；同文件 `:403` 记『命令环境/工具姿势/提交纪律/改完必做 四类条款此前只写在派活方手写的任务信里』、`:576`『无条件返回：哲学纪律 / 命令环境 / 工具姿势 / 提交纪律 / 改完必做 / 效率达标线 / 派活纪律』（段内无编译口）。
- 期望修法：期望修法：在 `模块库/开工编排/实现/开工编排.py` 的「通用纪律.改完必做」补编译口命令（运行时侧改动后热接入）；任务信那半条已落地，可从待办划掉。

**12. §九 9.4「本轮已落地 vs 待第二批」状态表**

- 现场证据：独立复核（不采信自述）：① 编译口最小可用（--变更/--文件/--全仓/--自证/--联动，退出码 0/1/2，五态）→ 基准版本 `:501-505` 参数齐备、`:13 退出码：0 干净通过…1 失败…2 未核验`、五态结论在 docstring 与实现一致 ⇒ **真**；② AGENTS.md 五处 → 逐处 sed 核对 ⇒ **真**；③ 反向验证 + 三拍自证 → 本任务实跑 `--自证` 0.343s 退 0 ⇒ **真**；④ 漂移检测升阻断（存量/新增两桶）→ 实跑 `漂移检测 --报告` → `判红 0 条（默认值硬 0／必填硬 0／新增漏声明 0）` + `只报 存量漏声明 93 条`，退出码 0 ⇒ **真**；⑤ 派活任务信自动携带编译口命令 → **半落地**（派活任务信.py:191 有，开工编排 0 命中）；⑥ 变异样本库 + --自证 跑全量样本 ⇒ **未落地**；⑦ 踩坑命中 ≥2 升级警告 + 判据化闭环 ⇒ **未落地**；⑧ 抽样真跑 ⇒ **未落地**。
- 期望修法：表内四行 ✅ 独立复核全部为真（非空头承诺）；⏳ 四行中一行已半落地。数字更新：漂移检测存量真值为 93 条漏声明 + 260 条实现默认值差异（报告 §8.2 写 126/157，过期）。

**13. §十 10.1 其余共享资源行 + 10.2 派活前三查 + 10.3 暗坑形态学 + 10.4 分级验收**

- 现场证据：共享临时路径 PID 隔离（报告 11.4 待修项）→ `git show 92557827:开发工具/开发编译口/编译口.py | sed -n '463p'` → `坏样本目录 = Path(tempfile.gettempdir()) / f"开发编译口自证_{os.getpid()}"` ⇒ **已落地**；HTML 端口池 → `开发工具/HTML验证/端口池.py:6` 解析 `"45080-45180"` 区间 ⇒ 成立；文件租约 → `rg -l "开工即占|收工释放"` 命中 `模块库/开工编排/实现/开工编排.py`、`平台控制面/能力目录/文件租约存储.py` ⇒ 成立；`grep -n 改动面 开发工具/任务记忆/派活任务信.py` → 仅 :149 入参说明，无「改动面声明」字段与派发前两两比对；`grep -c 开发编译口 模块库/开工编排/实现/开工编排.py` → 0。
- 期望修法：表内两处已落地（临时路径 PID 隔离、租约机制）；仍成立的是：① `派活任务信.py` 未带「改动面声明 + 两两比对」；② 任务信未自动带 `开工即占`；③ 回传未加「影响面自检」栏。期望修法：把「开工即占 + 改动面声明」接进派活任务信生成器，并把「编译口输出的影响面清单 vs 任务信声明的改动面，不一致即当场拦截」写进 10.4 回传模板。

#### 【仍成立·数字过期】2 条

**1. 扩容4 全仓 O(N) 门禁的增量化与基线自动化**

- 现场证据：`head -25 开发工具/全量重算摘要.py` → 『本入口一次扫遍全部正式根，凡「磁盘 ≠ 重算」一律重算』（全仓口径，无包指纹增量分支）；`grep -n 增量 开发工具/全量重算摘要.py` → 0；`python3.14 -c "import json;d=json.load(open('开发文档/项目证据/能力id冻结基线.json',encoding='utf-8'));print(d['能力数'],d['基线时点'])"` → `713 2026-09-20`，而装配实测已 746，基线仍靠人工 `--冻结`。仅『门禁并行化』已落地（`门禁并行路数 = 8`，见 #212④）。
- 期望修法：期望修法（未落地）：① 摘要按包指纹增量（只重算变更子树 + 受影响聚合父包）；② 能力id基线改为「从装配注册表自动导出 + 人工只审增量 diff」，fail-closed 性质不变。数字更新：报告写基线 686→689，真值 713。

**2. §七 该补充的 6 件·③ 测试红接线**

- 现场证据：`开发工具/测试体系门禁.py:19-22` → 『`python3.14 -m unittest <模块>`，`rc≠0` 即不通过…接线时实测 188 个模块里 16 个实跑即红』；`:113 存量真跑红: tuple[str, ...] = (` 与 `:131 **2026-09-17 基线清空（记录）**…故按「只减不增」清空本清单（`存量真跑红` 现为空元组）』；`grep -n "验证历史\|账本" 开发工具/测试体系门禁.py` → **0 命中**。
- 期望修法：『红即阻断 + 基线只减不增』成立；『16 红存量基线』数字过期（现为空元组/0 项）；『缺把结果写进证据账本』仍成立。期望修法：把测试体系门禁结论写进 `验证历史.jsonl`。

#### 【仍成立·建议未落地】9 条

**1. 扩容3 注册事实源单一化：门禁读「注册快照」而不是 AST 追写法**

- 现场证据：`rg -l "注册快照" -g '*.py' -g '*.json' -g '!工程缓存/**'` → **0 命中**；对照 `python3.14 -m 开发工具.契约编译.漂移检测 --报告` 输出仍带『未解析注册 22／参数口径未解析 54』（即 AST 解析面仍存在读不到的情形，靠显式登记而非快照）。
- 期望修法：期望修法（未落地，可直接粘）：装配完成后导出**注册快照**（JSON，带包指纹），编辑期门禁对快照做判据而非对源码做 AST 猜测；与哲学 12.1 同向，可搭 #56 已落地的制品化便车。

**2. 扩容5 sys.path 自举收敛为唯一引导模块**

- 现场证据：`rg -l "引导模块" -g '*.py' -g '!工程缓存/**'` → 0 命中；`rg -n "sys\.path\.(insert|append)" -g '*.py' -g '!工程缓存/**' | wc -l` → 344（报告 587→291→现场 344）。
- 期望修法：期望修法（未落地）：收敛为一个引导模块（如 `公共契约/` 下），全部入口一行接入，预期降到 20 处以内；防回潮判据进门禁「`sys.path.insert` 仅允许出现在引导模块与测试自举」。注意报告 §四 该条正文仍写 587，与观察3 的 291 自相矛盾，应统一为现场值。

**3. 扩容7 目录约定与文档口径的机器对账**

- 现场证据：`rg -l "目录对账" -g '*.py' -g '!工程缓存/**'` → 0 命中；对照警告3 的漂移（技能库/ 未登记、运维脚本/ 0 提及）现场仍在。
- 期望修法：期望修法（未落地）：加只报告不阻断的检查（沿用 `Markdown 文档体例规范.md` 第九节姿势）——`ls -d */` 每一项必须能在 AGENTS.md 目录约定或其明示豁免里找到，`开发文档/` 新目录同理；新目录未登记即报。

**4. 扩容8 网关运行形态演进顺序（#56 制品化 → 0046 槽位 → 0042 多租户 → 0043 L4）**

- 现场证据：`lsof -a -p 10181 -d cwd -Fn | tail -1` → `n/Users/hekunhua/Documents/Agent/PHP/系统工程平台/工程缓存/制品仓库/平台客户端环境/平台客户端`；`ps -o command= -p 10181` → `…平台客户端/运行核心/启动运行核心网关.py` ⇒ **40007 已跑激活制品而非源码树**；`git log --oneline -15 -- 开发文档/未完成事项.md` → `196babb1 …修 #56 切制品引入的运行态分叉`、`5ef55719 登记 #215：#56 切制品的两个前置缺口`、`c4aea5a4 #216② 收口：制品里不再产生第二个模块对象`、`37957f10 #219 防回潮：运行态同根判据…` ⇒ #56 已销号落地；`lsof -nP -iTCP:48001 -sTCP:LISTEN` → 空（开发槽未起）；`head -6 开发文档/决策记录/0042_多租户数据面策略端口.md` → 『本批不落代码』。
- 期望修法：排序建议本身仍成立，且已走出第一步（#56 制品化落地、前置『源码树干净』不再需要）。期望修法：把本条待办起点从「#56」改为「决策 0046 开发槽/稳定槽全面铺开」，再接 0042 多租户、0043 L4；决策 0044 / §8.2.1 #53 的单机边界照旧。

**5. 扩容9 验证场景与真实生产库解耦（记忆支持库加可选 存储目录 参数）**

- 现场证据：`rg -n "存储目录" 支持库/后端/记忆支持库/` → **0 命中**；`ls 支持库/后端/记忆支持库/能力契约/` → 只有 `参数契约.json`；`git show 92557827:开发文档/未完成事项.md` 五、已知覆盖缺口 §6b『记忆支持库的验证场景跑在真实生产库上（读类能力值无法定值）…该包能力契约里**没有 `存储目录` 参数**…须裁决』。
- 期望修法：期望修法（未落地，须先裁决）：按 §8.5-6b 原口径——先裁「对外契约加可选 `存储目录` 参数」（属对外契约面变更）；裁决前用验证场景资源键隔离当临时护栏（已有先例「语音转写模型」键）。

**6. 扩容11 单机资源天花板与重资源能力的资源键准入（现役 8 键 + 写进新建指南 checklist）**

- 现场证据：`sed -n '35,78p' 开发工具/HTML验证/端口池.py` → `_场景资源键()` 现役 **8 键**：`Git仓库 / LibreOffice / 模型连接器 / FFmpeg / 组件控件 / 文件租约 / 提供者注册表 / 语音转写模型`（『现役 8 键』成立）；`git show 92557827:开发文档/规范/新建支持库与模块指南.md | grep -n 资源键` → **0 命中**。
- 期望修法：期望修法（未落地）：在 `开发文档/规范/新建支持库与模块指南.md` 的开工 checklist 补「重资源能力（模型/ffmpeg/大文件转码）必须同步登记验证场景资源键」判据，让护栏随能力生长。

**7. §八 8.5 元门禁 / 8.5 49 条款三分法迁移 / 8.6 新包脚手架**

- 现场证据：`rg -l "元门禁|规则自检" 开发工具/ -g '*.py'` → 基准版本无 `规则自检.py`（工作树已于 2026-09-22 新增，正在收口 #67）；`rg -l "新建包|脚手架" 开发工具/开发编译口/` → 0 命中（`--新建包` 未实现）。
- 期望修法：三项均未落地。期望修法（可直接粘）：① 元门禁——AGENTS.md 与 `开发文档/规范/` 新增规则必须带「判据：」行并指向真实存在的检查器/场景，编译口扫规则条目，无判据即红（这是 OK 判据①「纸面规则 = 0」的机器保障，现纸面规则仅剩 1 条，优先做）；② 49 条款按三分法逐条归位，每条配三拍自证；③ `--新建包 <包id>` 生成全要素骨架，起步即编译口全绿。

**8. §九 9.2 假绿/假红规避（规则 1–6 + 抽样真跑）**

- 现场证据：`git show 92557827:开发工具/开发编译口/编译口.py | sed -n '623,626p'` → `if 退出码 == -1:  # ★ 假绿通道（P1）：未核验与角色无关 —— 只报告类门禁超时/无法执行同样不许放行。  未核验 = True` ⇒ **未核验判断在角色判断之前**；`:634 只报红项.append(f"{门禁['名字']}（退出码 {退出码}）")` ⇒ 只报告类判红不静默丢弃；实跑 `--自证` → `坏样本 py_compile 退出码=1（期望非 0）：SyntaxError` / `编译口本体 py_compile 退出码=0` / `三拍自证通过：绿必绿 / 坏必红 / 恢复归绿`，`real 0m0.343s`，退出码 0；`rg -l 变异样本 开发工具/ -g '*.py'` → 仅 `开发工具/验证_能力id冻结基线反向验证.py`（先例）。
- 期望修法：规则 2（fail-closed）已修完且本任务独立复现三拍通过；规则 1 变异样本常态化、规则 6 豁免审计期、抽样真跑（静态与真实的锚）三项未落地。期望修法：`--自证` 扩展为跑全部变异样本（任一坏样本没变红 = 判据假绿即编译口红）；豁免条目加「为什么不是缺陷」+ 审计期。

**9. §九 9.3 反复犯错/反复探索 → 坑的三级火箭**

- 现场证据：`grep -n "查开工上下文|踩坑|命中次数" 开发工具/任务记忆/派活任务信.py` → 仅 `:156` 描述 `开工即占` 返回值里的『开工上下文』，**无自动调用** `查开工上下文`、无把命中坑原文写进任务信；`rg -n 命中次数 模块库/ -g '*.py'` → 字段已有（报告『踩坑库带命中次数字段』成立）；『命中 ≥2 升级警告』『判据化闭环』未见实现。
- 期望修法：三级均未落地。期望修法（按报告原样，第一级最便宜先做）：① 派活任务信生成时自动调 `查开工上下文(本次任务)`，把命中 3–5 条坑原文写进任务信；② 命中 ≥2 时文案升级为警告并提示决策 0040；③ 命中 ≥2 的坑强制二选一（写成编译口判据 + 变异样本，或自动登记立项补底层）。

#### 【仍成立·断言为真非缺陷】16 条

**1. §一 门禁结果·全仓语法编译 0 失败**

- 现场证据：python3.14 逐文件 ast.parse：`扫描 1214 个 .py，语法失败 0，耗时 12.63s`（避开 py_compile 写 __pycache__）。报告值 py 1003 → 真值 1214（数字过期）。
- 期望修法：机制成立，只需把 1003 改为现场值并注明口径（报告用的是 py_compile，本证真用 ast.parse 以免写盘；两者对语法判定等价）。

**2. §一 门禁结果·环境自检 9 大项全过、跳过包 0**

- 现场证据：`unset PYTHONPATH; python3.14 -m 开发工具.环境自检` → 第 8 项『装配冒烟（后端核心.启动()）【通过】2.11s 装配成功：状态=运行中、能力数=746、跳过包 0 个』；第 9 项『外部命令可用性【警告】11/12 项可用；缺 browser-use』；结论『没有【不支持】项』，退出码 0。
- 期望修法：跳过包 0 成立；『9 大项全过』应改为『9 项：8 通过 + 1 警告（不阻断）』；装配能力 699 → 真值 746（数字过期）。

**3. §一 门禁结果·薄壳接入面 工具数=3**

- 现场证据：`开发工具/薄壳/工具清单.py:1` 『薄壳工具清单：3 个薄壳工具的协议名、中文名、描述、入参与转发目标（唯一元数据来源）』；`:211 "薄壳工具数": len(清单)`。
- 期望修法：无需修。

**4. §一 门禁结果·凭证 fail-closed 无凭证返回 401**

- 现场证据：`curl -sS -m 5 -o /dev/null -w '%{http_code}' http://127.0.0.1:40007/健康` → 401；体 `{"请求id":"b6f6f04aae20480d","操作":"HTTP边界","成功":false,"错误码":"权限不足","错误说明":"访问凭证缺失或无效"}`。
- 期望修法：无需修。

**5. §一 门禁结果·旧接入面下线 8766 / 40006 均无监听**

- 现场证据：`lsof -nP -iTCP:8766 -sTCP:LISTEN` 与 `:40006` 均空；`curl -m 3` → `Failed to connect to 127.0.0.1 port 8766/40006`（HTTP 000）。
- 期望修法：无需修。

**6. §一 规模口径（py 1003 / md 699 / 正式包 142 / 测试模块 215 / 装配能力 699；支持库 123 / 基础模块 11 / 功能模块 8）**

- 现场证据：rg 复算：py **1212**（`rg --files -g '*.py' -g '!工程缓存/**' | wc -l`）、md **741**、包声明 **146**、测试_*.py **240**；按 包声明.json 类型字段分类：支持库 **127** / 基础模块 11 / 功能模块 8（合计 146）；装配能力 **746**（环境自检第 8 项）。
- 期望修法：数字全部过期（py +209、md +42、包 +4、测试 +25、能力 +47）。数字口径『引用前必须重算』本身正确，不算缺陷；引用时按上式现场重跑。

**7. §一 纪律面干净项六项（模块库静态导入支持库 0 / 六英文错误码生产代码 0 / 裸 except 0 / 归档目录 py 0 / sys.path 跨树 0 / 第三方导入 115 处）**

- 现场证据：`rg -c "^(from|import) 支持库" 模块库/ -g '*.py'` → 0 文件；`rg --files 开发文档/归档 -g '*.py' | wc -l` → 0；`rg -n 'except\s*:' -g '*.py' -g '!工程缓存/**'` 命中 5 处，逐条核为**注释行**（『不许 except: pass 吞掉』文案）⇒ 真裸 except 0；sys.path 344 处中不含『根/入径』的 10 行逐条核过（'.'、sys.argv[1]、适配层、门禁目录、注入目录）全在仓库内 ⇒ 跨树 0；第三方导入 12 模块名口径 **95 处**（报告 115）。
- 期望修法：五项成立（两项需注明口径：裸 except 要用 AST 口径、第三方导入要写清模块名集合）；115 处 → 真值 95（数字过期）。

**8. 观察3 sys.path 自举样板 291 处**

- 现场证据：`rg -c "sys\.path\.(insert|append)" -g '*.py' -g '!工程缓存/**' | awk -F: '{s+=$2} END {print s}'` → **344**；`rg -n … | wc -l` → 344。报告值 291 → 真值 344（+53）。
- 期望修法：仍成立（处数继续增长，印证『新包脚手架会继续复制』）。治理见 扩容5。

**9. §五 升级顺序建议（波次0–3 一页纸）**

- 现场证据：`git show 92557827:开发文档/未完成事项.md | head -22` → 待修 0 / 待执行大项 0 / 待裁决 0 / 新增债务 9 / 七 11 / 7.8 1，合计 20 编号条目；波次0 的「观察4 移 md」「观察2 补句」现场均未做（见对应条目）；波次2 的 #55 已消解、#56 已落地、D-1 三对副本已消解。
- 期望修法：波次编排本身仍成立，但波次2 真实剩余量比报告小。期望修法：波次0 保留（观察2 补句 / 观察4 移 md / 警告3 技能库登记）；波次1 的 EC-4 已由 0024 裁决可直接执行；波次2 改为「E-8 类型体系 → 扩容6 行级配对修复 → 扩容3 注册快照搭 #56 制品化便车」。

**10. §七 总判断·四判据自报现状（49 条款 8 机器/18 半/24 人工/3 纸面；门禁三拍自证 0；联动 10+ 份生成物）**

- 现场证据：`git show 92557827:开发文档/目标定义_什么是OK.md | grep -n 现状` → ①『逐条强制情况见 开发文档/哲学总账.md（本文件不复述数字）』②『0 个门禁有破坏试验；发布门禁真实强制面只有 5 个检查器』③『改 1 个源文件要手工联动多份生成物（每包 10+ 份 json）』④『当前"通过"混用』；`git show 92557827:开发文档/哲学总账.md` 逐行解析 → 状态行 55：**机器强制 12 / 半强制 18 / 人工强制 21 / 纸面规则 1**（另 3 行为『四条均人工强制』×2、『半强制（最要紧的一条）』×1）；`反向试验` 命中 8（报告称 #67 要求该栏命中 0，现已有 8，且工作树已新增 `开发工具/规则自检.py` + 编译口「规则自检」阻断项，正在收口 #67）。
- 期望修法：『49 条款 8/18/24/3』既已不在 OK.md 里、现场真值也不同 ⇒ 数字过期（纸面 3 → **1**、机器 8 → **12**、条款总数 49 → 55 行/61 条款量级）。『门禁三拍自证 0 个』『联动 10+ 份生成物』两条现场仍成立。期望修法：引用该组数字时改为「现场跑哲学总账三态解析」；纸面规则剩 1 条，最易吃掉。

**11. §七 该修改的 7 项（按序）**

- 现场证据：#55 已销号且 12 个千行文件全拆完（见 扩容1）；#56 已落地（40007 cwd = 工程缓存/制品仓库/平台客户端环境/平台客户端）；D-1 三对同构副本已抽唯一实现（见 扩容2）；EC-4 已由决策 0024 裁决待执行（见 扩容10）。
- 期望修法：顺序建议不构成缺陷，但真值已变。期望修法：改为「E-8 类型体系 → EC-4 收口（清单补 2 夹具 + 判据排除别名表）→ 验证场景与生产库解耦 + 自举收敛」。

**12. §八 现状盘点（快速编译有独立 CLI / 单文件自查 --改过 / 统一验证器范围）**

- 现场证据：`ls 开发工具/快速编译/` → `运行快速编译.py`、`单文件自查.py`；`grep -n add_argument 开发工具/快速编译/运行快速编译.py` → `:179 --包目录`、`:180 --文件`、`:181 --执行样例`、`:182 --执行外部` ⇒ 独立 CLI 存在；`grep -n 改过 开发工具/快速编译/单文件自查.py` → `:15 … --改过   # 只查 git 工作树里已改动的 .py`；`grep -n 范围列表 开发工具/统一验证/统一验证器.py` → `:29 范围列表 = ["结构","契约","资产","装配","加载器","示例项目","项目适配"]` = **7 项**。
- 期望修法：报告 §11.3「修正 4」的两条（快速编译可独立调用、漏说单文件自查）现场成立；唯一要改的是『统一验证器六范围』→ 实为 **7 个范围**；『五阶段编排口不存在』的收窄结论已随 `开发工具/开发编译口/` 落地而消解。

**13. §八 8.2 计时取证表（六项静态门禁合计约 6 秒）**

- 现场证据：`git show 92557827:AGENTS.md | sed -n '76p'` → 『实测约 11–12 秒（2026-09-21 现场计时：`--变更` 三遍 11.41 / 12.24 / 12.09 秒；14 项静态门禁已于 2026-09-21 改 8 路并行（`门禁并行路数 = 8`），此前串行为 27.5 秒）』；本任务实测 `python3.14 -m 开发工具.开发编译口.编译口 --自证` → `real 0m0.343s`，退出码 0；`git show 92557827:开发文档/未完成事项.md | grep -n 184` → #184 记『AGENTS.md 两处写「编译口实测约 5 秒」，现场计时单遍 27.5 秒…已修 ②（改 28 秒并注明只跑一遍）』。
- 期望修法：『慢的不是检查，是没有那一个按钮』结论成立；表内秒数已过期（6 秒 → 27.5 秒 → 瘦身后 12.2 秒）。期望修法：该表标「历史快照」并附取证命令，或按 AGENTS.md 口径改为「编译口单轮 11–12 秒」。

**14. §八 8.4 五阶段落地形态与 ★证真注记（--包/--新建包 第一版未实现）**

- 现场证据：`ls 开发工具/开发编译口/` → `__init__.py` 442 B、`编译口.py` 45112 B；`git show 92557827:开发工具/开发编译口/编译口.py | grep -n add_argument` → `:501 --变更`、`:502 --文件`、`:503 --全仓`、`:504 --自证`（互斥组 required=True）、`:505 --联动`；`grep -n 步骤` 同版本 → `:521 步骤1 影响面`、`:541 步骤2 生成物核对`、`:557 步骤3 静态规则全查`、`:590 步骤3b 并发起跑`、`:638 步骤4 生成物自动修复（本版未实现，跳过；见审计件 §8.7 第二批）`、`:641 步骤5 定向测试点名`；`grep -n add_argument … | grep -c "包"` → 0 ⇒ `--包`/`--新建包` 确未实现。
- 期望修法：落地形态与注记全部成立（含步骤4 非静默跳号、退出码 0/1/2 五态）。无需修。

**15. §十 10.1 共享可变资源表·网关并发预算（报告写单能力并发调用上限 4 / 队列长度 100）**

- 现场证据：`git show 92557827:平台控制面/提供者/资源预算.json` → `"并发调用上限": 8`、`"队列长度": 100`。
- 期望修法：数字过期：并发上限真值 **8**（非 4），队列 100 成立。期望修法：40 路派活的分批与间隔按 **8/100** 重算（报告 §10.1 末行与 §11.5 的 4/100 都要改）。

**16. §十一 11.5 对报告结论的影响（不变/下调/上调）**

- 现场证据：『不变』：易语言模式可行且秒级 —— 本任务实测 `--自证` 0.343s；语法编译 0 失败/凭证 401/旧接入面下线/薄壳 3 工具成立（见 §一 各条）。『下调』：补充 6 件现场复核为 **2 已有**（依赖防火墙、文件租约）+ **4 部分有**（三拍账本、测试红接线、性能基线、注册快照），非『3 部分有 + 1 撤销』。『上调』：网关并发预算真值 **8/100**（报告 4/100）。
- 期望修法：期望修法：① 『补充 6 件』改为「2 已有 + 4 部分有」；② 并发预算参数改 8/100；③ 『门禁全绿』须限定为「语法/凭证/旧接入面/薄壳绿；摘要闭合随并行写入漂移」。

#### 【已消解】8 条

**1. 警告1 生产残留：支持库/后端/办公文档支持库/文档生成/实现/签名校验.py 混有 CAPABILITY_NOT_FOUND / PROVIDER_UNAVAILABLE 英文死值**

- 现场证据：`git show 92557827:支持库/后端/办公文档支持库/文档生成/实现/签名校验.py > /tmp/签名校验.py`；`grep -n 'CAPABILITY_NOT_FOUND\|PROVIDER_UNAVAILABLE' /tmp/签名校验.py` → **无命中（退出码 1）**；第 86 行现为 `if 结果.错误码 in {"提供者不可用", "能力不存在"}:`（中文码）。
- 依据：已按决策 0003 汉化，无需再改；EC-4 收口波次清单里不必再列该文件。

**2. 警告3③④ 临时文档/ 与 交付记录/、分析/ 三个『口径外目录』**

- 现场证据：`ls 开发文档/临时文档/` → 只剩 `README.md`（787 B，写明『这个目录通常应该是空的』『结论一旦并入即删除，不留过程稿』）⇒ ③ 结论已并入且目录已清空；`git show 92557827:开发文档/规范/Markdown 文档体例规范.md | grep -n 交付记录` → :104『**适用面**：权威文档 7 份 + 根 README.md + 包级 说明/*.md。方案 / 分析 / 交付记录 / 决策记录**不要求**（按自身体例），但也**不得发明第二种元信息头**』⇒ ④ 已给正式身份。另：报告之后新增登记 `开发工具/开发编译口/`（AGENTS.md:434），属正向外溢。
- 依据：两项已消解，无需再修；报告若再版应把 ③④ 划掉。

**3. 观察1 运行核心/ 下 platform.system() 取值 4 处**

- 现场证据：`rg -n "platform\.system" 运行核心/ -g '*.py'` → **0 命中（退出码 1）**；同域现为 `平台适配.本机系统名()`：`运行环境管理器/环境管理器.py:119/:219`、`环境指纹/环境指纹.py:109`、`运行环境管理器/镜像发布.py:102`、`运行环境管理器/强制校验.py:272`；`git show 92557827:公共契约/运行时/平台适配.py` :93『这是 platform.system() 的**唯一取值点**（第一轮审计 E-1）』；`开发工具/验证门禁/平台判断越界检测.py:121` 已把裸读 platform.system 列为判据。
- 依据：报告自己提的『可选收口：给 公共契约/运行时/平台适配.py 补本机系统名取值能力』即已落地那一步。防后人误修：不要再按『4 处裸读 platform.system』去修。

**4. 扩容1 多会话并行的第一瓶颈是千行文件（列 12 个 >1000 行 py）**

- 现场证据：`rg --files -g '*.py' -g '!工程缓存/**' -0 | xargs -0 wc -l | awk '$1>1000 && $2!="total"' | sort -rn` → **仅 1 个：1464 公共契约/运行时/平台适配.py**。报告列的 12 个现为：运行发布门禁.py 688 / 漂移检测.py 552 / 权威状态.py 243 / 模型连接器.py 895 / 公开调用完整性门禁.py 690 / 网关核心.py 105 / 本地网关.py 720 / 任务进程.py 367 / 环境自检.py 27 / 超时执行.py 53 / 独立进程.py 337（`运行核心/资源协调/并发控制.py` 路径已不存在）。#55 已从债务清单销号。
- 依据：12 个千行文件已全部拆完，本条已消解。**新情况**：`公共契约/运行时/平台适配.py`（1464 行）成为全仓唯一千行文件且不在原清单里——期望修法：不必重开扩容1，改为按既有拆分方案给该文件单独立项。

**5. 扩容2 适配层唯一化 + 同构副本清退（举 3 对副本实例）**

- 现场证据：① `grep -n 资源键表\|取成员索引 运行核心/加载器/提供者隔离/独立进程.py 平台控制面/提供者/进程提供者.py` → 两腿均 `from 公共契约.运行时.资源键表 import 取成员索引`（进程提供者.py:302 注明『键 → 成员索引的语义已收口到 公共契约/运行时/资源键表.py』）；② `head -8 支持库/后端/转写支持库/转写/实现/子进程解析.py` → 『唯一实现在 支持库/适配层/MLXWhisper提供者（D-3 收口，本包不放第二份）…本文件改为转调』；③ `head -8 支持库/后端/图像处理支持库/图像解码/实现/子进程解析.py` → 『唯一实现在 支持库/适配层/Pillow提供者（D-2 收口）…本文件改为转调』。
- 依据：报告举的三对同构副本全部已抽唯一实现 + 转调垫片（sys.modules[__name__] = 唯一实现），本条已消解；防后人按旧报告再清退一次。D-1 清单本体（开发文档/方案/适配层唯一化迁移清单_20260917.md，244 KB）仍在，其剩余进度需另核。

**6. 扩容10 错误码中文收口的时机成本（称视图层英文键是「刻意的兼容决定、待裁决」）**

- 现场证据：`grep -n 英文错误码 -A6 开发文档/决策记录/0024_类型铁律边界与六项待裁裁决.md` → `## 第四项：视图层英文错误码 —— **按 0003 汉化；确有外部依赖就用兼容映射**`；`**裁决**：**汉化**。决策记录 0003 明文「对外错误使用中文错误码与中文说明，不用英文枚举」`；`**被否决的做法**：以「注释自述是兼容键」为由整体保留英文码`。
- 依据：前提『待裁决』已不成立——决策 0024 第四项已裁「汉化 + 确有外部依赖才用兼容映射」，并明确否决整体保留。期望修法：本条并入 警告1/EC-4 收口波次执行，不必另立「给裁决一个触发点」。

**7. §九 9.1「AGENTS.md 已钉死第一动作」五处改动**

- 现场证据：`git show 92557827:AGENTS.md` 逐处核对：`:3 最后更新：2026-09-20`；`:314 **开发循环第一动作（华哥 2026-09-19 裁决「易语言模式」）**`；`:319-320` 含 `python3.14 -m 开发工具.开发编译口.编译口 --变更` 与 `--自证`；`:393`『不许拿跑一堆 unittest 替代编译口』『改检查器/门禁判据时必须跑 --自证 三拍』『解析不出来或门禁崩溃一律按「未核验」处理，不许静默跳过』；`:76` 条目 7 升级为开发编译口（含全量重算摘要闭合）；`:434 开发工具/开发编译口/  开发循环一键口（…）`。
- 依据：五处全部落地，无需修；后人不要按旧报告再改一遍。

**8. §十一 11.4 编译口待修清单（P1×2 / P2×2 / P3×5 / 同批 1 + 额外发现 P1）称「2026-09-19 夜间已全部修完并反向验证」**

- 现场证据：**基准版本** `git show 92557827:开发工具/开发编译口/编译口.py`（677 行）逐项命中：P1 只报告类门禁被吞 → `:623-626` 未核验判断在角色判断之前、`:634` 只报红项非 0 列出；P1 坏样本并行互删 → `:463 f"开发编译口自证_{os.getpid()}"`；P2 --全仓 不编译 → `:353 def _全仓py编译()`、`:561 退出码, 输出 = _全仓py编译()`（`:356` 注明『文件清单由本口自己枚举后整批传给 py_compile，**不经过 shell**』）；P2 git 不可用裸回溯 → `:22` 五态说明、`:171 Git不可用`、`:229/:239 raise Git不可用`；P3 步骤 4 跳过 → `:638` 显式打印；P3 摘要重复跑 → `:609 已在步骤2 核对，跳过（避免同轮重复执行）`；P3 未核验不列已记红项 → `:648-652`；P3 解码异常未捕获 → `:190 except (OSError, UnicodeDecodeError)`；P3 quotePath 假红 → `:236 ["git","-c","core.quotePath=false","status","--porcelain","-uall"]`；同批 AGENTS.md 表述 → `:393`『解析不出来或门禁崩溃一律按「未核验」处理，不许静默跳过』；额外 P1 `.strip()` 吃首字 → `:187 return 完成.returncode, 输出.rstrip(), …`（`:182` 注释写明为何不能用 strip）、`:244 状态, 路径 = 行[:2], 行[3:].strip().strip('"')`。实跑：`python3.14 -m 开发工具.开发编译口.编译口 --自证` → 三拍通过，`real 0m0.343s`，退出码 0。差异核对：`diff <(git show 92557827:…编译口.py) 开发工具/开发编译口/编译口.py | wc -l` → 9 行，全部是工作树新增的「规则自检」门禁（2026-09-22 补入 #67），不触碰任何修复项。
- 依据：10 项清单 + 额外 P1 **全部独立复核为真**，该表的『已修完』可信、非空头承诺，本条已消解，无需再修。唯 11.4 末行『验证总账：--全仓 干净通过（1005 个 .py）』的 1005 是过期数字（现 1214）。

#### 【不是缺陷】7 条

**1. §一 门禁结果·摘要闭合『全部闭合：无可重算项』**

- 现场证据：`python3.14 -m 开发工具.全量重算摘要 --只报` → 逐包列『文件摘要不一致: 说明/使用说明.md』…`共 145 项待重算：摘要不闭合，判红（去掉 --只报 重跑即可写入修复）`。同时 `git status --porcelain -uall | wc -l` = 351，diff 内容为说明书机器印记（并行会话在做 #184）。
- 依据：门禁本体行为正确（如实判红），不闭合是并行会话在途写说明书的中间态；报告 §一 抬头自己已写『全部数字只保证与所附取证命令同刻一致；并行建包期间会随提交漂移，引用前必须重算』。防后人误修：不要把这 145 项当缺陷去修摘要，等并行批落定后重跑即可归零。

**2. 观察5 决策记录编号现状（0010 合法跳号 / 0022 撞号已收口）**

- 现场证据：`ls 开发文档/决策记录/ | grep "^0010"` → 无（合法跳号）；`grep -n 编号说明 开发文档/决策记录/0048_*.md` → :3『本记录原写 0022（0022_裁决窗口复用.md）…编号按 12.4④「四位递增、不复用」顺延为 0048，原 0022 文件按「编号不复用」删除（内容已完整并入本文）』；`git log --oneline 12954720 -1` → `12954720 收口公开调用完整性门禁 9 项既有红 + 能力id基线随新增重建 + 补 0048 编号说明`（提交真实存在）。
- 依据：防后人误报：0010 缺号合规、0022 撞号已在 12954720 收口，两条都无需处理。

**3. §七 该补充的 6 件·① 分层依赖门禁**

- 现场证据：`ls -la 运行核心/依赖防火墙.py` → 55341 B（2026-09-21 11:45）；`rg -l 激活制品 运行核心/依赖防火墙.py` → 命中（发布必经链）。
- 依据：报告自己已改判『已存在，初稿是重复造轮子』——现场核实为真。防后人误修：不要新建第二套分层依赖门禁。

**4. §七 该补充的 6 件·⑤ 并行开发文件租约**

- 现场证据：`rg -l "开工即占|收工释放" -g '*.py' -g '!工程缓存/**'` → `模块库/开工编排/实现/开工编排.py`、`平台控制面/能力目录/文件租约存储.py`；`grep -n 开工即占 开发工具/任务记忆/派活任务信.py` → :149 入参说明、:156 返回值。
- 依据：『已存在』成立（C-14 原子租约门）。缺口「派活任务信自动带『开工即占 + 改动面声明』」仍成立，见 §十 条目。

**5. §十 10.1 共享可变资源表·网关运行态热接入互斥锁**

- 现场证据：`grep -n "_装配锁|热接入" 后端核心/后端核心.py` → `:53 _装配锁 = threading.Lock()`、`:126 with _装配锁:`（全量装配）、`:386 with _装配锁:`（热接入，整个函数体在锁内）；报告 §11.3『推翻 1』已自行改判为「热接入其实有互斥锁」。
- 依据：证真推翻 1 成立：热接入有互斥锁、锁序单向无死锁。防后人误修：不要给热接入新增锁；真实缝隙是变更包重载（sys.modules 弹出重装）与在途调用的微观交错窗口。

**6. §十一 11.1/11.2 证真方式与总分表（五路只读子代理，断言 9/9/10/12/12）**

- 现场证据：`git show 92557827:开发文档/分析/深度审计_底座升级清单_20260919.md` 第 336–349 行为过程自述；现场无任何可核验产物（子代理回传件未落盘，报告自述记忆包按「用完即弃」已清理）；但 11.2 表内被标注的「修正/推翻」结论我已逐条按真代码复核（见下三条）。
- 依据：属过程自述、**不可采信为事实**（本批不采信），但其结论方向经独立复核正确。防后人误用：引用时只引用 11.3 的四条修正，不要引用 11.2 的断言计数。

**7. §十一 11.3 证真推翻与修正了什么（推翻 1/2、证伪 1、修正 3/4）**

- 现场证据：推翻 1 → `后端核心/后端核心.py:53/:126/:386`（_装配锁 定义/装配/热接入）⇒ 真；推翻 2 → `ls -la 运行核心/依赖防火墙.py` 55341 B ⇒ 真；证伪 1 → 基准版本编译口 `:623-626/:634` fail-closed 已实现 ⇒ 真；修正 3 → sys.path 现场 344（报告 587→291，方向正确）⇒ 真；修正 4 → `运行快速编译.py:179-182` 独立 CLI + `单文件自查.py:15 --改过` ⇒ 真（唯『统一验证器六范围』应改 7 范围）。
- 依据：四条修正经真代码独立复核全部为真，报告正文已按此改，无需再动；本批只补两处数字（sys.path 344、统一验证器 7 范围）。

### 1 · 批B · 生成物地图_20260918.md + 说明书混血包行级对账_20260918.md

#### 【仍成立·真缺陷】2 条

**1. §三 该件自称「本批唯一交付的代码文件」生成物一致性检查.py 内含死代码**

- 现场证据：开发工具/契约编译/生成物一致性检查.py:438 def 打印包报告(包目录: Path, 结论: list[检查项]) -> None:；grep -rn 打印包报告 --include=*.py 开发工具 测试中心 运行核心 → 只有这一处定义行，零调用。
- 期望修法：已被后续件覆盖登记：开发文档/分析/死代码逐条归因与处置_20260921.md 第 13 条「③真死代码该删（纯打印辅助零调用）｜删 打印包报告（需要时在 __main__ 内联）」。按该条处置。

**2. §七.2③ 白名单补登 14 份形态漂移说明书（或裁决其去留）**

- 现场证据：6 份已补登（技能库/桌面宿主/浏览器宿主/模型连接器/进程管理/请求 → 状态=手工维护）；但现场新增 15 份形态漂移且全部不在白名单：代码解析支持库/仓库地图、代码解析支持库/语义索引、数据操作支持库/度量、文件系统支持库/内容检索、文档转换支持库/LibreOffice转换、检索支持库/统一检索、版本控制支持库/Git操作、系统核心支持库/资源管理、适配层/Git提供者、适配层/LibreOffice提供者、适配层/Swift编译器提供者、适配层/代码地图提供者、模块库/文件管理、模块库/文档读取、模块库/自修复工具（逐条与 说明书白名单.json 的 42 条路径比对，全部「不在白名单」）。
- 期望修法：对这 15 份补登记白名单或逐份裁决去留（登记缺口，非代码缺陷）。

#### 【仍成立·数字过期】11 条

**1. 生成物份数总账：1741 条 / 822 可机器重算 / 一致 807 / 手工维护 161 / 不可重算 752 / 跳过 6**

- 现场证据：python3.14 开发工具/契约编译/生成物一致性检查.py --全部 --json（HEAD 快照）→ 只读 True、写盘尝试 []、包数 146；汇总 检查项总数 1801、一致 814、手工维护 176、不可重算 775、跳过 6、不一致 15、形态漂移 15；可机器重算合计 814+15+15=844。
- 期望修法：重跑 --全部 --json 刷新整表；结构口径（分类与自洽）成立，全部数值过期。

**2. §2.1 编译器派生物「135 包 × 4 类 JSON 全仓零漂移」**

- 现场证据：现场推翻：--全部 --json 报 14 条 JSON 真不一致（参数契约 4 + 包声明 2 + 能力搜索数据 5 + Agent查询数据 3），落在 4 包：支持库/后端/数据操作支持库/数据集合、支持库/后端/文档转换支持库/LibreOffice转换、支持库/适配层/LibreOffice提供者、模块库/开工编排。独立通道 python3.14 -m 测试中心.开发工具.测试_漂移门禁 → 「仍有 4 包漂移（能力定义与派生物不一致，需按事实源重编译）」点名同 4 包。逐包差异：数据集合 契约说明已带「2026-09-21 #94 明确标注…」而能力定义.json 仍旧短句；LibreOffice转换/提供者 契约有 可重试性「超时可重试；限流可重试」与错误码 限流 而定义无；开工编排 契约 开工即占.租约 说明含 内容指纹（L7，提交 8b382a65）而定义为「字典型，含 状态/租约id清单/申请清单/认领数/说明」。
- 期望修法：把这 4 包的说明/错误码/可重试性同步回唯一事实源 能力定义.json，再跑 开发工具/契约编译/能力定义编译器 重编译 4 类派生物（模块库/开工编排 另需重生成 __init__.py），最后跑 测试_漂移门禁 归零。未登记在 未完成事项.md（20 条里无此项）→ 建议补登记。

**3. §2.4 依赖锁「0 处漂移」（32 份）**

- 现场证据：python3.14 -m 开发工具.重建依赖锁 --json → 「目标锁数」34、「需重建」0、「其中环境漂移」0、「拒绝」0；环境块 Python 3.14.4 / macOS / arm64 与当前平台一致。
- 期望修法：口径成立，份数过期：正文「32 份」改 34 份。

**4. §2.7「伪生成物 0 份」（判定口径：带生成标记却与重算不同者）**

- 现场证据：带生成标记的 __init__.py 现场 4 份（原 3 份）；--全部 --json 中 模块库/开工编排 的 __init__.py 状态=不一致，详情「带生成标记却与重生成不同：重生成多 1 行／现行多 1 行」；现场读该文件 :1 = # 本文件由契约编译器自动生成，禁止手工修改、:11 def 注册能力(注册表)。→ 原「0 份」结论被推翻，现为 1 份。
- 期望修法：用能力定义编译器重生成 模块库/开工编排/__init__.py（该包同时在 4 包派生物漂移名单内，宜一并处置）。未登记在 未完成事项.md → 建议补登记。

**5. §2.7 三小节（①14 份说明书不在白名单 ②132 份 __init__.py 无生成标记 ③默认配置 12 份/设计说明 141 份全仓零校验者）**

- 现场证据：①现场为 15 份不在白名单（原 14 已消解）；②__init__.py 手工维护 136 + 不一致 1（带标记）= 137 份无生成标记（口径成立，数值过期）；③默认配置.json 13 份、设计说明.md 147 份，grep -rn 设计说明 --include=*.py 开发工具/开发编译口 开发工具/发布门禁 开发工具/组件合规 → 0 命中；默认配置 在 开发工具/组件合规/模块合规.py:53 仅作目录名集合出现，不是校验者 → 零校验者结论仍成立。
- 期望修法：刷新份数；③「默认配置 / 设计说明 连校验者都没有，先裁决去留再谈自动化」仍待裁。

**6. §四.1「find 不排除 工程缓存 时会得 5174 —— 该目录下有 5033 份制品仓库里的包声明副本」**

- 现场证据：find ./工程缓存 -name 包声明.json | wc -l → 7278（原 5033）。
- 期望修法：「必须排除 工程缓存」这条纪律仍有效，数字须刷新为 7278。

**7. §四「必留行清单」**

- 现场证据：HEAD 快照 + G1 实跑 difflib 同法复算：数据交换 0→0（完全成立，6936 字节逐字节相同）；密码签名提供者 :77/:78/:90/:91 → 4 行且与原清单完全重合（四行内容逐字复现）；测试支持库 41→57（原 41 行全在必留集内，新增 16）；组件生成 50→61（原 50 行全在必留集内，新增 11）；文本补丁 12→43（与原清单交集 10，:29、:33 已不在）；文件系统支持库/文件操作 3（:1/:10/:211）→641（交集 0）。
- 期望修法：口径可用但行号已漂：按内容而非行号引用；文件操作/文本补丁 的说明书已从 G1 节格式迁移为「G1 首部 + G2 能力清单表 + G2 能力节」混合形态（:14 起 ## 能力清单 5 列表、:41 起 ## 文件系统支持库.文件操作.读取文件（读取文件）），行级对账口径本身不再适用，须先重判血统；引用前重跑 §7.1。

**8. §五「可刷行清单」**

- 现场证据：复算四档合计（6 包）：逐字 316 + 说明书滞后 74 + 人工增补 624 + 人工改写 182 = 1196 = 现存行合计（自检相等），生成器产出 572 行。原清单「可刷合计 110/230/46/52/87/32」与现行完全对不上（例：文件操作 可刷 230→83，文本补丁 46→53，组件生成 32→21，测试支持库 52→36）。
- 期望修法：整表作废重出，或改为「按内容比对 + 重跑 §7.1」。

**9. §7.3 #4 / #6「行号会漂…引用前必须重跑 §7.1」**

- 现场证据：已实际发生：文件操作 使用说明 233 行（252c1048, 2026-09-19）→ 246（ce54bb1a）→ 689（87a28a33）→ 723（65808c3f）→ 724（89549f30, 2026-09-21）；文本补丁 58 → 96；数据交换 110、测试支持库 93、密码签名提供者 91、组件生成 82 不变。
- 期望修法：引用前一律重跑；建议在 §四/§五 表头标注「已作废，须重算」。

**10. §7.3 #5「未核对项：本文只对照 G1；G2 产出形态与这 6 份不匹配，未跑，判为不相关」**

- 现场证据：该自陈的边界已成真问题面：6 包中 2 份已含 G2 形态（文件操作、文本补丁：## 能力清单 5 列表 + ## <能力id>（<中文名称>）节，G2 = 支持库/后端/组件规范支持库/实现/说明书生成器.py:143/:150）；全仓形态分布 = 含 ## 能力清单 的 使用说明.md 33 份、含 G1 节格式 ## 能力 的 114 份、带机器印记 类型：包级说明-使用说明 的 18 份（两套形态并存）；权威工具实测 python3.14 -m 开发工具.MD文档生成.说明书.合并重生成 --包 <6包> 对 文件操作（账 252/现 724）、测试支持库（账 43/现 93）、组件生成（账 26/现 82）三条报「账行数远小于现行…：疑似血统判错（判为 G1）；请核对生成器或手工指定血统」。
- 期望修法：重跑 G2 对账并重判这 6 包血统；合并重生成 的血统自动判定对混合形态会误判，须先给出显式血统声明。

**11. §二 现场口径「实跑结果：发现成功 True 包数 135 → 写出 136」**

- 现场证据：HEAD 快照实跑同一入口 写出说明书(发现全部(支持库, 模块库), 输出目录) → 「发现成功 True 包数 140」→「写出 141」（140 包 + 1 份目录说明书）。
- 期望修法：口径成立，数字刷新为 140 / 141。

#### 【仍成立·建议未落地】1 条

**1. §七.5 8 类无生成器物要不要建生成器（752 条）**

- 现场证据：现场「不可重算」775 条：权限契约 140 / 配置契约 139 / 依赖契约 21 / 资源预算 137 / 复用决策 138 / 生命周期契约 48 / 默认配置 13 / 验证场景引用 140（另含说明类）。
- 期望修法：仍待裁决；权限契约 与 配置契约 仍是收益最高的两个候选（有结构、有意义、有校验者）。

#### 【仍成立·断言为真非缺陷】5 条

**1. §2.5 能力定义编译器.生成验证场景引用 恒返回 None**

- 现场证据：开发工具/契约编译/能力定义编译器.py:506-514 仍是同一 docstring（旧三键格式缺 契约版本、生成即覆盖合法文件）+ return None；现场 验证场景引用.json 140 份，全部状态「不可重算（全仓无生成器）」。
- 期望修法：决定仍生效，无需改动。

**2. §1.1「6 个混血包是哪 6 个」**

- 现场证据：开发文档/项目证据/说明书白名单.json 中 类别=B混血包 仍为 6 条，路径逐条相同：支持库/后端/数据操作支持库/数据交换（手写区 1）、支持库/后端/文件系统支持库/文件操作（2）、支持库/后端/文件系统支持库/文本补丁（12）、支持库/后端/测试支持库（35）、支持库/适配层/密码签名提供者（4）、模块库/组件生成（45）；处置 均为「禁整文件重生成；重生成前须行级合并（保手写行）」。
- 期望修法：无需改动。

**3. §7.3 #2「数据交换 与生成器产出逐字节相同（6936 字节、sha256 一致）；白名单原记 1 行是行内口径」**

- 现场证据：HEAD 快照 支持库/后端/数据操作支持库/数据交换/说明/使用说明.md = 6936 字节，sha256 与 G1 实跑产出完全相同（a == b 为真）；白名单该条证据仍写「与 G1 生成器实跑逐字相同（差 0 行）→ 手写区落在行内（:54 反序列化CSV 示例的换行续行）」。
- 期望修法：完全复现，无需改动。

**4. §1.3 与白名单已登记「整行集合差」的对齐（50 / 34 / 14 / 10）**

- 现场证据：说明书白名单.json 计数.生成物类含人工改写行_明细 原文仍为「模块库/组件生成 50 行｜支持库/后端/测试支持库 34｜文件系统支持库/文件操作 14｜文件系统支持库/文本补丁 10｜代码解析支持库/代码地图 4｜适配层/密码签名提供者 4｜技能库 1｜前端/桌面宿主 1｜前端/浏览器宿主 1｜大语言模型支持库/模型连接器 1｜系统核心支持库/进程管理 1｜网络通信支持库/请求 1（按「现存有、生成器不产」的整行集合差口径）」—— 被引的 50/34/14/10 一字未变。
- 期望修法：无需改动。

**5. §六.1 第 2 条「不许把生成器指向包内 说明/；现行唯一合法做法：生成到 /tmp → 点状回贴 → 再落包内」**

- 现场证据：开发工具/MD文档生成/说明书/说明书生成器.py:156-165 写出说明书(声明列表, 输出目录) 只做 输出目录.mkdir + 文件.write_text，不触包内 说明/；本批两次实跑（工作树与 HEAD 快照）产出全部落 /tmp/证真批B/，仓库零改动。
- 期望修法：纪律仍有效。

#### 【已消解】15 条

**1. 全仓包数「141」（§四.1 口径更正 139→141）**

- 现场证据：HEAD 快照 find . -name 包声明.json -not -path ./工程缓存/* | wc -l → 146；能力定义.json → 140；使用说明.md → 147。原 141 已过期。
- 依据：已被后续件覆盖：开发文档/分析/全盘审计_20260921.md §3.3 #76 原文「现场 扫描正式包=146」；开发文档/未完成事项.md §7.2 #76 已登记。引用一律以 146 为准。

**2. §2.2 完整性摘要「1 处真漂移」支持库/适配层/pdfplumber提供者**

- 现场证据：python3.14 开发工具/全量重算摘要.py --只报 → 「全部闭合：无可重算项」，exit 0；HEAD 快照内 pdfplumber提供者 的 完整性摘要.json 状态=一致；全仓 完整性摘要.json 一致 140 / 不一致 0。
- 依据：该件 §五 已自述「04:23:34 复跑：全部闭合…已被写者自己闭合」，本批复算确认，此后摘要面保持闭合。

**3. §2.3 说明书「14 处形态漂移，0 处真缺陷」及 14 项清单**

- 现场证据：逐包查 HEAD 快照：技能库/后端/技能库、支持库/前端/桌面宿主、支持库/前端/浏览器宿主、支持库/后端/大语言模型支持库/模型连接器、支持库/后端/系统核心支持库/进程管理、支持库/后端/网络通信支持库/请求 现状态=手工维护（白名单「生成物类·含人工改写行」）；支持库/前端/组件控件、支持库/后端/并发控制支持库 现状态=一致「与 G1 生成器逐字相同」；6 个聚合父包（办公文档/大语言模型/数据操作/文件系统/系统核心/网络通信）现状态=一致。原 14 项一份不剩。
- 依据：消解处：开发文档/方案/说明书存量治理施工表_20260918.md §二 G7（补登记 7 条，白名单 34→41）与 G8（6 个聚合父包重生成 153→274/406→560/538→681/219→274/494→868/76→87 行）。注意现场另有 15 份新形态漂移且全部不在白名单，见「§七.2③」条。

**4. §2.3 / §七.3「2 处实质口径冲突：组件控件、并发控制支持库 的 句柄 参数 说明书 整数型 vs 契约/定义 文本型」**

- 现场证据：支持库/后端/并发控制支持库/能力契约/参数契约.json 逐条实测 句柄 → 整数型（18 处全整数型）；支持库/前端/组件控件 实测 窗口句柄/页面句柄/组件句柄/句柄id → 整数型；两份使用说明.md 现状态=一致（与 G1 生成器逐字相同），并发控制说明书 :202 起 句柄:整数型 与契约同值。
- 依据：冲突已由「契约/定义改判为整数型」消解，无需再裁。

**5. §一 第 17 项 说明/目录树与命令.md「全仓无校验者」**

- 现场证据：开发工具/开发编译口/编译口.py:108-115 新增阻断项 {"名字": "目录树与命令", "模块": "开发工具.MD文档生成", "参数": ["--类型","包级说明-目录树与命令","--校验"], "角色": "阻断", "超时秒": 120}，注释明写「2026-09-20 补入…此前本类零生成器覆盖…实测已真实漂移 1 份（PDF生成）」。现场 目录树与命令.md 67 份。
- 依据：改为「已有阻断级校验者」。

**6. §三 只读校验入口「未接门禁（任务边界）：本批只交付入口与数据，接线是下一步」**

- 现场证据：开发工具/发布门禁/运行发布门禁_防回潮判据.py:563 调 开发工具.契约编译.生成物一致性检查.入口 的全量 JSON 口径、:588 检查器 = 扫描根/开发工具/契约编译/生成物一致性检查.py；运行发布门禁_防回潮公共.py:318/:326 明写判据本体就是该文件、不另立第二份实现；说明书存量治理施工表_20260918.md §二 G11「防回潮·说明书白名单联动…已接进发布门禁必经路径（防回潮循环第 6 项）」、G12 反向验证三段式已完成。
- 依据：接线已完成。

**7. §五「与既有门禁现场跑数的分歧」5 行（检测能力定义漂移 6 包假红 / 检测完整性摘要格式 / 重建依赖锁 / 全量重算摘要 1 项 / 错误码文案派生 1 项违规）**

- 现场证据：逐行复算：① 测试_漂移门禁 输出中「存在包声明但无能力定义」命中 0（原 6 个聚合父包假红消失），报红改为 4 包真漂移；② 完整性摘要 一致 140/不一致 0 → 仍 0 条；③ 重建依赖锁 0/34（份数更新）；④ 全量重算摘要 →「全部闭合」exit 0（原 1 项）；⑤ 开发工具/错误码文案派生.py:62 说明表源 = ("运行核心","统一网关","协议","错误说明表.py")（原指 网关核心.py），校验输出「不变式通过：说明表 368 码 == 派生结果（模板派生 279 + 手写例外 89），无违规」exit 0。
- 依据：5 行里 4 行分歧已闭合；仅剩「4 包真漂移」需处置（见 §2.1 条）。

**8. §七.1 接线门禁（C 路线）：把 6 类派生物接成发布门禁强制项**

- 现场证据：证据同「§三 未接门禁」条：运行发布门禁_防回潮判据.py:563/:588 + 运行发布门禁_防回潮公共.py:318/:326 + 施工表 G11/G12。
- 依据：已落地。

**9. §七.2① 检测能力定义漂移 的「存在包声明但无能力定义」要对 6 个聚合父包放行**

- 现场证据：python3.14 -m 测试中心.开发工具.测试_漂移门禁 现场输出中「存在包声明但无能力定义」grep -c → 0；报红为 4 包真漂移（数据集合/LibreOffice转换/LibreOffice提供者/开工编排），不再有聚合父包假红。
- 依据：假红已消除。

**10. §七.2② 错误码文案派生.py 的 说明表源 改指 协议表.py**

- 现场证据：开发工具/错误码文案派生.py:62 说明表源 = ("运行核心","统一网关","协议","错误说明表.py")；python3.14 开发工具/错误码文案派生.py 校验 → 通过、exit 0。
- 依据：判据已跟上搬迁。

**11. §七.3 裁决 2 处实质口径冲突（组件控件 / 并发控制支持库 的 句柄 参数）**

- 现场证据：同「§2.3/§七.3 2 处实质口径冲突」条：参数契约实测 句柄 → 整数型，说明书 与 G1 逐字相同。
- 依据：已裁并已落地。

**12. §七.4 6 个聚合父包的说明书要跟子域（不被 发现全部() 收）**

- 现场证据：说明书存量治理施工表_20260918.md §二 G8 已完成：6 份重生成（办公文档 153→274、大语言模型 406→560、数据操作 538→681、文件系统 219→274、系统核心 494→868、网络通信 76→87 行）；HEAD 快照这 6 包的 使用说明.md 状态全部=一致「与 G1 生成器逐字相同」；--全部 --json 的「跳过 6」正是这 6 个聚合父包（无能力定义，不产派生物）。
- 依据：已消解。

**13. §一 逐包差额解释（原记 vs 实测 0/3/12/41/4/50）**

- 现场证据：已被后续件覆盖并执行完毕：开发文档/方案/说明书存量治理施工表_20260918.md §二 G3「B混血包 6 份的行级对账 — ✅ 已完成（2026-09-19）」、§六「G3 实测对账结果」逐包数字与本件 §一 完全吻合（数据交换 110/110,0,0｜文件操作 233/230,14,12｜文本补丁 58/43,12,0｜测试支持库 93/43,41,7｜密码签名提供者 91/87,4,0｜组件生成 82/26,50,5）、§二 G4「合并式重生成 — ✅ 已完成（2026-09-19）…2 份有改动、7 份零改动」、§七「人工行零丢失=True」。另 开发文档/未完成事项.md 已整体重构，不再存在 §8.3 / 第 37 项。
- 依据：差额解释作为历史口径仍有价值，但该债已闭。

**14. §7.3 #1「本文是分析不是处置：6 包说明书一行未改…第 37 项仍然开着」**

- 现场证据：说明书存量治理施工表_20260918.md §二 G4 = ✅ 已完成（2026-09-19）对 6 包做行级合并式重生成；开发文档/未完成事项.md 已无 §8.3 / 第 37 项编号（该清单已整体重构；grep -c 混血包|行级合并 → 2 处，均非该债务条目）。
- 依据：该债务已闭。

**15. §7.3 #3「文件操作 :1 标题写『文件系统』而包声明 名称=文件操作，本次未裁」**

- 现场证据：HEAD 该文件 :1 = 「# 文件操作（支持库.后端.文件系统支持库.文件操作）」；说明书白名单.json 该条证据原文「2026-09-19 行级合并已完成…合并后 232 行…改标题 文件系统 → 文件操作」；说明书存量治理施工表_20260918.md §七 同载该改名。
- 依据：已裁并已改（取包声明/生成器口径）。

#### 【不是缺陷】1 条

**1. §六.1 第 5 条「版本行滞后不算缺陷（决策记录 0020）」**

- 现场证据：开发文档/决策记录/0020_版本控制判据等于对外契约变化.md 仍在（依据有效）。
- 依据：防误修：不要为刷版本行去重生成整本说明书（那正是手写区被抹的路径）。

### 2 · 批C · 端到端背压核对_20260918.md + 配置双入口收口_20260918.md

#### 【仍成立·真缺陷】42 条

**1. §1层结论矩阵·背-1 入口层有界线程上限＝并发上限（默认64、区间1..256、生产256）——原判「表述需改」**

- 现场证据：运行核心/统一网关/安全/有界服务器.py:59 最大工作线程=64、:61 校验1<=x<=256、:64 request_queue_size=min(128,x)、:65 BoundedSemaphore；配置侧 运行核心/统一网关/本地网关.py:584 默认64、:585 区间已放宽为1..512、:655 最大工作线程=self.并发上限；生产值不再写死256：运行核心/自适应选型/并发自适应.py:57-62（大核档基线256）→ 运行核心/启动运行核心网关.py:33,75-77,156
- 期望修法：把「生产装配256」改为「按硬件画像取值，本机32核档=256，画像不可用回落64」；并把 本地网关.并发上限 的1..512 与 有界服务器 的1..256 对齐（见发现2）

**2. §1·背-2 入口层满载回 限流/HTTP429+结构化信封，信封无 可重试**

- 现场证据：有界服务器.py:72-76（acquire(blocking=False)失败→_写限流响应）、:98-109（错误码=限流、错误说明=网关工作线程已达上限、HTTP/1.1 429 Too Many Requests、固定4行头）；码表 本地网关.py:95（限流:429）；未登记码回落500 本地网关.py:530
- 期望修法：信封增 可重试（见落点4/5）

**3. §1·背-3 入口层退避提示未见实现**

- 现场证据：有界服务器.py:105-109 响应头仅 Content-Type/Content-Length/Cache-Control/Connection，无 Retry-After
- 期望修法：429头加 Retry-After（限流层取窗口剩余，源 安全/限流器.py:84 窗口秒）

**4. §1·背-5 网关层 限流→429，网关响应结构无 可重试**

- 现场证据：比对入口 安全/限流器.py:173 进入请求、:191 全局并发、:198 单维并发、:203 窗口频率；落码 运行核心/统一网关/核心处理/网关处理面.py:58,63-64（被限流=真→_设置失败(响应,"限流")）；响应字段全集 运行核心/统一网关/协议/网关信封.py:55-78（请求id/操作/成功/值/错误码/错误说明/详情/句柄/耗时毫秒/请求版本/当前版本/版本差异/被忽略参数）无 可重试；文案 协议/错误说明表.py:44
- 期望修法：网关响应 增 可重试:bool（协议只增不删），_设置失败 按码给默认值（限流/资源繁忙/提供者不可用/外部不可访问/超时→真）

**5. §1·背-6 网关层下游连接器也不读 可重试**

- 现场证据：运行核心/能力调用/HTTP连接器.py 全文件 可重试 命中0；返回字典仍只取7键，耗时毫秒在:164，返回结构必填集在:196
- 期望修法：与落点4同批接通，否则新增字段无人消费

**6. §1·背-8 能力调用层上限与入口上限同源但两个独立计数器，靠256=256维持，无单一门禁——原论证已失效**

- 现场证据：超时执行/基础.py:15-21 注释称「本地网关/流式HTTP 的参数校验区间都是1..256」；HEAD实测 本地网关.py:585 为1..512、运行核心/统一网关/传输/流式HTTP.py:33 最大并发通道上限=512；两侧各自校验仍在（有界服务器.py:61、本地网关.py:585），确无单一门禁
- 期望修法：把 基础.py:18 注释改成实际口径，或把网关侧区间收回256；并加一条「入口上限≥执行线程上限」的机器校验（当前靠人读注释）

**7. §1·背-9 任务调度层 最大活动数4/最大排队数16/提交截止秒30.0，生产装配未覆盖**

- 现场证据：池默认 运行核心/任务调度/任务进程.py:77-79；门面默认 任务系统.py:62-63；生产装配链 运行核心/启动运行核心网关.py:127 构造任务系统(后端)→运行核心/任务调度/任务接入.py:52-54 任务系统(存储目录=存储目录 or 默认任务存储目录())，不传上限
- 期望修法：上限改可从装配注入（落点12）

**8. §1·背-10 任务调度层 资源繁忙 未登记两表→被伪装成 内部错误/500**

- 现场证据：异常类型 任务进程_对象.py:71-75；抛出点 任务进程.py:166-167（排队满）、:173-175（等槽超截止）、:161/:181（停止态）；门面裸转 任务系统.py:211-213；网关裸调用 运行核心/统一网关/核心处理/网关执行面.py:220；兜底 核心处理/网关处理面.py:83-86（except Exception→内部错误）；HTTP500回落 本地网关.py:530；逐码计数：资源繁忙 在 本地网关.py 命中0、在 协议/错误说明表.py 命中0（对照 限流/提供者不可用/外部不可访问 各命中1）
- 期望修法：落点1/2/3 三条同批做

**9. §1·背-13 外部模型层 超时/响应体/内存门槛，无每模型并发闸门**

- 现场证据：支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:127 默认超时秒=1800；支持库/适配层/模型HTTP提供者.py:127 HTTP超时600、:555 流式120；模型连接基元.py:198（HTTP响应超过4MB上限）、:34 内存安全阈值0.80→本地启动准备与守卫.py:255（资源预算未验证）、:262（资源不足）；模型工具调度.py:84 调度默认并行上限=10（产出计划非闸门）
- 期望修法：仍成立，行号漂移（旧35/356/36/138/145/1180→新127/198/34/255/262/84）

**10. §3不协同 N1 任务层「忙」被伪装成「服务端故障」**

- 现场证据：见背-10全部行号；网关侧对 资源繁忙 零分支、零登记（grep实测0）
- 期望修法：P0：网关执行面.py:220 外包 try/except 资源繁忙错误；协议/错误说明表.py:32起增 资源繁忙；本地网关.py:84起 公开错误码状态映射 增 资源繁忙:503（不要落500）；任务系统.py:213 同步转码

**11. §3 N2 网关对外丢掉「可重试」**

- 现场证据：协议/网关信封.py:55-78 字段全集无 可重试；HTTP连接器.py grep 可重试=0
- 期望修法：P0：网关响应增字段+_设置失败按码给默认值+信封三处同步（落点4/5/6）

**12. §3 N3 全链路无退避提示（Retry-After 0处产生/解析）**

- 现场证据：全仓 --include=*.py grep Retry-After|retry_after：生产代码0命中（唯二命中在测试中心的无关文案）；退避算法 模型工具调度.py:184 def 重试调度、常量:155/:159/:168（3/0.5/300.0）；grep 重试调度( 全仓仅1处=自身def，仍无生产调用方
- 期望修法：P1：429头加 Retry-After；把 重试调度.下次退避秒 接进出站失败详情

**13. §3 N4 上游429可重试判定两处相反**

- 现场证据：提供者层 支持库/适配层/模型HTTP提供者.py:173-174（状态码==429→可重试=True）；连接器层 模型连接基元.py:221（429:请求限流）→:224（可重试=错误.code>=500 ⇒ 429判假）
- 期望修法：P1：两处共用同一「状态码→错误码+可重试」映射函数（落点9）

**14. §3 N5 任务队列对已入队任务无时长上限**

- 现场证据：任务进程.py:153-156（注释原文「已入队任务等槽位不受截止约束（有界队列语义：排队任务不得丢失）」）、:157 截止只在入队前算、:163-165 队列满且截止≤0→资源繁忙、:171-176 未入队→wait(剩余截止)、:178-179 已入队→wait(timeout=0.1)心跳无总截止；对端 本地网关.py:582 请求超时1800、启动运行核心网关.py:145
- 期望修法：P1：给「已入队」也设总排队截止，到点走同一错误码并写进对外可读状态（落点11）

**15. §3 N6 同一「满」两种闸门、一个错误码——可分辨度略增**

- 现场证据：入口闸门 有界服务器.py:65,72-76,98-109（说明=网关工作线程已达上限）vs 网关五维闸门 安全/限流器.py:191,198,203（说明=并发请求数超过上限N/<维度>并发超过上限/<维度>调用频率超过上限）+网关处理面.py:63-64；两者错误码都是 限流、HTTP都是429（本地网关.py:95）
- 期望修法：P2：给429带层级标识（码层面仍无法分辨；文案层面现已能分辨——比旧报告更弱一档）

**16. §3 N7 两个已配置的限流维度不生效**

- 现场证据：安全/限流器.py:233 def 进入任务（:237-238 自述全仓目前没有调用方）、:253 def 进入流式（:259 自述全仓目前没有调用方）；全仓 grep 进入任务|进入流式 除定义/自述/文档外0调用方；最大任务数=20（:73）、最大流式连接=10（:74）从不参与判定
- 期望修法：P2：接入任务/流式分支，或删掉这两个参数（不留「配置了不生效」）

**17. §3 N8 幂等「进行中」表无界**

- 现场证据：无界声明 运行核心/统一网关/网关核心.py:92 self._幂等进行中: dict[str,tuple[str,threading.Event]]={}；插入 核心处理/网关幂等面.py:60-63；只有已完成表被裁剪 :79-80、:87-88（while len>500 pop）；_幂等进行中 只在 :78、:86 收尾pop
- 期望修法：P2：_幂等进行中 加同口径上限与淘汰；或对 请求id 加强来源约束

**18. §3 N9 提供者层两条实现都不在产线——重复度已降一档**

- 现场证据：独立进程.py:193 class 提供者进程池 调用方只有 测试中心/第一批维修/测试_Provider进程.py:20,99,190 与 测试中心/运行核心/测试_硬件画像与档位.py:367,370；平台控制面/提供者/进程提供者.py:268 class 本地进程提供者 调用方只有同两个测试；生产入口仍是 提供者生命周期.py:320+:497；已消解部分：两者共用的资源键表已下沉为唯一件 公共契约/运行时/资源键表.py:3-4
- 期望修法：仍成立部分：二选一未做；期望按「同一件事两套实现即缺陷」删一份（连测试一并收），或明确写清保留理由

**19. §3 N10 提供者级并发只在启动路径判**

- 现场证据：提供者生命周期.py:46,94,101 最大进程数、:320 def 启动提供者、:332-333、:360-363（TOCTOU复查后 return 假,"运行中进程数已达上限…拒绝启动"）；调用路径 :497 def 调用能力 全程只有 :527 调用超时与 :49 取消等待，无提供者级并发准入
- 期望修法：P2：把准入延伸到调用路径，或把「最大进程数不是并发上限」写进边界说明

**20. §3 N11 流式通道削平 可重试**

- 现场证据：组装处 运行核心/统一网关/传输/流式HTTP.py:320-324（只取 数据.get("错误码") 与 数据.get("错误说明") 两键调 通道.失败）；失败() 签名 :174-178 也只收两参；源事件确带 可重试：模型HTTP提供者.py:287-291、模型连接基元.py:684-688；下发链路 _终止(:137)→迭代事件(:213) 确认不是静默丢弃
- 期望修法：P0/P1：失败事件把源事件的 可重试（及 状态码）一并带上

**21. §3 N12 同域两份有界HTTP实现行为不一致——收敛方案只写在注释里**

- 现场证据：本域 安全/有界服务器.py:32,59-65,72-76,98-109；那份 公共契约/运行时/有界HTTP.py:16 最大线程数=32、:22 满载 self.shutdown_request(请求) 不回任何响应；域外调用方实测仍是那份：开发工具/轻代码前端编辑器/启动编辑器.py:27、开发工具/能力网关/能力网关.py:42、支持库/前端/浏览器宿主/实现/浏览器宿主.py:13；有界服务器.py:46-55 已把四步收敛方案写进docstring，正文一字未改（:11-12 自述逐字节搬动）
- 期望修法：P2：按该四步方案实施；或明确接受两份（则需删掉「违反哲学1.3」的自述）

**22. §3 N13 同步「拒绝」vs 任务「排队」无统一约定**

- 现场证据：能力调用层不排队 超时执行/准入闸门.py:19-23（等待秒=0）；任务层排队长等 任务进程.py:157-179；协议侧 运行核心/统一网关/协议/操作协议表.py:9,18 只有 允许操作表/操作权限表，全文无 限流 条目（旧 协议表.py:96 已不存在）；限流 文案现只在 协议/错误说明表.py:44
- 期望修法：P2：把「拒绝还是排队」上升为协议约定；落点需改指 协议/错误说明表.py（旧落点文件已拆掉）

**23. §3 补充：启动路径返回 (bool,str) 而非错误码**

- 现场证据：提供者生命周期.py:333、:363 都是 return 假, "运行中进程数已达上限（…），拒绝启动 …"
- 期望修法：与「失败必带错误码」口径对齐，或明确它是装配期接口不属调用契约

**24. §4 落点1 网关核心.py:956 包 try/except**

- 现场证据：现址 核心处理/网关执行面.py:220（任务对象=self.任务系统.提交(），其上无try/except；异常落到 核心处理/网关处理面.py:85-86
- 期望修法：外包 try/except 资源繁忙错误 → _设置失败(响应,"资源繁忙")

**25. §4 落点2 两表登记 资源繁忙**

- 现场证据：协议/错误说明表.py:32起表内 资源繁忙 命中0；本地网关.py:84起映射内命中0
- 期望修法：说明表增文案；映射增503

**26. §4 落点3 任务系统.py:213 同步转码**

- 现场证据：任务系统.py:211-213 仍是裸转（独立对象=self.进程池.提交(，无except）
- 期望修法：同步转码，避免只在网关解一次

**27. §4 落点4 网关响应增 可重试**

- 现场证据：协议/网关信封.py:55-78（无该字段）、:80-108 转字典；_设置失败 在 核心处理/网关处理面.py:140-161 一带
- 期望修法：增字段+按码默认值

**28. §4 落点5 三处信封同步补 可重试**

- 现场证据：有界服务器.py:98-109（线程满载信封）、传输/网关边界面.py:157-179（_拒绝）、传输/流式路由面.py:165（_拒绝(429,"限流",…)）
- 期望修法：三处同批补字段

**29. §4 落点6 流式失败事件带上 可重试**

- 现场证据：传输/流式HTTP.py:320-324 + :174-178
- 期望修法：见N11

**30. §4 落点7 429响应头加 Retry-After**

- 现场证据：有界服务器.py:105-109 固定4行头
- 期望修法：加头；窗口剩余源 安全/限流器.py:84

**31. §4 落点8 重试调度 接进出站失败详情**

- 现场证据：模型工具调度.py:184 定义、:199 返回{是否重试,下次退避秒,原因}；生产调用方 grep 0
- 期望修法：接入+消费方（模块库/直播逐字稿/实现/精校裁决.py:198 关键词白名单）改读字段

**32. §4 落点9 连接器429归入可重试**

- 现场证据：模型连接基元.py:221,224
- 期望修法：与 模型HTTP提供者.py:173-174 对齐

**33. §4 落点10 提供者读响应头 Retry-After**

- 现场证据：模型HTTP提供者.py:122 headers=请求头 只有请求头构造、:168-179 _错误响应 只按状态码分流，无响应头读取
- 期望修法：读响应头作为落点8输入

**34. §4 落点11 已入队总排队截止**

- 现场证据：任务进程.py:155-179
- 期望修法：复用/新增截止参数，到点走同一错误码

**35. §4 落点12 任务上限可从装配注入**

- 现场证据：任务系统.py:62-63、任务接入.py:52-54、启动运行核心网关.py:127
- 期望修法：装配传值

**36. §4 落点13 N6层级标识——部分仍成立**

- 现场证据：文案已天然分层（有界服务器.py:101 vs 安全/限流器.py:192,200,205），但错误码同为一个 限流
- 期望修法：若要码层面可分辨，仍需带层级标识/详情

**37. §4 落点14 N7接 进入任务/进入流式**

- 现场证据：安全/限流器.py:233,253，全仓0调用方
- 期望修法：接入或删参数

**38. §4 落点15 N8 _幂等进行中 加界**

- 现场证据：网关核心.py:92、核心处理/网关幂等面.py:60-63,78,86 vs :79-80,87-88
- 期望修法：加同口径上限/淘汰

**39. §4 落点16 N13上升为协议约定——落点文件已失效**

- 现场证据：协议/操作协议表.py 全文无 限流；旧 协议表.py 已不存在
- 期望修法：落点改指 协议/错误说明表.py（或新建协议约定块）

**40. §4 落点17 N9提供者层二选一**

- 现场证据：独立进程.py:193、进程提供者.py:268 两份俱在
- 期望修法：删一份连测试一并收；共用件已下沉（公共契约/运行时/资源键表.py），收口成本比旧报告更低

**41. §4 落点18 N10准入延伸到调用路径**

- 现场证据：提供者生命周期.py:320,332-333,360-363 vs :497
- 期望修法：延伸或写清边界

**42. §六待办3 配置适配.py::读取配置 零调用方 + 项目配置.json 不存在——仍挂着**

- 现场证据：项目适配层/配置适配/配置适配.py:23；项目适配层/项目配置/ 只有 默认配置.json/开发配置.json/发布配置.json（ls实测）
- 期望修法：或接线到门面层统一入口，或按「无据误建物」清掉

#### 【仍成立·建议未落地】1 条

**1. §六待办4 README「包化另立项」——仍挂着，与本批无关**

- 现场证据：支持库/适配层/说明/README.md:71-74,87,131 仍在
- 期望修法：仍挂着

#### 【仍成立·断言为真非缺陷】21 条

**1. §1·背-7 能力调用层 全局256/每能力64/每能力闸门512，满载直接拒绝不排队，唯一同时给码+可重试+明细**

- 现场证据：运行核心/能力调用/超时执行/基础.py:25 默认全局执行线程上限=256、:26 默认每能力执行线程上限=64、:34 每能力闸门数上限=512；不排队语义 超时执行/准入闸门.py:19-23（等待秒=0，容量闸门满载直接拒绝）；拒绝三件套 超时执行/执行器_结果面.py:18-29（限流+说明+来源=超时执行器.线程准入+可重试=真+详情）；载体 公共契约/基础类型/结果类型.py:49 def 可重试
- 期望修法：仍成立；行号整体漂移（旧110/111/119/501→新25/26/34+执行器_结果面.py:18-29）

**2. §1·背-11 提供者层 池大小/生命周期/进程提供者三处上限**

- 现场证据：运行核心/加载器/提供者隔离/独立进程.py:105 最大允许池大小=16、:196 池大小=2/最大资源键数=4096；运行核心/运行环境管理器/提供者生命周期.py:46 默认最大进程数=8、:94、:48 重启3、:49 取消等待2.0、:98 调用超时3.0；平台控制面/提供者/进程提供者.py:271-272（池大小1/4096）、:19 最大允许池大小=16
- 期望修法：仍成立，行号漂移（旧901/68/271→新196/105/271-272）

**3. §1·背-12 提供者层 满码带可重试**

- 现场证据：独立进程.py:252-253（进程调用结果(假,错误码=资源繁忙,说明=Provider资源键表已满,可重试=真)）；独立进程_调用面.py:34 字段、:41 转字典带出；进程提供者.py:340/342/348；生命周期:523（外部不可访问）、:526,:556-561（已取消，现带可重试=真）、:534-535（滞留响应超预算，可重试=真）、:565（超时，可重试=真）；脚本实测 资源繁忙/已取消/滞留响应超预算 三码在两张表命中0
- 期望修法：仍成立；新增事实：生命周期「已取消」两条现也带可重试=真（旧报告只记滞留响应超预算），且三码均未登记两表

**4. §4 已有可用件·可重试 载体**

- 现场证据：公共契约/基础类型/结果类型.py:49（旧40）
- 期望修法：可用件，无需改动

**5. §4 已有可用件·满载三件套范本**

- 现场证据：超时执行/执行器_结果面.py:18-29（旧 超时执行.py:501）
- 期望修法：可用件，作为落点4/5范本

**6. §4 已有可用件·退避算法**

- 现场证据：模型工具调度.py:184（旧 模型连接器.py:1250-1270）
- 期望修法：可用件，接落点8

**7. §4 已有可用件·有界队列范本**

- 现场证据：任务进程.py:128-179 + 任务进程_任务归还面.py:35（_退出队列/_退出排队）
- 期望修法：可用件，作为落点11范本

**8. §4 已有可用件·限流拒绝留痕**

- 现场证据：安全/限流器.py:164 def _记录拒绝
- 期望修法：可用件，落点7可复用

**9. §4 已有可用件·幂等键等待有界**

- 现场证据：核心处理/网关幂等面.py:69-71（完成事件.wait(timeout=max(0.1,请求.超时秒))→超时）
- 期望修法：可用件，落点15可参照

**10. §二依据1 分层口径不允许合并（支持库只许依赖公共契约；项目适配层不许依赖支持库）**

- 现场证据：运行核心/依赖防火墙.py 允许依赖表："支持库":{"公共契约"}、"项目适配层":{"公共契约","运行核心"}（sed -n '/允许依赖表/,/^}/p' 实测，含头注释镜像块逐项同源）
- 期望修法：仍成立；但注意「现状0违规」前提已破（见发现1：实测2条违规）

**11. §二依据2 读的不是同一份数据、交付的不是同一件事**

- 现场证据：提供者层数据源 支持库/适配层/提供者配置/提供者配置.json（272B）；门面层数据源 项目适配层/项目配置/（默认配置.json/开发配置.json/发布配置.json，无 项目配置.json）；门面层实现三件在 项目适配层/配置适配/配置合并.py:17,51,84、配置校验.py:30
- 期望修法：仍成立

**12. §二依据3 包级公开能力面不交集**

- 现场证据：实测 ['读取提供者配置','获取能力配置'] vs ['配置读取结果','读取JSON配置','读取配置目录','合并配置','解析环境配置','校验配置','配置校验结果']（支持库/适配层/提供者配置读取/__init__.py:17 / 项目适配层/配置适配/__init__.py:7-11），无同名符号
- 期望修法：仍成立

**13. §二依据4 唯一真重复已在 e08c554f 删掉且不在本目录**

- 现场证据：git log -1 e08c554f = 2026-08-18 五域审计修复落地…⑤死代码清理9处（…配置适配/配置读取…）；git show --stat e08c554f 含 项目适配层/配置适配/配置读取.py | 8 -、项目适配层/配置适配/配置适配.py | 20 -
- 期望修法：仍成立（历史事实，可复算）

**14. §二依据5 目录/文件撞名是「看着像重复」的唯一来源（已消除）**

- 现场证据：支持库/适配层/提供者配置读取/ 存在（4文件），支持库/适配层/配置读取 不存在；门面层仍保留文件 项目适配层/配置适配/配置读取.py（撞名只剩「目录名 vs 文件名」一侧已消失）；提供者配置读取/__init__.py:1 docstring原文即「提供者配置读取包导出」
- 期望修法：仍成立

**15. §二依据6 引用面实测（提供者层0处；门面层4处）——数字相同、位置漂移**

- 现场证据：提供者层：全仓 grep 适配层.配置读取 命中0；适配层.提供者配置读取 只命中自身（__init__.py:15、提供者配置读取.py:19）+门禁测试字面量（测试中心/开发工具/测试_公开调用完整性门禁.py:123-124）；门面层外部import恰4处：启动监督器/健康监督/核心.py:51,52,57（旧 健康监督.py:40,41,46）、开发工具/组件合规/合规测试包.py:439（旧646）
- 期望修法：仍成立；引用前请按新路径取数

**16. §三3.1 目录改名（提供者层唯一实质动作）**

- 现场证据：新目录4文件：__init__.py、提供者配置读取.py、配置读取工具.py、说明/设计说明.md
- 期望修法：仍成立

**17. §三3.2 两侧新增设计说明**

- 现场证据：支持库/适配层/提供者配置读取/说明/设计说明.md、项目适配层/配置适配/说明/设计说明.md 均在
- 期望修法：仍成立

**18. §三3.4 有意未改项**

- 现场证据：项目适配层/配置适配/配置适配.py:23 def 读取配置 仍在、零调用方（grep 读取配置 除同名变体外无调用）；支持库/适配层/提供者配置/提供者配置.json 未动
- 期望修法：仍成立

**19. §四 反向验证结论（旧名失效、新名可达、__all__不变）**

- 现场证据：实测 from 支持库.适配层.提供者配置读取 import 读取提供者配置,获取能力配置 → 新包OK；import 支持库.适配层.配置读取 → ModuleNotFoundError: No module named '支持库.适配层.配置读取'；两包 __all__ 打印如上
- 期望修法：结论仍成立；但复现脚本已丢：/tmp/配置双入口_反向验证.py 与 /tmp/反向验证_改前.txt 均 No such file or directory ⇒ §七验收命令里的 diff 那条已不可复跑

**20. §四4.3 另三条轻检测——计数一致**

- 现场证据：三套合跑 Ran 58 tests（=26+18+14，与旧报告26/18/14逐项一致），1失败＝测试_依赖防火墙口径.test_反向_撤掉层授权必须真的出现越层违规（AssertionError: 2 != 0，前提0违规被破，见发现1）；健康配置/健康监督两套全绿
- 期望修法：本批未引入；属新状态，应并入未完成事项

**21. §五 对上游方案的纠正建议——前提未变**

- 现场证据：提供者层0调用方、门面层4处import、__all__不交集、层表不许合并——四条前提 HEAD 实测全部未变
- 期望修法：仍成立；建议保留

#### 【已消解】6 条

**1. §1·背-4 网关层五维限流上限全是构造默认值（无配置文件、无环境变量入口）——部分已消解**

- 现场证据：安全/限流器.py:72-84 五维默认值仍在（最大并发请求50/最大任务数20/最大流式连接10/单维度并发40/窗口秒10.0），但生产已不再吃默认：运行核心/启动运行核心网关.py:141 限流器实例=限流器(**_限流器参数)，参数来自 运行核心/自适应选型/并发自适应.py:224-237（最大并发请求/单维度并发=硬件画像值）；限流器.py 内 grep os.environ 命中0
- 依据：已消解部分：并发两维已由装配注入（2026-09-21 裁决，实测60路并发200×40+429×20卡在40）；未消解部分：最大任务数=20、最大流式连接=10 仍无注入方且对应闸门无调用方（见背-14/N7）

**2. §5 复算脚本可复现性——可复现性已丢失**

- 现场证据：unset PYTHONPATH; python3.14 /private/tmp/系统平台临时脚本_20260918/b8_verify_v2.py：该目录现存内容不含任何 b8_*.py（ls实测只有05_/08_/b1_/clone_等别的会话脚本）
- 依据：依据：脚本未随报告入库、/private/tmp 被清理 ⇒ 198锚点复算已无法按原命令复跑；本文改用「HEAD快照+内容定位」重核，行号以本批为准

**3. §三3.3 中央名录与文档同步（5处）——已扩到5/5**

- 现场证据：①门禁名录已新名且已搬到 开发工具/公开调用完整性门禁_包发现.py:54-59（系统适配器名表=frozenset({"提供者配置读取",…})），开发工具/公开调用完整性门禁.py:111 只是再导出；②测试字面量已新名（:104,105,121,123,124 共5处，旧报告记3处）；③支持库/适配层/说明/README.md:35,71,74,131 已同步；④运行核心/依赖防火墙.py:652 注释已是 提供者配置读取/；⑤AGENTS.md:472 也已是「提供者配置读取」（旧报告记为未改、阻塞）
- 依据：已消解；AGENTS.md grep 配置读取 仅命中 :472 这一行新名

**4. §四4.3 轻检测（门禁48 tests：46 OK+2预存失败）——2个预存失败已修**

- 现场证据：unset PYTHONPATH; python3.14 -m unittest 测试中心.开发工具.测试_公开调用完整性门禁 → Ran 49 tests … OK（49全绿）；缺口根因同步：支持库/后端/组件规范支持库/说明/使用说明.md:7 版本已是1.2.0 = 能力定义.json:3 版本
- 依据：已消解；引用前按49计数重跑

**5. §六待办1 AGENTS.md:275 名录行未同步（阻塞，需华哥手动改）**

- 现场证据：AGENTS.md:472（现址）「系统适配器豁免：…（提供者配置读取、配置契约、…）」，grep -n "配置读取" AGENTS.md 仅此一行；门禁侧 开发工具/公开调用完整性门禁_包发现.py:54 同值 ⇒ 名录两侧现已同源
- 依据：已消解，可从待办删除

**6. §六待办2 依赖分两段.md（生成物）名字待重跑**

- 现场证据：支持库/适配层/说明/依赖分两段.md:59 条目已是「| 提供者配置读取 | 内部件 | 支持库/适配层/提供者配置读取 | …」（旧名0命中）⇒ 该生成物已重跑过
- 依据：已消解；若仍需门禁接入，那是 未完成事项.md:97 的另一条（依赖分两段声明件未被任何门禁调用），与本批无关

#### 【不是缺陷】1 条

**1. 邻域：未完成事项.md:249 幂等表 LRU 名实不符裁决**

- 现场证据：开发文档/未完成事项.md:249 已就「幂等表LRU名实不符」做裁决（实为FIFO，现场注释已无LRU字样）
- 依据：依据：登记口径即「不是缺陷」；上限缺失本身仍是N8，两条不冲突

### 3 · 批D · 测试中心归口盘点_20260918.md + 超长文件拆分候选_20260918.md + 缺陷热点巡诊_结论_20260919.md

#### 【仍成立·真缺陷】6 条

**1. §2.2【A 桶】真会断/真会过期的引用 23 处 / 12 文件**

- 现场证据：12 个文件全部 `git show HEAD:` 成功，逐条 grep 命中：AGENTS.md:122/126（3 处，第一批维修.测试_HTML验证器 ×2 + 测试_客户端构建安全 ×1）；公共契约/HTTP路由契约.json:93/103/111/121/133/141（6 处）；公共契约/能力契约/契约指纹.py:10；平台控制面/提供者/进程提供者.py:156/191；开发工具/测试体系门禁.py:264；开发工具/统一能力入口/视图/错误码与建议口径说明.md:81/88；支持库/适配层/TreeSitter提供者/实现/提供者.py:183；同包 生命周期契约.json:17；同包 说明/目录树与命令.md:34；运行核心/依赖防火墙.py:194/766；运行核心/加载器/提供者隔离/独立进程.py（2 处，内容已换）；运行核心/运行诊断/诊断中心/诊断中心.py:58。合计 23 处 / 12 文件，与原文逐条同量。
- 期望修法：A 桶清单作为「若执行搬运则必改」的真源仍有效，且需补 1 处新发现：开发工具/发布门禁/运行发布门禁_防回潮判据.py:296 残留 `测试_视图错误码键一致.py` docstring 片段。注：第 11 项内容已换 —— 原文记 独立进程.py:401/437 的 测试_Provider进程.py::test_P0_14，HEAD 上已改为 测试中心/运行核心/测试_硬件画像与档位.py（:70/:100），处数仍 2 故总数不变。

**2. §5.1【高】既有归口方案自身存在判据自相矛盾（同一文档 :381 要搬 vs :721/:725/:747 不搬）**

- 现场证据：`git grep -n "测试_视图错误码键一致" HEAD -- 开发文档/方案/测试中心归口方案_20260918.md`：`:383`（行号漂移，原 :381）映射表写「平台控制面/测试_视图错误码键一致.py → 开发工具/测试_视图错误码键一致.py」＝**要搬**；`:723`（原 :721）风险清单写「✅ 域名与文件名都不变，零改动」＝**不搬**；`:727`「这 8 个域名与 测试_视图错误码键一致.py 这个文件名此后不许再改」；`:749`「本方案（按域归位）本就不动它，故迁移可安全进行」。`find 测试中心 -name '测试_视图错误码键一致.py'` 命中 测试中心/平台控制面/（即执行了「不搬」这一支），映射表那一支仍挂着。
- 期望修法：改方案正文、不改测试：把 :383 那行映射删掉或加「（已裁定不搬，2026-09-18）」注记。理由：:727 的「此后不许再改」已写进判据，且生产侧硬依赖已移除（见下条），此时选「不搬」代价为零。

**3. §5.1 附带「另有一条独立在途红」：测试中心.第一批维修.测试_发布门禁 实测 Ran 12 → 1 FAIL（test_删除测试中心后生产门禁仍可导入并执行制品选择）**

- 现场证据：HEAD 上真跑：`unset PYTHONPATH; python3.14 -m unittest 测试中心.第一批维修.测试_发布门禁.生产门禁独立性测试` → FAILED (failures=1)，报错 `AssertionError: '测试中心' unexpectedly found in '运行发布门禁_逐包合规.py'`，断言在 测试中心/第一批维修/测试_发布门禁.py:213。断言原文 :210-213 = `for 文件 in (系统根/"开发工具"/"发布门禁").glob("*.py"): self.assertNotIn("测试中心", 文本, 文件.name)`。**恶化证据**：`git grep -c 测试中心 HEAD -- 开发工具/发布门禁/` 显示 3 个文件含该字样（运行发布门禁.py ×3 :105/:243/:342、运行发布门禁_逐包合规.py ×1 :32、运行发布门禁_防回潮判据.py ×1 :6）—— 原文只 1 个文件违规，拆分把注释复制到了新文件里。
- 期望修法：这条是「判据在、被注释撞红」，不是功能缺陷。两条路：① 把 5 处注释里的「测试中心」改成「测试侧/测试目录」等中性表述（最小改动，判据不动）；② 把断言从「源码文本不得含字符串」升级为「AST 里不得出现指向 测试中心/ 的路径字面量」。**倾向 ①** —— 判据立法本意是「生产门禁不依赖测试目录」，注释不是依赖。

**4. 第一优先 #2 支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py（原 1667 行 / 35 次；表内自述「🟢 已拆完（2026-09-19）：1591 → 744 行（≤800 达标）」）**

- 现场证据：`git show HEAD:…模型连接器.py | wc -l` = **895**；`git log --oneline --follow` = 43。同目录已有 9 个拆分兄弟件：模型连接基元.py / 环境配置.py / 本地启动准备与守卫.py / 模型工具调度.py / 模型探针.py / 模型路由.py / 模型用量.py / 模型供应链校验.py / 响应归一化.py。744 → 895，**重新越过 800 线**；③ 的独立复算也把它列为修复≥5 且 >800 行的 5 个之一（修复 13 / 总 38 / 修复率 0.34 / 热度 11635，全仓第 3）。
- 期望修法：按原表已识别的两条「不拆」约束（本地模型启动器的进程与句柄段、「句柄与连接管理」公共底座）之外重找落点：优先看 模型路由.py / 模型用量.py / 模型供应链校验.py 三个已独立职责件能否再吸收主文件对应段。**注意**：原文警告 `_HTTP调用模型` 的 NameError 是刻意保留（修它会动 3 个测试判定），拆分时别顺手修。

**5. 第三·低频观察 技能库/后端/技能库/实现/技能库.py（845 行 / 7 次，原判：提交 ≤7 按 6.1 低频不拆）**

- 现场证据：`git show HEAD:技能库/后端/技能库/实现/技能库.py | wc -l` = **936**；`git log --oneline --follow` = 11。行数 845 → 936（离 800 线更远），提交次数 7 → 11，**跨过 10–19 次的第二优先分档边界**，原判「低频（≤7）」的前提已不满足。
- 期望修法：按第二优先重估：先看 11 次提交里修复类占多少（本批 ③ 复算中该文件未进热点榜 TOP15，说明修复次数偏低）——若近期提交只是功能增量可维持观察；若继续攀升，按职责拆（技能注册 / 技能执行 / 技能发现）。

**6. 结论 2「本轮 15 路拆完后，『修复≥5 且 >800 行』的已经全部清零」**

- 现场证据：复算 `修复≥5 且 行数>800`：**5 个**（原文断言 = 0 个，仅 运行发布门禁 除外）——13/38/0.34/895行/11635 支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py；9/19/0.47/822行/7398 支持库/后端/系统核心支持库/进程管理/实现/进程管理.py；5/13/0.39/1464行/7320 公共契约/运行时/平台适配.py；9/16/0.56/801行/7209 开发工具/契约编译/能力定义编译器.py；6/16/0.38/811行/4866 运行核心/依赖防火墙.py。统计面共 742 个曾修复过的 .py。
- 期望修法：「清零」结论已被推翻。把 模型连接器.py（895）、进程管理.py（822）、能力定义编译器.py（801）补进拆分候选；平台适配.py（1464 行但只 13 次提交、修复率 0.39）与 依赖防火墙.py（811 行 / 16 次提交）作第二档。注：模型连接器.py 与 ②-2-2 是同一文件，两条证据互相印证它真的长回去了。

#### 【仍成立·数字过期】1 条

**1. 结论 3「真正的天生易错区是测试文件和小文件，不是大文件」（修复率 ≥0.5 且修复 ≥5 的有 40 个）**

- 现场证据：复算 `修复率≥0.5 且 修复≥5` = **52 个**（原 40）。其中 测试中心/ 开头 **17** 个；平均行数 **385** 行；>800 行的仅 **1** 个（开发工具/契约编译/能力定义编译器.py，801 行）。易错榜前 5：0.88 (7/8) 182行 测试中心/开发工具/测试_反向破坏门禁28.py；0.86 (6/7) 155行 支持库/后端/大语言模型支持库/嵌入缓存/实现/嵌入缓存.py；0.83 (10/12) 394行 测试中心/发布门禁/测试_发布门禁收敛.py；0.83 (5/6) 320行 开发工具/复用审计/能力调用图审计.py；0.83 (5/6) 273行 平台控制面/能力目录/文件租约存储.py。
- 期望修法：方向完全未变（榜前 5 全是 ≤394 行小件，其中 2 个测试文件；52 个里只 1 个超 800 行），但原文点名的 4 个文件有 2 个数字已漂：测试中心/模块库/测试_自修复工具.py 0.89(8/9)/323行 → 0.67(10/15)/590行；模块库/自修复工具/__init__.py 0.73(8/11)/125行 → 0.588(10/17)/159行（已跌出 ≥0.7 组）。另 2 个逐项全同：测试_发布门禁收敛.py 0.83(10/12)/394行；自修复工具.py 0.69(9/13)/254行。期望：结论方向保留，两条漂了的按新数字重述（测试_自修复工具 行数翻倍说明它其实该拆了）。

#### 【仍成立·建议未落地】3 条

**1. 结论 3 的行动「对修复率 ≥0.7 的做的是判据/契约固化，不是拆分」（原文 8 个）**

- 现场证据：复算 修复率≥0.7 的 = **15 个**（原 8 个），且这 15 个**全部 ≤800 行**（0 个需要拆分）—— 与「该固化不该拆」的判据完全一致。
- 期望修法：按 15 个排期，优先 测试中心/开发工具/测试_反向破坏门禁28.py（0.88）、嵌入缓存.py（0.86）、测试中心/发布门禁/测试_发布门禁收敛.py（0.83）。原文举例的 模块库/自修复工具/（__init__.py 0.73 + 实现 0.69）现已分别降到 0.588 / 0.692 —— 可作为「固化有效」的正面样本。

**2. §五 建议 2「对修复率 ≥0.7 且 ≤800 行的 8 个文件做判据固化（不是拆分）」**

- 现场证据：这类文件从 8 个增至 **15 个**（复算见结论 3 行动条），全仓无对应的「契约层冻结」落地记录。
- 期望修法：同结论 3 行动条：按 15 个排期，优先 0.88 / 0.86 / 0.83 三个。

**3. §五 建议 3「把『缺陷 hotspot』纳入常态巡诊 —— 与 开发效率巡检.py 并列，按周跑一次看趋势」**

- 现场证据：工具侧已落地：`git grep -n 缺陷热点巡诊 HEAD -- 开发文档/规范/任务类型规范.json` 命中 :45「查哪块代码经常出 bug —— python3.14 开发工具/缺陷热点巡诊.py（修复类提交×行数排序、修复率、根因分布、趋势；--html 出网页）」；开发文档/决策记录/0052_缺陷热点巡诊工具与门禁收口重建.md 明写「新增 开发工具/缺陷热点巡诊.py（正式工具，不是临时脚本）」。但「按周跑」无接线证据：`git ls-tree -r --name-only HEAD | grep 巡检` 只有 开发工具/开发效率巡检.py、模块库/自修复工具/实现/效率巡检.py、开发文档/分析/巡检_修复效率归因_20260921.md；全仓 缺陷热点巡诊 的引用只有工具自身、决策记录 0052、两份分析件、未完成事项.md、任务类型规范.json —— 无任何定时/巡检编排把它挂上。
- 期望修法：要么把该工具挂进 开发工具/开发效率巡检.py 的一条检查项（同一入口，天然获得「按周跑」），要么在 开发文档/未完成事项.md 明写「暂不常态接线，按需手跑」，避免这条建议悬空。

#### 【仍成立·断言为真非缺陷】3 条

**1. §1.3「真正的归口对象是 4 个批次/旧层命名的有内容目录（38 文件）」**

- 现场证据：逐目录文件数：`测试中心/第一批维修/` 30、`测试中心/加载器/` 5、`测试中心/发布门禁/` 4、`测试中心/底座升级/` 1（合计 40，比原 38 多 2 为在途新增）。四个目录全在且全部有内容；顶层仍未出现「阶段命名 + 有内容」的并存。
- 期望修法：该条已被 b2f936e5（2026-09-18 04:54）正式销项判「不搬」，故不再作为待办；若将来重启，须按 §2.2 的 A 桶 23 处 / 12 文件清单同批改写。

**2. 结论 1 附带判断「不是行数大所以要拆，是改一次修一次」（原文修复率 0.53）**

- 现场证据：复算 运行发布门禁.py：修复 **34** / 总 63 = **修复率 0.54**，**仍是全仓修复率最高的高频大件**（第二名 本地网关.py 0.46，第三名 合规测试包.py 0.61 但只有 23 次提交）。
- 期望修法：判据从「拆分」改为「判据/契约固化」：该文件已 ≤800 行，继续拆收益递减；应按原文 §五 建议 2 路线，把反复返工的那几条判据（防回潮存量基线、英文键冻结、注册口径存量基线）提到契约层冻结。

**3. §四 根因分布（合计 637 次归类；契约与类型 + 门禁与判据 + 生成物同步 = 54.4%）**

- 现场证据：用同一套 根因表 正则复算，合计 **769** 次归类（原 637）：门禁与判据 158 (20.5%)←124 (19.5%)；契约与类型 153 (19.9%)←129 (20.3%)；文档漂移 123 (16.0%)←105 (16.5%)；生成物同步 109 (14.2%)←93 (14.6%)；并发与竞态 87 (11.3%)←73 (11.5%)；路径与编码 72 (9.4%)←58 (9.1%)；超时与性能 38 (4.9%)←29 (4.6%)；跨平台 29 (3.8%)←26 (4.1%)。三项合计 19.9+20.5+14.2 = **54.6%**（原 54.4%）。
- 期望修法：无（结论稳定）。分布几乎逐项不变，只前两名互换（门禁与判据 124 → 158 反超契约与类型）。这是本批三份件里最稳的一条，可作「判据没冻结」这一根因判断的长期证据。

#### 【已消解】18 条

**1. §1.1/§1.2/§1.4「顶层 28 个目录 = 能力域 9 + 纯阶段名 10 + 批次旧层 9」**

- 现场证据：`find 测试中心 -maxdepth 1 -type d -not -name __pycache__ | wc -l` = 17（含 测试中心 自身 → 子目录 16）。`ls -1 测试中心/` = __init__.py 公共契约 加载器 发布门禁 启动监督器 平台控制面 底座升级 开发工具 慢速层 技能库 支持库 模块库 示例项目 第一批维修 辅助 运行核心 项目适配。第三阶段…第十三阶段、权威状态/、结构迁移/、资源并发/ 全部不在；`git ls-tree -r --name-only HEAD 测试中心/` 的目录集合里也没有这些名字（确认非工作树脏读）。新增 测试中心/技能库/（d44be028 引入 测试_交付收尾网关腿.py）。
- 依据：消解证据：28 → 16 个子目录，纯阶段名 10 → 0，能力域命名 9 → 14。原文「28 个顶层目录」的实测口径已无对象。

**2. §1.1/§1.4「空壳 15 个顶层 / 19 个全树空目录」**

- 现场证据：`find 测试中心 -type d -empty | sort` 仅 2 条：测试中心/慢速层、测试中心/辅助；`wc -l` = 2。原 15 个空壳顶层里 13 个已删（10 个阶段目录 + 权威状态/结构迁移/资源并发），慢速层/辅助 下 6 个空壳子目录已删（故两父目录现在自己成了 -empty）。注：原文档「顶层空壳 15 + 嵌套 6」与「全树 19」并不矛盾 —— 慢速层/辅助 因含（空）子目录而不被 `find -type d -empty` 计入，故 19 = (15-2) + 6。
- 依据：残留修法（零风险）：文档 §三 B1 命令原样可用，现只剩 2 条 —— `find 测试中心 -type d -empty -delete`。四条「零引用」硬依据仍成立（见 1-6 条）。

**3. §2.3「15 个空壳目录被源码/配置引用 = 0 处」四条独立证据**

- 现场证据：对象只剩 2 个空目录。`git grep -n "慢速层|测试中心/辅助"` HEAD 仅 3 条：开发工具/发布门禁/运行发布门禁_执行项分组_制品与流程.py:284（说明性注释「慢速层真实执行 已移除」，非路径引用）、开发文档/归档/202608_第十四阶段/工作包清单.json:367（归档件，12.4⑤ 不作派活依据）、以及本文自身。`git ls-tree -r HEAD 测试中心/ | grep -E '第三阶段|慢速层|权威状态|结构迁移|资源并发'` 只剩 2 条文件名（测试_权威状态门禁.py、测试_第十四阶段直连规则.py），非目录。第 4 条证据仍成立：开发工具/测试体系门禁实现/发现.py:68 与 开发工具/测试伪装门禁实现/发现.py:56 仍是 测试中心.rglob(测试文件模式)。
- 依据：15 个空壳已删 13 个，「零引用」结论不再有对象；残留 2 个仍满足零引用。

**4. §1.4 数量总表（测试中心 内 .py 209 / 测试_*.py 202 / 非 测试_ 的 .py 7 / 测试_工作包NN_ 11）**

- 现场证据：`find 测试中心 -name '*.py' -not -path '*__pycache__*' | wc -l` = 247（原 209）；`-name '测试_*.py'` = 240（原 202）。`git ls-tree -r HEAD 测试中心/ | grep '\.py$' | grep -v '/测试_'` 仍是 **7** 个：6 个 __init__.py + 运行核心/冷启动脚本.py（与原口径逐项一致）。`grep -c "测试_工作包"` = 11，且全部在 测试中心/平台控制面/（工作包01…11）。
- 依据：消解证据：209/202 两行已消解；「非 测试_ 的 7」与「工作包 11」两行仍成立。

**5. §2.4 门禁与目录约定的实际依赖表（7 行，其中 运行发布门禁.py:930 依赖三段路径全等）**

- 现场证据：其余 6 行逐行复核仍在：开发工具/测试体系门禁实现/发现.py:68、开发工具/测试伪装门禁实现/发现.py:56 均为 `sorted(测试中心.rglob(测试文件名模式))`；开发工具/测试伪装门禁实现/命名空间.py:38 `测试自身根 = ("测试中心",)`；开发工具/组件合规/模块合规.py:33 `系统根 = next(祖先 for 祖先 in Path(__file__).resolve().parents…`；AGENTS.md 只依赖顶层名。唯一硬依赖那行已消失：`git grep -n "_视图登记源" HEAD` 在 开发工具/ 下只命中一条注释 —— 开发工具/发布门禁/运行发布门禁_防回潮公共.py:69「`_视图登记源` 已移除（2026-09-18 修根）：它原先读测试侧的用例文件，但那三张表本身就在生产视图里…」。
- 依据：消解证据：表内结论「门禁体系不对阶段目录或任何子域名做约定」仍成立且更强（连唯一的三段路径硬读都没了）。

**6. §三 方案 A（全量搬运）/ 方案 B（只删空壳）与「先执行 B1，B2 与方案 A 一起交华哥排期」**

- 现场证据：`git show --stat b2f936e5` = 「销项：测试中心归口盘点完成，判定「不搬」（附三条硬依据）」，只改 开发文档/未完成事项.md 1 行；其 diff 把 §8.4 第 8 项从「**测试中心/ 30 个阶段目录按能力域归口**」改为 ~~删除线~~ + 「✅ 已盘点完成，判定不搬（2026-09-18）」。B1 实质已执行：19 个空目录只剩 2 个，用的就是 §三 B1 的 `find 测试中心 -type d -empty -delete`。方案 A 未执行：4 个批次/旧层目录与 40 文件原样在位。
- 依据：文档 §三/§六 的「❌ 未执行」状态位应改为「B1 已执行、A 已销项判不搬」，并把「顶层 28 → 13」更新为 16（清完残留 2 个空壳后为 14）。

**7. §5.1 后半【生产侧硬伤】运行发布门禁.py:930 按三段路径硬读 测试_视图错误码键一致.py，按方案搬会让门禁读不到真源变红**

- 现场证据：`_视图登记源` 已从 运行发布门禁.py 移除，改为 运行发布门禁_防回潮公共.py:69 注释明写「已移除（2026-09-18 修根）…① 生产门禁不得依赖任何测试目录（有独立断言守着这条）；② 测试文件里的键是它自己从生产视图读出来的结果，不是真源」；真源改为生产视图三表 + 同文件 :79 `_冻结英文键表`。发布门禁已不读任何测试文件路径，「搬它会门禁红」的因果链已断。
- 依据：残留文档漂移（新发现）：运行发布门禁_防回潮判据.py:296 留着半截 docstring「测试_视图错误码键一致.py`` 的 ``英文中文对应`` ∪ ``无中文对应的英文键``）」，且上一行指路的 `运行发布门禁.py::_冻结英文键表` 也是错的（该常量实际在 运行发布门禁_防回潮公共.py:79）。期望：删残留片段 + 改指路。

**8. §5.2【中】与并行在途改动对撞（测试中心/ 80 项未提交，加载器/4 + 发布门禁/1 正被改）**

- 现场证据：开工时 `git status --short` = 2 项（模块库/知识检索 两文件），`测试中心/` 下**零**未提交项；收尾时虽涨到数百项（并行会话重生成 */说明/*.md），但 `测试中心/` 仍未出现其中。对撞前提消失。
- 依据：无。此条可从「风险清单」降级为历史记录。

**9. §5.3【中】任务口径与现场不一致（任务标题写「30 个阶段目录」，实测顶层 28）**

- 现场证据：b2f936e5 的 开发文档/未完成事项.md diff 已把 §8.4 第 8 项逐字回填为「实测顶层 28 个、阶段命名 10 个且**全是空壳**；真正有内容的批次/旧层目录 4 个」，并采纳了 §5.3 建议的改述口径。
- 依据：派活口径已回填且该项已销项，不会再出现「照 30 个阶段目录派活找不到对象」。

**10. §六 交付物与状态表（「目录搬运 ❌ 未执行」「空壳目录删除 ❌ 未执行」）**

- 现场证据：空壳删除已实质执行（19 → 2，见 §1.4 条）；目录搬运已由 b2f936e5 正式销项判「不搬」。两个 ❌ 都已不是「未执行」。
- 依据：状态位应改为「已执行 / 已裁定不做」。

**11. 第一优先 #1 开发工具/发布门禁/运行发布门禁.py（2626 行 / 49 次，拆分方向：按检查项簇拆）**

- 现场证据：`git show HEAD:… | wc -l` = **688**；`git log --oneline --follow -- … | wc -l` = 63。`ls -1 开发工具/发布门禁/` 显示拆出 14 个兄弟件：发布治理.py / 契约变更门禁.py / 权威状态门禁.py / 第十四阶段门禁.py / 运行发布门禁_制品验证.py / _反向破坏.py / _底座.py / _执行项分组_制品与流程.py / _执行项分组_真跑.py / _源码边界与待检清单.py / _逐包合规.py / _防回潮公共.py / _防回潮判据.py + 防回潮存量基线.json。
- 依据：消解证据：2626 → 688 行（≤800 达标），拆分方向与原表一致。

**12. 第一优先 #3 运行核心/统一网关/网关核心.py（1338 行 / 40 次；自述已拆到 99 行）**

- 现场证据：`git show HEAD:运行核心/统一网关/网关核心.py | wc -l` = **105**；`git log --oneline --follow` = 44。
- 依据：消解证据：105 行（宿主只剩装配骨架），与原表「99 行」同量级，差异是后续增量。

**13. 第一优先 #4 运行核心/统一网关/本地网关.py（1156 行 / 30 次，拆分方向：按「转发 / 流式 / 审计」拆）**

- 现场证据：`git show HEAD:运行核心/统一网关/本地网关.py | wc -l` = **720**；`git log --oneline --follow` = 48。
- 依据：消解证据：1156 → 720 行（≤800 达标）。提交次数 30 → 48 说明「高频」未变，但读取成本已降；③ 复算中它是热度第二名（修复 22 / 总 48 / 修复率 0.46 / 热度 15840）。

**14. 第一优先 #5 平台控制面/包仓库/平台客户端制品.py（963 行 / 20 次；自述已拆到 520 行）**

- 现场证据：`git show HEAD:平台控制面/包仓库/平台客户端制品.py | wc -l` = **527**；`git log --oneline --follow` = 23。
- 依据：消解证据：527 行，与原表「520 行」同量级。

**15. 第二优先（提交 10–19 次且 >800 行）9 个文件：公开调用完整性门禁.py / 权威状态.py / 契约编译/漂移检测.py / 并发控制.py / 组件合规/合规测试包.py / 任务调度/任务进程.py / 系统核心支持库/资源管理/实现/资源管理.py / 客户端/构建平台客户端.py / 后端核心/后端核心.py**

- 现场证据：逐文件复算（格式 行数/提交次数，括号内为原值）：690/25（1533/17）开发工具/公开调用完整性门禁.py；243/24（1259/16）运行核心/权威状态.py；552/17（2150/11）开发工具/契约编译/漂移检测.py；269/12（1016/10）支持库/后端/并发控制支持库/实现/并发控制.py；792/23（998/19）开发工具/组件合规/合规测试包.py；367/18（930/13）运行核心/任务调度/任务进程.py；550/18（874/14）支持库/后端/系统核心支持库/资源管理/实现/资源管理.py；522/24（868/18）客户端/构建平台客户端.py；715/23（811/18）后端核心/后端核心.py。
- 依据：消解证据：9 个全部降到 800 行以下。最接近的是 合规测试包.py 792 行（余量仅 8 行）——值得盯；降幅最大是 漂移检测.py 2150 → 552。

**16. 第三·低频观察 开发工具/环境自检.py（1120 行 / 1 次）**

- 现场证据：`git show HEAD:开发工具/环境自检.py | wc -l` = **27**；`git log --oneline --follow` = 4。开发文档/未完成事项.md:192 明确记为「入口腿」（真实现唯一在包内 核心.py、平铺件只做自举 + 等价再导出）。
- 依据：消解证据：1120 → 27 行。

**17. 结论 1「运行发布门禁.py 是唯一的超一级热点，必须先拆」（修复 32 / 总 60 / 行数 3336 / 热度 106752，比第二名高 8.9 倍）**

- 现场证据：按 开发工具/缺陷热点巡诊.py 的口径重实现（修复词正则、`git log --no-merges --name-only --format=%x01%s` 一次遍历、排除 工程缓存/开发文档/归档/__pycache__、热度 = 修复提交 × HEAD 行数；脚本 /tmp/巡诊复算.py，只写 /tmp，未跑 --html 以免写 工程缓存/）。复算 TOP2：34/63/0.54/**688**/**23392** 开发工具/发布门禁/运行发布门禁.py；22/48/0.46/720/15840 运行核心/统一网关/本地网关.py。行数 3336 → 688，热度 106752 → 23392，与第二名倍数 8.9 → **1.48**。
- 依据：消解证据：文件已拆（见 ②-2-1 条），「超一级」「8.9 倍」「必须先拆」三个量词都不再成立。它仍是第一，但已是普通第一名。

**18. §五 建议 1「拆 运行发布门禁.py（3336 行）—— 唯一超一级热点，收益最大」**

- 现场证据：现为 **688 行**（见结论 1 条与 ②-2-1 条），`ls -1 开发工具/发布门禁/` 显示已按检查项簇拆出 14 个兄弟件。
- 依据：该项已开工并完成。

#### 【不是缺陷】4 条

**1. §0 快照锚点（HEAD 96a09976 / 全仓未提交 367 / 测试中心未提交 80 / 测试资产 md5 3f7482964b4b1542 / 扫描面 3340 文件）**

- 现场证据：`git rev-parse HEAD` = 925578278be8ad9b0de2b3b0e156daae614543d6；开工时 `git status --short` 仅 2 项（模块库/知识检索 两文件），收尾时涨到数百项（并行会话重生成 */说明/*.md + 2 未追踪新件）。锚点数字已全部不可复现。
- 依据：§0 是文档自述的「复算必须对齐这一行」的时点锚点，作用是让当时数字可复现，不是结论；文档自带「⚠ 快照期间有并行子代理在改 测试中心/」警示。锚点过期属正常，不构成缺陷。

**2. §5.5【低】C 桶 96 处 / D 桶 5 处一律不改 + 方案/裸布尔迁移分批方案_20260917.md（42 处）需追加迁移注记**

- 现场证据：`git grep -c "测试中心/第一批维修" HEAD -- 开发文档/方案/裸布尔迁移分批方案_20260917.md` = 20（原 42，该方案自身被裁过），仍是本仓最大引用面之一。
- 依据：按 12.4⑤「历史由 git 保留」，C/D 桶不改是正确口径，不是缺陷。追加迁移注记的前提是执行搬运，而搬运已销项判「不搬」，故注记不再必要。

**3. 第三·低频观察 公共契约/运行时/进程终止.py（835 行 / 5 次，原判：不拆）**

- 现场证据：`git show HEAD:公共契约/运行时/进程终止.py | wc -l` = **867**；`git log --oneline --follow` = 7。
- 依据：依据：867 行、7 次提交，**仍满足原判据「提交 ≤7 次、低频文件到 800 行也无所谓（哲学 6.1）」**，原文给的判定是「不拆」，该判定在 HEAD 上依然适用（行数 +32、次数 +2，边界未破）。

**4. §复现命令（`find … -exec wc -l … | awk '$1>800'` + `git log --oneline --follow -- <文件> | wc -l`）**

- 现场证据：两条命令在 HEAD 上原样可跑，本批全部复算即用它：17 个文件的 `--follow` 全部返回非零次数（重命名后仍能追到旧历史）。
- 依据：依据：口径可靠、命令可复现，不构成缺陷。

### 4 · 批E · 能力面与拆分清单_20260918.md + 备份恢复演练实测_20260918.md

#### 【仍成立·真缺陷】1 条

**1. §五 缺口3（演练没有定期跑）—— 核是否已加定期任务**

- 现场证据：`crontab -l` 仅 1 条（OpenCode 自我进化），无演练条目；`ls ~/Library/LaunchAgents` 两个 backup plist 均非本平台（personal-distillation-backup → 人脉数据系统/脚本/备份正式库.sh；huashi_v1_daily_backup → ~/.huashiwangzu/daily_backup.sh）；`git grep -ln 备份恢复演练 HEAD` 仅命中 自身/核对/设计说明/决策记录0043/本报告/方案/未完成事项；`git grep -ln '备份恢复演练|执行备份|校验备份' HEAD -- 开发工具/发布门禁/` 仅 第十四阶段门禁.py（即报告所述「临时目录假数据」腿）；`git ls-tree -r HEAD | grep -i 'plist|cron|定时|周期任务'` 零命中。
- 期望修法：仍成立。期望修法：① 挂 launchd 定期任务跑 `unset PYTHONPATH; python3.14 开发工具/备份恢复演练.py --规模 N --反向验证` 并按退出码(0/3/2/4)判红留日志；② 或接发布前必跑清单（接线点 开发工具/发布门禁/，须另开裁决窗口）。

#### 【仍成立·数字过期】12 条

**1. §0/§1.1 口径裁定「830 作废，装配口径为准」**

- 现场证据：基准快照 /tmp/基准快照（git archive 9255782 解出）装配：`状态= 结果(成功=True, 值='后端核心已就绪，746 个能力')`、`能力数= 746`；同快照 rglob：`包声明份数= 146 累加= 990 去重= 746 重复= 244`。报告值 679/845/166，830 仍非任一实测值；未完成事项.md:120 条目 #7 仍登记该裁定。
- 期望修法：仍成立（数字过期）：679→746、845→990、166→244，补包声明份数 146；裁定文字与纪律（不得引用 rglob 累加值/830）一字不改。

**2. §1.2 装配口径按层构成表（679：支持库515/平台控制面90/模块库65/技能库4/运行核心3/启动监督器2）**

- 现场证据：`首段= {'支持库': 556, '平台控制面': 105, '模块库': 76, '技能库': 4, '运行核心': 3, '启动监督器': 2}`（合计 746）。
- 期望修法：仍成立（数字过期）：按真值重填；§0「支持库 515 条/75.8%」「模块库仅 65 条」随之更新（方向性结论「能力增长落支持库」仍成立）。

**3. §1.3 派生事实「参与装配正式包 135 个，单能力包 21 个」**

- 现场证据：`包数= 140`、`单能力包= 21`。
- 期望修法：仍成立（数字过期）：135→140；单能力包 21 保留。

**4. §1.3 能力最多的包 Top 8**

- 现场证据：`39 支持库.后端.并发控制支持库 / 27 …模型连接器 / 23 …数据集合 / 22 …文件操作 / 22 …资源管理 / 20 平台控制面.包仓库 / 19 …项目文档支持库 / 15 …会话存储`；版本控制支持库.Git操作 已改名 支持库.适配层.Git提供者(14)。
- 期望修法：仍成立（数字过期）：重填 Top8（模型连接器 21→27、包仓库 14→20、新增项目文档支持库 19）；表下「不得与聚合父包口径并列引用」的注保留。

**5. §2.1 Top 20（提交次数 × 行数）**

- 现场证据：真值 Top3：`43344 63 688 开发工具/发布门禁/运行发布门禁.py`、`34560 48 720 运行核心/统一网关/本地网关.py`、`34010 38 895 …/模型连接器.py`；原表 4 项质变：运行发布门禁 47×2369→63×688、网关核心 35×1236→44×105、统一入口 10×629→12×112、流式HTTP 已不存在。
- 期望修法：仍成立（数字过期）：整表重生成；引用前重跑 §2 自带取证命令。

**6. §2.2「提交≥10 × 超160行 = 35 个」**

- 现场证据：`提交>=10 且 超160 = 56`；对照：`与报告§2.2清单(35)重叠 = 32`、`报告35中已掉出 = ['平台控制面/统一入口.py','运行核心/统一网关/流式HTTP.py','运行核心/统一网关/网关核心.py']`、`新增入榜 = 24`。
- 期望修法：仍成立（数字过期）：35→56；补口径提醒——该集合随新增文件增长（非测试 .py 721→964），不是「拆完归零」的指标。

**7. §2.3「提交≥15 × 超160行 = 12 个」**

- 现场证据：`提交>=15 且 超160 = 29`；全 29 条已用 `git log --oneline -- <文件> | wc -l` 逐文件复核，与单遍计数完全一致（如 63 688 运行发布门禁.py、48 720 本地网关.py、38 895 模型连接器.py、24 522 构建平台客户端.py、15 515 开发工具/薄壳/薄壳服务.py）。
- 期望修法：仍成立（数字过期）：12→29；建议删掉「与旧稿对照」列（旧稿已被 2026-09-19 拆分批次取代）。

**8. §3 总量「非测试 .py 720 → 超160行 219 个(30.4%)、合计 82,084 行；≥500 行 47」**

- 现场证据：基准快照实测：`非测试py总数= 964`、`超160行= 359`、`合计行数= 120127`、`≥500行= 59`；xargs wc -l 独立复核同为 359。报告数字在报告时刻提交 97ca5792 上可复现：`该提交 非测试py=721 / 旧超160行=219 / 旧合计行数=82260 / 旧≥500=47`，单文件抽样 2369→@97ca5792=2545、1667/1236/1156/218/755 全中 ⇒ 属真漂移非造假。
- 期望修法：仍成立（数字过期）：964 / 359（37.2%）/ 120,127 / 59。关键新结论：158 个超 160 行文件是 09-18 后新增件（期间新增 301 删 13）⇒ 阈值清单是生成侧问题，只靠事后拆不收敛。

**9. §3.1 按顶层目录分布表（219）**

- 现场证据：`96 开发工具 / 95 支持库 / 61 运行核心 / 56 平台控制面 / 23 模块库 / 13 公共契约 / 4 技能库 / 3 客户端 / 3 后端核心 / 2 项目适配层 / 2 启动监督器 / 1 前端核心` = 359。
- 期望修法：仍成立（数字过期）：重填表；报告「后端核心仅 1 个」等结论已不成立（真值 3）。

**10. §3.2 全量清单（219 个）**

- 现场证据：219 条逐条回查：`报告条数= 219  已不存在= 3  仍>160= 201  已降到<=160= 15`；3 条已不存在＝运行核心/统一网关/流式HTTP.py(755)、运行核心/统一网关/限流器.py(288)、开发工具/说明书生成/说明书生成器.py(167)。
- 期望修法：仍成立（数字过期）：201/219 至今仍 >160，表作为「当时的 219 个」忠实；但已不是当前全量（359），须整表重生成。

**11. §六 交付物「演练入口 190 行 / 核对引擎 153 行」**

- 现场证据：`wc -l /tmp/基准快照/开发工具/备份恢复演练.py` = 252；`…/备份恢复演练核对.py` = 144。演练已按 #63 改为 `_合并生产根()` 经能力 `查询备份源根` 逐类取权威宿主再合并。
- 期望修法：仍成立（数字过期）：190→252、153→144；§二「源副本＝生产数据根」表述已过时，须改为「逐类各自权威宿主合并」。

**12. §三 实测 RPO/RTO 数字表（三规模）**

- 现场证据：现行演练实测（规模 0）：`备份 5.6376s／585728 字节`、`实测 RTO 5.427s`、`实测 RPO 6.7769s`、恢复+核对 8.5102s；对照报告表规模 0 的 备份 0.0045s / RTO 0.0022s / RPO 0.9021s，差 3 个数量级（备份对象已从空库变为真实 13911 个制品）。
- 期望修法：仍成立（数字过期）：整表按「逐类权威宿主」口径重跑重填；「RTO 相对契约 591 倍余量」推论失效（真值约 11 倍）；保留「只有 2 万行实测，更大规模属外推」的诚实边界。

#### 【已消解】6 条

**1. §2.2/§2.3 点名对象 运行核心/统一网关/网关核心.py（报告 35×1236）**

- 现场证据：`wc -l /tmp/基准快照/运行核心/统一网关/网关核心.py` = 105；拆分提交 `3630368e 按职责拆到最细：网关核心 1338→99 行 + 拆分哲学补备注`、`53602a31 统一网关按职责建目录树（22 平铺 → 3 入口 + 5 职责域）`；`运行核心/统一网关/` 现 22 个 .py（传输/核心处理/安全/协议/运行态）。
- 依据：已拆分：拆到 运行核心/统一网关/{传输,核心处理,安全,协议,运行态}/ 共 22 个 .py，入口 网关核心.py 降为 105 行。

**2. §2.2/§3.2 点名对象 平台控制面/统一入口.py（报告 10×629）**

- 现场证据：`wc -l /tmp/基准快照/平台控制面/统一入口.py` = 112；`git cat-file -e 97ca5792:平台控制面/统一入口/核心.py` → `fatal: … but not in '97ca5792'`（报告期不存在）；现目录：核心.py 655 / __init__.py 135 / 实现/能力入口.py 88 / __main__.py 12。
- 依据：已拆分：内容落到 平台控制面/统一入口/ 同名目录包，平铺文件降为 112 行入口壳。

**3. §2.1/§3.2 点名对象 运行核心/统一网关/流式HTTP.py（报告 13×755）**

- 现场证据：`git ls-tree -r --name-only HEAD | grep 流式` → `运行核心/统一网关/传输/流式HTTP.py`、`运行核心/统一网关/传输/流式路由面.py`；新路径行数 810 / 199；原路径已不存在。
- 依据：已消解（搬家 + 拆出流式路由面）：新落点 运行核心/统一网关/传输/。

**4. §3.2 清单中已降到 ≤160 行的 15 条**

- 现场证据：行数变化：1236→105 网关核心.py、1120→27 开发工具/环境自检.py、702→53 运行核心/能力调用/超时执行.py、629→112 平台控制面/统一入口.py、595→55 启动监督器/健康监督.py、277→58 平台控制面/授权.py、271→42 图像解码/实现/提供者.py 等；拆分提交可查：`545e6afc 拆上帝文件：开发工具/环境自检.py（1137 行）→ 同名目录包`、`322dca6e 拆上帝文件：运行核心/能力调用/超时执行.py（1061 行）→ 同名目录包`、`77fb6d73 健康监督——真实现搬进 健康监督/核心.py（606行）、平铺降为 54 行壳`、`0bd41a7e 同名文件+同名包合并对②授权`。
- 依据：已消解：均为「同名文件 → 同名目录包」形态（环境自检/、超时执行/、健康监督/、授权/）；网关核心/统一入口见前两条。

**5. §五 缺口1（恢复到「已存在的目标目录」必假红，自称已修）—— 独立复核是否真修**

- 现场证据：代码：平台控制面/备份恢复/恢复执行.py:158-182 —— 待校验表延后收集(164/172-174)，`finally: _释放目标库(独占连接)`(178-179) 之后才跑校验(180-182)。独立探针（自写、非复跑报告）：连做 3 次恢复到同一已存在目录 → `第1次…成功=True 状态=通过 逐类原因=['完整性检查通过','54 条证据哈希全部一致','全部制品摘要一致']`、第2次同、第3次同；目标目录 = ['制品','权威状态.db','证据账本.json']。官方演练：`重新恢复成功=True → 还原后一致=True → 符合预期=True`，退出码 0。
- 依据：已消解（修复真实有效，3/3 复现）。建议把「同一目标目录连续恢复 N 次」固化为回归测试腿（目前仅演练 2 次反向验证腿覆盖）。

**6. §五 缺口2（生产数据根四类数据缺两类 ⇒ 备份恒校验不通过）—— 现场核验是否仍缺**

- 现场证据：契约已改：`git show 97ca5792:…/备份契约.py:19 四类数据表 = ("权威状态","包仓库","证据账本","项目锁")` → `git show HEAD:…/备份契约.py:36 三类数据表 = ("权威状态","包仓库","证据账本")`；新增 平台控制面/备份恢复/类根解析.py（85 行，自述「2026-09-20 修 #63」）。现场实测：`生产侧逐类根：{"权威状态":"…/工程缓存/平台控制面","证据账本":"…/工程缓存/平台控制面","包仓库":"…/工程缓存/制品仓库"}`、`校验通过=True`、`恢复成功=True（状态：通过）`、三类全绿（权威状态1281 / 证据账本55 / 包仓库13911）。原指认缺件现场仍在但已不影响：`ls 工程缓存/运行数据` 无 `制品/`、`find 工程缓存 -maxdepth 3 -name 项目锁.json` 零命中。
- 依据：已消解：契约由「四类共用一个根」改为「三类各自取权威宿主」，项目锁移出契约；不再恒校验不通过。建议把「三类各自权威宿主 + 项目锁已移出契约」写进使用说明。

#### 【不是缺陷】2 条

**1. §4「快照声明：盘点时工作区非干净」**

- 现场证据：报告第 481 行自述「盘点时工作区非干净……行数/提交次数类数字是当次快照，随在途提交会小幅漂移……引用前请重跑取证命令」。本次证真恰好证明其成立：报告期数字可复现，当前已整体漂移。
- 依据：不是缺陷（诚实声明，无需改）；建议把「小幅漂移」升级为「漂移量级已达 +65%（219→359），引用前必须重跑」。

**2. §4「未并入 未完成事项.md（两项仍待并入）」**

- 现场证据：未完成事项.md:199 登记「`分析/` 下 20260918 批审计与归因件（约 14 份）尚未逐条证真」，清单逐件点名含 `能力面与拆分清单_20260918.md`；口径裁定单独登记为未完成事项.md:120 条目 #7。
- 依据：不是缺陷：属登记形态差异（整件挂在待证真批次），非漏登记；本批证真完成后按「仍成立的并入，已消解的直接删」处置。

### 5 · 批F · 子代理探索耗时归因 + 开工上下文检索_归因与方案 + 新建支持库回填链路归因 + DGX-Spark本地算力视频（四件）

#### 【仍成立·真缺陷】1 条

**1. F1-3 §四 补底层清单 2「开工注入：按『本次要改的文件』判定地图覆盖率」**

- 现场证据：git show HEAD:模块库/开工编排/实现/开工编排.py → :373「# ④ 代码地图可用性（强判据缺原子能力，这里只给目录存在性弱判据）」:374「图示结果 = _调用(能力_判断存在, {"文件路径": str(项目根解析 / 代码地图目录名)})」:521「"判据": f"仅目录存在性弱判据（{代码地图目录名}）；codegraph status 探针缺原子能力，见 缺失原子能力"」。开工编排.开工准备 参数含 修改路径（列表型）但代码地图段不使用它判覆盖率。开发文档/规范/开发支持库_细则.md §四 已独立登记「② 地图判据退化｜同模块的代码地图段｜改调已登记的 代码地图提供者.查询项目地图状态，读「待同步变更」」与「② 缺失清单失真｜开工编排.py:71-90｜删掉静态清单改现场探测」。
- 期望修法：期望修法：① 开工编排.py:373-374 改调已登记的 代码地图提供者.查询项目地图状态(项目根目录=<项目根>)，把「待同步变更」带进返回值；② 代码地图段增「覆盖率」键，拿 修改路径 与地图 files 表（或 查询节点 模式=路径子串）比对，不覆盖即明确报「地图不覆盖 X 文件，勿依赖符号查询」；③ 连带修 开工编排.py:83-85 静态缺失能力清单失真 —— 仍列 代码地图.查询状态 为缺口，而该能力早已以 代码地图提供者.查询项目地图状态 登记在案。与 开发支持库_细则.md §四 两条待办合并派活。

#### 【已消解】17 条

**1. F1-1 根因「代码地图陈旧 28 天且平台没有重建/更新能力」**

- 现场证据：sqlite3 immutable 只读直查 .codegraph/codegraph.db：db mtime 2026-09-21 20:14:42，nodes=24075、edges=53031、files=1212，meta index_state=complete；节点含「资源账本维护循环」1 个、「协议表」5 个（归因时为 0）。真跑 代码地图提供者.查询项目地图状态 → 索引时间 2026-09-21 20:14:42、文件数 1212、节点数 24075、待同步变更=Added 2/Modified 7，EXIT=0。全仓 753 条去重能力id 含「地图」7 条（代码地图提供者.重建项目地图索引/查询项目地图状态/检查提供者 在册）。
- 依据：已消解：地图不再陈旧 28 天（现约 5 小时、仅 9 文件待同步），归因点名的两个 0 节点符号已在图内，「平台无重建能力」不成立。

**2. F1-2 §四 补底层清单 1「地图保鲜：补重建/同步索引原子能力」**

- 现场证据：git ls-files | grep 代码地图提供者 → 支持库/适配层/代码地图提供者/{__init__.py,包声明.json,实现/提供者.py,实现/子进程入口.py,生命周期契约.json,能力契约/参数契约.json,能力定义.json,配置契约/配置契约.json,说明/使用说明.md,说明/设计说明.md,验证场景引用.json} + 测试中心/支持库/测试_代码地图提供者.py。能力id：代码地图提供者.重建项目地图索引 / .查询项目地图状态 / .检查提供者。开发文档/规范/开发支持库_细则.md:95「代码地图.查询状态 —— 能力早就登记了：代码地图提供者.查询项目地图状态」。代码解析支持库.代码地图 版本仍 1.0.1，仅 查询节点/查询关系。
- 依据：已消解：重建/同步索引原子能力已存在。落点与归因预期（支持库/后端/代码解析支持库/代码地图/）不同且更正确 —— .codegraph 由第三方 codegraph CLI 维护，按决策记录0021「依赖分两段」应住适配层，实际落在 支持库/适配层/代码地图提供者/。

**3. F1-4 §四 补底层清单 3「契约注入：开工上下文带参数名清单」**

- 现场证据：git show HEAD:开发文档/规范/任务类型规范.json :33「调能力前先查真实名字 —— 能力目录.搜索能力(参数={'关键词':…,'含常用参数':True}) 拿到能力id与参数名再调；凭印象拼 id 会 404、拼错参数名会被静默忽略（2026-09-21 两次实测）」:37「查符号/引用/影响面 —— 代码解析支持库.代码地图.查询节点(参数={'代码地图路径':'<项目根>/.codegraph/codegraph.db','关键词':'<符号名>'})」。开工编排.py:402「# ⑥b 通用纪律（2026-09-18 追加；无条件返回，与任务类型无关）」:411 取通用纪律 :571 返回「通用纪律」键；:468「搜符号：代码解析支持库.代码地图.查询节点(代码地图路径=…, 关键词=…, 模式='名称子串')」。AGENTS.md:38-40 把 查开工上下文/记踩坑 写成开工必经动作。
- 依据：已消解：归因 §三.3「必须猜参数名」的根因已消除 —— 开工准备无条件返回 通用纪律，其中 工具姿势 段逐字带能力id+必填参数名，权威源在 任务类型规范.json:33/37，并追加「先查 能力目录.搜索能力(含常用参数=True) 再调」的动作。

**4. F1-5 §四 补底层清单 4 / §五「当下止血 codegraph sync 是否仍有效」**

- 现场证据：同 F1-1 两段输出：索引时间 2026-09-21 20:14:42、待同步变更 Added 2/Modified 7、资源账本维护循环 与 协议表 均已在图内。开工编排.py:472-475 只把「地图会过期（必须先看状态）→ 代码地图提供者.查询项目地图状态」与「重建项目地图索引(项目根目录=…, 超时秒=540.0)（增量 sync 实测 2 秒）」写进纪律文本，不在开工流程里自行拉子进程。
- 依据：已消解：止血仍有效，且已从「一次性手工 sync」升级为平台能力（重建项目地图索引 + 查询项目地图状态 作判据），漂移量级从 28 天降到数小时。归因 §六 否决的「让子代理开工先跑 codegraph sync」（第二执行腿）未被违反。

**5. F2-3 §四「三条新原子能力（建坑索引/查开工上下文/记踩坑）」**

- 现场证据：能力id 检索：含「坑」2 条 = 项目文档支持库.建坑索引 / 项目文档支持库.记踩坑；含「开工」5 条 = 开工编排.{取任务类型规范,开工准备,开工即占,收工释放} + 项目文档支持库.查开工上下文。真跑 查开工上下文(项目名=系统工程平台, 本次任务=新增支持库能力并写验证场景, 返回条数=3) → 成功=true，检索词自动切分为 [新增,增支,支持,持库,库能,能力,力并,并写,写验,验证,证场,场景]，坑列表含 {条目id:e922616b6835b2b8, 场景id:新增能力, 症状/根因/判据/原文位置, 状态:已固化, 命中次数:8, 相关度:75.5, 提示:「已由判据强制，直接照做即可，不必读原文」} 与 {状态:已用, 命中次数:3, 相关度:71.0}，EXIT=0。开发文档/规范/踩坑条目.json 已落盘（五键 场景id/症状/根因/判据/原文位置，说明「新坑产生时用 记踩坑 单条落盘，或直接在本文件追加后跑 建坑索引」）。
- 依据：已消解：三条能力全部新增并真跑通；状态机按 §4.2 设计生效（已固化条目返回「已由判据强制，不必读原文」，已用条目带 命中次数/相关度 排序）；数据落点复用既有库、未新开第二套检索，与 §4.3 一致。

**6. F2-4 §六 待裁决第 1 问「三条新能力是否照上表新增」**

- 现场证据：git show HEAD:AGENTS.md :38「项目文档支持库.查开工上下文(项目名="系统工程平台", 本次任务="<我正要做什么>")（经 40007 /网关/调用）」:40「撞报错时把报错原文直接当 本次任务 再查一次；踩到新坑时用 项目文档支持库.记踩坑(...) 落盘」。开发文档/项目说明.md:545「开工与规范注入 ｜ 模块库/开工编排.开工准备 / 开工编排.取任务类型规范 / 项目文档支持库.查开工上下文（历史踩坑）｜ 可用；任何 Agent 开工调一次即得本次任务的强规范」。git grep 决策记录 全目录（最大号 0058）搜 建坑索引/查开工上下文/记踩坑/坑条目 → 零命中。
- 依据：已消解：裁决=是，已落地并写进 AGENTS.md:38-40、项目说明.md:545、开发文档/规范/开发支持库.md + 踩坑条目.json。登记缺口（非能力缺口）：归因 §五 第 8 项原计划「决策记录 0041 + 哲学 14.4」未兑现，决策记录无一条记录该裁决；若需可追溯建议补一条（编号接 0059）。

**7. F2-5 §六 待裁决第 2 问「哲学正文瘦身是否同期做」**

- 现场证据：git ls-files 开发文档/ | grep 哲学 → 开发文档/哲学总账.md（620 行/18,249 字）+ 开发文档/规范/哲学实测教训与外移区.md（141 行/4,127 字）；归因 §六 声称原状态 59 子项/23,987 字/356 行。外移区头部：「最后更新：2026-09-20 … 哲学条文里『过程叙述、实测数字、样板、起因』的外移落点。哲学正文只留规则本体 + 判据（人读三秒能记住），细节全在本文件；正文按条目号给索引跳转」+ 华哥 2026-09-18 原话「每一个哲学，其实都可以精炼一点不需要那么多字…尽量就节省一些上下文空间」+「外移不删一字 —— 原文逐段搬到这里；正文只留规则本体一句 + 详见本文件 §X」。
- 依据：已消解：裁决=做了，且形态与归因建议一致（原文保留、只外移过程叙述/实测数字/样板）。实际执行比归因建议（A 类 5 条原文保留、B 类 5 条逐条过）更彻底：按「规则本体 vs 过程叙述」切分外移，落点 开发文档/规范/哲学实测教训与外移区.md；条款原文唯一真源仍在 开发文档/项目说明.md 的《设计哲学》段（哲学总账.md 头部唯一事实源声明明确）。

**8. F3-1 缺陷 A「模板生成器不产『待作者补』清单」**

- 现场证据：git grep -n 待作者补 HEAD → 开发文档/规范/新建支持库与模块指南.md:176「### 步骤 1.5：填齐模板产的三件『待作者补』文件（新增，2026-09-18）」；支持库/后端/组件规范支持库/实现/待作者补清单.py:1「待作者补清单：新建包后一次性告诉作者要补哪几件、合法值是什么、补完怎么验」:72 def 生成待作者补清单；支持库模板生成器.py:619「# 待作者补清单：把「要补哪几件 + 隐性硬约束 + 自检顺序」一次性交给作者」:623 from …待作者补清单 import 生成待作者补清单 :624 _写(输出目录 / "待作者补.md", …) :630 返回值结构新增「待作者补清单」键。真实产物：支持库/后端/代码解析支持库/语义索引/待作者补.md（含「一、必须补的内容（骨架→真实）」表 + 逐能力说明 + 「二、★ 隐性硬约束（撞一次就浪费一轮，先看这里）」）。
- 依据：已消解：生成支持库模板 现在额外产 <包目录>/待作者补.md（生成器 619-630 行，实现落在新模块 支持库/后端/组件规范支持库/实现/待作者补清单.py，133 行），归因要的「要补哪几件+合法值+判据命令+补完怎么验」逐项齐备，且已有真实包在用。

**9. F3-2 缺陷 B「隐性规则只存在于抛出点的代码里，不在生成期告知」**

- 现场证据：git show HEAD:支持库/后端/组件规范支持库/实现/待作者补清单.py :6-7「隐性规则（def 注册能力 豁免、$动态 六类、失败用例要 返回断言、说明书默认拒绝覆盖、权限要逐能力声明）全是撞出来才知道的」:19-22「入口 __init__.py 的 def 注册能力 —— 入口必须直接定义 def 注册能力(注册表)（不是从别处 import）；判据：python3.14 开发工具/包体检.py <包目录>（看「入口注册能力」项）」:24-26「$动态 取值 —— 字段集必须与允许表精确一致」:30-31「必须写 {"成功": false, "状态码": <查真源映射>, "返回断言": {"错误码": …}}；错误码要放进 返回断言 里，不是并列键；状态码不许猜，查 公开错误码状态映射」:43-44「权限契约 —— 必须逐能力声明」:49「默认拒绝覆盖（防有损覆盖人工成果）；人工改过的即便传许可也拒绝」。grep -c 开发文档/规范/新建支持库与模块指南.md → $动态 6、返回断言 3、拒绝覆盖 3。
- 依据：已消解：五条隐性规则全部前移到生成期，逐条落在 待作者补清单.py 的「隐性硬约束」段（带合法值+判据命令），并写进 新建支持库与模块指南.md 步骤 1.5（第 176 行起）。残差（不判缺陷）：五条中 $动态/返回断言/拒绝覆盖 三条在指南里逐字可搜到；def 注册能力 豁免与「权限逐能力声明」两条在 待作者补清单.py 齐备但指南正文无逐字表述（指南 117/119/145/146 行只列产出物），属可选加强。

**10. F3-3 缺陷 C「开发工具/开发入口.py.生成说明书 指向已废弃接口（实测崩）」**

- 现场证据：git show HEAD:开发工具/开发入口.py :192-203「def 生成说明书(组件目录: Path) -> str: … 2026-09-18 修：旧实现读包内 能力契约/*.json 后调 契约编译器.生成说明书(契约)（单能力文本生成），实测 KeyError: '能力id' —— 该入口早已不是那条腿。现行唯一权威是 组件规范支持库.生成说明书(包目录, 输出根目录)…本函数只做薄委托，不保留任何第二套生成逻辑」:202 from 支持库.后端.组件规范支持库 import 生成说明书 as 唯一生成。真跑（本次现场）：cd <项目根> && unset PYTHONPATH; python3.14 -m 开发工具.开发入口 生成说明书 '{"组件目录":"支持库/适配层/代码地图提供者"}' → {"成功": false, "错误码": "内部错误", "说明": "拒绝覆盖已有说明书: …/代码地图提供者/说明/使用说明.md"} EXIT=1（业务结果，非异常）。git grep -n 契约编译器 HEAD -- '*.py' | grep 生成说明书 → 唯一命中在解释历史缺陷的 docstring，无实际调用点。
- 依据：已消解（真跑复现：不再崩）：KeyError: '能力id' 不再出现，旧调用点已删，改为薄委托唯一腿；返回的是 说明书生成器.校验输出目标 的血统判定结果（人工类默认拒绝覆盖），属正常工作。口径提醒：脚本路径直跑（python3.14 开发工具/开发入口.py）会因 公共契约 不在 sys.path 抛 ModuleNotFoundError，必须用 -m 形式（调用姿势问题，非缺陷 C 残留）。

**11. F3-4 缺陷 D「没有『一键体检 + 精确缺项』的开工入口」**

- 现场证据：git ls-files 开发工具/ | awk -F/ 'NF==2' → 含 开发工具/包体检.py（另有 规范漂移体检.py、验证场景体检.py、环境自检.py、缺陷热点巡诊.py）。git show HEAD:开发工具/包体检.py :1-14「包体检：给一个支持库/模块包目录，一次性报「还差哪几件、差哪条、合法值、怎么修」。为什么要它（2026-09-18 归因，见 开发文档/分析/新建支持库回填链路归因_20260918.md）… 本入口把这些判据汇总到一处、一次报全，且不装配、不编译、不写盘（纯只读），可在开工第一步就跑…用法：python3.14 开发工具/包体检.py <包目录>（退出码 0=无缺项，1=有缺项）」；内建 动态允许表（6 类）、骨架标记表、生成标记、九要素目录。真跑（本次现场）：python3.14 开发工具/包体检.py 支持库/适配层/代码地图提供者 → 「✅ 无缺项（可进入编译与组件合规）／合计缺项 0 项」EXIT=0。
- 依据：已消解：包体检.py 已新增并真跑通过，docstring 直接引用本归因件，命中归因要求的全部四点（给包目录/一次报全/报每件差哪条+合法值+怎么修/纯只读不装配不编译不写盘、可开工第一步跑），正是「挤牙膏」的解法。

**12. F3-5 §三末「说明书『拒绝覆盖』的处理（本次第 7 条坑）」**

- 现场证据：git show HEAD:支持库/后端/组件规范支持库/实现/说明书生成器.py :15-17 骨架标记表=("**骨架占位**","骨架占位","（骨架，返回未实现/占位值）","待填写") :178 def 校验输出目标(…, *, 允许重建骨架: bool = False) :181「★ 骨架态重建（2026-09-18 补，归因见 开发文档/分析/新建支持库回填链路归因_20260918.md）」:184-187「许可条件双重收紧，只放行『确定没被人工改过』的骨架：① 调用方显式传 允许重建骨架=True（不给默认放行）；② 目标文件确实命中骨架标记…人工改过的说明书（无骨架标记）即便传了许可也照样拒绝 —— 人工权威不被有损覆盖」:209-210 if not (允许重建骨架 and 是骨架): raise FileExistsError。组件规范支持库.生成说明书 版本 1.2.0，参数新增 允许重建骨架 逻辑型 选填 默认 False。
- 依据：已消解：归因 §三末给的正解逐字落地（显式骨架重建语义 + 双重收紧 + 人工类照样拒绝）。残差（可选加强，非缺陷）：开发工具/开发入口.py:203 调唯一腿时未透传 允许重建骨架，故经该入口刷骨架态说明书仍会被拒（需直接调能力并传参）；归因只要求「唯一腿支持该语义」，此条已满足。

**13. F4-1 §四 A「自动发现本地算力节点 —— 真缺口」**

- 现场证据：全仓 753 条能力id：含「发现」0 条；含「端点」2 条 = 大语言模型支持库.模型连接器.探测模型端点 / 系统核心支持库.协议白名单.校验端点。开发文档/方案/本机推理端点自动发现方案_20260918.md 头部：「状态：已废弃（2026-09-18，方向错位：本方案为「扫端口自动发现」，按华哥口径应做的是「模型探针」，见 模型探针与动态路由方案_20260918.md）／理由保留（防重复提议）：扫端口式「发现」是给人省手填配置用的（视频场景），而底座用户是 agent —— agent 写 JSON 配置本来就快，不缺「替它发现」这个动作」。开发文档/方案/模型探针与动态路由方案_20260918.md 头部：「状态：已完结（编号 50 探针已实现并验收；编号 51–53 已实现并验收；编号 54 待裁决）／落地记录：2026-09-18 晚，开工 ID 探针簇（TZ-20260918-01）+ 路由簇（TZ-20260918-02）」。
- 依据：已消解（形态被裁决改变）：A 条判定的真缺口已被处置 —— 本文建议的「扫端口自动发现」方案被华哥裁决废弃（方向错位），替代的「模型探针」方案已完结并验收，落地为 大语言模型支持库.模型连接器.探测模型端点。「发现=0 条」这一实测事实仍准确，但缺口性质已从「未处置」变为「已按裁决以另一形态处置」。

**14. F4-3 §四 C「端点清单 / 健康探测 / 选型 —— 真缺口」**

- 现场证据：全仓 753 条能力id：含「选型」0 条；含「选择」3 条 = 大语言模型支持库.模型连接器.选择模型 / 文件选择描述.创建文件选择描述 / 文件选择描述.校验文件选择描述。git show HEAD:支持库/后端/大语言模型支持库/模型连接器/能力定义.json → 选择模型 版本 1.0.0，说明「按历史实测表现把候选模型排序，给出建议与理由（只读建议，不自动切换）。第一层硬门槛永远执行（上下文超限排除、候选端点不可达排除）；第二层统计打分仅在启用智能路由时执行（读本地调用记录、四维加权、不消耗额度）。默认保守模式。底座不做隐私/安全判断（哲学 9.3）；参考路径只是用户自带规则的可选输入，不给则完全不涉及」；参数 候选清单/任务描述/任务类型/上下文长度/启用智能路由(默认False)/参考路径/启用额度标注/权重/最少样本数(默认3)/数据库路径；返回 {建议,排除,模式,打分依据,额度说明,诊断}。
- 依据：已消解：C 条判定的真缺口已补齐 —— 选择模型 已注册（1.0.0），且逐字满足本文 §五 P1 的三条边界（只做中立技术匹配／不内嵌隐私合规判断／不做自动切换）。「含选型=0」只是命名差异，归因 §四 C 的搜法「搜『选择模型 / 切换 / 难度』」已命中该项。

**15. F4-8 §五 P0「本机推理端点自动发现与接管（建议做）」**

- 现场证据：大语言模型支持库.模型连接器.探测模型端点 版本 1.0.0，说明「探测一个模型端点是否可用、走哪种协议。目的：调用方只给 url 与 api_key 即可；缺的参数自动补齐——第一步向端点取模型清单（未给模型时，用 GET /models），第二步拿模型试协议（给了协议只打这一个，未给则按 chat_completions→codex_responses→anthropic_messages 依次试，命中即停）。可用性判据是响应体特征字段（choices/output/content+type），不看状态码——宽容服务器对任意路径都回 200…不猜根路径：url 原样使用，全失败时列出试过的完整路径」；参数 url(必填)/api_key/模型/协议/超时秒(默认15)；返回 {可用,命中协议,模型,模型清单,响应毫秒,逐项结果,诊断}。方案状态见 F4-1（本机推理端点自动发现方案＝已废弃/方向错位；模型探针与动态路由方案＝已完结）。
- 依据：已消解：P0 的原始形态（扫本机监听端口+探测 /v1/models→返回可用端点清单）已被华哥裁决废弃；替代形态（模型探针）已实现并验收，落地 探测模型端点，落点与本文建议一致（支持库/后端/大语言模型支持库/模型连接器/）。唯一偏离是「不做扫端口」——属裁决不属未落地；P0 的边界「只做发现+探活，不做自动切换」被遵守。

**16. F4-9 §五 P1「模型端点选型（可做，需先确认需求）」**

- 现场证据：同 F4-3（选择模型 版本 1.0.0 已注册，参数/返回/边界逐条对齐 P1）。开发文档/方案/智能路由与模型打分方案_20260918.md 头部「状态：起草（策划完成，待华哥确认开工范围）／前置已落地：方案/模型探针与动态路由方案_20260918.md（探针已实现验收，开工 ID 探针簇）／落点：支持库/后端/大语言模型支持库/模型连接器」；前置方案标注「编号 51–53 已实现并验收」。
- 依据：已消解：P1 的前置「待华哥确认后再开工」已满足且已开工 —— 核心能力 选择模型 已落地并严守 P1 唯一关键边界（中立技术匹配、不含隐私判断、不自动切换）。残留的是「智能路由与模型打分」方案自身标注起草态、待确认开工范围，属该方案的后续范围问题，不是 P1 未落地。

**17. F4-11 §七 待华哥裁决三问**

- 现场证据：问1（P0 是否开工）：本机推理端点自动发现方案 状态＝已废弃（方向错位）+ 模型探针与动态路由方案 状态＝已完结，落地 探测模型端点。问2（P1 是否立项）：选择模型 1.0.0 已注册 + 智能路由与模型打分方案 状态＝起草（待确认开工范围）。问3（定位是否要动）：开发文档/项目说明.md:85「多实例、分布式不在范围内」未改。
- 依据：已消解：三问均已裁决并留下痕迹 —— ①不做扫端口式发现、改做模型探针且已落地；②立项且核心能力已落地；③定位不动（跨机池继续在范围外）。唯一未闭合的是「智能路由与模型打分方案」的范围确认（该方案自身标注起草态），属新方案的范围问题，不是本归因件 §七 的遗留。

#### 【不是缺陷】8 条

**1. F2-1 §2.1「检索底座一条不缺（切块/建索引库/写入索引/查询索引）」**

- 现场证据：全仓 753 条去重能力id：项目文档支持库 共 19 条，含 写入索引/切块/建全仓索引/建坑索引/建索引库/查询索引/查开工上下文/记踩坑/扫描文档集合/查哲学条款 等。归因声明的 4 条（切块/建索引库/写入索引/查询索引）全部在册。
- 依据：依据：该条本身就是归因「排除能力不足」的结论，非缺陷项，无须修。其自我修正的教训（判有没有能力必须按职责语义找、不能按名字关键词找）与本次核验方法一致。

**2. F2-2 §三「已跑通链路（切块/写索引/查询）」**

- 现场证据：真跑只读能力：python3.14 -m 开发工具.开发入口 真实调用能力 '{"能力id":"项目文档支持库.查询索引","参数":{"项目名":"系统工程平台","关键词":"状态码","返回条数":3}}' → 成功=true，命中列表含 {文档相对路径:开发文档/决策记录/0040_…md, 起始行:25, 结束行:33, 块类型:表格, 命中次数:3, 片段:…}，命中数=53，已索引文档数=831，索引新鲜度=部分过期（过期文档数 165 / 已核验 707），EXIT=0。写入索引 属写盘，按只读纪律未跑。
- 依据：依据：归因描述的三段链真跑仍通，且返回形态升级（只回块片段、不载入全文，新增 细节级别/生效搜索词/扩词记录/项目已建索引/索引新鲜度 等键），「不全量载入」目标仍达成。附带观察（不属本四件）：文档索引当前部分过期（165/707），据其结果下结论前应先跑 建全仓索引 —— 能力已自带该提示，属设计内行为。

**3. F4-2 §四 B「接管已在运行的模型端点 —— 已覆盖」**

- 现场证据：大语言模型支持库.模型连接器 包版本 1.2.8、27 条能力，连接LLM 在册；探测模型端点 说明与本文 §4.3 证据 1 口径一致（「调用方只给 url 与 api_key 即可；缺的参数自动补齐」）。
- 依据：依据：本文自判「已覆盖，无需新增」，属非缺陷结论；本次核验一致，无须修。

**4. F4-4 §四 D「隐私路由 —— 不应进底座」**

- 现场证据：开发文档/项目说明.md:296「9.3 隐私/安全是上层业务的事：V3 等业务端涉及隐私、风险、合规时，由业务端主动要求并自理（在调用前脱敏、在结果后加安全层）。底座不做任何"为了隐私而限制"」（第 9 条 底座 0 加密 0 限制，见 :289）。全仓 753 条能力id 检索：无任何 隐私路由/敏感分类/上下文打码 类能力。
- 依据：依据：本文判定「不应进底座（按 9.3 归业务端）」的依据仍然成立且未被推翻 —— 哲学 9.3 原文原样在位，底座能力面至今零隐私路由类能力，即该结论被遵守（没有误落地）。属设计取舍（防误修清单），非缺陷。

**5. F4-5 §四 E「跨机算力池（PAIR 式多机分发）—— 超出定位」**

- 现场证据：开发文档/项目说明.md:85「- 多实例、分布式不在范围内：限流与状态治理按进程内 + 本机权威状态实现（见设计哲学第 9、11 条）」（本文引用时是原第 83 行，现 85 行，行号漂 2）。全仓 753 条能力id 检索：无 算力池/多机分发/集群 类能力。注：项目说明.md:587 的「多实例模式」指 HTML 验证器的端口池/多制品实例（验证设施），与平台跨机分布式不是同一件事。
- 依据：依据：本文引用的定位明文逐字仍在，底座能力面至今零跨机池化能力，即「超出定位、只记录不落地」被遵守。属设计取舍，非缺陷。

**6. F4-6 §四 F「硬件门槛：PAIR 要求 Apple M4 及更新 —— 本机不满足」**

- 现场证据：system_profiler SPHardwareDataType | grep -E "Chip|Memory|Model Name" → Model Name: Mac Studio / Chip: Apple M3 Ultra / Memory: 96 GB。
- 依据：依据：本机芯片仍是 Apple M3 Ultra / 96 GB，与归因实测一致，不在 PAIR 的 M4+ 门槛内 ⇒「不能直接装 PAIR」仍成立。这是环境事实（不是平台缺陷），其结论用途是「否决直接用 PAIR」这一防重复提议理由，该理由继续有效。

**7. F4-7 §四 G「接口策略：不新增集群 API、代理引擎既有接口、占默认端口 —— 可借鉴」**

- 现场证据：本文自判「可借鉴的设计取向，非新增能力」；与 开发文档/决策记录/0001_单一网关与唯一调用腿.md 的「唯一调用腿」取向同向；底座至今仍是唯一网关 + 唯一调用腿（公共契约 / 运行核心/统一网关 未新增第二入口）。
- 依据：依据：这是设计取向的肯定判断，不是缺陷项，无修法可写。

**8. F4-10 §五 P2「只记录不落地（防后人重复评估）」**

- 现场证据：开发文档/项目说明.md:85「多实例、分布式不在范围内」定位明文仍在；:296 哲学 9.3 隐私/安全是上层业务的事 仍在；system_profiler → Chip: Apple M3 Ultra（不在 PAIR 的 M4+ 门槛内）；全仓 753 条能力id → 无 算力池/多机分发/隐私路由/敏感分类/上下文打码 类能力。
- 依据：依据：P2 三条「不做的理由」全部仍然成立且三条都被实际遵守（能力面零相关能力）。⇒ P2 应当继续维持「只记录不落地」；若要动，须先改 开发文档/项目说明.md 的定位（定位级裁决，不在本轮范围）。属防误修清单项，不是缺陷。
