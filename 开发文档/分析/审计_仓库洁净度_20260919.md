# 仓库洁净度审计（第一轮 · 只读）

> 最后更新：2026-09-19
> 口径：实测（全部条目在 HEAD `836e9066` 现场重跑，命令与输出摘要随条附上；未跑测试、未做任何写操作）
> 维护者：小马仔（受派：仓库洁净度审计第一轮）

---

## 〇、结论摘要（分级）

**一句话**：本仓**没有"病入膏肓"的洁净度问题**——不存在制品/安装包/构建产物进仓、不存在第二套归档顶层、不存在 `工程缓存/` 误入库、无大文件与二进制残留；**17 个顶层目录里 13 个是合规的**。问题集中在**三类"纸面已收口、现场还没清"的尾巴**：留库的一次性脚本、违反自己承诺的入库件、以及指向已删物的死链接。

| 级别 | 数量 | 条目 | 判定依据 |
|---|---:|---|---|
| **P0（应立即处置）** | 2 | ① 素材目录 4 个一次性脚本（含 2 个 .sh 实测脚本）；⑦ `开发工具/性能基准/结果/` 两个 JSON 入库，撞本包说明自订的"新结果默认不要入库" | 哲学 1.6 / 本包说明第 4 行 |
| **P1（本批应处置）** | 6 | ② 临时文档目录 1 份过程稿；③ 三份"已取代"方案仍把全仓批次表留在正文；④ 两份自称"唯一编排入口"；⑧ 技能库/交付收尾 SKILL.md 指已删物；⑨ 限流边界说明指已删底稿目录 | 哲学 1.6 / 4.5 / 11 / 规范第三节 |
| **P2（记账即可）** | 5 | ⑤ 三份附表引用 `/tmp` 已删路径；⑥ 两份附表内容重叠；⑩⑥ 五项方案缺五态声明；⑪⑥ 十六份方案未登记进文档地图 C 栏；⑫ 目录约定漂移 7 处 | 规范第二节 / 六、七节 / 主开发文档第 147 行 |
| **不是缺陷（设计取舍）** | 9 | 见第五节 | 1.7 边界澄清 / 2.3 / 7.9 / 体例规范第八节 |

**最值得华哥看一眼的两条**：

1. **素材目录那 4 个脚本不是"文档素材"，是真能连网关的一次性实测脚本**——其中 `测底座接管.sh` / `复核对端接管.sh` 里写死了取凭证的 `plutil` 命令与 `POST 40007/网关/调用` 的完整调用体（现场读到实际内容）。它们落在 `开发文档/` 里，而 AGENTS.md 第 20 行明写"不再新增……临时审计 Markdown"，哲学 1.6 明写"探针、验收、一次性搬迁脚本一律不落在底座仓库"。**这是 P0**。
2. **`开发工具/性能基准/结果/` 的两个 JSON 是"违反自己说明"入库的**——`开发工具/性能基准/说明/性能基准说明.md:4` 白纸黑字写"**新结果默认不要入库**（`git add` 时排除 `结果/`）"，但该目录 2 个 JSON 已被跟踪（`git log` 只有 `adbef399` 一次提交，即随包一起进仓）。它前半句自带了"除非要留一份对照锚点"的例外，属**半自证**，需要人裁定。

---

## 一、逐类明细

### 条目 1 · 临时脚本 / 一次性探针 / 迁移脚本残留（哲学 1.6）

**双路径查法**：① 按文件名（`git ls-files | rg "探针|临时|一次性|迁移|验收|检查"`）；② 按内容（`rg "一次性脚本|用完即删|本脚本只在本次|跑完即删|事后删除"`），并逐条读判定。

#### 1.1【P0】`开发文档/分析/素材_DGX-Spark本地算力视频_20260918/` 下 4 个脚本进仓

- **路径**：
  - `开发文档/分析/素材_DGX-Spark本地算力视频_20260918/测端点扫描.py`（3.7K）
  - `开发文档/分析/素材_DGX-Spark本地算力视频_20260918/测mdns标准库.py`（3.5K）
  - `开发文档/分析/素材_DGX-Spark本地算力视频_20260918/测底座接管.sh`（3.3K）
  - `开发文档/分析/素材_DGX-Spark本地算力视频_20260918/复核对端接管.sh`（2.5K）
- **现场证据**：
  - `git ls-files 开发文档 | rg "\.(py|sh)$"` → **全仓 `开发文档/` 下的 `.py`/`.sh` 只有这 4 个**（这是"文档目录里不该有可执行物"的最强信号）。
  - `read_file` 实读 `测端点扫描.py` 首段：`"""实测：纯标准库扫本机推理端点的可行性与耗时（方案性能数字必须真实）……判据：单次发现调用必须远低于华哥的「单步 2 分钟 = bug」红线，目标 < 3 秒。"""`
  - `read_file` 实读 `测底座接管.sh:3`：`TOKEN=$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)`，`:7` 为真实 `POST http://127.0.0.1:40007/网关/调用` 调用体 —— **不是示意，是现场能连网关的一次性探针**。
  - **用途已被权威件吸收**：`开发文档/方案/本机推理端点自动发现方案_20260918.md:211`、`:220` 引用了 `测mdns标准库.py` / `测端点扫描.py`；但**该方案状态是"已废弃"**（`本机推理端点自动发现方案_20260918.md:7`：`> 状态：**已废弃（2026-09-18，方向错位……）**`）——即**引用它的方案已经死了，脚本还活着**。
- **违规条款**：哲学 1.6（临时脚本不留库）；AGENTS.md:20（"不再新增项目记忆、阶段报告或临时审计 Markdown"）。
- **影响面**：`开发文档/` 被当作"仓库里的通用垃圾位"先例；两个 `.sh` 含凭证取值命令与网关调用样例，会被后续 Agent 当成"官方脚本"照抄。
- **建议处置**：**移出仓库**（内容已进方案结论与 `开发文档/分析/DGX-Spark本地算力视频_识别与底座价值评估_20260918.md`），或至少把 4 个脚本迁到 `/private/tmp/系统平台临时脚本_20260918/`（该目录**现仍存在**，见 `ls -d /private/tmp/系统平台临时脚本_20260918`）。
- **是否已自带理由**：**无**。目录内唯一提到它们的是 `DGX-Spark……评估_20260918.md:18`「取证：页面文本与逐字稿见同目录 `页面文本.txt`、`逐字稿.txt`」——**只给 .txt 落理由，没给 4 个脚本落理由**。

#### 1.2【P1】`开发文档/临时文档/01_补关键值断言_无法定值与剩余缺口_20260918.md`

- **路径**：`开发文档/临时文档/01_补关键值断言_无法定值与剩余缺口_20260918.md`（123 行）
- **现场证据**：
  - 同目录 `README.md:1`：「# 临时文档（工作台目录，**用完即删**）」；`:3`「**这个目录通常应该是空的。**」；`:7-8`「结论一旦并入 `项目说明.md` / `未完成事项.md` / `规范/` / `决策记录/`，原稿立即删除……2026-09-17 已按此清空过一次」。
  - 该稿自带收口条件（`:121-123`）："**本批只收口自己负责的域，不做全仓基线判定**：建议主会话裁定口径后重挂基线（属判据问题，非本批范围）" —— **条件未闭环，稿子留了**。
  - `ls -1A 开发文档/临时文档` → `01_补关键值断言_无法定值与剩余缺口_20260918.md` + `README.md`（其余历史批次目录 `20260917_限流边界收口/`、`20260915_MCP收敛/` **均已删除**）。
  - 该稿 `:103` 明写"临时脚本一律放 `/private/tmp/系统平台临时脚本_20260918/`，**不进仓库**"—— **脚本面做对了，文档面没走**。
- **违规条款**：哲学 1.6；决策记录 `0004`（临时文档结论并入权威后即删除）；哲学 4.1。
- **影响面**：`临时文档/` 是"开工先落盘"的入口目录之一（`项目说明.md:819`），留稿会被下一轮误当权威。
- **建议处置**：**已闭环** → 删除；**未闭环** → 结论并入 `未完成事项.md` 第 13 项后立即删除（不得留在 `临时文档/`）。
- **是否已自带理由**：**部分**（它自己声明"属判据问题，非本批范围"）——但该理由说明的是**没做完**，不是"该留"。按 1.8 判：**理由不足，仍按可疑项报**。

#### 1.3【P1】`开发文档/交付记录/20260918_夜间推进简报_给华哥.md`

- **现场证据**：`git log --format="%h %ad" --date=short --` 该文件 → 新建于 `d4af0bb7 2026-09-18`；而"不再新增阶段报告"的写入口在 `AGENTS.md:20`：「当前版本功能和用法只写入 `开发文档/项目说明.md` 及包级说明，**不再新增项目记忆、阶段报告或临时审计 Markdown**」。
- **判定**：**不是缺陷**（该简报是 09-18 深夜的**面交件**，落盘时间早于/同期于该条收口，且 `交付记录/` 与 `分析/` 的区别就是"面交 vs 内部"）。**登记备忘**：本条应作为 09-18 之后**不再新增**的分界线样本。
- **是否已自带理由**：**有**（文件头 `:3`「面向：华哥 ｜ 撰写：小马仔 ｜ 时段：2026-09-18 00:30 – 05:35 CST」）。

#### 1.4 名称含「探针 / 临时 / 迁移 / 验收」但**实为正式代码或正式夹具**（按 1.8 判合法）

| 路径 | 为何合法（现场读到的理由） |
|---|---|
| `支持库/适配层/系统探针.py`、`支持库/适配层/硬件画像探针.py` | 在 `AGENTS.md:296-300` 的**系统适配器豁免名单**里被逐个点名（"系统探针.py 等"），且是运行时真调用件 |
| `支持库/后端/大语言模型支持库/模型连接器/实现/模型探针.py` | 方案 `模型探针与动态路由方案_20260918.md` 的**能力本体**（编号 50 探针已实现并验收） |
| `支持库/后端/OCR识别支持库/OCR识别/实现/临时文件.py`、`支持库/适配层/Tesseract提供者/实现/临时文件.py` | 是能力实现内的"临时文件句柄"，不是探针脚本 |
| `开发工具/测试体系门禁实现/导入探针.py` | 测试体系门禁的实现件，被 `开发工具/测试体系门禁.py` 调用 |
| `测试中心/支持库/测试_模型连接器探针.py` 等 3 个测试 | `测试中心/` 是"唯一测试代码位置"（AGENTS.md:261） |
| `开发文档/方案/迁移面_附表.md`、`适配层唯一化迁移清单_20260917.md`、`裸布尔迁移分批方案_20260917.md`、`底座扩容总纲_一次性做透_20260917.md` | 方案文档本体（文件名含"迁移/一次性"是主题词，不是脚本） |
| `开发文档/交付记录/**/验收*.md`（5 份） | 历史交付留档，`交付记录/` 用途即"验收结果"（主开发文档:79） |
| `开发文档/归档/202608_第十四阶段/验收契约.json` —— **注意这一份** | 落在归档内，按体例规范第八节归档件不再参与规范约束。**但见条目 3.2 的"归档件被正文当数据源引用"** |

- 按 1.8：以上**均已自带理由**（豁免名单点名 / 包内用途自明 / 目录用途已声明），**判合法，不需处置**。

**本类小结**：真需要动的只有 **1.1（4 个脚本）**与 **1.2（1 份过程稿）**；其余为合法命名。

---

### 条目 2 · 制品 / 安装包 / 构建产物是否进仓（哲学 1.7）

#### 2.1 本类未发现（制品/安装包/构建产物层面干净）

- **现场证据**：
  - `git ls-files | rg -i "\.(zip|tar|gz|7z|rar|dmg|pkg|exe|msi|whl|jar|so|dylib|dll)$"` → **只有 1 个 `zip`**：`模块库/自修复工具/验证夹具/最小Git仓库.zip`（3597 字节，是"自修复工具"能力的验证夹具）。
  - `客户端/` 目录只有 `构建平台客户端.py`（构建脚本），**没有任何构建产物**；制品落点按代码口径全在 `工程缓存/制品仓库/`（`客户端/构建平台客户端.py:43-46`），而 `工程缓存/` 已 gitignore。
  - 无 `dist/`、`build/`、`release/`、`*.dmg`、`*.pkg`。
- **违规条款**：不适用（合规）。
- **建议处置**：无需处置。

#### 2.2【P0】`开发工具/性能基准/结果/` 两个 JSON 入库 —— 撞本包说明自订的"默认不入库"

- **路径**：
  - `开发工具/性能基准/结果/基准结果-20260918-182155.json`（59112 字节）
  - `开发工具/性能基准/结果/基准结果-20260918-182322.json`（62417 字节）
- **现场证据**：
  - `read_file 开发工具/性能基准/说明/性能基准说明.md:4` → `【提交口径】结果 JSON 在 结果/ 下，**新结果默认不要入库**（git add 时排除 结果/，只提交代码与说明；**除非要留一份对照锚点**）。`
  - `git ls-files 开发工具/性能基准/结果/` → 2 个文件**已被跟踪**。
  - `git log --oneline -3 -- 开发工具/性能基准/结果/` → 只有 `adbef399 性能基准B2：新增 开发工具/性能基准/…`（**随包首提一起进仓，不是"事后补锚点"**）。
  - 体积对照：这两个是**全仓 tracked 文件第 6、第 8 大**（`stat` 排序：213039 / 126211 / 87879 / 70101 / **62417** / 59112 …）。
- **违规条款**：哲学 1.7（制品不做本地归档）；本包说明自订的提交口径（属"自己的规矩自己没守"）。
- **影响面**：性能基准每次跑都产新 JSON（说明 `:26` 写"每次运行一个新文件"），一旦默认入库会**持续堆积历史快照**，正是 1.7 要消灭的"本地制品归档库"。
- **建议处置**：**二选一**——(a) 删掉 1 个、按说明只留 **1 份对照锚点**并在说明里点明是哪一份；(b) 全删，改为"锚点值写进说明文档"（派生物优先于手写，见 7.9）。**倾向 (b)**。
- **是否已自带理由**：**半**——说明文本自带"除非要留一份对照锚点"的例外，但**没有一个文件被点名标注为锚点**，且提交信息里也没写"这是锚点"。按 1.8：**豁免无落点 = 仍需裁定**。

---

### 条目 3 · 归档违规

#### 3.1 本类未发现（顶层归档唯一）

- **现场证据**：`git ls-files | awk` 抽全部含 `归档` 的路径 → **只出现一个前缀 `开发文档/归档/`**。
- **归档顶层条目清单**（`ls -1A 开发文档/归档`，5 条，**全部符合 `年月_主题` 命名**）：
  ```
  202608_第十四阶段
  202609_接入面切换（8766→薄壳）
  202609_未完成事项_历史进展.md
  202609_未完成事项_取证与历史（A–I 段与历轮收口）
  202609_系统工程平台审计
  ```
  `ls -1A 开发文档/归档 | rg -v "^20[0-9]{4}_"` → **空**（无裸名条目）。
- **违规条款**：不适用。

#### 3.2【P2】归档件被"非权威文档"以数据源方式引用（**权威文档侧已基本收口**）

| 引用方 | 位置 | 被引归档件 | 判定 |
|---|---|---|---|
| `开发工具/统一能力入口/视图/错误码与建议口径说明.md:19` | 包级说明 | `开发文档/归档/202608_第十四阶段/工作包清单.json` | **P2**：包级说明属"权威面之外"，但仍是活文档；引用写法是"设计源头文件"（**当事实源用**，最强形态） |
| `开发文档/规范/多会话并行开发规约.md:236`、`:302` | **规范** | `归档/202609_系统工程平台审计/旁路问题_待下一批.md` | **P2**：规范**把归档件当"写目标"**（"抢不到就写进……"），不是"指涉历史"。这是唯一一处**功能性依赖归档件** |
| `开发文档/未完成事项.md:38`、`:162` | **权威文档** | `归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/` | **P2（边界）**：正文写了完整路径，触发"归档件路径不得出现在权威文档正文链接里"。但 `:162` 同时明写"归档件**不作为派活依据**"——**属于"声明式指涉"而非"链接引用"**，需裁定是否算违规 |
| `开发文档/交付记录/20260911_热接入子包归属修复/剩余风险.md:18` | 交付记录 | 同上归档 | **可接受**（交付记录属历史留档，非权威正文） |
| `开发文档/主开发文档.md:81` | 文档地图 | `[归档/](./归档/)` | **已知例外，已收口**：体例规范 `:145` 明写"历史上 `开发文档/主开发文档.md` 的文档地图把 `归档/` 列为链接，与这条冲突，**已按本条收口**"；主开发文档 `:81` 自己标注"目录名与链接仅为文档地图导航位" |

- **违规条款**：`AGENTS.md:282` + 体例规范 `:181`（"归档件**不作为派活依据**，其路径**不得出现在权威文档正文的链接里**（正文需要指涉历史时写「见 Git 提交」）"）。
- **影响面**：规范引用归档件最危险——**归档件一旦被删，规范直接指向空气**（AGENTS.md:241-244 的"规范不得指向已删除的东西"是同一根因）。
- **建议处置**：① `多会话并行开发规约.md` 第 236/302 行改为"写进 `开发文档/未完成事项.md` 的旁路登记段"（**债务清单才是活落点**）；② `错误码与建议口径说明.md:19` 改为"见 Git 提交"或把口径就地写全；③ `未完成事项.md` 两处保留但确认写法统一为"见 Git 提交"。
- **是否已自带理由**：**部分**（主开发文档自带"导航位"理由；未完成事项自带"不作为派活依据"理由；**多会话规约与错误码说明没给理由**）。

---

### 条目 4 · 方案文档生命周期违规（哲学 4.5 + 规范第二/三节）

**盘面**：`开发文档/方案/` 共 **39 份 `*.md`**（`ls -1 开发文档/方案 | wc -l` → 39）。

#### 4.1【P2】五态声明缺失 / 非法（判据照规范 `:112-115` 原样）

**规范判据（原样照抄执行）**：
```bash
for f in 开发文档/方案/*.md; do
  rg -q '^> 状态：\*\*(起草|生效|已完结|已取代|已废弃)' "$f" || echo "状态非法/缺失: $f"
done
```
**实测输出（5 项）**：

| 文件 | 现有写法 | 问题 |
|---|---|---|
| `二次审计落地编排_20260918.md:6` | `> 状态：生效（**唯一编排入口**，本批次的派活依据）` | 缺 `**` 包裹 → 判据不认；**且见 4.2** |
| `拆上帝文件执行方案_20260918.md` | 头部**只有三行**（最后更新/口径/维护者），**无状态行** | 完全缺失 |
| `检索能力升级方案_20260918.md` | 同上，**无状态行**（文内 `:124` 提到"状态从「方案」改「已落地」"，但头部没改） | 完全缺失 |
| `稳定槽切换执行方案_20260918.md:6` | `> 状态：起草（**门槛：…未到「生效」条件**）` | 缺 `**` 包裹 |
| `端到端背压修复方案_20260918.md` | 头部只有三行，**无状态行** | 完全缺失 |

**其余 34 份**状态值分布（`rg --no-filename "^> 状态：" 开发文档/方案/*.md | sort | uniq -c` 实读）：生效 15+3(附表)+4(变体) / 起草 3+2(变体) / 已完结 5 / 已取代 3 / 已废弃 1。**五态值域本身没有越界**，问题只在"格式"与"缺项"。

- **违规条款**：哲学 4.5；`项目说明.md:183`「**判据**：方案文档头部必须有 `> 状态：` 五态声明」。
- **影响面**：规范 `:51` 明写"没有状态声明的方案文档，**不得作为派活依据**"——这 3 份无状态行的（拆上帝文件 / 检索能力升级 / 端到端背压）**按规范字面已经不能被派活**，而 `拆上帝文件执行方案` 与 `端到端背压修复方案` 正文都写着"待华哥确认后开工"。
- **建议处置**：**改文档**（补 `> 状态：**起草**（…）` 或标 `已取代/已完结`），不改文件名、不动内容。这是**纯头部补写**，零风险。
- **是否已自带理由**：**无**（缺项）。

#### 4.2【P1】两份文档自称"唯一编排入口"（撞 4.5 + 规范第三节"同一时刻只允许一份"）

- **现场证据（两份都在，且都不是纯历史件）**：
  - `开发文档/方案/执行编排_唯一入口_20260918.md:7` → `> **覆盖关系**：本文是**全仓唯一编排入口**。子方案只写「本件事怎么落」，**不得再写批次顺序**；`
  - `开发文档/方案/二次审计落地编排_20260918.md:6` → `> 状态：生效（**唯一编排入口**，本批次的派活依据）`
- **时间线（`git log` 实读）**：
  - `86f02287 2026-09-18 文档互踩收敛：立方案文档生命周期规范 + 三份旧编排标取代 + 唯一入口转正` — 改的是 `执行编排_唯一入口`
  - `482574ed 2026-09-18 二次审计落地：决策记录 0041-0046 + **编排入口** + 哲学 14.4 + 债务 §8.7（21 条）` — **同日更早**建立 `二次审计落地编排`
  - → 结论：**`二次审计落地编排` 先立、`执行编排_唯一入口` 后转正**，后立者没有回头把先立者的自称改掉（规范 `:61` 要求"换入口时旧入口必须同批改为 `已取代`"——**这条动作没做**）。
- **共享面证据**：`主开发文档.md:67-74` 专门开了 **「C+ 本批次编排（二次审计落地专用）」** 一节，`:71` 称其为"**二次审计唯一编排入口**"，`:73-74` 又补"该编排是**过程件**…证真结论已被 `未完成事项.md §8.7` 与决策 `0041`–`0046` 吸收，**编排本身不作为事实源**"。而 `执行编排_唯一入口` 在 C 栏（`:45`）被称"**全仓唯一编排入口**"。
- **违规条款**：哲学 4.5；规范第三节；规范第四节「自称唯一」判据（"正文出现「唯一权威 / 唯一主线 / 编排入口 / 唯一入口」的文档 > 1 份 → 保留真正的唯一者，其余改措辞并指向它"）。
- **影响面**：**新会话仍要分辨"以谁为准"**——这正是 2026-09-18 收敛要消灭的问题（`项目说明.md:184` 华哥原话"文档不能互相踩"）。注意规范 `:118-120` 的反验教训：判据必须**只扫 H1 标题**（`rg '^# .*(唯一权威|唯一主线|编排入口|唯一入口)' 开发文档/方案/*.md` 实测**只有 1 命中** = `执行编排（唯一入口）`）——**所以这两份用 H1 判据扫不出来，是靠读状态行才发现的，属"判据盲区"**。
- **建议处置**：**改文档措辞**：把 `二次审计落地编排_20260918.md:6` 的"唯一编排入口"改为"**本批次过程件（编排职责已并入 `执行编排_唯一入口_20260918.md`）**"，或按规范 `:61` 正式标 `> 状态：**已完结（2026-09-18，其结论已被 §8.7 与决策 0041–0046 吸收）**`。**不要删文件**。
- **是否已自带理由**：**部分**（主开发文档 `:73-74` 给了"过程件、不作为事实源"的定性，但**方案文件自己的状态行没改**——文件与索引不一致）。

#### 4.3【P1】三份"已取代"方案仍把**全仓批次表留在正文**（撞 4.5 编排冲突判据）

规范 `:72` 判据：「多份文档含「批次 + 依赖 + 顺序」表 → **只留编排入口，其余删表改为指引**」。

| 文件 | 状态行 | 正文残留 | 有无"取代理由" |
|---|---|---|---|
| `底座升级方案.md` | `> 状态：**已取代（2026-09-18）→ 见 执行编排_唯一入口_20260918.md**` | `:8` `## 当前批次：身份、路径、资源边界与发布一致性维修`、`:87` `### 当前批次完成标准` | **有**（`:4` 取代理由写得很完整） |
| `底座扩容总纲_一次性做透_20260917.md` | 同上 | `:294` `## 五、批次与依赖`（整节保留） | **有**（`:4`"其「五、批次与依赖」被新入口第五节统一编号取代"） |
| `底座大版本升级方案_20260918.md` | 同上 | `:43` `## 三、批次与依赖（**唯一主线**）`、`:46` 批 0–批 5 全表、`:85`"批次依赖一句话"、`:111` 批 0 状态行 | **有**（`:4`） |

- **违规条款**：哲学 4.5；规范第四节「编排冲突」；规范第三节"**不得再把批次表留在正文**（改为一行指引）"。
- **影响面**：三份都是**能被新会话搜到的活文件**（`rg "批 1|批 0"` 命中 9 份）；`底座大版本升级方案` 里那句 `（唯一主线）` 尤其危险——它是**行内自称**，H1 判据扫不到。
- **建议处置**：**删表留指引**（把三节的表格整体替换为一行"批次顺序见 `执行编排_唯一入口_20260918.md`"）。**理由已自带，动作未做** —— 属"收口半程"，不是"新问题"。
- **是否已自带理由**：**有**（三份的 `:4` 取代理由都在）。按 1.8：**豁免合法，但"已取代件不得再留批次表"这条义务不因有理由而免除**——因为留表本身就是"第二条腿"。

#### 4.4【P2】两份附表内容重叠 + 引用 `/tmp` 已删路径

- **路径**：`开发文档/方案/迁移面_附表.md`（260 行，`# 附表 D｜迁移面逐条清单`）、`开发文档/方案/适配层腿清单_附表.md`（645 行，`# 附表 A｜12 对包两列清单`）
- **现场证据**：
  - 两份**头部完全相同**（都是空行 + 空行 + `> 状态：**生效（2026-09-18）** —— 附表，随其主方案；…`），内容板块高度重叠（都是"逐对包 × 消费方 → 现 id → 目标 id"表）。
  - `适配层唯一化迁移清单_20260917.md:383` 写：`附表 A/B/C：/tmp/适配层腿清单_附表.md（已内联到本文末尾）`；`:385` 写 `附表 D（迁移面逐条）：/tmp/迁移面_附表.md` —— **`/tmp` 源路径已不存在**（脚本按规离库），**且 A/B/C 已内联进正文**（`:397` `## 附表 A｜12 对包两列清单`），**文件却又单独存在一份** → **同一份表在仓库里有两份副本**。
- **违规条款**：规范第四节「主题重叠」（"两份文档写同一件事的落地方案 → 合并到主题归属者，另一份改为引用"）；哲学 1.6 余续（/tmp 引用是历史留痕，属可接受）。
- **建议处置**：保留 `适配层腿清单_附表.md` 与 `迁移面_附表.md` **各一份**（它们已是 `/tmp` 副本的仓内正式落点，属"移入 方案/"的正常收口），但**删掉 `适配层唯一化迁移清单` 末尾的内联副本**，改为一行指引；同时把 `:383/:385` 的 `/tmp` 路径改写为"见本目录 `适配层腿清单_附表.md` / `迁移面_附表.md`"。
- **是否已自带理由**：**无**（`/tmp` 路径没标注"已删除"）。

#### 4.5【P2】16 份方案未登记进 `主开发文档.md` 的文档地图 C 栏

- **规范依据**：体例规范 `:147`「`开发文档/方案/` 下的每一份**都必须**在 `开发文档/主开发文档.md` 的 C 栏登记；**新增方案未登记即视为找不到的文档**」。
- **现场证据**（`rg -q -F <basename> 主开发文档.md` 逐份实测，**未登记 16 份**）：
  ```
  任务类型规范强制注入方案_20260918.md       发布门禁拆分方案_20260918.md
  底座升级方案.md                            底座大版本升级方案_20260918.md
  底座扩容总纲_一次性做透_20260917.md        开工句柄文件乐观锁方案_20260918.md
  拆上帝文件执行方案_20260918.md             文件改动流水与登记不冻结方案_20260918.md
  智能路由与模型打分方案_20260918.md         本机推理端点自动发现方案_20260918.md
  检索能力升级方案_20260918.md               模型探针与动态路由方案_20260918.md
  测试中心归口方案_20260918.md               稳定槽切换执行方案_20260918.md
  端到端背压修复方案_20260918.md             说明书存量治理施工表_20260918.md
  ```
- **影响面**：39 份方案里 **41% 在文档地图里搜不到**。新会话按"开工阅读顺序"（AGENTS.md:284）走文档地图 B 栏，**C 栏是唯一入口**，缺登记等于"这些文档不存在"。
- **建议处置**：**改文档**（把 C 栏按状态分组补全：已取代三份可单列一行"历史编排来源"）。
- **是否已自带理由**：**无**。

---

### 条目 5 · 目录约定与物理目录漂移（撞 AGENTS.md「目录约定」段）

**对账表**（左：`AGENTS.md:248-271` 的约定块；右：`ls -1A -d */` 实测 17 个顶层目录）：

| 顶层目录 | AGENTS 约定块有？ | 备注 |
|---|---|---|
| `公共契约/`、`平台控制面/`、`启动监督器/`、`运行核心/`、`前端核心/`、`后端核心/`、`支持库/`、`模块库/`、`项目适配层/`、`开发工具/`、`测试中心/`、`示例项目/`、`客户端/` | ✅ | 13 个对齐 |
| `开发文档/` | ✅（`:267-269`） | 对齐 |
| `工程缓存/` | ✅（`:270`） | 对齐 |
| **`技能库/`** | ❌ **约定块没有** | **真漂移**：它是 `公共契约/正式根.py:27` `正式根名表` 的**成员**（`("支持库","模块库","技能库","平台控制面","运行核心","启动监督器")`），`项目说明.md:875` 也说"全树装配会扫 支持库/ + 模块库/ + **技能库/**"，但 AGENTS 目录约定段整段没提 |
| **`运维脚本/`** | ❌ **约定块没有** | **真漂移**：`运维脚本/热接入.py` 是**现役运维入口**（文件头 `:8-10` 自述"40007 是全机唯一执行网关……热接入只动变更包"），属正式代码，却没进目录约定 |
| `.codegraph/` | ⚠️ 只在正文提到（`:11`、`:19`），**目录约定块没有** | 半漂移 |
| `.pytest_cache/`、`.ruff_cache/`、`__pycache__/` | ❌ | **不是漂移**（缓存，已 gitignore） |

**反向核对（约定有、物理没了）**：`rg -n "^开发文档/|^工程缓存/"` 全部存在 → **无"约定有而物理无"的项目**。

- **另注（`项目说明.md` 侧）**：`项目说明.md:802` 写"`公共契约/正式根.py` 的 `正式根名表`（**支持库 / 模块库 / 技能库 / 平台控制面 / 运行核心**）是「哪些顶层目录参与发现…」的唯一出处"——**现场实读该表有 6 项，比这句多一个 `启动监督器`**。属**文档与代码的轻微不同步**（该句漏列 `启动监督器`）。
- **违规条款**：哲学 2.3 精神（顶层结构要有唯一出处且不漂移）；AGENTS.md 自身"本文件只允许与其一致"（`:110-111`）。
- **影响面**：新会话读 AGENTS 目录约定**会漏掉 `技能库/` 与 `运维脚本/`**，而这两个一个参与全树装配、一个是在线热接入入口。
- **建议处置**：**改文档**（AGENTS.md 目录约定块补 `技能库/` 与 `运维脚本/` 两行；`项目说明.md:802` 的括号补 `启动监督器`）。**不动目录**。
- **是否已自带理由**：**无**。

---

### 条目 6 · `工程缓存/` 是否被跟踪 / 大文件与二进制残留

#### 6.1 本类未发现（`工程缓存/` 已正确忽略）

- **现场证据**：
  - `git ls-files | rg -c "^工程缓存/"` → **无输出（0 命中）**。
  - `git status --ignored --short` → `!! 工程缓存/` **整目录被忽略**（`.gitignore:12` `工程缓存/`，`:11` 注释"工程缓存可重新生成，不作为正式源代码"）。
  - `du -sh 工程缓存` → **6.0G**（本机在跑的真实生成物），**0 字节进版本库**。
  - `git status --short` → **完全干净（0 行）**，无未跟踪杂物散落在 `.gitignore` 之外。
- **建议处置**：无需处置。

#### 6.2 大文件与二进制残留：本类未发现

- **现场证据**：`git ls-files` 全量 `stat` 排序，**> 500KB 的文件：0 个**。最大 30 个里没有一个是媒体/安装包，全是大 `.py`/`.json`/`.md`（最大 213039 字节 = `开发工具/发布门禁/运行发布门禁.py`）。
- **二进制扩展名全量**（16 png / 10 pdf / 8 docx / 4 wav / 4 mp4 / 4 db / 2 xlsx / 2 sqlite3 / 2 rtf / 2 pptx / 1 jpg / 1 zip / 1 gguf）→ **逐个核对，全部是能力验证夹具**：
  - `支持库/**/验证夹具/*`、`夹具/*`（最小 PDF/docx/wav/png/xml/rtf/gguf），最大者 `模块库/自修复工具/验证夹具/最小Git仓库.zip` 仅 **3597 字节**；
  - `开发文档/参考资料/参考截图/易语言IDE/*.png`（6 张，华哥给的参考截图，属参考资料豁免面）；
  - `技能库/技能/交付收尾/验证夹具/{底座运行.db(61440), 权威状态.db(16384)}` — 技能验证夹具；
  - `支持库/**/验证夹具/验证模型.gguf` — **只有 24 字节**（占位模型，不是真模型）。
- **专项提示（**合法但需知会**）**：`支持库/后端/版本控制支持库/Git操作/验证夹具/临时仓库模板/git目录/**` 与 `支持库/适配层/Git提供者/验证夹具/临时仓库模板/git目录/**` —— 这是**两个嵌套的裸 Git 仓库**（`HEAD` / `config` / `index` / `objects/…` / `refs/heads/{main,source}`），**内容完全同源**（objects 哈希逐个一致）。它们是 Git 能力的验证夹具，属"最小可复现仓库模板"，**判合法**；但**两份副本**值得在下一轮做"夹具去重"专项（不是本轮结论）。
- **违规条款**：不适用。

---

### 条目 7 · 文档里指向已删除物的链接

**筛查面**：`MCP工具箱/`（2026-09-15 整目录删除）、`8766`（已下线）、`40006`（未启用）、`8866`（已删）、`临时文档/` 下已删批次目录。

#### 7.1【P1】`技能库/技能/交付收尾/SKILL.md:69` —— 指向已删目录，且**无任何"已删除"标注**

- **现场证据**：`rg -n "MCP工具箱" 技能库/技能/交付收尾/SKILL.md` → `:69  ## 边界与落点（与原 \`MCP工具箱/协作状态.py 收口登记\` 链路对齐）`
- **为何是 P1**：`开发文档/规范/批量开发流程与分级验证.md:22-25` 明写：「**已删除、任何文档与任务书都不得再引用**：8766 管理端 / 8866 / `MCP工具箱/` / `tool_job_submit` / `apply_file_patch` / `development_start` / `file_lease` / `claim_capability` / `workspace` / `mcp_feedback` / `verify_and_record` / `project_context` / `codegraph_explore`。」
- **影响面**：`技能库/技能/**/SKILL.md` 是**技能包定义件**（会被 Agent 当操作面读取），点向已删目录最容易让下游照抄。
- **建议处置**：**改文档**（改为"收口登记链路见 `平台控制面` 的对应能力"，或删掉括注）。
- **是否已自带理由**：**无**（既没标"已删除"，也没给替代落点）。

#### 7.2【P2】`开发文档/规范/` 与依赖目录下的死目录链接

| 位置 | 死目标 | 现场证据 | 判定 |
|---|---|---|---|
| `运行核心/统一网关/说明/限流边界说明.md:66`、`:79` | `开发文档/临时文档/20260917_限流边界收口/核对限流边界.py` | `ls -d 开发文档/临时文档/20260917_限流边界收口` → `No such file or directory` | **P2 真死链**（包级说明里的取证命令，照抄即失败） |
| `开发文档/方案/适配层唯一化迁移清单_20260917.md:641`、`适配层腿清单_附表.md:251`、`后端腿_附表E.md:374` | `开发文档/临时文档/20260915_MCP收敛/` | `ls -d` → `No such file or directory`；**但三处都自带了「★ 证据锚说明」**：「该轮已收口、底稿按「结论并入权威后即删」删除（**历史见 Git**）。**能力 id 与消费方 `文件:行号` 仍是有效的迁移依据**，底稿行号只作取证留痕」 | **按 1.8 判合法**（理由自证 + 正确使用了"见 Git"写法）—— **是本次审计里"正确的处理范本"** |
| `开发文档/规范/`、`模块库/**/说明/`、`支持库/**/说明/` 里的 `MCP工具箱/*.py` 引用（约 25 份文件命中） | 已删目录 | 多数写作"**原** MCP 工具 `test_resource`（`MCP工具箱/测试资源.py`，461 行）""**旧** `MCP工具箱/文件租约.py`""`MCP工具箱/` 层**已整目录删除**" | **多数合法**（用了"原/旧/已删"限定词）。**未加限定词的**：`模块库/组件生成/说明/设计说明.md:14`（`MCP工具箱/项目服务.py:833 是全仓唯一生产调用点`）、`技能库/技能/交付收尾/SKILL.md:69`、`支持库/后端/代码解析支持库/代码地图/说明/设计说明.md:27`（"**不落 `MCP工具箱`**"——这条是设计决策，合法） |
| `开发文档/当前流程图.md:119`、`:141` | `8866` / `MCP工具箱/能力网关.py` | 两条都**自带"已随整目录删除/已删除下线"** | **合法** |
| `开发工具/能力网关/能力网关.py:158` | `8866` | help 文本**自带"8866 为已删除的旧 MCP工具箱体系"** | **合法** |
| `README.md:800` | `8766` | 「> 历史口径（**已失效，勿再沿用**）：8766 HTTP MCP…已于 2026-09-15 随 D 清场下线」 | **合法（范本）** |
| `AGENTS.md:9/17/26/60/64/72/242/266` | `8766`/`40006`/`MCP工具箱/` | 全部**自带"已清场下线/当前未启用/无监听"** | **合法** |

- **违规条款**：AGENTS.md:241-244「**规范不得指向已删除的东西**……指向已清场接入面会产生"整份规范不可执行"」；哲学 2.x（依赖本地具备）。
- **建议处置**：**改文档** ① `限流边界说明.md:66/:79` 改为"见 Git 提交"或改指向现役脚本；② `组件生成/说明/设计说明.md:14` 给 `MCP工具箱/项目服务.py` 加"（已删除，见 Git 提交）"限定词；③ `技能库/技能/交付收尾/SKILL.md:69` 同 7.1。
- **是否已自带理由**：**多数有，三处没有**（见上表加粗）。

---

## 二、处置建议表

> 「自带理由」列 = 该条目在现场是否读到**文件自身的理由说明**（按哲学 1.8：无理由的豁免判违规）。
> 「动作类型」列：**移出** / **删** / **改文档** / **保留并补理由** / **待裁定**。

| # | 路径 | 类别 | 违规条款 | 动作类型 | 自带理由 | 优先级 |
|---|---|---|---:|---|---|---|
| 1.1 | `开发文档/分析/素材_DGX-Spark本地算力视频_20260918/{测端点扫描.py,测mdns标准库.py,测底座接管.sh,复核对端接管.sh}` | 一次性探针脚本留库 | 哲学 1.6；AGENTS:20 | **移出仓库**（→ `/private/tmp/系统平台临时脚本_20260918/`，该目录仍在） | ❌ 无 | **P0** |
| 2.2 | `开发工具/性能基准/结果/基准结果-20260918-{182155,182322}.json` | 制品入库（撞本包说明） | 哲学 1.7；本包说明 `:4` | **删**（保留 1 份并在说明里点名，或改为锚点值写进说明） | ⚠️ 半（说明有例外但未点名） | **P0** |
| 1.2 | `开发文档/临时文档/01_补关键值断言_无法定值与剩余缺口_20260918.md` | 临时过程稿留库 | 哲学 1.6；决策 `0004` | **删**（结论先并入 `未完成事项.md` 第 13 项） | ⚠️ 部分（仅说明"未做完"） | **P1** |
| 4.2 | `开发文档/方案/二次审计落地编排_20260918.md:6` | 双自称唯一编排入口 | 哲学 4.5；规范三/四节 | **改文档**（改措辞或标已完结） | ⚠️ 半（索引已定性，文件未改） | **P1** |
| 4.3 | `开发文档/方案/{底座升级方案.md, 底座扩容总纲_一次性做透_20260917.md, 底座大版本升级方案_20260918.md}` | 已取代件仍留全仓批次表 | 哲学 4.5；规范三/四节 | **改文档**（删表留一行指引） | ✅ 有（`:4` 取代理由） | **P1** |
| 7.1 | `技能库/技能/交付收尾/SKILL.md:69` | 指向已删物 · 无标注 | AGENTS:241-244；规范批量开发流程 `:22` | **改文档** | ❌ 无 | **P1** |
| 7.2 | `运行核心/统一网关/说明/限流边界说明.md:66,:79` | 指向已删目录 | 同上 | **改文档** | ❌ 无 | **P1** |
| 4.1 | `开发文档/方案/{拆上帝文件执行方案, 检索能力升级方案, 端到端背压修复方案}_20260918.md` | 缺五态状态行 | 哲学 4.5；`项目说明.md:183` | **改文档**（补头部） | ❌ 无 | **P2** |
| 4.1 | `开发文档/方案/{二次审计落地编排, 稳定槽切换执行方案}_20260918.md` | 状态行格式非法（缺 `**`） | 同上 | **改文档** | ❌ 无 | **P2** |
| 4.5 | `开发文档/主开发文档.md` C 栏 | 16 份方案未登记 | 体例规范 `:147` | **改文档** | ❌ 无 | **P2** |
| 5 | `AGENTS.md:248-271` 目录约定块 + `开发文档/项目说明.md:802` | 目录约定漂移（缺 `技能库/`、`运维脚本/`；正式根表漏列 `启动监督器`） | 哲学 2.3 精神；AGENTS 自洽条款 | **改文档** | ❌ 无 | **P2** |
| 3.2 | `开发文档/规范/多会话并行开发规约.md:236,:302` | 规范把归档件当写目标 | AGENTS:282；体例规范 `:181` | **改文档**（落点改为债务清单） | ❌ 无 | **P2** |
| 3.2 | `开发工具/统一能力入口/视图/错误码与建议口径说明.md:19` | 包级说明引用归档件作事实源 | 同上 | **改文档**或**保留并补理由** | ❌ 无 | **P2** |
| 4.4 | `开发文档/方案/适配层唯一化迁移清单_20260917.md:383,:385`（`/tmp` 路径）+ 末尾内联附表 | 引用已删路径 + 附表双副本 | 哲学 1.6 余续；规范四节 | **改文档** | ❌ 无 | **P2** |
| 7.2 | `模块库/组件生成/说明/设计说明.md:14` | 指向已删物 · 无限定词 | AGENTS:241-244 | **改文档**（补"已删除"） | ❌ 无 | **P2** |
| — | 条目 1.4 全部 8 组（系统探针 / 模型探针 / 导入探针 / 临时文件.py / 测试_*探针 / 迁移类方案 / 交付记录验收件 / 归档内验收契约.json） | 命名含敏感词但合法 | — | **保留**（不动） | ✅ 有 | — |
| — | `开发文档/交付记录/20260918_夜间推进简报_给华哥.md` | 面交件（分界线样本） | — | **保留**（并作为 09-18 后"不再新增"的界桩） | ✅ 有 | — |
| — | `工程缓存/`（6.0G） | 生成物 | — | **保留**（已 gitignore） | ✅ 有（`.gitignore:11-12`） | — |
| — | `支持库/**/验证夹具/*`、`夹具/*`（含 2 个嵌套裸 Git 仓库） | 验证夹具二进制 | — | **保留**；嵌套 Git 仓库**下一轮专项去重** | ✅ 有 | — |
| — | `开发文档/归档/` 全部 5 条 | 归档 | — | **保留**（命名全部合规） | ✅ 有 | — |

**登记但本轮不动（供下一轮）**：
- `支持库/后端/版本控制支持库/Git操作/验证夹具/临时仓库模板/` 与 `支持库/适配层/Git提供者/验证夹具/临时仓库模板/` **同源双副本**（objects 哈希一致）→ 夹具去重候选。
- `开发文档/分析/` 下 **28 份** 临时审计报告（`ls -1 开发文档/分析/*.md | wc -l` = 26 + 素材目录 + …）—— 与"不再新增临时审计 Markdown"（AGENTS:20）存在时间差，**建议下一轮做"分析/ 目录生命周期"专项**（本轮只登记）。

---

## 三、「不是缺陷（设计取舍）」清单

以下条目**现场看着像违规，读完理由后判合法**——列出以防下一轮重复报、重复改。

1. **`开发文档/归档/` 存在且被引用**：哲学 1.7 的"不做本地归档"经 **2026-09-17 边界澄清**只管成品/制品，文档历史另按体例规范第八节处置——**只有一个顶层归档是符合的**，不是"留了归档库"。
2. **`开发工具/` 下 20 个单文件脚本**（`全量重算摘要.py`、`发布门禁`、`包体检.py`、`开发效率巡检.py`…）：逐个核对，**全部是正式工具**（在 `AGENTS.md` 验收段被点名、被门禁/任务类型规范 `:68` 引用、或属 `开发编译口/` 体系）。**不是"可疑单文件脚本"**。
3. **`/tmp`、`/private/tmp/系统平台临时脚本_20260918/` 出现在文档里**：这是**规范要求的正确姿势**（`多会话并行开发规约.md:349`「临时脚本必须在受管临时目录……**禁止留在仓库内**」），且 `开发文档/分析/*_回传_20260918.md` 里多处明写"临时脚本已全部移出仓库到 `/private/tmp/…`"——**是合规证据，不是违规**。
4. **归档件被**交付记录**引用**（`20260911_热接入子包归属修复/剩余风险.md:18`）：交付记录属"历史留档、只读不改"（主开发文档:79），不属"权威文档正文"。
5. **`主开发文档.md:81` 把 `归档/` 列为导航链接**：体例规范 `:145` **已知例外并已收口**（"目录名与链接仅为文档地图导航位"）。
6. **`技能库/` 不在"正式根"与 AGENTS 目录约定的差异**：`技能库/` 确在 `正式根名表` 内（`公共契约/正式根.py:27`）——它是**正式层**，属"AGENTS 漏登记"（见条目 5），不是"多出来的野目录"。
7. **两份附表（`适配层腿清单_附表.md` / `迁移面_附表.md`）单独成文件**：它们是 `/tmp` 副本**正式移入 `方案/` 的落点**（决策 `0004`"需要长期保留的可执行规格 → 移入 `方案/`"），**方向正确**；问题只在"与内联副本重复"（见 4.4）。
8. **`开发文档/参考资料/外部源码研究/` 下的 `.md` 不受文档体例约束**：体例规范 `:5` **明列三类豁免**（`工程缓存/`、`开发文档/参考资料/`、`开发文档/归档/`）。
9. **`工程缓存/` 高达 6.0G**：`7.9 生成物三律` + `.gitignore:11-12` 明确它是"可删除、可重新生成"的生成物区，且**仓库内已有回收能力**（`开发工具/工程缓存回收.py`：按引用/按访问时间/并行日志/验证运行残留四类，`--试运行` 可先看；`开发工具/提供者环境回收.py`：只留现用摘要）。**不进版本库 = 达标**。

---

## 四、审计局限（必须说清的边界）

1. **本报告只覆盖"仓库内可枚举的物理对象"**：不做代码质量、能力正确性、测试覆盖判定，不跑任何测试（任务纪律禁止），因此**"某文件已无人引用"≠"可删"** 的结论一律降级为"待裁定"。
2. **未做全仓死链穷举**：条目 7 只扫了 `MCP工具箱`/`8766`/`40006`/`8866`/临时文档已删目录五类**已知清场面**；未对全部 700 份 `.md` 做逐路径可达性校验（`审计报告_全仓现状_20260918.md:215` 已记"`开发文档/` 正文 284 条唯一死路径、463 处命中"——**那是历史快照，本轮未复算**）。
3. **未读全部 39 份方案的正文**：条目 4 的三条判据（状态行 / 自称唯一 / 全仓批次表）走的是**模式扫描 + 命中处精读**，可能存在"用别的词表达同一义务"的漏网（例：`底座大版本升级方案:43` 的"（唯一主线）"就是靠行内扫描才捞到的，**说明 H1 判据确有盲区**）。
4. **`git log` 时间线只用于判"谁先谁后"**，不用于判"谁对"——`二次审计落地编排` 与 `执行编排_唯一入口` 的先后关系有提交证据，但**哪个该留由华哥裁定**（本报告只给倾向，不代替裁决）。
5. **判"废弃但需保留"依赖"文件是否自带理由"**：本轮有 3 处是"理由不足但仍需人判"（`性能基准/结果/`、`临时文档/01_*`、`二次审计落地编排`），已按 1.8 标为 **⚠️ 半**，未擅自判违规。
6. **未动任何文件**：本报告是唯一写入物；所有"建议处置"均为登记，**未执行删除/移动/改文件/`git add`/`git commit`**；`git status` 在审计开始与结束时均为**完全干净**。

---

## 五、复现命令（全量）

```bash
export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台

# 条目 1：临时脚本双路径查
git ls-files | rg -i "探针|临时|一次性|迁移|验收|检查一下"
rg -n "一次性脚本|用完即删|本脚本只在本次|跑完即删|事后删除" --glob "!开发文档/归档/**" --glob "!工程缓存/**" .
git ls-files 开发文档 | rg "\.(py|sh)$"          # → 仅素材目录 4 个

# 条目 2：制品进仓
git ls-files | rg -i "\.(zip|dmg|pkg|exe|msi|whl|jar|so|dylib|dll)$"
git ls-files 开发工具/性能基准/结果/
rg -n "提交口径" 开发工具/性能基准/说明/性能基准说明.md

# 条目 3：归档违规
git ls-files | awk -F/ '{for(i=1;i<=NF;i++) if($i=="归档"){print substr($0,1,index($0,"归档")+6); break}}' | sort -u
rg -n "归档/" --glob "*.md" -g '!开发文档/归档/**' .

# 条目 4：方案生命周期
ls -1 开发文档/方案 | wc -l
for f in 开发文档/方案/*.md; do rg -q '^> 状态：\*\*(起草|生效|已完结|已取代|已废弃)' "$f" || echo "状态非法/缺失: $f"; done
rg -n --with-filename "^> 状态：" 开发文档/方案/*.md
rg -n '^# .*(唯一权威|唯一主线|编排入口|唯一入口)' 开发文档/方案/*.md   # H1 判据 → 只 1 命中

# 条目 5：目录约定对账
ls -1A -d */        # 17 个
rg -n "^公共契约/|^技能库/|^运维脚本/" AGENTS.md
rg -n -A3 "正式根名表" 公共契约/正式根.py

# 条目 6：缓存与大文件
git ls-files | rg -c "^工程缓存/"           # → 0
git status --ignored --short | rg "工程缓存"
git ls-files -z | xargs -0 -I{} sh -c 'test -f "{}" && stat -f "%z %N" "{}"' | sort -rn | head -30

# 条目 7：死链
rg -n "MCP工具箱|8766|40006|8866" --glob "*.md" -g '!开发文档/归档/**' -g '!开发文档/参考资料/**' .
ls -d 开发文档/临时文档/20260917_限流边界收口 开发文档/临时文档/20260915_MCP收敛 2>&1
```
