# 系统工程平台 · 批Q·Q-k：全仓静态编译 3 条「只报告判红」归零（2026-09-24）

> 开工ID：`开工-20260924-103642-fe67`（首轮）／`开工-20260924-123859-4ea2`（续做落盘）
> 口径：**分析件**；只记本轮实测与处置，不新增设计结论
> 上游：`开发文档/未完成事项.md` 批Q 收尾读数「只报告判红 3 条（本批未清零，已派 Q-k 归零）」
<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

---

## 〇、三条处置结论（先给结果）

| # | 检查项 | 定性 | 权威节点 | 改法 | 改后读数（静态编译 类型=1，HEAD `141fab83`） |
|---|---|---|---|---|---|
| 1 | 测试伪装门禁 | **真缺陷**（测试 patch 被测模块本体成员 ＝ 测的是假对象） | `开发工具/测试伪装门禁实现/桩检测.py::判定规则1` ＋ `开发工具/测试伪装门禁.py::存量违约上限` | 走**生产自带的注入口**（`存储目录` 参数 ＋ 两个注册口），patch 一处不留 | **检查项「测试伪装门禁」通过 · 退出码 0**（已不在判红列表） |
| 2 | md查重 | **非缺陷**（降噪器固有读数 ＋ 模板同源；不是「文件内有重复段落」） | `开发工具/MD文档生成/查重.py`（判据本体）＋ `开发工具/开发编译口/编译口.py:174`（检查项登记，角色＝只报告） | 不动生成物、不动生成器、不放宽判据；登记「非缺陷·待裁」 | 仍 `只报告判红`（退出码 1）——**结构上不可达 0**，见 §二 |
| 3 | 制品新鲜度 | **非缺陷**（开发循环里按设计必红：提交动作本身移动 HEAD） | `开发工具/制品新鲜度门禁.py::判定`（转调发布门禁唯一腿 `校验制品来源绑定`／`校验制品落后提交数`） | 归零出口＝**重建制品 ＋ 重启网关**（发布动作，**主会话收口时做**） | 仍 `只报告判红`（退出码 1，落后 HEAD 1 个提交） |

**本轮总读数**（`自修复工具.静态编译`，`类型=1`，`模块ID=平台控制面/能力目录/文件租约存储.py ＋ 测试中心/公共契约/测试_仓库只读锁.py`）：

```
成功数量 18 ／ 失败数量 2 ／ 检查项数 20 ／ 失败门禁 [] ／ 五态结论 只报告 ／ 退出码 0
结论原文：五态结论：只报告 —— 无阻断红项，但只报告类有判红：md查重（退出码 1）、制品新鲜度（退出码 1）
```

对比改前（`类型=0` 全仓，2026-09-24 10:37）：`成功 17 ／ 失败数量 3 ／ 失败门禁 []`，只报红 3 条（测试伪装门禁、md查重、制品新鲜度）。**3 条 → 2 条，减掉的那条是真缺陷、已归零。**

---

## 一、测试伪装门禁：真缺陷，按权威节点改测试（已归零）

### 1.1 判据与「为什么不能走豁免」

- 判据本体：`桩检测.判定规则1` —— 目标模块落在生产命名空间即命中；**与本文件 import 的生产模块精确相等 = `P1 被测模块本体`**。例外只有两类：`wraps=`（自动豁免）与 `豁免清单.json` 人工登记。
- ★ 关键（决定了出口只有一条）：`测试伪装门禁.py` 的判绿口径是 `计数 = 违规数 + 清单豁免数 <= 存量违约上限(213)` —— **被清单豁免的条目照样计入计数**，docstring 原文「登记进豁免清单**不能**把门禁变绿（唯一合法路径是修代码）」。故「登记豁免」不是出口，**必须改代码**。

### 1.2 现场事实（改前）

命中行 `测试中心/公共契约/测试_仓库只读锁.py:1338`：

```python
补丁 = mock.patch.object(租约边界, "默认存储目录", 临时.name)   # 规则1·P1 本体成员 ＋ 规则2 裸桩
```

这一处同时贡献 **规则1 ＋1（P1 本体成员）** 与 **规则2 ＋1** ⇒ 计数 215 > 上限 213 ⇒ 判红。

**定性依据（为什么是真缺陷、不是「可登记的依赖注入」）**：该 patch 换掉的是**被测模块所在链路的模块级常量**，被换掉之后「`查询()` 读到哪一本租约账」就不再由生产代码决定 —— 用例断言的是**夹具指定的那本账**，测的确实是假对象。判据原文那句「疑似依赖注入（替换值是真实对象引用，可登记豁免）」是**提示**，不是出口（见 1.1）。

### 1.3 改法（走生产自带的注入口，不新增机制）

`存储目录` **本来就是租约这一族腿的通用口径**（`申请文件租约` / `查询文件租约` / `回收过期文件租约` / `续租` / `释放` 都收它），只有两个「注册腿」写死 `取存储("")`：

| 落点 | 改前 | 改后 |
|---|---|---|
| `平台控制面/能力目录/文件租约存储.py::活跃写租约相对路径集` | `活跃写租约相对路径集()` ＋ `取存储("")` | `活跃写租约相对路径集(存储目录: Any = "")` ＋ `取存储(存储目录)` |
| 同文件 `::回收过期租约并报数` | `回收过期租约并报数(项目根)` ＋ `取存储("")` ＋ `回收过期文件租约(项目根=str(根))` | 加 `存储目录: Any = ""`，并把 `存储目录` **一路传到回收腿** `回收过期文件租约(项目根=str(根), 存储目录=存储目录)` |

缺省 `""` ＝ **逐字沿用旧行为**（判据本体一个字未动、无第二套实现）；显式给出即读那一本账。第二处那个「一路传到回收腿」是必须的：只读快照读这本账、回收改判改那本账，就是「读的是 A 账、改的是 B 账」。

测试侧（`测试中心/公共契约/测试_仓库只读锁.py`，类 `Test查询即懒回收_过期窗口第三态`）：

- 删掉 `mock.patch.object(...)`（`mock` 在本文件另有 3 处 `autospec=True` 的正当用法，保留）；
- `_换账目录()` 只开临时目录并记 `self.账目录`；
- `_认领()` 传 `存储目录=self.账目录`；`_账上记录()` 用 `取存储(self.账目录)`；
- `_接真租约账()` 把**两个**注册口都绑到同一本账：`设写租约事实源(lambda: 活跃写租约相对路径集(存储目录=账目录))` ＋ `设租约回收器(lambda 根: 回收过期租约并报数(根, 存储目录=账目录))`，并按 `addCleanup` 还原（缺一即假绿：只绑事实源，回收器仍去改判平台默认那本账）。

### 1.4 读数（改前 → 改后）

| 读数 | 改前 | 改后 |
|---|---|---|
| 检查项「测试伪装门禁」（MCP 编译口） | `退出码=1`，只报红：`[规则1·patch被测对象本体] …:1338 — [P1 被测模块本体·本体成员]` | **通过 · 退出码 0**（不在判红列表） |
| 门禁直跑（**取证性**，见 §六） | 规则1 违规 **94**（P1 **1**／P2 93）＋规则2 **110** ＋规则3 **11** ⇒ 计数 **215 > 上限 213**，退出码 1 | 规则1 违规 **93**（P1 **0**）＋规则2 **109** ＋规则3 **11** ⇒ 计数 **213 = 上限 213**，退出码 0 |

### 1.5 副作用核查（不许「修一个红、带出另一个」）

同一轮 `动态编译`（`类型=1`）的**定向测试**里 `测试中心.公共契约.测试_仓库只读锁` 判红 `failures=12, errors=4` —— **既存红，与本改动无关**：

- `开发文档/未完成事项.md` N-8／O-17／O-12 与 `开发文档/分析/审计落点_单一节点收口_20260924.md` §9.1 已记：`python3.14 -m unittest 测试中心.公共契约.测试_仓库只读锁` **改前/改后均为 `FAILED (failures=12, errors=4)`**（16 条结构性不可达，待裁）；
- 本轮实测同一读数（`failures=12, errors=4`，失败集合逐条一致）；
- 本改动涉及的类 `Test查询即懒回收_过期窗口第三态` **5 条用例全绿**（含反向样本 `test_判据本体未改宽_过期租约不进豁免面`、`test_摘掉回收器即回到修前形状_过期窗口算缺口`）。

---

## 二、md查重：非缺陷（降噪器固有读数 ＋ 模板同源）

### 2.1 先纠正现场记录（这一条是本件最重要的增量）

批Q 收尾把这一条记成「`模块库/文档生成/说明/目录树与命令.md`（该文件内有重复段落/重复条目）」。**实测不成立**：

- 编译口对只报告项只保留输出**尾部 6 行**，而 `--查重 --取前 3` 每对占 3 行 ⇒ 尾巴恰好落在 `模块库/文档生成/说明/目录树与命令.md` × `模块库/文档解析/说明/目录树与命令.md` 这一对上。**那行的真实含义是「与另一份同类型生成物相似 0.88」，不是「文件内有重复段落」。**
- 该文件**文件内无重复**：非空 34 行、**重复行 0、重复段落 0**。
- 与 `模块库/文档解析/说明/目录树与命令.md` 的 `diff -u` 差异**只有包名／包id／目录树条目／命令名与参数**（同模板、同骨架、逐字同源的部分是共用口径句）。⇒ 生成器**没有**串包、没有写错树，**生成侧无落点**。

### 2.2 定性

- 该件是**生成物**（机器印记 `生成器：开发工具.MD文档生成`；`开发文档/规范/文档类型判据.json` 类型 `包级说明-目录树与命令`：树块＝生成区，`公开命令`／`规范边界`＝人工区）。
- 判据本体 `查重.py` 自己声明过这类是**已知未收口**：「`使用说明.md` / `目录树与命令.md` 这类**整份生成物**彼此之间的相似（另有存量，未收口）」，并把定位写成**降噪器**（「把候选摆到人面前供裁决，不阻断提交」）。⇒ **非缺陷**。

### 2.3 为什么退出码 0「结构上不可达」

`--查重`（全仓模式）**只要有任意一对 ≥0.40 就 `return 1`**：本轮实测 **546 份参与、866 对**（尾巴这对 0.88）。要它退 0，只能改判据射程或改该检查项的角色/退出码语义 —— 两者都是**放宽判据 / 改裁决**，属华哥裁决项，本子任务不做（也不该做）。

### 2.4 改法与读数

- 改法：**不动生成物**（机器管理件，手改会被「拒绝手写md」判成手写件，且下次 `--写盘` 直接覆盖）、**不动生成器**（无落点）、**不放宽判据**；本件登记为「非缺陷·待裁」，把候选摆给裁决。
- 读数：改前 `[只报告] md查重 退出码=1 耗时=3.68s`；改后 `退出码=1 耗时=4.02s`（尾巴同一对）。

---

## 三、制品新鲜度：非缺陷（归零出口在主会话）

### 3.1 复跑取证（未自愈，但形态已变）

| 时点 | 读数 |
|---|---|
| 批Q 收尾（主会话） | `旧制品阻断：当前工作区（含未提交变更）真实字节指纹已变化`（来源 `befb758e`、落后 **0**） |
| 本轮复跑（10:37 静态编译 `类型=0`） | 来源 `befb758e` ≠ HEAD `f48ef31c`，**落后提交数 1**，`来源绑定通过=false` |
| 本轮复跑（12:4x 静态编译 `类型=1`，HEAD `141fab83`） | 来源 `23ec6a2c` ≠ HEAD `141fab83`，**落后提交数 1**，`来源绑定通过=false` |

⇒ 「工作区指纹已变化」那一半随提交**已自愈**；剩下的一半是「来源提交 ≠ 当前 HEAD」。**中间那次读数尤其说明问题**：R-17 已在 `23ec6a2c` 重建过制品（`23d9c42552f35e5d` 已安装、网关重启），**HEAD 一动到 `141fab83` 它立刻又红**。

### 3.2 定性

**非缺陷**：`制品新鲜度门禁.py` 的 docstring 原文已写明本项在开发循环里按设计必红 —— 「`校验制品来源绑定` 要求『制品来源提交 = 当前 HEAD』，而**提交这个动作本身就会移动 HEAD** ⇒ 制品只能在提交之后重建 ⇒ 开发循环里『阻断』在结构上不可能自洽（要提交就得先有制品，要有制品就得先提交）。阻断留在**发布门禁**（提交 → 重建 → 再发，那条链闭合）；开发循环只负责把『落后几个提交』**说清楚**」。它在本口的角色登记就是**只报告**。

### 3.3 归零出口

**重建制品 ＋ 重启网关**（`客户端/构建平台客户端.py --安装` ＋ 重启网关）—— 这是**发布动作**，按本轮裁决**由主会话收口时做**；本子任务不做（重建会移动运行态、重启会中断正在服务的网关）。

---

## 四、改了哪些文件（精确路径）

| 文件 | 改动 |
|---|---|
| `平台控制面/能力目录/文件租约存储.py` | 两个注册腿补 `存储目录: Any = ""` 注入口 ＋ 把 `存储目录` 一路传到回收腿（4 处精确替换） |
| `测试中心/公共契约/测试_仓库只读锁.py` | 删 `mock.patch.object(租约边界, "默认存储目录", …)`；账目录改走 `存储目录` 参数 ＋ 两个注册口；类/夹具注释同步（4 处精确替换） |
| `开发文档/分析/批Q单据_Qk_只报告判红归零_20260924.md` | 本件（新增） |

**未改**：`模块库/文档生成/说明/目录树与命令.md`（生成物，§二）、任何判据文件、任何基线数值。**未 git 提交**（按裁决由主会话统一收口）—— 收口实况见 §7.5。

---

## 五、未闭环项与原因

| 项 | 状态 | 原因／出口 |
|---|---|---|
| md查重 退出码 1 | 未闭环（**非缺陷·待裁**） | 降噪器只要有一对 ≥0.40 就退 1（866 对）；归零＝改射程或改角色语义 ⇒ 须裁决 |
| 制品新鲜度 退出码 1 | 未闭环（**归零出口在主会话**） | 须「重建制品 ＋ 重启网关」；且**每次提交后必红**（判据件已如实声明） |
| `测试中心.公共契约.测试_仓库只读锁` 12 failures ＋ 4 errors | 未闭环（**既存红**，非本改动引入） | N-8／O-17／O-12：批F 断言与批N 口径正面冲突，16 条结构性不可达，须裁决（推荐「按 `所有者` 分档」） |

---

## 六、取证方式说明（哪一类终端只读）

- **结论性读数一律取 MCP 编译口**：`自修复工具.静态编译`（`类型=0` 全仓一次、`类型=1` 本改动集一次）与 `自修复工具.动态编译`（`类型=1`，含定向测试）。
- 终端只用于两类**明令的只读取证**：① `git status/log/show` 只读状态；② `开发工具/测试伪装门禁.py --json` 与 `开发工具.MD文档生成 --查重`、`diff -u` 的**取证性直跑**（只为拿判据原文与逐字差异，**不作为结论性读数**，结论以上表 MCP 编译口读数为准）。
- 路径传参：普通路径一律**项目根相对路径**（posix、不带 `..`），只有 `项目根`／`源码根` 写绝对路径。

---

## 七、收口复跑取证（2026-09-24 12:41，全仓 `类型=0`，HEAD `141fab83`）

> 目的：验证第 1 条是否真归零、第 3 条形态是否变化。**读数取 MCP 编译口**（异步腿：`任务提交` → `任务id d1a48ad8497745a7` → `任务查询`）。

```
命令：/opt/homebrew/opt/python@3.14/bin/python3.14 -m 开发工具.开发编译口.编译口 --不跑点名 --全仓
成功数量 16 ／ 失败数量 4 ／ 检查项数 20 ／ 受影响包数 1 ／ 五态结论 失败 ／ 退出码 1
结论原文：五态结论：失败 —— 阻断类红项：契约漂移（退出码 1）、公开调用完整性（退出码 1）、生成物复跑仍红（2 条）。按红项修完再跑一遍。
```

### 7.1 第 1 条：已归零（复跑确认，两条腿互证）

**`测试伪装门禁` 不在本轮红项里** —— 20 个检查项里它已通过（改前它是判红项）。本轮 4 条红项逐条为：`契约漂移`（阻断）、`公开调用完整性`（阻断）、`md查重`（只报告）、`制品新鲜度`（只报告）。

⇒ **3 条只报红 → 2 条**，减掉的正是唯一那条真缺陷。

**第二条腿（取证性直跑，`开发工具.测试伪装门禁`）**：

```
（测试伪装门禁通过：计数 213 <= 存量违约上限 213；未新增（存量未清零，通过只表示「没比基线更差」，不代表测试桩已合规））
规则1 禁 patch 被测对象/生产路径：违规 93（P1 被测模块本体 0／P2 生产路径 93；本体成员 47／依赖边界 37／未知 9）｜wraps 豁免 17｜清单豁免 0｜观察 18
退出码=0
```

★ 关键读数：**`P1 被测模块本体 0`**（改前为 1，那 1 条正是 `测试_仓库只读锁.py:1338`）。且 `清单豁免 0` —— 不是靠登记豁免换来的绿，是**代码真改**换来的绿。

### 7.2 第 3 条：形态与 §三 逐字一致（未自愈）

```
制品来源提交 23ec6a2ceeab1eaff57de61b03dd10c36f1fbf1b ／ HEAD 141fab835463ca275a31054e316a93af18b7b75a
落后提交数 1 ／ 落后阈值 0 ／ 来源绑定通过 false
说明：制品落后 HEAD 1 个提交 —— 网关跑的是旧装配，须重建制品 + 重启网关 ⇒ 源码已改而运行态未改
```

⇒ 「工作区指纹已变化」那一半随提交**已自愈**；剩下的一半是「来源提交 ≠ 当前 HEAD」，归零出口＝**重建制品 ＋ 重启网关**（发布动作，主会话收口时做）。

### 7.3 新出现的 2 条**阻断**红：落在本任务写集之外（如实回报，未越界处理）

| 检查项 | 红项原文 | 归属 |
|---|---|---|
| 契约漂移 | `[判红] 模块库/开工编排: 实现默认值差异｜开工编排.开工准备#资料级别#'指针'#None` | 他会话在飞的 `模块库/开工编排/*`（R-16／R-21「一次取全／默认送达」；工作区现存 9 个文件未提交改动） |
| 公开调用完整性 | `[存量] 支持库/后端/OCR识别支持库/OCR识别｜内部层-可绑定集仍含 × 3` | `OCR识别支持库`，与本任务无关 |

判据：`开工编排.开工准备` 的 `资料级别` 在 `能力定义.json` 与 `能力契约/参数契约.json` 里声明默认值 `"指针"`，而本轮编译读到的**实现侧默认值**仍是 `None`（工作区源码 `模块库/开工编排/实现/开工编排.py:544` 已改成 `"指针"`）⇒ 属**契约侧未随源码刷新**的读数，归因与处置落在开工编排那条线上。

★ 与 10:37 那轮全仓读数（`成功 17 ／ 失败 3 ／ 失败门禁 []`）对比：这 2 条**阻断**红是 10:37 之后出现的（R-16 把 `资料级别` 加进契约默认值，实现侧当时给 `None`）。本任务**未碰** `模块库/开工编排/` 与 `OCR识别支持库/` 的任何文件（`git status` 可核：本任务写集只有 §四 那三份）。

### 7.4 副作用复核：改后定向测试的**红数逐字未变**（12 failures ＋ 4 errors）

§1.5 的「既存红」结论本轮**复跑确认**（同一命令 `--文件 测试中心/公共契约/测试_仓库只读锁.py`；分片读数取自 MCP 回执原文）：

| 时点 | 分片进度串 | 用例数 | failures | errors |
|---|---|---|---|---|
| 改前（两轮，`测试伪装门禁` 仍判红） | `............EF..EEE..FFF....F.FF.FFFF.F............` | 51 | **12** | **4** |
| 改后（本轮 12:48） | `............EF..EEE..FFF....F.FF.FF.............FF.F............` | 64 | **12** | **4** |

⇒ **本改动未新增红**（`E` 数 4→4、`F` 数 12→12 逐字相同）；用例数 51→64 的 **+13** 落在新增的连续 13 个 `.` 上（全绿），不是本改动引入。

★ 归因旁证：`841e3707` 对 `测试中心/公共契约/测试_仓库只读锁.py` 的改动是 `+57 −21`，而本改动只占其中 4 处精确替换 ⇒ 该文件在本提交里被**两个改动合并**（本任务 ＋ 批R 那条线），故「51 → 64」不能用本改动解释。

### 7.5 提交落点（主会话收口实况，本子任务不改 git）

本件写集（§四 三份）已由**主会话**按裁决收口，**不是本子任务提交的**：

| 文件 | 落点 |
|---|---|
| `平台控制面/能力目录/文件租约存储.py` | `841e3707`（批R 收口，含本任务两处注册腿加 `存储目录` 注入口） |
| `测试中心/公共契约/测试_仓库只读锁.py` | 同上（本任务 4 处精确替换；该提交同时带批R 那条线，故合计 `+57 −21`） |
| `开发文档/分析/批Q单据_Qk_只报告判红归零_20260924.md` | 本件正文（§〇–§七）入 `b9eeaea3`；§7.4／§7.5 为收口后补记，仍为未提交态 |

⇒ §四 那句「未 git 提交」指的是**写盘当时**的事实；收口动作由主会话做，本子任务全程未 `git add`／`git commit`。

