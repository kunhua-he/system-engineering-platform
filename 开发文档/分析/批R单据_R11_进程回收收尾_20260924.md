<!-- 机器管理｜类型：分析｜生成器：开发工具.MD文档生成 -->

# 批R·R-11 进程回收收尾（R-6 的 U1–U6）

- 日期：2026-09-24｜开工ID `开工-20260924-112000-a57a`｜任务：批R·R-11 进程回收收尾（U1-U6）
- 收口节点：`公共契约/运行时/进程终止.py`（R-6 已建 4 原语 `信号已发出` / `进程组存活` / `等待进程组退出` / `发进程组信号` ＋ 唯一入口 `结束并留痕`）
- **本批只改调用点；节点零改动、无新增能力面**。
- 口径：`收口前` = 提交 `f48ef31c`（R-6 尚未入库）；`收口后` = 当前工作区。全仓计入（含 `测试中心/`），排除 `工程缓存/`。

## 一、结论速览

| 项 | 收口前 | 收口后 |
|---|---|---|
| U1–U4 自写「终止→宽限→强杀→复查」升级顺序 | 5 处 | **0 处** |
| U5 本地终止 helper（薄委托）def | 15 处 | **0 处** |
| U5 测试按名打桩／直接调用点 | 15 处（10 patch ＋ 5 直接调用） | **0 处** |
| U6 `_终止未登记工作器` | `终止进程组(pid, 强杀)` 单发 | `发进程组信号`（第一发没真发出信号 ⇒ 按组号补发） |
| `结束并留痕(` 调用点（排除节点自身） | 4 | **49** |
| 真实进程回收实证 | — | **6 个调用点全零孤儿** |

处数变化：**薄委托 15 → 0；自写升级顺序 5 → 0；打桩点 15 → 0**。

## 二、U1–U4 · 5 处自写升级顺序（逐条改前／改后）

### U1 `公共契约/句柄体系.py::_回收单个资源`（进程分支）

改前：

```python
进程终止.终止进程组(pid, 信号="终止")
进程终止.等待进程消失(pid, 超时秒=_回收等待秒)
if self._检查进程存活(pid):
    进程终止.终止进程组(pid, 信号="强杀")
仍存活 = self._检查进程存活(pid)          # 复查口径 = 单进程句柄
```

改后：

```python
已收敛 = 进程终止.结束并留痕(pid, 位置="句柄体系._回收单个资源",
                            宽限秒=_回收等待秒, 等待秒=_回收等待秒)
结论 = 回收结论_已回收 if 已收敛 else 回收结论_未回收
```

- **复查口径（单据要求「先核对再改」）**：改前 `self._检查进程存活(pid)` 只看单进程；改后收口层判**整组**（组长已回收但同组子孙仍在 ⇒ 未收敛），口径更严。
- **测试覆盖核对**：`测试中心/公共契约/测试_句柄资源预算.py`、`测试中心/运行核心/测试_资源句柄网关.py`、`测试中心/运行核心/测试_唤醒重校准.py` 三个文件对 `进程终止` 相关符号（`进程终止|终止进程组|进程存活|结束并留痕|进程组存活`）**全部 0 命中** ⇒ 该分支**无打桩点**，复查口径变化不会打穿测试。
- 第二分支（进程已不在）同步改：改前 `终止进程组(pid, 强杀)`（非整数 PID 抛异常被 `except` 吞掉），改后 `结束并留痕(...)`（幂等；非法 PID 由收口层按 `参数错误` 明确拒绝并留痕）。

### U2 `支持库/适配层/浏览器自动化提供者/实现/提供者.py::_关闭命名守护进程`

改前：

```python
进程终止.终止进程组(pid, 信号="终止")
if not 进程终止.等待进程消失(pid, 超时秒=预算秒):
    进程终止.终止进程组(pid, 信号="强杀")
    进程终止.等待进程消失(pid, 超时秒=预算秒)
```

改后：

```python
已收敛 = 进程终止.结束并留痕(pid, 位置="浏览器自动化提供者.关闭命名守护进程",
                            宽限秒=预算秒, 等待秒=预算秒)
if not 已收敛:
    return {"成功": False, "错误码": "资源未收敛",
            "错误说明": f"守护进程 {pid} 仍未收敛（含同组子孙）"}
```

- **复查口径**：改前只看 `进程存活(pid)`（单进程）；守护进程若自建独立进程组，其同组子孙此前漏收 ⇒ 改后整组。
- **测试覆盖核对**：`测试中心/支持库/测试_浏览器自动化支持库.py`、`测试中心/支持库/测试_浏览器自动化Provider.py` 对 `_关闭命名守护进程` 与 `进程终止` **0 命中** ⇒ 无打桩点。

### U3 `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py::_回收看守名下模型进程`

改前：

```python
if 进程终止.进程存活(模型进程号):
    try:
        进程终止.终止进程组(模型进程号, 信号="终止")
        if not 进程终止.等待进程消失(模型进程号, 超时秒=5.0):
            进程终止.终止进程组(模型进程号, 信号="强杀")
            进程终止.等待进程消失(模型进程号, 超时秒=5.0)
    except Exception as 错误:
        降级记录表.append(f"看守名下模型进程 {模型进程号} 回收异常: {错误}")
```

改后：

```python
if 进程终止.进程组存活(模型进程号):
    已收敛 = 进程终止.结束并留痕(模型进程号, 位置="模型连接器.回收看守名下模型进程",
                                宽限秒=5.0, 等待秒=5.0)
    if not 已收敛:
        降级记录表.append(f"看守名下模型进程 {模型进程号} 强杀后仍未收敛"
                          "（见 进程终止.回收失败留痕快照()）")
```

- 前置判据由「单进程存活」升为「**整组存活**」：看守用 `子进程组启动标志()` 拉起模型进程（独立组长），只判组长会漏掉「组长已回收、同组子孙仍在」。
- 裸 `try/except` 去掉：收口层**永不抛异常**，异常已在节点内收成结果。

### U4 `支持库/后端/大语言模型支持库/模型连接器/实现/本地启动准备与守卫.py`（2 处）

> 单据给的路径 `运行核心/加载器/提供者隔离/本地启动准备与守卫.py` 在盘上**不存在**；实际落点是本文件。

**U4a `回收看守残留`**

```python
# 改前
进程终止.终止进程组(模型进程号, 信号="终止")
消失 = 进程终止.等待进程消失(模型进程号, 超时秒=5.0)
if not 消失:
    进程终止.终止进程组(模型进程号, 信号="强杀")
    消失 = 进程终止.等待进程消失(模型进程号, 超时秒=5.0)
if 消失: ...
# 改后
已收敛 = 进程终止.结束并留痕(模型进程号, 位置="本地启动准备与守卫.回收看守残留",
                            宽限秒=5.0, 等待秒=5.0)
if 已收敛: ...
```

文案同步由「强杀后仍在／仍未消失」改为「强杀后仍未收敛」（复查口径改整组，文案不得再说「消失」）。

**U4b `运行本地模型看守` 的 finally 收尾**

```python
# 改前
进程终止.终止进程组(进程.pid, 信号="终止")
if not 进程终止.等待进程消失(进程.pid, 超时秒=5.0):
    进程终止.终止进程组(进程.pid, 信号="强杀")
    进程终止.等待进程消失(进程.pid, 超时秒=5.0)
# 改后
进程终止.结束并留痕(进程, 位置="本地启动准备与守卫.看守收尾", 宽限秒=5.0, 等待秒=5.0)
```

本处**手上有 `Popen` 句柄**，直接传句柄（收口层能拿到真实退出码）。

## 三、U5 · 15 处薄委托删除 × 5 个测试文件打桩改造（成对清单）

### 3.1 删除清单（15 处，全部零本地实现：函数体只有 1 行转调节点）

| # | 文件 :: 已删 helper | 调用点改后形态 | 打桩改造 |
|---|---|---|---|
| 1 | `支持库/适配层/PyMuPDF提供者/实现/提供者.py::_终止进程组` | `执行任务` 的 `终止回调` ＋ `finally` 内联 `结束并留痕` | 有：测试_PyMuPDF提供者.py |
| 2 | `支持库/适配层/Pillow提供者/实现/提供者.py::_终止进程组` | 同上 | 有：测试_Pillow提供者.py |
| 3 | `支持库/适配层/MLXWhisper提供者/实现/提供者.py::_终止进程组` | 同上 | 有：测试_MLXWhisper提供者.py |
| 4 | `支持库/适配层/PDF隔离提供者/实现/隔离提供者.py::_终止进程组` | 同上 | 有：测试_PDF隔离提供者.py |
| 5 | `支持库/适配层/密码签名提供者/实现/提供者管理器.py::_终止进程组` | 同上 | 有：测试_密码签名提供者_生命周期.py |
| 6 | `支持库/适配层/Git提供者/实现/受管执行.py::_终止进程组` | `执行git()` 的 `终止回调` 内联 | 无（无按名打桩） |
| 7 | `支持库/适配层/代码地图提供者/实现/提供者.py::_终止进程组` | `终止回调` 内联 | 无 |
| 8 | `支持库/适配层/reportlab提供者/实现/子进程管理器.py::_终止子进程组` | `终止回调` ＋ `finally` 内联 | 无 |
| 9 | `支持库/适配层/系统探针提供者/实现/系统探针.py::终止探针子进程`（公开名） | `检查系统工具` 的 `终止回调` ＋ `finally` 内联；公开再导出同步删 | 无 |
| 10 | `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py::_终止进程组`（带 `强制` 形参） | `终止回调` 内联 | 无 |
| 11 | `支持库/后端/文档转换支持库/LibreOffice转换/实现/文档转换.py::_终止进程组` | `终止回调` 内联 | 无 |
| 12 | `平台控制面/统一入口/核心.py::_终止进程组`（方法，返回 bool） | 两个调用点内联 `结束并留痕` | 无 |
| 13 | `平台控制面/提供者/资源硬限制.py::_终止进程组`（方法，返回 bool） | `_受限通信` 输出上限分支内联 | 无 |
| 14 | `平台控制面/提供者/进程组管理.py::_强杀整组` | 转 `进程终止.发进程组信号`（两处） | 无 |
| 15 | `支持库/后端/组件规范支持库/实现/支持库模板生成器.py::_终止进程组`（模板串） | 模板不再生成该 helper，改生成 `结束并留痕` 调用 | 无 |

### 3.2 打桩改造清单（5 个测试文件、15 个打桩／调用点，与 3.1 同批做）

| 测试文件 | 改前 | 改后 |
|---|---|---|
| `测试中心/支持库/测试_PyMuPDF提供者.py` | `patch.object(提供者模块, "_终止进程组")` ×2 ＋ `提供者模块._终止进程组(进程)` ×1 | `patch.object(进程终止, "结束并留痕")` ×2 ＋ `进程终止.结束并留痕(...)` ×1 |
| `测试中心/支持库/测试_Pillow提供者.py` | patch ×3 ＋ 直接调用 ×2 | patch ×3 ＋ `结束并留痕` ×2 |
| `测试中心/支持库/测试_PDF隔离提供者.py` | patch ×2 ＋ 直接调用 ×1 | patch ×2 ＋ `结束并留痕` ×1 |
| `测试中心/支持库/测试_MLXWhisper提供者.py` | patch ×1 | patch ×1 |
| `测试中心/支持库/测试_密码签名提供者_生命周期.py` | patch ×2 ＋ 直接调用 ×1 | patch ×2 ＋ `结束并留痕` ×1 |

- 辅助函数签名同步放宽：`def _关闭进程(进程, **_忽略)` —— 打桩点由「只收 1 个位置参」改为「位置参 ＋ 关键字参（位置／宽限秒／等待秒）」。
- 5 个测试文件均新增 `from 公共契约.运行时 import 进程终止`。
- 其余 10 处薄委托**无按名打桩**（改后 `rg '_终止进程组' 测试中心/` = 0 命中），故无连带改造。
- **不同批删除就会打穿这 5 个文件**（旧打桩按名 patch 一个已不存在的属性 ⇒ `AttributeError`），故删除与改造必须同批；本批已同批完成。

### 3.3 不留旧名别名（公开面同步删）

- `支持库/适配层/系统探针.py`：删 `终止探针子进程` 的 import 与 `__all__` 项。
- `支持库/适配层/系统探针提供者/__init__.py`：同上。
- 改后全仓 `rg '_终止进程组|_终止子进程组|终止探针子进程|_强杀整组'` 仅余 **2 处解释串**（`开发工具/依赖生命周期审计/审计核心.py` 的判据说明、`支持库/后端/文档转换支持库/textutil转换/实现/文本转换.py` 的模块 docstring），**零代码引用**。

## 四、U6 · `运行核心/任务调度/任务进程.py::_终止未登记工作器`

```python
# 改前
if not 进程终止.终止进程组(进程.pid, 信号=信号_强杀).成功:
# 改后
if not 进程终止.信号已发出(进程终止.发进程组信号(进程.pid, 信号=信号_强杀)):
```

- 判据由 `结果.成功`（`进程不存在` 也算失败）改为 `信号已发出`（`已终止` / `已终止（回退）` 为真）。
- 语义收益：`进程.start()` 抛错时组长可能已被 `Popen` 回收，此时 `终止进程组(pid)` 只能回「进程不存在」而**组内子孙仍在**；`发进程组信号` 第一发没真发出信号时**按组号补发一次**，正好覆盖该场景。句柄兜底保留。
- 该函数**不是薄委托**（有本地兜底逻辑），故不计入 15 处删除；helper 总数 16 → 1 就是它。

## 五、回归读数

命令在 `/private/tmp/对照_HEAD`（`f48ef31c` 干净检出）与仓库根各跑一遍；排除 `工程缓存/`。

| 判据 | 收口前 | 收口后 |
|---|---|---|
| 行首 `def (_终止进程组|_终止子进程组|终止探针子进程|_强杀整组)` | **15** | **0** |
| 同上 ＋ `_终止未登记工作器` | 16 | 1 |
| 生产腿 `信号="强杀"`（排除节点／`测试中心/`／`开发工具/环境自检/`） | **12** | **5** |
| `结束并留痕(` 调用点（排除节点自身） | 4 | **49** |
| 整组原语调用点（`进程组存活|等待进程组退出|发进程组信号|信号已发出`，排除节点） | 33 | 40 |
| 测试 `patch.object(<模块>, "_终止进程组")` | 10 | **0** |
| 测试 `模块._终止进程组(` | 5 | **0** |

**「自写升级顺序 5 → 0」逐条对账**：

| 收口前落点（`信号="强杀"` 所在行） | 收口后 |
|---|---|
| `公共契约/句柄体系.py:341` | 0（U1 转 `结束并留痕`） |
| `支持库/适配层/浏览器自动化提供者/实现/提供者.py:272` | 0（U2） |
| `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:625` | 0（U3） |
| `支持库/后端/大语言模型支持库/模型连接器/实现/本地启动准备与守卫.py:581` | 0（U4a） |
| `支持库/后端/大语言模型支持库/模型连接器/实现/本地启动准备与守卫.py:708` | 0（U4b） |

**收口后仍存的 5 处 `信号="强杀"`**（逐条判据，均非自写升级顺序）：

- `运行核心/任务调度/任务进程.py:432` — 调节点 `发进程组信号`（U6）。
- `平台控制面/提供者/进程组管理.py:116` / `:134` — 调节点 `发进程组信号`（R-6 已接入）。
- `运行核心/加载器/提供者隔离/独立进程_账本.py:239` — 调节点 `按组号终止`。
- `公共契约/句柄体系.py:385` — **未做项**，见 §七.1。

## 六、零孤儿实证（真实进程，真 `/bin/sh` ＋ 真 `ps`，全程不打桩）

- 脚本：`/tmp/r11验证/验证_R11_不留孤儿.py`、`/tmp/r11验证/验证_R11_U4第二落点.py`（输出副本 `/tmp/r11验证/输出.txt`、`输出U4b.txt`）。
- 枚举口径：`ps -A -o pid=,pgid=,command=` 按 PGID 过滤；每组都用 `子进程组启动标志()`（`start_new_session`）真起独立组长 ＋ 同组子孙。

| 场景 | 调用点 | 回收前组内成员 | 调用点读数 | 回收后组内成员 |
|---|---|---|---|---|
| 一 | U1 `句柄体系._回收单个资源` | 2 | `已回收=True 结论='已回收' 说明='已终止进程（含进程组）'` | **0** |
| 一·幂等 | 同上（进程已回收） | — | `已回收=True 结论='已回收' 说明='进程已不存在（无泄露）'` | — |
| 二 | U2 `浏览器自动化提供者._关闭命名守护进程` | 2 | `{'成功': True, '值': {'状态': '守护进程已回收', 'PID': 71991}}` | **0** |
| 三 | U3 `模型连接器._回收看守名下模型进程` | 3 | 账本状态 `'已回收'`；`降级记录表=deque([], maxlen=1000)` | **0** |
| 四 | U4a `回收看守残留` | 3 | `{'已回收': True, '原因': '归属与世代校验通过，已回收模型进程 72005（端口 59098）'}` | **0** |
| 四·归属不符 | 同上（命令行不含本代端口） | 3 | `{'已回收': False, '原因': '进程 72013 命令行不含本代端口 59096（归属不符），不动手'}`；复查 `进程组存活=True`（**该动手才动手**） | — |
| 五 | U6 `任务进程池._终止未登记工作器` | 2（组长已回收，只剩同组子孙） | 见下 | **0** |
| 六 | 节点面 `结束并留痕`（`trap '' TERM`） | 2 | 第一发被忽略 ⇒ 升级强杀，`确认收敛=True`；`回收失败留痕快照=[]` | **0** |
| 七 | U4b `运行本地模型看守` 的 `finally` | 3 | 看守退出码 `3`（健康检查未通过）⇒ finally 回收；账本状态 `'已回收'` | **0** |

**关键原始读数**

- 场景五（U6 靶心语义）：

```
组长pid=72019 组号=72019 起组后组内成员=[python 验证_R11_不留孤儿.py, 72021 sleep 300, 72023 sleep 300]
组长已回收 句柄复核 进程存活=False（应 False）
整组口径 进程组存活=True（应 True：同组子孙仍在）
动手前 组内成员（组长已不在，只剩同组子孙）=['72021 72019 sleep 300', '72023 72019 sleep 300']
回收后 进程组存活=False 组内成员=[]
```

⇒ 改前那版 `终止进程组(pid)` 在这个态下只会回「进程不存在」，**两个子孙会留下**；改后按组号补发，组内归零。

- 场景二：`ps -p 71991 -o command=` 真读 `'/bin/sh -c sleep 300; true browser_harness.daemon'`（归属校验真通过后才动手）。
- 场景六：`trap '' TERM` 组第一发无效，`结束并留痕` 自动升级强杀并**确认收敛**；**留痕快照为空**（只有失败才留痕）。
- 场景七：看守日志尾 `['健康检查未通过，收工（启动日志即失败原因）', '收尾：终止模型进程 72668', '看守退出；模型进程仍存活=False']`。

**总判：6 个调用点回收前 2/2/3/3/2/3 → 回收后 0/0/0/0/0/0，零孤儿。**
跑完后全机 `ps -A -o pid=,pgid=,command= | grep 'sleep 300'` 为空；证据期写入的看守账本与日志已清理。

## 七、租约与未做项

### 7.1 未做项（逐条判据）

1. **`公共契约/句柄体系.py:385`（端口分支）保留自持「强杀 → 等消失 → 复查端口」**。
   - 判据：它**不是**「终止→宽限→强杀→复查」升级顺序 —— 无宽限阶段，语义是「**端口占用者**清理 ＋ 端口复查」（`lsof` 枚举 → 越权跳过 → 归属内强杀 → 复查端口是否真释放），与 U1 的**进程**回收是两件事；且不在单据给定的 5 个落点内。
   - 按「不改写集外文件」保留并登记；本批 `git diff` 该段 0 命中。

2. **Windows 分支未真机复核**：节点自述「Windows 分支未经真机实测（开发机 macOS）」，本批沿用该边界，未新增平台判断（收口后 5 处 `信号="强杀"` 全在节点原语调用上）。

3. **未跑编译、未 git 提交**（本单据纪律）⇒ 本批**没有**「静态编译／动态编译」读数，发布门禁结论不因本批变化。

4. **租约缺口（已闭合）**：本任务前序（11:20:08）认领的 41 条租约**未含** `支持库/后端/组件规范支持库/实现/支持库模板生成器.py`（其模板串里的 `_终止进程组` 本批已删）。重认领时已把它纳入，本次 42 条覆盖**完整写集**。

5. **存量文档漂移 3 条（不在本批写集）**：
   - `支持库/后端/文档转换支持库/textutil转换/生命周期契约.json` 的「释放策略／停止入口」仍写 `_终止进程组()`；
   - `支持库/适配层/textutil提供者/生命周期契约.json` 同上；
   - `支持库/后端/文档转换支持库/textutil转换/实现/文本转换.py:4` 的模块 docstring 仍提「一处内部薄委托（`_终止进程组`）」。
   三条均为**更早批次**删 helper 时漏改，本批未动。

6. **textutil 适配层腿的 `终止回调` 直接调 `强制结束子进程` 而未走 `结束并留痕`**（`支持库/适配层/textutil提供者/实现/文本转换.py:95`），返回值被丢弃。按节点口径「失败仍会登记进同一份留痕」，**不构成静默**，属调用口径未统一，登记为下一批候选。

### 7.2 本批租约（42 条，全部活跃，过期 2026-09-24 12:02:43）

开工ID `开工-20260924-112000-a57a`。租约id清单（收工释放用，与 §八 逐条对应）：

`23a928be135f48f6`（句柄体系）`7d263d6bbdc64a07`（浏览器自动化提供者）`07ad141591fd43ad`（模型连接器）`768b883ea5f14ba1`（本地启动准备与守卫）`7d285911826144cc`（任务进程）`d826ac68c12b46bf`（资源硬限制）`ac2accd8b6f74981`（进程组管理）`7da51fcc407745b8`（统一入口/核心）`9991001ff37e4605`（LibreOffice转换/文档转换）`1304413f6c494466`（进程管理）`9b7cb14d68e443e5`（支持库模板生成器）`bc7b0f0ffd444b48`（Git提供者/受管执行）`92b5a27e84004257`（MLXWhisper提供者）`08c7e9fdebcf43b1`（PDF隔离提供者）`68de2bc211544ab6`（Pillow提供者）`041f388cac4f4fc8`（PyMuPDF提供者）`51abcc1f36b24f88`（reportlab/子进程管理器）`a8514150883c4fce`（代码地图提供者）`ab8ac70cefac4ad1`（密码签名提供者）`14869c4e92194ef4`（系统探针提供者/实现）`266c1d14357d4572`（适配层/系统探针.py）`44c4e5ab82f743dd`（系统探针提供者/__init__）`6d4a9cfe3cd342b2`（测试_PyMuPDF提供者）`e6aff0b90b254e13`（测试_Pillow提供者）`266a546a36024f63`（测试_MLXWhisper提供者）`704ced1c41734db9`（测试_PDF隔离提供者）`355dffac7653452e`（测试_密码签名提供者_生命周期）`41562012bde949b5``6b1b9ea1539143c5``ba07a1aa9c0448fe``451d7de0004342e5``5efe009989394ec2``84fb669937444657``132f92a0eee7497e``ac9210bbd3da4a24``4d3a0a8050c7419d``d68fef3096e44e81``c986d9c8c29748af``5a1f8529af2e4714``02238efa89ff4d35``88be04fe1492414a`（14 份生命周期契约.json）`4505f583027145d7`（本单据）

**租约接续实录（本单据落盘的合规链）**：

1. 本任务前序（同一上下文）在 `11:20:08` 认领 41 条；单据落盘前先调 `开工编排.开工即占`（42 条写集）⇒ 返回 `文件已被占用`，占用者即**本任务前序** `开工-20260924-112000-a57a`（`任务` 文本逐字一致）。按纪律「任一条被占即整体失败 ⇒ 不要抢」，**未强认领**。
2. 随后 `11:43:45` 那 41 条被**自动回收**（原因：`心跳停摆超过 900.0 秒未续租`，见 `工程缓存/平台控制面/文件租约.json` 的 `释放原因`）⇒ 写窗口关闭 ⇒ 首次 `文件系统支持库.文件操作.写入文件` 被 `越界` 拒绝（判据 = `公共契约/运行时/写入授权.校验写入授权` 的第二判据「受管路径必须被一条活跃写租约覆盖」）。
3. 重新 `开工编排.开工即占`（42 条，含补上此前漏认领的 `支持库/后端/组件规范支持库/实现/支持库模板生成器.py`）⇒ `已开工=真`，本次租约过期 `12:02:43`，随即落盘本单据。

## 八、本批改动文件

**代码 22**：`公共契约/句柄体系.py`、`支持库/适配层/浏览器自动化提供者/实现/提供者.py`、`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py`、`支持库/后端/大语言模型支持库/模型连接器/实现/本地启动准备与守卫.py`、`运行核心/任务调度/任务进程.py`、`平台控制面/提供者/资源硬限制.py`、`平台控制面/提供者/进程组管理.py`、`平台控制面/统一入口/核心.py`、`支持库/后端/文档转换支持库/LibreOffice转换/实现/文档转换.py`、`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py`、`支持库/后端/组件规范支持库/实现/支持库模板生成器.py`、`支持库/适配层/Git提供者/实现/受管执行.py`、`支持库/适配层/MLXWhisper提供者/实现/提供者.py`、`支持库/适配层/PDF隔离提供者/实现/隔离提供者.py`、`支持库/适配层/Pillow提供者/实现/提供者.py`、`支持库/适配层/PyMuPDF提供者/实现/提供者.py`、`支持库/适配层/reportlab提供者/实现/子进程管理器.py`、`支持库/适配层/代码地图提供者/实现/提供者.py`、`支持库/适配层/密码签名提供者/实现/提供者管理器.py`、`支持库/适配层/系统探针提供者/实现/系统探针.py`、`支持库/适配层/系统探针.py`、`支持库/适配层/系统探针提供者/__init__.py`

**测试 5**：`测试中心/支持库/测试_PyMuPDF提供者.py`、`测试中心/支持库/测试_Pillow提供者.py`、`测试中心/支持库/测试_MLXWhisper提供者.py`、`测试中心/支持库/测试_PDF隔离提供者.py`、`测试中心/支持库/测试_密码签名提供者_生命周期.py`

**契约 14**：`支持库/后端/文档转换支持库/LibreOffice转换/生命周期契约.json`、`支持库/后端/文档转换支持库/PDF生成/生命周期契约.json`、`支持库/后端/文档转换支持库/PDF隔离提供者/生命周期契约.json`、`支持库/后端/版本控制支持库/Git操作/生命周期契约.json`、`支持库/后端/转写支持库/转写/生命周期契约.json`、`支持库/适配层/Git提供者/生命周期契约.json`、`支持库/适配层/MLXWhisper提供者/生命周期契约.json`、`支持库/适配层/PDF隔离提供者/生命周期契约.json`、`支持库/适配层/Pillow提供者/生命周期契约.json`、`支持库/适配层/PyMuPDF提供者/生命周期契约.json`、`支持库/适配层/reportlab提供者/生命周期契约.json`、`支持库/适配层/代码地图提供者/生命周期契约.json`、`支持库/适配层/密码签名提供者/生命周期契约.json`、`支持库/适配层/系统探针提供者/生命周期契约.json`（把「停止入口／释放策略」里已删的 helper 名改为「调 `公共契约.运行时.进程终止.结束并留痕`」）

**单据 1**：`开发文档/分析/批R单据_R11_进程回收收尾_20260924.md`（本文件）

**未改节点**：`公共契约/运行时/进程终止.py` 本批 **0 改动**（R-6 已建 4 原语 ＋ `结束并留痕`）。
