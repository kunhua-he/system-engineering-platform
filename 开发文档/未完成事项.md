# 系统工程平台 · 未完成事项（唯一债务清单）

> 最后更新：2026-09-19 夜（主会话逐条现场重核；**已闭合条目按口径直接删除，不留流水账**）
> 口径：实测（数字随建包与提交逐日变化，**引用前必须重跑取证命令**）
> 维护者：唯一债务清单
> **编号不复用**：编号只增不重用，历史条目关闭即删。
>
> **当前真实账目（2026-09-19 夜实测）**：
> - 一、待修：**9**
> - 二、待执行的大项：**4**
> - 三、待裁决：**0**
> - 四、新增债务：**1**
> - 五、已知覆盖缺口（**不是缺陷，是口径提示**）：**4**
> - **∴ 真正待处置 = 9 + 4 + 1 = 14 条**；覆盖缺口 4 条不计入债务。

> 本文件是当前唯一未完成事项入口。
> 事实源：`开发文档/项目说明.md`；执行顺序：`开发文档/方案/执行编排_唯一入口_20260918.md`；
> 多会话协调：`开发文档/规范/多会话并行开发规约.md`。
> 历轮取证与历史已归档：`开发文档/归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`
> （**不作为派活依据**，历史由 Git 与归档保留）。

---

## 一、待修（修法已明确，可直接派活）

| # | 事项 | 规模 | 落点 / 判据 |
|---|---|---|---|
| 23 | **说明书存量治理施工表**（剩 **G3/G4/G5/G9** 四格） | 中 | 已完成 G1/G2/G6/G8/G10/G11/G12（白名单 41 条、6 个聚合父包重生成可复现 6/6、联动门禁已接线）。**剩四格是「行级合并」——不可自动化，需华哥逐行确认**（保手写行、刷生成行）。禁止对含实质漂移的包裸重生成 |
| 34 | **3 条激活指针能力立项** ⏸ 本轮实做后撤回，**已定清落点** | 小 | 规格齐备：`方案/客户端构建能力化落地方案_20260917.md`（三条能力的参数/返回/错误码）。<br>**卡点（已定位，非推断）**：验证器硬约束「正式公开能力全集 == 正向目标步骤能力全集」⇒ `重建` 必须有正向成功场景；但 `重建` 的成功前提是**存在已安装副本**，场景只能用受管临时目录。两条实测事实：① `安装制品` 的 `目标目录` 含**包层**，而 `重建` 读 `<环境目录>/平台客户端` —— **路径语义差一层**；② `安装制品` 要求制品**已在状态库入库**（只放磁盘目录报 `制品不存在`）。<br>当前：`诊断`/`重置` 已有正向场景并通过验证器；`重建` 缺正向目标步骤。**下一步需先裁定**「`安装制品.目标目录` 与 激活指针 `环境目录` 是否应同一口径」 |
| 36 | **构建链调用面未收口** | 小-中 | `客户端/构建平台客户端.py` 全部**直调 `接入.X()` 实现方法**而非能力 id（★行号随改动漂移，引用前用 `grep -n '接入\.' 客户端/构建平台客户端.py` 现场取）。含 4 条重叠结论：`构建制品`／`查询制品布局` **真重复 → 勿再开同名能力**；`校验可重复构建` **真缺口**；`安装制品` 语义重复待裁。并入时同步扩 C-11 覆盖面 |
| 37 | **说明书：B 类 6 混血包的 99 行手写区行级合并债** | 中 | 将来重生成这 6 包**必须先做行级合并**（保手写行、刷生成行）—— 白名单是**文件级、不是段落级**。★ 术语：**生成物类**＝含生成器签名句、**人工类**＝不含；血统判据与禁令见 `规范/新建支持库与模块指南.md` 说明书治理节 |
| 38 | **包扫描事实源分叉** | 中 | 两套扫描器口径仍不同（现场核实：二者均未改）：`运行核心/加载器/包发现/发现器.py:32-56` 按「聚合库名表 + `能力定义.json` 是否存在」跳过子域；`开发工具/项目编译/正式包索引.py:44-105` 按「非生产路径片段 + 非生产字段 + 状态字段」判定。<br>**后果**：运行发现与正式索引的包/能力数长期不一致（曾实测 127 声明 vs 109+18+1）。<br>**验收判据**：两处扫描结果对同一系统根**逐包一致**，或显式声明「唯一扫描事实源、另一处只读它的产物」 |
| 55 | **拆上帝文件（剩 2 个）** | 中 | 现场重算：>1000 行只剩 `开发工具/环境自检.py` **1138 行**、`运行核心/能力调用/超时执行.py` **1062 行**（其中 `class 超时执行器` 独占 454 行）。<br>**方案已落盘**：`方案/剩余两个上帝文件拆分方案_20260919.md`（自然分组 + 建议落点 + 各自风险 + 验收三项）。<br>★ 风险：`环境自检.py` 是**按路径调用**的脚本（规范 19 处）且有 `sys.path` 自举顺序坑（`9121ca55` 已栽过一次）；`超时执行器` 是**被网关真实调用的生产类**，mixin 边界取「账本/快照」类无状态方法最安全 |
| 74 | **含只读属性的目录树删除：其余 20+ 处落点未收口** | 中 | **已收口仅 2 处**（`环境管理器.py`、`远程镜像.py` 经 `清只读后删除树`/`确保可删`）。其余同型 `rmtree`/`shutil.move` **无只读兜底**（落点随改动漂移，派活前现场复核）：`运行核心/运行环境管理器/提供者生命周期.py`、`平台控制面/包仓库/{服务,版本仓库,可复现构建器,制品保留}.py`、`开发工具/{项目编译/项目编译器,工程缓存回收,发布门禁/权威状态门禁}.py`、`客户端/构建平台客户端.py`、`支持库/后端/文件系统支持库/*`、`运行核心/资源协调/服务.py`、`平台控制面/备份恢复/恢复工具.py` 等。<br>**收口方向（已定，未实施）**：统一改走 `远程镜像.清只读后删除树`，或按最小接口在 `公共契约/运行时/平台适配.py` 补 `清只读并确保可删(路径) -> None`（错误原样抛 `OSError`，不返回布尔、不吞），`远程镜像` 改薄委托。<br>**同类真发现**：POSIX 上父目录 `0o555` 时连非只读文件也删不掉 → `确保可删` 会给父目录补 `0o700` |
| 75 | **`计算环境摘要` 的 `提供者id` 参数是潜在脆弱点**（当前无实际触发） | 小 | `环境管理器.py` 把 `提供者id` 写进指纹，而指纹是**环境目录名的唯一依据** ⇒ 同一把锁、同一提供者，换个 id 形态就得到不同摘要（实测目录名口径 `188665eb096c1f2e` ≠ 全限定口径 `8bfeda124c527e63`）。<br>**当前不触发**：全仓生产调用点**全部传 `提供者目录.name`**，`rg` 实测无任何生产调用点传全限定 id。<br>**收口方向（未实施，需先裁定「同一提供者能否有两个受管环境」）**：① 把 `提供者id` 从指纹里**移除**（指纹只该表达「环境构成」，不该含身份）；或 ② 保留但强制内部归一为 `提供者目录.name` |
| 76 | **HTML 黑盒 · 4 项能力缺真实成功目标场景** | 中 | **口径提醒：不是本批引入**，是**制品缺失长期掩盖的存量红**（09-18 起就在 HTML 验证证据里）；制品一恢复，门禁才真正跑到 HTML 矩阵这一步。<br>四个能力：`代码地图提供者.查询项目地图状态`（场景跑在制品副本里，那里永远没有 `.codegraph` → **断言不成立**）／`开工编排.取任务类型规范`（场景读仓库现场 `开发文档/规范/任务类型规范.json`，制品不含 `开发文档/`）／`平台控制面.能力目录.文件租约留言`（场景写**字符串式** `$上一步.租约id清单.0`，平台只认字典式 `{"$动态":"步骤返回"}`）／`直播逐字稿.全自动精校`（裁决模型原用 `gemma-3-1b:latest` —— **1B 小模型做裁决必然失败**；已改 `qwen2.5-14b:latest`）。<br>★ 场景不得依赖仓库现场文件；必须能在**制品副本环境独立跑绿**。<br>★ 裁决模型可用环境变量 `LLM裁决_模型/url/api_key/协议/上下文长度` 代替显式配置（见 `项目说明.md`） |
| 77 | **测试桩违规 293 / 上限 271** | 小-中 | 超出部分**几乎全部来自一个新测试文件**：`测试中心/运行核心/测试_跨平台收口原语.py` **24 条**（由 `728d8b0e` 新建，非本批引入）。构成：规则2「裸桩缺 `autospec=True`」18 条（主要是 `mock.patch.object(sys,'platform',…)`）+ 规则1「patch 生产路径」6 条。<br>改法按门禁自己给的：规则2 补规格或 `autospec=True`；规则1 三选一（不 patch／加 `wraps=`／确属依赖注入式替换则在 `开发工具/测试伪装门禁实现/豁免清单.json` 登记）。**优先改代码，不许改上限、不许滥用豁免** |

### 一.1 派活前必须现场复核（来自一次真实浪费）

> **教训**：按清单原样派出 10 路并行维修，其中一路（原第 44 项「3 条错误码补登记」）**是过时条目**——
> 现场实测三个码早已登记在案，子代理到场无活可干，只能在「到底改没改」上反复推敲，白费一轮。
>
> **判据（此后一律照做）**：从本清单派活前，**先用提交版本核验条目前提是否仍成立**
> （`git show HEAD:<文件>`，不要读工作树——并行维修时工作树是中间态），前提已不成立的条目**当场删掉**；
> 前提成立的，把**复核时点与核验命令**写进任务信再派。清单是「待办线索」不是「事实」，
> 字面照抄派活＝派空任务。

### 一.2 并行维修期禁止 `git add -A`（来自一次真实污染）

> **教训**：主会话用 `git add -A` 提交时，**子代理的反向验证正在跑**，其**刻意写入的变异值**
> 连同正常产出一起被提交进 HEAD。**代价：一次假红被短暂固化成「已验」状态 + 一路子代理中断本业。**
>
> **判据（此后一律照做）**：
> ① **提交一律用精确路径 `git add <文件>…`，禁用 `git add -A` / `git add .`**（并行维修期尤其）；
> ② 提交前 `git status --short` 核对清单 —— **出现不属于本次任务的条目就停手**，先问清来源；
> ③ **反向验证期间变异值就在工作区**，父会话此时提交 = **把假红固化成「已验」**。
>    正确姿势：**等子代理报「已还原」再提交**，或提交前 `git diff --cached` 逐文件确认无
>    `篡改` / `反向样本` / `临时` 字样；
> ④ **共享制品仓库在并行期也会竞态** —— 并行期**构建与黑盒验证应串行**，或各用独立制品目录。

---

## 二、待执行的大项（方案已出，可直接排期）

| # | 大项 | 方案落点 | 规模 |
|---|---|---|---|
| 1 | **D-1 适配层唯一化** | `开发文档/方案/适配层唯一化迁移清单_20260917.md`：真调用点 57 条/17 文件、消费方 14 包/去重 50 文件 | 大 |
| 3 | **E-8 类型体系收口** | `开发文档/方案/类型体系收口盘点_20260917.md`：16 型 6 有实/7 半实/3 零消费；归一表实测 16 张 | 大 |
| 7 | **D-6 同名文件+同名包合并 / D-5 同名跨层归位 / D-14 开发工具根平铺** | 依 1.1「能解决就按唯一链路修到底、解决不了就重构」+ 2.1 + 1.3；属大重构，单独立项排期 | 大 |
| 56 | **§12-E1 40007 从「跑源码树」切到「跑激活制品」** | 实测 plist 确认在跑源码；制品内已有服务入口。⏳ 主会话亲自做，**须等源码树干净 + 门禁通过之后** | 中 |

---

## 三、待裁决

**当前无。**

---

## 四、新增债务

| # | 缺陷 | 现场证据 | 期望修法 |
|---|---|---|---|
| 1 | **网关重启遗留模型孤儿 → wired 显存泄漏**（真缺陷，已在调用方侧兜底） | 实测因果链：网关被 SIGTERM 后它启动的 `llama-server` 变孤儿（PPID=1），llama.cpp「睡眠」只卸权重、**进程仍攥约 6.5GB wired 显存**（`ps` 里 RSS 显示 0，极易漏看）；网关重启后内存中的模型索引随进程消失，下次 `连接LLM` 认为是无实例 → 再起一个，旧孤儿无人认领。<br>取证：`ps -o pid,ppid,rss,command -ax \| grep llama-server`（看 PPID=1）、杀前杀后 `vm_stat` wired 39.2→12.8GB、`sysctl vm.swapusage` 35.4→21.7GB。<br>**实拍**：`PID 1358 PPID=1`（bge-reranker-v2-m3）＝已确认遗留孤儿。 | 网关启动时**按状态机登记的归属回收**上一代遗留的模型子进程。判据必须是「归属+世代」而非端口号（现场 `PID 59365 PPID=1` 是**合法的长驻 embedding 服务**，按端口盲杀会误杀）。<br>**前提更正（实测）**：原方案称「句柄系统已落库」—— 实读 `公共契约/句柄体系.py` 确认**句柄体系是纯内存态**（无任何持久化）⇒「按状态机登记的归属回收」这条路径**当前不可用**。<br>**下一步（需先裁定机制归属）**：新增「模型子进程世代账本」（落盘 PID + 启动时刻 + 归属标识），或给句柄体系加持久化。两者都属**底座机制新增** |

---

## 五、已知覆盖缺口（**不是缺陷，是「还没验到」或「口径提示」**）

> **保留目的是防后人误判，不需要「修」**。

| # | 缺口 | 说明 |
|---|---|---|
| 4 | **环境/运行态依赖类（非缺陷，别去修代码）** | **实测判据**：转写/媒体类在**并发 ≥2** 下会因**多实例同时加载同一份 3GB 模型权重互抢**而失败，报错现象是「统一返回缺字段 `['值','请求id','耗时毫秒']`」（**现象误导：像返回契约缺陷，实为环境并发**）。处置：`_场景资源键` 加键「语音转写模型」令其独占同实例串行。<br>**通用判据**：若顶层红突然从个位数跳到 20+，且失败模式是「统一返回缺字段」或「500」＋连带的「覆盖对账缺少 N 个能力」，**先怀疑自己动了运行态/环境**（清理了 `工程缓存/平台控制面`、`运行数据`、`制品仓库/平台客户端状态`、`平台客户端环境`），**不要按代码缺陷去修** |
| 6 | **旧口径作废：曾报「158 条零场景能力 / 23.8% 未覆盖」** | 实测**零场景 = 0 条**。旧值系算法漏读（只统计「场景内嵌在 验证场景引用.json 里」的情形，漏了 83 份外部场景文件 `验证场景/*.json`）。**再见到 158 / 23.8% 一律按作废处理** |
| 6b | **记忆支持库的验证场景跑在真实生产库上（读类能力值无法定值）** | 该包能力契约里**没有 `存储目录` 参数**，故真跑 `列出记忆` 返回的是**真实生产数据**。<br>① **11 步缺关键值中的 10 步是「读类」** → 返回值取决于真实库当时内容，**确实无法定值**；<br>② **隐患（待裁）**：验证场景读写**真实生产记忆库**。建议给该包能力加可选 `存储目录` 参数（与 `资源管理`/`项目文档` 同款），场景改用受管临时目录；**但这是对外契约面变更（新增参数），须裁决**。<br>**当前不构成缺陷**：验证器全绿，且实测该包能力对真实库只做读/写单条测试数据 |
| 7 | **「能力面 830」口径不可用** | 按 `包声明.json` rglob 累加会因父包重复登记**虚高到 830**；正确口径 = **装配口径**（经 40007 网关 `/健康` 或装配输出）。引用能力总数一律用装配口径，不用 rglob 累加值 |

---

## 六、待主会话亲自做

| # | 事项 | 前置 |
|---|---|---|
| 56 | 40007 从「跑源码树」切到「跑激活制品」（见 §二） | 源码树干净 + 门禁通过 |
| — | §四 模型孤儿机制归属裁定 | 需先定方向 |
