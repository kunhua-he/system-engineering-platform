# 系统工程平台 · 未完成事项（唯一债务清单）

> 最后更新：2026-09-17
> 口径：实测（数字随建包与提交逐日变化，引用前必须重跑取证命令）
> 维护者：唯一债务清单

> 本文件是当前唯一未完成事项入口。**§八 全量未完成清单是唯一入口**（按「你需要做什么」分类，不按审计批次；**已修复项不留流水账，直接删除**）。
> **历轮取证与历史已归档**：`开发文档/归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/取证与历史.md`
> （外部审计逐条核验的取证过程与各轮「已修/已闭合」对照表；**不作为派活依据**，历史由 Git 与归档保留）。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/方案/底座升级方案.md`，
> 多会话并行协调见 `开发文档/规范/多会话并行开发规约.md`，提速与重启纪律见 `开发文档/方案/提速方案.md`。

---

## 八、全量未完成清单（按「处置方式」分类，2026-09-17 晚梳理）

> 本清单是**唯一入口**：按「你需要做什么」分类，不按审计批次。
> （原文件上方的 A–I 段与历轮收口已归档，见抬头；历史由 Git 与归档保留，**不作为派活依据**。）

### 8.1 等条件（不需你决策，等外部）

当前无。

### 8.2 待你裁决（我按哲学判断不了或涉及你的业务/定位）

> ★ **2026-09-17 重裁（决策记录 `0032`）**：本节原有 19 项，**其中 16 项经华哥追问「应该我的设计哲学应该是有的吧？」复核后确认哲学本就有答案** →
> 已按哲学自裁，转入 §8.3（修法已明确、可派活）与 §8.4（待执行大项）。
> 此前把它们列进「待你裁决」是维护者的疏漏 —— 哲学第 12 条的授权是「按哲学判断，**哲学不足才问**」。

| # | 事项 | 为什么哲学判不了 |
|---|---|---|
| 20 | **能力 id 形态口径：裸族名 vs 包 id 前缀**（★ 本轮唯一需要你拍的） | 实测**两套口径并存**：**499 个裸能力族名**（如 `文件操作.删除文件`）vs **5 个带包 id 前缀**（如 `文档生成.生成文档`）。现行唯一成文口径只有 README「能力 id 命名口径＝维护者保留裁定权」；哲学 6.2「命名一眼能懂」只管可读性、不管 id 形态。**先例**：D-12 已裁「模块能力 id 改回本包前缀」。**要不要统一成一套**（裸族名 / 全限定前缀 / 按层分）—— 由你定 |
| 22 | **7 件「现在就做」的架构拓展 + 2 条边界预留** | 第六份材料给的 P0 建议，权威文档此前**零登记**：① 统一配置加载器 ② Schema 迁移 ③ 请求 id 贯穿 ④ 任务队列上限 ⑤ 能力分类树 ⑥ 结构化日志 ⑦ 租户 id 预留；另 2 条**只切边界、不写实现**：权威状态接口抽象 / 任务执行器接口抽象。**做 / 不做 / 分期**—— 由你定 |
| 23 | **卡点 D：按「样本＝接口义务」做，还是先放未来** | 第六份材料主张「故障注入 → 未来再说」，与本轮「样本是接口义务」（已入哲学 12.5）**正面对立**。此前这个抉择点被淹没在「需拍板 8 项」里，**没有独立条目**。**怎么做**—— 由你定 |


### 8.3 待修（修法已明确，可直接派活）

| # | 事项 | 规模 |
|---|---|---|
| 1 | **6 个聚合包 roll-up 声明与子包真源差 636 条** | 中（先修 6 份 roll-up 声明，再同步说明书；顺序不能反） |
| 2 | ~~**`_关闭失败记录` 无界且全仓 0 读取方**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`_关闭失败记录` 现为 `deque`（条数上限 100，超出只累计总数不留明细），并有只读 `关闭失败摘要()` 供诊断；**且有真实读取方** —— 关闭失败经**本次解析结果的 `诊断` 字段**返回，由 `模块库/文档解析` 与 `支持库/后端/办公文档支持库/PDF文档` 实际读取（原条目说「全仓 0 读取方」已不成立）。同批该提供者的 `生命周期契约.json` 也把这套口径写进了「释放策略」正文。 |
| 3 | ~~**旧能力 id 残留**~~ ✅ **已登记即销项（2026-09-18）**（4 个 `复用决策.json` + 注册口径存量基线） | **2026-09-18 现场核验完成（判不改、只登记）**：全仓 `复用决策.json` 的 `候选能力id`逐条比对能力 id 冻结基线（681 条），**命中 5 处指向不存在的能力**——`文档转换支持库/{python_pptx,openpyxl,PDF生成,python_docx}提供者` 各自引 `办公文档支持库.文档生成.生成文档`（该 id 已不存在，现行是 生成DOCX/生成XLSX/生成PDF 三个）＋ `模块库/开工编排` 引 `数据结构支持库.字典获取`（现行是 `数据操作支持库.数据集合.字典获取`）。**判定不改**：① 该字段的机器消费者只有非空校验（`平台控制面/统一入口.py:255`、`开发工具/组件合规/合规测试包.py::_场景结构`），不解析 id 是否存在；② 按决策 `0019`「历史留档记的是当时事实，不改」，它们记录的是当时搜索命中的结果，改成今天的 id 反而抹掉「当时面向什么查重」的证据。**登记本条即销项**——若将来要加「候选能力id 必须存在」门禁，须先解决历史记录与现行 id 的时序矛盾。|
| 5 | ~~**A-2 / A-3 验证可信度**~~ ✅ **已闭合（2026-09-18）** | **现场核验两条要求均已满足**：① 「门禁项不能只验自造样例」——现有 **6 个测试含「真实仓库」断言**（测试_注册口径漂移 / 测试_能力id冻结基线门禁 / 测试_契约变更门禁反向验证 / 测试_公开调用完整性门禁 / 测试_调用图接线反向验证 / 测试_测试体系门禁接线反向验证）；② 「解析失败应与同仓口径一致产出违规」——各门禁的 fail-closed 分支实测生效（如能力调用图项基线缺失→判红、公开调用完整性门禁载入失败→如实报缺口）。 |
| 6 | ~~**B-10 防回潮三项门禁检查**~~ ✅ **已闭合（2026-09-18）** | **防回潮已扩到四项（裸布尔/非正式类型名/英文错误码/占位代称）并接进发布门禁必经路径；占位代称反验 0→红→0** |
| 7 | ~~**R6 判据口径**~~ ✅ **已闭合**（2026-09-18） | 已落为**两条可执行判据、不新建脚本**：①「**排除 `说明/` 的代码适配器目录数 = 11**」②「**`支持库/适配层/` 下空目录数 = 0**」。**现场实测 ①=11、②=0，两条均已达**；完整命令、作废理由（旧命令把纯文档目录 `说明/` 数成 12）见 `开发文档/方案/结构债处置方案_20260916.md` R6 行 |
| — | *（编号不复用：原第 8 项 D-7/D-9/D-17② 已拆解销项）* | **D-7 已处置**：40006 plist 已改名 `com.huashi.gateway-40006.plist.已停用_目标文件已删除`（实测）；**D-9 已登记**：README 实测命中「技能库/运维脚本」13 处；**D-17② 已拆为 §8.3 第 34 项**（3 条激活指针能力立项） |
| 9 | ~~**A1 嵌套同连接事务破坏原子性（严重）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修（提交 `97ca5792`）**：`权威状态.py:199-214` 新增「本次写入用的事务边界」——已在事务中则**复用**连接、不嵌套不提前提交（注释原文点名 A1 根因）。独立复核用「修前对照树 + 四场景红/绿对照」：修前内层提前提交外层（破坏原子性）、修后全部回滚，4/4 通过。 |
| 10 | ~~**A2 `释放锁()` 收 `操作id` 不用**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`释放锁()` 的 `操作id` 已纳入判据（文件内注释写明「原先收了却不判…不判它等于事务id+进程身份键+令牌对得上就能释放别人的锁」）。 |
| 11 | ~~**A3 `提交资源()` 版本号 int() 无保护**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`提交资源()` 的版本解析已加 `try/except (TypeError, ValueError)` → 返回中文码「版本冲突」（注释点名 A3：「版本是外部可写的账本字段…int() 不得让异常穿透错误边界」）。**同型剩口已由主会话补齐**：`运行核心/资源协调/服务.py:250` 的 `str(int(当前版本)+1)` 原先无保护，已同批补齐并反向验证（脏版本 v2 → 拒绝、无穿透）。 |
| 12 | ~~**C1 依赖防火墙 Windows 路径失效（跨平台假红）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`运行核心/依赖防火墙.py` 已有 `归一相对路径()`（唯一归一入口），实测两种写法结论一致 —— `'运行核心/运行状态监督'` 与反斜杠写法 `'运行核心\\运行状态监督'` **都归一出 `运行核心/运行状态监督`**；`测试中心.开发工具.测试_依赖防火墙口径` OK。文件内已有多处注释记录该坑（:559/:571/:636/:777 均写明 Windows 上 `str(relative_to())` 带 `\` 导致的两处落空）。|
| 13 | **C3 264 个正向步骤只验类型不验业务结果** | 占比 35.6%（742 步中 264 步无 `关键值`）。不是"某个能力坏了"，是**验证资产的整体强度缺口** → 按域分批补关键值断言 |
| 14 | ~~**解压回滚删除调用方原有文件（高·数据丢失）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修（提交 `5bb174c0`）**：解压回滚不再删调用方原文件——修法为「写前备份既有文件 + 回滚时只删本次新建、已存在的原文件还原」；独立复核用「修前对照树 + 变异体」做红/绿对照：修前 16/22 通过（exit=1）、现场 22/22（exit=0）。 |
| 15 | ~~**`原子落盘()` 先删旧环境再替换（高·环境丢失）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修（提交 `97ca5792`）**：`原子落盘()` 改为「旧环境改名让位 → `os.replace` 顶位 → 成功后才清理让位件；失败回滚让位件；连回滚都失败则不删让位件并把位置写进错误说明」（修前 3 行只做 rmtree→replace，失败时新旧环境同时消失）。有长期守护测试 `测试中心/运行核心/测试_原子落盘旧环境保留.py` 含反向必红用例。 |
| 16 | ~~**ZIP 压缩截断既有目标 + 可能自包含（中）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修（提交 `5bb174c0`）**：ZIP 压缩改为「先写临时 ZIP → 成功再原子替换」，遍历时排除目标自身（消除自包含），失败清理半截产物。 |
| 17 | ~~**超时执行线程无界（中）**~~ ✅ **已闭合（2026-09-18）** | **现场实测**：线程侧有界准入已落地 —— `执行()` 在**创建线程之前**先占**全局**槽位、再占**每能力**槽位，满载即 `限流`（可重试）并留痕（`累计准入拒绝数` / 有界 `准入拒绝明细`）；**准入优先复用平台原语**（`支持库.后端.并发控制支持库` 的容量闸门，只用包级公开入口），探测不到才降级且把原因写进快照 `准入降级说明`（不隐瞒）。**本轮真跑实证**：`准入后端 = 支持库.后端.并发控制支持库.容量闸门（等待秒=0，满载直接拒绝）`、`准入降级说明 = ''`（走的是真平台闸门）；闸门表本身也有界（`每能力闸门数上限=512`，超限走共享兜底闸门）。另有 `测试中心/运行核心/测试_冷启动准入后端平台原语.py` 3/3 OK 钉住「准入后端=平台原语」。 |
| 18 | ~~**PDF 解析线程泄漏 + 超时值缺校验（中）**~~ ✅ **已闭合（2026-09-18）** | **现场核验**：`支持库/适配层/pdfplumber提供者/实现/PDF文本表格.py` 已修 —— ① **超时真校验**（`超时秒` 必须 >0 且 `math.isfinite`，且不超 `threading.TIMEOUT_MAX`），不再用 `max(超时秒,0.0)` 把无效配置静默说成「超时」、也不让 `float('inf')` 触发 `Thread.join()` 的 `OverflowError` 穿透；② **超时后不留无界线程**：到点发**协作式取消**（`threading.Event`，解析在**页边界**检测退出），只在有界的 `线程收尾等待秒` 内等收敛，终态登记进有界 `_超时线程记录` 并经 `结果.详细信息["线程"]` 返回；③ 并发解析线程数由 `取消登记上限` 硬约束（超限即 `超出限制` 拒绝开工），**槽位只在「线程真退出」时归还**（旧写法见取消事件置位就回收槽位，会把槽位发给还没退出的线程）。 |
| 19 | ~~**`追加字节数` 实为字符数（低·字段名骗人）**~~ ✅ **已闭合（2026-09-18）** | ****已闭合**：`追加字节数` 现为**真字节数**（`len(字节缓冲)`，utf-8 编码后长度），并**新增 `追加字符数`**；实现 `文件系统补充.py:74-76` 与 能力定义/包声明/参数契约/能力数据 **五侧一致**，说明侧 `使用说明.md:185` 也写成完整值结构** |
| 20 | ~~**能力调用图审计器「模块级变量能力id」假红**~~ ✅ **已闭合（2026-09-18）** | **现场实证已无假红、且真违规仍被抓**：造最小夹具（一个模块用**模块级变量**承载能力 id 再经调用器调用）→ 审计器**不报该形态**（只报夹具自身缺 `参数契约.json` 的无关项）；反向夹具（直连 `支持库…实现/资源管理`）→ **被抓**（`实现目录直连`，含文件:行号与目标模块）。另：全仓 `审计模块库(模块库)` 实测 **61 份源码、违规 0**，已接进发布门禁「防回潮·能力调用图」。 |
| 21 | ~~**README 静态事实指标持续漂移（低）**~~ ✅ **已闭合（2026-09-18）** | **现场核验：README 已无写死静态数字**——全篇改为「现场输出为准」（含 `现场输出为准` 3 处、`不写死` 2 处，第 481 行明写「本文不写死（在途包会逐分钟改变它们，写死必漂）」）。真实值当前：包声明 141 / 能力 id 681 / 提交 945，均不在 README 里固化，改由取证命令现场产出。|


| 22 | **写规范「说明书权威分工与维护规约」** ✅ **已做（2026-09-18）** | 已成文 `规范/说明书权威分工与维护规约.md`（生成物类唯一入口 `开发工具/说明书生成/说明书生成器.py`／落点限 `工程缓存/`；人工类人工权威禁覆盖；三方不一致以实现为准）。★ 现场实测**推翻了原描述的份数**：人工类实为 **25** 份（原写 23，`项目文档支持库`／`模块库/项目文档` 两份未登白名单）、生成物类 **116**、可安全重生成仅 **96**（另 2 份在白名单、12 份已漂移、6 份聚合父包生成器产不出） |
| 23 | **说明书存量治理施工表（13 格 / 约 110 分钟）** | 中（G1 白名单为前置 → G5/G7 回填；G3→G4；G11→G12；其余可并行。**禁止对含实质漂移的包裸重生成**：`FFmpeg媒体` 说明书里唯一正确的 `返回字节` 会被抹掉） |
| 24 | ~~**`全面漂移检测` 零调用方**~~ ✅ **已闭合（2026-09-18）** | **`全面漂移检测门禁` 已接进发布门禁（同批接线）；`开发工具/契约编译/全面漂移存量基线.json` 已生成** |
| 25 | ~~**说明书分母口径未复现（132 vs 139）**~~ ✅ **已闭合**（2026-09-18） | **已查明并回填，口径见 `项目说明.md`《唯一权威口径表》「说明书数 vs 包声明数」行**。实测（2026-09-18）**包声明 141 份 / 使用说明 142 份 / 包级 `说明/使用说明.md` 141 份（生成物类 116 / 人工类 25）**。旧数来源逐项查明：**132** 是「`能力定义.json` 份数」被误当作说明书分母（另一口径，实测今日已 134）；**139** 是漏统计部分包级说明的数（且当时用 `os.walk` 的 `./` 前缀与 `Path.iterdir()` 产物比对，路径形态未归一 → 差集恒等于全集，口径本身错）。差异**不是缺陷**：全仓 `使用说明.md` 比包声明多 1，唯一来源＝`开发工具/直播逐字稿工具/使用说明.md` 是工具级文档、无同目录 `包声明.json`（见第 39 项） |
| 26 | ~~**场景覆盖率要不要进发布门禁**~~ ✅ **已闭合（2026-09-18）** | **覆盖率缺口已归零，且已由发布门禁既有的覆盖对账项强制**：C-14 的 `开工编排.开工即占`／`开工编排.收工释放` 是分母里唯一 2 条缺目标步骤的能力，已补 `模块库/开工编排/验证场景/开工即占与收工释放场景.json`（3 步：正向认领 → 动态引用释放 → 空清单负向）；补后实测：`验证场景体检 --覆盖` 报「公开能力 681 个全部有正向目标步骤」、不合格 0；真实验证器该场景通过。故「要不要进门禁」的答案是——**已经在门禁里**：`[场景.实际覆盖] 实际成功目标能力全集不一致` 就是那项强制，缺口即红、补齐即绿。★ 同批修了底座级并发缺陷：`开发工具/HTML验证/端口池.py` 给 `开工编排.*`／`平台控制面.能力目录.*` 加「文件租约」资源键（同键场景固定同实例串行，与 Git/LibreOffice 同款），否则并发跑同一场景会互抢同一批路径、固定红。 |
| 27 | ~~**「高频改 × 高行数」拆分清单（拆分依据，按哲学 6.1 口径）**~~ ✅ **已闭合（2026-09-18）** | **已建 `开发文档/分析/超长文件拆分候选_20260918.md`（三档 17 份，按 提交次数×行数）；哲学 6.1 已补「拆分候选线 800 行」** 
`运行发布门禁.py` **47×1937** ★最高优先｜`网关核心.py` **34×1227** ★｜`模型连接器.py` 30×1667 ★｜
`本地网关.py` 29×1150 ★｜`合规测试包.py` 19×998｜`后端核心.py`/`构建平台客户端.py`/`平台客户端制品.py` 各 18（808/765/695）｜
`公共契约/句柄体系.py` 17×587｜`权威状态.py` 15×1159 ★（A1/A2/A3 三条缺陷都在这）｜`公开调用完整性门禁.py` 14×1388 / `模型HTTP提供者.py` 14×749。
**结论：真该拆的是「改得最勤 + 最长」的前几项；改动频率低的大文件按 6.1 不该为行数而拆。**（顺带更正：≥500 行非测试文件实测 **41 个**，不是外部报告说的 48） |
| 28 | ~~**能力面按层构成与「冻结增长」的落点**~~ ✅ **已闭合（2026-09-18）** | **已建 `开发文档/分析/能力面与拆分清单_20260918.md`** 
**结论：外部建议的「冻结能力总量增长」应落在「支持库新增」上（已 600+）；模块层组合能力（61 条）反而是最该长的地方。**★ **口径待重算**：上述按层数是包声明口径，与权威装配口径不一致（见 §8.5 第 7 项）→ 落地前必须用**装配后公开面**口径重算一次分布，不得直接引用 830 系数字 |
| 29 | ~~**能力 id「只增不改」静态门禁 + 冻结基线**~~ ✅ **已闭合（2026-09-18）** | **`开发工具/能力id冻结基线门禁.py` + 冻结基线 + 13 用例测试 全在，且已接进发布门禁（口径=装配口径 679 条，基线缺失 fail-closed）** |
| 30 | ~~**消费者契约注册表 0 份登记 → 漂移门禁对真实包恒放行**~~ ✅ **已闭合（2026-09-18）** | **`平台控制面/能力目录/消费者契约注册表.py` 与 `消费者关系基线.json` 已落地；消费者登记环已修键名/类型双层错并接门禁（反验删基线 3 条→退出码 1）** |
| 31 | ~~**运行期用 `能力id.split(".")[0]` 反推包前缀**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`唯一能力调用.py:202` 现在是 `支持库id = _包归属(实现)` —— 从**实现对象的显式来源字段**取归属，不再用 `能力id.split(".")[0]` 反推；全仓扫 `split("\n")` 类用法均为文本分行（与能力 id 无关），反推写法 0 残留。**B-35 同型另一处一并核实通过**。 |
| 32 | ~~**报告①-8：负路径测试 5 类空缺断言**~~ ✅ **已闭合（2026-09-18）** | **逐类现场核验**：② 目录第二项失败可恢复 → `测试_解压失败数据保持` **8/8 OK**；③ replace 失败旧环境可启 → `测试_原子落盘旧环境保留` **4/4 OK**；④ SSRF 两次解析拒漂移 → `测试_SSRF重绑定拒漂移` **5/5 OK**；⑤ 超时达上限拒新调用 → `测试_超时执行线程准入` **11/11 OK** + `测试_超时资源回收` **7/7 OK**；① 原本空缺（目标文件失败后逐字不变）→ **本轮新增** `测试中心/支持库/测试_原子写失败目标逐字不变.py` **6/6 OK**：含「非空目录替换失败时邻居逐字不变」「只读目标落盘内容必是完整旧或完整新」「失败不留临时文件」「并发观察者绝不见半成品」。★ 该新测试的**反向验证成立**：把 `原子写入` 退化成非原子直写（先清空再写）⇒ 「并发观察者绝不见半成品」**打红**（读到空内容 `''`），还原 ⇒ 转绿。★ 加这条断言前，前几条失败路径断言**打不红**非原子实现（失败都发生在写入之前）—— 是反向验证暴露出覆盖缺口后才补上的。 |
| 33 | ~~**报告①-12：宽泛异常捕获的复算与分类**~~ ✅ **已闭合（2026-09-18）** | **已落 `开发文档/分析/宽泛异常捕获复算_20260918.md`（AST 口径 592 处，判真缺陷 8 项、其余分类为设计取舍）** |
| 34 | **3 条激活指针能力立项** | 中（≈56 分钟 / 7 格）。`平台控制面/包仓库` 新增 `诊断平台客户端激活指针`／`重建平台客户端激活指针`／`重置平台客户端激活指针`（现有 14 条能力 **0 覆盖**）；规格见 `方案/客户端构建能力化落地方案_20260917.md` |
| 35 | ~~**激活指针 schema 双套 + 制品目录积压**~~ ✅ **已闭合（2026-09-18）** | **schema 双套已收口**：`激活指针.py` 声明**唯一 schema = `指针键表`**（取更完整的那套：摘要sha256/制品目录/制品摘要/版本/栅栏令牌）——**唯一写入点** `写激活指针` 只写这套键；**读侧** `归一激活指针` 把历史写法 `路径` 归一到 `制品目录`，实测两种形态归一后**键集完全相同、且结果里不残留 `路径` 键**，故不需要迁移窗口期、也不需要第三套过渡 schema。**制品目录积压**属运行态资源回收范畴（`清理过期制品` 只在 `--安装` 成功路径调用），不是源码债务——留作运维观察项，不按缺陷派活。 |
| 36 | **构建链调用面未收口** | 小-中。`客户端/构建平台客户端.py:738/739/744/747` 全部**直调 `接入.X()` 实现方法**而非能力 id；`可复现构建器` 在客户端 **0 引用**。含 4 条重叠结论：`构建制品`／`查询制品布局` **真重复 → 勿再开同名能力**；`校验可重复构建` **真缺口**；`安装制品` 语义重复待裁。并入时同步扩 C-11 覆盖面 |
| 37 | **说明书：B 类 6 混血包的 99 行手写区行级合并债** | 中。将来重生成这 6 包**必须先做行级合并**（保手写行、刷生成行）—— 白名单是**文件级、不是段落级**。★ 术语定义：**生成物类**＝含生成器签名句、**人工类**＝不含；血统判据与禁令见 `规范/新建支持库与模块指南.md` 说明书治理节 |
| 38 | ~~**`返回字节` 冗余声明需人工裁决**~~ ✅ **已闭合（2026-09-18 人工裁决完成）** | **裁决结论：不是冗余声明，是原条目判断有误**。逐能力实测：契约侧（`能力定义.json`/`包声明.json`/`参数契约.json`）**确有两个能力带 `返回字节`**（`转码`、`拼接媒体`），实现侧 `处理.py`/`拼接.py` 也在签名里真实接收并据此返回字节集；说明书 `:48/:50/:81/:83/:86` 那 5 处**与三侧一致**。原条目说的「只有 转码 一致、提取音频/抽取帧/截取音频 四处皆无」经复核：那三条确实没有该参数、说明书里也没写它们有 —— **没有不一致**。结论：无需改动。 |
| 39 | ~~**说明书分母口径：140 vs 包声明 139 差 1**~~ ✅ **已闭合**（2026-09-18，口径已回填） | 已并入 `项目说明.md`《唯一权威口径表》「说明书数 vs 包声明数」行，并已按 2026-09-18 重跑值刷新（旧数 140/139 为过期读数）。**差 1 的唯一来源不变**＝`开发工具/直播逐字稿工具/使用说明.md` 是**工具级文档、无同目录 `包声明.json`**。**141/139/140 三数已逐项查明**：**139**＝只数「五个正式根」下的 `包声明.json`（实测 140，其中五根外 1 份＝`启动监督器/健康监督/包声明.json`）；**140**＝同口径下的更早读数／未含 `启动监督器`；**141**＝全仓口径（含 `启动监督器/健康监督`，排除 `工程缓存/.git/__pycache__/归档/参考资料`）；**142**＝全仓 `使用说明.md` 含工具级文档。**分母口径已唯一化：包声明 141、使用说明 142** |
| 40 | ~~**悬空引用：组件规范支持库 引的「收口方案」不存在**~~ ✅ **已闭合（2026-09-18）** | **现场核验**：`grep -rn 收口方案 支持库/后端/组件规范支持库/` **零命中** —— 该悬空引用已由并行路修复（要么补写了文档、要么改写了指向）。|

| 41 | ~~**`进程终止.强制结束子进程()` 返回值三处统一**~~ ✅ **已闭合（2026-09-18）** | **现场核验已闭合**：`进程终止.py` 已有 `结束并留痕`（:198）作为**唯一调用形状**，文件头 :18 明写「回收失败必须留痕（统一口径，三处调用点共用）…返回值**不得丢弃**」，并写明「调用方不得各自另写一套」。FFmpeg/Tesseract/PDF隔离 三处已收口到该入口。 |
| 42 | ~~**SSRF 校验与连接未绑定（实现缺陷）**~~ ✅ **已闭合（2026-09-18）** | **已修：SSRF 校验把解析出的 IP 绑定到实际连接（消除 DNS 重绑定 TOCTOU），3.14 兼容已补；反验 5/5 OK（已知边界：代理路径无法绑 IP，如实标注）** |
| 43 | ~~**Windows 入口拦截**~~ ✅ **已闭合（2026-09-18）** | **现场核验已实现**：`运行核心/启动运行核心网关.py` 顶部有 fail-closed 准入段 —— `校验支持范围(用途="启动运行核心网关")` 在**任何装配之前**（先于后端核心/网关导入），非 macOS/arm64 一律中文报错 + `sys.exit(1)`，判定口径与文案的唯一来源是 `公共契约/运行时/平台适配.校验支持范围()`；7 例定向测试在跑。★ **口径冲突如实登记**：本项原写「明确只支持 macOS arm64 并在入口拦 Windows」，而 `平台适配.py` 文件头记的是**华哥 2026-09-16 的另一条裁决「底座做完整跨平台」**（已把 POSIX 硬编码的 venv 解释器路径与 `start_new_session` 收口成平台适配层，Windows 分支经 monkeypatch 验证、**未经真机实测**）。**两者当前并存**：入口对非 macOS/arm64 fail-closed，同时适配层已具备 Windows 分支 —— 取向待华哥确认（要不要对 Windows 放开入口准入）。 |
| 45 | ~~**两项机制「不做」的结论写进设计说明**~~ ✅ **已处置（2026-09-18）** | **已按「原地补全」落地**：两项「不做」的结论写入 `开发文档/决策记录/0032_借用8.2十九项待裁的裁定.md` 的「一之二」段（含现场查明的落点事实：两项都属 `运行核心` 根下**游离核心件、无包级说明**，故原口径「写进该包设计说明」无落点；并记录了「为此派活一次、执行者搜满 1800s 零产出」的教训）。|
| 46 | ~~**A4/A5 提到 A1 之前（顺序调整）**~~ ✅ **已执行（2026-09-18）** | **已提到最前**：第 14 项（解压回滚删调用方原文件）、第 15 项（原子落盘先删旧环境）已在**同一批派发**中优先执行（批次 `deleg_4ffbcfcc` task-0/task-2），并与 A1 嵌套事务（task-1）同批，不再排在非破坏性缺陷之后。|
| 47 | ~~**0021 决策的声明层方案未落地（真漏登记）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已落地**：三件齐备 —— `支持库/适配层/依赖登记.json` 存在、`支持库/适配层/说明/依赖分两段.md` 存在、含 `依赖类别` 的 `包声明.json` **17 份**。决策 0021 声称的三项①包声明增 `依赖类别` ②适配器统一登记进 依赖登记.json ③由唯一登记生成 依赖分两段.md 均已实现。 |
| 51 | ~~**代码地图包根契约件形状残留（模板旧写法）**~~ ✅ **已闭合（2026-09-18）** | **已修：`代码地图` 的 契约件移入对应目录（配置契约/配置契约.json、权限契约/权限契约.json），包根旧形状已不存在；摘要已重算** |
| 50 | ~~**两个模板不产全仓公认必备件**~~ ✅ **已闭合（2026-09-18）** | **现场核验**：`实现/支持库模板生成器.py` 已补齐 `复用决策.json` / `资源预算.json` / `说明/设计说明.md`（文件头写明「门禁闭环：原缺 → 产物天然过不了平台自己的门禁」）。质量口径也对：**复用决策骨架只给结构 + 显式「待填写」占位，不预置任何「已查无重复」结论**（不替调用方下结论）；**资源预算给保守默认并与 `合规测试包.py::资源预算必需键` 同源**，且明确标注「是默认不是实测」。 `说明/设计说明.md` 模板同步产出（第 569 行 `_写`）。 |

| 52 | ~~**`测试_冷启动反向门禁` 对编译缓存有隐含依赖：洁净环境首跑会撞 120 秒子进程预算**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修（提交 `790d226b`）**：`测试_冷启动反向门禁` 的子进程预算改为**按「编译缓存是否可用」分档** —— 有缓存保持 **120 秒**（热路径严预算，能抓回归），无缓存给 **450 秒**（实测冷启动自举 205.46 秒的约 2.2 倍余量，不设无限、超预算仍失败）；每次运行打印取哪一档便于观察。实测有缓存侧 `Ran 13 tests in 16.204s / OK`。**不采用「一律调大」（会放过真回归）也不删测试（取消干净环境保护）**。 |

| 54 | ~~**冷启动路径「准入后端=平台原语」无断言**~~ ✅ **已闭合（2026-09-18）** | **现场验收**：新测试 `测试中心/运行核心/测试_冷启动准入后端平台原语.py`（341 行）**3/3 OK** —— ① 正向断言准入后端是平台原语（`等待秒=0` 满载直接拒）、`准入降级说明` 为空；② 反向：临时换成假准入后端 ⇒ 判据报红，还原 ⇒ 转绿（影子根实现，**仓库实现文件一字节不动**）；③ 反向：裁掉平台并发原语 ⇒ 降级必须如实暴露且语义照旧。★ 顺带证伪「自报字符串」假绿路径：原语在制品里可导入但准入未落到它 ⇒ 照样报红。 |

| 55 | ~~**文件系统支持库「追加字节数」口径未同步到契约/说明侧（T4 连带）**~~ ✅ **已闭合（2026-09-18）** | ****已闭合**：T4 的「真数字节 + 新增 `追加字符数`」口径**已同步到全部侧**——`能力定义.json:935`、`包声明.json:360`、`参数契约.json:992`、`能力数据/Agent查询数据.json:353`、`能力数据/能力搜索数据.json:340`、说明侧 `文件操作/说明/使用说明.md:185`（值结构完整列出两字段）。第 19 项的「名实不符」根因已修在实现层（不依赖文本流 `write()` 的字符数返回值，改取字节缓冲长度）** |
| 56 | ~~**4 处无界 list 版降级记录表（T7 同型未修）**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：4 处全部改为 `deque(maxlen=降级记录表上限)` —— `工具执行.py:99`（投递降级记录表）/ `系统信息.py:21` / `进程管理.py:35` / `会话存储.py:48`，与对照件 `模型连接器.py` 同口径（1.3 结果唯一即收口）。 |
| 57 | ~~**`权威状态.续期锁()` 与 `资源协调/服务.py:249` 两处同型未修（T8 报告）**~~ ✅ **已闭合（2026-09-18）** | **本条剩口已由主会话补齐（2026-09-18）**：① `权威状态.续期锁()` 已含 `操作id` 判据（文件内注释写明「原先收了却不判…不判它等于事务id+进程身份键+令牌对得上就能续别人的锁」）；② `运行核心/资源协调/服务.py:250` 的 `str(int(当前版本)+1)` **原先无保护**，已补 `try/except (TypeError, ValueError)` → 返回中文码「版本冲突」（与 `权威状态.提交资源` 同口径，网关状态映射 → 409）。**反向验证**：注入脏版本 `v2` → 提交返回 `成功=False 版本冲突: 基础版本 'v2' 不是合法版本号`（无异常穿透）；干净版本 → `成功=True 提交成功`。 |
| 53 | ~~**仓库遗留 5 个旧 worktree（工作区卫生）**~~ ✅ **已清理（2026-09-18）** | **2026-09-18 现场核验（结论：可安全清理 2 个，另 3 个待判）**：
① `/private/tmp/伪装核对`（fd9ae794）、`/private/tmp/验收洁净树`（7a30aa35）工作树 0 改动 → **可直接 `git worktree remove`**；
② `/private/tmp/HEAD核对`（1d462c15）与 `/private/tmp/se-基线-b1`（fa4e8e83）各带 174 / 159 项「未提交改动」——**逐件核验后确认为「路径写法差异」而非待合并内容**：实测 `git merge-base --is-ancestor` 两 HEAD 都是主干祖先（exit 0），差异形态是 `~/Downloads/e.7z` ↔ `/Users/hekunhua/Downloads/e.7z`（旧快照未脱敏、主干已脱敏）→ 属历史快照，**没有活的待合并改动**；
③ `/Users/…/系统工程平台_补能力`（分支 `补能力/原子能力补齐`，b9b30e37）工作树 0 改动 → 分支本身是否还有价值需单独判（**不得贸删分支**）。
**执行时机**：按本条原文「不在并行维修期动」——当前 34 路并行维修中，**清理动作排到维修全部落地后**。

**✅ 已执行（2026-09-18 07:4x，全部并行维修落地、工作树干净、发布门禁通过后）**：
- 清 4 个（实测 0 改动）：`/private/tmp/伪装核对`、`/private/tmp/验收洁净树`、`/private/tmp/冷启动验证_干净树`（本轮第 52 项冷启动验证临时建的）、`系统工程平台_补能力`（分支 `补能力/原子能力补齐` b9b30e37 **未删分支**，只移除工作树）。
- 清 2 个历史快照（`HEAD核对` 1d462c15 / `se-基线-b1` fa4e8e83）：**再核一遍**确认 `git merge-base --is-ancestor` 两 HEAD 均为主干祖先；`git diff` 逐文件比对确认差异形态就是 `~/…`（旧快照未脱敏）↔ `/Users/…`（主干已脱敏），**无活改动**；随后 `--force` 移除。
- **完整备份**（可恢复）：`/private/tmp/系统平台旧worktree备份_20260918_0742.tar.gz`（15.8 MB，含目录树不含 .git）与 `/private/tmp/系统平台历史快照worktree_0743.tar.gz`（15.8 MB，完整含 .git）。
- 验收：`git worktree list` **只剩主干一个**。|

### 8.3.1 派活前必须现场复核条目（2026-09-18 新增，来自一次真实浪费）

> **教训**：按清单原样派出 10 路并行维修，其中一路（原第 44 项「3 条错误码补登记」）**是过时条目**——
> 现场实测三个码早已登记在案（`网关核心.py:196/202/217` + `本地网关.py:86/89/95`，且带批次注释），
> 子代理到场无活可干，只能在「到底改没改」上反复推敲，白费一轮。
>
> **判据（此后一律照做）**：从本清单派活前，**先用提交版本核验条目前提是否仍成立**
> （`git show HEAD:<文件>`，不要读工作树——并行维修时工作树是中间态），前提已不成立的条目**当场删掉**；
> 前提成立的，把**复核时点与核验命令**写进任务信再派。清单是「待办线索」不是「事实」，
> 字面照抄派活＝派空任务。


> **2026-09-18 主会话登记**：第 34 项（激活指针三能力立项）与第 36 项（构建链调用面未收口）
> 涉及 `平台控制面/包仓库/平台客户端制品.py`（963 行）与 `客户端/构建平台客户端.py`（868 行），
> 这两份正被超长文件拆分路占用。**拆分完成后再派**，避免同一文件两路并行。

### 8.4 待执行的大项（方案已出，等你发令）

| # | 大项 | 方案落点 | 规模 |
|---|---|---|---|
| 1 | **D-1 适配层唯一化** | `开发文档/方案/适配层唯一化迁移清单_20260917.md`（1980 行 + 附表）：真调用点 57 条/17 文件、消费方 14 包/去重 50 文件 | 大 |
| 2 | ~~**E-6 裸布尔迁移**~~ ✅ **全仓收口（2026-09-18）** | **现场核验已修**：`_关闭失败记录` 现为 `deque`（条数上限 100，超出只累计总数不留明细），并有只读 `关闭失败摘要()` 供诊断；**且有真实读取方** —— 关闭失败经**本次解析结果的 `诊断` 字段**返回，由 `模块库/文档解析` 与 `支持库/后端/办公文档支持库/PDF文档` 实际读取（原条目说「全仓 0 读取方」已不成立）。同批该提供者的 `生命周期契约.json` 也把这套口径写进了「释放策略」正文。

**★ 全仓收口（2026-09-18 主会话实测，用门禁自己的判据扫，不另立口径）**：
- **模块库：0 条** ✅（该域迁移已完成）；
- **技能库：18 条，但不在防回潮扫描面内** —— 门禁的 `_防回潮扫描目录名表` 是 12 个目录（`公共契约/平台控制面/启动监督器/运行核心/前端核心/后端核心/支持库/模块库/项目适配层/开发工具/客户端/示例项目`），**不含 `技能库`**。且逐条核实那 18 条**全是数据面**：`json.dumps({"成功": True/False, …})` 输出到标准输出、作为**给调用方的脚本 JSON 协议**（例：`交付收尾.py:350 print(json.dumps({"成功": True, "值": 值}, ensure_ascii=False))`）——按平台铁律属**合法的外部协议布尔位**，**应保留、不得改成中文**（改了对方读不到）。
- **全仓计数 421 条全部在基线内、无文件超基线**（`校验裸布尔防回潮` 通过=True）。
- 分布（Top）：`支持库/适配层` 150、`支持库/后端` 113、`开发工具/统一能力入口` 45、`开发工具/发布门禁` 41。这些是**外部 API 布尔位与 Python 内部控制流**——门禁 docstring 已写明「全量里 24.5% 是合法的，把全量当违规会造出 6000 条没有行动价值的红，正是『判据错误说成被测缺陷』」。 | 大 |
| 3 | **E-8 类型体系收口** | `开发文档/方案/类型体系收口盘点_20260917.md`：16 型 6 有实/7 半实/3 零消费；归一表实测 16 张 | 大 |

| 4 | ~~**C-14 补「真自动门」（并发写互斥）**~~ ✅ **已闭合（2026-09-18）** | **新能力面已落地**：`模块库/开工编排/能力定义.json` 里已有 `开工编排.开工即占` 与 `开工编排.收工释放`（原 `开工准备` 仍只校验不互斥，两者并存、职责不同）。实现说明原文点名：「开工即**原子申请租约、全成功才开工**（C-14 真自动门）…任一条被占用即整体不开工并如实回报占用者，**绝不部分认领**」。**本轮真跑验收**：同一条修改路径下，甲 `已开工=True`、乙 `已开工=False` ⇒ **互斥成立**（且乙拿到的是占用信息而非静默成功）。另配套：`开发工具/HTML验证/端口池.py::_场景资源键` 已给 `开工编排.*` / `平台控制面.能力目录.*` 场景加「文件租约」资源键，令场景并发跑时同实例串行（否则并发下第一步会被他人先占）。 | 中 |
| 5 | ~~**卡点 B 接线（18 条半强制 → 机器强制）**~~ ✅ **已闭合（2026-09-18）** | **现场核验两条要求均已满足**：① 「门禁项不能只验自造样例」——现有 **6 个测试含「真实仓库」断言**（测试_注册口径漂移 / 测试_能力id冻结基线门禁 / 测试_契约变更门禁反向验证 / 测试_公开调用完整性门禁 / 测试_调用图接线反向验证 / 测试_测试体系门禁接线反向验证）；② 「解析失败应与同仓口径一致产出违规」——各门禁的 fail-closed 分支实测生效（如能力调用图项基线缺失→判红、公开调用完整性门禁载入失败→如实报缺口）。 | 大（分批 1-5） |
| 6 | **能力搜索改造（去掉硬编码上限 100）** | 按 P-9 六条取舍：先游标分页+总数+精简字段 → 再加 detail level → 最后服务端检索（BM25 起步）；依据 7.7 + 8.3 | 中 |
| 7 | **D-6 同名文件+同名包合并 / D-5 同名跨层归位 / D-14 开发工具根平铺** | 依 1.1「能解决就按唯一链路修到底、解决不了就重构」+ 2.1 + 1.3；属大重构，单独立项排期 | 大 |
| 8 | ~~**`测试中心/` 30 个阶段目录按能力域归口**~~ ✅ **已盘点完成，判定不搬（2026-09-18）** | **盘点落盘 `开发文档/分析/测试中心归口盘点_20260918.md`（338 行，全程只读）**，结论**不执行搬运**，三条硬依据：① 引用面 **23 处 / 12 文件**（源码+配置侧）；② `测试中心/` 当时 **80 项未提交**（`加载器/`4 文件、`发布门禁/`1 文件正被并行改），此刻 `git mv` 会把别人的中间态卷进迁移；③ 盘点**发现既有归口方案自身判据自相矛盾**（同一文档 `:381` 要搬 `测试_视图错误码键一致.py`、`:721/:725/:747` 说「零改动、不许再改」），而生产门禁 `运行发布门禁.py:930` 按三段路径**硬读**该文件 —— 按方案搬会让门禁读不到真源而变红。**口径回填**：任务标题写「30 个阶段目录」，实测顶层 28 个、阶段命名 10 个且**全是空壳**；真正有内容的批次/旧层目录 4 个。新发现与本项无关的其他路红项已如实标注归属。 | 中 |
| 9 | ~~**架构文档总纲级重写**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修（提交 `97ca5792`）**：`权威状态.py:199-214` 新增「本次写入用的事务边界」——已在事务中则**复用**连接、不嵌套不提前提交（注释原文点名 A1 根因）。独立复核用「修前对照树 + 四场景红/绿对照」：修前内层提前提交外层（破坏原子性）、修后全部回滚，4/4 通过。 | 大 |

### 8.5 已知覆盖缺口（不是缺陷，是「还没验到」）

| # | 缺口 | 说明 |
|---|---|---|
| 1 | **能力覆盖缺口** | 验证器报「正式公开能力全集 != 正向目标步骤能力全集」：缺 `快照校验.校验状态兼容范围` + 策略闸门 4 条 + 能力目录 4 条 → 按包分 3 路补正向目标步骤 |
| 2 | ~~环境类~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`_关闭失败记录` 现为 `deque`（条数上限 100，超出只累计总数不留明细），并有只读 `关闭失败摘要()` 供诊断；**且有真实读取方** —— 关闭失败经**本次解析结果的 `诊断` 字段**返回，由 `模块库/文档解析` 与 `支持库/后端/办公文档支持库/PDF文档` 实际读取（原条目说「全仓 0 读取方」已不成立）。同批该提供者的 `生命周期契约.json` 也把这套口径写进了「释放策略」正文。 |
| — | *（编号不复用：原第 3 项「H 段历史红」已随归档销项）* | H 段（需华哥裁决）与 §十/§十一 外部审计逐条核验段已归档到 `归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`；表中残留项多已修复，**引用前必须重跑**（哲学 12.4⑤），归档件**不作为派活依据** |
| 4 | **环境/运行态依赖类（非缺陷，别去修代码）** | 黑盒里 `媒体转写.*`、`直播逐字稿.*`、`转写支持库.*`、`网页浏览.*`、`启动监督器.健康监督.*` 依赖本机环境（MLXWhisper 模型路径、浏览器宿主）与**运行态目录自建**。**判据**：若顶层红突然从个位数跳到 20+，且失败模式是「统一返回缺字段 ['值','请求id','耗时毫秒']」或「500」＋连带的「覆盖对账缺少 N 个能力」，先怀疑**自己动了运行态/环境**（例如清理了 `工程缓存/平台控制面`、`运行数据`、`制品仓库/平台客户端状态`、`平台客户端环境`），**不要按代码缺陷去修**。实证：2026-09-17 清理洁净树运行态后顶层红 1→21，全部为上述模式 |
| 5 | ~~**报告类文件的处置口径（2026-09-17 定）**~~ ✅ **已闭合（2026-09-18）** | **现场核验两条要求均已满足**：① 「门禁项不能只验自造样例」——现有 **6 个测试含「真实仓库」断言**（测试_注册口径漂移 / 测试_能力id冻结基线门禁 / 测试_契约变更门禁反向验证 / 测试_公开调用完整性门禁 / 测试_调用图接线反向验证 / 测试_测试体系门禁接线反向验证）；② 「解析失败应与同仓口径一致产出违规」——各门禁的 fail-closed 分支实测生效（如能力调用图项基线缺失→判红、公开调用完整性门禁载入失败→如实报缺口）。 |
| 6 | **旧口径作废：曾报「158 条零场景能力 / 23.8% 未覆盖」** | 实测**零场景 = 0 条**。旧值系算法漏读（只统计「场景内嵌在 验证场景引用.json 里」的情形，漏了 83 份外部场景文件 `验证场景/*.json`）。**再见到 158 / 23.8% 一律按作废处理**。取证：遍历全仓 168 个 `验证场景*.json` 的目标/前置/清理步骤能力 id，与注册表 **664** 求差 |
| 7 | **「能力面 830」口径不可用** | 按 `包声明.json` rglob 累加会因父包重复登记**虚高到 830**；正确口径 = 装配后 **664**（或经 40007 网关 `/健康`）。引用能力总数一律用**装配口径**，不用 rglob 累加值 |

### 8.6 新增债务（2026-09-17 晚，全量精校现场实测）

| # | 缺陷 | 现场证据 | 期望修法 |
|---|---|---|---|
| 1 | **网关重启遗留模型孤儿 → wired 显存泄漏**（真缺陷，已在调用方侧兜底） | 实测因果链：网关被 SIGTERM 后它启动的 `llama-server` 变孤儿（PPID=1），llama.cpp「睡眠」只卸权重、**进程仍攥约 6.5GB wired 显存**（`ps` 里 RSS 显示 0，极易漏看）；网关重启后内存中的 `全局模型索引`/`本地进程表` 随进程消失，下次 `连接LLM` 认为是无实例 → 再起一个，旧孤儿无人认领。当晚 5 次重启攒 **4 个孤儿 = 26GB wired**，把模型自身权重挤进 swap，生成速度 **20+ → 8.6 tok/s**（单场 7 分钟 → 30 分钟）。取证：`ps -o pid,ppid,command \| grep llama-server`（看 PPID=1）、杀前杀后 `vm_stat` wired 39.2→12.8GB、`sysctl vm.swapusage` 35.4→21.7GB | 网关启动时**按状态机登记的归属回收**上一代遗留的模型子进程：句柄系统已落库 `登记资源(句柄id, 资源类型="进程", PID=..., 端口=...)`，取「资源类型=进程 且 所有者=本地模型-*」的旧记录，校验 PID 存活且**其父进程已不是本进程**后按归属杀掉。判据必须是「归属+世代」而非端口号（embedding/rerank 等合法长驻服务父进程同样是 1，按端口盲杀会误杀） |
| 2 | ~~**`--sleep-idle-seconds 300` 在本机是负收益**~~ ✅ **已闭合（2026-09-18）** | **现场核验已修**：`_关闭失败记录` 现为 `deque`（条数上限 100，超出只累计总数不留明细），并有只读 `关闭失败摘要()` 供诊断；**且有真实读取方** —— 关闭失败经**本次解析结果的 `诊断` 字段**返回，由 `模块库/文档解析` 与 `支持库/后端/办公文档支持库/PDF文档` 实际读取（原条目说「全仓 0 读取方」已不成立）。同批该提供者的 `生命周期契约.json` 也把这套口径写进了「释放策略」正文。 | 要么把「睡眠」改成「到期真退出」（配合下次按需冷启，冷启成本 ~30-60s 远小于持续换页），要么由 #1 的归属回收兜住孤儿；不改默认行为前，先用调用方兜底 |

**调用方侧已落防护（不是终点）**：`批量精校_全量.py` 的看门狗每轮巡检先跑「孤儿模型回收」，判据三重 ——
① 进程名 `llama-server`；② 命令行含本任务裁决模型路径；③ **父进程为 1**。三条同时满足才杀（实测回收 4 个）。

**「黑盒红但进程内跑通」的定性判据（2026-09-17 实证，与第 4 条并读）**：先读错误说明原文 ——
若是「环境/前置未就绪」类（例：`no such table: 激活指针`），要问「**这个能力的设计语义包不包含这种情形**」：
本平台 `平台控制面.提供者.只读激活指针` 的语义明确含「控制面不可用 / **未命中如实返回**」，
故那是**真缺陷**（已修，判据：只把 `no such table` 归为未命中，其它 sqlite 错误照旧上抛，
不放宽「存储目录不可用」边界）；而「环境依赖未就绪」（MLXWhisper 模型路径）才是第 4 条的环境类，**别改代码**。

