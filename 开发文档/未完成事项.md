# 系统工程平台 · 未完成事项（唯一债务清单）

> 本文件是当前唯一未完成事项入口。本轮只记录已经用源码、契约和真实命令核实的问题；修复后必须重新验证并立即删除对应条目。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/底座升级方案.md`。

## 当前状态

- 正式索引：当前源码正式包索引以实时扫描为准；浏览器自动化（12 能力）、直播逐字稿（13 能力）均已落地并进入正式索引，能力冲突 0。
- 正式源码语法：受影响 Python 文件已通过 `python3.14 -m py_compile`。
- 当前正式索引实测（2026-09-10）：91 个支持库、14 个模块库、105 个包、498 项能力、能力冲突 0。
- 当前基线：`d973a836d9367349c8a2bb792c45cbfda1cab273`，工作树 0 脏文件，已推送 GitHub 私有仓。
- 验收口径：正式判定为"编译产物 + HTML GET/POST 并发黑盒"（华哥 2026-08-29 裁决）；显式全量 unittest 不再作为常规门槛。本轮证据：40007 装配 498 能力、新能力黑盒 6/6、热接入回归 10 组全绿、声明与注册 0 不一致。
- 依赖生命周期审计：16 个标准第三方 Provider 0 违规；2 个无包声明的非标准内部适配器按规则跳过。
- res 与 chat LLM 均已真实验收：res 使用 `gpt-5.6-sol + codex_responses`；chat 使用 `deepseek-v4-flash`、`hy4`，普通生成和 `/网关/流式` 均 HTTP 200，流式增量与完成事件正常，句柄查询/释放通过。
- 独立复验（2026-09-10，主会话亲测非子代理自述）：四阶段压缩原子分组成对性成立（对照 `原子分组=False` 确会拆对）、降级链成立；结构化任务结果三种终态穷举正确；重试调度与契约一致。

## 阻断事项

（无。当前已完成审计报告涉及的功能失败、测试资源释放和 Provider 生命周期审计收口。）

## 重要事项

- **P2 Whisper 生产精度标杆**：MLX large-v3 链路可用，已建立短音频基准，但业务品牌词仍有误识别；尚无真实业务音频+人工参考稿的长音频逐窗口精校结论。
- **P2 维护性债务（已评估，不属阻断）**：正式源码 129 个文件超 160 行（最大：发布门禁 1445 行、权威状态 936 行、项目服务 828 行）；系统工程平台无 160 行硬规则，按需拆分不凑数字。
- **P1 三个开发工具脚本直接运行方式不可用（2026-09-10 现场复测）**：`开发工具/发布门禁/运行发布门禁.py`、`开发工具/HTML验证/验证器.py`、`开发工具/轻代码前端编辑器/启动编辑器.py` 用 `python3.14 <路径>.py` 直接执行会 `ModuleNotFoundError`（缺项目根 `sys.path` 初始化）；`-m` 模块方式可用。权威文档命令已统一改为 `-m` 形式；脚本层 `sys.path` 初始化待下一阶段修复（修复后两种方式都应可用）。
- **P2 底座侧待修（V3 项目记忆登记的依赖项）**：① `发送请求` 的 3xx 响应未携带 `Location`，调用方无法经唯一入口安全逐跳跟随重定向；② LLM 协议短值 `chat`/`res` 与长值 `chat_completions`/`codex_responses` 尚未在平台侧统一裁决。
- **P2 制品指针语义待确认（对账 R3）**：`工程缓存/制品仓库/` 下并存三份 `当前.json`（`平台客户端制品/`、`平台客户端环境/`、`平台客户端环境-失效-20260822190912/`），其中制品指针指向的版本未登记入权威制品表；需先定权威写入语义（谁写、写哪份）再统一或清理失效目录。
- **P2 验证账本落后源码（对账 R1）**：末条账本记录锚定 `f05477c`，当前基线已到 `d973a836`；跨度内缺少账本成功验证记录，需按发布门禁补录一次。
- **P2 接口语义瑕疵（2026-09-10 独立复验发现）**：`大语言模型支持库.模型连接器.重试调度` 的参数名 `错误码白名单` 实际语义是"已命中的可重试错误码集合"（纯计算工具，调用方须先自行比对），名称易被误读为"过滤规则"，且无"实际错误码"入参。改进方向二选一：改名（破坏性，需评估调用方）或补 `实际错误码` 入参做真实比对；定稿前不动契约。
- **P3 历史残留观察项（不阻断）**：`工程缓存/协作状态/` 仍有 21 个历史任务 JSON（14 关闭/4 完成/3 创建），来自早期任务重组；清理需先确认无在途引用。

## 已核实通过（非债务）

- A批次资源边界与资源治理全部收口：裸 communicate 受限通信、有界 HTTP 服务、JSONL 有界轮转、流式 payload 预算、句柄/降级/回滚表上限、psutil 缺失拒绝、结束回调异常可观测。
- 权威状态锁身份键回退修复（提交资源与获取锁/续期/释放一致），HTML 黑盒 简单窗口/资源管理 场景通过。
- 资源与任务清理：历史挂起任务关闭重组、残留端口/进程清理、清理失败证据保留。
- 发布闭环：当前 HEAD 制品重新编译入库签名激活、lineage 绑定、HTML 黑盒 408/408、发布门禁通过。
- D批次补充审计涉及的模板、受限通信、凭证、网关异常映射、临时目录、幂等、发布缓存等问题已按当前代码和测试重新核对并收口。
- 当前全量显式测试已按 164 个模块重新执行：1707 个用例、0 失败、0 错误、退出码 0；最新一次 ResourceWarning 为 0。

## 维护规则

- 新问题必须先读取当前代码和测试，写失败回归测试，再做最小修复。
- 修复后必须跑定向测试、受影响波次和必要的真实 HTTP 验证；没有证据不得删除条目。
- 已修复条目立即从本文件删除，不保留历史债务流水。
- 历史方案、旧测试统计和已删除文档不回填本文件；历史由 Git 提交保留。
