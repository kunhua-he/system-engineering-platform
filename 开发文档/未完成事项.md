# 系统工程平台 · 未完成事项（唯一债务清单）

> 本文件是当前唯一未完成事项入口。本轮只记录已经用源码、契约和真实命令核实的问题；修复后必须重新验证并立即删除对应条目。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/底座升级方案.md`。

## 当前状态

- 正式索引：当前源码正式包索引以实时扫描为准；浏览器自动化、直播逐字稿已进入最新候选制品，能力冲突 0。
- 正式源码语法：受影响 Python 文件和转写契约派生文件已通过 `python3.14 -m py_compile`。
- 当前正式索引实测：82 个支持库、14 个模块库、96 个正式包、419 项能力、能力冲突 0。
- 最新一次显式全量：164 个模块、1707 个用例、0 失败、0 错误、退出码 0、ResourceWarning 0；底座功能和资源释放回归已通过。
- 依赖生命周期审计：16 个标准第三方 Provider 0 违规；2 个无包声明的非标准内部适配器按规则跳过。
- res 与 chat LLM 均已真实验收：res 使用 `gpt-5.6-sol + codex_responses`；chat 使用 `deepseek-v4-flash`、`hy4`，普通生成和 `/网关/流式` 均 HTTP 200，流式增量与完成事件正常，句柄查询/释放通过。
- 定向回归：底座转写/直播相关契约、包声明、能力契约和 MLX 测试 51 项通过。

## 阻断事项

（无。当前已完成审计报告涉及的功能失败、测试资源释放和 Provider 生命周期审计收口。）

## 重要事项

- **P2 Whisper 生产精度标杆**：MLX large-v3 链路可用，已建立短音频基准，但业务品牌词仍有误识别；尚无真实业务音频+人工参考稿的长音频逐窗口精校结论。
- **P2 维护性债务（已评估，不属阻断）**：正式源码 129 个文件超 160 行（最大：发布门禁 1445 行、权威状态 936 行、项目服务 828 行）；系统工程平台无 160 行硬规则，按需拆分不凑数字。

## 已核实通过（非债务）

- A批次资源边界与资源治理全部收口：裸 communicate 受限通信、有界 HTTP 服务、JSONL 有界轮转、流式 payload 预算、句柄/降级/回滚表上限、psutil 缺失拒绝、结束回调异常可观测。
- 权威状态锁身份键回退修复（提交资源与获取锁/续期/释放一致），HTML 黑盒 简单窗口/资源管理 场景通过。
- 资源与任务清理：历史挂起任务关闭重组、残留端口/进程清理、清理失败证据保留。
- 发布闭环：当前 HEAD 制品重新编译入库签名激活、lineage 绑定、HTML 黑盒 408/408、发布门禁通过。
- D批次补充审计涉及的模板、受限通信、凭证、网关异常映射、临时目录、幂等、发布缓存等问题已按当前代码和测试重新核对并收口。
- 当前全量显式测试已按 164 个模块重新执行：1707 个用例、0 失败、0 错误、退出码 0；最新一次 ResourceWarning 为 0。

## 维护规则

- 新问题必须先读取当前代码和测试，写失败回归测试，再做最小修复。
- 修复后必须跑定向测试、受影响波次和必要的真实 HTTP 验证；没有证据不得删除条目。
- 已修复条目立即从本文件删除，不保留历史债务流水。
- 历史方案、旧测试统计和已删除文档不回填本文件；历史由 Git 提交保留。
