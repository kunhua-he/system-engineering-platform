# 系统工程平台 · 未完成事项（唯一债务清单）

> 最后更新：2026-09-21（**机器生成**；刷新区跑 `python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --写盘`，核验跑 `python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验`）
> 口径：实测（数字随建包与提交逐日变化，**引用前必须重跑取证命令**）
> 维护者：唯一债务清单
> **编号不复用**：编号只增不重用，历史条目关闭即删。
>
> **当前真实账目**（**机器生成，禁止手改**；核验 `python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验`）：
>
> | 分区 | 条数 | 条目 |
> |---|---|---|
>
> | 一、待修（可直接派活） | **1** | #63 |
> | 二、待执行的大项 | **1** | #56 |
> | 三、待裁决 | **0** | —（空） |
> | 四、新增债务 | **24** | #60 / #182 / #183 / #184 / #185 / #186 / #187 / #188 / #189 / #192 / #190 / #181 / #193 / #195 / #196 / #199 / #200 / #201 / #202 / #208 / #204 / #210 / #212 / #213 |
> | 七、第一轮审计逐条并入（7.1–7.6） | **33** | #67 / #68 / #69 / #73 / #74 / #75 / #76 / #77 / #78 / #79 / #80 / #81 / #82 / #84 / #85 / #89 / #90 / #91 / #92 / #93 / #94 / #95 / #96 / #97 / #98 / #99 / #100 / #101 / #102 / #103 / #104 / #105 / #106 |
> | 七之 7.8 20260918 批审计待证真 | **1** | 表内无编号，**以「项」计，不进编号合计** |
> | 七之 7.9.1 跨平台无关真缺陷 | **4** | #156 / #162 / #163 / #172 |
> | 七之 7.9.2 跨平台补充 | **5** | #173 / #174 / #175 / #176 / #177 |
> | **合计（计入待办的编号条目）** | **68** | 上表各带编号分区之和 |
> | **全文件编号条目** | **71** | 68（计入待办）+ 3（五段「已知覆盖缺口」，占编号但不计入待办） |
>
> **不计入待办的三类**：
> - 五、已知覆盖缺口 **3**（#4 / #6 / #7）：**不是缺陷**，是「还没验到」或口径提示；
> - 七之 7.7 + 7.9.5「不是缺陷（设计取舍）」**12 条**：**防误修清单**；
> - 七之 7.9.4 与既有审计重复 **6 条**：不重复登记，统一在跨平台那轮处置。

> 本文件是当前唯一未完成事项入口。
> 事实源：`开发文档/项目说明.md`；执行顺序：`开发文档/方案/执行编排_唯一入口_20260918.md`；
> 多会话协调：`开发文档/规范/多会话并行开发规约.md`。
> 历轮取证与历史已归档：`开发文档/归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`
> （**不作为派活依据**，历史由 Git 与归档保留）。
<!-- 机器管理｜类型：债务清单｜生成器：开发工具.MD文档生成 -->


---

## 一、待修（修法已明确，可直接派活）

| # | 事项 | 规模 | 落点 / 判据 |
|---|---|---|---|
| 63 | **备份契约的「根」是错的：默认根备不到活的权威状态；包仓库与证据账本另有真实宿主；`项目锁` 是纸面类（全仓无产出方）** | **已修（2026-09-20，开工-20260920-230521-c4da）**：契约改为**逐类取各自唯一解析器**（新增 `平台控制面/备份恢复/类根解析.py`：三类转调 `平台控制面.平台状态.默认存储目录`、包仓库转调 `平台控制面/包仓库/制品布局.制品仓库缓存根()`，**只转调不复制**；显式传根仍是三类共用根＝夹具/演练/测试的合法用法）。缺省根由 `解析运行数据根()` 改为**空串＝逐类回落**。新增只读自省能力 `平台控制面.备份恢复.查询备份源根`（包版本 1.0.0 → **1.1.0**，对外新增面按判定要求递增）。**实测（默认根、无夹具）**：修前 `校验备份` **通过=False**（包仓库缺失、证据账本空、项目锁缺失，且备到的是 `运行数据` 里证据 0 条的**停更副本**）；修后 **通过=True**，逐类来源＝权威状态 `生产副本（VACUUM INTO）` 417792 字节 / 包仓库 `生产副本` **15633 个真实文件** / 证据账本 54 条。端到端演练 RTO 5.07s、三类逐类核对全绿；反向验证 `符合预期=True`；依赖审计 0 新增违规；`测试_工作包09/10` 退出码 0/0；摘要全闭合。原诊断如下 —— 2026-09-20 现场复现（`python3.14 开发工具/备份恢复演练.py` + 用**默认根**真跑备份，无夹具）⇒ **四类挂三类**。① **默认根与活库不同源**：备份契约默认根 = `工程缓存/运行数据`（`备份恢复/实现/能力入口.py:38` 经 `解析运行数据根`），而**活的**平台状态库在 `工程缓存/平台控制面/权威状态.db`（证据 2 条 / 结构 1.5.0 / 09-19 在动）——`平台状态` 默认存储目录 = `<运行缓存根>/平台控制面`（`平台状态/实现/能力入口.py:30`）。默认根下那份 `运行数据/权威状态.db` 是停更副本（证据 **0** 条 / 结构 1.4.0 / 09-18 后未动）⇒ **备到了、但备的是错的那一份**。② **包仓库根口径错**：契约按 `<存储根>/制品` 拼（`备份契约.py:28` 文件名表），真实制品在 `工程缓存/制品仓库`（15633 文件）——与 `运行数据` **平级**，且已有唯一解析器 `平台控制面.包仓库.制品布局.制品仓库缓存根()`（明写「禁止裸拼 `工程缓存`」）⇒ 契约绕开解析器硬拼，永远找不到。③ **证据账本**：契约从 `<默认根>/权威状态.db` 读 `证据` 表（`备份执行.py:82`）⇒ 0 条 ⇒ 空账本判失败；真实证据在同 ++①②++ 那半边。④ **`项目锁.json` 全仓无产出方**（`git grep 锁定时间` 只命中 `校验函数.py:14` 必填字段表 / `备份恢复演练核对.py` 夹具 / 测试夹具）——它的语义（`项目id`/`所有者`）已被 **`权威状态.锁` 表**（列含 `项目id`/`所有者`/`获取时间`/`租约截止`）承载 ⇒ **同一事实两套表示**，且该 JSON 从未存在。**实测（默认根、无夹具）**：`执行备份` → 权威状态 ✓ 212992 字节（**停更副本**）/ 包仓库 `缺失=True` / 证据账本 `条数=0` / 项目锁 `缺失=True`；`校验备份` → **通过=False**，原因 `包仓库: 备份缺失；证据账本: 证据账本为空或不是数组（无可校验证据）；项目锁: 备份缺失`。**修法（①②③ 同源）**：契约不再假设「四类共用一个根」，改**逐类取各自唯一解析器**——状态库与证据账本用平台状态默认存储目录、包仓库用 `制品仓库缓存根()`；**④ 已修（2026-09-21，#63 收口）**：删 `项目锁` 独立类（其语义 `项目id`/`所有者` 已由 `权威状态.锁` 表承载 ＝ **同一事实两套表示**，且 `项目锁.json` 全仓无产出方），对外契约 **四类改三类**；`校验函数`/`类根解析`/`备份契约`/`备份执行`/`恢复执行`/`能力入口`/`能力定义`/`设计说明`/`验证场景引用` 同步收口，编译器 5 份派生物 0 问题 |

### 一.1 派活前必须现场复核（来自一次真实浪费）

> **教训**：按清单原样派出 10 路并行维修，其中一路（原第 44 项「3 条错误码补登记」）**是过时条目**——
> 现场实测三个码早已登记在案，子代理到场无活可干，只能在「到底改没改」上反复推敲，白费一轮。
>
> **判据（此后一律照做）**：从本清单派活前，**先用提交版本核验条目前提是否仍成立**
> （`git show HEAD:<文件>`，不要读工作树——并行维修时工作树是中间态），前提已不成立的条目**当场删掉**；
> 前提成立的，把**复核时点与核验命令**写进任务信再派。清单是「待办线索」不是「事实」，
> 字面照抄派活＝派空任务。

### 一.2 并行维修期禁止 `git add -A`（来自一次真实污染）

> **教训**：主会话用 `git add -A` 提交时，**子代理的反向验证正在跑**，其**刻意写入的变异值**
> 连同正常产出一起被提交进 HEAD。**代价：一次假红被短暂固化成「已验」状态 + 一路子代理中断本业。**
>
> **判据（此后一律照做）**：
> ① **提交一律用精确路径 `git add <文件>…`，禁用 `git add -A` / `git add .`**（并行维修期尤其）；
> ② 提交前 `git status --short` 核对清单 —— **出现不属于本次任务的条目就停手**，先问清来源；
> ③ **反向验证期间变异值就在工作区**，父会话此时提交 = **把假红固化成「已验」**。
>    正确姿势：**等子代理报「已还原」再提交**，或提交前 `git diff --cached` 逐文件确认无
>    `篡改` / `反向样本` / `临时` 字样；
> ④ **共享制品仓库在并行期也会竞态** —— 并行期**构建与黑盒验证应串行**，或各用独立制品目录。

---

## 二、待执行的大项（方案已出，可直接排期）

| # | 大项 | 方案落点 | 规模 |
|---|---|---|---|
| 56 | **§12-E1 40007 从「跑源码树」切到「跑激活制品」** | 实测 plist 确认在跑源码；制品内已有服务入口。⏳ 主会话亲自做，**须等源码树干净 + 门禁通过之后** | 中 |<br>**2026-09-19 主会话勘察（切换落点已探明，前置未满足）**：① 源码入口 `运行核心/启动运行核心网关.py` 用**无前缀导入**（`from 公共契约.…`），制品入口 `平台客户端/运行核心/启动运行核心网关.py` 用**带包前缀导入**（`from 平台客户端.公共契约.…`）且入口内写死 `sys.path.insert(0, ".")` ⇒ 切换＝**plist 三处同改**：`WorkingDirectory` → `工程缓存/制品仓库/平台客户端环境/平台客户端`、`ProgramArguments[1]` → `平台客户端/运行核心/启动运行核心网关.py`、`PYTHONPATH` 保持 `.`；② 制品侧另有独立启动器 `运行入口/启动.py`（端口 45080、自带前端页面路由），与 40007 生产网关**不是同一条腿**，切换只动 40007 那条；③ **前置仍未满足**：清单要求「源码树干净 + 门禁通过」，而并行维修期两项都不成立 ⇒ 本项**留到收尾串行之后**做。

---


## 三、待裁决

| # | 待裁事项 | 选项 |
|---|---|---|
| — | 见 §四 `#60`（`知识检索.检查可用性` 装配 `重排服务` vs 如实声明降级）—— 同一件事只在 §四 记一次，本节不另立摘要行（防重号） |

---

## 四、新增债务

| # | 缺陷 | 现场证据 | 期望修法 |
|---|---|---|---|
| 60 | **`知识检索.检查可用性` 长期返回 `可用=false`（缺失支持库 `重排服务`）**，而模块对外宣称可用 | 2026-09-20 主会话真网关实测：`HTTP 200` + 值 `{"可用": false, "缺失支持库": ["重排服务"], "知识库": "未指定"}`。该包能力 id 是 `知识检索.检查可用性`（**不带 `模块库.` 前缀**，审计报告写的 `模块库.知识检索.检查可用性` 会 404）。⇒ 「模块宣称可用、其实装不上」的**能力可用性自述与事实不符** | 二选一：① 给知识检索装配 `重排服务` 依赖（属环境/装配面）；② 在包声明/说明书里如实写明「重排服务未装配时降级行为」。**先裁决走哪条**，不要直接改契约 |
| 182 | **`检查错误码登记` 的早退把「实现侧错误码」四条判据全部短路，3 个单测恒空** | 2026-09-20 实测（开工-20260920-182332-7166）：`python3.14 -m unittest 测试中心.开发工具.测试_公开调用完整性门禁` → 49 测试 **3 红**（`test_实现侧自造码未登记检出` / `test_实现侧源码不可解析出条目` / `test_实现侧扫描面为空出条目`），三者断言全部拿到 `[]`。**根因**：`开发工具/公开调用完整性门禁.py:361-363` —— `if not isinstance(状态映射, dict) or not isinstance(说明表, dict):` 之后 `if 根.resolve() != 仓库根.resolve(): return []`；单测夹具根只在 `运行核心/统一网关/` 下写了 `本地网关.py`/`网关核心.py` 两个最小表，而 `错误码状态映射源`/`错误码说明表源` 指向的是**仓库根下的固定相对路径**，夹具根读不到 ⇒ 两张表都不是 dict ⇒ **非仓库根直接 `return []`**，标签 378-388 的实现侧四条判据（含「扫描面为空必须判红」）**一行都没执行**。⇒ 判据在测试面上等于不存在（同一类「扫描面为空恒绿」自证，正是该门禁自己在 `:382-383` 想防的那种）。**本项与说明书链改动无关**（`git status` 该文件干净） | 二选一：① 夹具根按 `错误码状态映射源`/`错误码说明表源` 的真实相对路径造表（测试侧跟上实现口径）；② 早退判据改为「仅当**根是仓库根**时才跳过 ①②，标签 ④ 的实现侧判据恒执行」。**推荐②**（实现侧判据本就只依赖 `实现/**.py`，不需要两张表存在）。**注意**：本条只是测试红，生产侧真仓库路径下行为未观察到异常 |
| 183 | **搜索零命中兜底扩词过度宽松：整串匹配不到即退「全表 539 条」** | 2026-09-20 实测（开工-20260920-192419-7548）：`能力目录.搜索能力(关键词="PDF转文本")` → `总数=539`（**等于全表**）、`是否扩词=True` —— 即整串匹配 0 命中后走双字滑窗兜底，滑窗二字几乎命中所有记录（「文本」「转」等），等于**没有过滤**。对照：`查数据库` → 130 条（24% 全表）。⇒ 「No results / 全绿」两种假象都不对，调用方拿到 539 条无法判断哪条是真相关。**性质**：#108 的零命中兜底（2026-09-20 ZCode 修）**方向对、判据缺**——缺「兜底结果相关度下限」与「兜底命中占比上限」两条护栏。**期望**：兜底路径加**分数阈值 + 占比上限**（如命中超全表 30% 即视为无效兜底，改回如实报零命中），并说明「兜底命中非精确匹配」。**注意**：`连mysql`/`发邮件` 零命中**不是本缺陷** —— 现场全仓 539 条能力里 **0 条**含 `mysql`/`邮件`/`mail`（本平台确无此二类能力），故那 2 例是如实报零，正确 |
| 184 | **全仓 700 份 md 未纳入机器管理，且「生成器权威文档」自身的耗时数字与现场不符** | 2026-09-20 实测（开工-20260920-192419-7548）：① `python3.14 -m 开发工具.MD文档生成 --待归一清单` → **700 份** tracked md 无机器印记（即仍是手写件）；② `AGENTS.md` 两处写「编译口实测约 5 秒」，现场计时**单遍 27.5 秒**（大头：参数可用性 8.1s + 重复腿与旁路 5.8s + 测试体系门禁 5.8s）—— 权威文档里的数字未按「必须现场重跑」写。已修 ②（改 28 秒并注明只跑一遍）；① 属**逐批推进**项：按类依次 `--类型 <类型> --写盘 --补头` 加机器印记，配合新增的编译口「拒绝手写md」阻断项（存量冻结、新增阻断） | ① 逐类归一（按份数×偏离率排序，报告见 `开发缓存`；本批已派 4 路只读盘点）；② 已修 |
| 185 | **`None（None）` 生成器垃圾散布 12 份说明书、共 239 处；白名单内 4 份被「不得覆盖」一并冻结** | 2026-09-20 实测（开工-20260920-192419-7548，全仓 tracked md 逐份数）：**共 12 份、239 处**。**★白名单内 4 份（被「生成器不得覆盖」冻结，垃圾保护在最严的护栏里）**：`平台控制面/包仓库/说明/使用说明.md` **112 处**、`平台控制面/发布管理/说明/使用说明.md` **36 处**、`模块库/项目文档/说明/使用说明.md` **8 处**、`平台控制面/能力反馈/说明/使用说明.md` **6 处**（共 162 处）。**未登记 7 份**：`支持库/后端/并发控制支持库/说明/设计说明.md` 39、`办公文档支持库/表格公式/说明/设计说明.md` 9、`数值计算支持库/说明/设计说明.md` 9、`数据库连接支持库/psycopg数据库/说明/设计说明.md` 5、`数据操作支持库/度量/说明/使用说明.md` 5、`适配层/代码地图提供者/说明/使用说明.md` 3、`浏览器自动化支持库/说明/设计说明.md` 1（共 71 处）。另 6 处出现在 `开发文档/未完成事项.md` **本条目自身的引文里**（非缺陷）。原文形态：`**验证状态**：已纳入验证场景：None（None）、None（None）、…` —— G2 生成器在**场景 id/范围取不到**时渲染出的字面 `None（None）`。**问题性质**：白名单处置是「生成器不得覆盖」（本意保护**人工改写**），但其对照字段自己写着「仅验证状态行差（**现存为 None**，生成器多一条 None）」⇒ **垃圾被当成人工成果保护**，护栏语义失真（护的是污染）。**期望**：① 12 份统一走**行级合并**（唯一合法路径 `开发工具.说明书生成.合并重生成`）刷掉 `None（None）`；② 合并后重建白名单对照字段与基线；③ 复核 G2 生成器为何渲染 None（场景引用缺 id/范围，属 `验证场景引用.json` 三形态口径）。**红线**：白名单内 4 份**不得整文件重生成**（其余人工改写会被抹） | **已修（2026-09-20，开工-20260920-214719-4a04）**：238 处全清零，实测 `rg -c 'None（None）' --glob '*.md'` 全仓 **0 命中**（原 238 处含 4 份白名单内 162 处；`未完成事项.md` 自身引文 5 处随之自然减少）。① 6 份**使用说明**走 `开发工具.说明书生成.合并重生成 --写盘` 行级合并（人工行 15/0/0/0/0/0 行原样保留）；② 5 份**设计说明**是 G2 旧账缺陷的存量，用现行 G2 生成器正确账做**行级替换**（脚本只换「含 `None（None）` 的那一行」，非整文件生成），共 63 行；③ 白名单 2 处对照字段已更新为「已清零」；④ G2 生成器「场景 id 取不到渲染 None」的根因已在此前提交修掉（`生成验证状态` 按三形态真实登记渲染）。**残留**：`支持库/后端/项目文档支持库/说明/使用说明.md` 的对照字段仍记「生成器渲染 None」，实测**已不成立**（该份人工行 96 行、含真实场景名，生成器现也产真实行）→ 见 #188 |
| 186 | **`说明/目录树与命令.md` 的目录树与包内实物对不上（PDF生成少一个文件、多一个文件）** | 2026-09-20 实测（开工-20260920-192419-7548，核 67 份全量）：`支持库/后端/文档转换支持库/PDF生成/说明/目录树与命令.md:15` 写 `├── 实现/渲染表格.py`，而 `实现/` 实物为 `生成PDF.py` + `子进程入口.py`（**无 `渲染表格.py`**）⇒ 照目录树执行会打不开文件（与 `#181` 同类：文件搬动后说明未同步）。**对照口径**：判据须排除「未实现占位」件 —— `支持库/适配层/MySQL提供者/说明/目录树与命令.md` 的 `__init__.py`/`实现/提供者.py` 属**未来计划清单**（该件首行明写「未实现占位」「实现批准后必须补齐」），**不是漂移**（实测全仓此类占位件仅 1 份）。⇒ 67 份里真漂移 **1 份** | **已修（2026-09-20，开工-20260920-214719-4a04）**：① 该件树块已按实物刷新（删 `实现/渲染表格.py`）；② 「考虑加机器判据」已落地为真生成器 —— `开发工具.MD文档生成 --类型 包级说明-目录树与命令`（`开发工具/MD文档生成/文档类型_目录树与命令.py`），按同包 `完整性摘要.json` 对账，已接进编译口**阻断**项；③ 判据初版误伤 61 份（根目录行 / `完整性摘要.json` 自身 / 行尾注释 / 带注释的目录条目），已逐条收紧到 **绿 65 / 红 0 / 缺摘要跳过 1**（MySQL提供者未实现占位件）；④ 反向验证三拍留证：绿退 0 / 弄坏退 1 / 写盘退 0 且与 HEAD 字节一致 |
| 187 | **74 份 `说明/设计说明.md` 里装的是「使用说明」的内容（调用口径），与自身首部声明的分工自相矛盾** | 2026-09-20 实测（开工-20260920-214719-4a04，144 份设计说明逐份特征判定）：**74 份**同时含「**参数表**/**返回结构**/**错误码表**/**调用示例**」调用口径四件套 + `**验证状态**` 行 + `## 能力清单` 表 —— 即 `使用说明` 的骨架，而其 H1 与首部引用块却写着「本文是该库的**设计哲学与边界权威**……调用口径……以同目录 `使用说明.md` 为准（由能力契约生成）」。**同一份文件里自己否定自己**：头部说「调用口径不在这」，正文全是调用口径。对照 51 份正确形态（如 `平台控制面/能力反馈/说明/设计说明.md`）：无四件套、无 `验证状态`。**成因线索**：`24704a44`（2026-09-19「说明书拆两个权威：设计说明.md=设计哲学/边界，使用说明.md=调用口径」）**首次**引入这些文件时，74 份的正文直接取了使用说明的账；`开发工具/说明书生成/合并重生成.py` 写死 `说明书相对名=("说明","使用说明.md")`，**永不处理设计说明** → 无生成器可刷、只能靠人记（与 `#186` 同类空头承诺）。**期望**：① 先裁决「设计说明该不该有调用口径段」——不该则按 `支持库模板生成器.设计说明模板` 的真实骨架（本包是什么/不是什么/管什么/不管什么/为什么这样切/失败如何表达）重建**人工区**，**不可机器硬刷**（那 74 份正文本是别人按设计说明"名义"写的内容，机器重写会丢事实）；② 该不该有「验证状态」行也一并裁决。**本条目只做定性与范围冻结，不动手改**（涉及人工内容处置，须华哥裁决） | 待裁决（范围 74 份已冻结；`rg -l '\*\*参数表\*\*' --glob '**/说明/设计说明.md'` 可复现） |
| 188 | **`合并重生成` 对「人工重写过整段」的说明书会产**有损**结果：重复能力清单行 + 删掉代码块内的人工注释** | 2026-09-20 实测（开工-20260920-214719-4a04，对 `支持库/后端/项目文档支持库/说明/使用说明.md` 实跑 `--写盘`）：① 能力清单表**插入一行重复**（`项目文档支持库.查询索引`，账侧返回列渲染成 `{'类型': …}` 而现行是 `结果型`，被当「新行」补账）；② 人工在 ```` ```python ```` 调用示例**围栏内**写的注释行 `# 四档粒度（默认「简介」档与改前逐字段兼容）` 被删；③ 参数表插入重复参数行（`项目名` 两行）。**已回退**（`git checkout` 后 `rg -c '四档粒度'` 由 0 恢复为 1）。**性质**：工具级「人工行零丢失」自检**判不出这两类损伤** —— 它按「字段键配对」判，重复行与围栏内注释都不在判据里 ⇒ **自检通过但结果有损**（「自检口径不足 → 假绿」）。**影响面**：该工具是白名单铁律指定的「刷新说明书的唯一合法路径」，全部 142 份都可能被调用 | **已修（2026-09-20，开工-20260920-222358-5bb2，提交 `91084bcc` + 修正 `3f707ff9`）**：① `围栏序集()` 判出反引号围栏行，**判定期**先于「行 in 账集」取现行（根因是 `字段键()` 把围栏内 `#` 判成「标题行」而标题恒取账 ⇒ 注释被顶掉）；② **新增「重复条目」判据** —— 同一张表内（`##` 小节边界切断）首列同键出现 ≥2 次即判结构异常，并入既有 `人工行零丢失` fail-closed；③ 测试 `测试中心/开发工具/测试_合并重生成反向验证.py` **11 例全绿**，含三条判据存在性证明。**全仓干跑从误报 20 份收紧到 4 份，且 4 份全为真重复**（含 #188 原始现场）。**判据三轮收紧（每轮都跑全仓实测）**：整份计数 → 账侧合法新增顶大计数（`包id` 3→5）误报 20 份；按 `##` 小节计数 → 序号错位 + `- 依赖能力` 本就同键多行，误报 19 份；最终落「单张表」。**（曾据此误报记过一条 `模块库/测试资源` 的「账侧血统选错」，经验证为旧版判据的假条目，已删 —— 当前版该份 意外丢 0 / 零丢失判绿）**。**★自引入回归（已修并留回归样本）**：中间版顺手加了「账侧围栏整块丢弃 + 输出期保护」，致现行示例行走「行在账集」分支而账那份已被丢弃 ⇒ 两边都落不下去，`模块库/测试资源` 示例行消失、111 行膨胀到 174 行（对比：HEAD 意外丢 0 行/示例行在；中间版 意外丢 2 行/示例行没了）。修正为**只保留判定期一处保护**、账侧丢弃回到「只丢空行」，修正后 意外丢 0 行/示例行在/输出 127 行。**测试自身也踩坑并修正**：第一版反向样本只删一处保护 → 另一处兜住 → 弄坏后仍全绿（假绿）；且曾把缺陷态断言写成「自检变红」，而 #188 现场恰恰是「内容被删但自检判绿」—— 断言写反等于测了不存在的行为，已纠正 | 已修（阻断级，含反向验证；本轮未对生产说明书批量写盘） |
| 189 | **`合并重生成` 把「生成器自有内容」当人工成果保住：表条目整行不参与配对 ⇒ 同一张表被插成两张、包版本行与包声明分叉**（华哥口径「全部 md 都要经生成器写」的落地缺口） | 2026-09-20 实测（开工-20260920-230521-c4da，修 #63 后写盘本包说明书时暴露）：① **表条目行落 `("独有", 整行)`** ⇒ 现-only 与账-only 的同一条目配不上 ⇒ 现行判「保」+ 账行另补 ⇒ **`平台控制面/备份恢复/说明/使用说明.md` 能力清单表被整张插成第二张（+38 行）、参数表插入重复的 `存储根目录` 行**（现行参数表有表头 `\| 参数 \| 类型 \| … \|` 而 G2 账的参数表**无表头**，连表头都配不上）。② **`- 版本：` 被兜底分支判「人工改写」保住** ⇒ 包版本 1.0.0 → 1.1.0（新增对外能力，判定要求递增）后，说明书仍写 1.0.0，而包声明/能力定义/清单表都已 1.1.0 ⇒ **同一份文件里两个版本号**。**性质**：把「上一版生成器的产出」误当「人工内容」＝ **生成器自有字段漂移永远刷不掉**（全仓实测同键值不同：返回结构 72 处 / 验证状态 52 处 / 说明 14 处 / 一句话说明 13 处 / 依赖能力 12 处 / 错误码 9 处 / 调用示例 6 处 / 入口 2 处 / 版本 1 处）。**处置**：本包被写坏的那份已 `git checkout` 回退 | **已修（2026-09-20，开工-20260920-230521-c4da）**：① `字段键()` 新增 `表条目`/`表头`/`表分隔` 三类键（表格行按**首单元格**配对，不再按整行）—— `_表头首列` 提升为模块级常量，与 `重复条目行` 共用一份口径；② 新增 **`生成器自有字段`** 常量（版本/返回结构/验证状态/错误码/参数列表/可选参数/必填参数/依赖能力/调用示例/说明/一句话说明/入口），**同键值不同时恒刷**（账即权威），兜底分支只留给真人工改写；③ **写盘处补加机器印记** —— 该工具原先只写正文不加印记，导致**经生成器写的 md 反被 `--拒绝手写校验` 判成「手写」**（实测：本包说明书被判红「发现手写 md 1 份」，而它恰是生成器产出）；④ 测试升级 `测试_合并重生成反向验证.py` **12 例全绿**，含「退回缺陷态必重现重复」的反向样本（单行替换，不用整块片段 —— 整块曾因注释里的反引号匹配失败而使反向样本失效）。**实测**：本包写盘后能力清单 **5 条**（不是 9 条）、版本行 **1.1.0**、同表重复 0、人工行零丢失 True、`--拒绝手写校验` 转绿（2 份全「机器管理」） | 已修（含反向验证与全仓干跑） |

| 192 | **HTML 验证链没有加速器：并发形同虚设（三重串行 + 无连接复用 + 限流卡在 40）**（华哥 2026-09-21 提问「html 的状态机，没有多线程或者调整加速器吗？它本质上只是通过 html 调用网关而已，速度应该很快才对」） | 2026-09-21 实测（开工-20260921-034136-1e09，读实现 + 现场数字）：**① 每请求新建 TCP 连接**：`开发工具/HTML验证/HTTP请求.py:40` 用 `urllib.request.urlopen`（逐次调用，无连接池、无 keep-alive 复用、无 Session）⇒ 每个能力步骤 = DNS + TCP 三次握手全过程。**② 场景内步骤强制串行**：`开发工具/HTML验证/场景执行器.py` 的 `执行场景()` 用 `for 步骤 in 场景.目标步骤:` 顺序跑（步骤间经 `返回表` 传值），**无依赖的步骤也未并行** —— 这是最大一块浪费。**③ 并发上限按「场景数」而非「请求数」**：`max_workers=min(并发, len(场景束.场景列表))`，场景少时线程数上不去。**④ 加速器被限流卡死（关键）**：`运行核心/统一网关/安全/限流器.py:80` 默认 `单维度并发 = 40`、`窗口秒 = 10.0`、`单能力频率 = 200`，而 `网关核心.py:80` 是 `限流器()` **默认构造**（无人传参）⇒ 生效值就是这些默认值。**现场数字完全吻合**：细则记载「60 路并发 ⇒ 总 3.70s ｜ 200×40 + 429×20」—— 通过数正好 **40 = 单维度并发默认值**，被拒的 20 个即 `限流`（网关映射 429）。⇒ **把客户端线程池从 64 拉到 512 没用**：瓶颈在服务端 `单维度并发=40`，客户端再多线程也只会拿到更多 429 | 分三档处置（**先证据后动手**，禁止直接调阈值）：① **连接复用**（最稳、零语义风险）：`HTTP请求.py` 改用长连接复用（标准库 `http.client.HTTPConnection` 显式复用，或每次验证会话持一条连接）。② **场景内步骤并行化**：按 `返回表` 依赖做拓扑分层——无依赖的步骤同层并行，有依赖的串行等待。③ **限流阈值**：`单维度并发=40` 是 2026-09-16 华哥裁决「本地私有部署按相对安全放宽」定下的（原 20），**改动需重新裁决**；且必须区分「验证期批量」与「日常运行」两种负载——正解是**给验证链一条受控的更高配额**（例如经配置项显式传入限流器构造），而**不是**全局放大默认值（那会削弱日常防护）。**判据**：改后重跑 60 路并发，期望 `429 = 0` 且总耗时显著下降；数字写回本节并复核「通过数是否仍等于单维度并发」 |
| | **【本条已收口 · 2026-09-21，开工-20260921-042748-7f63】根因与上文假设完全不同，实测如下** | **真根因是「每次能力调用全仓扫目录 + 缓存体在并发下等于没写」（持 GIL 完全串行），不是限流阈值。** 定位过程（三层取证）：① **纯 HTTP `/健康` 64 并发 = 0.11s（单次 3ms），而能力调用 64 并发 = 6.07s（单次 5.78s）** ⇒ HTTP 层无罪，瓶颈在能力链路；② **抓线程栈**：24 线程全部停在 `glob.select_recursive_step` ⇒ 指向 `模块库/能力目录/实现/能力索引.py` 的目录遍历；③ **栈回溯**：`能力目录.搜索能力` → `_索引` → `构建能力索引` → `索引缓存指纹` → `扫描包目录` ⇒ **每次调用都真走一遍目录树**。现场规模：全仓 **386613 文件 / 11G**、`包声明.json` **7404 个其中 7261 个恰在被排除的 `工程缓存/` 内**（即每次调用都白走一遍缓存目录）；`rglob` 先全量遍历、事后才按 `相对.parts` 排除 ⇒ **剪枝不生效**。**并发下缓存全部失效（关键）**：`构建能力索引` 是「锁外读缓存 → 未命中就重建」，64 个线程同时发现缓存为空 ⇒ **各自全量重建索引**（实测变异样本③：关闭单飞后 64 线程墙钟 **127194ms**、吞吐 1 req/s）。**修复三处**（均零语义变化）：① `扫描包目录` 改 `os.walk` **就地剪枝**（`子目录表[:] = [...]`），根本不进被排除目录；② `构建能力索引` 改**持锁单飞**（读缓存→校验指纹→重建→回填全在锁内），新增 `_索引缓存锁`；③ `索引缓存指纹` 加 **5 秒窗口缓存**（新增 `_指纹缓存`/`_指纹缓存锁`，经 `_缓存窗口秒` 与既有 `系统底座_能力索引缓存秒=0` 开关联动，关缓存时三层全关）；④ `能力目录.搜索能力` 的整串匹配与滑窗打分原先**各自现做 `json.dumps`**（542 条记录单次 13.5ms）⇒ 新增 `_检索全文表` 预序列化并按下标复用（单次 0.77ms，快 17 倍）。**实测（本机 M3 Ultra/32核）**：进程内 64 线程 6087ms → **66ms**；经 **40007 真实 HTTP** 并发阶梯：8 路 0.61s→**0.012s**、32 路 2.64s→**0.041s**、64 路 5.46s→**0.087s**（墙钟 0.16s、**429 = 0**）；吞吐 73 → **984 req/s（13.5×）**。**正确性三重校验**：扫描结果与旧实现**逐字一致**（125=125，集合相等）、索引内容与「关缓存」模式逐字一致（542 条）、关缓存开关确实还原成真遍历。**反向验证（变异样本全部检出，判据有效）**：①指纹缓存关闭 → 慢 27.4×；②全文预序列化关闭 → 慢 13.8×；③索引单飞关闭 → 慢 **1914.9×**。**★另据反向验证删掉一层无用缓存**：曾给 `扫描包目录` 加 TTL 缓存，变异样本证明它**零收益**（关闭后吞吐 981→981 req/s），因调用方已持锁单飞、N 线程只有第一个走到它 ⇒ 已删除（无收益的缓存层只增加状态与失效风险）。**仍未做**（原①②项，见本条目上方处置）：`HTTP请求.py` 连接复用、`场景执行器.py` 步骤并行 —— 但本轮实测表明**它们不是主要瓶颈**（HTTP 层 64 并发仅 0.11s），优先级应下调。**限流阈值未改**：`单维度并发` 已由 2026-09-21「按硬件画像+实时负载定并发」接管（32 核空闲 → 256），无需再动默认值 | **已收口（真根因与假设不同，见左栏取证）**。连接复用与步骤并行降为可选优化 |

| 190 | **「装到环境」链断裂：制品入库被签名信任判据拒绝，导致「只跑制品」的单腿铁律无法落地**（华哥 2026-09-21 裁决单腿后第一个硬阻塞） | 2026-09-21 实测（开工-20260921-030308-77be）：① 构建制品成功（`平台客户端-6af57ee2cc04adf3`，可重复构建校验通过）；② 追加 `--安装` 时失败：`RuntimeError: 能力调用失败（验证失败）：平台控制面.包仓库.入库客户端制品：发布者公钥与既有信任记录不一致`（退出码 1）；③ **公钥本身一致**：磁盘 `工程缓存/制品仓库/平台客户端密钥/发布者公钥.pem` 与 `工程缓存/制品仓库/平台客户端状态/权威状态.db` 的 `信任` 表登记值**逐字节相同**（双方 sha256 均为 `bec6425d6ef272ab`，已实测比对）；④ **真因在信任记录读取腿双源**：`平台控制面/包仓库/平台客户端制品.py:205` 用 `self.状态.读取记录("信任","发布者",self.发布者)` 取记录，而 `self.状态目录` 由 `:103` 决定（`状态目录` 参数不传才回落 `缓存根/平台客户端状态`；构建脚本经 `**目录参数` 显式传入）⇒ 使用的状态库与密钥落盘那份**可能不是同一个**；实测两个候选库中 `工程缓存/平台控制面/权威状态.db` 的 `信任` 表**为空**、`工程缓存/制品仓库/平台客户端状态/权威状态.db` 才有记录 ⇒ 读到空记录/另一条即判 `公钥与既有信任记录不一致`（fail-closed，行为正确，但判据指向了双事实源）。⑤ 另注：磁盘信任元数据 `平台客户端信任/元数据/根信任.json` 记的公钥（`MCowBQYDK2VwAyEAdPylYMH+WciZytPbreIvOoazYbmiHy3yDNilQVi7ON8=`）与现用密钥公钥（`...AyEABKQRF9wlMWDiF0FADKju6J13jg1ekH6YAUv7s4j/lxc=`）**不同**，该文件自 2026-08-28 起未更新 ⇒ 8-28 之后任何「安装到环境」尝试都会撞上本条。**性质**：不是本次改动引入；是「同一事实两套表示」（哲学第 1 条）在信任链上的实例。**2026-09-21 复核补证（开工-20260921-034136-1e09）**：经 40007 HTTP 调 `入库客户端制品` 稳定复现同一错误文案；实测**三份（能力返回／库内记录／盘上文件）113 字节逐字相同** ⇒ 报错文案与公钥值无关，是 `elif 信任记录["公钥"] != 公钥PEM` 分支在**读到别的库**时抛出；全仓 9 个 `权威状态.db` 里 `信任` 表**只有 1 个非空**（`工程缓存/制品仓库/平台客户端状态/`），其余 3 个同名库全为空表、5 个无此表 | 二选一并**先裁决**：① 收敛信任记录为**单一事实源**（定哪个库是权威，另一份删除或改为只读派生；同步修正 `根信任.json` 的公钥）；② 若确属「构建指定状态目录 ≠ 密钥所在目录」的合法用法，则把密钥也随同一状态目录定位，禁止两处各存一份。**禁止**：同步公钥了事（那是绕护栏、且不改双源本质）；**禁止**保留两条读取腿。**2026-09-21 收口复核（开工-20260921-193110-b377）**：① 全仓 `权威状态.db` 的 `信任` 表实测**只有 1 个非空**（`工程缓存/制品仓库/平台客户端状态/权威状态.db`，1 行），其余为 0 行或无此表 ⇒ 与上轮结论一致，**双源仍在**；② **`平台客户端制品.py:103` 的默认 状态目录 正好就是那个非空库**（`缓存根/平台客户端状态`），而 `生成或读取密钥` 的默认 密钥目录 也是 `缓存根/平台客户端密钥` ⇒ **默认参数下两者同根、信任校验能过**；分歧只在调用方**显式传 状态目录**时产生（与上轮推断一致）；③ 用默认参数对**已存在**的制品 `平台客户端-09b48372e368682f` 调 `入库客户端制品`，被**另一道闸**先拦下：`验证失败: 同一包同一版本禁止双摘要（已存在 e8aafa0703a8…）` —— 因为 `入库` 的 `版本 = 制品目录摘要段`（`平台客户端制品.py:200` 传 `版本=摘要16`），**同制品重入必撞这道闸、走不到信任校验** ⇒ **要复现本条必须先用新制品**，而新制品又受 **#213** 阻断 ⇒ #190 与 #213 **互为前置，先裁 #213 才能验 #190** |

| 181 | **`规则类文件` 的指向漂移此前无机器判据，只靠人记得同步** | 2026-09-20 新建 `python3.14 -m 开发工具.规范漂移体检` 后当场扫出 **1 条真漂移**（其余是判据假红，已逐条收紧到 0）：`开发文档/规范/网关调用最小示例.md:219` 指向 `运行核心/统一网关/安全边界.py`，而该文件早在提交 `53602a31`（2026-09-19「统一网关按职责建目录树」）就被移进 `安全/` 子目录——**规范未同步，执行者照做会打不开文件**。这正是 `AGENTS.md` 记的 2026-09-16 同类事故（规范指向已删除接入面）的又一次复发。已修该条并把体检接进编译口「阻断」项 | 已修。**留作判据存在性证明**：新增规则文件时必须过 `开发工具.规范漂移体检`（现已随编译口自动跑，0.06 秒） |

| 193 | **`合并重生成` 的行级配对缺「小节作用域」：包级 `- 版本：x` 与能力级 `- 版本：x` 文本相同即误配 ⇒ `冲()` 跳到后面那一处、中间整段账行一次吐出 ⇒ 后续全部超配对，`--写盘` 被 fail-closed 挡住**（#189 的同族：都是「文本相等 ≠ 同一处」） | 2026-09-21 实测（开工-20260921-064256-6fc0，改 `Git操作.提交` 契约后刷两份使用说明时暴露）：`支持库/适配层/Git提供者/说明/使用说明.md` 超配对 **153 行**、`支持库/后端/版本控制支持库/Git操作/说明/使用说明.md` **86 行**；逐行 dump 现行 vs 账定位到根因 —— 现行第 6 行（包级）`- 版本：1.1.0` 与账里**提交能力的**版本行 `- 版本：1.1.0` 文本相同（账 index 51），`行 in 账集` 走精确匹配 ⇒ `冲(51)` 一次吐出账 [0..51]，指针盒跳到 52 ⇒ 之后每条现行行的账位置都 < 52，全部落 `超配对`（空白行不受影响，故 153 = 167 行 − 6 − 8 空白）。**修法**：输出期在「精确匹配」之前先看同键配对（`判定=="刷"` 且有 `配对[序]`）是否给出**更靠前**的账位置，是则走配对分支；对「零漂移」文件是空操作（零漂移 ⇒ 无配对项），对已漂移文件只会比现在跳得更近。**当前处置**：本轮**未改生成器**，改为按账逐字手改那两行 + `--校验` 复验（两份均报「零漂移」，即手改结果与生成器产出逐字相同） | 待修（须先做全仓「改前/改后 `--校验` 漂移份数」对比，确认 143 份说明书无回归；属独立一轮，不与业务改动混提） |


| 195 | **`热接入` 报「成功包」后改动代码并未生效，且第二次调用即报「无待接入变更」⇒ 旧代码永久驻留到重启；而本仓默认口径正是「不重启」** | 2026-09-21 实测（开工-20260921-064256-6fc0）：① 改 `支持库/适配层/Git提供者/实现/白名单.py`（提交消息放开换行）+ `实现/提交回滚.py`（返回新增 提交文件/文件数）；② 跑 `运维脚本/热接入.py` → `✓ 热接入完成（0.20s）`、`变更包 5`，成功包含 `支持库.适配层.Git提供者（变更）`；③ 再调 `版本控制支持库.Git操作.提交`（多行消息）→ 仍返回**旧文案**「提交消息禁止换行与控制字符」，而该字符串**全仓 grep 0 命中**（盘上新文案是「提交消息禁止回车与控制字符（换行可用）」）⇒ 跑的是内存里的旧模块；④ 改单行消息重试 → 提交成功，但返回值**没有** `提交文件/文件数` 两个新字段 ⇒ 新的 `提交回滚.py` 同样没生效；⑤ 再跑 `热接入.py` → `变更包 0 / 新增包 0`「（无待接入变更：代码与注册表一致）」⇒ **变更记录已被消费，重试也刷不出来**。**性质**：`决策记录 0008` 要求「改完能力走热接入、不重启」，而热接入的成功报告**不构成「改动已生效」的证据**（假绿）；本轮因此只能用单行提交消息收工。**候选根因（未证真，待诊断）**：`后端核心/后端核心.py:272-295` 的 `待处理` 顺序不是依赖序（本次成功包顺序为 `Git操作` 在前、`适配层.Git提供者` 在后），且只弹「`__file__` 落在该包根下」的模块 —— 上层包可能在依赖被重载前就把旧函数对象绑死；需按 `包id` + `__file__` 双条件核实，不排除另有缓存层 | 先诊断再修：① `待处理` 按包依赖序重排（被依赖者先重载）；② 弹模块后加「重载已生效」自证（比对重载前后模块 `__file__` 的 mtime 或函数对象 `id`）；③ 热接入返回值补 `生效包` / `未生效包` 两键，**禁止把「已装配」当「已生效」**；④ 反向验证：改一个**已导入**模块的行为，热接入后必须现场观测到新行为（本轮改的正是已导入模块，天然就是这条反向样本）。**本轮未处置**：不重启网关（会中断其他会话在途调用，与 0008 相悖），故本轮改动待下次网关重启后生效。**2026-09-21 收口（华哥裁决「你直接重启就好了吧」）**：重启后现场复验 —— 同一次 `提交` 调用改报 `路径越界`（不再报消息含换行），证明新 `白名单.py` 已生效；`--探活` 报 `能力数 727`。「旧代码驻留到重启」成立，**重启是当前唯一生效手段**。**①–④ 四条修复已全部落地并现场验收（2026-09-21 复核）**：`后端核心/后端核心.py` 的 `排序键` 已按依赖序排（被依赖者先重载）、`_自证重载已生效`（三条判据：模块对象必须是新对象／旧模块必须全部退场／…）已接在装配后、**未生效包表非空时直接回 `热接入失败` 信封而不是成功**（这根治了假绿）、返回值已带 `生效包`/`未生效包`/`依赖序`/`依赖序说明` 四键。**现场取证**：改完 `模块库/能力目录/实现/能力索引.py` 后调 `热接入` → 直接回 `热接入失败：以下包已重新装配但自证未生效（旧代码仍在场，需重启网关）: 模块库.能力目录: 模块重载后不在场（文件在盘上却未重新载入）` —— 即它**拒绡声称成功**，正是本条要的行为。⇒ 本条已关闭 |

| 196 | **本轮（2026-09-21 纯 MCP 开发）十个卡点的「MCP 落点」对照表：哪些已落地、哪些待做**（华哥要求「总结卡点，怎么优化成 mcp 的落点，让新会话也能一次命中」） | 判定口径：**凡本轮为完成任务而调用过一次终端，即该处存在一个落点缺口**。① **改完能力不生效**（热接入不重载已导入模块，见 #195）→ 落点 `运维脚本/热接入.py` + 新增 `开发工具/能力网关/重启网关.py`（触发即返回、结论写 `工程缓存/网关重启结果.json`）：**已落地**（重启实测 就绪轮次 5 / 2.35 秒 / PID 73661→74482）；待补：热接入返回 `生效包`/`未生效包` 两键，禁止把「已装配」当「已生效」。② **网关重启/探活只能回终端**（手写 `launchctl kickstart` + `curl` 轮询循环，本轮连写 3 次）→ 落点 `重启网关.py --探活 / 触发 / --看结果`，经 `执行命令` 一次调用即得结构化结论：**已落地**。③ **薄壳是会话启动时的旧进程，改了不生效、Agent 无从知道**→ 落点 `tool_catalog` 补「薄壳工具面指纹/版本」，开工时对一次即知自己拿的是不是旧壳：**已落地（2026-09-21 当日复核）**——`tool_catalog` 实测返回 `进程内指纹`/`盘上指纹`/`是否一致` 三键，另带 `指纹口径`/`判据`/`提示`/`不一致文件`/`进程启动时刻`/`进程号`；当场报出「是否一致=假、不一致文件=工具清单.py+薄壳服务.py、进程号 6267」。④ **MD 文档生成只能拼命令行**（本轮调了 8 次 `python3.14 -m 开发工具.MD文档生成 --类型 … --文件 … --校验/--写盘/--人工改`，还试错了一次 `--补头` 不生效）→ 落点 `文档生成.校验 / 写盘 / 人工改 / 列类型` 四个能力（结构化参数、返回 `有漂移份数 / 超配对行数 / 人工行零丢失`）：**进行中（2026-09-21 已派子代理收口）**。⑤ **开发编译口输出被截断**（`--变更` 单次 157250 字节，撞 tool resultBudget 后被截掉后半）→ 落点 `开发编译口.编译口` 能力化，返回**结构化**五态结论 + 红项清单（分「本次变更引入 / 存量冻结」），而不是一大坨文本：**进行中（2026-09-21 已派子代理收口）**。⑥ **`查询文件租约` 忽略 `路径清单`、全量返回 401KB**（打爆上下文，已登记）→ 落点：真按 `路径清单` 过滤：**误报，已证清（2026-09-21 当日复核）**——参数名不是 `路径清单` 而是 `修改路径`（契约原文「项目根相对路径列表；留空表示取全部租约事实；越界路径判 路径越界」）；实测 `修改路径=["平台控制面/能力目录"]` **过滤生效**（全量 412 条 → 0 条）。控体量另有 `摘要=真`（去留言全文与指纹、改回 `留言数`）与 `只要活跃=真`。真因是**调用方凭印象拼参数名**（与 ⑨ 同类）→ 教训已进坑库，**不是缺陷**。⑦ **`收工释放` 报「已结束且已释放」而记录仍写 `状态: 活跃`**（本轮据此误判过一次缺陷，实为「过期即已结束」的正确行为，只是两个字段并列看起来自相矛盾）→ 落点：返回值补 `过期未清` 计数，或把过期状态写实：**已修（2026-09-21，开工-20260921-151536-c107）**——`释放文件租约` 复用查询侧 `_改判过期` 同一判据，非活跃分支如实回带 `状态: 已过期` + `结果: 已过期且已释放`，并新增 `过期未清数` 计数（只动读数、不改盘上数据）。实测：租约 `fffe6f4094b14a0a`（持有 1 秒后过期）释放 → `结果=已过期且已释放`、`状态=已过期`、`过期未清数=1`；修前同一调用回「已结束且已释放 + 状态：活跃」。⑧ **能力搜索零命中兜底返回全表**（546 条 ≈ 全表，本轮踩 2 次）→ 见 #183。⑨ ~~**常用参数组合账为空**~~ **【本条已作废 · 2026-09-21 当日复核】是我把入参读成了状态位** —— `含常用参数` 是 `搜索能力` 的**入参**（逻辑型、默认 假），不是「账有没有记」的状态位；我见它恒回 `false` 就判「账是空的」，错。实测账本一直在正常记账：`工程缓存/运行数据/调用参数账.sqlite3` **33 行**（mtime 与本轮同步，说明每次调用都在写）；`能力目录.常用参数组合(能力id='系统核心支持库.进程管理.执行命令', 组数=5)` 返回 **2 组真实组合**（33 次 / 2 次，带 `样例` 与 `最近时间`，按次数倒序）。**真正的缺口不是账、是「新会话怎么知道去要」**：契约刻意把 `含常用参数` 默认设为 假（控返回体量），而 `通用纪律.工具姿势` 只写「找能力 —— capability_search('<关键词>')」，没提这个开关 ⇒ 新会话默认永远拿不到那 5 组。**已落地**：工具姿势补一条「查能力带 含常用参数=真」（通用纪律 1.3.1）。⑩ **HTTP 请求行必须 ASCII**（中文路径不百分号编码直接 `UnicodeEncodeError`，本轮写重启脚本时踩到）→ 已进坑库 | 按 ①→⑧ 优先级派活；①②⑨ 已落地可直接用，③④⑤ 是「新会话最容易再踩」的三条。**验收判据**：改完后重跑本轮任务，**终端调用次数应为 0**（终端只允许出现在「落点本身尚未建成」的过渡期） |


---

| 199 | **【已定性 · 不是缺陷】`值字段` 是薄壳工具的入参，不是能力参数** | 2026-09-21 深层研究（开工-20260921-130322-ee73）：连续 3 次误判后读实现定性 —— `开发工具/薄壳/工具清单.py:142` 与 `薄壳服务.py:106 _裁剪值()` 证明 `值字段` / `返回上限字符` 是**薄壳工具（MCP 工具）的入参**，与 `参数` / `能力id` / `项目根` **平级**；`_裁剪值` 先按 `值字段` 投影再按上限裁剪，逻辑正确。前几次「不生效」的真因是**把它们写进了 `参数` 里**（当成能力参数 → 被忽略）。实测正确用法（平级传 `值字段: ["匹配列表"]`）：返回体只剩 `匹配列表`，且回带 `值裁剪.保留字段: ["匹配列表"]`。 | **不是缺陷（防后人误修）**。**已补节点提示词**：`开发工具/薄壳/工具清单.py` 的工具说明加一句「这两个入参与 `参数` 平级，不要写进 `参数` 里」（该文件含他人在途改动，本轮未提交，随其落地一并提交）。 |
| 200 | **公开调用完整性 12 项存量红（非本轮引入）** | `编译口 --变更` 唯一剩余阻断项：`支持库/后端/大语言模型支持库/模型连接器`（说明书-未含能力名 等 6 项）、`项目文档支持库`（4 项）、`开工编排`（1 项）、`Swift`（1 项）；形态是 说明书-缺能力段/缺返回载位/缺包级版本载位 + 验证场景未覆盖。 | 生成器对「人工类」说明书拒写，需逐包手补（参照 `文件系统支持库/文件操作/说明/使用说明.md` 的能力段九行式），补完跑 `python3.14 -m 开发工具.MD文档生成 --类型 包级说明-使用说明 --加印记 --文件 <路径>` 加机器印记，再跑 包体检 + 编译口。 |

| 201 | **装配失败会让整包能力凭空消失，而 MCP 侧零提示**（华哥 2026-09-21 批次推进时实测踩到） | 2026-09-21 实测（开工-20260921-125604-2928）：给 `平台控制面.能力目录.查询文件租约` 加两个参数时，`能力定义.json` 插在 `含现场指纹` **之前**、`__init__.py` 的 `_参数契约表` 加在**之后** ⇒ 装配器 fail-closed 拒**整包**（该包 14 个能力一起消失，能力数 733→719），而 MCP 侧只看到「能力未注册」（404），**真原因只写在 `/tmp/网关40007.err` 的 `[装配告警]` 里** —— 不 tail 日志就永远查不到（正是「调终端=能力缺失」的典型）。另同一日志里 `开发工具.文档生成器` 也报装配失败（`parameter without a default follows parameter with a default (提供者.py, line 76)`）。 | **已修（2026-09-21，开工-20260921-130015-7803）**：① `开发工具/能力网关/重启网关.py` 新增 `装配告警()` 读数，`--探活` / 重启结论里直接回 `{告警数, 跳过包清单, 提示}`（口径：取日志末尾 80 行，代表最近一次启动）；② `开发工具/契约编译/漂移判定.py` 的「能力定义 JSON 解析失败」报错加提示（中文 真/假 是最常见原因）；③ `平台控制面/能力目录/说明/设计说明.md` 补两条铁律（参数名序必须与契约逐字逐序一致；`能力定义.json` 布尔写 true/false）。**待做**：把「参数名序一致」提前到**编译期**静态检查（当前只能靠装配器运行时拦）。**已修（2026-09-21）**：名序判据本就在（`开发工具/契约编译/口径比对.py` 的 `比对注册口径明细`：`注册名 != 契约名` 即判红，靠 AST 静态解析，不导入实现），真正的缺口是**扫描范围**：`_全仓注册口径扫描` 原用白名单 `("支持库", "模块库", "技能库")`，而本条场景在 `平台控制面/` —— **根本不被扫**。已改为「全部顶层目录 − 排除表（`工程缓存`/`测试中心`/`开发工具`/`开发文档`）」。**实测对比**：包数 123→**140**、契约能力 635→**745**，新增覆盖 17 个包而**新增判红 0 条**（名序不一致 0／默认值漏声明 0／必填漏声明 0／硬不一致 0）⇒ 纯扩覆盖零回退。**反向验证**：把 `平台控制面/策略闸门` 的注册参数表与名序镜像**同步换序**（两表自洽、仅与契约不一致）后，改范围前报告 `判红 0 条`（看不见）、改范围后当场报出该条 —— 证明判据承重且范围修正是必需的。已加三拍测试 `测试中心/开发工具/测试_注册口径漂移.扫描范围与名序判据`（平台控制面下必红／名序一致必绿／排除表内必不被扫）。⇒ 本条已关闭 |
| 202 | **装配告警口径不能区分「本次启动」与「历史启动」** | 2026-09-21 实测（开工-20260921-130015-7803）：`重启网关.py --探活` 新报的 `装配告警` 里含 `开发工具.文档生成器｜入口装配｜装配失败（parameter without a default follows parameter with a default (提供者.py, line 76)）`，而**该目录已不存在**（`判断存在` 回 假）—— 告警来自更早一次启动的日志残留；`/tmp/网关40007.err` 是 append 模式，多次启动的告警会累积（实测重启后日志仍只有 4 行旧内容）。 | **已修（2026-09-21，开工-20260921-130015-7803）**：`重启网关.py` 在 `launchctl kickstart` **之前**清空错误日志，使重启后的 `装配告警` 读数只代表本次装配；`--探活`（不重启）读的是上次重启后的日志，历史告警不会再混入。**残留**：日志里 `开发工具.文档生成器` 的入口参数默认值顺序问题（`提供者.py:76`）已随目录清理一并消失，无需另修。 |
| 208 | **检索四路缺口（G1–G5）：目录树 / 代码语义索引 / 仓库地图 / 统一检索入口 / 决策层真跑通** | 实测（2026-09-21，华哥「搜索以准确为主 + 完完全全用 MCP」立项）：① **目录树**——搜「目录树」「文件树」「列目录」均零命中，只有单层 `列出目录`，看一棵树要 N 次往返，Agent 为省事就去敲终端（漂移源）；② **代码语义索引**——`知识检索.检索知识` 只面向指定文档目录，**不索引代码**，`代码地图` 要精确名字，中间缺「自然语言 → 代码位置」这一层；③ **仓库地图**——有符号/关系（`代码地图.查询节点/查询关系`）但**无排序、无 token 预算**，大仓看结构只能逐文件读；④ **统一检索入口**——六路能力分散在 6+ 个包，Agent 自己选路就是漂移；⑤ **决策层**——`连接决策模型`/`执行决策` 已注册但验证场景未覆盖（公开调用完整性门禁 2 项红），即接入有了、没真跑通。**可行性已实测全绿**：tree_sitter / networkx / numpy 均可用，BGE-M3 已常驻，**不需引任何新依赖**。 | 按 `开发文档/方案/检索与索引升级方案_20260921.md` 五期推进（目录树 → 仓库地图 → 代码语义索引 → 统一检索入口 → 决策层路由），每期独立验收（真调用 + 反向验证 + 准度抽样）；完成后逐条销账。 |
| 204 | **15 个包仍用「手工重建参数声明」的旧入口模式** | 平台已有正确模式（`运行核心/运行状态监督/__init__.py:38` 的 `_参数契约表` 整条透传 + `_注册参数名序` 名序护盾，2026-09-18 为「丢参」缺陷注入），**39 个包已用**；仍有 **15 个包**用 `契约表 = [` 手工重建。实测（#197 修复过程）：`文件系统支持库.文件操作` 手工声明漏了 `起始行`/`结束行` ⇒ 网关按入口契约表传参，新参数被静默丢弃，验收两次都拿到全文。**当前这 15 个包与契约一致（漂移检测全绿），是潜在风险不是现存缺陷**。 | 按 `运行状态监督` 的模式逐个改造这 15 个包（参数整条透传 + 名序护盾）；改造前先跑 `开发工具.契约编译.漂移检测` 确认当前是否一致。 |
| 210 | **`合并重生成` 的同键配对缺「小节作用域」：同名字段行跨小节误配，历史态说明书残余超配对（#193 的未覆盖面）** | 2026-09-21 实测（开工-20260921-152650-9123）：#193 的修法（`同键配对位` 在精确匹配前生效，已在 HEAD `fee243e5`）把 `支持库/后端/版本控制支持库/Git操作` 的超配对 86 → **0**，但 `支持库/适配层/Git提供者` 历史态残余 **92 行**：其现/账 `###` 小节序列逐项相同（非顺序错位），残余全落在**同名字段行**上（账里 `参数列表`/`一句话说明` 等同键各 14 处）—— 即字段键不含所属小节作用域，同键配对本身会跨小节误配。**风险受控**：超配对非零即 fail-closed 拒写，不会静默写坏。**现状**：对现行磁盘两份跑真工具均为「零漂移、固定点」（主会话复验：`扫描 2 份说明书；有漂移 0 份`），故该残余只在历史态复现 | 待修：字段键加**所属小节作用域**（或配对时按 `###` 小节分段后再同键配对）；须先做全仓「改前/改后 `--校验` 漂移份数」对比确认无回归 |
| 212 | **「改一个能力」的派生同步成本是修改本身的十倍：单 bug 修复 17.6 分钟里改代码只占 3 分钟（17%）** | 2026-09-21 巡检（样本 #156，开工-20260921-152804-82f1）：改「恢复执行」返回结构 4 个键 → 动 **9 个文件**（恢复执行.py / 能力入口.py / 参数契约 / 能力定义 / 使用说明两处 / 设计说明 / 第十四阶段门禁 / 演练 / 完整性摘要），耗时 1056 秒；其中**真改代码 ~3 分钟**，其余为同步派生物、探针真跑、HTTP 两态验收、定向测试、热接入重试。编译器只自动生成 6 份（契约/`__init__`/两份能力数据/完整性摘要/参数契约），**说明书两份、验证场景、权限契约、能力定义手工段仍需逐份手改**，漏一份即被 `包体检`/`验证场景体检`/`公开调用完整性` 判红（实测 #196 ④ 新增 4 个能力→需补 4 条权限声明+2 个正向场景步骤+修非法断言字段）。另本轮黑洞：**单命令超 15 秒 189 条**（编译口单轮 39.3 秒，其中 `md查重` 14.09 秒）、**sleep 等后台 15 次**（`sleep 420` 单条烧光整路预算，纯 bug）、**4 路撞 2400 秒硬超时**、**派活纪律 34/34 未送达**（手写 context 丢了工具姿势/提交纪律/pathspec/命令环境） | 待修（按投产比）：① 派活一律经 `开发工具/任务记忆/派活任务信.py` 生成（**未做**，本轮 34/34 路仍是手写 context）；② sleep 清零、>15 秒一律后台化（**未做**）；③ 派生物收敛（**部分已做**：说明书两份已能一条命令刷新 —— `python3.14 -m 开发工具.MD文档生成 --类型 包级说明-使用说明 --写盘`，实测 `模块库/自修复工具` 补账 16 行、`模块库/文件管理` 补账 6 行、`模块库/项目文档` 补账 43 行且人工行零丢失；**并修掉两处「生成器拒写 ↔ 门禁要求机器印记」互锁死结** —— `模块库/项目文档` 说明书在白名单里被登记为「人工类」，而该条自身证据写的是「与生成器逐字相同（差 0 行人工改写）」= 本就是生成物类，改类后行级合并刷新通过；`支持库/适配层/说明/README.md` 无任何类型归属 ⇒ `--加印记` 也走不通，新增文档类型「说明目录导航」（序号 11）补上纳管路径。权限契约、验证场景骨架仍手改）；④ **编译口瘦身已做**：`开发工具/开发编译口/编译口.py` 的 14 项静态门禁由串行改并行（`门禁并行路数 = 8`），单轮 **30.4 秒 → 12.2 秒**（改前 30378.64 毫秒 / 改后 12161.96 毫秒，同一 `--变更` 口径；单项判据、命令行、超时秒一分未动，只有「等」的方式从相加变取最大）。详见 `开发文档/分析/巡检_修复效率归因_20260921.md` |
| 213 | **客户端制品构建没有 HTTP 腿 ⇒ 在单腿铁律下客户端制品无法重建，#56（40007 切制品）因此不可达**（2026-09-21 收口时实测，开工-20260921-193110-b377） | 单腿铁律 §3/§4 写「构建/入库/安装一律走 HTTP 能力：`平台控制面.包仓库.构建制品` → 入库 → 签名 → 安装 → 校验」，并明令「**不得**再跑 `python3.14 客户端/构建平台客户端.py`（py 腿，无豁免）」。实测这条链**断在第一步**：① `构建制品`（`平台控制面/包仓库/服务.py:95`）是**通用包制品构建器**，签名 `构建制品(*, 包id, 版本, 文件表, 构建输入, 文件模式)` —— 它要求**调用方把全量文件内容当参数传进来**，产出的是内容寻址的**制品记录**，**不产出 `平台客户端-<摘要16>` 目录形态**；② `平台客户端-<摘要16>` 这个身份目录名来自 `平台控制面/包仓库/制品布局.py:151` 的 `计算目录摘要16`，而**唯一产出该目录的实现是 py 腿 `客户端/构建平台客户端.py`**（`制品目录 = 系统根/工程缓存/制品仓库/平台客户端制品`，`:188 构建平台客户端制品目录`）；③ `入库客户端制品` 的入参 `制品目录` 明确要求「**已构建的**客户端制品目录（须为 平台客户端-<摘要16> 形态，且目录名摘要与实算摘要一致）」⇒ 它**消费**已构建制品，不负责构建。⇒ 结论：`AGENTS.md` 把 `构建制品` 当作客户端构建的 HTTP 入口是**事实错误**；按现行实现，客户端制品的构建**只能**由被明令禁止的 py 腿完成 ⇒ 单腿铁律要求「40007 跑激活制品」，而激活制品**无法在铁律下重建**（现役制品是 2026-09-19 21:50 建的，不含此后全部修复）。**旁证**：`客户端/构建平台客户端.py` 是**整条链**（构建 + 入库 + 安装，`--安装` 见 #190），不只构建一步 | 三选一，**先裁决**：① 新增对外能力 `平台控制面.包仓库.构建客户端制品`（从源码树按冻结输入构建 `平台客户端-<摘要16>`），把 py 腿逻辑收进能力、删掉 py 腿（合哲学 1.2 与单腿铁律，但属**新能力立项**：需能力搜索 / 复用决策 / 契约 / 验收场景 / 装配计划）；② 让 `构建制品` 接受「源码树根 + 冻结输入」直接产出客户端形态（改既有能力契约，只增可选参数）；③ 撤销「客户端制品必须走 HTTP」这条要求（**等于承认单腿铁律对构建步不成立**，与华哥 2026-09-21 裁决相悖）。**禁止**：继续跑 py 腿当没看见 —— 那就是「两条腿」本身。**★ 2026-09-21 华哥裁决（开工-20260921-193546-600a）**：「按照设计哲学来吧，**如果已经有原子能力的就增加参数，如果是没有原子能力的就封装**」。现场核实：客户端构建的核心（收集源码树 + AST 改写导入 + 重算包内摘要 + 生成入口/制品壳 + 身份摘要发布）**没有任何原子能力**（全仓搜 `复制并重写`/`_安全复制目录树`/`生成入口`/`生成可运行制品壳`/`重算包内完整性摘要` 只命中 `客户端/` 下四个模块）⇒ **走「封装」**。**已排除的错误做法（实测踩过）**：让新能力在函数内 lazy import `客户端.构建平台客户端` —— 该脚本模块级调 `脚本入口准入("构建平台客户端制品")`，而 `公共契约/运行时/平台适配.py:441-446` 的 `脚本入口准入` 在不支持平台上**直接 `sys.exit(1)` 终止进程** ⇒ 那等于给网关装了个地雷（平台支持范围一变，一次能力调用就把网关杀掉），**禁止**。**实施计划（精确到文件，下一轮照做）**：① 把三个 helper 模块搬进包：`客户端/受控文件读写.py`（247 行）→ `平台控制面/包仓库/实现/受控文件读写.py`、`客户端/制品生成与自校验.py`（261 行）→ `.../实现/制品生成与自校验.py`、`客户端/导入重写器.py` → `.../实现/导入重写器.py`（三份互相 import 的 `from 客户端.X import …` 同批改成包内路径）；② 把 `客户端/构建平台客户端.py:104 复制并重写` / `:149 复制非Py文件` / `:187 构建` 的构建面（不含安装面与能力调用助手）搬进 `平台控制面/包仓库/实现/客户端构建.py`；③ 新增对外能力 `平台控制面.包仓库.构建客户端制品`（**新能力立项**，按 AGENTS.md 需七项齐备：确认需求=本条 / 能力搜索=已做零命中 / 复用决策=复用 制品布局.计算目录摘要16 与包仓库原子发布，不另造第二套 / 占用租约 / 验收契约 / 装配计划 / 工作包）——**新增参数要动四处**（实现签名 / `能力定义.json` / `包仓库/__init__.py` 注册表 / 重编译派生物）；④ `客户端/构建平台客户端.py` 瘦成薄 CLI（只留入口/常量/能力调用），或按单腿铁律直接删；⑤ 两处测试改 import：`测试中心/第一批维修/测试_客户端构建安全.py:18`、`测试中心/平台控制面/测试_平台客户端制品接入.py:24`；⑥ 验证：**单跑该能力一次 HTTP 调用**（传 源码树根 + 冻结输入，确认传参成功且拿回 `制品摘要`/`制品目录`），**不跑全量 HTML 黑盒**（华哥 2026-09-21：黑盒单跑改动的那个、传参验通即可）。⇒ 本条未开工，**完成后再做 #56**（#56 与 #190 均卡在本条） |

## 五、已知覆盖缺口（**不是缺陷，是「还没验到」或「口径提示」**）

> **保留目的是防后人误判，不需要「修」**。

| # | 缺口 | 说明 |
|---|---|---|
| 4 | **环境/运行态依赖类（非缺陷，别去修代码）** | **实测判据**：转写/媒体类在**并发 ≥2** 下会因**多实例同时加载同一份 3GB 模型权重互抢**而失败，报错现象是「统一返回缺字段 `['值','请求id','耗时毫秒']`」（**现象误导：像返回契约缺陷，实为环境并发**）。处置：`_场景资源键` 加键「语音转写模型」令其独占同实例串行。<br>**通用判据**：若顶层红突然从个位数跳到 20+，且失败模式是「统一返回缺字段」或「500」＋连带的「覆盖对账缺少 N 个能力」，**先怀疑自己动了运行态/环境**（清理了 `工程缓存/平台控制面`、`运行数据`、`制品仓库/平台客户端状态`、`平台客户端环境`），**不要按代码缺陷去修** |
| 6 | **旧口径作废：曾报「158 条零场景能力 / 23.8% 未覆盖」** | 实测**零场景 = 0 条**。旧值系算法漏读（只统计「场景内嵌在 验证场景引用.json 里」的情形，漏了 83 份外部场景文件 `验证场景/*.json`）。**再见到 158 / 23.8% 一律按作废处理** |
| 6b | **记忆支持库的验证场景跑在真实生产库上（读类能力值无法定值）** | 该包能力契约里**没有 `存储目录` 参数**，故真跑 `列出记忆` 返回的是**真实生产数据**。<br>① **11 步缺关键值中的 10 步是「读类」** → 返回值取决于真实库当时内容，**确实无法定值**；<br>② **隐患（待裁）**：验证场景读写**真实生产记忆库**。建议给该包能力加可选 `存储目录` 参数（与 `资源管理`/`项目文档` 同款），场景改用受管临时目录；**但这是对外契约面变更（新增参数），须裁决**。<br>**当前不构成缺陷**：验证器全绿，且实测该包能力对真实库只做读/写单条测试数据 |
| 7 | **「能力面 830」口径不可用** | 按 `包声明.json` rglob 累加会因父包重复登记**虚高到 830**；正确口径 = **装配口径**（经 40007 网关 `/健康` 或装配输出）。引用能力总数一律用装配口径，不用 rglob 累加值 |

---

## 六、待主会话亲自做

| # | 事项 | 前置 |
|---|---|---|
| — | 见 §二 `#56`（40007 从「跑源码树」切到「跑激活制品」）—— 同一件事只在 §二 记一次，本节不另立摘要行（防重号） | 源码树干净 + 门禁通过 |
| — | §四 模型孤儿机制归属裁定 | 需先定方向 |

---

## 七、第一轮审计「仍成立 / 部分成立」逐条并入（唯一事实源，2026-09-20）

> 来源：`开发文档/分析/审计发现汇总_20260919.md`（三路只读证真现场重核：开工 ID
> `开工-20260920-003000-z1aa`/`-z1bb`/`-z1cc`）。**本清单为唯一待办入口，汇总件不再另记待办**。
> 编号从 64 起，不复用、不跳号。凡现场已消解或属设计取舍的，写「不是缺陷」并给依据 —— 防后人误修。

### 7.1 空转与死代码（原审计 `审计_空转与死代码_20260919.md`）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 67 | 哲学总账缺「反向试验」栏、`开发工具/` 无「规则自检」强制点 | 现场计数：`反向试验` 在 `开发文档/哲学总账.md` 命中 **0**（`判据` 98 / `强制点` 58 / `状态` 60 / `证据` 12）；`rg '规则自检' 开发工具/` 零命中。期望：总账补第三栏，并把「规则自检」挂成门禁项 |
| 68 | 编译口「变更集无 .py 即跳过」被归入通过桶 | `开发工具/开发编译口/编译口.py:337-339` 直接 `退出码, 输出 = 0, ""`，结论行 `:396` 写「干净通过」不含「语法编译未核验」字样。期望：该支改 `未核验 = True`，或结论行追加未核验声明 |
| 69 | 死代码清单缺逐条归因与处置 | 全仓无「零调用条目逐条归因（①/②/③）+ 最小改法 + 是否共享文件 + 验证命令」的处置清单。**口径须写死**：语料是否含 `测试中心` —— 现场三口径复算为 **32（含测试中心）/ 38 / 46**，报告原数「33」不可复现。逐条差异已查清：**31 存活 / 2 已接线 / 1 新增** |

### 7.2 文档事实源分叉（原审计 `审计_文档事实源分叉_20260919.md`）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 73 | `方案/二次审计落地编排_20260918.md` 仍自称「唯一编排入口」 | 该文件 `:6` 与 `方案/执行编排_唯一入口_20260918.md:7` 同时自称唯一；`主开发文档.md:71` 亦用该措辞（`:73-74` 又写「该编排是过程件…不作为事实源」，两行互相矛盾）。期望：`:6` 改「已完结 · 过程件」；`主开发文档.md:71` 去「唯一编排入口」措辞 |
| 74 | `AGENTS.md` 把一次性审计过程件写成硬规则 | `AGENTS.md:193`「方案全文见 `开发文档/分析/深度审计_底座升级清单_20260919.md` 第八、九节」——**该文件仍在**（49KB），但它是「某次审计的过程件」，不该当硬规则源。期望：改指 `开发文档/规范/批量开发流程与分级验证.md`，删对 `分析/` 的指向 |
| 75 | 手写包数/能力数/说明书分母过期 | 现场：包声明 **142**、使用说明 **143**、包级 142（生成物类 **115** / 人工类 **27**）、能力定义 **136**；`项目说明.md:42` 写 142、`:43` 写 136、`:49` 写「141｜142｜141（生成物类 116 / 人工类 25）」—— 三行数字与其取证命令均不符，且 `:49` 未注明口径切法。期望：三行一律改「跑命令取真值」，`:49` 补「人工类两套切法」说明 |
| 76 | 生成物 `规范/能力目录说明书.md` 带过期数字且缺输入指纹 | `:5` 写「共 138 份包声明」（现场全仓 142）；输入指纹三项（源内容摘要/生成器版本/契约版本）零命中。期望：重跑说明书生成器刷新；输入指纹单独立项 |
| 77 | 「5 根」vs「6 根」：包级设计说明未跟 `公共契约/正式根.py` | `公共契约/正式根.py:30` 已 6 根（含 `启动监督器`）；`启动监督器/健康监督/说明/设计说明.md:56-59` 仍写「5 根」且引用失效行号（写 `正式根.py:23`，真实在 `:30`）。另 `方案/适配层唯一化迁移清单_20260917.md:243` 写「唯一在五根之外的包」。期望：整段重写为「本包已在名表内、参与装配发现」，行号引用改「见 `公共契约/正式根.py::正式根名表`」 |
| 78 | `规范/场景覆盖率口径.md` 自称权威处但不在任何清单 | `:5` 自称「权威处＝本文件」；`:106/:108/:109/:110/:131` 分母仍写 **681**（现场 `项目说明.md:44` 为 689）；`AGENTS.md` 规范清单与 `主开发文档.md` B 栏**均未列它**。期望：681 改「跑第六节取证命令取真值」；补登规范清单；「权威处」措辞降级 |
| 79 | 决策 `0013` 未按规则标「已变更」，另 2 份状态值越出五态 | `决策记录/0013_设计哲学收敛为12主条.md:3` 仍写「现行（已落地）」而现场是 14 主条（`0030` 记的是 12→13）；`0021:4` 写「声明层方案未落地」、`0048:6` 写「已实施并真实验收通过」—— 均不在模板五态（草稿/评审中/现行/已变更/已废弃）内。期望：`0013:3` 改「已变更（→ 见 0030）」；`0021`/`0048` 状态值归五态 |
| 80 | `交付记录/` 于「不再新增」后新增且带过期数 | `主开发文档.md:79` 写「历史留档…只读不改、**不再新增**」，但 `交付记录/20260918_夜间推进简报_给华哥.md` 于规则生效后新增（`d4af0bb7`，2026-09-18 05:35），且文件内 `681` 仍出现 3 次。期望：移入 `开发文档/归档/`，或给 `:79` 补例外 —— **两边都保留现状不可** |
| 81 | `主开发文档.md` 文档地图漏列 `项目证据/` | 文档地图 D 栏（`:76-82`）只有 4 行（交付记录/决策记录/归档/参考资料）；而 `AGENTS.md:43` 承认「运行时验证账本只写入本仓库 `开发文档/项目证据/` 的机器文件」。期望：D 栏补一行「`项目证据/` 机器判据账本：只由能力写入、不手改」 |
| 82 | 权威文档元信息头「最后更新」落后于实际改动 | `主开发文档.md:3` 写 2026-09-17，而该文件 2026-09-20 仍在改；`当前流程图.md:3` 写 09-17、实际改动 09-18。期望：两份头部改为实际最近改动日（**提交前动作，长期靠习惯**） |

### 7.3 重复腿与旁路（原审计 `审计_重复腿与旁路_20260919.md`）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 84 | 网关自建 sqlite 幂等库，与支持库能力同目的 | 真实现已搬到 `运行核心/统一网关/运行态/幂等存储.py:11/:54`（手写 SQL，未走能力）；已被 `调用腿唯一性检测` 登记存量。同层样板在：`运行核心/任务调度/任务系统.py:110/:197`、`灰度指标.py:121/:128`。期望：按现有样板的**引导顺序约束**先核可行性再改，别直接动 |
| 85 | 4 份手写 HTTP 客户端未收口 | `开发工具/薄壳/网关转发.py:45`、`开发工具/HTML验证/热接入回归.py:36-41`、`开发工具/直播逐字稿工具/批量精校队列.py:161-164/:193-197/:317`（`urllib.request` 6 处）、`开发工具/性能基准/基准环境.py:69`（`http.client.HTTPConnection`）。期望：统一走现成件（如 `开发工具/薄壳/网关转发.py:转发()`），删各份自建 |

### 7.4 能力唯一性降重（原审计 `审计_能力唯一性降重_20260919.md`）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 89 | `模块库/*` 逐能力 1:1 纯透传门面：参数面被裁（门面比底座弱） | 现场复算：`文件系统支持库.文件操作.读取文件 [文件路径(必), 编码(选)]` vs `文件管理.读取文件 [文件路径(必)]`；`写入文件` 缺 `编码`/`允许危险路径`；`删除文件` 缺 `允许危险路径`。实现仍是 `return _调用支持库(...)` 单行透传。期望：按报告选项 A **补齐参数面**（不动能力 id） |
| 90 | 提供者注册机制两套并存、零连接 | `支持库/适配层/提供者注册表/`（`提供者注册表.py` + `标准提供者.py`，**无包声明、无能力定义**）与 `平台控制面/提供者/注册表/`（4 条能力）实测零连接。期望：按报告**先登记 + 补只读对齐判据**，暂不合并。**已登记**：`支持库/适配层/说明/README.md` 第五节（5.1 两套并存事实表 + 5.3 六条只读对齐判据）；2026-09-21 复算「甲乙互不引用」仍成立（`rg` 双向 0 命中），但甲（适配层那套）有真实消费者（`运行核心/运行环境管理器/提供者生命周期.py:100,194` 当只读事实点），故「零连接」仅指甲乙之间 |
| 91 | 适配层根下 12 个「非正式包」目录 | 现场 18 有包声明 / 12 无（HTTP服务适配器／MySQL提供者／动态库适配器／密钥提供者／提供者注册表／提供者配置／提供者配置读取／数据库适配器／本地进程适配器／说明／配置契约／配置安全）。期望：与依赖分层审计合并处置，只在 `支持库/适配层/说明/README.md` 写清 A/B 形态判据，**不补能力定义**（`AGENTS.md:327-329` 已明文豁免）。**已登记**：`支持库/适配层/说明/README.md` 第四节（4.1 三条判据 + 4.2 A 形态 19 名 + 4.3 B 形态 12 目录逐项依据 + 4.4 三条张力）；2026-09-21 现场复算 **19 有包声明 / 12 无**（原记 18 为 09-19 快照，差量 = 新增 `说明/`）；豁免原文行号实为 `AGENTS.md:442-447`，`327-329` 是过期行号 |
| 92 | `流式创建摘要` 与 `创建内容摘要` 返回类型不一致 | 模块腿契约声明 `结果型{成功:逻辑型, 值:文本型}`，底座实现 `支持库/后端/系统核心支持库/资源管理/实现/资源管理.py:97 def 创建内容摘要(...) -> str:` 仍返回裸字符串且抛 `FileNotFoundError`（`:100-101`），模块层被迫 try/except 包装。期望：**修底座返回结构**（不修模块） |
| 93 | `续约句柄` / `续租句柄` 参数相同、返回差一字 | `会话存储/实现/会话存储.py:258`、`模型连接器/实现/模型连接器.py:697`；另 6 套 `释放句柄` 并存（`组件控件:79`／`并发控制:217`／`模型连接器:710`／`会话存储:248`／`进程管理:367`／`数据结构:96`）。期望：命名与返回结构统一 |
| 94 | `数据集合` 内含 3 条文本类能力 | `数据操作支持库.数据集合.文本截取`（`:996`）/`文本去首尾`（`:1059`）等仍在。期望：按能力域归位或明确标注为「数据集合的文本处理子面」 |

### 7.5 依赖分层归属（原审计 `审计_依赖分层归属_20260919.md`）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 95 | 密钥提供者被依赖生命周期审计「非标准」跳过 | 审计输出 `跳过: 密钥提供者: 无 包声明.json，非标准第三方支持库提供者，跳过`（退出码 1）。期望：给它补包声明或让审计支持「无包声明的内部提供者」形态 |
| 96 | `模型服务.py` / `系统探针.py` 直连第三方无锁、不进审计 | 两者仍是 `支持库/适配层/` 下平铺 `.py`（未包化为 `模型服务提供者/`、`系统探针提供者/`）；**依赖登记已补声明**（`模型服务` 版本 2.13.0、`系统探针` 版本 7.2.2），但「该建锁没锁」的判据缺口仍在。期望：包化 + 补依赖锁，纳入依赖生命周期审计 |
| 97 | 依赖分两段声明件未被任何门禁调用 | `rg '依赖分两段\|依赖登记' 开发工具/发布门禁/` 零命中；`生成依赖分两段.py --校验` 真退出码 1（2 项：输入指纹过期、与派生结果不一致）。期望：接入发布门禁 |
| 98 | 三项环境依赖不在 `环境自检` 外部命令表 | `开发工具/环境自检/基础.py:33-42` 外部命令表仍 8 项（ffmpeg/ffprobe/tesseract/soffice｜libreoffice/textutil/git/ps/pgrep），**无 security / node / codegraph / browser-use**。期望：补入表并纳入自检 |
| 99 | `豁免正向场景` 判据形态不足以守住边界 | 现场复算 `能力定义.json` 含 `外部依赖` = **0/7374**（零实例）；判据 `开发工具/HTML验证/场景加载.py:102-103` 只认「`豁免正向场景 is True` + 有 `地址`」，**未加两档拒本地**。期望：判据加「拒本地地址」档 |
| 100 | 第三方 import 分布需持续对账（分布已一致，边角新增 4 项） | 现场 AST 复算全仓第三方 import 总数 **117**：`reportlab 34`/`openpyxl 12`/`cryptography 11`/`pptx 10`/`docx 10`/`mcp 9`/`fitz 4`；**新增** `faster_whisper 1`（转写双后端）、`技能底座能力 2`（合规）。期望：建「第三方 import 分布基线」并纳入门禁，新增须显式登记 |
| 101 | 依赖锁覆盖率与口径需持续对账 | `依赖锁.json` 现场 **33** 份；依赖生命周期审计报「标准提供者 33 个，跳过 3」。期望：把「跳过」的 3 个逐条定性（属豁免还是缺口） |

### 7.6 平台判断越界（原审计 `审计_平台判断越界_20260919.md`）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 102 | 三处 docstring 仍写 `start_new_session / killpg`（2/3 仍成立） | `技能库/后端/技能库/实现/技能库.py:15` 仍写「超时与进程组回收：start_new_session 独立进程组，超时 killpg 连孙进程一并回收」，而实现是 `:609 平台适配.子进程组启动标志()` + `:332 进程终止.强制结束子进程(...)`；`支持库/后端/版本控制支持库/Git操作/实现/受管执行.py:4` 仍写「独立进程组（start_new_session）+ 超时 SIGTERM→SIGKILL 回收」，而同文件 `:26` 已写「平台判断、信号号或 killpg 调用，也不再自己 wait 收尾」——**同文件内自相矛盾**。（第三处 `OCR识别/实现/受管进程.py` 已修）期望：两处 docstring 改对 |
| 103 | 本机系统名/架构取值上报唯一口径缺位 | `公共契约/运行时/平台适配.py` 无 `本机系统名()`（零命中）；`platform.system()` 取值点仍散在 7 处：`强制校验.py:258`、`镜像发布.py:101`、`环境管理器.py:119/:219`、`环境指纹.py:108`、`系统信息.py:38`、`基准环境.py:228`、`跨平台部署验证.py:109`；`强制校验.py:64-72` 的 `_归一化系统名`（`macos→darwin` 粘合补丁）仍在 ⇒ 双口径未消除。期望：**先补 `平台适配.本机系统名()` 收口层原语**，再把 `_归一化系统名` 降级为历史锁兼容（**在有正式原语之前不要直接删**） |
| 104 | 平台专有能力「可用性 + 拒绝语义」统一表达（可选） | `系统核心支持库/进程管理/实现/进程管理.py:628` 的 `if not (平台适配.是macOS() and _shutil.which("sandbox-exec")):` 组合判据仍在。报告自标「可选，当前 fail-closed 行为正确」⇒ **不阻断，属观察项** |
| 105 | 「平台判断只许出现在两个收口层」这条铁律无门禁覆盖 | `rg 'sys\.platform\|os\.name' 开发工具/发布门禁/ 开发工具/复用审计/ 开发工具/验证门禁/` 只有 **1 处注释**命中 ⇒ 无任何自动防回潮判据。期望：补一条防回潮判据（收口层白名单 + 其余位置判红） |
| 106 | **测试夹具写进仓库正式根、且不清理**（2026-09-20 实证一次） | 现场：`支持库/适配层/未生成丙提供者/`、`真实甲提供者/` 两个目录各含一个 `依赖锁.json`（内容 `{"依赖":["假依赖丙==2.0"],"python":"3.14"}`）——**未跟踪、全仓零引用、确为测试夹具**。溯源：`测试中心/开发工具/测试_退出码诚实口径.py` 的**早期版本**把夹具造在仓库里（现版本已改为造在 `tempfile.mkdtemp` 临时目录 + `addCleanup(shutil.rmtree)`，跑一遍复查**未重现**）。⇒ 违反 AGENTS.md「测试不得破坏共享状态」。**本次已手工清除**（备份 `…/污染夹具备份`）。期望：① 入库一份**判据**：测试只许写 `tempfile` / `工程缓存/` 下的受管目录，禁止写仓库正式根；② 同类排查（`rg 'mkdtemp|TemporaryDirectory' 测试中心/` 之外的裸 `mkdir` 写仓库的相对路径）。**处置口径**：这是同一现象**第一次**出现（哲学 14.3「出过一次错就修一修，出过两次以上直接研究原理」）⇒ 先登记 + 修夹具；再现一次则补验证门禁级检测器 |

### 7.7 不是缺陷（设计取舍）· 防误修清单

> 以下条目**不进待办**。后人看到现象时不要「顺手修」—— 每条都经现场核实为**有意设计**。

| 现象 | 为什么不是缺陷 |
|---|---|
| ~~「声明 699 / 注册 468」能力数差~~ | ⚠️ **本条已改判（2026-09-20）**：原判「口径自洽、不是缺陷」**不成立**，已转为真债务 **#107**。实测被过滤的内部层包能力**照常注册在网关注册表、照常可调**，却从发现面消失 ⇒ Agent 有 id 才调得动、而 id 只能靠 `rg` 枚举挖，违反哲学「Agent 只记两个动作：搜索能力 → 调用能力」。成因是 `模块库/能力目录/实现/能力索引.py:95 是否公开包` 的整包过滤，不是设计取舍。**#107 已修并关闭（2026-09-21 复核取证）**：内部层包已全部进发现面（带 `内部层` 标记供调用方取舍），残差实测 `745 = 673 发现面 + 72`，那 72 条是 `19 个 支持库.适配层.* 包`的**短名别名**（差额逐条对得上）；**72/72 都有可发现的双胞胎**（`Git操作.提交` → `版本控制支持库.Git操作.提交`），**零漏网** ⇒ 功能层面无「调得动搜不到」。不把这 72 条放进发现面的理由：会造成 72 条重复记录，拉低检索精确率主指标（收益为负）。证据已写进 `能力索引.是否可发现包` 的 docstring |
| 调用面 50 条后端腿 id 字符串未改 | 迁移清单 10.3.3 已改判「实现载体搬家、对外 id 不变」⇒ **按设计不需要改** |
| 适配层根下 12 个无包声明目录 | `AGENTS.md:327-329` 明文豁免「不对外提供能力契约的内部适配器」（与 #91 的差别：**不补能力定义**，只写清形态判据） |
| LibreOffice 对不迁移 | 迁移清单 10.7 三条硬证据（零字节产物判定 / 失败重试 / `--norestore` 适配层均弱于后端腿） |
| `类型转换` 10 项说明同模板（#93 同域） | 报告结论即「仅登记不改」；其真正待办是 #93 的命名与返回统一 |
| 本机系统名双口径（#103 的现状） | `_归一化系统名` 是**历史锁兼容补丁**，先补正式原语再降级，**不许直接删** |
| 同名平铺件与同名包并存（5 处） | 全部是「真实现唯一在包内 `核心.py`、平铺件只做自举 + 等价再导出」的**合法形态**；`开发工具/环境自检.py` 与 `运行核心/能力调用/超时执行.py` 是**判据真按路径引用**的入口腿（`#7` 已据此销项） |
| 备份恢复演练的「生产数据根缺件」 | 演练本体 RPO/RTO 已实测可用（RTO 0.0023 秒 / RPO 0.6903 秒，四类逐类核对全绿）；缺件本身是 #63 的真债务，**不是演练工具的缺陷** |

### 7.8 20260918 批审计（未逐条证真，待处理）

| 事项 | 现场证据 |
|---|---|
| `分析/` 下 20260918 批审计与归因件（约 14 份）尚未逐条证真 | 现存含 `深度审计_底座升级清单_20260919.md`（49KB）· `备份恢复演练实测_20260918.md` · `生成物地图_20260918.md` · `说明书混血包行级对账_20260918.md` · `测试中心归口盘点_20260918.md` · `配置双入口收口_20260918.md` · `端到端背压核对_20260918.md` · `能力面与拆分清单_20260918.md` · `超长文件拆分候选_20260918.md` · `缺陷热点巡诊_结论_20260919.md` · `子代理探索耗时归因_20260918.md` · `开工上下文检索_归因与方案_20260918.md` · `新建支持库回填链路归因_20260918.md` · `DGX-Spark本地算力视频_识别与底座价值评估_20260918.md`。期望：**按 20260919 批同一方法**派只读证真，仍成立的并入本清单，已消解的直接删 |

### 7.9 第二轮：外部审计（黄文辉，2026-09-19/20）逐条并入

> 来源：黄文辉 2026-09-19 微信发来两份报告（实体在微信收件目录，非自述）：
> A `审计报告_朋友项目_system-engineering-platform.md`（35,759 B / 448 行，1017 py，5 并行 agent 纯静态，30 条原子 bug）；
> B `代码审计报告_2026-09-19.md`（10,339 B / 90 行，Windows Python 3.14.7 实测，8 条）。
> 证真方式：**逐条回当前工作树读真代码**（开工 `开工-20260920-021403-32f4`）；报告快照早于当前 HEAD，
> 行号漂 1–3 行，故以**真代码内容**定性。
> **去重（重要）**：本仓 2026-09-19 已有一轮「跨平台审计」（`ee8d5dd5` 落盘 8 路，含
> `审计_Windows兼容阻碍点_20260919.md` 707 行，桶 A12 项/A组 + 桶 B B1–B16）。**黄文辉报告的
> Windows 条目与其中 B1–B16 大面积重复**，本节只登记**该轮未覆盖**的项；重复项统一指向该审计，不重复登记。
> **编号**：接历史最大号（全历史已至 #148），本节从 **149** 起，不复用。
> 结论分布：成立 31 / 修正（非缺陷）2 / 部分成立 2 / 不成立 2 / **与既有审计重复 6**。

#### 7.9.1 跨平台无关的真实缺陷（**可直接派活**，macOS 上就是缺陷）

> **本批已修 6 条（2026-09-20，`89939065`）**：#149/#150/#151/#152/#153/#154 —— 另有 18 条待修（#155–#172）。

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 156 | 恢复执行空表边界返回 False | `平台控制面/备份恢复/恢复执行.py:169 bool(结果表) and all(项["成功"] for 项 in 结果表)`，空表 → False。**口径待定**：本仓哲学 1.4 是「空集 ≠ 通过」，不能照报告直接改成 True —— 须先按哲学裁决「空表算通过还是未核验」，再改 |
| 162 | 构建制品缺 fsync + check-then-act 竞态 | `平台控制面/包仓库/服务.py:126 if not 最终目录.exists():` → `:127 os.rename(临时目录,最终目录)`，前无 fsync、两者间无锁。期望：rename 前 fsync 临时目录；用 `os.replace` 兜容竞态或加锁 |
| 163 | 版本仓库 rename 缺 fsync | `平台控制面/包仓库/版本仓库.py:85 临时路径.rename(目标路径)` 前无 fsync。期望：rename 前 fsync 临时文件（建议统一走 `平台适配.原子写入` 原语） |
| 172 | 平台客户端制品 chmod 隐式平台判断 | `平台控制面/包仓库/平台客户端制品.py:126 except OSError:` / `:127 pass  # 平台不支持 chmod 时忽略（Windows 类）` —— 调用点自写平台判断，违反「平台判断只收口在两处」铁律。期望：走 `平台适配.支持chmod()` 收口层封装 |

#### 7.9.2 该轮 Windows 审计**未覆盖**的跨平台项（新增，可能与 B1–B16 合并处置）

| # | 事项 | 现场证据 / 期望修法 |
|---|---|---|
| 173 | **Windows 上 SSE 断开检测 100% 失效（`socket.MSG_DONTWAIT` 不存在）** | `运行核心/统一网关/传输/流式HTTP.py:58 连接.recv(4096, socket.MSG_DONTWAIT)`；调用方 `:610 监视客户端断开` 的 except 分支为 `:624 上行数据超限`/`:629 (BlockingIOError,InterruptedError)`/`:631 (OSError,ValueError)` —— **不含 `AttributeError`**，Windows 上访问该常量直接抛并**逃出 daemon 线程**（只留 stderr traceback），断开即清理完全不触发、有界信号量拖到 30 秒超时才释放。**B 报告已在本机 Windows 实测证实该常量不存在**。期望：收口到 `平台适配` 加「非阻塞真读一次」原语（`setblocking(False)` 后 `recv` + `BlockingIOError` 表示暂无数据）。**注意**：zcode 审计 B9 已覆盖「`select.select` 用在非 socket 句柄」但**未覆盖本条的 `MSG_DONTWAIT`**，二者须一起改 |
| 174 | 危险路径护栏判据 Unix-only，Windows 系统目录零覆盖 | `支持库/后端/文件系统支持库/文件操作/实现/危险路径.py:72-75` 判据全为 `/etc`、`/private/etc`、`/System`、`/private/System`；Windows 上 `Path("/etc")` 解析成当前盘 `\etc`，真正该拦的 `C:\Windows\System32\drivers\etc\hosts`、启动文件夹、`C:\Program Files`、PowerShell profile **全无判据**（仅 `~/.ssh` 因同名侥幸命中）。期望：按 `是Windows()` 分支补 Windows 系统位置表，并把判据收口到平台适配层 |
| 175 | 物料清单直接调 `platform.platform()` | `平台控制面/包仓库/物料清单.py:11 import platform`、`:66 目标平台 = 构建结果.get("目标平台") or platform.platform()` —— 调用点自写平台判断。期望：改走 `平台适配.当前平台标识()`（与 #103 的 `本机系统名()` 收口一并做，避免两套口径） |
| 176 | HTML 验证器端口正则取首匹配（功能性假绿） | `开发工具/HTML验证/制品进程.py:196 re.search(r"127\.0\.0\.1:(\d+)", 文本)`，`:197` 再判「已启动/启动」关键字。若制品启动期先打印「连接网关 127.0.0.1:40007」再打印「已启动 127.0.0.1:45080」，正则会捕获 **40007** 作为监听端口 → 后续健康检查与场景验证全打到网关而非制品（全绿假象或全红）。期望：改 `re.findall` 取最后匹配，或锚定 `已启动.*127\.0\.0\.1:(\d+)` |
| 177 | Windows 句柄 64 位截断风险 | `公共契约/运行时/进程终止.py:290 句柄 = 内核.OpenProcess(...)`，`ctypes.windll` 默认把返回值当 `c_int`（32 位有符号），而 Win64 的 `HANDLE` 是 64 位指针 → 高位非零时截断，后续 `WaitForSingleObject/CloseHandle` 拿到错误句柄。期望：显式 `restype = ctypes.c_void_p` + 对三个 API 用 `argtypes` 声明。（该项与 zcode 审计 A4「Windows 探活」同函数，真机验证时一并办） |

#### 7.9.3 待裁决（**已清空**）

> 原 #178 / #179 两条已于 2026-09-20 裁决完毕（开工-20260920-185221-c08c），
> 结论与判据归 `开发文档/决策记录/0056_流式传输特例与模型加载信任边界.md`；
> 按「已修条目直接删」口径，本段不再保留条目。

#### 7.9.4 与既有「跨平台审计」重复（**不重复登记，统一在该轮处置**）

| 黄文辉条目 | 已覆盖处 |
|---|---|
| A·H1 客户端构建 `dir_fd` | zcode 审计 **B5**（`客户端/构建平台客户端.py` 全程 `dir_fd=`，Windows 抛 `NotImplementedError`） |
| A·H2 进程组管理调 `ps` / M7 `which` / M9 句柄体系 `ps` / M1 发布门禁 `lsof` | zcode 审计 **B4**（`ps`/`pgrep`/`textutil`/`sandbox-exec`/`security`/`sysctl` 在 Windows 无原生等价） |
| A·H7 `try: import fcntl` | zcode 审计 **B8**（文件锁 `fcntl` 无 Windows 实现，3 处；`单文件互斥存储` 是拒绝对外服务而非降级） |
| B·P3-3 进程终止 Windows 分支未真机实测 | zcode 审计 §一「三条最重要的判断」+ 桶 A A4（已如实声明「Windows 分支未经真机实测」） |
| B·P1-1 `MSG_DONTWAIT` | **部分重复**：zcode **B9** 覆盖「`select.select` 用在非 socket 句柄」，但未覆盖 `MSG_DONTWAIT` 本身 —— 故已在 #173 单列 |
| A·H3 `平台判断越界`主题 | zcode 审计整轮 + 本清单 **#103**（`本机系统名` 收口缺位，7 处） |

#### 7.9.5 「不是缺陷（设计取舍）」防误修清单（防后人照报告误改）

| 报告条目 | 现场证据 | 为什么不修 |
|---|---|---|
| A·H1「Windows 完全不可用」定级 | 入口 `客户端/构建平台客户端.py` 已调 `脚本入口准入`（`平台适配.py:328`），换平台时中文原因 + 退 1 fail-closed | **平台明示排除**，且 zcode 审计已判定「准入是有意裁决不是缺陷」（B1） |
| A·L11 `import subprocess` 死代码 | `构建平台客户端.py:32` 注释已明写「subprocess/tempfile→原文件内零引用（历史遗留，随搬家一并去掉）」，`:33-37` 实际 import 段已无 subprocess | **报告基于旧快照**，现场已清理干净 |
| B·P3-4 幂等表 LRU 名实不符 | `运行核心/统一网关/核心处理/网关幂等面.py:79,87 while len(self._幂等表) > 500: self._幂等表.pop(next(iter(...)))` 实为 FIFO | 淘汰实为 FIFO 属实，但**现场注释未出现「LRU」字样**（搬迁重写时已去掉）⇒ 无「名实不符」可言 |
| B·P3-2「内部模型服务未见鉴权」 | `支持库/适配层/模型服务.py:99 --host default="127.0.0.1"` | **默认已是回环**；「未见鉴权」不成立（`trust_remote_code` 部分已单列 #179） |

---

> **本轮与 zcode 那轮的边界**：zcode 审计解决的是「**能不能换平台**」（阻断级 B1–B16，含准入/依赖锁）；
> 本轮黄文辉报告的价值在 **7.9.1 的跨平台无关真实缺陷**（22 条，macOS 上就成立）与 **7.9.2 的 5 条补充**。
> 派活时**只派 7.9.1**；7.9.2 与 B1–B16 合并处置；7.9.3 待裁决；7.9.4/7.9.5 不派。
