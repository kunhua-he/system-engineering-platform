# 系统工程平台 · 未完成事项（唯一债务清单）

> 本文件是当前唯一未完成事项入口。本轮只记录已经用源码、契约和真实命令核实的问题；修复后必须重新验证并立即删除对应条目。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/底座升级方案.md`。

## 当前状态

- 正式索引：86 个支持库声明、12 个模块声明、408 项能力 owner；现场核对能力 id 唯一、能力冲突 0。
- 正式源码语法：613 个 Python 文件全部通过 `python3.14 -m py_compile`（字节码写入审计临时目录）。
- 发布状态：审计阻断。HTML 验证器职责拆分、正式制品编译和正常链路门禁通过，但全平台审计发现权限、路径、依赖门禁和资源边界问题，修复并复验前不得宣称全盘收口。
- 已核实闭环：项目编译器工作包 13/13、HTML 验证器模块边界/执行/多实例回归 48/48、3实例与8实例完整 HTML 黑盒均为 408/408 能力、545/545 步骤通过，清理失败 0、资源残留 0；发布门禁退出码 0。

## 阻断事项

- **P1 依赖防火墙未接入发布门禁**：`运行核心/依赖防火墙.py` 可发现 `后端核心/后端核心.py:67` 越层依赖，但 `开发工具/发布门禁/运行发布门禁.py` 主流程未调用依赖审计，存在违规时仍可能发布通过。
- **P1 临时上下文路径校验可穿越**：`MCP工具箱/临时上下文.py:74-81` 使用字符串前缀和 `.lstrip('./')`，未做规范化真实路径校验；`../../允许/secret` 可绕过允许目录判断。
- **P1 临时上下文缺少身份校验**：`MCP工具箱/项目服务.py:591-617` 读取、清理、范围核对未绑定调用方身份，知道 `work_id` 即可能访问或删除其他任务上下文。
- **P1 资源句柄归属校验可空身份绕过**：`运行核心/资源协调/句柄服务.py:141-148`、`运行核心/资源协调/服务.py:189-196` 在项目/所有者为空时跳过归属校验，可能读取或提交其他项目资源事务。
- **P1 任务进程响应无界读取**：`运行核心/任务调度/任务进程.py:257-260` 直接 `recv_bytes()`，异常工作器可发送超大消息造成监视线程内存耗尽。
- **P1 HTTP 连接器响应无界读取**：`运行核心/能力调用/HTTP连接器.py:216-222` 对正常和错误响应直接 `read()`，未设置统一响应字节上限。
- **P1 模块合规审计不递归**：`MCP工具箱/模块合规.py:203-206` 只扫描 `实现/*.py`，嵌套实现目录中的违规代码可绕过审计。
- **P1 聚合父包吞掉注册异常**：`支持库/后端/系统核心支持库/__init__.py:96-105` 使用 `except Exception: pass`，子包导入或注册失败时能力可能静默缺失但装配仍成功。
- **P1/P2 能力索引事实源分叉**：`运行核心/加载器/包发现/发现器.py:38-55` 与 `开发工具/项目编译/正式包索引.py:67-105` 对聚合父包/叶包采用相反筛选规则，能力 owner、依赖解析和运行注册存在三套口径。
- **P1 发布物料清单路径可穿越**：`MCP工具箱/发布治理.py:314-329` 直接拼接制品目录与清单相对路径，未拒绝绝对路径或 `..`，制品外文件可被纳入校验；发布证据目录接口也需限制在工程缓存边界内。

## 重要事项

- **P2（多租户时升 P1）任务查询缺少归属校验**：`运行核心/任务调度/任务系统.py:197-208` 按任务 id 直接返回，网关未将凭证主体绑定到项目/用户上下文。
- **P2 依赖防火墙存在静默漏检**：`运行核心/依赖防火墙.py:160-170` 对特定字符串路径跳过扫描，`SyntaxError/OSError` 直接 `continue`，动态导入检查也未覆盖 `importlib.import_module` 属性调用。
- **P2 测试导入失败**：`测试中心.MCP工具箱.测试_发布治理` 使用文件定位方式导入时触发相对导入错误，当前测试入口不能稳定执行。
- **P2 文档规则冲突**：`开发文档/项目说明.md:169` 仍写 MCP 连接失败即阻断，与 `AGENTS.md` 已允许直接 HTML 只读审计的规则不一致。
- **P1 聚合注册吞异常是系统性问题**：除系统核心支持库外，办公文档、文件系统、数据操作、网络通信、大语言模型等多个支持库 `__init__.py` 均使用 `except Exception: pass`，需统一改为可观测且 fail-closed。
- **待确认权限语义**：`后端核心/后端核心.py:171-173` 权限表存在时空用户 id 不拒绝；若非明确的内部无身份调用场景，应改为 fail-closed。

## 重要事项

## 已核实通过（非债务）

- 资源回收失效顺序、记忆支持库统一调用/依赖/副作用契约、整数句柄、轻代码对象父子关系、后端核心唯一调用、前端调用入口、流式 HTTP 统一调用器已有局部验证证据。
- V3 普通记忆读写已核实为底座客户端薄壳；V3 项目状态 SQLite 属于 MCP 协作治理唯一事实源，不作为普通记忆第二实现迁移。
- R1 MCP 包级导入、唯一模块入口和 8766 常驻服务版本已通过真实 `verify_and_record`，25/25 定向测试成功。
- R2 记忆支持库已迁移到验证场景/v1，92/92 正式包场景可加载，408/408 公开能力均有成功目标步骤，受影响波次 59/59 通过。
- R3 客户端摘要已改为标准 JSON；6 个聚合包摘要由唯一生成器批量重算，受影响波次 89/89 通过；旧无效/悬空归档已清理。
- R5 已完成当前 HEAD 制品的正式入库、签名、激活，以及 32 并发完整 HTML 黑盒和发布门禁；HTML 408/408 能力、545/545 步骤通过，清理失败 0、资源残留 0。

## 维护规则

- 新问题必须先读取当前代码和测试，写失败回归测试，再做最小修复。
- 修复后必须跑定向测试、受影响波次和必要的真实 HTTP 验证；没有证据不得删除条目。
- 已修复条目立即从本文件删除，不保留历史债务流水。
- 历史方案、旧测试统计和已删除文档不回填本文件；历史由 Git 提交保留。
