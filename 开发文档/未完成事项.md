# 系统工程平台 · 未完成事项（唯一债务清单）

> 本文件是当前唯一未完成事项入口。**§八 全量未完成清单是唯一入口**（按「你需要做什么」分类，不按审计批次；**已修复项不留流水账，直接删除**）。
> **历轮取证与历史已归档**：`开发文档/归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/取证与历史.md`
> （外部审计逐条核验的取证过程与各轮「已修/已闭合」对照表；**不作为派活依据**，历史由 Git 与归档保留）。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/方案/底座升级方案.md`，
> 多会话并行协调见 `开发文档/规范/多会话并行开发规约.md`，提速与重启纪律见 `开发文档/方案/提速方案.md`。

---

## 八、全量未完成清单（按「处置方式」分类，2026-09-17 晚梳理）

> 本清单是**唯一入口**：按「你需要做什么」分类，不按审计批次。
> （原文件上方的 A–I 段与历轮收口已归档，见抬头；历史由 Git 与归档保留，**不作为派活依据**。）

### 8.1 等条件（不需你决策，等外部）

当前无。

### 8.2 待你裁决（我按哲学判断不了或涉及你的业务/定位）

| # | 事项 | 为什么要你定 |
|---|---|---|
| — | *（编号不复用：第 6/7/8 项已由决策记录 `0030` + 哲学 12.4②/13.1/13.2 裁决并销项，编号留空不再使用）* | 已裁，见 `决策记录/0030_设计哲学收口_废止1.5_替换9.4_吸收外部借鉴.md` |
| 1 | **C-14：并发写护栏的「自动门」并不存在** | 实测「开工即原子申请租约、全成功才开工」这套**公开原子能力根本没有**（`开工编排.开工准备` 只校验不互斥）→ 多会话并发**没有平台级互斥**，目前只能靠父会话切文件域。**要不要把它做成真闸门**（要新能力面）由你定 |
| 2 | **`进程终止.强制结束子进程()` 返回值被丢弃** | FFmpeg/Tesseract/PDF隔离三处 `finally` 都丢弃（回收失败＝仍存活会静默）；单改一处会造第二套口径，要么统一改要么统一登记 |
| 3 | **E 段三条口径** | ①`模块库` 里 4 个平台工具型模块算不算越位 ②`测试中心/` 30 个阶段目录是否按能力域归口 ③发布门禁→平台控制面 走「补能力面」的口径要写进哪份文档 |
| 4 | **D-6 架构级取舍** | `平台控制面` 存在「同名文件 + 同名包」并存（`授权.py`+`授权/`、`统一入口.py`+`统一入口/`）与 3 个服务平铺（`策略中心.py`/`核心快照.py`/`资源监督.py`）→ 合并属大重构 |
| 5 | **D-5 / D-14 结构债** | `模块库/能力目录` ↔ `平台控制面/能力目录` 同名跨层；`开发工具/` 根平铺无二级分层（9 个散 .py + 4 个门禁 + 两套同名配对） |
| 9 | **架构大方向（华哥已表态）** | 华哥原话：「我其实想一次性把架构文档做好他。不然每次都挤牙膏升级，太慢了」→ 需要一次**架构文档总纲级重写**（把哲学、门禁、证据、验收、边界一次性收口），取代逐条增补。范围与优先级待定 |
| 10 | **SSRF 校验与连接未绑定（报告①-1，机制已确证）** | 定性需你确认：本条是「**防护做了却没绑住**」（校验解析出的 IP 从未传给连接，urllib 自己再解析一次）→ 属实现缺陷，**与 9.5「相对安全」不冲突**（9.5 说的是不做绝对安全，不是允许校验形同虚设）。修法：校验后固定 IP 并让连接该 IP（保留 Host/SNI），或连接后核对 peername。**要不要按"实现缺陷"修，还是按"能力萎缩"顾虑维持现状**——由你定 |
| 11 | **Windows 支持态度（报告①-5 + 归档件 §十一 C1 同指）** | 两处跨平台缺口：`远程镜像.py:465-481` 对目录 `os.open(O_RDONLY)` fsync（POSIX 专属，Windows 报 PermissionError）、`依赖防火墙.py:557` 反斜杠路径让豁免表失效。**建议明确「当前只支持 macOS arm64」并在入口拦 Windows**；要支持则单独立项建真实 Windows CI |
| 12 | **模块拆分口径打架（报告①-11 vs 哲学 6.1）** | 报告按**行数**（48 文件 >500 行、9 文件 >1000 行）建议拆；哲学 6.1 明写「**行数阈值不作为门禁**，只按**改动频率**与**读取成本**」。建议**坚持 6.1**，但补一份「高频改 + 高行数」清单作为拆分依据 |
| 13 | **★ 外部借鉴：8 条哲学候选 + 12 条架构机制（本轮新增）** | 已精读 **35 个工程治理类开源项目**（2.6G，落 `~/Documents/Agent/github 源码参考/40_工程治理与构建/`），产出 `开发文档/方案/外部借鉴落实方案_哲学与架构候选_20260917.md`。**最要紧三条**：① **P-3 规则三要素**（判据/强制点/反向试验，缺一即纸面规则）② **P-4 门禁四态**（干净通过/债务冻结/未核验/失败，不得共用一个词）③ **P-5 生成物三律**（输入指纹 + 勿手改标记 + 判据=跑完生成器后无 diff）。外部实证：kyverno `verify-codegen`、dependency-cruiser error/warn 双档、stryker high/low/break（break 默认 null）、cosmic-ray「样本是接口义务+元测试遍历」、pre-commit 可被 `--no-verify` 绕过（**本地钩子不是强制点**）。**需你拍板 8 项，见该方案第四节**。★★ 补充（第二批下载的 tach 实证）：它的 `unused_ignore_directives`（**没被用到的豁免=报错**）与 `require_ignore_directive_reasons`（**豁免必须带理由**）两条规则，正是本项目**「哲学 1.4 空转即杀」+「P-7 例外自证」的现成机器化** —— 直接补上 271 项存量基线的最大漏洞：**修好的存量不会被自动挤出去**。★ **补（2026-09-17）**：① 12 条架构机制的**内容**此前只在方案文件里，权威文档只指向文件 —— 待裁/待办项：**M-3** 结构化变更摘要 + `--模拟` 模式、**M-12** 一规则一文件规则总账形态（注：**与现行单文件总账形态相反，属待裁，不得当规范写**）、**M-4** 门禁项拆文件、**M-5/M-6** 破坏样本 + 变异测试、**M-10** 能力面快照、**M-11** 契约兼容档位进包声明（**动契约，需你批**）；② 外部实证名单补全（原只收 6 个）：另有 **OPA、Litmus、ArchUnit、conftest、please、terraform、GitHub Rulesets `Evaluate`**；③ **卡点 D 待裁**：按「样本＝接口义务」做，还是先放未来（见 §8.2 第 23 项） |
| 15 | **★ 卡点 B 接线清单（18 条半强制）** | `开发文档/哲学总账.md` 已建账：49 条款实测 = 8 条机器强制 / **18 条半强制（判据模块在、发布门禁不调用）** / 24 条人工纪律 / 3 条纸面。★ **2026-09-17 晚现状更新**：哲学已收口为 **13 主条 / 52 子项**（决策记录 `0030`；1.5 已废止删除、9.4 换四分类、新增 1.8/7.9/12.5/第 13 条），**四态分布待批 1 接线后一次性重测绘**（旧速览表分项和 53≠合计 49，属缺陷，已删不留）。最要紧三条：**5.3 对外契约变更判定**（44KB 判据完整、发布门禁零命中）、**1.3/8.2 能力调用图审计**（零命中）、**1.4 反向破坏**（零命中）。另 3 条纸面规则（1.5/1.6/1.7），其中 1.5 与 1.7 直接冲突需先裁决。取证：`rg -n '^\s*(from|import)' 开发工具/发布门禁/运行发布门禁.py`（31 个自家模块，门禁类仅 5 个）。**方案见总纲第四节第 1 层** |
| 16 | **★ 能力调用图审计有识别缺口（批 1 前置）** | `开发工具/复用审计/能力调用图审计.py::_入口能力()` 只认 `能力id=<字面量>` 与 `能力id=<循环变量>`，**不认 `能力id=模块级常量变量`**（如 `模块库/文档生成` 的 `本模块能力id = "文档生成.生成文档"`）→ 注册集算成空 → 假报"漂移"。实测：`审计模块库()` 违规数 **1**、成功 False；该条详情 `声明:[文档生成.生成文档] 契约:[文档生成.生成文档] 注册:[]`（声明==契约，只有注册空）。**危害**：批 1 把它接成强制门禁后会常年报红，且红因是判据自己认不出合法包。**修法**：把现有"循环变量映射"推广成"模块级常量映射"，同构补第三种形态。**禁止**：把"注册集为空"当合法 / 登记豁免 / 白名单排掉该包（均为放宽）。详见 `开发文档/方案/批1落地清单_接线三条判据_20260917.md` 五之二。★ **2026-09-17 晚复核：仍未修**（现场实测 `审计模块库()` → 审计文件 53 / **违规 1** / 成功 False；`_入口能力()` 源码仍无「模块级常量映射」）→ 属**可立即派活的待修项**，不再是「等你裁」：修法见批 1 清单五之二，验收＝违规数 0 且**故意删掉一个包的注册调用仍报红** |
| — | *（编号不复用：第 14 项「另一会话在途」已收口）* | 该会话已于 19:45 停笔；父会话 21:58 复核 mtime 2h13m 未再更改后接手：独立验收（py_compile 4/4 + 摘要 1 项 → 重算后全闭合）→ 精确路径提交 `f125f219`；两份决策记录顺延为 **0028/0029** 并修正正文标题 |


| 17 | **能力搜索硬编码上限 100** | `后端核心.能力搜索(*, 关键词="", 限制=20)` 内部 `限制 = min(限制, 100)`，而全量注册 **664** 条 → 关键词为空时最多只列 100 条，**Agent 没有「浏览」通道**，新人不知道平台有什么。**要不要补一条「分页 / 按域浏览」通道**（属能力面设计，不是缺陷）—— 由你定。★ **现成解法与六条取舍已收口在** `方案/外部借鉴落实方案_哲学与架构候选_20260917.md` §二之二 P-9（K8s 清单聚合/细节按需、Anthropic `defer_loading`+`tool_reference`、OpenAI namespace、MCP 游标分页；处置顺序＝先游标分页+总数+精简字段 → 再加 detail level → 最后服务端检索） |
| 18 | **3 条错误码登记红：写了就会改对外 HTTP 状态码** | `文件已被占用`、`租约不存在`、`写回压缩结果失败` 需写进 `运行核心/统一网关/本地网关.py::公开错误码状态映射`；一写就把这 3 个码的对外状态码**从回落 500 改成裁决值**，属对外行为变更 —— 由你定 |
| 19 | **两项架构级机制与既有哲学需对齐** | ① **运行期依赖防火墙**（`sys.meta_path` 导入钩子）：给每次 import 加开销，且要与 9.5「相对安全」对齐；② **任务区分进程级 / 线程级**（轻任务走线程池）：与第 1 条「进程隔离彻底」可能冲突。★ **实测量化（2026-09-17）**：轻量能力也 fork 进程，**约 20MB/次**；改线程池预估**提速约 10 倍**；落法＝加「执行模型: 线程\|进程」字段 —— 由你定 |
| 20 | **能力 id 形态口径：裸族名 vs 包 id 前缀**（★ 本轮唯一需要你拍的） | 实测**两套口径并存**：**499 个裸能力族名**（如 `文件操作.删除文件`）vs **5 个带包 id 前缀**（如 `文档生成.生成文档`）。现行唯一成文口径只有 README「能力 id 命名口径＝维护者保留裁定权」；哲学 6.2「命名一眼能懂」只管可读性、不管 id 形态。**先例**：D-12 已裁「模块能力 id 改回本包前缀」。**要不要统一成一套**（裸族名 / 全限定前缀 / 按层分）—— 由你定 |
| 21 | **A4/A5（数据丢失、环境丢失）要不要提到 A1 之前** | 本轮「待拍板」里**唯一没有落点**的一条。实测 §8.3 现顺序是 A1/A2/A3（第 9-11 项，嵌套事务原子性等）在前、A4/A5（第 14/15 项：解压回滚**删掉调用方原文件**、原子落盘**先删旧环境**）在后 —— 而 A4/A5 是**不可逆数据丢失**。**要不要提到最前**—— 由你定 |
| 22 | **7 件「现在就做」的架构拓展 + 2 条边界预留** | 第六份材料给的 P0 建议，权威文档此前**零登记**：① 统一配置加载器 ② Schema 迁移 ③ 请求 id 贯穿 ④ 任务队列上限 ⑤ 能力分类树 ⑥ 结构化日志 ⑦ 租户 id 预留；另 2 条**只切边界、不写实现**：权威状态接口抽象 / 任务执行器接口抽象。**做 / 不做 / 分期**—— 由你定 |
| 23 | **卡点 D：按「样本＝接口义务」做，还是先放未来** | 第六份材料主张「故障注入 → 未来再说」，与本轮「样本是接口义务」（已入哲学 12.5）**正面对立**。此前这个抉择点被淹没在「需拍板 8 项」里，**没有独立条目**。**怎么做**—— 由你定 |

### 8.3 待修（修法已明确，可直接派活）

| # | 事项 | 规模 |
|---|---|---|
| 1 | **6 个聚合包 roll-up 声明与子包真源差 636 条** | 中（先修 6 份 roll-up 声明，再同步说明书；顺序不能反） |
| 2 | **`_关闭失败记录` 无界且全仓 0 读取方** | 小（两腿跨域须同批：`适配层/pdfplumber提供者` + `文档转换支持库/PDF文本表格`） |
| 3 | **旧能力 id 残留**（4 个 `复用决策.json` + 注册口径存量基线） | 小（先判该字段是否被机器消费） |
| 4 | **C-15：错误码登记环盲区** | 小-中（扩一条「实现侧产生的码 ⊆ 状态映射码」，扫 `实现/` 的 `错误码=` 字面量） |
| 5 | **A-2 / A-3 验证可信度** | 小（解析失败应与同仓口径一致产出违规；门禁项不能只验自造样例）。出处＝归档件 A 段（`归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`） |
| 6 | **B-10 防回潮三项门禁检查** | 小（裸布尔 / 非正式类型名 / 英文错误码，加进发布门禁） |
| 7 | **R6 判据口径** | 极小（改成两条：「排除 `说明/` 的代码适配器目录数=11」+「空目录数=0」） |
| — | *（编号不复用：原第 8 项 D-7/D-9/D-17② 已拆解销项）* | **D-7 已处置**：40006 plist 已改名 `com.huashi.gateway-40006.plist.已停用_目标文件已删除`（实测）；**D-9 已登记**：README 实测命中「技能库/运维脚本」13 处；**D-17② 已拆为 §8.3 第 34 项**（3 条激活指针能力立项） |
| 9 | **A1 嵌套同连接事务破坏原子性（严重）** | `运行核心/权威状态.py:956` 外层 `with 连接` 里循环调用 `回收租约()`(:610)/`失效句柄()`(:503)，两者各自 `with 连接` → 内层退出即提交外层。**已实证**（对照实验：外层中途抛异常后内层仍落盘）。修法：给这两个方法加「传入现有连接」重载，或在本函数内联 SQL（照 `失效句柄并记录证据` 那条路径的既有做法） |
| 10 | **A2 `释放锁()` 收 `操作id` 不用** | `权威状态.py:827`。要么加进 WHERE，要么从签名删除（二选一，须先问调用方是否有依赖） |
| 11 | **A3 `提交资源()` 版本号 int() 无保护** | `权威状态.py:732`。非数字版本时抛 ValueError 穿透错误边界 |
| 12 | **C1 依赖防火墙 Windows 路径失效（跨平台假红）** | `依赖防火墙.py:557`（`str(relative_to())` 精确查豁免表）+ `同包实现导入()`(:268)。**已实证**：反斜杠输入下豁免 `False`、同包判定 `False`。修法：统一用 `as_posix()` 或按 `parts` 比对 |
| 13 | **C3 264 个正向步骤只验类型不验业务结果** | 占比 35.6%（742 步中 264 步无 `关键值`）。不是"某个能力坏了"，是**验证资产的整体强度缺口** → 按域分批补关键值断言 |
| 14 | **解压回滚删除调用方原有文件（高·数据丢失）** | `文件系统支持库/文件操作/实现/文件系统补充.py:248` 用 `open(目标,"wb")` 直接截断既有文件，`:258` 写完才登记进 `已写路径列表`，失败时 `_清理解压半成品` 按列表 `unlink()` → **把调用方原文件删掉且无备份**。实测逐行吻合（报告①-2） |
| 15 | **`原子落盘()` 先删旧环境再替换（高·环境丢失）** | `运行核心/运行环境管理器/远程镜像.py:493-495`：`shutil.rmtree(目标目录)` **然后** `os.replace(临时, 目标)` → rmtree 后 replace 失败则**新旧环境同时消失**，`except OSError` 只清临时目录不回滚（报告①-3） |
| 16 | **ZIP 压缩截断既有目标 + 可能自包含（中）** | `文件系统补充.py:92` `ZipFile(目标路径,"w")` 截断既有 ZIP；`os.walk(源路径)` 未排除目标自身 → 目标在源目录内会被枚举写入自身；失败不清理半截 ZIP（报告①-6） |
| 17 | **超时执行线程无界（中·账本已有界）** | `运行核心/能力调用/超时执行.py`：账本硬上限已实现且自述「条目数 ≤ 硬上限恒成立」；**但实际线程数无任何全局/每能力上限或熔断** → 报告①-4 的核部分成立（补的是线程侧资源准入，不是账本） |
| 18 | **PDF 解析线程泄漏 + 超时值缺校验（中）** | `pdfplumber提供者/实现/PDF文本表格.py:146` daemon 线程、`:148` join 超时后线程继续跑；`:134` 只有类型校验，缺 `>0` 与 `math.isfinite()`（报告①-7） |
| 19 | **`追加字节数` 实为字符数（低·字段名骗人）** | `文件系统补充.py:64` `f.write()` 返回字符数却记为「追加字节数」（中文时 报2 实际6，报告①-9） |
| 20 | **能力调用图审计器「模块级变量能力id」假红（★ 与 §8.2 第 16 项同源，同一处理）** | `模块库/文档生成/__init__.py:25` `本模块能力id = "文档生成.生成文档"`、`:45` 传变量名注册；审计器 `能力调用图审计.py:122-148` 只认字符串常量与 for 列表 → 抽不到 → 判漂移。**实测复现**（审计文件数 53、违规 1 条）。判据：路线 A（审计器支持常量折叠）优于路线 B（改包写法） |
| 21 | **README 静态事实指标持续漂移（低）** | 实测包声明 **139**、提交 **886**；README 写 133/865、报告说 134/868 → **三者都不同**，证明"手写静态数字"必然漂移。修法：动态生成或去掉静态值（报告①-10） |


| 22 | **写规范「说明书权威分工与维护规约」** | 小（① 甲支=生成物，定唯一生成入口与落点；② 乙支 23 份 + 6 份混血包=人工权威，禁生成器覆盖；③ **说明书是调用口径权威、契约是机器可读副本、实现是最终事实**，三方不一致以实现为准）。未落纸前「按说明书还是按契约」的争论会重复发生 |
| 23 | **说明书存量治理施工表（13 格 / 约 110 分钟）** | 中（G1 白名单为前置 → G5/G7 回填；G3→G4；G11→G12；其余可并行。**禁止对含实质漂移的包裸重生成**：`FFmpeg媒体` 说明书里唯一正确的 `返回字节` 会被抹掉） |
| 24 | **`全面漂移检测` 零调用方** | 小（修好 `检测说明书一致` 也不接门禁，漂移仍会积存 → 属门禁覆盖问题，与 A-3「门禁项只验自造样例」同型） |
| 25 | **说明书分母口径未复现（132 vs 139）** | 极小（实测分母 139 而非 132，差 7 包来源不明 → 对外引用前必须回填口径，否则两套数字互相打脸） |
| 26 | **场景覆盖率要不要进发布门禁** | 小（若进，「未覆盖」会立刻把门禁打红 → 必须先定存量口径；与 §8.5 第 1 项「能力覆盖缺口」同源） |
| 27 | **「高频改 × 高行数」拆分清单（拆分依据，按哲学 6.1 口径）** | 小-中。实测 Top 9（提交次数 × 行数）：
`运行发布门禁.py` **47×1937** ★最高优先｜`网关核心.py` **34×1227** ★｜`模型连接器.py` 30×1667 ★｜
`本地网关.py` 29×1150 ★｜`合规测试包.py` 19×998｜`后端核心.py`/`构建平台客户端.py`/`平台客户端制品.py` 各 18（808/765/695）｜
`公共契约/句柄体系.py` 17×587｜`权威状态.py` 15×1159 ★（A1/A2/A3 三条缺陷都在这）｜`公开调用完整性门禁.py` 14×1388 / `模型HTTP提供者.py` 14×749。
**结论：真该拆的是「改得最勤 + 最长」的前几项；改动频率低的大文件按 6.1 不该为行数而拆。**（顺带更正：≥500 行非测试文件实测 **41 个**，不是外部报告说的 48） |
| 28 | **能力面按层构成与「冻结增长」的落点** | 小。原稿实测按层：支持库 671（占 81%）｜平台控制面 89｜模块库 **61**｜技能库 4｜运行核心 3｜启动监督器 2（合计 830，包声明口径）；单能力包 21 个；能力最多的包为 数据操作 48／系统核心 44／并发控制 39／大语言模型 36。
**结论：外部建议的「冻结能力总量增长」应落在「支持库新增」上（已 600+）；模块层组合能力（61 条）反而是最该长的地方。**★ **口径待重算**：上述按层数是包声明口径，与权威装配口径不一致（见 §8.5 第 7 项）→ 落地前必须用**装配后公开面**口径重算一次分布，不得直接引用 830 系数字 |
| 29 | **能力 id「只增不改」静态门禁 + 冻结基线** | 中。新增判据「id 只增不改」+ 冻结基线清单；**基线文件缺失时必须 fail-closed**（不许静默放行）。波及面（并入前须重测）：最热 id `文件系统支持库.文件操作.删除文件` 波及 **34 文件**；单包内嵌 id 的消费者文件 **9 类**（能力契约／验证场景引用／能力定义／搜索数据／Agent查询数据／权限契约／完整性摘要／复用决策／包声明），约 **11 处/包**。**执行前置**：先出 id 命名规范结论、再冻基线（否则两套口径一起被锁死）。判据同 `未完成事项` 的 B-35 型 |
| 30 | **消费者契约注册表 0 份登记 → 漂移门禁对真实包恒放行** | 小。实测 `find . -name "消费者契约*.json"`（排除 `工程缓存`/`.git`）**仍为 0** → 属**机制空转**（不是机制缺陷）。补两件：① 填表；② 加「**公开能力必须有登记**」反向断言 |
| 31 | **运行期用 `能力id.split(".")[0]` 反推包前缀** | 小。`运行核心/能力调用/唯一能力调用.py:202` 实测仍在原位 → 应改用**显式来源字段**（B-35 是同型另一处，须同批看） |
| 32 | **报告①-8：负路径测试 5 类空缺断言** | 小-中。与「正向 264/742 步只验形状」**合并为一条**「验证资产强度缺口＝正向关键值 + 负向数据保持」。5 类：目标文件失败后逐字不变／目录第二项失败可恢复／replace 失败旧环境可启／SSRF 两次解析须拒漂移／超时达上限拒新调用。★ 并入后须把 `方案/C类待补证据_定向复现_20260917.md` 里「已在核验文档『报告①-8』登记」一句改指本条，否则它一删就悬空 |
| 33 | **报告①-12：宽泛异常捕获的复算与分类** | 小。★ **必须带口径标注**：同一份材料两次统计不同（568 处/31.6%，有留痕 210、无留痕 346 → 另一口径改成 280）→ **并入前必须自己 AST 复算一次，不照抄** |
| 34 | **3 条激活指针能力立项** | 中（≈56 分钟 / 7 格）。`平台控制面/包仓库` 新增 `诊断平台客户端激活指针`／`重建平台客户端激活指针`／`重置平台客户端激活指针`（现有 14 条能力 **0 覆盖**）；规格见 `方案/客户端构建能力化落地方案_20260917.md` |
| 35 | **激活指针 schema 双套 + 制品目录积压** | 小-中。① `平台客户端制品/当前.json`（键 `路径`）与 `平台客户端环境/当前.json`（键 `制品目录`/`制品摘要`）**同名不同 schema**；② `平台客户端制品/` 下积压 **5 个** `平台客户端-<摘要>` 目录（`清理过期制品` 只在 `--安装` 成功路径调用） |
| 36 | **构建链调用面未收口** | 小-中。`客户端/构建平台客户端.py:738/739/744/747` 全部**直调 `接入.X()` 实现方法**而非能力 id；`可复现构建器` 在客户端 **0 引用**。含 4 条重叠结论：`构建制品`／`查询制品布局` **真重复 → 勿再开同名能力**；`校验可重复构建` **真缺口**；`安装制品` 语义重复待裁。并入时同步扩 C-11 覆盖面 |
| 37 | **说明书：B 类 6 混血包的 99 行手写区行级合并债** | 中。将来重生成这 6 包**必须先做行级合并**（保手写行、刷生成行）—— 白名单是**文件级、不是段落级**。★ 术语定义：**甲支**＝含生成器签名句、**乙支**＝不含；血统判据与禁令见 `规范/新建支持库与模块指南.md` 说明书治理节 |
| 38 | **`返回字节` 冗余声明需人工裁决** | 小。实测 `返回字节` 只在 `转码` 三侧一致；`提取音频`／`抽取帧`／`截取音频` **四处皆无** → 说明书那 3 处是「**与实现不符的冗余声明**」。同批改 `开发工具/公开调用完整性门禁存量基线.json` 里已过期的描述 |
| 39 | **说明书分母口径：140 vs 包声明 139 差 1** | 极小。唯一来源＝`开发工具/直播逐字稿工具/使用说明.md` 是**工具级文档、无同目录 `包声明.json`**；已并入 `项目说明.md`《唯一权威口径表》 |
| 40 | **悬空引用：组件规范支持库 引的「收口方案」不存在** | 极小。`支持库/后端/组件规范支持库/说明/使用说明.md:11` 引 `开发工具/说明书生成/ 收口方案` **实测不存在** → 补写该文档或改写指向 |

### 8.4 待执行的大项（方案已出，等你发令）

| # | 大项 | 方案落点 | 规模 |
|---|---|---|---|
| 1 | **D-1 适配层唯一化** | `开发文档/方案/适配层唯一化迁移清单_20260917.md`（1980 行 + 附表）：真调用点 57 条/17 文件、消费方 14 包/去重 50 文件 | 大 |
| 2 | **E-6 裸布尔迁移** | `开发文档/方案/裸布尔迁移分批方案_20260917.md`：AST 6623 处/657 文件，分 10 批（批间零重叠、每批带验收命令） | 大 |
| 3 | **E-8 类型体系收口** | `开发文档/方案/类型体系收口盘点_20260917.md`：16 型 6 有实/7 半实/3 零消费；归一表实测 16 张 | 大 |

### 8.5 已知覆盖缺口（不是缺陷，是「还没验到」）

| # | 缺口 | 说明 |
|---|---|---|
| 1 | **能力覆盖缺口** | 验证器报「正式公开能力全集 != 正向目标步骤能力全集」：缺 `快照校验.校验状态兼容范围` + 策略闸门 4 条 + 能力目录 4 条 → 按包分 3 路补正向目标步骤 |
| 2 | 环境类 | `直播逐字稿.全自动精校正向` / `转写支持库.转写音频文件` 报「环境依赖未就绪」（MLXWhisper 模型路径）—— **非代码缺陷**，本机环境未就绪 |
| — | *（编号不复用：原第 3 项「H 段历史红」已随归档销项）* | H 段（需华哥裁决）与 §十/§十一 外部审计逐条核验段已归档到 `归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`；表中残留项多已修复，**引用前必须重跑**（哲学 12.4⑤），归档件**不作为派活依据** |
| 4 | **环境/运行态依赖类（非缺陷，别去修代码）** | 黑盒里 `媒体转写.*`、`直播逐字稿.*`、`转写支持库.*`、`网页浏览.*`、`启动监督器.健康监督.*` 依赖本机环境（MLXWhisper 模型路径、浏览器宿主）与**运行态目录自建**。**判据**：若顶层红突然从个位数跳到 20+，且失败模式是「统一返回缺字段 ['值','请求id','耗时毫秒']」或「500」＋连带的「覆盖对账缺少 N 个能力」，先怀疑**自己动了运行态/环境**（例如清理了 `工程缓存/平台控制面`、`运行数据`、`制品仓库/平台客户端状态`、`平台客户端环境`），**不要按代码缺陷去修**。实证：2026-09-17 清理洁净树运行态后顶层红 1→21，全部为上述模式 |
| 5 | **报告类文件的处置口径（2026-09-17 定）** | ① **已修的一律不留在本清单**（华哥：「完成的就移除掉，不需要留下流水账」）——历史由 Git 保留，不要在清单里记提交号与过程；② 只有**可复用的判据**值得留，且必须**并入既有判据条**（如本条与第 4 条），不单开「已修」小节；③ 外部审计报告的逐条核实结论属**取证与历史** → **已归档**（`归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`），本清单不再承载核验过程 |
| 6 | **旧口径作废：曾报「158 条零场景能力 / 23.8% 未覆盖」** | 实测**零场景 = 0 条**。旧值系算法漏读（只统计「场景内嵌在 验证场景引用.json 里」的情形，漏了 83 份外部场景文件 `验证场景/*.json`）。**再见到 158 / 23.8% 一律按作废处理**。取证：遍历全仓 168 个 `验证场景*.json` 的目标/前置/清理步骤能力 id，与注册表 **664** 求差 |
| 7 | **「能力面 830」口径不可用** | 按 `包声明.json` rglob 累加会因父包重复登记**虚高到 830**；正确口径 = 装配后 **664**（或经 40007 网关 `/健康`）。引用能力总数一律用**装配口径**，不用 rglob 累加值 |

### 8.6 新增债务（2026-09-17 晚，全量精校现场实测）

| # | 缺陷 | 现场证据 | 期望修法 |
|---|---|---|---|
| 1 | **网关重启遗留模型孤儿 → wired 显存泄漏**（真缺陷，已在调用方侧兜底） | 实测因果链：网关被 SIGTERM 后它启动的 `llama-server` 变孤儿（PPID=1），llama.cpp「睡眠」只卸权重、**进程仍攥约 6.5GB wired 显存**（`ps` 里 RSS 显示 0，极易漏看）；网关重启后内存中的 `全局模型索引`/`本地进程表` 随进程消失，下次 `连接LLM` 认为是无实例 → 再起一个，旧孤儿无人认领。当晚 5 次重启攒 **4 个孤儿 = 26GB wired**，把模型自身权重挤进 swap，生成速度 **20+ → 8.6 tok/s**（单场 7 分钟 → 30 分钟）。取证：`ps -o pid,ppid,command \| grep llama-server`（看 PPID=1）、杀前杀后 `vm_stat` wired 39.2→12.8GB、`sysctl vm.swapusage` 35.4→21.7GB | 网关启动时**按状态机登记的归属回收**上一代遗留的模型子进程：句柄系统已落库 `登记资源(句柄id, 资源类型="进程", PID=..., 端口=...)`，取「资源类型=进程 且 所有者=本地模型-*」的旧记录，校验 PID 存活且**其父进程已不是本进程**后按归属杀掉。判据必须是「归属+世代」而非端口号（embedding/rerank 等合法长驻服务父进程同样是 1，按端口盲杀会误杀） |
| 2 | **`--sleep-idle-seconds 300` 在本机是负收益** | `模型连接器.py:681` 启动命令固定带该参数。本机实测：睡眠只省下权重（可重载），**但保留进程与 Metal 上下文（wired 不可回收）**，而本机 wired 一旦吃紧就直接把权重挤进 swap → 越睡越慢 | 要么把「睡眠」改成「到期真退出」（配合下次按需冷启，冷启成本 ~30-60s 远小于持续换页），要么由 #1 的归属回收兜住孤儿；不改默认行为前，先用调用方兜底 |

**调用方侧已落防护（不是终点）**：`批量精校_全量.py` 的看门狗每轮巡检先跑「孤儿模型回收」，判据三重 ——
① 进程名 `llama-server`；② 命令行含本任务裁决模型路径；③ **父进程为 1**。三条同时满足才杀（实测回收 4 个）。

**「黑盒红但进程内跑通」的定性判据（2026-09-17 实证，与第 4 条并读）**：先读错误说明原文 ——
若是「环境/前置未就绪」类（例：`no such table: 激活指针`），要问「**这个能力的设计语义包不包含这种情形**」：
本平台 `平台控制面.提供者.只读激活指针` 的语义明确含「控制面不可用 / **未命中如实返回**」，
故那是**真缺陷**（已修，判据：只把 `no such table` 归为未命中，其它 sqlite 错误照旧上抛，
不放宽「存储目录不可用」边界）；而「环境依赖未就绪」（MLXWhisper 模型路径）才是第 4 条的环境类，**别改代码**。

