# 系统工程平台 · 未完成事项（唯一债务清单）

> 本文件是当前唯一未完成事项入口。本轮只记录已经用源码、契约和真实命令核实的问题；修复后必须重新验证并立即删除对应条目。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/底座升级方案.md`。

## 当前状态

- 正式索引：92 个正式包（含支持库/模块），408 项能力 owner 唯一，能力冲突 0，验证场景 v1 全部绑定。
- 正式源码语法：524 个正式 Python 文件全部通过 `python3.14 -m py_compile`。
- 发布状态：**通过**（2026-09-03 01:47）。当前激活制品 `平台客户端-64d65315cde2c1b0` 绑定提交 `9be74da`；
  32 并发完整 HTML 黑盒 408/408 能力、545/545 步骤通过，清理失败 0、资源残留 0；发布门禁退出码 0。

## 阻断事项

（无。当前无未修复阻断项。）

## 重要事项

- **P2（多租户时升 P1）任务查询缺少归属校验**：`运行核心/任务调度/任务系统.py:197-208` 按任务 id 直接返回，网关未将凭证主体绑定到项目/用户上下文；单机部署影响有限。
- **P2 依赖防火墙存在静默漏检**：`运行核心/依赖防火墙.py:160-170` 对特定字符串路径跳过扫描，`SyntaxError/OSError` 直接 `continue`，动态导入检查未覆盖 `importlib.import_module` 属性调用。
- **P2 文档规则冲突**：`开发文档/项目说明.md:169` 仍写 MCP 连接失败即阻断，与 `AGENTS.md` 已允许直接 HTML 只读审计的规则不一致。
- **P2 代码规模债务**：正式源码 129 个文件超 160 行（最大：发布门禁 1445 行、权威状态 936 行、项目服务 828 行）；属维护性债务，按需拆分，不凑数字。
- **P2 媒体处理 FFmpeg 场景偶发抖动**：`媒体处理.本机真实彩色音视频` 偶现 `No such process`（子进程竞态），重跑通过；属场景层时序问题，观察是否复发。

## 已核实通过（非债务）

- A批次资源边界与资源治理全部收口：裸 communicate 受限通信、有界 HTTP 服务、JSONL 有界轮转、流式 payload 预算、句柄/降级/回滚表上限、psutil 缺失拒绝、结束回调异常可观测。
- 权威状态锁身份键回退修复（提交资源与获取锁/续期/释放一致），HTML 黑盒 简单窗口/资源管理 场景通过。
- 资源与任务清理：历史挂起任务关闭重组、残留端口/进程清理、清理失败证据保留。
- 发布闭环：当前 HEAD 制品重新编译入库签名激活、lineage 绑定、HTML 黑盒 408/408、发布门禁通过。

## 维护规则

- 新问题必须先读取当前代码和测试，写失败回归测试，再做最小修复。
- 修复后必须跑定向测试、受影响波次和必要的真实 HTTP 验证；没有证据不得删除条目。
- 已修复条目立即从本文件删除，不保留历史债务流水。
- 历史方案、旧测试统计和已删除文档不回填本文件；历史由 Git 提交保留。
