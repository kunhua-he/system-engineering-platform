# 系统工程平台 · 未完成事项（唯一债务清单）

> 最后更新：2026-09-18 17:1x（逐条现场重核；#40 已修销项、#13 真源判据实测转绿；已闭合条目按口径删除）
> 口径：实测（数字随建包与提交逐日变化，引用前必须重跑取证命令）
> 维护者：唯一债务清单
>
> **当前真实账目（2026-09-18 下午实测，按节统计；已闭合条目已删，不留流水账）**：
> - §8.1 等条件：**0**
> - §8.2 待你裁决：**0** —— ★ 三条已于 2026-09-18 全体裁完（决策 `0036`），执行方案已落盘，代码待峰值期后推进
>   ★ **§8.2.1 另有 5 条**（2026-09-18 晚，外部素材触发）：**编号 50 已销项**（探针已实现并验收），51–54 待裁决 ——
>   子方案 `方案/模型探针与动态路由方案_20260918.md`（**探针部分已落地**）；已废弃件 `方案/本机推理端点自动发现方案_20260918.md`（扫端口方向），
>   按 `规范/方案文档生命周期规范.md#4.5` 同批登记
> - §8.3 待修：**6**（说明书施工表剩 4 格 / 3 条激活指针能力 / 构建链调用面 / B 类混血包行级合并债
>   / **38 包扫描事实源分叉** / **39 结构债 R5 剩余两条**）
>   —— ★ **本轮销项 2 条**：`40 能力id冻结基线口径漂移`（**2026-09-18 二次销项**：基线随新增能力重建 **686→689**，`测试_能力id冻结基线门禁` 13/13 绿、门禁仍绿；提交 `c40f4506`）；
>   `13 C3 关键值口径`（真源 `正向断言有实断言()` 重算，**零断言 0 成立**；★数字类只见口径与取证命令，绝对值随并发建包逐分钟变化，不再写死）。
>   ★ 口径教训：只扫 `验证场景/*.json` 会漏掉内联在 `验证场景引用.json` 里的场景 —— 必须调 `场景加载._解析场景引用`）
> - §8.4 待执行大项：**3**（D-1 适配层唯一化 / E-8 类型体系 / D 系列重构）
> - §8.5 已知覆盖缺口：**4** —— ★ 是「口径提示」不是债务（环境依赖类 / 旧口径作废 158 / 记忆支持库跑真实库 / 能力面 830），
>   **保留目的是防后人误判，不需要「修」**
> - §8.6 新增债务：**1**（网关重启遗留模型孤儿）
>
> **∴ 真正待处置 = §8.3(6) + §8.4(3) + §8.6(1) + §8.7(21) = 31 条**；§8.5 的 4 条不计入债务。
> ★ §8.7 是 2026-09-18 二次审计新增，其中 41–49 正在并发维修、57–61 只落决策不动代码。
> ★ **另有 3 条已裁决的执行大项**（决策 `0036`，见 §8.2），不在上表计数内 —— 它们是**已定方向的施工**，不是待议债务。

> 本文件是当前唯一未完成事项入口。**§八 全量未完成清单是唯一入口**（按「你需要做什么」分类，不按审计批次；**已修复项不留流水账，直接删除**）。
> **历轮取证与历史已归档**：`开发文档/归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/取证与历史.md`
> （外部审计逐条核验的取证过程与各轮「已修/已闭合」对照表；**不作为派活依据**，历史由 Git 与归档保留）。
> 当前事实见 `开发文档/项目说明.md`，执行顺序见 `开发文档/方案/执行编排_唯一入口_20260918.md`，
> 多会话并行协调见 `开发文档/规范/多会话并行开发规约.md`，提速与重启纪律见 `开发文档/方案/提速方案.md`。

---

## 八、全量未完成清单（按「处置方式」分类，2026-09-17 晚梳理）

> 本清单是**唯一入口**：按「你需要做什么」分类，不按审计批次。
> （原文件上方的 A–I 段与历轮收口已归档，见抬头；历史由 Git 与归档保留，**不作为派活依据**。）

### 8.1 等条件（不需你决策，等外部）

当前无。

### 8.2 待你裁决

**当前无 —— 已于 2026-09-18 全体裁完（决策记录 `0036`）**：

| 原 # | 事项 | 裁决 |
|---|---|---|
| 20 | 能力 id 形态口径 | **全仓统一为 `<包id>.<能力名>`**；不保留旧 id 别名（不要第二条腿） |
| 22 | 7 件架构拓展 + 2 条边界预留 | **全做** |
| 23 | 卡点 D：样本＝接口义务 vs 放未来 | **做掉**（按哲学 12.5，不推迟） |

**取舍标准（本次明确，取代「省成本 / 判断收益」）**：完善 · 高效运转 · 单腿运行 · 完全可控 · 大量兼容 · 简单易用。
**执行方案**：`方案/能力id全仓统一方案_20260918.md`、`方案/架构拓展7加2落地方案_20260918.md`、
`方案/故障注入样本落地方案_20260918.md`、`方案/执行编排_唯一入口_20260918.md`（**唯一编排入口**）。
**边界**：本轮只落 MD，代码待峰值期（约 18:00）结束后按总执行方案高并发推进。

#### 8.2.1 新增待裁决（2026-09-18 晚，外部素材触发）

> 立项依据：`开发文档/分析/DGX-Spark本地算力视频_识别与底座价值评估_20260918.md`
> 需求口径：华哥 2026-09-18 晚口述（**探针优先；用什么模型本质由用户配置**）
> 子方案：`开发文档/方案/模型探针与动态路由方案_20260918.md`（状态：起草）
> 已废弃件：`开发文档/方案/本机推理端点自动发现方案_20260918.md`（扫端口方向，**不作为派活依据**）
> 按 `规范/方案文档生命周期规范.md#4.5` 同批登记，防「子方案写了待办、债务清单没登记」的隐形债。

| # | 事项 | 现场取证（2026-09-18 20:00–21:0x 实测） | 建议 |
|---|---|---|---|
| 50 | ~~**模型探针**（P0）：新增 `大语言模型支持库.模型连接器.探测模型端点`~~ ✅ **2026-09-18 晚已落地并验收（开工 ID `探针簇`）** | 底座现有 **22 处探针惯例**（`检查提供者`/`检查可用性`/`版本探针`）**全是探本机工具**，**无一处探模型端点**；实测暴露三个难点：① llama-server 宽容接受任意路径（三协议全 200）⇒ 判据必须看**响应体特征字段**（`choices`/`output`/`content+type`）；② 根路径不一定是 `/v1`（8328 的 `/v1/chat/completions` 是 404）；③ **HTTP 头只允许 latin-1**，非 ASCII key 曾误报「连接失败」（已改为参数校验拒绝）。 | **已完成**。验收取证：定向单测 **15/15 绿**（含夹具与反向）；真实 HTTP 经 40007 **13/13 通过**；包级摘要能力数 22 / 文件 23，父包级联一致；真实命中实测（8328 `/hermes-chat/v1`）自动取模型 → 命中 `chat_completions` → 回带 `deepseek-v4.1-flash` |
| 51 | ~~**动态路由**（P1）：`按任务选模型`~~ ✅ **2026-09-18 晚已落地并验收（开工 ID `路由簇`）**——华哥口径「可以新增一个原子能力，可以复用之前开发过的」，实现为 `选择模型`（复用 `探测模型端点` 做可达性硬门槛） | 华哥口径：策划→GPT/Claude，日常→DeepSeek，清洗/私有→本地，生图按难易分流；<br>实测华哥判断成立：本地**无常驻**（端口十分钟变两轮）、**窗口仅 32768**（100K 请求实测报 `exceed_context_size_error`） | **已完成**。★ 关键设计：**默认保守模式（零额度）**，用户显式开 `启用智能路由` 才走统计打分；打分四维（成功率/效率/性价比/匹配度），基准取候选集合内中位数；**样本不足不给分只如实标注** |
| 52 | ~~**候选清单来源**~~ ✅ 已按华哥口径落地 | 华哥口径：「用什么模型，本质上是用户去配置的」 | **已完成**：候选清单由调用方传，底座不存台账；`选择模型` 的 `候选清单` 为必填 |
| 53 | ~~**多矩阵 / 跨机算力池**~~ ⏸ **仍未做（口径不变）** | 华哥：「就这台电脑」「**无法验证**」；`项目说明.md` 明文「分布式不在范围内」 | **不做**（若将来有验证办法再议） |
| 55 | **模型用量记录与打分**（2026-09-18 晚新增） | 华哥口径：路由要「加上算法」，用户历史用过的所有模型都要打分、要能给「哪个模型适合什么任务、性价比、效率最高」<br>★ **现场核实燃料为空**：`会话存储.db` 会话表 0 行、消息表 0 行、成本统计非空 0 行；运行态六域**无「模型用量」域**；全底座搜「调用记录/用量统计/评分」**0 条能力** | ✅ **已落地**（开工 ID `路由簇`）：新增 `记录模型调用`（燃料入口，不耗额度）+ `汇总模型表现`（纯统计，不耗额度）；存本包专用库 `大语言模型支持库.模型用量.db`（照会话存储/嵌入缓存先例，**不动他人包**） |
| 56 | **「额度说明」必须备注给用户**（2026-09-18 晚新增） | 华哥：「这个会后台消耗 LLM 额度，需要给用户备注上」 | ✅ **已落地**：`选择模型` 返回固定带 `额度说明`，**默认开启**（实测坑：`if not 启用额度标注` 会让默认 None 判假、说明恒为空，已修）；只有显式传假才清空 |
| 57 | **用户自带规则的「参考路径」**（2026-09-18 晚新增） | 华哥：「底座不涉及隐私的东西。但是可以给用户一个选择，让用户输入一个数据库的绝对路径或者是什么样的文件的绝对路径或者文件夹等…也算是一个选项」+「整个底座框架，是不做隐私，安全等相关的问题。因为当限制越多，底座本身的兼容性就越差」 | ✅ **已落地**：`选择模型.参考路径`（选填，**不给=完全不涉及**）。★ 底座**不定义「什么是敏感」**——只把文件里的词原样取出当匹配模式；判据：换成普通品牌词表机制同样可用（证明是通用外部规则输入，非隐私专用件）。与哲学 9.3 逐字一致 |
| 54 | **隐私路由** | 哲学第 9 条「0 业务假设」；9.3「隐私是业务端的事」 | **不进底座**（归业务端，如 V3） |



### 8.3 待修（修法已明确，可直接派活）

| # | 事项 | 规模 |
|---|---|---|
| — | *（编号不复用：原第 8 项 D-7/D-9/D-17② 已拆解销项）* | **D-7 已处置**：40006 plist 已改名 `com.huashi.gateway-40006.plist.已停用_目标文件已删除`（实测）；**D-9 已闭**：README 已于 `7ebece38` 重写，`grep -c '技能库/运维脚本' README.md` = **0**（2026-09-18 复测）；**D-17② 已拆为 §8.3 第 34 项**（3 条激活指针能力立项） |
| 23 | **说明书存量治理施工表**（剩 **G3/G4/G5/G9** 四格） | 中。已完成 G1/G2/G6/G8/G10/G11/G12（白名单 41 条、6 个聚合父包重生成可复现 6/6、联动门禁已接线）；**剩四格是「行级合并」——不可自动化，需华哥逐行确认**（保手写行、刷生成行）。禁止对含实质漂移的包裸重生成 |
| 34 | **3 条激活指针能力立项** ⏸ **本轮实做后撤回（2026-09-18），已定清落点** | **规格齐备**：`方案/客户端构建能力化落地方案_20260917.md` 已给出三条能力的参数/返回/错误码（`诊断`/`重建`/`重置平台客户端激活指针`）。
**卡点（已定位，非推断）**：验证器硬约束「正式公开能力全集 == 正向目标步骤能力全集」⇒ `重建` 必须有正向成功场景；
但 `重建` 的成功前提是**存在已安装副本**，而场景只能用受管临时目录。两条实测事实：
- `安装制品` 的 `目标目录` 含**包层**，而 `重建` 读 `<环境目录>/平台客户端` —— **路径语义差一层**；
- `安装制品` 要求制品**已在状态库入库**（只放磁盘目录报 `制品不存在`）。
当前状态：`诊断`/`重置` 已有正向场景并通过验证器；`重建` 缺正向目标步骤。本轮试写的两版场景已**全部撤回**
（不把「为过门禁而写的假场景」留进仓库），本项代码改动亦撤回，撤回后验证器回到退出码 0。
**下一步（需先裁定口径）**：确认「`安装制品.目标目录` 与 激活指针 `环境目录` 是否应同一口径」——裁定后补 `重建` 正向场景即可（工序很小）。规格见 `方案/客户端构建能力化落地方案_20260917.md` |
| 36 | **构建链调用面未收口** | 小-中。`客户端/构建平台客户端.py` 全部**直调 `接入.X()` 实现方法**而非能力 id（★行号随改动漂移，引用前用 `grep -n '接入\.' 客户端/构建平台客户端.py` 现场取，勿抄旧行号）；`可复现构建器` 在客户端 **0 引用**。含 4 条重叠结论：`构建制品`／`查询制品布局` **真重复 → 勿再开同名能力**；`校验可重复构建` **真缺口**；`安装制品` 语义重复待裁。并入时同步扩 C-11 覆盖面 |
| 37 | **说明书：B 类 6 混血包的 99 行手写区行级合并债** | 中。将来重生成这 6 包**必须先做行级合并**（保手写行、刷生成行）—— 白名单是**文件级、不是段落级**。★ 术语定义：**生成物类**＝含生成器签名句、**人工类**＝不含；血统判据与禁令见 `规范/新建支持库与模块指南.md` 说明书治理节 |
| 38 | **包扫描事实源分叉**（★ 2026-09-18 从被取代的 `底座升级方案.md` 抢救登记） | 两套扫描器口径仍不同（**现场核实：二者均未改**）：
`运行核心/加载器/包发现/发现器.py:32-56` 按「聚合库名表 + `能力定义.json` 是否存在」跳过子域；
`开发工具/项目编译/正式包索引.py:44-105` 按「非生产路径片段 + 非生产字段 + 状态字段」判定。
**后果**：运行发现与正式索引的包/能力数长期不一致（曾实测 127 声明 vs 109+18+1）。
**验收判据**：两处扫描结果对同一系统根**逐包一致**，或显式声明「唯一扫描事实源、另一处只读它的产物」。
★ 同源：G8 真因（发现器连聚合库自身也跳过）已修，但**口径分叉本体未收口** |
| 39 | **结构债 R5 剩余两条** | ✅ **2026-09-18 已闭**：① `支持库/核心/` 空壳（含 `__pycache__`）已删（全仓零代码引用，提交 `c40f4506`）；② `底座升级方案.md` 的 4 处死路径 `支持库/核心/类型转换` 已改为真路径 `支持库/后端/数据操作支持库/类型转换`，另 2 处裸前缀改为明确标注的历史说明（原条目写「6 处」是按裸前缀计，精确路径只有 4 处）。R5 判据「命中数＝0」现成立 |
| — | *（编号不复用：原 39 条的两项均已闭，过程已并入 git）* |
### 8.3.1 派活前必须现场复核条目（2026-09-18 新增，来自一次真实浪费）

> **教训**：按清单原样派出 10 路并行维修，其中一路（原第 44 项「3 条错误码补登记」）**是过时条目**——
> 现场实测三个码早已登记在案（`网关核心.py:196/202/217` + `本地网关.py:86/89/95`，且带批次注释），
> 子代理到场无活可干，只能在「到底改没改」上反复推敲，白费一轮。
>
> **判据（此后一律照做）**：从本清单派活前，**先用提交版本核验条目前提是否仍成立**
> （`git show HEAD:<文件>`，不要读工作树——并行维修时工作树是中间态），前提已不成立的条目**当场删掉**；
> 前提成立的，把**复核时点与核验命令**写进任务信再派。清单是「待办线索」不是「事实」，
> 字面照抄派活＝派空任务。


> **2026-09-18 主会话登记**：第 34 项（激活指针三能力立项）与第 36 项（构建链调用面未收口）
> 涉及 `平台控制面/包仓库/平台客户端制品.py`（963 行）与 `客户端/构建平台客户端.py`（868 行），
> 这两份正被超长文件拆分路占用。**拆分完成后再派**，避免同一文件两路并行。

### 8.3.2 并行维修期禁止 `git add -A`（2026-09-18 新增，来自一次真实污染）

> **教训**：我用 `git add -A` 提交时，**子代理的反向验证正在跑**，其**刻意写入的变异值**
> （`动态库提供者.调用动态库` 的 `0.0.0-篡改`、`平台状态.写入记录` 的 `记录.状态=已被篡改的状态`）
> 连同其正常产出一起被提交进 HEAD。子代理发现「变异值进了 HEAD」后停下反向验证来修，
> 主会话同步修正（`7a26da90`）。**代价：一次假红被短暂固化成"已验"状态 + 一路子代理中断本业。**
>
> **判据（此后一律照做）**：
> ① **提交一律用精确路径 `git add <文件>…`，禁用 `git add -A` / `git add .`**（并行维修期尤其）；
> ② 提交前 `git status --short` 核对清单 —— **出现不属于本次任务的条目就停手**，先问清来源；
> ③ **反向验证期间变异值就在工作区**，与最终值只差一步还原；父会话此时提交 = **把假红固化成"已验"**。
>    正确姿势：**等子代理报「已还原」再提交**，或提交前 `git diff --cached` 逐文件确认无
>    `篡改` / `反向样本` / `临时` 字样；
> ④ 同一理由：**共享制品仓库（`工程缓存/制品仓库/`）在并行期也会竞态** ——
>    本轮主会话与子代理同时跑构建，连续 3 次构建失败（`制品副本不一致` / `制品目录缺失`）。
>    并行期**构建与黑盒验证应串行**，或各用独立制品目录。


### 8.4 待执行的大项（方案已出，可直接排期）

> **编号不复用**：本节编号为 1/3/7，缺 2、4–6（历史条目已闭并删除，编号不再复用）。

| # | 大项 | 方案落点 | 规模 |
|---|---|---|---|
| 1 | **D-1 适配层唯一化** | `开发文档/方案/适配层唯一化迁移清单_20260917.md`（1980 行 + 附表）：真调用点 57 条/17 文件、消费方 14 包/去重 50 文件 | 大 |
| 3 | **E-8 类型体系收口** | `开发文档/方案/类型体系收口盘点_20260917.md`：16 型 6 有实/7 半实/3 零消费；归一表实测 16 张 | 大 |

| 7 | **D-6 同名文件+同名包合并 / D-5 同名跨层归位 / D-14 开发工具根平铺** | 依 1.1「能解决就按唯一链路修到底、解决不了就重构」+ 2.1 + 1.3；属大重构，单独立项排期 | 大 |
### 8.5 已知覆盖缺口（不是缺陷，是「还没验到」）

| # | 缺口 | 说明 |
|---|---|---|
| — | *（编号不复用：原第 3 项「H 段历史红」已随归档销项）* | H 段（需华哥裁决）与 §十/§十一 外部审计逐条核验段已归档到 `归档/202609_未完成事项_取证与历史（A–I 段与历轮收口）/`；表中残留项多已修复，**引用前必须重跑**（哲学 12.4⑤），归档件**不作为派活依据** |
| 4 | **环境/运行态依赖类（非缺陷，别去修代码）** |
**★ 实测判据（2026-09-18）**：转写/媒体类在**并发 ≥2** 下会因**多实例同时加载同一份 3GB 模型权重互抢**而失败，
报错现象是「统一返回缺字段 `['值','请求id','耗时毫秒']`」（**现象误导：像返回契约缺陷，实为环境并发**）。
处置：`_场景资源键` 加第 8 个键「语音转写模型」令其独占同实例串行；并发 8 跑全量验证器退出码 0（原 8 项红全消解）。
**通用判据**：若顶层红突然从个位数跳到 20+，且失败模式是「统一返回缺字段」或「500」＋连带的「覆盖对账缺少 N 个能力」，
先怀疑**自己动了运行态/环境**（清理了 `工程缓存/平台控制面`、`运行数据`、`制品仓库/平台客户端状态`、`平台客户端环境`），**不要按代码缺陷去修**。
| 6 | **旧口径作废：曾报「158 条零场景能力 / 23.8% 未覆盖」** | 实测**零场景 = 0 条**。旧值系算法漏读（只统计「场景内嵌在 验证场景引用.json 里」的情形，漏了 83 份外部场景文件 `验证场景/*.json`）。**再见到 158 / 23.8% 一律按作废处理**。取证：遍历全仓 168 个 `验证场景*.json` 的目标/前置/清理步骤能力 id，与注册表 **664** 求差 |
| 6b | **记忆支持库的验证场景跑在**真实生产库**上（读类能力值无法定值）** | **2026-09-18 实测发现**：`记忆支持库` 的场景（`完整生命周期正向HTTP` / `项目维度与仅关键词正向HTTP` 等）**不带 `存储目录` 参数**（该包能力契约里确实没有这个参数），故真跑 `列出记忆` 返回的是**真实生产数据**（实测读到 `华世王镞v3--部署流程`、`合规测试`、`新记忆` 三条真实记忆）。
**两个后果**：
① **11 步缺关键值中的 10 步是「读类」**（`搜索记忆` / `最近记忆` / `列出记忆`）—— 返回值取决于**真实库当时的内容**（数量、条目、时间戳每次都不同）⇒ **确实无法定值**，本轮只补了可稳定定值的 `生成标识`（`{"$": "r2-场景契约维修记忆"}`）。
② **隐患（待裁）**：验证场景读写**真实生产记忆库** —— 若某天某步写坏或删错，会**动到生产数据**。建议：给 `记忆支持库` 的能力加可选 `存储目录` 参数（与 `资源管理` / `项目文档` 等包同款），场景改用受管临时目录；**但这是对外契约面变更（新增参数），须裁决**。
**当前不构成缺陷**：验证器全绿（681 能力 / 1047 步），且实测该包能力对真实库只做读/写单条测试数据。 |
| 7 | **「能力面 830」口径不可用** | 按 `包声明.json` rglob 累加会因父包重复登记**虚高到 830**；正确口径 = 装配后 **664**（或经 40007 网关 `/健康`）。引用能力总数一律用**装配口径**，不用 rglob 累加值 |


### 8.7 二次审计（2026-09-18 外部独立审计）落地

> 审计报告：`/Users/hekunhua/Doubao/chats/2026-09-17/new-chat-2/系统工程平台_二次全新审计_20260918.md`
> **逐条证真 + 文件冲突矩阵 + 波次编排**：`方案/二次审计落地编排_20260918.md`（本批唯一编排入口）
> ★ 修法已定位、8 路并发维修**已派出**（2026-09-18 18:0x）；本清单只登记**尚未闭合**的部分

| # | 事项 | 状态 |
|---|---|---|
| 41 | **P0-1 跨重启世代账本 + 启动 reaper**（常驻提供者 setsid 脱离后网关重启即孤儿、占显存） | ✅ 已提交 `8ceaed5b` |
| 42 | **P0-2 超时排空双读者竞态**（`select+readline` vs 后台 `os.read` 同管道） | ✅ 已提交 `8ceaed5b`（与 41 同单） |
| 43 | **P1-3/P1-4/P2-12 执行形态分流**（每调用一线程、3 秒超时杀进程、两条装配腿） | ✅ 已提交 `9c7d9b59`（验收路收口；P2-12 那条反验未变红，**根因已查明属既有保护**，见回传） |
| 44 | **P1-5 任务族持锁做慢 I/O**（池锁内 join+fsync，异常路径毛刺） | ✅ 已提交 `cf19ddc5`（父会话补提交，19 测试真跑） |
| 45 | **P1-6 文案当机器判据（实测 8 处）** | ✅ 网关侧已提交（`76ec667e`/`e79f5954`）；句柄侧随 `e79f5954` 入库；哲学 **14.4** 已立规 |
| 46 | **P2-7/8/10 状态层硬化**（租约不抢占 / CAS busy 误归类 / 标识符白名单 / PRAGMA 循环外提 / 单事务 UPSERT） | ✅ 已提交 `84b1b4cc` + `be2de855` |
| 47 | **B5 幂等表纯内存重启失忆** + **B3 缺就绪门** | ✅ 已提交 `e79f5954`（B5）/ `14730490`（B3）；**父会话亲测**：46 项 0 失败、`/就绪` 未就绪真返 503、`/健康` 未动 |
| 48 | **B2 无性能基准**（速度只能靠感觉争论） | ✅ 已提交 `adbef399` / `4b2e9ddd` / `091174c5` |
| 49 | **§11 零配置自适应**（硬件画像 → 离散档位 → 默认参数；首版档位标定成与现值一致、零行为变化） | ✅ 已提交 `45991985` |
| 50 | **B4 缺优雅停机** | ✅ **已接线并提交**（`b67a88fe` 模块本体 + `d10a3c16` 接线 + `90f6b136` 命名清理）。**2026-09-18 夜现场复核实测**：`运行核心/启动运行核心网关.py:106-273` 已装信号处理器（`安装停机信号处理`），第 276 行仍是兜底空转；`停机编排.py` 566 行已入库。实盘：18.11 秒在途请求被优雅排空，退出码 0 |
| 51 | **B6 合盖睡眠/时钟跳变未重校准** | ✅ **已提交 `26234d4e`**（唤醒重判 + 心跳单调时基）。**2026-09-18 夜现场复核**：`运行核心/权威状态.py:811` 起「唤醒重校准（审计 §9-B6）」段落已在 HEAD |
| 52 | **B7 有备份无恢复演练（RPO/RTO）** | ✅ 已提交 `3e77becd` + `4bc164ef`。**父会话亲测**：RTO **0.0129s**、四类数据全绿、反向验证真红→真绿；顺带修「恢复覆盖已有库必假红」（1/3 → 3/3）。**残留缺口**：生产数据根四类缺两类（`制品/`、`项目锁.json`），四类权威落点不同根，须先裁定 |
| 53 | **B8 端到端背压是否协同需核对** | ✅ 已提交 `61668d13`（198 锚点复算 0 缺失）。**核出 13 条不协同**，下游修复方案见 `方案/端到端背压修复方案_20260918.md`（分 4 批）；最重 = `资源繁忙` 未登记两表穿成 **500**（父会话实测：两表 0 命中 vs `限流` 8/15） |
| 54 | **B9 模型二进制的供应链** | ✅ **已提交 `734913b6`**（`模型连接器/实现/模型供应链校验.py` 412 行 + `数据/模型制品清单.json` 56 条实测条目；判定顺序 存在性→字节数→sha256）。**2026-09-18 夜现场复核**：提交内容与清单标「维修中」不符，已按现场修正 |
| 55 | **B1 拆上帝文件**（★ **HEAD 实测 12 个 >1000 行**，不只原审计的 8 个；8 个核心文件合计 **7930 行**） | ⏳ **必须最后单独一波**；执行方案 + 安全网已就绪：`方案/拆上帝文件执行方案_20260918.md` + `开发工具/门面核验.py`（AST 比对对外门面，四类弄坏全抓到）。**★ 2026-09-18 全仓现状审计补报 4 个漏项**（不在原 8 个名单里）：`开发工具/发布门禁/运行发布门禁.py` 3268 / `开发工具/契约编译/漂移检测.py` 2210 / `开发工具/公开调用完整性门禁.py` 1544 / `开发工具/环境自检.py` 1121 —— 全在 `开发工具/` 下，与 A–F 簇零冲突，**可先于 8 个核心文件单独做一轮** |
| 56 | **§12-E1 40007 从「跑源码树」切到「跑激活制品」**（实测 plist 确认在跑源码；制品内已有服务入口） | ⏳ 主会话亲自做，**须等源码树干净 + 门禁通过之后** |
| 57 | **§8 多租户数据面**（策略端口 + 租户列 + fail-closed 启动自检） | 📋 已落决策 **0042**，代码另批 |
| 58 | **§9 成熟度 L4**（可观测/备份演练/容量基线） | 📋 已落决策 **0043** |
| 59 | **§10 语言选型与容器边界** | 📋 已落决策 **0044**（结论：主干不换栈） |
| 60 | **§11 零配置自适应** | 🔧 见 49；决策 **0045** |
| 61 | **§12 开发槽/稳定槽分离** | 📋 已落决策 **0046**；E1 见 56 |

**处置原则（本批）**：41–54 **已全部提交**（父会话逐条现场验收，含亲测关键断言）；
53 已出下游方案、55 最后单独一波；56 主会话亲自做且须等门禁通过；
57–59、61 只落决策不动代码（架构级，需另批）。

**★ P2-13 现场核实（`55e6eb7c`）带出的三处新活**：
| # | 事项 | 状态 |
|---|---|---|
| 62 | **P2-13② 第二份同构副本**：`平台控制面/提供者/进程提供者.py` 资源键表满 4096 **无淘汰**（审计只报了 `独立进程.py`，报告漏了这份） | ✅ **已提交 `43c7ce07`**（OrderedDict + move_to_end + popitem 淘汰，删除拒绝分支；24 新键被拒 0 次；反验三拍真红真绿）。★ **主实现 `独立进程.py` 当时根本没修过**（R 路现场核实：`grep -c 淘汰` = 0，从 `d290c4a3` 创建至今一字未改）→ **主实现已由主会话亲自修完并提交 `134b081a`**（派出的 `deleg_ba773e1e` 中断无产出；同一现象两次失败，按哲学 3.1 转主会话直接做）：OrderedDict + move_to_end + popitem 淘汰、删拒绝分支；定向 6/6（满表 24 新键被拒 0 次、真 LRU、淘汰键重进同一成员对象、200 轮成员表不动）；反验三拍真红真绿；回归 34 例全过。★ **「抽唯一实现」未做**（两份仍是同构实现 = 哲学 1.2 待收口，独立债务） |
| 63 | **P2-13③ file: URI 未编码**：`运行核心/权威状态.py:1338` 路径含 `#`/`?` 时 `?mode=ro` 被吞 ⇒ **只读失效可写** + 截断连空库 `integrity_check` 仍 `ok` **掩盖真实损坏**（报告口径「中文路径」是错的） | ✅ **已提交 `b5b06843`**（建唯一构造口径 `只读库URI()`，用 `Path.resolve().as_uri()` 而非 `quote()`——标准库现成能力且管规范化与 safe 字符；三路径 `?`/`#`/中文含空格 全绿 16/16；反验拍1 实测缺陷版把**真损坏**报成「数据库完整」）。★ **同类裸拼还在 9 处**（我只报了 6 处，主会话扫全 9 处）→ 已派 `deleg_80dcbb08` 批量收口到唯一口径 |
| 64 | **P2-13⑤ 独立任务身份判据靠隐式默认**：`任务进程.py:101` 无 `eq=False`，队列用 `==` 判身份；当前不误判但属**隐式依赖**（加 `@dataclass` 即静默失效） | ✅ **已提交 `4a1d0c7a` + `2923364e`**（类体显式绑定 `__eq__ = object.__eq__` / `__hash__ = object.__hash__`，**不选** `@数据类(相等=假)`——实测后者对无类级字段注解的类会生成「比空元组」的 `__eq__`，**任意两实例判等**，失效范围拉满；且显式绑定兼作护盾：stdlib 不覆盖类体已有 `__eq__`，挡住后来者叠 `@dataclass`）。反验四拍真红真绿（拍1a 实测 `独立任务() unexpectedly found in [独立任务()]` = 静默失效现场）；54 例回归全过 |
**★ 本轮新发现（2026-09-18 夜间，主会话现场取证）**：
| # | 事项 | 状态 |
|---|---|---|
| 65 | **检索能力三类真浪费**（华哥 2026-09-18 指出）：① 粒度不可选（要标题只给片段、要全文给不了，Agent 只能再读文件）；② 一次只试一组词（搜不到就换词重来，每次完整往返 1.8–2.7 秒）；③ 搜不到不留痕（同一个词下个会话/子代理再撞一次，教训不沉淀） | ✅ **已提交 `bcea8c51`**（14 文件 / +1334）：`查询索引` 加 返回粒度四档（标题/简介/内容/完整，默认档与改前 7 字段逐字段兼容）+ 搜索词（多组候选独立打分取最高）+ 截断上限；新增 `登记未命中词`/`列出未命中词` 两能力 + 零命中组自动扩词；新增 `实现/库连接.py` 抽公共层（两边互相 import 会循环，实测 `ImportError`）。反验三拍真红真绿；真装配 691 能力/跳过 0；契约漂移判红 0 |
| 66 | **`任务系统.py` 的 `任务` 是 `@dataclass` 按值比较**（N 路存疑项） | ✅ **已核实：不是缺陷**（2026-09-18 主会话现场判定）。判据：① `任务` 只在 `self.任务表: dict[str, 任务]` 里按 **任务id 字符串** 索引（`任务表[任务对象.任务id]`，全仓 12 处均如此），**从不做对象身份比较**；② 真正做身份判断的 `等待队列` 是 `list[独立任务]`（`任务进程.py:215`），用的是**另一个类**，且已在 `4a1d0c7a` 显式绑定身份语义；③ 实测 `任务(T1,X) == 任务(T1,X)` 为 True（确按值），但它不入任何 `in`/`remove`/`set`/`dict-key` 判身份路径，故按值比较无害 | 
| 67 | **发布门禁 5 项红灯存量债**（主会话逐个定性：**与搜索升级无关，我的包零违规**）：裸布尔 21 条（真源 `可重试=True`，信封字段应用正式类型 `真`/`假`）、占位代称 77 条（`开发工具/性能基准/结果对比.py` 的旧天干代称）、文档生命周期 1 条、对外契约 2 包（组件规范支持库版本未递增 / 代码地图提供者基线缺失） | ⏳ **按 14.3 待治本**：裸布尔要全仓批量改 + 占位代称要清（旧代称违反哲学 6.2「禁用天干代称」） |
| 68 | **★ 子代理中断频发（2026-09-18 夜间 8 次「outcome unknown」）**：manifest 长期卡 `status=running`，但父会话实际已收到错误单；`deleg_6a97fc40`/`80dcbb08`/`ba773e1e`/`d0039f31` 等皆如此。**归因**：不是子代理没干活（多数有真实产出），而是**收尾阶段丢失终态回执**。**对策**（已用）：① 派活后按 manifest + 日志双取证判「真在跑 vs 已死」；② 有产出的路**直接派验收路收口**，不重跑原活；③ **同一现象两次失败即转主会话亲自做**（哲学 3.1，本轮 `独立进程.py` 已实证有效） | ⏳ 待查根因（Hermes delegation 层，非本项目代码） |
| 69 | ★ **优雅停机编排接线**（外部二次审计 B4） | ✅ **已接线**（`d10a3c16`）。**2026-09-18 夜现场复核实测**：`运行核心/启动运行核心网关.py:106-273` 装信号处理器；缺口②（plist ExitTimeOut）已写 60 并三层验收。**残留**：缺口①（`本地网关.py:439` 的 `优雅停止()` join 量纲仍是 1800 秒）已绕开未根治 —— 停机编排不走它，但直接拿它当停机用仍会踩坑，属独立小债（见下行） |
|  | **实测铁证**（python3.14 亲跑）：不装信号处理器 → SIGTERM 后**退出码 -15、atexit 完全不跑**；装处理器 → 退出码 0、atexit 正常执行。而 `运行核心/启动运行核心网关.py` **全文件 0 处 `signal.signal`**，第 105–107 行是裸 `while True: time.sleep(3600)` → 现状下 `launchctl kickstart -k` 时，第 54 行 `atexit.register(任务系统实例.关闭全部)` 与第 97 行 `atexit.register(账本维护.停止)` **都不执行**。 | |
|  | **缺口①（量纲错）**：`本地网关.py:439` 的 `优雅停止()` 里 `线程.join(timeout=max(1.0, self.请求超时秒))`，而启动时 `请求超时秒=1800` → join 最多等 **1800 秒**，与 launchd 默认 `ExitTimeOut` **20 秒**严重不匹配 → 第 20 秒被 SIGKILL，排空全白做。 | |
|  | **缺口②（plist 缺项）已修**：`ExitTimeOut` 原为**缺项**（系统默认 20 秒）→ 已写入 **60** 并 bootout/bootstrap 重载。三层验收：① plist 回读 = 60；② `launchctl print` 内存值 **`exit timeout = 60`**；③ 网关只读探测 `已配置退出超时()` 由 **假→真**。备份 `~/.hermes/config_backups/plist_gateway_40007_ExitTimeOut_20260918`。**实盘依据**：生产网关曾出现 **18.11 秒**在途请求（距旧缺省 20 秒仅差 1.89 秒）。 |
|  | **缺口①（量纲错）已绕开但未根治**：`本地网关.py:439` 的 `优雅停止()` 里 `线程.join(timeout=max(1.0, 请求超时秒))` 仍是 **1800 秒**量纲。停机编排的「停止收新请求」段**不复用**它（只关 listen socket + 停 serve_forever），`优雅停止()` 降为收尾项（此时监听已关、join 立即返回）。★ **若未来有人直接拿 `优雅停止()` 当停机用，仍会踩同一个量纲坑** → 建议后续改成可选超时参数（默认不变）。 | |
|  | **接线面全部齐备**（勿造新面）：`本地网关服务器.优雅停止()`（:439）｜`后端核心` 的 `活动请求数`(:49)/`停止标记`(:85)/`启用自动排空()`(:105)/`注册排空终止器()`(:123)｜`独立进程.清扫上一代常驻进程()`。 | |
|  | **模块本体已入库**：`运行核心/统一网关/停机编排.py`（566 行）提交 **`b67a88fe`**。 | |




**★ 提交纪律事故（2026-09-18 实测）**：C 簇自称「精确路径提交」，实际把 **F 簇的 4 个文件夹带**进 `e79f5954`（提交信息写 B5）。
根因：只 `git add` 自己文件却跑**不带 pathspec 的 `git commit`** ⇒ 索引是全局共享的。
已补底层：「提交三件套」注入子代理必读路径 + 规约 §9.1 立规 + 父会话验收加「看 `git show --stat` 文件数」判据。

### 8.6 新增债务（全量精校现场实测）

| # | 缺陷 | 现场证据 | 期望修法 |
|---|---|---|---|
| 1 | **网关重启遗留模型孤儿 → wired 显存泄漏**（真缺陷，已在调用方侧兜底） | 实测因果链：网关被 SIGTERM 后它启动的 `llama-server` 变孤儿（PPID=1），llama.cpp「睡眠」只卸权重、**进程仍攥约 6.5GB wired 显存**（`ps` 里 RSS 显示 0，极易漏看）；网关重启后内存中的 `全局模型索引`/`本地进程表` 随进程消失，下次 `连接LLM` 认为是无实例 → 再起一个，旧孤儿无人认领。当晚 5 次重启攒 **4 个孤儿 = 26GB wired**，把模型自身权重挤进 swap，生成速度 **20+ → 8.6 tok/s**（单场 7 分钟 → 30 分钟）。取证：`ps -o pid,ppid,command \

**★ 本轮实测复核（2026-09-18，含一处方案前提更正）**：
① **现场确有孤儿**：`ps -o pid,ppid,rss,command -ax | grep llama-server` 实拍到 `PID 1358 PPID=1`（bge-reranker-v2-m3，`--sleep-idle-seconds 300`）—— 即**已确认的遗留孤儿**；同批还有 `PID 35793 PPID=61453 RSS≈28GB`（Qwen3.6-27B，父进程健在，非孤儿）。
② **方案前提有一处不成立**（实测更正）：原方案写「句柄系统**已落库** `登记资源(句柄id, 资源类型="进程", PID=…)`，取旧记录按归属杀」——但实读 `公共契约/句柄体系.py` 确认**句柄体系是纯内存态**（有 `_淘汰失效句柄`/`失效`/`状态快照`，**无任何持久化与落库**；`登记资源` 写的是内存对象 `对象.绑定资源`）。⇒ 网关重启后**拿不到上一代的登记**，因此「按状态机登记的归属回收」这条路径**当前不可用**。
③ **为什么没顺手改（按哲学 14.3「补底层」的方向与 3.1「停手」的边界）**：要让这条成立，需要先给句柄体系**加持久化**（或另设「模型子进程世代账本」），再让网关启动时读它并按「归属 + 世代」判据杀——判据必须是归属而非端口号（现场第三条 `PID 59365 PPID=1` 是**合法的长驻 embedding 服务**，按端口盲杀会误杀它）。这是**底座级机制新增**，不是随手能改的一处缺陷。
④ **现状的兜底**（本条原描述即写明「已在调用方侧兜底」）：网关重启会先由调用方释放模型句柄；但**跨代孤儿（上一代网关启动的、下一代网关无记忆的进程）仍无人认领**，wired 泄漏风险仍在。
**下一步（需先裁定机制归属）**：是否新增「模型子进程世代账本」（落盘 PID + 启动时刻 + 归属标识，网关启动时按「归属匹配 + 父进程非本进程」回收）；或给句柄体系加持久化。两种都属底座机制新增，**须先定方向再动**。| grep llama-server`（看 PPID=1）、杀前杀后 `vm_stat` wired 39.2→12.8GB、`sysctl vm.swapusage` 35.4→21.7GB | 网关启动时**按状态机登记的归属回收**上一代遗留的模型子进程：句柄系统已落库 `登记资源(句柄id, 资源类型="进程", PID=..., 端口=...)`，取「资源类型=进程 且 所有者=本地模型-*」的旧记录，校验 PID 存活且**其父进程已不是本进程**后按归属杀掉。判据必须是「归属+世代」而非端口号（embedding/rerank 等合法长驻服务父进程同样是 1，按端口盲杀会误杀） |

### 8.7 模块库入口注册丢参「必填」→ 8 包 38 条能力必填校验静默失效（2026-09-18 已修）

> ★ **提交事实更正（2026-09-18 夜间）**：本条目正文先是**被并行兄弟会话 `ea6379c0` 夹带提交**
> （该提交只 `git add` 自己的文件却跑了不带 pathspec 的 `git commit`，索引全局共享 → 把我的
> `开发文档/未完成事项.md` 追加一并带走；**与本文件 §8.6 末尾已登记的那起提交纪律事故同一根因、
> 同一晚第二次复发**）。我自己的 8 个包 `__init__.py` + 8 份 `完整性摘要.json` 未被夹带，
> 由后续精确路径提交一并入库。

| # | 缺陷 | 真相 | 状态 |
|---|---|---|---|
| 70 | **模块库 `__init__.py` 注册参数时把契约的 `必填`/`默认值`/`说明` 全丢掉** | 见下 | ✅ **已修 8 包 / 38 条能力**（提交见本节末） |

**缺陷本质（不是风格问题，是生产级静默失效）**

- 现场：经 40007 网关调 `直播逐字稿.全自动精校` 不传参数 → **HTTP 500** `TypeError: 全自动精校() missing 4 required positional arguments: '源文件路径', '导出路径', '缓存目录', and '模式'`；而 `个人蒸馏支持库.查询上下文` 同样空参 → **HTTP 400**「缺少必填参数 主体标识」。同一入口、同一协议，行为分叉。
- 根因链：
  1. `运行核心/统一网关/网关核心.py:1128-1137`（`调用能力` 分支）确实先调 `self._能力参数错误(能力id, 已归一参数)`；
  2. `网关核心.py:846` → `校验能力参数(能力id, getattr(实现, "参数", []), 参数)`；
  3. `运行核心/统一网关/类型规格.py:168` 判据是 `if 项.get("必填") is 真 and 名称 not in 参数:` —— **严格同一性 `is 真`**；
  4. 而受害包的 `实现.参数` 形如 `{"名称": "源文件路径", "类型": "文本型"}` —— **只有 2 个键，`必填` 根本不存在** → `项.get("必填")` 返回 `None` → `None is 真` = 假 → **必填校验静默失效**；
  5. 少传的参数穿到实现体（`运行核心/能力调用/唯一能力调用.py:299-307` 的 `except Exception` 兜底把提供者异常映射公开码）→ `_公开错误码` 未登记 `TypeError` → 回落 **`内部错误` 500**。
- **根因行号**（原始写法，一行丢三个字段）：
  `模块库/直播逐字稿/__init__.py:110-113` 的 `类型表` + `:134` 的 `参数=[{"名称": 名称, "类型": 类型表[能力id][名称]} for 名称 in 参数名]`。
  同构扩散 **7 个包**（`图像处理:32,55`｜`自修复工具:24,48`｜`OCR:26,45`｜`媒体处理:32,54`｜`媒体转写:32,50`｜`测试资源:22,40`｜`直播逐字稿:110,134`），**共 32 条能力**在此范围内；
  另有 **1 个变体** `模块库/简单窗口/__init__.py:33,52`（`参数类型表` 手工列举 `(参数名, 类型)` 二元组，**`必填` 字段压根没出现过**）—— 6 条能力同缺陷。

**与门禁的关系（为什么一直没人发现）**

- `开发工具/发布门禁/运行发布门禁.py` 的「裸布尔」项把 **`必填=True` 用裸布尔** 记入存量债（当前账：存量基线 421，实测 589 条），治本方向是把裸布尔迁到正式类型 `真`/`假`（同一缺陷的另一面：字段名对、值类型对，但**字段本身被丢了**这一层无人管）。
- `开发工具/契约编译/漂移检测.py` 的「注册口径」子检测**本来能抓到**（判据含参数默认值/必填：注册侧缺声明而契约 `必填=True` → 漏声明分桶），但受害包的注册表是 `[{...} for 名称 in 参数名]` 这种**下标取动态表**的形态 → `读取注册口径` 的 AST 静态求值拿到 `未解析哨兵` → 整个参数表解析为 `None`（`引用 漂移检测.py:1431-1433`，判不出即跳过）→ **门禁静默放行**。实测：改前这 8 包在检测器里参数口径**全部未解析**（32 条能力 / 38 个参数表），改后仍是未解析（38 条）—— 即**本修法并未让静态门禁覆盖到它们**，这是遗留的独立缺口，见下。

**已修范围（一次命中 8 包，哲学 3.3）**

- 8 个包全部改成**从 `能力契约/参数契约.json` 整条透传**（`[dict(参数) for 参数 in 条目["参数"]]`，含 `名称`/`类型`/`必填`/`默认值`/`说明` 五键），入口不再手工重建参数项；
- 同时加**名序闸门** `_参数声明(能力id, 参数名)`：注册名序 ≠ 契约名序即抛 `ValueError`（不静默漏参）。实测 8 包 38 条能力名序与契约**逐条一致**（无一例外），闸门当前为护盾、未触发；
- 契约本身无改动：`能力契约/参数契约.json` 与 `能力定义.json` 早就是完整声明（五键齐全），本次只消灭入口侧的「第二套事实源」（哲学 12.1）；
- 8 个包的 `完整性摘要.json` 已按唯一生成器重算闭合。

**验收证据（全部 python3.14 实跑）**

| 项 | 值 |
|---|---|
| 改前 | `直播逐字稿.全自动精校` 空参 → **500** `TypeError: missing 4 required positional arguments`；`OCR.识别图片文件` → 500；`媒体转写.转写音频文件` → 500；`图像处理.缩放图像` → 500；`媒体处理.转码` → 500；`测试资源.申请资源` → 500；`自修复工具.验证修复` → 500；`简单窗口.打开窗口` → 500；对照 `个人蒸馏支持库.查询上下文` → 400 |
| 改后 | 上述 8 条 + `直播逐字稿.转写媒体文件`/`媒体转写.转写视频文件` 空参 → 全部 **400「参数不合法：能力 X 缺少必填参数 Y」**；`直播逐字稿.全自动精校` 报 **「缺少必填参数 源文件路径」**（契约第一个必填字段）；对照项仍 400、行为不变 |
| 反验红/绿 | 把 `OCR.__init__` 的 `_参数声明` 返回值改回只留 `名称+类型` → 热接入 → `OCR.识别图片文件` 空参 **500**（必红）；还原 → 热接入 → **400**（必绿） |
| 真装配 | `装配成功: True`，`已注册能力: 630 / 声明能力: 630`，**跳过包 0**，问题清单 0；网关热接入：**变更包 8 / 失败包 0 / 已注册能力数 691** |
| 契约漂移 | `python3.14 -m 开发工具.契约编译.漂移检测 --报告` → **判红 0 条**（默认值硬 0／必填硬 0／新增漏声明 0） |

**遗留（独立债务，本任务未修，不得当成已收口）**

| # | 缺口 | 实测规模 | 期望 |
|---|---|---|---|
| 71 | ~~**全仓同类「必填 被丢」仍在 26 个包 / 104 条能力**~~ | 见下 | ✅ **已修 31 包（提交 `f59e2266`，64 文件 / +2560 −486）** |
| 72 | **漂移检测的「注册口径」读不到入口注册参数表** → 门禁对受害包静默放行 | 见下 | ⚠️ **已按治本方向处置：判据数值变好（117 → 107），71 包 0 残留未解析；在途 8 包仍待收口（提交见 71）** |
| 73 | **`必填` 值的裸布尔存量债**（哲学层面同源） | 门禁账：存量基线 421 / 实测 589 条裸布尔；本次 8 包产出 `"必填": true`（**JSON 契约里的 `true` 是合法传输形态**，见 `公共契约/基础类型/逻辑类型.py` 的三层口径），但门禁若把「入口注册表里的裸 `True`」计入，则与「裸布尔迁正式类型」的治本方向并行 | 待发布门禁裸布尔项全仓收口时一并处理（`必填` 的**存在性**已由本次修好，剩下的是**值写法**） |

**★ 已知代价（如实登记，不遮掩）**：本次修法让「参数声明从契约整条透传」，入口侧因此不再有可被 AST 静态求值的参数字面量 —— 漂移检测对 8 包的注册口径从「简单窗口解析出 6 条（缺必填的假绿）」变成「8 包全未解析（38 条）」。**假绿消失、真绿仍未到位**（见 72）。运行期行为已由网关真跑验收（400 正确）；编辑期门禁覆盖需按 72 补。

---

### 8.7 #71 / #72 收口（2026-09-18 夜间，第二路接手，提交 `f59e2266`）

**#71 已修：31 个包（比登记的 26 包更大，重扫实测）**

- **重扫口径**：不采信既定清单，逐包 `import` 后调 `注册能力(间谍注册表)`，拿**真实注册进去的参数项**与契约比对（`参数项` 缺 `必填` 键即命中）。实测 **丢参包 30 个 / 129 条能力**；另加 **平台控制面/发布管理** 12 条 —— 它是同一缺陷的**更宽形态**：注册参数项是**纯字符串**（`参数=["包id", …]`），网关 `校验能力参数` 的 `if not isinstance(项, dict): continue` 让必填与类型**整项跳过**，比「丢字段」更彻底。合计 **31 包**。
- **一次改全（哲学 3.3）**：三种注册形态统一到一套注入块，`参数=` 一律改 `参数=_参数契约表[能力id]`：
  ① 单条能力（能力id 字面量）—— 名序从旧表达式静态解析并与契约逐字核对，不一致即整包放弃；
  ② 循环表驱动（能力id 是 for 变量）—— 入口里的「参数类型表 / 参数名列表」**整列撤除**（会漂移的第二套事实源），元组同位置删一项、for 目标名删一个；
  ③ 参数由实现层函数给（能力目录 `注册参数(契约条目)`）—— 同 ②，并按「同一事两套实现即缺陷，旧实现直接删」清退 `实现/注册文件租约.py` 的 `注册参数`（该函数只取 名称/类型/必填，漏 `默认值`，是同一缺陷的第三种变体）。
- **名序护盾保留**：每包保留 `_注册参数名序` + `_校验参数名序(参数契约表, 注册参数名序)`，`注册能力` 首行调用；名序 ≠ 契约名序即 raise（不静默漏参）。

**#72 处置（★ 关键：不重蹈「拿门禁覆盖率换干净」）**

上一路修 8 包时把参数声明变成「函数内推导」，AST 读取器读不到 →「参数口径未解析」从 32 涨到 116。本次明确**不重蹈**，判据是「该数必须变好」：

| 口径 | 改前 | 改后 | 说明 |
|---|---|---|---|
| 参数口径未解析 | **117** | **107** | ✅ 变好 |
| 已解析注册数 | 574 | **584** | ✅ 变好 |
| 必填已声明数 | 1143 | **1249** | ✅ 变好 |
| 必填漏声明数 | 50 | **0** | ✅ 清零 |
| 默认值已声明数 | 669 | **790** | ✅ 变好 |
| 默认值漏声明数 | 148 | **126** | ✅ 变好 |
| 注册口径判红数 | 0 | **0** | 无回归 |

做法：契约镜像落成 **`注册能力` 函数体内的字面量赋值**（`_参数契约表 = {…}`，**不做类型标注**——带标注会变成 `ast.AnnAssign`，漂移读取器只认 `ast.Assign`），键是 for 循环里的常量，于是 `开发工具/契约编译/漂移检测` 的 `_静态字面量` 走 `ast.Subscript` 分支能解析出整条参数**含 `必填`**；31 包实测 **0 条未解析**。另把 `支持库/后端/数据操作支持库/类型转换` 的循环表由元组字面量改为**列表字面量**（读取器只认 `ast.List`），该包 10 条从 0 解析变为全解析。副作用：「裸布尔」防回潮门禁（全文件 AST 扫描）现在能看见 `必填`，不再靠「AST 看不见所以绿」过关。

**验收证据（全部 python3.14 实跑，40007 网关）**

| 项 | 值 |
|---|---|
| 改前（空参，代表 5+ 包） | `窗口描述.创建窗口定义` **500** `TypeError: missing 4 required positional arguments`；`组件控件.创建窗口` 500；`资源描述.创建资源描述` 500；`状态描述.创建窗口状态` 500；`基础组件描述.创建组件定义` 500；`文件选择描述.创建文件选择描述` 500；`类型转换.文本转整数` 500；`psycopg数据库.连接数据库` 500；`文件管理.读取文件` 500；`文档读取.读取正文` 500；`文档解析.解析文档` 500；`网页分析.提取网页信息` 500；`统一入口.执行稳定操作` 500；`能力反馈.登记能力反馈` 500；`需求登记.登记需求` 500；`策略闸门.判定复用策略` 500；`授权.注册身份` 500；`资源预算监督.校验预算声明` 500 |
| 改后（同一组调用） | 上述全部 → **400「参数不合法：能力 X 缺少必填参数 Y」**；抽摊 32 次调用状态码分布 `{500:18, 503:1, 400:9, 404:4, 200:2}` → `{400:27, 503:1, 404:4, 200:2}`（503 是 `桌面宿主` 缺 `_tkinter`，404 是三条能力 id 与代码不一致的既有问题，200/400 对照项行为不变） |
| 反验红/绿 | 把 `窗口描述.__init__.py` 的 `_参数契约表` 改回只留 名称+类型 → 热接入 → 空参 **500** `TypeError: missing 4 required positional arguments`（必红）；还原 → 热接入 → **400**「缺少必填参数 窗口id」（必绿） |
| 真装配 | 网关热接入 `变更包 32 / 失败包 0 / 已注册能力数 693`；31 包 + 聚合父包 `数据操作支持库` 的 `完整性摘要.json` 全部按唯一生成器重算闭合 |
| 漂移 | 注册口径判红 **0 条**（默认值硬 0 / 必填硬 0 / 新增漏声明 0） |
| 残留 | 全仓运行时间谍扫描「丢参包 **0** / 丢参能力 **0**」；「注册参数含纯字符串项」**0 包** |

**仍未收口（如实登记）**

- **在途 8 包（上一路 `3893e0dd` 修的模块库包）**仍是「函数内推导」形态 → 它们的 8 条仍计入「参数口径未解析」。本次未碰它们（超出作业边界），但**同一套注入块可直接照搬**：把各包 `_参数声明(能力id)` 改为函数体内字面量 `_参数契约表[能力id]` 即可同步拿回 AST 可见性。
- **#73 未动**：`必填` 值的裸布尔写法（入口字面量里是 `True`/`False`）。本次按「JSON 契约原值镜像」写，理由是 `公共契约/基础类型/逻辑类型.py` 自己就写着 `真 = True`（JSON 无 真/假 字面量，契约侧同理）；若要迁成 `真`/`假`，可整体改注入块（一处模板改动 ×31 包）。
- **新发现（未登记为债务，先记此处）**：`平台控制面/{备份恢复,快照校验,平台状态}` 的三条被点名的能力 id 与代码不一致（网关 404「能力未注册」），与本次缺陷同源（入口能力 id 与能力定义分叉），需单独核实。

---
