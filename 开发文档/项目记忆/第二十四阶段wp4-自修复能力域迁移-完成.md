---
名称: 第二十四阶段wp4-自修复能力域迁移-完成
标签: []
创建时间: 2026-08-03T07:13:54.437642+00:00
---

完成（提交 8afcab1，分支 迁移/自修复）：
调用方式：自修复模块经 项目适配层/自修复能力/ 受控封装执行工作区/文件补丁/进程/验证（__init__.py 公共入口导出 4 组能力；错误码见 错误码.py，统一 自修复能力错误/补丁错误）。
关键决策：
1. 应用补丁到仓库 改为：记录原提交哈希→创建独立worktree（项目外临时目录）→工作区内精确应用补丁→定向验证（内容+白名单验证命令）→提交→受控git cherry-pick合入生产树→try/finally清理worktree；回滚=git revert提交 或 worktree清理 或旧快照兼容。
2. 平台能力缺口：git工作区/进程受管执行/文件补丁/定向验证 均无平台公开能力，由适配层自实现（不依赖平台客户端引导）。
3. 进程受管执行：白名单命令+参数列表（禁shell、禁python -c代码入口）+参数禁字符（;|&$`<>换行）+超时/取消进程组SIGKILL+输出上限截断。
4. 文件补丁：唯一匹配多重拒绝、base哈希校验、受控根内读写（../、绝对路径、符号链接逃逸拒绝）、原子写。
5. 修复既有bug：应用补丁到仓库 验证回滚分支用英文key(data/success)导致永不触发→改中文key；回滚快照属性 before_content 不匹配 dataclass 中文字段→适配层代码块结果加兼容property。
6. 锁管理.py 保留os.open直连（并发原语非补丁边界，报告已说明）。
验证：43项迁移测试全过；邻域回归3failed/6passed 与基线完全一致（既有测试键名不匹配遗留）。
