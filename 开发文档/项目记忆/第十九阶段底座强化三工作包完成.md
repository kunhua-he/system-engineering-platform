---
名称: 第十九阶段底座强化三工作包完成
标签: [第十九阶段, 项目锁闭包, 提供者锁强制校验, 客户端制品包仓库]
创建时间: 2026-08-03T01:44:49.546786+00:00
---

---
名称: 第十九阶段底座强化三工作包完成
标签: [第十九阶段, 项目锁闭包, 提供者锁强制校验, 客户端制品包仓库]
创建时间: 2026-08-03T02:00:00+08:00
---

# 调用方式（完成后）

## 工作包一：项目锁传递依赖闭包硬门禁
`项目适配层/依赖锁定/传递闭包.py` → `校验传递闭包(项目根目录, 系统根目录)` 返回 闭包校验结果（成功/问题列表稳定排序）。覆盖：缺项/多余项/版本漂移/循环依赖/提供者锁缺失。测试：测试中心/项目适配/测试_项目锁传递闭包.py（10 测试）。

## 工作包二：提供者依赖锁环境强制校验
`运行核心/运行环境管理器/强制校验.py` → `校验提供者环境(提供者目录, 提供者id, *, 自动清理=True)`。七规则：缺锁/空锁/精确版本（拒绝 >=/*/待定/==/latest/1.0.*）/依赖闭包覆盖/环境指纹（Python/OS/CPU）/隔离环境/错误回滚（半态清理）。测试：测试中心/运行核心/测试_强制校验.py（11 测试）。

## 工作包三：平台客户端制品入包仓库
`平台控制面/包仓库/平台客户端制品.py` → `平台客户端制品接入` 类（入库/校验制品/安装到环境/检查激活指针/校验稳定路径）。`客户端/构建平台客户端.py --安装` 现经包仓库机制（内容寻址+物料清单+Ed25519签名+信任元数据+原子安装+激活指针）。测试：测试中心/平台控制面/测试_平台客户端制品接入.py（14 测试）。

# 关键决策
- 三包全 fail-closed；反向破坏测试真实失败（变异验证/反向恢复证明）
- 闭包"覆盖判定"而非"非空"（无传递依赖的纯库允许闭包为空）
- V3 稳定路径契约不变（平台客户端/ + 当前.json 含版本/令牌新字段）
- 复用既有体系：解析器循环检测/环境管理器锁读取/包仓库签名信任发布管理——未复制第二套

# 验证状态
合并波次 3 文件 35 测试 + 5 邻域回归全过；防火墙 0 违规；真实客户端构建入库+签名+安装+激活成功（制品 34e76294…）；V3 稳定路径 docx 36626 字节。提交：平台 348b671、V3 52eb23c。

# 遗留（后续）
1. 12 提供者 能力定义.json 顶层缺 提供者.默认 字段（仅 psycopg2/psycopg 有）→ 补齐并重编译
2. 校验提供者环境 未挂启动监督器/装配硬门禁 → 下一阶段接线
3. 可信仓库元数据.发布目标"目标被替换"与包仓库正文摘要算法不兼容（恒真误报已绕开）→ 对齐摘要语义
4. 阶段收口全量由主协调者统一执行
