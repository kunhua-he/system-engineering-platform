---
名称: 第二十三阶段A环境镜像签名发布与代理-完成
标签: [第二十三阶段A, 镜像签名, 发布工具, 代理下载]
创建时间: 2026-08-03T05:34:52.017477+00:00
---

---
名称: 第二十三阶段A环境镜像签名发布与代理-完成
标签: [第二十三阶段A, 镜像签名, 发布工具, 代理下载]
创建时间: 2026-08-03T05:40:00+08:00
---

# 调用方式（完成后）

## 镜像清单签名与验证（远程镜像.py）
签名清单(清单, 私钥PEM) / 验证清单签名(清单, 公钥PEM)：Ed25519 经 支持库.适配层.密码签名提供者 公开入口；签名正文=_清单稳定序列化（去签名键+sort_keys+紧凑）。校验器=公钥验证强制（镜像签名无效）+信任指纹双保险；公钥缺失 fail-closed。错误码 镜像签名无效。

## 镜像发布工具（镜像发布.py）
发布环境镜像(环境目录, 输出目录, 私钥PEM, *, 元数据, 公钥PEM="")：收集正式文件→文件清单(路径+sha256)→制品摘要+清单摘要→远程镜像清单→签名→tar防穿越打包→五件套（镜像清单.json/制品.tar.gz/镜像签名.txt/制品摘要.txt/发布元数据.json）。防路径穿越：绝对路径/../符号链接逃逸拒绝；产物不含私钥。

## 代理下载与原子落盘（远程镜像.py）
远程镜像配置.代理地址（来自配置不硬编码，4780死变量已清除）；_请求地址 自实现代理GET（绕过 macOS proxy_bypass）；未配置显式直连。下载→解包(_校验解包成员+filter=data+_复核落盘边界)→复校验(制品摘要+文件清单摘要)→同步落盘(fsync)→原子落盘(fsync+os.replace)；失败清理无半成品。

# 验证状态
合并波次 3 新（18+14+15=47）+5 邻域全过；阶段收口：常规 728/发布门禁通过/V3 链 docx 36622+迁移回归 12 passed。提交 cc7b52d 已推送（4935ce7..cc7b52d）。

# 反馈闭环
feedback_review(b2e55873241446b5) 读取 8 条（本阶段 4 + 并行 B/C 4）：立即修复 0、升级池 7（真实镜像站补验/https代理专项/开工上下文合并状态快照/发布工具进门禁/代理https+鉴权+熔断/B-C阶段各项）、无需处理 4（镜像未启用环境限制/并行文件冲突已解决/macOS proxy_bypass已解决）。

# 遗留/升级池
1. 真实镜像站端到端验证（待运维方提供临时镜像服务）
2. https 代理通道专项验证
3. 开工上下文增加"同阶段工作包合并状态"快照
4. 发布工具接入发布门禁
5. 代理 https+鉴权+重定向/超时熔断细化
