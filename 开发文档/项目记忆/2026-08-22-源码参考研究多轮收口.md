# 源码参考研究多轮收口（2026-08-22）

## 目标与边界

本轮继承源码参考库权威文档，执行完整项目级静态取证，不修改参考项目源码、依赖、配置或运行数据。用户限定除代码地图外不调用 MCP；实际只使用 shell、Git、目标仓库 `codegraph` CLI 和 `127.0.0.1:4780` 远程代理。

## 并行范围

- 05 个人自我蒸馏参考：29 个项目复核；仅补充 `Second-Me/ARCHITECTURE.md` 的源码证据索引和增量审计入口。
- 10 Agent 平台、协议 SDK、12 工作流：修订 coze-studio、langgraph、DeepSeek-Harness、python-sdk、prefect 五份文档的 CodeGraph 证据边界，记录索引规模和中断风险。
- 15 知识库：补充 MaxKB、QAnything 的当前 HEAD、远程分支漂移、真实调用链、行号、已证/未证/待核边界。

## 总账事实

- 当前工作树 `开发文档/源码参考研究/项目/` 可见 167 份 `ARCHITECTURE.md`，全部包含文本流程图；按 `.git` 目录可识别 86 个仓库。两者不能混成一个“已审计项目数”。
- 当前可见 Git 仓库均已有独立 `.codegraph/`；未发现需要在共享工作树新建代码图的仓库。coze-studio、openclaw 的索引存在中断或引用待同步，不得将“可定位”写成“索引完整”。
- 远程落后已确认：hermes-agent、openclaw、DeepSeek-Harness、prefect、MaxKB v2、QAnything master；未 fetch、未 pull、未覆盖本地工作树，下一轮必须用 4780 在隔离目录建立快照后逐文件核对。
- 动态运行验证、数据库/向量库/模型/队列/浏览器/GPU、压力、强杀、发布验证均未执行；文档统一保留为待核或 L0/L1 静态证据。
- 统计纠偏：平台文档镜像树曾显示 86 个 `.git`，不能代表源码归档全量；外部源码归档根目录实际核对为 166 个根仓库，166/166 有 `.codegraph`。嵌套 docs/wiki/vendor 不算独立项目。

## 验证

- 所有并行工作包均报告 `git diff --check` 退出码 0。
- 未运行全量测试，符合本轮只读源码审计边界。
- 主协调者新增的权威文档段落需要最终执行平台根 `git diff --check`、修改路径白名单扫描和架构文档流程图/行号存在性检查。

## 下一轮

优先审计远程领先且底座价值高的 OpenCode、MaxKB、QAnything、hermes-agent、openclaw、DeepSeek-Harness、prefect 隔离快照；比较原子能力、模块流程、资源生命周期、失败恢复和真实测试后，再决定是否补充支持库/模块库节点。本轮没有足够证据新增顶层节点，也没有进入正式底座实现。

## 第二轮补充

- 增量复核完成：Second-Me、Hindsight、Graphiti、Mem0、Hermes-agent、OpenClaw、DeepSeek-Harness、Prefect、MaxKB、QAnything，以及 30 区 `instructor`。
- 远程未证保持诚实：DeepSeek-Harness/Prefect 已有远程 SHA 但快照未完成；Hermes/OpenClaw 受 4780 TLS/超时影响未取得远程 SHA。未把挂起下载当作成功。
- `instructor` 唯一架构文档补到 500 行以上，新增 provider 注册矩阵、失败/取消矩阵、资源所有权和支持库裁决。

## 第三轮短定点复核

- DeepSeek-Harness/Prefect 固定 SHA raw 请求：TLS 握手超时，curl 28、0 bytes；远程 SHA 已知但源码未证。
- Hermes/OpenClaw GitHub API：curl 28，远程 HEAD 未证。
- MaxKB/QAnything 固定 raw：curl 56/HTTP 404；`git ls-remote` 超时或退出 142，远程实现未证。
- 以上失败均已写入对应唯一架构文档，未覆盖源码或把失败请求当成远程差异结论。
- 支持库/模块库选择规则已写入 `开发文档/支持库总览.md` 与 `开发文档/模块库总览.md`；本轮没有新增正式包或模块。
