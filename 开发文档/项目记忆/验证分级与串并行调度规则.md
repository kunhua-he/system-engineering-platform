---
名称: 验证分级与串并行调度规则
标签: [测试, 加速, 并行, 慢速证据, 环境指纹]
创建时间: 2026-08-01T12:06:06.739206+00:00
---

慢速场景不删除，改为事件驱动运行环境证据。源码摘要、依赖锁、Python、OS、CPU架构、sys.executable/soffice/psql文件指纹一致，且成功证据不超过7天时直接复用；相关实现/环境变化、过期或故障修复回归时使用 --范围 慢速 --强制慢速。工作包永不跑慢速。唯一入口同时支持 --测试文件 多文件有界并行、--阶段波次验证、阶段收口常规全量一次、正式发布全部与门禁一次。

阶段失败时，唯一入口原子记录验证范围、原始阶段列表、失败阶段和原因。修复后使用 `python3.14 测试中心/运行测试.py --继续`：入口先重算失败阶段之前的源码、依赖和环境证据；证据仍有效则从失败阶段继续，任何上游摘要失效则自动回退到最早失效阶段。成功后自动删除断点。断点只减少重复执行，不得跳过失效证据，也不得代替阶段收口或正式发布门禁。
