# 系统工程平台 · 审计报告（简短声明）

日期：2026-08-18
审计范围：全仓库只读审计 + 主验收回归
审计提交：027300b（主干，已推送 origin）

## 一、健康基线（实测）

- 编译：554 个 `.py` 文件全部通过（Python 3.14.4）。
- 测试：常规全量 **1091/1091 通过**，零跳过零失败（342.9 秒）。
- 仓库：单 worktree；41 个本地分支全部已并入主干（0 未合并）。
- 本次本地改动已全部提交并推送至 `origin/主干`。

## 二、合规项

1. 分层纪律：模块库零静态导入支持库（只走能力调用器）；项目适配层零支持库导入；无深入实现目录的跨包调用。
2. 第三方隔离：适配层外零第三方导入；18 个提供者包七要素齐全。
3. 测试纪律：测试全部位于 `测试中心/`（159 文件），纯 unittest，无 pytest。
4. 业务隔离：无华世王镞_v3 代码引用（仅测试样本与防御性 grep）。
5. 门禁有效：发布门禁、零测试门禁、反向破坏测试均在套件中真实运行。

## 三、发现的问题

| 级别 | 位置 | 问题 |
|---|---|---|
| 低 | `支持库/适配层/PDF隔离提供者/实现/子进程解析.py:196,206` | SyntaxWarning：`finally` 块内 `return`，会吞掉异常。 |
| 低 | `运行核心/运行环境管理器/提供者生命周期.py:171` | 绕过包级入口深入 `提供者注册表.提供者注册表` 取 `提供者路由信息`，该符号未在 `__all__` 导出，违反"跨包只经包级中文入口"。 |
| 信息 | `支持库/适配层/` 7 个系统适配器 | 无包声明/完整性摘要（18 个提供者全有）；建议明文规定系统适配器豁免，避免误判。 |
| 已清理 | 仓库根 12 个 `.测试值.*.tmp` | 测试残留，已删除并加入 gitignore。 |
| 环境 | 本会话 MCP | project_toolkit 绑定 华世王镞_v3，与本仓库根不一致；本仓库会话应载入 system_engineering_toolkit。 |
| 环境 | git 身份 | committer 自动生成为 `用户@主机名`，建议显式设置 user.name/email。 |

## 四、第二轮补充审计

追加核对：能力搜索入口、示例项目端到端、证据 JSONL、分层倒置、完整性摘要一致性。

### 通过项
- 能力搜索入口正常（`--能力 读取文件` 返回 4 个能力实现）。
- 示例项目端到端成功：装配 183 能力、适配验证 11/11 通过、统一结果结构正确。
- `开发文档/项目证据/*.jsonl` 全部为合法 JSON，0 坏行。
- 分层零倒置：支持库不导入模块库/核心；公共契约不导入其他层；模块库不导入核心/控制面。
- 新增 `工具名映射.py` 导入正常；`客户端/构建平台客户端.py` 编译通过。

### 新发现问题（中）

| 级别 | 位置 | 问题 |
|---|---|---|
| 中 | `支持库/后端/文档生成` | **完整性摘要漂移**。提交 `1304300` 修改了 `实现/文档生成.py`（磁盘 sha256 前缀 `b24ed430`），但未重算该包 `完整性摘要.json`（仍登记旧值 `67917d5f`）。导致 49 个正式包校验中唯一失败项。当前激活制品 `5adbf37a809c48f4` 内该文件为旧版（`67917d5f`），与源码不一致。制品全量核对 668 文件中仅此 1 处源码漂移，影响面小但应在下次发布修复。 |

### 根因与修复建议
- 根因：修改实现文件后漏跑 `开发工具/组件规范/完整性摘要.py` 重算，漂移未被发布门禁拦截。
- 建议：重算该包完整性摘要 → 重新构建制品 → 切换激活指针，使制品与源码对齐。

## 五、第三轮补充审计

追加核对：静默吞错、资源有界性、能力契约完整性、能力id唯一性。

### 通过项
- 全仓零裸 `except:`（无静默吞错）。
- 线程池 `ThreadPoolExecutor(max_workers=并行数)` 有界，无无界队列/线程。
- 全部含 `包声明.json` 的正式包均带 `能力契约/` 目录。

### 能力id唯一性
- 扫描 145 个能力id，3 个跨包重复（`演示文稿.解析演示文稿`、`表格文档.解析表格文档`、`文字文档.解析文字文档`）。
- 均为后端支持库与对应 `.pptx/openpyxl/python_docx 提供者` 同时声明同一 id。
- 结论：符合注册表规则"公开能力重复注册失败，只允许全摘要一致的幂等重放"——**设计内行为，非违规**。

## 六、结论

工程质量高，门禁总体真实有效。缺陷清单：
- 低：PDF隔离提供者 `finally` 内 `return`（2处）；运行核心深入包内取未导出符号。
- 中：完整性摘要漂移 1 处（文档生成），根因为"改实现漏重算摘要 + 发布门禁未接必经路径"。
- 信息/环境：7 系统适配器无包声明；`.测试值` 残留（已清理）；MCP 实例绑定错项目；git 身份自动生成。

核心建议：把快检（摘要校验/编译/命名）左移进 pre-commit 钩子，并把摘要改为构建期派生，使漂移无法存在；发布门禁退化为罕见最终仪式。

## 七、修复落点调研（子代理回填）

### A. 完整性摘要漂移（中）
- 落点1：重算 `支持库/后端/文档生成/完整性摘要.json`（包id `支持库.后端.文档生成`、版本 `1.0.0`、`已废弃=true`）。
  命令：`python3.14 开发工具/说明书生成/完整性摘要生成器.py`（全量刷新安全，其余包字节不变）或单包重算。
- 落点2：`python3.14 客户端/构建平台客户端.py --安装` 重建客户端制品并 CAS 激活（构建器以内容寻址摘要为幂等键，摘要变化即强制新制品）。
- 验证：`测试中心/开发工具/测试_完整性摘要统一.py` + `测试中心/支持库/测试_文档生成.py`；单包门禁 `运行发布门禁.py --包目录 支持库/后端/文档生成`。
- 关键：全量门禁因 `已废弃=true` 默认豁免该包（`运行发布门禁.py:198-203`），正是漏网根因；防再发需补「含已废弃包的全仓逐包摘要校验」。

### B. PDF隔离提供者 finally return（低）
- 落点：`支持库/适配层/PDF隔离提供者/实现/子进程解析.py:196,206`（函数 `解析PDF为字典` 122-207 行），单出口重构，finally 只记录关闭失败、函数尾统一返回。
- 全仓 AST 扫描确认仅此 1 文件 2 处；psycopg 系列是 finally 之后 return，正常。
- 防再发：编译检查加 `-W error::SyntaxWarning`，或根级 `.ruff.toml` 选 `B012`。

### C. 运行核心深入导入未导出符号（低）
- 落点1：`支持库/适配层/提供者注册表/__init__.py` 第 3 行 import 追加 `提供者路由信息`，第 6 行 `__all__` 追加该符号。
- 落点2：`运行核心/运行环境管理器/提供者生命周期.py:171` 改走包级入口 `from 支持库.适配层.提供者注册表 import 提供者路由信息`。
- 两步必须同做（只改一处会 ImportError 或仍是违规深入导入）。
- 边界弱违规：`开发工具/发布门禁/第十四阶段门禁.py:231` 密钥提供者导入（该目录无 `__init__.py`，若补声明则同步改）。

### D. 信息级与环境项
- D1 系统适配器豁免：豁免是设计内（`扫描正式包` 以 `包声明.json` 为标记天然不收录），AGENTS.md 未明文。修复：在根 `AGENTS.md` 第 190 行后插入「系统适配器豁免」条款（措辞见子代理报告），不推荐给 7 适配器补声明。
- D2 MCP 绑定错位：本仓库 `.mcp.json` 已正确定义 `system_engineering_toolkit/developer/caller` 三实例，但全局 `~/.config/opencode/opencode.json` 只注册了 `project_toolkit`（指向华世王镞_v3 聚合网关），本仓库会话未载入本仓库 MCP。修复：全局 opencode.json `mcp` 段追加 `system_engineering_toolkit`（stdio，配置同根 `.mcp.json`）。
- D3 git 身份：`git config user.name "系统工程平台"` + `git config user.email "用户@<真实邮箱>"`（真实邮箱未确认）。

### 未确认项
1. D3 真实邮箱地址。
2. D2 opencode 是否自动合并项目级 `.mcp.json`（当前未合并，需修复后实测工具前缀）。
3. 已废弃包摘要校验是否纳入门禁默认扫描（防再发决策）。
