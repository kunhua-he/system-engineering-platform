# 测试加速施工计划（统一落盘）

日期：2026-08-19
上游：20260819-测试加速方案.md（主方案）+ 8 份审计落点（数字自洽/缓存模型/失败断点/并行预算/假绿审计/兼容性/git指纹与GC/watch守护）
性质：把 8 份落点修正整合为分层施工计划，每层独立验证，有 bug 回写主方案文档。

---

## 分层总览

| 层 | 内容 | 风险 | 状态 |
|---|---|---|---|
| 第 1 层 | 零风险基建：缓存原子写、环境摘要修复、rglob 收窄、零测试判定、缓存测试、发布门禁传参、资源键核对、GC 机制、md 应用审计修正 | 低 | ✅ 已落地 `370612e` |
| 第 2 层 | 阶段并行 + 全局并发预算（主进程唯一线程池扁平化） | 中 | ✅ 已落地 `d4df5d4` |
| 第 3 层 | 文件级缓存 + 实测依赖记录（audit hook）+ git 指纹 + 内容寻址 GC | 中高 | ✅ 已落地 `14201c3` + `e0fe157` |
| 第 4 层 | 慢测试先行、watch 守护（降级可选项） | 高 | ⏸ 评估后决定（当前不做） |

每层完成后：跑对应测试 → 有 bug 更新主方案文档 → 全量回归 → 进下一层。

## 落地成果（实测）

| 场景 | 改造前 | 改造后 |
|---|---|---|
| 改 1 个小文件 | 320s（整阶段重跑） | **0.32s**（只重跑依赖它的测试文件） |
| 代码没动 | 1.36s | **0.25s**（全缓存命中） |
| 大改动全量 | 344s | **256s**（阶段并行 + 缓存细分叠加） |
| 阶段级缓存 | 单文件覆盖写 | 独立 {阶段名}.json 原子写 |

修复项：两个已知基线失败（测试_协作状态 test_零残留、测试_能力搜索覆盖率 test_字段缺失）已随本计划修复 `5ff341a`。

---

## 第 1 层工作包划分（并发 8 子代理，文件互不冲突）

### WP1：运行测试.py 基建（一个文件，全部改动由该工作包完成）
- 缓存原子写：`写入缓存` 改 `工程缓存/验证缓存/{阶段名}.json` + 临时文件 `os.replace`；`读取缓存` 改为聚合读取目录内全部 json；`计算断点续跑阶段` 读取点同步。
- 环境摘要修复：外部程序清单扩展（soffice/tesseract/ffmpeg/ffprobe/textutil/docker/psql/security/sw_vers/git，按实际引用）；`阶段缓存可复用` 放开非敏感阶段也校验环境摘要。
- rglob 收窄：`运行环境摘要` 的 `rglob("*依赖锁*.json")` 改为定点 glob（`支持库/适配层/*/依赖锁.json` + `示例项目/适配层示例/依赖锁定.json`）。
- 受影响测试：测试_零测试门禁.py（阶段缓存可复用断言）、测试_验证门禁.py。
- 验收：`--测试文件` 两文件全过；`--范围 常规` 全过。

### WP2：验证门禁零测试判定回归测试（测试中心/MCP工具箱/测试_验证门禁.py）
- 新增用例：真零测试（Ran 0 tests/no tests ran）必检出；元测试输出（"零测试门禁失败"字样）放行。
- 验收：该文件全过。

### WP3：缓存读写定向测试新增（新文件 测试中心/测试_缓存读写.py）
- 覆盖：写入/读取聚合/并发覆盖（多进程写不同阶段文件不互斥）/损坏自动失效/结构版本校验。
- 验收：该文件全过。

### WP4：发布门禁显式传参（开发工具/发布门禁/运行发布门禁.py）
- 内部常规全量调用显式传 `--并行数 1`（或新增禁用并行开关），不依赖"默认并行"行为漂移。
- 验收：py_compile + 定向测试。

### WP5：资源键核对表（新文档 开发文档/临时文件/资源键核对表.md）
- 只读分析：静态契约 47 文件、项目装配 7 文件、真实进程 2 文件的资源键特征（固定端口/测试数据库/外部应用/单实例程序/子进程/共享目录/超时断言），逐文件输出"可并行/串行"结论。
- 重点：支持库提供者测试族（psycopg 5432 端口、soffice 单实例、tesseract 等）、测试_零测试门禁元测试。
- 产出事实校准"静态契约 71s→20-30s"预期是否成立。

### WP6：工程缓存 GC 机制（脚本 + 清理说明）
- 新增 开发工具/工程缓存回收.py：按引用（测试文件不存在即删文件级缓存）+ 按访问时间（30 天）+ 并行日志 7 天 + 验证运行/ 残留兜底；明确不触碰 提供者运行环境/制品仓库/发布证据/核心快照。
- 并行_*.log 为审计证据暂保留（落点文件引用），脚本支持后由 GC 按 7 天清理。

### WP7：协作状态收口回归（测试中心/MCP工具箱/测试_协作状态.py）
- 验证排除表新增（项目证据/临时文件）后：写证据文件不影响指纹；收口登记不再 EVIDENCE_MISMATCH。
- 注意：test_零残留 为已知基线失败（历史登记文件使断言失效），本次不改该断言。

### WP8：主方案文档应用审计修正 + 本施工计划归档
- 把 8 份落点修正全部应用进 20260819-测试加速方案.md（数字口径/缓存判据/资源键/环境摘要/失败断点/假绿防线/watch 降级/32上限/git指纹等）。
- 修正项按落点文件清单逐条核对，不遗漏。

---

## 第 2~4 层要点（第 1 层验证通过后启动）

### 第 2 层：阶段并行 + 全局并发预算
- 主进程唯一 ThreadPoolExecutor 扁平化（max_workers = os.cpu_count()），阶段与文件统一为叶子任务。
- 失败即终止（_终止进程组 + cancel 未启动任务）；断点写"阶段顺序表下标最小真实失败"；区分真实失败与被终止。
- 移除 32 上限（同步改 测试_零测试门禁.py:128 断言为 ≤ os.cpu_count()）。
- 阶段子进程 teardown（临时根/资源清单）；输出按序回放。
- 新增 `--并行阶段` 参数；`--继续` 沿用断点并行策略（保守先串行）。
- 慢速层表述修正：`--范围 慢速/全部` 均执行。

### 第 3 层：文件级缓存 + audit hook + git 指纹 + GC
- audit hook：装于每测试文件独立子进程；pyc 规范化回源 `.py`；TMPDIR 排除；fd 型 open 忽略；按路径去重；网络事件标弱依赖；stat 静态扫描标弱依赖；弱依赖 TTL ≤3600s；记录文件自身不触发审计。
- 依赖判据：测试前已存在 + 被读（或读事件排除 TMPDIR），替代有矛盾的"写后读回"。
- git 指纹：一次 `git ls-files -s -z` + `git status --porcelain -z` 建映射；干净文件取 hash 截断 16 位；untracked 必须参与目录摘要（防假绿）；路径统一仓库根相对。
- GC：按引用 + 按访问时间双策略，与文件级缓存同步落地。

### 第 4 层（评估后决定）
- 慢测试先行：依赖补充优化 5 耗时画像，无画像按文件大小兜底。
- watch：删除"预热 import 缓存"与"依赖图"；只做调度（检测变化→查文件级缓存依赖表→调度独立子进程）；双向 flock 互斥；有界队列；pid/空闲超时/信号停止。默认降级为可选项。

---

## 跨层守则
1. 每层改动最小化，只动本层必需；不做超出该层的重构。
2. 每层独立验收（定向测试 → 常规全量），有 bug 先回写主方案文档再修代码。
3. 环境敏感阶段（真实进程/网关/浏览器/发布门禁/慢速层）永不并行。
4. 缓存原子写是并行前提，第 1 层必须先完成。
5. 先全局并发池，再放开 32 上限（资源必须有界铁律）。
6. 假绿防线五层逐层落实，审计 hook 禁装常驻/主进程。
