# 开发支持库 · 使用流程图

> 最后更新：2026-09-21
> 口径：实测（命令与能力均现场可跑）
> 维护者：全局规范
<!-- 机器管理｜类型：规范｜生成器：开发工具.MD文档生成 -->

**图例**：`⬛` = 能力调用（MCP `capability_call` 经网关）　`▷` = 终端脚本　`◆` = 判断点　`❌` = 已知缺陷

**怎么读**：**缺陷写在它所属的那个节点里**，不另设缺陷章节、不用往下翻；看到哪个节点，它的毛病就在它旁边。取证命令统一在 §五「查毛病」。


## 一、一次实际开发，从头到尾

```text
【接到任务】
     │
     ▼
▷ 步骤0  环境自检
     python3.14 -m 开发工具.环境自检
     ├─ 有不支持项 → 停，先修环境
     └─ 全绿 ─┐
              ▼
⬛ 开工第一步  开工编排.开工准备
     参数={项目根, 任务, 修改路径}
     ↓ 一次返回 19 块（实测 720.44 毫秒）
     ★ 全流程最重的一个节点：下面就地把它逐块展开，每块的毛病写在它自己那一行。
```

```text
⬛ 开工准备 · 就地全展开（节点顺序 = 实现顺序）

 ├─① 入参校验
 │    ❌ D7 顶层「项目根」**不自动透传**
 │       实测：顶层 项目根 已传，仍 HTTP 400
 │             「参数不合法：能力 开工编排.开工准备 缺少必填参数 项目根」
 │       在 `参数` 里再传一次才 200 ⇒ **第一次调用必错**，容易被误判成「能力坏了」
 │
 ├─② 修改路径 → 项目根边界校验
 │   归一化 + Path.resolve().is_relative_to(项目根) + 路径安全.校验路径
 │   ├─ 越界 → 整体失败（不是逐条跳过）
 │   └─ 通过 → 通过清单 / 越界清单            ✅ 正常
 │
 ├─③ Git 状态
 │   当前分支 ／ 最近提交 ／ 未提交修改清单 ／ 工作区指纹
 │   ❌ D1 工作区指纹恒为空（""）
 │      根因：缺原子能力「发布治理.当前工作区指纹」
 │            （git status --porcelain 变更清单 + 每个变更/未跟踪文件的真实字节 sha256）
 │      连带：⑥ 的「匹配成功记录数」恒 0 ⇒ 分数硬顶 50
 │
 ├─④ 证据账本
 │   读 开发文档/项目证据/验证历史.jsonl → 筛「退出码=0 且 判定成功」→ 最近 N 条
 │   ❌ D4 账本断更：262 条，最新 2026-09-11，距今约 10 天
 │      ⇒ 开工看到的「最近成功验证」**不代表当前代码**
 │   ❌ D1 同一根：缺原子能力「验证证据.读取成功记录」
 │      —— 只能由本模块自己拼（判断存在 + 读取文件 + 按行分割 + 反序列化 JSON）
 │
 ├─⑤ 代码地图可用性
 │   只判断 .codegraph 目录存在与否
 │   ❌ D5 弱判据：目录在就报「可用」，不跑 codegraph status
 │      ⇒ 地图过期也报「可用」 ⇒ 新代码搜不到，被当成不存在
 │
 ├─⑥ 证据可信度
 │   分数 = 20(项目规则) + 30(地图可用) + min(50, 匹配数×25)
 │   ❌ D2 **硬顶 50**：匹配数依赖 ③ 的工作区指纹，指纹恒空 ⇒ 匹配数恒 0
 │      ⇒ 分数恒 ≤ 50 = 「部分可信」 ⇒「**已验证**」（≥75）**当前不可达**
 │
 ├─⑦ 任务类型强规范
 │   切词打分 → 按相关度降序取前 N 类（含 强规范 / 判据 / 原文位置）
 │   ❌ D6 字典 7 类里**没有「调研 / 审计 / 只读盘点」**
 │      7 类 = 加新能力 ｜ 改契约 ｜ 写验证场景 ｜ 修缺陷 ｜ 改文档 ｜ 派活与验收 ｜ 拆超长文件
 │      实测传「调研开工第一步信息完整性」⇒ 任务类型 `[]`、强规范 `[]`（0 条规范）
 │
 ├─⑧ 通用纪律
 │   无条件返回 7 段：哲学纪律 / 命令环境 / 工具姿势 / 提交纪律 / 改完必做 /
 │                    效率达标线 / 派活纪律；取不到即报，不静默省略      ✅ 正常
 │   （缺这一段 = 注入链断裂，**不得当「没有纪律」放行**）
 │
 ├─⑨ 文件租约实况
 │   查这批路径上的活跃租约 → 撞车清单 / 占用者清单
 │   查不出来如实报「未查询」，绝不回落成「无撞车」放行      ✅ 正常
 │
 └─⑩ 组装返回（19 块）
     ❌ D3 其中 4 块**实际有、契约没声明**：
        开发流程 ／ 开发流程提示 ／ 强规范提示 ／ 强规范是否截断
        —— 漏掉的恰是**强制四步开发流程**（①搜承载 → ②建包 → ③编译体检 → ④收口）

 19 块实测清单（✅ = 正常，❌ = 毛病见上面对应节点）：
   开工ID ｜ 项目（名称/根目录/任务/验证级别）｜ 路径校验 ｜ 文件租约 ｜ 下一步
   通用纪律 ｜ 通用纪律问题（""）｜ 通用纪律提示                            ✅
   代码地图（可用=true 判据=仅目录存在性）                                  ❌ D5
   证据可信度（50 / 部分可信 / 工作区指纹 ""）                              ❌ D2
   最近成功验证（3 条，最新 2026-09-11）                                    ❌ D4
   工作区快照（未提交 45，工作区指纹 ""）                                   ❌ D1
   任务类型（[]）｜ 强规范（[]）                                            ❌ D6
   开发流程 ｜ 开发流程提示 ｜ 强规范提示 ｜ 强规范是否截断                   ❌ D3
   缺失原子能力（4 项 —— 上面多条缺陷的根）

 4 项缺失原子能力（开工准备自己登记，逐项对应上面的缺陷）：
   发布治理.当前工作区指纹   → 工作区指纹恒空（③）
   验证证据.读取成功记录     → 「最近成功验证」只能自己拼（④）
   代码地图.查询状态         → 只能看目录在不在（⑤）
   开工编排.验证计划         → 该跑哪些测试推不出来
```

```text
     │
     ▼
◆ 开工ID 拿到了吗？
     ├─ 没拿到 → 停（后面所有动作都不带开工ID，无法追溯）
     └─ 拿到 ─┐
              ▼
【动手前先查 —— 别重复发明】
     │
     ├─⬛ 项目文档支持库.建全仓索引 → 查询索引     有没有既有承载
     ├─⬛ 项目文档支持库.查开工上下文             历史踩过什么坑
     ├─⬛ 项目文档支持库.查哲学条款               条款怎么说的
     └─▷ 开发工具/派活前核验.py                  这条待办修好了吗
              │
              ▼
⬛ 开工编排.开工即占
     参数={项目根, 任务, 修改路径, 开工ID}
     ↓ 返回
     已开工=真 ／ 租约id清单 ／ 开工上下文
     ├─ 已开工=假 → 撞车清单（谁在改、什么时候开始）→ 等 或 换文件
     └─ 已开工=真 ─┐
                   ▼
【改代码】
     │
     ├─ 新建包 ─⬛ 组件规范支持库.生成支持库模板 / 生成模块模板 / 生成技能模板
     ├─ 改契约 ─ 编辑 能力定义.json（唯一可编辑事实源）
     │              └─▷ 开发工具.契约编译.能力定义编译器（重新派生）
     └─ 改实现 ─ 实现/<包名>.py
                   │
                   ▼
▷ 编译口（一键，15 项）
     python3.14 -m 开发工具.开发编译口.编译口 --变更
     ├─ 退出 1 → 看红项 → 修 → 重跑
     └─ 退出 0 ─┐
                ▼
▷ 定向测试（只跑这个包）
     python3.14 -m unittest 测试中心.<层>.<包>
     ├─ 红 → 修
     └─ 绿 ─┐
            ▼
◆ 改了什么？
     ├─ 只改一个小单元 → 到此够（定向冒烟）
     ├─ 一批同类       → 跑受影响波次
     └─ 全部工作包完成 ─┐
                        ▼
▷ HTML 全量黑盒（唯一允许的「全量」）
     python3.14 -m 开发工具.HTML验证.验证器 --并发 32
     对已编译制品发真实 GET/POST；只认 HTTP 状态码与统一结果
     ├─ 红 → 修
     └─ 绿 ─┐
            ▼
▷ 摘要重算（改过包内文件必跑，父包会级联）
     python3.14 -m 开发工具.全量重算摘要
     判据：不闭合 0/N
     │
     ▼
▷ 文档收口（改了什么就刷什么）
     ├─ 改契约  → python3.14 -m 开发工具.MD文档生成 --类型 包级说明-使用说明 --写盘
     ├─ 改正文  → python3.14 -m 开发工具.MD文档生成 --人工改 --文件 X --正文文件 草稿 --写盘
     └─ 加条目  → python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --写盘
     │
     ▼
▷ 提交
     git status --short          ← 先看全貌：有没有别人的文件
     git add <只加自己改的>       ← 禁 git add -A
     git commit -m "开工ID: 改了什么/为什么/怎么验的"
     │
     ▼
⬛ 开工编排.收工释放
     参数={租约id清单, 原因}
     ↓ 返回
     释放数 ／ 释放清单 ／ 幂等清单
```


## 二、任务类型分岔（走哪条验证链）

```text
                【本次改动】
                     │
      ┌──────────────┼──────────────┬───────────────┐
      ▼              ▼              ▼               ▼
   改契约          改实现          改文档          只查不改
      │              │              │               │
      ▼              ▼              ▼               ▼
▷能力定义编译器   ▷编译口        ▷MD文档生成      ⬛查询能力
▷契约漂移检测     ▷定向测试      --写盘           ⬛读取能力
▷编译口           ↓              ▷拒绝手写校验
↓                 ▷HTML全量      ↓
▷说明书重生成     ↓              ▷全量重算摘要
↓                 ▷全量重算摘要  ↓
▷全量重算摘要     ↓              ▷提交
↓                 ▷提交
▷提交

     ★ 共同铁律：非核心改动**不中途跑验证**（哲学 12.1）——
       同一处改动跑了第二次验证 = 第一次缺了「先查事实」这一步。

     ❌ D6：**「调研 / 审计 / 只读盘点」不在这张分岔表里**
        —— 本次实测「调研开工第一步信息完整性」命中 0 类，
           拿到 0 条强规范、0 条判据。分岔表与 ⑦ 的字典 7 类是同一份数据，
           补一类两边同时生效。
```


## 三、并发开发（多个会话/子代理同时改）

```text
【主会话】
     │
     ├─⬛ 开工编排.开工即占（原子认领整批路径）
     │      └─ 同一份文件也能上锁；谁先拿到谁先改
     │
     ├─ 写任务记忆包
     │     工程缓存/任务记忆/<开工ID>.md
     │     五段：开工ID+目标 / 允许修改目录 / 已核实事实包 / 验收命令与期望 / 边界与禁做
     │
     ├─▷ 开发工具/任务记忆/派活任务信.py（自动拼固定条款）
     │
     └─ 派子代理（各自独立工作树 + 硬超时）
            │
            ▼
     【子代理】开工第一步：读记忆包 → 现场复核（对照一两条命令，不从零探索）
            │
            ├─ 发现记忆包与现场不符 → 如实回报，不许硬做
            └─ 做完 → 写完制品即退出（不自行结束的按 bug 处理）
            │
            ▼
     【主会话】逐个现场验收 → 合并 → 重启 → 真实 HTTP 验证 → 收工释放
```


## 四、出错了往哪查

```text
【编译口红项】
     │
     ├─ 摘要闭合      → ▷ 全量重算摘要（改过包内文件没重算）
     ├─ 契约漂移      → 契约 ↔ 实现/产物不一致（改契约没重编译，或实现没跟上）
     ├─ 公开调用完整性 → 说明书缺能力段 / 错误码没登记
     ├─ 能力id冻结基线 → 能力id 被改名或删除（只增不改）
     ├─ 验证场景体检   → 场景文件不合格
     ├─ 重复腿与旁路   → 同动作双路径 / 调用腿不唯一 / 技能库绕网关 / 平台判断越界
     ├─ 测试体系门禁   → 测试不可导入 / 零测试 / 未解释跳过
     ├─ 防回潮·反向破坏 → 13 类破坏没被拒绝（判据失效）
     ├─ 参数可用性     → 参数缺失/不可用
     ├─ 规范漂移      → 规范里的路径/命令指向失效
     ├─ 债务清单      → 账目与正文实数不一致（改了正文没刷账）
     ├─ 目录树与命令   → 树块与同包摘要不一致
     └─ 拒绝手写md    → 本轮改动的 md 没有机器印记
     │
     ▼
【网关/调用失败】
     ├─ 非本平台项目根 → 项目根没传/不是目录/缺 AGENTS.md 或 开发文档/项目说明.md
     ├─ 网关凭证缺失   → 环境变量「系统库网关凭证」没设
     ├─ 网关不可达     → 40007 没起（launchctl 看进程）
     ├─ 能力不存在     → 能力id 写错，或写成工具名
     ├─ 参数不合法     → 参数与契约不符（双精度传 30 不传 30.0；缺参/多余参数）
     │                   ❌ D7：顶层 项目根 不自动透传，须在 参数 里再给一次
     │                           实测：顶层传了也照样 400「缺少必填参数 项目根」
     └─ 提供者不可用   → ▷ 环境自检（依赖锁指纹不符 / 模型未配置）
                         ❌ D8：**直连 Python 调能力 = [E未装配]**
                             `from 模块库.开工编排.实现.开工编排 import 开工准备`
                             →「[E未装配] 能力调用器状态=未装配……
                                禁止直接导入支持库/提供者绕过能力调用服务」
                             ⇒ 定位问题时不能直连调试，只能走 40007 网关
```


## 五、全流程命令速查（照抄可跑）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH && export PYTHONPATH=.          # 必做，防 venv 污染

▷ 环境            python3.14 -m 开发工具.环境自检
▷ 编译口          python3.14 -m 开发工具.开发编译口.编译口 --变更
▷ 项目编译        python3.14 -m 开发工具.项目编译.项目编译器
▷ HTML全量        python3.14 -m 开发工具.HTML验证.验证器 --并发 32
▷ 摘要重算        python3.14 -m 开发工具.全量重算摘要
▷ 发布门禁        python3.14 -m 开发工具.发布门禁.运行发布门禁
▷ 依赖锁重建      python3.14 -m 开发工具.重建依赖锁
▷ 文档生成        python3.14 -m 开发工具.MD文档生成 --类型 <类型> --写盘
▷ 人改文档        python3.14 -m 开发工具.MD文档生成 --人工改 --文件 X --正文文件 草稿 --写盘
▷ 拒绝手写        python3.14 -m 开发工具.MD文档生成 --拒绝手写校验
▷ 能力检索        python3.14 开发工具/能力搜索/能力搜索器.py --能力 <关键词>

⬛ 开工准备        能力id=开工编排.开工准备
⬛ 开工即占        能力id=开工编排.开工即占
⬛ 收工释放        能力id=开工编排.收工释放
⬛ 查能力          能力id=能力目录.搜索能力
⬛ 读契约          能力id=能力目录.读取能力
⬛ 查踩坑          能力id=项目文档支持库.查开工上下文
⬛ 查条款          能力id=项目文档支持库.查哲学条款
⬛ 备份            能力id=平台控制面.备份恢复.执行备份
```

### 查毛病（引用 §一 里任一条缺陷前，先跑这几条复核）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH && export PYTHONPATH=.

# D7 / D1 / D2 / D3 / D5 / D6：调一次开工准备，看真实返回
#   ⚠ 顶层 项目根 与 参数.项目根 都要给（顶层那个不会透传，D7）
curl -sS -X POST http://127.0.0.1:40007/网关/调用 \
  -H 'Content-Type: application/json' -H "Authorization: Bearer ***" \
  -d '{"操作":"调用能力","能力id":"开工编排.开工准备","项目根":"'"$PWD"'",
       "参数":{"项目根":"'"$PWD"'","任务":"<本次任务>","修改路径":["<相对路径>"]}}'

# D4：账本条数与最新时间
python3.14 -c "import json,pathlib as p;L=[json.loads(x) for x in p.Path('开发文档/项目证据/验证历史.jsonl').read_text(encoding='utf-8').splitlines() if x.strip()];print('条数',len(L),'最新',max(r['时间'] for r in L if r.get('时间')))"

# D1：模块自己登记的待补能力（4 条）
rg -n "待补能力清单 = \(" -A 24 模块库/开工编排/实现/开工编排.py

# D6：任务类型字典全部类型名
rg -n '"任务类型"' 开发文档/规范/任务类型规范.json

# D3：契约声明的返回结构（与实际返回逐块对比）
python3.14 -c "import json;d=json.load(open('模块库/开工编排/能力契约/参数契约.json',encoding='utf-8'));print(json.dumps([c['返回'] for c in d['能力契约'] if c['能力id'].endswith('开工准备')],ensure_ascii=False,indent=2))"

# D8：直连 Python 调能力（期望 [E未装配]）
python3.14 -c "from 模块库.开工编排.实现.开工编排 import 开工准备"
```
