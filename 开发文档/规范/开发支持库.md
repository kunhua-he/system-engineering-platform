# 开发支持库 · 流程图

> 最后更新：2026-09-21
> 口径：实测（图里每个数字都可现场复现，命令见《开发支持库 · 细则》）
> 维护者：全局规范
<!-- 机器管理｜类型：规范｜生成器：开发工具.MD文档生成 -->

**图例**：`坏` = 不能用　`半` = 能跑但有坑　`通` = 好用　`注` = 结论框（不占节点位）

**怎么读**：自上而下走一遍；**每节一个框，框内分「能 / 不能」两栏** —— 坏在哪就写在那个节点自己身上，不用往下翻。

```text
╔══════════════════════════════════════════════════════════════════════╗
║ 开发支持库 · 全流程图                                                ║
║ 每节一个框：框内分「能用 / 不能」；坏在哪就写在那节自己身上          ║
╚══════════════════════════════════════════════════════════════════════╝
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ① 环境自检                                                        坏 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 自检好使：九大项逐项探，说不支持就真不支持                    │
│ 不能 │ 报「能力数 0」—— 但这是**源码重装口径**（新起进程从零装配）   │
│      │ 它撞上那份坏锁（Swift编译器提供者）就过不了                   │
│      │ ★ 不代表平台不可用：常驻网关此刻健康、能力数 720（见下框）    │
│ 注意 │ 修法（只对源码重装口径有效，未执行）：                        │
│      │ 重建依赖锁 --写入 --全部                                      │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ✦ 两条口径别搞混（我先前就栽在这）                                注 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 常驻网关 PID 5463，启动 2026-09-20 22:36:58，已服务 3h+       │
│      │ 实测 GET /健康 ⇒ 状态健康，能力数 720                         │
│      │ 实测 搜索能力 ⇒ 总数 540，真调能力成功                        │
│      │ → 平台此刻**好好的**，720 个能力全部在线                      │
│ 不能 │ 环境自检那个「0」= 源码重装口径                               │
│      │ 坏锁加入 2026-09-21 00:50:59，晚于网关启动 2h14m              │
│      │ → 在跑的网关**从来没见过**那份坏锁，所以它没事                │
│      │ → 「0」只说明：照现在源码重开一次，会被闸门顶住               │
│ 注意 │ 判据：平台可用不可用，**以常驻网关真实 HTTP 返回为准**        │
│      │ 不以某个 py 自检脚本的本次快照为准                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ✦ 两个门禁别搞混                                                  注 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 空锁 = 包 与 直接依赖 均为空 ⇒ 单包级，只跳过该包             │
│      │ 指纹 = 锁内 Python／操作系统／CPU 与当前机器一致              │
│      │ Swift 那份两者都有内容 ⇒ 不属于空锁                           │
│ 不能 │ 缺指纹或为空 ⇒ 无法判定 ⇒ 跨包硬门禁 ⇒ 整体阻断               │
│      │ 依据：指南 8.2『提供者环境未就绪』属整体阻断                  │
│ 注意 │ 指南 8.1『依赖锁内容为空或非法』= 规则2 空锁                  │
│      │ 指南 8.2『提供者环境未就绪』= 规则5 指纹 ⇒ 不冲突             │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ✦ 验证腿：HTML（主线）vs py 脚本（辅助）                          注 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 40007 网关：POST /网关/调用 传参即测，浏览器可发              │
│      │ 实测 20 路并发 → 总 2.02s，状态码 200×20 全绿                 │
│      │ 实测 60 路并发 → 总 3.73s，200×40 + 429×20                    │
│      │ 　（429 = 网关自带限流，不是坏，是保护）                      │
│      │ → HTML 传参 + fetch 并发 = 真实验收，快且不看内部实现         │
│ 不能 │ py 脚本单进程串行：一轮十几秒起，还得自己解析输出             │
│      │ → 慢的根源就在这条腿，不该拿它当主验证                        │
│ 注意 │ ⚠ 浏览器直连 40007 会被 CORS 拦（允许来源表默认空）           │
│      │ 　解法：页面与转发同源（不改网关、不重启、不加白名单）        │
│      │ 底座已明写：HTML 是正式发布主判定（华哥 2026-08-29 裁决）     │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ② 开工准备                                                        坏 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 路径边界校验：越界当场拒，不悄悄跳过                          │
│      │ 通用纪律：七段无条件返回，取不到就报                          │
│      │ 文件租约实况：查不出就说「未查询」，不回落放行                │
│ 不能 │ 顶层「项目根」不透传 → 第一次调用必错                         │
│      │ 工作区指纹写死空串（实现有，没接线）                          │
│      │ 代码地图「可用」只看目录在不在 → 过期也报可用                 │
│      │ 任务类型字典仅 7 类，没「调研/审计」                          │
│      │ 「缺失原子能力」是死字面量 → 报的 4 项全不成立                │
│      │ 契约漏声明 4 块（含强制四步开发流程）                         │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ③ 开工即占                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 原子认领整批路径，任一被占则整体不开工并回报占用者            │
│      │ 同一份文件也能上锁，谁先拿到谁先改                            │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ④ 动手前先查                                                      通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 查全仓有没有既有承载                                          │
│      │ 查这个坑以前踩过没有                                          │
│      │ 查哲学条款怎么说的                                            │
│      │ 查这条待办修好了没有                                          │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑤ 改代码                                                          通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 建新包 → 走模板生成器，不手搓                                 │
│      │ 改契约 → 只编辑 能力定义.json（唯一可编辑源）                 │
│      │ 改实现 → 改 实现/<包名>.py                                    │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑥ 编译口                                                          半 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 十五项检查全跑得动，约 28 秒                                  │
│      │ 只查自己：--文件 <你改的文件>                                 │
│ 不能 │ 两条红：公开调用完整性、防回潮·反向破坏                       │
│      │ 　都不是你的改动引入，是别人的在途 Swift 那波                 │
│ 注意 │ 存量冻结不算红：契约漂移判红 0／漏声明 126                    │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑦ 定向测试                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 只跑编译口点名的受影响模块，不跑全量                          │
│ 不能 │ 无                                                            │
│ 注意 │ 门禁「通过」只限快速级：真跑级未执行                          │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑧ HTML 全量黑盒                                                   通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 对已编译制品发真实 GET/POST，多线程并发                       │
│      │ 只看 HTTP 状态码和返回值，后端实现它不管                      │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑨ 摘要重算                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 判据是硬的：全部闭合：无可重算项                              │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑩ 文档收口                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 人改也走生成器：草稿 → 校验落盘                               │
│      │ 按类型整份刷：--类型 <类型> --写盘                            │
│ 不能 │ 不许直接手写 md 文件                                          │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑪ 提交                                                            通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ git status 先看全貌 → 只 add 自己改的                         │
│      │ commit 带开工ID，必须带精确路径                               │
│ 不能 │ 无                                                            │
│ 注意 │ 索引全局共享：不带路径会把别人已暂存的带进来                  │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑫ 收工释放                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 按租约 id 释放，返回 释放数／清单／幂等清单                   │
│ 不能 │ 无                                                            │
└──────────────────────────────────────────────────────────────────────┘
                                    ▼
                                    【完】
                                   ▼
                    ┌──────────── 出错了往哪岔 ────────────┐
                    │ 编译口红项 ─→ 看编译口输出，14 项对号 │
                    │ 网关/调用失败 ─→ 看错误码，见细则     │
                    └──────────────────────────────────────┘
```

**图里三条硬结论的取证**（只读，未改代码）：

```bash
# ① 平台此刻可用不可用 —— 看常驻网关真实 HTTP
▷ curl -sS http://127.0.0.1:40007/健康 -H "Authorization: Bearer $TOKEN"
⇒ {"状态":"健康","能力数":720}          ← 720 个能力全部在线

# ② 环境自检那条「0」的口径 —— 它是源码重装，不是常驻网关
▷ ps -o pid,lstart,command -p 5463
⇒ 启动 2026-09-20 22:36:58（已服务 3h+）
▷ git log -1 --format=%ci -- 支持库/适配层/Swift编译器提供者/依赖锁.json
⇒ 2026-09-21 00:50:59   ← 坏锁比网关晚 2h14m 进来，网关没见过它

# ③ HTML 传参并发真的能跑（浏览器那条腿）
▷ 20 路并发 POST /网关/调用
⇒ 总 2.02s ｜ 状态码 200×20
▷ 60 路并发 POST /网关/调用
⇒ 总 3.73s ｜ 200×40 + 429×20（429 = 网关自带限流，是保护不是坏）
```
