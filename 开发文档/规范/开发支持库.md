# 开发支持库 · 流程图

> 最后更新：2026-09-21
> 口径：实测（图里每个数字都可现场复现，命令见《开发支持库 · 细则》）
> 维护者：全局规范
<!-- 机器管理｜类型：规范｜生成器：开发工具.MD文档生成 -->

**图例**：`坏` = 不能用　`半` = 能跑但有坑　`通` = 好用　`注` = 结论框（不占节点位）

**怎么读**：自上而下走一遍；**每节一个框，框内分「能 / 不能」两栏** —— 坏在哪就写在那个节点自己身上，不用往下翻。

```text
╔════════════════════════════════════════════════════════════════╗
║ 开发支持库 · 全流程图                                          ║
║ 每节一个框：框内分「能用 / 不能」；坏在哪就写在那个节点自己身上 ║
╚════════════════════════════════════════════════════════════════╝
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ① 环境自检                                                  坏 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 自检本身好使：九大项逐项探，说不支持就真不支持          │
│ 不能 │ 本机过不了：能力数 0，一个能力都没装配出来              │
│      │ 根因 Swift编译器提供者/依赖锁.json 三字段是空串         │
│      │ 修法(未执行)：重建依赖锁 --写入 --全部                  │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ✦ 为什么是「0」—— 不是缺能力，是装配压根没开始              注 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 全仓 137 个能力定义、720 个能力声明 —— 一个不少         │
│      │ 34 份依赖锁只有 1 份坏（Swift编译器提供者）             │
│      │ 工具实测汇总：需重建 1 份／拒绝 0 份                    │
│      │ 装配本来有单包隔离：坏包只跳过它，其余照常装            │
│      │ （后端核心写着「半成品包不阻断全平台」+ 有专测）        │
│ 不能 │ 它死在装配启动之前：提供者环境段跨包硬门禁 fail-closed  │
│      │ 管理器.py 校验提供者环境 → 失败 → 直接 return           │
│      │ 装配一步都没跑 ⇒ 看起来像「能力全没了」                 │
│ 注意 │ 定性：不是缺原子能力，也不是坏 1 个连带 719 个          │
│      │ 是「一个提供者环境指纹没跟上这台机器」把闸门顶住了      │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ✦ 两个门禁别搞混（我一开始就搞混过）                        注 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 空锁 = 「包 与 直接依赖 均为空」⇒ 单包级，只跳过该包    │
│      │ 指纹 = 锁内 Python／操作系统／CPU 与当前机器一致        │
│      │ Swift 那份两者都有内容 ⇒ 不属于空锁                     │
│ 不能 │ 缺指纹或为空 ⇒ 无法判定 ⇒ 跨包硬门禁 ⇒ 整体阻断         │
│      │ 依据：指南 8.2『提供者环境未就绪』属整体阻断            │
│ 注意 │ 指南 8.1『依赖锁内容为空或非法』= 规则2空锁             │
│      │ 指南 8.2『提供者环境未就绪』= 规则5指纹 ⇒ 两者不冲突    │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ② 开工准备                                                  坏 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 路径边界校验：越界当场拒，不悄悄跳过                    │
│      │ 通用纪律：七段无条件返回，取不到就报                    │
│      │ 文件租约实况：查不出就说「未查询」，不回落放行          │
│ 不能 │ 顶层「项目根」不透传 → 第一次调用必错                   │
│      │ 工作区指纹写死空串（实现有，没接线）                    │
│      │ 代码地图「可用」只看目录在不在 → 过期也报可用           │
│      │ 任务类型字典仅 7 类，没有「调研/审计」                  │
│      │ 「缺失原子能力」是死字面量 → 报的 4 项全不成立          │
│      │ 契约漏声明 4 块（含强制四步开发流程）                   │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ③ 开工即占                                                  通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 原子认领整批路径，任一被占则整体不开工并回报占用者      │
│      │ 同一份文件也能上锁，谁先拿到谁先改                      │
│ 不能 │ 无                                                      │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ④ 动手前先查                                                通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 查全仓有没有既有承载                                    │
│      │ 查这个坑以前踩过没有                                    │
│      │ 查哲学条款怎么说的                                      │
│      │ 查这条待办修好了没有                                    │
│ 不能 │ 无                                                      │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑤ 改代码                                                    通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 建新包 → 走模板生成器，不手搓                           │
│      │ 改契约 → 只编辑 能力定义.json（唯一可编辑事实源）       │
│      │ 改实现 → 改 实现/<包名>.py                              │
│ 不能 │ 无                                                      │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑥ 编译口                                                    半 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 十五项检查全跑得动，约 28 秒                            │
│      │ 只查自己：--文件 <你改的文件>                           │
│ 不能 │ 两条红：公开调用完整性、防回潮·反向破坏                 │
│      │ 　（都不是你的改动引入，是别人的在途 Swift 那波）       │
│ 注意 │ 存量冻结不算红：契约漂移判红 0／漏声明 126              │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑦ 定向测试                                                  通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 只跑编译口点名的受影响模块，不跑全量                    │
│ 不能 │ 无                                                      │
│ 注意 │ 门禁的「通过」只限快速级：真跑级未执行                  │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑧ HTML 全量黑盒                                             通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 对已编译制品发真实 GET/POST，多线程并发                 │
│      │ 只看 HTTP 状态码和返回值，后端怎么实现它不管            │
│ 不能 │ 无                                                      │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑨ 摘要重算                                                  通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 判据是硬的：全部闭合：无可重算项                        │
│ 不能 │ 无                                                      │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑩ 文档收口                                                  通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 人改也走生成器：草稿 → 校验落盘                         │
│      │ 按类型整份刷：--类型 <类型> --写盘                      │
│ 不能 │ 不许直接手写 md 文件                                    │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑪ 提交                                                      通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ git status 先看全貌 → 只 add 自己改的                   │
│      │ commit 带开工ID，必须带精确路径                         │
│ 不能 │ 无                                                      │
│ 注意 │ 索引全局共享：不带路径会把别人已暂存的带进来            │
└────────────────────────────────┬───────────────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ ⑫ 收工释放                                                  通 │
├────────────────────────────────────────────────────────────────┤
│ 能   │ 按租约 id 释放，返回 释放数／清单／幂等清单             │
│ 不能 │ 无                                                      │
└────────────────────────────────────────────────────────────────┘
                                 ▼
                                 【完】
                    ┌──────────── 出错了往哪岔 ────────────┐
                    │ 编译口红项 ─→ 看编译口输出，14 项对号 │
                    │ 网关/调用失败 ─→ 看错误码，见细则     │
                    └──────────────────────────────────────┘
```

**两处 `注` 框的取证**（只读，未改任何代码）：

```bash
# 坏了几份锁 + 谁拒绝 —— 工具自己给权威汇总
▷ python3.14 -m 开发工具.重建依赖锁          # 只报不写
⇒ 目标 34 份｜需重建 1 份（环境漂移 1 份）｜已写入 0 份｜拒绝 0 份
⇒ 未写盘。确认后加 --写入（批量加 --全部）

# 全仓能力声明总数
⇒ 能力定义.json 137 份 ｜ 声明能力总数 720

# 单包隔离机制（坏包只跳过它）
⇒ 后端核心/后端核心.py：「单包级跳过告警（半成品包不阻断全平台）」
⇒ 测试中心/加载器/测试_装配单包隔离.py（专测）

# 死在哪一步（装配启动之前）
⇒ 运行核心/加载器/生命周期管理/管理器.py:380-392
   校验提供者环境 → 失败 → 结果.问题列表 → if 问题列表: return

# 判据分界（指南第八节）
⇒ 开发文档/规范/新建支持库与模块指南.md:373-391
   8.1 只跳过该包：依赖锁内容为空或非法（= 强制校验规则2 空锁）
   8.2 仍整体阻断：适配层第三方提供者缺依赖锁、提供者环境未就绪
```

**结论一句话**：能力一个不缺，是 **1 个提供者的环境指纹没跟上这台机器**，被跨包硬门禁 fail-closed 顶住了闸门 —— 所以看起来像「能力全没了」。
