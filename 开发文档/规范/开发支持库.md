# 开发支持库 · 流程图

> 最后更新：2026-09-21
> 口径：实测（图里每个数字都可现场复现，命令见《开发支持库 · 细则》）
> 维护者：全局规范
<!-- 机器管理｜类型：规范｜生成器：开发工具.MD文档生成 -->

**图例**：`坏` = 不能用　`半` = 能跑但有坑　`通` = 好用　`注` = 结论框（不占节点位）

**怎么读**：自上而下走一遍；**每节一个框，框内分「能 / 不能」两栏** —— 坏在哪就写在那个节点自己身上，不用往下翻。

```text
╔══════════════════════════════════════════════════════════════════════╗
║ 开发支持库 · 全流程图                                                ║
║ 每节一个框：框内分「能用 / 不能」；坏在哪就写在那节自己身上          ║
╚══════════════════════════════════════════════════════════════════════╝
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ① 步骤 0 · 本机能不能跑                                           通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 发 HTTP 请求问常驻网关：curl 40007 /健康 ⇒ 能力数>0 就能跑    │
│      │ 实测：「状态：健康，能力数 720」                              │
│      │ 就是一个 HTTP 请求，任何 HTTP 客户端都能发，不是跑脚本        │
│      │ AGENTS.md 已按此固化（提交 203921fc）                         │
│      │ 不看实现、不看脚本快照 —— 只认真实 HTTP 返回                  │
│ 不能 │ 连不上网关，或能力数 0 ⇒ 判不可跑                             │
│ 注意 │ 这是「问底座」而不是「自己重装一遍」（两者差别见下框）        │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ✦ 「环境自检」报的能力数 0 是什么                                 注 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 环境自检：新起进程按当前源码从零装配的体检                    │
│      │ 实测 21 项：通过 19 ／ 警告 0 ／ 不支持 2，退出码 1           │
│      │ 不支持的两项：依赖锁一致性、装配冒烟                          │
│      │ 坏锁落点：Swift编译器提供者 依赖锁三字段是空串                │
│      │ 全仓 137 个能力定义、720 个能力声明 —— 一个不少               │
│ 不能 │ 所以它报「0」不代表平台不可用                                 │
│      │ 反例就在眼前：它报 0 的同一时刻，                             │
│      │ 　常驻网关 GET /健康 ⇒ 状态健康、能力数 720                   │
│      │ 　搜索能力 ⇒ 总数 540，真调能力成功                           │
│ 注意 │ 判据：平台可用与否以常驻网关真实 HTTP 返回为准                │
│      │ 环境自检只作「照现在源码重开一次行不行」的参考                │
│      │ 其修法（未执行）：重建依赖锁 --写入 --全部                    │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ✦ 验证腿：HTML / HTTP（主线）vs py 脚本（辅助）                   注 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 40007 网关：POST /网关/调用 传参即测，浏览器可发              │
│      │ 实测 20 路并发 → 总 1.84s ｜ 状态码 200×20 全绿               │
│      │ 实测 60 路并发 → 总 3.70s ｜ 200×40 + 429×20                  │
│      │ 　（429 = 网关自带限流，不是坏，是保护）                      │
│      │ → 传参 + 并发 = 真实验收，快且不看内部实现                    │
│ 不能 │ py 脚本单进程串行：一轮十几秒起，还要自己解析输出             │
│      │ → 慢的根源就在这条腿，不该拿它当主验证                        │
│ 注意 │ ⚠ 浏览器直连 40007 会被 CORS 拦（允许来源表默认空）           │
│      │ 　解法：页面与转发同源（不改网关、不重启、不加白名单）        │
│      │ 底座已明写：HTML 是正式发布主判定（华哥 2026-08-29 裁决）     │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ✦ HTTP 获取与写入：四条腿本机实测全通                             注 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 写：文件管理.写入文件 —— 6ms，自动建父目录，落盘可比对        │
│      │ 读：文档读取.结构化读取 —— 4ms，原文原样读回                  │
│      │ 列：文件管理.列出文件 —— 返回目录条目                         │
│      │ 删：文件管理.删除文件 —— 幂等（不存在也算成功）               │
│      │ 实测链路：HTTP 写 → HTTP 读回原文一致 → 磁盘字节一致 → 删净   │
│ 不能 │ 不存在「必须先起页面才能读写」这回事                          │
│      │ 页面只是人看的壳；能力本身 HTTP 直达，页面与 curl 同一入口    │
│ 注意 │ 浏览器页面直连要同源转发（见上框 CORS）；同源后与 curl 等价   │
│      │ 少数动作目前只有 py 脚本腿、无能力入口（缺口，见 ⑨ 框）       │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ② 开工准备                                                        坏 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 路径边界校验：越界当场拒，不悄悄跳过                          │
│      │ 通用纪律：七段无条件返回，取不到就报                          │
│      │ 文件租约实况：查不出就说「未查询」，不回落放行                │
│      │ 撞车保护：同路径被别人占着就整体不开工，并回报占用者          │
│ 不能 │ 顶层「项目根」不透传 → 第一次调用必错（缺必填参数 项目根）    │
│      │ 工作区指纹实测为空串（证据可信度、工作区快照两处都空）        │
│      │ 代码地图「可用」只看目录在不在 → 待同步 22 个仍报可用         │
│      │ 任务类型字典仅 7 类，传「调研」实测命中 0 类、规范 0 条       │
│      │ 「缺失原子能力」是死字面量 → 恒报 4 项，其中 2 项实测已登记   │
│      │ 契约漏声明 4 块（含强制四步开发流程）                         │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ③ 开工即占                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 原子认领整批路径，任一被占则整体不开工并回报占用者            │
│      │ 同一份文件也能上锁，谁先拿到谁先改                            │
│      │ 实测：认领 1 份 → 已开工=真、租约id、认领数 1                 │
│      │ 实测撞车：同路径再占 → 已开工=假，并点名占用者与租约id        │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ④ 动手前先查                                                      通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 查全仓有没有既有承载（查询索引，实测索引已建）                │
│      │ 查这个坑以前踩过没有（查开工上下文）                          │
│      │ 查哲学条款怎么说的（查哲学条款）                              │
│ 不能 │ 查「这条待办修好了没有」—— 实测无此能力，零命中               │
│ 注意 │ 索引新鲜度实测「部分过期」：按开发流程应先重建索引再搜        │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑤ 改代码                                                          通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 建新包 → 走模板生成器，不手搓                                 │
│      │ 改契约 → 只编辑 能力定义.json（唯一可编辑源）                 │
│      │ 改实现 → 改 实现/<包名>.py                                    │
│ 不能 │ 无                                                            │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑥ 编译口                                                          半 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 十五项检查全跑得动；单项耗时实测合计约 40 秒                  │
│      │ 只查自己：--文件 <你改的文件>                                 │
│ 不能 │ 两条红：公开调用完整性、防回潮·反向破坏                       │
│      │ 　都不是你的改动引入，是别人的在途 Swift 那波                 │
│ 注意 │ 存量冻结不算红：契约漂移判红 0／漏声明 126                    │
│      │ 只报告类：测试伪装门禁 1 处、md查重（降噪器非裁决器）         │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑦ 定向测试                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 只跑编译口点名的受影响模块，不跑全量                          │
│ 不能 │ 无                                                            │
│ 注意 │ 门禁「通过」只限快速级：真跑级未执行                          │
│      │ 验证计划要传目录级目标；传单个 md 文件实测报 目标不存在       │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑧ HTML 全量黑盒                                                   通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 对已编译制品发真实 GET/POST，多线程并发                       │
│      │ 只看 HTTP 状态码和返回值，后端实现它不管                      │
│ 不能 │ 无                                                            │
│ 注意 │ 验证器必须传 --制品 或 --制品指针；都不传直接报错退出         │
│      │ 制品指针实测在 工程缓存/制品仓库/平台客户端制品/当前.json     │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑨ 摘要重算                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 判据是硬的：全部闭合：无可重算项（实测退出 0）                │
│      │ 经 HTTP 调：系统核心支持库.进程管理.执行命令（实测退出码 0）  │
│      │ 参数先探再填：命令=文本型 shell 串／超时秒=双精度数型         │
│ 不能 │ 直接 python3.14 脚本.py 起 py 腿 —— 全砍（单腿铁律无豁免）   │
│ 注意 │ 「开发期本地命令按设计如此」的旧说法已作废：那是探索不足      │
│      │ 通用执行腿一直在（执行命令），一次搜不到要继续往下搜          │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑩ 文档收口                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 人改也走生成器：草稿 → --人工改 --写盘（实测落盘成功）        │
│      │ 按类型整份刷：--类型 <类型> --写盘                            │
│ 不能 │ 不许直接手写 md 文件（门禁实测通过：改动集无手写）            │
│ 注意 │ 规范类型无可算生成区，整份属人工区，生成器只做判据与印记      │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑪ 提交                                                            通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ git status 先看全貌 → 只 add 自己改的                         │
│      │ commit 带开工ID，必须带精确路径                               │
│ 不能 │ 无                                                            │
│ 注意 │ 索引全局共享：不带路径会把别人已暂存的带进来                  │
└───────────────────────────────────┬──────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│ ⑫ 收工释放                                                        通 │
├──────────────────────────────────────────────────────────────────────┤
│ 能   │ 按租约 id 释放，返回 释放数／清单／幂等清单                   │
│      │ 实测：释放 1 份 → 释放数 1，清单带内容指纹                    │
│ 不能 │ 无                                                            │
└──────────────────────────────────────────────────────────────────────┘
                                    ▼
                                    【完】
                                    ┌──────────── 出错了往哪岔 ────────────┐
                                    │ 编译口红项 ─→ 看编译口输出，14 项对号 │
                                    │ 网关/调用失败 ─→ 看错误码，见细则     │
                                    └──────────────────────────────────────┘
```

**步骤 0 的取证**（AGENTS.md 已按此固化，提交 `203921fc`）：

```bash
TOKEN="$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - \
         ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)"
curl -sS http://127.0.0.1:40007/健康 -H "Authorization: Bearer ***"
⇒ {"状态":"健康","能力数":720}       # 能力数 > 0 ⇒ 本机可跑
```

**验证腿的取证**（20/60 路并发，均当轮实测）：

```bash
▷ 20 路并发 POST /网关/调用 ⇒ 总 1.84s ｜ 状态码 200×20
▷ 60 路并发 POST /网关/调用 ⇒ 总 3.70s ｜ 200×40 + 429×20（429=网关限流保护）
```

**HTTP 获取与写入的取证**（四条腿，均经 `POST /网关/调用`，当轮实测）：

```bash
▷ 写  能力id=文件管理.写入文件        ⇒ 成功 ｜ 6ms ｜ 自动建父目录、落盘可比对
▷ 读  能力id=文档读取.结构化读取      ⇒ 成功 ｜ 4ms ｜ 写入内容原样读回
▷ 列  能力id=文件管理.列出文件        ⇒ 成功 ｜ 返回目录条目
▷ 删  能力id=文件管理.删除文件        ⇒ 成功 ｜ 幂等（不存在也算成功）
# 链路实证：HTTP 写 → HTTP 读回原文一致 → 磁盘字节一致 → HTTP 删净
```

**环境自检的取证**（21 项，当轮实测）：

```bash
▷ python3.14 -m 开发工具.环境自检
⇒ 共 21 项：通过 19｜警告 0｜不支持 2，退出码 1
   不支持的：3 依赖锁一致性（Swift编译器提供者三字段空串）、8 装配冒烟（能力数 0）
```
