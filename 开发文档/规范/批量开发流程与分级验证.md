---
名称: 批量开发流程与分级验证
标签: [开发流程, 分级验证, 资源释放, 编译]
创建时间: 2026-08-29T07:25:50.686076+00:00
更新时间: 2026-09-16（按当前唯一操作面重写；原版整段挂在已清场的 8766 管理端 MCP 工具上，照做不可执行）
---

# 批量开发流程与分级验证

## 一、唯一操作面（2026-09-15 D 清场后）

| 用途 | 唯一入口 |
|---|---|
| 能力发现 / 调用 | stdio 薄壳 3 工具：`capability_search` / `capability_call` / `tool_catalog`（`开发工具/薄壳/薄壳服务.py`，**不监听端口**） |
| 执行 | `POST http://127.0.0.1:40007/网关/调用` —— **唯一执行腿**（无凭证 401，带凭证 200） |
| 本机可用性 | `python3.14 -m 开发工具.环境自检`（21 项，有不支持项即非零退出） |
| 单次变更编译 | `python3.14 -m 开发工具.项目编译.项目编译器` —— 只编译本次变更的小单元 |
| 全量验收 | `python3.14 -m 开发工具.HTML验证.验证器 --并发 32` —— **唯一允许的「全量」** |
| 摘要重算 | `python3.14 -m 开发工具.全量重算摘要 [--只报]` —— 改过包后必跑（会级联重算聚合父包） |
| 依赖锁重建 | `python3.14 -m 开发工具.重建依赖锁 [--执行]` —— 换平台/换环境后跑，默认干跑 |

> **已删除、任何文档与任务书都不得再引用**：8766 管理端 / 8866 / `MCP工具箱/` /
> `tool_job_submit` / `apply_file_patch` / `development_start` / `file_lease` /
> `claim_capability` / `workspace` / `mcp_feedback` / `verify_and_record` /
> `project_context` / `codegraph_explore`。
> 只读能力发现网关 40006 **当前未启用**（无监听，launchd `com.huashi.gateway-40006` 退出码 2）。

## 二、分级验证纪律（禁止每个小改动重复跑全量）

1. **步骤 0**：`python3.14 -m 开发工具.环境自检` —— 先确认本机能不能跑，有不支持项就别往下走。
2. **定位影响范围**：读目标包的 `说明/设计说明.md` 与能力契约；找能力面用薄壳 `capability_search`。
3. **冻结工作包**：同一目标的改动放一个包；动手前登记临时开发文档，写明**允许修改目录**与回退方式。
4. **按级跑，不越级**：
   - **小改动** → 只跑受影响的编译器检查与契约检查 + 该包定向冒烟；
   - **一批同类** → 跑受影响波次；
   - **全部工作包完成** → 才跑一次 HTML 高并发全量；
   - **正式发布前** → 编译制品 + `运行发布门禁`。
5. **验收必须走真实 HTTP**。**禁止**用缓存命中、单测全绿、文本匹配或子代理自述冒充真实通过。
6. **收工**：`git add` **只加自己改的文件**（**禁止 `git add -A`**）；提交信息写清「改了什么 / 为什么 / 怎么验的」；
   改了包必须跑 `全量重算摘要`，全仓判据为「不闭合 0/N」。

## 三、资源释放（统一遵循）

```text
请求结束 → 有界等待 → 必要时强杀 → 确认进程/线程已结束 → 关闭管道和临时资源 → 删除权威记录
```

- **未收敛必须保留账本**；禁止伪装成功、先删记录、或先启动替代进程。
- 资源回收**按状态机登记的归属**处理（资源指纹 / 归属），**禁止按端口号盲杀**。

## 四、本份为什么被重写（踩坑记录）

原版全文只有一段，流程第 1、2、6 步与资源释放段**整段挂在 2026-09-15 已清场的 8766 管理端 MCP 工具**上
（`project_context` / `codegraph_explore` / `mcp_feedback` / `verify_and_record`）—— **照做完全不可执行**。
规范一旦指向不存在的东西，就会长期没人遵循。本份按当前唯一操作面重写，**纪律原意全部保留**，只换操作面。
同类教训见 `开发文档/规范/多会话并行开发规约.md`。
