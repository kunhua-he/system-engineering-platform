# 底座全盘复查问题清单（批次三）

> 本批只记录问题和证据，不把现有测试通过当成修复完成。范围覆盖易语言架构映射、支持库/模块库、运行核心、统一网关、发布门禁、测试编排和 IDE/编译入口。未使用 V3 MCP。

## 流程图

```text
易语言 ARCHITECTURE.md + 当前源码
                 |
                 v
静态契约/重复 owner/入口扫描
                 |
                 v
门禁反向破坏 + 真实服务 + 进程组/端口/句柄清理
                 |
                 +--> 失败证据归档（本文件）
                 |
                 v
批次 A：假绿与资源治理
                 |
                 v
批次 B：支持库唯一 owner 与六环契约
                 |
                 v
批次 C：IDE 工程树、编译制品和运行入口
                 |
                 v
全量复查 -> 新问题继续归档 -> 下一批
```

## A. 门禁与运行治理阻断

| 编号 | 严重度 | 落点 | 证据/复现 | 修复落点 |
|---|---|---|---|---|
| A1 | P0 | `开发工具/发布门禁/运行发布门禁.py:321-329` | macOS `grep -r -P` 返回码为 2，门禁只读 stdout，仍可能判中文边界通过 | 改为 Python AST/文件扫描；任何子进程非 0、stderr 或解析失败阻断 |
| A2 | P0 | `运行核心/统一网关/本地网关.py:20-57` | 配置端口 `4780` 可直接监听，未接入端口策略 | 统一调用端口保留策略；保留端口拒绝，绑定失败不随机回退 |
| A3 | P0 | `支持库/前端/浏览器宿主/实现/浏览器宿主.py:25-27` | 任意 `OSError` 静默改绑端口 0 并返回成功；端口占用/保留端口被伪装成功 | 只允许明确的端口 0 自动分配；指定端口失败必须结构化失败 |
| A4 | P0 | `开发工具/发布门禁/运行发布门禁.py:692-701` | 任意调用者注入 `系统级支持库_残留审计_递归防护=1` 即可跳过慢速真实执行 | 递归标记绑定事务、父 PID、源码摘要和 nonce；无法核验即阻断 |
| A5 | P1 | `开发工具/发布门禁/运行发布门禁.py:76-82` | `subprocess.run(timeout)` 返回 -1 后孙进程仍存活，实测存在 sleep 残留 | `start_new_session=True`，超时杀进程组并验证归零；保留 stdout/stderr 证据 |
| A6 | P1 | 浏览器门禁 `运行发布门禁.py:459-466` | 只检查 HTML 字符串，不启动 HTTP 或发起调用；地址 `127.0.0.1:1` 仍可成功 | 启动隔离服务，真实 GET/POST/能力返回和优雅停止必须通过 |
| A7 | P1 | `运行核心/任务调度/任务进程.py:253-261,196-197` | `_完成` 先改终态后持久化；写盘失败被监视线程吞掉，活动表/资源可能不回收 | 持久化失败进入明确失败终态，强制从活动表移除并验证资源归零 |
| A8 | P1 | `运行核心/资源协调/服务.py:264-284` | 清理快照使用 `ignore_errors=True` 后无论是否删除成功都记为完成；恢复缺失快照捕获异常后继续，调用方可收到空列表误判成功 | 清理失败保留错误证据并返回失败；恢复缺失项必须是明确失败结果 |
| A9 | P1 | `运行核心/加载器/提供者隔离/独立进程.py:74-86,146-151,244-270` | 隔离环境异常静默回退系统解释器；停止忽略成功字段/退出码；子进程未独立进程组，强杀等待异常未结构化 | 隔离失败 fail-closed；进程组启动/停止/回收有退出码和零残留证据 |
| A10 | P1 | `测试中心/运行测试.py:521-545` | 目录扫描超过 100 项返回空表，阶段只依赖弱缓存；依赖增删可能继续命中缓存 | 超限直接阻断或生成完整目录摘要，禁止空表进入缓存成功 |
| A11 | P0 | `测试中心/运行测试.py:132-146` | `git status` 非零时保留 blob 映射且脏集合为空，工作区改动可被当成干净文件而命中缓存 | 任一 Git 指纹失败清空 blob 映射并回退现场摘要，写入失败证据 |
| A12 | P0 | `运行核心/能力调用/唯一能力调用.py:160-173` | 实现返回失败结果/`成功=False` 时调用历史仍记录 `成功=True`，统一证据链假绿 | 调用边界规范化结果并以真实成功字段记录证据，非法返回阻断 |

## B. 唯一能力与契约阻断

| 编号 | 证据 | 当前问题 |
|---|---|---|
| B1 | `P0-能力重复扫描.json` | 198 条声明、195 个能力 id；`文字文档.解析文字文档`、`演示文稿.解析演示文稿`、`表格文档.解析表格文档` 各有两个 owner |
| B2 | `开发工具/公开调用完整性门禁.py` | 当前实测 65 项违规：13 个后端/11 个模块缺 `能力定义.json`，模板包误纳入正式扫描，另有同义能力跨包重复 |
| B3 | `开发工具/依赖生命周期审计/运行审计.py` | 17 个标准 Provider 缺健康探针；5 个 Provider 缺停止/关闭/释放证据；FFmpeg/PDF隔离混装多个第三方 |
| B4 | `支持库/后端/{文字文档,演示文稿,表格文档}/包声明.json` | 唯一后端 owner 当前带 `已废弃=true`，与架构文档“保留后端 owner”相反；Provider 仍注册同一公开 id，实际装配事实与文档分叉 |
| B5 | 多个声明 | 类型仍混用 `布尔/数字/任意`，未统一到核心中文类型词典；参数/返回/错误/状态/资源预算不能由单一契约生成 |

## C. 易语言架构映射缺口

| 编号 | 证据 | 当前问题 |
|---|---|---|
| C1 | `开发文档/源码参考研究/易语言/ARCHITECTURE.md`“命令索引是 ABI” | 当前能力契约没有独立、不可重排的命令序号/历史隐藏项，升级可能重排能力数组 |
| C2 | 同文档“结构化工程树/编辑期-编译期-运行期三边界” | IDE 页面 JSON 主要是组件列表，缺程序结构、子程序、变量、资源、编译目标和源位置映射 |
| C3 | `开发工具/项目编译/项目编译器.py` | 编译器能复制运行时和引用包，但未对“仅打包实际使用能力”的结果做反向删除验证；生成启动器与开发网关契约需统一 |
| C4 | `开发工具/轻代码前端编辑器/启动编辑器.py` | 编辑器是单页组件视图，缺稳定节点 id 的操作日志/冲突合并/撤销持久化证据；Agent 与用户入口尚未由同一工程网关真实验证 |
| C5 | P0 | `开发工具/快速编译/运行快速编译.py:112-152` | 临时包仅缺参数/入口/能力契约仍返回 0、问题为空；未校验注册入口和聚合契约 | 快速编译必须读取并严格比对声明、能力定义、契约、注册入口、返回和错误 |
| C6 | P0 | `开发工具/项目编译/项目编译器.py:87-92` | 生成 HTML 平铺组件，丢失父容器、尺寸、事件、参数模板；设计预览与制品行为不一致 | 编译器按结构树递归渲染，事件按契约映射参数，设计态/运行态同一 IR |
| C7 | P1 | `开发工具/项目编译/项目编译器.py:204-210` | 多页面只取排序后的第一个 JSON 生成 index，其他页面无 HTML/路由 | 生成页面路由表和每页制品，缺失/冲突路由阻断 |
| C8 | P1 | `开发工具/项目编译/项目编译器.py:147-153` | 能力归属用 `setdefault` 静默选重复 owner | 重复公开能力直接阻断编译，必须按唯一注册表解析 |
| C9 | P1 | `开发工具/项目编译/项目编译器.py:164-177,181-190` | 只读包声明依赖，漏读依赖契约；固定复制整个运行时适配层，清单与实际制品不闭合 | 合并并校验依赖契约，按实际依赖闭包裁剪或以机器清单证明每个文件必要 |
| C10 | P1 | `开发工具/项目编译/项目编译器.py:89-91` | 标题/组件文本插入 HTML/脚本模板，特殊文本可破坏脚本并形成注入 | 使用 JSON 安全编码和 DOM textContent，禁止未经编码的 innerHTML 数据插值 |
| C11 | P2 | `开发工具/轻代码前端编辑器/启动编辑器.py:260-266` | 页面 JSON 解析失败静默回退空白 IDE，用户看见“成功启动”但数据丢失风险 | 解析失败明确错误页/非零启动，不回退空白工程 |
| C12 | P2 | IDE/编译入口 | IDE 编译页、项目编译器、快速编译器三套入口规则不一致 | 收口到快速编译契约；IDE 和项目编译器只调用同一验证核心 |
| C13 | P0 | `开发工具/轻代码前端编辑器/编译页面.py:19-46` | 未知组件、缺失父组件、树环、未注册事件能力仍返回 0；能力依赖仅被收集未被契约验证 | 页面 IR 编译先做父子/环/组件目录/事件能力注册校验，失败即阻断并由所有入口复用 |
| C14 | P1 | `开发工具/轻代码前端编辑器/编译页面.py:25-46` | 只去重对象 id，不去重组件 id；父子绑定/删除按组件 id 查找会错绑 | 页面内组件 id 与对象 id 均唯一，父组件必须存在且为容器 |
| C15 | P1 | `开发工具/快速编译/运行快速编译.py:112-152` | 快速编译只校验浅层声明，缺能力契约/入口/真实返回验证 | 统一编译验证核心，缺任一环节非零退出 |

## D. 本轮新增问题（易语言权威文档增量复读）

本节只追加当前工作树的新证据，不覆盖前面已经记录的问题。易语言权威文档当前为
`开发文档/源码参考研究/易语言/ARCHITECTURE.md`（2349 行，2026-08-22 21:08 更新，当前快照摘要
`23590b6f...`；该文件不在 Git HEAD，不能宣称相对上一版的差异）；它新增的三套制品、帮助目录、
IDE 用户态扩展和多源工程流程，要求平台的编辑、编译、运行契约不能各自分叉。

| 编号 | 严重度 | 落点 | 证据/复现 | 修复落点 |
|---|---|---|---|---|
| D1 | P1 | `运行核心/运行环境管理器/环境管理器.py:203-239` | 当前新增的空锁/缺锁 fail-closed 与同文件原有“空依赖回退系统解释器”契约、无第三方 Provider 工作包测试冲突；定向测试 52 项中 2 失败 1 错误。 | 明确“缺锁”“空锁”“显式无第三方依赖”三种状态；只允许有明确声明的纯标准库 Provider 回退系统解释器，其他缺锁/空锁阻断。 |
| D2 | P1 | `开发工具/组件合规/合规测试包.py:663-678` | 对没有必填参数的能力仍构造“缺必填参数”请求，导致 12 个正式包报 12/13；发布门禁退出 1，但失败是验证器误报。 | 仅当契约存在必填参数时省略一个必填项；无必填参数能力必须验证默认参数路径，不得伪造缺参场景。 |
| D3 | P1 | `测试中心/运行测试.py:165-173` | 目录摘要排除 `完整性摘要.json`；只修改完整性清单时阶段缓存摘要不变，可复用旧绿。 | 将完整性摘要纳入目录指纹，并增加“只改摘要即缓存失效”反向测试。 |
| D4 | P1 | `启动监督器/健康监督.py:416-423` | 健康证据 JSON 缺字段或解析失败默认按“成功/健康”处理，裁剪时可被删除；临时坏行复现为缺失行被 TTL/条数裁掉。 | 关键字段缺失/解析失败按损坏失败处理并保护留存，不能作为健康成功记录。 |
| D5 | P1 | `支持库/核心/类型转换/能力契约/参数契约.json` 与多个活跃包声明 | 活跃契约使用“文本/布尔/整数/数字/任意”等旧词，核心权威类型为“文本型/逻辑型/整数型/双精度数型”；类型转换包声明还缺参数类型，包声明与参数契约存在多组不一致。 | 建立唯一类型枚举和生成源，统一包声明、能力契约、搜索数据和 IDE 展示；迁移完成前禁止新增能力。 |
| D6 | P1 | `支持库/后端/{文字文档,演示文稿,表格文档}/包声明.json` 与对应 Provider | 已废弃后端包仍保留公开能力，与 `python_docx/python_pptx/openpyxl` Provider 重复 owner；静态能力目录仍有两套事实。 | 保留一个公开支持库 owner，Provider 只实现不注册同名公开能力；旧包实现和派生物在迁移后删除。 |
| D7 | P1 | `开发工具/项目编译/项目编译器.py:39-79,147-153,204-210` | 编译器扫描项目下全部 JSON（会吸入缓存/历史模型）、`setdefault` 静默选择重复 owner、只读取第一个页面 JSON，制品平铺组件并丢失嵌套/事件/多页面路由。 | 只扫描声明的输入目录；重复 owner、路由冲突、依赖缺失直接阻断；以统一页面 IR 生成全部页面和结构树。 |
| D8 | P1 | `开发工具/轻代码前端编辑器/编译页面.py:19-56` | 未校验组件目录、父组件存在性/容器类型、树环、事件能力注册和组件 id 唯一性，未知组件仍可生成成功制品。 | 页面编译与 IDE 工程网关共用严格校验核心，所有入口对未知组件/父子/未注册能力 fail-closed。 |
| D9 | P1 | `MCP工具箱/启动HTTP服务.sh:7` | 直接执行脚本即因中文变量赋值报 `command not found`，8766 无法按文档启动；本轮只能绕过脚本直接启动 Python 服务。 | 改为 POSIX 合法变量名并保留中文参数语义，增加脚本启动与 8766 健康探针测试；绝不使用 4780。 |
| D10 | P2 | `支持库/前端/浏览器宿主/能力契约/参数契约.json` 与 `实现/浏览器宿主.py:28-32` | 契约声明端口占用自动选可用端口，实现在指定端口占用时直接失败；语义漂移。 | 统一为“端口=0才自动分配；指定端口失败即失败”，并复用保留端口策略。 |
| D11 | P2 | `运行核心/统一网关/本地网关.py:223-232` | 端口可用探测未调用公共保留端口策略，4780 空闲时可能错误报告“可用”。 | 探测与实际监听都调用同一端口策略，4780 永远拒绝。 |
| D12 | P1 | `支持库/后端/网页解析/实现/网页解析.py` | 同一处理回调存在重复定义，后定义覆盖前定义；当前测试未覆盖被覆盖分支。 | 删除重复实现，补 AST/行为测试，保留唯一回调 owner。 |

本轮验证边界：`4780` 由 ClashX 占用，项目未监听；`8766` 仅运行项目自己的
`system_engineering_toolkit`，未使用 V3 MCP。上述问题均需修复后重新跑对应工作包，不能以当前
定向测试通过替代全量收口。

## 批次顺序

1. A1-A7：先修假绿、端口、超时进程组、任务持久化和真实浏览器验证；批后跑慢速与资源残留审计。
2. B1-B5：冻结唯一 owner，清理/迁移旧声明和模板扫描边界，生成统一类型与六环派生物；批后跑公开调用完整性、依赖生命周期、反向破坏。
3. C1-C4：补项目 IR/稳定命令索引/编译依赖闭合和双入口真实链路；批后跑 IDE、编译制品和全量门禁。

任何批次发现的新问题追加到本文件，不在未完成批次中宣称全绿。

## E. 本批次全盘扫描补充（2026-08-22）

扫描范围：`支持库/**`、`模块库/**`、`运行核心/**`、`MCP工具箱/**`、`开发工具/**`；未调用 V3 MCP，未访问 4780。

| 编号 | 严重度 | 证据 | 当前问题 | 第一批处理 |
|---|---|---|---|---|
| E1 | P0 | 53 个 `包声明.json`，213 条能力声明，210 个唯一能力 id | `文字文档.解析文字文档`、`演示文稿.解析演示文稿`、`表格文档.解析表格文档` 各有两个 owner | 保留一个公开支持库 owner，Provider 只作为实现，不注册同名能力 |
| E2 | P0 | `支持库/`、`模块库/` 共 122 个 JSON 文件命中旧类型字段，快速编译工作区退出码 10 | `文本/整数/布尔/浮点数/数字/任意/字典/列表/字节集` 与公共类型目录并存 | 以公共类型目录为唯一机器表，先迁移正式契约，旧词直接删除 |
| E3 | P1 | `开发工具/契约编译/聚合契约解析.py:14-15` | 严格类型表仍是旧类型；合法 `文本型/逻辑型/句柄型` 会被拒绝 | 改为加载公共类型目录导出的唯一枚举 |
| E4 | P1 | `开发工具/契约编译/能力定义编译器.py:1-16`、现有 53 包 | 只有部分包有 `能力定义.json`，包声明和参数契约仍是双写事实源 | 先阻断缺能力定义的正式包，再按包生成派生物，不做兼容双写 |
| E5 | P1 | `支持库/前端/组件控件` | 缺 `说明/目录树与命令.md` | 补齐包说明后才允许进入正式扫描 |
| E6 | P1 | `MCP工具箱/能力网关.py:24,127-131` 与 `运行核心/统一网关/{本地网关.py,流式HTTP.py}` | 存在第二套 HTTP 能力网关和独立路由/错误/资源语义 | 删除旧能力网关公开监听，统一委托唯一网关核心 |
| E7 | P1 | `开发工具/轻代码前端编辑器/启动编辑器.py:316-326` | “编译工程”只生成页面 JSON，不产生独立制品和依赖闭包 | 统一调用项目编译器，输出独立产物清单 |
| E8 | P1 | `开发工具/项目编译/项目编译器.py` 运行入口 | 独立制品启动时执行 `pip install`，无法脱离开发网关/网络独立运行 | 编译时锁定并验证环境；运行期禁止 pip，缺依赖直接失败 |
| E9 | P0 | `运行核心/统一网关/本地网关.py:165-170` | `/健康` 不调用真实网关核心，注入空核心仍返回健康 | 健康路由必须通过真实后端状态，缺失或失败返回非成功 |
| E10 | P1 | `开发工具/开发入口.py:121-136` | 启动失败被忽略，且无 finally 关闭后端，可能残留资源 | 启动失败立即返回；所有执行路径 finally 关闭并验证归零 |
| E11 | P1 | `开发工具/轻代码前端编辑器/启动编辑器.py:380-384` | 指定端口异常静默回退随机端口 | 只有显式端口 0 才允许随机；占用/权限失败必须阻断 |

本批次扫描的 `python3.14 -m py_compile` 退出码为 0；该结果只证明语法可解析，不覆盖上述契约、唯一 owner、真实健康、制品独立性和资源释放要求。

## F. 批次一修复记录：类型契约收口

### 流程图

```text
公共类型目录
      |
      v
契约解析器/快速编译器切换正式类型表
      |
      v
包声明、参数契约、能力数据、模块注册迁移
      |
      v
定向契约测试
      |
      v
工作区快速编译复查
      |
      +--> 重复 owner 仍阻断，进入批次二
```

### 已完成

- 新增 `公共契约/基础类型/类型表.py`，作为机器可读正式类型唯一来源。
- `聚合契约解析.py`、`快速编译/运行快速编译.py`、`漂移检测.py` 不再把旧短类型当正式类型。
- 迁移支持库/模块库 JSON 中明确可判定的 `文本/整数/布尔/浮点数/数字/字典/列表/字节/空/结果` 类型。
- 将前端回调契约改为 `句柄型`，文档生成参数改为 `字典型`，OCR 取消事件改为 `资源引用型`。
- 四个模块入口改为读取本包参数契约，不再以 `任意` 注册运行时参数；统一返回 `结果型`。
- 更新契约编译测试夹具与断言，删除旧类型期望。

### 验证

```text
python3.14 -m py_compile（受影响文件） -> 0
测试中心/运行测试.py --测试文件 测试中心/开发工具/测试_契约编译搜索.py --并行数 1 -> 14/14 通过
开发工具/快速编译/运行快速编译.py --范围 工作区 -> 退出码 20
```

退出码 20 只剩 3 个重复 owner：文字文档、演示文稿、表格文档的后端包与 Provider 重复。类型迁移本批次不再有旧类型阻断；重复 owner 是批次二的唯一 P0 阻断。
