# 底座升级批次 5 全量复扫结果

```text
批次5修复
  |
  +--> 能力定义 25 包补齐 / 派生物重建
  +--> 公开扫描边界收口
  +--> 编译依赖闭包 + 制品依赖锁
  +--> 独立制品零 pyc 启动
  |
  v
全量复扫
  +--> 局部工作包：通过
  +--> 公开完整性：36 -> 8
  +--> 全量测试：93 项中 2 项失败
  +--> 发布门禁：失败
  |
  v
下一批：唯一 owner 迁移、声明/注册计数闭合、真实调用参数修复、Provider 摘要重建
```

## 本批已完成并验证

- `测试中心/开发工具/测试_能力定义编译器.py`：13/13，退出码 0。
- `测试中心/开发工具/测试_项目编译器.py`：3/3，退出码 0。
- `测试中心/开发工具/测试_公开调用完整性门禁.py`：19/19，退出码 0。
- `开发工具/公开调用完整性门禁.py`：违规从 36 项降为 8 项。
- 新鲜独立制品的依赖闭包、`依赖锁.json` 和启动零 `.pyc` 已验证。

## 全量复扫证据

### 常规全量测试

命令：

```bash
python3.14 测试中心/运行测试.py
```

结果：退出码 1，93 项中 2 项失败。

1. `测试中心/项目适配/测试_项目适配层.py::test_有效绑定`
   - `文字文档.解析文字文档` 被后端 owner 与 Provider 宿主同时声明。
   - 这是 Provider 公开 owner 收口后暴露的适配层绑定问题。
2. `测试中心/第三阶段/测试_第三阶段验证场景.py::test_联合装配`
   - `声明能力数=138`，`已注册能力数=201`。
   - 能力定义派生后，装配统计把非公开/重复注册能力纳入注册计数，声明集合与注册集合未使用同一公开边界。

### 发布门禁

命令：

```bash
python3.14 开发工具/发布门禁/运行发布门禁.py
```

结果：退出码 1。

仍需修复的真实问题：

- 公开能力同义冲突 8 项：文件系统/文件管理 7 项，文档转换/媒体处理的“检查提供者”1 项。
- 真实调用参数与生成注册入口不闭合：PDF文档、文字文档、演示文稿、表格文档、类型转换、OCR、图像处理、媒体处理、媒体转写、文件管理、文档解析、网页分析、自修复工具等成功路径出现 `missing required parameter`。
- 资源管理成功场景使用不存在的测试路径，导致真实返回失败。
- 多个 Provider 的 `完整性摘要.json` 对 `__init__.py` 漂移；需重新生成摘要并反向验证。
- 慢速层退出码 1，即使阻断项数显示 0，也不能视为通过。

## 下一批唯一修复清单

| 顺序 | 落点 | 修复目标 | 验收 |
|---:|---|---|---|
| 1 | `项目适配层/支持库绑定/` 与 Provider 声明 | Provider 只能作为后端 owner 的实现依赖，不得作为第二宿主参与绑定 | `测试_项目适配层.py` 全通过，重复宿主为 0 |
| 2 | `运行核心/加载器/生命周期管理/管理器.py` | 声明能力数、注册能力数只统计正式公开 owner；Provider 内部能力不进入公开计数 | 第三阶段联合装配计数相等 |
| 3 | 正式包 `__init__.py` 与 `能力契约/参数契约.json` | 逐能力按契约参数调用，修复生成入口与实现函数签名漂移；不通过默认空值掩盖缺参 | 权威合规真实调用 13/13 |
| 4 | `支持库/后端/资源管理` 验证场景 | 使用真实存在且受控的测试输入，失败路径仍保持 fail-closed | 资源管理真实成功/失败均有证据 |
| 5 | `支持库/适配层/*提供者/完整性摘要.json` | 以当前文件清单重建摘要，再做篡改反向验证 | 摘要一致性全部通过 |
| 6 | 慢速阶段失败原因 | 定位退出码 1 的慢速场景，不能用“阻断项数 0”放行 | 慢速命令退出码 0 |

## 证据边界

本文件记录的是批次5修复后的全量现状。批次5不再继续修复上述新问题；它们已经成为批次6的完整输入。当前项目仍不得宣称发布全绿。
