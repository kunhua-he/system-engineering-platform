# 底座升级批次 7 全量复扫结果

```text
批次7修复
  |
  +--> 文档/PPT Provider 内部能力迁移
  +--> 文件管理/媒体处理同义名称收口
  +--> Provider 测试迁移到内部契约
  |
  v
全量复扫
  +--> 公开调用完整性：通过，0 项违规
  +--> Provider/文档定向测试：通过
  +--> 全量项目测试：93 项中 12 项失败
  |
  v
下一批：文档生成依赖闭包与遗留装配、示例项目、Agent 诊断链路
```

## 已验证

- `开发工具/公开调用完整性门禁.py`：退出码 0，六环一致。
- 文档/PPT Provider、文档解析模块相关测试：30/30 通过。
- 文件管理/媒体处理及公开门禁工作包：60/60 通过。
- 未访问、未监听 `4780`。

## 全量复扫

命令：`python3.14 测试中心/运行测试.py`

结果：退出码 1，项目装配阶段 93 项中 12 项失败。

主要失败落点：

1. `模块库.文档生成` 依赖 `文字文档.生成文字文档`、`演示文稿.生成演示文稿`，但公开 owner 已迁移为 Provider 内部能力/后端生成支持库，装配报告“无提供者”。模块声明仍是旧依赖，违反模块不直接绑定 Provider 的边界。
2. 示例项目与项目适配层、第三/四阶段装配因上述依赖失败而级联失败。
3. `测试_第十一阶段跨进程与Agent.py` 的搜索/调用诊断链路返回值为空，需单独复现调用证据，不得用缓存覆盖。

## 下一批唯一清单

| 顺序 | 落点 | 动作 | 验收 |
|---:|---|---|---|
| 1 | `模块库/文档生成/包声明.json`、能力定义、实现、依赖契约 | 迁移到公开后端生成能力（模块只依赖能力 id，不依赖 Provider）；补齐文档生成四格式依赖闭包 | 示例项目与项目适配层装配通过 |
| 2 | `支持库/后端/文档生成` 与模块文档生成 | 统一生成能力 owner，确保文字/PPT/XLSX/PDF 只存在一个公开后端 owner，Provider 仅内部映射 | 文档生成真实生成/失败测试通过 |
| 3 | `测试中心/示例项目`、第四阶段 | 重新跑完整装配和停止/卸载资源测试，验证无级联失败 | 项目装配阶段全通过 |
| 4 | `测试中心/第十一阶段跨进程与Agent.py` | 复现搜索→查看→调用→诊断返回值为空的真实链路，修复结果传递或明确失败 | 真实返回值为预期列表并有证据 |

## 证据边界

批次7已清零公开能力重复门禁，但全量项目测试仍失败，不能宣称底座全绿或发布通过。上述 12 项失败进入批次8。
