# 2026-08-22 慢速复查问题清单（批次二）

## 流程

```text
端口隔离修复
  -> 慢速真实执行
  -> 根因归并
  -> 工作包20/消费者入口/证据链批次修复
  -> 慢速复跑
  -> 发布门禁复核
```

## 根因

| 编号 | 根因落点 | 现象 | 处理边界 |
| --- | --- | --- | --- |
| S1 | `示例项目/稳定平台完整示例/` | 示例执行返回缺少需求id、计划、v1/v2 摘要，后续 6 个断言连锁 KeyError | 修复一次完整链路的异常传播和结果闭合，不改测试断言 |
| S2 | `示例项目/前后端核心示例/`、`示例项目/平台控制面示例/` | 消费者契约声明入口文件不存在 | 恢复为真实可运行的最小消费者入口，走公开契约，不复制实现 |
| S3 | 工作包20证据写入 | 证据类型缺少“签名”，阻断清单不为空 | 在真实签名动作成功后写入签名证据，保持哈希可追溯 |
| S4 | 浏览器/项目编译默认端口 | 旧默认 4780 与代理冲突 | 已改为 45080；生成制品和契约需继续复核 |
| S5 | 残留审计、发布门禁 | 由 S1-S3 失败连带退出 1 | 根因修复后复跑，不单独放宽门禁 |

环境能力缺失（macOS Python 无 tkinter）已转换为统一“宿主不可用”结果，逐包 13/13 已通过；这不是假成功。
