# 2026-08-22 发布门禁问题清单（批次一）

## 流程

```text
发布门禁
  -> 逐包权威验证
  -> 冷启动装配与真实调用
  -> 完整性摘要
  -> 慢速阶段
  -> 批次问题清单
  -> 修复
  -> 全量验证
```

## 发现

| 编号 | 落点 | 证据 | 判定 |
| --- | --- | --- | --- |
| B1 | `支持库/前端/桌面宿主/` | 资源预算摘要不一致；复用决策缺少“搜索词/候选能力id”；真实启动在当前 Python 环境不可用 | 阻断，结构与元数据必须修复；运行环境不可用需明确记录，不得伪绿 |
| B2 | `支持库/前端/浏览器宿主/` | `实现/浏览器宿主.py` 摘要不一致；复用决策缺少“搜索词/候选能力id” | 阻断，派生摘要和复用决策必须修复 |
| B3 | 冷启动装配调用器 | 发布门禁输出 `调用器可用=false`，异常为 `'list' object has no attribute 'get'` | 阻断，装配结果解析与调用器绑定不兼容 |
| B4 | 慢速阶段 | 慢速层退出码 1，但场景输出“防御有效、阻断项数 0” | 阻断，测试编排必须让退出码与真实判定一致 |
| B5 | 客户端制品 | 之前激活指针指向半安装制品；本批已重建并通过 PyMuPDF 工作包 | 已修复，需由全量验证复核 |

## 本批验收

- 结构/派生摘要：逐包权威验证与完整性摘要退出码 0。
- 冷启动：调用器可用，至少一个能力真实返回统一结果。
- 慢速：所有场景判定通过时阶段退出码为 0；真实失败仍非零。
- 外部环境不可用：必须标记未验证并阻断发布，不得转成成功。
