# 批次：全盘复扫与发布门禁收口

## 流程图

```text
依赖/生命周期复扫
  -> 18 个标准提供者全部通过
  -> 正式发布门禁首次运行
  -> 发现并发结构迁移竞态
  -> 修复并发初始化重试
  -> 结构迁移 6/6 + 40 轮回归
  -> 正式发布门禁 172 项通过
  -> 等待架构文档与代码落点复核
```

## 本批落点

- 依赖生命周期审计：18 个标准提供者全部通过，2 个无包声明内部目录按规则跳过。
- 生命周期契约增加身份、入口存在性、释放语义反向校验；伪造契约测试被阻断。
- `运行核心/权威状态.py`：对 SQLite 并发 DDL 期间的可恢复 schema/锁竞争做最多 4 次有界重试，未知迁移错误继续 fail-closed。

## 验证证据

- 生命周期审计定向测试：7/7 通过。
- 结构迁移测试：6/6 通过，连续 40 轮通过。
- 正式发布门禁：退出码 0，172 个测试通过，发布状态“通过”。
- 未使用 V3 MCP，未占用 4780。

## 待下一批复核

只读架构审计仍在进行，重点核对易语言 `ARCHITECTURE.md` 对工程树、命令索引 ABI、编辑/编译/运行边界的要求，是否在当前代码中都有唯一权威落点；若发现缺口，下一批重新列清单后再修复。
