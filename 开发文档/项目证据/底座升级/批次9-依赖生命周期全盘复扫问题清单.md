# 批次 9：依赖生命周期全盘复扫问题清单

## 流程

```text
全盘扫描 18 个标准提供者
  -> 依赖归属核对（发行包/组件）
  -> 健康探针与停止入口核对
  -> 集中修复一批
  -> 定向门禁
  -> 全量复扫
```

## 扫描证据

扫描命令：`python3.14 开发工具/依赖生命周期审计/运行审计.py`

扫描时间：2026-08-23

结果：18 个标准提供者，13 个提供者 18 条违规；2 个非标准内部适配器按规则跳过。

| 类别 | 数量 | 具体对象 |
|---|---:|---|
| 混装归属待修 | 2 | FFmpeg提供者、PDF隔离提供者 |
| 缺健康探针契约/证据 | 11 | Pillow、PyMuPDF、openpyxl、pdfplumber、pg8000、psycopg2、psycopg、python_docx、python_pptx、reportlab、密码签名 |
| 缺停止/释放入口 | 5 | openpyxl、pdfplumber、python_docx、python_pptx、reportlab |

本清单不把这些违规折算为通过。混装项需区分同一发行包的多个命令/组件与真正跨发行包混装；健康与停止项需有可执行实现或明确契约证据。修复后必须重算完整性摘要并重新扫描。

## 本批边界

- 只修改上述提供者生命周期/依赖证据及其审计测试，不改业务项目。
- 不使用 V3 MCP，不占用 4780 端口。
- 定向验证先执行；主协调者随后执行一次全量复扫和发布门禁。
