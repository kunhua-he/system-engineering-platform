# 底座升级批次复扫清单（本批修复后）

```text
本批审计与修复
      |
      v
页面模型/独立制品/Provider owner 收口
      |
      v
专项验证：页面 6/6 + 加载器 12/12 + 反向破坏 6/6
      |
      v
全量复扫
      +--> 仍有阻断：公开完整性 36 项
      |       |
      |       +--> 正式包缺 能力定义.json
      |       +--> 模板包仍在正式扫描边界
      |       +--> 支持库与模块库同义能力命名重复
      |
      v
下一批：能力定义派生闭环与模块/支持库命名裁决
```

## 本批已验证

- 独立制品重新编译到 `示例项目/可双击演示/产物文件夹/20260822-demo`，入口收口为 `运行入口/启动.py`；开发态页面、编辑记录、Provider 元数据和运行缓存不再进入制品。
- 真实启动 `45084`：GET `/` 返回 200；POST `/api/call` 调用 `文本处理.统计长度` 返回成功和值 4；停止后端口释放；运行期间制品无 `工程缓存` 或 `.pyc`。
- 页面模型工作包 6/6、加载器工作包 12/12、反向破坏工作包 6/6、全仓 Python 语法编译退出码 0。
- Provider 重复公开 owner 不再参与公开 owner 选择；公开后端保留唯一 owner，Provider 只作为底层能力补充。

## 下一批仍需修复

| 优先级 | 具体落点 | 当前证据 | 修复目标 |
|---|---|---|---|
| P0 | `开发工具/公开调用完整性门禁.py`、正式支持库/模块库 | 36 项违规；14 个后端包、10 个模块缺 `能力定义.json` | 以能力定义为唯一编辑源，逐包生成并校验包声明、聚合契约、搜索、验证和摘要 |
| P0 | `模块库/_模板` | 模板包仍被公开门禁扫描，缺搜索/验证/说明文件 | 移出正式扫描根或明确标记为非正式模板，禁止进入能力注册表 |
| P1 | `支持库/后端/文件系统` 与 `模块库/文件管理` | 7 个同义命令名跨包重复 | 原子能力只留支持库 owner，模块改为组合门面并使用不同公开模块能力命名 |
| P1 | `支持库/后端/文档转换` 与 `模块库/媒体处理` | `检查提供者` 同义能力重复 | 统一能力 id/命令名，模块只保存模块能力 id，不复制 Provider 命令 |
| P1 | 全部正式包 | 420 处包声明与能力契约参数字段漂移 | 生成物禁止手工双写，先修参数必填/默认/类型，再更新完整性摘要 |
| P1 | `开发文档/项目说明.md` | 正式支持库数量仍把不存在的本地 LLM Provider 计入统计 | 按当前实际包重新生成计数，LLM 保持未实现状态，不伪造能力 |

公开完整性门禁本批仍为非零退出，不能宣称全量通过。
