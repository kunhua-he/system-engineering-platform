# 底座升级批次 6 全量复扫结果

```text
批次6修复
  |
  +--> Provider 宿主过滤 + 公开计数闭合
  +--> 正式包真实调用样例修复
  +--> openpyxl 内部能力映射
  +--> 18 个 Provider 摘要重建
  +--> 门禁失败证据保留完整场景名
  |
  v
全量复扫
  +--> 公开门禁：仍 8 项同义能力阻断
  +--> 定向工作包：通过
  +--> 全量测试：628 项中 16 项失败
  +--> 发布门禁：未重新宣称通过
  |
  v
批次7：Provider 内部能力迁移后的文档/PPT 链路和旧契约清洗
```

## 本批完成证据

- 项目适配层与第三阶段装配：16/16、18/18；联合装配声明/公开注册计数 138/138。
- 表格文档、openpyxl Provider、组件合规、发布门禁定向测试全部通过。
- Provider 完整性摘要 18/18 闭合。
- 发布门禁反向破坏测试 6/6 通过；失败证据现在保留“公共入口”等场景名。
- 未访问、未监听 `4780`。

## 全量结果

命令：

```bash
python3.14 测试中心/运行测试.py
```

结果：退出码 1，628 项中 16 项失败。失败集中在：

1. `测试_python-pptx提供者.py::test_入口注册两项能力`
   - Provider 入口现在只应注册内部实现能力，但旧测试仍期待公开 `演示文稿.解析演示文稿`。
2. `测试_文字文档.py`
   - DOC/DOCX 成功解析出现递归深度或“提供者不可用”；损坏/伪装输入错误码被遮蔽为提供者不可用。
3. `测试_演示文稿.py`
   - PPTX 成功解析与边界失败同样出现递归/Provider 不可用。
4. `测试_文档解析模块.py`
   - 文档解析模块调用的 `文字文档.解析文字文档` 未注册或无法到达唯一后端 owner，导致成功与文件不存在错误路径均失败。

## 公开门禁状态

`python3.14 开发工具/公开调用完整性门禁.py` 仍退出码 1，8 项均为正式包同义能力：

- 文件系统/文件管理：读取、写入、删除、复制、移动、读取二进制、读取文件头部字节，共 7 项。
- 文档转换/媒体处理：检查提供者，共 1 项。

这些能力重复不能用“模块也需要”解释放行；下一批必须保留支持库原子 owner，模块改为组合公开能力名并通过能力 id 调用。

## 批次7唯一清单

| 顺序 | 落点 | 动作 | 验收 |
|---:|---|---|---|
| 1 | `支持库/适配层/python_docx提供者`、`python_pptx提供者`、后端文字/演示文稿 | 为 Provider 建立内部实现能力 id/映射；后端公开能力继续唯一 owner；修正错误传播，禁止递归调用公开 owner | 文字文档/PPT 全部真实测试通过 |
| 2 | `模块库/文档解析` 与装配测试 | 模块只调用后端公开能力，确保依赖闭包含后端 owner 和内部 Provider | 文档解析模块全测试通过 |
| 3 | `测试中心/支持库/测试_*提供者.py` | 将旧的“Provider 公开能力”断言迁移到内部能力契约，保留公开 owner 反向阻断 | Provider 测试通过且无重复 owner |
| 4 | `模块库/文件管理`、`模块库/媒体处理` 与公开门禁 | 删除/重命名同义公开能力，改成模块组合流程，保留能力依赖而非复制原子动作 | 公开门禁 8 项降为 0 |
| 5 | 文档/PPT 错误映射 | Provider 不可用、文件损坏、超限、文件不存在分别保持明确错误码 | 失败语义测试通过 |

## 证据边界

本文件记录批次6后的真实现状。批次6不能宣称全量通过或发布通过；上述 16 项失败与 8 项重复能力是批次7输入。
