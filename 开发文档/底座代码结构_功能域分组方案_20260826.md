# 底座代码结构 · 功能域分组方案（唯一权威 · 2026-08-26）

> 参照易语言支持库架构 + 华哥建议，把底座 30 个零散支持库收敛为 6 个聚合支持库。
> 语义清晰、四级嵌套、原子能力一个边界。本文件即唯一权威。

---

## 一、总览：6 大支持库

```
┌────────────────────────────────────────────────────────────────────┐
│                        支持库/后端/                                 │
├────────────────────────────────────────────────────────────────────┤
│  系统核心支持库    →  状态机/连接器/会话/权限/额度/守卫/工具/资源     │
│  大语言模型支持库  →  LLM/EM/Reranker/VLM/ASR/流式/缓存/压缩/会话   │
│  办公文档支持库    →  pdf文件/word文件/表格文件/演示文稿/生成/转换    │
│  文件系统支持库    →  文件读写/目录/复制/移动/临时资源                │
│  数据操作支持库    →  数据集合/数据交换/文本处理/时间日期             │
│  网络通信支持库    →  请求/网页解析                                  │
├────────────────────────────────────────────────────────────────────┤
│  适配层（不变）    →  第三方/系统适配器                              │
│  核心（不变）      →  类型转换                                       │
└────────────────────────────────────────────────────────────────────┘
```

---

## 二、四级嵌套结构

```
一级 支持库        二级 域           三级 子域        四级 能力动作
────────────     ──────────       ──────────       ─────────────
系统核心支持库  →  系统核心        →  连接器        →  创建进程
系统核心支持库  →  系统核心        →  会话存储      →  创建会话
大语言模型支持库→  大语言模型      →  本地          →  连接
大语言模型支持库→  大语言模型      →  云端          →  连接
大语言模型支持库→  向量模型        →  本地          →  连接
大语言模型支持库→  向量模型        →  云端          →  连接
大语言模型支持库→  重排模型        →  本地          →  连接
大语言模型支持库→  重排模型        →  云端          →  连接
大语言模型支持库→  流式推理        →  —            →  提交请求   ← 通用，2级
大语言模型支持库→  会话读取        →  —            →  读取历史   ← 通用，2级
大语言模型支持库→  嵌入缓存        →  —            →  查询嵌入   ← 通用，2级
大语言模型支持库→  上下文压缩      →  —            →  压缩历史   ← 通用，2级
办公文档支持库  →  pdf文件        →  —            →  解析
文件系统支持库  →  文件操作        →  —            →  读取文件
数据操作支持库  →  数据集合        →  —            →  列表添加
网络通信支持库  →  请求            →  —            →  发送GET
```

---

## 三、系统核心支持库

```
┌──────────────────────────────────────────────────────────────────┐
│ 系统核心支持库                                                     │
│                                                                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐    │
│  │ 状态机   │ │ 连接器   │ │ 会话存储 │ │ 检查点   │ │ 权限审批 │    │
│  │ (句柄)   │ │ (创建)   │ │ (三表)   │ │ (恢复)   │ │ (门禁)   │    │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐    │
│  │ 协议白名单│ │ 幂等消息 │ │ 路径安全 │ │ 额度控制 │ │ 删除守卫 │    │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘    │
│  ┌─────────┐ ┌─────────┐                                        │
│  │ 工具执行 │ │ 资源管理 │                                        │
│  └─────────┘ └─────────┘                                        │
└──────────────────────────────────────────────────────────────────┘

能力示例：
  系统核心支持库.系统核心.会话存储.创建会话
  系统核心支持库.系统核心.权限审批.校验动作
  系统核心支持库.系统核心.额度控制.检查额度
  系统核心支持库.系统核心.资源管理.原子写入
```

---

## 四、大语言模型支持库

```
┌──────────────────────────────────────────────────────────────────────┐
│ 大语言模型支持库     ← 华哥定名。每个模型类型都分【本地/云端】，      │
│                        通用能力放 2 级/3 级，不区分部署形态。          │
│                                                                        │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐          │
│  │ 大语言模型(LLM) │  │ 向量模型(EM)   │  │ 重排模型       │          │
│  │ ┌─────┐┌─────┐ │  │ ┌─────┐┌─────┐│  │ ┌─────┐┌─────┐│          │
│  │ │ 本地 ││ 云端 │ │  │ │ 本地 ││ 云端 ││  │ │ 本地 ││ 云端 ││          │
│  │ │连接  ││连接  │ │  │ │连接  ││连接  ││  │ │连接  ││连接  ││          │
│  │ │对话  ││对话  │ │  │ │嵌入  ││嵌入  ││  │ │重排  ││重排  ││          │
│  │ └─────┘└─────┘ │  │ └─────┘└─────┘│  │ └─────┘└─────┘│          │
│  └────────────────┘  └────────────────┘  └────────────────┘          │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐          │
│  │ 视觉语言模型VLM │  │ 语音识别(ASR)  │  │ 文本转语音(TTS) │          │
│  │ ┌─────┐┌─────┐ │  │ ┌─────┐┌─────┐│  │ ┌─────┐┌─────┐│          │
│  │ │ 本地 ││ 云端 │ │  │ │ 本地 ││ 云端 ││  │ │ 本地 ││ 云端 ││          │
│  │ │连接  ││连接  │ │  │ │连接  ││连接  ││  │ │连接  ││连接  ││          │
│  │ │看图  ││看图  │ │  │ │转写  ││转写  ││  │ │合成  ││合成  ││          │
│  │ └─────┘└─────┘ │  │ └─────┘└─────┘│  │ └─────┘└─────┘│          │
│  └────────────────┘  └────────────────┘  └────────────────┘          │
│                                                                        │
│  ┌──────────────────────────── 通用能力（2级/3级，不分本地云端）───┐   │
│  │  流式推理(提交/消费/关闭) │ 会话读取(读取历史/估算token)         │   │
│  │  嵌入缓存(查询/存储/统计/清空) │ 上下文压缩(估算/压缩历史)       │   │
│  │  重新排序(重排/检查可用性) │ 环境配置(加载/查询)                 │   │
│  └───────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘

能力示例：
  大语言模型支持库.大语言模型.本地.连接
  大语言模型支持库.大语言模型.云端.连接
  大语言模型支持库.大语言模型.本地.生成对话
  大语言模型支持库.大语言模型.云端.生成对话
  大语言模型支持库.向量模型.本地.连接
  大语言模型支持库.向量模型.云端.连接
  大语言模型支持库.向量模型.本地.生成嵌入
  大语言模型支持库.重排模型.本地.连接
  大语言模型支持库.重排模型.云端.连接
  大语言模型支持库.视觉语言模型.本地.连接
  大语言模型支持库.语音识别.云端.连接
  大语言模型支持库.文本转语音.本地.连接
  大语言模型支持库.流式推理.提交请求       ← 通用
  大语言模型支持库.会话读取.读取历史       ← 通用
  大语言模型支持库.嵌入缓存.查询嵌入       ← 通用
  大语言模型支持库.上下文压缩.压缩历史     ← 通用
```

---

## 五、办公文档支持库

```
┌──────────────────────────────────────────────────────────────────┐
│ 办公文档支持库                                                     │
│                                                                    │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐              │
│  │ pdf文件      │ │ word文件     │ │ 表格文件     │              │
│  │  → 解析      │ │  → 解析      │ │  → 解析      │              │
│  │  → 导出      │ │  → 导出      │ │  → 导出      │              │
│  └──────────────┘ └──────────────┘ └──────────────┘              │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐              │
│  │ 演示文稿      │ │ 文档生成     │ │ 文档转换     │              │
│  │  → 解析      │ │  → 生成DOCX  │ │  → 转换文本  │              │
│  │  → 导出      │ │  → 生成XLSX  │ │  → 转换办公  │              │
│  └──────────────┘ └──────────────┘ └──────────────┘              │
│  ┌──────────────┐                                                │
│  │ 模板渲染      │                                                │
│  │  → 渲染模板  │                                                │
│  └──────────────┘                                                │
└──────────────────────────────────────────────────────────────────┘

能力示例：
  办公文档支持库.pdf文件.解析
  办公文档支持库.word文件.解析
  办公文档支持库.表格文件.解析
  办公文档支持库.文档生成.生成PDF
```

---

## 六、文件系统支持库（独立）

```
┌──────────────────────────────────────────────────────────────────┐
│ 文件系统支持库                                                     │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ 文件操作                                                      │  │
│  │  读取文件 │ 写入文件 │ 判断存在 │ 列出目录 │ 删除文件         │  │
│  │  复制文件 │ 移动文件 │ 获取大小 │ 获取修改时间 │ 创建目录     │  │
│  │  读取二进制 │ 读取头部字节 │ 登记临时资源 │ 清理临时资源     │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘

能力示例：
  文件系统支持库.文件操作.读取文件
  文件系统支持库.文件操作.写入文件
```

---

## 七、数据操作支持库

```
┌──────────────────────────────────────────────────────────────────┐
│ 数据操作支持库                                                     │
│                                                                    │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐              │
│  │ 数据集合      │ │ 数据交换     │ │ 文本处理     │              │
│  │  列表/字典    │ │  JSON/CSV    │ │  分割/合并   │              │
│  │  并集/交集    │ │  序列化/反   │ │  替换/查找   │              │
│  │  过滤/映射    │ │  序列化      │ │  去空白/大小写│             │
│  └──────────────┘ └──────────────┘ └──────────────┘              │
│  ┌──────────────┐                                                │
│  │ 时间日期      │                                                │
│  │  当前时间     │                                                │
│  │  格式化/解析  │                                                │
│  └──────────────┘                                                │
└──────────────────────────────────────────────────────────────────┘

能力示例：
  数据操作支持库.数据集合.列表添加
  数据操作支持库.文本处理.分割文本
  数据操作支持库.时间日期.获取当前时间
```

---

## 八、网络通信支持库

```
┌──────────────────────────────────────────────────────────────────┐
│ 网络通信支持库                                                     │
│  ┌──────────────┐ ┌──────────────┐                              │
│  │ 请求         │ │ 网页解析     │                              │
│  │  发送GET     │ │  提取标题    │                              │
│  │  发送POST    │ │  提取正文    │                              │
│  └──────────────┘ └──────────────┘                              │
└──────────────────────────────────────────────────────────────────┘

能力示例：
  网络通信支持库.请求.发送GET
  网络通信支持库.网页解析.提取标题
```

---

## 九、未变更

```
模块库/        → 组合层，不变（引用的能力 id 同步更新）
公共契约/      → 不变
适配层/        → 第三方适配器，不变
核心/          → 类型转换，不变
前端/          → 不变
```

---

## 十、迁移

```
30 个零散库 → 6 个聚合支持库（一次 v2.0.0 全量迁移）

1. 建 6 个支持库目录 + 子域目录          ✅ 完成
2. 复制实现/能力定义/包声明/能力契约/摘要  ✅ 完成
3. 脚本批量改能力 id 前缀                ✅ 完成（510 文件）
4. 更新 __init__.py 注册                ✅ 完成（聚合入口+注册能力）
5. 更新模块库依赖契约 + V3 客户端        ✅ 完成（V3 底座客户端同步）
6. 删旧目录、重建摘要                    ✅ 完成（29 旧库删除）
7. 重启网关装配、验证每个能力            ✅ 完成（272 能力装配）
8. 更新字典 40001                       ✅ 完成（动态搜索新 id）

迁移状态：✅ 已完成（2026-08-26，v2.0.0 tag）
问题登记：旧库残留/旧路径引用/包id叠错/聚合入口注册/适配层误伤/发现器子域跳过 全部修复
```