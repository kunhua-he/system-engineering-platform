# 旁路问题（待下一批处理）

来源：2026-09-11 六环补账批检 —— 用平台自身 `开发工具/HTML验证/场景加载.py` 解析器，对 19 个已补账包做只读批检（解析引用 → 解析多步骤 → 比对正向覆盖）。

批检结论：16 包可解析且正向覆盖齐全；2 包（环境变量 / 路径安全）静态绝对路径违规，已就地修复并复验通过；余下 1 项为结构性阻塞，转入本清单。

---

## B1　个人蒸馏支持库.查询上下文 —— 无法写出正向场景

- **现象**：`支持库/后端/个人蒸馏支持库/验证场景.json` 中 `查询上下文` 只有负向步骤（`成功=false / 状态码=500 / 错误码=个人蒸馏不可用`）。
- **门禁口径**：`开发工具/HTML验证/场景加载.py::_校验场景全集` 强制 `正向目标能力 == 公开能力`——每个公开能力必须至少有一个 `预期成功=true` 的目标步骤，负向步骤不计入。
- **阻塞原因**：`查询上下文` 依赖外部服务 `http://127.0.0.1:8898/接口/v1/人格/当前`（环境变量 `V3个人蒸馏地址`）。实测该接口对任意合法参数（含服务自带默认 `用户:华哥` / `个人/默认`）**恒返回 HTTP 500**（上游无数据/未播种）；缺参时返回 422。故不存在可稳定通过的正向断言。
- **门禁无豁免**：`开发工具/公开调用完整性门禁.py` 无“外部依赖能力排除”开关（已确认）。
- **待决选项（下一批择一）**：
  1. 上报上游修复 `8898` 的 `/接口/v1/人格/当前`（播种或修 500），使其可返回 200，再补正向场景；
  2. 若该能力在本平台侧不应公开，从 `能力定义.json` / `包声明.json` 摘除，并同步六环产物（搜索数据 / 说明书 / 场景）；
  3. 给门禁加“外部依赖能力”标注与豁免通道（属架构改动，需华哥拍板）。
- **影响**：六环门禁 `个人蒸馏支持库` 恒缺 1 项目标，无法转绿。

---

## B2　检查点恢复.保存检查点 —— 运行期注册参数表缺 `中断原因`（契约漂移）

- **来源**：2026-09-11 补账子代理（工具执行 + 检查点恢复）实测发现。
- **现象**：`支持库/后端/系统核心支持库/检查点恢复/__init__.py` 注册 `保存检查点` 的参数表**缺 `中断原因`**。网关按**运行期注册**校验入参，传 `中断原因` 会返回 400「收到未知参数 中断原因」。
- **漂移三方**：`能力定义.json` 与 `能力数据/能力搜索数据.json` 都声明了 `中断原因`，`实现/` 也支持它；唯独运行期注册表没有 → 声明与运行期不一致。
- **修复点**：改 `支持库/后端/系统核心支持库/检查点恢复/__init__.py` 的注册参数表，补 `中断原因`（补账子代理被禁止改 `__init__.py`，故顺延）。
- **当前规避**：补账场景已绕开该参数（改用 `登记中断` 承载中断语义），所以门禁可过，但运行时该参数仍是坏的。
- **优先级**：中（真实运行时缺陷，非仅治理账）。

---

## B3　数据操作支持库.数据集合 —— 门禁 7 项缺口 ✅ 本批已修

- **来源**：2026-09-11 门禁实测。六环违规从 204 项收敛至 **7 项，全部集中在本包**。
- **现象**：本包 13:54–14:02 新增 3 个能力（`展平字典` / `展平结构条目` / `规范化条目指纹`）。`能力定义.json` 已写全，但六环其余环未同步：说明书缺 3 个能力名、搜索缺 `展平字典`、场景未覆盖 3 个能力。
- **上一批为何未修**：审计期本包登记为**并发禁区**（另一会话 13:54–14:02 仍在写本包、HEAD 持续推进），"不影响他"条件不成立。
- **本批判定可修**：本包文件全部入库、工作区零脏改、最后写入距今 1 小时以上、该会话 HEAD 已转到 `办公文档支持库` → 判定已放手。
- **本批修复**：补 `说明/使用说明.md` 3 个能力名（表格行 + 独立章节）、补 `能力数据/能力搜索数据.json` 的 `展平字典` 条目、补 `验证场景.json` 的 `数据集合.三能力正向`（3 步正向），并**统一重算 `完整性摘要.json`**。
- **落点验证**：平台自身 `场景加载` 解析器逐场景通过（二十能力链 20 步 / 三能力 3 步）；门禁本包违规 7 → **0**；摘要复验一致。
- **附带改进**：按华哥要求，本包全部样例数据由「甲/乙/丙」改为口语化业务词（补水面膜 / 精华 / 城市 / 库存 / 张三 / 李四），期望值全部实跑实现取得，避免手写断言漂移。见记忆 `测试数据要口语化`。

---

## B4　代码解析支持库.解析代码文件 —— 必填标 false 但无默认规则文件（软风险）

- **来源**：2026-09-11 静态普查子代理（A/B 类缺陷全仓普查）附带发现。
- **现象**：`解析代码文件` 的 `规则文件路径` 参数标为**必填 false**，但包内**无任何默认规则文件**，且该方法无条件调用 `加载切块规则()`。只传三个必填参数（文件id/路径/扩展名）必然返回 `失败("代码解析失败","未配置切块规则文件路径")`。
- **同类**：`解析代码字节` 同理会失败，但它有 `规则` 字典参数可替代（传入即可绕过）。
- **建议（择一）**：把 `规则文件路径` 标 `必填 true`；或随包附默认 `切块规则.json`。
- **优先级**：低（契约标注不精确，非崩溃；有 `规则` 参数的路子可绕过）。

---

## B5　办公文档支持库.轻量文本解析 —— 门禁唯一剩余缺口（1 项，另一会话在途）

- **来源**：2026-09-11 数据集合修完后重跑门禁实测。六环仅剩 **1 项**，落在此包。
- **现象**：本包是另一会话本期新增的子包（HEAD `cd031bc` / `aee9b1d` / `639f901` 系列为同会话 `办公文档支持库.表格公式` 薄壳化工作）。已具备 `包声明.json` / `能力定义.json` / `能力数据/能力搜索数据.json` / `说明/使用说明.md` / `完整性摘要.json`，**缺 `验证场景.json` 与 `验证场景引用.json`**。
- **门禁口径**：`[验证场景-验证场景引用.json缺失] 能力id=*`。
- **为何未修**：与 B3 同理，属**在途工作包**，另一会话随时可能继续写；且本批已把它（含 `测试中心/支持库/核心/测试_轻量文本解析.py`）随"全部提交"一并入库，若再并行补场景易撞车，故顺延。
- **待决**：确认该会话收工后，按 `数据集合.三能力正向` / `文本处理.解析区间正向` 同法补一份 `验证场景.json`（正向步骤，断言取实跑值）+ `验证场景引用.json`，并重算本包 `完整性摘要.json`。修完即全绿。
- **影响**：六环门禁 100% 剩余缺口 = 本包 1 项。
- **连带**：`支持库/后端/办公文档支持库/完整性摘要.json`（父级聚合）未登记 `轻量文本解析/*` 三件（`__init__.py` / `包声明.json` / `实现/轻量文本解析.py`），全树摘要校验报"清单不闭合"。属同一在途工作，待该会话收工时由其自行重算聚合摘要。

---

## B6　支持库/适配层/YAML提供者.完整性摘要.json —— 自身哈希陈旧（既有脏态，勿动）

- **来源**：2026-09-11 全树摘要校验。
- **现象**：`完整性摘要.json` 记录的自身 sha256 与当前文件内容不符（`文件摘要不一致: 完整性摘要.json`）。会话开工时该文件已是 `M`（另一会话在途），本批误经空排除路径重算后已**回退至入库版本**，恢复其既有脏态。
- **为何未修**：本文件在审计期登记的**并发禁区**清单内。
- **待决**：由该会话自行收口；若确认其已放手，下一批按平台生成器重算即可（须带正确排除路径，勿再用空排除）。

---

## B7　（占位）后续批次追加
