# 系统工程平台 · 审计报告（2026-09-11）

> 审计基线：HEAD `639f901a`（审计起点 `85a2940`，期间并发会话持续提交）；实测口径：正式索引实时扫描 + 发布门禁真实执行 + HTML 黑盒真实 HTTP。
> 本报告是维修工作载件，结论并入权威文档并完成维修后按归档规则处理。

## 结论

**功能全绿；治理账本经本轮维修已大幅转绿，剩余 2 项（各 1 个修复点，余下全部转入旁路清单）。**
- 已转绿：依赖防火墙（P0-1）、中文边界、依赖锁一致、包结构/声明/注册表/提供者隔离、完整性摘要一致（M10 重算 35 包，复验 0 不一致）。
- 仍红：正式制品来源与字节指纹（制品过期，需重编译）、公开调用完整性（六环剩余 **1 项**，落另一会话在途新包 `轻量文本解析`，见 B5）。

## 一、功能层（绿）

- HTML 黑盒验证器（唯一正式判定）：435 场景 / 419 能力 / 557 步骤，0 清理失败、0 残留。
- 浏览器真实交互、HTTP 流式响应、客户端断开清理、任务取消、发布事务恢复、敏感配置保护、网关限流、有状态自动排空、子进程/端口清理、工程缓存无源码，全通过。

## 二、治理层

### 已转绿（本轮维修成果）

| 项 | 原状态 | 现状态 | 依据 |
|---|---|---|---|
| P0-1　依赖防火墙 | ✗ 数据交换深导入另一库 `实现/` | ✓ 违规=无（审计 578 文件）| 已改为包级中文入口 |
| P0-2　中文边界 | ✗ `热接入回归.py` 英文命名 | ✓ 英文命名文件=无 | 已改中文 |
| P0-3　依赖锁一致 | ✗ 3 包版本漂移 1.0.0/1.1.0 | ✓ 校验 20 项无漂移 | 版本对齐 1.1.0 |
| 包结构 / 声明 / 注册表 / 提供者隔离 | — | ✓ | 107 包 |

### 仍红

**R1　正式制品来源与字节指纹（M7，缓办）**
制品锚 `f05477c` ≠ HEAD `639f901a`。制品未随源码重编译，属过期锚，非缺陷。重编译影响运行态，并发期风险高，缓办。

**R2　公开调用完整性（六环历史欠账，维修收口）**
门禁实测 **204 项 / 30 包 → 1 项 / 1 包**（维修后）。原 7 项全部落在 `数据操作支持库/数据集合`，已于本批修复（补说明书 3 名 + 搜索 1 条 + 场景 3 步，重算摘要；落点验证：平台场景解析逐场景通过、门禁本包 7→0、摘要一致），记录见 `旁路问题_待下一批.md` **B3**。
**剩余 1 项**：`办公文档支持库.轻量文本解析` 缺 `验证场景引用.json` —— 另一会话本期新增的在途子包，未动，见 **B5**。修完即全绿。
补账分三类：验证场景未覆盖（按真实语义写场景）、说明书未含能力名、搜索未含能力id；由 16 组子代理并行补账 + 本线程批检修正完成。
**本线程批检**：用平台自身 `场景加载` 解析器对 20 个已补账包做只读批检（解析引用 → 解析多步骤 → 比对正向覆盖），发现并就地修复 2 包静态绝对路径违规（环境变量 / 路径安全，改受管临时路径动态展开）；1 包无法正向验证（个人蒸馏，见 B1）；1 包场景三缺（文本处理.解析区间，已补）。

**硬约束（本轮新发现，已写入子代理任务书）**：`开发工具/HTML验证/单步场景.py::_解析断言` 硬拒**空返回断言**（`返回断言不可为空`）。正向步骤必须至少含 `值` / `值类型` / `关键值` / `必需字段`+`字段类型` 之一；断言基准是网关返回信封的 **`值`** 子树，`关键值` 为相对 `值` 的点分路径；负向步骤必须声明非空 `错误码`。允许字段仅 `{错误码,包含,值类型,关键值,必需字段,字段类型,值}`。

**旁注**：HTML 黑盒的 `_发送请求` 不带 Authorization 头，走**免鉴权的独立制品网关**，与需鉴权的运行态 40007 网关是两条通道 —— 黑盒与手工验证的鉴权口径不同，属设计而非缺陷。

**R3　完整性摘要一致（已转绿，M10）**
逐文件校验 107 包；本轮补账（改 `能力数据/能力搜索数据.json`）连带 35 包摘要过期。M10 用平台自身 `开发工具/组件规范/完整性摘要.py::迁移旧格式摘要（系统根, 排除路径=…）` 统一重算 35 包，**排除 3 个并发禁区**；复验 0 不一致。禁区自身摘要未动（属另一会话领域）。

### P1 遗留（本轮未动）

**P1-1　直播逐字稿实现层 26 处边界违规**
`模块库/直播逐字稿/实现/`（全自动精校、缓存与续跑、复核调度、导出落盘、模式注册表、分片转写、复核编排）集中 26 处（白名单外导入 / 直接 I/O / json 原子操作）。量大需专项，缓办。

**P1-2　技能库接入（部分完成）**
`开发工具/项目编译/正式包索引.py` 已接入技能库（本轮 M3；实测 94 支持库包含技能库 1、能力 542、能力冲突 0）。**但门禁的 `扫描段` 仍只含 `支持库/后端` + `模块库`**，技能库未纳入门禁扫描，且缺 完整性摘要 + 验证场景。技能库目录是并发会话（`待重启事项_技能库目录归位.md`）的领域，本轮**未动**。

**P1-3　网关流式"第二条腿"（改判为合规豁免，非缺陷）**
`运行核心/统一网关/本地网关.py:694` 的 `from 支持库...模型连接器 import 流式生成对话` 走的是**包级中文入口**、未穿透 `实现/`，符合跨包铁律；SSE 传输无法用 JSON 结果契约承载生成器，属架构特例。已加注释终止重复审计。

## 三、并发边界（维修期间禁碰）

> 旁路问题（本批未决，转入下一批）：见同目录 `旁路问题_待下一批.md`（B1 个人蒸馏无法正向验证 / B2 检查点恢复注册参数漂移 / B3 数据集合门禁 7 项 / B4 代码解析必填标注软风险）。


```
开发文档/待重启事项_技能库目录归位.md
支持库/后端/数据操作支持库/数据集合/            （整包 7 文件）
支持库/适配层/YAML提供者/完整性摘要.json
```
维修期间 HEAD 由 `85a2940` 推进至 `639f901a`（另一会话持续提交）。

## 四、维修清单与状态

| 编号 | 修复点 | 目标文件 | 状态 |
|---|---|---|---|
| M1 | P0-1 深导入改包级入口 | 数据交换/实现/数据交换.py | ✅ 完成 |
| M2 | 摘要生成器支持排除路径 | 开发工具/组件规范/完整性摘要.py | ✅ 完成 |
| M3 | 技能库接入正式索引 | 开发工具/项目编译/正式包索引.py | ✅ 完成 |
| M4 | 网关流式第二条腿 | 运行核心/统一网关/本地网关.py | ✅ 完成（改判合规豁免 + 注释）|
| M5 | 英文命名 | 开发工具/HTML验证/热接入回归.py | ✅ 完成 |
| M6 | 依赖锁版本对齐 | 文本处理/文件操作/资源管理 依赖锁定.json | ✅ 完成 |
| M11 | 个人蒸馏 URL 未编码 | 个人蒸馏支持库/实现/个人蒸馏.py:59 | ✅ 源码完成（生效需重载网关 / M7 重编译）|
| M12 | 按规则切块 无参构造崩溃 | 代码解析支持库/实现/代码解析.py | ✅ 源码完成（生效需重载网关 / M7 重编译）|
| M13 | 同类缺陷静态普查 | 全仓 `__init__.py` + URL 拼接点 | ✅ 完成（A 类 0 / B 类 0；113 注册包 + 28 URL 点交叉四轮无新命中）|
| M9 | 六环欠账补账（30 包）| 各包 能力定义/搜索/说明书/场景 | ✅ 完成（16 组子代理；门禁 204→7，余 7 项属并发禁区，见 B3）|
| M14 | 场景文件批检（门禁不校验可解析性）| 20 包验证场景 | ✅ 完成（修 2 包静态路径 + 补 1 包场景三缺）|
| M10 | 补账后摘要统一重算 | 各包 完整性摘要.json | ✅ 完成（重算 35 包，复验 0 不一致，排除 3 禁区）|
| M15 | 数据集合门禁 7 项（B3）| 数据集合 说明/搜索/场景/摘要 | ✅ 完成（门禁本包 7→0；样例数据改口语化，期望值实跑取得）|
| M7 | 制品重编译 | 工程缓存/制品仓库 | ⏸ 缓办（影响运行态、并发期风险高）|
| M8 | 逐字稿 26 处违规 | 模块库/直播逐字稿/实现/ | ⏸ 缓办（量大、需专项）|
