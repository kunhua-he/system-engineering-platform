# 0030 设计哲学收口：废止 1.5、替换 9.4、吸收外部借鉴 P-1~P-8

> 状态：**现行** ｜ 2026-09-17
> 依据：华哥「都写入好，然后废弃的、流水账等这一些。都直接删除掉，没必要保留。我只需要就是当前规范。历史的账，可以移除掉。保持干净一些。」
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 一、改了什么（12 主条 / 49 子项 → **13 主条 / 53 子项**）

| 动作 | 条款 | 内容 |
|---|---|---|
| 废止删除 | 1.5 | 「旧版进归档」与 1.7「不建本地归档、直接删」直接对立；1.7（晚于本项）+ 5.3②「无引用才允许清理」已覆盖其意图 ⇒ **整条删除**，不留划线原文 |
| 替换 | 9.4 | 「同样的输入 → 同样的输出」是无条件规则，而随机数 / 当前时间 / 网络 / 浏览器 / 大模型五类正式能力天然不满足 ⇒ 换成**确定性四分类**（纯确定性 / 可重放 / 外部观测 / 有副作用） |
| 同源标注 | 1.3、8.2 | 与 1.2「不保留旧腿」是同一件事的同一面，规则本体以 1.2 为准；原文保留 |
| 补边界 | 1.2 | 消解与 5.2 的表面冲突：内部第二执行腿禁止 / 边界协议兼容映射允许但必须带【到期版本 + 使用计数 + 删除条件】/ 静默长期兼容禁止（P-2） |
| 新增 | 1.8 | 例外必须自证：豁免无理由判违规（P-7） |
| 并入 | 5.3 | 契约兼容性分档（兼容 / 破坏 / 冻结），判据复用同一个 `契约指纹()`（P-1） |
| 新增 | 7.9 | 生成物三律：输入指纹 + 勿手改标记 + 判据＝跑完生成器后 `git status` 干净（P-5） |
| 新增 | 12.5 | 规则的破坏样本是接口义务 + 元测试遍历全部检查项（P-6） |
| 新增主条 | 第 13 条 | 13.1 规则三要素（判据 / 强制点 / 反向试验，缺一即纸面规则）；13.2 门禁结论五态（干净通过 / 债务冻结 / 只报告 / 未核验 / 失败）（P-3 + P-4） |
| 修订 | 12.4② | 上限口径改为**主条 ≤14、子项 ≤56**（收口后实测 13 / 53，留 2 位余量）；上调必须同批写明理由 |
| 批量删除 | 各条「落地记录」实测段 + 第 11 条实测记录表 | 属流水账；历史由 Git 保留 |

**P-1~P-8 八条外部借鉴候选全部消化，无一落空。**

## 二、被否决的做法（防止后续重提）

```
✗ C 型废止声明（保留划线原文 + 写废止于哪条）
      → 华哥裁定**直接删**，哲学正文只留当前规范

✗ 全量重排子项编号（让 1.4 之后直接接 1.5）
      → 会让全仓引用（未完成事项 / 总账 / 决策记录 / 方案）批量失效，收益不成立（第 11 条）
      → 改为「编号不复用 + 第 1 条留一行空号说明」

✗ 新增 5.4「契约兼容分档」
      → 「5.4」这个编号已被更早一次合并吃掉，**不复用** ⇒ 并入 5.3 同条目承载

✗ 给 9.4 加「确定性类别」字段进 能力定义.json ／ 给 包声明.json 加「契约兼容档位」字段（M-11）
      → **动契约**，本轮不做；分类先只作说明与验证场景口径，档位只作判据口径

✗ 保留旧「总账速览」表
      → 实测分项和 8+18+24+3 = 53 ≠ 合计 49，且与逐条总账两处对不上 ——
        有缺陷的旧账不留（与本次「历史账移除」口径一致），改为结构实测 + 待重测绘

✗ 本轮先把四态统计重算一遍
      → 批 1 接线会让 🟡 成片转 ✅，接线前重算的数第二天就过期
      → 改为「条目级同步本轮做完，四态统计留待批 1 接线后一次性重测绘」
```

## 三、后果与在办

```
① 四态统计（机器强制 / 半强制 / 人工 / 纸面）**待批 1 接线后一次性重测绘**，
   口径统一为 哲学总账.md 第一节引言那四条（不得各表各算）。
② 债务清单 §8.2 中已由本决策消解的条目（门禁语义分裂、哲学冲突 A）已同步销项。
③ 本轮**零代码改动、零契约变更**；接线（批 1）在华哥发令后单独开工。
```
