# 0041 二次审计落地：逐条证真 + 文件冲突矩阵 + 波次编排

> 最后更新：2026-09-18
> 口径：实测（每条带 `文件:行` 取证）
> 维护者：小马仔

## 一、为什么单独立此记录

外部审计报告来了 674 行、含 P0–P2 + §8–§12 五个架构专题。若按报告编号直接派活，
会踩两个坑：① 报告是**外部视角**，与仓库台账可能有重复或过时条目；
② 报告按**问题**分组，而并发维修的硬约束是「**不同时改同一个文件**」。

按华哥的审计哲学条款（第一轮找全、第二轮证真+定位+排序、第三轮一次修完），
本轮把「证真」与「按文件聚类」做成两件独立交付，结论落 `方案/二次审计落地编排_20260918.md`。

## 二、逐条证真结论（报告可信度高）

**P0/P1 全部成立**：P0-1（`独立进程.py:280` setsid + `提供者生命周期.py:101` 纯内存成员表）、
P0-2（`提供者生命周期.py:449-479` select+readline 直读管道 vs `独立进程.py:144` 后台线程 os.read **同管道**）、
P1-3（5 处 `_终止并重启`）、P1-4（`超时执行.py:582`）、P1-5（`任务进程.py:537/542/554` 池锁内 join+fsync）、
P2-7/P2-10/P2-12、B1（7306 行，与报告一致）、B2/B3/B4/B5。

**★ 一处修正**：P2-10 的 `状态存储.py` 真位置在 `平台控制面/平台状态/`，**不在** `运行核心/`（报告未给路径）。

**★ 一处扩大**：P1-6 实测 **8 处**文案当判据，比报告多 2 处 —— 新增发现
`唯一能力调用.py:117` `"缺失" in 说明` 与 `权威状态.py:262` `"no such table"/"database is locked" in 文本`。

**★ 报告主动剔除的 4 条「疑似问题」复核后确认剔除正确**：
`句柄体系.py` 裸调 `ps`（已先走 `进程终止.进程存活(pid)`，`ps` 仅用于 POSIX 上排除 zombie）、
`流式HTTP.py:599` select 对象是 socket（合法）、嵌套同连接事务已修（三处传 `连接=`）、限流槽位无泄漏。

## 三、文件冲突矩阵（并发的唯一约束）

把问题**按文件聚类**成 6 个互斥簇（A 提供者族／B 能力调用族／C 网关族／D 状态族／E 任务族／F 句柄收口族）
＋ 2 个自由簇（G 性能基准新增目录／H 文档与配置）＝ **8 路并行**；
**B1 拆上帝文件涉及 A–F 全部文件，必须最后单独一波**。

## 四、被否决的做法

- **按报告编号逐条派活** —— 同一文件被多路同时改，必然冲突（本批 A 簇 3 个问题同文件、C 簇 3 个问题同文件）。
- **把 §8–§12 五个架构专题当维修项直接落代码** —— 它们是**架构级决策**（多租户数据面／成熟度／语言选型／
  零配置／双槽），落代码前需先定方向；本批**只落决策记录**，代码另批。
- **B1 与其他波次并行** —— 拆完文件，其他簇全返工。

## 五、验收口径

维修期只做 `py_compile` 与单文件轻检测（哲学 12.1③）；**全批完成才跑**：
定向 unittest → 场景体检 → 全量重算摘要 → 重建制品 → 发布门禁。每处修复配反向验证（12.5）。
