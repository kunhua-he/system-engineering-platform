# 04 D批次补充审计工作台

> 审计时间：2026-09-03。A/B/C 批次全部收口后，按"隐藏问题"维度（临时目录泄漏、超时线程、测试全量覆盖、未跟踪文件、事实源一致性、错误映射链）补充排查。
> 原则：只记录真实发现，不凑数。已评估为非阻断/非功能缺陷的观察项也一并记录。

## 已确认无问题（非发现）

| 检查项 | 结果 |
|---|---|
| 导入循环 | ✅ 0 |
| daemon 线程/Timer 累积（正式代码） | ✅ 无 |
| Git 工作树 vs 磁盘正式文件 | ✅ 唯一差异为 工程缓存回收.py（已入库） |
| Pillow/图像解码 完整性摘要 | ✅ 已重算对齐 |
| 自修复工具 完整性摘要 | ✅ 已重算对齐 |
| 发布门禁（408/408 能力、545/545 步骤） | ✅ 通过 |

## 发现与修复清单

| 编号 | 问题 | 根因 | 修复 | 验证 |
|---|---|---|---|---|
| D01 | 模板生成器假绿 | C03 改受限通信时参数名误用 `input=`（签名是 `输入=`）；转义层级错误（生成文件 `\\n` 应为 `\n`）；受限通信先写 stdin 再起读取线程 → 输出超管道缓冲死锁 | 参数名修正、转义修正、读取线程先启动 | 测试 9/9 |
| D02 | Pillow 提供者"超大输出"映射丢失 | 超限检查 `>` 应为 `>=`；测试 `_假进程` 只实现 communicate 不兼容受限通信 | 检查符号修正 + mock 补流接口 | 测试 43/43 |
| D03 | 说明书生成器验证状态 None（None） | Pillow 验证场景引用.json 缺 场景id/范围 字段（a900492 迁移丢三要素） | 恢复 v1 合规格式；场景id 对齐真实场景文件；场景加载.py 允许 范围 字段 | 测试 6/6 + 场景解析成功 |
| D04 | 反向破坏门禁28 白名单漏 3 文件 | 测试_模型连接与并发释放/测试_记忆支持库 直导实现但不在允许表 | 补白名单 | 23/23（含说明书+模板生成器） |
| D05 | 演示文稿测试导入错误 | 聚合库重构后路径未更新（办公文档支持库.文档转换 → 文档转换支持库.LibreOffice转换） | 修正导入 + 检查提供者() 签名 + 返回键 LibreOffice | 测试 9/9 |
| D06 | 测试_HTTP服务支持库 假绿 | 模块 e7f17c3 已删（迁移有界HTTP/本地网关），测试残留 import 失败 | 删除测试 + 清理 .pyc + 删除空壳目录 | 全量扫描不再报 |
| D07 | 模块库 10 测试 setUpClass 崩溃 | 网关默认 要求凭证=True，测试未配 | 批量注入 `要求凭证=False, 禁止客户端身份=False` | 模块库 175/175 |
| D08 | 网关 FileNotFoundError 映射丢失 | 网关核心缺 FileNotFoundError 分支 + `_执行幂等请求` 的 `except Exception` 吞掉后统一转"内部错误" | 补映射分支；幂等表异常路径不缓存部分响应直接传播 | 文件内容扩充+自修复 2/2 |
| D09 | 自修复工具验证命令退出码非零仍提交 | `验证修复` 只查 执行.成功 不查退出码 | 加退出码非零 → 验证失败 | 测试通过 |
| D10 | 本地网关校验顺序不一致 | 凭证检查先于保留端口检查（其他网关先端口） | 调整为先端口后凭证 | 测试_批次三假绿 5/5 |
| D11 | 诊断复现临时目录泄漏 | mkdtemp 自建目录无 finally 清理（8 个 mkdtemp 候选，其余 7 个测试工具或调用方负责） | try/finally 清理自建目录 | 语法+逻辑检查 |
| D12 | Git提供者超时测试假绿 | mock 只实现 communicate/wait，受限通信需 poll() 返回 None 触发超时 | mock 补 poll() + 测试注入 超时秒=1 | 测试 18/18 |
| D13 | 工程缓存回收.py（WP6）未入库 | 8/22 创建后一直未提交 | git add 入库 | Git 干净 |

## 同类问题批量检查记录

- mkdtemp/TemporaryDirectory 全项目扫描 8 处 → 仅 诊断复现.py 真泄漏（其余为测试夹具或调用方持有路径负责清理）
- 模块库测试同类 `配置={"请求超时秒": 10}` → 10 个文件 11 处批量修复
- 网关启动顺序 → 流式HTTP/能力网关均先端口后凭证，仅本地网关不一致（已修）

## 最终状态

- D01~D13 全部收口。
- 全量测试：76 个测试模块 0 失败（修复前 18 个失败）。
- 核心波次：网关安全 38/38、冷启动+模块对称+句柄 22/22、支持库 18+43+9+66、模块库 175/175、开发工具 23/23。
- HTML 黑盒 408/408 能力、545/545 步骤；发布门禁通过。
- 当前激活制品：`平台客户端-54d1f7f45960099c`，提交 `a8ceed8`。
- Git 提交：`84f2b58`（模板生成器修复）、`addb815`（入库+网关异常+自修复+本地网关+诊断复现+模块库凭证）、`a8ceed8`（Pillow引用修正+门禁通过）。
