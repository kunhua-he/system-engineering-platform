# 05 批次D 清场执行清单（只读盘点完成，等 A/B/C 全部完成后执行）

> 依据：`开发文档/项目说明.md` 第 22 条（不做本地归档、旧版直接删，GitHub 提交历史就是归档）。
> **前置条件：批次 A（薄壳落地）+ B（语义落库）+ C（核验补能力）全部完成后才能执行本清单。**
> 不可逆点已标注 ⛔；执行到 ⛔ 前会再报一次现场状态。

## 一、删除清单（`MCP工具箱/` 整目录）

| 项 | 实测 | 处置 |
|---|---|---|
| Python 模块 | **22 个**，合计 **5975 行**（`__init__` / 临时上下文 / 任务观测 / 作业系统 / 使用反馈 / 公开能力 / 协作状态 / 发布治理 / 工作区管理 / 工具名映射 / 支持库协作 / 文件租约 / 文件补丁 / 核心治理 / 模块合规 / 测试资源 / 热重载 / 能力占用 / 能力网关 / 角色权限 / 项目服务 / 验证门禁） | ⛔ 整目录删除 |
| 非 py 文件 | `README.md`、`启动HTTP服务.sh`、`启动能力网关.sh` | ⛔ 一并删除 |
| **例外（先迁走再删）** | `能力网关.py` 属**只读能力发现网关（40006）**，与 MCP 工具无关（上一轮已核实：40006 是独立只读入口，不是第二条执行腿） | 先确认 40006 是否仍在用；在用途就迁到独立目录（不留在薄壳里） |

## 二、端口与进程下线

| 项 | 实测 | 处置 |
|---|---|---|
| 8766 监听进程 | `Python` PID **33333**（`127.0.0.1:8766`，手动启动，**无 LaunchAgent**） | ⛔ `kill <PID>`；因为**没有开机自启条目**，不会自动复活 |
| 日志 | `工程缓存/mcp_http_8766.log`（6211 B，最后写入 09-11 22:37） | 随清场删除（属临时日志，非权威证据） |
| 客户端配置 | `.mcp.json:5` 与 `opencode.json:6` 均指向 `http://127.0.0.1:8766/mcp/` | 切到薄壳（样例由批次 A2 产出）：`待切换_.mcp.json` / `待切换_opencode.json` |

## 三、测试同步（`测试中心/MCP工具箱/` 共 20 个测试文件 + 2 个运行器自检目录）

| 类别 | 文件 | 处置 |
|---|---|---|
| 测的是**要删的旧实现** | 测试_临时上下文 / 测试_任务观测 / 测试_作业系统 / 测试_协作状态 / 测试_发布治理 / 测试_工作区与清理 / 测试_工作区清理增强 / 测试_工作区管理 / 测试_支持库协作 / 测试_文件租约 / 测试_文件补丁 / 测试_核心治理 / 测试_模块合规 / 测试_能力占用 / 测试_角色权限矩阵 / 测试_角色门面 / 测试_项目服务 / 测试_验证门禁 | 随旧实现一并删除（它们只测旧壳） |
| 测的是**留下的能力** | 测试_能力网关（对应 40006 只读网关）、测试_能力搜索覆盖率 | 保留，并把指向改成新落点（`模块库/能力目录/`） |
| 运行器自检残留 | `运行器自检_3bd8a36b`、`运行器自检_8c2464d1` | 属历史残留 → 删除（第 17 条：仓库只保留最干净状态） |

## 四、文档回填（按第 24 条：报错先回填文档；此处是主动回填）

| 文档 | 要改什么 |
|---|---|
| `AGENTS.md` | **子代理继承协议**现在强制调 `project_context` / `codegraph_explore` / `mcp_feedback` / `verify_and_record` + 「专属 MCP 实例」→ 全部改成薄壳口径（`调用能力` + 新的能力 id / 技能 id） |
| `开发文档/项目说明.md` | 「MCP 与只读 HTML 审计」节、「能做什么→5. 启动 Agent MCP 适配」节：改成 3 工具薄壳 + 无端口；能力数等口径同步 |
| `开发文档/主开发文档.md` | 若有 8766 相关行 → 更新；补本次新增文档索引 |
| `开发文档/未完成事项.md` | 「在办：MCP 层收敛」表结项；把 E 批次 8 项真缺陷列为下一轮 |
| `开发文档/当前流程图.md` | 链路图里 MCP 节点改成薄壳（唯一网关 → 技能库/模块库/支持库） |
| `开发文档/多会话并行开发规约.md`、`底座升级方案.md`、`提速方案.md` | 凡引用 8766 / 50 工具 / 旧工具名处，改为薄壳口径或标注「已收敛」 |

## 五、执行顺序（每步可独立回退）

```text
1. 确认 A/B/C 产物已验收（薄壳 tools/list=3、技能包受控执行通过、缺口能力已补）
2. 切客户端配置（.mcp.json / opencode.json → 薄壳），实测 AI 客户端能列到 3 个工具 ⛔
3. kill 8766（PID 以现场为准）→ 确认端口不再监听
4. 迁走 40006 能力网关（如仍在用）→ 删除 MCP工具箱/ 整目录 ⛔
5. 删除 18 个旧测试 + 2 个运行器自检残留；保留并改指向 2 个能力测试
6. 按第四节逐份回填文档（AGENTS.md 需华哥授权）
7. 全链收口：改源码 → 提交 → 重编制品并激活 → 跑发布门禁（一次性验证，符合第 23 条）
```

**回退方式**：全部改动走 Git（删目录=一次提交），回退即 `git revert` 或从提交历史取回文件；
8766 若要临时恢复，重跑 `MCP工具箱/启动HTTP服务.sh`（脚本在提交历史里）。
