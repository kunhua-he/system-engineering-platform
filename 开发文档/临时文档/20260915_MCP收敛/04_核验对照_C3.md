# 04 核验对照 · 批次 C3（7 个「任务/协作/反馈/能力检索」类工具）

> 性质：**只读核验**（未改任何源码文件、未写任何运行库数据、未跑门禁与全量测试）。
> 口径：`03_总表与处置.md` 把这 7 个工具判为「直接调能力」→ 本文逐个证实或推翻，
> 给出「目标能力 → 存在证据（文件:行号）→ 入参映射 → 结论」。
> 环境：python3.14；`unset PYTHONPATH`；根 = `~/Documents/Agent/PHP/系统工程平台`。

## ⚠️ 〇、核验现场是动态的（必读）

本批核验期间 **A / B5 / C0 三路并发落地**，同一份代码在 13 分钟内被改了三轮。
本文按两个时点各自留痕，**结论以最终时点（18:48）为准**：

| 时点 | 现场变化（与本批 7 个工具相关） |
|---|---|
| **18:35**（核验起点） | `模块库/能力目录` **不存在**；`支持库/后端/反馈支持库` **不存在**；`平台控制面/能力反馈/` 只有 `反馈服务.py`（能力反馈，8 态状态机） |
| **18:41** | 新增 `模块库/开工编排`（C3-1/C3-2 候选落点）：**只有 1 个能力 `开工编排.开工准备`**（= `development_start` 的收敛形态），参数里**没有 work_id / parent_work_id** |
| **18:43** | 新增 `模块库/能力目录`（C0）：能力**声明**有了，`实现/能力目录.py` 当时是骨架（返回 `结果.失败("未实现", …)`）；新增 `平台控制面/能力反馈/反馈语义.py`、`开工id校验.py` |
| **18:45-18:46** | 新增 **`平台控制面/能力反馈/开发反馈服务.py`（28 KB）**：4 个能力 `登记开发反馈 / 查询开发反馈 / 复核开发反馈 / 登记升级候选`，**逐条写明了旧 MCP 工具映射** |
| **18:48** | `模块库/能力目录` 补齐 `能力定义.json` + **真实现**（不再返回「未实现」）；实测可跑 |

---

## 一、一条决定性前置：能力只能注册在 支持库 / 模块库 / 技能库

「直接调能力」的落地通道是唯一网关 `调用能力(能力id, 参数)`（`00_收口方案与依赖.md:29-40`）。
三根正式包根之外**没有能力id 可调**。实测（18:48 复验）：

```
find 平台控制面 -name 能力定义.json   → 0 命中
find 运行核心   -name 能力定义.json   → 0 命中
find 开发工具   -name 能力定义.json   → 0 命中
```

→ 凡目标实现落在 `平台控制面/`、`运行核心/`、`开发工具/` 的，
**当前都没有能力id**，必须「登记/补成能力」才算满足批次 C 的验收口径。

### 能力面检索实测（`模块库.能力目录.搜索能力`，真跑，18:48）

| 关键词 | 结果 |
|---|---|
| `反馈` | **总数 0** ← 反馈能力全在 `平台控制面`，检索面看不到（关键缺口） |
| `协作` | 0（18:35 用 CLI 实测；平台能力面确无协作状态能力） |
| `占用` | 仅 `并发控制支持库.占用容量`（**并发容量**，非能力占用租约） |
| `裁决` / `依赖` | 只有无关命中（`直播逐字稿.全自动精校` 参数含「裁决模型配置」；`知识检索.检查可用性` 返回含「缺失支持库」） |
| `检索` | `文件系统支持库.内容检索.正则搜索`、`知识检索.检索知识`（文件/知识检索，非能力检索） |
| `能力目录` | **2 条**：`能力目录.搜索能力`、`能力目录.读取能力` ← C0 已交付且可检索 |

复核：`关闭 PYTHONPATH && python3.14 开发工具/能力搜索/能力搜索器.py --能力 反馈` → 「未找到能力: 反馈」（与上表一致）。

### 运行库现场（只读实测：`PRAGMA query_only=ON`，未写任何数据）

| 库 | 表 | 行数 | 含义 |
|---|---|---:|---|
| `工程缓存/运行数据/底座运行.db` | `协作状态` | **26** | 本批 2 工具的目标域，**在用** |
| 同上 | `能力占用` | **22** | `claim_capability` 的**镜像账本**，在用 |
| 同上 | `任务` | **8** | `运行核心/任务调度/任务系统.py` 的能力异步任务台账（与 work_id 无关） |
| 同上 | `会话`/`缓存索引`/`检查点索引`/`发布`/`灰度观测`/`灰度状态` | 0 | 死表（与 `03_总表` 6.2 一致） |
| `工程缓存/平台控制面/权威状态.db` | `占用租约` | **5** | `claim_capability` 的互斥判据 |
| 同上 | `能力反馈` | **1** | 能力反馈专表（8 态状态机） |
| 同上 | `证据` | **0** | B5 开发反馈的事实源，**尚未接线（0 行）** |
| 同上 | `需求` | **0** | 升级池（`登记升级候选` 的落点），**0 行** |

---

## 二、7 行结论（以 18:48 现场为准）

| 工具 | 目标能力 | 存在？ | 结论 | 剩余缺口 |
|---|---|---|---|---|
| `register_task` | `协作状态` 域写入 / `模块库.开工编排` | 原子写入**存在**；登记语义**仍缺** | **部分映射**：域在用（26 行），父子映射/去重/指纹无载体；`开工编排` 现只有 `开工准备` 一个能力，无 work_id/parent 入参 | C3-1、C3-9 |
| `collaboration_status` | 四源聚合（协作状态域 + 反馈 + 证据 + 临时上下文 + 指纹） | 唯一源有原子能力；聚合**仍缺** | **部分映射**：须与 C3-1 同批落 | C3-2（新增） |
| `feedback_status` | `平台控制面.能力反馈.查询开发反馈` | **✅ 已落地（18:46）** | **映射成立**：入参 `开工id` 对齐；但**未登记为能力**（能力面搜「反馈」=0），且事实源 `证据` 表 0 行 | C3-5 同类（平台控制面登记） |
| `feedback_review` | `平台控制面.能力反馈.复核开发反馈` + `登记升级候选` | **✅ 已落地（18:46）** | **映射成立**：`work_id→开工id`、`task→任务(子串)`、`limit→限制(1–100 默认 20，与旧默认 20 一致)`；**升级池通道已补齐** | 原 C3-4 **关闭**；剩登记为能力 |
| `claim_capability` | `平台控制面/能力目录.服务` 申请占用四动作 | 逻辑存在、**未注册为能力** | **需补能力**：真调已在用（租约 5 行） | C3-5、C3-6 |
| `dependency_arbitration` | 依赖闭包裁决 | 裁决门面**只在待删目录** | **需补能力**：`运行核心` 有零件无门面 | C3-7、C3-9 |
| `capability_search` | `模块库.能力目录.搜索能力` / `.读取能力` | **✅ 已交付（C0，18:48）且实测可跑** | **映射成立**：`keyword→关键词`、`limit→限制` | 原 C3-8 降级为 3 项口径差异 |

**判「直接调能力」的 7 个核销账：证实 3（feedback_status / feedback_review / capability_search，
其中 2 个是核验期间才落地）、部分证实 3（register_task / collaboration_status / dependency_arbitration）、
需补能力 1（claim_capability）。**

---

## 三、逐工具：目标能力 → 存在证据 → 入参映射 → 结论

### C3-① `register_task`（频次 27/24）

**目标能力**：开工上下文（work_id / 任务 / 角色 / worktree / 允许路径 / 基线提交 / 父 work_id）登记进运行库。

| 层 | 证据 |
|---|---|
| 工具定义 / 分发 | `MCP工具箱/项目服务.py:460` / `:870-873` |
| 实现 | `MCP工具箱/协作状态.py:226-275 登记任务`（`_校验开工id` 16 位十六进制 `:55-60`；重复拒绝 `:248-249`；父→子回写 `:261-271`；代码指纹 `:250-256`→`:206-223`） |
| 落点 | `协作状态.py:158-169 _写运行库` → 能力 **`数据库连接支持库.SQLite数据库.写入运行态`** |
| 原子能力存在 | `支持库/后端/数据库连接支持库/SQLite数据库/能力定义.json:129`（写入运行态）、`:392`（查询运行态）；契约 `能力契约/参数契约.json:142`、`:428` |
| 域定义 | `…/SQLite数据库/实现/提供者.py:173-177`（`协作状态` 域：work_id/任务/角色/worktree路径/允许路径/基线提交/代码指纹/parent_work_id/子任务列表/生命周期/登记时间/…） |
| 现场 | 底座运行.db `协作状态` = **26 行** |
| 新落点现状（18:41） | `模块库/开工编排/包声明.json` 能力数组 **只有 `开工编排.开工准备`**（参数：任务/项目根/修改路径/历史数量/验证级别）→ **无 work_id / parent_work_id** |

**入参映射**：`work_id`✅（域主键）、`任务`✅、`角色`✅、`worktree路径`⚠️（列在，登记语义不在）、
`允许路径[]`⚠️（TEXT/JSON 文本落列，`协作状态.py:128-143`）、`基线提交`⚠️、
`parent_work_id`❌（父记录 `子任务列表` 互写 + 已登记校验没有能力）。

**❌ 口径错误（须更正）**：`03_总表与处置.md:44` 写「register_task → `写入运行态/查询运行态`（**域=任务**）」——
实测 **不成立**：`register_task` 落 **`协作状态` 域**（`协作状态.py:162`）。
`任务` 域属于 `运行核心/任务调度/任务系统.py:187`（写）/`:110`（读），列 = `任务id/能力id/请求id/项目id/用户id/进度/错误码/错误说明/完成时间/取消标记`（PRAGMA 实测），**装不下 work_id / parent_work_id / 允许路径**。

**结论：部分映射**。原子能力存在，登记语义无载体 → 缺口 C3-1。

---

### C3-② `collaboration_status`（频次 28/25）

| 数据源 | 证据 | 有原子能力？ |
|---|---|---|
| 运行库 `协作状态` 域 | `协作状态.py:362-398 查询协作状态` → `:88` 调 `查询运行态` | ✅（`能力定义.json:392`） |
| MCP 使用反馈 JSONL | `协作状态.py:293-301` → `开发文档/项目证据/MCP使用反馈.jsonl`（**470 行**，488 KB） | ❌（18:46 起有开发反馈能力，但**归档在 `证据` 账本**，见 C3-③） |
| 验证证据 JSONL | `协作状态.py:304-321` → `开发文档/项目证据/验证历史.jsonl`（**262 行**） | ❌ |
| 临时上下文 JSON | `协作状态.py:347` → `MCP工具箱/临时上下文.py` → `工程缓存/MCP临时上下文/{work_id}.json` | ❌（`会话` 表 0 行死表） |
| 代码指纹（实时） | `协作状态.py:338` → `:206-223 计算代码指纹` | ❌ |

**入参映射**：`work_id`✅；`任务`（关键词模糊匹配）❌（`查询运行态` 按列条件查，不做跨源聚合，
也取不到「未反馈 / 证据指纹不匹配 / 子代理未登记」三阻断标记）。

**结论：部分映射**（5 源只有 1 源有原子能力）→ 缺口 C3-2（本文新增，`03_总表` 6.2 清单未列此项）。

---

### C3-③ `feedback_status`（频次 99/72）——核验期间由「不能映射」翻转为「映射成立」

| 项 | 证据 |
|---|---|
| 工具定义 / 分发 | `项目服务.py:427` / `:668-669`（`查询反馈状态(反馈路径, 当前开工id)`） |
| 旧实现 | `MCP工具箱/使用反馈.py:47-58`（读 JSONL，按 `开工id` 倒序命中） |
| **新目标能力（18:46）** | **`平台控制面.能力反馈.查询开发反馈`**：`平台控制面/能力反馈/开发反馈服务.py:51-54`（能力清单表）、`:130-137`（能力表定义：入参 `开工id`；返回 `已反馈/开工id/反馈/数量/来源`）、`:159-164`（**旧工具映射：`feedback_status` → `查询开发反馈`**）、`:255 def 查询状态` |
| 事实源 | `权威状态.db` `证据` 账本（类型 `开发反馈`，`:17-21`），**+ JSONL 只读回退**（`:23-29`、`:94 默认回退文件`）——存量 470 行历史反馈仍算「已反馈」 |
| 开工id 校验 | `平台控制面/能力反馈/开工id校验.py:1-21`（格式 + 运行库 `协作状态` 域已登记，**fail-closed**）→ 与 6.2 缺口①「`mcp_feedback` 未走 `_有效开工id`」的处置对齐 |
| 现场 | `证据` 表 **0 行**（能力已写、尚未接线） |

**入参映射**：工具无入参（固定 `当前开工id`）→ 目标 `开工id`（必填）✅ 对齐。

**结论：映射成立**（旧结论「不能映射」已被 18:46 的落地推翻）。
**剩余**：① 平台控制面**没有 `能力定义.json`** → `查询开发反馈` 未登记为可检索/可调用能力（能力面搜「反馈」= 0）；
② 事实源 0 行 → 需接线（`开发反馈服务.py:37-39` 已写明一行接线路径）。

---

### C3-④ `feedback_review`（频次 87/75）——同样翻转为「映射成立」，且升级池通道补齐

| 项 | 证据 |
|---|---|
| 工具定义 / 分发 | `项目服务.py:428` / `:670-676`（走 `_有效开工id`，空 work_id 不得退化为全量读 `:671-672`） |
| 旧实现 | `使用反馈.py:61-89`（过滤 开工id/任务，抽 4 字段非「无」者 → `升级候选[]`） |
| **新目标能力（18:46）** | **`平台控制面.能力反馈.复核开发反馈`**：`开发反馈服务.py:138-146`（入参 `开工id?` / `开工id表?` / `任务?`（子串）/ `限制?` 1–100 默认 20；返回 `数量/反馈列表/升级候选/查询是否截断`）、`:271 def 复核`、`:290`（「必须给出开工id或开工id表（**禁止全量读取**）」） |
| **升级池通道（原缺口 C3-4）** | **`平台控制面.能力反馈.登记升级候选`**：`:147-156`（入参 开工id + 来源反馈id + 类别；返回 `需求id/需求状态=待确认`）；`:41-46` 明写通道 = `平台控制面/需求登记.py:21 登记需求` → 平台升级池；`:159-164` 映射 `register_requirement` → `登记升级候选` |
| 现场 | `需求` 表 **0 行**（通道已建、未使用） |

**入参映射**：`work_id`→`开工id` ✅；`task`→`任务`（子串过滤）✅；`limit`→`限制`（旧默认 20 ↔ 目标默认 20）✅。

**结论：映射成立**。原缺口 C3-4「升级候选→升级池通道不存在」**已由 B5 关闭**（同一 `需求` 表，
`策略中心/统一入口` 只对具体需求id 校验确认状态，新登记需求不会误放行）。
**剩余**：同 C3-③（登记为能力 + 接线）；另 `复核` 新增了 `开工id表` 批量入参，旧工具没有，属增值不属缺口。

---

### C3-⑤ `claim_capability`（频次 22/22）

| 项 | 证据 |
|---|---|
| 工具定义 / 分发 | `项目服务.py:448`（`操作` 枚举 登记/查询/续租/释放/回收过期）/ `:807-829` |
| 实现 | `MCP工具箱/能力占用.py`：`:243 申请能力占用`、`:307 续租`、`:323 释放`、`:346 释放开工id`、`:378 回收过期`、`:395 查询` |
| **真调（成立）** | `平台控制面/能力目录/服务.py:29 class 能力目录`、`:80 申请占用`、`:95 续租`、`:102 释放占用`、`:106 回收过期占用` |
| 互斥判据 | `平台控制面/平台状态.py:49-54`（`占用租约` 唯一部分索引 `ON 占用租约(能力id) WHERE 状态='活跃'`） |
| 现场 | `权威状态.db.占用租约` = **5 行**；列 = `租约id/能力id/领域/契约指纹/任务/所有者/心跳/过期时间/释放证据/状态` |
| 镜像写 | `能力占用.py:125-171 _写账本`（域 `能力占用`）+ `:290-303`；底座运行.db `能力占用` = **22 行** |
| 是否注册为能力 | ❌ 平台控制面 全仓无 `能力定义.json`；能力面搜 `占用` 只命中 `并发控制支持库.占用容量`（无关） |

**入参映射**

| 工具入参 | 目标 | 判定 |
|---|---|---|
| `操作=登记` | `申请占用(能力id, 领域, 契约指纹, 任务, 所有者, 心跳秒)` | ⚠️ 多一层派生：`领域` 固定 `"能力"`（`能力占用.py:30`）、`心跳秒` 300、`契约指纹` 传 `""` |
| `操作=查询` | `服务.状态.查询记录("占用租约", …)` + 能力id 过滤（`:395-412`） | ⚠️ **跨层直查状态表**，非 `服务` 公开方法 → 能力化须定契约 |
| `操作=续租/释放` | `续租(租约id)` / `释放占用(租约id, 证据)` 均**单条** | ⚠️ 工具的 `租约id表[]` 批量语义无对应入参 |
| `操作=回收过期` | `回收过期占用(心跳超时秒=300)` | ✅ |
| `能力id` | 同名 | ✅ |
| `提供包id` / `开工id` | 目标叫 **`所有者` / `任务`** | ⚠️ 改名映射 |
| `证据` | `释放证据` | ✅ |

**结论：目标能力存在（逻辑唯一、已在真用），但「直接调能力」当前落不了地**（无能力id 可经网关调）
→ 缺口 C3-5；另镜像写运行库 `能力占用` 域（22 行）构成第二写入方 → 缺口 C3-6
（与 `03_总表:130`「仅剩又镜像写运行库能力占用域待收敛」一致，**核实成立**）。

---

### C3-⑥ `dependency_arbitration`（频次 5/3）

| 项 | 证据 |
|---|---|
| 工具定义 / 分发 | `项目服务.py:459`（`包id` 必填）/ `:868-869` |
| 裁决实现 | **`MCP工具箱/发布治理.py:559-570 依赖裁决`** → `:533-556 校验依赖闭包`（`:540 发现全部`、`:544 选择全部提供者`、`:549 解析依赖`、`:550 缺口 = 缺失能力 + 版本冲突 + 循环`） |
| 复用的零件（存在） | `运行核心/加载器/包发现/发现器.py:59 发现全部`、`运行核心/加载器/提供者选择/选择器.py:56 选择全部提供者`、`运行核心/加载器/依赖解析/解析器.py:54 解析依赖` |
| 另一份同源调用点 | `开发工具/快速编译/运行快速编译.py:141 解析依赖闭包`（`开发工具/项目编译/正式包索引.py:218`） |
| 同名异物 | `运行核心/依赖防火墙.py:291 审计依赖` = **层间 import 方向审计**（`允许依赖表 :55-72`、`第三方前缀表 :75-82`、`核心层表 :74`），**不做**依赖闭包裁决 |
| 现场（18:48） | 无新增产物；`运行核心/` 仍无 `能力定义.json` |

**入参映射**：`包id` → 无任何能力入参（`运行核心` 三个组件是**函数**不是能力）。

**结论：需补能力**，两条：
1. **裁决门面在待删目录**：唯一「包id → 裁决结果」入口是 `MCP工具箱/发布治理.py:559`，D 批次删目录后即消失；
2. **❌ 口径错误**：`03_总表与处置.md:38` 写「依赖防火墙/契约仲裁已是唯一实现」——`依赖防火墙` 是层间导入审计，
   与依赖闭包（缺包/版本漂移/循环）不是一回事，两者不能互替。→ 缺口 C3-7。

---

### C3-⑦ `capability_search`（频次 106/71）——核验期间 C0 交付，实测可跑

| 项 | 证据 |
|---|---|
| 工具定义 / 分发 | `项目服务.py:424`（`keyword`、`limit` 1–10 默认 5）/ `:650-653` |
| 旧 MCP 侧实现 | `MCP工具箱/公开能力.py:214-223`（内部 `:196-211 _全部能力`，声明源 `:49-60`） |
| **新目标能力（C0，18:48）** | **`模块库.能力目录`**：`包声明.json` + `能力定义.json` 声明 `能力目录.搜索能力` / `能力目录.读取能力`；契约 `模块库/能力目录/能力契约/参数契约.json`（`关键词` 文本默认 ""；`限制` 整数默认 5，1~100）；实现 `模块库/能力目录/实现/能力目录.py:71 def 搜索能力`、`:103 def 读取能力` |
| **真跑实测（只读）** | `搜索能力('反馈', 5)` → 成功 True、**总数 0**；`搜索能力('能力目录', 5)` → **总数 2**；`读取能力('能力目录.搜索能力')` → 成功 True，值含 `能力id/名称/中文名/中文名称/一句话说明/说明/包id/包名称/提供方/提供者…` |
| 并行实现（真重复，仍待收编） | ① `开发工具/能力搜索/能力搜索器.py:250 搜索公开能力`（14 字段，`:275` CLI，实测可跑）；② `开发工具/开发入口.py:29 搜索能力`（5 字段）/:55 查看契约；③ 旧 MCP 侧 `公开能力.py:214`（10 字段） |
| 现场缺口 | `模块库/能力目录/` **缺 `能力数据/`**（调用示例来源，`公开能力.py:56`）与 **`验证场景/`**；`能力定义.json` 已在 |
| 旧包对照 | `模块库/知识检索/` 有 `能力定义.json`+`能力数据/`+`验证场景/`+`复用决策.json`+`资源预算.json`（新包尚不齐） |

**入参映射**：`keyword`→`关键词` ✅（改名，语义一致）；`limit`（1–10 默认 5）→`限制`（1–100 默认 5）⚠️ **上界差 10 倍**，
夹取规则不同（旧 MCP 侧 `公开能力.py:219` 是 1–100 夹取、工具 schema 限 1–10）。

**结论：映射成立（目标能力已存在且实测可跑）**。原 C3-8「待 C0 交付」**关闭**，降级为 3 项口径差异：
① `limit` 上界；② 三份并行实现（10/14/5 字段）须收编，口径以 14 字段版为准；③ 新包缺 `能力数据/`、`验证场景/`。

---

## 四、缺口清单（最终版）

| 编号 | 缺口 | 状态 | 证据 |
|---|---|---|---|
| **C3-1** | 「登记任务」语义（16 位 work_id 校验 + 父/子互写 + 重复拒绝 + 代码指纹）无能力承载，只在待删目录 | **开** | `协作状态.py:226-275`、`:206-223`；`模块库/开工编排/包声明.json` 只有 `开工准备`（无 work_id 入参） |
| **C3-2** | 「协作状态聚合 + 三阻断标记」无能力承载；5 源里 3 源本身无能力 | **开（本文新增）** | `协作状态.py:293-321`、`:338`、`:347` |
| **C3-3** | 开发反馈能力已落地，但**未登记为可检索/可调用能力**（平台控制面无 `能力定义.json`，能力面搜「反馈」=0）；事实源 `证据` 表 0 行 | **半开（原「表无 work_id 列」已解决）** | `开发反馈服务.py:51-54/159-164`；`find 平台控制面 -name 能力定义.json` = 0；`证据`=0 |
| **C3-4** | 「升级候选 → 升级池」通道 | **✅ 已关闭（B5，18:46）** | `开发反馈服务.py:147-156`、`:41-46` → `需求登记.py:21` |
| **C3-5** | `平台控制面/能力目录.服务` 占用四动作未注册为能力 → 网关调不到 | **开** | `能力目录/服务.py:80/95/102/106`；平台控制面 `能力定义.json` = 0 |
| **C3-6** | `claim_capability` 镜像写运行库 `能力占用` 域（22 行）= 同一事实两处写 | **开** | `能力占用.py:125-171`、`:290-303` |
| **C3-7** | 依赖闭包裁决门面只在 `MCP工具箱/发布治理.py:559-570`；`运行核心` 有零件无门面且非能力层；`快速编译.py:141` 是第 4 份同源调用点 | **开** | `发布治理.py:533-570`、`发现器.py:59`、`选择器.py:56`、`解析器.py:54`、`运行快速编译.py:141` |
| **C3-8** | 能力检索：`limit` 上界差异（1–10 vs 1–100）；三份并行实现待收编；新包缺 `能力数据/`、`验证场景/` | **降级（C0 已交付主体）** | `模块库/能力目录/…`；`能力搜索器.py:250`；`开发入口.py:29`；`公开能力.py:214` |
| **C3-9** | **口径更正 2 处**：`03_总表:44`「register_task → 域=任务」错（实为 `协作状态` 域）；`03_总表:38`「依赖防火墙=唯一实现」错（它只做层间导入审计） | **开** | 本文 C3-①、C3-⑥ 节实测 |

---

## 五、不能映射的入参（收口时必须显式处置）

| 工具 | 入参 | 为何不能映射 |
|---|---|---|
| `register_task` | `worktree路径`、`允许路径[]`、`基线提交`、`parent_work_id` | `协作状态` 域**列都在**，但登记语义无能力；若照 `03_总表:44` 的错口径去调 `任务` 域，这 4 个字段在 `任务` 域**根本不存在** |
| `register_task` | `parent_work_id` | 目标 `模块库.开工编排` 现无任何接受 work_id 的能力（只有 `开工准备`） |
| `collaboration_status` | `任务`（关键词模糊匹配） | `查询运行态` 按列条件查，无跨源聚合与阻断标记 |
| `claim_capability` | `能力id表[]`、`租约id表[]`（批量） | 目标层 `续租/释放占用` 是**单条**；批量循环与查询跨层直查状态表都在 MCP 侧（`能力占用.py:307-343`、`:395-412`） |
| `claim_capability` | `提供包id`、`开工id` | 目标参数名叫 `所有者`、`任务` → 改名映射 |
| `claim_capability` | `操作=查询` 的返回值 | 目标无对应公开方法，需新定契约（现直查 `占用租约` 表） |
| `dependency_arbitration` | `包id` | 无能力入参（`运行核心` 组件是函数） |
| `capability_search` | `limit` | 1–10（工具 schema）↔ 1–100（目标 `限制`）**取值域不同**，夹取须在薄壳侧定 |
| `feedback_status`/`feedback_review` | 无（已全部对上） | ✅ 无不能映射项（含 `work_id`/`task`/`limit` 全部对齐） |

---

## 六、只读痕迹声明

- 未改任何源码文件；未跑门禁、未跑全量测试；未写任何运行库/平台状态数据。
- 读库方式：`sqlite3.connect(...)` + `PRAGMA query_only=ON`，只执行 `SELECT`/`PRAGMA`。
- 首次读 `底座运行.db` 时 SQLite 生成 `底座运行.db-wal`(0 B)/`-shm`，确认无进程持有（`lsof` 退出码 1）后已删除；
  `底座运行.db` 大小/mtime 未变（327680 B / 15:29）。
- `工程缓存/平台控制面/权威状态.db` 的 `-wal`/`-shm` 未删除：B5 正在活跃写该库，删辅助文件有风险。
- 报告文件：`开发文档/临时文档/20260915_MCP收敛/04_核验对照_C3.md`（本文件）。
