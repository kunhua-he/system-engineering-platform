# 阶段1 批5 分类映射：发布/模板/快照/依赖 类 10 个 MCP 工具

- 日期：2026-09-15
- 范围：`validate_module_compliance`、`generate_module_template`、`create_core_snapshot`、`query_core_snapshot`、`compatibility_check`、`rollback_gate`、`switch_active_pointer`、`reuse_search`、`claim_capability`、`dependency_arbitration`
- 性质：**只读**。未改代码、未重启、未做 git 写。所有实测产物只落 `/tmp`（脚本：`/tmp/批5证据.py`、`/tmp/批5证据2.py`、`/tmp/批5证据3.py`、`/tmp/批5证据4.py`）。
- 判据：哲学第 10 条「结果唯一即收口」、哲学第 18 条「阈值不设硬门禁」（`开发文档/项目说明.md:253`）、目标「8766 只留查询/调用能力薄壳」、`开发文档/新建支持库与模块指南.md:3-4`（三个模板「必须用」）。

## 分类口径（五选一）

| 分类 | 含义 |
|---|---|
| 保留薄壳 | 转发/查询，底座无第二份同结果实现，工具名有独立价值 |
| 去重保壳 | 工具名保留，但删掉 8766 内自建的那份**实现**（规则表/算法），改为调用底座唯一实现 |
| 合并入口 | 同一函数已有另一个入口，两个入口留一个（实现本来只有一份） |
| 真重复可直接删 | 同结果两套实现，且底座已有权威实现在跑；删掉工具不丢任何能力 |
| 迁移下沉 | 属发布/门禁执行面，不该做常驻 MCP 工具，下沉到 开发工具 CLI/门禁 |

---

## 一、分类映射表（11 列）

| 工具名 | 真实职责 | 实现位置（文件:行号） | 入参 | 返回 | 它实际调用的底座能力/脚本 | 是否重复（证据 + 行为对比） | 权限边界 | 分类建议 | 理由 | 迁移落点 |
|---|---|---|---|---|---|---|---|---|---|---|
| `validate_module_compliance` | 按模块名做「权威 13 项组件合规 + 模块边界 AST 审计」 | `MCP工具箱/模块合规.py:236`（入口）/`:224-233`（转发权威）`/`:191`（自建审计）`/`:22-60`（自建规则表）；分发 `MCP工具箱/项目服务.py:830-831` | `模块名`（必填，str） | `{成功, 错误码, 违规列表[], 权威合规{通过数,场景数,成功}}`；错误码 MODULE_NOT_FOUND / AUTHORITY_VIOLATION / IMPORT_VIOLATION / BOUNDARY_VIOLATION | 权威半：`开发工具/组件合规/合规测试包.py:380 组件合规().执行()`（13 项，真复用）；审计半：**自建** AST 规则表，未复用 `运行核心/依赖防火墙.py:291 审计依赖`（已进发布门禁，见 `开发工具/发布门禁/运行发布门禁.py:1013-1019`） | **同职责两套实现（已漂移）**。① 规则表对比（实测 `/tmp/批5证据2.py`）：`模块合规.第三方根模块` 与 `依赖防火墙.第三方前缀表` 同为 29 项但 **11 项各有一方独有**——MCP 独有 `cv2/matplotlib/chardet/pydub/moviepy/selenium/playwright/sqlalchemy/whisper/ffmpeg/密码操作`，防火墙独有 `pypdf/PyPDF2/psutil/tree_sitter/tree_sitter_typescript/transformers/fastapi/starlette/uvicorn/mcp/mlx_whisper`。MCP 是**扁平名单**（`白名单外导入`），防火墙是**分层允许依赖表**（`允许依赖表/核心层表/层名称表`，规则名「第三方直连（一第三方一支持库，仅提供者可导入）」）。② 行为差异（实测 `/tmp/批5证据4.py`：合成模块同时 `import pypdf, psutil, cv2, matplotlib.pyplot, chardet, tree_sitter`）：**MCP 报 6 条（第三方导入 3 + 白名单外导入 3），依赖防火墙报 3 条（统一规则 3）**。③ 14 个真实模块（OCR…自修复工具）两器当前均 0 违规（`/tmp/批5证据3.py`），说明是"潜伏漂移"而非已爆红；但新模块用 `pypdf`/`psutil` 会两边判定不同。④ `开发工具/复用审计/能力调用图审计.py:24-28` 明写「第三方前缀表 与 运行核心/依赖防火墙.py **共用同一个 set 对象**，禁止在本文件重定义」——即底座内已确立"规则表唯一"的规矩，MCP 这份是唯一违规的重定义者 | 无（`/tmp/批5证据2.py`/`批5证据4.py` 均为只读审计）；已知缺口：`模块名` 无路径边界（`模块合规.py:71-79` 无 resolve/is_relative_to，见上一轮 `子报告_11:1558-1572`） | 去重保壳 | 权威 13 项是真复用（保留）；自建 AST 规则表是 `依赖防火墙` 的**第三份副本且已漂移 22 项**，删自建表改调 `依赖防火墙.审计依赖` 即可，能力不丢 | `模块合规.py` 删除 `:22-188` 自建规则与审计，`审计模块边界` 改为委托 `运行核心/依赖防火墙.审计依赖`；工具名与 `项目服务.py:449` 目录描述不动；顺带补 `模块名` 的 `resolve()+is_relative_to(模块库)` 边界 |
| `generate_module_template` | 生成模块模板（聚合契约+包声明+经调用器实现骨架+对称入口+完整性摘要+测试骨架） | 转发体 `MCP工具箱/项目服务.py:832-841`（**零业务逻辑**）；真实现 `开发工具/组件规范/模块模板生成器.py:195 生成模块模板` | `模块名`（必填）、`类型`（基础模块\|功能模块）、`能力清单`（必填 array）、`依赖能力清单` | `{成功, 模块目录, 测试骨架, 文件数}` 或 `{成功:false, 错误码, 说明}`；错误码 参数不合法/类型不合法/路径逃逸/已存在/能力重复… | `开发工具/组件规范/模块模板生成器.生成模块模板`（真实现，内部再用 `开发工具/组件规范/组件规范.生成完整性摘要`、`公共契约.版本规则.契约版本`） | **实现只有一份（非重复）**，但**入口有两个**：同一函数已被 `开发工具/开发入口.py:172` 以 `创建模块模板` 薄委托暴露（CLI 操作名 `创建模块模板`，见 `:218-222/:262-263`）。行为对比：`开发入口.创建模块模板` 固定塞入 1 个示例能力（`开发入口.py:175-177`），**不能传完整能力清单**；MCP 壳支持全参。两者是"同一件事的缩水版 + 全参版" | 无额外（写盘目标 `模块库/<名>` 与 `测试中心/模块库/测试_<名>.py`，有 `is_relative_to` 双根越界拒绝与"已存在拒绝覆盖"，`模块模板生成器.py:205-210`） | 合并入口 | 实现不该动（是"必须用"的唯一生成器）；但 `开发入口.创建模块模板` 与 MCP 壳是同一门面两入口，属 `开发工具/开发入口.py:6` 自述「命令行、MCP 和 Python 调用复用同一实现（禁止三套逻辑）」的落地偏差 | 留 MCP 全参壳；`开发入口.py:166-181` 的 `创建模块模板` 改为同参委托（或直接删该缩水入口）。工具名保留，`8766` 侧仍是"薄壳" |
| `create_core_snapshot` | 把 `运行核心+公共契约` 全量文件复制成不可变快照，写 `清单.json`+`摘要.json` | `MCP工具箱/核心治理.py:150-195`（自建）；分发 `项目服务.py:842-843` | `说明`（可选） | `{成功, 快照路径, 时间戳, 摘要, 文件数, 说明}` | 用 `支持库.适配层.内容摘要`、`平台控制面.包仓库.规范化相对路径`（组件复用），但**快照算法自建**，未复用 `开发工具/组件规范/核心快照模板.生成快照清单模板` | **同结果两套实现（实测同值）**。`/tmp/批5证据.py`：同一输入（项目根、范围 `运行核心+公共契约`，MCP 落 `快照根={tmp}/核心快照`、模板落 `目标={tmp}/模板快照`）→ 两边 `文件数=86`、**`文件表` 逐条相同 = true**、**`摘要` 相同 = true**、公共字段 `摘要/文件数/文件表/类型/范围` 完全一致。且 `开发工具/组件规范/核心快照模板.py:3` 自己写着「与 核心治理.py 摘要语义一致」——**自认同源的双实现**。第三套在 `平台控制面/核心快照.py:31 创建快照`（写同一根目录 `工程缓存/核心快照/`，但只有 `摘要.json`+`核心.manifest`，无 `清单.json`） | 写入 `项目根/工程缓存/核心快照/`，**快照根不可注入校验**（`核心治理.py:154-155`；上一轮 `子报告_11:1592`）；无授权/身份校验（`子报告_11:1656` 记录 8766 全工具免凭证可调） | 去重保壳（若保留 MCP 侧）/ 真重复（若保留 `核心快照模板.py` 侧） | 一条结果两份实现，违反哲学第 10 条；`核心快照模板.py` 是 `新建支持库与模块指南.md:4` 明列"必须用"的三模板之一，MCP 这份是编外实现 | 二选一：①（推荐）删 `核心治理.py:120-284` 自建算法，`创建/查询/兼容/回滚` 改为委托 `核心快照模板`（需给它补"时间戳目录 + 列表"两项能力）；② 反之删 `核心快照模板.py` 并把 `测试中心/开发工具/测试_核心快照模板.py` 迁到 MCP 侧 |
| `query_core_snapshot` | 列 `工程缓存/核心快照/*/清单.json`，返回时间戳/摘要/文件数/说明 | `MCP工具箱/核心治理.py:198-223`；分发 `项目服务.py:844-845` | 无 | `{成功, 快照列表[], 数量}` | 纯本地文件读，无底座能力调用 | **是同一快照格式集群里的一环，且与真实数据互不可见**。`/tmp/批5证据.py` + 现场：`工程缓存/核心快照/` 有 **5 个真实快照**（`01474f575aac4b2e`/`210b348195764144`/`7c9f08f34bca47b1`/`a8123ffafe0444c2`/`e570fbbcf3694c7f`，均由 `平台控制面/核心快照.py:31` 产出，目录内只有 `摘要.json`+`核心.manifest`），**`query_core_snapshot` 返回 `数量 = 0`**——因为它硬要求 `清单.json`。即：真快照查不到，只有 MCP 自产快照能查到。上一轮使用反馈原文也点过它「未提供按时间戳/摘要筛选参数」 | 只读，无越权面；但 `查询` 无分页/无上限，快照根同样不可注入 | 去重保壳（与 `create_core_snapshot` 同生共死） | 单独看无重复实现，但它的存在把"快照两套格式"固化成产品面：平台快照在 8766 眼里等于不存在 | 跟随 `create_core_snapshot` 的落点，改为读唯一快照实现的产物（并支持读平台侧快照记录）；补"时间戳/摘要筛选"（使用反馈已提） |
| `compatibility_check` | 名义"兼容性"，实际做「快照完整性 + 新增文件 160 行预算」 | `MCP工具箱/核心治理.py:226-284`；分发 `项目服务.py:846-847`；`行数上限=160` 在 `:29` | `快照标识`（必填） | `{成功, 错误码(漂移/超限), 可检查, 差异列表[], 超限列表[], 漂移数, 超限数}` | 无底座能力调用；判定逻辑自建 | **三重问题**。① **名字撞已有权威结果**：底座早有 `运行核心/加载器/版本系统/契约兼容.py:34 检查契约兼容(旧契约,新契约) → {结论,原因,需要适配器,必须升主版本}`（另有 `公共契约/版本规则/比较.py:57 比较版本`、`开发工具/契约编译/漂移检测.py:199 检测契约升级`）。实测（`/tmp/批5证据.py`）：同一对契约 → 底座给 `{结论:"需要适配器", 必须升主版本:true, 原因:["返回结构变化","删除错误码"]}`；而 MCP `compatibility_check` **入参是"快照标识"，与兼容性无关**。② **实现与 `核心快照模板.py:115 校验快照模板` 同职责**（磁盘摘要 vs 清单 + 聚合摘要比对），只少了 `当前.json` 一项校验。③ **160 行硬门禁写反了项目哲学**：`开发文档/项目说明.md:253` 哲学第 18 条「阈值不设硬门禁…160 行的原意只是方便修改，不是上限」，`开发文档/未完成事项.md:85` 明确「平台**不设行数硬门禁**」；而本工具会把超 160 行判为 `错误码=超限`。实测核心范围内 **30 个文件超 160 行**（最大 `运行核心/权威状态.py:1123`），该门禁一启用即长期红/永久空转。④ 对平台真实快照直接误报：`兼容性检查('01474f575aac4b2e')` → `错误码=快照损坏`、`消息=快照缺少清单.json`（实测 `/tmp/批5证据.py` 现场调用） | 只读；快照标识有 `is_absolute()/..` 段/前缀校验（`核心治理.py:51-58`），这是本批少见的边界正确项 | 真重复可直接删 | 删掉不丢任何能力：**兼容性**由 `契约兼容.py` 权威承担、**漂移**由 `漂移检测.py` 承担、**快照校验**由 `核心快照模板.py` 承担；本工具独有的"160 行超限"与哲学第 18 条冲突，且对真实快照必然误报 | 从 `项目服务.py:453` 删工具项与 `:846-847` 分发；`核心治理.兼容性检查/行数上限` 一并删；若确需"核心漂移"能力，改为在 `兼容性检查` 位置调用 `开发工具/契约编译/漂移检测.全面漂移检测` 或删名后并入 `校验快照模板` |
| `rollback_gate` | 校验快照完整性，可选"原子切换激活指针回滚（不覆盖源码）" | `MCP工具箱/核心治理.py:287-331`；分发 `项目服务.py:848-849` | `快照标识`（必填）、`执行回滚`（bool） | `{成功, 错误码(快照不存在/快照损坏/陈旧令牌), 可回滚, 已执行回滚, 当前激活, 摘要}` | 自建：`_校验快照完整性`（`:120-147`）+ `_写激活指针CAS`（`:91-117`）→ 写 `工程缓存/核心快照/当前.json` | **同职责实现第 3/4 份**。① 完整性校验 = `核心快照模板.py:115 校验快照模板`（少 `当前.json` 一项）。② 指针 CAS = `平台控制面/发布管理/服务.py:71 切换激活指针`（单调版本+栅栏令牌+CAS，陈旧拒绝，`:127 回滚`）+ `平台控制面/包仓库/平台客户端制品.py:321-328`。③ 但**指针存储不同**：`rollback_gate` 写 `工程缓存/核心快照/当前.json`（快照指针），`发布管理` 写 DB 表 `激活指针` 与制品环境 `当前.json`——**同一"回滚"概念两套指针、两套 CAS**，互不感知。④ 上一轮审计已证 平台控制面 那套有幂等与回滚证据（`子报告_11:2041-2048` 稳定平台完整示例：`回滚后指针状态 = 回滚`），本工具这套**没有对应的端到端证据**。使用反馈原文亦提「回滚门禁可增强为支持漂移检查通过才允许激活…可考虑把 当前.json 纳入兼容性检查」 | 写 `当前.json`（只元数据，**不覆盖源码**，已核实 `核心治理.py:111-114`）；无身份/授权校验；快照根不可注入 | 去重保壳 | 8766 侧"核心快照回滚"这个门面值得留，但**自建的第二套指针 CAS 必须删**（指针切换要唯一），改为调 `平台控制面/发布管理` 的指针机制 | `核心治理.py:91-147/287-331` 自建 CAS 与校验删掉，`回滚门禁` 改为：校验委托 `核心快照模板.校验快照模板`；回滚委托 `平台控制面/发布管理.切换激活指针`（或至少统一到 `工程缓存/制品仓库/平台客户端环境/当前.json` 一个指针位） |
| `switch_active_pointer` | 完成制品/清单/签名/信任/正式证据/来源指纹校验后 CAS 切换激活指针 | `MCP工具箱/发布治理.py:459-528`（自建 CAS 在 `:423-456`；字段契约自认在 `:53-58`；`环境目录` 在 `:50`）；分发 `项目服务.py:862-867` | `目标摘要`(32hex)、`旧令牌`(int)、`提交`(40hex)、`来源指纹`(64hex)、`工作区指纹`(可选) | `{成功, 错误码(陈旧令牌/指针缺失/参数无效), 消息, 数据{新指针, 指针文件, 激活证据}}` | 复用 `运行核心.加载器.包发现.发现器`、`客户端` 侧签名/信任读取；CAS 写入自建 | **同一份文件上第 4 份 CAS 实现**。实测（`/tmp/批5证据.py`）：`发布治理.环境目录 = …/工程缓存/制品仓库/平台客户端环境`，该目录下 `当前.json` **真实存在**，字段 `[制品摘要, 制品目录, 摘要sha256, 栅栏令牌, 版本]` 与 `发布治理.摘要字段/制品目录字段/制品摘要字段/版本字段/栅栏令牌字段` 逐项相同；而 `发布治理.py:53` 注释自供「激活指针字段名（与 `平台控制面/包仓库/平台客户端制品.py` 的指针契约一致）」。即四份实现在写**同一个文件**：`平台控制面/发布管理/服务.py:71`（DB 权威）、`平台控制面/包仓库/平台客户端制品.py:321-328/:433`（安装链）、`客户端/构建平台客户端.py:571-602`（同步校验）、`MCP工具箱/发布治理.py:423`（本工具）。**幂等语义也不同**：`发布管理.激活` 目标一致即"幂等复用"（`:53-55`），本工具只做令牌 CAS | 有真实门禁：制品签名/信任、正式发布证据、来源指纹与工作区指纹一致性（`发布治理.py:480-502`），这是它比其它三份多出来的价值；但无身份/授权校验（`子报告_11:1656`） | 去重保壳 | 「指针切换必须唯一」是硬约束，同文件 4 份 CAS 是典型"结果不唯一"；但它的证据门禁（来源指纹+正式证据）有独立价值，**不能整条删**，应把 CAS 收敛到 `平台控制面/发布管理` | `发布治理.py:423-456 自建 CAS` 删除，`切换激活指针` 保留证据校验段（`:469-502`）后改为调 `平台控制面/发布管理.切换激活指针`；字段契约 `发布治理.py:53-58` 改为从 `平台客户端制品` 导入（禁止重定义） |
| `reuse_search` | 关键词扫既有能力，返回"已存在可复用/无现成" | `MCP工具箱/支持库协作.py:110-124`（`_能力定义表` 在 `:80-107`）；分发 `项目服务.py:805-806` | `关键词`（必填）、`limit`（**schema 里有但实现未用**） | `{成功, 值{关键词, 状态, 候选[{能力id,中文名称,说明,包id,包路径}]}}` | 纯本地：读 `支持库/适配层/*/能力定义.json` + `支持库/后端/*/能力定义.json` | **同职责第 3 份实现，且口径与底座相反**。① 底座已有 `开发工具/能力搜索/能力搜索器.py:250 搜索公开能力(项目根, 关键词, 限制)`（+ `:275 主函数` CLI，14+ 字段：提供方/返回/权限/资源预算/调用示例/最近成功验证…），另有 `平台控制面/能力目录/服务.py:51 搜索能力`、`开发工具/开发入口.py:29 搜索能力`。② 实测同关键词"图像"（`/tmp/批5证据.py`）：`复用搜索` **30 条**、`能力搜索器` **39 条**；`复用搜索` 把 **3 个 `支持库.适配层.*` 内部提供者**（PDF隔离提供者/Pillow提供者/PyMuPDF提供者）当成"可复用能力"暴露，而 `能力搜索器` 明确过滤它们（`能力搜索器.py:53-54`「适配层提供者是内部实现边界，不对外暴露，一律过滤」）。③ 职责重叠但数据源不同（能力定义.json vs 包声明.json+聚合契约），字段数差 5 vs 24 | 只读；`limit` 参数声明了却未生效（调用方传 100 也只会拿到全量候选） | 真重复可直接删 | 同一个问题"有没有现成能力可复用"已有权威实现（`能力搜索器`，且是 14 字段最全的一份），本工具既重复又把内部层当公开候选（**口径错误**）；删掉不丢能力，`复用决策` 环节改走 `能力搜索器`。另：`开发工具/复用审计/` 是 AST 审计，与本工具无关，不构成重复 | 从 `项目服务.py:447` 删工具项与 `:805-806` 分发；`支持库协作.py:80-124` 的 `复用搜索/_能力定义表` 删除；`复用决策.json` 的"搜索词"来源改为 `开发工具/能力搜索/能力搜索器.搜索公开能力`（`合规测试包.py:467-478` 只校验非空，无需改） |
| `claim_capability` | 能力级占用租约：登记/查询/续租/释放/回收过期（原子互斥+心跳+过期） | `MCP工具箱/能力占用.py:243 申请 / :307 续租 / :323 释放 / :378 回收过期 / :395 查询`；分发 `项目服务.py:807-829`（传 `平台控制面目录`，见 `:67`） | `操作`(登记\|查询\|续租\|释放\|回收过期)、`能力id`、`提供包id`、`开工id`、`能力id表`、`租约id表`、`证据` | `{成功, 错误码(参数不合法/登记失败/占用冲突), 值{能力id,提供包id,开工id,占用时间,租约id,幂等,同步错误}}` | **真复用权威服务**：`_服务()` → `平台控制面/能力目录/服务.py:80 申请占用 / :95 续租 / :102 释放占用 / :106 回收过期占用`（`能力占用.py:67-70` 直接 `能力目录(平台状态(存储目录))`）；库为**与 `平台控制面/统一入口` 同一个** `工程缓存/平台控制面/权威状态.db`（实测 `项目服务.py:67` 的 `平台控制面目录` 与 `统一入口.py:74` 默认值一致）；另额外镜像写 `工程缓存/运行数据/底座运行.db` 的 `能力占用` 域 | **实现不重复（加壳，不是第二套）**——这一点必须与初判区分：`能力占用.py:259-264` 调的就是平台权威 `能力目录.申请占用`，**同一个 DB、同一张 `占用租约` 表**，所以"谁占了哪个能力"这个结果只有一个来源 ✅（符合哲学第 10 条）。MCP 壳的真实增量：① 同包重复占用 → **幂等成功 + 刷新心跳**，异包 → `占用冲突` 并回带现有占用者详情（`:265-289`）；② 申请前先 `回收过期占用`（`:261`，防永久锁）；③ 镜像账本（失败不阻断，挂 `同步错误`，不静默）。**待收敛的唯一重复点**：同一占用事实**两处落账**——`占用租约` 表（权威）与 `底座运行.db 能力占用` 域（镜像）；`平台控制面/统一入口.py:42` 的"申请能力占用"稳定操作则是另一个入口（无幂等/冲突详情/过期回收）| 无身份/授权校验：平台侧同一动作要求 `令牌`（`统一入口.py:110-120`，`授权.py:30` 权限级 3），本工具免凭证即可登记占用（`子报告_11:1656`）。`能力id/提供包id/开工id` 有标识正则与长度校验（`能力占用.py:53-58`） | 保留薄壳 | 判定被初判推翻并纠正：它**不是**重复实现，是"权威服务 + 幂等/冲突然语义 + 过期回收"的加壳，方向正确；但它把"占用"这个唯一事实又抄了一份镜像账本，且缺授权 | 保留在 8766。收敛两点：① 镜像账本（`_写账本/底座运行.db 能力占用 域`）→ 与 `协作状态/作业系统` 一并评估是否只留 `占用租约` 一张表；② 授权桥：8766 侧身份经 `平台控制面/授权` 引导后再调（见「不确定项」1）。`delivery_closeout` 依赖的 `释放开工id能力占用`（`项目服务.py:886-889`）保持不动 |
| `dependency_arbitration` | 按包id 裁决其依赖闭包：缺失能力/版本冲突/循环 | `MCP工具箱/发布治理.py:559-570`（+ `:533-556 校验依赖闭包`）；分发 `项目服务.py:868-869` | `包id`（必填） | `{成功, 错误码(裁决失败/参数无效), 消息, 数据{缺口[]}}` | 复用 `运行核心.加载器.包发现.发现器.发现全部`、`运行核心.加载器.提供者选择.选择器.选择全部提供者`、`运行核心.加载器.依赖解析.解析器.解析依赖`（`解析结果` 自带 `缺失能力/版本冲突/循环`，`解析器.py:19-29`）——**真复用，无第二份实现** | **非重复**。底座内其它"依赖"判定各管一段、不构成同结果：`平台控制面/策略中心.py:56 _依赖判定`（查 DB `能力条目` 登记表，非真实包发现）、`开发工具/开发入口.py:89 查看依赖关系`（用 `解析依赖(目标表, {})` 空提供者表，退化口径）、`开发工具/依赖生命周期审计/审计核心.py`（审第三方提供者的依赖锁/健康探针，另一轴）。本工具是唯一"按包id 给出依赖缺口"的入口 | 只读（发现+解析）；无路径/身份校验，但也不写盘；`系统根` 可注入（测试用） | 保留薄壳 | 组件是三份共享的（发现器/选择器/解析器），工具本身零复制逻辑，正是"查询/调用薄壳"的样板；不宜删（策略中心那套查的是登记表，查不到真实缺提供者） | 保留在 8766；建议与 `平台控制面/策略中心._依赖判定` 对齐口径（或让 `统一入口.提交候选包` 的依赖门禁调本能力），并在描述里写明"基于真实包发现，非登记表" |

---

## 二、摘要

**10 个工具的分档**

- 保留薄壳 **2**：`dependency_arbitration`、`claim_capability`（后者为加壳型薄壳，真复用权威服务）
- 合并入口 **1**：`generate_module_template`
- 去重保壳 **3**：`validate_module_compliance`、`rollback_gate`、`switch_active_pointer`
- 真重复可直接删 **4**：`compatibility_check`、`reuse_search`、`create_core_snapshot`+`query_core_snapshot`（一对，与 `核心快照模板.py` 二选一，建议删 MCP 侧）

**本批最硬的三条证据（全部实测可复现）**

1. **同一份结果两套实现、摘要逐条相同**：`核心治理.创建核心快照` vs `核心快照模板.生成快照清单模板` → 同输入 `文件数=86`、`文件表` 完全一致、`摘要` 相同（`/tmp/批5证据.py`）。而 `核心快照模板.py:3` 自述「与 核心治理.py 摘要语义一致」。
2. **同职责规则表已漂移 22 项，判定结果不同**：`模块合规.第三方根模块` vs `依赖防火墙.第三方前缀表` 各 11 项独有；合成模块同文件 MCP 报 **6** 条、防火墙报 **3** 条（`/tmp/批5证据2.py`/`批5证据4.py`）。而 `能力调用图审计.py:24-28` 明写「共用同一个 set 对象，禁止在本文件重定义」。
3. **同一份文件上 4 份 CAS**：`工程缓存/制品仓库/平台客户端环境/当前.json` 字段 `[制品摘要, 制品目录, 摘要sha256, 栅栏令牌, 版本]` 现场存在，`发布治理.py:53` 自认与 `平台客户端制品.py` 同契约；写它的有 `发布管理/服务.py:71`、`平台客户端制品.py:321-328/:433`、`构建平台客户端.py:571-602`、`发布治理.py:423`。

**次硬证据（同样实测）**

4. **快照两套格式互不可见**：`工程缓存/核心快照/` 有 5 个平台快照（只有 `摘要.json`+`核心.manifest`），`query_core_snapshot` 返回 `数量 = 0`；`compatibility_check` 对它报 `快照损坏：快照缺少清单.json`。
5. **160 行硬门禁与哲学第 18 条冲突**：`项目说明.md:253`「阈值不设硬门禁…160 不是上限」，`未完成事项.md:85`「不设行数硬门禁」；而本工具把 >160 判 `超限`，核心范围现存 **30 个文件**超限（最大 `运行核心/权威状态.py:1123`）。
6. **复用搜索把内部层当公开候选**：同关键词"图像" 30 vs 39 条，`复用搜索` 暴露 3 个 `支持库.适配层.*` 提供者，`能力搜索器.py:53-54` 明确禁止暴露。
7. **能力占用：结果唯一（同一 DB 同一表），但同一事实两处落账**：`claim_capability` 经 `能力目录.申请占用` 写 `工程缓存/平台控制面/权威状态.db` 的 `占用租约`（权威），同时镜像写 `工程缓存/运行数据/底座运行.db` 的 `能力占用` 域。**注意这一条不足以判它重复**——初判曾误以为它自建第二套实现，实测代码路径证明是加壳（`能力占用.py:67-70/:259-264`），已在表内纠正。

## 三、附录 A：与 `02_使用频次统计.md` 的交叉引用（本批 10 工具）

取自 `工程缓存/MCP任务观测/事件.jsonl`（活服务 `tools/list` 双源一致的 50 工具口径）：

| 工具 | 总次数 | 有效次数（带开工上下文） | 最后使用（CST） | 与本文档结论的关系 |
|---|---:|---:|---|---|
| `claim_capability` | 22 | 22 | 2026-09-11 21:49 | 集中爆发：`09-11T13:49:31~32` 同一 0.6 秒内 14 次 = 工作包批量登记，非稳态使用；结论"保留薄壳"不变，但**不是高频门面** |
| `reuse_search` | 7 | 7 | 2026-09-11 17:13 | 与"真重复可删"相容：低频 + 口径错误 |
| `validate_module_compliance` | 5 | 1 | 2026-09-12 11:17 | 低频（有效仅 1）；"去重保壳"不变 |
| `dependency_arbitration` | 5 | 3 | 2026-08-18 21:18 | 低频但零复制逻辑；"保留薄壳"不变 |
| `switch_active_pointer` | 2 | 2 | 2026-08-05 15:40 | 仅早期 2 次；印证"4 份 CAS 里这条最小用" |
| `compatibility_check` | **0** | 0 | **从未调用** | 直接支撑"真重复可直接删" |
| `create_core_snapshot` | **0** | 0 | **从未调用** | 直接支撑"真重复可直接删" |
| `query_core_snapshot` | **0** | 0 | **从未调用** | 直接支撑"真重复可直接删" |
| `rollback_gate` | **0** | 0 | **从未调用** | 印证"自建 CAS 无端到端证据"；仍建议去重保壳而非直删（回滚门面在 8766 侧位置合理） |
| `generate_module_template` | **0** | 0 | **从未调用** | 印证"同一门面两入口、缩水版在 CLI"；合并入口优先级低 |

> 本批 10 个工具里 **5 个是零调用僵尸**（`compatibility_check`/`create_core_snapshot`/`query_core_snapshot`/`rollback_gate`/`generate_module_template`），占全平台 8 个僵尸的 62.5%。**"从未被调用"本身不构成删除理由**（可能是入口不可达或没人知道），但与本文档的实现级重复证据叠加后，`compatibility_check`/`create_core_snapshot`/`query_core_snapshot` 三个可以直接删；`rollback_gate`/`generate_module_template` 因门面位置合理（回滚、模块创建）应保留为薄壳。

## 四、任务副作用披露（只读任务内的自造文件）

- 本批为取证运行了 4 个脚本（全在 `/tmp`），其中一次误把 `存储目录` 传成 `项目根/平台控制面`（正确值应为 `项目根/工程缓存/平台控制面`，见 `项目服务.py:67`），导致 `平台控制面/权威状态.db` 被**创建**（21 张表，仅 `进程` 表 3 行，3 行时间戳 `1789466745/1789466783/1789467182 = 18:05:45/18:06:23/18:13:02` 全部对应本轮 3 次运行，`元信息.结构版本=1.4.0`，其余表全空）。
- 处置：**已把该文件移动到 `/tmp/批5_误建_平台控制面_权威状态.db`**（未删除，可恢复），仓库内 `平台控制面/` 恢复原状。
- 另核实：`工程缓存/核心快照/` 仍是 5 个快照（未新增）；`git status` 除本目录 `开发文档/临时文档/20260915_MCP收敛/` 外无本轮新增。仓库内既有改动（`开发文档/主开发文档.md`、`新建支持库与模块指南.md`、`项目说明.md` 已改，`开发工具/组件规范/技能模板生成器.py`、`开发文档/新建技能包指南.md` 未跟踪）**非本轮产生**。


## 五、不确定项

1. **`claim_capability` 的授权桥未定（保留但缺授权）**：它已真复用权威服务（不再是删除候选），但平台侧同一动作必须经 `统一入口.执行操作(令牌=…)`（`统一入口.py:110-120`，`授权.py:30` 申请能力占用权限级 3），而 8766 MCP 进程当前无身份令牌（`子报告_11:1656`：本机任意进程可调全部工具、无凭证）。**是否给 8766 注入受限服务身份**（引导身份？只读+占用权角色？）需专题裁决；未定前本工具保持免授权现状，只收敛镜像账本。
2. **`compatibility_check` 删后是否有调用方依赖**：全仓 grep 只有 `项目服务.py` 与 `测试中心/MCP工具箱/测试_核心治理.py` 引用（无 CLI/门禁/其它模块）。删除时需同步删 `测试_核心治理.py` 中相关用例；**是否存在外部（非本仓）AI 客户端按工具名调用**，本批无法核实。
3. **`switch_active_pointer` 的 CAS 收敛目标未定**：`平台控制面/发布管理` 是 DB 指针（`激活指针` 表，`指针id=包id`），而本工具/制品链是**文件指针**（`平台客户端环境/当前.json`）。两者是"同一概念的两种载体"，收敛需要一次明确裁决（文件指针是否降级为 DB 指针的投影）。本批只确认"4 份实现都写同一文件"，未做收敛方案验证（属写操作，本批只读）。
4. **`创建/查询核心快照` 二选一的最终归属未定**：推荐保留 `核心快照模板.py`（指南列为"必须用"），但它目前只被 `测试中心/开发工具/测试_核心快照模板.py` 调用，**无生产调用方**；而 MCP 侧有生产调用方（MCP 工具本身）。归属裁决需要产品口径，不是纯技术判断。
5. **`模块模板生成器` 与 `支持库模板生成器`/`核心快照模板` 的关系**：本批已核实 `模块模板生成器` 不 import 另两个模板生成器（三者目标不同：模块包 / 支持库包 / 快照骨架），**不构成重复**；但 `开发工具/开发入口.py:141-163 创建支持库模板` 是**另一份手写九要素支持库模板**（与 `支持库模板生成器.生成支持库模板` 同职责、且九要素目录多一个 `依赖契约/配置契约/权限契约` 差异），属**批外重复**，建议交由负责"开发工具/组件规范"那一批处理。
6. `query_core_snapshot` 的 `limit`/筛选能力缺失、`reuse_search` 的 `limit` 参数声明未生效——均为已存在的小缺陷，记录备查，本批不改。
