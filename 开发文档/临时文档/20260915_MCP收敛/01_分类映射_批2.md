# 批 2 · 反馈 / 记忆 / 项目上下文 类 MCP 工具三分类映射（10 个）

> 只读分析，未改任何代码、未重启服务、未做 git 写。
> 根：`~/Documents/Agent/PHP/系统工程平台`；Python 3.14；`unset PYTHONPATH`。
> 判据来源：`开发文档/新建支持库与模块指南.md` 分层归属表 + 哲学第 10 条「结果唯一即收口」+ 第 11 条「空转即杀」+ 第 17 条「临时脚本不留库」；`开发文档/通用能力反馈接口与平台自治治理.md`；`AGENTS.md:11`（不再新增项目记忆 Markdown）；`开发文档/未完成事项.md`（运行态入库口径）。
> 冻结基线：HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`（与 `子报告_11` 同基线）。

## 分类口径（五选一，本表统一使用）

| 代号 | 含义（引指南/目标） |
| --- | --- |
| **薄壳保留** | 已是「查询能力/调用能力」薄壳：只做入参归一化 + 转发底座能力/账本，8766 保留 |
| **收敛→支持库** | 真实实现是原子能力（存储/脱敏/有界写），应下沉 `支持库/后端/<包>`，8766 只留薄壳 |
| **收敛→适配层提供者/环境依赖** | 第三方工具/外部接口翻译，应在 `支持库/适配层/<提供者>` + 适配层声明层声明环境依赖 |
| **收敛→模块库** | 多能力组合成一个业务动作，只经 `获取能力调用器().调用能力(能力id, 参数)` |
| **收敛→技能库** | 固定工作流（先做什么后做什么），不新增底座能力 |

## 映射表（固定 11 列）

| 工具名 | 真实职责（一句话） | 实现位置（文件:行号） | 入参 | 返回 | 实际调用/读写的底座能力、库或外部接口（能力id/路径） | 是否重复（证） | 权限边界 | 分类建议 | 理由（引指南条目/哲学条） | 迁移落点 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `mcp_feedback` | 按开工id把「总结/不满意/多余/缺失/升级建议」五字段脱敏后追加进 JSONL 反馈账本（成功验证入账的前置门禁） | 定义 `MCP工具箱/项目服务.py:426`；分发 `:658-667`；实现 `MCP工具箱/使用反馈.py:24-44`；路径常量 `项目服务.py:60` | `work_id?`、`summary`、`dissatisfaction`、`redundant`、`missing`、`upgrade_suggestion`（后五个必填） | `{成功:true, 开工id, 反馈门禁:"已满足"}` | 写 `~/Documents/Agent/PHP/系统工程平台/开发文档/项目证据/MCP使用反馈.jsonl`（488 KB，现场）经 `公共契约.运行时.有界IO.追加JSONL`；**未**调用 `平台控制面/能力反馈/反馈服务.py` | **与「通用能力反馈接口」不重复**（对象不同：本工具=开发过程/开工id/五字段/JSONL；能力反馈=运行时诊断/能力id+契约版本+错误码/8 态状态机/SQLite）；**但存在缺口**：升级候选无状态机、无回写、无转正式需求通道 | 无认证（8766 回环直通）；**未走 `_有效开工id`**（`项目服务.py:659` 直接用原始 `work_id`）→ 任意 16 位 id 可写，自证门禁（审计「重要-3」已证） | **收敛→支持库**（8766 留薄壳） | 五字段非空校验 + 脱敏 + 有界追加是可复用原子能力，不属于「查询/调用薄壳」；同一职责在控制面已有 `反馈服务.py`，两条腿违反哲学第 10 条 | 新建/并入 `支持库/后端/能力反馈支持库`（登记/查询/脱敏/有界上限）；补 `_有效开工id` 校验；与 `平台控制面/能力反馈/反馈服务.py` 同一事实源 |
| `feedback_status` | 查「本次开工id」是否已提交使用反馈 | 定义 `项目服务.py:427`；分发 `:668-669`；实现 `使用反馈.py:47-58` | 无（内部用 `当前开工id`） | `{已反馈, 开工id, 反馈?, 查询是否截断?}` | 只读 `开发文档/项目证据/MCP使用反馈.jsonl` 经 `读取JSONL` | 否（纯查询，与 `feedback_review` 粒度不同：本工具只认当前开工id） | 无认证；**边界最严**：不接受 work_id 参数，只能用进程全局 `当前开工id`（`项目服务.py:73`） | **薄壳保留** | 纯查询能力，符合「8766 只留查询/调用薄壳」目标；无独立逻辑 | 保留；实现体随 `能力反馈支持库.查询反馈状态` 下沉 |
| `feedback_review` | 维护者按开工id/任务读原始反馈与升级候选列表 | 定义 `项目服务.py:428`；分发 `:670-676`；实现 `使用反馈.py:61-89` | `work_id?`、`task?`、`limit?` | `{数量, 反馈列表[], 升级候选[], 查询是否截断?}` | 只读 `开发文档/项目证据/MCP使用反馈.jsonl`；`升级候选` 由「不满意/多余/缺失/升级建议」四字段非「无」者拼出 | 否（反馈的读侧）；**与 `register_requirement` 断链**——升级候选不落入 `工程缓存/需求登记/` | 无认证；**有 `_有效开工id` 校验**（`项目服务.py:672` → `:292-304`），空 work_id 不退化为全量读 | **薄壳保留** | 纯查询能力（只读+聚合），符合薄壳目标；缺口在「候选→需求→状态回写」链路缺失，属平台升级池缺口，不属本工具职责 | 保留；补 `升级候选 → 平台控制面/需求登记.py` 通道（与 `register_requirement` 同源） |
| `memory_search` | 在**Markdown 目录**里按关键词打分搜「系统工程平台自己的项目记忆」 | 定义 `项目服务.py:437`；分发 `:686-687`；实现 `_搜索记忆` `:209-225`；路径常量 `:58` | `query`（必填）、`limit?`（1-10，默认 5） | `[{路径, 分数, 摘要(前1200字)}]` | 读 `~/Documents/Agent/PHP/系统工程平台/开发文档/项目记忆/*.md`（**该目录现场不存在**，`ls` 报 No such file or directory） | **重复（重）：与官方 `记忆支持库` 撞车**——`支持库/后端/记忆支持库/实现/记忆.py:23` 的权威库 = `~/Documents/Agent/PHP/系统工程平台/工程缓存/运行数据/记忆库.db`（表 `项目记忆` 2 行 / `记忆向量` 0 行），实现头注释 `记忆.py:6` 明写「存储：SQLite 记忆库（唯一权威），**不保留历史 Markdown 双份**」 | 无认证；**无任何路径/开工id 边界校验**，任意调用者可搜任意标题文件 | **收敛→支持库**（或直接删除） | 哲学第 10 条「结果唯一即收口」+「运行态一律入库」：Markdown 腿是被 `记忆支持库` 明确废弃的第二份；`AGENTS.md:11` 也已声明「不再新增项目记忆 Markdown」 | 删除本工具，改调 `支持库.后端.记忆支持库.搜索记忆`（能力id `记忆支持库.搜索记忆`，9 能力之一）；8766 如需暴露则留薄壳 |
| `memory_write` | 往**Markdown 目录**写一条带 YAML frontmatter 的长期记忆（标题白名单→文件名） | 定义 `项目服务.py:438`；分发 `:688-689`；实现 `_写入记忆` `:228-238`；路径常量 `:58` | `title`、`body`（必填）、`labels?` | `{成功:true, 路径}` | 写 `~/Documents/Agent/PHP/系统工程平台/开发文档/项目记忆/<标题>.md`（临时文件 + `replace` 原子改名）；目录不存在时 `mkdir` 自建 | 同上，**同一处重复**；且与 `支持库.记忆支持库.写入记忆`（自动生成向量、记忆类型/深度/标签/智能体/状态）能力明显更弱 | 无认证；无路径/开工id 边界；标题仅做字符白名单（无穿越，但也无授权） | **收敛→支持库**（或直接删除） | 哲学第 10 条 + 第 11 条（空转即杀）：目录现场不存在 = 从未真实使用；官方能力已含向量与完整字段，属第二实现 | 删除本工具，改调 `记忆支持库.写入记忆`；`开发文档/项目记忆/` 目录不再创建 |
| `project_context` | 返回开工上下文：项目身份/根目录 + 代码地图状态 + 证据可信度（20+30+匹配×25）+ 最近 1-3 次成功验证 + 工作区快照 | 定义 `项目服务.py:422`；分发 `:635-636`；实现 `_开工上下文` `:155-206`（配套 `_代码指纹` `:104-115`、`_读取成功记录` `:118-131`、`_代码地图状态` `:134-142`、`_工作区快照` `:145-152`） | `task?`、`history_limit?`（1-3，默认 3） | `{项目{名称,根目录,MCP实例,角色门面,任务,开工id}, 代码地图, 证据可信度, 最近成功验证[], 工作区快照, 固定流程, 探索策略, 验证策略, 反馈门禁}` | ① `git rev-parse HEAD` / `git status --short` / `git worktree list`（外部 git CLI，cwd=项目根）② 读 `开发文档/项目证据/验证历史.jsonl`（228 KB）③ 经 `_执行` 调**外部 CLI** `codegraph status <项目根>` ④ 委托 `MCP工具箱.发布治理.当前工作区指纹` | **部分重复**：`development_start` = 本工具 + 验证计划 + 观测 + 文件租约（见下），`development_start` 是超集 → 开工上下文本体无第二实现，但**同一结果有两个入口** | 无认证；`当前任务名称/当前开工id` 是**模块级全局变量**（`项目服务.py:72-73`，`:156-159`），8766 单事件循环多会话共享 → 会话间开工id 互相顶掉（审计「重要-4」已证） | **收敛→模块库**（8766 留查询薄壳） | 指南「组装能力（多能力组合成一个业务动作）→ 模块库」：本工具组合了版本控制/账本读取/代码地图/指纹四类来源，却直接 `subprocess` + 直读文件，未走「只经能力调用器」 | 新建 `模块库/开工上下文`（组合 `版本控制支持库.Git操作.*` + `文件系统.*` + `CodeGraph提供者.*` + 证据账本能力）；8766 留薄壳 |
| `role_profile` | 返回单网关（无角色）模式说明 + 全部可用工具名清单 | 定义 `项目服务.py:423`；分发 `:644-649`；实现体 `MCP工具箱/角色权限.py:26-36`（`网关说明()`） | 无 | `{模式, 实例, 说明, 权限模式, 边界, 可用工具[], 可用工具数}` | 读模块常量：`角色权限.py:11 网关实例名`、`:14 网关角色名`；工具清单来自内存 `_工具定义列表` | **重复（中）：与 `tool_catalog` 撞车**——`tool_catalog`（`项目服务.py:470`、`:578-614`）返回同一份工具清单且带 中文名/协议名/描述/分类，`role_profile` 只是它的子集 + 一段固定文案 | 无认证；只读静态信息，无风险面 | **薄壳保留**（建议与 `tool_catalog` 合并） | 「角色体系已整体移除」（`角色权限.py:1-5`），本工具已是空壳文件上的描述器；保留符合薄壳目标，但两份工具清单违反哲学第 10 条 | 保留薄壳；建议把「网关说明」并入 `tool_catalog` 返回，`role_profile` 降为别名或删除 |
| `codegraph_explore` | 在**本仓 `.codegraph/` 代码地图**里探索符号/源码/调用链（转发外部 codegraph CLI） | 定义 `项目服务.py:436`；分发 `:684-685`；实现 `_探索代码` `:314-325`（配套 `_代码地图状态` `:134-142`、`_执行` `:87-101`） | `query`（必填） | `{退出码, 标准输出(截 48000), 标准错误(截 4000), 查询范围[12 个正式目录]}` | **外部 CLI，非 MCP 转发**：`subprocess.run(["codegraph","explore",查询,"--max-files","20"], cwd=项目根, timeout=60)`；二进制 = `~/.npm-global/bin/codegraph`（Node 工具，`.codegraph/daemon.log` 记 v1.5.0）；索引 = `~/Documents/Agent/PHP/系统工程平台/.codegraph/` | 否（底座内无第二处代码地图实现）；但**外部有第二入口**：Hermes 已注册独立 MCP `mcp__codegraph__codegraph_explore`，两者指向同一 CLI | 无认证；`query` 原样传 CLI（无注入面，`shell=False`）；`cwd` 固定项目根，不接受调用者路径（这点比多数工具严） | **收敛→适配层提供者/环境依赖** | 指南第 3 行「独立进程/服务/动态库/外部接口 → 声明为**环境依赖**（适配层声明层分段）」；第 2 行「把第三方库翻译成中文能力 → `支持库/适配层/<提供者>`」。**明确不是「另一个 MCP 服务的转发」**，是外部 CLI 子进程 | 新建 `支持库/适配层/CodeGraph提供者`（能力：探索代码/代码地图状态），并在适配层声明层声明环境依赖（入口 `codegraph`、版本下限、健康探针 `codegraph status`、降级=返回「地图不可用」）；8766 留薄壳 |
| `temporary_context` | 按开工id 读写/核对/清理子任务**临时上下文**（允许目录/已确认事实/验证计划，TTL 300-86400s，默认 2h，过期自动删） | 定义 `项目服务.py:442`；分发 `:720-748`；实现 `MCP工具箱/临时上下文.py:26-70`（+`核对修改范围` `:73-97`、`清理过期上下文` `:100-113`）；路径常量 `项目服务.py:61` | `operation`（写入/读取/核对范围/清理）、`work_id`；写入时还有 `parent_task`、`role`、`allowed_paths`、`memory_queries`、`confirmed_facts`、`verification_commands`、`ttl_seconds` | 写入 `{成功, 开工id, 过期秒数}`；读取 `{成功, 开工id, 父任务, 角色, 允许目录, 定向记忆查询, 已确认事实, 验证计划, 创建时间, 过期时间, 剩余秒数}`；核对 `{成功, 越界路径[], 错误码 TEMPORARY_CONTEXT_SCOPE_MISMATCH}` | 写/读 `~/Documents/Agent/PHP/系统工程平台/工程缓存/MCP临时上下文/<开工id>.json`（临时文件 `.json.tmp` + `replace`）；**注意 `工程缓存/运行数据/底座运行.db` 已有 `会话` 表但 0 行、全仓 0 写入方（死表）** | 否（无第二处子任务上下文实现）；`清理过期上下文` 由 `development_start` 顺带调用（`项目服务.py:404`） | 无认证；**写入有 `_有效开工id` 校验**（`项目服务.py:726-729`，未授权返回错误码「未授权」）；`_安全标识` 严格白名单（仅 `[A-Za-z0-9_-中文]`、≤64、拒 `/ \ NUL`、拒 `.`/`..`） | **收敛→支持库**（8766 留薄壳） | 「运行态一律入库」（`未完成事项.md:174-181` 三域入库口径；`子报告_09` 第三节结论「代码层成立」）：这是**会话态**，却写 JSON 文件而非运行库；且底座已有等价但空置的 `会话` 域 | 新建 `支持库/后端/会话态存储`（有界 TTL 键值 + 范围核对），落 `工程缓存/运行数据/底座运行.db` 的 `会话` 域；8766 留薄壳 |
| `development_start` | 开发统一开工入口：**一次**返回开工上下文 + 验证计划，并**开工即批量申请文件租约**（全成功才放行） | 定义 `项目服务.py:441`；分发 `:696-719`；实现 `_统一开发入口` `:399-417` + `_验证计划` `:328-396` + 文件租约 `:704-719`（底层 `MCP工具箱/文件租约.py:54-90`） | `task`（必填）、`modified_paths?`、`level?`（工作包/合并波次/阶段收口/正式发布）、`artifact?`、`history_limit?` | `{开工上下文{...}, 验证计划{修改路径,验证级别,受影响测试目录,并行建议,建议命令[],是否需要全量,说明}, 文件租约}`；租约冲突时 `{成功:false, 错误码, 消息, 冲突文件, 占用者, 占用开始时间, 已回滚[], 处置}` | ① `_开工上下文`（见 `project_context` 行）② `清理过期上下文(工程缓存/MCP临时上下文)` ③ `任务开始(...)` 写 `工程缓存/MCP任务观测/事件.jsonl` ④ 读 `测试中心/**/测试_*.py` 文件系统枚举 ⑤ `申请文件租约(工程缓存/平台控制面, ...)` → 底层复用 `平台控制面/能力目录/服务.py` 的 `占用租约`（键 `文件::<路径>`、领域 `文件`，原子插入 + 心跳 300s + 过期回收） | **重复（超集）**：`development_start` ⊃ `project_context` + `verification_plan`（`:690-695` 独立入口仍存在，`AGENTS.md:7` 仍要求先调 `project_context`）→ 同一结果两条调用路径（反馈账本 `MCP使用反馈.jsonl:16` 已记录「可用 development_start 替代重复的 project_context + verification_plan」） | 无认证；**`modified_paths` 无 relative_to 项目根校验**——`文件租约.申请文件租约` 只做 `归一化路径()`（`文件租约.py:62`），路径可逃出项目根（与 `子报告_11` V6/建议-1 同型）；但租约本身是真互斥、真回滚 | **收敛→模块库**（8766 留调用薄壳） | 指南「组装能力（多能力组合成一个业务动作）→ 模块库，只经 `获取能力调用器().调用能力(能力id, 参数)`」：本工具组合了上下文/验证计划/观测/租约四件事，却在 MCP 进程内直接编排；且两条开工入口违反哲学第 10 条 | 新建 `模块库/开工编排`（组合开工上下文能力 + 验证计划能力 + 观测能力 + 文件租约能力）；`project_context` 与 `verification_plan` 收口为它的子集或删除；8766 留唯一调用薄壳 |
| `task_observation`（**重点查证附带项**，不在 10 个内） | 被动记录任务/阶段/工具耗时事件并生成效率报告（不记提示词与源码） | 定义 `项目服务.py:443`；分发 `:749-771`；实现 `MCP工具箱/任务观测.py`；路径常量 `项目服务.py:62` | `operation`（开始/结束/查询/阶段开始/阶段结束/报告）、`task_id`；另有 `work_id`、`parent_task_id`、`success`、`error_code`、`child_count`、`phase`、`phase_id`、`note` | 任务/阶段事件报告（含最大耗时阶段、工具耗时、未分类间隔、未闭合阶段） | 写 `~/Documents/Agent/PHP/系统工程平台/工程缓存/MCP任务观测/事件.jsonl`；每次工具调用结束由 `工具事件(...)` 自动落一条（`项目服务.py:970`） | 否（观测唯一实现） | 无认证；**有 `_有效开工id` 校验**（`项目服务.py:752`） | **收敛→支持库**（8766 留薄壳） | 运行态 JSONL 未入库，与「运行态一律入库」冲突；但属审计轨迹类，需与 `未完成事项.md` 的「审计/失败/事件三类判为审计轨迹，保留文件」口径一并裁决（见不确定项） | 新建/并入 `支持库/后端/观测账本`（有界事件 + 聚合报告）；或按现行口界保留文件式但纳入 `工程缓存/` 边界声明 |

## 摘要

1. **`memory_search` / `memory_write` 不是外部 Hermes 记忆体系，是底座自己的第二条腿（且已死）。**
   它们读写的路径是绝对路径 `~/Documents/Agent/PHP/系统工程平台/开发文档/项目记忆/*.md` —— **该目录现场不存在**（`ls` → No such file or directory），`MCP工具箱/` 全目录对 `hermes/.hermes` **0 引用**。真正权威的记忆库是 `支持库/后端/记忆支持库/实现/记忆.py:23` 的 `~/Documents/Agent/PHP/系统工程平台/工程缓存/运行数据/记忆库.db`（表 `项目记忆`/`记忆向量`），该文件头注释 `记忆.py:6` 明写「SQLite 记忆库（唯一权威），**不保留历史 Markdown 双份**」。→ 结论：**内腿重复，非外腿**；处置建议直接删除两个工具，改调 `记忆支持库.搜索记忆` / `记忆支持库.写入记忆`（该包 9 个正式能力已在能力目录登记）。
2. **`codegraph_explore` 不是另一个 MCP 服务的转发**，是**外部 CLI 子进程**（`subprocess.run(["codegraph", ...])`，二进制 `~/.npm-global/bin/codegraph`，Node 工具 v1.5.0，索引 `~/Documents/Agent/PHP/系统工程平台/.codegraph/`）。应按指南第 3 行归「环境依赖」+ 第 2 行归「适配层提供者」，**不属底座能力**。（Hermes 侧另有一个同源 MCP `mcp__codegraph__codegraph_explore`，同一 CLI 的第二入口。）
3. **`mcp_feedback` / `feedback_status` / `feedback_review` 与底座已有《通用能力反馈接口与平台自治治理》＋`平台控制面/能力反馈/反馈服务.py` 是两条腿、不是重复，但存在真实缺口。**
   两条腿的对象不同（开发过程/开工id/五字段/JSONL ↔ 运行时诊断/能力id+契约版本+错误码/8 态状态机/SQLite），不构成第 10 条重复。缺口有两处：①`mcp_feedback` **未走 `_有效开工id`**（`项目服务.py:659` 用原始 `work_id`）→ 自证门禁（审计「重要-3」）；②`feedback_review` 产出的「升级候选」**没有任何通道**进入平台升级池（`register_requirement` 走的是另一条腿 `工程缓存/需求登记/`），即「反馈→需求→修复→状态回写」在 MCP 侧只有读、没有状态迁移。
4. **`temporary_context` / `task_observation` 都是会话态/观测态，却写 JSONL 文件而非入库。**
   会话态落 `工程缓存/MCP临时上下文/<开工id>.json`，观测态落 `工程缓存/MCP任务观测/事件.jsonl`；而 `工程缓存/运行数据/底座运行.db` 里**已有 `会话` 表但 0 行、全仓 0 写入方（死表）**。与「运行态一律入库」（`未完成事项.md:174-181` 三域已入库，`子报告_09` 第三节）冲突 → 会话态应迁入 `会话` 域；观测态属审计轨迹，需裁决。
5. **`role_profile` / `project_context` 的对应关系：**
   - `role_profile` 对应的「平台控制面角色权限」**已不存在**——`MCP工具箱/角色权限.py` 已重写为 45 行空壳（「角色体系已整体移除」），本工具只是空壳上的固定文案 + 工具清单，且**与 `tool_catalog` 撞车**（后者返回同一份清单且更全）。建议并入 `tool_catalog`。
   - `project_context` 对应的「开工证据」能力**部分存在**：证据账本 `开发文档/项目证据/验证历史.jsonl`、工作区指纹委托 `MCP工具箱/发布治理.py`、文件租约复用 `平台控制面/能力目录/服务.py` 的 `占用租约`。但它是**组合体**（git + 账本 + 代码地图 + 指纹），且 `development_start` 是它的**超集**（同一结果两个入口），应共同收敛进一个模块库开工编排能力。
6. **权限边界全局事实（对本批 10 个工具全部适用）：** 8766 为单网关无角色（`role_profile.py` 常量 `网关实例名` / `网关角色名`），全部 50 工具对回环调用者直通、**无认证**（`子报告_11` 阻断-3）；本批内唯一做 `_有效开工id` 校验的是 `feedback_review` / `temporary_context(写入)` / `task_observation`；`mcp_feedback`、`memory_search`、`memory_write` **无开工id 校验**；`development_start` 的 `modified_paths` **无项目根边界校验**（`文件租约.py:62` 仅归一化）。

## 不确定项（不得当结论用）

1. **`开发文档/项目记忆/` 是否曾存在过、被谁清空**：只确认「现场不存在」+「无代码写入它以外的引用」；未查 git 历史，无法断言是「从未使用」还是「迁移后删除」。（`AGENTS.md:11`「不再新增项目记忆」是最强的旁证，但不是直接证据。）
2. **`记忆库.db` 的 2 行 `项目记忆` 是谁写的**：只读了库与实现，未反查写入方调用链（可能由 40007 侧或历史脚本写入）。也未确认 `memory_write` 是否曾被真实调用过（反馈账本 488 KB 未逐条统计工具名）。
3. **`codegraph` CLI 是否已在适配层声明层登记为环境依赖**：只确认未找到任何 `CodeGraph*提供者` 包（`支持库/适配层/` 现场 28 个提供者，无 CodeGraph）；未逐行读适配层声明层分段文件确认「未登记」。
4. **`task_observation` 的事件 JSONL 是否被 `未完成事项.md` 的「审计轨迹保留文件」口径豁免**：只读到「发布/诊断事件待迁；审计/失败/事件三类判为审计轨迹保留文件」这句概括，未找到把「MCP任务观测」明确列名归类的原文。
5. **`development_start` 的文件租约路径逃逸是否真实可达**：只确认 `申请文件租约` 无 `relative_to` 校验；未实测用 `../` 路径申请租约（会写库，冻结边界禁止）。
6. **8766 运行中实例是否与本批源码一致**：`子报告_11` 实测 `tools/list` = 50 且 `serverInfo.version = 1.28.1`，与本表读到的源码一致；但本次未再发起 HTTP 实测（只读分析）。

## 取证命令（全部真实执行，可复现）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
# 工具定义与分发
grep -n 'Tool(name=' MCP工具箱/项目服务.py
sed -n '55,62p;155,238p;314,325p;399,417p;617,720p' MCP工具箱/项目服务.py
# 记忆腿 vs 记忆库
sed -n '1,30p' 支持库/后端/记忆支持库/实现/记忆.py
ls -d 开发文档/项目记忆            # → No such file or directory
grep -rn "hermes\|\.hermes" MCP工具箱/*.py     # → 0 命中
python3.14 -c "import sqlite3;c=sqlite3.connect('工程缓存/运行数据/记忆库.db');print([r[0] for r in c.execute(\"select name from sqlite_master where type='table'\")])"
# codegraph 外部 CLI
which codegraph && codegraph --help
# 反馈两条腿
sed -n '92,199p' 平台控制面/能力反馈/反馈服务.py
# 运行库域与死表
python3.14 -c "import sqlite3;c=sqlite3.connect('工程缓存/运行数据/底座运行.db');print([(r[0],c.execute(f'select count(*) from \\\"{r[0]}\\\"').fetchone()[0]) for r in c.execute(\"select name from sqlite_master where type='table'\")])"
grep -rn '"会话"' --include=*.py 支持库/ 运行核心/ MCP工具箱/ 平台控制面/   # → 0 命中（死表）
# 文件租约边界
sed -n '54,90p' MCP工具箱/文件租约.py
# 技能库（迁移落点参照）
python3.14 -c "import json;d=json.load(open('技能库/后端/技能库/能力定义.json',encoding='utf-8'));[print(c.get('能力id')) for c in d['能力列表']]"
```
