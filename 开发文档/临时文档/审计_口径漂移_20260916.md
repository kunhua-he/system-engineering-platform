# 口径漂移审计（2026-09-16）

> 第 4 路【口径漂移】只读审计。**本轮未修改任何项目文件**，临时脚本/探针全部在 `/tmp/` 或命令行内完成。
> 环境：`export PATH=$(dirname $(which git)):$PATH  # 若本机 git 不可用，见开发文档的环境注记` + `unset PYTHONPATH` + `python3.14`。
> 在途改动（`支持库/适配层/密码签名提供者/**` 等，`git status` 未提交项）属其他会话，本报告不计为问题。

---

## 一、真实基线（实测）

| 口径 | 实测值 | 取证命令 |
|---|---|---|
| **运行时装配（唯一权威口径）能力数** | **611** | `python3.14 -c "from pathlib import Path; from 后端核心.后端核心 import 后端核心; b=后端核心(Path('.')); b.启动(); print(b.状态快照()['能力数'])"` |
| 正式包索引 | 支持库 **107** + 模块库 **18** + 技能库 **1** = **126 包**；能力所有者 **611**；冲突 **0**；排除包 **0** | `python3.14 -c "from 开发工具.项目编译.正式包索引 import 构建索引; print(构建索引('.'))"` |
| 源头包数（磁盘 `包声明.json`） | **131** | `find 支持库 模块库 技能库 平台控制面 运行核心 -name 包声明.json \| wc -l` |
| 源头能力数（`能力定义.json`） | **125 份** | `find 支持库 模块库 技能库 平台控制面 运行核心 -name 能力定义.json \| wc -l` |
| 完整性摘要扫描正式包 | **131** 个目录 | `python3.14 -c "from pathlib import Path; from 开发工具.组件规范.完整性摘要 import 扫描正式包; print(len(扫描正式包(Path('.'))))"` |
| 非适配层口径 `装配系统()` | **553** | `python3.14 -c "from 公共契约.能力契约.契约 import 能力注册表; from 运行核心.加载器.生命周期管理.管理器 import 装配系统; from pathlib import Path; print(装配系统(Path('支持库'),Path('模块库'),能力注册表(),Path('技能库')).已注册能力数)"` |
| **40007 常驻进程实际能力数** | **551**（与源码装配 611 差 60 → 常驻制品是旧版） | `python3.14 运维脚本/热接入.py` 输出首行 `当前能力数: 551` |
| Agent 接入面 | stdio 薄壳 **3 工具**（`tool_catalog`/`capability_search`/`capability_call`），**无 HTTP 端口** | `python3.14 -B 开发工具/薄壳/自测_stdio客户端.py` → `tools/list 工具数 = 3` |
| 端口实测 | **40007 监听中**（PID 96808）；**40006 无监听**；**8766 无监听**；**8866 无监听** | `lsof -nP -iTCP:40006 -sTCP:LISTEN` / `:40007` / `:8766` / `:8866` |
| `com.huashi.gateway-40006` 服务态 | `launchctl list` → 最后退出码 **2**（崩溃/未启动）；plist `ProgramArguments` 指向 **已删除的 `MCP工具箱/能力网关.py`** | `launchctl list \| grep huashi.gateway` ；`python3.14 -c "import plistlib;print(plistlib.load(open('$HOME/Library/LaunchAgents/com.huashi.gateway-40006.plist','rb'))['ProgramArguments'])"` |
| 制品仓库 | 内容寻址目录 **2 份**（`c0c76b1132d78d8147fa7d61d170ef70`、`平台客户端-1c3e59188acba3c4`）；激活指针 `当前.json` → `平台客户端-1c3e59188acba3c4`，**版本 94** | `ls 工程缓存/制品仓库/平台客户端制品/ \| wc -l`；`cat 工程缓存/制品仓库/平台客户端环境/当前.json` |
| Git | HEAD `b02f69fb`，提交总数 **621**，tag **3**；远端 `git@github.com:kunhua-he/system-engineering-platform.git` | `git rev-list --count HEAD`；`git remote -v` |
| 已删除目录 | **`MCP工具箱/` 不存在**（根目录）；残留 `测试中心/MCP工具箱/`（2 个测试文件） | `ls -d MCP工具箱` → `No such file or directory` |

---

## 二、漂移清单

| # | 文档位置 | 文档写的 | 实测/真相 | 证据命令 | 严重度 |
|---|---|---|---|---|---|
| 1 | `开发文档/项目说明.md:41`（口径表「运行时装配（唯一权威口径）」） | 能力数 **554**；已过 HTML 黑盒 814/814 步 | **611** | `python3.14 -c "...后端核心(Path('.')); b.启动(); print(b.状态快照()['能力数'])"` → 611 | **P0** |
| 2 | `项目说明.md:40`（口径表「正式索引」） | 支持库 **96** + 模块库 18 + 技能库 1 = **115 包**、能力所有者 **554**、冲突 0 | 支持库 **107** + 18 + 1 = **126 包**、能力所有者 **611**、冲突 0 | `构建索引('.')` → `{'支持库':107,'模块库':18,'技能库':1,'能力所有者':611,'能力冲突':0,'排除包':0}` | **P0** |
| 3 | `项目说明.md:38`（口径表「源头包数（磁盘）」） | 五个正式根合计 **120** 个 `包声明.json` | **131** | `find 支持库 模块库 技能库 平台控制面 运行核心 -name 包声明.json \| wc -l` → 131 | **P1** |
| 4 | `项目说明.md:39` | `能力定义.json` **114 份** | **125 份** | `find … -name 能力定义.json \| wc -l` → 125 | **P1** |
| 5 | `项目说明.md:45` | 完整性摘要扫描正式包 **120** 个目录 | **131** | `扫描正式包(Path('.'))` → 131 | **P1** |
| 6 | `项目说明.md:44`（「非适配层公开包口径」） | 直接调 `装配系统()` 得 **496**；比唯一口径少 **55** 条 | **553**；比 611 少 **58** 条 | 装配系统探针 → 553 | **P1** |
| 7 | `项目说明.md:46`（2026-09-16 复量收口注） | 「正式索引 115 包 / 能力所有者 554 与运行时装配已同为 **554**」 | 实为 **126 包 / 611＝611**；`装配系统()` 553 仍与 611 不齐 | 同 #1/#2 | **P0** |
| 8 | `项目说明.md:49`（历史值行） | 历史值列举到「549 能力 / 116 包摘要（2026-09-11）」等 | 行本身标了「不是当前值」，**无需改**；但当前值行（#1/#2）未同步，导致读者看到的最大数字是 554 | — | P2（随 #1/#2 一并修） |
| 9 | `项目说明.md:48` | 制品 内容寻址目录 **21 份**；激活指针 `平台客户端-bde9c61158de5f2e`（**版本 91**） | 内容寻址目录 **2 份**；指针 `平台客户端-1c3e59188acba3c4`（**版本 94**） | `ls 工程缓存/制品仓库/平台客户端制品/ \| wc -l` → 2；`cat 工程缓存/制品仓库/平台客户端环境/当前.json` | **P0** |
| 10 | `项目说明.md:43`（口径表「只读能力发现网关」） | `40006`（**`开发工具/能力网关/能力网关.sh 40006`**） | 文件名实为 **`启动能力网关.sh`**（不存在 `能力网关.sh`）；且 **40006 当前无监听** | `ls 开发工具/能力网关/` → `启动能力网关.sh`、`能力网关.py`；`lsof -nP -iTCP:40006 -sTCP:LISTEN` → 空 | **P0** |
| 11 | `项目说明.md:310`（能力域表） | 能力发现网关 = **`MCP工具箱/能力网关.py`（默认 8866）**，可用 | 路径 **已删除**；**8866 无监听**；与同文件 `:43` 的「40006」**自相矛盾**（8866 vs 40006 两套口径并存） | `ls -d MCP工具箱` → 不存在；`lsof -nP -iTCP:8866 -sTCP:LISTEN` → 空 | **P0** |
| 12 | `项目说明.md:311`（能力域表） | Agent 工具接入 = **`MCP工具箱/项目服务.py`**，「单一 MCP 入口」 | 目录已删；现接入面为 **`开发工具/薄壳/薄壳服务.py`（3 工具、无端口）** | `ls -d MCP工具箱`；`python3.14 -B 开发工具/薄壳/自测_stdio客户端.py` → 工具数 3 | **P0** |
| 13 | `项目说明.md:696` | 「**549 个**正式能力全部可作为后台任务提交」 | **611** | 同 #1 | **P1** |
| 14 | `AGENTS.md:5` | 「本仓库使用独立 MCP：system_engineering_toolkit（**Streamable HTTP**，配置见根目录 opencode.json，**remote http://127.0.0.1:8766/mcp/**）」 | 8766 **已下线无监听**；`opencode.json`/`.mcp.json` 已改指 **stdio 薄壳**；与本文件 `:49`（「8766 已于 2026-09-15 删除、8766 已下线、已切 stdio 薄壳」）**自相矛盾** | `lsof -nP -iTCP:8766 -sTCP:LISTEN` → 空；`cat opencode.json` → `"type":"local"` 指向 `薄壳服务.py` | **P0** |
| 15 | `AGENTS.md:8` | 「MCP 客户端如连接，仅允许使用唯一的 `http://127.0.0.1:8766/mcp/`」 | 该地址已不存在，照做必然连不上 | 同 #14 | **P0** |
| 16 | `AGENTS.md:16` | 「本平台只保留一个权威 MCP 服务 system_engineering_toolkit（**HTTP 8766**）」 | 现为 **stdio 薄壳、无 HTTP 端口** | 同 #14 | **P0** |
| 17 | `开发文档/当前流程图.md:112 / :134 / :356` | 能力发现网关 **8866**（`MCP工具箱/能力网关.py`） | 8866 无监听、路径已删；且 `项目说明.md:43` 写的是 **40006** → 全库 8866/40006 两套数字 | `lsof -nP -iTCP:8866 -sTCP:LISTEN` → 空 | **P0** |
| 18 | `当前流程图.md:369` | `MCP（MCP工具箱/项目服务.py, 8766）` | 路径已删、8766 下线 | `ls -d MCP工具箱` | **P1** |
| 19 | `当前流程图.md:721` | 「能力注册表内…相关能力 = 0 条（**498 项能力全扫**）」 | **611** 项 | 同 #1 | **P2** |
| 20 | `开发文档/多会话并行开发规约.md:11` | 「本文只引用**本仓 8766 管理端（MCP）真实注册的 46 个工具**」 | 8766 已下线、`MCP工具箱/` 已整目录删除；实际接入面 = **stdio 薄壳 3 工具**；协作机制已迁为底座能力（如 `模块库.开工编排.*`） | 同 #14；`构建索引` 显示 `模块库:18`（含 `开工编排`、`组件生成`） | **P0** |
| 21 | `多会话并行开发规约.md` 全文条 15/19/20/24/25/29/30/33/73/88/148-152/154/162-171/185/187 | 以 `development_start` / `register_task` / `temporary_context` / `file_lease` / `claim_capability` / `apply_file_patch` / `tool_job_submit` / `reload_tool_modules` / `workspace` 为操作面 | 这些工具随 `MCP工具箱/` 与 8766 一并消失，**照着本规约做无法执行**（本文件 mtime 2026-09-16 15:17，晚于 2026-09-15 清场，属未同步的活文档） | `ls -d MCP工具箱` → 不存在；`lsof :8766` → 空 | **P0** |
| 22 | `多会话并行开发规约.md:33` | 「45 个工具里没有动态能力执行入口」 | 与同文件 `:11` 的「46 个工具」自相矛盾；且工具面已整体变更 | — | **P2** |
| 23 | `开发文档/能力目录说明书.md:5` + 表体（9–120 行） | 「共 **112** 份包声明」，表内 112 行 | **131** 份包声明 / 索引 **126** 包；表内缺 `模块库.开工编排`、`模块库.组件生成`、`平台控制面.*` 等新包 | `find … -name 包声明.json \| wc -l` → 131；`构建索引('.')` | **P1** |
| 24 | `开发文档/未完成事项.md:13` | 「第四轮已全部收口：发布门禁 **70 项通过 / 0 项失败（发布状态＝通过）**… 能力数 **538＝538**」 | 实测发布门禁 **发布状态：失败，exit 1**；能力数 **611** | `python3.14 开发工具/发布门禁/运行发布门禁.py` → `发布状态: 失败`，exit 1 | **P0** |
| 25 | `未完成事项.md:121`（B6 README 版本漂移） | 「README:66/69/72 三套口径并存 —— **属实**」（未标已修） | README 已修：`:96` 已写明「历史口径（已失效，勿再沿用）：8766 HTTP MCP…已下线」，全文无 8766 remote 地址 → **该债务条目本身已过期** | `grep -n "8766\|Streamable" README.md` → 仅 :96 一条 | **P2** |
| 26 | `未完成事项.md:66`（待办表） | 「`MCP工具箱` 令牌 A 层…本机任意进程可无凭证调用 **50 工具**」 | `MCP工具箱/` 已删、8766 下线，**条目失效** | `ls -d MCP工具箱` | **P1** |
| 27 | `未完成事项.md:8–30`（〇、当前在办） | 在办/落点仍为 `MCP工具箱/`、8766 管理端、`05_D清场清单` | 清场已完成（同文件 `:793` 自证 15b 完成），在办区的落点全为已删对象 | 同 #20 | **P1** |
| 28 | `开发文档/底座升级方案.md:16` | 「运行态：**8766 当前没有监听进程**。这不是"旧进程仍运行"的事实，而是**服务未启动**」 | 不是「服务未启动」，而是**服务与其载体已整体删除**；按此判断会去「启动」一个不存在的服务 | `ls -d MCP工具箱` → 不存在 | **P1** |
| 29 | `底座升级方案.md:287`（R1 行） | 「重启并核对 **8766 常驻服务**」 | 目标不存在 | 同 #28 | **P1** |
| 30 | `开发文档/提速方案.md:15 / :28 / :198` | 「免重启管理端 **8766**，即时生效」「已经常驻：网关 40007 / **管理端 8766**」「管理端 8766 常驻进程；工具实现改动现已热加载」 | 8766 不存在 | `lsof -nP -iTCP:8766 -sTCP:LISTEN` → 空 | **P1** |
| 31 | 仓库实况 vs `未完成事项.md:793` | 「删 `MCP工具箱/`（22 py 5882 行）+ 18 个旧测试」 | 根目录 `MCP工具箱/` 已删，但 **`测试中心/MCP工具箱/` 仍在**（`测试_能力网关.py`、`测试_能力搜索覆盖率.py`） | `ls 测试中心/MCP工具箱/` | **P2** |
| 32 | `项目说明.md:54` | 「40007 常驻入口 plist…WorkingDirectory 已修正为正式平台目录；当前实测监听 127.0.0.1:40007」 | **属实**（plist `WorkingDirectory=<仓库根>`，40007 在监听）——列为对照，非问题 | `plistlib.load(…gateway-40007.plist)` | 无（已核对） |
| 33 | `项目说明.md:725–730` 常驻配置块 | 日志 `/tmp/网关40007.log` / `.err`；入口 `运行核心/启动运行核心网关.py` | **属实**（plist 三个字段逐项匹配） | 同 #32 | 无（已核对） |
| 34 | `项目说明.md:19`、`:27`、`:32` Git 口径 | 基线提交 `e44b119…` 存在；旧值 `1c69ea5d…` 不再存在；备份库 `系统工程平台_仓库备份_重写前_20260916.git` | **属实**（`git cat-file -t e44b119…` → commit；`1c69ea5d` → 不存在；备份目录存在） | `git cat-file -t e44b119773a3e732b66bad881e2efa46f4a00bec` | 无（已核对） |

### 漂移归因（一句话）
清场（2026-09-15 D 清场 + 8766 下线）与新增包落地（611 能力 / 126 包 / 131 包声明）这两件事**只刷新了 `项目说明.md` 的部分行与 `README.md`**，其余主要文档（`AGENTS.md` 前 16 行、`当前流程图.md`、`多会话并行开发规约.md`、`提速方案.md`、`底座升级方案.md`、`能力目录说明书.md`、`未完成事项.md` 若干条）**没有同批回填**，于是形成「同一仓内三套口径并存」：8766(已删) / 8866(已删) / 40006(未监听)。

---

## 三、坏掉的命令（抽测结果）

**抽测 16 条，跑不通 7 条（含 1 条"能执行但语义失效"）。**

| # | 文档位置 | 文档命令 | 实测结果 | 判定 |
|---|---|---|---|---|
| 1 | `README.md:73` / `项目说明.md:41` | `python3.14 -c "…后端核心(Path('.')); b.启动(); print(b.状态快照()['能力数'])"` | 输出 **611**，exit 0 | ✅ 跑通 |
| 2 | （历史写法，`未完成事项.md:192` 记为已修） | `后端核心('.')` | `TypeError: unsupported operand type(s) for /: 'str' and 'str'`（`后端核心/后端核心.py:117`） | ✅ 已修（现行文档全用 `Path`/`系统根`，仅此处作对照） |
| 3 | `README.md:87` / `AGENTS.md:142` / `主开发文档.md:40` | `python3.14 开发工具/发布门禁/运行发布门禁.py` | **exit 1，`发布状态: 失败`**。红项：`正式制品来源与字节指纹`（制品内 `模块库/测试资源` 参数类型 `文本/布尔/整数` 不在正式类型表）、`依赖防火墙`（4 条越层）、`契约声明一致`（3 条注册/契约不一致）、`HTML契约真实执行矩阵`（能力数 551） | ❌ **跑不通**（且与 #24 债务条目「0 项失败」直接冲突） |
| 4 | `README.md:86` / `AGENTS.md:143` | `python3.14 开发工具/能力搜索/能力搜索器.py --能力 读取文件` | exit 0，正常返回能力清单 | ✅ 跑通 |
| 5 | `README.md:88` / `AGENTS.md:144` | `python3.14 示例项目/适配层示例/运行入口/运行.py` | **exit 1**：`[5] 提供者网关启动失败: 缺失凭证: 环境变量 系统库网关凭证 未设置`（前 4 步正常，`[4] 装配系统: 611 个能力 ✓`） | ❌ **跑不通**（文档未提示需先导出 `系统库网关凭证`） |
| 6 | `README.md:82` | `python3.14 -m 开发工具.HTML验证.验证器 --制品 <目录> --并发 8` | `--help` exit 0，参数 `--制品/--并发/--实例数/--端口池/--激活到` 均存在 | ✅ 跑通（参数面一致） |
| 7 | `项目说明.md:42` / `AGENTS.md:52` | `python3.14 -B 开发工具/薄壳/自测_stdio客户端.py` | exit 0；`tools/list 工具数 = 3`（`capability_search`/`capability_call`/`tool_catalog`） | ✅ 跑通（与「3 工具」口径一致） |
| 8 | `新建支持库与模块指南.md:81` | `python3.14 -m 测试中心.开发工具.测试_唯一版本` | **`No module named 测试中心.开发工具.测试_唯一版本`**，exit 1（该目录下实际只有 `测试_契约版本唯一.py`、`测试_依赖生命周期审计.py` 等 21 个文件） | ❌ **跑不通** |
| 9 | `新建支持库与模块指南.md:80` | `python3.14 -m 开发工具.公开调用完整性门禁` | exit 0，`公开调用完整性门禁通过：六环一致，无违规` | ✅ 跑通 |
| 10 | `新建支持库与模块指南.md:89` | `python3.14 -m 测试中心.开发工具.测试_契约版本唯一` | exit 0，`Ran 4 tests … OK` | ✅ 跑通 |
| 11 | `AGENTS.md:77` | `python3.14 -m unittest 测试中心.第一批维修.测试_HTML验证器` | exit 0，`Ran 42 tests in 6.088s OK` | ✅ 跑通 |
| 12 | `新建技能包指南.md:183` | `python3.14 运维脚本/热接入.py` | **exit 1，`✗ 热接入失败（2.58s）`**：`平台控制面.平台状态: No module named '平台控制面.平台状态.状态存储'; '平台控制面.平台状态' is not a package`（同类 6 处）。首行还暴露常驻态 `当前能力数: 551`（≠ 源码 611） | ❌ **跑不通** |
| 13 | `项目说明.md:43` | `开发工具/能力网关/能力网关.sh 40006` | **文件不存在**（目录内为 `启动能力网关.sh`）；且 40006 无监听，launchd 服务最后退出码 2（plist 指向已删的 `MCP工具箱/能力网关.py`） | ❌ **跑不通** |
| 14 | `新建支持库与模块指南.md:60-65` | `生成模块模板(模块名=…, 依赖清单=[…])` | 形参名为 **`依赖能力清单`**，实参名 `依赖清单` → 会 `TypeError: unexpected keyword argument '依赖清单'` | ❌ **跑不通** |
| 15 | `网关调用最小示例.md`（全部 curl 示例） | `curl -X POST http://127.0.0.1:40007/网关/调用 -d '{"操作":"能力目录",…}'`；`GET /健康` | 返回 `{"操作":"HTTP边界","成功":false,"错误码":"权限不足","错误说明":"访问凭证缺失或无效"}` | ⚠️ **能执行但语义失效**：示例块未带凭证头，正文「凭证」章节虽说明要 `Authorization: Bearer`，但整篇可复制示例一条都没带，照抄必失败 |
| 16 | `项目说明.md:19` 基线口径 | `git cat-file -t e44b119773a3e732b66bad881e2efa46f4a00bec` | 输出 `commit`，exit 0 | ✅ 跑通 |

### 附带发现（同属口径面，供其他路参考）
- `运维脚本/热接入.py` 首行 `当前能力数: 551` 与源码 `611` 相差 60 → 印证 `未完成事项.md:811`「40007 常驻制品落后于源码」仍未收口；`发布门禁` 的 `HTML契约真实执行矩阵` 同样报 `能力数=551`。
- `开发工具/能力网关/` 目录内同时存在 `启动能力网关.sh` 与 `能力网关.py`，命名与文档不一致是本次口径漂移的直接来源之一。

---

## 四、建议修法（按优先级）

1. **P0 统一数字口径**：以 `项目说明.md` 口径表为唯一刷新点，一次性把 611 / 126 包 / 107+18+1 / 131 包声明 / 125 能力定义 / 131 摘要包 / 553（非适配层）/ 制品 2 份·版本 94 全部落表，并删掉 `:46` 那句已失效的「已同为 554」收口注。`能力目录说明书.md` 走生成器重生成。
2. **P0 一次性清掉 8766/8866/`MCP工具箱` 残留**：`AGENTS.md:5/:8/:16`、`当前流程图.md:112/:134/:356/:369`、`项目说明.md:310/:311`、`多会话并行开发规约.md`（整篇操作面）、`提速方案.md`、`底座升级方案.md:16/:287` 全部改写为「stdio 薄壳 3 工具 + 40007」现状；`多会话并行开发规约.md` 需按 `模块库.开工编排` 等现役能力重写操作面，否则整篇不可执行。
3. **P0 修可执行命令**：`项目说明.md:43` 改 `启动能力网关.sh`；`新建支持库与模块指南.md:81` 改 `测试中心.开发工具.测试_契约版本唯一`；`:64` 改 `依赖能力清单=`；`网关调用最小示例.md` 每个 curl 补 `-H 'Authorization: Bearer <凭证>'`。
4. **P1 修正 40006 定位**：`com.huashi.gateway-40006.plist` 的 `MCP工具箱/能力网关.py` 已不存在（服务退出码 2），要么改指 `开发工具/能力网关/能力网关.py` 并 `bootstrap`，要么在文档里明确「40006 当前未启用」——现在文档写「可用」但端口无监听。
5. **P1 债务清单自清**：`未完成事项.md` 中 B6（:121）、`MCP工具箱` 令牌 A 层（:66）、〇在办区（:8–30）按「修复后立即删除对应条目」的自身规则清掉或标记已失效；`:13`「发布门禁 0 项失败」需与实测红项对齐。
