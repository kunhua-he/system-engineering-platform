# 结构设计债 D-1 ~ D-17 现场核实与处置方案（2026-09-16）

> **本文件只给方案，不含任何代码改动。** 由 D 段核实路产出，供主会话下一轮一次性派发。
> **核实时点**：2026-09-16 23:47–23:58 CST。**命令环境**：`export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH`，解释器 `/opt/homebrew/bin/python3.14`（3.14.4），全仓搜索用 `rg`。
> **⚠️ 并发事实（先读）**：核实期间**另有会话正在改本仓工作树**——`git status --porcelain` 实测 **93 项**（88 M / 5 ??）；`HEAD` 在核实过程中由 `89de94a7` 变为 **`385e5dc2`**（`git reflog` 实测）。故每条都记了「实测时点」；凡与债务清单原文数字不一致的，**不一定是清单写错，也可能是并发会话刚改掉**，派发前须按本文命令复跑。

## 一、总览

| 结论 | 条数 | 编号 |
|---|---|---|
| **真重复/真缺陷（本轮可落地）** | 5 | D-3、D-12、D-14、D-15、D-16 |
| **真重复（须单独立项）** | 4 | D-1、D-2、D-4、D-13 |
| **合法门面分层（误判为重复）→ 不动** | 3 | D-5、D-6、D-11 |
| **数字已漂移 / 文档失真（须改写原文）** | 5 | D-2、D-8、D-9、D-10、D-14 |
| **已过期或已修（可销项）** | 1 | D-7 |
| **设计缺陷（跨轮修）** | 1 | D-17 |

**最关键的判断纪律**：D-6 的 `授权.py` / `统一入口.py` 与同名包目录、D-11 的两层 `句柄体系.py`、D-5 的两个 `能力目录` —— **名字相同但都不是重复实现**。前者是「按文件路径接续导出」的合法门面层，后者是不同能力域（公开能力检索 vs 消费者契约登记）。**若按「名字相同即重复」去删，会直接删掉唯一实现。**

---

## 二、逐条现场核实（四要素）

### D-1 12 对包两套实现两套能力 id；适配层腿 16/17 僵尸

**现场证据**
- 成对判定（按能力定义 `能力名` 段对齐）→ 实测 **12 对**，两腿目录与两套 id 同时在线：`FFmpeg媒体` `Git操作` `LibreOffice转换` `OCR识别` `PDF文本表格` `PDF渲染` `PDF生成` `PDF隔离提供者` `textutil转换` `图像解码` `浏览器自动化` `转写`。
  例：`支持库/适配层/FFmpeg提供者/能力定义.json` → 包id `支持库.适配层.FFmpeg提供者`，能力 `FFmpeg媒体.{检查提供者,探测媒体,提取音频,转码,抽取帧}`（5 条）；`支持库/后端/媒体处理支持库/FFmpeg媒体/能力定义.json` → 能力 `媒体处理支持库.FFmpeg媒体.{…同上…,截取音频}`（6 条，**id 多一层前缀**）。
- 适配层腿引用面（严格边界正则 `(?<![\w\.])ID(?![\w])`，排除「后端腿 id 后缀误命中」，逐包实测 **17 包**）：
  - **真生产消费 1 处**：`支持库/后端/办公文档支持库/文档生成/实现/签名校验.py:17` `校验PDF能力id = "PDF隔离提供者.校验PDF"`（另见 `:6,72`、`文档生成/包声明.json:10,102`、`依赖契约/依赖契约.json`）。
  - **仅测试消费 4 处**：`测试中心/支持库/{测试_Git提供者,测试_psycopg提供者,测试_reportlab提供者}.py`、`测试中心/开发工具/{测试_反向破坏门禁28,测试_说明书生成器}.py`。
  - **零引用 12 处**：FFmpeg、LibreOffice、MCP协议、MLXWhisper、PyMuPDF、Tesseract、TreeSitter、YAML、pdfplumber、textutil、密码签名、浏览器自动化（密码签名在 `运行核心/统一网关/网关核心.py:385,445`、`本地网关.py:148,208` 的命中**全是注释** `# 密码签名.对称解密`，非调用）。
- 与原文差异：原文「16 个外部引用 0 处」→ 现场为「**零引用 12 / 仅测试 4 / 真生产 1**」。原文若用普通词边界，会把后端腿 id 的后缀误算成命中。

**定性**：**真重复**（哲学 1.2「不保留旧腿」、1.3「结果唯一即收口」）。

**处置**：**单独立项**（H-1 已裁「本轮不动代码」）。**迁移立项书见第五节**。

---

### D-2 逐字节相同 `.py` 7 组

**现场证据**（`find … -exec shasum -a 256` 后按哈希分组）
- **逐字节相同的业务代码 6 组**（空 `__init__.py` 组已剔除）：① `文档转换支持库/PDF文本表格/实现/PDF文本表格.py` ＝ `适配层/pdfplumber提供者/实现/PDF文本表格.py`；② `…/PDF隔离提供者/实现/子进程解析.py` ＝ `适配层/PDF隔离提供者/…`；③ `…/PDF生成/实现/渲染表格.py` ＝ `适配层/reportlab提供者/…`；④ `图像处理支持库/图像解码/实现/子进程解析.py` ＝ `适配层/Pillow提供者/…`；⑤ `OCR识别支持库/OCR识别/实现/临时文件.py` ＝ `适配层/Tesseract提供者/…`；⑥ `…/PDF渲染/实现/子进程解析.py` ＝ `适配层/PyMuPDF提供者/…`。
- **第 7 组已不存在**：`shasum -a 256 支持库/适配层/FFmpeg提供者/实现/进程管理.py 支持库/后端/媒体处理支持库/FFmpeg媒体/实现/进程管理.py` → `bb5d8ac7…` vs `49f3d64b…`（**不相同**）；`find . -newermt '-15 minutes'` 显示前者刚被并发会话改过。
- 与原文一致部分：OCR「受管进程」、textutil「文本转换」两组确已不相同（`bddccc29…`/`ec7947ae…`、`b8ed309b…`/`b547bd34…`）。

**定性**：**真重复**（哲学 1.3「副本、克隆一律不许留」）。但这 6 组**全部落在 D-1 的 12 对之内**，是 D-1 的从属症状，非独立债。

**处置**：**并入 D-1 立项**，作为其**批 4**（文件级去重收尾）；本轮不动代码。不建议拆出单独做——它依赖批 1–3 的 id 收口。

---

### D-3 后端腿直连第三方：剩 `文档转换支持库/PDF生成` 主进程 import reportlab

**现场证据**
- `支持库/后端/文档转换支持库/PDF生成/实现/生成PDF.py:1` 自述「（主进程直接 import reportlab）」；`:57-59` 「reportlab 是否可导入（纯 Python，主进程直接加载）」→ `importlib.import_module("reportlab")`；`:15` `import importlib`。
- `…/PDF生成/实现/渲染表格.py:12-14` 函数体内 `from reportlab.lib.colors import HexColor` / `from reportlab.lib.units import cm` / `from reportlab.platypus import Spacer, Table, TableStyle`。
- `…/PDF生成/__init__.py:1` 自述「（主进程直接加载 reportlab）」。对照：其余 3/4 已收敛（`子进程解析.py` 已把 PIL/fitz/mlx_whisper 放进子进程内）。

**定性**：**历史遗留**（哲学 2.1/2.3：第三方翻译层应把外部形态吃在边界、按受管执行单元隔离）。**不是越层**（本包自述的主进程加载，程序自洽），是**翻译层隔离形态不一致**。

**处置**：**本轮可落地**（见 R4）。

---

### D-4 能力检索 4 条路径未收口

**现场证据**（4 条全活）：`开发工具/开发入口.py:29` `from 运行核心.加载器.包发现.发现器 import 扫描目录` → `:30` 自建 `声明表 = 扫描目录(…)`；`开发工具/统一能力入口/Agent查询/查询入口.py:49` `from 开发工具.能力搜索.能力搜索器 import 搜索带原因`；`开发工具/能力搜索/能力搜索器.py` 仍在（13968 字节，调用方实测 `README.md:60`、`AGENTS.md:147`、`测试中心/开发工具/测试_契约编译搜索.py:23`、`查询入口.py:49`）；`模块库/能力目录/说明/设计说明.md:14` 仍自述「真实检索由**三份并行实现各自完成**」（表中 `MCP工具箱/公开能力.py` 已删，属陈旧）。第四条：`模块库/能力目录` 有 `能力目录.搜索能力`/`能力目录.读取能力`。

**定性**：**真重复**（哲学 1.2/1.3、第 8 条能力唯一性）。

**处置**：**单独立项**（须先裁决「唯一检索」是 `模块库.能力目录.*` 还是 `开发工具/能力搜索/能力搜索器.py`，再改 4 处调用方；牵动 `测试_契约编译搜索.py:151,211,221` 的 14 字段断言）。本轮可先只纠 `模块库/能力目录/说明/设计说明.md:14` 的失真表述（去掉已删的 `MCP工具箱/公开能力.py` 行）。

---

### D-5 `模块库/能力目录` ↔ `平台控制面/能力目录` 同名跨层 —— **非重复**

**现场证据**：`模块库/能力目录/能力定义.json` → 包id `模块库.能力目录`，能力 `['能力目录.搜索能力','能力目录.读取能力']`（**公开能力检索**）；`平台控制面/能力目录/能力定义.json` → 包id `平台控制面.能力目录`，能力 `['平台控制面.能力目录.{登记消费者契约,查询消费者契约,删除消费者契约,判定契约漂移,判定契约门禁}']`（**消费者契约登记与契约门禁**）。全仓聚合后**重复能力 id = 0**（脚本实测，覆盖 支持库+模块库+平台控制面）→ **无 id 碰撞**。

**定性**：**同名不同义，不是真重复**。两包职责/id/数据全不同；属**命名歧义**（违反第 6 条「一眼能懂」精神）。

**处置**：本轮只加**消歧说明**（两侧设计说明各一行）；改名会改包 id/能力 id，牵动包声明/定义/契约/搜索数据/说明书/索引，**单独立项**。验证：`rg -n "平台控制面.能力目录|模块库.能力目录" --glob '!工程缓存/**' . | wc -l`（改名前后应一致）。

---

### D-6 `平台控制面` 同名文件+同名包并存 + 3 个服务平铺 —— **误判，非重复**

**现场证据**
- `ls 平台控制面/`：顶层文件 `授权.py`(189 行) `统一入口.py`(513) `核心快照.py`(112) `策略中心.py`(95) `资源监督.py`(171)；同名目录 `授权/`、`统一入口/` 均在。
- `平台控制面/授权/__init__.py:22-30` 原文：「与既有 `平台控制面/授权.py` 的关系（**不复制、不双写**）：`授权.py` 是授权语义的**唯一实现**…本正式包目录与它同名，按 Python 导入规则包目录优先——若本入口不显式接续，`from 平台控制面.授权 import 授权服务` 会全部失联。因此本入口按**文件路径**显式载入该唯一实现并原样再导出（**同一模块对象，不产生第二份实现**）」，实现见 `:33-35` `importlib.util` + `_唯一实现文件 = …parent.parent / "授权.py"`。
- 三个新包同构：`策略闸门/__init__.py:3`「判定语义**唯一实现在** `平台控制面/策略中心.py`，本包只做边界校验与结果归一，**不复制判定逻辑**」；`资源预算监督/__init__.py:3`（唯一实现 `资源监督.py`）；`快照校验/__init__.py:3`（唯一实现 `核心快照.py`）。
- 能力面 9/2/6/3/4 条，与平铺文件**无一条 id 重复**。
- 现状更新：平铺 `策略中心.py`/`核心快照.py`/`资源监督.py` **仍平铺**（与原文一致），但**各自已有能力面门面包**（原文未提，属进展）。

**定性**：**合法门面层与提供者层分层**。`*.py` = 语义唯一实现（提供者），同名包目录 = 能力面门面（接入层）。唯一真代价是**同名目录会遮蔽同名模块**，门面须永久维护 `importlib` 按文件路径接续 —— 作者已明文记录。

**处置**：**不动**。理由：① 无 id 重复；② 门面显式声明不复制；③ 改法只有「平铺文件搬进 `实现/`」这种大手术，牵动 `统一入口.py` 等 5 个文件 import 与门禁白名单，收益低于风险；④ 哲学 6.1「阈值不设硬门禁」。仅建议补一条注释级说明，防下一个会话误删。验证：`rg -n "同一模块对象|不复制、不双写|不复制判定逻辑" 平台控制面/*/__init__.py`（期望 5 命中）。

---

### D-7 失效文档路径 + 网关端口双口径 —— **已过期，可销项**

**现场证据**：`开发工具/能力网关/能力网关.py:151` `add_argument("--端口", type=int, default=40006, help="网关端口（默认 40006，与 启动能力网关.sh 一致；8866 为已删除的旧 MCP工具箱体系）")` → **默认已是 40006**（原文「仍 8866」**已过期**）；`启动能力网关.sh:12` `PORT="${1:-40006}"` 一致。`rg -n "8866" 开发文档/项目说明.md README.md` → **仅 1 命中**且是正确表述（`项目说明.md:335` 标注「原 `MCP工具箱/能力网关.py`（8866）…已于 2026-09-15 删除」）；原文指的 `:330/:332/:526/:529` 与 `:43` 矛盾**已消除**（现为 `:42/:43/:335/:538`）。`find 测试中心 -maxdepth 1 -name 'MCP*'` **空**；`git log --diff-filter=D --name-only -- '测试中心/MCP工具箱'` 显示由 **`385e5dc2`（本核实时点内的新提交）** 删除 `测试_能力网关.py`、`测试_能力搜索覆盖率.py`。`rg -n "测试中心/MCP工具箱" 公共契约/HTTP路由契约.json` → **0 命中**。

**定性**：**已过期/已修**（端口口径已统一；失效路径已修；`测试中心/MCP工具箱` 已被并发会话删除）。

**处置**：**销项**。因改仓是并发会话做的，**销项前必须复跑**（见 R7），确认后再按 `未完成事项.md` 自述规则删除 D-7 条目。

---

### D-8 适配层根 19 项非包内容混放

**现场证据**（**用 Python 复算，不用 bash `[ -f ]`**：bash 在 UTF-8 下把 `$d包声明` 当变量名解析，会把全部目录误判为「无包声明」——本路已踩到并纠正）
- `支持库/适配层` 下**有 `包声明.json` 17 个目录**（即 D-1 的 17 提供者）；**无 `包声明.json` 12 个目录**：`配置安全` `配置读取` `本地进程适配器` `提供者配置` `提供者注册表` `HTTP服务适配器` `配置契约` `密钥提供者` `模型服务` `数据库适配器` `动态库适配器` `MySQL提供者`。
- **根散装 `.py` 7 个**：`系统探针.py` `适配契约.py` `模型服务.py` `脱敏模式.py` `__init__.py` `密码适配.py` `模型HTTP提供者.py`。**12 + 7 = 19 项 —— 与债务原文完全一致。**
- `模型服务/` 仍为 **0 文件空目录**，与 `模型服务.py` **同名并存**（`ls -la` 仅 `.`/`..`）。12 个无包目录中 `密钥提供者`/`提供者配置`/`MySQL提供者` 各仅 1 文件，`模型服务` 0 文件。
- **与 H-2 的关系（须更正任务书口径）**：H-2 原文只裁「顶层目录瘦身（`客户端/`、`运维脚本/` 各 1 文件）」＝ **D-10**，**射程不含 D-8**；现场 19 项确实仍在。

**定性**：**历史遗留**（哲学 2.3 只规定「两段」，这批是**段内未包化**的散装件；不是不该存在，而是形态不统一）。

**处置**：**本轮可落地＝只删空壳 + 只补登记说明**（见 R6）；**包化另立项**。

---

### D-9 技能库/运维脚本未登记；`支持库/核心/` 空壳仍被文档当现场

**现场证据**：`grep -c "技能库" README.md` → **0**（`grep -ni "技能" README.md` 亦 0），而 `find 技能库 -name '包声明.json'` → `技能库/后端/技能库/包声明.json`（**实存 1 个正式包**，含 `验证夹具/技能工作区/{示范技能,越权技能,残缺技能}`）→ 原文「README 提 `技能库` 0 次」**仍成立**。`grep -c "运维脚本" README.md` → **1**，但仅在 `README.md:7` 的 H-2 自裁说明里，**不在 `:25-42` 目录职责表内**（该表 18 行无 `技能库/` 行、无 `运维脚本/` 行）；`项目说明.md` 中 `运维脚本` 0 命中。`支持库/核心/` 仍空壳：`ls -la` 仅 `__init__.py`（**40 字节**）+ `__pycache__`，`du -sh` = 8.0K。`rg -n "支持库/核心" 开发文档/方案/底座升级方案.md` → **6 命中**（`:370 :390 :413 :416 :493 :562`）；真实位置是 `支持库/后端/数据操作支持库/类型转换`（`find 支持库 -type d -name "类型转换"`）。

**定性**：**文档失真 + 死空壳（历史遗留）**。`技能库` 是**已建成却未进 README 目录表**；`支持库/核心/` 是**已死的空壳**（哲学 1.4「空转即杀」）。

**处置**：**本轮可落地**（见 R5，三项都只动文档/空目录，零代码风险）。

---

### D-10 单文件/小文件顶层目录未瘦身

**现场证据**：`ls -d */` → **17 个**顶层目录（`公共契约 前端核心 后端核心 启动监督器 客户端 工程缓存 平台控制面 开发工具 开发文档 技能库 支持库 模块库 测试中心 示例项目 运维脚本 运行核心 项目适配层`）。顶层文件数：`客户端/`1 `运维脚本/`1 `后端核心/`1 `支持库/`1 `模块库/`1 `测试中心/`1 `项目适配层/`1 `启动监督器/`2 `前端核心/`3 `公共契约/`4 `平台控制面/`6 `运行核心/`6 `开发工具/`9。H-2 已落 README：`README.md:5-8`「## 目录约定的两点说明（2026-09-16 自裁…）」+「**单文件顶层目录合法**…不为「目录里文件太少」做 import 前缀大迁移」。**不一致处**：H-2 要求「写进 README 说明各层职责」，但目录表 `:25-42` **缺 `技能库/`、`运维脚本/` 两行**。

**定性**：**已裁决 + 已落盘，但与裁决文字有缺口**（judgment 落了，「职责说明」没落）。

**处置**：**不动代码**（与 H-2 一致，本路确认一致）；**本轮可落地＝补 README 两行**（与 D-9 同一处，合并做）。验证：`ls -d */ | wc -l`（仍 17）；`sed -n '25,42p' README.md | grep -c "^|"`（18 → 20）。

---

### D-11 句柄体系两层同名（实现位置与层定位相反）—— **非重复，不动**

**现场证据**：`wc -l 公共契约/句柄体系.py` → **377**（唯一实现）；`运行核心/句柄体系.py` → **2 行**：`"""运行核心句柄体系包级入口；唯一实现位于公共契约。"""` + `from 公共契约.句柄体系 import *`。

**定性**：**合法门面层**（运行核心侧只是转发；无第二份实现、无 id 重复）。**与 H-3 自裁「不搬」一致。**

**处置**：**不动**（H-3 理由：句柄是**契约**不是支持库能力实现；搬走会让契约层失去唯一性 —— 哲学第 5 条、第 2 条）。可选补强：那 2 行门面加一行「为何留在这里」注释，防误删。验证：`wc -l 公共契约/句柄体系.py 运行核心/句柄体系.py`（377 / 2）。

---

### D-12 模块能力 id 前缀顶着支持库名 —— **真缺陷，本轮可落地**

**现场证据**
- `模块库/文档生成/__init__.py:20` `能力id="办公文档支持库.文档生成.生成文档"`，而同一次 `注册表.注册(…)` 的 `:21` 是 `包id="模块库.文档生成"`；`能力定义.json` → `包id=模块库.文档生成`、能力 `['办公文档支持库.文档生成.生成文档']`（**1/1 不符**）。
- **一致性对照（18 个 模块库 包全量脚本实测）**：**只有 `模块库.文档生成` 不符**，其余 17 包（`OCR`/`图像处理`/`媒体处理`/`媒体转写`/`开工编排`/`文件管理`/`文档解析`/`文档读取`/`测试资源`/`直播逐字稿`/`知识检索`/`简单窗口`/`组件生成`/`网页分析`/`网页浏览`/`能力目录`/`自修复工具`）**全部 0 不符**。全仓能力 id 重复数 **0** → 不是覆盖支持库能力，而是**用别的库的名字命名自己**。
- **审计工具已被改造成容忍它**：`开发工具/复用审计/能力调用图审计.py:147-158` `_导出名()` docstring 原文「兼容两种命名形态：`文档生成.生成文档`（模块库自命名）与 `办公文档支持库.文档生成.生成文档`（**带支持库包名前缀**）。原先只用 `removeprefix(...)`，第二种形态剥不掉前缀 → `__all__` 恒不等于注册集，会让 `模块库/文档生成` 这类包永久判漂移。」
- **同批必须改的引用点（实测 13 处，全在包内 + 1 处测试断言）**：`包声明.json:28`、`能力定义.json:7`、`能力契约/参数契约.json:5,44`、`能力数据/能力搜索数据.json:3`、`能力数据/Agent查询数据.json:3`、`完整性摘要.json:77`、`权限契约/权限契约.json:2`、`验证场景/正向HTTP场景.json:10`、`说明/设计说明.md:14,16,40`、`说明/使用说明.md:17,26`；测试断言 `测试中心/模块库/测试_文档生成模块.py:182`。另 `支持库/后端/文档转换支持库/{python_docx提供者,PDF生成,openpyxl提供者}/复用决策.json` 各 1 处把它列入复用关系（**须判定指向的是模块能力还是支持库能力，不许盲改**）。

**定性**：**真缺陷（历史遗留 + 兼容壳掩盖）**（哲学 1.2「不保留旧名别名」、第 6 条、第 8 条）。

**处置**：**本轮可落地**（见 R3）。分两步：先改名 → 再单独立项删 `_导出名()` 的兼容分支（删分支牵动 `测试_契约编译搜索.py` 等审计测试，不混做）。

---

### D-13 两个同名 `说明书生成器.py` 并存

**现场证据**
- `支持库/后端/组件规范支持库/实现/说明书生成器.py`（**147 行**）：`读取数据源` `收集能力` `生成调用示例` `生成验证状态` `生成说明书文本` `校验输出目标` `生成说明书(包目录, 输出根目录) -> Path`；`:146` `目标路径.write_text(生成说明书文本(包目录),…)`；输出 `<输出根目录>/说明/使用说明.md`；自述「从 能力定义+能力契约+能力搜索数据+验证场景引用 生成 Agent 可读使用说明书」。
- `开发工具/说明书生成/说明书生成器.py`（**154 行**）：`_包契约表` `_参数列表` `_调用示例文本` `生成单包说明书` `生成目录说明书` `写出说明书`；自述「从**包声明+唯一聚合契约**生成机器可读说明书」，`_包契约表` 复用 `开发工具/契约编译/聚合契约解析.解析聚合契约`。
- **两者活口不同**：前者被 `支持库/后端/组件规范支持库/__init__.py:50-53,67` 导出、被 `测试中心/开发工具/测试_说明书生成器.py:18` 调用；后者被 `开发工具/说明书生成/运行生成.py:17` + `测试中心/开发工具/测试_契约编译搜索.py:24` 调用。
- **第三处同义入口**：`开发工具/开发入口.py:192-200` 自己也有 `生成说明书(组件目录)`，转调 `开发工具/契约编译/契约编译器.生成说明书`（`:149`，`:264` 产出 `<能力id>_说明书.md`）。
- `组件规范支持库/能力定义.json` 只有 3 条能力（`生成完整性摘要`/`校验完整性摘要`/`生成模块模板`）→ **`生成说明书` 无能力面**，只能 import。

**定性**：**真重复**（三处同名同义入口，输出目标不同但都叫「说明书」）。

**处置**：**单独立项**（须先定唯一口径：「每包 `说明/使用说明.md`」还是「目录级聚合 Markdown」；并裁决 `生成说明书` 是否切能力面再动）。**不建议本轮动**：无能力面，改名/合并会同时碰 `组件规范支持库/__init__.py` 导出面、`测试_说明书生成器.py`、`运行生成.py`、`契约编译器.py` 四处。本轮只加消歧说明（两侧 docstring 各指向对方）。验证：`find . -name '说明书生成器.py' -not -path './工程缓存/*' | wc -l`（期望 2）。

---

### D-14 `开发工具/` 根平铺无二级分层

**现场证据**：`ls -1 开发工具/*.py` → **9 个**：`全量重算摘要.py` `公开调用完整性门禁.py` `工程缓存回收.py` `开发入口.py` `更新完整性摘要.py` `测试伪装门禁.py` `测试体系门禁.py` `环境自检.py` `重建依赖锁.py`。**门禁类实际 3 个**（原文说 4 个 → **数字漂移**）：`公开调用完整性门禁.py` `测试伪装门禁.py` `测试体系门禁.py`。**两套同名配对仍在**：`测试伪装门禁.py` + `测试伪装门禁实现/`（8 文件）、`测试体系门禁.py` + `测试体系门禁实现/`（8 文件）。二级目录实测 18 个：`HTML验证 依赖生命周期审计 发布门禁 复用审计 契约编译 快速编译 测试伪装门禁实现 测试体系门禁实现 直播逐字稿工具 组件合规 统一能力入口 统一验证 能力搜索 能力网关 薄壳 说明书生成 轻代码前端编辑器 项目编译`。

**定性**：**历史遗留 + 命名歧义**。`门禁.py` + `门禁实现/` 配对**不是重复**（一个是 CLI 入口、一个是实现目录），但**同名并列在根上不可推导**。根平铺 9 个文件本身**不是债**（哲学 6.1「阈值不设硬门禁」）。

**处置**：**本轮可落地（可选）**：把 3 个门禁类与两个 `实现/` 收敛进 `开发工具/门禁/`；其余 6 个根文件不动（见 R9）。**风险**：`公开调用完整性门禁.py` 被按模块路径调用（`python3.14 -m 开发工具.公开调用完整性门禁`），搬目录即改模块路径，牵动 `AGENTS.md`/`README.md`/`多会话并行开发规约.md` 的命令示例 → **搬前必须全仓 grep**。嫌险可降级为「只加 README 说明、保留现状」，即 D-14 转「不动并写明理由」。

---

### D-15 `开发工具/更新完整性摘要.py` 零引用死入口 —— **本轮可落地（删）**

**现场证据**：存在（2148 字节）。**代码引用 0 处**：`rg -l "更新完整性摘要" --glob '!工程缓存/**' --glob '!开发文档/**' .` → **0 命中**。**文档引用 2 处（照做会误导）**：`开发文档/方案/待重启事项_技能库目录归位.md:31` 与 `开发文档/决策记录/0014_转码升级硬编与音频直通.md:73` 均写 `python3.14 开发工具/更新完整性摘要.py`。**功能已被取代且更弱**（`cat` 实读）：只扫 `目标目录 = ["支持库","模块库","技能库"]`（`:9`），且只**改已有条目的 sha256**（`:31-38` 在既有 `文件清单` 内循环），**从不重算清单结构**（不增不删条目）→ 正是原文说的「只改 hash 不重算清单结构」。对照 `开发工具/全量重算摘要.py:1-17` 自述「冷启动入口：扫遍全部正式根，凡「磁盘 ≠ 重算」一律重算」，`正式根表` 含 **10 个根**（是小脚本覆盖面的 3.3 倍）。

**定性**：**真死代码 + 弱实现**（哲学 1.4「空转即杀」、1.6「临时脚本不留库」）。

**处置**：**本轮可落地**（见 R1）。

---

### D-16 两个同名 `运行审计.py` —— **本轮可落地（改名消歧）**

**现场证据**：`开发工具/复用审计/运行审计.py`（**33 行**，`cat` 实读）docstring「运行审计：模块库 能力调用图与原子旁路审计 命令行入口」，`from 开发工具.复用审计.能力调用图审计 import 审计模块库`，`--目录` 默认 `系统根/模块库`；`开发工具/依赖生命周期审计/运行审计.py`（**70 行**）docstring「依赖与生命周期审计命令行入口（发布门禁候选，只读）」，`from 开发工具.依赖生命周期审计.审计核心 import 审计全部`，`--系统根`，审计各提供者的依赖锁/包声明/完整性摘要/健康探针/停止释放证据。**两者是不同审计、不同 CLI、不同参数，不是重复实现。** **无外部调用方**：`rg -n "运行审计" --glob '!工程缓存/**' --glob '!开发文档/**' .` → 仅 4 命中，**全是两个文件自己 docstring 里的用法行**。

**定性**：**命名歧义**（非重复）。名字太泛（「运行审计」指什么不可推导），违反第 6 条。

**处置**：**本轮可落地（纯改名，零引用方）**（见 R2）。
**R2 已落地（2026-09-17）**：两文件 `git mv` 为 `运行能力调用图审计.py` / `运行依赖生命周期审计.py`，docstring 用法行同改；全仓 3 处命令文本同步改（`开发文档/方案/提速方案.md:126`、`开发文档/交付记录/20260915_底座收口夜班/夜班交付说明.md:38`、本文件「每批共同门禁」行）；实测 `find 开发工具 -name '运行审计.py' | wc -l` = **0**，两个新入口真跑退出码均 **0**（复用审计 53 文件 / 0 违规；依赖生命周期审计 17 提供者全通过、跳过 2）。

---

### D-17 陈旧激活指针一旦产生就没有出口 —— **设计缺陷；①已复现 ②仍活**

**现场证据**（本时点逐项复算，**故障已换位**）
- `平台控制面/包仓库/平台客户端制品.py:455` `def 检查激活指针(self) -> str | None:`；`:466` `指向目录 = self.客户端制品目录 / 制品名 if 制品名 else None` → **校验的是制品仓库里的源目录，不是已安装副本**（与原文一致）。`:500-507`（`校验稳定路径` 内）先校验 `self.环境目录 / "平台客户端"`（已安装副本），再校验 `指向目录`。
- **逐目录实算（`python3.14` 调 `计算目录摘要16`，定义在 `平台客户端制品.py:36`）**：
  | 目录 | 实算摘要16 | 指针要求 | 结果 |
  |---|---|---|---|
  | 源制品 `工程缓存/制品仓库/平台客户端制品/平台客户端-7ac9c97e93ddebc4` | `7ac9c97e93ddebc4` | `7ac9c97e93ddebc4` | **✅ 现相符**（登记时是 `7b6a147661a3ae79`＝损坏） |
  | **已安装副本** `工程缓存/制品仓库/平台客户端环境/平台客户端` | `43949ce2e44a36f8` | `7ac9c97e93ddebc4` | **❌ 不符** |
  - `cat 工程缓存/制品仓库/平台客户端环境/当前.json` → `{"摘要sha256":"7ac9c97e93ddebc4","制品目录":"平台客户端-7ac9c97e93ddebc4","制品摘要":"40cb4560ee5ab088fe885ca5b6407a3c","版本":105,"栅栏令牌":105}`；同目录有 `当前.json.bak_陈旧指针_20260916`（登记时的人工兜底痕迹）。
- **根因① 已复现**：`find 工程缓存/制品仓库/平台客户端环境/平台客户端 -maxdepth 1` 实测列出 **`工程缓存/` `前端/` `运行入口/` `平台客户端/` `编译清单.json` `制品来源.json` `制品摘要.json` `制品完整性摘要.json`** —— 运行时数据（`工程缓存/`）被写回了**已安装制品目录内部**，这正是副本摘要从 `7ac9c97e93ddebc4` 漂到 `43949ce2e44a36f8` 的直接原因。
- **根因② 仍活**：`客户端/构建平台客户端.py:11` 用法仅 `[--安装]`；`:641` `安装 = "--安装" in sys.argv` —— **无任何 `--重置激活指针` / `--诊断` 出口**；失败路径 `:623` `raise RuntimeError(f"安装失败: {安装消息 or '未返回安装目标'}")`。

**定性**：**设计缺陷**（哲学 1.1「按唯一链路修到底」+ 第 3 条 3.2 的延伸：**拒绝要可诊断、可修复**；当前是「永久拒绝 + 无出口」）。与登记时的差别：症状从「源制品损坏」换位到「已安装副本被运行时写脏」→ 说明**根因①是主要且持续的驱动**，源制品那次损坏只是同因的一个副本。

**处置**：**本轮可落地**（见 R8），但**须先只读定位制品内写点**；涉及制品安装链，**禁止盲改**。

---

## 三、汇总表（D-1 ~ D-17）

| 编号 | 现场证据（摘要） | 定性 | 处置 | 工作量级 |
|---|---|---|---|---|
| **D-1** | 12 对两腿+两套 id 全在；17 提供者中真生产消费 1（`PDF隔离提供者.校验PDF`←`文档生成/实现/签名校验.py:17`）、仅测试 4、零引用 12 | 真重复 | **单独立项**（立项书·第五节） | **XL**（9 模块 / 4 批） |
| **D-2** | 逐字节相同 **6 组**（第 7 组 `进程管理` 已被并发会话拆开：`bb5d8ac7…`≠`49f3d64b…`） | 真重复（D-1 从属） | **并入 D-1**（其批 4） | L |
| **D-3** | `PDF生成/实现/生成PDF.py:1,57-59` + `渲染表格.py:12-14` 主进程 reportlab | 历史遗留 | **本轮可落地** | S（1 包 2 文件） |
| **D-4** | 4 条路径全活：`开发入口.py:29`／`查询入口.py:49`／`能力搜索器.py` 在／`模块库/能力目录/说明/设计说明.md:14` | 真重复 | **单独立项**（先纠设计说明失真） | M |
| **D-5** | 两包同名、包id/能力id 完全不同；全仓重复能力 id **0** | **同名不同义（非重复）** | 只加消歧说明；改名**另立项** | S／M |
| **D-6** | `授权/__init__.py:22-30`「不复制、不双写…同一模块对象」；`策略闸门/资源预算监督/快照校验/__init__.py:3` 同义 | **合法门面层（非重复）** | **不动**（补注释说明） | S |
| **D-7** | `能力网关.py:151` 默认已 **40006**；`8866` 全文档仅 1 处且标「已删」；`测试中心/MCP工具箱/` 已由 `385e5dc2` 删除；`HTTP路由契约.json` 0 命中 | **已过期/已修** | **销项**（须复验后删条目） | XS |
| **D-8** | **12 无包目录 + 7 散装 .py = 19 项**（与原文一致）；`模型服务/` 0 文件且与 `模型服务.py` 同名并存；**不在 H-2 射程** | 历史遗留 | **本轮可落地**（删空壳+补登记）；包化另立项 | S／L |
| **D-9** | README 提 `技能库` **0** 次但 `技能库/后端/技能库/包声明.json` 实存；`运维脚本` 仅 README:7、目录表无行；`支持库/核心/` 仅 40 字节；`底座升级方案.md` **6 处**已死路径 | 文档失真 + 死空壳 | **本轮可落地** | S |
| **D-10** | 顶层目录 **17**；README:5-8 已落 H-2「单文件合法」；但目录表 18 行缺 `技能库/`、`运维脚本/` | 已裁决 + 落盘有缺口 | **不动代码**；补 README 两行 | XS |
| **D-11** | `公共契约/句柄体系.py` **377** 行唯一实现 + `运行核心/句柄体系.py` **2 行** `import *` | **合法门面层（非重复）** | **不动**（与 H-3 一致） | XS |
| **D-12** | `__init__.py:20` 能力id `办公文档支持库.…` vs `:21` 包id `模块库.文档生成`；18 包中**唯一**不符；`能力调用图审计.py:147-158` 已兼容它 | **真缺陷（兼容壳掩盖）** | **本轮可落地** | S（14 落点） |
| **D-13** | 两个 `说明书生成器.py`（147/154 行）函数集/调用方全不同；另有 `开发入口.py:192` 第三处；`生成说明书` **无能力面** | 真重复（三处） | **单独立项**；本轮只加消歧说明 | M |
| **D-14** | `开发工具/*.py` **9 个**；门禁类 **3 个**（原文 4，**漂移**）；两套 `.py`+`实现/` 同名并列仍在 | 历史遗留 + 命名歧义 | **本轮可落地**（收敛 `开发工具/门禁/`）；嫌险则转「不动+写理由」 | M |
| **D-15** | 代码引用 **0**；文档引用 2 处；`cat` 实读只扫 3 根且只改 hash | **真死代码 + 弱实现** | **本轮可落地（删）** | XS |
| **D-16** | 两份 33/70 行、不同 CLI 不同目标；`rg` 实测**零外部调用方** | 命名歧义（非重复） | **本轮可落地（改名）** | XS |
| **D-17** | 源制品现相符；**副本**实算 `43949ce2e44a36f8` ≠ 指针 `7ac9c97e93ddebc4`；副本内**多出 `工程缓存/`**；`:641` 仅 `--安装`、**无重置出口** | **设计缺陷** | **本轮可落地**（写根外移 + 加重置出口） | M |

---

## 四、「本轮可立即落地」子清单（供主会话下一轮一次性全派）

> 全部为低风险、可独立验收、互不重叠项，按**文件域独占**即可并行。**D-1/D-2/D-4/D-13 不在本轮**（见第五节）。

| # | 条目 | 落点（文件级独占） | 最小改法 | 验收命令 |
|---|---|---|---|---|
| R1 | **D-15** | `开发工具/更新完整性摘要.py`(删)、`开发文档/方案/待重启事项_技能库目录归位.md:31`、`开发文档/决策记录/0014_转码升级硬编与音频直通.md:73` | 删文件；两处命令改 `python3.14 -m 开发工具.全量重算摘要` | `rg -n "更新完整性摘要.py" --glob '!工程缓存/**' .`＝0；`python3.14 -m 开发工具.全量重算摘要 --只报` |
| R2 | **D-16** | `开发工具/复用审计/运行审计.py`、`开发工具/依赖生命周期审计/运行审计.py` | `git mv` 为 `运行能力调用图审计.py` / `运行依赖生命周期审计.py`，同步各自 docstring 用法行 | `find 开发工具 -name '运行审计.py' \| wc -l`＝0；`find 开发工具 -name '运行*审计.py' \| sort`＝2 |
| R3 | **D-12** | `模块库/文档生成/`（13 处字符串）+ `测试中心/模块库/测试_文档生成模块.py:182` | id 改 `模块库.文档生成.生成文档`；3 个 `复用决策.json` 先判定再动；重算摘要 | `rg -n "办公文档支持库.文档生成.生成文档" --glob '!工程缓存/**' .` 仅剩审计 docstring；`python3.14 -m unittest 测试中心.模块库.测试_文档生成模块` |
| R4 | **D-3** | `支持库/后端/文档转换支持库/PDF生成/`（`__init__.py`、`实现/生成PDF.py`、`实现/渲染表格.py`） | 按同仓 `子进程解析.py` 写法把 reportlab 搬进子进程；改自述；**不动能力 id** | `rg -n "from reportlab\|import_module\(\"reportlab\"\)" 支持库/后端/文档转换支持库/PDF生成/` 只在子进程文件命中；`python3.14 -m unittest 测试中心.支持库.测试_reportlab提供者` |
| R5 | **D-9 + D-10** | `README.md:25-42`、`开发文档/方案/底座升级方案.md`(6 处)、`支持库/核心/` | README 补 `技能库/`、`运维脚本/` 两行；6 处 `支持库/核心/类型转换` → `支持库/后端/数据操作支持库/类型转换`；删 `支持库/核心/` 空壳 | `grep -c "技能库" README.md`≥1；`rg -n "支持库/核心" 开发文档/方案/底座升级方案.md`＝0 |
| R6 | **D-8** | `支持库/适配层/模型服务/`(0 文件空目录)、`支持库/适配层` 说明/README | `rmdir 支持库/适配层/模型服务`；12 个无包目录逐项写职责 | `python3.14 -c "from pathlib import Path;print(sum(1 for d in Path('支持库/适配层').iterdir() if d.is_dir() and d.name!='__pycache__' and not (d/'包声明.json').is_file()))"` 由 12 → 11 |
| R7 | **D-7 复验销项** | `开发文档/未完成事项.md`(D 段) | 复跑命令确认后，按该文件自述规则**删除 D-7 条目** | `rg -n "8866" 开发文档/项目说明.md README.md AGENTS.md`；`ls 测试中心/MCP工具箱 2>&1`；`rg -n "MCP工具箱" 公共契约/HTTP路由契约.json 测试中心/` |
| R8 | **D-17** | `客户端/构建平台客户端.py`、制品运行入口（**须先只读定位写点**） | ① 运行时缓存根外移出制品目录；② 加 `--重置激活指针`（复用既有 `当前.json.bak_` 流程） | 副本实算摘要 == `当前.json.摘要sha256`；`ls 工程缓存/制品仓库/平台客户端环境/平台客户端/工程缓存`＝不存在 |
| R9 | **D-14（可选）** | `开发工具/{公开调用完整性门禁.py,测试伪装门禁.py,测试伪装门禁实现/,测试体系门禁.py,测试体系门禁实现/}` | 收敛进 `开发工具/门禁/`；**搬前先统计全仓引用** | `rg -n "公开调用完整性门禁\|测试伪装门禁\|测试体系门禁" --glob '!工程缓存/**' .` |
| R10 | **D-5/D-6/D-11/D-13 消歧说明** | 各包 `说明/设计说明.md` / `__init__.py` docstring | 只加「同名不同物」一行，**不动代码** | `rg -n "同名\|唯一实现\|不复制" 平台控制面/*/__init__.py 模块库/能力目录/说明/设计说明.md` |

**派发纪律（沿用本路协议）**：每条独占文件域；R1/R2 与 R7 无重叠；**R3 与 R5 都要动 `开发文档`，须分给不同路或串行**；**R8 必须先只读定位再改**，禁止直接改安装链。

---

## 五、迁移立项书（D-1 / D-2：适配层为唯一实现）

> 依据：H-1 自裁「适配层为唯一实现；后端成对包降为门面或删除；**本轮不动代码**，迁移按 D-1 单独立项」。**本路现场复核 H-1 结论成立**，但把「8 个模块」更正为 **9 个**。

**5.1 立项目标**：消除 12 对包的两套实现与两套能力 id；`支持库/适配层/*提供者` 成为第三方翻译层唯一实现；`支持库/后端/*` 的成对包**降门面或删除**（二选一，见 5.3）。**不在本轮维修范围**，不得与 R1–R10 混做。

**5.2 受影响调用方（实测 = 9 个模块，不是 8 个）**——对 12 对后端腿能力 id 做严格边界全仓扫描（`模块库/**`）：

| 模块 | 后端腿 → 目标提供者 | 消费点（文件） |
|---|---|---|
| `模块库/OCR` | `OCR识别` → Tesseract | `实现/OCR.py`、`包声明.json`、`复用决策.json`、`依赖契约/依赖契约.json`、`说明/使用说明.md` |
| `模块库/图像处理` | `图像解码` → Pillow | `实现/图像处理.py`、`包声明.json`、`说明/使用说明.md` |
| `模块库/媒体处理` | `FFmpeg媒体` → FFmpeg | `实现/媒体处理.py`、`包声明.json`、`复用决策.json`、`依赖契约/依赖契约.json`、`说明/使用说明.md` |
| `模块库/媒体转写` | `转写` → MLXWhisper | `__init__.py`、`实现/媒体转写.py`、`能力定义.json`、`能力契约/参数契约.json`、`能力数据/能力搜索数据.json`、`包声明.json`、`复用决策.json`、`依赖契约/依赖契约.json`、`说明/{使用说明,设计说明}.md` |
| `模块库/开工编排` | `Git操作` → Git | `实现/开工编排.py`、`包声明.json`、`复用决策.json`、`依赖契约/依赖契约.json`、`说明/{设计说明,待补能力清单}.md` |
| `模块库/文档解析` | `LibreOffice转换` + `PDF文本表格` → LibreOffice/pdfplumber | `实现/文档解析.py`、`包声明.json`、`复用决策.json`、`说明/使用说明.md` |
| `模块库/直播逐字稿` | `FFmpeg媒体` + `转写` | `实现/{直播逐字稿,全自动精校,分片转写,复核调度}.py`、`包声明.json`、`复用决策.json`、`依赖契约/依赖契约.json`、`说明/使用说明.md` |
| `模块库/网页浏览` | `浏览器自动化支持库` → 浏览器自动化 | `实现/网页浏览.py`、`包声明.json`、`复用决策.json`、`说明/使用说明.md` |
| `模块库/自修复工具` | `Git操作` → Git | `实现/自修复工具.py`、`包声明.json`、`复用决策.json`、`说明/使用说明.md` |

**未被任何模块消费的后端腿 4 个**：`textutil转换`、`PDF生成`、`PDF渲染`、`PDF隔离提供者`。注意：`PDF隔离提供者` 反被 `办公文档支持库/文档生成/实现/签名校验.py:17` 直接消费**适配层腿** `PDF隔离提供者.校验PDF` —— 这是全仓唯一已站在适配层腿上的消费点，迁移时应**保持不动**。
**测试侧同批落点**（否则测试红）：`测试中心/支持库/{测试_Git提供者,测试_psycopg提供者,测试_reportlab提供者}.py`、`测试中心/模块库/{测试_OCR,测试_直播逐字稿,测试_逐字稿分片转写,测试_逐字稿复核调度}.py`。

**5.3 两条路线（立项时二选一，工作量差 3~5 倍）**
- **路线 A（推荐·成本低）**：后端成对包**降为门面** —— 保留原能力 id（`媒体处理支持库.FFmpeg媒体.*` 等），实现改为内部转调适配层腿，`__init__.py` 只做转发与再导出。**9 个模块的调用 id 全不用改**；只改 12 个后端包的实现体 + 删 6 组重复 `.py`。
- **路线 B（更干净·成本高）**：删后端成对包，**9 个模块的调用 id 全改**为适配层腿 id，并同步 `包声明.json`/`能力契约`/`能力数据`/`复用决策`/`依赖契约`/`说明×2`/`测试中心` 断言。代价示例：`模块库/直播逐字稿` 要改 4 个实现文件 + 5 个元数据文件；`模块库/媒体转写` 全元数据都要改。
- **两条路线都必须同批做**：`开发工具/复用审计/能力调用图审计.py` 的白名单与文档、`开发工具/发布门禁/**` 中引用这些 id 的项。

**5.4 分 4 批（每批独立可回归）**

| 批次 | 范围 | 每批回归 |
|---|---|---|
| **批 1** | 4 个**零消费**后端腿：`textutil转换`、`PDF生成`、`PDF渲染`、`PDF隔离提供者`（该腿只做「后端成对包降门面」） | `python3.14 -m unittest 测试中心.支持库.测试_reportlab提供者` + 装配冒烟 |
| **批 2** | 单模块消费的 5 对：`OCR识别`、`图像解码`、`浏览器自动化`、`Git操作`（`开工编排`+`自修复工具`）、`转写` | 装配冒烟 + `测试中心.模块库.{测试_OCR,测试_文档生成模块}` + `能力调用图审计` 退出码 |
| **批 3** | 多模块/最重的 3 对：`FFmpeg媒体`（`媒体处理`+`直播逐字稿`）、`LibreOffice转换`+`PDF文本表格`（`文档解析`） | 装配冒烟 + `测试中心.模块库.{测试_直播逐字稿,测试_逐字稿分片转写,测试_逐字稿复核调度}` |
| **批 4** | **文件级去重收尾（D-2）**：清 6 组逐字节重复 `.py`，统一指向适配层唯一实现 | `find … -exec shasum -a 256` 重算：业务代码重复组＝**0**；`python3.14 -m 开发工具.全量重算摘要` 全闭合 |

**每批共同门禁（缺一不可）**：装配冒烟（`README.md:98-124` 脚本，**真断言 + 退出码**：能力数 > 0、`装配跳过包` 为空）→ `python3.14 开发工具/复用审计/运行能力调用图审计.py`（0 违规）→ `python3.14 开发工具/依赖生命周期审计/运行依赖生命周期审计.py`（0 违规）→ 全量摘要闭合。**正式结论仍以 HTML 黑盒 + 发布门禁为准**（本路按约束未跑）。
**禁止项**：迁移期间不得新增任何第三方封装到 `支持库/后端/*`（H-1「先冻结」条款）。

---

## 六、H-1 / H-2 自裁与现场一致性核实

| 裁决 | 原文口径 | 现场核实结果 | 是否一致 |
|---|---|---|---|
| **H-1** | 适配层为唯一实现；本轮不动代码；迁移按 D-1 立项；「要改 **8 个模块**的调用 id」 | 结论成立；但实测受影响模块 **9 个**（见 5.2） | ⚠️ **不一致（8 → 9）**，须改 |
| **H-1** | 「先冻结：新增第三方封装只进适配层」 | 静态命令无法证「冻结」；**现场可证**：`支持库/适配层/` 17 提供者、`支持库/后端/` 成对模块 12 对，未再增加 | 一致（间接） |
| **H-2** | 承认单文件顶层目录合法，**写进 README 说明各层职责**，不做 import 前缀大迁移 | `README.md:5-8` 已落「单文件顶层目录合法」**judgment**；但 `:25-42` **目录职责表缺 `技能库/`、`运维脚本/` 两行** → 只落一半 | ⚠️ **部分不一致** → R5 处置 |
| **H-2 射程** | 任务书把 **D-8** 也归入 H-2 自裁 | **不成立**：H-2 原文只针对「顶层目录瘦身（`客户端/`、`运维脚本/` 各 1 文件）」＝ **D-10**；**D-8（适配层根 19 项混放）不在其射程**，现场 19 项也确实仍在 | ⚠️ **任务书口径需更正** |
| **H-3** | 句柄体系留在契约层不搬 | `公共契约/句柄体系.py` 377 行唯一实现 + `运行核心/句柄体系.py` 2 行转发 | 一致 |

---

## 七、剩余风险与未竟事项

1. **并发改仓是本文件最大保真度威胁**：核实期间 `git status` 93 项、`HEAD` 从 `89de94a7` → `385e5dc2`（`测试中心/MCP工具箱/` 由该提交删除、`FFmpeg提供者/实现/进程管理.py` 被改写导致 D-2 由 7 组变 6 组）。**派发前各路必须复跑本文验收命令**；凡写「已修/已过期」的（D-7）一律按「需复验后销项」处理。
2. **D-17 两个根因都指向制品安装链**，本路只做只读定位（`检查激活指针:455/466`、`校验稳定路径:500-507`、`构建平台客户端.py:641`），**未展开制品内 `平台客户端/运行入口/` 的写点定位** —— R8 派发时应要求「先只读定位写点、再改」。
3. **D-4/D-13 的收敛口径未定**（唯一检索／唯一说明书形态分别该是谁），本路**只登记不选边**；需华哥或主会话裁决，否则两路会各改一半。
4. **本次未跑**（按任务约束）：HTML 黑盒验证器、发布门禁、`40007` 常驻重装配。故本文「能力数 625 / 越层 0」等口径均引自 `开发文档/未完成事项.md` 既有基线，**未在本路重新证真**。
5. **D-14 若搬迁 `开发工具/门禁/`**，会改 `python3.14 -m 开发工具.公开调用完整性门禁` 这类模块路径，牵动 `AGENTS.md`/`README.md`/`多会话并行开发规约.md` 的命令示例 → **R9 若派发，须同批 grep 全仓命令串**。
6. **若继续派孙代理，必须继续传递本协议**（允许修改域＝只新建/只改本文件所述落点；命令环境 `export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH`；python 用 `/opt/homebrew/bin/python3.14`；禁用 HTML 黑盒与发布门禁、禁用 `sleep` 轮询；结论必须带 `文件:行号` + 真实输出）。
