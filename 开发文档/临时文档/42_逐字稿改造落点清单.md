# 42 直播逐字稿改造落点清单

> 目的：把 M8「逐字稿 26 处违规」的**全部问题**先找齐、每条给出**落点**（改哪个文件哪一行、换成什么），一次性执行，最后统一跑一次验证。
> 依据：`MCP工具箱/模块合规.py::校验模块合规(Path('.'), '直播逐字稿')` 实跑输出（27 条）。
> 原则（华哥定）：支持库 = 原子能力，模块 = 定义能力，技能 = 工作流封装。不搞公开/不公开。能用、稳定、快即可。

---

## 一、实测违规总清单（27 条）

### A 类　结构缺件（1 条）

| # | 位置 | 现状 | 落点 |
|---|---|---|---|
| A1 | `模块库/直播逐字稿/` | 缺 `资源预算.json` | 新建，按 `模块库/网页分析/资源预算.json` 的 10 键格式，取值按逐字稿实际（ffmpeg 起子进程、单次超时长） |

### B 类　白名单外导入（16 条）——根因：子文件互相 import

| # | 位置 | 现状 | 落点 |
|---|---|---|---|
| B1 | `实现/全自动精校.py` L14,15,16,17,19,21,22,23,24,25,26,27,28 | 13 行 `from 模块库.直播逐字稿.实现.X import ...` | 全删；改从注入的 `步骤` 表取函数 |
| B2 | `实现/分片转写.py` L14 | `from …实现.缓存与续跑 import 读JSON, 写JSON` | 全删；改为 `读JSON/写JSON` 由调用方注入（全自动精校转发） |
| B3 | `实现/复核编排.py` L9 | `from …实现.证据包 import 生成证据包` | 全删；改从 `步骤` 表取 |
| B4 | `实现/复核编排.py` L10 | `from …实现.复核调度 import 复核区间` | 全删；改从 `步骤` 表取 |

**落点机制（不需要改平台任何代码）**
- 依据 `运行核心/依赖防火墙.py:209`：`# 强制拒绝 1：跨包导入 实现/ 目录（同包 __init__ 导自身实现是合法入口模式）` → **同包 `__init__.py` 导自身 `实现/` 是平台认可的合法入口**。
- 依据 `MCP工具箱/模块合规.py`：只审计 `实现/**/*.py`，**模块根 `__init__.py` 不审计**。
- 依据华哥铁律「主文件只做调度，子文件不写 require_once」→ 映射为：
  - `模块库/直播逐字稿/__init__.py` = **主文件**：import 全部 16 个 `实现/` 模块，组 `步骤` 表，闭包注入后注册 4 个对能力。
  - `实现/*.py` = **子文件**：彼此零 import，所需兄弟函数一律走注入参数。
- 现状 `__init__.py` L17-20 已经是「同包 `__init__` 导自身实现」的合法写法，只是把装配范围从 4 个函数扩到 16 个模块 + 注入。

### C 类　格式解析原子操作（8 条）——换成底座「数据交换」

| # | 位置 | 现状 | 落点 |
|---|---|---|---|
| C1 | `实现/复核调度.py` L111 | `json.dumps(...)` 写复核盘 | `数据操作支持库.数据交换.序列化JSON` + `文件系统支持库.文件操作.写入文件` |
| C2 | `实现/复核调度.py` L142 | `json.loads(Path.read_text())` 读已有复核 | `文件系统支持库.文件操作.读取文件` + `数据操作支持库.数据交换.反序列化JSON` |
| C3 | `实现/导出落盘.py` L53 | `json.dumps(数据…)` | `数据操作支持库.数据交换.序列化JSON` |
| C4 | `实现/模式注册表.py` L22 | `json.loads(模式数据路径.read_text())` | `文件操作.读取文件` + `数据交换.反序列化JSON` |
| C5 | `实现/直播逐字稿.py` L148 | `json.dumps(...)` | `数据操作支持库.数据交换.序列化JSON` |
| C6 | `实现/直播逐字稿.py` L176 | `json.loads(...)` | `数据操作支持库.数据交换.反序列化JSON` |
| C7 | `实现/缓存与续跑.py` L68 | `json.loads(Path.read_text())` | `文件操作.读取文件` + `数据交换.反序列化JSON` |
| C8 | `实现/缓存与续跑.py` L78 | `json.dumps(...)` | `数据操作支持库.数据交换.序列化JSON` |

### D 类　直接 I/O（2 条）——换成底座「文件操作 / 资源管理」

| # | 位置 | 现状 | 落点 |
|---|---|---|---|
| D1 | `实现/缓存与续跑.py` L54 | 裸 `open(文件路径,"rb")` 分块 sha256 | `系统核心支持库.资源管理.创建内容摘要(文件路径, 算法)` |
| D2 | `实现/缓存与续跑.py` L68 | `Path.read_text` | `文件系统支持库.文件操作.读取文件` |

### E 类　隐形 I/O（检查器看不到，但同一铁律要一起清）

> 这些走 Path 对象（变量接收者），`模块合规` 观察不到，所以不计入 27 条。但既然母铁律是「子文件不碰 I/O」，一起换底座才算彻底。

| # | 位置 | 现状 | 落点 |
|---|---|---|---|
| E1 | `实现/缓存与续跑.py` L42 | `路径.mkdir(parents=True, exist_ok=True)` | `文件系统支持库.文件操作.创建目录(目录路径, 递归=True)` |
| E2 | `实现/缓存与续跑.py` L77 | `目标.parent.mkdir(...)` | 同上（`写入文件` 本身会建父目录，可直接省） |
| E3 | `实现/缓存与续跑.py` L120,121,124 | `is_file() / iterdir()` | `文件系统支持库.文件操作.判断存在` |
| E4 | `实现/导出落盘.py` L19,32,34,37,41 | `write_text / mkdir / stat / replace / unlink` | `系统核心支持库.资源管理.原子写入`（临时文件+改名的整段逻辑由底座承担） |
| E5 | `实现/导出落盘.py` L64,66 | `glob(".*.临时-*") / unlink()` | `文件系统支持库.文件操作.删除文件`（保留 glob 扫描，删除动作走底座） |
| E6 | `实现/分片转写.py` L40 | `目标.is_file() / stat().st_size` | `文件系统支持库.文件操作.判断存在` |
| E7 | `实现/复核调度.py` L105,106,133,135 | `mkdir / write_text / is_file / read_text` | `文件操作.创建目录 / 读取文件 / 判断存在` |
| E8 | `实现/模式注册表.py` L22 | `read_text` | `文件操作.读取文件` |

> 说明：E 类如果单独做，会出现「合规过了但铁律没守」。华哥要彻底，故与 C/D 合并同批。

### F 类　参数契约漂移（1 条，非门禁项但会导致运行期 400）

| # | 位置 | 现状 | 落点 |
|---|---|---|---|
| F1 | `直播逐字稿.全自动精校` | 三方不一致：`包声明.json` 9 参数 ／ `能力契约/参数契约.json` 12 参数 ／ `__init__.py` 注册 12 参数 | **以运行期为准，统一为 12**：`包声明.json` 补 `输出结构/附加要求/裁决模型句柄` |

### G 类　权威合规 12/13（1 条 FAIL，已知可接受）

- `校验模块合规` 附带的「权威合规：通过数 12 / 场景数 13」。
- 唯一 FAIL = 需要真实网关/模型的实调场景（`合规测试包.py`）。
- **华哥 2026-08-29 已裁决**：逐包 13/13 权威合规**已移出发布门禁**（`运行发布门禁.py` 载明「慢速审计不需要，能访问能拿数据就是没毛病」）。故本条**不作为本轮目标**，仅记录。

---

## 二、底座原子能力对照（本轮要用的，签名已核对）

| 原子能力 | 参数 | 返回 |
|---|---|---|
| `数据操作支持库.数据交换.序列化JSON` | `数据`(字典型) | 结果型，值=JSON 文本 |
| `数据操作支持库.数据交换.反序列化JSON` | `文本`(文本型) | 结果型，值=字典/列表 |
| `文件系统支持库.文件操作.读取文件` | `文件路径`, `编码`=utf-8 | 结果型，值=文本 |
| `文件系统支持库.文件操作.写入文件` | `文件路径`, `内容`, `编码`=utf-8（**自动建父目录**） | 结果型 |
| `文件系统支持库.文件操作.创建目录` | `目录路径`, `递归`=True | 结果型（已存在视为幂等成功） |
| `文件系统支持库.文件操作.判断存在` | `文件路径` | 逻辑型 |
| `文件系统支持库.文件操作.删除文件` | `文件路径` | 结果型（不存在视为幂等成功） |
| `系统核心支持库.资源管理.原子写入` | `目标路径`, `内容`（临时文件+fsync+rename，保留权限） | 结果型，值=真 |
| `系统核心支持库.资源管理.创建内容摘要` | `文件路径`, `算法` | 结果型，值=sha256 hex |
| `数据操作支持库.时间日期.获取当前时间` | `时区`=Asia/Shanghai | 结果型，值=ISO 文本 |

**取用方式**：子文件内 `from 公共契约.能力契约.调用器 import 获取能力调用器`，该导入在 `模块合规._分类导入` 白名单内（`公共契约.*`），`全自动精校.py` 现有 `_调用` 已是这个写法，不违规。

**要点**：
- 这些底座能力本身**已在平台注册**，无需新增支持库、无需新增能力、无需动六环产物。
- `调用能力` 目前由上层已注入（单测注入「纯假调用器」，生产注入唯一能力调用服务）。改动沿用同一注入通道，不引入新机制。

---

## 三、执行批次（全部改完再验证）

| 批次 | 内容 | 文件 |
|---|---|---|
| 第 1 批 | 建 `资源预算.json`；对齐 `包声明.json` 12 参数（F1） | `模块库/直播逐字稿/` |
| 第 2 批 | `缓存与续跑.py` 去 I/O+JSON（C7/C8/D1/D2/E1/E2/E3）：8 个函数改走底座 | 实现/缓存与续跑.py |
| 第 3 批 | `导出落盘.py`（C3/E4/E5）改 `原子写入` | 实现/导出落盘.py |
| 第 4 批 | `模式注册表.py`（C4/E8）、`直播逐字稿.py`（C5/C6）改底座 | 实现/ |
| 第 5 批 | `复核调度.py`（C1/C2/E7）改底座 | 实现/复核调度.py |
| 第 6 批 | 删 `分片转写.py` L14、`复核编排.py` L9/L10，改注入（B2/B3/B4/E6） | 实现/ |
| 第 7 批 | `全自动精校.py` 删 13 行 import，改 `步骤` 表（B1） | 实现/全自动精校.py |
| 第 8 批 | `__init__.py` 主文件化：import 16 模块 → 组步骤表 → 闭包注入 → 注册 4 能力 | `模块库/直播逐字稿/__init__.py` |
| 第 9 批 | 适配受影响单测（`测试_逐字稿分片转写.py` 等注入 JSON 助手） | 测试中心/模块库/ |

---

## 四、收口验证（最后一次性跑）

1. `校验模块合规(Path('.'), '直播逐字稿')` → 违规 **27 → 0**（权威合规 12/13 除外）。
2. 9 个逐字稿单测全过（叶子测试为主，`测试_直播逐字稿` 收口时跑一次）。
3. `公开调用完整性门禁.py` → 本模块 0 项。
4. 用平台生成器（非空排除路径）重算本包 `完整性摘要.json`。
5. `依赖防火墙.py` → 无新增违规。

---

## 五、并发边界（本轮绝对不碰）

- `开发文档/待重启事项_技能库目录归位.md`
- `支持库/适配层/YAML提供者/完整性摘要.json`
- `支持库/后端/数据操作支持库/数据集合/**`
- `支持库/后端/办公文档支持库/轻量文本解析/**`
- `支持库/后端/代码解析支持库/**`（另一会话在途，含未跟踪 `语法索引/`）
- `后端核心/后端核心.py`（另一会话正在改 `_校验热接入包`）

> 本方案**不需要动 `后端核心.py`**（不加新能力、不改注册语义），与另一会话零交集。

---

## 六、遗留与待决

- **B 类方案前提**：`__init__.py` 主文件化后，模块对外仍是原来 4 个能力，声明/注册/搜索/契约维持 4=4=4=4，`模块对称门禁` 不受影响；`装配系统` 的「声明 ⊆ 注册」单向校验也不受影响。
- **F1 参数契约漂移**为运行期真实缺陷（传 `输出结构` 等参数会被网关切 400），本轮顺手修。
- **G 类**（权威合规 12/13 的实调场景）已由华哥裁决移出发布门禁，不在本轮范围。

---

## 七、阶段二：实际结果（2026-09-11 收口）

### 执行批次（9 批，全部落地）

| 批次 | 内容 | 文件 |
|------|------|------|
| 1 | 资源预算 + 参数契约补 3 参数 | `资源预算.json`、`包声明.json`、`能力契约/参数契约.json`、`能力定义.json` |
| 2 | 去 I/O + JSON：8 函数走底座 | `实现/缓存与续跑.py` |
| 3 | 原子写入替临时文件逻辑 | `实现/导出落盘.py` |
| 4 | 模式注册表 + 主转写去 I/O | `实现/模式注册表.py`、`实现/直播逐字稿.py` |
| 5 | 复核盘落盘/复用走底座 | `实现/复核调度.py` |
| 6 | 删跨文件导入，改注入 | `实现/分片转写.py`、`实现/复核编排.py` |
| 7 | 删 13 行同级导入，主流程改 `步骤` 注入 | `实现/全自动精校.py` |
| 8 | 主文件化：导入 16 子模块 + 组 38 键步骤表 | `__init__.py` |
| 9 | 测试补底座装配 | `测试中心/模块库/` 三个测试 |

### 计划外补清（同铁律，工具因接收者为变量而看不见）

- `实现/入参校验.py` 三处真实文件 I/O：
  - `目录.mkdir(parents=True, exist_ok=True)` → `文件系统支持库.文件操作.创建目录{递归:True}`
  - `源文件.is_file()` → `文件系统支持库.文件操作.获取大小`（实测：对文件成功、对目录/缺失返回 `文件不存在`，语义与 `is_file` 等价，且顺手拿到大小）
  - `源文件.stat().st_size` → 复用上一步的 `获取大小.值`
  - 保留 `shutil.disk_usage`：底座无「磁盘剩余空间」原子能力，无等价替代。

### 新增错误码（主流程装配守卫）

`实现/全自动精校.py` 入口新增 `装配缺失` 守卫（步骤表缺项即如实失败，不静默降级）；已同步声明到 `能力契约/参数契约.json` 与 `能力定义.json`。

### 五道验证（一次跑完，全绿）

| 项 | 命令 | 结果 |
|----|------|------|
| 1 语法编译 | `python3 -m py_compile` 12 文件 | 通过 |
| 2 模块合规 | `校验模块合规(Path('.'), '直播逐字稿')` | **违规 27 → 0**；权威合规 **13/13**（改造前 12/13） |
| 3 单元测试 | `python3 -m unittest` 9 文件 | **129 tests OK**（8 叶子 117 + 模块级 12），0.64s + 1.16s |
| 4 公开调用完整性门禁 | `python3 开发工具/公开调用完整性门禁.py` | 本模块 **0 项**（全仓 2 项属另一会话的 `语法索引`、`轻量文本解析`） |
| 5 依赖防火墙 | `审计依赖(绝对路径/模块库/直播逐字稿)` | **0 违规** |

`完整性摘要.json` 已用 `完整性摘要生成器.生成单包摘要`（非空排除路径）单包重算，37 文件，二次生成一致（不自我污染）。

### 调用方式（不变）

外部只经模块公开入口的 4 个能力调用，与改造前完全一致：

```python
from 模块库.直播逐字稿 import 全自动精校
结果 = 全自动精校(
    源文件路径="/abs/录制/2026-09-10_直播.mp4",
    导出路径="/abs/出稿/2026-09-10_直播_精校稿.md",
    缓存目录="/abs/缓存/2026-09-10_直播",
    模式=1,
    附加术语="华世王镞、合作店、抖店",
    输出结构="",          # 消费者按业务传，底座不写死
    附加要求="",
)
```

子文件之间零 import：全流程由主文件 `__init__.py` 的 38 键 `步骤表` 经参数注入；缺一步即 `装配缺失`。任一步报错只改那一个子文件。
