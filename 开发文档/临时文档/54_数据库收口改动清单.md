# 54 数据库访问重复实现收口改动清单（可执行方案）

> 性质：**只读盘点 + 可执行改动清单**。本次**未修改、未移动、未删除任何源码/测试/配置/文档**（只新建本文件）；未执行 `git` 写操作；未运行任何写盘编译器；**未连接任何数据库**（只做 grep/哈希/JSON 读取/只读门禁扫描）。
> 探针时间：2026-09-14 20:41 CST（macOS 26.6.2 / Python 3.14.4）。
> 上游依据：`开发文档/临时文档/50_数据库四套实现收敛_子代理盘点.md`（全局盘点）、`开发文档/临时文档/53_待裁决判据缺口清单.md:39`（C1 项）、`开发文档/决策记录/0008_数据库连接池唯一化与anthropic协议.md`（决定一/二/三/四）。
> **重要前提**：`支持库/后端/数据库连接支持库/psycopg数据库` 与 `测试中心/支持库/测试_数据库连接池.py` 当前处于**未提交改动**中（`git status --porcelain` 显示为 ` M` / `??`）。本清单以**当次工作区实时状态**为基线，凡引用行号均为当次实测行号。

---

## 0. 裁决口径（本次收口的唯一判据）

华哥已裁（最高优先级，原文）：

> 「同一目的出现两套实现＝重复，**保留最全面的那个 + 归因用的原子能力**，其余蒸馏进唯一支持库」
> 「数据库池/句柄/连接这类底层机制归支持库（原子能力），第三方库翻译层归 `支持库/适配层` 提供者，模块只组装，业务不碰。」

据此推出的三条硬判据，本清单每一步都按它判定：

| 判据 | 落地含义 |
|---|---|
| J1 最全面者胜 | 同一件事两份实现，保留能力/语义/bug 修复最全的那份 |
| J2 对外能力面只在支持库 | 池/句柄/连接（对外原子能力）归 `支持库/后端`；`支持库/适配层` 的 Provider 只做第三方驱动翻译，**不得成为公开能力 owner**（该口径已是现役门禁规则：`开发工具/公开调用完整性门禁.py:156` 「提供者-适配层Provider不得成为公开owner」） |
| J3 零调用方不留第二腿 | 已被 0008 决定二否掉的「兼容入口/旧腿」一律不留；没有调用方的第二份实现不得以"兼容"名义留存 |

---

## 1. 结论速览（改动总账）

现场核实后，`数据库访问` 这件事实测存在 **9 处实现面**，其中要动手的是 **6 处**：

| 编号 | 位置 | 形态 | 实现行数 / sha256(16) | 生产调用方 | 判定 |
|---|---|---|---|---|---|
| **A4** | `支持库/后端/数据库连接支持库/psycopg数据库` | **句柄 + 唯一同步有界池**（5 能力） | 371 行 / `f064ccd1ff748e4c` | **0**（测试 1 个） | **保留＝PostgreSQL 唯一对外能力面** |
| A1 | `支持库/后端/数据库连接支持库/SQLite数据库` | 无池自包含（4 能力） | 132 行 / `4f03ca0bb8884a4f` | **0**（测试 1 个） | 保留＝SQLite 唯一对外能力面 |
| **A2** | `支持库/后端/数据库连接支持库/pg8000数据库` | 无句柄（4 能力） | 201 行 / `ffc534c74cde2ffc` | **0** | **删（下线）** |
| **A3** | `支持库/后端/数据库连接支持库/psycopg2数据库` | 无句柄（4 能力） | 202 行 / `0d8793c1a11b1e41` | **0**（测试 1 个，5 处字符串夹具） | **删（下线）** |
| **B1** | `支持库/适配层/pg8000提供者` | 与 A2 **字节级完全相同** | 201 行 / `ffc534c74cde2ffc` | **0**（测试 1 个） | **删** |
| **B2** | `支持库/适配层/psycopg2提供者` | 与 A3 **字节级完全相同** | 202 行 / `0d8793c1a11b1e41` | **0**（测试 1 个） | **删** |
| **B3** | `支持库/适配层/psycopg提供者` | A4 的**旧版副本**（无池、连接串传参） | 193 行 / `7f53a0de42d1bb5a` | **0**（测试 1 个） | **剥掉公开能力面**（见 §4.3 二选一） |
| **D** | `平台控制面/提供者/PostgreSQL提供者.py` | 第三套 PG 访问（驱动三选一、长连接） | 179 行 / `4ccd4d07f9e7d85a` | **0（含 0 测试）** | **删** |
| C | `平台控制面/提供者/数据库提供者.py` | SQLite 池（类 + 线程执行器） | 485 行 / `d945b5d20c694758` | **0**（测试 1 个，10 处） | **待裁决**（C2 项，另立授权） |

**总账**

| 项 | 数量 | 明细 |
|---|---|---|
| 要删的**包目录** | **4 个**（B1、B2、A2、A3）+（B3 若采"（乙）整包删"则 **5 个**） | 文件数 20 + 18 + 18 + 19（+18）＝ 75（+18）个文件 |
| 要删的**单文件** | **1 个**（`平台控制面/提供者/PostgreSQL提供者.py`）；（C 若授权，再 +1） | — |
| 要**改行**的源码/测试/配置文件 | **10 个** | 见 §5 逐行表 |
| 要**同步重算**的产物文件 | **≥ 15 个** | 见 §6 逐产物表 |
| 生产代码真调用方 | **0 个** | 全仓 grep 实测（含 `模块库/`、`运行核心/`、`平台控制面/`、`开发工具/`、`项目适配层/`、`公共契约/`、`示例项目/`） |
| 测试调用方 | **4 个测试文件** | `测试_pg8000提供者.py`、`测试_psycopg2提供者.py`、`测试_psycopg提供者.py`、`测试_项目锁传递闭包.py` |

---

## 2. 现场核实结果（逐项硬证据）

### 2.1 三个适配层提供者：注册了哪些公开能力 / 被谁引用 / 哈希是否字节级相同

**注册的公开能力（实测读 `__init__.py::注册能力`）**

| 适配层包 | 包id | 注册的能力 id（实测行号） | 能力数 |
|---|---|---|---|
| `psycopg提供者` | `支持库.适配层.psycopg提供者` | `psycopg数据库.连接`(`__init__.py:24`)、`.查询`(`:27`)、`.事务执行`(`:31`)、`.关闭`(`:35`) | 4 |
| `pg8000提供者` | `支持库.适配层.pg8000提供者` | `pg8000数据库.连接`(`:24`)、`.查询`(`:27`)、`.事务执行`(`:31`)、`.关闭`(`:35`) | 4 |
| `psycopg2提供者` | `支持库.适配层.psycopg2提供者` | `psycopg2数据库.连接`(`:24`)、`.查询`(`:27`)、`.事务执行`(`:31`)、`.关闭`(`:35`) | 4 |

三者 `包声明.json` 均为 `"类型": "支持库"`、`"版本": "1.0.0"`、**均未标 `已废弃`** → 全部进发现器、全部被 `安装支持库` 注册（`运行核心/加载器/包安装/支持库安装.py:75-76` 只跳过 `已废弃`）。

**能力 id 引用面（grep 全仓，排除 `__pycache__`/`工程缓存`/`.git`，命令见 §8）**

```
grep -rnIE 'pg8000数据库\.(连接|查询|事务执行|关闭)"' . | grep -v '__pycache__|工程缓存'
  → 命中 19 个文件：全部在 支持库/适配层/pg8000提供者/** 与 支持库/后端/数据库连接支持库/pg8000数据库/** 两包自身契约产物内
     （外加 1 处跨包引用：支持库/后端/数据库连接支持库/SQLite数据库/复用决策.json:5）
grep -rnIE 'psycopg2数据库\.(连接|查询|事务执行|关闭)"' .  → 全部命中 支持库/适配层/psycopg2提供者/** 与 支持库/后端/数据库连接支持库/psycopg2数据库/**
grep -rnIE 'psycopg数据库\.(连接|查询|事务执行|关闭)"' .   → 全部命中 支持库/适配层/psycopg提供者/**
```

**结论**：三套适配层能力 id **在适配层三包目录之外零引用**（测试中心/开发工具/模块库/平台控制面/运行核心/公共契约/项目适配层 全部为 0 命中）。

**实现文件哈希比对（`shasum -a 256` 实测，16 位前缀）**

| 适配层实现 | sha256(16) | 后端同名子包实现 | sha256(16) | 是否字节级相同 |
|---|---|---|---|---|
| `适配层/pg8000提供者/实现/提供者.py` | `ffc534c74cde2ffc` | `后端/…/pg8000数据库/实现/提供者.py` | `ffc534c74cde2ffc` | ✅ **完全相同** |
| `适配层/psycopg2提供者/实现/提供者.py` | `0d8793c1a11b1e41` | `后端/…/psycopg2数据库/实现/提供者.py` | `0d8793c1a11b1e41` | ✅ **完全相同** |
| `适配层/psycopg提供者/实现/提供者.py` | `7f53a0de42d1bb5a` | `后端/…/psycopg数据库/实现/提供者.py` | `f064ccd1ff748e4c` | ❌ 不同（旧版副本） |

`diff -rq`（整目录）进一步确认：pg8000/psycopg2 两份**只有 `__init__.py`/包声明/能力定义/契约/摘要/说明**等"包壳元数据"不同，**实现代码零差异**；psycopg 一份差异还在 `实现/提供者.py` 本身（无池 193 行 vs 池+句柄 371 行）。

**派生证据（很重要）**：后端三包的 `说明/目录树与命令.md:3` 至今写着**适配层的包id**——
- `后端/…/psycopg数据库/说明/目录树与命令.md:3` → `> 包id：\`支持库.适配层.psycopg提供者\``
- `后端/…/pg8000数据库/说明/目录树与命令.md:3` → `> 包id：\`支持库.适配层.pg8000提供者\``
- `后端/…/psycopg2数据库/说明/目录树与命令.md:3` → `> 包id：\`支持库.适配层.psycopg2提供者\``

即三套"适配层提供者"就是**后端子包被整目录复制改名**的产物，说明书都抄成了原包 id。这是"同一份代码两个包壳"的直接书证。

**额外事实（回退友好选项的依据）**：`支持库/适配层/__init__.py` 只导出 `数据库适配器/HTTP服务适配器/本地进程适配器/动态库适配器/提供者注册表/密码签名`（`:27-32`、`:70-76`），**不导出这三套提供者**；`开发工具/项目编译/项目编译器.py:172/:634` 的适配层收录白名单同样**不含**这三套（只含 `提供者注册表/密码签名提供者/数据库适配器/HTTP服务适配器/本地进程适配器/动态库适配器/配置契约`），而 `客户端/构建平台客户端.py:30-31` 的 `顶层包表` 整树复制整个 `支持库` → 制品里确实有它们（实测最新制品目录含三套）。

### 2.2 后端四个子包：公开能力 / 真实调用方 / psycopg 五能力是否被别处引用

**公开能力（实测读 `__init__.py::注册能力`）**

| 子包 | 注册能力 id | 数量 | 实现行 | sha256(16) |
|---|---|---|---|---|
| `psycopg数据库` | `数据库连接支持库.psycopg数据库.连接数据库`(`__init__.py:28`)、`.查询数据库`(`:32`)、`.事务执行数据库`(`:36`)、`.关闭数据库连接`(`:40`)、`.连接池状态`(`:43`) | **5** | 371 | `f064ccd1ff748e4c` |
| `pg8000数据库` | `数据库连接支持库.pg8000数据库.连接`(`:24`)、`.查询`(`:27`)、`.事务执行`(`:31`)、`.关闭`(`:35`) | 4 | 201 | `ffc534c74cde2ffc` |
| `psycopg2数据库` | `数据库连接支持库.psycopg2数据库.连接`(`:24`)、`.查询`(`:27`)、`.事务执行`(`:31`)、`.关闭`(`:35`) | 4 | 202 | `0d8793c1a11b1e41` |
| `SQLite数据库` | `数据库连接支持库.SQLite数据库.连接`(`:17`)、`.查询`(`:20`)、`.事务执行`(`:24`)、`.关闭`(`:28`) | 4 | 132 | `4f03ca0bb8884a4f` |

**真实调用方（grep 全仓，排除包自身目录）**

| 子包 | 生产调用方 | 测试调用方 | 其他引用 |
|---|---|---|---|
| `psycopg数据库`（保留） | **0** | `测试中心/支持库/测试_数据库连接池.py:29-31`（import 五能力）、`:33-34`（包目录）、`:35`（连接串 `127.0.0.1:5432`） | `开发文档/交付记录/20260914_…/验收记录.md:76`（文档） |
| `pg8000数据库` | **0** | **0** | 仅 `支持库/后端/数据库连接支持库/SQLite数据库/复用决策.json:5` 把 `数据库连接支持库.pg8000数据库.连接` 列为**候选能力id** |
| `psycopg2数据库` | **0** | `测试中心/项目适配/测试_项目锁传递闭包.py:177/:179/:180/:191/:192`（**包id 字符串夹具**，非能力调用） | — |
| `SQLite数据库` | **0** | `测试中心/支持库/测试_SQLite数据库.py:17` | — |

**`psycopg数据库` 现行五能力是否已被别处引用？—— 结论：只在 1 个测试文件里被引用，生产代码零引用。**

```
grep -rnIE '数据库连接支持库\.psycopg数据库' .（排除 包内/工程缓存/__pycache__）
→ 包外仅 1 处代码命中：测试中心/支持库/测试_数据库连接池.py:29
→ 其余 6 处均为开发文档（50_…盘点.md:73-77、交付记录/20260914_…/验收记录.md:76）
```

`测试_数据库连接池.py:37` 已把"旧能力不得残留"写成断言（`旧能力后缀 = (".连接", ".查询", ".事务执行", ".关闭")`，`:213` 处 `assertFalse(名称.endswith(旧能力后缀))`），即 A4 侧**自我收口已完成**，缺的只是把**别的腿**拆掉。

**推论（关键）**：A4 的五能力目前**没有被任何业务方接线**——所谓"新链已通"只到"HTTP 端到端验证通过"这一层（见 `未完成事项.md:114`），下游 V3/网关侧尚无实际消费者。所以**删旧腿不会打断任何在跑的调用链**，但**也没有任何真实业务在验证新链**（这是本收口最大的隐性风险，见 §7 R5）。

### 2.3 平台控制面两个文件：真实调用方逐个列出

#### D `平台控制面/提供者/PostgreSQL提供者.py`（179 行，sha `4ccd4d07f9e7d85a`，mtime 2026-07-31）

全仓引用**只有 2 处，且都在同一个文件里**，都是"重新导出"而非调用：

| # | 位置（文件:行） | 性质 |
|---|---|---|
| 1 | `平台控制面/提供者/__init__.py:5` | `from 平台控制面.提供者 import (HTTP提供者, PostgreSQL提供者, 采样器, 动态库提供者, 数据库提供者, 进程提供者, 进程组管理, 资源硬限制)` —— import |
| 2 | `平台控制面/提供者/__init__.py:18` | `__all__` 列表中的字符串 `"PostgreSQL提供者"` |

**零调用方证据**

```bash
cd "~/Documents/Agent/PHP/系统工程平台"
grep -rnIE 'PostgreSQL提供者' --exclude-dir=__pycache__ --exclude-dir=工程缓存 \
  --exclude-dir=.git --exclude-dir=.pytest_cache --exclude-dir=.ruff_cache .
```
实测结果：**13 行 / 5 个文件**，其中
- `平台控制面/提供者/PostgreSQL提供者.py:78`（类定义 `class PostgreSQL提供者:`）—— 自身
- `平台控制面/提供者/__init__.py:5`、`:18` —— 上表两处
- `开发文档/临时文档/50_…md`（6 行）、`开发文档/临时文档/53_…md:39`、`开发文档/项目证据/MCP使用反馈.jsonl:142` —— 文档/证据

**测试引用：0**（`测试中心/**` 零命中）。类内 8 个方法（`检测`/`打开连接`/`查询`/`执行`/`提交`/`回滚`/`关闭`/`重连`）**无一被外部调用**，返回自建 `提供结果` dataclass 而非平台统一 `结果` 类型。

#### C `平台控制面/提供者/数据库提供者.py`（485 行，sha `d945b5d20c694758`，mtime 2026-08-31）

| # | 位置（文件:行） | 性质 |
|---|---|---|
| 1 | `平台控制面/提供者/__init__.py:6` | import（与 HTTP提供者 等同批） |
| 2 | `平台控制面/提供者/__init__.py:18` | `__all__` 字符串 |
| 3 | `测试中心/第一批维修/测试_SQLite并发与收敛.py:16` | `from 平台控制面.提供者.数据库提供者 import 数据库提供者` |
| 4-12 | 同测试 `:68`、`:111`、`:148`、`:149`、`:178`、`:206`、`:237`、`:266`、`:285`、`:298` | 实例化/继承（`数据库提供者(...)`、`class 慢读提供者(数据库提供者)`）—— 共 **10 处** |

**生产调用方：0**（`模块库/`、`运行核心/`、`后端核心/`、`平台控制面/` 其余模块零 import）。

**其唯一对外工厂零调用方证据**

```bash
grep -rnIE '创建数据库能力函数' --exclude-dir=__pycache__ --exclude-dir=工程缓存 --exclude-dir=.git .
```
实测：**4 行 / 2 个文件** —— `平台控制面/提供者/数据库提供者.py:469`（定义）、`:479`（内部返回的闭包），其余 2 行是 `50_…盘点.md` 文档描述。**即：定义处之外零引用。**

C 不在本批动作内（属 `53_…md:39` C2 项，未获授权），本清单只给方案与代价（§4.6）。

### 2.4 交叉引用与陈旧引用（收口时会踩到的地雷）

| # | 位置 | 事实 | 影响 |
|---|---|---|---|
| X1 | `支持库/后端/数据库连接支持库/SQLite数据库/复用决策.json:5` | 候选能力id 列表**包含 `数据库连接支持库.pg8000数据库.连接`**（该文件当前正处于未提交改动 ` M`） | 删 A2 后此引用指向不存在的包 → 复用决策台账失真 |
| X2 | 后端三包的 `说明/目录树与命令.md:3` | 写着适配层的包id（见 §2.1） | 说明书名实不符，收口时一并纠正 |
| X3 | `支持库/后端/数据库连接支持库/psycopg数据库/__init__.py:4` | docstring 仍写「公开能力：…连接/查询/事务执行/关闭」（旧四能力名），实际注册的是五能力（含 `连接数据库` 等新名） | 名实不符 |
| X4 | `运行核心/依赖防火墙.py:58-59` | 第三方模块白名单含 `pg8000`、`psycopg`、`psycopg2` | 删包后 `pg8000`/`psycopg2` 成无归属条目 |
| X5 | `运行核心/环境指纹/环境指纹.py:29/:31/:32` | 版本探测含 `pg8000`、`psycopg`、`psycopg2` | 同上（口径问题，非技术风险） |
| X6 | `开发工具/复用审计/提供者直连规则.py:23` | `"数据库"` 直连模块表含 `psycopg2/psycopg/pg8000/pymysql/pymongo/redis/sqlite3` | 唯一能力面收敛后此表应随之收敛 |
| X7 | `测试中心/慢速层/第十三阶段/` | 只剩 `__pycache__/测试_数据库提供者.cpython-314.pyc`，源码不在树内；`开发文档/项目证据/验证历史.jsonl:242` 仍拿它当成功证据 | 历史证据悬空（另立批次） |

---

## 3. 保留 / 删除判定（逐条给依据，对齐口径）

| 判定 | 对象 | 依据（口径 + 实测） | 结论 |
|---|---|---|---|
| **保留（最全面者）** | A4 `后端/…/psycopg数据库` | J1：唯一"句柄 + 唯一同步有界池"（371 行 vs 副本 193 行）；唯一有**真跑通真实闭环**的测试（`测试_数据库连接池.py` 10 项）；**唯一符合底座"一次传参必须用句柄"铁律**；0008 决定一已裁"底座只保留唯一一套同步池" | 保留为 **PostgreSQL 唯一对外能力面** |
| **保留** | A1 `后端/…/SQLite数据库` | 不同数据库种类，不构成"同一件事多套"；0008 决定一的设计正是 `数据库种类 → 提供者(sqlite/postgresql)` | 保留为 **SQLite 唯一对外能力面** |
| **删（同源副本）** | B1 ↔ A2、B2 ↔ A3 | 实现文件 sha256 **字节级完全相同**；J2：适配层 Provider 不得做公开能力 owner（门禁规则原文）；J3：无调用方 | 删 B1、B2；A2、A3 一并下线（驱动已被 0008 唯一化为 psycopg3） |
| **剥面或删（过期副本）** | B3 `适配层/psycopg提供者` | J1：A4 是其超集；J2：其 4 个自包含能力是"第二腿"；J3：0008 决定二已明令"旧四条连接串能力直接删除，不留兼容" | 二选一，见 §4.3 |
| **删** | D `平台控制面/提供者/PostgreSQL提供者.py` | J3：零调用方＋零测试；2026-08-18 只读审计已记为重复实现（`项目证据/MCP使用反馈.jsonl:142`） | 删 |
| **待裁决** | C `平台控制面/提供者/数据库提供者.py` | 与 A1 同一后端 `sqlite3` 的两套实现；J3：零生产调用方 | 方案见 §4.6（需 C2 授权） |
| **保留（合法分层）** | `支持库/适配层/数据库适配器` + `提供者注册表/标准提供者.py` | 模拟提供者（不连真实库）、`AGENTS.md:215` 明确豁免 | 保留（建议改名标注"模拟"，非本批动作） |

---

## 4. 收口改动清单（分步、可执行、每步带回退）

> 执行顺序不可调换：**先加门禁 → 再删副本 → 再清引用 → 最后重算产物与制品**。
> 每步的"验证命令"均为**只读**命令；`python3.14 -m unittest` 会真连 `127.0.0.1:5432`（本机 PostgreSQL 在跑），属预期行为。

### 4.0 前置：冻结基线（必须先做）

当前 A4 与 `测试_数据库连接池.py` 处于未提交改动中，**同批删除会与并行改动交叠**。

```bash
cd "~/Documents/Agent/PHP/系统工程平台"
git status --porcelain | grep -E 'psycopg数据库|测试_数据库连接池'   # 必须为空才开工
git rev-parse --short HEAD                                          # 记录基线指纹
```

- **判据（开工条件）**：`支持库/后端/数据库连接支持库/psycopg数据库/**` 与 `测试中心/支持库/测试_数据库连接池.py` **均已提交**，且 `python3.14 -m unittest 测试中心.支持库.测试_数据库连接池` 全绿。
- **回退**：本步无副作用。

### 4.1 【P0-1】删适配层两套同源副本（B1、B2）

**动作**：删除两个整包目录

```
支持库/适配层/pg8000提供者/     （20 个文件，含 验证夹具/最小数据库状态.sql、验证场景/正向HTTP场景.json）
支持库/适配层/psycopg2提供者/   （18 个文件）
```

**依据**：实现文件与后端同名子包 **sha256 完全相同**（`ffc534c74cde2ffc` / `0d8793c1a11b1e41`）；能力 id 在包外零引用；包id 未被 `支持库/适配层/__init__.py` 导出；未被 `项目编译器` 适配层白名单收录。
**同步产物**：无（包整体消失，其 `能力定义.json`/`权限契约`/`验证场景引用.json`/`能力搜索数据.json`/`说明书`/`完整性摘要.json` 随包移除）。`完整摘` 全局计数由动态扫描得出，无需手改。`依赖生命周期审计` 的标准提供者数将由 **17 → 15**（实测基线见 §8）。
**同一批必须同改**：配套测试（§5 表 T2/T3）——否则 `unittest` 收集即 `ModuleNotFoundError`。
**验证命令**：

```bash
grep -rnIE '适配层\.(pg8000|psycopg2)提供者' --exclude-dir=__pycache__ --exclude-dir=工程缓存 --exclude-dir=.git . \
  | grep -v '^./开发文档/'          # 期望：0 命中
python3.14 开发工具/依赖生命周期审计/运行审计.py   # 期望：标准提供者 15 个，违规仍仅 YAML提供者 2 条
python3.14 -m 开发工具.公开调用完整性门禁          # 期望：通过，0 违规
```
**回退**：`git checkout -- 支持库/适配层/pg8000提供者 支持库/适配层/psycopg2提供者`（若已 `git rm`：`git reset -- <路径> && git checkout -- <路径>`）。两包零调用方，回退成本≈0。

### 4.2 【P0-2】下线后端两个非选驱动子包（A2、A3）

**动作**：删除两个整包目录

```
支持库/后端/数据库连接支持库/pg8000数据库/     （18 个文件）
支持库/后端/数据库连接支持库/psycopg2数据库/   （19 个文件，含 验证场景.json）
```

**依据**：0008 决定一「PostgreSQL 只保留唯一一套同步池」+ 决定二「旧四条连接串能力直接删除，不留兼容」；两包生产调用方 0；驱动已被唯一化为 `psycopg`(v3)。
**同步产物（逐条）**：

| 产物 | 文件 | 改动 |
|---|---|---|
| 能力定义 | 两包各自的 `能力定义.json` | 随包删除（各 4 能力） |
| 权限契约 | 两包 `权限契约/权限契约.json` | 随包删除 |
| 验证场景 | `psycopg2数据库/验证场景.json`、`pg8000数据库/验证场景引用.json` | 随包删除 |
| 说明书 | 两包 `说明/使用说明.md`、`说明/目录树与命令.md` | 随包删除 |
| 完整性摘要 | 两包 `完整性摘要.json` | 随包删除 |
| **正式包索引** | `开发工具/项目编译/正式包索引.py::构建索引` | **动态重算**，无静态文件；删包后索引自动少 2 项 |
| **六环门禁产物** | `开发工具/公开调用完整性门禁.py` 扫描段 `支持库/后端` | 动态；能力从 6 条重复降为 5 条唯一（psycopg）+ 4 条（SQLite） |
| **跨包引用残留** | `支持库/后端/数据库连接支持库/SQLite数据库/复用决策.json:5` | 见 §4.4 M2 |

**验证命令**：

```bash
grep -rnIE '数据库连接支持库\.(pg8000|psycopg2)数据库' --exclude-dir=__pycache__ --exclude-dir=工程缓存 --exclude-dir=.git . \
  | grep -v '^./开发文档/'          # 期望：0 命中（测试夹具已按 §4.4 M3 改完）
python3.14 -m 开发工具.公开调用完整性门禁
python3.14 -c "from pathlib import Path;from 开发工具.组件规范.完整性摘要 import 扫描正式包;print(len(扫描正式包(Path('.').resolve())))"
                                    # 期望：116 → 111（删 3 适配层 + 2 后端）
```
**回退**：同 4.1（两包零生产调用方）。

### 4.3 【P0-3】处置 B3 `支持库/适配层/psycopg提供者`（二选一，需华哥拍板）

**（甲）保留包壳作唯一驱动翻译层，剥掉它的公开能力面 —— 对齐口径原文，推荐**

- 动作（改文件，不删包）：
  1. `支持库/适配层/psycopg提供者/__init__.py` —— **删除 `注册能力` 函数体**（`:21-46`），`__all__`（`:18`）去掉 `"注册能力"`；保留 `连接/查询/事务执行/关闭` 作为内部实现入口。
     *注：`运行核心/加载器/包安装/支持库安装.py` 按包加载后调 `注册能力(注册表)`，需确认无该函数时加载器走"无公开能力"分支而不是抛异常 —— **执行前必须先只读核实**（`支持库安装.py:61-79`）。*
  2. `支持库/适配层/psycopg提供者/包声明.json` —— `能力` 列表清空（`:10-` 起 4 条全删）。
  3. `支持库/适配层/psycopg提供者/能力定义.json` —— `能力列表` 清空（`:7/:61/:130/:194` 四条），保留包级 `能力id`/`说明`/`提供者` 块。
  4. `支持库/适配层/psycopg提供者/能力契约/参数契约.json` —— `能力契约` 清空（4 条）。
  5. `支持库/适配层/psycopg提供者/权限契约/权限契约.json` —— 4 条映射（`:2-19`）清空。
  6. `支持库/适配层/psycopg提供者/验证场景引用.json` —— 场景 `psycopg数据库.本机最小PostgreSQL` 移除（`:11/:33/:57/:82` 步骤）。
  7. `支持库/适配层/psycopg提供者/能力数据/能力搜索数据.json` + `Agent查询数据.json` —— 4 条能力项移除。
  8. `支持库/适配层/psycopg提供者/说明/使用说明.md` —— 删"公开能力"节，改为"内部驱动封装层"。
  9. **重算** `支持库/适配层/psycopg提供者/完整性摘要.json`（包内任何文件改动后必须重算，否则摘要"清单不闭合"/"文件摘要不一致"）。
- 已核实**不会**因此变红的门禁：`依赖生命周期审计` 的「缺健康探针」可由其 `生命周期契约.json` 的 `健康探针`（方式/入口/成功条件/失败码）满足，`缺停止入口` 由其 `释放策略` 满足（`审计核心.py:109-177`）；`公开调用完整性门禁` 扫描段不含 `支持库/适配层`（`公开调用完整性门禁.py:21-22`）。
- **代价/前置条件**：剥完面之后，这个包在**没有任何消费者**的情况下等价于"零调用方的空壳"（A4 自己 `import psycopg`、不调它）。要让（甲）名副其实，须再做一步接线：把 A4 `实现/提供者.py:30` 的 `import psycopg` 与 `:193` 的 `psycopg.connect(...)` 改为调用适配层封装 —— **这会动到刚验收过的池实现（371 行 / 10 项测试全绿）**，风险等级中。故（甲）建议拆成两个批次：**甲-1 剥面（低风险，本批做）**，**甲-2 接线（另批做，动 A4 实现）**。
- **验证命令**：`python3.14 开发工具/依赖生命周期审计/运行审计.py`（期望违规提供者仍只 1 个）＋ `python3.14 -c "from 开发工具.组件规范.完整性摘要 import 校验完整性摘要;print(校验完整性摘要(__import__('pathlib').Path('支持库/适配层/psycopg提供者')))"`（期望 `(True, [])`）。
- **回退**：`git checkout -- 支持库/适配层/psycopg提供者`。

**（乙）整包删除（最小动作面）**

- 动作：删 `支持库/适配层/psycopg提供者/`（18 个文件）。
- 优点：动作面最小、无中间态、无双份残留；与 J1「保留最全面者 + 其余蒸馏进唯一支持库」的字面最贴（psycopg 的翻译代码已完整存在于 A4 内）。
- 缺点：与口径第一句「第三方库翻译层归 `支持库/适配层` 提供者」形式不符——收口后适配层将没有任何 psycopg 封装层。
- 验证/回退：同 4.1。

> **给华哥的一句话选择**：（甲）守"翻译层归适配层"的形式，代价是要么留空壳要么再动 A4（中风险）；（乙）一次删净、零风险，代价是形式上空缺。**若按 J1/J3 字面（"其余蒸馏进唯一支持库"）→ 选（乙）；若以分层形式优先 → 选（甲-1）。**

### 4.4 【P0-4】删平台控制面第三套 PG 实现（D）

**动作**：删除 `平台控制面/提供者/PostgreSQL提供者.py`（1 个文件 / 179 行 / sha `4ccd4d07f9e7d85a`）。

**同批必改（§5 表 M1）**：`平台控制面/提供者/__init__.py`

| 文件:行 | 现状 | 改为 |
|---|---|---|
| `平台控制面/提供者/__init__.py:5` | `from 平台控制面.提供者 import (HTTP提供者, PostgreSQL提供者, 采样器, 动态库提供者,\n                            数据库提供者, 进程提供者, 进程组管理, 资源硬限制)` | 去掉 `PostgreSQL提供者,`（该行变 7 个名字） |
| `平台控制面/提供者/__init__.py:18` | `    "HTTP提供者", "PostgreSQL提供者", "采样器", "动态库提供者", "数据库提供者",` | 去掉 `"PostgreSQL提供者", ` |

**同步产物**：无（该文件不是支持库包，无 `包声明.json`、不进能力注册表、不进正式包索引、不在六环扫描段）。
**验证命令**：

```bash
python3.14 -c "import 平台控制面.提供者"                     # 期望：无 ImportError
grep -rnIE 'PostgreSQL提供者' --exclude-dir=__pycache__ --exclude-dir=工程缓存 --exclude-dir=.git . | grep -v '^./开发文档/'
                                                              # 期望：0 命中
```
**回退**：`git checkout -- 平台控制面/提供者/PostgreSQL提供者.py 平台控制面/提供者/__init__.py`。

### 4.5 【P0-5】清理残留引用与陈旧产物（§5 表的 M1–M10 逐行）

按 §5 表逐条执行。**每一处改动若落在支持库包目录内，必须重算该包的 `完整性摘要.json`**（否则 `开发工具/组件规范/完整性摘要.py::校验完整性摘要` 会报"文件摘要不一致 / 清单不闭合"）。

### 4.6 【P1，需另立授权】C `平台控制面/提供者/数据库提供者.py` 与 A1 的 SQLite 唯一化

不在本批动作内（`53_…md:39` C2 项）。两条路径：

- **方案甲（对齐 J2，推荐）**：把 C 的池语义（有界池、借出超时、失效替换、写资源键排队、`sqlite3.interrupt()` 超时中断、关闭硬截止）**下移到 `支持库/后端/数据库连接支持库/SQLite数据库`**，C 降为薄门面或删除。代价：A1 形态从"无池自包含"变为"池 + 句柄" → `测试_SQLite数据库.py`（3 测试）与 `测试_SQLite并发与收敛.py`（9 测试 / 312 行）**全部要迁移重写**。
- **方案乙（最小改动）**：C 保留为"控制面内部池"，文档与命名明确它不是对外能力面。代价：同一后端长期两套实现，且 C 零生产调用方 → 与"足够干净的底座"相悖。
- **回退**：C 零生产调用方，任一方案`git checkout` 可回退；但**测试迁移不可逆**（只能在 git 层回退）。

---

## 5. 要改的文件与精确行（逐行表）

> 「同步产物」列的缩写：**摘要**＝该包 `完整性摘要.json` 必须重算；**索引**＝正式包索引动态重算（无静态文件）；**六环**＝`公开调用完整性门禁` 重跑；**审计**＝`依赖生命周期审计` 重跑。

| # | 文件 | 行号 | 现状 | 改动 | 同步产物 |
|---|---|---|---|---|---|
| **M1** | `平台控制面/提供者/__init__.py` | `:5`、`:18` | 含 `PostgreSQL提供者` | 两处移除 | 无（非支持库包） |
| **M2** | `支持库/后端/数据库连接支持库/SQLite数据库/复用决策.json` | `:5` | `"数据库连接支持库.pg8000数据库.连接"` | 删除该候选（或换成 `…psycopg数据库.连接数据库`） | **摘要**（SQLite数据库包）＋**审计** |
| **M3** | `测试中心/项目适配/测试_项目锁传递闭包.py` | `:177`、`:179`、`:180`、`:191`、`:192` | 用 `支持库.后端.数据库连接支持库.psycopg2数据库` 作包id 夹具 | 换成保留包id（如 `支持库.后端.数据库连接支持库.SQLite数据库`）或任意稳定中性包id；`_提供者目录`(`项目适配层/依赖锁定/传递闭包.py:86-97`) 按 `能力定义.json` 存在性解析，删包后该测试**反向证明断言必红**（见 §6） | 无 |
| **M4** | `测试中心/支持库/测试_pg8000提供者.py` | 全文件（186 行 / 9 测试） | `:19-20` import 适配层包、`:22-25` 定位包目录、`:26` 连 `54320` | 三选一：① 整体删除（能力已下线）；② 重写为 A4 的补充用例；③ 迁到 `测试中心/支持库/测试_数据库连接池.py` 并**改连 5432**（勿从 54320 换到另一个空端口，那是把 skip 搬家＝假绿） | 无 |
| **M5** | `测试中心/支持库/测试_psycopg2提供者.py` | `:19`、`:92`、`:110`、`:118`、`:128`、`:164` | import 包 + mock 路径 + 摘要包id 断言 | 同 M4（mock 路径 `支持库.适配层.psycopg2提供者.实现.提供者.*` 全部失效） | 无 |
| **M6** | `测试中心/支持库/测试_psycopg提供者.py` | `:19`、`:20`、`:26`、`:88-92`、`:122`、`:130`、`:140`、`:176` | 同上 | 按 §4.3 的（甲）/（乙）分支：甲-1 → 删除"公开能力/注册/场景"相关用例、保留驱动翻译用例；乙 → 整体删除 | 无 |
| **M7** | `运行核心/依赖防火墙.py` | `:58`（`…"pg8000",`）、`:59`（`…"psycopg", "psycopg2", …`） | 白名单含三驱动 | 裁决后清理无归属条目（**口径问题**：保留＝如实反映宿主环境，删除＝体现唯一依赖） | 无（不阻断） |
| **M8** | `运行核心/环境指纹/环境指纹.py` | `:29`(`"pg8000"`)、`:31`(`"psycopg"`)、`:32`(`"psycopg2"`) | 版本探测含三驱动 | 同 M7 口径 | 无 |
| **M9** | `开发工具/复用审计/提供者直连规则.py` | `:23` | `"模块": ["psycopg2", "psycopg", "pg8000", "pymysql", …]` | 唯一能力面为 psycopg 后收敛为 `["psycopg", "sqlite3"]`（`pymysql/pymongo/redis` 按各自能力面另裁） | 无 |
| **M10** | 三处陈旧说明书/文档串 | 见下 | — | 纠正 | **摘要**（涉及包） |
| ⤷ | `支持库/后端/数据库连接支持库/psycopg数据库/说明/目录树与命令.md` | `:3` | `> 包id：\`支持库.适配层.psycopg提供者\`` | 改为 `支持库.后端.数据库连接支持库.psycopg数据库` | **摘要** |
| ⤷ | `支持库/后端/数据库连接支持库/pg8000数据库/说明/目录树与命令.md` | `:3` | 同上（适配层包id） | 随包删除（4.2） | — |
| ⤷ | `支持库/后端/数据库连接支持库/psycopg2数据库/说明/目录树与命令.md` | `:3` | 同上（适配层包id） | 随包删除（4.2） | — |
| ⤷ | `支持库/后端/数据库连接支持库/psycopg数据库/__init__.py` | `:4` | docstring 写旧四能力名 `连接/查询/事务执行/关闭` | 改为五能力名 `连接数据库/查询数据库/事务执行数据库/连接池状态/关闭数据库连接` | **摘要** |

**要改的文件计数：10 个**（M1–M10；M10 行内含 4 个具体文件，合计 **13 个物理文件**）。

---

## 6. 会因此变红的测试（逐个给机制，不是估计）

| # | 测试入口 | 变红机制 | 当前基线 | 处置 |
|---|---|---|---|---|
| T1 | `测试中心.支持库.测试_pg8000提供者` | `:19` `from 支持库.适配层.pg8000提供者 import …` → **`ModuleNotFoundError`（收集期即崩，不是 fail）** | `Ran 9, OK (skipped=1)`（skip 原因：`127.0.0.1:54320 Connection refused`） | 按 M4 处置 |
| T2 | `测试中心.支持库.测试_psycopg2提供者` | `:19` import 同包 + `:92/:110/:118/:128` mock 目标路径失效 → 收集期崩 | `Ran 9, OK (skipped=1)` | 按 M5 处置 |
| T3 | `测试中心.支持库.测试_psycopg提供者` | `:19-20` import 同包 + `:20` 深入 `实现` 模块 → 收集期崩 | `Ran 9, OK (skipped=1)` | 按 M6 处置 |
| **T4** | `测试中心.项目适配.测试_项目锁传递闭包` → `test_锁内版本与能力定义版本不符失败` | 该用例用 `支持库.后端.数据库连接支持库.psycopg2数据库` 作夹具；`校验传递闭包` 经 `_提供者目录`（`项目适配层/依赖锁定/传递闭包.py:86-97`）按 `能力定义.json` 是否存在定位包目录，删包后返回 `None` → **用例末句 `self.assertTrue(恢复结果.成功, …)` 必红**（`测试_项目锁传递闭包.py:191-195`） | 未知（本次未跑，见下） | 按 M3 改夹具包id |
| T5 | `测试中心.第一批维修.测试_SQLite并发与收敛`（312 行 / 9 测试） | 仅 **§4.6 方案甲** 才红（`:16` import C、`:68` 等 10 处实例化、`:111` 继承） | 未跑 | 随 C 裁决另批 |
| T6 | `测试中心.支持库.测试_数据库连接池` | **不变红**（保留侧）；但它是 A4 唯一真闭环证据，收口后仍是唯一 | `Ran 10, OK`（doc 50 实测，真连 5432） | 保留 |
| T7 | `测试中心.支持库.测试_SQLite数据库` | **不变红**（A1 未动） | `Ran 3, OK`（doc 50 实测） | 保留 |

**本次纪律说明**：按"不连数据库"要求，**本次未运行任何测试**。T1–T3 的 `skipped=1` 与 T6/T7 的全绿引自上表注明的既有实测记录（`50_…盘点.md:175-182`）；T4 的变红机制来自**代码路径静态核实**（`传递闭包.py:86-97` + 用例断言），非运行结果。

**另外两类"会红但不在测试里"的**：

| 门禁/产物 | 变红机制 | 处置 |
|---|---|---|
| `完整性摘要` 校验（每包） | 任何包内文件改动（M2/M10）未重算摘要 → 「文件摘要不一致」/「清单不闭合」 | 每改必重算 |
| `依赖生命周期审计` | 数量变化不判红；但**若 4.3(甲) 把 `能力定义.json` 能力列表清空且生命契约不合规** → 「缺健康探针」 | 已核实其 `生命周期契约.json` 合规，不清空契约即安全 |
| `公开调用完整性门禁` | 删包/quota 变化**不会判红**（扫描段动态） | 每步后重跑作证据 |

---

## 7. 风险与未知

| # | 风险 | 等级 | 事实/证据 | 处置 |
|---|---|---|---|---|
| **R1** | **并行会话交叠**：A4 与 `测试_数据库连接池.py` 正被改造且**未提交**（`git status` 显示 ` M` / `??`） | **高** | `支持库/后端/数据库连接支持库/psycopg数据库/**` 12 个文件为 ` M`；`测试中心/支持库/测试_数据库连接池.py` 为 `??` | 严格执行 §4.0 开工条件：先落定并提交，再动删除 |
| **R2** | **T4 必红**（`测试_项目锁传递闭包`）——删 A3 后包目录不存在，"反向证明"断言无从成立 | 中 | `传递闭包.py:86-97` + 用例 `:191-195` | M3 换夹具包id；**执行前先跑一次该测试确认基线** |
| **R3** | **假绿搬家**：三套适配层提供者的"真实闭环"测试当前全部 `skipped`（连 **54320**，实测无监听）；若把用例迁到 A4 却换个空端口，等于把 skip 搬到新家 | 中 | `测试_pg8000提供者.py:26`、`测试_psycopg2提供者.py`、`测试_psycopg提供者.py:26` 均写 `54320`；本机 `5432` 有 `postgres` 监听 | 迁移后**必须真连 `127.0.0.1:5432`**（`测试_数据库连接池.py:35` 的连接串口径） |
| **R4** | **门禁盲区未补**：现有两道"重复"门禁抓不到本次这类重复 ——「公开能力重复注册失败」只在**同 id** 时触发（三套前缀不同，永不触发）；`开发工具/复用审计/能力调用图审计.py:132/:153` 的"重复能力"**只审模块库函数体**，不审支持库之间 | 中 | `公开调用完整性门禁.py:241` 仅比对同名能力；`能力调用图审计.py:153` 限定模块库 | **收口同批应新增一条审计**：同一第三方发行包（`依赖锁.json` 的 `模块名`）只能被一个支持库/提供者声明。判据现成。否则同类问题必然复发 |
| **R5** | **能力面收缩的外部影响**：A4 五能力在当前仓库**没有任何生产消费者**（只有 1 个测试引用）；旧能力面（pg8000/psycopg2/连接串四能力）同样零仓库内调用方 → **外部项目/V3 侧按旧能力 id 调用无法从本机判定** | **高（未知面）** | §2.1/§2.2 全仓 grep 结果；`未完成事项.md:114` 只到 HTTP 端到端 | 删除前向 V3 侧确认其调用的能力 id；若 V3 已按旧 id 接线，**先改成新 id 再删**（0008 决定二已定"不留兼容"，故不做重定向壳） |
| **R6** | **无法确定归属的两处**：① `运行核心/依赖防火墙.py:58-59` 与 `运行核心/环境指纹/环境指纹.py:29/:31/:32` 的 `pg8000/psycopg2` 条目删不删（口径问题）；② `示例项目/可双击演示/产物文件夹/**` 与 `工程缓存/制品仓库/**` 里的同名包是**历史快照**，删不删不影响源码（制品重编译后自然更新） | 低 | 实测 grep | ① 交主协调定口径；② 不动历史制品，仅重编译最新制品 |
| **R7** | **制品与发布门禁波动**：源删除 → 制品内容变（`客户端/构建平台客户端.py:30-31` 整树复制 `支持库`）→ `平台客户端制品` 指纹变化 → 发布门禁/制品对账需重跑 | 低（预期成本） | `工程缓存/制品仓库/平台客户端制品/平台客户端-e0e11e9801e44b7f/…/支持库/适配层/` 下实测存在三套 | 重编译最新制品并重跑 `测试中心/第一批维修/测试_客户端构建安全.py`（`:166` 数据库夹具门禁） |
| **R8** | **B3 处置未定**（§4.3 甲/乙） | 中 | 口径原文 vs J1/J3 字面存在解释空间 | **本批唯一需要华哥拍板的点**；两案回退成本均≈0 |
| **R9** | **C 未授权**（§4.6） | 中 | `53_…md:39` C2 项 | 另立授权批次 |
| **R10** | `测试中心/慢速层/第十三阶段/测试_数据库提供者.py` 源码不在树内、只剩 `.pyc`，而 `项目证据/验证历史.jsonl:242` 仍拿它当成功证据 | 低 | 实测 `find` | 证据回收（另批） |

---

## 8. 证据索引（命令 + 计数 + 哈希，全部当次实测）

**哈希与行数**

```bash
cd "~/Documents/Agent/PHP/系统工程平台"
for f in 支持库/适配层/{psycopg,pg8000,psycopg2}提供者/实现/提供者.py \
         支持库/后端/数据库连接支持库/{psycopg,pg8000,psycopg2,SQLite}数据库/实现/提供者.py \
         平台控制面/提供者/PostgreSQL提供者.py 平台控制面/提供者/数据库提供者.py; do
  echo "$(shasum -a 256 "$f" | cut -c1-16) $(wc -l < "$f") $f"; done
```

实测结果：

```
7f53a0de42d1bb5a   193  支持库/适配层/psycopg提供者/实现/提供者.py
ffc534c74cde2ffc   201  支持库/适配层/pg8000提供者/实现/提供者.py
0d8793c1a11b1e41   202  支持库/适配层/psycopg2提供者/实现/提供者.py
f064ccd1ff748e4c   371  支持库/后端/数据库连接支持库/psycopg数据库/实现/提供者.py
ffc534c74cde2ffc   201  支持库/后端/数据库连接支持库/pg8000数据库/实现/提供者.py
0d8793c1a11b1e41   202  支持库/后端/数据库连接支持库/psycopg2数据库/实现/提供者.py
4f03ca0bb8884a4f   132  支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py
4ccd4d07f9e7d85a   179  平台控制面/提供者/PostgreSQL提供者.py
d945b5d20c694758   485  平台控制面/提供者/数据库提供者.py
```

**整目录差异**

```bash
diff -rq --exclude=__pycache__ 支持库/适配层/pg8000提供者 支持库/后端/数据库连接支持库/pg8000数据库
diff -rq --exclude=__pycache__ 支持库/适配层/psycopg2提供者 支持库/后端/数据库连接支持库/psycopg2数据库
diff -rq --exclude=__pycache__ 支持库/适配层/psycopg提供者 支持库/后端/数据库连接支持库/psycopg数据库
```
实测：pg8000 → **13 项差异，全部是包壳元数据**（实现文件不在差异列表内 ⇒ 字节级相同），且 `Only in 支持库/适配层/pg8000提供者: 验证场景 / 验证夹具`；psycopg2 → 12 项差异（同为包壳）+ `Only in …/psycopg2数据库: 验证场景.json`；psycopg → 差异含 `实现/提供者.py`（本条即"旧版副本"证据）。

**引用面 grep（计数）**

```bash
EX="--exclude-dir=__pycache__ --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=.pytest_cache --exclude-dir=.ruff_cache --exclude-dir=.codegraph"
grep -rnIE $EX '适配层\.psycopg提供者'  . | wc -l    # 30 行 / 12 文件（含 3 处测试 + 1 处后端陈旧说明书）
grep -rnIE $EX '适配层\.pg8000提供者'   . | wc -l    # 30 行 / 13 文件
grep -rnIE $EX '适配层\.psycopg2提供者' . | wc -l    # 33 行 / 12 文件
grep -rnIE $EX '数据库连接支持库\.psycopg数据库'  . | wc -l   # 110 行（包内 103 + 包外 7：测试 1 + 文档 6）
grep -rnIE $EX '数据库连接支持库\.pg8000数据库'   . | wc -l   # 79 行（包外 2，全为文档）
grep -rnIE $EX '数据库连接支持库\.psycopg2数据库' . | wc -l   # 84 行（包外 5，全为 测试_项目锁传递闭包.py:177/179/180/191/192）
grep -rnIE $EX 'PostgreSQL提供者' .                  # 13 行 / 5 文件（代码 3 + 文档 10 行）
grep -rnIE $EX '创建数据库能力函数' .                # 4 行（定义 2 + 文档 2）—— 零调用方
```

**只读门禁基线（当次实跑）**

```bash
unset PYTHONPATH
python3.14 -m 开发工具.公开调用完整性门禁          # → 「公开调用完整性门禁通过：六环一致，无违规」，退出码 0
python3.14 开发工具/依赖生命周期审计/运行审计.py   # → 标准提供者 17 个（跳过 MySQL提供者/密钥提供者）；违规 1 个提供者 2 条（YAML提供者：缺健康探针、缺停止入口），退出码 1
python3.14 - <<'PY'
from pathlib import Path
from 开发工具.组件规范.完整性摘要 import 扫描正式包, 校验完整性摘要
根 = Path(".").resolve(); 包 = 扫描正式包(根); print("扫描正式包总数 =", len(包))
for p in sorted(包):
    if p.name in ("pg8000提供者","psycopg2提供者","psycopg提供者","psycopg数据库","pg8000数据库","psycopg2数据库","SQLite数据库"):
        print(p.relative_to(根), 校验完整性摘要(p))
PY
# → 扫描正式包总数 = 116
# → 7 个数据库相关包全部 (True, [])，摘要自闭合，无既有红项
```

**`支持库/适配层` 下含 `包声明.json` 的提供者数 = 17**（`find 支持库/适配层 -maxdepth 2 -name 包声明.json | wc -l`，等于上条"标准提供者 17 个"）。删 B1/B2 后 → **15**；若 B3 采（乙）→ **14**。**无任何测试断言这个数字**（全仓 grep `116|len(…包)==` 仅命中 `开发工具/快速编译/运行快速编译.py:161` 的无关用例）。

**`git status --porcelain` 关键行（开工条件依据）**

```
 M 支持库/后端/数据库连接支持库/psycopg数据库/实现/提供者.py
 M 支持库/后端/数据库连接支持库/psycopg数据库/能力定义.json
 M 支持库/后端/数据库连接支持库/psycopg数据库/权限契约/权限契约.json
 M 支持库/后端/数据库连接支持库/psycopg数据库/验证场景.json
 M 支持库/后端/数据库连接支持库/SQLite数据库/复用决策.json      ← 本清单 M2
?? 测试中心/支持库/测试_数据库连接池.py
```

---

## 9. 推荐执行顺序（一页版）

| 序 | 动作 | 风险 | 前置 |
|---|---|---|---|
| 0 | 提交/落定 A4 与 `测试_数据库连接池.py`（§4.0） | — | — |
| 1 | 跑一次 `测试_项目锁传递闭包` 记录基线（R2） | — | 0 |
| 2 | 新增"同一第三方发行包只能被一个包声明"审计（R4，防复发） | 低 | 0 |
| 3 | 删 B1、B2（§4.1）+ 同步 T1/T2 | 低 | 2 |
| 4 | 删 A2、A3（§4.2）+ M2、M3 + 同步 T4 | 低 | 3 |
| 5 | 删 D（§4.4）+ M1 | 低 | 4 |
| 6 | B3 按（甲-1 / 乙）处置（§4.3）+ 同步 T3 | 中（待拍板） | 5 |
| 7 | M7/M8/M9 口径清理 + M10 说明书纠正 + **每包重算摘要** | 低 | 6 |
| 8 | 重跑 3 大门禁 + 重编译制品 + 发布门禁 | 低 | 7 |
| 9 | C 与 SQLite 唯一化（§4.6） | 中 | 另立授权 |

---

*本文件为只读盘点产物，仅落盘 Markdown；第 4 节各项动作均需华哥/主协调单独授权后另行执行。*
