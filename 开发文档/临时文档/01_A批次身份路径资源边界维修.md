# A批次审计与维修工作台（先清理资源/任务，再修代码）

> 审计时间：2026-09-02 22:31:47 +0800。本文是当前任务工作台；最终有效结论须回写 `开发文档/项目说明.md`，已修复事项不在债务清单重复保留。

## 开工流程（已按华哥裁决收口）

```text
只读盘点当前任务/资源/进程/端口/临时目录
  → 处理过期任务、已知残留资源和未收敛终态
  → 将同类问题按“接口读取/子进程输出/HTTP线程/日志/发布制品”重新组合
  → 为真实失败补最小回归
  → 批量维修，不新增旁路身份限制或 Agent 前置门禁
  → 受影响定向测试
  → 当前 HEAD 重建制品
  → 真实 HTML/MCP POST 黑盒
  → 发布门禁与文档收口
```

## 审计已落盘的问题与落点

| 编号 | 级别 | 当前落点 | 状态 |
|---|---|---|---|
| A01 | 已修复 | `测试中心/MCP工具箱/测试_项目服务.py:128-139`、`MCP工具箱/临时上下文.py:77-84` | `./MCP工具箱` 现在规范化为 `MCP工具箱`，`..`/绝对路径仍拒绝；55/55 回归通过 |
| A02 | 阻断 | `工程缓存/制品仓库/平台客户端/制品来源.json`、`开发工具/发布门禁/运行发布门禁.py:345-395` | 制品完整但绑定旧提交/旧工作区指纹，不能证明当前 HEAD |
| A03 | 部分修复 | `公共契约/运行时/有界IO.py`、`支持库/适配层/模型HTTP提供者.py:47-48`、PyMuPDF/PDF/Pillow/密码签名/Git/textutil/LibreOffice Provider | 已接入公共有界读取/受限通信；模型 HTTP 和第一组 Provider 已验证；其他 HTTP/文件入口仍待处理 |
| A04 | 已修复 | `支持库/适配层/系统探针.py:94`、`支持库/后端/文档转换支持库/PDF渲染/实现/提供者.py:65-68`、`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:142,168`、`支持库/后端/文档转换支持库/LibreOffice转换/实现/文档转换.py:127` | 5处裸 communicate 全部改为受限通信；54/54 定向通过 |
| A05 | 已修复 | `MCP工具箱/能力网关.py:132`、`支持库/前端/浏览器宿主/实现/浏览器宿主.py:51`、`开发工具/轻代码前端编辑器/启动编辑器.py:403`、`公共契约/运行时/有界HTTP.py` | 3处服务统一使用公共有界线程服务器；相关回归12/12通过 |
| A06 | 部分修复 | `公共契约/运行时/有界IO.py`、`MCP工具箱/任务观测.py:19-22,65-83`、`MCP工具箱/项目服务.py:97-107`、`MCP工具箱/协作状态.py:130-159`、`MCP工具箱/使用反馈.py:19-82`、`运行核心/运行诊断/安全审计/安全审计.py:34-119` | JSONL 查询统一有界，任务观测/反馈/安全审计追加轮转；相关回归55/55通过；其他日志入口仍待接入 |
| A07 | 已修复 | `运行核心/统一网关/流式HTTP.py:42-63,126-130,220-226` | 单事件 256KB、累计 4MB；超限转结构化失败终态并停止生产；相关回归4/4通过 |
| A08 | 已修复 | `公共契约/句柄体系.py:68,114-117`、`支持库/后端/并发控制支持库/实现/并发控制.py:22`、`运行核心/加载器/版本系统/热切换.py:89` | 句柄绑定资源上限1000、回收证据上限1000；并发控制降级记录表、热切换回滚记录表上限1000；相关测试通过 |
| A09 | 已修复 | `运行核心/统一网关/流式HTTP.py:120-124` | 结束回调异常改为 logging.error 记录，不再静默吞掉 |
| A10 | 已修复 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:25-28,67-97,150-159` | psutil 缺失时返回 `资源预算未验证` 并拒绝新模型；降级记录最多1000条；相关回归5/5通过 |
| A11 | 重要 | `开发文档/项目说明.md:12-15`、`开发文档/未完成事项.md:8-53` | HEAD、包数量、制品和已修复债务均存在事实漂移 |

## 资源与任务清理验收口径

- 只清理本项目 `工程缓存/` 内、且能由任务/资源清单明确归属的过期资源；不删除 Downloads，不清理未知外部资源。
- 任务终态必须是成功、失败、取消、超时或崩溃之一；过期任务不能继续处于等待/运行/取消中。
- 任务、进程、线程、端口、句柄、临时目录、资源清单逐项对账；清理失败必须保留结构化证据，不能靠删除记录掩盖。
- 发现同类问题时按资源类别重新组合维修；不单点复制第二套限制，不把旁路身份校验混进本批。

## 发现的问题

A批次维修：包扫描事实源分叉、物料清单路径、工作区/任务/审计身份边界

详见 `开发文档/底座升级方案.md` 顶部当前批次章节。

## 解决方案

逐项补失败回归测试 → 最小修复 → 定向测试 → 下一项。

## 影响范围

- `MCP工具箱/发布治理.py`
- `MCP工具箱/工作区管理.py`
- `MCP工具箱/项目服务.py`
- `运行核心/任务调度/任务系统.py`
- `运行核心/运行诊断/安全审计/安全审计.py`
- `运行核心/加载器/包发现/发现器.py`
- `开发工具/项目编译/正式包索引.py`
- 对应 `测试中心/` 测试文件
- `开发文档/项目说明.md`

## 方向裁决

- 代码规范可以进入编译器/契约检查：变量、函数、文件命名、模块边界、接口字段和返回结构统一。
- 不新增旁路身份限制、Agent 前置读文档限制、额外任务状态机或 MCP 自我证明链；`AGENTS.md` 只是上下文，读取失败不能阻断接口执行。
- HTML/MCP/公开 HTTP 已有接口直接走真实 `GET/POST`，不再增加代理套娃或第二调用路径。

---
## 实际结果

- 修复 `MCP工具箱/发布治理.py` 的物料清单路径边界：规范化后拒绝绝对路径、`..` 和制品目录外路径。
- 将激活指针 CAS 的读指针、令牌校验、版本/令牌递增逻辑抽为 `_CAS切换激活指针`，正式 `切换激活指针` 继续负责前置校验、证据落盘和原子写回。
- 激活指针测试改为直接覆盖底层 CAS，并在连续切换场景模拟上层写回新指针，避免把无效摘要带入完整发布前置校验。
- 通过现有 `清理资源` 接口消费失效资源清单 `工程缓存/测试资源清单/41b84176c40e4ca4.jsonl`；外部目标已不存在，未删除外部文件。资源类型契约现已支持历史别名 `临时验证脚本` → `文件`，未知类型登记时拒绝；原清理失败证据 `工程缓存/测试临时/清理失败.json` 保留为历史证据。
- 核对并停止本项目历史残留进程：PID 3486 的 40001 字典服务、PID 6296 的旧 45111 平台客户端及其 52743/53574 监听；Git worktree 和端口复核无残留。
- 当前系统工程平台 14 个长期 `创建` 协作任务改为 `关闭/历史重组`，保留原始记录和失败/未完成事实；LLaVA-NeXT、PySceneDetect、ecompress 三个外部项目任务未改。
- 任务观测日志追加 104 个“历史任务重组关闭”终态；重新解析后最后事件为“任务开始”的任务数为 0，原始历史事件未改写。
- 修复临时上下文范围规范化：允许 `./目录`，继续拒绝 `..` 和绝对路径。
- 建立唯一公共有界读取器 `公共契约/运行时/有界IO.py`；发布门禁 `运行子进程` 改为边读边计数，超过 200KB 或超时均回收进程组；模型 HTTP 响应改为先限额再 JSON 解析。
- 建立唯一公共有界 HTTP 服务器 `公共契约/运行时/有界HTTP.py`，能力网关、浏览器宿主、轻代码编辑器统一复用；浏览器宿主包完整性摘要已用唯一生成器重算。

## 验证结果

- `python3.14 -m unittest 测试中心.MCP工具箱.测试_发布治理`：20 项通过，退出码 0。
- `python3.14 -m py_compile MCP工具箱/发布治理.py 测试中心/MCP工具箱/测试_发布治理.py`：退出码 0。
- `git diff --check`：退出码 0。
- 真实 MCP HTTP 黑盒：`POST http://127.0.0.1:8766/mcp/` 的 `initialize` 返回 200、`text/event-stream` 和 `mcp-session-id`；随后 `notifications/initialized` 返回 202，`tools/list` 返回 200 并返回真实工具目录；验证后服务已停止，8766 无残留监听。
- 治理资源/任务定向波次：`测试_工作区管理`、`测试_临时上下文`、`测试_工作区与清理`、`测试_工作区清理增强`、`测试_协作状态`、`测试_项目服务` 共 55/55 通过，退出码 0。
- 有界读取器与发布治理定向波次：公共有界 IO、发布治理共 23/23 通过，退出码 0；300KB 发布门禁超限输出实际返回退出码 -1 并回收进程组；模型 HTTP 正常/超限响应探针通过。
- 有界 HTTP 服务与摘要定向波次：浏览器宿主、能力网关、轻代码编辑器共 12/12 通过，退出码 0；浏览器宿主完整性摘要与当前源码重新对齐。
- JSONL 资源波次：公共有界 IO、任务观测、协作状态、项目服务共 53/53 通过，退出码 0；任务观测日志轮转与有界尾读实际接入。
- 安全审计/反馈日志波次：安全审计有界查询、反馈读写与任务观测共 55/55 通过，退出码 0。
- 流式 payload 波次：流式 HTTP 凭证/路由/生命周期与单事件、累计 payload 超限测试共 4/4 通过，退出码 0。
- Provider 受限通信波次：PyMuPDF、PDF 隔离、Pillow、密码签名、Git 共 103/103 通过；PyMuPDF/PDF 生命周期 30/30 在 `-W error::ResourceWarning` 下通过；LibreOffice/textutil 文档转换 14/14 通过，退出码均为 0。
- 模型资源预算波次：模型连接器内存探针缺失拒绝、降级记录有界、线程池释放共 5/5 通过，退出码 0；模型连接器摘要已重算。
- 句柄资源预算波次：句柄绑定资源上限、回收证据上限共 2/2 通过，退出码 0。

## 未完成事项

- 当前制品尚未按当前 HEAD 重新编译并重新完成 lineage、HTML/MCP POST 黑盒和发布门禁闭环。
- A03～A10 资源治理问题仍待按类别批量维修；本文件不作为底座整体完成证明。