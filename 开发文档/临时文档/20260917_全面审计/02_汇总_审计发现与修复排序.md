# 全面审计汇总与修复排序（2026-09-17）

> 第一轮=找全（10 路并行 + 5 路派生），第二轮=证真+排序，第三轮=一次修完。
> 判据：每条必须带 `路径:行号` + 现场命令 + 真实输出；分级只有 阻断/重要/建议。
> **父会话已亲自复核的关键结论**在下文标 `✅父已复核`。

## 〇、怎么审的（覆盖面）

| 路 | 域 | 交付 |
|---|---|---|
| D1 | `开发文档/` 74 份 md / 12,524 行 | 25 条（阻断 3/重要 13/建议 9） |
| D2 | `支持库/**/说明` 262 篇 / 5.1 万行全过 | 22 条（阻断 7/重要 11/建议 4），产物含 `/tmp/审计汇总_支持库说明262篇.md` |
| D3 | `模块库`+`平台控制面` 说明 77 篇 | 22 条（重要 13/建议 9） |
| D4 | 文档缺口（7 目录 247py/9md） | 应补清单 12 项 |
| D5 | 对外文档 + 架构图 | 8 条（阻断 3/重要 5）+ **4 张图已真渲染** |
| C1 | `运行核心/`+`公共契约/` | 11 条（重要 4/建议 7） |
| C2 | `平台控制面/`+`开发工具/` | 25 条（**绕过类阻断 7**/重要 14/建议 4） |
| C3 | `支持库/` 300py | 超时；其 4 路派生已交 13+10 条 |
| C4 | `模块库/`+`测试中心/` 195py | 21 条（**阻断 4**/重要 14/建议 3） |
| C5 | 小域合集 | 超时；派生路有产出 |

**关键规模数（现场实测）**：包 139 ｜ 能力 662（全局唯一，重复 0）｜ 制品目录 10 ｜ 全仓 286,494 文件 / 9.4G。

---

## 一、阻断级（必须修）

### A. 门禁可被绕过 —— 7 条（最严重：它让"绿"不可信）

| # | 缺陷 | 证据 | 父复核 |
|---|---|---|---|
| A1 | **发布门禁「第三方权限网络文件进程声明」恒绿**：查的是 `制品目录/支持库/适配层`，而真实制品的支持库在 `制品目录/平台客户端/支持库/…` → 取到空集即返回通过，还被计入"N/N 项强制门禁全过" | `开发工具/发布门禁/运行发布门禁.py:659`、`:747-750`、消费点 `:939-940` | ✅ 对真实制品根 `通过=True｜跳过（空集）`；对 `制品/平台客户端` `通过=False` 并报 3 条真问题 |
| A2 | **公开调用完整性门禁的「聚合父包」自证豁免**：加一个占位子目录即整包跳过六环 + 全局唯一检查 | `开发工具/公开调用完整性门禁.py:88-101`、`:137-138`、`:568-578` | 探针：加占位子目录后重复提供者从 1 条→0 条 |
| A3 | **制品签名两套排除表 → `校验签名` 恒 False，而 `入库` 仍报成功** | `包仓库/平台客户端制品.py:85`（缺 `制品完整性摘要.json`）vs `包仓库/签名能力.py:17-20`（含它） | 现场制品判 False、DB 35/35 条都含该文件 |
| A4 | **`安装制品` 调用点没传受控目录 → 任意已存在单层目录被整体替换删除** | `包仓库/安装能力.py:75`（`校验安装目标目录(目标目录)`）、`:106-122` | ✅ `:28` 签名有 `受控目录=None`、`:54-57` 传了才校验父目录；探针实测原始文件被删 |
| A5 | **文件租约互斥不成立**：`取存储` 每次 new 实例 → 锁随实例创建，check-then-act 无跨实例锁 | `能力目录/实现/租约边界.py:63-69`、`文件租约存储.py:57,89-91` | 探针：多线程双认领 2/120、跨进程 10/60 |
| A6 | **租约存储非原子写**：固定临时名 → 并发写坏 JSON | `文件租约存储.py:76-87` | 探针：损坏 40/120，损坏后 3/4 路回"存储目录不可用" |
| A7 | **需求确认闸门可绕过**：任意未登记需求 id 可被"确认" | `需求登记/需求服务.py:74`（`写入记录` 即 INSERT OR REPLACE）、`:39-47` | 探针：登记装配计划 → 确认需求 → `是否已确认=True` |
| A8 | **HTML 黑盒正向场景可写成恒真空气断言**（`预期包含` 匹配整包 JSON，"成功"是信封必现字段） | `HTML验证/返回判定.py:112`、`:98-109` | 探针：`_判定(任意成功信封) → True` |

### B. 测试假绿 —— 4 条（"绿"的另一个来源）

| # | 缺陷 | 证据 | 父复核 |
|---|---|---|---|
| B1 | **全仓唯一自动化测试门禁「只导入不执行」**：188 模块中 **16 个实跑即红**全被计为绿；且它**不在发布门禁内** | `开发工具/测试体系门禁.py:19-22`、`运行发布门禁.py:1726` | ✅ 原文确认 + 抽跑 3 个模块全 FAILED |
| B2 | **`测试伪装门禁`（唯一能识别自证测试的门禁）271 违约，也不在发布门禁内** | `运行发布门禁.py:12,1726`；`测试伪装门禁实现/豁免清单.json` 条目 0 | 报告：规则1 违规 121、规则2 违规 137 |
| B3 | **僵尸测试**：`测试_核心快照模板` 导入即失败（`清单文件名` 已改名为 `快照清单文件名`），登记项只记了它、没记根因 | `测试中心/开发工具/测试_核心快照模板.py:21-25` vs `组件规范支持库/__init__.py:87` | ✅ 我实跑 `errors=1` |
| B4 | **三个门禁守护测试长期真红**（公开调用完整性 / 依赖防火墙 / 聚合摘要收口）→ "守护链"已断 | `测试_公开调用完整性门禁.py:229`、`测试_依赖防火墙口径.py:87`、`测试_聚合摘要收口.py:18` | ✅ 我实跑依赖防火墙 `failures=1` |
| B5 | 6 个测试目录 **0 跟踪文件**（`git ls-files` = 0） | `测试中心/{第四阶段,权威状态,资源并发,结构迁移…}` | 现场 |

### C. 编排层掩盖失败 —— 4 条（会交付"假成品"）

| # | 缺陷 | 证据 |
|---|---|---|
| C1 | **`全自动精校` 把「分片转写部分失败 + 复核 0/1」过滤掉，照样交付标"已完成"的成品稿** | `模块库/直播逐字稿/实现/全自动精校.py:163-166`（不看 `状态`）、`:192-207`、`:229-238` |
| C2 | **质检门禁在底稿为空/极短时恒过**（长度比、数字保留率一律 1.0） | `质检门禁.py:111`、`:69-71`；探针：空底稿 → 通过=True |
| C3 | **主流程能力 `直播逐字稿.全自动精校` 在测试中心零覆盖**，测试仍全绿 | `测试_直播逐字稿.py:128,133` 写死 3 个能力 vs 能力定义 4 条 |
| C4 | 落盘/回收零失败检查（6 处写操作返回值全丢）→ 缓存目录没建也报"已完成" | `全自动精校.py:147,148,179,191,216,239` |

### D. 文档阻断 —— 3 条

| # | 缺陷 | 证据 |
|---|---|---|
| D1 | **《当前流程图》自述「不再维护 Mermaid」**，与"要架构图"的要求直接冲突；全仓自有文档 mermaid 块 0 个 | `开发文档/当前流程图.md:3` |
| D2 | **6 个聚合父包的 `设计说明.md` 是纯壳（7 行、正文 0 行）**，而 **100 篇使用说明把"设计权威"指向它** → 设计权威整体悬空 | `支持库/后端/{系统核心,大语言模型,数据操作,文件系统,网络通信,办公文档}支持库/说明/设计说明.md:1-7` |
| D3 | **24 篇 `目录树与命令.md` 的包id 写错**（11 篇指向别的包、13 篇缺层级段） | `OCR识别/OCR识别/说明/目录树与命令.md:3` 等 |
| D4 | **README 部署第 4 步照抄起不来**（命令没带凭证，网关 `要求凭证=真`） | `README.md:245` vs `本地网关.py:395-398` |
| D5 | **README 把「HTTP连接器不发凭证」当硬缺口**，而代码已提供该参数 | `README.md:65-67,191-194` vs `HTTP连接器.py:49-61,216-218` |

---

## 二、重要级（归并）

- **代码（C1/C2/C4）**：句柄表未加锁写（并发 `RuntimeError`）｜热切换「旧版本弃用期」恒空转（包id 只取首段 + `if False` 死代码）｜任务进程池异常路径把任务永久留队列｜超时残留账本名义有上限实为无界｜`创建并绑定` 丢弃 CAS 返回值｜`资源协调.提交` finally 三连无嵌套保护｜流式与本地网关对客户端身份口径不一致｜环境缓存「提交」非原子｜权威状态每线程连接只在关闭时收割。
- **测试（C4）**：漂移门禁 2 条真红（`系统信息` 契约与能力定义真漂移）｜组件生成 6 红（能力未注册 + 3 条安全负测失效）｜OCR 6 红｜3 处测试绕过包级入口直导实现层（反向破坏门禁 28 真红）｜测试越层改写生产常量 + 断言已废止的固定标记｜断点续跑指纹兜底空串 → 两个不同文件判同一指纹｜`产物存在()` fail-open｜疑难标记吞异常（0 疑难 0 死循环）｜开工编排把「调用器未装配」报成「项目根不存在」。
- **文档（D1-D3）**：`未完成事项.md` 自己与自己互斥（〇段已判可销项，P0 表仍列）｜规范里的长任务能力 id 不存在｜67 个能力的错误码表设计说明与契约不一致｜57 篇设计说明包版本滞后｜参数名被渲染成类型（52 行/14 篇）｜`=值` 占位示例 62 篇｜30 篇说明引用的能力 id 形态不合规｜13 篇说明缺标准包头。
- **对外（D5）**：README 目录表缺 `技能库/`｜项目说明「正式根名表」5 根 vs 代码 6 根｜`HTTP路由契约.json` 缺两条现役路径。

## 三、建议级（归并）

文档数字过期（制品份数/容量/能力数）｜决策记录重号缺号（0008/0009 各两篇、无 0010）｜临时文档遗留（20260915_MCP收敛 13 份）｜归档里"待下一批"的项在债务清单 0 命中｜15 处 `try/except: pass`｜夹具脆弱性｜完整性摘要对 `工程缓存/**` 与 `*.pyc` 不敏感｜`校验签名` 不校验记录里的包id/版本｜契约编译按文件名跳过契约不进问题清单。

## 四、不是缺陷（设计取舍，不要改）

- 单机私有部署、相对安全、能力全经唯一网关 —— 既定决策。
- 进程回收**按登记归属 PID/进程组**，**未见按端口盲杀**（正面结论，已复核）。
- 凭证边界实测 fail-closed（大小写敏感、空串拒绝、`bearer` 小写不通过）。
- 41 篇人工血统设计说明无版本行（体例差异，非错误）。
- `支持库/适配层/MySQL提供者` 自述"未实现、未注册"，是占位。
- `目录树与命令.md` 只列文件不单列目录（口径歧义）。

---

## 五、第三轮修复批次（唯一约束：不改同一个文件）

**批 0（已验证，我复核过，最优先）**：A1 门禁层级｜A4 受控目录｜B1/B2 门禁接入发布门禁｜B3 改名同步｜B4 三条守护测试。
**批 1（门禁/契约层）**：A2 A3 A6 A7 A8（各一文件）｜C2 契约编译严格化｜"摘要对 `工程缓存`/`pyc` 不敏感"。
**批 2（租约/并发层）**：A5（存储实例缓存 + flock）｜C1 句柄表加锁｜C1 热切换包id｜C1 任务队列异常回滚｜C1 CAS 返回值｜C1 finally 嵌套保护。
**批 3（编排层）**：C1-C4（`全自动精校`/`质检门禁`/落盘返回值/`缓存与续跑`/`疑难标记`/`开工编排`）。
**批 4（文档）**：D1 流程图口径 + 补图｜D2 六篇设计说明回填｜D3 24 篇包id｜D4/D5 README｜`未完成事项` 自洽｜规范里失效 id｜治理方案回填数字。
**批 5（文档缺口）**：`规范/验证场景编写规范.md`｜`测试中心/说明/使用说明.md`｜`运行核心/加载器/说明`｜`任务调度/说明`｜`公共契约/说明/契约总表`｜`项目适配层/说明`｜`架构总览.md` 进文档地图（我做的，我补）。

**已验证过的探针脚本**（可复跑）：C2 路 `/tmp/审计探针_*.py`、`/tmp/audit1-5.py`、`/tmp/探针_门禁绕过*.py`；C1 路 `/tmp/探针_句柄竞态3.py`、`/tmp/探针_热切换*.py`、`/tmp/探针_超时残留上限.py`；C4 路 `/tmp/审计_编排层*.py`、`/tmp/质检空底稿.py`。
