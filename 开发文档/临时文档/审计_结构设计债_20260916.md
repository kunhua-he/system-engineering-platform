# 结构性设计债审计（2026-09-16）

> 审计范围：顶层目录定位与内容是否相符、各层 `工具/` 类目录的层归属、同功能多实现（重复实现）、
> 「同一功能两条调用路径」的第二入口。**只读审计**，未修改任何项目文件（临时脚本在 `/tmp/`，仅本文件落盘）。
> 对应哲学条款：第 1 条（干净底座与唯一化）、第 2 条（分层与依赖归属）、第 4 条（发现即记录）、第 8 条（能力唯一性）。
> 取证环境：`export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH`，`/opt/homebrew/bin/python3.14`。
> 说明：本机**无 `timeout`/`gtimeout`**，限时只写在 Python 里（本次审计因此有一处命令超时，已改窄重跑）。
> 与在途改动的关系：`支持库/适配层/密码签名提供者/**`、`测试中心/支持库/测试_密码签名提供者.py`、
> `示例项目/适配层示例/依赖锁定.json` 为其他会话在途改动（`git status` 实测），**本报告不作为问题**。

---

## 一、顶层目录定位对照表（含「说不清」的）

现场 17 个顶层目录（实测文件数已排除 `__pycache__`）：

| 顶层目录 | 声明定位（来源） | 实测内容 | 是否相符 |
|---|---|---|---|
| `公共契约/` | README：包声明、能力契约、基础类型、错误结构、生命周期、版本规则 | 29 文件/9 目录；**另有 `句柄体系.py`（375 行含 threading/signal 的状态机实现）** | ⚠ 不符：契约层承载有状态治理实现（见 §5-Ⅸ） |
| `支持库/` | README：后端原子支持库、前端描述模型、适配层（第三方/系统/外部协议边界） | 1815 文件/828 目录；含 101 个包（后端 24 库 + 前端 9 + 适配层 17 + 散装 19 项） | ⚠ 部分不符：`支持库/核心/` 只剩 `__init__.py`（空壳层）；`适配层/` 根混放（见 §5-Ⅵ、§2） |
| `模块库/` | README：只组合支持库完成功能的样板模块 | 341 文件/167 目录；18 包，含 `能力目录`/`测试资源`/`组件生成`/`自修复工具` 4 个**平台自身工具型模块** | ⚠ 定位说不清：平台自修复/能力检索属治理，放在「业务组合模块」层 |
| `技能库/` | `公共契约/正式根.py` 的第三正式根；`项目说明.md:535` 有定义 | 49 文件/25 目录；1 个包（`技能库/后端/技能库`） | ⚠ **不在 `README.md` 目录表、不在 `项目说明.md` 目录表** → 正式根未登记到任何顶层目录对照表（见 §5-Ⅶ） |
| `平台控制面/` | README：需求登记、能力目录、包仓库、策略中心、发布管理、证据账本（唯一写权威） | 222 文件/79 目录；根下 **6 个平铺 .py**（授权/核心快照/策略中心/统一入口/资源监督/需求登记）+ 11 包 | ⚠ 不符：6 个权威服务只有 3 个补成正式包（包仓库/发布管理/能力目录），另 3 个仍平铺；`授权.py`+`授权/`、`统一入口.py`+`统一入口/` **文件与目录同名并存**（见 §5-Ⅲ） |
| `运行核心/` | README：加载器、任务调度、资源协调、能力调用、统一网关、运行诊断、依赖防火墙、运行环境管理器、环境指纹 | 61 文件/22 目录；根下 5 个平铺 .py（依赖防火墙/句柄体系 2 行壳/启动运行核心网关/权威状态 1123 行/资源id编码/进程身份） | ⚠ 部分不符：`句柄体系.py` 只是 `from 公共契约.句柄体系 import *` 的 2 行转发壳 |
| `开发工具/` | README：统一能力入口、能力搜索、说明书生成、契约编译、组件规范、组件合规、复用审计、发布门禁、统一验证（9 项） | 111 文件/24 目录；实际 **28 个子项**，根下另平铺 6 个 .py（1323 行） | ⚠ **不符**：README 只列 9 项，实测多出 16 项（HTML验证/依赖生命周期审计/快速编译/直播逐字稿工具/能力网关/薄壳/轻代码前端编辑器/项目编译/测试伪装门禁/测试体系门禁/公开调用完整性门禁/工程缓存回收/开发入口/更新完整性摘要…）→ 已成杂物间（见 §2） |
| `测试中心/` | README：独立测试环境（unittest），唯一测试代码位置 | 189 文件/37 目录；含 `测试中心/开发工具/`、`测试中心/MCP工具箱/` 两个**镜像命名**目录 | ⚠ 命名指向：`测试中心/MCP工具箱/` 的镜像目标顶层 `MCP工具箱/` 已整目录删除 |
| `项目适配层/` | README：项目声明、配置合并、依赖锁定、模块绑定、运行入口 | 27 文件/11 目录 | 相符 |
| `示例项目/` | README：最小运行样板 | 25 文件/14 目录；6 个子项（前后端核心示例/可双击演示/平台控制面示例/稳定平台完整示例/适配层示例/示例项目.py） | 相符（`可双击演示` 带 `开发文件夹/前端IDE/启动.py`，职责偏演示而非示例，轻微） |
| `客户端/` | README：平台客户端制品构建脚本 | **1 个文件**（`构建平台客户端.py` 642 行） | ⚠ 定位说不清：整个顶层目录只为 1 个构建脚本存在（见 §5-Ⅷ） |
| `运维脚本/` | **全仓 0 处定位文档**（`grep -rn 运维脚本 README.md 开发文档/项目说明.md 开发文档/主开发文档.md` 命中 0） | **1 个文件**（`热接入.py` 127 行） | ⚠ 无定位、无归属：为何单开顶层目录、与 `开发工具/` 的边界均未写 |
| `启动监督器/` | README：核心启动、资源限制、健康检查、版本切换与核心回退 | 2 文件（健康监督 587 / 容量基线 166） | ⚠ 定位说不清：与 `运行核心/` 无书面边界（`健康监督.py` 直依赖 `项目适配层/配置适配`，属运行宿主职责） |
| `前端核心/` | README：窗口、页面、组件、状态、事件、路由和渲染宿主 | 3 文件（前端核心 150/浏览器交互 240/渲染提供者 68） | ⚠ 与 `支持库/前端/*`（9 包：窗口描述/状态描述/组件控件/浏览器宿主/桌面宿主/资源描述/文件选择描述/基础组件描述）**职责重叠**，同一批概念（窗口/状态/组件/宿主）分散两层 |
| `后端核心/` | README：后端执行单元、请求上下文、能力执行和宿主级生命周期 | 1 文件（768 行） | 相符（单文件宿主，属已知形态） |
| `开发文档/` | README：当前版本说明、决策记录与参考资料 | 248 文件 | 相符 |
| `工程缓存/` | README：生成物（可删除） | 22.5 万文件 | 相符（职责边界清楚：一次性生成物） |

**结论**：「定位说不清」或「与内容不符」的顶层目录共 **9 个**（公共契约/支持库/模块库/技能库/平台控制面/运行核心/开发工具/客户端/运维脚本/启动监督器/前端核心，其中技能库=未登记、运维脚本=无定位）；定位清晰的 6 个（项目适配层/示例项目/后端核心/开发文档/工程缓存/测试中心）。

---

## 二、工具目录普查

命令：`find . -type d \( -name "*工具*" \) -not -path "./工程缓存/*" -not -path "./.git/*"`

| 目录 | 层归属判断 | 是否符合该层约束 |
|---|---|---|
| `开发工具/`（顶层） | 终端/开发工具层（非正式根、非生成物） | ⚠ **部分不符**：28 个子项无二级分层规范。门禁类散落——`公开调用完整性门禁.py`/`测试伪装门禁.py`/`测试体系门禁.py` 平铺在根，`发布门禁/` 是子目录；编译类同时有 `契约编译/`、`快速编译/`、`项目编译/`、`薄壳/`；构建类另有 `客户端/` 与 `运维脚本/` |
| `开发工具/直播逐字稿工具/` | 模块自带运维 CLI 的**归位落点**（已按「模块库禁网络/子进程」从 `模块库/直播逐字稿/工具/` 迁出） | ✅ 归位本身合规；⚠ **但缺归口层**：目录名把「某个模块」写进工具层，再来 3 个模块的运维脚本就会再长出 3 个同风格目录（建议归口 `开发工具/模块运维/<模块名>/`） |
| `测试中心/开发工具/` | 测试侧镜像（21 个 `测试_*.py`） | ⚠ 命名让「开发工具」一词指两层（顶层工具层 vs 测试侧测试集），且与顶层目录同名易误判归属 |
| `测试中心/MCP工具箱/` | 测试侧镜像（2 个 `测试_*.py`） | ⚠ 镜像的顶层 `MCP工具箱/` 已删除（2026-09-15 D 清场）→ 命名指向不存在的层 |
| `模块库/自修复工具/` | 模块（正式包，4 能力） | ⚠ 名称含「工具」但实为模块；且自修复属平台治理，见 §一 模块库行 |
| `支持库/后端/系统核心支持库/工具执行/` | 原子能力子域（8 能力） | ✅ 符合 |

**结论**：`模块库`/`支持库`/`技能库` 三个正式根内**已无 `工具/` 目录**（已知那两处运维 CLI 归位问题已收口，本次复核确认）。剩余问题集中在**工具层的分层缺失**（`开发工具/` 无二级规范）与**测试侧镜像命名指向已删目录**。

---

## 三、重复实现清单（含被谁用）

### 3.1 逐字节相同文件（全仓 `sha256` 实测，8 组）

命令：`find . -type f -name "*.py" -not -path "./工程缓存/*" -not -name "__init__.py" -exec shasum -a 256 {} \; | sort | awk 'uniq -d'`

| # | 文件（逐字节相同） | 归属两条腿 |
|---|---|---|
| 1 | `实现/子进程解析.py` | `支持库/后端/图像处理支持库/图像解码/` = `支持库/适配层/Pillow提供者/` |
| 2 | `实现/临时文件.py` | `支持库/后端/OCR识别支持库/OCR识别/` = `支持库/适配层/Tesseract提供者/` |
| 3 | `实现/受管进程.py` | 同上（OCR/Tesseract 第二处） |
| 4 | `实现/子进程解析.py` | `支持库/后端/文档转换支持库/PDF渲染/` = `支持库/适配层/PyMuPDF提供者/` |
| 5 | `实现/子进程解析.py` | `支持库/后端/文档转换支持库/PDF隔离提供者/` = `支持库/适配层/PDF隔离提供者/` |
| 6 | `实现/PDF文本表格.py` | `支持库/后端/文档转换支持库/PDF文本表格/` = `支持库/适配层/pdfplumber提供者/` |
| 7 | `实现/渲染表格.py` | `支持库/后端/文档转换支持库/PDF生成/` = `支持库/适配层/reportlab提供者/` |
| 8 | `实现/文本转换.py` | `支持库/后端/文档转换支持库/textutil转换/` = `支持库/适配层/textutil提供者/` |

> 任务信线索给的是「5 对」；全量重算为 **8 组**（覆盖 7 对包；OCR/Tesseract 一对贡献同 2 组）。

### 3.2 同功能两套完整实现（12 对包，能力 id 也两套）

不只是「文件相同」，而是**每个第三方都有两个正式包、两套能力 id、同时被装配**（实测 611 条能力条目**无重复 id** → 两套 id 各自独立，都算「唯一」）：

| # | 后端腿（能力 id 前缀） | 适配层腿（能力 id 前缀） | 逐字节相同文件数 | 后端腿是否直接 import 第三方 |
|---|---|---|---|---|
| 1 | `媒体处理支持库.FFmpeg媒体.*` (6) | `FFmpeg媒体.*` (5) | 3（说明/预算/配置契约） | 否（外部命令） |
| 2 | `OCR识别支持库.OCR识别.*` (3) | `OCR识别.*` (3) | 6 | 否（外部命令 tesseract） |
| 3 | `图像处理支持库.图像解码.*` (9) | `图像解码.*` (9) | 4 | **是** `from PIL` |
| 4 | `文档转换支持库.PDF渲染.*` (4) | `PDF渲染.*` (4) | 5 | **是** `import fitz` |
| 5 | `文档转换支持库.PDF文本表格.*` (2) | `PDF文本表格.*` (2) | 5 | 否（子进程隔离） |
| 6 | `文档转换支持库.PDF生成.*` (1) | `PDF生成.*` (1) | 3 | **是** `from reportlab...` |
| 7 | `文档转换支持库.PDF隔离提供者.*` (3) | `PDF隔离提供者.*` (3) | 5 | 否（子进程隔离） |
| 8 | `文档转换支持库.textutil转换.*` (2) | `textutil转换.*` (2) | 4 | 否（外部命令） |
| 9 | `文档转换支持库.LibreOffice转换.*` (2) | `LibreOffice转换.*` (2) | 0 | 否（外部命令） |
| 10 | `版本控制支持库.Git操作.*` (13) | `Git操作.*` (9) | 19（含夹具） | 否（外部命令 git） |
| 11 | `转写支持库.转写.*` (3) | `转写.*` (3) | 3 | **是** `import mlx_whisper` |
| 12 | `浏览器自动化支持库.*` (7) | `浏览器自动化.检查可用` (1) | 0 | 否（后端腿改为 import 适配层，见下） |

### 3.3 谁在用哪条腿（精确 id 引用实测，排除测试/文档/自身）

对 17 个适配层包逐能力 id 做**带词边界**的精确匹配（`(?<![.\w])<id>(?![\w])`，避免被长 id 子串误命中）：

- **16 / 17 个适配层提供者包：零代码引用**（`FFmpeg提供者`/`Git提供者`/`LibreOffice提供者`/`MLXWhisper提供者`/`Pillow提供者`/`PyMuPDF提供者`/`Tesseract提供者`/`pdfplumber提供者`/`reportlab提供者`/`textutil提供者`/`TreeSitter提供者`/`YAML提供者`/`psycopg提供者`/`MCP协议提供者`/`浏览器自动化提供者`/`密码签名提供者`）。
- **唯一例外**：`支持库/适配层/PDF隔离提供者` 的 `PDF隔离提供者.校验PDF` 被 `支持库/后端/办公文档支持库/文档生成/实现/签名校验.py:17,80` 引用。
- **后端腿才是被消费的那条**：
  - 模块库调用方：`模块库/OCR/实现/OCR.py` → `OCR识别支持库.OCR识别.*`；`模块库/图像处理/实现/图像处理.py` → `图像处理支持库.图像解码.*`（11 处）；`模块库/媒体处理/实现/媒体处理.py` → `媒体处理支持库.FFmpeg媒体.*`（5 处）；`模块库/媒体转写/实现/媒体转写.py` + `模块库/直播逐字稿/实现/{直播逐字稿,全自动精校,分片转写,复核调度}.py` → `转写支持库.转写.*`；`模块库/自修复工具/实现/自修复工具.py` + `模块库/开工编排/实现/开工编排.py` → `版本控制支持库.Git操作.*`。
  - 支持库内部门面：`办公文档支持库/{文字文档,表格文档,演示文稿,文档生成}` → `文档转换支持库.{LibreOffice转换,PDF文本表格,PDF生成,PDF隔离提供者}.*`。

### 3.4 其他同功能多实现

| 功能 | 实现点 | 被谁用 |
|---|---|---|
| **能力检索（4 条路径）** | ① `模块库/能力目录`（`能力目录.搜索能力`/`读取能力`，自称唯一，14 字段）② `开发工具/能力搜索/能力搜索器.py`（14 字段，带原因）③ `开发工具/开发入口.py::搜索能力`（5 字段，裸读 `包声明.json`）④ `开发工具/统一能力入口/Agent查询/查询入口.py`（又 `from 开发工具.能力搜索.能力搜索器 import 搜索带原因`） | ① 薄壳 `capability_search` 转发目标；② 测试 + ④ 内部复用；③ Agent 开发闭环 |
| **说明书生成（2 个同名文件）** | ① `开发工具/组件规范/说明书生成器.py`（147 行，读 能力定义+参数契约+搜索数据+验证场景引用 → 写 `<输出根>/说明/使用说明.md`）② `开发工具/说明书生成/说明书生成器.py`（154 行，读 包声明+唯一聚合契约 → 机器可读说明书） | ① 仅 `测试中心/开发工具/测试_说明书生成器.py`；② `开发工具/说明书生成/运行生成.py` |
| **完整性摘要写入（4 处，与第 2 路有交集）** | ① `开发工具/组件规范/完整性摘要.py::生成完整性摘要`（唯一实现）② `开发工具/说明书生成/完整性摘要生成器.py`（转发①）③ `开发工具/更新完整性摘要.py`（**62 行，只改 hash 不重算清单结构，全仓零引用**）④ `开发工具/组件规范/组件规范.py:90`+`契约编译/能力定义编译器.py:457`（别名 `生成文件清单摘要`） | ① 被门禁/合规/编译器广泛引用；③ **零调用方** |
| **命令行审计入口（2 个同名 `运行审计.py`）** | `开发工具/复用审计/运行审计.py`（33 行，能力调用图/原子旁路）+ `开发工具/依赖生命周期审计/运行审计.py`（70 行，依赖锁/健康探针） | 各自目录内，同名易误判「同一入口」 |
| **同名包（跨层）** | `模块库/能力目录` ↔ `平台控制面/能力目录`；`公共契约/句柄体系.py` ↔ `运行核心/句柄体系.py`；`公共契约/生命周期/生命周期.py` ↔ `平台控制面/包仓库/生命周期.py`；`支持库/适配层/模型服务.py` ↔ `支持库/适配层/模型服务/`（**目录 0 文件**） | 见 §4、§5 |
| **顶层目录内同名文件+目录并存** | `平台控制面/授权.py`(189) + `平台控制面/授权/`(正式包)；`平台控制面/统一入口.py`(527) + `平台控制面/统一入口/`(正式包) | 两者互引：`授权/实现/能力入口.py:22` → `from 平台控制面.授权 import 取共享授权服务`；`统一入口/实现/能力入口.py:23` → `from 平台控制面.统一入口 import 统一能力服务` |

---

## 四、第二入口清单

| # | 功能 | 入口 A | 入口 B | 实测证据 | 是否已收口 |
|---|---|---|---|---|---|
| 1 | 调用能力 | 内部 `唯一能力调用服务.调用能力(能力id,参数)` | HTTP `POST /网关/调用`；MCP 薄壳 3 工具 | `开发工具/薄壳/工具清单.py` 只声明「转发操作=调用能力」到网关 | ✅ 合规（薄壳零业务语义，符合「唯一调用腿」） |
| 2 | 能力检索/契约查询 | 模块能力 `能力目录.搜索能力`（经网关） | 3 条本地直算路径（`能力搜索器.py`、`开发入口.py::搜索能力`、`Agent查询/查询入口.py`） | `模块库/能力目录/说明/设计说明.md` 自述「真实检索由三份并行实现各自完成」；`MCP工具箱/公开能力.py` 已删，另**两份未删** | ❌ **未收口**（4 条并存） |
| 3 | 说明书生成 | `说明书生成/说明书生成器.py` | `组件规范/说明书生成器.py` | 两文件同名、数据源/输出风格/输出文件名均不同 | ❌ 未收口 |
| 4 | 完整性摘要写入 | `组件规范/完整性摘要.py::生成完整性摘要` | `开发工具/更新完整性摘要.py`（旁路改 hash） | 后者全仓零引用，且不重算清单结构 | ❌ 未收口（残留死入口） |
| 5 | 模块脚手板 | `组件规范/模块模板生成器.py` | `开发入口.py::创建模块模板` | `开发入口.py:166-180` **已是薄委托**（`from 开发工具.组件规范.模块模板生成器 import 生成模块模板`） | ✅ **已收口**（线索所指的第二入口现已消失，实测确认） |
| 6 | PDF 隔离提供者 | `支持库/后端/文档转换支持库/PDF隔离提供者`（长 id） | `支持库/适配层/PDF隔离提供者`（短 id） | 同一提供者两个包：`办公文档支持库/PDF文档/实现/PDF文档.py` 调 `文档转换支持库.PDF隔离提供者.解析PDF`；`办公文档支持库/文档生成/实现/签名校验.py:17` 调 `PDF隔离提供者.校验PDF` | ❌ 未收口（**两条腿被两个调用点各接一条**，是「一功能两入口」的活证据） |
| 7 | 能力发现网关 | `开发工具/能力网关/能力网关.py` + `启动能力网关.sh`（只读，40006） | 文档另写 `MCP工具箱/能力网关.py`（默认 8866，已删除） | `测试中心/MCP工具箱/测试_能力网关.py` 是唯一残留 | ❌ 文档双口径 + 一处指向已删目录 |
| 8 | 热接入 | `运维脚本/热接入.py`（对 40007 做增量装配） | `开发工具/HTML验证/热接入回归.py`（热接入回归验证） | 二者一为执行、一为回归，非严格重复；但顶层 `运维脚本/` 仅为它存在 | ⚠ 边界未书面化 |
| 9 | 句柄体系 | `公共契约/句柄体系.py`（实现） | `运行核心/句柄体系.py`（`import *` 转发） | `运行核心/句柄体系.py` 全文 2 行 | ❌ 同一名字两层，实现位置与层定位相反 |
| 10 | 平台治理入口 | `平台控制面/统一入口.py`（12 稳定操作） | `平台控制面/统一入口/`（正式包，2 能力，按文件路径接续导出） | 包 `复用决策.json` 自述「保留 `统一入口.py` 为唯一实现，本包入口按文件路径接续再导出，不产生第二份实现」 | ⚠ 设计上声明为「非第二份实现」，但**同目录同名 `.py` 与包并存**是结构性绕行（见 §5-Ⅲ） |

---

## 五、结构债清单（按危害排序）

| # | 结构债 | 位置 | 会导致什么新问题 | 修法方向 | 影响面 |
|---|---|---|---|---|---|
| Ⅰ | **每个第三方两套完整实现、两套能力 id 同时在线，且后端腿直接 import 第三方（违「第三方只能进适配层」），适配层腿 16/17 包零代码引用** | `支持库/后端/{媒体处理,OCR识别,图像处理,文档转换,版本控制,转写,浏览器自动化}支持库/*` ↔ `支持库/适配层/*提供者`（12 对） | ① 改一个提供者要改两处，漂移即两套行为；② 新人/新会话按 id 前缀选腿，会选到违分层的那条；③ 适配层腿是僵尸包：门禁/摘要/验证场景全在建，却无人调用，**假绿风险持续累积**；④ 后端腿 `import fitz/PIL/reportlab/mlx_whisper` 使「核心只允许标准库」这条铁律在事实层面已被打破 | 按第 4 条收口：**保留适配层提供者为唯一实现**（它符合分层），后端成对包降为门面（实现改经调用器调短 id）或直接删包并把 8 个模块的调用 id 改短；两套 id 合并为一套；`浏览器自动化支持库` 已是「后端 import 适配层」的门面形态，可作为其余 11 对的样板 | 13 对包 ≈ 支持库包数 20%；联动 `模块库/{OCR,图像处理,媒体处理,媒体转写,文档解析,文档生成,自修复工具,开工编排,直播逐字稿}` 9 个包；`验证场景引用.json`/说明书/`能力搜索数据.json` 全量重生成；`完整性摘要` 级联 |
| Ⅱ | **能力检索 4 条路径并存 + 能力目录同名跨层** | `模块库/能力目录`、`开发工具/能力搜索/能力搜索器.py`、`开发工具/开发入口.py::搜索能力`、`开发工具/统一能力入口/Agent查询/查询入口.py`；`平台控制面/能力目录` | ① 四套字段口径（14/14/5/14）分别演进，调用方看到的「能力清单」不一致；② 新模块建设员照旧路径直读 `包声明.json`，绕过唯一能力；③ 同名包使 id 前缀判断失效（薄壳侧用不带根前缀的 `能力目录.搜索能力`） | 保留「最全面 + 归因」两处：`模块库/能力目录`（唯一对外）+ 薄壳转发；删 `开发入口.py::搜索能力` 的本地扫描实现（改为经能力调用）；`能力搜索器.py` 降为测试辅助或清掉；`Agent查询/查询入口.py` 改为经能力调用而非 import 搜索器 | 4 个文件 ≈ 800 行；薄壳 `工具清单.py` 不用改（已转发）；`测试中心/开发工具/测试_契约编译搜索.py` 直接 import 搜索器需同步 |
| Ⅲ | **平台控制面同目录「文件 + 同名包」并存，且 6 个权威服务只有 3 个包化** | `平台控制面/授权.py`+`授权/`、`统一入口.py`+`统一入口/`；`策略中心.py`/`需求登记.py`/`资源监督.py`/`核心快照.py` 仍平铺 | ① `from 平台控制面.授权 import X` 的解析取决于包优先于模块这一隐式顺序；一旦有人把包改名/移动，**静默命中另一边**、无报错；② 治理面「一半在包体系内、一半在外」→ 门禁/装配/摘要只覆盖一半，新增第 7 个服务时无处安放；③ `平台控制面` 已是 5 个正式根之一，平铺文件不受任何包级门禁约束 | 把 `授权.py`/`统一入口.py` 的真实实现移进对应包的 `实现/`（或删除同名文件、由包内实现承载），彻底消除同名并存；把 `策略中心.py`/`需求登记.py`/`资源监督.py`/`核心快照.py` 按 `包仓库/发布管理/能力目录` 的既有形态补成正式包（每包至少 1 能力 + 验证场景） | 约 6 个文件 ≈ 2200 行；`平台控制面/__init__.py` 的导出面；装配能力数（554 → +N）；`完整性摘要` 重算 |
| Ⅳ | **`项目说明.md`《能做什么》表 3 行路径已失效（指向已删除的 `MCP工具箱/`），且只读网关端口双口径（8866/40006）** | `开发文档/项目说明.md:326,328,522` | ① 后续会话照抄命令 → `no such file`；② 8866 与 40006 两个端口口径并存，真正常驻的是 40006；③ 《唯一事实源》文件本身失准，会连带降低整份口径表的可信度 | 逐行对照现场改写：删除 `MCP工具箱/` 引用，改指 `开发工具/能力网关/能力网关.py` + `启动能力网关.sh`；端口口径统一到 40006；`测试中心/MCP工具箱/` 目录改名（如 `测试中心/能力网关/`）以免命名指向已删层 | 3 行文档 + 1 个测试目录改名（2 个文件）；与第 1 路（口径漂移）**有交集，建议合并处理** |
| Ⅴ | **一个功能两条入口的活证据：PDF 隔离提供者的两条腿被两个调用点各接一条** | `办公文档支持库/PDF文档/实现/PDF文档.py`（长 id）vs `办公文档支持库/文档生成/实现/签名校验.py:17`（短 id） | ① 同一 PDF 解析语义走两个包，修复只修一边时「解析对了、校验还是老的」；② 让「能力 id 全局唯一」看起来成立（611 条无重复），实际同一实现有两条门 | 与 Ⅰ 同批处理：统一短 id，`签名校验.py` 与 `PDF文档.py` 指向同一包 | 2 个文件 + Ⅰ 的影响面 |
| Ⅵ | **`支持库/适配层/` 根目录 19 项非包内容与 17 个正式包平铺混放，含 0 文件空目录** | `支持库/适配层/{HTTP服务适配器,MySQL提供者,动态库适配器,密钥提供者,提供者注册表,提供者配置,数据库适配器,本地进程适配器,模型服务/,配置契约,配置安全,配置读取}` + `{密码适配,模型HTTP提供者,模型服务,系统探针,脱敏模式,适配契约}.py` | ① 无 `包声明.json` → 既不被装配、也不被门禁/摘要看见，成了「看不见的一层」；② 哲学第 10 条要求的「第三方翻译层/环境依赖层两段」在该目录**一段都没落地**；③ `模型服务/` 是 **0 文件目录**，与 `模型服务.py` 同名并存；④ `MySQL提供者/` 仅 1 个非代码文件 | 按第 10 条把适配层切成「第三方翻译层 / 环境依赖层」两段并落目录；19 项各自归位（能包化的补 `包声明.json`+能力；纯辅助模块下沉到有归属的子目录）；删 `模型服务/` 空目录 | 19 项 + 6 个散装 .py；涉及 `启动监督器/健康监督.py` 对 `支持库.适配层.系统探针` 的引用；`运维脚本/热接入.py` 对网关的引用不受影响 |
| Ⅶ | **`技能库/`（第三正式根）未登记在任何顶层目录对照表；`支持库/核心/` 空壳层仍被文档当现场；`运维脚本/` 无定位** | `README.md` 目录表缺 `技能库/`；`支持库/核心/` 只有 `__init__.py`；`开发文档/底座升级方案.md:366,386,409,412,489,558` 仍写 `支持库/核心/类型转换` | ① 新会话读 README 建立的心智模型里没有技能库 → 会把「技能」塞进模块库；② 按 `底座升级方案.md` 跑 `--包目录 支持库/核心/类型转换` 直接失败；③ `运维脚本/` 无文档，下一个人不知道能不能往这里加脚本 | README 目录表补 `技能库/` 与 `运维脚本/`（写明它是「跨层运维脚本」还是「构建宿主」）；删 `支持库/核心/` 空目录并把 `底座升级方案.md` 的历史路径标注为「已迁移至 `支持库/后端/<域>`」 | 1 份 README 表 + 1 个空目录 + 1 份历史方案文档 6 处 |
| Ⅷ | **5 个顶层目录既非正式根也非生成物、每个只装 1~3 个文件，无归属规范** | `客户端/`(1 文件)、`运维脚本/`(1 文件)、`前端核心/`(3)、`启动监督器/`(2)、`后端核心/`(1) | ① 同类脚本落点分裂（构建/运维散在 `客户端/`、`运维脚本/`、`开发工具/薄壳/`、`开发工具/项目编译/`、`开发工具/直播逐字稿工具/`）；② 下一个「XX核心」或零散脚本无处安放，只能再开一个顶层目录；③ 顶层目录数只增不减（已 17 个） | 明确顶层目录的**三类**制度：正式包根（5，有 `正式根.py`）、生成物（`工程缓存/`）、文档（`开发文档/`）；其余归入既有服务层（如 `运行核心/宿主/` 收 `前端核心/后端核心/启动监督器/`），单脚本目录不新开顶层 | 5 个目录搬迁 + 全仓 import 前缀改写（历史教训：`parents[N]` 硬编码根定位会批量失效，须改「向上找 支持库+模块库 双目录」）+ 测试/门禁同步 |
| Ⅸ | **有状态治理实现放在契约层：`公共契约/句柄体系.py` 是唯一实现（375 行，含 threading/signal/有界表/回收证据表），`运行核心/句柄体系.py` 只是 2 行 `import *` 转发** | `公共契约/句柄体系.py` ↔ `运行核心/句柄体系.py` | ① 「契约层」被当成实现层引用，`公共契约` 的定位声明与实际不符；② 同一名字两层，任何人 `from 运行核心.句柄体系 import ...` 与 `from 公共契约.句柄体系 import ...` 得到同一对象但归属判断相反；③ 后续想把状态机拆分/替换时，动契约层会牵动所有契约消费者 | 实现移入 `运行核心/`（如 `运行核心/句柄管理/`），`公共契约/句柄体系.py` 只留类型/常量/协议；或在 `公共契约` 的 README 定位里显式豁免并写明理由（二选一，不能既不搬也不申明） | 375 行 + 全仓引用点（`支持库/后端/浏览器自动化支持库/实现/浏览器自动化.py:11` 等）+ 完整性摘要重算 |
| Ⅹ | **模块能力 id 前缀顶着支持库名** | `模块库/文档生成/能力定义.json` → 能力 id `办公文档支持库.文档生成.生成文档`（包 id 却是 `模块库.文档生成`） | ① 「能力 id 前缀 = 层/库归属」这条判断口径失效（全仓仅此 1 例，但是坏样板）；② 与 `支持库/后端/办公文档支持库/文档生成/` 的 7 个能力同域前缀，看图/搜索时分不清谁是模块谁是支持库 | 能力 id 改为 `文档生成.生成文档`（与包目录名一致），同步 `能力契约/参数契约.json`、`能力数据/*.json`、`验证场景引用.json`、说明书；调用方（`模块库/文档解析` 同域命名可作参照） | 1 个包 12~15 个文件 + `复用决策.json` |
| Ⅺ | **两个同名 `说明书生成器.py` 长期并存（数据源/输出风格/输出文件名都不同）** | `开发工具/组件规范/说明书生成器.py`(147) ↔ `开发工具/说明书生成/说明书生成器.py`(154) | ① 平台 `说明/使用说明.md` 出现两种格式（已在历史上发生过）；② 谁被调用取决于调用点，改一处不生效 | 保留「读包声明+唯一聚合契约」的那个（与平台现行说明书口径一致），另一个删除；`测试中心/开发工具/测试_说明书生成器.py` 同步改指向 | 1 个文件删除 + 1 个测试文件改指向 |
| Ⅻ | **`开发工具/` 根平铺 6 个门禁/工具脚本（1323 行），与 22 个子目录无二级分层；同类门禁跨根与子目录分家** | `开发工具/{公开调用完整性门禁,工程缓存回收,开发入口,更新完整性摘要,测试伪装门禁,测试体系门禁}.py`；`测试伪装门禁.py`+`测试伪装门禁实现/`、`测试体系门禁.py`+`测试体系门禁实现/` 两套同名配对 | ① 「平台有几个门禁」数不清（4 个平铺 + `发布门禁/` 子目录）；② 新增门禁无归口，继续平铺；③ `更新完整性摘要.py` 这类零引用脚本会一直被当成可用入口 | 在 `开发工具/` 下设二级归口：`门禁/`（收 4 个门禁 + `发布门禁/`）、`编译/`（契约编译+快速编译+项目编译）、`生成/`（说明书+摘要+模板）；根只留 `开发入口.py`；零引用的 `更新完整性摘要.py` 按第 5 条**直接删除** | 26 个文件/目录归位 + 全仓引用改写（`测试中心` 多处 import 路径）+ 发布门禁入口命令同步 |

**结构债总数：12 条**（Ⅰ–Ⅻ）。其中 Ⅰ/Ⅱ/Ⅲ 为最高危；Ⅳ/Ⅴ/Ⅵ 为高；Ⅶ–Ⅻ 为中。

---

## 六、无法判定 / 需华哥裁决的

1. **适配层的最终形态（Ⅰ 与 Ⅵ 合并裁决）**：是「17 个提供者包 + 两段（第三方翻译层/环境依赖层）」还是「后端成对包保留为门面、提供者包只做翻译」？两条路的工作量差 3~5 倍。我倾向适配层提供者为唯一实现（符合第 2 条分层），但**这会把 8 个模块的调用 id 全改一遍**，属阶段级动作，需你点头再动。
2. **顶层目录要不要瘦身（Ⅷ）**：`客户端/`、`运维脚本/` 各只装 1 个文件。是把它们并进既有服务层（要全仓改 import 前缀），还是承认「单文件顶层目录」合法并写进 README？这是命名/形态偏好，不是技术判断。
3. **`公共契约/句柄体系.py` 搬不搬（Ⅸ）**：搬 = 契约层变干净但要动 375 行核心；不搬 = 契约层定位声明一直与实际不符。属口径偏好。
4. **`模块库` 里 4 个平台工具型模块（`能力目录`/`测试资源`/`组件生成`/`自修复工具`）算不算越位**：它们是模块（合法），但业务上是平台自身治理。要看你想不想让「模块库」只放对外业务功能。
5. **本次未展开**：`测试中心/` 下 30 个阶段的测试目录（第三~十三阶段 + 结构迁移 + 第一/七~十一）是否属「历史分层残留」——按第 12 条（阈值不设硬门禁、只看改动频率与读取成本）我**没有**把它列为结构债，但若你要「测试目录也按能力域归口」，这是一整批工作。

## 七、关键命令与输出留痕

```bash
# 0) 环境
export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台

# 1) 顶层目录 + 文件数（17 个顶层目录）
for d in */; do echo "$d $(find "$d" -type f | wc -l)"; done
# → 支持库 1815 / 模块库 341 / 平台控制面 222 / 开发工具 111 / 测试中心 189 / 公共契约 29 …

# 2) 工具类目录普查（正式根内已无 工具/ 目录）= 本次结论依据
find . -type d -name "*工具*" -not -path "./工程缓存/*" -not -path "./.git/*"
# → ./开发工具  ./开发工具/直播逐字稿工具  ./支持库/后端/系统核心支持库/工具执行
#   ./模块库/自修复工具  ./测试中心/MCP工具箱  ./测试中心/开发工具

# 3) 逐字节相同文件（8 组，非线索所说的 5 对）
find . -type f -name "*.py" -not -path "./工程缓存/*" -not -name "__init__.py" \
  -exec shasum -a 256 {} \; | sort | awk '{print $1}' | uniq -d

# 4) 五根包数（与项目说明.md 口径表 120/114 对照）
find 支持库 模块库 技能库 平台控制面 运行核心 -name 包声明.json | wc -l   # → 131
find 支持库 模块库 技能库 平台控制面 运行核心 -name 能力定义.json | wc -l   # → 125

# 5) 能力 id 全量去重（611 条，0 重复 → 两条腿靠两套 id 各自「唯一」）
/opt/homebrew/bin/python3.14 -B /tmp/审计_能力id普查.py
# → 能力定义.json 中的能力条目总数: 611 / 去重后能力 id 数: 611 / 包id重复: []

# 6) 适配层包外部引用（精确词边界匹配）
/opt/homebrew/bin/python3.14 -B /tmp/审计_外部引用3.py
# → 17 个适配层包中 16 个「零引用」，唯一例外 PDF隔离提供者.校验PDF

# 7) 两条腿同一实现的两处调用点（第二入口活证据）
grep -n "PDF隔离提供者" 支持库/后端/办公文档支持库/文档生成/实现/签名校验.py
# → 17:校验PDF能力id = "PDF隔离提供者.校验PDF"   80:结果 = 获取能力调用器().调用能力(校验PDF能力id,…)

# 8) 同名文件/目录并存
ls 平台控制面/授权.py 平台控制面/授权/ 平台控制面/统一入口.py 平台控制面/统一入口/
# 9) 失效文档路径
grep -n "MCP工具箱" 开发文档/项目说明.md
# → 326(MCP工具箱/能力网关.py 默认8866) 328(MCP工具箱/项目服务.py) 522(./MCP工具箱/启动HTTP服务.sh 8766)
```

**行为纪律留痕**：一次全仓 `grep -rn` 循环因范围过宽超时 180s（已改窄重跑）；
`timeout 600 …` 因本机无 `timeout` 直接 `command not found`（已按提示改为 Python 内限时/直接跑）。
除本文件外**未写任何项目文件**；临时脚本 `审计_包声明普查{2}.py`、`审计_能力id普查.py`、
`审计_能力清单.py`、`审计_两腿对照.py`、`审计_外部引用{2,3}.py` 均在 `/tmp/`。
