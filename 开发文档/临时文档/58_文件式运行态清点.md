# 58 文件式运行态清点（入库迁移清单）

> 只读清点，2026-09-15。口径见 `57_底座收口总清单_审计.md` 第四节（华哥定盘：运行态一律入库；
> 支持库可有自己的表/库文件，但库文件统一放 `工程缓存/运行数据/` 且一律经唯一 SQLite 支持库访问；证据类保留文件）。
> 清点方式：扫描 `运行核心/平台控制面/模块库/MCP工具箱/启动监督器/技能库/客户端/开发工具` 的写盘调用（`write_text`/`write_bytes`/`json.dump`/`.jsonl`/`open(...,'w')`/`sqlite3.connect`），共 **117 处、涉及 51 个文件**。

## 一、结论概览

| 分类 | 处数 | 处置 |
|---|---|---|
| **运行态状态** | **53** | 入运行库（`工程缓存/运行数据/底座运行.db`），本清单第二节逐条给目标表 |
| **不可变证据** | 42 | **保留文件**（证据天生适合文件，不入库） |
| **缓存** | 22 | 保留或进库；能丢的按清理策略处理 |

另有 `工程缓存/` 下 **43 个**目录/文件，其中 **9 个是空目录**、**6 个库文件与散落 JSONL 并存**（两套做法并存正是本次要收口的问题）。

## 二、运行态状态落点 → 目标表（可直接排批）

| 域 | 现状落点（文件:行为） | 目标 |
|---|---|---|
| 任务 | `运行核心/任务调度/任务系统.py:83,109` → `任务.jsonl` | 运行库 `任务` 表 |
| 作业 | `MCP工具箱/作业系统.py:122` → `作业.jsonl` | 运行库 `作业` 表 |
| 协作状态 | `MCP工具箱/工作区管理.py:140` → `清单目录/<work_id>.jsonl` | 运行库 `协作状态` 表 |
| 能力占用 | `工程缓存/能力占用/*.json`（22 个文件） | 运行库 `能力占用` 表 |
| 检查点 | `工程缓存/检查点.db`（已是 SQLite） | 运行库 `检查点索引` 表（或保留自有库并登记） |
| 会话 | `工程缓存/会话存储.db`、`记忆库.db`（已是 SQLite） | 运行库 `会话` 表 / 保留自有库并登记 |
| 缓存索引 | `嵌入缓存.db`、`大语言模型支持库.嵌入缓存.db`（已是 SQLite） | 运行库 `缓存索引` 表（或保留自有库并登记） |
| 发布/灰度 | `平台控制面/发布管理/发布事务/事务恢复.py:44` → `操作日志.jsonl`；`运行核心/加载器/版本系统/灰度指标.py:59,81` → `灰度指标.jsonl` + 状态 JSON | **需新增域** `发布`（含 灰度指标/操作日志/回滚事务）；先定表结构再迁 |
| 审计/失败/事件 | `运行核心/运行诊断/安全审计/安全审计.py:31`、`诊断中心/失败记录.py:86,99`、`运行事件/事件日志.py:26` → `*.jsonl` | **需新增域** `诊断事件`（审计/失败/事件三类可同表分列） |
| 需求登记 | `平台控制面/需求登记.py:74` → `状态.写入记录("需求", ...)`（已走状态层，非裸文件） | 保持（已是状态层写入），登记即可 |
| 制品/安装记录 | `平台控制面/包仓库/版本仓库.py:94`、`可信仓库元数据.py:43` | **保留文件**：属发布证据链（签名/信任），不是可丢状态 |
| 开发工具脚手架写入 | `开发工具/开发入口.py`、`模块模板生成器.py`、`HTML验证/热接入回归.py`、`复用审计/反向破坏/破坏验证.py` 等 | **不算运行态**：是生成/测试夹具写入，保留 |

**底座内部直连 SQLite 的点（新口径要求经唯一支持库）**：`运行核心/权威状态.py:965`、`平台控制面/备份恢复/备份执行.py:76`、`平台控制面/提供者/控制面对账.py:28`、`开发工具/发布门禁/权威状态门禁.py:21,57`。
→ 属**核心机制直连**，改动风险高：**先登记，不在本批动**；后续评估「经 SQLite 支持库访问」的等价封装（权威状态是运行时热路径，需先测性能）。

## 三、不可变证据（42 处，保留文件，附理由）

- `工程缓存/HTML验证证据/`（137 个文件、197 MB）：HTML 黑盒验证的原始证据，**不可变**，验完不重写。
- `工程缓存/发布证据/`（1 个）：发布门禁落账证据，与制品摘要/工作区指纹比对用。
- `工程缓存/清理失败证据/`（空）：预留给清理失败留痕。
- `平台控制面/备份恢复/*`：备份/恢复快照（含 `快照_*` 命名），属灾备证据链。
- 理由：证据的价值在于**原样可核验**，塞进数据库反而破坏「原文件可追溯」；且第 22 条「本地不归档」针对的是**制品/版本**，不含证据。

## 四、缓存类（22 处）与可直接清理项

| 目录 | 规模 | 处置 |
|---|---|---|
| `提供者运行环境/` | **8.8 GB / 204576 文件** | 第三方运行环境（虚拟环境），**保留但需确认可重建**：建议登记为「环境依赖缓存」，给出重建命令后按需清理 |
| `编译缓存/` | 74 MB / 11157 文件 | 保留（可重建） |
| `验证缓存/` | 12.9 MB / 196 文件 | 保留（可重建） |
| `快速编译/` | 6 个 | 保留（`最近一次.json` 报告） |
| `测试临时/`、`调试TMPDIR/`、`句柄调试/` | 0–1 个 | 用完即清（第 17 条） |
| **空目录 9 个**：`MCP临时上下文/`、`失败记录/`、`说明书/`、`验证工作区/`、`验证运行/`、`任务工作区/`、`测试资源清单/`、`调试TMPDIR/`、`清理失败证据/` | 0 | **直接删**（无结果的东西不该存在，第 11 条；写入方按需创建目录） |
| 散落文件：`批次三-公开调用门禁.txt`、`示例项目_临时文件.txt`、`示例项目_文档.txt`、`验证锁.lock`、`mcp_http_8766.log` | 各 1 | 逐个判定后清（锁文件确认无持锁方） |
| `独立项目验收3/`、`独立项目验收4/` | 48 + 48 文件 | 历史验收残留，确认无引用后清 |
| `核心快照/` | 14 个 | 快照属版本治理 → 按第 22 条只留最新（需先确认引用计数为 0） |

## 五、迁移批次建议

1. **批 3-1（纯机械、风险最低）**：`任务`、`作业`、`协作状态`、`能力占用` 四域改写入运行库；这四个的写入方都是单点类（`任务系统.py`、`作业系统.py`、`工作区管理.py`、能力占用模块），改完各跑一次定向测试 + 真实 HTTP。
2. **批 3-2（需先加表）**：新增 `发布` 与 `诊断事件` 两个域表，再迁 `发布操作日志/灰度指标/安全审计/失败记录/运行事件`。
3. **批 3-3（自有库登记）**：`检查点.db`、`会话存储.db`、`记忆库.db`、`嵌入缓存.db` 改为「在运行数据目录下、经 SQLite 支持库访问」并登记到域表（或明确保留自有库的理由）。
4. **批 3-4（清理）**：删 8 个空目录 + 散落文件 + 历史验收残留；`提供者运行环境` 给出重建方式后再决定是否清。

**兼容读取**：四个域的旧 JSONL 一律**保留只读兼容**（先查库、库空再读旧文件），避免迁移期丢历史任务；确认稳定后再删旧文件——不搞双写。

## 六、验证口径

- 每域迁移后：定向测试（写入/查询/并发）+ 真实 HTTP（40007 走 `工程缓存/运行数据/底座运行.db`）+ 六环门禁 + 摘要闭合。
- 判定「迁移完成」的标准：**该域不再产生新文件**，且旧文件读不到新记录（即不再双写）。
