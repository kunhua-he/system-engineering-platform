# 验证可信度（假绿）审计（2026-09-16）

审计对象：`/Users/hekunhua/Documents/Agent/PHP/系统工程平台`（只读，未修改任何项目文件；临时脚本在 `/tmp/`）
审计维度：全仓「看起来绿、实际没真验证」的地方——宽 `except` 吞异常、恒真断言、弱断言、**验证器/门禁/审计器自身故障被当通过**。
判别口径：**「执行异常/内部故障」与「被测系统真的拒绝了」必须可区分**；不可区分处即假绿风险点。

---

## 一、静态扫描结果（与存量基线的区分）

### 1. 测试伪装门禁（mock 三条硬规则·首发只报不拦）

```
命令：/opt/homebrew/bin/python3.14 -B 开发工具/测试伪装门禁.py
退出码：1
基线：测试文件 182 个（解析失败 0）｜patch 调用点 190 个
       （对象目标 98、字典目标 54、字符串目标 32、动态目标 6）
规则1 禁 patch 被测对象/生产路径：违规 118（P1 本体 21／P2 生产路径 97）｜wraps 豁免 1｜清单豁免 0
规则2 一律 autospec=True：违规 129（存量基线）｜合规 5｜patch.dict 不适用 54｜create_autospec 0
规则3 负面断言强度下限：恒真违规 1｜强度不足违规 12｜用例总数 1941
       观察（无直接断言 23／弱断言 141）
豁免清单：条目 0，命中 0，失效 0
共 260 项违规（规则1 118、规则2 129、规则3 13）
耗时 1.2 秒（只读 AST 扫描）
```

**结论（区分口径）**：
- 规则2 的 129 项、规则1 的 97 项 P2、规则3 的 141 弱断言 + 23 无断言观察 = **存量基线**。门禁自身文档已明写口径（`测试伪装门禁.py:16-18`「存量 185 处全违规，所以首发**只报不拦**……避免一次性全红导致门禁被绕过」），并有豁免清单机制（当前 0 条）。**不算缺陷**。
- 规则3 的 13 项（恒真 1 + 强度不足 12）是**真实违规**，但它们指向的是**测试用例写法**，不是验证器可信度 → 归存量基线，本轮不列为缺陷。
- 该扫描器自身**可信**：解析失败（读取失败/语法错误）不静默丢弃，而是升级为「解析失败（fail-closed）」违规（`测试伪装门禁.py:144-153`）；发现口径实测无遗漏（见 §3）。

### 2. 全仓可疑形态扫描（AST + grep，取证用）

```
命令：/opt/homebrew/bin/python3.14 -B /tmp/假绿AST扫描.py   # 自写只读 AST 扫描
命中 346 处「except 体只做 pass/continue/return(None)/空体/日志」形态
```

- **源目录内 `except: pass` 单行形态：0 处**（`grep -rn -E "except [A-Za-z_.]+( as [^:]+)?:\s*pass\s*$"` 在生产目录 → 0 命中）。
- **裸 `except:` 全仓源码仅 1 处**：`开发工具/更新完整性摘要.py:24`（见 §2 风险点表外说明，方向是 fail-closed）。
- 346 处命中里 95% 以上是**资源清理/临时目录/进程回收**类（`shutil.rmtree`、`os.killpg`、`queue.Empty`、`ProcessLookupError`），且都紧跟在 `检查(..., False, ...)` 或返回失败的分支上 → **不是假绿**。
- `except: pass` 大量出现在 `工程缓存/**`（制品仓库、提供者运行环境 site-packages、编译器产物）——**构建产物与第三方包，不是本仓源码，不算缺陷**。
- 恒真式断言全仓仅 1 处：`开发工具/复用审计/反向破坏/破坏验证.py:224`（见风险点 #3）。`assert True` / `assertEqual(x, x)` / `assertTrue(True)` 在源目录 **0 处**。
- 无条件 `unittest.skip` / `skipIf(True)`：源目录 **0 处**。

---

## 二、假绿风险点（按危害排序）

| # | 位置 | 形态 | 为何会掩盖真问题 | 修法 | 危害等级 |
|---|------|------|----------------|------|---------|
| 1 | `开发工具/发布门禁/运行发布门禁.py:942,1654,1664`（`工作包超时秒`） | **死参数**：签名收参、argparse 收参、原样转发，函数体零引用 | `--help` 明写「单个测试工作包硬超时秒数（默认180；**超时立即终止并报告失败**）」，实际**没有任何超时被施加**。传参者以为加了硬保险，工作包卡死时门禁不会终止也不会报失败 → 「有超时保护」是假控制 | 接回真实工作包执行并按其判定红/绿；若已无工作包概念，**删除参数与 help 文案**（名实不符比没有更坏） | **高**（门禁对外承诺不存在的能力） |
| 2 | `开发工具/发布门禁/运行发布门禁.py` 全文；`开发工具/测试体系门禁.py`；`开发工具/测试伪装门禁.py` | **测试门禁未接线**：发布门禁的 `门禁项列表` 无任何测试项，全仓无生产调用点 | 实测 `运行发布门禁.执行门禁` 只做 HTML 黑盒验证 + 语法编译，**从不执行 182 个测试文件 / 1941 个用例**；而该文件 docstring 第 6 行仍声称门禁含「测试全部通过」。测试体系门禁（可导入性/零测试/未解释跳过）与测试伪装门禁（§1 的 260 项）都**只在手工执行时生效**。发布判定可全绿而无任何测试证据 | 由发布门禁属主按既定接口接线：`from 开发工具.测试体系门禁 import 运行门禁`、`from 开发工具.测试伪装门禁 import 运行门禁并摘要`，按 `强制` 决定是否阻断；或至少把 docstring 改成与实现一致的「发布只认 HTML 黑盒验证」（`运行发布门禁.py:997-998` 已记录该裁决） | **高**（门禁覆盖面的假象） |
| 3 | `开发工具/复用审计/反向破坏/破坏验证.py:219,224` | **恒真表达式残骸**（与已修的 13/13 假绿同源） | `验证失败 = 漂移 is None and False or True` —— 实测恒为 `True`（`漂移 is None` 时 `True and False=False → False or True=True`；非 None 时同理 `False and False=False → True`）。紧跟的 225 行 `验证失败 = 漂移 is not None` 覆盖了它，故当前**不致假绿**；但这是上一轮修复同一文件时没清掉的恒真赋值，一旦 225 行被改动/删除，「参数类型修改」场景立刻变成**无条件检出**（恒真成功）。同行上方 219 行 `问题 = []` 亦为死变量 | 删除 224 行与 219 行死变量；并在该文件加一条「本文件禁止任何字面量恒真赋值」的自检 | **中高**（同源残骸，一次编辑即转真缺陷） |
| 4 | `开发工具/复用审计/反向破坏/破坏验证.py:146-147` | **`恒真检查` 名实相反、语义空转** | `全部失败 = all(证据.验证结果)` → `恒真检查 = 全部失败`。变量名叫「全部失败」而算的是「全部检出」；`恒真检查` 在 docstring 里定义为「不允许恒真成功（全部失败仍通过的验证器=恒真）」——即应有**对照组**（验证器对未破坏输入必须不报检出）。现实现只等于「13 项全检出」，且是 `成功` 属性的必需位 → 是恒真灌水位，不提供 docstring 承诺的任何保证 | 加真对照：同一验证器跑**未破坏**输入必须不报检出（正向对照），否则 `恒真检查=False`；或把字段更名为 `全部检出` 并让 `成功` 不再依赖它 | **中**（假保证 / 报告语义误导） |
| 5 | `开发工具/统一验证/统一验证器.py:400-407,411-430,433-440` | **子进程结论不看退出码** | 三处 `_子进程.run([...grep...])` 只取 `.stdout` 判定：`越界导入`空 → 记「项目适配.边界无越界」通过；`非法行`空 → 记「零第三方依赖」通过；`项目耦合命中`空 → 记「零外部项目依赖」通过。**grep 自身故障（退出码 2，目录/权限问题）与「真的没匹配」在报告里完全不可区分**，且 stdout 为空时一律走「通过」。另 411 行这次 grep **未带 `--include=*.py`**（400 行那次带了），扫描面与判据不一致 | 校验 `returncode in (0,1)`（1=无匹配）；非 0/1 或结果为空一律记失败并附 `stderr`；补 `--include=*.py` 统一扫描面 | **中**（三条边界门禁在工具故障时假绿） |
| 6 | `开发工具/公开调用完整性门禁.py:46-50`（`读取json`） | **宽 `except Exception` 把「读取失败」混成「内容缺失」** | `读取json` 一律 `return None`；调用点 `声明 = 读取json(...) or {}`。方向是 fail-closed（能力列表空 → 记「声明-包声明.json未列能力」违规，:155-156），**不构成假绿**；但报告缺口类型写「未列能力」而真实原因是「JSON 不可读」——**验证器内部故障与被测系统真缺能力在报告里不可区分**，会把修复带偏 | 区分两种缺口类型：「包声明不可读（含异常类型）」与「未列能力」分别出条目 | 低（定位保真度） |
| 7 | `开发工具/复用审计/提供者直连规则.py:93-96` | **解析失败 → 返回空 = 无违规**（fail-open） | `审计源码` 的 `except SyntaxError: return []`：源码不可解析时输出「零直连」，与同仓 `开发工具/复用审计/能力调用图审计.py:204-205` 对同情形记「解析失败」违规**口径相反**。当前唯一调用点（`第十四阶段门禁._项7:310,314`）喂的是函数内字面量样例，不触发此分支，故为**潜在** fail-open | 与能力调用图审计统一口径：解析失败即产出违规条目 | 低（潜在） |
| 8 | `开发工具/发布门禁/第十四阶段门禁.py:305-319`（`_项7_直连规则`） | **门禁项只对自造样例验证规则引擎，不覆盖真实面** | 项名「未声明提供者直连规则」，实测只用两段**硬编码字符串样例**（`import grpc…` 必须阻断 / `def 计算(x)` 必须放行）证明判定器能判。全仓 `grep` 实测：`提供者直连规则` 在 `开发工具/测试中心/平台控制面/运行核心` 中**无第二处调用点**，注册表从未跑在真实 `支持库/适配层/**` 源码上 → 真实直连面**零覆盖**，该项恒绿 | 增加真实扫描：把注册表跑在真实适配层实现源码上，样例自测保留为辅助证据 | 低-中（自证不算证据） |

**表外说明（查过、判为非缺陷，取证留痕）**
- `开发工具/更新完整性摘要.py:24` 的裸 `except:` → `return False, "JSON解析失败"`：方向 fail-closed，不是吞异常放行。
- `开发工具/发布门禁/运行发布门禁.py:301-308`（`_是否已废弃包`）`except Exception: return False`：不可读 → 当「非废弃」→ **进入**后续校验，fail-closed。
- `开发工具/发布门禁/运行发布门禁.py:578`（`_资源释放证据通过`）：全仓 `grep` 仅 1 处（定义本身），**无调用点** → 死代码（潜在误导，非假绿）。
- `开发工具/发布门禁/运行发布门禁.py:838`（`执行逐包权威合规`）：门禁内无调用点（1.5 逐包 13/13 已按华哥 2026-08-29 裁决移除），仅测试中心调用 → 存量收敛后的死代码，**有明文裁决，不算缺陷**。
- `开发工具/组件规范/完整性摘要.py:83-85` 的 `except …: pass`：只省略 `能力数/能力清单/能力定义摘要` 附加字段，不影响 sha256 文件清单这一权威部分；校验侧另有 `依赖生命周期审计` 做全 dict 相等兜底 → 非假绿。

---

## 三、验证器/门禁自身可信度逐个判定

判定问题（逐项）：「它失败时真的会红吗？有没有『失败被吐下去』的分支？」
判别口径：只有**【执行异常/内部故障】**与**【被测系统真的拒绝】**可区分、且异常一律记红，才算可信。

| 验证器 / 门禁 | 失败会红吗 | 关键证据 | 判定 |
|---|---|---|---|
| `开发工具/发布门禁/运行发布门禁.py` | **会** | `检查()` 对强制项空详情自动翻 False（:949-953）；`汇总()` 无强制项→「阻断」（:108-110）；子进程 Popen 失败→`-1`→退出码判红（:134-135,1177）；HTML 验证器异常→`检查(...,False)`（:1154-1157）；字节复核异常→False（:1163-1164）；空集分支统一用「跳过（空集，不构成证据）」且不冒充核验（:667-672,763-777）。反向篡改、陈旧令牌、空需求 id 等均加了**正向对照**（:1526-1531 明文注释「若首次切换本身没成，只断言第二次被拒会恒真通过」） | **可信**（两处缺陷 #1/#2 属覆盖与承诺，不属红绿失真） |
| `开发工具/测试体系门禁.py` + `测试体系门禁实现/` | **会** | 探测超时/探针无法启动/无标记输出/解析失败/子进程超时**一律判导入失败（fail-closed）**（`可导入性检查.py:69-82`）；`导入成功 = 记录.成功 and returncode==0`（:86）；语法错误→记「未解释跳过」违规（`跳过检查.py:72-76`）；源码读取失败→违规（:112-119） | **可信**；缺陷仅是**未被接线**（#2） |
| `开发工具/测试伪装门禁.py` + `实现/` | **会** | 解析失败升级为违规（:144-153）；`--豁免` 清单写错即退出码 2「fail-closed，不静默降级」（:277-281）；退出码 0/1/2 语义明确（:287-293） | **可信**；规则 3 弱断言只进「观察」是**明示口径**（`断言检测.py:29-30`） |
| `开发工具/发布门禁/权威状态门禁.py` | **会** | 7 项场景全是真实断言 + **正向对照**（如「令牌2 > 令牌1 且旧令牌被拒」:103；「握手成功 且 死锁已回收 且 新令牌 > 旧令牌」:146-148）；异常→`(名称, False, 异常类型: 消息)`（:225-226） | **可信** |
| `开发工具/发布门禁/第十四阶段门禁.py` | **会** | 场景异常→False（:421-423）；并发互斥项**自带正向+反向双证据**，并留注释记录上一版「只在空目录跑单次迁移=零证据」的修正（:210-214,237-239） | **可信**（除 #7/#8 两处覆盖缺口） |
| `开发工具/组件合规/合规测试包.py`（13 项） | **会** | `执行()` 场景异常→`(False, f"异常: {错误}")`（:459-464）——与已修的「破坏验证」**同口径、已正确**；视图包只跑 2 项且**不虚增**总场景数（:453-458） | **可信** |
| `开发工具/组件规范/完整性摘要.py` | **会** | sha256 必须 **64 位十六进制 + 精确相等**（拒截断摘要冒充，:132-134）；文件路径越界拒绝（:136-139）；清单必须**双向闭合**（未登记/多余都判红，:151-154）；`摘要算法` 必须**显式**为 sha256（:111-112） | **可信**；低危：附加字段（能力数/能力清单）在本校验器内不被核对 |
| `开发工具/复用审计/能力调用图审计.py` | **会** | 解析失败→记「解析失败」违规（:204-205）；四者漂移（声明/契约/注册/`__all__`）对账（:176-178）；`_同包实现` 按路径段逐段相等（防 `startswith("")` 恒真放行，:105-110） | **可信** |
| `开发工具/依赖生命周期审计/审计核心.py` | **会** | 所有失败路径都产出违规文本：缺锁/锁损坏/锁空/混装/缺摘要/摘要漂移/缺探针/契约损坏/缺停止入口（:61-67,82-86,89-106,109-142,145-177） | **可信**（偏宽：探针与停止入口只做「关键词存在性」静态检查，属**判定强度**问题而非假绿） |
| `开发工具/复用审计/反向破坏/破坏验证.py` | **会**（已修） | `执行()` 中 `except Exception` 现记 `验证结果=False` + `检出方式=执行异常`（:136-143），与 docstring :8-9 一致 | **基本可信**；残留见 #3/#4 |
| `开发工具/统一验证/统一验证器.py` | 部分 | 结构/契约验证的「通过项条件化」已修（:52-55,86-88 明文注释「通过数被恒真项灌水」）；但项目适配三项 grep 见 #5 | **有假绿分支**（#5） |
| `开发工具/HTML验证/**`（验证器/应用/服务/场景执行器/返回判定/验证报告） | **会** | `验证结果.通过` 默认 `False`（`验证报告.py:12`）；步骤异常与验证异常都只写 `失败原因`+`定位线索`（「场景执行器」/「验证器」），**不置通过**（`验证场景执行器.py:84-86`、`返回判定.py:116-118`）；场景束异常返回 `通过=False` 的合成结果（`场景执行器.py:145-149`）；零场景→「阻断」（`验证报告.py:70-71`）；实际成功目标全集 ≠ 目标全集 → 追加失败项（:184-190）；残留 → 计失败（:191-192） | **可信** |

---

## 附：关键命令与原始输出

```
export PATH=/Library/Developer/CommandLineTools/usr/bin:$PATH; unset PYTHONPATH
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台

# 1) 静态扫描（§一.1）—— 260 项违规，退出码 1
/opt/homebrew/bin/python3.14 -B 开发工具/测试伪装门禁.py

# 2) 发现口径无遗漏（§一）
find 测试中心 -name '测试_*.py' -not -path '*__pycache__*' | wc -l            → 182
find . -name '测试_*.py' -not -path '*__pycache__*' -not -path './工程缓存/*' | wc -l → 182

# 3) 死参数取证（#1）—— 只有 3 处，全在签名/argparse/转发
grep -rn "工作包超时秒" 开发工具/
  开发工具/发布门禁/运行发布门禁.py:942:   测试并行数: int = 16, 工作包超时秒: int = 180) -> 门禁结果:
  开发工具/发布门禁/运行发布门禁.py:1654:  "--工作包超时秒", type=int, default=180,
  开发工具/发布门禁/运行发布门禁.py:1664:  工作包超时秒=参数.工作包超时秒,

# 4) 测试门禁未接线取证（#2）
grep -rn --include=*.py "测试体系门禁\|测试伪装门禁" 公共契约 后端核心 前端核心 启动监督器 客户端 \
  平台控制面 开发工具 支持库 模块库 测试中心 示例项目 运维脚本 运行核心 项目适配层
  → 除自身文件与 测试中心/__init__.py 的说明文字外，无任何生产调用点
grep -n "unittest\|测试体系\|伪装门禁\|全量测试\|工作包" 开发工具/发布门禁/运行发布门禁.py
  → 只有参数名/注释，无测试执行调用

# 5) 恒真残骸（#3）
sed -n '216,228p' 开发工具/复用审计/反向破坏/破坏验证.py
  验证失败 = 漂移 is None and False or True      ← 恒真，紧跟被下一行覆盖
  验证失败 = 漂移 is not None

# 6) 自写只读 AST 扫描（§一.2）—— 346 处「except 只做 pass/continue/return None/日志」
/opt/homebrew/bin/python3.14 -B /tmp/假绿AST扫描.py
```

**边界声明**：本次为只读审计，未修改任何项目文件；`支持库/适配层/密码签名提供者/**` 等在途改动未作为问题上报。发布门禁、测试体系门禁、测试伪装门禁中涉及真实制品/子进程的重型路径**未实机跑通**（需正式制品与 600 秒级超时），相关判定均基于源码取证 + 静态扫描实测输出，已在各条目中标明证据来源。
