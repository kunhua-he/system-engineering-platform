# 子报告 09 · 平台控制面治理与运行数据库（原子代理 HTTP 503 失败，父会话接手完成）

- 冻结边界：分支 `主干`，HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`
- 方式：**只读**（SQLite `mode=ro` URI 打开），未写入任何库、未改源码、未重启服务

## 分级计数：阻断 0 / 重要 2 / 建议 3

## 一、运行数据库 `工程缓存/运行数据/底座运行.db`（0.31 MB）

```
integrity_check = ok      foreign_key_check = 0 条      表数 = 10
任务 8 行 | 会话 0 | 作业 1 | 协作状态 26 | 发布 0 | 检查点索引 0
灰度状态 0 | 灰度观测 1 | 缓存索引 0 | 能力占用 22
```

- **建议 1：10 个域表里 5 个恒空**（会话、发布、检查点索引、缓存索引、灰度状态）。
  - 若这些表对应的功能确实已改用各自独立库（`检查点.db`、`大语言模型支持库.嵌入缓存.db`）或尚未启用，则属「建了没人写」的空转表，按哲学第 11 条应删或明确标注为预留；
  - 需华哥口径：**保留为预留 vs 直接删**。
  - 复现：`sqlite3 -readonly 工程缓存/运行数据/底座运行.db "SELECT name FROM sqlite_master WHERE type='table'"` + 逐表 `COUNT(*)`。
- **重要 1：测试数据写进了正式运行库**：`灰度观测` 表唯一一行是 `观测id='验收-灰度-1'`，内容是测试夹具（`能力id='验收.能力'`）。正式运行库里留验收假数据，会污染任何「按表统计灰度效果」的下游判断。
  - 复现：`SELECT * FROM 灰度观测` → 见 `验收-灰度-1`。
- **建议 2：三处数值/计数字段落成 TEXT**：`会话.压缩次数`、`发布.灰度比例`、`缓存索引.命中次数` 均为 `TEXT`。
  - 与 2026-09-15 已修过的「逻辑型必须 INTEGER、数值型必须 REAL」是同一类问题，只是这次落在**尚无人写的表**上，所以没被 PRAGMA 断言抓到。建议在建表 DDL 层面统一收口，而不是只锁有数据的表。

## 二、权威状态库 `工程缓存/权威状态/权威状态.db`（3.33 MB）

```
integrity_check = ok      foreign_key_check = 0 条      表数 = 9
句柄 9130 | 租约 9166 | 进程 3631 | 回收证据 158 | 资源版本 64 | 元信息 1
事务 0 | 引用计数 0 | 锁 0
```

- **建议 3：句柄/租约/进程三表累计 9k/9k/3.6k 行，未见有界回收证据**。
  - 句柄体系与租约都有「过期回收」语义，但表里行数远超当前活跃数；若没有按运行实例归档/裁剪，长期会一直涨（当前库 3.33 MB 尚可接受，属趋势风险）。
  - 需补一步：统计 `进程` 表中已死亡 PID 占比，并确认落库侧是否有上限（本次未做，列未确认项）。
- `引用计数 = 0` 与「版本兼容.清理」的判据（全库计数 >0 才拒绝）一致，**该守卫在当前库状态下会放行删除** —— 行为与 2026-09-14 的修复口径相符，非缺陷。
- 文档声称「权威状态库 13.2MB（原 53.9MB）」「53.9MB → 8.7MB」，现场为 **3.33 MB**：属 VACUUM 之后的新值，但文档未标「历史快照」，见子报告 10 的口径问题。

## 三、运行数据目录的库与代码引用（收口「运行态一律入库」）

| 库文件 | 大小 | 被源码引用 |
|---|---|---|
| `底座运行.db` | 320 KB | 10 个文件（`MCP工具箱/协作状态.py`、`能力占用.py`、`作业系统.py`、`运行核心/任务调度/任务系统.py` 等） |
| `大语言模型支持库.嵌入缓存.db` | 12 KB | 2 个文件（嵌入缓存 包内） |
| `检查点.db` | 16 KB | 2 个文件（检查点恢复 包内 + 测试） |
| `记忆库.db` | 24 KB | 1 个文件（记忆支持库 包内） |

- `解析运行数据根` 被 **7 个文件**引用（任务系统/工具执行/检查点恢复/会话存储等），唯一解析器口径成立。
- `大语言模型支持库.会话存储.db` **当前不存在**（按需创建），不算缺失。
- 结论：**「运行态一律入库、唯一解析器」这条在代码层成立**；未发现 2026-09-15 修过的 `../../../../工程缓存` 深度错位复活（`支持库/工程缓存/` 目录现场不存在）。

## 未确认项
1. `权威状态库.进程` 表 3631 行里已死亡 PID 占比、以及是否有按实例裁剪逻辑（需读写入侧代码）。
2. `会话/发布/检查点索引/缓存索引` 四张空表在写入侧代码里是否还有调用点（本次只做了表的只读统计，未逐个反查写入方）。
3. `平台客户端状态/`（14 MB）账本里 12 份制品记录是否属「应清理」，需与子报告 00 的制品口径一并裁决。

## 本次执行过的命令（可复现）
```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 /tmp/底座审计_运行库核查.py     # 只读 mode=ro 打开两个库，逐表计数 + PRAGMA 检查
```
