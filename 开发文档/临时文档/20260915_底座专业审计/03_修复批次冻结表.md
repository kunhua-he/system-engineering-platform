# 修复批次冻结表（第二次审计产出 · 施工用）

> 冻结边界：HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`。
> 落点来源：`落点清单_*.md`（10 份，第二轮证真产物）。每条落点都带「证真命令 + path:行号 + 最小改法 + 验证命令」。
> 执行纪律：**同一文件同一时刻只有一个属主**；包内文件改动必走「包版本 → 重编译 → 摘要级联 → 依赖锁」四连；每批末尾跑该批验收，不跑全量 unittest。

---

## 批次0 · 全局口径冻结（串行单线，必须先完成）

| 编号 | 内容 | 落点 | 验证 |
|---|---|---|---|
| 0-1 | 契约版本统一 `2.0.0`：`公共契约/HTTP路由契约.json` + 5 处默认参数改 `None + 取契约版本()` + `依赖锁定.py:23` 改 import 事实源 + `模块模板生成器.py:17` 待读模板正文 | 落点清单_11b L-01（8 处逐一） | `grep -c '契约版本.*"1.0.0"'` = 0；`测试中心.开发工具.测试_漂移门禁` |
| 0-2 | 类型表分叉：`类型目录.md:9-23` 补 `JSON值型` + 加自动比对 | 落点清单_11b L-02 | 文档命中数 1；7 个包不受影响 |
| 0-3 | 网关公开错误码三处同一提交同步：`网关核心.py:64-84` 公开表补 8 码 + `本地网关.py:839-849` 码→HTTP 映射 + `网关核心.py:671-682` 异常分支 | 落点清单_06 F-03 | 真实 HTTP 逐码探针（表外码不再落 500） |
| 0-4 | `契约.声明一致` 归一 + 接线：先 `契约.py:45/113` 做 `结果`→`结果型` 归一（否则首报 ≈335 噪声），再挂 `漂移检测.py` 子检测 + 发布门禁（先只报不拦） | 落点清单_03 重要-10 | 首报条数从 ~335 降到 ~13 真问题 |

> 0-3 与 0-4 都落在 `网关核心.py`/`契约.py` → **同一属主一次改完**。

## 批次1 · 安全阻断（并行 6 路，文件域互不重叠）

| 编号 | 内容 | 属主文件域 | 备注 |
|---|---|---|---|
| 1-1 | 沙箱 SBPL 注入 + profile 语法错仍报成功（S-01 + S-12）；同包 `检测危险命令未接线`（S-06）**同函数区** → 一次改完 | `支持库/后端/系统核心支持库/进程管理/` | 落点 `实现/进程管理.py:245-271`、`:311-313`、`:331-332`、`:320-326` |
| 1-2 | SSRF：**先**接线强校验 `出站安全.校验出站URL`（S-04），**再**在 `网络请求.py:132-137` 接逐跳校验（S-02，依赖 S-04） | `支持库/后端/网络通信支持库/`（请求 + 出站安全） | 组 B 有先后依赖 |
| 1-3 | 解压体积上限（S-07 + S-09 同函数合并） | `支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py:82-98` | 一次提交 |
| 1-4 | 环境变量/环境文件脱敏（S-03）+ 网关成功返回值脱敏 | `系统核心支持库/环境变量` + **`网关核心.py`（与批次0 同文件 → 串在 0 之后）** | 组 D |
| 1-5 | MCP 安全：`workspace` 仓根约束（M1）+ `test_resource` 自杀禁用（M2，**必须只加子进程分支**，不能打红 2 个现有测试）+ 8766 鉴权（M3） | `MCP工具箱/项目服务.py`、`工作区管理.py`、`测试资源.py` | 同目录 → 组内串行，单属主 |
| 1-6 | 直播逐字稿业务词抽参（N1，11 项落点） | `模块库/直播逐字稿/` | 需重算摘要；同步 `测试_直播逐字稿.py:196,201`；制品需重建 |

## 批次2 · 运行态生效（串行单点，需确认无在跑任务）

重启 40007 → 校验 `系统底座_指纹校验=1`（plist，代码零改动）→ 重启 8766 → 重跑行为探针：
`重试调度` 传 `实际错误码=B` 应回 `是否重试=false`；`实际错误码=123` 应回 400；不传须保持旧行为。
（本轮实测：进程任务非终态 = 0，此刻重启不中断任务；重启前须重跑该计数。）

## 批次3 · 资源与有界（3 并行 + 1 串行）

- 3-1 三包并行：`会话存储`(12+1 处)、`工具执行`(4)、`检查点恢复`(10) —— 按 R1 结论**只改建连函数**（照抄 `嵌入缓存.py:27-74`），调用点 0 改动；
- 3-2 串行：`权威状态.py` 回收器接线 + 补 3 条 DELETE（R2/R4）；
- 3-3 `事件日志` 有界化（R3）；3-4 `句柄表/租约表` 淘汰（R7，注意 `资源监督` deque 不可 JSON 序列化）；
- R6 `except Exception: pass` 现场 4 处（3 代码 + 1 docstring），逐处收窄或记留痕 + 改文档。

## 批次4 · 契约注册口径（按包并行，网关表串行）

- 注册元数据：类型名非正式（41 短名）、必填缺失（98 处）、返回口径（322 条旧口径）、契约漏列参数（22 条中 15 条真死参）；
- 落点清单_03 给出「权威源 vs 从属源」判定，逐包属主独立；网关别名表补 `"数值型": "双精度数型"`（`句柄/任意` 不入表）。

## 批次5 · 门禁接线与判据修正（串行单点）

- `依赖防火墙.py`：A/B/E/G/H **一次改完**一次复跑（期望基线 585 文件 / 1 违规）；技能库层表两张表同改；
- 先修判据 bug：`防火墙审计_规则.py:115` AttributeError、`能力调用图审计.py:126-129` 前缀假设；
- `运行发布门禁.py:1017` 之后接线：复用审计 + 平台控制面防火墙 + 破坏验证（先只报）+ 测试体系门禁（全量可导入性）。

## 批次6 · 测试体系

- 僵尸测试 `测试_psycopg提供者.py` 按现行接口改写（或删除并同步 `测试_反向破坏门禁28.py:29` 白名单）；
- 4 条假绿改写（`测试_加载器.py:31-45`、`测试_统一编译器.py:61-77`、`测试_外部版本探针统一.py:58`、`测试_句柄与数值契约冻结.py:48-56`）；
- 聚合入口：**推荐 A 轻量版**（重建聚合入口 + 全量可导入性门禁）；无论选哪个，「全量可导入性」门禁必须落地；
- 删除 `灰度观测` 假数据行 + 在 `测试_运行数据库.py` 加「正式库不得含测试标识行」断言；
- mock 治理 3 条硬规则 → 新建 `开发工具/测试伪装门禁.py`。

## 批次7 · 死代码 / 未接线

- **删**：`运行核心/租约管理.py`、`版本系统/兼容适配器.py`、`版本系统/状态切换.py`（已核实被取代/自违契约）；
- **保留并登记**：`本地协议.py`/`运行生成.py`/`更新完整性摘要.py`/`热接入回归.py`（是对外工具入口，不是死码）；
- **删** 运行库 4 张恒空表（会话/发布/检查点索引/缓存索引），**保留** `灰度状态`（已接线，仅因未发生灰度发布）；
- 接线 `破坏验证.py`（`复用审计/运行审计.py --反向破坏` + 发布门禁，先只报）。

## 批次8 · 文档与结构（串行单线）

- `项目说明.md`：按「唯一权威口径表」6 行重写（源头/装配/运行时/制品/完整性/历史值，每行带取证命令）；
- `未完成事项.md`：删 ≥5 条已修复条目 + 改正自相矛盾条目（`17 Provider` 等）；
- 制品仓库：保留 `平台客户端-0eec30284da0d3bd`、删其余 11 份，**先账本后磁盘**（`构建平台客户端.py:624` 补清 `客户端制品目录`；`平台状态.py` 补受控删除入口）；
- 三套摘要权威化（16/32/64 位各归其位，账本与指针共用同一字段）；
- 结构清理挂到 `运行发布门禁.py:49` 收尾钩子（`__pycache__`、空目录、孤儿提供者环境）。

## 收口 · 一次全量

重编译制品 → HTML 黑盒全量（`--并发 32`）→ 发布门禁 → 真实 HTTP 探针复核 → `git` 提交。

---

## 已知会被打红的「预期红」清单（避免误判）

- 接线 `依赖防火墙` 补第三方表后 **新增 16 条违规**（`语法索引` 2 / `模型连接器` 1 / `MCP工具箱` 8 / `适配层模型服务.py` 5）→ 属真实存量，需同批修或被显式豁免；
- 接线 `契约.声明一致` 归一前 ~335 条、归一后 ~13 条；
- 发布门禁接线复用审计后，`审计模块库` 现有 **1 违规**（`健康监督.py:36-43`）→ 先修后接或先只报。


---

## 执行进度台账（父会话按回传逐个核，2026-09-15）

| 批次 | 状态 | 关键证据 |
|---|---|---|
| 0A 契约版本+类型表 | ✅ 完成 | `契约版本 1.0.0` 字面量 grep **0**；依赖锁反向验真（改回 1.0.0 可检出 4 条）；6 个测试模块全 OK；**它引入的 1 条回归由父会话当场修**（`测试_模块模板收敛.py:66` → 断言唯一事实源，10/10 OK） |
| 0B 网关错误码+声明一致 | ⚠️ 超时但已落盘 → 已派收口路 | 公开表补 8 表外码、状态映射 28 码对齐、`注册口径别名表`+`归一注册口径`、`漂移检测.py` +350 行、发布门禁接线（只报不拦） |
| 1-1 沙箱 | ✅ 完成 | 注入载荷修前读到工作区外 → 修后 `参数不合法` 拒收；profile 语法错不再伪绿；危险命令检测接线；`测试_进程管理` 13/13 |
| 1-2 SSRF | ✅ 完成 | 强校验接线+逐跳；修前取回 `SECRET-INTERNAL-DATA` → 修后校验层即拒；反向不误拦；契约测试 3/3 |
| 1-3 解压上限 | ✅ 完成 | 三道上限+路径越界+失败回滚；修前 204KB→200MB 落盘、兄弟目录绕过 → 修后拒收且 0 落盘；7/7 对照；组件合规 13/13 |
| 1-4 环境变量脱敏 | ⚠️ 超时但已落盘 → **父会话进程内实测验收通过** | 默认 `敏感键 → ***`、普通键不误掩、`包含敏感值=True` 取明文；只差升版本+摘要（并入四连） |
| 1-5 MCP 三项 | ✅ 完成 | 仓根白名单、killpg 自杀卡口（3 个最深点）、8766 令牌+回环/Host/Origin 守卫（10/10，临时端口 8799 实测，8766 未动）；工具数仍 50 |
| 1-6 去行业化 | ✅ 完成 | 包内品牌词/业务路径 **0 命中**；plist 运行产物移出正式包；版本 1.2.0 + 摘要 + 六环 0 违规 |
| 3-1a/3-1b/3-1c 连接泄漏 | ✅ 完成 | 三包均改为**建连函数缓存复用 + try/finally**，调用点 0 改动（字节级证明）；20 次调用未关闭连接 20→1；3-1c 额外加 fork 不复用父连接、按 `(st_dev, st_ino)` 识别库替换 |
| 3-2 权威状态回收 | ⚠️ 超时但已落盘 → 父会话核 diff（+285 行） | 7 天保留窗口、三表 DELETE、维护线程 `释放当前线程连接`、真删 8983 行、等价性 0 处不一致、端到端 0.063s |
| 3-3 事件日志有界 | ✅ 完成 | 8MB×3 分片，磁盘上界 ≈24MB（原无界）；查询改分片尾部倒读，0.0001s 不随总量线性 |
| 3-4 句柄淘汰 + R6 | ✅ 完成 | 1200 句柄全失效 → 表 1200→200（双限：300s/200 条）；`句柄体系.py` 的 `except pass` 残留 **0** |
| R6 另两处 | ✅ 完成 | `HTTP提供者` 收窄为 `OSError`+留痕；`合规测试包:953` 真故障被吞改留痕（实测：`ConnectionResetError` 留痕、`AttributeError` 正常上抛） |
| 5 判据 | ✅ 完成 | 技能库层表两张表同改；第三方前缀表 +10 包（**净增 17 条**，非清单估的 16）；排除片段改**名精确匹配**；G/H 加固；两个门禁判据 bug 修复 |
| 6-1 僵尸测试 | ✅ 完成 | 改写为现行接口 14 用例全绿（无 PG 服务也绿）；193 文件可导入失败 **1→0**；白名单无需改；另发现 psycopg 提供者 2 个真缺陷（已派修） |
| 6-2 四条假绿 | ✅ 完成 | 全部改真验证 + **变异检验**（破坏生产路径必须变红）；暴露 1 条真违规（验证场景 int/float，已派修） |
| 6-3 测试体系门禁 | ✅ 完成 | 新建门禁（全量可导入性/零测试/未解释跳过/连字符）+ 子模块 6 文件；夹具自测违规/干净/空目录/只报告/退出码 2 全对；**未接线**（待发布门禁属主空闲后接） |
| 7-1 删死模块 | ✅ 完成 | 删 3 模块 + 3 个孤立 pyc；全仓引用 **0**；父会话已清文档陈旧引用（0 处） |
| 7-2 运行库 | ✅ 完成 | 4 张恒空表定义删除（保留 `灰度状态`）；`灰度观测` 假数据行删除（聚合污染实测归零）；写入侧加「拒绝验收标识行」护栏 |

### 维修期发现的新缺陷（已派修，不属原清单）

1. `支持库/适配层/psycopg提供者/实现/提供者.py::_解析URL` 库名不做 URL 反转义；`校验连接串` 不校验 scheme。
2. `支持库/后端/文件系统支持库/文件操作/验证场景.json` 的 `最大压缩比: 1` 写成整数（契约声明 `双精度数型`）。
3. 调用示例块里整数字面量对应双精度参数约 138 处（口径问题，先只报不改）。
4. 依赖防火墙新增违规 17 条中 5 条来自「适配层根下文件未豁免」。
5. `能力调用图审计` 第三方前缀表未同步、只扫 `实现/*.py`。
6. mock 治理无任何门禁（autospec 存量 0/185）。
