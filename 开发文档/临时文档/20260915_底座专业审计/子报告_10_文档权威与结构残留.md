# 子报告 10 · 文档权威性与结构残留（2026-09-15 底座专业审计）

> 审计对象：系统工程平台（华哥口中的「底座」）　根目录 `~/Documents/Agent/PHP/系统工程平台`
> 冻结边界：分支 `主干` / HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`、工作树 0 脏（仅 `开发文档/临时文档/` 未跟踪）
> 本报告为只读审计，唯一写入 = 本文件。**未改源码/配置/运行数据/制品/数据库，未重启服务，未跑发布门禁，未重编制品。**

---

## 一句话结论

**未发现阻断级问题，但「权威文档与现场不一致」是系统性的**：9 项「判定全绿」里有 6 项可原样复现、1 项口径与现场不符（能力数 536 vs 文档 549）、2 项需跑发布门禁/写运行数据才能复现（按冻结边界跳过）；`未完成事项.md` 里**至少 5 条已修复事项仍挂在开放区（含 1 条与同文件的「判定全绿」自相矛盾）**，`项目说明.md` 抽查 16 条当前事实有 **5 条与现场不符**（包数/能力数/基线提交/上次制品/记忆能力数），结构残留除 `.bak=0` 外**普遍未清**（`__pycache__` 315、历史制品副本 12 份 186M、空目录 267 个、已删包的孤儿运行环境 4 个），目录规则有**两处硬违规**（决策记录编号重复且缺号、归档命名不合 `年月_主题`）。

### 分级计数

| 级别 | 数量 | 说明 |
|---|---|---|
| 阻断 | **0** | 无 |
| 重要 | **10** | 文档与现场矛盾 5 条、结构残留 2 条、目录规则违规 2 条、流水账/悬空引用 1 条（含合并项） |
| 建议 | **13** | 命名违规、索引不全、冗余文件、历史数字不可复现 |
| 已核实（现场成立） | **24** | 含「已核实通过」抽检 9 条、项目说明抽查 11 条 |
| 未确认/跳过 | **5** | 见文末 |

---

## 一、`未完成事项.md` 条目 vs 现场

### 1.1 「已核实通过（非债务）」抽检 9 条 —— 全部现场成立 ✅

| # | 文档条目（行号） | 复现命令 | 真实输出 | 判定 |
|---|---|---|---|---|
| A1 | 六环公开调用完整性门禁 **0 违规**（L35 / L223） | `unset PYTHONPATH; python3.14 -m 开发工具.公开调用完整性门禁` | `公开调用完整性门禁通过：六环一致，无违规`，退出码 `0` | ✅ 成立 |
| A2 | 完整性摘要 **112 包 / 不闭合 0**（L225） | `python3.14 -c "from 开发工具.组件规范.完整性摘要 import 扫描正式包,校验完整性摘要;..."`（见文末命令清单） | `正式包数 112` / `不闭合 0` | ✅ 成立（但 L36/L89 又写「116 包」→ 见 10-10） |
| A3 | 组件合规 **112 包 0 未通过**（L223） | `python3.14 -c "from 开发工具.组件合规.合规测试包 import 组件合规; ... 全量执行"` | `组件合规 包数 112 未通过 0` | ✅ 成立 |
| A4 | 契约版本唯一 **2.0.0**（L18 / L202） | `python3.14 -c "统计全部 参数契约.json 的 契约版本"` | `{'2.0.0': 106}`（106 个契约文件全部 2.0.0，无第二版本） | ✅ 成立 |
| A5 | 运行库 `工程缓存/运行数据/底座运行.db`（**10 个域表**）（L20） | `python3.14 -c "import sqlite3; sqlite3.connect('file:工程缓存/运行数据/底座运行.db?mode=ro',uri=True)"` | `10 ['任务','会话','作业','协作状态','发布','检查点索引','灰度状态','灰度观测','缓存索引','能力占用']` | ✅ 成立（含 L129 声称新增的 灰度观测/灰度状态） |
| A6 | **8 个前端包源头 `能力定义.json` 已补齐**（L233 审计项 1） | `ls 支持库/前端/*/能力定义.json` | 8 个包全部有 `能力定义.json`（基础组件描述/文件选择描述/桌面宿主/浏览器宿主/状态描述/窗口描述/组件控件/资源描述） | ✅ 成立 |
| A7 | **`重试调度` 已补可选 `实际错误码`**（L240 审计项 6） | `grep -n 实际错误码 支持库/后端/大语言模型支持库/模型连接器/包声明.json 支持库/后端/大语言模型支持库/模型连接器/__init__.py` | `包声明.json:537 "名称": "实际错误码"`；`__init__.py:112 {"名称": "实际错误码", "类型": "文本型", "必填": False}`；`说明/使用说明.md:202` 已列入参数 | ✅ 成立（但 L68/L198 仍称待裁决 → 见 10-04） |
| A8 | **办公文档 2 个夹具已重造、不含「甲」**（L242 审计项 7） | `python3.14 -c "zipfile 读取 pptx/xlsx 全部 .xml 判 甲"` | `…演示文稿/夹具/最小中文演示.pptx 不含甲` / `…表格文档/夹具/最小中文表格.xlsx 不含甲` | ✅ 成立（但 L26/L70 仍称 2 处残留在 → 见 10-03） |
| A9 | **`压缩数据库` 已作为正规原子能力补齐**（L272） | `grep -n 压缩数据库 支持库/后端/数据库连接支持库/SQLite数据库/能力定义.json` | `261: "能力id": "数据库连接支持库.SQLite数据库.压缩数据库"` | ✅ 成立 |

### 1.2 「重要事项 / 仍开放」条目 —— 仍存在性核对

| # | 条目 | 现场 | 判定 |
|---|---|---|---|
| B1 | L61 业务参数沉淀到业务侧；L62 长场次性能复测；L63 直播污染治理；L64 全自动精校缺进程内集成测试；L65 Whisper 精度标杆 | 均属编排层/业务侧或需真实长音频，本轮未复测 | 仍开放（与底座文档规则不冲突） |
| B2 | L67 制品指针语义（两份 `当前.json` 键形状不一） | 现场 `工程缓存/制品仓库/平台客户端制品/当前.json` + `平台客户端环境/当前.json` 两份均在 | 仍开放 ✅（且正文「见 58 号文档」为悬空引用 → 10-09） |
| B3 | L39 依赖生命周期审计 **17 个标准 Provider、违规 1 个 2 条**（YAML提供者） | **现场 15 个标准 Provider、0 违规**；YAML提供者已有 `能力定义.json` 且 `生命周期契约.json` 有 `停止入口` | ❌ **已修复未删** → **10-01** |
| B4 | L164-178 「运行核心里的 `except Exception: pass`（18 处，需逐点判断）」 | **全仓 `grep -rnP 'except\s+Exception\s*:\s*(?:#[^\n]*\n\s*)?pass\b'` = 0 命中** | ❌ **已修复未删** → **10-02** |
| B5 | L26 / L70 办公文档 2 夹具「改名需重造二进制夹具（待华哥定）」 | 现场夹具已重造、不含「甲」（A8） | ❌ **已修复未删** → **10-03** |
| B6 | L68 接口语义瑕疵「定稿前不动契约」；L198 #5「建议补 `实际错误码` 入参，不改名」 | 现场已补（A7） | ❌ **已修复未删** → **10-04** |
| B7 | L60「全自动精校编译产物 HTML 黑盒与发布门禁未重跑；发布前必须补跑」 | 同文件 L216-229 已判「发布门禁 通过」、L258-267 记「发布门禁 通过（退出码 0）」 | ❌ **同一文件内自相矛盾** → **10-05** |
| B8 | L66「正式源码约 600 个文件超 160 行」（P2 维护性债务） | 同条自述「2026-09-15 按哲学第 18 条定性为『非债务』」 | ❌ 定性为「非债务」却仍挂在债务清单 → 建议级（并入 10-08） |

### 1.3 「2026-09-15 审计结论：判定全绿」9 小项逐项复现

| 小项（L218-229） | 文档声称 | 本轮实测 | 判定 |
|---|---|---|---|
| 发布门禁 | 通过（制品 `0eec30284da0d3bd`，提交 `ac21c9f0`） | **按冻结边界跳过**（禁止跑发布门禁）。旁证：激活指针 `平台客户端环境/当前.json` = `{"摘要sha256":"0eec30284da0d3bd","制品目录":"平台客户端-0eec30284da0d3bd","制品摘要":"7f68175962d12ca48e01966c9c78676f","版本":76}`，且该制品目录确实存在 | **未确认（跳过）** |
| 组件合规（112 包） | 0 未通过 | `组件合规 包数 112 未通过 0` | ✅ **复现** |
| 依赖生命周期审计 | 全部通过：无违规 | `标准提供者 15 个（跳过 2 个非标准目录）` / `全部通过：无违规` | ✅ **复现**（但与同文件 L39 的「17 个 / 1 违规」矛盾 → 10-01） |
| 六环（公开调用完整性） | 0 违规 | `无违规`，退出码 0 | ✅ **复现** |
| 能力定义校验 / 契约漂移 | 106 包 0 不达标 / 0 漂移 | `能力定义.json` 文件数 **106**；全部 `参数契约.json` 契约版本 **2.0.0**（唯一） | ✅ 数量与版本口径**复现**；「漂移 0」未单独跑检测器（未确认，见文末） |
| 完整性摘要 | 112 包 0 不闭合 | `正式包数 112` / `不闭合 0` | ✅ **复现** |
| 运行态真实探针 | 连接 / 库压缩 / 编码转换 三项 HTTP 200 | **按冻结边界跳过**（库压缩 = VACUUM，会改运行数据库）。只做只读 `/健康`：`{"状态":"健康","能力数":536,"当前版本":"2.0.0"}` | **部分确认（/健康 200）** |
| 磁盘 | 工程缓存 3.1G；权威状态库 13.2MB | 工程缓存 **3.1G** ✅；权威状态库：`平台控制面/权威状态.db` = **294912 字节(0.29MB)** + `-wal` **4128272 字节**；`工程缓存/权威状态/权威状态.db` = **3493888 字节(3.33MB)** — **13.2MB 与 8.7MB（L273）两个数字均不可复现** | ⚠️ **磁盘总量成立，权威状态库数字不成立** → 10-13 |
| 制品仓库 | 内容寻址目录 1 个（只留一个权威）+ 激活指针在位 | 内容寻址目录 **1 个** ✅（`制品仓库/7f68175962d12ca48e01966c9c78676f/`）；激活指针在位 ✅；**但 `制品仓库/平台客户端制品/` 仍有 12 份历史制品副本、共 186M** | ⚠️ **部分成立** → 10-06 |
| 结构残留 | `.bak` 0；临时文档 0 份；`__pycache__` 已清 | `.bak/.orig/.old` **0** ✅；临时文档审计前 0 份 ✅；`__pycache__` **315 个（非工程缓存）** ❌ | ⚠️ **2/3 成立** → 10-11 |

---

## 二、`项目说明.md`「当前事实」抽查 16 条

| # | 文档声称（path:行号） | 现场 | 判定 |
|---|---|---|---|
| C1 | `开发文档/项目说明.md:14` 当前正式索引实测：**96 个支持库、14 个模块库、1 个技能库、合计 111 个包、549 项能力** | `开发工具/项目编译/正式包索引.py::构建索引` → `支持库 92 / 模块库 14 / 技能库 1`，**能力所有者 536**；磁盘 `包声明.json` **112** | ❌ **不一致（96→92、111→107、549→536）** → 10-10 |
| C2 | `项目说明.md:15` 完整性摘要 **116 个正式包**、不闭合 0 | **112 个正式包**、不闭合 0 | ❌ 包数不一致（与 `未完成事项.md:225` 的 112 又互斥） → 10-10 |
| C3 | `项目说明.md:13` 当前工作区基线提交 `e255e57` | HEAD = **`1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`**（`未完成事项.md:37` 同样写 `e255e57`） | ❌ 不一致且未标「历史快照」 → 10-10 |
| C4 | `项目说明.md:18` 上一次正式发布制品 `平台客户端-2ee78ea86b3fb981` | `ls -d 工程缓存/制品仓库/平台客户端制品/平台客户端-2ee78ea86b3fb981` → `No such file or directory`；现场 12 个制品目录中无此摘要 | ❌ 不可复现 → 10-10 |
| C5 | `项目说明.md:336` 记忆支持库「当前登记…**8 项能力**」 | `支持库/后端/记忆支持库/能力定义.json` = **9** 项（多出 `解析配置回退链`） | ❌ 不一致 → 10-10 |
| C6 | `项目说明.md:440` 「**549 个正式能力**全部可作为后台任务提交」 | 网关 `GET /健康` 只读实测 `能力数 536` | ❌ 不一致 → 10-10 |
| C7 | `项目说明.md:20/462` 40007 唯一对外业务执行网关、launchd 常驻 | `lsof -nP -iTCP -sTCP:LISTEN` → `Python 50802 … TCP 127.0.0.1:40007 (LISTEN)` | ✅ 成立 |
| C8 | `项目说明.md:292/295` MCP 8766 `http://127.0.0.1:8766/mcp/` | LISTEN `Python 40029 … 127.0.0.1:8766` | ✅ 成立 |
| C9 | `项目说明.md:265` `4780` 是宿主代理保留端口，任何应用不得监听 | `ClashX 6979 … TCP *:4780 (LISTEN)`（代理自身监听，符合保留语义） | ✅ 成立 |
| C10 | `项目说明.md:471` plist WorkingDirectory = 正式平台目录 | `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `<string>~/Documents/Agent/PHP/系统工程平台</string>` | ✅ 成立 |
| C11 | `项目说明.md:11` `模块库/_模板` 已删除 | `ls 模块库/` 无 `_模板` | ✅ 成立 |
| C12 | `项目说明.md:305` 技能库为第三根正式包根 | `技能库/后端/技能库/包声明.json` 存在，类型「支持库」；正式索引含 `技能库 1` | ✅ 成立 |
| C13 | `项目说明.md:19` 当前稳定指针 `工程缓存/制品仓库/平台客户端环境/当前.json` | 文件存在，内容见 1.3 表 | ✅ 成立 |
| C14 | `项目说明.md:9` 编译器当前版本 `1.4.0` | `开发工具/项目编译/项目编译器.py:30` → `编译器版本 = "1.4.0"` | ✅ 成立 |
| C15 | `项目说明.md:460` 外部源码研究保留 **168 份**项目级 `ARCHITECTURE.md` | `find 开发文档/参考资料/外部源码研究 -name ARCHITECTURE.md \| wc -l` → `168` | ✅ 成立 |
| C16 | `项目说明.md:139` 说明书「两个权威」**112/112 包**完成 | `设计说明.md` **112** 份（正好每包一份）；`使用说明.md` **113** 份 —— 多出的一份在 `模块库/直播逐字稿/工具/使用说明.md`（该包内 2 份） | ⚠️ 口径基本成立，存在 1 份冗余 → 10-10 |

---

## 三、`AGENTS.md` 与 `项目说明.md` 的真实矛盾点

> AGENTS.md:`85-87` 自声明「本仓库内部权威冻结文档（`项目说明.md`）；本文件只允许与其一致，**不一致时以冻结文档为准并在同一任务内修复本文件**」。

### 10-14【重要】「只保留一套权威文档」规则与现场 15 份顶层 .md 不一致
- **影响**：AGENTS.md 是硬规则文件，其声明的权威文档范围（4 份）与现场（15 份顶层 `.md`，另有 3 份自称「唯一权威」）不一致，新会话无法据此判断哪份可信。
- **证据**：`AGENTS.md:197` 只列 4 份（项目说明/未完成事项/当前流程图/主开发文档）；现场 `ls 开发文档/*.md` = 15 份，其中 `开发文档/底座升级方案.md:1` 自称「底座升级执行计划（唯一权威）」、`开发文档/资源回收确认机制.md` 被 `主开发文档.md:16` 标为「唯一权威」、`开发文档/通用能力反馈接口与平台自治治理.md:15` 同为权威条目。
- **复现命令**：`ls -1 开发文档/*.md | wc -l` → `15`；`sed -n '197p' AGENTS.md`
- **判定**：已核实。AGENTS.md 未按自己的条款「在同一任务内修复本文件」。

### 10-15【重要】`项目说明.md` 违反自己（及 AGENTS）的「不保留流水账」条款
- **影响**：唯一事实源被历史批次/审计流水污染，读者会把已失效的历史数字当成当前事实（C1-C6 的不一致全部由这类残留引起）。
- **证据**：`项目说明.md:3` 自述「历史修复、阶段过程和审计流水不在本文件保留」；实际仍保留 `:16`「本轮验收证据（2026-09-10，历史快照）」、`:23-25`「已完成审计批次 A/B/C/D/E」「已完成底座增强批次…」、`:188-191`、`:202-203`。AGENTS.md:198 同口径「已修复事项从债务清单直接删除，不留流水账」。
- **复现命令**：`grep -n "已完成审计批次\|已完成底座增强批次\|本轮验收证据" 开发文档/项目说明.md`
- **判定**：已核实。

### 10-16【建议】数字标注规则（实测 / 历史快照 + 取证命令）大面积未执行
- **影响**：无法区分「当前事实」与「旧快照」，是 C1-C6 不一致的制度性原因。
- **证据**：`AGENTS.md:201` 要求「权威文档里的数字必须标清『实测』还是『历史快照』并给出取证命令」。实际：`项目说明.md:14-18` 只写「（2026-09-11）」，无取证命令；`未完成事项.md:20` 写「工程缓存 3.5G」而现场 `du -sh 工程缓存` = **3.1G**，同文件 `:227` 又写 3.1G；`未完成事项.md:16` 写「能力定义源头 515 能力」而现场 `能力定义.json` 条目合计 **536**。
- **复现命令**：`du -sh 工程缓存` → `3.1G`；见文末 D1 命令（条目汇总）
- **判定**：已核实。

---

## 四、结构残留与命名清点

### 10-11【重要】`__pycache__` 315 个未清（文档声称「已清」）
- **影响**：`未完成事项.md:229` 把「`__pycache__` 已清」作为「结构残留」审计项的通过证据；现场不成立。虽然是 gitignore 覆盖项、不影响仓库洁净度，但**该条通过证据不可复现**。
- **证据**：`find . -name '__pycache__' -type d -not -path './工程缓存/*' | wc -l` = `315`；`*.pyc` = `594`。
- **mtime 直方图（判断是否刚生成）**：`find … -exec stat -f '%Sm' -t '%H:%M'` → **`2  13:14`、`313  13:33`** —— 313 个集中在同一分钟，是被审计过程之外的**批量导入/编译扫描一次性生成**（`未完成事项.md` 落盘时间 13:31，`git log` 收口提交 13:45），故属于「文档声称清理之后又被自动扫描生成」，而非 8 月的陈旧残留。
- **复现命令**：`find . -name '__pycache__' -type d -not -path './工程缓存/*' -exec stat -f '%Sm' -t '%Y-%m-%d %H:%M' {} \; | sort | uniq -c`
- **判定**：已核实（真实输出：`315 个，313 个为 2026-09-15 13:33`）。

### 10-06【重要】制品仓库残留 12 份历史制品副本（186M），与「只留一个权威」矛盾
- **影响**：`未完成事项.md:228` 以「内容寻址目录 1 个（只留一个权威）」作为「制品仓库」审计项通过证据；`:282` 声称「制品仓库历史副本与信任链多份→各留一份」；`项目说明.md:250`（哲学 22）「不做本地归档，旧版直接删」。现场 12 份并列副本仍在，占 **186M**，且会误导「回滚只切换不可变旧制品」的判断。
- **证据（真实输出）**：`工程缓存/制品仓库/平台客户端制品/` 下
  `平台客户端-0eec30284da0d3bd(16M)`、`-1246f3724c0f18c4(15M)`、`-196e13bced099f3a(16M)`、`-31004a8293cfc801(15M)`、`-3115d8c77ebdf54b(15M)`、`-3c336f5dfabf5e95(16M)`、`-5af9543caaa12140(15M)`、`-5df7cd7cd887927a(15M)`、`-6a6b9814527de6d1(15M)`、`-bfd1a9b042d1b7fb(16M)`、`-d55ec84645dbde35(16M)`、`-f705f14f5f8e4cc7(15M)` + `当前.json`；顶层内容寻址目录只有 `7f68175962d12ca48e01966c9c78676f`（1 个）。激活指针指向 `-0eec30284da0d3bd`。
- **复现命令**：`du -sh 工程缓存/制品仓库/平台客户端制品/平台客户端-*/ | sort -rh; ls -d 工程缓存/制品仓库/*/ | grep -v 平台客户端`
- **判定**：已核实。

### 10-17【建议】`工程缓存/验证运行/` 267 个空目录，未在任何权威文档登记
- **证据**：`ls -A 工程缓存/验证运行 | wc -l` = `267`；`du -sh 工程缓存/验证运行` = `0B`；mtime 集中在 `2026-08-29`。
- **复现命令**：`ls -lt 工程缓存/验证运行 | tail -3`
- **判定**：已核实。

### 10-18【建议】`工程缓存/协作状态/` 迁移后遗留空目录
- **证据**：`未完成事项.md:143` 称「`工程缓存/协作状态/` 25 个历史 JSON … → 删除（不再留两条腿）」；现场目录仍在、内容 `0` 项。
- **复现命令**：`ls -A 工程缓存/协作状态 | wc -l` → `0`
- **判定**：已核实。

### 10-19【建议】`工程缓存/提供者运行环境/` 存在 4 个「已删包」的孤儿环境目录
- **证据**：`未完成事项.md:75` 记录 `适配层/pg8000提供者`、`适配层/psycopg2提供者`、`后端/pg8000数据库`、`后端/psycopg2数据库` 四包**已整包删除**；现场 `工程缓存/提供者运行环境/` 仍有 `pg8000提供者`、`pg8000数据库`、`psycopg2提供者`、`psycopg2数据库` 四个目录（mtime `Sep 15 12:09`）。
- **复现命令**：`ls 工程缓存/提供者运行环境/ | grep -E 'pg8000|psycopg2'`
- **判定**：已核实。

### 10-20【建议】命名违规（非中文文件名）
- **规则**：`AGENTS.md:143-149`「全中文命名：变量、函数、类、文件、目录、注释一律中文；只保留 ID、PPT、OCR、LLM 这类人人认识的缩写」。
- **已核实的违规样本**：
  - `开发文档/参考截图/易语言IDE/07ac3dd5766f806e6e751b73db192259.png` —— 纯 hex 文件名。
  - `开发文档/决策记录/0008_数据库连接池唯一化与anthropic协议.md` —— 决策文件名含英文 `anthropic`。
  - `开发文档/项目证据/底座升级/P0-能力重复扫描.json`、`B0-浏览器自动化基线.json` —— 英文前缀 + 半角连字符。
  - `开发文档/交付记录/20260914_数据库连接池与anthropic协议/` —— 交付记录目录名同上。
  - 系统性项：`Agent查询数据.json` **106 份**（每包一份）、`正向HTTP场景.json` **42 份**。`HTTP` 属「外部协议」可豁免；`Agent` 不在 `AGENTS.md:147` 明列的例外缩写内。
- **复现命令**：`python3.14` 脚本扫描正式目录 basename 含 ASCII 字母（见文末命令清单）；输出「含英文的非豁免文件总数 **224**」。
- **判定**：已核实（数量与样本）。豁免判断（HTTP/Git 夹具）需华哥口径。

### 10-21【建议】`.bak/.orig/.old` = 0（该项通过证据成立）
- **证据**：`find . \( -name '*.bak*' -o -name '*.orig' -o -name '*.old' \) -not -path './工程缓存/*' -not -path './.git/*'` → 无输出。
- **判定**：已核实（与 `未完成事项.md:229` 一致）。

### 4.x 误放文件 / 调试输出 / 一次性脚本 —— 未发现
- `find . -name '.DS_Store'`（非工程缓存）= **0**；`*.tmp` / `.测试值.*` / `测试输出*` 在正式目录 = **0**。
- 根目录唯一日志为 `.codegraph/daemon.log`（被 `.codegraph/.gitignore` 覆盖，`git ls-files .codegraph` 仅含 `.gitignore` 与 `codegraph.db` 未跟踪），属代码地图工具自身产物。
- 疑似「一次性脚本」命中 4 处（`支持库/适配层/系统探针.py`、`Tesseract提供者/实现/临时文件.py`、`OCR识别支持库/OCR识别/实现/临时文件.py`、`MCP工具箱/临时上下文.py`）—— 实为正式模块（命名含「临时/探针」），**不是** 哲学第 17 条所指的一次性脚本。仓内无「探针/搬迁/scratch」类裸脚本。
- **判定**：已核实（无此类残留）。

---

## 五、目录与文档数量对账

### 10-07【重要】`决策记录/` 编号重复且缺号，违反「四位递增不复用」
- **影响**：`AGENTS.md:199` 明定「编号四位递增且不复用」。现场 0008、0009 各占 2 份、缺 0010，导致「引用决策编号」不可靠。
- **证据（真实输出）**：`0008_数据库连接池唯一化与anthropic协议.md` 与 `0008_能力更新走热接入不重启网关.md`；`0009_依赖分两段_第三方翻译层与环境依赖层.md` 与 `0009_技能库落地.md`；编号序列 `0001-0007,0008×2,0009×2,0011`（**无 0010**）。另有 `模板.md`（未编号，`主开发文档.md:21` 已登记，可接受）。
- **复现命令**：`ls 开发文档/决策记录/ | grep -oE '^[0-9]{4}' | sort | uniq -c`
- **判定**：已核实。

### 10-22【建议】归档命名不合 `年月_主题`
- **规则**：`AGENTS.md:200`「归档只保留一个顶层 `开发文档/归档/`，按 `年月_主题` 命名」。`主开发文档.md:81` 同口径。
- **证据**：`开发文档/归档/2026-08_第十四阶段/`（`2026-08` 非 `年月` 连续数字格式，且内容为 5 个 JSON：`公开操作契约.json` 等，非文档）；另一份 `开发文档/归档/202609_系统工程平台审计/`（格式合规，内含 `审计报告_20260911.md`、`旁路问题_待下一批.md`）。**2 份归档中 1 份违规**，即「出现第二套并行命名」。
- **复现命令**：`find 开发文档/归档 -maxdepth 2 | sort`
- **判定**：已核实。

### 10-23【建议】`主开发文档.md` 正文引用 `归档/`，与自身规则冲突
- **证据**：`主开发文档.md:20` 表格行 `| [归档/](./归档/) | 历史阶段资料（按 年月_主题 组织 …） |`；而**同一文件** `:79` 明写「归档类文件不得被本文件正文引用」，`AGENTS.md:200` 同款。
- **判定**：已核实（规则冲突；若认定「目录索引行不算引用」需在规则里写明，目前两者互斥）。

### 10-24【建议】`主开发文档.md` 索引不全，缺 5 份顶层文档
- **证据**：`主开发文档.md:9-27` 索引表未收录 `开发文档/多会话并行开发规约.md`、`提速方案.md`、`新建支持库与模块指南.md`、`能力目录说明书.md`、`待重启事项_技能库目录归位.md`（5 份）。
- **复现命令**：`ls -1 开发文档/*.md`（15 份）与 `主开发文档.md` 索引条目对比
- **判定**：已核实。

### 10-25【建议】`待重启事项_技能库目录归位.md` 已完成却仍留顶层
- **证据**：该文件 `:3` 自述「状态：**✅ 已完成（2026-09-11 01:2x 执行）**」。按 `AGENTS.md:198` 应在结论并入权威文档后删除、或按 `:200` 归入 `归档/`。
- **判定**：已核实。

### 10-26【建议】`交付记录/` 命名与规则基本一致
- **证据**：6 个目录全部为 `年月日_主题`：`20260911_模型HTTP提供者生成参数补齐`、`20260911_热接入子包归属修复`、`20260914_数据库收口与网关空转清理`、`20260914_数据库连接池与anthropic协议`、`20260914_编译器非破坏与清理引用门`、`20260915_底座收口夜班`。文件形态不统一（前 2 个 5 件套 `交付报告/修改文件清单/元信息.json/剩余风险/验收命令结果`，后 4 个单一 `验收记录.md`）——不属命名违规。
- **判定**：已核实（命名合规，仅 `anthropic` 英文一项见 10-20）。

### 10-27【建议】`参考资料/`、`参考截图/` 分类名合规
- **证据**：`开发文档/参考资料/外部源码研究/…`（168 份 `ARCHITECTURE.md`）、`开发文档/参考截图/易语言IDE/…`，分类目录均为中文 ✅。截图文件名多为长中文句（可接受），仅 1 个 hex 名违规（见 10-20）。
- **判定**：已核实。

---

## 六、临时文档现状

- **审计前**：`开发文档/临时文档/` 为空（`git status --porcelain` 仅报 `?? 开发文档/临时文档/`，即审计开始时新建的空目录被登记为未跟踪）。
- **审计中**：现有 3 个文件（本目录即本次审计工作台）
  - `开发文档/临时文档/20260915_底座专业审计/00_审计总纲与边界.md`（父会话）
  - `开发文档/临时文档/20260915_底座专业审计/子报告_00_父会话独立复核.md`（父会话）
  - `开发文档/临时文档/20260915_底座专业审计/子报告_10_文档权威与结构残留.md`（本报告）
- **与规则的关系**：`AGENTS.md:198` 允许临时工作台存在，但「结论并入权威文档后立即删除」。**本目录属合规的临时工作台，收口时必须删除**；`未完成事项.md:11/250` 已声明 57/58 号按此规矩删除。
- **10-09【重要】正文对已删临时文档的悬空引用**：`未完成事项.md:67`「见 58 号文档」、`:190`「剩余项见上『债务全景图』与 57／58 号临时工作台」、`:192`「判据与建议见 57 号文档 B 组」——而 `:250` 已声明 57/58 号**已收口删除**，现场 `开发文档/临时文档/` 内无 57/58 号。三处引用全部悬空。
  - **复现命令**：`grep -n "57 号\|58 号\|57／58" 开发文档/未完成事项.md`
  - **判定**：已核实。

---

## 七、逐条问题汇总（按级别）

### 重要（10 条）

| 编号 | 标题 | 一句话影响 |
|---|---|---|
| 10-01 | 依赖审计条目「17 Provider / 违规 1 个 2 条」已修复未删 | 债务清单挂着不存在的问题，且与同文件「全绿」互斥 |
| 10-02 | 运行核心 18 处 `except Exception: pass` 已修复未删 | 现场 grep = 0，条目应删 |
| 10-03 | 办公文档 2 夹具含「甲」已修复未删 | 现场不含「甲」，`:26/:70` 与 `:242` 自相矛盾 |
| 10-04 | 「重试调度」待裁决已修复未删 | `实际错误码` 已落地，`:68/:198` 与 `:240` 自相矛盾 |
| 10-05 | 「全自动精校发布门禁未重跑」与「判定全绿」自相矛盾 | 同一文件两处结论互斥 |
| 10-06 | 制品仓库 12 份历史制品副本 186M | 违反「只留一个权威 / 旧版直接删」 |
| 10-07 | 决策记录编号 0008/0009 重复、缺 0010 | 违反「四位递增不复用」 |
| 10-08 | 未完成事项保留约 50 行已修复/已核实流水账 | 违反「不留流水账」，是 10-01~10-05 的成因 |
| 10-09 | 正文悬空引用已删除的 57/58 号临时文档 | 3 处引用指向不存在的文件 |
| 10-10 | 项目说明 5 处关键数字/提交与现场不符 | 唯一事实源失真（111包/549能力/116包/e255e57/2ee78ea8）+ 记忆 8→9 |

### 建议（13 条）

| 编号 | 标题 |
|---|---|
| 10-11 | `__pycache__` 315 个未清（文档称已清；13:33 批量再生） |
| 10-12 | 未完成事项 `:16`「能力定义源头 515」与 `:20`「工程缓存 3.5G」不可复现（实测 536 / 3.1G） |
| 10-13 | 权威状态库大小 13.2MB / 8.7MB 均不可复现（实测 0.29MB+4.1MB WAL / 3.33MB） |
| 10-14 | AGENTS「只保留一套权威文档（4 份）」与现场 15 份顶层 .md 不一致 |
| 10-15 | 项目说明违反自身「不保留流水账」条款 |
| 10-16 | 数字标注（实测/历史快照 + 取证命令）规则大面积未执行 |
| 10-17 | `工程缓存/验证运行/` 267 个空目录（0B，2026-08-29） |
| 10-18 | `工程缓存/协作状态/` 迁移后遗留空目录 |
| 10-19 | `提供者运行环境/` 4 个已删包的孤儿环境目录 |
| 10-20 | 命名违规（hex png、`anthropic`、`P0-`/`B0-`、`Agent查询数据.json`×106） |
| 10-22 | 归档 `2026-08_第十四阶段` 不合 `年月_主题` |
| 10-23 | `主开发文档.md:20` 引用 `归档/` 与 `:79` 自定规则冲突 |
| 10-24 | `主开发文档.md` 索引缺 5 份顶层文档 |
| 10-25 | `待重启事项_技能库目录归位.md` 已完成仍留顶层 |
| 10-27b | 依赖契约.json 实际 17 份、`未完成事项.md:140` 称「保留 16 个」 |
| 10-27c | `模块库/直播逐字稿` 有 2 份 `使用说明.md`（`说明/` 与 `工具/`），打破「112/112 每包一份」 |

---

## 八、命令清单（全部只读，可逐条复现）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
GIT=/Library/Developer/CommandLineTools/usr/bin/git

# 0) 冻结边界与工作树
$GIT log --oneline -1 ; $GIT rev-parse --abbrev-ref HEAD ; $GIT status --porcelain

# 1) 包数与能力数
find . -name '包声明.json' -not -path './工程缓存/*' | wc -l          # 112
find . -name '能力定义.json' -not -path './工程缓存/*' | wc -l        # 106
unset PYTHONPATH
python3.14 -m 开发工具.公开调用完整性门禁                              # 无违规，退出码 0
python3.14 -m 开发工具.依赖生命周期审计.运行审计                        # 标准提供者 15 / 全部通过：无违规
curl -s http://127.0.0.1:40007/健康 -H 'X-System-Credential: html-blackbox-verifier'   # 能力数 536

# D1) 能力定义条目汇总（106 文件 / 536 条）
python3.14 - <<'EOF'
import json,glob
tot=files=0
for p in glob.glob('**/能力定义.json',recursive=True):
    if p.startswith('工程缓存/'): continue
    files+=1; d=json.load(open(p,encoding='utf-8'))
    tot+=len(d.get('能力') or d.get('能力列表') or [])
print('能力定义文件',files,'条目合计',tot)
EOF

# D2) 正式索引（支持库 92 / 模块库 14 / 技能库 1 / 能力所有者 536）
python3.14 -c "
from pathlib import Path
from 开发工具.项目编译.正式包索引 import 构建索引
i=构建索引(Path.cwd()); print(len(i['支持库']), len(i['模块库']), len(i['技能库']), i['能力所有者'])"

# D3) 完整性摘要全量校验（112 包 / 不闭合 0）
python3.14 -c "
from pathlib import Path
from 开发工具.组件规范.完整性摘要 import 扫描正式包, 校验完整性摘要
ps=扫描正式包(Path.cwd()); bad=[p for p in ps if not 校验完整性摘要(p)[0]]
print('正式包数', len(ps), '不闭合', len(bad))"

# D4) 组件合规全量（112 包 / 0 未通过）
python3.14 -c "
from pathlib import Path
from 开发工具.组件规范.完整性摘要 import 扫描正式包
from 开发工具.组件合规.合规测试包 import 组件合规
ps=扫描正式包(Path.cwd())
print('组件合规 包数', len(ps), '未通过', sum(0 if 组件合规(p).执行().成功 else 1 for p in ps))"

# D5) 契约版本分布（全 2.0.0）
python3.14 -c "
import json,collections
from pathlib import Path
c=collections.Counter()
for p in Path.cwd().rglob('参数契约.json'):
    if '工程缓存' in p.parts: continue
    c[json.loads(p.read_text(encoding='utf-8')).get('契约版本')]+=1
print(dict(c))"

# D6) 运行库域表（10 个）
python3.14 -c "
import sqlite3
c=sqlite3.connect('file:工程缓存/运行数据/底座运行.db?mode=ro',uri=True)
r=[x[0] for x in c.execute(\"select name from sqlite_master where type='table'\")]
print(len(r), r)"

# D7) except Exception: pass 全仓计数（0）
grep -rnP 'except\s+Exception\s*:\s*(?:#[^\n]*\n\s*)?pass\b' --include=*.py . | grep -v 工程缓存 | grep -v __pycache__ | wc -l

# D8) 结构残留
find . -name '__pycache__' -type d -not -path './工程缓存/*' | wc -l                                  # 315
find . -name '__pycache__' -type d -not -path './工程缓存/*' -exec stat -f '%Sm' -t '%H:%M' {} \; | sort | uniq -c   # 2×13:14 / 313×13:33
find . \( -name '*.bak*' -o -name '*.orig' -o -name '*.old' \) -not -path './工程缓存/*' -not -path './.git/*'  # 空
ls -A 工程缓存/验证运行 | wc -l                                                                        # 267
ls -A 工程缓存/协作状态 | wc -l                                                                        # 0
ls -d 工程缓存/制品仓库/*/ | grep -v 平台客户端                                                         # 仅 1 个内容寻址目录
du -sh 工程缓存/制品仓库/平台客户端制品/平台客户端-*/ | sort -rh                                        # 12 份 15-16M，合计 186M
ls 工程缓存/提供者运行环境/ | grep -E 'pg8000|psycopg2'                                                 # 4 个已删包残留

# D9) 文档规则核对
ls 开发文档/决策记录/ | grep -oE '^[0-9]{4}' | sort | uniq -c          # 0008×2 / 0009×2 / 无 0010
find 开发文档/归档 -maxdepth 2 | sort                                   # 2026-08_第十四阶段 命名不合规
grep -n "57 号\|58 号\|57／58" 开发文档/未完成事项.md                    # 3 处悬空引用
find 开发文档/临时文档 -type f                                          # 审计前为空；现 3 份审计工作台
ls -1 开发文档/*.md | wc -l                                             # 15（AGENTS 只认 4 份权威）

# D10) 夹具不含「甲」
python3.14 -c "
import zipfile,glob
for p in glob.glob('支持库/后端/办公文档支持库/*/夹具/*'):
    if p.lower().endswith(('.pptx','.xlsx')):
        z=zipfile.ZipFile(p)
        print(p, '含甲' if any('甲' in z.read(n).decode('utf-8','ignore') for n in z.namelist() if n.endswith('.xml')) else '不含甲')"
```

---

## 九、跳过项（按冻结边界）

| 跳过项 | 原因 |
|---|---|
| 发布门禁 `python3.14 开发工具/发布门禁/运行发布门禁.py` | 冻结边界明令禁止跑发布门禁 |
| 重编制品 / 编译缓存 | 冻结边界禁止重编制品 |
| 运行态真实探针三项（连接 / **库压缩** / 编码转换） | 「库压缩」= `压缩数据库`(VACUUM)，会改写运行数据库文件，属「禁止改运行数据」；只以只读 `GET /健康` 代替 |
| `测试中心` 全量 unittest | 冻结边界 + 与本文档权威性主题无关 |
| 重启 40007 / 8766 | 冻结边界禁止重启服务 |
| `开发文档/参考资料/外部源码研究/**` 内部 `.git`、hash 命名、空目录 | 外部源码研究副本，`项目说明.md:321` 明定「只读研究资料，不是正式包/运行时/能力目录」，其中英文与 hash 名属外部仓库固有，不计命名违规 |

---

## 十、未确认项

| # | 未确认内容 | 缺什么证据 |
|---|---|---|
| U1 | 「2026-09-15 审计结论」第 1 项 **发布门禁 通过**（制品 `0eec30284da0d3bd`、提交 `ac21c9f0`） | 需跑发布门禁（被冻结边界禁止）；仅确认激活指针与制品目录存在 |
| U2 | 第 5 项 **契约漂移 0 包** | 未跑 `开发工具/契约编译/漂移检测.检测能力定义漂移` 全仓扫描（本轮只跑了契约版本唯一性） |
| U3 | 第 7 项 **运行态真实探针三项 HTTP 200** | 未跑（涉及 VACUUM 写库）；只确认 `/健康` 200 与 `当前版本 2.0.0` |
| U4 | 权威状态库「53.9MB → 8.7MB」（`未完成事项.md:273`）与「13.2MB」（`:227`）两处数字 | 两个库文件实测 294912B(+WAL 4128272B) 与 3493888B，均不匹配；不清楚文档「权威状态库」指的是哪一个物理文件（可能是当时的其他时刻值），**需文档方指明口径** |
| U5 | `未完成事项.md:226` 「运行态真实探针…三项均 HTTP 200」的**实施时间点**与当前 40007 实例（PID 50802）是否同一份代码 | 未比对网关进程启动时刻与 HEAD |

---

## 十一、本轮对权威文档的建议改动（仅建议，未改动）

1. `未完成事项.md`：删除 L39、L60、L68、L164-178、L198#5、L26/L70 六处已修复/已过时条目（10-01~10-05）；删除 L66（已定性非债务）；删除 L67/L190/L192 对 57/58 号的悬空引用（10-09）；把「已核实通过（非债务）」整节压缩为一句并移出（10-08）。
2. `项目说明.md`：把 L14/L15/L440 的包数与能力数改为实测值（正式索引 92/14/1=107、能力所有者 536、`包声明.json` 112）；L13 基线提交改 `1c69ea5d` 并标「实测」；L18 上次制品改为「以激活指针为准」（现指针 `0eec30284da0d3bd`）；L336 记忆支持库改 9 项；补充 L14/L15 的取证命令（10-10、10-16）。
3. `主开发文档.md`：补录 5 份顶层文档；`归档/` 行去掉或修改 `:79` 规则措辞（10-23、10-24）。
4. `AGENTS.md`：把「一套权威文档」的边界写清（是 4 份绝对权威 + 若干专题权威，还是严格 4 份），并同步 `:197`（10-14）。
5. 结构清理（华哥裁定后执行）：删 12 份历史制品副本（10-06）、267 个空验证目录（10-17）、空协作状态目录（10-18）、4 个孤儿提供者环境（10-19）、315 个 `__pycache__`（10-11）；决策记录重编号或把重复编号的 2 组改为「0008/0010」「0009/0012」（10-07）；`归档/2026-08_第十四阶段` 改名 `202608_第十四阶段`（10-22）。
