# 落点清单 06 · 运行态与网关（F-01～F-11 二次证真 + 修复落点）

> 对象：系统工程平台（底座）· 冻结 HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`（主干）
> 上游：`开发文档/临时文档/20260915_底座专业审计/子报告_06_运行态与网关真实HTTP.md`
> 本轮性质：**只读二次证真**（不重启服务、不改源码/配置/运行数据、不跑发布门禁）。唯一写入 = 本文件。
> 二次证真时刻：**2026-09-15 14:41–14:48 CST**（`date` 实测）；凭证取自 `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables.系统库网关凭证 = html-blackbox-verifier`。
> 只读探针一律：`POST http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8`，头 `Authorization: Bearer html-blackbox-verifier` + `Content-Type: application/json`。

## 二次证真总判定

| 编号 | 级别 | 上游判定 | 本轮二次证真判定 | 备注 |
|---|---|---|---|---|
| F-01 | 阻断 | 40007 内 `模型连接器` 跑旧实现 | **真（三重复现，黑盒行为逐字一致）** | 无修正 |
| F-02 | 阻断 | 8766 进程滞后 3 天 | **真（进程时间线 + 8 模块 mtime 复现）** | 无修正 |
| F-03 | 重要 | 错误码表不齐/状态码分裂 | **真（主体成立）**，但「7 码代码内零出现」子主张**部分误报**（实测只有 4 码零出现） | 见下「本轮修正」 |
| F-04 | 重要 | KeyError 全局映射语义错位 | **真（两条路径逐字复现）** | 无修正 |
| F-05 | 重要 | 「唯一网关」不成立 | **真（40006 常驻 LISTEN、文档 0 命中、非执行腿）** | 口径落点已按要求收窄 |
| F-06 | 重要 | 装配数 536 vs 文档 549/498 | **真（`/健康`=536 + 磁盘 106/536 双口径闭合）** | 无修正 |
| F-07 | 重要 | CORS 生产未授权任何来源 | **真（OPTIONS 403 复现，无 Allow-* 头）** | 无修正 |
| F-08 | 重要/运维 | 审计落盘随 TMPDIR 漂移 | **真（TMPDIR + 双落点复现）** | 定位到源码行 |
| F-09 | 建议 | 错误说明泄漏内部签名 + 两套文案 | **真（两条文案逐字复现）** | 无修正 |
| F-10 | 建议 | 必填校验先于类型校验 | **真（补齐必填后才报类型错）** | 无修正 |
| F-11 | 建议 | 账本内码 ≠ 网关公开码 | **真（账本 `已取消` vs 公开 `调用已取消`）** | 无修正 |

**本轮修正（对上游报告的 3 处收窄）**
1. **F-03 子主张收窄**：上游称「公开表里 7 个码代码内零出现」。实测 `调用已取消`（`网关核心.py:632` 任务取消失败分支）、`提供者崩溃`（`流式HTTP.py:286` 通道失败）、`版本冲突`（`资源协调/服务.py:203,229` 失效句柄原因）**均在代码内出现**。真正零出现（表外不可达）的只有 **4 个**：`请求结构错误`、`契约不存在`、`契约版本不兼容`、`资源释放失败`。→ 上游该行应改「7 个」为「4 个」。
2. **F-03 漏登码补 2 个**：另有 `状态版本冲突`（`本地网关.py:474`，能力反馈路由）与 `句柄失效`（模块域，探针实测 500）**两张表都不在**，上游未列全。→ 表外码由 6 个修正为 **8 个**。
3. **F-05 定名**：40006 实测 `GET /健康` 与 `POST /网关/调用` 均回 `路由不存在`（`执行入口已迁移到 /网关/调用，未定义路由`）→ 确认为**只读能力发现/工具网关**，**不是第二条执行腿**。落点只按「文档端口与实跑不一致」处理。

---

## 一、逐条落点

### F-01（阻断）40007 内已装配 `模型连接器` 跑旧实现，新参数被静默丢弃仍回 200

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | ① 进程：`ps -o pid,lstart,command -p 50802` → `Tue Sep 15 12:42:12 2026`，`lsof -nP -iTCP:40007 -sTCP:LISTEN` → PID 50802 LISTEN 127.0.0.1:40007。② 进程后改动：`find . -name "*.py" -newermt "2026-09-15 12:42:12" -not -path "./工程缓存/*" -not -path "./开发文档/*"` → 4 个：`模型连接器/实现/模型连接器.py` `Sep 15 13:16:40`、`模型连接器/__init__.py` `13:16:53`、`开发工具/组件规范/完整性摘要.py` `13:24:32`、`开发工具/契约编译/能力定义编译器.py` `13:24:49`（后两个是构建期工具，不在运行加载路径）。网关层五文件 mtime 全早于 12:42：`本地网关.py 00:35:18`、`网关核心.py 01:40:36`、`流式HTTP.py 11:00:55`、`安全边界.py 09-08 22:38:49`、`启动运行核心网关.py 09-12 01:33:33` → 网关骨架=当前源码。③ 契约是新的：`能力详情` → `"包版本": "1.2.3"`，参数表第 6 项 `{"名称":"实际错误码","类型":"文本型","必填":false}`，说明含「传了则模式1 按「实际错误码∈白名单」判定命中」。④ **行为是旧的（关键）**：传 `实际错误码="限流"`、白名单 `["超时"]` → **HTTP 200**，`值={"是否重试": true, "下次退避秒": 0.55, "原因": "白名单命中：可重试错误码 ['超时']（上限 3 次）；退避计划=[0.55, 1.1, 2.2]"}`（=旧分支文案）。⑤ 反向：传 `实际错误码=123`（应报类型错）→ **HTTP 200 成功**，参数被完全忽略。⑥ 旁证：`安全审计.jsonl` 全量 12677 行，`grep -c 热接入` → **0**（进程启动后从未热接入）。 |
| **判定** | **真**（时间线 + 契约读盘机制 + 黑盒行为三重独立复现） |
| **修复落点** | **首选（免重启）**：`POST /网关/热接入`（允许操作表第 13 项，权限位「调用」）→ 触发 `网关核心._处理热接入请求` 增量装配，把 `支持库/后端/大语言模型支持库/模型连接器` 重装进内存注册表。<br>**兜底（需重启）**：`launchctl kickstart -k gui/501/com.huashi.gateway-40007`（启动即全量扫 `支持库/模块库/技能库`，内存注册表自动换新）。<br>**配套（防复发，最小改法）**：`后端核心/后端核心.py:226-229` `启用包指纹校验()` 已实现，但开关 `系统底座_指纹校验` 未写进 `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 的 `EnvironmentVariables` → 加一行 `系统底座_指纹校验=1` 即可让「源码与装配不一致」至少可被检出（注意：须评估指纹表初始化开销，属配置改动，需华哥批）。<br>**注意**：`能力详情` 走 `后端核心.py:678-690` 每次实时读盘 `包目录/能力契约/参数契约.json`，所以「契约看起来是新的」永远不能作为「实现是新的」的证据——恢复后必须用行为探针（下）验收。 |
| **影响面** | 装配范围限定 `支持库.后端.大语言模型支持库.模型连接器`（21 个能力，含 `重试调度`、`生成对话`、流式链路）。**不影响**网关层与其余 105 包（进程启动后 mtime 无变化）。**契约风险面**：任何按新契约传 `实际错误码` 的上层调用目前都在静默失效（跨仓引用面属其他子报告）。 |
| **是否共享文件** | 是。`模型连接器/实现/模型连接器.py`、`模型连接器/__init__.py`、`能力契约/参数契约.json`、`能力定义.json` 同属「大语言模型支持库」共享文件；热接入/重启会连带刷新同包其余 20 个能力。 |
| **验证命令** | **生效判定（三连，缺一不可）**：<br>```A='Authorization: Bearer html-blackbox-verifier'; C='Content-Type: application/json'```<br>```U='http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8'```<br>① 新行为：`curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"大语言模型支持库.模型连接器.重试调度","参数":{"重试模式":"normal","错误码白名单":["超时"],"实际错误码":"限流","最大重试次数":3}}' "$U"` → 期望 `是否重试=false`、`原因` 含「白名单未命中：实际错误码 限流 不在可重试集合 ['超时'] 内」（**当前实测为 `是否重试=true` = 未生效**）<br>② 类型护栏：同请求把 `实际错误码` 改 `123` → 期望 **HTTP 400 + `参数不合法`**（当前实测 **HTTP 200 = 未生效**）<br>③ 旧行为不回归：`{"重试模式":"normal","错误码白名单":["超时"]}`（不传该参数）→ 期望仍 `是否重试=true`（向后兼容）<br>④ 热接入证据：`热接入` 后 `grep -c 热接入` 审计日志应 ≥1（当前 0）。 |

**重启 40007 是否会中断在跑任务？——只读实测：不会（当前无在跑任务）**

```
ls 工程缓存/任务状态/                    → 任务.jsonl  进程任务
任务文件总数 = 8，非终态 = 0
逐条：0c7d3da0fa5a4f0b 失败/内部错误 · 21d34d15de81473f 已取消/已取消 · 4aebcc563e1c4660 已取消/已取消
      681491e891e3409f 成功 · 7ab691da077d4d8a 成功 · a5a8c431cd404bf3 失败/内部错误
      b148d6850f734cb9 成功 · da177f9d77e44452 成功
```

- **判定：8 个任务文件全部为终态（成功/失败/已取消），非终态 = 0 → 此刻重启 40007 不会中断任何在跑任务**。
- 但纪律上仍须**重启前重跑该计数**（计数是有时效的快照）：`工程缓存/任务状态/进程任务/*.json` 中出现 `状态 ∈ {排队中, 运行中, 取消中}` 即不得重启。
- 已知语义（`运行核心/启动运行核心网关.py:19-21` 注释）：**非终态任务在重启后收敛为「崩溃」**，续跑由能力自身缓存承担 → 所以这不是「重启即可随意处理」，而是「有在跑任务就必须等或等它收敛」。
- 另注：本轮 8 个任务全部来自历史（`任务.jsonl` 末行停留在 `2026-09-12 01:51:53`），说明**任务账本主文件已不再追加**，实况落点在 `任务状态/进程任务/<任务id>.json`（与 F-11 呼应）。

### F-02（阻断）8766 MCP 进程跑 9-12 的代码，落后 HEAD 3 天

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | `ps -o pid,lstart,command -p 40029` → `Sat Sep 12 11:27:02 2026`，命令 `python -m MCP工具箱.项目服务 --http --端口 8766`；`lsof -nP -iTCP:8766 -sTCP:LISTEN` → PID 40029 LISTEN 127.0.0.1:8766。`find MCP工具箱 -name "*.py" -newermt "2026-09-12 11:27:02" -not -path "*/__pycache__/*"` → **8 个**：`项目服务.py 09-15 01:11:31`（**入口**）、`作业系统.py 01:14:47`、`协作状态.py 01:14:47`、`能力占用.py 01:19:00`、`验证门禁.py 09-13 11:29:52`、`发布治理.py 09-13 11:53:16`、`文件租约.py 09-13 12:11:42`、`公开能力.py 09-13 12:14:03`。 |
| **判定** | **真**（进程启动时刻 + 8 模块 mtime 双重证据；行为级差异上游已声明跳过，本轮同样按边界跳过） |
| **修复落点** | `MCP工具箱/项目服务.py`（入口，09-15 01:11 已改）+ 同批 7 模块。**修复动作二选一，需华哥裁决**：<br>① **免重启**：若 01:11 那批改动**只改工具实现、未新增/删除工具、未改参数表** → 调 MCP 工具 `reload_tool_modules`（机制在 `MCP工具箱/热重载.py`，文档 `未完成事项.md:51` 明确「改工具**实现**可免重启即时生效」）。<br>② **必须重启**：只要涉及**新增/删除工具、参数表变更、文件租约、收口指纹**（`未完成事项.md:51-52` 边界）→ 按重启纪律「凑成一批一次性重启」，且**重启前确认无在跑长任务**。<br>**关键前提**：`项目服务.py` 是**入口**，入口改动通常归类为需重启 → **默认按②处理**，除非先证明只改实现。判定命令：`git diff --stat 5093d6e7..HEAD -- MCP工具箱/项目服务.py` + 查是否触碰工具注册表。 |
| **影响面** | MCP 侧一切「真实 HTTP 验收」结论的时效性（作业系统、验证门禁、发布治理、文件租约、协作状态、能力占用、公开能力）。**不影响** 40007 执行链（两者进程独立）。 |
| **是否共享文件** | 是。`MCP工具箱/项目服务.py` 是工具注册入口，与 7 个工具模块同批；`MCP工具箱/热重载.py` 为共享机制文件。 |
| **验证命令** | ① 重启/热重载后：`ps -o pid,lstart,command -p $(lsof -nP -iTCP:8766 -sTCP:LISTEN -t)` → `STARTED` 应晚于 `2026-09-15 01:19:00`。<br>② 协议面：`curl -s -D - -o - -X POST http://127.0.0.1:8766/mcp/ -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"audit06b","version":"1.0"}}}'` → 应仍 `200` + `mcp-session-id`；带该 session 调 `tools/list` → 工具数应仍为 **50**。<br>③ 热重载证据：`工程缓存/MCP任务观测/事件.jsonl` 中 `reload_tool_modules`/`热重载` 命中应 ≥1（上游实测 4382 条中为 0）。 |

### F-03（重要）错误码契约不齐：6 码无公开说明、7 码落默认 500、句柄语义状态码分裂

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | ① 公开表：`grep -n "公开错误说明表" -A 24 运行核心/统一网关/网关核心.py` → 定义在 **`网关核心.py:64`**，条目 **`:65-83` 共 19 个码**（请求结构错误/操作不存在/参数不合法/契约不存在/契约版本不兼容/能力不存在/提供者不可用/句柄无效/句柄已过期/权限不足/限流/超时/调用已取消/提供者崩溃/返回结果不符合契约/资源释放失败/幂等键冲突/版本冲突/内部错误）。<br>② HTTP 映射：`运行核心/统一网关/本地网关.py:839-849` 共 **19 个码**，`:849` `}.get(响应.错误码, 500)` → **默认 500**。<br>③ 表外码（实测存在）：`事件负载超限`(`流式HTTP.py:96,97,111`)、`反馈不存在`(`本地网关.py:458`)、`文件不存在`(`网关核心.py:676`)、`方法不允许`(`本地网关.py:504,594,596`；`流式HTTP.py:641`)、`未知路径`(`本地网关.py:336`；`流式HTTP.py:486`)、`路由不存在`(`本地网关.py:492,517`)、`状态版本冲突`(`本地网关.py:474`)、`句柄失效`(模块域，探针实测)。<br>④ 真实 HTTP 分裂复现：`{"操作":"资源状态","句柄":428101}` → **HTTP 400**，`错误码="句柄无效"`，`说明="句柄不存在或格式错误"`；`{"能力id":"系统核心支持库.进程管理.查询进程状态","参数":{"句柄":999998}}` → **HTTP 500**，`错误码="句柄失效"`，`说明="句柄不存在: 999998"`。 |
| **判定** | **真（主体成立）**，两处子主张需修正：<br>（a）上游「7 码代码内零出现」→ 实测 `调用已取消`(`网关核心.py:632`)、`提供者崩溃`(`流式HTTP.py:286`)、`版本冲突`(`资源协调/服务.py:203,229`)**均有出现**，真正零出现只有 **4 个**：`请求结构错误`、`契约不存在`、`契约版本不兼容`、`资源释放失败`。<br>（b）表外码应为 **8 个**（上游列 6 个，漏 `状态版本冲突`、`句柄失效`）。<br>（c）默认 500 是 `本地网关.py:849` 的显式默认，**是否属设计意图需华哥裁决**。 |
| **修复落点（三处必须同步）** | **落点①：`运行核心/统一网关/网关核心.py:64-84` `公开错误说明表`** —— 补 8 项文案：<br>`"事件负载超限": "流式事件负载超过流式预算"`、`"反馈不存在": "反馈记录不存在"`、`"文件不存在": "请求的文件不存在"`、`"方法不允许": "该路径不支持此 HTTP 方法"`、`"未知路径": "请求路径未授权"`、`"路由不存在": "请求的子路由未定义"`、`"状态版本冲突": "反馈状态版本冲突"`、`"句柄失效": "句柄不存在或已失效"`。<br>**落点②：`运行核心/统一网关/本地网关.py:839-849` 码→HTTP 映射** —— 显式登记 8 项：`"未知路径": 404, "路由不存在": 404, "方法不允许": 405, "事件负载超限": 413, "反馈不存在": 404, "文件不存在": 404, "状态版本冲突": 409, "句柄失效": 400`；并把 `内部错误` 显式写 `500`（取代隐式默认），使「未登记码」仍只有真正未处理者落 500。<br>**落点③：处理链异常分支 `运行核心/统一网关/网关核心.py:671-682`** —— `except KeyError:`(671-672)、`except FileNotFoundError:`(677-678)、`except Exception:`(681-683) 三处的码应与新表逐一对齐；模块域返回的 `句柄失效` 建议在 `_设置失败` 前做一次「模块码 → 网关公开码」归一（或按上表登记为合法公开码）。<br>**注意：三处必须同一提交改完**，否则会出现「有文案无状态码」或「有状态码无文案」的新裂缝。 |
| **影响面** | 全部外部调用方（含 V3 前端、其他 Agent 客户端）的错误分支判断；`开发文档/网关调用最小示例.md` 未给完整码表，调用方只能按现场猜。状态码分裂会让「句柄过期/无效」的重试与提示逻辑走错路（400 可提示用户重输，500 会被当服务端故障）。 |
| **是否共享文件** | 是（且是跨子报告高冲突文件）。`网关核心.py` 同时被 F-01（装配/能力详情）、F-04（KeyError 分支）、F-11（公开码表）引用；`本地网关.py` 同时被 F-03（状态映射）、F-07（CORS/允许来源表）引用。**改动必须与 F-04 / F-07 / F-11 串行合并**。 |
| **验证命令** | ① `grep -c '"' <(sed -n '65,84p' 运行核心/统一网关/网关核心.py)` 与 `sed -n '838,850p' 运行核心/统一网关/本地网关.py` 须含相同 8 个新码名；<br>② 分裂消除：两条探针（资源入口句柄 428101 / 能力入口句柄 999998）应返回**同一 HTTP 状态码与同一语义族**（预期均为 400，或均按新表登记）；<br>③ 表外码实测：`curl -s -X POST ... -d '{}' http://127.0.0.1:40007/网关/请求` → `未知路径` 应回 **404**（原即 404，属已显式传入状态码的路径）；`curl -s -H "$A" http://127.0.0.1:40007/网关/调用` → `方法不允许` 应回 **405**；<br>④ 回归：`事件负载超限`/`反馈不存在` 走一次真实探针，确认不再落 500。 |

### F-04（重要）KeyError 全局映射把「任务/包不存在」报成「能力不存在」

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | 静态：`运行核心/统一网关/网关核心.py:671-672` `except KeyError: self._设置失败(响应, "能力不存在")`；`网关核心.py:619-620`（`任务查询`）`if 任务对象 is None: raise KeyError(任务id)`；`网关核心.py:465-466`（`包详情`）`if 详情 is None: raise KeyError(包id)`；`网关核心.py:473-474`（`能力详情`）`raise KeyError(能力id)`（**这一条语义正确**，不该动）。<br>实测①：`{"操作":"任务查询","参数":{"任务id":"不存在任务id0000"}}` → **HTTP 404**，`错误码="能力不存在"`，`错误说明="请求的能力不存在"`。<br>实测②：`{"操作":"包详情","参数":{"包id":"支持库.后端.大语言模型支持库"}}` → **HTTP 404**，同样 `错误码="能力不存在"`、`错误说明="请求的能力不存在"`。 |
| **判定** | **真**（两条同类路径逐字复现；`网关核心.py:671` 是唯一收口点） |
| **修复落点** | `运行核心/统一网关/网关核心.py:671-672`：**不要改 `except KeyError` 的全局语义**，改为**在各业务分支内先判空并抛带领域的异常**，最小改法：<br>① `网关核心.py:619-620`（`任务查询`）→ 与 `任务取消` 同构：任务不存在时**不 raise**，直接 `self._设置失败(响应, "任务不存在")`（HTTP 建议 404）并 `return`。<br>② `网关核心.py:465-466`（`包详情`）→ 同样改 `self._设置失败(响应, "包不存在")`（HTTP 建议 404）。**`网关核心.py:473-474`（`能力详情`）保持 `raise KeyError(能力id)` 不动**（该处「能力不存在」语义正确）。<br>③ 新增两个公开码 `任务不存在`(建议 404)、`包不存在`(建议 404) → **必须同步 F-03 的落点①②**（否则新码又掉进默认 500）。<br>④ 保留 `:671` 的 `except KeyError → 能力不存在` 作为**兜底**（对应 `调用能力` 时能力 id 未注册的真实语义，探针 #4 已核实为 404 `能力不存在`）。<br>**先例**：`未完成事项.md:91` 已用同一手法修过资源三操作（`能力不存在`→`句柄无效`），本次是把**任务/包两条同类路径一并收口**。 |
| **影响面** | 任务域、包域的外部调用方；无法区分「能力 id 写错」与「任务 id 不存在/已被清理」。任务查询是长任务轮询主路径，误判会导致客户端把「任务不存在」当成「能力部署错误」而上报。 |
| **是否共享文件** | 是。与 F-03（码表）、F-11（公开码）、F-01（`调用能力` 语义）同文件 `网关核心.py`。**串行**：F-03 落点①②先落，F-04 的新码才有的落点。 |
| **验证命令** | ① `{"操作":"任务查询","参数":{"任务id":"不存在任务id0000"}}` → 期望 `错误码="任务不存在"`（不再 `能力不存在`），HTTP 与 F-03 新表一致；<br>② `{"操作":"包详情","参数":{"包id":"支持库.后端.大语言模型支持库"}}` → 期望 `错误码="包不存在"`；<br>③ 不回归：`{"能力id":"不存在的库.不存在的能力","参数":{}}` → 仍须 `HTTP 404 能力不存在`、`说明="能力未注册: 不存在的库.不存在的能力"`（上游探针 #4 基线）。 |

### F-05（重要）「40007 唯一网关」不成立：40006 亦常驻 LISTEN，文档零提及

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | `lsof -nP -iTCP:40006 -sTCP:LISTEN` → `Python 6934 用户 3u IPv4 ... TCP 127.0.0.1:40006 (LISTEN)`；`ps -o pid,lstart,command -p 6934` → `Fri Sep 11 20:46:21 2026`，命令 `Python MCP工具箱/能力网关.py --端口 40006 --地址 127.0.0.1`。<br>**非执行腿（本轮实测）**：`curl -s http://127.0.0.1:40006/健康` → **HTTP 404** `{"成功":false,"错误码":"路由不存在","错误说明":"未定义路由: /健康"}`；`POST /网关/调用` → **HTTP 404** `"说明":"执行入口已迁移到 /网关/调用，未定义路由: /网关/调用"`。<br>文档零记载：`grep -rn "40006" 开发文档/项目说明.md 开发文档/未完成事项.md AGENTS.md` → **0 命中**。 |
| **判定** | **真**（进程/端口/plist 已核实；文档零提及属事实核对项）。**口径需收窄**：40006 **不是第二条执行腿**，是**只读能力发现/工具网关**（其全部已知路由均回 `路由不存在`，无 `/健康` 亦无 `/网关/调用`）。 |
| **修复落点（只修口径，不把 40006 当第二条执行腿）** | **落点①**：`开发文档/网关调用最小示例.md:59`「`/网关/调用` 是唯一执行入口；项目不做 `/网关/请求` 兼容。模块和支持库都使用同一套入口，**不直接暴露各自端口**」—— 此句**关于执行入口是正确的**（40007 唯一执行入口成立），但「不直接暴露各自端口」与「127.0.0.1:40006 常驻 LISTEN」并列时会被读成矛盾。最小改法：**保留原句**，在其后补一句事实注记：<br>「**端口事实**：本机另有 `127.0.0.1:40006`（`MCP工具箱/能力网关.py`，LaunchAgent `com.huashi.gateway-40006`，`KeepAlive=true`）常驻监听，**只读能力发现/工具网关，不是执行入口**：`GET /健康`、`POST /网关/调用` 均返回 `404 路由不存在`；执行入口仍唯一为 40007。」<br>**落点②**：`开发文档/项目说明.md`「唯一网关/端口」相关段落 —— 同样加同一句端口事实注记（定稿前先 `grep -n "40007\|唯一网关\|端口" 开发文档/项目说明.md` 定位插入行）。<br>**明确不做**：不在任何文档里把 40006 写成「第二条执行腿」，不写「双网关」；不因本条改动 40006 进程/plist（属运维，需华哥批）。 |
| **影响面** | 审计前提与文档准确性；端口占用清单；后续排查时「谁占着 40006」。**不影响 40007 契约本身**。实际功能影响有限（40006 无执行入口）。 |
| **是否共享文件** | 否（纯文档）。但与 F-06 同属「文档口径」类，建议同一提交改。 |
| **验证命令** | ① `grep -rn "40006" 开发文档/项目说明.md 开发文档/未完成事项.md` → 改后应 ≥2 命中且含「不是执行入口」字样；<br>② `lsof -nP -iTCP:40006 -sTCP:LISTEN`（确认事实未变）；<br>③ `curl -s http://127.0.0.1:40006/健康 -w '\nHTTP=%{http_code}\n'` → 应仍 `404 路由不存在`（不改代码则此结果不变）。 |

### F-06（重要）装配能力数：现网 536，文档并存 549 / 498

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | 现网：`curl -s -H "$A" http://127.0.0.1:40007/健康` → `HTTP=200`，`值={"状态":"健康","能力数":536}`。<br>磁盘复算（`unset PYTHONPATH; python3.14`）：`支持库/**/能力定义.json` + `模块库/**` + `技能库/**` → **文件数=106，条目=536** → 与现网 536 完全一致（双口径闭合）。<br>文档三值：`开发文档/项目说明.md:14` 549（2026-09-11 口径）、`:16` 498（2026-09-10 历史快照）、`:440` 549；`开发文档/未完成事项.md:34` 549、`:38` 498、`:17` 制品口径 536。 |
| **判定** | **真**（536 是当前唯一成立值；549/498 为历史口径，文档未标注时效） |
| **修复落点** | **落点①**：`开发文档/项目说明.md:14` → 把「合计 111 个包、549 项能力」改为「**首次实测（2026-09-11）：549 项；当前运行态（2026-09-15 `/健康` 实测）：536 项**」，并保留 09-11 日期标注以示口径来源。<br>**落点②**：`开发文档/项目说明.md:440` → 「**549 个正式能力全部可作为后台任务提交**」改为「**当前运行态 536 个能力全部可作为后台任务提交**」（功能断言不变，只换数）。<br>**落点③**：`开发文档/未完成事项.md:34` → 同①处理（549 标日期 + 补当前 536）；`:38` 的 498 保留但加「历史快照（2026-08-29/09-10）」前缀。<br>**落点④（权威口径收口）**：在两份文档各自「口径」段加一句：「**能力数实时权威查询 = `GET http://127.0.0.1:40007/健康` 的 `值.能力数`**；磁盘口径 = `支持库/模块库/技能库` 下 `能力定义.json` 的 `能力列表` 条目合计；两值应相等，不等即为装配滞后（见 F-01）。」—— 这一句同时是 F-01 的可观测性护栏。<br>**注意**：549 与 536 的 **13 项差额明细**属子报告 01 统计口径范围，本落点**只改数字与口径说明，不下差额结论**。 |
| **影响面** | 文档可信度；「549 能力全部可提交为任务」是任务系统的容量承诺，数字错了会让容量评估偏差 2.4%。**不影响运行态代码**。 |
| **是否共享文件** | 否（纯文档）。`开发文档/未完成事项.md` 同时被 F-02（重启纪律 `:51-52`）、F-05、F-11（`:77/:91` 句柄语义）引用 → 与那几条**串行合并**，避免同文件多次改。 |
| **验证命令** | ① `curl -s -H "$A" http://127.0.0.1:40007/健康` → `能力数` 值与文档新写入值逐字相等；<br>② `unset PYTHONPATH && python3.14 - <<'EOF'` …（统计脚本，见子报告命令清单 #12）→ 输出 `文件数= 106 条目= 536`，与文档一致；<br>③ `grep -rn "549" 开发文档/项目说明.md 开发文档/未完成事项.md` → 每一处命中都必须带日期标注。 |

### F-07（重要）CORS 头在代码里，但生产未授权任何来源 → 预检一律 403

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | 实测：`curl -s -i -X OPTIONS -H "Origin: http://127.0.0.1:3000" -H "Access-Control-Request-Headers: authorization,content-type" http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8` → **`HTTP/1.0 403 Forbidden`**，体 `{"请求id":"32288e2a11784f04","操作":"HTTP边界","成功":false,"错误码":"权限不足","错误说明":"跨域来源未授权","耗时毫秒":0.0}`，响应头**无任何 `Access-Control-Allow-*`**。<br>静态：`本地网关.py:134` `允许来源表=set(self.配置.get("允许来源表", set()))` → 缺省空集；`安全边界` 侧同样 `default_factory=set`；`本地网关.py:260` / `:279` 判定 `来源 not in 安全配置实例.允许来源表` → 403；生产装配 `运行核心/启动运行核心网关.py:24-35` 的配置字典只有 `请求超时秒/要求凭证/凭证环境变量/禁止客户端身份/请求大小上限`，**确实未传 `允许来源表`**。 |
| **判定** | **真**（生产装配缺省=空集为静态确定；403 真实复现） |
| **修复落点** | 二选一，**需华哥裁决**：<br>**方案A（推荐，不改代码）**：前端继续走同源/服务端转发，文档化「浏览器不得跨域直连 40007」，并在 `开发文档/网关调用最小示例.md` 补一句「**跨域预检会 403**，浏览器宿主须经同源代理」。→ **落点：文档一处**。<br>**方案B（要跨域直连时）**：`运行核心/启动运行核心网关.py:24-35` 的 `配置` 字典**加** `"允许来源表": {"http://127.0.0.1:3000", ...}`（显式白名单，**禁止用 `"*"`**：`:279` 与 `流式HTTP.py:607` 的判定是 `来源 not in 允许来源表`，通配符不在其语义内，写了也不生效且会误导）。<br>**配套**：`本地网关.py:134` 的 `set()` 缺省**保持不变**（缺省拒绝是正确安全姿态），只在装配层显式开白名单。 |
| **影响面** | 浏览器直连 40007 的一切场景（含 `/网关/流式` SSE，`流式HTTP.py:458/584/607` 同一套判定）。**若前端走同源代理则实际影响为 0**——本轮未确认宿主转发方式，属未确认项。服务端/命令行调用（curl、其他 Agent）不受影响。 |
| **是否共享文件** | 是。`本地网关.py` 与 F-03 同文件（`:134` 与 `:839-849` 相距很远，可并行编辑但**需同一批次提交**）；`启动运行核心网关.py` 与 F-01 的指纹校验开关同文件（同一配置字典）→ **F-01 配套与 F-07 方案B 必须串行**（改同一 dict）。 |
| **验证命令** | ① 方案A：`curl -s -D - -o /dev/null -X OPTIONS -H "Origin: http://127.0.0.1:3000" http://127.0.0.1:40007/网关/调用 \| head -5` → 应**仍为 403**（预期如此，属文档化预期行为）；<br>② 方案B：同一命令 → 应 **204** 且响应头含 `Access-Control-Allow-Origin: http://127.0.0.1:3000`、`Access-Control-Allow-Methods`、`Access-Control-Allow-Headers`、`Access-Control-Allow-Credentials`、`Access-Control-Max-Age`；<br>③ 负向不回归：`-H "Origin: http://evil.example"` → 必须仍 **403**（白名单不可放宽）。 |

### F-08（重要/运维）安全审计落盘位置随 TMPDIR 漂移

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | `ps eww -p 50802 \| tr ' ' '\n' \| grep TMPDIR` → `TMPDIR=/var/folders/hx/4zdx_n0s1tb0t1fl1q9zw4140000gn/T/`。<br>`find /var/folders -name 安全审计.jsonl` → `…/T/系统级支持库_审计/安全审计.jsonl`（**12677 行**，`grep -c 热接入`=0）与 `…/T/审计探针__zumabkl/安全审计.jsonl`。<br>旧落点：`ls -l /tmp/系统级支持库_审计/` → `安全审计.jsonl 5804 B Sep 13 14:47`（**停更**）。<br>源码定位：`运行核心/运行诊断/安全审计/安全审计.py:28-30` `默认存储目录()` → `os.environ.get("系统库审计目录", str(Path(tempfile.gettempdir()) / "系统级支持库_审计"))`。 |
| **判定** | **真**（TMPDIR + 双落点 + 源码行三证齐全） |
| **修复落点** | **落点①（代码，推荐）**：`运行核心/运行诊断/安全审计/安全审计.py:29-30` —— 把默认目录从 `tempfile.gettempdir()` 改为**系统根目录下固定路径**（如 `Path(__file__).resolve().parents[3] / "工程缓存" / "审计"`，需与项目既有缓存目录约定对齐），**环境变量 `系统库审计目录` 覆盖优先级保持不变**。理由：审计是证据链，不能随进程环境漂移。<br>**落点②（配置，免改代码的临时解）**：`~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 的 `EnvironmentVariables` **加** `系统库审计目录=~/Documents/Agent/PHP/系统工程平台/工程缓存/审计`。属配置改动，**需华哥批**；且注意 `热重载/重启后才生效`。<br>**落点③（运维文档）**：`开发文档/未完成事项.md` 补一句排查纪律：「**查安全审计先取 `ps eww <网关PID> \| tr ' ' '\n' \| grep TMPDIR`，按 TMPDIR 下的 `系统级支持库_审计/安全审计.jsonl` 找；`/tmp` 下的同名文件可能是历史残留（已在 09-13 停更）**」。<br>**注意**：`安全审计.py` 同目录风格被 `灰度指标.py:256-259`（`系统库灰度目录`）、`版本注册表.py:72-74`（`系统库版本目录`）复用 → 本落点**只改审计**，灰度/版本两处是否同改**属需裁决项**（一并改会扩大影响面）。 |
| **影响面** | 运维排查（按 `/tmp` 找会误判「未落盘」）；审计证据的可追溯性与归档；合规取证时路径不稳定。**不影响网关功能**。 |
| **是否共享文件** | 否（独立文件）。但若采纳落点②，则与 **F-01 的指纹校验开关、F-07 方案B 的 `允许来源表` 改同一个 plist 的 `EnvironmentVariables`** → **三条必须串行、一次性合并下次重启**（符合 `未完成事项.md:52` 重启纪律）。 |
| **验证命令** | ① 改动后：`ps eww -p $(lsof -nP -iTCP:40007 -sTCP:LISTEN -t) \| tr ' ' '\n' \| grep 系统库审计目录` → 应回显新路径；<br>② 发一次无害只读探针后 `ls -l <新路径>/安全审计.jsonl` → mtime 应为当前时刻且行数递增；<br>③ `find /var/folders -name 安全审计.jsonl` → 重启后不应再有**新增行**（旧文件保留为历史）。 |

### F-09（建议）错误说明泄漏 Python 原生签名，且同类错误两套文案

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | 文案A：`{"能力id":"文档读取.结构化读取","参数":{}}` → `错误码="参数不合法"`，`说明="能力 文档读取.结构化读取 调用参数缺少必填项：结构化读取() missing 1 required positional argument: '文件路径'"`（**泄漏函数名 + Python 原生 TypeError 文本**）。<br>文案B：`{"能力id":"PDF文本表格.解析PDF","参数":{"最大页数":"abc"}}` → `错误码="参数不合法"`，`说明="参数不合法：能力 PDF文本表格.解析PDF 缺少必填参数 文件路径"`（**网关自产，无 Python 痕迹**）。 |
| **判定** | **真**（两条文案逐字复现，确为两套） |
| **修复落点** | **落点①（收口文案源头）**：`运行核心/统一网关/网关核心.py` 的 `_设置失败` 前的错误说明生成处 —— 上游已核实 `:223` `脱敏错误信息()` 是**原样回带**，未做清洗。最小改法：在 `脱敏错误信息()`（`运行核心/运行诊断/运行事件/脱敏工具.py`）内**加一条规则**：把 `missing \d+ required positional argument: '(\w+)'` 归一为「缺少必填参数 \1」，`unexpected keyword argument` 归一为「不支持的参数 \1」—— **一处收口，覆盖全部能力**。<br>**落点②（能力侧）**：`支持库/.../文档读取/实现/*.py` 的必填校验若依赖 Python 签名报错，补显式必填校验（与 `PDF文本表格.解析PDF` 用的同一套网关校验口径），使 A 路径也走「缺少必填参数 <名字>」。<br>**优先①**：改一处即可让所有能力文案统一，风险最低。 |
| **影响面** | 所有通过 40007 报错的外部调用方看到的文案；泄漏内容为**能力内部函数名**（低危但属信息泄漏）；文案不统一导致客户端无法用统一正则解析「缺哪个参数」。 |
| **是否共享文件** | 是。`网关核心.py` 与 F-03/F-04/F-11 同文件 → **串行**；`脱敏工具.py` 若被多处引用，改前须 `grep -rn "脱敏错误信息" --include=*.py` 确认调用面。 |
| **验证命令** | ① 文案A 探针（`文档读取.结构化读取` 空参）→ `说明` 应**不再含** `missing 1 required positional argument` 与 `()`；<br>② 文案B 探针（`PDF文本表格.解析PDF`）→ 文案应与①**同构**（同一前缀、同一「缺少必填参数 X」句式）；<br>③ 全局不回归：`{"能力id":"文档读取.结构化读取","参数":{"文件路径":123}}` → 仍须 `400 参数不合法` + 「必须是 文本型」（上游探针 #7 基线）。 |

### F-10（建议）必填参数校验先于类型校验

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | ① `{"能力id":"PDF文本表格.解析PDF","参数":{"最大页数":"abc"}}`（**同时**缺 `文件路径` 且 `最大页数` 类型错）→ 只报 `参数不合法：能力 PDF文本表格.解析PDF 缺少必填参数 文件路径`。<br>② 补上 `文件路径` 后同一类型错：`{"文件路径":"/tmp/x.pdf","最大页数":"abc"}` → 才报 `参数不合法：能力 PDF文本表格.解析PDF 的参数 最大页数 必须是 整数型`。 |
| **判定** | **真**（两阶段顺序确定：必填检查在前，类型检查在后） |
| **修复落点** | **不改校验顺序**（先必填后类型是合理设计：缺参数时无法判断类型）。**最小改法 = 让一次报错带全部问题**：`运行核心/统一网关/网关核心.py` 的能力入参校验处，把「首个错误即 return」改为**累积问题列表**，以 `；` 连接一次性回带（如「缺少必填参数 文件路径；参数 最大页数 必须是 整数型」）。<br>**若评估改动面过大**（会动到契约测试基线）→ **降级方案：文档化**。在 `开发文档/网关调用最小示例.md:98`「只有两类会报错」段后补一句：「**注意：报错按「先必填、后类型」两阶段返回，一次只报一类；调用方修完必填需重试一次才能看到类型错误**」。<br>**裁决点**：改动 vs 文档化，二选一。 |
| **影响面** | 契约测试覆盖面（只测一种会漏另一种，上游已点明）；调用方的调试往返次数（2 次 vs 1 次）。 |
| **是否共享文件** | 是。`网关核心.py` 能力入参校验处与 F-03/F-04/F-09 同文件 → **串行**。若改校验聚合，**必须同时跑契约/网关回归**（属发布门禁范围，本轮不跑）。 |
| **验证命令** | ① 若采纳聚合：探针①应回「缺少必填参数 文件路径；参数 最大页数 必须是 整数型」两条并列；<br>② 若采纳文档化：`grep -n "先必填\|两阶段" 开发文档/网关调用最小示例.md` 应命中；<br>③ 不回归：`{"文件路径":"/tmp/x.pdf","最大页数":"abc"}` → 仍须单报「必须是 整数型」。 |

### F-11（建议）任务账本内错误码与网关公开码不一致

| 列 | 内容 |
|---|---|
| **二次证真（命令+真实输出摘要）** | 账本逐条（`工程缓存/任务状态/进程任务/*.json`）：`21d34d15de81473f 状态=已取消 错误码='已取消'`、`4aebcc563e1c4660 状态=已取消 错误码='已取消'`、`0c7d3da0fa5a4f0b 状态=失败 错误码='内部错误'`、`a5a8c431cd404bf3 状态=失败 错误码='内部错误'`、四个成功任务 `错误码=''`。<br>网关公开侧：`网关核心.py:632` `"错误码": "" if 成功 else "调用已取消"`；`网关核心.py:77` 公开表 `"调用已取消"`；`本地网关.py:848` 映射 `"调用已取消": 409`。<br>内码来源：`运行核心/任务调度/任务系统.py:24 状态_已取消 = "已取消"`、`运行核心/任务调度/任务进程.py:25 任务状态_已取消 = "已取消"`、`任务进程.py:252` `self._完成失败(任务对象, 任务状态_已取消, "已取消", "任务已取消", …)`。<br>账本落点：`任务.jsonl` 末行停留在 `2026-09-12 01:51:53`（**主账本不再追加**），实况写 `任务状态/进程任务/<任务id>.json`。 |
| **判定** | **真**（账本内码 `已取消` 与网关公开码 `调用已取消` 实测并存；`内部错误` 恰与公开表同字，属偶然一致） |
| **修复落点** | 二选一，**需华哥裁决**：<br>**方案A（推荐，最小改法）：台账码向公开码对齐。** 落点 `运行核心/任务调度/任务进程.py:252` 的第 3 个位置参数 `"已取消"` → 改 `"调用已取消"`；`运行核心/任务调度/任务系统.py:24` 与 `任务进程.py:25` 的 `状态_已取消` **保持 `"已取消"` 不动**（那是**任务状态枚举**，不是错误码，两者语义不同，不可混改）。<br>⚠️ **关键区分**：`状态字段` 用 `已取消`（正确，是状态），`错误码字段` 用 `调用已取消`（要对齐公开码）。上游报告只提「不一致」，未点明这是**两个字段各有一套空间** → 修复时必须只改错误码字段。<br>**方案B（零代码）**：在公开码表侧补 `"已取消"` 作为 `调用已取消` 的别名，或文档化「账本内码为任务系统私有码，不经网关对外」。**不推荐**：会让同一语义长期两个码。<br>**配套**：`开发文档/未完成事项.md:77/:91` 里引用 `已取消` 的表述加注「（账本内码）/ 对外公开码为 `调用已取消`」。 |
| **影响面** | 任务账本消费方（运维排查、任务审计、续跑逻辑）；按公开码写分支的程序读账本会漏判。历史 `任务.jsonl` 里的旧条目**建议不回溯改写**（属运行数据，本轮也不允许改）。 |
| **是否共享文件** | 是。`任务进程.py` 与任务状态机共用（改错误码字段前须确认该参数不被同时用作状态写入）；`网关核心.py:632` 若同步调整则与 F-03/F-04/F-09 同文件 → **串行**。 |
| **验证命令** | ① 改动后提交一个长任务再取消（**属执行类操作，需华哥批**）：`工程缓存/任务状态/进程任务/<新任务id>.json` 的 `错误码` 应为 `调用已取消`、`状态` 应仍为 `已取消`（**两者必须不同**，这是判对的关键）；<br>② 网关侧同任务：`{"操作":"任务查询","参数":{"任务id":"<id>"}}` → `状态=已取消`；<br>③ 不回归：`任务进程.py:25` `任务状态_已取消` 仍为 `已取消`，`任务系统.py:24` 同。 |

---

## 二、共享文件与串行要求

### 2.1 共享文件清单（同文件命中 ≥2 条发现的即列入，**必须串行合并**）

| 文件 | 命中的发现 | 冲突点 | 串行要求 |
|---|---|---|---|
| `运行核心/统一网关/网关核心.py` | **F-01、F-03、F-04、F-09、F-10、F-11** | `:64-84` 公开错误说明表 · `:671-682` 异常收口 · `:619-620` 任务查询抛错 · `:465-466` 包详情抛错 · `:632` 任务取消公开码 · 能力入参校验 | **一条主线串行**：先 F-03（码表 + 状态映射基线）→ 再 F-04（新码有落点）→ 再 F-11（`调用已取消` 对齐）→ 再 F-09/F-10（文案与校验聚合，动同一函数区）。**禁止并行编辑**。 |
| `运行核心/统一网关/本地网关.py` | **F-03、F-07** | `:134` 允许来源表缺省 · `:839-849` 码→HTTP 映射 | 两个改动点物理距离远、可同一批次，但**必须同一提交**（避免中间态「有映射无来源判定」）。 |
| `运行核心/启动运行核心网关.py` | **F-01（指纹开关配套）、F-07（方案B）** | `:24-35` 同一个配置字典 | **串行**：同一个 `配置` dict 两次编辑，必须一次性改完。 |
| `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` | **F-01（`系统底座_指纹校验`）、F-08（`系统库审计目录`）** | 同一个 `EnvironmentVariables` 段 | **串行 + 一次性重启**（符合 `未完成事项.md:52` 重启纪律：能热加载就不重启、必须重启的凑一批）。 |
| `开发文档/未完成事项.md` | **F-02（`:51-52` 重启纪律）、F-05、F-06（`:34/:38` 数字）、F-11（`:77/:91` 码）** | 同文件多处 | **串行合并为一次文档提交**。 |
| `开发文档/项目说明.md` | **F-05、F-06（`:14/:16/:440`）** | 同文件多处 | 串行合并。 |
| `开发文档/网关调用最小示例.md` | **F-05（端口事实）、F-07（跨域 403）、F-10（两阶段校验）** | `:59` 入口句、`:98` 报错段 | 串行合并（同一段落的相邻句）。 |
| `MCP工具箱/项目服务.py` 及同批 7 模块 | **F-02** | 工具注册入口 | 属独立子系统，**可与 40007 侧并行**（不同进程、不同文件树）。 |

### 2.2 可并行的腿（互不共享文件）

| 腿 | 包含 | 说明 |
|---|---|---|
| **腿A：40007 网关内核** | F-03 → F-04 → F-11 → F-09/F-10（`网关核心.py` 主线）+ F-07 方案B（`本地网关.py`/`启动…py`） | **串行**，唯一热点文件 `网关核心.py`。 |
| **腿B：MCP 8766** | F-02（`MCP工具箱/` 全批） | **与腿A 完全并行**（不同进程、不同目录，零文件交集）。 |
| **腿C：文档口径** | F-05、F-06（+ F-03/F-07 的文档补充句） | 与腿A/腿B 并行；但与腿A 有**内容依赖**（文档数值须等腿A 落地后取真实值复测再写）。建议**腿A 定稿后**收尾。 |
| **腿D：运维/配置** | F-01 指纹开关、F-08 审计目录（plist）+ F-08 落点①（`安全审计.py`） | 改动前需华哥批；plist 两条**合并一次性重启**。 |
| **腿E：独立代码文件** | F-08 落点①（`运行核心/运行诊断/安全审计/安全审计.py`）、F-09 落点①（`运行诊断/运行事件/脱敏工具.py`）、F-11（`运行核心/任务调度/任务进程.py`） | 与腿A 无文件交集，**可并行**；但 F-11 若同步动 `网关核心.py:632` 则并入腿A。 |

### 2.3 全局串行硬约束

1. **`网关核心.py` 是唯一不可并行的热点**：F-01/F-03/F-04/F-09/F-10/F-11 六条落点同文件，必须一条主线依次落地，每次改完立即跑该条验证命令。
2. **新码必须与码表同批**：F-04 的 `任务不存在`/`包不存在`、F-03 的 8 个补登码，**只要有一个先落而另一个后落**，就会出现「新码掉默认 500」的中间态 → 码表（F-03 落点①）+ HTTP 映射（F-03 落点②）+ 异常分支（F-04）**必须在同一提交**。
3. **plist 改动必须凑批**：F-01（指纹校验）+ F-08（审计目录）合并成一次重启；重启前必须重跑 `非终态任务数 = 0` 检查。
4. **F-01 优先于一切**：它是唯一「静默失效、返回 200」的阻断项，其他条目的「真实 HTTP 验收」都建立在运行态=源码之上。**F-01 未收口前，F-03～F-11 的修复不宜宣称验收通过**（本次修复若改的是 `网关核心.py`，会随同一次重启一并生效——但要明确这一点，避免「改完就以为验完了」）。

### 2.4 重启与验证总方案（本轮不执行，供维修轮使用）

| 步骤 | 动作 | 前置检查 | 时长影响 |
|---|---|---|---|
| 1 | 重跑 `非终态任务数 = 0`（`工程缓存/任务状态/进程任务/*.json` 中 `状态 ∉ {成功,失败,已取消}`） | 有非终态则**不得重启**（重启后收敛为「崩溃」） | 秒级 |
| 2 | 40007 优先走**热接入**（`POST /网关/热接入`）而非重启——先把 `模型连接器` 换新，验证 F-01 生效 | `热接入` 需凭证 + 「调用」权限位 | 秒级 |
| 3 | F-01 热接入后立刻跑三条验证命令（新行为 / 类型护栏 / 旧行为不回归） | — | 秒级 |
| 4 | 8766：先判定 `项目服务.py` 改动是否触碰工具增减/参数表 → 是则按批重启，否则 `reload_tool_modules` | `ps -o lstart` 确认新进程时刻 | 分钟级 |
| 5 | plist 改动（F-01 指纹开关 + F-08 审计目录）合并后 **一次性重启 40007** | 重跑步骤1 | 分钟级 |
| 6 | 全量收尾验证：`/健康` 能力数、F-03 探针、F-04 探针、F-07 OPTIONS、审计落点 mtime | — | 分钟级 |

**本轮未执行任何重启/热接入/写操作；本表仅落点，不含执行结果。**

---

## 三、本轮产生的写入（如实申报）

| 路径 | 变化 | 原因 |
|---|---|---|
| `开发文档/临时文档/20260915_底座专业审计/落点清单_06_运行态网关.md` | 新建（本文件） | 唯一计划内写入 |

其余：未改源码、未改配置、未重启服务、未热接入、未跑发布门禁、未提交任务、未调外部模型、未删文件、未触碰数据库。只读探针均属纯计算/查询能力（`健康`/`能力详情`/`重试调度` 纯计算/`任务查询`/`包详情`/`资源状态`/`OPTIONS` 预检拒答），未产生新任务记录（`工程缓存/任务状态/进程任务/` 仍为 8 个文件）；网关安全审计按设计追加本文件全部探针请求。
