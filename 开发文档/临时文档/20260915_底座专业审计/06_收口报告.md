# 审计与维修收口报告（第三轮结束后）

> 冻结基线：`1c69ea5d` → 维修提交 `c0c0878b`（本地提交，未推远程）。
> 执行依据：`开发文档/项目说明.md` 第 23 条（审计与维修分三轮）。本报告只写实测事实。

---

## 一、三轮执行总账

| 轮次 | 目标 | 产出 |
|---|---|---|
| 第一轮 | **找全所有问题** | 11 路只读审计 → 15 份报告（含 3 路失败/超时由父会话接手）；`01_总报告.md` 汇总：阻断 8 / 重要 40+ / 建议 55+ |
| 第二轮 | **证真 + 精确定位落点 + 排序** | 10 份 `落点清单_*.md`（每条带证真命令/path:行号/最小改法/影响面/是否共享文件/验证命令）+ `03_修复批次冻结表.md`（批次、串并行、并发度、验收链）；**纠正第一轮 6 处定级/事实** |
| 第三轮 | **一次修完** | 8 个批次 + 6 个维修期新发现，共 **30+ 路**维修子代理（文件域零重叠），父会话同步做批次8文档与四连收口 |

---

## 二、维修成果（全部有修前/修后对照证据）

### 安全（6 项，全部阻断级）
| 项 | 修前实测 | 修后实测 |
|---|---|---|
| 沙箱 SBPL 注入 | 注入载荷读到工作区外文件（退出码 0） | `参数不合法` 拒收；profile 语法错不再伪绿 |
| SSRF | 302 跳到 `0.0.0.0` 取回 `SECRET-INTERNAL-DATA` | 校验层即拒；强校验能力接线（此前 0 调用方） |
| 解压上限 | 204KB → 200MB 落盘、兄弟目录绕过放行 | 三道上限 + 路径越界，落盘 0 字节、无半成品 |
| 环境变量脱敏 | 明文回显全部环境与任意 `.env` | 敏感键默认 `***`（`包含敏感值=True` 才明文） |
| MCP 三项 | 可对任意 git 仓提交；`test_resource` 可 killpg 自身；8766 无鉴权 | 仓根白名单；子进程分支卡口（3 个最深处）；令牌中间件 + 回环/Host/Origin 守卫（10/10） |
| 启动脚本 | `${中文变量:-}` → `bad substitution`（fail-closed 失效） | ASCII 变量 + `printenv`；fail-closed 实测退出码 1，正常路径真启动 |

### 资源（4 项）
- 三包 SQLite 连接 1:1 泄漏（26+1 处）→ 建连函数缓存复用，**调用点 0 改动**（字节级证明）；20 次调用未关闭连接 20→1
- 权威状态三表只增不删 → 回收器接线 + 三表 DELETE + **7 天保留窗口**；真删 8983 行、等价性 0 处不一致
- 句柄表无界 → 双限淘汰（300s / 200 条）：1200 句柄全失效后表 1200→200
- 事件日志无界 → 8MB×3 分片（磁盘上界 ≈24MB）+ 分片倒读查询

### 契约与门禁
- 契约版本 8 处 `1.0.0` 字面量 → 唯一事实源（`grep` 计数 0）；类型目录补 `JSON值型`
- 网关 `公开错误说明表` 与 `错误码状态映射` **34 码 == 34 码**（补登 6 个新码，含此前两表都漏登的 `热接入失败`）
- 依赖防火墙：补技能库层（两张表同改）+ 10 个第三方包 + 排除片段改名精确匹配 → 现场违规 19 → 13
- 能力调用图审计：第三方前缀表改共享常量、范围扩到包内全量 `.py`、加模块库禁网络 → 模块库 0 → 9 条**真红**（都在 `模块库/直播逐字稿/工具/`，即应移出模块库的对象）
- 新建两个门禁：`测试体系门禁`（全量可导入性/零测试/未解释跳过）、`测试伪装门禁`（mock 三条硬规则，存量 289 项）

### 测试与清理
- 僵尸测试清零：193 文件可导入失败 **1 → 0**
- 四条假绿改真验证并过**变异检验**（破坏生产路径必须变红）
- 运行库 4 张恒空表定义删除、`灰度观测` 验收假数据行删除（聚合污染实测归零）、写入侧加防复发护栏
- 删 3 个零引用死模块；业务品牌词/业务路径去行业化（包内命中 0）

### 四连收口（父会话串行执行）
- 重编译：**12 包，问题 0**
- 摘要级联后全树 **112 包 / 0 不闭合**
- 依赖锁重生成：**0 漂移**
- 制品：重编 + 安装 + 激活，`制品来源.json` = `提交 c0c0878b`、工作区干净；顺带清理 4 份过期制品（释放 10.9MB）

---

## 三、验证轮结果（终态）——门禁全绿

**发布门禁：70 项通过 / 0 项失败，发布状态＝通过。**
提交链（均本地，未推远程）：`1c69ea5d` → `c0c0878b` → `6001e96c` → `84910b35` → `da8fe6ae` → `568a5e94` → `6a2b9b7f`。

| 验证 | 修前基线 | 终态 |
|---|---|---|
| 验收探针集 | 6/9 | **10/10** |
| 发布门禁 | 65 通过 / 7 失败 | **70 通过 / 0 失败** |
| 全仓 `py_compile` | — | 退出码 0 |
| 摘要闭合（112→113 包） | — | **0 不闭合** |
| 依赖锁 | — | **0 漂移** |
| 能力数（磁盘源头 vs 运行态） | 536 | **538 = 538**（新增 TreeSitter提供者 2 个） |
| HTML 黑盒 | 536 能力 / 710 步骤 | **538 能力 / 729 步骤，清理失败 0，资源残留 0** |
| HTML 契约真实执行矩阵 | — | **538 个能力真实 HTTP 成功并完成资源收口** |
| 40007 | 旧装配（批次1 之前） | 重启后装配 538，探针 10/10 |
| 8766 | 9-12 旧代码 | PID 33333，`tools/list` = 50 工具 |

### 本轮（第四轮）新增修复
| 项 | 修前 | 修后 |
|---|---|---|
| 契约注册口径不一致 | 19 条（8 包） | **0 条** |
| 依赖防火墙存量违规 | 13 条 | **0 条**（宿主白名单 9 + 适配层封装 2+1 + 行级豁免 1） |
| `调用示例` 双精度数型写成整数 | 138 处（37 包） | **0 处** |
| 网关类型校验 | 只校 7/16 类，其余 fail-open（脏值漏进实现抛 TypeError） | **等宽 + 未知类型 fail-closed**；实测 `等待秒:"abc"` 500→**400**、`字节:123` → **400「必须是 字节集型」** |
| `组件控件` 非法类型 | 注册 `任意`、事实源 `结果型<窗口句柄型>` | 双侧对齐正式类型（`字典`／`结果型` + 真实 `值结构`） |
| 发布门禁空集项 | 恒报「无需声明」字样（空转） | 显式「跳过（空集，不构成证据）」 |

### 维修自身引入并被纠正的回归（全部记录在案）
1. `句柄失效` 被映射成 400 → 回退到文档口径 500（对齐既有场景）。
2. 1-1 新增验证场景含静态绝对路径 + 多写一个键 → 回到恰好四键。
3. N6 类型校验收紧 → 逼出 `组件控件` 的伪类型脏数据（这正是「空转校验变真校验」的应有代价，已修数据而非放松判据）。
4. 我的验收探针把「合法文本 `"123"`」误标为类型错 → 修正断言并补真负向探针。

## 四、下一轮输入（已核实，非猜测）

| 项 | 实测范围 | 性质 |
|---|---|---|
| `默认值` 声明双精度数型却写整数 | **142 处 / 36 文件** | 根因项：只改示例治表象，删示例重建即复发 |
| 注册用短类型名 | 41 处（如 `组件控件` 的 `文本`/`句柄`，归一可容） | 口径统一，非阻断 |
| 注册 `返回口径` 旧写法 `结果` | 322 处 | 归一可容（`结果`→`结果型`），机械批处理 |
| `系统探针` 未升为正式包 | 依赖无法登记（三种形状全判红） | 结构项：需新建 `支持库/适配层/系统资源提供者/` |
| `组件控件` 其余短类型名 | 该包内 | 与上一行同类的口径统一 |

### 本轮新发现的真问题（非原清单）
- **文本型未拒 int**：`重试调度` 传 `实际错误码=123`（int）被自动当文本接受 → 与「整数型拒文本」不对称（类型边界放宽，`子报告_03` 同类）。
- **验证场景禁止静态绝对路径**：1-1 新增的注入载荷场景用静态绝对路径 → 验证器整轮拒绝。已改（删该步、保留危险命令拦截步），并把注入证明留在探针集与实现级用例。
- psycopg 库名 URL 未反转义 / scheme 未校白名单（已修）；`验证场景.json` `最大压缩比:1` 写成整数（已修）；调用示例 138 处同类 int/float（**只报未改**）。

---

## 四、需华哥一句话

1. **`AGENTS.md` 的 MCP 访问守卫补丁**：写入被受保护文件审批拦住（超时＝未同意）。补丁正文见本文第五节，说「可以写」我就落。
2. **依赖防火墙 13 条的口径**：`MCP工具箱`（平台自身 MCP 服务实现）直连 mcp/starlette/uvicorn 是「合法环境依赖」还是「必须经适配层」？这决定 9 条是豁免还是改造。
3. **发布门禁「第三方访问声明」空集语义**（唯一真口径项）：现役制品不含 `支持库/`，该项恒为空集。按哲学第 11 条建议：不判红，但显式声明「空集不构成证据」。

---

## 五、AGENTS.md 待批补丁（MCP 访问守卫，`### MCP 连接与阻断` 段）

访问守卫分两层：**B 层 = 回环 + Host/Origin，始终生效**；**A 层 = 令牌，默认不启用**，设置环境变量 `MCP工具箱_访问令牌` 后生效（缺失/错误返回 401，同时接受 `Authorization: Bearer <令牌>` 与 `X-MCP-Token`）。
- **启用**：`env "MCP工具箱_访问令牌=<ASCII 令牌>" ./MCP工具箱/启动HTTP服务.sh 8766`。令牌**必须是 ASCII**（两个客户端都拒绝非 ASCII 头值）；bash 不能 `export`/不能用中文变量名，故用 `env` 传入，脚本内部以 `printenv` 读取。
- **客户端 header**：`opencode.json` 用 `Bearer {env:MCP工具箱_访问令牌}`；`.mcp.json` 只能用 ASCII 名 `${MCP_TOOLKIT_TOKEN}`（Claude Code 的 `${}` 不解析中文变量名）→ 启用时需**同名同值**额外导出该 ASCII 别名。
- **回退**：未设令牌即不强制（两处 header 展开为空值，实测对未启令牌的 8766 仍 200 且客户端连通）；此时 8766 只由 B 层保护，**本机任意进程仍可无凭证调用全部工具**。
