# 子报告 11 · 边缘目录覆盖专项：前端核心 / 后端核心 / 项目适配层 / MCP工具箱 / 启动监督器 / 客户端 / 示例项目 / 运维脚本 / 技能库 / 公共契约

> 冻结边界：分支 `主干`、HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`、工作树 `0 脏`。审计全程只读；本次唯一写入 = 本报告文件与 `/tmp` 临时文件。
> 审计时间起点 2026-09-15 13:44（总纲落盘）。所有命令以 `unset PYTHONPATH` + `python3.14` 执行；`git` 用 `/Library/Developer/CommandLineTools/usr/bin/git`。

## 一句话结论（总）

这十个「正式审计少覆盖」的边缘目录里，**公共契约、项目适配层（自身）、技能库三块基本立得住**（适配层 0 处越界 import、配置合并是真实现；技能库六环是真机制、摘要双向闭合、被 6 条链路真实依赖）；但**边缘目录整体处于「声明与现场不一致」的高发状态**：5 项阻断集中在「玩具/工具目录直接拿到写权限与执行权」（MCP `workspace` 可提交任意 git 仓库、`test_resource` 可自杀 8766 进程组、8766 无认证；`模块库/直播逐字稿` 这一**正式包**把业务项目绝对路径与业务品牌词写进底座源码并随制品分发；**AGENTS.md 自己列为正式验收入口的 `示例项目/适配层示例/运行入口/运行.py` 在冻结 HEAD 上真实运行失败**），28 项重要集中在「声称的职责没有实现/没有接线，而没人用的旧实现还留着」（前端核心与启动监督器大面积零调用零测试、依赖防火墙三目录免检、指纹门禁生产恒假、类型表文档与机器表分叉、契约版本双口径）。

## 分级计数

| 范围 | 阻断 | 重要 | 建议 | 小计 |
|---|---|---|---|---|
| 前端核心 / 后端核心 / 启动监督器 / 客户端 / 运维脚本（8 个 py） | 0 | 9 | 5 | 14 |
| 项目适配层（27 文件） | 0 | 2 | 12 | 14 |
| 全仓「业务项目引用」铁律专项 | 1 | 3 | 6 | 10 |
| MCP工具箱（25 文件） | 3 | 7 | 6 | 16 |
| 示例项目（25 文件） | 1 | 3 | 6 | 10 |
| 技能库（30 文件） | 0 | 1 | 3 | 4 |
| 公共契约（26 文件） | 0 | 4 | 5 | 9 |
| **合计** | **5** | **29** | **43** | **77** |

## 目录规模口径（现场实测，含 `__pycache__` 与去缓存两口径）

| 目录 | 父会话口径（含 `__pycache__`） | 本次去缓存文件数 | 说明 |
|---|---|---|---|
| 公共契约 | 50 | 26 | 8 个 py 子目录 + `HTTP路由契约.json` |
| 前端核心 | 6 | 3 | `前端核心.py` / `浏览器交互.py` / `渲染提供者.py` |
| 后端核心 | 2 | 1 | `后端核心.py`（1639 行） |
| 启动监督器 | 4 | 2 | `健康监督.py` / `容量基线.py` |
| 客户端 | 2 | 1 | `构建平台客户端.py` |
| 项目适配层 | 51 | 27 | 10 个子包 + 3 个项目配置 |
| MCP工具箱 | 47 | 25 | 22 个 py + 1 README + 2 sh |
| 示例项目 | 37 | 25 | 5 个示例 + `示例项目.py` |
| 运维脚本 | 1 | 1 | `热接入.py` |
| 技能库 | 32 | 30 | 第三根正式包根（1 个正式包 + 验证夹具） |

> 父会话口径 = `find <目录> -type f | wc -l`；去缓存口径 = 追加 `-not -path '*__pycache__*'`。两口径都对，差值全部是 `__pycache__/*.pyc`。

## 本次审计与边界遵守

- 全程未改源码/配置/运行数据/制品/数据库；未删文件；未重启 8766/40007；未跑发布门禁；未重编制品。
- 现场复核（审计收口时）：`git status --porcelain` → 仅 `?? 开发文档/临时文档/`；`git diff --stat` → 0 行；`git rev-parse HEAD` → `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`，与冻结值一致。
- 只读探测（非改动）：`lsof`、`curl` 本机 8766、`plutil -p` 只读读取 LaunchAgent、`python3.14 -c` 内存内直调。
- 方法：先读代码再下判定；文档声称一律独立复现；「语法通过」≠「可导入」；「HTTP 200」≠「能力真实可用」；不采信历史报告与子代理自述作为终判（关键 5 项阻断均由主代理回读源码行复核）。

---

## 第 7 节 · 公共契约（26 文件 / 1904 行 py + 142 行 json + 108 行 md）

### 第 7 节小结

公共契约是本次覆盖里**结构最整齐**的一块：8 个 py 子目录 6 个有包级中文入口、类型/错误/版本/生命周期/句柄五套契约都在被大量真实引用（`结果` 149 处、`能力契约` 171 处、`包声明` 50 处、`调用器` 44 处、`契约版本` 41 处、`生命周期` 40 处、`类型表` 22 处均按「生产代码（排除测试中心/工程缓存）」统计）。但**「唯一事实源」这句话在 4 个地方不成立**：文档类型表与机器类型表分叉、契约版本存在 1.0.0/2.0.0 双口径、冻结的错误码常量 10 个零引用、唯一有界 HTTP 服务器契约实现被复制 4 份且零测试。

### P-01【重要】`HTTP路由契约.json` 与 7 处代码把「契约版本」写成 `1.0.0`，与唯一事实源 `2.0.0` 不一致

- **级别**：重要
- **影响**：`开发文档/项目说明.md:248`（华哥第五轮裁决第 20 条）声称「全平台的『契约版本』恒等于事实源 `公共契约/版本规则/契约版本.py`，不分散到各包各自演进」。现场存在两套口径：事实源 `2.0.0`，而路由契约与运行核心/适配层/模板生成器共 7 处默认 `1.0.0`。按哲学第 21 条版本不一致本身不失败（只回报差异），所以这不是功能故障，而是**「唯一」这个前提不成立**——后续任何按「契约版本」做的强制参数门禁会在两套基线之间摇摆。
- **证据**：
  - `公共契约/HTTP路由契约.json:2` → `"契约版本": "1.0.0"`（同文件 `:8` 请求字段说明亦为「默认 1.0.0」）
  - `公共契约/版本规则/契约版本.py:15` → `契约版本 = "2.0.0"`（文件头部自述「事实源只有这一处」）
  - `运行核心/加载器/版本系统/版本注册表.py:31`、`:94`（`契约版本: str = "1.0.0"`）
  - `运行核心/能力调用/HTTP连接器.py:44`（构造参数默认 `契约版本: str = "1.0.0"`）
  - `运行核心/加载器/提供者隔离/独立进程.py:237`、`:469`
  - `项目适配层/依赖锁定/依赖锁定.py:23` → `契约版本常量 = "1.0.0"`
  - `开发工具/组件规范/模块模板生成器.py:17` → `契约版本 = "1.0.0"`
  - 消费侧 `运行核心/统一网关/网关核心.py:643-647` 会回带「请求版本 / 当前版本 / 差异原因」三项，不失败。
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
  grep -rn '契约版本[^=]*=[^=]*"1\.0\.0"' --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存
  python3.14 -c "from 公共契约.版本规则.契约版本 import 契约版本; print(契约版本)"
  ```
- **真实输出摘录**：
  ```
  ./开发工具/组件规范/模块模板生成器.py:17:契约版本 = "1.0.0"
  ./运行核心/加载器/提供者隔离/独立进程.py:237:             契约版本: str = "1.0.0") -> 进程调用结果:
  ./运行核心/加载器/提供者隔离/独立进程.py:469:             契约版本: str = "1.0.0", 资源键: str | None = None) -> 进程调用结果:
  ./运行核心/加载器/版本系统/版本注册表.py:31:    契约版本: str = "1.0.0"
  ./运行核心/加载器/版本系统/版本注册表.py:94:    def 注册版本(self, 包id: str, 版本: str, *, 契约版本: str = "1.0.0",
  ./运行核心/能力调用/HTTP连接器.py:44:                 默认超时秒: float = 10.0, 契约版本: str = "1.0.0") -> None:
  ./项目适配层/依赖锁定/依赖锁定.py:23:契约版本常量 = "1.0.0"
  --- 计数 --- 7
  2.0.0
  ```
- **状态**：已核实。

### P-02【重要】正式类型表（机器）16 项 vs 类型目录（文档）15 项：`JSON值型` 文档里完全没有，但已被 7 个包真实使用

- **级别**：重要
- **影响**：`公共契约/基础类型/类型表.py:3-4` 的 docstring 声称「契约编译器、能力搜索和 IDE 只允许消费这里的名称；历史短名不属于运行时兼容层」。事实上机器表与文档表已经分叉：`JSON值型` 是真在被用的正式类型，却不在 `类型目录.md` 的表格与「命名冻结」清单里；另一个方向，`类型目录.md:40` 列了一串「历史短名」，两个文件没有任何自动比对。类型名唯一性靠人肉维护。
- **证据**：
  - `公共契约/基础类型/类型表.py:8-11`：16 项（含 `"JSON值型"`）
  - `公共契约/基础类型/类型目录.md:9-23`：表格 15 行，无 `JSON值型`
  - `公共契约/基础类型/类型目录.md:34-38`：命名冻结清单 15 项，无 `JSON值型`
  - 真实使用（抽样）：`支持库/适配层/YAML提供者/能力契约/参数契约.json:64`、`支持库/后端/数据操作支持库/数据交换/能力契约/参数契约.json:355`、`支持库/后端/数据操作支持库/数据集合/能力契约/参数契约.json:1334`、`支持库/后端/数据操作支持库/数据集合/包声明.json:492,535`
  - 全仓 `JSON值型` 命中 25 处（`grep -rho '"类型"[[:space:]]*:[[:space:]]*"[^"]*"'` 值分布统计）
- **复现命令**：
  ```bash
  unset PYTHONPATH; python3.14 -c "from 公共契约.基础类型.类型表 import 正式类型表; print(len(正式类型表), sorted(正式类型表))"
  grep -c "JSON值型" 公共契约/基础类型/类型目录.md
  grep -rn "JSON值型" --include='*.json' 支持库 | wc -l
  ```
- **真实输出摘录**：
  ```
  16 ['JSON值型', '列表型', '单精度数型', '双精度数型', '句柄型', '字典型', '字节型', '字节集型', '整数型', '文本型', '日期时间型', '空值型', '结果型', '资源引用型', '逻辑型', '长整数型']
  0        ← 类型目录.md 里 JSON值型 命中数
  ```
- **状态**：已核实。

### P-03【重要】`错误结构.py` 冻结的「句柄层」错误码全仓 0 引用；真实返回码是另一个词「句柄失效」，且以裸字符串散落在 20+ 处

- **级别**：重要
- **影响**：`公共契约/错误结构/错误结构.py:58` 自述「网关边界错误码（HTTP 层统一，工作包 1 冻结）」。实测这 10 个常量在全仓（排除自身与自身 `__init__`）**0 引用**：`错误码_句柄无效`、`错误码_句柄已过期`、`错误码_资源释放失败`、`错误码_幂等键冲突`、`错误码_提供者崩溃`、`错误码_调用已取消`、`错误码_状态跳转非法`、`错误码_循环依赖`、`错误码_提供者冲突`、`错误码_资源未清理`。真实代码里的句柄类错误码是 `"句柄失效"`（`错误结构.py` 里没有这个常量），`项目说明.md:381` 的口径也是 `句柄失效`。除 `限流`(6) 与 `返回结果不符合契约`(4) 外，其余常量形同装饰 → 错误码没有单一常量出处，全都靠字面量对齐，符合「空转即杀」（哲学第 11 条）该杀的范围。
- **证据**：
  - `公共契约/错误结构/错误结构.py:68-69`（`句柄无效` / `句柄已过期`）、`:58-76`（网关边界区块）
  - `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:122,134,144` → `结果.失败("句柄失效", …)`
  - `支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py:168,202,222,248,271,295,328,360,389,417,462,489,516`（13 处 `"句柄失效"`）
  - `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:367` → `_失败("句柄失效", 原因)`
  - `公共契约/生命周期/生命周期.py:84` 用字面量 `"状态跳转非法"` 而不是 `错误码_状态跳转非法`
- **复现命令**：
  ```bash
  for n in 错误码_句柄无效 错误码_句柄已过期 错误码_资源释放失败 错误码_幂等键冲突 错误码_提供者崩溃 错误码_调用已取消 错误码_状态跳转非法 错误码_循环依赖 错误码_提供者冲突 错误码_资源未清理; do
    printf "%-22s %s\n" "$n" "$(grep -rn "$n" --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存 | grep -v '公共契约/错误结构' | wc -l)"
  done
  grep -rn '"句柄失效"' --include='*.py' 支持库 | wc -l
  ```
- **真实输出摘录**：
  ```
  错误码_句柄无效   0
  错误码_句柄已过期 0
  错误码_资源释放失败 0
  错误码_幂等键冲突 0
  错误码_提供者崩溃 0
  错误码_调用已取消 0
  错误码_状态跳转非法 0
  错误码_循环依赖   0
  错误码_提供者冲突 0
  错误码_资源未清理 0
  ```
- **状态**：已核实。

### P-04【重要】「有界线程HTTP服务器」4 份实现共存；唯一契约实现本身 0 测试，而门禁断言的是被复制进制品的第 3 份

- **级别**：重要
- **影响**：`公共契约/运行时/有界HTTP.py` 是平台声明的统一有界 HTTP 服务实现（`最大线程数` 可配），另有 3 份各自实现：
  1. `运行核心/统一网关/本地网关.py:26` — 自带一份（构造参数名 `最大工作线程`，与契约实现签名不同）；
  2. `开发工具/项目编译/项目编译器.py:529` — 编译制品时把一份类定义**内联进生成代码**（`启动器`）；
  3. `开发工具/HTML验证/验证服务.py:101` — 内联一份，硬编码 `BoundedSemaphore(32)`。
  违反 `项目说明.md:47`「同一职责的内部实现也不得复制」。更关键的是：`测试中心/第一批维修/测试_统一编译器.py:41-42` 用 `assertIn("有界线程HTTP服务器", 启动器)` / `assertIn("BoundedSemaphore(并发上限)", 启动器)` **把第 3 份复制品的文本当成正确性断言**，而契约实现 `公共契约/运行时/有界HTTP.py` 在测试中心的直接引用数为 0。
- **证据**：见上四处行号 + `测试中心/第一批维修/测试_统一编译器.py:41-42`
- **复现命令**：
  ```bash
  grep -rn "有界线程HTTP服务器\|BoundedSemaphore" --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存 | grep -E 'class 有界线程HTTP服务器|BoundedSemaphore\('
  grep -rl "有界HTTP" 测试中心 --include='*.py' | wc -l
  ```
- **真实输出摘录**：
  ```
  ./公共契约/运行时/有界HTTP.py:10:class 有界线程HTTP服务器(ThreadingHTTPServer):
  ./运行核心/统一网关/本地网关.py:26:class 有界线程HTTP服务器(ThreadingHTTPServer):
  ./开发工具/项目编译/项目编译器.py:529:    class 有界线程HTTP服务器(ThreadingHTTPServer):
  ./开发工具/HTML验证/验证服务.py:101:    class 有界线程HTTP服务器(ThreadingHTTPServer):
  ./公共契约/运行时/有界HTTP.py:18:        self._线程信号量 = threading.BoundedSemaphore(max(1, int(最大线程数)))
  ./运行核心/统一网关/本地网关.py:38:        self._工作线程信号量 = threading.BoundedSemaphore(最大工作线程)
  ./开发工具/项目编译/项目编译器.py:534:            self._线程信号量 = threading.BoundedSemaphore(并发上限)
  ./开发工具/HTML验证/验证服务.py:106:            self._线程信号量 = threading.BoundedSemaphore(32)
  --- 测试中心直接引用 有界HTTP: 0
  ```
- **状态**：已核实。

### P-05【建议】`公共契约/运行时/` 与 `公共契约/诊断/` 缺 `__init__.py`；`公共契约/__init__.py` 是 0 字节空文件

- **级别**：建议
- **影响**：同层 6 个子目录（包声明/基础类型/版本规则/生命周期/能力契约/错误结构）都有包级中文入口并写 `__all__`，只有 `运行时` 与 `诊断` 没有 → 靠隐式命名空间包工作，跨包必须写成 `from 公共契约.运行时.有界IO import …`（子模块深引）。`公共契约/__init__.py` 内容为 0 字节，整个公共契约没有包级入口，与 `AGENTS.md:204-218`「`__init__.py`：包级中文入口」的约定不齐。功能正常，属结构一致性问题。
- **证据**：`公共契约/运行时/`、`公共契约/诊断/` 目录清单；`wc -c 公共契约/__init__.py` → 0；对比 `公共契约/生命周期/__init__.py`（10 行、有 `__all__`）
- **复现命令**：
  ```bash
  for d in 公共契约 公共契约/包声明 公共契约/基础类型 公共契约/版本规则 公共契约/生命周期 公共契约/能力契约 公共契约/错误结构 公共契约/运行时 公共契约/诊断; do
    printf "%-22s __init__.py=%s\n" "$d" "$([ -f "$d/__init__.py" ] && echo 有 || echo 缺)"; done
  wc -c 公共契约/__init__.py
  ```
- **真实输出摘录**：
  ```
  公共契约/运行时 __init__.py=缺
  公共契约/诊断    __init__.py=缺
         0 公共契约/__init__.py
  ```
- **状态**：已核实。

### P-06【建议】`HTTP路由契约.json` 使用 2 个不在正式类型表里的类型名（`任意型`、`对象型`）

- **级别**：建议
- **影响**：唯一契约解析器 `开发工具/契约编译/聚合契约解析.py` 只把 `任意`（常量 `任意类型 = "任意"`）当特殊值处理，其余类型一律要过 `正式类型表`（严格模式 `:62-63` 报「不在正式类型表」）。`HTTP路由契约.json` 写了 `"值": "任意型"` 与 `"参数": "对象型（必填）"`，这两个名字既不是 `任意` 也不在正式类型表。当前未触发门禁（该文件不按 S0 聚合格式解析），属潜在踩点；`开发工具/HTML验证/返回判定.py:12` 里另有 `"对象型"` 映射，说明它有实际语义但从未登记为正式类型。
- **证据**：`公共契约/HTTP路由契约.json:5`、`:26`、`:8-9`；`开发工具/契约编译/聚合契约解析.py:14,18,59-63`；`开发工具/HTML验证/返回判定.py:12`
- **复现命令**：
  ```bash
  python3.14 -c "from 公共契约.基础类型.类型表 import 正式类型表; print('任意型' in 正式类型表, '对象型' in 正式类型表)"
  grep -rn '"任意型"\|"对象型"' --include='*.json' --include='*.py' 公共契约 开发工具 支持库 | grep -v __pycache__
  ```
- **真实输出摘录**：
  ```
  False False
  公共契约/HTTP路由契约.json:5:    "值": "任意型",
  公共契约/HTTP路由契约.json:26:      "值": "任意型",
  开发工具/HTML验证/返回判定.py:12:        "对象型": lambda 项: isinstance(项, dict),
  ```
- **状态**：已核实。

### P-07【建议】`句柄体系` 并发保护不完整：5 个方法持 `_锁`，另 5 个方法读写同一批字段不持锁

- **级别**：建议
- **影响**：文件自述「公共句柄契约与唯一状态机实现」，并用 `threading.Lock()` 保护状态。实测持锁点只有 5 处（`创建句柄:76`、`恢复句柄:92`、`登记资源:114`、`查询资源:128`、`核查回收:247`）；`校验:101`（读 `句柄表` + 读 `状态`）、`失效:267`（**写** 状态/失效时间/失效原因 三字段，并在失败时回滚三字段）、`查询回收证据:285`、`活跃句柄数:288`、`状态快照:291` 全部无锁。同一文件内两套并发口径；`失效()` 与 `校验()` 可交错出「已判定有效但状态正在被改写」的窗口。
- **证据**：`公共契约/句柄体系.py:73`（`self._锁`）、`:76,92,114,128,247`（持锁）、`:101-107`、`:267-283`、`:285-292`（无锁）
- **复现命令**：
  ```bash
  grep -n "with self._锁\|    def " 公共契约/句柄体系.py
  ```
- **真实输出摘录**：
  ```
  75:    def 创建句柄(...)          76:        with self._锁:
  86:    def 恢复句柄(...)          92:        with self._锁:
  101:    def 校验(...)               ← 无锁
  111:    def 登记资源(...)         114:        with self._锁:
  126:    def 查询资源(...)         128:        with self._锁:
  245:    def 核查回收(...)         247:        with self._锁:
  267:    def 失效(...)              ← 无锁（写状态）
  285:    def 查询回收证据(...)      ← 无锁
  288:    def 活跃句柄数(...)        ← 无锁
  291:    def 状态快照(...)          ← 无锁
  ```
- **状态**：已核实（静态判定；未构造并发实测，故不升「重要」）。

### P-08【建议】句柄 id 的整数合法域与字符串合法域不对称

- **级别**：建议
- **影响**：`生成句柄id()` 产出 `1..999999` 的整数；`是合法句柄id()` 对字符串要求**恰好 6 位十进制**。于是 `1..99999` 这一段的整数句柄是合法的，但按文档「字符串仅兼容固定六位数字格式」表达不出来（`"12345"` 判非法、`"012345"` 才合法）。当前 40007 走整数句柄，问题潜伏；任何把句柄经 JSON 文本/HTTP 头转手的路径会踩到。
- **证据**：`公共契约/句柄体系.py:30`（`句柄位数 = 6`）、`:35-39`、`:42-44`；`开发文档/项目说明.md:349`；`公共契约/HTTP路由契约.json`（`句柄边界`：「1 到 999999 的整数」）
- **复现命令**：
  ```bash
  python3.14 -c "
  from 公共契约.句柄体系 import 是合法句柄id
  print(是合法句柄id(12345), 是合法句柄id('12345'), 是合法句柄id('012345'))"
  ```
- **真实输出摘录**：`True False True`
- **状态**：已核实。

### P-09【建议】`包声明.转字典()` 丢失 `配置项` 与 `已废弃` 两个字段（构造接得住、转出不带）

- **级别**：建议
- **影响**：`从字典构建()` 会接收并存下 `配置项` 与 `已废弃`（`:114,116`），但 `转字典()`（`:58-70`）只输出 10 个键，`配置项`/`已废弃` 不在其中。任何「声明 → 转字典 → 落盘/透传」的链路会静默丢这两项（`已废弃` 是装配跳过判据，丢了会把废弃包当正常包处理）。
- **证据**：`公共契约/包声明/声明.py:53-54`（字段）、`:58-70`（转字典键集合）
- **复现命令**：
  ```bash
  python3.14 -c "
  from 公共契约.包声明 import 从字典构建
  o=从字典构建({'包id':'x','名称':'测试','类型':'支持库','版本':'1.0.0','能力':[],'配置项':[{'键':'a'}],'已废弃':True})
  print(sorted(o.转字典())); print('已废弃' in o.转字典(), '配置项' in o.转字典())"
  ```
- **真实输出摘录**：
  ```
  ['依赖', '入口', '内部层', '包id', '名称', '来源路径', '版本', '类型', '能力', '说明']
  False False
  ```
- **状态**：已核实。

### 第 7 节 · 公共契约测试覆盖实测

| 契约文件 | 测试中心直接引用文件数 | 生产代码引用文件数 | 判定 |
|---|---|---|---|
| 结果类型 / 结果 | 24 | 149 | 覆盖充分 |
| 能力契约 / 契约 | 111 | 171 | 覆盖充分 |
| 包声明 / 声明 | 55 | 50 | 覆盖充分 |
| 调用器 | 30 | 44 | 覆盖充分 |
| 契约版本 | 24 | 41 | 覆盖充分 |
| 生命周期 | 21 | 40 | 覆盖充分 |
| 比较（版本） | 2 | 27 | 覆盖偏薄 |
| 数值类型 | 2 | 2 | 覆盖偏薄 |
| 端口策略 | 2 | 7 | 覆盖偏薄 |
| 句柄体系 | 2 | 12 | 覆盖偏薄 |
| 忽略记录 | 4 | 10 | 一般 |
| 文档结构 | 1 | 12 | 覆盖偏薄 |
| 有界IO | 1 | 24 | 覆盖偏薄（被 24 处适配层依赖，只有 1 个测试文件） |
| 类型表 | 3 | 22 | 覆盖偏薄 |
| 运行缓存 | 2 | 10 | 覆盖偏薄 |
| 错误结构 | 0 | 7 | **无直接测试** |
| 有界HTTP | 0 | 3 | **无直接测试**（且见 P-04） |

> `测试中心/公共契约/` 实测只有 5 个测试文件：`测试_包声明契约.py`(55 行)、`测试_句柄资源预算.py`(41)、`测试_数值类型契约.py`(45)、`测试_有界IO.py`(93)、`测试_能力契约.py`(56)，合计 290 行，对应 1904 行契约代码。

---

## 第 1 节 · 前端核心 / 后端核心 / 启动监督器 / 客户端 / 运维脚本

> 本节由专项子审计产出，原文完整保留；数据与证据格式同上（path:行号 + 真实命令 + 真实输出摘录）。

### 子审计 A：前端核心 / 后端核心 / 启动监督器 / 客户端 / 运维脚本（8 个 py）

- 审计对象：系统工程平台（底座）根 = `~/Documents/Agent/PHP/系统工程平台`
- 冻结边界：HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`，分支 `主干`（实测 `git rev-parse HEAD` 与 `git branch --show-current` 一致）
- 口径：只认现场证据（path:行号 + 真实命令 + 真实输出摘录）；不采信文档声称；三档分级（阻断/重要/建议）
- 写盘限制：仅写 `/tmp`；未改仓库任何跟踪文件（`git status --porcelain` 仅见既有未跟踪 `?? 开发文档/临时文档/`）

### 一句话结论

8 个文件全部语法通过、全部可只读导入、无一是只有 `pass/docstring` 的空壳；但 **4 个文件存在真实的治理缺口**：`启动监督器/容量基线.py` 与 `前端核心/渲染提供者.py` 是零调用者零测试的**死代码**，`启动监督器/健康监督.py` **只有测试在调用、生产无接线**，`运维脚本/热接入.py` 所在目录 **AGENTS.md 未声明、发布门禁与编译器正式目录也不含它**（游离于治理之外）；另有 1 个已真实复现的**装配模板全局缓存不按系统根分键**缺陷（换根实例静默拿到别根注册表并返回成功）。

### 分级计数

- 阻断：**0**
- 重要：**9**
- 建议：**5**

### 8 文件逐项结论

| # | 文件 | 行数 | 职责与 AGENTS.md:170-193 一致性 | 测试覆盖 | 错误处理/资源释放 | 重复第二实现 | 空壳 | 真实调用者 |
|---|---|---|---|---|---|---|---|---|
| 1 | `前端核心/前端核心.py` | 150 | 一致（窗口/页面/组件/状态/事件/路由 + 渲染分派） | 有：`测试_前端核心唯一调用.py`（2 用例，只覆盖 设置调用入口/调用后端能力） | 无线程/进程/句柄；`渲染()` 无 try（不产生资源） | `窗口定义/组件定义` 与 `支持库/前端/*描述` 语义重叠（分层设计，非第二实现） | 否 | 测试（2）+ `示例项目/前后端核心示例/运行示例.py:12,22`；渲染链零调用 |
| 2 | `前端核心/浏览器交互.py` | 240 | 一致（渲染宿主：生成页面 + 只经 HTTP 网关调用） | 测试中心 0 引用；仅发布门禁 `运行发布门禁.py:1201` 作为生产检查调用 | 无线程/句柄；JS 侧 超时器 `clearTimeout` 在 finally（157/202） | 与 `支持库/前端/浏览器宿主/实现/浏览器宿主.py:28` 页面生成重复 | 否 | `开发工具/发布门禁/运行发布门禁.py:1201`、`前端核心/渲染提供者.py:52` |
| 3 | `前端核心/渲染提供者.py` | 68 | 一致（渲染提供者=渲染宿主的具体实现） | **无（0）** | 无资源 | `_安全脚本数据` 与 `浏览器交互.py:22-27` 逐字符重复 | 否（有真实实现） | **零调用者（死代码）** |
| 4 | `后端核心/后端核心.py` | 746 | 一致（执行单元/请求上下文/能力执行/宿主生命周期） | 强：21 个测试文件引用，专项 `测试_后端核心关闭.py`、`测试_后端核心唯一调用.py` | 有界：无自建线程/进程；`调用` 与 `_调用内部` 有 finally（502-514、521-539）；关闭走 `资源句柄服务.关闭服务()`（718/733） | 未发现重复：调用委托 `运行核心.唯一能力调用`、装配委托 `运行核心.加载器`、资源委托 `运行核心.资源协调`（网关 `网关核心.py:443-444` 直接回调后端核心，无第二套健康判定） | 否 | 生产：`运行核心/启动运行核心网关.py:5,10`（40007 常驻）；测试 21 文件 |
| 5 | `启动监督器/健康监督.py` | 587 | 部分一致（只覆盖「健康检查」；不含核心启动/版本切换/回退） | 强：`测试_健康监督.py` 18、`测试_健康配置.py` 26、`测试_提供者生命周期.py` 1 = 45 用例 | 有界：daemon 线程 + `停止周期检查` join（257）、证据保留策略裁剪（380-467）、证据写失败不 raise（347） | `汇入进程健康` 明确「不做第二套健康判定」（207-215），与 `提供者生命周期.健康检查` 无重复 | 否 | **只有测试**（3 文件），生产 0 接线 |
| 6 | `启动监督器/容量基线.py` | 166 | 部分一致（只覆盖「资源限制」；不含核心启动/版本切换/回退） | **无（0）** | 有锁、有排空超时（109），但证据文件无任何裁剪/上限（127-136） | 未发现 | 否（有真实实现） | **零调用者（死代码）** |
| 7 | `客户端/构建平台客户端.py` | 634 | 一致（平台客户端制品构建脚本） | 强：`测试_客户端构建安全.py` 21 + `测试_平台客户端制品接入.py` 14 = 35 用例 | 好：目录 fd `O_NOFOLLOW` + `finally os.close`（166-170、207-215）；**`构建()` 无 try/finally → 失败残留临时制品目录（537-555）** | 无（调用 `平台控制面/包仓库/平台客户端制品` 公开入口，非第二实现） | 否 | 手工/文档化脚本 + 2 测试文件；不在任何自动发布入口链路 |
| 8 | `运维脚本/热接入.py` | 127 | **AGENTS.md 目录约定未声明该目录**（172-191 无 `运维脚本/`）；按 AGENTS.md:168「独立脚本未被发布入口调用等同门禁不存在」 | **无 unittest**；黑盒回归在 `开发工具/HTML验证/热接入回归.py`（需 40007 在线，本次未运行） | 请求有超时；`_网关进程号` 的 `subprocess.run` **无超时**（119-123） | 无（是对 `/网关/热接入` 的唯一运维入口） | 否 | 人工执行（`开发文档/决策记录/0009` 等运维步骤）；无程序调用者 |

### 问题清单

##### A-01【重要】`后端核心.装配()` 装配模板是模块级全局、不按 `系统根目录` 分键：换根实例静默拿到别根注册表并返回成功
- **影响**：同一进程内第二个 `后端核心(另一个系统根)` 调 `装配()/启动()` 时，不再扫描它自己的 `支持库/模块库/技能库`，而是深拷贝第一个根的注册表，能力表错误却返回 `成功=True, 值=<能力数>`；缓存命中分支（`:116`）还会**覆盖实例自带的注册表**（含 `注册能力()` 已注册项）。当前 40007 生产路径是单实例单根，暂不可达；一旦出现同进程第二个不同根（测试、`开发工具` 验证器、未来多实例/多制品同进程），即升级为阻断。
- **证据**：
  - `后端核心/后端核心.py:32` `_装配模板: 能力注册表 | None = None`（模块级全局）
  - `后端核心/后端核心.py:105-118`：`if _装配模板 is None:` 才用 `self.系统根目录` 装配；`else: self.注册表 = copy.deepcopy(_装配模板)` —— 分支内从不校验根是否相同
  - `后端核心/后端核心.py:121` 缓存命中后仍调 `初始化包指纹表()`（重新发现 `self.系统根目录`），与注册表事实源不一致
- **复现命令**（全部读写在 `/tmp`）：
```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
rm -rf /tmp/底座审计_装配模板
for R in A B; do mkdir -p "/tmp/底座审计_装配模板/$R/支持库/前端" "/tmp/底座审计_装配模板/$R/模块库" "/tmp/底座审计_装配模板/$R/技能库"; done
cp -R 支持库/前端/窗口描述      "/tmp/底座审计_装配模板/A/支持库/前端/"
cp -R 支持库/前端/基础组件描述 "/tmp/底座审计_装配模板/B/支持库/前端/"
python3.14 -c '
import sys; sys.path.insert(0, ".")
from pathlib import Path
from 后端核心.后端核心 import 后端核心
A = Path("/tmp/底座审计_装配模板/A"); B = Path("/tmp/底座审计_装配模板/B")
a = 后端核心(A); print("A 装配:", a.装配().成功, a.注册表.能力id列表)
b = 后端核心(B); r = b.装配(); print("B 装配:", r.成功, r.值, b.注册表.能力id列表)
'
```
- **真实输出摘录**：
```
A 装配: True | 值: 4 | 问题: None
A 能力: ['窗口描述.创建窗口定义', '窗口描述.反序列化窗口定义', '窗口描述.序列化窗口定义', '窗口描述.校验窗口定义']
B 装配: True | 值: 4
B 能力: ['窗口描述.创建窗口定义', '窗口描述.反序列化窗口定义', '窗口描述.序列化窗口定义', '窗口描述.校验窗口定义']
```
  （B 目录里只有 `基础组件描述`，却报出 `窗口描述.*` 四条能力 + `成功=True`）
- **已核实**（真实运行复现）

##### A-02【重要】`启动监督器/容量基线.py` 是死代码：全仓零调用者、测试中心零引用
- **影响**：AGENTS.md:175 声明 `启动监督器/` 承担「资源限制」，该文件是唯一实现（P1-16 声明基线/采样/超限处理链/熔断/证据），但无人装配、无人调用 → 「资源限制」这一职责实际不存在（表里写了、链上没有）。按华哥「空转即杀」应删或接线。
- **证据**：
  - `启动监督器/容量基线.py:59`（class 容量基线）、`:66`（默认证据文件）、`:130`（证据 id），是 `容量基线` 一词在全仓的**全部**出现位置（排除 `工程缓存/` 与 `开发文档/`）
  - `grep -rl 容量基线 测试中心 --include='*.py'` → 退出码 1（0 命中）
  - `开发文档/归档/2026-08_第十四阶段/工作包清单.json:345` 指向 `测试中心/平台控制面/测试_工作包16_容量基线.py`，该测试文件**已不存在**（`测试中心/平台控制面/` 实测无此文件）
  - `开发文档/项目说明.md`（唯一事实源）0 命中「容量基线」；`开发文档/未完成事项.md`（唯一债务清单）0 命中 → 这是**未登记的死代码**
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rn "容量基线" --include='*.py' --include='*.json' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/' | grep -v '^\./开发文档/'
grep -rl "容量基线" 测试中心 --include='*.py'; echo "exit=$?"
ls 测试中心/平台控制面/ | grep 容量基线 || echo "无 测试_工作包16_容量基线.py"
```
- **真实输出摘录**：
```
./启动监督器/容量基线.py:59:class 容量基线:
./启动监督器/容量基线.py:66:                         / "工程缓存" / "启动监督器" / "容量基线证据.jsonl")
./启动监督器/容量基线.py:130:            证据id = f"容量基线-{self._证据序号}"
exit=1
无 测试_工作包16_容量基线.py
```
- **已核实**

##### A-03【重要】`前端核心/渲染提供者.py` 是死代码：零调用者、零测试；发布门禁绕过它直连 `浏览器交互`
- **影响**：`测试渲染提供者 / HTML渲染提供者 / 创建渲染提供者` 三个公开类/函数无人使用；`前端核心/前端核心.py:139-146` 的渲染分派永远拿不到提供者（构造参数 `渲染提供者` 在生产与测试中从不注入）。同时 `宿主类型="浏览器HTML"` 只在死文件里定义 → 前端渲染能力实际只有门禁那条直连路径可用。
- **证据**：
  - `前端核心/渲染提供者.py:20/44/64`（三个公开入口）
  - 全仓「渲染提供者」命中只有 `前端核心/前端核心.py`（docstring/形参名）与 `前端核心/渲染提供者.py` 自身 → 0 外部引用
  - 门禁直连证据：`开发工具/发布门禁/运行发布门禁.py:1201` `from 前端核心.浏览器交互 import 浏览器交互提供者`（跳过 `渲染提供者`）
  - `grep -rl 渲染提供者 测试中心` → 0 命中
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rn "渲染提供者" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/' | grep -v '^\./前端核心/'
grep -rn "创建渲染提供者\|测试渲染提供者\|HTML渲染提供者" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
grep -rn "浏览器交互" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
```
- **真实输出摘录**：第一条命令无输出（`（无任何外部文件引用：零调用者）`）；第二条只见 `前端核心/渲染提供者.py` 与其自身 docstring；第三条含 `开发工具/发布门禁/运行发布门禁.py:1201`
- **已核实**

##### A-04【重要】`启动监督器/健康监督.py` 有实现、有 45 用例，但生产零接线（唯一调用者是测试）
- **影响**：AGENTS.md:175「健康检查」在生产从未运行：40007 网关启动脚本 `运行核心/启动运行核心网关.py` 不导入它；`汇入进程健康`（第三十阶段 C3 声明的运行核心→启动监督器接线）只在测试里被调用一次。配置化（周期/探针超时/失败阈值/证据保留策略）与证据裁剪在生产永不发生。
- **证据**：
  - `启动监督器/健康监督.py:110`（class）、`:207`（汇入进程健康）、`:569`（从配置创建健康监督）
  - 全仓调用点仅：`测试中心/启动监督器/测试_健康监督.py:22`、`测试中心/启动监督器/测试_健康配置.py:22`、`测试中心/运行核心/测试_提供者生命周期.py:32,377`
  - `grep -rn "健康监督\|容量基线" 运行核心 项目适配层 MCP工具箱 平台控制面 开发工具 示例项目 公共契约 支持库 模块库 --include='*.py'` → 仅两处 docstring 提及：`运行核心/环境指纹/环境指纹.py:77`、`项目适配层/配置适配/配置合并.py:6`
  - `grep -rn "汇入进程健康" --include='*.py' .`（排除 `工程缓存/`）→ 除定义处外只有 `测试_提供者生命周期.py:372-377`
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rn "from 启动监督器" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
grep -rn "汇入进程健康" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
grep -n "健康监督\|容量基线" 运行核心/启动运行核心网关.py || echo "网关启动脚本不含健康监督"
```
- **真实输出摘录**：第一条仅 3 个测试文件；第二条仅定义处 + `测试_提供者生命周期.py:372,377`；第三条 `网关启动脚本不含健康监督`
- **已核实**

##### A-05【重要】`启动监督器/` 目录职责与 AGENTS.md:175 不符：缺「核心启动」「版本切换与核心回退」两项实现
- **影响**：声明四项（核心启动 / 资源限制 / 健康检查 / 版本切换与核心回退），目录内只有 2 个 py，实测落地为：健康检查=`健康监督.py`（无生产接线）、资源限制=`容量基线.py`（死代码）；**核心启动**在 `运行核心/启动运行核心网关.py:10-11` + LaunchAgent `com.huashi.gateway-40007`；**版本切换与核心回退**在 `平台控制面/核心快照.py`（其 `:1` docstring 自称「核心快照与启动监督器」，`:26-104` 快照/签名/校验/健康检查）。目录约定与实际归属不一致，审计时无法按目录定位职责。
- **证据**：
  - `AGENTS.md:175` `启动监督器/ 核心启动、资源限制、健康检查、版本切换与核心回退`
  - `find 启动监督器 客户端 前端核心 后端核心 运维脚本 -maxdepth 1` → `启动监督器/` 只有 `健康监督.py`、`容量基线.py`
  - `运行核心/启动运行核心网关.py:10-11`（`后端 = 后端核心()` / `后端.启动()`）
  - `平台控制面/核心快照.py:1,26,31,58,72,104`
  - `~/Library/LaunchAgents/com.huashi.gateway-40007.plist`（`ProgramArguments = python3.14 运行核心/启动运行核心网关.py`）
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
sed -n '175p' AGENTS.md
find 启动监督器 -maxdepth 1 -type f
sed -n '1p;104p' 平台控制面/核心快照.py
plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist | grep -A3 ProgramArguments
```
- **真实输出摘录**：`启动监督器/ 核心启动、资源限制、健康检查、版本切换与核心回退`；`启动监督器/健康监督.py`、`启动监督器/容量基线.py`；`"""核心快照与启动监督器：不可变平台核心快照 + 签名/完整性验证 + 激活指针。`；`0 => "/opt/homebrew/bin/python3.14"` `1 => "运行核心/启动运行核心网关.py"`
- **已核实**

##### A-06【重要】`客户端/构建平台客户端.py` 深入另一包的私有实现（`_生成HTML` 等 4 个下划线函数）
- **影响**：违反铁律「跨包调用只能经公开契约与包级中文入口，禁止深入实现目录」。该文件是平台客户端制品壳的**唯一来源**，`开发工具/项目编译/项目编译器.py` 的私有签名/模板一变，构建静默产出残缺制品（`制品壳` 页面或启动器缺失），且不会被任何门禁识别为越权。
- **证据**：
  - `客户端/构建平台客户端.py:411` `from 开发工具.项目编译.项目编译器 import _生成HTML, _生成启动器, _写入并编译Python`
  - `客户端/构建平台客户端.py:458` `from 开发工具.项目编译.项目编译器 import 读取工作区字节指纹, _制品文件摘要`
  - 被导入者为私有：`开发工具/项目编译/项目编译器.py:103` `def _写入并编译Python`、`:115` `def _制品文件摘要`、`:422` `def _生成HTML`、`:434` `def _生成启动器`
  - 依赖防火墙看不到：`客户端/` 不在 `运行核心/依赖防火墙.py:33-43` 层表 → 归「其他」并 `:281-282` 免方向检查
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -n "from 开发工具" 客户端/构建平台客户端.py
grep -n "^def _生成HTML\|^def _生成启动器\|^def _写入并编译Python\|^def _制品文件摘要" 开发工具/项目编译/项目编译器.py
grep -n "客户端" 运行核心/依赖防火墙.py || echo "依赖防火墙层表不含 客户端"
```
- **真实输出摘录**：`411:    from 开发工具.项目编译.项目编译器 import _生成HTML, _生成启动器, _写入并编译Python`；`458:    from 开发工具.项目编译.项目编译器 import 读取工作区字节指纹, _制品文件摘要`；`103:def _写入并编译Python(...)`、`115:def _制品文件摘要(...)`、`422:def _生成HTML(...)`、`434:def _生成启动器(...)`；`依赖防火墙层表不含 客户端`
- **已核实**

##### A-07【重要】`运维脚本/` 目录不在 AGENTS.md 目录约定，也不在发布门禁/编译器/代码地图的正式目录表 → 游离于治理之外
- **影响**：`运维脚本/热接入.py` 是 40007 能力上线的唯一人工入口（`开发文档/决策记录/0009` 等把它写进上线步骤），但：①目录约定无此目录（合规矩性不成立）；②发布门禁的正式源码目录表不含它 → 英文命名扫描/违规扫描不覆盖该脚本；③项目编译器的「关键源码目录」不含它 → 来源指纹不覆盖它；④MCP 代码地图目录表不含它。命中 AGENTS.md:168「独立脚本未被发布入口调用等同门禁不存在」。
- **证据**：
  - `AGENTS.md:172-191`（目录约定代码块全文）只有 `启动监督器/`（175）、`客户端/`（187），**无 `运维脚本/`**；`grep -n "运维脚本" AGENTS.md` → 0 命中
  - `开发工具/发布门禁/运行发布门禁.py:67-70` `正式源码目录名表 = ("公共契约","平台控制面","启动监督器","运行核心","前端核心","后端核心","支持库","模块库","项目适配层","开发工具","MCP工具箱","客户端","示例项目")` → 无 `运维脚本`
  - `开发工具/项目编译/项目编译器.py:31-34` `关键源码目录` → 无 `运维脚本`
  - `MCP工具箱/项目服务.py:306-310` `全部目录表` → 无 `运维脚本`
  - `运维脚本/热接入.py` 无 `__pycache__`、无任何程序化调用者；全仓唯一程序引用是 `MCP工具箱/热重载.py:7` 的 docstring 提及
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
sed -n '172,191p' AGENTS.md
grep -n "运维脚本" AGENTS.md || echo "AGENTS.md 0 命中 运维脚本"
sed -n '67,70p' 开发工具/发布门禁/运行发布门禁.py
sed -n '31,34p' 开发工具/项目编译/项目编译器.py
sed -n '306,310p' MCP工具箱/项目服务.py
```
- **真实输出摘录**：`AGENTS.md 0 命中 运维脚本`；正式源码目录名表 13 项中无 `运维脚本`；`关键源码目录 = {".git","公共契约",...,"客户端","MCP工具箱","开发文档"}` 无 `运维脚本`；`全部目录表` 12 项无 `运维脚本`
- **已核实**

##### A-08【重要】`系统底座_指纹校验` 无任何地方设置 → 生产环境的包指纹门禁从未生效
- **影响**：`启用包指纹校验()` 是「被改动未重新热接入即报错」的唯一守门（防制品/源码静默漂移）。全仓唯一出现是读取处；生产 LaunchAgent 环境变量里没有它 → 40007 从未注入校验器，`校验包指纹`/`清空包指纹缓存` 在生产是不触发分支。按「恒真门禁直接删除」口径，这是**默认关闭的完整性门禁**。
- **证据**：
  - `后端核心/后端核心.py:224-232`（开关读取 `:226`；不满足即 `return False`）
  - `后端核心/后端核心.py:136` `self.启用包指纹校验()`；`:206-222` `校验包指纹`；`:420-421` `清空包指纹缓存`
  - `grep -rn "系统底座_指纹校验"` 全仓（排除 `工程缓存/`）仅 2 命中：`后端核心/后端核心.py:226` 与 `开发文档/项目说明.md:24`（文档声称）
  - `plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables` 只有 `MLXWhisper提供者_模型名/模型路径/PATH/PYTHONPATH/系统库网关凭证/系统底座_提供者环境根`
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rn "系统底座_指纹校验" --include='*.py' --include='*.json' --include='*.plist' --include='*.sh' --include='*.md' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist | grep -c "指纹校验" || echo "LaunchAgent 未设置 系统底座_指纹校验"
```
- **真实输出摘录**：`./后端核心/后端核心.py:226:        开关 = str(os.environ.get("系统底座_指纹校验", "")).strip().lower()`、`./开发文档/项目说明.md:24:...（制品指纹漂移即报错，开关 系统底座_指纹校验）`；LaunchAgent 环境变量无该项
- **已核实**

##### A-09【重要】依赖防火墙层表缺 `启动监督器/`、`客户端/`、`运维脚本/` → 三目录在依赖审计中被归为「其他」并免越层检查
- **影响**：`健康监督.py:36-43` 直接 import `支持库.适配层.系统探针` 与 `项目适配层.配置适配.*`（最内层核心邻近目录依赖最外层绑定层），审计**不报违规**（实测 582 文件 0 违规）。这三个目录是 AGENTS.md 声明的正式层，却不在 `层名称表`/`允许依赖表`，等于依赖治理对它们失效。
- **证据**：
  - `运行核心/依赖防火墙.py:33-43` `层名称表`（含 公共契约/支持库/模块库/前端核心/后端核心/运行核心/项目适配层/开发工具/其他；**无 启动监督器/客户端/运维脚本**）
  - `运行核心/依赖防火墙.py:106` `return 层名称表.get(顶层, "其他")`
  - `运行核心/依赖防火墙.py:281-282` `if 来源层 == "其他": continue  # 工具/门禁/验证 文件可依赖任意层`
  - `启动监督器/健康监督.py:36-43` 的两组跨层 import
  - 实测：`审计文件数 = 582 | 违规数 = 0`，且 8 个目标文件都在审计范围内
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
sed -n '33,43p;106p;281,282p' 运行核心/依赖防火墙.py
python3.14 -c '
import sys; sys.path.insert(0, ".")
from 运行核心.依赖防火墙 import 审计依赖
r = 审计依赖(); print("审计文件数 =", r.审计文件数, "| 违规数 =", len(r.违规列表))
'
```
- **真实输出摘录**：`审计文件数 = 582 | 违规数 = 0`（同时确认 `前端核心/3 + 后端核心/1 + 启动监督器/2 + 客户端/1 + 运维脚本/1` 个文件均被枚举）
- **已核实**

##### A-10【建议】`启动监督器/容量基线.py` 证据文件无界追加，无裁剪/TTL/条数上限
- **影响**：违反 AGENTS.md:164「所有线程、进程、队列、缓存、日志、临时文件、句柄和终态记录必须有界」。同级 `健康监督.py` 已实现保留策略（`:380-467` `_裁剪证据`），同一目录两套口径。
- **证据**：`启动监督器/容量基线.py:127-136`（`open(..., "a")` 追加，无裁剪）；`grep -n "裁剪\|保留条数\|上限" 启动监督器/容量基线.py` 只命中基线**字段名**（`:9-10,93-94`），无裁剪逻辑；对照 `启动监督器/健康监督.py:56`（保留条数 2000）、`:355`（写后裁剪）
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
sed -n '127,136p' 启动监督器/容量基线.py
grep -n "保留条数\|裁剪\|_裁剪证据" 启动监督器/容量基线.py || echo "容量基线 无裁剪逻辑"
grep -n "保留条数\|_裁剪证据" 启动监督器/健康监督.py
```
- **真实输出摘录**：`容量基线 无裁剪逻辑`（仅 `:130` 证据 id 行）；`健康监督.py:56: "健康监督证据保留策略": {"保留条数": 2000, "保留TTL秒": 0},`、`:355: self._裁剪证据(时间戳)`
- **已核实**

##### A-11【建议】`客户端/构建平台客户端.py::构建()` 失败路径残留 `平台客户端-临时` 制品目录
- **影响**：`构建()` 区间 `:511-592` 没有外层 `try/finally`（仅 `:563-566`、`:577-584` 两个局部 fd 释放）。`复制并重写`/`生成可运行制品壳`/`安装到环境` 任一步抛错，`工程缓存/制品仓库/平台客户端制品/平台客户端-临时/`（约 30MB）留在盘上；同目录还有 `平台客户端-<摘要16>` 半成品可能残留。
- **证据**：`客户端/构建平台客户端.py:511`（def 构建）…`:537-541`（建临时制品根）、`:542-554`（复制/摘要/改名）、`:589-592`（可选安装）之间无 try；`grep -n "try:" 客户端/构建平台客户端.py` 在 511-592 只有 563、577
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -n "try:\|finally:" 客户端/构建平台客户端.py | awk -F: '$1>=511 && $1<=592'
sed -n '537,556p' 客户端/构建平台客户端.py
```
- **真实输出摘录**：`563:    try:`、`566:    finally:`、`577:    try:`、`584:    finally:`（无外层 try/finally）
- **已核实**（静态证据充分；未实际触发失败构建——禁止重编制品，故"残留目录"未跑实测）

##### A-12【建议】`前端核心` 的渲染/窗口生命周期链在生产零调用、零测试：前端核心层当前无生产宿主
- **影响**：`渲染()`、`注册窗口`、`打开窗口`、`关闭窗口`、`触发事件`、`设置状态`、`状态快照` 在生产无调用者；`渲染提供者` 从不注入（见 A-03）。40007 网关与 `运行核心` 都不装载 `前端核心`。实测调用者只有：`测试中心/第一批维修/测试_前端核心唯一调用.py`（只用 `设置调用入口`/`调用后端能力`）与 `示例项目/前后端核心示例/运行示例.py:12,22-25`（只用 注册窗口/打开窗口/关闭窗口）。
- **证据**：
  - `前端核心/前端核心.py:92-125`（注册/打开/关闭/事件/状态）、`:139-146`（渲染）
  - `grep -rn "前端核心()" --include='*.py' .`（排除 `工程缓存/`）→ 3 处：`测试_前端核心唯一调用.py:22,27`、`示例项目/前后端核心示例/运行示例.py:22`
  - `grep -rn "\.渲染(" --include='*.py' .`（排除 `工程缓存/`）→ 3 处，其中 2 处是定义/内部转发，唯一外部调用是门禁 `运行发布门禁.py:1206` 且用的是**直连的 `浏览器交互提供者`**，不是 `前端核心.渲染`
  - `grep -rn "前端核心" 项目适配层 运行核心 MCP工具箱 --include='*.py'` → 全部是依赖防火墙规则表/目录清单字符串，无实例化
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rn "前端核心()" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
grep -rn "\.渲染(" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
grep -c "def test_" 测试中心/第一批维修/测试_前端核心唯一调用.py
```
- **真实输出摘录**：调用者仅 3 处（2 测试 + 1 示例）；`测试_前端核心唯一调用.py` 用例数 `2`
- **已核实**

##### A-13【建议】重复的第二实现：页面生成 + `/网关/调用` 前端脚本写了三份，`_安全脚本数据` 又复制两份
- **影响**：同一「页面只经 `/网关/调用` 调能力」的能力在三处各自实现（`前端核心/浏览器交互.py:223-240` 完整版、`支持库/前端/浏览器宿主/实现/浏览器宿主.py:28` 单行版、`开发工具/契约编译/契约编译器.py:73` 生成的 JS stub），安全转义函数在 `前端核心` 内被逐字符复制。任一处修 XSS/超时/取消语义时另两处会漏改。
- **证据**：
  - `前端核心/浏览器交互.py:22-27` 与 `前端核心/渲染提供者.py:13-17`：`_安全脚本数据` 两处逐字符相同
  - `支持库/前端/浏览器宿主/实现/浏览器宿主.py:28`（内联 HTML + `fetch(网关地址+'/网关/调用')`）vs `前端核心/浏览器交互.py:142-147`（`fetch(页面配置.网关地址 + "/网关/调用")`）
  - `开发工具/契约编译/契约编译器.py:73`（生成 `fetch(网关地址 + "/网关/调用")` 的 JS）
  - 发布门禁**同时**使用两者：`运行发布门禁.py:1201-1205`（`浏览器交互提供者`）与 `:1212-1216`（`启动网页服务`）→ 说明两条腿都在线上
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台
sed -n '22,27p' 前端核心/浏览器交互.py; sed -n '13,17p' 前端核心/渲染提供者.py
grep -n "网关/调用" 支持库/前端/浏览器宿主/实现/浏览器宿主.py 开发工具/契约编译/契约编译器.py 前端核心/浏览器交互.py
```
- **真实输出摘录**：两段 `_安全脚本数据` 输出逐字符一致；`网关/调用` 三处命中
- **已核实**

##### A-14【建议】死导入与无超时子进程
- **影响**：①`客户端/构建平台客户端.py:22` `import subprocess`、`:24` `import tempfile` 全文件未使用（噪声 + 误导读者以为有子进程/临时目录逻辑）；②`运维脚本/热接入.py:117-123` `subprocess.run([...pgrep...])` 无 `timeout`，`pgrep` 卡住或 `bash` 异常时该运维脚本无限挂起；③同文件 `:26` 硬编码 `~/Library/LaunchAgents/com.huashi.gateway-40007.plist`，仅 macOS 可用（无回退、无提示）。
- **证据**：
  - AST 未使用导入检查输出：`客户端/构建平台客户端.py: 未使用导入 -> {'annotations': 14, 'subprocess': 22, 'tempfile': 24}`（`annotations` 为 `from __future__` 误报，其余 8 个文件均无未使用导入）
  - `grep -n "subprocess" 客户端/构建平台客户端.py` → 仅 `22:import subprocess`
  - `运维脚本/热接入.py:117-123`
- **复现命令**：
```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 /tmp/未用导入检查.py
grep -n "subprocess" 客户端/构建平台客户端.py
sed -n '117,123p' 运维脚本/热接入.py
```
- **真实输出摘录**：见上；`sed` 输出 `结果 = subprocess.run([...], capture_output=True, text=True,)`（无 timeout 参数）
- **已核实**

### 8 文件被真实调用/依赖反查总表

| 文件 | import 它的生产文件 | import 它的测试文件 | 判定 |
|---|---|---|---|
| `前端核心/前端核心.py` | `开发工具/发布门禁/运行发布门禁.py:1527-1528`（仅取 `组件定义/窗口定义/页面定义/渲染请求` 数据类） | `测试中心/第一批维修/测试_前端核心唯一调用.py:12` | 活（弱）：核心逻辑（窗口生命周期/渲染）无生产调用 |
| `前端核心/浏览器交互.py` | `开发工具/发布门禁/运行发布门禁.py:1201`、`前端核心/渲染提供者.py:52` | 无 | 活：仅发布门禁直连 |
| `前端核心/渲染提供者.py` | 无 | 无 | **死代码** |
| `后端核心/后端核心.py` | `运行核心/启动运行核心网关.py:5,10`（40007 常驻） | 21 个文件 | 活：平台唯一执行宿主 |
| `启动监督器/健康监督.py` | 无 | `测试_健康监督.py`、`测试_健康配置.py`、`测试_提供者生命周期.py` | 活（仅测试）：**生产无接线** |
| `启动监督器/容量基线.py` | 无 | 无 | **死代码** |
| `客户端/构建平台客户端.py` | 无自动入口（人工脚本） | `测试_客户端构建安全.py:16`、`测试_平台客户端制品接入.py:24` | 活（手工/文档化）：无自动发布链调用 |
| `运维脚本/热接入.py` | 无 | 无 unittest（黑盒回归在 `开发工具/HTML验证/热接入回归.py`，需 40007 在线） | 活（人工运维）：目录未声明、不在门禁正式目录 |

### 命令清单（本次全部真实执行）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
/Library/Developer/CommandLineTools/usr/bin/git rev-parse HEAD && git branch --show-current && git status --porcelain
sed -n '160,219p' AGENTS.md; sed -n '170,193p' AGENTS.md
find 前端核心 后端核心 启动监督器 客户端 运维脚本 -type f | sort
wc -l 前端核心/*.py 后端核心/*.py 启动监督器/*.py 客户端/*.py 运维脚本/*.py
grep -rl "前端核心\|后端核心\|启动监督器\|客户端\|运维脚本" 测试中心 --include='*.py'
grep -rn "<关键词>" --include='*.py' . | grep -v '/__pycache__/' | grep -v '^\./工程缓存/'
grep -rn "from 后端核心.后端核心 import 后端核心" 测试中心 --include='*.py'   # 21 文件
grep -c "def test_" <各测试文件>
unset PYTHONPATH && python3.14 -m py_compile <8 个文件>          # 全部 OK
unset PYTHONPATH && python3.14 -c '<逐个 import 7 个模块>'        # 全部 IMPORT OK
unset PYTHONPATH && python3.14 /tmp/未用导入检查.py               # AST 未用导入
unset PYTHONPATH && python3.14 -c '<装配模板 A/B 复现>'            # A-01 复现
unset PYTHONPATH && python3.14 -c '<依赖防火墙 审计依赖()>'        # 582 文件 / 0 违规
sed -n '33,43p;106p;281,282p' 运行核心/依赖防火墙.py
sed -n '67,70p' 开发工具/发布门禁/运行发布门禁.py
sed -n '31,34p' 开发工具/项目编译/项目编译器.py
sed -n '306,310p' MCP工具箱/项目服务.py
grep -n "系统底座_指纹校验" <全仓多种后缀>
plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist        # 只读
rm -rf 运维脚本/__pycache__      # 回收本次 py_compile 自建的缓存目录（原无所谓，恢复冻结态）
```

### 跳过项（因冻结边界或成本未执行）

1. **未运行任何发布门禁 / 统一验证器**（明令禁止）。
2. **未运行黑盒回归** `开发工具/HTML验证/热接入回归.py`、HTML 黑盒验证器 —— 会在 `支持库/` 下写演练包、并需要 40007 在线。
3. **未重建平台客户端制品**（`客户端/构建平台客户端.py` 只读审计，未执行 `构建()`），故 A-11 的"失败残留目录"未做运行时复现。
4. **未运行 `测试中心/启动监督器/*` 与 `测试中心/第一批维修/*` 的 unittest**（未在允许清单内；本轮只做静态 + 只读运行）。
5. **未重启/触碰 40007 网关与 LaunchAgent**（只 `plutil -p` 只读）。
6. **未把 `A-01` 的污染方向扩到真实系统根验证**（第二实例若用真实根会在 `工程缓存/` 写目录，越界）。

### 未确认项

1. **A-11 的运行时残留**：只确认了 `构建()` 无外层 `try/finally`（静态），未真实触发一次失败构建来观察 `平台客户端-临时` 残留（禁止重编制品）。
2. **A-01 是否已在测试套件里被触发**：已核对 `测试_后端核心唯一调用/关闭`（用临时根但**不调** `装配/启动`）、`测试_热接入子包归属`（临时根但只调 `_校验热接入包`）、`测试_资源句柄网关/渐进能力目录/项目适配层`（均用真实系统根）→ 当前用例序列**不会**触发；但"全量 unittest discover 的类级 `setUpClass` 顺序是否可能先跑临时根装配"未做端到端实证（需跑测试，未在允许清单内）→ 标注**未确认**。
3. **`运维脚本/热接入.py` 的实际可用性**：未连 40007 实调（会真实触发热接入、有副作用），故只想静态证据。
4. **`健康监督.py` 是否被非 Python 入口（`.mcp.json` / opencode.json / 其它 shell 脚本）引用**：本次只扫了 `*.py/*.json/*.md/*.sh/*.yaml` 关键词，未逐文件审查 `.mcp.json`、`opencode.json` 全文 → 未确认（但这两者按内容应为 MCP/agent 配置，与健康监督无关）。
5. **`容量基线.py` 是否被「已删除但仍在 git 历史/分支」的入口引用**：只查了工作区，未查 git 历史 → 未确认。

---

## 第 2·3 节 · 项目适配层 + 全仓「业务项目引用」铁律专项

> 本节由专项子审计产出，原文完整保留；数据与证据格式同上（path:行号 + 真实命令 + 真实输出摘录）。

### 子审计 B：项目适配层真实质量 + 全仓「业务项目引用」铁律专项

- 审计对象：系统工程平台（底座），项目根 `~/Documents/Agent/PHP/系统工程平台`
- 冻结基线：HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`，分支 `主干`（本文所有取证均在该基线）
- 只读声明：本次未修改仓库任何文件；唯一写入为 `/tmp/子审计_B_项目适配层与业务引用.md`
- 环境：macOS；python 用 `python3.14`（跑前 `unset PYTHONPATH`）；git 用 `/Library/Developer/CommandLineTools/usr/bin/git`；grep 优先工具 `search_files`，大批量用 `grep -rn` + `--exclude-dir`

### 一句话结论

**项目适配层本身很干净**：27 个文件里 0 处 import 支持库/模块库/技能库/提供者/实现源码，配置合并是真实现（有真实调用方 + 实测生效），密钥不写日志不写盘，依赖锁定/传递闭包/锁定校验都不是死代码；**但全仓铁律被 模块库/直播逐字稿 这一个正式包打破**——它把业务项目绝对路径与业务品牌术语写进了底座源码、契约、验收数据和 LaunchAgent，且现有门禁完全不覆盖 模块库（只查 2 个目录）。

---

### 分级计数

| 级别 | 数量 |
|---|---|
| 阻断 | 1 |
| 重要 | 5 |
| 建议 | 12 |
| 业务引用违规（代码/配置 `.py/.json/.plist`） | 21 |

---

### 第一部分：项目适配层真实质量

##### 0. 审计面清点（已核实）

```
$ find 项目适配层 -type f -not -path "*__pycache__*" | wc -l
27
```

9 个包 + 1 个配置目录：项目声明、配置适配、依赖锁定、支持库绑定、模块绑定、能力适配、路径适配、运行入口、项目初始化、项目配置（3 个 json）。

##### 1【建议】越界判定：0 处 import 支持库/模块库/提供者/实现源码

**铁律原文**：项目适配层只绑定模块版本、配置、权限、路径、密钥引用和项目中文别名；不得调用支持库、提供者或实现源码。

**复现命令**

```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rnE "^\s*(from|import)\s+(支持库|模块库|技能库)" 项目适配层 --include="*.py"
grep -rnE "\.实现\.|提供者" 项目适配层 --include="*.py" | grep -E "^\S+:[0-9]+:\s*(from|import)"
```

**真实输出**

```
（第一条：0 命中，退出码 1）
第二条命中：
项目适配层/支持库绑定/支持库绑定.py:15:from 运行核心.加载器.提供者选择.选择器 import 选择提供者
项目适配层/依赖锁定/传递闭包.py:25:from 运行核心.加载器.提供者选择.选择器 import 选择全部提供者
项目适配层/依赖锁定/锁定校验.py:18:from 运行核心.加载器.提供者选择.选择器 import 选择全部提供者
项目适配层/依赖锁定/依赖锁定.py:17:from 运行核心.加载器.提供者选择.选择器 import 选择全部提供者
```

**逐包判定**

| 包 | 跨层 import（全部列出） | 是否越界 |
|---|---|---|
| 项目声明 | 仅标准库（json/dataclasses/pathlib） | 否 |
| 配置适配（4 文件） | 仅标准库 + 包内 | 否 |
| 依赖锁定 | `公共契约.包声明`、`运行核心.加载器.包发现.发现器`、`运行核心.加载器.依赖解析.解析器`、`运行核心.加载器.提供者选择.选择器`、`运行核心.运行环境管理器`（函数内） | 否（见判定说明） |
| 支持库绑定 | `公共契约.包声明`、`公共契约.版本规则.比较`、`运行核心.加载器.包发现.发现器`、`运行核心.加载器.提供者选择.选择器` | 否（见判定说明） |
| 模块绑定 | `公共契约.版本规则.比较`、`运行核心.加载器.包发现.发现器` | 否 |
| 能力适配 | `运行核心.能力调用.HTTP连接器` | 否 |
| 路径适配 | 仅标准库 | 否 |
| 运行入口 | `公共契约.基础类型.结果类型`、`公共契约.能力契约.契约` | 否 |
| 项目初始化 | 包内 | 否 |

**判定说明（如实标注边界，不夸大也不放过）**

- 0 命中 `支持库.` / `模块库.` / `技能库.` 前缀导入，0 命中 `.实现.` 深入实现目录——铁律的硬项 100% 干净。
- 全部跨层依赖落在 `公共契约` + `运行核心` 两个顶层，这两层被 `运行核心/依赖防火墙.py:52` 显式白名单化：
  ```
  运行核心/依赖防火墙.py:52:    "项目适配层": {"公共契约", "运行核心"},
  ```
  即按平台自己的防火墙规则，适配层依赖 运行核心 是**合规**的。
- 唯一需人工判定项：4 处 import 的模块名含「提供者」（`运行核心.加载器.提供者选择.选择器`）。核实其物理位置在 `运行核心/加载器/提供者选择/`，是核心的**选择器**，不是任何提供者实现，也不读取提供者源码（只按包声明挑 owner）。**按铁律本意（不得调用提供者）判为不违规**；但铁律字面「不得调用…提供者」与模块命名存在字面冲突，判为 **建议**：建议在 AGENTS.md 铁律里把「提供者」限定为「支持库/适配层 下的提供者实现」，避免后续审计歧义。

**已核实 / 未确认**：已核实。

##### 2【已核实·非空壳】配置合并是真实现，五层顺序真实生效

**有没有真的做配置合并**——有，且**有真实调用方**（不是只被测试叫）。

**真实调用方（唯一生产消费者）**

```
启动监督器/健康监督.py:37:from 项目适配层.配置适配.配置读取 import 配置读取结果, 读取JSON配置
启动监督器/健康监督.py:39:    段级深合并配置,
启动监督器/健康监督.py:40:    合并配置,
启动监督器/健康监督.py:41:    解析环境配置,
启动监督器/健康监督.py:43:from 项目适配层.配置适配.配置校验 import 校验配置
启动监督器/健康监督.py:520:        else 系统根 / "项目适配层" / "项目配置"
```

`启动监督器/健康监督.py:528` 实际调用 `合并配置(支持库默认=…, 项目默认=…, 环境配置=…, 显式覆盖=…)`，`:544` 调用 `段级深合并配置(层级表, "健康监督证据保留策略")`，`:562` 调用 `校验配置`，配置目录默认取 `项目适配层/项目配置`。

**实测输出**（真实执行，未编造）

```
$ cd 项目根 && unset PYTHONPATH && python3.14 - <<'PY' ... PY
1) 五层结果 a = 5 | 来源 = 运行入口显式覆盖 | 成功 = True
2) 环境变量进入合并结果？ False | 结果键: ['b']
3) 段级深合并: {'x': 1, 'y': 9} {'x': '支持库默认配置', 'y': '运行入口显式覆盖'}
4) 敏感告警文本: 疑似明文密钥: api_token 的值疑似真实密钥（应改为环境变量名或外部密钥引用）
   敏感值是否出现在告警文本里？ False
5) 开发环境配置键: ['外部服务地址', '数据库连接标识', '环境名称', '调试开关', '默认超时秒']
```

**结论**：`支持库默认配置 → 项目默认配置 → 当前环境配置 → 外部提供者配置 → 运行入口显式覆盖` 五层顺序**真实生效**（a=5，来源=运行入口显式覆盖）；段级深合并真实生效。

**但审计口径里的两级缺失（如实标注，不算违规，算缺口）**：
- **环境变量层**：`合并配置` 不读环境变量（实测 `os.environ` 设键后不进合并结果）。环境变量只在 `项目适配层/能力适配/能力适配.py:71-78` 被单独读取（网关地址/端口/超时）。
- **请求覆盖层**：`合并配置` 的 `显式覆盖` 是「运行入口显式覆盖」，没有「请求级覆盖」独立层级。文档 `配置读取.py:7-9` 自己声明的顺序也是「支持库默认→项目默认→环境配置→提供者配置→运行入口显式覆盖」，**文档与实现一致**，只是与本次审计口径的「环境变量/启动参数/请求覆盖」三级不对应。

**已核实 / 未确认**：已核实。

##### 3【重要】依赖锁定/传递闭包/锁定校验 —— **不是死代码**，但发现 1 处真实断裂（假通过）

**不是死代码，已实测**

```
$ unset PYTHONPATH && python3.14 -c "…校验传递闭包(示例项目/适配层示例, .)…"
校验传递闭包 → 成功: True | 校验项数: 13 | 包数量: 4 | 问题: []
校验锁定文件 → 成功: True | 校验项数: 30 | 漂移: []
```

调用链（真实）：

| 函数 | 定义 | 真实调用方 |
|---|---|---|
| `生成依赖锁定` | `项目适配层/依赖锁定/依赖锁定.py:81` | `示例项目/适配层示例/运行入口/运行.py:40`、`开发工具/统一验证/统一验证器.py:301`、测试中心 2 处 |
| `校验锁定文件` | `项目适配层/依赖锁定/锁定校验.py:33` | **`开发工具/发布门禁/运行发布门禁.py:825`（发布门禁，硬门禁）**、`:844` |
| `校验传递闭包` | `项目适配层/依赖锁定/传递闭包.py:119` | 仅 `测试中心/项目适配/测试_项目锁传递闭包.py:100`（**未接入任何门禁**，见 建议-10） |
| `计算完整性摘要` | `项目适配层/依赖锁定/依赖锁定.py:37` | `锁定校验.py:127`（内部复用） |

**问题 B1-1【重要】锁定校验 与唯一破坏验证调用方接口不匹配 → 「依赖锁篡改」破坏场景恒真假通过**

- 影响：`开发工具/复用审计/反向破坏` 的 13 个破坏场景中，「依赖锁篡改」这一项**永远不可能真正验证失败**——它一进函数就 ImportError，被外层 `except Exception` 兜成 `验证结果=True`，即"验证器失败"被记成通过。这是反向破坏门禁的**恒真漏洞**，等于该场景形同不存在。
- 证据（path:行号）

```
开发工具/复用审计/反向破坏/破坏验证.py:112:        from 项目适配层.依赖锁定.锁定校验 import 校验锁定
开发工具/复用审计/反向破坏/破坏验证.py:114:        验证失败 = not 校验锁定(锁定数据, 目录).成功
项目适配层/依赖锁定/锁定校验.py:33:def 校验锁定文件(项目根目录: Path, 系统根目录: Path) -> 锁定校验结果:
开发工具/复用审计/反向破坏/破坏验证.py:96:        for 名称, 函数 in 场景函数表:
开发工具/复用审计/反向破坏/破坏验证.py:97:            try:
开发工具/复用审计/反向破坏/破坏验证.py:98:                证据 = 函数()
开发工具/复用审计/反向破坏/破坏验证.py:99:            except Exception as 错误:
开发工具/复用审计/反向破坏/破坏验证.py:100:                证据 = 破坏证据(场景名=名称, 破坏方式="执行异常", 验证结果=True, 详情=str(错误))
```

  调用方还传了 `(锁定数据: dict, 目录)`，而被调函数的签名是 `(项目根目录: Path, 系统根目录: Path)`——**名字与签名双重不符**，两处都错。
- 复现命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 -c "from 项目适配层.依赖锁定.锁定校验 import 校验锁定"
```

- 真实输出摘录

```
导入 校验锁定: 失败 -> cannot import name '校验锁定' from '项目适配层.依赖锁定.锁定校验' (~/Documents/Agent/PHP/系统工程平台/项目适配层/依赖锁定/锁定校验.py)
模块导出: ['Path', 'annotations', 'dataclass', 'field', 'json', '加载声明文件', '发现全部', '契约版本常量', '校验锁定文件', '能力版本常量', '解析依赖', '计算完整性摘要', '选择全部提供者', '锁定校验结果', '锁定版本常量']
```

- 归责：断链是**消费方（复用审计）单方面用错名字**，项目适配层自身接口自洽（`__init__.py` 只导出 `生成依赖锁定/锁定结果/计算完整性摘要`，`校验锁定文件` 由 发布门禁 正确调用）。**但修复点可两边任一**，故同时报出。
- **已核实 / 未确认**：已核实（静态 + 真实 import 输出）。

##### 4【已核实·无泄露】密钥引用不写日志、不写盘

**复现命令**

```bash
grep -rn "print(\|logging\|日志\|json.dump\|write_text\|environ\|密钥\|密码\|token\|secret" 项目适配层 --include="*.py"
grep -rn "print(\|logging\|写入日志\|log(" 项目适配层 --include="*.py"
```

**真实输出（第 2 条 0 命中，退出码 1；第 1 条全量命中如下）**

```
项目适配层/依赖锁定/依赖锁定.py:153:    临时路径.write_text(json.dumps(锁定数据, ensure_ascii=False, indent=2), encoding="utf-8")
项目适配层/能力适配/能力适配.py:71-77:  os.environ.get("系统工程平台网关地址|网关端口|网关超时秒", …)
项目适配层/配置适配/配置校验.py:14:敏感关键词表 = ("密码","口令","令牌","私钥","密钥","token","secret","password","private_key")
项目适配层/配置适配/配置校验.py:84:  消息 = f"疑似明文密钥: {名称} 的值疑似真实密钥（应改为环境变量名或外部密钥引用）"
项目适配层/项目初始化/项目初始化.py:49,55:  依赖声明路径.write_text / 锁定路径.write_text（仅 项目id，无敏感值）
项目适配层/项目声明/项目声明.py:68:  临时路径.write_text(json.dumps(声明.转字典()…))（仅声明字段）
```

**判定（逐条）**：
- 适配层内 **0 条 `print` / `logging` / 日志写入**。
- 3 处 `write_text`：分别写 `依赖锁定.json`（包id/版本/摘要）、`依赖声明.json` + `依赖锁定.json`（仅 `项目id`）、`项目声明.json`（声明字段）——**均无敏感值**。
- `配置校验.py:84/90` 只把**配置名**拼进告警，**不拼值**（实测确认，见第 2 节输出第 4 行：告警文本里有 `api_token`，无 `sk-…` 明文）。
- `能力适配.py:71-78` 只**读**环境变量名，不打印、不落盘。

**已核实 / 未确认**：已核实。

##### 5【建议】配套文件要求：适配层被正式包索引豁免（已核实），但 AGENTS.md 未明文写这条豁免

**AGENTS.md 第204-218行**（"每个支持库必备"）要求：`包声明.json` / `能力契约/` / `实现/` / `说明/` / `验证场景引用.json` / `完整性摘要.json` / `__init__.py`。

**实测：项目适配层 0 个 `包声明.json`**

```
$ find 项目适配层 -name "包声明.json"
（空）
$ for d in 公共契约 平台控制面 启动监督器 运行核心 前端核心 后端核心 支持库 模块库 技能库 项目适配层 开发工具 测试中心 示例项目 客户端 MCP工具箱; do echo "$d: $(find $d -name 包声明.json | wc -l)"; done
公共契约: 0
平台控制面: 0
启动监督器: 0
运行核心: 0
前端核心: 0
后端核心: 0
支持库: 97
模块库: 14
技能库: 1
项目适配层: 0
开发工具: 0
测试中心: 0
示例项目: 0
客户端: 0
MCP工具箱: 0
```

**跑正式包索引确认是否收录项目适配层**（读源码 + 真实构建索引，未跑发布门禁、未重编制品）

`开发工具/项目编译/正式包索引.py:97-99`：

```python
def 构建索引(系统根: Path) -> dict[str, Any]:
    ...
    支持库, 支持库排除 = _扫描包(系统根, "支持库")
    技能库, 技能库排除 = _扫描包(系统根, "技能库")
    模块库, 模块库排除 = _扫描包(系统根, "模块库")
```

`_扫描包` 以 `根.rglob("包声明.json")` 为标记（`正式包索引.py:67`），**三个根只扫 支持库/技能库/模块库**。

**真实执行输出**

```
$ unset PYTHONPATH && python3.14 -c "…构建索引(Path('.'))…"
模块库.直播逐字稿 在正式索引: True
项目适配层 是否被索引收录: []
```

**判定（已核实）**：
- 项目适配层**不属于正式包索引**——它和 `公共契约/运行核心/启动监督器/前端核心/后端核心/开发工具/测试中心` 一样，是平台自身代码层，不按"支持库六件套"要求。
- AGENTS.md 第204-218行标题是"**每个支持库**必备"，AGENTS.md 第216-218行的豁免条款只针对 `支持库/适配层/` 下的内部适配器，**没有一句写"项目适配层豁免"**。
- 故：**不是项目适配层违规，是规范文本有空白** → 判 **建议**：建议在 AGENTS.md 明确一句「`项目适配层/` 与 核心各层同口径，不要求 包声明.json/能力契约/完整性摘要.json/验证场景引用.json，`扫描正式包` 不收录」，避免后续按"缺件"误报。

**已核实 / 未确认**：已核实。

##### 6【建议】项目配置/*.json 内容与业务引用判定

**全文（3 个文件共 27 行）**

```jsonc
// 项目适配层/项目配置/默认配置.json
{ "环境名称":"默认", "资源目录":"项目资源", "默认编码":"utf-8", "默认超时秒":30,
  "默认重试次数":1, "外部服务地址":"", "数据库连接标识":"", "进程启动参数":[],
  "缓存策略":"无", "资源限制":{"最大连接数":10,"最大临时文件数":20} }

// 项目适配层/项目配置/开发配置.json
{ "环境名称":"开发", "外部服务地址":"http://127.0.0.1:8000",
  "数据库连接标识":"本地开发库", "默认超时秒":10, "调试开关":true }

// 项目适配层/项目配置/发布配置.json
{ "环境名称":"发布", "外部服务地址":"https://服务.example.com",
  "数据库连接标识":"生产库（经密钥引用）", "默认超时秒":5, "默认重试次数":3, "调试开关":false }
```

**业务项目路径 / 数据库端口引用 grep**

```bash
grep -rn -E "华世王镞|huashi|V3|5432|3306|6379|/Users/|业务" 项目适配层/项目配置/
### 退出码 1，0 命中
```

**判定（已核实）**：
- **0 处引用业务项目路径**，**0 处引用业务数据库端口**（`数据库连接标识` 是抽象标识 `本地开发库` / `生产库（经密钥引用）`，**不写端口、不写库名、不写连接串**——符合铁律的"密钥引用"定位）。
- 唯一可挑的点：`开发配置.json:3` 的 `http://127.0.0.1:8000` 中 **8000 不在白名单端口**（40007 / 8766 / 8866 / 45080-45180 / 4780）。但它是文档性示例值（`example.com` 同款风格），全文 0 处业务指向 → **建议**：把示例端口改成白名单内端口或 `…:8000` 加注释标"示例"。

**已核实 / 未确认**：已核实。

##### 7【建议】适配层内的死代码清单（实测 定义=1 引用=0）

分子级核查（遍历 项目适配层 全部 `def`/`class`，统计全仓（排 `工程缓存`、`__pycache__`）引用文件数）：

| # | 死代码 | 证据 | 建议 |
|---|---|---|---|
| B1-2【重要】 | `项目适配层/配置适配/配置适配.py` **整文件 35 行**（`读取配置` / `配置结果`）全仓 0 引用，**连自家 `配置适配/__init__.py` 都不导出它** | `配置适配/配置适配.py:23` `配置适配/配置适配.py:15`；`配置适配/__init__.py` 只导出 配置读取/配置合并/配置校验 三包 | 删文件或补导出并给出调用方；现状是与 `配置读取.读取JSON配置` 职责重叠的第二套读取器 |
| B1-3 | `项目适配层/路径适配/` **整包 56 行**（`项目路径`）全仓 0 引用 | `路径适配/路径适配.py:16`；`grep -rn "项目路径(" .` → 0 命中（唯一的"命中"是 `项目初始化.py:26` 的同名形参，非该类） | 删包或接入 项目初始化/示例项目 |
| B1-4 | `能力适配.获取HTTP连接器` 无真实调用方（仅 `能力适配/__init__.py:1` 导出） | `能力适配/能力适配.py:68`；示例项目**绕过**它自建连接器：`示例项目/适配层示例/运行入口/运行.py:18,66-67` 直接 `HTTP连接器(...)` | 让示例/统一验证走 获取HTTP连接器，否则"网关地址由适配层配置"这条设计是空转 |
| B1-5 | `配置读取.读取配置目录` 无真实调用方（仅 `配置适配/__init__.py:3` 导出） | `配置读取/配置读取.py:54` | 同上 |
| B1-6 | `依赖锁定.依赖锁定.py:14` 与 `锁定校验.锁定校验.py:15` 导入的 `加载声明文件` **在本文件从未使用** | AST 复核输出：`项目适配层/依赖锁定/依赖锁定.py:14: 加载声明文件`、`项目适配层/依赖锁定/锁定校验.py:15: 加载声明文件` | 清理未使用导入 |
| B1-10 | `依赖锁定.传递闭包.校验传递闭包`（320 行实现）**未接入任何门禁**，只有 1 个测试文件调用 | `传递闭包/传递闭包.py:119` → 唯一外部调用 `测试中心/项目适配/测试_项目锁传递闭包.py:100`；`发布门禁/运行发布门禁.py:822` 只调 `锁定校验.校验锁定文件`，**没调 校验传递闭包** | 把 6 项闭包校验接进发布门禁；否则"传递依赖闭包硬门禁"（该文件自己第 1 行写的标题）实际不是门禁 |

> 注：B1-1（`校验锁定` 名字断裂）与 B1-10（传递闭包未接门禁）都属"依赖锁定三件套"的真实缺口，二者叠加的含义是——**项目锁侧的强制力只剩 发布门禁 里的一条 `校验锁定文件`**。

##### 第一部分小结（已核实项）

- 越界导入：**0 命中**（支持库/模块库/技能库/实现目录）。
- 配置合并：**真实现 + 真调用方 + 实测生效**，非空壳。
- 依赖锁定/传递闭包/锁定校验：**都不是死代码**（实测通过，且有发布门禁/示例项目/统一验证/测试四类调用方）。
- 密钥：**0 日志、0 敏感写盘**，告警文本只带配置名不带值。
- 配套文件：**项目适配层被正式包索引豁免**（已跑索引确认），规范文本有空白。
- 项目配置：**0 业务路径、0 数据库端口** 引用。

---

### 第二部分：全仓「业务项目引用」铁律专项 grep

**统一排除集**：`--exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=.codegraph --exclude-dir=.pytest_cache --exclude-dir=.ruff_cache`

##### V1【阻断】模块库/直播逐字稿（正式包，已进正式索引）内嵌业务项目路径与业务品牌术语

**这是本次审计最重的一条。** 该包经 `构建索引` 确认是**正式包**（实测输出：`模块库.直播逐字稿 在正式索引: True`），却把华世王镞 V3 业务的**绝对路径、品牌词、行业术语、门店政策**全部写死进底座源码/契约/验收数据/LaunchAgent。

**复现命令**

```bash
cd ~/Documents/Agent/PHP/系统工程平台
grep -rn -F -e "华世王镞" -e "祛斑" -e "huashi" -e "招商" --include="*.py" --include="*.json" --include="*.plist" --include="*.md" \
  --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=开发文档 . \
  | grep "^./模块库/直播逐字稿"
```

**真实命中（path:行号）—— 13 条**

```
模块库/直播逐字稿/工具/批量精校队列.py:36:老项目根 = Path("~/Documents/Hermes工作区/录音分析/免费祛斑免费敷面膜直播精校项目")
模块库/直播逐字稿/工具/批量精校队列.py:37:权威根 = Path("~/Documents/Hermes工作区/录音分析/直播逐字稿")
模块库/直播逐字稿/工具/批量精校队列.py:43:    "附加术语": "华世王镞、免费祛斑、免费去斑、合作店、免费敷面膜、抖音、河北邢台、任泽区",
模块库/直播逐字稿/工具/批量精校队列.py:46:             "「忌嘴」不能写成「记嘴」、「尴尬期」不能写成「尴尬气」；品牌词华世王镞必须原样保留"),
模块库/直播逐字稿/工具/com.huashi.zizhugao-queue.plist:6:    <string>com.huashi.zizhugao-queue</string>
模块库/直播逐字稿/工具/com.huashi.zizhugao-queue.plist:10:        <string>~/Documents/Hermes工作区/录音分析/直播逐字稿/工具/批量精校队列.py</string>
模块库/直播逐字稿/工具/安装或卸载队列常驻.py:19:标签 = "com.huashi.zizhugao-queue"
模块库/直播逐字稿/工具/使用说明.md:12:| `com.huashi.zizhugao-queue.plist` | LaunchAgent 定义（KeepAlive + RunAtLoad） |
模块库/直播逐字稿/工具/使用说明.md:50:老项目根 = Path("~/Documents/Hermes工作区/录音分析/免费祛斑免费敷面膜直播精校项目")
模块库/直播逐字稿/能力契约/参数契约.json:370:     "附加术语": "华世王镞、合作店、抖店",
模块库/直播逐字稿/说明/使用说明.md:63:- 调用示例：直播逐字稿.全自动精校(… 附加术语=华世王镞、合作店、抖店, …)
模块库/直播逐字稿/模式数据/模式注册表.json:19:      "适用": "招商会、带货方案会、合作方案沟通",
模块库/直播逐字稿/完整性摘要.json:87:      "路径": "工具/com.huashi.zizhugao-queue.plist",
```

**外加同包内的业务环境端点（2 条，端口类，见 V7）**

```
模块库/直播逐字稿/工具/批量精校队列.py:66:  "url": "http://127.0.0.1:8328/hermes-chat/v1", "api_key": 密钥,
模块库/直播逐字稿/工具/批量精校队列.py:98:  "url": "http://127.0.0.1:8328/hermes-chat/v1", "api_key": 密钥,
模块库/直播逐字稿/验证场景/正向HTTP场景.json:138:  "url": "http://127.0.0.1:11434/v1",
```

**外加硬编码默认网关凭证（1 条，见 V9）**

```
模块库/直播逐字稿/工具/批量精校队列.py:33:凭证 = os.environ.get("系统库网关凭证", "html-blackbox-verifier")
模块库/直播逐字稿/工具/批量精校队列.py:34:密钥 = os.environ.get("HERMES_CUSTOM_CUSTOM_API_KEY", "").strip()
```

**分类判定：代码/配置引用（违规）** —— 14 条属 .py/.json/.plist（真正的功能耦合），3 条属包内 .md（`工具/使用说明.md:12,50`、`说明/使用说明.md:63`，属包的一部分，同样违规）；`完整性摘要.json:87` 是文件清单哈希，随源文件同步，属**连带**违规。

**影响**
- 违反 AGENTS.md:151-153「严禁导入、引用、读取任何业务项目的代码、数据库、配置、测试、运行数据」——这里直接写死了业务项目（原**祛斑直播精校项目**）的两个绝对路径，其中 `老项目根` 是**读取路径**，构成真实读取业务项目数据。
- 违反 README.md:3 自己对 模块库 的定位（"组合支持库完成功能的样板模块"）——行业写死内容（祛斑/面膜/合作店/邢台任泽区）与 `开发文档/未完成事项.md:61` 声称的"底座已彻底去掉行业写死内容（9 个模式提示词零行业词，已断言）"**直接矛盾**：断言只覆盖了 `模式注册表` 的 9 个模式提示词，没覆盖 `工具/` 脚本与 `能力契约` 示例。
- 该包被 `客户端/构建平台客户端.py` 打进平台客户端制品 → 业务路径与品牌词会随**制品**分发（`工程缓存/制品仓库/平台客户端制品/平台客户端-1246f3724c0f18c4/平台客户端/模块库/直播逐字稿/…` 已见同一批内容，属制品，未计入源码违规数）。

**已核实 / 未确认**：已核实（真实 grep 输出 + `构建索引` 确认正式包身份 + 文件现场 sed 复核）。

##### V2【重要】不存在全仓业务引用门禁：唯一相关检查只覆盖 2 个目录

**复现命令**

```bash
grep -rn "华世王镞\|业务项目\|禁词\|业务引用\|行业词\|huashi" \
  开发工具/发布门禁/运行发布门禁.py 开发工具/统一验证/统一验证器.py 运行核心/依赖防火墙.py
```

**真实输出（全部命中）**

```
开发工具/发布门禁/运行发布门禁.py:466:    plist = Path.home() / "Library/LaunchAgents/com.huashi.gateway-40007.plist"
开发工具/统一验证/统一验证器.py:428:    # 8. 项目可脱离外部业务项目独立运行
开发工具/统一验证/统一验证器.py:430:        ["grep", "-rn", "华世王镞\\|MCP工具箱/\\|功能模块/\\|后端服务/\\|应用软件/", "示例项目/适配层示例", "项目适配层"],
```

`统一验证器.py:428` 那一段的门禁原文（`统一验证器.py:429-435`）：命中即 `报告.记录失败("外部项目依赖", …)`——**它只扫 `示例项目/适配层示例` 和 `项目适配层` 两个目录**。

**分类判定：文档/门禁自身引用（不构成违规，但构成"门禁缺失"）**

**影响**：V1 的 14 条违规**不会**被任何门禁拦住——`模块库/`、`支持库/`、`技能库/`、`前端核心/` 全在扫描范围之外。`依赖防火墙.py` 只管 import 拓扑，不查字符串。即铁律"严禁引用业务项目"目前**没有全仓强制力**。

**已核实 / 未确认**：已核实。

##### V3【重要】支持库以业务项目名（V3）命名环境变量与配置契约

**复现命令**

```bash
grep -rn -F "V3个人蒸馏地址" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=开发文档 .
```

**真实命中（path:行号）—— 6 条**

```
支持库/后端/个人蒸馏支持库/实现/个人蒸馏.py:26:    return os.environ.get("V3个人蒸馏地址", "http://127.0.0.1:8898").rstrip("/")
支持库/后端/个人蒸馏支持库/配置契约/配置契约.json:1:{"配置项": [{"名称": "V3个人蒸馏地址", "来源": "环境变量", "默认": "http://127.0.0.1:8898"}]}
支持库/后端/个人蒸馏支持库/包声明.json:6: …外部服务地址可用环境变量 V3个人蒸馏地址 覆盖（默认 http://127.0.0.1:8898）。
支持库/后端/个人蒸馏支持库/能力定义.json:4: …外部服务地址可用环境变量 V3个人蒸馏地址 覆盖（默认 http://127.0.0.1:8898）。
支持库/后端/个人蒸馏支持库/说明/使用说明.md:5: …外部服务地址可用环境变量 V3个人蒸馏地址 覆盖（默认 http://127.0.0.1:8898）。
支持库/后端/个人蒸馏支持库/说明/设计说明.md:7: …可用环境变量 V3个人蒸馏地址 覆盖。
```

**分类判定：代码/配置引用（违规）** —— 环境变量名 `V3个人蒸馏地址` 把业务项目名写进了底座支持库的**对外配置契约**（`配置契约.json` 是包的公开接口），属"引用业务项目"的命名耦合。虽不读业务代码/数据库，但铁律「严禁导入、引用…任何业务项目」的字面已被触及；且违反"通用能力不得含具体业务项目名"的口径（对照 `开发文档/通用能力反馈接口与平台自治治理.md:254`：「平台源码不包含任何具体业务项目名称或项目专属分支」）。

**已核实 / 未确认**：已核实。

##### V4【重要】底座 LaunchAgent 标签带业务品牌 "huashi"

**复现命令**

```bash
grep -rn -F "com.huashi" --include="*.py" --include="*.json" --include="*.plist" \
  --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=开发文档 .
```

**真实命中（path:行号）**

```
开发工具/发布门禁/运行发布门禁.py:466:    plist = Path.home() / "Library/LaunchAgents/com.huashi.gateway-40007.plist"
运维脚本/热接入.py:26:凭证来源 = Path.home() / "Library" / "LaunchAgents" / "com.huashi.gateway-40007.plist"
运维脚本/热接入.py:8:为什么用它：40007 是全机唯一执行网关，被 V3 后端、Agent 工具面等依赖。
模块库/直播逐字稿/工具/安装或卸载队列常驻.py:19:标签 = "com.huashi.zizhugao-queue"
模块库/直播逐字稿/工具/com.huashi.zizhugao-queue.plist:6:    <string>com.huashi.zizhugao-queue</string>
```

**分类判定：代码/配置引用（违规）** —— `huashi` = 华世（华世王镞）品牌缩写，底座自己的 40007 网关 LaunchAgent 与 发布门禁 都靠它定位，属**运行耦合**（不是纯文档）。`运维脚本/热接入.py:8` 的注释 "被 V3 后端…依赖" 属文档叙述。

**已核实 / 未确认**：已核实。

##### V5【建议】业务品牌词出现在验收场景/测试数据（功能无害，口径不干净）

**真实命中（path:行号）**

```
支持库/后端/数据操作支持库/数据集合/验证场景.json:417:  "文本": "华世王镞护肤"
支持库/后端/数据操作支持库/数据集合/验证场景.json:435:  "文本": " 华世王镞 "
支持库/后端/数据操作支持库/数据集合/验证场景.json:443:  "结果文本": "华世王镞"
支持库/后端/数据操作支持库/数据集合/验证场景.json:464:  "结果文本": "华世王镞华世王镞"
支持库/后端/编码转换支持库/验证场景/正向HTTP场景.json:12:  "文本": "华世王镞G8"
支持库/后端/编码转换支持库/验证场景/正向HTTP场景.json:49:  "文本": "华世王镞G8"
支持库/后端/文件系统支持库/内容检索/验证场景.json:32:  "内容": "华世王镞内容检索验证关键字"
支持库/适配层/YAML提供者/验证场景引用.json:13:  "文本": "品牌: 华世王镞\n产品: 补水面膜\n库存: 12"
支持库/适配层/YAML提供者/验证场景引用.json:26:  "品牌": "华世王镞"
支持库/适配层/YAML提供者/验证场景引用.json:48:  "包含": "品牌: 华世王镞"
技能库/后端/技能库/验证场景.json:82:  "参数": {"文本": "华世王镞 护肤"}
技能库/后端/技能库/验证夹具/技能工作区/示范技能/验证声明.json:6:  "参数": {"文本": "华世王镞 护肤"}
测试中心/支持库/测试_reportlab提供者.py:68,73
测试中心/模块库/测试_直播逐字稿.py:188,196,201
测试中心/模块库/测试_逐字稿证据包.py:38,40
```

**分类判定：配置引用（违规，但无功能耦合）** —— `验证场景*.json` 是正式包的一部分（会被"场景闭合"门禁消费），`验证夹具/*.json` 也是包内文件；写成 `华世王镞` 只是示例文本，无路径读取、无环境依赖。同时 `模块库.直播逐字稿` 的**测试**也把品牌词当断言值（`测试_直播逐字稿.py:188,196,201`）——测试是**验证业务耦合的证据**，反过来说明该模块的定位就是业务专用。

**建议**：把验证场景/夹具里的品牌词换成中性词（`示例品牌`），测试改用中性断言。

**已核实 / 未确认**：已核实（计数口径：`测试中心/*` 3 文件 7 行、`支持库/*验证场景*` 5 文件 11 行、`技能库` 2 文件 2 行）。

##### V6【建议】硬编码本机绝对路径

**兄弟项目名（`ls ~/Documents/Agent/PHP/`）→ 全仓 grep 结果**

拿到的兄弟目录名（含 worktree/备份/环境目录）共 50+ 个，逐一 grep（`华世王镞_v1/v2/v3/_v3_*/_clean/_backup/资料`、`俏小喵课件网站`、`DouyinLiveRecorder`、`UU_本地模型`、`agent中间件`、`dify控制台`、`hermes_服务站`、`openclaw_服务站`、`opencode服务站`、`本地Agent网关`、`mysql操作`、`平台阶段NN-worktrees` 等）。

- **代码/配置（.py/.json/.plist）中 0 命中**任何兄弟项目目录名。
- 验证：`grep -rn -F "华世王镞_v3" --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ .` → **0 命中**（74 条命中全部落在 `开发文档/`）。
- `letta` / `模型启动` / `数据库备份` 的命中全部是**外部开源源码研究文档**里的词形巧合（`letta-ai/letta`、`Memory-Palace` 备份脚本），非本机兄弟目录引用。

**硬编码底座自身绝对路径 / 本机路径（排除工程缓存与开发文档）**

```bash
grep -rn -F "~/Documents/Agent/PHP/" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=开发文档 .
```

**真实输出（8 条，全部列出）**

```
./.codegraph/daemon.log:1:[CodeGraph daemon] Listening on ~/Documents/Agent/PHP/系统工程平台/.codegraph/daemon.sock (pid 40827, v1.5.0). Idle timeout 300000ms.
./开发工具/更新完整性摘要.py:9:系统根 = Path("~/Documents/Agent/PHP/系统工程平台")
./模块库/直播逐字稿/说明/目录树与命令.md:25:cd ~/Documents/Agent/PHP/系统工程平台
Binary file ./.ruff_cache/0.16.0/1511203214539460607 matches
Binary file ./.ruff_cache/0.16.0/15401007534956279855 matches
Binary file ./.ruff_cache/0.16.0/1734591558387803384 matches
Binary file ./.ruff_cache/0.16.0/5812793186736382318 matches
Binary file ./.ruff_cache/0.16.0/6155852074237461061 matches
```

**分类判定：**
- `开发工具/更新完整性摘要.py:9` → **代码引用（违规）**：硬编码底座自身绝对路径，换机/换目录即断。属"底座可移植性"问题，非业务项目引用。
- `模块库/直播逐字稿/说明/目录树与命令.md:25` → 文档叙述。
- `.codegraph/daemon.log`、`.ruff_cache/*` → 缓存/运行日志，非源码（`--exclude-dir` 已排除 `工程缓存`，这两处是**未在排除表**的缓存目录）。

**另有非 `Agent/PHP/` 的硬编码业务绝对路径（前置告警，见 V1）**：

```
模块库/直播逐字稿/工具/批量精校队列.py:36:~/Documents/Hermes工作区/录音分析/免费祛斑免费敷面膜直播精校项目
模块库/直播逐字稿/工具/批量精校队列.py:37:~/Documents/Hermes工作区/录音分析/直播逐字稿
模块库/直播逐字稿/工具/com.huashi.zizhugao-queue.plist:10:同上（plist 里写死脚本绝对路径）
模块库/直播逐字稿/工具/使用说明.md:50:同上
```

**已核实 / 未确认**：已核实。

##### V7【建议】端口核查：非白名单端口清单

**白名单**：40007、8766、8866、45080-45180、4780。

**全量端口统计**

```bash
grep -rnoE "(127\.0\.0\.1|localhost|0\.0\.0\.0)[:/][0-9]{4,5}" --include="*.py" --include="*.json" --include="*.sh" --include="*.plist" \
  --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ . \
  | sed 's/.*://' | sort | uniq -c | sort -rn
```

**真实输出**

```
  14 8328
  10 45080
   9 54320
   6 45139
   5 8898
   5 5432
   4 45138
   4 45082
   4 40007
   2 8899
   2 8766
   2 59999
   2 45678
   1 8000
   1 50051
   1 4780
   1 4443
   1 11434
```

再核 `45xxx` 全段：`45080 45081 45082 45083 45084 45090 45094 45095 45096 45108 45109 45112 45115 45131 45132 45138 45139 45168 45180` → **全部落在 45080-45180 白名单内，合规**。

**非白名单端口（逐条定位）**

| 端口 | 位置 | 判定 |
|---|---|---|
| 8000 | `项目适配层/项目配置/开发配置.json:3` | 示例值，非业务库端口（详见第一部分第 6 节） |
| 8898 | `支持库/后端/个人蒸馏支持库/实现/个人蒸馏.py:26`（默认值）、`配置契约/配置契约.json:1`、`包声明.json:6`、`能力定义.json:4`、`说明/使用说明.md:5`、`说明/设计说明.md:7` | 与 V3 命名绑定（V3），判 重要（并入 V3） |
| 8899 | `前端核心/浏览器交互.py:36`、`前端核心/渲染提供者.py:50` | 前端核心默认网关地址，疑为个人蒸馏/浏览器网关占位，非业务库 |
| 8328 | `支持库/后端/大语言模型支持库/包声明.json:541,619,690`、`模型连接器/__init__.py:81-83`、`能力定义.json:59/190/300`、`能力契约/参数契约.json:56/195/312`（均为**文档示例串**）+ **`模块库/直播逐字稿/工具/批量精校队列.py:66,98`（真实硬编码业务环境端点）** | 支持库侧为示例文档；`批量精校队列.py` 侧为**违规**（见 V1） |
| 11434 | `模块库/直播逐字稿/验证场景/正向HTTP场景.json:138` | Ollama 端口写死在该包验收场景，随包进正式索引 → 违规（见 V1），建议改白名单端口或占位 |
| 50051 | `开发工具/发布门禁/第十四阶段门禁.py` | 门禁内 gRPC 类探针端口，非业务库 |
| 4443 | `测试中心/第一批维修/测试_网关安全与有界并发.py` | 测试端口 |
| 45678 | `测试中心/发布门禁/测试_发布门禁收敛.py` | 测试端口 |
| 59999 | `测试中心/支持库/测试_psycopg提供者.py`、`测试_数据库连接池.py` | 测试端口（不可达连接用） |
| 54320 | `测试中心/支持库/测试_psycopg提供者.py:26,88-92,124,132,142` | 测试端口（故意不可达） |
| **5432 / 3306 / 6379（数据库端口专项）** | 见下 | **0 处业务库指向** |

**数据库端口专项（5432 共 27 命中，逐类归位）**

```bash
grep -rn -F "5432" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=.codegraph --exclude-dir=.pytest_cache --exclude-dir=.ruff_cache . | awk -F: '{print $1}' | ...
  12 测试中心/支持库            （测试连本机 PostgreSQL，测试_数据库连接池.py:36 连接串，无服务时 skipTest）
   3 开发文档/交付记录/…         （文档叙述）
   2 支持库/后端/数据库连接支持库/psycopg数据库/验证场景.json:12,150  （本机验证连接串 postgresql://postgres@127.0.0.1:5432/postgres）
   2 开发文档/项目证据/
   2 开发文档/
   1 模块库/直播逐字稿/完整性摘要.json:120   ← 假阳性：sha256 串 "…5432…" 子串
   1 支持库/适配层/psycopg提供者/说明/设计说明.md:28: （文档示例 postgresql://用户:***@主机:5432/数据库）
   1 支持库/适配层/psycopg提供者/实现/提供者.py:43: {"port": 解析.port or 5432}  （默认端口，非业务库）
   1 支持库/后端/文档转换支持库/PDF渲染/完整性摘要.json:12  ← 假阳性：sha256 子串
   1 开发文档/参考资料/外部源码研究/…（khoj 文档）
```

- **5432**：全部为「本机 PostgreSQL 默认端口 / 测试 / 验证串 / sha256 假阳性 / 外部研究文档」。**0 处指向华世王镞 V3 或其他业务项目的数据库**。
- **3306**：7 命中**全部**为 `sha256` / `摘要` 十六进制串里的子串假阳性（`支持库/适配层/PyMuPDF提供者/完整性摘要.json:28` 等），**0 处真实 mysql 端口引用**。
- **6379**：1 命中 `开发文档/参考资料/外部源码研究/项目/30_多模态与媒体分析/50_dataset_annotation_quality/cvat/ARCHITECTURE.md:130`（CVAT 外部研究文档），**0 处底座代码引用**。

**分类判定**：5432/3306/6379 三个数据库端口 **0 违规**；非白名单端口里只有 **8328 / 11434 的 `模块库/直播逐字稿` 两处**属真实违规（并入 V1），其余为示例/测试/占位。

**已核实 / 未确认**：已核实。

##### V8【已核实·0 违规】数据库文件名引用（*.db / *.sqlite / *.sqlite3）

**复现命令**

```bash
grep -rn -E "\.(db|sqlite|sqlite3)" --include="*.py" --include="*.json" --include="*.sh" --include="*.plist" \
  --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ .
```

**真实输出归类（全量 100+ 条，逐类验证，无一例外）**

| 类别 | 代表命中 | 是否 `工程缓存/` 或临时 |
|---|---|---|
| 底座运行库 | `MCP工具箱/作业系统.py:66` → `系统根/工程缓存/运行数据/底座运行.db`；`协作状态.py:52`、`能力占用.py:91` 同 | **工程缓存/（是）** |
| 平台控制面权威状态 | `运行核心/权威状态.py:124` → `self.存储目录/"权威状态.db"`；`平台控制面/备份恢复/备份执行.py:21,44,48-50,76`、`恢复执行.py:28,46,48,50`、`开发工具/发布门禁/权威状态门禁.py:21,57`、`平台控制面/提供者/控制面对账.py:25`（只读 `mode=ro`） | 存储目录 = `工程缓存/平台控制面/` |
| 支持库自身库文件 | `会话存储.py:28`、`嵌入缓存.py:25`、`检查点恢复.py:21,183`、`工具执行.py:85`、`记忆.py:23` → 均走 `解析运行数据根(...)` | **工程缓存/（是）** |
| 契约示例路径 | `支持库/后端/数据库连接支持库/SQLite数据库/能力契约/参数契约.json:45,111,395,547` → `/tmp/示例.db` | /tmp |
| 验证夹具 | `验证场景*.json` 里的 `"相对路径": "检查点恢复/检查点场景.db"`、`"来源": "夹具/空检查点.db"` | 夹具目录（相对） |
| 测试临时库 | `测试中心/支持库/测试_SQLite数据库.py:23`、`测试_运行数据库.py:40,106`、`测试_检查点恢复隔离.py:15,16`、`测试_嵌入缓存线程安全.py:15` 等 → `Path(self.临时目录.name)/…` | tempfile 临时目录 |
| 构建期排除规则 | `客户端/构建平台客户端.py:366,442`（`suffix in {".db",".sqlite",".sqlite3"}` 则不打进制品）、`平台控制面/包仓库/平台客户端制品.py:59`（禁用扩展名表） | 规则，非路径引用 |

**判定：0 违规**。全部 `*.db/*.sqlite/*.sqlite3` 引用要么在 `工程缓存/`，要么是 `/tmp` 示例，要么是 `tempfile` 临时目录，要么是相对夹具名，要么是扩展名规则表。**0 处指向业务项目数据库文件**。

**已核实 / 未确认**：已核实。

##### V9【建议】底座模块内硬编码默认网关凭证与业务环境密钥变量名

```
模块库/直播逐字稿/工具/批量精校队列.py:33:凭证 = os.environ.get("系统库网关凭证", "html-blackbox-verifier")
模块库/直播逐字稿/工具/批量精校队列.py:34:密钥 = os.environ.get("HERMES_CUSTOM_CUSTOM_API_KEY", "").strip()
```

- `:33` 把网关 Bearer 凭证字面量 `html-blackbox-verifier` 写死在底座正式包脚本里（无业务路径，但是**凭证默认值硬编码**），且该脚本会被打进平台客户端制品。
- `:34` 引用的是 **Hermes 环境变量名**（`HERMES_CUSTOM_CUSTOM_API_KEY`），是外部宿主环境的专属键，属环境耦合。
- 判定：**代码引用（建议级）**——不构成敏感值泄露（`:34` 默认空串、`:33` 是中性字符串），但写死凭证默认值是安全卫生问题。

**已核实 / 未确认**：已核实。

##### V10【0 命中确认】以下检索项明确 0 命中

| 检索项 | 命令要点 | 结果 |
|---|---|---|
| `华世王镞_v3` 在**代码/配置**中 | `grep -rn -F "华世王镞_v3" --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ .` | **0 命中**（74 条全在 `开发文档/`） |
| 兄弟项目目录名在 `.py/.json/.plist` | 50+ 个目录名逐一 grep，排除 `开发文档`/`工程缓存` | **0 命中**（唯一例外是 `模块库/直播逐字稿` 里的 **非 PHP 目录**业务路径 `…/Hermes工作区/录音分析/…`，已单列 V1/V6） |
| `3306` 真实端口引用 | 见 V7 | **0 命中**（7 条全是 sha256 假阳性） |
| `6379` 底座代码引用 | 见 V7 | **0 命中**（1 条在外部研究文档） |
| 业务项目数据库文件路径 | 见 V8 | **0 命中** |
| `项目适配层` 内业务引用 | `grep -rn -E "华世王镞\|huashi\|V3\|/Users/\|业务" 项目适配层/` | **0 命中**（唯一"业务项目"字样是 `项目声明/项目声明.py:4` 的文档字符串"适配层只消费声明，不读取业务项目代码"，属正向声明） |

##### 第二部分命中分类汇总

| 类别 | 条数 | 明细（精确口径：`业务引用违规数 = 21`，只算 `.py/.json/.plist` 代码/配置文件） |
|---|---|---|
| **代码/配置引用（违规）** | **21** | **模块库/直播逐字稿 15 条**：`工具/批量精校队列.py:33,34,36,37,43,46,66,98`（8）、`工具/安装或卸载队列常驻.py:19`（1）、`工具/com.huashi.zizhugao-queue.plist:6,10`（2）、`能力契约/参数契约.json:370`（1）、`模式数据/模式注册表.json:19`（1）、`完整性摘要.json:87`（1）、`验证场景/正向HTTP场景.json:138`（1）；**支持库/后端/个人蒸馏支持库 4 条**：`实现/个人蒸馏.py:26`、`配置契约/配置契约.json:1`、`包声明.json:6`、`能力定义.json:4`；**门禁/运维 2 条**：`开发工具/发布门禁/运行发布门禁.py:466`、`运维脚本/热接入.py:26`。 |
| **包内文档引用（违规，另计不计入 21）** | 5 | `模块库/直播逐字稿/工具/使用说明.md:12,50`、`模块库/直播逐字稿/说明/使用说明.md:63`、`支持库/后端/个人蒸馏支持库/说明/使用说明.md:5`、`说明/设计说明.md:7` |
| **纯文档叙述（需人工判定）** | 约 74（`华世王镞_v3`）+ 31（`业务项目`）+ 6（`上层业务`）+ 大量外部源码研究 md | `开发文档/**` 与 `开发文档/参考资料/外部源码研究/**`；注意 `开发文档/通用能力反馈接口与平台自治治理.md:254` 自己定的规矩是"不包含任何具体业务项目名称" |
| **门禁自身引用（白名单性，不判违规）** | 1 | `开发工具/统一验证/统一验证器.py:430`（禁词检查串） |
| **假阳性（哈希子串）** | 8 | `3306`×7、`5432`×2 中的 `模块库/直播逐字稿/完整性摘要.json:120`、`支持库/后端/文档转换支持库/PDF渲染/完整性摘要.json:12` |

---

### 命令清单（全部真实执行过）

```bash
### 基线
cd ~/Documents/Agent/PHP/系统工程平台 && pwd
/Library/Developer/CommandLineTools/usr/bin/git rev-parse HEAD
/Library/Developer/CommandLineTools/usr/bin/git branch --show-current
find 项目适配层 -type f -not -path "*__pycache__*" | sort
sed -n '141,218p' AGENTS.md

### 第一部分·越界导入
grep -rnE "^\s*(from|import)\s+(支持库|模块库|技能库)" 项目适配层 --include="*.py"
grep -rnE "\.实现\.|提供者" 项目适配层 --include="*.py" | grep -E "^\S+:[0-9]+:\s*(from|import)"
grep -rn "^ *\(from\|import\) " 项目适配层 --include="*.py" | sort

### 第一部分·配置合并实测（python3.14 内联，见正文输出）
### 第一部分·密钥泄露
grep -rn "print(\|logging\|logger\|日志\|json.dump\|write_text\|write_bytes\|environ\|getenv\|密钥\|密码\|token\|secret" 项目适配层 --include="*.py"
grep -rn "print(\|logging\|写入日志\|log(" 项目适配层 --include="*.py"

### 第一部分·死代码与调用方
grep -rn "项目适配层" --include="*.py" . | grep -v "^./项目适配层/" | grep -v __pycache__ | grep -v "工程缓存"
grep -rn "校验锁定文件\|校验传递闭包\|生成依赖锁定" --include="*.py" .
grep -rn "获取HTTP连接器\|读取配置目录\|读取配置(" --include="*.py" .
grep -rn "项目路径(" --include="*.py" .

### 第一部分·配套文件与正式包索引
find 项目适配层 -name "包声明.json"
for d in 公共契约 平台控制面 启动监督器 运行核心 前端核心 后端核心 支持库 模块库 技能库 项目适配层 开发工具 测试中心 示例项目 客户端 MCP工具箱; do echo "$d: $(find $d -name 包声明.json | wc -l)"; done
sed -n '59,200p' 开发工具/项目编译/正式包索引.py
### python3.14: from 正式包索引 import 构建索引; 构建索引(Path('.'))

### 第一部分·存活实测 + 接口断裂实测（python3.14 内联）
### python3.14 -c "from 项目适配层.依赖锁定.锁定校验 import 校验锁定"   → ImportError
### python3.14 -c "…校验传递闭包(示例项目/适配层示例, .)… / …校验锁定文件(…)"   → 成功 True

### 第二部分·业务词
grep -rn -F -e "华世王镞" -e "王镞" -e "huashi" -e "祛斑" -e "招商" -e "V3" -e "v3" \
  --include="*.py" --include="*.json" --include="*.plist" --include="*.js" --include="*.ts" --include="*.html" --include="*.vue" --include="*.sh" --include="*.md" \
  --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=开发文档 .
grep -rn -F "业务项目" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ .
grep -rn -F "上层业务" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ .

### 第二部分·兄弟项目与绝对路径
ls -1 ~/Documents/Agent/PHP/
grep -rn -F "~/Documents/Agent/PHP/" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__ --exclude-dir=开发文档 .
grep -rn -F "华世王镞_v3" --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ .

### 第二部分·端口与数据库文件
grep -rnoE "(127\.0\.0\.1|localhost|0\.0\.0\.0)[:/][0-9]{4,5}" --include="*.py" --include="*.json" --include="*.sh" --include="*.plist" \
  --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ . | sed 's/.*://' | sort | uniq -c | sort -rn
grep -rnoE "45(0[89][0-9]|1[0-7][0-9]|180)" --include="*.py" --include="*.json" --exclude-dir=工程缓存 --exclude-dir=开发文档 --exclude-dir=.git . | sed 's/.*://' | sort -u
grep -rn -E "\.(db|sqlite|sqlite3)" --include="*.py" --include="*.json" --include="*.sh" --include="*.plist" \
  --exclude-dir=开发文档 --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=__pycache__ .

### 第二部分·门禁覆盖
grep -rn "华世王镞\|业务项目\|禁词\|业务引用\|行业词\|huashi" 开发工具/发布门禁/运行发布门禁.py 开发工具/统一验证/统一验证器.py 运行核心/依赖防火墙.py
sed -n '390,435p' 开发工具/统一验证/统一验证器.py
```

---

### 跳过项（按冻结边界主动未做）

1. **未跑发布门禁**（`开发工具/发布门禁/运行发布门禁.py`）——冻结边界明确禁止。
2. **未重编制品**（未跑 `客户端/构建平台客户端.py`、`开发工具/项目编译/项目编译器.py`）——冻结边界禁止。
3. **未跑测试**（未跑 `测试中心/运行测试.py`）——会写运行数据；`校验传递闭包`/`校验锁定文件` 用只读直调替代（两者均只 `read_text`，不写盘，已核）。
4. **未跑 `正式包索引.py` 的 CLI**（可能触发编译）——改为 `import 构建索引` 纯内存调用，只读。
5. **未对 `工程缓存/`、`.git/`、`.codegraph/`、`.ruff_cache/`、`.pytest_cache/`、`__pycache__/` 内的命中做源码判定**——这些是生成物/缓存，按 AGENTS.md 第162行"工程缓存可删除、可重新生成，不作为正式源代码"处理；仅在 V6 中如实标注了 `.codegraph/daemon.log`、`.ruff_cache/*` 未被排除集覆盖这个**事实**。
6. **未修改 `开发文档/`、`AGENTS.md`**——只读审计，所有"建议改 AGENTS.md"均为放行给主审计的提议，未落笔。

### 未确认项

1. **`前端核心/浏览器交互.py:36` / `渲染提供者.py:50` 的 `127.0.0.1:8899` 归属**——未确认它指向哪个服务（疑似个人蒸馏/浏览器网关），也未确认是否与 `8898`（V3 个人蒸馏）同源。**未确认**。
2. **`模块库/直播逐字稿` 的业务耦合是否为"已记录在案的例外"**——`开发文档/未完成事项.md:61` 声称"底座已彻底去掉行业写死内容（9 个模式提示词零行业词，已断言）"，与 V1 实测冲突。未确认这两者哪个是权威、以及 `工具/批量精校队列.py` 是否属"消费者侧脚本"被有意保留。**未确认**（属口径裁决，超出只读取证范围）。
3. **V1 命中的 `模块库/直播逐字稿` 脚本是否仍在实际运行**（`~/Library/LaunchAgents/com.huashi.zizhugao-queue.plist` 是否已安装）——冻结边界禁止动服务，未查 launchd 状态。**未确认**。
4. **`数据库连接标识` 在运行时最终解析成什么**（`默认配置.json`/`开发配置.json`/`发布配置.json` 只有抽象标识，真正的连接串由哪层提供）——搜索未找到消费 `数据库连接标识` 的代码，疑为预留字段。**未确认**。
5. **`开发工具/更新完整性摘要.py:9` 硬编码系统根是否被门禁调用**——未展开追踪其调用链。**未确认**。

---

*本文所有 path:行号 与命令输出均来自 HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc` 现场取证；无一处推算或转录自文档声称。*

---

## 第 4 节 · MCP工具箱

> 本节由专项子审计产出，原文完整保留；数据与证据格式同上（path:行号 + 真实命令 + 真实输出摘录）。

### 子审计 C：系统工程平台（底座）· MCP工具箱/

- 审计对象：`~/Documents/Agent/PHP/系统工程平台/MCP工具箱/`（25 个文件，去缓存）
- 冻结点：HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`，分支 `主干`
- 现场服务：8766 平台 MCP（PID 40029，PGID 40021）LISTEN；40007 网关（PID 50802）LISTEN；8866 能力网关未监听
- 口径：只认现场证据（path:行号 + 真实命令 + 真实输出摘录）。未写仓库任何文件，仅写 `/tmp`；未重启任何服务。
- 统计：**阻断 3 / 重要 7 / 建议 6**

---

### 1) MCP 真实工具数 = 50（唯一入口声明一致）

**真实工具数：50**

源码唯一权威表：`MCP工具箱/项目服务.py:419-470`（`_工具定义列表`）。

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && python3 -c "import re;s=open('MCP工具箱/项目服务.py',encoding='utf-8').read();n=re.findall(r'Tool\(name=\"([^\"]+)\"',s);print(len(n));print(n)"
```

真实输出（节选）：`50 ['project_context', 'role_profile', ...]`

运行中服务 `tools/list` 实测（HTTP 8766，无需凭证）：

```
initialize → {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-06-18","capabilities":{"experimental":{},"tools":{"listChanged":false}},"serverInfo":{"name":"system_engineering_toolkit","version":"1.28.1"}}}
tools/list → 真实 tools/list 数量 = 50
```

逐个工具名（1-50）：
`project_context, role_profile, capability_search, capability_read, mcp_feedback, feedback_status, feedback_review, support_library_development_guide, module_development_guide, core_development_guide, project_development_guide, platform_build_development_guide, platform_maintenance_guide, release_guide, codegraph_explore, memory_search, memory_write, verify_and_record, verification_plan, development_start, temporary_context, task_observation, workspace, test_resource, register_requirement, reuse_search, claim_capability, validate_module_compliance, generate_module_template, create_core_snapshot, query_core_snapshot, compatibility_check, rollback_gate, run_release_gate, check_release_evidence, generate_release_evidence, switch_active_pointer, dependency_arbitration, register_task, collaboration_status, delivery_closeout, file_lease, apply_file_patch, validate_verification_command, judge_verification_result, tool_job_submit, tool_job_query, tool_job_cancel, tool_catalog, reload_tool_modules`

中文名映射覆盖 50/50，无缺无多：

```bash
python3.14 -c "...映射条目数 = 50 / 缺中文名: [] / 多余映射: []"
```

**与声称的一致性：一致（已核实）**
- `开发文档/项目说明.md:62`「Agent 工具接入 | `MCP工具箱/项目服务.py` | 单一 MCP 入口；连接失败即阻断，不启用第二工具链」——仓库内只有一个 MCP Server 声明：`项目服务.py:68 服务 = Server("system_engineering_toolkit")`（全仓 grep `Server(` 无第二处 MCP 服务，另有 2 处是支持库夹具的 `HTTPServer`）。
- 两个客户端配置都指向同一 8766：`opencode.json:6 "url": "http://127.0.0.1:8766/mcp/"`、`.mcp.json:5` 同址。
- `AGENTS.md:41`「`MCP工具箱/项目服务.py` 是唯一权威实现」——一致（未发现第二套 MCP 实例）。
- `工具列表()` 为全量直通、不按角色过滤：`项目服务.py:473-476`。

> 建议-4（见第 5 节尾部）：`开发文档/项目说明.md:60` 说 8866 能力网关「只读搜索/契约」，而 `能力网关.py:40` docstring 自称「搜索/契约/**执行** 三个路由」，`能力网关.py:92-95` 的 POST 已固定 404。仅注释与文案不一致，行为是只读的。

---

### 2) 作业系统.py：线程池 + 单进程，无超时实现，内存作业表无界

**真实实现（已核实，逐条给证据）**

| 项 | 实现 | 证据 |
|---|---|---|
| 调度 | 进程内 `ThreadPoolExecutor(max_workers=1)`（默认**串行队列**），提交即返回快照 | `作业系统.py:123-124`、`:281-302` |
| 独立进程？ | **否**。模块自述放弃 multiprocessing fork，改用线程池 | `作业系统.py:7-9`「那边用 multiprocessing fork 独立进程（可 killpg 真取消），本模块在 asyncio 进程内，fork 有死锁风险，故改用线程池」 |
| 取消 | 尽力取消：未开始的可 `Future.cancel()` 真取消；运行中只打 `取消标记` 并丢弃产出 | `作业系统.py:362-377`、`:328-330`「运行中被打上取消标记，产出已丢弃」 |
| 超时 | **无超时实现**。`状态_超时` 只有定义与终态集，全文件无任何赋值点 | `作业系统.py:43`、`:46`（`grep -n "状态_超时"` 仅 2 行命中） |
| 失败语义 | 执行器崩溃→失败；返回 `{"成功": false}`→失败；非 JSON 文本→**判成功**并放 `原始输出` | `:320-323`、`:331-334`、`:526-532` |
| 账本落点 | 底座运行库 `工程缓存/运行数据/底座运行.db` 的 `作业` 域（经唯一 SQLite 支持库，`获取能力调用器().调用能力`） | `:17-20`、`:61-66`、`:142-176`、`:224-251`（`域: "作业"` 在 `:159`、`:245`） |
| 旧账本 | `工程缓存/作业状态/作业.jsonl` 只读兼容、不再写，缺页按 作业id 一次性补齐 | `:138-140`、`:178-191`、`:194-206` |
| 重启收敛 | 非终态作业一律收敛为「崩溃」 | `:214-219` |
| 有界 | 结果落库 512KB 截断（`结果已截断` 标记回写）——**有界**；但内存 `self.作业表` 只增不删——**无界** | 有界：`:49`、`:253-277`；无界：`:119`（无淘汰逻辑，`grep 作业表` 10 处无任何 `pop/clear`） |

**对比 40007**：`运行核心/任务调度/任务系统.py:110`、`:187` 用同一个库的 **`任务`** 域，`:72` 另有 `进程任务` 目录 —— 两套任务系统域不冲突、状态名对齐（同文档声明），但**同一进程组内存在两套调度器**。

**分级**：重要-5（内存作业表无界 + 超时状态不可达）；建议（非 JSON 结果恒判成功）。与 `AGENTS.md:154`「所有线程、进程、队列、缓存、日志、临时文件、句柄和终态记录必须有界」冲突。

---

### 3) 热重载.py：只做同包模块级 reload，失败静默，无残留文件，真能被触发

- 粒度：**模块级**，`importlib.reload(sys.modules[全名])`，只重载 `MCP工具箱/` 同包模块，并按 `from .X` 依赖做**拓扑序**（被依赖者在前）。
  证据：`热重载.py:32-71`（AST 解析 `ast.ImportFrom` `level==1`）、`:96`、`:116-127`。
- 不做整包：明确排除入口与自己，也不重载 `公共契约`/`运行核心`/`平台控制面`；新增/删除工具与参数表变更仍需重启。
  证据：`热重载.py:10-11`、`:95 排除集 = set(排除 or ()) | {"__init__", "热重载"}`。
- 失败语义：**失败不报错**——`except Exception: continue`，保留旧实现、快照不更新以便下次重试。
  证据：`热重载.py:121-125`。即：热重载层面「失败即报错」**不成立**（`项目说明.md` 里「失败即报错」指的是网关侧热接入，不是本模块；`未完成事项.md:51` 也如实写了「坏源码不抛错、保留旧实现」）。
- 残留文件：**无**。本模块不写盘（无 `write_text/open("w")/unlink/rmtree`），只读 mtime。→「残留文件」问题不存在。
- 谁调用（真实触发链，全仓 grep 仅两处）：
  1. `项目服务.py:618 本次重载 = 扫描并重载工具模块(Path(__file__).parent, globals(), {Path(__file__).name})` —— **每次工具调用入口都跑一次 25 文件 mtime 扫描**；
  2. 工具 `reload_tool_modules`：`项目服务.py:469` 定义、`:635-641` 返回结果。
  并发保护：`:29 _重载锁 = threading.RLock()`、`:80` 串行包装（作业线程与事件循环会并发进入）。
- 回填边界：`_回填` 只覆盖「目标全局已有且 `__module__` 指向本模块」的名字，新增符号不会注入。
  证据：`热重载.py:131-141`。

**分级**：建议-5（失败静默降级；文档已如实声明）。

---

### 4) 协作状态 / 文件租约：有真状态机，无死锁但无硬截止；存在第二套资源回收实现

##### 4.1 文件租约：真状态机 + 原子互斥（已核实）
- 状态机在平台控制面：`活跃 → 已释放`（带释放证据）/`活跃 → 已过期`；互斥靠唯一部分索引原子插入，不是「先查再写」。
  证据：`平台控制面/能力目录/服务.py:80-93`（`原子插入` + 注释「唯一部分索引（活跃租约同能力唯一）原子保证互斥」，`:87 过期时间 = time.time() + 心跳秒 * 10`）、`:102-104`（释放）、`:106-114`（回收过期）。
- 终态释放：`活跃/已释放/已过期` 三态明确；MCP 侧收口时按所有者批量释放，且**真实消费释放返回值**（失败不谎报）。
  证据：`文件租约.py:113-126`、`能力占用.py:346-360`、调用点 `项目服务.py:875-887`。
- 死锁判断：**不会永久死锁**——申请时先回收过期（`文件租约.py:70`），会话崩溃后 300 秒可被重新认领（心跳 300s：`文件租约.py:19`、`:70`）。
- 但**无硬截止**：`续租` 只刷 `心跳`（`平台控制面/能力目录/服务.py:99-100`），持有方可无限续租 → 属**饥饿/拿不到**风险，而非死锁。对照平台既有租约实现是有硬截止的：`运行核心/租约管理.py:22-32`（`空闲超时秒` + `硬截止时间`）。
- 批量申请全成功才通过、失败回滚：`文件租约.py:79-85`（真实现，非仅声明）。
- **死字段**：`过期时间` 在申请时写入（`服务.py:87`），但回收逻辑只用 `心跳 <= now-心跳超时秒`（`服务.py:110-112`），`过期时间` 从不被读 → 状态机有列不驱动任何行为。

##### 4.2 协作状态：是任务登记，不是租约（与租约表不重复）
- 生命周期 `创建/进行/完成`，落 `底座运行.db` 的 `协作状态` 域；收口置 `完成`（`协作状态.py:254-260`、`:349-351`、`:434-441`）。
- 「关闭」分支只存在于读取归一化（`协作状态.py:350`），无任何写入方设置 `关闭`。
- 与 `文件租约.py` 使用**不同表/不同域**（`占用租约` 表 vs `协作状态` 域），不是同一状态机的两份实现。

##### 4.3 第二实现风险（重要）
- `公共契约/句柄体系.py:3-5` 自述：「资源生命周期（华哥口径）：资源登记绑定到句柄 → 状态机失效时统一监控回收（PID/端口/连接/临时文件），**不旁路拼接独立回收能力**」；且该文件本身已实现 PID/端口回收：`句柄体系.py:140`、`:156-164`、`:168-188`（`os.killpg/kill`、SIGTERM→SIGKILL）。
- 而 MCP 侧另建一套回收：`测试资源.py:281-296`（`os.killpg(os.getpgid(pid), signal.SIGKILL)`）、`:299+`（端口释放）、`:224-254`（按记录清理文件/目录/进程/端口/线程/句柄），且不绑句柄、不经句柄状态机。
- 同处还有第三套终态语义：`作业系统.py` 线程取消 + 七态机。
- 结论：**同一职责被实现了三次**（句柄体系回收 / 测试资源回收 / 作业线程取消），与 `AGENTS.md:107`「同一职责的内部实现也不得复制」及 `开发文档/项目说明.md:47`「不得复制一套能力注册、参数校验或执行语义」的精神冲突。

---

### 5) 写路径与门禁绕过（含具体代码证据）

##### 阻断-1：`workspace` 的提交/合并把「调用者传入的 path」当项目根用 → 可对任意 git 仓库（含本仓）提交/切分支/合并

- 分发侧把调用者参数直接当**项目根**形参传进去：
  `项目服务.py:779-782`
  ```
  操作 = "提交" → 工作区提交(Path(str(参数["path"])), str(参数["message"]), 路径列表=...)
  操作 = "合并" → 合并分支(Path(str(参数["path"])), str(参数["target_branch"]), str(参数["source_branch"]), ...)
  ```
- 实现侧该形参就叫 `项目根目录`，并被当作 git 操作目录，**没有任何 relative_to 项目根边界校验**：
  `工作区管理.py:307 操作目录 = Path(项目根目录).resolve()` → `:308 ["git","switch",目标分支]` / `:312-318 ["git","merge",...]`；
  `工作区管理.py:333 操作目录 = Path(项目根目录).resolve()` → `:334 ["git","add",...]`（无 paths 时 `:341 "-A"`）→ `:345 ["git","commit","-m",消息]` → `:348 git log`。
- 对比：同一文件的 `关闭工作区` 是有边界校验的（`工作区管理.py:74-77 目标 == 项目根目录.resolve() or not str(目标).startswith(项目根文本)` → 拒绝）——说明边界校验只在部分入口实现，提交/合并入口漏做。
- 影响：8766 无认证（见第 7 节），本机任意进程可让该服务在**任意 git 仓库**（含本仓库 `主干`）执行 `git add -A` + `commit`、`switch`、`merge`，完全绕过 `AGENTS.md:9` 的「不得绕过 project_context、mcp_feedback、verify_and_record、路径边界和验证命令白名单」。
- 只读复现（不执行写操作，只读源码+只读命令）：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台 && awk 'NR>=303 && NR<=349 {printf "%d|%s\n",NR,$0}' MCP工具箱/工作区管理.py
  ```
  （真实输出见上文引用的 307/308/312-318/333/337-339/345 行）

##### 阻断-2：`test_resource` 允许登记「本进程自身 pid」为子进程资源，清理时 `killpg` 自身进程组 → 可自杀 8766

- 登记放行自身 pid：`测试资源.py:107-109`
  ```
  if pid == os.getpid() or _是当前进程后代(pid):
      _已启动子进程pid集合.add(pid)
      return pid
  ```
- 清理前复核同口径放行：`测试资源.py:117-121`（`_复核清理归属`）。
- 触发链：`测试资源.py:237-242`
  ```
  if 类型 == "子进程":
      _复核清理归属(int(标识), f"子进程:{标识}")
      _终止子进程组(int(标识))
  ```
  → `测试资源.py:281-296`
  ```
  os.killpg(os.getpgid(pid), signal.SIGKILL)
  ```
- 对照：端口分支有自身守卫 `测试资源.py:302`（`if 占用pid is not None and int(占用pid) != os.getpid():`），子进程分支**没有**。
- 现场进程组（真实输出）：
  ```
  40021  40018  40021  login -pflq 用户 /bin/bash -lc exec ./MCP工具箱/启动HTTP服务.sh 8766
  40029  40021  40021  .../Python -m MCP工具箱.项目服务 --http --端口 8766
  Python  40029 ... TCP 127.0.0.1:8766 (LISTEN)
  ```
  `os.getpgid(40029) = 40021` → `killpg(40021, SIGKILL)` 同时杀掉 8766 服务进程与其 bash 包装（脚本未 `setsid`，`启动HTTP服务.sh:12 exec python3.14 ...`）。即：**本机任意进程可通过 MCP 工具自杀平台 MCP 服务**（DoS）。
- 注：未实弹触发（冻结边界禁止重启服务），仅代码路径 + 进程组事实核实；端到端杀伤为**未确认**。

##### 重要-1：`validate_module_compliance` 的「模块名」无路径边界 → 目录穿越并进入权威合规器执行链
- 无 resolve、无 `is_relative_to`：`模块合规.py:71-79`
  ```
  模块名 = 模块名.strip().removeprefix("模块库.").strip("/")
  模块目录 = 项目根 / "模块库" / 模块名
  if not 模块目录.is_dir(): return None
  ```
  对照同层有校验的写法：`验证门禁.py:90 解析.is_relative_to(工作根.resolve())`。
- 真实复现（只读，复刻该函数逻辑，未执行任何模块）：
  ```
  模块名='../../..'      -> .../模块库/../../..  resolve= ~/Documents/Agent  逃出模块库 = True
  模块名='../公共契约'    -> .../模块库/../公共契约  resolve= .../系统工程平台/公共契约  逃出模块库 = True
  模块名='..'            -> .../模块库/..  resolve= .../系统工程平台  逃出模块库 = True
  ```
- 穿越目录随后交给权威合规器：`模块合规.py:224-233 `_权威合规违规` → 模块目录 = _定位模块目录(...) → 报告 = 组件合规(模块目录).执行()`。该链路对目录内 `包声明.json` 的 `入口` 做 `spec_from_file_location` + `exec_module`（子代理已读得 `开发工具/组件合规/合规测试包.py:184-188`、`:789-793`）；因目标目录需含 `包声明.json`，端到端「执行任意目录内 .py」为**部分核实/未确认**。

##### 重要-2：发布证据可「仅凭入参自洽」生成（模块级），MCP 面已封
- 关键字段全是入参：`发布治理.py:265-273`（`退出码: int, 指纹: str, 制品摘要, 来源指纹, 能力覆盖, 场景覆盖, 真实结果`），`:291-301` 仅在 `证据类型 == 正式发布证据类型` 时加校验。
- 可自洽绕过真实指纹：`发布治理.py:298 预期工作区指纹 = 核验工作区指纹 or 当前工作区指纹()` 与 `:242 if 条目.get("工作区指纹") != 实际工作区指纹` —— 调用者把 `指纹` 与 `核验工作区指纹` 传成同值即通过，落盘路径 `工程缓存/发布证据/{提交}.json`（`:49`、`:278`、`:304-307`）。
- MCP 面已封（已核实）：`项目服务.py:855-859` 对 `generate_release_evidence` 恒返回 `{"成功": False, "错误码": "命令拒绝"}`；唯一写入在 `verify_and_record` 内（`:938-950`），前置 `:927-928` 反馈门禁 + `:930-932` 白名单校验。
- 分级：**重要**（能直接调用该模块或能写 `工程缓存/` 的本机进程仍可伪造正式发布证据；`工程缓存/` 被 `.gitignore` 忽略，无版本层可查）。

##### 重要-3：门禁「自证」——`register_task` 无授权 + `mcp_feedback` 未校验 work_id
- `项目服务.py:657 反馈开工id = str(参数.get("work_id") or 当前开工id)` —— 直接用原始值；而同一文件 `:630-631` 对观测事件用的是 `_有效开工id(...)`（会校验当前/已登记）。两处口径不一致。
- `_有效开工id` 的放行判据是「当前开工id 或 有临时上下文 或 协作状态已登记」：`项目服务.py:290-302`。
- 而 `register_task` 无任何授权模型，任意 16 位十六进制 id 都能登记：`项目服务.py:868-871` → `协作状态.py:226-275`（`_校验开工id` 只查格式，`:55-60`）。
- 结论：`verify_and_record`/`delivery_closeout` 的「未反馈阻断」可以被同一调用方用「先自己登记 work_id → 自己写 5 字段反馈 → 再记录验证」满足，属**自证门禁**，不是外部约束。

##### 重要-4：开工身份是进程级全局变量 → 多会话互相漂移
- `项目服务.py:60-61 当前任务名称 = "" / 当前开工id = ""`（模块全局）；`:153-157` 每次 `project_context` 按「任务名变化」就重新生成开工id。
- 8766 是单 uvicorn 单事件循环多会话（`项目服务.py:992 stateless=False`），所有会话共享这两个全局 → 会话 A 的 `当前开工id` 会被会话 B 的 `project_context` 顶掉，反馈/证据/租约归属/收口全跟着漂移。这与 `AGENTS.md:79-83`「不得为支持库、模块、核心分别复制一套会产生分叉状态的 MCP 实例」的立意（防分叉状态）方向相反。

##### 重要-6：若干目录参数无边界校验（MCP 未透出，模块级）
- `发布治理.py:277 目录 = Path(证据目录) if 证据目录 else 发布证据目录`（`:278` 之后 `:304` 写入）；`:473-476`、`:509-511` 同型（环境目录、激活证据路径）。绝对路径/`../` 均可写任意目录。
- `核心治理.py:154-155/:288/:303` 快照根目录无校验（落点 `项目根/工程缓存/核心快照`，`:37-39`；激活指针 `:303-304`）。回滚只写 `当前.json`（`:111-114`），**不覆盖源码**（已核实）。
- 反例（有校验）：`发布治理.py:372-374` 制品相对路径 `startswith(str(制品目录.resolve()) + "/")`；`核心治理.py:51-58` 快照标识 `is_absolute()/`..` 段/前缀`校验。

##### 建议-1 / 建议-2 / 建议-3
- 建议-1：`工作区管理.py:337-339` 提交路径只拦 `"-"` 前缀，未拦 `..` / 绝对路径（实测由 git 自身 `fatal: ... outside repository` 兜住）。
- 建议-2：`支持库协作.py:73` 需求登记 `_原子写(工程缓存/需求登记/{work_id}.json)`，无 TTL、全仓无清理者（现场 5 个文件，`ls 工程缓存/需求登记/`）。
- 建议-3：占用租约 `过期时间` 死字段（见 4.1）。

---

### 6) 与 40007 网关：MCP 不是「转发到唯一入口」，而是在自己进程内另起一套运行核心执行能力

**证据 1：MCP工具箱内没有任何对 40007 / `/网关/调用` 的调用代码（只有注释与文案）**
```bash
cd ~/Documents/Agent/PHP/系统工程平台 && grep -rn "40007\|网关/调用\|requests\|urllib.request\|httpx" MCP工具箱/*.py MCP工具箱/*.sh
```
真实输出（全部为注释/说明/无关词，无一处真实 HTTP 客户端）：
```
MCP工具箱/作业系统.py:7:与 40007 `运行核心/任务调度/任务系统.py` 的差异：...
MCP工具箱/能力网关.py:95: "错误说明": f"执行入口已迁移到 /网关/调用，未定义路由: {路径}"
MCP工具箱/启动能力网关.sh:4:# 默认端口 8866（能力搜索/契约），执行统一走运行核心 40007 /网关/调用。
```

**证据 2：MCP 进程内确实执行能力（第二份运行核心实例）**
- 写入通道直接以底座能力改盘：`文件补丁.py:90-103`
  ```
  from 开发工具.开发入口 import 真实调用能力
  调用 = 真实调用能力(能力id_应用精确替换, {...}, 用户id=str(开工id or ""))
  ```
- 该入口在**本进程内**装配全量注册表并调用：`开发工具/开发入口.py:123-138`
  ```
  from 后端核心.后端核心 import 后端核心
  后端 = 后端核心(); 后端.启动()
  结果 = 后端.调用(能力id, 参数 or {}, 上下文=运行上下文(...))
  ```
  → `后端核心/后端核心.py:124-137`（启动=装配）→ `:100-122` 装配系统装载 `支持库/模块库/技能库` 并 `设置全局唯一服务(唯一能力调用服务)` → `:529 self._唯一调用服务.调用能力(...)`。
- 另有两条同型直连：`作业系统.py:142-153`（`import 运行核心.能力调用.唯一能力调用` + `获取能力调用器().调用能力("数据库连接支持库.SQLite数据库.查询运行态"/"写入运行态", ...)`）、`协作状态.py:70-81` 同型。
- 因此 MCP 进程自身读写 `工程缓存/运行数据/底座运行.db` 的 `作业`/`协作状态`/`能力占用` 域 —— 与 40007 侧 `运行核心/任务调度/任务系统.py:110/:187` 写的 `任务` 域并存。

**判定**
- 形式上合规：MCP 侧确实只经 `调用能力(能力id, 参数)` 这个唯一 API，未发现 MCP 内直连注册表 / Provider / `支持库.*.实现`（`模块合规.py` 只做 AST 审计，未 import 调用者模块）。
- 实质上是**第二条腿（第二份运行态）**：`开发文档/项目说明.md:44-45` 明确写「外部入口：`POST /网关/调用`；**MCP只负责把工具请求转到这个入口**」，而现场 MCP 不转发、在自身进程里另装配一套注册表/句柄/包指纹/热接入状态执行能力。两个进程各持一份运行态，句柄与租约互不可见（句柄由 `公共契约/句柄体系.py` + `运行核心/资源协调` 进程内维护）。
- 分级：**重要-7**（实现同一份代码，未见第二套执行语义复制，故不到「阻断」；但与冻结文档声明的外部唯一入口不一致，属两条执行腿）。

---

### 7) 权限与认证：无权限模型、无认证，本机任意进程可调用

**角色权限.py = 已空壳（45 行）**
- `角色权限.py:1-5`「角色体系已整体移除：不再维护角色白名单、角色工具表、实例角色表或角色所属实例。网关直通 + 默认拒绝（未知工具名 / 越界路径 / 未登记子任务仍拒绝，不静默放行）」。
- 仅剩常量与文案：`:11 网关实例名 = "system_engineering_toolkit"`、`:14 网关角色名 = "平台维护者"`、`:26-36 网关说明()`、`:39-45 获取角色指南()`；`越权拒绝` 类定义在 `:17-23`，但 `call_tool` 未知工具走的是 `项目服务.py:965 raise ValueError(f"未知工具：{名称}")`，不是该异常。
- 工具列表不按角色过滤：`项目服务.py:474-476`「单网关模式：全部工具直通注入，不按角色过滤」；`_工具目录` 的 `可调用集 = set(定义表)`（`:579`）。

**服务无认证（已核实）**
- 只监听回环、单路由、无中间件：`项目服务.py:1000-1006 应用 = Starlette(routes=[Route("/mcp/", endpoint=...)])` / `uvicorn.Config(应用, host="127.0.0.1", port=端口)`。
- 源码无任何鉴权关键字：`grep -n "Authorization\|token\|鉴权\|认证\|凭证\|api_key\|Bearer" MCP工具箱/项目服务.py` → **无输出**（exit 1）。
- 实测无凭证可完整调用：
  ```bash
  curl -sS -m 8 -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' \
    -X POST http://127.0.0.1:8766/mcp/ -d '{"jsonrpc":"2.0","id":1,"method":"initialize",...}'
  # → 200 + mcp-session-id: 9d87834930e64dcab88c0cd39896edb5（未提供任何凭证）
  curl ... tools/call role_profile
  # → 200 {"模式":"单网关（无角色）","权限模式":"网关直通 + 默认拒绝（未知工具 / 越界路径）", "可用工具":[...50 项...]}
  ```
- 结论：**本机任意进程（任意用户身份、无凭证）可调用全部 50 个工具**，其中包含写盘（`apply_file_patch`）、git 操作（`workspace`）、杀进程（`test_resource`）、改账本（`verify_and_record`/`mcp_feedback`）、回滚（`rollback_gate`）。唯一真实边界是工具内部自校验（路径 `relative_to`、验证命令白名单），而第 5 节证明这些校验在若干入口缺失。
- 分级：**阻断-3**（无认证 + 无权限模型 + 存在第 5 节的写/杀路径）。

---

### 命令清单（全部真实执行过）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
/Library/Developer/CommandLineTools/usr/bin/git rev-parse HEAD && git branch --show-current
wc -l MCP工具箱/*.py MCP工具箱/*.sh
awk 'NR>=55 && NR<=70' 开发文档/项目说明.md            # 第62行「单一 MCP 入口」
sed -n '1,60p' AGENTS.md ; sed -n '85,170p' AGENTS.md
python3 -c "..."                                        # 源码 Tool(name=...) 计数 → 50
curl -sS -m 8 -D - -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' \
  -X POST http://127.0.0.1:8766/mcp/ -d '{"jsonrpc":"2.0","id":1,"method":"initialize",...}'   # 200 + session-id，无凭证
curl ... {"method":"tools/list"}                        # 真实 tools/list = 50
curl ... {"method":"tools/call","params":{"name":"role_profile"}}   # 无凭证成功返回
python3.14 -c "..."                                      # 工具名映射覆盖 50/50
python3.14 - <<'PY' ...                                  # 复刻 _定位模块目录 → 穿越实证
grep -rn "40007\|网关/调用\|requests\|urllib.request\|httpx" MCP工具箱/*.py MCP工具箱/*.sh
grep -rn "mcp/server\|from mcp import\|Server(" --include=*.py .     # 仅 1 个 MCP Server
grep -n "Authorization\|token\|鉴权\|认证\|凭证\|api_key\|Bearer" MCP工具箱/项目服务.py   # 无输出
grep -n "状态_超时" MCP工具箱/作业系统.py                  # 仅定义+终态集，无赋值
grep -rn "最大超时秒" --include=*.py .                    # 仅定义一处，无引用
grep -rn "释放开工id能力占用\|释放工作包文件租约" --include=*.py .
ps -axo pid=,ppid=,pgid=,command= | grep 8766 ; lsof -nP -iTCP -sTCP:LISTEN | grep -E "8766|40007|8866"
awk 'NR>=303 && NR<=349' MCP工具箱/工作区管理.py
awk 'NR>=75 && NR<=125' 平台控制面/能力目录/服务.py
awk 'NR>=215 && NR<=300' MCP工具箱/测试资源.py
awk 'NR>=60 && NR<=95' MCP工具箱/模块合规.py
ls -la 工程缓存/需求登记/
```

### 跳过项（按冻结边界未做）

- 未重启 / 未停止 8766、40007；未触碰 8866（未运行，未启动）。
- 未实弹触发 `test_resource` 自杀链（会导致服务被杀，违反「禁止重启服务」），仅做进程组事实核实 → 端到端杀伤**未确认**。
- 未执行 `verify_and_record` / `run_release_gate` / 发布门禁（冻结禁止）。
- 未写任何仓库文件、未改运行数据/数据库/制品；`/tmp` 外无写入。
- 未做「组件合规 exec_module 执行任意目录 .py」的端到端实弹（需构造含 包声明.json 的目录，且属执行面）→ **未确认**。

### 未确认项

1. `test_resource` 登记自身 pid → 清理 → 8766 被杀：代码路径与 PGID 事实已核实，端到端杀伤未实弹。
2. `validate_module_compliance` 穿越目录后是否必然走到「加载该目录包声明 入口 并 exec_module」（依赖该目录存在 `包声明.json`）。
3. 能力占用账本（`能力占用` 域）upsert 语义与行数上限（该模块写 SQLite，未逐行读完全部 412 行）。
4. 8766 与 40007 同时对 `底座运行.db` 的并发写锁行为（未做并发写入压测）。
5. 8866 能力网关接口形态（服务未监听，未实测）。
6. `热重载` 在真实并发（事件循环 + 作业线程）下的重载正确性（未做压测）。

---

## 第 5·6 节 · 示例项目 + 技能库

> 本节由专项子审计产出，原文完整保留；数据与证据格式同上（path:行号 + 真实命令 + 真实输出摘录）。

### 子审计 D：示例项目 + 技能库

> 审计对象：系统工程平台（底座）`~/Documents/Agent/PHP/系统工程平台`
> 冻结边界：HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`，分支 `主干`
> 环境：macOS，`python3.14`，跑前 `unset PYTHONPATH`，git 用 `/Library/Developer/CommandLineTools/usr/bin/git`
> 审计方式：只读 + 真实运行（示例入口/能力调用）。全程未改仓库任何文件。

---

### 一句话结论

示例项目侧，5 个示例中 4 个真实跑通且与公开消费者契约逐字一致，但 **AGENTS.md:132 列为正式验收入口的 `示例项目/适配层示例/运行入口/运行.py` 在 HEAD 上真实运行失败（退出码 1），关键输出从未出现**——这是「加守卫时漏改调用方」的同型回归，且平台自己的统一验证器已用 `要求凭证: False` 绕开（说明缺口被承认但示例入口未修）；技术库侧，**技能库是一个真实落地、非孤岛、完整性摘要闭合的正式包，六环是真机制而不是话术**，但它对 AGENTS.md:131 的正式验收入口 `能力搜索器` 不可见（搜索器只传两根，未传第三根）。

---

### 分级计数

| 级别 | 条数 |
|---|---|
| 阻断 | **1** |
| 重要 | **3** |
| 建议 | **9** |

---

### 第一部分：示例项目真实可跑性

### 0. 示例项目清单（去缓存后 25 个跟踪文件，与任务口径一致）

```
真实命令：/Library/Developer/CommandLineTools/usr/bin/git ls-files 示例项目 | wc -l
真实输出：25
```

目录与入口：

| 示例 | 入口 | 真实运行结果 |
|---|---|---|
| 前后端核心示例 | `示例项目/前后端核心示例/运行示例.py` | 退出码 0 ✅ |
| 可双击演示 | `示例项目/可双击演示/开发文件夹/编译.py` | 退出码 0 ✅（输出改指 /tmp） |
| 平台控制面示例 | `示例项目/平台控制面示例/运行示例.py` | 退出码 0 ✅ |
| 稳定平台完整示例 | `示例项目/稳定平台完整示例/稳定平台完整示例.py`（无 `__main__`，类入口） | 退出码 0 ✅ |
| 适配层示例 | `示例项目/适配层示例/运行入口/运行.py` | **退出码 1 ❌** |
| （最小样板）示例项目 | `示例项目/示例项目.py` | 退出码 0 ✅ |

---

### D-01 【阻断】AGENTS.md:132 正式验收入口真实运行失败，关键输出从未出现

**影响**
`AGENTS.md:132` 把 `python3.14 示例项目/适配层示例/运行入口/运行.py` 列为「正式验收入口（唯一标准链路）」之一。在冻结 HEAD 上，该命令**裸跑必然失败**；即使补上文档要求的凭证环境变量，仍在第 [6] 步失败。`开发工具/契约编译/消费者契约.py:168-171` 为该入口声明的「退出码 0 + 关键输出 `适配层示例运行成功` / `[8] 项目适配验证: 通过`」**从未产生过**。即：底座的正式验收链路里有一条入口是恒失败的，任何把它当门禁的流程都是假绿。

**证据（path:行号）**
- `AGENTS.md:132` — `python3.14 示例项目/适配层示例/运行入口/运行.py`（正式验收入口）
- `AGENTS.md:135-136` —「门禁：…发布只有'通过/阻断'两种结果，其余均非零退出码」
- `示例项目/适配层示例/运行入口/运行.py:53` — `提供者网关 = 本地网关服务器(网关核心实例=网关核心(后端), 地址="127.0.0.1", 端口=0)`（**未传 `配置`**）
- `示例项目/适配层示例/运行入口/运行.py:59` — `网关 = 本地网关服务器(…)`（同样未传 `配置`）
- `运行核心/统一网关/本地网关.py:126` — `要求凭证 = bool(self.配置.get("要求凭证", True))`（**默认要求凭证**）
- `运行核心/统一网关/本地网关.py:344` — `self._拒绝(401, "权限不足", "访问凭证缺失或无效")`（与实测报错逐字一致）
- `运行核心/能力调用/HTTP连接器.py:208-215` — 请求头只有 `Content-Type` 与 `X-Request-ID`，**从不发送 `Authorization`**
- `运行核心/统一网关/安全边界.py:30` — 凭证管理器环境变量默认 `系统库网关凭证`
- `开发工具/统一验证/统一验证器.py:331` — `网关配置 = {"请求超时秒": 30, "要求凭证": False, "禁止客户端身份": False}`（**统一验证器用免凭证配置绕开同一坑**）
- `开发文档/未完成事项.md:89` — 该条目自述「加守卫时漏改调用方」并明确「顺带修两处下游：网关默认要求环境变量 `系统库网关凭证` 故传免凭证配置」——**修了验证工具，没修被它当作权威同法的示例入口**
- `开发工具/契约编译/消费者契约.py:168-171` — 声明 `适配层示例` 退出码 0 且关键输出含 `适配层示例运行成功`、`[8] 项目适配验证: 通过`
- `README.md:56` — 同样把它当命令示例列出

**复现命令 + 真实输出（裸跑，按 AGENTS.md:132 原样）**

```bash
cd ~/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH; python3.14 示例项目/适配层示例/运行入口/运行.py; echo "退出码=$?"
```
```
适配层示例运行开始
[1] 绑定支持库: 文件系统、文本处理 ✓
[2] 绑定模块: 文件管理 ✓
[3] 生成依赖锁: 4 个包 ✓
[4] 装配系统: 536 个能力 ✓
[5] 提供者网关启动失败: 缺失凭证: 环境变量 系统库网关凭证 未设置
退出码=1
```

**复现命令 + 真实输出（补文档凭证后，仍失败）**

```bash
unset PYTHONPATH; env '系统库网关凭证=local-verify-123' python3.14 示例项目/适配层示例/运行入口/运行.py; echo "退出码=$?"
```
```
适配层示例运行开始
[1] 绑定支持库: 文件系统、文本处理 ✓
[2] 绑定模块: 文件管理 ✓
[3] 生成依赖锁: 4 个包 ✓
[4] 装配系统: 536 个能力 ✓
[6] 调用失败: 写入=权限不足 访问凭证缺失或无效; 读取=权限不足 访问凭证缺失或无效; 分割=权限不足 访问凭证缺失或无效
退出码=1
```

**已核实**：`[5]` 失败原因 = 网关默认要凭证 + 示例没传 `配置`；`[6]` 失败原因 = HTTP连接器不发 Authorization + 网关默认要凭证。未修 `配置` 时第 [6] 步永远过不去。**未确认**：是否有仓库外的 shell 包装/CI 在跑之前 export 免凭证配置（本次只按 AGENTS.md:132 原样跑）。

---

### D-02 【重要】编译/编辑器产物 `编译制品.json` 入库，且与页面源不一致

**影响**
违反 `AGENTS.md:162`「工程缓存可删除、可重新生成，不作为正式源代码」与「制品不入源码库」。且该产物与同目录页面源互相矛盾，任何一个读它的人都会拿到错的页面事实。

**证据（path:行号）**
- 入库文件：`示例项目/可双击演示/开发文件夹/前端/页面/编译制品.json`（`git ls-files` 命中）
- 生成方：`开发工具/轻代码前端编辑器/启动编辑器.py:344` — `输出文件 = 文件.parent / "编译制品.json"`；`开发工具/项目编译/项目编译器.py:690` — `(编译页面目录 / "编译制品.json").write_text(…)`
- **编译器自己把它排除在源码之外**：`开发工具/项目编译/项目编译器.py:262`、`:669` — `路径.name not in {"编译制品.json", "编辑记录.json"}`
- 内容矛盾（实测）：
  - `编译制品.json`：`修订号 24`、`标题 "新页面"`、`项目类型 "网页项目"`、`组件列表 []`
  - `前端/页面/主页.json`：`修订号 0`、`标题 "底座演示：文本工具"`、`项目类型 "软件项目"`、3 个组件

**复现命令**
```bash
git ls-files 示例项目/可双击演示/开发文件夹/前端/页面/
cat 示例项目/可双击演示/开发文件夹/前端/页面/编译制品.json
cat 示例项目/可双击演示/开发文件夹/前端/页面/主页.json
```
**真实输出摘录**
```
示例项目/可双击演示/开发文件夹/前端/页面/主页.json
示例项目/可双击演示/开发文件夹/前端/页面/编译制品.json
示例项目/可双击演示/开发文件夹/前端/页面/编辑记录.jsonl
```
```json
{"制品类型": "轻代码前端制品", "契约版本": "1.0.0",
 "页面": {"工程id": "工程_ecd17824092045a8", "修订号": 24, "页面id": "主页",
          "标题": "新页面", "项目类型": "网页项目", "组件列表": []},
 "能力依赖": [], "编译器": "轻代码前端编译器.1.0.0"}
```

**已核实**：文件在 git 跟踪内，内容是编辑器/编译器的产出，且被编译器按「非源码」排除。

---

### D-03 【重要】运行数据 `编辑记录.jsonl` 入库（29 条编辑会话日志，15 KB）

**影响**
运行数据进入正式源码库，与 `AGENTS.md:162` 冲突；同目录同类文件已被编译器显式排除，说明平台自己认定它不是源码。

**证据（path:行号）**
- 入库文件：`示例项目/可双击演示/开发文件夹/前端/页面/编辑记录.jsonl`（15276 字节，29 行，时间戳全部 `2026-08-22T16:56`–`17:11`）
- 生成方：`开发工具/轻代码前端编辑器/启动编辑器.py:145`、`:357` — `日志 = 文件.parent / "编辑记录.jsonl"`
- 排除方：`开发工具/项目编译/项目编译器.py:262`、`:669`

**复现命令**
```bash
ls -l 示例项目/可双击演示/开发文件夹/前端/页面/编辑记录.jsonl
head -2 示例项目/可双击演示/开发文件夹/前端/页面/编辑记录.jsonl
```
**真实输出摘录**
```
-rw-r--r--@ 1 用户  staff  15276 Aug 22 17:11 示例项目/可双击演示/开发文件夹/前端/页面/编辑记录.jsonl
{"请求id": "请求_494ae463-…", "操作id": "请求_494ae463-…", "来源": "模块视图", "工程id": "工程_ecd17824092045a8",
 "基准修订号": 0, "新修订号": 1, "提交结果": "成功", "时间": "2026-08-22T16:56:21"}
```

**已核实**：git 跟踪、时间戳集中在同一天的一次交互式编辑会话。

---

### D-04 【建议】`示例项目/可双击演示/产物文件夹/` 空目录存在，且未被 .gitignore 覆盖

**影响**
`产物文件夹/` 是编译输出目录，当前为空（空目录 git 不跟踪，故未入库）。但 `.gitignore` 无 `产物文件夹` 条目，任何一次按 `项目说明.md:272` 的命令编译，产物就会直接落进工作树，污染 `git status`。

**证据（path:行号）**
- `示例项目/可双击演示/产物文件夹/`（空，`ls -la` 只有 `.`/`..`）
- `.gitignore` 全文含 `工程缓存/`、`__pycache__/`、`*.pyc`，**无 `产物文件夹`**
- `示例项目/可双击演示/开发文件夹/项目声明.json` — `"编译输出目录": "../产物文件夹/20260822-demo"`（指向仓库内）
- `开发工具/轻代码前端编辑器/…` 与 `项目说明.md:270-273` 的正式命令 `--输出 示例项目/可双击演示/产物文件夹/20260822-demo`（**输出到仓库内**）

**复现命令**
```bash
ls -la 示例项目/可双击演示/产物文件夹/
cat .gitignore
```
**真实输出摘录**
```
total 0
drwxr-xr-x@ 2 用户  staff   64 Sep 15 13:05 .
drwxr-xr-x@ 6 用户  staff  192 Sep 15 13:14 ..
```
```
### 工程缓存可重新生成，不作为正式源代码
工程缓存/
```
（无 `产物文件夹` / `产物` 条目）

**已核实**：目录现在为空、未被跟踪、不在 .gitignore。

---

### D-05 【建议】`适配层示例/项目资源/*.txt` 三个 txt 是运行/人工遗留脏文件

**影响**
`示例项目/适配层示例/项目资源/` 被入库的 3 个 txt：
- `示例输出.txt`：**由入口自己每次运行重写**（`运行.py:75-77`），内容恒为 `适配层示例内容` → 纯运行输出，不应入库。
- `入口测试.txt`（内容 `入口测试`）、`验证输出.txt`（内容 `适配验证输出`）：**全仓无任何代码/配置引用**（grep 零命中）→ 人工调试遗留。

**证据（path:行号）**
- `示例项目/适配层示例/运行入口/运行.py:74-77`：
  ```python
  资源 = 项目根 / "项目资源"
  资源.mkdir(parents=True, exist_ok=True)
  路径 = str(资源 / "示例输出.txt")
  写入 = 入口.调用("写入文件", {"文件路径": 路径, "内容": "适配层示例内容"})
  ```
- 三个 txt 的入库状态与引用：`git ls-files 示例项目/适配层示例/项目资源/` 命中 3 个；grep `入口测试.txt` / `验证输出.txt` 在 `示例项目/` 外与内均零命中

**复现命令**
```bash
for f in 入口测试.txt 示例输出.txt 验证输出.txt; do
  echo "--- $f ---"; ls -l "示例项目/适配层示例/项目资源/$f"
  shasum -a 256 "示例项目/适配层示例/项目资源/$f"; cat "示例项目/适配层示例/项目资源/$f"; echo; done
grep -rn "入口测试.txt\|验证输出.txt" --include=*.py --include=*.json . | grep -v __pycache__ | grep -v 工程缓存
```
**真实输出摘录**
```
--- 入口测试.txt ---
-rw-r--r--@ 1 用户  staff  12 Aug 28 20:23 …/入口测试.txt
da5be550455a7fcf31a03aec0dfdd275faeb3d4bdca64126d91015b138bbb51d  …/入口测试.txt
入口测试
--- 示例输出.txt ---
-rw-r--r--@ 1 用户  staff  21 Aug 29 20:01 …/示例输出.txt
f50e5ea79988ed8116e06b69b5a90dfdd17563415c24112583398df1d619f441  …/示例输出.txt
适配层示例内容
--- 验证输出.txt ---
-rw-r--r--@ 1 用户  staff  19 Sep 13 13:03 …/验证输出.txt
513d0b60c4b0c1433385ca3079dfd0e1dd9fd8fb24d6d9d0073ab4f0a4b5b49b  …/验证输出.txt
适配验证输出
```
（`grep 入口测试.txt / 验证输出.txt` 输出为空 = 无引用）

**已核实**：`示例输出.txt` 内容由运行写入；另外两个无引用。**另**：本次真实运行（裸跑）在第 [5] 步即失败，**没有执行到写盘步骤**，运行前后 `示例输出.txt` 哈希一致（`f50e5ea7…f441`），仓库零改动。

---

### D-06 【建议】`编译.py` 默认输出指向仓库内 `产物文件夹`

**影响**
不带 `--输出` 直接跑 `编译.py` 会把整套编译产物写进仓库工作树。

**证据（path:行号）**
- `示例项目/可双击演示/开发文件夹/编译.py:16-19`
  ```python
  解析器.add_argument("--输出", type=Path, default=None)
  …
  输出 = 参数.输出 or 开发目录.parent / "产物文件夹" / f"{datetime.now():%Y%m%d}-{参数.名称}"
  ```
- `开发文档/项目说明.md:270-273` 的正式命令显式传 `--输出 示例项目/可双击演示/产物文件夹/20260822-demo`（同样落在仓库内）

**复现命令（本次为不改仓库，输出改指 /tmp）**
```bash
cd ~/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B \
  示例项目/可双击演示/开发文件夹/编译.py --输出 /tmp/子审计D_编译产物/20260822-demo
```
**真实输出摘录**
```
编译完成：/private/tmp/子审计D_编译产物/20260822-demo
支持库 3 个，模块 1 个
退出码=0
```
产物含 `编译清单.json`、`制品来源.json`、`制品完整性摘要.json`、`运行入口/启动.py`（与 `开发文档/项目说明.md:275` 的四项要求一致，已核实）。
**运行后 `git status --porcelain` 仅 `?? 开发文档/临时文档/`（审计前既存），仓库零改动。**

**已核实**：编译链真实可用；默认输出目录在仓库内（结构性问题，非功能故障）。

---

### 第一部分 · 通过项（已核实，非问题）

##### P1. 四个示例真实跑通，关键输出与公开消费者契约逐字一致

对照 `开发工具/契约编译/消费者契约.py:167-183` 声明的「退出码 / 关键输出」：

**前后端核心示例**
```bash
unset PYTHONPATH; python3.14 示例项目/前后端核心示例/运行示例.py; echo "退出码=$?"
```
```
前后端核心示例
[4] HTML 页面宿主: 成功=True
[8] HTML 调用边界: 统一网关=True
本地页面宿主已优雅关闭
退出码=0
```
（4 项关键输出全中；端口 45081 运行前实测未被占用）

**平台控制面示例**
```bash
unset PYTHONPATH; python3.14 示例项目/平台控制面示例/运行示例.py; echo "退出码=$?"
```
```
平台控制面完整闭环示例通过 ✓
[13] 统一入口调用: 成功=True 真实返回值=4
退出码=0
```
（2 项关键输出全中；写盘仅在 `tempfile.mkdtemp`，`消费者契约.py:163` 声明的「不触碰共享工程缓存」成立）

**示例项目（最小样板）**
```bash
unset PYTHONPATH; python3.14 示例项目/示例项目.py; echo "退出码=$?"
```
```
[1] 装配成功: 105 个包, 479 个能力
[2] 搜索 读取文件 → ['文件系统支持库.文件操作.读取文件', '文件系统支持库.文件操作.读取文件头部字节', '文件管理.读取文件', '文件管理.读取文件头部字节']
[3] 经注册表调用 文件系统支持库.文件操作.写入文件 成功
[4] 经公开入口调用 文件系统支持库.文件操作.读取文件 成功
[5] 模块组合调用 文档读取.结构化读取 成功（标题=示例标题）
示例项目运行成功
退出码=0
```
（写盘只在 `工程缓存/`（.gitignore 覆盖），运行后 `git status` 无变化）

**稳定平台完整示例**
该文件无 `if __name__ == "__main__"`（`示例项目/稳定平台完整示例/稳定平台完整示例.py` 全文 69 行，只有类定义），入口是类；平台内唯一调用方是 `开发工具/发布门禁/第十四阶段门禁.py:276-278`（发布门禁，本次禁跑）。按同法进程内调用：

```bash
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "
import sys; sys.path.insert(0, '.')
from 示例项目.稳定平台完整示例.稳定平台完整示例 import 稳定平台完整示例
结果 = 稳定平台完整示例().执行()
print('调用结果 =', 结果['调用结果'])
print('升级后指针状态 =', 结果['升级后指针'].get('状态'))
print('回滚后指针状态 =', 结果['回滚后指针'].get('状态'))
print('证据条数 =', len(结果['证据']))
"
```
```
调用结果 = {'字数': 7, '行数': 2}
升级后指针状态 = 激活
回滚后指针状态 = 回滚
证据条数 = 12
退出码=0
```
（写盘只在 `tempfile.mkdtemp(prefix="工作包20_")` → /tmp；满足 `第十四阶段门禁.py:282-284` 的四项判定）

**⚠ 未确认为「正式入口」**：该示例没有任何 `__main__`/脚本级入口，`AGENTS.md:123-133` 的五条正式验收入口不含它。它只能经发布门禁或进程内调用，属「非独立可跑示例」。

##### P2. 契约一致性：没有示例 import 实现目录或支持库内部（铁律未破）

全量 import 清单（`grep -rh "^from \|^import " 示例项目/ --include=*.py | sort -u`，去重后）：
```
from 支持库.后端.文件系统支持库.文件操作 import 读取文件, 写入文件     ← 公开包入口，合规
from 模块库.文件管理 import 设置HTTP连接器                              ← 公开包入口，合规
from 模块库.文档读取 import 结构化读取                                  ← 公开包入口，合规
from 前端核心.前端核心 import 前端核心, 页面定义, 窗口定义
from 后端核心.后端核心 import 后端核心
from 平台控制面.授权 import 发布者 / 组件开发Agent
from 平台控制面.统一入口 import 统一能力服务
from 开发工具.轻代码前端编辑器.启动编辑器 import 主函数
from 开发工具.项目编译.项目编译器 import 编译项目
from 支持库.前端.浏览器宿主 import 启动网页服务
from 支持库.适配层 import 生成密钥对
from 运行核心.加载器.生命周期管理.管理器 import 装配系统
from 运行核心.加载器.包发现.发现器 import 发现全部
from 运行核心.统一网关.本地网关 import 本地网关服务器
from 运行核心.统一网关.网关核心 import 网关核心
from 运行核心.能力调用.HTTP连接器 import HTTP连接器                ← 唯一 HTTP 门面
from 项目适配层.* import …
public stdlib: argparse / datetime / json / sys / tempfile / typing / urllib.request / pathlib
```
**结论**：`AGENTS.md:159-160` 禁止的 `from <包>.实现.<模块> import …` 形式在 `示例项目/` **零命中**；能力调用走 `项目入口.调用` / `HTTP连接器` / `统一能力服务.执行操作` / `注册表.获取`（唯一入口），未出现示例绕过能力进程直调实现。

##### P3. 契约能力 id 存在性（只读验证）

`示例项目/示例项目.py:29` 调用 `文件系统支持库.文件操作.写入文件`（能力搜索器对 `读取文件` 实测命中同一包 2 项，写入口径同源）；
`示例项目/可双击演示/开发文件夹/前端/页面/主页.json` 里声明的能力 id 为 `数据操作支持库.文本处理.统计长度`，与 `示例项目/适配层示例/项目声明.json:7` 绑定的 `支持库.后端.数据操作支持库.文本处理` 同族（该包在 `示例项目/适配层示例/运行入口/运行.py:31` 亦被绑定并校验通过 —— 见裸跑输出 `[1] 绑定支持库: 文件系统、文本处理 ✓`）。
`示例项目/前后端核心示例/运行示例.py:19` 声明的 `数据操作支持库.文本处理.分割文本` 与 `运行.py:79` 实际调用的 `分割文本` 同名（因 D-01，该步在本 HEAD 上未能执行到，**该能力 id 的端到端调用未确认**）。

##### P4. 无业务项目引用

```bash
grep -rn "华世王镞\|俏小喵\|5432\|V3\|功能模块/\|后端服务/\|应用软件/\|psycopg\|postgres\|数据库" 示例项目/ | grep -v __pycache__
```
**真实输出：空（零命中）**。
`开发工具/统一验证/统一验证器.py:430` 正是用同一条 grep 校验 `示例项目/适配层示例` 与 `项目适配层`——本审计独立复现，结论一致：示例项目不引用业务项目。

---

### 第二部分：技能库（第三根正式包根）真实质量

### 0. 结构清单与必备项对照

`技能库/后端/技能库/` 跟踪文件 30 个（`git ls-files 技能库 | wc -l` → `30`），实体 32 个（多 2 个 `__pycache__/*.pyc`）。

对照 `AGENTS.md:204-218`「每个支持库必备」：

| 必备项（AGENTS.md:206-212） | 实际 | 判定 |
|---|---|---|
| `包声明.json` | ✅ `技能库/后端/技能库/包声明.json` | 齐 |
| `能力契约/` | ✅ `能力契约/参数契约.json`（契约版本 2.0.0，4 条） | 齐 |
| `实现/` | ✅ `实现/技能库.py`（570 行） | 齐 |
| `说明/` | ✅ `说明/使用说明.md` + `说明/设计说明.md` | 齐 |
| `验证场景引用.json` | ✅ | 齐 |
| `完整性摘要.json` | ✅ 29 条文件清单 | 齐 |
| `__init__.py` | ✅ 含 `注册能力(注册表)` | 齐 |

额外具备：`复用决策.json`、`权限契约/权限契约.json`、`配置契约/配置契约.json`、`能力定义.json`、`能力数据/Agent查询数据.json`、`能力数据/能力搜索数据.json`、`资源预算.json`、`验证场景.json`、`验证夹具/技能工作区/`。

对照 `开发文档/项目说明.md:305`（技能库与支持库/模块库平级、包声明类型同为「支持库」、不另开 HTTP 入口、走底座唯一执行入口）：
- 包声明类型 ✅ `技能库/后端/技能库/包声明.json` → `"类型": "支持库"`
- 与支持库/模块库平级 ✅（顶层 `技能库/`）
- 不另开 HTTP 入口 ✅ `技能库/后端/技能库/__init__.py:1-5` 明写「技能是给 Agent 消费的可复用工作流，本身不另开 HTTP 入口，经底座唯一执行入口 `POST /网关/调用` 调用」；`说明/设计说明.md:21` 同口径；`说明/设计说明.md:37` 走 `40007/网关/调用`
- 未违反「包级公开入口只负责能力注册与装配」（`项目说明.md:308`）✅ `__init__.py` 只导出 4 个函数 + `注册能力`

---

### D-07 【重要】技能库 4 个能力对 `AGENTS.md:131` 正式验收入口「能力搜索器」不可见

**影响**
`AGENTS.md:131` 把 `开发工具/能力搜索/能力搜索器.py --能力 <词>` 列为正式验收入口；`开发文档/项目说明.md:14`、`开发文档/未完成事项.md:34` 均声称技能库「已作为第三根正式包根进入索引」。实测：**搜索器看不到技能库的任何能力**，4 个能力 id / 名称 / 说明全部零命中，退出码 1。底座自己的 Agent 能力发现链路（搜索器）与装配链路（后端核心）对技能库的认知不一致。

**证据（path:行号）**
- `AGENTS.md:131` — 能力搜索器为正式验收入口
- `开发工具/能力搜索/能力搜索器.py:286` — `发现 = 发现全部(系统根 / "支持库", 系统根 / "模块库")`（**只传两根，未传 `技能库根目录`**）
- `开发工具/能力搜索/能力搜索器.py:258` — 同一缺口在 `搜索公开能力()` 里复现
- `开发工具/能力搜索/能力搜索器.py:79` — `for 根目录 in (系统根 / "支持库", 系统根 / "模块库")`（`加载聚合契约索引` 同样只扫两根 → 技能库的 `能力契约/参数契约.json` 永不加载）
- 对照：`运行核心/加载器/包发现/发现器.py:59-72` 支持第三根；`后端核心/后端核心.py:109,238,274` 三处都传 `self.系统根目录 / "技能库"`；`开发工具/统一验证/统一验证器.py:31`、`开发工具/公开调用完整性门禁.py:22`、`开发工具/组件规范/完整性摘要.py:159`、`开发工具/项目编译/正式包索引.py:88` **都传了第三根**
- `开发文档/未完成事项.md:95` 自述本次把「认得它」在底座全链路做齐（依赖锁定/发布治理/统一验证器/完整性摘要/公开调用完整性门禁/客户端顶层包表/测试镜像）——**遗漏了能力搜索器**

**复现命令 + 真实输出**
```bash
cd ~/Documents/Agent/PHP/系统工程平台
for w in 技能 技能包 技能库 校验技能包 运行技能包 生成索引 扫描技能包; do
  echo "---- --能力 $w ----"
  unset PYTHONPATH; python3.14 开发工具/能力搜索/能力搜索器.py --能力 "$w" 2>&1 | head -3
done
```
```
---- --能力 技能 ----      未找到能力: 技能
---- --能力 技能包 ----    未找到能力: 技能包
---- --能力 技能库 ----    未找到能力: 技能库
---- --能力 校验技能包 ---- 未找到能力: 校验技能包
---- --能力 运行技能包 ---- 未找到能力: 运行技能包
---- --能力 生成索引 ----  未找到能力: 生成索引
---- --能力 扫描技能包 ---- 未找到能力: 扫描技能包
```
对照（同命令、同会话）：
```
---- --能力 读取文件 ----
找到 7 个能力实现：
  - 能力id: 文件系统支持库.文件操作.读取文件
```
直接验证发现器口径：
```bash
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "
import sys, pathlib; sys.path.insert(0,'.')
from 运行核心.加载器.包发现.发现器 import 发现全部
r  = 发现全部(pathlib.Path('支持库'), pathlib.Path('模块库'))
r2 = 发现全部(pathlib.Path('支持库'), pathlib.Path('模块库'), pathlib.Path('技能库'))
print('不传第三根 → 包数 =', len(r.声明列表))
print('传第三根   → 包数 =', len(r2.声明列表))
print('技能库包在结果里?', any(d.包id=='技能库.后端.技能库' for d in r2.声明列表))
print('不传第三根时在结果里?', any(d.包id=='技能库.后端.技能库' for d in r.声明列表))
"
```
```
不传第三根 → 包数 = 105
传第三根   → 包数 = 106
技能库包在结果里? True
不传第三根时在结果里? False
```

**已核实**：搜索器不传第三根是唯一原因；技能库确实在 `正式包索引` 中（见 P6）。**孤岛判定：不是孤岛包**——技能库被 `后端核心` 装配、被 `发现器`/`统一验证器`/`公开调用完整性门禁`/`完整性摘要扫描`/`正式包索引`/`客户端顶层包表`/`测试中心`（≥6 处）真实认它，仅在 `能力搜索器` 这一条正式验收入口上缺失。

---

### D-08 【建议】技能库顶层与 `后端/` 缺 `__init__.py`，与另外两根包根结构不一致

**证据**
```bash
for d in 支持库 模块库 技能库 技能库/后端; do echo -n "$d/__init__.py: "; [ -f "$d/__init__.py" ] && echo 存在 || echo 缺失; done
```
```
支持库/__init__.py: 存在
模块库/__init__.py: 存在
技能库/__init__.py: 缺失
技能库/后端/__init__.py: 缺失
```
对照：`客户端/构建平台客户端.py:30` 把 `技能库` 与 `支持库`/`模块库` 并列进 `顶层包表`（口径上视作同等顶层包）。

**影响**：靠 Python 3 隐式命名空间包侥幸可用（实测导入成功，见下），但三根正式包根结构不齐，且不显式声明顶层包身份对后续打包/重写/命名空间工具是隐患。
**复现 + 真实输出（证明当前可用，故只列建议）**
```bash
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "
import sys; sys.path.insert(0,'.')
import 技能库.后端.技能库 as m
print('导入成功, __all__ =', m.__all__)
print('模块文件 =', m.__file__)
import sys as s; print('技能库 有 __file__?', getattr(s.modules['技能库'],'__file__','无(命名空间包)'))
"
```
```
导入成功, __all__ = ['校验技能包', '运行技能包', '扫描技能包', '生成技能索引']
模块文件 = …/系统工程平台/技能库/后端/技能库/__init__.py
技能库 有 __file__? 无(命名空间包)
```
**已核实**：缺文件属实，导入仍可用。

---

### D-09 【建议】`资源预算.json` 声明 `子进程上限: 0`，而「运行技能包」的核心动作就是起子进程

**证据（path:行号）**
- `技能库/后端/技能库/资源预算.json` — `"子进程上限": 0`
- `技能库/后端/技能库/实现/技能库.py:288-297` — `进程 = subprocess.Popen([固定解释器, str(脚本路径)], …, start_new_session=True, preexec_fn=设置资源预算(预算))`
- `技能库/后端/技能库/能力定义.json` — `运行技能包` 的 `行为.资源释放 = "子进程进程组回收"`、`副作用 = "启动受控子进程（技能脚本，可写文件）"`
- 该声明不是死文件：`后端核心/后端核心.py:701` — `返回正文["资源预算"] = self._读取JSON(包目录 / "资源预算.json")`
- 且是必需键集合之一：`平台控制面/资源监督.py:32` — `必需预算键 = ("内存上限", "线程上限", "子进程上限", "并发调用上限", …)`；`平台控制面/策略中心.py:91` 同

**影响**：同一份预算里 `子进程上限=0` 与该包唯一有副作用的能力的真实语义直接冲突。若下游按该数字做硬限制，`运行技能包` 将被恒判超限；若下游不按它限制，则该字段在技能库上是误导性声明。
**已核实**：字段值与实现行为矛盾；**未确认**：是否真有下游把它当硬限制执行（`资源监督.注册执行单元` 只校验键齐全、只用 `线程上限`/`并发调用上限` 建池，见 `平台控制面/资源监督.py:36-56`，未见对 `子进程上限` 的强制）。据此定级为「建议」而非「重要」。

---

### D-10 【建议】`复用决策.json` 是缺「决策」字段的残件

**证据**
```bash
cat 技能库/后端/技能库/复用决策.json
cat 模块库/文件管理/复用决策.json
cat 支持库/后端/文件系统支持库/文件操作/复用决策.json
```
```
### 技能库
{"搜索词": ["校验技能包","运行技能包","扫描技能包","生成索引"],
 "候选能力id": ["技能库.技能包.校验技能包","技能库.受控执行.运行技能包","技能库.技能索引.扫描技能包","技能库.技能索引.生成索引"]}

### 对照 模块库/文件管理 —— 有 决策 字段
{"搜索词": ["文件","读取文件",…],
 "候选能力id": ["文件系统支持库.文件操作.读取文件",…],
 "决策": "复用文件系统与资源管理支持库公开能力，模块只做参数装配与结果组合，不重复实现文件原子操作"}
```
**影响**：`AGENTS.md:165-166` 把「复用决策」列为开工前置且「缺少任一项不得开工」。技能库的候选集就是它自己（同 id 自证复用），没有说明「为什么新建而不是复用既有能力」「与 V3 旧实现的取舍」。对照包能给出决策结论，技能库不能。
**已核实**：字段缺失属实（同类对照文件均带 `决策`）。

---

### D-11 【建议】4 个能力的能力契约 `调用示例` 全部缺必填参数

**证据（path:行号 + 真实输出）**
```bash
python3 -c "
import json
d = json.load(open('技能库/后端/技能库/能力契约/参数契约.json'))
for c in d['能力契约']:
    print(c['能力id'], '→', json.dumps(c.get('调用示例'), ensure_ascii=False))
"
```
```
技能库.技能包.校验技能包   → {"能力id": "技能库.技能包.校验技能包", "参数": {}}
技能库.受控执行.运行技能包 → {"能力id": "技能库.受控执行.运行技能包", "参数": {"超时秒": 30, "输出上限": 1048576}}
技能库.技能索引.扫描技能包 → {"能力id": "技能库.技能索引.扫描技能包", "参数": {}}
技能库.技能索引.生成索引   → {"能力id": "技能库.技能索引.生成索引", "参数": {"写回索引": true}}
```
四条全部缺 `技能根目录`（必填）；`运行技能包` 还缺 `能力标识`（必填），只给了两个可选参数。
下游行为（`开发工具/能力搜索/能力搜索器.py:134-144`）只对「完全没给示例」做默认值回退，**给了空示例就原样渲染**，会产出 `技能库.技能包.校验技能包()` 这种不可执行的示例，与 `能力搜索器.py:6` 的自述口径「不出现 无调用示例」冲突。
**已核实**：四条示例的内容如上（逐字来自文件）。**未确认**：`能力搜索器` 是否会在技能库可见后（修 D-07）触发该渲染缺陷的可见后果——当前因 D-07 尚不可见，属潜在缺陷。

---

### D-12 【建议】技能库内部文档/契约三处版本与数值漂移

| 位置 | 值 |
|---|---|
| `包声明.json` | `"版本": "1.0.2"` |
| `能力定义.json` | `"版本": "1.0.2"` |
| `完整性摘要.json` | `"版本": "1.0.2"` |
| `说明/使用说明.md:4` | `- 版本：1.0.1` ← **不一致** |

| 位置 | 值 |
|---|---|
| `能力契约/参数契约.json` / `能力定义.json` | `输出上限` 默认 `1048576` |
| `配置契约/配置契约.json` | `"默认最大输出字节": 8388608` ← **不一致（1 MiB vs 8 MiB）** |

| 位置 | 值 |
|---|---|
| `说明/设计说明.md:52` | `"能力标识":"_示范.纯文本统计"` |
| `验证夹具/技能工作区/索引.json` + `示范技能/契约.json` | `示范.纯文本统计`（**无下划线**）← **不一致** |

而 `说明/设计说明.md:5` 自述「两份说明都计入包指纹，任何改动都要升包版本」。
**已核实**：以上均为逐字读取的真实内容。

---

### D-13 【建议】底座正式包的验证场景里写死了业务品牌词

**证据（path:行号 + 真实内容）**
- `技能库/后端/技能库/验证场景.json` —「运行示范技能」步骤：
  ```json
  "参数": {"文本": "华世王镞 护肤"}
  ```
- 对照 `开发文档/未完成事项.md:61`：底座「已彻底去掉行业写死内容（9 个模式提示词零行业词，已断言）」
- 独立复查（本审计真实运行）：`grep -rn "华世王镞" 示例项目/` **零命中**；技能库这份 `验证场景.json` 是全仓**能力包内**唯一的品牌词落点（其余命中全在 `开发文档/` 叙述与外部源码研究里）

**影响**：底座应「不认识任何具体项目」（`技能库/后端/技能库/说明/设计说明.md:59` 自述）；把业务品牌当示例参数写进正式能力包的验证场景，与这条自述及 `AGENTS.md:151-152` 的边界精神不一致（属语义泄漏，不影响功能）。
**另**：同场景文件 `验证场景.json:107` 用 `禁止导入前缀: ["功能模块"]`，`验证夹具/技能工作区/越权技能/scripts/读项目.py:6` 写 `from 功能模块.订单 import 查询订单`——`功能模块.订单` 在本仓**不存在**，作为「越权反面夹具」是刻意设计（预期被 AST 审计拦下），**已核实**（见 P5），不计为问题。
**已核实**：内容逐字属实。

---

### 第二部分 · 通过项（已核实，非问题）

##### P5. 「六环」是真实机制，不是文档话术

**代码落点（path:行号）**
- 六件套常量：`技能库/后端/技能库/实现/技能库.py:44` — `技能包必需文件 = ("SKILL.md", "契约.json", "工作流.json", "依赖.json", "验证声明.json")`（+ `scripts/` 目录 → `:214-215`，共六环）
- 校验函数：`实现/技能库.py:208-216`（`校验技能包结构`）
- 强制执行点：`实现/技能库.py:429-431`（`运行技能包` 结构不完整即 `结构不完整` 返回，不进沙箱）
- 数据口径一致：`能力定义.json` / `包声明.json` / `能力契约/参数契约.json` 三处说明均写「六件套结构（SKILL.md/契约.json/工作流.json/依赖.json/验证声明.json/scripts）」；`说明/设计说明.md:9-19` 给出同一份目录结构图

**真实运行证据（逐条对照 `验证场景.json` 断言）**

```bash
cd ~/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "
import sys; sys.path.insert(0,'.')
from 技能库.后端.技能库 import 校验技能包, 运行技能包, 扫描技能包, 生成技能索引
根='技能库/后端/技能库/验证夹具/技能工作区'
print('1', 校验技能包(根,'示范.纯文本统计').值)
print('2', 校验技能包(根,'残缺技能').值)
print('3', 扫描技能包(根).值)
print('4', 生成技能索引(根, 写回索引=False).值)
print('5', 运行技能包(根,'示范.纯文本统计',{'文本':'华世王镞 护肤'}).值)
r=运行技能包(根,'越权技能.读项目',{'订单号':'A1001'}, 禁止导入前缀=['功能模块'])
print('6', r.成功, r.错误码)
"
```
真实输出（`验证场景.json` 期望值 → 实测）：
```
1 期望 {通过:true, 来源:"索引"} → 实测 {'通过': True, '技能目录': '…/示范技能', '来源': '索引', '缺失': []}   ✅
2 期望 {通过:false, 来源:"路径", 包含 SKILL.md} → 实测 {'通过': False, '来源': '路径',
     '缺失': ['SKILL.md','工作流.json','依赖.json','验证声明.json','scripts/']}                      ✅
3 期望 {数量:2} → 实测 {'数量': 2, 技能列表: 示范技能(完整) + 越权技能(完整)}                          ✅
4 期望 {数量:2, 重复标识:[]} → 实测 {'数量': 2, '跳过': [{'路径':'残缺技能','原因':'契约缺少能力标识'}],
     '重复标识': []}                                                                                 ✅
5 期望 {字符数:7, 词数:2} → 实测 {'字符数': 7, '词数': 2, '耗时秒': 0.025}                             ✅
6 期望 错误码 脚本审计未通过 → 实测 False 脚本审计未通过（越权 import 在沙箱前被拦）                     ✅
```
**结论**：六环（六件套结构）在代码里是被真实校验、真实拦截的机制，不是文档话术。4 个能力真实可调、行为与声明一致。

##### P6. 验证夹具被 `验证场景.json` 真实引用（不是摆设）；完整性摘要双向闭合

**夹具引用链（已核实）**
- `技能库/后端/技能库/验证场景引用.json` → `[{"场景文件": "验证场景.json"}]`
- `技能库/后端/技能库/验证场景.json` 两个场景共 10 个步骤，全部指向 `验证夹具/技能工作区`：
  - 场景 `技能库.技能包与索引正向链`（5 步）：`示范技能`（标识 `示范.纯文本统计`）×2、`残缺技能`（路径回退）×1、`扫描技能包`/`生成索引` 各 1
  - 场景 `技能库.受控执行负向与导入审计`（5 步）：`越权技能`（`越权技能.读项目` + `禁止导入前缀:["功能模块"]`）、`不存在的技能`、空 `能力标识`、空 `技能标识或路径`、不存在的技能根
- 三个夹具目录全部被引用：`示范技能/`（正向）、`越权技能/`（负向·审计拦截）、`残缺技能/`（负向·结构缺失）。**无摆设夹具。**
- 夹具非空壳（逐字核实）：`示范技能/` 有 `SKILL.md`/`契约.json`/`工作流.json`/`依赖.json`/`验证声明.json`/`scripts/统计.py` 六件套齐全；`残缺技能/` 只有 `契约.json`（内容 `{}`）→ 缺 5 项；`越权技能/` 六件套齐全但入口 `scripts/读项目.py:6` 刻意 `from 功能模块.订单 import 查询订单`
- 夹具 `索引.json` 只登记 2 个（`示范.纯文本统计`、`越权技能.读项目`），与「扫描/生成索引 = 2」的断言自洽；`残缺技能` 未登记，故走「路径回退」分支（实测 `来源: 路径`）
- 场景用 `{"$动态": "制品根", "相对路径": "技能库/后端/技能库/验证夹具/技能工作区"}`，执行方为 `开发工具/HTML验证/`（`场景加载.py`/`场景执行器.py`/`多步场景.py`），走正式 HTTP 黑盒

**完整性摘要闭合复算（双向）**
```bash
cat > /tmp/子审计D_摘要闭合.py <<'EOF'
import hashlib, json
from pathlib import Path
包目录 = Path("技能库/后端/技能库")
清单 = json.loads((包目录/"完整性摘要.json").read_text(encoding="utf-8"))["文件清单"]
不匹配=[p for p in 清单 if (包目录/p["路径"]).is_file()
        and hashlib.sha256((包目录/p["路径"]).read_bytes()).hexdigest()!=p["sha256"]]
缺失=[p["路径"] for p in 清单 if not (包目录/p["路径"]).is_file()]
已登记={p["路径"] for p in 清单}
实体={p.relative_to(包目录).as_posix() for p in 包目录.rglob("*")
      if p.is_file() and "__pycache__" not in p.parts and p.name!="完整性摘要.json"}
print("清单条数 =", len(清单), "| 实体文件数 =", len(实体))
print("哈希不匹配 =", [p["路径"] for p in 不匹配])
print("清单指向但不存在 =", 缺失)
print("实体存在但未登记 =", sorted(实体-已登记))
print("闭合 =", not(不匹配 or 缺失 or (实体-已登记)))
EOF
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B /tmp/子审计D_摘要闭合.py
```
```
清单条数 = 29 | 实体文件数 = 29
哈希不匹配 = []
清单指向但不存在 = []
实体存在但未登记 = []
闭合 = True
退出码=0
```
（正向 29/29 哈希逐字节匹配；反向包内 29 个实体文件全部已登记，零漏、零多。）

##### P7. 来源判定：平台自研实现（V3 迁移 + 底座自建沙箱），不是照搬外部技能体系

**证据**
- `技能库/后端/技能库/实现/技能库.py:1-22` 开头自述：
  > `迁移自 V3（薄壳化 1-9 受控脚本沙箱 / 1-10 技能包结构校验）：模块库/开发工具/脚本执行器.py（401 行）、模块库/开发工具/技能包校验器.py`
- `开发文档/决策记录/0009_技能库落地.md:10-15` —「把 V3 自持的…技能库落地」，包位置与类型记录
- `开发文档/决策记录/0006`、`0007` 为该包的定义与立项决策
- 安全机制是自建的（不是 Hermes/Claude 技能体系的东西）：`实现/技能库.py:54-69` 硬禁止模块集合 + 硬禁止调用链（`os.system/popen/fork/spawn*/exec*/pty.spawn`）、`:159-203` **AST 静态导入审计**、`:93-105` `RLIMIT_CPU`/`RLIMIT_FSIZE`、`:108-117` `killpg` 进程组回收、`:80-90` 环境变量白名单 fail-closed、`:219-260` 索引白名单 + 路径逃逸校验
- 与 SKILL.md 生态的关系：**格式同构**（`SKILL.md` 带说明 + 同目录 `scripts/`），`说明/设计说明.md:13` 明写「SKILL.md Agent 读的提示词（YAML 头 + 说明）」。即：借鉴了技能包**约定格式**，运行时/校验/沙箱是底座自研，**未照搬任何外部技能运行库**。
- `实现/技能库.py:7-10` 明确「与 V3 版的关键差别：**全部参数化**，不绑任何具体项目」——技能根目录与禁止导入前缀都由调用方传入。

**⚠ 需上级注意的边界问题（本次定级「未确认」，不作问题条目）**：该包明写在 HEAD 之前**从 V3（业务项目 `华世王镞_v3`）迁移代码**。`AGENTS.md:151-152` 禁止「导入、引用、读取任何业务项目的代码…」，读作**运行时**边界，历史迁移不属其字面范围；但迁移来源是业务项目这一点，建议由上级确认是否需在 `开发文档/决策记录/` 里补一份「迁移已完成、底座侧不再依赖 V3」的收口记录（`决策记录/0009` 已记录落地，未记录迁移收口）。

##### P8. 技能库不是孤岛包（被多链路真实依赖）

真实命中（`grep -rn "技能库"` 去缓存后，代码侧）：
```
运行核心/加载器/包发现/发现器.py:62,71-72,64-67        发现全部 第三根
运行核心/加载器/生命周期管理/管理器.py:65,77            装配系统 透传第三根
后端核心/后端核心.py:109,238,274                      装配/包指纹表/热接入 三处传 技能库
后端核心/后端核心.py:637-645                          渐进能力目录 独立「技能库」桶
开发工具/组件合规/合规测试包.py:583                    传第三根
开发工具/组件规范/完整性摘要.py:159                    扫描 (支持库, 模块库, 技能库)
开发工具/更新完整性摘要.py:10                          目标目录含 技能库
开发工具/统一验证/统一验证器.py:31,411                技能根目录 + 本项目根
开发工具/公开调用完整性门禁.py:22                      扫描段含 技能库
开发工具/项目编译/正式包索引.py:88,90,110-111,146-153 索引含技能库（分支 支持库+技能库）
客户端/构建平台客户端.py:30                           顶层包表含 技能库
测试中心: 测试_句柄与数值契约冻结.py / 测试_能力唯一事实源.py:87 / 测试_验证场景可解析.py:23
         / 测试_契约版本唯一.py:29 / 测试_完整性摘要统一.py:284 / 测试_渐进能力目录.py:20,133-135
```
`测试中心/运行核心/测试_渐进能力目录.py:133` 有专测 `test_技能库作为第三根正式包根可见`。
**正式包索引实测**：
```bash
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "
import sys; from pathlib import Path; sys.path.insert(0,'.')
from 开发工具.项目编译.正式包索引 import 构建索引
d = 构建索引(Path('.'))
print('支持库(=支持库+技能库合并) =', len(d['支持库']), '| 模块库 =', len(d['模块库']), '| 技能库 =', len(d['技能库']))
print('技能库键 =', list(d['技能库']))
"
```
```
支持库(=支持库+技能库合并) = 92 | 模块库 = 14 | 技能库 = 1
技能库键 = ['技能库.后端.技能库']
```
**⚠ 未确认项**：`开发文档/项目说明.md:14` 与 `开发文档/未完成事项.md:34` 声称「96 个支持库、14 个模块库、1 个技能库、合计 111 包」。本次 `构建索引` 实测为合并后 92 + 模块库 14 = **106 个包**（技能库 1 个含在 92 内），与 111 差 5。**未确认**差异原因（可能是「适配层豁免」口径不同或索引口径变更）；本次不据此判缺陷，仅如实记录实测值。

---

### 命令清单（本次真实执行的全部命令）

第一部分（示例项目）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
git rev-parse HEAD; git branch --show-current; git status --porcelain
git ls-files 示例项目 | wc -l
find 示例项目 -type f | sort; find 示例项目 -type d | sort
ls -la 示例项目/可双击演示/产物文件夹/
for f in 入口测试.txt 示例输出.txt 验证输出.txt; do ls -l "示例项目/适配层示例/项目资源/$f"; shasum -a 256 "示例项目/适配层示例/项目资源/$f"; cat "示例项目/适配层示例/项目资源/$f"; done
cp 示例项目/适配层示例/项目资源/示例输出.txt /tmp/子审计D_运行前_示例输出.txt

### 【必跑】正式验收入口，裸跑
unset PYTHONPATH; python3.14 示例项目/适配层示例/运行入口/运行.py; echo "退出码=$?"
### 【必跑】补文档凭证后复跑
unset PYTHONPATH; env '系统库网关凭证=local-verify-123' python3.14 示例项目/适配层示例/运行入口/运行.py; echo "退出码=$?"

unset PYTHONPATH; python3.14 示例项目/平台控制面示例/运行示例.py; echo "退出码=$?"
unset PYTHONPATH; python3.14 示例项目/前后端核心示例/运行示例.py; echo "退出码=$?"
unset PYTHONPATH; python3.14 示例项目/示例项目.py; echo "退出码=$?"
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "
import sys; sys.path.insert(0,'.')
from 示例项目.稳定平台完整示例.稳定平台完整示例 import 稳定平台完整示例
结果 = 稳定平台完整示例().执行(); print(结果['调用结果'], 结果['升级后指针'].get('状态'), 结果['回滚后指针'].get('状态'), len(结果['证据']))"
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B \
  示例项目/可双击演示/开发文件夹/编译.py --输出 /tmp/子审计D_编译产物/20260822-demo
find /tmp/子审计D_编译产物 -type f | sort

grep -rn "系统库网关凭证" --include=*.py --include=*.md --include=*.json . | grep -v __pycache__ | grep -v 工程缓存
grep -rn "权限不足\|访问凭证缺失" 运行核心/统一网关/安全边界.py 运行核心/统一网关/本地网关.py
grep -rn "华世王镞\|俏小喵\|5432\|V3\|功能模块/\|后端服务/\|应用软件/\|psycopg\|postgres\|数据库" 示例项目/ | grep -v __pycache__
grep -rhn "^from \|^import \|    from \|    import " 示例项目/ --include=*.py | sed 's/^[0-9]*://' | sort -u
lsof -nP -iTCP:45081 -sTCP:LISTEN; lsof -nP -iTCP:45082 -sTCP:LISTEN
git status --porcelain    # 全部步骤后复查
```

第二部分（技能库）

```bash
find 技能库 -type f | sort; find 技能库 -type f | wc -l
git ls-files 技能库 | wc -l
cat 技能库/后端/技能库/包声明.json 技能库/后端/技能库/__init__.py 技能库/后端/技能库/能力定义.json 技能库/后端/技能库/复用决策.json
cat 技能库/后端/技能库/验证场景.json 技能库/后端/技能库/验证场景引用.json 技能库/后端/技能库/完整性摘要.json
cat 技能库/后端/技能库/权限契约/权限契约.json 技能库/后端/技能库/配置契约/配置契约.json 技能库/后端/技能库/资源预算.json
cat 技能库/后端/技能库/能力数据/Agent查询数据.json 技能库/后端/技能库/能力数据/能力搜索数据.json
cat 技能库/后端/技能库/验证夹具/技能工作区/索引.json
cat 技能库/后端/技能库/验证夹具/技能工作区/{示范技能,越权技能,残缺技能}/契约.json
cat 技能库/后端/技能库/验证夹具/技能工作区/越权技能/scripts/读项目.py
cat 技能库/后端/技能库/验证夹具/技能工作区/示范技能/scripts/统计.py
for d in 支持库 模块库 技能库 技能库/后端; do [ -f "$d/__init__.py" ] && echo "$d 存在" || echo "$d 缺失"; done

for w in 技能 技能包 技能库 校验技能包 运行技能包 生成索引 扫描技能包 读取文件; do
  unset PYTHONPATH; python3.14 开发工具/能力搜索/能力搜索器.py --能力 "$w"; done

unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "…校验技能包/运行技能包/扫描技能包/生成技能索引 六环实测…"
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "…发现全部 两根 vs 三根 对比…"
unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B -c "…正式包索引.构建索引…"
PYTHONDONTWRITEBYTECODE=1 python3.14 -B /tmp/子审计D_摘要闭合.py

grep -rn "技能库" 运行核心/ 后端核心/ 开发工具/ 测试中心/ 平台控制面/ 公共契约/ 项目适配层/ | grep -v __pycache__
grep -rn "资源预算.json\|子进程上限" --include=*.py 运行核心/ 后端核心/ 支持库/适配层/ 公共契约/ 平台控制面/ | grep -v __pycache__
git status --porcelain; git diff --stat    # 收尾复查
```

---

### 跳过项（本次未执行，及原因）

| 跳过项 | 原因 |
|---|---|
| `python3.14 开发工具/发布门禁/运行发布门禁.py` | 冻结边界明令禁跑发布门禁 |
| `python3.14 开发工具/发布门禁/第十四阶段门禁.py` | 同上（故「稳定平台完整示例」改按同法进程内调用，已在 P1 说明） |
| `python3.14 -m 开发工具.HTML验证.验证器 …` | 需重编制品/监听端口，冻结边界禁止重编制品 |
| 按 `项目说明.md:272,281` 原样把制品编译进 `示例项目/可双击演示/产物文件夹/`、再启 `--端口 45082` 演示 | 会向仓库写入新文件（违反唯一允许写入=/tmp）；编译链本身已在 D-06 用 `--输出 /tmp` 真实跑通 |
| `示例项目/可双击演示/开发文件夹/前端IDE/启动.py`（`启动前端IDE.command`/`.bat`） | 启动交互式轻代码编辑器、监听固定端口 45081 并要求人工操作，属会写盘/长驻进程；只做只读代码检查（`前端IDE/启动.py:19-24` 复用 `开发工具.轻代码前端编辑器.启动编辑器.主函数`，端口 45081，文件固定 `开发文件夹/前端/页面/主页.json`） |
| 技能库 `生成索引` 带 `写回索引=True` | 会改写 `验证夹具/技能工作区/索引.json`（仓库文件），改用 `写回索引=False` 实测（结果与断言一致） |
| 全量 `python3.14 -m py_compile $(find . -name '*.py')` | 与本次两个审计对象无关，且 `AGENTS.md:220-225` 的验证属另立条目；本次以真实运行为准 |

---

### 未确认项汇总

1. **是否有仓库外包装（CI/脚本/shell profile）在跑 D-01 入口前 export 免凭证配置** —— 本次严格按 `AGENTS.md:132` 原样执行；未发现仓库内有任何包装脚本调用该入口（`grep -rn "适配层示例"` 仅命中 `README.md:56`、`AGENTS.md:132`、测试与验证工具）。
2. **`开发文档/项目说明.md:14` 的「96 支持库 + 14 模块库 + 1 技能库 = 111 包」与本次 `构建索引` 实测 106 包的差异原因** —— 疑似「适配层豁免」口径或索引口径变更，本次只如实记录两组数字，不判缺陷。
3. **`资源预算.json: 子进程上限=0` 是否真有下游把它当硬限制执行** —— 已核实 `资源监督.注册执行单元`（`平台控制面/资源监督.py:42-56`）只校验键齐全并使用 `线程上限`/`并发调用上限`，未见对 `子进程上限` 的强制；故 D-09 定级「建议」。
4. **D-11 的不可执行调用示例是否已造成实际下游后果** —— 因 D-07（搜索器看不到技能库），当前该示例不可见，属潜在缺陷。
5. **`技能库/后端/技能库/说明/使用说明.md` 是否为「由能力契约生成」的产物**（自述如此）—— 只核对了版本号漂移（D-12），未核对其生成链路是否真实存在。
6. **`示例项目/适配层示例/项目资源/入口测试.txt`、`验证输出.txt` 的人工来源** —— 已核实无代码引用，无法判定由哪次手工验证写入。

---

### 交付说明

- 本报告：`/tmp/子审计_D_示例与技能库.md`
- 辅助脚本：`/tmp/子审计D_摘要闭合.py`（技能库完整性摘要双向闭合复算）
- 运行前快照：`/tmp/子审计D_运行前_示例输出.txt`（证明示例入口真实运行时未改动该仓库文件）
- 编译产物落点：`/tmp/子审计D_编译产物/`（避免污染仓库）
- **仓库零改动**：收尾 `git status --porcelain` 仅有审计前既存的 `?? 开发文档/临时文档/`；`git diff --stat` 为空；HEAD 仍为 `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`

---

## 汇总命令清单（本报告全部真实执行过的命令，按节归集）

```bash
# 环境与冻结边界
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
/Library/Developer/CommandLineTools/usr/bin/git rev-parse HEAD
/Library/Developer/CommandLineTools/usr/bin/git status --porcelain
/Library/Developer/CommandLineTools/usr/bin/git diff --stat

# 目录规模（两口径）
for d in 公共契约 前端核心 后端核心 启动监督器 客户端 项目适配层 MCP工具箱 示例项目 运维脚本 技能库; do
  printf "%-14s 全部=%s 去缓存=%s\n" "$d" "$(find "$d" -type f | wc -l)" "$(find "$d" -type f -not -path '*__pycache__*' | wc -l)"; done

# 第 1 节（前端/后端核心/监督器/客户端/运维脚本）
python3.14 -m py_compile <8 个 py>
python3.14 -c "import 前端核心.前端核心, 前端核心.渲染提供者, 前端核心.浏览器交互, 后端核心.后端核心, 启动监督器.健康监督, 启动监督器.容量基线, 客户端.构建平台客户端"
grep -rn "容量基线\|渲染提供者\|健康监督\|热接入\|构建平台客户端" --include='*.py' 测试中心 运行核心 开发工具 平台控制面
plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist     # 只读
python3.14 -c "<依赖防火墙.审计依赖() 只读调用>"                      # 582 文件 / 0 违规

# 第 2·3 节（项目适配层 + 业务引用铁律）
grep -rnE "^\s*(from|import)\s+(支持库|模块库|技能库)" 项目适配层 --include='*.py'   # 退出码 1，0 命中
python3.14 -c "from 项目适配层.依赖锁定.依赖锁定 import 生成依赖锁定; …"               # 只读直调
python3.14 -c "from 项目适配层.依赖锁定.锁定校验 import 校验锁定文件; …"
python3.14 -c "from 项目适配层.配置适配.配置合并 import 合并配置; …"                   # 五层顺序实测
grep -rn "华世王镞\|huashi\|V3\|5432\|3306\|6379\|业务项目" --include='*.py' --include='*.json' --include='*.plist'
grep -rn -F "~/Documents/Agent/PHP/" --exclude-dir=.git --exclude-dir=工程缓存 --exclude-dir=__pycache__
ls ~/Documents/Agent/PHP/            # 取兄弟项目名后逐一 grep

# 第 4 节（MCP工具箱）
curl -s -m 5 -X POST http://127.0.0.1:8766/mcp/ -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}'
python3.14 -c "<tools/list 只读>"                     # 实测 50 个工具
grep -rn "path\|resolve\|relative_to" MCP工具箱/工作区管理.py MCP工具箱/模块合规.py MCP工具箱/测试资源.py

# 第 5·6 节（示例项目 + 技能库）
unset PYTHONPATH; python3.14 示例项目/适配层示例/运行入口/运行.py            # 真实退出码 1（见 D-01）
unset PYTHONPATH; python3.14 示例项目/示例项目.py                            # 退出码 0
unset PYTHONPATH; python3.14 示例项目/前后端核心示例/运行示例.py             # 退出码 0
unset PYTHONPATH; python3.14 示例项目/平台控制面示例/运行示例.py             # 退出码 0
unset PYTHONPATH; python3.14 示例项目/稳定平台完整示例/稳定平台完整示例.py   # 退出码 0
python3.14 开发工具/能力搜索/能力搜索器.py --能力 <关键词>
python3.14 -c "<技能库完整性摘要 sha256 双向复算>"   # 29/29 匹配，闭合 True

# 第 7 节（公共契约）
grep -rn '契约版本[^=]*=[^=]*"1\.0\.0"' --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存
python3.14 -c "from 公共契约.基础类型.类型表 import 正式类型表; print(len(正式类型表), sorted(正式类型表))"
python3.14 -c "from 公共契约.版本规则.契约版本 import 契约版本; print(契约版本)"
python3.14 -c "from 公共契约.句柄体系 import 是合法句柄id; print(是合法句柄id(12345), 是合法句柄id('12345'), 是合法句柄id('012345'))"
python3.14 -c "from 公共契约.包声明 import 从字典构建; o=从字典构建({...}); print(sorted(o.转字典()))"
grep -n "with self._锁\|    def " 公共契约/句柄体系.py
grep -rn "有界线程HTTP服务器\|BoundedSemaphore" --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存
for n in 错误码_句柄无效 … 错误码_资源未清理; do grep -rn "$n" --include='*.py' . | grep -v '公共契约/错误结构' | wc -l; done
for d in 公共契约 … 公共契约/诊断; do [ -f "$d/__init__.py" ] && echo 有 || echo 缺; done
```

## 跳过项（按冻结边界或成本控制未执行）

| 跳过项 | 原因 |
|---|---|
| 发布门禁 `python3.14 开发工具/发布门禁/运行发布门禁.py` | 明令禁止跑发布门禁 |
| HTML 黑盒验证器 / 重新编译制品 / 制品激活 | 明令禁止重编制品、禁跑发布门禁 |
| 任何 `unittest` 全量或定向执行 | 未授权；且 AGENTS.md 规定编译期禁跑全量 |
| 重启 8766 / 40007 / launchctl kickstart / 改 LaunchAgent | 禁止重启服务与改配置（仅 `plutil -p` 只读读） |
| 写任何仓库文件（含 `工程缓存/`） | 唯一允许写入 = 本报告与 `/tmp` |
| `示例项目/可双击演示/开发文件夹/前端IDE/启动.py`（交互式、固定端口） | 交互 + 端口占用风险，改做只读检查 |
| 数据库写入、迁移、`VACUUM`、运行库直查写 | 禁止动运行数据与数据库 |
| `MCP工具箱` 的写路径（文件补丁/租约申请/工作区提交）真实触发 | 会改仓库与运行状态，只做代码级判定 |
| `test_resource` 自杀链端到端实弹 | 会杀掉 8766 平台 MCP 服务，禁止 |
| 8866 能力网关真实探测 | 现场未监听，未起服务（禁重启） |

## 未确认项（汇总，全部不据以判缺陷）

1. **`后端核心.装配()` 装配模板全局缓存的污染方向未扩到真实系统根**（扩测会写 `工程缓存/`，越界）；40007 单实例单根时不可达，出现同进程第二个不同根即升级为阻断。
2. `.ruff_cache/*`、`.codegraph/daemon.log` 内有本机绝对路径命中，未做源码判定。
3. 40007 与 8766 并发写同一 SQLite 的锁行为未实测。
4. `模块库/直播逐字稿/工具/` 下的 `com.huashi.zizhugao-queue.plist` 是否已装载、是否在跑（禁动服务，未查 `launchctl`）。
5. `模块库/直播逐字稿` 的业务写死是否属「已记录例外」需口径裁决：`未完成事项.md:61` 声称「底座已彻底去掉行业写死内容（9 个模式提示词零行业词，已断言）」，与实测的 `工具/` 脚本、`能力契约/` 示例、`验证场景/` 数据直接矛盾——断言只覆盖 `模式数据/模式注册表.json` 的 9 个提示词。
6. `系统底座_指纹校验` 是否有平台外（launchd/外部守护）注入点未穷尽。
7. 前端核心默认 `http://127.0.0.1:8899` 的默认值归属判定：现场 8899 由另一项目 `人脉数据系统/图谱前端/服务.py`（PID 33052）监听，非平台服务；该默认值是否曾被刻意指向外部工具未确认。
8. 开发文档声称 111 个正式包 vs 现场实测 106 包的口径差异（本文不据以判缺陷，口径待父会话裁决）。
9. `模块库/直播逐字稿` 的业务耦合是「底座缺通用能力时的临时消费者脚本」还是正式能力的一部分，需裁决其去留层级。
10. `能力占用` 域 upsert 与行数上限、`协作状态` 表规模上限未实测压力。
11. 技能库 `资源预算.json 子进程上限: 0` 与「运行技能包」必起子进程的矛盾，是配置未落还是设计如此，未确认。

---

*本报告为只读审计产物；未对系统做出任何修改。审计期间 HEAD 保持 `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`，工作树仅新增本审计临时目录。*
