# 子报告_01_统计口径与可执行性复算（父会话接手完成）

> 原始子代理两次因上游 HTTP 503 失败（无产物），由父会话直接接手跑完。

## 结论
- 测试文件总数 **193**；**真实 `import` 失败 1 个**；含连字符文件名 **0** 个。
- 含义：`py_compile`/AST 全绿 ≠ 可导入；本次用子进程真实 `import` 逐个验证，结论见下。


### 导入错误（含相对导入失败）（1 个）

- `测试中心/支持库/测试_psycopg提供者.py`
  - 错误：`ImportError: cannot import name '关闭' from '支持库.适配层.psycopg提供者' (~/Documents/Agent/PHP/系统工程平台/支持库/适配层/psycopg提供者/__init__.py)`

## 复现命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 -B -c "import 测试中心.<模块路径>"   # 逐个；或见本次脚本 /tmp/底座审计_僵尸测试复扫.py
```


---

## 父会话复核：这 1 个 import 失败的定性与影响（重要 1 条）

**`测试中心/支持库/测试_psycopg提供者.py` 是僵尸测试（永久无法运行）。**

- 证据（`:19`）：`from 支持库.适配层.psycopg提供者 import 关闭, 连接, 查询, 注册能力, 事务执行`
- 现场（`支持库/适配层/psycopg提供者/__init__.py:23`）：`__all__ = ["检查可用性", "打开连接", "释放连接", "归类错误", "解析连接串", "校验连接串", ...]` —— **`关闭/连接/查询/事务执行` 这四个旧符号已在 2026-09-14「数据库收口」时删除**（该次收口把 psycopg 落成最底层翻译层，旧四条连接串能力全删）。
- 真实复现（不是静态推断）：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
  python3.14 -B -m unittest 测试中心.支持库.测试_psycopg提供者
  # → ImportError: cannot import name '关闭' from '支持库.适配层.psycopg提供者'
  # → Ran 1 test … FAILED (errors=1)，退出码 1
  ```
- 为什么长期没人发现：**没有任何聚合入口会跑这 193 个测试文件**（见子报告 02a 的 E-1：`测试中心/运行测试.py` 已不存在），所以这类「收集期就炸」的文件不会在任何常规验证里报红。
- 连带问题：`测试中心/开发工具/测试_反向破坏门禁28.py:29` 的**白名单里还写着 `测试_psycopg提供者.py`** —— 门禁白名单引用了一个连 import 都不成立的文件，属同类漏改。
- 建议动作：① 删除或改写该测试文件为现行 psycopg 提供者接口（`检查可用性/打开连接/释放连接`）；② 同步清理反向破坏门禁白名单；③ 给测试中心补一个「全量可导入性」检查（一条命令即可，成本极低），把这类僵尸挡在入口。

### 附：其余维度复算结论（父会话实测）
- 193 个 `测试_*.py` 中，**含连字符文件名 0 个**（这类命名必然 SyntaxError，本仓已无）。
- 唯一 import 失败 1 个（即上述）；其余 192 个可正常导入。
