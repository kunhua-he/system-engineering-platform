# 落点清单 10 · 文档权威性与结构残留（第二轮：定真 + 落点）

> 审计对象：系统工程平台（底座）　根目录 `~/Documents/Agent/PHP/系统工程平台`
> 冻结边界：分支 `主干` / HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`。本轮**只读**：未改源码/文档/配置/运行数据/制品/数据库，未重启服务，未跑发布门禁，未重编制品。
> 唯一写入 = 本文件。「二次证真」列全部为本轮现场命令的真实输出（写在本文件时：2026-09-15 14:41–15:05）。
> 落点口径：文档类 = `文档名:节/行` + 改成什么；结构类 = 删/移哪个路径或挂到哪个门禁收尾。
> 环境：`python3.14` + `unset PYTHONPATH`。

---

## 一、逐条二次证真 + 落点（固定列）

| 编号 | 二次证真（命令 + 输出摘要） | 判定 | 落点（文档:节/行 → 改成什么；结构类写清删/移路径） | 影响面 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| 10-10a 包数/能力数 | `find . -name '包声明.json' -not -path './工程缓存/*' \| wc -l` → `112`；`能力定义.json` → `106`；`python3.14 -c "…正式包索引.构建索引…"` → `支持库 92 模块库 14 技能库 1`；能力条目汇总 → `能力定义文件 106 条目合计 536` | ✅ 成立（文档 96/111/549 均错） | `开发文档/项目说明.md:14` → 整行改写为：`- 当前正式索引实测（取证命令见下行）：支持库 92、模块库 14、技能库 1，合计 107 个正式包；磁盘 包声明.json 112 个；能力源头 106 个 能力定义.json / 536 条能力`；并新增一行「口径表见 §1」 | 唯一事实源失真的根因；读者会把 549 当成能力全集 | **是**（落点清单_02/05/06/07 均引用 `项目说明.md`） | 同左三行命令 |
| 10-10b 完整性摘要包数 | `python3.14 -c "…完整性摘要.扫描正式包…"` → `正式包数 112 / 不闭合 0`（子报告原始实测）；本轮复核 `包声明.json` 计数同为 `112` | ✅ 成立（文档 116 错） | `项目说明.md:15` → 把 `**116 个正式包**` 改为 `**112 个正式包**`，并在行尾补取证命令 `python3.14 -c "from 开发工具.组件规范.完整性摘要 import 扫描正式包; …"` | 与 `未完成事项.md:225` 的 112 互斥 | **是** | 同上 |
| 10-10c 基线提交 | `git rev-parse HEAD` → `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`；`项目说明.md:13` / `未完成事项.md:37` 均写 `e255e57` | ✅ 成立（未标历史快照） | `项目说明.md:13` → `- 当前工作区基线提交（实测）：\`1c69ea5d\`（取证：git rev-parse HEAD）；e255e57 为 2026-09-11 历史快照`；`未完成事项.md:37` 同款双写 | 新会话会拿错基线做比对 | **是** | `git rev-parse HEAD` |
| 10-10d 上次制品 | `ls -d 工程缓存/制品仓库/平台客户端制品/平台客户端-2ee78ea86b3fb981` → `No such file or directory`；现场 12 个目录中无 `2ee78ea8*` | ✅ 成立（不可复现） | `项目说明.md:18` → 改为：`- 当前激活制品（以指针为准）：\`工程缓存/制品仓库/平台客户端环境/当前.json\` → 摘要sha256 \`0eec30284da0d3bd\` / 制品摘要 \`7f68175962d12ca48e01966c9c78676f\` / 版本 76（取证：cat 该文件）；2ee78ea86b3fb981 已不存在，属历史值` | 制品回滚判断被引到不存在的制品 | **是** | `cat 工程缓存/制品仓库/平台客户端环境/当前.json` |
| 10-10e 记忆能力数 | `python3.14 -c "json 读 支持库/后端/记忆支持库/能力定义.json"` → `条目 9`，多出 `记忆支持库.解析配置回退链` | ✅ 成立（文档 8 错） | `项目说明.md:335`（不是 336，精确行号已复核）→ `…当前登记 写入、追加、完成、删除、搜索、最近、列出、生成标识、解析配置回退链 9 项能力…` | 记忆包能力清单少一项 | **是** | 同左命令 |
| 10-10f 549 能力可用性 | `项目说明.md:440` 写「549 个正式能力全部可作为后台任务提交」；能力源头实测 `536` | ✅ 成立 | `项目说明.md:440` → `**536 个正式能力全部可作为后台任务提交**`（与 §1 口径表同源，改一处必须同步另一处） | 对外承诺数字 | **是** | 能力条目汇总命令 |
| 10-01 依赖审计「17 Provider / 违规 1」 | `未完成事项.md:39` 仍写 `17 个标准 Provider，违规 1 个 / 2 条`；子报告实测 `标准提供者 15 个（跳过 2 个非标准目录）/ 全部通过：无违规`；本轮现场 `支持库/适配层/YAML提供者/能力定义.json` 与 `生命周期契约.json` 均在（A7 已证） | ❌ 已修复未删 + 同文件自相矛盾 | `未完成事项.md:39` → **整条删除**（结论已被 `:221` 审计结论「依赖生命周期审计：全部通过：无违规」覆盖） | 债务清单挂着不存在的问题；与同文件全绿互斥 | **是**（多任务同改 `未完成事项.md`） | `python3.14 -m 开发工具.依赖生命周期审计.运行审计` |
| 10-02 `except Exception: pass` 18 处 | 本轮复核空目录批次同源命令见子报告 D7；子报告实测 `grep -rnP 'except\s+Exception\s*:\s*(?:#[^\n]*\n\s*)?pass\b'` = `0` 命中 | ✅ 成立（已修复未删） | `未完成事项.md:164-178` → **整节（含「现场清单」代码块）删除**；若需留痕，把「判据：失败必须明确」一句并入 `AGENTS.md`/哲学条目，清单不留 | 18 处清单已全失效，会误导人再去改一遍 | **是** | 同 D7 grep 命令 |
| 10-03 办公文档夹具含「甲」 | 子报告 A8 实测 `最小中文演示.pptx 不含甲` / `最小中文表格.xlsx 不含甲`（`zipfile` 读全部 `.xml`） | ❌ 已修复未删 + 自相矛盾 | `未完成事项.md:26`（「办公文档 2 个夹具…含『甲』…待华哥定」）与 `:70`（P3 命名残留 2 处夹具）→ **两条删除**；保留 `:242` 的已重造结论 | 与 `:242` 直接互斥 | **是** | 见子报告 D10 脚本 |
| 10-04 「重试调度」待裁决 | `未完成事项.md:68` 写「定稿前不动契约」、`:198 #5` 写「建议补 `实际错误码`」；A7 实测 `包声明.json:537`、`__init__.py:112` 已落地，`:240` 已记为完成 | ❌ 已修复未删 + 自相矛盾 | `未完成事项.md:68` **整条删除**；`:198` 的 #5 小项删除（保留 #5 之外的同节其他项） | 同一文件三处口径不一致 | **是** | `grep -n 实际错误码 支持库/后端/大语言模型支持库/模型连接器/包声明.json` |
| 10-05 全自动精校「发布门禁未重跑」 | `未完成事项.md:60` 写「剩编译产物 HTML 黑盒与发布门禁未重跑；发布前必须补跑」；同文件 `:221` 判「发布门禁 通过」、`:258-267` 记「通过（退出码 0）」 | ❌ 同一文件自相矛盾 | `未完成事项.md:60` → **整条删除**，替换为一行指针：`- 全自动精校：验证场景已补齐（:60 原有事实保留一句），制品与门禁结论见本文件「2026-09-15 审计结论」节` | 两处结论互斥，新会话无法判断 | **是** | `sed -n '60p;221p' 开发文档/未完成事项.md` |
| 10-06 制品仓库 12 份副本 186M | 本轮 `du -sh 工程缓存/制品仓库/平台客户端制品/平台客户端-*/ \| sort -rh` → 12 份（5×16M + 7×15M）；`ls -d 工程缓存/制品仓库/*/` → 7 个顶层目录；`cat 平台客户端制品/当前.json` → `{"摘要sha256":"0eec30284da0d3bd","路径":"平台客户端-0eec30284da0d3bd"}` | ✅ 成立（与「只留一个权威」矛盾） | 见 **§3 制品仓库方案**（本轮只写方案） | 186M 空间 + 误导回滚判断 | 否（制品层，仅本清单覆盖） | 同左两条命令 |
| 10-07 决策记录编号重复缺号 | `ls 开发文档/决策记录/` → `0008_数据库连接池唯一化与anthropic协议.md` + `0008_能力更新走热接入不重启网关.md`；`0009_依赖分两段_第三方翻译层与环境依赖层.md` + `0009_技能库落地.md`；编号序列 `0001-0007, 0008×2, 0009×2, 0011`，**无 0010** | ✅ 成立 | 见 **§6 目录规则改名方案** | 「引用决策编号」不可靠 | 是（`主开发文档.md:21` 已登记该目录） | `ls 开发文档/决策记录/ \| grep -oE '^[0-9]{4}' \| sort \| uniq -c` |
| 10-11 `__pycache__` 315 个 | 本轮 `find . -name '__pycache__' -type d -not -path './工程缓存/*' \| wc -l` → `315`（与子报告一致） | ✅ 成立（文档称「已清」不成立） | 见 **§5 结构清理挂载点**（挂到构建/门禁收尾，不做一次性手删） | 门禁通过证据不可复现；非仓库洁净度问题 | 否 | 同左命令 |
| 10-17 空目录 267 个 | `ls -A 工程缓存/验证运行 \| wc -l` → `267`；`du -sh 工程缓存/验证运行` → `0B`（子报告） | ✅ 成立 | 见 §5：挂到发布门禁收尾（`工程缓存/验证运行` 未登记空目录回收） | 0B 但仍污染目录盘点 | 否 | 同左命令 |
| 10-18 协作状态空目录 | `ls -A 工程缓存/协作状态 \| wc -l` → `0`（而 `未完成事项.md:143` 记「→ 删除」） | ✅ 成立 | 结构：删 `工程缓存/协作状态/`（空目录）；文档 `未完成事项.md:143` 该行按 §5 归入「结构清理」收口 | 两条腿的残留观感 | 否 | 同左命令 |
| 10-19 孤儿提供者环境 4 个 | `ls 工程缓存/提供者运行环境/ \| grep -E 'pg8000\|psycopg2'` → `pg8000提供者 / pg8000数据库 / psycopg2提供者 / psycopg2数据库`（四包已按 `未完成事项.md:75` 整包删除） | ✅ 成立 | 见 §5：删 4 个目录；挂到门禁收尾做「环境目录 ↔ 现存包」存在性对账 | 已删包的运行环境残留 | 否 | 同左命令 |
| 10-22 归档命名违规 | `find 开发文档/归档 -maxdepth 2` → `2026-08_第十四阶段/`（5 个 JSON）与 `202609_系统工程平台审计/`（合规） | ✅ 成立 | 见 **§6**（改名 `202608_第十四阶段`，或按规则改名并说明内容为 JSON 快照） | 出现第二套并行命名 | 是（`主开发文档.md:20` 引用 `归档/`） | 同左命令 |
| 10-09 悬空引用 57/58 号 | `grep -n "57 号\|58 号\|57／58" 开发文档/未完成事项.md` → `:67`（见 58 号文档）、`:190`、`:192` 共 3 处；`开发文档/临时文档/` 内无 57/58 号 | ✅ 成立 | `未完成事项.md:67` 删「见 58 号文档」；`:190` 删「与 57／58 号临时工作台」；`:192` 删「判据与建议见 57 号文档 B 组」→ 三处改为指向本文件对应小节 | 3 处引用指向不存在的文件 | **是** | 同左 grep |
| 10-14 AGENTS 权威文档边界 | `ls -1 开发文档/*.md \| wc -l` → `15`；`AGENTS.md:197` 只列 4 份 | ✅ 成立 | `AGENTS.md:197` → 改为两级写法：`绝对权威 = 4 份（项目说明/未完成事项/当前流程图/主开发文档）；专题权威 = 其余顶层文档，各自在其前言声明覆盖面，冲突时以绝对权威为准`；同时把 3 份自称「唯一权威」的文档（`底座升级方案.md:1`、被 `主开发文档.md:16` 标注的 `资源回收确认机制.md`、`通用能力反馈接口与平台自治治理.md:15`）前言里的「唯一权威」改为「专题权威」 | 硬规则文件与新会话判断依据 | **是**（AGENTS.md 被全部任务共享） | `ls -1 开发文档/*.md \| wc -l`；`sed -n '197p' AGENTS.md` |
| 10-15/10-16 流水账与数字标注 | `grep -n "已完成审计批次\|本轮验收证据" 开发文档/项目说明.md` 命中 `:16`（历史快照）、`:23-25`；`未完成事项.md:16` 写源头 515 / `:20` 写 `工程缓存 3.5G`，实测 536 / 3.1G | ✅ 成立 | `项目说明.md:3` 的「不留流水账」条款 → 保留并**执行**：删 `:16`（本轮验收证据 2026-09-10 段）、`:23-25`（已完成审计批次/增强批次共 3 行），历史值移入 `开发文档/交付记录/`；`未完成事项.md:16` → `能力定义源头 536 能力`、`:20` → `工程缓存 3.1G`，并各自补取证命令 | 制度性根因 | **是** | `grep -n "已完成审计批次" 开发文档/项目说明.md`；`du -sh 工程缓存` |
| 10-27b 依赖契约份数 | `未完成事项.md:140` 称「保留 16 个」；子报告实测 `依赖契约.json` 实际 17 份（未在本轮复跑，按子报告采信） | ⚠️ 部分（本轮未复跑计数） | `未完成事项.md:140` → 数字改为现场实测值并补取证命令（改前先跑一次计数命令确认） | 数字口径 | **是** | `find . -name '依赖契约.json' -not -path './工程缓存/*' \| wc -l` |
| 10-27c 使用说明多一份 | `项目说明.md:139` 称「112/112 包」；子报告实测 `设计说明.md` 112 份、`使用说明.md` 113 份，多出 `模块库/直播逐字稿/工具/使用说明.md` | ⚠️ 部分 | 二选一（需华哥定）：① 删 `模块库/直播逐字稿/工具/使用说明.md`（若其内容并入 `说明/使用说明.md`）；② 若确为工具级说明书，则把 `项目说明.md:139` 口径改为「每包至少一份，允许包内多份并逐份登记」 | 打破 1:1 口径 | **是**（项目说明） | `find . -name '使用说明.md' -not -path './工程缓存/*' \| wc -l` |

---

## 二、§1 唯一权威口径表（建议原样写进 `项目说明.md` 顶部，并让 `未完成事项.md` 只引用不重写）

规则：**一行一类口径，每行必须写「数字 + 范围 + 取证命令 + 时刻」，不允许只有数字**。四类口径互不替代：

| 口径行 | 含义（事实源） | 当前实测值（2026-09-15） | 取证命令 |
|---|---|---|---|
| **源头数** | 磁盘上正式目录里的源头文件 | `包声明.json` **112** 个；`能力定义.json` **106** 个 → **536** 条能力 | `find . -name '包声明.json' -not -path './工程缓存/*' \| wc -l`；`find . -name '能力定义.json' -not -path './工程缓存/*' \| wc -l`；能力条目汇总脚本（子报告 D1） |
| **装配数（正式索引）** | 编译器按正式索引装配的可调用视图 | 支持库 **92** + 模块库 **14** + 技能库 **1** = **107** 包；能力所有者 **536** | `python3.14 -c "from pathlib import Path; from 开发工具.项目编译.正式包索引 import 构建索引; i=构建索引(Path.cwd()); print(len(i['支持库']), len(i['模块库']), len(i['技能库']), len(i['能力所有者']))"` |
| **运行时数（网关）** | 40007 常驻网关实际暴露的能力数 | `能力数 536`、`当前版本 2.0.0` | `curl -s http://127.0.0.1:40007/健康 -H 'X-System-Credential: …'` |
| **制品数（发布/验证）** | 指针与账本绑定的那一份制品 | 构建摘要（16 位）`0eec30284da0d3bd`；包仓库制品摘要（32 位）`7f68175962d12ca48e01966c9c78676f`；版本/栅栏令牌 `76`；HTML 账本「场景制品摘要」（64 位）`cdc1b72ae9b8071860ea4f43af8ca534469aa83be52d3d973f5ab6c69e7fbd04` | `cat 工程缓存/制品仓库/平台客户端环境/当前.json`、`平台客户端制品/当前.json` |
| **完整性摘要口径** | 组件规范扫描的正式包 | **112 包 / 不闭合 0** | 子报告 D3 命令 |
| **历史值（禁止当当前事实）** | 只在历史快照里成立的旧数字 | `96 支持库 / 111 包 / 549 能力 / 116 包 / 源头 515 / 已登记 514 / 装配 498 / 基线 e255e57 / 上一次制品 2ee78ea86b3fb981 / 权威状态库 13.2MB、8.7MB / 工程缓存 3.5G` | 无需命令——这些值现场已不可复现（本轮逐一验过） |

**写法要求（防回潮）**：① 口径表放 `项目说明.md` 顶部，正文任何数字必须写「见口径表」或同源引用；② `未完成事项.md:16` 现状基线一行改为引用口径表，不再自报数字；③ 每次改数字必须同时给取证命令，否则视为未完成。

---

## 三、制品仓库方案（10-06；本轮只写方案，不动手）

### 3.1 现场事实（本轮二次证真）
- 内容寻址顶层目录 `工程缓存/制品仓库/` 下共 **7 个顶层目录**：`7f68175962d12ca48e01966c9c78676f/`（16M，被环境指针 `制品摘要` 引用）、`平台客户端信任/`、`平台客户端制品/`、`平台客户端密钥/`、`平台客户端构建/`、`平台客户端状态/`、`平台客户端环境/`。
- `平台客户端制品/` 下 **12 份** `平台客户端-<16位>` 副本（5×16M + 7×15M）+ `当前.json`；指针指向 `平台客户端-0eec30284da0d3bd`。
- **保留哪一份**：`平台客户端-0eec30284da0d3bd`（唯一被 `平台客户端制品/当前.json`、`平台客户端环境/当前.json`、`平台客户端信任/制品/平台客户端_0eec30284da0d3bd.bin`、`信任/元数据/目标_平台客户端_0eec30284da0d3bd.json` 同时引用）；其余 11 份无指针引用。
- **状态账本真相（修正子报告 10 的表述）**：`工程缓存/制品仓库/平台客户端状态/权威状态.db` 的 `制品` 表**只有 6 行**，不是 12 行——
  `(a378d1b2b82c03abf465d1d5782c1009, 31004a8293cfc801)`、`(3defb0579284493b5453ea24c8087f00, 3c336f5dfabf5e95)`、`(6f14f99c2e3a7a8b1dea04465a4cf1e9, d55ec84645dbde35)`、`(8556dcee07813a3c9efbd161f4918386, bfd1a9b042d1b7fb)`、`(53face2a32356a3be9305c4e05b95a5a, 196e13bced099f3a)`、`(7f68175962d12ca48e01966c9c78676f, 0eec30284da0d3bd)`。
  → 其中 **5 行的 32 位 `制品摘要` 指向的顶层内容寻址目录已不存在**（只剩 `7f68…`）= 账本自身就有 5 条悬空记录；**另外 6 份制品目录在账本里根本没有行**。所以「每份制品都在账本里有一条记录」不成立。
- `发布` 表 17 行按 16 位摘要登记了构建发布记录，**11 份非激活件里有 6 份（`6a6b9814527de6d1`、`5df7cd7cd887927a`、`3115d8c77ebdf54b`、`1246f3724c0f18c4`、`f705f14f5f8e4cc7`、`79ba4b953cccdb0d`）在 `发布` 表有行**；删目录不同步删行会留下指向不存在制品的发布记录。
- 清理逻辑现状（根因）：`客户端/构建平台客户端.py:475 清理过期制品(仓库根, 当前摘要, 当前身份)` 只在 `接入.制品根目录`（= `工程缓存/制品仓库/`，见 `平台控制面/包仓库/平台客户端制品.py:122`）的**顶层**遍历，识别「32 位内容寻址目录」与「`平台客户端-*` 身份目录」。而构建产物写进的是**子目录** `平台客户端制品/`（`构建平台客户端.py:548` `摘要制品根 = 制品目录 / f"{客户端前缀}-{摘要[:16]}"`）——子目录不在 `iterdir()` 视野内，**因此 12 份副本从不被清理**。`清理过期制品` 也只在 `--安装` 分支调用（`构建平台客户端.py:624`）。

### 3.2 具体方案（待华哥裁定后执行）

**A. 保留清单（唯一权威）**
1. 目录：`工程缓存/制品仓库/平台客户端制品/平台客户端-0eec30284da0d3bd/`
2. 顶层内容寻址：`工程缓存/制品仓库/7f68175962d12ca48e01966c9c78676f/`
3. 信任链：`平台客户端信任/制品/平台客户端_0eec30284da0d3bd.bin` + `信任/元数据/目标_平台客户端_0eec30284da0d3bd.json` + `快照_92.json`（各留最新一份）
4. 指针：`平台客户端制品/当前.json`、`平台客户端环境/当前.json`（不动）

**B. 删除清单（11 份目录 + 账本同步）**——顺序必须是「先账本、后磁盘」，否则中途失败会留下悬空指针：
1. 先删 `工程缓存/制品仓库/平台客户端状态/权威状态.db` 中 `制品` 表的 5 条**悬空行**（其 32 位摘要对应顶层目录已不存在）。
2. 再删 `发布` 表里指向待删 11 份的发布记录行（或按华哥裁定改为「标记归档」——但哲学 22「旧版直接删」支持直接删行）。
3. 最后删 11 份目录：`平台客户端-1246f3724c0f18c4`、`-196e13bced099f3a`、`-31004a8293cfc801`、`-3115d8c77ebdf54b`、`-3c336f5dfabf5e95`、`-5af9543caaa12140`、`-5df7cd7cd887927a`、`-6a6b9814527de6d1`、`-bfd1a9b042d1b7fb`、`-d55ec84645dbde35`、`-f705f14f5f8e4cc7`（预计释放约 170M）。
4. 删完后 `制品` 表应只剩 1 行（`7f68175962d12ca48e01966c9c78676f / 0eec30284da0d3bd`），与 `激活指针` 表（现 1 行：`平台客户端 / 7f68175962d12ca48e01966c9c78676f / 76 / 76 / 激活`）对齐。

**C. 状态账本怎么同步（机制落点，防复发）**
- 落点①：`客户端/构建平台客户端.py:624` 调用处改为同时清理**两个**根：`清理过期制品(接入.制品根目录, …)` + `清理过期制品(接入.客户端制品目录, …)`（`客户端制品目录` 默认 = `工程缓存/制品仓库/平台客户端制品`，见 `平台客户端制品.py:124`）。这样每次重编自动收敛为 1 份。
- 落点②：`清理过期制品`（`构建平台客户端.py:475`）补一段「删目录后同步删账本」：对每个被删的 16 位摘要调用 `接入.状态` 删除 `制品`/`发布` 表对应行（`平台控制面/平台状态.py` 现有 API 只有 `写入记录/原子插入/读取记录/查询记录/条件更新/追加证据/查询证据`，**没有删除记录方法**——需在 `平台状态.py` 增一个受控的 `删除记录(表, 条件SQL, 参数)`，或在 `包仓库/服务.py` 增 `注销制品(制品摘要)` 收口，禁止脚本里裸 sqlite 删）。
- 落点③：把「制品仓库目录数 == 1 + 指针命中」变成**门禁项**：`开发工具/发布门禁/运行发布门禁.py` 增加「制品仓库收敛」检查（`工程缓存/制品仓库/平台客户端制品/` 下 `平台客户端-*` 目录数必须为 1，且等于两个 `当前.json` 的 `摘要sha256`）。

**D. 验证命令（执行后逐条跑）**
```bash
cd ~/Documents/Agent/PHP/系统工程平台
ls -d 工程缓存/制品仓库/平台客户端制品/平台客户端-*/ | wc -l          # 期望 1
ls -d 工程缓存/制品仓库/*/ | wc -l                                     # 期望 7（顶层结构不变）
du -sh 工程缓存/制品仓库/                                              # 期望 247M → 约 77M
python3.14 -c "
import sqlite3
c=sqlite3.connect('file:工程缓存/制品仓库/平台客户端状态/权威状态.db?mode=ro',uri=True)
print('制品', list(c.execute('select 制品摘要,版本 from 制品')))
print('激活指针', list(c.execute('select * from 激活指针')))"
cat 工程缓存/制品仓库/平台客户端环境/当前.json                          # 指针仍指向 0eec30284da0d3bd
python3.14 开发工具/发布门禁/运行发布门禁.py                            # 华哥放行后跑（本轮冻结边界禁止）
```

---

## 四、同一份制品三套摘要 → 权威化方案（附现有引用点清单）

### 4.1 三套摘要的真实口径（本轮从代码取证，纠正「只是截断」的说法）

| 套 | 值（当前激活件） | 算法与范围（代码事实源） | 语义 |
|---|---|---|---|
| ① 16 位「摘要sha256」 | `0eec30284da0d3bd` | `客户端/构建平台客户端.py:425 计算制品摘要(制品根)` → 对**制品根**（含 `平台客户端/` 包层与运行壳）路径 + 字节 sha256，取 `[:16]`（`:546`） | **构建摘要 / 制品目录摘要**：决定目录名 `平台客户端-0eec30284da0d3bd` 与 `平台客户端制品/当前.json` 的 `摘要sha256` |
| ② 32 位「制品摘要」 | `7f68175962d12ca48e01966c9c78676f` | `平台控制面/包仓库/服务.py:39 构建制品` → 对 `json({包id,版本,文件清单{路径:{sha256,大小,模式}},构建输入})` 求 `内容摘要`（`:68`）；版本 = ① | **包仓库内容寻址主键（制品主键）**：顶层目录名 `工程缓存/制品仓库/7f68…/`、`权威状态.db 制品.制品摘要(PK)`、`激活指针.目标`、`平台客户端环境/当前.json.制品摘要` |
| ③ 64 位「场景制品摘要」 | `cdc1b72ae9b8071860ea4f43af8ca534469aa83be52d3d973f5ab6c69e7fbd04` | `开发工具/HTML验证/单实例验证.py:35` `报告.制品摘要前 = _制品全文件摘要(制品目录)` → `当前摘要`；`:43 报告.场景制品摘要 = 当前摘要`；写进 `开发工具/HTML验证/验证证据.py:43` | **验证期全文件摘要**：绑定「被黑盒验证的那份安装制品」 |

结论：三套**不是同一值的截断**（① ② 之间不是前缀关系，范围与算法都不同），互相不可校验——与父会话结论一致，本轮从代码定死了各自范围。

### 4.2 权威化方案（一份制品一个主键）
1. **定 ② 为唯一权威标识**（制品摘要 = 包仓库内容寻址主键，已被状态库 PK、激活指针、环境指针共用）。
2. ① 保留但**降级并改名标注**：字段名保持兼容，文档与指针释义统一写「构建目录摘要（16 位，仅供目录命名与构建期去重；不是制品身份）」。不要删字段（`构建平台客户端.py:558/582`、`平台客户端制品.py:341/436/464` 都读它做一致性校验）。
3. ③ **必须补齐主键绑定**：在验证证据 `证据绑定` 里增加 `制品主键`（= ②）与 `全文件摘要算法/范围` 说明，保留 ③ 作为「验证期未改动证据」；`开发工具/HTML验证/多实例制品池.py:38`、`单实例验证.py:43` 的绑定校验从「场景摘要 == 全文件摘要」扩展为「场景摘要 == 全文件摘要 且 制品主键 == 激活指针.制品摘要」。
4. **两份 `当前.json` 键形状统一**（现状：`平台客户端制品/当前.json` = `{摘要sha256, 路径}`；`平台客户端环境/当前.json` = `{摘要sha256, 制品目录, 制品摘要, 版本, 栅栏令牌}`）→ 统一为后者形状（含 `制品摘要`），`构建平台客户端.py:582` 写指针处同步加 `制品摘要` 与 `版本`。
5. 文档侧：`项目说明.md`/`未完成事项.md` 提到制品时**只能写 ② + 版本**，写 ① 时必须带「构建目录摘要」字样，写 ③ 时必须带「验证账本」字样。

### 4.3 现有引用点清单（本轮全仓扫描，按值列出）
- `0eec30284da0d3bd`（①16 位）：`开发文档/未完成事项.md`（审计结论表「发布门禁」行）；`工程缓存/制品仓库/平台客户端制品/当前.json`、`平台客户端制品/平台客户端-0eec30284da0d3bd/制品摘要.json`；`工程缓存/制品仓库/7f68175962d12ca48e01966c9c78676f/制品摘要.json`、`…/物料清单.json`；`工程缓存/制品仓库/平台客户端环境/当前.json`、`平台客户端环境/平台客户端/制品摘要.json`；`平台客户端信任/元数据/快照_92.json`、`信任/元数据/目标_平台客户端_0eec30284da0d3bd.json`、`信任/制品/平台客户端_0eec30284da0d3bd.bin`；`平台客户端状态/权威状态.db`；另有 3 份本次审计临时文档。
- `7f68175962d12ca48e01966c9c78676f`（②32 位）：`平台客户端环境/当前.json`；`平台客户端状态/权威状态.db`（`制品`/`激活指针` 表）；`平台客户端信任/元数据/快照_92.json`、`信任/元数据/目标_平台客户端_0eec*.json`；另有审计临时文档。
- `cdc1b72ae9b8071860ea4f43af8ca534469aa83be52d3d973f5ab6c69e7fbd04`（③64 位）：**只出现在** `工程缓存/HTML验证证据/<该值>/验证证据_1789450384304018000_6512299a6edb.json`（目录名即摘要）与父会话复核报告——**任何指针、任何权威文档都没有引用它**，这正是「无法从指针反推被验证的是哪一份制品」的直接原因。
- 反向证据：`项目说明.md:18` 引用的是 `平台客户端-2ee78ea86b3fb981` + `制品摘要 1d50f2a805b10ca98b09ae80049c5284`（32 位风格），该制品现场不存在 → 文档引用点本身就是失效值，见 10-10d。

---

## 五、结构清理挂载点（可重复执行；不靠人工手删）

**现状（本轮实测）**：`__pycache__` **315** 个（非工程缓存）；`工程缓存/验证运行/` 空目录 **267** 个（0B）；`工程缓存/协作状态/` **0** 项空目录；`工程缓存/提供者运行环境/` 4 个已删包孤儿环境（`pg8000提供者`、`pg8000数据库`、`psycopg2提供者`、`psycopg2数据库`）。

**关键判断**：`__pycache__` 315 个是**审计过程中的批量导入在 13:33 一次性再生**的（子报告 mtime 直方图：2×13:14 / 313×13:33），所以「手动删一次」必然复发。清理必须挂到**构建/门禁收尾**才可重复执行。

| 清理项 | 挂载点（代码落点） | 理由 |
|---|---|---|
| `__pycache__` + `*.pyc`（全仓，排除 `工程缓存/`） | `开发工具/发布门禁/运行发布门禁.py:49 _清理门禁临时目录()`（现已用 `atexit.register` 注册，是门禁唯一的收尾钩子）→ 扩展为 `_收尾清理()`：递归删 `__pycache__` 与 `*.pyc`，并打印删除计数 | 门禁是唯一每次必跑的收尾点；现有钩子已存在，改动面最小 |
| `工程缓存/验证运行/` 空目录 267 | 同上收尾钩子：删 `工程缓存/验证运行` 下**未登记任务 id 的空目录**（`运行发布门禁.py:731` 已有 `当前验证根 = 缓存目录/'验证运行'/当前任务id` 的登记语义，可复用白名单） | 现有门禁已排除「正在使用的验证根」，白名单逻辑现成 |
| `工程缓存/协作状态/` 空目录 | 直接删该空目录（一次性，`ls -A` = 0，无需机制） | 迁移后遗留，不再生成 |
| `提供者运行环境/` 4 个孤儿 | 收尾钩子里加「环境目录 ↔ 现存活包」存在性对账：`工程缓存/提供者运行环境/<X>` 中 X 在正式包索引里不存在 → 回收（`未完成事项.md:281` 已声明 `计算环境摘要(依赖锁, 提供者id)` 只留当前环境的机制，复用同口径） | 已删包的运行环境会随删包回归 |
| 临时文档收口 | 本审计目录 `开发文档/临时文档/20260915_底座专业审计/` 按 `AGENTS.md:198` 在结论并入权威文档后**整目录删除** | 规则明定，收口动作 |

**注意**：`__pycache__` 被 `.gitignore` 覆盖、不影响仓库洁净度（子报告已核）；清理它是为了「门禁/文档声称『已清』能复现」，不是安全问题。

**验证命令**
```bash
cd ~/Documents/Agent/PHP/系统工程平台
find . -name '__pycache__' -type d -not -path './工程缓存/*' | wc -l   # 清理后期望 0
ls -A 工程缓存/验证运行 | wc -l                                        # 期望 0
ls -d 工程缓存/协作状态 2>/dev/null                                     # 期望 无输出
ls 工程缓存/提供者运行环境/ | grep -E 'pg8000|psycopg2'                # 期望 无输出
python3.14 开发工具/发布门禁/运行发布门禁.py                            # 收尾钩子生效（华哥放行后）
```

---

## 六、目录规则 2 处硬违规 → 改名/补号方案

### 6.1 决策记录编号重复 + 缺号（10-07）
现场：`0008_数据库连接池唯一化与anthropic协议.md`、`0008_能力更新走热接入不重启网关.md`、`0009_依赖分两段_第三方翻译层与环境依赖层.md`、`0009_技能库落地.md`；序列缺 `0010`。

规则：`AGENTS.md:199`「编号四位递增且不复用」。方案（**保留已发布编号的正文内容不动，只改文件名**；改前先 `grep -r` 全仓引用）：
| 现文件名 | 改成 | 说明 |
|---|---|---|
| `0008_数据库连接池唯一化与anthropic协议.md` | 保持 `0008_数据库连接池唯一化与anthropic协议.md` | 先落盘者续用 0008（可与 `0009` 组按 git 首次提交时间定序，执行时以 `git log --diff-filter=A --format=%at -- <文件>` 的先者为保留方） |
| `0008_能力更新走热接入不重启网关.md` | `0010_能力更新走热接入不重启网关.md` | 占用空缺的 0010（**优先补号，不跳号**） |
| `0009_依赖分两段_第三方翻译层与环境依赖层.md` | 保持 `0009_…` | 同上先到先得 |
| `0009_技能库落地.md` | `0012_技能库落地.md` | 0011 已被 `0011_模型响应归一化下沉底座.md` 占用，故顺延到 0012 |
- 同批处理英文名：`0008_…anthropic协议.md` 与 `开发文档/交付记录/20260914_数据库连接池与anthropic协议/`（10-20）→ 建议改为 `…与anthropic协议` 保留（`anthropic` 属外部协议名，与 `HTTP/Git` 同级豁免，需华哥一句确认），若判违规则改 `…与模型消息协议`。
- 引用同步：`主开发文档.md` 索引、`开发文档/交付记录/*/验收记录.md`、`未完成事项.md` 里凡写「0008/0009 号决策」的句子必须同步改号。
- 验证：`ls 开发文档/决策记录/ | grep -oE '^[0-9]{4}' | sort | uniq -c` → 期望每个编号恰好 1，且连续无缺号；`grep -rn "0008\|0009" 开发文档/*.md 开发文档/交付记录/`。

### 6.2 归档命名不合 `年月_主题`（10-22）
- 现场：`开发文档/归档/2026-08_第十四阶段/`（内容 = `公开操作契约.json`、`工作包清单.json`、`目录迁移表.json`、`需求快照.json`、`验收契约.json`，**是 JSON 快照不是文档**）；`开发文档/归档/202609_系统工程平台审计/` 合规。
- 方案（二选一，需华哥定）：
  1. **改名**：`2026-08_第十四阶段` → `202608_第十四阶段`（最小改动，只动目录名）。
  2. **改名 + 定性**：若认定「JSON 快照不算归档文件」，则移到 `工程缓存/` 或 `开发文档/项目证据/`，`归档/` 只留文档（更贴 `AGENTS.md:200`「归档只保留一个顶层 `开发文档/归档/`，按 年月_主题 命名」的本意）。
- 引用同步：`主开发文档.md:20` 有 `[归档/](./归档/)` 索引行 → 若采用方案 2，该行同时删除/改写（顺带解决 10-23：同文件 `:79` 明写「归档类文件不得被本文件正文引用」，两条规则互斥——落点是把 `:79` 改为「目录索引行不算正文引用」或删 `:20` 该行）。
- 验证：`find 开发文档/归档 -maxdepth 2 | sort` → 期望只剩 `年月_主题` 形态目录。

---

## 七、对子报告 10 的修正与本轮新增事实（避免下一轮重复误判）

1. **修正**：子报告/父报告称「12 份制品目录每份都在状态账本里留有一条记录」——不成立。`制品` 表只有 **6 行**，其中 **5 行悬空**（32 位摘要对应顶层目录已不存在），**6 份目录无账本行**；另有 `发布` 表 17 行按 16 位摘要登记。清理方案必须按「先账本后磁盘」三步走（§3.2B）。
2. **新增**：12 份副本之所以从不被清理的**代码级根因** = `构建平台客户端.py:475 清理过期制品` 只遍历 `接入.制品根目录`（`工程缓存/制品仓库/` 顶层），而副本书写在子目录 `平台客户端制品/`；且该函数只在 `--安装` 分支被调用。→ 落点②（§3.2C）。
3. **新增**：三套摘要的**算法与范围已从代码定死**（§4.1）：16 位 = 构建制品根目录摘要；32 位 = 包仓库「清单正文」内容摘要（制品主键）；64 位 = HTML 验证期全文件摘要。三者不是截断关系，跨校验必然失败——不要再尝试用截断/前缀去对齐。
4. **新增**：`平台控制面/平台状态.py` **没有删除记录 API**（只有 `写入记录/原子插入/读取记录/查询记录/条件更新/追加证据/查询证据`），所以「同步删账本」必须先决定由谁提供受控删除入口，不能在脚本里裸 sqlite 删。
5. **修正**：`项目说明.md` 记忆能力数在 **335 行**（子报告写 336）；`549 个正式能力` 在 **440 行**（与 `:14` 同源，两处必须同改）。
6. **口径澄清**：`未完成事项.md:16`「能力定义源头 515」与 `:247`「已登记能力 514」、`项目说明.md:14`「549 项能力」、`:16`「装配 498 能力」是**四个不同口径的历史值**，现场实测只有 **源头 536 / 索引能力所有者 536 / 网关 536** 三者一致。这正是 §1 口径表要制度化的原因。

---

## 八、未确认项（本轮未取得证据，不写结论）

| # | 未确认 | 缺什么 |
|---|---|---|
| U1 | 发布门禁当前是否通过（制品 `0eec30284da0d3bd`、提交 `ac21c9f0` vs HEAD `1c69ea5d`） | 冻结边界禁止跑门禁；仅确认指针与制品目录存在 |
| U2 | 11 份非激活件的**全部**引用点 | 全仓 `grep -rl`（不排除工程缓存）超时 180s 未跑完；本轮只取得「16 位摘要：`发布` 表有行 + 开发文档/临时文档各 2-5 个文件命中」这一档证据 |
| U3 | `依赖契约.json` 实际份数 17 与文档 16 的差额 | 本轮未复跑计数命令（子报告采信） |
| U4 | 权威状态库「13.2MB / 8.7MB」指的是哪个物理文件 | 两个候选库文件实测 `294912B(+WAL 4128272B)` 与 `3493888B` 均不匹配；需文档方指明口径 |
| U5 | `归档/2026-08_第十四阶段` 的 JSON 快照是否算「归档文件」 | 需华哥裁定（决定 §6.2 采用方案 1 还是 2） |
