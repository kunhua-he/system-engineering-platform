# 子报告 08 · 死代码 / 空转 / 门禁有效性（原子代理 1800s 超时且零产物，父会话自行重跑）

- 冻结边界：分支 `主干`、HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`
- 方式：**单遍内存扫描**（读取全仓 2691 个文本文件语料，597 个生产 `.py` 逐个判定「点路径 + 末段名」是否有其它文件引用）+ 逐点上文核读 + 正反例现场验算
- 判定纪律：零引用 ≠ 死代码（对外能力、夹具、示例、命令行工具入口都可合法零引用），以下逐条标注类型

## 分级计数：阻断 0 / 重要 2 / 建议 6

---

## 一、未接线的机制（重要，2 条）：写好了、也没人用

| 机制 | 规模 | 现场判定 | 证据 |
|---|---|---|---|
| **反向破坏验证** | 287 行、13 种破坏场景（依赖锁篡改/摘要篡改/类型修改/错误码删除/实现删除/重复能力/循环依赖/提供者崩溃/任务超时/资源泄漏/新版本健康失败/客户端断开/激活指向不存在版本） | **生产代码与测试 0 引用**；`发布门禁` 里 `反向破坏` 只作为目录清单字面量出现（`开发工具/发布门禁/运行发布门禁.py:1513`） | `开发工具/复用审计/反向破坏/破坏验证.py`；父会话扫描 + `grep -rn 反向破坏` |
| **契约一致性校验器** | `公共契约/能力契约/契约.py:45,113` 的 `声明一致 / 校验声明一致` | **生产装配 0 调用点**，所以子报告 03 那 9 类漂移（类型边界失效/位宽失效/返回形态双义…）永远不会被门禁拦住 | `子报告_03`；父会话复核 `grep` |

同类（由其它子报告给出、父会话已抽验）：`网络通信支持库.出站安全.校验出站URL`（SSRF 强校验）0 生产调用方；`进程管理.检测危险命令` 0 执行侧调用方；`权威状态` 的 `清理死亡进程资源/扫描过期租约` 0 生产调用方（`子报告_05`）。

## 二、零引用模块（重要 0 / 建议 1 类，逐条定性）

**真·无任何引用（生产代码、测试、文档全无）**：

| 文件 | 行数 | 定性 |
|---|---|---|
| `运行核心/租约管理.py` | 138 | **疑似被取代**：租约语义已进 `运行核心/权威状态.py`（结构化锁表 + 租约列），本模块 0 引用 → 按「结果唯一即收口」应删或明确标注 |
| `运行核心/加载器/版本系统/兼容适配器.py` | 73 | **死模块**：连同包 `版本系统/` 内部都无引用；其文档字符串自己写着「永久保留无引用适配器」是禁止项 |
| `运行核心/加载器/版本系统/状态切换.py` | 75 | **死模块**：同上，同目录 0 引用 |
| `启动监督器/容量基线.py` | 166 | **死模块**：P1-16 的容量基线/熔断全链（含证据落盘）无任何调用方 |
| `开发工具/统一能力入口/Agent查询/本地协议.py` | — | 死模块（`Agent查询` 入口未接线） |
| `开发工具/复用审计/反向破坏/破坏验证.py` | 287 | 见上表（未接线） |
| `开发工具/说明书生成/运行生成.py` | — | 仅文档提及；生产与测试 0 引用 |
| `开发工具/更新完整性摘要.py` | — | 仅 `开发文档/待重启事项_技能库目录归位.md` 提及 |
| `开发工具/HTML验证/热接入回归.py` | — | 仅 `项目说明.md`/`未完成事项.md` 提及（文档把它当可用工具，实际无调用链） |

**合法的零引用（不算缺陷，列出避免误判）**：
- `技能库/后端/技能库/验证夹具/技能工作区/示范技能/scripts/统计.py`（验证夹具）
- `示例项目/可双击演示/开发文件夹/编译.py`、`…/前端IDE/启动.py`（示例入口）
- `示例项目/适配层示例/运行入口/运行.py`（**由 AGENTS.md:132 命令行调用**，所以「零引用」是正常的；但该入口本身跑不过，见子报告 00 B5）

## 三、空转展示/入口（建议级）

1. **两个模块没有 `__main__`**：`开发工具/契约编译/漂移检测.py`、`开发工具/契约编译/能力定义编译器.py`（`grep -c '__main__'` → `0`）。因此 `python3.14 -m 开发工具.契约编译.漂移检测` **静默 exit 0、什么都不做**——谁把它当门禁就是恒绿空转。合法入口只有 `测试中心/开发工具/测试_漂移门禁`（unittest）与 `开发工具/发布门禁/运行发布门禁.py`。
2. **依赖审计末尾打印空标题**：`开发工具/依赖生命周期审计/运行审计.py` 输出最后一行是 `类别统计：` 后面**没有任何数据**。
3. **5 张恒空的运行域表**：`工程缓存/运行数据/底座运行.db` 的 `会话/发布/检查点索引/缓存索引/灰度状态` 全为 0 行（见 `子报告_09`）。
4. `测试中心/慢速层/`、`测试中心/辅助/` 两个目录**是空壳**（0 文件），而文档/历史契约仍宣称有慢速层与辅助层测试。

## 四、门禁有效性正反例（现场验算「触发条件能否真的触发」）

**父会话亲跑的通过项（真实退出码 0）**：
- `python3.14 -B -m 开发工具.公开调用完整性门禁` → `六环一致，无违规`
- `python3.14 -B 开发工具/依赖生命周期审计/运行审计.py` → `标准提供者 15 个 … 全部通过：无违规`
- 组件合规 112 包 → `未通过 0`
- `python3.14 -B -m unittest 测试中心.开发工具.测试_漂移门禁` → `Ran 2 tests OK`

**父会话亲跑/抽验的漏点（恒空或过宽）**：
- **技能库恒空**：`运行核心/依赖防火墙.py` 里 `技能库` 出现 **0 次**（层名称表/允许依赖表均无）→ 层级规则对第三根包根整体跳过（子报告 04 用正反例实测：技能库→模块库/运行核心放行，支持库→模块库命中）。
- **第三方判据是硬编码清单**：`依赖防火墙.py:57-61` 未收录 psutil/mlx_whisper/tree_sitter/mcp/starlette/uvicorn；正反例实测 psutil 放行、yaml 命中。
- **排除片段过宽**：`开发工具/HTML验证/验证器.py`（发布门禁主判定器）整文件被排除出审计（`子报告_04` E）。
- **发布门禁调用面**：`复用审计` **0 次**、`平台控制面/提供者/防火墙审计` **0 次**（父会话 `grep -c` 实核）；`依赖生命周期审计` 1 次、`依赖防火墙` 3 次、组件合规 4 次、完整性摘要 13 次、六环 4 次（这几条是真接线，纠正子报告 04 的表述）。

## 五、一次性脚本/仓库残留

- 现场**未发现**明显的一次性探针/搬迁脚本残留（`.sh` 两个是正式启动脚本；`MCP工具箱/临时上下文.py`、`支持库/**/临时文件.py`、`平台控制面/发布管理/迁移互斥编排器.py` 都是生产模块，不是临时脚本）。
- 但**审计期间会不断再生**：`__pycache__` 315 个（313 个 mtime=13:33）、空目录 267 个、历史制品 12 份 186M（见 `子报告_10`）→ 说明「清理」是一次性人工动作，没有挂到门禁/构建收尾。

## 六、未确认项

1. 零引用判定用「点路径 + 末段名」字符串匹配；若某模块仅被打包进字符串常量（如插件表）或经动态 `__import__` 装载，会被误判为零引用（现场 `__path__` 0 处、二级相对导入 0 处，风险低但未穷尽）。
2. `运行核心/租约管理.py` 是否已被 `权威状态.py` 完整取代，需读两侧实现逐项对齐后才能定「删」。
3. `开发工具/HTML验证/热接入回归.py` 是否属「有文档、无调用链」的工具，需华哥口径。

## 七、本次执行过的命令（可复现）
```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 /tmp/底座审计_零引用复核2.py          # 单遍内存扫描：2691 语料 / 597 生产模块
grep -c '__main__' 开发工具/契约编译/漂移检测.py 开发工具/契约编译/能力定义编译器.py
python3.14 -B -m 开发工具.公开调用完整性门禁
python3.14 -B 开发工具/依赖生命周期审计/运行审计.py
```
