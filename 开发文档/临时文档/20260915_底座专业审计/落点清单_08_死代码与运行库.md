# 落点清单 08 · 死代码 / 未接线机制 / 空转入口 / 运行库空表与假数据

- 冻结边界：分支 `主干`，HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`；**只读**（SQLite `mode=ro`），未改源码/配置/运行数据/制品/库，未重启服务，未跑发布门禁。唯一写入 = 本文件。
- 输入：`子报告_08_死代码空转与重复实现.md`、`子报告_09_平台控制面治理与运行数据库.md`。
- 本轮做了三件事：① 自己重跑零引用扫描（2458 个文本文件，排除 `工程缓存/开发文档/参考资料/__pycache__/.git`）；② 逐条读实现定「真死代码 vs 对外入口 vs 未接线能力」；③ 把「删 / 接线」落到具体文件的具体位置，并点出共享文件冲突。
- 判定总表：**删 = 6 条**（租约管理 / 兼容适配器 / 状态切换 / 运行库4张表 / 灰度观测假数据行 / 慢速层空壳）、**接线 = 4 条**（破坏验证 / 契约声明一致 / 本地协议入口登记 / 热接入回归归属）、**保留并修 = 3 条**（容量基线二选一、更新完整性摘要、空转入口防护）、**保留 = 1 条**（灰度状态表）。

---

## 一、真·零引用模块（逐条二次证真 + 决策）

| 编号 | 二次证真 | 判定 | 决策与修复落点 | 连带改动 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| 1 | `运行核心/租约管理.py`（138 行）。证真：全仓 2458 个文本文件对 `租约管理` **0 命中**（排除 codegraph 索引库与审计文档）；读实现 = 内存 `dict` 租约表 + `RLock`，绑 `运行核心.句柄体系`（内存句柄），方法 `创建租约/心跳/回收/扫描过期/清理全部/查询/活跃租约数`。对照 `运行核心/权威状态.py`（994 行）：有 `保存租约/保存句柄与租约（同一 SQLite 事务）/租约心跳/读取租约/续租租约/回收租约/扫描过期租约` + `租约` 表（租约id/资源id/项目id/所有者/句柄id/空闲超时秒/硬截止时间/最后心跳/已回收/进程身份键，结构 1.2.0）。真实调用链在 `运行核心/资源协调/句柄服务.py`（:131 保存句柄与租约、:173 续租租约、:286 回收租约）与 `资源协调/服务.py`（:282 精准回收死亡进程锁与租约）。 | **真死代码（职责已被取代）**。判据：7 类语义（创建/心跳/续租/回收/过期扫描/查询/清理）两侧一一对应，差异只在「内存 vs 持久化」；生产调用链只走 `权威状态 + 句柄服务` 一侧，`句柄服务.py` 里 0 处 import 本模块。 | **删** `运行核心/租约管理.py`。若华哥要保留「内存租约」形态，那也必须是新建带测试的能力，不能以零引用文件留在生产目录。 | 无生产引用、无测试引用（`测试中心/` grep `租约管理` 0 命中）；`运行核心/` 下 **无** `完整性摘要.json`（全仓 74 份全部在 `支持库/**`），**不需重算完整性摘要**；`开发工具/公开调用完整性门禁.py:22` 扫描段 = `支持库/后端 + 模块库 + 技能库`，不受影响；组件合规 112 包统计不含 `运行核心/`，不变。 | 否（独占文件） | 删前 `grep -rn "租约管理" --include=*.py . \| grep -v __pycache__ \| grep -v 工程缓存` → 0；删后 `python3.14 -B -m unittest 测试中心.运行核心.测试_资源句柄网关 测试中心.运行核心.测试_句柄账本持久化` 仍绿 |
| 2 | `运行核心/加载器/版本系统/兼容适配器.py`（73 行）。证真：`兼容适配器` 全仓 **0 命中**（仅 `.codegraph/codegraph.db` 索引）。同目录对比：`热切换.py`（被 发布门禁/发布事务/契约编译器/查询入口 引用）、`版本注册表.py`（被 发布门禁/开发入口/查询入口 引用）、`契约兼容.py`（被 查询入口/诊断中心 引用）、`弃用清理.py`（被 本地协议.py 引用）、`灰度指标.py`（被 热切换.py + 测试_灰度指标入库 引用）→ **只有 `兼容适配器.py` 与 `状态切换.py` 无人引用**。本文件 docstring 自己写着禁止项「永久保留无引用适配器」。 | **真死代码**（且自违契约） | **删** `运行核心/加载器/版本系统/兼容适配器.py`。 | `版本系统/` 无 `__init__.py`（命名空间包），删文件不影响其余 5 个模块导入；无测试引用；无摘要需重算。 | 否 | `grep -rn "兼容适配器" --include=*.py . \| grep -v __pycache__ \| grep -v 工程缓存` → 0；`python3.14 -B -m unittest 测试中心.开发工具.测试_契约版本唯一` 仍绿 |
| 3 | `运行核心/加载器/版本系统/状态切换.py`（75 行）。证真：`状态切换` 全仓 **0 命中**（仅 codegraph 索引）。读实现 = `无状态能力表`（前缀匹配）+ `判断有无状态/无状态切换/有状态切换/执行切换`。生产路径对照：`热切换.py` 的 `真实热切换(..., 状态可迁移=True)` 把「状态可迁移」当**入参**（`开发工具/统一能力入口/Agent查询/本地协议.py:177` 传 `状态可迁移=参数.get("状态可迁移", True)` 即证据）→ 本模块的「按能力id 自动判定有/无状态」不在任何调用链上。 | **真死代码** | **删** `运行核心/加载器/版本系统/状态切换.py`。若未来要做「按能力id 自动判定状态」并落进 `真实热切换` 默认值，那是新功能，须带测试再建。 | 同编号 2。另：`热切换.py` 目前默认 `状态可迁移=True`（乐观默认），删除本模块后该默认值失去「另一侧判据」，建议在 `热切换.py` docstring 显式写明「默认乐观、调用方负责判定」，避免下轮审计再判成「重复实现」。 | 否 | `grep -rn "状态切换" --include=*.py . \| grep -v __pycache__ \| grep -v 工程缓存` → 0；`python3.14 -B -m unittest 测试中心.开发工具.测试_漂移门禁` 仍绿 |
| 4 | `启动监督器/容量基线.py`（166 行）。证真：`容量基线` 0 命中（仅 `开发文档/归档/2026-08_第十四阶段/工作包清单.json` 的历史工包记录 + 审计文档）。读实现：**真实采样**（macOS `ps -o rss=`、`pgrep -P`、`/dev/fd`、`os.walk` 求和）、超限处理链、JSON Lines 证据追加、连续超限熔断，全部真实现非桩。 | **未接线机制**（能力完整、0 调用方）——不是死代码，是「没接线的能力」 | **二选一，推荐 A**：<br>**A 接线**：落点 = `启动监督器/健康监督.py` 的巡检循环每轮起始处 —— `采样内存RSS/采样进程数/采样句柄数/采样临时目录` → `容量基线.对比超限()` → 超限走处理链（拒绝/排空/释放/追加证据）→ 连续超限达阈值熔断；证据落 `工程缓存/容量基线证据/`。<br>**B 不接线**：按「无引用即删」移出生产目录 → `开发文档/归档/2026-09_未接线能力/容量基线.py`，并在 `开发文档/未完成事项.md` 挂「P1-16 容量基线熔断未接线」条目（否则下轮审计还会扫到）。 | 接 A：需新增 `测试中心/启动监督器/测试_容量基线接线.py`（正例=超限即熔断、反例=未超限不写证据）。接 B：`启动监督器/` 生产文件只剩 `健康监督.py`；无摘要重算；无测试引用。 | 否（本文件独占；但 A 方案要改 `启动监督器/健康监督.py`） | `grep -rn "容量基线" --include=*.py . \| grep -v __pycache__ \| grep -v 工程缓存` → 0；A 方案验收 `python3.14 -B -m unittest 测试中心.启动监督器.测试_容量基线接线` |
| 5 | `开发工具/统一能力入口/Agent查询/本地协议.py`（244 行）。证真：`本地协议` 0 引用（仅审计文档）——**但读实现后判定它不是死代码**：文件自带 `if __name__ == "__main__": raise SystemExit(主函数())`，docstring 给命令行用法（`echo '{...}' \| python3.14 Agent查询/本地协议.py` 或 `--请求 '{...}'`），`主函数` 认 14 个操作（搜索包/查看版本/查看能力详情/执行热切换/执行回滚/查询卸载条件…），内部 `from 开发工具.统一能力入口.Agent查询.查询入口 import 查询入口` 自装配。兄弟文件 `查询入口.py`（136 行）全仓**只被 `本地协议.py` 引用**→ 二者是「协议入口 + 查询实现」一对，零引用属正常。 | **对外能力/工具入口**（非死代码），但当前**失联**：无文档、无 AGENTS 指引、无门禁、不在 `开发工具/开发入口.py` 子命令表 | **保留 + 补接线（低成本、必做）**：落点二处 —— ① `开发文档/网关调用最小示例.md` 增「Agent 本地查询协议」一节，给一条可复制的 `stdin JSON` 调用；② `开发工具/开发入口.py` 增 `Agent查询` 子命令，让入口可被发现。理由必须写进说明书：它是「Agent 不读源码也能查能力」的标准通道，删了就断掉这条设计意图。 | 无删除连带；补文档后无须重算摘要（`开发工具/` 无 `完整性摘要.json`）。 | 否（但落点在 `开发工具/开发入口.py`，与他人共用） | 只读探针（可跑）：`printf '{"操作":"搜索包","参数":{"关键词":"读取文件"}}' \| python3.14 开发工具/统一能力入口/Agent查询/本地协议.py`；登记验证 `grep -n "本地协议\|Agent查询" 开发工具/开发入口.py` |
| 6 | `开发工具/说明书生成/运行生成.py`（32 行）。证真：`运行生成` 0 引用（仅审计文档）。读实现：`__main__` → `运行核心.加载器.包发现.发现器.发现全部(支持库, 模块库)` → `说明书生成器.写出说明书(..., 系统根/工程缓存/说明书)`。同目录 `说明书生成器.py` 有测试（`测试中心/开发工具/测试_说明书生成器.py`，测的是实现不是本脚本）。 | **对外工具入口**（一键脚本）——非死代码 | **保留 + 登记**：落点 = `开发工具/开发入口.py` 增 `说明书生成` 子命令；`开发文档/主开发文档.md` 的常用命令列出。理由：它是一键全量再生成说明书的唯一入口（`说明书生成器.py` 是库）。 | 无删除连带。**注意**：运行时写 `工程缓存/说明书/`（制品目录），不要并入发布门禁的只读段。 | 否（落点在 `开发工具/开发入口.py`，共用） | `python3.14 -B 开发工具/说明书生成/运行生成.py`（会写 `工程缓存/说明书/`，本轮只读**未跑**） |
| 7 | `开发工具/更新完整性摘要.py`（63 行）。证真：`更新完整性摘要` 仅 `开发文档/待重启事项_技能库目录归位.md` 提及，代码 0 引用。读实现发现两处**实际缺陷**：① `系统根 = Path("~/Documents/Agent/PHP/系统工程平台")` **硬编码绝对路径**；② 文件不存在时只 `print ⚠️ 文件不存在` + `continue`，**不把条目从 `文件清单` 移除** → 删文件后摘要留下幽灵条目，摘要与实盘漂移且无人发现。目标目录 `支持库/模块库/技能库` 正好覆盖全仓 74 份 `完整性摘要.json`。 | **真实在用能力（职责正当）+ 2 处实现缺陷** | **保留 + 修**（不是删）：① 系统根改为 `Path(__file__).resolve()` 上溯探测（与 `运行生成.py` 同款写法）；② 文件不存在时从 `文件清单` 剔除并按需写回，打印「已移除 N 个幽灵条目」；③ 增 `--检查` 只读模式（有漂移返回码非 0），供发布门禁调用。 | 修完须把「摘要与实盘一致」接进发布门禁的 完整性摘要 段（该段现有 13 处调用）。 | **是** —— 它写 `支持库/**/完整性摘要.json`（74 份，能力包权威摘要），且与发布门禁、完整性摘要生成器共享口径 | `python3.14 -B 开发工具/更新完整性摘要.py`（会改文件，本轮**未跑**）；只读版 `python3.14 -B 开发工具/更新完整性摘要.py --检查` |
| 8 | `开发工具/HTML验证/热接入回归.py`（283 行）。证真：`热接入回归` 只在 `开发文档/项目说明.md`、`未完成事项.md`、交付记录、归档审计报告 出现，代码 0 引用。读实现：对真实 `http://127.0.0.1:40007` 网关发黑盒 HTTP（`凭证头 = Bearer html-blackbox-verifier`），演练包 `支持库/后端/热接入演练支持库` 在测试期落地、结束（含异常路径）删除并热接入卸载，`__main__` 有主流程。 | **对外回归工具入口**（非死代码）；文档当它「可用」，但不在任何门禁/unittest 里 → **有文档、无调用链** | **保留 + 定归属（本次给推荐口径）**：登记为**发布前手动回归**（写进 `开发文档/主开发文档.md` 命令段 + `AGENTS.md`），前置条件写明「40007 网关在线、不得与其它热接入测试并发」。**不并入发布门禁**（它真删真建正式支持库目录，进自动门禁风险高）。若要自动化，放 `测试中心/慢速层/`（该目录现为空壳，正是这类慢用例该待的地方，见附条 12）。 | 自动化前需先去掉硬编码（`网关地址 40007` 与 `凭证头` 改为可配置）；无需重算摘要。 | 否（落点在文档 + 可选 `测试中心/慢速层/`） | `python3.14 -B 开发工具/HTML验证/热接入回归.py`（需网关在线，本轮**未跑**） |

---

## 二、未接线机制（给出接线落点 + 预估报错量）

| 编号 | 二次证真 | 判定 | 决策与修复落点 | 连带改动 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| 9 | `开发工具/复用审计/反向破坏/破坏验证.py`（287 行 / `破坏场景表` 13 种）。证真：`破坏验证器/破坏场景表/破坏验证报告` 全仓**只在该文件内出现**（外部 0 引用）。**澄清父报的一处易误读**：`开发工具/发布门禁/运行发布门禁.py:1513` 的 `反向破坏` 是「24 个旧过渡顶层目录不得存在」这段的**目录黑名单字面量**（列表含 加载器/版本系统/契约编译/反向破坏…），与「调用本模块」无关；`开发工具/复用审计/运行审计.py` 只 `from 开发工具.复用审计.能力调用图审计 import 审计模块库`，不引本模块；`测试中心/开发工具/测试_反向破坏门禁28.py`（177 行）是**另一回事**——第二十八阶段图像能力反向断言，与本模块无 import 关系。本文件无 `__main__`（作开关用需补）。 | **未接线机制（真未接线）** | **接线（价值高：13 种破坏就是「门禁被破坏后能否抓住」的负测）**。<br>落点二选一，推荐「非阻断负测」先上：<br>① `开发工具/复用审计/运行审计.py` 尾部加 `--反向破坏` 开关（默认关）；<br>② `开发工具/发布门禁/运行发布门禁.py` 的「复用审计」调用段接一次 —— **顺带修一个既有漏点：`复用审计` 在发布门禁里当前 0 次调用**（父会话 `grep -c` 实核）。<br>首发设为**只报不阻断**（写报告不置红），观测 2–3 轮后再决定是否升阻断。 | **预估报错量（按依赖分层，非猜测性总数）**：13 种里 **9 种**为纯文件/注册表级（依赖锁篡改、摘要篡改、类型修改、错误码删除、实现删除、重复能力、循环依赖、新版本健康失败、激活指向不存在版本）→ 可离线必过；**4 种**依赖真实网关/超时窗口（提供者崩溃、任务超时、资源泄漏、客户端断开）→ 无网关或慢机器上必红。<br>因此首接设计必须是「环境相关 4 项可跳过 + 其余 9 项必过」，否则**首接必红**。<br>另需补 `测试中心/开发工具/测试_反向破坏接线.py`；`破坏验证.py` 需补 `主函数/--全部`。 | **是** —— 落点在 `开发工具/复用审计/运行审计.py` 与 `开发工具/发布门禁/运行发布门禁.py`（两个都在用的门禁主文件，改动须与本批其它条目排同一批，避免撞车） | 接线前 `grep -rn "破坏验证器" . \| grep -v __pycache__` → 仅 1 文件；接线后 `python3.14 -B 开发工具/复用审计/运行审计.py --反向破坏` 应打印 13 项证据与通过数 |
| 10 | `公共契约/能力契约/契约.py:45` `能力实现.声明一致(声明)` 与 `:113` `能力注册表.校验声明一致(包id, 声明列表)`。证真：两名全仓**只出现在 `契约.py` 自身**（`工程缓存/制品仓库/平台客户端制品/*` 与 `工程缓存/编译缓存/*` 里的副本是历史制品，不算调用）。读实现：`声明一致` 比对 `声明参数名序列 == 实现参数名序列` 且 `返回` 字符串相同；`校验声明一致` 返回 4 类问题（声明了但未注册实现 / 声明方与实现方不一致 / 声明参数返回与实现不一致 / 已注册实现但未在声明中列出）。→ 子报告_03 的 9 类漂移正是在这里该拦而无人调。 | **未接线机制（生产装配 0 调用点）** | **接线（这就是"声明与实现一致"的守门函数，不能只当库放着）**。落点二处：<br>① **注册完成点**：`运行核心/能力调用/唯一能力调用.py` 的「懒装配/注册完成」步，对每个装配完的包调 `注册表.校验声明一致(包id, 声明列表)`，非空则写内部错误码（契约声明漂移）并在该包自检/健康里暴露；<br>② **门禁**：`开发工具/发布门禁/运行发布门禁.py` 的「公开调用完整性」段加一条（对全部 `支持库/后端 + 模块库` 包汇总不一致清单）。 | **预估报错量：必须先只报不阻断**。做法：新建只读扫描入口（如 `python3.14 -B -m 开发工具.契约声明漂移扫描`）先出基线清单，再按 子报告_03 的 9 类口径分批收敛 → 白名单 → 归零后升阻断。**首批命中量本轮不做计数**（细分口径以 `子报告_03_契约三产物一致性.md` 为准，本轮只证「0 调用点」与「落点位置」）。<br>另：`工程缓存/制品仓库/平台客户端制品/*/公共契约/能力契约/契约.py` 是制品副本，不受源码改动影响；但客户端制品重新打包后才会带上新调用点。 | **是** —— `公共契约/能力契约/契约.py` 是全能力共用契约；落点在 `运行核心/能力调用/唯一能力调用.py`（唯一调用腿）+ 发布门禁 | `grep -rn "校验声明一致\|声明一致" --include=*.py . \| grep -v __pycache__ \| grep -v 工程缓存` → 当前仅 `契约.py`；接线后应 +`唯一能力调用.py` +门禁 |

---

## 三、空转入口（无 `__main__`、静默 0）

| 编号 | 二次证真 | 判定 | 决策与修复落点 | 连带改动 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| 11 | `开发工具/契约编译/漂移检测.py`（380 行）、`开发工具/契约编译/能力定义编译器.py`（567 行）。证真：`grep -c '__main__'` 两文件均 **0**（现场实测）→ `python3.14 -m 开发工具.契约编译.漂移检测` **静默 exit 0、什么都不做**，谁拿它当门禁就是恒绿空转。合法入口确实存在：`测试中心/开发工具/测试_漂移门禁.py`（父会话亲跑 `Ran 2 tests OK`）、`测试中心/开发工具/测试_能力定义编译器.py`、`开发工具/发布门禁/运行发布门禁.py`。 | **空转展示**（模块本身不是死代码，是「-m 入口」是恒绿陷阱） | **默认：加防护提示，不加 `__main__`**（避免制造第二个「真相入口」）。落点：两文件顶端 docstring 写明「本模块非命令行入口，请走 `测试中心/开发工具/测试_漂移门禁` 或 `开发工具/发布门禁`」，并加 `if __name__ == "__main__":` 分支打印该提示并 `raise SystemExit(2)`（**非 0**），使误用当场变红。<br>**例外分支**：若华哥要把漂移检测做成 CI 可单跑的只读门禁，则加真正 `主函数`（有漂移→1、无漂移→0）并在 `开发工具/开发入口.py` 登记。**两条路二选一，禁止既静默 0 又当门禁。** | docstring/入口改动不影响摘要（`开发工具/` 无 `完整性摘要.json`）；`公开调用完整性门禁`/组件合规只扫 `支持库/后端 + 模块库 + 技能库`，不涉及 `开发工具/`，不受影响。 | 否（两文件独占；例外分支要动 `开发工具/开发入口.py`） | `grep -c '__main__' 开发工具/契约编译/漂移检测.py 开发工具/契约编译/能力定义编译器.py`；`python3.14 -m 开发工具.契约编译.漂移检测; echo $?` → 现 0，改后应为 2 |
| 附 12 | **依赖审计末行空标题**：`开发工具/依赖生命周期审计/运行审计.py`。证真：现场跑 `python3.14 -B 开发工具/依赖生命周期审计/运行审计.py` → 末行确为 `类别统计：` 后面**无任何数据**（前文：`标准提供者 15 个（跳过 2 个非标准目录）`、`全部通过：无违规`）。 | **空转展示（打印 bug：统计字典空/未填充）** | **修**：落点在该脚本的汇总打印处，显式填充 `类别统计`（标准提供者 / 适配提供者 / 跳过 三类计数），并在 `测试中心/开发工具/测试_依赖生命周期审计.py` 加一条断言（输出含数字，非空标题）。 | 无（独占文件）。 | 否 | `python3.14 -B 开发工具/依赖生命周期审计/运行审计.py \| tail -3` → 末行应含数字；`python3.14 -B -m unittest 测试中心.开发工具.测试_依赖生命周期审计` |
| 附 13 | **两个空壳测试目录**：`测试中心/慢速层/`、`测试中心/辅助/`。证真：`find 测试中心/慢速层 测试中心/辅助 -type f` → **0 文件**（两个目录都存在但全空），而历史契约/文档仍宣称有慢速层与辅助层测试。 | **结构残留（空壳）** | **二选一**：<br>① **接线（推荐）**：把编号 8 的 `热接入回归.py` 等慢用例归到 `测试中心/慢速层/`，让目录名副其实；<br>② **删目录**：同时改掉文档里「慢速层/辅助层测试」的表述（否则文档继续宣称不存在的层）。 | 选 ① 需 `测试中心/慢速层/__init__.py`；选 ② 需同步改 `开发文档/` 相关描述。 | 否 | `find 测试中心/慢速层 测试中心/辅助 -type f \| wc -l` → 现 0 |

---

## 四、运行数据库 5 张恒空域表（逐表判定 + 写入侧落点）

**统一背景（本轮新证真，父报未查写入侧）**：`支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py:215` 的 `_创建运行库表` 对 `运行库表定义.items()` **全量遍历建表**，且被三处调用（:253 初始化运行数据库、:310 查询运行态、:346 写入运行态）→ **只要打开运行库就会把 10 个域表全部 `CREATE TABLE IF NOT EXISTS` 建出来**，所以表存在 ≠ 有人在写。这是「5 张恒空」的机制成因。另注 `初始化运行数据库` 的 docstring 写「创建底座运行库**七**个域表」而实现是**十**个（口径不一致，随本次一并修）。

| 编号 | 二次证真 | 判定 | 决策与修复落点 | 连带改动 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| 14a `会话` | 现场 `COUNT(*) = 0`。写入侧证真：`支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py:28` → `默认库路径 = 解析运行数据根(...) / "大语言模型支持库.会话存储.db"`（**独立库**），不写 `底座运行.db`。全仓无任何代码对 `底座运行.db` 的 `会话` 域写入。 | **空转表（同义能力已迁专用库）** | **删域**：从 `提供者.py:158 运行库表定义` 移除 `会话` 条目、`运行库主键字段(:202)` 移除 `会话: 会话id`，并对现有库执行 `DROP TABLE "会话"`（本轮只读，未执行）。<br>附带收益：`会话.压缩次数` 被落成 TEXT 的类型问题（子报告_09 建议 2）随表删除自动消失。 | 表消失会打红 `测试中心/支持库/测试_运行数据库.py:26` 的「十个域」断言（含 `会话`）→ 必须同步改该清单；`写入运行态` 的域白名单校验（:269 `域必须是：…`）报错文案随清单变化；`工程缓存/制品仓库` 里的旧库副本不受影响。 | **是** —— `提供者.py` 是 10 域统一权威，MCP工具箱/协作状态.py、能力占用.py、作业系统.py、任务系统.py、灰度指标.py 全经它 | `sqlite3 -readonly 工程缓存/运行数据/底座运行.db "SELECT COUNT(*) FROM 会话"` → 0；删后 `sqlite3 -readonly … "SELECT name FROM sqlite_master WHERE type='table'"`；`python3.14 -B -m unittest 测试中心.支持库.测试_运行数据库` |
| 14b `检查点索引` | `COUNT(*) = 0`。写入侧证真：检查点走 `检查点.db`（子报告_09 已列：2 个文件引用，检查点恢复包内 + 测试），不写 `底座运行.db`。 | **空转表（已迁专用库）** | **删域**：同 14a（`运行库表定义` + `运行库主键字段` 移除 `检查点索引`，对现有库 `DROP TABLE "检查点索引"`）。 | 同 14a（`测试_运行数据库.py:26` 十域断言含 `检查点索引`）。 | **是** | 同 14a（表名换 `检查点索引`） |
| 14c `缓存索引` | `COUNT(*) = 0`。写入侧证真：嵌入缓存走 `大语言模型支持库.嵌入缓存.db`（`嵌入缓存.py:25 默认数据库路径`），不写 `底座运行.db`。 | **空转表（已迁专用库）** | **删域**：同 14a（并删 `缓存索引`；其 `命中次数` TEXT 问题一并消失）。 | 同 14a。 | **是** | 同 14a（表名换 `缓存索引`） |
| 14d `发布` | `COUNT(*) = 0`。写入侧证真：`平台控制面/发布管理/服务.py` 大量 `self.状态.写入记录("发布", …)` / `条件更新("发布", …)`，而 `self.状态` 是 `平台控制面/平台状态.py` 的 `平台状态(权威状态)` 子类（`平台状态.校验规则表["1.3.0"]["表"]` = 证据/需求/工作包/能力条目/占用租约/制品/信任/策略/发布/激活指针/核心快照，含 `发布` 与 `激活指针`），写的是**平台控制面自己的权威状态库**，不是 `底座运行.db`。→ 同一「发布」域在两个库里同名，口径双义。 | **空转表（同名双义，必须收口）** | **删域**：从 `运行库表定义`（含 `发布`）与 `运行库主键字段(:203 发布: 发布id)` 移除，并对现有库 `DROP TABLE "发布"`。收口后「发布事实」唯一权威 = 平台控制面的 `发布` 表。 | 同 14a；另需在 `平台控制面/发布管理/服务.py` 或平台控制面文档里标注「发布事实唯一落在平台状态库」，防止有人再往 `底座运行.db.发布` 写。 | **是**（跨 提供者.py + 平台控制面口径） | 同 14a（表名换 `发布`）；写入侧证真 `grep -rn '写入记录("发布"' 平台控制面/发布管理/服务.py` |
| 14e `灰度状态` | `COUNT(*) = 0`。写入侧证真（**这张不是空转**）：`运行核心/加载器/版本系统/灰度指标.py:185` `self._写入行("灰度状态", …)`、:139 `self._查询行("灰度状态")`；且有测试 `测试中心/运行核心/测试_灰度指标入库.py:74` 断言 `_库行数("灰度状态") == 1`。0 行只是因为**正式库上还没发生过灰度发布**。 | **保留（已接线，0 行为正常态）** | **保留并标注为「预留但已接线」**：不动表结构（其 `灰度比例/观察窗口秒` 已是 REAL，无类型问题）。若要在文档里解释空表，写「空 = 尚未发生灰度发布，非未接线」，避免下轮审计重复判死。 | 无 | 否（本表已有专属写入/读取链） | `grep -n '灰度状态' 运行核心/加载器/版本系统/灰度指标.py`；`python3.14 -B -m unittest 测试中心.运行核心.测试_灰度指标入库` |

**删除四张表的操作注意**：`_创建运行库表` 是「按字典全量建表」，从字典移除域后**不会自动删已存在的表**，必须对 `工程缓存/运行数据/底座运行.db` 单独 `DROP TABLE`（并对 `工程缓存/制品仓库/**` 及 `平台客户端状态/` 里的历史库副本按需处理）。删除顺序建议：先改字典（止新增）→ 再改测试清单 → 最后 DROP（避免中途门禁红）。

---

## 五、`灰度观测` 里的测试假数据行（逐条定真）

| 编号 | 二次证真 | 判定 | 决策与修复落点 | 连带改动 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| 15 | 现场 `SELECT * FROM 灰度观测` → 唯一一行：`id='验收-灰度-1'`，`创建时间=2026-09-15T02:52:39.363+00:00`，`状态='未开始'`，`载荷={"观测id":"验收-灰度-1","时间":"2026-09-15 11:00:00","能力id":"验收.能力","版本":"1.0.0","成功":true,"耗时毫秒":12.5,"超时":false}`。**关键新证真**：全仓 `grep -rln '验收-灰度-1'` 只命中审计文档两处（`01_总报告.md`、`子报告_09`），**源码里找不到写入者** → 判定为**一次性手工验收命令直接写进了正式运行库**（当时的验收用了真实库路径而非 `tempfile` 临时库），所以无法用 grep 复现入口。 | **重要（真实污染）**：正式运行库留有验收假数据，`灰度指标.py` 的 `聚合()` 是「从库重算」，假行会污染任何「按表统计灰度效果」的下游判断。 | **删行 + 防复发**，落点三处：<br>① 数据侧：`DELETE FROM 灰度观测 WHERE 观测id='验收-灰度-1'`（本轮只读，**未执行**；执行前建议把该行原样存一份到 `开发文档/交付记录/`）。<br>② 防复发（关键）：效仿 `测试_灰度指标入库.py` 的做法，要求所有灰度验收走 `tempfile` 临时库；并在 `测试中心/支持库/测试_运行数据库.py` 增一条断言「正式库不得含测试标识行」（`id LIKE '验收-%'` 或 `能力id LIKE '验收%'` 或 载荷含 `验收` → 直接失败）。<br>③ 口径侧：删行后确认 `灰度指标.聚合()` 输出归零，证明污染确实来自这一行。 | 落点跨「运行数据文件 + `测试中心/支持库/测试_运行数据库.py` + `测试中心/运行核心/测试_灰度指标入库.py`」。注意：**不要**用改测试的方式掩盖（假数据是制品/数据层问题，测试只负责防复发）。 | **是** —— 是运行数据库文件，且落点在两个共用测试文件里 | `sqlite3 -readonly 工程缓存/运行数据/底座运行.db "SELECT COUNT(*) FROM 灰度观测"` → 现 1、删后 0；`grep -rn '验收-' 测试中心 支持库 运行核心 \| grep -v __pycache__` → 期望 0；`python3.14 -B -m unittest 测试中心.支持库.测试_运行数据库` |

---

## 六、共享文件冲突清单（改动必须同批协调）

下列文件是多个条目的落点，改动前必须与同批其它条目对齐，否则互相打红：

| 共享文件 | 涉及编号 | 冲突性质 |
|---|---|---|
| `支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py` | 14a–14e | 10 域建表权威；删域会同时影响 `测试_运行数据库.py` 的十域断言与域白名单报错文案 |
| `开发工具/发布门禁/运行发布门禁.py` | 9、10、11 | 三个条目都要往同一门禁加调用/开关；且现有 `复用审计` 0 调用是共同前置 |
| `运行核心/能力调用/唯一能力调用.py` | 10 | 唯一装配腿，接线点会进全部能力的注册路径 |
| `测试中心/支持库/测试_运行数据库.py` | 14a–14d、15 | 既有「十个域」清单断言 + 待新增「不得含测试标识行」断言 |
| `开发工具/开发入口.py` | 5、6、11 | 三处子命令登记 |
| `开发工具/复用审计/运行审计.py` | 9、附12 | 同一文件既加 `--反向破坏` 开关又修汇总打印 |

---

## 七、本清单保留的未确认项（不编造）

1. `破坏验证.py` 13 种破坏中「提供者崩溃/任务超时/资源泄漏/客户端断开」4 项的具体失败条件（超时秒数、是否要求 40007 在线）本轮只按代码依赖面判定，未逐个读测试实现 → 接线时须先跑一遍取真实基线。
2. 编号 10 的「首批命中条数」本轮**不做估值**：9 类漂移口径以 `子报告_03_契约三产物一致性.md` 为准，本轮只证「0 调用点 + 落点位置」。升阻断前须先出只读基线清单。
3. `工程缓存/制品仓库/**` 与 `平台客户端状态/` 内的历史库副本（含 `发布`/`会话` 等旧表）本轮**未逐库清点**，删域后是否需要同步处理，取决于制品保留口径（与子报告_00 的制品口径一并裁决）。
4. `开发工具/HTML验证/热接入回归.py` 是否并入自动门禁属**华哥口径**问题，本清单只给推荐（手动回归 + 可选 `测试中心/慢速层/`）。

## 八、本次执行过的命令（可复现）

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
# 零引用复核（本轮自跑：2458 个文本文件，排除 工程缓存/开发文档/参考资料/__pycache__/.git）
python3.14 - <<'EOF'   # 见会话内脚本：对 9 个目标模块名做全仓计数，全部 0 命中
EOF
grep -rn --include=*.py "运行核心.权威状态\|运行核心.租约管理\|运行核心.句柄体系" . | grep -v __pycache__
grep -rn "破坏验证器\|破坏场景表\|破坏验证报告" . | grep -v __pycache__ | grep -v "^./工程缓存"
grep -c '__main__' 开发工具/契约编译/漂移检测.py 开发工具/契约编译/能力定义编译器.py
sed -n '1495,1525p' 开发工具/发布门禁/运行发布门禁.py          # 证明 1513 行「反向破坏」是旧目录黑名单
sed -n '215,260p' 支持库/后端/数据库连接支持库/SQLite数据库/实现/提供者.py   # _创建运行库表 全量建表
sed -n '35,130p' 公共契约/能力契约/契约.py                     # 声明一致 / 校验声明一致 实现
python3.14 -B 开发工具/依赖生命周期审计/运行审计.py | tail -6   # 末行「类别统计：」无数据
sqlite3 -readonly 工程缓存/运行数据/底座运行.db "SELECT name FROM sqlite_master WHERE type='table'"
sqlite3 -readonly 工程缓存/运行数据/底座运行.db "SELECT * FROM 灰度观测"
find 测试中心/慢速层 测试中心/辅助 -type f | wc -l              # 0
find 运行核心 开发工具 公共契约 -name 完整性摘要.json | wc -l     # 0（摘要只存在于 支持库/**）
```
