# 子报告 06 · 运行态与网关真实 HTTP 专项审计

> 审计对象：系统工程平台（底座）· 冻结 HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`（主干）· 审计时刻 2026-09-15 13:45–14:02 CST
> 审计方式：进程现场（ps/lsof/launchctl）+ 40007 真实 HTTP 探针 + 8766 MCP 真机协议 + 全仓 mtime/契约比对。全程只读，未重启任何服务、未跑发布门禁、未重编制品、未调外部付费模型。

## 结论（一句话）

**40007 网关骨架跑的是当前源码，但网关内已装配的 `模型连接器` 包（21 个能力）跑的是 13:16 之前的旧实现——契约视图实时读盘所以"看起来是新的"，新参数在调用时被静默丢弃且照样返回 200 成功（阻断级）**；8766 MCP 进程停留在 9-12 的代码上（其后 8 个 MCP 工具箱模块含入口 `项目服务.py` 已改，且从未热重载，同样阻断）；其余网关面（错误码契约、类型铁律、任务三操作、流式 SSE、凭证、审计）在真实 HTTP 下**基本成立但有 4 处契约不齐/语义错位**，另有「40007 唯一网关」不成立、装配能力数 536≠文档 549 两个口径问题。

### 分级计数

| 级别 | 数量 | 编号 |
|---|---|---|
| 阻断 | 2 | F-01 40007 内实现滞后于 HEAD；F-02 8766 进程滞后 3 天 |
| 重要 | 6 | F-03 错误码表不齐/状态码分裂；F-04 KeyError 全局映射致语义错位；F-05「唯一网关」不成立（40006 在跑）；F-06 装配能力数 536 vs 文档 549/498；F-07 CORS 头在代码但生产未授权任何来源；F-08 审计落盘随 TMPDIR 漂移 |
| 建议 | 3 | F-09 错误说明泄漏 Python 原生签名/口径不一；F-10 必填校验先于类型校验；F-11 任务账本内码与网关公开码不一致 |
| 已核实通过 | 17 项 | 见「三、探针矩阵」与「四、文档声称对照」 |

---

## 二、逐条发现

### F-01（阻断）40007 运行进程内已装配的 `模型连接器` 包跑的是旧实现，契约却已是新契约 → 新参数被静默丢弃、仍返回 200 成功

- **级别**：阻断（限定范围：受影响 = `支持库.后端.大语言模型支持库.模型连接器`，21 个能力；网关层与其余 105 个包经 mtime 全仓扫描确认在进程启动后未改动）
- **影响**：
  1. 文档/`未完成事项.md` 里凡涉及 `模型连接器`（含 `重试调度`、`生成对话`、流式链路）的「真实 HTTP 验收通过」结论，**不能按当前 HEAD 的实现口径成立**——运行态跑的是 13:16 之前的实现。
  2. 更危险的是**静默失效**：`能力详情` 从磁盘实时读契约（含新参数 `实际错误码`），但参数过滤走的是装配时（12:42）建立的**内存注册表声明**（不含该参数）→ 新参数在边界被剔除、实现看不到、**请求返回 200 成功**，调用方无法感知参数未生效。
  3. 无任何护栏：`系统底座_指纹校验` 未在 plist 中设置，指纹校验器默认不注入（`后端核心/后端核心.py:226-228`），改了源码不热接入不会被发现。
- **证据（进程-源码时间线）**：
  - `ps -o pid,lstart,command -p 50802` → `Tue Sep 15 12:42:12 2026`（epoch 1789447332）
  - `lsof -a -p 50802 -d cwd -Fn` → `~/Documents/Agent/PHP/系统工程平台`
  - HEAD `1c69ea5d` 提交时间 `2026-09-15 13:32:02 +0800`（即进程启动后 50 分钟）
  - `find . -name "*.py" -newermt "2026-09-15 12:42:12" -not -path "./工程缓存/*" -not -path "./开发文档/*"` → **4 个文件**：
    - `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` mtime `Sep 15 13:16:40`（最后一次提交 `e5692f0e` 13:16:54）
    - `支持库/后端/大语言模型支持库/模型连接器/__init__.py` mtime `Sep 15 13:16:53`
    - `开发工具/组件规范/完整性摘要.py` mtime 13:24:32、`开发工具/契约编译/能力定义编译器.py` mtime 13:24:49（**构建期工具，不在运行进程加载路径内，不影响运行态**）
  - 网关层五个文件 mtime 全部早于 12:42:12：`本地网关.py` 09-15 00:35、`网关核心.py` 01:40、`流式HTTP.py` 11:00、`安全边界.py` 09-08、`启动运行核心网关.py` 09-12 01:33 → **网关骨架 = 当前源码**
  - `lsof -p 50802 | grep -E '\.py$'` → **0 条**：Python 读完源码即关 fd，lsof 无法用于判定「加载的是哪版源码」（方法学说明，勿再依赖 lsof）
- **证据（黑盒行为验证，这是关键）**：`重试调度` 在 `e5692f0e` 中新增可选入参 `实际错误码`（新行为：模式1 按「实际错误码∈白名单」判定命中）。
  - 先取证契约是新的：`POST /网关/调用 {"操作":"能力详情","参数":{"能力id":"大语言模型支持库.模型连接器.重试调度"}}`
    → `入参名= ['重试模式','错误码白名单','最大重试次数','退避基数秒','抖动比例','实际错误码']`、`包版本=1.2.3`（磁盘 `包声明.json` 亦为 1.2.3）
  - 再取行为：`{"能力id":"大语言模型支持库.模型连接器.重试调度","参数":{"重试模式":"normal","错误码白名单":["超时"],"实际错误码":"限流","最大重试次数":3}}`
    → **HTTP 200**，`值 = {"是否重试": true, "下次退避秒": 0.55, "原因": "白名单命中：可重试错误码 ['超时']（上限 3 次）；退避计划=[0.55, 1.1, 2.2]"}`
    → 这正是**旧实现分支**的文案（新实现传 `实际错误码="限流"` 时必须返回 `是否重试=false` + `原因="白名单未命中：实际错误码 限流 不在可重试集合 ['超时'] 内，不重试"`）
  - 反向对照：`{"实际错误码":123}`（新实现应返回 `参数不合法：实际错误码必须是文本`）→ **HTTP 200 成功**，`实际错误码` 被完全忽略
- **机制定位**：
  - `后端核心/后端核心.py:677-689` `能力详情()` 每次请求**实时读盘** `包目录/能力契约/参数契约.json` → 契约视图=新
  - `后端核心/后端核心.py:107-113` 装配在进程启动时扫描 `系统根目录/支持库` 并缓存内存注册表 → 实现与参数声明=旧（12:42）
  - `后端核心/后端核心.py:226-228` 指纹校验开关未设 → 无告警
  - `安全审计.jsonl` 全量检索 **热接入记录数 = 0** → 进程启动后从未热接入（旁证）
- **复现命令**：
  ```bash
  ps -o pid,lstart,command -p 50802
  cd "~/Documents/Agent/PHP/系统工程平台" && find . -name "*.py" -newermt "2026-09-15 12:42:12" -not -path "./工程缓存/*" -not -path "./开发文档/*"
  curl -s -X POST http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8 -H 'Authorization: Bearer ***' -H 'Content-Type: application/json' \
    -d '{"能力id":"大语言模型支持库.模型连接器.重试调度","参数":{"重试模式":"normal","错误码白名单":["超时"],"实际错误码":"限流","最大重试次数":3}}'
  ```
- **状态**：已核实（时间线 + 契约读盘机制 + 黑盒行为三重证据齐）。**未确认**：其余 105 个包未做逐个黑盒，仅以「进程启动后 mtime 无变化」判定为当前源码。

### F-02（阻断）8766 MCP 进程（PID 40029）跑的是 9-12 的代码，落后 HEAD 3 天

- **级别**：阻断（对 MCP 侧一切「真实 HTTP 验收」结论）
- **影响**：`项目服务.py` 等 8 个工具箱模块在其启动后被修改，运行态仍是旧实现；文档中 9-13～9-15 期间对 MCP 行为的验收（作业系统、验证门禁、发布治理、文件租约相关）**不能认定为在当前代码下通过**。
- **证据**：
  - `ps -o pid,lstart,command -p 40029` → `Sat Sep 12 11:27:02 2026`，命令 `python -m MCP工具箱.项目服务 --http --端口 8766`；cwd 同上；`lsof -nP -iTCP:8766 -sTCP:LISTEN` → PID 40029 LISTEN 127.0.0.1:8766
  - `find MCP工具箱 -name "*.py" -newermt "2026-09-12 11:27:02"` → **8 个**
    `验证门禁.py` 09-13 11:29:52、`发布治理.py` 09-13 11:53:16、`文件租约.py` 09-13 12:11:42、`公开能力.py` 09-13 12:14:03、**`项目服务.py` 09-15 01:11:31（入口）**、`作业系统.py` 09-15 01:14:47、`协作状态.py` 09-15 01:14:47、`能力占用.py` 09-15 01:19:00
  - `git log -1 -- MCP工具箱/项目服务.py` → `5093d6e7 2026-09-15 01:14:34`
  - 平台确有热重载机制（`reload_tool_modules` 工具在册），但 `工程缓存/MCP任务观测/事件.jsonl` 全量 4382 条事件中 `reload_tool_modules`/`热重载` 命中 **0 条** → 未热重载
- **复现命令**：`ps -o pid,lstart,command -p 40029`；`cd "~/Documents/Agent/PHP/系统工程平台" && find MCP工具箱 -name "*.py" -newermt "2026-09-12 11:27:02"`
- **状态**：已核实（时间线与文件 mtime）。**未确认**：行为级差异未做黑盒验证（按边界未提交作业、未跑长工具，见「跳过项」）。

### F-03（重要）错误码契约不齐：网关层 18 个对外错误码里 6 个无公开说明、7 个无 HTTP 状态映射（落默认 500），同一「句柄不存在」在不同入口返回 400 与 500

- **级别**：重要
- **影响**：调用方按 `开发文档/网关调用最小示例.md` 的「错误码表」写分支会漏判；`句柄` 语义在两条路径上状态码分裂。
- **证据（静态，网关层四文件扫描）**：
  - `运行核心/统一网关/网关核心.py:64` `公开错误说明表` 共 **19 个码**
  - `运行核心/统一网关/本地网关.py:839-849` HTTP 状态映射表共 **19 个码**，其 `.get(码, 500)` 默认 500
  - 网关层实际会对外返回 **18 个码**，其中
    - **不在公开错误说明表**：`事件负载超限`(流式HTTP.py:96)、`反馈不存在`(本地网关.py:458)、`文件不存在`(网关核心.py:676)、`方法不允许`(本地网关.py:504)、`未知路径`(本地网关.py:336)、`路由不存在`(本地网关.py:492)
      （前三条说明靠显式传入故仍有文案，`未知路径`/`方法不允许`/`路由不存在` 亦显式传说明 → 文案不缺，但**码未登记**）
    - **不在 HTTP 状态映射表**：上述 6 个 + `内部错误`（网关核心.py:682）→ 全部落默认 **500**
  - 公开表里 7 个码代码内零出现：`请求结构错误`、`契约不存在`、`契约版本不兼容`、`调用已取消`、`提供者崩溃`、`资源释放失败`、`版本冲突`（网关层不可达，属表冗余）
- **证据（真实 HTTP，同义不同码）**：
  - 走资源操作：`{"操作":"资源状态","句柄":428101,"参数":{}}` → **HTTP 400**，`错误码="句柄无效"`，`说明="句柄不存在或格式错误"`
  - 走能力入参（模块返回 `句柄失效`，该码不在两张表内）：`{"能力id":"系统核心支持库.进程管理.查询进程状态","参数":{"句柄":999998}}` → **HTTP 500**，`错误码="句柄失效"`，`说明="句柄不存在: 999998"`
  - 流式侧同码：`POST /网关/流式` 句柄 999999 → SSE `失败事件`，`错误码="句柄失效"`，`说明="句柄不存在: 999999"`
- **复现命令**：见上两条 curl（凭证头 `Authorization: Bearer ***`）
- **状态**：已核实（静态表 + 三处真实返回）。**未确认**：`错误说明` 是否会被上游 `脱敏错误信息` 进一步改写（`网关核心.py:223` 原样回带，实测未被改写）。

### F-04（重要）KeyError 全局映射把「任务/包不存在」也报成「能力不存在」

- **级别**：重要
- **影响**：任务域、包域的错误码语义错位，调用方无法区分「能力 id 写错」和「任务 id 不存在/已过期清理」。
- **证据**：
  - `运行核心/统一网关/网关核心.py:671-672`：`except KeyError: self._设置失败(响应, "能力不存在")`，而 `:620` `任务查询` 任务不存在时 `raise KeyError(任务id)`、`:466` `包详情` 包不存在时 `raise KeyError(包id)`
  - 实测：`{"操作":"任务查询","参数":{"任务id":"不存在任务id0000"}}` → **HTTP 404**，`错误码="能力不存在"`，`说明="请求的能力不存在"`
  - 实测：`{"操作":"包详情","参数":{"包id":"支持库.后端.大语言模型支持库"}}` → **HTTP 404**，同样 `能力不存在`（该包确非叶子包，但报错码仍不该是能力域）
  - 对照：`未完成事项.md:91` 已修过同类问题（资源三操作 `能力不存在`→`句柄无效`），**任务/包两条同类路径未一并修**
- **复现命令**：`curl -s -X POST http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8 -H 'Authorization: Bearer ***' -H 'Content-Type: application/json' -d '{"操作":"任务查询","参数":{"任务id":"不存在任务id0000"}}'`
- **状态**：已核实

### F-05（重要）「40007 唯一网关」不成立：40006 网关进程同样常驻 LISTEN，文档零提及

- **级别**：重要（口径/事实核对；不影响 40007 契约本身）
- **影响**：审计前提「40007 唯一网关」与实际不符；多一个常驻监听面（其鉴权与路由策略未在本报告范围，见子报告 07）。
- **证据**：
  - `lsof -nP -iTCP:40006 -sTCP:LISTEN` → `Python 6934 用户 3u IPv4 ... TCP 127.0.0.1:40006 (LISTEN)`
  - `ps -o pid,lstart,command -p 6934` → `Fri Sep 11 20:46:21 2026 ... MCP工具箱/能力网关.py --端口 40006 --地址 127.0.0.1`
  - `~/Library/LaunchAgents/com.huashi.gateway-40006.plist`：`Label=com.huashi.gateway-40006`、`KeepAlive=true`、`RunAtLoad=true`、日志 `/tmp/网关40006.log`
  - `launchctl list | grep huashi` 另有常驻 `com.huashi.gateway-watchdog`(PID 78873)、`com.huashi.base-dict`、`com.huashi.wangzu.frontend` 等
  - `grep -rn "40006" 开发文档/项目说明.md 开发文档/未完成事项.md AGENTS.md` → **0 命中**（文档无记载）
  - 该进程不是第二条执行入口：`POST http://127.0.0.1:40006/网关/调用` → `HTTP 404 {"成功":false,"错误码":"路由不存在","错误说明":"执行入口已迁移到 /网关/调用，未定义路由: /网关/调用"}`；`GET /健康` 亦 `路由不存在`
- **复现命令**：`lsof -nP -iTCP:40006 -sTCP:LISTEN; ps -o pid,lstart,command -p 6934; /usr/bin/plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40006.plist`
- **状态**：已核实（进程/端口/plist；文档零提及为您可裁决项）。**未确认**：40006 的授权用途与所有者（未读其全部实现）。

### F-06（重要）装配能力数：现网 536，文档同时存在 549 / 498 两个数字

- **级别**：重要
- **影响**：文档 `项目说明.md:14`「549 项能力」、`:440`「549 个正式能力全部可作为后台任务提交」、`未完成事项.md:34`「549 项能力」均**非当前运行态**；`:16` 与 `未完成事项.md:38` 的「498 能力」是 08-29 历史快照。
- **证据**：
  - 现网：`GET /健康` → `{"状态":"健康","能力数":536}`
  - 磁盘权威口径：`支持库/模块库/技能库` 下 `能力定义.json` **106 个文件 / 能力条目合计 536**（支持库 91 包、模块库 14 包、技能库 1 包）→ 与现网 536 一致
  - 启动日志（launchd stdout `/tmp/网关40007.log`）最后一段落盘块写的是 **534**（说明这是更早一次启动的输出），文档 `未完成事项.md:55,94` 引用的是 `549`（2026-09-11 索引口径）
  - **注意**：日志文件 mtime 仅 `Sep 15 00:51` 且不含 12:42 这次启动的横幅 → Python 对文件 stdout 采用块缓冲，当前运行进程的启动横幅仍在缓冲区未落盘；**不能以日志判定当前进程**
- **复现命令**：`curl -s http://127.0.0.1:40007/健康 -H 'Authorization: Bearer ***'`；
  `cd 项目根 && unset PYTHONPATH && python3.14 -c "…统计 能力定义.json 的 能力列表 条目…"`（脚本见「命令清单」#12）
- **状态**：已核实。**未确认**：549 与 536 的差额 13 项具体归属（属子报告 01 统计口径范围）。

### F-07（重要）CORS 头在代码里保留，但生产装配未授权任何来源 → 浏览器跨域预检一律 403

- **级别**：重要（若前端依赖同源代理则实际影响有限）
- **影响**：`本地网关.py:275-304` 保留了完整 CORS 预检（204 + `Access-Control-Allow-Origin/Methods/Headers/Credentials/Max-Age`），但 `允许来源表` 默认空集（`安全边界.py:26` 附近 `允许来源表: set = field(default_factory=set)`），而生产启动装配 `运行核心/启动运行核心网关.py:24-35` 的配置字典**未传 `允许来源表`** → 任何跨域预检都 403。
- **证据**：`curl -X OPTIONS -H "Origin: http://127.0.0.1:3000" -H "Access-Control-Request-Headers: authorization,content-type" http://127.0.0.1:40007/网关/调用` → `HTTP/1.0 403`，体 `{"操作":"HTTP边界","成功":false,"错误码":"权限不足","错误说明":"跨域来源未授权"}`（响应头**无** `Access-Control-Allow-*`）
- **复现命令**：同上
- **状态**：已核实（生产未配置来源=空集为静态确定；我未修配置）。**未确认**：是否有其他部署路径（浏览器宿主是否经服务端转发）在实际使用跨域直连。

### F-08（重要/运维）安全审计落盘位置随 `TMPDIR` 漂移，常规路径下看不到

- **级别**：建议偏重要（审计功能本身正常）
- **影响**：`安全审计.默认存储目录()` 在未设 `系统库审计目录` 时取 `tempfile.gettempdir()`；launchd 注入了 `TMPDIR=/var/folders/hx/.../T/`，于是本轮审计文件落在 `/var/folders/hx/.../T/系统级支持库_审计/安全审计.jsonl`（12615 条），而 `/tmp/系统级支持库_审计/安全审计.jsonl` 停在 09-13 14:47（9 条）。排查时按 `/tmp` 找审计会误判「未落盘」。
- **证据**：`ps eww -p 50802` 含 `TMPDIR=/var/folders/hx/4zdx_n0s1tb0t1fl1q9zw4140000gn/T/`；`find /var/folders -name "安全审计.jsonl"` 命中两处；`/tmp/系统级支持库_审计/安全审计.jsonl` mtime `Sep 13 14:47`
- **复现命令**：`ps eww -p 50802 | tr ' ' '\n' | grep TMPDIR; find /var/folders -name 安全审计.jsonl`
- **状态**：已核实

### F-09（建议）错误说明泄漏实现内部文本、且同类错误两套文案

- **影响**：调用方按 `错误说明` 做提示会露出 Python 原生签名与内部函数名。
- **证据**：
  - `{"能力id":"文档读取.结构化读取","参数":{}}` → `说明="能力 文档读取.结构化读取 调用参数缺少必填项：结构化读取() missing 1 required positional argument: '文件路径'"`
  - 同为缺必填，另一能力：`{"能力id":"PDF文本表格.解析PDF","参数":{"最大页数":"abc"}}` → `说明="参数不合法：能力 PDF文本表格.解析PDF 缺少必填参数 文件路径"`（另一套文案）
- **复现命令**：见上
- **状态**：已核实

### F-10（建议）必填参数校验先于类型校验

- **影响**：同时存在「缺必填」与「类型错」时只报前者的，契约测试若只测一种会漏。
- **证据**：`{"能力id":"PDF文本表格.解析PDF","参数":{"最大页数":"abc"}}`（缺 `文件路径` 且 `最大页数` 类型错）→ 只报 `缺少必填参数 文件路径`；补上 `文件路径` 后才报 `参数 最大页数 必须是 整数型`
- **状态**：已核实

### F-11（建议）任务账本内错误码与网关公开码不一致

- **证据**：取消后账本 `工程缓存/任务状态/进程任务/4aebcc563e1c4660.json` → `"错误码": "已取消"`，而网关公开码表用 `调用已取消`（`网关核心.py:632`）；`未完成事项.md` 亦记录过 `已取消`。账本旧条目还有 `已取消/成功/崩溃` 等私有码。
- **状态**：已核实

---

## 三、探针矩阵（真实 HTTP，全部有原始返回，请求id 可在安全审计中检索）

凭证：`~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables.系统库网关凭证 = html-blackbox-verifier`（`launchctl print` 同值），头 `Authorization: Bearer <值>`；路径含中文一律 URL 编码。

| # | 探针 | 请求 | HTTP | 错误码 | 真实返回摘录（截断） | 已核实 |
|---|---|---|---|---|---|---|
| 1 | 健康（带凭证） | `GET /健康` | **200** 0.002s | — | `{"操作":"健康检查","成功":true,"值":{"状态":"健康","能力数":536},"当前版本":"2.0.0"}` | ✅ |
| 2 | 无凭证 | `POST /网关/调用` 无 Authorization | **401** | 权限不足 | `说明="访问凭证缺失或无效"` | ✅ |
| 3 | 错误凭证 | `Bearer wrong-cred` | **401** | 权限不足 | 同上（不泄漏差异） | ✅ |
| 4 | 不存在的能力 | `{"能力id":"不存在的库.不存在的能力"}` | **404** | 能力不存在 | `说明="能力未注册: 不存在的库.不存在的能力"` | ✅ |
| 5 | 未知操作 | `{"操作":"不存在的操作"}` | **400** | 操作不存在 | `说明="请求的操作不在允许操作表内"` | ✅ |
| 6 | 缺必填参数 | `文档读取.结构化读取` 空参数 | **400** | 参数不合法 | 点名 `文件路径`（文案见 F-09） | ✅ |
| 7 | 参数类型不符·文本型 | `文件路径=123` | **400** | 参数不合法 | `说明="参数不合法：能力 文档读取.结构化读取 的参数 文件路径 必须是 文本型"` | ✅ |
| 8 | 参数类型不符·整数型（传文本） | `最大页数="abc"` + 补齐必填 | **400** | 参数不合法 | `…的参数 最大页数 必须是 整数型` | ✅ |
| 9 | 整数型拒逻辑型 | `最大页数=true` | **400** | 参数不合法 | 同上（`true` 不被当作整数） | ✅ |
| 10 | 整数型拒超范围 | `最大页数=2147483648`(2^31) | **400** | 参数不合法 | 同上（32 位边界生效） | ✅ |
| 11 | 双精度数型拒整数 | `OCR识别.版本探针 超时秒=5` | **400** | 参数不合法 | `…的参数 超时秒 必须是 双精度数型`（**不做隐式 int→float**） | ✅ |
| 12 | 双精度数型拒文本 | `超时秒="5.0"` | **400** | 参数不合法 | 同上 | ✅ |
| 13 | 长整数型拒浮点 | `长整数转文本 长整数=1.5` | **400** | 参数不合法 | `…的参数 长整数 必须是 长整数型` | ✅ |
| 14 | 未知字段剔除（顶层+参数内） | `能力搜索` 带 `未知顶层字段乙`、参数 `未知参数甲` | **200** | — | 正常返回 3 条搜索结果（**不报错、被剔除**） | ✅ |
| 15 | 参数非对象 | `{"操作":"能力搜索","参数":"x"}` | **400** | 参数不合法 | `说明="参数必须是对象"` | ✅ |
| 16 | 句柄不存在（资源入口） | `{"操作":"资源状态","句柄":428101}` | **400** | 句柄无效 | `说明="句柄不存在或格式错误"`（**注意：不是 500**，与本任务预设不同） | ✅ |
| 17 | 句柄格式非法 | `句柄="abc"` | **400** | 参数不合法 | `说明="句柄必须是 1 到 999999 的整数，只能使用网关返回值"` | ✅ |
| 18 | 句柄不存在（能力入口） | `系统核心支持库.进程管理.查询进程状态 句柄=999998` | **500** | 句柄失效 | `说明="句柄不存在: 999998"`（状态码分裂，见 F-03） | ✅ |
| 19 | 未知路径 | `POST /网关/请求` | **404** | 未知路径 | `说明="请求路径未授权"`（该码未登记公开表） | ✅ |
| 20 | 方法不允许 | `GET /网关/调用` | **405** | 方法不允许 | `说明="能力执行只支持 POST /网关/调用"` | ✅ |
| 21 | CORS 预检未授权来源 | `OPTIONS` + `Origin: http://127.0.0.1:3000` | **403** | 权限不足 | `说明="跨域来源未授权"`，无 `Access-Control-Allow-*` | ✅ |
| 22 | 任务提交 | `{"操作":"任务提交","能力id":"数据操作支持库.类型转换.长整数转文本","参数":{"长整数":123},"超时秒":60}` | **200** 0.040s | — | `值={"任务id":"4aebcc563e1c4660","状态":"运行中"}` | ✅ |
| 23 | 任务查询 | 同上任务id | **200** | — | `状态=运行中, 进度=0.0, 创建时间=2026-09-15 13:54:50, 取消标记=false` | ✅ |
| 24 | 任务取消 | 同上任务id | **200** 0.010s | — | `值="任务已取消，完整工作进程组终止完成，已确认退出"` | ✅ |
| 25 | 任务查询·不存在任务id | `任务id="不存在任务id0000"` | **404** | **能力不存在** | `说明="请求的能力不存在"`（语义错位，见 F-04） | ✅ |
| 26 | 任务查询·缺任务id | 参数空 | **400** | 参数不合法 | `说明="参数不合法：操作 任务查询 缺少必填字段 任务id"` | ✅ |
| 27 | 流式·能力id 不对 | `POST /网关/流式` 传普通能力 | **404** | 能力不存在 | `说明="流式入口只支持模型连接器生成对话"` | ✅ |
| 28 | 流式·参数非对象 | `参数="x"` | **400** | 参数不合法 | `说明="流式参数必须是对象"` | ✅ |
| 29 | 流式·缺句柄 | 无句柄 | **400** | 参数不合法 | `说明="句柄必须是 1 到 999999 的整数"` | ✅ |
| 30 | 流式·消息列表空 | `消息列表=[]` | **400** | 参数不合法 | `说明="消息列表必须是非空列表"` | ✅ |
| 31 | 流式·流式输出非真 | `流式输出=false` | **400** | 参数不合法 | `说明="流式输出必须是逻辑型真值"` | ✅ |
| 32 | 流式·句柄不存在（真实 SSE） | 句柄 999999 合法参数 | **200** `text/event-stream` | — | `event: 首个事件` → `data:{…"事件序号":1…}`；随后 `event: 失败事件` → `data:{"错误码":"句柄失效","错误说明":"句柄不存在: 999999"}`。**全程未调用外部模型**（无可用句柄即失败退出） | ✅ |
| 33 | 能力目录分页 | `{"操作":"能力目录","参数":{"偏移":0,"限制":20}}` | **200** 0.044s | — | 首屏含 `使用顺序`、`支持库[领域→包(能力数)]`、`总包数/偏移/限制/下一偏移/是否完成` | ✅（未跑完分页，见未确认项） |
| 34 | 能力搜索 | `{"操作":"能力搜索","参数":{"关键词":"","限制":3}}` | **200** 0.004s | — | 返回 3 条 `{能力id,包id,简介}` | ✅ |
| 35 | 能力详情（契约实时读盘） | `生成对话`/`重试调度` | **200** | — | `生成对话`：包版本 `1.2.3`、契约版本 `2.0.0`、参数含 `句柄/消息列表/系统提示词/流式输出/温度/最大令牌数…`；`重试调度`：入参含新增 `实际错误码` | ✅ |
| 36 | 包详情 | `支持库.前端.基础组件描述` → **200**（`版本 1.0.3` + 命令列表）；`支持库.后端.大语言模型支持库` → **404 能力不存在** | 200/404 | — | 见 F-04 | ✅ |

**8766 MCP 真机协议（只读）**

| # | 项 | 结果 | 已核实 |
|---|---|---|---|
| 37 | `POST /mcp/` `initialize`（`Accept: application/json, text/event-stream`） | **200**，`content-type: text/event-stream`，返回 `mcp-session-id`，`serverInfo={"name":"system_engineering_toolkit","version":"1.28.1"}`，`protocolVersion=2025-06-18` | ✅ |
| 38 | `POST /mcp`（无尾斜杠） | **307** → `location: http://127.0.0.1:8766/mcp/`（AGENTS.md/README/opencode.json 均写 `/mcp/`，**文档无错**） | ✅ |
| 39 | `notifications/initialized` | HTTP **202** | ✅ |
| 40 | `tools/list` | **50 个工具**，与文档「47+3=50」**完全一致**；名称见命令清单 #18 | ✅ |
| 41 | `tool_catalog`（工具目录工具） | `条目数 50`、`当前实例可用工具数 50` | ✅ |
| 42 | `project_context`（真调） | 0.73s 返回：代码地图可用（CodeGraph 564 文件/10896 节点/22788 边）、`证据可信度={"分数":50,"等级":"部分可信","提交":"1c69ea5d…","匹配成功记录数":0}`、最近成功验证（09-11 记录） | ✅（**副作用见第八节**） |
| 43 | `mcp_feedback`（可用性抽查） | 以缺字段入参调用 → `{"isError":true,"content":[{"text":"Input validation error: 'dissatisfaction' is a required property"}]}` → 工具在册且服务端响应正常；**未提交真实反馈**（避免写入） | ✅（可用性）/ 未做真实提交 |

---

## 四、与文档声称对照

| 审计项 | 文档声称 | 现场实测 | 判定 |
|---|---|---|---|
| 装配能力数 | `项目说明.md:14` 549；`:440` 549；`未完成事项.md:34` 549；`:17` 制品口径 536；`项目说明.md:16` 历史 498 | `/健康` = **536**；磁盘 `能力定义.json` 106 文件/**536** 条目 | **文档三值并存，现网只有 536 成立**（F-06） |
| 唯一网关 | `网关调用最小示例.md`「/网关/调用 是唯一执行入口」；AGENTS.md 单 MCP | 40007 唯一执行入口 ✅；但 **40006 亦 LISTEN**（无文档） | 执行入口唯一成立；「只有一个网关进程」不成立（F-05） |
| 允许操作表 | `网关核心.py:49-54` 13 项（含 `热接入`） | 未知操作 400 点名，正常操作全通；`热接入` 未实测（避免动运行态） | 一致（`热接入` 未确认） |
| 错误码表 | `最小示例.md` 四条兼容口径：只有「必填缺失」「类型不符」会报错，其余放行 | 未知字段（顶层+参数内）实测放行 ✅；必填/类型实测 400 且点名 ✅ | 一致 |
| 错误码表完整性 | 未给完整码表；`当前流程图.md:44` 称失败码「全部有中文错误说明，来自 公开错误说明表」 | 网关层 18 码中 **6 码不在公开表、7 码落默认 500**；模块域 `句柄失效` 等亦不在表 | **不成立**（F-03） |
| CORS 头 | 代码保留 `do_OPTIONS` 全 CORS 头 | 头在代码 ✅；生产 `允许来源表` 空集 → 预检 403 | 代码保留、生产不可用（F-07） |
| 句柄不存在语义 | `未完成事项.md:77`「无效句柄 500 句柄失效」；`:91`「资源状态 不存在句柄 → 句柄无效」 | 两条都复现：资源入口 **400 句柄无效**、能力入口 **500 句柄失效** | 文档两句各自成立，但**合起来看状态码分裂**（F-03） |
| 任务系统 | 549 能力全部可提交；账本落 `工程缓存/任务状态/任务.jsonl`；`取消` 返回「任务已取消，完整工作进程组终止完成，已确认退出」 | 提交 40ms/查询/取消全通，取消返回文案**逐字一致**；但账本实际新增的是 `任务状态/进程任务/<任务id>.json`（`任务.jsonl` 未追加） | 功能一致；**账本落点描述与实况有差**（F-11 相关） |
| 流式对话 | `未完成事项.md:92` 生成参数三层透传 | 参数校验 5 项逐条通过；真实 SSE 首帧+失败事件正常；**因句柄池无可用句柄未做真模型透传** | 校验层已核实；透传未确认 |
| MCP 工具数 | 50（47+3） | `tools/list` = **50**，逐名比对一致 | 一致 |
| MCP 进程 | `未完成事项.md:52` 重启纪律 + 热加载「可免重启即时生效」 | 进程 09-12 11:27 起，其后 8 模块（含入口）已改，**热重载记录 0** | **运行态陈旧**（F-02） |
| 网关重启历史 | `未完成事项.md:59`「runs = 25、上次退出信号 -15」 | `launchctl print gui/501/com.huashi.gateway-40007` → **runs = 24**、`last terminating signal = Terminated: 15`（SIGTERM）、`state = running`、`pid = 50802`、`forks = 69`、`execs = 2`、`ThrottleInterval 5`、`KeepAlive{SuccessfulExit=false}` | 信号一致；runs 24≠25（文档是当时值） |
| 凭证要求 | 启用凭证后须带 `Authorization: Bearer ***` | 无凭证/错凭证均 401；`/健康` 也强制凭证（`本地网关.py:338` 仅在 `要求凭证=false` 时放行） | 一致（比文档更严） |

---

## 五、命令清单（本次实际执行，可复现）

```bash
# 1 冻结边界与进程
cd "~/Documents/Agent/PHP/系统工程平台"
/Library/Developer/CommandLineTools/usr/bin/git log -1 --format='%H%n%ci%n%s'
/Library/Developer/CommandLineTools/usr/bin/git status --porcelain          # → 仅 ?? 开发文档/临时文档/
ps -o pid,lstart,etime,command -p 50802 -p 40029
lsof -a -p 50802 -d cwd -Fn ; lsof -a -p 40029 -d cwd -Fn
lsof -nP -iTCP:40007 -sTCP:LISTEN ; lsof -nP -iTCP:8766 -sTCP:LISTEN
lsof -p 50802 | grep -E '\.py$' | wc -l          # → 0（lsof 不能判源码版本）

# 2 进程-源码一致性
find . -name "*.py" -newermt "2026-09-15 12:42:12" -not -path "./工程缓存/*" -not -path "./开发文档/*" -not -path "*/__pycache__/*"
stat -f "%Sm %N" 运行核心/统一网关/本地网关.py 运行核心/统一网关/网关核心.py 运行核心/统一网关/流式HTTP.py 运行核心/启动运行核心网关.py 运行核心/任务调度/任务系统.py
# 8766 侧
find MCP工具箱 -name "*.py" -newermt "2026-09-12 11:27:02" -exec stat -f "%Sm  %N" {} \;

# 3 launchd 与凭证
launchctl print gui/$UID/com.huashi.gateway-40007 | sed -n '1,120p'
launchctl list | grep -i huashi
lsof -nP -iTCP:40006 -sTCP:LISTEN ; ps -o pid,lstart,command -p 6934
/usr/bin/plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40006.plist
read ~/Library/LaunchAgents/com.huashi.gateway-40007.plist    # 系统库网关凭证 / PYTHONPATH=. / 无 系统底座_指纹校验

# 4 40007 真实 HTTP（凭证用 plist 值；中文路径 URL 编码）
A='Authorization: Bearer <系统库网关凭证>'; C='Content-Type: application/json'
U='http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8'
curl -s -w '\nHTTP=%{http_code} 耗时=%{time_total}s\n' -H "$A" http://127.0.0.1:40007/健康
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"不存在的库.不存在的能力","参数":{}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"不存在的操作","参数":{}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"文档读取.结构化读取","参数":{}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"文档读取.结构化读取","参数":{"文件路径":123}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"PDF文本表格.解析PDF","参数":{"文件路径":"/tmp/审计06_不存在的文件.pdf","最大页数":"abc"}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"PDF文本表格.解析PDF","参数":{"文件路径":"/tmp/审计06_不存在的文件.pdf","最大页数":true}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"PDF文本表格.解析PDF","参数":{"文件路径":"/tmp/审计06_不存在的文件.pdf","最大页数":2147483648}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"OCR识别.版本探针","参数":{"超时秒":5}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"数据操作支持库.类型转换.长整数转文本","参数":{"长整数":1.5}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"能力搜索","参数":{"关键词":"","限制":3,"未知参数甲":"x"},"未知顶层字段乙":1}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"资源状态","句柄":428101,"参数":{}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"资源状态","句柄":"abc","参数":{}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"系统核心支持库.进程管理.查询进程状态","参数":{"句柄":999998}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{}' http://127.0.0.1:40007/网关/请求            # 404 未知路径
curl -s -H "$A" http://127.0.0.1:40007/网关/调用                                     # 405 方法不允许
curl -s -X OPTIONS -H "Origin: http://127.0.0.1:3000" -H "Access-Control-Request-Headers: authorization,content-type" http://127.0.0.1:40007/网关/调用
# 错误码/任务/流式
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"包详情","参数":{"包id":"支持库.后端.大语言模型支持库"}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"任务提交","能力id":"数据操作支持库.类型转换.长整数转文本","参数":{"长整数":123},"超时秒":60}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"任务查询","参数":{"任务id":"<任务id>"}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"任务取消","参数":{"任务id":"<任务id>"}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"任务查询","参数":{"任务id":"不存在任务id0000"}}' "$U"
S='http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E6%B5%81%E5%BC%8F'
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"文档读取.结构化读取","参数":{"消息列表":[{"角色":"用户","内容":"hi"}],"流式输出":true,"句柄":1}}' "$S"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"大语言模型支持库.模型连接器.生成对话","参数":{"句柄":999999,"消息列表":[{"角色":"用户","内容":"hi"}],"流式输出":true},"最大事件数":3,"最大持续秒":5}' "$S"
# 旧实现取证（F-01 关键）
curl -s -X POST -H "$A" -H "$C" -d '{"操作":"能力详情","参数":{"能力id":"大语言模型支持库.模型连接器.重试调度"}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"大语言模型支持库.模型连接器.重试调度","参数":{"重试模式":"normal","错误码白名单":["超时"],"实际错误码":"限流","最大重试次数":3}}' "$U"
curl -s -X POST -H "$A" -H "$C" -d '{"能力id":"大语言模型支持库.模型连接器.重试调度","参数":{"重试模式":"normal","错误码白名单":["超时"],"实际错误码":123}}' "$U"

# 5 8766 MCP（只读）
curl -s -D - -o - -X POST http://127.0.0.1:8766/mcp/ -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"audit06","version":"1.0"}}}'
curl -s -D - -o - -X POST http://127.0.0.1:8766/mcp -H 'Content-Type: application/json' -H 'Accept: application/json, text/event-stream' -d '{}'   # 307
# 带 mcp-session-id 调 tools/list、tool_catalog、project_context、mcp_feedback（python3.14 + urllib，见第 18/19 条凭据说明）

# 6 静态契约比对
grep -n "公开错误说明表" -A 22 运行核心/统一网关/网关核心.py
sed -n '839,849p' 运行核心/统一网关/本地网关.py
sed -n '275,305p' 运行核心/统一网关/本地网关.py            # do_OPTIONS CORS
sed -n '741,850p' 运行核心/统一网关/本地网关.py            # _处理网关请求
sed -n '639,740p' 运行核心/统一网关/本地网关.py            # 流式
sed -n '677,690p' 后端核心/后端核心.py                     # 能力详情 实时读盘
sed -n '224,232p' 后端核心/后端核心.py                     # 指纹校验开关
sed -n '669,682p' 运行核心/统一网关/网关核心.py            # except KeyError → 能力不存在
# 能力数复算（python3.14，unset PYTHONPATH）
unset PYTHONPATH && python3.14 - <<'EOF'
import json, glob
合计=包=0
for p in glob.glob("支持库/**/能力定义.json",recursive=True)+glob.glob("模块库/**/能力定义.json",recursive=True)+glob.glob("技能库/**/能力定义.json",recursive=True):
    d=json.load(open(p,encoding="utf-8")); 包+=1; 合计+=len(d.get("能力列表") or [])
print("能力定义.json 文件数=",包,"能力条目合计=",合计)     # → 106 / 536
EOF
# 审计与账本
ps eww -p 50802 | tr ' ' '\n' | grep TMPDIR
find /var/folders -name 安全审计.jsonl 2>/dev/null
stat -f "%Sm %N" /tmp/网关40007.log /tmp/网关40007.err 工程缓存/任务状态/任务.jsonl
```

---

## 六、跳过项（按冻结边界主动跳过，非失败）

1. **不重启任何服务**：未执行 `launchctl kickstart`/`bootout`/`bootstrap`，未 kill 任何进程 → 因此「重启后旧实现是否会换新」属推断（会），未实测。
2. **不跑发布门禁、不重编制品**：未执行 `run_release_gate`、未运行任何编译器/摘要生成器 → 因此 549/536 差额的成因未做重编译验证。
3. **不调外部付费模型**：流式与 `生成对话` 未做真实模型透传（仅参数校验 + 无可用句柄的失败路径）。
4. **8766 长工具与作业不触发**：未调用 `validate_module_compliance`、`run_release_gate`、`tool_job_submit` 等 → 8766 陈旧性的**行为级**证据缺失。
5. **未提交任何真实 MCP 反馈**：`mcp_feedback` 只用缺字段入参做可用性探针。
6. **未实测 `热接入` 操作**（会改运行态装配），仅用审计日志 0 条记录 + 黑盒行为反推「未热接入」。
7. **未改任何源码/配置/数据库**，未删任何文件，未读取或修改 40006 的业务数据。
8. **未做 40007 分页全量目录遍历到最后一页**（首屏 + 单页抽样已足以核对结构；全量 536 项与磁盘复算已闭合）。

---

## 七、未确认项（明确列出，不装懂）

1. **8766 运行代码与当前源码的行为差异**：仅以文件 mtime + 热重载记录 0 判定陈旧；`项目服务.py` 09-15 01:11 那批改动（发布治理/作业系统/协作状态/能力占用）的**旧/新行为差**未做黑盒对照（边界禁止提交作业与长工具）。
2. **40007 除 `模型连接器` 外其余 105 包是否逐一同版**：以「进程启动后全仓 `*.py` mtime 无变化」为判据，未做逐包行为指纹比对（无引用级证据；`校验包指纹` 未启用）。
3. **`实际错误码` 静默丢弃的调用面**：是否有上层（V3 等）已在按新契约传该参数、从而正在静默失效，未做跨仓引用扫描（属其他子报告范围）。
4. **F-03 的 500 默认落点是否符合设计意图**：代码 `.get(码, 500)` 是显式默认，但契约文档未声明，属「设计选择 vs 缺口」需裁决。
5. **40006 的用途与授权**（文档零提及）；`com.huashi.gateway-watchdog` 的行为未审（属子报告 05/07 范围）。
6. **549 与 536 的 13 项差额明细**（属子报告 01 统计口径）。
7. **`/tmp/网关40007.log` 缓冲未落盘**：当前进程启动横幅仍在缓冲区，故无法用日志单独确认「本次启动装配了多少能力」；现网 536 取自 `/健康` + 磁盘复算（两者一致）。
8. **分页 `能力目录` 的 `是否完成/下一偏移` 末页终止行为**未跑到最后一页验证。

---

## 八、审计期间产生的写入（如实申报，均为只读审计的不可避免副作用）

| 路径 | 变化 | 原因 |
|---|---|---|
| `工程缓存/任务状态/进程任务/4aebcc563e1c4660.json` | **新建**（421 B，mtime 2026-09-15 13:54:50） | 任务系统探针：提交→取消 `数据操作支持库.类型转换.长整数转文本`（任务系统探针必需；`任务.jsonl` 未追加，其余文件未动） |
| `/var/folders/hx/…/T/系统级支持库_审计/安全审计.jsonl` | **追加**（本报告全部探针请求 + 边界拒绝记录） | 网关安全审计，设计行为 |
| `工程缓存/MCP任务观测/事件.jsonl` | **追加 1 条**（`{"事件":"工具完成","工具":"project_context","时间":"2026-09-15T05:57:58Z"}`） | 8766 `project_context` 探针（开工id `56324e1c6c8c42dd`，未落协作状态账本） |
| 本报告文件 | 新建 | 唯一计划内写入 |

其余：未改源码、未改配置、未删文件、未重编制品、未重启服务、未触碰数据库。
