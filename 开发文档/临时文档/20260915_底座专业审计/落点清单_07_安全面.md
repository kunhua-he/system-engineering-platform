# 落点清单 07 · 安全面（S-01 ～ S-08 第二轮「定真 + 落点」）

> 对象：系统工程平台（底座）｜分支 `主干`｜HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`
> 第二轮边界：**只读**。未改源码/配置/运行数据/制品/数据库，未删文件，未重启服务，未跑发布门禁。唯一写入 = 本文件 + `/private/tmp` 下自造夹具（用后已清理）。
> 上游输入：`子报告_07_安全面.md`（已逐条回读原文）
> 环境：macOS，`python3.14`，跑前一律 `unset PYTHONPATH`；网关 `127.0.0.1:40007` 在本轮期间处于运行态（`lsof` 确认 PID 50802 监听）。

## 零、第二轮结论速览

| 编号 | 一句话 | 判定 |
|---|---|---|
| S-01 | 沙箱 `工作目录` 未转义 → SBPL 注入，网关端到端逃逸，**本轮独立复现成功** | 真（阻断） |
| S-02 | 重定向不做逐跳 SSRF 校验，302 后取回被禁目标内容，**本轮独立复现成功** | 真 |
| S-03 | 环境变量能力明文回显网关自身凭证 + 任意 `.env`，**本轮独立复现成功** | 真 |
| S-04 | 强版 `校验出站URL` 全仓 0 生产调用方（唯一命中是审计文档自身） | 真 |
| S-05 | 凭证字面量出现在 3 个 `.py` + 4 处权威文档 | 真（凭证为本地测试值） |
| S-06 | `检测危险命令` 仅命中命令安全包自身，三条起进程能力均未调用 | 真 |
| S-07 | `解压文件` 签名无上限参数，203956 字节 → 209715200 字节落盘（1028:1） | 真 |
| S-08 | `trust_remote_code=True` 仅 2 处，均在 `模型服务.py`；未加载外部模型 | 真（是否保留需华哥裁决） |

**零误报**：S-01 ～ S-08 八条全部二次证真成立，无一条为误报。

---

## 一、逐条定真与落点

| 编号 | 二次证真（命令 + 真实输出摘要）| 判定 | 修复落点（文件:行号 + 最小改法）| 影响面 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| S-01 | **A 组（注入载荷，经 40007 网关）**：`工作目录 = /tmp/r2_inject/A"` + 换行 + `(allow file-read* (subpath "工程平台根"))` + `(allow file-read* file-write* (subpath "/private/tmp/r2_escape_out"))` + 换行 + `(allow file-read* file-write* (subpath "B` → 输出 `网关成功: True \| 退出码: 0 \| 沙箱: macOS sandbox-exec`；`STDOUT: '# 系统工程平台\n\n与任何业务项---\nWRITE_OK\n'`；`工作区外真落盘: True \| 内容: R2_PWNED`。**B 组（对照，普通工作目录）**：`STDOUT: 'WRITE_DENIED\nhead: …/README.md: Operation not permitted\n'`；`对照工作区外文件存在: False`。**C 组（新增对照：路径含裸双引号但无注入意图）**：`网关成功: True \| 退出码: 65 \| STDERR: 'None' \| 成功执行: False`（profile 语法被破坏，sandbox-exec 退出 65，而网关层仍报“成功”）。三组对照同时证明：逃逸组真落盘、基线真被拒、坏路径真报语法错。 | **真** | ① **`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:245-271`**（`_构建沙箱配置` f-string 直拼整份 SBPL）。最小改法：**不要再插值原始字符串**；在 `:247` 前对入参做 SBPL 字面量转义 `esc = str(p).replace("\\","\\\\").replace('"','\\"')`，且 `:270` 改为 `(allow file-read* file-write* (subpath "{{esc}"))`；同时把函数签名收为 `_构建沙箱配置(工作目录: pathlib.Path)`，从类型上堵住“传字符串即拼文本”。<br>② **`进程管理.py:311-313`**（当前仅 `Path(...).resolve()` + `is_dir()`）。最小改法：`resolve()` 之后加一行硬校验——路径字符串含 `"` 或 `\` 或 `\n` 或 `\r` 或 `)` 即 `return 结果.失败("参数不合法", "工作目录含非法字符", 来源="进程管理")`。这是**最低成本的阻断修复**（单点、不碰 SBPL 文本）。<br>③ **`进程管理.py:331-332`**（`配置 = _构建沙箱配置(str(工作区))` / `argv = ["sandbox-exec","-p",配置,…]`）：若走 ① 的转义方案，此处无需改；若走 ② 的拒绝方案，此处加 `assert` 双保险。<br>④ **profile 语法错误的处理位置 = `进程管理.py:320-326` 之后、`:331` 之前**：应在构造 SBPL 前就完成字符校验（fail-closed 早失败），而不是让 `sandbox-exec` 退出 65 后再由调用方猜（当前 `:359-365` 只在值里标 `成功执行=False`，网关层仍 `成功=True`，即 S-12）。 | 改动落在**进程管理包内**，必须同步：① `支持库/后端/系统核心支持库/进程管理/包声明.json` 版本 `1.1.2 → 1.1.3`；② 同目录 `能力定义.json`（若 `工作目录` 说明补“禁 `"`/换行”）；③ `__init__.py:12` 的能力注册说明（**能力 id 与参数列表不变**，故不产生调用方破坏）；④ `完整性摘要.json` 必须重算（`实现/进程管理.py`、`包声明.json`、`能力定义.json`、`__init__.py` 至少 4 条哈希）——用 `开发工具/更新完整性摘要.py`（硬编码系统根，见 S-14）；⑤ `验证场景.json` / `验证场景引用.json` 需补“注入工作目录被拒”用例；⑥ 无重编译要求（Python 源码包，非制品）；⑦ 调用方 `模块库/直播逐字稿/工具/批量精校队列.py`（唯一直连网关者）不传该能力，**不受连带**；⑧ 不触发依赖锁变更（进程管理 `依赖: []`）。 | **是（高危共享）**：`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py` —— 同时是 **S-06 的修复落点**（同文件，且 S-06 要改的 `:162`/`:289` 与 S-01 要改的 `:289`/`:311-313`/`:331` 在同一函数区），**S-01 与 S-06 必须串行，不可并行**。同包 `包声明.json`/`能力定义.json`/`完整性摘要.json`/`验证场景.json` 也是该包任何并行任务（子报告 08 的死代码/重复实现、子报告 04 的架构边界）的同改文件。 | 复跑本轮 A/B/C 三组脚本（见本节证真列）。**最小证伪**：仅跑 A 组，期望 `工作区外真落盘: False` 且 `STDOUT` 出现 `Operation not permitted` / `WRITE_DENIED`；若仍为 `True` + `R2_PWNED` 则修复无效。补充单点证伪：`python3.14 -c "import sys;sys.path.insert(0,'.');from 支持库.后端.系统核心支持库.进程管理.实现.进程管理 import _构建沙箱配置;print(repr(_构建沙箱配置('/tmp/a\"b')))"` —— 修复后该输入应抛错或输出中被转义，不再是裸 `"`。 |
| S-02 | `unset PYTHONPATH; export no_proxy='*' NO_PROXY='*'`，起本机双 HTTP 服务器（A: 302 → `http://0.0.0.0:{pB}/secret`；B: 返回 `SECRET-INTERNAL-DATA`），直接调 `_校验协议与SSRF`：<br>`1) 直接校验 0.0.0.0: 被拦 -> SSRF防护: 禁止访问内网/保留地址: 0.0.0.0`<br>`4) 直接校验云元数据: 被拦 -> SSRF防护: 禁止访问内网/保留地址: 169.254.169.254`<br>调 `发送请求(地址=http://127.0.0.1:{pA}/, 允许回环=True, 超时秒=5)`：<br>`2) 默认跟随重定向 -> 成功: True \| 值: {'状态码': 200, …, '响应文本': 'SECRET-INTERNA…'}`<br>`3) 显式 跟随重定向=False -> 成功: False \| 值: HTTP 302: Found`<br>第 1/4 步证明平台自己判 `0.0.0.0` 与云元数据为禁目标，第 2 步证明它**仍被取回**，第 3 步证明只有显式关掉才拦住 —— 即“第二跳从未过校验”。 | **真** | **`支持库/后端/网络通信支持库/请求/实现/网络请求.py:132-137`**（`if 跟随重定向 is False:` 分支 + `打开器 = build_opener(*处理程序) if 处理程序 else urllib.request.urlopen`）。最小改法：把 `if 跟随重定向 is False:` 改成 `if/else`，`else` 分支装配一个**逐跳校验重定向器**——`class 校验重定向(urllib.request.HTTPRedirectHandler): def redirect_request(self, req, fp, code, msg, headers, newurl): _校验协议与SSRF(newurl, 允许回环); return super().redirect_request(...)`，并 `处理程序.append(校验重定向())`；这样默认路径（`:140` 行 `urlopen` 兜底）不再有“无守卫”的分支。<br>配套（同文件两处同源缺陷）：**`网络请求.py:198`**（`下载文件`）与 **`网络请求.py:243`**（`上传文件`）各自只有一次前置 `_校验协议与SSRF`，需套用同一逐跳处理器。<br>另一处“责任转移”的文案要同时改：**`网络请求.py:166-168`** 注释把逐跳校验责任推给调用方，应改写为“本能力已内建逐跳校验”。 | 改动落在**网络通信支持库.请求 包内**：① `支持库/后端/网络通信支持库/请求/包声明.json` 版本号需 +1（当前值未在本轮读取，修复时以文件实际值为准）；② `能力定义.json` 参数面**不变**（`跟随重定向` 仍保留），故对外契约不破；③ `完整性摘要.json` 重算（`实现/网络请求.py`、`__init__.py`）；④ `验证场景.json` 补“合法初址 302 → 禁用目标应被拒”用例（**这是现有验证场景的真实盲区**，本轮的 302 复现可直接转成场景）；⑤ 若同时修 **S-04**（把弱版换成强版），两处改同一函数 → 见 S-04 影响面；⑥ 运行侧连带：`127.0.0.1` 自测链路（网关自测脚本、`开发工具/HTML验证/制品进程.py` 走本机端口）必须继续传 `允许回环=True`，否则会从“放行”变“报错”，可能让 HTML 黑盒回归变红；⑦ 无重编译、无依赖锁（不新增依赖）。 | **是（中危共享）**：`支持库/后端/网络通信支持库/请求/实现/网络请求.py` —— **与 S-04 同文件同函数区（`:39-58`、`:94`、`:198`、`:243`）**，S-02 与 S-04 必须串行或合并为一次改动。同包 `包声明.json`/`能力定义.json`/`完整性摘要.json`/`验证场景.json` 与网络通信支持库其他子包任务共享。 | 复跑本轮双服务器脚本。**最小证伪**：第 2 步应为 `成功: False` 且 `错误说明` 含 `SSRF防护`；同时第 1、4 步保持“被拦”，第 3 步保持 `HTTP 302: Found`（证明只收紧了默认路径，没破坏显式禁用路径）。 |
| S-03 | 经 40007 网关（凭证 `html-blackbox-verifier`）：<br>**a) 列出环境变量** → `网关成功: True \| 返回变量数: 17`；`『系统库网关凭证』键存在: True \| 明文==请求凭证: True \| 长度: 22`；`本进程外宿主环境变量数(对照): 152`；`敏感键名命中: []`（本轮网关进程环境内无 `*_KEY/*_TOKEN` 命名的键，故“横向取全部密钥”在本轮运行态下未命中，**核心危害是凭证自举**）。<br>**b) 读取环境文件 `/tmp/r2_audit.env`**（自造假密钥 `R2_API_KEY=sk-fake-r2-1234567890`）→ `网关成功: True \| 返回键: ['R2_API_KEY','NORMAL']`；`R2_API_KEY 明文回显: True`。<br>**对照**：同端点无凭证 → `无凭证 HTTP: 401`。即：**拿网关凭证调一次能力，能把网关凭证原文取回来**。 | **真** | ① **`支持库/后端/系统核心支持库/环境变量/实现/环境变量.py:46-51`**（`列出环境变量`，`:49` 为 `os.environ.items()` 全量列表推导）。最小改法：加一个模块级 `_脱敏(键, 值)`，对键名命中 `KEY/TOKEN/SECRET/PASSWORD/CREDENTIAL/凭证` 的值返回掩码（如 `"***"`），且**只在 `前缀` 参数显式给出时才返回原值**（把“全量明文”变成“需显式点名”）。<br>② **`环境变量.py:53-94`**（`读取环境文件`，`:94` 为 `return 结果.成功结果({"文件存在": True, "条数": len(环境), "环境": 环境})`）。最小改法：`{"环境": {k: _脱敏(k, v) for k, v in 环境.items()}}`，并**拒绝 `.env` 路径落在项目根之外**（越权读取面，当前 `文件路径` 是任意绝对路径、无校验）。<br>③ **网关侧兜底（成功返回值也要脱敏）**：`运行核心/统一网关/网关核心.py:211,223,580,633` 目前只对 `响应.错误说明` 调 `脱敏错误信息`（`安全边界.py:16` 导入）。最小改法：在 `:211`/`:223`/`:580` 附近对 `值` 做同款“敏感键掩码”，复用 `支持库/后端/系统核心支持库/环境变量/实现/环境变量.py` 的同一个 `_脱敏` 函数（**一处实现、两处调用**），避免第二份漂移。<br>④ **接线既有白名单接口**：`运行核心/统一网关/安全边界.py:66-69` 已有 `读取环境变量白名单(允许变量表)`，其 docstring 明写“不允许任意模块读取全部环境变量”——把 `列出环境变量` 的实现改为走该白名单，才算兑现平台自述原则。 | ① **环境变量包**：`支持库/后端/系统核心支持库/环境变量/包声明.json` 版本 +1；`__init__.py:11-12` / `能力定义.json` 的参数与说明可能变（若新增“是否脱敏”开关，则**参数面变化需同步 `能力契约/`、`权限契约/`、`配置契约/` 三份契约**）；`完整性摘要.json` 重算（`实现/环境变量.py`、`包声明.json`、`__init__.py`）；`验证场景.json` 补两条用例（“列出环境变量不含网关凭证明文”、“读取环境文件返回值已脱敏”）。<br>② **网关侧是全局影响面**：`运行核心/统一网关/网关核心.py` 是**全部 536 个能力的唯一出口**，在这里加成功值脱敏会改变**所有能力**的响应体形状 → 必须全量回归（至少在 `开发工具/HTML验证/` 黑盒链路上重跑）；`运行核心/统一网关/安全边界.py` 若新增脱敏函数或被复用，也要算版本（该目录是否成包需现场核 `包声明.json`）。<br>③ **文档**：`开发文档/项目说明.md` 的能力说明（明文示例 `:166` 等）需同步；`开发文档/能力目录说明书.md` 由 `开发工具/说明书生成` 生成，需重生成。<br>④ 与 **子报告 09（凭证/运行数据面）** 高度重叠，`运行数据/权威状态.db` 侧若也含凭证则要一起收口——**跨主题，需串行或明确分工**。<br>⑤ 与 **S-05** 同因：脱敏做完后，S-05 的“凭证随仓库分发”才不再是“取回原文”的直通车。 | **是（高危共享）**：`支持库/后端/系统核心支持库/环境变量/实现/环境变量.py`（S-03 独占，但同包 JSON 与其他“系统核心支持库”子包批量改动共享）；**`运行核心/统一网关/网关核心.py`** —— 与 **S-05（若在网关侧做凭证来源改造）/ S-06（若把危险命令检测做成网关前置策略）** 同文件，任何“网关脱敏/边界/限流”主题任务都会改它，**必须串行**。`开发文档/项目说明.md` 是文档类任务的公共热点。 | 复跑本轮两段 cURL（完整脚本见子报告 07 的 S-03 节，本轮原样跑通）。**最小可跑**：先 `printf 'MY_API_KEY=<自造假密钥>\nNORMAL=1\n' > /tmp/r2_audit.env`，再 `unset PYTHONPATH && python3.14 <S-03 复现脚本>`；脚本内 POST `http://127.0.0.1:40007/%E7%BD%91%E5%85%B3/%E8%B0%83%E7%94%A8`，头 `Content-Type: application/json` + `X-System-Credential: <网关凭证>`，依次调 `系统核心支持库.环境变量.列出环境变量`（参数 `{}`）与 `系统核心支持库.环境变量.读取环境文件`（参数 `{"文件路径": "/tmp/r2_audit.env"}`）。**最小证伪**：取回值里 `系统库网关凭证` 的值 != 请求所用网关凭证原文（修复后应不相等，或该键直接不出现在返回里）；`读取环境文件` 返回值里 `MY_API_KEY` 的值 != 自造假密钥。**回归证伪**：同一凭证调 `系统核心支持库.进程管理.检查命令可用`（参数 `{"命令":"sh"}`）应仍 `成功: True`（证明成功值脱敏没把正常返回值打坏）；无凭证调同端点应仍 `401`。 |
| S-04 | `rg -n '校验出站URL' .`（排除 `工程缓存/`、`.git/`、`__pycache__/`）→ **`.py` 文件零命中**；全部命中都是审计文档自身（`子报告_07:205,218,455`、`01_总报告:73`、`子报告_00:203`、`子报告_08:18`）。代码侧：`支持库/后端/网络通信支持库/出站安全/实现/出站安全.py:101` 定义 `校验出站URL(地址, 解析DNS=True, DNS超时秒=None) -> 结果`（返回 `{允许,原因,地址}`，含云元数据封锁/运营商 NAT/IPv4-mapped IPv6/内嵌凭据拒/DNS 超时/协议白名单）；而 `支持库/后端/网络通信支持库/请求/实现/网络请求.py:39-58` 的 `_校验协议与SSRF` 只有 `scheme in (http,https)` + `:50 socket.gethostbyname` 单次解析 + `is_loopback/is_private/is_link_local/is_reserved/is_multicast`——**无 100.64.0.0/10、无“始终封锁 IP 列表”、无 IPv6 路径**，且校验与连接是两次独立解析（TOCTOU）。 | **真** | **`支持库/后端/网络通信支持库/请求/实现/网络请求.py:94`**（唯一一次校验调用）、**`:198`**（下载）、**`:243`**（上传）。最小改法（推荐“委托”而非“复制”）：把 `_校验协议与SSRF` 的**函数体**改为委托强版——`from 支持库.后端.网络通信支持库.出站安全.实现.出站安全 import 校验出站URL`，内部调 `校验出站URL(地址, 解析DNS=True)`，把 `允许=False` 转成 `raise ValueError(原因)`，回环分支用 `允许回环` 参数显式放行；`_校验协议与SSRF` 的**签名不变**，则 `:94`/`:198`/`:243` 三个调用点一行都不用改（**这是最小改法**）。<br>若要消 TOCTOU：把 `:140` 附近的 `urllib.request.urlopen` 换成“连到已解析并校验过的 IP、Host 头带原主机名”的 opener——**这是较大改动，建议单独立项**。<br>另：`出站安全.py:101` 的 docstring 已写“只做判定，不发起网络请求”，与是否被委托无关，无需改。 | ① **请求包**：`请求/包声明.json` 的 `依赖` 需新增 `支持库.后端.网络通信支持库.出站安全` → **会改变依赖腿/依赖防火墙的判定**，必须核对 `开发工具/依赖生命周期审计`、`开发工具/组件合规` 与“依赖防火墙白名单”（子报告 07 未确认项 7：`运行核心/依赖防火墙.py` 的模块白名单是否被强制**尚未验证**——本改动正好会踩到它，需先确认）。<br>② 同库内跨子包引用（请求 → 出站安全）在**同一次平台编译**中是否允许，需按平台规则确认；若不允许，则退为“把强版逻辑上移到 `网络通信支持库` 公共实现”，影响面扩大到整个网络通信支持库。<br>③ `请求/包声明.json` 版本 +1；`请求/完整性摘要.json` 重算（`实现/网络请求.py` 必有，`包声明.json` 因依赖变必重算）；`出站安全` 包若被改也需 +1 与重算。<br>④ `验证场景.json` 补“0.0.0.0 / 100.64.1.1 / 169.254.169.254 / `file://` / 内嵌凭据 全被拒 + 公网放行”用例（可直接用子报告 S-04 那张 14 用例表）。<br>⑤ 行为连带：`允许回环` 的既有用法（`运行核心/统一网关/安全边界.py:119-120` 预期显式传 `True`）必须仍成立，否则本机能力调用全断。<br>⑥ 与 **S-02** 同一函数区，**必须合并/串行**。<br>⑦ 无重编译（源码包）。 | **是（高危共享）**：`支持库/后端/网络通信支持库/请求/实现/网络请求.py`（与 S-02 冲突）；`请求/包声明.json`（依赖新增会与 **子报告 04 架构边界与依赖腿** 的结论冲突——依赖腿变化必须在两个主题间对齐）；`支持库/后端/网络通信支持库/出站安全/实现/出站安全.py` 与 `运行核心/依赖防火墙.py` 也是跨主题共享点。 | **静态证伪**：`rg -n -g '*.py' -g '!工程缓存/**' -g '!测试中心/**' -g '!**/__pycache__/**' '出站安全' 支持库/模块库` → 修复后应出现 `网络请求.py` 的导入（当前 0 命中）。**行为证伪**：`unset PYTHONPATH && python3.14 -c "import sys;sys.path.insert(0,'.');from 支持库.后端.网络通信支持库.请求.实现.网络请求 import _校验协议与SSRF;` 逐个喂 `http://0.0.0.0:1/`、`http://100.64.1.1/`、`http://[::1]/`、`file:///etc/passwd`、`http://user:pw@127.0.0.1/` → 修复后 `100.64.1.1` 与 `file://` **必须**抛 `ValueError`（当前 `file://` 抛、`100.64.1.1` 视解析结果可能放行）；`http://example.com/` 必须放行。 |
| S-05 | `rg -n 'html-blackbox-verifier' .`（排除 `工程缓存/`、`.git/`、`__pycache__/`）→ **源码 `.py` 仅 3 处**：`./开发工具/HTML验证/热接入回归.py:24: 凭证头 = {"Authorization": "Bearer html-blackbox-verifier"}`（字面量）、`./开发工具/HTML验证/制品进程.py:123: 环境.setdefault("系统库网关凭证", "html-blackbox-verifier")`（兜底）、`./模块库/直播逐字稿/工具/批量精校队列.py:33: 凭证 = os.environ.get("系统库网关凭证", "html-blackbox-verifier")`（兜底）。**权威文档 4 处**：`开发文档/项目说明.md:166,472,509,514`。另有非权威文档 `开发文档/底座升级方案.md:20,69`，以及运行态 plist 里的同值（子报告 06:172 记 `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables.系统库网关凭证`）。网关本体正确：`运行核心/统一网关/安全边界.py:45` 从环境变量取、`:46-47` 缺失即失败。 | **真**（凭证本身是本机测试值，非生产密钥，故不升阻断） | ① **`模块库/直播逐字稿/工具/批量精校队列.py:33`** → 去掉默认值，改 `凭证 = os.environ["系统库网关凭证"]`（KeyError 即明确失败，与 `安全边界.py:45-47` 同口径）。**该文件会被打进平台客户端制品**（子报告 11:1275），所以这是“随制品分发凭证”的源头。<br>② **`开发工具/HTML验证/热接入回归.py:24`** → 改 `凭证头 = {"Authorization": "Bearer " + os.environ["系统库网关凭证"]}`，缺失即抛。<br>③ **`开发工具/HTML验证/制品进程.py:123`** → `setdefault` 的默认值不能再是**固定已知串**；改为“缺失时用一次性随机值注入”（保证“默认凭证不可预测”），并保留 `setdefault` 语义以便黑盒验证仍能注入。<br>④ **`开发文档/项目说明.md:166,472,509,514`** → 明文替换为 `$系统库网关凭证` 占位符，`:472` 的“（当前值 html-blackbox-verifier…）”整段改写为“运行时由 `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 注入，不得写入仓库”。<br>⑤ `开发文档/底座升级方案.md:20,69` 同步占位化。<br>⑥ 真正的“轮换”动作（改 plist 值、改 `模块库` 制品）**属配置变更，本轮禁止**，应作为单独发布任务。 | ① **模块库/直播逐字稿包**：改 `工具/批量精校队列.py` 需动该包 `包声明.json` 版本 + `完整性摘要.json` 重算（该包结构完整：`包声明.json`/`能力定义.json`/`完整性摘要.json`/`验证场景.json` 齐备，本轮已 `ls` 确认）。<br>② **平台客户端制品需重编**（`工程缓存/制品仓库/平台客户端环境/平台客户端`，见 `开发文档/底座升级方案.md:69` 的验证命令）——**重编属发布门禁动作，本轮禁止**，且会连带 `开发工具/更新完整性摘要.py` 与制品指纹。<br>③ **HTML 验证回归链路**：`热接入回归.py`、`制品进程.py` 改动会直接影响 `开发工具/HTML验证/` 的黑盒门禁（子报告 07 的 G-07 正面控制），必须重跑该门禁确认没把验证链打断。<br>④ **文档侧**：`开发文档/项目说明.md` 是多主题共享文档；与 **S-03** 同因（脱敏后才真正止血）。<br>⑤ 与 **子报告 04:194**、**子报告 11:1271,1275** 记录同一点，三份报告结论需一致，不可各改各的。<br>⑥ 不涉及能力定义/契约/依赖锁。 | **是（中危共享）**：`模块库/直播逐字稿/工具/批量精校队列.py`（子报告 04 架构边界、子报告 11 前端与MCP 都已点名同一行）；`开发工具/HTML验证/制品进程.py` 与 `开发工具/HTML验证/热接入回归.py`（HTML 验证门禁任务共享）；`开发文档/项目说明.md`（所有文档类任务共享）。**建议 S-05 单独立项走“凭证轮换 + 占位化”一条线，与 S-03 的脱敏分开提交**，否则一次改动跨 3 个包 + 1 个制品链 + 4 份文档，回滚困难。 | **静态证伪**：`rg -n -g '*.py' -g '!工程缓存/**' -g '!测试中心/**' -g '!**/__pycache__/**' '"html-blackbox-verifier"\|'"'"'html-blackbox-verifier'"'"'' .` → 修复后源码 `.py` **应 0 命中**（文档占位后文档也不该命中）。**行为证伪**：`env -u 系统库网关凭证 python3.14 -c "import sys;sys.path.insert(0,'模块库/直播逐字稿/工具')"` 之类导入/运行入口应明确报“凭证缺失”，而不是静默用默认值连上 40007。**回归证伪**：`开发工具/HTML验证/` 黑盒门禁在注入受管凭证后应仍全绿。 |
| S-06 | `rg -n -g '!工程缓存/**' -g '!.git/**' -g '!**/__pycache__/**' -g '*.py' '检测危险命令' .` → **仅 3 处、全在命令安全包自身**：`./支持库/后端/系统核心支持库/命令安全/__init__.py:5`（导入）、`:8`（`__all__`）、`:16`（能力注册 `系统核心支持库.命令安全.检测危险命令`），加实现 `./支持库/后端/系统核心支持库/命令安全/实现/命令安全.py:145`。**零第三方调用方**。核对三条起进程能力：`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:43`（`启动进程`）、`:162`（`执行命令`）、`:289`（`沙箱执行命令`）——`grep -n "^def"` 全列该文件函数后确认三者函数体内**无任何** `命令安全`/`检测危险命令` 调用；`:296-305` docstring 明写“危险命令检测不在此能力内（那是策略不是机制），由调用方先做”。实现本身有料：`命令安全.py:24` 危险模式列表、`:69` 直接匹配、`:77` 提取解码片段（base64/hex）、`:100` 提取子命令片段、`:110` 展开变量拼接、`:124` `_检测`（带深度）。 | **真**（“实现存在但未接线”成立；实现质量本身不评估） | **方案 A（机制侧，改进程管理）**：`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:162`（`执行命令`）在**参数合法性校验之后、发起 subprocess 之前**插入 `危险 = 检测危险命令(命令)`，非成功即 `return 结果.失败("危险命令", 危险.值或错误说明, 来源="进程管理")`；`:289`（`沙箱执行命令`）在**工作目录校验（`:309-313`）之后、构造 SBPL（`:331`）之前**插同一段（此时也顺带覆盖 S-01 的注入载荷）。`:43`（`启动进程`）走参数列表不经 shell，建议**不默认拦截**，只在新参数 `检查危险命令: bool` 为真时启用。<br>**方案 B（策略侧，改网关，更贴合平台自述）**：在 `运行核心/统一网关/网关核心.py` 的调用管线里加“能力 id → 前置策略”钩子，把 `检测危险命令` 作为 `进程管理.*` 的前置策略装配，**不改进程管理**。优点：`进程管理.py` 不与 S-01 抢同一文件、策略可配；缺点：网关层改动是全局面。<br>**推荐**：若 S-01 与 S-06 排同批，走 **B**（解耦同文件冲突）；若分两批，走 **A** 更直接。<br>另需改 `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:296-305` 的 docstring（“不在此能力内”的表述要么改为“已内建/由网关前置”）。 | ① **方案 A 落在进程管理包**：`进程管理/包声明.json` 版本 `1.1.2 → 1.1.3`（**与 S-01 会撞同一个版本号，必须同一批只 +1 一次**）；`请求/包声明.json` 的 `依赖` 需新增 `支持库.后端.系统核心支持库.命令安全`（进程管理当前 `依赖: []`，见 包声明.json 原文）→ **依赖腿变化，需过 `开发工具/依赖生命周期审计` 与依赖防火墙**；`能力定义.json`/`__init__.py` 若新增 `检查危险命令` 参数则**参数面变化 → 同步 `能力契约/`、`权限契约/`、`配置契约/`**；`完整性摘要.json` 重算；`验证场景.json` 补“`rm -rf` / base64 变体 / `$(...)` 子命令 被拒”用例。<br>② **行为回归是重点**：仓库里真实调用 `执行命令`/`沙箱执行命令` 的链路（`开发工具/HTML验证/制品进程.py`、`模块库/直播逐字稿/工具/*`、任何 CLI 包装）若传的命令含 `rm`/`chmod`/管道到 shell 等模式，会在修复后**由成功变失败**——必须先把现有调用点全量 grep 出来逐个确认。<br>③ **方案 B 落在网关**：改 `运行核心/统一网关/网关核心.py`（**与 S-03 同文件，必须串行**），且策略需在 `安全边界.py`/策略中心登记；网关是全局出口，需全量回归。<br>④ 无重编译（源码包）。<br>⑤ 与 **子报告 08「死代码空转与重复实现」:18** 直接重叠（该报告把这条列为“0 调用方”同类）——两个主题会同时盯 `命令安全` 包，需统一口径。 | **是（高危共享）**：若走方案 A，**`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py` 与 S-01 完全同文件、同函数区**（S-01 改 `:245-271`/`:311-313`/`:331`，S-06 改 `:162`/`:289`），且**两者都会把 `包声明.json` 版本 +1**——并行必然产生版本号与摘要双重冲突，**必须串行或合批**。若走方案 B，则与 `运行核心/统一网关/网关核心.py` 的 **S-03** 冲突。同包 `包声明.json`/`能力定义.json`/`完整性摘要.json`/`验证场景.json` 与子报告 08 主题共享。 | **静态证伪**：`rg -n -g '*.py' -g '!工程缓存/**' -g '!**/__pycache__/**' '检测危险命令' .` → 修复后应**不再只有命令安全包自身**（方案 A 命中 `进程管理.py`；方案 B 命中网关侧策略装配文件）。<br>**行为证伪（最小真实证据）**：经 40007 调 `系统核心支持库.进程管理.沙箱执行命令`，参数 `{"命令":"rm -rf /private/tmp/r2_should_not_run","工作目录":"/private/tmp/r2_danger"}`（先 `mkdir` 该目录 + 放一个哨兵文件）→ 修复后应 `成功: False` 且哨兵文件**仍在**；对照：`{"命令":"echo ok"}` 应仍 `成功: True`。命令安全能力自身的正确性另用 `检测危险命令` 直调 20+ 模式验证（不进本轮边界）。 |
| S-07 | `inspect.signature(解压文件)` → `(源路径: 'str' = None, 目标目录: 'str' = None) -> '结果'`（**参数表里没有体积/条目数上限**）。实测：用 `zipfile.ZIP_DEFLATED` 造含 `b"\0"*(200*1024*1024)` 的 `bomb.bin` → `压缩包磁盘大小: 203956 字节 ; 解压后预计: 209715200 字节`；调 `解压文件(源路径=z, 目标目录=基+"/out")` → `返回: True {'目标目录': '/tmp/r2_zbomb/out', '条目数': 1} 耗时0.10s 落盘字节: 209715200`；`压缩比: 1028.2 : 1`。对照代码：`支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py:94` 注释写“防 zip 炸弹：路径穿越防护”，`:95-96` 只做前缀校验，`:98 压缩包.extractall(目标目录)` **无任何上限**；同平台 `运行核心/运行环境管理器/远程镜像.py:434,446` 的 tar 解包有 `最大字节数` 上限 —— 口径不一致。 | **真** | **`支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py:82-98`**（`解压文件` 全函数）。最小改法三步：<br>① `:82` 签名加两个可选参数 `最大字节数: int = None`、`最大条目数: int = None`（默认常量如 `默认解压上限字节 = 1 * 1024**3`、`默认解压条目上限 = 10000`，与 `远程镜像.py` 同风格）；<br>② `:92-98` 把 `压缩包.extractall(目标目录)` **换掉**——改为先遍历 `压缩包.infolist()` 累计 `成员.file_size` 与条目数，**超限直接 `结果.失败("超过解压上限", …)` 并且一行都不落盘**（fail-fast，比“边解边看”更干净）；再逐成员 `open()` + 分块 `shutil.copyfileobj` 落盘（同时天然获得“写盘中途可中止”的能力）；<br>③ `:94-96` 顺手把 `目标.startswith(os.path.normpath(目标目录))` 改成 `Path(目标).resolve().is_relative_to(Path(目标目录).resolve())`（正确写法仓库里已有：`运行核心/运行环境管理器/远程镜像.py:386,394`），并把 `:94` 注释写清“同时覆盖 zip 炸弹与路径穿越”——**这一条与 S-09 是同一处代码，合并修**。 | ① **文件系统支持库.文件操作包**：`文件系统补充.py` 是包内实现 → 改它必须动 `支持库/后端/文件系统支持库/文件操作/包声明.json` 版本、`支持库/后端/文件系统支持库/文件操作/完整性摘要.json` 重算。<br>② **参数面变化要同步契约**：`文件系统支持库/文件操作/__init__.py:62` 的能力注册里 `解压文件` 参数表当前是 `[{"名称":"源路径","类型":"文本型","必填":True},{"名称":"目标目录","类型":"文本型","必填":True}]` —— 新增两个可选参数后，`能力定义.json` 与 `能力契约/`、`权限契约/`、`配置契约/` 都要同改；`__init__.py:30,38` 的导出列表不用改。<br>③ **调用方零外链但要看制品链**：全仓 `.py` grep `解压文件` 只命中该包自身 `__init__.py:30,38,62`（本轮实测），**没有第三方 Python 调用方** → 直接调用侧风险低；但平台控制面/包仓库的**装包链路**（若经 java/前端或能力 id 调用 `文件系统支持库.文件操作.解压文件`）需按能力 id 再 grep 一次 JSON/契约确认，默认上限不能把既有制品包解压卡死（`工程缓存/制品仓库` 里的制品体积需核对）。<br>④ `验证场景.json` 补两条：压缩比炸弹被拒（不落盘）、条目数超限被拒。<br>⑤ 与 **S-09 同一函数**，必须合并一次提交。<br>⑥ 无重编译（源码包）。 | **是（中危共享）**：`支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py` —— **与 S-09 完全同函数（`:82-102`，S-09 点名 `:95-97`）**，必须串行或合并。同包 `包声明.json`/`能力定义.json`/`__init__.py`/`完整性摘要.json`/`验证场景.json` 与文件系统支持库其他任务共享。对照文件 `运行核心/运行环境管理器/远程镜像.py`（`:386,394,434,446`）若做口径统一，与“运行核心/制品/发布”主题共享。 | 复跑本轮 zip 炸弹脚本。**最小证伪**：`返回:` 必为 `False` 且 `值`/`错误说明` 含“上限/超限”字样，`落盘字节` 应为 `0`（fail-fast）或远小于 209715200；对照：用正常小包（如 `压缩文件` 产生的 100KB 包）解压应仍 `返回: True` 且内容完整（证明没把正常路径打死）。**边界证伪**：紧贴上限的包（如 1GB-1 字节）应通过、超 1 字节应拒绝——证明比较符方向正确。 |
| S-08 | `rg -n 'trust_remote_code' .`（排除 `工程缓存/`、`.git/`）→ **主仓 `.py` 仅 2 处**，均在 `./支持库/适配层/模型服务.py`：`:29 self.分词器 = AutoTokenizer.from_pretrained(self.模型路径, trust_remote_code=True, padding_side="left")`、`:31 self.模型 = 类.from_pretrained(self.模型路径, trust_remote_code=True, torch_dtype=self.数据类型).to(self.设备).eval()`。其余命中全在 `开发文档/参考资料/外部源码研究/**`（第三方项目文档，非本仓代码）。`sed -n '20,40p'` 确认两行都在 `def 加载(self) -> None:`（`:25` 起）内，`类 = AutoModelForCausalLM if self.模型类型 == "重排" else AutoModel`（`:30`）；`:31` 的 `.eval()` 是 `torch.nn.Module.eval()` 推理模式切换，**不是内建 `eval`**（与子报告 G-02 一致）。**本轮未加载任何外部模型仓 → “是否已被利用”不成立，无法判定。** | **真**（代码确证）；「是否保留 `trust_remote_code`」**需华哥裁决** | **`支持库/适配层/模型服务.py:29,31`**。最小改法：把 `self.模型路径` 与 `trust_remote_code` 一起收进“模型清单”配置——<br>① `:29`/`:31` 的 `trust_remote_code=True` 改为 `trust_remote_code=self.允许远程代码`（默认 `False`），并在 `:25` `加载()` 开头加白名单校验：`模型路径` 的 repo 名 + `revision`（固定 commit hash）必须在 `允许模型表` 内，否则 `raise ValueError`；<br>② 对确实需要远端代码的模型（若本机在用的 Qwen3-Embedding / bge-reranker 之类需要），在模型清单里**显式 allowlist 该 repo@revision**，把“默认信任”翻转为“默认拒绝 + 点名放行”；<br>③ 更彻底（可选、非本批）：把 HF 加载放到独立进程（参照 `运行核心/加载器/提供者隔离/独立进程.py` 已有模式），让远端代码不落在底座进程内。<br>**裁决点（需华哥定）**：本机现役模型是否依赖 `trust_remote_code`？若依赖，改默认 `False` 会导致模型加载失败——**必须先现场核本机模型来源，再决定“默认 False + allowlist”还是“保留 True 但加 revision pin 与来源校验”**。 | ① **`支持库/适配层/模型服务.py` 不属任何 `包声明.json` 覆盖**：本轮 `find 支持库/适配层 -maxdepth 2 -name 包声明.json` 只列出 `Tesseract提供者`、`Git提供者`、`Pillow提供者`、`MLXWhisper提供者`、`FFmpeg提供者`、`LibreOffice提供者`、`YAML提供者`、`reportlab提供者`、`pdfplumber提供者`、`psycopg提供者` 等**各提供者子目录**，`适配层` 根目录下没有 `包声明.json`/`能力定义.json`/`完整性摘要.json` → **不产生包版本、能力定义、摘要级联**（这是本条影响面比 S-01/S-07 小的原因）。<br>② 但仍需确认 `适配层` 是否被某个聚合摘要/index 扫描（`开发工具/更新完整性摘要.py` 的 `目标目录 = ["支持库","模块库","技能库"]`，只扫“含 `完整性摘要.json` 的包”，故 `模型服务.py` 不在其内）——修复时顺带跑一次该工具确认无意外哈希变化。<br>③ **调用方连带**：任何 `from 支持库.适配层.模型服务 import …` 的模块（嵌入/重排能力、本地模型服务）在默认 `False` 后会加载失败 → 必须先全量 grep 调用点，并把 `允许远程代码`/模型清单注入到位。<br>④ **配置面**：模型清单/revision 属配置，改动前需明确回退方式（`git checkout` 单文件 + 清单文件版本）；**本轮禁止改配置**，因此本条只能“定真 + 落点”，落地动作留待华哥裁决。<br>⑤ 与 **子报告 09（运行数据/凭证面）** 无直接重叠；与 `运行核心/加载器/提供者隔离` 主题（可选方案 ③）重叠。 | **否（本条独占）**：`支持库/适配层/模型服务.py` 在本轮 8 条中**只有 S-08 一处落点**，未被任何其他 S-0x 点名；同文件也非其他子报告的改点（子报告 04/08/11 均未点 `模型服务.py`）。**风险点**：它与 `运行核心/加载器/提供者隔离/独立进程.py`、`支持库/适配层/*提供者/` 各包同属“适配层/模型服务”主题，若另开“模型服务治理”任务会撞车。模型清单文件若被多主题引用也需串行。 | **静态证伪**：`rg -n -g '*.py' -g '!工程缓存/**' -g '!测试中心/**' 'trust_remote_code' 支持库 模块库 运行核心` → 修复后应**不再出现 `trust_remote_code=True` 字面量**（应为 `trust_remote_code=self.允许远程代码` 之类）。<br>**行为证伪（最小真实证据，不加载外部模型）**：`unset PYTHONPATH && python3.14 -c "import sys;sys.path.insert(0,'.');from 支持库.适配层.模型服务 import *;print('import ok')"` 应仍可导入（证明改动没打破模块级语法/导入）；进一步：构造 `模型服务(模型路径='/tmp/not_allowlisted')` 并调 `加载()`，期望在**联网之前**就 `raise ValueError`（证明 allowlist 生效），**不要**真去 `from_pretrained` 一个外网仓库。 |

---

## 二、共享文件与串行要求

### 2.1 必须串行的文件级冲突（并行会直接产生 diff 冲突或版本号/摘要冲突）

| 冲突组 | 共享文件 | 涉及编号 | 冲突性质 | 处理建议 |
|---|---|---|---|---|
| **组 A（同文件同函数区）** | `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py` | **S-01 + S-06** | S-01 改 `:245-271`、`:311-313`、`:331`；S-06 改 `:162`、`:289`。两处都在“起进程/沙箱”区域，且**两处都会把 `进程管理/包声明.json` 从 `1.1.2` +1**，并行必然版本号撞车 + `完整性摘要.json` 双向覆盖 | **合并为一次提交**（一次 +1 到 1.1.3，一次重算摘要）；或 S-06 改走“方案 B 网关前置策略”以解耦 |
| **组 B（同文件同函数区）** | `支持库/后端/网络通信支持库/请求/实现/网络请求.py` | **S-02 + S-04** | S-02 改 `:132-137`（重定向处理程序），S-04 改 `:39-58`/`:94`/`:198`/`:243`（校验实现）。S-04 的“委托强版”会改写 `_校验协议与SSRF` 函数体，S-02 要用它做逐跳校验 → **S-04 必须先落地，S-02 在改好的基础上接逐跳** | **严格串行：先 S-04 后 S-02**（S-02 依赖 S-04 的结果，反之 S-04 落地时 S-02 的行号会漂） |
| **组 C（同文件）** | `支持库/后端/文件系统支持库/文件操作/实现/文件系统补充.py` | **S-07 + S-09** | 两者都改 `解压文件`（S-07 改 `:82-98` 签名+落盘，S-09 改 `:95-97` 遍历校验）——**同一函数，同一批** | **合并为一次提交**（一次改完签名、上限、`.resolve().is_relative_to()`） |
| **组 D（同文件）** | `运行核心/统一网关/网关核心.py` | **S-03 + S-06（若走方案 B）** | S-03 要在这里给**成功返回值**加敏感键脱敏；S-06 方案 B 要在这里加“能力前置策略”钩子。都在调用管线同一段（`:211`/`:223`/`:580`/`:633` 附近） | **串行**。建议 **S-03 先做**（纯脱敏、改动内聚），S-06 再在其上加策略钩子 |
| **组 E（依赖腿互斥）** | `支持库/后端/网络通信支持库/请求/包声明.json` | **S-04**（新增依赖 `出站安全`） vs **S-06 方案 A**（新增依赖 `命令安全`，改的是 `进程管理/包声明.json`） | 不是同一文件，但两者都**改变依赖腿**，都会影响 `开发工具/依赖生命周期审计`、`开发工具/组件合规`、依赖防火墙白名单 | 依赖腿变更**同批一次做**（一次审计跑过），不要分两次 |
| **组 F（同文件，跨主题）** | `模块库/直播逐字稿/工具/批量精校队列.py` | **S-05** 与 **子报告 04:194 / 子报告 11:1271,1275** | 三份报告都点名 `:33` 同一行；且该文件还硬编码了 3 个绝对路径（S-14 的点名地） | **S-05 与 S-14 建议合并**（一次改掉凭证兜底 + 绝对路径），不要两个主题各交一版 |
| **组 G（同文件，跨主题）** | `开发工具/HTML验证/制品进程.py`、`开发工具/HTML验证/热接入回归.py` | **S-05** 与 HTML 黑盒验证门禁任务 | S-05 改凭证来源后，验证链的注入方式会变；门禁任务可能同时改这两个文件 | **串行**：先落 S-05 的凭证占位化，再改门禁 |

### 2.2 摘要/版本/契约级联（不是文件冲突，但会互相覆盖）

- **同一批里若多个包被改，必须等所有源码改完，再一次跑 `开发工具/更新完整性摘要.py`**（该脚本硬编码系统根，见 S-14；它按 `完整性摘要.json` 的 `文件清单` 逐条重算 sha256）。分批跑会互相覆盖。
- **凡改包内文件，必同步四件套**：`包声明.json`（版本号）→ `能力定义.json`（能力 id/参数/说明）→ `完整性摘要.json`（哈希）→ `验证场景.json` / `验证场景引用.json`（用例）。本主题涉及的包合计 **4 个**：
  1. `支持库/后端/系统核心支持库/进程管理`（S-01、S-06A）— 现版本 `1.1.2`
  2. `支持库/后端/系统核心支持库/环境变量`（S-03）
  3. `支持库/后端/网络通信支持库/请求`（S-02、S-04）— 依赖需变
  4. `支持库/后端/文件系统支持库/文件操作`（S-07+S-09）
  5. `模块库/直播逐字稿`（S-05，改 `工具/批量精校队列.py`）
  → 其中 **1 号包被 S-01 与 S-06 两次命中，版本号只能 +1 一次**。
- **参数面变化会外溢到契约**：S-07（`解压文件` 新增 `最大字节数`/`最大条目数`）、S-03（若加脱敏开关）、S-06A（若加 `检查危险命令`）**都会改能力参数表** → 必须同步 `能力契约/`、`权限契约/`（+`配置契约/` 若涉及配置项），否则契约编译会红。
- **重编译要求**：本轮 8 条落点**全部只改 Python 源码包，无 C/Rust 扩展、无 Rust 二进制热替换需求**；但 **S-05 会连带“平台客户端制品重编”**（`模块库/直播逐字稿/工具/批量精校队列.py` 被打进制品，见 `开发文档/底座升级方案.md:20,69`）→ 制品重编与指纹更新属发布门禁动作。
- **文档同步**：`开发文档/项目说明.md`（S-03、S-05）、`开发文档/底座升级方案.md`（S-05）、`开发文档/能力目录说明书.md`（S-01/S-03/S-06 若说明变化，需跑 `开发工具/说明书生成` 重生成）。`开发文档/项目说明.md` 是本主题内**被两条命中且也是全项目公共热点**的文件。

### 2.3 与本主题外的其他审计主题的冲突

| 其他主题 | 与本主题的共享文件 | 说明 |
|---|---|---|
| 子报告 04（架构边界与依赖腿） | `支持库/后端/网络通信支持库/请求/包声明.json`、`模块库/直播逐字稿/工具/批量精校队列.py` | S-04 新增依赖、S-05 改凭证，都会改变依赖腿与“模块库直连网关”的结论 → 两份报告结论必须对齐后再动手 |
| 子报告 08（死代码空转与重复实现） | `支持库/后端/系统核心支持库/进程管理/实现/进程管理.py`、`命令安全` 包、`出站安全` 包 | 子报告 08 已把 `校验出站URL`、`检测危险命令`、`权威状态.清理死亡进程资源` 列为“0 调用方”同类 → **S-04/S-06 的修复方式（接线 vs 标记废弃）需要与子报告 08 统一裁决**，否则一边接线一边删会造成返工 |
| 子报告 09（运行数据/凭证） | `运行数据/权威状态.db`、凭证面 | 与 S-03 的“凭证自举”同一根问题，脱敏方案需两边一致 |
| 子报告 11（前端/适配层/MCP 工具箱） | `模块库/直播逐字稿/工具/批量精校队列.py`、`MCP工具箱/项目服务.py`（S-11 建议级） | S-05 与子报告 11:1271,1275 同点；若把 MCP 8766 也加鉴权，与 S-07（`解压文件` 能力经 MCP 暴露）有调用链交集 |
| 子报告 10（文档权威与结构残留） | `开发文档/项目说明.md`、`开发文档/能力目录说明书.md` | S-05 的文档占位化会与“文档权威”主题的改稿撞车 |
| 子报告 06（运行态与网关真实 HTTP） | `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` | S-05 真正轮换凭证必须改它（**配置，本轮禁止**）；子报告 06:172 已记录同值 |

### 2.4 建议的落地顺序（单线程，最少返工）

```
第 1 批（同函数合并，一次提交）   S-07 + S-09   → 文件系统支持库.文件操作
第 2 批（先强后接）              S-04 → 等落地后 → S-02   → 网络通信支持库.请求
第 3 批（阻断，最高优先，可独立）  S-01（+ 顺带 S-06 方案 A 同批，一次 +1 版本）→ 系统核心支持库.进程管理
第 4 批（脱敏先行）              S-03          → 环境变量包 + 网关侧脱敏（网关权威状态跑一次）
第 5 批（若选 S-06 方案 B）       S-06B         → 在已改过的网关核心上叠策略钩子
第 6 批（配置/B 端动作，需华哥）   S-05 + S-14 合并 → 凭证占位化 + 绝对路径收敛（轮换与制品重编另立发布任务）
第 7 批（需华哥裁决）             S-08          → 先核本机模型是否依赖 trust_remote_code
每批末：跑一次 `开发工具/更新完整性摘要.py`，再跑对应包的 `验证场景.json` 用例与 `开发工具/HTML验证/` 黑盒门禁
```

---

## 三、本轮未做（明确留白）

| 项 | 原因 |
|---|---|
| S-04 的 DNS rebinding 实际利用 | 需受控 DNS 服务器，超出“只读 + 127.0.0.1 无害探测”边界。仅以代码确证“校验用 `gethostbyname`、连接再解析一次”的 TOCTOU 结构 |
| S-08 的实际利用 | 本轮**未加载任何外部模型仓**，只确认代码位置与调用方范围 |
| S-06 / S-07 / S-03 的**修复后端到端回归** | 修复属改动，本轮只读；给出的是“修复后应得到的证伪输出” |
| S-05 的凭证轮换与制品重编 | 改 plist 属配置变更、制品重编属发布门禁，两者本轮均禁止 |
| 依赖防火墙（`运行核心/依赖防火墙.py`）是否强制生效 | 属子报告 07 未确认项 7，本轮未验证——但 **S-04/S-06 的依赖新增会踩到它，落地前必须先确认**（已写进 S-04/S-06 影响面） |
| 工程缓存/、测试中心/ 内的再扫描 | 与子报告 07 边界一致，未重复 |

---

## 四、本轮真实执行过的命令清单（可复跑）

1. `git log -1 --format=%H` → `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`；`date` → `Tue Sep 15 14:41:10 CST 2026`
2. `sed -n '240,275p;285,340p' 支持库/后端/系统核心支持库/进程管理/实现/进程管理.py` → 定 `_构建沙箱配置:245`、subpath 插值 `:270`、`配置=_构建沙箱配置(str(工作区)):331`、`argv=…sandbox-exec…:332`、`工作区=_Path(...).resolve():311`、`:320` fail-closed
3. `grep -n '^def' 支持库/后端/系统核心支持库/进程管理/实现/进程管理.py` → `启动进程:43`、`执行命令:162`、`沙箱执行命令:289`、`_构建沙箱配置:245`、`_沙箱安全环境:274`
4. `grep -n '跟随重定向|_校验协议与SSRF|gethostbyname' 支持库/后端/网络通信支持库/请求/实现/网络请求.py` → `_校验协议与SSRF:39`、`gethostbyname:50`、`发送请求:74`、`校验调用:94`、`跟随重定向 is False:132`、`:198`、`:243`
5. **S-01 三组复现**（A 注入经网关 / B 普通工作目录对照 / C 裸双引号语法错）→ 见表格 S-01 行输出
6. **S-03 两组复现**（列出环境变量 / 读取环境文件，均经 40007）+ 无凭证 401 对照 → 见表格 S-03 行输出
7. **S-02 复现**（本机双 HTTP 服务器 + 302 到 0.0.0.0）→ `被拦/放行` 四步输出见表格 S-02 行
8. **S-07 复现**（`inspect.signature` + 203956B → 209715200B）→ 输出见表格 S-07 行
9. `rg -n '校验出站URL' .`（S-04，排除工程缓存/.git/__pycache__）→ `.py` 零命中
10. `rg -n '检测危险命令' .`（S-06，同上排除）→ 仅 `命令安全/__init__.py:5,8,16` 与 `实现/命令安全.py:145`
11. `rg -n 'html-blackbox-verifier' .`（S-05）→ 源码 3 处 + 文档 4 处
12. `rg -n 'trust_remote_code' .`（S-08）→ 主仓 2 处（`模型服务.py:29,31`）
13. `inspect.signature(解压文件)` + `grep -n` `文件系统补充.py` → `解压文件:82`、`extractall:98`、前缀校验 `:95-96`
14. `cat 支持库/后端/系统核心支持库/进程管理/包声明.json`（版本 `1.1.2`、`依赖: []`）+ `sed -n '1,30p' 完整性摘要.json`
15. `sed -n '95,115p' 出站安全/实现/出站安全.py` → `校验出站URL:101`
16. `sed -n '1,40p' 开发工具/更新完整性摘要.py` → `目标目录 = ["支持库","模块库","技能库"]`、硬编码系统根
17. `sed -n '60,95p' 运行核心/统一网关/安全边界.py` → `读取环境变量白名单:66`、`校验路径:85`
18. `grep -n '脱敏' 运行核心/统一网关/网关核心.py` → `:211,223,580,633`（仅错误说明脱敏）
19. `lsof -nP -iTCP -sTCP:LISTEN | grep -E '40007|8766|8765'` → 三端口均为 `127.0.0.1` 监听
20. `rg -n '解压文件' .` → 第三方 `.py` 调用方 0（仅本包 `__init__.py:30,38,62`）
21. `ls 支持库/后端/系统核心支持库/进程管理/` → 确认为完整包（`包声明.json`/`能力定义.json`/`完整性摘要.json`/`验证场景.json`/`能力契约`/`权限契约`/`配置契约`/`说明` 等）
22. `find 支持库/适配层 -maxdepth 2 -name 包声明.json` → 仅各 `*提供者` 子目录，`模型服务.py` 不属任何包
