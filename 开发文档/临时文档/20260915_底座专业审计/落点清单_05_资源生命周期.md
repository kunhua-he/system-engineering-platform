# 落点清单 05 · 资源与生命周期（第二轮「定真 + 落点」）

> 对象：系统工程平台（底座）｜分支 `主干`｜HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`（现场核对一致）
> 纪律：未改源码/配置/运行数据/制品/数据库，未重启服务。对照实验只在 `/tmp`（临时库 + `权威状态.db` 只读副本）。**本文件是唯一写入。**
> 环境：macOS 26.6.2 / python3.14（跑 python 前 `unset PYTHONPATH`）｜本轮时间：2026-09-15
> 上游依据：`子报告_05_资源与生命周期.md`（R1–R8）。本轮只做「二次证真 + 落点」，不重复上游的审计叙事。

---

## 一句话定真结论

**R1–R8 全部二次证真成立，无一条被推翻；R1 的实际泄漏点是 27 个（子报告计入 26 个，漏计 `会话存储.py:134`），且严格 1:1（N 次调用 = N 个未关闭连接，实测 N=1/2/3/5/10/20）。R1 的最小改法是「3 个包共 4 个建连函数改为缓存工厂」，调用点 0 改动 —— 比逐个 try/finally 更小、更安全。**

---

## 定真总表（固定列）

| 编号 | 二次证真（命令 + 输出摘要） | 判定 | 修复落点（文件:行号 + 最小改法） | 影响面 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| R1 | `grep -n 'as 连接'` 三包 = 12/4/10 = **26 处精确命中**；探针 20 次调用 → 检查点恢复 **20/20**、工具执行 **20/20**、会话存储 **20/20**（严格 1:1，N=1/2/3/5/10 逐一等于 N）；另发现 `会话存储.py:134` 单独调用 → **1 个未关闭**（第 27 处）；同库对照 嵌入缓存 **0/20**、记忆支持库 **0/20** | **成立（加强：26 → 27）** | 4 个建连函数改缓存工厂：`会话存储.py:90`、`工具执行.py:88`、`检查点恢复.py:27`、`检查点恢复.py:186`（照抄 `嵌入缓存.py:27-74`）；调用点 0 改动。附带修 `会话存储.py:145` 空清理函数 | 三条热路径（对话/投递/任务状态机）；现场 40007 FD=98 平稳 ⇒ 潜伏型 | 否（三包各自私有实现文件） | 重跑三包探针：`创建任务×20→0`、`登记投递×20→0`、`创建会话×20→0` |
| R2 | `grep 'DELETE FROM' 权威状态.py` = **仅 3 处**（`:243 元信息`、`:751/:856 锁`），无 句柄/租约/进程；副本实测 **句柄 9130（有效 8972）/ 租约 9166（未回收 9008）/ 进程 3633 / 回收证据 158**；启动恢复 **3.033s / 13.14MB / 内存句柄表 8972 = 有效句柄数**；`启动运行核心网关.py` 对 `崩溃清理\|清理死亡进程资源\|扫描过期` **0 命中** | **成立（并现场复现启动全量灌入）** | 接线：`服务.py:331 清理崩溃残留` 增加启动+周期清扫，由 `启动运行核心网关.py` 调用；判死判据复用 `权威状态.py:849-852`；新增 3 条 DELETE + 2 处"过期即不恢复"过滤（见下） | 长驻网关启动耗时/内存单调增长；句柄 id 空间单调消耗 | **是**（`运行核心/权威状态.py`、`资源协调/服务.py`、`资源协调/句柄服务.py`） | 副本重启实测 + `权威状态门禁` 门禁 |
| R3 | `事件日志.py:32` 裸 `open("a")`、`:54` `read_text` 整文件读；`grep '追加JSONL\|轮转'` = False；`有界IO.py:85` 已有 `追加JSONL` 实现未用 | **成立（缺陷在模块自身）** | `事件日志.py:28-37 写入` 改调 `有界IO.追加JSONL`；`:54 查询` 改尾部倒读（抄 `启动监督器/健康监督.py:358-376`） | 仅 `运行核心/运行诊断` 包；下游注入方待查（可确定性修复，不依赖死代码裁决） | 否 | 写入 3000 条后检查轮转文件出现 |
| R4 | 损坏库调 `权威状态.损坏恢复` → 返回 `(False,'数据库损坏且无可用备份')`，`gc.collect()` 后 **未关闭连接数 = 1** | **成立** | `运行核心/权威状态.py:966-968`：把 `close()` 收进 `try/finally`（3 行改动） | 数据库损坏时的恢复路径（故障叠加场景） | **是**（`权威状态.py`，与 R2 同文件） | 损坏库探针：`未关闭连接数 = 0` |
| R5 | 实例化 `备份执行能力` + 损坏的「证据账本」源 → 抛 `DatabaseError`，`gc.collect()` 后 **未关闭连接数 = 1**；对照 `校验函数.校验权威状态`（`:19-27` try/finally）**= 0** | **成立** | `平台控制面/备份恢复/备份执行.py:54-63`：`close()` 收进 `try/finally`（同文件 `:76-80` 已有正确写法可抄） | 备份/恢复异常路径；同目录兄弟函数已正确 ⇒ 属遗漏 | 否（仅本文件） | 损坏源探针：`未关闭连接数 = 0` |
| R6 | 等价正则全仓扫描 → **源码根命中 4 处**（`句柄体系.py:215`、`HTTP提供者.py:72`、`合规测试包.py:953` + `忽略记录.py:3` 文档字符串），**运行核心/ = 0 处**；`记录忽略(` 调用点 = **21 处** | **成立（现场 3 处代码 + 1 处误命中）** | `句柄体系.py:215` → `except (ProcessLookupError, PermissionError) as 错误: 记录忽略(...)`；`HTTP提供者.py:72` → `except OSError: 记录忽略(...)`；`合规测试包.py:953` → `except Exception as 错误: 记录忽略(...)`；文档改 `未完成事项.md:164-178` | 1 处真故障被吞（合规工具自身可信度）；其余为口径一致性 | `句柄体系.py` **是**；其余两处否 | 重跑等价正则：源码根仅剩 `忽略记录.py:3`（正则误命中） |
| R7 | 句柄体系：创建 300 → 句柄表 300，全部失效后 **句柄表仍 300 / 活跃 0 / 回收证据 300**，源码 `del self.句柄表`+`句柄表.pop` 均 False；租约：创建 200 → 回收后 **租约表仍 200 / 活跃 0**，`租约表.pop` False、`租约表.clear` 仅关闭路径 | **成立** | `句柄体系.py:267-283 失效` 末尾加淘汰；`租约管理.py:85-98 回收` 末尾加淘汰（保幂等语义，见下）；`资源监督.py:121` 动作表改 `deque(maxlen)` | 单进程内存单调增长；句柄 id 耗尽由 `句柄体系.py:75-84`（100 次随机重试 → `RuntimeError("六位句柄已耗尽")`）实证 | `公共契约/句柄体系.py`、`运行核心/租约管理.py`、`平台控制面/资源监督.py` **均为共享文件** | 重跑 R7 探针：失效后句柄表应回落 |
| R8 | `第十四阶段门禁.py` `mkdtemp` **8 处**（`:31,59,75,97,136,145,190,236`），`rmtree/TemporaryDirectory` 仅 **1 处**（`:64`，只回收 `:59` 的工作区）；`:137,138` 两个编排器全函数无 `清理()`（仅 `:151-156` 并发线程内调用）；`运行发布门禁.py:1216,1220` `urlopen` 响应未关、`:1220-1224` `HTTPError` 分支未 `错误.close()` | **成立（未实测 ResourceWarning：禁跑门禁）** | `第十四阶段门禁.py` 各 `_项N` 用 `try/finally: shutil.rmtree(...)`；`:137,138` 加 `finally: 编排器1.清理(); 编排器2.清理()`；`运行发布门禁.py:1216` 改 `with urlopen(...) as 响应:` | 仅 `开发工具/`、门禁与 `MCP工具箱` 侧；每次跑门禁在 `/tmp` 与 fd 留痕 | 否（两个门禁文件互不重叠） | 跑门禁后 `ls -d /tmp/门禁_*` 应为空；fd 不增 |

---

## R1（重要）连接 1:1 泄漏：27 个调用点，逐点清单

### 二次证真（本轮实测）

```
# 调用点精确命中（grep 'as 连接'）
会话存储.py  :208,231,255,279,302,335,365,396,423,467,496,521   → 12 处
工具执行.py  :116,134,158,180                                   →  4 处
检查点恢复.py:64,85,135,162（_连接） + :212,230,252,274,307,329（_任务连接） → 10 处
合计 26 处（与子报告逐一一致，行号无一偏差）

# 严格 1:1 实测（N 次调用 = N 个未关闭连接）
检查点恢复 创建任务×20        → 20 个未关闭
工具执行   登记投递×20        → 20 个未关闭
会话存储   创建会话×20        → 20 个未关闭   （N=1/2/3/5/10 逐一等于 N）
检查点恢复 保存检查点×1/×3    →  1 / 3 个未关闭
工具执行   登记投递×1/×3      →  1 / 3 个未关闭
会话存储   连接会话存储×1     →  1 个未关闭   ← 第 27 个泄漏点（子报告未计）

# 同库反向对照（证明不是探针写法问题）
嵌入缓存   查询嵌入×20        →  0 个未关闭
记忆支持库 写入记忆×20        →  0 个未关闭
```

### 逐点清单（27 个，含建连函数行号）

| # | 文件:行号 | 建连函数（每次新建） | 调用形态 |
|---|---|---|---|
| 1 | `支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py:208` | `_连接` @ `:90-104`（`sqlite3.connect(库路径, timeout=10)`，**无缓存**） | `with 锁, _连接(路径) as 连接:` |
| 2 | 同上 `:231` | 同上 | 同上 |
| 3 | 同上 `:255` | 同上 | 同上 |
| 4 | 同上 `:279` | 同上 | 同上 |
| 5 | 同上 `:302` | 同上 | 同上 |
| 6 | 同上 `:335` | 同上 | 同上 |
| 7 | 同上 `:365` | 同上 | 同上 |
| 8 | 同上 `:396` | 同上 | 同上 |
| 9 | 同上 `:423` | 同上 | 同上 |
| 10 | 同上 `:467` | 同上 | 同上 |
| 11 | 同上 `:496` | 同上 | 同上 |
| 12 | 同上 `:521` | 同上 | 同上 |
| **13** | **同上 `:134`（`连接会话存储` 内）** | 同上 | **`_连接(路径)` —— 返回值直接丢弃，仅用于建表，永不 close（子报告漏项）** |
| 14 | `支持库/后端/系统核心支持库/工具执行/实现/工具执行.py:116` | `_投递连接` @ `:88-104`（`_sqlite3.connect(库路径, timeout=5)`，**无缓存**） | `with 锁, _投递连接(路径) as 连接:` |
| 15 | 同上 `:134` | 同上 | 同上 |
| 16 | 同上 `:158` | 同上 | 同上 |
| 17 | 同上 `:180` | 同上 | 同上 |
| 18 | `支持库/后端/系统核心支持库/检查点恢复/实现/检查点恢复.py:64` | `_连接` @ `:27-51`（**无缓存**） | `with 锁, _连接(路径) as 连接:` |
| 19 | 同上 `:85` | 同上 | 同上 |
| 20 | 同上 `:135` | 同上 | 同上 |
| 21 | 同上 `:162` | 同上 | 同上 |
| 22 | 同上 `:212` | `_任务连接` @ `:186-203`（**无缓存**） | `with 锁, _任务连接(路径) as 连接:` |
| 23 | 同上 `:230` | 同上 | 同上 |
| 24 | 同上 `:252` | 同上 | 同上 |
| 25 | 同上 `:274` | 同上 | 同上 |
| 26 | 同上 `:307` | 同上 | 同上 |
| 27 | 同上 `:329` | 同上 | 同上 |

**根因（一句话）**：`sqlite3.Connection` 的 `with` 是**事务**上下文管理器（`__exit__` 只 commit/rollback），**不是关闭器**；`fd` 要等 `__del__` 被 GC 触发才释放。

### 统一修法：收敛到一个连接工厂（推荐，改动 4 个函数 / 调用点 0 改动）

**为什么不是逐个 try/finally**：26 处若逐个改，必须同时把原 `with 连接:` 的**隐式 commit** 改写为显式 `连接.commit()`，等于同时动 26 处事务语义 —— 改动面与回归面都大一个数量级。改为「建连函数内部缓存」后，26 个调用点的源码一行都不用动，`with 连接:` 仍只承担 commit 语义，而连接由工厂持有。

| 落点 | 文件:行号 | 最小改法 |
|---|---|---|
| 1 | `支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py:90-104` | `_连接` 内部改为「按 库路径 缓存模块级连接」：模块级 `_缓存连接 / _缓存路径 / _缓存锁`，命中同路径直接返回；换路径先 `close()` 旧的；新建时加 `check_same_thread=False` |
| 2 | `支持库/后端/系统核心支持库/工具执行/实现/工具执行.py:88-104` | `_投递连接` 同上（缓存键 = 目标路径） |
| 3 | `支持库/后端/系统核心支持库/检查点恢复/实现/检查点恢复.py:27-51` | `_连接` 同上 |
| 4 | 同上 `:186-203` | `_任务连接` 同上（与 `_连接` 各自独立缓存，两库互不干扰） |
| 5 | 三个包各 +1 处 | 加 `关闭()` / `atexit` 收口钩子（`嵌入缓存.py:36-49 _关闭连接` 已有实现，可整段照抄） |
| 6 | `会话存储.py:145` | `清理函数=(lambda ...: None)` 是**空实现** —— 句柄失效时声明要回收「SQLite连接」却什么都不做。若采用落点 1（连接由工厂持有），此处登记应删除或改为持有态查询；若坚持句柄持连接，则必须改成真正 `close()` |

**可直接照搬的正确样板（同库内已有两种）**

| 样板 | 位置 | 特征 | 适用性 |
|---|---|---|---|
| `嵌入缓存._取连接` | `支持库/后端/大语言模型支持库/嵌入缓存/实现/嵌入缓存.py:55-74`（配合 `:27-29` 模块级缓存槽、`:36-49 _关闭连接`） | 模块级缓存 + `check_same_thread=False` + 换路径先 close + 显式关闭器 | **首选，三包可直接照抄**（三包都在模块级 `锁` 内调用，语义兼容） |
| `记忆支持库._连接` | `支持库/后端/记忆支持库/实现/记忆.py:26` + 调用方 `try/finally: 连接.close()`（`:61-62,71-72,100,142-143,170-171,187-188,203-204,245-246,264-265,282-283`） | 每次新建、用后即关 | 备选（可照搬的写法，但要在 26 个调用点逐个落地，改动面大） |

**影响面**：三条热路径（会话存储每次对话调用、工具执行每次认领投递、检查点恢复每次任务状态机操作）。现场 40007 已运行 1h06m、FD=98 平稳 ⇒ 当前调用频次低，属**潜伏型**缺陷，不是正在发生的故障。并发注意：缓存连接必须配 `check_same_thread=False`（三包已有模块级 `锁` 串行化调用）；多网关实例各自一份缓存，互不影响。

**验证命令**（改后必须为 0/0/0）：

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
PYTHONDONTWRITEBYTECODE=1 python3.14 /tmp/audit05r2_probe.py   # 三包 20 次调用应全部 = 0
```

---

## R2（重要）三张权威账本表只增不删 + 启动全量灌内存

### 二次证真（本轮对 `/tmp` 只读副本实测）

```
cp 工程缓存/权威状态/权威状态.db /tmp/audit05r2_句柄副本/   （只读复制，不碰运行库）
  句柄 总行数 = 9130      状态=有效 = 8972
  租约 总行数 = 9166      已回收=0 = 9008
  进程 总行数 = 3633      ←（子报告记 3631，说明审计窗口内仍在增长）
  回收证据 总行数 = 158   （= 历史上只成功回收过 158 次）
  句柄 创建时间范围 = 2026-08-28 16:05:19 → 2026-09-15 11:04:31（18 天 9130 行）

实例化 资源句柄服务(副本)：
  启动耗时 = 3.033s
  内存增量 = 13.14 MB
  内存句柄表大小 = 8972
  表大小 == 有效句柄数 ? True    ← 启动全量灌入，且永不淘汰

# 生产调用方检索
grep -rn '崩溃清理|清理死亡进程资源|扫描过期' --include='*.py' .
  → 定义：权威状态.py:823、权威状态.py:565、租约管理.py:102
  → 包装：资源协调/服务.py:281-283、:331-334 清理崩溃残留（= 恢复未完成事务 + 清理死亡进程资源 + 恢复缺失快照）
  → 唯一调用方：开发工具/发布门禁/权威状态门禁.py:141
  → 运行核心/启动运行核心网关.py：0 命中（生产入口未接线）
```

### 三条链路的真实终态（源码逐行核对）

| 表 | `清理死亡进程资源` 对它做了什么 | 行是否真的减少 |
|---|---|---|
| `锁` | `权威状态.py:856 DELETE FROM 锁 WHERE 资源id=? AND 进程身份键=?` | **减少（唯一真删）** |
| `租约` | `权威状态.py:858 → 回收租约:533-538` 只 `UPDATE 租约 SET 已回收=1` | **不减少（只改标记）** |
| `句柄` | `权威状态.py:871 → 失效句柄` 只 `UPDATE 句柄 SET 状态='已失效'` | **不减少（只改标记）** |
| `事务` | `权威状态.py:864 UPDATE 事务 SET 状态='失败'` | 不减少（业务表，正常） |
| `进程` | **只读不写** —— `:847` 仅 `SELECT ... FROM 进程` 做候选筛选 | **不减少（永久累积）** |

⇒ 子报告 R2 的表述需精确化：`清理死亡进程资源` **不是**「不删行的回收器」，它对 `锁` 是真删；**真问题是它从未被生产调用，且对 `租约/句柄/进程` 只做状态标记，缺 `DELETE` 与保留策略**。

### 回收器接线落点

| # | 落点 | 最小改法 |
|---|---|---|
| 1 | `运行核心/启动运行核心网关.py`（网关装配处） | 启动时调用一次 `资源协调服务.清理崩溃残留()`（`服务.py:331`），让 `清理死亡进程资源` 真正进入生产路径 |
| 2 | `运行核心/资源协调/服务.py:281-283 / :331-334` | 保持包装不变；另加一个 `周期清扫()`（复用 `清理死亡进程资源(心跳超时秒=…)` + `状态.扫描过期租约()`），由网关后台线程按固定间隔驱动 |
| 3 | `运行核心/权威状态.py:823` `清理死亡进程资源` 末尾（`:873 return` 之前） | 追加 3 条真删（与 `锁` 的 DELETE 同级）：`DELETE FROM 句柄 WHERE 状态='已失效' AND 失效时间 < :保留截止`、`DELETE FROM 租约 WHERE 已回收=1 AND … < :保留截止`、`DELETE FROM 进程 WHERE 最后心跳 < :保留截止 AND 身份键 NOT IN (SELECT 进程身份键 FROM 锁)` |
| 4 | `运行核心/资源协调/句柄服务.py:27-49` `_恢复账本句柄` | 循环体内加一条「过期即不恢复」过滤：`租约 = …读取租约()；if not 租约 or 租约["已回收"] or float(租约["硬截止时间"]) <= time.time(): continue` —— **直接降低启动耗时与内存**。`硬截止时间` 现场存的是 epoch 秒（写入方 `句柄服务.py:127,136` 用 `time.time()+默认超时秒`；`权威状态.py:566 扫描过期租约` 也用 `time.time()` 比较），可直接与 `time.time()` 比 |

### 「已死进程的句柄/租约行如何判死回收」判据

**判据已存在于源码中（`运行核心/权威状态.py:849-852`），本轮核对可直接复用，不必新造：**

| 判据 | 表达式 | 语义 |
|---|---|---|
| 判据 A｜身份不存活（优先） | `not 系统进程存在(int(进程id))`（`运行核心/进程身份.系统进程存在`） | 操作系统层面进程已消失 ⇒ 立即回收，不受心跳时间影响 |
| 判据 B｜心跳过期（兜底） | `最后心跳 <= time.time() - 心跳超时秒`（`心跳超时秒` 默认 `15.0`） | 进程仍在但失联（卡死/被挂起/机器休眠）⇒ 按失联回收 |
| 作用域 | `项目id`/`所有者` 为空时 `条件='1=1'`（`权威状态.py:836-839`） | 无参调用 = **全账本扫**，可直接用作启动清扫 |
| 幂等 | `回收租约` `UPDATE … WHERE 已回收=0`、`rowcount==0` 即返回 False | 重复清扫安全 |

**判死之后还应判「够老才删」（本轮需新增，因为现有回收器不含删除）**：
- 行级判据 = `状态='已失效'/'已回收=1'` **且**（`失效时间` / `最后心跳`）< `now - 保留窗口`（建议 7 天，与「18 天累积 9130 行」的现场规模匹配）；
- 残留引用保护 = `进程` 行仅当 `身份键` 不再被 `锁` 表引用时才允许删（`锁` 表行少、语义明确）；
- 保留窗口必须是可配置常量，不能硬编码（便于门禁与运维裁决）。

**影响面**：权威状态库是长驻网关启动必经路径（每次重启 3.03s / 13.14MB，且随账本单调增长）；句柄 id 为六位空间（`句柄体系.py:75-84` 100 次随机重试后 `raise RuntimeError("六位句柄已耗尽")`），只增不删最终会把创建句柄变成随机失败。**属「需华哥裁决产品口径」的一条**（回收窗口多少天、是否允许真删审计行）。

**验证命令**：

```bash
# 接线后：重启网关观察 fd/内存/启动耗时下降；副本演练（不动正式库）
cp 工程缓存/权威状态/权威状态.db /tmp/副本/ && python3.14 -c "…资源句柄服务(/tmp/副本)…"   # 句柄表应显著小于 8972
```

---

## R3（重要）事件日志零有界

**二次证真**：`运行核心/运行诊断/运行事件/事件日志.py` 全文 90 行逐行核对 —— `:32 with self.当前文件.open("a", encoding="utf-8") as 文件` 裸追加；全文件不含 `追加JSONL`/`stat().st_size`/`replace`/`轮转`；`:54 self.当前文件.read_text(encoding="utf-8").splitlines()` 整文件读进内存。同库 `公共契约/运行时/有界IO.py:85-107 追加JSONL`（默认上限 8MB、轮转 2 份、`强制落盘` 可选）已存在且已被 `MCP工具箱/任务观测.py:25`、`MCP工具箱/使用反馈.py:41`、`运行核心/运行诊断/安全审计/安全审计.py:66` 使用。

**修复落点**

| 项 | 文件:行号 | 最小改法 |
|---|---|---|
| 写入有界 | `事件日志.py:28-37` | `写入` 内把 `open("a")` 替换为 `有界IO.追加JSONL(self.当前文件, 字典, 强制落盘=False)`；保留 `except OSError: 降级告警数 += 1` 语义 |
| 上限可配 | `事件日志.py:21-26 __init__` | 新增 `最大文件字节数`/`轮转数量` 两个参数（缺省取 `有界IO.默认JSONL文件上限字节`、2），转发给 `追加JSONL`；`self.当前文件` 命名不变以免破坏调用方 |
| 查询有界 | `事件日志.py:39-72 查询` | 改尾部倒读（照抄 `启动监督器/健康监督.py:358-376 _读尾部行` 的倒读实现），只需读 `最近条数` 的量级；`聚合错误码`/`最近失败` 自动受益 |

**影响面**：`运行核心/运行诊断` 是公开包；模块自带「有界」承诺与实现相反 ⇒ 无论是否活路径都应修（修复是自包含的，不依赖死代码裁决）。**是否共享文件**：否。**验证命令**：写 3000 条后应出现 `运行事件.jsonl.1` 轮转文件；查询 1 万条日志的耗时与内存不再随文件线性增长。

---

## R4（重要）损坏恢复的只读校验连接在异常路径泄漏

**二次证真**：用 `/tmp` 临时库构造损坏（首 16 字节写 `\x00`）后调 `权威状态.损坏恢复(备份目录=不存在)` → 返回 `(False, '数据库损坏且无可用备份')`，`gc.collect()` 后 `unclosed database` **未关闭连接数 = 1**。源码：`:966` 建只读 URI 连接 → `:967 PRAGMA integrity_check`（损坏时先抛 `sqlite3.DatabaseError`）→ `:968 校验连接.close()`（永不执行）→ `:972 except sqlite3.DatabaseError: pass`。

**修复落点**：`运行核心/权威状态.py:966-968` —— 把 `close()` 移入 `try/finally`：

```python
校验连接 = sqlite3.connect(f"file:{self.数据库路径}?mode=ro", uri=True)
try:
    结果 = 校验连接.execute("PRAGMA integrity_check").fetchone()
finally:
    校验连接.close()
```

**影响面**：只在数据库损坏时走，但损坏恢复正是「出事时」的路径 ⇒ 故障叠加（越需要恢复越漏连接）。**是否共享文件**：**是**（`权威状态.py`，与 R2 改同一文件，必须串行）。**验证命令**：损坏库探针 `未关闭连接数 = 0`。

---

## R5（重要）`备份执行._备份一类` 证据账本裸连接无 try/finally

**二次证真**：`_备份一类` 是 `备份执行能力` 的**方法**（`平台控制面/备份恢复/备份执行.py:19`），证据账本分支 `:54` 裸 `sqlite3.connect(源, timeout=10)`，`:55-62` 连续 4 次 `execute`，`:63` 才 `close()`。用 `/tmp` 构造「源不是数据库」（写入 `b"not a database"`）后调用 → 抛 `DatabaseError: file is not a database`，`gc.collect()` 后 **未关闭连接数 = 1**；同一份损坏输入走 `校验函数.校验权威状态`（`:19-27`，内含 `:23-24 try/finally: 连接.close()`）→ **未关闭连接数 = 0**。

**修复落点**：`平台控制面/备份恢复/备份执行.py:54-63` —— `close()` 收进 `try/finally`。同文件 `:76-80`（`执行备份` 的 WAL 检查点）已是正确写法，**照抄即可**：

```python
连接 = sqlite3.connect(str(源), timeout=10)
try:
    …（原 :55-62 的 4 次 execute）…
finally:
    连接.close()
```

**影响面**：备份/恢复的异常路径（源库被截断/非 sqlite/表结构异常）。属遗漏而非设计。**是否共享文件**：否。**验证命令**：损坏源探针 `未关闭连接数 = 0`。

---

## R6（建议）`except Exception: pass` 残留：路径权威计数

### 现场权威计数（本轮用与子报告同一正则复算）

正则：`except\s+Exception\s*:\s*(?:#[^\n]*\n\s*)?pass\b`（跨行 + 允许中间注释行），排除 `工程缓存` / `参考资料` / `开发文档` / `.git` / `__pycache__`。

```
源码根命中总数: 4
  ./公共契约/句柄体系.py:215              ← 代码（真残留）
  ./平台控制面/提供者/HTTP提供者.py:72      ← 代码（真残留）
  ./开发工具/组件合规/合规测试包.py:953     ← 代码（真残留）
  ./公共契约/诊断/忽略记录.py:3            ← 文档字符串，正则误命中
运行核心/ 命中: 0
记录忽略( 调用点（排除 忽略记录.py 自身）= 21
```

**注意口径陷阱（本轮实测到的关键点）**：这三处**必须**用上面那条跨行正则才数得到 —— 用朴素的单行 `grep 'except Exception: pass'` 只能命中 `句柄体系.py:215` 一处，另外两处因为 `except Exception:` 下一行才是 `pass`（带注释）而漏掉。这正是子报告「25 处 vs 18 处 vs 3 处」口径反复的根源：**同一现场，正则不同结论不同**。

### 逐处修法（收窄异常类型 vs 记入留痕）

| # | 位置 | 现有形态 | 判定 | 最小改法 |
|---|---|---|---|---|
| 1 | `公共契约/句柄体系.py:215` | `try: os.kill(pid, signal.SIGKILL)` / `except Exception: pass`，紧随 `:216 time.sleep(1)` 后 `:218-222` 复查端口是否真释放并写入 `说明` | 清理容错，后果已被兜住 | **收窄 + 留痕**：`except (ProcessLookupError, PermissionError) as 错误: 记录忽略('句柄体系.回收端口占用进程', 错误)` |
| 2 | `平台控制面/提供者/HTTP提供者.py:72` | `try: self.send_error(500, "处理器异常")` / `except Exception: pass  # 连接已断开（客户端取消）` | 尽力而为（客户端已断开，无法再写响应） | **收窄**：`except OSError as 错误: 记录忽略('HTTP提供者.异常响应', 错误)` —— 比 `记录忽略(Exception)` 更精确，且与「不允许保持 pass」口径一致 |
| 3 | `开发工具/组件合规/合规测试包.py:953` | `finally:` 内逐个调 `shutdown/server_close/关闭/释放/close`，`except Exception: pass`（`:947` 注释明写「必须真实释放，防止固定端口和线程残留污染后续包」） | **真故障被吞** —— 释放失败后，后续包被残留污染时无法归因 | **记入留痕（不能静默）**：`except Exception as 错误: 记录忽略('合规测试包.能力释放', 错误)`；并考虑把失败计入该包的 `问题` 列表 |

### 文档该怎么改（`开发文档/未完成事项.md`）

| 文档现状 | 应改为 |
|---|---|
| `:164` 「待处理：运行核心里的 `except Exception: pass`（18 处，需逐点判断）」 | **删除整条**（现场 `运行核心/` 命中 0 处，已修完） |
| `:171-176` 逐点清单（实列 19 处，与标题「18 处」自相矛盾，且漏 `合规测试包.py:953`） | 改为一行结论：「已完成。源码根余 1 处代码（`公共契约/句柄体系.py:215`），另 2 处按本清单收窄类型后留痕（`HTTP提供者.py:72`、`合规测试包.py:953`）」 |
| `:134` 「全平台清零：20 处改为 `记录忽略(...)`」 | 更新为 **21 处**（现场实测调用点数） |
| 口径说明 | **补一句正则口径**：本项统计必须用跨行正则 `except\s+Exception\s*:\s*(?:#[^\n]*\n\s*)?pass\b`；单行 grep 会漏数 2 处 |

**影响面**：1 处真故障被吞（合规工具自身可信度）；其余为口径一致性。**是否共享文件**：`公共契约/句柄体系.py` **是**（与 R7 改同一文件，必须串行）；`HTTP提供者.py`、`合规测试包.py`、`未完成事项.md` 否。**验证命令**：重跑等价正则 → 源码根应只剩 `忽略记录.py:3`（文档字符串误命中）；`记录忽略(` 调用点 21 → 24。

---

## R7（建议）内存句柄表/租约表无淘汰；资源监督「动作」表无上限

**二次证真（本轮行为实测）**

```
句柄体系：创建 300 → 句柄表 = 300，活跃 = 300
          全部失效后 → 句柄表 = 300，活跃 = 0，回收证据 = 300     ← 表不回落
          源码含 'del self.句柄表' = False，'句柄表.pop' = False
租约管理：创建 200 → 租约表 = 200，活跃 = 200
          全部回收后 → 租约表 = 200，活跃 = 0                     ← 表不回落
          源码含 'del self.租约表' = False，'租约表.pop' = False，'租约表.clear' = True（仅 关闭/清理全部 路径）
资源监督：资源监督器.__init__(状态) → :121 self._报告表[单元id]["动作"].append(原因)（无 maxlen、无裁剪）
          :126 采样 里 setdefault 的 "动作": [] 亦为无界 list
```

**修复落点**

| # | 文件:行号 | 最小改法 | 注意 |
|---|---|---|---|
| 1 | `公共契约/句柄体系.py:267-283 失效`（在 `回收证据表.append` 之后、`return True` 之前） | 加 `self.句柄表.pop(句柄id, None)`；`活跃句柄数` 逻辑不变（`sum(状态==有效)` 仍正确，因已无该行） | 需先确认没有「失效后按句柄id再查」的调用方（`查询/校验` 在 `:267` 前已取到对象；建议改后跑 `测试中心.公共契约.测试_句柄资源预算`）。**幂等语义有变化**：二次 `失效` 会返回「句柄不存在」而不是「已失效（幂等）」⇒ 若要保幂等，把已失效 id 记进有界 `deque(maxlen=N)` 的「已回收id」集合 |
| 2 | `运行核心/租约管理.py:85-98 回收`（`return True` 之前） | 同上：`self.租约表.pop(租约id, None)` | 同 #1：`回收` 的幂等分支依赖 `租约对象.已回收`，pop 后二次回收会走「租约不存在」⇒ 需同步调整判据或引入已回收 id 集合 |
| 3 | `平台控制面/资源监督.py:121` + `:126` | `"动作": deque(maxlen=动作上限)`（如 50），并在 `采样()` 返回时 `list(报告["动作"])` | **必须同时改 `:126` 的初始化和 `采样` 的返回**：`deque` 不可 JSON 序列化（`追加证据` / 查询路径会踩），否则引入新故障 |
| 4 | `公共契约/句柄体系.py:75-84`（不改代码，登记风险） | 句柄 id 为六位空间，创建靠 100 次随机重试，`raise RuntimeError("六位句柄已耗尽")` | 落点 #1 直接降低该风险；R2 的账本 DELETE 是另一半 |

**影响面**：单进程内存单调增长（量级小于 R2 账本，但句柄 id 耗尽会**变成硬故障**）；`资源监督` 是控制面常驻对象，长时间运行会累积。**是否共享文件**：`公共契约/句柄体系.py` **是**（与 R6 #1 同文件）；`运行核心/租约管理.py`、`平台控制面/资源监督.py` 均为共享文件，互不重叠但需各自回归。**验证命令**：重跑 R7 探针 → 「全部失效后 句柄表」应回到 0；「全部回收后 租约表」应回到 0；`资源监督.采样()` 返回值仍可 JSON 序列化。

---

## R8（建议）门禁与临时目录的调用方未释放连接 / 未回收临时目录

**二次证真（源码逐行；禁跑门禁 ⇒ 未实测 ResourceWarning）**

```
开发工具/发布门禁/第十四阶段门禁.py
  mkdtemp 共 8 处：:31(消费者契约) :59(可复现) :75(可信仓库) :97(备份恢复)
                    :136(迁移互斥) :145(迁移互斥2) :190(二阶恢复) :236(五视图)
  rmtree/TemporaryDirectory 仅 1 处：:64 shutil.rmtree(工作区)  ← 只回收 :59 那一个
  :137 编排器1 = 迁移互斥编排器(目录) / :138 编排器2 = 迁移互斥编排器(目录)
      → 全函数（:_项5_双迁移互斥）无 编排器1.清理() / 编排器2.清理()
      → 仅 :151-156 的并发线程内调用 编排.清理()（异常时记入 记录忽略）
  _打开 @ 迁移互斥编排器.py:27-33 把连接长期挂在 self._连接；清理 @ :152-159 是唯一关连接入口

开发工具/发布门禁/运行发布门禁.py
  :1216 页面响应 = urlopen(Request(地址, method="GET"), timeout=3).read().decode("utf-8")   ← 响应无 with / 无 close
  :1220 urlopen(旧API请求, timeout=3)                                                        ← 同上
  :1220-1224 except HTTPError as 错误: 旧API状态 = 错误.code                                  ← 未 错误.close()
```

**修复落点**

| # | 文件:行号 | 最小改法 |
|---|---|---|
| 1 | `第十四阶段门禁.py:136-156`（`_项5_双迁移互斥`） | `目录`/`并发目录` 用 `try/finally: shutil.rmtree(…, ignore_errors=True)`；并在 finally 里 `编排器1.清理(); 编排器2.清理()`（`清理()` 已幂等：`_连接 is None` 直接返回） |
| 2 | `第十四阶段门禁.py:31,75,97,190,236`（另 5 个 mkdtemp） | 各 `_项N` 统一 `try/finally: shutil.rmtree(…, ignore_errors=True)`；或抽一个 `临时目录()` 上下文管理器（与 `:59-64` 已有的 rmtree 写法统一） |
| 3 | `运行发布门禁.py:1216` | `with urlopen(Request(地址, method="GET"), timeout=3) as 响应: 页面响应 = 响应.read().decode("utf-8")` |
| 4 | `运行发布门禁.py:1220-1224` | `with urlopen(旧API请求, timeout=3): 旧API状态 = 200`（成功路径）；`except HTTPError as 错误:` 分支加 `finally: 错误.close()`（`错误` 是响应对象，`HTTPError` 可当上下文管理器） |

**影响面**：仅 `开发工具/`、门禁与 `MCP工具箱` 侧；门禁不常跑，但每次跑都在 `/tmp` 与 fd 上留痕。**是否共享文件**：否（两个门禁文件互不重叠）。**验证命令**：跑完门禁后 `ls -d /tmp/门禁_*` 应为空（现场当前为 0 个 `门禁_*` 目录，仅 3 个 `门禁_*.txt` 文件）；`lsof -p <门禁pid>` 在退出前 fd 不增长。

---

## 共享文件与串行要求

**同文件冲突矩阵**（多落点改到同一文件 ⇒ 必须串行，禁止并行 worktree 或并行 Agent 同时改）

| 共享文件 | 涉及的落点条目 | 冲突性质 | 串行建议 |
|---|---|---|---|
| `运行核心/权威状态.py` | **R2**（`:823` 追加删除/接线、`:849-852` 判死判据）、**R4**（`:966-968` try/finally） | 同文件不同函数，但会被同一提交/同一回归覆盖；且 R2 的删除逻辑与 R4 的损坏恢复同属可靠性区（`:879` 起同一区块） | 一次提交内先 R4（3 行、零风险）再 R2（新增逻辑）；**不要**并发开两个分支 |
| `公共契约/句柄体系.py` | **R6 #1**（`:215` 收窄异常）、**R7 #1**（`:267-283 失效` 加淘汰） | 同文件、相邻区块；R7 会改变「失效」返回值语义，R6 改的是同对象里的清理容错分支 | 先 R6 #1（独立、无副作用），再 R7 #1（必须带 `测试中心.公共契约.测试_句柄资源预算` 回归） |
| `运行核心/资源协调/句柄服务.py` | **R2**（`:27-49` 加「过期即不恢复」过滤） | 启动路径，改动直接影响网关启动行为 | 与 `权威状态.py` 的 R2 改动**同一批次**提交（判据一致性），先跑副本实例化验证再上生产 |
| `运行核心/资源协调/服务.py` | **R2**（`:281-283`、`:331-334` 接线/周期清扫） | 包装层，被门禁 `权威状态门禁.py:141` 依赖 | 与 R2 同批；改后必须跑 `开发工具/发布门禁/权威状态门禁.py` |
| `运行核心/租约管理.py` | **R7 #2**（`:85-98 回收` 加淘汰） | 与 R7 #1 语义对称，但不同文件可并行；不过测试需同批 | 与 R2 的租约 DELETE 一起验收（同一个「回收语义」议题） |
| `公共契约/运行时/有界IO.py` | **R3**（只**调用** `追加JSONL`，不改该文件） | 无冲突 | 只读引用，不列入串行 |
| 三个泄漏包（`会话存储.py`/`工具执行.py`/`检查点恢复.py`） | **R1**（各自动内私有实现文件） | **无冲突**，可完全并行 | 三包可各自独立提交；建议用同一套缓存工厂模板（照抄 `嵌入缓存.py:27-74`）保证写法一致 |

**串行批次建议（每批一次回归）**

1. **批次 A（零风险一行级）**：R4 → R5 → R6（三处异常收窄）→ R3（事件日志改 `追加JSONL`）。同文件冲突：`句柄体系.py` 仅 R6 动。
2. **批次 B（R1，收益最大、最独立）**：三包 4 个建连函数改缓存工厂 + `会话存储.py:145` 清理函数。三包互不重叠，可并行；改完跑 R1 探针（0/0/0）与三包定向单测。
3. **批次 C（R2 + R7，同一「回收语义」议题，必须串行）**：先 `权威状态.py` 的 R4 已合入（批次 A）→ 再 R2 的 DELETE + 接线 + `句柄服务.py:27-49` 过滤 → 再 R7 的 `句柄体系.py:267`/`租约管理.py:85`/`资源监督.py:121`。共享文件 `权威状态.py`、`句柄服务.py`、`句柄体系.py` 全部落在本批次，**只能串行**，并跑 `权威状态门禁`。
4. **批次 D（R8，工具侧，与生产路径无关）**：两个门禁文件，互不重叠，可与任意批次并行。

**回归门禁（每批都跑）**

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
PYTHONDONTWRITEBYTECODE=1 python3.14 -m unittest 测试中心.公共契约.测试_有界IO 测试中心.公共契约.测试_句柄资源预算
PYTHONDONTWRITEBYTECODE=1 python3.14 -m unittest 测试中心.支持库.测试_SQLite数据库 测试中心.支持库.测试_记忆支持库
PYTHONDONTWRITEBYTECODE=1 python3.14 -m unittest 测试中心.运行核心.测试_提供者生命周期 测试中心.运行核心.测试_任务进程池有界
PYTHONDONTWRITEBYTECODE=1 python3.14 /tmp/audit05r2_probe.py         # R1 三包应 = 0/0/0
PYTHONDONTWRITEBYTECODE=1 python3.14 /tmp/audit05r2_probe2.py        # R5 应 = 0
```

> 注：R1 的三条泄漏路径**没有任何测试覆盖**（上游审计已证：7 个模块 3 波全绿，R1 三包不在波次内），所以「测试全绿」不能作为 R1 的验收证据；**必须**用上面的定向探针（真实调用计数）作为验收。落点批次完成后建议给三包各补一个 ResourceWarning 用例（`warnings.simplefilter("error", ResourceWarning)` + 结束前 `gc.collect()`）。

---

## 本轮复现命令与写入范围

```bash
# 冻结边界
git -C <根> rev-parse HEAD        # 1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc

# R1 调用点精确命中
grep -n 'as 连接' 支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py
grep -n 'as 连接' 支持库/后端/系统核心支持库/工具执行/实现/工具执行.py
grep -n 'as 连接' 支持库/后端/系统核心支持库/检查点恢复/实现/检查点恢复.py
# R1 泄漏计数探针（/tmp）：三包 20 次调用 + 嵌入缓存/记忆支持库对照
python3.14 /tmp/audit05r2_probe.py
# R5 方法级探针 + R7 API 探明
python3.14 /tmp/audit05r2_probe2.py
# R2 只读副本 + 启动恢复实测
cp 工程缓存/权威状态/权威状态.db /tmp/audit05r2_句柄副本/ ; python3.14 <<'PY' … 资源句柄服务(副本) … PY
# R6 等价正则（跨行）全仓扫描 + 记录忽略计数
# R7 行为实测（300 句柄全失效 / 200 租约全回收）
# R4 损坏库临时副本探针
```

**写入范围**：仅 `/tmp/audit05r2_probe.py`、`/tmp/audit05r2_probe2.py`、`/tmp/audit05r2_句柄副本/`（只读副本）、以及若干 `/tmp/探针_*` 临时库；外加本落点文件。**未修改任何工程源码/配置/运行数据/制品/数据库，未删除任何文件，未重启服务。**

## 与子报告的差异（本轮新增/修正）

| # | 项 | 子报告 | 本轮实测 | 说明 |
|---|---|---|---|---|
| 1 | R1 泄漏点计数 | 26 | **27** | 多出 `会话存储.py:134`（`连接会话存储` 内的建表连接，返回值丢弃、永不 close；单独调用实测 1 个未关闭） |
| 2 | R1 泄漏比例 | 「1:1」 | **严格 1:1（N=1/2/3/5/10/20 全部等于 N）** | 子报告采样值 20/20/21 中，会话存储的「21」是 `连接会话存储` 的连接被延迟到下一次 `gc.collect()` 才计数所致，非每调用 2 个 |
| 3 | R2 `清理死亡进程资源` 性质 | 「唯一回收器」 | 它对 `锁` 表是**真 DELETE**（`:856`）；对 `租约/句柄` 只做状态标记、对 `进程` 只读不写 | 落点因此是「接线 + 补 DELETE + 保留窗口」，而非「调用已有回收器即可」 |
| 4 | R2 现场行数 | 句柄 9130 / 租约 9166 / 进程 3631 | 句柄 9130 / 租约 9166 / **进程 3633** | 审计窗口内账本仍在增长（+2 行），进一步印证「只增不删」 |
| 5 | R6 计数口径 | 正则跨行、命中 4 处 | 同结论 | **补：该结论依赖跨行正则**；单行 `grep 'except Exception: pass'` 只能命中 1 处，这是历史口径反复的技术根因 |
