# 下一轮修复清单（维修期发现、按哲学第 23 条不在中途反复验证/返工）

> 来源：批次0/1 各路维修回传中「域外/连带/既有红」项。收口验证跑完一轮后统一处理。

## 一、既有红灯（子代理已用 `git archive HEAD` 纯净副本对照证明「与本次维修无关」）

| 编号 | 测试 | 现象 | 对照证据 |
|---|---|---|---|
| L1 | `测试中心.**.测试_Provider进程` | 5 failures（池 `启动()` 失败） | 批次0A 在 HEAD 纯净副本复跑同样红 |
| L2 | `测试中心.运行核心.测试_网关安全与有界并发` | 1 failure：`test_缺统一结果字段的字典绝不包装成功`（落在 `网关核心._设置后端字典结果`） | 同上；且网关文件正是批次0-3 属主域，需在其改动落地后重判 |
| L3 | `测试中心.模块库.测试_逐字稿*` | 1 failure：`测试_逐字稿有效底稿.test_剔除摘要数字正确`（`有效字符数` 期望 1 实得 3） | 批次1-6 证明该测试纯计算、不读本次改动文件 |
| L4 | `测试中心.MCP工具箱.*`（22 模块整包） | `failures=1 errors=3~4` | 与 HEAD 纯净副本同量级（含 `测试_作业系统.tearDown` 的临时目录竞态，单跑 18 OK） |

## 二、口径/边界待收口（非缺陷，但需一次定死）

| 编号 | 事项 | 建议 |
|---|---|---|
| C1 | 16 个正式包 `生命周期契约.json` + 1 个 `依赖契约/依赖契约.json` 仍写 `"契约版本":"1.0.0"`（现无任何消费方） | 若要「全仓只有一处契约版本」，另立批次走四连；否则明确「描述性字段不参与口径」 |
| C2 | `配置契约` 的 `资源限制.最大文件大小MB=100` 与解压新默认上限 1GiB **不同源** | 定死一处为权威，另一处引用它 |
| C3 | `启动进程` 按落点**有意不接线**危险命令检测（argv 直启无命令文本） | 保留为已知边界并写入说明书 |
| C4 | 危险命令检测在「调用器未装配」时**留痕放行**（不回退拦截） | 已写明理由（避免打断脚本/单测直连），需确认是否接受 |
| C5 | 制品内嵌的是旧 `依赖锁定.py`（`契约版本常量='1.0.0'`） | 批次8 重建制品后自动对齐 |
| C6 | `包声明.json.依赖` 会被下次以 `依赖=[]` 调 `编译能力定义` 覆盖 | 编译器层面跨域项，需在编译器加「保留既有依赖」或收口时传参 |
| C7 | `工作区管理` 提交根白名单**默认额外放行系统临时目录**（为不打断现有 3 个临时仓用例） | 可用 `工作区严格提交根=1` 关闭；需确认默认口径 |
| C8 | 8766 鉴权 **A 层（token）默认未启用**（客户端 header 能力待核实） | 已派收尾路核实 `.mcp.json`/`opencode.json`/`AGENTS.md` |
| C9 | 发布门禁「第三方访问声明」空集语义（唯一真口径项） | 按哲学第 11 条：不判红，但显式说明为空集、不构成证据 |
| C10 | 机器上仍有旧标签真实部署 `~/Library/LaunchAgents/com.huashi.zizhugao-queue.plist`（指向业务路径） | 需华哥确认是否迁移到参数化安装（会重启常驻队列）；**属运行态，未动** |

## 三、按哲学第 23 条的执行顺序

1. 收工当前在飞的全部维修路（含收尾 3 路）；
2. 父会话串行做**四连收口**（包版本 → 重编译契约 → 摘要级联 → 依赖锁重生成）；
3. **跑一轮**完整验证：`验收探针集.py`（对照修前基线 6/9）→ HTML 黑盒全量 → 发布门禁；
4. 把本轮跑出的失败 + 本节清单一起，作为**下一轮**统一修复输入。
