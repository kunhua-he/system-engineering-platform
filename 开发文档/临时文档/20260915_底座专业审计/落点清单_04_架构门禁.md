# 落点清单 04 · 架构边界与门禁（第二轮：定真 + 落点）

> 上游子报告：`开发文档/临时文档/20260915_底座专业审计/子报告_04_架构边界与依赖腿.md`
> 冻结 HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`（主干）；本轮**只读**，唯一写入 = 本文件。
> 环境：python3.14；`unset PYTHONPATH`；复核时间 2026-09-15。
> 基线实测：`审计依赖(系统根)` → **审计文件数 582，违规 0**（本轮复跑，与上游一致）。

## 复核总表

| 编号 | 二次证真（命令 + 输出摘要） | 判定 | 修复落点（文件:行号 + 最小改法） | 影响面（会不会误报现有合规代码） | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| A 技能库未进层体系 | 进程内夹具：改前 `技能库→模块库 / →公共契约 / →运行核心 / →支持库 / →标准库` **5 例全放行**；把 `技能库` 补进 `层名称表`+`允许依赖表` 后：`技能库→模块库` **命中「越层依赖」**、`→运行核心` **命中**、`→公共契约` **放行**、`→标准库` 放行。现场三文件 `_确定层` 实测均为 `其他`（`技能库/后端/技能库/__init__.py`、`…/实现/技能库.py`、`…/验证夹具/…/统计.py`） | **成立**（判据恒空，非现存违规） | `运行核心/依赖防火墙.py:33-43` 在 `层名称表` 增 `"技能库": "技能库",`；`:45-54` 在 `允许依赖表` 增 `"技能库": {"公共契约"},`（与 `支持库` 同口径）。**两张表必须同时改**：只改层表时 `.get(来源层, set())` 返回空集，`技能库→支持库` 等也会报红 | **零误报**：把两张表改到位后，现场 582 文件复跑 **违规仍为 0**（实测）；技能库现状只 `from 公共契约… import 结果`（`:38`）。唯一需裁决的是「技能库能否用支持库原子能力」——若允许，把 `"支持库"` 加入其允许集 | **是**（`依赖防火墙.py`，与 B/E/G/H 撞车） | 见下方「A 验证命令」 |
| B 第三方判据硬编码 | 用 `sys.stdlib_module_names` + 现行两张表做全量分类（582 文件）：表外候选 **10 个真第三方包**，全部为第三方发行包、与标准库**无名字冲突**（实测 `候选 ∩ stdlib = 空`）。把这 10 个并入 `第三方前缀表`（进程内模拟，未写盘）后现场新增违规 **16 条**：`MCP工具箱/项目服务.py` ×8（mcp 5 + starlette 2 + uvicorn 1）、`支持库/后端/代码解析支持库/语法索引/实现/语法索引.py` ×2（tree_sitter、tree_sitter_typescript）、`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` ×1（psutil）、`支持库/适配层/模型服务.py` ×5（torch 2、uvicorn、fastapi、transformers）。包声明核对：`语法索引`、`模型连接器` 均**无 `内部层` 键**（`转写` 才有 `内部层: true`） | **成立**；并**新增一条上游漏项**：`适配层` 豁免口径过窄 | ①`运行核心/依赖防火墙.py:57-61` 补 10 条：`mcp, starlette, uvicorn, fastapi, transformers, torch, psutil, mlx_whisper, tree_sitter, tree_sitter_typescript`。②`:251-259` 的 `是否提供者文件` 只认 `适配层/<X>提供者/` 目录 + `密码适配.py` 单文件；`支持库/适配层/模型服务.py`（`torch/uvicorn/fastapi/transformers`，实读 `:9-13`）落在豁免之外 → 需按 `AGENTS.md:214-218`「适配层内部适配器豁免」口径，增判「路径在 `支持库/适配层/` 根下即豁免」或要求该文件迁入 `*提供者/` 目录（**二选一，须产品裁决**）。③可选加固：把 `:250` 从「命中表即违规」反转为「非标准库、非仓库顶层目录名、非 `公共契约` → 违规」，硬编码表退化为豁免兜底 | 只加包名不清豁免 = **16 条红**；加包名 + 补齐适配层豁免 = **11 条红**（语法索引 2 + 模型连接器 1 + MCP工具箱 8）。这 11 条中 `语法索引`/`模型连接器` 是真违规（应为 `tree_sitter`/`psutil` 建独立提供者）；`MCP工具箱` 8 条需裁决「工具箱是否等同适配层」。**现有合规代码不会被误报**（`支持库/适配层/` 内 11 文件 34 站点实测全部命中豁免，加表后仍不报） | **是**（`依赖防火墙.py`） | 见下方「B 验证命令」 |
| C 发布门禁未调 4 门禁 | 全仓 grep（排除测试/缓存）：`审计模块库` 唯一外部引用者 `开发工具/复用审计/运行审计.py:18`；`破坏验证` 仅自身文件；`审计平台控制面` 仅 `平台控制面/提供者/__init__.py:15` 导出；`审计全部` 仅 `开发工具/依赖生命周期审计/运行审计.py:24`。发布门禁内 grep 命中的门禁 = 依赖防火墙 `:1002`、公开调用完整性 `:1010`、组件合规 `:757-763`、提供者进程一致性/隔离边界 `:974-980`、十四阶段十项 `:1492-1493`。**只读实测三项接线后表现**：`审计平台控制面()`（默认目录）= 54 文件 / **0 违规** / 0.11s；`审计全部(系统根)` = 15 提供者全通过 / **0 违规** / 0.06s；`审计模块库(模块库)` = 29 文件 / **1 违规**（`文档生成/__init__.py` 漂移）→ **接线即报红** | **成立**（并纠正：见「纠正登记」；另发现门禁自身崩溃缺陷） | 接线段落 = `开发工具/发布门禁/运行发布门禁.py:1017` 之后、`:1019` 空行之前，即紧接 `:1009-1017` 公开调用块，同一 `执行门禁()` 函数（`:876` 定义，`:885` 定义局部 `检查()` 可直接用），照抄现有 `try/except` 四段式，每段各写一条 `检查(名称, 通过, 详情)`。**接线前必须先修判据崩溃**：`平台控制面/提供者/防火墙审计_规则.py:115` 用 `节点.func.id`，当 `compile/eval/exec` 由属性调用（如 `re.compile(...)`）触发该分支时抛 `AttributeError: 'Attribute' object has no attribute 'id'` —— 实测传整个系统根给 `审计平台控制面(系统根)` 会崩，故接线时必须传 `平台控制面` 目录，或把 `:115` 改成 `getattr(节点.func, "id", None) or getattr(节点.func, "attr", "")`。`破坏验证.py` 会 `tempfile.mkdtemp` 造可破坏组件并做 13 种主动破坏（`:1-9`、`:73-80`），**不得**直接接进发布门禁主流程，须在隔离副本内运行后只回传证据 | 接线后按现状即 **1 条红**（`审计模块库`），且该红是**判据假设错误而非模块库违规**：`能力调用图审计.py:126-129` 假定能力id 带包名前缀（`前缀 = f"{包目录.name}."`），而 `模块库/文档生成` 的能力id 是 `办公文档支持库.文档生成.生成文档`，`removeprefix("文档生成.")` 不生效 → `__all__` 恒不等于注册集。**应先修判据再接线**，否则发布门禁永久红。耗时预估：三项只读合计实测 **<0.2 秒**（0.11 + 0.06 + 秒级），相对门禁整体（HTML 黑盒子进程 `超时秒=600`）可忽略；`破坏验证` 未测（越出只读边界） | **是**（`运行发布门禁.py`，与 K2 撞车） | 见下方「C 验证命令」 |
| D 能力调用图审计只扫 `实现/*.py` | 进程内实跑 `能力调用图审计.审计模块库(模块库)` → **审计文件数 29**，恰等于 `模块库.rglob("实现/*.py")` 的 29；`模块库.rglob("工具/*.py")` = **2** 个文件均**不**在实现集合内；`依赖防火墙` 对这两个文件实测 **True（在审计范围）**。硬编码业务项目路径实读：`批量精校队列.py:36` `老项目根 = Path("~/Documents/Hermes工作区/录音分析/免费祛斑免费敷面膜直播精校项目")`、`安装或卸载队列常驻.py:20` `工具目录 = Path("~/Documents/Hermes工作区/录音分析/直播逐字稿/工具")` | **成立** | ①`开发工具/复用审计/能力调用图审计.py:138` 把 `实际目录.rglob("实现/*.py")` 改为遍历包内全部 `*.py`（`实现/` + `工具/` + `__init__.py`），并保留 `:139` 的 pycache 过滤；②同文件新增一条规则或改到 `运行核心/依赖防火墙.py` 新增「引用业务项目绝对路径」（判据：源码字符串字面量含 `/Users/` 或 `Hermes工作区` 且不在 `排除片段表` 命中的测试/文档路径），依据 `AGENTS.md:151`。③根治：这两个文件本身是业务项目脚本（打 LaunchAgent、读业务进度文件、urllib 打网关），应**移出 `模块库/`**（建议归 `运维脚本/` 或删除），移出后 D 与 F 两条一起消失 | 落地前建议先看 `:126-129` 的前缀假设问题（见 C 行）；D 的 `实现/`→全量扩展会连带把 `__init__.py` 纳入，若前缀假设不修则新增假红 | 否（单文件） | 见下方「D 验证命令」 |
| E `排除片段表` 让「验证器」免审计 | 复跑基线：`开发工具/HTML验证/验证器.py`、`开发工具/统一验证/统一验证器.py` **均不在审计范围**（582 文件内）。摘掉 `"验证器"` 后重跑 → **584 文件、1 违规**（`统一验证器.py:340` 动态导入绕过依赖审计，实读 `:338-340` 为 `importlib.import_module(模块名)`，`模块名` 来自 `:354` 的 `发现全部(...)` 结果）。为定位第 3 个文件，用「精确名匹配」变体重跑 → **585 文件**，新增 3 个：`开发工具/HTML验证/验证器.py`、`开发工具/统一验证/统一验证器.py`、**`开发工具/工程缓存回收.py`**（上游未提及，其文件名含子串 `工程缓存` 而被误剪）。该变体下违规仍为 1 | **成立**；并**修正**：受害文件是 3 个而非 2 个；`"验证器"` 换成 `"验证器.py"` **无效**（`统一验证器.py` 仍含该子串，实测排除后仍是 582 文件） | `运行核心/依赖防火墙.py:183` 把 `排除片段表` 语义从「子串命中」改为「名称精确相等」，条目改为 `("pycache", "__pycache__", "工程缓存", "测试中心", "示例项目", "开发文档")`（**去掉 `"验证器"`**，并同步 `:198`、`:200` 的判定从 `片段 in 名` 改为 `名 in 排除片段表`）。对 `统一验证器.py:340` 的处理二选一：写成 `:98-101` 那类「有界列表聚合」形态；或在 `:246` 附近加**行级豁免注释**机制（如源码行带 `# 依赖防火墙豁免：按发现结果装载模块连接器` 即跳过），比按层整体豁免更小 | **仅 1 条需处理**，且属合理动态装载（按发现结果绑定连接器），不是真违规；其余两文件实测 0 违规。改后审计文件数 582→585，不影响任何现有合规代码 | **是**（`依赖防火墙.py`） | 见下方「E 验证命令」 |
| F 第二调用腿 | 实跑：`127.0.0.1:40007` 生产非测试命中 4 处 —— `模块库/直播逐字稿/工具/批量精校队列.py:30`（自拼网关 URL）+ `:33`（`凭证 = os.environ.get("系统库网关凭证", "html-blackbox-verifier")`，与 `开发工具/HTML验证/制品进程.py:123` 同一个硬编码默认值）、`运维脚本/热接入.py:25`、`运行核心/启动运行核心网关.py:40`（后两处是脚本/打印，不算第二腿）。`唯一能力调用服务(` 生产实例化点= `后端核心/后端核心.py`（初始化）+ `运行核心/能力调用/唯一能力调用.py` 自身文档字符串示例，**未见第二实例**。HTTP 双实现实测：`支持库/适配层/模型HTTP提供者.py:10,119,126,588,595`（`urllib.request`）与 `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:233-237,325,330`（`_HTTP调用模型` 内 `import urllib.request`）、`:639-644` | **成立**（两腿并存属实，但腿 1 的载体是业务脚本） | ①腿 2（模块库直连网关）：根治 = 把 `模块库/直播逐字稿/工具/批量精校队列.py` 与 `安装或卸载队列常驻.py` 移出 `模块库/`（同 D③）；若必须留，在 `运行核心/依赖防火墙.py` 新增规则「模块库层禁止 `urllib/http.client/socket/requests`」（顶层名 `urllib`/`http`/`socket` 在 `来源层 == "模块库"` 时报违规），落点在 `:250` 的前后新增一个强制拒绝分支。②腿 3（两条 HTTP 实现）：属**架构判断**非缺陷（适配层提供者 + 连接器兜底，协议别名表有测试兜底），建议登记为决策记录而非改码；若要收敛，最小改法是让 `模型连接器._HTTP调用模型`（`:233`）改为调用 `支持库.适配层.模型HTTP提供者` 的公开函数，避免两份 `urllib.request` | 新增「模块库禁网络模块」规则后 **现场零新增违规**（实测两个拐弯文件是唯一命中者，且它们正是待搬迁对象） | 否（改动落在 `依赖防火墙.py` 时与 A/B/E/G/H 撞车） | 见下方「F 验证命令」 |
| G 受控聚合导入绕过 | 进程内夹具：`G1` 有界列表变量拼 `"模块库.乙."` → **放行**；`G2` 把前缀改由变量提供（`_前缀 = "模块库.乙."`）→ **仍放行**（绕过比上游描述更宽）；`G3` 对照：同文件写 `importlib.import_module("模块库.乙.丙")` 字面量 → **命中「支持库反向导入」**；`G4` 对照：同层聚合（现场真实形态，实读 `支持库/后端/系统核心支持库/__init__.py:98-101`）→ **放行（正确）** | **成立**，且**加深**：前缀走变量也放行 | `运行核心/依赖防火墙.py:112-138` 保留 `有界变量` 识别，但**不要再丢弃前缀**：把 `:138` 的 `导入表.append(("<受控聚合导入>", …))` 改为携带 `_字面字符串(参数)` 中左侧字面量拼出的前缀（如 `"支持库.后端.系统核心支持库."`），并且**要求该前缀必须是字面量**（`G2` 形态前缀为变量 → 记 `<动态参数>` 报违规）。随后让正常的层向检查（`:274-285`）作用在该前缀上。若前缀指向 `来源层` 自身或来源层允许表内 → 放行（保住现有 6 个聚合入口） | **零误报**：现场 6 个聚合入口（`支持库/后端/{系统核心,大语言模型,数据操作,文件系统,网络通信,办公文档}支持库/__init__.py`）的前缀都是 `支持库.后端.X.`，与来源层 `支持库` 同名 → 同层导入在 `:274` 被 `顶层 != 来源层` 放行，行为不变 | **是**（`依赖防火墙.py`） | 见下方「G 验证命令」 |
| H `实现/` 同包判定用字符串前缀 | 进程内正反例：`H1` 兄弟同名前缀包（`支持库/后端/甲/__init__.py` 导入 `支持库.后端.甲子.实现.乙`）→ **放行（误放行）**；对照：`支持库/后端/丙/__init__.py` 同样导入 → **命中「跨包禁止导入 实现/ 目录」**。`H2` 仓库根文件（`根文件.py` 导入 `支持库.后端.甲.实现.甲`）→ **放行（误放行）**；对照：包内文件导入他包实现 → **命中**。`H3` 段名写作 `实现子` 的包 → **放行** | **成立**；**机制纠正**：H3 不是 `startswith` 误判，而是 `:231` 的 `"实现" in 模块名.split(".")` 要求**整段等于「实现」**，`实现子`/`实现evil` 这类段名根本进不了该规则（结果同为放行，但归因不同）。真正的前缀漏洞是 H1（兄弟包共享名字前缀）与 H2（仓库根文件前缀为空串，`startswith("")` 恒真） | `运行核心/依赖防火墙.py:232-234`：把 `文件包前缀` 由 `".".join(...parts[:-1])` 改为**元组** `tuple(文件.relative_to(系统根).with_suffix("").parts[:-1])`，判定改为「模块名前 N 段逐段相等」（`tuple(模块名.split("."))[:len(前缀)] == 前缀`）；`parts` 长度为 1（仓库根文件）时前缀应为**空元组**且**不放行**（根文件不属于任何包，任何含 `实现` 段的导入都是跨包）。同时把 `:231` 的段判定从 `== "实现"` 收紧为 `段 == "实现"`（保持）并在 `:232` 之前把段名以 `实现` 开头的包也视为实现目录（可选，按命名规范 `实现` 是唯一合法名，收紧为报「实现目录命名不合规」亦可） | **零误报**：现场无仓库根 `.py`、无兄弟同名前缀包（实测 582 文件 0 违规），属纯加固；H1/H2 都是新增拦截能力，不改变任何现有合规代码 | **是**（`依赖防火墙.py`） | 见下方「H 验证命令」 |
| I 公开调用完整性 `提取导出名` 回退 | 进程内实跑 `提取导出名`（签名 `(源码: str) -> set[str]`）：`__all__ = ['读取文件']` → `{'读取文件'}`；`__all__ = ('读取文件',)`（元组）→ **`{'读取文件'}`（识别成功）**；`__all__ = ['写入文件']` + `__all__ += ['读取文件']` → **`{'写入文件'}`（静默丢掉增量项）**；`from 甲 import 无关名字`（无 `__all__`）→ `{'无关名字'}`（回退成立）。对照源码 `:122-135`：只处理 `ast.Assign`，`__all__` 元素取 `e.value`（对 tuple 同样有效） | **成立**；**两处纠正**：①上游「元组形式不识别」**不成立**，实测已识别；②真正的漏项是 `__all__ += [...]` 不被识别（返回最后一个 `Assign` 的元素集，静默产生**不完整导出集**，比整体回退更隐蔽） | `开发工具/公开调用完整性门禁.py:127-134`：把 `全名` 收集改为在 `树.body` 循环里同时处理 `ast.Assign` 与 `ast.AugAssign`（`AugAssign.target.id == "__all__"` 且 `op` 为 `Add`）并**累加**（`全名 |= {…}`，注意 `AugAssign` 的值是 `List/Tuple` 而非 `Set`）；`__all__ = list(...)` 这类非字面量仍走 `None` 回退 | 未确认（可能收紧现有包）：需先按下方命令统计「哪些正式包缺 `__all__` 或使用 `+=`」再定。收紧会把这些包从「宽松回退通过」变为「按真实导出集判定」，可能新增红 | 否（单文件） | 见下方「I 验证命令」 |
| J 组件合规「场景真实返回值」只判自洽 | 读码复核 `开发工具/组件合规/合规测试包.py:896-898`（`return True, f"真实调用 {len(注册表.能力id列表)} 个能力全部非空返回"`）与 `:934-948` 逐能力判定：成功→不得携带失败结构；失败→必须带稳定错误码；注释原文「判的是自洽性」「避免把编译检查变成环境检查」。**未做活体反例**（构造恒失败组件包需完整六件套，越出只读边界） | **成立**（名实偏宽，且注释自述为刻意设计） | `开发工具/组件合规/合规测试包.py:934-945`：在现有逐能力判定之外，累计「失败结果数」与「失败错误码种类数」；当 `失败数 == len(注册表.能力id列表)`（全部能力都失败）时判不通过，详情写明「全部 N 个能力均返回失败（%s），未取得真实返回值，判为未真实执行」。建议按**告警项**（`强制=False`）而非硬失败，避免无外部资源环境下误红 | 会对「零外部依赖环境下跑」的组件包产生红/告警，需华哥裁决阈值；不掩护任何现有合规代码（现有包实测全通过） | 否（单文件） | 见下方「J 验证命令」 |
| K 技能库「认得 / 不认得」清单 | **7 处漏点逐处实测确认**：K1=`依赖防火墙.py:33-43,45-54` 无键（见 A）；K2=`运行发布门禁.py:67-70` `正式源码目录名表` = 公共契约/平台控制面/启动监督器/运行核心/前端核心/后端核心/支持库/模块库/项目适配层/开发工具/MCP工具箱/客户端/示例项目 —— **无技能库**；K3=`MCP工具箱/项目服务.py:306-310` `全部目录表` —— **无技能库**（含 测试中心、示例项目）；K4=`项目编译器.py:31-35` `关键源码目录` —— **无技能库**（含 客户端、MCP工具箱、开发文档）；K5=`合规测试包.py:330-339` `_提供者锁定` 只 `rglob` 支持库 + 模块库；K6=`HTML验证/制品事实.py:63-65` 只 `("模块库","支持库")`；K7=`运行环境管理器/环境管理器.py:104-110` `_定位系统根` 只判 支持库+模块库。权威依据纠正：`AGENTS.md` **通篇无「技能库」三字**（实测 grep 0 命中），上游引用的 `AGENTS.md:89-91,98,193` 不支持该结论；权威依据是 `开发文档/项目说明.md:305`（「本平台的第三根正式包根，与支持库、模块库平级，包声明类型同为「支持库」」）与 `:429`，以及 `开发工具/公开调用完整性门禁.py:3,22`、`开发工具/项目编译/正式包索引.py:88-90,110-111`、`加载器/包发现/发现器.py:66-71` | **成立**（7 处全对；判据依据需换锚点） | K2 `运行发布门禁.py:67-70` 的元组加 `"技能库",`（同时覆盖 `_正式Python源码文件()` 的 py_compile 语法门禁）｜K3 `MCP工具箱/项目服务.py:306-310` 加 `"技能库",`｜K4 `项目编译器.py:31-35` 加 `"技能库",`（保护 `:83-86` 的「拒绝输出到源码关键目录」护栏）｜K5 `合规测试包.py:334-336` 把 `[*支持库根.rglob("包声明.json"), *模块库根.rglob("包声明.json")]` 加上 `技能库根`（三根写法照抄 `:583`）｜K6 `制品事实.py:63-65` 两处元组加 `"技能库"`｜K7 `环境管理器.py:104-110` 判据加 `(祖先 / "技能库").is_dir()`｜K1 见 A | K2 会让 py_compile 覆盖技能库 4 个 `.py`（现无语法错，实测可解析）→ 零红；K3/K4 是范围扩大（K4 属纯护栏，未构造触发场景）→ 零红；K5/K6/K7 是**判据放宽**，只会减少 fail-closed 误报 | **是**（K2 与 C 同文件 `运行发布门禁.py`） | 见下方「K 验证命令」 |
| L 项目适配层深入运行核心子目录 import | 实跑 `rg '^\s*(from\|import)\s+' 项目适配层`：跨层 import 仅 `公共契约.*`（`依赖锁定.py:14`、`锁定校验.py:15`、`支持库绑定.py:12-13`、`模块绑定.py:12`、`运行入口/项目入口.py:12-13`）与 `运行核心.*`（`依赖锁定.py:15-17`、`锁定校验.py:16-18`、`传递闭包.py:23-26`、`支持库绑定.py:14-15`、`模块绑定.py:13`、`能力适配/能力适配.py:12`），**零** `支持库`/`提供者`/`实现` 引用；其中 `依赖锁定.py:67` 有 1 处包级形态 `from 运行核心.运行环境管理器 import 环境摘要信息`，其余 6 处直落子目录（`运行核心.能力调用.HTTP连接器`、`运行核心.加载器.包发现.发现器` 等）。`运行核心/` 确无 `__init__.py`（命名空间包） | **成立**（现状不算违规，属耦合风险） | 判据侧无落点（现接口下合规）；加固落点：`运行核心/` 增一层包级入口 `运行核心/__init__.py`（按 `AGENTS.md:204-212` 的「包级中文入口」模式导出 `发现全部/解析依赖/选择提供者/环境摘要信息/HTTP连接器`），再把 `项目适配层` 的 6 处深导改为 `from 运行核心 import …`。**须注意**：给 `运行核心/` 加 `__init__.py` 会让 `_确定层` 不变（层名取顶层目录，已是 `运行核心`），但会让 `依赖防火墙` 多审 1 个文件 | 加 `运行核心/__init__.py` 后该文件本身会被审计（顶层目录 `运行核心`∈层表），其导入必须落在 `运行核心` 允许表 `{"公共契约","支持库"}` 内，否则**新增 1 条红** | 否 | 见下方「L 验证命令」 |
| M `sys.path`/动态装载盘点 | 实跑：`__path__` 命中 **3 处**（`测试中心/运行核心/测试_冷启动反向门禁.py:5` 注释、`平台控制面/提供者/防火墙审计.py:8`、`平台控制面/提供者/防火墙审计_规则.py:16`，后两处是**判据文本**不是实际使用）→ **修正**：上游「全仓唯一 1 处」不准确。二级相对导入 `^\s*from\s+\.{2,}` → **0 处**（复核一致）。`sys.path` 非测试非缓存命中 **120 处**。`spec_from_file_location` 生产命中 4 处：`运行核心/加载器/包安装/模块安装.py:44`、`支持库安装.py:45`、`支持库/后端/文档转换支持库/python_pptx提供者/__init__.py:16`、`开发工具/组件合规/合规测试包.py:184`（另测试中心 4 处） | **成立**（计数需修正） | `运行核心/启动运行核心网关.py:4` 的 `sys.path.insert(0, ".")` 改为绝对系统根自举（照抄 `客户端/构建平台客户端.py:28-29` 的自根插入写法）。动态装载的路径越界校验（`模块安装.py:29-38`）与对向守卫（`提供者生命周期.py:272-279`，已接 `运行发布门禁.py:976-980`）已成立，无需改 | 自举路径改绝对不影响现场审计结果（该文件已在审计范围内）；无新增红 | 否 | 见下方「M 验证命令」 |

---

## 纠正登记（与上游子报告不一致处）

| 处 | 上游表述 | 本轮实测结论 |
|---|---|---|
| A 判据依据 | 引 `AGENTS.md:89-91,98`（四层方向）、`:193`（技能库=第三根正式包根） | `AGENTS.md` **通篇无「技能库」字样**（grep 0 命中）；`:193` 原文是「能力扩充按运行域/能力领域/组件包分类」，与技能库无关。③权威依据换为 `开发文档/项目说明.md:305`（明写「第三根正式包根…包声明类型同为『支持库』」）+ `:429` + `公开调用完整性门禁.py:3,22` + `正式包索引.py:88-90,110-111` + `发现器.py:66-71` |
| B 适配层豁免 | 只列了 `*提供者/` 目录与 `密码适配.py` 的豁免，未提 `支持库/适配层/` **根下直接文件** | 补：`支持库/适配层/模型服务.py:9-13`（`torch/uvicorn/fastapi/transformers`）落在 `:251-259` 的 `是否提供者文件` 判据之外 —— 加第三方表后会**新增 5 条红**，而 `AGENTS.md:214-218` 的系统适配器豁免口径覆盖它。这是判据口径不一致，需与加表同批处理 |
| C 依赖生命周期审计 | 「发布门禁**未调用** `依赖生命周期审计/运行审计.py`（`审计全部`）」+ 表内「唯一引用者」列 | **按父会话纠正与本轮复核修正口径**：该审计的 `扫描提供者目录` 单函数**已接** `运行发布门禁.py:624-625`（在 `校验第三方访问声明()` 内校验提供者依赖锁）。正确主张是「**整套** `审计全部`（依赖锁/包声明/完整性摘要/健康探针/停止释放证据 6 类）未被发布门禁调用」，不是「该审计完全未被调用」。本条已按此口径复核并保留 |
| C 落地前提 | 未提门禁自身崩溃 | 新发现：`平台控制面/提供者/防火墙审计_规则.py:115` 在 `func` 为 `Attribute` 时 `AttributeError`（实测把系统根传进 `审计平台控制面()` 即崩）。接线 `审计平台控制面` 前必须先修该行或只传 `平台控制面` 目录 |
| C 首红来源 | 未评估接线后表现 | 实测：`审计模块库` 接线即 **1 条红**，但该红源自 `能力调用图审计.py:126-129` 假设「能力id 带包名前缀」，而 `模块库/文档生成` 的能力id 为 `办公文档支持库.文档生成.生成文档` → 判据假设错误。**须先修判据再接线** |
| E 受害范围 | 2 个文件（`HTML验证/验证器.py`、`统一验证/统一验证器.py`）；替换方案未验证 | 实为 **3 个**（多出 `开发工具/工程缓存回收.py`，因文件名含子串 `工程缓存` 被误剪）；且把 `"验证器"` 换成 `"验证器.py"` **无效**（`统一验证器.py` 仍含该子串，实测仍为 582 文件），正确改法是**按名精确匹配** |
| H 归因 | 「`X.A.实现子.y` 因 `startswith` 字符串前缀被误判同包而放行」 | 归因不准：`实现子`/`实现evil` 因 `:231` 的段级判定（要求整段 `== "实现"`）而**根本没进入该规则**；真正的 `startswith` 漏洞是本轮实测的 H1（兄弟同名前缀包 `甲`↔`甲子`）与 H2（仓库根文件 `文件包前缀 = ""`） |
| I 元组形式 | 「`__all__ += [...]`、元组形式均不识别」 | 元组形式**已识别**（实测 `__all__ = ('读取文件',)` → `{'读取文件'}`）；不被识别的是 `__all__ += [...]`，且它是**部分丢失**（返回最后一个 `Assign` 的元素集）而非整体回退 |
| M `__path__` | 「全仓唯一命中是测试文件注释」 | 实测 3 处命中：测试注释 1 处 + `平台控制面/提供者/防火墙审计.py:8`、`防火墙审计_规则.py:16`（后两处是防火墙的**判据文本**，登记有 `spec_from_file_location`/`module_from_spec` 等禁用词，非实际拼接）。结论（无实际 `__path__` 拼接）不变，计数需修正 |
| M `sys.path` | 「全仓 440 处命中」 | 本轮按「非测试中心、非工程缓存」口径实测 **120 处**（口径不同，非矛盾；上游数字含测试与缓存树） |

---

## A 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
# 反例：技能库 → 模块库（改后应「命中」；改前实测「放行」）
python3.14 - <<'PYEOF'
import sys, pathlib, tempfile, copy
sys.path.insert(0, "."); import 运行核心.依赖防火墙 as f
def 判(导入行, 补技能库):
    根 = pathlib.Path(tempfile.mkdtemp(prefix="fw1_", dir="/tmp"))
    p = 根 / "技能库/后端/甲/实现/甲.py"; p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(导入行 + "\n", encoding="utf-8")
    旧根, 旧层, 旧允许 = f.系统根, dict(f.层名称表), copy.deepcopy(f.允许依赖表)
    f.系统根 = 根
    if 补技能库:
        f.层名称表["技能库"] = "技能库"; f.允许依赖表["技能库"] = {"公共契约"}
    try: r = f.审计依赖(根)
    finally:
        f.系统根 = 旧根; f.层名称表.clear(); f.层名称表.update(旧层)
        f.允许依赖表.clear(); f.允许依赖表.update(旧允许)
    print(("命中" if r.违规列表 else "放行"), [v.规则 for v in r.违规列表])
判("from 模块库.乙 import 丙", True)                 # 应 命中（反例）
判("from 公共契约.基础类型.结果类型 import 结果", True)  # 应 放行（正例）
PYEOF
# 影响面复核：改到位后现场仍应 0 违规
python3.14 -c "
import sys, copy; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f
f.层名称表['技能库']='技能库'; f.允许依赖表['技能库']={'公共契约'}
r=f.审计依赖(f.系统根); print('审计文件数=',r.审计文件数,'违规数=',len(r.违规列表))"
```

## B 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
# 表外第三方全量盘点（本轮口径：标准库用 sys.stdlib_module_names，不靠硬编码表）
python3.14 - <<'PYEOF'
import sys, ast, collections
sys.path.insert(0, "."); import 运行核心.依赖防火墙 as f
标准 = set(sys.stdlib_module_names); 表外 = collections.defaultdict(list)
for p in f._遍历待审计源码(f.系统根):
    if f._所在包已废弃(p): continue
    rel = str(p.relative_to(f.系统根))
    for 名, 行 in f._解析导入(ast.parse(p.read_text(encoding="utf-8"))):
        顶层 = (名 or "").split(".")[0]
        if not 顶层 or 顶层.startswith("_") or 顶层 in f.标准库前缀表 or 顶层 in 标准: continue
        if 顶层 in f.层名称表 or 顶层 in f.第三方前缀表 or 顶层 == "公共契约": continue
        表外[顶层].append(f"{rel}:{行}")
for 名 in sorted(表外, key=lambda k: -len(表外[k])):
    print(f"{名:26s} {len(表外[名]):3d}  {表外[名][:3]}")
PYEOF
# 补表后新增违规（进程内模拟，不写盘）
python3.14 -c "
import sys; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f
候选={'mcp','starlette','uvicorn','fastapi','transformers','torch','psutil','mlx_whisper','tree_sitter','tree_sitter_typescript'}
旧=set(f.第三方前缀表); f.第三方前缀表 |= 候选
r=f.审计依赖(f.系统根); f.第三方前缀表.clear(); f.第三方前缀表.update(旧)
print('补表后新增违规 =', len(r.违规列表))
for v in r.违规列表: print('  ', v.来源层, v.文件, v.行号, v.目标)"
```

## C 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台
# 四门禁外部引用者盘点（应为：只有各自 CLI / __init__）
rg -n '审计模块库|破坏验证|审计平台控制面|审计全部' --glob '*.py' --glob '!测试中心/**' --glob '!工程缓存/**' .
rg -n '依赖防火墙|公开调用完整性|组件合规|提供者隔离边界|第十四阶段|扫描提供者目录' 开发工具/发布门禁/运行发布门禁.py
# 接线后耗时/报红预估（只读，不跑发布门禁）
unset PYTHONPATH && python3.14 -c "
import sys, time; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f; 根=f.系统根
from 平台控制面.提供者.防火墙审计 import 审计平台控制面
t=time.time(); r1=审计平台控制面(); print('审计平台控制面', r1.审计文件数, len(r1.违规列表), f'{time.time()-t:.2f}s')
from 开发工具.依赖生命周期审计.审计核心 import 审计全部
t=time.time(); L,S=审计全部(根); print('审计全部', len(L), sum(len(x.违规列表) for x in L), f'{time.time()-t:.2f}s')
from 开发工具.复用审计.能力调用图审计 import 审计模块库
t=time.time(); r3=审计模块库(根/'模块库'); print('审计模块库(接线首红来源)', r3.审计文件数, len(r3.违规列表), f'{time.time()-t:.2f}s')
for v in r3.违规列表: print('   ', v.文件, v.类型, v.详情[:80])"
```

## D 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 -c "
import sys; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f
根=f.系统根
from 开发工具.复用审计.能力调用图审计 import 审计模块库
r=审计模块库(根/'模块库'); print('审计模块库 文件数=',r.审计文件数)
实现={str(p.relative_to(根)) for p in (根/'模块库').rglob('实现/*.py')}
工具={str(p.relative_to(根)) for p in (根/'模块库').rglob('工具/*.py')}
print('实现集合=',len(实现),'工具集合=',len(工具))
for t in 工具: print('  工具文件是否被能力调用图审计覆盖 =', t in 实现, t)"
rg -n --glob '*.py' --glob '!测试中心/**' 'Hermes工作区|~/Documents/Hermes' .
```

## E 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
# 基线（应为 582 / 两个验证器不在审计范围）
python3.14 -c "
import sys; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f
s={str(p.relative_to(f.系统根)) for p in f._遍历待审计源码(f.系统根)}
print('审计文件数=',len(s))
for t in ['开发工具/HTML验证/验证器.py','开发工具/统一验证/统一验证器.py','开发工具/工程缓存回收.py']:
    print(('在' if t in s else '不在'), t)"
# 改后预期（精确名匹配）：585 文件、1 违规（统一验证器.py:340）
# 反例（错误改法）：把 '验证器' 换成 '验证器.py' 仍会 582 —— 别这么改
```

## F 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台
rg -n '127.0.0.1:40007' --glob '*.py' --glob '!测试中心/**' .
rg -rn '唯一能力调用服务\(' --glob '*.py' --glob '!测试中心/**' .
rg -n 'urllib.request' 支持库/适配层/模型HTTP提供者.py 支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py
```

## G 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
# 反例：聚合式动态导入指向 模块库（改后应 命中；改前实测 放行）
python3.14 -c "
import sys, pathlib, tempfile; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f
根=pathlib.Path(tempfile.mkdtemp(prefix='fwG_',dir='/tmp'))
p=根/'支持库/后端/甲/__init__.py'; p.parent.mkdir(parents=True,exist_ok=True)
p.write_text('import importlib\nfor 子库名 in [\"乙\"]:\n    importlib.import_module(\"模块库.乙.\" + 子库名)\n',encoding='utf-8')
f.系统根=根; r=f.审计依赖(根); print('命中' if r.违规列表 else '放行', [v.规则 for v in r.违规列表])"
# 正例：现场 6 个聚合入口改后仍应放行
python3.14 -c "
import sys; sys.path.insert(0,'.'); import 运行核心.依赖防火墙 as f
r=f.审计依赖(f.系统根); print('现场违规数=',len(r.违规列表))"
```

## H 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 - <<'PYEOF'
import sys, pathlib, tempfile; sys.path.insert(0, "."); import 运行核心.依赖防火墙 as f
def 跑(结构, 说明):
    根 = pathlib.Path(tempfile.mkdtemp(prefix="fwH_", dir="/tmp"))
    for 路径, 内容 in 结构.items():
        q = 根 / 路径; q.parent.mkdir(parents=True, exist_ok=True); q.write_text(内容, encoding="utf-8")
    旧 = f.系统根; f.系统根 = 根
    try: r = f.审计依赖(根)
    finally: f.系统根 = 旧
    print(("命中" if r.违规列表 else "放行"), 说明, [v.规则 for v in r.违规列表])
跑({"支持库/后端/甲/__init__.py": "from 支持库.后端.甲子.实现.乙 import 丙\n"}, "反例 H1 兄弟同名前缀包（应命中）")
跑({"根文件.py": "from 支持库.后端.甲.实现.甲 import 乙\n"}, "反例 H2 仓库根文件（应命中）")
跑({"支持库/后端/丁/实现/丁.py": "from 支持库.后端.甲.实现.甲 import 乙\n"}, "正例 包内实现文件导他包（应命中）")
PYEOF
```

## I 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 -c "
import sys, ast; sys.path.insert(0,'.')
from 开发工具.公开调用完整性门禁 import 提取导出名
for 源码 in [\"__all__ = ['读取文件']\n\", \"__all__ = ('读取文件',)\n\", \"__all__ = ['写入文件']\n__all__ += ['读取文件']\n\", 'from 甲 import 无关名字\n']:
    print(repr(源码.strip()), '→', 提取导出名(源码))"
# 影响面：统计缺 __all__ 或使用 += 的正式包（决定是否收紧）
rg -n '^__all__' 支持库/后端 模块库 技能库 --glob '*.py' | wc -l
rg -n '^\s*__all__\s*\+=' 支持库 模块库 技能库 --glob '*.py'
```

## J 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台
sed -n '890,900p'  开发工具/组件合规/合规测试包.py    # 通过结论（只看非空返回）
sed -n '930,950p'  开发工具/组件合规/合规测试包.py    # 逐能力只判自洽性
sed -n '326,340p'  开发工具/组件合规/合规测试包.py    # K5 _提供者锁定 只扫两根
```

## K 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台
sed -n '67,70p'   开发工具/发布门禁/运行发布门禁.py   # K2 无技能库
sed -n '306,310p' MCP工具箱/项目服务.py              # K3 无技能库
sed -n '31,35p'   开发工具/项目编译/项目编译器.py     # K4 无技能库
sed -n '330,339p' 开发工具/组件合规/合规测试包.py     # K5 只扫支持库+模块库
sed -n '63,65p'   开发工具/HTML验证/制品事实.py       # K6 只认模块库+支持库
sed -n '104,110p' 运行核心/运行环境管理器/环境管理器.py # K7 判据只两根
rg -n '技能库' AGENTS.md                              # 应为 0 命中（依据换锚点）
rg -n '技能库' 开发文档/项目说明.md | head            # 权威依据 305 / 429
```

## L 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台
rg -n '^\s*(from|import)\s+' --glob '*.py' 项目适配层 | grep -v '__future__'
ls 运行核心/__init__.py 2>/dev/null || echo "运行核心/ 无 __init__.py（命名空间包）"
```

## M 验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台
rg -n 'sys\.path' --glob '*.py' --glob '!测试中心/**' --glob '!工程缓存/**' . | wc -l
rg -n '__path__|spec_from_file_location' --glob '*.py' --glob '!工程缓存/**' .
rg -n '^\s*from\s+\.{2,}' --glob '*.py' --glob '!工程缓存/**' . || echo "二级相对导入 0 处"
```

---

## 共享文件与串行要求

**结论：本主题的 13 条落点集中在 1 个文件上 —— `运行核心/依赖防火墙.py`（A、B、E、G、H 五条 + F 的可选新规则），另有 `开发工具/发布门禁/运行发布门禁.py` 被 C 与 K2 同时占用。这两个文件必须串行改，不可与其它主题并发。**

### 串行批次建议（同批次内顺序执行，不同批次之间不得并行）

| 批次 | 独占文件 | 本主题条目 | 与其它主题的撞车风险 |
|---|---|---|---|
| **批次 1** | `运行核心/依赖防火墙.py` | A（两表）+ B（第三方表 + 适配层豁免口径）+ E（排除片段表语义）+ G（受控聚合前缀）+ H（实现目录段级判定）+ F②（模块库禁网络模块，可选） | **极高**。该文件是所有依赖类主题的唯一判据源；任何「加一层/加一包/改剪枝」都会改变全仓审计结果（实测每处改动都会让 582 这个数字和 0 违规这个结论变化）。**必须一次改完、一次全量复跑**，禁止分批提交导致中途 582/违规数漂移。改动前先 `git stash` 或分支隔离，改后统一跑：`审计依赖(系统根)` 期望 585 文件 / 1 违规（=E 的 `统一验证器.py:340`）或按豁免方案为 0 |
| **批次 2** | `开发工具/发布门禁/运行发布门禁.py` | C（4 门禁接线）+ K2（正式源码目录名表加技能库） | **极高**。发布门禁是唯一判定入口，也是「十四阶段十项门禁」「旧过渡目录不存在」等其它主题的共同落点；且接线顺序影响首次全绿时间。**必须先修 `能力调用图审计.py:126-129` 的前缀假设与 `防火墙审计_规则.py:115` 的属性调用崩溃，再接线**，否则首次运行必红且无法区分「新门禁真拦」与「判据 bug」 |
| **批次 3** | `开发工具/复用审计/能力调用图审计.py` | D（遍历范围扩到包内全部 `.py`）+ C 的前缀假设修正 | 中。该文件只被 `复用审计/运行审计.py` 与发布门禁引用；但 C 的接线依赖它的输出，**批次 3 必须先于批次 2 完成** |
| **批次 4** | `开发工具/公开调用完整性门禁.py` | I（`__all__` 增量赋值） | 中。该门禁已接在发布门禁 `:1010`，收紧会直接改变发布结果 —— 必须先跑 `I 验证命令` 里的「缺 `__all__` 包统计」再决定是否收紧 |
| **批次 5** | 零散单文件（可并行） | J（`合规测试包.py:934-945`）、K3（`MCP工具箱/项目服务.py:306-310`）、K4（`项目编译器.py:31-35`）、K5（`合规测试包.py:330-339`）、K6（`制品事实.py:63-65`）、K7（`环境管理器.py:104-110`）、L（`运行核心/__init__.py` 新增）、M（`启动运行核心网关.py:4`） | 低。唯一注意：J 与 K5 同在 `合规测试包.py`（同文件内不同函数，可同批但须一次改完），L 会新增文件需重建完整性摘要（若该目录纳入摘要） |

### 跨主题共享（需与其它子报告协调）

- `运行核心/依赖防火墙.py`：**架构边界（本报告 A/B/E/G/H）+ 任何「依赖方向/第三方边界/审计范围」主题的唯一判据文件**。任何人改它的层表/第三方表/排除表都会推翻其它主题的「0 违规」基线。**建议本主题持有该文件的独占改动权，改完在权威文档标定新的基线圈（文件数 + 违规数 + 命令）**。
- `开发工具/发布门禁/运行发布门禁.py`：C（接线）、K2（技能库目录名）与「门禁接线/发布链路」类主题共用；同一函数 `执行门禁()`（`:876`）内的顺位插入互相影响打印顺序与首次红点定位。
- `开发工具/组件合规/合规测试包.py`：J（真实返回值判据）与 K5（`_提供者锁定` 扫描根）同文件，另有其它主题可能引用其 `组件合规` 入口（发布门禁 `:757-763`）。
- `开发工具/复用审计/能力调用图审计.py`：D（遍历范围）与 C（前缀假设）双向依赖 —— **改这个文件前先确认发布门禁侧是否已接线**，否则会出现「改了没人跑」或「先接线后改判据导致长期红」。
