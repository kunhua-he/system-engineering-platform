# 子报告 04 · 架构边界与依赖腿

> 审计对象：系统工程平台（底座）　冻结 HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`（主干）
> 方式：只读扫描 + 只读门禁函数在进程内调用 + /tmp 正反例夹具；未改源码/配置/运行数据/制品

## 一句话结论

**分层主体成立**（生产代码 0 处跨包深导、0 处相对深导、0 处第二轮第三方直连到 正式代码/项目适配层、旧入口确为未授权路径），**但门禁判据本身有 13 处漏点/过宽**——最要紧的是第三根正式包根「技能库」未被依赖防火墙的层体系收录（层级规则对技能库**恒空**）、第三方判据表是硬编码清单（漏 psutil/mlx_whisper/tree_sitter/mcp/starlette/uvicorn 等，现场 3 处非豁免直连因此免检）、以及发布门禁**未调用**复用审计/依赖生命周期审计/平台控制面专属防火墙/反向破坏验证 4 个门禁（按铁律等同门禁不存在）。

### 分级计数

| 级别 | 数量 | 条目 |
|---|---|---|
| 阻断 | **0** | — |
| 重要 | **7** | A–G |
| 建议 | **6** | H–M |
| 已核实为「无违规」的正面结论 | 4 | 1、2、4、14 |

---

## A. 「技能库」未进入依赖防火墙的层体系 —— 层级规则对第三根包根恒空

- **级别**：重要
- **影响**：`技能库/` 是 AGENTS 明示的第三根正式包根（与支持库、模块库平级，包声明类型同为「支持库」）。依赖防火墙的 `层名称表`（第 33-43 行）与 `允许依赖表`（第 45-54 行）**都没有「技能库」键**，技能库文件被 `_确定层` 判成「其他」→ 第 281-282 行 `if 来源层 == "其他": continue` 直接跳过全部方向规则。结果：技能库反向依赖 模块库/运行核心/核心 层**不产生任何违规**（只有「跨包导入 实现/ 目录」这一条仍能命中）。同一缺陷使 `平台控制面`、`MCP工具箱`、`客户端`、`启动监督器`、`运维脚本` 也落入「其他」而免方向检查（这些是设计意图，技能库不是）。
- **证据**：
  - `运行核心/依赖防火墙.py:33-43`（层名称表无 技能库）、`:45-54`（允许依赖表无 技能库）、`:274-285`（`来源层 == "其他"` → continue）
  - `AGENTS.md:89-91,98`（模块只保存能力 id、四层方向）、`AGENTS.md:193`（技能库=第三根正式包根，与支持库/模块库平级）
  - `开发工具/公开调用完整性门禁.py:3-4,22`（`扫描段 = ("支持库/后端", "模块库", "技能库")`，确认技能库是正式公开包根）
- **复现命令**（进程内正反例，写盘只在 /tmp）：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  unset PYTHONPATH
  python3.14 - <<'PYEOF'
  import sys, tempfile, pathlib, textwrap
  sys.path.insert(0, ".")
  import 运行核心.依赖防火墙 as 防火墙
  def 跑(结构, 说明):
      根 = pathlib.Path(tempfile.mkdtemp(prefix="fw_", dir="/tmp"))
      for 相对路径, 内容 in 结构.items():
          路径 = 根 / 相对路径; 路径.parent.mkdir(parents=True, exist_ok=True)
          路径.write_text(textwrap.dedent(内容), encoding="utf-8")
      旧 = 防火墙.系统根; 防火墙.系统根 = 根
      try: r = 防火墙.审计依赖(根)
      finally: 防火墙.系统根 = 旧
      print(("命中" if r.违规列表 else "放行"), 说明, [(v.目标, v.规则) for v in r.违规列表])
  跑({"技能库/后端/甲/实现/甲.py": "from 模块库.乙 import 丙\nimport 运行核心.依赖防火墙\nfrom 支持库.后端/丙/实现/丙.py"[:0] or "from 模块库.乙 import 丙\n"}, "技能库 → 模块库")
  跑({"支持库/后端/甲/实现/甲.py": "from 模块库.乙 import 丙\n"}, "支持库 → 模块库")
  PYEOF
  ```
- **真实输出摘录**（本次实跑）：
  ```
  [放行] 技能库 → 模块库/运行核心（第三根正式包根，层级等同支持库）  审计文件=1
  [命中] 支持库 → 模块库 反向依赖  审计文件=1
          → 支持库→模块库.乙 | 支持库反向导入 | 支持库/后端/甲/实现/甲.py:1
  ```
  即：同一条「反向导入」规则，对支持库能真触发，对技能库恒空。
- **现场影响面**：技能库现有 4 个 .py 文件、`技能库/后端/技能库/实现/技能库.py:38` 只 `from 公共契约.基础类型.结果类型 import 结果`，**目前没有实际越层**；所以这条是「判据恒空」而非「现存违规」——但技能库新增任何跨层依赖都不会被拦。
- **状态**：已核实（进程内正反例 + 现场代码扫描）

---

## B. 第三方判据表是硬编码清单 —— 现场 3 处非豁免第三方直连免检

- **级别**：重要
- **影响**：铁律是「只允许标准库；第三方依赖必须封装在 `支持库/适配层/` 对应提供者」（`AGENTS.md:150`）。依赖防火墙靠两张硬编码表识别第三方：`第三方前缀表`（第 57-61 行，19 个包名）与 `标准库前缀表`（第 62-72 行）。**表外第三方=完全无感**（顶层既不在标准库表也不在第三方表时，只剩层名称表检查，第三方名不在层名称表 → 直接放行）。
- **证据**：
  - 判据：`运行核心/依赖防火墙.py:57-61`、`:250-267`
  - 表外（未被收录）第三方现场直连：
    - `支持库/后端/代码解析支持库/语法索引/实现/语法索引.py:221` → `import tree_sitter_typescript`、`:222` → `from tree_sitter import Language, Parser`（该包 `包声明.json` 无 `内部层` 标记 → 连内部层豁免也没有）
    - `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:27` → `import psutil`（同上，无 `内部层` 标记）
    - `MCP工具箱/项目服务.py:15-18,990` → `mcp.*`；`:987-988` → `starlette`；`:989` → `uvicorn`
  - 表内但靠 `内部层` 豁免的：`支持库/后端/文档转换支持库/{PDF生成,PDF文本表格,PDF渲染,PDF隔离提供者,openpyxl提供者,python_docx提供者,python_pptx提供者}/实现/*.py`、`图像处理支持库/图像解码/实现/子进程解析.py:26`（PIL）、`转写支持库/转写/实现/子进程解析.py:24`（mlx_whisper）——共 11 个文件 `"内部层": true`
- **全量清单（非 `支持库/适配层/` 的第三方 import 站点）**：
  | 位置 | 站点数 | 内部层 |
  |---|---|---|
  | `支持库/后端/代码解析支持库/语法索引/实现/语法索引.py:221,222` | 2 | 无（**严格违规**） |
  | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:27` | 1 | 无（**严格违规**） |
  | `支持库/后端/图像处理支持库/图像解码/实现/子进程解析.py:26` | 1 | true |
  | `支持库/后端/文档转换支持库/PDF生成/实现/生成PDF.py:76-81,137,143,151` | 9 | true |
  | `支持库/后端/文档转换支持库/PDF生成/实现/渲染表格.py:12-14` | 3 | true |
  | `支持库/后端/文档转换支持库/PDF文本表格/实现/PDF文本表格.py:38` | 1 | true |
  | `支持库/后端/文档转换支持库/PDF渲染/实现/子进程解析.py:24` | 1 | true |
  | `支持库/后端/文档转换支持库/PDF隔离提供者/实现/子进程解析.py:26,35` | 2 | true |
  | `支持库/后端/文档转换支持库/openpyxl提供者/实现/表格文档.py:37` | 1 | true |
  | `支持库/后端/文档转换支持库/openpyxl提供者/实现/表格文档生成.py:63,109,159` | 3 | true |
  | `支持库/后端/文档转换支持库/python_docx提供者/实现/文字文档.py:79,132,235` | 3 | true |
  | `支持库/后端/文档转换支持库/python_pptx提供者/实现/演示文稿.py:11,12,132` | 3 | true |
  | `支持库/后端/转写支持库/转写/实现/子进程解析.py:24` | 1 | true |
  | `MCP工具箱/项目服务.py:15-18,987-990` | 8 | 无包声明（**严格违规**） |
  合计：**支持库/后端 13 文件 31 站点 + MCP工具箱 1 文件 8 站点 = 39 站点**；另有 `测试中心/` 15 文件 47 站点（测试用夹具，按本次口径不计违规）。
  `支持库/适配层/` 内 11 文件 34 站点为**合规**（第三方边界所在地）。
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n --glob '*.py' -e '^\s*(import|from)\s+(reportlab|openpyxl|pptx|docx|fitz|pdfplumber|PIL|psutil|mlx_whisper|tree_sitter|tree_sitter_typescript|mcp|starlette|uvicorn)' 支持库 MCP工具箱 | grep -v '^支持库/适配层/'
  ```
- **真实输出摘录（进程内正反例）**：
  ```
  [放行] 支持库/后端 直连 psutil/mlx_whisper/tree_sitter/torch（第三方表未收录）
  [命中] 对照：同一位置 import yaml（表内收录）
          → 支持库→yaml | 第三方直连（一第三方一支持库，仅提供者可导入） | 支持库/后端/甲/实现/甲.py:1
  ```
- **状态**：已核实（现场扫描 39 站点 + 表内/表外正反例）

---

## C. 发布门禁未调用 4 个门禁 —— 独立脚本等同门禁不存在

- **级别**：重要
- **影响**：`AGENTS.md:167-168` 明写「发布前必须通过依赖防火墙……独立脚本未被发布入口调用等同门禁不存在」。现场核验 `开发工具/发布门禁/运行发布门禁.py` 实际调用的判据是：依赖防火墙（:1002-1008）、公开调用完整性（:1010-1017）、组件合规（:757-763）、提供者隔离边界/提供者生命周期（:976-980）、第十四阶段十项门禁（:1481-1487，其中含 `提供者直连规则`）。**以下 4 个门禁发布入口从未调用**（只有自身 CLI、自身 `__init__` 或测试引用）：
  | 门禁 | 唯一引用者 | 判定 |
  |---|---|---|
  | `开发工具/复用审计/能力调用图审计.py`（`审计模块库`） | 只有 `开发工具/复用审计/运行审计.py:18`（其自身 CLI） | 未被发布门禁调用 |
  | `开发工具/复用审计/反向破坏/破坏验证.py` | 无外部调用者（仅自身文档字符串） | 未被发布门禁调用 |
  | `开发工具/依赖生命周期审计/运行审计.py`（`审计全部`） | 发布门禁只在 `:624` 用了它的 `扫描提供者目录` 单函数，**不是**整套生命周期审计 | 未被发布门禁调用 |
  | `平台控制面/提供者/防火墙审计.py`（`审计平台控制面`） | 只有 `平台控制面/提供者/__init__.py` 导出 | 未被发布门禁调用 |
- **证据**：`开发工具/发布门禁/运行发布门禁.py:624,757-763,976-980,1002-1017,1481-1487,1512-1514`；`开发工具/复用审计/运行审计.py:18-29`；`开发工具/复用审计/能力调用图审计.py:131`；`平台控制面/提供者/防火墙审计.py:36`；`开发工具/依赖生命周期审计/运行审计.py`（CLI 头）
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n '依赖防火墙|公开调用完整性门禁|组件合规|复用审计|能力调用图审计|提供者直连规则|防火墙审计|依赖生命周期审计|审计模块库|审计平台控制面' 开发工具/发布门禁/运行发布门禁.py
  rg -rn '审计模块库|破坏验证|审计平台控制面|审计全部' --glob '*.py' --glob '!测试中心/**' --glob '!工程缓存/**' .
  ```
- **真实输出摘录**：
  ```
  1002:        from 运行核心.依赖防火墙 import 审计依赖, 审计结果转清单
  1010:        from 开发工具.公开调用完整性门禁 import 运行门禁 as 运行公开调用门禁
  762/763:     报告 = 组件合规(包目录).执行()
  624:         from 开发工具.依赖生命周期审计.审计核心 import 扫描提供者目录
  （grep 全仓未出现 运行发布门禁 调用 审计模块库/破坏验证/审计平台控制面/审计全部）
  ```
- **状态**：已核实（tokens: 引用关系全仓 grep + 发布门禁逐段读）

---

## D. 能力调用图审计只扫 `实现/*.py` —— 模块库工具目录整体逃逸，且含业务项目硬编码路径

- **级别**：重要
- **影响**：`能力调用图审计.审计模块库` 只遍历 `实际目录.rglob("实现/*.py")`（第 138 行），包级 `__init__.py` 与**非 `实现/` 子目录**完全不审。现场 `模块库` 有 2 个这样的文件：
  - `模块库/直播逐字稿/工具/批量精校队列.py` —— 直接 `urllib.request` 打网关、硬编码业务项目绝对路径（**违反 `AGENTS.md:151` 严禁引用任何业务项目的代码/配置/运行数据**）
  - `模块库/直播逐字稿/工具/安装或卸载队列常驻.py` —— 同样硬编码业务项目绝对路径
  这两个文件被依赖防火墙覆盖（在 582 个审计文件内），但依赖防火墙**没有任何「引用业务项目路径」规则**，所以现状是：这条铁律在两个门禁里都没有判据。
- **证据**：`开发工具/复用审计/能力调用图审计.py:131-157`（尤其 `:138`）；`模块库/直播逐字稿/工具/批量精校队列.py:30,33,36,37`（`~/Documents/Hermes工作区/录音分析/...`）；`模块库/直播逐字稿/工具/安装或卸载队列常驻.py:20,24`
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n --glob '*.py' --glob '!测试中心/**' 'Hermes工作区|~/Documents/Hermes' .
  python3.14 -c "import sys;sys.path.insert(0,'.');import 运行核心.依赖防火墙 as f;print([str(p.relative_to(f.系统根)) for p in f._遍历待审计源码(f.系统根) if '直播逐字稿/工具' in str(p)])"
  ```
- **真实输出摘录**：
  ```
  ./模块库/直播逐字稿/工具/批量精校队列.py:36:老项目根 = Path("~/Documents/Hermes工作区/录音分析/免费祛斑免费敷面膜直播精校项目")
  ./模块库/直播逐字稿/工具/安装或卸载队列常驻.py:20:工具目录 = Path("~/Documents/Hermes工作区/录音分析/直播逐字稿/工具")
  （依赖防火墙遍历结果）在审计范围 模块库/直播逐字稿/工具/批量精校队列.py
  （能力调用图审计 rglob("实现/*.py") → 该文件不在其遍历集合内）
  ```
- **状态**：已核实

---

## E. 依赖防火墙的 `排除片段表` 让「验证器」整文件免审计 —— 正好是发布门禁自己依赖的 HTML 验证器

- **级别**：重要
- **影响**：`排除片段表 = ("pycache", "工程缓存", "测试中心", "示例项目", "验证器", "开发文档")`（第 183 行）按**目录名/文件名子串**剪枝。被它挡掉的生产源码：`开发工具/HTML验证/验证器.py`、`开发工具/统一验证/统一验证器.py` —— 而 `运行发布门禁.py:1032` 正式发布主判定就是 `开发工具/HTML验证/验证器.py`。即「发布门禁的核心判定器」不在依赖审计视野内。
- **证据**：`运行核心/依赖防火墙.py:183,186-202`；`开发工具/发布门禁/运行发布门禁.py:1032-1046`；`AGENTS.md:75,166`
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
  python3.14 -c "
  import sys; sys.path.insert(0,'.')
  import 运行核心.依赖防火墙 as f
  s={str(p.relative_to(f.系统根)) for p in f._遍历待审计源码(f.系统根)}
  print('审计文件数=', len(s))
  for t in ['开发工具/HTML验证/验证器.py','开发工具/统一验证/统一验证器.py','模块库/直播逐字稿/工具/批量精校队列.py']:
      print(('在审计范围' if t in s else '不在审计范围'), t)
  "
  ```
- **真实输出摘录**：
  ```
  审计文件数= 582
  不在审计范围 开发工具/HTML验证/验证器.py
  不在审计范围 开发工具/统一验证/统一验证器.py
  在审计范围 模块库/直播逐字稿/工具/批量精校队列.py
  ```
  另：正反例夹具证实「名字含 验证器的文件整文件不入审计」→ `[放行] 开发工具 下 名为 验证器 的文件（排除片段表命中）  审计文件=0`
- **状态**：已核实

---

## F. 第二调用腿：模块库工具直连 40007 网关；模型连接器与适配层提供者并行两条 HTTP 腿

- **级别**：重要
- **影响**：分层要求模块只经「运行核心注入的唯一能力调用器」调能力（`AGENTS.md:97-99`），唯一能力调用服务现场只在 `后端核心/后端核心.py` 实例化。但存在两条绕开它的实际调用腿：
  1. **模块库内直连网关**：`模块库/直播逐字稿/工具/批量精校队列.py:30` 自行拼 `http://127.0.0.1:40007/网关/调用`，用 `urllib.request` POST（`:69-70`），凭证默认值硬编码 `html-blackbox-verifier`（`:33`）。它既不经唯一调用器，也不经 `获取唯一调用服务`。
  2. **同一 HTTP 能力两条实现**：`支持库/适配层/模型HTTP提供者.py`（`:9-10` `urllib.request`，`注册提供者()`）与 `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:236-245` 的 `_HTTP调用模型`（自带 `urllib.request`）并行存在；连接器只从适配层复用了「端点归一/载荷构造/头检查」三个函数，HTTP 收发本身是第二条腿。注释自述为「URL连接的默认兼容调用器；与受管 Provider 保持同一协议契约」，协议别名表靠 `测试中心/支持库/测试_模型协议别名` 断言两份一致防漂移（`适配层/模型HTTP提供者.py:17-19` 注释）。
- **证据**：`模块库/直播逐字稿/工具/批量精校队列.py:30,33,61-70`；`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:233-245,575-590,630-650`；`支持库/适配层/模型HTTP提供者.py:1-12,17-19`；`运行核心/能力调用/唯一能力调用.py:1-8,54-64`；`后端核心/后端核心.py`（唯一实例化点）
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n '127.0.0.1:40007' --glob '*.py' --glob '!测试中心/**' .
  rg -rn '唯一能力调用服务\(' --glob '*.py' --glob '!测试中心/**' .
  rg -n 'urllib.request|_HTTP调用模型' 支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py
  ```
- **真实输出摘录**：
  ```
  模块库/直播逐字稿/工具/批量精校队列.py:30:    网关 = "http://127.0.0.1:40007/" + urllib.parse.quote("网关/调用")
  模块库/直播逐字稿/工具/批量精校队列.py:33:    凭证 = os.environ.get("系统库网关凭证", "html-blackbox-verifier")
  ./后端核心/后端核心.py:  self._唯一调用服务 = 唯一能力调用服务(self.注册表)
  ./运行核心/能力调用/唯一能力调用.py: 服务 = 唯一能力调用服务(注册表)   # 文档字符串示例
  ```
- **状态**：已核实（第二批为「同名能力两条实现」判定，属架构判断；协议一致性有测试兜底）

---

## G. 有界聚合动态导入白名单：`<受控聚合导入>` 跳过全部层向与第三方检查

- **级别**：重要
- **影响**：`_解析导入` 对 `importlib.import_module("前缀." + 循环变量)` 这种「循环变量来自本文件字面量列表」的写法记为 `<受控聚合导入>`（第 112-138 行），此标记既不在标准库表也不在层名称表，后续所有判定**全部放行**。这意味着任何支持库聚合入口只要写成「for 子库名 in [字面量列表]: import_module(前缀 + 子库名)」，就能导入任意层而不被拦。
- **证据**：`运行核心/依赖防火墙.py:112-151,220-248`；现场真实使用点 `支持库/后端/系统核心支持库/__init__.py:98-101`、`支持库/后端/大语言模型支持库/__init__.py:87-90`、`支持库/后端/数据操作支持库/__init__.py:95-98`、`支持库/后端/文件系统支持库/__init__.py:39-42`、`支持库/后端/网络通信支持库/__init__.py:21-24`、`支持库/后端/办公文档支持库/__init__.py:36-39`（均为 `importlib.import_module("支持库.后端.X." + 子库名)`）
- **复现命令**（进程内正反例）：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
  # 夹具：支持库/后端/甲/__init__.py 用有界列表变量拼 "模块库.乙." + 子库名
  # → 实测 [放行]；同文件若写成 importlib.import_module("模块库.乙.丙") 字面量则会命中
  ```
- **真实输出摘录**：
  ```
  [放行] 聚合式动态导入（有界列表变量）指向 模块库  审计文件=1
  [命中] 动态导入（参数不可解析）  → 支持库→<动态参数> | 动态导入绕过依赖审计
  ```
- **状态**：已核实（正反例）。当前 6 个聚合入口自述为「有界白名单」，属设计豁免；风险在于该豁免是**文本级**的，判据只认「参数里出现了字面量列表变量名」，不校验拼接前缀指向哪一层。

---

## H. `实现/` 同包判定用字符串前缀（非按段），且仓库根文件前缀为空串

- **级别**：建议
- **影响**：第 231-235 行判定「跨包导入 实现/」时用 `模块名.startswith(文件包前缀)`，是**字符串前缀**而非「按点分段前缀」。因此 `X.A.实现evil.y`、`X.A.实现子.y` 这类以 `X.A.实现` 开头的**其它包**会被误判为同包而放行；另外文件若位于仓库根（`parts` 长度 1），`文件包前缀` 被赋成空串 `""`，`startswith("")` 恒真 → 该文件任意 `实现` 导入全放行。
- **证据**：`运行核心/依赖防火墙.py:230-236`
- **复现命令**：同 A 的进程内夹具（本次实跑）
- **真实输出摘录**：
  ```
  [放行] 前缀非段级匹配：实现子 包（字符串前缀命中 实现）  审计文件=1
  ```
  当前仓库根无直接 `.py` 文件，`实现子/实现evil` 命名包也不存在 → **理论绕过，无现实命中**。
- **状态**：已核实（判据逻辑 + 夹具）；现实影响：未确认（未发现同名前缀包）

---

## I. 公开调用完整性门禁：无 `__all__` 时「声明未导出」判据回退成任意导入名

- **级别**：建议
- **影响**：`提取导出名`（第 122-135 行）在 `__all__` 缺失时回退为「该文件全部顶层 ImportFrom 名」。于是「公开调用-声明未导出」这条（第 214-218 行）退化成「只要入口 import 过任何同名符号即通过」。同时第 133 行只识别 `ast.Assign` 形式的 `__all__`（`__all__ += [...]`、元组形式均不识别 → 落回同样宽松的回退）。
- **证据**：`开发工具/公开调用完整性门禁.py:122-135,207-225`
- **复现命令**：进程内调用 `提取导出名`（本次实跑）
- **真实输出摘录**：
  ```
  提取导出名（有 __all__）= ['读取文件']
  提取导出名（无 __all__（回退到顶层 import 名））= ['无关名字']
  提取导出名（__all__ 多出一项）= ['写入文件', '读取文件']
  ```
- **状态**：已核实（判据 + 正反例）。是否造成假绿需看具体包：**未确认**（未逐包核对是否有包缺 `__all__`）

---

## J. 组件合规「场景真实返回值」不要求成功，只要求自洽

- **级别**：建议
- **影响**：`_场景真实返回值` 的通过结论是「真实调用 N 个能力全部非空返回」（第 898 行），逐能力只判：成功不得携带失败结构、失败必须带稳定错误码（第 938-945 行注释自述「判的是自洽性」）。即一个**永远返回 `提供者不可用`** 的能力也能过这一项。作为「真实返回值」判据，名实偏宽（注释承认这是刻意的环境无关设计）。
- **证据**：`开发工具/组件合规/合规测试包.py:804-898`（`:898`、`:938-945`）；`:331` 提供者锁定的口径说明
- **复现命令**：`sed -n '880,898p' 开发工具/组件合规/合规测试包.py`
- **真实输出摘录**：`return True, f"真实调用 {len(注册表.能力id列表)} 个能力全部非空返回"`
- **状态**：已核实（读码 + 注释自述）；**未做活体反例**（构造一份恒失败组件包需要完整六件套，超出只读边界）

---

## K. 技能库作为第三根正式包根的「认得 / 不认得」清单

- **级别**：建议（漏点逐处即缺陷）
- **认得技能库**（这些位置已按三根处理）：
  - `开发工具/公开调用完整性门禁.py:22`（扫描段含 技能库）
  - `开发工具/项目编译/正式包索引.py:88-90`（`技能库, 技能库排除 = _扫描包(系统根, "技能库")`）、`:108-114`、`:132-134`、`:146-153`、`:173-186`（`包所属根`：技能库→支持库）
  - `运行核心/加载器/包发现/发现器.py:59-76`（`发现全部(支持库根, 模块根, 技能库根目录=None)`，可选第三根）
  - `后端核心/后端核心.py:109,238,274,591-645`（四个桶含技能库）
  - `项目适配层/依赖锁定/依赖锁定.py:54-56,91`、`项目适配层/依赖锁定/传递闭包.py:90-93,166`
  - `MCP工具箱/发布治理.py:540,562`
  - `开发工具/组件规范/完整性摘要.py:157-159`（`for 根目录名 in ("支持库","模块库","技能库")`）
- **不认得技能库**（漏点）：
  1. `运行核心/依赖防火墙.py:33-43,45-54` —— 见条目 A（层级规则恒空）
  2. `开发工具/发布门禁/运行发布门禁.py:67-70` `正式源码目录名表` 无 技能库 → `_正式Python源码文件()` 不枚举技能库源码 → **py_compile 语法编译门禁不覆盖技能库**
  3. `MCP工具箱/项目服务.py:307-310` `全部目录表` 无 技能库 → codegraph 代码地图探索范围不含技能库
  4. `开发工具/项目编译/项目编译器.py:31-35` `关键源码目录` 无 技能库 → `:83-86` 的「拒绝输出到源码关键目录」护栏不保护 `技能库/`
  5. `开发工具/组件合规/合规测试包.py:330-339` `_提供者锁定` 只扫 `支持库` 与 `模块库` → 依赖技能库能力的组件会被误判「依赖能力无锁定提供者」（fail-closed 误报，不会假绿）
  6. `开发工具/HTML验证/制品事实.py:63-65` 能力根识别只用 `("模块库","支持库")`
  7. `运行核心/运行环境管理器/环境管理器.py:104-110` `_定位系统根` 只用「支持库+模块库」判据
- **证据 / 复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n --glob '*.py' --glob '!测试中心/**' '技能库' . | head -40
  rg -n --glob '*.py' --glob '!测试中心/**' '支持库.{0,6}模块库|模块库.{0,6}支持库' . | grep -v '技能库'
  ```
- **真实输出摘录**：第二条命令返回 15 处（上方 1–7 的原始行；其中 `运行核心/加载器/包发现/发现器.py:67` 的注释已明确说明技能库「单独成根只是让它在目录上与支持库/模块库平级」，属已核对项）
- **状态**：已核实（逐处读码）；第 4 处「护栏不保护技能库」的现实可触发路径：**未确认**（未构造编译器输出到 `技能库/` 的场景）

---

## L. 项目适配层深入运行核心子目录 import（未经包级中文入口）

- **级别**：建议
- **影响**：项目适配层全部 import 已逐条核对：只依赖 `公共契约`（5 处）与 `运行核心.*`（6 处），**没有任何** `支持库`/`提供者`/`实现` 引用（符合 `AGENTS.md:110-111`）。但 6 处都是**直接落到运行核心的子目录模块**，不经任何包级中文入口，例如 `项目适配层/能力适配/能力适配.py:12 → from 运行核心.能力调用.HTTP连接器 import ...`、`模块绑定.py:13 → 运行核心.加载器.包发现.发现器`、`依赖锁定/传递闭包.py:23-26 → 发现器/解析器/选择器/环境管理器`。`运行核心/` 本身没有 `__init__.py` 与包声明（命名空间包），所以「包级中文入口」这条约束在此层没有落点——按现接口不算违规，但适配层与核心内部结构强耦合。
- **证据**：`项目适配层/**`（见复现命令输出）；`AGENTS.md:104-111`；`运行核心/` 目录无 `__init__.py`
- **复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n '^\s*(from|import)\s+' --glob '*.py' 项目适配层 | grep -v 'from __future__'
  ```
- **状态**：已核实

---

## M. `sys.path` / 动态装载的现场盘点（含「第二源码树」的正式部署形态与对向守卫）

- **级别**：建议
- **影响**：铁律禁止「通过 sys.path、包 `__path__`、动态导入或全局单例拼接两个源码树」（`AGENTS.md:118`）。现场盘点结论：
  1. **`__path__` 拼接：0 处**（全仓唯一 `__path__` 命中是 `测试中心/运行核心/测试_冷启动反向门禁.py:5` 的注释文本）。
  2. **二级及以上相对导入：0 处**；含「实现」的相对导入只有 2 处，且都在 `开发工具/组件规范/支持库模板生成器.py:287-288` 的**模板字符串**里（生成新包 `__init__.py` 用）。
  3. **生产 `sys.path` 写入点**（全仓 440 处命中，绝大多数是 `测试中心/` 与 `开发工具/` 脚本自举）关键位：
     - `运行核心/启动运行核心网关.py:4` → `sys.path.insert(0, ".")`：**cwd 相对**，非绝对系统根（脆弱点：从别的 cwd 启动会把错误目录置顶）
     - `支持库/适配层/{PyMuPDF,Pillow,PDF隔离,MLXWhisper}提供者/实现/子进程入口.py`、`支持库/后端/{图像处理支持库/图像解码,转写支持库/转写,文档转换支持库/PDF渲染}/实现/子进程入口.py` → 把**「平台客户端环境目录」插到 `sys.path` 首位**（如 `PyMuPDF提供者/实现/子进程入口.py:29-30,78-79`）。这就是「第二个源码树」的正式部署形态（制品环境重写导入前缀），属有意设计，且带**对向守卫**：`运行核心/运行环境管理器/提供者生命周期.py:272-279` 遍历主进程 `sys.path`，命中「提供者实现目录」即记问题（fail-closed），该守卫被发布门禁在 `开发工具/发布门禁/运行发布门禁.py:976-980` 调用。
     - `客户端/构建平台客户端.py:28-29`、`平台控制面/统一入口.py:27-28`、`平台控制面/发布管理/二阶恢复.py:23-24` → 自根插入（脚本态自举）
  4. **反射/按路径动态装载**：
     - `运行核心/加载器/包安装/模块安装.py:44-45`、`支持库安装.py:45` → `importlib.util.spec_from_file_location(模块名, 入口路径)`，**有路径越界校验**（`模块安装.py:29-38`：拒绝绝对路径与 `..`、必须 `is_relative_to(包根/系统根)`）
     - `支持库/后端/文档转换支持库/python_pptx提供者/__init__.py:12-16` → 按文件路径装载自身实现
     - `开发工具/组件合规/合规测试包.py:181-184`、`开发工具/统一验证/统一验证器.py:339-340`、`开发工具/组件规范/支持库模板生成器.py`、`测试中心/__init__.py:14-28` → 同名模式（工具/测试侧）
  5. **模块级全局单例**：`运行核心/能力调用/运行上下文/上下文.py:120` → `全局上下文管理器 = 上下文管理器()`；`唯一能力调用服务` 只在 `后端核心/后端核心.py` 实例化。未见第二实例构造点（见 F 的反面结论）。
- **证据 / 复现命令**：
  ```bash
  cd ~/Documents/Agent/PHP/系统工程平台
  rg -n 'sys\.path|__path__|spec_from_file_location|module_from_spec|SourceFileLoader|import_module\(' --glob '*.py' --glob '!测试中心/**' .
  rg -n '^\s*from\s+\.{2,}' --glob '*.py' .        # 结果：0
  ```
- **状态**：已核实（清单 + 计数）；「是否存在绕过提供者生命周期守卫的现场路径」：**未确认**

---

## 14. 正面结论（已核实无违规）

1. **跨包深入实现目录（绝对导入）：生产代码 0 处**。全仓命中 `实现` 的绝对导入 572 处，其中「文件不属于被导入包」的 61 处**全部位于 `测试中心/`**（测试经实现内部验证夹具，与调用者条款一致）。包内 `__init__.py` 导自身 `实现` 是既定入口模式（例：`模块库/媒体处理/__init__.py:8-13`）。复现：`rg -n 'from\s+(支持库|模块库)\.[\w\.]*实现' .`
2. **相对导入深入实现目录 / 二级相对导入：0 处**（唯一 2 处为模板字符串，见 M.2）。
3. **模块库未直接 import 支持库或第三方**：跨层矩阵显示 `模块库 -> 公共契约 36` 且**无** `模块库 -> 支持库/第三方`；模块实现经连接器调用（`模块库/文件管理/实现/文件管理.py:31` `响应 = 连接器.调用能力(能力id, 参数)`）。复现：AST 跨层矩阵（见命令清单）或 `rg -n '^\s*(import|from)\s+(支持库|reportlab|docx|openpyxl|fitz|PIL)' 模块库`。
4. **项目适配层未调用支持库/提供者/实现源码**（见 L）。

---

## 旧入口 404 核验（第 5 项）

- **代码核（已核实）**：`公共契约/HTTP路由契约.json:9-34` 定义唯一执行入口 `POST /网关/调用`；`:130-141` 把 `/能力/执行`、`/网关/请求` 登记为「已废弃，不做兼容」。网关路径白名单 `运行核心/统一网关/安全边界.py:26` 只含 `/健康 /网关/调用 /网关/流式 /网关/流式/取消 /能力/搜索 /能力/目录 /能力/契约`（`本地网关.py:131` 另并 `/网关/热接入 /平台/能力反馈`）；`:92-93` 未在白名单即返回「请求路径未授权」→ `本地网关.py:333-341` `_拒绝(404, "未知路径", ...)`；DOC 分支 `:503-505`/`:591-594` 也把非 `/网关/调用` 的 POST 判 405/404。
- **进程内判据验证（已核实，不联网、不写盘）**：
  ```
  校验路径 ｜能力｜执行: 通过=False 说明=请求路径未授权
  校验路径 ｜网关｜请求: 通过=False 说明=请求路径未授权
  校验路径 ｜网关｜调用: 通过=True  说明=
  校验路径 ｜能力｜搜索: 通过=True  说明=
  校验路径 ｜健康:      通过=True  说明=
  ```
  （`｜` 为规避终端输出脱敏的替代写法，源码里是 `/`）
- **现场引用**：字面量 `/能力/执行` 全仓 4 处 = 契约 1 处 + 测试 1 处 + 参考文档 2 处；`/网关/请求` 3 处 = 契约 1 处 + 文档 1 处 + 测试 1 处。**生产代码 0 调用者**。
- **真实 HTTP 探测：未执行**（见跳过项 1）。
- **状态**：已核实（代码核 + 进程内白名单判定）；真实 HTTP 状态码：**未确认**（依 `_拒绝(404,"未知路径")` 判定为 404）

---

## 命令清单（本次全部实际执行过的只读命令）

```bash
export GIT=/Library/Developer/CommandLineTools/usr/bin/git
cd ~/Documents/Agent/PHP/系统工程平台
$GIT rev-parse --abbrev-ref HEAD && $GIT rev-parse HEAD && $GIT status --porcelain

# 1 跨包深入实现目录
rg -n 'from\s+(支持库|模块库)\.[\w\.]*实现' .
# 2 相对导入
rg -n '^\s*from\s+(\.+[\w\.]*)\s+import' .        # 含「实现」2 处（模板字符串）
rg -n '^\s*from\s+\.{2,}' .                       # 0 处
# 3 第三方越界
rg -n --glob '*.py' -e '^\s*(import|from)\s+(reportlab|openpyxl|pptx|docx|fitz|pdfplumber|PIL|psutil|mlx_whisper|tree_sitter|tree_sitter_typescript|mcp|starlette|uvicorn)' 支持库 MCP工具箱
# 4 sys.path / 动态导入
rg -n 'sys\.path|__path__|spec_from_file_location|module_from_spec|import_module\(|__import__\(' --glob '*.py' .
# 5 网络腿
rg -n '^\s*(import|from)\s+(urllib|http\.client|socket|requests|httpx|aiohttp)\b' --glob '*.py' --glob '!测试中心/**' .
rg -n '127.0.0.1:40007' --glob '*.py' .
rg -rn '唯一能力调用服务\(' --glob '*.py' --glob '!测试中心/**' .
# 6 门禁与引用关系
rg -n '依赖防火墙|公开调用完整性门禁|组件合规|复用审计|审计模块库|提供者直连规则|防火墙审计|依赖生命周期审计' 开发工具/发布门禁/运行发布门禁.py
rg -rn '审计模块库|破坏验证|审计平台控制面|审计全部' --glob '*.py' --glob '!测试中心/**' .
# 7 技能库
rg -n --glob '*.py' --glob '!测试中心/**' '技能库' .
rg -n --glob '*.py' --glob '!测试中心/**' '支持库.{0,6}模块库|模块库.{0,6}支持库' . | grep -v '技能库'
rg -n --glob '*.py' --glob '!测试中心/**' 'Hermes工作区|~/Documents/Hermes' .
# 8 只读跑发布门禁里的依赖判据（不写盘）
unset PYTHONPATH && python3.14 -c "import sys;sys.path.insert(0,'.');import 运行核心.依赖防火墙 as f;r=f.审计依赖(f.系统根);print(r.审计文件数,len(r.违规列表))"
#  → 真实输出：审计文件数= 582  违规数= 0
# 9 端口只读确认
lsof -nP -iTCP:40007 -sTCP:LISTEN   # Python PID 50802 LISTEN
lsof -nP -iTCP:8766 -sTCP:LISTEN    # Python PID 40029 LISTEN
```

**进程内夹具（写盘只在 /tmp 的 tempfile 目录，仓库零写入）**：依赖防火墙 8 例正反例、公开调用完整性门禁 3 例导出名、网关路径白名单 7 例（脚本见条目 A / I / 旧入口 404 章节原文）。

---

## 跳过项

1. **真实 HTTP 探测旧入口 `/能力/执行`、`/网关/请求`**：跳过。原因：网关对越界路径走 `_拒绝(404,"未知路径")`，而 `_拒绝` 会调 `网关核心实例.审计.记录(...)`（`本地网关.py:306-317`），其实现是**追加式 JSONL 落盘**（`运行核心/运行诊断/安全审计/安全审计.py:22-30,39-63` 写 `安全审计.jsonl`）。审计边界禁止改运行数据，故改为「代码核 + 进程内白名单判定」双证据；`40007/8766` 只做只读 `lsof` 端口确认。
2. **跑发布门禁 / HTML 验证器 / 组件合规全量**：跳过（任务明确禁止跑发布门禁、禁止重编制品）。组件合规的「恒失败组件」活体反例因此未做（见 J）。
3. **构造并运行真实包级夹具（六件套）**：跳过（会写仓库内 包声明/能力契约等文件，越界）。判据验证一律用 `/tmp` 夹具 + 读码。
4. **`工程缓存/` 内代码、`.codegraph/`**：按审计总纲排除（生成物）。

## 未确认项

1. **真实 HTTP 状态码**：旧入口依代码判定为 404、未越权路径为「路径未授权」，未做线上探测（跳过项 1），现场状态码未实测。
2. **公开调用完整性门禁「导出名回退」是否造成现存假绿**：需逐包核对哪些正式包缺 `__all__`，本次未逐包统计。
3. **组件合规「真实返回值」是否放过现存恒失败能力**：需活体夹具，未做。
4. **`项目编译器` 危险输出目录护栏漏 `技能库` 的可触发路径**：未构造「输出目录指向 `技能库/`」的调用场景。
5. **`sys.path` 第二源码树是否存在绕过 `提供者生命周期.检查隔离边界` 的现场路径**：守卫生效已核实，绕过路径未穷举。
6. **模型连接器 `_HTTP调用模型` 与适配层 `模型HTTP提供者` 的协议一致性**：仅读到「靠测试断言别名表一致」的注释与测试引用，未实跑该测试。
7. **`支持库/后端` 那 11 个 `内部层: true` 包的豁免是否经过显式决策登记**：未在 `开发文档/决策记录/` 逐条比对（超出本子报告范围，建议由 09 号控制面治理子报告覆盖）。
