# 子报告 02a：测试中心「恒真断言 / 无断言测试 / 吞错 / 无效 skip / 孤儿测试」审计

- 审计对象：系统工程平台（底座），项目根 `~/Documents/Agent/PHP/系统工程平台`
- 冻结边界：分支 **主干**，HEAD **`1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`**（审计开始时用 `git rev-parse HEAD` 校验，与任务书一致）
- 审计时间：2026-09-15
- 扫描范围：`测试中心/` 下全部 `测试_*.py` = **193 个**（15 个目录）
- 方法：只读静态取证（AST + 文本扫描），**未执行任何测试**，未改源码/配置/数据，未重启服务
- 工具：`python3.14`（每次运行前 `unset PYTHONPATH`）、`grep`（本机 BSD grep 无 `-P`，改用 Python 正则）

## 0. 结论速览

| 维度 | 命中 | 判定 |
|---|---|---|
| A 恒真断言 | **1 条 / 1 文件** | 重要（真恒真，断言两侧为同一表达式） |
| B 无断言测试 | 直接无断言 **0**；空方法体 **0**；整文件零断言 **0**；断言藏在同文件辅助方法内 **18 条 / 3 文件** | 建议（不是假绿，但"测试名与断言分离"） |
| C 吞错（`except: pass`） | **20 条 / 11 文件**，其中**吞掉断言 0 条** | 建议 19 条（清理/回收路径）；**重要 1 条**（跳过坏 JSON，见 C-1） |
| D 无效/静默 skip | **0 条**（恒真 `skipIf` 0、无条件 `@unittest.skip` 0、模块级 `import` 静默降级 0、首行 `return` 跳过 0、`raise SkipTest` 0、`expectedFailure` 0） | D 维度干净；附带 1 条建议（`except OSError: skipTest`） |
| E 孤儿测试 | `测试中心/慢速层/`、`测试中心/辅助/` 下 **0 个测试文件（目录空壳）**；**163 / 193（84.5%）** 的测试文件在"测试中心以外"零引用 | **阻断（口径待总审计仲裁）**：见 E-1 |

**一句话**：测试中心的断言质量本身过硬（2052 个测试方法 / 6647 处断言，无一条恒真 skip、无一文件零断言、无一处 `except` 吞掉断言）；真正的问题不在测试怎么写，而在**没有任何正式入口会跑这 193 个文件**——`测试中心/运行测试.py`（归档验收契约声明的"唯一测试入口"）在本仓**已不存在**，发布门禁 0 处加载 unittest。

## 1. 分级问题清单

### 阻断（1 条，口径待总审计仲裁）

**E-1：193 个 unittest 测试文件在正式链路中无加载入口，"唯一测试入口"文件已不存在。**

- 归档验收契约声明：`开发文档/归档/2026-08_第十四阶段/需求快照.json:40` → "测试入口": `测试中心/运行测试.py（唯一，参数 --范围 常规|慢速|全部，跳过即失败）`
- 磁盘事实：`测试中心/运行测试.py` **不存在**（`ls 测试中心/*.py` 只列出 `测试中心/__init__.py`）
- 发布门禁不加载任何 unittest：`grep -c "unittest" 开发工具/发布门禁/运行发布门禁.py` = **0**；其 `执行门禁()` 的"运行测试"分支（`开发工具/发布门禁/运行发布门禁.py:1025-1079`）实际执行的是 HTML 黑盒验证器 `开发工具.HTML验证.验证器`（`:1045-1049`），与 `测试中心/` 无关
- 统一验证器不是测试执行器：`开发工具/统一验证/统一验证器.py:27` 的范围列表 `["结构","契约","资产","装配","加载器","示例项目","项目适配"]`，无"测试"范围
- 仍被引用的"唯一发现机制"是 `测试中心/__init__.py:40`（`文件列表 = sorted(测试中心目录.rglob("测试_*.py"))`，`load_tests` 协议），但其文档字符串（`:7`）给出的命令是 `python3.14 -m unittest discover -s 测试中心 -p '测试_*.py' -v`，而项目 `AGENTS.md:138` 明令"**不使用 unittest discover 或不存在的聚合测试脚本**"
- 按项目 `AGENTS.md:168` 铁律"独立脚本未被发布入口调用等同门禁不存在"，以及 `AGENTS.md:135` 声明的"门禁：实际加载全部测试"，**该门禁在正式发布链路上不存在**
- 为什么可能不算阻断（如实标注）：HTML 黑盒验证器是另一条**真实 HTTP** 验证链路，可能是有意以它替代 unittest 全量；且本仓历史文档（`开发文档/未完成事项.md:119`）仍在讨论"`测试中心/运行测试.py` 调 pytest 是否带 `-rs`"，说明该文件曾存在。**是否等同于测试全量失守，需总审计与其他子报告交叉裁决。**
- 命令：
  ```bash
  ls 测试中心/*.py
  grep -c "unittest" 开发工具/发布门禁/运行发布门禁.py
  grep -rn "运行测试" --include='*.py' --include='*.json' . | grep -v 工程缓存
  ```

### 重要（2 条）

**A-1：恒真断言 1 条。**

- `测试中心/平台控制面/测试_工作包11_消费者契约注册表.py:61`
  ```python
  self.assertEqual(查询["数据"], 查询["数据"], "绑定记录必须持久化一致")
  ```
  `assertEqual` 两个实参是**同一个表达式**，无论注册表是否真的持久化一致都会通过；断言文本"必须持久化一致"与代码语义不符（应比对落盘后重新读取的结果）。原意图疑似为 `self.assertEqual(json.loads(存储文件.read_text())[...], 查询["数据"])`（同方法 `:59-60` 已有落盘读取）。
- 该行是**全量 193 文件里唯一的真恒真断言**，其余 6646 处断言的实参互不相同。

**C-1：`except json.JSONDecodeError: pass` 静默跳过损坏 JSON，扫描范围还被路径字符串收窄。**

- `测试中心/第一批维修/测试_句柄与数值契约冻结.py:52`
  ```python
  if "验证场景" not in 文件.as_posix():
      continue
  try:
      扫描(json.loads(文件.read_text(encoding="utf-8")), ...)
  except json.JSONDecodeError:
      pass          # ← 损坏的 JSON 被当作"无需检查"
  ...
  self.assertEqual(违规, [])   # :54 最终断言
  ```
- 双重问题：① 一个语法损坏的 JSON 会被静默跳过，测试仍然判"通过"；② 扫描范围按**路径含「验证场景」**过滤，实测只覆盖 **183 / 1345** 个 JSON（`支持库`+`模块库`+`技能库` 下，13.6%），其余 1162 个 JSON 不进入"句柄与数值契约冻结"检查，而断言是 `违规 == []`（范围外恒为真）。
- `except` 未吞掉任何断言本身（C 类 ALGORITHM 已程序化确认：20 条吞错中，`try` 体内含断言者 **0 条**）——风险在"检查范围被静默缩小"。

### 建议（20 条）

- **B-1 断言与测试方法分离（18 条 / 3 文件）**：测试方法体自身无断言，断言在同文件的私有辅助方法里。判定为**不是假绿**（辅助方法内确有断言），但方法名承诺的检查在调用链外，破坏性修改辅助方法不会被这些用例名直接定位。分布：
  - `测试中心/支持库/测试_进程管理.py`（9 条，如 `:37 → self._断言失败(...)`，断言在 `:101-104 _断言超时` 一类辅助方法）
  - `测试中心/开发工具/测试_项目编译器.py`（7 条，`:30-49 → self._验证模块Provider闭包(...)`）
  - `测试中心/第一批维修/测试_客户端构建安全.py`（2 条，`:133`、`:156 → self.断言失败含路径(...)`）
- **C-2 清理/回收路径的 `except: pass`（19 条 / 10 文件）**：均为测试收尾（关闭管道、回收子进程、恢复文件权限），吞掉的是"清理动作失败"，不掩盖断言。按类型：`subprocess.TimeoutExpired` 6、`(OSError, ValueError)` 6、`ProcessLookupError` 3、`(AttributeError, OSError)` 2、`OSError` 1、`(BrokenPipeError, ConnectionResetError)` 1。
- **D-1 `self.skipTest` 写在 `except OSError` 分支内（1 条）**：`测试中心/开发工具/测试_说明书生成器.py:81` —— `symlink_to` 抛 `OSError` 即跳过整条用例；本机 macOS 正常支持符号链接，该分支不会触发，属可接受写法，但"跳过原因"由异常隐式给出。37 次 `skipTest` 中其余 36 次都有显式环境守卫（见 D 节）。
- **E-2 测试互引/清单硬编码（1 处）**：`测试中心/开发工具/测试_反向破坏门禁28.py:24-37` 硬编码了 20 个"允许实现层直导"的测试文件名，`:41` 再 `glob("测试_*.py")` 遍历；测试改名/新增需同步维护这张白名单，否则该门禁会误报。全量测试之间**无 import 互引**（`grep -rn "^from 测试中心\|^import 测试中心\|import 测试_" 测试中心/` = 0 命中），连带失败风险低。
- **E-3 空壳目录（2 个）**：`测试中心/慢速层/`（含 `第十三阶段/`、`第十四阶段/`、`第十八阶段/`）与 `测试中心/辅助/`（含 `可复现构建审计/`、`灾难恢复审计/`、`统一体验面审计/`）**均为空目录，0 个文件**，配套的 `--范围 慢速` 入口（`测试中心/运行测试.py`）也不存在 → "慢速层/辅助层测试"目前是纯概念，无实体。

## 2. A 类：恒真断言

**命中 1 条 / 1 文件（占 193 文件的 0.52%）。**

| 位置 | 代码 | 判定 |
|---|---|---|
| `测试中心/平台控制面/测试_工作包11_消费者契约注册表.py:61` | `self.assertEqual(查询["数据"], 查询["数据"], "绑定记录必须持久化一致")` | 两侧同一表达式 → 恒真 |

检测口径（AST，覆盖以下模式，**全量 0 命中**）：
- `assertTrue(常量真)` / `assertTrue(1)` / `assertTrue("非空")`
- `assertFalse(常量假)` / `assertFalse(0)`
- `assertEqual(a, a)` / `assertIs(a, a)` / `assertNotEqual(a, a)` / `assertGreater(a, a)` 等两侧同一 AST 表达式
- `assertIsNotNone(字面量)` / `assertIsNone(None)`
- 裸 `assert True` / `assert 1`

## 3. B 类：无断言测试

**直接无断言 0 条；空方法体 0 条；整文件零断言 0 条。**

| 口径 | 数量 |
|---|---|
| 测试文件 | 193 |
| 测试方法（`test_*` / `测试_*`，含 `unittest.TestCase` 子类内方法） | **2052** |
| 断言调用（`self.assert*` + 裸 `assert`） | **6647** |
| 方法体无任何断言/异常预期（直接） | **0** |
| 方法体只有 docstring（空测试） | **0** |
| 整个文件零断言 | **0** |
| 断言在同文件辅助方法内（间接，见 B-1） | 18 条 / 3 文件 |

判定器口径：方法体内是否存在 `ast.Assert`、`ast.Raise`、`self.assert*`（`attr.startswith("assert")`）、`self.fail`、`assertThat`/`expect`；再加 2 层调用链解析（`self._断言失败(...)` → 辅助方法内是否有断言）。零命中表示**没有"跑完什么也不验就绿"的测试**。

命令：
```bash
unset PYTHONPATH && python3.14 - <<'PY'
import ast,os
ROOT="~/Documents/Agent/PHP/系统工程平台"
files=[os.path.join(dp,f) for dp,dn,fn in os.walk(ROOT+"/测试中心") if "__pycache__" not in dp for f in fn if f.startswith("测试_") and f.endswith(".py")]
m=a=0
for p in files:
    t=ast.parse(open(p,encoding="utf-8").read())
    for n in ast.walk(t):
        if isinstance(n,ast.ClassDef):
            m+=len([x for x in n.body if isinstance(x,(ast.FunctionDef,ast.AsyncFunctionDef)) and (x.name.startswith("test") or x.name.startswith("测试"))])
        if isinstance(n,ast.Assert): a+=1
        if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr.startswith("assert"): a+=1
print(len(files),m,a)
PY
```
（实测输出：`193 2052 6647`）

## 4. C 类：吞错 / 静默失败

**命中 20 条 / 11 文件。其中 `try` 体内含断言的 0 条**（即没有"断言被 `except` 吞掉"的情形）。

| `except` 类型 | 条数 | 性质 |
|---|---|---|
| `subprocess.TimeoutExpired` | 6 | 清理（等子进程退出超时） |
| `(OSError, ValueError)` | 6 | 清理（关流） |
| `ProcessLookupError` | 3 | 清理（SIGKILL 后进程已消失） |
| `(AttributeError, OSError)` | 2 | 清理（`os.chflags` 恢复权限） |
| `OSError` | 1 | 清理（`chmod 0o755` 恢复权限） |
| `(BrokenPipeError, ConnectionResetError)` | 1 | 清理（测试内桩 HTTP 服务写响应） |
| `json.JSONDecodeError` | 1 | **重要**，见 C-1 |

逐条证据（`path:行号`）：

- `测试中心/MCP工具箱/测试_工作区清理增强.py:131` `except (AttributeError, OSError): pass`（`os.chflags` 置不可变）
- `测试中心/MCP工具箱/测试_工作区清理增强.py:208` `except (AttributeError, OSError): pass`
- `测试中心/MCP工具箱/测试_工作区清理增强.py:212` `except OSError: pass`
- `测试中心/支持库/测试_MLXWhisper提供者.py:43` `except subprocess.TimeoutExpired: pass`
- `测试中心/支持库/测试_PDF隔离提供者.py:91` `except (OSError, ValueError): pass`
- `测试中心/支持库/测试_PDF隔离提供者.py:116` `except subprocess.TimeoutExpired: pass`
- `测试中心/支持库/测试_PDF隔离提供者.py:123` `except (OSError, ValueError): pass`
- `测试中心/支持库/测试_PDF隔离提供者.py:153` `except subprocess.TimeoutExpired: pass`
- `测试中心/支持库/测试_PDF隔离提供者.py:160` `except (OSError, ValueError): pass`
- `测试中心/支持库/测试_Pillow提供者.py:124` `except subprocess.TimeoutExpired: pass`
- `测试中心/支持库/测试_Pillow提供者.py:130` `except (OSError, ValueError): pass`
- `测试中心/支持库/测试_PyMuPDF提供者.py:53` `except subprocess.TimeoutExpired: pass`
- `测试中心/支持库/测试_PyMuPDF提供者.py:59` `except (OSError, ValueError): pass`
- `测试中心/支持库/测试_密码签名提供者_生命周期.py:39` `except subprocess.TimeoutExpired: pass`
- `测试中心/支持库/测试_密码签名提供者_生命周期.py:46` `except (OSError, ValueError): pass`
- `测试中心/第一批维修/测试_LibreOffice受管池.py:216` `except ProcessLookupError: pass`（后接 `else:` 分支 `self.fail`，语义正确）
- `测试中心/第一批维修/测试_Provider进程.py:209` `except ProcessLookupError: pass`（`finally` 内清理）
- `测试中心/第一批维修/测试_任务进程收敛.py:98` `except ProcessLookupError: pass`（收尾 SIGKILL）
- `测试中心/第一批维修/测试_句柄与数值契约冻结.py:52` **`except json.JSONDecodeError: pass` ← 重要（C-1）**
- `测试中心/运行核心/测试_HTTP连接器.py:57` `except (BrokenPipeError, ConnectionResetError): pass`

另：**裸 `except:` 0 处**，**`except: continue` 0 处**。全量 `try/except` 中吞错为 `pass` 或 `continue` 的仅以上 20 处。

## 5. D 类：无效 / 静默 skip

**无效 skip：0 条。** 逐项清零证据：

| 模式 | 命中 |
|---|---|
| `@unittest.skip`（无条件跳过） | **0** |
| 恒真/恒假 `skipIf(True/False, ...)`、`skipUnless(True/False, ...)` | **0**（10 处 skip 装饰器全部是条件表达式，非常量） |
| `try: import X` / `except ImportError: pass` 把整个测试跳掉 | **0**（模块级 `try` + `pass/continue` 0 处，模块级 `try` + `pass` 0 处） |
| 测试方法首行 `return` 当跳过 | **0** |
| 条件早退 `if ...: return` 当跳过 | **0** |
| `raise unittest.SkipTest` / `expectedFailure` / `pytest.mark.skip` / `pytest.skip` | **0**（全仓测试不依赖 pytest） |

skip 的实际总量（**全部有原因字符串**，符合"未解释跳过即失败"的门禁口径）：
- **装饰器 10 处**，全是环境守卫型 `skipUnless`：
  - `测试中心/支持库/测试_psycopg提供者.py:148` `@unittest.skipUnless(_驱动可用(), 'psycopg 未安装')`
  - `测试中心/支持库/测试_进程管理.py:63` `@unittest.skipIf(有无内核沙箱, '本机有 sandbox-exec，无法验 fail-closed')`
  - `测试中心/模块库/测试_OCR.py:215,221,227,236,242,252,258` `@unittest.skipUnless(_工具可用(), '本机未配置 tesseract，如实跳过')`
  - `测试中心/第一批维修/测试_HTML验证器.py:679` `@unittest.skipUnless(os.name == 'posix', '进程组回收仅在 POSIX 验证')`（挂在类上）
- **`self.skipTest(...)` 调用 37 次 / 12 文件**：36 次带显式环境守卫（`if not 查找LibreOffice命令():`、`if not self.可用:`、`if not 有无内核沙箱:`、`if shutil.which("textutil") is None:` 等，逐条守卫文本已在明细中核对）；**唯一 1 条分支型**：`测试中心/开发工具/测试_说明书生成器.py:81`（写在 `except OSError:` 内，见 D-1）。
- 全部 37 次均带中文原因字符串；`测试中心/支持库/测试_Tesseract提供者.py`（10 次）与 `测试中心/模块库/测试_媒体处理.py`（5 次）、`测试中心/支持库/测试_系统探针.py`（4 次）是环境缺失时的主要跳过源——**本机若缺 tesseract/ffmpeg/LibreOffice，这些用例会跳过而非失败**，覆盖度随环境漂移（属设计选择，已在原因字符串中如实声明）。

## 6. E 类：孤儿测试

### 6.1 任务书指定的两个目录

| 目录 | 测试文件数 | 结论 |
|---|---|---|
| `测试中心/慢速层/`（子目录：第十三阶段、第十四阶段、第十八阶段） | **0** | 空壳目录，无文件（`find 测试中心/慢速层 -type f` = 空） |
| `测试中心/辅助/`（子目录：可复现构建审计、灾难恢复审计、统一体验面审计） | **0** | 空壳目录，无文件 |

→ **原口径"慢速层/辅助下的测试文件是否被入口引用"在本仓不适用：这两个层没有任何测试文件**。与之配套的 `--范围 慢速` 入口（`测试中心/运行测试.py`）也不存在（见 6.3）。

### 6.2 全量 193 个文件的引用面（替代口径，更有解释力）

扫描 2692 个非缓存文件（`.py/.md/.json/.yaml/.sh/.txt/.cfg/.toml`，排除 `工程缓存/`、`__pycache__`、本审计目录），以文件名词干匹配 `测试_XX` 与模块路径 `测试中心.X.测试_XX`：

| 指标 | 数量 | 占比 |
|---|---|---|
| 测试文件总数 | 193 | 100% |
| **在"测试中心以外"的任何文件中零引用** | **163** | **84.5%** |
| 有测试中心以外的引用（含 JSON 契约字段） | 30 | 15.5% |
| 被全仓 JSON 的 `"验证场景": "测试中心/..."` 字段点名（去重文件） | 4 | 2.1% |

- 有引用的 30 个里，多数是**声明性引用**而非执行入口：如 `公共契约/HTTP路由契约.json:41,48` → `测试中心/MCP工具箱/测试_能力网关.py`、`开发文档/归档/2026-08_第十四阶段/工作包清单.json:15` → `测试中心/平台控制面/测试_工作包01_普通用户视图.py`（该工作包清单 16 处全部指向已归档阶段的文件）
- 真正的"被生产代码引用"仅 3 处：`MCP工具箱/项目服务.py:341,343`（`测试_平台客户端制品接入.py`、`测试_项目服务.py`，作为工具清单/映射的一部分）、`运行核心/运行诊断/诊断中心/诊断中心.py:54,57`（`测试_能力契约.py`、`测试_加载器.py`，作为诊断提示）、`开发工具/公开调用完整性门禁.py:176`（`测试_公开调用完整性门禁.py`）
- **结论**：193 个测试文件里 **163 个（84.5%）只能靠人手在命令行敲模块路径才能跑到**（`AGENTS.md:128` 的 `python3.14 -m unittest 测试中心.<受影响测试模块>`）；没有任何门禁、清单或 CI 会自动跑到它们。

### 6.3 唯一的批量发现机制与它为何不生效

- 机制：`测试中心/__init__.py:40`
  ```python
  文件列表 = sorted(测试中心目录.rglob("测试_*.py"))
  ```
  该 `load_tests` 协议**能覆盖全部 193 个文件**（含中文文件名），导入失败会被转成失败用例（`:44-54`），不会静默跳过——写得是对的。
- 但不生效：项目 `AGENTS.md:138` 明令"不使用 unittest discover 或不存在的聚合测试脚本"；`测试中心/__init__.py:7` 文档里给的命令正是 `python3.14 -m unittest discover -s 测试中心 -p '测试_*.py' -v`；而归档验收契约声明的"唯一测试入口"`测试中心/运行测试.py` 已不存在。
- 结果：**193 个文件"可被发现"但"无入口驱动"** → 见 E-1（阻断级待仲裁）。

## 7. 全量统计表

| 项目 | 数量 | 占 193 文件 | 占 2052 测试方法 |
|---|---|---|---|
| 测试文件（`测试中心/**/测试_*.py`） | 193 | — | — |
| 测试方法 | 2052 | — | — |
| 断言调用（`self.assert*` + 裸 `assert`） | 6647 | — | 平均 3.24/方法 |
| A 恒真断言 | 1 条 | 0.52%（1 文件） | 0.05% |
| B 直接无断言 | 0 条 | 0% | 0% |
| B 空方法体 | 0 条 | 0% | 0% |
| B 整文件零断言 | 0 个 | 0% | — |
| B 间接断言（辅助方法内） | 18 条 | 1.55%（3 文件） | 0.88% |
| C 吞错 `except: pass` | 20 条 | 5.70%（11 文件） | — |
| C 吞掉断言 | 0 条 | 0% | 0% |
| D 无效 skip（恒真/无条件/静默降级/首行 return） | 0 条 | 0% | 0% |
| D 全部 skip 装饰器 | 10 处（均带条件+原因） | — | — |
| D 全部 `skipTest` 调用 | 37 次（均带原因；36 次有环境守卫） | — | — |
| E 慢速层/辅助 测试文件 | 0 个（空目录） | — | — |
| E 测试中心以外零引用的文件 | 163 个 | **84.5%** | — |

目录分布（193 个）：支持库 39、第一批维修 29、运行核心 26、开发工具 21、模块库 21、MCP工具箱 20、平台控制面 16、公共契约 5、支持库/核心 5、加载器 3、发布门禁 2、启动监督器 2、项目适配 2、底座升级 1、示例项目 1。
（未列入上表的 `第五阶段/第六阶段/第七阶段/第八阶段/第九阶段/第十阶段/第十一阶段/第十三阶段/第三阶段/第四阶段/结构迁移/资源并发/权威状态` 等目录下**无** `测试_*.py`。）

## 8. 本次实际跑过的命令

> 环境：macOS，所有 Python 均 `unset PYTHONPATH` 后执行 `python3.14`。**未执行任何测试、未跑门禁、未改任何文件。**

```bash
# 0. 冻结边界校验
cd ~/Documents/Agent/PHP/系统工程平台
git rev-parse HEAD        # → 1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc
git branch --show-current # → 主干
find 测试中心 -name '测试_*.py' | wc -l          # → 193

# 1. 本机 grep 无 -P（BSD grep），改 Python AST + 正则
grep -rnP 'assertTrue\(\s*True' ...   # → grep: invalid option -- P（报错原文，已弃用）

# 2. A/B/C/D 全量 AST 扫描（脚本见第 3、4、5 节命令块，及 /tmp/子报告02a_明细.json）
unset PYTHONPATH && python3.14 - <<'PY' ... PY

# 3. 引用面 / 孤儿统计
unset PYTHONPATH && python3.14 - <<'PY' ... PY    # 扫 2692 个非缓存文件

# 4. 发现机制取证
ls 测试中心/*.py                                  # → 只有 __init__.py，无 运行测试.py
grep -c "unittest" 开发工具/发布门禁/运行发布门禁.py          # → 0
grep -rn "unittest\|TestLoader\|测试中心" --include='*.py' 开发工具 运行核心 平台控制面 MCP工具箱
grep -rn "运行测试" --include='*.py' --include='*.json' --include='*.md' . | grep -v 工程缓存
sed -n '1025,1079p' 开发工具/发布门禁/运行发布门禁.py          # “运行测试”实为 HTML 黑盒验证器
head -60 测试中心/__init__.py
grep -rn '"验证场景": "测试中心/' --include='*.json' . | wc -l  # → 10 处

# 5. 单独核对的关键片段
sed -n '52,66p'   测试中心/平台控制面/测试_工作包11_消费者契约注册表.py   # A-1
sed -n '38,56p'   测试中心/第一批维修/测试_句柄与数值契约冻结.py         # C-1
sed -n '70,90p'   测试中心/开发工具/测试_说明书生成器.py               # D-1
sed -n '14,46p'   测试中心/开发工具/测试_反向破坏门禁28.py             # E-2
find 支持库 模块库 技能库 -name '*.json' -path '*验证场景*' | wc -l     # → 183 / 1345
find 测试中心/慢速层 测试中心/辅助 -type f                            # → 空
```

## 9. 未确认项

1. **所有测试的实际通过/失败状态未知**：本审计只做只读静态取证，未执行测试（既受冻结边界限制，也受"不跑全量测试"的效率要求）。A/B/C/D 的判定全部基于**代码结构**，不代表运行期行为。
2. **E-1 的定级待仲裁**：HTML 黑盒验证器是否被有意设计为 unittest 全量的替代品，本子报告无法裁决；`测试中心/运行测试.py` 何时被删除、是否有意删除，未查 Git 历史（冻结边界未禁止，但本轮未做）。
3. **B 类间接断言的调用链只解析了 2 层**：更深的间接（辅助方法再调用别的文件的公共函数）会被误判为"无断言"——但实测连 2 层解析后命中即为 0，故该风险未影响结论。
4. **C 类只做 AST 结构判定**：没有验证这些 `except: pass` 在运行期是否会真的吞掉有价值信息（例如清理失败导致残留进程/残留目录）。`测试中心/第一批维修/测试_句柄与数值契约冻结.py:48` 的路径过滤（`"验证场景" not in 文件.as_posix()`）是否有意为之，未确认。
5. **D 类"未解释跳过"的实际判定未跑**：判定器是 `MCP工具箱/验证门禁.py:169 _检出未解释跳过`，而"跳过是否带原因"本轮只按肉眼+正则确认 37 次 `skipTest` 均有原因字符串，未通过该判定器实测（会出现偶发假红的口径问题已在 `开发文档/未完成事项.md:119` 记录）。
6. **引用面统计以文件名字符串为口径**：动态拼接路径（如 `f"测试_{名称}.py"`）引用不会被计入——`开发工具/组件规范/模块模板生成器.py:161`、`开发工具/组件规范/支持库模板生成器.py:373` 就是这种生成式引用，故"163 个零引用"应理解为"无静态点名引用"，不等于运行期不可能被加载。
7. `测试中心/慢速层/`、`测试中心/辅助/` 的空壳子目录是有意保留的占位还是历史残留，未确认。
