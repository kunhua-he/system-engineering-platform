# 底座专业审计 · 总报告（2026-09-15）

> 审计对象：系统工程平台（华哥口径的「底座」）
> 冻结边界：分支 `主干`、HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`、工作树 0 脏（审计前后一致）
> 方式：11 路只读子代理 + 父会话亲自复现；**全程未改源码/配置/运行数据/制品/数据库，未重启任何服务，未跑发布门禁**
> 唯一写入：本目录 `开发文档/临时文档/20260915_底座专业审计/`
> 取证纪律：文档声称 ≠ 现场事实；子代理回执 ≠ 证据，凡进「阻断」的都由父会话亲自复现或现场核对

---

## 一、一句话结论

**底座的骨架是真的、门禁链是真的、编译制品与 HEAD 是一致的**（六环 0 违规、组件合规 112 包 0 未通过、依赖审计 0 违规、摘要 112 包 0 不闭合、HTML 黑盒 536/536 能力通过——这些我都亲自复跑过）。
**真正的问题集中在三类：① 安全护栏形同虚设（沙箱可一行目录名绕开、SSRF 校验没接线、解压无上限）；② 运行态与源码不同步（40007 与 8766 都在跑旧代码，且新参数被静默丢弃仍返回成功）；③ 「已完成的声称」与现场不符（文档、测试入口、制品仓库、验收命令）。**

分级合计（去重后）：**阻断 8 / 重要 40+ / 建议 55+**

---

## 二、阻断级（8 条，逐条给出父会话取证方式）

### B1【安全·已复现】沙箱可被一行「工作目录」绕开，逃逸到整机
- 机理：`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:270` 把 `工作目录` 原样插进 SBPL 文本（`(allow file-read* file-write* (subpath "{工作目录}"))`），只 `resolve()` 防 `..`，**未转义 `"`**；SBPL 双引号闭合后可用 `;` 注释吸收模板尾部，从而注入任意 `(allow ...)`。
- 父会话复现（经唯一网关 40007 真实调用 `系统核心支持库.进程管理.沙箱执行命令`）：工作目录名 `x")) (allow file-read* (subpath "/")) ;` → **HTTP 200、成功=true、退出码 0、真实读到工作区外文件**；对照普通目录退出码 1 读不到。
- 附带：**profile 语法错误时能力仍返回 `成功=true`**（退出码 65 被吞）。
- 复现：`python3.14 /tmp/底座审计_沙箱逃逸复现.py`｜详见 `子报告_07_安全面.md`（S-01/S-12）

### B2【运行态·已核实】40007 网关内跑的是旧实现，新参数被静默丢弃且仍返回 200 成功
- PID 50802 启动于 12:42:12，而 12:43–13:32 有 8 次提交。父会话探针：`大语言模型支持库.模型连接器.重试调度` 传 `实际错误码=B`（不在白名单）仍返回 `是否重试=true` —— 13:16 才加的语义没生效。
- 更危险的是**静默**：能力详情实时读盘所以「看着是新的」，参数过滤走 12:42 的内存注册表 → 新参数被边界剔除，**请求照样 200 成功**。
- 详见 `子报告_06`（F-01）

### B3【运行态·已核实】8766 MCP 跑的是 3 天前代码
- PID 40029 启动于 **9-12 11:27**，而 `MCP工具箱/项目服务.py` 改于 **9-15 01:11**、`热重载.py` 改于 9-12 11:10；从未触发热重载。→ MCP 侧一切「真实 HTTP 验收」测的都是旧代码。

### B4【边界·已核实】正式包内嵌业务品牌词与地域，违反底座/业务切割
- `模块库/直播逐字稿/能力契约/参数契约.json:370`、`工具/批量精校队列.py:43,46`、`说明/使用说明.md:63` 写死「华世王镞、免费祛斑、合作店、河北邢台、任泽区」。
- 与「底座不写死行业细节、业务细节由调用方传参」的裁决直接冲突（该包已进正式索引、随制品分发）。

### B5【验收·已复现】AGENTS.md 声明的正式验收入口真实运行失败
- `AGENTS.md:132` = `python3.14 示例项目/适配层示例/运行入口/运行.py`
- 直接跑：退出码 1，卡 `[5] 提供者网关启动失败: 缺失凭证: 环境变量 系统库网关凭证 未设置`
- 补 `系统库网关凭证`（进程内设）再跑：退出码 1，卡 `[6] 调用失败: 写入=权限不足 访问凭证缺失或无效; 读取=…; 分割=…`
- 即**不是单纯缺环境变量**，示例自身没通过鉴权链；而 `示例项目/前后端核心示例/运行示例.py` 退出码 0（说明是这一条示例的问题）。

### B6【测试·已复现】193 个测试文件没有任何聚合入口，且已长出僵尸测试
- `测试中心/运行测试.py` **不存在**（磁盘只有 `__init__.py`），`测试中心/慢速层/`、`测试中心/辅助/` 是**空目录**；AGENTS.md 又明令禁用 `unittest discover`。
- **父会话实核：`grep -c unittest 开发工具/发布门禁/运行发布门禁.py` = `0`** —— 发布门禁从不加载 unittest（`:1025` 的 `运行测试` 实为 HTML 黑盒验证器）。按 AGENTS.md 自己的铁律「未被发布入口调用等同门禁不存在」，**193 个测试文件当前没有任何正式入口驱动**。
- 唯一异常：`测试中心/支持库/测试_psycopg提供者.py` 真实 `import` 失败（引用了 9-14 数据库收口时删掉的 `关闭/连接/查询/事务执行`）→ **永久无法运行**，且它还在 `测试中心/开发工具/测试_反向破坏门禁28.py:29` 白名单里。
- 复现：`python3.14 -B -c "import 测试中心.支持库.测试_psycopg提供者"`（193 个里唯一失败；含连字符文件名 0 个）

### B7【安全·子代理声称，父会话未复现】MCP 工具目录拿到写权限与执行权
- `workspace` 把调用者传入的 `path` 当项目根 → 可对任意 git 仓库（含本仓）提交/切分支/合并；
- `test_resource` 允许登记「本进程自身 pid」→ 清理时 `killpg` 自身进程组（可自杀 8766）；
- 父会话已核实：`MCP工具箱/项目服务.py`、`角色权限.py` 里**凭证/鉴权相关行 0 处**（8766 无认证，仅 127.0.0.1）。
- 未复现部分：提交/自杀的可达性（需实际构造，本轮按纪律未做）→ 详见 `子报告_11`

### B8【口径·已核实】「唯一测试入口/唯一网关/唯一制品」三条唯一性声称都不成立
- 测试入口：不存在（B6）
- 唯一执行网关：40007 是唯一执行口（40006 是只读能力发现网关，见「误报纠正」），但**文档写能力发现网关默认 8866，实际跑在 40006**
- 制品唯一：`工程缓存/制品仓库/平台客户端制品/` 现场 **12 份内容寻址制品**（文档写「只留 1 个权威」）

---

## 三、重要级（按主题合并，只列头部）

| 主题 | 条数 | 头部问题 | 证据 |
|---|---|---|---|
| 契约三产物 | 9 | 类型边界在运行期失效（注册表类型名非正式且不在别名表／`JSON值型` vs `文本型`）；整数位宽边界未落实；361/536 条调用示例缺必填参数（照抄必失败）；契约漏列参数致 22 个实现形参永不可达；一致性校验器 `声明一致` 在生产装配**零调用点** | `子报告_03` |
| 架构与门禁判据 | 7 | 第三根正式包根「技能库」未被 `运行核心/依赖防火墙.py` 收录（层级规则对它**恒空**，父会话核实 0 处）；第三方判据是硬编码清单（漏 psutil/mlx_whisper 等）；**复用审计 / 平台控制面防火墙审计 在发布门禁里 0 调用**（父会话已改判，见纠正） | `子报告_04` + 父会话 |
| 资源与生命周期 | 5 | 26 个调用点「每次调用泄漏一个 SQLite 连接」；3 张权威账本表只增不删且启动全量灌内存（句柄 9130/租约 9166/进程 3631 行，父会话现场计数）；事件日志零有界 | `子报告_05` + 父会话 |
| 文档与结构残留 | 10 | 权威文档与现场不一致（项目说明 16 条抽查 5 条错；未完成事项 ≥5 条已修事项仍挂开放区且与同文件「判定全绿」自相矛盾）；结构残留普遍未清（`__pycache__` 315、历史制品 12 份 186M、空目录 267）；目录规则 2 处硬违规（决策记录编号重复缺号、归档命名不合规） | `子报告_10` |
| 安全（非阻断） | 7 | 重定向 SSRF 绕过；**环境变量能力可返回宿主全部环境与任意 `.env` 明文**（父会话核实 `实现/环境变量.py:49,53-92`）；**SSRF 校验能力 0 生产调用方**（父会话核实: `网络通信支持库.出站安全.校验出站URL` 全仓只出现在自己的摘要与目录表）；**解压文件无体积上限**（父会话核实签名 `文件系统补充.py:82` 无上限参数）；`trust_remote_code=True` | `子报告_07` + 父会话 |
| 网关契约 | 6 | 18 个对外错误码里 6 个无公开说明、7 个无 HTTP 状态映射（落 500）；`KeyError` 全局映射把「任务/包不存在」也报成「能力不存在」；CORS 未授权任何来源；审计落盘随 TMPDIR 漂移 | `子报告_06` |
| 测试假绿 | 4 | 父会话已复核成立：`测试中心/加载器/测试_加载器.py:36-45` `test_重复包id被拒绝` **断言的是测试自己体内重写的计数循环**（`发现全部()` 返回值被丢弃）→ 删光生产重复检测照样绿；同文件 `:31-34` `test_能力id唯一` 只断言 `发现.成功`；`测试中心/运行核心/测试_外部版本探针统一.py:58` patch 掉生产探针本体 `检查系统工具` 后只断言 `call_count == 2`（docstring 自述「patch 生效即证明」）；`测试中心/第一批维修/测试_句柄与数值契约冻结.py:48-56` 扫描面被 `'验证场景' not in path: continue` 收窄到 13.6% 的 JSON、坏 JSON 静默 `pass`，末尾却断言 `违规 == []` | `子报告_02b` + `02a` + 父会话 |
| 边缘目录 | 5+28 | 前端核心/启动监督器大面积零调用零测试；指纹门禁生产恒假；类型表文档与机器表分叉；契约版本双口径 | `子报告_11` |
| 死代码/空转 | 2+6 | **机制未接线**：`反向破坏验证`（287 行 / 13 种破坏场景）生产代码与测试 **0 引用**；`契约.声明一致` 校验器 0 调用点（→ 03 的 9 类漂移永不被拦）；`运行核心/租约管理.py`(138 行)、`版本系统/兼容适配器.py`(73)、`状态切换.py`(75)、`启动监督器/容量基线.py`(166) 等 **真·零引用模块**；两个编译器模块**无 `__main__`**（`-m` 静默 exit 0 的空转）；依赖审计打印空标题；慢速层/辅助层是空壳目录 | `子报告_08`（父会话接手） |
| 运行数据库 | 2 | 10 个域表 5 个恒空（会话/发布/检查点索引/缓存索引/灰度状态）；**测试假数据写进正式库**（`灰度观测` 唯一一行是 `验收-灰度-1`/`验收.能力`） | `子报告_09`（父会话接手） |

---

## 四、对子代理结论的纠正（父会话现场核对，避免误导）

1. **40006 不是「第二条执行网关」**：它是 `MCP工具箱/能力网关.py`（只读能力发现网关），探 `/健康` 返回 `404 路由不存在`。子报告 06 的 F-05 不成立，应改判为「端口与文档不一致（文档写 8866，实跑 40006）」。
2. **8765 不属于底座**：命令行是 `…/华世王镞_v3/MCP工具箱/运行时/MCP服务入口_v3独立.py`，cwd 在 V3。子报告 07 的 S-11 把它并入底座安全面属**越界**，已剔除。
3. **发布门禁确实调用了依赖生命周期审计（`:624`）**：子报告 04 说「未调用依赖生命周期审计/反向破坏」不准确；真正 0 调用的是「复用审计」与「平台控制面/提供者/防火墙审计」（父会话 `grep -c` 已核）。
4. **沙箱「哨兵文件仍存在」类断言**：`测试中心/第一批维修/测试_统一编译器.py:62` 确实测到了「校验先于 rmtree、被拒即不删」这条顺序性质，只有哨兵存在性那句空转 → 从阻断下调为重要。
5. **文档数字多数可复现，但未标「历史快照」**：515/536/549/534、116/112/111 包、13.2MB/8.7MB/3.33MB 等，读者无法判断哪个是当前值。
6. **S-05「网关凭证硬编码进源码」被夸大**（父会话复核）：生产源码里**没有**硬编码凭证；命中的两处字面量在**测试文件**内（`测试中心/第一批维修/测试_网关安全与有界并发.py:37`、`测试中心/运行核心/测试_网关安全负测.py:169`），属测试夹具（写法上仍建议改为进程内 `os.environ` 注入）。网关侧是正确做法：`运行核心/统一网关/本地网关.py:128` 读环境变量 `系统库网关凭证`，`启动运行核心网关.py:26` 注释写明「凭证只从受控环境变量加载」。真正的问题是 `开发文档/项目说明.md:136,141,514` 用**明文示例凭证**写教程（示例值 ≠ 部署凭证）。→ 从「重要」下调为「建议」。

### 批次收口说明（哪些报告由谁完成）
- 3 路失败/超时：01（统计口径，HTTP 503×2 → **父会话接手**）、09（控制面/运行库，503 → **父会话接手**）、08（死代码/空转，1800s 超时且零产物 → **父会话自行重跑补入**）。
- 11 号（边缘目录）在超时前**已完整落盘 209KB**，父会话按其结论做了 3 条现场复核（见下）。
- 02（测试真实性，503）已拆成 02a/02b 两路重派并完成。

---

## 五、需华哥一句话的（4 项）

1. **沙箱这条要立刻修吗？** 修法我建议：拒绝含 `"`/`\`/换行的路径 + SBPL 结构化拼装 + profile 语法错误必须返回失败（不伪绿）。
2. **40007/8766 要不要重启到当前源码？** 重启会中断在跑任务，且按纪律我没动；重启后我会重跑「重试调度 实际错误码」探针复验。
3. **`模块库/直播逐字稿` 里的业务品牌词**：是抽成调用方参数包（底座去行业化），还是接受底座带业务词？
4. **测试体系口径**：193 个 unittest 是否恢复一个聚合入口（含「全量可导入性」检查），还是正式承认「只走 HTML 黑盒 + 定向 unittest」并把 `测试中心/运行测试.py` 的引用全部清掉？

---

## 六、未确认项（如实列出）

- 子报告 11 的 MCP `workspace`/`test_resource` 可达性未实际构造复现（只核实了 8766 无认证）。
- 子报告 03 的「15 参类型边界失效」「22 个形参不可达」未逐条由父会话复现（其结论基于全量脚本比对，可信度中）。
- `权威状态库.进程` 表 3631 行里死亡 PID 占比、以及是否按实例裁剪，未查写入侧代码。
- 12 份制品中 11 份非激活件，除状态账本外是否还有别处引用（只扫了 信任/状态/HTML证据/发布证据 四处）。
- 发布门禁本身**本轮未重跑**（写证据 + 激活指针，按只读审计纪律跳过）；如需可单独授权执行。

---

## 七、报告索引（全部在本目录）

| 文件 | 内容 |
|---|---|
| `00_审计总纲与边界.md` | 冻结边界与父会话基线复算 |
| `子报告_00_父会话独立复核.md` | 父会话第一手证据（含沙箱逃逸复现、纠正、运行态三条） |
| `子报告_01_统计口径与可执行性复算.md` | 193 个测试文件可导入性 + 僵尸测试 |
| `子报告_02a_测试恒真断言与孤儿测试.md` | 恒真断言/吞错/skip/孤儿 |
| `子报告_02b_测试mock掩盖与断言强度.md` | mock 假绿四类 + 断言强度 |
| `子报告_03_契约三产物一致性.md` | 能力定义↔契约↔实现↔说明书 |
| `子报告_04_架构边界与依赖腿.md` | 分层/深导/第二腿/门禁判据 |
| `子报告_05_资源与生命周期.md` | 连接/句柄/日志有界性 |
| `子报告_06_运行态与网关真实HTTP.md` | 真实 HTTP 探针矩阵 |
| `子报告_07_安全面.md` | 注入/SSRF/凭证/解压 |
| `子报告_09_平台控制面治理与运行数据库.md` | 两个 SQLite 库逐表核查（父会话接手） |
| `子报告_10_文档权威与结构残留.md` | 权威文档 vs 现场 |
| `子报告_11_前端与适配层与MCP工具箱覆盖.md` | 边缘目录覆盖 |
| `子报告_08_死代码空转与重复实现.md` | 零引用模块、未接线机制、空转入口（父会话接手完成） |
