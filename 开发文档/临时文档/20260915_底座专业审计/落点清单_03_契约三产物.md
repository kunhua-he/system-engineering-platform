# 落点清单 03 · 契约三产物一致性（第二轮：逐条二次证真 + 修复落点）

> 对象：`子报告_03_契约三产物一致性.md`（重要 11 条 / 建议 7 条），逐条独立复算
> 冻结边界：分支 `主干`、HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`
> 方式：**只读**二次证真（现场命令 + AST/JSON 全量复算），未改任何源码 / 配置 / 运行数据 / 制品 / 数据库，未重启服务，未跑发布门禁
> 唯一写入：本文件
> 本文中「报告」= 子报告_03；「实测」= 本轮现场命令输出

---

## 〇、口径前提（定真前必须锁死的一件事）

| 口径 | 路径 | 权威级别 | 谁消费 |
|---|---|---|---|
| **参数契约** | `<包>/能力契约/参数契约.json` | **权威源①** | 对外契约、文档、门禁 |
| **能力定义** | `<包>/能力定义.json` | **权威源②**（应与①逐字段镜像） | 能力搜索、说明书生成 |
| **注册元数据** | `<包>/__init__.py` → `注册能力()` | **从属源**（应由①②派生） | 运行期网关真实消费 |
| **实现形参** | `<包>/实现/*.py` | **行为事实源**（不可被声明改写） | Python 调用 |

**关键机制（决定修复方向，实测行号）**：网关在边界按**注册元数据**参数名剔除未知字段、按**注册元数据**的类型名做校验——
- 必填判据 = `项.get("必填") is True`：`运行核心/统一网关/网关核心.py:276`
- 未知字段剔除 = `_过滤能力参数`：`运行核心/统一网关/网关核心.py:237`，调用 `_应用参数别名`（`:250`，别名表定义 `:29-33`）
- 类型归一化表（历史短名 12 个）：`运行核心/统一网关/网关核心.py:285-288`
- 类型判据表（仅 5 类 + 4 类数值分支）：`:290-296` / `:297-303`；未命中落 `elif 类型匹配 is not None...`（`:304`）→ **静默通过**

**推论（贯穿全部条目）**：**注册元数据是运行期真正的"权威"，契约是名义权威。** 因此修复必须遵循一条铁律——**改权威源（契约/定义），让注册元数据跟；只有当契约本身错时才改契约**，且每处改动必须同步「契约→定义→注册→说明书」四份，否则又造出新的影子口径。

---

## 一、重要-1 ～ 重要-11

### 重要-1｜注册类型名非正式且不在别名表 → 网关类型校验被静默跳过

| 列 | 内容 |
|---|---|
| **二次证真** | `grep -n '句柄\|任意' 支持库/前端/组件控件/__init__.py` → `:14` 创建页面`窗口句柄="句柄"`、`:15` 创建组件`页面句柄="句柄"`、`:16` 设置组件属性`组件句柄="句柄"`/`属性值="任意"`、`:17` 绑定组件事件`组件句柄="句柄"`（**报告写 14/16/17，实测偏移 1 行，以现场为准**）<br>`grep -n 数值型 支持库/后端/并发控制支持库/__init__.py` → `:107` 等待秒、`:133`/`:153` 退避秒（与报告一致）<br>契约侧实测：`组件控件.创建页面` 窗口句柄=**整数型**、`设置组件属性` 组件句柄=**整数型**/属性值=**字典型**；`并发控制支持库.占用容量` 等待秒=**双精度数型**<br>`运行核心/统一网关/网关核心.py:285-288` 别名表 12 项 = 文本/整数/长整数/单精度数/双精度数/浮点数/逻辑/布尔/列表/字典/映射 —— **不含** `句柄`/`任意`/`数值型` |
| **判定** | **证真**（静态 + 行号 + 契约真值三重核对；报告的运行期复现结论方向成立） |
| **修复落点** | ① 权威源不动（契约已正确）。② **从属源必修**：`支持库/前端/组件控件/__init__.py:14,15,16,17` 把 `"句柄"`→按契约改 `"整数型"`（页面/组件/窗口句柄在本包实现里就是整数句柄）；`:16` `"属性值":"任意"`→`"字典型"`；元组式 `"文本"/"字典"`→`"文本型"/"字典型"`。<br>③ `支持库/后端/并发控制支持库/__init__.py:107,133,153` `"数值型"`→`"双精度数型"`（契约即此，网关数值分支 `:297-303` 直接生效）。<br>④ **兜底（第二道防线）**：`运行核心/统一网关/网关核心.py:285-288` 别名表补 `"数值型": "双精度数型"` 一处即可覆盖全仓；`句柄`/`任意` **不建议**进别名表（语义不唯一：句柄=整数型，任意/JSON值型 才是"任意值"）。<br>⑤ 若担心未知类型继续静默：把 `:304` 的 `elif 类型匹配 is not None` 改为"未知类型名 → 直接报 `参数不合法（未登记类型）`"，但这是**网关行为变更**，必须单独排期 + 全量扫一遍全仓注册类型名后再开 |
| **影响面** | 需改注册元数据（约 2 包 7 处）；**若动网关别名表/未命中分支，则属网关行为变更**：需重编译网关制品、重跑发布门禁、说明书记载口径变更；不改能力集合、不改包版本（可顺带升补丁位）；无摘要级联（注册元数据不进完整性摘要） |
| **是否共享文件** | `运行核心/统一网关/网关核心.py` = **强共享**（与子报告 06 运行态、04 架构边界、其它参数别名议题撞车）→ 串行 |
| **验证命令** | `unset PYTHONPATH && python3.14 -c "import ast,os;src=open('支持库/前端/组件控件/__init__.py',encoding='utf-8').read();print([n for n in ('句柄','任意','数值型') if '\"%s\"'%n in src])"` → 期望 `[]`<br>网关侧：改后 `curl -X POST .../网关/调用 -d '{"能力id":"组件控件.设置组件属性","参数":{"组件句柄":"不是整数而是文本","属性名":"标题","属性值":{"a":1}}}'` → 期望 `参数不合法：...必须是 整数型`（不再落到 `句柄不合法`） |

---

### 重要-2｜注册 `JSON值型` vs 契约 `文本型`（7 个参数，运行期边界被放宽）

| 列 | 内容 |
|---|---|
| **二次证真** | 独立 AST 复算全仓「注册参数类型 ≠ 契约参数类型」= **7 条**，且**全部集中在 `数据集合`**：<br>`列表添加.条目`、`列表移除.条目`、`列表查找.目标`、`字典设置.值`、`字典获取.默认值`、`按字段查找.匹配值`、`过滤.等于值` → 注册 `JSON值型`，契约 `文本型`（实测打印逐条一致）<br>`grep -n JSON值型 支持库/后端/数据操作支持库/数据集合/__init__.py` → `:31,32,34,35,36,40,41`（与报告完全一致） |
| **判定** | **证真**（行号 1:1 吻合） |
| **修复落点** | 二选一，**推荐 (B)**：<br>**(A) 改契约**（把权威源放宽）：`支持库/后端/数据操作支持库/数据集合/能力契约/参数契约.json` 7 处 `文本型`→`JSON值型` + `能力定义.json` 同字段镜像 → 优点：与实现真实行为一致（实现确实接受任意 JSON 值，报告 §重要-2 已实测 `列表添加` 接受字典并成功）；缺点：对外契约语义变宽，需写明"任意 JSON 值"并同步说明书。**注意 `:31` 与 `:32/:34/:40/:41` 的语义不同**：`列表添加.条目` 是"任意 JSON 元素"，`字典设置.值` 是"任意 JSON 值"，`按字段查找.匹配值`/`过滤.等于值` 的匹配语义与文本强相关，建议逐条判断而非批量替换。<br>**(B) 改注册（保持契约）**：`数据集合/__init__.py:31,32,34,35,36,40,41` `"JSON值型"`→`"文本型"` → 优点：契约不动、零级联；缺点：若确有意支持非文本，会把实现能力人为关掉（需先确认实现是否真做类型判别）。<br>**先决动作**：读 `数据集合/实现/*.py` 对应函数体，确认是"文本专用"还是"任意 JSON"，再定 A/B —— 这是本节唯一没做的动作（属实现语义判定，超出静态比对） |
| **影响面** | 走 (A)：改契约 + 能力定义 + 说明书 + 漂移检测基线（`开发工具/契约编译/漂移检测.py` 的类型漂移规则会重算）；如包内改了契约版本，需重生成 `完整性摘要.json` 与说明书。走 (B)：仅注册元数据，无级联 |
| **是否共享文件** | 否（`数据集合` 独占本包） |
| **验证命令** | `python3.14 -c "import json;c=json.load(open('支持库/后端/数据操作支持库/数据集合/能力契约/参数契约.json',encoding='utf-8'));src=open('支持库/后端/数据操作支持库/数据集合/__init__.py',encoding='utf-8').read();import re;print({x['能力id']:[(p['名称'],p['类型']) for p in x['参数']] for x in c['能力契约'] if 'JSON' in src})"` → 改后两侧类型应逐条相等 |

---

### 重要-3｜`整数型`/`长整数型` 位宽未落实，错误码 `数值溢出` 形同虚设

| 列 | 内容 |
|---|---|
| **二次证真** | `sed -n '1,30p' 支持库/后端/数据操作支持库/类型转换/实现/类型转换.py` 实测：`:14 def 文本转整数(文本, 进制=10)`、`:19 return 结果.成功结果(int(值.值, 进制))`、`:22 except (OverflowError, TypeError): :23 return 结果.失败("数值溢出", ...)`、`:25-26 def 文本转长整数 → return 文本转整数(文本, 进制)`<br>即：`int()` 为任意精度，**只有 >4300 位十进制才抛 OverflowError**；32/64 位边界无人断言 → `数值溢出` 不可达。契约返回声明 `结果型/值=整数型`，错误码含 `数值溢出`（实测契约含该码）<br>权威源侧：`公共契约/基础类型/类型目录.md`（类型表节）明确「`整数型` 有符号 32 位（-2³¹~2³¹-1）、`长整数型` 64 位」；机器口径表 `公共契约/基础类型/类型表.py:7-14` 列 `正式类型表`（含 16 个正式名） |
| **判定** | **证真**（实现行号与报告一致；位宽冻结口径来自类型目录正文，实测确认） |
| **修复落点** | 权威源（类型目录）不动，修实现：`支持库/后端/数据操作支持库/类型转换/实现/类型转换.py:19` 改法 ->在 `int()` 后加范围断言：`值2 = int(值.值, 进制)`；`if not -2**31 <= 值2 <= 2**31-1: return 结果.失败("数值溢出", "整数超出 32 位范围")`；`:22-23` 保留 OverflowError 兜底。<br>`文本转长整数`（`:25-26`）**不要**继续直接委托 —— 改为在本函数内做 64 位断言后调用一个共用的内部函数（或给 `文本转整数` 加 `位宽: int = 32` 私有形参，不改公开契约）。<br>权威源一致性：契约 `…/类型转换/能力契约/参数契约.json` 已列 `数值溢出`，**无需改契约**；`文本转长整数` 返回 `值=长整数型` 应与实现断言对齐 |
| **影响面** | 仅改实现；**需重跑该包的验证场景/单测**（`测试中心` 内相关用例）；不改契约、不改注册、不改包版本必需，但**行为变更**（原本成功的大数现在失败）→ 必须列入变更说明，并检查是否有内部调用方传大数（`grep -rn "文本转整数" 支持库 模块库`）；说明书如写"任意精度"需同步修订 |
| **是否共享文件** | 否（本包独占），但**行为变更会影响调用方**，与子报告 04 依赖腿可能有交叉 |
| **验证命令** | `python3.14 -c "import sys;sys.path.insert(0,'.');from 支持库.后端.数据操作支持库.类型转换.实现.类型转换 import 文本转整数 as f;print(f('99999999999999999999').成功, f('99999999999999999999').错误码)"` → 期望 `False 数值溢出`<br>回归：`f('123').值 == 123`；`f('2147483647').成功 is True`；`f('2147483648').错误码 == '数值溢出'` |

---

### 重要-4｜`资源管理` 7 个能力契约写 `结果型`，实现返回裸值

| 列 | 内容 |
|---|---|
| **二次证真** | `grep -n '^def 创建内容摘要\|^def 创建不可变快照\|^def 创建唯一运行目录\|^def 原子写入\|^def 原子替换\|^def 比较并交换\|^def 安全释放' 支持库/后端/系统核心支持库/资源管理/实现/资源管理.py` 实测：<br>`:79 -> str`、`:91 -> str`、`:135 -> Path`、`:142 原子写入(`、`:196 -> tuple[bool,str]`、`:210 -> tuple[bool,str]`、`:289 -> tuple[bool,str]`（7/7 与报告一致）<br>`grep -c '结果\.'` 同文件 = **52**（报告判定依据是"这 7 个函数体内无 `结果.`"，本轮未逐函数体内联统计，**属部分复算**） |
| **判定** | **证真（签名级）**；"7 个函数体内零 `结果.`"为报告结论，本轮未再逐函数切片 |
| **修复落点** | **推荐改实现（与全仓统一结果哲学一致）**：`支持库/后端/系统核心支持库/资源管理/实现/资源管理.py` 7 个函数返回 `结果.成功结果(...)` / `结果.失败("码", ...)`（`:79, :91, :135, :142, :196, :210, :289`）→ 需同时补全失败码字面量，使 `契约错误码[]` 全部可达（与【建议-1】「`创建不可变快照.快照失败` 虚列」**同源修复**：先加实现码，虚列自然消失）。<br>备选：把契约返回改成裸值声明（`str`/`bool`…）——**不推荐**，因为网关 `网关核心.py:228 _能力结果类型合法` 会按契约校验，且全仓 528/536 用 `结果型`，破例会造成第三套口径。<br>权威源侧：契约 `…/资源管理/能力契约/参数契约.json` 的 `返回` 与 `错误码` **保持不变**（本来就是正确的目标态） |
| **影响面** | 改实现（7 函数）+ 需重跑本包验证场景 + 影响所有 `资源管理` 调用方（子报告 04 依赖腿、运行核心 装载/租约/受管状态相关），**调用方返回形态从裸值变 `结果` → 属破坏性变更**，须逐一改调用点；契约/定义/注册三产物不变；说明书需按契约重新生成；如包版本升级须重算 `完整性摘要.json` |
| **是否共享文件** | 否（本包独占），但**调用方散落在运行核心**，与其它主题可能撞车 → 需先 `grep -rn "资源管理.原子写入\|创建内容摘要" 运行核心 平台控制面` 清点 |
| **验证命令** | `python3.14 -c "import inspect,sys;sys.path.insert(0,'.');import 支持库.后端.系统核心支持库.资源管理.实现.资源管理 as m;[print(n, inspect.signature(getattr(m,n)).return_annotation) for n in ['创建内容摘要','创建不可变快照','创建唯一运行目录','原子写入','原子替换','比较并交换','安全释放资源']]"` → 改后应全部显示 `结果`<br>行为回归：`m.原子写入(...)` 返回对象应含 `.成功/.错误码` |

---

### 重要-5｜`桌面宿主.启动桌面窗口` 同一函数两种返回形态（成功 `int` / 失败 `结果`）

| 列 | 内容 |
|---|---|
| **二次证真** | `grep -n 'def 启动桌面窗口\|return ' 支持库/前端/桌面宿主/实现/桌面宿主.py` 实测：`:10 def 启动桌面窗口(*, 标题..., 状态目录: str = "") -> int`、`:21 return 0`、`:23 return 结果.失败("宿主不可用", ...)`、`:31 return 结果.失败("宿主不可用", ...)`、`:47 根.mainloop(); return 0`<br>契约 `返回 = {"类型":"整数型"}`、注册 `返回="整数型"`（报告所引一致） |
| **判定** | **证真（静态）**。运行期未触发（报告 U-1 保持未确认，本轮同样不做——触发即弹窗，越界） |
| **修复落点** | 二选一，**推荐 (A)**：<br>**(A) 改实现为统一 `结果`**：`支持库/前端/桌面宿主/实现/桌面宿主.py:10` 注解 `-> int` 改 `-> 结果`；`:21`、`:47` 的 `return 0` 改 `return 结果.成功结果({...})`（值可含 `退出码`/`帧数`）；`:23`、`:31` 保持 `结果.失败`。**同时**改权威源两处：契约 `支持库/前端/桌面宿主/能力契约/参数契约.json` 的 `返回.类型` `整数型`→`结果型`、`能力定义.json` 同字段镜像、注册 `支持库/前端/桌面宿主/__init__.py` `返回="整数型"`→`"结果型"`。<br>**(B) 只改实现**：删掉两处 `return 结果.失败(...)`，改为返回非 0 整数/异常 —— 与"宿主不可用要说清原因"冲突，不推荐。<br>**(C) 最小止血**：保持两形态，先把 `整数型` 改成 `JSON值型` 让网关不拦 —— 属掩盖，不采纳 |
| **影响面** | 走 (A)：实现 + 契约 + 定义 + 注册 + 说明书 五处同步（同一个包内，级联可控）；`完整性摘要.json` 与包版本是否同步升级按项目约定；`示例项目/平台控制面` 里若有直调需改 |
| **是否共享文件** | 否（本包独占） |
| **验证命令** | `python3.14 -c "import ast,json;m=ast.parse(open('支持库/前端/桌面宿主/实现/桌面宿主.py',encoding='utf-8').read());f=[n for n in m.body if getattr(n,'name','')=='启动桌面窗口'][0];print(ast.unparse(f.returns))"` → 期望 `结果`<br>契约对齐：`python3.14 -c "import json;print(json.load(open('支持库/前端/桌面宿主/能力契约/参数契约.json',encoding='utf-8'))['能力契约'][0]['返回'])"`。**运行期验证需在无图形环境 + 受管验证=True + 临时状态目录的受控进程内做**（报告 U-1 路径），本清单不执行 |

---

### 重要-6｜错误码：实现真产生、契约未列（10 条）

| 列 | 内容 |
|---|---|
| **二次证真** | 逐条比对「实现字面量」vs「契约 `错误码[]`」，**10/10 全部复现**：<br>`大语言模型支持库.模型连接器.注册本地进程` 契约 `[参数不合法, 句柄失效]` **缺 `进程未绑定`**（实现 `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:781`）<br>`媒体处理支持库.FFmpeg媒体.检查提供者` / 适配层 `FFmpeg媒体.检查提供者` 契约 `[提供者不可用]` **缺 `参数不合法`**<br>`文档转换支持库.LibreOffice转换.检查提供者` / `textutil转换.检查提供者` 及两个适配层同名能力 契约 `[提供者不可用]` **缺 `外部提供者不可用`**（实现 `…/LibreOffice提供者/实现/文档转换.py:61,65`、`文档转换支持库/LibreOffice转换/实现/文档转换.py:56,59`、`…/textutil提供者/实现/文本转换.py:52,55`、`…/textutil转换/实现/文本转换.py:52,55`）<br>`LibreOffice转换.转换办公文件`（适配层）**缺 `提供者配置错误`**（`支持库/适配层/LibreOffice提供者/实现/文档转换.py:448`）<br>`简单窗口.创建窗口` / `简单窗口.获取窗口描述` **缺 `返回结果不符合契约`**（`模块库/简单窗口/实现/简单窗口.py:26,48,51,74,80,126` 六处产出，实测） |
| **判定** | **证真（10/10）** |
| **修复落点** | **权威源改契约**（补充声明，实现无需改）：<br>① `支持库/后端/大语言模型支持库/模型连接器/能力契约/参数契约.json`（`注册本地进程` 项）`错误码[]` 增 `进程未绑定`；② 各 `检查提供者` 项 `错误码[]` 增 `参数不合法` / `外部提供者不可用`（4 份契约：媒体处理支持库.FFmpeg媒体、文档转换支持库.LibreOffice转换、文档转换支持库.textutil转换、适配层各自两份）；③ `LibreOffice转换.转换办公文件` 增 `提供者配置错误`；④ `模块库/简单窗口/能力契约/参数契约.json` 两个能力增 `返回结果不符合契约`。<br>**同步四处**：对应 `能力定义.json` 的 `错误码` 数组（镜像）→ 注册元数据（若 `注册能力` 内联错误码，如 `模块库/简单窗口/__init__.py`）→ `说明/使用说明.md` 的「错误码」行（本包说明书当前写「错误码：无」的要一并消掉）。<br>**治理建议（更优）**：`进程未绑定`/`外部提供者不可用`/`提供者配置错误` 三个私有码重复出现在"适配层 + 支持库双层"，建议在 `公共契约` 里登记为**公共错误码**再引用，避免以后每加一层又漏一次（需评估是否动 `网关核心.py:64-84` 公开错误码表 → 与重要-1 同属网关共享文件，串行） |
| **影响面** | 改契约 + 能力定义 + 说明书（3 份/包 × 4-5 包）；**不动实现、不动注册、不重编译、无摘要级联**（除非包版本号一并升位）；漂移检测基线需重算 |
| **是否共享文件** | 契约/定义/说明书各自独立；**若采纳"登记公共错误码"则撞 `运行核心/统一网关/网关核心.py:64-84`（强共享，与重要-1 串行）** |
| **验证命令** | `python3.14 -c "import json;d=json.load(open('支持库/后端/文档转换支持库/LibreOffice转换/能力契约/参数契约.json',encoding='utf-8'));print([x['错误码'] for x in d['能力契约'] if x['能力id'].endswith('检查提供者')])"` → 改后应含 `外部提供者不可用`<br>反向核对（确保没有造出虚列，与建议-1 联动）：`grep -c '外部提供者不可用' 支持库/后端/文档转换支持库/LibreOffice转换/实现/文档转换.py` ≥ 1 |

---

### 重要-7｜契约漏列参数 → 实现形参经网关不可达（22 条）— **本轮结论有修正**

| 列 | 内容 |
|---|---|
| **二次证真** | 独立 AST 复算「实现形参集合 − 契约参数集合」，**22 条中 19 条复现**（逐条实测，见下），`密码签名.*` 3 条：`支持库/后端/密码签名提供者/实现/能力调用.py` **路径不存在**，实际路径为 **`支持库/适配层/密码签名提供者/实现/能力调用.py`**（报告路径写错），内容与报告一致（`:25 签名(..., 超时秒: float=...)`、`:31 验证签名`、`:38 公钥指纹`、`:8` 行注释自述"注册能力契约不含该参数"）→ **22/22 均成立，仅路径需订正**。<br>**关键修正（本轮新发现）**：报告的机制描述「契约没声明 → 网关剔除」**只在"注册也没声明"时才成立**。实测逐条核对注册元数据：<br>• **注册表也无该形参 → 真死参（7 类）**：`组件控件.创建窗口(项目id,所有者)`、`组件控件.创建页面(标题)`、`上下文压缩.压缩历史(句柄,会话id)`、`资源管理.{创建,读取,更新,释放}受管状态(项目id,用户id)`、`记忆支持库.{写入,追加,完成,删除,搜索,最近,列出}记忆(库路径)` → 注册文件内该参数名出现次数 = **0**<br>• **注册表有该形参 → 经网关可达，属"契约漏列"而非死参（3 类）**：`并发控制支持库.获取信号量(超时秒)`、`网络通信支持库.请求.下载文件(最大字节数)`、`密码签名.{签名,验证签名,公钥指纹}(超时秒)`（`支持库/适配层/密码签名提供者/__init__.py:25` 实测有 `超时秒/双精度数型/必填False/默认30`）→ 这三类现状是"能用但契约没写"，修复性质是**补文档**，不是"打通死参" |
| **判定** | **证真（22/22，其中 19 条 + 3 条路径订正）；但"死参"定性需拆成 15 条真死参 + 4 条契约漏列**（`会话存储.父会话id` 注册内含该名 6 处 → 归"契约漏列"更稳妥，需按包内上下文人工确认归属） |
| **修复落点** | 分两类处理：<br>**A. 真死参（契约与注册都缺、实现有）→ 权威源补参数**：在对应 `能力契约/参数契约.json` + `能力定义.json` 的该能力 `参数[]` 内补声明（`名称`/`类型`/`必填`/`默认值`/`说明`），再在 `__init__.py` 注册参数表补同项。优先清单（按业务价值）：`记忆支持库.*` 7 处 `库路径`（**可切换存储位置，当前永远用默认库**）、`资源管理.*` 4 处 `项目id/用户id`（**租户隔离失效**）、`组件控件.创建窗口` `项目id/所有者`、`组件控件.创建页面` `标题`。<br>**B. 契约漏列（注册有、契约无）→ 只补契约与定义**：`并发控制支持库.获取信号量` 补 `超时秒`（注册在 `支持库/后端/并发控制支持库/__init__.py:97`）、`网络通信支持库.请求.下载文件` 补 `最大字节数`（注册 `:31` 附近已有）、`密码签名.*` 3 条补 `超时秒/双精度数型/必填False/默认30`。注册元数据**不动**。<br>**C. 反向项**：`并发控制支持库.获取信号量` 注册含 `超时秒` 而契约无 —— 与 B 同一处修复，一次补齐两侧 |
| **影响面** | A 类：契约+定义+注册+说明书 四份落盘；**网关将开始对入参做未知字段剔除的"反向"生效**（原来被剔除的字段现在会被接受并传给实现）→ 属**行为开放**，需确认实现内这些形参有默认值兜底（有默认值即向后兼容，实测均有）；是否重编译制品按项目约定（契约变 → 漂移检测基线变）。B 类：纯声明补齐，零行为变更。**均不涉及摘要级联**（`完整性摘要.json` 覆盖制品，不覆盖 `__init__.py` 注册表，须以项目实际摘要口径复核） |
| **是否共享文件** | 各自包内；`记忆支持库`、`资源管理`、`并发控制支持库` 是运行期热点包，**与子报告 05 资源与生命周期主题撞车风险高** → 串行 |
| **验证命令** | 复算 22 条：`python3.14 - <<'PY'`（AST 取实现形参）+ JSON 取契约参数，逐条打印差集 → 改后期望差集为空<br>**运行期验证（真死参是否打通）**：网关 `curl` 调 `记忆支持库.列出记忆` 传 `{"库路径":"/tmp/xxx"}`，若返回的库与不传时不同 → 打通；不传时返回默认库（报告 §重要-7 已实测 `{}` 可用）<br>注册元数据核对：`grep -c '"库路径"' 支持库/后端/记忆支持库/__init__.py` → 改后应 > 0（当前 = 0） |

---

### 重要-8｜契约 `调用示例` 大量缺必填参数，示例照抄必失败

| 列 | 内容 |
|---|---|
| **二次证真** | 独立 JSON 全量复算 536 条契约：<br>• `调用示例` 为非 `{能力id, 参数}` 包裹式的 = **0 条**（报告写「8 条裸参数式」，**本轮口径下未复现**，可能报告把"参数缺失"与"格式非包裹"混算 → 需按报告口径复查这 8 条）<br>• 示例参数缺「契约标 `必填=true`」的项 = **367 条**（报告 361，差异 6 条）<br>• 其中示例 `参数 = {}` 空对象 = **223 条**（报告 247，差异 24 条）<br>方向与量级**完全一致**（约 2/3 示例不可执行），差异来源是"必填"判定口径（是否把"实现有默认值故可省"计入可省） |
| **判定** | **证真（量级一致，计数有 ±6/±24 的口径差；"0 条裸参数式"与报告的 8 条冲突，需报告方复核）** |
| **修复落点** | 权威源 `调用示例` 字段批量修：<br>① 生成脚本：按每个能力的 `参数[]` 里 `必填=true` 的项，从 `说明/使用说明.md` 或实现默认值/类型构造最小可执行值（文本型→`"示例"`、整数型→`1`、逻辑型→`true`、列表型→`[]`、字典型→`{}`、路径类→`/tmp` 前缀），写回 `能力契约/参数契约.json` 的 `调用示例.参数`。<br>② 落点不在单个文件，而在**生成器**：`开发工具/契约编译/契约编译器.py`（或 `开发工具/说明书生成/`）内新增"示例必填完备性"生成步骤 → 一次覆盖 536 条；`开发工具/契约编译/漂移检测.py` 增加一条"示例缺必填 → 报错"规则，防止回归。<br>③ 若手工修：优先修 `技能库/后端/技能库/能力契约/参数契约.json:30-33`、`:142-144`、`网络通信支持库.请求.*`、`XML解析支持库.*` 等被报告点名的条目 |
| **影响面** | 只改契约（+定义如含示例镜像）；**不改行为、不改注册、不重编译**；但契约是权威源 → 改完需重生成说明书、重算漂移基线；建议同时把生成步骤挂门禁（与【重要-10】同一治理动作，一次排期做） |
| **是否共享文件** | `开发工具/契约编译/契约编译器.py`、`漂移检测.py` = **强共享**（与重要-10、建议-3/4 全部有关）→ 串行，一次改完 |
| **验证命令** | 复算脚本：对 536 条契约，`示例参数 ⊇ {必填参数名}` 计数 → 改后期望 = 536<br>最小可执行性抽测（真实网关，只读能力）：`curl ... -d '{"能力id":"XML解析支持库.解析XML","参数":'$(python3.14 -c "import json;print(json.dumps(json.load(open('支持库/后端/XML解析支持库/能力契约/参数契约.json',encoding='utf-8'))['能力契约'][0]['调用示例']['参数'],ensure_ascii=False))")'}'` → 期望 `成功: true` |

---

### 重要-9｜使用说明.md 与契约冲突（版本滞后 / 「错误码：无」/ 字段不一致）

| 列 | 内容 |
|---|---|
| **二次证真** | 现场实测：<br>• 版本滞后：`技能库/后端/技能库/说明/使用说明.md:4 - 版本：1.0.1` vs `包声明.json:5 "版本": "1.0.2"` **复现**<br>• `grep -c '错误码：无' 支持库/后端/数据操作支持库/说明/使用说明.md` = **48**（与报告一致）；**全仓 `使用说明.md` 内「错误码：无」合计 = 171 节**（与报告 171 完全一致）<br>• 必填相反：`支持库/后端/办公文档支持库/说明/使用说明.md:16/27/38` 实测 `- 必填参数：文件路径, 格式, 最大字节数, 超时秒` 等，而契约该能力仅 `文件路径` 必填 **复现**<br>• 覆盖率：`使用说明.md` **113 份**、`设计说明.md` **112 份**（报告写 112/112；多出的 1 份需定位是哪个非标准包，不影响结论） |
| **判定** | **证真（4 项证据全部复现，其中「171 节」精确吻合；文件数 113 vs 报告的 112 需订正）** |
| **修复落点** | ① **先定性产物属性**（报告 U-6 未确认）：查 `开发工具/说明书生成/` 是否存在生成器与命令记录 —— 若为生成物，**唯一落点是生成器**，禁止手改 113 份 md；若为手写物，则落点是 `开发工具/契约编译/漂移检测.py` 增加"说明书↔契约"校验规则 + 逐包重写（113 份，工作量大）。<br>② 具体字段修复口径：版本行 ← `包声明.json.版本`；「错误码」行 ← 契约 `错误码[]`（现 171 处写「无」的直接删掉该行或按契约列出）；「必填参数/可选参数」← 契约 `参数[].必填`（如 `办公文档支持库/说明/使用说明.md:16,27,38` 只保留 `文件路径`）。<br>③ 16 个非能力节标题（如 `PDF文档/说明/使用说明.md` 的 `### 默认最大页数（类型: 整数型）`）需从逐能力模板里剔除或改归"配置项"章节 |
| **影响面** | 改文档 113 份（或生成器 + 重生成）；**不改行为、不改契约**；若说明书参与 `完整性摘要.json`，需重算摘要并升包版本——**这是本清单里唯一可能引发 111 个包版本级联的条目**，实施前必须确认摘要口径 |
| **是否共享文件** | 生成器（若存在）与 `漂移检测.py` = 强共享（与重要-8、重要-10 同文件族）→ 串行 |
| **验证命令** | 复核计数：`grep -rc '错误码：无' 支持库 模块库 技能库 --include='使用说明.md' | awk -F: '{s+=$2} END{print s}'` → 改后期望 0<br>版本一致性脚本：逐包比对 `使用说明.md` 首行版本 vs `包声明.json.版本` → 期望 0 不一致<br>覆盖率不回归：`find 支持库 模块库 技能库 -name 使用说明.md | wc -l` 与包数对齐 |

---

### 重要-10｜契约一致性校验器（`声明一致`/`校验声明一致`）零生产调用点 —— **这是全部漂移的根因**

| 列 | 内容 |
|---|---|
| **二次证真** | `grep -rn "校验声明一致\|声明一致" --include='*.py' . | grep -v 工程缓存 | grep -v 测试中心` 实测命中 **5 处，全部非生产**：<br>`公共契约/能力契约/契约.py:18`（注释）、`:45 def 声明一致`、`:113 def 校验声明一致`、`:122 实现.声明一致(声明)`（`校验声明一致` 内部自调）、`运行核心/能力调用/唯一能力调用.py:342`（注释里提到"参数名集合一致"，**不是调用**）<br>→ **生产调用点 = 0，完全复现**。函数签名：`能力实现.声明一致(声明) -> bool`（`:45`）、`能力注册表.校验声明一致(包id, 声明列表) -> list[str]`（`:113`） |
| **判定** | **证真（零调用点，逐处核对）** |
| **修复落点（接线方案）** | **接线分三步，先"只报不拦"**：<br>**第 1 步（运行期只报，不改行为）**：`运行核心/加载器/包安装/支持库安装.py` → `安装支持库(声明, 注册表)`（实测该函数在 `:62` 左右调用 `注册函数(注册表)` 后 `return 注册表.能力id列表`）里，注册完成后追加：<br>`from 公共契约.能力契约.契约 import 能力注册表`（若已可用）→ 用 `声明.能力` 构造 `能力声明` 列表，调用 `注册表.校验声明一致(声明.包id, 声明列表)`，结果写运行日志（**不 raise**）。<br>**第 2 步（编译期门禁接线，真正的落点）**：`开发工具/契约编译/漂移检测.py` 已具备完整的漂移框架（`:27 漂移结果`、`:45 检测声明无实现`、`:82 检测参数漂移`、`:122 检测返回漂移`），在其中**新增一条"注册口径漂移"子检测**，输入 = 契约 JSON + `__init__.py` 注册表（AST 或装配期注册表对象），输出 = `契约.py:113` 的问题列表；再在发布门禁 `开发工具/发布门禁/运行发布门禁.py` 调用它（先 `警告` 级，修完切 `阻断` 级）。<br>**第 3 步（防新增）**：`开发工具/契约编译/契约编译器.py` 编译完每个包时跑同一函数，产出 `漂移报告`（可落到临时目录，不进制品）<br>**为什么必须放"编译期门禁"而不是"运行期"**：运行期只报不拦等于没有门禁；而运行期一旦拦截会让现网 300+ 条口径差直接拒绝服务。 |
| **接线后预计报多少条（估算方法）** | **方法**：对全部 112 包 `__init__.py` 做 AST 取「注册 `返回`/`参数` 类型」+ JSON 取契约同名能力，逐字段比对（本清单已跑通该脚本，只是注册表解析覆盖率受限）。<br>**本轮的独立估算（比报告更全）**：<br>• 注册返回 `结果` ↔ 契约 `结果型`：**322 条**（报告 139；报告只做了 166 条配对，本次可判定 517 条）→ **报告数值是下界**<br>• 注册返回真不一致：**5 条**（4 条 `网络通信支持库.请求.*` + 1 条 `浏览器宿主.启动网页服务`，与报告逐条吻合）<br>• 注册返回历史短名：**1 条**（`文件系统支持库.文件操作.判断存在` 注册 `"布尔"` @ `支持库/后端/文件系统支持库/文件操作/__init__.py:49`，契约 `逻辑型`，与报告吻合）<br>• 注册参数类型 ≠ 契约类型：**7 条**（即重要-2）<br>**⇒ 悲观上界 ≈ 322 + 5 + 1 + 7 = 335 条**；若门禁只校验「参数名序 + 返回串」且把 `结果`/`结果型` 视为等价（报告 L-2 口径），则降为 **13 条**。<br>**建议接线前置**：先在 `契约.py:45 声明一致`/`:113 校验声明一致` 内**显式把 `结果` 归一为 `结果型`**（一个映射表），这样首次上线报出 13 条真问题而不是 335 条噪声。 |
| **影响面** | 需改 **运行核心加载器 + 开发工具契约编译/漂移检测 + 发布门禁** 三类文件；**属门禁行为变更** → 需重跑发布门禁（本清单禁止执行）、可能触发一批包被拦下（因此分"警告/阻断"两阶段）；不改能力定义/契约数据；不动摘要 |
| **是否共享文件** | **强共享（本清单最大冲突点）**：`运行核心/加载器/包安装/支持库安装.py`、`开发工具/契约编译/漂移检测.py`、`契约编译器.py`、`开发工具/发布门禁/运行发布门禁.py`、`公共契约/能力契约/契约.py` —— 与重要-8、重要-9、建议-3/4 以及子报告 01/04/12 相关主题全部撞车 → **必须串行，且本清单建议排为"第 0 号任务"** |
| **验证命令** | 接线后只读复算：`unset PYTHONPATH && python3.14 -c "import sys;sys.path.insert(0,'.');from 公共契约.能力契约.契约 import 能力注册表;r=能力注册表();print(r.校验声明一致('测试',[]))"` → 期望 `[]`（空列表=无问题，仅验证可调用）<br>端到端：`grep -rn '校验声明一致' --include='*.py' . | grep -v 工程缓存 | grep -v 测试中心 | grep -v '公共契约/能力契约/契约.py'` → 期望出现**至少 1 个生产调用点**（当前为 0） |

---

### 重要-11｜注册返回 vs 契约返回：旧口径 + 真不一致 + 历史短名

| 列 | 内容 |
|---|---|
| **二次证真** | 独立 AST/JSON 复算（可判定 517 条 / 契约 536 条）：<br>• **注册 `"结果"` ↔ 契约 `结果型` = 322 条**（报告 139 —— 报告仅配对 166 条，**本清单数值更高，报告是保守下界**）<br>• **真不一致 = 5 条，逐条吻合**：`网络通信支持库.请求.发送请求/上传文件/下载文件/构建查询串`（注册 `结果型`，契约 `{状态码,响应头,响应文本,错误信息}` 等结构串；证据 `支持库/后端/网络通信支持库/请求/__init__.py` 表内 `"结果型"` 第 4 元素 + `能力定义.json:90,181,322`）+ `浏览器宿主.启动网页服务`（注册 `"服务句柄与地址"` @ `支持库/前端/浏览器宿主/__init__.py:21`，契约 `服务句柄与地址；受管验证时为可序列化回收结果` @ `能力契约/参数契约.json:67`）<br>• **历史短名 = 1 条，吻合**：`文件系统支持库.文件操作.判断存在` 注册 `"布尔"` @ `支持库/后端/文件系统支持库/文件操作/__init__.py:49`，契约 `逻辑型` |
| **判定** | **证真（真不一致 5/5、短名 1/1 精确复现）；旧口径计数 322 ≫ 139 → 报告偏低估，修量级需按 322 准备** |
| **修复落点** | 权威源（契约）正确，**改从属源（注册）**：<br>① 旧口径 322 条：全仓 `__init__.py` 注册返回串 `"结果"` → `"结果型"`（机械替换，**建议脚本化**：AST 定位 4 元组表第 4 位 / `返回=` 关键字实参 / `返回表` 字典值，只替换字面量 `"结果"`，**不要**动 `结果型`、`结果[...]`、`结果型<...>`）。<br>② 真不一致 5 条（**这才是真问题**）：<br>&nbsp;&nbsp;- `网络通信支持库.请求.*` 4 条：契约写的是"结构串"（非法类型名），注册写 `结果型`（正式名）。**权威源错、从属源对** → 反常规：应改**契约**为 `结果型`，并把结构串移到 `值结构` 或 `说明` 字段（同时消掉【建议-3】的 4 条），`能力定义.json` 镜像同改。<br>&nbsp;&nbsp;- `浏览器宿主.启动网页服务`：同上处理（契约 `服务句柄与地址；受管验证时为可序列化回收结果` 拆成 `类型=结果型` + `说明=...`）。<br>③ 短名 1 条：`支持库/后端/文件系统支持库/文件操作/__init__.py:49` `"布尔"` → `"逻辑型"` |
| **影响面** | ①322 条：纯注册元数据机械替换，**无行为变更**（网关别名表已把 `结果` 当合法串？实测别名表不含 `结果`/`结果型`，但 `结果` 仅影响注册表内的返回声明消费方，不影响入参校验）→ 需重跑漂移检测基线；`返回` 字段若非 536 条全改会留下新口径差，**建议一次性全量替换**。<br>②5 条：改契约 + 定义 → 需重生成说明书、重算漂移基线、可能升包版本。<br>③1 条：注册元数据。**均不涉及重编译/摘要级联**（除非包版本升位） |
| **是否共享文件** | ①涉及**几乎全部包**的 `__init__.py`（112 个文件）→ 与任何"改注册元数据"的主题（重要-1、重要-2、重要-7A、建议-5）**全部撞车**，必须**合并成一次机械批处理**，不要分散到多条任务里重复改同一文件 |
| **验证命令** | 复算：`grep -rn ', "结果")' 支持库 模块库 技能库 --include='__init__.py' | wc -l` → 改后期望 0（当前 6 个文件命中该紧凑写法，另有多行写法需 AST 统计）<br>精确复算脚本（AST，见本清单 §一 口径）：比对 536 条 → 期望输出 `旧口径 0 / 真不一致 0 / 短名 0`<br>抽验：`grep -n '判断存在' 支持库/后端/文件系统支持库/文件操作/__init__.py` → 第 49 行末应为 `"逻辑型"` |

---

## 二、建议-1 ～ 建议-7

### 建议-1｜净虚列错误码 6 条

| 列 | 内容 |
|---|---|
| **二次证真** | 逐条落地复核：<br>• `浏览器自动化.导航页面` 契约含 `地址不允许`（`支持库/后端/浏览器自动化支持库/能力契约/参数契约.json:131`）；`grep -n '地址不允许\|导航失败\|操作失败' 支持库/后端/浏览器自动化支持库/实现/浏览器自动化.py` → **零命中** ✓<br>• `浏览器自动化.页面操作` 契约含 `操作失败`（同契约 `:293`）；实现同样零命中 ✓<br>• `资源管理.创建不可变快照` 的 `快照失败`：`grep -n 快照失败 支持库/后端/系统核心支持库/资源管理/实现/资源管理.py` → **零命中** ✓（与重要-4 同源）<br>• `模块库/直播逐字稿.全自动精校` 的 `复核失败`：全仓命中 `说明/使用说明.md:62`、`设计说明.md:99`、`能力定义.json:337`、`参数契约.json:350`、`能力数据/*.json`、`实现/复核编排.py:4`（**该行是模块 docstring 注释，不是产出**）→ 实现内无产出 ✓<br>• `模块库/网页浏览.打开并读取网页` 的 `导航失败`：命中 `能力定义.json:35`、`参数契约.json:37`、`使用说明.md:25`、`能力数据/*`，`模块库/网页浏览/实现/` 内**零命中** ✓ |
| **判定** | **证真（6/6；报告列为 5 行是因为适配层同名能力合并计数）** |
| **修复落点** | **改契约（删虚列）** 5-6 处：<br>① `支持库/后端/浏览器自动化支持库/能力契约/参数契约.json` 删 `地址不允许`/`导航失败`/`操作失败` —— **但先做一件事**：这三个码是"该包应该产生却漏了"还是"本来就不该有"？实测实现内无对应失败路径，属**漏实现**的可能性高（`导航页面` 失败时若抛异常会变 `内部错误`）→ 更优改法是**补实现码**而非删契约（与重要-6 同类）。<br>② `资源管理.创建不可变快照`：随【重要-4】改实现为 `结果` 时一并补 `快照失败`（一处改动消两个问题）。<br>③ `直播逐字稿.全自动精校` 的 `复核失败`：确认 `实现/复核编排.py` 是否有失败回传路径；有则补字面量，无则删契约项。<br>④ `模块库/网页浏览.打开并读取网页` 的 `导航失败`：同 ③（模块库层多经 `浏览器自动化` 失败上抛，需确认是"透传该码"还是"真的不产生"）。<br>**同步**：契约删改后，`能力定义.json` 错误码数组、`说明/使用说明.md` 错误码行、`能力数据/*.json` 三处镜像同步（否则制造新的三产物不一致） |
| **影响面** | 契约 + 定义 + 说明书 + 能力数据（每包 4 份）；**若选择"补实现"则改实现 + 需重跑该包验证场景**；不动注册、不重编译、无摘要级联 |
| **是否共享文件** | 各包独立；`能力数据/*.json` 是**生成物**（若由 `开发工具/能力搜索/能力搜索器.py` 生成，则落点在生成器）→ 需先确认生成关系 |
| **验证命令** | `grep -rn '地址不允许\|导航失败\|操作失败' 支持库/后端/浏览器自动化支持库/实现/ | wc -l` → 若选"补实现"，期望 ≥1；若选"删契约"，则 `grep -rn '地址不允许' 支持库/后端/浏览器自动化支持库/能力契约/参数契约.json | wc -l` 期望 0<br>反向一致性：全仓 `python3.14` 脚本比对「契约错误码 ⊆ 实现字面量 ∪ 网关公开码 ∪ 跨包字面量」→ 虚列计数期望 0 |

---

### 建议-2｜6 个聚合支持库在 `包声明.json` 里用另一套口径再声明全部能力

| 列 | 内容 |
|---|---|
| **二次证真** | 实测 6 个聚合包（`支持库/后端/{办公文档,大语言模型,数据操作,文件系统,系统核心,网络通信}支持库`）四件套**全部缺失**：`能力定义.json`=无、`能力契约/参数契约.json`=无、`权限契约/权限契约.json`=无、`验证场景引用.json`=无（6×4=24 项全 False，逐包打印确认）<br>`支持库/后端/文件系统支持库/包声明.json` 顶层键实测 = `['包id','名称','类型','版本','说明','入口','句柄超时秒','依赖','能力']` → **存在 `能力` 数组**（即第二套能力声明） |
| **判定** | **证真（6/6，四件套缺失 + `包声明.json.能力` 存在，全部复现）** |
| **修复落点** | **二选一，推荐 (A)**：<br>**(A) 去掉 `包声明.json` 里的 `能力` 数组**（保留 `包id/名称/类型/版本/说明/入口/依赖`）：落点 6 个 `支持库/后端/*支持库/包声明.json`。风险：需先 `grep -rn '"能力"' 运行核心 开发工具 平台控制面 --include='*.py'` 确认没有消费方依赖它（装配器 `运行核心/加载器/包安装/支持库安装.py:发现支持库` 实测只读 `包声明.包id/已废弃/能力`，`安装全部支持库` 里 `已注册.extend(能力.能力id for 能力 in 声明.能力)` **确实消费了 `声明.能力`** → 删字段会连带改动加载器，**需与加载器同批改**）。<br>**(B) 保留字段但改为"聚合引用"**：`能力` 换成 `子域包列表`（仅列 `支持库.后端.文件系统支持库.文件操作` 等子包 id），加载器改为"聚合包不注册能力，只提供分组"。这更符合"聚合包不承载能力"的设计（报告 L-8），但改动面更大。<br>**不建议**给聚合包补一套 `能力定义.json`（会与子域包重复，制造第 5 个口径） |
| **影响面** | 6 个 `包声明.json` + **加载器（若改 `已注册` 逻辑）**；`包声明.json` 参与 `完整性摘要.json` 与发布门禁 → **需重算摘要、可能全仓包版本联动**；不动契约 |
| **是否共享文件** | `运行核心/加载器/包安装/支持库安装.py` = **强共享**（与重要-10 接线同文件）→ 串行 |
| **验证命令** | `for p in 办公文档 大语言模型 数据操作 文件系统 系统核心 网络通信; do python3.14 -c "import json;print('$p', '能力' in json.load(open('支持库/后端/${p}支持库/包声明.json',encoding='utf-8')))"; done` → 改后（A）期望全 False<br>装配回归：`python3.14 -c "..."` 调 `安装全部支持库` 期望注册能力数仍 = 536（**只读验证，不得跑发布门禁**） |

---

### 建议-3｜返回字段残留非正式类型名（报告 10 条 → **实测 13 条**）

| 列 | 内容 |
|---|---|
| **二次证真** | 独立全量复算「`能力定义.json` 的 `返回.类型` ∉ `公共契约/基础类型/类型表.py:7-14 正式类型表`」= **13 条**（报告 10 条，**漏 3 条**）：<br>报告已列 10 条全部复现：`组件控件.创建窗口`=`结果型<窗口句柄型>`、`创建页面`=`结果型<页面句柄型>`、`创建组件`=`结果型<组件句柄型>`、`绑定组件事件`=`结果型<事件定义型>`（以上 `支持库/前端/组件控件/能力定义.json`）；`办公文档支持库.文档生成.合并文本文件`=`{输出路径, 合并文件数, 总字节数}`、`文档统计`=`{路径, 行数, 字数, 字符数, 字节数}`；`网络通信支持库.请求.发送请求`=`{状态码, 响应头, 响应文本, 错误信息}`、`上传文件`、`下载文件`、`构建查询串`<br>**新增 3 条报告未列**：`内部.演示文稿.解析`=`结果[通用文档]`、`内部.演示文稿.生成`=`结果[生成产物]`（`支持库/后端/文档转换支持库/python_pptx提供者/能力定义.json`）、`浏览器宿主.启动网页服务`=`服务句柄与地址；受管验证时为可序列化回收结果`（`支持库/前端/浏览器宿主/能力定义.json`）<br>另核实：`窗口句柄型/页面句柄型/组件句柄型/事件定义型` **不在** `类型表.py` 的 `正式类型表`（16 项实测） |
| **判定** | **证真，且比报告更全（13 vs 10）** |
| **修复落点** | 分三组，**权威源改 `能力定义.json` + `参数契约.json`（两侧同源，必须同改）**：<br>**组 1（4 条句柄型泛型写法）**：`支持库/前端/组件控件/能力定义.json` + `能力契约/参数契约.json` 的 `返回` 由 `结果型<窗口句柄型>` 改为 `结果型`，把"值是窗口句柄"写进 `值结构`/`说明` 字段（**不改实现**，实现本就返回 `结果`）。若项目确需 4 个句柄类型，另一条路是**把它们正式登记进 `公共契约/基础类型/类型表.py:7-14` + `类型目录.md`**（两条路二选一，**前者成本低、后者语义更丰富但要动公共契约，与建议-4 同一文件族**）。<br>**组 2（6 条结构串）**：`办公文档支持库.文档生成.*`(2) + `网络通信支持库.请求.*`(4) 的 `返回` 改为 `结果型`，结构串移入 `值结构` 字段（**与重要-11 ② 同一处修复，必须合并做**）。<br>**组 3（3 条新增）**：`内部.演示文稿.解析/生成` 的 `结果[通用文档]`/`结果[生成产物]` → `结果型`（结构写 `值结构`）；`浏览器宿主.启动网页服务` 同组 2 处理。 |
| **影响面** | 改 5 个包的 `能力定义.json` + `参数契约.json`（10 份文件）→ **触发说明书重生成、漂移检测基线重算、能力搜索数据（`能力数据/*.json`）重生成**；不动实现、不动注册；若包版本升位则动摘要 |
| **是否共享文件** | `公共契约/基础类型/类型表.py`、`类型目录.md` **仅当选择"登记新类型"路线才动** → 与建议-4 强共享；其余为包内文件 |
| **验证命令** | 复算脚本：遍历 `能力定义.json` 的 `返回.类型`，`assert 类型 in 正式类型表` → 改后期望违规 = 0<br>`grep -rn '结果型<\|{状态码\|{输出路径\|结果\[' 支持库 模块库 --include='能力定义.json' --include='参数契约.json' | wc -l` → 期望 0 |

---

### 建议-4｜`类型目录.md` 未收录已冻结的 `JSON值型`

| 列 | 内容 |
|---|---|
| **二次证真** | 实测 `公共契约/基础类型/类型表.py:7-14` 的 `正式类型表` **含** `JSON值型`（16 项：逻辑型/字节型/整数型/长整数型/单精度数型/双精度数型/文本型/字节集型/列表型/字典型/日期时间型/资源引用型/句柄型/结果型/空值型/**JSON值型**）<br>`公共契约/基础类型/类型目录.md` 实测：正文类型表（`## 类型表`）15 行 **无 `JSON值型`**；「## 命名冻结」节的正式名清单同样 15 个 **无 `JSON值型`** → **文档与代码口径不一致，复现**<br>使用面：`JSON值型` 至少 8 个参数在用（`数据集合/__init__.py:31,32,34,35,36,40,41,69,85` 实测 9 处） |
| **判定** | **证真** |
| **修复落点** | **只改文档（权威源补全）**：`公共契约/基础类型/类型目录.md` 两处 —— ① `## 类型表` 表格增一行 `| \`JSON值型\` | 任意合法 JSON 值（字典/列表/标量） | 由 `公共契约/基础类型/类型表.py` + `网关核心.py:290-296 _是JSON值` 校验 | 请求体、透传参数 |`；② 「## 命名冻结」清单补 `JSON值型`。<br>**追加建议**：`网关核心.py:290-296` 的类型判据表只覆盖 5 类（逻辑/文本/列表/字典/JSON值）+ 4 类数值，缺 `字节型/字节集型/日期时间型/资源引用型/句柄型/结果型/空值型` → 这些类型名一旦被注册进参数表就是"未知类型 → 静默不校验"，**与重要-1 同源风险**。建议在 `类型目录.md` 的「类型校验规则」节明确写清"哪些类型在网关边界有机器校验、哪些只做文档约束"（避免实现者对 `日期时间型` 产生错误安全感）。 |
| **影响面** | 单文件文档变更，**零代码影响、无级联**；但 `类型目录.md` 是公共契约文档，参与文档权威口径（子报告 10 文档权威主题）→ 需串行确认 |
| **是否共享文件** | **强共享**：`公共契约/基础类型/类型目录.md` 与 `类型表.py` 会被建议-3（若走"登记新类型"路线）、子报告 10、子报告 04（公共契约边界）同时触碰 → 串行 |
| **验证命令** | `grep -c 'JSON值型' 公共契约/基础类型/类型目录.md` → 改后 ≥ 2<br>一致性：`python3.14 -c "import sys;sys.path.insert(0,'.');from 公共契约.基础类型.类型表 import 正式类型表;doc=open('公共契约/基础类型/类型目录.md',encoding='utf-8').read();print([t for t in 正式类型表 if t not in doc])"` → 期望 `[]` |

---

### 建议-5｜注册参数项缺「必填」键 → 网关不校验必填，靠 `TypeError` 兜底

| 列 | 内容 |
|---|---|
| **二次证真** | 机制复现：`运行核心/统一网关/网关核心.py:276` `if 项.get("必填") is True and 名称 not in 参数:` → **无 `必填` 键 = 不校验**，与报告一致<br>计数：本轮 AST 全量扫描 112 包 `__init__.py`，**字面量参数字典共 1193 项，其中缺 `必填` 键 = 47 项 / 涉及 23 个文件**（报告 98 处 —— **差异来源明确**：报告把"元组式 `[{"名称": n, "类型": t} for n,t in 参数]` 在运行期生成、源码里不存在 `必填` 键的项也算进去了；本轮只统计字面量，故 47 是**下界**。分布 Top：`数据库连接支持库/psycopg数据库/__init__.py` 12、`SQLite数据库/__init__.py` 11、`模块库/{网页分析,文档生成,文档解析}` 各 2、`数据操作支持库/类型转换` 1、`前端/桌面宿主` 1、`前端/资源描述` 1）<br>实证文件：`支持库/后端/数据操作支持库/类型转换/__init__.py:21` 用元组式 `参数=[{"名称": 名称, "类型": 类型} for 名称, 类型 in 参数]`（实测该行）→ 运行期无 `必填`；`支持库/前端/基础组件描述/__init__.py` 同型 |
| **判定** | **证真（机制 1:1 复现；计数为下界 47 / 报告 98，口径差异已定位）** |
| **修复落点** | **改从属源（注册元数据）**，两类写法两种改法：<br>① **元组式**（如 `数据操作支持库/类型转换/__init__.py:21`、`前端/基础组件描述/__init__.py:24-30`）：把元组从 `(名称, 类型)` 扩为 `(名称, 类型, 必填)`，推导式改 `[{"名称": n, "类型": t, "必填": r} for n, t, r in 参数]`，逐条按契约的 `必填` 布尔值填（**权威源 = 契约 `参数[].必填`**）。<br>② **字面量字典式**：在缺 `必填` 的 47 个字典里补 `"必填": true/false`（优先 `psycopg数据库` 23 项 + `SQLite数据库` 11 项）。<br>③ **可选（更彻底）**：在 `公共契约/能力契约/契约.py` 的 `能力实现` 构造处（`:27` 类定义 / `能力声明` 解析）统一补全 `必填` 默认值 —— **不推荐**：会把"漏写"变成"默认 False"，反而更容易出错参不报错；正确姿势是**漏写就在门禁报错**（与重要-10 同一动作）。<br>④ 顺带修 `网关核心.py:557-562` 的 `TypeError` 兜底分支（注释已自认此现象）：兜底分支仍保留，但错误说明应统一为 `参数不合法：缺少必填参数 X`，不要把 Python 原文外泄（与重要-1 的"Python 异常原文外泄"同一治理点） |
| **影响面** | 改约 23 个 `__init__.py`（47-98 处）→ **需重跑该包验证场景**；**行为变更**：原本缺参落到 `TypeError` 且报错文案难懂的调用，现在会在网关边界被拦成 `参数不合法`（**这是正向变更，但会让原来"侥幸通过"的调用方暴露**，需先跑一遍全量调用样本）；不改契约、不动摘要 |
| **是否共享文件** | 与重要-11（同一批 `__init__.py`）、重要-1/2（注册元数据）**高度重叠** → 合并成一次"注册元数据批处理"任务；`网关核心.py:557-562` 与重要-1 共享 → 串行 |
| **验证命令** | 复算：AST 扫全仓 `__init__.py` 的参数字典，`len([d for d in dicts if '必填' not in d])` → 改后期望 0（含元组式需先展开）<br>运行期：网关调 `数据操作支持库.类型转换.文本转整数` 传 `{"进制":10}` → 期望 `参数不合法：...缺少必填参数 文本`（当前是 `missing 1 required positional argument: '文本'` 的 Python 原文，报告 §重要-8 已实测） |

---

### 建议-6｜权限契约混入非能力 id 配置键（5 包）

| 列 | 内容 |
|---|---|
| **二次证真** | 独立复算：全仓 `权限契约.json` 共 **106 份**（与报告一致）；顶层键 ∉ 本包能力 id 集合的 = **5 包，逐包吻合**：<br>`支持库/适配层/YAML提供者/权限契约/权限契约.json` → `['只读','写路径']`<br>`支持库/后端/代码解析支持库/…` → `['只读','写路径']`<br>`支持库/后端/个人蒸馏支持库/…` → `['只读','写路径']`<br>`支持库/后端/浏览器自动化支持库/…` → `['包id','默认权限','权限','禁止']`<br>`模块库/网页浏览/…` → `['默认权限','禁止']`<br>补充口径实测：106 份中**能力未登记权限 = 0**（报告结论成立） |
| **判定** | **证真（5/5 精确复现，混入键名与报告完全一致）** |
| **修复落点** | **改从属配置（权限契约）**，两种键迁移：<br>① `只读`/`写路径`/`禁止`/`默认权限`/`权限`/`包id` 这些**属配置语义**，应迁到 `配置契约/配置契约.json`（这 5 包均已有 `配置契约/` 目录，实测 `支持库/适配层/密码签名提供者` 等包存在该目录）—— 落点：5 个 `权限契约.json` 删这些键 + 5 个 `配置契约/配置契约.json` 增对应项。<br>② **前提**：先确认消费者是谁 —— `grep -rn '权限契约' 运行核心 平台控制面 开发工具 --include='*.py'`，若消费者是"按能力 id 遍历"（如网关鉴权），则非法键会被误当能力名（无害但脏）；若消费者做 `键 in 能力id` 判断则已免疫。<br>③ 若确认无消费者读这些键：可直接删（最省事，但会丢配置信息 → 不推荐，先迁后删） |
| **影响面** | 5 个 `权限契约.json`（+5 个 `配置契约.json`）；**若权限契约参与鉴权或摘要，需重算摘要**；不改代码、不改契约、不动注册 |
| **是否共享文件** | 各包独立；若 `运行核心` 鉴权模块读权限契约，则与子报告 07 安全面撞车 → 需核对 |
| **验证命令** | 复算：`python3.14 -c "import json,glob;print([p for p in glob.glob('**/权限契约/权限契约.json',recursive=True) if [k for k in json.load(open(p,encoding='utf-8')) if not k.startswith(('支持库','模块库','技能库','适配层','内部','浏览器'))]])"` → 改后期望 `[]`<br>`grep -c '只读\|写路径' 支持库/后端/代码解析支持库/权限契约/权限契约.json` → 期望 0 |

---

### 建议-7｜2 个验证场景文件存在但未被引用

| 列 | 内容 |
|---|---|
| **二次证真** | 实测：<br>• `支持库/后端/代码解析支持库/语法索引/验证场景.json` **存在** ✓<br>• `支持库/后端/代码解析支持库/语法索引/验证场景引用.json` **存在**，内容 `{"契约版本":"验证场景/v1","验证场景引用":[{"场景文件":"验证场景.json"}]}` ✓<br>• `支持库/后端/代码解析支持库/验证场景引用.json`（上层）内容 `{"契约版本":"验证场景/v1","验证场景引用":[{"场景文件":"验证场景.json"}]}` → **只引用自身目录的 `验证场景.json`，未列子目录 `语法索引/*`** ✓<br>→ 即"语法索引 下的验证场景 未被上层引用"成立；但**注意**：`语法索引/验证场景引用.json` 确实引用了 `语法索引/验证场景.json`（相对路径自洽），所以"2 个文件完全无人引用"的表述**需订正为"未被包含该两文件的验证体系引用（自引用除外）"** |
| **判定** | **基本证真（存在性与上层引用缺失均复现；"未被任何引用文件登记"需按自引用口径订正）** |
| **修复落点** | **补引用（权威源 = 上层引用文件）**：`支持库/后端/代码解析支持库/验证场景引用.json` 的 `验证场景引用` 数组增两项：`{"场景文件":"语法索引/验证场景.json"}` 与 `{"场景文件":"语法索引/验证场景引用.json"}`（**或**如子目录体系独立，则维持现状 + 在 `说明/设计说明.md` 写明"子域自带验证场景引用，由子域验证链驱动"）。<br>**先决动作**：确认引用文件的消费者如何解析 `场景文件` 路径（相对引用文件目录 or 相对包根）—— 落点决定用不用加 `语法索引/` 前缀。 |
| **影响面** | 1 个 JSON 文件；若 `验证场景引用.json` 参与统一验证器（`开发工具/统一验证/统一验证器.py`）或摘要 → **改后需重跑该验证器**（**只读验证可跑；若它会写盘/落制品则不在本清单授权范围**），可能引出子目录场景未通过的情况（这是好事：把隐藏的失败暴露出来） |
| **是否共享文件** | 各包独立；`开发工具/统一验证/统一验证器.py` 若需确认路径解析口径，与子报告 01/02 验证类主题有交叉 |
| **验证命令** | 复算：`python3.14 -c "import json,os;p='支持库/后端/代码解析支持库/验证场景引用.json';d=json.load(open(p,encoding='utf-8'));print([x for x in d['验证场景引用']])"` → 改后应含 `语法索引/验证场景.json`<br>反向完整：全仓脚本「所有 `验证场景.json` 是否都被某个 `验证场景引用.json` 覆盖」→ 改后期望未覆盖 = 0 |

---

## 三、共享文件与串行要求（**排期前必读**）

### 3.1 强共享文件（一次只能有一个主题改，其余排队）

| 共享文件 | 本清单命中条目 | 说明 |
|---|---|---|
| `运行核心/统一网关/网关核心.py` | 重要-1（别名表 `:285-288`、类型判据 `:290-296`、未命中分支 `:304`）、重要-6（公共错误码表 `:64-84`）、建议-5（`TypeError` 兜底 `:557-562`）、建议-4（`_是JSON值`） | **最大冲突点**。别名表/校验分支/错误码表三处一旦并行改必冲突；建议**一次任务只开一个**，顺序：别名表与类型判据（重要-1）→ 公共错误码（重要-6）→ 兜底文案（建议-5） |
| `运行核心/加载器/包安装/支持库安装.py` | 重要-10（接线校验器）、建议-2（`声明.能力` 消费逻辑） | 同一函数 `安装支持库`/`安装全部支持库`，**必须同批改** |
| `开发工具/契约编译/漂移检测.py` | 重要-8（示例完备性规则）、重要-9（说明书↔契约规则）、重要-10（注册口径子检测）、建议-3/4（类型合法性规则） | 四条建议都往同一文件加规则 → **合并成一次"漂移检测规则扩展"任务** |
| `开发工具/契约编译/契约编译器.py` + `能力定义编译器.py` | 重要-8/9/10、建议-3 | 编译期落点集中在这里 |
| `开发工具/发布门禁/运行发布门禁.py` | 重要-10（门禁接线） | 与总纲"禁止跑发布门禁"直接相关：**改它可以，跑它需授权** |
| `公共契约/能力契约/契约.py`（`:27` 类、`:45` 声明一致、`:113` 校验声明一致） | 重要-10、重要-11（`结果`→`结果型` 归一建议）、建议-5（必填补全） | 三处语义改动会互相影响，串行 |
| `公共契约/基础类型/类型表.py` + `类型目录.md` | 建议-4（必做）、建议-3（若走"登记窗口句柄型等"路线） | 一次改完两文件，不要分批 |
| **112 个包的 `__init__.py`（注册元数据批处理）** | 重要-1、重要-2、重要-7A、重要-11①③、建议-5 | **强烈建议合并为一次全仓机械批处理**（一个脚本 + 一份 diff），否则同一文件被 5 条任务反复改，冲突与漏改必然发生 |
| `能力数据/*.json`（能力搜索数据） | 建议-1、建议-3 | 疑似生成物 → 先定位生成器，改生成器不手改数据 |

### 3.2 串行建议顺序（按"投入产出 + 依赖"排序）

1. **第 0 步（先做，其它全依赖它）**：重要-10 接线 —— 先在 `契约.py` 内做 `结果`→`结果型` 归一，再把 `校验声明一致` 接进 `漂移检测.py` + `发布门禁`（**只报不拦**）。理由：它一次性把全部漂移变成"可见清单"，后面的修复才有验收依据（报告 §七.1 同判断）。
2. **第 1 步（一次机械批处理）**：重要-11①（322 条 `结果`→`结果型`）+ 建议-5（补 `必填`）+ 重要-1/2（注册类型名）+ 重要-7A（补注册参数）→ 同一批 `__init__.py` 改完，一次跑门禁验收。
3. **第 2 步（契约侧同步）**：重要-6（补错误码）+ 重要-7B/C（补契约参数）+ 重要-11②③ + 建议-1（清/补虚列）+ 建议-3（13 条非法返回名）—— 这四组都改"契约 + 定义 + 说明书"，**同一批文件**，一起改再统一重生成说明书。
4. **第 3 步（实现侧，行为变更，需单独回归）**：重要-3（位宽断言）、重要-4（资源管理 7 函数返回 `结果`）、重要-5（桌面宿主返回统一）—— **每一条都是破坏性变更**，各自独立排期 + 独立回归。
5. **第 4 步（文档与结构）**：重要-9（重生成 113 份说明书）、建议-2（聚合包 `包声明.json`）、建议-6（权限契约键迁移）、建议-7（验证场景引用）、建议-4（类型目录补 `JSON值型`）。
6. **第 5 步（体验类）**：重要-8（361→367 条调用示例补必填），建议挂到第 0 步的生成器里自动产出。

### 3.3 与其它子报告的潜在撞车（需总纲裁决）

| 本清单条目 | 可能撞车的子报告主题 |
|---|---|
| 重要-10、建议-2（加载器）、重要-1（网关） | 子报告 04 架构边界与依赖腿、06 运行态与网关真实 HTTP |
| 重要-4、重要-7A（资源管理/记忆/并发） | 子报告 05 资源与生命周期 |
| 建议-6（权限契约）、重要-1（边界校验） | 子报告 07 安全面 |
| 重要-9、建议-4（文档权威） | 子报告 10 文档权威与结构残留 |
| 重要-8、建议-1/3（能力搜索数据） | 子报告 01 统计口径、11 前端/适配层/MCP 覆盖 |
| 重要-3（类型转换行为变更） | 子报告 08 重复实现（类型转换在适配层可能有第二套） |

---

## 四、本轮未做/未确认（诚实声明）

| # | 项 | 原因 |
|---|---|---|
| 1 | 重要-5 运行期返回体 | 触发即弹 tkinter 窗口，越界（承报告 U-1） |
| 2 | 任何真实网关 HTTP 调用 | 只读复算已足够定真；避免对运行态干扰 |
| 3 | 发布门禁、契约编译、装配重跑、制品重编 | 冻结边界明确禁止 |
| 4 | `注册元数据` 解析的 100% 覆盖 | 本次 AST 可精确判定 517/536（报告 166），`返回` 字段有 19 条为变量/表达式无法静态判定 → 重要-11 的 322 应视为**下界**，精确值需接线校验器后在运行期取 |
| 5 | 「报告 8 条裸参数式调用示例」 | 本轮复算为 0 条，与报告冲突，**不冒充已核实**，留给报告方按其对口径复核 |
| 6 | 重要-7 中 `会话存储.父会话id` 归属 | 注册文件内命中 6 次同名，无法从字符串出现位置断定是否注册为该能力参数 → 归"疑为契约漏列"，需人工确认 |
| 7 | 重要-4「7 个函数体内零 `结果.`」 | 本轮只复算签名（7/7 吻合），未逐函数切片统计函数体内 `结果.` |
| 8 | 说明书是"生成物"还是"手写物" | 未查生成器与命令记录（承报告 U-6）；**这一条决定重要-9 的落点性质**，实施前必须先查 `开发工具/说明书生成/` |

---

*本清单由只读二次证真产生；所有行号、计数与判定均来自现场命令输出，未做推断性补全；与报告不一致处已逐条标注并给出差异原因。*
