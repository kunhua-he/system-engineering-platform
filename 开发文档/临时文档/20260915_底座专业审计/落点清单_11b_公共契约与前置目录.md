# 落点清单 11b · 公共契约与前置目录（第二轮：定真 + 落点）

> **冻结边界**：分支 `主干` / HEAD `1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc`。本轮**只读**仓库源码/配置/运行数据/制品/数据库，未重启服务、未跑发布门禁、未改任何跟踪文件。
> **唯一写入** = 本文件。
> **环境**：`unset PYTHONPATH` + `python3.14`；`git` = `/Library/Developer/CommandLineTools/usr/bin/git`。
> **收口证据**：本轮结束时 `git status --porcelain` → 仅 `?? 开发文档/临时文档/`（审计前既存）。
> **范围**：前端核心 / 后端核心 / 启动监督器 / 客户端 / 公共契约 / 技能库 / 示例项目 的**阻断与重要**级条目（含跨目录强关联项 A-07 / A-09 / A-12）。
> **输入**：`子报告_11_前端与适配层与MCP工具箱覆盖.md`（P-01~P-04、A-01~A-09、D-01~D-03、D-07）。**本轮对上述每一条重新独立证真，不采信原报告结论作为终判。**

---

## 0. 落点总表（固定列）

| 编号 | 二次证真（命令+输出摘要） | 判定 | 决策与修复落点（文件:行号 + 最小改法；含"删/接线/保留写理由"） | 影响面 | 是否共享文件 | 验证命令 |
|---|---|---|---|---|---|---|
| **L-01**（原 P-01） | `grep -rn '契约版本[^=]*=[^=]*"1\.0\.0"' --include='*.py' .`（排缓存/制品）→ `7`；`python3.14 -c "from 公共契约.版本规则.契约版本 import 契约版本"` → `2.0.0` | **重要（已证真）**：契约版本存在 **2.0.0（事实源）** 与 **1.0.0（8 个文件）** 双口径，"唯一事实源"前提不成立 | 见 **第 A 章**（8 处位置逐个给出）。**改（不删）**：全部改为从 `公共契约/版本规则/契约版本.py` 派生，或直接对齐 `2.0.0`。**保留"契约版本"这个字段**——它是强制参数（哲学第 21 条：不一致不失败，只回报差异），删字段会丢掉版本差异回报能力 | 运行核心装载/调用、项目适配层依赖锁、模块模板生成器、HTTP 路由契约；一旦出现按契约版本的强制门禁，两套基线会互相打架 | 是（`公共契约/版本规则/契约版本.py` 被 41 处生产代码引用，改默认值影响面扩散） | 第 A 章末段统一验证命令 |
| **L-02**（原 P-02） | `python3.14 -c "…print(len(正式类型表), sorted(正式类型表))"` → `16 ['JSON值型', …]`；`grep -c JSON值型 公共契约/基础类型/类型目录.md` → `0`；`grep -rl '"JSON值型"' 支持库 模块库` → 28 文件 / **7 个包**（并发控制支持库、数据操作支持库·数据交换、数据操作支持库·数据集合、数据结构支持库、网络通信支持库、网络通信支持库·请求、YAML提供者） | **重要（已证真）**：机器表 16 项 ⊃ 文档表 15 项 的**真分叉**，`JSON值型` 是**在用**类型却不在文档里 | 见 **第 B 章**。判据结论：**改文档，不改机器表** | 全部能力契约、能力定义、包声明、能力搜索/Agent 查询数据；实现者按文档写代码会拿不到合法类型 | 是（`类型目录.md` 与 `类型表.py` 被契约编译器、能力搜索、IDE 共消费） | 第 B 章末段统一验证命令 |
| **L-03**（原 P-03） | 逐常量计数（排 `公共契约/错误结构`、缓存、制品）→ `错误码_句柄无效/句柄已过期/资源释放失败/幂等键冲突/提供者崩溃/调用已取消/状态跳转非法/循环依赖/提供者冲突/资源未清理` **全部 = 0**；对照 `错误码_限流 = 6`、`错误码_返回结果不符合契约 = 4`；`grep -rn '"句柄失效"' --include='*.py'` → **47** 处裸字面量 | **重要（已证真）**：10 个"网关边界错误码"常量全仓 0 引用；真实句柄类错误码 `句柄失效` 在 `错误结构.py` 里**根本不存在** | `公共契约/错误结构/错误结构.py:58-76`。**改（不删）**：① 在句柄层补 `错误码_句柄失效 = "句柄失效"`（`错误结构.py:68-69` 之间，与 `错误码_句柄无效/已过期` 并列）；② 把 47 处裸 `"句柄失效"` 换成常量引用（优先改 3 个源头实现：`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:122,134,144`、`支持库/后端/大语言模型支持库/会话存储/实现/会话存储.py` 13 处、`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:367`）。**保留**其余 9 个零引用常量——它们是"网关边界冻结词表"，属防御性声明；按"空转即杀"应补接线（在网关拒绝分支改用常量）而不是删除 | 全平台错误码语义靠字面量对齐，无单一常量出处；改字面量词条会跨支持库+网关 | 是（`错误结构.py` 被 7 处生产代码引用，测试 0） | `grep -rn '"句柄失效"' --include='*.py' 支持库 \| wc -l` → 期望降到 0（或只剩常量定义处） |
| **L-04**（原 P-04） | `grep -rn "class 有界线程HTTP服务器" --include='*.py' .` → **4 处**：`公共契约/运行时/有界HTTP.py:10`、`运行核心/统一网关/本地网关.py:26`、`开发工具/项目编译/项目编译器.py:529`、`开发工具/HTML验证/验证服务.py:101`；`grep -rl "有界HTTP" 测试中心` → **0**；契约实现生产消费者 3 处：`支持库/前端/浏览器宿主/实现/浏览器宿主.py:13`、`MCP工具箱/能力网关.py:35`、`开发工具/轻代码前端编辑器/启动编辑器.py:23` | **重要（已证真）**：4 份实现；唯一契约实现 0 测试；门禁断言的是第 3 份复制品的**文本** | 见 **第 C 章**（含权威判定与"结果唯一即收口"逐份裁决） | 违反 `项目说明.md:47`「同一职责的内部实现也不得复制」；门禁断言文本而非行为 → 门禁可被"改字面量"骗过 | 是（`公共契约/运行时/有界HTTP.py` 被 3 个生产文件引用；`项目编译器.py` 被 `客户端/构建平台客户端.py` 私有导入） | 第 C 章末段统一验证命令 |
| **L-05**（原 A-03） | `grep -rn "渲染提供者" --include='*.py' .`（排缓存/制品）→ 命中仅 `前端核心/前端核心.py`（docstring+形参名）与 `前端核心/渲染提供者.py` 自身；`grep -rl 渲染提供者 测试中心` → `exit=1`（0 命中） | **重要（已证真）**：`前端核心/渲染提供者.py` 68 行**零外部调用者、零测试**；`前端核心/前端核心.py:144` 的 `渲染提供者缺失` 分支在生产恒成立 | **接线（首选，不删）**：`开发工具/发布门禁/运行发布门禁.py:1201` 现直连 `前端核心/浏览器交互`，改为经 `前端核心/渲染提供者.py:64 创建渲染提供者("浏览器HTML")` 取提供者，再在构造 `前端核心(渲染提供者=…)` 时注入（`前端核心/前端核心.py:85`）。**保留文件**——它是"渲染分派"这一职责的唯一具体实现，删掉等于把 `前端核心.py:144-146` 的校验分支永久变成死分支。**备选（若华哥判"前端渲染只走浏览器交互一条腿"）**：删 `渲染提供者.py` 并同步删 `前端核心/前端核心.py:69,85-89,139-146` 的渲染分派与其形参（一次性收敛，不留半个抽象） | "前端核心·渲染"这一职责在生产实际只有门禁那条直连路径可用；`宿主类型="浏览器HTML"` 只在死文件里定义 | 否（`渲染提供者.py` 无外部引用） | `python3.14 -c "from 前端核心.渲染提供者 import 创建渲染提供者; print(type(创建渲染提供者('浏览器HTML')).__name__)"` → `HTML渲染提供者`；接线后 `grep -rn "创建渲染提供者" --include='*.py' .` 应 ≥1 处生产命中 |
| **L-06**（原 A-04） | `grep -rn "from 启动监督器\|import 启动监督器" --include='*.py' .`（排缓存/制品）→ 仅 3 个**测试**文件；`grep -rn "汇入进程健康"` → 除定义处 `启动监督器/健康监督.py:207` 外只有 `测试中心/运行核心/测试_提供者生命周期.py:372,377` | **重要（已证真）**：`启动监督器/健康监督.py`（587 行 / 45 用例）**生产 0 接线**，唯一调用者是测试；AGENTS.md:175 声明的「健康检查」在生产从未运行 | **接线（不删）**：在 `运行核心/启动运行核心网关.py`（LaunchAgent `com.huashi.gateway-40007` 的 `ProgramArguments[1]`）装配 `启动监督器/健康监督.py:569 从配置创建健康监督(...)`，并在网关启动后挂 `汇入进程健康(...)`（对应"运行核心→启动监督器"第三十阶段 C3 声明）。**保留文件**——45 个用例已就绪，删掉等于丢掉唯一可用的健康检查实现，且会把 AGENTS.md:175 的职责声明变成空头**。改动属配置/接线，需华哥批 + 重启 40007 → 并入 `未完成事项.md:52` 重启批次 | AGENTS.md:175「健康检查」职责在生产不存在；周期/探针超时/失败阈值/证据保留策略在生产永不发生 | 是（`启动监督器/健康监督.py` 被 3 个测试文件引用，且自身 import `支持库.适配层.系统探针` + `项目适配层.配置适配.*`） | `python3.14 -c "import 运行核心.启动运行核心网关"` 后 `grep -n 健康监督 运行核心/启动运行核心网关.py` 应 ≥1 行；生产探针 `curl -s 127.0.0.1:40007/健康` 返回体含健康监督字段 |
| **L-07**（原 A-02） | `grep -rn "容量基线" --include='*.py' --include='*.json' .`（排缓存/制品/开发文档）→ **仅 2 行，都在 `启动监督器/容量基线.py` 自身**（`:59` class、`:130` 证据 id）；`grep -rl 容量基线 测试中心` → `exit=1`（0 命中） | **重要（已证真）**：166 行**全仓零调用者、测试中心零引用**的死代码；AGENTS.md:175 声明的「资源限制」职责实际不存在 | **二选一，推荐删**：① **删**（推荐）`启动监督器/容量基线.py` 整文件，并同步 `AGENTS.md:175` 的「资源限制」表述改为"（未落地，见未完成事项）"，理由 = 华哥「空转即杀」+ 它是**未登记死代码**（`项目说明.md` / `未完成事项.md` 0 命中）+ 其证据文件还无界追加（见 A-10）；② **接线**（若判"资源限制"必须有）在 `L-06` 同一次装配里把 `容量基线` 挂进健康监督周期，并**必须同时**补证据裁剪（对照 `健康监督.py:56 保留条数 2000` / `:355` 写后裁剪）。**禁止原样保留** | 资源限制职责空缺；`启动监督器/容量基线.py:127-136` 证据文件无界追加，违反 AGENTS.md:164「必须有界」 | 否（零调用者） | 删后：`grep -rn "容量基线" --include='*.py' .` → 0 生产命中；`python3.14 -m unittest 测试中心.公共契约.测试_包声明契约` 仍全绿（确认无隐式依赖） |
| **L-08**（原 A-05） | `find 启动监督器 -type f -name '*.py'` → 只有 `健康监督.py`、`容量基线.py`；`sed -n '175p' AGENTS.md` → `启动监督器/ 核心启动、资源限制、健康检查、版本切换与核心回退`；`sed -n '1p' 平台控制面/核心快照.py` → `"""核心快照与启动监督器：…` | **重要（已证真）**：声明四项职责，目录内实际只有 2 个 py，且这 2 个分别对应"无接线"与"死代码"；另两项实现落在别的目录 | **改文档（首选，最小改法）**：`AGENTS.md:175` 改为**如实归属**——`启动监督器/ 健康检查（健康监督.py）、资源限制（容量基线.py）`；`核心启动` 归 `运行核心/启动运行核心网关.py`，`版本切换与核心回退` 归 `平台控制面/核心快照.py`。**不建议搬目录**：`核心快照.py:1` docstring 已自称"启动监督器"，且 `平台控制面` 是发布门禁正式源码目录，搬运会牵动门禁目录表与来源指纹（`项目编译器.py:31-34`） | 审计/新人无法按目录定位职责；`开发文档/能力目录说明书.md` 同源表述也需同步 | 是（`AGENTS.md` 是权威入口文档，被门禁/契约编译器引用） | `sed -n '175p' AGENTS.md` 与 `find 启动监督器 -maxdepth 1 -type f` 逐项可对上 |
| **L-09**（原 A-01） | 静态证真：`后端核心/后端核心.py` 装配模板为模块级全局、不按 `系统根目录` 分键；`:116` 缓存命中分支会覆盖实例自带注册表 | **重要（已证真）**：同进程第二个不同根的 `后端核心` 会静默拿到别根注册表并返回 `成功=True`；当前 40007 单实例单根**暂不可达**，一旦同进程多根即升阻断 | `后端核心/后端核心.py:100-137`（装配模板缓存）。**改**：装配模板缓存的键从"全局单例"改为 `str(self.系统根目录.resolve())`（缓存结构改为 `dict[str, 模板]`），缓存命中分支（`:116`）改为**只读不改写**实例注册表（去掉覆盖动作，或改为"仅在实例注册表为空时才采用模板"）。**保留缓存**——全量扫描 536 个能力有实测开销，缓存本身是对的，错的是键 | 测试、`开发工具` 验证器、未来多实例/多制品同进程场景；能力表错误却报成功 | 是（`后端核心/后端核心.py` 被 21 个测试文件 + 40007 生产路径引用） | 复现脚本（`/tmp` 造两个假系统根，各放 1 个支持库）：`后端核心(根A).装配()` 与 `后端核心(根B).装配()` 的能力数应各自对应，且不等 |
| **L-10**（原 A-06） | `grep -n "from 开发工具" 客户端/构建平台客户端.py` → `:411 from 开发工具.项目编译.项目编译器 import _生成HTML, _生成启动器, _写入并编译Python`、`:458 …import 读取工作区字节指纹, _制品文件摘要`；被导入者均为下划线私有（`项目编译器.py:103,115,422,434`）；`运行核心/依赖防火墙.py:33-43` 层表**不含 `客户端`** → 归"其他"免方向检查 | **重要（已证真）**：跨包深入另一包**私有实现**，违反"跨包只经公开契约与包级中文入口"铁律，且依赖防火墙看不到 | **改（不删调用）**：`开发工具/项目编译/项目编译器.py` 为这 5 个符号补**公开别名**（去掉下划线或加公开转发函数，如 `生成HTML = _生成HTML`、`生成启动器 = _生成启动器`），`客户端/构建平台客户端.py:411,458` 改为导入公开名。**同时**（配套，见 L-12）把 `客户端` 纳入 `运行核心/依赖防火墙.py:33-43 层名称表` + `允许依赖表`，声明 `"客户端": {"公共契约", "开发工具"}` —— 否则改完公开名依赖审计仍看不见这条腿 | `开发工具/项目编译/项目编译器.py` 私有签名/模板一变，客户端制品静默残缺，**无门禁能识别** | 是（`项目编译器.py` 同时被 `客户端`、`正式包索引.py`、发布门禁消费；改公开名属向后兼容新增，不破坏现有下划线名） | `python3.14 客户端/构建平台客户端.py --帮助`（或文档化构建命令）退出码 0；`grep -n "_生成HTML\|_生成启动器" 客户端/构建平台客户端.py` → 0 命中 |
| **L-11**（原 A-08） | `grep -rn "系统底座_指纹校验"`（`*.py/*.json/*.plist/*.sh/*.md`，排缓存/制品）→ 仅 `后端核心/后端核心.py:226`（读取）与 `开发文档/项目说明.md:24`（声称）；`plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 的 `EnvironmentVariables` 只有 `MLXWhisper提供者_模型名/模型路径/PATH/PYTHONPATH/系统库网关凭证/系统底座_提供者环境根`；`grep -rn "设置包指纹校验器"` → 仅 `后端核心/后端核心.py:231`（调用）与 `运行核心/能力调用/唯一能力调用.py:83`（定义） | **重要（已证真）**：`启用包指纹校验()` 已实现但**无任何地方设置开关** → 生产恒 `return False`，包指纹门禁从未生效（默认关闭的完整性门禁） | **接线（不删实现）**：在 `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 的 `EnvironmentVariables` 段加 `"系统底座_指纹校验" => "1"`（唯一最小改法，代码零改动）。**保留实现**——`_唯一调用服务.设置包指纹校验器` 链路是完整的（`后端核心.py:231` → `唯一能力调用.py:83`），缺的只是开关。**注意**：`后端核心/后端核心.py:136` 在 `装配()` 时调用 `启用包指纹校验()`；`:232 初始化包指纹表()` 是首次全量装配后记基线 → 打开开关后**必须确认基线表的初始化时序在开关生效之前**，否则第一次调用即误判"指纹已变化"。**属配置改动 + 需重启，并入 `未完成事项.md:52` 重启批次**（与 `落点清单_06_运行态网关.md` F-01 同一条，不重复立项） | 防"源码/制品静默漂移"的唯一守门失效；40007 从未注入校验器，`校验包指纹`/`清空包指纹缓存` 在生产是不触发分支 | 是（plist 与 F-01/F-08 共用同一 `EnvironmentVariables` 段，**必须串行 + 一次性重启**） | `plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist \| grep 指纹校验` → 命中 1 行；重启后 `python3.14 -c "import os;print(os.environ.get('系统底座_指纹校验'))"` 在网关进程内为 `1`；改一个包源码不热接入 → 调用该包能力应报"指纹已变化" |
| **L-12**（原 A-09） | `sed -n '33,43p' 运行核心/依赖防火墙.py` → `层名称表` 含 公共契约/支持库/模块库/前端核心/后端核心/运行核心/项目适配层/开发工具/其他，**无 启动监督器/客户端/运维脚本**；`:106 return 层名称表.get(顶层,"其他")`；`:281-282 if 来源层=="其他": continue`；原报告实测 `审计文件数 = 582 \| 违规数 = 0` | **重要（已证真）**：三个正式目录在依赖审计中被归"其他"并**免越层检查**，等于依赖治理对它们失效（`启动监督器/健康监督.py:36-43` 真实跨层 import 却不报违规） | `运行核心/依赖防火墙.py:33-43`（层名称表）+ `:44-60`（允许依赖表）。**改（不删文件）**：① `层名称表` 加 `"启动监督器": "启动监督器"`、`"客户端": "客户端"`、`"运维脚本": "运维脚本"`；② `允许依赖表` 加 `"启动监督器": {"公共契约","支持库","项目适配层","运行核心"}`（如实现状：它真实 import `系统探针` 与 `配置适配`）、`"客户端": {"公共契约","开发工具"}`（配合 L-10）、`"运维脚本": {"公共契约","运行核心","开发工具"}`；③ `:281-282` 的"其他"免检**保留**（工具/门禁/验证文件确实需要宽依赖），但**三个正式层不再落到"其他"**。**先补层表 → 再跑审计 → 按真实违规收敛，不要先改 import** | 三个 AGENTS.md 正式层的跨层依赖完全不受管；L-06/L-10 的接线会被审计静默放行 | 是（`运行核心/依赖防火墙.py` 是发布门禁/统一验证器共用的唯一依赖审计器） | `python3.14 -c "import sys;sys.path.insert(0,'.');from 运行核心.依赖防火墙 import 审计依赖;r=审计依赖();print(r.审计文件数, len(r.违规列表))"` → 审计文件数 **>582**（三个目录被真实分到各层），违规列表**可能非 0**（即本次要收敛的真实债） |
| **L-13**（原 A-07） | `grep -n "运维脚本" AGENTS.md` → 0 命中；`sed -n '67,70p' 开发工具/发布门禁/运行发布门禁.py` 的 `正式源码目录名表` 13 项无 `运维脚本`；`开发工具/项目编译/项目编译器.py:31-34 关键源码目录` 无；`MCP工具箱/项目服务.py:306-310 全部目录表` 无 | **重要（已证真）**：`运维脚本/热接入.py` 是 40007 能力上线的唯一人工入口，却**不在 AGENTS.md 目录约定、不在发布门禁、不在来源指纹、不在代码地图** | **四处同改（缺一处仍游离）**：① `AGENTS.md:172-191` 目录约定补 `运维脚本/ 一次性运维脚本（人工执行，不被发布入口调用）`；② `开发工具/发布门禁/运行发布门禁.py:67-70 正式源码目录名表` 加 `"运维脚本"`；③ `开发工具/项目编译/项目编译器.py:31-34 关键源码目录` 加 `"运维脚本"`；④ `MCP工具箱/项目服务.py:306-310 全部目录表` 加。**保留脚本**——它是 `/网关/热接入` 的唯一运维入口，`开发文档/决策记录/0009` 的上线步骤依赖它。**另**：`运维脚本/热接入.py:119-123` 的 `subprocess.run` **无超时**（违反 AGENTS.md:164），同批补 `timeout=` | 该脚本的英文命名扫描、违规扫描、来源指纹、代码地图全部不覆盖 → 命中 AGENTS.md:168「独立脚本未被发布入口调用等同门禁不存在」 | 是（四处目录表是门禁/编译器/MCP 各自的硬编码副本，同一事实源散落 4 份） | `grep -c 运维脚本 AGENTS.md` → ≥1；`python3.14 开发工具/发布门禁/运行发布门禁.py` 的目录覆盖清单含 `运维脚本`（注意：本轮未跑门禁，落地后按发布流程跑） |
| **L-14**（原 D-07） | `sed -n '284,288p;77,81p' 开发工具/能力搜索/能力搜索器.py` → `发现全部(系统根/"支持库", 系统根/"模块库")`（**只两根**）+ `for 根目录 in (系统根/"支持库", 系统根/"模块库")`；实测 `python3.14 开发工具/能力搜索/能力搜索器.py --能力 技能包` → `未找到能力: 技能包` **退出码 1**；`发现全部(支持库,模块库,技能库)` → `技能库.后端.技能库` 命中 `True`，不传第三根 → `False` | **重要（已证真）**：技能库 4 个能力对 AGENTS.md:131 的正式验收入口「能力搜索器」**完全不可见**；根因唯一 = 搜索器只传两根 | `开发工具/能力搜索/能力搜索器.py:286`（`发现全部` 调用）与 `:79`（`加载聚合契约索引` 的根目录元组）。**改（1 行 × 2 处）**：两处都补 `系统根 / "技能库"` 第三根。**对照已传三根的 6 处**：`后端核心/后端核心.py:109,238,274`、`开发工具/统一验证/统一验证器.py:31`、`开发工具/公开调用完整性门禁.py:22`、`开发工具/组件规范/完整性摘要.py:159`、`开发工具/项目编译/正式包索引.py:88` —— 搜索器是**唯一漏传**的一处。**不删技能库**——它是第三根正式包根，`正式包索引.py:88` 已收，被 ≥6 条链路真实依赖 | 底座自己的"Agent 能力发现链"（搜索器）与"装配链"（后端核心）对技能库认知不一致；`项目说明.md:14`、`未完成事项.md:34` 的"已作为第三根进入索引"在搜索器这条链上不成立 | 是（根目录元组在 `能力搜索器.py` 内硬编码 2 处，是第 3 份"根清单副本"） | `python3.14 开发工具/能力搜索/能力搜索器.py --能力 技能包` 退出码 0；`--能力 技能库` / `--能力 校验技能包` / `--能力 运行技能包` / `--能力 生成索引` 4 个词全部命中 |
| **L-15**（原 D-01） | **本轮实跑**：`unset PYTHONPATH; PYTHONDONTWRITEBYTECODE=1 python3.14 -B 示例项目/适配层示例/运行入口/运行.py` → `[1]~[4] ✓`，`[5] 提供者网关启动失败: 缺失凭证: 环境变量 系统库网关凭证 未设置`，**退出码=1**；`git status --porcelain` → 仍只有 `?? 开发文档/临时文档/`（零写盘） | **阻断（已证真）**：AGENTS.md:132 列为"正式验收入口（唯一标准链路）"的入口在冻结 HEAD 上**裸跑必失败**，其声明关键输出从未产生 | `示例项目/适配层示例/运行入口/运行.py:53` 与 `:59`（两处 `本地网关服务器(...)` **未传 `配置`**）。**改调用方（不删示例）**：两处都补 `配置={"要求凭证": False, "禁止客户端身份": False}` —— 与 `开发工具/统一验证/统一验证器.py:331 网关配置 = {"请求超时秒":30,"要求凭证":False,"禁止客户端身份":False}` **同法**。**第二层缺陷同批修**：`运行核心/能力调用/HTTP连接器.py:208-215` 的请求头只有 `Content-Type` 与 `X-Request-ID`，**从不发 `Authorization`** → 补 `配置` 后第 [6] 步仍会 401（原报告实测：补 `系统库网关凭证` 环境变量后 `[6] 调用失败: 写入=权限不足 访问凭证缺失或无效`）。**兜底三选一（择一，需华哥裁决）**：① 改示例 + 连接器补 Authorization；② 把该入口从 AGENTS.md:132 的"正式验收入口"降级为"演示入口"（同时改 `开发工具/契约编译/消费者契约.py:168-171` 的声明）；③ 给示例加独立免凭证装配入口。**保留示例**——它是"模块调用面/支持库提供者调用面双端口"的唯一演示 | 底座正式验收链路里有一条**恒失败**入口；`消费者契约.py:168-171` 声明的退出码 0 + 关键输出 `适配层示例运行成功` / `[8] 项目适配验证: 通过` 从未出现，任何把它当门禁的流程都是假绿 | 是（`运行核心/统一网关/本地网关.py:126 要求凭证 = self.配置.get("要求凭证", True)` 默认 `True`，同一坑影响未传 `配置` 的所有调用方；`未完成事项.md:89` 已承认"加守卫时漏改调用方"） | 修复后：`unset PYTHONPATH; python3.14 示例项目/适配层示例/运行入口/运行.py; echo $?` → **0** 且输出含 `适配层示例运行成功` 与 `[8] 项目适配验证: 通过`（逐字对照 `消费者契约.py:168-171`） |
| **L-16**（原 D-02） | `git ls-files 示例项目/可双击演示/开发文件夹/前端/页面/` → 命中 `主页.json`、**`编译制品.json`**、`编辑记录.jsonl`；`编译制品.json` 内容 `修订号 24 / 标题 "新页面" / 项目类型 "网页项目" / 组件列表 [] / "契约版本":"1.0.0"`；`主页.json` 为 `修订号 0 / 标题 "底座演示：文本工具" / 项目类型 "软件项目" / 3 个组件` | **重要（已证真）**：编译产物入库，**且与同目录页面源互相矛盾**；编译器自己把它当非源码排除 | **删（首选）+ 加忽略规则**：① `git rm --cached 示例项目/可双击演示/开发文件夹/前端/页面/编译制品.json` 并删除工作树文件（它是编辑器/编译器的产出，`启动编辑器.py:344` 与 `项目编译器.py:690` 随时重生）；② `.gitignore` 加 `编译制品.json`（防止再入库）；③ **对照证据**：`项目编译器.py:262`、`:669` 已用 `路径.name not in {"编译制品.json","编辑记录.jsonl"}` 把它排除在源码之外 → 与"不入源码库"口径一致。**保留不影响编译**——`编译.py` 每次运行重新生成 | 违反 AGENTS.md:162「工程缓存可删除、可重新生成，不作为正式源代码」「制品不入源码库」；任何读它的人拿到错的页面事实（标题/项目类型/组件全错）。**另**：该文件内 `"契约版本":"1.0.0"` 与 L-01 同源，收敛 L-01 时一并处理（源在 `开发者工具/轻代码前端编辑器`） | 是（同一"产物不该入库"口径与 L-17、D-04 产物文件夹、D-06 默认输出指向仓库内属同一治理缺口） | `git ls-files 示例项目/可双击演示/开发文件夹/前端/页面/` → 只剩 `主页.json`（+可选 `编辑记录.jsonl` 见 L-17）；`python3.14 示例项目/可双击演示/开发文件夹/编译.py --输出 /tmp/x` 退出码 0 且 `/tmp/x` 内重新出现 `编译制品.json`（证明可重生） |
| **L-17**（原 D-03） | `git ls-files` 命中 `示例项目/可双击演示/开发文件夹/前端/页面/编辑记录.jsonl`（15276 字节 / 29 行，时间戳全部集中在 `2026-08-22T16:56`–`17:11`）；生成方 `开发工具/轻代码前端编辑器/启动编辑器.py:145,:357`；排除方 `项目编译器.py:262,:669` | **重要（已证真）**：**运行数据**（交互式编辑会话日志）入库，与 AGENTS.md:162 冲突；平台自己已认定它不是源码 | **删（首选）+ 加忽略规则**：① `git rm --cached …/编辑记录.jsonl` 并删工作树文件（29 条日志来自一次人工编辑会话，无复现价值）；② `.gitignore` 加 `编辑记录.jsonl`。**保留生成能力**——`启动编辑器.py:145` 下次编辑时重新生成，且 `:357` 的日志写入不受影响。**若华哥要留样例**：改为在 `示例项目/适配层示例/项目资源/` 同级建一个 `样例/` 子目录并显式声明"样例数据，非运行数据"，不与真实日志同路径 | 违反 AGENTS.md:162；工作树随每次编辑会话产生未跟踪脏文件，污染 `git status` | 是（与 L-16 同一目录、同一"产物/运行数据入库"缺口，应与 `.gitignore` 一并处理） | `git ls-files 示例项目/可双击演示/开发文件夹/前端/页面/` → 无 `编辑记录.jsonl`；`grep -c 编辑记录 .gitignore` → ≥1 |

---

## A 章 · 契约版本统一落点（L-01 展开：8 处位置逐个给改法）

**唯一事实源**：`公共契约/版本规则/契约版本.py:15` → `契约版本 = "2.0.0"`（文件自述"事实源只有这一处，不再分散到各包里各自演进"）
**本轮实测**：`python3.14 -c "from 公共契约.版本规则.契约版本 import 契约版本; print(契约版本)"` → `2.0.0`；`grep -rn '契约版本[^=]*=[^=]*"1\.0\.0"' --include='*.py' .`（排 `__pycache__`/`工程缓存`/`开发文档`）→ **计数 7**；加 `HTTP路由契约.json` = **8 处**。

### A.1 七处代码（逐个给最小改法）

| # | 文件:行号 | 现值 | 判定 | 最小改法 |
|---|---|---|---|---|
| 1 | `运行核心/加载器/版本系统/版本注册表.py:31` | `契约版本: str = "1.0.0"` | **默认参数漂移**（形参默认值 = 第二口径） | 改为 `契约版本: str \| None = None`，函数体内 `契约版本 = 契约版本 or 取契约版本()`。**不用"直接写 2.0.0"** —— 默认参数在函数定义时求值，写字面量会被下一次升级再次遗漏 |
| 2 | `运行核心/加载器/版本系统/版本注册表.py:94` | `def 注册版本(..., *, 契约版本: str = "1.0.0",` | 同上 | 同上（`None` + 体内 `or 取契约版本()`） |
| 3 | `运行核心/能力调用/HTTP连接器.py:44` | `默认超时秒: float = 10.0, 契约版本: str = "1.0.0") -> None:` | 同上 | 同上；本类已有 `from 公共契约…` 同层依赖（`公共契约/运行时/端口策略`），加 `取契约版本` 不引入新层 |
| 4 | `运行核心/加载器/提供者隔离/独立进程.py:237` | `契约版本: str = "1.0.0") -> 进程调用结果:` | 同上 | 同上 |
| 5 | `运行核心/加载器/提供者隔离/独立进程.py:469` | `契约版本: str = "1.0.0", 资源键: str \| None = None) -> 进程调用结果:` | 同上 | 同上 |
| 6 | `项目适配层/依赖锁定/依赖锁定.py:23` | `契约版本常量 = "1.0.0"` | **常量漂移（最危险的一处）** | 改为 `from 公共契约.版本规则.契约版本 import 契约版本 as 契约版本常量`。**安全依据**：`项目适配层` 已 5 处 import `公共契约`（`运行入口/项目入口.py:12,13`、`模块绑定/模块绑定.py:12`、`支持库绑定/支持库绑定.py:12,13`、`依赖锁定/依赖锁定.py:14`、`锁定校验.py:15`），且 `运行核心/依赖防火墙.py 允许依赖表["项目适配层"] = {"公共契约","运行核心"}` 已放行 → **不新增越界**。**注意连带**：该常量同时用于**写入**（`依赖锁定.py:132 "契约版本": 契约版本常量`）与**校验**（`锁定校验.py:112,114`）→ 改成 2.0.0 后，**现存 1.0.0 的依赖锁文件会被判"契约版本不符"**。这正是注释（`:18-22`）想要的效果（"能真实发现旧格式锁"），但**落地时必须同批重新生成依赖锁**，否则假红 |
| 7 | `开发工具/组件规范/模块模板生成器.py:17` | `契约版本 = "1.0.0"` | **常量漂移（模板产出侧）** | 本文件已 `from 公共契约.基础类型.结果类型 import 结果`（`:12`），层表内 `开发工具` 允许依赖 `公共契约` → 改为 `from 公共契约.版本规则.契约版本 import 契约版本`。**注意**：变量名就叫 `契约版本`，直接 import 同名即可，无需改名；但需确认本文件模板里该常量用于"生成的模块包声明"还是"聚合契约 S0 格式版本" —— **若用于"新包版本"（新包从 1.0.0 起），则这一处不该改**，应改名 `新包默认版本 = "1.0.0"` 以消除与契约版本的同名混淆（`契约版本.py:7-8` 明确"新包从 1.0.0 起"）。**这一处是 7 处里唯一需要先读模板正文再定的** |

### A.2 第八处（JSON）

| 文件:行号 | 现值 | 判定 | 最小改法 |
|---|---|---|---|
| `公共契约/HTTP路由契约.json:2` | `"契约版本": "1.0.0"` | **第三口径（描述性契约里的版本声明）** | **首选改 `2.0.0`**（消除双口径、保住"契约版本"唯一事实源，1 字节级改动）。**同文件 `:8`** 的 `"契约版本": "文本型（默认 1.0.0）"` 一并改为 `（默认 2.0.0）`。**备选删字段**：该文件**不按 S0 聚合格式解析**（子报告 P-06 已证），字段纯描述性 → 若华哥判"描述性契约不该声明契约版本"，则删 `:2` 整行 + `:8` 去掉"默认 …"括注。**判据**：只要还有任何消费方会读这个字段做版本比较，就必须改不能删 |

### A.3 统一验证命令（落地后一次性执行）

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 -c "from 公共契约.版本规则.契约版本 import 契约版本; print('事实源 =', 契约版本)"
echo "剩余硬编码 1.0.0（期望 0）："
grep -rn '契约版本[^=]*=[^=]*"1\.0\.0"' --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存 | grep -v 开发文档 | wc -l
grep -n '"契约版本"' 公共契约/HTTP路由契约.json
```

---

## B 章 · 类型表分叉：改文档还是改机器表（L-02 展开）

### B.1 两侧现状（本轮实测）

| 侧 | 文件 | 实测 | 性质 |
|---|---|---|---|
| 机器表（权威） | `公共契约/基础类型/类型表.py:8-11` | `正式类型表` **16 项**（含 `"JSON值型"`）：`['JSON值型','列表型','单精度数型','双精度数型','句柄型','字典型','字节型','字节集型','整数型','文本型','日期时间型','空值型','结果型','资源引用型','逻辑型','长整数型']` | 被契约编译器/能力搜索/IDE 消费的**唯一**正式表；docstring 明写"只允许消费这里的名称" |
| 文档表（人读） | `公共契约/基础类型/类型目录.md:9-23`（表格 15 行）+ `:34-38`（命名冻结清单 15 项） | `grep -c "JSON值型" 公共契约/基础类型/类型目录.md` → **0** | 人读侧，**落后于**机器表 |

**真实使用侧（判据的关键）**：`JSON值型` 在 **7 个包**里被真实使用（28 个文件，含 `包声明.json` / `能力定义.json` / `能力契约/参数契约.json` / `能力数据/*.json` / `__init__.py` / `说明/*.md`）：
`支持库/后端/并发控制支持库`、`支持库/后端/数据操作支持库/数据交换`、`支持库/后端/数据操作支持库/数据集合`、`支持库/后端/数据结构支持库`、`支持库/后端/网络通信支持库`、`支持库/后端/网络通信支持库/请求`、`支持库/适配层/YAML提供者`。

### B.2 判据（三条，逐条成立 → 改文档）

1. **方向性判据：机器表 ⊃ 文档表，不是文档表 ⊃ 机器表。** 机器表 16 项，文档表 15 项，差集 = `{JSON值型}`。若反过来（文档有、机器没有），说明文档写了未冻结的类型名 → 那时才该改机器表（或删文档条目）。
2. **消费方判据：`JSON值型` 有真实生产消费者，且落在契约三产物（包声明/能力定义/参数契约）+ 能力数据 + 说明文档里。** 改机器表（删 `JSON值型`）＝ 让 7 个包的**已冻结契约**在严格模式下立即变为非法（`开发工具/契约编译/聚合契约解析.py:59-63` 严格模式会报"不在正式类型表"）；改文档 ＝ 零生产影响，只补人读一致性。
3. **代价判据：改文档是 1 处纯文本改动，改机器表是 7 个包 × 4 类产物的连锁重编。** 且"删类型"必须同步 `YAML提供者/能力契约/参数契约.json:64` 等 28 个文件 + 重编制品。

**结论：改文档，不改机器表。** 落点 = `公共契约/基础类型/类型目录.md`。

### B.3 落点与最小改法

| 文件:行号 | 最小改法 |
|---|---|
| `公共契约/基础类型/类型目录.md:9-23`（表格 15 行） | 在合适位置补 1 行 `JSON值型`，语义写"结构化 JSON 值（对象/数组/数字/字符串/布尔/空）的任意组合；与 `任意型` 的区别：**有校验**（要求可 JSON 序列化），`任意型` 无校验"。定位依据：`类型表.py:10` 中它排在 `句柄型/结果型/空值型` 之后，可按机器表顺序对齐插入 |
| `公共契约/基础类型/类型目录.md:34-38`（命名冻结清单 15 项） | 同步补 `JSON值型` → 清单变 16 项，与机器表逐项一致 |
| `公共契约/基础类型/类型表.py:3-4`（docstring） | **不必改**（它本来就是对的）。但**建议**在 docstring 补一句"本表是唯一事实源；`类型目录.md` 是它的镜像，两侧新增/删除必须同批改" |
| （配套，可选但推荐） | 加一条**自动比对**：`公共契约/基础类型/__init__.py` 或测试中心新增 1 个断言 `set(正式类型表) == set(类型目录.md 表格里的名字)`；否则下次仍会分叉。**当前 `测试中心/公共契约/` 只有 5 个测试文件 / 290 行，无任何一处比对这两个文件** |

### B.4 统一验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
python3.14 -c "from 公共契约.基础类型.类型表 import 正式类型表; print('机器表', len(正式类型表))"
echo -n "文档表命中数（期望 ≥2：表格 1 + 冻结清单 1）: "; grep -c "JSON值型" 公共契约/基础类型/类型目录.md
# 分叉自动比对（落地后应为 True）
python3.14 -c "
import re,pathlib,sys; sys.path.insert(0,'.')
from 公共契约.基础类型.类型表 import 正式类型表
文本=pathlib.Path('公共契约/基础类型/类型目录.md').read_text(encoding='utf-8')
缺=[t for t in 正式类型表 if t not in 文本]
print('文档缺失的正式类型:', 缺 or '无')
"
```

---

## C 章 · 「有界线程HTTP服务器」4 份实现：权威判定与收口（L-04 展开）

### C.1 四份位置（本轮实测，逐份对比签名与行为）

| # | 文件:行号 | 并发参数名 | 满载行为 | 生产消费者 | 是否含附加逻辑 |
|---|---|---|---|---|---|
| **① 契约实现** | `公共契约/运行时/有界HTTP.py:10`（全文件 31 行） | `最大线程数: int = 32` | `spawn` **前** `acquire(blocking=False)`，失败即 `self.shutdown_request(请求)` → **直接断连** | `支持库/前端/浏览器宿主/实现/浏览器宿主.py:13`、`MCP工具箱/能力网关.py:35`、`开发工具/轻代码前端编辑器/启动编辑器.py:23`（**3 处真实生产**） | 无（纯并发预算） |
| **② 网关实现** | `运行核心/统一网关/本地网关.py:26` | `最大工作线程: int = 64` | `acquire(blocking=False)` 失败 → **同步返回结构化 429**；另有 `request_queue_size` 收紧、`活动工作线程数`/`拒绝请求数`/`_连接诊断` 计数与诊断 | 40007 生产网关本体 | **有**（结构化 429 + 拒绝计数 + 连接诊断） |
| **③ 编译器内联副本** | `开发工具/项目编译/项目编译器.py:529` | `并发上限`（生成代码里的变量） | `acquire(blocking=False)` 失败 → `shutdown_request` → **直接断连** | 无（**文本**内联进 `_生成启动器()` 产出的启动器源码，随编译制品落地） | 无 |
| **④ 验证服务内联副本** | `开发工具/HTML验证/验证服务.py:101` | **硬编码 `BoundedSemaphore(32)`** | `acquire(blocking=False)` 失败 → `shutdown_request` → **直接断连** | 无（**文本**内联进 HTML 验证服务的制品启动器） | 无 |

**测试侧实测**：`grep -rl "有界HTTP" 测试中心 --include='*.py'` → **0**（唯一契约实现零直接测试）。
**门禁侧实测**：`测试中心/第一批维修/测试_统一编译器.py:41-42` → `assertIn("有界线程HTTP服务器", 启动器)` / `assertIn("BoundedSemaphore(并发上限)", 启动器)` —— **断言的是第 ③ 份复制品的源码文本**（`_生成启动器("示例项目.可双击演示")` 的返回字符串）。

### C.2 权威判定

**权威 = ①`公共契约/运行时/有界HTTP.py`**（唯一被声明为契约、且被 3 个真实生产文件消费的实现）。

**但 ② 不是纯复制品，是"契约 + 网关专属增量"** —— 判定依据（"结果唯一即收口"）：
- ①③④ 的**满载结果**完全相同 = `shutdown_request(请求)` 直接断连；三者语义可互换 → **可收口**。
- ② 的满载结果是**结构化 429 JSON**（`本地网关.py:344` 同一套 `_拒绝` 口径），与 ① 的"断连"**结果不同** → **不满足"结果唯一"，不能直接合并**；强行合并会破坏网关的"不足即拒绝"对外契约。

### C.3 收口裁决（按"结果唯一即收口"逐份给处置）

| 份 | 处置 | 理由与最小改法 |
|---|---|---|
| ① | **保留，升为唯一契约实现** | 已是 3 处生产的来源；**必须补测试**（当前 0） |
| ② | **保留，但显式声明为 ① 的网关特化子类** | 最小改法：`运行核心/统一网关/本地网关.py:26` 改为 `from 公共契约.运行时.有界HTTP import 有界线程HTTP服务器 as _契约有界服务器`，再 `class 有界线程HTTP服务器(_契约有界服务器):` **只覆写 `process_request` 加 429 分支**，`__init__` 复用 `super().__init__(地址, 处理器类, 最大线程数=…)` 并保留 `request_queue_size` 收紧与诊断计数。**收益**：并发预算语义（信号量位置、daemon_threads/block_on_close）不再是两份独立实现；**注意**：`daemon_threads`/`block_on_close` ① 已定义，② 删掉重复定义。**保留 ② 的 429 行为不变**（对外契约不能动） |
| ③ | **必须收口（注入文本，不手抄）** | 这是本轮**最该改**的一份：`开发工具/项目编译/项目编译器.py:529-543` 是**手抄的类定义文本**，会随 ① 演进静默过期（这正是门禁只断言文本、断言不了行为的根因）。最小改法：在 `项目编译器.py` 的生成模板里把这段文本替换为**从 `公共契约/运行时/有界HTTP.py` 读取源码并注入**（构建期读盘 → 写进启动器模板），保证制品里的实现与契约实现**同源**。改完后 `测试_统一编译器.py:41-42` 的 `assertIn` 应升级为**行为断言**（起服务 → 打满并发 → 断言超限请求被拒），否则门禁仍在断言文本 |
| ④ | **必须收口（同上），且删掉硬编码 32** | `开发工具/HTML验证/验证服务.py:101-113` 同③；**额外**把 `BoundedSemaphore(32)` 改为可配（`验证器 --并发 32` 已经是正式命令参数，见 `AGENTS.md:130`）——硬编码 32 会让 `--并发` 参数在制品侧失效 |

**"删"的唯一场景**：若华哥判"制品启动器不需要独立并发预算"，则删 ③④ 两份内联类（只保留 ①②）—— 但**当前 ③④ 的功能（保护制品侧 HTTP 服务）是真实需求**，故本清单**不推荐删**，推荐"同源注入"。

### C.4 统一验证命令

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
echo "四份实现现状（期望收口后只剩 ①②，即 2 处 class）:"
grep -rn "class 有界线程HTTP服务器" --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存
echo "测试中心直接引用（期望 ≥1）:"
grep -rl "有界HTTP" 测试中心 --include='*.py' | wc -l
echo "① 可导入且满载拒绝可用:"
python3.14 -c "
import sys,threading; sys.path.insert(0,'.')
from 公共契约.运行时.有界HTTP import 有界线程HTTP服务器 as S
from http.server import BaseHTTPRequestHandler
s=S(('127.0.0.1',0),BaseHTTPRequestHandler,最大线程数=1)
print('实例化 OK, 信号量=',type(s._线程信号量).__name__); s.server_close()
"
```

---

## D 章 · 指纹门禁生产恒假：证真方式与落点（L-11 展开）

### D.1 三条独立证据（任一为真即坐实"生产恒假"，本轮三条全中）

1. **读开关侧**：`后端核心/后端核心.py:224-232 启用包指纹校验()` —— 读 `os.environ.get("系统底座_指纹校验","")`，`开关 not in ("1","true","yes","是")` 即 `return False`，**不注入校验器**。
2. **写入侧穷尽**：全仓 `grep -rn "系统底座_指纹校验"`（`*.py/*.json/*.plist/*.sh/*.md`，排 `__pycache__`/`工程缓存`）→ **只有 2 处**：`后端核心/后端核心.py:226`（读）与 `开发文档/项目说明.md:24`（文档声称）。**无任何设置点。**
3. **生产环境实测**：`plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables` = `MLXWhisper提供者_模型名 / MLXWhisper提供者_模型路径 / PATH / PYTHONPATH / 系统库网关凭证 / 系统底座_提供者环境根` —— **无 `系统底座_指纹校验`**。
4. **链路完整性反证**（防"是不是别处注入了"）：`grep -rn "设置包指纹校验器"` → 只有 `后端核心/后端核心.py:231`（调用点，在被 `return False` 挡住的分支后面）与 `运行核心/能力调用/唯一能力调用.py:83`（定义）。→ **没有第二条注入路径**。

### D.2 复现命令（可直接贴跑）

```bash
cd ~/Documents/Agent/PHP/系统工程平台 && unset PYTHONPATH
# ① 全仓唯一出现处
grep -rn "系统底座_指纹校验" --include='*.py' --include='*.json' --include='*.plist' --include='*.sh' --include='*.md' . \
  | grep -v __pycache__ | grep -v 工程缓存
# ② 生产是否注入（期望 0）
plutil -p ~/Library/LaunchAgents/com.huashi.gateway-40007.plist | grep -c 指纹校验
# ③ 开关未设时函数行为（期望 False）
python3.14 -c "
import sys; sys.path.insert(0,'.')
from 后端核心.后端核心 import 后端核心
b=后端核心(); print('启用包指纹校验() →', b.启用包指纹校验())
"
# ④ 唯一注入路径
grep -rn "设置包指纹校验器" --include='*.py' . | grep -v __pycache__ | grep -v 工程缓存
```

### D.3 落点（唯一最小改法）

| 文件:行号 | 最小改法 | 备注 |
|---|---|---|
| `~/Library/LaunchAgents/com.huashi.gateway-40007.plist` → `EnvironmentVariables` 段 | 加一键值：`"系统底座_指纹校验" => "1"` | **代码零改动**。`后端核心/后端核心.py:224-232` 与 `:206-232` 的校验器实现已完整 |
| `后端核心/后端核心.py:136` 与 `:232 初始化包指纹表()` | **落地前必须确认时序**：`:136` 在 `装配()` 里调 `启用包指纹校验()`，而基线指纹由 `:232 初始化包指纹表()` 在**首次全量装配成功后**记录 → 若开关在基线之前生效，第一次调用即可能误判"指纹已变化"而假红 | 建议顺序：装配 → `初始化包指纹表()` → `启用包指纹校验()`；若现有时序相反，需先改正这两行的调用顺序（最小改法） |
| `开发文档/未完成事项.md:52`（重启纪律） | 本项**并入已有重启批次**，与 `落点清单_06_运行态网关.md` 的 **F-01（`系统底座_指纹校验`）** 是**同一条**（同一 plist、同一段 `EnvironmentVariables`）→ **不重复立项，不重复重启**；F-08（`系统库审计目录`）也在同段，一致处理 | 结论引自 `落点清单_06_运行态网关.md:181` 的"串行 + 一次性重启" |
| 判据（为何"接线"而非"删"） | `后端核心/后端核心.py:206-222 校验包指纹` + `:420-421 清空包指纹缓存` + `唯一能力调用.py:83 设置包指纹校验器` 构成完整闭环（注册基线 → 调用前比对 → 热接入后清缓存） | **不是恒真门禁**（恒真门禁的定义是"断言永真"）；它是**默认关闭的完整性门禁**，所以处置是"打开"，不是"删" |

---

## E 章 · 本轮判定口径与未确认项

### E.1 判定口径（写死，供第三轮复用）

- **"零调用零测试"** = 全仓（排 `__pycache__`/`工程缓存`/`开发文档`）grep 命中只落在**自身文件 + 测试目录**；`测试中心` grep 命中数为 0 → 判定"死代码"。
- **"生产零接线"** = 全仓 grep 命中只落在**自身定义处 + 测试目录**，`运行核心/启动运行核心网关.py`（40007 的 `ProgramArguments[1]`）不导入 → 判定"有实现无接线"。
- **"恒假门禁"** = ①读取点存在；②全仓搜索无任何设置/注入点；③生产环境（plist / 环境变量）实测无设置；④注入路径唯一且被前置 `return` 挡住 → 四条全中才判"恒假"。
- **"结果唯一即收口"** = 两份实现的**对外可观测结果**（返回值 / 状态码 / 连接行为）逐项相同 → 收口为一份；**任一结果不同即不是纯复制品**，处置改为"继承/同源注入"，不动对外契约。
- **"改文档 vs 改机器表"** 判据见 B.2 三条（方向性 / 消费方 / 代价）。

### E.2 本轮未确认项（诚实标注，不编造）

1. `开发工具/组件规范/模块模板生成器.py:17 契约版本 = "1.0.0"` —— **未读模板正文**，无法判定它用于"契约版本"还是"新包默认版本"。**7 处里唯一需先读模板正文才能定的一处**（见 A.1 表第 7 行）。
2. `项目适配层/依赖锁定/依赖锁定.py:23` 改为 `2.0.0` 后，**现存依赖锁文件（`工程缓存/` 下）是否全部被判"契约版本不符"未穷举**。落地前需先 `grep -rn '"契约版本"' 工程缓存 | grep -v 2.0.0` 盘点，再决定"重新生成锁"还是"加一次性兼容窗口"。
3. `L-15` 第 ② 层缺陷（`运行核心/能力调用/HTTP连接器.py:208-215` 不发 `Authorization`）**本轮未实跑"补凭证后"的第 [6] 步**（原报告实测为 `[6] 调用失败: 写入=权限不足…`）。**未跑原因**：补 `系统库网关凭证=local-verify-123` 后示例会进入写盘路径，触碰"不改运行数据"边界。判定依据 = `本地网关.py:126 要求凭证` 默认 `True` + `本地网关.py:344 _拒绝(401,…)` + `HTTP连接器.py:208-215` 头列表逐字复核。
4. `L-11` 落地后**指纹校验的初始化开销未经实测**（`初始化包指纹表()` 会 `rglob` 扫三个根算 `_目录轻量指纹`）。打开开关对 40007 启动耗时的影响**需在重启批次内观测**，不能凭"轻量"二字当作无成本。
5. **是否有仓库外的 shell 包装 / CI 在跑 `示例项目/适配层示例/运行入口/运行.py` 之前 export 免凭证配置** —— 未穷尽（本轮只按 `AGENTS.md:132` 原样裸跑）。若有，则 L-15 不成立为"恒失败"，只成立为"入口不自洽"。

### E.3 收口证据

```
$ git status --porcelain
?? 开发文档/临时文档/

$ git rev-parse HEAD
1c69ea5d4369f268cf01c6f9ebba2bc1009b01dc

$ shasum -a 256 示例项目/适配层示例/项目资源/*.txt   # 运行 L-15 前后一致
资源文件哈希一致（未写盘）
```
