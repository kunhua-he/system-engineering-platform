# 反假绿审计与维修方案（第一轮调研）

> 当前状态：第一轮主落点已确认，54 个漏测文件尚未逐文件完成归类；本文件不是“全部落点已冻结”的最终方案。未完成全落点前，不执行代码维修。

## 1. 审计范围

- 项目：系统工程平台
- 当前提交：`f47b8472f7fa22cad64a486ab8de0867781335f7`
- 审计方式：只读读取源码、真实执行定向测试、内存构造缓存探针、核对 Git 状态与远程提交
- 审计期间：未修改代码、测试、配置或远程仓库

## 2. 已确认的越过落点

### 2.1 测试文件没有全部进入全量入口

证据：

- `测试中心/运行测试.py:71-85` 使用固定阶段匹配表；
- `测试中心/运行测试.py:1319-1334` 只累加阶段内测试数；
- 实际扫描得到 168 个测试文件，阶段集合只覆盖 114 个；
- 54 个文件、634 个测试用例没有进入当前标准/慢速阶段集合。

重点漏测文件：

- `测试中心/开发工具/测试_项目编译器.py`
- `测试中心/MCP工具箱/` 下 17 个测试文件
- `测试中心/开发工具/` 下多个门禁/编译/模板测试文件
- `测试中心/运行核心/` 下多个环境、生命周期、镜像、注册表测试文件
- `测试中心/启动监督器/` 下健康检查测试文件

定向真实结果：

- `测试中心/开发工具/测试_项目编译器.py`：13 个测试，8 failures，1 error；
- MCP 验证门禁：28/28；
- 缓存读写：33/33；
- 强制校验：11/11；
- 唯一注册表调用器：13/13；
- 组件合规收敛：13/13。

### 2.2 发布门禁内部没有调用完整范围

证据：

- `开发工具/发布门禁/运行发布门禁.py:450-477`；
- `开发工具/发布门禁/运行发布门禁.py:467` 调用：

```text
python3.14 测试中心/运行测试.py --并行数 1
```

没有使用 `--范围 全部`，因此发布门禁只验证当前常规阶段，未覆盖上述 634 个测试用例。

### 2.3 “逐包13/13”门禁允许失败包通过

证据：

- `开发工具/发布门禁/运行发布门禁.py:218-250`；
- `开发工具/发布门禁/运行发布门禁.py:351-360`。

真实扫描结果：

- 候选包：91 个；
- 未达到 13/13：31 个；
- `执行逐包权威合规()` 仍返回整体通过：`True`；
- 原因：只有 `模块/基础模块/功能模块` 失败会加入阻断列表，支持库失败只披露。

这与项目硬规则 `AGENTS.md:131-132` 的“债务通过必须失败”冲突。

### 2.4 验证缓存可以被伪造后接受

证据：

- `测试中心/运行测试.py:278-295`；
- `测试中心/运行测试.py:353-390`。

在不写入缓存文件的内存探针中：

```text
文件级伪造缓存 accepted = True
阶段伪造缓存 accepted = True
```

当前缓存校验了摘要、环境、依赖和时间，但没有证明缓存确实由一次真实测试运行产生。

## 3. 已确认的真实失败落点

### 3.1 项目编译器测试与当前架构漂移

文件：`测试中心/开发工具/测试_项目编译器.py`

- L26-L49 仍断言旧的 `支持库.适配层.*提供者` 闭包；
- 当前真实闭包返回后端支持库 owner；
- `模块库/自修复工具/包声明.json` 仍有 `支持库.适配层.Git提供者`，但能力 owner 已是 `支持库.后端.版本控制支持库.Git操作`；
- L51-L74 依赖已删除的 `模块库/_模板`，模板 fail-closed 测试夹具需要重建。

### 3.2 31 个支持库/提供者失败分为两类

纯契约缺失：

- XML解析、文档生成、模板渲染；
- 上下文压缩、会话存储、嵌入缓存、模型连接器、流式推理、重排服务；
- 数值计算、正则表达式、路径安全、额度控制、记忆支持；
- 数据集合、文件操作、数据结构、协议白名单、工具执行、幂等消息、权限审批、检查点恢复、环境变量；
- 编码转换、网络请求。

需要真实代码/真实返回验证：

- 数据集合：实现收到未知参数；
- 进程管理：资源释放、`open` 未关闭、吞异常、释放句柄失败语义；
- 并发控制：释放句柄缺失败语义；
- 幂等消息：缺必填参数没有失败返回；
- Pillow：契约传 `字节`，实现使用 `字节b64`；
- Tesseract：正式包入口缺 `注册能力`，真实入口不可达；
- 文件操作：公共入口路径仍不符合当前父包架构。

## 4. 第一轮维修方向

### 批次一：测试覆盖收口

修改目标：`测试中心/运行测试.py`、发布门禁调用链。

- 建立唯一测试覆盖清单；
- 168 个测试文件必须全部归属且不能重复；
- `--范围 全部` 必须覆盖全部清单；
- 发布门禁必须核验完整范围；
- 输出发现数、覆盖数、执行数、复用数、跳过数和总数；
- 覆盖缺失或重复时直接失败。

### 批次二：项目编译器测试和声明对齐

修改目标：

- `测试中心/开发工具/测试_项目编译器.py`
- `模块库/自修复工具/包声明.json`

原则：按当前真实 owner 和当前模板设计修复，不通过放宽断言制造绿。

### 批次三：31 个失败包逐包收口

权威验证器保持不降级。

- 先补真实契约、配置、权限、版本、错误码和调用示例；
- 再修真实返回值、资源释放、入口注册和异常语义；
- 每个包补对应反向/真实调用测试；
- 所有正式候选包达到 13/13；
- 支持库失败不得只做披露后放行。

### 批次四：缓存反假绿

修改目标：`测试中心/运行测试.py`、`测试中心/测试_缓存读写.py`。

- 正式发布和 `--范围 全部` 禁止复用旧缓存；
- 增加成功标志、测试数、摘要、依赖和环境字段篡改测试；
- 缓存只能作为普通增量加速证据，不能作为正式发布唯一证据；
- 正式发布必须有真实执行证据。

### 批次五：最终正式验收

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
rm -rf 工程缓存/验证缓存 工程缓存/验证断点.json
unset PYTHONPATH
PYTHONDONTWRITEBYTECODE=1 python3.14 测试中心/运行测试.py --范围 全部
python3.14 开发工具/发布门禁/运行发布门禁.py
```

最终要求：

- 未纳入测试文件为 0；
- 重复测试文件为 0；
- 跳过测试为 0；
- 正式候选包全部 13/13；
- 缓存伪造被拒绝；
- 发布状态为通过。

## 5. 下一步边界

当前只完成第一轮主落点调研。下一步先逐文件读取 54 个漏测文件，给每个文件确定：归属阶段、资源依赖、是否真实可执行、是否需要修复或明确废弃。完成这份逐文件清单并回读确认后，才进入代码维修。

## 第二轮：测试归一化与重复审计

结论：**尚未全部归一化，存在重复执行、逻辑重复和多入口流程**。

### 结构统计

- 测试文件总数：168；
- 当前标准阶段覆盖文件：83；
- 当前慢速阶段覆盖文件：31；
- 标准/慢速阶段去重覆盖文件：114；
- 阶段外文件：54；
- 阶段外测试用例：634；
- 当前 `--范围 全部` 阶段累计测试：1264；
- 跨阶段重复文件：1 个，实际重复计入 8 个测试；
- 完全相同文件内容：0 组；
- AST 检出的同构测试体：17 组（其中部分是不同 Provider 的有意一致性验收，不能直接全部删除）。

因此当前 `--范围 全部` 仍不是仓库全部测试；它只是“常规阶段 + 慢速阶段”，还有 54 个文件未归属。

### 已确认重复

- `测试中心/权威状态/测试_反向破坏.py` 同时归属“权威状态”和“反向破坏”两个阶段，当前会复用/重复计入其中的测试证据；
- `测试中心/第九阶段/测试_反向破坏.py` 与 `测试中心/第九阶段/测试_门禁.py` 有 6 个完全相同的测试体，且分别被“反向破坏”和“发布门禁”阶段收集；
- `测试中心/开发工具/测试_模块模板收敛.py` 与 `测试中心/开发工具/测试_模块模板生成器.py` 有 4 组同构测试体，目前均属于阶段外测试；
- 三个 PostgreSQL Provider 测试有多组同构测试，属于同一契约的 Provider 矩阵验收，建议后续抽取公共测试辅助而不是简单删除。

### 流程未完全统一

- 167 个测试文件包含独立 `unittest.main()` 入口，`测试中心/支持库/测试_FFmpeg提供者.py` 没有该入口；
- `测试中心/运行测试.py:1182-1238` 是阶段内逐文件/缓存流程；
- `测试中心/运行测试.py:1241-1307` 是内部阶段整套件流程；
- `测试中心/运行测试.py:1434-1444` 是指定测试文件工作包流程；
- 因此“直接运行测试文件”“工作包验收”“阶段验收”“全量验收”存在多套执行路径，不能称为单一统一流程。

### 归一化维修方向

1. 建立唯一测试清单，所有测试文件唯一归属阶段；重复归属直接阻断；
2. 统一由 `测试中心/运行测试.py` 调度，测试文件不再承担独立入口职责；
3. 把明确重复的门禁测试合并为唯一事实源；
4. Provider 矩阵保留覆盖，但抽取共享测试辅助并在报告中标注“矩阵实例”，避免重复计数；
5. 将阶段外 54 个文件逐个判定为正式测试、慢速测试或明确废弃，禁止静默遗漏；
6. 归一化后再重新计算总测试数，旧的 1090 不能继续作为最终总数。

## 第三轮：架构多腿一致性审计

### 已确认的验证腿

1. `测试中心/运行测试.py`：阶段测试、工作包测试、内部阶段测试三种调度路径；
2. `开发工具/组件合规/合规测试包.py`：13 项组件合规权威验证器；
3. `开发工具/发布门禁/运行发布门禁.py`：正式发布门禁；
4. `MCP工具箱/验证门禁.py`：验证命令白名单、退出码、零测试和跳过文本判定；
5. `MCP工具箱/模块合规.py`：当前确实委托组件合规，并叠加模块 AST 边界审计；
6. `MCP工具箱/发布治理.py`：当前调用正确的 `开发工具/发布门禁/运行发布门禁.py`；
7. `开发工具/统一验证/统一验证器.py`：独立的结构/契约/资产/装配/加载器/示例验证入口；
8. `开发工具/开发入口.py`：开发者入口，内部另有“执行发布检查”子进程入口。

### 已确认的分叉

- `开发工具/统一验证/统一验证器.py` 正确入口
  `python3.14 -m 开发工具.统一验证.统一验证器 --目标 全部 --范围 结构`
  真实结果为 50 项通过、24 项失败；24 项失败正好来自 6 个聚合父包缺少父包级实现/说明/能力契约/验证场景。
  发布门禁则跳过这 6 个父包，两个验证器对同一仓库得出不同口径。
- `开发工具/统一验证/统一验证器.py` 文件头声称的
  `python3.14 -m 验证器.统一验证器` 入口实际不存在，真实返回 ModuleNotFoundError。
- `开发工具/开发入口.py:211-219` 的“执行发布检查”调用
  `系统根/发布门禁/运行发布门禁.py`；真实路径是
  `系统根/开发工具/发布门禁/运行发布门禁.py`，实际调用结果为退出码 2、发布状态未知。
- `MCP工具箱/发布治理.py:43-46,98-128` 使用正确发布门禁路径，说明当前存在两条发布入口，其中一条已悬空。

### 多腿收口原则

- 只保留一个发布判定事实源：`开发工具/发布门禁/运行发布门禁.py`；
- 统一验证器、MCP 验证门禁、开发入口只能做参数校验或薄转发，不重复维护发布判定；
- 聚合父包判定必须由正式索引提供统一结果，不能由不同入口各自猜目录结构；
- 所有入口必须使用同一测试覆盖清单、同一结果模型和同一失败口径；
- 每个入口都要做真实调用冒烟，悬空入口必须修复或明确删除，不能继续留在可用文档中。
