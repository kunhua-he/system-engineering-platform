# 52 D 类小债务子代理盘点（死代码 / 名实不符 / 占位命名 / 孤儿文件 / 超长文件）

- 盘点仓库：`~/Documents/Agent/PHP/系统工程平台`
- 盘点基点：HEAD `c9116e0`（假绿维修第六批）
- 盘点方式：**只读**。本轮不修改任何源码/测试/配置/文档（仅写本文件），不做 git 操作，不删除、不重命名。
- 盘点纪律：每一条都在现场重新读代码/文件核实，**不照抄审计结论**；审计报告可能过时或误报的地方已单独标注。

## 结论总览

| 编号 | 事项 | 判定 |
|---|---|---|
| 1 | 孤儿文件 `示例项目/适配层示例/项目资源/入口测试.txt` | **真问题**（孤儿，无写出者无读取者） |
| 2 | `开发工具/契约编译/漂移检测.py::全面漂移检测` 0 调用者 | **真问题**（死代码）；「永久空跑」描述**已过期** |
| 3 | `MCP工具箱/项目服务.py` 正式发布证据五项与状态 | **误报**（五项确全取入参；写死「通过」有双重门控） |
| 4 | `平台控制面/提供者/版本兼容_回退.py` 引用计数守卫 | **真问题**（守卫恒空，跨命名空间查错键） |
| 5 | `幂等重放` 摘要口径 | **误报**（已在上一批改为「形状」口径并如实写文档串） |
| 6 | 两份 `当前.json` 指针形状不一致 | **真问题**（形状不同，已被登记为 P2，影响有限） |
| 7 | 两包「流列表」说明文本与键名「流」 | **真问题**，且数量**已过期**（实测 15 处，非 10 处） |
| 8 | 「重试调度」参数名 `错误码白名单` 误导 | **真问题**（已登记未处理） |
| 9 | 甲/乙 占位命名 | **真问题**（口径待定）；统计**已微过期**（78，非 75） |
| 10 | 超 160 行文件统计 | **统计已过期**（实测 600，非 129） |

真问题 **7 条**（1/2/4/6/7/8/9），误报 **2 条**（3/5），纯统计项 1 条（10）。

---

## 1. 孤儿文件 `入口测试.txt`

**现状（现场读到）**

- 文件确实存在：`示例项目/适配层示例/项目资源/入口测试.txt`，12 字节，内容只有一行 `入口测试`（无换行），修改时间 2026-08-28 20:23。
- 同目录另有 `示例输出.txt`（21 字节）、`验证输出.txt`（19 字节，内容 `适配验证输出`，修改时间 2026-09-13 13:03）。
- 全仓检索 `入口测试` 只命中：该文件自身、`开发文档/临时文档/47_假绿盘点与维修.md:243,271,423` 的处置记录、以及一个**无关**的测试用例名 `测试中心/MCP工具箱/测试_项目服务.py:89`（"统一入口测试"，是验证命令名，不读该文件）。
- 结论性事实：**无写出者、无读取者**，是历史残留。

**判定**：真问题（孤儿受跟踪文件）。

**影响范围**：仅 `示例项目/适配层示例/项目资源/` 静态资源；无代码引用，删除不影响任何链路（含示例运行入口）。

**推荐修法（不动手）**：由华哥裁定后删除 —— 属「删除已跟踪文件」，按规则须先确认。删除前建议跑 `python3.14 -m unittest 测试中心.MCP工具箱.测试_项目服务` 与 `python3.14 示例项目/适配层示例/运行入口/运行.py` 各一次确认无读取；若想留历史证据，改为移入 `开发文档/归档/` 而非删除。

**风险**：低。

---

## 2. 能力「漂移检测.全面漂移检测」0 调用者

**现状（现场读到）**

- 定义在 `开发工具/契约编译/漂移检测.py:323-376`。
- 全仓 `grep -rn '全面漂移检测' --include=*.py`（排除 `工程缓存/`）只命中该文件自身：
  - `:3`、`:10`、`:60` 文档串，
  - `:323` 函数定义。
  **无 import、无调用点、无测试调用。**
- 该模块的其它导入方只取别的检测器：
  - `开发工具/复用审计/反向破坏/破坏验证.py:135,150,159` → `检测参数漂移 / 检测错误码漂移 / 检测声明无实现`
  - `开发工具/组件合规/合规测试包.py:683` → `主版本号`
  - `测试中心/开发工具/测试_能力定义编译器.py:23` → `检测能力定义漂移`
- 「永久空跑」的描述**已过期**：函数已不再静默通过 ——
  - `:352-355` 先尝试 `实现目录/{能力名}.py` 直接命中；
  - `:356-361` 猜不中时**显式登记** `实现文件未定位，返回结构/错误码漂移未执行: ...`（而不是当成「无漂移」）；
  - `:343-349` 注释写明本仓实现文件普遍「一包一文件」，549 条能力两项检测命中 0。

**判定**：真问题 —— **死代码（0 调用者）**；但「永久空跑」这一具体描述已被上一批修掉，属**已过期描述**。

**影响范围**：契约漂移检测能力实际未接入任何编译/发布链路（`_正式证据问题`、发布门禁都不调用它）。

**推荐修法（三选一，本轮不动手）**
1. 删除该函数（连带 `:3-11`、`:60-66` 的关联文档串）——最干净，风险最低；
2. 按**注册映射**（`__init__` 的 `注册能力`，或解析三种注册写法）重建「能力 → 实现文件」解析，再让编译器/门禁真正调用，使「返回结构漂移 / 错误码漂移」两项生效（能补上真检测，工作量大）；
3. 维持现状，但在 `开发文档/未完成事项.md` 明确登记为「已裁决不接线」，避免下次审计重复报。

**风险**：① 将来要恢复需从提交历史取回；② 注册写法多样，静态解析易误报；③ 维持现状则「死代码」长期挂账。

---

## 3. `项目服务.py` 正式发布证据：五项是否全取入参、状态是否写死

**现状（现场读到）**：`MCP工具箱/项目服务.py:937-948`，`verify_and_record` 分支：

```
937  if tuple(命令) == 唯一发布命令 and 判定.get("成功") is True:
938      发布证据结果 = 生成发布证据(
939          str(数据.get("提交", "")), str(参数["name"]),
940          int(数据.get("退出码", -1)), str(数据.get("工作区指纹", "")),
941          证据类型=正式发布证据类型, 命令=命令,
942          制品摘要=str(参数.get("artifact_digest", "")),
943          来源指纹=str(参数.get("source_fingerprint", "")),
944          能力覆盖=list(参数.get("capability_coverage", [])),
945          场景覆盖=dict(参数.get("scenario_coverage", {})),
946          真实结果=dict(参数.get("real_result", {})), 状态="通过",
947          核验工作区指纹=str(数据.get("工作区指纹", "")),
```

- **五项（制品摘要/来源指纹/能力覆盖/场景覆盖/真实结果）全部取自调用方入参** —— 不是写死的。审计的问句答案为「是，全取入参」。
- **状态写死 `"通过"`（`:946`）** —— 属实，但**不构成伪造面**，属可接受定值。

**门控（为什么写死无害）**
1. 写入前提：本次命令等于 `唯一发布命令` 且**真实执行**判定成功（`:937`）；未反馈 MCP 使用反馈、命令不在白名单都直接拒绝（`:926-931`）。
2. 落盘前二次校验：`MCP工具箱/发布治理.py:291-301` 调 `_正式证据问题`（`:235-262`）—— 要求命令 == 唯一发布命令、制品摘要合法模式、**状态必须为「通过」且退出码 0**、工作区指纹必须是**当前真实指纹**、来源指纹合法、能力覆盖非空、场景覆盖 `总数>0 且 通过数=总数 且 失败数=0`、真实结果 `成功 is True`。非「通过」根本写不进去，写死与校验同义。
3. 独立伪造被拒：`:857` 对 `generate_release_evidence` 直接返回「正式发布证据只能由 verify_and_record 真实执行唯一发布命令后生成」。
4. 激活时再比对：`发布治理.py:344-353` 把证据的 `制品摘要`/`来源指纹` 与待激活制品及当前工作区指纹比对，不一致即拒。

**判定**：**误报**。五项确系全取入参；`状态="通过"` 是校验规则的必然结果而非漏洞。

**影响范围**：无。

**推荐修法**：无需改代码。可选纯可读性项：把 `:946` 的常量换成 `状态=("通过" if 判定.get("成功") else "失败")` 或加注释说明；**不建议**为该项单开改动批次。

**风险**：无。

---

## 4. `版本兼容_回退.py` 的引用计数守卫是否恒空

**现状（现场读到）**

- 守卫本体：`平台控制面/提供者/版本兼容_回退.py:57-65`
  ```
  57  # 引用计数检查：引用未归零禁止删除快照（拒绝同样写证据留痕）
  58  活跃引用 = self.状态.查询记录("引用计数", "包id=? AND 计数>0", (迁移["旧快照id"],))
  59  if 活跃引用:
  60      证据id = self._证据(迁移, {"禁止删除": True, ...}, "清理被拒")
  ...  非空则拒绝删除
  66  旧目录 = self.快照.快照根目录 / 迁移["旧快照id"]
  67  if 旧目录.is_dir():
  68      shutil.rmtree(旧目录)
  ```
- 表结构：`运行核心/权威状态.py:311` 建 `引用计数(引用键 TEXT PRIMARY KEY, 包id TEXT, 版本 TEXT, 计数 INTEGER)`；`:111` 的 1.0.0 校验规则表已含该表。`平台控制面/平台状态.py:18-22` 子类化继承同一迁移链 → 平台库中该表存在。
- **真实写入键是「资源id」**：`运行核心/权威状态.py:786-793` `增加引用(包id, 版本)` 用 `引用键 = f"{包id}@{版本}"`；唯一调用点 `运行核心/资源协调/服务.py:97` 传 `包id=资源id`（资源id 来自 `:87` 的 `self._资源id(资源id)`，是资源标识），减少在 `:122`。
- **守卫传的是「核心快照id」**：`迁移["旧快照id"]` 由 `平台控制面/提供者/版本兼容.py:78,95` 写入，值来自 `平台控制面/核心快照.py:35` `快照id = uuid.uuid4().hex[:16]`。

**判定**：**真问题（守卫恒空 / 名实不符）**。两个命名空间互不相交 —— 引用计数表按「资源id@版本」记账，守卫却拿「核心快照id」当 `包id` 查，因此**有活跃引用时也不会命中**。真正消费引用计数的是资源协调链路：`运行核心/资源协调/服务.py:286-300` `清理过期快照` 按 `资源id/版本` 查 `引用数`。文档串 `平台控制面/提供者/版本兼容.py:17` 与 `平台控制面/核心快照.py:8` 都仍写着「引用未归零禁止删除快照」这条承诺，实际在版本迁移链路上是空的。

**影响范围**：`平台控制面` 核心快照迁移的 `清理` 阶段 —— 旧核心快照目录可能在仍被引用时被 `rmtree`（`:66-68`）。当前**无现网故障证据**（尚无调用方把快照id登记进引用计数），属「承诺未实现」。

**推荐修法（二选一，本轮不动手）**
1. **让快照迁移真实登记引用**（倾向）：在 `版本兼容` 的双读/切换阶段用 `增加引用(包id=旧快照id, 版本=...)` 记账，清理时按同一键查询，与资源协调同源口径；
2. 若确认核心快照不参与引用计数：**删除该守卫**，并如实修改 `版本兼容.py:17` 与 `核心快照.py:8` 的文档串，不再承诺。

**风险**：① 引入新写入点，需同步迁移互斥/证据与测试，避免记账与清理阶段不同步；② 放弃保护前须确认确无并行读者，否则会出现「删了还在读」。

---

## 5. `幂等重放` 的摘要口径

**现状（现场读到）**：`运行核心/能力调用/唯一能力调用.py:339-357`

- 现实现按**参数形状**判定：`_声明参数名(实现)` 取声明参数名集合，多参能力要求 `set(参数) == 声明参数名`（`:355`），单参聚合契约恒复用（`:353-354`）；`try/except` 兜底返回 False。
- 原审计描述的 `return ... or isinstance(参数, dict)` **恒真短路已不存在**。
- 值级摘要函数 `_参数摘要` 在 `:388-402`，文档串**明确写「**当前未接线**」**并给出出处（停止的工作包，MCP反馈开工id `17b41a8e9b0b43f4`，2026-08-01）。
- 口径已统一：`:4` 模块文档串与 `AGENTS.md:116` 都写作「只允许**参数形状**一致的幂等重放」。
- 补充实测：`幂等重放` 现全仓 **0 调用者**（`grep '\.幂等重放('` 除定义外无命中），仅协议声明 `公共契约/能力契约/调用器.py:31` 与测试桩（`测试中心/支持库/测试_表格文档.py:29` 等）。

**判定**：**误报（审计结论已过期）**。当前不存在「只做值级摘要」的实现，也没有与其它摘要混用口径；文档串与代码一致。

**影响范围**：无。

**推荐修法**：无需改。可选清理项：把 `_参数摘要` 与其文档串一并删除（现在是「承接意图但没人调用」的函数），或把它移入 `未完成事项.md` 待裁决项。**不建议**在 0 调用者状态下实现值级比较。

**风险**：无。

---

## 6. 两份 `当前.json` 指针形状

**现状（现场读到，均为单行 JSON）**

| 路径 | 内容 | 键数 |
|---|---|---|
| `工程缓存/制品仓库/平台客户端制品/当前.json` | `{"摘要sha256": "e0e11e9801e44b7f", "路径": "平台客户端-e0e11e9801e44b7f"}` | 2 |
| `工程缓存/制品仓库/平台客户端环境/当前.json` | `{"摘要sha256":"2ee78ea86b3fb981","制品目录":"平台客户端-2ee78ea86b3fb981","制品摘要":"1d50f2a805b10ca98b09ae80049c5284","版本":50,"栅栏令牌":50}` | 5 |
| `工程缓存/制品仓库/平台客户端环境-失效-20260822190912/当前.json` | 失效目录残留（第三份） | — |

- 写入方不同：`客户端/构建平台客户端.py:535-545`（制品目录指针，2 键）；`平台控制面/包仓库/平台客户端制品.py:394-418`（环境目录指针，注释写「保持外部调用方稳定路径契约并携带 版本/栅栏令牌」，5 键）。
- 真实消费者读的是**环境目录**那份：`支持库/适配层/Pillow提供者/实现/子进程入口.py:28,36`、`支持库/适配层/PyMuPDF提供者/实现/子进程入口.py:33`、`支持库/后端/图像处理支持库/图像解码/实现/子进程入口.py:28,36`、`支持库/后端/文档转换支持库/PDF渲染/实现/子进程入口.py` 等。

**判定**：**真问题（形状不一致 + 三份并存）**，但**影响有限**：制品目录那份是构建器自用指针，未被外部子进程入口解析。已在 `开发文档/未完成事项.md:95` 登记为 P2「制品指针语义待确认」。

**影响范围**：指针契约不统一；`摘要sha256` 在两份里的语义也需确认（制品目录那份对应目录名，环境那份对应 5 键版本）。

**推荐修法（未完成事项.md:95 已给方向，本轮不动手）**：先定权威写入语义（谁写、写哪份），再统一字段形状（建议统一到环境目录的 5 键版本，并把 `路径` 作为兼容别名保留），最后清理 `平台客户端环境-失效-20260822190912/`。

**风险**：中 —— 若贸然统一形状，构建器与子进程入口会同时受影响（两处各按自己形状读键），必须同批改并跑 `测试中心/平台控制面/测试_平台客户端制品接入` 与子进程入口相关测试。

---

## 7. 两包「流列表」说明文本 vs 键名「流」

**现状（现场读到）**

- 真实返回键是 **`流`**：`支持库/适配层/FFmpeg提供者/实现/探测.py:119`、`支持库/后端/媒体处理支持库/FFmpeg媒体/实现/探测.py:121`（`"流": 流列表`）。
- 消费方也按 `流` 取：`FFmpeg提供者/实现/处理.py:119`、`FFmpeg媒体/实现/处理.py:119,199`；契约侧期望键名同为 `流`：`FFmpeg提供者/验证场景引用.json:50`、`FFmpeg媒体/验证场景.json:56,63`。
- 说明文本却写「流列表」，两包共 **15 处**（审计说 10 处，**数量已过期**）：
  - **FFmpeg提供者 7 处**：`包声明.json:36`、`__init__.py:6`、`说明/使用说明.md:10`、`说明/目录树与命令.md:45`、`能力定义.json:54`、`能力契约/参数契约.json:80`、`能力数据/能力搜索数据.json:18`
  - **FFmpeg媒体 8 处**：`包声明.json:36`、`__init__.py:6`、`说明/使用说明.md:11` **和** `:46`（同文件 2 处）、`说明/目录树与命令.md:45`、`能力定义.json:54`、`能力契约/参数契约.json:80`、`能力数据/能力搜索数据.json:18`
- 注意区分：实现目录里名为 `流列表` 的**局部变量**（`探测.py:106,108` 等）不算错，只有「返回 {时长秒, **流列表**, 格式, ...}」这类**说明文本**是错的。

**判定**：**真问题（名实不符）**，且数量统计已过期（实测 15 处）。

**影响范围**：说明书 / 能力搜索数据 / 包声明 / 参数契约 的返回结构描述与真实 HTTP 返回不一致 —— Agent 或调用方按说明书写代码会取错键。

**推荐修法**：改 `能力定义.json` 的 `说明` 字段（**唯一事实源**，两包各 1 处），然后**重编译生成**派生文件（`包声明.json`、`能力契约/参数契约.json`、`能力数据/能力搜索数据.json`、`__init__.py`、`说明/使用说明.md`、`说明/目录树与命令.md`）。**不要手改派生文件** —— 手改会被 `检测能力定义漂移` 判为漂移。

**风险**：低；但必须走编译器生成，且两包派生文件需同批更新。

---

## 8. 「重试调度」参数名误导

**现状（现场读到）**：`支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1157-1159`

```
1157 def 重试调度(*, 重试模式: Any = None, 错误码白名单: list | None = None,
1158              最大重试次数: int | None = None, 退避基数秒: Any = None,
1159              抖动比例: Any = None) -> 结果:
```

- 实现里 `错误码白名单` **只判非空**：`:1209` `是否重试 = bool(白名单列表) and 上限次数 > 0`；`:1212-1215` 的「命中」说明也只是把整个列表打印出来，**从不与实际错误码比对**。
- 签名中**没有任何入参接收「本次实际错误码」**，即调用方必须先自行比对，白名单语义实为「已命中的可重试错误码集合」。

**判定**：**真问题（名实不符）**，且已登记未处理 —— `开发文档/未完成事项.md:97`（2026-09-10 独立复验发现）原文：形参名 `错误码白名单` 实际语义是「已命中的可重试错误码集合」，易被误读为「过滤规则」，且缺 `实际错误码` 入参。本轮现场核实与登记**完全一致**。

**影响范围**：该能力所有调用方的语义理解；纯计算工具、无副作用，不会误删/误写数据。

**推荐修法（未完成事项.md:97 已给二选一，本轮不动契约）**
1. **改名**（如 `已命中可重试错误码`）——破坏性，需评估调用方；
2. **补 `实际错误码` 入参**做真实比对，让白名单回归「过滤规则」本义。

**风险**：① 改名会波及 `能力定义.json:1215`、`能力契约/参数契约.json:1285`、`验证场景.json:499`、`能力搜索数据.json:396`、`说明/使用说明.md:556-582` 与调用方，需同批重编译；② 补入参属能力扩展，需按项目规则走「能力搜索 → 复用决策 → 验收契约」流程。

---

## 9. 甲/乙 占位命名

**口径与实测**

- 范围：`*.py / *.json / *.md`，排除 `工程缓存/`、`.git/`、`开发文档/参考资料/`、`开发文档/归档/`。
- **含「甲」或「乙」的文件：105 个**（排除 `示例项目/`；含示例项目为 108 个）。
- **同时含「甲」与「乙」的文件：78 个**（排除 `示例项目/`；含示例项目为 81 个）。未完成事项.md:101 记的是「75 个」，量级一致 → **统计已微过期（75 → 78）**。
- 反例警告：模式含「丁」会大量命中 **`补丁`** 二字（如 `支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py` 的 27 次命中全是「补丁」）—— **不能**把「丁」计入占位命名统计。

**性质（现场抽样，与 `未完成事项.md:101` 结论一致）**：绝大多数是**抽象对称占位**，不是业务示例数据。典型：

- `平台控制面/包仓库/可复现构建器.py:110-123` —— `工作区甲`/`工作区乙`、`摘要甲`/`摘要乙`（双工作区可复现构建对比）
- `开发工具/发布门禁/权威状态门禁.py:90-105` —— `事务id="甲"/"乙"`、`进程甲`/`进程乙`、`令牌甲`/`令牌乙`
- `开发工具/统一验证/统一验证器.py:110-111`（`分割文本("甲,乙,丙", ",")`）、`:223-225`（包id「甲」「乙」依赖循环）
- `开发工具/发布门禁/第十四阶段门禁.py:32` —— 消费者id `消费甲`
- `测试中心/加载器/测试_装配锁与幂等.py:86-263` —— 测试夹具的临时包名 `甲`/`乙`（出现 77 次，最多）
- 契约侧样例值：`支持库/后端/数据结构支持库/验证场景引用.json:48-170`（压入甲／入队甲／值「甲」）、`支持库/后端/数据操作支持库/数据交换/验证场景.json`、`支持库/后端/数据操作支持库/数据集合/*`（能力定义/参数契约/包声明/能力搜索数据/使用说明 各 8 次）等

**判定**：**真问题（口径待确认的观察项）**；统计已微过期。它在**正式代码**里也出现（可复现构建器、门禁、统一验证器），不只是测试夹具。

**影响范围**：命名可读性；不影响功能。测试中心的临时包名属测试夹具，清理收益低。

**推荐修法**：**逐个判断再改，禁止批量替换**。建议口径：
- 正式代码里的抽象对称位改成语义名（`工作区甲/乙` → `工作区首个/工作区重建`；`摘要甲/乙` → `基准摘要/复算摘要`；`事务id 甲/乙` → `旧事务/新事务`）；
- `验证场景.json` 的样例值若确属「场景/用例样例数据」，按华哥「测试数据要口语化」规则替换；
- 测试夹具（`测试中心/**` 的临时包名）可保留。

**风险**：中 —— 涉及契约 JSON 需重编译；门禁/验证器改字面量会波及对应测试断言，需同批改并跑受影响测试。

### 占位命名真实清单（前 30 条，按出现次数降序；总数 105 个文件）

| # | 文件 | 出现次数 |
|---|---|---|
| 1 | `测试中心/加载器/测试_装配锁与幂等.py` | 77 |
| 2 | `测试中心/MCP工具箱/测试_工作区管理.py` | 27 |
| 3 | `测试中心/平台控制面/测试_制品与资源.py` | 26 |
| 4 | `测试中心/第一批维修/测试_检查点恢复隔离.py` | 20 |
| 5 | `测试中心/第一批维修/测试_HTML验证器.py` | 17 |
| 6 | `模块库/文件管理/验证场景/正向HTTP场景.json` | 17 |
| 7 | `开发工具/发布门禁/第十四阶段门禁.py` | 17 |
| 8 | `支持库/后端/数据操作支持库/数据交换/验证场景.json` | 16 |
| 9 | `支持库/后端/并发控制支持库/验证场景.json` | 16 |
| 10 | `测试中心/运行核心/测试_资源句柄网关.py` | 15 |
| 11 | `测试中心/平台控制面/测试_工作包11_消费者契约注册表.py` | 15 |
| 12 | `测试中心/平台控制面/测试_工作包09_备份契约.py` | 15 |
| 13 | `测试中心/第一批维修/测试_统一编译器.py` | 14 |
| 14 | `测试中心/MCP工具箱/测试_任务观测.py` | 14 |
| 15 | `测试中心/加载器/测试_加载器.py` | 12 |
| 16 | `开发工具/发布门禁/权威状态门禁.py` | 12 |
| 17 | `平台控制面/包仓库/可复现构建器.py` | 12 |
| 18 | `测试中心/第一批维修/测试_简单窗口受管状态.py` | 11 |
| 19 | `测试中心/发布门禁/测试_发布门禁收敛.py` | 11 |
| 20 | `测试中心/公共契约/测试_能力契约.py` | 11 |
| 21 | `测试中心/第一批维修/测试_运行缓存制品不变.py` | 10 |
| 22 | `测试中心/第一批维修/测试_发布门禁.py` | 10 |
| 23 | `测试中心/开发工具/测试_项目编译器.py` | 10 |
| 24 | `测试中心/平台控制面/测试_工作包04_维护者视图.py` | 10 |
| 25 | `支持库/后端/数据结构支持库/验证场景引用.json` | 10 |
| 26 | `支持库/后端/数据操作支持库/文本处理/验证场景.json` | 10 |
| 27 | `测试中心/运行核心/测试_句柄账本持久化.py` | 9 |
| 28 | `测试中心/支持库/测试_进程文件补齐.py` | 9 |
| 29 | `测试中心/平台控制面/测试_平台控制面.py` | 9 |
| 30 | `测试中心/平台控制面/测试_平台客户端制品接入.py` | 9 |

（第 31-105 条见 `grep -rcE '[甲乙]' ... | sort -t: -k2 -rn` 的完整输出，本节末尾证据索引给命令。）

---

## 10. 超 160 行文件统计

**口径**：文件类型 `*.py / *.json / *.md / *.js / *.html / *.css / *.sh`；排除 `工程缓存/`、`.git/`、`.codegraph/`、`开发文档/参考资料/`。

**实测（本轮）**：**600 个**文件超过 160 行，合计 **212,489 行**。审计结论的 129 → **统计已过期**（与实测差 4.65 倍）。

**分口径对照**（同一批文件，仅改排除范围）：

| 口径 | 超 160 行文件数 |
|---|---|
| 全类型（含 `示例项目/可双击演示/产物文件夹/`） | 600 |
| 全类型（排除 `示例项目/可双击演示/产物文件夹/`） | 538 |
| 仅 `*.py`（排除产物） | 258 |
| 仅正式源码目录 `*.py`（支持库/模块库/运行核心/平台控制面/开发工具/公共契约/前端核心/后端核心/启动监督器/项目适配层/客户端/MCP工具箱/技能库） | 162 |
| 仅 `*.py` 且排除 `测试中心/` | 162 |

**129 不落在任何一个口径里** → 判定为「统计已过期」（该数字可能取自假绿维修批次之前的快照，或口径未注明）。

**按二级目录分布 Top 10**

| # | 目录 | 文件数 |
|---|---|---|
| 1 | `支持库/后端` | 236 |
| 2 | `示例项目/可双击演示`（产物副本） | 64 |
| 3 | `支持库/适配层` | 50 |
| 4 | `测试中心/支持库` | 21 |
| 5 | `测试中心/运行核心` | 16 |
| 6 | `测试中心/第一批维修` | 14 |
| 7 | `模块库/直播逐字稿` | 12 |
| 8 | `测试中心/模块库` | 12 |
| 9 | `测试中心/MCP工具箱` | 9 |
| 10 | `测试中心/开发工具` | 9 |

按顶层目录：`支持库` 291、`测试中心` 95、`示例项目` 64、`模块库` 49、`开发工具` 28、`运行核心` 25、`平台控制面` 14、`MCP工具箱` 11、`开发文档` 10，其余 ≤ 4。

**最长 10 个文件**

| # | 文件 | 行数 |
|---|---|---|
| 1 | `支持库/后端/并发控制支持库/能力契约/参数契约.json` | 2370 |
| 2 | `支持库/后端/并发控制支持库/能力定义.json` | 2214 |
| 3 | `开发工具/发布门禁/运行发布门禁.py` | 1545 |
| 4 | `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py` | 1535 |
| 5 | `支持库/后端/大语言模型支持库/模型连接器/能力契约/参数契约.json` | 1484 |
| 6 | `支持库/后端/系统核心支持库/资源管理/能力契约/参数契约.json` | 1447 |
| 7 | `支持库/后端/系统核心支持库/资源管理/能力定义.json` | 1406 |
| 8 | `支持库/后端/大语言模型支持库/模型连接器/能力定义.json` | 1404 |
| 9 | `支持库/后端/数据操作支持库/数据集合/能力契约/参数契约.json` | 1355 |
| 10 | `模块库/文件管理/验证场景/正向HTTP场景.json` 与 `示例项目/可双击演示/产物文件夹/20260822-demo/模块库/文件管理/验证场景/正向HTTP场景.json`（同一份的产物副本，各 1198） | 1198 |

**分类**

- **生成类 JSON**（`能力定义.json` / `能力契约/参数契约.json` / `能力搜索数据.json` / `Agent查询数据.json` / `包声明.json` / `完整性摘要.json` / `验证场景.json`）—— 结构平铺，「超长」等于「数据条目多」，**不建议按代码拆分**。真正可讨论的是「能力定义要不要拆包」，但项目未采用该思路。
- **手写 Python**（这才是该拆的候选）：`开发工具/发布门禁/运行发布门禁.py` 1545、`模型连接器/实现/模型连接器.py` 1535、`MCP工具箱/项目服务.py` 1018、`运行核心/权威状态.py` 993、`开发工具/组件合规/合规测试包.py` 932、`运行核心/统一网关/本地网关.py` 901、`开发工具/项目编译/项目编译器.py` 780。
- **文档 md**：`支持库/后端/并发控制支持库/说明/使用说明.md` 1194、`开发文档/当前流程图.md` 728、`开发文档/底座升级方案.md` 588、`开发文档/项目说明.md` 545 —— 多为生成类说明，随能力数增长。
- **编译产物副本**：`示例项目/可双击演示/产物文件夹/20260822-demo/` 下 64 个，统计时应排除，否则与源码重复计数。

**判定**：**真问题但不阻断**（本任务只要求统计与分类）；审计的 129 属**统计已过期**。

**推荐修法**：只对「手写 Python > 600 行」排优先级（先 `运行发布门禁.py`、`模型连接器.py`、`权威状态.py`、`项目服务.py`、`合规测试包.py`）；生成类 JSON 与产物副本不计入债务。若落进 `未完成事项.md`，**必须标口径与取证命令**（AGENTS.md 要求数字标「实测」或「历史快照」）。

---

## 证据索引（文件路径 + 行号）

**1 孤儿文件**
- `示例项目/适配层示例/项目资源/入口测试.txt:1`（内容 `入口测试`，12 字节）；同目录 `示例输出.txt`（21 字节）、`验证输出.txt:1`（`适配验证输出`）
- `开发文档/临时文档/47_假绿盘点与维修.md:243,271,423`（历史处置记录）
- 反例（无关命中）：`测试中心/MCP工具箱/测试_项目服务.py:89`

**2 全面漂移检测**
- `开发工具/契约编译/漂移检测.py:3,10,60,323,352,353,354,355,356,357,358,359,360,361`
- `开发工具/复用审计/反向破坏/破坏验证.py:135,150,159`；`开发工具/组件合规/合规测试包.py:683`；`测试中心/开发工具/测试_能力定义编译器.py:23`

**3 正式发布证据**
- `MCP工具箱/项目服务.py:857,926,937,938,942,943,944,945,946,947`
- `MCP工具箱/发布治理.py:235,236,238,240,242,244,246,249,257,259,291,299,300,344,351,353`

**4 引用计数守卫**
- `平台控制面/提供者/版本兼容_回退.py:57,58,59,60,66,67,68`
- `平台控制面/提供者/版本兼容.py:17,78,95`
- `平台控制面/核心快照.py:8,35`
- `运行核心/权威状态.py:111,311,786,789,791,795,800,806`
- `运行核心/资源协调/服务.py:87,97,122,286,296`
- `平台控制面/平台状态.py:18,22`；`公共契约/能力契约/调用器.py`（协议侧无关）

**5 幂等重放**
- `运行核心/能力调用/唯一能力调用.py:4,339,340,344,348,352,353,354,355,388,389,391,392,393`
- `AGENTS.md:116`；`公共契约/能力契约/调用器.py:31`；`测试中心/支持库/测试_表格文档.py:29`

**6 当前.json**
- `工程缓存/制品仓库/平台客户端制品/当前.json:1`
- `工程缓存/制品仓库/平台客户端环境/当前.json:1`
- `工程缓存/制品仓库/平台客户端环境-失效-20260822190912/当前.json`
- `客户端/构建平台客户端.py:535,539,545,566,570`；`平台控制面/包仓库/平台客户端制品.py:299,304,305,394,403,404,418,440,445,447`
- 消费者：`支持库/适配层/Pillow提供者/实现/子进程入口.py:28,36`；`支持库/适配层/PyMuPDF提供者/实现/子进程入口.py:33`；`支持库/后端/图像处理支持库/图像解码/实现/子进程入口.py:28,36`
- 登记：`开发文档/未完成事项.md:95`

**7 流列表**
- 实现键名：`支持库/适配层/FFmpeg提供者/实现/探测.py:119`；`支持库/后端/媒体处理支持库/FFmpeg媒体/实现/探测.py:121`
- 消费方：`FFmpeg提供者/实现/处理.py:119`；`FFmpeg媒体/实现/处理.py:119,199`；`FFmpeg提供者/验证场景引用.json:50`；`FFmpeg媒体/验证场景.json:56,63`
- 说明文本 15 处：`FFmpeg提供者` 的 `包声明.json:36`、`__init__.py:6`、`说明/使用说明.md:10`、`说明/目录树与命令.md:45`、`能力定义.json:54`、`能力契约/参数契约.json:80`、`能力数据/能力搜索数据.json:18`；`FFmpeg媒体` 的 `包声明.json:36`、`__init__.py:6`、`说明/使用说明.md:11,46`、`说明/目录树与命令.md:45`、`能力定义.json:54`、`能力契约/参数契约.json:80`、`能力数据/能力搜索数据.json:18`

**8 重试调度**
- `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py:1150,1157,1158,1159,1163,1164,1209,1212,1213,1214,1215`
- 契约/说明：`模型连接器/能力定义.json:1215,1217`；`能力契约/参数契约.json:1285`；`验证场景.json:499`；`能力数据/能力搜索数据.json:396`；`说明/使用说明.md:556,582`
- 登记：`开发文档/未完成事项.md:97`

**9 占位命名**
- 登记：`开发文档/未完成事项.md:101`
- 正式代码样例：`平台控制面/包仓库/可复现构建器.py:110,111,112,114,115,119,120,121,122,123`；`开发工具/发布门禁/权威状态门禁.py:90,91,92,93,94,96,97,99,100,101,103,105`；`开发工具/发布门禁/第十四阶段门禁.py:32`；`开发工具/统一验证/统一验证器.py:110,111,223,224,225`
- 测试夹具样例：`测试中心/加载器/测试_装配锁与幂等.py:86,87,88,91,92,96,136,137,141,153,155,157,158,189,193,208,209,211,260`
- 契约样例值：`支持库/后端/数据结构支持库/验证场景引用.json:48,56,72,80,107,114,122,135,143,170`；`支持库/后端/数据操作支持库/数据交换/验证场景.json:160`
- 「丁」污染反例：`支持库/后端/文件系统支持库/文本补丁/实现/文本补丁.py`（27 次命中全为「补丁」）

**10 超长文件**
- 统计源文件 `/tmp/长文件.txt`（`wc -l` 全量输出），最长条目见上文表

**本轮取证命令（可复现）**

```bash
cd ~/Documents/Agent/PHP/系统工程平台

# 1 孤儿文件
find . -name '入口测试.txt' -not -path './工程缓存/*'
grep -rn '入口测试' . --exclude-dir=工程缓存 --exclude-dir=.git

# 2 全面漂移检测 调用者
grep -rn '全面漂移检测' . --include='*.py' | grep -v 工程缓存

# 6 当前.json
find . -name '当前.json'; cat 工程缓存/制品仓库/平台客户端制品/当前.json
cat 工程缓存/制品仓库/平台客户端环境/当前.json

# 7 流列表 计数
for f in 支持库/适配层/FFmpeg提供者/{包声明.json,__init__.py,能力定义.json} \
         支持库/适配层/FFmpeg提供者/说明/{使用说明.md,目录树与命令.md} \
         支持库/适配层/FFmpeg提供者/能力契约/参数契约.json \
         支持库/适配层/FFmpeg提供者/能力数据/能力搜索数据.json ; do echo "$(grep -c 流列表 $f)  $f"; done
# （FFmpeg媒体 同形，另注意 说明/使用说明.md 为 2 处）

# 9 占位命名
grep -rcE '[甲乙]' --include='*.py' --include='*.json' --include='*.md' \
  --exclude-dir=工程缓存 --exclude-dir=.git --exclude-dir=参考资料 --exclude-dir=归档 \
  --exclude-dir=示例项目 . | grep -v ':0$' | sort -t: -k2 -rn
# 同时含甲乙：comm -12 <(grep -rl 甲 ...) <(grep -rl 乙 ...)

# 10 超 160 行
find . -type f \( -name '*.py' -o -name '*.json' -o -name '*.md' -o -name '*.js' \
  -o -name '*.html' -o -name '*.css' -o -name '*.sh' \) \
  -not -path './工程缓存/*' -not -path './.git/*' -not -path './开发文档/参考资料/*' \
  -not -path './.codegraph/*' -exec wc -l {} + | awk '$2!="total" && $1>160' | wc -l
```

---

## 本轮未做（边界）

- 未修改任何源码/测试/配置/文档（仅写本文件 `开发文档/临时文档/52_死代码与名实不符_子代理盘点.md`）。
- 未执行任何 git 操作；未删除、未重命名任何文件（含孤儿 `入口测试.txt`）。
- 未对第 2/4/6/7/8/9 项实施修复，只给修法与风险；第 3/5 项判定为误报，未改代码。
