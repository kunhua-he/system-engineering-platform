# 审计复核与双项目分割收口（2026-09-07）

## 开工流程（先审核，确认后施工）

### 目标

核实 `审计报告_系统工程平台_20260907.md` 的每条重要结论；把系统工程平台与华世王镞 V3 收敛为两个独立项目：底座只维护通用能力和公开契约，V3 只维护业务、权限、业务数据库和外部能力 HTTP 消费；清除当前事实源中的交叉项目描述、旧入口和重复待办，但不误动历史证据和华哥在途改动。

### 已完成的只读核验

- 底座当前 HEAD：`f05477c304fb0f71bd5c5057451e9ed745a66b45`；工作树已有在途修改和未跟踪文档，不能 reset、clean 或一次性提交。
- 底座显式测试模块：164；粗计测试方法：1698；正式包索引：82 个支持库、14 个模块库、96 个正式包、419 项能力、冲突 0、排除 0。
- 媒体转写定向测试：20 项中 18 项通过、2 项失败；“进程崩溃/超时”当前仍被模块包装成“转写失败”。
- 测试网关凭证：渐进能力目录、项目适配层两个测试类在未显式装配 `系统库网关凭证` 时均报错，报告结论成立。
- Provider 白名单：浏览器自动化测试 3 处实现层直导未加入白名单，单独运行失败，报告结论成立。
- fitz 探针单独运行：16 项通过；组件合规收敛单独运行：13 项通过；报告所称全量顺序污染仍需在修复其他项后用显式全量复核，不能把单独通过当作全量闭合。
- 能力唯一事实源定向回归：3 项通过，但在负向 HTTP 响应中出现 `HTTPError` ResourceWarning，报告结论成立。
- 启动脚本默认端口为 40006，Python 入口和项目说明默认 8866，端口漂移成立；40007 是执行入口，不与发现网关合并。

### 施工批次

#### 批次一：底座真实问题最小修复

1. 媒体模块透传稳定错误码：保留“进程崩溃、超时、取消、超出限制、未配置模型、模型缺失、文件不存在”等底层契约；仅在没有稳定错误码时使用模块默认错误码。
2. 测试网关凭证统一装配：测试内部使用显式、仅测试进程可见的 ASCII 凭证，不依赖父 Shell；不得写入长期配置、日志或提交文件。
3. 关闭 HTTPError 响应流：所有负向 HTTP 读取路径使用明确的关闭边界，并用 `ResourceWarning` 严格回归。
4. 关闭权威状态门禁子进程 stdout/stderr 管道：强杀、等待和异常路径均闭合；同时核对权威状态连接关闭生命周期，不通过屏蔽警告收口。
5. Provider 白名单同步浏览器自动化内部生命周期测试，保持门禁语义为“实现层直导只允许真实内部生命周期测试”。
6. fitz 与组件合规测试改为进程隔离或唯一模块名/显式清理，消除顺序依赖。
7. 能力发现网关统一默认端口 8866；同步脚本、入口、说明和最小示例，40007 执行网关保持不变。

#### 批次二：底座唯一事实源收口

1. 重新生成当前正式索引、测试统计和真实运行证据。
2. 更新 `开发文档/项目说明.md`、`开发文档/未完成事项.md`、`开发文档/当前流程图.md`，删除失效的“已通过”历史数字和项目专属描述。
3. 底座活动规则、源码和当前文档不再写入具体业务项目名称、项目目录、调用方专属逻辑或项目适配实现路径；统一写“外部调用方/外部项目”。
4. 审计报告中的已确认事实吸收到唯一债务清单和主文档后，审计报告作为过程证据归档或删除，不与当前事实源并列。

#### 批次三：V3 独立项目收口

1. V3 保留唯一公开外部能力 HTTP 消费客户端和冻结 HTTP 字段契约；不导入底座源码、制品、内部数据库、内部 Provider 或控制面。
2. V3 当前文档只描述“外部能力服务/公开能力网关”的消费边界；不复制底座内部实现规则，不把底座内部文件路径、Provider 名称、制品仓库和底座治理任务写成 V3 事实。
3. V3 未完成事项只保留 V3 自身的公开 HTML/HTTP 矩阵与 Whisper 业务精度标杆；底座 Provider、资源治理、底座测试债务归底座清单。
4. 历史方案逐份人工核对：已解决内容吸收后归档，仍有效内容进入对应项目唯一权威文档，不能让临时文档继续成为第二事实源。

#### 批次四：分级验证与收口

- 每个代码批次先跑受影响 unittest 和 `py_compile`，并启用 `PYTHONWARNINGS=error::ResourceWarning` 做资源回归。
- 底座一批修复完成后跑一次显式全量测试，记录模块数、用例数、失败、错误、跳过、警告、耗时和退出码。
- V3 只跑受影响的公开 HTTP/HTML 冒烟；不把底座全量测试结果冒充 V3 验收。
- 收口前分别做底座/V3 的 `git diff --check`、跨项目引用扫描、主文档回读和工作树对账；不推送远程、不覆盖既有未提交改动。

### 回退方式

- 每一批只修改登记的文件，先保留当前工作树；代码问题用 Git 反向提交或精确恢复，不执行 `git reset --hard`、`git clean`、强制覆盖和远程推送。
- 测试临时目录、测试凭证和本地运行态不进入仓库；服务配置不改长期端口，端口统一只改项目默认值和测试参数。
- 文档误删时从 Git 或本轮前备份恢复；历史审计报告在吸收前不删除。

## 审计报告当前裁决

| 报告项 | 当前裁决 | 处理归属 |
|---|---|---|
| 全量显式测试 164/1696，6 失败 2 错误 | 模块总数与当前一致；失败总数需修复后重新跑，不能直接沿用报告总数 | 底座 |
| 媒体错误码透传 | 已复现，真实问题 | 底座 |
| 测试网关凭证 | 已复现，真实问题 | 底座测试装配 |
| SQLite 连接资源警告 | 代码存在长期线程连接设计，需严格 ResourceWarning 回归后定点修复 | 底座 |
| 门禁子进程管道警告 | 报告证据与当前源码一致，真实问题 | 底座 |
| HTTPError 响应未关闭 | 已复现，真实问题 | 底座测试/HTTP边界 |
| 96/419 正式索引统计 | 已复现，报告统计成立；旧文档数字失效 | 底座文档 |
| 项目说明历史“全部通过” | 与当前失败证据冲突，需按当前实测改写 | 底座文档 |
| 能力发现端口 40006/8866 漂移 | 已复现，真实问题；统一 8866，40007 保持执行 | 底座 |
| Provider 白名单 | 已复现，真实问题 | 底座测试门禁 |
| fitz 顺序污染 | 单独测试通过，不能取消；全量复核确认后处理 | 底座测试隔离 |
| 组件合规顺序污染 | 单独测试通过，不能取消；全量复核确认后处理 | 底座测试隔离 |

## 收口验收结果（底座批次一）

- 底座实际修改文件：已修复底座代码、测试隔离、端口脚本、正式摘要和底座文档；未修改 V3。
- 底座定向验证：媒体 20/20；凭证/项目适配 24/24；Provider/能力事实源 14/14；fitz/组件 29/29；权威状态门禁 1/1；聚合摘要 1/1；LibreOffice 受管池 7/7；平台控制面及资源链 132/132；进程文件补齐 8/8。
- 底座显式全量验证：最近一次完整运行 164 个模块、1707 个用例、0 失败、0 错误、退出码 0、424.001 秒；ResourceWarning 0。
- 资源门禁：最新一次普通全量 `ResourceWarning` 计数为 0；带分配栈诊断全量因第三方 PDF 隔离子进程耗时异常主动终止，不作为验收证据。
- V3 HTTP/HTML 验收：本轮未执行。
- 两仓交叉引用扫描：底座活动文档/源码/规则文件已泛化为“外部项目”边界；V3 规则文件保持自身项目规则，未引入底座内部实现、制品或运行数据；两个 `AGENTS.md` 已分别备份并独立核对。
- 未完成事项：底座审计报告涉及项已收口；底座后续仅保留按需拆分维护性债务。V3 任务不在本轮处理。

## Agent 规则文件独立收口

- 底座 `AGENTS.md`：保留通用平台能力、公开契约、唯一网关、禁止依赖业务项目等规则；已删除 V3 专属聚合接入描述和点名引用。
- V3 `AGENTS.md`：保留 V3 自身的业务项目规则、MCP 入口、权限边界和跨仓库隔离规则；未复制底座内部实现规则。
- 修改前备份：
  - `~/.hermes/backups/项目规则文件/系统工程平台_AGENTS.md.bak_20260908_141322`
  - `~/.hermes/backups/项目规则文件/华世王镞_v3_AGENTS.md.bak_20260908_141322`
- 本次范围：仅修改底座 `AGENTS.md` 与本收口工作台；未修改 V3 代码和 V3 `AGENTS.md`。
- 验证方式：分别回读两个规则文件，执行跨项目关键词扫描、`git diff --check`；底座既有全量验证证据仍以 1707/1707、ResourceWarning 0 为准。
