# 剩余小尾巴 · 批量修复清单（2026-09-15）

> 华哥口径：**先落文件，再批量修复**。本文件只留**未解决**事项；条目一旦修复即从本文件删除，
> 结论合并到唯一权威文档（`开发文档/未完成事项.md` / `开发文档/项目说明.md`）。

发布门禁当前状态：**通过**（制品 `6a6b9814527de6d1`，提交 `51b3da36`）。以下三项不阻塞上线，但都在"干净底座"口径内。

---

## 尾巴 1：信任链历史快照保留策略 —— **已收口（2026-09-15）**

- **问题**：`工程缓存/制品仓库/平台客户端信任/`（快照区）积累历史快照，其中一部分被**当前制品的信任链**引用；
  按"不归档、废弃直接删"不能一概删除，否则签名/信任校验会断。
- **华哥口径（2026-09-15 定）**：**每个只保留一个权威在本地，过期的直接删**，删错从 git 恢复。
- **已做**：快照 76→1 份（留最新）、目标 62→1 份（留当前制品）、签名制品包 4→1 份；
  同时清掉两份陈旧指针备份文件（含 2026-09-06 那份）。
- **验证**：由发布门禁的信任/签名项与"二阶恢复"项负责，收口后重跑门禁确认。

## 尾巴 1b：工程缓存磁盘收口（"过期就删"）—— **已执行**

- **已释放**：HTML 验证证据 236M→只留最近一次、编译缓存 107M→只留最近一次、制品仓库历史副本（4 份内容寻址目录/多份身份目录）→只留当前，
  信任链多份→各留 1 份；提供者运行环境**同一提供者的多份环境摘要→只留依赖锁算出的那一份**（过期副本约 5.59 GB）。
- **保留口径**：本地只有**一个权威**（当前制品 + 当前环境），其余可从 git/重建恢复。
- **验证**：收口后重跑发布门禁 + 真实调用（提供者能力）确认没删坏。

## 尾巴 2：空转项扫描（55/57/58 号一直没产出独立清单）

- **问题**："空转的直接杀掉"是既定哲学，但一直没有**独立清单**；目前只有文件式运行态清点覆盖了一部分。
- **口径（先定死，避免越扫越宽）**：空转 = ① 已登记能力既无正向验证目标、也无任何调用方（能力调用图）
  ② 定时/常驻任务长期无产出 ③ 测试/文档引用了已不存在的目标。
- **落点**：`开发工具/统一能力入口/`、`能力目录/`、`测试中心/`、定时任务与 launchd 注册项。
- **批次**：① 只读出清单（数量 + 逐条落点）② 逐条人工核对（区分"真空转"与"合法门面层/外部入口"）
  ③ 批量收口（删除或补正向验证）④ 回归。
- **验收口径**：清单落盘 + 收口后六环/组件合规/发布门禁仍全绿。

## 尾巴 3：热路径"忽略"→"显式失败"逐点评审

- **问题**：全平台 20 处 `except Exception: pass` 已改为**留痕**（行为不变、失败可见），但其中热路径上
  若有"本该失败却被忽略"的点，仍会静默降级。
- **落点**：`公共契约/诊断/忽略记录.py` 的调用点（`公共契约/句柄体系.py`、`运行核心/权威状态.py`、
  `运行核心/租约管理.py`、`运行核心/统一网关/流式HTTP.py`、`运行核心/加载器/*`、`平台控制面/提供者/*` 等）。
- **批次**：① 逐点判定"真该忽略（清理/降级）"还是"该显式失败"② 该失败的点改成显式失败并补错误码
  ③ 保留忽略的点写清理由 ④ 定向测试 + 受影响波次回归。
- **验收口径**：判定表落盘 + 定向测试通过 + 受影响波次通过 + 涉及运行时改动的做真实 HTTP 冒烟。
