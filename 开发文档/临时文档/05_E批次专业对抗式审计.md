# E 批次 · 专业对抗式审计（2026-09-03）

> 审计方式：不再找"看得见的 bug"，改为**独立复算 + 对抗验证**：文档数字 vs 磁盘真实、可导入性 vs 语法检查、调用腿唯一性、假绿统计、安全信号。
> 原则：每条发现必须有 path:line 证据与复现命令；只读审计，不改任何代码。

## 审计基线（独立复算全部通过，先确认可信部分）

| 声称 | 独立复算 | 结论 |
|---|---|---|
| 92 正式包 | 构建索引 = 80 支持库 + 12 模块库 = 92 | ✅ 一致 |
| 408 能力 owner 唯一 | 能力所有者 408 条、冲突 0 | ✅ 一致 |
| 完整性摘要对齐 | 98 摘要 × 2081 文件条目全 sha256 一致 | ✅ 真实 |
| HTML 黑盒 408/408 | 最新证据 08:26：424 场景/545 步/0 失败/并发32/资源残留0 | ✅ 真实 |
| 激活制品 54d1f7f45960099c | 当前.json 版本45 一致 | ✅ 一致 |

## 发现清单

### E1【重要】僵尸测试 ×3：模块级 import 必然失败，声称"全量 76 模块 0 失败"从未包含它们

**证据（复现命令，均 FAILED errors=1）：**

```bash
PYTHONPATH=. python3.14 -m unittest 测试中心.MCP工具箱.测试_角色门面        # ImportError 相对导入无父包
PYTHONPATH=. python3.14 -m unittest 测试中心.MCP工具箱.测试_角色权限矩阵    # ImportError 相对导入无父包
PYTHONPATH=. python3.14 -m unittest 测试中心.支持库.测试_示例提供者         # ModuleNotFoundError: 示例提供者
```

根因：
- `测试_角色门面.py:21` / `测试_角色权限矩阵.py`：用 `importlib.util.spec_from_file_location` 直接加载 `MCP工具箱/项目服务.py`，但 `项目服务.py` 内部用相对导入（`from .工具名映射 import ...`），脱离包上下文加载必然失败。08-18 单网关改造（700448b）后遗留，08-28 备份提交（222b059）仍在。
- `测试_示例提供者.py:10`：`from 示例提供者 import 示例操作`，但全项目**不存在** `示例提供者` 包。该文件由 **D 轮提交 84f2b58（2026-09-03 05:25）新建**，D 轮声称"测试 9/9 通过"，但此文件从未能运行——D 轮自己引入了僵尸测试。

影响：
- "全量测试：76 个测试模块 0 失败"（未完成事项.md:12、04_D批次补充审计.md:44）是**选择性统计**——157 个 `测试_*.py` 中 3 个必然失败的文件从未被包含，数字不可复现（按任何口径数不出 76：157 全量 / 131 排除历史阶段 / 129 现行无连字符）。
- `python_docx提供者` / `python_pptx提供者` 缺少真实可运行的专属测试覆盖（现有同名文件见 E2）。

### E2【重要】连字符测试文件 ×2：命名违反 Python 标识符规则，`import` 语句必然 SyntaxError

- `测试中心/支持库/测试_python-docx提供者.py`
- `测试中心/支持库/测试_python-pptx提供者.py`

证据：`python3.14 -c "import 测试中心.支持库.测试_python-docx提供者"` → `SyntaxError: invalid syntax`（模块名 `python-docx提供者` 中 `-` 被解析为减号）。

修正说明：`python3.14 -m unittest 测试中心.支持库.测试_python-docx提供者` 能跑（9 tests OK），因 unittest 走文件系统字符串加载；但任何静态 `import` 语句、AST 工具、或把它写进另一个模块的 import 都会失败。属命名违规 + 可加载性脆弱，且两个文件内容 import 的是 `python_docx提供者`（下划线）而**文件名是连字符**——命名与内容不一致。

### E3【建议】正式源码文件数口径漂移：文档 525 vs 防火墙 523 vs 磁盘 526

- 未完成事项.md:9 / 项目说明.md:13 声称"525 个正式 Python 文件语法通过"；
- 依赖防火墙实跑 `审计文件数=523`（排除 3 个：见其跳过规则——废弃包、验证器目录等）；
- 磁盘排除测试/示例/开发文档后 = **526**；
- git ls-files 全仓 .py = 823（含测试中心 164、开发文档/源码参考研究等）。
- 无任何代码/文档定义 "525" 的精确口径，三个数字互相矛盾（差 1~3 个文件）。文档数字应可复现。

### E4【建议】D 轮收尾不干净：文档改动未提交 + 临时文档未跟踪

- `开发文档/未完成事项.md`、`开发文档/项目说明.md` 已修改**未提交**（内容声称 HEAD `a8ceed8`、制品 `54d1f7f45960099c`，但 git 工作区有 diff）；
- `开发文档/临时文档/04_D批次补充审计.md` 是**未跟踪文件**（D 轮工作台从未 git add/commit），违反"临时文档问题修复后删除或收口"规则——D 轮"全部收口"却把工作台留在工作树。

### E5【观察】`trust_remote_code=True` 加载本地模型

- `支持库/适配层/模型服务.py:29,31`：`AutoTokenizer.from_pretrained(..., trust_remote_code=True)`。
- 本地路径参数 + 脚本独立运行（非 HTTP 输入），风险低；但若模型路径来源被外部控制，会执行模型仓库内任意代码。建议模型路径白名单或环境变量限定。

### E6【观察】except pass 215 处（正式代码 67 文件）

抽查句柄体系.py / 统一入口.py / 权威状态.py / 制品进程.py：绝大多数是**进程清理/连接关闭的防御性吞异常**（kill 已退出进程、关已关连接），语义可接受。无证据表明业务错误被静默吞掉。维持 C04 判定：设计意图，不阻断。

## 已排除（对抗验证通过，非问题）

- 唯一调用腿：模块/支持库门面统一经 `调用能力`/`连接器`/`获取能力调用器()`，未发现业务代码旁路直达注册表/Provider。
- 依赖防火墙：0 违规（523 文件）。
- 密钥硬编码：正式代码 0 命中。
- 动态导入绕过：115 处命中均为标准库合理用法（importlib/__import__("hashlib") 等），无业务绕过。
- 空测试体 / assert True：0。
- 测试中心空目录：无（此前误报，实际仅含 __pycache__）。

## 修复状态（E 批次执行后更新）

| 编号 | 修复动作 | 验证 |
|---|---|---|
| E1 | 角色门面/角色权限矩阵改为包导入（`from MCP工具箱 import 项目服务/角色权限`）；误放模板产物 `测试_示例提供者.py` 删除（真实覆盖在 `测试_支持库模板生成器.py`） | 角色 16/16 OK；全量可导入复扫 156/156 |
| E2 | 两个连字符测试改名 `python_docx`/`python_pptx`，同步反向破坏门禁28 白名单 | 改名后 20/20 OK；反向门禁 8/8 OK |
| E3 | 文档口径修正为 526（写明排除范围） | 全量 py_compile 退出码 0 |
| E4 | 文档改动 + 04/05 工作台统一提交收口 | git 干净 |

## 结论

底座核心可信度高：完整性摘要、能力索引、HTML 黑盒、发布门禁全部真实可复现。**真正的问题是"测试统计口径失真"**——僵尸测试 ×3 + 连字符命名 ×2 长期存在于"全量 0 失败/语法全通过"的声称之外，其中 1 个还是 D 轮自己引入的。这会让后续审计对"测试全绿"产生虚假信心。E 批次已将全部 5 项修复，测试文件恢复 100% 可导入。

验证记录：全部复现命令如上；E 批次仅修改测试文件命名/导入方式、删除误放测试、更新文档数字，未触碰任何正式源码与制品。
