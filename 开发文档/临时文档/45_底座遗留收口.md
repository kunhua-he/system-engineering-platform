# 45 · 底座遗留收口（华哥外出窗口 2 小时）

## 阶段一：开始前

### 本批目标

华哥指令：**今天把底座问题全搞好，保持干净整洁；没完成的就全完成掉；拿不准的保留、后面问。** 上一批（提交 `fb2929a`）已收口 B8 收口指纹 + 文件级占用租约 + 热加载 + 依赖防火墙剪枝。

本批处理《未完成事项.md》里**不需要华哥拍板**的底座遗留项。

### 在办清单

| # | 项 | 性质 | 依据 |
|---|----|------|------|
| 1 | 三个开发工具脚本**直接执行**报 `ModuleNotFoundError`（缺项目根 `sys.path` 初始化） | 真 bug，可修 | 未完成事项 P1 |
| 2 | `发送请求` 遇 3xx **未回传 `Location`**，调用方无法经唯一入口逐跳跟随重定向 | 底座正确性缺口 | 未完成事项 P2 ① |
| 3 | LLM 协议**短值 `chat`/`res` 与长值 `chat_completions`/`codex_responses`** 未统一裁决 | 底座一致性 | 未完成事项 P2 ② |
| 4 | **写入通道**（机制二前置）：把 `文件系统支持库.文本补丁.应用精确替换` 的「预期文件摘要」乐观锁接到 MCP | 新能力，机制二前置 | 规约 §五 第 4 行 |
| 5 | **长工具转后台作业**（提交作业 + 轮询 + 可取回 + 可取消） | 新能力，根治管理端排队 | 规约 §五 第 6 行 |
| 6 | `工程缓存/协作状态/` 历史残留 JSON 清理 | 整洁 | 未完成事项 P3 |
| 7 | 验证账本落后源码（锚 `f05477c` ≠ 当前 HEAD） | 按发布门禁补录 | 未完成事项 P2（对账 R1） |

### 明确**保留、不动**（等华哥或等别的会话）

| 项 | 为什么不动 |
|----|-----------|
| 个人蒸馏支持库.查询上下文 六环缺 1 项 | 三选一涉架构，**待华哥拍板** |
| 制品重编译（R1/M7） | 影响运行态、并发期风险高，**明示缓办** |
| 甲/乙 占位命名 75 文件 | 是否清理**待华哥定口径** |
| 制品指针语义（三份 `当前.json`） | 需先定「谁写、写哪份」= **决策** |
| `重试调度` 参数名 `错误码白名单` | 改名是破坏性契约变更，**定稿前不动** |
| `支持库/适配层/YAML提供者/完整性摘要.json` | **等另一会话**自行收口 |
| 六环门禁 2 项缺口（语法索引 / 轻量文本解析） | **另一会话在途包**，并行补易撞车 |
| 另一会话报的 4 项（V7 网关配置管理 / 连接池 / 能力注册表 / git_sync_workflow） | **卡在华哥决策**上，且属该会话工作流 |

### 已知的跨会话事实（影响处置）

- `工程缓存/` 在 `.gitignore` 内（`工程缓存/` 整目录忽略）→ 里面的 `能力占用/*.json`（22 条）与 `协作状态/*.json` **本地生效、不入库**，不影响底座 git 状态。
- **`工程缓存/能力占用/` 里那 22 条是另一会话刚补登的**（表格公式 9、轻量文本解析 2、语法索引 3、浏览器自动化 6 等）→ **属有效登记，不得当残留清理**。
- 本仓有多会话并发提交：另一会话已提交 `23d69d0` / `24521b6` / `a7992f8`（模型HTTP提供者生成参数），本批在其上。

### 影响范围

**第 4 项（写入通道）落地设计**（2026-09-11，动手前记录）：

- 新增 `MCP工具箱/文件补丁.py`：`应用文件补丁(项目根, 存储目录, *, 文件路径, 旧文本, 新文本, 预期文件摘要="", 预期旧文本摘要="", 起始行=None, 结束行=None, 写入=True, 开工id="")`。
- **写盘不重写**：委托底座原子能力 `文件系统支持库.文本补丁.应用精确替换`（唯一匹配校验 + 「预期文件摘要」比对 + 临时文件 + `os.replace` 原子写 + 差异统计），本模块只做三件事——路径归一化与越界拒绝、活跃文件租约核对、结果整形。
- **不许硬闯**：写前查活跃文件租约（复用 `文件租约.查询文件占用`），若该文件被**别的开工id**持有则拒绝并回带占用者与占用开始时间；同一开工id 放行。只读预览（`写入=false`）不查租约。
- 一处底座补强：`开发工具/开发入口.py::真实调用能力` 增加回传 `详情`（原只回传 成功/值/错误码/说明/证据链）。否则 `应用精确替换` 失败时 `结果.详细信息` 里的 `当前文件摘要` 被丢掉，违反规约第 5 行「对不上即拒绝并回带磁盘当前摘要」。纯增字段，现仅 `开发入口.py` 自身 CLI 调用，无破坏面。
- 入口：`MCP工具箱/项目服务.py` 新增 Tool `apply_file_patch` + 中文映射 `应用文件补丁` + 分派分支。**新增工具必须重启 8766 才生效**——按重启纪律凑批，不为此单独重启。
- 测试：`测试中心/MCP工具箱/测试_文件补丁.py`（真实临时文件，覆盖 成功写 / 摘要不符带回当前摘要 / 路径越界 / 只读预览不落盘 / 异开工id 占用拒绝 / 同开工id 放行）。

### 回退路径

- 逐项独立：（1）（2）（3）（6）（7）均为小改，可按提交粒度回退；新能力（4）（5）为纯新增，回退 = 删除新文件 + 还原入口新增分支。
- 不新增表、不改版本、不引入框架。

## 阶段二：完成后追加（2026-09-11 收口）

### 实际结果

| # | 项 | 结果 | 提交 |
|---|----|------|------|
| 1 | 开发工具脚本直接执行 | ✅ 六个脚本（原清单 3 个 + 连带 3 个）统一补项目根 `sys.path`；`-m` 与直接执行两条路都可跑 | `84fc7d1` |
| 2 | `发送请求` 3xx 回传响应头 | ✅ `HTTPError` 分支改 `结果.失败` 并回传 状态码 + 完整响应头（含 `Location`）；失败态不再伪装成功，5xx 标可重试。回归 `测试_网络请求` 3/3 | `bd71678` |
| 3 | LLM 协议短值/长值 | ✅ 适配层镜像连接器别名表，非法协议在校验前拒绝；`测试_模型协议别名` 4/4 且断言两表一致防漂移。受影响波次 54/54 | `bd71678` |
| 4 | 写入通道（机制二前置） | ✅ 新增 `apply_file_patch`，委托底座能力 `文本补丁.应用精确替换`，写前带「预期文件摘要」+ 核对他人文件租约。`测试_文件补丁` 9/9；并用真实 `调用工具` 预览模式跑通整条分派链 | `bf66a48` |
| 5 | 长工具转后台作业 | ⛔ **未做**——属新能力，需先定「挂哪一层（8766 管理端 / 40007 网关）+ 重启语义」，**待华哥拍板** | — |
| 6 | `工程缓存/协作状态/` 残留清理 | ⬜ **未动**——`能力占用/` 那 22 条经复核为另一会话补登的**有效登记**，不得当残留清；`协作状态/` 21 条属他人任务记录，并发期不删 | — |
| 7 | 验证账本 R1 补录 | ⬜ **未动**——需跑发布门禁，属编译产物/制品仓库**独占**资源，另一会话在途时按避让表缓办 | — |

### 额外收口（本批顺带，未在原清单）

- **全仓 115 个正式包完整性摘要闭合**（`f831482`）：头部多处聚合摘要在源文件改动后未回算，全树报「清单不闭合」——网络通信支持库及请求（改 `网络请求.py` 连带）、大语言模型支持库（`a155e4e` 改模型连接器未回算父级，7 处哈希不符）、YAML提供者（清单多记自身与已删除的 `验证场景引用.json`）、办公文档支持库（未登记轻量文本解析三件）。按生成器逐个重算，扫描结果与磁盘全等；属另一会话在途源码的 5 处**只重算不提交**，随其源码一起收口。
- **债务清单收口**（`b38b30b`）：删除三项已消除条目（发送请求 3xx / LLM 协议短值、三个开发工具脚本、YAML提供者摘要陈旧 + 办公文档父级聚合未登记），结论并入「已核实通过」。
- **补记一条 P1 缺口**：`claim_capability` 实为 `工程缓存/能力占用/*.json` **永久锁**——无释放、无过期、判据非原子（TOCTOU），与规约第 19 行「能力级占用租约（原子互斥）」口径不符。因另一会话此刻正持 14 条在途占用，**未改机制**，附一次到位的建议改法待拍板；规约工具表与自检表已同步如实标注。

### 调用方式（写入通道）

工具名 `apply_file_patch`（中文名 `应用文件补丁`，`工具名映射.py` 已登记）。

- 必填：`文件路径`（仓库相对路径，或落在项目根内的绝对路径）、`旧文本`、`新文本`
- 可选：`预期文件摘要`（磁盘当前内容 sha256，不传则不校验）、`预期旧文本摘要`、`起始行`、`结束行`、`写入`（默认 `true`；`false` 只预览不落盘）、`开工id`（默认取当前开工上下文）
- 返回：`成功` / `错误码` / `说明` / `文件路径` / `值`（含 差异、新文件摘要、增减行数）/ `详情`（失败时带 `当前文件摘要` 供重读）/ `证据链`
- 失败码：`文件摘要不符`（文件已被改动）、`FILE_LEASE_CONFLICT`（被别的开工id 占租约）、`路径越界`、`文件不存在`、`参数不合法`

### ⚠️ 生效条件（本次未重启）

新增 MCP 工具需**重启 8766 管理端**才在工具表可见（工具增删与参数表变更不在热加载范围内）。本次**未重启**：`当前开工id` 是 8766 进程内的全局量，重启会清掉另一会话的开工上下文（`尚未建立开工上下文`），属共享态操作，按「拿不准就保留、后面问」留待华哥择时。代码与测试均已入库，重启后即可用。

### 本批验证证据

- `测试中心.MCP工具箱` 受影响模块：测试_项目服务 15/15、测试_文件补丁 9/9、测试_文件租约 10/10、测试_支持库协作 8/8、测试_协作状态 14/14、测试_验证门禁 25/25、测试_能力网关 8/8 —— 合计 89/89。
- `测试中心.支持库`：测试_网络请求 3/3、测试_模型协议别名 4/4（见「已核实通过」）。
- 全仓 115 正式包摘要闭合校验：不闭合 0。