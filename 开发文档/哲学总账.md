# 哲学总账（逐条：判据 → 强制点 → 反向试验 → 状态）

> 最后更新：2026-09-22
> 口径：实测（数字随建包与提交逐日变化，引用前必须重跑取证命令）
> 维护者：强制情况登记

> 2026-09-17 建账 ｜ 华哥要求：「哲学总账的话，我觉得也可以列出来，做干净他也很好。**只要保持唯一性就好**」
> **唯一事实源声明**：条款原文的唯一真源是 `开发文档/项目说明.md` 的《设计哲学》段（第 1–14 条）。
> **条数口径也只在那里声明一处**：本文件引用条数时只写「现行主条 / 子项数」，不另行定义范围。
> 本文件**只登记"强制情况"**，不重述条款原文 —— 原文变了，本文件失效重算。
> 画法：缩进式，不依赖右边界对齐。
> **统计口径与重测绘时机（2026-09-17 定）**：三态判定的统一口径 = ① **✅机器强制**＝有判据模块**且**被必经路径（发布门禁 / 编译器期）真实调用；
> ② **🟡半强制**＝有判据、门禁未调用；③ **🔵人工**＝无机器判据但有明确强制者；④ **⚠️纸面**＝两者皆无。
> **本轮（吸收 P-1~P-8、废止 1.5、新增第 13 条）只做条目级同步；四态统计留待批 1 接线后一次性重测绘**——
> 接线会让 🟡 成片转 ✅，接线前重算的数第二天就过期。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


---

## 一、总账怎么读（状态四态，永不共用一个词）

```
✅ 机器强制
     有判据模块 + 已在发布门禁（不可绕过）里真实调用
     判据：把违规改进去 → 发布门禁必须报红

🟡 半强制
     有判据模块，但门禁未接线 / 只报告不阻断 / 只在单元测试里
     判据：能指出"缺哪一步接线"

🔵 人工强制
     流程纪律，无机器判据（如"发现即记录""审计分三轮"）
     不判为缺陷，但**必须写明"谁在强制"** —— 否则等同纸面

⚠️ 纸面规则
     既无机器判据、也无明确人工强制点
     ⇒ **这是本总账要消灭的东西**（对应拟新增哲学第 13 条 P-3）
```

**读法**：只看"⚠️"那段就能知道还差什么。其余是留档。

---

## 一之二、发布门禁的**真实强制面**（权威清单，2026-09-17 实测）

```
口径：只看 `^\s*(from|import)` 真实导入，不看字面关键词出现
      （教训：发布门禁里"反向破坏""确定性""测试体系门禁"都出现过，
        但**全是帮助/枚举文本**，零调用 —— 只看字面会得出完全相反的结论）

发布门禁真实 import 的自家模块：31 个
  门禁/检查器类（真正干"拦"这件事的）：★ 只有 5 个
     ① 开发工具.依赖生命周期审计.审计核心
     ② 开发工具.公开调用完整性门禁
     ③ 开发工具.发布门禁.权威状态门禁
     ④ 开发工具.发布门禁.第十四阶段门禁
     ⑤ 开发工具.测试伪装门禁
  编译/校验类
     开发工具.契约编译.漂移检测 / 聚合契约解析 / 能力定义编译器
     开发工具.项目编译(+项目编译器) / 开发工具.组件合规.合规测试包
     支持库.后端.组件规范支持库（校验完整性摘要）
     项目适配层.依赖锁定.锁定校验
  运行期类
     运行核心.依赖防火墙 / 运行环境管理器.提供者生命周期
     运行核心.加载器.版本系统.版本注册表 / 热切换 / 提供者隔离.独立进程
     运行核心.统一网关.安全边界 / 限流器 / 流式HTTP
     运行核心.运行诊断.诊断中心.失败记录 / 诊断复现
     运行核心.任务调度.任务进程 / 后端核心.后端核心
     公共契约.运行时（有界IO / 运行缓存 / 平台适配）
     支持库.前端.浏览器宿主 / 开发工具.HTML验证.环境依赖 / 支持库.适配层

★ 明确**不在**强制面的（有实现、有单测、但门禁不调用）：
     · 开发工具/契约编译/对外契约变更判定.py   → 哲学 5.3 的判据
     · 开发工具/复用审计/能力调用图审计.py     → 哲学 1.3 / 8.2 的判据
     · 开发工具/复用审计/反向破坏/破坏验证.py   → 哲学 1.4 的反向证明工具
     · 开发工具/测试体系门禁.py                → 只在帮助文本里被提及
     · 公共契约/版本规则/契约版本.py            → 哲学 5.1 的判据

★★ **2026-09-18 接线进度（父会话核验，按真实 import 判定，不看字面）**：
     已接进发布门禁（本批新增）：
       ✅ 开发工具/契约编译/漂移检测（全面漂移，含基线外新增强制项）
       ✅ 开发工具/能力id冻结基线门禁（能力id只增不改）
       ✅ 防回潮四项（裸布尔 / 非正式类型名 / 英文错误码 / 占位代称）
       ✅ 消费者登记环（公开调用完整性门禁的消费者登记环）
      ★★ **2026-09-18 全部接完（卡点 B 4/4）**：
       ✅ 开发工具/复用审计/能力调用图审计.py  → 门禁项「防回潮·能力调用图」
          （审计 61 份模块库源码、违规 0；反验：故意直连→红且精确报出文件:行号）
       ✅ 开发工具/复用审计/反向破坏/破坏验证.py → 门禁项「防回潮·反向破坏」
          （破坏场景 13/13 + 恒真真对照通过；**耗时实测 2.00 秒**，预算 120 秒）
       ✅ 开发工具/契约编译/对外契约变更判定.py → 门禁项「防回潮·对外契约变更」
          （基准=激活制品内同名包，判据面 134 包／677 能力、耗时 0.07 秒；
           **§上线即抓出主会话一处漏**：补错误码未递增版本 → 报「要求递增 3 包」→ 已按 5.3 修）
       ✅ 开发工具/测试体系门禁.py → 门禁项「测试体系门禁」
          （执行级别=快速级、耗时 5.6 秒、206 个测试文件全量可导入性校验；
           **未执行全量真跑时会显式声明「本轮未执行全量测试」**，不静默）

      ⇒ **卡点 B 收口**：哲学判据这一层从「几乎全在门禁之外」变为**已接线四类真判据**。
        发布门禁的「通过」现在同时意味着：契约没漂、能力没少、没回潮、破坏检测器不恒真、
        对外契约变更由版本承载、测试资产可导入且零跳过。
```

**⇒ 结论：底座有 5 个真门禁 + 一批编译校验件；而"哲学判据"这一层几乎全在门禁之外。**
**这就是卡点 B 的精确形状 —— 不是"没有判据"，是"判据没接到拦人的位置上"。**

---

## 二、总账速览

```
条款结构（**以 `开发文档/项目说明.md`《设计哲学》段为准，本文件不另行定义条数**）
  上限：主条 ≤14、子项 ≤60（12.4②：2026-09-18 因新增 12.3⑧⑨ 上调 56→60，上调必须同批写明理由）
  在限条数：见 `项目说明.md` 的《设计哲学》段（该处给取证命令，本文件不复述数字）

★ 四态统计：**待批 1 接线后一次性重测绘**（口径见第一节引言）
```

**⇒ 当前唯一可以确定的一句话：条款结构与额度已收口（在限内，在限条数以 `项目说明.md` 为准）；四态分布以批 1 接线后的重测绘为准。**

## 三、逐条总账

### 第 1 条 干净底座与唯一化

```
1.1 干净底座优先
     判据    无独立判据（是决策原则，不是可判定规则）
     强制点  🔵 人工（每次遇到问题第一问；由审计与复盘强制）
     状态    人工强制

1.2 不保留旧腿
     判据    `测试中心/.../测试_能力唯一事实源.py`（能力唯一事实源）
     强制点  🟡 只在该单元测试里；**发布门禁未接线**
     证据    `rg -n '不保留' 开发工具/发布门禁/运行发布门禁.py` → 零命中
     状态    半强制 ← 应接线

1.3 结果唯一即收口
     判据    `开发工具/复用审计/能力调用图审计.py` + `提供者直连规则.py`（实现存在）
     强制点  🟡 **发布门禁未调用**（★ 自我更正：我上一版据"关键词命中"标成 ✅，是错的）
     证据    `rg -n '^\s*(from|import)' 开发工具/发布门禁/运行发布门禁.py` → 31 个自家模块里
             **无 `开发工具.复用审计.能力调用图审计`**；
             `rg -c '能力调用图|复用审计' 发布门禁` → 0 次
     状态    半强制 ← 应接线

1.4 空转即杀
     判据    `开发工具/复用审计/反向破坏/破坏验证.py`（实现存在）+ `测试_反向破坏门禁28.py`
     强制点  🟡 **发布门禁未调用**（★ 自我更正：发布门禁里"反向破坏"仅出现 1 次且是
             **包名清单字符串**（第 1862 行的组件枚举），不是调用 —— 又一个"字面≠调用"）
     证据    `rg -c '破坏验证' 发布门禁` → 0 次；`rg -n '反向破坏' 发布门禁` → 仅 1862 行枚举
     状态    半强制 ← 应接线
     ★ 缺口  只查"门禁恒真/恒空"，**不查"豁免空转"** —— 见第 4 层改法（tach 机制）

1.6 临时脚本不留库
     判据    无（可用 AST/文件名扫描实现，但当前没有）
     强制点  ⚠️ 无
     证据    `rg -n '临时脚本' 开发工具/ 测试中心/` → 零命中
     状态    纸面规则 ← 可低成本机器化（扫 `*.sh`/`探针*.py` 在仓库根等）

1.7 成品与制品不做本地归档，旧版直接删
     判据    文档历史：仓库中只存在一个顶层 `开发文档/归档/`（不得出现第二个归档库）
             成品与制品：本机不保留旧制品/安装包
     强制点  ⚠️ 无
     状态    人工强制 ← 口径已按 2026-09-17 裁决澄清（成品与文档历史分开）
```

1.8 例外必须自证（2026-09-17 新增，外部借鉴 P-7）
     判据    `运行核心/依赖防火墙.py::文件级豁免表`（现为 `{路径: 可依赖层}`，**无理由字段**）
     强制点  🟡「无理由豁免判违规」**尚未实现**；存量基线 271 项 / 23 项同理无理由
     状态    半强制 ← 应接线
     ★ 补（2026-09-17，外部 tach 实证）：**豁免/规则的默认档要分级** ——
       「能机器判定的默认判红（error）、需人判断的默认只报（off）」，
       并把「未被使用的豁免」本身也判红（没被用到的豁免＝陈旧豁免）。
       对照：哲学 13.2 的五态中「只报告」档就是这条的落地形态。

### 第 2 条 分层与依赖归属

```
2.1 分层与归属固定
     判据    `运行核心/依赖防火墙.py`（AST 静态审计）
     强制点  ✅ 编译器期强制
     状态    机器强制

2.2 依赖分两类，且必须本地具备
     判据    `开发工具/依赖生命周期审计/审计核心.py` + 32 份 `依赖锁.json`
     强制点  ✅ 发布门禁命中（"依赖声明/依赖锁/环境依赖"）+ 环境自检
     状态    机器强制

2.3 依赖位置分两段，不新开并列顶层
     判据    依赖防火墙层表（`_所在包是内部层`）
     强制点  ✅ 编译器期（层表校验）
     状态    机器强制
     ★ 待补  层表未含 `平台控制面`（已在项目说明落地记录登记过）

2.4 翻译器原则（内外之间只留一种形态）
     判据    网关归一化（中文值 → 内部形态）有实现
     强制点  🟡 有实现与测试，**发布门禁未接线**（无"网关外不得出现中文值判断"这类检查）
     状态    半强制 ← 应接线
```

### 第 3 条 失败与报错必须留痕

```
3.1 同一件事重复报错两次必须停下
     判据    无（是行为纪律）
     强制点  🔵 人工（由审计复盘强制：查"是否原地重试"）
     状态    人工强制

3.2 上游失败即明确报错，各修各的
     判据    错误信封 + 错误码中文（网关层已强制）
     强制点  🔵 人工（上游侧表现依赖人工判断）
     状态    人工强制（但错误信封本身是机器强制）

3.3 报错必须回填说明文档（一次命中）
     判据    无（文档质量是人工判断）
     强制点  🔵 人工（公开调用完整性门禁提供部分支撑）
     状态    人工强制
```

### 第 4 条 发现即记录

```
4.1 发现即记录，与「当下修不修」无关
4.2 落痕按类归口，不新开事实源
4.3 必须带现场证据
4.4 记录不等于立刻修
     判据    无（纪律条款）
     强制点  🔵 人工（由审计轮次强制：第 12 条 12.1/12.2 跑的时候顺带查）
     状态    四条均人工强制
     ★ 说明  这四条天然不该机器化 —— 但按 P-3 必须写明"谁在强制"：
             **强制者 = 审计流程（第 12 条）；证据 = 每轮审计的落盘清单**

4.5 方案文档本身也归口（2026-09-18 华哥新增）
     判据    `开发文档/规范/方案文档生命周期判据.json`（规范文本的机器投影）
             + 能力 `支持库/后端/项目文档支持库.校验集合判据`
             （`支持库/后端/项目文档支持库/实现/集合校验.py::校验集合判据`）
     强制点  ✅ **发布门禁已接线**：门禁项「防回潮·文档生命周期」
             （`开发工具/发布门禁/运行发布门禁.py` 基线 `"文档生命周期": {"态": "干净通过", "存量基线": 0, "阻断": 真}`
             + 防回潮调用表接入 `校验文档生命周期防回潮`）
     状态    机器强制
     证据    `rg -n '文档生命周期' 开发工具/发布门禁/运行发布门禁.py` → 基线项 + 调用表双命中；
             已判定三条：编排唯一 / 状态声明合法 / 引用可达；
             未机器化三条（隐形债 / 主题重叠 / 同数不同值）在判据文件 `暂未机器化` 里显式声明不判
```

### 第 5 条 契约与版本口径

```
5.1 契约唯一，组件各自迭代版本
     判据    `公共契约/版本规则/契约版本.py`（唯一事实源）+ `测试_契约版本唯一.py`
     强制点  🟡 只在单元测试；发布门禁未接线
     状态    半强制 ← 应接线
     证据    `rg -n '契约版本' 开发工具/发布门禁/运行发布门禁.py` → 零命中

5.2 版本不对齐默认不失败，只有强制参数才失败
     判据    `运行核心/加载器/版本系统/契约兼容.py`；`测试_网关兼容性.py`
     强制点  🟡 只在单元测试
     状态    半强制 ← 应接线

5.3 ★★★ 版本递增的判据＝对外契约指纹变化
     判据    `开发工具/契约编译/对外契约变更判定.py`（44KB，实现完整）
             + `公共契约/能力契约/契约指纹.py::契约指纹()`
     强制点  ✗ **发布门禁零命中**（2026-09-17 实测确认）
     证据    `rg -n '契约变更|对外契约' 开发工具/发布门禁/运行发布门禁.py` → 零命中
             `rg -n '对外契约变更判定' --glob '*.py'` → 只命中 `测试中心/开发工具/测试_对外契约变更判定.py`
     状态    ★★ **半强制（最要紧的一条）** —— 判据齐全、单元测试齐全、但**没有任何
             强制点**：改契约不递增版本，发布门禁照样放行
     ⇒ 这就是外部审计说的"契约变化必须升级版本的判定模块没有接入正式发布门禁"，**属实**
     ＋ 同日补「契约兼容性分档」（P-1）：兼容 / 破坏 / 冻结 三档，判据复用同一个 `契约指纹()`
       —— 与 5.3 同条目承载，**不新增子项编号**（原 5.4 已于更早一次合并进 5.3，编号不复用）
```

### 第 6 条 门槛与命名

```
6.1 阈值不设硬门禁
     判据    反向：仓库里**不应**存在行数阈值门禁
     强制点  🔵 人工（靠代码评审；也可机器化为"扫到行数阈值门禁即报红"）
     状态    人工强制

6.2 命名一眼能懂
     判据    `测试伪装门禁`（占位/桩检测） + `公开调用完整性门禁`
     强制点  ✅ 发布门禁命中（"占位/命名"）
     状态    机器强制
     ★ 补充  2026-09-17 全仓清理天干代称（子代理 46 处真违规 → A/B/C），
             并已修一处改名撞到的顺序断言
     ★ 补充  2026-09-18 「英文只留最底层一处」并入本条（6.2 第二段）：
             判据＝本仓自有面上新写代码不得直连英文入口（样板 `公共契约/基础类型/数据类.py`）；
             第三方协议签名位置（`os.walk(topdown=…)` 类）不在本条约束内（防与 6.2 首段打架）
             强制点  🔵 人工（组件/文档评审）；后续可机器化为"新文件里 `from dataclasses import` 类直连"扫描
             状态    人工强制 ← 可机器化
```

### 第 7 条 编译与验证铁律

```
7.1 编译器绝对禁止跑全量
     判据    `开发工具/契约编译/契约编译器.py` 只编小单元
     强制点  ✅ 编译器自身结构强制 + 发布门禁命中
     状态    机器强制

7.2 HTML 可以跑全量
     判据    发布门禁跑 HTML 黑盒（并发峰值 32）
     强制点  ✅ 发布门禁主判定
     状态    机器强制

7.3 后端怎么实现，HTML 不需要知道
     判据    黑盒只认 HTTP 返回
     强制点  ✅ 发布门禁主判定
     状态    机器强制

7.4 统一调用接口铁律
     判据    `公开调用完整性门禁`（23 项存量基线）+ 复用审计（提供者直连规则）
     强制点  ✅ 发布门禁已接入（"经 `运行公开调用门禁`"）
     状态    机器强制
     ★ 缺口  存量 23 项**无收敛期限** → 建议按 P-7 加"豁免带理由 + 空转检测"

7.5 测试中心总运行器已移除
     判据    仓库里不应存在总运行器
     强制点  ✅ 物理移除（结构强制）
     状态    机器强制（物理）

7.6 验证场景引用.json 是验证场景的输入来源
     判据    `开发工具/HTML验证/场景加载.py`（严格校验契约版本，旧格式直接阻断）
     强制点  🟡 加载器强制校验格式，但"每个包都必须有"未进发布门禁
     状态    半强制

7.7 能力不足就补底座，不许收窄约束
     判据    无（是决策原则）
     强制点  🔵 人工（由审计强制：查"是否放过收窄约束的补丁"）
     状态    人工强制

7.8 上层一律经 HTML/HTTP 请求底座执行
     判据    `公开调用完整性门禁` + HTML 黑盒
     强制点  ✅ 发布门禁命中
     状态    机器强制
```

7.9 生成物三律（2026-09-17 新增，外部借鉴 P-5）+ 派生优先于手写（2026-09-18 并入）
     判据    `开发工具.全量重算摘要`（已存在，能力上等价于 `verify-codegen`）
     强制点  🟡 **发布门禁未接线**；接线后判据 = 跑完生成器后 `git status` 必须干净
     状态    半强制 ← 应接线
     ★ 补充  2026-09-18 「派生优先于手写」并入本条：遇到需手写维护的规范/配置/清单/契约件，
             第一问是"能不能派生"；确实只能手写的必须写明为什么不能派生。
             判据    手写件旁找不到"不可派生"理由的，按可疑项复核
             强制点  🔵 人工（组件/文档评审）
             状态    人工强制 ← 可机器化
     ★ 补充  2026-09-20 「全项目 md 单一生产者」并入本条（华哥：全部 md 都要通过生成器写；
              人的参与方式是改生成器的**输入源**，不是经手文件）：
              ① 人是参与方（改输入源），不是被排除方；"留人工区"≠"人工区可以不走生成器"；
              ② 生成器自有字段（版本/返回结构/验证状态/错误码/参数列表/依赖能力/调用示例/入口）
                 的旧值＝漂移，必须按事实源刷掉，不得判成"人工改写"而保留
                 （例外仅限 `开发文档/项目证据/说明书白名单.json` 登记的人工类）；
              ③ 一处声明 → 派生所有呈现；同一件事两种表达按 1.3 收口；
              ④ `--写盘` 是唯一写入形态，无机器印记的 md 判为手写。
              判据    `python3.14 -m 开发工具.MD文档生成 --拒绝手写校验`
              强制点  ✅ 编译口「拒绝手写md」项（`开发工具/开发编译口/编译口.py:120`，必经路径）
              状态    机器强制（判据 + 必经路径均已存在）

### 第 8 条 能力唯一性与拼积木

```
8.1 借鉴机制逻辑，不照搬项目结构
     判据    无
     强制点  🔵 人工（由审计强制）
     状态    人工强制

8.2 能力唯一权威，杜绝旁路
     判据    `测试_能力唯一事实源.py` + `能力调用图审计.py`
     强制点  🟡 只在单元测试；发布门禁未命中"旁路/能力唯一"
     状态    半强制 ← 应接线

8.3 原子能力精而不杂
     判据    能力定义编译（输入/输出/存储格式/错误码必须齐）
     强制点  🟡 编译器期检查契约齐全，但"是否该拆/该合"无判据
     状态    半强制
```

### 第 9 条 底座 0 加密 0 限制

```
9.1 0 加密
     判据    无（是产品定位声明）
     强制点  🔵 人工
     状态    人工强制
9.2 0 限制
     判据    无（是产品定位）
     强制点  🔵 人工
     状态    人工强制
9.3 隐私/安全是上层业务的事
     判据    边界声明
     强制点  🟡 网关安全边界有实现
     状态    半强制
9.4 确定性按能力类别（2026-09-17 替换原「同样的输入 → 同样的输出」，外部借鉴 P-8）
     判据    四分类（纯确定性 / 可重放 / 外部观测 / 有副作用）声明在**能力说明与验证场景口径**里
     强制点  🟡 分类是验强分派依据，**尚无机器强制**；**本轮不动契约**（不新增 能力定义.json 字段）
     状态    半强制 ← 应接线
     ★ 收益  直接给出「742 个正向步骤里 264 步只验形状」的分派依据：
             外部观测类本来就验不了值（合理），其余该补关键值断言（真欠账）
9.5 相对安全，不做绝对安全
     判据    边界声明（华哥 2026-09-16 裁决）
     强制点  🟡 安全边界有实现，但"是否把相对安全包装成绝对"无判据
     状态    半强制
```

### 第 10 条 底座自我完备：缺什么补什么

```
10.1 先判定"是不是真的缺"        
10.2 补成原子能力，不是补成特例   判据    无（决策流程）
10.3 补进"对应的那个库"          强制点  🔵 人工（由审计强制）
10.4 全套齐活才算补完            状态    四条均人工强制
10.5 补完必须真实验收
     判据    HTML 黑盒（真实验收的机器形态）
     强制点  🟡 有机器形态，但"每次补完是否都跑了黑盒"无强制
     状态    半强制
```

### 第 11 条 环境与工具/架构准入判定

```
11.1 准入判定——「对比现状，是否更快、更稳定」
     判据    无（判断原则）
     强制点  🔵 人工（由审计强制）
     状态    人工强制
11.2 工具调用超过 5 秒即判原理，并改底层
     判据    无（判断原则；但已有机器载体：`开发工具/开发效率巡检.py` 的「单命令超 15 秒」硬门禁）
     强制点  🟡 部分机器（效率巡检能报耗时；「是否真去判原理并改底层」无人强制）
     状态    半强制
```

### 第 12 条 审计与维修分三轮

```
12.1 审计与维修分三轮
12.2 一次性盘清、一次性全派、回来统一验收
12.3 子代理单任务规模
12.4 哲学与文档空间的收敛纪律
     判据    无（流程纪律）
     强制点  🔵 人工（华哥亲自把关）
     状态    四条均人工强制
     ★ 说明  这是最高级别的强制点（华哥本人），不需要机器化；
             但应在总账里显式写明"强制者 = 华哥"
     ★ 补充  2026-09-18 12.1 新增第④条「第二轮证真必须用提交版本核验条目前提」
             （`git show HEAD:<文件>`，不读工作树——并行维修时工作树是中间态；
              前提不成立的条目当场删）。教训：按清单原样派 10 路，其中 1 路是过时条目。
             强制点  🔵 人工（主会话派活前自检）；后续可机器化为「债务清单条目必须先过证真」
             状态    人工强制 ← 可机器化
     ★ 补充  2026-09-18 12.3 新增第⑤条「任务信必须注入任务记忆包（开工ID + 已核实事实包）」
             （华哥原话：「不做什么限制，只是单纯的可以统一一个任务的记忆」）。
             落点与五段结构见 `规范/批量开发流程与分级验证.md` 第四节；
             缺任一项即任务信不合规、不得派出。
     ★ 补充  2026-09-18 12.1 新增第⑤条「条目落点必须现场写明，不许让执行者猜」：
             派活前必须把「改哪个文件/落哪个包/哪个 文件:行号」写进任务信；
             自问「执行者读完任务信，是否知道第一个要打开的文件路径」——答不出就是不完整。
             教训：一条写文档的小活因落点含糊，子代理为定位「是哪两个包」搜满 1800s、零产出。
             判定方式 人工（主会话派活前自检）
             强制点  🔵 人工；后续可机器化为「任务信格式检查」
             状态    人工强制 ← 可机器化
```

---

12.5 规则的破坏样本是接口义务（2026-09-17 新增，外部借鉴 P-6）
     判据    无（待建「取样() 接口义务」+ 元测试遍历）
     强制点  🔵 人工（由审计强制）
     状态    人工强制 ← 可机器化

### 第 13 条 规则的规则：三要素与门禁五态（2026-09-17 新增）

13.1 规则三要素
     判据    `开发工具/规则自检.py`（四类判据：第三栏存在 / 栏行自洽 / 逐条总账三要素 /
             本判据件被必经路径真实引用）
     强制点  ✅ 编译口门禁清单「规则自检」项（`开发工具/开发编译口/编译口.py`，必经路径）
             + 发布门禁「规则自检（13.1 三要素）」项（`开发工具/发布门禁/运行发布门禁.py` 第 31 段）
     反向试验 已实测（2026-09-22：`python3.14 -m 开发工具.验证_规则自检反向验证`
             八拍全通过 —— 七档弄坏均报红且点名准确，真仓库未弄坏为绿）
             —— 可复现的验证件即 `开发工具/验证_规则自检反向验证.py`，见「四之二」栏 13.1 行
     状态    机器强制（判据 + 两处必经路径均已存在）

13.2 门禁结论五态
     判据    无（待门禁按五态输出）
     强制点  🔵 人工；批 1 起新增项一律走「干净通过」，挂基线必须显式配成「债务冻结」
     状态    人工强制 ← 批 1 接线

13.3 规则四环链：规范 → 判据 → 能力 → 门禁（2026-09-18 新增）
     判据    无机器判据（四环是**归因口径**：沿四环各指认一个落点，指不出 ③ 的按纸面规则处理）
     强制点  🔵 人工（强制者 = 华哥把关 + 审计）；批 1 元检查接线后转 🟡
     状态    人工强制 ← 批 1 接线
     证据    `rg -n '规则三要素|四环' 开发工具/发布门禁/运行发布门禁.py` → 零命中（发布门禁无「规则自检」项）
     备注    与 13.1 的分界：13.1 问「判据在哪」，13.3 追问「判据由谁承载、谁能调用」

---

### 第 14 条 归因与校验的位置（2026-09-18 新增）

```
14.1 归因三分：先分类，再动手
     判据    无机器判据（是派活前的归因分类纪律）
     强制点  🔵 人工（主会话派活前自检）；后续可接进债务清单格式检查（条目必须带 归因 字段）
     状态    人工强制 ← 可机器化
     备注    实测样板：依赖锁空壳锁一例归为②架构错位，修法＝判据上移公共契约 + 模板不造空锁 +
             门禁接线，而不是删掉那一份文件

14.2 判据挂在产出点：生成期即自检
     判据    `开发工具/公开调用完整性门禁.py` 的「依赖锁环」（首个样板：把装配期判据接到生成链）
     强制点  🟡 判据已在（接线后能抓出空壳锁，删掉即转绿）；**尚未接进发布门禁**
     状态    半强制 ← 应接线（把「生成器产出后自检」列为发布门禁检查项）
     备注    与 13.1 不打架：产出点自检正是必经路径上的检查，不是「靠人记得单独跑」

14.3 反复犯的错就是底层的缺口（2026-09-18 新增）
     判据    `开发工具/契约编译/能力定义编译器.py`：`从包推导编译参数(包目录)` 一键入口
             + 产出点包类型校验 `_装配器可装类型`
     —— 接口形状自证：调用方零元数据，包类型不合法在**编译期**报错并给出该填什么
     强制点  🟡 样板已落地（调用方零元数据 + 编译期类型拦），但「同类错误第二次出现即补底层」
             这条**派活前自检尚无判据、门禁零接线**
     状态    半强制 ← 应接线
     证据    `rg -n '_装配器可装类型|从包推导编译参数' 开发工具/契约编译/能力定义编译器.py` → 命中；
             全仓实测该入口：常规包 135 全通过 / 聚合父包 6（不编译是正确结果）/ 失败 0
     备注    与 14.2 的分界：14.2 管**产物侧**（产出 → 校验），14.3 管**入参侧**（接口形状）

14.4 文案给人看，错误码给机器判（2026-09-18 二次审计新增）
     判据    `开发工具/契约编译/能力定义编译器.py::_装配器可装类型` 先例 + 结构化字段判定样板
             （`isinstance(异常, 对应异常类)` + `异常.sqlite_errorcode`）
     —— **无全仓判据模块**：没有「扫 `in str(异常)` 控制分支」的原子能力
     强制点  🔵 人工（组件/文档评审）；后续可机器化为「新文件出现 `in str(异常)` 控制分支即报红」
     状态    人工强制 ← 可机器化
     证据    `rg -ln 'in str\(异常\)' --glob '!工程缓存/**' -g '*.py'` → 零命中
             （八处已收口；仅剩一处同类待收口：`平台控制面/提供者/控制面对账.py:41`
              `if "no such table" in str(错误).lower()` —— 用的是英文消息子串，属 14.4 反面，
              应改用结构化字段；本行只登记，不在本批改代码）
```

---

## 四、⚠️ 纸面规则清单（要消灭的）

```
条款   说明                          低成本机器化方案
────────────────────────────────────────────────────────────────
1.7    成品与制品不做本地归档           成品/制品：本机不留旧制品、安装包（判据可扫制品仓库旧版本）
                                       文档历史：只允许一个顶层 开发文档/归档/（不得第二个归档库）
1.6    临时脚本不留库                  判据 = 仓库根/各层出现 一次性脚本名模式即报红
                                       （扫描 `*探针*.py`、`*验收*.sh`、`搬迁*.py` 等）
```

**⇒ 收口后只剩 1 条纸面规则（1.6）；1.5 已废止删除（销项），1.7 已按 2026-09-17 裁决澄清口径并转为人工强制。**

---

## 四之二、反向试验栏（13.1 第三要素，2026-09-21 建栏 / 2026-09-22 接门禁）

> 口径唯一事实源 = `项目说明.md` 第 13 条（`:425`）：「**有判据、有强制点、有反向试验，才叫规则**；
> 三者缺一即为「纸面规则」，由发布门禁的「规则自检」项报红。」
> 本栏**只登记「反向试验在不在」**（取一条违规 → 故意弄坏 → 判据 / 门禁必须报红），不重述条款原文。
> **本栏判据**：栏内**每一行都必须带「反向试验」字样**，且 `反向试验` 命中行数 ≥ 本栏行数。
> 建栏时（2026-09-21）实测命中 **0**；建判据件时（2026-09-22）实测命中 **8 < 栏行 14**
> —— 栏自己声明的判据当时并不成立，这是本栏行行重写的原因。
> 登记值只有三档：**已实测**（有可复现的弄坏→报红记录，须附命令 / 日期）/ **纸面**（口径已写、未实测）
> / **未做**（还没验过）。三档由 `开发工具/规则自检.py` 判据② 机器校验：
> 状态非法 / 标「已实测」却无可复现证据 / 条款编号在逐条总账里悬空，一律报红。

```
条款   反向试验：故意弄坏 → 必须报红（样本）                      登记状态
────────────────────────────────────────────────────────────────────────────
13.1   反向试验：摘掉本判据件的必经路径接线（编译口清单删一项）⇒ 规则自检必须报红   已实测（2026-09-22：`python3.14 -m 开发工具.验证_规则自检反向验证` 八拍全通过 —— 摘线 / 删字样 / 状态非法 / 抽证据 / 超基线 / 总账读不成 各档均报红且点名准确，真仓库未弄坏为绿）
13.2   反向试验：让两条门禁结论共用「通过」一词 ⇒ 必须报红          未做
13.3   反向试验：让判据指不出承载能力（四环断在③）⇒ 必须报红        未做
14.1   反向试验：取一条无 `归因` 字段的条目走派活前检查 ⇒ 必须被拦下  纸面（口径已写，未实测）
14.2   反向试验：造一份空壳依赖锁 ⇒ 生成期 / 门禁必须报红           已实测（2026-09-18：接线后能抓出，删掉即转绿）
14.3   反向试验：同一工具同类错第二次 ⇒ 必须报红                     未做
14.4   反向试验：新文件出现 `in str(异常)` 控制分支 ⇒ 必须报红        未做（判据模块亦缺）
4.5    反向试验：写一份违反生命周期三判据的方案文档 ⇒「防回潮·文档生命周期」报红  未做（门禁已接线，反验样本未留）
5.3    反向试验：改契约面而不递增版本 ⇒ 门禁必须报红                未做（判据已接线，反验样本未留）
1.2    反向试验：再写一套同能力实现（第二套腿）⇒ 必须报红            未做
1.4    反向试验：把破坏检测器改成恒真 ⇒ 必须报红                     未做（有 `测试_反向破坏门禁28.py`，破坏样本记录未留）
1.8    反向试验：造一条无理由豁免 ⇒ 依赖防火墙必须报红               未做
7.9    反向试验：跑完生成器后让工作树留 diff ⇒ 必须报红              未做
12.5   反向试验：新增一个门禁项却不带破坏样本 ⇒ 必须报红             未做
────────────────────────────────────────────────────────────────────────────
统计   已实测 2 / 纸面 1 / 未做 11（共 14 行）
```

**⇒ 本栏暴露的真问题**：14 行里只有 2 行有可复现的真反向试验 —— 即「规则三要素」这条元规则
**自己**此前也是纸面规则。2026-09-22 已按 13.1 自己的话把它收口：
判据件 `开发工具/规则自检.py`（四类判据）+ **两处必经路径接线**（编译口门禁清单「规则自检」项、
发布门禁「规则自检（13.1 三要素）」项）——「判据未被门禁引用 → 报红」现在由它自己执行。
**反向试验留痕**：`开发工具/验证_规则自检反向验证.py`（夹具根七档弄坏 + 真仓库恢复拍，
八拍全通过；与 `验证_能力id冻结基线反向验证.py` 同形制，直跑退 0 即全过）。
**剩余缺口如实登记**：逐条总账 45 块 / 60 条目里 **44 块仍缺 `反向试验` 标签**，
按「债务冻结」记入 `防回潮阻断配置`（基线 44，只减不增，新增条款缺第三要素即红）；
缺口清单每轮由判据打印出来，不静默。

---

## 五、半强制 16 条 → 接线清单（卡点 B 的施工表）

```
优先级   条款   判据模块（已存在）                        接线后判据
──────────────────────────────────────────────────────────────────────
★★★     5.3    开发工具/契约编译/对外契约变更判定.py       契约面变但版本没递增 → 报红
★★★     1.2    测试_能力唯一事实源 / 能力调用图审计         出现第二套实现 → 报红
★★★     8.2    同上（能力唯一）                            出现旁路 → 报红
★★       5.1    公共契约/版本规则/契约版本.py               契约版本不唯一 → 报红
★★       5.2    运行核心/加载器/版本系统/契约兼容.py        强制参数不满足却放行 → 报红
★★       2.4    网关归一化                                  网关外出现形态判断 → 报红
★        7.6    开发工具/HTML验证/场景加载.py               某包缺验证场景引用 → 报红
★        9.4    各提供者确定性测试                          确定性分类缺失 → 报红（按 P-8 改后）
★        9.3    网关安全边界                                越过边界 → 报红
★        9.5    安全边界                                    把相对安全包装成绝对 → 报红
★        8.3    能力定义编译器                              契约面不全 → 报红（已有）
★        10.5   HTML 黑盒                                   补完没跑黑盒 → 报红
★        1.4    复用审计/反向破坏                           豁免空转 → 报红（需新增）
```

★★★     1.8    依赖防火墙.文件级豁免表                      豁免无理由 → 报红
★★★     7.9    开发工具.全量重算摘要                          跑完生成器 git 有 diff → 报红
✅ 已接   13.1   开发工具/规则自检.py（四类判据）               新增条款缺第三要素 → 报红
★        12.5   门禁检查项 × 破坏样本元测试                   新增项不带样本 → 报红

**⇒ 接线原则（13.2）：每条门禁结论必须落在五态之一（干净通过 / 债务冻结 / 只报告 / 未核验 / 失败），不得共用「通过」一词。**

---

## 六、维护规则（防止本文件膨胀）

```
① 本文件只登记**强制情况**，不重述条款原文（原文真源 = 项目说明.md）
② 条款新增/删除由项目说明.md 决定；本文件随之增删，不自行新增条款
③ 已接线为机器强制的，状态改为 ✅ 并留下"接线提交"；**不留过程流水账**
④ 状态四态（✅/🟡/🔵/⚠️）不得扩展或改名 —— 名词膨胀会毁掉总账
⑤ 每次大改哲学后重跑一次机械测绘（本文件第三节的表格口径）；
   **统计口径统一为第一节引言那四条**，不得各表各算（旧速览表就是因口径不一而自相矛盾，已删）
```
