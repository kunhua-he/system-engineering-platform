# 系统工程平台 · 架构（顶层骨架 ＋ 真实落点）

> 最后更新：2026-09-26 ｜ 状态：**策划稿（待华哥确稿）** ｜ 代码未动
> 顶层骨架＝华哥 2026-09-26 定稿的目录树。本文＝**唯一架构文件**，只画结构与流向；迁移与搬迁顺序不在本文（另行商量）。
> 口径：实测。会变的数字不写死，取证命令见 §十二。
<!-- 机器管理｜类型：方案｜生成器：人工 -->

## 一、顶层骨架

```text
系统工程平台/
├─ 运行内核/          纯内核：不依赖能力内容
│   ├─ 公共契约/       契约层 —— 唯一零依赖层
│   ├─ 运行核心/       装配 · 调用 · 句柄 · 任务 · 超时
│   ├─ 编译器/         快速验证 / 慢速验证
│   └─ 网关核心/       生产 40007 ／ 开发 40008
├─ 底座开发MCP/       维护底座用的开发面
│   ├─ MCP薄壳/        调网关能力的唯一入口（改代码 / 改文件必经此）
│   └─ 项目记忆/       项目开发记忆
├─ 支持库/            最小原子能力
├─ 模块库/            组合原子能力
├─ 技能库/            工作流封装
├─ 第三方库/          第三方源 / 二进制（不检索，仅供封装后调用）
├─ 工程缓存/          编译出来的制品
└─ 开发文档/          开发文档
```

## 二、节点 → 真实落点（全量，实测）

| 你的节点 | 当前仓库里的真实落点 | 包数（实测） |
|---|---|---|
| 运行内核 / 公共契约 | `公共契约/` | 0（非正式包根） |
| 运行内核 / 运行核心 | `运行核心/` | 1（`运行核心.运行状态监督`） |
| 运行内核 / 编译器 | `开发工具/{开发编译口,验证门禁,发布门禁,项目编译,契约编译}` | 0 |
| 运行内核 / 网关核心 | `运行核心/统一网关/` ＋ `运行核心/启动运行核心网关.py` | 0 |
| 底座开发MCP / MCP薄壳 | `开发工具/薄壳/` | 0 |
| 底座开发MCP / 项目记忆 | **无此节点**：现散在 `开发文档/规范/踩坑条目.json` · `开发文档/项目证据/` · 仓外 `~/.zcode` | — |
| 支持库 | `支持库/` | ≈100+ |
| 模块库 | `模块库/` | 19 |
| 技能库 | `技能库/` | 1（`技能库.后端.技能库`） |
| 第三方库 | **无此节点**：现为 pip 装进 `工程缓存/` 各提供者 venv | — |
| 工程缓存 | `工程缓存/` | — |
| 开发文档 | `开发文档/` | — |

### 尚未归位的现存层（你的树里没有它们的位置，只登记不决定）

| 现存层 | 是什么 | 包数（实测） |
|---|---|---|
| `启动监督器/` | 健康监督 · 容量基线（**核心面**之一） | 1 |
| `前端核心/` | 窗口 / 渲染契约（**核心面**之一） | 0（3 个 .py） |
| `后端核心/` | 通用运行宿主：装配 · 注册 · 生命周期（**核心面**之一，网关装配点） | 0（4 个 .py） |
| `平台控制面/` | 能力目录（注册/租约/改动流水）· 包仓库（制品）· 发布管理 · 授权 | 15 |
| `项目适配层/` | 项目级适配（依赖防火墙里独立成层） | — |
| `客户端/` | 制品构建（`构建平台客户端.py`，核心支撑面） | — |
| `测试中心/` | 全部测试 | — |
| `运维脚本/` | 运维壳（仓库锁、重启网关…） | — |
| `示例项目/` | 示例 | — |

## 三、分层与依赖方向（唯一事实源）

```text
判据住两处，改口径只改这两处：
① 公共契约/正式根.py
     正式根名表:30 ／ 源码层附加名表:56 ／ 源码层非正式根表:69
     核心面层表:99 ／ 核心支撑面表:111 ／ 非核心面层表:125 ／ 生成式目录表:47
② 运行核心/依赖防火墙.py
     层名称表:107 ／ 允许依赖表:163 ／ 文件级豁免表:216 ／ 头注释镜像块:13（机器校验同源）

层 ＝ 文件相对系统根的第一段目录名（_确定层:345）；制品态先剥制品包名前缀（剥制品根前缀:148）。
```

```text
┌─ 运行内核 ────────────────────────────────────────────────────────────┐
│  公共契约 ── 依赖 = 空集（唯一真正自洽的一层）                          │
│  运行核心 → 公共契约 ＋ 支持库   ← 这一条是今天「内核不自洽」的根        │
│  前端核心 / 后端核心 → 公共契约 ＋ 运行核心                             │
│  启动监督器 → 公共契约 ＋ 支持库 ＋ 项目适配层                           │
└───────────────────────────────────────────────────────────────────────┘
        ▲ 向下依赖
┌─ 能力内容 ────────────────────────────────────────────────────────────┐
│  支持库 → 公共契约        模块库 → 公共契约 ＋ 支持库                    │
│  技能库 → 公共契约        项目适配层 → 公共契约 ＋ 运行核心               │
│  平台控制面 → 公共契约 ＋ 支持库 ＋ 运行核心                             │
└───────────────────────────────────────────────────────────────────────┘
        ▲ 消费者
┌─ 开发面（不被任何层依赖）─────────────────────────────────────────────┐
│  底座开发MCP（薄壳）· 开发工具其余散件 · 客户端 · 运维脚本 · 测试中心      │
└───────────────────────────────────────────────────────────────────────┘
```

## 四、一次能力调用的全链路

```text
调用方
 │ capability_call(能力id, 数组=[按契约行序的位置参数], 开工ID=…)
 ▼
① 薄壳        开发工具/薄壳/薄壳服务.py
     · 超上限转「全量回执指针」，取回须 返回字符数=0
     · 失败回执附 按契约行序的 参数表／数组样例／照抄式样（公共契约/能力契约/数组文本.py）
 │ HTTP :40007（生产）／:40008（开发）
 ▼
② 网关入口    运行核心/统一网关/网关处理面.py
     · 未知参数在网关边界**忽略**（网关边界忽略 ／ 进程内拒绝，两层刻意不同）
 ▼
③ 参数校验    网关参数校验面.py → 唯一能力调用_调用参数.py
     · 类型判据：公共契约/基础类型/类型规格.py ＋ 字段名册.py
     · 进程内未知参数 ⇒ 参数不合法（未知参数错误(TypeError)）
 ▼
④ 注册表分发  公共契约/能力契约/契约.py::能力注册表
     · 能力id 必须带包前缀（短名 ⇒ 能力未注册）
 ▼
⑤ 执行        运行核心/统一网关/网关执行面.py
     · 同步直调；耗时任务走异步腿（§八）· 写类能力先过 写入授权（§十）
 ▼
⑥ 结果信封    公共契约/基础类型/结果类型.py
     · 成功无值＝1 ／ 成功有值＝正文 ／ 失败＝-1
     · 账本：公共契约/诊断/调用账本.py
```

## 五、写入与租约链路

```text
开工编排.开工准备       模块库/开工编排/实现/开工编排.py
   └─ 发 开工ID ＋ 六项开工资料（占用状态/契约指针/规范片段/能力映射/纪律段）
        ▼
开工编排.开工即占       修改路径全量申报 ⇒ 原子认领，任一被占即整体失败
   ├─ 发文件租约（所有者＝开工ID）  平台控制面/能力目录/实现/文件租约申请.py
   ├─ 认领即解锁「目标 ＋ 待建父链」 公共契约/运行时/仓库只读锁.py
   └─ 校验路径边界                 支持库/后端/系统核心支持库/路径安全
        ▼
改          唯一写腿：文件系统支持库.文件操作 ／ 文本补丁
   └─ 穿锁＝解锁(目标＋父目录) → 写 → 回锁；新建文件继承父目录锁状态
        ▼
提交        版本控制支持库.Git操作.提交（只用精确路径，禁 add -A）
   ├─ commit-msg 钩子 验开工ID    开发工具/git钩子/commit-msg
   └─ pre-commit 钩子 验锁        开发工具/git钩子/pre-commit
        ▼
开工编排.收工释放       租约终结，同路径可立即再申请

回收腿：平台控制面/能力目录/实现/文件租约维护.py::回收过期文件租约
        三半并集 ＝ 到点 ｜ 心跳停摆 ｜ 所有者进程消失
```

## 六、编译链路（快速验证 / 慢速验证）

```text
自修复工具.静态编译（快速）／动态编译（慢速）   模块库/自修复工具/实现/
   │ 组编译口命令（预算＝调用方超时−60，恒小于调用方超时）
   │ 类型=0 全仓 ／ 类型=1 指定库或模块；静态档固定 --不跑点名
   ▼
开发工具/开发编译口/编译口.py（全轮预算；耗尽 ⇒ 剩余项不起子进程、记未核验，不占通过位）
   步骤1 影响面（git 变更集／--文件／--库／--全仓）
   步骤2 生成物全自动（数据源 开发文档/规范/生成物登记表.json；唯一执行器 生成物联动.py）
   步骤3 语法编译（py_compile）
   步骤3b 门禁并行（8 路，下表 22 项）
   步骤4 生成物复跑确认（判据＝步骤3b 真实退出码）
   步骤5 定向测试（按受影响包点名 unittest，走 执行命令集 并行）
   ▼
五态结论：干净通过 ｜ 债务冻结 ｜ 只报告 ｜ 未核验 ｜ 失败（退出码 0/0/0/2/1）
```

### 门禁清单（编译口 22 项，全列）

| # | 门禁 | 模块 | 角色 |
|---|---|---|---|
| 1 | 摘要闭合 | 开发工具.全量重算摘要 | 阻断 |
| 2 | 契约漂移 | 开发工具.契约编译.漂移检测 | 阻断 |
| 3 | 公开调用完整性 | 开发工具.公开调用完整性门禁 | 阻断 |
| 4 | 能力id冻结基线 | 开发工具.能力id冻结基线门禁 | 阻断 |
| 5 | 验证场景体检 | 开发工具.验证场景体检 | 阻断 |
| 6 | 重复腿与旁路 | 开发工具.验证门禁.运行验证门禁 | 阻断 |
| 7 | 测试体系门禁 | 开发工具.测试体系门禁 | 阻断 |
| 8 | 测试伪装门禁 | 开发工具.测试伪装门禁 | 只报告 |
| 9 | 防回潮·反向破坏 | 开发工具.发布门禁.运行发布门禁（in-process 直调） | 阻断 |
| 10 | 参数可用性 | 开发工具.契约编译.参数可用性体检 | 阻断 |
| 11 | 规范漂移 | 开发工具.规范漂移体检 | 阻断 |
| 12 | 债务清单 | 开发工具.MD文档生成（--类型 债务清单 --校验） | 阻断 |
| 13 | 目录树与命令 | 开发工具.MD文档生成（--类型 包级说明-目录树与命令） | 阻断 |
| 14 | 拒绝手写md | 开发工具.MD文档生成（--拒绝手写校验） | 阻断 |
| 15 | md查重 | 开发工具.MD文档生成（--查重） | 只报告 |
| 16 | 规则自检 | 开发工具.规则自检 | 阻断 |
| 17 | 依赖分两段 | 开发工具.依赖派生.生成依赖分两段 | 阻断 |
| 18 | 第三方导入分布 | 开发工具.第三方导入分布基线门禁 | 阻断 |
| 19 | 测试写入边界 | 开发工具.测试写入边界门禁 | 阻断 |
| 20 | 参数口径 | 开发工具.参数口径审计 | 阻断 |
| 21 | 制品新鲜度 | 开发工具.制品新鲜度门禁 | 只报告 |
| 22 | 写入凭证透传 | 开发工具.写入凭证透传门禁 | 只报告 |

## 七、上线链路（制品 ＋ 双端口）

```text
自修复工具.构建平台客户端制品(安装=真)
   → 客户端/构建平台客户端.py   源码树 ⇒ 工程缓存/制品仓库/平台客户端制品/<内容摘要>/
   → 平台控制面/包仓库/        入库（内容寻址＋签名）→ 安装 → 激活
        激活稳定路径 ＝ 工程缓存/制品仓库/平台客户端环境/平台客户端
   → 重启网关：launchctl kickstart -k gui/$(id -u)/com.huashi.gateway-40007

双端口：端口由环境变量 系统底座_网关端口 决定（默认 40007），
        运行核心/启动运行核心网关.py:134 读取 ⇒ 40008 只是第二个 launchd 服务。
        ★ 两个实例必须各带自己的 系统底座_工程缓存根，否则运行态静默分叉。
        ★ launchd plist 不在仓库内（仓内无 *.plist）。

判据：开发工具/制品新鲜度门禁.py（制品来源提交 vs 当前 HEAD ＋ 工作区字节指纹）
注意：上线=真 在编译红时静默跳过构建 ⇒ 上线不靠它，靠上面两步。
```

## 八、异步与长任务

```text
耗时命令（>15 秒）：启动进程 → 查询进程状态（秒回）→ 释放句柄
   · 支持库/后端/系统核心支持库/进程管理/（执行命令 · 执行命令集[并行 8 路] · 启动/查询/释放）
   · ★ 释放句柄 ＝ 终止 ＋ 回收 ⇒ 长任务不要用它
任务腿：系统核心支持库.检查点恢复（创建/领取/完成/登记中断）＋ 工具执行（登记/完成投递）
   · 无句柄、输出落 工程缓存/底座运行.db、输出硬上限（超限 结果=null ⇒ 大输出必重定向）
```

## 九、文档与生成物体系

```text
唯一生成器：开发工具/MD文档生成.py（一个入口，--类型 选类，--校验/--写盘/--新建/--补头）
类型账目：项目文档.列文档类型（权威文档／规范／包级说明×3／决策记录／方案／分析／债务清单… 17 类）
派生件全自动：开发文档/规范/生成物登记表.json（数据）→ 开发编译口/生成物联动.py（唯一执行器）
   · 每包 完整性摘要.json ｜ 未完成事项.md 账目块 ｜ 说明/目录树与命令.md ｜ 依赖分两段
债务唯一清单：开发文档/未完成事项.md（账目由生成器按正文实数算出，禁手改生成区）
机器印记：每份 md 头部 `<!-- 机器管理｜类型：…｜生成器：… -->`，手写件一律拒绝（门禁 #14）
```

## 十、安全与权限链路

```text
写凭证：非只读能力必须带 开工ID ⇒ 开工编排.开工即占 发放
写入授权：公共契约/运行时/写入授权.py（受管路径无活跃写租约 ⇒ 越界）
仓库只读锁：公共契约/运行时/仓库只读锁.py（内核级 chflags；只有平台写腿能开窗）
   · 日常命令：运维脚本/仓库锁.py（上锁 ／ --查看 ／ --补齐 ／ --下锁）
网关凭证：系统库网关凭证（解锁门槛只认它）
git 钩子：开发工具/git钩子/（hooksPath 指向它）：commit-msg 验开工ID、pre-commit 验锁
```

## 十一、数据落点

```text
工程缓存/制品仓库/平台客户端制品/<摘要>/    制品（内容寻址＋签名）
工程缓存/制品仓库/平台客户端环境/平台客户端  激活制品（稳定路径，网关跑的就是它）
工程缓存/编译口单写者锁.json               编译口单写者锁
.codegraph/codegraph.db                   代码地图（代码解析支持库）
工程缓存/底座运行.db                      任务腿落库
开发文档/项目证据/能力id冻结基线.json        能力id 基线
开发文档/项目证据/MD写入流水.jsonl          md 写入流水
开发文档/项目证据/验证历史.jsonl            成功验证记录
开发文档/规范/生成物登记表.json             派生凭据清单（数据）
```

## 十二、取证命令（本文不写死任何会变的数字）

```bash
python3.14 -m 开发工具.MD文档生成 --类型 债务清单 --校验      # 债务逐条（唯一事实源＝未完成事项.md）
python3.14 -m 开发工具.开发编译口.编译口 --不跑点名 --全仓     # 门禁全查（22 项）
python3.14 -m 开发工具.规则自检                              # 判据/强制点/反向试验 三要素
python3.14 -m 开发工具.运行状态监督.提供者 --审计依赖          # import 层向（依赖防火墙）
```

## 十三、待归位与待裁（只登记，不决定）

| # | 事项 | 实测依据 |
|---|---|---|
| 1 | 「核心」是纸面：`是核心面路径` 全仓零调用方 | 公共契约/正式根.py:131 |
| 2 | 内核边界在判据上是开的：明文放行 支持库 | 运行核心/依赖防火墙.py:175 |
| 3 | 已跨 6 处 内核→支持库，含依赖防火墙自己 | 依赖防火墙.py:63 · 资源协调/服务.py:22 · 运行环境管理器/__init__.py:32 · 镜像发布.py:58 · 远程镜像.py:46 · 脱敏工具.py:12 |
| 4 | 层只认顶层目录；`运行内核/` 四子层会压成一层 | 运行核心/依赖防火墙.py:345 |
| 5 | 公共契约 搬迁会断 ≈200 处 `from 公共契约` | 全仓 .py（回执原字符 11,763，已排 工程缓存/开发文档） |
| 6 | `包id` 带层名（如 `模块库.自修复工具`） | 各 包声明.json:2 |
| 7 | `第三方库/` 与「一第三方一支持库一提供者」＋venv 隔离冲突 | 运行核心/依赖防火墙.py:265 |
| 8 | 9 个现存层未归位 | 见 §二 |
| 9 | 搬迁会让 未完成事项.md 每条债务的归属路径失效 | 开发文档/未完成事项.md |

逐行号节点树（随代码漂移，按需重取）：`开发文档/架构总览.md` 附录 A；逐条债务与修法：`开发文档/未完成事项.md`。
