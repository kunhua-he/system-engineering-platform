# 三条裁决总执行方案（峰值期后高并发推进）

> 最后更新：2026-09-18
> 口径：实测（数字随建包与提交逐日变化，引用前必须重跑取证命令）
> 维护者：开发文档/方案

## 一、裁决与子方案索引

**华哥 2026-09-18 三条裁决**，本次只出 MD、不动代码；峰值期（约 18:00 结束）后按本文高并发执行。

| 裁决 | 内容 | 子方案（唯一落点） | 预估规模 |
|---|---|---|---|
① | 能力 id **全仓统一**；**不要有第二条腿** | `开发文档/方案/能力id全仓统一方案_20260918.md` | **254 条 id + 约 5100 处引用** |
② | 架构拓展 7+2 **全做** | `开发文档/方案/架构拓展7加2落地方案_20260918.md` | 9 件（3 件已有基础 / 1 件待收口 / 4 件新增 / 2 件只切边界）|
③ | 故障注入样本 **做掉** | `开发文档/方案/故障注入样本落地方案_20260918.md` | 145 检查项逐项配对，分 3 阶段 |

**三条都遵同一组标准（华哥原话）**：完善 / 高效运转 / **单腿运行** / 完全可控 / 大量兼容 / 简单易用。
**不以「省成本」或「判断收益」为取舍依据。**

---

## 二、三条之间的依赖关系（决定并发编排）

```
裁决①（能力 id 统一）
   └─ 独立，但**引用面最广**（5100 处）⇒ 与其他两条同时改会互相踩
裁决②（架构拓展）
   ├─ ⑧⑨ 接口抽象 ── 独立，无行为变更
   ├─ ①② 配置收口 + 迁移通用件 ── 独立
   ├─ ③ 请求 id 贯穿 ── ★ 会在「统一返回信封」上与裁决①的引用点重叠
   ├─ ④⑥ ── 独立
   └─ ⑤⑦ ── ⑤ 依赖能力目录（与①同为能力面）；⑦ 动契约与状态库
裁决③（故障注入样本）
   └─ 独立（只碰 开发工具/ + 测试中心/），**全程可并行**
```

**冲突点只有两处**（必须错开或串行）：
1. **裁决①的引用点** vs **裁决②③的「统一返回信封」**（`请求id` 字段）—— 同在 `*.py` 调用点与场景 JSON；
2. **裁决①的包结构** vs **裁决②⑤的能力分类树**（都按包 id 派生）。

⇒ **编排原则：裁决③全程并行；裁决②先做不冲突的 ⑧⑨①④⑥；裁决①单独一批独占引用面；②的 ③⑤⑦ 在①之后。**

---

## 三、执行批次（峰值期后照此推进）

### 第 0 批：地基（串行，1 轮）
- 建 **能力 id 迁移工具**（按包 id 派生新 id + 全仓引用替换 + 差异报告）—— 一份工具，不手工改；
- 建 **门禁样本覆盖元测试**（12.5 的强制点）；
- 冻结当前基线：`能力id冻结基线`、`防回潮存量基线`、`HTML 黑盒` 全绿快照。
- **验收**：工具在**一个包**上试跑，红/绿可控；元测试能报出缺样本。

### 第 1 批：低风险高价值（可高并发，4–6 路）
- ②⑧⑨ 接口抽象（只切边界、无行为变更）
- ②① 配置加载器收口（消灭重复入口）
- ②④ 任务队列上限
- ②⑥ 结构化日志推广
- ③ 阶段1 样本（已接线的强制项）
- **并发前提**：各路子代理**独立目录**，禁碰 `工程缓存/制品仓库/`（8.3.2 竞态教训）

### 第 2 批：能力 id 统一（独占，按包分片并发）
- 按 `能力id全仓统一方案` 第四节批 1–4 推进（模块库 67 → 支持库后端 ~120 → 前端+适配层 ~67 → 其它 6）；
- **每片一个子代理、一个独立包**，互不重叠；
- 全量回归在第 2 批末尾**串行**跑一次（不中途反复跑，哲学 12.1③）。

### 第 3 批：依赖①的②③⑤⑦（串行）
- ②③ 请求 id 贯穿（引用面已统一，此时做最省）
- ②⑤ 能力分类树（按新 id 派生）
- ②⑦ 租户 id 预留（动契约与状态库，须全量回归）

### 第 4 批：收口（串行，1 轮）
- ③ 阶段2/3 样本补齐（元测试常驻，缺样本即红）
- 全仓回归：`全量重算摘要` + HTML 黑盒 + 发布门禁 + 新增门禁项全部生效
- 文档收口：结论并入 `项目说明.md` / `未完成事项.md`，本方案族按体例规范归档

---

## 四、并发纪律（直接沿用既有教训，不重犯）

| 纪律 | 来源 |
|---|---|
**精确路径 `git add`，禁用 `git add -A`** | `未完成事项.md` 8.3.2（血案：变异值进 HEAD）|
**并行期构建与黑盒验证串行**；各用独立制品目录 | 同上（血案：连续 3 次构建失败）|
**反向验证期间等「已还原」再提交** | 同上 |
**子代理必须独立 worktree / 独立目录** | `多会话并行开发规约` |
**派活前现场复核条目前提** | 8.3.1（血案：过时条目白派一轮）|
**每处修复配反向验证** | AGENTS.md 硬规则 |
**任务信必带继承协议九条** | AGENTS.md |
**不中途反复跑全量** | 哲学 12.1③ |

---

## 五、完成判据（华哥口径逐条对应）

| 标准 | 可验证判据 |
|---|---|
**完善** | 三条裁决的子方案**全部**有落点/契约/场景/门禁；无「待补」项 |
**高效运转** | 关键单步耗时现场实测（不以「本来慢」归因）|
**单腿运行** | ① 无旧 id 别名；② 无重复入口（配置/迁移/日志/队列/请求id）；③ 无第二门禁项 |
**完全可控** | 队列/日志/缓存/句柄全有界；满载如实拒绝；资源回收按归属而非端口 |
**大量兼容** | 681 能力 / 1047 步骤保持可用；只增不改（对外契约变更另有版本递增承载）|
**简单易用** | 调用方只经唯一公开入口，不感知底层来源 |

---

## 六、时间与峰值

- **当前**：峰值期，只做 MD（本方案族），不动代码；
- **峰值结束（约 18:00）后**：按本文第三节批次推进，**高并发**；
- **要求**：不抠并发数（`max_concurrent_children` 实为 300），但要**不冲突、不重叠、不空转**。
