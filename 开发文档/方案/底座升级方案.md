# 系统工程平台 · 底座升级执行计划（唯一权威）

## 当前批次：身份、路径、资源边界与发布一致性维修（待华哥确认开工）

### 当前事实核验结论

以当前工作树 `e02f1b2eaff926f58c4c15a7bbab2bbbee97ecda` 为边界，工作树干净。旧报告中的部分问题已经被后续代码修复，不能重复维修；当前仍确认的维修对象如下：

- **P1 包扫描事实源分叉**：`运行核心/加载器/包发现/发现器.py:29-56` 与 `开发工具/项目编译/正式包索引.py:44-105` 二者父包/叶包筛选口径不同，需统一唯一扫描事实源。**2026-09-16 现场实测口径**：运行发现（`发现器.发现全部(系统根/支持库, 系统根/模块库)`，已含声明式自动纳入的 `平台控制面`）= **127 个声明**；正式包索引（`正式包索引.构建索引(系统根)`）= **支持库 109 + 模块库 18 + 技能库 1**，能力所有者 625、能力冲突 0。全仓 `包声明.json` 实数 **133**（含 6 个不进装配表的聚合父包）。
- **P1 发布物料清单路径边界（路径已更正，问题已修复）**：落点由已删除的 `MCP工具箱/发布治理.py` 改为 **`开发工具/发布门禁/发布治理.py`**；2026-09-16 实读 `_读取物料清单`（:363-380）已对清单项做合法性与越界拒绝：文件摘要不合法 → `物料清单文件摘要不合法`、路径越界 → `物料清单路径越界`、正式文件不存在 → `物料清单中的正式文件不存在`。旧条目的“未在读取前拒绝”已不成立。
- **P1 工作区破坏性操作范围（条目整体失效）**：原落点 `MCP工具箱/工作区管理.py`、`MCP工具箱/项目服务.py` 已随 `MCP工具箱/` 整目录删除（2026-09-15），仓库内不再存在该工具面与隔离工作区工具（实测 `find . -name '工作区管理.py' -o -name '项目服务.py'` 在 `工程缓存/` 外无命中）；本条不再有可维修对象，保留仅为历史记录。
- **P1/P2 任务与审计归属边界**：`运行核心/任务调度/任务系统.py:197-236` 的任务查询/取消只按任务 id；`运行核心/运行诊断/安全审计/安全审计.py:68-107` 支持空过滤条件全量读取且无返回上限。多项目/多用户场景下仍缺权威身份绑定。
- **P1/P2 资源上限未收口**：`支持库/适配层/模型HTTP提供者.py:47-48`、`支持库/后端/系统核心支持库/进程管理/实现/进程管理.py:136-170` 等仍存在无界响应或子进程输出读取；若干浏览器/编辑器服务仍使用无容量限制的线程 HTTP 服务，日志和 JSONL 查询也未统一限长/轮转。
- **P1/P2 聚合注册异常可静默吞掉**：多个正式支持库包级入口仍存在宽泛异常吞掉路径；必须逐个区分“预期不可用 Provider”与“注册/导入失败”，后者改为可观测并阻断。
- **文档事实漂移**：`开发文档/项目说明.md:12-15,49-51` 仍引用旧 HEAD、旧 MCP 连接语义；`开发文档/未完成事项.md` 同时保留已完成事项和当前债务，需在维修收口时按唯一事实源清理。
- **运行态**：8766 管理端**已整体删除**（2026-09-15 随 D 清场，`MCP工具箱/` 整目录连同该 HTTP MCP 服务一并下线），不存在"没监听=未启动"的问题，也不再有"启动 8766 做 MCP 初始化/反馈/验证回读"这一步。当前 agent 接入面见下：**stdio 薄壳**（`开发工具/薄壳/`，3 工具、无端口）+ **40007 唯一执行网关**（实测 2026-09-16 常驻，PID 82241）。

### 明确排除（已核实，不重复改）

- `开发工具/HTML验证/制品进程.py:120-130` 已通过 `环境.setdefault("系统库网关凭证", "html-blackbox-verifier")` 注入受管测试凭证；旧“凭证未注入”条目已失效。
- 记忆支持库的 92 份 `验证场景引用.json` 当前均为 `验证场景/v1`；**2026-09-16 实测**正式索引能力 **625**（`正式包索引.构建索引` 的 `能力所有者` 计数，旧文写的 408 已过期）、能力冲突 0。
- 资源清单路径已改为固定 `工程缓存/测试资源清单/<安全开工id>.jsonl`；不再按旧审计中的任意路径参数复现该问题。
- `MCP工具箱/临时上下文.py:12-23` 已对开工 id 做严格字符校验；旧清洗碰撞/路径穿越条目不作为本批重复维修点。（该文件已随 `MCP工具箱/` 整体删除，条目仅作历史记录；现在"开工"入口是网关能力 `开工编排.开工准备`。）
- `运行核心/统一网关/限流器.py:78-146` 已按项目、用户、能力、任务、提供者分别计数；旧“只做全局计数”条目不作为本批维修点。

### 允许修改范围与单链路流程

```text
冻结当前 HEAD/工作树/债务清单
  → 为每个确认问题补最小失败回归测试
  → 先收口唯一身份、路径和包扫描事实源
  → 再批量收口 HTTP/进程/线程/日志资源上限
  → 再处理聚合注册异常与文档事实漂移
  → 受影响 unittest 定向回归
  → 当前 HEAD 重新编译唯一制品
  → 真实 HTML 黑盒验证
  → stdio 薄壳自测（initialize / tools/list / tools/call，无端口；8766 已删除）
  → 发布门禁；任一失败保持阻断
```

- 不做降级、旁路、兼容入口、静默 fallback，不连接正式业务数据库，不主动推送远程。
- 只修改上述维修对象涉及的 `开发工具/薄壳/`、`运行核心/`、`公共契约/`、`平台控制面/`、`支持库/`、`开发工具/`、`测试中心/` 和对应 `开发文档/` 文件；每一批先列实际文件，再动手。（旧清单里的 `MCP工具箱/` 已整体删除，换成现在的接入面 `开发工具/薄壳/`。）
- 统一入口仍是 `调用能力(能力id, 参数)`；HTML 黑盒仍是功能主验收；MCP 只作治理链验收，不新增第二执行腿。
- 代码规范可以进入编译器/契约检查：变量、函数、文件命名、模块边界、接口字段和返回结构必须统一；但不得把 Agent 提示词、AGENTS.md、身份旁路或额外任务状态机变成运行前置门禁。
- HTML 与 MCP/公开 HTTP 接口优先走真实 `GET/POST`；已有接口能直接请求时，不再新增代理套娃、自我验证链或第二调用路径。文档只提供上下文，读取失败不能阻断正式接口执行。
- 资源共享按资源键串行：固定端口、测试数据库、发布指针、外部应用和强杀目标不得并行；纯契约测试才可并行。

### 分批维修与回退

| 批次 | 收口内容 | 进入下一批硬条件 | 回退方式 |
|---|---|---|---|
| A | 包扫描唯一事实源、物料清单路径、工作区/任务/审计身份边界 | 反向越界测试全部阻断，受影响定向测试通过 | 保留本批独立提交，失败即恢复该提交，不动已通过批次 |
| B | HTTP/进程/线程/输入/日志统一上限与 finally 回收 | 超限返回结构化错误，压力/超时/回收无残留 | 只回退本批资源边界改动 |
| C | 聚合注册异常、依赖防火墙健壮性、文档事实源 | 导入失败可见且门禁非零；文档与代码统计一致 | 只回退本批注册与文档改动 |
| D | 当前 HEAD 编译、HTML 黑盒、stdio 薄壳自测、发布门禁 | 所有正式命令真实退出码 0，证据绑定当前 HEAD | 不激活新制品，保留旧激活指针 |

当前已完成的 A 批次局部回归：发布物料清单路径边界和激活指针 CAS 的定向测试已通过；包扫描、工作区/任务/审计身份边界尚未收口，不能据此宣称 A 批次完成。

### 分级验收命令

```bash
# 工作包/合并波次：只运行明确受影响模块，不用 discover，不跑全量编译
env -u PYTHONPATH PYTHONDONTWRITEBYTECODE=1 python3.14 -m unittest 测试中心.<受影响模块>

# 当前 HEAD 重新生成唯一制品
env -u PYTHONPATH PYTHONDONTWRITEBYTECODE=1 python3.14 客户端/构建平台客户端.py --安装

# 正式功能主验收：真实 HTTP，固定端口池，不读取源码内部结果
# 注意：中文环境变量名必须经 env 传（bash 不能把「中文名=值」当赋值，直接写前缀会报 command not found）
env 系统库网关凭证=html-blackbox-verifier PYTHONPATH= PYTHONDONTWRITEBYTECODE=1 python3.14 -m 开发工具.HTML验证.验证器 --制品 工程缓存/制品仓库/平台客户端环境/平台客户端 --并发 32 --实例数 3 --端口池 45080-45082

# stdio 薄壳接入面自测（无端口；凭证同样经 env 传）
env 系统库网关凭证="$(plutil -extract EnvironmentVariables.系统库网关凭证 raw -o - ~/Library/LaunchAgents/com.huashi.gateway-40007.plist)" PYTHONDONTWRITEBYTECODE=1 python3.14 -B 开发工具/薄壳/自测_stdio客户端.py

# MCP 治理链和唯一发布判定
env -u PYTHONPATH PYTHONDONTWRITEBYTECODE=1 python3.14 开发工具/发布门禁/运行发布门禁.py
```

### 当前批次完成标准

- 当前工作树、HEAD、制品来源指纹、制品摘要和验证证据逐项绑定；不复用旧提交或无绑定报告。
- 包扫描、能力 owner、依赖锁、注册入口和完整性清单的计数与集合一致；冲突直接阻断。
- 越界路径、空身份跨任务/跨项目访问、超限输入、超限响应、子进程输出、线程和日志增长均有失败回归并真实返回结构化阻断。
- 正式 HTML 黑盒覆盖当前制品全部公开能力，资源清理失败 0、残留 0；8766 MCP 服务真实启动并完成最小治理调用；发布门禁退出码 0。
- 未达到以上全部条件前，发布结论保持“阻断”，不得用历史 408/408、545/545 或旧门禁结果代替当前证据。

---

> 本文件是唯一执行计划。它只回答：先确认什么、改哪些文件、为什么改、依赖什么、用什么命令验收、什么结果才允许进入下一步。
> 项目事实以 `开发文档/项目说明.md` 为上位权威；支持库/模块清单以 `开发工具/项目编译/正式包索引.py` 现场生成，逐包细节以包声明和能力契约为准；本文件只规定升级顺序和工程落点。

## 0. HTML 验证器职责拆分工作流（已完成）

原 `开发工具/HTML验证/验证器.py` 为 1816 行、86141 字节并混装十类职责；现已拆为19个单向依赖职责模块，CLI `验证器.py` 为24行，所有职责文件均不超过160行。拆分未改变45080统一入口、端口池、资源键、真实HTTP返回、证据结构或发布判定。

```text
冻结当前 46/46 定向回归与真实 HTTP 基线
  → 先迁移纯数据模型和常量
  → 再迁移无副作用的事实读取、场景解析和返回判定
  → 再迁移 HTTP、进程生命周期和动态值
  → 再迁移场景执行器和多实例制品池
  → 最后迁移证据、验证服务和 CLI 调度
  → 删除验证器.py 内部旧实现，不留转发兼容层
  → 当前 HEAD 重建制品、真实 3/8 实例黑盒、发布门禁
```

### 0.1 目标目录和唯一职责

```text
开发工具/HTML验证/
├── 验证器.py            CLI 唯一入口，只解析参数、调用应用服务和映射退出码，目标≤80行
├── 常量.py              端口、并发、超时、输出上限和统一返回字段
├── 单步场景.py          验证场景、验证步骤及严格字典解析
├── 多步场景.py          多步骤场景、验证场景束及步骤统计
├── 验证报告.py          验证结果、验证报告及汇总序列化
├── 制品事实.py          工作区指纹、制品摘要、启动器定位和公开能力扫描
├── 场景加载.py          JSON读取、场景引用解析、动态声明和能力全集闭合校验
├── 端口池.py            端口池解析、端口探测、资源键和场景分片
├── 制品进程.py          有界输出、启动、健康等待、进程组回收和残留核对
├── HTTP请求.py          回环地址校验、请求编码、发送和错误归一化
├── 返回判定.py          统一返回、字段类型、关键值和完整值断言
├── 动态值.py            制品根、受管临时路径、夹具复制和步骤返回展开
├── 场景执行器.py        场景内前置→目标→finally清理和场景间资源键并发
├── 多实例制品池.py      N实例并行启动、分片执行、报告合并和全实例回收
├── 验证证据.py          制品前后绑定、证据落盘和场景文件生成
└── 验证服务.py          浏览器验证页、代理和服务模式
```

每个文件只承担表中一项职责，目标 60～160 行；确需超过 160 行时必须先继续按职责拆分，不得以“相关代码放一起”为由重新聚合。`验证器.py` 不保存模型、解析器、执行器或进程池实现。

### 0.2 单向依赖冻结

```text
常量
  → 单步场景 / 多步场景 / 验证报告
  → 制品事实 / HTTP请求 / 返回判定 / 动态值 / 端口池 / 制品进程
  → 场景加载 / 场景执行器
  → 多实例制品池 / 验证证据 / 验证服务
  → 验证器.py
```

- 下层模块不得导入上层调度模块；禁止循环导入、运行时动态导入和 `sys.path` 拼接。
- 多实例制品池只调度 `场景执行器`，不得复制单步请求、返回判定、动态值或清理逻辑。
- 资源键只在 `端口池.py` 定义一份；LibreOffice、模型连接器、FFmpeg 的锁定规则不得散落。
- 统一返回判定只在 `返回判定.py` 定义一份；单实例、多实例、服务模式必须共用。
- 单实例制品端口默认由系统动态分配（`--端口 0`），不是固定端口兼容层；多实例验证使用端口池 `45080-45180`；发布门禁同样使用动态端口，句柄回收即释放，不占用固定端口。

### 0.3 分批迁移和硬门槛

| 波次 | 搬运范围 | 验证命令 | 硬条件 |
|---|---|---|---|
| S1 | `常量.py`、场景模型、验证报告 | `python3.14 -m unittest 测试中心.第一批维修.测试_HTML验证器` | 数据字典、异常文本、汇总字段逐项一致 |
| S2 | 制品事实、场景加载、返回判定、动态值 | 同上 | 旧格式阻断、408能力覆盖、动态路径安全行为一致 |
| S3 | HTTP请求、制品进程、端口池 | `python3.14 -m unittest 测试中心.第一批维修.测试_HTML验证器 测试中心.第一批维修.测试_HTML验证器多实例` | 启动/超时/回收/端口池测试全部通过 |
| S4 | 场景执行器、多实例制品池 | 同上 | 46/46通过；3实例 408/545、清理0、残留0 |
| S5 | 证据、验证服务、CLI；删除旧实现 | 同上 | `验证器.py`≤80行；无旧函数转发层；无循环依赖 |
| S6 | 正式收口 | HTML 8实例拉满 + 发布门禁 | 408/408、545/545、清理0、残留0、门禁退出码0 |

### 0.4 不变量和失败策略

- 所有公开调用仍通过真实 `POST /网关/调用`，拆分后不得新增进程内测试执行腿。
- 场景内顺序、动态引用、finally 清理、制品前后字节绑定和证据字段逐项保持一致。
- 任一波次失败立即停在当前波次；不保留双实现、旧入口 fallback 或临时转发包装。
- 测试应按新职责导入对应模块；旧测试不得继续依赖 `验证器._私有函数`，CLI 集成测试只验证公开入口。
- 完成判定看真实行为和制品证据，不以文件数量、搬运完成或单元测试总数替代 HTTP 黑盒。

### 0.5 逐文件实施计划（已批准执行）

> 执行方式：当前会话内按任务顺序实施；每个任务先迁移测试导入并观察失败，再搬运唯一实现，验证通过后删除旧定义。禁止先复制后长期双写。

#### 任务1：冻结模型和常量边界

**新增文件**：`开发工具/HTML验证/__init__.py`、`常量.py`、`单步场景.py`、`多步场景.py`、`验证报告.py`。

**精确搬运**：

- `验证器.py:36-48` 常量 → `常量.py`；公开名称保持 `默认并发/并发上限/固定端口/固定端口池/场景契约版本/统一返回字段`。
- `验证器.py:51-243` 的 `验证场景/验证步骤` → `单步场景.py`。
- `验证器.py:247-288` 的 `多步骤验证场景/验证场景束` → `多步场景.py`。
- `验证器.py:292-382` 的 `验证结果/验证报告` → `验证报告.py`。
- `__init__.py` 只导出数据模型和 `主函数`，不执行 CLI、不创建全局服务。

**测试改动**：`测试中心/第一批维修/测试_HTML验证器.py`、`测试_HTML验证器多实例.py` 从对应职责模块导入模型；禁止再从 `验证器.py` 导入模型。

**验证**：`python3.14 -m unittest 测试中心.第一批维修.测试_HTML验证器 测试中心.第一批维修.测试_HTML验证器多实例`；预期46项全部通过。

#### 任务2：拆分制品事实和场景加载

**新增文件**：`制品事实.py`、`场景加载.py`。

**精确搬运**：

- `验证器.py:385-443` 的工作区指纹、制品摘要、启动器定位、严格JSON读取，以及 `446-510/657-688` 的公开能力扫描 → `制品事实.py`。
- `验证器.py:513-654/663-683` 的场景引用解析、安全路径、动态声明、多步骤解析、场景全集校验和场景加载 → `场景加载.py`。
- `场景加载.py` 只依赖模型、常量和 `制品事实.py`；不得导入执行器、进程池或 CLI。

**测试改动**：所有 `_制品全文件摘要/_解析场景引用/_加载场景/_扫描公开能力` 断言改为导入新模块；测试里的 monkeypatch 指向真实定义模块。

**验证**：定向46项；另用只读脚本加载正式激活制品，硬条件为92个正式包、126个场景、408个正向目标能力、545个步骤、差集0。

#### 任务3：拆分HTTP、返回判定、动态值和制品进程

**新增文件**：`HTTP请求.py`、`返回判定.py`、`动态值.py`、`制品进程.py`。

**精确搬运**：

- `验证器.py:691-709/945-982` 的直连地址和发送请求 → `HTTP请求.py`。
- `验证器.py:985-1100` 的类型匹配、路径取值、统一返回和断言 → `返回判定.py`。
- `验证器.py:1103-1147` 的动态值展开 → `动态值.py`。
- `验证器.py:789-942` 的有界输出、管道读取、进程组活跃/回收和制品启动 → `制品进程.py`。
- 进程模块只返回进程、实际端口和有界输出，不解析场景、不写证据。

**测试改动**：地址安全、请求异常、统一返回、动态路径和进程组回收测试分别导入真实职责模块；保留 POSIX 子进程树实测。

**验证**：定向46项；端口启动/回收测试必须证明进程退出、子进程组不存在、端口可再次绑定。

#### 任务4：拆分端口池与场景执行器

**新增文件**：`端口池.py`、`场景执行器.py`。

**精确搬运**：

- `验证器.py:712-786` 的端口池解析、资源键、场景分片和端口探测 → `端口池.py`。
- `验证器.py:1150-1298` 的场景内顺序、动态参数、HTTP调用、finally清理、资源键锁和报告聚合 → `场景执行器.py`。
- `端口池.py` 是 LibreOffice/模型连接器/FFmpeg 资源键唯一事实源；执行器只调用 `场景资源键()`。
- 场景执行器通过构造参数注入请求函数，测试使用显式假请求器，不再 monkeypatch CLI 模块。

**测试改动**：多实例7项端口/分片测试直接导入 `端口池.py`；多步骤执行测试直接导入 `场景执行器.py`。

**验证**：定向46项；独立场景并发峰值≥2，资源键场景同键串行，场景内步骤顺序和finally清理保持不变。

#### 任务5：拆分单实例与多实例应用服务

**新增文件**：`单实例验证.py`、`多实例制品池.py`。

**精确搬运**：

- `验证器.py:1317-1437` 的制品绑定、单实例启动、健康检查和单实例执行 → `单实例验证.py`。
- `验证器.py:1440-1559` 的N实例并行启动、分片子束、报告合并和异常回收 → `多实例制品池.py`。
- 多实例池必须调用单实例验证和场景执行器；不得复制HTTP请求、返回判定、证据或进程回收实现。
- 多实例报告必须以子束能力全集对账，最终再以408能力全量对账；目标能力数、步骤数和资源回收字段必须完整。

**测试改动**：新增2实例真实最小制品测试，验证两个独立端口、场景不重不漏、任何实例启动失败时已启动实例全部回收。

**验证**：定向回归；正式激活制品3实例/端口池45080-45082实测408/545、清理0、残留0。

#### 任务6：拆分证据、验证服务和CLI

**新增文件**：`验证证据.py`、`验证服务.py`。

**精确搬运**：

- `验证器.py:1301-1314/1562-1608` 的前后摘要绑定、证据目录、证据落盘和场景文件生成 → `验证证据.py`。
- `验证器.py:1611-1708` 的验证页、场景JSON、代理和执行验证服务 → `验证服务.py`。
- `验证器.py` 最终只保留 argparse、`主函数`、应用服务选择、统一finally回收、证据保存、汇总打印和退出码映射，目标≤80行。
- 删除 `验证器.py` 中全部旧模型和私有实现；不保留旧名称转发层。

**测试改动**：证据绑定测试导入 `验证证据.py`；服务模式测试导入 `验证服务.py`；CLI测试只通过 `主函数` 和真实命令行参数验证。

**验证**：定向回归；AST检查所有新Python文件≤160行、`验证器.py`≤80行；内容搜索确认无循环导入、无 `sys.path`、无旧私有实现重复定义。

#### 任务7：正式制品与发布收口

**执行顺序**：

步骤 1：`python3.14 客户端/构建平台客户端.py --安装`；
步骤 2：`python3.14 -m 开发工具.HTML验证.验证器 --制品 工程缓存/制品仓库/平台客户端环境/平台客户端 --并发 12 --实例数 3 --端口池 45080-45082`；
步骤 3：`python3.14 -m 开发工具.HTML验证.验证器 --制品 工程缓存/制品仓库/平台客户端环境/平台客户端 --并发 64 --实例数 8 --端口池 45080-45180`；
步骤 4：`python3.14 开发工具/发布门禁/运行发布门禁.py`。

**完成条件**：定向回归全部通过；真实HTTP均为408/408能力、545/545步骤、清理失败0、资源残留0；所有验证实例和45080-45180端口全部释放；发布门禁退出码0；从 `未完成事项.md` 删除AUDIT-012；同步 `项目说明.md` 当前文件结构；`git diff --check`通过。

## 0.6 历史维修工作流（已完成）

上一轮以 `开发文档/未完成事项.md` 的 AUDIT 条目为唯一维修清单，严格禁止降级、旁路、兼容入口和静默兜底；该轮 R1～R5 已完成并通过正式发布门禁。

```text
冻结当前 HEAD、工作区和债务清单
  → 为每个维修点补真实失败回归测试并观察失败
  → 收口唯一测试入口、MCP 包级导入和常驻服务版本
  → 迁移记忆支持库场景到验证场景/v1，批量重算受影响摘要
  → 修复制品摘要生成，清理无绑定旧报告/失效归档候选
  → 删除旧文档口径和废弃入口引用，不保留兼容层
  → 以当前 HEAD 编译唯一制品
  → 真实 HTML 黑盒全量验证
  → 发布门禁与证据读回
```

| 阶段 | 代码/文档落点 | 具体动作 | 进入下一阶段的硬条件 |
| --- | --- | --- | --- |
| R1 | `MCP工具箱/`、`测试中心/`、规则文档 | 统一包级导入和唯一模块测试命令；重启并核对 8766 常驻服务 | 源码与常驻服务对同一命令返回一致；无第二入口 |
| R2 | `支持库/后端/记忆支持库/验证场景引用.json` | 按现行 v1 契约补齐 8 项能力场景，保留正向/负向/清理闭环 | 92 个叶子正式包全部为 v1，场景全集可加载 |
| R3 | `客户端/构建平台客户端.py`、`工程缓存/发布归档/` | 修复 JSON 生成；无绑定报告不得进入正式证据候选；聚合摘要按唯一口径处理 | 新制品全部元数据可解析，来源/摘要/清单一致 |
| R4 | `开发文档/`、`开发文档/项目证据/` | 合并最新事实，删除旧“408/568 已通过”和废弃入口表述；历史账本不改写 | 文档、证据和源码只指向当前唯一链路 |
| R5 | 当前 HEAD 制品 | 编译、HTML 黑盒、发布门禁，按固定顺序写入新证据 | 每个命令真实退出码为 0，制品来源绑定当前 HEAD，才允许发布 |

当前状态：R1～R5 和 HTML 验证器 S1～S7 均已完成并通过真实验证；正式制品已入库、签名并激活，3实例与8实例 HTML 黑盒均为408/408能力、545/545步骤、清理0、残留0，8实例耗时24秒，发布门禁退出码0。验收入口、AGENTS.md 和 MCP 白名单均已收口；当前没有未完成事项。旧失效/悬空归档已清理，历史账本不改写。

## 1. 目标与边界

### 1.1 目标

把当前系统工程平台收敛为易语言式的通用底座：

    确认需求
      → 能力搜索与复用判断
      → 选择基础类型 / 核心支持库原子命令 / 外部 Provider / 模块流程
      → 冻结参数、返回、错误码、状态机、资源预算和依赖
      → 编译唯一契约
      → 包级注册入口装配
      → 运行核心统一调用、租约、超时、取消、回收和证据
      → 模块组合流程
      → HTTP 网关返回统一结果
      → 反向破坏、冷启动、真实失败和零残留验证
      → 发布门禁

最终必须满足：一个能力 id 只有一个 owner、一个契约来源、一个注册路径；一个外部服务/驱动/协议只归属一个支持库；支持库只做原子动作，模块只做领域流程，运行核心只做治理；所有参数和返回使用公共易语言式基础类型；LLM 是可选能力；缺契约、缺注册、缺依赖锁、缺真实返回或缺释放证据的能力不能发布。

### 1.2 本次不做

本计划阶段不新增业务功能，不复制开源项目，不把所有 Provider 一次接入，不把未实现的 MySQL/远程 LLM 写成可用能力，不以“测试通过”替代真实外部验证。

## 2. 当前基线与阻断

| 项目 | 当前事实 | 处理 |
| --- | --- | --- |
| 正式支持库 | 86 个 | 以正式包索引现场生成 |
| 正式模块 | 12 个 | 以正式包索引现场生成 |
| 正式能力 | 408 项 | 声明=注册=搜索=客户端可见，双向差集为0 |
| 当前制品验证 | 当前 HEAD 正式激活制品已完成 32 并发 HTML 黑盒与发布门禁 | 408/408 能力、545/545 步骤通过；清理失败 0、资源残留 0 |
| LLM/ASR | 已有正式能力包和Provider边界 | 继续按当前包声明、依赖和真实返回核验 |
| MySQL | 没有正式包 | 只保留规范占位 |
| 当前未完成债务 | `开发文档/未完成事项.md` 当前为空 | HTML 验证器已按职责拆为单向依赖模块，主入口只做调度 |

## 3. 唯一权威和分层

    开发文档/项目说明.md
      ├─ 开发文档/方案/底座升级方案.md        本文件，唯一执行计划
      ├─ 开发工具/项目编译/正式包索引.py  支持库/模块现场清单
      ├─ 公共契约/基础类型/类型目录.md   类型唯一词典
      ├─ 各包/能力定义.json              能力原始编辑源
      ├─ 各包/包声明.json                包身份和依赖
      ├─ 公共契约/能力契约/契约.py       唯一注册表
      ├─ 运行核心/加载器/生命周期管理/管理器.py  唯一装配
      ├─ 运行核心/能力调用/唯一能力调用.py       唯一调用器
      └─ 开发工具/发布门禁/运行发布门禁.py       唯一发布判定

冲突按上图从上到下裁决。说明书、源码研究、测试输出和历史报告只能提供证据，不能新增 owner。

### 3.1 基础类型

正式名称只允许：逻辑型、字节型、整数型、长整数型、单精度数型、双精度数型、文本型、字节集型、列表型、字典型、日期时间型、资源引用型、句柄型、结果型、空值型。

历史契约中的文本、整数、布尔、浮点数、数字、字典、列表只能由迁移解析器识别，不能继续生成新契约。类型转换属于核心支持库原子能力：

    '123'（文本型） → 类型转换.文本转整数 → 123（整数型）

'12.3' 转整数必须失败，禁止静默截断或四舍五入。

### 3.2 核心支持库

    支持库/
    └── 核心/
        ├── 类型转换/
        ├── 类型校验/
        ├── 数据编码/
        ├── 错误处理/
        └── 资源引用/

支持库/核心是能力域；每个子目录是独立包、独立能力 id、独立契约和独立验证。子支持库只做原子动作，不能编排业务流程。没有包声明.json 的占位目录不计入正式包。

### 3.3 LLM 判定

    节点输入
      → 能否由确定性规则完成？
          ├─ 能：不声明 LLM
          └─ 不能：是否需要生成/理解/分类/抽取/Embedding/重排？
                    ├─ 否：寻找专用支持库
                    └─ 是：声明最小抽象能力，由运行核心选择 Provider

文件读写、执行 SQL、哈希、时间计算、格式转换、状态流转、资源释放不得默认声明 LLM。Ollama、Chat、Responses、Anthropic、本地模型是独立 Provider；模块只声明对话.*、Embedding.*、重排.*，不声明 SDK 包。

## 4. 阶段总表

| 阶段 | 目的 | 主要落点 | 进入条件 | 退出证据 |
| --- | --- | --- | --- | --- |
| P0 | 基线和差异表 | 开发文档/项目证据/底座升级 | 工作树可读取 | 包、能力、入口、测试四表 |
| P0.5 | F5 快速编译与原则检查 | 开发工具/快速编译 | P0 完成 | 当前包可编译、可装配试跑、错误非零 |
| P1 | 类型和能力条目冻结 | 公共契约、契约编译器 | P0 完成 | 严格解析通过 |
| P2 | 核心支持库原子能力 | 支持库/核心 | P1 完成 | 类型转换真实返回/失败 |
| P3 | 包契约和入口收敛 | 支持库、模块包目录 | P1 完成 | 声明=注册=搜索 |
| P4 | 唯一装配和调用 | 运行核心/加载器、能力调用、网关 | P3 样板通过 | 冷启动真实调用 |
| P5 | 资源和状态治理 | 任务、租约、Provider 生命周期 | P4 通过 | 超时取消崩溃零残留 |
| P6 | 外部 Provider 迁移 | 适配层、提供者目录 | P5 通过 | 版本/依赖/隔离/返回 |
| P7 | 模块流程收敛 | 模块库及测试 | P4/P5 通过 | 子能力失败不伪绿 |
| P8 | 数据库、LLM、ASR 分族 | 对应 Provider/契约 | P6 通过 | 独立配置和真实失败 |
| P9 | 全量审计发布 | 测试中心、发布门禁 | P0-P8 完成 | 全部命令退出码 0 |

不得跳阶段。前置证据失效，回到对应阶段。

### P0.5：契约测试编译器与原则检查

这是开发者每次完成一个支持库、子支持库或模块后立即运行的快速入口；IDE 的 F5 可以调用它，但它本身不是 IDE 专属按钮，也不是简单的 py_compile 或全量发布门禁。

**唯一入口（当前实现）**

```text
开发工具/快速编译/运行快速编译.py
```

```bash
# 当前包：契约、类型、入口、注册、依赖和最小装配试跑
python3.14 开发工具/快速编译/运行快速编译.py --包目录 支持库/核心/类型转换

# 当前文件所属包：由路径反推包目录
python3.14 开发工具/快速编译/运行快速编译.py --文件 支持库/核心/类型转换/实现/类型转换.py

# 工作区原则检查：不启动真实外部 Provider
python3.14 开发工具/快速编译/运行快速编译.py --范围 工作区 --不执行外部

# 当前包契约样例和确定性能力试跑
python3.14 开发工具/快速编译/运行快速编译.py --包目录 模块库/文档读取 --执行样例
```

**快速编译的固定顺序**

```text
读取目标包
  → 目录结构检查
  → JSON/能力定义严格解析
  → 易语言式类型检查
  → 能力定义与派生契约摘要检查
  → 参数/返回/错误码/状态机/资源预算闭合检查
  → 包级入口和注册能力 AST 检查
  → 依赖图、依赖锁和 Provider 选择检查
  → 临时注册表装配试跑
  → 确定性能力最小调用
  → 资源释放和临时目录检查
  → 输出结构化报告和退出码
```

**F5 必须直接发现的原则错误**

- 缺包声明、能力定义、能力契约、入口、注册能力或资源预算；
- 使用旧短类型、任意类型、参数顺序漂移、返回结构漂移；
- 声明能力未注册、注册能力未声明、同一能力重复 owner；
- 派生文件不是由当前能力定义生成，摘要不一致或被手工修改；
- 模块直接导入支持库实现、Provider、第三方或实现目录；
- 依赖能力不存在、依赖循环、依赖锁缺失/版本不符；
- LLM 被声明为隐式依赖，或节点声明了不需要的 LLM；
- 统一结果被裸字典、裸字符串、裸句柄或异常替代；
- 确定性失败被空值、空列表、0 或假成功包装；
- 注册表试装配失败、关闭后仍有线程/进程/临时资源。

**真实 Provider 的边界**

F5 默认不下载模型、不启动数据库、不调用远程 API。Provider 缺失时输出 `未验证/外部依赖不可用`，退出码不能是成功；只有明确传入 `--执行外部` 且环境指纹和权限契约满足时，才执行真实 Provider 冒烟。真实 Provider 的超时、取消、崩溃和零残留仍由慢速测试和发布门禁验证，不能由一次 F5 试跑替代。

**固定退出码**

| 退出码 | 含义 | 是否允许继续开发 |
| ---: | --- | --- |
| 0 | 契约、原则检查、装配试跑和适用的样例全部通过 | 可以 |
| 10 | 契约/类型/入口/依赖原则错误 | 不可以，先修当前包 |
| 20 | 注册表装配或唯一 owner 冲突 | 不可以，先修装配 |
| 30 | 确定性样例真实返回失败 | 不可以，先修实现 |
| 40 | 资源释放不完整或发现残留 | 不可以，先修资源治理 |
| 50 | 外部 Provider 未配置或不可达 | 不算绿；只能标记未验证，进入慢速验证 |

F5 报告落在 `工程缓存/快速编译/`，包含包指纹、能力清单、失败文件/行号、退出码、是否执行外部、调用结果和资源残留；工程缓存可删除，不能作为正式事实源。

## 5. 阶段执行细则

### P0：基线和差异表

落点：扫描支持库/**/包声明.json、模块库/**/包声明.json；读取每包 __init__.py、实现、能力契约和测试；写入开发文档/项目证据/底座升级/P0-包能力入口对账.json。

必须有四张表：包表、能力表、入口表、验证表。记录 Git、Python、系统和外部工具指纹。当前基线为86个支持库、12个模块、408项能力；历史基线与现场扫描差异逐项有原因；模板、废弃、重复 owner、未注册均列阻断。

### P1：类型和能力条目冻结

落点：公共契约/基础类型、公共契约/能力契约/契约.py、开发工具/契约编译/聚合契约解析.py、开发工具/契约编译/能力定义编译器.py。

能力原始条目必须包含：能力 id、版本、归属、Provider、参数类型/必填/默认/约束、返回值结构、错误码、状态机、资源预算、调用示例、验证命令。能力定义.json 是唯一编辑源；包声明、参数契约、搜索数据、注册入口、完整性摘要是派生物。缺字段、任意类型、空错误码、自动合成示例和类型漂移直接失败。

验证：

    python3.14 测试中心/开发工具/测试_契约编译搜索.py
    python3.14 测试中心/公共契约/测试_能力契约.py

### P2：核心支持库

第一批只实现支持库/核心/类型转换和类型校验。最小命令：类型转换.文本转整数、文本转长整数、整数转文本、长整数转文本、文本转单精度数、文本转双精度数、数值转文本、文本转布尔、校验类型。

必须验证正常值、空白、非法字符、小数、负号、进制、溢出、NaN、无穷和错误类型。纯计算无第三方、无业务状态、无无界缓存。

### P3：包契约和入口

每个正式包必须有：包声明.json、能力定义.json、能力契约/参数契约.json、能力搜索数据、资源预算、验证场景引用、完整性摘要、包级 __init__.py 和包级使用说明（`说明/使用说明.md`）。

落点：支持库/**/__init__.py、模块库/**/__init__.py、运行核心/加载器/包安装、开发工具/组件合规/合规测试包.py。

必须修复：合规测试回退实现目录；只验证首个能力；模块/测试直接导入实现；契约、入口参数和返回类型漂移。每个声明能力逐项注册、逐项调用、逐项记录；少一项即非零。LLM 不从已移除的本地 Provider 迁移，后续按云端协议支持库重新立项。

### P4：唯一装配和调用

落点：运行核心/加载器/生命周期管理/管理器.py、公共契约/能力契约/契约.py、运行核心/能力调用/唯一能力调用.py、运行核心/统一网关、开发工具/能力搜索/能力搜索器.py。

HTTP、MCP、模块、脚本全部进入唯一调用器。禁止 sys.path、测试预注册和直接导入实现绕过。删声明、注册、契约、锁或实现必须失败。

验证：

    python3.14 测试中心/运行核心/测试_冷启动反向门禁.py
    python3.14 测试中心/运行核心/测试_唯一注册表调用器.py

### P5：运行资源和状态机

落点：运行核心/任务调度/任务系统.py、任务进程.py、资源协调、运行环境管理器/提供者生命周期.py、启动监督器/健康监督.py。（原列的 `运行核心/租约管理.py` 已于 2026-09-15 删除：语义被 `运行核心/权威状态.py` 的持久化租约表 + `资源协调/句柄服务.py` 真实调用链取代）

修复：持久化异常不能继续成功；入队任务必须有终态；关闭必须停止并 join 后台线程；无探针不能健康成功；租约过期、取消、超时、崩溃必须回收。正常、失败、超时、取消、崩溃、重复关闭后，线程、进程、端口、临时目录、句柄和证据必须对账。

### P6：外部 Provider

顺序：标准库 Provider → 文件/文档 → FFmpeg/OCR/ASR → 数据库 → LLM → GPU/浏览器。每个 Provider 补依赖锁、环境指纹、版本探针、权限、资源预算、隔离策略和真实失败验证。原生扩展和不可控全局状态不得进主进程。

### P7：模块流程

顺序：文件管理/网页分析 → 文档读取/文档解析/文档生成 → OCR/图像处理/媒体处理 → 媒体转写 → 自修复工具。

文档读取失败必须透传；文档解析必须解包统一结果并拒绝坏块；自修复必须解包结果；简单窗口必须有界、关闭释放、TTL 回收；模块不得导入 Provider/实现目录。模块必须有自己的流程状态、产物、失败、取消和恢复语义。

### P8：三类高风险 Provider

数据库：MySQL 先真实服务/驱动/权限验证，再建立支持库/适配层/MySQL提供者；PostgreSQL/SQLite 从控制面旁路迁移。每个数据库只保留连接、执行SQL、事务、断开；执行SQL返回列、行、影响行数和最后插入 id；密码只用密钥引用。

LLM：Ollama、Chat、Responses、Anthropic、本地模型独立包；统一对话检查、生成、流式、关闭流；无模型/密钥/端点时如实失败，禁止模拟成功。

ASR：MLX Whisper、Whisper、云 ASR 只替换 Provider；语音识别原子能力与媒体转写模块分离。

### P9：发布

唯一命令：

    python3.14 -m 开发工具.HTML验证.验证器 --制品 <编译产物目录>
    # 多实例制品池：一次部署 N 个制品进程、端口池隔离，实例间零端口竞争
    python3.14 -m 开发工具.HTML验证.验证器 --制品 <编译产物目录> --实例数 3 --端口池 45080-45180
    # 可选：显式覆盖场景级并发，范围 1 到 64；默认按 CPU 动态计算
    python3.14 -m 开发工具.HTML验证.验证器 --制品 <编译产物目录> --并发 32
    python3.14 开发工具/发布门禁/运行发布门禁.py

开发期只运行本次改动对应的明确测试文件或模块化`unittest`入口，不把已退役的测试中心总运行器当作正式发布命令；正式发布只认编译制品真实HTML/HTTP黑盒和发布门禁。

以下任一项直接阻断：零测试、导入失败、未解释跳过、裸返回、空错误码、缺证据、缺依赖锁、错版本/摘要、双 Provider、冷启动装配失败、资源残留、旧缓存缺环境摘要、外部 Provider 未真实执行。

## 6. 工作包顺序

| 编号 | 工作包 | 代码落点 | 依赖 | 验收 |
| --- | --- | --- | --- | --- |
| WP-01 | 包/能力/入口对账 | 开发文档/项目证据/底座升级 | 无 | 四表完整 |
| WP-00 | F5 快速编译器 | 开发工具/快速编译/运行快速编译.py | WP-01 | 当前包原则检查非零可阻断 |
| WP-02 | 类型迁移与严格解析 | 公共契约、契约编译器 | WP-01 | 旧短类型不再生成 |
| WP-03 | 类型转换核心支持库 | 支持库/核心/类型转换 | WP-02 | 转换和失败矩阵 |
| WP-04 | 低风险样板闭环 | 后端/网页解析或文本处理 | WP-02 | 编译、注册、调用、反向删除 |
| WP-05 | 包入口和合规门禁 | 包入口、组件合规、搜索器 | WP-04 | 每项能力真实验证 |
| WP-06 | 唯一装配调用器 | 加载器、注册表、调用器、网关 | WP-05 | 冷启动调用 |
| WP-07 | 运行资源治理 | 任务、租约、Provider、健康 | WP-06 | 零残留 |
| WP-08 | 模块失败传播 | 文档、自修复、窗口、媒体 | WP-06/07 | 失败不伪绿 |
| WP-09 | 数据库 Provider | MySQL/PostgreSQL/SQLite | WP-07 | 连接/执行SQL/事务/断开 |
| WP-10 | LLM/ASR Provider | 适配层、媒体转写 | WP-07 | 无配置失败/真实返回 |
| WP-11 | 全量反向审计 | 发布门禁、测试中心 | WP-00~10 | 破坏全部阻断 |
| WP-12 | 发布收口 | 项目说明、正式索引、证据 | WP-11 | 编译制品HTML黑盒和发布门禁退出码 0 |

工作包只能修改自己声明的目录；依赖包未通过不得开工。

## 7. 交付格式和完成标准

每个工作包回报必须包含：工作包 id、基线指纹、允许目录、能力/文件清单、契约变更、代码变更、验证命令、退出码、真实返回样例、失败/取消/超时样例、资源释放证据、剩余风险。缺任一字段不算交付。

本计划完成的唯一判定：

- 86 个支持库、12 个模块和408项正式能力逐项声明=注册=搜索=客户端可见；
- 类型全部使用易语言式正式名称；
- 核心支持库子包可独立注册、复用且没有第二注册表/网关；
- 数据库、LLM、ASR 的 Provider 边界、配置和失败语义真实可证；
- 节点不会无条件依赖 LLM；
- 唯一装配、唯一调用器、唯一网关、唯一资源治理成立；
- 冷启动、反向破坏、失败传播、取消、超时、崩溃、租约过期、零残留有退出码证据；
- 常规、慢速、全部和发布门禁全部退出码 0；
- 文档数量、路径、owner、状态和证据一致；
- 不存在第二份执行计划或平行路线。

在此之前只能标记“规范已冻结”“实现未验证”或“真实验证通过”，不得笼统称为底座升级完成。
