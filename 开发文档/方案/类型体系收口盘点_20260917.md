# 16 正式类型体系现状盘点（E-8 / B-2 / E-5 执行前置）

> 状态：**已完成（2026-09-19）** —— TY-1/TY-2/TY-3 已落地（提交 `75d66ee1`）；TY-4/TY-5 未做，见 §5.2
> 与 §9「未做完项」。批次顺序以 `执行编排_唯一入口_20260918.md` 为准。

> 本文件曾是**只读盘点**（2026-09-17），数字为当日快照；**2026-09-19 E-8 执行时已按现场重算**
> 并写入 §2 收口表。凡会随并发建包继续漂移的数字，一律只给**口径 + 取证命令**（§1），
> 不把数字当判据读。
> 设计哲学依据：`开发文档/项目说明.md`「类型值的唯一形态（2026-09-16 补）」「逐类型值形态表（唯一口径）」「逻辑型的三层口径（唯一）」，以及哲学第 9 条 9.5「**相对安全，不做绝对安全**」（底座只守「不造成系统性破坏」的底线，其上的边界由调用方传参指定，**任何安全机制都要在宣言与说明书里如实写明是相对安全、不许包装成绝对边界**）。
<!-- 机器管理｜类型：方案｜生成器：开发工具.MD文档生成 -->


## 1. 口径与命令

命令一律在系统根目录执行：`unset PYTHONPATH`、`export PATH=/Library/Developer/CommandLineTools/usr/bin:/opt/homebrew/bin:$PATH`、全线 `python3.14`。

| 用途 | 口径（取证命令） |
|---|---|
| 类型位声明数 | 遍历全部 `能力契约/参数契约.json`，收敛 `能力契约[].参数[].类型` 与 `能力契约[].返回.类型`／`返回.值结构`；**排除 `调用示例` 与被扫描对象的一切参数/默认值上下文**（收敛路径 `$.类型` 分不清「类型位」与「名为『类型』的参数值」，见 §2.4） |
| 校验函数的可复用面 | `公共契约/基础类型/` 各模块的 `校验X类型` / `确保X类型`；对照网关 `类型匹配表` 的键集 |
| 归一表（短名→正式名） | 现场读 `运行核心/统一网关/协议/类型规格.py:类型短名映射` |
| 网关内联判定残留 | `rg -n '_是[A-Za-z]*值|lambda 值:' 运行核心/统一网关/协议/类型规格.py` —— 期望 **0 命中**（E-8 后为委托式，无内联） |
| 网关类型表返潮 | `python3.14 -c "from 运行核心.统一网关.协议.类型规格 import _类型表自检; print(_类型表自检())"` → 期望 `[]`（两档：键越界 / 正式类型缺判定） |
| 能力面 16 型覆盖 | `python3.14 测试中心/支持库/核心/测试_类型校验十六型覆盖.py` → 期望退出码 0 |
| 错误码规范集 | AST 读 `公共契约/错误结构/错误结构.py` 的 `错误码_*` 常量 |
| 错误码两张表 | AST 读 `运行核心/统一网关/协议/错误说明表.py`、`运行核心/统一网关/本地网关.py` 的公开错误码映射 |

> **2026-09-19 现场快照**（仅供量级参考，勿当判据）：136 包 / 701 能力 /
> 参数契约 136 份 / 参数位 2059 处 / 返回位 566 处（其中 `结果型` 564）。

## 2. 16 个正式类型：声明点 / 校验函数 / 真实消费方

判定口径（三档）——**✅ 有实**：`公共契约` 里有独立、可复用、可 import 的校验函数；**△ 半实**：只有 `运行核心/统一网关/网关核心.py:类型匹配表` 的**内联**判定（私有 helper 或 lambda），无公共可复用层；**❌ 有名无实**：连契约参数面都 0 处声明。

| # | 正式类型 | 声明点 | 校验函数（可复用） | 真实消费方（实测） | 契约参数面声明数 | 判定 |
|---|---|---|---|---|---:|---|
| 1 | `逻辑型` | `类型表.py:8` ＋ `逻辑类型.py:20-21`（`真`/`假`） | ✅ `校验逻辑类型`/`确保逻辑类型`/`转JSON值`/`由JSON值` | `类型转换.py:10`（委托校验表）、`支持库/前端/前端契约校验/实现/提供者.py:21`（`真, 假`）、`网关核心.py:543`、`基础类型/__init__.py` | 150 | ✅ 有实（但 `转JSON值`/`由JSON值` 仍 **0 消费**，只被 `__init__` 再导出） |
| 2 | `字节型` | `类型表.py:8`（唯一） | ❌ 无（只有 `网关核心.py:543` lambda） | `网关核心.py:543`（唯一判定点） | 0 | ❌ **有名无实**（契约零使用；全仓仅 6 处字样：模板生成器 2、网关 2、类型表 1、快速编译 1） |
| 3 | `整数型` | `类型表.py:8` ＋ `数值类型.py:25:数值类型定义` | ✅ `校验数值类型`/`确保数值类型`（64 位边界） | `类型转换.py`、`网关核心.py:数值类型名`、`开发工具/HTML验证/返回判定.py:30`、`测试中心/公共契约/测试_数值类型契约.py` | 592 | ✅ 有实 |
| 4 | `长整数型` | 同上（128 位） | ✅ 同 `校验数值类型` | 同上 | 2 | ✅ 有实 |
| 5 | `单精度数型` | 同上（binary32 可编码） | ✅ 同 `校验数值类型` | 同上 | 0 | △ 半实：**契约零声明**，只有校验函数与测试在用 |
| 6 | `双精度数型` | 同上（binary64） | ✅ 同 `校验数值类型` | 同上 | 380 | ✅ 有实 |
| 7 | `文本型` | `类型表.py:8`（唯一） | ❌ 无公共校验函数（`网关核心.py:543` lambda `isinstance(值,str)`） | `网关核心.py:_能力参数错误`（唯一校验点） | 2064 | △ 半实（最高频类型，却没有可复用校验层） |
| 8 | `字节集型` | `类型表.py:8`（唯一） | ❌ 无（网关私有 `_是字节集值`） | `网关核心.py:543` | 68 | △ 半实 |
| 9 | `列表型` | `类型表.py:8`（唯一） | ❌ 无（网关 lambda） | `网关核心.py:543` | 208 | △ 半实 |
| 10 | `字典型` | `类型表.py:8`（唯一） | ❌ 无（网关 lambda） | `网关核心.py:543` | 246 | △ 半实 |
| 11 | `日期时间型` | `类型表.py:8`（唯一；**`数值类型.py` 里没有，也无 ISO-8601 校验**） | ❌ 无（网关私有 `_是日期时间值`） | `网关核心.py:543`（唯一） | 0 | ❌ **有名无实**（契约零使用；全仓仅 5 处字样） |
| 12 | `资源引用型` | `类型表.py:8`（唯一） | ❌ 无（网关私有 `_是资源引用值`） | `网关核心.py:543` | 4 | △ 半实（契约仅 4 处） |
| 13 | `句柄型` | `类型表.py:8` ＋ `句柄体系.py` | ✅ `校验句柄型`/`确保句柄型`/`是合法句柄id`/`生成句柄id` | `类型转换.py:10`、`运行核心/能力调用/HTTP连接器.py:34,85`、`网关核心.py:543` | 2 | ✅ 有实（B-2 的 `确保句柄型` **已存在**） |
| 14 | `结果型` | `类型表.py:8` ＋ `结果类型.py:结果` | ❌ 无公共校验函数（网关私有 `_是结果值`） | `网关核心.py:543` ＋ 全仓 1088 处契约声明 | 1088 | △ 半实（**声明量第二高**，仍无公共校验层） |
| 15 | `空值型` | `类型表.py:8`（**唯一**） | ❌ 无；**`空值` 常量全仓 0 定义点** | `网关核心.py:543` lambda `值 is None` | 2 | ❌ **有名无实**（类型目录写「只能写 `空值`」，但 `空值` 这个名字在代码里不存在） |
| 16 | `JSON值型` | `类型表.py:8`（唯一） | ❌ 无（网关私有 `_是JSON值`） | `网关核心.py:543` | 32 | △ 半实 |

### 2.1 汇总（16 型的实有程度）

| 档 | 数量 | 类型 |
|---|---:|---|
| ✅ 有实（公共契约里有独立可复用校验函数） | 6 | 逻辑型、整数型、长整数型、单精度数型、双精度数型、句柄型 |
| △ 半实（只有网关内联判定） | 7 | 文本型、字节集型、列表型、字典型、资源引用型、结果型、JSON值型 |
| ❌ 有名无实（契约参数面 0 声明） | 3 | 字节型、单精度数型（校验层有、契约零用）、日期时间型 |

### 2.2 全仓 0 命中的校验函数（复跑一致，E-8 数字成立）

实测（逐名 `str.count`，范围 = 除 `__pycache__`/`工程缓存`/`开发文档` 外的全部 `.py`）：

| 函数名 | 命中 | 函数名 | 命中 |
|---|---:|---|---:|
| `校验文本型` | **0** | `校验字节型` | **0** |
| `校验列表型` | **0** | `校验字节集型` | **0** |
| `校验字典型` | **0** | `校验JSON值型` | **0** |
| `校验空值型` | **0** | `校验结果型` | **0** |
| `校验日期时间型` | **0** | `确保文本型` | **0** |
| `校验资源引用型` | **0** | `确保空值型` | **0** |
| `确保结果型` | **0** | | |

对照：`校验逻辑类型` 7 处 / 3 文件；`校验句柄型` 9 处 / 3 文件；`校验数值类型` 28 处 / 6 文件；`确保逻辑类型` 5 处 / 2 文件；`确保句柄型` 3 处 / 1 文件；`确保数值类型` 7 处 / 4 文件。

### 2.3 唯一真实校验点与它的覆盖率

- **唯一校验点**：`运行核心/统一网关/网关核心.py::网关核心._能力参数错误`（项目说明第 6 节明文「只在网关唯一校验点做，不在各能力里重复写」）。
- 该点对 16 型的覆盖：**12 型**走 `类型匹配表`（`逻辑型/文本型/列表型/字典型/JSON值型/字节型/字节集型/日期时间型/资源引用型/句柄型/结果型/空值型`）＋ **4 型**走 `数值类型名` → `校验数值类型`（`整数型/长整数型/单精度数型/双精度数型`）= **16 型全覆盖（等宽）**。
- 契约里声明非正式类型名 → **fail-closed**（`参数不合法：…声明了非正式类型名`）；`_类型表自检()` 在模块级拦住 `类型匹配表` 键越界。
- **能力面 `类型转换.校验类型` 只覆盖 6/16**（数值 4 + 逻辑型 + 句柄型），其余 10 型**明确失败并回带缺口详情**（`目标类型「X」缺公共契约校验层`）。这就是「类型体系有名无实」对外的可见面。

### 2.4 契约里的非正式类型名残余（B-3 复测）

| 文件 | 值 | 说明 |
|---|---|---|
| `支持库/后端/组件规范支持库/能力契约/参数契约.json` | `基础模块` | 包类型写进类型位 |
| `模块库/组件生成/能力契约/参数契约.json` | `基础模块` | 同上 |
| `平台控制面/平台状态/能力契约/参数契约.json` | 空串 `""` | 类型位为空 |

**实测 3 处**（旧 B-3 记 5 处，其中 `模型连接器/{能力定义,参数契约}.json` 的 `文本型（结构事件为 跳过）` **已不存在**，扫描含中文括号的类型位只剩上述空串 1 处）。

**★ 2026-09-18 现场复核更正：上述 3 处全部是误报，实际 0 处真残余。**
逐处读原文确认（不是只看扫描命中）：
- `组件规范支持库/能力契约/参数契约.json`：`:140` 真类型位是 `"类型": "文本型"`；被命中的 `:199` 在 **`调用示例.参数`** 里（`{"类型": "基础模块"}`）—— 那是**参数值**，只因为**参数的名称恰好叫「类型」**才被 `$.类型` 路径表达式捞进来；`:142` 同理（`默认值`）。
- `模块库/组件生成/...`：`:80` 与 `:27` 同一形态（调用示例的参数值 / 默认值）。
- `平台控制面/平台状态/...`：`:447` 在 `调用示例.参数` 里（`"类型": ""`），也是参数值。

**∴ 本条判据须修正**：收敛路径 `$.类型` 不能区分「类型位」与「名为『类型』的参数值」；正确判据应限定在 **`能力契约[].参数[].类型`** 与 **`能力契约[].返回.值结构`** 两类真实类型位，**排除 `调用示例` 与被扫描对象的一切参数/默认值上下文**。
**修正后实测：真残余 0 处** ⇒ 「类型位 ∈ 16 正式表」的门禁**开得起**（不会被这 3 处假红挡住）。

## 3. 类型名归一表：现状与收口（E-8 的一部分）

详见 `/tmp/裸布尔盘点.md` 第 6 节（同一份实测）。要点：

| 项 | 实测 |
|---|---|
| 全仓「纯类型名清单/归一表」（键数 ≥4） | **16 张** |
| 唯一事实源 | `公共契约/基础类型/类型表.py:正式类型表`（16 项） |
| 归一/别名表（短名→正式名） | 4 张：`网关核心.py:520`（18 键）、`能力定义编译器.py:42`（10 键）、`运行快速编译.py:24`（11 键）、`配置校验.py:56`（6 键） |
| 判定表（正式名→校验） | 2 张：`网关核心.py:543`（12 键）、`漂移检测.py:131`（9 键，正式名→Python 注解） |
| 清单表（允许类型） | 8 张：模块模板生成器 16 ×2、技能模板生成器 6 ×2、支持库模板生成器 5、页面模型 5、合规测试包 4、数值类型 4 |
| 与旧口径「5 份 / 键集 19/13/10/11/7」对照 | 18（旧19，`数值型` 已撤除）、12（旧13，同因）、10、11、（7 键表**已不存在**，最接近 `漂移检测` 9 键） |

**收口可行路径（4 步，行为不变量）**：
1. 新增 `公共契约/基础类型/历史短名.py`：并集 18 ∪ 10 ∪ 11 ∪ 6（去重后逐条确认），唯一一份「短名→正式名」。
2. 新增 `公共契约/基础类型/正式名到注解.py`：16 项 → `str/int/bool/float/list/dict/bytes/None/句柄/datetime`。
3. `网关核心.py:543` 改为由第 2 节 TY1 新增的公共校验函数组装；`数值类型名` 改为 `import 数值类型定义`。
4. 其余 8 张清单表一律 `from 公共契约.基础类型.类型表 import 正式类型表`。
**风险**：① 短名并集里 `数字→双精度数型`（快速编译独有）与 `映射→字典型`（网关独有）是单侧映射，合并丢一条就会让旧契约解析失败；② `漂移检测.py` 删本地表后，「未知类型 = 空注解 = 不判漂移」会变成显式失败，可能给既有包**报新漂移**（须与「零新增红」门禁口径对齐）；③ `模块模板生成器` 的表决定**新生成包**的允许类型，改错会批量产错包。

## 4. 错误码膨胀：分层清单

### 4.1 五个事实层的实测规模

| 层 | 定义点 | 实测 |
|---|---|---|
| L1 规范集（**32 条**） | `公共契约/错误结构/错误结构.py` 的 `错误码_*` 常量 | **32** 条：依赖缺失/内部错误/协议错误/参数不合法/参数错误/句柄已过期/句柄无效/外部不可访问/外部未安装/契约不存在/契约版本不兼容/幂等键冲突/循环依赖/提供者不可用/提供者冲突/提供者崩溃/文件不可写/文件不存在/未实现/权限不足/版本不兼容/版本冲突/状态跳转非法/能力不存在/能力重复/请求结构错误/调用已取消/资源未清理/资源释放失败/超时/返回结果不符合契约/限流 |
| L2 契约声明唯一码 | 405 个契约文件（`能力定义.json`/`参数契约.json`/`包声明.json`） | 唯一 **324** 个；总声明 **4270** 处 |
| L2a 其中在 L1 内 | — | **15** 个 |
| L2b **不在 L1 内（膨胀主体）** | — | **309** 个 |
| L3 网关公开错误说明表 | `网关核心.py:83` | **340** 键 |
| L3 网关公开错误码状态映射 | `本地网关.py:36` | **340** 键，与说明表**键集完全相同**（两侧差集均为 0 → 当前零漂移） |
| L3a 说明表有、契约里 0 声明（孤儿码） | — | **16** 条 |
| L4 英文错误码（违反决策 0003「对外错误只走中文」） | 全仓 `.py` | **64 处 / 8 种**：`CALL_FAILED`13、`CALL_TIMEOUT`12、`PERMISSION_DENIED`10、`SESSION_EXPIRED`7、`PROVIDER_UNAVAILABLE`6、`REQUIREMENT_UNCONFIRMED`6、`CAPABILITY_NOT_FOUND`5、`RESOURCE_EXCEEDED`5 |
| L5 同值重复定义点 | 各文件里的 `错误码_*`/`错误_*` 常量 | 唯一 81 个值；**同一值在 >1 文件定义 = 9 值 / 30 点**（`参数不合法` 10 文件、`超时`/`提供者不可用`/`连接失败`/`查询失败` 各 3） |

### 4.2 L2b（309 条规范集外的契约码）再分层

| 分法 | 分层 | 数量 | 处置方向 |
|---|---|---:|---|
| 按**声明包数** | ≥3 包（通用面） | **52** | 逐条裁决升入 L1 规范集（如 `文件损坏`98、`超出限制`138、`提供者崩溃`96、`句柄失效`94、`进程崩溃`68） |
| | 2 包 | **35** | 两包之间归一（谁吞并谁需业务裁决） |
| | 仅 1 包（包内私有） | **237** | 留在包内，但必须显式登记进包声明/能力定义 |
| 按**声明频次** | ≥10 次 | **56** | 事实上的通用码 → 优先升 L1 |
| | 3–9 次 | **93** | 次批 |
| | ≤2 次 | **175** | 单点码 → 保留或并入同义码 |

高频膨胀码（实测声明次数）：`参数不合法`1224（已在 L1）、`提供者不可用`334（已在 L1）、`超时`248（已在 L1）、`文件不存在`150（已在 L1）、`超出限制`138、`文件损坏`98、`提供者崩溃`96（L1）、`句柄失效`94、`进程崩溃`68、`取消`62、`命令失败`56、`超大`54、`内部错误`50（L1）、`格式未知`48、`存储目录不可用`46、`查询失败`38、`目录不存在`38、`仓库不存在`32、`路径越界`30、`生成失败`28。

### 4.3 L4 英文码的落点（分两类，处置不同）

| 类 | 落点 | 性质 | 处置 |
|---|---|---|---|
| 视图层「向后兼容键」 | `开发工具/统一能力入口/视图/{普通用户视图,调用Agent视图,发布者视图,维护者视图}.py` | `错误码建议表` 里与中文键**映射同一句建议**（代码注释明写「历史英文键保留作向后兼容，两套键映射同一句建议，语义不分裂」） | 保留一个版本后删英文键，或改为只读别名；**不要直接删**（测试 `测试_视图错误码键一致.py` 在断言键集） |
| 需求登记域码 | `平台控制面/需求登记/需求错误码.py`（`REQUIREMENT_REQUIRED/NOT_FOUND/UNCONFIRMED/REGISTER_FAILED/QUERY_FAILED/INVALID_PARAMETER`） | 域内码表，键是**中文码**、值是英文码 | 按 B-16 修法汉化（`需求未确认→需求未确认`），或明确降级为「域内内部键、不对外返回」并写进说明书 |
| **已修（勿照抄旧条目）** | B-8/B-16 列的 `INTERNAL_ERROR`/`CAPABILITY_REQUIRED`/`RESTART_LIMIT`/`IMPORT_VIOLATION`/`NOT_REGISTERED`/`CONSUMER_REQUIRED`/`INVALID_CONTRACT`/`HOST_UNAVAILABLE` | — | **本机实测全部 0 处**（已汉化）；`测试中心/平台控制面/测试_工作包11_消费者契约注册表.py:135` 的 `NOT_REGISTERED` 断言也已不在 |

## 5. 收口批次（E-8 的可执行分批方案）

### 5.1 错误码收口批次

| 批次 | 内容 | 文件集合 | 规模 | 验收方式 | 回归风险 |
|---|---|---|---|---|---|
| EC-1 | **冻结唯一权威码集**：L1 规范集 32 条 ＋ 逐条裁决 52 条「≥3 包通用码」升格（升格 = 在 `错误结构.py` 加 `错误码_*` 常量并在网关两张表登记） | `公共契约/错误结构/错误结构.py`、`运行核心/统一网关/{网关核心.py,本地网关.py}` | 32 + ≤52 条 | ① L1 ⊆ 说明表 ⊆ 映射表；② 门禁判据②「映射键集 == 说明键集」仍绿；③ 新增码各有 ≥1 个真实消费方 | **中**：网关两张表是公开错误码的唯一定级点，加错码会让状态码错 |
| EC-2 | **237 条单包私有码登记**：机器生成清单（按包），逐包确认「保留为私有码」还是「并入同义通用码」 | 各包 `能力定义.json`（不改代码） | 237 条 / 约 130 包 | 每个码都能在「实现侧产生 ⊆ 声明侧」判据里找到出处 | 低（只补声明） |
| EC-3 | **L3 孤儿码对账**：16 条「说明表有、契约 0 声明」→ 删除或补声明 | `运行核心/统一网关/{网关核心.py,本地网关.py}` ＋ 相关包 | 16 条 | 两张表键集仍相等；`grep` 该 16 条在全仓 0 命中后删除 | 低-中（可能是历史兼容码；先查测试断言） |
| EC-4 | **英文码归零**：8 种 / 64 处 | `开发工具/统一能力入口/视图/*.py`（4 文件）、`平台控制面/需求登记/需求错误码.py` | 64 处 / 5 文件 | 全仓 `grep -E 'CALL_FAILED\|CALL_TIMEOUT\|PERMISSION_DENIED\|SESSION_EXPIRED\|PROVIDER_UNAVAILABLE\|CAPABILITY_NOT_FOUND\|RESOURCE_EXCEEDED\|REQUIREMENT_'` = 0（或仅剩显式别名表）；`测试中心/平台控制面/测试_视图错误码键一致.py` 同批改 | **中**：视图键集被测试断言，且文件注释声明「保留英文键是刻意的兼容决定」→ **需先裁决** |
| EC-5 | **重复定义点收口**：9 值 / 30 点 | `公共契约/基础类型/文档结构.py`（10 条 `错误_*`）、`公共契约/错误结构/错误结构.py`、`支持库/适配层/psycopg提供者/实现/提供者.py`、`支持库/后端/数据库连接支持库/{SQLite,psycopg}数据库/实现/提供者.py`、`平台控制面/能力反馈/{实现/能力入口.py,开工id校验.py}`、`技能库/技能/{交付收尾,验证编排}/scripts/*.py` | 30 点 | 同一码值只保留 1 个定义点；其余改 `import`；`文档结构.py` 注释自述 3 条无消费方 → 直接删 | 低（B-7 已在台账） |
| EC-6 | **门禁补判据**：① 实现侧产生的码 ⊆ 状态映射码（C-15 盲区）；② 契约声明码 ⊆ 规范集（或显式登记为私有码）；③ 英文码 0 处 | `开发工具/发布门禁/*`、`开发工具/公开调用完整性门禁.py` | 3 条判据 | 反向验证：造一条未登记码 → 门禁 rc≠0；造一条英文码 → 同 | 低-中（判据一开就会出存量红，**必须排在 EC-1~EC-3 之后**） |

> **顺序硬约束**：EC-1 → EC-2 → EC-3 → EC-5 → EC-6；EC-4 独立（需裁决后可并行）。EC-6 若提前，存量红会淹没真信号。

### 5.2 16 类型体系收口批次

| 批次 | 内容 | 文件集合 | 规模 | 验收方式 | 回归风险 |
|---|---|---|---|---|---|
| TY-1 | **补齐缺失的公共校验层**：新增 `空值型` 定义点（`空值` 常量）＋ 9 个 `校验X型`（文本/列表/字典/空值/日期时间/资源引用/字节/字节集/JSON值）＋ 结果型校验 | `公共契约/基础类型/`（现有 5 个模块 → 按类拆：`文本类型.py`/`集合类型.py`/`时间类型.py`/`二进制类型.py`/`JSON类型.py`，或就地扩 `类型目录.md` 所列） | 10 型新增函数 | ① 每个新函数有正/反例测试（`测试中心/公共契约/测试_类型体系契约.py`）；② `类型转换.校验类型` 对 16 型全绿；③ 边界与 `类型目录.md` 逐条一致（如 `空值型` 只接受 `空值`、拒绝空文本） | 低（纯新增，不动既有调用；唯一风险是 `空值 = None` 与「不得用空文本冒充」的口径要在实现与文档同批对齐） |
| TY-2 | **网关改为消费公共校验层**：`类型匹配表` 的 12 个内联 lambda/私有 helper 改为 import TY-1 的函数；`数值类型名` 改为 import `数值类型定义` | `运行核心/统一网关/网关核心.py` | 1 文件 / 12 项 | ① `_类型表自检()` 返回 `[]`；② `测试中心/运行核心/测试_网关类型收口.py` 全绿；③ 反向：故意声明非正式名 → 仍 fail-closed | **中**：唯一校验点，改错等于全平台参数校验失效；必须「先 TY-1 全绿、再切网关」 |
| TY-3 | **能力面补齐**：`类型转换.校验类型` 的 `委托校验表` 从 2 项（＋数值 4 型）= 6 型扩到 16 型 | `支持库/后端/数据操作支持库/类型转换/实现/类型转换.py` | 1 文件 | 16 型逐一真调返回 `成功=true`；反例（`"真"` 传逻辑型、`42` 传文本型…) 返回 `参数不合法` | 低（此前 10 型是「明确失败」，改后变「正确判定」——**行为变化本身要写进说明书**） |
| TY-4 | **归一表 16 张 → 2 张**（见第 3 节 4 步） | `公共契约/基础类型/{历史短名.py,正式名到注解.py}`（新增）＋ `网关核心.py`、`能力定义编译器.py`、`运行快速编译.py`、`漂移检测.py`、`配置校验.py`、3 个模板生成器、`页面模型.py`、`合规测试包.py` | 16 张 / 12 文件 | ① 旧短名契约解析结果逐字节不变（用 `类型转换.文本转逻辑`、网关 `类型短名映射` 的现有测试）；② `漂移检测` 存量包 0 新红；③ 全仓 `grep '文本"' 等短名清单` = 0 | **中-高**：`漂移检测` 由「未知类型不判」变「未知类型失败」会出存量红；模板生成器改错会批量产错包 |
| TY-5 | **门禁补判据**：① 契约类型位 ∈ 16 正式表（含 `空串` 拒绝）；② `类型匹配表` 键 ⊆ 正式表（已由 `_类型表自检` 覆盖，提升为门禁项）；③ 新增包模板类型一律取 `正式类型表` | `开发工具/发布门禁/*`、`开发工具/组件合规/合规测试包.py` | 3 条判据 | 反向：类型位写 `基础模块` → rc≠0 | 中（**前置**：先修第 2.4 节 3 处非正式类型名，否则一开就红） |

> **顺序硬约束**：先修 2.4 的 3 处 → TY-1 → TY-2 → TY-3 → TY-4 → TY-5。TY-1/TY-2 与任务 A 的「批1（唯一事实源+唯一校验点）」是**同一批文件**，必须由同一条线做，否则两条线会写同一个 `网关核心.py`。

## 6. 与旧数字的差异（以实测为准）

| 项 | 旧数字（`未完成事项.md`） | 本机实测 | 判定 |
|---|---|---|---|
| `公共契约/基础类型` 模块数 | 5 个模块 | **5 个 `.py`**（`结果类型/数值类型/逻辑类型/文档结构/类型表`）＋ `类型目录.md` | 一致 |
| `空值` 无定义点 | 无定义点 | **仍无定义点**（全仓 `空值` 仅出现在 `句柄体系.py:96` 的注释里） | 一致 |
| 9 个 `校验X型` 全仓 0 命中 | 0 命中 | **仍 0 命中**（另 `校验结果型`/`确保文本型`/`确保空值型`/`确保结果型` 也是 0） | 一致 |
| 归一表 5 份 键集 19/13/10/11/7 | 5 份 | **16 张**（键数 ≥4）；对应物 18/12/10/11，**7 键表不存在** | 部分无法复现（旧文无路径） |
| 契约唯一错误码 | 323 个 | **324 个** | **+1**；同因下「不在 32 条规范集内」308 → **309**（+1） |
| 不在 32 条规范集内 | 308 个 | **309 个** | +1，与总数差一致 |
| 英文错误码 | B-8/B-16 列 20+15+14+5+… 处 | **8 种 / 64 处**，且 `INTERNAL_ERROR`/`CAPABILITY_REQUIRED`/`NOT_REGISTERED`/`HOST_UNAVAILABLE` 等**已全部 0 处（已汉化）** | **B-8/B-16 部分已修**，残留只在视图层与需求登记域 |
| 契约类型位非正式值（B-3） | 5 处 | **3 处**（2×`基础模块` ＋ 1×空串）；`文本型（结构事件为 跳过）` 已不存在 | **-2**，部分已修 |
| B-2 `句柄型` 两套强度 | 无 `确保句柄型`；`网关核心.py:_是句柄值` 仍 `isinstance(int/str/callable)` → 任意字符串句柄放行 | **`确保句柄型` 已存在**（`公共契约/句柄体系.py:106`）；`HTTP连接器.py:85` 已用 `校验句柄型(句柄)`；网关 `_是句柄值` 对 `str` 已改为 `是合法句柄id(值)`（**但 `callable(值)` 仍直接放行**，属进程内可调用句柄的刻意例外） | **B-2 大部分已修**；残留 1 项需裁决：`callable` 直通是否保留 |
| E-5 验证器类型表 11 键 | `HTML验证/返回判定.py:10-22` 11 键 | **该表已删**：`返回判定.py` 现在 `import 类型匹配表` ＋ `正式类型表`，三段判定顺序写在 docstring 里 | **E-5 已修**（本次复测未发现第二张 11 键表） |

## 7. 无法判定的项

1. **`字节型`/`日期时间型`/`单精度数型` 该不该留在 16 正式表**：三者在契约参数面 **0 声明**。设计哲学要求「16 型等宽校验」，但「零消费的类型」是否算空转（哲学第 1 条 4 项「空转即杀」）**需华哥裁决**：保留则必须补公共校验函数（TY-1 已含），删除则要同批改 `类型表.py`/`类型目录.md`/网关与模板表。
2. **`结果型` 是否需要一个公共校验函数**：它有 1088 处契约声明、却只有网关私有 `_是结果值`。补公共函数的收益（一致性与可复用）与代价（`结果` 类自身在 `结果类型.py`，函数落哪里需定）**需裁决**。
3. **L4 视图层英文键是「刻意的向后兼容」还是「违规残留」**：文件注释明写「历史英文键保留作向后兼容…两套键映射同一句建议，语义不分裂」，这与决策 0003「对外错误只走中文」冲突但理由充分 → **需华哥裁决**（删 / 留别名 / 彻底不改）。
4. **L2b 里 35 条「2 个包共有」的码归属谁**：两包之间的语义差异（如 `文件超限` vs `超出限制` vs `超大`）需要业务语义裁决，机器只能给数量。
5. **错误码「不在规范集内」的判据本身未冻结**：规范集 32 条只存在于 `公共契约/错误结构/错误结构.py` 的常量里，**没有任何门禁把「契约声明码 ⊆ 规范集」作为判据**（EC-6 才要加）。在此之前，308/309 这类数字会随并发建包继续漂移。
6. **`公共契约/错误结构/错误结构.py` 与 `公共契约/基础类型/文档结构.py` 谁是被收口方**：后者注释自述「是声明端契约的镜像」且 3 条无消费方 —— 删除方向需裁决（B-7 已在台账但未处置）。

## 8. 快照与并发警告

- **本盘点期间未写入任何仓库文件**（全部脚本与产物在 `/tmp`）。`git status` 显示 92 个文件 `M` 属**其他并发会话**的存量改动（复盘期间仍在写：`公共契约/句柄体系.py` 13:03、`平台控制面/统一入口.py`、`开发文档/未完成事项.md` 13:00）。
- **漂移已实测**：契约唯一错误码 13:05 复跑仍为 **324**（稳定）；同一时间裸布尔计数复跑为 6612/648（原 6623/657，差额来自并发写入），说明**文件级计数会漂、错误码计数当前稳**。
- 数字取自 2026-09-17 12:50–13:03 实测快照。**动手前必须重跑**：契约类型位/错误码计数（`/tmp/类型_契约消费面.py`）、归一表扫描、`公共契约/基础类型` 目录清单 —— 并发建包会改变计数。
- **只读**：未提交、未跑门禁、未编译、未改任何仓库文件。
- **同一批文件冲突提醒**：`运行核心/统一网关/网关核心.py` 同时出现在本文件的 TY-2/TY-4、任务 A 的「批1」、以及另一个并发会话的改动清单里 → **网关文件必须由单一条线串行修改**，不可两条线并行。

## 9. E-8 收口结果表（2026-09-19 执行，提交 `75d66ee1`）

> 下表的「现场参数位」是 **2026-09-19 当日快照**（并发建包会让它漂），**不作判据读**：
> 要复算请按 §1 的口径与取证命令重跑。档位与落点是结论，数字只是当时的量级。

判定三档口径不变：**✅ 有实** = `公共契约` 里有独立、可复用、可 import 的校验函数；
**△ 半实** = 只有网关内联判定（私有 helper 或 lambda），无公共可复用层；
**❌ 有名无实** = 连契约参数面都 0 处声明。

| # | 类型 | 改前档位 | 改后档位 | 落点（唯一真源） | 现场参数位 |
|---|---|---|---|---|---:|
| 1 | `逻辑型` | ✅ 有实 | ✅ 有实（判定不变；`由JSON值` 改为委托 `校验逻辑类型`） | `基础类型/逻辑类型.py` | 91 |
| 2 | `文本型` | △ 半实（网关 lambda；**最高频类型却无可复用层**） | ✅ **有实** | `基础类型/文本类型.py`（新增） | **1156** |
| 3 | `整数型` | ✅ 有实 | ✅ 有实（判定不变） | `基础类型/数值类型.py` | 313 |
| 4 | `长整数型` | ✅ 有实 | ✅ 有实（判定不变） | `基础类型/数值类型.py` | 1 |
| 5 | `单精度数型` | △ 半实（校验层有、契约零用） | ✅ 有实（**保留 16 型**，见决策 `0024` 第三项） | `基础类型/数值类型.py` | 0 |
| 6 | `双精度数型` | ✅ 有实 | ✅ 有实（判定不变） | `基础类型/数值类型.py` | 203 |
| 7 | `字节型` | ❌ 有名无实（内联 lambda，契约零声明） | ✅ **有实** | `基础类型/二进制类型.py`（新增） | 0 |
| 8 | `字节集型` | △ 半实（网关私有 `_是字节集值`） | ✅ **有实** | `基础类型/二进制类型.py`（新增） | 34 |
| 9 | `列表型` | △ 半实（网关 lambda） | ✅ **有实** | `基础类型/集合类型.py`（新增） | 117 |
| 10 | `字典型` | △ 半实（网关 lambda） | ✅ **有实**（键强制 `str`） | `基础类型/集合类型.py`（新增） | 124 |
| 11 | `日期时间型` | ❌ 有名无实（判定是 `isinstance(str)`，任意文本放行 = 假绿） | ✅ **有实**（ISO-8601 可解析） | `基础类型/时间类型.py`（新增） | 0 |
| 12 | `资源引用型` | △ 半实（网关私有 `_是资源引用值`） | ✅ **有实** | `基础类型/资源引用类型.py`（新增） | 2 |
| 13 | `句柄型` | ✅ 有实 | ✅ 有实（判定不变，委托 `校验句柄型`） | `公共契约/句柄体系.py` | 1 |
| 14 | `结果型` | △ 半实（网关私有 `_是结果值`，**契约声明第二高**） | ✅ **有实** | `基础类型/结果类型.py`（补 `校验结果类型`／`确保结果类型`） | 564（返回位） |
| 15 | `空值型` | ❌ 有名无实（**双重**：无公共校验层 ＋ `空值` 常量全仓 0 定义点） | ✅ **有实** | `基础类型/空值类型.py`（新增，含公共常量 `空值`） | 1 |
| 16 | `JSON值型` | △ 半实（网关私有 `_是JSON值`） | ✅ **有实**（拒绝 NaN／Infinity／非文本键） | `基础类型/JSON类型.py`（新增） | 16 |

**档位汇总**：改前 ✅6 / △7 / ❌3 → 改后 **✅16 / △0 / ❌0**（16 型等宽，无缺宽）。

### 9.1 网关侧：唯一校验点改消费公共层

- `运行核心/统一网关/协议/类型规格.py` 删除 6 个私有判定 helper
  （`_是JSON值`／`_是字节集值`／`_是日期时间值`／`_是资源引用值`／`_是句柄值`／`_是结果值`）
  与 5 个内联 lambda；`类型匹配表` 12 个键**全部改为委托** `公共契约` 的函数。
- `数值类型名` 由 `frozenset(数值类型定义)` 派生（原为手写第二份，增删类型要改两处）。
- `_类型表自检()` 补第二档判据：**缺宽也报** —— `类型匹配表` 键集 ∪ `数值类型名`
  必须恒等于 16 项 `正式类型表`。原判据只管「多」（键越界），不管「少」：
  有人从表里删键、或新增正式类型却忘了登记判定，会静默落进「声明了类型却谁都不校验」的假绿。

### 9.2 能力面：`类型转换.校验类型` 16 型全覆盖

- `委托校验表` 2 键 → **12 键**；连同数值四型，16/16 全覆盖。
- 删除「缺公共契约校验层」失败分支与 `详情={"缺口":"公共契约缺校验层"}` 形态。
- 能力版本 `1.0.1` → `1.0.2`（对外说明变更），契约重编译 ＋ 使用说明／设计说明同步。
- **相对安全（哲学 9.5，必须如实写明）**：`字节集型` 的 base64 文本形态**不校验 base64
  合法性**；`资源引用型` **不校验**「摘要与大小是否真由运行核心签发」。两处都在模块
  docstring 里写明是相对安全，不许被包装成绝对边界。

### 9.3 反向验证三拍（真实输出）

| 判据 | 弄坏方式 | 拍1 必红 | 拍2 还原必绿 | 拍3 sha256 一致 |
|---|---|---|---|---|
| 网关 `_类型表自检` 缺宽档 | 删 `类型匹配表` 的 `"文本型"` 键 | 退出码 **1**，报 `正式类型缺可执行判定（既不在 类型匹配表，也不在 数值类型名）: ['文本型']` | 退出码 **0**，`[]` | `d526788a366f6691…` 一致 ✅ |
| `校验类型` 16 型覆盖 | 删 `委托校验表` 的 `"文本型"` 键 | 退出码 **1**，`AssertionError: Lists differ: [('文本型', '未知目标类型：文本型')] != []` | 退出码 **0**，5 tests OK | `d14317adef2f3875…` 一致 ✅ |
| `日期时间型` ISO 收紧 | `校验日期时间类型` 退回 `isinstance(值, str)` | 退出码 **1**，`AssertionError: True is not false : 日期时间型 的反例 '下周三' 必须判不合格` | 退出码 **0**，5 tests OK | `880e468e02d0c696…` 一致 ✅ |

### 9.4 收口后复核（真实退出码）

| 命令 | 结果 |
|---|---|
| `python3.14 -m 开发工具.全量重算摘要` | 全部闭合（136 包），退出码 0 |
| `python3.14 -B -m 开发工具.开发编译口.编译口 --文件 <13 个改过的 .py>` | **干净通过**，退出码 0（13 个 .py 编译通过；契约漂移判红 0 条；公开调用完整性通过） |
| `python3.14 -m 开发工具.契约编译.漂移检测 --报告` | **判红 0 条**，退出码 0 |
| `python3.14 -m 开发工具.契约编译.对外契约变更判定` | 退出码 0 |
| `测试中心/支持库/核心/测试_类型校验十六型覆盖.py`（新增） | 5 tests OK，退出码 0 |
| `测试中心/运行核心/测试_网关类型收口.py`（真实 HTTP） | 3 tests OK，退出码 0 |
| `测试中心/运行核心/测试_网关协议表唯一性.py` | 6 tests OK，退出码 0（106 秒） |
| `测试中心/运行核心/测试_网关兼容性.py` | 11 tests OK，退出码 0 |

### 9.5 未做完项（如实列出，不粉饰）

- **TY-4 归一表 16 张 → 2 张**：未做。E-8 只处理了「判定唯一真源」这一半；
  「短名归一表跨文件重复」那一半（`能力定义编译器.py:42`／`运行快速编译.py:24`／
  `配置校验.py:56` 等）仍分散。风险与前置条件见 §3。
- **TY-5 门禁补判据**（类型位 ∈ 16 正式表）**未加**：属发布门禁改动，本批并发期
  禁改门禁与制品。`_类型表自检()` 的两档判据已就位，门禁可直接读它。
- **存量参数位未换类型**：`空值型`（1 处）／`资源引用型`（2 处）等仍为存量声明，
  E-8 只补校验层，未逐包裁决把它们换成更合适的类型（那属越界）。
- **`字节型` 参数位仍 0 处**：保留 16 型不删（决策 `0024` 第三项：类型目录是允许集合），
  本批未新增消费方 —— 若长期零使用，仍按决策登记「待观测」。
- **本机存量的「测试文件不插系统根」形态**（`测试_类型转换.py`／`测试_数值类型契约.py`／
  `测试_资源句柄网关.py`）未动：它们需 `PYTHONPATH=.` 才能直跑，按测试入口跑则为绿
  （例：`python3.14 -m unittest 测试中心.支持库.核心.测试_类型转换` → OK）。
  属存量入口形态，**报告不修**。
- **§2 表格的旧数字（`2064`／`1088`／`12 型走类型匹配表` 等）保留原文未逐格回填**：
  它们是 2026-09-17 快照，与 §9 的现场口径并存时以 §9 为准；正文数字按 §2.4 口径重算即可。
