# #55 剩余两个上帝文件拆分方案（2026-09-19 只读勘察）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


**背景**：未完成事项 #55「拆上帝文件」文档登记 12 个 >1000 行文件，装置现场重算
**只剩 2 个**（前序拆分批次已消化 10 个）。本方案只做**落点勘察**，未改任何代码。

**勘察方式**：`ast.parse` 取顶层定义与行号区间（只读，零写入）。

---

## 一、`开发工具/环境自检.py`（1138 行）

**结构**：34 个顶层定义（1 class + 33 def），覆盖 917 行。

**自然分组（按职责聚类，行号连续，可直接按块搬）**：

| 组 | 行号区间 | 内容 | 行数 |
|---|---|---|---|
| **探测组·子进程与进程组** | 313–377, 464–591 | `_探_子进程组启动标志`、`_探_进程存活`、`_探_进程组号`、`_探_按组号探活`、`_探_按组号终止_参数与幂等`、`_探_强制结束子进程_参数与幂等` | ~172 |
| **真跑自检组·进程** | 622–742 | `自检_进程组真跑`、`自检_强制结束真跑` | ~119 |
| **探测组·环境与能力** | 380–461 | `_可列目录`、`_探_内存峰值原始单位`、`_探_要求POSIX能力`、`_探_虚拟环境解释器路径`、`_探_句柄枚举目录表` | ~133 |
| **自检组·解释器与边界** | 173–301 | `自检_解释器与版本`、`自检_平台与架构`、`自检_依赖锁一致性` | ~117 |
| **自检组·平台特性** | 749–893 | `自检_dirfd`、`自检_preexec_fn`、`自检_os_fork`、`自检_中文环境变量名` | ~129 |
| **装配与外部** | 900–1001 | `自检_装配冒烟`、`自检_外部命令` | ~96 |
| **门面与入口** | 96–166, 594–615, 1008–1133 | 数据类 `自检项`、辅助、`自检_收口层能力`、`跑全部自检`、`打印文本`、`主` | ~200 |

**建议落点**（沿用「同名目录包法」，对外符号零变化）：

```
开发工具/环境自检/__init__.py        # 门面：原样再导出全部对外符号（跑全部自检/主/自检项）
开发工具/环境自检/探测_进程组.py      # 探测组·子进程与进程组
开发工具/环境自检/探测_环境能力.py    # 探测组·环境与能力
开发工具/环境自检/自检_进程真跑.py    # 真跑自检组·进程
开发工具/环境自检/自检_解释器与边界.py
开发工具/环境自检/自检_平台特性.py
开发工具/环境自检/自检_装配与外部.py
开发工具/环境自检/入口.py            # 跑全部自检 / 打印文本 / 主
```

**风险**：
- `开发工具/环境自检.py` 是**按路径调用**的脚本（规范里 19 处写 `python3.14 开发工具/环境自检.py`），改同名目录后路径调用会失效 → 要么保留 `环境自检.py` 作薄入口（内部 import 子模块），要么同步改规范里的 19 处。
- 该文件 `_准备导入路径`（134–139 行）做 sys.path 自举 —— **搬迁时必须保证自举仍先于仓库级 import**（2026-09-19 已因同类问题栽过一次，见提交 `9121ca55`）。

---

## 二、`运行核心/能力调用/超时执行.py`（1062 行）

**结构**：18 个顶层定义，其中 **`class 超时执行器` 独占 454 行（587–1040）** —— 单类就是主因。

**自然分组**：

| 组 | 行号区间 | 内容 | 行数 |
|---|---|---|---|
| **执行形态判定** | 190–359 | `执行通道`、`执行形态取值`、`声明执行形态`、`_本平台包id`、`_系统根目录`、`_推断执行形态`、`判定执行形态`、`_形态替身`、`形态快照` | ~170 |
| **准入门面** | 392–544 | `_平台闸门后端`、`_标准库闸门后端`、`_解析准入后端`、`重置准入后端`、`_归一上限` | ~155 |
| **残留账本** | 548–580, 1049–1061 | `残留执行`（数据类）、`残留账本快照`、`线程准入快照` | ~46 |
| **超时执行器本体** | 587–1040 | `class 超时执行器` | **454** |

**建议落点**（按 `split-oversized-module-by-mixin` 技能的既有做法：**冻结 API 的类拆成 mixin**）：

```
运行核心/能力调用/超时执行/__init__.py       # 门面：再导出 超时执行器 / 执行 / 状态快照 等对外符号
运行核心/能力调用/超时执行/执行形态.py        # 执行形态判定组
运行核心/能力调用/超时执行/准入后端.py        # 准入门面组
运行核心/能力调用/超时执行/残留账本.py        # 残留执行 + 快照
运行核心/能力调用/超时执行/执行器_核心.py     # 超时执行器主体（前半：执行/等待/超时结论）
运行核心/能力调用/超时执行/执行器_账本面.py   # 超时执行器的账本相关方法（mixin）
```

**风险**：
- `超时执行器` 是**被网关真实调用的生产类**（"一个挂死的实现会永久占用 HTTP 工作线程" 是它要解决的问题）→ 拆分后必须跑**真实调用**验证，不能只跑语法。
- 该类有线程计数与 `接排空管理器` 的耦合，mixin 边界取「账本/快照」一类**无状态耦合**的方法最安全。

---

## 三、执行顺序建议

1. **超时执行.py 优先**（单类 454 行是明确主因，mixin 拆法已有现成技能与先例）。
2. **环境自检.py 次之**（拆分面更碎，且要处理「按路径调用」的兼容）。
3. 两者都**必须**：
   - `python3.14 -m 开发工具.全量重算摘要` 期望「全部闭合」；
   - 对外符号零变化核验（`开发工具/门面核验.py`，AST 比对）；
   - 定向真跑（不是只跑语法）。

---

**本方案状态**：⏳ 待排期（本轮主线是门禁存量收口，未动这两个文件）。
