# 待重启事项（技能库目录归位）

> 状态：**已完结（2026-09-11 01:2x 执行，2026-09-18 补标）** —— 技能库目录归位已生效，本文件只作历史参考
> 记录时间：2026-09-11
> 记录人：小马仔（华哥授权：可自行决定重启时机，共 3 次重启额度）
<!-- 机器管理｜类型：方案｜生成器：开发工具.MD文档生成 -->


## 一、为什么需要重启

技能库包现在落在 `支持库/后端/技能库/`，华哥要求它**在目录上与支持库、模块库平级**（顶层 `技能库/`）。
但底座装配器的包发现原来**只扫两个根**（`支持库/`、`模块库/`），顶层新根识别不了——
改发现器必须重启网关才生效（热接入用的是进程内的旧装配代码）。

按华哥口径：「等能重启就放好目录就行了」。

## 二、已经做完的准备（代码就绪，重启后生效）

| 文件 | 改动 |
|---|---|
| `运行核心/加载器/包发现/发现器.py` | `发现全部()` 增加可选第三根 `技能库根目录`；不传时行为与原来完全一致（已实测向后兼容：包数 103 不变） |
| `运行核心/加载器/生命周期管理/管理器.py` | `装配系统()` 透传 `技能库根目录` |
| `后端核心/后端核心.py` | 装配、初始化包指纹表、热接入 三处调用都传 `self.系统根目录 / "技能库"` |

**已验证**：模拟系统根（顶层放 `技能库/后端/技能库/`）→ 发现器正确发现包 `支持库.后端.技能库` ✓

## 三、重启时要执行的两步

```bash
# 第 1 步：技能库包搬到顶层（与支持库、模块库平级）
cd ~/Documents/Agent/PHP/系统工程平台
git mv 支持库/后端/技能库 技能库/后端/技能库
python3.14 -m 开发工具.全量重算摘要

# 第 2 步：重启 40007 网关（让发现器新逻辑与顶层目录同时生效）
launchctl kickstart -k gui/$(id -u)/com.huashi.gateway-40007
```

**⚠️ 顺序不能反**：先搬包后重启。若先重启（发现器认顶层了，但包还在支持库下），
技能库仍能被扫到（支持库根照样扫），**但**下一次搬包时必须再重启一次。
一步到位最省重启次数。

## 四、重启后必须验收（逐项）

| 验收项 | 期望 |
|---|---|
| 网关监听 | `lsof -nP -i :40007` 有 LISTEN |
| 健康检查 | `python3.14 运维脚本/热接入.py --查看` → 能力数 **509** |
| 技能库 4 个能力 | 校验 / 运行 / 生成索引 / 扫描技能包 全部可调 |
| 真实执行 | `_示范.纯文本统计` 传 `hello world 华世王镞` → 字符数 16 / 词数 3 |
| 顶层目录识别 | 网关日志/能力目录中技能库包路径为 `技能库/后端/技能库` |
| V3 链路 | `测试中心/模块库/支持库/测试_Skill包结构.py` + `测试_批量导入Skill.py` + `测试_capability_execute强制门禁.py` → **32 passed** |

## 五、重启前必须确认（安全前置）

1. `lsof -nP -i :40007` 没有**活跃在途**连接（长连接保活不算，但要确认对端不在跑任务）
2. **另一个会话（逐字稿任务）不在使用 40007**——2026-09-11 01:2x 实测到 `/tmp/复现500.py`（非本会话脚本）
   连着 40007，**当时决定不重启**，等它结束
3. V3 后端（33000）、MCP 工具箱（8765）、PgBouncer（6432）都在监听 → 重启会短暂影响它们的调用，
   需确认此刻无在途业务请求

## 六、回滚预案（若重启后异常）

```bash
cd ~/Documents/Agent/PHP/系统工程平台
git mv 技能库/后端/技能库 支持库/后端/技能库    # 搬回原位
launchctl kickstart -k gui/$(id -u)/com.huashi.gateway-40007
```

发现器改动是**向后兼容**的（第三根可选，不传即旧行为），所以搬回原位 + 重启即可恢复。
华哥给了 3 次重启额度，正常情况用 1 次。

## 七、重启额度使用记录

| 序号 | 时间 | 用途 | 结果 |
|---|---|---|---|
| 1 | 2026-09-11 01:17 | 技能库归位顶层 + 发现器第三根生效 | ✅ 成功：能力数 509、4 个能力全通、V3 链路 32 passed |

**重启额度剩余：2 次**

## 八、执行记录（2026-09-11）

**第 1 次重启失败的原因（重要教训）**：搬包后**包 id 必须与目录路径同步**改为
`技能库.后端.技能库`，否则装配时报 `No module named '支持库.后端.技能库'`。
第一次重启就是这个原因没起来，改包 id 与 `__init__.py` 导入路径后再重启即成功。

**最终结果**

| 验收项 | 结果 |
|---|---|
| 网关进程 | 67350（LISTEN） |
| 能力数 | 509（与重启前一致，无增减） |
| 技能库 4 个能力 | 扫描 8 个 / 生成索引 7 条 / 校验通过 / 真实执行字符数 16·词数 3（0.032 秒） |
| 包路径 | `技能库/后端/技能库`（顶层，与支持库、模块库平级） |
| V3 链路 | 32 passed |
