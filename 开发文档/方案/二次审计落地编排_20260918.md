# 二次审计落地：证真结果 · 文件冲突矩阵 · 并发维修编排

> 最后更新：2026-09-18 18:0x（审计报告落盘当天）
> 口径：**逐条现场取证**（`文件:行` + 实测命令）；报告值与本文件不一致时以本文件为准
> 维护者：小马仔
> 状态：生效（**唯一编排入口**，本批次的派活依据）

---

## 一、证真结果（第二轮核心交付：报告说的 ≠ 现场）

审计报告 §2–§4 的每条都在当前工作树复核过。**结论：P0/P1 全部成立，P2 有一条路径需修正。**

| 编号 | 报告结论 | 现场取证 | 判定 |
|---|---|---|---|
| **P0-1** | 常驻提供者无跨重启世代账本 | `独立进程.py:280` 用 `平台适配.子进程组启动标志()`（setsid 脱离）；`提供者生命周期.py:101` `self._进程表` 纯内存；`句柄体系.py` 无落库 | ✅ **成立** |
| **P0-2** | 超时排空是双读者竞态 | `提供者生命周期.py:449-479` `select.select` → `readline()` 直读 `进程.stdout`；而 `独立进程.py:144` 后台线程 `os.read(描述符,4096)` 读**同一管道** | ✅ **成立** |
| **P1-3** | 3 秒超时 + 超时即杀进程 | `提供者生命周期.py` 5 处 `_终止并重启`（399/407/414/427/444）；`独立进程.py` 默认调用超时 3.0 | ✅ **成立** |
| **P1-4** | HTTP 每调用一线程 | `超时执行.py:582` `threading.Thread(...).start()` | ✅ **成立** |
| **P1-5** | 持锁做慢 I/O | `任务进程.py:537` `_终止工作器`（join）+ `:542/554` `_持久化`（fsync）均在该文件池锁内 | ✅ **成立** |
| **P1-6** | 文案子串当机器判据 | **实测 8 处**（比报告多 2 处）：`只读状态库.py:54` `"no such table"`、`管理器.py:113` `"重复注册"`、`网关核心.py:909` `"required positional argument"`、`本地网关.py:987` + `流式HTTP.py:581` `"请求id已存在"`、`句柄体系.py:339-346` `"无泄露"`、`权威状态.py:262` `"no such table"/"database is locked"`、**★新发现 `唯一能力调用.py:117` `"缺失" in 说明`** | ✅ **成立（且范围更大）** |
| **P2-7** | 获取锁不抢占过期租约 | `权威状态.py` 租约截止有写入（:884/918）有字段（:423），获锁 SELECT 不读它 | ✅ **成立** |
| **P2-10** | PRAGMA 在行循环里 | `平台控制面/平台状态/状态存储.py:293`（读取记录）、`:301`（查询记录）都在**结果行循环内** | ✅ **成立** |
| **P2-12** | 两条装配腿 | `唯一能力调用.py:588` `创建并绑定`（有 CAS）与 `:626` `_惰性装配`（无 CAS，:646 才进锁） | ✅ **成立** |
| **B4** | 缺优雅停机 | 全仓 grep `signal.signal` / `SIGTERM` **处理器 0 处**（只有注释里的信号语义名） | ✅ **成立** |
| **B5** | 幂等表纯内存 | `网关核心.py:567` `self._幂等表: dict[...]`，`:772` 500 条 FIFO | ✅ **成立** |
| **B1** | 上帝文件 | 实测 8 个共 **7306 行**（报告数字一致）：权威状态 1260 / 本地网关 1164 / 网关核心 1077 / 任务进程 931 / 依赖防火墙 789 / 超时执行 732 / 独立进程 683 / 唯一能力调用 670 | ✅ **成立** |
| **B2** | 无性能基准 | `开发工具/性能基准*` **不存在** | ✅ **成立** |
| **B3** | 只有活探针 | grep `/就绪` / `readiness` **0 处** | ✅ **成立** |
| **§12-E1** | 40007 跑源码树 | plist 实测：`WorkingDirectory = <源码目录>`、`ProgramArguments = python3.14 运行核心/启动运行核心网关.py` | ✅ **成立（最关键的一条）** |
| **§8.2** | 数据面单机信任 | `安全边界.py:29` 默认权限范围固定 `{查询,调用,任务}`；`本地网关.py:814` 服务端写死注入 | ✅ **成立** |
| **P2-2（报告 §4）** | 状态存储文件路径 | 报告未给路径；**真位置 = `平台控制面/平台状态/状态存储.py`**（不在 `运行核心/`） | ⚠️ **路径修正** |

**★ 报告主动剔除的 4 条「疑似问题」我也复核了**：`句柄体系.py` 裸调 `ps`（已先走 `进程终止.进程存活`）、`流式HTTP.py:599` select 对象是 socket（合法）、嵌套同连接事务已修（三处传 `连接=`）、限流槽位无泄漏（finally 配对）—— **剔除正确**。

---

## 二、★ 文件冲突矩阵（这是并发能否并行的唯一约束）

审计哲学条款铁律：**唯一约束是「不要同时改同一个文件」**。按文件而非按问题聚类：

| 簇 | 涉及文件 | 归入的问题 | 并发判定 |
|---|---|---|---|
| **A 提供者族** | `独立进程.py`(683)、`提供者生命周期.py`、`启动监督器/` | P0-1 世代账本+reaper、P0-2 删超时排空、P1-3 提供者侧 | **1 路，串行内部**（三问题同文件，拆开必然冲突） |
| **B 能力调用族** | `唯一能力调用.py`(670)、`超时执行.py`(732) | P1-4 每调用一线程、P1-3 分流点、P2-12 两条装配腿、P1-6⑧ | **1 路** |
| **C 网关族** | `网关核心.py`(1077)、`本地网关.py`(1164)、`流式HTTP.py` | P1-6④⑥、B5 幂等持久化、B3 就绪门 | **1 路**（三问题同文件） |
| **D 状态族** | `权威状态.py`(1260)、`平台控制面/平台状态/状态存储.py` | P2-7 租约、P2-8 CAS、P2-10 三处硬化、P1-6⑦ | **1 路** |
| **E 任务族** | `任务进程.py`(931)、`任务工作器.py`、`任务系统.py` | P1-5 锁外回收/fsync 批量、P2-11 协议口径 | **1 路** |
| **F 句柄收口族** | `句柄体系.py`、`管理器.py`、`只读状态库.py` | P1-6①-③ | **1 路** |
| **G 独立（不碰上述文件）** | 新增 `开发工具/性能基准/`、新增硬件画像 | B2 性能基准、§11-D1/D2 硬件画像 | **可与任意簇并行** |
| **H 文件外（配置/文档）** | launchd plist、`开发文档/` | §12-E1 稳定槽、§8-§11 专题决策记录 | **可与任意簇并行** |
| **★ I 拆上帝文件（B1）** | **A–F 的全部文件** | B1 | **必须最后做**（与所有簇冲突；拆完再动会让所有簇返工） |

**并发上限**：A–F 各 1 路 = **6 路并行**，+ G + H = **8 路**。**B1 单独放最后一波。**

---

## 三、波次排序（按依赖 + 风险，不按报告给的编号）

| 波 | 内容 | 为何这个顺序 | 验收 |
|---|---|---|---|
| **波 1** | **A 簇**：P0-2 删超时排空（先做，风险最小、改动最小）→ P0-1 世代账本 + 启动 reaper | P0-2 是 P0-1 的前置：不删排空，reaper 收回来的进程还会被「超时杀」反复制造 | ① 反验：注释掉 reaper ⇒ 孤儿复现；② 真起模型进程 → kill -9 网关 → 重启 → 断言旧 pgid 被整组回收、账本无残留 |
| **波 1（并行）** | **D 簇**、**E 簇**、**F 簇**、**G 簇**、**H 簇** | 与 A 无文件交集 | 各自定向拒测 + 反向验证 |
| **波 2** | **B 簇**（执行形态分流）、**C 簇**（B5 幂等持久化 + B3 就绪门） | B 依赖 A 的「提供者不再超时杀」；C 依赖 B5 的库表（与 D 簇同库但不同表，可并行） | B：纯计算能力断言**同线程直调**；长耗时断言走任务通道可取消。C：重启网关后重试同幂等键**不重复执行副作用** |
| **波 3** | **B1 拆上帝文件** | 必须等 A–F 全部完工（否则拆完所有簇返工） | 拆完对外门面不变 + 定向测试全绿 |

**§8/§9/§10/§11/§12 的处理**：这五章是**架构级专题**（多租户数据面、成熟度阶梯、语言选型、零配置自适应、开发/交付分离），**不是维修项**。处置：**只落决策记录 + 方案文档，不在本批动代码**（除 §12-E1 属 P0 体验、可立即做，归 H 簇）。

---

## 四、主会话自己的工作项（审计哲学：子代理忙时不许空等）

| # | 工作项 | 为何归主会话 |
|---|---|---|
| 1 | **§12-E1 稳定槽切换**（launchd 从源码树切到激活制品） | 涉及 launchd 重载 + 制品激活，**不可逆且影响在线服务**，必须主会话亲自做并现场验证 |
| 2 | **落 5 份决策记录**（§8 多租户 / §9 成熟度 / §10 语言选型 / §11 零配置 / §12 双槽） | 纯文档，不冲突；**华哥已明确这几章只落决策不落代码** |
| 3 | **P1-6 铁律入哲学**（「文案给人看，错误码给机器判」） | 哲学改动是本轮唯一权威文档变更，需主会话统一改 |
| 4 | **派活前核验**（`开发工具/派活前核验.py`） | 12.3⑩ 要求：派活前用 `git show HEAD:` 核验条目前提仍成立 |
| 5 | **逐个回传现场验收 + 收口** | 不采信自述 |

---

## 五、验收口径（全批统一，不在中途反复跑）

- **维修期**：只做 `py_compile` 语法 + 单文件轻检测（哲学 12.1③）
- **全批完成才跑**：定向 `unittest` → `开发工具/验证场景体检.py` → `全量重算摘要` → 重建制品 → **发布门禁**
- **反向验证必做**（哲学 12.5）：每处修复配「故意弄坏 ⇒ 必须变红 ⇒ 还原恢复绿」
- **唯一正式验收**：HTML 黑盒（真实 HTTP 对编译制品），其余为辅助

---

## 六、诚实声明

- 本文件的**证真记录已是现场实测**；引用的报告数字（26GB/4 孤儿/8.6 tok/s）**未重跑**（属历史现象），已在未完成事项登记。
- §8–§12 五章的**结论采信但不落代码**；若要落，按本文件第三波之后的独立批次。
- **B1 拆文件**风险最高（8 个核心文件、7306 行），排在最后且单独一波。


---

## 七、并发实况（2026-09-18 晚：波 1 已收，波 1b/2 在跑，**验收波已派**）

| 路 | 簇 | 任务 | 记忆包 | 状态 |
|---|---|---|---|---|
| 1 | **A** | P0-1 世代账本 + 启动 reaper + P0-2 双读者 + P1-3 | `二次审计A_提供者世代账本` | ✅ 已提交 `8ceaed5b` |
| 2 | **B** | P1-4 执行形态分流 + P2-12 两条装配腿 | `二次审计B_执行形态分流` | ⏱ 超时但**有成果** → 验收波在收口 |
| 3 | **C** | B5 幂等持久化 + B3 就绪门 + P1-6 网关侧 | `二次审计C_网关幂等与就绪门` | 🔧 重派第 2 版在跑 |
| 4 | **D** | P2-7/8/10 状态层硬化 + P1-6 一处 | `二次审计D_状态层硬化` | ✅ 已提交 `84b1b4cc` + `be2de855` |
| 5 | **E** | P1-5 锁外回收 + P2-11 边界协议 | `二次审计E_任务族锁外回收` | ✅ 已提交 `cf19ddc5`（主会话补） |
| 6 | **F** | P1-6 句柄/幂等/sqlite 三处文案判据 | `二次审计F_句柄文案判据收口` | ⏱ 超时但**有成果** → 验收波在收口 |
| 7 | **G** | B2 性能基准工具（新增目录） | `二次审计G_性能基准` | ✅ 已提交 `adbef399` / `4b2e9ddd` / `091174c5` |
| 8 | **H** | §11 硬件画像 → 档位 → 默认参数 | `二次审计H_硬件画像与档位` | ⏱ 超时但**有成果** → 验收波在收口 |
| 9 | **I** | B7 备份恢复演练（实测 RPO/RTO） | `二次审计I_备份恢复演练` | ✅ 已提交 `3e77becd` + `4bc164ef` |
| 10 | **J** | B8 端到端背压核对（只读审计） | `二次审计J_端到端背压核对` | ✅ 已提交 `61668d13` |
| 11 | **K** | B9 模型二进制供应链 | `二次审计K_模型二进制供应链` | ⏱ 超时但**有成果** → 验收波在收口（并入 O） |
| 12 | **P** | P2-13 五个小点现场核实（只读） | `二次审计P_P2-13五个小点` | 🔧 波 2a 在跑 |
| 13 | **Q** | 配置双入口收口 | `二次审计Q_配置双入口收口` | 🔧 波 2a 在跑 |

**验收波（2026-09-18 晚，`deleg_4525b96e`，5 路）**：超时≠零产出 —— 现场盘点发现 4 路超时路
**都有真实未提交成果**，按「接手在途工作」独立验收 + 补提交；同时落地 M（睡眠唤醒，已与
主会话另建的 `B6` 包**合并为一份**，不留第二腿）。

| 验收路 | 收口对象 | 记忆包 |
|---|---|---|
| 1 | B 簇遗留（`唯一能力调用.py` + `超时执行.py`） | `验收B_能力调用执行形态` |
| 2 | F 簇遗留（句柄/契约/管理器/只读状态库） | `验收F_句柄文案判据` |
| 3 | H 簇遗留（硬件画像/自适应选型/平台适配） | `验收H_硬件画像与档位` |
| 4 | K 簇遗留 + **O 漏项**（select 对管道在 Windows 不可用，3 处） | `验收K_模型供应链与管道残留` |
| 5 | **M = B6** 睡眠唤醒重校准（唤醒检测 + reaper + 租约重判） | `二次审计M_睡眠唤醒重校准` |

**未派（原因）**：
- **L = B4 优雅停机** → 落点在 `本地网关.py`（C 簇在改）→ **已用 `派活前核验.py --查重` 机械确认撞车**，须等 C 收口
- **N = B1 拆上帝文件** → 涉及 A–F 全部文件 → **必须最后单独一波**
- **§12-E1 切稳定槽** → 主会话亲自做，须等源码树干净 + 门禁通过

**★ 本批补的底层（防再犯）**：
- `开发工具/派活前核验.py` 加 **`--查重`**（抓同文件撞车 + 报「未声明允许修改段」的包）与 **`--落点`**（对 HEAD 核验 `路径:行号`，报失效/漂移）
- `规范/多会话并行开发规约.md` **§9.4** 立任务包格式硬要求：必须有可解析的「允许修改」段；同一审计条目只保留一份包
- 实测教训：主会话另建 `B6` 包时 `二次审计M` 早已存在、`验收K` 含 O 段时 `二次审计O` 早已存在 —— **同一件事两份包 = 第二腿**

## 八、主会话并行工作项（子代理忙时不空等）

| 项 | 状态 |
|---|---|
| 6 份决策记录 0041–0046 落盘 | ✅ |
| 哲学 **14.4**「文案给人看，错误码给机器判」立规（14 主条 / 60 子项，用满余量） | ✅ |
| `未完成事项.md` 新增 **§8.7**（21 条，41–61） | ✅ |
| `主开发文档.md` 登记本批编排 + 0041–0046 | ✅ |
| `派活前核验.py` 对 10 个目标文件查 git 历史（确认无派空活） | ✅ |
