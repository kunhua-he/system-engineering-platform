# 说明书存量治理施工表（第 23 项）

> 状态：**生效（2026-09-18）** —— 按此执行；批次顺序以 `执行编排_唯一入口_20260918.md` 为准

> 依据：`开发文档/项目证据/说明书白名单.json`（G1 复核已完成）+ `开发文档/规范/新建支持库与模块指南.md` 说明书治理节
> 前置状态：**G1 白名单已完成**（2026-09-18），本表可执行
> 铁律：**禁止对含实质漂移的包裸重生成**（见本表第 G5/G6 格的禁令原文）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


---

## 一、施工前的三条硬前提（违反即返工）

1. **白名单是文件级、不是段落级**。`B混血包` 6 份含人工手写区，
   将来重生成**必须先做行级合并**（保手写行、刷生成行）—— 这是第 37 项，未做前**不得重生成这 6 份**。
2. **两支生成器并存且各有产区**：
   - **G1** = `开发工具/说明书生成/说明书生成器.py`（输入 `包声明.json + 能力契约/参数契约.json`）
   - **G2** = `支持库/后端/组件规范支持库/实现/说明书生成器.py`（输入 `能力定义.json + 参数契约.json + 能力搜索数据.json + 验证场景引用.json`）
   - G2 **拒绝覆盖已有文件**（`校验输出目标` 抛 `FileExistsError`）—— 这是**有意设计**，不是缺陷。
3. **改说明书前先确认它的血统**（用 `开发工具/契约编译/生成物一致性检查.py` 或下表 G1 格的命令），
   否则会把人工内容抹掉（历史上真发生过：`FFmpeg媒体` 的 `返回字节` 曾被误判为"多余"）。

---

## 二、13 格施工表（按依赖排序，▷ 表示可并行）

| 格 | 对象 | 前置 | 动作 | 取证 / 校验命令 |
|---|---|---|---|---|
| **G1** | 全仓说明书血统 | — | **已完成**（白名单 34 条、计数 22 项） | `python3.14 -c "import sys,json;sys.path.insert(0,'.');w=json.load(open('开发文档/项目证据/说明书白名单.json',encoding='utf-8'));print(w['计数'])"` |
| **G2** | `生成物类` 116 份 | G1 | 本期**不动**（逐字同生成器，重生成无收益） | 抽查：`python3.14 -c "from 开发工具.契约编译.生成物一致性检查 import *"`（见该模块 `说明/`） |
| **G3** ▷ | `B混血包` 6 份的行级对账 | G1 | **✅ 已完成（2026-09-19）**：按 §三 口径实跑 9 份（6 混血 + 2 词汇行改写 + `FFmpeg提供者`），对账表见下方「六、G3 实测对账结果」 | `python3.14 /private/tmp/系统平台临时脚本_20260918/G3对账_20260919.py` |
| **G4** | `B混血包` 6 份的合并式重生成 | G3 | **✅ 已完成（2026-09-19）**：9 份全部按行级合并（保手写行、刷生成行），其中 **2 份有改动**（`文件系统支持库/文件操作` 12 行陈旧生成行刷新 + 标题/说明行刷新；`代码解析支持库/代码地图` 补回 `## 能力` 小节行），**7 份本就已是固定点（零改动）**。合并后 `生成物一致性检查` 判为「白名单内（手工维护）」 | 合并器：`python3.14 /private/tmp/系统平台临时脚本_20260918/G4行级合并.py [--写盘]`；判据：`python3.14 开发工具/契约编译/生成物一致性检查.py <包>` |
| **G5** | `FFmpeg媒体` / `FFmpeg提供者` | G4 | **✅ 已核（2026-09-19）**：两包实跑为**零改动**（`FFmpeg媒体` 现行 87 行 == 账 87 行，仅 `错误码` 行分隔符差异；`FFmpeg提供者` 65 行同）。`返回字节` 在生成器产出中**同样存在**，无人工行丢失风险 —— 与白名单「G1 复核更正」一致 | 逐行 diff：见 G4 合并器逐包报告 |
| **G6** | 其余 `人工类` 25 份 | G1 | **不重生成**（白名单登记即终态） | `说明书白名单.json` 里 `处置=生成器不得覆盖` |
| **G7** ▷ | 新增包与白名单的**联动机制** | G1 | **✅ 已完成（2026-09-18）**：真因＝白名单**数组只有 34 条**，而**计数明细列出 12 份「生成物类含人工改写行」**，其中 **7 份不在数组里**（有漂移但未登记）—— 这就是"新建包与登记无联动"的具体后果。已**补登记 7 条**（技能库/桌面宿主/浏览器宿主/模型连接器/进程管理/请求/FFmpeg提供者），白名单合计 **41 条** | 见 G1 复核结论「被漏登原因」：`项目文档支持库` / `模块库/项目文档` 曾因"新增包与白名单登记没有联动"漏登 |
| **G8** ▷ | 6 个聚合父包 | G1 | **✅ 已完成（2026-09-18）**：真因＝**说明书落后于源**（不是「非生成物」）——生成器对聚合父包**能正确产出**（源里新增子域会补进去），现行 6 份只是早期生成的旧版。已**重生成 6 份**（办公文档 153→274 行、大语言模型 406→560、数据操作 538→681、文件系统 219→274、系统核心 494→868、网络通信 76→87），**逐行核对零人工内容丢失**（丢的 266 行全是旧模板生成格式），**重生成后可复现 6/6**；发布门禁通过。★ 附：**发现器判据与注释不一致**（注释写「形如 4 部分」，代码写 `len(相对部分) >= 3`，把聚合库自身也跳过）→ 实测按注释口径纳入聚合库自身会**炸出 244 条能力 id 重复**（聚合库能力 = 子域能力并集），故现行跳过**意图正确**，只登记不一致、不改判据 | `grep -rl 调用口径以本文为准 --glob "**/说明/使用说明.md"`（★ 必须带 `**/` 前缀，缺前缀返回 0） |
| **G9** | `生成物类·词汇行已人工改写` 2 份 | G1 | 与 G3/G4 同法（行级合并） | 同 G3 |
| **G10** ▷ | 说明书版本字段 | — | 与 `能力契约/参数契约.json` 的版本/错误码**逐条对账** | `python3.14 -m unittest 测试中心.开发工具.测试_公开调用完整性门禁`（该测试已覆盖「说明书-包级/能力级版本不一致」「说明书-缺错误码」「说明书-缺参数」） |
| **G11** | **说明书与白名单的联动门禁** | G7 | **✅ 已完成（2026-09-18）**：新增门禁项 **`防回潮·说明书白名单联动`**（`校验说明书白名单联动`），取「干净通过」态、基线 0；**判据只调** `开发工具/契约编译/生成物一致性检查.py`（`--全部 --json`），不另写扫描；已接进发布门禁必经路径（防回潮循环第 6 项） | 参考既有范式：`防回潮·占位代称` 的接线写法 |
| **G12** | G11 的反向验证 | G11 | **✅ 已完成（2026-09-18）**：删掉白名单里一条已登记（`系统核心支持库/进程管理`）→ 门禁**红且精确点名**该路径、给出修法 → 还原 → **绿**（三段式成对） | 反向验证三段式：破坏前绿 → 破坏后红 → 还原后绿 |
| **G13** ▷ | 本表归档 | 全部 | 全部落地后，本表并入 `开发文档/规范/新建支持库与模块指南.md` 说明书治理节，删除本临时件 | — |

---

## 三、G3 行级对账的可执行做法（6 份 B 混血包）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
# 逐个包：先拿 G1 产出，再与现行逐行 diff
python3.14 - <<'PY'
import sys, json, difflib
sys.path.insert(0, ".")
from pathlib import Path
from 公共契约.包声明.声明 import 从字典构建
from 开发工具.说明书生成.说明书生成器 import 生成单包说明书

包表 = [  # 6 份 B 混血包（血统细分含「G1 + 人工手写区」）
    # 从白名单动态取，避免硬编码漂移
]
w = json.load(open("开发文档/项目证据/说明书白名单.json", encoding="utf-8"))
包表 = [x["路径"].rsplit("/说明/使用说明.md", 1)[0] for x in w["白名单"]
        if "人工手写区" in str(x.get("血统细分", ""))]

for 包 in 包表:
    p = Path(包)
    d = json.loads((p / "包声明.json").read_text(encoding="utf-8"))
    账 = 生成单包说明书(从字典构建(d, 来源路径=str(p / "包声明.json"))).strip().splitlines()
    现 = (p / "说明/使用说明.md").read_text(encoding="utf-8").strip().splitlines()
    手写行 = [l for l in 现 if l not in set(账)]
    生成缺行 = [l for l in 账 if l not in set(现)]
    print(f"{包}: 手写/差异行 {len(手写行)} 行；生成器有而现行无 {len(生成缺行)} 行")
PY
```

**输出即 G3 的对账表**；G4 按它做行级合并。

---

## 四、为什么本表当初不直接执行（诚实交代）

- **G4/G5 是高风险动作**：`B混血包` 6 份含人工手写区，行级合并没有自动化依据时**极易丢内容**。
  本轮已把**对账做法写死**（§三），但**行级合并的逐行判定需要人工确认** —— 当时只能由华哥验收。
- **G7/G11 是机制建设**：新建包与白名单的联动当时**不存在**（G1 复核明确记载了漏登实例）。
  补它属"补底层"（哲学 14.3），但会改发布门禁，当时不擅自接线。**G7/G11/G12 已于 2026-09-18 落地。**
- 因此本表把 **13 格全部拆到"可执行 + 有命令 + 有前置"**，把**需要决策或高风险的两格**留给华哥。
- **2026-09-19 收口**：G3/G4/G5/G9 已用**机器可复核的字段键配对规则**（§七）完成，不再依赖逐行人工拍板 ——
  规则对每份都给出逐行判定（保/刷）与「人工行零丢失」自检，反向验证三拍成对（见 §七「校验」）。

---

## 五、状态

| 格 | 状态 |
|---|---|
| G1 | ✅ 已完成（2026-09-18，白名单 34 条 / 计数 22 项；2026-09-19 补登至 42 条） |
| G2 / G6 | ✅ 判定不动（有依据） |
| G10 | ✅ 已由 `测试_公开调用完整性门禁` 覆盖并转绿（48/48 OK） |
| G3 | ✅ 已完成（2026-09-19，9 份对账表见 §六） |
| G4 | ✅ 已完成（2026-09-19，9 份合并式重生成：2 份有改动、7 份零改动，见 §七） |
| G5 | ✅ 已核（2026-09-19，两包零改动，`返回字节` 无丢失风险，见 §七） |
| G9 | ✅ 已完成（2026-09-19，2 份词汇行改写包按同一合并器处理，均零改动） |
| G7 / G11 / G12 | ✅ 已完成（2026-09-18，补登记 7 条 + 门禁接线 + 反验成对） |
| G8 | ✅ 已完成（2026-09-18，重生成 6 份 + 可复现 6/6） |
| G13 | 📋 归档动作，最后做 |

---

## 六、G3 实测对账结果（2026-09-19 实跑，9 份）

脚本 `/private/tmp/系统平台临时脚本_20260918/G3对账_20260919.py`（口径同 §三，范围含 6 混血包 +
2 份 `生成物类·词汇行已人工改写` + `FFmpeg提供者`）。**合并前**实测：

| 包 | 现行行 / G1账行 | 手写/差异行 | 生成器多出 |
|---|---|---|---|
| `支持库/后端/数据操作支持库/数据交换` | 110 / 110 | 0 | 0 |
| `支持库/后端/文件系统支持库/文件操作` | 233 / 230 | 14 | 12 |
| `支持库/后端/文件系统支持库/文本补丁` | 58 / 43 | 12 | 0 |
| `支持库/后端/测试支持库` | 93 / 43 | 41 | 7 |
| `支持库/适配层/密码签名提供者` | 91 / 87 | 4 | 0 |
| `模块库/组件生成` | 82 / 26 | 50 | 5 |
| `支持库/后端/代码解析支持库/代码地图` | 31 / 32 | 4 | 5 |
| `支持库/后端/媒体处理支持库/FFmpeg媒体` | 87 / 87 | 1 | 1 |
| `支持库/适配层/FFmpeg提供者` | 65 / 65 | 1 | 1 |

---

## 七、G4/G5/G9 合并式重生成（2026-09-19 实跑）

合并器 `/private/tmp/系统平台临时脚本_20260918/G4行级合并.py`（默认干跑，`--写盘` 才落盘）。
合并规则（可复核、非人工逐行拍板）：现-only 行与账-only 行按**字段键**配对 —— 无同键配对者为
**人工行**（保）、现值 ⊊ 账值者为**陈旧生成行**（刷）、值空且下方接缩进/围栏者为**人工块表头**
（保）、其余为**人工改写行**（保，丢弃账的同键对照行）。**空行一律取现行**（人工代码块内空行
不得被账吞掉）。标题行恒取账。

**结果：9 份里 2 份有改动、7 份零改动**（后 7 份已是该规则的固定点）：

| 包 | 改动 |
|---|---|
| `支持库/后端/文件系统支持库/文件操作` | 12 行陈旧生成行刷新（`危险路径护栏` 补句 + `必填参数：` → `无`）+ 标题 `文件系统` → `文件操作` + 包说明行刷新 |
| `支持库/后端/代码解析支持库/代码地图` | 补回 `## 能力` 小节行（1 行） |
| 其余 7 份 | 零改动（含 `FFmpeg媒体` / `FFmpeg提供者`，G5 两包人工行零丢失） |

**校验**：`人工行零丢失=True`（全 9 包，含首行非空/空行数不超现行/末行非空三条结构纪律）；
`生成物一致性检查` 4 包定向 0 不一致；`--全部` 142 包 1755 项 0 不一致；`全量重算摘要` 全部闭合；
反向验证三拍成对（弄坏必红 / 还原必绿 / sha 一致）；**幂等**：重跑合并器「有改动 0 包」。

---
