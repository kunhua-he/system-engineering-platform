# MCP 与能力网关升级总方案（A–F 全层）

> 最后更新：2026-09-21
> 口径：**只读盘点 + 方案**。所有「现状」为现场读源码取证（带 `文件:行`）；所有「落点」已在代码里核实存在。未改任何代码。
> 维护者：总方案（合并《MCP优化调研》12 份、《MCP接入面瘦身与网关协议对齐方案》、《C层升级方案》）
> 状态：起草
<!-- 机器管理｜类型：方案｜生成器：开发工具.MD文档生成 -->

---

## 零、判据（唯一，且有先后）

华哥 2026-09-21 定：
> *"我们一直的口径都是这个，你不要说是否收益最高，而是**是否足够稳定，足够高效**，这个才是评判的标准。"*

**判据只有两条，且顺序不可换：**

| 顺序 | 判据 | 硬检查项 | 不达标怎么办 |
|---|---|---|---|
| **先** | **稳定性** | ① 不引入新的不确定性（并发/时序/双写/双轨）② **只增不改**既有默认行为 ③ 一处改动、**可单点回退** ④ **无假绿空间** ⑤ 不新增第二腿/映射层/兼容壳 | **直接排除或降级 —— 不管它省多少往返** |
| **后** | **高效性** | ① 减少**往返次数**（首要）② 减少单次耗时 ③ 更容易**一次命中** | 过不了稳定性，高效性再高也不做 |

**为什么稳定必须在前**：不稳定的改动会把「一次往返」变成「反复排查」，**高效性直接变负**。

### 0.1 ★ 声明：历史报告的「性价比」排序一律作废

本方案覆盖以下三份既有产物的排序口径：

| 产物 | 状态 |
|---|---|
| `开发文档/分析/MCP优化调研/最终审计报告_MCP与能力网关.md` | 已在 §1.1 加判据纠正，§四 已按新判据**重排** |
| `开发文档/方案/C层升级方案_热重载与重启_20260921.md` | 本方案吸收其全部内容；其「最该先做」「代价最小」措辞以本方案 §二 为准 |
| `开发文档/方案/MCP接入面瘦身与网关协议对齐方案_20260921.md`（zcode） | 其 L1–L11 逐条归属见本方案 §三；**「批次」顺序按本方案 §四 重排** |

**凡本仓文档里出现的「最高性价比」「性价比排序」「代价最小」，一律不是判据。**

---

## 一、按新判据重审：**翻案 4 条**（先回答「方案有没有漏洞」）

把旧稿按「性价比」排的清单拿新判据逐条重审，**4 条翻案**：

| # | 旧判（性价比口径） | 新判（稳定+高效口径） | 翻案理由（稳定性不达标） |
|---|---|---|---|
| **1** | **A1 并发** 排 P0 首位，理由「省往返最多」 | **降到 P1**，且必须改成 **fail-closed** 才允许做 | 并发后**资源租约冲突从「串行天然规避」变成「必须逐能力显式声明」**，745 个能力漏标一个就撞车 ⇒ **引入新的不确定性**。改法：`可否并行` **缺省假**，只有显式声明才并行（见 §二 A1） |
| **2** | **E1「同层目录优先」排序** 列为「代价 1 行」的小改动 | **删掉，不做** | **会改变输出字节** —— `目录树` 契约声明了 `排序稳定: true`，任何对树文本做逐字快照的测试/场景会红 ⇒ **只增不改**这条不达标 |
| **3** | **C2-② 消除真空期**（`SO_REUSEPORT`）列为「二选一」 | **明确排除** | **引入双进程同时持端口的新时序不确定性**；且 `docker/compose` 源码明写滚动更新只在 Swarm 有 ⇒ 「简单重建有中断」是业界常态，**接受真空期代价 0** |
| **4** | **D2 `verify-generated` 三行语义** 称「最高性价比的一条」 | **保留，但加硬前置** | **前置：重算必须幂等** —— 不幂等则判据自己抖（每跑一次都报差异）⇒ **不幂等就不做** |

**另有 2 条加了「稳定性门槛」**（不是翻案，是加护栏）：
- **C1 第 4 条判据**（`gc.get_referrers`）：前置「**先实测耗时**」（本项目有单命令 15 秒硬上限），超阈值则改为只告警或采样检测。
- **F1 返回体量列**：要动**权威账本**（`安全审计.jsonl`）⇒ 拆两步，先只加**分位**（纯计算，零风险）。

---

## 二、全层方案（每条**只选一种做法**，并给稳定性 / 高效性论证）

### A 层 —— 往返次数与签名自描述

#### A1 薄壳一次只许调 1 个工具

| 项 | 内容 |
|---|---|
| **选定做法** | 薄壳加 **`工具清单`**（列表型，只增）→ 能力注册表加 **`可否并行`（缺省 `假`）** → 调度层**读写门闸**（可并行拿读锁、其余拿写锁）→ 结果**按提交顺序**回灌 |
| **为什么不选「缺省并行」** | 缺省并行会让 745 个能力里**所有写类瞬间进入并发**，撞车概率爆炸。**缺省假 = fail-closed**：不声明任何能力时，行为与今天**逐字一致**（零风险上线）；只有显式声明过的能力才并行 |
| **稳定性** | ✅ 默认行为不变（`可否并行` 缺省假 ⇒ 现状就是串行）；✅ 写类漏标 ⇒ 仍串行，**不会撞车**；✅ 旧 `工具` 字段保留为单元素 ⇒ 旧调用方无感；✅ 门闸是单点，可回退 |
| **高效性** | ✅✅ **直接砍往返次数**：一次 turn 收多个能力。这是本方案里唯一能减少**「同一轮内」**往返的一条 |
| **外部依据** | `openai/codex`：`router.rs:233-237` `.unwrap_or(false)` **缺省不并行**；`parallel.rs:124,179-183` `Arc<RwLock<()>>` 读锁并行/写锁独占；`turn.rs:2503` `FuturesOrdered` 按提交顺序回灌；`registry.rs:520` 逐工具声明。MCP server **默认串行、需显式 opt-in**（`rmcp_client.rs:2186` vs `:2408`）。**协议层批量是官方删掉的**（`2025-06-18/changelog.mdx:12`）⇒ 客户端自己拼是唯一路 |
| **落点** | `开发工具/薄壳/工具清单.py`（工具定义 + `工具清单` 入参）、`开发工具/薄壳/薄壳服务.py`（调用入口 + 门闸）；能力注册表侧 `可否并行` 字段落点待 §七 回填 |
| **判据** | ① 不传 `工具清单` 时行为与今天**逐字一致**；② 传 3 个可并行能力 ⇒ 总耗时 < 串行；③ 传 2 个写类能力 ⇒ **串行执行**（门闸生效） |

#### A2 签名不自描述 → 第一跳必错

| 项 | 内容 |
|---|---|
| **选定做法** | **不动工具描述长度**；改**错误体四段式**（错误码 + 原文回显 + 候选签名 + 规则原文），见 B2 |
| **为什么不选「在描述里补更多参数说明」** | 工具描述是**常驻注入**（每次访问都付），错误体是**按需付**（只在失败时付一次）⇒ 高效性上错误体更优；且描述加长与 §三 的瘦身方向**相反** |
| **稳定性** | ✅ **只改错误分支，成功路径一字不动** |
| **高效性** | ✅ 省掉「猜 → 失败 → 再猜」的往返（实测**猜参数名错 3 次**） |
| **外部依据** | `Aider-AI/aider` `editblock_coder.py:84-114` 四段式 + `find_similar_lines` → "Did you mean…" + `base_coder.py:100` 硬上限 3 次反思 |
| **落点** | 网关错误体构造（与 B2 同处，待 §七 回填） |
| **判据** | 传一个写错的参数名 ⇒ 返回体里出现**候选正确签名**（多候选全列，不猜） |

#### A2b 枚举取值不可见（★ 本层最该先做）

| 项 | 内容 |
|---|---|
| **选定做法** | **能力契约补 `可选值` 字段**（机器可读枚举），**不在描述里写取值散文** |
| **为什么这是最该先做的** | 现场核实：**薄壳自己 3 个工具的 schema 已经有 `enum`**（`工具清单.py:71` `细节级别` 三档），而 **745 个能力一个都没有** ⇒ 字段形态**已被本项目自己验证过**，只是没下沉到能力层 |
| **稳定性** | ✅ **纯新增字段**，旧调用方无感；✅ 走既有契约编译派生，不新造机制 |
| **高效性** | ✅✅ **一次命中**（不用试错取值）。枚举式参数（`细节级别`/`模式`/`冲突策略`/`值类型`）现在是「参数名对了、值写错、白跑一趟」 |
| **外部依据** | **两条互不相干的权威同时要求**：MCP 官方 Registry `Input.choices`（*"user **must** select one of these values"*）+ OpenTelemetry `gen_ai.tool.definitions` 的 JSON Schema `enum`（**MUST** 遵循）。zcode §3.1 给了性价比数字：**参数描述约 +300% token，而 enum + default 只要 +40%** |
| **落点** | 能力契约 `参数契约.json` + `开发工具/契约编译/能力定义编译器.py` 的派生物生成（待 §七 回填精确字段集） |
| **判据** | 任取一个枚举式参数的能力 ⇒ 契约里能读到 `可选值`，且网关**拒绝非法取值并列出合法值** |

---

### B 层 —— 返回体经济与报错可修

#### B1 返回体超限被丢（★ P0 第一条）

| 项 | 内容 |
|---|---|
| **选定做法** | **深裁字符串叶子，结构化容器永不丢**；截断处附 `...[truncated N chars]...`；回带「原长 / 已截断 / 取全量方式」 |
| **为什么不选「分页」** | MCP 规范 `nextCursor` **只覆盖 4 个 list 操作**（`resources/list`、`resources/templates/list`、`prompts/list`、`tools/list`），**`tools/call` 不在内** ⇒ 规范**没有**为「一次工具调用的返回体」定义分页（原判已纠正） |
| **为什么不选「落盘 + 取回」** | 引入**新存储 + 清理策略**（稳定性风险：缓存被误删）；Hermes 那套完整实现要连带定落盘根。**深裁不引入任何新存储** |
| **稳定性** | ✅ 一处函数、可单点回退；✅ 不改契约、不改并发。⚠️ 前置：**下游若断言字段长度会红** ⇒ 先跑 `开发工具/契约编译/漂移检测 --报告` 确认 0 条 |
| **高效性** | ✅✅ **本轮现场复现 3 次**：81874 字符→裁到 3292（**同一能力重调 6 次**）；33314→**整段丢**；29708→**整段丢，只回 92 字符摘要，答案零字节**。每次都是**纯损失**（成本照付、收益为零） |
| **外部依据** | `cline/cline` `message-builder.ts:1087` *"Deep-truncates string values inside structured tool outputs"*（递归到每个字符串，容器永不丢）、`:1537` 头尾各半、`:53-61` 四种截断标记。**MCP 规范 `tools.mdx:500` 要求结构化与文本「并存、互为兜底」**，本项目「二选一」正是被规范否定的处置 |
| **落点** | `开发工具/薄壳/薄壳服务.py:106 _裁剪值()`（现状：注释原文「上限时做**结构化裁剪**（字典丢尾键 / 列表截尾 / 长文本截断）」；`:150`/`:152`/`:163` 三种说明；`默认返回上限字符 = 6000` 在 `:79`） |
| **判据** | 构造一个超限长文本 ⇒ 返回体里**有截断说明且能按说明取全量**（不再是「键消失」）。**反向验证**：把实现退回「丢键」，判据必须转红 |

#### B2 门禁报红不给落点/修法

| 项 | 内容 |
|---|---|
| **选定做法** | 红项**对象化六件套**：判据名 / 落点`文件:行:列` / 原文片段 / 修法 / **可自动修级别** / 规则文档落点；另加**指纹**（跨轮次认领同一条）与**未修项标记**；再加本项目特有的 **`修复命令`** 字段 |
| **为什么必须加 `修复命令`** | 本项目多数门禁的修法是**生成器命令**（如「改完契约必须重跑契约编译器」），**SARIF 的 `fixes` 表达不了**（它只表达文本替换）⇒ 命令必须由**门禁自己给**，不许渲染层拼（拼错比不写更坏） |
| **稳定性** | ✅ **判定逻辑一律不许动**，只换形状；✅ **退出码与五态结论一字不动**；✅ 判红时违规条目**一条不砍**（现在靠 `违规条目前缀 = "["` 全量透出，改成对象数组后必须保持全量） |
| **高效性** | ✅ 省三轮：「再全仓搜一次找落点」「再猜怎么改」「再问该跑什么命令」 |
| **外部依据** | SARIF `fixes[]{deletedRegion, insertedContent}` + `reportingDescriptor.helpUri`；ruff `SuggestedFixes`；Rust `Applicability`（可机械应用/需人工，clippy 在宏展开时**自动降级不许吹牛**）；golangci-lint `Fingerprint()` + `notFixableIssues` |
| **落点** | `开发工具/开发编译口/编译口.py` 的 `_尾部()` + 各门禁打印点（`门禁清单` 已是机器可读结构，改造基础在） |
| **判据** | 造一条红项 ⇒ 输出里**同时**有落点、片段、修法、可自动修级别、规则文档落点、指纹；**没有落点的红项写「无落点」不许编** |

---

### C 层 —— 热重载与重启

#### C1 热接入不重载已导入模块

**现状（已强于多数开源项目，实测）**：`后端核心/后端核心.py` 已有三件套 ——
`_包内模块表()`（`:189`，按 `__file__` 归属取「对象标识/文件/mtime纳秒/字节数」）、
`_自证重载已生效()`（`:212`，**三条判据**：① 模块对象必须换新 ② 旧模块对象必须全部退场 ③ 重载后源文件可现场 stat）、
`_包导入依赖图()`（`:255`，按 **AST 真实导入关系**排重载顺序）。

> `_包导入依赖图` 的存在本身就是一条硬教训（源码注释原文）：`包声明.json` 的 `依赖` 是**空的**，
> 只按声明的依赖排，**根本排不出「提供者在前」** ⇒ **不能信声明，要扫源码**。

| 项 | 内容 |
|---|---|
| **选定做法** | 补**第 4 条判据：类身份漂移只读检测**（重载前在场的类，若仍有实例存活，其 `__class__` 必须已指向新类） |
| **为什么这是真缺口** | 现有三判据查的都是**「模块对象」**换没换，查不到「**模块换了，但已持住旧类实例的地方仍跑旧类方法**」 |
| **稳定性** | ✅ **只读检测，不改重载逻辑**。⚠️ **硬前置：先实测 `gc.get_referrers` 耗时**（本项目有单命令 15 秒硬上限）；超阈值 ⇒ 改为**只告警**或**采样检测** |
| **高效性** | ✅ 省「热接入后行为诡异 → 反复排查是不是没生效」的往返 |
| **外部依据** | `ipython/ipython` `autoreload.py:372-381 update_instances()`：`gc.get_referrers(旧类)` + `object.__setattr__(ref, "__class__", 新类)` —— **类身份漂移的唯一解法** |
| **落点** | `后端核心/后端核心.py:212 _自证重载已生效()` |
| **判据** | 造一个「模块级全局持旧类实例」的包 ⇒ 热接入后**判据④报未生效**。**反向验证**：删掉判据④ ⇒ 场景转绿（假绿复现） |

#### C2 重启打断在途调用

**现状（四段停机已完整实现）**：`运行核心/统一网关/停机编排.py` ——
阶段表 `:48-55` = `(停止收新请求, 排空在途, 整组回收, 持久化)`；
`默认排空上限秒 = 20.0`（`:59`）、`默认排空复查窗口秒 = 2.0`（`:61`）、`默认收尾项预算秒 = 10.0`（`:65`）；
`launchd退出超时建议()`（`:68-80`）= 20+30+2+8 = **60 秒**（与 plist 实测 `ExitTimeOut=60` 一致 ✓）；
`_段_排空在途`（`:301`）有**截断清单逐条记录**、超时后取消流式通道 + 复查窗口；
`_跑A簇兜底回收`（`:388`）只**调用** `清扫上一代常驻进程()`，**不重写回收逻辑** ✓。

**⚠️ 纠正一条流传的错误结论**：「整组回收无宽限期」**不成立**。真源：
`公共契约/运行时/进程终止.py:58-59` `终止宽限秒 = 1.0`；`:541-570` `强制结束子进程` docstring 原文
**「结束子进程并确认死透：`终止` → 宽限等待 → `强杀` → 等待 → 复查」**；`:617-620` 有「宽限内自行退出 / 未退出升级强杀」两分支。
`独立进程_账本.py:70` 的 `清扫等待秒 = 2.0` 是**强杀后等整组消失的上限**，**不是宽限期**。

##### C2-① 宽限只有 1.0 秒且回收路径没传参（★ C 层唯一该先做的）

| 项 | 内容 |
|---|---|
| **选定做法** | 给 `_回收账本行` 加 **`宽限秒` 形参**并**透传**给 `强制结束子进程`；**默认值保持 1.0 不变**，由调用方显式传长宽限 |
| **为什么不直接改默认值** | 改默认会**变更既有行为**（所有调用方回收时长都变）⇒ 违「只增不改」。**只加形参 + 透传**，默认一字不动 |
| **稳定性** | ✅ **默认行为完全不变**；✅ 1 个形参 + 1 处透传，可单点回退 |
| **高效性** | ✅ 省「在途长调用被 1 秒宽限腰斩 → 整批重跑」的往返（转码/转写可达分钟级） |
| **落点** | `运行核心/加载器/提供者隔离/独立进程_账本.py:212 _回收账本行()`（现状：`:228` 调 `进程终止.强制结束子进程(组长进程id)` **未传 `宽限秒`** ⇒ 吃默认 1.0 秒） |
| **判据** | 造「常驻提供者持在途长调用」场景 ⇒ 回收时说明表出现「**N 秒宽限内自行退出**」而非「升级为强杀」。**反向验证**：`宽限秒` 退回 1.0 ⇒ 判据转红 |

##### C2-② 新旧重叠期

| 项 | 内容 |
|---|---|
| **选定做法** | **不做重叠期**（接受真空期）；**改做「就绪探针门」** —— 切换前等 `/健康` 连续 N 次成功再切指针 |
| **为什么不做重叠期** | `SO_REUSEPORT` 两进程共持 40007 = **新时序不确定性**（稳定性不达标）；且 `docker/compose` 源码明写滚动更新只在 Swarm 有 ⇒ 「简单重建有中断」是**业界常态**，接受真空期**代价 0** |
| **为什么就绪探针门是稳的** | **不引入并发、不改端口、不加进程** —— 只加一个「等判据」；且 `热切换.真实热切换` 第 3 步**已有健康检查**，是同一种判据的复用 |
| **稳定性** | ✅ 只加等待；✅ 探针失败 ⇒ **拒绝切换并保留旧世代**（fail-closed） |
| **高效性** | ✅ 省「切完发现新世代没好 → 回滚 → 再切」的往返 |
| **外部依据** | `kubernetes` `pod-lifecycle.md:825-832` **readinessProbe 决定何时接流量**；`maxSurge` 缺省 25% |
| **落点** | `运行核心/统一网关/停机编排.py:48` 阶段表 + `运行核心/启动运行核心网关.py` |
| **判据** | 停掉新世代健康端点 ⇒ **切换被拒**且旧世代保留 |

##### C2-③ 网关进程自身的 blue-green

| 项 | 内容 |
|---|---|
| **选定做法** | **登记不做**（本轮） |
| **理由** | 切换频率低；且 `运行核心/加载器/版本系统/热切换.py:277 真实热切换()` **已是完整 10 步 blue-green**（启动旧→启动新→健康检查→最小调用→影子→灰度→切换→排空→停止，失败自动回滚），**需要时把对象从「提供者子进程」换成「网关世代」即可，不必新造算法** |
| **必须继承的一条诚实口径** | `热切换.py:341-350` 原文：`灰度比例表` **全仓 0 个读取者**，`调用路由` 只按进程状态路由 ⇒ **`灰度比例` 只记录意图、不产生分流效果**。**本方案同样不许声称「灰度切换」**，除非真做了按比例分流 |

---

### D 层 —— 派生物与互锁

#### D1 编译器只产 6 份派生物

| 项 | 内容 |
|---|---|
| **选定做法** | **生成物头统一为「生成器名 + 来源契约 + 契约版本」**，门禁**按头识别** |
| **为什么不选「派生物注册表」** | 注册表要把 6 个生成器**对象化**（它们当前**共享编译上下文**）⇒ 中改动 + 稳定性风险（共享状态拆开易出时序问题）。**头统一是「只增」**：存量一次性重写头 |
| **稳定性** | ✅ 只增头字段；✅ 门禁识别方式单点改 |
| **高效性** | ✅ 让「这份文件是谁生成的、依据哪份契约的哪版」**一眼可读**，省「反查生成器」的往返 |
| **外部依据** | `protobuf` `compiler/cpp/file.cc:170` 的 `// source: $filename$` |
| **落点** | `开发工具/契约编译/能力定义编译器.py` 的 `生成标记` 常量 |
| **判据** | 任取一份派生物 ⇒ 头部能读到生成器名 + 来源契约 + 契约版本 |

#### D2 派生物互锁（★ 结构上根治）

| 项 | 内容 |
|---|---|
| **选定做法** | **照抄 `verify-generated` 三行语义**：跑生成器（**走写入口**）→ `git status --porcelain` 非空即红 → **原样打印差异文件名**；**删掉「要求生成物存在」这条独立判据** |
| **为什么这样结构上不可能互锁** | 生成器拒写 ⇒ 无差异 ⇒ 判绿。成熟生态**根本没有**「要求生成物存在」这条独立判据 |
| **死结机制（已核实）** | `开发工具/生成物一致性检查.py:300-308` **直接 `import` 生成器函数做逐字比对、绕开了写入口**；而同文件 `:276-282` 记「该生成器**拒绝覆盖已有文件**」⇒ 两条口径相撞 |
| **稳定性** | ⚠️ **硬前置：重算必须幂等**（不幂等则判据自己抖）；✅ 删掉一条独立判据 ⇒ **减少**判据，不是增加 |
| **高效性** | ✅ 省「门禁红 → 猜是生成器问题还是内容问题 → 手工重跑」的往返 |
| **外部依据** | `kubernetes-sigs/controller-tools` `hack/verify-generated.sh:20-26`；`ent` `ci.yml:81-87`（**同一条**）；`buf` `disallow_comment_ignores: true`（防「把派生物登记为人工区来消红」） |
| **落点** | `开发工具/生成物一致性检查.py:271-275`（白名单）、`:300-308`（逐字比对）、`:276-282`（拒写记录） |
| **判据** | 手改一份派生物 ⇒ 门禁报红并**打印该文件名**。**反向验证**：让生成器拒写 ⇒ 判据应**判绿**（不互锁） |

---

### E 层 —— 目录树与工具发现

#### E1 目录树与代码地图

**现场已不成立的事实**：「只有单层列目录、没有树」**是错的** ——
`文件系统支持库.文件操作.目录树` 已于**本日**（提交 `65808c3f`）落地（深度/glob/排除/条数上限/`已截断` 全有，**实测 3.7 毫秒**）；
`代码解析支持库.仓库地图` 已是 **PageRank + token 预算二分（与 aider 同口径）**。

| 项 | 内容 |
|---|---|
| **选定做法** | **只做两口**：① 忽略规则加 **glob**（可选参数，只增）；② 返回体加 **`估算token`** 字段（只增字段） |
| **明确不做** | **「同层目录优先」排序 —— 删掉**（会改变输出字节，`排序规则` 声明了 `排序稳定: true`，逐字快照类场景会红） |
| **另一条（不是开发缺口）** | `取仓库地图` 实测 **HTTP 404** ⇒ **能力在、图未建**，属**落地缺口**（跑一次 `建符号图` 即可），**不是要写代码** |
| **稳定性** | ✅ 忽略规则是**新增可选参数**（不传 = 行为不变）；✅ `估算token` 是**只增字段** |
| **高效性** | ✅ 省「取回的树里全是噪音目录 → 再筛一次」的往返；✅ `估算token` 让调用方**知道该不该继续下钻**（省一次试探） |
| **外部依据** | `modelcontextprotocol/servers` 的 filesystem `directory_tree`（递归 + `minimatch`，**不到 80 行**）；`repomix` `defaultIgnore.ts` 87 条分节忽略清单、`filePathSort.ts`；`gitingest` 用 `tiktoken` 算 token |
| **⚠️ 一条隐性风险（必须同时处理）** | 地图的 token 口径是「**字符/3**」估算，与薄壳 `返回上限字符` **不是同一把尺子** ⇒ 可能出现「地图说 4096 token，网关按字符裁掉一半」。**两把尺子必须对齐**，否则 `估算token` 反而误导 |
| **落点** | `文件系统支持库.文件操作.目录树` 实现、`代码解析支持库.仓库地图`（待 §七 回填精确文件:行） |
| **判据** | ① 传 glob 忽略 ⇒ 结果里无匹配项，**不传时结果逐字不变**；② 返回体有 `估算token`，且与网关字符口径**能换算** |

#### E2 工具发现要三跳

| 项 | 内容 |
|---|---|
| **选定做法** | **搜索精确命中唯一一条时，直接给全签名**（其余情况行为不变） |
| **为什么不改 P-9 默认精简** | P-9 有明文依据（`模块库/能力目录/能力定义.json:10,45`「精简字段默认返回」+「返回指针而不是全文」，理由是**保住 prompt 前缀缓存**）⇒ **不动默认**，只开一个口子 |
| **稳定性** | ✅ **只增行为**（仅在「精确命中唯一一条」这个窄场景），多命中时**逐字不变** |
| **高效性** | ✅ 省掉 `describe` 那一跳（实测 **21 次**）。且 RAG-MCP 硬数据正面支持：**全量注入 13.62% vs 检索式 43.13%** ⇒ **一次给全不伤准确率，反而提升** |
| **外部依据** | Anthropic Tool Search Tool：命中返回 `tool_reference`，**API 自动展开成完整定义**；OpenAI `tool_search` + `defer_loading`。**没有任何一家把「失败回带签名」当主路径** |
| **落点** | `模块库/能力目录/实现/能力索引.py` + `能力目录.搜索能力` 实现（待 §七 回填） |
| **判据** | 用一个**精确全名**搜 ⇒ 返回体里**直接有参数名/类型/必填**，不需再调 `读取能力` |

---

### F 层 —— 可观测性

#### F1 耗时榜

**现场已不成立的事实**：「有账本，没有耗时榜」**是错的** ——
`运行核心/运行诊断/安全审计/安全审计.py::慢调用报告()`（第 **177-258** 行）**已存在**：按能力聚合〔条数/最慢毫秒/平均毫秒/等级〕+ 最慢明细 Top N + 3秒/10秒分级。

| 项 | 内容 |
|---|---|
| **选定做法** | **第一步（本批）**：在既有 `慢调用报告` 上加**分位**（p50/p95/p99）—— **纯计算，零风险**。<br>**第二步（另批）**：加窗口对比 + 返回体量列 |
| **为什么拆两步** | 「返回体量列」要动**权威账本**（`安全审计.jsonl`）⇒ 稳定性风险；分位只是**对已有数据换算法**，不动存储 |
| **明确不做** | **不新建第二份耗时日志** —— 参数账文件头已把「双写」判为**静默分叉源**；新建会直接违「同一件事两套实现即缺陷」 |
| **稳定性** | ✅ 分位是纯计算、单函数；✅ 不新建存储 |
| **高效性** | ✅ 只有**点值阈值**出不了 SLO（`sloth` 坏事件举例即 `"latency > 250ms"` ⇒ SLO 必须落在分布上）⇒ 分位让「慢」可判定，省排查往返 |
| **一个结构性事实** | 耗时明细唯一落点 = `安全审计.jsonl`（`记录()` 第 104 行 `耗时毫秒`）；**`公共契约/诊断/调用账本.py` 是「参数账」（主键 `(能力id,入参形状)+调用次数`），不含耗时列** ⇒ 出榜需**联两个账本** |
| **落点** | `运行核心/运行诊断/安全审计/安全审计.py:177-258 慢调用报告()` |
| **判据** | 造 10 次不同耗时调用 ⇒ 榜里有 p50/p95/p99（不是只有最慢值） |

#### F2 「一次全查」的原子口

| 项 | 内容 |
|---|---|
| **选定做法** | **不新增查询入口**；改为给账本加**分类维度**（用 OTel 工具类型三分法：**Extension** / **Function** / **Datastore**） |
| **为什么不新增原子口** | 会形成**第二个查询入口** ⇒ 违「唯一入口」；且账本字段增加是**只增**，而新增入口是**第二腿** |
| **稳定性** | ✅ 只增字段；✅ 不动入口 |
| **高效性** | ✅ 让「一类调用整体慢」一眼可见（按类型聚合），省「逐个能力看」的往返 |
| **外部依据** | OTel 官方定义：Extension（agent 侧直调外部 API）/ Function（客户端执行预定义函数）/ Datastore（检索查询外部数据） |
| **落点** | 审计账本字段 + `慢调用报告` 聚合维度 |

---

## 三、zcode 方案 L1–L11 的归属（防重复、防漏）

| zcode 项 | 归属本方案 | 关系 |
|---|---|---|
| L1 薄壳工具描述瘦身 | §二 A2 + §零判据 | **同一件事**（瘦身砍「规则散文」，不砍签名） |
| L2 细节的按需取用处 | §二 A2（落点待定） | 同一件事；**不许新建第二套规范**，先查既有规范能否承接 |
| L3 丢键改「缩」 | §二 B1 | **同一件事**（B1 就是它） |
| L4 大输出切片（新增原子能力） | **本方案未选**（见 §六 排除） | 落盘引入新存储 + 清理策略 ⇒ 稳定性不达标。**B1 深裁已覆盖 90% 场景**；真需要时另立方案 |
| L5 被忽略参数回带清单 | §二 A2 同族（**B 层**） | 同一件事：**保留「未知参数不失败」**（协议兼容不变），**新增回带清单**（只增） |
| L6 参数转义成本（数组形态） | **本方案未选**（见 §六） | 要改**各能力契约 + 网关入参** ⇒ 波及面大、改的是既有参数形态（违「只增不改」）。**登记备查** |
| L7 写类乐观锁进必经路径 | §二 P1 第 10 条 | **同一件事**，且**这是提稳定性本身** ⇒ 建议优先级**上调** |
| L8 写类原子写 | §二 P1 第 10 条 | 同一件事（`tempfile` + `os.replace` + 恢复权限位） |
| L9 `additionalProperties` 对齐 | §二 A2b 同族 | **已现场核实：`rg additionalProperties 开发工具/薄壳/` → 0 命中**。⚠️ **这是兼容性硬要求**（Claude 作为 MCP 客户端强制要求 `additionalProperties: false`，否则报 `Invalid schema`）⇒ 建议**优先级上调** |
| L10 校验错误语义（`isError`） | §二 A2 / B2 | 同一件事。**SEP-1303（Final）**：输入校验失败**必须走 Tool Execution Error**，不许走 Protocol Error（协议错误被客户端吞掉、模型看不见 ⇒ **反复重试同一错误**）⇒ 这也**提稳定性** |
| L11 慢调用诊断自带 | §二 F1 | 同一件事（分位是它的前置） |

**zcode 的 4 条待裁定** → 本方案对应处置：
- **裁1 未知参数拒绝还是忽略**：本方案**选「忽略 + 回带清单」**（稳定性：保留协议兼容不变，只增回带）—— 但**必须同时修两份权威文档的正面冲突**（`当前流程图.md:63/119` 写「立即拒绝」vs `网关调用最小示例.md:129-138` 写「一律忽略」），否则**下一轮还会有人按另一份文档做**
- **裁2 L4 落点**：本方案**不做 L4**，此裁定作废
- **裁3 L1 细节放哪**：**并入既有 `网关调用最小示例.md`**（稳定性：不新增规范清单，符合「1.3 结果唯一即收口」）
- **裁4 L9 现状**：**已核实** —— `开发工具/薄壳/` 下 `additionalProperties` **0 命中**，需**薄壳显式声明**

---

## 四、批次（按判据排序，**不按收益**）

| 批次 | 内容 | 入选理由（稳定 → 高效） | 判据 | 反向验证 |
|---|---|---|---|---|
| **批 1** | **B1 深裁** + **A2b 补 `可选值`** + **A2/B2 错误体四段式** | 三条**只增不改 + 一处改动 + 可单点回退**；直接减往返 | 见各条 | 各条均须一条反向验证 |
| **批 2** | **C2-① 宽限参数化** + **zcode L9 `additionalProperties`** + **zcode L10 `isError`** | 前一条 1 形参；后两条是**兼容性硬要求**（不做会被客户端拒/反复重试） | 见各条 | 宽限退回 1.0 ⇒ 转红 |
| **批 3** | **zcode L7/L8 写类乐观锁 + 原子写** | **提稳定性本身**（防撞车、防换符号链接） | 见 §三 | A 会话认领后 B 改同一文件 ⇒ A 写回**被拒并回带当前摘要** |
| **批 4** | **E1 两口（glob + 估算token）** + **E2 精确命中给全签名** + **F1 分位** | 全是**只增**（可选参数 / 只增字段 / 纯计算） | 见各条 | E1 不传 glob 时结果**逐字不变** |
| **批 5** | **D2 `verify-generated`** + **D1 生成物头** | **前置：先验证重算幂等**；不幂等则**不做** | 见各条 | 让生成器拒写 ⇒ 判据应判绿 |
| **批 6** | **A1 并发（fail-closed）** + **C1 第 4 条判据** + **C2-② 就绪探针门** | 三条**各带硬前置**：A1 要「缺省假」落地并冻结写类清单；C1 要**先实测耗时**；C2-② 要探针失败能 fail-closed | 见各条 | 各条均须一条反向验证 |

**顺序理由**：批 1 全是「只增不改」，批 2 是「兼容性刚需」，批 3 是「提稳定性」，批 4 全是「只增」，
批 5/6 各带硬前置 —— **前置没验证完就不许开工**（这是稳定性判据的硬要求，不是建议）。

---

## 五、验收口径（华哥 2026-09-21 定）

**一律经 `POST /网关/调用` 真实调用量取**，不看 py 内部调用、不拿单元测试结论当发布依据。

**每条必须有「反向验证」**：把修复退回旧行为 ⇒ 判据必须转红。没有反向验证的判据**不算判据**（可能是假绿）。

---

## 六、明确排除（稳定性不达标，不论收益）

| 排除项 | 不达标的稳定性理由 |
|---|---|
| **L4 大输出落盘 + 取回** | 引入**新存储 + 清理策略**（缓存被误删风险）；B1 深裁已覆盖 90% 场景 |
| **L6 参数改数组形态** | 要改**各能力契约 + 网关入参**的既有参数形态 ⇒ 违「只增不改」，波及面大 |
| **E1「同层目录优先」排序** | **会改变输出字节** ⇒ 逐字快照类场景会红 |
| **C2-② 两进程共享 40007** | 新时序不确定性；`docker/compose` 证明「简单重建有中断」是业界常态 |
| **C2-③ 网关世代 blue-green** | 改动大、切换频率低；`真实热切换` 10 步骨架**已存在**，需要时复用 |
| **jurigged 式原地热补丁** | 会引入「半新半旧」的不可判定态（作者自述边界多） |
| **watchfiles 文件监听自动重载** | 会让**半成品自动上线** |
| **envoy 式双进程状态搬运** | 官方原文明确在途连接**不迁移** |
| **etcd/consul 配置中心 watch** | 与「本地私有部署、唯一网关」的轻量定位冲突 |
| **新建第二份耗时日志** | 参数账文件头已把双写判为**静默分叉源** |
| **新增「一次全查」查询入口** | 会形成**第二个查询入口** ⇒ 违「唯一入口」 |
| **照抄 MCP Registry 的反域名命名** | 本项目全中文能力 id 已满足唯一性目标，形态不同不必照搬 |
| **给 `执行命令` 加 shell** | 官方安全共识是**反的**（*"prefer non-shell execution methods (e.g., posix_spawn)"*）⇒ 直启是**正确设计**，要改的是**把不支持的语法明确告知** |

---

## 七、落点核实状态（诚实标注）

| 层 | 落点 | 状态 |
|---|---|---|
| A1 | `开发工具/薄壳/工具清单.py`、`开发工具/薄壳/薄壳服务.py` | ✅ 已核实（能力注册表 `可否并行` 字段落点**待回填**） |
| A2 / A2b | `工具清单.py:71`（薄壳自己有 enum）、能力契约 `参数契约.json` | ✅ 已核实「薄壳有、能力层没有」；**精确字段集待回填** |
| B1 | `开发工具/薄壳/薄壳服务.py:106 _裁剪值()`、`:79 默认返回上限字符 = 6000` | ✅ 已核实 |
| B2 | `开发工具/开发编译口/编译口.py` `_尾部()` + 门禁打印点 | ✅ 落点已确认；**逐门禁打印点清单待回填** |
| C1 | `后端核心/后端核心.py:189/212/255` | ✅ 已核实（三判据 + AST 依赖图） |
| C2-① | `独立进程_账本.py:212 _回收账本行()`、`:228`、`:70`；`进程终止.py:58-59`、`:541-570`、`:617-620` | ✅ 已核实（**并纠正了「无宽限期」的错误结论**） |
| C2-② / ③ | `停机编排.py:48/294/301/351/388/420`；`热切换.py:277/341-350/377` | ✅ 已核实 |
| D1 / D2 | `开发工具/契约编译/能力定义编译器.py`；`开发工具/生成物一致性检查.py:271-275/276-282/300-308` | ⚠️ **待回填**（已由 6 路探查中） |
| E1 / E2 | `文件系统支持库.文件操作.目录树`、`代码解析支持库.仓库地图`、`模块库/能力目录/实现/能力索引.py` | ⚠️ **待回填** |
| F1 / F2 | `运行核心/运行诊断/安全审计/安全审计.py:104/177-258`；`公共契约/诊断/调用账本.py:45-55` | ✅ 已核实 |
| zcode L5 / L9 / L10 / L11 | `网关参数校验面.py:102 _过滤能力参数()`；薄壳 `additionalProperties`（0 命中）；网关错误体；慢调用提示 | ✅ 落点已确认；**错误体构造点待回填** |

**6 路并行落点探查已派出**（A/B/D/E/F 各一层 + C 层交叉复核），产出落在
`开发文档/分析/MCP优化调研/落点核实/`，回来后**逐条回填本表**并把「待回填」清零。

---

## 八、一页结论

**判据只有两条：先过稳定性，再过高效性。** 按它重审，**旧稿翻案 4 条**（A1 降级、E1 排序删除、C2-② 排除、D2 加硬前置），另 2 条加了稳定性护栏。

**最该先做的三条（都在批 1，共同点是「只增不改 + 一处改动 + 可单点回退」）**：
① **B1 返回体深裁**（现状会**整段丢键**，本轮实测重调 6 次）；
② **A2b 能力契约补 `可选值`**（薄壳自己已有 `enum`，745 个能力一个都没有）；
③ **A2/B2 错误体四段式**（**只改错误分支**，成功路径一字不动）。

**两条「被低估、建议上调」的**（zcode 独有，且都是**兼容性硬要求**）：
**L9 `additionalProperties`**（Claude 作为客户端**强制要求**，否则报 `Invalid schema`）、
**L10 `isError` 语义**（SEP-1303 Final：走 Protocol Error 会被客户端吞掉 ⇒ **模型反复重试同一错误**）。

**一条必须先修的文档冲突**：**裁1**（未知参数「拒绝」还是「忽略」）——
`当前流程图.md:63/119` 与 `网关调用最小示例.md:129-138` **正面冲突**，
不定下来，下一轮还会有人按另一份文档做。
