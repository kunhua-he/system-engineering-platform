# 底座收口夜班交付说明（2026-09-15 凌晨）

> 给华哥早上看的一页：**现在什么状态、能不能用、还剩什么**。工作明细见
> `开发文档/临时文档/57_底座收口总清单_审计.md`（收口清单）、`58_文件式运行态清点.md`（入库迁移清单）。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 一、一句话结论

**底座现在是干净基线、可用状态**：112 个正式包、531 项能力、六环门禁 0 违规、摘要 0 不闭合、
漂移 0、组件合规逐包全通过、依赖审计 0 违规；运行态开始统一入库（不再满天写文件）。
**调用方只需要 HTML 传参**——底座内部怎么存、怎么跑、怎么装配，都不需要知道。

## 二、调用方只需记住四条（详见 `开发文档/网关调用最小示例.md`）

```js
const r = await fetch("http://127.0.0.1:40007/网关/调用", {
  method: "POST", headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ 操作: "调用能力", 能力id: "文档读取.结构化读取", 参数: { 文件路径: "/tmp/a.md" } })
}).then(r => r.json());
```

1. **多传字段不会失败**：顶层、操作参数、能力入参里的未知字段一律忽略（网关边界剔除）。
2. **只有两类会报错**：必填参数缺失、参数类型不符（`参数不合法` + 点名哪个字段）。
3. **版本不一致不失败**：返回里带 `请求版本` / `当前版本` / `版本差异`；只有强制约束不满足才失败。
4. **信封只增不减**：新增字段只追加，旧键语义永不改。

长任务走「任务提交 → 任务查询 → 任务取消」，不要用同步调用硬扛。

## 三、今晚做完的（都带真实验收证据）

| 批次 | 内容 | 证据 |
|---|---|---|
| 批 0 | **兼容性一次性扩宽**：三处「未知字段即拒」全放开、实现返回缺键不再判违约（缺键补默认、类型不符仍拦）、信封只增不改并追加上报版本三项、旧名 `契约版本` 作别名永久可解析；缺必填由 **500 内部错误改为 400 明确点名** | 真实 HTTP：多传字段 200、旧版本号 200 带三项信息、缺必填 400、类型不符 400 |
| 批 0 | **底座运行库落地**：`工程缓存/运行数据/底座运行.db`，八个域表（任务/作业/协作状态/能力占用/检查点索引/会话/缓存索引/发布）+ 三个能力（初始化/写入/查询运行态） | 真库写入 + 查询通过；契约/摘要/六环/场景/说明书齐 |
| 批 1 | **漂移门禁 67 → 0**：46 包是检测器口径假报（没把既有契约传给生成器）、21 包是真漂移（只改定义没重编译契约）→ 修口径 + 逐包按唯一事实源重编译 + 新增反回归门禁 | `测试_漂移门禁` 2/2；全仓检测 0 |
| 批 3-1 | **任务域入库**：`任务系统` 不再写 `任务.jsonl`，改写入运行库（旧账本只读兼容 + 首次一次性搬迁） | 真实 HTTP 提交→轮询成功；旧账本大小不变；库里查到新任务 |
| 批 3-2 | **作业 / 协作状态 / 能力占用 三域入库**（并行子代理完成，主会话复核） | 旧文件**不再增长**（哈希/文件数前后一致）；运行库行数 作业 1 / 协作状态 26 / 能力占用 22；`测试_作业系统 18`/`测试_协作状态 18`/`测试_能力占用 15` 全 OK；六环通过 |
| 批 3-4 | **清理**：删 8 个空目录、4 个散落文件、2 个历史验收残留、6 个无引用旧快照（工程缓存 43 → 29 项） | 目录清单前后对比 |
| 批 4 | **A5 YAML提供者**：补 `能力定义.json`（含 `检查可用性` 探针）+ 生命周期契约 + 说明书 → 依赖审计 **2 条违规 → 0** | `python3.14 -m 开发工具.依赖生命周期审计.运行依赖生命周期审计` 全部通过 |
| 批 4 | **A4 组件合规收口**：权限契约缺声明 / 五件缺失 / 能力契约未声明错误码 → 按权威清单批量补（209 文件）+ 逐包重编译与摘要回算；合规器新增「聚合视图包」判据（与 `正式包索引` 同源） | 未通过包 **44 → 9 →（复核中）**；表格公式/轻量文本解析 **9/13 → 13/13** |
| 批 4 | **A4-① 真缺陷**：`Git操作.校验分支名` 公开能力成功时返回 `None`（违反自己"返回结果型"契约）→ 改为统一结果包装；`进程管理`/`会话存储`/`检查点恢复`/`浏览器自动化提供者`/`批量精校队列` 的 `except: pass` → 留痕（进程回收失败还会随失败说明回报） | 进程管理 13/13（1 跳过）、契约与漂移门禁全绿 |
| 批 5 | **A6 去重复实现**：删 `平台控制面/提供者/数据库提供者.py`（485 行、零生产调用方的第二套池）+ 清导出 + 删其专属测试 | 导入 OK、受影响波次 37/37、无残留引用 |
| 批 5 | **A3 A/B命名**：占位命名清理 **87 → 0 个包内引用**（剩余仅文档里"AB"这个词本身）；`集合A/集合B` 参数名改为一眼能懂的 `集合1/集合2`，并在网关加**能力参数别名表**（旧名永久可解析，第 21 条兼容） | 11 个文件改名；契约/摘要/六环全绿；真实 HTTP 验证新旧参数名都可调用 |


## 四、我判定「不改」的（附理由，避免以后重复讨论）

1. **依赖侧版本判定**：`传递闭包` 本来就用 `_满足约束(锁版本, 约束)`（按约束判定，不是严格相等）；`强制校验` 对第三方锁**故意要求精确版本**（可复现性）；`资源协调/服务.py` 的 `版本` 是乐观并发计数。→ 第 21 条在请求侧已落地，依赖侧本就正确，**不动**。
2. **两份 `当前.json` 键形状不同**：一份是构建指针（`路径`），一份是激活指针（`制品目录/制品摘要/版本/栅栏令牌`），各有写入方与文档说明，**设计如此**。
3. **信任链历史元数据（快照/根信任旧代）**：属验证链依赖（`可信仓库元数据` 会按版本回查旧代），第 22 条「不归档」针对的是**制品/版本**，不是信任链 → 保留。
4. **行数阈值**：按第 18 条不设硬门禁，只看改动频率与读取成本。

## 五、还剩什么

**待你一句话（7 项，判据与建议见 57 号 B 组）**：发布门禁第三方访问声明空集语义；
网关「任务不存在」错误码；4 个能力契约标必填但实现容忍缺参；重试调度参数名；依赖两段是否物理切目录；约 60 个能力错误码回填。
（原「个人蒸馏 8898 恒 500」一项**已随上游恢复自动消除**——豁免声明已删、正向场景已补。）

**不依赖裁决、我可以继续做的**：
- 运行态入库只剩两处：**发布/诊断事件**（`发布` 域表已建好，`发布操作日志`/`灰度指标` 待迁；审计/失败/事件三类判为审计轨迹，保留文件）；
  （`检查点.db`／`会话存储.db`／`记忆库.db`／`嵌入缓存.db` **已统一到 `工程缓存/运行数据/`**，批 3-3 已完成）
- **`测试资源清单/*.jsonl`**（`测试资源.py`）仍是文件式，需裁决是否入库（子代理批 3-2 的范围外项）；
- A3 A/B命名：**包内引用已清零**（`集合A/集合B` → `集合1/集合2`，旧名走网关参数别名表）；仅剩开发文档里"AB"这个词本身（讲禁用规则用，不改）；
- A4-③ 说明书生成器与仓内格式漂移（需先修生成器，属工具侧）；
- **运行核心 18 处 `except Exception: pass`**（热路径，逐点判断；清单在 `未完成事项.md`）；
- E1 协作状态历史残留 / E3 验证账本补录（补录需跑发布门禁）。

## 七、发布门禁（唯一验收链路）：**已通过**

**发布状态：通过**（退出码 0；69 项 ✓、失败 0、目标能力 535、步骤 709、清理失败 0、资源残留 0）。

门禁从「末尾失败、明细看不出原因」逐步定位到可复现根因，**失败项收敛：37 → 8 → 4 → 2 → 0**。
过程中修的都是真问题，没有一处靠改门禁或跳过取证：

| 现象 | 真实根因 | 处理 |
|---|---|---|
| 一大批「统一返回缺字段」 | 其实是 **HTTP 502 无响应断连**（64 并发偶发 `Remote end closed connection`），属传输抖动 | 验证器只对「一个字节响应都没有」**重试一次**并把 `传输重试` 写进证据；有响应的失败一律不重试 |
| 报某包「契约缺字段/类型不在表」 | **制品比源码旧**（多次重编译契约/类型名后没重编） | 固化流程：改源码 → 重编译 → 摘要 → **重编制品** → 再跑门禁 |
| `psycopg提供者` 场景报错 → 黑盒零场景 | 内联场景**缺 `清理步骤` 键** | 补键 + 新增 `测试_验证场景可解析`（用验证器自身解析器遍历全仓） |
| `Git操作.校验分支名` 断言不符 | **我自己的回归**：实现本来就返回合规结果，我的包装又包一层把 `True` 翻成 `False` | 改为「缺参才拦、其余透传」 |
| 文件管理/环境变量/文本处理/转写/SQLite 断言不符 | **改了内容没同步断言**：夹具 base64/哈希/上限留旧值；环境变量多一个 `环境` 键；`查找文本` 是**下标**语义（返回 2 不是 1）、`统计长度`=5；转写输出首字母大写 | 逐项**先验实现语义再改断言**（实现都是对的） |
| 直播逐字稿需要本地模型 | 场景**禁止静态绝对路径** | 验证器新增 **「环境变量」动态值类型**（白名单两处同源）+ 门禁**环境依赖注入**（进程环境 → 40007 launchd 配置），取不到就报「环境依赖未就绪」 |
| 个人蒸馏 查询上下文 原为上游 500 | 上游 8898 已恢复 | **删掉豁免声明**并改正向场景（豁免不是长期通行证） |

**门禁运行前置（本机）**：`MLXWhisper提供者_模型路径` / `MLXWhisper提供者_模型名` —— 发布门禁自动从
`~/Library/LaunchAgents/com.huashi.gateway-40007.plist` 取；换机器时需先在环境里声明这两个变量。

## 八、当前实测基线（可直接引用）

```text
发布门禁：**通过**（唯一验收链路，退出码 0）
正式包 112 ｜ 能力 535 ｜ 契约版本唯一 2.0.0
能力定义不达标 0 ｜ 漂移 0 ｜ 摘要不闭合 0 ｜ 六环门禁 0 违规
组件合规 0 未通过包（原 44）｜ 依赖审计 0 违规 ｜ 场景可解析门禁通过
运行库：工程缓存/运行数据/（底座运行.db **十个**域表 + 库文件全部统一在此）
  任务 7 / 作业 1 / 协作状态 26 / 能力占用 22 行（协作状态旧文件已清，库唯一）
制品：唯一最新（入库/签名/安装/激活），制品仓库约 114M
网关 40007：健康 200
```

## 九、白天续做（2026-09-15 上午，已完成并验收）

| 项 | 结果 |
|---|---|
| **灰度指标入库**（运行态最后一块） | 运行库加 `灰度观测`／`灰度状态` 两域表，`灰度指标.py` 改走运行库（旧 JSONL 只读兼容 + 一次性搬迁）。**顺带修 2 个真 bug**：逻辑字段落 TEXT `"0"/"1"`（Python 里 `"0"` 是**真值**）→ 成功/失败/超时**统计算错**；数值字段落 TEXT → `'0.25'` 变字符串。已改 INTEGER/REAL + 读取归一 + 测试加 PRAGMA 类型断言锁住；测试 5/5、6/6 通过，生产库真实写入读回验证通过 |
| **`except Exception: pass` 清零** | 全平台 **20 处**改为「留痕」：行为不变（仍忽略），失败可见（新增 `公共契约/诊断/忽略记录.py`，有界 200 条可查） |
| **说明书唯一格式** | 按你定的「只留唯一值」：生成器为唯一事实源，**111/111 包**说明书重写 + 新增 `开发文档/能力目录说明书.md`；旧叙述稿可由 git 历史找回 |
| **协作状态旧文件**（E1） | 25 个历史 JSON 已被运行库 26 行完全覆盖 → 删除（不留两条腿），删后库仍正常服务 |
| **验证账本**（E3） | 已有「失败 0」的最新账本记录（场景 580／通过 709），并随最新制品重跑门禁重锚 |
| **测试资源清单** | 按你定的：**保持文件式**（测试证据留本地，不入库，不污染运行数据库） |

**你的决定已全部落地**：① 说明书**只留两个权威**——`说明/设计说明.md`（该库的**设计哲学与边界**，旧手写稿从 git 历史原样取回）+ `说明/使用说明.md`（**调用口径**：能力/参数/返回结构/错误码/调用示例，由能力契约生成），**两份都计入包指纹**，生成器已内置权威分工声明；② 91 个空白内部依赖声明**已删 90 个**（剩 16 个有真实依赖），判据两处**同源**「无文件 = 无内部依赖」；③ 测试资源清单**不入库**（文件式，测试证据留本地）；④ 依赖**不按项目切目录**——底座只装**原子能力**（内部依赖只走唯一能力调用／外部第三方必须经适配层翻译／项目侧走 HTTP 不进底座仓库）；⑤ 全仓包版本随内容变更升至 `1.0.2`。

## 十、发布门禁：**通过**（真实证据，不是"应该能过"）

| 项 | 实测值 |
|---|---|
| **发布状态** | **通过**（退出码 0） |
| 制品 | `6a6b9814527de6d1`（已入库/签名/安装/激活；激活指针已校验） |
| 依赖锁 | 校验 30 项，**0 漂移** |
| 组件合规 | 112 包，**0 未通过** |
| 公开调用完整性门禁（六环） | 通过，**0 违规** |
| 能力定义校验 / 漂移 | **0 不达标** / **0 漂移包** |
| 完整性摘要 | 112 包，**0 不闭合** |
| 账本最新记录 | 场景 580／通过 709／**失败 0** |
| 工作树 | 干净（提交 `51b3da36`） |

## 十一、今天在门禁链上抓出的真问题（9 项，全部真修，没有一处靠放宽门禁蒙过）

| # | 问题 | 性质 |
|---|---|---|
| 1 | 运行库把逻辑/数值字段存成 TEXT | **真 bug**：`"0"` 在 Python 是**真值** → 成功/失败/超时统计整体算错 |
| 2 | `Git操作.校验分支名` 包装重复包一层 | 我自己的回归（把 True 翻成 False） |
| 3 | 验证场景缺 `清理步骤` 键 / 夹具内容改了断言没同步 | 口径不同步 |
| 4 | 门禁两条硬约束：制品字节 = 工作区字节、制品来源提交 = HEAD | 已固化进技能（正确顺序：改码→提交→重编→门禁→最后改文档） |
| 5 | 说明书统一后未升包版本 | 平台规则：同一包同一版本禁止双摘要 |
| 6 | 摘要回算写回旧版本号 | 我的脚本错（已改为按当前版本回算） |
| 7 | 包升版本后依赖锁没跟着更新 | 依赖锁需重新生成（已纳入升版本流程） |
| 8 | 陈旧激活指针（指向被删的制品目录） | 我误删导致；用平台 `发布管理` **CAS** 切指针修复，坏指针文件留证 |
| 9 | 制品身份口径误伤溯源元数据 | 身份=只算内容文件（幂等）；存储/安装**仍带**溯源元数据（门禁要读 `制品来源.json`） |


