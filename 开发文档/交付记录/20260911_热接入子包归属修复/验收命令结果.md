# 验收命令结果
<!-- 机器管理｜类型：交付记录｜生成器：开发工具.MD文档生成 -->


## 1. 新增门禁黑盒测试

```bash
/opt/homebrew/opt/python@3.14/bin/python3.14 -m unittest 测试中心.运行核心.测试_热接入子包归属 -v
```

```
test_声明未注册仍被拦下 ... ok
test_未独立成包的子域仍算父包未声明 ... ok
test_父包自注册未声明仍被拦下 ... ok
test_独立子包能力不计入父包注册未声明 ... ok
----------------------------------------------------------------------
Ran 4 tests in 0.018s
OK
```

MCP `verify_and_record` 入账结果：退出码 0 / 判定「验证结果判定通过」。

## 2. 相关既有回归

```bash
python3.14 -m unittest 测试中心.支持库.核心.测试_语法索引   # Ran 32 tests ... OK
python3.14 -m unittest 测试中心.运行核心.测试_包发现聚合子域  # Ran 1 test ... OK
```

## 3. 活网关反证测试（40007）

```bash
touch 支持库/后端/代码解析支持库/.热接入验证临时文件
python3.14 运维脚本/热接入.py
```

修复前：`✗ 热接入失败 … 注册未声明: ['代码解析支持库.语法索引.批量解析语法', …]`
修复后：

```
✓ 热接入完成（0.59s）
新增包 0 / 变更包 1 / 卸载包 0 / 已注册能力数 549
成功包: · 支持库.后端.代码解析支持库（变更）
```

## 4. 三个新能力真实 HTTP 调用（40007 `/网关/调用`）

```bash
POST http://127.0.0.1:40007/网关/调用
Authorization: Bearer <系统库网关凭证>
{"能力id": "代码解析支持库.语法索引.解析Python语法", "参数": {"代码文本": "…"}}
```

| 能力 | 成功 | 结果 |
|------|------|------|
| 解析Python语法 | True | 事件序号 0导入 → 1符号T → 2类属性 → 3符号T.m → 4/5调用 |
| 解析脚本语法 | True | 解析器 tree-sitter；导入/调用事实正确 |
| 批量解析语法 | True | 结果保序 `['a.py','c.vue']`；`bad.py` 隔离为 `语法错误` |
