# 修改文件清单
<!-- 机器管理｜类型：交付记录｜生成器：开发工具.MD文档生成 -->


| 文件 | 操作 | 说明 |
|------|------|------|
| `后端核心/后端核心.py` | 修改 | `热接入()` 收集并传入已发现包 id 集合；`_校验热接入包` 前缀归属分支改为只覆盖未独立成包的子域 |
| `测试中心/运行核心/测试_热接入子包归属.py` | 新增 | 4 条黑盒测试：独立子包不计入父包 / 未独立子域仍算父包 / 父包自注册未声明仍拦下 / 声明未注册仍拦下 |
| `支持库/后端/代码解析支持库/完整性摘要.json` | 修改 | 重新生成，覆盖新增的 `语法索引/` 8 个文件（24 文件），版本 1.1.0 |
| `开发文档/交付记录/20260911_热接入子包归属修复/` | 新增 | 本五件套 |

## 不属于本工作包的改动（其他会话在途，禁止提交）

- `支持库/后端/代码解析支持库/{__init__.py,包声明.json,能力契约/参数契约.json,能力定义.json}`（B4 旁路修复）
- `支持库/后端/系统核心支持库/检查点恢复/{__init__.py,完整性摘要.json}`（B2 旁路修复）
- `支持库/后端/代码解析支持库/语法索引/`、`测试中心/支持库/核心/测试_语法索引.py`（归属 4/6 支持库工作包）
