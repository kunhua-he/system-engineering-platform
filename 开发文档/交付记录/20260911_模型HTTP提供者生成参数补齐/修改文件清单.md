# 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `支持库/适配层/模型HTTP提供者.py` | 修改 | 新增 `_归一化响应()` 延迟导入连接器 `归一化对话响应`；`调用对话` 签名补 `温度/最大令牌数/工具/响应格式`，载荷按 chat_completions / codex_responses 两协议映射，成功值键集对齐 `_HTTP调用模型` |
| `测试中心/支持库/测试_模型HTTP提供者.py` | 新增 | 3 条注册路径契约回归：不清空 `调用函数表`，显式 `注册模型HTTP提供者()`，走 `生成对话` 全量生成参数 |
| `开发文档/临时文档/44_模型HTTP提供者调用对话生成参数补齐.md` | 新增 | 本任务临时文档（阶段一问题 / 阶段二实际结果） |
| `开发文档/交付记录/20260911_模型HTTP提供者生成参数补齐/` | 新增 | 本五件套 |

## 提交

| 提交 | 内容 |
|------|------|
| `23d69d0` | 修复 `调用对话` 生成参数缺失 + 临时文档 44 |
| `24521b6` | 补注册路径生成参数契约回归测试 |

## 未改动（有意）

- `支持库/后端/大语言模型支持库/模型连接器/实现/模型连接器.py`（`_HTTP调用模型`、`_调用模型`、`生成对话` 均未动）
- `支持库/后端/大语言模型支持库/__init__.py`（注册点未动）
- `注册模型HTTP提供者()` 未动

## 说明

`支持库/适配层/模型HTTP提供者.py` 不被任何 `完整性摘要.json` 收录（已 grep 确认），
无需重算包摘要。
