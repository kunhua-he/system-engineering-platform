# 验收命令结果
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 1. 新增契约回归（底座，注册路径）

```bash
python3.14 -m 测试中心.支持库.测试_模型HTTP提供者
```

```
...
----------------------------------------------------------------------
Ran 3 tests in 1.546s

OK
```

覆盖：chat 协议 `temperature / max_tokens / tools / response_format` 全量下传 + 响应归一化键集；
codex 协议 `temperature / max_output_tokens / text.format` 映射；单次级 `附加请求头` 覆盖连接级同名键。

> 该测试刻意**不清空** `调用函数表`（对照组：`测试中心/第一批维修/测试_模型连接器HTTP.py`
> 清空函数表测 `_HTTP调用模型` 兜底路径）。修复前该测试必失败于
> `调用对话() got an unexpected keyword argument '温度'`。

MCP `verify_and_record` 入账结果：退出码 0 / 判定「验证结果判定通过」/ 提交 `24521b6`。

## 2. V3 侧端到端验收（会话亲缘性，真实 HTTP 经 40007）

```bash
python3.14 测试中心/运行测试.py --测试文件 测试中心/后端服务/其他测试/网关测试/测试_底座提供商会话亲缘性.py
```

```
通过:     10
失败:     0
非法跳过: 0
10 passed in 2.122s
```

含两条真实 HTTP 用例（经 40007 打到本进程回显上游）：
`测试_真实HTTP_会话头与额外请求头确实出站`、`测试_真实HTTP_流式会话头与额外请求头确实出站`。

## 3. 40007 冷启动实况

```bash
launchctl kickstart -k gui/$(id -u)/com.huashi.gateway-40007
lsof -nP -iTCP:40007 -sTCP:LISTEN
```

```
Python  38865 用户        9u  IPv4 0xfbfacf6f7fa29199  0t0  TCP 127.0.0.1:40007 (LISTEN)
```

重启后非流式与流式两条真实 HTTP 路径均通过（见上）。修复前此状态下每次 LLM 调用
均返回 `模型调用失败: … unexpected keyword argument '温度'`。

## 4. 未覆盖（明确声明）

- 流式路径的 `温度/最大令牌数/工具/响应格式` 仍被丢弃 —— `模型连接器.流式生成对话`
  签名不接收这四个参数，属既有设计面，见 `剩余风险.md` 第 3 条。
- 热接入态未单独复验：`支持库/适配层` 不可热挂载，本改动只经冷启动重启生效。
