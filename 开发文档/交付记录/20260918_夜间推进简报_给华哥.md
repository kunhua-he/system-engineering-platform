# 系统工程平台 · 2026-09-18 夜间推进简报

> 面向：华哥 ｜ 撰写：小马仔 ｜ 时段：2026-09-18 00:30 – 05:35 CST
> 口径：所有数字均为**现场实测**，附取证方式；报告不写推测值。

---

## 一、一句话总结

**51 个子代理分 8 批跑完，50+ 个提交落地。**四条最贵的指标全线收敛：
裸布尔降 74%、全面漂移降 79%、覆盖率缺口归零、依赖防火墙归零。
**你的两条建议（禁用代称、800 行拆分）都已落成机制。**
卡点 B（判据在但门禁不调）从 0 条接到 4 条，正在接最后 3 条。

---

## 二、四条指标的收敛（实测）

| 指标 | 开工前 | 现在 | 降幅 | 取证方式 |
|---|---:|---:|---:|---|
| 裸布尔存量 | 1610 条 | **421 条** | **-74%** | `校验裸布尔防回潮()` 每次现场报数 |
| 全面漂移缺口 | 126 条 | **26 条** | **-79%** | `全面漂移扫描('.')` |
| 场景覆盖率缺口 | 2 条 | **0 条** | 归零 | `验证场景体检 --覆盖` 报「公开能力 681 个全部有正向目标步骤」 |
| 依赖防火墙违规 | 4 条 | **0 条** | 归零 | `审计依赖('.')` |
| 中文边界假红 | 4 条 | **0 条** | 归零 | `_扫描英文函数命名()` 返回空 |
| 防回潮四项 | 三项未接 | **全绿** | — | 裸布尔/非正式类型名/英文错误码/占位代称 |

> **为什么 126 → 26 是"修判据"而不是"改数据"**：逐条归因后发现
> **参数顺序漂移 56 条 + 声明无实现 16 条**全部是判据误判（判据抓错实现文件、
> 把可选参数当漂移、认不出转调文件），真正的缺陷只有 26 条。修的是判据。

---

## 三、你的两条建议 → 已落成机制

### 1. "怎么又来甲乙丙丁了"

| 动作 | 结果 |
|---|---|
| 全仓清零 | **7 个文档 117 行** → 统一改为 `生成物类／人工类` |
| 落入哲学 | 规范里加"取代早前用甲乙代称的写法"一句，防后续再用 |
| **接进门禁** | **防回潮第四项「占位代称」**：扫描 3162 份正式源码与文档，存量 0、新增即红 |
| 反向验证 | 0 →（写入含代称夹具）**红且精确定位到夹具行** →（还原）0 |

**这条的价值不在清零，在于"下次谁再写就会被门禁拦住"。**

### 2. "超过 1000 行的拆到 800 左右"

| 动作 | 结果 |
|---|---|
| 落入哲学 | 6.1 补「**拆分候选线 = 800 行**」（不是失败线、不是门禁，是候选线） |
| 建候选清单 | `超长文件拆分候选_20260918.md`：按「**提交次数 × 行数**」分三档 |
| **已拆完 2 份** | `平台客户端制品.py` **963 → 520 行**（拆出 3 文件，测试 18/18 OK）<br>`网关核心.py` **1240 → 1074 行**（拆出 2 文件，五表逐项相等已核） |
| 低频不拆 | 3 份按哲学 6.1 原文「低频文件到 800 行也无所谓」**明确不拆** |

> 排序依据用的是你哲学里的原文判据：**改动频率 × 读取成本**。
> 第一优先 5 份 = 提交次数 ≥20 且 >800 行（发布门禁 49 次、模型连接器 35 次…）。

---

## 四、补底层：修流程缺口，不是再叮嘱一次

按你的"反复犯错的开发应该补底层"，本夜补了 **4 个工具 + 3 条哲学条款**：

| 补的东西 | 治的是什么病 | 实测效果 |
|---|---|---|
| `开发工具/派活前核验.py` | 我按旧清单**重派了 6 路已修完的活**，白耗 30 分钟 | 派活前查 git 历史判"是否已修"，命中就改派"独立复核" |
| `开发工具/快速编译/单文件自查.py` | 并行期**某路把语法错的中间态留在磁盘**，让别的路 11 个测试全红 | 改完立刻自查（<1 秒），当天就拦住同类 |
| `开发工具/任务记忆/任务记忆.py` | 子代理**反复全盘搜**（某路 80 次 terminal 调用全在找文件） | 任务信只给一行读命令，正文进记忆包 |
| 哲学 12.3⑧⑨⑩ + 14.3 | 上面三件事的共同根因 | 写进哲学，后续 Agent 照做 |

---

## 五、卡点 B：判据在但门禁不调（本轮最重要的结构修复）

**问题的形状**：底座有 5 个真门禁，但"哲学判据"这一层**几乎全在门禁之外** ——
判据写了、单测有了、就是没接到"拦人"的位置上。按哲学 13.1 那叫**纸面规则**。

### 判定口径（关键：按真实 import 判，不看字面）
> 总账自己写明的教训：`"反向破坏"`、`"测试体系门禁"`在发布门禁里**都出现过**，
> 但全是帮助文本、零调用 —— 只看字面会得出完全相反的结论。

### 进度
| 判据 | 状态 |
|---|---|
| 全面漂移检测 | ✅ 已接（含"基线外新增"强制项） |
| 能力 id 只增不改 | ✅ 已接（冻结基线 679 条，缺失 fail-closed） |
| 防回潮四项 | ✅ 已接 |
| 消费者登记环 | ✅ 已接（修了键名/类型双层错，反验删基线 3 条→红） |
| **能力调用图审计**（哲学 1.3/8.2） | ✅ **本轮已接**（防回潮项五，反验：0→红精确定位→0） |
| 破坏验证（哲学 1.4） | 🔄 接线中 |
| 对外契约变更判定（哲学 5.3） | 🔄 接线中 |
| **测试体系门禁**（★ 最要紧） | 🔄 接线中 |

> **为什么"测试体系门禁"最要紧**：现状是**"门禁绿 ≠ 测试跑过"** ——
> 发布门禁文件头自己写着"本门禁不执行全量 unittest（测试体系门禁仍需单独手工执行）"。
> 这条接上后，发布门禁的"通过"才真正可信。

---

## 六、本轮抓到的真缺陷（都是真问题，不是误报）

| # | 缺陷 | 我的处置 |
|---|---|---|
| 1 | **我自己引入的**：生产门禁去读「测试中心」取真源 | 判据归位 → 直读生产视图 + 冻结 6 个真实英文历史键；门禁源码"测试中心"字样 **0 处** |
| 2 | **我自己引入的**：基线停在 589 而实测 421 → **虚高基线会漏判回潮** | 按本项自己写明的规则重建基线 589→421；反验 0→红→0 |
| 3 | 说明书参数类型不一致 **20 处**（两份人工类说明书没同步契约改动） | 逐处修；**只改「参数列表」**，返回结构里的 `句柄id: 整数型` 保留（那是对的） |
| 4 | 开工编排说明书缺 2 个能力段 | 人工补齐（**先真跑全链取真值**再写，不编） |
| 5 | 判据模块载入失败（按路径加载时缺 `sys.path`） | 显式补根 |
| 6 | `测试_支持库模板生成器` 期望过时（模板已按决策 0033 正确实现，测试还要求必有依赖锁） | 改成**两向断言**（无第三方→无锁；有第三方→有锁） |
| 7 | `测试_发布门禁收敛` 越界样本用 `main`，与白名单冲突 | **换样本不改白名单**（白名单对：`main` 是 Python 入口惯例） |
| 8 | 依赖防火墙 4 条违规（`MCP工具箱/` 该删的目录被误还原） | 备份后清理，恢复 D 清场既定状态 |
| 9 | 证据 JSONL 带**冲突标记**（历史遗留未解合并） | 取 ours 覆盖（证据只增不删），**0 行非法 JSON** |

---

## 七、账目校准（这条对你重要：账本本身失真了）

`开发文档/未完成事项.md` **严重滞后** —— 我逐个现场核验后：

- **累计 26 项**"其实早已完成但还标着待办" → 已逐个销项（附核验证据）
- §8.3 待修项从"几乎全开"变成 **33 条已闭合 / 19 条未闭合**
- 另修了我自己**一次误判**：以为"追加字节数"说明侧写错，
  现场读真源（能力定义 + 实现逐行）确认**两个字段本就并存**，那 3 处写法正确，已还原零副作用

**已闭合的代表项**：A 段四项（A1 嵌套事务破坏原子性 / A2 释放锁 / A3 版本保护 / A4-A5）、
SSRF 校验 IP 绑定、解压回滚删用户文件、原子落盘丢环境、ZIP 截断既有目标、
激活指针 schema 双套、依赖防火墙 Windows 路径、README 静态漂移…

---

## 八、留给你决定的两件事

| # | 事项 | 现状 | 需要你 |
|---|---|---|---|
| 1 | **`MCP工具箱/` 目录** | 平台文档明确该目录 2026-09-15 已随 D 清场删除，但磁盘被误还原出 6 个 `.py`（导致依赖防火墙误报 4 条违规）。我**备份到 `/private/tmp/` 后清理** | 若你要保留，说一声我恢复 |
| 2 | **5 个旧 worktree** | 已核验：2 个 0 改动（可删）、2 个带"路径写法差异"（`~/` vs 绝对路径，是历史快照，**无活的待合并改动**）、1 个是分支 worktree（不得贸删） | 清理排在维修全部落地后；要不要现在清 |

### 另有一处口径冲突待你确认
第 43 项债务原写"**只支持 macOS arm64，入口拦 Windows**"，
而 `公共契约/运行时/平台适配.py` 文件头记的是**你 2026-09-16 的另一条裁决「底座做完整跨平台」**
（已收口 POSIX 硬编码的 venv 路径与 `start_new_session`，Windows 分支经 monkeypatch 验证、**未经真机实测**）。

**两者当前并存**：入口对非 macOS/arm64 fail-closed，同时适配层已具备 Windows 分支。
**要不要对 Windows 放开入口准入？** 这个需要你定。

---

## 九、剩余未闭合项（19 条，按类）

**已在做（子代理在途）**：C3 正向步骤补业务断言、超时线程准入、PDF 线程泄漏、
审计器模块级变量假红、负路径 5 类断言、混血说明书对账、产品模板缺件、冷启动预算、准入后端断言

**待办（未派）**：
- 6 个聚合包 roll-up 声明与子包真源差（判据/表达问题）
- 说明书存量治理施工表（13 格，含禁裸重生成约束）
- 能力面按层构成的"冻结增长"落点
- 构建链调用面未收口（4 处直调）+ 激活指针三能力立项
- 6 个聚合包与说明书 B 类 6 混血包的行级合并债

---

## 十、复核方式（审计者可直接跑）

```bash
cd /Users/hekunhua/Documents/Agent/PHP/系统工程平台
unset PYTHONPATH

# ① 四项防回潮（每项都会现场报数）
python3.14 -c "import sys;sys.path.insert(0,'.');import 开发工具.发布门禁.运行发布门禁 as M;
for n,f in [('裸布尔',M.校验裸布尔防回潮),('非正式类型名',M.校验非正式类型名防回潮),
            ('英文错误码',M.校验英文错误码防回潮),('占位代称',M.校验占位代称防回潮)]:
    print(n, f()[0])"

# ② 覆盖率（应报「公开能力 681 个全部有正向目标步骤」）
python3.14 开发工具/验证场景体检.py --覆盖 工程缓存/制品仓库/平台客户端制品/<最新制品>

# ③ 依赖防火墙（应报 0 违规）
python3.14 -c "import sys;sys.path.insert(0,'.');from pathlib import Path;
from 运行核心.依赖防火墙 import 审计依赖;x=审计依赖(Path('.'));print('违规',len(x.违规列表))"

# ④ 派活前核验（新增工具，看某缺陷是否已修）
python3.14 开发工具/派活前核验.py --文件 <文件> --关键词 <关键词>
```

---

*本简报依据现场实测撰写；所有"已修"结论均有真实测试或反向验证支撑，无推测值。*
