# 收口轮交付报告（2026-09-16）

> 口径：本报告是 **2026-09-16 收口轮**的交付记录；数字均为**本机现场实测**，非历史快照。
> 当前事实源仍是 `开发文档/项目说明.md`，债务清单仍是 `开发文档/未完成事项.md`。
<!-- 机器管理｜类型：交付记录｜生成器：开发工具.MD文档生成 -->


## 一、本轮做了什么（按类）

### 1. 跨平台收口（8 个批次 A1→B7）
- 新增两个收口层：`公共契约/运行时/平台适配.py`（纯平台判定）、`公共契约/运行时/进程终止.py`（进程与进程组）。
- 全仓进程专有调用（`killpg` / `SIGKILL` / `start_new_session` / `setsid` / `os.fork`）**全部收进这两个文件**；调用点保留原语义，不再各写一份平台分支。
- 四个维度扫完：平台判定 35 处 → 0（收口层内）、进程终止 160 处 → 0、解释器路径 12 处 → 0、POSIX 专有（`resource`/`fcntl`/`os.fork`）→ 惰性导入 + 能力探测。
- 另扫九个跨平台维度：换行符（新增 `.gitattributes` 全仓 `eol=lf`）、编码、路径/`os.devnull`、外部命令（`shutil.which`）、文件锁、时区、大小写等。
- **规矩**：平台差异只许在两个收口文件里判断；调用点不许自己写平台判断；某处必须加判断才能改通时报回来补收口层，不许就地分叉。

### 2. 治「验证骗人」（本轮最大产出）
今日累计修掉 **9 处**「门禁/验证器在骗人」的问题，含：破坏验证器宽 `except` 把执行异常记成检出成功、合规检查器误报与同进程假红、依赖审计死判据、发布门禁死参数与测试门禁从未接线、恒真门禁（键名不一致致校验永不生效）、**场景加载失败掩盖 31 项既有失败**、HTML 验证器类型表只有 11/16 个正式类型名（假红 + 真漏检）、门禁漏扫 `平台控制面`。
**判定工具一旦不准，所有「验证通过」都不可信** —— 这是本轮修它们的理由。

### 3. 部署可用性（华哥问「GitHub 上的别人能不能部署」）
- 数据库：6 个被跟踪的 db **全是验证夹具**（假数据；3 个 0 字节），运行时真库在 `工程缓存/` 已忽略 → **不需要处理**。
- 硬编码路径清零：`/Users/hekunhua` 6 处 → `<仓库根>`；`/opt/homebrew` 27 处 → 9 个生产代码改 `python3.14`（靠 PATH 查找）+ 12 个文档。
- **干净克隆冷启动实测通过**：`工程缓存/` 不存在、装配 625 / 无跳过包、六环 0、运行时库自动创建。
- 新增 `开发工具/环境自检.py`（21 项）：拿到仓库一跑就知道本机能不能跑、缺什么。

### 4. 新增工具
| 工具 | 用途 |
|---|---|
| `开发工具/环境自检.py` | 21 项本机可用性自检；有不支持项即非零退出 |
| `开发工具/重建依赖锁.py` | 换平台/换环境后重建锁；默认干跑只报差异，`--执行` 才写盘。**此工具此前根本不存在**，而报错文案一直让人「去重建」 |
| `开发工具/全量重算摘要.py` | 摘要重算（会级联重算聚合父包，全仓 10 个）；踩过 4 次「漏算父包」后工具化 |

## 二、收口动作（本轮）

### 文档收口
- `开发文档/未完成事项.md`：并入八份审计报告的全部结论（9 段约 130 条：〇已修销项 / A 验证可信度 / B 类型命名与契约 / C 能力化缺口 / D 结构设计债 / E 越层分层偏差 / F 资源与生命周期 / G 口径漂移 / H 需裁决），并修正本文件自身 6 处失真。
- `AGENTS.md`：补 7 条新铁律；修 5 处失效入口（8766 体系已整体删除 → stdio 薄壳 3 工具 + 40007）。
- `项目说明.md` / `当前流程图.md` / `底座升级方案.md` / `提速方案.md` / `新建支持库与模块指南.md` / `网关调用最小示例.md` / `能力目录说明书.md` / `README.md`：按实测改写失效入口与数字口径。
- `开发文档/主开发文档.md`：补三个新工具入口 + 4 个漏索引文档 + 环境自检提为「步骤 0」。
- `开发文档/多会话并行开发规约.md`：**整篇重写**（265→383 行）——原篇基于已删除的 8766 管理端 46 工具，照做不可执行。
- **删除 8 份临时审计报告**（结论并入权威文档后按项目规则清理；历史由 git 保留）。

### 制品收口
- 激活制品 `平台客户端-f099668f35cdb924`（版本 104），**制品来源提交 = 当前 HEAD**。
- 清理过期制品 6 项，释放 26.3 MB。

## 三、终检数据（本机实测）

```text
装配         True 运行中 625 跳过包 0
六环门禁      0 违规
依赖防火墙    645 文件 / 0 违规
摘要         全闭合
环境自检      21 项通过、退出码 0
制品         来源提交 = HEAD，激活正常
```

## 四、本轮的两个实测新发现

1. **并发写护栏的「自动门」并不存在**：`开工编排.开工准备` 可达但**只校验、不互斥**（文件租约实测恒为「未申请」，说明申请/续租/释放尚无公开原子能力）；写前 SHA256 乐观锁**通道已通**（假摘要 → 500 `文件摘要不符`，干跑不落盘）但「预期文件摘要」是**可选**参数 → 属可用护栏、非强制闸门。已记入债务清单 C-14。
2. **制品进程会把运行时数据写回制品目录内部**：跑 HTML 黑盒时写出 `工程缓存/…` 等 6 个文件，导致激活指针校验发现摘要不符 → **拒绝安装**。机制正确（脏制品不许上线），根因与「场景相对路径写脏制品」同源，已记入待办。

## 五、两处只有真机才能验证（如实标注，未写成「已验证」）

- `客户端/构建平台客户端.py` 的 `dir_fd=`（POSIX 专有安全打开写法，属构建机工具）。
- **中文环境变量名**（`os.environ.get("系统库运行时")` 等 19 处）：全仓 42 处中文键，父进程传 → 子进程逐字回读比对已在本机验证，但 Windows 上的行为未测。
  → **处置：仅留备注，不深究**（华哥 2026-09-16 裁决）。

## 六、华哥裁决（2026-09-16）

**错误码不得因未登记而回落成 500 内部错误。** `发布者不存在` 这类错误码**契约里已经声明**，网关按契约登记即可；**契约声明了什么就登记什么**，按平台既有口径（X 不存在 → 404）。据此：「77 个已声明但未登记状态映射的错误码」按契约补齐登记，并同批加门禁项「能力定义声明码 ⊆ 状态映射码」防第四批漂移。
