# 数据库访问收口 + 网关空转清理：验收记录（2026-09-14）

> 依据华哥裁决：哲学第 10 条（结果唯一即收口：保留最全面的那个 + 归因原子能力，其余蒸馏进唯一支持库）、第 11 条（空转即杀）、第 12 条（第三方在最底层翻译层）、第 17 条（临时脚本不留库）。
> 关联：`开发文档/决策记录/0008`（唯一同步池）、`0009`（依赖分两段）。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 一、数据库访问收口（重复腿全部删除）

**核实（主会话现场复核，非照抄子代理）**：
- `适配层/pg8000提供者` 的实现文件与 `后端/pg8000数据库` **sha256 完全相同**；`适配层/psycopg2提供者` 与 `后端/psycopg2数据库` 同理（仅 `__init__.py` 不同）——同一份代码被复制两遍。
- `适配层/psycopg提供者` 是 `后端/psycopg数据库` 的**过期副本**（193 行 vs 371 行）。
- 旧能力 id（`psycopg数据库.连接/查询/事务执行/关闭`、`pg8000数据库.*`、`psycopg2数据库.*`）在包外**零引用**；**V3 侧生产代码零引用**（45 处命中里 44 处在审计文档、1 处在测试且非该 id）。
- `平台控制面/提供者/PostgreSQL提供者.py` 零调用方零测试；`平台控制面/提供者/数据库提供者.py`（SQLite 池）零生产调用方。

**已删除（4 个整包 + 1 单文件 + 2 个死测试）**：
`支持库/适配层/pg8000提供者`、`支持库/适配层/psycopg2提供者`、`支持库/后端/数据库连接支持库/pg8000数据库`、`支持库/后端/数据库连接支持库/psycopg2数据库`、`平台控制面/提供者/PostgreSQL提供者.py`、`测试中心/支持库/测试_pg8000提供者.py`、`测试中心/支持库/测试_psycopg2提供者.py`。

**引用清理（逐处）**：`平台控制面/提供者/__init__.py` 两处导出、`SQLite数据库/复用决策.json` 旧候选、`测试中心/项目适配/测试_项目锁传递闭包.py` 5 处夹具包id（改用保留包 `SQLite数据库`）、`运行核心/环境指纹/环境指纹.py` 两条已不存在的驱动版本探测。
**保留判定**：`运行核心/依赖防火墙.py` 与 `开发工具/复用审计/提供者直连规则.py` 里的驱动名表**保留**——它们用于识别第三方导入，留着能抓住「重新引入旧驱动」，删了反而漏检。

## 二、psycopg 落成最底层翻译层（第三方边界归位）

**改造**：`支持库/适配层/psycopg提供者` 从「旧四能力拷贝」改为**唯一直接接触 psycopg 的翻译层**——对外 9 个中文原语（打开连接 / 释放连接 / 归类错误 / 解析连接串 / 校验连接串 / 校验超时 / 驱动可用 / 驱动版本）＋唯一公开能力「检查可用性」（健康探针，无参数、无副作用）。旧四条「连接串一次性能力」**全删**。
`支持库/后端/数据库连接支持库/psycopg数据库` 的六份本地重复原语与 `import psycopg` **全删**，改经翻译层调用——支持库里再无第三方英文 API（实测 0 处 `psycopg.` 引用）。

**产物同步**：能力定义（唯一探针能力）、包声明能力声明、验证场景引用（唯一正向步骤覆盖唯一公开能力）、权限契约、参数契约、能力数据、完整性摘要，全部按平台编译器与生成器重算。

**验收**：`编译能力定义` 问题列表 `[]`；`公开调用完整性门禁`（六环）**通过 0 违规**；`依赖生命周期审计` 仅剩历史项 `YAML提供者` 2 条（缺健康探针/缺停止入口，既有债务）；`测试中心.支持库.测试_数据库连接池` **10/10**（真连本机 5432）；翻译层与支持库摘要**均闭合**。

## 三、网关三操作「空转即杀」（华哥本轮裁决）

`版本查询 / 激活版本查询 / 诊断查询` 三操作：零装配、零调用方，且三者共用同一个从未注入的回调（`诊断查询` 与版本无关却同入口，语义自相矛盾）→ 按第 11 条**直接删除**。

**改动**：`运行核心/统一网关/网关核心.py` 删 `允许操作表` 3 项、`操作权限表` 3 项、`允许字段表` 3 项、构造函数 `版本查询` 形参与赋值、处理分支（613 → 599 行）。**装配面**：全仓无装配点，删除后无残留（示例项目制品 20260822-demo 是旧封装产物，按第 14 条属旧制品，不手改）。

**验收**：`py_compile` 通过；网关波次 `测试_网关安全负测`+`测试_资源句柄网关`+`测试_网关任务接入` **23/23**；重启 40007 后真实 HTTP——`健康检查` 200、三操作均 **400 明确失败**（不再是「提供者不可用」）；端到端池链路复跑全绿：建池 200（句柄 726929）→ 查询 200 → 事务 200 → 池状态 200 → 无效句柄 **500 句柄失效** → 关闭 200。

## 四、临时脚本清理

本次全部探针/验收脚本已从 `/tmp` 删除；仓库内无一次性脚本（已 grep 核实，命中的均为正式工具内的「探针」字样）。该口径固化为哲学第 17 条。

## 五、未做（如实声明）

1. **SQLite 池收口未做**：`平台控制面/提供者/数据库提供者.py`（485 行有界池）零生产调用方，按第 10 条应把「有界池/超时中断/硬截止关闭」蒸馏进唯一支持库 `支持库/后端/数据库连接支持库/SQLite数据库` 后删除该文件（连带重写 `测试中心/第一批维修/测试_SQLite并发与收敛.py`）。属独立一批。
2. **三产物治理字段未做**：25 包 / 69 能力缺 行为/错误码/返回/提供者；编译器已修成非破坏，全量重生成待排一批。
3. **制品重编译与归档未做**：激活制品锚 `f05477c` 落后 HEAD 120 提交，归档机制（0 处实现）待建；不重编译则发布门禁「校验制品来源绑定」必红。
4. **`平台控制面/提供者/数据库提供者.py` 之外的 D 类小项**（两份「当前.json」键形状、`错误码白名单` 只判非空等）见 `开发文档/临时文档/52_死代码与名实不符_子代理盘点.md`。
