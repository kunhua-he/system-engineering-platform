# 媒体处理（模块库.媒体处理）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：基础模块
- 版本：1.0.3
- 说明：组合 FFmpeg 支持库公开能力（基础模块）：检查提供者、探测媒体（时长/流/格式）、提取音频（wav/mp3等，字节返回）、转码（编码选项白名单透传）、抽取帧；只经 获取能力调用器().调用能力 调用，错误码统一透传。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `媒体处理支持库.FFmpeg媒体.检查提供者` 版本约束 `>=1.0.0`
- 能力 `媒体处理支持库.FFmpeg媒体.探测媒体` 版本约束 `>=1.0.0`
- 能力 `媒体处理支持库.FFmpeg媒体.提取音频` 版本约束 `>=1.0.0`
- 能力 `媒体处理支持库.FFmpeg媒体.转码` 版本约束 `>=1.0.0`
- 能力 `媒体处理支持库.FFmpeg媒体.抽取帧` 版本约束 `>=1.0.0`

## 能力
### 检查媒体处理提供者（`媒体处理.检查提供者`）
- 版本：1.0.0
- 一句话说明：检查 ffmpeg/ffprobe 可用性并返回版本；缺失 → 提供者不可用
- 所属支持库或模块：模块库.媒体处理
- 参数列表：超时秒:双精度数型
- 必填参数：无
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '字典（版本：文本）', '错误': '字典（错误码/说明/来源）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用
- 调用示例：媒体处理.检查提供者(超时秒=30.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.检查提供者, 媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.提取音频, 媒体处理支持库.FFmpeg媒体.转码, 媒体处理支持库.FFmpeg媒体.抽取帧
### 探测媒体（`媒体处理.探测媒体`）
- 版本：1.0.0
- 一句话说明：探测媒体文件：{时长秒, 流, 格式, 大小字节, 比特率}；损坏 → 损坏媒体
- 所属支持库或模块：模块库.媒体处理
- 参数列表：文件路径:文本型, 超时秒:双精度数型
- 必填参数：文件路径
- 可选参数：超时秒
- 返回结构：{'类型': '结果型', '值结构': {'时长秒': '浮点数', '流': '列表型', '格式': '文本型', '大小字节': '整数型', '比特率': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超时, 取消, 进程崩溃
- 调用示例：媒体处理.探测媒体(超时秒=60.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.检查提供者, 媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.提取音频, 媒体处理支持库.FFmpeg媒体.转码, 媒体处理支持库.FFmpeg媒体.抽取帧
### 提取音频（`媒体处理.提取音频`）
- 版本：1.0.0
- 一句话说明：从视频提取首个音频轨（wav/mp3/aac/m4a/ogg/flac），字节经 b64 返回；无音轨 → 无音轨
- 所属支持库或模块：模块库.媒体处理
- 参数列表：文件路径:文本型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：{'类型': '结果型', '值结构': {'字节b64': '文本型', '格式': '文本型', '字节数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 无音轨, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理.提取音频(输出格式=wav, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.检查提供者, 媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.提取音频, 媒体处理支持库.FFmpeg媒体.转码, 媒体处理支持库.FFmpeg媒体.抽取帧
### 转码（`媒体处理.转码`）
- 版本：1.0.0
- 一句话说明：ffmpeg 转码（mp4/mkv/webm/mov/avi）：返回 {格式, 字节数, 输出路径}（可选 字节b64）；编码选项白名单：分辨率/视频码率/音频码率/帧率/视频编码器/音频编码器/码率上限/缓冲/视频标签/预设；Apple Silicon 推荐 h264_videotoolbox/hevc_videotoolbox 硬编
- 所属支持库或模块：模块库.媒体处理
- 参数列表：文件路径:文本型, 输出格式:文本型, 编码选项:字典型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型, 返回字节:逻辑型
- 必填参数：文件路径
- 可选参数：输出格式, 编码选项, 输出路径, 超时秒, 最大输出字节, 最大时长秒, 返回字节
- 返回结构：{'类型': '结果型', '值结构': {'字节b64': '文本型', '格式': '文本型', '字节数': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理.转码(输出格式=mp4, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.检查提供者, 媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.提取音频, 媒体处理支持库.FFmpeg媒体.转码, 媒体处理支持库.FFmpeg媒体.抽取帧
### 抽取帧（`媒体处理.抽取帧`）
- 版本：1.0.0
- 一句话说明：ffmpeg 抽帧（jpg/png，时间点秒），字节经 b64 返回：{字节b64, 格式, 宽度, 高度}
- 所属支持库或模块：模块库.媒体处理
- 参数列表：文件路径:文本型, 时间点秒:双精度数型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 最大时长秒:双精度数型
- 必填参数：文件路径, 时间点秒
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 最大时长秒
- 返回结构：{'类型': '结果型', '值结构': {'字节b64': '文本型', '格式': '文本型', '宽度': '整数型', '高度': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 损坏媒体, 超长媒体, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理.抽取帧(输出格式=jpg, 超时秒=60.0, 最大输出字节=209715200, 最大时长秒=7200.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.检查提供者, 媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.提取音频, 媒体处理支持库.FFmpeg媒体.转码, 媒体处理支持库.FFmpeg媒体.抽取帧
### 拼接媒体（`媒体处理.拼接媒体`）
- 版本：1.0.0
- 一句话说明：按顺序无损拼接多个同源分片成一个文件（concat demuxer + -c copy，不重编码）；模块层门面，经唯一网关调用 FFmpeg 支持库原子能力；错误码透传
- 所属支持库或模块：模块库.媒体处理
- 参数列表：文件列表:列表型, 输出格式:文本型, 输出路径:文本型, 超时秒:双精度数型, 最大输出字节:整数型, 返回字节:逻辑型
- 必填参数：文件列表
- 可选参数：输出格式, 输出路径, 超时秒, 最大输出字节, 返回字节
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 提供者不可用, 超时, 取消, 进程崩溃, 超出限制
- 调用示例：媒体处理.拼接媒体(输出格式=mp4, 超时秒=60, 最大输出字节=209715200, 返回字节=True)
- 依赖能力：媒体处理支持库.FFmpeg媒体.检查提供者, 媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.提取音频, 媒体处理支持库.FFmpeg媒体.转码, 媒体处理支持库.FFmpeg媒体.抽取帧
