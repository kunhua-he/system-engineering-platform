# 能力目录 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 包id：模块库.能力目录
- 版本：1.2.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 能力目录.搜索能力 | 搜索能力 | 1.2.0 | {'类型': '结果型', '值结构': {'关键词': '文本型', '限制': '整数型', '总数': '整数型', '返回数': '整数型', '是否截断': '逻辑型', '能力列表': '列表型', '项目根': '文本型', '游标': '文本型', '下一条游标': '文本型', '细节级别': '文本型', '是否扩词': '逻辑型', '含常用参数': '逻辑型'}} | 按关键词只读检索支持库与模块库公开能力：整条记录大小写不敏感子串匹配，命中按 能力id 稳定升序排列，支持 游标 分页与三档 细节级别。默认档＝「名称」：每条只回 能力id/名称/中文名称/包id/一句话说明 五个精简字段，把 参数/行为/返回/资源预算 这类重字段挡在默认返回之外，保住 Agent 的 prompt 前缀缓存（依据 P-9 第 6 条「精简字段默认返回」与第 2 条「返回指针而不是全文」）；需要重字段必须显式传 细节级别=完整契约，该档与「读取能力」逐字段一致。旧调用（只传 关键词/限制）照常可用，只是返回项由 21 字段变精简。游标不透明、基于排序键（能力id）而非偏移，翻页不漏不重。不加载实现；适配层包（同一实现的第二条腿）不暴露，内部层包进发现面并标 `内部层=true`。 零命中兜底：整串子串匹配 0 命中时，改用双字滑窗按命中词数打分重排（返回 `是否扩词=true` 标注本次走了兜底；命中时老行为逐字不变）。 |
| 能力目录.搜索能力 | 搜索能力 | 1.2.0 | {'类型': '结果型', '值结构': {'关键词': '文本型', '限制': '整数型', '总数': '整数型', '返回数': '整数型', '是否截断': '逻辑型', '能力列表': '列表型', '项目根': '文本型', '游标': '文本型', '下一条游标': '文本型', '细节级别': '文本型', '是否扩词': '逻辑型', '含常用参数': '逻辑型'}} | 按关键词只读检索支持库与模块库公开能力：整条记录大小写不敏感子串匹配，命中按 能力id 稳定升序排列，支持 游标 分页与三档 细节级别。默认档＝「名称」：每条只回 能力id/名称/中文名称/包id/一句话说明 五个精简字段，把 参数/行为/返回/资源预算 这类重字段挡在默认返回之外，保住 Agent 的 prompt 前缀缓存（依据 P-9 第 6 条「精简字段默认返回」与第 2 条「返回指针而不是全文」）；需要重字段必须显式传 细节级别=完整契约，该档与「读取能力」逐字段一致。旧调用（只传 关键词/限制）照常可用，只是返回项由 21 字段变精简。游标不透明、基于排序键（能力id）而非偏移，翻页不漏不重。不加载实现，不暴露适配层与内部层包。 零命中兜底：整串子串匹配 0 命中时，改用双字滑窗按命中词数打分重排（`是否扩词=true` 标注本次走了兜底；命中时老行为逐字不变）。 完整契约档同样带 前置能力/后继能力/使用指引 三键（口径与「读取能力」逐字一致）。 |
| 能力目录.读取能力 | 读取能力 | 1.0.0 | {'类型': '结果型', '值结构': {'能力id': '文本型', '中文名称': '文本型', '说明': '文本型', '包id': '文本型', '包名称': '文本型', '提供方': '文本型', '类型': '文本型', '版本': '文本型', '包版本': '文本型', '参数': '列表型', '参数名': '列表型', '必填': '列表型', '默认值': '字典型', '返回结构': 'JSON值型', '错误码': '列表型', '调用示例': '字典型', '调用示例文本': '文本型', '权限': '文本型', '资源预算': '文本型', '依赖': '列表型', '最近成功验证': '文本型', '验证状态': '文本型', '前置能力': '列表型', '后继能力': '列表型', '使用指引': '列表型'}} | 按能力id只读读取单个公开能力的完整契约：参数/必填/默认值/返回结构/错误码/调用示例/权限/资源预算/依赖/版本/最近成功验证。 因果指引（2026-09-20 补）：记录含 前置能力/后继能力/使用指引 三键，让「搜到即会用、不再反复探索」——前置能力＝本能力的句柄参数由谁产生（如 `大语言模型支持库.会话存储.读取历史` 的句柄由 `连接会话存储` 返回），后继能力＝用完该句柄该调谁释放（如 `创建信号量` 的后继为 `释放信号量`），使用指引＝把两者写成可照做的句子。三者全部**从既有参数说明派生**（认 `由 X 返回` 句式），不新增契约字段、契约不因此变版。无句柄参数的能力三键为空列表。 |
| 能力目录.常用参数组合 | 常用参数组合 | 1.0.0 | {'类型': '结果型', '值结构': {'能力id': '文本型', '上限': '整数型', '命中能力数': '整数型', '组合数': '整数型', '常用参数组合': '列表型', '问题': '文本型', '说明': '文本型'}} | 按能力 id 返回该能力历史上最常用的几组入参（含真实样例，默认 5 组），供调用方照抄、免试错。数据源是调用参数账（SQLite，主键 (能力id, 入参形状)，网关每调一次自增 1，读时按调用次数倒序取前 N）—— 口径是「全时段精确计数」，不是「最近 N 条窗口」的近似值。本能力上线前的历史调用没有入参记录，一律不补；一个组合都没有时 常用参数组合 为空列表（如实结论不是错误），此时以 读取能力 的 调用示例 为准；账本读不出来时同样回空列表但 问题 非空。能力id 为空则汇总全部能力（每段各取前 上限 组）。 |

## 能力目录.搜索能力（搜索能力）

**说明**：按关键词只读检索支持库与模块库公开能力：整条记录大小写不敏感子串匹配，命中按 能力id 稳定升序排列，支持 游标 分页与三档 细节级别。默认档＝「名称」：每条只回 能力id/名称/中文名称/包id/一句话说明 五个精简字段，把 参数/行为/返回/资源预算 这类重字段挡在默认返回之外，保住 Agent 的 prompt 前缀缓存（依据 P-9 第 6 条「精简字段默认返回」与第 2 条「返回指针而不是全文」）；需要重字段必须显式传 细节级别=完整契约，该档与「读取能力」逐字段一致。旧调用（只传 关键词/限制）照常可用，只是返回项由 21 字段变精简。游标不透明、基于排序键（能力id）而非偏移，翻页不漏不重。不加载实现；适配层包（同一实现的第二条腿）不暴露，内部层包进发现面并标 `内部层=true`。 零命中兜底：整串子串匹配 0 命中时，改用双字滑窗按命中词数打分重排（返回 `是否扩词=true` 标注本次走了兜底；命中时老行为逐字不变）。

**说明**：按关键词只读检索支持库与模块库公开能力：整条记录大小写不敏感子串匹配，命中按 能力id 稳定升序排列，支持 游标 分页与三档 细节级别。默认档＝「名称」：每条只回 能力id/名称/中文名称/包id/一句话说明 五个精简字段，把 参数/行为/返回/资源预算 这类重字段挡在默认返回之外，保住 Agent 的 prompt 前缀缓存（依据 P-9 第 6 条「精简字段默认返回」与第 2 条「返回指针而不是全文」）；需要重字段必须显式传 细节级别=完整契约，该档与「读取能力」逐字段一致。旧调用（只传 关键词/限制）照常可用，只是返回项由 21 字段变精简。游标不透明、基于排序键（能力id）而非偏移，翻页不漏不重。不加载实现，不暴露适配层与内部层包。 零命中兜底：整串子串匹配 0 命中时，改用双字滑窗按命中词数打分重排（`是否扩词=true` 标注本次走了兜底；命中时老行为逐字不变）。 完整契约档同样带 前置能力/后继能力/使用指引 三键（口径与「读取能力」逐字一致）。
**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 关键词 | 文本型 | 否 |  | 搜索关键词；空字符串表示不过滤（按 限制 返回候选） |
| 限制 | 整数型 | 否 | 5 | 每页最多返回条数；1~100，超出按边界收敛，非整数一律拒绝（语义为「每页大小」，不再是「总量硬上限」——总量由 游标 翻页取全） |
| 游标 | 文本型 | 否 |  | 翻页游标：空 = 从第一页取；此后原样传回上一次返回的 下一条游标。不透明，调用方不得解析或自行构造；同一轮翻页必须与首次调用传相同的 关键词（不同即拒绝，避免漏项/重项） |
| 细节级别 | 文本型 | 否 | 名称 | 返回明细档位，三档：名称（默认，仅 能力id/名称/中文名称/包id/一句话说明）／名称+说明（追加 说明/包名称/提供方/类型/版本/包版本）／完整契约（全部字段，与 读取能力 一致）。不传取默认档「名称」；传其它值一律拒绝 |
| 含常用参数 | 逻辑型 | 否 | false | 为 真 时每条命中多带一个 常用参数：该能力历史上最常怎么传参的前 5 组（含真实样例）。默认 假 —— 每条挂 5 组会把返回体量放大数倍，正是「调用几次打爆上下文」的成因；要细则就该显式要（与 细节级别 同一取舍）。数据源见 能力目录.常用参数组合 |

**返回结构**：{'类型': '结果型', '值结构': {'关键词': '文本型', '限制': '整数型', '总数': '整数型', '返回数': '整数型', '是否截断': '逻辑型', '能力列表': '列表型', '项目根': '文本型', '游标': '文本型', '下一条游标': '文本型', '细节级别': '文本型', '是否扩词': '逻辑型', '含常用参数': '逻辑型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 内部错误 |

**调用示例**：

```python
能力目录.搜索能力(关键词=值, 限制=值, 游标=值, 细节级别=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：能力目录.检索公开能力正向HTTP（正向）

## 能力目录.常用参数组合（常用参数组合）

**说明**：按能力 id 返回该能力历史上最常用的几组入参（含真实样例，默认 5 组），供调用方照抄、免试错。数据源是**调用参数账**（SQLite，主键 `(能力id, 入参形状)`，网关每调一次自增 1，读时按调用次数倒序取前 N）—— 口径是「全时段精确计数」，不是「最近 N 条窗口」的近似值。本能力上线前的历史调用没有入参记录，一律不补（不编造样例）；一个组合都没有（能力从没被调用过）时 `常用参数组合` 为空列表，这是如实结论不是错误，此时应以 `读取能力` 的 `调用示例` 为准；账本读不出来时同样回空列表但 `问题` 字段非空，调用方据此区分两种情况。`能力id` 为空则汇总全部能力（每段各取前 `上限` 组）。

**为什么要有它**：契约只声明参数**类型**，不声明**一组能跑通的取值**；调用方（尤其 Agent）只能靠试错摸格式 —— 实测踩过「`超时秒` 传 120 被拒、传 120.0 才过」连错 3 次，以及「`项目根` 要在 `参数` 里再给一遍」连错 4 次。这两类错误的共同点不是类型错，而是**不知道别人实际怎么传**。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 能力id | 文本型 | 否 |  | 要查的能力 id，例如 文件系统支持库.文本补丁.应用精确替换；空字符串表示汇总全部能力（每段各取前 上限 组） |
| 上限 | 整数型 | 否 | 5 | 最多返回几组；1~100，超出按边界收敛，非整数一律拒绝。默认 5 组 |

**返回结构**：{'类型': '结果型', '值结构': {'能力id': '文本型', '上限': '整数型', '命中能力数': '整数型', '组合数': '整数型', '常用参数组合': '列表型', '问题': '文本型', '说明': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 内部错误 |

**调用示例**：

```python
能力目录.常用参数组合(能力id=值, 上限=值)
```

**验证状态**：已纳入验证场景：能力目录.检索公开能力正向HTTP（正向）

## 能力目录.读取能力（读取能力）

**说明**：按能力id只读读取单个公开能力的完整契约：参数/必填/默认值/返回结构/错误码/调用示例/权限/资源预算/依赖/版本/最近成功验证。 因果指引（2026-09-20 补）：记录含 前置能力/后继能力/使用指引 三键，让「搜到即会用、不再反复探索」——前置能力＝本能力的句柄参数由谁产生（如 `大语言模型支持库.会话存储.读取历史` 的句柄由 `连接会话存储` 返回），后继能力＝用完该句柄该调谁释放（如 `创建信号量` 的后继为 `释放信号量`），使用指引＝把两者写成可照做的句子。三者全部**从既有参数说明派生**（认 `由 X 返回` 句式），不新增契约字段、契约不因此变版。无句柄参数的能力三键为空列表。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 能力id | 文本型 | 是 | None | 能力id，例如 能力目录.搜索能力 |

**返回结构**：{'类型': '结果型', '值结构': {'能力id': '文本型', '中文名称': '文本型', '说明': '文本型', '包id': '文本型', '包名称': '文本型', '提供方': '文本型', '类型': '文本型', '版本': '文本型', '包版本': '文本型', '参数': '列表型', '参数名': '列表型', '必填': '列表型', '默认值': '字典型', '返回结构': 'JSON值型', '错误码': '列表型', '调用示例': '字典型', '调用示例文本': '文本型', '权限': '文本型', '资源预算': '文本型', '依赖': '列表型', '最近成功验证': '文本型', '验证状态': '文本型', '前置能力': '列表型', '后继能力': '列表型', '使用指引': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 能力不存在 |
| 文件不存在 |
| 内部错误 |

**调用示例**：

```python
能力目录.读取能力(能力id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：能力目录.检索公开能力正向HTTP（正向）
