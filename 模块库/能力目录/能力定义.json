{
 "包id": "模块库.能力目录",
 "版本": "1.2.0",
 "说明": "能力目录模块：把「平台有哪些能力」收敛成 搜索能力（关键词→能力记录列表）、读取能力（能力id→完整契约）与 常用参数组合（能力id→该能力历史上最常用的几组入参）三个只读能力，数据只来自包声明/参数契约/能力定义/权限契约/资源预算/验证历史，不加载任何能力实现，供薄壳「查询能力」转发。",
 "能力列表": [
  {
   "能力id": "能力目录.搜索能力",
   "版本": "1.2.0",
   "中文名称": "搜索能力",
   "说明": "按关键词只读检索支持库与模块库公开能力：整条记录大小写不敏感子串匹配，命中按 能力id 稳定升序排列，支持 游标 分页与三档 细节级别。默认档＝「名称」：每条只回 能力id/名称/中文名称/包id/一句话说明 五个精简字段，把 参数/行为/返回/资源预算 这类重字段挡在默认返回之外，保住 Agent 的 prompt 前缀缓存（依据 P-9 第 6 条「精简字段默认返回」与第 2 条「返回指针而不是全文」）；需要重字段必须显式传 细节级别=完整契约，该档与「读取能力」逐字段一致。旧调用（只传 关键词/限制）照常可用，只是返回项由 21 字段变精简。游标不透明、基于排序键（能力id）而非偏移，翻页不漏不重。不加载实现，不暴露适配层与内部层包。 零命中兜底：整串子串匹配 0 命中时，改用双字滑窗按命中词数打分重排（`是否扩词=true` 标注本次走了兜底；命中时老行为逐字不变）。 完整契约档同样带 前置能力/后继能力/使用指引 三键（口径与「读取能力」逐字一致）。 常用参数（2026-09-21 补）：`含常用参数=真` 时每条命中追加 `常用参数` 键 —— 该能力历史上最常怎么传参的前 5 组（含真实样例），免掉「契约只声明类型、不声明一组能跑通的取值」导致的试错；默认关（返回体量优先），单查用 能力目录.常用参数组合。",
   "参数": [
    {
     "名称": "关键词",
     "类型": "文本型",
     "必填": false,
     "默认值": "",
     "说明": "搜索关键词；空字符串表示不过滤（按 限制 返回候选）"
    },
    {
     "名称": "限制",
     "类型": "整数型",
     "必填": false,
     "默认值": 5,
     "说明": "每页最多返回条数；1~100，超出按边界收敛，非整数一律拒绝（语义为「每页大小」，不再是「总量硬上限」——总量由 游标 翻页取全）"
    },
    {
     "名称": "游标",
     "类型": "文本型",
     "必填": false,
     "默认值": "",
     "说明": "翻页游标：空 = 从第一页取；此后原样传回上一次返回的 下一条游标。不透明，调用方不得解析或自行构造；同一轮翻页必须与首次调用传相同的 关键词（不同即拒绝，避免漏项/重项）"
    },
    {
     "名称": "细节级别",
     "类型": "文本型",
     "必填": false,
     "默认值": "名称",
     "说明": "返回明细档位，三档：名称（默认，仅 能力id/名称/中文名称/包id/一句话说明）／名称+说明（追加 说明/包名称/提供方/类型/版本/包版本）／完整契约（全部字段，与 读取能力 一致）。不传取默认档「名称」；传其它值一律拒绝。**例外（2026-09-21，只增）**：当 关键词 恰好等于某个能力 id 且全表只此一条时，自动升到「完整契约」直接给全签名（回带 `精确命中升档=true` 标明本次升了档）—— 治「知道确切 id 还要再跳一次读契约」；多命中（真检索）时逐字不变"
    },
    {
     "名称": "含常用参数",
     "类型": "逻辑型",
     "必填": false,
     "默认值": false,
     "说明": "为 真 时每条命中多带一个 常用参数：该能力历史上最常怎么传参的前 5 组（含真实样例）。默认 假 —— 每条挂 5 组会把返回体量放大数倍，正是「调用几次打爆上下文」的成因；要细则就该显式要（与 细节级别 同一取舍）。数据源见 能力目录.常用参数组合。"
    }
   ],
   "返回": {
    "类型": "结果型",
    "值结构": {
     "关键词": "文本型，本次检索关键词原值（空=不过滤）",
     "限制": "整数型，每页大小（1~100，越界已收敛）",
     "总数": "整数型，命中总数（全量，非本页条数）",
     "返回数": "整数型，本页返回条数",
     "是否截断": "逻辑型，是否还有下一页",
     "能力列表": "列表型，本页能力记录（字段按 细节级别 投影）",
     "项目根": "文本型，检索所用项目根绝对路径",
     "游标": "文本型，本次入参游标原值（空=第一页）",
     "下一条游标": "文本型，下一页游标（空=没有更多）",
     "细节级别": "文本型，本次实际生效档位（名称 / 名称+说明 / 完整契约）",
     "是否扩词": "逻辑型，是否走了双字滑窗零命中兜底（兜底命中为真）",
     "含常用参数": "逻辑型，本次是否附带常用参数（每条命中多带 常用参数 键）"
    }
   },
   "错误码": [
    "参数不合法",
    "文件不存在",
    "内部错误"
   ],
   "行为": {
    "修改输入": false,
    "幂等": true,
    "副作用": "只读",
    "排序稳定": true,
    "时区": "不涉及",
    "编码": "utf-8",
    "精度": "不涉及",
    "空值": "关键词空字符串表示不过滤；限制空值取默认 5；游标空字符串表示从第一页取；细节级别空值或未传取默认档「名称」",
    "输入上限": "限制＝每页大小，最多 100 条、超出按边界收敛；单页总量上限由 限制 控制，总量不受上限约束（游标翻页可取全）",
    "超时可重试": true,
    "取消": "不支持",
    "重试条件": "只读无副作用，失败可原样重试",
    "事务边界": "无",
    "补偿动作": "无",
    "线程安全": true,
    "进程安全": true,
    "资源释放": "无句柄，不留常驻资源",
    "错误码": "统一",
    "可重试性": "参数错误不可重试；数据根缺失/索引为空修复后可重试"
   },
   "提供者": {
    "默认": "模块库.能力目录",
    "版本": ">=1.0.0"
   }
  },
  {
   "能力id": "能力目录.读取能力",
   "版本": "1.0.0",
   "中文名称": "读取能力",
   "说明": "按能力id只读读取单个公开能力的完整契约：参数/必填/默认值/返回结构/错误码/调用示例/权限/资源预算/依赖/版本/最近成功验证。 因果指引（2026-09-20 补）：记录含 前置能力/后继能力/使用指引 三键，让「搜到即会用、不再反复探索」——前置能力＝本能力的句柄参数由谁产生（如 `大语言模型支持库.会话存储.读取历史` 的句柄由 `连接会话存储` 返回），后继能力＝用完该句柄该调谁释放（如 `创建信号量` 的后继为 `释放信号量`），使用指引＝把两者写成可照做的句子。三者全部**从既有参数说明派生**（认 `由 X 返回` 句式），不新增契约字段、契约不因此变版。无句柄参数的能力三键为空列表。",
   "参数": [
    {
     "名称": "能力id",
     "类型": "文本型",
     "必填": true,
     "默认值": null,
     "说明": "能力id，例如 能力目录.搜索能力"
    }
   ],
   "返回": {
    "类型": "结果型",
    "值结构": {
     "能力id": "文本型，能力唯一标识（如 模块库.图像处理.分析图像文件）",
     "中文名称": "文本型，能力中文名",
     "说明": "文本型，能力说明",
     "包id": "文本型，所属包标识",
     "包名称": "文本型，所属包中文名",
     "提供方": "文本型，提供方（取所属包id）",
     "类型": "文本型，所属包类型（支持库/模块库）",
     "版本": "文本型，能力版本",
     "包版本": "文本型，所属包版本",
     "参数": "列表型，参数清单（每项含 名称/类型/必填/默认值/说明）",
     "参数名": "列表型，参数名列表（顺序与 参数 一致）",
     "必填": "列表型，必填参数名列表",
     "默认值": "字典型，参数默认值（参数名→默认值，未声明默认值的不出现）",
     "返回结构": "JSON值型，返回结构声明（该能力的 返回.类型 与 值结构）",
     "错误码": "列表型，错误码清单",
     "调用示例": "字典型，调用示例（能力id→参数示例）",
     "调用示例文本": "文本型，调用示例的可读文本",
     "权限": "文本型，权限声明",
     "资源预算": "文本型，资源预算声明",
     "依赖": "列表型，依赖的能力/支持库清单",
     "最近成功验证": "文本型，最近一次成功验证记录",
     "验证状态": "文本型，验证状态",
     "前置能力": "列表型，前置能力清单（按参数句柄引用推导）",
     "后继能力": "列表型，后继能力清单",
     "使用指引": "列表型，使用指引条目（前置/后继/句柄参数渲染）"
    }
   },
   "错误码": [
    "参数不合法",
    "能力不存在",
    "文件不存在",
    "内部错误"
   ],
   "行为": {
    "修改输入": false,
    "幂等": true,
    "副作用": "只读",
    "排序稳定": true,
    "时区": "不涉及",
    "编码": "utf-8",
    "精度": "不涉及",
    "空值": "能力id 必填，空文本或非文本一律参数不合法",
    "输入上限": "单次一个能力id，无批量入参",
    "超时可重试": true,
    "取消": "不支持",
    "重试条件": "只读无副作用，失败可原样重试",
    "事务边界": "无",
    "补偿动作": "无",
    "线程安全": true,
    "进程安全": true,
    "资源释放": "无句柄，不留常驻资源",
    "错误码": "统一",
    "可重试性": "参数错误不可重试；能力不存在需先搜索定位"
   },
   "提供者": {
    "默认": "模块库.能力目录",
    "版本": ">=1.0.0"
   }
  },
  {
   "能力id": "能力目录.常用参数组合",
   "版本": "1.0.0",
   "中文名称": "常用参数组合",
   "说明": "按能力 id 返回该能力历史上最常用的几组入参（含真实样例，默认 5 组），供调用方照抄、免试错。数据源是调用参数账（SQLite，主键 (能力id, 入参形状)，网关每调一次自增 1，读时按调用次数倒序取前 N）——口径是「全时段精确计数」，不是「最近 N 条窗口」的近似值。本能力上线前的历史调用没有入参记录，一律不补（不编造样例）；一个组合都没有（能力从没被调用过）时 常用参数组合 为空列表，这是如实结论不是错误，此时应以 读取能力 的 调用示例 为准；账本读不出来时同样回空列表但 问题 字段非空，调用方据此区分两种情况。`能力id` 为空则汇总全部能力（每段各取前 上限 组）。",
   "参数": [
    {
     "名称": "能力id",
     "类型": "文本型",
     "必填": false,
     "默认值": "",
     "说明": "要查的能力 id，例如 文件系统支持库.文本补丁.应用精确替换；空字符串表示汇总全部能力（每段各取前 上限 组）"
    },
    {
     "名称": "上限",
     "类型": "整数型",
     "必填": false,
     "默认值": 5,
     "说明": "最多返回几组；1~100，超出按边界收敛，非整数一律拒绝。默认 5 组"
    }
   ],
   "返回": {
    "类型": "结果型",
    "值结构": {
     "能力id": "文本型，查询的能力 id（空=汇总全部能力）",
     "上限": "整数型，每个能力取前 N 组（越界已收敛）",
     "命中能力数": "整数型，调用参数账中命中该能力 id 的记录数",
     "组合数": "整数型，本次返回的组合组数",
     "常用参数组合": "列表型，组合清单（每项含 入参形状/次数/最近一次真实样例）",
     "问题": "文本型，账本读取问题（空=读取正常；非空时 常用参数组合 为空属如实结论）",
     "说明": "文本型，数据来源与使用口径（来自调用参数账、按次数倒序；样例只作展示不作校验）"
    }
   },
   "错误码": [
    "参数不合法",
    "内部错误"
   ],
   "行为": {
    "修改输入": false,
    "幂等": true,
    "副作用": "只读",
    "排序稳定": true,
    "时区": "不涉及",
    "编码": "utf-8",
    "精度": "不涉及",
    "空值": "能力id 空字符串表示汇总全部能力；上限 空值取默认 5",
    "输入上限": "上限最多 100 组、超出按边界收敛；单次只查一个能力或全部",
    "超时可重试": true,
    "取消": "不支持",
    "重试条件": "只读无副作用，失败可原样重试",
    "事务边界": "无",
    "补偿动作": "无",
    "线程安全": true,
    "进程安全": true,
    "资源释放": "无句柄，不留常驻资源",
    "错误码": "统一",
    "可重试性": "参数错误不可重试；账本不可读修复后可重试"
   },
   "提供者": {
    "默认": "模块库.能力目录",
    "版本": ">=1.0.0"
   }
  }
 ]
}
