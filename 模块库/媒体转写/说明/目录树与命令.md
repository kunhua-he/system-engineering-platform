# 媒体转写 · 目录树与命令

> 包id：`模块库.媒体转写`；版本：`1.0.3`；类型：`功能模块`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。
<!-- 机器管理｜类型：包级说明-目录树与命令｜生成器：开发工具.MD文档生成 -->


## 目录树

```text
媒体转写/
├── __init__.py
├── 依赖契约/依赖契约.json
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/媒体转写.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/Agent查询数据.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
```

## 公开命令

### `媒体转写.转写视频文件`

- 名称：转写视频文件
- 返回：`结果型`
- 说明：视频转写：视频文件直接经 转写.转写音频文件 转写；未配置模型如实返回 未配置模型
- 参数：
  - `文件路径`；文本型
  - `转写超时秒`；双精度数型
  - `取消令牌id`；文本型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `媒体转写.转写音频文件`

- 名称：转写音频文件
- 返回：`结果型`
- 说明：转写音频文件为文本（经 MLX Whisper 受管子进程）
- 参数：
  - `文件路径`；文本型
  - `超时秒`；双精度数型
  - `取消令牌id`；文本型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `媒体转写.检查可用性`

- 名称：检查可用性
- 返回：`结果型`
- 说明：检查转写可用性；模型未配置如实返回 未配置模型
- 参数：
  - `超时秒`；双精度数型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `媒体转写.获取模型版本`

- 名称：获取模型版本
- 返回：`结果型`
- 说明：获取 MLX Whisper 模型与库版本
- 参数：
  - `超时秒`；双精度数型
  - `配置`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
