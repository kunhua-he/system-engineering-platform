# 媒体转写（模块库.媒体转写）

- 类型：功能模块
- 版本：1.0.3
- 说明：组合 MLX Whisper 支持库公开能力（功能模块）：检查可用性、获取模型版本、转写音频文件、转写视频文件（视频直接经 转写支持库.转写.转写音频文件 转写，mlx_whisper 内部解码音轨）；未配置模型如实返回，不伪造转写成功；临时媒体/音频由支持库层管理。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `转写支持库.转写.检查可用性` 版本约束 `>=1.0.0`
- 能力 `转写支持库.转写.转写音频文件` 版本约束 `>=1.0.0`
- 能力 `转写支持库.转写.获取模型版本` 版本约束 `>=1.0.0`

## 能力
### 转写视频文件（`媒体转写.转写视频文件`）
- 版本：1.0.0
- 一句话说明：视频转写：视频文件直接经 转写支持库.转写.转写音频文件 转写；未配置模型如实返回 未配置模型
- 所属支持库或模块：模块库.媒体转写
- 参数列表：文件路径:文本型, 转写超时秒:双精度数型, 取消令牌id:文本型, 配置:字典型
- 必填参数：文件路径
- 可选参数：转写超时秒, 取消令牌id, 配置
- 返回结构：{'类型': '结果型', '值结构': {'文本': '文本型', '语言': '文本型', '模型名': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 未配置模型, 模型缺失, 转写失败, 超时, 取消, 进程崩溃, 超出限制, 提供者不可用
- 调用示例：媒体转写.转写视频文件(转写超时秒=300, 取消令牌id=)
- 依赖能力：转写支持库.转写.检查可用性, 转写支持库.转写.转写音频文件, 转写支持库.转写.获取模型版本
### 转写音频文件（`媒体转写.转写音频文件`）
- 版本：1.0.0
- 一句话说明：转写音频文件为文本（经 MLX Whisper 受管子进程）
- 所属支持库或模块：模块库.媒体转写
- 参数列表：文件路径:文本型, 超时秒:双精度数型, 取消令牌id:文本型, 配置:字典型
- 必填参数：文件路径
- 可选参数：超时秒, 取消令牌id, 配置
- 返回结构：{'类型': '结果型', '值结构': {'文本': '文本型', '语言': '文本型', '模型名': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 未配置模型, 模型缺失, 转写失败, 超时, 取消, 进程崩溃, 超出限制, 提供者不可用
- 调用示例：媒体转写.转写音频文件(超时秒=300, 取消令牌id=)
- 依赖能力：转写支持库.转写.检查可用性, 转写支持库.转写.转写音频文件, 转写支持库.转写.获取模型版本
### 检查可用性（`媒体转写.检查可用性`）
- 版本：1.0.0
- 一句话说明：检查转写可用性；模型未配置如实返回 未配置模型
- 所属支持库或模块：模块库.媒体转写
- 参数列表：超时秒:双精度数型, 配置:字典型
- 必填参数：无
- 可选参数：超时秒, 配置
- 返回结构：{'类型': '结果型', '值结构': {'可用': '逻辑型', '模型名': '文本型', '模型版本': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 未配置模型, 模型缺失, 提供者不可用, 超时, 进程崩溃
- 调用示例：媒体转写.检查可用性(超时秒=300)
- 依赖能力：转写支持库.转写.检查可用性, 转写支持库.转写.转写音频文件, 转写支持库.转写.获取模型版本
### 获取模型版本（`媒体转写.获取模型版本`）
- 版本：1.0.0
- 一句话说明：获取 MLX Whisper 模型与库版本
- 所属支持库或模块：模块库.媒体转写
- 参数列表：超时秒:双精度数型, 配置:字典型
- 必填参数：无
- 可选参数：超时秒, 配置
- 返回结构：{'类型': '结果型', '值结构': {'模型名': '文本型', '模型版本': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 未配置模型, 模型缺失, 提供者不可用, 超时, 进程崩溃
- 调用示例：媒体转写.获取模型版本(超时秒=300)
- 依赖能力：转写支持库.转写.检查可用性, 转写支持库.转写.转写音频文件, 转写支持库.转写.获取模型版本
