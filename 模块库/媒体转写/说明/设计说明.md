# 媒体转写 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：模块库.媒体转写
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 媒体转写.转写视频文件 | 转写视频文件 | 1.0.0 | {'类型': '结果型', '值结构': {'文本': '文本型', '语言': '文本型', '模型名': '文本型'}} | 视频转写：视频文件直接经 转写支持库.转写.转写音频文件 转写；未配置模型如实返回 未配置模型 |
| 媒体转写.转写音频文件 | 转写音频文件 | 1.0.0 | {'类型': '结果型', '值结构': {'文本': '文本型', '语言': '文本型', '模型名': '文本型'}} | 转写音频文件为文本（经 MLX Whisper 受管子进程） |
| 媒体转写.检查可用性 | 检查可用性 | 1.0.0 | {'类型': '结果型', '值结构': {'可用': '逻辑型', '模型名': '文本型', '模型版本': '文本型'}} | 检查转写可用性；模型未配置如实返回 未配置模型 |
| 媒体转写.获取模型版本 | 获取模型版本 | 1.0.0 | {'类型': '结果型', '值结构': {'模型名': '文本型', '模型版本': '文本型'}} | 获取 MLX Whisper 模型与库版本 |

## 媒体转写.转写视频文件（转写视频文件）

**说明**：视频转写：视频文件直接经 转写支持库.转写.转写音频文件 转写；未配置模型如实返回 未配置模型

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 视频文件绝对路径 |
| 转写超时秒 | 双精度数型 | 否 | 300 | 转写子进程超时 |
| 取消令牌id | 文本型 | 否 |  | 取消令牌标识（文本；空串表示不取消） |
| 配置 | 字典型 | 否 | None | 模型配置（模型路径/模型名）；缺省读环境变量 |

**返回结构**：{'类型': '结果型', '值结构': {'文本': '文本型', '语言': '文本型', '模型名': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 未配置模型 |
| 模型缺失 |
| 转写失败 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
| 提供者不可用 |

**调用示例**：

```python
媒体转写.转写视频文件(文件路径=值, 转写超时秒=值, 取消令牌id=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体转写.转写音频文件（转写音频文件）

**说明**：转写音频文件为文本（经 MLX Whisper 受管子进程）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 音频文件绝对路径 |
| 超时秒 | 双精度数型 | 否 | 300 | 转写子进程超时 |
| 取消令牌id | 文本型 | 否 |  | 取消令牌标识（文本；空串表示不取消） |
| 配置 | 字典型 | 否 | None | 模型配置（模型路径/模型名）；缺省读环境变量 |

**返回结构**：{'类型': '结果型', '值结构': {'文本': '文本型', '语言': '文本型', '模型名': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 未配置模型 |
| 模型缺失 |
| 转写失败 |
| 超时 |
| 取消 |
| 进程崩溃 |
| 超出限制 |
| 提供者不可用 |

**调用示例**：

```python
媒体转写.转写音频文件(文件路径=值, 超时秒=值, 取消令牌id=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体转写.检查可用性（检查可用性）

**说明**：检查转写可用性；模型未配置如实返回 未配置模型

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 300 | 子进程超时 |
| 配置 | 字典型 | 否 | None | 模型配置（模型路径/模型名）；缺省读环境变量 |

**返回结构**：{'类型': '结果型', '值结构': {'可用': '逻辑型', '模型名': '文本型', '模型版本': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 未配置模型 |
| 模型缺失 |
| 提供者不可用 |
| 超时 |
| 进程崩溃 |

**调用示例**：

```python
媒体转写.检查可用性(超时秒=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 媒体转写.获取模型版本（获取模型版本）

**说明**：获取 MLX Whisper 模型与库版本

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 超时秒 | 双精度数型 | 否 | 300 | 子进程超时 |
| 配置 | 字典型 | 否 | None | 模型配置（模型路径/模型名）；缺省读环境变量 |

**返回结构**：{'类型': '结果型', '值结构': {'模型名': '文本型', '模型版本': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 未配置模型 |
| 模型缺失 |
| 提供者不可用 |
| 超时 |
| 进程崩溃 |

**调用示例**：

```python
媒体转写.获取模型版本(超时秒=值, 配置=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
