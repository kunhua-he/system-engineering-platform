# 直播逐字稿（模块库.直播逐字稿）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：功能模块
- 版本：2.0.0
- 说明：直播逐字稿模块（功能模块）：组合 媒体处理支持库(ffmpeg探测)/转写支持库(MLX Whisper) 公开能力，传视频或音频绝对路径即产出带时间戳的原始逐字稿文件；未配置模型如实返回 未配置模型；调用方统一走 获取能力调用器().调用能力。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `媒体处理支持库.FFmpeg媒体.探测媒体` 版本约束 `>=1.0.0`
- 能力 `媒体处理支持库.FFmpeg媒体.检查提供者` 版本约束 `>=1.0.0`
- 能力 `转写支持库.转写.转写音频文件` 版本约束 `>=1.0.0`
- 能力 `转写支持库.转写.检查可用性` 版本约束 `>=1.0.0`
- 能力 `大语言模型支持库.模型连接器.连接LLM` 版本约束 `>=1.0.0`
- 能力 `大语言模型支持库.模型连接器.生成对话` 版本约束 `>=1.0.0`
- 能力 `大语言模型支持库.模型连接器.释放句柄` 版本约束 `>=1.0.0`
- 能力 `媒体处理支持库.FFmpeg媒体.截取音频` 版本约束 `>=1.0.0`

## 能力
### 转写媒体文件（`直播逐字稿.转写媒体文件`）
- 版本：1.0.0
- 一句话说明：转写直播媒体文件为原始逐字稿：探测媒体→按类型走 媒体转写 公开能力→产出 02_转录底稿/03_原始逐字稿.txt；未配置模型如实返回 未配置模型，绝不伪造转写成功
- 所属支持库或模块：模块库.直播逐字稿
- 参数列表：文件路径:文本型, 输出目录:文本型, 分片秒数:整数型, 重叠秒数:整数型, 模型配置:字典型, 附加术语:文本型, 超时秒:双精度数型, 开工ID:文本型
- 必填参数：文件路径, 输出目录, 开工ID
- 可选参数：分片秒数, 重叠秒数, 模型配置, 附加术语, 超时秒
- 返回结构：{'类型': '结果型', '值结构': {'输出目录': '文本型', '状态': '文本型', '场次名': '文本型', '原始逐字稿路径': '文本型', '疑难清单路径': '文本型', '已完成分片': '整型', '分片总数': '整型', '语言': '文本型', '时长秒': '双精度数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 未配置模型, 模型缺失, 媒体探测失败, 转写失败, 写入失败, 超时, 取消, 进程崩溃, 超出限制, 提供者不可用, 越界
- 调用示例：直播逐字稿.转写媒体文件(分片秒数=300, 重叠秒数=15, 附加术语=, 超时秒=300.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.检查提供者, 转写支持库.转写.转写音频文件, 转写支持库.转写.检查可用性, 大语言模型支持库.模型连接器.连接LLM, 大语言模型支持库.模型连接器.生成对话, 大语言模型支持库.模型连接器.释放句柄, 媒体处理支持库.FFmpeg媒体.截取音频
### 检查可用性（`直播逐字稿.检查可用性`）
- 版本：1.0.0
- 一句话说明：检查直播逐字稿链路可用性：媒体处理(ffmpeg)与转写模型是否就绪；返回 {可用, ffmpeg可用, 转写可用, 转写详情}
- 所属支持库或模块：模块库.直播逐字稿
- 参数列表：超时秒:双精度数型, 配置:字典型
- 必填参数：无
- 可选参数：超时秒, 配置
- 返回结构：{'类型': '结果型', '值结构': {'可用': '逻辑型', 'ffmpeg可用': '逻辑型', '转写可用': '逻辑型', '转写详情': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 超时
- 调用示例：直播逐字稿.检查可用性(超时秒=60.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.检查提供者, 转写支持库.转写.转写音频文件, 转写支持库.转写.检查可用性, 大语言模型支持库.模型连接器.连接LLM, 大语言模型支持库.模型连接器.生成对话, 大语言模型支持库.模型连接器.释放句柄, 媒体处理支持库.FFmpeg媒体.截取音频
### 读取项目状态（`直播逐字稿.读取项目状态`）
- 版本：1.0.0
- 一句话说明：读取输出目录下 00_元数据/项目状态.json（断点续跑/进度查询）
- 所属支持库或模块：模块库.直播逐字稿
- 参数列表：输出目录:文本型
- 必填参数：输出目录
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'状态': '文本型', '阶段': '文本型', '源文件': '文本型', '时长秒': '双精度数型', '文本长度': '整型', '语言': '文本型', '分片秒数': '整型', '重叠秒数': '整型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在, 读取失败
- 调用示例：直播逐字稿.读取项目状态()
- 依赖能力：媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.检查提供者, 转写支持库.转写.转写音频文件, 转写支持库.转写.检查可用性, 大语言模型支持库.模型连接器.连接LLM, 大语言模型支持库.模型连接器.生成对话, 大语言模型支持库.模型连接器.释放句柄, 媒体处理支持库.FFmpeg媒体.截取音频
### 全自动精校（`直播逐字稿.全自动精校`）
- 版本：1.3.0
- 一句话说明：全自动精校（唯一对外能力）：传 源文件绝对路径 + 导出路径 + 缓存目录 + 模式号，一次调用完成 探测→转写→疑难标记→复核区间→逐窗裁决→按模式排版→质检门禁→原子导出；用户只看到导出的一份逐字稿。行业细节（分节结构、易错词校正）由消费者通过 输出结构 / 附加要求 / 附加术语 传参，底座不写死任何行业内容。
- 所属支持库或模块：模块库.直播逐字稿
- 参数列表：源文件路径:文本型, 导出路径:文本型, 缓存目录:文本型, 模式:整数型, 分片秒数:整数型, 附加术语:文本型, 模型配置:字典型, 裁决模型配置:字典型, 超时秒:双精度数型, 输出结构:文本型, 附加要求:文本型, 裁决模型句柄:整数型, 开工ID:文本型
- 必填参数：源文件路径, 导出路径, 缓存目录, 模式, 开工ID
- 可选参数：分片秒数, 附加术语, 模型配置, 裁决模型配置, 超时秒, 输出结构, 附加要求, 裁决模型句柄
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 不支持的媒体格式, 无音轨, 媒体探测失败, 磁盘空间不足, 未配置模型, 模型缺失, 转写失败, 复核失败, 裁决失败, 质检不通过, 写入失败, 未知模式, 提供者不可用, 超时, 取消, 进程崩溃, 超出限制, 装配缺失, 越界
- 调用示例：直播逐字稿.全自动精校(源文件路径=/abs/录制/2026-09-10_直播.mp4, 导出路径=/abs/出稿/2026-09-10_直播_精校稿.md, 缓存目录=/abs/缓存/2026-09-10_直播, 模式=1, 分片秒数=300, 附加术语=示例品牌、示例门店, 超时秒=3600.0)
- 依赖能力：媒体处理支持库.FFmpeg媒体.探测媒体, 媒体处理支持库.FFmpeg媒体.检查提供者, 转写支持库.转写.转写音频文件, 转写支持库.转写.检查可用性, 大语言模型支持库.模型连接器.连接LLM, 大语言模型支持库.模型连接器.生成对话, 大语言模型支持库.模型连接器.释放句柄, 媒体处理支持库.FFmpeg媒体.截取音频
