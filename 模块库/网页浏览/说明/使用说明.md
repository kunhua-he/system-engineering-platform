<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 网页浏览（模块库.网页浏览）

- 类型：基础模块
- 版本：1.0.0
- 说明：通过统一能力连接器组合浏览器会话、导航、读取和关闭。
- 入口：__init__.py

## 依赖能力
- 能力 `浏览器自动化.创建会话` 版本约束 `>=1.0.0`
- 能力 `浏览器自动化.导航页面` 版本约束 `>=1.0.0`
- 能力 `浏览器自动化.读取页面` 版本约束 `>=1.0.0`
- 能力 `浏览器自动化.关闭会话` 版本约束 `>=1.0.0`

## 能力
### 打开并读取网页（`网页浏览.打开并读取网页`）
- 版本：1.0.0
- 一句话说明：（无）
- 所属支持库或模块：模块库.网页浏览
- 参数列表：地址:文本型, 最大长度:整数型, 会话模式:文本型
- 必填参数：地址
- 可选参数：最大长度, 会话模式
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 导航失败, 资源未收敛, 返回结果不符合契约, 内部错误
- 调用示例：网页浏览.打开并读取网页(最大长度=20000, 会话模式=临时)
- 依赖能力：浏览器自动化.创建会话, 浏览器自动化.导航页面, 浏览器自动化.读取页面, 浏览器自动化.关闭会话
