<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 自修复工具（模块库.自修复工具）

- 类型：功能模块
- 版本：1.2.0
- 说明：组合 Git 支持库、文件系统（补丁/临时资源）、资源管理与受管进程：创建修复工作区、验证修复、回滚修复、获取当前提交哈希。
- 入口：__init__.py

## 依赖能力
- 能力 `版本控制支持库.Git操作.检查提供者` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.创建工作区` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.关闭工作区` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.提交` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.回滚` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.挑拣合入` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.当前状态` 版本约束 `>=1.0.0`
- 能力 `版本控制支持库.Git操作.获取当前提交哈希` 版本约束 `>=1.0.0`
- 能力 `文件系统支持库.文件操作.读取文件` 版本约束 `>=1.0.0`
- 能力 `文件系统支持库.文件操作.写入文件` 版本约束 `>=1.0.0`
- 能力 `文件系统支持库.文件操作.登记临时资源` 版本约束 `>=1.0.0`
- 能力 `文件系统支持库.文件操作.清理全部临时资源` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.创建内容摘要` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.创建唯一运行目录` 版本约束 `>=1.0.0`

## 能力
### 创建修复工作区（`自修复工具.创建修复工作区`）
- 版本：1.0.0
- 一句话说明：Git worktree + 精确补丁 + 定向验证 + 可回滚提交组合流程
- 所属支持库或模块：模块库.自修复工具
- 参数列表：仓库路径:文本型, 补丁列表:列表型, 验证命令:列表型, 提交消息:文本型, 期望摘要:文本型, 检查文件:文本型, 分支名:文本型, 超时秒:双精度数型
- 必填参数：仓库路径, 补丁列表, 验证命令, 提交消息
- 可选参数：期望摘要, 检查文件, 分支名, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 路径逃逸, 补丁多重匹配, 验证失败, 未提交修改, 冲突
- 调用示例：自修复工具.创建修复工作区(期望摘要=, 检查文件=, 分支名=, 超时秒=60)
- 依赖能力：版本控制支持库.Git操作.检查提供者, 版本控制支持库.Git操作.创建工作区, 版本控制支持库.Git操作.关闭工作区, 版本控制支持库.Git操作.提交, 版本控制支持库.Git操作.回滚, 版本控制支持库.Git操作.挑拣合入, 版本控制支持库.Git操作.当前状态, 版本控制支持库.Git操作.获取当前提交哈希, 文件系统支持库.文件操作.读取文件, 文件系统支持库.文件操作.写入文件, 文件系统支持库.文件操作.登记临时资源, 文件系统支持库.文件操作.清理全部临时资源, 系统核心支持库.资源管理.创建内容摘要, 系统核心支持库.资源管理.创建唯一运行目录
### 验证修复（`自修复工具.验证修复`）
- 版本：1.0.0
- 一句话说明：应用后运行验证命令并检查内容摘要
- 所属支持库或模块：模块库.自修复工具
- 参数列表：仓库路径:文本型, 验证命令:列表型, 期望摘要:文本型, 检查文件:文本型, 超时秒:双精度数型
- 必填参数：仓库路径, 验证命令
- 可选参数：期望摘要, 检查文件, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 验证失败
- 调用示例：自修复工具.验证修复(期望摘要=, 检查文件=, 超时秒=60)
- 依赖能力：版本控制支持库.Git操作.检查提供者, 版本控制支持库.Git操作.创建工作区, 版本控制支持库.Git操作.关闭工作区, 版本控制支持库.Git操作.提交, 版本控制支持库.Git操作.回滚, 版本控制支持库.Git操作.挑拣合入, 版本控制支持库.Git操作.当前状态, 版本控制支持库.Git操作.获取当前提交哈希, 文件系统支持库.文件操作.读取文件, 文件系统支持库.文件操作.写入文件, 文件系统支持库.文件操作.登记临时资源, 文件系统支持库.文件操作.清理全部临时资源, 系统核心支持库.资源管理.创建内容摘要, 系统核心支持库.资源管理.创建唯一运行目录
### 回滚修复（`自修复工具.回滚修复`）
- 版本：1.0.0
- 一句话说明：git revert/cherry-pick 组合撤销或挑拣修复
- 所属支持库或模块：模块库.自修复工具
- 参数列表：仓库路径:文本型, 提交哈希:文本型, 操作:文本型, 超时秒:双精度数型
- 必填参数：仓库路径, 提交哈希
- 可选参数：操作, 超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 未提交修改, 回滚失败, 冲突
- 调用示例：自修复工具.回滚修复(操作=回滚, 超时秒=60)
- 依赖能力：版本控制支持库.Git操作.检查提供者, 版本控制支持库.Git操作.创建工作区, 版本控制支持库.Git操作.关闭工作区, 版本控制支持库.Git操作.提交, 版本控制支持库.Git操作.回滚, 版本控制支持库.Git操作.挑拣合入, 版本控制支持库.Git操作.当前状态, 版本控制支持库.Git操作.获取当前提交哈希, 文件系统支持库.文件操作.读取文件, 文件系统支持库.文件操作.写入文件, 文件系统支持库.文件操作.登记临时资源, 文件系统支持库.文件操作.清理全部临时资源, 系统核心支持库.资源管理.创建内容摘要, 系统核心支持库.资源管理.创建唯一运行目录
### 获取当前提交哈希（`自修复工具.获取当前提交哈希`）
- 版本：1.0.0
- 一句话说明：获取仓库当前提交哈希与分支
- 所属支持库或模块：模块库.自修复工具
- 参数列表：仓库路径:文本型, 超时秒:双精度数型
- 必填参数：仓库路径
- 可选参数：超时秒
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 命令失败, 超时, 取消
- 调用示例：自修复工具.获取当前提交哈希(超时秒=60)
- 依赖能力：版本控制支持库.Git操作.检查提供者, 版本控制支持库.Git操作.创建工作区, 版本控制支持库.Git操作.关闭工作区, 版本控制支持库.Git操作.提交, 版本控制支持库.Git操作.回滚, 版本控制支持库.Git操作.挑拣合入, 版本控制支持库.Git操作.当前状态, 版本控制支持库.Git操作.获取当前提交哈希, 文件系统支持库.文件操作.读取文件, 文件系统支持库.文件操作.写入文件, 文件系统支持库.文件操作.登记临时资源, 文件系统支持库.文件操作.清理全部临时资源, 系统核心支持库.资源管理.创建内容摘要, 系统核心支持库.资源管理.创建唯一运行目录
