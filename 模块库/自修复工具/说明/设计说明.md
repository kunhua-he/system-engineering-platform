# 自修复工具 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：模块库.自修复工具
- 版本：1.2.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 自修复工具.创建修复工作区 | 创建修复工作区 | 1.0.0 | {'类型': '结果型', '值结构': {'路径': '工作区路径', '分支': '工作区分支', '提交': '提交哈希', '摘要': '验证摘要', '工作区清理': '已关闭或错误码'}} | Git worktree + 精确补丁 + 定向验证 + 可回滚提交组合流程 |
| 自修复工具.验证修复 | 验证修复 | 1.0.0 | {'类型': '结果型', '值结构': {'输出': '验证命令标准输出', '摘要': '检查文件摘要', '验证通过': True}} | 应用后运行验证命令并检查内容摘要 |
| 自修复工具.回滚修复 | 回滚修复 | 1.0.0 | {'类型': '结果型', '值结构': {'提交': '撤销或挑拣提交哈希'}} | git revert/cherry-pick 组合撤销或挑拣修复 |
| 自修复工具.获取当前提交哈希 | 获取当前提交哈希 | 1.0.0 | {'类型': '结果型', '值结构': {'提交哈希': '当前 HEAD 哈希', '分支': '当前分支名'}} | 获取仓库当前提交哈希与分支（经 Git 支持库公开能力） |

## 自修复工具.创建修复工作区（创建修复工作区）

**说明**：Git worktree + 精确补丁 + 定向验证 + 可回滚提交组合流程

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 补丁列表 | 列表型 | 是 | None | 补丁项含 文件(相对路径)/旧内容/新内容 字段 |
| 验证命令 | 列表型 | 是 | None | 受管执行命令参数列表（禁 shell） |
| 提交消息 | 文本型 | 是 | None | 单行提交消息 |
| 期望摘要 | 文本型 | 否 |  | 可选：检查文件期望 sha256 摘要 |
| 检查文件 | 文本型 | 否 |  | 可选：摘要检查相对路径 |
| 分支名 | 文本型 | 否 |  | 可选：修复工作区分支名 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：{'类型': '结果型', '值结构': {'路径': '工作区路径', '分支': '工作区分支', '提交': '提交哈希', '摘要': '验证摘要', '工作区清理': '已关闭或错误码'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 路径逃逸 |
| 补丁多重匹配 |
| 验证失败 |
| 未提交修改 |
| 冲突 |

**调用示例**：

```python
自修复工具.创建修复工作区(仓库路径=值, 补丁列表=值, 验证命令=值, 提交消息=值, 期望摘要=值, 检查文件=值, 分支名=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 自修复工具.验证修复（验证修复）

**说明**：应用后运行验证命令并检查内容摘要

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 工作区或仓库目录绝对路径 |
| 验证命令 | 列表型 | 是 | None | 受管执行命令参数列表（禁 shell） |
| 期望摘要 | 文本型 | 否 |  | 可选：期望 sha256 摘要 |
| 检查文件 | 文本型 | 否 |  | 可选：摘要检查相对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：{'类型': '结果型', '值结构': {'输出': '验证命令标准输出', '摘要': '检查文件摘要', '验证通过': True}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 验证失败 |

**调用示例**：

```python
自修复工具.验证修复(仓库路径=值, 验证命令=值, 期望摘要=值, 检查文件=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 自修复工具.回滚修复（回滚修复）

**说明**：git revert/cherry-pick 组合撤销或挑拣修复

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 提交哈希 | 文本型 | 是 | None | 7-40 位十六进制提交哈希 |
| 操作 | 文本型 | 否 | 回滚 | 回滚(revert) 或 挑拣合入(cherry-pick) |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：{'类型': '结果型', '值结构': {'提交': '撤销或挑拣提交哈希'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 未提交修改 |
| 回滚失败 |
| 冲突 |

**调用示例**：

```python
自修复工具.回滚修复(仓库路径=值, 提交哈希=值, 操作=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 自修复工具.获取当前提交哈希（获取当前提交哈希）

**说明**：获取仓库当前提交哈希与分支（经 Git 支持库公开能力）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 仓库路径 | 文本型 | 是 | None | 仓库目录绝对路径 |
| 超时秒 | 双精度数型 | 否 | 60 | 命令超时 |

**返回结构**：{'类型': '结果型', '值结构': {'提交哈希': '当前 HEAD 哈希', '分支': '当前分支名'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 命令失败 |
| 超时 |
| 取消 |

**调用示例**：

```python
自修复工具.获取当前提交哈希(仓库路径=值, 超时秒=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
