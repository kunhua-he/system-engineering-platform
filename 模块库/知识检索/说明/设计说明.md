# 知识检索 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：模块库.知识检索
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 知识检索.检索知识 | 检索知识 | 1.0.0 | {'类型': '结果型', '值结构': {'来源': '文本型', '相关文档': '列表型', '重排结果': '列表型', '缓存命中': '逻辑型', '总数': '整数型'}} | 知识检索：查嵌入缓存→加载文档→重排排序，返回来源/相关文档/重排结果/缓存命中。 |
| 知识检索.检查可用性 | 检查可用性 | 1.0.0 | {'类型': '结果型', '值结构': {'可用': '逻辑型', '缺失支持库': '列表型'}} | 检查模块依赖的支持库是否就绪。 |

## 知识检索.检索知识（检索知识）

**说明**：知识检索：查嵌入缓存→加载文档→重排排序，返回来源/相关文档/重排结果/缓存命中。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 查询 | 文本型 | 是 | None | 检索查询 |
| 知识库 | 文本型 | 是 | None | 知识库目录路径 |
| 模型名 | 文本型 | 否 | bge-m3 | 嵌入模型名 |
| 提供者 | 文本型 | 否 | 本地 | 提供者标识 |
| 顶部数量 | 整数型 | 否 | 5 | 返回顶部 N 条 |

**返回结构**：{'类型': '结果型', '值结构': {'来源': '文本型', '相关文档': '列表型', '重排结果': '列表型', '缓存命中': '逻辑型', '总数': '整数型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |

**调用示例**：

```python
知识检索.检索知识(查询=值, 知识库=值, 模型名=值, 提供者=值, 顶部数量=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 知识检索.检查可用性（检查可用性）

**说明**：检查模块依赖的支持库是否就绪。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 知识库 | 文本型 | 否 | None | 知识库路径 |

**返回结构**：{'类型': '结果型', '值结构': {'可用': '逻辑型', '缺失支持库': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |

**调用示例**：

```python
知识检索.检查可用性(知识库=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
