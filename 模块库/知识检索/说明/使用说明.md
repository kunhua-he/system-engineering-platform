<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 知识检索（模块库.知识检索）

- 类型：基础模块
- 版本：1.0.0
- 说明：组合 嵌入缓存/重排服务/文件系统 完成知识检索流程。模块只持能力 id 与契约版本。
- 入口：__init__.py

## 依赖能力
- 能力 `大语言模型支持库.嵌入缓存.查询嵌入` 版本约束 `>=1.0.0`
- 能力 `大语言模型支持库.重排服务.重排` 版本约束 `>=1.0.0`
- 能力 `文件系统支持库.文件操作.列出目录` 版本约束 `>=1.0.0`
- 能力 `文件系统支持库.文件操作.读取文件` 版本约束 `>=1.0.0`

## 能力
### 检索知识（`知识检索.检索知识`）
- 版本：1.0.0
- 一句话说明：检索并重排知识
- 所属支持库或模块：模块库.知识检索
- 参数列表：查询:文本型, 知识库:文本型, 模型名:文本型, 提供者:文本型, 顶部数量:整数型
- 必填参数：查询, 知识库
- 可选参数：模型名, 提供者, 顶部数量
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 返回结果不符合契约
- 调用示例：知识检索.检索知识()
- 依赖能力：大语言模型支持库.嵌入缓存.查询嵌入, 大语言模型支持库.重排服务.重排, 文件系统支持库.文件操作.列出目录, 文件系统支持库.文件操作.读取文件
### 检查可用性（`知识检索.检查可用性`）
- 版本：1.0.0
- 一句话说明：检查模块可用性
- 所属支持库或模块：模块库.知识检索
- 参数列表：知识库:文本型
- 必填参数：无
- 可选参数：知识库
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法
- 调用示例：知识检索.检查可用性()
- 依赖能力：大语言模型支持库.嵌入缓存.查询嵌入, 大语言模型支持库.重排服务.重排, 文件系统支持库.文件操作.列出目录, 文件系统支持库.文件操作.读取文件
