# 知识检索模块

## 职责
组合 嵌入缓存/重排服务/文件系统 完成知识检索流程。

## 能力
| 能力 | 说明 |
|---|---|
| `知识检索.检索知识` | 查缓存→加载文档→重排 |
| `知识检索.检查可用性` | 检查依赖支持库就绪 |

## 依赖
- 嵌入缓存.查询嵌入（>=1.0.0）
- 重排服务.重排（>=1.0.0）
- 文件系统.列出目录 / 读取文件（>=1.0.0）
