"""知识检索模块包级中文入口。

调用者只能从此入口导入，禁止深入 实现/ 目录。
"""

from __future__ import annotations

from 模块库.知识检索.实现.知识检索 import 检索知识
from 模块库.知识检索.实现.知识检索 import 检查可用性
from 公共契约.基础类型.逻辑类型 import 真, 假

__all__ = [
    "检索知识",
    "检查可用性",
]


from 模块库.知识检索.实现.知识检索 import 检索知识
from 模块库.知识检索.实现.知识检索 import 检查可用性


def 注册能力(注册表) -> None:
    """由支持库加载器调用。"""
    from 公共契约.能力契约.契约 import 能力实现

    for 能力id, 函数, 参数表 in [
        ("知识检索.检索知识", 检索知识, [{"名称": "查询", "类型": "文本型", "必填": 真, "默认值": None, "说明": "检索查询"}, {"名称": "知识库", "类型": "文本型", "必填": 真, "默认值": None, "说明": "知识库目录路径"}, {"名称": "模型名", "类型": "文本型", "必填": 假, "默认值": "bge-m3", "说明": "嵌入模型名"}, {"名称": "提供者", "类型": "文本型", "必填": 假, "默认值": "本地", "说明": "提供者标识"}, {"名称": "顶部数量", "类型": "整数型", "必填": 假, "默认值": 5, "说明": "返回顶部 N 条"}]),
        ("知识检索.检查可用性", 检查可用性, [{"名称": "知识库", "类型": "文本型", "必填": 假, "默认值": None, "说明": "知识库路径"}]),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="模块库.知识检索",
                实现函数=函数,
                参数=参数表,
                返回="结果",
                说明="检查模块依赖的支持库是否就绪。",
            )
        )
