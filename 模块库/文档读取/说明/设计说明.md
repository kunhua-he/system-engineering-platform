# 文档读取 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。
<!-- 机器管理｜类型：包级说明-设计说明｜生成器：开发工具.MD文档生成 -->


- 包id：模块库.文档读取
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 文档读取.读取正文 | 读取正文 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '文本型', '成功': '逻辑型', '错误': '错误结构'}} | 读取文档正文文本：经 文件系统支持库.文件操作.读取文件 能力读取，返回 结果(值=文本)；文件路径非法返回 参数不合法，文件不存在等错误透传支持库。 |
| 文档读取.提取标题 | 提取标题 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '文本型', '成功': '逻辑型', '错误': '错误结构'}} | 提取文档标题（首个非空行）：经 文件系统支持库.文件操作.读取文件 + 数据操作支持库.文本处理.按行分割/去空白 组合，无标题返回空字符串。 |
| 文档读取.计算段落数 | 计算段落数 | 1.0.0 | {'类型': '结果型', '值结构': {'值': '整数型', '成功': '逻辑型', '错误': '错误结构'}} | 统计非空段落数：经 文件系统支持库.文件操作.读取文件 + 数据操作支持库.文本处理.按行分割/去空白 组合，返回 结果(值=整数)。 |
| 文档读取.结构化读取 | 结构化读取 | 1.0.0 | {'类型': '结果型', '值结构': {'值': {'正文': '文本型', '标题': '文本型', '段落数': '整数型', '错误': '空', '序列化文本': '文本型'}, '成功': '逻辑型', '错误': '错误结构'}} | 结构化读取：正文/标题/段落数 组合并经 数据操作支持库.数据交换.序列化JSON 产出 序列化文本，返回 结果(值=结构字典)。 |

## 文档读取.读取正文（读取正文）

**说明**：读取文档正文文本：经 文件系统支持库.文件操作.读取文件 能力读取，返回 结果(值=文本)；文件路径非法返回 参数不合法，文件不存在等错误透传支持库。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文档文件路径（非空文本） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '文本型', '成功': '逻辑型', '错误': '错误结构'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |

**调用示例**：

```python
文档读取.读取正文(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文档读取.提取标题（提取标题）

**说明**：提取文档标题（首个非空行）：经 文件系统支持库.文件操作.读取文件 + 数据操作支持库.文本处理.按行分割/去空白 组合，无标题返回空字符串。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文档文件路径（非空文本） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '文本型', '成功': '逻辑型', '错误': '错误结构'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |

**调用示例**：

```python
文档读取.提取标题(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文档读取.计算段落数（计算段落数）

**说明**：统计非空段落数：经 文件系统支持库.文件操作.读取文件 + 数据操作支持库.文本处理.按行分割/去空白 组合，返回 结果(值=整数)。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文档文件路径（非空文本） |

**返回结构**：{'类型': '结果型', '值结构': {'值': '整数型', '成功': '逻辑型', '错误': '错误结构'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 提供者不可用 |

**调用示例**：

```python
文档读取.计算段落数(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文档读取.结构化读取（结构化读取）

**说明**：结构化读取：正文/标题/段落数 组合并经 数据操作支持库.数据交换.序列化JSON 产出 序列化文本，返回 结果(值=结构字典)。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 文档文件路径（非空文本） |

**返回结构**：{'类型': '结果型', '值结构': {'值': {'正文': '文本型', '标题': '文本型', '段落数': '整数型', '错误': '空', '序列化文本': '文本型'}, '成功': '逻辑型', '错误': '错误结构'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 文件不存在 |
| 序列化失败 |
| 提供者不可用 |

**调用示例**：

```python
文档读取.结构化读取(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
