<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 简单窗口（模块库.简单窗口）

- 类型：基础模块
- 版本：1.0.0
- 说明：基础模块：经能力调用器组合 窗口描述/状态描述 支持库能力，管理窗口定义与状态流转；不做真实渲染。
- 入口：__init__.py

## 依赖能力
- 能力 `窗口描述.创建窗口定义` 版本约束 `>=1.0.0`
- 能力 `窗口描述.校验窗口定义` 版本约束 `>=1.0.0`
- 能力 `状态描述.创建窗口状态` 版本约束 `>=1.0.0`
- 能力 `状态描述.状态流转校验` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.创建受管状态` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.读取受管状态` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.更新受管状态` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.释放受管状态` 版本约束 `>=1.0.0`

## 能力
### 创建窗口（`简单窗口.创建窗口`）
- 版本：1.0.0
- 一句话说明：创建窗口定义并初始化为关闭状态
- 所属支持库或模块：模块库.简单窗口
- 参数列表：窗口id:文本型, 标题:文本型, 宽度:整数型, 高度:整数型
- 必填参数：窗口id, 标题, 宽度, 高度
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 内部错误
- 调用示例：简单窗口.创建窗口()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 打开窗口（`简单窗口.打开窗口`）
- 版本：1.0.0
- 一句话说明：将窗口状态流转为打开
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 状态流转不合法, 内部错误
- 调用示例：简单窗口.打开窗口()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 关闭窗口（`简单窗口.关闭窗口`）
- 版本：1.0.0
- 一句话说明：将窗口状态流转为关闭
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 状态流转不合法, 内部错误
- 调用示例：简单窗口.关闭窗口()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 传递参数（`简单窗口.传递参数`）
- 版本：1.0.0
- 一句话说明：向窗口传递参数并返回新状态
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型, 参数:字典型
- 必填参数：句柄, 参数
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 内部错误
- 调用示例：简单窗口.传递参数()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 获取窗口描述（`简单窗口.获取窗口描述`）
- 版本：1.0.0
- 一句话说明：获取窗口定义与状态描述
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 内部错误
- 调用示例：简单窗口.获取窗口描述()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 释放窗口（`简单窗口.释放窗口`）
- 版本：1.0.0
- 一句话说明：释放窗口受管状态句柄
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 句柄已过期, 内部错误
- 调用示例：简单窗口.释放窗口(句柄=1)
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
