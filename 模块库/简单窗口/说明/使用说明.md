# 简单窗口（模块库.简单窗口）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：基础模块
- 版本：1.0.2
- 说明：基础模块：经能力调用器组合 窗口描述/状态描述 支持库能力，管理窗口定义与状态流转；不做真实渲染。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `窗口描述.创建窗口定义` 版本约束 `>=1.0.0`
- 能力 `窗口描述.校验窗口定义` 版本约束 `>=1.0.0`
- 能力 `状态描述.创建窗口状态` 版本约束 `>=1.0.0`
- 能力 `状态描述.状态流转校验` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.创建受管状态` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.读取受管状态` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.更新受管状态` 版本约束 `>=1.0.0`
- 能力 `系统核心支持库.资源管理.释放受管状态` 版本约束 `>=1.0.0`

## 能力
### 创建窗口（`简单窗口.创建窗口`）
- 版本：1.0.0
- 一句话说明：创建窗口定义并初始化为关闭状态；经能力调用器组合窗口描述与状态描述支持库能力
- 所属支持库或模块：模块库.简单窗口
- 参数列表：窗口id:文本型, 标题:文本型, 宽度:整数型, 高度:整数型
- 必填参数：窗口id, 标题, 宽度, 高度
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'窗口id': '文本型', '标题': '文本型', '宽度': '整数型', '高度': '整数型', '状态': '文本型', '句柄': '整数型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 内部错误
- 调用示例：简单窗口.创建窗口()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 打开窗口（`简单窗口.打开窗口`）
- 版本：1.0.0
- 一句话说明：将窗口状态流转为打开；经能力调用器调用状态描述状态流转校验能力
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '流转记录': '列表型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 状态流转不合法, 内部错误
- 调用示例：简单窗口.打开窗口()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 关闭窗口（`简单窗口.关闭窗口`）
- 版本：1.0.0
- 一句话说明：将窗口状态流转为关闭；经能力调用器调用状态描述状态流转校验能力
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '流转记录': '列表型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 状态流转不合法, 内部错误
- 调用示例：简单窗口.关闭窗口()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 传递参数（`简单窗口.传递参数`）
- 版本：1.0.0
- 一句话说明：向窗口传递参数并返回新状态
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型, 参数:字典型
- 必填参数：句柄, 参数
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '参数': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 内部错误
- 调用示例：简单窗口.传递参数()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 获取窗口描述（`简单窗口.获取窗口描述`）
- 版本：1.0.0
- 一句话说明：获取窗口定义与当前状态、参数描述
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'窗口id': '文本型', '标题': '文本型', '宽度': '整数型', '高度': '整数型', '状态': '文本型', '参数': '字典型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 窗口不存在, 内部错误
- 调用示例：简单窗口.获取窗口描述()
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
### 释放窗口（`简单窗口.释放窗口`）
- 版本：1.0.0
- 一句话说明：释放窗口受管状态句柄
- 所属支持库或模块：模块库.简单窗口
- 参数列表：句柄:整数型
- 必填参数：句柄
- 可选参数：无
- 返回结构：{'类型': '结果型', '值结构': {'句柄': '整数型', '状态': '文本型'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 句柄无效, 句柄已过期, 内部错误
- 调用示例：简单窗口.释放窗口(句柄=1)
- 依赖能力：窗口描述.创建窗口定义, 窗口描述.校验窗口定义, 状态描述.创建窗口状态, 状态描述.状态流转校验, 系统核心支持库.资源管理.创建受管状态, 系统核心支持库.资源管理.读取受管状态, 系统核心支持库.资源管理.更新受管状态, 系统核心支持库.资源管理.释放受管状态
