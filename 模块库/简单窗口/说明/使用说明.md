# 简单窗口 使用说明

- 包id：模块库.简单窗口
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 简单窗口.创建窗口 | 创建窗口 | 1.0.0 | {'类型': '结果型', '值结构': {'窗口id': '文本型', '标题': '文本型', '宽度': '整数型', '高度': '整数型', '状态': '文本型'}} | 创建窗口定义并初始化为关闭状态；经能力调用器组合窗口描述与状态描述支持库能力 |
| 简单窗口.打开窗口 | 打开窗口 | 1.0.0 | {'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '流转记录': '列表型'}} | 将窗口状态流转为打开；经能力调用器调用状态描述状态流转校验能力 |
| 简单窗口.关闭窗口 | 关闭窗口 | 1.0.0 | {'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '流转记录': '列表型'}} | 将窗口状态流转为关闭；经能力调用器调用状态描述状态流转校验能力 |
| 简单窗口.传递参数 | 传递参数 | 1.0.0 | {'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '参数': '字典型'}} | 向窗口传递参数并返回新状态 |
| 简单窗口.获取窗口描述 | 获取窗口描述 | 1.0.0 | {'类型': '结果型', '值结构': {'窗口id': '文本型', '标题': '文本型', '宽度': '整数型', '高度': '整数型', '状态': '文本型', '参数': '字典型'}} | 获取窗口定义与当前状态、参数描述 |

## 简单窗口.创建窗口（创建窗口）

**说明**：创建窗口定义并初始化为关闭状态；经能力调用器组合窗口描述与状态描述支持库能力

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 窗口id | 文本型 | 是 | None | 窗口唯一标识 |
| 标题 | 文本型 | 是 | None | 窗口标题 |
| 宽度 | 整数型 | 是 | None | 窗口宽度像素 |
| 高度 | 整数型 | 是 | None | 窗口高度像素 |

**返回结构**：{'类型': '结果型', '值结构': {'窗口id': '文本型', '标题': '文本型', '宽度': '整数型', '高度': '整数型', '状态': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 窗口不存在 |
| 内部错误 |

**调用示例**：

```python
简单窗口.创建窗口(窗口id=值, 标题=值, 宽度=值, 高度=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 简单窗口.打开窗口（打开窗口）

**说明**：将窗口状态流转为打开；经能力调用器调用状态描述状态流转校验能力

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 窗口id | 文本型 | 是 | None | 窗口唯一标识 |

**返回结构**：{'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '流转记录': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 窗口不存在 |
| 状态流转不合法 |
| 内部错误 |

**调用示例**：

```python
简单窗口.打开窗口(窗口id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 简单窗口.关闭窗口（关闭窗口）

**说明**：将窗口状态流转为关闭；经能力调用器调用状态描述状态流转校验能力

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 窗口id | 文本型 | 是 | None | 窗口唯一标识 |

**返回结构**：{'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '流转记录': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 窗口不存在 |
| 状态流转不合法 |
| 内部错误 |

**调用示例**：

```python
简单窗口.关闭窗口(窗口id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 简单窗口.传递参数（传递参数）

**说明**：向窗口传递参数并返回新状态

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 窗口id | 文本型 | 是 | None | 窗口唯一标识 |
| 参数 | 字典型 | 是 | None | 要传递给窗口的参数字典 |

**返回结构**：{'类型': '结果型', '值结构': {'窗口id': '文本型', '状态': '文本型', '参数': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 窗口不存在 |
| 内部错误 |

**调用示例**：

```python
简单窗口.传递参数(窗口id=值, 参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 简单窗口.获取窗口描述（获取窗口描述）

**说明**：获取窗口定义与当前状态、参数描述

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 窗口id | 文本型 | 是 | None | 窗口唯一标识 |

**返回结构**：{'类型': '结果型', '值结构': {'窗口id': '文本型', '标题': '文本型', '宽度': '整数型', '高度': '整数型', '状态': '文本型', '参数': '字典型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 窗口不存在 |
| 内部错误 |

**调用示例**：

```python
简单窗口.获取窗口描述(窗口id=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
