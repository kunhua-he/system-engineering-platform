# 简单窗口 · 目录树与命令

> 包id：`模块库.简单窗口`；版本：`1.0.2`；类型：`基础模块`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。
<!-- 机器管理｜类型：包级说明-目录树与命令｜生成器：开发工具.MD文档生成 -->


## 目录树

```text
简单窗口/
├── __init__.py
├── 依赖契约/依赖契约.json
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/简单窗口.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/Agent查询数据.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
```

## 公开命令

### `简单窗口.创建窗口`

- 名称：创建窗口
- 返回：`结果型`
- 说明：创建窗口定义并初始化为关闭状态
- 参数：
  - `窗口id`；文本型
  - `标题`；文本型
  - `宽度`；整数型
  - `高度`；整数型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `简单窗口.打开窗口`

- 名称：打开窗口
- 返回：`结果型`
- 说明：将窗口状态流转为打开
- 参数：
  - `窗口id`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `简单窗口.关闭窗口`

- 名称：关闭窗口
- 返回：`结果型`
- 说明：将窗口状态流转为关闭
- 参数：
  - `窗口id`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `简单窗口.传递参数`

- 名称：传递参数
- 返回：`结果型`
- 说明：向窗口传递参数并返回新状态
- 参数：
  - `窗口id`；文本型
  - `参数`；字典型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `简单窗口.获取窗口描述`

- 名称：获取窗口描述
- 返回：`结果型`
- 说明：获取窗口定义与状态描述
- 参数：
  - `窗口id`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
