# 项目文档 使用说明
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 包id：模块库.项目文档
- 版本：1.0.2

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 项目文档.登记项目 | 登记项目 | 1.0.1 | {'类型': '结果型', '值结构': {'项目名': '文本型，登记的项目名（同项目名覆盖更新，幂等）', '项目根目录': '文本型，项目根绝对路径', '文档根相对路径': '文本型，相对项目根的文档根路径', '规范文件': '文本型，该项目使用的规范文件路径（空=用默认判据文件）', '是否新增': '逻辑型，本次是否为新增登记（假=覆盖更新已有登记）', '文档根绝对路径': '文本型，校验通过的文档根绝对路径', '判据文件': '文本型，判据文件绝对路径（生成区校验的依据）', '判据文件就位': '逻辑型，判据文件是否真实存在（缺则后续校验无法执行）'}} | 登记一个项目：先校验项目根与文档根真实存在、判据文件就位，再写入登记表（同项目名覆盖更新，幂等）；项目根不存在返回 目录不存在。支持库的存储能力不校验目录存在性，边界校验在模块层做。返回登记档案与文档根绝对路径、判据文件及其就位状态。 |
| 项目文档.写文档 | 写文档 | 1.0.2 | {'类型': '结果型', '值结构': {'项目名': '文本型，写入的项目名', '文档相对路径': '文本型，相对文档根的文档路径（已去首尾 /）', '文档绝对路径': '文本型，实际落盘的目标绝对路径（必在文档根内）', '判据文件': '文本型，本次校验所用判据文件绝对路径（无判据的「通过」是假绿）', '入库': '字典型，后台入库受理情况：已受理 / 是否完成 / 说明（失败不回滚已落盘文件）'}} | 按规范判据校验正文，合规才落盘成 md，并在后台线程（非守护、单次、30 秒硬超时）入库；不合规不落盘，单独返回违规范清单（判据id/严重级/行号）。判据文件缺失返回 判据文件不存在，不假装通过；目标已存在且未声明覆盖返回 已存在；路径越出文档根返回 路径越界。入库失败不回滚已落盘文件（文件是权威、索引是派生）。 |
| 项目文档.查文档 | 查文档 | 1.0.1 | {'类型': '结果型', '值结构': {'项目名': '文本型，查询的项目名', '关键词': '文本型，本次查询关键词', '命中列表': '列表型，命中条目（文档相对路径/文档绝对路径/起始行/结束行/块类型/命中词数/命中次数/片段/可回溯）', '命中数': '整数型，命中条目数', '是否截断': '逻辑型，是否因条数上限被截断', '数据来源': '文本型，数据出处（索引 / 回退搜索（索引未命中）/ 索引与回退均无结果）', '文档根绝对路径': '文本型，本次检索所用文档根绝对路径'}} | 按项目 + 关键词查文档索引，返回文件相对路径/起始行/结束行/片段，每条带 可回溯 标记（索引记录的文件必须真实存在，否则标为不可回溯）；索引未命中时回退到文档根内的正文正则搜索，不返回假空。数据来源字段如实标注是索引命中还是回退搜索。 |
| 项目文档.查记忆 | 查记忆 | 1.0.1 | {'类型': '结果型', '值结构': {'项目名': '文本型，查询的项目名（作为记忆的项目过滤键）', '关键词': '文本型，本次查询关键词', '记忆列表': '列表型，命中的历史记忆条目', '命中数': '整数型，命中记忆条数', '来源': '文本型，数据出处（固定为 记忆支持库.搜索记忆）'}} | 查该项目的历史记忆：委托 记忆支持库.搜索记忆（带项目过滤），不重造记忆能力。默认 仅关键词=真，只用关键词在名称+正文+标签上匹配，不调用大模型与向量（结构化查询不付 LLM 代价）。 |
| 项目文档.列文档类型 | 列文档类型 | 1.0.0 | {'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1，绝不冒充 0）', '通过': '逻辑型，退出码为 0 即为真', '类型列表': '列表型，解析出的文档类型条目（类型名/处置方式等）', '类型数': '整数型，类型条目数', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}} | 列出全仓已登记的文档类型与处置方式（等价 `python3.14 -m 开发工具.MD文档生成 --列出`）：经 系统核心支持库.进程管理.执行命令 跑平台唯一 md 生成器，回带逐类序号/类型名/现存份数/处置，另给解析后的 类型列表。项目根留空自举（向上找含 `开发文档/` 的目录）；自举不到返回 参数不合法，不猜。生成器跑不起来透传 执行命令 的错误码，不假装列过。 |
| 项目文档.校验文档生成区 | 校验文档生成区 | 1.0.0 | {'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '类型名': '文本型，本次校验的文档类型名', '文件': '文本型，本次校验的单份文件（未指定序号时为空）', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1）', '通过': '逻辑型，退出码为 0 即为真（生成区与生成器算出的那份一致）', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}} | 核验某类（或某一份）文档的生成区是否为生成器算出来的那份（等价 `--类型 X --校验`）。`通过 = 生成器退出码为 0`：退出码 1 即「有违规范/有漂移」，违规范清单就在 输出行 里原样回带；退出码 2 即拒绝执行（生成区边界缺失等）。类型名 优先、其次 序号，两者都没给返回 参数不合法。相对路径含 `..` 返回 路径越界。 |
| 项目文档.写文档生成区 | 写文档生成区 | 1.0.0 | {'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '类型名': '文本型，本次刷新的文档类型名', '文件': '文本型，本次刷新的单份文件（未指定序号时为空）', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1）', '通过': '逻辑型，退出码为 0 即为真（刷新并补机器印记成功）', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}} | 刷新某类（或某一份）文档的生成区并补机器印记（等价 `--类型 X --写盘`）。这是「生成区只有生成器能写」在能力侧的落点：生成区永远由生成器算，调用方改不了。`通过 = 生成器退出码为 0`，明细在 输出行 里原样回带；生成区边界切不出时生成器 fail-closed 拒绝（退出码 2），本能力照实回带不假装写过。 |
| 项目文档.人工改文档 | 人工改文档 | 1.0.0 | {'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '目标文件': '文本型，被修改的目标文档绝对路径', '草稿文件': '文本型，提供正文的草稿文件绝对路径', '是否落盘': '逻辑型，是否真写且退出码为 0（干跑时为假）', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1）', '通过': '逻辑型，退出码为 0 即为真', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}} | 人工改正文的唯一落盘通道（等价 `--人工改 --文件 <目标> --正文文件 <草稿>`）：生成器负责「类型判据校验 → 生成区保护 → 加机器印记 → 落盘」四步。缺 写盘=真 时只干跑并报结论。**草稿改动了生成区即被拒绝**（fail-closed），输出行里指明该生成区归谁管、怎么恢复。是否落盘 = 声明要写 且 退出码为 0，被拒绝时绝不报已落盘。 |

## 项目文档.登记项目（登记项目）

**说明**：登记一个项目：先校验项目根与文档根真实存在、判据文件就位，再写入登记表（同项目名覆盖更新，幂等）；项目根不存在返回 目录不存在。支持库的存储能力不校验目录存在性，边界校验在模块层做。返回登记档案与文档根绝对路径、判据文件及其就位状态。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目名 | 文本型 | 是 | None | 项目名（登记表主键） |
| 项目根目录 | 文本型 | 是 | None | 项目绝对路径 |
| 文档根相对路径 | 文本型 | 否 | 开发文档 | 相对项目根的文档根 |
| 规范文件 | 文本型 | 否 |  | 规范文件相对项目根的路径（留空按约定推导判据文件） |
| 库文件 | 文本型 | 否 | None | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |

**返回结构**：{'类型': '结果型', '值结构': {'项目名': '文本型，登记的项目名（同项目名覆盖更新，幂等）', '项目根目录': '文本型，项目根绝对路径', '文档根相对路径': '文本型，相对项目根的文档根路径', '规范文件': '文本型，该项目使用的规范文件路径（空=用默认判据文件）', '是否新增': '逻辑型，本次是否为新增登记（假=覆盖更新已有登记）', '文档根绝对路径': '文本型，校验通过的文档根绝对路径', '判据文件': '文本型，判据文件绝对路径（生成区校验的依据）', '判据文件就位': '逻辑型，判据文件是否真实存在（缺则后续校验无法执行）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 项目未登记 |
| 索引库不可用 |
| 提供者不可用 |

**调用示例**：

```python
项目文档.登记项目(项目名=值, 项目根目录=值, 文档根相对路径=值, 规范文件=值, 库文件=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）

## 项目文档.写文档（写文档）

**说明**：按规范判据校验正文，合规才落盘成 md，并在后台线程（非守护、单次、30 秒硬超时）入库；不合规不落盘，单独返回违规范清单（判据id/严重级/行号）。判据文件缺失返回 判据文件不存在，不假装通过；目标已存在且未声明覆盖返回 已存在；路径越出文档根返回 路径越界。入库失败不回滚已落盘文件（文件是权威、索引是派生）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目名 | 文本型 | 是 | None | 已登记的项目名 |
| 文档相对路径 | 文本型 | 是 | None | 相对**文档根**的路径，须落在文档根内。解析基准 = 登记档案的 文档根相对路径（不是项目根）：本仓 系统工程平台 登记为 "."（=项目根），故想落到 开发文档/ 下必须写全 开发文档/分析/…，写 分析/… 会落到仓库根 |
| 正文 | 文本型 | 是 | None | 文档正文文本 |
| 判据文件 | 文本型 | 否 |  | 规范判据 JSON 绝对路径；留空按登记档案的 规范文件 推导，再退到 <文档根>/规范/Markdown 文档体例判据.json 与 <项目根>/开发文档/规范/Markdown 文档体例判据.json（逐条按存在性取第一条命中，全不命中即明确失败并列出候选） |
| 是否权威文档 | 逻辑型 | 否 | False | 真=按权威文档口径校验（含元信息头要求） |
| 覆盖 | 逻辑型 | 否 | False | 真=允许覆盖已存在文件 |
| 库文件 | 文本型 | 否 | None | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |
| 等待入库完成 | 逻辑型 | 否 | False | 真=同步入库（诊断与验证用）；假=后台线程入库 |

**返回结构**：{'类型': '结果型', '值结构': {'项目名': '文本型，写入的项目名', '文档相对路径': '文本型，相对文档根的文档路径（已去首尾 /）', '文档绝对路径': '文本型，实际落盘的目标绝对路径（必在文档根内）', '判据文件': '文本型，本次校验所用判据文件绝对路径（无判据的「通过」是假绿）', '入库': '字典型，后台入库受理情况：已受理 / 是否完成 / 说明（失败不回滚已落盘文件）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 目录不存在 |
| 路径越界 |
| 已存在 |
| 判据文件不存在 |
| 项目未登记 |
| 索引库不可用 |
| 提供者不可用 |
| 内部错误 |

**调用示例**：

```python
项目文档.写文档(项目名=值, 文档相对路径=值, 正文=值, 判据文件=值, 是否权威文档=值, 覆盖=值, 库文件=值, 等待入库完成=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）

## 项目文档.查文档（查文档）

**说明**：按项目 + 关键词查文档索引，返回文件相对路径/起始行/结束行/片段，每条带 可回溯 标记（索引记录的文件必须真实存在，否则标为不可回溯）；索引未命中时回退到文档根内的正文正则搜索，不返回假空。数据来源字段如实标注是索引命中还是回退搜索。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目名 | 文本型 | 是 | None | 已登记的项目名 |
| 关键词 | 文本型 | 是 | None | 查询关键词（空白分隔多词，全命中优先） |
| 返回条数 | 整数型 | 否 | 20 | 返回上限 |
| 库文件 | 文本型 | 否 | None | 库文件路径；留空取默认（口径：写项目根相对路径（posix、不带 ..）；绝对路径兼容） |

**返回结构**：{'类型': '结果型', '值结构': {'项目名': '文本型，查询的项目名', '关键词': '文本型，本次查询关键词', '命中列表': '列表型，命中条目（文档相对路径/文档绝对路径/起始行/结束行/块类型/命中词数/命中次数/片段/可回溯）', '命中数': '整数型，命中条目数', '是否截断': '逻辑型，是否因条数上限被截断', '数据来源': '文本型，数据出处（索引 / 回退搜索（索引未命中）/ 索引与回退均无结果）', '文档根绝对路径': '文本型，本次检索所用文档根绝对路径'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 项目未登记 |
| 索引库不可用 |
| 提供者不可用 |

**调用示例**：

```python
项目文档.查文档(项目名=值, 关键词=值, 返回条数=值, 库文件=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）

## 项目文档.查记忆（查记忆）

**说明**：查该项目的历史记忆：委托 记忆支持库.搜索记忆（带项目过滤），不重造记忆能力。默认 仅关键词=真，只用关键词在名称+正文+标签上匹配，不调用大模型与向量（结构化查询不付 LLM 代价）。

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目名 | 文本型 | 是 | None | 记忆的项目过滤键 |
| 关键词 | 文本型 | 是 | None | 查询内容 |
| 返回条数 | 整数型 | 否 | 5 | 返回条数 |
| 仅关键词 | 逻辑型 | 否 | True | 真=不调用大模型与向量 |

**返回结构**：{'类型': '结果型', '值结构': {'项目名': '文本型，查询的项目名（作为记忆的项目过滤键）', '关键词': '文本型，本次查询关键词', '记忆列表': '列表型，命中的历史记忆条目', '命中数': '整数型，命中记忆条数', '来源': '文本型，数据出处（固定为 记忆支持库.搜索记忆）'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |

**调用示例**：

```python
项目文档.查记忆(项目名=值, 关键词=值, 返回条数=值, 仅关键词=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）
## 项目文档.列文档类型（列文档类型）
**说明**：列出全仓已登记的文档类型与处置方式（等价 `python3.14 -m 开发工具.MD文档生成 --列出`）：经 系统核心支持库.进程管理.执行命令 跑平台唯一 md 生成器，回带逐类序号/类型名/现存份数/处置，另给解析后的 类型列表。项目根留空自举（向上找含 `开发文档/` 的目录）；自举不到返回 参数不合法，不猜。生成器跑不起来透传 执行命令 的错误码，不假装列过。
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目根 | 文本型 | 否 |  | 目标项目根的绝对路径；留空自举（向上找含 开发文档/ 的目录），自举失败即 参数不合法 |
**返回结构**：{'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1，绝不冒充 0）', '通过': '逻辑型，退出码为 0 即为真', '类型列表': '列表型，解析出的文档类型条目（类型名/处置方式等）', '类型数': '整数型，类型条目数', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}}（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 内部错误 |
**调用示例**：
```python
项目文档.列文档类型(项目根=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）
## 项目文档.校验文档生成区（校验文档生成区）
**说明**：核验某类（或某一份）文档的生成区是否为生成器算出来的那份（等价 `--类型 X --校验`）。`通过 = 生成器退出码为 0`：退出码 1 即「有违规范/有漂移」，违规范清单就在 输出行 里原样回带；退出码 2 即拒绝执行（生成区边界缺失等）。类型名 优先、其次 序号，两者都没给返回 参数不合法。相对路径含 `..` 返回 路径越界。
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目根 | 文本型 | 否 |  | 目标项目根的绝对路径；留空自举 |
| 类型名 | 文本型 | 否 |  | 文档类型中文名（用 项目文档.列文档类型 查）；与 序号 二选一，类型名 优先 |
| 序号 | 整数型 | 否 | 0 | 文档类型序号；类型名 未给时用它 |
| 文件 | 文本型 | 否 |  | 只核这一份（相对项目根的路径）；留空核该类型全部现存文件 |
**返回结构**：{'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '类型名': '文本型，本次校验的文档类型名', '文件': '文本型，本次校验的单份文件（未指定序号时为空）', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1）', '通过': '逻辑型，退出码为 0 即为真（生成区与生成器算出的那份一致）', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}}（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 路径越界 |
| 提供者不可用 |
| 内部错误 |
**调用示例**：
```python
项目文档.校验文档生成区(项目根=值, 类型名=值, 序号=值, 文件=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）
## 项目文档.写文档生成区（写文档生成区）
**说明**：刷新某类（或某一份）文档的生成区并补机器印记（等价 `--类型 X --写盘`）。这是「生成区只有生成器能写」在能力侧的落点：生成区永远由生成器算，调用方改不了。`通过 = 生成器退出码为 0`，明细在 输出行 里原样回带；生成区边界切不出时生成器 fail-closed 拒绝（退出码 2），本能力照实回带不假装写过。
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目根 | 文本型 | 否 |  | 目标项目根的绝对路径；留空自举 |
| 类型名 | 文本型 | 否 |  | 文档类型中文名；与 序号 二选一，类型名 优先 |
| 序号 | 整数型 | 否 | 0 | 文档类型序号；类型名 未给时用它 |
| 文件 | 文本型 | 否 |  | 只处理这一份（相对项目根的路径）；留空处理该类型全部现存文件 |
| 补头 | 逻辑型 | 否 | False | 真=允许给缺元信息头的文件补头（默认只刷已有的，不强加） |
**返回结构**：{'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '类型名': '文本型，本次刷新的文档类型名', '文件': '文本型，本次刷新的单份文件（未指定序号时为空）', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1）', '通过': '逻辑型，退出码为 0 即为真（刷新并补机器印记成功）', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}}（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 路径越界 |
| 提供者不可用 |
| 内部错误 |
**调用示例**：
```python
项目文档.写文档生成区(项目根=值, 类型名=值, 序号=值, 文件=值, 补头=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）
## 项目文档.人工改文档（人工改文档）
**说明**：人工改正文的唯一落盘通道（等价 `--人工改 --文件 <目标> --正文文件 <草稿>`）：生成器负责「类型判据校验 → 生成区保护 → 加机器印记 → 落盘」四步。缺 写盘=真 时只干跑并报结论。**草稿改动了生成区即被拒绝**（fail-closed），输出行里指明该生成区归谁管、怎么恢复。是否落盘 = 声明要写 且 退出码为 0，被拒绝时绝不报已落盘。
**参数表**：
| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 项目根 | 文本型 | 否 |  | 目标项目根的绝对路径；留空自举 |
| 目标文件 | 文本型 | 是 | None | 要改的目标（相对项目根的路径，须落在某类文档的路径判据下） |
| 草稿文件 | 文本型 | 是 | None | 人先写好的草稿（相对项目根或绝对路径）；人从不直接写目标文件 |
| 写盘 | 逻辑型 | 否 | False | 真=落盘；假=只干跑校验并报结论 |
**返回结构**：{'类型': '结果型', '值结构': {'项目根': '文本型，项目根绝对路径', '目标文件': '文本型，被修改的目标文档绝对路径', '草稿文件': '文本型，提供正文的草稿文件绝对路径', '是否落盘': '逻辑型，是否真写且退出码为 0（干跑时为假）', '命令': '文本型，实际执行的生成器命令行', '退出码': '整数型，生成器进程退出码（未跑起来为 -1）', '通过': '逻辑型，退出码为 0 即为真', '输出行': '列表型，生成器标准输出按行拆分（去尾空行）', '错误输出': '文本型，生成器标准错误输出（无则空）'}}（统一结果：成功/值/错误）
**错误码表**：
| 错误码 |
| --- |
| 参数不合法 |
| 路径越界 |
| 提供者不可用 |
| 内部错误 |
**调用示例**：
```python
项目文档.人工改文档(项目根=值, 目标文件=值, 草稿文件=值, 写盘=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。
**验证状态**：已纳入验证场景：验证场景/正向写查链场景.json（未登记场景id）、验证场景/拒收与越界场景.json（未登记场景id）、验证场景/文档生成器动作场景.json（未登记场景id）
