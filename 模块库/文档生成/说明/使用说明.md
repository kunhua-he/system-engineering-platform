# 文档生成 使用说明

- 包id：模块库.文档生成
- 版本：1.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 办公文档支持库.文档生成.生成文档 | 生成文档 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': {'格式': '文本型', '字节b64': '文本型', '媒体类型': '文本型', '摘要': '文本型', '诊断': '列表型', '附加': '字典型'}}} | 按格式生成文档（docx/xlsx/pptx/pdf，大小写与点前缀自动归一化），返回统一 结果[生成产物]（字节/媒体类型/摘要/诊断） |

## 办公文档支持库.文档生成.生成文档（生成文档）

**说明**：按格式生成文档（docx/xlsx/pptx/pdf，大小写与点前缀自动归一化），返回统一 结果[生成产物]（字节/媒体类型/摘要/诊断）

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 格式 | 文本型 | 是 | None | docx/xlsx/pptx/pdf |
| 内容参数 | 字典型 | 是 | None | 与支持库对应生成能力一致的结构化生成参数 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': {'格式': '文本型', '字节b64': '文本型', '媒体类型': '文本型', '摘要': '文本型', '诊断': '列表型', '附加': '字典型'}}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 参数不合法 |
| 提供者不可用 |
| 生成失败 |

**调用示例**：

```python
办公文档支持库.文档生成.生成文档(格式=值, 内容参数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
