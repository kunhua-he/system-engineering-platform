<!-- 本文件由 说明书生成器 生成（唯一事实源：包声明 + 能力定义 + 聚合契约），请勿手改 -->

# 文档生成（模块库.文档生成）

- 类型：功能模块
- 版本：1.0.0
- 说明：组合文档生成支持库：按格式（docx/xlsx/pptx/pdf）生成文档，返回统一 生成产物（字节/媒体类型/摘要/诊断）。
- 入口：__init__.py

## 依赖能力
- 能力 `办公文档支持库.文档生成.生成DOCX` 版本约束 `>=1.0.0`
- 能力 `办公文档支持库.文档生成.生成XLSX` 版本约束 `>=1.0.0`
- 能力 `办公文档支持库.文档生成.生成PPTX` 版本约束 `>=1.0.0`
- 能力 `办公文档支持库.文档生成.生成PDF` 版本约束 `>=1.0.0`

## 能力
### 生成文档（`办公文档支持库.文档生成.生成文档`）
- 版本：1.0.0
- 一句话说明：按格式生成文档（docx/xlsx/pptx/pdf），返回 结果[生成产物]
- 所属支持库或模块：模块库.文档生成
- 参数列表：格式:文本型, 内容参数:字典型
- 必填参数：格式, 内容参数
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 提供者不可用, 生成失败
- 调用示例：办公文档支持库.文档生成.生成文档()
- 依赖能力：办公文档支持库.文档生成.生成DOCX, 办公文档支持库.文档生成.生成XLSX, 办公文档支持库.文档生成.生成PPTX, 办公文档支持库.文档生成.生成PDF
