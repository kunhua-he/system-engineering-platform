"""文件管理模块包级中文入口。

对外能力经项目适配层装配的 HTTP连接器调用统一网关，
不直接导入支持库/提供者实现。
"""

from __future__ import annotations

from 模块库.文件管理.实现.文件管理 import 读取文件, 设置HTTP连接器
from 模块库.文件管理.实现.文件管理 import 写入文件
from 模块库.文件管理.实现.文件管理 import 复制文件
from 模块库.文件管理.实现.文件管理 import 移动文件
from 模块库.文件管理.实现.文件管理 import 删除文件
from 模块库.文件管理.实现.文件管理 import 列出文件
from 模块库.文件管理.实现.文件管理 import 读取二进制文件
from 模块库.文件管理.实现.文件管理 import 读取文件头部字节
from 模块库.文件管理.实现.文件管理 import 流式创建摘要

__all__ = [
    "设置HTTP连接器",
    "读取文件",
    "写入文件",
    "复制文件",
    "移动文件",
    "删除文件",
    "列出文件",
    "读取二进制文件",
    "读取文件头部字节",
    "流式创建摘要",
]



def _校验参数名序(参数契约表: dict[str, list[dict]],
                  注册参数名序: dict[str, list[str]]) -> None:
    """装配期闸门：注册参数名序必须逐字等于契约名序，否则本包装配失败。

    为什么要有它：注册处一旦漏一个参数（或顺序写反），网关的必填校验就与契约分叉
    ——那正是本包 2026-09-18 被修掉的缺陷形态。宁可装配期就 raise，也不静默漏参。
    """
    for 能力id, 契约参数 in 参数契约表.items():
        注册名序 = 注册参数名序.get(能力id)
        if 注册名序 is None:
            raise ValueError(f"能力 {能力id} 有契约但注册处未声明参数名序")
        契约名序 = [参数.get("名称") for 参数 in 契约参数]
        if 契约名序 != list(注册名序):
            raise ValueError(
                f"能力 {能力id} 注册参数名序 {list(注册名序)} 与契约参数名序 {契约名序} "
                "不一致：参数事实源只有 能力契约/参数契约.json 一处（哲学 12.1），"
                "入口不得另立一份")

def 注册能力(注册表) -> None:
    """由模块加载器调用，向能力注册表注册本模块全部能力。"""
# ===== 参数声明透传（契约整条镜像；本块由「丢参」缺陷修复注入，勿删）=====
    # 参数契约镜像（**函数内字面量**；本块由「丢参」缺陷修复注入，勿删）
    #
    # 缺陷溯源（2026-09-18）：本入口原先手工重建注册参数声明（只写 名称/类型），把
    # `必填` 丢掉；而网关唯一校验点 `运行核心/统一网关/类型规格.py` 的判据是
    # `项.get("必填") is 真` → `None is 真` = 假 → 必填校验静默失效：少传参数不再被
    # 网关拦下，直接落进实现体抛 TypeError，用户拿到 HTTP 500 而不是干净的 400
    # 「缺少必填参数 X」。修法：参数声明从 `能力契约/参数契约.json` **整条透传**
    # （`参数=_参数契约表[能力id]`），入口不再另立第二套事实源（哲学 12.1）。
    #
    # 为什么是**函数内字面量**而不是「运行时读 JSON」：
    #  ① `_参数契约表` 是字面量、键是常量，`开发工具/契约编译/漂移检测` 的 AST 读取器
    #     （`_静态字面量` 的 `ast.Subscript` 分支）能解析出**整条参数含 `必填`** ——
    #     改前那处「参数口径未解析」不因本次修复恶化，反而把读不到的 `必填` 读到了。
    #     放在函数内是因为该读取器只收 `注册能力` 函数体内的赋值（`_扫描注册语句`）。
    #  ② 「裸布尔」防回潮门禁是全文件 AST 扫描，但只对三类判违规（`Return` 裸布尔 / 信封键 `成功`·`可重试` / `结果(裸布尔)`）；`必填` **不在**违规集里 —— 即字面量里的 `必填: True` 「看得见但不计违规」（实测命中 0 条）。
    # 表内 True/False 是**契约 JSON 的原值镜像**（JSON 无 真/假 字面量；`公共契约/基础类型/
    # 逻辑类型.py` 自己就写着 `真 = True`，JSON 契约侧同理），键集与契约逐字对应
    # （契约写了的键才留，没写的不补），不给漂移检测造出「注册声明了契约没声明的默认值」。
    # 为什么按下标取而不是包一层 `_参数声明(能力id)`：包一层会让 AST 一律判「不可静态
    # 判定」，等于拿门禁覆盖率换干净（不可接受）。名序护盾由 `_注册参数名序` +
    # `_校验参数名序()` 承担，装配期逐条对契约核名序，不一致即 raise。
    _参数契约表 = {
        "文件管理.写入文件": [
            {"名称": "文件路径", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "内容", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.列出文件": [
            {"名称": "目录路径", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.删除文件": [
            {"名称": "文件路径", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.复制文件": [
            {"名称": "源路径", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "目标路径", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.流式创建摘要": [
            {"名称": "文件路径", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.移动文件": [
            {"名称": "源路径", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "目标路径", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.读取二进制文件": [
            {"名称": "受控根目录", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "相对路径", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "最大字节数", "类型": "整数型", "必填": False, "默认值": 0},
        ],
        "文件管理.读取文件": [
            {"名称": "文件路径", "类型": "文本型", "必填": True, "默认值": None},
        ],
        "文件管理.读取文件头部字节": [
            {"名称": "受控根目录", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "相对路径", "类型": "文本型", "必填": True, "默认值": None},
            {"名称": "字节数", "类型": "整数型", "必填": False, "默认值": 4096},
        ],
    }
    _注册参数名序 = {
        "文件管理.写入文件": ["文件路径", "内容"],
        "文件管理.列出文件": ["目录路径"],
        "文件管理.删除文件": ["文件路径"],
        "文件管理.复制文件": ["源路径", "目标路径"],
        "文件管理.流式创建摘要": ["文件路径"],
        "文件管理.移动文件": ["源路径", "目标路径"],
        "文件管理.读取二进制文件": ["受控根目录", "相对路径", "最大字节数"],
        "文件管理.读取文件": ["文件路径"],
        "文件管理.读取文件头部字节": ["受控根目录", "相对路径", "字节数"],
    }
    _校验参数名序(_参数契约表, _注册参数名序)
# ===== 参数声明透传 结束 =====
    from 公共契约.能力契约.契约 import 能力实现

    for 能力id, 函数, 返回类型, 说明 in [
        ("文件管理.读取文件", 读取文件,  "结果", "读取文本文件（默认 utf-8），返回 结果[文本]"),
        ("文件管理.写入文件", 写入文件,  "结果", "写入文本文件（默认 utf-8，自动创建父目录）"),
        ("文件管理.复制文件", 复制文件,  "结果", "复制文件到目标路径（自动创建父目录）"),
        ("文件管理.移动文件", 移动文件,  "结果", "移动文件到目标路径（自动创建父目录）"),
        ("文件管理.删除文件", 删除文件,  "结果", "删除文件（不存在视为成功，幂等）"),
        ("文件管理.列出文件", 列出文件,  "结果", "列出目录下条目名称（排序）"),
        ("文件管理.读取二进制文件", 读取二进制文件,  "结果", "受控根内读取二进制文件（路径越界拒绝、超限拒绝）"),
        ("文件管理.读取文件头部字节", 读取文件头部字节,  "结果", "受控根内只读文件头部 N 字节"),
        ("文件管理.流式创建摘要", 流式创建摘要,  "结果", "大文件分块流式创建内容摘要（sha256）"),
    ]:
        注册表.注册(
            能力实现(
                能力id=能力id,
                包id="模块库.文件管理",
                实现函数=函数,
                参数=_参数契约表[能力id],
                返回=返回类型,
                说明=说明,
            )
        )


def _参数类型(名称: str) -> str:
    """注册参数类型与聚合契约对称：最大字节数/字节数 为整数，其余为文本。"""
    return "整数" if 名称 in ("最大字节数", "字节数") else "文本"
