# 文件管理 · 目录树与命令

> 包id：`模块库.文件管理`；版本：`2.0.2`；类型：`基础模块`
> 口径：本目录树是**关键文件清单**，不是全量清单；全量文件与指纹以同包 `完整性摘要.json` 为准。
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


## 目录树

```text
文件管理/
├── __init__.py
├── 依赖契约/依赖契约.json
├── 包声明.json
├── 复用决策.json
├── 完整性摘要.json
├── 实现/文件管理.py
├── 权限契约/权限契约.json
├── 能力契约/参数契约.json
├── 能力数据/Agent查询数据.json
├── 能力数据/能力搜索数据.json
├── 说明/使用说明.md
├── 资源预算.json
├── 配置契约/配置契约.json
├── 验证场景引用.json
```

## 公开命令

### `文件管理.读取文件`（读取文件内容）

- 名称：读取文件
- 返回：`结果型`
- 说明：读取文本文件（默认 utf-8），返回 结果[文本]
- 参数：
  - `文件路径`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.写入文件`（写入文件并创建目录）

- 名称：写入文件
- 返回：`结果型`
- 说明：写入文本文件（默认 utf-8，自动创建父目录）
- 参数：
  - `文件路径`；文本型
  - `内容`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.复制文件`（复制文件并创建目录）

- 名称：复制文件
- 返回：`结果型`
- 说明：复制文件到目标路径（自动创建父目录）
- 参数：
  - `源路径`；文本型
  - `目标路径`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.移动文件`（移动文件并创建目录）

- 名称：移动文件
- 返回：`结果型`
- 说明：移动文件到目标路径（自动创建父目录）
- 参数：
  - `源路径`；文本型
  - `目标路径`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.删除文件`（幂等删除文件）

- 名称：删除文件
- 返回：`结果型`
- 说明：删除文件（不存在视为成功，幂等）
- 参数：
  - `文件路径`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.列出文件`

- 名称：列出文件
- 返回：`结果型`
- 说明：列出目录下条目名称（排序），返回 结果[名称列表]
- 参数：
  - `目录路径`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.读取二进制文件`（受控读取二进制内容）

- 名称：读取二进制文件
- 返回：`结果型`
- 说明：受控根内读取二进制文件（路径越界拒绝、超限拒绝）
- 参数：
  - `受控根目录`；文本型
  - `相对路径`；文本型
  - `最大字节数`；整数型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.读取文件头部字节`（受控读取文件头部）

- 名称：读取文件头部字节
- 返回：`结果型`
- 说明：受控根内只读文件头部 N 字节
- 参数：
  - `受控根目录`；文本型
  - `相对路径`；文本型
  - `字节数`；整数型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

### `文件管理.流式创建摘要`

- 名称：流式创建摘要
- 返回：`结果型`
- 说明：大文件分块流式创建内容摘要（sha256），返回 结果[摘要文本]
- 参数：
  - `文件路径`；文本型
- 调用入口：`__init__.py`（只能经包级入口调用）
- 返回外壳：成功返回 `成功=true, 值=实际值`；失败返回 `成功=false, 错误码, 错误说明`。

## 规范边界

本文件只描述当前包的实际目录和公开命令；实现细节、错误码全集、资源预算和状态机以同目录 `包声明.json`、`能力契约/参数契约.json`、`资源预算.json` 和验证场景为准。目录或命令变化后必须重新生成本文件。
