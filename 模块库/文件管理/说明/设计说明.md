# 文件管理 设计说明（设计权威）

> 本文是该库的**设计哲学与边界权威**：它为什么这样切、管什么、不管什么、失败如何表达。
> 调用口径——能力、参数、返回结构、错误码与调用示例——以同目录 `使用说明.md` 为准（由能力契约生成）。
> 两份说明都计入包指纹，任何改动都要升包版本。

- 包id：模块库.文件管理
- 版本：2.0.0

## 能力清单

| 能力id | 名称 | 版本 | 返回 | 说明 |
| --- | --- | --- | --- | --- |
| 文件管理.读取文件 | 读取文件内容 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '文本型'}} | 读取文本文件（默认 utf-8），返回 结果[文本] |
| 文件管理.写入文件 | 写入文件并创建目录 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}} | 写入文本文件（默认 utf-8，自动创建父目录），返回 结果 |
| 文件管理.复制文件 | 复制文件并创建目录 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}} | 复制文件到目标路径（自动创建父目录），返回 结果 |
| 文件管理.移动文件 | 移动文件并创建目录 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}} | 移动文件到目标路径（自动创建父目录），返回 结果 |
| 文件管理.删除文件 | 幂等删除文件 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}} | 删除文件（不存在视为成功，幂等），返回 结果 |
| 文件管理.列出文件 | 列出文件 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '列表型'}} | 列出目录下条目名称（排序），返回 结果[名称列表] |
| 文件管理.读取二进制文件 | 受控读取二进制内容 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '字节'}} | 在受控根目录内按相对路径读取二进制文件（路径越界拒绝、超限拒绝），返回 结果[字节] |
| 文件管理.读取文件头部字节 | 受控读取文件头部 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '字节'}} | 在受控根目录内按相对路径只读文件头部 N 字节（不加载全文件），返回 结果[字节] |
| 文件管理.流式创建摘要 | 流式创建摘要 | 1.0.0 | {'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '文本型'}} | 大文件分块流式创建内容摘要（sha256），返回 结果[摘要文本] |

## 文件管理.读取文件（读取文件内容）

**说明**：读取文本文件（默认 utf-8），返回 结果[文本]

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 待读取文件绝对路径 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 文件读取失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.读取文件(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.写入文件（写入文件并创建目录）

**说明**：写入文本文件（默认 utf-8，自动创建父目录），返回 结果

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 目标文件绝对路径 |
| 内容 | 文本型 | 是 | None | 待写入文本内容 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不可写 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.写入文件(文件路径=值, 内容=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.复制文件（复制文件并创建目录）

**说明**：复制文件到目标路径（自动创建父目录），返回 结果

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源文件绝对路径 |
| 目标路径 | 文本型 | 是 | None | 目标文件绝对路径 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 文件复制失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.复制文件(源路径=值, 目标路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.移动文件（移动文件并创建目录）

**说明**：移动文件到目标路径（自动创建父目录），返回 结果

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 源路径 | 文本型 | 是 | None | 源文件绝对路径 |
| 目标路径 | 文本型 | 是 | None | 目标文件绝对路径 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 文件移动失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.移动文件(源路径=值, 目标路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.删除文件（幂等删除文件）

**说明**：删除文件（不存在视为成功，幂等），返回 结果

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 待删除文件绝对路径 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '空'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件删除失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.删除文件(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.列出文件（列出文件）

**说明**：列出目录下条目名称（排序），返回 结果[名称列表]

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 目录路径 | 文本型 | 是 | None | 待列出目录绝对路径 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '列表型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 目录不存在 |
| 目录读取失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.列出文件(目录路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.读取二进制文件（受控读取二进制内容）

**说明**：在受控根目录内按相对路径读取二进制文件（路径越界拒绝、超限拒绝），返回 结果[字节]

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 受控根目录 | 文本型 | 是 | None | 受控根目录绝对路径 |
| 相对路径 | 文本型 | 是 | None | 根目录内相对路径（不得越界） |
| 最大字节数 | 整数型 | 否 | 0 | 超过该字节数拒绝读取；0 表示不限制 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '字节'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 路径越界 |
| 文件超限 |
| 文件不存在 |
| 目录不存在 |
| 文件读取失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.读取二进制文件(受控根目录=值, 相对路径=值, 最大字节数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.读取文件头部字节（受控读取文件头部）

**说明**：在受控根目录内按相对路径只读文件头部 N 字节（不加载全文件），返回 结果[字节]

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 受控根目录 | 文本型 | 是 | None | 受控根目录绝对路径 |
| 相对路径 | 文本型 | 是 | None | 根目录内相对路径（不得越界） |
| 字节数 | 整数型 | 否 | 4096 | 只读头部字节数 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '字节'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 路径越界 |
| 文件不存在 |
| 目录不存在 |
| 文件读取失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.读取文件头部字节(受控根目录=值, 相对路径=值, 字节数=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）

## 文件管理.流式创建摘要（流式创建摘要）

**说明**：大文件分块流式创建内容摘要（sha256），返回 结果[摘要文本]

**参数表**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| 文件路径 | 文本型 | 是 | None | 待摘要文件绝对路径 |

**返回结构**：{'类型': '结果型', '值结构': {'成功': '逻辑型', '值': '文本型'}}（统一结果：成功/值/错误）

**错误码表**：

| 错误码 |
| --- |
| 文件不存在 |
| 摘要失败 |
| 提供者不可用 |

**调用示例**：

```python
文件管理.流式创建摘要(文件路径=值)
```
> 示例为骨架需验证：能力搜索数据无现成示例，按参数自动生成。

**验证状态**：已纳入验证场景：支持库.资产验证（资产）、支持库.能力契约验证（契约）
