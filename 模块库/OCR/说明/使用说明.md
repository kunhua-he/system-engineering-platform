# OCR（模块库.OCR）
<!-- 机器管理｜类型：包级说明-使用说明｜生成器：开发工具.MD文档生成 -->


- 类型：基础模块
- 版本：1.0.2
- 说明：基础模块：组合 Tesseract 支持库公开能力（识别图片文字/识别图片文件/可用性检查），只经 获取能力调用器().调用能力 调用，取消令牌id 文本语义（支持库侧不传 callable）。
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 依赖能力
- 能力 `OCR识别支持库.OCR识别.识别图片` 版本约束 `>=1.0.0`
- 能力 `OCR识别支持库.OCR识别.语言包列表` 版本约束 `>=1.0.0`
- 能力 `OCR识别支持库.OCR识别.版本探针` 版本约束 `>=1.0.0`

## 能力
### 识别图片文字（`OCR.识别图片文字`）
- 版本：1.0.0
- 一句话说明：识别图片文字：图片路径与图片字节二选一，返回 {文本} 或 词级数据 {词列表}
- 所属支持库或模块：模块库.OCR
- 参数列表：图片路径:文本型, 图片字节:字节集型, 语言:文本型, 词级数据:逻辑型, 超时秒:双精度数型, 取消令牌id:文本型, 输出上限字节:整数型, 命令路径:文本型
- 必填参数：无
- 可选参数：图片路径, 图片字节, 语言, 词级数据, 超时秒, 取消令牌id, 输出上限字节, 命令路径
- 返回结构：{'类型': '结果型', '值结构': {'文本': '识别出的文本（词级数据为 false 时）', '词列表': '词级数据列表（词级数据为 true 时，每项含 文本/置信度/坐标）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 工具缺失, 语言包缺失, 识别失败, 进程崩溃, 超时, 取消, 超出限制, 命令失败, 版本不兼容, 提供者不可用
- 调用示例：OCR.识别图片文字(语言=eng, 词级数据=False, 超时秒=60.0, 取消令牌id=, 命令路径=)
- 依赖能力：OCR识别支持库.OCR识别.识别图片, OCR识别支持库.OCR识别.语言包列表, OCR识别支持库.OCR识别.版本探针
### 识别图片文件（`OCR.识别图片文件`）
- 版本：1.0.0
- 一句话说明：按图片文件路径识别文字，返回 {文本} 或 词级数据 {词列表}
- 所属支持库或模块：模块库.OCR
- 参数列表：图片路径:文本型, 语言:文本型, 词级数据:逻辑型, 超时秒:双精度数型, 取消令牌id:文本型, 输出上限字节:整数型, 命令路径:文本型
- 必填参数：图片路径
- 可选参数：语言, 词级数据, 超时秒, 取消令牌id, 输出上限字节, 命令路径
- 返回结构：{'类型': '结果型', '值结构': {'文本': '识别出的文本（词级数据为 false 时）', '词列表': '词级数据列表（词级数据为 true 时，每项含 文本/置信度/坐标）'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 文件不存在, 工具缺失, 语言包缺失, 识别失败, 进程崩溃, 超时, 取消, 超出限制, 命令失败, 版本不兼容, 提供者不可用
- 调用示例：OCR.识别图片文件(语言=eng, 词级数据=False, 超时秒=60.0, 取消令牌id=, 命令路径=)
- 依赖能力：OCR识别支持库.OCR识别.识别图片, OCR识别支持库.OCR识别.语言包列表, OCR识别支持库.OCR识别.版本探针
### 可用性检查（`OCR.可用性检查`）
- 版本：1.0.0
- 一句话说明：探测 tesseract 版本与语言包：返回 版本/满足最低版本/语言列表/数据目录/语言数量
- 所属支持库或模块：模块库.OCR
- 参数列表：超时秒:双精度数型, 命令路径:文本型
- 必填参数：无
- 可选参数：超时秒, 命令路径
- 返回结构：{'类型': '结果型', '值结构': {'tesseract': 'tesseract 命令名或路径', '版本': '探测到的版本号', '满足最低版本': '是否满足最低版本（布尔）', '语言列表': '已安装语言包列表', '数据目录': 'tesseract 数据目录', '语言数量': '语言包数量'}}（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 工具缺失, 版本不兼容, 命令失败, 进程崩溃, 超时, 取消, 超出限制, 提供者不可用
- 调用示例：OCR.可用性检查(超时秒=10.0, 命令路径=)
- 依赖能力：OCR识别支持库.OCR识别.识别图片, OCR识别支持库.OCR识别.语言包列表, OCR识别支持库.OCR识别.版本探针
