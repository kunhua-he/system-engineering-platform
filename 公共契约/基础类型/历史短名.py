"""历史短名 → 正式类型名的**唯一**归一表（全仓只有这一份）。

**为什么独立成文件**（2026-09-24 批R R-1，华哥裁决「类型相似的合并成 1 类」）：
同一判据此前有 **3 份表**，条目互不相同（覆盖宽度不同、同键冲突 0 处）：

1. `运行核心/统一网关/协议/类型规格.py`（原 `类型短名映射`，18 键，网关边界归一；已删，转调本表）；
2. `开发工具/契约编译/能力定义编译器.py`（原 `易语言类型别名`，10 键，契约编译归一；已删，转调本表）；
3. `开发工具/快速编译/运行快速编译.py`（原 `旧类型`，11 键，声明面判红；已删，转调本表）。

后果是**静默分叉已经成立**：同一个 `能力定义.json` 里写 `数字` / `空`，经 ③ 认得、
经 ① 不归一（原样透传）⇒ 同一份声明在两端得到不同结论。

**并集 20 键**（现场实测，同键冲突 0 处）= ① 18 键 ∪ {`数字`→双精度数型、`空`→空值型}。
三处（网关边界 / 能力定义编译器 / 运行快速编译）与 `开发工具/契约编译/漂移判定.py`
**一律转调本表**；任何地方不得再写第二份短名表（哲学 1.2：同一件事两套实现即缺陷）。

**设计锚（本表不是新机制）**：`开发文档/方案/裸布尔迁移分批方案_20260917.md:217` 与
`开发文档/方案/类型体系收口盘点_20260917.md:183`（TY-4）早已指向本文件。

**分工不重叠**：`类型表.py::正式类型表`（16 项）定义「平台有哪些类型」；本表只回答
「历史短名该归到哪个正式名」，是它之前的一层归一。

**只减不增**：新增短名键等于放宽历史包袱，须先有裁决。
"""

from __future__ import annotations

#: 历史短名 → 正式类型名。键集 = 原三份表的并集（20 键），值全部落在 `正式类型表` 内。
历史短名到正式名: dict[str, str] = {
    "文本": "文本型", "整数": "整数型", "长整数": "长整数型",
    "单精度数": "单精度数型", "双精度数": "双精度数型",
    "浮点数": "双精度数型", "逻辑": "逻辑型", "布尔": "逻辑型",
    "列表": "列表型", "字典": "字典型", "映射": "字典型",
    "字节": "字节型", "字节集": "字节集型", "日期时间": "日期时间型",
    "资源引用": "资源引用型", "句柄": "句柄型", "结果": "结果型",
    "空值": "空值型", "数字": "双精度数型", "空": "空值型",
}
