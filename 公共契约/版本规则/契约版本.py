"""平台契约版本（华哥 2026-09-14 裁决，哲学第 5 条 1 项）。

契约只有一套：全平台「契约版本」（聚合契约 S0 格式版本）恒等于这里的值，
事实源只有这一处，不再分散到各包里各自演进。

边界（别搞混）：
- **支持库/模块/能力各自有自己的迭代版本**（1.0.0 → 1.1.0 → …），按自身迭代展示，
  不强制与契约版本对齐；新包从 1.0.0 起。
- 带运算符的依赖约束（如 `>=1.0.0`）是**强制参数**，不满足才失败；
  单纯版本不对齐不算失败（哲学第 5 条 2 项）。
- 计数类（栅栏令牌/资源版本）、机制版本（状态结构版本/编译器版本/HTML 验证器版本）、
  环境依赖最低版本（Tesseract `>=5.0.0`）各有自己的轴，不属于契约版本。
"""

契约版本 = "2.0.0"


def 取契约版本() -> str:
    """取平台契约版本。"""
    return 契约版本
