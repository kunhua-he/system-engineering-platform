"""跨平台平台适配：平台判定 / 虚拟环境解释器相对路径 / 子进程组启动标志。

背景（华哥 2026-09-16 裁决：底座做完整跨平台）：底座此前在 **Windows 11 / AMD64** 上
完全跑不起来，根因之一是「POSIX 布局被硬编码」——POSIX 的虚拟环境解释器是
``venv/bin/python3``，Windows 上真实布局是 ``venv/Scripts/python.exe``；POSIX 用
``start_new_session=True`` 开独立进程组，Windows 用 ``creationflags=CREATE_NEW_PROCESS_GROUP``。

本模块是**全平台唯一实现**（哲学第 8 条唯一性判定②：同一件事两种语义 → 同一份实现 +
模式变量）。后续批次把 6 处硬编码 ``bin/python3`` 与全部 ``start_new_session=True``
调用点切到本模块，调用点不再自带平台分支。

**设计约束**

1. **不引入第三方**：只用标准库（``sys`` / ``subprocess`` / ``platform`` / ``pathlib`` /
   ``re``）与**同一 ``公共契约`` 根内**的 `基础类型.逻辑类型`（中文逻辑字面量 `真`/`假`，
   见《类型目录》：正式代码一律从该模块取，不散写英文 ``True``/``False``）。
2. **平台判定在「调用时」读取** ``sys.platform``，不在导入时冻结——这样测试可以用
   monkeypatch 把整个模块切到 Windows 形态验证分支选择，也为将来可能的平台探测留口。
3. **平台不支持的能力显式报错**（``平台不支持错误``），绝不静默降级成别的语义。

**诚实标注：本模块的 Windows 分支未经真机实测**（开发机为 macOS）。Windows 分支的
判定逻辑仅通过 monkeypatch 模拟 ``sys.platform == "win32"`` 做过分支选择验证，
真实 Windows 上的行为需在 Windows 11 / AMD64 机器上复核。

**诚实标注（转写后端，2026-09-19 新增）**：`转写后端()` / `转写后端库名()` 的
**Windows / Linux → ``faster-whisper``** 这一支**同样未经真机实测** —— 开发机为 macOS，
本次只做了 monkeypatch 分支选择验证（``win32`` / ``linux`` / ``darwin`` 三态）。
真实 Windows / Linux 上的「依赖可装性 + 转写可用性」必须由真机取证补：
阿里云 Linux（x86_64 / Python 3.14）与 GitHub Actions Windows runner。
**不许把「代码写好了」当成「Windows / Linux 能跑了」**——那是把文档里的跨平台
误读成已验收的跨平台（与既有 Windows 分支同一诚实口径，见 README《当前支持矩阵》）。

**诚实标注（调用点行为分叉收口，2026-09-19 新增）**：本节四个新原语
（`多进程启动上下文()` / `拆分命令文本()` / `进程内存RSS字节()` / `平台稳定缓存根()`）的
**Windows / Linux 分支同样未经真机实测**（开发机为 macOS），只做了 monkeypatch 分支
选择验证（见 `测试中心/运行核心/测试_跨平台收口原语.py`）。它们的**POSIX / macOS 分支
是本机真跑**（fork 上下文、shlex posix 语义、`ps -o rss=`、`~/Library/Caches` 布局）。
真机取证仍待 GitHub Actions Windows runner 与阿里云 Linux。
"""

from __future__ import annotations

# ── 命名空间补齐（2026-09-23 拆成同名包）─────────────────────────────────────
# 下面这些名字在**拆前**就长在 `公共契约.运行时.平台适配` 模块对象上（stdlib 绑定与
# 正式类型名/函数）。拆成包后若不显式补回，`平台适配.platform` / `平台适配.Path` 这类
# 取用就会失联 —— 属真实的行为变化，不是风格问题。
# 判据（与 `开发工具/环境自检/__init__.py` 同一处置）：拆前/拆后 `dir(模块)` 逐名相等，
# 且 `平台适配.<名> is 子模块.<名>`（同一对象，不是副本）。
# 实测依赖面：`测试中心/支持库/测试_转写跨平台后端.py` 有 15 处
# `mock.patch.object(平台适配.platform, "machine", …)`（`platform` 是模块单例，
# 打桩对子模块同样生效）；`平台适配.Path` / `平台适配.Mapping` / `平台适配.Any`
# 是既有公开面（型检与调用点都在用）。
import ctypes
import os
import platform
import re
import shlex
import shutil
import socket
import stat
import subprocess
import sys
from collections.abc import Mapping
from pathlib import Path
from typing import Any

from 公共契约.基础类型.逻辑类型 import 真, 假
from 公共契约.诊断.忽略记录 import 记录忽略


from 公共契约.运行时.平台适配.判定 import (
    平台表,
    平台不支持错误,
    原始平台标志,
    本机系统名,
    当前平台标识,
    当前平台,
    是Windows,
    是macOS,
    是Linux,
    是POSIX,
    支持chmod,
)
from 公共契约.运行时.平台适配.采样基元 import (
    内存采样超时秒,
    图形采样超时秒,
    采样命令输出上限字节,
    伪文件读取上限字节,
    sysctl候选路径,
    vm_stat候选路径,
    system_profiler候选路径,
    Linux内存信息路径,
    Linux处理器信息路径,
    _执行只读采样命令,
    _读伪文件,
)
from 公共契约.运行时.平台适配.准入 import (
    正式支持矩阵,
    当前架构,
    校验支持范围,
    脚本入口准入,
)
from 公共契约.运行时.平台适配.硬件画像 import (
    _MACH_HOST_VM_INFO64,
    _libSystem路径,
    _虚拟机统计64,
    _macOS内存容量_系统调用,
    _macOS可用内存字节,
    _Linux内存容量,
    内存容量信息,
    物理核数信息,
    处理器型号信息,
    图形加速信息,
    AppleSilicon架构表,
    是否AppleSilicon,
)
from 公共契约.运行时.平台适配.转写后端选择 import (
    转写后端标识表,
    转写后端库名表,
    转写后端标识_MLX,
    转写后端标识_FASTER_WHISPER,
    转写后端,
    转写后端库名,
)
from 公共契约.运行时.平台适配.身份对账 import (
    MCP识别符号,
    对账MCP身份,
    修MCP白名单变量名,
    MCP身份准入,
    脚本入口准入MCP,
)
from 公共契约.运行时.平台适配.要求POSIX import (
    要求POSIX能力,
)
from 公共契约.运行时.平台适配.解释器 import (
    POSIX解释器相对路径,
    Windows解释器相对路径,
    虚拟环境解释器相对路径,
    虚拟环境解释器路径,
)
from 公共契约.运行时.平台适配.资源口径 import (
    POSIX句柄目录表,
    Windows新建进程组标志,
    内存峰值原始单位,
    句柄枚举目录表,
    子进程组启动标志,
)
from 公共契约.运行时.平台适配.非阻塞读 import (
    非阻塞读上限字节,
    非阻塞真读一次,
)
from 公共契约.运行时.平台适配.删除树 import (
    清只读删除留痕前缀,
    清除只读属性,
    _确保目录可写,
    确保可删,
    移动并可删,
    清只读并确保可删,
    清只读后删除树,
)
from 公共契约.运行时.平台适配.进程分叉 import (
    ps候选路径,
    Linux进程目录,
    缺省缓存应用名,
    多进程启动上下文,
    _剥成对引号,
    拆分命令文本,
    经shell命令表,
    _macOS进程RSS字节,
    _Linux进程RSS字节,
    进程内存RSS字节,
    平台稳定缓存根,
)
from 公共契约.运行时.平台适配.路径解析 import (
    项目根环境变量名,
    解析路径,
)
from 公共契约.运行时.平台适配.系统位置 import (
    POSIX系统位置表,
    Windows系统位置表,
    POSIX家目录敏感位置表,
    Windows家目录敏感位置表,
    系统位置表,
    家目录敏感位置表,
)


__all__ = [
    "平台表",
    "POSIX解释器相对路径",
    "Windows解释器相对路径",
    "POSIX句柄目录表",
    "Windows新建进程组标志",
    "平台不支持错误",
    "原始平台标志",
    "本机系统名",
    "当前平台标识",
    "当前平台",
    "支持chmod",
    "非阻塞真读一次",
    "非阻塞读上限字节",
    "系统位置表",
    "POSIX系统位置表",
    "Windows系统位置表",
    "家目录敏感位置表",
    "POSIX家目录敏感位置表",
    "Windows家目录敏感位置表",
    "是Windows",
    "是macOS",
    "是Linux",
    "是POSIX",
    "虚拟环境解释器相对路径",
    "虚拟环境解释器路径",
    "内存峰值原始单位",
    "句柄枚举目录表",
    "子进程组启动标志",
    "要求POSIX能力",
    "正式支持矩阵",
    "AppleSilicon架构表",
    "当前架构",
    "校验支持范围",
    "脚本入口准入",
    # 2026-09-23 身份锁落盘（本轮①②③的②）：`修MCP白名单变量名` / `MCP身份准入` 是
    # 仓库根 `sitecustomize.py` 与两条写入腿（`文件系统支持库` 跨层调用）的**新公开原语**，
    # 未列入 `__all__` 时型检会报 `reportAttributeAccessIssue`（运行时可用，但公开面
    # 自述与实际不符）—— 与既有各次补列同一处置：新原语必须同时进公开面。
    "修MCP白名单变量名",
    "MCP身份准入",
    # 2026-09-18 H 簇补列：硬件画像采样的四个取法与 Apple Silicon 判定是**新公开原语**
    # （`支持库/适配层/硬件画像探针.py` 跨层调用它们），未列入 `__all__` 时型检会报
    # `reportAttributeAccessIssue`（运行时可用，但公开面自述与实际不符）。
    # 与 `进程终止.py` 补 `__all__` 同一处置：新原语必须同时进公开面。
    "内存容量信息",
    "物理核数信息",
    "处理器型号信息",
    "图形加速信息",
    "是否AppleSilicon",
    # 2026-09-19 转写后端选择（跨平台双后端）：`转写后端`/`转写后端库名` 是两处转写
    # 提供者的**新公开原语**（`支持库/适配层/MLXWhisper提供者` 与
    # `支持库/后端/转写支持库/转写` 的实现跨层调用它们），未列入 `__all__` 时型检会报
    # `reportAttributeAccessIssue`（运行时可用，但公开面自述与实际不符）——
    # 与 2026-09-18 硬件画像四原语补列同一处置：新原语必须同时进公开面。
    "转写后端标识表",
    "转写后端库名表",
    "转写后端标识_MLX",
    "转写后端标识_FASTER_WHISPER",
    "转写后端",
    "转写后端库名",
    # 2026-09-19 调用点行为分叉收口：`任务进程.py` / `进程管理.py` / `容量基线.py` /
    # `运行缓存.py` 四处调用点改调这四个新原语（跨层调用），同一处置 —— 必须进公开面。
    "多进程启动上下文",
    "拆分命令文本",
    # 2026-09-23 补 shell 形态：`经shell命令表` 是「一条命令串包成解释器 argv」的
    # **全平台唯一实现**（`进程管理.启动进程/执行命令` 的 `经shell=真` 分支跨层调用它），
    # 未列入 `__all__` 时型检会报 `reportAttributeAccessIssue` —— 与既有补列同一处置。
    "经shell命令表",
    "进程内存RSS字节",
    "平台稳定缓存根",
    "ps候选路径",
    "缺省缓存应用名",
    # 2026-09-19 H 路「含只读属性的目录树删除」收口：本模块是这三个原语的**唯一实现**
    # （`远程镜像` 改薄委托、全仓调用点改调它们）。未列入 `__all__` 时型检会报
    # `reportAttributeAccessIssue`（运行时可用，但公开面自述与实际不符）——
    # 与既有补列同一处置：新原语必须同时进公开面。
    "清只读删除留痕前缀",
    "清除只读属性",
    "确保可删",
    "清只读并确保可删",
    "清只读后删除树",
    "移动并可删",
    # 2026-09-23 路径解析收口：`解析路径` 是「相对路径按根解析」的**全平台唯一实现**，
    # `项目根环境变量名` 是它的默认根配置项。网关（跨层调用）据它做派发前的入参归一，
    # 未列入 `__all__` 时型检会报 `reportAttributeAccessIssue` —— 与既有补列同一处置。
    "项目根环境变量名",
    "解析路径",
    # 2026-09-23 身份锁（本轮①②③的②）：`脚本入口准入MCP` 是「要求经 MCP」的入口准入门，
    # `开发工具/开发编译口/编译口.py` 与 `生成物联动.py` **跨层调用**它（模块级、先于装配导入）。
    # 未列入 `__all__` 时型检会报 `reportAttributeAccessIssue`（运行时可用，但公开面自述
    # 与实际不符）—— 与既有补列同一处置（2026-09-23 复核实测：编译口接了它却不在公开面）。
    "脚本入口准入MCP",
]
