"""操作协议表：允许的操作白名单 + 每个操作所需权限（对外协议口径）。

2026-09-19 从 `网关核心.py` 按职责拆出。原处再导出，调用方零改动。
"""

from __future__ import annotations


允许操作表 = {
    "健康检查", "能力目录", "能力搜索", "包详情", "能力详情", "调用能力",
    "资源状态", "资源续租", "资源关闭", "任务提交",
    "任务查询", "任务取消",
    "热接入",
}



操作权限表 = {
    "健康检查": "查询", "能力目录": "查询", "能力搜索": "查询",
    "包详情": "查询", "能力详情": "查询", "资源状态": "查询",
    "资源续租": "调用", "资源关闭": "调用",
    "调用能力": "调用", "任务提交": "任务", "任务查询": "任务",
    "任务取消": "任务", "热接入": "调用",
}


#: 能力级权限档位（只增不改）。缺省 = 沿用 `操作权限表["调用能力"]` = "调用"。
#:
#: 为什么要有它（未完成事项 §四「危险能力没有按能力的授权门」）：`操作权限表` 只细到
#: 「操作」⇒ `调用能力` 一个档位盖住全部 749 条能力，只读查询与「任意命令执行」同权。
#: 本表把档位下沉到**能力 id**，只给「能执行任意代码/命令」与「能提权」这两类挂新档。
#:
#: 档位口径：`管理员` 是**默认权限范围之外**的新档（见 `安全/安全边界.py` 的 `默认权限范围`）。
#: ⇒ 阶段一（默认权限范围**含**管理员）行为不变；阶段二（默认权限范围**不含**管理员）
#: 调用方必须显式声明「管理员」才能调这些能力。
#:
#: 收哪些（逐个按能力面核，不拍脑袋）：判据只有两条 —— ① 能执行**任意**代码/命令；
#: ② 能提权。故只收 `系统核心支持库.进程管理` 的启动/终止/执行命令族（argv 直启或
#: 经 shell 都能跑任意程序），加唯一提权路径 `平台控制面.授权.引导授予`。
#: **不收** 只读/清理类（查询进程状态、等待进程结束、检查命令可用、释放句柄）——
#: 它们只作用于调用方自己持有的句柄，不是任意执行，收进来只会让正常清理在阶段二被误拒。
能力权限表: dict[str, str] = {
    "系统核心支持库.进程管理.启动进程": "管理员",
    "系统核心支持库.进程管理.终止进程": "管理员",
    "系统核心支持库.进程管理.执行命令": "管理员",
    "系统核心支持库.进程管理.执行命令集": "管理员",
    "系统核心支持库.进程管理.沙箱执行命令": "管理员",
    "平台控制面.授权.引导授予": "管理员",
}
