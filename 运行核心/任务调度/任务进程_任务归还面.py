"""任务归还面：等待队列的入队/归还（有界提交的排队槽记账，混入类）。

**为什么独立成文件**：`_退出排队` 是「入队即占槽、上抛必归还」不变式的唯一归还口，
与「关闭/排空整队清空」（`_拒绝等待队列`）是同一件事的两个方向，两条腿分居两个文件
便于对照（哲学 6.1：低频但独立可切的簇）。

**对外零变化（2026-09-19 拆分）**：成员名与签名逐字保留（自冻结基线
`/tmp/拆分基线/任务进程.py`，sha256 前16 = 3227f281a4400958，对应提交 1dc0f0f6）。
宿主属性口径的唯一事实源在 `任务进程.py` `任务进程池.__init__`。

**导入方向**：本文件只被 `任务进程.py` 模块级导入，**不得反向导入** `任务进程.py`
（会成环）；需要的宿主符号全部来自无环的叶子模块 `公共契约.基础类型.逻辑类型`
与 `任务进程_对象.py`。
"""

from __future__ import annotations

from typing import Any

from 公共契约.基础类型.逻辑类型 import 真, 假
from 运行核心.任务调度.任务进程_对象 import 独立任务


class 任务归还面:
    """等待队列归还簇：把任务从等待队列还回去。

    宿主契约（类注解，真源＝`任务进程.任务进程池.__init__`）：`等待队列` / `提交条件`。
    `提交条件` 的 notify_all 与池锁同源，归还后必须唤醒等在「队列已满」分支的提交方。
    """

    # ---- 宿主契约（由 任务进程.任务进程池 提供，只声明不赋值） ----
    等待队列: list[独立任务]
    提交条件: Any

    def _退出排队(self, 任务对象: 独立任务) -> bool:
        """把任务从等待队列还回去（幂等）；**返回是否真的占用过一个排队槽**。

        入队/出队必须成对：只要调用方可能抛出，就一定要先经过这里再上抛
        （`提交` 的 `except BaseException` 是唯一兜底）。移除后 `notify_all()`：
        排队槽被归还，等在「队列已满」分支上的提交方不该白等到硬截止。
        """
        if 任务对象 not in self.等待队列:
            return 假
        self.等待队列.remove(任务对象)
        self.提交条件.notify_all()
        return 真
