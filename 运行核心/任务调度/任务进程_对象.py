"""任务对象与进程入口：状态常量、进程组号探测、独立任务载体、子进程入口（叶子模块）。

**为什么独立成文件**：这四个东西是 `任务进程.py` 里**没有池状态依赖**的最小闭环 ——
八个任务状态常量与终态集合、`信号_终止`/`信号_强杀` 语义名、`_本进程组号`（进程组号
探测，只问收口层能力）、`资源繁忙错误`（有界容量的统一错误类型）、`_还原执行器`
（spawn 送字节 / fork 送函数对象）、`_执行任务进程入口`（子进程第一栈：先 setsid
建独立进程组、再执行能力）、`独立任务`（任务状态载体）。它们被三个混入面
（`任务进程_任务归还面.py` / `任务进程_监视轮询面.py` / `任务进程_回收落盘面.py`）
共同引用，居中放在本叶子模块可让**任何方向都不成环**。

**本文件是叶子**：只依赖 `公共契约.*` 与 `任务工作器.执行单次任务`，不导入任何
`任务进程*` 模块。

**跨平台收口（华哥 2026-09-16 裁决）**：独立进程组建立与进程组号探测全部走
`公共契约/运行时/进程终止.py` —— **本文件调用点不含任何平台判断**（无 `os.name` /
`sys.platform` / `os.killpg` / `os.getpgrp`；**多进程启动方式也由
`平台适配.多进程启动上下文()` 收口，真正的 fork / spawn 分支选择在宿主
`任务进程.py` 的 `任务进程池.__init__` 接「上下文 + 是否需序列化」两个事实**）。
`os.setsid` 出现在 `_执行任务进程入口`，前置条件由收口层 `进程组号(本进程pid)`
回答「本平台有无进程组概念」（非 POSIX 平台如实回 `None`，那时不建立、也绝不谎报就绪）。

**对外零变化（2026-09-19 拆分）**：本文件全部内容逐字取自冻结基线
`/tmp/拆分基线/任务进程.py`（1113 行，sha256 前16 = 3227f281a4400958，对应提交
1dc0f0f6），成员名、签名、常量值一个不改；旧路径 `运行核心.任务调度.任务进程`
把这些名字**原样再导出**（已由冻结基线现场核对）。

**判据唯一事实源**：`独立任务` 的身份语义（`__eq__`/`__hash__` 绑解释器自带对象身份）
的机器判据在 `测试中心/运行核心/测试_任务身份判据.py` —— 该文件逐字保留本类语义，
改动即变红。
"""

from __future__ import annotations

import json
import multiprocessing
import pickle
import os
import time
import uuid
from collections.abc import Callable
from typing import Any

from 公共契约.运行时 import 进程终止
from 运行核心.任务调度.任务工作器 import 执行单次任务
from 公共契约.基础类型.逻辑类型 import 真, 假


任务状态_等待中 = "等待中"
任务状态_运行中 = "运行中"
任务状态_成功 = "成功"
任务状态_失败 = "失败"
任务状态_取消中 = "取消中"
任务状态_已取消 = "已取消"
任务状态_超时 = "超时"
任务状态_崩溃 = "崩溃"

_终态 = {任务状态_成功, 任务状态_失败, 任务状态_已取消, 任务状态_超时, 任务状态_崩溃}

#: 收口层 进程终止 的信号语义名（POSIX → SIGTERM/SIGKILL；Windows → taskkill 不带/带 /F）
信号_终止 = "终止"
信号_强杀 = "强杀"


def _本进程组号() -> int | None:
    """本进程所属进程组号；平台无进程组概念时由收口层如实回 ``None``。

    只问能力、不写平台判断：这是「不要打自己」安全闸与「本平台有无进程组」判定的唯一来源。
    """
    return 进程终止.进程组号(os.getpid())

class 资源繁忙错误(Exception):
    """提交任务超过进程池有界容量时的统一资源繁忙错误，区分"忙"与"失败"。"""

    def __init__(self, 消息: str = "") -> None:
        super().__init__(消息 or "任务进程池资源繁忙，拒绝提交新任务")
def _还原执行器(执行器: Any) -> Callable:
    """还原执行器句柄：`spawn` 送达的是字节（显式序列化），`fork` 直接是函数对象。"""
    if isinstance(执行器, (bytes, bytearray)):
        return pickle.loads(bytes(执行器))
    return 执行器
def _执行任务进程入口(进程组就绪事件: Any, 发送连接: Any, 执行器: Any,
                    请求: dict[str, Any], 取消事件: Any) -> None:
    """先建立独立会话/进程组，再允许能力启动任何后代进程。

    「本平台是否具备独立进程组」由收口层判定（`进程终止.进程组号(本进程pid)`，非 POSIX
    平台如实回 `None`）：**不具备时不建立、也绝不谎报就绪** —— 该平台上「整棵回收」由
    收口层的进程树终止（Windows `taskkill /T`）承担，`_发送工作器信号` 走同一条收口；
    具备时按 POSIX 语义 `os.setsid()` 建独立进程组，建立失败（`OSError`）即明确报
    「进程组建立失败」并拒绝执行 —— 绝不静默跳过，那会让取消/超时路径无法整组回收。
    """
    函数 = _还原执行器(执行器)
    if 进程终止.进程组号(os.getpid()) is not None:
        try:
            os.setsid()
        except OSError as 错误:
            响应 = {
                "任务id": 请求.get("任务id", ""), "成功": 假,
                "错误码": "进程组建立失败", "错误说明": str(错误),
            }
            try:
                发送连接.send_bytes(json.dumps(响应, ensure_ascii=False).encode("utf-8"))
            finally:
                发送连接.close()
            return
        进程组就绪事件.set()
    执行单次任务(发送连接, 函数, 请求, 取消事件)
class 独立任务:
    """一个独立任务的状态载体；**判身份一律按对象身份（is），不许改成按字段值比较**。

    **为什么必须身份比较**：同一条逻辑任务在系统里会同时存在**多个对象** —— 至少有
    「池内存里的那个对象」和「`查询` 从磁盘快照重建的那个对象」（`查询` 读 json 后
    新建 `独立任务` 再逐键 `setattr`）。两者的 `任务id` / `能力id` 等字段**逐字相同**。
    而池里两处判定问的都是「**是不是我**」，不是「有没有一个和我长得一样的」：

    - `提交`：`if 任务对象 not in self.等待队列` —— 判断「我这个对象入队了没」；
    - `_退出排队`：`if 任务对象 not in self.等待队列` / `self.等待队列.remove(任务对象)`
      —— 归还**自己**占的那一个排队槽。

    一旦本类变成按字段比较（谁给它叠一个 `@数据类(相等=真)` / `@dataclass(eq=True)`，
    或手写按字段比较的 `__eq__`），`in` / `remove` 会命中**快照重建的孪生对象**：

    - `提交` 误判「我已经在队列里」而跳过入队 —— 排队容量记账与实际脱节；
    - `_退出排队` 把**别人的槽**移除（或该归还的槽归还不掉）—— 死条目永久占槽，后续
      正常提交被误判成「资源繁忙」，且没有任何调用方能再清理它。

    两条都是**静默失效**：不抛异常、不打日志、任务照样跑，只是账本悄悄错。更糟的是
    本类**没有类级字段注解**，`@dataclass(eq=True)` 生成的 `__eq__` 比较的是空元组
    —— **任意两个 `独立任务` 实例都判为相等**（已实测），失效范围会拉满。

    所以身份语义在这里**显式声明**，不再依赖「手写类恰好没定义 `__eq__`」这个隐式默认：
    `__eq__` / `__hash__` 在最底层绑定到解释器自带的身份比较，本类**不自定义任何比较
    逻辑**（哲学 1.2：同一件事不留两套 —— 身份比较的实现只有解释器那一份）。

    这两行兼作**护盾**：`dataclasses` 对**类体里已定义**的 `__eq__` 一律不覆盖，因此
    今后谁在类上叠一个 `@数据类(相等=真)` 也改不动本类的比较语义（已实测核对）。

    机器守着的判据见 `测试中心/运行核心/测试_任务身份判据.py`：比较语义一旦变成按值，
    该文件的断言立刻变红。
    """

    #: 身份语义的唯一实现来源（解释器自带的对象身份比较），不在此重写比较逻辑。
    #: 必须写在**类体**（不能写在 `__init__` 里）：`dataclasses` 只认类体里已有的
    #: `__eq__`，写在类体才能同时起到「显式声明」与「挡住 @数据类(相等=真)」两个作用。
    __eq__ = object.__eq__
    __hash__ = object.__hash__

    def __init__(self, *, 任务id: str = "", 能力id: str = "", 项目id: str = "",
                 用户id: str = "", 请求id: str = "", 超时秒: float = 10.0) -> None:
        self.任务id = 任务id or uuid.uuid4().hex[:16]
        self.能力id = 能力id
        self.项目id = 项目id
        self.用户id = 用户id
        self.请求id = 请求id
        self.超时秒 = max(0.01, float(超时秒))
        self.状态 = 任务状态_等待中
        self.进度 = 0.0
        self.结果: Any = None
        self.错误码 = ""
        self.错误说明 = ""
        self.日志: list[str] = []
        self.进程: multiprocessing.Process | None = None
        self.进程组id: int | None = None
        self.进程组就绪事件: Any = None
        self.接收连接: Any = None
        self.取消事件: Any = None
        self.待发布状态 = ""
        self.待发布错误码 = ""
        self.待发布错误说明 = ""
        #: 结果连接是否已被读取过（单读者仲裁，B-07）：每任务最多收取一次结果，
        #: 不可能出现两个读者对同一连接并发 poll→recv。
        self.结果已收取 = 假
        #: 回收者（持回收权的线程名）：非空 = 有线程正在**锁外**做终止/join。
        #: 池锁在锁外回收期间是放开的，这张「回收权」标签就是并发闸：
        #: 其他线程看到非空即不介入（不重复发信号、不并发动同一连接、不重复发布终态）。
        #: P1-5 之前没有它 —— 因为整段回收都在池锁里，锁本身就是互斥；把慢 I/O
        #: 移出锁外，就必须把它显式表达出来（句柄体系「回收中」标记的同形做法）。
        self.回收者 = ""
        #: 本会话是否真实起过工作进程（B-08）：从磁盘快照重建的对象恒为 False，
        #: 它没有状态话语权，不能把重启收敛结果覆盖回「运行中」。
        self.本会话活动 = 假
        self.截止时刻: float = 0.0
        self.创建时间 = time.strftime("%Y-%m-%d %H:%M:%S")
        self.完成时间 = ""

    def 转字典(self) -> dict[str, Any]:
        return {
            "任务id": self.任务id, "能力id": self.能力id, "状态": self.状态,
            "进度": self.进度, "结果": self.结果, "错误码": self.错误码,
            "错误说明": self.错误说明, "日志": list(self.日志),
            "项目id": self.项目id, "用户id": self.用户id, "请求id": self.请求id,
            "创建时间": self.创建时间, "完成时间": self.完成时间,
        }

