"""进程调用结果：一次进程调用的统一结果信封（不泄漏原生对象的唯一类型）。

**为什么独立成文件**：`进程调用结果` 被本包六处共用 —— 主文件的两个调用入口
（`独立进程.调用` / `提供者进程池.调用`）与三个混入面，且**被跨包消费**
（`运行核心/运行环境管理器/提供者生命周期.py` 模块级导入它并原样返回，
`测试中心/运行核心/测试_硬件画像与档位.py` 也按它取数）。放成
**只依赖标准库的无环叶子**，任何一面都不必为了拿到结果类型去导入主文件
（那会形成「主文件 → 子模块 → 主文件」的环）。

**对外零变化（2026-09-19 拆分）**：正文逐字取自冻结基线
`/tmp/拆分基线/独立进程.py`（1044 行，sha256 前16 = 963513edb4dd3228）；
字段名、类型、默认值（含 `来源="独立进程"`）与 `转字典()` 的返回键一字未改；
主文件经「导入即再导出」拿到**同一对象**
（`独立进程.进程调用结果 is 独立进程_调用面.进程调用结果`）。
"""

from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any

from 公共契约.基础类型.逻辑类型 import 假


@dataclass
class 进程调用结果:
    """一次进程调用的统一结果（不泄漏原生对象）。"""

    成功: bool
    值: Any = None
    错误码: str = ""
    错误说明: str = ""
    来源: str = "独立进程"
    可重试: bool = 假
    详细信息: dict[str, Any] = field(default_factory=dict)

    def 转字典(self) -> dict[str, Any]:
        return {
            "成功": self.成功, "值": self.值, "错误码": self.错误码,
            "错误说明": self.错误说明, "来源": self.来源,
            "可重试": self.可重试, "详细信息": self.详细信息,
        }
