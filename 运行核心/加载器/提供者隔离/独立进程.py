"""真实独立进程：subprocess 运行的工作进程管理。

支持：启动（含启动超时）、标准输入输出通信、请求id、调用超时、取消、
健康检查、崩溃检测、自动重启（次数限制）、资源状态记录、优雅停止、
强制终止、进程退出码记录、日志隔离。

核心只能通过统一中文协议调用提供者，不得泄漏原生进程对象。

**跨平台收口（华哥 2026-09-16 裁决：底座做完整跨平台）**：子进程组启动标志、进程组存活
判定、组信号发送全部走 `公共契约/运行时/平台适配/` 与 `公共契约/运行时/进程终止.py`
—— **本文件调用点不含任何平台判断**（无 `os.name` / `sys.platform` / `os.killpg` /
`os.getpgrp`）。**「进程组归属」与「隔离回收」语义不变**：组长被 `poll()` 回收后，
「同组子孙仍在」只有收口层的按组号原语（`按组号探活` / `按组号终止`）能表达。

**管道读取同样不做平台判断**：stdout 由后台线程阻塞式 `os.read` 直读内核并用
`公共契约/运行时/有界IO.受限读取` 排空+限界，调用方在条件变量上等一个完整行
—— 不再用 `select` 轮询管道 fd（Windows 的 `select` 只接受 socket，轮询管道必然报错）。

**stdout 只有一个读者（唯一所有权）**：管道 fd 归本文件的后台读线程独有，
上层（如 `运行核心/运行环境管理器/提供者生命周期.py`）**永不直接碰管道 fd**：
要丢掉滞留响应行一律走 `弃置滞留行()` —— 它只在同一把交接锁下从后台线程维护的
`_读取缓冲` 里非阻塞取/弃完整行。旧实现让上层 `select + readline` 直读同一个
管道，两个读者分食同一字节流（竞态：要么上层偷走后台上游字节，要么
`select` 报可读但只有半行时 `readline` 无超时包裹地永久阻塞）。

**跨重启世代账本（常驻提供者进程）**：常驻工作进程用 `子进程组启动标志()`
（POSIX `setsid`）脱离网关会话/进程组，网关一退出就切断 OS 的「父死子随」兜底
—— 工作进程及模型子孙会被 launchd 收养、继续占显存与端口，而新网关内存里没有
上一代 PID/pgid，定位不到也回收不掉。故启动成功后把
`{网关世代id, 网关进程id, 提供者id, 组长进程id, 进程组号, 端口, 资源键, 启动时间}`
原子落进底座运行库（`常驻提供者进程` 表，本文件就地 `CREATE TABLE IF NOT EXISTS`，
WAL + `synchronous=FULL`），整组确认收敛后销行；新网关启动时调一次
`清扫上一代常驻进程()` 把上一代遗留整组回收。
判据是**归属 + 世代**（记录里的 `网关进程id` 已消失才算孤儿），**不按端口号盲杀**
—— embedding/rerank 这类合法长驻服务的父进程同样是 1，按端口杀会误杀。


**拆分说明（2026-09-19，对外零变化）**：原 1044 行单文件按职责簇拆成 6 个同包模块，
成员名、签名、默认值、模块级常量值与再导出面**一个不改**（对照冻结基线
`/tmp/拆分基线/独立进程.py`，1044 行，sha256 前16 = 963513edb4dd3228，
对应 HEAD 干净工作区）：

- `独立进程_对象.py`：五个进程状态常量 / `日志上限` / `stderr日志上限` /
  `最大允许池大小` / `响应行上限字节` / `读取块大小`（**叶子模块，常量唯一定义处**：
  新会话只读它就能拿到本子系统全部标定常量）；
- `独立进程_调用面.py`：`进程调用结果`（**叶子模块**：被本包六处与跨包
  `提供者生命周期.py` 共用，放叶子使任何面都不必为拿结果类型去导入主文件）；
- `独立进程_账本.py`：`_定位系统根` / `当前网关世代id` / `账本库路径` /
  `读取常驻进程账本` / `清扫上一代常驻进程` + 账本常量（跨重启世代账本唯一定义处，
  身份计数器 `_网关世代id` 与 sqlite 连接对象的唯一赋值处都在这里）；
- `独立进程_管道读取面.py`：`_描述符读取器` / `_启动stderr消费` / `_启动stdout消费` /
  `_收块` / `_消费stderr` / `_消费stdout` / `_读取一行` / `弃置滞留行` / `_关闭管道`
  （**stdout 唯一读者**与滞留行排空口径的实现所在：行读取与排空共用同一把交接锁
  `_读取条件` 与同一个 `_读取缓冲`，放同一文件才有一个读者视角）；
- `独立进程_启动调用面.py`：`_确定解释器` / `_记录日志` / `启动` / `_发送请求` /
  `调用` / `健康检查` / `崩溃检测` / `_登记世代账本` / `_注销世代账本`；
- `独立进程_进程组收口面.py`：`信号_终止` / `信号_强杀` / `_已发出信号` /
  `_进程组存活` / `_等待进程组退出` / `_发进程组信号` / `_核对资源收敛` / `_关闭结果` /
  `关闭` / `重试关闭` / `优雅停止` / `强制终止` / `停止` / `关闭并清理`
  （调用点不含任何平台判断）。

`独立进程` 改为 `(启动调用面, 进程组收口面, 管道读取面)` 三个混入类的子类，
`self.x` 照旧解析 —— 子类 / 调用方 / `mock.patch` 都无感；`__init__`（目录口径、
解释器来源、账本字段、读缓冲与通信锁的**唯一落点**）、`账本错误` 留痕字段、
`提供者进程池`、`进程管理器` 仍只在本文件定义。混入类的宿主依赖写成**类注解**
（契约，宿主缺哪个属性当场 `AttributeError`），兄弟混入类的协作方法只作类型声明、
**不写同名占位方法**（占位方法会按 MRO 抢先遮蔽真实实现）。

**外部只依赖旧直连路径的读者不受影响（已现场核对）**：
`测试中心/运行核心/测试_硬件画像与档位.py` 用 AST 从本文件读
`提供者进程池.__init__` 的 `池大小` / `最大资源键数` 默认值与模块级常量
`最大允许池大小`（后两者本就在本文件，前者经再导出仍在同名字面量处），
且本文件的 `from ..._对象 import` 清单补齐了 `日志上限` / `stderr日志上限` /
`响应行上限字节` / `读取块大小` / `进程状态_*`（即便本文件不再直接使用，
也保留再导出面）。

**import 清单按各模块实际使用重新分配**（属实现细节，公开面不比对 import）；
被搬家模块级的名字全部**导入即再导出**（`独立进程.进程状态_运行中 is
独立进程_对象.进程状态_运行中` 等，同一对象，不是拷贝）。
"""

from __future__ import annotations

import subprocess
import threading
import time
from collections import OrderedDict, deque
from pathlib import Path
from typing import Any

from 公共契约.基础类型.逻辑类型 import 真, 假
from 公共契约.运行时.资源键表 import 取成员索引
from 公共契约.版本规则.契约版本 import 取契约版本
from 运行核心.加载器.提供者隔离.独立进程_对象 import (
    进程状态_已创建, 进程状态_启动中, 进程状态_运行中, 进程状态_已停止, 进程状态_故障,
    日志上限, 响应行上限字节, 读取块大小)
# ↓ 这两个常量的**唯一定义处就在本文件**（故意不搬家）：
#   ① 唯一使用点也在本文件（`独立进程.__init__` 的 stderr 环形队列 /
#      `提供者进程池.__init__` 的池大小校验）；
#   ② `测试中心/运行核心/测试_硬件画像与档位.py::_读源码常量` 按源码 AST 直接从**本文件**
#      读 `最大允许池大小` 的字面量（「现值对账的唯一取数口径」）—— 搬走它会让该测试读不到，
#      而在本文件再写一份副本又等于同一件事留两套（公开面也会多出一处字面量）。
#   故按「留一处定义、留一个读者口径」处理，取值与拆分前逐字相同。
stderr日志上限 = 200
最大允许池大小 = 16

from 运行核心.加载器.提供者隔离.独立进程_调用面 import 进程调用结果
from 运行核心.加载器.提供者隔离.独立进程_账本 import (
    常驻进程账本表名, 账本库环境变量, 账本连接超时秒, 账本列, 清扫等待秒,
    当前网关世代id, 账本库路径, 读取常驻进程账本, 清扫上一代常驻进程,
    _定位系统根, _登记常驻进程, _注销常驻进程)
from 运行核心.加载器.提供者隔离.独立进程_管道读取面 import (
    排空窗口秒, 排空预算秒, 排空预算字节, _描述符读取器, 管道读取面)
# 上面两处提示：`排空窗口秒/排空预算秒/排空预算字节` 与 `_描述符读取器` 的
# **唯一定义处**是 `独立进程_管道读取面.py`，本文件这行是**再导出**（同一对象，不是拷贝）。
from 运行核心.加载器.提供者隔离.独立进程_启动调用面 import 启动调用面
from 运行核心.加载器.提供者隔离.独立进程_进程组收口面 import (
    信号_终止, 信号_强杀, _已发出信号, 进程组收口面)

# 上面几张 import 清单就是**再导出面**：`独立进程.当前网关世代id` /
# `独立进程.最大允许池大小` / `独立进程.进程调用结果` 等与各自新模块里的是
# **同一个对象**（导入即绑定，非拷贝），与拆分前「模块内只有一处定义」一致。
# 口径（split-oversized-module-by-mixin 技能）：import 差异属实现细节，
# import 清单按**各模块实际使用**重新分配（已用作用域感知检查器逐个核对，见回传）。


class 独立进程(启动调用面, 进程组收口面, 管道读取面):
    """真实独立工作进程（subprocess + JSON 行协议）。

    三簇能力已搬到混入面（启动调用面 / 进程组收口面 / 管道读取面），`self.x` 照旧解析。
    本类只留 `__init__`：目录口径、解释器来源、账本字段、读缓冲与通信锁的唯一落点。

    ⚠ 不要在本类**重写** `账本错误` / `状态` / `进程` / `关闭账本` 等字段的类型注解 ——
    混入面的类注解即契约，重写会让「宿主缺属性」当场变静默。
    """

    # ---- 宿主契约（本类 `__init__` 是唯一赋值处，混入面只声明类型不赋值） ----
    名称: str
    工作器路径: Path
    启动超时秒: float
    调用超时秒: float
    最大重启次数: int
    解释器路径: str | None
    提供者目录: Path | None
    端口: int | None
    资源键: str
    账本错误: str
    状态: str
    进程: subprocess.Popen | None
    重启次数: int
    退出码: int | None
    日志列表: list[str]
    stderr日志: deque
    关闭账本: list[dict[str, Any]]

    def __init__(self, 名称: str, *, 工作器路径: Path | None = None,
                 启动超时秒: float = 5.0, 调用超时秒: float = 3.0,
                 最大重启次数: int = 3, 解释器路径: str | None = None,
                 提供者目录: Path | None = None,
                 端口: int | None = None, 资源键: str = "") -> None:
        self.名称 = 名称
        self.工作器路径 = 工作器路径 or Path(__file__).resolve().parent / "进程工作器.py"
        self.启动超时秒 = 启动超时秒
        self.调用超时秒 = 调用超时秒
        self.最大重启次数 = 最大重启次数
        self.解释器路径 = 解释器路径
        self.提供者目录 = 提供者目录
        # 世代账本的诊断字段：端口/资源键只如实记录调用方声明的事实，
        # **不参与判活与回收**（按端口盲杀会误伤 embedding/rerank 等合法长驻服务）。
        self.端口 = 端口
        self.资源键 = 资源键 or 名称
        self.账本错误 = ""  # 落账/销账失败留痕（可见，不静默）
        self.状态 = 进程状态_已创建
        self.进程: subprocess.Popen | None = None
        self.重启次数 = 0
        self.退出码: int | None = None
        self.日志列表: list[str] = []
        self.stderr日志 = deque(maxlen=stderr日志上限)
        self._stderr线程: threading.Thread | None = None
        self.关闭账本: list[dict[str, Any]] = []
        self._读取缓冲 = b""  # 后台读线程交接的行缓冲（os.read 直读内核，无预读）
        # 后台 stdout 读线程 ↔ 调用方的交接（条件变量：不用 sleep 轮询，也不用 select）
        self._读取条件 = threading.Condition()
        self._读取结束 = 假  # 后台读线程已 EOF/出错
        self._读取超限 = 假  # 单行超过 响应行上限字节
        self._读取序号 = 0  # 交接序号：每收到一块 +1（供「窗口内无新数据」判定，不靠 sleep 轮询）
        self._stdout线程: threading.Thread | None = None
        # JSON 行协议是一问一答；同一 Provider 进程不能让多个线程交叉
        # 写 stdin/读 stdout，否则迟到响应会被下一请求消费。
        self._通信锁 = threading.RLock()


class 提供者进程池:
    """有界 Provider 进程池；资源键稳定绑定成员，每个成员独立管道和通信锁。"""

    def __init__(self, 名称: str, *, 池大小: int = 2, 最大资源键数: int = 4096,
                 工作器路径: Path | None = None, 启动超时秒: float = 5.0,
                 调用超时秒: float = 3.0, 最大重启次数: int = 3,
                 解释器路径: str | None = None, 提供者目录: Path | None = None) -> None:
        if not 1 <= int(池大小) <= 最大允许池大小:
            raise ValueError(f"池大小必须在 1..{最大允许池大小} 之间")
        if 最大资源键数 < 池大小:
            raise ValueError("最大资源键数不得小于池大小")
        self.名称 = 名称
        self.池大小 = int(池大小)
        self.最大资源键数 = int(最大资源键数)
        self.成员表 = [独立进程(
            f"{名称}#{序号 + 1}", 工作器路径=工作器路径,
            启动超时秒=启动超时秒, 调用超时秒=调用超时秒,
            最大重启次数=最大重启次数, 解释器路径=解释器路径,
            提供者目录=提供者目录) for 序号 in range(self.池大小)]
        self.资源分配: OrderedDict[str, int] = OrderedDict()
        self._分配锁 = threading.Lock()
        self.关闭账本: list[dict[str, Any]] = []
        self.运行状态 = 进程状态_已创建

    def 启动(self) -> tuple[bool, str]:
        已启动: list[独立进程] = []
        for 成员 in self.成员表:
            成功, 消息 = 成员.启动()
            if not 成功:
                for 已有成员 in 已启动:
                    已有成员.关闭并清理()
                self.运行状态 = 进程状态_故障
                return 假, f"成员 {成员.名称} 启动失败: {消息}"
            已启动.append(成员)
        self.运行状态 = 进程状态_运行中
        return 真, f"Provider进程池启动成功（{self.池大小} 个成员）"

    def _分配成员(self, 资源键: str) -> 独立进程 | None:
        """按资源键分配池成员（**有界 LRU**：满表淘汰最久未用，永不拒绝新键）。

        语义与「键 → 成员索引」判据**全部收口在 `公共契约/运行时/资源键表.py`**
        （同一缺陷曾在本仓出现三次，两份同构实现即哲学 1.2 缺陷）：
        命中刷新最近使用、未命中先淘汰最久未用再建立新行、被淘汰键重进按同一
        sha256 绑回同一成员。本方法只负责「持锁 + 取索引 + 查成员表」这三件事。

        `hashlib` 不再在本文件使用（判据已下移）；保留 import 会留下「本文件自己
        算过哈希」的误导线索，故同批移除。
        """
        with self._分配锁:
            索引 = 取成员索引(self.资源分配, self._分配锁, 资源键,
                           池大小=self.池大小, 最大资源键数=self.最大资源键数)
            return self.成员表[索引]

    def 调用(self, *, 能力id: str, 参数: dict | None = None,
             契约版本: str | None = None, 资源键: str | None = None) -> 进程调用结果:
        契约版本 = 契约版本 or 取契约版本()
        键 = str(资源键 or 能力id)
        成员 = self._分配成员(键)
        if 成员 is None:
            return 进程调用结果(假, 错误码="资源繁忙",
                              错误说明="Provider资源键表已满", 可重试=真)
        return 成员.调用(能力id=能力id, 参数=参数, 契约版本=契约版本)

    def 健康检查(self) -> bool:
        return bool(self.成员表) and all(成员.健康检查() for 成员 in self.成员表)

    def _核对资源收敛(self) -> list[str]:
        未收敛: list[str] = []
        for 序号, 成员 in enumerate(self.成员表):
            未收敛.extend(f"成员{序号 + 1}:{问题}" for 问题 in 成员._核对资源收敛())
        return 未收敛

    def 关闭(self) -> dict[str, Any]:
        成员结果 = [成员.关闭并清理() for 成员 in self.成员表]
        未收敛 = self._核对资源收敛()
        成功 = not 未收敛 and all(结果["成功"] for 结果 in 成员结果)
        if not 成功 and not 未收敛:
            未收敛 = [结果["错误说明"] for 结果 in 成员结果 if not 结果["成功"]]
        self.运行状态 = 进程状态_已停止 if 成功 else 进程状态_故障
        结果 = {
            "成功": 成功, "错误码": "" if 成功 else "资源未收敛",
            "错误说明": "Provider进程池资源已收敛" if 成功 else f"Provider进程池资源未收敛: {'；'.join(未收敛)}",
            "可重试": not 成功, "未收敛": 未收敛,
            "已使用SIGKILL": any(项.get("已使用SIGKILL", 假) for 项 in 成员结果),
            "成员结果": 成员结果,
        }
        self.关闭账本.append(dict(结果, 时间=time.time()))
        return 结果

    def 重试关闭(self) -> dict[str, Any]:
        return self.关闭()

    def 状态(self) -> dict[str, Any]:
        成员状态 = [{
            "名称": 成员.名称,
            "状态": 成员.状态,
            "pid": 成员.进程.pid if 成员.进程 is not None else None,
            "stderr日志": list(成员.stderr日志),
            "退出码": 成员.退出码,
        } for 成员 in self.成员表]
        return {
            "状态": self.运行状态, "池大小": self.池大小,
            "pid表": [项["pid"] for 项 in 成员状态 if 项["pid"] is not None],
            "成员表": 成员状态, "资源分配": dict(self.资源分配),
            "关闭账本": list(self.关闭账本),
        }


class 进程管理器:
    """进程管理器（第五阶段兼容接口）：创建、监控、重启、隔离。"""

    def __init__(self) -> None:
        self.进程表: dict[str, 独立进程] = {}
        self.日志表: list[str] = []

    def 创建进程(self, 名称: str, *, 版本: str = "") -> 独立进程:
        进程 = 独立进程(名称)
        进程.版本号 = 版本
        self.进程表[名称] = 进程
        return 进程

    def 启动并检查(self, 进程: 独立进程) -> tuple[bool, str]:
        成功, 消息 = 进程.启动()
        if not 成功:
            return 假, f"启动失败: {消息}"
        if not 进程.健康检查():
            return 假, "健康检查失败（已触发崩溃重启）"
        return 真, f"运行正常（版本 {getattr(进程, '版本号', '未知')}）"

    def 记录日志(self, 进程: 独立进程, 消息: str) -> None:
        self.日志表.append(f"[{进程.名称}:{getattr(进程, '进程', None).pid if 进程.进程 else '无pid'}] {消息}")

    def 全部健康(self) -> bool:
        return all(进程.状态 == "运行中" for 进程 in self.进程表.values())

    def 停止全部(self, *, 优雅: bool = 真) -> list[str]:
        结果列表 = []
        for 进程 in self.进程表.values():
            if 进程.状态 == "运行中":
                if 优雅:
                    结果, 消息 = 进程.优雅停止()
                else:
                    结果, 消息 = 进程.强制终止()
                结果列表.append(f"{进程.名称}: {消息}")
        return 结果列表
