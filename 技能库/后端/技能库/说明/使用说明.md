# 技能库（技能库.后端.技能库）
<!-- 机器管理｜类型：仓库内其他文档｜生成器：开发工具.MD文档生成 -->


- 类型：支持库
- 版本：1.1.2
- 说明：技能库
- 入口：__init__.py
- 权威分工：**调用口径以本文为准**（能力/参数/返回/错误码/调用示例，由能力契约生成）；
  该库的设计哲学与边界见同目录 `设计说明.md`（设计权威）。

## 能力
### 校验技能包（`技能库.技能包.校验技能包`）
- 版本：1.0.0
- 一句话说明：校验技能包六件套结构（SKILL.md/契约.json/工作流.json/依赖.json/验证声明.json/scripts）是否齐全，返回 {通过, 技能目录, 来源, 缺失}
- 所属支持库或模块：技能库.后端.技能库
- 参数列表：技能根目录:文本型, 技能标识或路径:文本型
- 必填参数：技能根目录, 技能标识或路径
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在, 技能未找到
- 调用示例：技能库.技能包.校验技能包()
- 依赖能力：无
### 运行技能包（`技能库.受控执行.运行技能包`）
- 版本：1.1.1
- 一句话说明：受控执行技能包：结构校验→定位入口→AST 导入审计→沙箱运行（进程组回收/超时/输出上限/环境白名单/资源预算）；可调能力白名单非空时注入合成模块 技能底座能力，技能脚本经唯一网关 HTTP 调用白名单内的底座能力（白名单外拒绝）；权限等级（可选，整数型）由调用方自选：1=纯标准库（即使给了白名单也不注入网关凭证）、2=白名单能力（要求白名单非空，否则明确报 参数不合法）、不传=自动（白名单非空为 2，否则 1，与历史行为一致）。**口径提示（哲学第 9 条 5 项「相对安全」）**：本机制是可选的收紧手段，**不是绝对安全边界**——技能脚本与平台同机同用户，持凭证者仍可绕过静态导入审计；需要强边界时应改用独立进程/系统沙箱，不要依赖本机制。
- 所属支持库或模块：技能库.后端.技能库
- 参数列表：技能根目录:文本型, 能力标识:文本型, 参数:字典型, 超时秒:双精度数型, 输出上限:整数型, 环境变量:字典型, 禁止导入前缀:列表型, 项目根目录:文本型, 可调能力白名单:列表型, 权限等级:整数型
- 必填参数：技能根目录, 能力标识
- 可选参数：参数, 超时秒, 输出上限, 环境变量, 禁止导入前缀, 项目根目录, 可调能力白名单, 权限等级
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在, 技能未找到, 结构不完整, 工作流无效, 缺少入口脚本, 脚本路径逃逸, 脚本读取失败, 脚本审计未通过, 非法环境变量, 启动失败, 超时, 输出超限, 脚本非零退出, 输出格式错误, 脚本执行失败, 能力调用凭证缺失
- 调用示例：技能库.受控执行.运行技能包(超时秒=30, 输出上限=1048576)
- 依赖能力：无
### 扫描技能包（`技能库.技能索引.扫描技能包`）
- 版本：1.0.0
- 一句话说明：扫描技能索引登记的技能包，返回 {数量, 技能列表}，逐项标注结构是否完整
- 所属支持库或模块：技能库.后端.技能库
- 参数列表：技能根目录:文本型
- 必填参数：技能根目录
- 可选参数：无
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在
- 调用示例：技能库.技能索引.扫描技能包()
- 依赖能力：无
### 生成索引（`技能库.技能索引.生成索引`）
- 版本：1.0.0
- 一句话说明：扫描技能根目录下全部技能包，从各自 契约.json 派生索引条目，并可选写回 索引.json（参数事实唯一源仍是契约）
- 所属支持库或模块：技能库.后端.技能库
- 参数列表：技能根目录:文本型, 写回索引:逻辑型
- 必填参数：技能根目录
- 可选参数：写回索引
- 返回结构：结果型（统一结果：成功/值/错误码/错误说明）
- 错误码：参数不合法, 目录不存在
- 调用示例：技能库.技能索引.生成索引(写回索引=True)
- 依赖能力：无
